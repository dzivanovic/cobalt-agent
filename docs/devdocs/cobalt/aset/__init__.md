# `src/cobalt/aset/__init__.py`

## What it does
Package marker for the ASET semi-auto sizing sheet (pre-beta slice 1).
Docstring states the package's one hard rule: grade and stops are
ALWAYS Dejan's input; every other module in this package only does math
and prefill, never guesses a risk decision.

## Key functions/classes
None.

## Data flow in/out
None.

## Config it reads
None directly — see `config.py` for the package's actual config schema.
