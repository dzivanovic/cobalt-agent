# Cobalt Assessment — Pass 4: Scanners and trading logic

Date: 2026-08-21 · Baseline: `5ae7b3a` (main) · Assessor: Claude Fable 5 · Mode: read-only toward the system (code read; pure/mocked tests run; one local, network-free Python invocation of `Playbook`; live DB via `SELECT` only; **no DB writes, no Finviz/yfinance/LLM calls**)

Scope: `brain/tactical.py` (60), `brain/playbook.py` (113), `brain/strategy.py` (49), `brain/strategies/second_day_play.py` (104), `tools/finance.py` (262), `configs/rules.yaml` (27), `configs/strategies.yaml` (82), the strategy/rule Pydantic models in `config.py:26-206,597-606`, `skills/research/{semantic_tagger,scanner_orchestrator,enrich_metadata}.py`, `skills/productivity/briefing.py` (115), `configs/prompts.yaml` (scheduler/research), `brain/cortex.py:25-52,189-224`, the `daily_in_play` gap, and `tests/test_strategies.py`.

Verdict legend: **RETAIN** · **BROKEN-FRICTION** · **KILL-candidate** (proposal only — Dejan decides). **UNVERIFIED** = inferred, not read/run. ORIGINAL INTENT is recorded per component (§6) from tracked sources only (commit log; PRD-001/System Manifest/ADR text in `dev_utils/`; docstrings; `COBALT-REQUIREMENTS.md`); vault ADRs not read; `cobalt_master_context.txt` absent.

---

## 0. Headline findings

| # | Finding | Evidence |
|---|---|---|
| **H-1** | **None of the four priority setups (Scalp Radar 5+9 EMA continuation, VWAP Continuation, Bouncy Ball, Big Dog) is encoded anywhere.** No EMA computation exists in the codebase (grep `ema` → only two YAML strings); no intraday VWAP (only anchored *daily* VWAPs in `finance.py:112-117,176-197`); no intraday bar source at all (`FinanceTool` = yfinance **daily** history, `finance.py:143`). The nearest artefacts are two **inactive, class-less** YAML stubs: `fashionably_late_scalp` (`trend_indicator: "9EMA"`, `baseline_indicator: "VWAP"`, `entry_trigger: "cross_9ema_vwap"`, `strategies.yaml:53-67`) and `second_chance_scalp` (break/retest, `:69-83`). "Bouncy Ball" and "Big Dog" appear nowhere. | grep; files cited |
| **H-2** | **No ASET grading, expected-value, or position-sizing logic exists.** Grep for grade/EV/sizing/R-multiple/daily-stop/regime across `src`, `configs`, `dev_utils`, `tests` yields: a prompt directive ("grade the risk (A, A-, B, B-, C) and dynamically calculate EV", `config.yaml:109`), routing hints ("Calculate EV for X" → TACTICAL, `prompts.yaml:145-146,175,199`), empty schema columns (`system_alerts.success_grade`, `r_multiple`, `strategy_signals.ai_logic_payload -- Exact EV math`, `trades.r_multiple`, `schema.sql:117,120,132,182`), and PRD-001's HUD formula `Score = Base + Fuel − Friction − Decay` (`create_prd.py:73-78`). The only "score" in code is `SecondDayPlay`'s 0–100 points (`second_day_play.py:62-76`), which is a setup-quality score, not risk-as-%-of-daily-stop, has no EV, no win-rate/regime input, no size output (it returns `risk_per_share`, `:89`, nothing about shares/$). Oura readiness, daily-stop limit, regime conditioning: absent. | grep; file:line |
| **H-3** | **The one implemented strategy cannot run.** `Playbook.run_all` calls `strategy.analyze(market_data)` (`playbook.py:86`) but `SecondDayPlay.analyze(self, ticker, market_data)` takes two args (`second_day_play.py:22`) → `TypeError` on every call (**confirmed locally**: "Error running second_day_play: … missing 1 required positional argument"). Even with the arity fixed: the class reads `config["parameters"]` (`:16`) while `strategies.yaml` has `filters` → `params = {}` (confirmed) so `min_rvol_day1: 1.5`, `max_gap`, `min_atr`, `liquidity`, `correlation`, `execution.*` are **never used**; and it expects keys `yesterday_close / yesterday_volume / average_volume / today_open / pre_market_high` (`:28-32`) that `FinanceTool.MarketMetrics` never produces (it emits `price, change_percent, volume, rsi, atr, rvol, avwap_*, sma_*`) → `y_rvol = 0` → **REJECTED for every real ticker** (confirmed). `Playbook.run_all` then reads `profile["base_score"]`/`["setup_quality"]` (`:91-92`) but the strategy nests the score under `scoring_engine.base_score` (`:91-92`). `SecondDayPlay` does not inherit the `Strategy` ABC (`strategy.py`, 0 importers) — `check_time_window` is never applied. Net: Strategos' "[⚔️ Strategy Scan]" has always printed an error line. | local run; file:line |
| **H-4** | **`rules.yaml` is half-consumed; `cortex_routing` is silently dropped.** `trading_rules` → `TradingRules` (`config.py:54-59`) → only consumer is `FinanceTool._get_rule` (`finance.py:159-169`): uses `rsi.period/overbought/oversold`, `atr.period/expansion_multiplier`, `moving_averages.bullish_cross.fast/slow`, `momentum.rvol_alert_threshold`; **unused**: `rvol_strong_threshold`, `atr.extension_multiplier`, `bullish_cross.logic`. The SMA windows are hard-coded 10/20 regardless of config (`finance.py:200-201,215-218`). `cortex_routing` (`rules.yaml:26-28`) is **not a `CobaltSettings` field** (`extra="ignore"`, `config.py:235`), and `Cortex` looks for `config.rules.cortex_routing` (`cortex.py:43-48`) → both keyword lists are **empty in production** → the deterministic ORCHESTRATOR fast-path and the high-risk SECURITY INTERCEPT (Pass 3 H-9) never trigger (the `test_cortex` fixture that would catch this is broken, Pass 3 H-10). | file:line |
| **H-5** | **`strategies.yaml` has two readers and a strict schema that the strategy ignores.** Reader 1: `config.py:597-606` validates every entry through `StrategyConfig` (**raises** on any unknown/missing field — e.g. adding a `parameters:` block or a new filter key is a hard failure) and stores it on `config.strategies` — which **nothing reads** (grep). Reader 2: `Playbook._load_config` (`playbook.py:25-42`) re-parses the YAML raw from a CWD-relative path and hands dicts to a hard-coded `class_map = {"second_day_play": SecondDayPlay}` (`:47-50`). So: rigid validation with no consumer; a consumer with no validation; new setups need **a YAML block + a Python class + a `class_map` edit**. The "anti-rigidity" principle (§4) is not met. | file:line |
| **H-6** | **`FinanceTool` is a daily-bar, yfinance, single-ticker indicator tool.** 2-year daily history (`finance.py:143`); RSI/ATR/RVOL (today's *partial* volume ÷ 20-day avg, `:106-110,174` — misleading intraday), anchored VWAP from last earnings date (yfinance `earnings_dates`, `:127-135`) and 42-day swing high/low (`:187-197`), SMA 10/20/50/100/200 + slope, signal string, "RVOL ALERT"/"PARABOLIC MOVE" flags. Reasonable swing-context tool; **not** a real-time/intraday source and not usable for any priority setup. It is also the morning briefing's market-data source (hard-coded `NVDA, SPY, BTC-USD`, `briefing.py:47`). | file:line |
| **H-7** | **`SemanticTagger` API vs `live_run_orchestrator` (Pass 2 H-3), resolved.** Current class: `SemanticTagger(batch_size=20)`, `run_batch()`, module-level `process_entire_queue(batch_size)` (`semantic_tagger.py:810,269,297`). Runner calls `SemanticTagger(db_connection=conn, llm=llm)` / `run_tagging_cycle(batch_size=100)` (`live_run_orchestrator.py:132,137`). **All five files first appear in one commit (`cc14caa`, 2026-04-07)** — the mismatch was committed, not regressed. Live DB shows the current code *did* run standalone: 72 theme entries with status `ACTIVE`/`NONE` and ISO timestamps in four batches 2026-04-05 17:42–17:47 (= `_update_instruments`, `:206-209`), while 275 entries have status `HOT` + date-only `added_at` (= the now-unused `_format_theme_state`, `:169-171`) from an earlier version. So the tagger works via `__main__`; only the orchestration wrapper is broken. Tagger also: `LLM(role='strategist')` per batch (`:250`), `max_tokens=24000` (`:251`), duplicate `import json/datetime` (`:187-188`), naive timestamps, `logging` + `loguru` both configured. | git log; live DB |
| **H-8** | **`daily_in_play` writer: inputs exist, are stringly-typed, and are ~5 months stale.** Consumable today per instrument: `market_snapshots.raw_data` (151 Finviz keys — `Price`, `Volume`, `Relative Volume`, `Gap`, `Change`, `Average True Range`, `Average Volume`, `Prev Close/Open/High/Low`, `Float %`, `Shares Float` (1 416/2 209 populated), `Short Float`, `Earnings Date` (1 225), `Sector/Industry`, `News Title/URL/Time`, `Performance (1…30 Minutes)`, `After-Hours *`, `Relative Strength Index (14)`, `Volatility (Week/Month)`, `active_on_scanners`), `instruments.metadata` (`sector/industry/atr/average_volume` 345, `market_cap` 244, `shares_float/short_float` 239), `instruments.active_themes` (state objects), `themes`. All Finviz values are **strings** with `%`, commas, `$`, `M/B` suffixes and US date strings (`3.88%`, `7385.28`, `2/26/2026 4:30:00 PM`). Target table: `daily_in_play(date, instrument_id, rank, catalyst, rvol_multiplier, notes)` + `UNIQUE(date, instrument_id)`; `strategy_signals`/`key_levels` exist for outputs. The writer needs: a Pydantic `ScreenerRow` parser, a ranking rule (RVOL/gap/float/price floors — today these exist only as prose inside the Gemini briefing prompt, `prompts.yaml:36-38`), and a fresh snapshot (last 2026-03-28). | live DB; file:line |

---

## 1. Priority-setup map (question 1)

| Priority setup (Requirements §6) | What exists today | Gap |
|---|---|---|
| Scalp Radar 5 + 9 EMA continuation | Nothing. No EMA code; no intraday bars. | Intraday OHLCV source (Finviz quote export `quote_export.ashx?…&p=d` is **daily**; TradingView/Polygon-class feed needed), EMA(5/9) calc, continuation rule, backtest harness. |
| VWAP Continuation | Daily anchored VWAPs only (`finance.py:112-117`); YAML stub `fashionably_late_scalp` (inactive, no class) references `VWAP`/`9EMA` cross. | Session VWAP from intraday bars; setup logic; class. |
| Bouncy Ball | Nothing (string absent from repo). | Everything. |
| Big Dog | Nothing (string absent from repo). | Everything. |
| *(non-priority, implemented)* Second Day Play | `SecondDayPlay` + YAML + 3 tests; broken end-to-end (H-3); "Math Package" for the defunct Ion HUD. | — |
| *(non-priority, stubs)* Second Chance Scalp | YAML only, inactive. | — |

Bidirectional (long/short) support: `direction: "BOTH"` in YAML, but `SecondDayPlay` hard-codes `"direction": "LONG"` (`:84`) and gap-down = reject (`:48-54`). No backtesting, live-example collection, regime-conditioned EV, or playbook-building support exists (§6 bullets 2–5).

## 2. ASET grading / EV / sizing (question 2)
- **Grading**: none. Persona prompt text only (`config.yaml:109`). Schema placeholders (`success_grade`, `r_multiple`, `ai_logic_payload`).
- **EV**: none computed anywhere; PRD-001 defines a HUD *score*, not EV; `second_day_play` score (base 50 + RVOL/gap points) is the only arithmetic.
- **Sizing**: none; `Sentinel (Risk): Position Sizing Logic` is a System-Manifest bullet (`generate_constitution.py:137`) with no code. `risk_per_share` is computed (`second_day_play.py:89`) but never converted to shares/$ or to a daily-stop %.
- **Regime / internals / Oura**: absent (no data source, no field).
- State: green-field. The 5-Pillar schema has landing spots (`strategy_signals`, `system_alerts`, `trades`) but no writers.

## 3. `rules.yaml` and `strategies.yaml` (question 3)

| File | Schema (Pydantic) | Readers | What it actually controls | Rigidity |
|---|---|---|---|---|
| `rules.yaml → trading_rules` | `TradingRules{momentum, moving_averages: dict, rsi, atr}` (`config.py:30-59`), all sub-blocks Optional, strict fields inside | `FinanceTool._get_rule` only | RSI period/OB/OS, ATR period + parabolic multiplier, cross fast/slow **labels** (math still 10/20), RVOL alert threshold | Adding a new rule family = new Pydantic class; unused keys are silently accepted (`moving_averages` is a free dict). Defaults in code (`finance.py:159-169`: RSI 14/70/30) differ from YAML (20/80/20) — YAML wins only if config loads. |
| `rules.yaml → cortex_routing` | none (dropped, H-4) | `Cortex` expects `config.rules.cortex_routing` | **nothing** (empty lists) | — |
| `strategies.yaml` | `StrategyConfig{name, active, direction, description?, time_window, filters: StrategyFilters (12 optional typed keys), execution, scoring?}` (`config.py:126-206`); validation raises on unknown keys | (1) `config.py:597-606` → `config.strategies` (no consumer); (2) `Playbook` raw YAML → `class_map` | In practice only `scoring.*` keys are read (`second_day_play.py:64-98`); `name`, `direction`, `time_window` are echoed by `list_strategies`; `filters`/`execution` ignored | Inactive entries must still validate; every new filter key must be added to `StrategyFilters`; every setup needs a Python class + `class_map` edit. "Ion modifiers" (`live_rvol_multiplier`, `spy_correlation_weight`, `resistance_penalty`, `time_decay_per_min`) are instructions for a Windows HUD that no longer exists in the plan. |
| `scanners.yaml` (Pass 2) | none (raw dict) | `ScannerOrchestrator`, `live_run_dynamic_scanners` | Finviz filter strings; `schedule` ignored | Filters are Finviz codes (`sh_avgvol: o2000`) — expressive but vendor-coupled. |

## 4. Tagger API vs orchestrator (question 4) — see H-7. Fix is mechanical: `SemanticTagger(batch_size=100)` then `run_batch()` in a loop (or call `process_entire_queue`); the tagger opens its own psycopg2 connection via config (`semantic_tagger.py:74-85`), so the runner's `conn`/`llm` injection is unnecessary.

## 5. What a `daily_in_play` writer would consume (question 5) — see H-8. Minimal deterministic pipeline (proposal): fresh ingestion cycle → for each snapshot row parse `ScreenerRow(Price, Volume, RelVol, Gap%, ATR, AvgVol, Float, ShortFloat, EarningsDate, Sector, News*)` → apply `rules.yaml`-driven floors (price ≥ 1, vol/avgvol thresholds, ATR/RVOL) → rank → `INSERT … ON CONFLICT (date, instrument_id)` into `daily_in_play(rank, rvol_multiplier, catalyst ← News Title/Earnings Date, notes ← scanner tags/themes)` → Mattermost post. All numeric work deterministic; LLM only for the optional catalyst sentence. Note `market_snapshots.price/volume` columns should be populated by the same parser (Pass 2 H-8).

---

## 6. Component map — verdict + ORIGINAL INTENT

| Component | File:lines | Verdict | Notes | ORIGINAL INTENT (documentation trail) |
|---|---|---|---|---|
| `Strategos` (Tactical dept head) | `tactical.py` | BROKEN-FRICTION | Ticker = first word; `FinanceTool.run` → `Playbook.run_all`; "STRATEGY"/"PLAYBOOK" menu. Always returns an error line for the scan (H-3). Reached via Cortex TACTICAL (`cortex.py:189-203`). | `9c1af63` (2026-02-11) "establish Cobalt-Ion architecture, PRD-001, and Tactical Brain scaffolding"; System Manifest "Strategos (Tactical): Market Analysis & Technical Indicators, Quant Logic & Strategy Generation, Pattern Recognition Engine" (`generate_constitution.py:125-128`); docstring "The Quantitative Trading Engine". |
| `Playbook` | `playbook.py` | BROKEN-FRICTION | CWD-relative YAML; hard-coded `class_map`; contract mismatch with strategy (H-3). | `8463874` (02-12) "Configurable Strategy Engine"; docstring "Loads trading strategies and parameters from strategies.yaml". PRD-001 Story A "identify the specific Strategies (from my Playbook)". |
| `Strategy` ABC | `strategy.py` | KILL-candidate (unused) or RETAIN-and-enforce | 0 importers; defines `analyze → signal/confidence/stop/target`, `check_time_window`. | `8463874`/`9c1af63`; docstring "enforces a standard structure for the Backtester and Live Engine" — backtester never built. |
| `SecondDayPlay` | `strategies/second_day_play.py` | BROKEN-FRICTION → KILL-candidate (non-priority setup) | H-3; returns PRD-001 "Math Package" (`zones`, `scoring_engine.modifiers`, `abort_conditions`) for Ion. | Docstring "Phase 3 (Tactical)… pull scoring rules from strategies.yaml"; `strategies.yaml:34` "NEW SCORING BLOCK (Added for Logic Lab & Ion)"; PRD-001 §4.1-4.2 (`create_prd.py:73-86`); ADR-001 "Cobalt generates the Strategy Math (Scoring Profile); Ion runs the math 10×/sec". Ion/HUD is out of scope now (Requirements §3: trading PC kept clean). |
| `strategies.yaml` | | RETAIN file / BROKEN schema use (H-5) | | `8463874`; `cf9cf4f` (03-01) "strict Pydantic playbook validation"; Requirements §4 "config-driven… adding a mode is a config block". |
| `rules.yaml → trading_rules` | | RETAIN (partially consumed, H-4) | | `53611fe` (02-10) "restore finance logic (RSI-20, Anchors, Crosses) from rules.yaml"; `0b880fb` (02-09) modular configs; file header "ROOT KEY: trading_rules". |
| `rules.yaml → cortex_routing` | | BROKEN-FRICTION (dropped, H-4) | | `87e16c7` (02-25) "Deterministic Fast-Path (Triage Desk)… prevent identity bias"; `cc14caa` moved keywords "use config-defined keywords" (`cortex.py:63,77` comments). |
| `FinanceTool` / `MarketMetrics` | `finance.py` | RETAIN (as swing-context tool; not for setups) | H-6; yfinance; earnings-date anchored VWAP is the one "earnings" touchpoint in code. | `b01b96f` (02-08) "Added Finance Tool"; `53611fe` (RSI-20, anchors, crosses); docstring "Strictly implements ALL rules.yaml logic"; System Manifest Scout "Data Gathering (FinanceTool)". |
| `ScannerOrchestrator` / `FinvizApiClient` / `scanners.yaml` | Pass 2 | RETAIN → fix | Collector for in-play candidates; typed columns empty. | `cc14caa`; PRD-001 Story A; Requirements §1/§5/§7 (Finviz scanners "functional today"). |
| `MetadataEnricher` | Pass 2 | RETAIN | float/short-float/ATR/avg-vol/market-cap/sector into `instruments.metadata`. | `cc14caa`; Requirements §8 Tier-1 (float, short interest). |
| `SemanticTagger` + `themes` + `sync_taxonomy` | `semantic_tagger.py`, `sync_taxonomy.py` | RETAIN (fix wrapper/status vocab) | H-7; status vocab drift (`HOT/WARM/COLD` model vs `ACTIVE/NONE` writes vs `active/ACTIVE/DORMANT` in `themes`). | `cc14caa`; docstrings "Semantic Tagging Drip Engine — Step 3", "Master Taxonomy Sync"; schema `themes` "hierarchical & AI Ready"; PRD-001 Story A (strategy/theme context per ticker). |
| `daily_in_play` / `strategy_signals` / `key_levels` (tables) | `schema.sql:40-66,126-134` | RETAIN (no writers) | H-8 landing zone. | `cc14caa` 5-Pillar; schema comments "Daily in-play ticker snapshot", "Cobalt's autonomous pattern recognition output", "Support/resistance zones… VWAP, PM_HIGH". |
| `MorningBriefing` skill | `briefing.py` | BROKEN-FRICTION → KILL-candidate (duplicate) | Hard-coded tickers/query; `LLM(model_name=…)` kwarg is silently ignored (`LLM` fields are `role/api_key`, `llm.py:24-25`; pydantic default `extra=ignore`) → always default role; writes `Briefing_<date>` to `0 - Inbox`. Second, different briefing lives in `scheduler.py` (Gemini + googleSearch prompt with the RVOL/ATR/price hard rules). | `0bf87fd` (02-09) "implemented morning briefing skill"; PRD-001 Story A "Morning Briefing (Context)"; Requirements §5 Premarket agent (prebell.laldinsoft.com model). |
| `prompts.yaml scheduler.morning_briefing` hard rules | `prompts.yaml:36-38` | BROKEN-FRICTION | "price > $1, PM volume > 100K, ATR > 0.4 or RVOL > 4; ADV14 > 2M, gap outside ±3%" — **the only in-play filter definition in the repo, and it lives in an LLM prompt**, not in `rules.yaml`/code. | UNVERIFIED (prompt text only). |
| `Cortex._run_tactical` | `cortex.py:189-203` | RETAIN | Thin dispatcher. | `87e16c7`, `eef5bdf` (02-18) "Intent-Based Routing". |
| `tests/test_strategies.py` | | BROKEN-FRICTION | 3 pass — against a `parameters` fixture the YAML doesn't have and hand-made keys the tool never produces; green while prod path is red. | `8463874`; docstring "Verifies that trading logic respects the configuration rules". |

---

## 7. Hardcoded values and structural assumptions

| Where | Value / assumption |
|---|---|
| `playbook.py:20,30` | `configs/strategies.yaml` CWD-relative, then package-relative fallback; `class_map` in code |
| `second_day_play.py:16,28-32,38,48,57-60,84` | `config["parameters"]`; data keys `yesterday_close…pre_market_high`; `min_rvol 1.5`; gap-down `0.98`; entry `pm_high+0.05`, stop `y_close-0.20`, target `2R`; `"direction":"LONG"`; defaults for every scoring key |
| `finance.py:143,159-169,187,200-204,229` | yfinance `period="2y"`; rule defaults RSI 14/70/30, ATR 14/5.0, cross 10/20, RVOL 3.0; swing window 42 bars; SMA windows 10/20/50/100/200; 5-day parabolic window (`iloc[-6]`) |
| `briefing.py:47,57,113` | tickers `NVDA, SPY, BTC-USD`; query "top technology and finance news today"; `gpt-4o` fallback name; folder `0 - Inbox` |
| `prompts.yaml:36-38` | in-play hard rules (price/volume/ATR/RVOL/ADV/gap) as prose |
| `semantic_tagger.py:250-251,206-209,169-171` | role `strategist`; `max_tokens=24000`; status vocab `ACTIVE/NONE` vs `HOT` |
| `strategies.yaml` time windows, `rules.yaml` thresholds | naive `"HH:MM"` strings, ET implied |
| `config.py:126-206` | `StrategyFilters` enumerates every allowed filter key |
| `cortex.py:43-48` | expects `config.rules.cortex_routing` (nonexistent) |

## 8. Tests run
- `uv run pytest tests/test_strategies.py -q` → **3 passed** (pure; see §6 caveat).
- Local network-free invocation of `Playbook().run_all(...)` and `SecondDayPlay.analyze(...)` → reproduced H-3 (`TypeError`; `params={}`; `REJECTED` on `MarketMetrics`-shaped input; `ACTIVE_WATCH` only with the test's synthetic keys).
- No tests exist for `Strategos`, `Playbook`, `FinanceTool`, `rules.yaml` consumption, `ScannerOrchestrator`, `MetadataEnricher`, `SemanticTagger`.

## 9. RETAIN / BROKEN-FRICTION / KILL-candidate summary
**RETAIN**: `FinanceTool` (swing-context indicators; not for setups); `rules.yaml → trading_rules` as the home for deterministic thresholds; `strategies.yaml` as the playbook file; 5-Pillar `daily_in_play`/`strategy_signals`/`key_levels` as output tables; scanner collector + enricher + tagger (Pass 2); `Cortex._run_tactical` dispatch.
**BROKEN-FRICTION**: `Playbook`↔`SecondDayPlay` contract (arity, `parameters` vs `filters`, data keys, result shape, no ABC) (H-3); `cortex_routing` dropped → empty keyword lists (H-4); `strategies.yaml` dual readers / unused validated copy / `class_map` (H-5); `rules.yaml` half-consumed + SMA windows hard-coded (H-4); `live_run_orchestrator` ↔ `SemanticTagger` (H-7); theme-status vocab drift; `market_snapshots` typed columns empty and all Finviz values stringly-typed (H-8); in-play filter rules living only in a prompt; `MorningBriefing` duplicate with hard-coded tickers and a dead `model_name` kwarg; `test_strategies` green-while-broken.
**KILL-candidates**: `SecondDayPlay` + its YAML block (non-priority setup, Ion-era "Math Package"); the two inactive YAML stubs unless rebuilt for the priority setups; `Strategy` ABC (or enforce it — Dejan's call); `skills/productivity/briefing.py` (superseded by the scheduler briefing, or vice-versa — one should go); the "Ion modifiers" scoring keys.

## 10. Inputs to other passes / INFRA / sprint planning
- **Sprint sequencing (income impact)**: the collector → typed snapshot → `daily_in_play` writer + alert is the shortest path to usable value (Pass 2 §4); grading/EV/sizing and the four priority setups are **new builds** needing an intraday data source first (TradingView access / Finviz Elite single-stock bars per Requirements §7 — re-verify).
- **Pass 5 (LLM routing)**: `LLM(model_name=…)` silently ignored; `SemanticTagger` role; briefing duplication; Cortex keyword lists empty → intercept never fires (cross-ref Pass 3 H-9).
- **Pass 6 (scribe/vault)**: `briefing.py` writes to `0 - Inbox`; scheduler writes `Morning_Briefing_*` too.
- **Pass 7 (config)**: `cortex_routing` not captured; `config.strategies` unconsumed; `StrategyFilters` rigidity; defaults-in-code vs YAML.
- **Research engine (§8)**: `Earnings Date`, `EPS/Revenue Surprise`, `Float %`, `Short Float/Interest/Ratio`, `News Title/URL/Time` already arrive in the Finviz export (H-8) — cheap Tier-1 fields before EDGAR/FMP.

## 11. UNVERIFIED (explicitly)
- Whether Finviz Elite's `quote_export.ashx` can deliver intraday bars (Requirements §7 asks to re-verify; the client requests `p=d` = daily).
- Whether any vault ADR defines ASET grading weights or the four setups' rules (vault not read).
- Whether `MorningBriefing` (`briefing.py`) has ever been invoked in prod (only reachable via Cortex INTEL "briefing").
- Exact tagger version that produced the 275 `HOT` entries (pre-commit working-tree code; not in git).
