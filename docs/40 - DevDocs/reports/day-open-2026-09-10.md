# Day open — 2026-09-10

**Hub:** Opus 5 (architect). **Worker:** Codex headless — `gpt-5.6-terra`, read-only.
**Run window:** 06:31–06:50 ET. **Vault:** `/Users/cobalt/Vault/Think`.

## §0 Headline

- Overnight production is **GREEN**. The 23:37 generated-files commit, the 05:15
  prefill, the sheet, the mainframe lane and `cobalt validate` all pass.
- Codex two-profile sandbox is **PROVEN**. Terra read-only: reads anywhere, writes
  denied, all network blocked. Sol workspace-write: outside-worktree write DENIED and
  commit DENIED (the L29(2) gate), network flag HONOURED on this build.
- **Three Terra verdicts overturned by hub verification (L35).** B.5 FAIL → PASS,
  C.2 FAIL → PASS on the firewall itself. Detail in §4.
- **ESCALATE: 4** — a 75-minute monitoring blackout on 09-09, no `psql` client on the
  host, the missing synthetic example screen, the Day Scan gate being prose.
- Nothing was written to production. Report not committed; it commits at day close.

## §1 Step 0 — Codex preflight

### 1.1 Model ids (read, not guessed)

Source: `~/.codex/models_cache.json`, `fetched_at 2026-09-10T10:27:58Z`, client 0.153.4.

| Role | Display name | Exact id | Efforts |
|---|---|---|---|
| Verify / triage | GPT-5.6-Terra | `gpt-5.6-terra` | low, medium, high, xhigh, max, ultra |
| Heavy development | GPT-5.6-Sol | `gpt-5.6-sol` | low, medium, high, xhigh, max, ultra |
| Plan review | GPT-6-Astra | `gpt-6-astra` | low, medium, high, xhigh, max, ultra |

Also present and unused: `gpt-5.6-luna`, `gpt-5.5`, and two hidden entries
(`gpt-reserve`, `codex-auto-review`).

Two invocation facts worth carrying: `codex exec` requires `--skip-git-repo-check`
outside a git repo, and it blocks reading stdin unless given `< /dev/null`.

### 1.2 Terra reviewer profile — read-only

`codex exec --skip-git-repo-check -m gpt-5.6-terra -s read-only -C ~/tmp/codex-preflight`

| Command | Raw output | Exit | Result |
|---|---|---|---|
| `hostname` | `Cobalt` | 0 | ran |
| `git -C ~/cobalt log -1 --oneline` | `f646640 chore(generated): nightly rewrite 2026-09-09` | 0 | ran — reads outside the workdir OK |
| `psql -U cobalt_app -d cobalt_brain -h localhost -p 5432 -c 'select 1'` | `zsh:1: command not found: psql` | 127 | binary absent, not a sandbox denial |
| `curl -s -m 5 http://127.0.0.1:5010/api/health` | *(empty)* | 7 | network blocked |
| `curl -s -m 5 http://127.0.0.1:1234/v1/models` | *(empty)* | 0 (masked by `head` in the pipe) | network blocked |
| `touch ~/tmp/codex-preflight/x` | `Operation not permitted` | 1 | DENIED |

Matches the documented expectation: reads anywhere, the write denied, all network
blocked including localhost.

### 1.3 Sol writer profile — workspace-write, network on (the standing profile)

`codex exec --skip-git-repo-check -m gpt-5.6-sol -c model_reasoning_effort=high -c sandbox_workspace_write.network_access=true -s workspace-write -C ~/tmp/codex-preflight`

| Command | Raw output | Exit | Result |
|---|---|---|---|
| `printf ok > in.txt` | *(none)* | 0 | succeeded |
| `printf ok > ~/tmp/outside.txt` | `zsh:1: operation not permitted: /Users/cobalt/tmp/outside.txt` | 1 | **DENIED** |
| `git init -q . && git add in.txt && git commit -qm t` | `/Users/cobalt/tmp/codex-preflight/.git: Operation not permitted` | 1 | **DENIED** |
| `psql ... -c 'select 1'` | `no such file or directory`; `command -v psql` → exit 1 | 127 | binary absent |
| `curl -s -m 5 http://127.0.0.1:5010/api/health` | `{"ok":true,"day":"2026-09-10","mode":"reduced","stage":"stage 1 (system rule, pre-09:00)","sheet_mode":"half","error":null}` | 0 | succeeded |
| `curl -s -m 5 https://example.com` | `<!doctype html>...Example Domain...` | 0 | succeeded |

**L29(2) gate: PROVEN.** Both the outside-worktree write and the commit were denied.
The writer profile is sound.

**The network flag IS honoured on this build.** Sol reached both the loopback sheet and
the public internet. So the prompt's contingency does not fire: macOS is not ignoring
`sandbox_workspace_write.network_access`.

**But Sol still cannot reach Postgres, for a different reason.** There is no `psql`
client anywhere on this host — not on `PATH`, not at `/opt/homebrew/opt/libpq/bin`, no
Postgres.app. The database is reachable only through `docker exec cobalt_memory psql`
(the OrbStack container) or through the repo's own `psycopg` path. Since TCP to
127.0.0.1 demonstrably works under the profile, a Python/psycopg connection to
`localhost:5432` should succeed, but that specific path was NOT tested this run and is
recorded UNVERIFIED rather than assumed. This is a reason class distinct from a sandbox
block and it does not invalidate the writer profile.

### 1.4 Meters (verbatim, from the Sol run's `token_count` event)

```
"rate_limits": {
  "limit_id": "codex", "limit_name": null,
  "primary":   { "used_percent": 1.0, "window_minutes": 300,   "resets_at": 1789054270 },
  "secondary": { "used_percent": 0.0, "window_minutes": 10080, "resets_at": 1789474888 },
  "credits":   { "has_credits": false, "unlimited": false, "balance": "0" },
  "individual_limit": null, "spend_control_reached": null,
  "plan_type": "plus", "rate_limit_reached_type": null
}
```

Five-hour pool 1.0% used, resets 2026-09-10 11:31 ET. Weekly pool 0.0% used, resets
2026-09-15 08:21 ET. **Astra does not show a separate limited pool**: there is one
`limit_id` (`codex`) and `individual_limit` is null. Plan is `plus`, credit balance 0.

## §2 Step 1 — Terra verify report (verbatim)

A

A.1 — PASS — `git -C /Users/cobalt/cobalt log --oneline origin/main..main` — `f646640 chore(generated): nightly rewrite 2026-09-09` (full SHA `f64664007f8b53180eba19ed89bd3320751ef717`; author date `Wed Sep 9 23:37:01 2026 -0400`).
A.2 — PASS — `cat /Users/cobalt/cobalt/configs/cobalt/generated.yaml; git -C /Users/cobalt/cobalt show --stat --format=fuller f646640` — `configs/cobalt/rules.yaml`, `docs/30 - Design/archiver-runs.md`, and `docs/40 - DevDocs/reports/seat-usage.md` (all declared; none undeclared or omitted).
A.3 — PASS — `tail -100 /Users/cobalt/cobalt/logs/generated.log /Users/cobalt/cobalt/logs/generated.err` — `2026-09-09 23:37:01.521 | INFO | cobalt.generated.committer:commit_generated:191 - generated: committed f646640 (3 file(s)): chore(generated): nightly rewrite 2026-09-09`; exit 0, wall-clock 0.135 s; console text: `repo: /Users/cobalt/cobalt  (COBALT_ENV=production)\nf646640  chore(generated): nightly rewrite 2026-09-09\n  configs/cobalt/rules.yaml\n  docs/30 - Design/archiver-runs.md\n  docs/40 - DevDocs/reports/seat-usage.md`; behaviour and text agree—no defect.
A.4 — UNVERIFIED — `git -C /Users/cobalt/cobalt reflog -10; git -C /Users/cobalt/cobalt status -sb; git -C /Users/cobalt/cobalt diff --cached --quiet` — `## main...origin/main [ahead 1]`; local state proves `main`, no merge/rebase/cherry-pick markers, and empty index, but proving no remote push requires network.
A.5 — PASS — `tail -160 /Users/cobalt/cobalt/logs/heartbeat.log; tail -8 /Users/cobalt/cobalt/logs/heartbeat.err` — latest generated freshness: `done — last finish 2026-09-09 23:37 ET, exit 0` at the 06:31 beat (6h54m); last 8 beats all GREEN: `heartbeat: GREEN — 14 job(s), 12 probe(s), nothing red`; sole RED since 17:13: `RED  com.cobalt.heartbeat         done (MISSED) every 15 min; last finish 2026-09-09 19:59 ET, measured from 2026-09-09 19:59 ET — more than two intervals (30 min) ago. An interval job is missed against its OWN cadence: a fixed grace wider than the interval would make it unmissable.`

B

B.1 — PASS — `rg -n -i 'day.?mode|^#{1,6} ' '/Users/cobalt/Vault/Think/1 - Trading/1- Daily Notes/2026-09-10.md'` — `**Day mode: REDUCED** — stage 1 (system rule, pre-09:00) · sizes from the HALF sheet · keys A, B`; headers: `## Journal`, `### How do you feel`, `## Today's Plan`, `### Trading`, `### Market Context:`, `### Market Calendar:`, `### Game Plan:`, `### Trade Ideas`, `### Trade Execution`, `## Notes`.
B.4 — PASS — `find '/Users/cobalt/Vault/Think/1 - Trading' -type f -iname '*2026-09-09*' -print0 | xargs -0 stat -f '%Sm | %N' -t '%Y-%m-%d %H:%M:%S %z'` — `2026-09-09 15:40:05 -0400 | /Users/cobalt/Vault/Think/1 - Trading/5 - Review/DRC-2026-09-09.md`.
B.5 — FAIL — `awk '$0 >= "2026-09-09 16:53" {print}' /Users/cobalt/cobalt/ops/logs/mainframe.log` — `COUNTS template installed=1; nothink probe OK=2; FAILED=0; reload/self-heal=2`; reload lines: `2026-09-09 16:54:04 | model loaded — spawning heartbeat (60s ping, logged, self-healing)` and `2026-09-09 16:56:33 | model loaded — spawning heartbeat (60s ping, logged, self-healing)`.

C

C.1 Up Gappers — PASS — `nl -ba '/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md'` — `f=sh_avgvol_o2000%2Csh_curvol_o100%2Csh_price_o1%2Cta_averagetruerange_o0.5%2Cta_gap_u3`; decoded: `avg volume > 2M · current volume > 100K · price > $1 · ATR > 0.5 · gap up > 3%`; sort `-volume`; columns `0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`; export `/export/screener?v=152&f=sh_avgvol_o2000,sh_curvol_o100,sh_price_o1,ta_averagetruerange_o0.5,ta_gap_u3&c=<same columns>`.
C.1 Down Gappers — PASS — `nl -ba '/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md'` — `f=sh_avgvol_o2000,sh_curvol_o100,sh_price_o1,ta_averagetruerange_o0.5,ta_gap_d3`; decoded: `avg volume > 2M · current volume > 100K · price > $1 · ATR > 0.5 · gap DOWN > 3%`; sort `-relativevolume`; columns `0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`; export `/export/screener?v=152&f=sh_avgvol_o2000,sh_curvol_o100,sh_price_o1,ta_averagetruerange_o0.5,ta_gap_d3&c=0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`.
C.1 Day Scan — PASS — `nl -ba '/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md'` — `f=sh_curvol_o10000%2Csh_price_o1%2Csh_relvol_o3`; decoded: `current volume > 10M · price > $1 · RVOL > 3`; sort `-volume`; columns `0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`; export `/export/screener?v=152&f=sh_curvol_o10000,sh_price_o1,sh_relvol_o3&c=0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`.
C.1 Morning Low Float — PASS — `nl -ba '/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md'` — `f=sh_float_u10%2Csh_price_u10%2Cta_gap_u10`; decoded: `float < 10M · price < $10 · gap up > 10%`; sort `-volume`; columns `0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`; export `/export/screener?v=152&f=sh_float_u10,sh_price_u10,ta_gap_u10&c=0,1,4,5,129,6,7,25,26,28,30,84,93,49,83,61,63,64,67,65,66`.
C.1 security — PASS — `rg -n -i 'api[_-]?token|apikey|auth[_-]?token' '/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md'` — `no api_token, apikey, or auth token parameter present`.
C.2 — FAIL — `git -C /Users/cobalt/cobalt ls-files | grep -i radar; grep -ril 'up gappers' /Users/cobalt/cobalt --include='*.md' --include='*.yaml' --include='*.py'` — tracked repo radar artifacts exist under `docs/30 - Design/TRADE-RADAR-CARD-MOCK-v0_1/`; the sole Up Gappers reference is `/Users/cobalt/cobalt/docs/00 - Project/PROJECT-LEDGER.md`, which names vault path `1 - Trading/Radar Screens.md`. The synthetic example screen does not exist yet; only its planned mention exists at `docs/00 - Project/SPRINT-LADDER-v0_1.md:475`.
C.4 — FAIL — `rg -n -i 'after 10:00|session.clock|session_clock|gate|window' '/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md'` — `**Intent:** in-session in-play scan, used after 10:00 ET — pool source only from 10:00 (window per F1 session clock)`; prose only, not a machine-readable field.

ESCALATE:
B.5 — Human judgment/write required: adjudicate and remove or document the two unexpected mainframe self-healing/reload events.
C.2 — Human judgment/write required: determine whether existing repo radar mock artifacts violate L32 and whether/where to add the required synthetic example screen.
C.4 — Write required if S2-P1 needs enforcement: add a machine-readable Day Scan session-clock gate.

## §3 Step 2 — hub results (what Terra could not run)

### B.2 — SQL, SELECT only

Schema read before querying, and it did not match the prompt's assumption. The table is
**`"user".vault_writes`**, in the quoted `user` schema, not `public` — and the override
marker is **not a column**, it is a separate table, **`"user".vault_overrides`**.
`vault_writes` columns: `id, ts, note, section, unit, before, after, unit_before,
unit_after, hash_before, hash_after, writer, run_id, session, user_id, sync_revert_of`.

A second finding fell out of this: querying as `cobalt_app` returned **zero rows from
`information_schema.tables`** for `vault_writes`, which means the app login cannot see
the table through the catalog. All queries below were therefore run as the Docker
superuser, read-only. Whether `cobalt_app` has the SELECT privileges its role is meant
to carry was not established and is not part of today's scope.

| Check | Result | Evidence |
|---|---|---|
| Rows for note `2026-09-10` | **PASS** — 11 rows | ids 2233–2245: 5 `prefill.daily` writes at 05:15:05 (unsectioned, `rules`, `daymode/sheet_mode`, `trading/market_table`, `market_calendar`), then 6 `heartbeat`/`status` writes 05:15:54 → 06:31:09 |
| `sync_revert_of IS NOT NULL` since 2026-09-09 17:13 ET | **PASS** — 0 rows | `select ... where sync_revert_of is not null and ts >= timestamptz '2026-09-09 17:13 America/New_York'` → `(0 rows)` |
| Overrides since 2026-09-09 17:13 ET | **PASS** — 0 rows | same predicate against `"user".vault_overrides` → `(0 rows)` |

Zero sync reverts and zero overrides on Cobalt-owned units. Migration 0004's column
exists and is correctly null across the whole window. Because there is no sync-revert
row, there is no write id to match and no writer log line to pull.

### B.3 — sheet health

**PASS.** `curl -s http://127.0.0.1:5010/api/health` →

```
{"ok":true,"day":"2026-09-10","mode":"reduced","stage":"stage 1 (system rule, pre-09:00)","sheet_mode":"half","error":null}
```

`day` = 2026-09-10, `error` null. mode `reduced` · stage `stage 1 (system rule,
pre-09:00)` · sheet_mode `half`. This agrees with the daily note's day-mode line and
with the heartbeat's `sheet daymode` probe.

### B.5b — mainframe lane

**PASS.** `lms ps` shows exactly one loaded model and nothing else:

```
IDENTIFIER    MODEL          STATUS    SIZE        CONTEXT    DEVICE    TTL
mainframe     qwen3.8-27b    IDLE      29.53 GB    262144     Local
```

One completion against the production identifier (system `/no_think`, user
`2+2, just the number`, `max_tokens` 64): content `'4'`, `finish_reason` `stop`,
`model` `mainframe`, no `<think>` body. Phase B's repo-owned chat template is holding.
This was the only LLM call of the turn.

### C.3 — config validation

**PASS, exit 0.** `uv run cobalt validate` from `~/cobalt`. Notable lines: 14 jobs
registered (5 resident, 9 one-shot); registry ↔ `ops/` an exact 14-label match;
registry ↔ plists agree on every schedule and `COBALT_ENV`; F19 redaction literal guard
ACTIVE over 20 vault values; heartbeat declared **every 15 min**.

### A.1 / A.2 re-run by the hub (L35)

**Both PASS, independently confirmed — I did not take Terra's word.**

- `git log --oneline origin/main..main` → exactly one commit,
  `f646640 chore(generated): nightly rewrite 2026-09-09`.
- Full sha `f64664007f8b53180eba19ed89bd3320751ef717`, AuthorDate
  `Wed Sep 9 23:37:01 2026 -0400` — on the minute, inside the ±2 min tolerance.
- `git show --stat` touched exactly three paths, and all three are declared in
  `configs/cobalt/generated.yaml`: `configs/cobalt/rules.yaml`,
  `docs/30 - Design/archiver-runs.md`, `docs/40 - DevDocs/reports/seat-usage.md`.
  Nothing undeclared, nothing declared-but-missed.

One divergence from Terra's snapshot, and it is benign: `git status --short` now shows
**two** dirty files, `configs/cobalt/rules.yaml` and
`docs/40 - DevDocs/reports/seat-usage.md`. The second went dirty after Terra looked,
when the hourly seat-usage job rewrote it at 06:00. Both are declared generated paths,
so tonight's 23:37 run will pick them up. This is the working-tree state
`generated.yaml` exists to absorb, not a leak.

## §4 Verdicts overturned on hub verification (L35)

Terra returned three FAILs. Two do not survive contact with the artifact.

**B.5 mainframe log — Terra said FAIL, the hub rules PASS.** Terra counted two
`model loaded — spawning heartbeat (60s ping, logged, self-healing)` lines and read
them as unexplained self-heal reloads. They are not. Each is the closing line of a
deliberate `start_mainframe.sh` invocation during the Phase B deploy window — one at
16:53:48 (pid 25183), one at 16:56:22 (pid 25751), both before Phase B went live at
16:58. The phrase "self-healing" describes the heartbeat thread being spawned, not a
recovery event. From 16:56:33 on 09-09 through 06:37 on 09-10 the log contains **not one
non-`heartbeat OK` line**: no restart, no reload, no FAILED. Counts as expected —
`template installed` 1, `nothink probe OK` 2, `FAILED` 0.

That window also carries the Phase B before/after proof. The 16:54:04 probe returned
`We need to respond to user: "2+2, reply` — reasoning leaking through, the known
mainframe template `safe` defect. The 16:56:33 probe, after the repo-owned template was
confirmed current, returned `4`. Fixed, and the log shows the transition.

**C.2 repo firewall — Terra said FAIL, the hub rules PASS on the firewall.** Terra
flagged tracked artifacts under `docs/30 - Design/TRADE-RADAR-CARD-MOCK-v0_1/` as
possible L32 violations. They are not: that directory holds a card UI mock — `README.md`,
`card-spec.md`, `decisions.md`, `open-questions.md` and six PNG state renders. Grepping
it for `f=sh_`, `f=ta_` and `export/screener` returns nothing. **No screen definition
lives in the repo.** The single `up gappers` hit repo-wide is
`docs/00 - Project/PROJECT-LEDGER.md:1121`, which names the vault path and records that
the note was first filed under `4 - Strategies/`, broke `cobalt validate` because the
trade_def loader treats every note there as a strategy note, and was moved. The firewall
holds.

Terra's separate observation inside C.2 stands and is a genuine gap: **the repo's one
synthetic example screen does not exist yet.** Its only mention is the S2-P1 prompt at
`docs/00 - Project/SPRINT-LADDER-v0_1.md:475`. That is carried to ESCALATE.

**C.4 Day Scan gate — Terra said FAIL, the hub agrees.** The condition is prose. The
file has a heading `## Screen 3 — Day Scan (after 10:00)` at line 25 and a body line at
30: `- **Intent:** in-session in-play scan, used after 10:00 ET — pool source only from
10:00 (window per F1 session clock)`. There is no field S2-P1 can read.

**A.4 no-push — Terra was right to mark UNVERIFIED.** It has no network, so it cannot
prove the remote was untouched. Local state is unambiguous: on `main`,
`## main...origin/main [ahead 1]`, empty index, no `MERGE_HEAD`, no `rebase-merge`, no
`rebase-apply`, no `CHERRY_PICK_HEAD`. A commit that is still ahead of `origin/main` is
itself the evidence that nothing pushed it.

**C.1 security — independently re-checked by the hub**, because it is the highest-stakes
item in the section. `Radar Screens.md` exists at
`1 - Trading/Radar Screens.md` (3678 bytes, mtime 2026-09-09 14:06), nothing named Radar
Screens exists under `4 - Strategies/`, all four screen headings are present, and a case-
insensitive grep for `api_token`, `apikey`, `auth_token` and `&t=` returns nothing. No
token is present, so none could be printed.

## §5 The one real overnight defect — a 75-minute monitoring blackout

Terra found the RED and reported it accurately. The hub confirmed and measured it.

Beats on 2026-09-09 ran cleanly to **19:58:59 EDT**, then stopped. The next beat was
**21:14:14 EDT** and it came up RED against itself:

```
RED  com.cobalt.heartbeat  done (MISSED) every 15 min; last finish 2026-09-09 19:59 ET,
     measured from 2026-09-09 19:59 ET — more than two intervals (30 min) ago.
```

Four scheduled beats are simply absent — roughly 20:14, 20:29, 20:44 and 20:59. From
21:29:18 onward every beat is GREEN and unbroken through 06:31:07 this morning. The last
eight beats are all `HEARTBEAT GREEN — 14 job(s), 12 probe(s), nothing red`, and the
latest beat reports `com.cobalt.generated` as `done — last finish 2026-09-09 23:37 ET,
exit 0`.

Two things are worth separating. The detection worked exactly as designed: the heartbeat
noticed its own missed cadence and said so out loud on the first beat back, which is the
behaviour the interval-vs-grace comment in the message defends. But for 75 minutes
nothing was watching anything, and this is the only RED in the whole window since 17:13.
The nightly archiver ran 20:30–20:53 inside that gap, which makes launchd deferring the
interval job under load the first hypothesis — untested, and stated here as a hypothesis
rather than a cause.

## §6 ESCALATE

```
ESCALATE:
1. HEARTBEAT BLACKOUT — 4 beats missing 2026-09-09 19:59 → 21:14 ET (75 min unmonitored,
   the only RED since 17:13). Self-detected correctly on the first beat back. Cause
   unknown; the archiver's 20:30-20:53 run sits inside the gap. Needs a ruling on whether
   launchd interval deferral under load is acceptable or needs a guard.
2. NO psql CLIENT ON THE HOST — not on PATH, not in libpq, no Postgres.app. Postgres is
   reachable only via `docker exec cobalt_memory psql` or the repo's psycopg path. Sol's
   sandbox network flag IS honoured, so this is a missing-binary problem, not a sandbox
   one. Decide: install libpq, or standardise every worker DB read on a repo command.
   Sub-item, UNVERIFIED and untested this run: a psycopg connection to localhost:5432
   from inside the Sol sandbox is expected to work but was not proven.
3. cobalt_app CANNOT SEE "user".vault_writes through information_schema (0 rows as that
   role; the table is visible as superuser). Either a deliberate grant boundary or a
   missing privilege. Worth one look before anything else relies on the app login for
   audit reads.
4. SYNTHETIC EXAMPLE SCREEN DOES NOT EXIST — the repo is supposed to ship one, and only
   its planned mention exists (`docs/00 - Project/SPRINT-LADDER-v0_1.md:475`). S2-P1 has
   no repo-side fixture to develop against. Write required.
5. DAY SCAN GATE IS PROSE, NOT A FIELD — "after 10:00 ET (window per F1 session clock)"
   is unreadable by S2-P1. If the gate must be enforced rather than remembered, it needs
   a machine-readable field. Write required, and it touches user data in the vault.
```

Items 1 and 3 are the two that could bite production. Items 4 and 5 are S2-P1 blockers,
both writes, neither taken today.

## §7 Closing state

- **`git status`: DIRTY** — `configs/cobalt/rules.yaml` and
  `docs/40 - DevDocs/reports/seat-usage.md`, both declared generated paths, both due to
  be absorbed by tonight's 23:37 run. This report adds a third untracked file.
- **`main` = origin + 1** — `## main...origin/main [ahead 1]`, the 23:37 commit
  `f646640`, unpushed as designed.

This report is not committed. It commits at day close.
