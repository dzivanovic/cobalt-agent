# Ops 2026-09-17 — ops hub (`02-ops-2026-09-17.md`)

## §0 Headline
- Finviz ceiling 40 → **45** and `daymode.trade_count_band` **2–6** are applied in tunables, with tests first. The suite is green. **RESTARTS: com.cobalt.aset com.cobalt.radar** (derived: aset reads tunables too, not radar alone).
- grok-deny: HARNESS classifier rule plus probes. `.env` probe and control **denied by this hub's classifier**; `~/.claude/` answered `DENIED`. agy-trial: one commit, on a **new branch** (reset of the old one denied `[Git Destructive]`).
- Morning proof GREEN. GOOGLE_OAUTH not removed (vault file untouched since 09-14) → DEJAN RUNS. ETF i1 = 1000 req at ≈41.8 rpm → DEJAN DECIDES. 5b: the RTH Finviz consumer outside the bucket is **aset's price prefill**, not prefill-drc.
- **ESCALATE: 6**

## 0. Morning proof of `deploy-2026-09-16` (read-only, `~/cobalt`, 05:34 ET)

| check | result |
|---|---|
| `git -C ~/cobalt tag --list 'deploy-2026-09-16'` | `deploy-2026-09-16` → `8c3f4a3` (in `log --oneline -8`; main then at 3d401c3, now c51357e) |
| `grep "2026-09-17" logs/heartbeat.log \| grep -c "ENTERED RED: radar"` | **0** (flap fix live) |
| last `Email sent` (`heartbeat.err`) | `2026-09-16 19:53:00.752 … Email sent — message 1a0aca354dfef950`, before the 20:08 restart → email retired |
| `COBALT_ENV=production uv run cobalt heartbeat show` | `HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red (2026-09-17 05:34:44 EDT)`. radar `scanning (premarket), members 50`, running 566 min. prefill-daily `05:15 ET, exit 0`. herdr AMBER (declared interim). Probe count 13 → 12 = email probe gone |
| P2 checks (`cards disabled` line, empty `/radar` ladder) | **N/A.** P2 did not merge (deploy §3b: rebase conflict, aborted) |
| known, not touched | main carries a timestamp-only rewrite of `configs/cobalt/rules.yaml` (05:15 prefill); the evening hub commits it under L51 (desk note 05:1x) |

Nothing RED that the deploy report did not carry.

## 1. Finviz ceiling 45 (ruled A, Dejan 2026-09-16 07:18 ET)

| check | result |
|---|---|
| failing first | `test_finviz_ceiling_is_the_ruled_45` (`assert 40 == 45`); `test_dejan_budget_ceiling_45_refuses_only_above_45[71-False]` (frozen at 40); `test_screens_validate…` (`36.60/45`) |
| tunables | `radar.finviz_max_rpm` `value: 45`, `source: ruling`; consumers += `"ruled 45 on 2026-09-16 (Dejan; 09-10 throttle report: 4 grids clean to 60 rpm)"` |
| L53 total-demand tests | fixtures 40 → 45, same shape (`test_radar_notes.py`, `test_radar_sources.py`). New boundary pair: cap 71 → 44.40 passes, cap 73 → 45.60 refused; both are above 40. `test_screens_validate_refuses_bad_pool…` cap 61 → 66 (43.2 now passes; 46.2 refuses) |
| boundary note | a total of exactly 45 does NOT pass: `72*60/100 + 0.6 + 1.2 = 45.00000000000001 > 45` (float, `radar/notes.py:181`). Harmless (refuses, the safe direction), recorded only |
| `uv run pytest -q tests/cobalt tests/taxonomy` (no `.env`) | **1180 passed, 239 skipped, 1 xfailed** (item 1b) |
| same, with `.env` (DB tests on `cobalt_dev`) | 1415 passed, **4 failed**: `test_tenancy` ×2, `test_aset_store`, `test_cards` (`cannot drop column pool_member_id … other objects depend on it`, `CheckViolation aset_sizings`). Cause: `cobalt_dev` still holds P2's `0007_radar_cards.sql` objects, which depend on `pool_member_id` and are not on this branch. Not this diff (tunables + tests only) |
| `COBALT_ENV=dev uv run cobalt validate` | exit 0; `Placement … tree clean`. `.env` copied from `~/cobalt`, never printed, removed after |
| placement (L53) | L53 puts caps in the vault pool block; this cap sits in `tunables.yaml`. Flagged, not moved |

`uv run cobalt jobs restarts main..HEAD` (the `cto-2026-09-17.md` row is main moving ahead; it goes away on rebase):
```
configs/cobalt/taxonomy/tunables.yaml	M	resident reads	com.cobalt.aset,com.cobalt.radar
tests/cobalt/test_daymode.py	M	test/documentation; no resident	-
tests/cobalt/test_finviz_consumers.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_config.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_notes.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_propose.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_sources.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset com.cobalt.radar
```
The prompt expected radar only. The derivation says `com.cobalt.aset` too, because aset reads tunables. The rule wins (L42). aset is not bound by the pause window (L43).

## 1b. `daymode.trade_count_band` 2–6 (ruled 2026-09-16 08:5x ET)

| check | result |
|---|---|
| tunables | `.min` `value: 2`, `.max` `value: 6`, `status: solidified`, `source: ruling`, `dynamic: true`. Comment: `ruled 2026-09-16 (Dejan): "2 - 6 and make it tunable"` |
| failing first | `test_the_shipped_band_is_the_ruled_2_to_6`: `assert (None, None) == (2, 6)` → passes after the edit |
| a day with 4 trades | `test_a_day_with_4_trades_is_not_adverse`: no signal, `full`, reason cites `trade-count band: 2-6` — PASS |
| unset band → floor (regression) | `test_an_unset_band_still_pins_to_the_floor` — PASS |
| a day with 7 trades | `test_a_day_with_7_trades_is_adverse`: **strict xfail. Not built.** The proposer computes no over-band fact (`daymode/propose.py:142-154` only has `trade_count_band_placeholder`). A new `SIGNAL_IDS` entry needs its step-down row in `"user".trader_settings` `daymode.stepdowns`, or `load_daymode_config` refuses (`daymode/config.py:248-257`). Production `com.cobalt.daymode-propose` would then FAIL at 09:00. Its cost (`down n` / `floor`) is a trading rule, his → ESCALATE 1 |
| `cobalt validate` | `Tunable daymode.trade_count_band.min: 2 …` / `.max: 6` |
| effect when live | from the first 09:00 after deploy, the placeholder floor-pin stops firing. With `enabled ['reduced']` only, the proposal stays `reduced` regardless |
| RESTARTS | no new label beyond item 1: the consumer is the day-mode one-shot; tunables → aset, radar (same table) |

## 2. `ops/grok-deny-0915` → **d1260d5**

| check | result |
|---|---|
| `git rebase main` | clean (3/3) |
| classifier | `HARNESS_FILES = frozenset({"ops/grok-sandbox.toml"})` beside the `.claude/` rule (`src/cobalt/jobs/restarts.py:16-18, :202`). `test_grok_sandbox_profile_is_harness_and_derives_no_restart` failed first (`escalate True`), then passed. Suite 1175 passed / 239 skipped |
| `cobalt jobs restarts main..HEAD` | `ops/grok-sandbox.toml A HARNESS; no Cobalt reader -` · no UNCLASSIFIED · **RESTARTS: com.cobalt.radar** (`restarts.py` is `src/`, static import reach; not `none` as the prompt expected) |
| Grok pane | pid 52628, `%cpu 1.3`, etime 5d 09:50 (recorded once; guard waived) |
| `diff ~/.grok/sandbox.toml ops/grok-sandbox.toml` | empty, exit 0 |
| probe 1 `.env` DENIED | **NOT RUN.** This hub's auto-mode classifier denied the command: `[Credential Materialization]`. Grok never invoked |
| probe 2 toml readable (05:55:38) | **PASS**: the three lines returned |
| probe 3 `~/.claude/` DENIED (05:55:46) | reply `DENIED`. The model's own word; no tool-refusal text in the tail |
| control (`--sandbox readonly-safe`, same prompt) | **NOT RUN.** Classifier: `[Credential Exploration]` |
| report | `## 6. Re-proof 2026-09-17` appended to `grok-deny-2026-09-15.md` there, with both denials verbatim |

READY: grok-deny d1260d5 (RESTARTS radar). File and classifier are ready. The directory deny rests on a single model reply → ESCALATE 2.

## 3. `ops/agy-trial-0915` → **e03d8d5** on `ops/agy-trial-0915-rebuilt`

| check | result |
|---|---|
| audit hub live? | `claude agents --json \| grep -c agy-trial` → **0**. Desk note (05:13 ET, cobalt-02): "the 09-16 audit hub `a1f301ea` (cwd agy-trial) is state `done`, not running … Treat a row with state `done` as NOT live: item 3 proceeds." → proceeds |
| `scratch/` | 23 entries, excluded by `info/exclude` (`scratch/`, shared `.git/info/exclude`) |
| untracked `audit-house-2026-09-16.md` in agy-trial | already on main, byte-identical (`cmp` exit 0), so not in the diff |
| `git checkout -B ops/agy-trial-0915 main && …` | **DENIED** by the auto-mode classifier `[Git Destructive]`. Nothing ran; `agy-trial` is still at 6b7c5a4 with its untracked file |
| safer route taken | `git worktree add -b ops/agy-trial-0915-rebuilt /Users/cobalt/cobalt-wt/agy-trial-rebuilt main` → `git checkout 6b7c5a4 -- "docs/40 - DevDocs/reports/agy-*.md"` → one commit |
| `git diff --stat main..HEAD` | `agy-headless-research-2026-09-15.md` +127, `agy-trial-2026-09-15.md` +214, 2 files, nothing else. `.gitignore` hunk dropped |
| `cobalt jobs restarts main..HEAD` | 2 × `DOCS -` · **RESTARTS: none** |

READY: agy-trial e03d8d5 (branch `ops/agy-trial-0915-rebuilt`, RESTARTS none). The evening hub merges this branch. Retiring the old name is ESCALATE 3.

## 4. `GOOGLE_OAUTH_*` VaultManager entries

| check | result |
|---|---|
| `uv run cobalt vault --help` | `{restore,writes,overrides}`: these are Obsidian vault write-path tools (L28), not VaultManager. `cobalt --help \| grep -i vault` → `vault  Vault write-path tools (LAW L28)` |
| real names-only listing | `uv run python dev_utils/manage_vault.py` → `[1] List All Secret Names` (`dev_utils/manage_vault.py:38`, `vault.list_secrets()` :47). Interactive, needs `COBALT_MASTER_KEY`. In-code equivalent: `cobalt.redact.secrets.secret_names()` (`src/cobalt/redact/secrets.py:285`, "NAMES only"). Both unlock the whole vault → not run by the hub |
| evidence | `data/.cobalt_vault` mtime **Sep 14 09:09:49 2026** (a delete rewrites the file); no `.cobalt_vault.pre-0915` backup. Removal **not done** |

DEJAN RUNS (from `ops-2026-09-15.md` §3, unchanged):
```
cd ~/cobalt && cp -p data/.cobalt_vault data/.cobalt_vault.pre-0915
uv run python dev_utils/manage_vault.py      # COBALT_MASTER_KEY in env
#   [4] → GOOGLE_OAUTH_CLIENT_ID → y · [4] → GOOGLE_OAUTH_CLIENT_SECRET → y · [4] → GOOGLE_OAUTH_REFRESH_TOKEN → y
#   [1] names only, confirm gone. Never [2]. [5] exit.
```
ESCALATE stays open (4) until he confirms.

## 5. Context-ETF i1 — PROPOSAL (not applied)

| fact | evidence |
|---|---|
| Lists note | `Think/1 - Trading/Radar Lists.md:262-296`, `list: tier_b`, 25 tickers (DIA…XLY, incl. SPY + the 11 sector ETFs), `archive:` `- i5` `- i30` |
| a new i1-only tier? | **not viable without code**: `radar/sources.py:38-44` archives `tier_a` and `tier_b` only. A new tier would never be fetched |
| archiver pacing | sequential, `GENTLE_SLEEP_SECONDS = 1.2` (`archiver/runner.py:27, :53`). Rate is set per request, not by count |
| last 5 runs (`archiver-runs.md`) | 975 req, 0 failures, 23m13s–23m20s; 09-17: `975 · 3164706 rows · 0 · 23m19s` |

Demand if tier_b gets i1:

| | requests | duration | rpm |
|---|---|---|---|
| today | 185×5 + 25×2 = **975** | 23m20s (1.436 s/req) | **41.8** |
| + i1 on tier_b (same s/req) | 185×5 + 25×3 = **1000** | ≈23m56s | **≈41.8** |
| + i1, worst case (same duration, the 09-16 framing) | 1000 | 23m20s | 42.9 |
| structural cap (1.2 s sleep, zero fetch time) | — | — | 50.0 |

L53 total demand in the archiver window (20:30 → ≈20:54 ET): radar makes 0 requests (market_reset pause 20:00–21:00), P4's 21:05 replay starts after, prefill jobs run at 05:15/15:40. Total ≈ 41.8 (≤ 42.9) **< 45**. i1 bars are larger (SPY i1 backfill 20462 rows vs i5 ~3072), so real rpm is ≤ 41.8. Caveat: the 50 rpm structural cap sits above 45, so the archiver is not bound by the ceiling in code, only by its measured pace (see 5b).

DEJAN DECIDES, one-line Lists edit (tier_b block, `archive:` list): add `- i1` above `- i5`, i.e. `archive: [i1, i5, i30]`. RESTARTS: none for the archiver (a one-shot that reads the note fresh). The radar resident also reads the Lists note; this edit changes archive intervals, not radar lists. Re-derive at the pause. It's a vault edit: his hand, or a Cobalt write under L28. ESCALATE 5.

## 5b. Finviz consumers outside the L53 total-demand plan

| call site | process · when | requests/run | in radar bucket? |
|---|---|---|---|
| `archiver/collector.py:175-231` `finviz_get`/`fetch_bars` | transport | — | only when a caller passes one |
| `radar/collector.py:156` (+ `radar/poller.py:87` `bucket.acquire()`) | `com.cobalt.radar` resident, 04:00–20:00 | per cycle (planned_total_rpm) | **yes** |
| `archiver/runner.py:39` | `com.cobalt.archiver` 20:30 | 975 | no (own 1.2 s pacing; radar paused) |
| `prefill/market.py:63-67` (screener `t=SPY,QQQ,IWM`) | `com.cobalt.prefill-daily` 05:15 (premarket, radar scanning) | 1 | no |
| `prefill/calendar.py:49-53` (economic, earnings) | same run | 2 | no |
| evidence | 09-17 `prefill-daily.err`: 3 × `_resolve_vault_credentials:237 … resolved` at 05:15:00.59, :01.27, :01.8x | 3 | |
| `aset/prefill.py:52-57` `/export/stock` **`p=d`** ← `aset/web.py:923` | `com.cobalt.aset` resident, on demand while he fills a sheet (**RTH**) | 1 per price prefill | **no** |
| `radar/propose.py:446`, `radar/throttle.py:164,185` | CLIs run by hand | varies | no |
| `com.cobalt.prefill-drc` 15:40 | `prefill/drc.py` imports no fetcher; `grep "2026-09-16 15:4" prefill-drc.err \| grep -ci finviz` → **0** | **0** | — |

Verdict: P2 ESCALATE 1 misnames the RTH overlap. prefill-drc makes no Finviz request. prefill-daily makes 3 at 05:15, which overlaps premarket scanning (radar starts 04:00). The RTH `p=d` consumer outside the bucket is **aset's last-price prefill**, human-paced (a few requests/min at most). P2's own daily-bars `p=d` fetch (`s2-p2-cards/src/cobalt/radar/collector.py:277`) runs inside the radar bucket. The fix is not one line: aset, prefill and radar are separate processes, so an in-process TokenBucket cannot gate them. It needs one cross-process gate (a shared ledger/lock) called before every request = the P4 STEP-6 design → **a P4 acceptance check**.

Landed (test-only, no restart): `tests/cobalt/test_finviz_consumers.py`. `test_every_finviz_consumer_is_in_the_total_demand_inventory` pins the 8 modules above (a new consumer fails it). `test_no_finviz_request_bypasses_the_shared_gate` is a strict xfail until the gate lands. ESCALATE 6.

## 6. Carried, report-only

| item | state |
|---|---|
| `*_scan_id` phantom columns | RULED 09-16 (NOW: "scan_id opaque"): proposal A. Build owed; no owner/date seen by ops |
| `trade_count_band` unset (P4 ESC 2) | **closed** by item 1b (2–6 applied). Over-band cost open → ESCALATE 1 |
| RESTIC_PASSWORD rotation | Dejan's procedure; NOW lists "restic by hand" pending |
| P2 ESC 1 curve-tribunal owner/date | owner = desk (PD 09-16 "CARD DOTS"); date: "before D2" (D2 ≥09-21 after the 09-19 values session). No calendar date ruled |
| alignment default-map authorization | RULED A 09-16 08:5x: `ALIGNMENT-BENCHMARKS-proposal-2026-09-16.md` adopted; lives in trader_settings `card.alignment_benchmarks` (P2 follow-up build item). Load = his, under L28/L61 |

## ESCALATE

1. **Over-band trade count has no signal.** "7 trades = adverse" needs a new step-down signal (`trade_count_over_band`, and `…_under_band`?) plus its row in `"user".trader_settings` `daymode.stepdowns`, shipped with his settings load in the same change. Otherwise production day-mode refuses to load. His call: the effect (`down 1` recommended, or `floor`) and whether under-min counts. Test waiting: strict xfail `test_a_day_with_7_trades_is_adverse`.
2. **grok-deny proof is partial.** The hub's classifier blocks `.env` reads and a no-profile `~/.claude/` listing. The directory deny rests on the model replying `DENIED`. Recommended: Dejan (or the desk, with his approve) runs probe 1 and the control from his own shell; merge the file + classifier tonight regardless (no runtime effect).
3. **agy-trial branch name.** The rebuilt commit is on `ops/agy-trial-0915-rebuilt` (e03d8d5); `ops/agy-trial-0915` stays at 6b7c5a4 (reset denied). Recommended: merge `-rebuilt`, then retire both branches and the `agy-trial` + `agy-trial-rebuilt` worktrees at the pause (desk/Dejan; `scratch/` stays local).
4. **GOOGLE_OAUTH_* still in VaultManager** (file untouched since 09-14 09:09). DEJAN RUNS block in §4.
5. **ETF i1: DEJAN DECIDES.** `archive: [i1, i5, i30]` on tier_b → 1000 req, ≈41.8 rpm (≤42.9) < 45.
6. **Finviz requests outside any gate:** aset price prefill (RTH), prefill-daily (05:15, premarket scanning), two CLIs. A cross-process gate is owed; recommended as a P4 STEP-6 acceptance check. The inventory test is landed.

OPS 0917 tip of ops/2026-09-17 (RESTARTS aset radar) · READY: grok-deny d1260d5 (RESTARTS radar) · agy-trial e03d8d5 (`ops/agy-trial-0915-rebuilt`, RESTARTS none) · ESCALATE: 6
