# ops-draft-0921 — report (2026-09-21)

Seat: prompt drafter `ops-draft-0921`, Opus 5, launched by the CTO desk. Source prompt: `docs/40 - DevDocs/prompts/2026-09-21/05-draft-ops-0921.md`. Written 06:03 ET (`date`: Mon Sep 21 06:03:00 EDT 2026). Nothing launched, nothing run against production, no git write, no restic, no secret file opened.

## §0 Headline
- DRAFTED (A) `reports/restic-rotation-steps-2026-09-21.md`: 23 steps for his hand. The method is `restic key add` → prove → `key remove`. The new value goes in vault key `RESTIC_PASSWORD`. Step 20 (the README on the backup disk) is left BLANK: it waits for his ruling.
- DRAFTED (B) `prompts/2026-09-21/06-ops-0921.md`. LAUNCH 1 is Sonnet 5 and runs items 1–5, 6a and 7 offline. LAUNCH 2 is Opus 5, runs step 6b only, and does a production restore read after he has rotated.
- RULE PROOF: 21 strings copied from `16-ops-2026-09-19.md`, each count 1. **NEW strings: 4** (3 allow + the standard push deny). The desk fills `R__` with his approval row.
- Item 2 is half done already: `UNATTENDED-LAUNCH.md:9` has carried the push deny since `40f12ac`, so one line remains.
- ESCALATE: 9 (5 `ASK DESK`). NOT KNOWN facts: 4.

## Backup FACTS
| fact | file:line | value or NOT KNOWN |
|---|---|---|
| Repositories | `configs/cobalt/backup.yaml:57-68` | ONE armed: `ssd`, local, `/Volumes/COBALT-BACKUP/restic`, `requires_mount /Volumes/COBALT-BACKUP`. `b2` configured and `enabled: false`, repo `""` |
| Where the password is read at run time | `configs/cobalt/backup.yaml:71`; `src/cobalt/backup/restic.py:137`; `src/cobalt/backup/secrets.py:28-40` | Vault key NAME `RESTIC_PASSWORD`, from Cobalt vault file `VAULT_FILE`. Handed to restic only as `RESTIC_PASSWORD` in the child env (`restic.py:19-22`) |
| Vault file path | `src/cobalt/redact/secrets.py:43` | `REPO_ROOT/data/.cobalt_vault`, i.e. `~/cobalt/data/.cobalt_vault`. `backup/secrets.py:35` uses `VAULT_FILE` directly, not the `COBALT_VAULT_FILE` override, so a worktree run cannot find it |
| Master key reaches the job | `ops/run_backup.sh:26-32` | `source ~/.cobalt_key` (exports `COBALT_MASTER_KEY`; inferred from the child `uv run` succeeding nightly, `logs/backup.err` DONE lines) |
| Interactive tool that writes a vault value | `dev_utils/manage_vault.py:63-68` → `src/cobalt_agent/security/vault.py:92-101` | Old-tree `VaultManager`, menu 3, hidden input (`password=True`). **Non-atomic** write (`open(…,"wb")`), vault path CWD-relative `data/.cobalt_vault` (`vault.py:19`). New-core `put_secret` (`redact/secrets.py:250`) is atomic but has NO CLI caller |
| When the job fires | `ops/com.cobalt.backup.plist:34-38, 71-80` | 21:40 ET, every day of the week |
| How long it runs | `logs/backup.err` (09-15…09-20) | 21:40:00 → 21:40:07–12, ~8 s. Timeout 3600 s (`RUNNING (timeout 3600s…)`) |
| Last snapshot | `logs/backup.err`, last line group | 09-20 21:40:07 `3bed3a73`, DONE |
| Include set | `configs/cobalt/backup.yaml:37-46` | `/Users/cobalt/Vault/Think` + a fresh `cobalt_brain` dump staged at `~/cobalt-backups/staging/cobalt_brain.sql` (`restic.py:48`). **Key store NOT included** |
| `cobalt backup` commands | `src/cobalt/backup/cli.py:3-6, 24-42` | `run [--dry-run]` · `status` · `restore <dest> <id> --into DIR [--include PATH]` |
| Working restore shape | `topics/cto-desk.md:62` (09-17 21:48 ET) | `COBALT_ENV=production uv run cobalt backup restore ssd latest --into <tmp> --include "<file>"` |
| restic version | `ls -la /opt/homebrew/bin/restic` → `../Cellar/restic/0.19.1/bin/restic` | 0.19.1 (has `key list/add/remove/passwd`) |
| Plaintext password copy on the backup disk | `ops/com.cobalt.backup.plist:11-15`; `PROJECT-LEDGER.md:1225, :1228` | `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt` (0600, 554 bytes; holds a `RESTIC_PASSWORD=` line). Read into a chat 09-11, so EXPOSED. Its fate is owed to him (`LEDGER:1229`). **Not opened by this drafter** |
| Recovery copy in a password manager | `PROJECT-LEDGER.md:1225` ("the vault copy plus LastPass already covers recovery", 09-11) | Stated 09-11. **NOT KNOWN** whether it still holds the current value |
| Heartbeat backup probe | `src/cobalt/heartbeat/runner.py:144`; `ops/com.cobalt.heartbeat.plist:60-61` | `backup_freshness` every 900 s |
| Keys in the repository today | — | **NOT KNOWN** (restic not run). Step 4 stops if the count is more than 1 |
| Whether a hub session's env carries `COBALT_MASTER_KEY` | — | **NOT KNOWN**. The desk's 09-17 restore worked, but how is not recorded. 6b fails closed on it |
| Which files make up "the Cobalt key store" | `PROJECT-LEDGER.md:1174` | **NOT KNOWN** (vault file only, or also `~/.cobalt_key`). `ASK DESK`, with the default "vault file only" |

## Files written
| path | size | seat |
|---|---|---|
| `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/restic-rotation-steps-2026-09-21.md` | 6,394 B | ops-draft-0921 (Opus 5), for Dejan's hand |
| `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/06-ops-0921.md` | 29,377 B (final) | ops-draft-0921 (Opus 5), for the `ops-0921` hub |
| `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/ops-draft-2026-09-21.md` | this file | ops-draft-0921 |

All three are UNTRACKED on `~/cobalt`. This seat has no git write, so the desk commits them by path.

## RULE PROOF for (B)
`grep -c -F` against `prompts/2026-09-19/16-ops-2026-09-19.md`, one call each. Every result = **1**: `"Bash(uv run pytest *)"` · `"Bash(uv run cobalt jobs restarts *)"` · `"Bash(git add *)"` · `"Bash(git commit *)"` · `"Bash(git diff *)"` · `"Bash(git status*)"` · `"Bash(git log*)"` · `"Bash(git show*)"` · `"Bash(git -C /Users/cobalt/cobalt log*)"` · `"Bash(cd *)"` · `"Bash(mkdir -p *)"` · `"Bash(ls *)"` · `"Bash(grep *)"` · `"Bash(tail *)"` · `"Bash(wc *)"` · `"Bash(date*)"` · `--disallowedTools "AskUserQuestion" "EnterWorktree"` · `--add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`. The last two needed `-e`: without it, this host's `grep` (ugrep) read the `--…` pattern as an option and exited 2. The prompt's AUTHORIZATION now says `-e`. `19-ops-round2.md` line 1 copies the same set byte for byte (its own claim, `19-ops-round2.md:1`).

**NEW strings: 4**
- N1 `"Bash(git check-ignore *)"` (LAUNCH 1, item 7). `grep -c -F "git check-ignore"` = 0 in 16 and 0 in 19.
- N2 `"Bash(COBALT_ENV=production uv run cobalt backup status)"` (LAUNCH 2).
- N3 `"Bash(COBALT_ENV=production uv run cobalt backup restore ssd latest *)"` (LAUNCH 2). `grep -c -F "cobalt backup"` = 0 in 16 and 0 in 19.
- N4 DENY `"Bash(git push*)"`. Not in 16/19; `grep -c -F` in `UNATTENDED-LAUNCH.md` = 1 (standard since 09-20 R12). A deny, so it grants nothing.

## Items of (B) that need a second house before they run (L67)
1. **Step 6b.** A production read (a full restore of the backup, plus a byte-compare of the encrypted vault file). The prompt's 6b section should be read by another house before LAUNCH 2.
2. **Item 6a.** A BUILD (config line + test). At least three houses check it before any deploy prompt carries it (L67 build rule). It has no production effect until merged.

Items 1, 2, 3, 4, 5 and 7 are docs, `.gitignore`, reads or an offline suite, with no production effect. RECOMMENDED but outside (B): step list (A) touches the only backup's password, so one other house should read it before he runs it (floor of L67; a mistake there costs the backup).

## READING (my narrowest readings, for the desk)
1. `UNATTENDED-LAUNCH.md` is a STANDARD, so it is edited in place. It has four in-place desk commits (`54d869e`, `0e52e5f`, `848e28e`, `40f12ac`) and calls itself "stable file, dateless". L19's re-issue applies to prompts.
2. Item 2's push line is already present (`UNATTENDED-LAUNCH.md:9`, `40f12ac`; `cto-2026-09-20.md:192`), so the hub adds only the wrap line and verifies the push line.
3. 6b runs on Opus 5 as a RELAUNCH of the same prompt file (L50). The other option was a `claude -p` headless builder, which would need a fifth new string, and L36 is cleaner without it.
4. In (A) he types the OLD password himself from LastPass. The alternative, `--password-command` feeding it from the vault, would be the model designing a secret pipe. Brief: "he types every secret".
5. `key add` → prove → `key remove`, never `key passwd`. passwd replaces the only key in place; add/remove keeps the old key valid until the new one is proven (steps 7 and 15).
6. Item 4 is an old-tree test (`tests/test_finviz_extractor.py:34` imports `cobalt_agent.*`). Disposition is a BACKLOG line only, with no `pyproject.toml` change. `testpaths = ["tests"]` (`pyproject.toml:91`) is why only a bare `pytest -q` sees it. The hub runs `--co` first and a bare `pytest -q` only if collection fails, so old-tree tests that could reach the network never execute.
7. Item 5: `wc -c` counts bytes but the L58 cap is characters. The 09-20 "margin 6" is a byte margin, so the hub measures both.
8. The vault write in (A) step 11 uses old-tree `dev_utils/manage_vault.py`, the only interactive writer. The new-core atomic `put_secret` has no CLI. Step 10's encrypted safety copy covers the non-atomic write.
9. 6b proves byte identity with `git diff --no-index --stat`, which is already allowed and prints no binary content. No `cmp`/`shasum` string is added.

## ESCALATE
1. `ASK DESK: the README on the backup disk — update it to the new password, or delete its password line (vault + LastPass as recovery)? His ruling, owed since 09-11 (LEDGER:1225, :1229). (A) step 20 is BLANK until he rules; the rotation itself does not wait on it — after step 13 the old password opens nothing. [06:03]`
2. `ASK DESK: "the Cobalt key store" for the include set — vault file only, or also ~/.cobalt_key? Both in one repository lets the restic password alone open every secret. (B) 6a default: vault file only. [06:03]`
3. `ASK DESK: the vault write in (A) step 11 uses the old-tree non-atomic manage_vault.py (the only interactive writer; step 10 takes an encrypted safety copy first). Acceptable, or should a Cobalt secret-set command over put_secret be built first? Default: as written. [06:03]`
4. `ASK DESK: does a --bg hub session's environment carry COBALT_MASTER_KEY? NOT KNOWN. (B) 6b's first production command fails closed on it; the desk may prefer to run 6b's restore itself, as it did 09-17. [06:03]`
5. `ASK DESK: item 7 — three named .gitignore paths (default), or a standing pattern for future settings packets (R40 "from now on")? [06:03]`
6. OWED, not in (B): the "fresh FULL restore proof" (BACKLOG.md:421) includes reloading the restored `cobalt_brain.sql` into a scratch database. No approved line has a DB rule, so 6b proves the dump is present and sized and marks the reload OWED.
7. (A) should be read by one other house before he runs it (see the second-house section). Desk's call, per L73: this is a recommendation, not a waiver.
8. `~/cobalt` is not clean and none of it is mine to touch: ` M configs/cobalt/rules.yaml`, untracked `reports/bars-r3-draft-2026-09-21.md`, the three settings-packet paths (item 7), and this seat's three files (above).
9. `cto-2026-09-21.md:28` pairs item 3 with "the empty 'Cards apply' tab `w2:t2C`". A herdr tab close is a desk action, so it is not in (B).

## CONTINUE
Done. The desk's next steps: read (A) and (B) end to end; commit the three files by path; fill `R__` in (B) after his ONE approval of N1–N4; bring him ESCALATE 1–5 one per message.

OPS 0921 DRAFTED · files: 2 · restic steps: 23 · NOT KNOWN facts: 4 · new rule strings: 4 · items needing a second house: 2 · READING: 9 · ESCALATE: 9
