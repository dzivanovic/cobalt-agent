AMEND BEFORE BUILD: one BLOCKER remains (R1-10); six AMEND findings remain or arise from the amendments.
R2 reviews amendment adequacy only; 2026-09-15 rulings R1–R11 stand. Unlisted R1 findings are closed as plan requirements, with implementation proofs still owed.
Evidence alias PLAN = docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md. Read-only review except this report; RESTARTS: none.

| id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording |
|---|---|---|---|---|---|
| 1 | BLOCKER | STEP-1 / STEP-4; R1-10 retained in part | Immutable snapshots are now required, but their storage home, run linkage and tenancy are still absent from the migration contract. Dark mode still says “seam rows only,” although snapshots must persist user-side even without a card. Ordered cohort, pool-unit/payload evidence and evaluation time are not explicitly frozen by the amendment; a cohort hash remains insufficient. This leaves the dark-day audit unable to rely on a defined, recoverable input artifact. | PLAN:95–96,173–177,191–192,270 | Add an immutable user-side run-input receipt to 0007, with user_id NOT NULL, trader FK/GUC default, run FK, explicit owner/grants and placement/rollback coverage. Store the exact consumed inputs required above plus evaluation time, ordered cohort/tie policy and pool-unit/payload bytes and hashes. Dark mode persists seam rows AND these receipts, but no cards or dots; COMPLETE requires the receipt durable. Replay/export reads the receipt, not current bars or mutable dots. |
| 2 | AMEND | STEP-6 / STEP-7; new from R1-5 | The loader amendment adds every card.* key to trader_settings, explicitly including card.dot.* and card.health.*. STEP-5/7 still put those thresholds in tunables.yaml. This introduces two writable authorities with no precedence and undermines the per-request reread/restart contract. | PLAN:206,213,215,258–259 | Extend trader_settings only for card.proposed_key, card.curves, card.alignment_default, card.shadow_promotion_bar and radar.cards_enabled. Keep card.dot.* and the eight card.health.* engine thresholds solely in tunables.yaml, with its existing validation and restart rules. Test that settings load rejects these tunable-only keys. |
| 3 | AMEND | STEP-4 expiry; new from R1-16 | “Non-WATCH states never take EXPIRED” overcorrects the FILLED prohibition: ARMED and TRIGGERED both legally expire. Implementing that test would leave unfilled armed/triggered cards alive beyond expiry or require changing the established state machine. | PLAN:199; src/cobalt/cards/models.py:95–112 | Apply each expiry cause only to its eligible states through ALLOWED: deadline/avoid expiry may use legal WATCH, ARMED and TRIGGERED → EXPIRED edges; stop-before-arm applies to WATCH. FILLED and terminal cards never expire. Test ARMED/TRIGGERED deadline expiry and FILLED refusal separately. |
| 4 | AMEND | STEP-9 / STEP-D4; R1-17 retained in part | The hash/preflight and reread requirements close the batch-integrity gap. The amendment still only requests a “declared treatment” for pre-migration cards; it does not declare one. D2 precedes D3/D4, so existing cards can retain a dot set lacking the newly mandatory catalyst factor. | PLAN:201,219–221,271–273 | On the first refresh after a validated definition acquires catalyst, reconcile each existing nonterminal radar card's dot set through the locked card update path: add the missing catalyst dot once, hollow/shadow with DESK_NA and no trader grade; preserve taps and immutable formation evidence, and record the new definition/receipt version. Test cards formed before and after D3/D4 and repeated refreshes. |
| 5 | AMEND | §6 Acceptance / §8 item 5; R1-24 retained in part | The calendar correction is sound, but the amendment still calls the same-evening “first post-reset scan” a restart/seam proof while admitting radar is idle overnight. Renaming the proof does not make a scan occur; STEP-D1 still requires a COMPLETE run that night. | PLAN:269,274,295; src/cobalt/radar/runner.py:34,161–167 | At the evening restart prove process startup, expected paused/idle status, schema reads, panel and heartbeat. Prove the first COMPLETE score run at the next scheduled scanning session; record that separately and begin the full dark trading-day evidence period then. Do not force an overnight production scan or count restart success as score-run success. |
| 6 | AMEND | §8 item 4 / STEP-5; R1-23 retained in part | Explicit N/A closes the immediate fabricated-grade risk. However, “cite the exact dated ruling … OR obtain them fresh through reviewed trader_settings” still allows fresh numeric values to substitute for authorization of the disputed SPY-sign/default-map semantics. Values alone do not resolve the recorded semantic rejection. | PLAN:203,294 | Until an exact dated ruling authorizes the alignment mapping semantics, emit explicit N/A. Reviewed trader_settings supply numeric values only after those semantics are authorized; fresh values alone cannot authorize the rejected method. If no authorizing ruling exists, retain the item OPEN for Dejan. |
| 7 | AMEND | §6 rollback; R1-18 retained in part | The amendment adds the right safety requirements but still leaves the executable procedure unresolved: it says the steps “need” a bounded sequence or repeatability proof while retaining the defective commands. Resident shutdown before schema removal and the additional snapshot/restore operations also remain requests rather than ordered rollback steps. | PLAN:269,275–281; src/cobalt/db_migrations/__init__.py:31–46 | Replace domains 1–2 with one named hub step: stop affected residents before any reverse migration; run cobalt db migrate --rollback --down-to 0005 --allow-prod once, selecting 0007 then 0006; complete the tested selective restore and code/config revert before restarting compatible residents. Extend D1's snapshot manifest to stop edits, sequence state and written vault units; name the selective restore commands that preserve surviving and post-D1 manual work. Verify the exact procedure on populated cobalt_dev before D1. |
