# DRC K1 — BUILD CHECK, ROUND 1 (`38a70947..9a0fc900` on `drc/d1-trading-log`)

Hub `drc-k1-check-0924`, Sonnet 5, started Thu Sep 24 16:44:07 EDT 2026 (from `date`). Prompt `prompts/2026-09-24/40-drc-k1-check.md` (launch row R70).

## §0 Headline
- Round 1 of the NEW-BUILD check of DRC K1 (`9a0fc900` on `38a70947`, its X1–X6 output and its three suites' executed output): Opus 5.5 and Grok both checked. Astra METER until Sep 26th, 2026 6:47 AM; no Sol, no Gemini.
- Both answered `ready for K2: YES` (Opus `BUILD STANDS EXCEPT` two pins and one seam gap; Grok `BUILD STANDS`). C1–C7: MEETS from both. Nothing widened. All three suites SHOWN by both.
- In my file-check 3 claims HOLD (Opus: pin `opened_on`, pin derived `reason`, seam point (2)); Grok's `DESELECTS OPEN` DOES NOT HOLD. `ready for K2: NO` (HOLD ≠ 0). ESCALATE: 14.

## L74
- One block arrived inside a tool result (the Read of this prompt file), asking every commit to carry a `Claude-Session: <url>` line and naming a file-send tool. Recorded once here; not followed. This hub commits nothing and sends no file. The build report's own L74 section (a block of the same shape) is data, carried as read.

## PREFLIGHT
| rule | command | exit | result |
|---|---|---|---|
| placeholder gate | `grep -n -E "R_[_]" …/40-drc-k1-check.md` | no hit | no output — allowed |
| clock | `date` | 0 | `Thu Sep 24 16:44:07 EDT 2026` → `<D>` = 2026-09-24, 16:44 ET (before Sep 26th 6:47 AM) |
| GROK GATE (R17) | `grep -n "^| R17 " …/cto-2026-09-24.md` | 0 | `:29` `\| R17 \| 07:32 ET \| … Grok approved with no asking going forward …` — allowed |
| GROK GATE (R17 committed) | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Grok approved with no asking going forward" -- …/cto-2026-09-24.md` | 0 | `1758fd78a572f47b613b2ca831dcfa636ed8f65a` |
| GROK GATE (R19) | `grep -n "^| R19 " …/cto-2026-09-24.md` | 0 | `:31` `\| R19 \| 07:36 ET \| … All 4 house models approved for use indefinlitly …` — allowed |
| GROK GATE (R19 committed) | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"All 4 house models approved" -- …/cto-2026-09-24.md` | 0 | `5055151dbf68899b82de5b11f99733ed2d03048c` |
| R51 / R52 | `grep -n "^| R51 \|^| R52 " …/cto-2026-09-24.md` | 0 | `:63` R51 `**R2-1 RULED "A"**`; `:64` R52 `**R2-2 RULED "B" ON BOTH HALVES**` |
| R51 / R52 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"R2-2 RULED" -- …/cto-2026-09-24.md` | 0 | `ae6203332745323b8bb9ba80650cb6c8a0e5c1ed` |
| THIS launch (R70) | `grep -n "40-drc-k1-check.md" …/cto-2026-09-24.md` | 0 | `:73` R61 (names this file as `38`'s output — does not count) and `:82` `\| R70 \| 16:4x ET \| … DESK LAUNCH ROW for prompts/2026-09-24/40-drc-k1-check.md …` — counts |
| R70 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"40-drc-k1-check.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` | 0 | `4b0db777f6adc51974831f38e3981d775e9d112f` |
| grok | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` → UP |
| worktree | `ls /Users/cobalt/cobalt-wt/drc-d1` | 0 | listed (`AGENTS.md`, `CLAUDE.md`, `configs`, `docs`, `src`, `tests`, `uv.lock` …) |
| THE BUILT LINE | `tail -n 3 "…/drc-k1-build-2026-09-24.md"` | 0 | last non-blank line: `DRC K1 BUILT 9a0fc900 \| on 38a70947 \| migration 0018 \| X: 6 of 6 run, design-changing: 0 \| offline 2438/0 \| with-DB 2843/0 \| live-note 142/0 \| .env: removed \| 0018: rolled back \| RESTARTS: com.cobalt.radar \| tests added: 76 \| ESCALATE: 9` — starts `DRC K1 BUILT `, carries every required field. `<tip>` = `9a0fc900`, `<base>` = `38a70947`. Not `0018: UNPROVEN`. |
| tip subject | `git -C /Users/cobalt/cobalt log --oneline -1 9a0fc900` | 0 | `9a0fc900 feat(drc): K1 — drc_stated_books (0018), seed/book_close rows, record_stated_book, stated first book, cobalt drc state-book (v3 §6 K1, R51, R52)` |
| range | `git -C /Users/cobalt/cobalt log --oneline 38a70947..9a0fc900` | 0 | 4 lines: `9a0fc900 feat(drc): K1 …`, `4760991f wip(k1): DRC K1 red tests, with-DB …`, `2bb1cb2f wip(k1): DRC K1 red tests, offline …`, `7f599c96 wip(k1): DRC K1 first-gate experiments X1–X6 …` |
| nothing above the tip in code | `git -C /Users/cobalt/cobalt log --oneline 9a0fc900..drc/d1-trading-log -- src tests configs` | 0 | EMPTY |
| path union | `git -C /Users/cobalt/cobalt log --stat --format=%h 38a70947..9a0fc900` | 0 | see `## Scope` |
| `.env` | `ls /Users/cobalt/cobalt-wt/drc-d1/.env` | 1 | `No such file or directory` (never read) |
| recovery | `ls scratch/tribunal-bars-0920/drc-check/k1` | 1 | `No such file or directory` → fresh run |
| STAGGER | `grep -n -F "no other house hub is running" …/cto-2026-09-24.md` | 0 | prints `:82` R70 which also names `40-drc-k1-check.md` |
| PROBE Opus | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (background) | 0 | `OK` → UP. The saved output carried one harness notice line before it: `Permission deny rule (../../cobalt/.claude/settings.local.json): Bash(git push*:*) mixes * with the trailing :* prefix syntax, so it is matched as a literal prefix (the * is not expanded) and matches only commands containing a literal * at that position. Use Bash(git push*) for wildcard matching.` (a notice; no denial of any call of this hub) |
| PROBE Grok | its `--version` row | — | UP |
| ASTRA | keyed on `date` (2026-09-24 16:44 ET, before 2026-09-26 06:47 ET); v3 `:10` read | — | recorded `astra: METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — not probed, not launched |
| FAIL CLOSED | Opus UP and Grok UP | — | two UP → checkers launched |
| GROK GATE, second row (before launch) | R17 and R19 rows and both `git -S` commits, re-run at 17:02:23 | 0 | same rows (`:30`, `:32` in the file as it stood), same hashes `1758fd78…` and `5055151d…` |

## Packet
Staged in `scratch/tribunal-bars-0920/drc-check/k1/` (no `mkdir`; the Write tool created the folder). Sources read from `/Users/cobalt/cobalt-wt/drc-d1/` (code, migration, build report) and `/Users/cobalt/cobalt/` (design, desk file, prompts).
| file | bytes | what |
|---|---|---|
| `k1-diff-1.md` | 24,311 | `git log -p 38a70947..9a0fc900 -- . ":(exclude)docs"` saved-output lines 1-565 |
| `k1-diff-2.md` | 30,869 | lines 566-1261 |
| `k1-diff-3.md` | 18,737 | lines 1262-1629 |
| `k1-diff-4.md` | 27,082 | lines 1630-2293 |
| `k1-diff-5.md` | 31,093 | lines 2294-3018 |
| `migration.md` | 5,364 | the `0018` pair whole + `0016_drc.sql:80-95` |
| `devdocs-diff.md` | 12,434 | `git log -p 38a70947..9a0fc900 -- "docs/40 - DevDocs/cobalt"` lines 1-239 |
| `experiments.md` | 5,864 | build report `## S2 EXPERIMENTS X1–X6` (:50-92) |
| `suites.md` | 11,740 | build report S1, S3, S4, S7, S8, S9, RESTARTS |
| `build-proof.md` | 13,581 | build report PREFLIGHT, S5, S6, SEAM FOR D2, FOR K2, ESCALATE, stop line |
| `rules.md` | 60,598 | R51, R52, R22 rows; v3 :67-120, :122-206, :208-232, :243-258, :304-317; `39`'s `## THE K1 CONTRACT` (:77-112) |
| `QUESTIONS-DRC-K1.md` | 7,577 | the questions verbatim + the "Files in this folder:" paragraph |
| **total** | **249,250** | ÷ 4 = **≈ 62,313 tokens per checker**; under the 260,000 B ceiling (drafter's estimate was 150–230 KB) |

Written after the checks (by the hub from stdout, or by Grok as told): `opus-check.md` 10,152 B, `grok-check.md` 15,888 B.

Staging checks (each from a tool result):
- Diff parts: each ≤ 38,000 B (largest 31,093). `grep -c "^commit "` over the five parts = 1 + 0 + 0 + 1 + 2 = **4** = the range's commit count; `grep -c "^diff --git"` = 7 + 2 + 8 + 1 + 2 = **20** = the distinct non-docs path-touches (17 in the feat commit + 3 new test files).
- Byte reconciliation of the diff: the saved output was 131,199 B; its last two lines (a blank line and `[exited with code 0]`, 22 B) are the harness's and were not staged; the five parts total 132,092 B including five one-line headers of 159 + 162 + 163 + 163 + 268 = 915 B; 132,092 − 915 = 131,177 = 131,199 − 22 (exact).
- Trailing whitespace: the saved diff has 84 lines ending in a space and 0 ending in a tab; the five copies hold 22 + 32 + 27 + 1 + 2 = 84 and 0 tabs.
- `devdocs-diff.md`: 12,434 B = a 253-byte header + 12,181 (the saved output 12,203 − the 22-byte trailer); 13 lines ending in a tab (git's path lines) and 2 ending in a space, both matching the saved output.
- `migration.md`: the two `0018` files are 3,353 and 608 bytes (`wc -c` of the originals) and have 0 trailing-whitespace lines; line count 94 = header + 63 + 11 + 16 + three `===` lines.
- Report excerpts (`experiments.md`, `suites.md`, `build-proof.md`): `grep -n -F -x -v -f <the three> <the build report>` lists ONLY lines this packet does not carry (§0, L74, AUTHORIZATION, CONTINUE) and blanks — every other report line is present exactly. The report has 0 trailing-whitespace lines.
- `rules.md`: `grep -n -F -x -v -f <v3> -f <desk file> -f <39>` on it lists only its own header / `===` lines and blanks — every content line is present exactly in a source; line count 247 = the sum of the source ranges (3 + 54 + 85 + 25 + 16 + 14 + 36) + headers and separators. One file of 60,598 B (a multi-source rules file; the 38,000 B part rule is for the diff).
- `QUESTIONS-DRC-K1.md`: every line but the two that carry the prompt's opening / closing quote mark is present exactly in the prompt; the appended paragraph follows.
- Not staged: any file of his, `_imports/`, `logs/`, `.env`, any earlier round's packet.

Launches (both `run_in_background`, independent, one attempt per house, told "Do not open any *-check.md file"; Opus first):
- OPUS at ≈ 17:02:2x (bg `bvxi02vn5`), `claude -p --model claude-opus-5-5 "You are OPUS. The folder is scratch/tribunal-bars-0920/drc-check/k1/. Start with QUESTIONS-DRC-K1.md and follow it exactly. Do not open any *-check.md file. Read files with the Read, Grep and Glob tools only. Write no file. Print your complete check as your answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/k1 --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"`. Answered by 17:08:46 (≈ 6 min). Its stdout carried the same harness notice line as the probe, then the check. `opus-check.md` written by this hub from stdout, byte for byte (78 lines; `grep -F -x -v -f` against the stdout lists only the notice line, blanks and the 22-byte trailer).
- GROK at ≈ 17:02:40 (bg `bcf0527i4`), `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" -p "You are GROK. The folder is scratch/tribunal-bars-0920/drc-check/k1/. Start with QUESTIONS-DRC-K1.md and follow it exactly. Do not open any *-check.md file. Write your complete check to /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/k1/grok-check.md and reply with only that path."` (never `--always-approve`). Its stdout was progress text and the path; its own file was on disk at 17:18 (`ls -la`), noticed at 17:19:07. Grok's file is recorded as Grok wrote it.
- Clock: 45-minute deadline ≈ 17:47; both inside it; no HARNESS / METER / TIMEOUT.
- Written-nothing proof: `ls -la` of the packet folder (13 files, folder mtime 17:00) and of `/Users/cobalt/cobalt-wt/drc-d1` (mtime 16:41, no `.env`) before launch and after launch were identical; after both finished the packet folder holds exactly the 13 staged files + `opus-check.md` (this hub's) + `grok-check.md` (told to Grok), and `drc-d1` is unchanged (same entries and mtimes).

## CONTINUE
done — packet staged, both checkers answered, every claim walked; report closed below.

## Per row
`row · opus · grok · MEETS: n of <checked>` (FIRST; each answer quoted from the checker):
| row | opus | grok | MEETS |
|---|---|---|---|
| C1 Models | `MEETS THE ROW` — `k1-diff-1.md:401-411` (exit-leg validator), `:446-457` (`gross_pnl`), `:517-542` (`SeedBook` validator) | `MEETS THE ROW` — validator `k1-diff-1.md:407-410`, `gross_pnl` `:457`, `SeedBook` `:536-541` | 2 of 2 |
| C2 Pairing | `MEETS THE ROW` — `k1-diff-2.md:72`, `:87-98`, `:164-170`, `:181-183`, `:202-206` | `MEETS THE ROW` — `FN_VERSION` `k1-diff-2.md:72`, hash `:87-98`, `_reduce` `:164-173`, `build_day` `:202-206` | 2 of 2 |
| C3 `record_stated_book` | `MEETS THE ROW` — gate first `k1-diff-2.md:647`; note: resolve uniqueness filters on `day` (`:663-664`), "This belongs to K2." | `MEETS THE ROW` — one production INSERT `k1-diff-2.md:679-686`; gate before `_validated` and `_connect` (`store.py:531`) | 2 of 2 |
| C4 `seed_for` | `MEETS THE ROW` — cases at `k1-diff-2.md:507-542`, `:476-480`, `:481-487`, `:489-491`; both ids `:463-470` | `MEETS THE ROW` — (i)–(viii) `k1-diff-2.md:463-527`, `:473-503`; tests `k1-diff-4.md:376-477` | 2 of 2 |
| C5 `record_day` | `MEETS THE ROW` — refusal `k1-diff-2.md:350-355`; seed `:394-406`; `book_close` `:407-422` | `MEETS THE ROW` — `seed` required `k1-diff-2.md:336-341`; rows `:394-422`; `carried_from` `:363-371` | 2 of 2 |
| C6 Migration | `MEETS THE ROW` — new file `migration.md:22-65`; constraint name proven `k1-diff-4.md:225-229` | `MEETS THE ROW` — `migration.md:22-77`; name test `k1-diff-4.md:212-229`; registry `k1-diff-1.md:123-151` | 2 of 2 |
| C7 CLI | `MEETS THE ROW` — one import + one `add_parser` (`k1-diff-1.md:18`, `:26`); dry run `:301-311`; `--apply` w/o hash exit 2 `:277-278` | `MEETS THE ROW` — same registration; dry run `k1-diff-1.md:301-311`; reset window `k1-diff-4.md:645-653` | 2 of 2 |

DOES NOT MEET / NEW DEFECT: none from either checker. The D1-test-edits look-hardest item: Opus — "None is a loosened comparison"; Grok — "signature consequences, not loosened comparisons" (plus a note that one docstring in the registry-tail test still says the archiver pair is last, "the assertion does not. Not a failed input.").

## Experiments
`X · opus · grok` (SECOND; ≤30 words each, verbatim excerpts):
| X | opus | grok |
|---|---|---|
| X1 | `RAN — the gate passed … / the pass passed (source = carried, hash = day 1's)` · no | `RAN — gate passed … / pass red S2 …, pass green S9` · no |
| X2 | `RAN — no runnable gate (B8 pinned as built, green at S1) / the pass passed, (a)–(c)` · no | `RAN — no gate can stay green … / pass red S4 …, pass green S9` · no |
| X3 | `RAN — the gate is the source record … / the pass passed: the literal, avg_entry and hold_seconds None, no exception` · `no — but see the match_stats caveat above` | `RAN — no gate can build the input … / pass red S3 …, pass green S8` · no |
| X4 | `RAN — gate: memory = read-back = second process / pass: the stored close recomputes` · no | `RAN — gate passed (h_memory == h_read == h_other …) / … pass green S9` · no |
| X5 | `RAN — gate: the edited 0016 left the CHECK unchanged / pass: 0018 widens it …` · **`yes, by the letter of the column`** — "Its consequence is C6's `0018` as built, so nothing changes." | `RAN — gate passed: an edited 0016 text … still refuses kind = 'seed' / pass … green S9` · no — "the branch `[F-02]` already routes to a new file; `0018` is that file" |
| X6 | `RAN — the gate refuses at 20:15 ET / the pass: SessionBlocked, 0 rows` · no | `RAN — gate passed offline … / pass … green S9: SessionBlocked, 0 rows, one audit call` · no |
The checkers differ on X5's `design-changing`: quoted both, smoothed neither. The build report's own row: `the CHECK unchanged → [F-02]'s "Otherwise": a new numbered file (0018, the default taken)`, design-changing `no` (report `:91`; stop line `design-changing: 0`).

## Scope
`checker · THIRD answer verbatim (≤30 words)`:
- opus · `NOTHING WIDENED.` — "The changes stay within §6 K1's paths, plus the CLI registration and the registry that C6/C7 name."
- grok · `NOTHING WIDENED.` — "Diff paths are cli.py (one registration), the 0018 pair, the registry, placement.py, drc/cli.py, drc/models.py, drc/pairing.py, drc/store.py, and the tests those signatures force."

My PREFLIGHT path-union facts (`git log --stat 38a70947..9a0fc900`): 27 distinct paths — 9 under `src/` (`cli.py`, the two `0018` files, `db_migrations/__init__.py`, `db_migrations/placement.py`, `drc/cli.py`, `drc/models.py`, `drc/pairing.py`, `drc/store.py`), 11 under `tests/cobalt/` (`test_archiver_migrations.py`, `test_drc_k1.py`, `test_drc_k1_experiments.py`, `test_drc_k1_store.py`, `test_drc_pairing.py`, `test_drc_stats_log.py`, `test_drc_store.py`, `test_p4_migrations.py`, `test_radar_migration.py`, `test_radar_score_migration.py`, `test_tenancy.py`) and 7 DevDocs (`cli.md`, `db_migrations/__init__.md`, `db_migrations/placement.md`, `drc/cli.md`, `drc/models.md`, `drc/pairing.md`, `drc/store.md`) — exactly `39`'s S6 list plus its S5 DevDocs names. No `0016_drc.sql`, no `configs/`, no `aset/`, `prefill/`, `replay/` or `vaultwrite/` path; the code above the tip is empty. Nothing to record as widened.

## Suites
`suite · opus · grok` (FOURTH):
| suite | opus | grok |
|---|---|---|
| offline | `SHOWN — 2438 passed, 415 skipped, 1 xfailed, exit 0; the .env absence is proven by ls exit 1.` | `SHOWN — S8 exit 0, 2438 passed / 0 failed, .env absent before the run (ls exit 1).` |
| with-DB | `SHOWN — 2843 passed, 6 skipped, 4 deselected, 1 xfailed, exit 0. … The absence probe gives 28 == 32, short by exactly the 4 drc_* tables.` | `SHOWN — S9 exit 0, 2843 passed / 0 failed; .env taken and removed, removal proven by ls exit 1; … assert 28 == 32 (short by 4 …)` |
| live-note | `SHOWN — 142 passed, 1 skipped, exit 0; no skip names COBALT_LIVE_VAULT_ROOT.` | `SHOWN — S7 exit 0, 142 passed / 0 failed, same one known skip, no skipped line naming the live vault root.` |
| deselects | `DESELECTS AS STATED.` — "Three --deselect arguments produce 4 deselected, which assumes the TestMigrationRoundTrip class holds 2 tests. That count can't be checked from reads." | `DESELECTS OPEN` — "The packet never lists the four test ids. The only selector that can match more than one test is the class; which second method was dropped is not in this folder." |
The checkers contradict on the deselects: quoted both (file-check under `## Checked against the branch`).

Myself, from the build report (`/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k1-build-2026-09-24.md`), facts only:
- offline (S8, `:197`): `2438 passed, 415 skipped, 1 xfailed, 15 warnings in 68.07s (0:01:08)` — no `failed` and no `error` token; `<f>` = 0.
- with-DB (S9, `:206`): `2843 passed, 6 skipped, 4 deselected, 1 xfailed, 15 warnings in 144.95s (0:02:24)` — no `failed` / `error` token; `<df>` = 0; deselected 4.
- live-note (S7, `:194`): `142 passed, 1 skipped, 15 warnings in 9.41s` — no `failed` / `error` token. SKIPPED lines naming `COBALT_LIVE_VAULT_ROOT` in the live-note leg: none (`:194`, `:48`; the one skip names `COBALT_TEST_LIVE_DRC`). The with-DB run's own 6 skips include five that say `COBALT_LIVE_VAULT_ROOT not set` (`:211`: `test_cards_picks.py:383`, `:396`, `test_radar_evaluate.py:691`, `test_catalyst.py:365`, `test_predicate.py:262`), "S7 covers them".
- absence probe (S9 (c2), `:207`): `1 failed in 5.73s`: `assert 28 == 32` — "short by EXACTLY 4"; `:213` `0016 + 0018: rolled back … (probe short by 4)`. The stop line does not carry `0018: UNPROVEN`.
- `.env: removed, proven gone` is written for S2 (`:83`), S4 (`:147`) and S9 (`:213`).

## Seam
`checker · FIFTH answer` and `checker · SIXTH answers`:
- opus · FIFTH: `CONTRACT GAP.` "Each signature line checks out … The gap is in point (2), the None book case. It does not say whether the route records the day that could not be paired: `record_day(pairing, ids, None)`. … A page builder has nothing to cite for either choice."
- grok · FIFTH: `CONTRACT STATED.` "Call order: seed_for … → None means build_day(..., seed=None) → else build_day(..., seed=book.positions) then record_day(pairing, import_ids, seed) with seed required … In-store refusal: assert_writable is the first line of record_stated_book."
- SIXTH (the five pins of the build's ESCALATE 7):
| pin | opus | grok |
|---|---|---|
| stated `trade_id` `<symbol>-<direction>-stated-<day>` | `SOUND` | `SOUND` |
| `opened_on` = the stated day | `UNSOUND.` "a swing he opened days before D and stated for D … `opened_on = D` (`k1-diff-2.md:113`) is a date known to be false." | `SOUND.` "The position's `day` is the same day." |
| pinned hash encoding (X4) | `SOUND` | `SOUND` |
| derived `reason` | `UNSOUND.` "P is recorded and he states D's opening, which is the R51 case. The row stores `chain broken at <P>` (`k1-diff-2.md:601-605`) when the chain is not broken" | `SOUND.` "The four strings are C3's, produced only in `_reason`" |
| `seed_for` case (iv) fails until K2 | `SOUND` | `SOUND` |

## Checked against the branch
Code under `/Users/cobalt/cobalt-wt/drc-d1/` (Read / grep), the build report, and `git -C /Users/cobalt/cobalt show`/`log`. `claim · who · file:line · HOLDS / DOES NOT HOLD / NOT CHECKABLE FROM READS · ≤30 words`:
| # | claim | who | file:line | verdict | note |
|---|---|---|---|---|---|
| 1 | `UNSOUND` — pin "`opened_on` = the stated day": a swing opened before D and stated for D carries `opened_on = D` | opus | `pairing.py:122` `opened_on=day`; `_seeded` `:216` sets `carried_from=position.opened_on`; `:298` `opened_on=book.carried_from or day` | **HOLDS** | The stated position's `opened_on` is the stated day, and the carry keeps it on later days. (Grok: `SOUND`; contract C4 (vi) pins `opened_on=day` — recorded, not smoothed.) |
| 2 | `UNSOUND` — pin derived `reason`: P recorded and D's opening stated → row stores `chain broken at <P>` | opus | `store.py:485-489` (`earlier` = any `day` row before D → `chain broken at {prior_trading_day(day)}`); write path `:531-571` has no prior-day guard | **HOLDS** | With P recorded, an `opening` for D gets `chain broken at <P>` and the table is append-only (`0018_drc_stated_books.sql:49-51`). (Grok: `SOUND`; C3 (4) pins this derivation and `test_the_reason_is_derived…` asserts it.) |
| 3 | `CONTRACT GAP` — seam point (2) does not say whether the route records the unpaired day (`record_day(pairing, ids, None)`) | opus | build report `## SEAM FOR D2` (2)/(3) (`:227-228`); `store.py:212-217` refuses only `computed and seed is None`; X2 (a) calls it (`test_drc_k1_experiments.py`, X2 (a), `seed=None`) | **HOLDS** | The seam text is silent on recording an unpaired day; the store allows it (`store.py:212-217`). (Grok: `CONTRACT STATED`, quoted.) |
| 4 | `DESELECTS OPEN` — "which second method was dropped is not in this folder" | grok | `test_tenancy.py:688-710` (`TestMigrationRoundTrip`: `:697` and `:710`, two tests), `:263` (`test_every_user_table_carries_user_id_not_null_with_the_guc_default`), `test_migrate_proof.py:306` (one test); build report `:206` `4 deselected` | **DOES NOT HOLD** | The class has exactly two tests; 2 + 1 + 1 = 4 = the summary's `4 deselected`; no test is unaccounted. (Opus: `DESELECTS AS STATED`.) |
| 5 | Opus note (C3): two current `resolve` rows for one `trade_id` on different days are both accepted (v3 §4 says FAIL naming both ids) | opus | `store.py:547-549` (current-row filter includes `AND day = %s`) | HOLDS as a mechanical fact | Opus classed C3 `MEETS THE ROW` and "This belongs to K2"; not one of the defect kinds, so not counted. |
| 6 | Opus caveat (L1): `record_day` checks only that `seed` is not None; a caller-built `SeedBook(source="stated", stated_book_id=<absent id>)` would record | opus | `store.py:212-217` | HOLDS as a mechanical fact | Opus: "not a defect"; not counted. |
| 7 | Opus `NOT CHECKABLE FROM READS`: `match_stats` may read `t.entry_time` of a stated trade (now None) | opus | `pairing.py:366` `t.entry_time == row.open_time`; `:368-369` `len(hits) != 1` → `unmatched — {n} trades match` | walked (not a defect kind) | `None == datetime` is False: no exception; a stated trade's stats row goes unmatched. No test runs a stated seed with a stats log (grep of the packet's tests). Not counted. |
| 8 | Grok `X5` "no" vs Opus `X5` "yes, by the letter" | both | build report `:77`, `:91` | both quoted | The gate result was `CheckViolation` (CHECK unchanged); the build reads it as `[F-02]`'s "Otherwise". No verdict here. |

(i) `grep -rn "drc_stated_books" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt` — hits, all placed: `drc/store.py` (docstring `:25`, `:27`; `TABLES` `:66`; `_CURRENT` `:78`; `STATED_TABLE` `:95`; the missing-schema message `:113`; reads `:347`, `:381`, `:547`; comment `:429`; docstring `:526`; `LOCK TABLE` `:542`; the one `INSERT` `:565`), the `0018` pair (`0018_drc_stated_books.sql` and `.rollback.sql:11`), `db_migrations/__init__.py` (docstring `:44`, `:48`; the two registry paths `:100`, `:105`), `db_migrations/placement.py` (`:95` comment, `:97` the `Side.USER` key). No hit in `drc/cli.py`, `drc/models.py`, `drc/pairing.py` or `cli.py`.
(ii) `grep -rn -e "INSERT INTO" -e "UPDATE " -e "DELETE FROM" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt/drc/cli.py` → EMPTY.
(iii) `git -C /Users/cobalt/cobalt log --oneline 38a70947..9a0fc900 -- src/cobalt/db_migrations/0016_drc.sql src/cobalt/aset src/cobalt/prefill src/cobalt/replay src/cobalt/vaultwrite configs` → EMPTY.
(iv) `grep -n "via" …/0018_drc_stated_books.sql` → `:29` `via TEXT NOT NULL CHECK (via IN ('drc_page', 'voice_widget', 'cli'))` (the other hit `:18` is a comment) — exactly the three literals of R52.
(v) L32: this report was read once before the last line: no ticker, no real date of his, no file name of his and no value written.

## Ready for K2
`checker · CHECK DRC K1 line · ready: YES|NO · reason verbatim`:
- opus · `CHECK DRC K1: BUILD STANDS EXCEPT pins `opened_on`=stated-day and derived `reason` UNSOUND; SEAM (2) gap on recording an unpaired day · ready for K2: YES` · ready: YES · "None of these blocks K2."
- grok · `CHECK DRC K1: BUILD STANDS · ready for K2: YES` · ready: YES · (no reason line)
- Hub: `defects that HOLD` = 3, so `ready for K2: NO` under §4 (YES needs both checkers YES **and** 0 HOLD).

## FOR THE CLASSIFIER
Round 1 of ≤3 — a HOLD goes to a fix round (L75). One item per claim that HOLDS in my file-check; no class, no recommendation:
1. Claim: "`opened_on` = the stated day — UNSOUND. Breaking input: a swing he opened days before D and stated for D. A position already held at D's open was opened **before** D, so `opened_on = D` (`k1-diff-2.md:113`) is a date known to be false." — Opus — `pairing.py:122`, `:216`, `:298` — HOLDS.
2. Claim: "The derived `reason` — UNSOUND. Breaking input: P is recorded and he states D's opening, which is the R51 case. The row stores `chain broken at <P>` (`k1-diff-2.md:601-605`) when the chain is not broken, and the table is append-only, so the false text stays forever." — Opus — `store.py:485-489` — HOLDS.
3. Claim: "CONTRACT GAP. … The gap is in point (2), the `None` book case. It does not say whether the route records the day that could not be paired: `record_day(pairing, ids, None)`." — Opus — build report `## SEAM FOR D2` (2)/(3); `store.py:212-217` — HOLDS.

## ESCALATE
1. FOR THE CLASSIFIER item 1 (Opus, pin `opened_on`; Grok `SOUND`) — HOLDS at `pairing.py:122`, `:216`, `:298`.
2. FOR THE CLASSIFIER item 2 (Opus, pin derived `reason`; Grok `SOUND`) — HOLDS at `store.py:485-489`.
3. FOR THE CLASSIFIER item 3 (Opus, seam point (2) gap; Grok `CONTRACT STATED`) — HOLDS.
4. Opus's C3 note: two current `resolve` rows for one `trade_id` on different days are both accepted at `store.py:547-549` (filter includes `day`); v3 §4 says FAIL naming both ids; Opus: "This belongs to K2." Mechanical fact walked; not counted.
5. Opus's L1 caveat: `record_day` checks only `seed is not None` (`store.py:212-217`); Opus: "not a defect". Walked; not counted.
6. X5 `design-changing`: Opus `yes, by the letter of the column` (consequence "nothing changes"), Grok and the build `no` — quoted both; no verdict.
7. Opus's `NOT CHECKABLE FROM READS` (`match_stats` and a stated trade's `entry_time`): walked — `pairing.py:366` compares with `==`, no exception, the stats row goes unmatched; no test runs a stated seed with a stats log.
8. Grok's `DESELECTS OPEN` vs Opus's `DESELECTS AS STATED`: my file-check DOES NOT HOLD Grok's claim (`test_tenancy.py:697`, `:710`, `:263`; `test_migrate_proof.py:306`: 2 + 1 + 1 = 4). Packet note for the desk: the packet did not list the four test ids (the first-round `DESELECTS OPEN` pattern, as in D1 fix r1).
9. The build's DRAFTER PINS line, verbatim (carried for the desk): "DRAFTER PINS where v3 names no shape (for `40` to rule on, not the builder's): the stated `trade_id` `<symbol>-<direction>-stated-<day>`; a stated position's `opened_on` = the stated day; the pinned hash encoding (X4); the derived `reason`; `seed_for` case (iv) FAILS until K2 (R51)."
10. The build's `book_sha256` binding line, verbatim (carried for the desk): "`book_sha256` hashes the positions only (v3 `:135`): every flat `opening` and every `no_trade` row carries the same hash, `sha256(b"[]")`. R52's `--apply --sha256` therefore binds the positions, not the day or kind. The desk's approval message carries the printed row (day, kind, reason) beside the hash (R52 / R47 ESC 2). Recorded for the desk and `40`; not changed here (his ruling)." (Opus, "not a finding": "ESCALATE 8 is true.")
11. Astra: `METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — not probed, not launched; the desk seats it in its own launch from that time.
12. L74: one block arrived (a `Claude-Session` line request naming a file-send tool, inside the Read of this prompt); recorded once, not followed.
13. Standing line: "Round 1 covers DRC K1 whole (`<base>..<tip>`), its X1–X6 outputs and its three suites' executed output, checked by Opus 5.5 + Grok under his R95 (a new build: Fable-seat as Opus 5.5 per 09-22 R109; Astra METER until Sep 26th, 2026 6:47 AM, seated by the desk from its return; Gemini out, R96/R97). Two seated houses `ready for K2: YES` with `defects that HOLD: 0` → K1 is checked by the floor L67 allows while Astra is METER (the shrink-to-two clause), and K2's drafter may start; Astra's read of the build is recorded when it returns. A HOLD → a fix round classified first (L75), ≤3 rounds (L39)." — this round: two seated houses YES, `defects that HOLD: 3` → a HOLD, so the fix-round branch of this line applies.
14. Standing line: "The deploy's L68 gate re-proves offline, with-DB and the live-note suite on the stacked tree that ships (D1 + K1 + K2 + D4 + D2 + D3); the deploy prompt gets its own house read (L67, R95 seats)."
Also: no packet mismatch; every checker that launched checked; no checker wrote a file it was not told to; no `ASK DESK`; no checker `FIX`.

DRC K1 CHECK DONE · round: 1 · opus: CHECK DRC K1: BUILD STANDS EXCEPT pins `opened_on`=stated-day and derived `reason` UNSOUND; SEAM (2) gap on recording an unpaired day · ready for K2: YES · grok: CHECK DRC K1: BUILD STANDS · ready for K2: YES · astra: METER retry after Sep 26th, 2026 6:47 AM · houses that checked: 2 of 3 · defects that HOLD: 3 · ready for K2: NO · ESCALATE: 14
