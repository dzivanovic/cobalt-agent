# DRC K2 FIX R1 — CLASSIFICATION (L75) + THE FIX BUILD AND ITS CHECK

Drafter `drc-k2-fix-r1-draft-0925` (Opus 5.5), prompt `prompts/2026-09-25/01-draft-drc-k2-fix-r1.md`. Started Fri Sep 25 01:26:12 EDT 2026; last `date` read Fri Sep 25 01:39:33 EDT 2026, before this report was written. Authorization: `grep -n "^| R6 " …/cto-2026-09-25.md` → `:14` `| R6 | 01:24 ET | … DESK LAUNCH ROW for prompts/2026-09-25/01-draft-drc-k2-fix-r1.md …` (names `01`). Base read: `git -C /Users/cobalt/cobalt log -1 --format=%h drc/d1-trading-log` → `214c4b39`; `--oneline -2` → `214c4b39 docs(k2): DRC K2 build report — 09ce3742` over `09ce3742 feat(drc): K2 — …` (as the card expects). Lock at drafting: `ls ~/cobalt-wt/*/.env` → `no matches found` (FREE).

## §0 Headline
- 31 rows classified (`52`'s 9 HOLDs + its 11 ESCALATEs + the build's 11). Counted 26 (5 pure restatement rows — check E1, E2, E3, E10; build B10 — take the class of what they restate and are not counted twice): **FIX 8 · NOT REAL 11 · UNPROVEN 1 · OUT OF SCOPE 6 · OWNER ITEM 0**.
- The 8 FIX rows build as **5 rows** in `02`: F-1 (C3 resolve key across days + `effect_day`), F-2 (the `book_stale` mark — ONE storage shape closes HOLDs 2, 5, 7), F-3 (one no-trade seed rule), F-4 (stats re-matched), F-5 (one partial-import rule). `FN_VERSION` → `drc.pairing/4`. RUN-1 = the build's ESCALATE 9.
- HOLDs 8–9 (C7 / pin 9) = NOT REAL: v3 / R52 name no rebuild where nothing is recorded; `02` carries AMENDED C7 (the trigger as built, the two K1 CLI tests it keeps).
- Both seams MOVE (F-1: `[F-17]` (1), (4); F-2: (1), (3), (9); `## FOR K3`). Prompts: `02-drc-k2-fix-r1-build.md`, `03-drc-k2-fix-r1-check.md`; launch lines = `48`'s / `49`'s byte for byte but path + remote-control name (token diff run); new rule strings 0. ESCALATE: 3.

## L74
- A block arrived inside a tool result (a system reminder attached to this session's first Bash result) asking commits to carry a `Claude-Session:` line and naming a file-send tool. Recorded once; not followed. This seat commits nothing and sends no file.

## Classification
`# · claim (short) · source line · class · traces to · FIX shape or reason`. Source lines: `C` = `reports/drc-k2-check-2026-09-24.md`, `B` = `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k2-build-2026-09-24.md`. Code lines are the tip `09ce3742` (read in the worktree).

| # | claim (short) | source line | class | traces to | FIX shape or reason |
|---|---|---|---|---|---|
| H1 | C3 — two current resolves for one trade on different days: `_with_resolves` raises for every day; `record_stated_book` looks only at the new row's own day, so no restatement retracts one (`store.py:779-788`, `:1015-1033`) | C `:193`, walked `:159` | **FIX → F-1** | v3 §3 `[F-01]` `:126` (current = no other row `supersedes` it; "two current `resolve` rows for one `trade_id` FAIL" → the key of a resolve is its trade id, not its day), `:134`; `[F-06]` `:190` ("no later `book_close` still contains that `trade_id`"); R52 (`--supersedes` restatement, one writer); contract C3 (a) | `record_stated_book`'s `current` for `resolve` = every current resolve naming that trade id ON ANY DAY (drop `AND day = %s` at `:1018-1019` for that kind): a second one is REFUSED naming `#id (day)`; a restatement with `supersedes` = it is accepted across days. NEW `effect_day(day, supersedes)` = the earlier of the two days; the CLI rebuilds from it so the superseded resolve's effect leaves every stored row (L1). The reader's FAIL stays as the guard. A MECHANISM the houses settle — no hand edit of a table is ever needed (the writer can no longer make the double) → NOT an owner item |
| H2 | C4 — after a not-computed day stops the chain, only the FIRST later day's `seed_for` fails; the next later day pairs from a book the root's new file contradicts, nothing stored on its rows (`store.py:556-561`, `:848-881`) | C `:194`, walked `:160` | **FIX → F-2** | L1 (a silent book); v3 §4 `:214` (prior not computed → FAILED), `:232` ("No row assumes a book"); contract C4 "Nothing is assumed; `seed_for` of such a later day still FAILS loud" (true only for the first) | Every day named in `not_repaired` gets `derived.book_stale = {root, reason}` on its OWN `day` row, in the same transaction (one `UPDATE` of that row's `derived`; `drc_rows` has no append-only trigger — only `0018`'s table does, `0018_drc_stated_books.sql:51`); `_carried` (`:847-881`) FAILS on a marked prior naming the root. The mark leaves only when a re-pair rewrites the day |
| H3 | C4 minor — a no-trade day via `record_day` stores its seed `carried` with no `no_trade_id`; `rebuild` relabels it `no_trade_carry` (`store.py:310-318`, `:662-669`, `:499-513`) | C `:195`, walked `:161` | **FIX → F-3** | L3 (ONE seed rule); v3 §2b `:97` (the no-trade day's seed is `source: no_trade_carry`), `[F-05]` `:99`; contract C5 ("never a second seed rule") | The relabel at `:499-513` becomes ONE helper `_no_trade_seed(seed, no_trade_id)`, called by `_repair` and by `record_day` when it resolved `no_trade_id`. The stated-link exception (a carried book naming a statement stays `carried`) is kept — RUN-1 proves it |
| H4 | C5 / A1 — a day stored not computed with a stats log keeps `not_computed.match = "…pairing did not run"`; once paired, `kept_match` writes the stats rows back unmatched (`store.py:471-490`, `:520-526`; `pairing.py:498-507`) | C `:196`, walked `:162` | **FIX → F-4** | A1 (v3 `:179`, a DESK READING of R51 — cited as such): "Re-run `match_stats` … from those stored rows against the new pairing"; "do not invent a match" is about a file lacking match inputs; L1 (a stored "pairing did not run" on a paired day is a value known false) | `kept_match` removed. The re-pair rebuilds the `ParsedStatsLog` from the stored rows with the stats import's OWN `missing` (its `drc_imports.reason`, through F-5's `missing_of`) and always runs `build_day` on it: still not computed → "pairing did not run" (true); computed → `match_stats` exactly as the first record (a `Side`-less file re-matches to the same `not computed — missing: Side`; `test_drc_k2_experiments.py:397` stays green) |
| H5 | pin 4 — "the chain STOPS at a not-computed day" UNSOUND | C `:197`, walked `:163` | **FIX → F-2** | as H2 | Same input as H2; F-2 keeps the stop (R51 "the import does not stop"; no later day is re-paired from an unknown book) and makes it LOUD on every later day |
| H6 | pin 6 — a partial current import → `not_computed.pairing` = its stored reason: a file missing only a non-pairing column pairs at its first record and is un-paired by every rebuild (`store.py:451-460` with `trading_log.py:170-171`) | C `:198`, walked `:164` | **FIX → F-5** | v3 §3 `:181` (B: "A `partial` import re-pairs to `not computed` exactly as its first record did"), X9 `:325` (`FR14`); L3 (ONE partial-import rule); A1 "An unchanged later day yields the same `derived` and `inputs`" | The pin is REPLACED. `models.py`: `PARTIAL_PREFIX` + `missing_of(outcome, reason)` (the one inverse of `partial_flag`; an unreadable reason → `ValueError` → loud `PairingError`). `trading_log.py`: `pairing_not_computed(missing)` = the two lines at `:170-171`, called by the parser AND by `_repair`. Named reversal: `test_drc_k2_experiments.py:450` (`== reason` → the first record's text) |
| H7 | seam CONTRACT GAP — `not_repaired` only on the earlier day's `day` row; neither D2 nor K3 can cite day N's own rows (`store.py:551-577`, `:569`) | C `:199`, walked `:165` | **FIX → F-2** (storage + SEAM STATEMENT) | L72 (the seam is a stated contract both builders cite); L1 | The same storage shape as H2 closes it: `book_stale` on day N's (and N+1's) OWN `day` row. `02` amends `## SEAM FOR D2` (3), (1), (9) and `## FOR K3` with `file:line`; pinned by `test_every_later_day_of_a_stopped_chain_carries_the_stale_mark_and_fails_its_successor` |
| H8 | C7 — `_rebuilds` false with no current import and no `day` row on or before the day: apply prints `stated; <day> has no import yet`, exit 0, no `rebuild` (`cli.py:127-137`, `:171-173`) | C `:200`, walked `:166` | **NOT REAL** (contract text amended) | v3 `[F-05]` `:97`, `:99` (the no-trade day carries a book that EXISTS: its seed is the prior close); `[F-06]` `:190` (the re-pair exists so that no LATER `book_close` holds the trade — none exists); R52 (the CLI covers three kinds, dry run, `--apply --sha256`; names no rebuild); C7 was a DRAFTER PIN (`51:111`), the build disclosed the narrowing (B `:248`) | ONE reading: a statement with nothing recorded on or before its day is stated and nothing more — no recorded book to carry, nothing to re-pair; the next import is a first import (`seed_for` → `None`, `state your opening book`), never an assumed book (L1). `02` carries AMENDED C7 (the trigger as built + `effect_day`), which keeps `test_drc_k1_store.py:646` `[no-trade]` / `[resolve]` green (C8) |
| H9 | pin 9 — the CLI's rebuild after `--apply` UNSOUND (same input) | C `:201`, walked `:167` | **NOT REAL** | as H8 | Same input and reading as H8 |
| E1 | Opus's `FIX … C5-kept-match, C4-stop-chain, C3-resolve-deadlock, C4-no-trade-seed`; its own escalate "a ruling on moving or retracting a resolve; the partial-import pin" | C `:204` | as H1–H7 (restatement) | L67 owner-items clause (a mechanism the houses settle never reaches him) | Settled by F-1 (a newer resolve for the same trade supersedes across days; the rebuild runs from the earlier day) and F-5 (v3 `:181`'s own words). Neither is his trading judgement → no owner item |
| E2 | Grok's `BUILD STANDS EXCEPT C7 · ready for K3: YES`; its MEETS on C3 / C4 / C5, SOUND on pins 4 / 6, `CONTRACT STATED` | C `:205` | as H8–H9 (restatement) | L35 (the hub walked the inputs, `:172-177`) | Its C7 / pin 9 → NOT REAL (H8–H9). Its MEETS / SOUND readings do not stand against the walked inputs of H1–H7 (Grok read the branch; Opus walked what follows from it) |
| E3 | every FOR THE CLASSIFIER item restated, `defects that HOLD: 9` | C `:206` | as H1–H9 (restatement) | — | — |
| E4 | the packet stop-and-resume (260,000 → 300,000 B ceiling, R5) | C `:207` | NOT REAL | `cto-2026-09-25.md` R5; L67 P-c (no round spent) | A packet note, no defect. Becomes a PACKET RULE in `03`: ceiling 300,000 B, the size sentence from MEASURED `wc -c` parts only, a named cut order (`rules.md` v3 `:124-143`, then `:97-104`) before `FAILED: packet` |
| E5 | no checker failed to check; no stray file; the one `ASK DESK` answered | C `:208` | NOT REAL | — | A record; nothing to build |
| E6 | the L74 line | C `:209` | NOT REAL | L74 | Recorded once there; never raised again |
| E7 | Astra METER until Sep 26th, 2026 6:47 AM | C `:210` | OUT OF SCOPE | L62 (as amended R19), L67 | The desk seats Astra for the K2 NEW-BUILD read from its return; a fix round is never Astra-seated |
| E8 | X12 `NOT RUN — hub-run read` | C `:211` | OUT OF SCOPE | v3 `:306`, `:328`; L41 | The desk's hub-run read of his file; carried in `02` / `03` |
| E9 | X10 / X11 GATE tests removed at S5 | C `:212` | NOT REAL | B `:246` (ESCALATE 3: base facts the contract replaces — C3 (iv) → R51; C2 `_trade`) | The reading stands. The experiments file already carries the one-line comments naming each removal (`test_drc_k2_experiments.py`, above X10's and X11's PASS tests); `02` edits nothing there but `:450` and `:535`, so they stay |
| E10 | the DRAFTER PINS (ten) with each house's SOUND / UNSOUND | C `:213-216` | as sub-rows (restatement) | `51:203` | pin 1 (compare key multiset, ids of both books) NOT REAL — kept · pin 2 (`stated_book_id` on a carried seed) NOT REAL — kept · pin 3 (`repaired` / `not_repaired` when a later day exists) NOT REAL — kept as the record (F-2 adds the marker on the later days) · pin 4 **FIX (F-2 = H5)** · pin 5 (a failed import FAILS the re-pair) NOT REAL — kept · pin 6 **FIX (F-5 = H6)** · pin 7 (resolve on its own day; superseded; FAILS otherwise) NOT REAL — kept (its cross-day deadlock is F-1) · pin 8 (exit leg only with price AND time) NOT REAL — kept · pin 9 NOT REAL (= H9; AMENDED C7) · pin 10 `FN_VERSION` NOT REAL as a rule — its value moves to `drc.pairing/4` because F-2…F-5 change stored shapes |
| E11 | the standing lines (round rule; the deploy's L68 gate) | C `:217-219` | OUT OF SCOPE | L39, L67, L68 | The desk's text; carried into `03`'s standing lines |
| B1 | S0: `no_trade_carry` hit in a K1 docstring | B `:244` | NOT REAL | — | Text, not code |
| B2 | R22 scope read as the drc-d1 pair | B `:245` | OUT OF SCOPE | `cto-2026-09-24.md` R22; R61 / R65 (1) | The desk's reading; `02` records it the same way |
| B3 | two S2 GATES removed (X10, X11) | B `:246` | NOT REAL | = E9 | as E9 |
| B4 | the K1 validator vs C1: a carried book naming a statement must pass `stated_differs` explicitly (`models.py:404-412`; `test_drc_k1.py:225`) | B `:247` | NOT REAL | L7 (the comparison is never left unstated); R51; C8 (K1's test unchanged) | Stricter than C1's text, never looser; every book `_seed` builds passes it (`store.py:747-754`); Opus read the same (C `:91`) |
| B5 | C7's trigger, narrowed | B `:248` | NOT REAL | = H8 | AMENDED C7 in `02` |
| B6 | a fourth lock take (S5) | B `:249` | NOT REAL | L76 | Each take was the four moves, released and proven (B `:111`); no defect |
| B7 | C5's partial pin vs `FR14`'s words ("as its first record did") | B `:250-253` | **FIX → F-5** | = H6 | as H6 |
| B8 | X12 | B `:254` | OUT OF SCOPE | = E8 | as E8 |
| B9 | a no-trade day with a stated opening beside a recorded prior keeps `source: carried` with the link and the difference — "unreached by any test; a builder reading" | B `:255` | **UNPROVEN → RUN-1** | L70 (not checkable from reads: no test runs it) | `02` F5: R51's order (opening + no_trade stated for `D_NEXT`, then `D` recorded) → assert `carried`, `stated_book_id`, non-empty `stated_differs`, `no_trade_id` on the `day` row, the R51 line; and the `record_day` path stores the byte-identical seed (F-3's second caller). A red is `xfail(strict=True)`, quoted, never fixed in this round |
| B10 | DRAFTER PINS | B `:256-266` | as E10 (restatement) | — | — |
| B11 | A1 is a DESK READING of R51 | B `:267` | OUT OF SCOPE | R51 row ("DESK READING, not his word"); the card: not this seat's to re-open | F-4 cites A1 as the desk's reading and implements its own words ("Re-run `match_stats` …") |

Counted rows (26): FIX 8 (H1–H7, B7) · NOT REAL 11 (H8, H9, E4, E5, E6, E9, B1, B3, B4, B5, B6) · UNPROVEN 1 (B9) · OUT OF SCOPE 6 (E7, E8, E11, B2, B8, B11) · OWNER ITEM 0. Not counted (restatements): E1, E2, E3, E10, B10.

Named test edits `02` carries (the only assertion / setup changes; each with `file:line`): `test_drc_k2.py:72-75` (renamed `…_is_4`), `test_drc_k1.py:66`, `test_drc_k1_store.py:497`, `test_drc_store.py:403` (`drc.pairing/4`); `test_drc_k2_experiments.py:450` (F-5); `test_drc_k2_experiments.py:535`, `test_drc_k2_store.py:193` (F-2: replaced by a stricter snapshot + mark assertion); `test_drc_k2_store.py:318` (F-1: SETUP only — the second resolve by a direct insert; assertions byte for byte). Paths beyond `51`'s S6 list: `src/cobalt/drc/trading_log.py` and its DevDoc (F-5 — the check's own cite `trading_log.py:170-171`; the rule moves into one function, no first-record behaviour change).

## FOR K3
- **Yes — both seams move.** `## SEAM FOR D2`: (1) gains the STALE-prior raise and states the two-resolves raise is now a guard (F-1, F-2); (3) gains `derived.book_stale` on each later day's own `day` row (F-2); (4) gains the resolve key (trade id, any day), `supersedes` across days and `effect_day` (F-1); (9) gains the stale root's rebuild (F-2). `## FOR K3`: a `book_stale` day renders STALE naming its root, never as a continuing book; `effect_day`'s `repaired` list; stats rows re-matched (F-4); `drc.pairing/4`; RUN-1's result. `02` writes both sections WHOLE with `file:line` from its tree.
- The desk decides whether K3's drafter waits for `03` or runs beside it citing `02`'s sections (L72: the seam is settled in a document both cite before either launches — `02`'s stated sections are that document once BUILT).

## OWNER ITEMS
none — the C3 retract / supersede shape is a mechanism (v3 `[F-01]`'s own key), the partial rule is v3 `:181`'s own words, and C7 is decided from v3 / R52's words; nothing here is his money, data, laws or trading judgement (L67 owner-items clause).

## FOR DEJAN
none — `02`'s launch line is `48`'s byte for byte but path + remote-control name (the drc-d1 `.env` pair is his R22); `03`'s is `49`'s byte for byte but path + remote-control name (standing strings R17 / R19). Token diff run by this seat (`diff` of the whitespace-split lines): exactly those two tokens differ in each.

## ESCALATE
1. **F-1 widens one read and one CLI call beyond the HOLD's two cited functions:** `effect_day` (store) and its use in `cli.py`'s `_rebuilds` / `cmd_state_book`. Reason: without it a cross-day supersede leaves the superseded resolve's closed trade in the earlier day's stored rows, and the later day's `_closed_on` then reads it as "closed by the export of <day>" — a stored value known false (L1). Named as part of F-1, not a new row; `03` checks it.
2. **F-5 adds `src/cobalt/drc/trading_log.py` (and its DevDoc) to the path list** — outside `51`'s S6 list, inside the HOLD's own citation (`trading_log.py:170-171`): ONE partial-import rule cannot live in two copies (L3). No first-record behaviour change; `test_drc_trading_log.py` is in `02`'s F4 offline proof.
3. **`FN_VERSION` → `drc.pairing/4`** (pin 10 kept as a rule, its value moved): four stored shapes change — `day.derived.book_stale` (F-2), a no-trade day's `seed.inputs.source` via `record_day` (F-3), a re-paired day's stats match (F-4), a partial re-pair's `not_computed.pairing` text (F-5). Four pins move (named above).

DRC K2 FIX R1 DRAFTED · FIX: 8 · NOT REAL: 11 · UNPROVEN: 1 · OUT OF SCOPE: 6 · OWNER ITEM: 0 · prompts: 2 · new rule strings: 0 · ESCALATE: 3
