# DEPLOY 2026-09-19 (3) — ops + archiver

Hub `deploy-d3-0919` (Opus 5, `claude-opus-5`), background session, cwd `/Users/cobalt/cobalt`.
Prompt: `docs/40 - DevDocs/prompts/2026-09-19/53-deploy-d3.md`, sha256 `4772ad490747d07e9fa7d062b95b3847fb27aed6e6ab1c41052a58bacdfb5503`.
Authorization: `cto-2026-09-19.md` §4 **R36** (16:4x ET) + **R36 (status)** (17:4x ET, carries the FINAL sha) + standing R32, R25, R26, R34, R35, R19; `cto-2026-09-18.md` §4 R18–R21 (L67). Verified below before anything ran.

## §0 Headline

- PREFLIGHT clean: **16 rows, ZERO denials.** Authorization verified — `R36 (status)`'s FINAL sha256 MATCHES this file, both rows committed (`6271160`, `92745c6`, `6f4ebc0`).
- Heartbeat **GREEN, 16 jobs, nothing red**; both residents UP (aset 80628, radar 80641 — deploy 2's pids, nothing respawned since); P13 proof exit 0, no `CHANGED`.
- P12 confirms `archive_progress` / `archive_incidents` are **absent from production**; P15 both branch stop lines green; P16 `blocks deploy 3: 0`.
- Clock at PREFLIGHT **17:45 ET**, inside R36's ≤ 20:15 condition.
- Run in progress — the L68 gate is next.

### Authorization, verified before anything ran

| what | where | verdict |
|---|---|---|
| the row that names THIS file | `cto-2026-09-19.md` §4 **R36**, 16:4x ET, "Approved." | PRESENT — names `prompts/2026-09-19/53-deploy-d3.md`, approves this launch line's **51 rule strings** (41 byte-identical to R30's deploy-2 line, 10 new), tag `deploy-2026-09-19c`, migrations 0010/0011, RESTARTS aset + radar, NO new scheduled job, **NO settings write — cards READ only**, archiver ships in `upsert` with ONE shadow night (R35) |
| **`R36 (status)`** — the row that carries the FINAL hash | §4 `R36 (status)`, 17:4x ET | PRESENT — the desk's record of THREE house rounds ending **`blocks the launch: 0`**; states no rule string changed in any round, so R36's "a rule-string change waits for him" never triggered; Astra's round-3 dissent on §8 (1) **adopted verbatim** rather than overridden |
| the file is the approved text | `R36 (status)`'s applied column: **FINAL sha256 `4772ad49…b5503`**, superseding the intermediate `314abc24…f5d23` and R36's pre-fold `3d18b352…9339` | `shasum -a 256` of the prompt = **`4772ad490747d07e9fa7d062b95b3847fb27aed6e6ab1c41052a58bacdfb5503`** — **EQUAL** |
| the rows are committed on main | `git log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` | **`6f4ebc0`** (`R36 (status)` carries the deploy-3 final sha) over `92745c6` (3 rounds cleared, FINAL sha recorded) over `6271160` (R36 approved in advance). `git status` shows `cto-2026-09-19.md` is **NOT modified**, so the rows read ARE the committed text |
| R36's three conditions | the row itself | (1) house check ZERO launch blockers — **MET** (`R36 (status)`: `blocks the launch: 0`) · (2) `D3 BUILD CHECK`'s `blocks deploy 3` stays 0 — **MET** (P16) · (3) start ≤ **20:15 ET** — **MET at 17:45** |
| standing rows | `cto-2026-09-19.md` §4 **R32** (L43 overruled tonight, green-before-21:00), **R25** (the REVOKE), **R26** (the dropped twin), **R34** (ceiling 50, backfill bounded), **R35** (ONE shadow night), **R19** (push is Dejan's word only; also: the `Claude-Session` commit line is ruled **NO**) | all found committed |
| `cto-2026-09-18.md` §4 R18–R21 | L67 (four-house tribunal, three checkers, meter floor) | all four rows present |
| not this deploy's approval | R7 (`02-deploy-stack-3.md`) and R30 (`38-deploy-p4.md`) | acknowledged — NOT read as approvals of this file |

No mismatch anywhere.

## PREFLIGHT

Ran 17:45–17:47 ET from `/Users/cobalt/cobalt`. **Zero denials — every allowlisted shape probed.**

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P1 | `git -C /Users/cobalt/cobalt status --porcelain` and `git … status` | 0 / 0 | allowed — ` M docs/40 - DevDocs/reports/seat-usage.md`, `?? …/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`. LONG form: `On branch main`, ahead of origin/main by **9 commits** (the desk pushed at 16:37, R19/§7948fbf), **no rebase in progress, no unmerged paths**. `cto-2026-09-19.md` NOT dirty |
| P2 | `git … tag scratch-allow-probe-d3-0919` then `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-d3-0919' (was 6f4ebc0)` |
| P3 | `git … commit --allow-empty` then `reset --soft HEAD~1` | 0 / 0 | allowed — `[main 08abf7d]` created, then `log --oneline -1` → **`6f4ebc0`** — HEAD is back |
| P4 | `date` | 0 | allowed — **`Sat Sep 19 17:45:55 EDT 2026`** — 17:45 ET ≤ 20:15. Read for the record; enforced at §2.1 |
| P5 | `COBALT_ENV=production uv run cobalt validate` | **0** | allowed — 13 trade_defs OK; **`Jobs (F17): 16 registered — 6 resident, 10 one-shot.`** — deploy 2's state, `com.cobalt.replay` present, exactly as expected; `registry <-> ops/: 16 label(s), exact match.`; `registry <-> plists: schedules and COBALT_ENV agree on every job.`; `Step-downs: … — 7 row(s)`; `61 engine tunable(s)`; `Placement (docs/PLACEMENT.md): tree clean` |
| P6 | `COBALT_ENV=production uv run cobalt backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 2.6 h old` (deploy 2's 15:11 snapshot) |
| P6 | `COBALT_ENV=production uv run cobalt backup run --dry-run` | 0 | allowed — `backup: cobalt_brain via pg_dump inside cobalt_memory — 645.3 MB`; `DRY RUN — nothing written, no jobs row touched.` |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..archiver/append-0919` | **0** | allowed — **`RESTARTS: com.cobalt.aset com.cobalt.radar`**, **0 UNCLASSIFIED**. See the P7 note |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..ops/2026-09-19` | **0** | allowed — **`RESTARTS: com.cobalt.aset com.cobalt.radar`**, **0 UNCLASSIFIED** |
| P8 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — **`HEARTBEAT GREEN — 16 job(s), 12 probe(s), nothing red  (2026-09-19 17:46:20 EDT)`**. See the P8 note |
| P9 | `launchctl print gui/501/com.cobalt.aset` · `…/com.cobalt.radar` | 0 / 0 | allowed — both `state = running`; **aset pid 80628**, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` · **radar pid 80641**, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`. **Both pids are exactly the ones deploy 2 left** — launchd has not respawned either since 15:14 ET. Both paths are the expected ones; no plist moved |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` | 0 | allowed — **`200`** |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` | 0 | allowed — **`200`** |
| P11 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(…) … HAVING count(*) > 1"` | 0 | allowed — header only (`tablename	array_agg`), **ZERO rows**. No duplicate table name anywhere in `public`/`user`/`system` |
| P12 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN ('archive_progress','archive_incidents')"` | 0 | allowed — header only (`schemaname	tablename`), **ZERO rows**. **Neither of 0010/0011's names exists anywhere in production** |
| P13 | `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` (`timeout` 600000) | **0** | allowed — proof table below. **No `CHANGED`, no tool timeout, nothing applied** |
| P14 | `ls -la <path>/.env` × 4 (`archiver-append`, `ops-2026-09-19`, `s2-p4`, `stack-deploy3-0919`) | 1 × 4 | allowed — **every one "No such file or directory"**. No other session holds `cobalt_dev`; the stack worktree does not exist yet |
| P15 | the two branch gates | — | **both `0` failed** — see below |
| P16 | the L67 gate | — | **`blocks deploy 3: 0`** — see below |

**P7 — INFORMATIONAL ONLY, and BOTH runs exited 0 with 0 UNCLASSIFIED.** Recorded, not judged (5.1 is the real derivation, on the merged tree). Both legs printed `RESTARTS: com.cobalt.aset com.cobalt.radar` verbatim as their last line.

Archiver leg (`main..archiver/append-0919`), the rows that carry a restart:

```
configs/cobalt/taxonomy/tunables.yaml	M	resident reads	com.cobalt.aset,com.cobalt.radar
src/cobalt/archiver/report.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/archiver/runner.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/archiver/store.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/__init__.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/placement.py	M	static import reach	com.cobalt.radar
src/cobalt/heartbeat/probes.py	M	static import reach	com.cobalt.radar
src/cobalt/replay/movers.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/replay/runner.py	M	static import reach	com.cobalt.radar
RESTARTS: com.cobalt.aset com.cobalt.radar
```

The four `0010`/`0011` `.sql` files classify `non-Python src asset  -`; the new `src/cobalt/archiver/*.py` modules classify `static import reach  -` (no resident imports them yet); every `docs/` path `DOCS  -` and every `tests/` path `test/documentation; no resident  -`.

Ops leg (`main..ops/2026-09-19`), its restart-carrying rows:

```
src/cobalt/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/daymode/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/daymode/propose.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/dayopen/checks.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/cli.py	M	static import reach	com.cobalt.radar
RESTARTS: com.cobalt.aset com.cobalt.radar
```

**Recorded, not a stop, and explained rather than left odd:** both legs list this evening's desk prompt files (`49`…`56`, and `53-deploy-d3.md` itself) as `D  DOCS  -`. A two-dot range diffs the two TIPS, and both branches were cut before those files were written, so they read as deletions on the branch side. They are `DOCS  -` either way, main keeps them through the merge, and nothing derives from them. Deploy 2's P7 exited 1 on an unclassified path; **this deploy's two legs exit 0 with nothing unclassified**, so the class that report predicted does not appear here.

**P8 — NO BASELINE RED TO RECORD, and no RED at all.** Every probe `OK`; the only non-OK line is the standing `AMB  com.cobalt.herdr  unmanaged  AMBER launchd unmanaged — loaded, not running (last exit 0) [launchd probe]; runs outside launchd by declared interim`, an AMBER, not a RED. The accepted-RED class the prompt names is therefore **not needed**, and 6.1's rule becomes "no RED at all". `radar  idle (overnight)` and `com.cobalt.radar  running 152 min, heartbeat fresh` — **Saturday is outside every scanning session, so `idle:overnight` is the correct shape**, and `radar.err` was not consulted because there is no RED to qualify. `sheet daymode  day mode reduced · stage 2 … — cards are accepted`. `com.cobalt.replay  pending  never run yet (registered, not yet due)` — deploy 2's job, armed, correct for a Saturday.

**P13 — the production proof, exit 0, nothing applied.** Gate checked term by term: **exit 0**, **no table marked `CHANGED`**, **no tool timeout**. It ran from `/Users/cobalt/cobalt` = main's code, which now includes P4: **26 tables**, as the prompt expects, and it does **not** mention `archive_progress`/`archive_incidents` — their absence is proven by P12, not here.

```
cobalt db migrate — PROOF ONLY on cobalt_brain (READ ONLY, nothing applied)

table                side    schema   rows         digest                             secs
------------------------------------------------------------------------------------------
aset_sizings         user    user     146          baac6ac19fd231057ebe2cbe7443c933   0.04
bars                 system  system   8834532      d21fc0b31265b2565c71432a6ebf6979   53.36
card_dot_taps        user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_dots            user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_stop_edits      user    user     1            cbae670bbd7acd13749ac41d99f25861   0.00
card_transitions     user    user     293          3986eb3f96571498667bbfbc3d80aa8c   0.00
cobalt_email_sends   system  system   463          c8fc112af72900bb260196353fea82bd   0.00
cobalt_jobs          system  system   16           0c2dd08d3961def7bfb783baba3f1694   0.00
cobalt_kill_switch   system  system   1            310655940a3a0ce10031f77a390733b6   0.00
cobalt_redactions    system  system   2            cf42d85ee49244ca40d419683eb3ca51   0.00
day_modes            user    user     10           d9139ce7290afb41d88404c32c59736b   0.00
desk_grade           system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_packet          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_regime          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
missed               user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
movers_daily         system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
picks                user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_membership     system  system   3334         72b94dfe689084e8e96cd880d031e3f3   0.05
radar_pool           system  system   1            88537229826d4da833be389abd8367be   0.00
radar_score          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_receipt  user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_run      system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
session_blocks       system  system   8            ddf2a773f48403300cbff196fe9aa419   0.00
traders              user    user     1            cf9aec4d006d82fb0f12be2d9afa10b9   0.00
vault_overrides      user    user     29           1f529ee789a2e27f1b73010bbae4cdd7   0.00
vault_writes         user    user     1331         e5740123a9afc6db82a34e693dd2bf79   0.12
------------------------------------------------------------------------------------------
26 table(s) probed on cobalt_brain; digest excludes user_id, vault_outcome, vault_reason, account_mode, pool_member_id, rank_metric, rank_value; aset_sizings: 25 card column(s) added by 0007. Proof cost: total 53.6 s — and a migration pays it TWICE (before and after), inside the outage.
NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.
```

**This deploy's outage budget: 53.6 s × 2 ≈ 107 s of proof.** Deploy 2's own preflight measured 47.1 s one-way and its live migration then paid 53.0 s per side — this probe already reads at that higher figure, so ≈107 s is the number to expect rather than a floor to be surprised by. Movement since deploy 2's 15:14 table, recorded not judged, all on tables 0010/0011 do not touch: `cobalt_jobs` **15 → 16 rows** (`com.cobalt.replay`, registered by deploy 2's 5.1), `vault_writes` **1329 → 1331** with its digest moved, `bars` steady at 8,834,532, `radar_membership` steady at 3334. P4's three new tables (`missed`, `movers_daily`, `picks`) are present and empty, as deploy 2 left them.

**P15 — THE TWO BRANCH GATES, both green.**

| branch | file · position | last line, quoted | verdict |
|---|---|---|---|
| `ops/2026-09-19` | `/Users/cobalt/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md`, **line 1817 of 1817** (the last line of the file) | `OPS 0919 DB be00506 (code tip; one docs-only commit fills this sha in) \| code d860bf7 \| offline 1608/0 (302 skipped vs round-2 298; +4 named) \| db 61/0 (tests/cobalt/test_migrate_proof.py only) \| … \| cobalt_dev: 0001–0009, unchanged (every row count and digest identical to the step-0 proof) \| .env: removed, proven gone \| RESTARTS: com.cobalt.radar (d860bf7..HEAD, 0 UNCLASSIFIED; branch 4c14712..HEAD com.cobalt.aset com.cobalt.radar) \| OWED: three-house check on this diff, next deploy prompt \| ESCALATE: 12` | **0 failed** on both suites (`offline 1608/0`, `db 61/0`) |
| `archiver/append-0919` | `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md`, **line 487 of 487** (the last line of the file) | `BACKFILL WINDOW 516f410 (report commit; the code is 85b6d7c, the sixth pin 529e1c9, the DevDoc 64ed1bc) \| R34 B applied: backfill refused 04:00-09:30 ET, ceiling 50 UNCHANGED (grep-proved at 1.2 and 2.1, configs/cobalt/taxonomy/tunables.yaml:494 value: 50) \| sixth pin fixed … GREEN for the first time at 2.2 (d) \| peak demand now 1.00 rpm for a backfill in its own free window (was 51.00 at 04:00) … \| offline 2146/0 \| db 2486/0 \| registry 0001-0011 \| cobalt_dev: RESTORED to 0001-0009 by --rollback --down-to 0009 (exactly 0011 then 0010, nothing else), post-restore proof identical to the post-suite proof on every digest, the only movement across the whole run being the suite's own cobalt_redactions 130 -> 131 \| .env: removed, proven gone \| RESTARTS: com.cobalt.aset com.cobalt.radar (52 paths, 0 UNCLASSIFIED) \| ESCALATE: 4` | **0 failed** on both suites (`offline 2146/0`, `db 2486/0`), registry `0001-0011` |

Neither line carries a non-zero `failed`, and both are present. The L68 gate is not blocked by branch redness.

**P16 — THE L67 GATE, met.** `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md`, last line verbatim:

```
D3 BUILD CHECK · grok: FIX FIRST 1 · gemini: SAFE TO DEPLOY · astra: METER · houses that read it: 2 of 3 · hub-verified REAL: 4 (blocks deploy 3: 0) · NOT REAL: 1 · UNVERIFIABLE: 5
```

**`blocks deploy 3: 0`.** `/Users/cobalt/cobalt-wt/agy-trial/scratch/astra-d3-0919/REVIEW.md` does **not exist** (`ls` → "No such file or directory"), so there is no second `ASTRA D3` line to read; the prompt makes that file conditional and the desk owns how many houses read it. The four MINOR findings left open (G-A1, G-A2, H1, H2) are carried to this report's ESCALATE, not fixed here.

### 0.2 GATE

**GATE: proven — commit + tag allowed by the session allowlist.** P2 created and deleted a tag; P3 created and reset an empty commit; HEAD proven back at `6f4ebc0`. (L62's launch proof.)

### 0.3 Not preflighted

No harmless variant exists inside their own pattern (UNATTENDED-LAUNCH §6), so each is probed by its first real use: `worktree add`, the stack `merge --no-edit`, `merge --ff-only`, `bootout`, `bootstrap`, `kickstart`, `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.

CONTINUE: step 1
