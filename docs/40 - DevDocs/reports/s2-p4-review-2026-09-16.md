# REPORT — S2-P4 plan Astra review (2026-09-16)

Seat: Sonnet 5, hub (L36 only spawner, L35 trust the artifact) · Branch `sprint-2/p4` · Worktree `~/cobalt-wt/s2-p4`.

## §0 Headline

- **S2-P4 plan: REVIEWED — ready for the builder. 3 rounds run (L29/L39 cap), 36 total findings folded as amendments, 0 dissents.** No build occurred; only the plan file and this report changed.
- Round 1: 27 findings (7 BLOCKER, 17 AMEND, 3 NOTE) — all folded (0 dissents). Round 2 (amendment-adequacy check): 7 findings (2 BLOCKER, 5 AMEND) — all defects in the hub's own R1 fold, all folded (0 dissents); 2 BLOCKERs held, forcing Round 3. Round 3 (FINAL per L39): 1 BLOCKER, 1 AMEND — both narrow completeness/ordering gaps in the hub's own R2 fold, folded, not dissented (neither contradicted R1–R7).
- Preconditions: worktree clean at `5312d3d`, rebased onto main (P3's `757b2ac`) → `71b0c16`, docs-only, no conflicts. `codex exec --help` confirmed `-m`/`-s`/`--skip-git-repo-check`.
- Clerk's update applied before Astra read anything: R7 amended in §R, header `Builder:` line, and Cost's `Build:` line — builder = Opus 5 headless this week (Dejan's 2026-09-15 Codex meter ruling, `areas/cobalt-houses.md`); Sol = none until the 09-19 reset.
- ESCALATE: 0 new (hub). Plan's own 4 ESCALATE items: item 2 sound-interim (confirmed); item 3 reframed (data-loss unproven, not established); item 4's L53 proof now sound in design but **stays an open deployment gate** (archiver's ~42rpm already exceeds the unchanged 40rpm ceiling); item 5 corrected for the amended builder, otherwise sound.

## Probe

```
wc -l "docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md" → 307 (pre-round-1, post-clerk-amendment)
codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "...wc -l...reply with only the number." → 307, exit 0, no usage-limit text
```
UP → proceeded to Round 1.

## Round 1 (`p4-review-astra-r1-2026-09-16.md`, 27 findings: 7 BLOCKER, 17 AMEND, 3 NOTE)

Astra's P2-plan read fell back to the sibling worktree (`~/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md`) since P4's own worktree carries no P2 plan file — noted in its own report header, not a defect in this review.

Disposition (the clerk's three rules, per `04-p2-review.md`): none of the 27 findings contradicted R1–R7 (as amended) — every one was a factual correction, first-run failure, tenancy/DDL defect, missing test, or a law gap (L28/L42/L43/L53/L57). All folded under (a)/(c); **0 dissents**.

| id | severity | disposition | plan section amended |
|---|---|---|---|
| 1 | BLOCKER | (c) migration-proof/first-run defect, amended | STEP-1 (digest-exclusion + populated-table proof, both merge orders) |
| 2 | BLOCKER | (c) tenancy/DDL defect, amended | STEP-1 (OWNER/GRANT REFERENCES on movers_daily, user_id DDL expanded) |
| 3 | AMEND | (a)+(c) factual correction + registry proof, amended | STEP-1 (placement/registry order tests, no P2 dependency) |
| 4 | BLOCKER | (c) first-run failure, amended | STEP-3 (fill owns one transaction on every route, savepoint scope) |
| 5 | AMEND | (c) missing propagation, amended | STEP-3 (typed fill result, banner wiring, log destination) |
| 6 | AMEND | (c) tenancy/cohort defect, amended | STEP-3 (USER-connection reads, pick-time cohort includes picked card) |
| 7 | AMEND | (a)+(c) definition correction, amended | STEP-3 CLI + K6 (FILLED transition, not state; deploy-cutoff for K6) |
| 8 | BLOCKER | (c) L57 defect, amended | STEP-1 missed DDL (immutable receipt payload, drop replay_job_id) |
| 9 | AMEND | (a)+(c) formula/test-name correction, amended | STEP-5 (planned_entry vs fill_price, mfe_r as bar-level observation) |
| 10 | AMEND | (c) resolver-usage defect, amended | STEP-5 (use public `_resolve_window_end`, no hardcoded 16:00/15:59) |
| 11 | AMEND | (c) gate-mapping gap, amended | STEP-5 (exhaustive state mapping, (at,id) tiebreak, rule_10 = proxy) |
| 12 | AMEND | (c) coverage-check gap, amended | STEP-5/STEP-6 (bar-coverage verification, exposed in job.result/miss line) |
| 13 | BLOCKER | (c) L53 defect, amended — **ESCALATE 4 corrected** | STEP-6 (one total-demand sum incl. archiver; fetch_bars in TokenBucket) |
| 14 | BLOCKER | (c) first-run failure, amended | STEP-6 (real settings-loader seam for radar.benchmark, pause-safe apply) |
| 15 | AMEND | (c) precondition-timing defect, amended | STEP-4 (tonight's-occurrence check; dry-run after archiver, not at 20:05) |
| 16 | AMEND | (c) missing test/first-run risk, amended | STEP-4 (enforced deadline before 21:40 backup) |
| 17 | AMEND | (c) test-claim defect, amended | STEP-4 (`as_job(skip=True)`, no cache/DDL writes on dry-run) |
| 18 | NOTE | (c) placement precision, amended | STEP-7 (zero-width placement after close_line; requires_vault stays read-only) |
| 19 | AMEND | (a)+(c) factual correction + missing field, amended | STEP-2 (MembershipRecord/PoolRow carry metric+value); K3 corrected |
| 20 | AMEND | (c) rerun-reconciliation gap, amended | STEP-6 (dated run identity, historical --date requires retained data) |
| 21 | AMEND | (c) missing contract/identity, amended | STEP-5 (named adapter callable, per-formation identity) |
| 22 | AMEND | (a)+(c) classifier gap, amended | §5 RESTARTS (explicit replay bootstrap action, smoke-config classification) |
| 23 | BLOCKER | (c) rollback ambiguity/P2-compat defect, amended | §6 Rollback (explicit --down-to 0007, ordered stop/snapshot, both merge orders) |
| 24 | AMEND | (c) checklist not executable, amended | STEP-9 (per-K exact command/expect deferred to build-time spec) |
| 25 | NOTE | no defect — confirms ESCALATE 2 handling sound | §8 ESCALATE 2 (annotated) |
| 26 | NOTE | (a) reframing — data-loss unproven, amended | §8 ESCALATE 3 (annotated) |
| 27 | NOTE | (a) stale builder reference corrected, amended | §8 ESCALATE 5 (annotated) |

Dissents this round: **0**.

## Round 2 (`p4-review-astra-r2-2026-09-16.md`, amendment-adequacy check on Round 1's fold: 2 BLOCKER, 5 AMEND — all defects in the hub's own R1 amendment wording, none re-opening a closed R1 item)

| id | severity | finding | disposition | plan section amended |
|---|---|---|---|---|
| R2-1 | BLOCKER | R1-8's immutable-receipt row collides with R1-20's rerun reconciliation: one unique row per subject can't both stay immutable and be overwritten by a rerun, and deleting a superseded mover breaks `missed.mover_id`'s FK | (c) amended — `run_seq`/`superseded_by` versioning column added to `"user".missed`, unique index narrowed to current-version-only (`WHERE superseded_by IS NULL`); `movers_daily` gains `active` instead of physical delete on reconciliation | STEP-1 (R1-8 block, new R2-1 bullet); STEP-6 (R1-20 bullet corrected to deactivate not delete) |
| R2-2 | BLOCKER | R1-19's fix touched `MembershipRecord`/`PoolRow` but missed `open_members()`'s other consumer, which builds `OpenMember(**row)` — a strict model with neither new field; first resident scan over a populated pool fails validation | (c) amended — `OpenMember` also gains nullable `rank_metric`/`rank_value`, HOLD-path COALESCE, resident-scan test added | STEP-2 (R1-19 block, new R2-2 bullet) |
| R2-3 | AMEND | R1-6's replay-side connection/role question was never folded for STEP-4/6: no single allowed side role can read USER settings, write SYSTEM movers/bars, and write USER misses in one transaction | (c) amended — replay's transaction boundary defined as side-scoped commit phases (USER phase; SYSTEM phase), never a cross-side transaction; STEP-4's "each step in its own transaction" clarified to mean per-side commit boundaries with receipt-keyed recovery | STEP-6 (new R2-3 bullet) |
| R2-4 | AMEND | R1-10's "call the public resolver" doesn't fix the bug: an explicit (non-ambiguous) window text resolves straight to a time without consulting the early-close fallback, so a hardcoded-adjacent defect survives on an explicit-time + half-day combination; bar timestamp convention still unstated | (c) amended — horizon bounded by `min(resolved window end, actual session close)` fetched separately in every case; bar-start timestamp convention stated explicitly; new early-close-with-explicit-window test | STEP-5 (R1-10 block corrected) |
| R2-5 | AMEND | R1-13's all-consumer sum has no named enforcement call site — today's refusal lives only in radar's `load_sources`; archiver and replay bypass it entirely, so the fixed proof design doesn't yet gate any real request path, and the archiver's own ~42rpm already exceeds the unchanged 40rpm ceiling | (c) amended — named shared gate called at every pre-request site (radar, archiver, replay movers+bars); **ESCALATE 4 explicitly kept OPEN as a deployment gate**, not closed by the proof-design fix alone | STEP-6 (R1-13 block corrected); §8 ESCALATE 4 (annotation strengthened) |
| R2-6 | AMEND | R1-22's added sentence ("this deploy doesn't need one") read as exempting the whole P4 deploy from a radar restart, contradicting the derivation table directly above it and D1's explicit kickstart — would violate L42 if taken literally | (c) amended — reworded: the P4 deploy restarts radar/aset per the derivation table; only the 21:05 replay run itself implies no *additional* restart | §5 RESTARTS (R1-22 block corrected) |
| R2-7 | AMEND | R1-21's "named callable + typed result" restated the requirement without naming P2's actual contract; the displayed `missed` unique key still omits member id/trigger timestamp needed for formation identity, so two builders could still implement incompatible interfaces | (c) amended — build step now records P2's actual shipped callable/args/capability marker/typed fields before implementing the adapter; `missed`'s formation-kind key extended with trigger timestamp + member id | STEP-5 (R1-21 block corrected) |

Dissents this round: **0** — every finding was a defect in the hub's own Round-1 amendment wording, not a re-opening of an R1-closed item or a challenge to R1–R7.

Per `02-p4-review.md` §2 (mirroring `04-p2-review.md`): Round 2 held 2 BLOCKERs (R2-1, R2-2), so Round 3 is required before the plan can stand.

## Round 3 (`p4-review-astra-r3-2026-09-16.md`, FINAL per L39 — no fourth round): 1 BLOCKER, 1 AMEND, both legitimate completeness/execution-order gaps in the hub's own Round-2 amendment wording

Note: the round-3 launch prompt mistakenly carried the "Round 2. Read your R1 file..." header text (a hub copy-paste error) instead of "Round 3. Read your R2 file..."; the prompt body's explicit "This is ROUND 3, FINAL per L39" and the r3 output filename were correct, and the report Astra was told to read already contained the full Round 1 + Round 2 disposition tables, so the review proceeded on the correct baseline. Not re-run, given the tight Codex meter this week (26% allowance) and no evidence the mislabel affected Astra's findings (both cite the actual current — post-R2 — plan text). Logged here per L48.

| id | severity | finding | disposition | plan section amended |
|---|---|---|---|---|
| R3-1 | BLOCKER | R2-1's versioning fix can't execute: INSERT-then-link ordering collides with its own partial-unique index (replacement + not-yet-retired predecessor briefly co-exist as current); no representation for a subject whose miss disappears entirely on rerun (corrected bars remove a trigger; a mover rises out of the exclusion set while staying top-N) | (c) amended, folded (legitimate completeness/ordering gap, not a ruling conflict) — `is_current` boolean replaces the `superseded_by`-based partial index; explicit 3-step serialized order (retire-current → insert-replacement → link-predecessor); disappeared subjects retire with `is_current=false`, no successor, retiring run identity recorded | STEP-1 (R1-8/R2-1 block, R3-1 correction) |
| R3-2 | AMEND | R2-4's fix declared bar-start timestamps and "closed" without saying closed **by the horizon** — a bar starting at or before H but running past it (e.g. a 10:30 bar for a 10:30 horizon) could still supply a later stop/close than H allows; the after-window branch needed the explicit `H` formula spelled out | (c) amended, folded — `H` formula stated explicitly (`W` then the after-window branch); eligibility tightened to `bar_start + 1 minute ≤ H`; a trigger leaving no eligible completed bar logs `input_stale`, writes no row | STEP-5 (R1-10/R2-4 block, R3-2 correction) |

Dissents this round, and across all 3 rounds: **0**. Neither Round 3 finding contradicted R1–R7 or introduced a new requirement outside the ruled scope — both were narrow, real defects in the hub's own prior amendment wording, appliable without touching a ruling. Per L39, Round 3 is final regardless of outcome; folding rather than dissenting was correct here (same precedent as the 2026-09-15 P2 review's Round 3). No `## Dissent (Astra)` section was needed or added to the plan.

## ESCALATE

Plan's own 4 ESCALATE items, final state:
- **Item 2** (`trade_count_band` unset): sound interim handling, confirmed unchanged across all 3 rounds — remains OPEN for Dejan to set the band; wiring it into a replay gate is separately specified when he does.
- **Item 3** (radar-cache retention mismatch): reframed — "unexplained data loss" was not established; corrected to a retention/inventory discrepancy pending the Ops ticket. Remains OPEN for Dejan/Ops.
- **Item 4** (archiver vs Finviz ceiling): **the plan's L53 proof is now sound in design** (one total-demand sum across every consumer, enforced at every real pre-request call site, tested for refusal) — but Astra's R2/R3 passes confirm this stays an **open deployment gate**: the archiver's own measured demand (~42 rpm) is already above the unchanged ceiling (40 rpm). Deployment is blocked until either a conforming measured-demand proof lands under the ceiling, or Dejan rules the ceiling/cadence number. This is unchanged from the plan's own framing — the review corrected the proof, not the ruling.
- **Item 5** (schedule): corrected for the amended R7 (builder = Opus 5 headless, not Sol); conditional scope-cut order confirmed sound — each further cut is its own recorded ruling, and cutting the movers i1 archive requires an explicit F13 rescoping alongside it.

No new hub-raised ESCALATE.

REVIEWED 71b0c16 | rounds 3 · dissent 0 · ESCALATE 0
