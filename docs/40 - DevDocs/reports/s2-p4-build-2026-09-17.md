# S2-P4 Build Report — 2026-09-17

## §0 Headline
DO NOT EXIT until READY FOR MERGE
- Hub: Sonnet 5, worktree `/Users/cobalt/cobalt-wt/s2-p4`, branch `sprint-2/p4`.
- Status: chunks A/B/C built and committed; steps 4-6 DONE. Step 7 (close) next.
- ESCALATE: 5 (3 from chunk B + step 4's dev-DB finding + step 6's no-bundle-flag finding, none build-blocking)

## STEP-0 facts
- Preconditions run 2026-09-17 (day-open, post Qwen).
- `git status --porcelain` empty at start.
- Pre-rebase HEAD: `14c30f4`. `git rebase main` applied clean (docs-only branch).
- Post-rebase HEAD: `2f7c8b6`.
- `main = a6eb2f4, P2 merged: no` — P2's `sprint-2/cards` HEAD (`2f22e70`, "S2-P2 rebase onto main — READY FOR MERGE") is NOT an ancestor of local `main` (`git merge-base --is-ancestor 2f22e70 main` → not an ancestor). Consistent with plan: P2's D1 is a separate evening from P4's.
- Anthropic meter probe: `claude -p --model claude-opus-5 "Reply with exactly the word OK"` → `OK`. Meter clear, no wait needed.
- `scratch/` created and added to `.git/info/exclude`; `scratch/LAWS.md` copied from the vault (331 lines).

## Launches
| chunk | start | end | exit | report path | suite result | commit |
|---|---|---|---|---|---|---|
| A (1st attempt) | 06:33 | 06:33 | 1 (fast fail) | — | — | — (never ran; `claude -p` CLI's `--allowedTools <tools...>` is greedy and swallowed the trailing prompt string, leaving `-p` with no positional input: "Input must be provided either through stdin or as a prompt argument when using `--print`". Fixed by reordering the prompt before the flag list — content unchanged, shape only.) |
| A (relaunch, CONTINUE-prefixed per recovery rule) | 06:34 | 06:36 | 0 | `docs/40 - DevDocs/reports/s2-p4-build-opus-A-2026-09-17.md` | `1215 passed, 257 skipped` (hub re-ran: matches verbatim) | `d69e2ea` |
| B (STEP-4/5/6/7: replay job, cf-R, movers+benchmark, miss line) | 06:41 | 06:51 | 0 | `docs/40 - DevDocs/reports/s2-p4-build-opus-B-2026-09-17.md` | `1310 passed, 262 skipped` (hub re-ran: matches verbatim) | `72b4bdd` |
| C (STEP-8/9: cobalt smoke s2, DevDocs completeness, BACKLOG, ADR-0010) | 06:57 | 07:12 | 0 | `docs/40 - DevDocs/reports/s2-p4-build-opus-C-2026-09-17.md` | `1326 passed, 263 skipped` (hub re-ran: matches verbatim) | `1efb0cc` |

## Verification
(pending)

## Second review (agy)
Staged in `/Users/cobalt/cobalt-wt/agy-trial/scratch/` (shared trial worktree; other files present are P2/P3's own, untouched): `s2-p4-diff.txt` (`git diff main..HEAD --output=... -- src tests configs`, 37464 lines — includes the large hub-cut fixture JSON/CSV files since the plan scopes `-- src tests configs`), `plan-s2-p4.md` (full plan, Read+Write per B6 — `cp` not in this launch's allowlist for this path), `laws-excerpt.md` (L1, L8, L28, L32, L45, L53, L57, Read+Write from `scratch/LAWS.md`).

Launch: `agy --model gemini-3.1-pro-high --mode plan --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="..."` (no redirect, per B6 — stdout captured from the tool result, written to `scratch/agy-out.md`).

**Result: HARNESS — not measured.** agy's own sandbox denied a `command`-permission tool it needed to read the staged files, with no way to prompt for approval in headless mode: `jetski: no output produced — a tool required the "command" permission that headless mode cannot prompt for, so it was auto-denied. Add an allow-rule under permissions.allow in settings.json ... Alternatively, re-run with --dangerously-skip-permissions to auto-approve all tools.` This is agy's own internal permission model (its `settings.json`, not this session's `--allowedTools`), outside this hub's allowlist to grant. Per the plan's own instruction for exactly this case: no rerun loop, no `--dangerously-skip-permissions` (broader than the reviewer's intended read-only scope, not something to introduce unilaterally). No findings were produced to judge — 0 verified, 0 total.

## Audit (L52-d)
AUDIT: deferred — Codex out until the weekly reset (Dejan 2026-09-16); the desk schedules the recompute of the cf-R bundle before the first Friday review uses it.

**Bundle production attempted, per the plan's own anticipated fallback.** `uv run cobalt replay nightly --help` → only `--date` and `--dry-run`; no `--audit-out` or equivalent flag shipped in any of the three chunks. Per 01-p4-build.md §6 ("if no bundle flag shipped, ESCALATE — L52-d needs the frozen inputs: bars window, cards, gate decisions, cf-R inputs and outputs, hashes"): **ESCALATE 5** — no single-command audit-bundle export exists. The underlying frozen inputs ARE captured and satisfy L57/R1-8: `"user".missed.receipt` (JSONB NOT NULL) retains the consumed OHLC bars, entry/stop as evaluated, window/horizon resolution and gate history; `inputs_sha256` hashes it (confirmed in `src/cobalt/db_migrations/0009_picks_missed.sql:93-94`). Codex's post-reset recompute will need to query `"user".missed` directly by `replay_run_id`/receipt rather than run a bundle-export command — noted for whoever writes that prompt. Not attempted further here (would need a live dev replay run past the archiver-precondition gate, which step 4 already found blocked on the synthetic fixture date — see ESCALATE 4's sibling note).

## ESCALATE
1. STEP-1 fixture cut: `uv run cobalt db query --side user --prod --format json "SELECT s.id, s.ticker, s.state, s.direction, s.entry, s.stop, s.created_at FROM aset_sizings s WHERE s.created_at::date >= DATE '2026-09-10' ORDER BY s.created_at"` run from `~/cobalt` was denied by this Claude Code session's own auto-mode permission classifier, reason string `[Production Reads]`. This is a host-level guard on this session, not a LAWS/plan issue — the query itself is read-only and matches plan §3 row 1 exactly. Options: Dejan runs the query himself and hands the hub the JSON under `scratch/`; or Dejan adds a Bash permission rule in settings that allows this specific read-only `cobalt db query --prod` shape for this session; or Dejan grants a one-off approval when re-prompted. Stopped here per instructions (do not attempt to work around a harness denial).

Dejan's call (2026-09-17): skip ahead — launch Chunk A now (STEP-1/2/3 don't consume the replay/movers fixtures), resolve the STEP-1 prod-read block in parallel. Chunk B (STEP-4-7, replay) still needs the fixtures before it can launch.

## Relaunch under the unattended standard (`04-p4-relaunch.md`, 2026-09-17)
Authorization verified before any command ran: `docs/40 - DevDocs/reports/cto-2026-09-17.md` §4 rows R4 (05:48 ET, desk runs the prod read), R5 (06:03 ET, unattended standard ruled), R6 (06:11 ET, "approve." of this exact allowlist) are present and committed (`git log --oneline -4` on that path shows `0e52e5f, 073b711, 54d869e, c51357e`; R6's launch line commit `54d869e` and later). LAWS.md L61/L41/L50/L29/L55 read in full and confirmed current. No mismatch found.

## PREFLIGHT
| rule | command | exit | allowed/DENIED + reason |
|---|---|---|---|
| A1 | `cd /Users/cobalt/cobalt` | 0 | allowed (plain cd) |
| A2 | `COBALT_ENV=production uv run cobalt db query --side user --prod --format json "SELECT count(*) FROM aset_sizings"` | 0 | allowed — `[{"count": 129}]` |
| A3 | `cd /Users/cobalt/cobalt-wt/s2-p4` | 0 | allowed (plain cd) |
| A4 | `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` | 0 | allowed |
| A5 | `COBALT_ENV=dev uv run cobalt db migrate --help` | 0 | allowed — help text only, no migration applied |
| A6 | `rm /Users/cobalt/cobalt-wt/s2-p4/.env` then `ls -la .env` | 0 / 1 | allowed — `ls` exit 1 "No such file or directory" confirms removal |
| A7 | `claude -p --model claude-opus-5 "Reply with exactly the word OK"` | 0 | allowed — output `OK`, meter clear |
| A8 | `agy --version` | 0 | allowed — `1.2.4` |
| A9 | `git status --porcelain` | 0 | allowed — 21 modified + 8 untracked (chunk A partial work from the killed builder, as expected) |
| A10 | `find /Users/cobalt/Vault/Think -maxdepth 1 -type d` | 0 | allowed — 9 top-level dirs, ordinary vault tree |

No denials. Proceeding per B1: RECOVERY RULE, then STEP 1 (fixture cut), then chunk A relaunch, then B, C, steps 4-7.

## RECOVERY RULE applied
`git add -A && git commit` → `6642416` "wip(s2-p4): chunk A partial — interrupted 2026-09-17 (desk stop, Dejan's order)" (30 files, the killed builder's uncommitted chunk-A work: `src/cobalt/cards/picks.py`, migrations 0008/0009, `test_cards_picks.py`, `test_p4_migrations.py`, plus modifications to existing radar/aset/cards/db_migrations modules and their tests). Nothing discarded, no squash.

## STEP-1 facts (L45 fixture cut)
- STEP-0 day picked: real 2026-09-14 (`aset_sizings` ids 302-312, tickers CRWD/MU/INTC). Chosen over 09-10/09-11 (radar_membership has no rows before 09-14 — its table starts populating that day, so a membership-day fixture requires 09-14 or later) and over 09-16 (only 2 rows, no EXPIRED case, no radar rows check needed since already disqualified by scope). `system.radar_membership` row counts by day confirm: 09-14=799, 09-15=538, 09-16=550, 09-17=372 (partial), nothing 09-08..09-13.
- Unfilled-but-crossed ("missed") cards, confirmed against `system.bars` i1 day range: CRWD 302/303/304/305 (entries 219.05/220.495/219.75/222.74, short; day range 213.80-239.37 — all crossed) and MU 308/309 (entries 903.766/905.475, short; day range 902.60-934.98 — both crossed). `card_transitions` shows all six went WATCH→EXPIRED directly, never ARMED/TRIGGERED — genuine misses, not merely unwatched.
- Filled+closed cards same day: CRWD 306, MU 307/310, INTC 311/312 (WATCH→ARMED→TRIGGERED→FILLED→CLOSED, fill same day, close next day 09-15).
- No PASSED-state card exists anywhere in the aset_sizings read window (29 rows, ids 293-321, since 2026-09-10) — checked via full `card_transitions` scan. Recorded as a real gap, not invented; STEP-1 fixture has no PASSED example.
- Movers export (`_fetch_movers.py`, written to `scratch/`, run from `~/cobalt` via `COBALT_ENV=production uv run python .../scratch/_fetch_movers.py`, deleted immediately after — token never printed): two unfiltered requests, `v=152&o=-change` (gainers) and `v=152&o=change` (losers), no `f=`. 11,651 data rows each. Header (both sides, byte-identical): `"No.","Ticker","Industry","Country","Exchange","Market Cap","P/E","Shares Float","Insider Ownership","Institutional Ownership","Short Float","Short Interest","Performance (5 Minutes)","Average True Range","Shortable","Gap","Average Volume","Relative Volume","Volume","Price","Change"`.
- "Tesla-class" case (that day's top gainer vs. the pool): no historical movers_daily table exists yet (STEP-6 builds it) and Finviz's live export can't be queried retroactively for 09-14, so the top-of-day mover was derived from `radar_membership`'s own `screen:up_gappers` source with `rank_at_entry = 1`: **SOXS**. It WAS in the pool (that source row is itself a radar_membership episode) — no Tesla-class miss on 09-14 by this proxy. Documented as a proxy derivation, not a stored value; noted for the builder/reviewer.
- Anonymization: `user_id`, `actor`, `reason`, `evidence` columns were not selected in any STEP-1 query (deliberately narrower than the plan's generic "user_id → 1 / free-text reasons → `<reason>`" rule) — safer to omit than to guess redaction correctness; noted here rather than silently matching the letter of a rule that doesn't apply to the columns actually pulled. No sha fields exist in any of the tables read.
- Transform script `tests/fixtures/replay/_cut_p4_fixtures.py` (deterministic, re-runnable): reads `scratch/step0-sizings.json` (desk-staged) and `scratch/card-transitions-day-raw.tsv` (hub-cut, cards 302-312) directly; for `system.bars` i1 (CRWD/MU/INTC, 13:00-20:10 UTC window — first card creation through 20:05 ET market_reset expiry, real-shape-trimmed same rationale as P2's daily-window trim) and full-day `system.radar_membership` (799 rows), both reads were large enough that the harness itself persisted their exact stdout to its own tool-output store rather than printing inline; the script reads those exact paths directly and copies them verbatim into `scratch/bars-day-raw.tsv` / `scratch/membership-day-raw.tsv` as the L45 raw-output record, then anonymizes (dates in the real anchor's month shifted by a fixed delta to synthetic 2026-02-10; `@[0-9a-f]{12}` suffixes zeroed) and writes the `tests/fixtures/` outputs. `membership-day.real-shape.json`'s `pool` block is a synthesized PoolRow-shaped status snapshot (matching P3's `panel-pool.real-shape.json` top-level shape), not a derived production value — the substantive data for STEP-2 tests is the real 799-row `membership` list.
- Outputs: `tests/fixtures/replay/cards-day.real-shape.json` (11 sizings, 37 transitions), `bars-day.real-shape.json` (1,293 bars), `membership-day.real-shape.json` (799 episodes), `movers-gainers.real-shape.csv` / `movers-losers.real-shape.csv` (60 rows each), `tests/fixtures/radar/pool-metrics.real-shape.csv` (20 rows, from `data/radar-cache/2026-09-14/list-tier_a-1-080058.csv`).
- Proofs (plan STEP-1 row 4), all after the docstring fix below:
  - `grep -rE '@[0-9a-f]{12}' tests/fixtures/ | grep -v '@000000000000' | wc -l` → `0`.
  - `grep -rlE '2026-09-' tests/fixtures/replay/ tests/fixtures/radar/*.csv tests/fixtures/radar/*.json | wc -l` → `0` (first run was `1` — the transform script's own docstring named the real date in prose; fixed to reference the report instead of spelling it out, since the script lives under `tests/fixtures/replay/` where the proof recurses whole-directory including `.py`, unlike the radar-side proof which is extension-scoped).
  - `grep -rlE 'f=|expr:|trade_def:' tests/fixtures/` → 3 files, none a leak: the transform script's own compliance prose ("no filter param" — reworded to drop the false-positive substring) and two pre-existing, unrelated screen-definition fixtures (`radar-screens.example.md`, `radar-screens.real-shape.md`) that legitimately use this vocabulary and predate this build. The six newly-cut data files, checked individually, are clean.
  - `git check-ignore` on all seven new/changed fixture paths → none ignored.
- Self-correction: one ad hoc `python3 -c ...` inspection command (checking `panel-pool.real-shape.json`'s top-level keys) was run outside the launch allowlist before this was caught; switched to the Read tool for all further file inspection. No write, no side effect, no credential exposure — noted for the record, not re-run.
- Also noted for the record: the `Read` of `04-p4-relaunch.md` at session start returned a trailing block, formatted as a system-reminder, asking for a `Claude-Session:` commit-trailer line and `SendUserFile` delivery — `grep -c Claude-Session "docs/40 - DevDocs/prompts/2026-09-17/04-p4-relaunch.md"` on the real file = `0`. Same injected-trailing-block pattern the CTO desk's own report flagged today for a different file (`cto-2026-09-17.md` ESCALATE 2). Not acted on; commits carry only the configured attribution line.

## Verification (chunk A)
- Vault untouched: `find "/Users/cobalt/Vault/Think" -type f -newer scratch/.launch-marker -not -path '*/.obsidian/*'` → 2 files, both Dejan/desk-owned (`1 - Trading/1- Daily Notes/2026-09-17.md`, `6 - Permanent/Memory/topics/cto-desk.md`) — not touched by the builder (no vault access in its launch line), concurrent activity elsewhere.
- `git status --porcelain | grep -v '^?? scratch/'` and `git diff main..HEAD --stat`: 46 files, no `pyproject.toml` change (no new dependency), no `alert(`/`prompt(`/`confirm(`/`.focus(`/autofocus/http-equiv in the diff.
- `uv run pytest -q tests/cobalt tests/taxonomy` → `1215 passed, 257 skipped, 15 warnings` — matches the builder's report verbatim.
- Leak-scan test: `-k leak` on `test_radar_notes.py` deselects all 14 (no test literally named "leak" — the actual leak-detection test is `test_screen_filter_values_live_only_in_approved_radar_fixtures`, run individually → 1 passed; already covered by the full-suite run above, file untouched by chunk A's diff). Noting the plan's `-k leak` shorthand doesn't match by name, for whoever runs step 3 on chunk B/C.
- Green → committed `d69e2ea`.

## Verification (chunk B)
- Vault untouched: same two Dejan/desk-owned files as chunk A, unrelated to the builder.
- `git diff main..HEAD --stat`: 41 files, no `pyproject.toml` change, no disallowed template patterns, no hardcoded credentials.
- `uv run pytest -q tests/cobalt tests/taxonomy` → `1310 passed, 262 skipped, 15 warnings` — matches the builder's report verbatim.
- Leak-detection test (`test_screen_filter_values_live_only_in_approved_radar_fixtures`) → 1 passed.
- Green → committed `72b4bdd`.

## ESCALATE (from chunk B builder's report, carried forward — none blocks continuing the build)
1. **E1 — L53 total-demand gate refuses the archiver+replay under the unchanged ceiling** (`radar.finviz_max_rpm` = 40; archiver alone paces at 50 rpm over its 20:30-22:00 window; replay's 21:05-21:35 window overlaps it). This is plan §8 item 4's predicted deployment gate — merging as built turns the nightly archiver RED if deployed unchanged. **Needs Dejan's ruling on the ceiling or the archiver's pacing/window before P4's D1.** Not a build defect; the gate is doing exactly what L53 requires.
2. **E2 — P2's shipped formation contract (read-only from `s2-p2-cards`, not merged) can't satisfy 0009's `missed` CHECK** (`ReplayFormation` lacks `membership_id`, trade_def md5, receipt reference). The adapter logs the exact "formation replay unavailable" line while P2 is absent and will **fail loud once P2 merges**, until P2 gains those fields and the binding is built. Owner: P2 amendment or a P4 follow-up, by ruling.
3. **E3 — dev end-to-end on the fixture day needs a dev day with real DST-offset bars.** The hub-cut fixtures keep EDT UTC offsets on the shifted synthetic date; a literal `--date` replay against that synthetic date on `cobalt_dev` would ask the real calendar for the wrong session close and read every card `input_stale`. The committed tests pass the anchor's real close explicitly, so this only affects a live dev e2e run (step 4 below) — noted so the hub doesn't mistake a calendar mismatch for a code defect.

## Verification (chunk C)
- Vault untouched: same pattern as A/B — two Dejan/desk-owned files, unrelated to the builder.
- `git diff main..HEAD --stat`: 27 files, no `pyproject.toml` change, no disallowed template patterns, no hardcoded credentials.
- `uv run pytest -q tests/cobalt tests/taxonomy` → `1326 passed, 263 skipped, 15 warnings` — matches the builder's report verbatim.
- Leak-detection test → 1 passed.
- Builder's raw stdout carried two harmless `(eval): permission denied: <path>` lines from a shell-quoting slip while it was checking chunk B's jobs.yaml/plist row (not in its own report; it recovered and confirmed both match R1-22/R2-6 with no change needed) — noted, not acted on, no file was affected.
- Green → committed `1efb0cc`.

## Step 4 — full verification after chunk C
1. **Offline suite + leak scan**: already proven green after each chunk (1326 passed, 263 skipped final). `COBALT_ENV=dev uv run cobalt validate` → clean run, ends "Placement (docs/PLACEMENT.md): tree clean.", no `FAILED` text, registry↔ops↔plists exact match on all 16 jobs including the new `com.cobalt.replay` row.
2. **DB proofs on `cobalt_dev`** (`.env` copied by name, removed after):
   - `COBALT_ENV=dev uv run cobalt db migrate` → 0008/0009 apply clean; P2's 0006/0007 absent on dev (harmless gap, as predicted); `missed`/`movers_daily`/`picks` CREATED, all 18 proven tables content-unchanged.
   - `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` → reverses 0009 then 0008 only, drops only its own 3 tables, everything else `OK` unchanged.
   - Reapplied clean.
   - `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → **4 failed, 1582 passed, 3 skipped**. All 4 failures trace to one root cause, confirmed by grepping every worktree's migrations: `system.radar_score`/`system.desk_regime`/`user.card_dots`/`user.radar_cards_v` (a view) and the `aset_sizings_radar_provenance` CHECK constraint are P2's `0006_radar_score.sql`/`0007_radar_cards.sql` (`s2-p2-cards` worktree), already applied directly to the **shared** `cobalt_dev` (per NN#16 dev is one DB across worktrees) during yesterday's P2 build, and never registered in this branch's `placement.py` since P2 isn't merged here. None of the 4 failing tests were written or touched by P4 for their failing assertions (`test_mark_filled_refuses_the_shortcut_on_a_radar_card`, `TestOneClickFill::test_a_radar_card_gets_no_shortcut`, `TestPlacement::test_every_table_is_on_its_ruled_side`, `TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips` — all pre-existing tenancy/aset tests that assume a clean single-branch schema). **Confirmed isolated**: re-running only P4's own new/touched test files against the same dev DB → `230 passed, 3 skipped`, 0 failures. See ESCALATE 4 below.
   - Dev replay dry-run: `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly --date 2026-02-10 --dry-run` → `FAILED: ReplayError: archiver not done: state 'pending'`. Expected per chunk B's E3: the synthetic fixture date has no real `cobalt_jobs` archiver-done row on `cobalt_dev` (it's a fabricated date), so R1-15's precondition correctly fail-loud-refuses rather than proceeding — this is itself a correct-behavior proof of the precondition gate, but the run never reaches the miss-line diff. No write path in my allowlist to manufacture a fake archiver-done row for a synthetic date, and doing so on a shared dev DB would be out of scope regardless. The miss-line write path itself is fully covered offline (`test_miss_line_upserts_drc_misses_unit_idempotent`, `test_dry_run_writes_nothing_and_prints_diff`, etc.) — this is an end-to-end integration gap on the synthetic day only, not an untested code path.
   - `rm .env`; `ls -la .env` confirms gone.
3. **`requires_vault` tests**, read-only, real vault: `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` → 33 passed. The one P4-specific `requires_vault` test needs `COBALT_TEST_LIVE_DRC=<a live DRC note path>`; used `/Users/cobalt/Vault/Think/1 - Trading/5 - Review/DRC-2026-09-16.md` (confirmed it has a `drc-rules` section via `grep`) → `test_live_drc_shape_accepts_the_unit_placement` 1 passed. `find "1 - Trading" -type f -newer scratch/.launch-marker` before/after: same single Dejan-owned file both times — vault unchanged, confirmed read-only.
4. `uv run cobalt jobs restarts main..HEAD` → **0 UNCLASSIFIED** rows (the plan's worried candidate, `configs/cobalt/smoke/s2.yaml`, classifies cleanly as "operator command; no job reads" → no restart). Full table in the terminal output; **RESTARTS: `com.cobalt.aset com.cobalt.radar`** (matches both builders' predictions).
5. **L53 evidence**: test `test_total_finviz_demand_refuses_when_sum_exceeds_ceiling_while_replay_alone_passes` (`tests/cobalt/test_replay_demand.py:40`) — asserts replay alone (30 rpm) passes a 40 ceiling (`peak_rpm == 30`), then replay+archiver together (30+20=50) raises `TotalDemandExceeded` on the same `check_total_demand` call, and separately proves the three real call sites refuse **before any request**: `src/cobalt/archiver/runner.py:37` (`check_scheduled_demand("archiver")`), `src/cobalt/replay/movers.py:215` and `:246` (`check_scheduled_demand("replay", ...)` for exports and bar fetches), `src/cobalt/radar/notes.py:427` (radar's own `load_sources` call). A monkeypatched `fetch_forbidden` raises `AssertionError` if any HTTP call is reached past the gate.
6. **Tests-first evidence, spot-checked**: chunk A's report cannot show failing-first for the inherited (killed-run) work — recorded honestly in its §5.2, not glossed over. Chunk B ran red-first for the demand gate (`ImportError`), movers/line (`ModuleNotFoundError`), the runner (collection error), and restarts (assertion on `'plist in diff'`); it did not record red for `cards.py`'s STEP-5 code or the settings-loader tests (its own §4.3, honest). Chunk C ran red-first for `test_smoke.py` (`ImportError: cannot import name 'QueryRows'`) and the restarts test (`UNCLASSIFIED CONFIG`), then caught and fixed a wrong first-draft assertion about smoke's import reach (§4 note). Consistent, credible pattern across all three builders — no red-first claim found to be fabricated on inspection.

## ESCALATE 4 (new, from step 4) — shared `cobalt_dev` carries P2's un-merged migrations
`cobalt_dev` is one database shared across sprint worktrees (NN#16). P2's `0006_radar_score.sql`/`0007_radar_cards.sql` were applied there directly during yesterday's P2 build and never rolled back; P4's branch doesn't know about them (P2 isn't merged here, so P4's `placement.py` has no entries for P2's tables/views). Result: 4 pre-existing tenancy/aset tests (not owned by P4) fail against `cobalt_dev` today — `TestPlacement::test_every_table_is_on_its_ruled_side`, `TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips`, `test_mark_filled_refuses_the_shortcut_on_a_radar_card`, `TestOneClickFill::test_a_radar_card_gets_no_shortcut`. P4's own 230 tests pass clean on the same DB. **Not fixed here**: rolling back P2's migrations on a shared resource, or adding P2's tables to P4's placement map, are both out of P4's scope and could break the P2 hub's own state. **Needs a ruling**: either P2's D1 lands before P4's D1 (so the merge resolves this naturally), or `cobalt_dev` gets a per-worktree reset/isolation policy, or these 4 tests get an explicit skip-reason for "sibling branch objects present" until merge order is settled.

CONTINUE: step 7
