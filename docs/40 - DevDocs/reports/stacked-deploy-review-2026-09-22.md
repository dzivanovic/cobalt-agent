# Stacked Deploy Review — 2026-09-22

## §0 Headline
Both houses read `05-stacked-deploy.md`. Grok: `RUN IT AFTER` 3 folds. Gemini: `DO NOT RUN` — a real gap in STEP-3.5's RELAUNCH RULE (verified against the file). 4 HOLDS rows block the launch (3 distinct root defects: the RELAUNCH RULE's conditions (i)/(ii) misfiring on an early crash — Gemini; the radar-cycle smoke check that can RED a healthy deploy — Grok; STEP-5's any-hour reach past 21:40 combined with the above — Grok), plus 6 folds total. Full facts below; no verdict of my own (L37).

## L74
(none recorded yet)

## PREFLIGHT
| rule | command | exit | allowed/DENIED + reason |
|---|---|---|---|
| date gate | `date` | 0 | allowed — Tue 2026-09-22 08:27 ET, within R39's window |
| grok present | `grok --version` | 0 | allowed — grok 1.0.25 (f7e67d6988e2) [stable] |
| agy present | `agy --version` | 0 | allowed — 1.2.8 |
| prompt exists | `ls -la ".../2026-09-22/05-stacked-deploy.md"` | 0 | allowed — 59351 bytes, Sep 22 08:13 |
| prompt committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -- "docs/40 - DevDocs/prompts/2026-09-22/05-stacked-deploy.md"` | 0 | allowed — 4063e3cbf0e7fdb6b140f01f69d8808a0d101b25 |
| stagger check | `tail -n 3` of stale-marker-check, ops-6a-check-r3, radar-benchmark-load-review | 0 / 0 / 1 | allowed — neither present report ends `(run in progress — next step under ## CONTINUE)`; radar-benchmark-load-review-2026-09-22.md absent (does not block) |
| recovery | `ls scratch/tribunal-bars-0920/stacked-deploy-0922` | 1 | allowed — "No such file or directory": fresh run |

Authorization (verified before PREFLIGHT):
- R3 (`cto-2026-09-22.md:13`): "we will deploy today after trading day ends at 11 am and continure building and deploying through the day if anything build and ready" — confirmed.
- R39 (`cto-2026-09-21.md:50`): "All approved" ... "Bash(grok *) and Bash(agy *) through 2026-09-22 23:59 ET" — confirmed.
- `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(grok *) and Bash(agy *) through 2026-09-22 23:59 ET" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` → `3ecbb278de133f1838d217e9745728ed6d6337e9` (non-empty, confirmed).

## Packet
Staged in `scratch/tribunal-bars-0920/stacked-deploy-0922/` (Read → Write/copy byte-identical, `wc -c` verified against source for every whole-file copy, trailing-whitespace count 0 on all five whole/part files, all parts ≤38,000 B):
- `05-stacked-deploy.part1.md` (34929 B) + `.part2.md` (24422 B) = 59351 B, matches source `05-stacked-deploy.md` exactly.
- `d3-proven.part1.md` (26842 B) + `.part2.md` (24467 B) = 51309 B, matches source `53-deploy-d3.md` exactly.
- `53-stale.md` (34650 B), matches source `53-stale-marker-deploy.md` exactly.
- `d3-outcome.md`, `branches.md`, `stop-lines.md`, `rulings.md`, `laws.md`, `restarts.md`, `clock.md`, `greps.txt`, `QUESTIONS.md` — compiled/synthesized per §1's spec, sourced from the commands and files it names.
Notable fact surfaced while staging (recorded for the houses, not a verdict): `ops/2026-09-21` was cut before the degraded-line deploy landed `71a8368`/`e21e4c2` on `main` (`src/cobalt/aset/radar_panel.py` + its test); `s2/stale-marker-0921` was cut after. See `branches.md`'s NOTE and `restarts.md`'s FACT.

## CONTINUE
next: write §4 close line

Both houses answered inside a single attempt each. Grok (task `bt4x4g23m`): launched 08:35:27 ET, completed naturally (exit 0) ~08:52 ET — ~16.5 min, over the nominal 15-min budget but it finished on its own (task-notification confirmed `completed`, not stopped by TaskStop) with a full, valid, closing `REVIEW:` line; recorded as-is rather than discarded, per L35 (trust the file) — a stalled/looping house would have been TaskStop'd, this one wasn't stalled. Gemini (task `bqw4nhaad`): launched 08:35:35 ET, completed 08:41:39 ET (~6 min), full closing `REVIEW:` line. Both wrote to `grok-review.md` / `gemini-review.md` in the packet folder (grok wrote its own per its `--allow Write(...)`; gemini printed, hub wrote it byte-for-byte).

## Per question

| Q | grok | gemini |
|---|---|---|
| Q1 ALLOWLIST | Covered (`05-stacked-deploy.part1.md:6`, 44 strings). Unused: `Bash(wc *)`. Wider than needed: `Bash(cd *)`, `Bash(COBALT_ENV=production uv run cobalt jobs *)` (only `jobs restarts` used), `Bash(git -C /Users/cobalt/cobalt add *)`/`commit *` (only the report path used). | Covered, `\?` phone curl matched by `.../*`. Unused: `"Bash(wc *)"`. Wider: `jobs *` (only `jobs restarts` used), `revert --no-edit *` (only `-m 2` used). |
| Q2 L68 GATE | NO — suites run on `<stack>` before any merge into `~/cobalt`; `.env` proven removed on every path before any stop/commit; 3.3's empty diff proves shipped=tested. ops's rebase does pull main's `radar_panel.py` (`71a8368`/`e21e4c2`) into the ops worktree but that path isn't identity-checked for ops nor shipped from ops — stale-marker branch supplies it and IS identity-checked (`part1.md:233`). | Yes on all sub-questions; NO sequence lands unproven code — cites `part2.md:19,27,77` and P10's proof that `rules.yaml`'s move is already inside `<main-at-gate>`. |
| Q3 WINDOW | Planned window matches (`restarts.md`, `part1.md:37-38`). STEP-4 cannot cross the 20:00/20:30/21:10/21:40 clocks (hard stops at 19:55/19:58). **But STEP-5 (0) is allowed "at any hour" and CAN cross 21:40** while `backup.yaml` changes and `com.cobalt.backup` reads it — 2.7's list omits `configs/` (deliberately, delegated to 2.5/2.6), so STEP-5 (0)'s own sentence ("2.7 proved... backup... is in this set") is imprecise. Leaving the radar up contradicts L66's literal text (both residents); disclosed as ESCALATE 1, not silent. | Window matches; radar-up proven safe by 2.7's empty diff; cannot cross clocks under the planned STEP-4 window (19:55/19:58 hard stops). Radar contradiction confirmed and disclosed. Did not examine STEP-5 (0)'s any-hour clause. |
| Q4 ROLLBACK | Holds structurally: ONE `revert --no-edit -m 2`, parent-2-is-main proven, restoration proven empty-diff-or-stay-down, STEP-5 never entered twice. **Defect: STEP-5 (2)'s FAILED line says `residents: DOWN` flatly even when the radar was never booted out** (`part2.md:153` vs `:159`'s per-resident tail, never reached on this branch). | Holds: cites the same mechanics (`part2.md:151,153,157,160`) as sufficient; did not flag the flat `residents: DOWN` wording. |
| Q5 DIALOG | NO — every merge/revert is `--no-edit`/`--abort`/`--ff-only`; no sleep/wait; `ASK DESK` never invoked by a step body; `AskUserQuestion` denied. | NO — same conclusion, same citations. |
| Q6 / verdict | Three concrete sequences: (1) the radar-cycle smoke check (STEP-4.5 (b)) can RED a good deploy — it demands a line "stamped at or after `<t down>`" with only 3 no-wait tails against a ~100s cadence, when the correct proof is pid-unchanged (already present) not a fresh cycle stamp; (2) STEP-5(2)'s flat `residents: DOWN`; (3) STEP-5 (0) crossing 21:40 while `backup.yaml` changes. **`REVIEW: RUN IT AFTER`** 3 folds. | **`REVIEW: DO NOT RUN`** — STEP-3.5's RELAUNCH RULE: a crash before STEP-1 leaves `main` == `deploy/stacked-0922`'s (unbuilt) tip, and condition (i) ("Main = gate's tip, so it was MERGED") fires as a false positive since the gate hasn't diverged yet; a crash right after a FAILED-PREFLIGHT report commit can similarly misfire condition (ii) into an unwarranted STEP-5(2). |

## Checked against the files
All claims below verified by the hub directly against the real files (L35); the houses' words are not trusted on their own (L37 — reporting facts only).

1. **claim** (05's own text): the launch line's 44 allow strings split 29 (byte-identical to `53-stale-marker-deploy.md`) + 6 (byte-identical to `53-deploy-d3.md`, absent from `53`) + 9 (new, in neither). · **who**: `05-stacked-deploy.md:9-13` (self-claim) · **file:line**: verified by `grep -c -F -e "<string>"` for all 44 strings against both `53-stale-marker-deploy.md` and `53-deploy-d3.md` · **HOLDS** — counts are exactly 29/6/9, no string 0-in-both outside the named nine · **blocks the launch? no** — the audit is accurate, not a defect.
2. **claim** (Q1, both houses): `Bash(wc *)` is granted but never used by any step. · **file:line**: `05-stacked-deploy.md:6` (grant) vs full-text search of steps (no `wc` call in any STEP) · **HOLDS** · **blocks the launch? no** — unused breadth, not a hazard; worth a fold for hygiene.
3. **claim** (Gemini Q6, sequence 1 — condition (i) false positive): a crash during STEP-0/PREFLIGHT, before STEP-1 ever runs, leaves `main` and `deploy/stacked-0922` both at the pre-launch cut tip (the desk cuts the gate FROM main's tip, `05-stacked-deploy.md:4`; STEP-0's P2/P3 tag-and-empty-commit probe reverts itself via `reset --soft HEAD~1`, `05-stacked-deploy.md:147-148`, so main is unchanged after STEP-0 completes). On relaunch, `05-stacked-deploy.md:99` mandates "apply STEP-3.5's RELAUNCH RULE FIRST" on ANY relaunch (report file already exists), not only one that reached STEP-4. STEP-3.5's condition (i) (`05-stacked-deploy.md:318`) reads only "Main = `deploy/stacked-0922`'s tip, so it was MERGED" — this is trivially true when the gate simply hasn't been built yet, not only when it was truly fast-forwarded. Condition (i)'s action bootstraps `<restart set>` and runs 4.5 smoke — but `<restart set>` was never derived (STEP-2.5 never ran) in this branch, so the relaunched session has no real value for it. · **HOLDS** · **blocks the launch? yes** — a session that dies early and is relaunched can misread "nothing happened yet" as "already shipped," and if 4.5's smoke passes trivially (production is genuinely unchanged, so it will), the natural continuation is STEP-6's green close — a `STACKED DEPLOY DONE` line and tag for a deploy that never merged.
4. **claim** (Gemini Q6, sequence 2 — condition (ii) "blindly reverts / destroys main"): a FAILED PREFLIGHT before STEP-1 commits only the report file (STEP-6's `add`/`commit -- "<report path>"`, `05-stacked-deploy.md:402`), advancing `main` by one non-merge commit while `deploy/stacked-0922` stays at the original cut tip. On relaunch this can satisfy condition (ii)'s "Main is neither `<pre-merge>` nor the gate tip" (`<pre-merge>` was never established in this branch) and route to STEP-5 (2). · **PARTIALLY HOLDS — the routing bug is real, the stated mechanism ("destroys main") is overstated.** `git revert --no-edit -m 2 <stack-final>` requires `<stack-final>` to BE a merge commit; a lone report commit is not one, so git itself refuses the command (`fatal: mainline was specified but commit ... is not a merge`). That error falls under STEP-5 (2)'s own "Any other revert error → do not improvise another spelling. Go on to (3)" (`05-stacked-deploy.md:389`) — a safe, non-destructive fallback, not data loss. **The real defect**: STEP-5 (1) runs FIRST and boots out any resident that is loaded and named by `<restart set>` — but `<restart set>` was never derived on this branch either, so it is undefined; if a relaunched session substitutes a stale/default and reaches STEP-5(1) with `aset` (and possibly `radar`) actually loaded, it takes them down unnecessarily, at any hour (STEP-5 (0) is "allowed at any hour"), before discovering the revert error and bringing them back up via (3). · **blocks the launch? yes** — an unwarranted, unauthorized-window resident bootout/restart cycle triggered by a misdiagnosed relaunch, not by an actual failed deploy.
5. **claim** (Grok Q6.1 — radar-cycle smoke check): STEP-4.5 (b)'s radar check (`05-stacked-deploy.md:353`) demands a `radar cycle:` line "stamped at or after `<t down>`" via at most three no-wait tails, while P12 records normal RTH cadence as "≈100 s apart" (`05-stacked-deploy.md:210`) and the window's own target is "< 60 s" (`05-stacked-deploy.md:16`). Since the radar is NOT restarted in the expected (aset-only) case, there is no startup burst to anchor a fresh line quickly. · **HOLDS** · **blocks the launch? yes** — a healthy, never-touched radar can plausibly fail this specific check purely on timing, sending a clean deploy to STEP-5 and reverting code that both suites passed. The pid-unchanged check at `05-stacked-deploy.md:345` (part of the same (a) sub-step) already proves "not restarted"; the cycle-freshness check is redundant and miscalibrated against its own stated cadence.
6. **claim** (Grok Q6.2 — STEP-5(2) flat `residents: DOWN`): the FAILED line at `05-stacked-deploy.md:387` ends `rollback: used — residents: DOWN` unconditionally, never naming aset/radar individually, even though STEP-5 (1) (`05-stacked-deploy.md:382`) only boots out residents actually in `<restart set>` — in the expected aset-only case the radar was never taken down. · **HOLDS** · **blocks the launch? no** — a reporting/wording gap (could mislead an on-call read of the stop line), not a mechanism defect; easy one-line fold.
7. **claim** (Grok Q6.3 — STEP-5(0)'s backup-safety sentence): `05-stacked-deploy.md:381` says "2.7 proved that nothing the archiver, replay or backup imports is in this set," but STEP-2.7's own path list (`05-stacked-deploy.md:293-294`) explicitly OMITS `configs` ("dropped because 2.5 derives the two config files") — so 2.7 never checks `configs/cobalt/backup.yaml`, the actual file ops changes. The real backup-safety argument is 2.5/2.6 (backup.yaml classified "no resident reads," no job row added) plus git's atomic file replace during a revert, not 2.7. · **HOLDS as a mis-citation**; the underlying protection (atomic replace, no live resident reader) still stands, so this is not proof of actual harm to a live 21:40 backup run — but combined with finding 4 above (STEP-5 reachable outside the intended window on a misdiagnosed relaunch) it is a real, narrow exposure window, not merely cosmetic. · **blocks the launch? yes**, bundled with finding 4 — not on its own.
8. **claim** (Gemini Q1 / Grok Q1 — L66's "both" vs radar-up): L66's text (`laws.md`, source `LAWS.md:340`) names both `com.cobalt.aset` and `com.cobalt.radar`; `05`'s STEP-2.5 plans aset-only when the derived table doesn't name the radar (`05-stacked-deploy.md:37-38`), and discloses the narrowing as "ESCALATE 1" (`05-stacked-deploy.md:40`, echoing `53-stale-marker-deploy.md`'s own prior disclosure of the same reading). · **HOLDS, and is already disclosed, not silent** · **blocks the launch? no** — R3's override sets aside the window clause, not L66's resident list, so this is a standing, previously-escalated reading carried forward, not a new gap `05` introduces.
9. (i) **Allow-string audit** (mandatory check): every one of the 44 strings counts ≥1 in `53-stale-marker-deploy.md` or `53-deploy-d3.md`, EXCEPT the nine named-new strings, which count 0 in both — see finding 1 above; full per-string counts recorded in this hub's working notes, available on request.
10. (ii) **Placeholder count**: `grep -c -E "R_[_]" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/05-stacked-deploy.md"` → **7**. Per this review's own instruction, a count above 0 is EXPECTED before the desk fills `R__A`/`R__L` and is NOT a blocker.

Where the houses contradict each other: Q3/Q4 — grok found the STEP-5(0)/21:40 and flat-DOWN issues that gemini's answer did not reach (gemini's Q3/Q4 cover only the planned STEP-4 window and the designed rollback mechanics, both of which are sound as far as they go). Q6 verdicts diverge (RUN IT AFTER vs DO NOT RUN) but do not conflict in substance — grok's three findings and gemini's relaunch-rule finding are four DIFFERENT, non-overlapping defects; nothing either house found is contradicted by the other's read.

## Folds proposed
- STEP-3.5: `RELAUNCH RULE condition (i): "Main = deploy/stacked-0922's tip, so it was MERGED."` → `RELAUNCH RULE condition (i): "Main = deploy/stacked-0922's tip AND the report's ## L68 GATE section is present, so it was MERGED; absent that section, nothing has run yet — resume at the step the CONTINUE breadcrumb names."`
- STEP-3.5: `RELAUNCH RULE condition (ii): "Main is neither <pre-merge> nor the gate tip, so a revert was interrupted."` → `RELAUNCH RULE condition (ii): "Main is neither <pre-merge> nor the gate tip AND <pre-merge> is a value this report already recorded, so a revert was interrupted; if <pre-merge> was never recorded, resume at the step the CONTINUE breadcrumb names instead."`
- STEP-4.5 (b): `a radar cycle: line stamped at or after <t down>` → `the LATEST radar cycle: line is stamped no more than 100 s before the current date call (proves the radar did not hang), not necessarily after <t down>`
- STEP-5 (2): `rollback: used — residents: DOWN` → `rollback: used — aset: DOWN — radar: <DOWN|UP, unchanged>`
- STEP-5 (0): `2.7 proved that nothing the archiver, replay or backup imports is in this set` → `2.6 and 2.5 proved backup.yaml is read only by the one-shot com.cobalt.backup/heartbeat, never a resident; 2.7 covers the archiver/replay/radar source paths only`
- General: drop `"Bash(wc *)"` from the launch line (unused by any step), or name the step that needs it.

## ESCALATE
- The nine allow strings and the two branch-named strings all check out against precedent (finding 1); no ESCALATE needed there.
- L66's radar-up narrowing is a carried, previously-escalated reading (finding 8), not new to `05` — desk already has it as ESCALATE 1 inside `05` itself.
- Grok's answer completed at ~16.5 min, over the nominal 15-min budget, but exited naturally (not stalled/looped) with a full valid answer; recorded rather than discarded — flag for the desk in case the budget should be widened for a packet this size (05 alone is 59 KB across 2 parts).

STACKED DEPLOY REVIEW DONE · houses: 2 of 2 · blockers: 4 · folds: 6
