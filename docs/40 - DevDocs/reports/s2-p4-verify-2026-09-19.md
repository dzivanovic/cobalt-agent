# S2-P4 verify — 2026-09-19

## §0 Headline
- DO NOT STOP until READY — reached 10:50 ET: READY, code tip `a59ee8c`, rebased onto main `919d562`; offline 1796/0, dev-DB 2114/0, `cobalt_dev` 0001–0009, rollback round trip ok.
- Built: column set one source + 151-column movers fixtures · plan correction (row 2) · smoke `compare` kind, K8 machine assertion (row 4) · K9 short side (row 8, L52 bound held) · K3 SQL ran on Postgres (row 6).
- **`Asset Type` rule ESCALATED**, not built: 16 fund rows have a blank `Asset Type` (table C ≠ 0); the desk chooses the rule (ESCALATE 1). K3 HOLD ambiguity confirmed on Postgres (ESCALATE 2).
- Found and fixed on the DB run: 3 test-side reds (2 params of a migration test, 1 P2 test vs P4's `FillResult`); no src change.
- ESCALATE: 16. OWED: three-house check, receipt-reference tribunal, deploy prompt.

## AUTHORIZATION
Written by the CTO desk (Fable `318239db`), not by Dejan. Verified by this session, 09:2x ET:
| check | evidence |
|---|---|
| rows committed on main | `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` → `db2dcb1`, `e748c8c`, `8b0a3bb`, `32bbfe8`, `6c582ef`, `920b0a9`, `11960d9`, `4da7429` (all desk commits; the R-rows `cto-2026-09-17.md` R5/R6, `cto-2026-09-18.md` R1/R11 are older, on main) |
| `cto-2026-09-17.md` R6 quote | line 47: 06:11 ET "approve." — the P4 hub list (`_fetch_movers.py` vault-token fetch, `.env` cp/rm by exact path, `COBALT_ENV=dev` migrate/pytest/validate/replay dry-run, read-only real-vault pytest, `claude -p`, git add/commit/rebase main, plain reads, `CronCreate`) |
| rule strings vs committed launch lines (`grep -c -F`, each = 1) | `01-p4-followup.md`: migrate*, replay nightly *, dev pytest/vault pytest triple, cp/rm/migrate/validate quad, `git rebase main`, and the tail `claude -p *` … `CronCreate` — identical except `Bash(grok *)` (DROPPED, as this prompt says); `04-p4-relaunch.md`: `_fetch_movers.py*` run, its `rm`, `_cut_p4_fixtures.py*` — each 1 |
| a rule in neither file | none |
| mismatch | none |

## PREFLIGHT
| rule / probe | command | exit | result |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | `Sat Sep 19 09:22:44 EDT 2026` |
| `Bash(git status*)` | `git status --porcelain` | 0 | empty |
| `Bash(git status*)` | `git status` (long) | 0 | "On branch sprint-2/p4 / nothing to commit, working tree clean" — no rebase in progress |
| `Bash(git log*)` | `git log --oneline -1` | 0 | `2a8784c` (first launch) |
| `Bash(git -C … status*)` | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | ` M docs/40 - DevDocs/reports/seat-usage.md` — desk's, not this session's, untouched |
| `Bash(git -C … *log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | `db2dcb1` (main tip; docs-only desk commits after deploy `2893a7f`/`8d17cee`) — the main this run rebases onto |
| `.env` cp | `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` | 0 | allowed |
| `COBALT_ENV=dev … migrate*` | `COBALT_ENV=dev uv run cobalt db migrate --help` | 0 | allowed (this branch's help lists `--allow-prod --down-to --rollback`; no `--proof-only` yet: main's harness arrives with the rebase) |
| `.env` rm | `rm …/.env` then `ls -la .env` | 0 / 1 | allowed; `ls: .env: No such file or directory` |
| `Bash(claude -p *)` | `claude -p --model claude-opus-5 "Reply with exactly the word OK"` | 0 | `OK`, no usage-limit message (L47 meter clear) |
| `Bash(uv run pytest *)` | `uv run pytest --co -q tests/cobalt/test_tenancy.py` | 0 | 34 collected |
Denials: none. The two script rules (`_fetch_movers.py`, `_cut_p4_fixtures.py`) are probed by first real use.

## §1 Rebase onto main
| item | value |
|---|---|
| main at 1.2 | `919d562` (desk docs-only commit after `db2dcb1`; the deploy tip is unchanged) |
| 1.1 measured by this run | `main..HEAD` = 65 rows; `976528f..HEAD` = 31; `--cherry-mark --right-only main...HEAD` = 34 `=` (976528f and below) + 31 `+` — agrees with the desk |
| `git rebase main` | dropped the 34 patch-identical commits itself ("skipped previously applied commit" ×34); replayed 31 |
| conflict stops | **ONE**, at 6/31 (`7ead430` chunk A), docs only: `docs/40 - DevDocs/cobalt/db_migrations/cli.md` — main's new "no `schema_migrations` ledger" paragraph vs P4's `## 2026-09-17 — S2-P4` section; both kept (main's first). No stop in `placement.py`, `__init__.py`, `cli.py`, tests or configs (the expected zones merged clean). The stop's subject is a P4 commit, not one of the 34 |
| resolver | ONE Opus headless run, brief `scratch/rebase-brief-v1.md`, note `scratch/rebase-resolve-v1.md`; hub-verified: `git diff --check` clean, no marker lines, `tests/cobalt/test_p4_migrations.py` + `test_cards_picks.py` 25 passed 17 skipped; `git add` that one file; `GIT_EDITOR=true git rebase --continue` |
| launch-shape note | the first resolver launch (the `01-p4-build.md` step-2 shape, prompt LAST) exited 1 in 0 s with `Input must be provided either through stdin or as a prompt argument` — `--allowedTools` is variadic and swallowed the trailing prompt as a tool name. Nothing ran, worktree untouched. Relaunched with the prompt FIRST (`claude -p "<prompt>" --model … --permission-mode acceptEdits --allowedTools <the same 17 rules, byte for byte>`), the shape `03-p2-rebase.md` §2.2 used. Same rule (`Bash(claude -p *)`), same list, no denial. **Every builder launch in this run uses the prompt-first shape** (finding for the desk: `01-p4-build.md` step 2's shape cannot run as written) |
| 1.3 count | `git log --oneline main..HEAD` = **31**; the two subject columns are identical row for row (same order: `04112ed docs(plan): S2-P4 plan + report …` at the bottom → `fc61ac2 docs(report): fill the close commit sha …` at the top); none missing, none extra |
| 1.3 harness diff | `git diff main HEAD -- src/cobalt/db_migrations/cli.py placement.py __init__.py` read in full: `__init__.py` = 0008/0009 in `FORWARD` + `REVERSE` (numeric order) + docstring; `cli.py` = `DIGEST_EXCLUDED_COLUMNS` gains `rank_metric`, `rank_value` (+ comment); `placement.py` = `CREATED_TABLES` gains `movers_daily`/`picks`/`missed`, `missed` leaves `DECLARED_TABLES`. Every changed line is P4's; not one line of main's harness (streamed proof, `--proof-only`, REPEATABLE READ, ambiguous-name refusal) altered or missing |
| 1.3 suite | `uv run pytest -q tests/cobalt tests/taxonomy` → **`1753 passed, 322 skipped, 1 xfailed, 15 warnings in 54.68s`**, 0 failed (09-18's offline line was 1714 passed; main added tests: more, not fewer) |
Tip after the rebase: `fc61ac2`.

## §2 Chunk AT — `Asset Type`
### 2.1 The export (hub, one production-token READ)
| item | value |
|---|---|
| FACTS checked before briefing | `radar.yaml` `export.v: 152`, `columns: "0-150"`, `required_headers` carries `Asset Type`; `radar/collector.py:161` builds `c` from `range(151)`; `replay/movers.py:210` `_params` builds the SAME set as a second literal; `radar/throttle.py:180` and `radar/propose.py:180` carry two more `range(151)` (four sites, not the two the desk named — the builder gets all four). The old fixtures were 21 columns: `ls -la` before the run: `scratch/movers-gainers-raw.csv` and `-losers-raw.csv` = 1,428,920 bytes each, Sep 17 06:26 |
| `_params` (quoted, `replay/movers.py:209-210`) | `def _params(self, side): return {"v": self.config.export.v, "c": ",".join(str(v) for v in range(151)), "o": SIDES[side]}` with `SIDES = {"gainers": "-change", "losers": "change"}`; no `f=` |
| script | `scratch/_fetch_movers.py` (not committed; deleted after the run): `resolve_token` + `finviz_get` from `cobalt.archiver.collector`, both silencers first, ONE outer `try/except Exception` → `ERROR <type>` + `sys.exit(2)`, inner retry silent (60 s, once), both bodies validated in memory (151 columns, `Asset Type` in the header, ≥1 row) before either file is written |
| token-safety: checked | script lines: 21–24 `logging.disable(CRITICAL)` then loguru `logger.remove()` BEFORE any cobalt import or credential call; 28 the only transport import; 41 `resolve_token()` inside the retried block; 47–52 `finviz_get(..., on_metrics=…)` — the callback prints an int status only; no `raise_for_status`, no `__cause__`, no `str(e)`, no URL/params/header/token in any print or f-string; 58–64 the retry handler prints nothing; 73–78 the one printing handler prints `type(e).__name__` only. Read against the run prompt paragraph line by line before the run |
| run 09:28–09:29 ET | `cd /Users/cobalt/cobalt` → `COBALT_ENV=production uv run python …/_fetch_movers.py` → tool result, verbatim: `gainers status 200` / `gainers rows 11640 columns 151` / `losers status 200` / `losers rows 11640 columns 151` / `written 2 files`. Exit 0, no retry needed (2 requests). Nothing else was in the tool result |
| cleanup | `cd` back; `rm …/_fetch_movers.py` → `ls scratch/_fetch_movers.py`: `No such file or directory`; `git status --porcelain` empty |
| after | `scratch/movers-gainers-raw.csv` and `-losers-raw.csv` = 11,481,555 bytes each, Sep 19 09:29 (151 columns; the top rows are cut by the cutter, 60 per side) |
| L53 note (for ESCALATE) | two Finviz `/export/screener` requests at ~09:28–09:29 ET on a Saturday, radar `idle:overnight`, archiver not running |

### 2.2 The evidence (builder AT-0, commit `27b1d36` `test(fixtures): Asset Type evidence mode for the P4 cutter`)
Builder report `s2-p4-verify-opus-AT0-2026-09-19.md`; tests-first: 10 red (missing attribute) → 11 green in `tests/cobalt/test_p4_asset_type_evidence.py`; hub re-ran that file (11 passed). Hub ran `uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence` (first real use of that script rule: allowed): stdout `files: 16307 (without the columns: 0); rows: 643998 (without the columns: 0); distinct fund tickers: 5702; distinct Asset Type values: 28; table C total: 16`. Inputs: every `*.csv` under `data/radar-cache/` (5 days, 09-14…09-18; scan snapshots repeat rows, so row counts are not distinct-ticker counts) + the two new raw movers exports.
Table A (non-blank `Asset Type`, with its `Industry`) — every one of the 28 values pairs ONLY with `Industry = Exchange Traded Fund`:
| Asset Type | rows | | Asset Type | rows |
|---|---|---|---|---|
| Bonds | 6176 | | Multi-Asset - Tactical / Active | 278 |
| Carbon Trading | 8 | | Multi-AssetTarget Date - 2030…2070 (9 values, 2 each) | 18 |
| Closed End Funds | 18 | | Preferred Stock | 176 |
| Commodities & Metals | 7890 | | Private Equity | 12 |
| CryptoCurrency | 7283 | | SPAC | 4 |
| Currency | 30 | | Hedge Fund Replication | 36 |
| Equities (Stocks) | 109257 | | MLP | 34 |
| Equities (Stocks) - IPO Based | 12 | | Multi-Asset - Conservative / - Growth-Aggressive / - Moderate / - Spread Between Asset Classes | 30 / 16 / 28 / 22 |
| Freight Futures | 4 | | | |
Table B (`Industry = Exchange Traded Fund`, by `Asset Type`): the same 28 values with the same counts, plus **`<blank>` = 16 rows**.
Table C: **(i) non-blank `Asset Type` with a non-fund `Industry` = 0 rows; (ii) fund `Industry` with a BLANK `Asset Type` = 16 rows (8 distinct tickers, each in both movers exports, none in the radar cache); total 16.**
DECISION RULE, in order: (0) zero funds — no (5702). (i) sample floor — met (5702 ≥ 10 distinct fund tickers, 28 ≥ 2 values). (ii) requires table C = 0 rows — it is 16. (iii) **`Asset Type: ESCALATED`.** Built: 2.3 and 2.5. NOT built: 2.4's rule, `not_equity` unchanged, `REQUIRED_HEADERS`/`unreported` branch untouched (the hub reads "nothing of 2.4's rule" to include them; both are named in 2.4). Reading of the evidence for the desk (no build): "non-blank `Asset Type`" never mislabels a stock (0 rows) but misses 8 real funds that Finviz leaves blank (the movers lists are the whole market and are where they show); `Industry = Exchange Traded Fund` includes every row of table A plus those 16 (table B ⊇ table A: every non-blank value sits on an ETF industry row), so on this sample it is the superset rule. Whether `Industry` could ever label a non-fund is not testable here — no non-fund row carries `Exchange Traded Fund`. Note `not_equity.values: [Exchange Traded Fund]` is already an `Industry` value, but `not_equity.header` is `Asset Type` — pointing the header at `Industry` would be a one-line config change; that is the desk's ruling to make, not built here.

### 2.3 / 2.5 The change (builder AT-1, commit `05ece47`; builder report `s2-p4-verify-opus-AT-2026-09-19.md`)
`fix(s2-p4): movers take the radar's column set from one source; fixture re-cut at 151 columns — the not-equity rule is ESCALATED, not built` (the escalated wording the prompt prescribes). Brief `scratch/at1-brief.md` carried all FOUR `range(151)` sites.
| item | result (hub-verified) |
|---|---|
| 2.3 one source | `cobalt.radar.config.screener_columns()` / `screener_columns_param()` beside `ExportConfig` (parses `a-b` / comma list, anything else crashes, L1); called by `radar/collector.py`, `replay/movers.py`, `radar/propose.py::_columns` and `radar/throttle.py` (`run_probe(columns=…)`, required kwarg; `command()` reads the config). Hub: `grep -rn "range(151)" src/cobalt` → **no output (0 hits)**. No config shape change |
| 2.3 tests | `test_every_finviz_consumer_asks_for_the_same_column_set` (collector = movers gainers = movers losers = throttle probe = the written-out string; passes before and after by design, stated) · `test_only_one_function_in_the_new_core_builds_the_column_set` (RED before: 4 literals) · `test_screener_columns_parses…` / `test_a_malformed_column_declaration_crashes` (RED: ImportError) |
| 2.5 fixture | new cutter mode `movers` (`uv run python tests/fixtures/replay/_cut_p4_fixtures.py movers`) re-cuts ONLY the two movers fixtures — the no-argument mode re-cuts every fixture and needs raw inputs that no longer all exist, so the prompt's bare command was not used (finding; the no-arg mode still works as before). Builder ran it: gainers 60 rows, 151 columns, **29** with a non-blank `Asset Type`; losers 60 rows, 151 columns, **19**; nothing appended (a real fund row sat at gainers rank 5 / losers rank 7); `_anonymize` shifted 23/26 dates; `grep -c "2026-09-"` on both fixtures = 0/0. Header row byte for byte from the raw file up to line endings (raw CRLF → LF, as the P2 cut of `pool-metrics.real-shape.csv` already is; a CRLF header would not equal the radar fixture's — one shape, asserted) |
| 2.5 tests | movers fixture header == radar `pool-metrics.real-shape.csv` header byte for byte · 151 columns with `Asset Type` · ≥1 real non-blank row per side · `parse_movers` reads `Asset Type` from the export · evidence tests now use the movers fixture as a REAL input (tables A/B non-empty; table C = 0 in the top 60, so C's disagreement path is covered by the hub's full-cache run only) |
| leak test | `test_screen_filter_values_live_only_in_approved_radar_fixtures` passes (inside the suite) |
| 2.6 suite | hub: `uv run pytest -q tests/cobalt tests/taxonomy` → **`1783 passed, 322 skipped, 1 xfailed, 15 warnings in 54.59s`**, 0 failed |
| 2.6 `not_equity` | `grep` of `src/cobalt/radar` + `replay`: the exclusion line (`radar/runner.py:144`), `NotEquityConfig`, `benchmark_misses`, `REQUIRED_HEADERS`, the `unreported` branch and `radar.yaml` are UNCHANGED (diff read) |
| 2.6 RESTARTS | `uv run cobalt jobs restarts main..HEAD`: `RESTARTS: com.cobalt.aset com.cobalt.radar` (0 UNCLASSIFIED); this chunk's own `src/cobalt/radar/*` and `replay/movers.py` changes derive radar/aset as before; **`not_equity` unchanged — no RESTARTS from this chunk's config** (no config file touched). The table also lists `D` rows for desk docs — main gained them after this rebase; they are not P4 changes |
Builder findings carried: (a) **the re-fetched exports are a different trading day (09-19 data) from `membership-day.real-shape.json` (the 09-14 anchor day)** — the re-cut forced ticker repairs in `test_replay_movers.py` (8 tests), `test_replay_runner.py` (2) and the evidence tests; each benchmark test now points at a real row that carries its case, but the mover set and the membership set no longer come from one session; re-cutting the membership fixture from the same day needs a production DB read — desk's call (ESCALATE 13). (b) `radar/throttle.py` and `propose.py` also hard-code `v: 152` — the same L3 duplicate one field over, not fixed (ESCALATE 14). (c) the two sibling cutters `_cut_p2_fixtures.py` / `_cut_panel_fixtures.py` mention the anchor-month pattern in comments (a `grep -rlE '2026-09-'` over the fixture dirs lists them; no fixture data leaks) — pre-existing, not mine.

## §3 Chunk TR — triage rows 2 · 4 · 8
Split into two builder runs (TR-A row 4 + the K3 test; TR-B row 8) because rows 4 and 8 both edit `configs/cobalt/smoke/s2.yaml` and `git add -p` is not available: one commit per item needs one run per item. Briefs `scratch/tr-brief.md`, `scratch/tr2-brief.md` (the two STOP sentences of rows 4 and 8 verbatim in the briefs).
| row | result |
|---|---|
| 2 plan text | hub, docs only: `[corrected 2026-09-19 — cto-2026-09-18.md §21 row 2 …]` under BOTH old wordings (STEP-5 Formations bullet, line 184; R1-21 bullet, line 189→190), old text kept. Commit `1bc0dd9` `docs(plan): s2-p4 — the shipped replay entrypoint, corrected in place`. Verified against the tree first: `radar/evaluate_cli.py:146 def replay_formations`, `radar/evaluate.py:132 EVALUATOR_VERSION = "s2p2.1"`. **BUILT** |
| 4 compare kind | TR-A (`s2-p4-verify-opus-TRA-2026-09-19.md`), commit `089b069` `feat(smoke): compare check kind; K8 counts_agree is a machine assertion again`. Design: one optional additive field `result_number` on `sql`/`job_row` names THE number of a check; `kind: compare` (`left/right/op`, only `eq`) is validated at load (unknown id, self-reference, operand below the row, operand with no number, op not eq → loud) and graded at run (equal PASS · unequal FAIL · operand ERROR → ERROR · non-numeric → ERROR); shipped `K8.3` `left: K8.1, right: K8.2, op: eq`, hand comparison text removed from K8.1/K8.2. STOP sentence did not trigger: an old smoke file with no compare row loads and runs byte-identically (a test proves it). Tests first: 6 red (quoted in the builder report) → 6 green. GRANT-reading test from FX kept. Hub: suite `1788 passed, 324 skipped, 1 xfailed, 0 failed`; `git diff` of `s2.yaml` read. **BUILT** |
| 6 (K3 test file) | commit `7b3846c` `test(smoke): K3's SQL runs on Postgres` — `tests/cobalt/test_smoke_k3_sql.py`, 2 `requires_db` tests (skip offline: 2 skipped, quoted by the builder); their run is step 4.6 |
| 8 K9 short side | TR-B (`s2-p4-verify-opus-TRB-2026-09-19.md`), commit `0443112` `fix(s2-p4): K9 compares against the rows the export really had (short side)`. `ReplayResult.movers_by_side` records per side `{exported, top_n, expected = min(top_n, exported)}` (validated as `min`); K9 became six rows (K9.1–K9.6: user-side stored count, system-side job row's `expected`, `compare` eq, per side) — the same tenancy wall as K8, no new grant. **L52 STOP sentence did not trigger** — hub read the diff: the only changed line inside `parse_movers` is the constructor call (`rows=tuple(rows[:top_n])` intact, `+ exported_rows=len(rows)`); `benchmark_misses`, `archive_movers`, `MoversStore`, `MoversCollector.exports`, `retained_exports` have zero changed lines; the runner gains one statement (`export_counts`) before any write; the builder also executed HEAD's `parse_movers` out of git against the new module at 8 caps × 2 sides: identical rows (`scratch/proof_rows.py`, gitignored). Tests first: 5 red (+1 collection error) → 8 green. Hub suite: `1796 passed, 324 skipped, 1 xfailed, 0 failed`. **BUILT** |
Consequences for the deploy prompt (ESCALATE 15): the replay `job.result` shape gains `movers_by_side`; until the first `com.cobalt.replay` run after deploy no job row carries it, so K9.2/K9.5 read FAIL and K9.3/K9.6 ERROR (a smoke run before that night is RED by design). The two new K9 SQL statements have never run on Postgres — step 4.5's `test_committed_queries_run_read_only_on_cobalt_dev` is their first run.

## §4 DB verification — `cobalt_dev` ONLY (10:35–…, `.env` copied in at 10:35 by name, never read)
### 4.2 State found (before any change), `COBALT_ENV=dev uv run cobalt db migrate --proof-only`, 10:35:30 ET
Verbatim facts: 26 tables probed; `aset_sizings` 1 row, **`bars` 1,043,443 rows**, `cobalt_jobs` 13, `vault_writes` 184; the P2 tables (`radar_score`, `radar_score_run`, `radar_score_receipt`, `card_dots`, `card_dot_taps`, `card_stop_edits`, `card_transitions`) present; **`missed`, `movers_daily`, `picks` = `-` (absent)**; `radar_membership`/`radar_pool` 0 rows. Proof cost **total 6.4 s** (`bars` 6.35 s), "a migration pays it TWICE". Last line: `NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.` (Full table is in the tool result; the numbers above are the ones the desk budgets from.)
So `cobalt_dev` held 0001–0007 (P2's re-apply from 09-18 stands) and **NOT** 0008/0009 — the prompt's "probably 0001–0009" was wrong. 4.3 (rollback first) does not apply: nothing of 0008/0009 to reverse.
### 4.4 Forward, `COBALT_ENV=dev uv run cobalt db migrate`, 10:35:43 ET
Because the harness keeps no ledger, this was a FULL forward over the registered list, not P4's alone: `-- applying 0001 … 0009` in order (nine lines). Result table: 23 pre-existing tables `OK` (digests before = after on every one); **`missed` CREATED (0 rows), `movers_daily` CREATED (0), `picks` CREATED (0)**; last lines `26 table(s) proven … content UNCHANGED on every table.` / `proof cost: BEFORE 5.9 s + AFTER 6.0 s = total 11.9 s; slowest table bars (5.8 s before).` No `CHANGED`, no serialization failure, no refusal. Since the forward was a full 0001–0009 pass, its duration is not P4's alone; the deploy number is 4.7's second forward (0008 + 0009 on a database that already holds 0007).
`system.bars` on dev = 1,043,443 rows; production has 8.83 M (≈8.5× more) and its proof took 104 s — dev's 6 s is NOT a prediction for production; 0008/0009 add no rows to `bars`, so the proof cost the deploy budgets is production's own last number (104 s before + 104 s after), plus P4's DDL, which ran here in the same forward pass without a separately measurable cost.

### 4.5 DB suite (10:36–10:39), `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`
**`3 failed, 2111 passed, 6 skipped, 1 xfailed, 15 warnings in 185.15s`.** The four reds of 09-17 ESCALATE 4 / followup ESCALATE 2 are GONE (0006/0007 sit under P4; none of the tenancy/placement tests is red). The 3 that ARE red are new, all test-side:
| test | assertion | cause |
|---|---|---|
| `test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]` and `[p4_before_p2]` | `test_p4_migrations.py:297` `assert _probe(conn, "radar_membership") == before` → the only differing item is `'seconds'` (0.00123 vs 0.00173) | main's harness (rebased under P4) adds the probe's wall time to `_probe`'s dict; P4's test compares whole dicts. Content (`digest`, `rows`) never differed |
| `test_radar_cards_db.py::test_manual_cards_are_unaffected` | `assert len(ids) == 2` → `TypeError: object of type 'FillResult' has no len()` | P2's test (now on main) treats `CardStore.fill` as returning a list; P4's ruled R1-5 returns `FillResult` (`transition_ids`) |
Neither is a live-value assertion on production-mirroring data, and no data was touched. Fix: tests only, builder run FIX-DB after 4.8 (no builder while `.env` sits in the worktree); the DB suite is then re-run in a second `.env` cycle (below).
### 4.6 ROW 6 — K3 on real Postgres (10:38)
`COBALT_ENV=dev uv run pytest -q tests/cobalt/test_smoke_k3_sql.py -rA` → **`2 passed in 0.18s`**: (1) **K3's statement EXECUTED** on `cobalt_dev` through the framework's own loader/renderer/read path, returned exactly one row with exactly the five declared columns (`post_deploy_admitted, metric_missing, value_null, rescanned_admitted, rescanned_metric_missing`); on dev `radar_membership` and `radar_pool` hold 0 rows (the proof table), so K3's baseline counters are all 0 by construction — the check would read KNOWN there (inferred from the empty tables; the test asserts shape, not the zeros). (2) **`test_k3_hold_row_reads_red_documented_ambiguity` — a HOLD-shaped row (pre-deploy `first_seen_at`, `rank_metric` NULL, `last_scan_id` = the pool's post-cutoff scan) made `rescanned_admitted ≥ 1` and `rescanned_metric_missing ≥ 1`, and the framework's grader returned FAIL naming `rescanned_metric_missing`.** So build2 ESCALATE 6 is CONFIRMED on real Postgres: a degraded-source night that HOLDs a pre-deploy episode reads K3 red, indistinguishable by column from a RETAIN defect. The rows lived in the test's own transaction and rolled back (radar tables 0 rows after). Nothing built. Also `test_committed_queries_run_read_only_on_cobalt_dev` → **1 passed**: every committed SQL check of `s2.yaml` (K2, K3, K5.x, K6, K8.2, K9.1, K9.4, K10.2 …) ran read-only on `cobalt_dev` — the two new K9 statements' first execution.
Decision for the desk (ESCALATE 6, unchanged options): accept the loud false positive with the read-against-`degraded_sources` procedure already in K3's `expect_text`; or a row-level discriminator (schema change, its own chunk); or revert `b327221`-lineage (K3 RETAIN grading). Recommendation: accept — it is loud, documented, and only reachable on a night with a degraded source.
### 4.7 Rollback round trip (10:39–10:40)
`COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` (10:39:54) → applied `0009_picks_missed.rollback.sql` then `0008_radar_value_movers.rollback.sql` and NOTHING else; `missed`, `movers_daily`, `picks` **DROPPED**; the other 23 tables `OK`, digests before = after; `26 table(s) proven … content UNCHANGED on every table`; proof cost 12.5 s. Then `COBALT_ENV=dev uv run cobalt db migrate` (10:40:11): the same nine `-- applying` lines, the three relations **CREATED**, 23 `OK`, proof cost **12.2 s** (BEFORE 6.1 + AFTER 6.1) — the number for P4's 0008 + 0009 on a database that already holds 0007 (the harness has no ledger, so the run re-applies the whole list; the proof is what costs, and it scales with `bars`: production's own last figure is the budget, dev's 12 s is not).
Side by side, 4.4 forward vs this second forward: every schema-object line is EQUAL — each of the 23 pre-existing tables `OK` with the same digest before/after, `missed`/`movers_daily`/`picks` `- -> user|system … CREATED` with digest `- -> d41d8cd9` and 0 rows; the nine `applying` lines equal. Differences, all explained (R18): durations (5.83/5.98 vs 6.08/6.02 s for `bars`, etc.); **`cobalt_redactions` 123 → 124 rows (digest `2797ee97` → `19538c9d`)**, the one row the DB suite of 4.5 wrote between the two runs (the digest is identical inside each run: before = after). **Round trip: ok.**
### 4.8 `.env`
`rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env`: `ls: .env: No such file or directory` → `git status --porcelain` shows no `.env` (only the report). `cobalt_dev` now holds **0001–0009**.

### 4.5b The three reds fixed and re-proven (10:43–10:47)
Builder run FIX-DB (brief `scratch/fix-db-reds-brief.md`, report `s2-p4-verify-opus-FIXDB-2026-09-19.md`), tests only — hub read the diff: `test_p4_migrations.py` gains `_content(probe)` (drops ONLY `seconds`, by exclusion, so a key added to `_probe` later is still compared) and the three equalities compare content; `before` also asserts `rows > 0` and `digest` are present (no vacuous "unchanged"); the two `["digest"]` field-wise lines were already fine; `test_radar_cards_db.py::test_manual_cards_are_unaffected` asserts `len(result.transition_ids) == 2` (the same count, the accessor of R1-5's `FillResult`). No `src/`, no fixture, no assertion weakened. Commit `a59ee8c` `fix(s2-p4): two dev-DB reds are test-side …`. Second `.env` cycle: `cp` → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → **`2114 passed, 6 skipped, 1 xfailed, 15 warnings in 190.12s` — 0 failed** (the 3 are green on real Postgres) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (state after the suite: 26 tables, `missed`/`movers_daily`/`picks` present with 0 rows, `bars` 1,043,443, `cobalt_redactions` 125 — the suite's own rows) → `rm .env` → `ls -la .env`: `No such file or directory`; `git status --porcelain`: the FIX-DB files only, no `.env`.
`cobalt_dev` now holds: **0001–0009**.
### 4.9 Real-vault read-only tests (no `.env`)
`COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` → **`33 passed, 63 skipped, 1741 deselected in 0.74s`** (the 63 skips are `requires_db`/DB-env tests — no `.env` here by design). No vault write.
### 4.10 OWED from build2, done here: `cobalt validate` (third `.env` cycle, 10:48)
`COBALT_ENV=dev uv run cobalt validate` → exit 0: 13 trade_defs validated from the vault, **`Jobs (F17): 16 registered — 6 resident, 10 one-shot`, `registry <-> ops/: 16 label(s), exact match`, `registry <-> plists: schedules and COBALT_ENV agree on every job`** (so `com.cobalt.replay`'s registry row, plist and the smoke file load), `reads: 3 config path(s) re-read at runtime by 2 resident(s)` (`radar.yaml` → radar; `tunables.yaml` → aset, radar; `aset.yaml` → aset), `Placement: tree clean`. `.env` removed again and proven (`ls -la .env` no such file; `git status --porcelain` empty).

## §5 Close
| check | result |
|---|---|
| offline suite | `uv run pytest -q tests/cobalt tests/taxonomy` → **`1796 passed, 324 skipped, 1 xfailed, 15 warnings in 56.24s`**, 0 failed |
| leak test | `test_screen_filter_values_live_only_in_approved_radar_fixtures` → **`1 passed, 1836 deselected`** |
| RESTARTS | `uv run cobalt jobs restarts 919d562..HEAD` (P4's own range; `main..HEAD` now also lists 24 desk docs that main gained after this rebase as `D`, none is P4's): 0 UNCLASSIFIED. Non-docs, non-test rows: `configs/cobalt/jobs.yaml M → -` · `configs/cobalt/smoke/s2.yaml A → -` (operator command) · `configs/cobalt/taxonomy/tunables.yaml M → com.cobalt.aset,com.cobalt.radar` · `ops/com.cobalt.replay.plist A → new one-shot, bootstrap once: com.cobalt.replay` · `ops/README.md M → -` · `src/cobalt/**` (archiver, aset, cards, cli, dayopen, db_migrations, db_query, jobs, radar, replay, settings, smoke) → `com.cobalt.aset` and/or `com.cobalt.radar` (no other resident) · `0008`/`0009` `.sql` + rollbacks `→ -`. **`RESTARTS: com.cobalt.aset com.cobalt.radar`** (+ bootstrap of `com.cobalt.replay`). This run added no config-shape change: `radar.yaml` `not_equity` is UNCHANGED. Full per-path table is deterministic: `uv run cobalt jobs restarts 919d562..HEAD` |
| commits `919d562..HEAD` | 31 own (step 1.3) + 12 of this run: `07c6528` report step 1 · `27b1d36` test(fixtures) evidence mode · `1bc0dd9` docs(plan) · `05ece47` fix column source + fixture re-cut · `355580b` report step 2 · `089b069` feat(smoke) compare kind · `7b3846c` test(smoke) K3 SQL · `89532a2` report · `0443112` fix K9 short side · `869780a` report · `5a2e450` report step 4 · `a59ee8c` fix two dev-DB reds test-side — then the close commit(s) |
| main | rebased onto `919d562`; `git -C /Users/cobalt/cobalt log --oneline -1` now `a89105f` (desk docs-only commits: `git diff --stat 919d562 main` = 24 files, all under `docs/`) — still READY; the deploy hub rebases again |
| worktree | clean (only `scratch/`, untracked by the local exclude); every `git show --stat` of this run's commits listed no `scratch/` path; `.env` absent (last `ls -la .env` 10:49) |

## ESCALATE (16)
New this run:
1. **`Asset Type` rule — ASK DESK: which rule — non-blank `Asset Type`, `Industry = Exchange Traded Fund`, or either? [09:37]** Evidence (counts, L32): 16,307 files / 643,998 rows / 5,702 distinct fund tickers / 28 distinct non-blank values; every non-blank `Asset Type` sits on an `Industry = Exchange Traded Fund` row (table C (i) = 0), and **16 fund-industry rows (8 distinct tickers, in both whole-market movers exports) have a BLANK `Asset Type`** (table C (ii)). So today's `not_equity.values: [Exchange Traded Fund]` matches nothing because its header is `Asset Type`; "non-blank `Asset Type`" would still let 8 real funds into the benchmark; `Industry = Exchange Traded Fund` covers the whole sample (one-line change of `not_equity.header` — not built). Safe default taken: NOTHING of 2.4's rule built, `REQUIRED_HEADERS`/`unreported` branch untouched. When built later: config-shape change → RESTARTS `com.cobalt.radar` (L42), 0009 undeployed makes the `inputs_sha256` change (`not_equity_values` → the new rule marker in `gate_detail`) safe only before deploy.
2. **K3 HOLD ambiguity is CONFIRMED on Postgres (row 6)** — 4.6: K3's SQL runs; a HOLD-stamped pre-deploy row reads FAIL. Options unchanged from build2 ESCALATE 6 (accept the loud false positive + the `degraded_sources` procedure already in `expect_text` — recommended; or a row-level discriminator = schema change; or revert `b327221`'s RETAIN grading). Desk's decision; nothing built.
3. **The movers fixtures and `membership-day.real-shape.json` are now different sessions** (movers = 09-19 exports, membership = the 09-14 anchor day): 14 tests across `test_replay_movers.py`/`test_replay_runner.py`/evidence had their pinned tickers re-pointed at real rows that still carry each case (e.g. the "Tesla-class" mover is now a rank-2 gainer with no membership episode at all). Coherence would need a membership re-cut from 09-19 = a production DB read this seat cannot make. Desk's call.
4. **`"v": 152` is the same L3 duplicate** in `radar/throttle.py` and `radar/propose.py` beside `config.export.v`. Not fixed (out of the ruled scope).
5. **For the deploy prompt:** (a) the replay `job.result` shape gains `movers_by_side` (commit `0443112`); until the first `com.cobalt.replay` run after deploy, K9.2/K9.5 read FAIL and K9.3/K9.6 ERROR (RED) — a smoke run before that night is red by design; (b) `radar/throttle.py`'s probe now takes the column declaration from the radar config; (c) `com.cobalt.replay` bootstrap once; (d) restart list = aset + radar; (e) migration proof budget = production's own 104 s × 2 (dev's 12 s is not a prediction; 0008 + 0009 add no `bars` rows).
6. **`01-p4-build.md` step 2's builder launch shape cannot run as written** (`--allowedTools` is variadic: the trailing prompt is swallowed, `claude -p` exits 1 in 0 s — happened once here, nothing ran). Every builder in this run used the prompt-first shape with the same 17-rule list. Also the cutter's bare no-argument run (prompt 2.5) re-cuts every fixture and needs raw inputs that no longer all exist; the new `movers` mode was used instead.
7. **L53 record:** two Finviz `/export/screener` requests at ≈09:28–09:29 ET, Saturday, radar `idle:overnight` (the prompt's statement; archiver not running); status 200 both, no retry. 11,640 rows × 151 columns each. Nothing else touched Finviz.
8. **DevDocs gaps for the close:** no page yet for `tests/cobalt/test_p4_asset_type_evidence.py`, `test_replay_movers.py`, `test_smoke_k3_sql.py` (`docs/40 - DevDocs/tests/` has 40 other test pages). The cutter's page was written.
9. **Housekeeping:** main moved after the rebase by desk docs-only commits (`919d562` → `a89105f`), so `jobs restarts main..HEAD` shows desk docs as `D`; P4's range is `919d562..HEAD`. The two sibling cutters `_cut_p2_fixtures.py`/`_cut_panel_fixtures.py` mention the anchor-month string in comments, so the bare `grep -rlE '2026-09-'` over the fixture dirs lists them (no fixture data; both movers fixtures = 0 hits).
10. **`.env` cycles:** three (step 4 main run; FIX-DB re-proof; `cobalt validate`), each `cp` by name, never read, `rm` + `ls -la .env` proven after each; no builder ran while it was present.
Carried, untouched, one line each (build2 numbering): **C7** FY-3 changes `inputs_sha256` for card misses computed from now on — safe only while `"user".missed` is undeployed; `not_equity_values` was NOT changed here (item 1 not built), so only FY-3's half applies to the deploy prompt · **C9** E2's receipt reference is content-addressed `(membership_id, trade_def_md5)`, not a row id — the receipt-reference tribunal's question (L52 seam clause) · **C5** 0001's schema-wide grant ran before 0002 moved tables: `cobalt_user` has SELECT on none of `bars`, `cobalt_jobs`, `cobalt_kill_switch`, `cobalt_redactions`, `cobalt_email_sends`, `session_blocks` — migration-level design, ops item · **C10** tests that inject a module via `sys.modules` alone can pass for the wrong reason (`import a.b.c as x`) — sweep · **C11** replay window seam (formation/candidate horizons are session close) — design, values session · **C12** L53 gate scores declared windows, not wall-clock — design question.
(That is 10 new + 6 carried = 16.)

MEMORY: the Finviz `Asset Type` column is filled only for funds and even then not always — 16 of ~5,700 fund tickers in the 09-14…09-19 sample were blank while `Industry = Exchange Traded Fund` held for all; a stock never carries a non-blank value [stated 2026-09-19 · Code, evidence `s2-p4-verify-2026-09-19.md` §2.2].
MEMORY: builder launch shape — put the `claude -p` prompt BEFORE `--allowedTools` (variadic option swallows a trailing prompt) [stated 2026-09-19 · Code].
RULING (proposed, for the desk): none.

READY 4cc6859 (code tip a59ee8c; one docs-only commit fills this sha in) | rebased onto 919d562 (main now a89105f, docs-only since) | offline 1796/0 | db 2114/0 | cobalt_dev: 0001–0009 | rollback round-trip ok | Asset Type ESCALATED | rows 2 built · 4 built · 6 built (K3 SQL ran, HOLD ambiguity confirmed → ESCALATE 2) · 8 built | RESTARTS: com.cobalt.aset com.cobalt.radar (+ bootstrap com.cobalt.replay) | OWED: three-house check, receipt-reference tribunal, deploy prompt | ESCALATE: 16

## FUND RULE (R16)
Hub `p4-fund-rule-0919` (Sonnet 5), 11:15–11:18 ET, prompt `prompts/2026-09-19/24-p4-fund-rule.md`. **Headline: FAILED at step 3, before any build — nothing built, no file under `src/`/`configs/`/`tests/` touched, no builder launched, no `.env` copied, no Finviz request, no DB touched. The chunk as written cannot be built inside its own bounds; ESCALATE 17 names the fork.**

### Authorization + preflight
| rule | command | exit | result |
|---|---|---|---|
| launch-line strings vs `13-p4-verify.md` line 1 | compared by hand (`comm`/`sort` are not on this allowlist) | — | every string of this launch line appears byte for byte in 13's; none missing → no mismatch |
| `date*` | `date` | 0 | `Sat Sep 19 11:15:52 EDT 2026` |
| `git status*` | `git status --porcelain` / `git status` | 0 | empty; "nothing to commit, working tree clean"; no rebase |
| `git log*` | `git log --oneline -1` | 0 | `d5947e7` (as the prompt expected) |
| `git -C … status*` / `log*` | main checkout | 0 | ` M docs/40 - DevDocs/reports/seat-usage.md` (desk's, untouched) · tip `5ab083e` |
| `ls *` | `ls -la scratch/movers-{gainers,losers}-raw.csv` | 0 | 11,481,555 bytes each, Sep 19 09:29 — no new Finviz call needed |
| `claude -p *` | Opus meter probe | 0 | `OK` (L47 clear) |
| `uv run pytest *` | `--co -q tests/cobalt/test_replay_movers.py` | 0 | 27 collected |
Denials: none. Reads done: LAWS.md in full, INDEX, `## NOW`, `cto-2026-09-19.md` R16, this report §2.2–§2.5 + ESCALATE 1, `radar.yaml`, `radar/config.py`, `radar/runner.py:144`, `replay/movers.py`, `replay/models.py`, `replay/runner.py:310-360`, migration 0008.

### Facts checked (§1) — all match the prompt except one, the blocker
- `radar.yaml:22-24` and `NotEquityConfig` (`radar/config.py:81-84`) as described; `radar/runner.py:144` inline comparison as described; `movers.py:159` (`config.not_equity.header` picks the raw column), `movers.py:347` (the test), `movers.py:328` (`not_equity_values` param), `movers.py:385` (`not_equity_values` in the stored `inputs`); `REQUIRED_HEADERS` `movers.py:83`; `export.required_headers` has no `Industry`. Also a third reader the prompt did not name: `replay/runner.py:353` passes `deps.radar_config.not_equity.values` into `benchmark_misses`.
- `ls /Users/cobalt/cobalt/data/radar-cache/*/movers-*` → no match (empty). No retained production movers export exists, so the `unreported` label question of step 4 would come out as the prompt expects.
- **BLOCKER (step 3):** the prompt says `MoverRow` gains `industry` "read at parse time … the same way `asset_type` is read", and `benchmark_misses` then applies the one evaluator. But `benchmark_misses` does not run on `MoverRow`; it runs on `StoredMover`, which `MoversStore.active()` reads back from `system.movers_daily` (`movers.py:484-498`, `replay/runner.py:333-353`, `models.py:373-387`). `movers_daily` (migration 0008, lines 17-32) has `asset_type` but **no `industry` column**, and `grep -rn industry` over `src/cobalt/replay` and migrations 0008/0009 returns nothing. On every non-dry-run night the benchmark therefore cannot see `Industry`; adding a field to `MoverRow` alone changes nothing at the test site. Carrying it through the DB needs a NEW migration (0010 + rollback, `FORWARD`/`REVERSE` in `db_migrations/__init__.py`, `placement.py`, `MoversStore._COLUMNS`/`reconcile`, `StoredMover`) plus a dev-DB migrate/validate proof — outside this chunk's stated bound ("touches no migration and no DB-facing code", step 7; the diff-stat bound in step 9 lists no migration path) and outside this launch line, which deliberately dropped `COBALT_ENV=dev uv run cobalt db migrate*` and `… validate*` (13's had them). A half-build would leave the branch tip red (config `values` removed while `movers.py` still reads it), or leave R16 applied by radar but not by the benchmark (the L3 split the chunk exists to close). Stopped per "YOU CAN ALWAYS STOP".

### ESCALATE 17 — how does the benchmark see `Industry`? ASK DESK [11:17] — RESOLVED — option B built (retry `p4-fund-rule-0919b`, see `### Retry (0919b)` below)
The rule R16 "C", the config shape (`asset_type_header` / `industry_header` / `industry_values`), the evaluator, both `required_headers` lists, tests and the counts re-run stay exactly as `24-p4-fund-rule.md` specifies. Only step 3's movers half needs a ruling. Two ways, both keep ONE evaluator (L3):
- **B (recommended; offline-only, fits this launch line): no schema change.** `movers_step` (`replay/runner.py:311-353`) already holds the parsed `exports` in memory on both the live and the retained path. `parse_movers` evaluates the one function on the raw row and `MoverRow` carries `industry` plus the verdict; the step hands `benchmark_misses` the verdicts by `(side, ticker)` (replacing `not_equity_values`), and the receipt `inputs` records each mover's `asset_type`, `industry` and verdict plus a rule marker in place of `not_equity_values` (L57: the receipt is where the deciding inputs are stored; `reconcile`'s idempotence already keys on `export_sha256`, so a changed `Industry` cannot hide behind unchanged stored columns). `system.movers_daily` unchanged. Same permissions as this launch line; nothing new for the deploy.
- **A: persist it.** New migration 0010 adding `system.movers_daily.industry` (`ADD COLUMN IF NOT EXISTS`, idempotent; 0008 is applied on `cobalt_dev` so it must not be edited), `StoredMover`/`MoversStore` carry it, `benchmark_misses` takes `NotEquityConfig` as the prompt wrote. Truer to "the row carries its inputs", but it is a new write-path migration (L29), needs a launch line with 13's `db migrate*` / `validate*` / `.env` cp+rm rules, and adds a migration to P4's deploy plan and to its migration proof budget.
No file was changed for either. Relaunch shape: the same prompt with step 3's movers half replaced by the chosen option (B needs no new permission).

MEMORY: `benchmark_misses` reads `StoredMover` from `system.movers_daily`; any new per-mover input the benchmark must decide on has to be persisted (migration) or passed in memory from the parsed exports [stated 2026-09-19 · Code, `replay/runner.py:333`, migration 0008].
RULING (proposed, for the desk): none — the fork is ESCALATE 17.

Housekeeping: `.env` never copied this run (`ls -la .env` → `No such file or directory`, 11:17); `scratch/` not staged; no Finviz request (L53: none this run).

FAILED: step 3 — `benchmark_misses` reads `StoredMover` from `system.movers_daily`, which has no `industry` column; carrying `Industry` needs a migration (out of this chunk's bound and allowlist) or a redesign the desk has not ruled — ESCALATE 17, nothing built (superseded by the retry below)

### Retry (0919b)
Hub `p4-fund-rule-0919b` (Sonnet 5), 11:23–12:00 ET, prompt `prompts/2026-09-19/24b-p4-fund-rule.md` (step 3 = option B, decide at ingest; steps 1-2 and 4-9 = `24-p4-fund-rule.md`). Builders: two headless Opus 5 runs (`claude -p`, L50/L29): FR-1 (evidence count + fixture row), FR-2 (the rule). **Headline: BUILT. Two commits `db1c095` + `2738cdc`; offline 1818/0, db 2136/0; dropped 131,348, stock rows hit 0; RESTARTS `com.cobalt.aset com.cobalt.radar`; no migration, no Finviz request; ESCALATE 4 new (18-21).**

**Authorization + preflight.** Launch-line strings: `comm -23` of 24b's rule strings against 13's and 24's launch lines prints only 24b's own prompt line (the `Read '…24b…'` argument); every rule string is in both. Preflight, all allowed, 0 denials: `date` (11:23) · `git status --porcelain` empty · `git status` clean, no rebase · `git log --oneline -1` `e79d55d` · main checkout tip `4177649`, its own dirty file = the desk's `seat-usage.md`, untouched · `ls -la scratch/movers-{gainers,losers}-raw.csv` 11,481,555 bytes each · `claude -p` Opus probe `OK` (L47 clear) · `uv run pytest --co -q tests/cobalt/test_replay_movers.py` 27 collected. Read: LAWS.md in full, INDEX, `## NOW`, `UNATTENDED-LAUNCH.md`, `cto-2026-09-19.md` §4 R16, this report §2.2 / ESCALATE 1 / 17, both prompts. Facts re-checked: `ls /Users/cobalt/cobalt/data/radar-cache/*/movers-*` → no match (no retained movers export, so `unreported` keeps a row-level meaning only); `reconcile` keys each side by ticker, so a duplicate (side, ticker) could never have been stored — the new lookup makes it loud instead.

**What was built (old → new).**
| item | change |
|---|---|
| config shape | `not_equity: {header, values}` → `{asset_type_header, industry_header, industry_values}`; the old shape is refused by name (`extra="forbid"`, five keys named in the error); `load_config` requires both headers in `export.required_headers`; `radar.yaml` `required_headers` + `Industry`; comment block rewritten with the evidence counts |
| one evaluator | `is_not_equity(row, config)` in `cobalt.radar.config` = `asset_type non-blank OR industry in industry_values`; called by `radar/runner.py:144` and, through a two-key mapping (`mover_is_not_equity`), by the movers benchmark. `industry_values` is compared in exactly one place |
| movers at ingest | `MoverRow.industry`; `parse_movers` reads both headers unconditionally; `not_equity_verdicts(exports)` builds `(side, ticker) → export row` ONCE in `movers_step` before `export_counts`/`reconcile`/`archive_movers`, passed to `benchmark_misses` on the dry-run AND live paths; `benchmark_misses(…, not_equity=<config>, verdicts=<lookup>)`; a lead with no verdict → `ReplayInputError` naming ticker/side; a duplicate (side, ticker) → `ReplayInputError` |
| receipt (L57) | miss-row `inputs` carry per mover `asset_type` + `industry` (from the verdict, never from the stored row), `not_equity_rule: "asset_type_or_etf_industry"` and `not_equity_industry_values` (the one addition: the marker names the rule, the list is the value it compared — without it a receipt could not replay its own decision if the value changes); `not_equity_values` is gone; `gate_detail` gains `industry` |
| headers | movers `REQUIRED_HEADERS` = `Ticker, Change, Volume, Asset Type, Industry`; a missing column is a FAILED parse naming it; `unreported` kept, meaning a blank CELL on a present column |
| NOT touched | `system.movers_daily`, `StoredMover`, `MoversStore`, every migration, `db_migrations/*` |

**Grep proofs** (builder's, re-read by the hub against the diff): `grep -rn "not_equity" src/cobalt` → the radar call site (`radar/runner.py:144` `is_not_equity(row, self.config.not_equity)`), the movers call (`replay/movers.py:437` → `mover_is_not_equity` → `is_not_equity`), `replay/runner.py:332/365` (verdicts + call); `grep -rn "industry_values" src/cobalt` → exactly the config field (`radar/config.py:97`), the evaluator (`:117`), the receipt line (`replay/movers.py:486`); `grep -rn "not_equity_values" src` → no hit.

**Tests** (all red first; builder reports `scratch/fr1-builder-report.md`, `scratch/fr2-builder-report.md`, not committed). FR-1: 5 new (`test_p4_asset_type_evidence.py`; red = missing attribute / no blank-type fund row in the fixture) + 12 assertions restated for the 61-row fixtures (rank 61 = the appended real row per side; no assertion weakened). FR-2: 17 new + 7 rewritten in the named files — `test_radar_config.py` (6 new: shipped shape, old shape refused naming its keys, both headers required, comment block carries the evidence, evaluator strips/reads both, empty header/value list refused), `test_replay_movers.py` (9 new, 6 rewritten: the four rule cases, the C(i) arm counted and printed (0 of 142 real-shape rows), industry read on every row, either header missing is loud, verdict lookup + duplicate refused, a `StoredMover` list that LACKS `Industry` still drops the blank-type funds (the round-trip case the prior plan would have missed), a staled stored `asset_type` decides nothing, no-verdict is loud, receipt inputs), `test_replay_runner.py` (1 new: dry-run and live reach `benchmark_misses` with the same lookup), `test_radar_replay.py` (1 new, 1 rewritten: acceptance matrix; the radar call site on four verbatim real rows). `test_radar_pool.py::test_inactive_degraded_and_not_equity_precedence` checked, unchanged (never reads the radar config). Rule cases on REAL rows: non-blank type + fund industry; BLANK type + fund industry (the FR-1 rows); blank type + non-fund industry. Non-blank type + non-fund industry has no real row anywhere (0 of 142; 0 on the full sample) — tested as a boolean on a mapping, no row fabricated, and the count is asserted so a future non-zero fails a test.

**Counts (step 6, hub re-ran `uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence` after the fix commit):** `files: 16307 (without the columns: 0); rows: 643998 (without the columns: 0); distinct fund tickers: 5702; distinct Asset Type values: 28; table C total: 16; dropped under the new rule: 131348; stock rows hit: 0`. Table A rows 131,332, table B rows 131,348, table C(i) 0 → A ⊆ B, so A ∪ B = 131,348 = `dropped_under_new_rule`; the 16 extra rows are table C(ii) (8 tickers, blank `Asset Type`). Stock rows hit = 0.

**Suites (step 7).** Offline `uv run pytest -q tests/cobalt tests/taxonomy` → `1818 passed, 324 skipped, 1 xfailed, 15 warnings in 55.92s` (0 failed; 1796 before, +22 = the new tests). DB re-run (`.env` copied by name, never read; no builder was running) `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → `2136 passed, 6 skipped, 1 xfailed, 15 warnings in 188.89s` (0 failed; 2114 before). `.env` removed, `ls -la .env` → `No such file or directory`, `git status --porcelain` empty. `cobalt_dev` after: unchanged, `0001–0009` (no migration ran; the suite creates and drops its own test databases as before).

**RESTARTS (step 8).** `uv run cobalt jobs restarts e79d55d..HEAD` (this chunk): `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED (`configs/cobalt/radar.yaml` → resident reads radar; `radar/config.py`, `radar/runner.py`, `replay/models.py`, `replay/movers.py` → static import reach aset + radar; `replay/runner.py` → radar). `main..HEAD` (whole branch) gives the same line; it also lists `ops/com.cobalt.replay.plist` A → bootstrap once `com.cobalt.replay`, and desk docs as `D` because main moved (P4's range is `919d562..HEAD`).

**Commits.** `db1c095` `test(fixtures): dropped-under-new-rule count in the evidence mode` (6 files: cutter, evidence test, two movers fixtures +1 row each, two test files' 61-row bookkeeping) · `2738cdc` `fix(s2-p4): not-equity for movers decided at ingest from the parsed export, never through the movers_daily round trip (R16 "C", ESCALATE 17 option B)` (16 files, 684+/77−). ONE commit for the whole rule rather than two: the config shape, the evaluator and the movers half cannot be split without a red tip (the movers parse reads the renamed config keys). `git show --stat` of both lists no `scratch/` path. `git diff --stat` bound: the files 24 named plus `src/cobalt/replay/runner.py`; outside it: the four files of ESCALATE 18, plus `tests/cobalt/test_radar_config.py` and `tests/cobalt/test_replay_runner.py`, which 24 does not name but where the new config and runner tests naturally live. The stop line's sha is the code tip; one docs-only commit (this report) follows it.

**ESCALATE (4 new; 17 resolved above).**
18. **Four test files outside the bound edited (accepted by the hub, revert on request):** `test_radar_collector.py`, `test_radar_daily.py`, `test_radar_evaluate.py`, `test_radar_runner.py` — 7 tests whose synthetic screen rows set `Asset Type: "Stock"` (a value Finviz never returns; it leaves the cell blank) and no `Industry`; R16 makes both a red. Only fixture rows changed (blank type + `Industry: Capital Markets`), no assertion weakened, none deleted. The hub also restated two stale docstring sentences (`test_p4_asset_type_evidence.py`, `_cut_p4_fixtures.py`) that still called the rule unbuilt. 
19. **L32 — real tickers pinned in tests.** The rewritten/new movers and radar tests locate real fixture rows by ticker (31 occurrences of the three new pins in `test_replay_movers.py`, 7 in `test_radar_replay.py`, 3 in `test_replay_runner.py`), as the pre-existing tests already did (e.g. the "Tesla-class" mover, 14 quoted ticker literals before this chunk). The prompt says "counts only in anything committed"; this report and the commit messages are counts-only, the test files are not. Desk's call: accept the existing convention (the fixtures are real-shape rows with tickers in them anyway), or a follow-up that finds rows by predicate. Nothing changed for it.
20. **DevDocs stale for this chunk (sprint close):** pages exist for `radar/config`, `replay/models`, `replay/movers`, `replay/runner`, `tests/fixtures/replay/_cut_p4_fixtures` and are now behind (`is_not_equity`, `not_equity_verdicts`, `mover_is_not_equity`, the new config keys, the evidence count). Not regenerated here (CLAUDE.md: DevDocs are generated at sprint close). Still no page for `test_p4_asset_type_evidence.py`, `test_replay_movers.py`, `test_smoke_k3_sql.py` (ESCALATE 8).
21. **For the deploy prompt:** (a) `com.cobalt.radar` restarts (config-shape change + import reach), inside the 20:00–21:00 ET pause or overnight idle (L43); the one config file that changed shape = `configs/cobalt/radar.yaml`, and `com.cobalt.aset` also imports `radar.config`, so it restarts with it (L42, one action); (b) the live radar collector now requires `Industry` in every screen export (`required_headers`) — `export.columns: "0-150"` carries it (evidence: 151 columns, `Industry` present in all 16,307 files), so no config change is owed, but a screen whose columns are narrowed by the vault pool block would fail loud; (c) mover-miss `inputs_sha256` changes for rows computed from now on (`not_equity_values` gone; `not_equity_rule`, `not_equity_industry_values`, per-mover `industry` added) — safe only while `"user".missed` (0009) is undeployed, extends carried C7; (d) the live effect of R16: the radar now drops every non-blank-`Asset Type` row and every `Industry = Exchange Traded Fund` row, i.e. the 5,702-ticker class plus the 8 blank-typed funds; on the evidence sample it drops 0 stock rows.

MEMORY: the movers benchmark decides not-equity from the parsed export rows at ingest (`not_equity_verdicts`), never from `movers_daily` — that table stores no `industry`; any new per-mover input the benchmark must decide on is either passed in memory from `exports` (as here) or persisted by migration [stated 2026-09-19 · Code, `replay/movers.py`, `replay/runner.py`, migration 0008].
MEMORY: R16 "C" is built: not-equity = non-blank `Asset Type` OR `Industry = Exchange Traded Fund`, one `is_not_equity` in `cobalt.radar.config`; 0 stock rows hit on 643,998 rows / 16,307 files; the 8 blank-typed funds are caught by `Industry` [stated 2026-09-19 · Code, `s2-p4-verify-2026-09-19.md` §Retry (0919b)].
RULING (proposed, for the desk): ESCALATE 18 accept or revert the four out-of-bound test files (recommended: accept; each is a fixture value Finviz never returns); ESCALATE 19 accept the ticker-pinning convention.

P4 FUND RULE 2738cdc | offline 1818/0 | db 2136/0 | dropped 131348 kept n/a | stock rows hit 0 | RESTARTS: com.cobalt.aset com.cobalt.radar

---

# ROUND 2 (tribunal fold)

Seat `p4-round2-0919`, prompt `docs/40 - DevDocs/prompts/2026-09-19/32-p4-round2.md`, worktree `/Users/cobalt/cobalt-wt/s2-p4`, branch `sprint-2/p4`. OFFLINE BY DESIGN — `cobalt_dev` is held by `ops-0919-db` (bg `e810326c`) and was not connected to, migrated, rolled back or read at any point in this run.

**§0.** Both blockers closed: **A1 FIXED** (the formation reconcile now keys on P2 having run, so an empty rerun retires its predecessors — R2-1) and **A3 PINNED** with `radar/pool.py` untouched, its L52 disclosure still owed to Dejan. A2 pinned as-is (design gap escalated, no behaviour changed), A4's cutter transforms built (fixture NOT regenerated, nothing personal ever shipped), A5's merge-order test no longer the same case twice.
Offline **1829 passed / 0 failed** (baseline 1818/0, +11 named new tests, skips unchanged at 324). RESTARTS: `com.cobalt.radar`, 0 UNCLASSIFIED.
**OFFLINE BY DESIGN: `cobalt_dev` untouched — no DB test ran this round; the two edited `@requires_db` merge-order parametrizations owe their first run to P4's DB step.**
**ESCALATE: 5** — A2 (deadline scope), A3 (L52 ruling owed), R2-1 (line-number drift), **R2-2 (the desk's A3 pre-read names the wrong half of the sort key — the disclosure text needs correcting)**, R2-3 (no `requires_db` twin for A1, with reason). Carried untouched: A6, A7, A8, A9/A10.

## ROUND 2 AUTHORIZATION

| check | command | result |
|---|---|---|
| 16 allow strings ⊂ `13-p4-verify.md` | `grep -c -F '<string>' '…/13-p4-verify.md'` ×16 | every one printed `1` |
| 16 allow strings ⊂ `24b-p4-fund-rule.md` | `grep -c -F '<string>' '…/24b-p4-fund-rule.md'` ×16 | every one printed `1` |
| deny rules in both | `grep -c -F -- '--disallowedTools "AskUserQuestion" "EnterWorktree"' <both>` | `1` and `1` |
| 13 committed on main | `git -C /Users/cobalt/cobalt log --oneline -1 -- "…/13-p4-verify.md"` | `db2dcb1 docs(desk): 09-19 P4 prompt check round 2 — 0 launch blockers, 7 findings folded (text only); p4-verify launches` — MATCHES the prompt's `db2dcb1` |
| 24b committed on main | `git -C /Users/cobalt/cobalt log --oneline -1 -- "…/24b-p4-fund-rule.md"` | `4177649 docs(desk): 09-19 P4 fund-rule chunk 24b — step 3 redesigned (decide at ingest, no migration; rules = 24's)` — MATCHES the prompt's `4177649` |
| L67 work item recorded | `git -C /Users/cobalt/cobalt log --oneline -1 -- "…/reports/cto-2026-09-19.md"` | `238374f docs(desk): 09-19 P4 round-2 fix launched (763578e6); A1 + A3 block P4, A3 ruling owed (L52 disclosure)` (no equality required; recorded) |

The 16 strings are a strict subset of both approved lists; no string in this launch line is absent from either file. Authorization holds.

## ROUND 2 PREFLIGHT

| rule | command | exit | allowed/DENIED |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | allowed — `Sat Sep 19 12:22:19 EDT 2026` |
| `Bash(git status*)` | `git status --porcelain` | 0 | allowed — EMPTY |
| `Bash(git status*)` | `git status` | 0 | allowed — long form quoted below |
| `Bash(git log*)` | `git log --oneline -3` | 0 | allowed — tip `c40f03c`, code tip `2738cdc` beneath |
| `Bash(ls *)` | `ls -la .env` | 1 | allowed — `ls: .env: No such file or directory` (the required answer) |
| `Bash(ls *)` | `ls -la scratch` | 0 | allowed — 50 entries, recorded below, none added to git |
| `Bash(git -C /Users/cobalt/cobalt *log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | allowed — `238374f docs(desk): 09-19 P4 round-2 fix launched (763578e6); A1 + A3 block P4, A3 ruling owed (L52 disclosure)` |
| `Bash(git diff *)` | `git diff --stat main -- src/cobalt/radar/` | 0 | allowed — the standing-constraint capture, quoted below |
| `Bash(grep *)` | `grep -c -F …` ×34 (authorization) | 0 | allowed |
| `Bash(uv run pytest *)` | `uv run pytest --co -q tests/cobalt/test_replay_runner.py tests/cobalt/test_p4_migrations.py` | 0 | allowed — `48 tests collected in 0.10s` |
| `Bash(uv run pytest *)` | `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`) | 0 | allowed — BASELINE below |

NO DENIALS. Rules in the launch line not probed at preflight because their first real use is the probe: `Bash(git add *)`, `Bash(git commit *)`, `Bash(git show*)`, `Bash(uv run cobalt jobs restarts *)`, `Bash(cd *)`, `Bash(mkdir -p *)`, `Bash(wc *)`, `Bash(git -C /Users/cobalt/cobalt status*)` (UNATTENDED-LAUNCH §6: a rule with no harmless variant inside its own pattern is probed by its first real use).

**`git status`, LONG FORM, IN FULL:**

```
On branch sprint-2/p4
nothing to commit, working tree clean
```

Branch is `sprint-2/p4`. No "rebase in progress", no "You have unmerged paths".

**`git log --oneline -3`:**

```
c40f03c docs(report): s2-p4 fund rule R16 (retry 0919b) — built via option B, ESCALATE 17 resolved; offline 1818/0, db 2136/0, 0 stock rows hit; 4 new ESCALATE
2738cdc fix(s2-p4): not-equity for movers decided at ingest from the parsed export, never through the movers_daily round trip (R16 "C", ESCALATE 17 option B)
db1c095 test(fixtures): dropped-under-new-rule count in the evidence mode
```

Expected tip `c40f03c` on top with the code tip `2738cdc` beneath it — as the prompt predicted for a first launch.

**BASELINE (verbatim, `uv run pytest -q tests/cobalt tests/taxonomy`):**

```
1818 passed, 324 skipped, 1 xfailed, 15 warnings in 55.69s
```

`failed` = 0. `passed` = 1818, matching the R16 close exactly. Skips 324, xfail 1 — these are the numbers this round's close is compared against.

**STANDING CONSTRAINT, PREFLIGHT CAPTURE — `git diff --stat main -- src/cobalt/radar/`:**

```
 src/cobalt/radar/collector.py    |   4 +-
 src/cobalt/radar/config.py       |  91 ++++++++++++-
 src/cobalt/radar/evaluate_cli.py |  23 ++++
 src/cobalt/radar/models.py       |  13 +-
 src/cobalt/radar/notes.py        | 277 +++++++++++++++++++++++++++++++++++++--
 src/cobalt/radar/pool.py         |  63 +++++++--
 src/cobalt/radar/propose.py      |  42 ++++--
 src/cobalt/radar/runner.py       |   4 +-
 src/cobalt/radar/store.py        |  40 ++++--
 src/cobalt/radar/throttle.py     |  18 ++-
 10 files changed, 527 insertions(+), 48 deletions(-)
```

**`ls -la scratch` (recorded; NOTHING here is ever added to git — L32):** `.launch-marker`, `agy-out.md`, `asset-type-evidence.md`, `at0-brief.md`, `at1-brief.md`, `bars-day-raw.tsv`, `card-transitions-day-raw.tsv`, `chunk-A-stderr.log`, `chunk-A-stdout.log`, `e2-brief.md`, `e2-src.patch`, `f1-brief-v2.md`, `f1-brief.md`, `f1-builder-output-v2.txt`, `f1-builder-output.txt`, `fix-db-reds-brief.md`, `fr1-brief.md`, `fr1-builder-report.md`, `fr2-brief.md`, `fr2-builder-report.md`, `fx-brief.md`, `fy-brief.md`, `jobs-restarts.txt`, `LAWS.md`, `membership-day-raw.tsv`, `movers-gainers-raw.csv`, `movers-losers-raw.csv`, `p4-devdoc-sections.md`, `p4b-devdoc-append.py`, `pool-metrics-raw.csv`, `proof_rows.py`, `rebase-brief-1.md`, `rebase-brief-1b.md`, `rebase-brief-2.md`, `rebase-brief-3.md`, `rebase-brief-4.md`, `rebase-brief-v1.md`, `rebase-resolve-1.md`, `rebase-resolve-1b.md`, `rebase-resolve-2.md`, `rebase-resolve-3.md`, `rebase-resolve-4.md`, `rebase-resolve-v1.md`, `render_k9.py`, `review/`, `step0-sizings.json`, `tr-brief.md`, `tr2-brief.md`.

## ROUND 2 — 1. A1, the empty formation rerun (BLOCKER 1 of 2) — FIXED, commit `f07b483`

**`facts.md` row A1: CONFIRMED on the live tip**, with one line-number correction (see ESCALATE R2-1 below). The guard, quoted verbatim from `src/cobalt/replay/runner.py` **line 426** (the prompt's index card says `:425`; `facts.md` row A1 says "real file 425–430", which contains it):

```
        if rows and not dry_run:
            result.reconcile["formation"] = deps.missed.reconcile(
                run_id=result.replay_run_id, trade_date=trade_date, kind="formation", rows=rows)
            state["formation_rows"] = deps.missed.current(trade_date, "formation")
```

Confirmed against the siblings: `movers_step` reconciles at `runner.py:369` `if not dry_run:` and `cards_step` at `runner.py:398` `if not dry_run:` — neither carries a row-count condition. Only the formation kind does.

**The status spelling, read at the source before the condition was written** (the prompt's stop-and-escalate condition — it does NOT apply; there is exactly one spelling and it is a named constant):

| fact | file:line |
|---|---|
| `FORMATION_UNAVAILABLE = "unavailable"` — one named constant, one spelling | `src/cobalt/replay/models.py:27` |
| the ONLY producer of that status: P2 absent | `src/cobalt/replay/runner.py:208` `return FormationOutcome(status=FORMATION_UNAVAILABLE)` |
| the ONLY other producer of a status: the capability marker | `src/cobalt/replay/formations.py:343` `status=evaluator_version, rows=tuple(rows),` |
| and that marker is constrained to a known set, so "available" is a closed set of one today | `src/cobalt/replay/formations.py:81` `SUPPORTED_EVALUATORS = frozenset({"s2p2.1"})`, enforced `:145` |
| `grep -rn 'unavailable' src/cobalt/replay/` → no second spelling anywhere (the other hits are prose, the DRC line text, and `cards.py`'s unrelated `stop_at_card_level_exemption`) | — |

**THE FIX** (`runner.py:426`, the only behavioural line changed in this step):

```
        if outcome.status != FORMATION_UNAVAILABLE and not dry_run:
```

`result.formation_misses`, the `MISS formation …` print lines and everything else in the step are byte-identical.

**TESTS FIRST — RED, quoted:** `uv run pytest -q tests/cobalt/test_replay_runner.py -k "retires_the_days_predecessors or never_reconciles_the_formation_kind or suppresses_it_in_the_run"` on the tip `c40f03c` + the new tests, BEFORE the runner edit:

```
FAILED tests/cobalt/test_replay_runner.py::test_an_open_radar_card_for_the_formations_subject_suppresses_it_in_the_run - AssertionError: assert 'missed.reconcile:formation' in ['job_store.get', 'c...
FAILED tests/cobalt/test_replay_runner.py::test_an_available_p2_with_no_formations_still_retires_the_days_predecessors - AssertionError: assert 'missed.reconcile:formation' in ['job_store.get', 'c...
2 failed, 1 passed, 31 deselected in 2.91s
```

**GREEN, after the fix, same command:**

```
3 passed, 31 deselected in 2.81s
```

Whole replay surface after the fix — `uv run pytest -q tests/cobalt/test_replay_runner.py tests/cobalt/test_replay_formations.py tests/cobalt/test_replay_line.py tests/cobalt/test_replay_cards.py` → `93 passed, 5 skipped in 12.63s`.

**What each test is:**

| test | what it pins | before | after |
|---|---|---|---|
| `test_an_available_p2_with_no_formations_still_retires_the_days_predecessors` (NEW) | the R2-1 case the plan names: P2 present and compatible (`status == "s2p2.1"`), formation list EMPTY, `dry_run=False` → `missed.reconcile` IS called for `kind="formation"` with `rows == []`, `inserted == 0`, and a predecessor seeded into the fake's current set is gone afterwards | RED | GREEN |
| `test_an_absent_p2_never_reconciles_the_formation_kind` (NEW) | the legitimate skip: `status == "unavailable"`, no rows → `missed.reconcile:formation` NOT in calls | **GREEN before AND after** — stated plainly, as the prompt requires; it is a pin of the half of the guard that was always right | GREEN | GREEN |
| `test_an_open_radar_card_for_the_formations_subject_suppresses_it_in_the_run` (`:362`, ONE assertion changed) | an AVAILABLE P2 with everything suppressed still reconciles | RED | GREEN |

**The changed assertion, before → after** (every other assertion in that test is byte-identical — the counts tuple `(1, 0, 1)` and the `formations: 0 not taken (no_card) · cf-R Σ +0.0R, n=0 · suppressed 1` line both stand unchanged):

```
-    assert "missed.reconcile:formation" not in calls
+    # P2 RAN here (available, everything suppressed): an available run with
+    # zero surviving rows still reconciles, so the day's predecessors are
+    # retired (plan STEP-1 R2-1). Only `unavailable` skips the reconcile.
+    assert "missed.reconcile:formation" in calls
+    assert result.reconcile["formation"].inserted == 0
```

The old assertion was wrong, not the plan: that test drives the REAL P2 binding (`formations=runner_mod.formation_replay`, `formation_sources=p2_sources`), so P2 ran, produced one candidate, and suppressed it. Under `reconcile`'s own contract ("retire every predecessor this run") a run that produced no surviving row must still retire the predecessors of the run before it. No ESCALATE is raised against the plan here.

**The `requires_db` twin: NOT written, and why (L3, L45).** The prompt asks for one "if and only if the suite's existing pattern has one for the sibling kinds". It does not: the whole branch carries exactly ONE `requires_db` test of reconcile semantics, `tests/cobalt/test_p4_migrations.py:394 test_missed_rerun_reconciles_in_the_ruled_order_against_the_live_unique_index`, and it is written at the SQL level on `kind='mover'` only — there is no card twin and no per-kind pattern to follow. That test already pins the exact R2-1 retirement this fix makes reachable ("A subject whose miss disappears on rerun: step (1) only", `test_p4_migrations.py:431-447`), and it is kind-agnostic SQL: a formation copy would assert the same index and the same check constraint with one string changed. Writing one would be a second copy of a proven path. Nothing new is OWED to the DB step from A1.

## ROUND 2 — 2. A2, the deadline's actual scope — PINNED, no behaviour changed, commit `b136768`

**`facts.md` row A2: CONFIRMED** on the live tip. Quotes:

| the row's claim | what the code says |
|---|---|
| `check_deadline` runs only between steps and before the vault write | `runner.py:455` `check_deadline(name)` inside the `for name, step in zip(STEPS, …)` loop, and `runner.py:447` `check_deadline("line (before the vault write)")`. `grep -n 'check_deadline' src/cobalt/replay/runner.py` → definition `:292`, and call sites `:381` (per card candidate, inside `cards_step`), `:447`, `:455`. No other call site. |
| only `run_async` work is under `wait_for` | `runner.py:304-305` `async def bounded(): return await asyncio.wait_for(coro, timeout=remaining)` |
| `formation_source(...)` and `missed.reconcile(...)` are synchronous | `runner.py:410` `outcome = deps.formation_source(` and the three `deps.missed.reconcile(` calls at `:370` (mover), `:399` (card), `:434` (formation) — plain calls, none wrapped in `run_async`. The only `run_async` call sites are `:322`, `:323`, `:347`, all collector/archive work. |
| the module docstring states the design | `runner.py:26-31`, verbatim: "THE DEADLINE (R1-16). `timeout_s` bounds a stall, not a healthy long run. … a running step is cut at the deadline (async work under `wait_for`); the vault write is re-checked immediately before it lands, so no write is ever late." |

Line-number note: the pre-vault-write check is at `:447` **after this round's step-1 commit** (it was `:440` before — the prompt's number — the +7 lines being step 1's comment). No other drift.

**A test of the "crosses the deadline → raises rather than writes" shape ALREADY EXISTS** and was not duplicated (L3): `test_r1_16_a_deadline_passing_between_steps_stops_before_the_vault_write`, `tests/cobalt/test_replay_runner.py:142`. **The missing half, now added:** that test accepts `failed.value.step in {"formations", "line"}`, so the between-steps `check_deadline` satisfies it and the `:447` check is never proven to be the thing that refuses. `grep -rn 'before the vault write' tests/ src/` returned exactly ONE hit before this commit — `runner.py:447` — no test anywhere asserted that check by its own name.

**The two tests added:**

| test | what it pins |
|---|---|
| `test_r1_16_the_check_immediately_before_the_vault_write_is_what_refuses_a_late_run` | the deadline is crossed by SYNCHRONOUS work inside the line step (`deps.drc_path`), AFTER that step's between-steps check has passed, so only `:447` can refuse: asserts `step == "line"`, `DeadlineExceeded`, `"line (before the vault write)" in str(error)`, no `writer` call, and the DRC note byte-identical to before. This is spec R1-16's stated bar, asserted directly. |
| `test_r1_16_a_synchronous_step_is_not_cut_mid_flight_the_documented_limitation` | the LIMITATION, asserted as the current contract: `missed.reconcile` for `kind="mover"` pushes the clock past the deadline mid-call and still RUNS TO COMPLETION — its rows are current, `result.mover_misses` matches them — and the run stops at the NEXT `check_deadline`, one step later (`step == "cards"`). Its docstring names it as the documented limitation and points at the A2 ESCALATE line below. |

**RED/GREEN, stated honestly: both are GREEN on their first run, and there is no RED phase to quote, because A2 changes NO code** — the prompt rules the fix (bounding synchronous work) a design change and NOT this round's. `uv run pytest -q tests/cobalt/test_replay_runner.py -k "r1_16"` → `5 passed, 31 deselected in 0.47s` (3 pre-existing + 2 new). They are not vacuous: each discriminates by construction — the first fails if any other `check_deadline` fires (it asserts the exact message and `step == "line"`), the second fails if the synchronous call is ever cut (it asserts the reconcile's rows exist and the failure lands one step LATER).

NOT done, deliberately, per the prompt: no timeout was added around a DB commit; `run_async` is untouched.

## ROUND 2 — 3. A3, the ranking sort key — PINNED, `pool.py` NOT TOUCHED, commit `5652260`

**VERDICT: crash-removal only — the conclusion of `facts.md` row A3 and of the desk's pre-read is CONFIRMED. But ONE OF THE PRE-READ'S THREE CLAIMS IS FACTUALLY WRONG about which half of the key tuple this branch edited, and the code wins (ESCALATE R2-2 below).** The corrected argument reaches the same place: no input that previously ran ranks differently, and no pool membership changes.

**The three claims, each confirmed or contradicted with a quote:**

**Claim 1 — "BOTH `max(...)` calls in the key tuple carry the NEW `is not None` filter (`pool.py:189`, `pool.py:198`)": HALF WRONG.** Both carry the filter *today* (`pool.py:189` and `pool.py:198`, confirmed by reading the file) — but only `:189` is new. `git diff main -- src/cobalt/radar/pool.py` contains exactly ONE added filter line, inside element 1's generator:

```
                 key=lambda name: (
+                    # None-filtered like the value expression below: a name
+                    # with no value on one list and a value on another
+                    # used to raise TypeError here (found S2-P4 STEP-2).
                     max(
                         (
                             source.metrics.get(name, {}).get(session_metric)
                             for source in sources if source.kind == "list" and name in source.tickers
+                            and source.metrics.get(name, {}).get(session_metric) is not None
                         ),
                         default=None,
                     ) is None,
```

Element 2 (`-(max(…, default=0))`) appears nowhere in the diff — it is unchanged by this branch.

**Claim 2 — "Pre-edit, element 2 was `-(max(<unfiltered gen>, default=0))`; any generator containing a `None` raised `TypeError` THERE": CONTRADICTED.** `git show main:src/cobalt/radar/pool.py` shows element 2 ALREADY carrying the filter on main, and element 1 carrying none — the exact opposite assignment:

```
                 key=lambda name: (
                     max(
                         (
                             source.metrics.get(name, {}).get(session_metric)
                             for source in sources if source.kind == "list" and name in source.tickers
                         ),
                         default=None,
                     ) is None,
                     -(
                         max(
                             (
                                 source.metrics.get(name, {}).get(session_metric)
                                 for source in sources if source.kind == "list" and name in source.tickers
                                 and source.metrics.get(name, {}).get(session_metric) is not None
                             ),
                             default=0,
                         )
                     ),
                     name,
                 ),
```

So the pre-edit crash was raised by **element 1**, not element 2, and this branch fixed element 1.

**The conclusion survives the correction — the corrected proof, case by case.** Pre-edit element 1 is `max(<unfiltered>, default=None) is None`; post-edit it is `max(<filtered>, default=None) is None`. Enumerating every input:

| the name's values across the lists carrying it | pre-edit element 1 | post-edit element 1 | same? |
|---|---|---|---|
| no lists carry it (empty generator) | `default=None` → `True` | `default=None` → `True` | SAME |
| exactly one value, `None` | `max` of one item → `None` → `True` | filtered → empty → `True` | SAME |
| exactly one value, a number | that number → `False` | that number → `False` | SAME |
| ≥2 values, all numbers | max → `False` | max → `False` | SAME |
| ≥2 values, any `None` among them (incl. all-`None`) | **`TypeError`** — `>` between `NoneType` and a number, or between two `NoneType` | filtered → `True` or a number | the ONLY input whose behaviour changed, and pre-edit it did not run at all |

Element 2 and element 3 (`name`) are byte-identical on both sides. **Therefore every input that did not raise produces an IDENTICAL key tuple, hence identical order and identical ranks.** The edit converts a crash into a defined placement, exactly as the pre-read concluded.

**Claim 3 — "`union` is consumed at `pool.py:206` only as `position = union.index(ticker) + 1`, never the `candidates` set": CONFIRMED.** `grep -n 'union' src/cobalt/radar/pool.py` → exactly two hits, `:179` (the `sorted(...)` assignment) and `:206` (`position = union.index(ticker) + 1`). `position` is one component of the outer `key` tuple at `:209`; the candidate set is never filtered by it, so pool MEMBERSHIP cannot move either.

**What the pre-edit crash actually did (checked, not assumed):** `decide(...)` is called at `src/cobalt/radar/runner.py:186`, which is OUTSIDE every `try` in `cycle()` (the first `try:` is at `:191`). `grep -rn 'except TypeError' src/cobalt/radar/` → one hit, `evaluate.py:282`, unrelated. So the pre-edit `TypeError` propagated out of the scan cycle — a loud crash, never a caught-and-degraded alternate ranking. Nothing silently took a different path.

**THE PINNING TEST** (in the EXISTING module `tests/cobalt/test_radar_pool.py`, which already covers `_ranked` through the public `decide` — no new module, L3):

| test | what it pins | result |
|---|---|---|
| `test_list_union_orders_by_value_descending_then_name_when_every_value_is_present` | all four names carry an rvol: expectation built from the key tuple itself (value descending, ties by name), NOT from a golden blob — `{"DDD": 1, "BBB": 2, "CCC": 3, "AAA": 4}` for rvol 9.0 / 4.0 / 4.0 / 1.0, all ADMIT | GREEN first run |
| `test_a_name_with_no_value_on_any_list_sorts_after_every_name_that_has_one` | two valueless names sort after every valued one and are ordered by name between themselves (`{"BBB": 1, "AAA": 2, "YYY": 3, "ZZZ": 4}`), with their `rank_value` None | GREEN first run |

`uv run pytest -q tests/cobalt/test_radar_pool.py` → `14 passed in 0.77s` (12 before, +2).

**RED/GREEN, honestly: both are GREEN on their first run and there is no RED to quote** — exactly the case the prompt anticipates. There is no code change in this step to make them fail; they are regression pins of today's order, and forcing an artificial RED would have meant changing `pool.py`, which this step forbids. Their value is that the predicted rank maps were written from the key tuple BEFORE running them and matched exactly — which is itself the confirmation that the key tuple reads as claimed.

**Deliberately NOT asserted:** neither test uses the one input whose behaviour the filter changed (a name with a value on one list and `None` on another). Pinning that case would settle Dejan's L52 ruling from inside the suite — and both tests as written pass under the filtered AND the unfiltered key, so a revert does not silently break them. The test block's comment says so in the file.

**`src/cobalt/radar/pool.py` was not edited, not reverted, not re-worded, not commented.** Proven at the close below.

## ROUND 2 — 4. A4, the cutter's two missing transforms — FIXED, fixture NOT regenerated, commit `39d76af`

**RE-VERIFIED ON THE CURRENT TIP — the hub's read holds exactly, and there is NO new finding:**

| check | command | result |
|---|---|---|
| owner id in the committed fixture | `grep -c user_id tests/fixtures/replay/cards-day.real-shape.json` | **`0`** |
| any free-text spelling | `grep -c -i -E 'reason\|note\|comment\|memo\|thesis\|rationale' <same file>` | **`0`** |
| size | `wc -c <same file>` | **`7544`** — the hub's 7,544 B to the byte |
| keys, read from the file | sizings `created_at, direction, entry, id, state, stop, ticker`; transitions `at, card_id, from_state, to_state` | matches the hub's lists exactly |

**Nothing personal shipped. `facts.md` row A4's "BLOCKER framing NOT REAL" is CONFIRMED, and the residual MINOR is CONFIRMED too.** Spec-01 §3, quoted verbatim from `docs/40 - DevDocs/prompts/2026-09-19/21-packet/spec-01-plan-2026-09-15.md:249`:

> `| tests/fixtures/replay/cards-day.real-shape.json | aset_sizings + card_transitions for the STEP-0 day (unfilled crossed, unfilled uncrossed, filled, passed, ≥2 filled overlapping for rule_10) | user_id → 1; all timestamps shifted to a fixed synthetic day with intraday offsets kept; free-text reasons → "<reason>" |`

Of those three transforms, only the date shift was implemented: `_anonymize` (`tests/fixtures/replay/_cut_p4_fixtures.py:154-155` before this commit) is `_HEX_SUFFIX_RE.sub("@000000000000", _shift_dates(text))` and nothing else. Both missing transforms are key-level and cannot be done by either text pass.

**THE FIX** — `strip_personal(rows)` added to the cutter and wired into `cut_cards_day` for BOTH arrays (`strip_personal(day_cards)` and `strip_personal(_tsv_rows(trans_text))`), inside the existing `_anonymize(json.dumps(...))` sandwich so the hex scrub and the date shift are byte-identical. `user_id`/`trader_id` → `1`; `reason`/`note`/`comment`/`rationale`/`thesis` → the literal `"<reason>"`; a NULL free-text cell stays NULL (a missing note is not free text, and its nullability is part of the real shape); input rows are copied, never mutated. The docstring narrative Astra flagged is rewritten to say that all three §3 transforms are now implemented, that the raw reads select neither column so the committed cut is unchanged, and that the tickers and card ids ARE the commissioned fixture (L45 real-shape), not an anonymization gap.

**TESTS FIRST — RED, quoted** (`uv run pytest -q tests/cobalt/test_p4_asset_type_evidence.py -k "owner_id or free_text or neither_column"`, before the cutter change):

```
FAILED tests/cobalt/test_p4_asset_type_evidence.py::test_the_committed_cards_fixture_carries_no_owner_id_and_no_free_text - AttributeError: module '_cut_p4_fixtures' has no attribute 'PERSONAL_ID_FIE...
FAILED tests/cobalt/test_p4_asset_type_evidence.py::test_the_cutter_flattens_the_owner_id_and_replaces_a_free_text_reason - AttributeError: module '_cut_p4_fixtures' has no attribute 'strip_personal'
FAILED tests/cobalt/test_p4_asset_type_evidence.py::test_a_row_carrying_neither_column_is_returned_unchanged - AttributeError: module '_cut_p4_fixtures' has no attribute 'strip_personal'
3 failed, 19 deselected in 0.08s
```

**GREEN after:** `uv run pytest -q tests/cobalt/test_p4_asset_type_evidence.py` → `22 passed in 0.09s` (19 before, +3).

**Honesty about the first test's RED**, since the prompt asks for it either way: the regression pin's FIXTURE assertions (the key sets, the two arrays) were GREEN from the start — the fixture is clean, exactly as the hub found. It went red only on its last two lines, which assert the fixture's keys do not intersect the cutter's declared personal-field sets, and those sets did not exist yet. **That is the stated purpose of that test: it is a regression pin, not a bug report.** It now fails the day a re-cut lets either class of column in.

**The cutter was NOT RE-RUN and the committed fixture was NOT REGENERATED.** The cutter reads `scratch/` raw exports and its run rule is not in this session's allowlist; `git diff --stat -- tests/fixtures/replay/` shows one file changed, `_cut_p4_fixtures.py` (+48/−2) — `cards-day.real-shape.json` is untouched, still 7,544 B as committed.

**Why these tests live in `test_p4_asset_type_evidence.py` (L3):** it is the one module that already loads the cutter by path (`_load_cutter`, `:69-81`). A new module would have meant a second loader for the same script.

## ROUND 2 — 5. A5, the vacuous "both merge orders" test — FIXED offline, DB run OWED, commit `9045260`

**`facts.md` row A5: CONFIRMED, both halves.**

| the row's claim | proof on this tip |
|---|---|
| `FORWARD` contains P2's 0006/0007, so the docstring's premise is false | `src/cobalt/db_migrations/__init__.py:56-57` — `MIGRATIONS_DIR / "0006_radar_score.sql",` and `MIGRATIONS_DIR / "0007_radar_cards.sql",` sit inside `FORWARD = (…)` between 0005 and 0008 |
| both parametrizations applied the same base | `test_p4_migrations.py:301` (pre-fix) `base = [p for p in FORWARD if _migration_version(p) < 8]`, outside the `if order == …`, so `p4_before_p2` already had 0006/0007 and `_simulate_p2_card_columns`'s `ADD COLUMN IF NOT EXISTS` was a no-op in it |
| the module docstring was stale | pre-fix `:9-10`: "P2's 0006/0007 are not in this tree (P2 not merged — hub report STEP-0)" |
| `_seed_membership` ran only AFTER the first apply, so the digest equality was not about pre-existing rows | pre-fix `:306` seeds after `:305` `_apply(conn, [FWD_0008, FWD_0009])` — and it could not have run earlier: its INSERT named `rank_metric,rank_value`, the two columns **0008 itself adds** |

That last point is why the seed could not simply be moved: moving the old `_seed_membership` above the first apply would have failed on columns that do not exist yet. The fix had to make a no-value seed possible.

**THE FIX — three parts:**

1. **`_merge_order_bases(order)`** returns `(applied before 0008/0009, applied after them)`. `p2_before_p4` → `(everything below 0008, [])`. `p4_before_p2` → `(everything below 0008 EXCEPT 0006/0007, [0006, 0007])`. The `@requires_db` test now uses it, and in the `p4_before_p2` half applies `p2_after` — P2's OWN migrations, not just the simulated columns — in the P2 position, before `_simulate_p2_card_columns` and the re-apply.
2. **`_membership_insert(ticker, scan_id, value)`** builds the seed statement; `value is None` names NEITHER new column, so a row can be seeded BEFORE 0008 runs. The `@requires_db` test now seeds `P4PRE` (no value) before the first apply, asserts it survives with `(rank_metric, rank_value) == (None, None)`, and only then seeds `P4A` with its non-null post-deploy value. **Every pre-existing assertion is kept** — the two `_content(...) == before` equalities, the three digest equalities, the two `to_regclass` NULL checks, the repeated-reverse no-op and the final re-apply all stand unchanged; nothing was deleted or weakened.
3. **The module docstring is rewritten** to say what is true on the rebased tree, and it records the stale claim and why it mattered rather than quietly dropping it.

**TESTS FIRST — the RED I CAN prove offline, quoted** (the helpers temporarily renamed so the new tests ran against a tree without them, then renamed back — the two new tests are the only thing that moved):

```
FAILED tests/cobalt/test_p4_migrations.py::test_the_two_merge_orders_no_longer_build_the_same_base - NameError: name '_merge_order_bases' is not defined
FAILED tests/cobalt/test_p4_migrations.py::test_a_row_can_be_seeded_before_0008_adds_its_columns - NameError: name '_membership_insert' is not defined
2 failed, 16 deselected in 0.07s
```

**GREEN after:** `uv run pytest -q tests/cobalt/test_p4_migrations.py` → `14 passed, 4 skipped in 0.04s` (12 passed before, +2; the 4 skips are the 3 `@requires_db` tests, one of them parametrized ×2).

| new offline test | what it proves without a database |
|---|---|
| `test_the_two_merge_orders_no_longer_build_the_same_base` | `FORWARD` really contains 0006/0007 (the docstring's premise is false); the two base lists are NOT equal; `p2_before_p4`'s base contains P2's versions and holds nothing back; `p4_before_p2`'s base contains NEITHER and holds back exactly `{6, 7}`; neither order's lists ever include 0008/0009 |
| `test_a_row_can_be_seeded_before_0008_adds_its_columns` | the no-value seed names neither `rank_metric` nor `rank_value` and carries exactly `(ticker, scan_id, scan_id)`; the valued seed names both and appends the value |

**THE OFFLINE BOUNDARY, STATED PLAINLY: `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership` was EDITED BUT NOT RUN.** It is `@requires_db` and `cobalt_dev` is held by `ops-0919-db` (bg `e810326c`) for the whole of this run. Its verification — both parametrizations — is OWED to P4's later DB step and is named again in the ESCALATE section and in the stop line. What this round proves about it is what the registry and the statement builder can show; the SQL behaviour is unproven until that run.

## ROUND 2 — 6. CLOSE (OFFLINE)

**Suite — `uv run pytest -q tests/cobalt tests/taxonomy`, verbatim:**

```
1829 passed, 324 skipped, 1 xfailed, 15 warnings in 56.11s
```

Against the PREFLIGHT BASELINE `1818 passed, 324 skipped, 1 xfailed, 15 warnings in 55.69s`:

| | baseline | close | movement |
|---|---|---|---|
| failed | 0 | **0** | none — the bar this round had to hold |
| passed | 1818 | 1829 | **+11**, and every one is named below |
| skipped | 324 | **324** | **UNCHANGED** — this round added no `requires_db` test (ESCALATE R2-3) |
| xfailed | 1 | 1 | none |

**The +11, by step, by name:**

| step | test | file |
|---|---|---|
| 1 (A1) | `test_an_available_p2_with_no_formations_still_retires_the_days_predecessors` | `tests/cobalt/test_replay_runner.py` |
| 1 (A1) | `test_an_absent_p2_never_reconciles_the_formation_kind` | same |
| 2 (A2) | `test_r1_16_the_check_immediately_before_the_vault_write_is_what_refuses_a_late_run` | same |
| 2 (A2) | `test_r1_16_a_synchronous_step_is_not_cut_mid_flight_the_documented_limitation` | same |
| 3 (A3) | `test_list_union_orders_by_value_descending_then_name_when_every_value_is_present` | `tests/cobalt/test_radar_pool.py` |
| 3 (A3) | `test_a_name_with_no_value_on_any_list_sorts_after_every_name_that_has_one` | same |
| 4 (A4) | `test_the_committed_cards_fixture_carries_no_owner_id_and_no_free_text` | `tests/cobalt/test_p4_asset_type_evidence.py` |
| 4 (A4) | `test_the_cutter_flattens_the_owner_id_and_replaces_a_free_text_reason` | same |
| 4 (A4) | `test_a_row_carrying_neither_column_is_returned_unchanged` | same |
| 5 (A5) | `test_the_two_merge_orders_no_longer_build_the_same_base` | `tests/cobalt/test_p4_migrations.py` |
| 5 (A5) | `test_a_row_can_be_seeded_before_0008_adds_its_columns` | same |

11 named, 11 in the count. One existing test (`test_an_open_radar_card_for_the_formations_subject_suppresses_it_in_the_run`) had one assertion changed and still passes, so it moves no number. No other movement to escalate.

**RESTARTS — `uv run cobalt jobs restarts 2738cdc..HEAD`, VERBATIM:**

```
path	change	rule	restart
docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md	M	DOCS	-
src/cobalt/replay/runner.py	M	static import reach	com.cobalt.radar
tests/cobalt/test_p4_asset_type_evidence.py	M	test/documentation; no resident	-
tests/cobalt/test_p4_migrations.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_pool.py	M	test/documentation; no resident	-
tests/cobalt/test_replay_runner.py	M	test/documentation; no resident	-
tests/fixtures/replay/_cut_p4_fixtures.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.radar
```

**0 UNCLASSIFIED.** Every changed path is one this prompt named — `src/cobalt/replay/`, `tests/cobalt/`, `tests/fixtures/replay/`, `docs/40 - DevDocs/`. No `scratch/`, no `data/`, no `configs/`. The one restart is `com.cobalt.radar`, derived by static import reach from the single source file this round touched (`replay/runner.py`); it is already in the branch's standing `RESTARTS:` line from the R16 close (`com.cobalt.aset com.cobalt.radar`), so this round adds no new resident to the deploy.

**STANDING CONSTRAINT — `git diff --stat main -- src/cobalt/radar/` at CLOSE:**

```
 src/cobalt/radar/collector.py    |   4 +-
 src/cobalt/radar/config.py       |  91 ++++++++++++-
 src/cobalt/radar/evaluate_cli.py |  23 ++++
 src/cobalt/radar/models.py       |  13 +-
 src/cobalt/radar/notes.py        | 277 +++++++++++++++++++++++++++++++++++++--
 src/cobalt/radar/pool.py         |  63 +++++++--
 src/cobalt/radar/propose.py      |  42 ++++--
 src/cobalt/radar/runner.py       |   4 +-
 src/cobalt/radar/store.py        |  40 ++++--
 src/cobalt/radar/throttle.py     |  18 ++-
 10 files changed, 527 insertions(+), 48 deletions(-)
```

**BYTE-IDENTICAL to the PREFLIGHT capture above** — same ten files, same per-file counts, same `527 insertions(+), 48 deletions(-)`. Not one line was added to `src/cobalt/radar/`.

**`src/cobalt/radar/pool.py` appears ZERO times in this round's own diff** — `git diff --stat 2738cdc HEAD`:

```
 .../reports/s2-p4-verify-2026-09-19.md             |  43 ++++++-
 src/cobalt/replay/runner.py                        |   9 +-
 tests/cobalt/test_p4_asset_type_evidence.py        |  66 ++++++++++
 tests/cobalt/test_p4_migrations.py                 | 134 +++++++++++++++++---
 tests/cobalt/test_radar_pool.py                    |  54 +++++++++
 tests/cobalt/test_replay_runner.py                 | 135 ++++++++++++++++++++-
 tests/fixtures/replay/_cut_p4_fixtures.py          |  50 +++++++-
 7 files changed, 470 insertions(+), 21 deletions(-)
```

Seven paths. `src/cobalt/radar/pool.py` is not among them; the only `radar` name is `tests/cobalt/test_radar_pool.py`, a test file. `git diff --stat 2738cdc HEAD -- src tests docs` returns the identical seven, so nothing changed outside those three trees either.

**Tree — `git status --porcelain`:** empty after the report commit below. **`ls -la .env`** → `ls: .env: No such file or directory` (quoted because the file is gitignored, so `git status` could never prove its absence).

**Commits, this round, one per item:**

| sha | item |
|---|---|
| `f07b483` | A1 — the formation reconcile keyed on P2 availability (fix + 2 new tests + 1 assertion updated) |
| `b136768` | A2 — the deadline's real scope pinned (2 new tests, no behaviour change) |
| `5652260` | A3 — the pool union ordering pinned (2 new tests, `pool.py` untouched) |
| `39d76af` | A4 — the cutter's two spec §3 transforms + the fixture pinned clean (3 new tests) |
| `9045260` | A5 — the merge-order test de-vacuumed + stale docstring (2 new offline tests, `@requires_db` test edited, not run) |

`git show --stat HEAD` was read after every one; no commit carries a `scratch/` path or any path this prompt did not name.

## ROUND 2 ESCALATE

**A2 (deadline scope):** the execution deadline bounds only async work and the pre-vault-write check; formation_source() and missed.reconcile() run synchronously past it. Spec R1-16's stated bar is met (no late vault write, margin held) and runner.py:26-31 documents the design. Bounding synchronous DB work is a design change — NOT made this round. Desk/Dejan to rule before or after deploy.

**A3 (L52):** the union sort key's None-filter in radar/pool.py:189,198 was shipped undisclosed in S2-P4 STEP-2. This chunk did not touch it. Effect per this round's read: crash-removal only. Dejan's ruling owed — waive-and-disclose or revert.

**R2-1 (line number, MINOR).** The prompt's index card places the A1 guard at `runner.py:425`; on the tip it was at **`:426`**. `facts.md` row A1 says "real file 425–430", which contains it. Recorded, not silently reconciled. (The same step's fix then moved the pre-vault-write `check_deadline` from `:440` to `:447`, which is why step 2 quotes `:447` where the prompt says `:440`.)

**R2-2 (the desk's A3 pre-read is WRONG about which element this branch edited — the conclusion survives, the reasoning does not).** The pre-read states: "Pre-edit, element 2 was `-(max(<unfiltered gen>, default=0))`. Any generator containing a `None` raised `TypeError` there". `git show main:src/cobalt/radar/pool.py` shows the OPPOSITE: on main, **element 2 already carried the `is not None` filter and element 1 did not**, and `git diff main -- src/cobalt/radar/pool.py` contains exactly ONE added filter line, inside **element 1**'s generator (`pool.py:189`). So `pool.py:198` is not a new filter at all — it is P2-era code this branch never touched, and the "new filter" is one line, not two. The verdict is unchanged (every input that did not raise produces an identical key tuple — the case table in §3 above proves it for element 1), but anyone re-deriving the L52 disclosure from the pre-read's wording would be reasoning from a false premise. **The disclosure Dejan is asked to rule on should say: one added filter, in element 1, at `pool.py:189`.**

**R2-3 (A1's `requires_db` twin: NOT written, deliberately).** The branch has no per-kind `requires_db` reconcile pattern to follow — one SQL-level test exists (`test_p4_migrations.py::test_missed_rerun_reconciles_in_the_ruled_order_against_the_live_unique_index`, `kind='mover'`) and it already pins the R2-1 retirement the A1 fix makes reachable. A formation copy would be a second copy of a proven path (L3). If the desk wants one anyway, it is a rename of that test's `kind` literal.

**OWED: the first run of every `requires_db` test this branch carries — nothing in this round ran against a database.** `cobalt_dev` is held by `ops-0919-db` (bg `e810326c`) and was not connected to, migrated, rolled back or read. By name, the ones this round TOUCHED and therefore owes first:

| test | file | why it is owed |
|---|---|---|
| `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]` | `tests/cobalt/test_p4_migrations.py` | **EDITED this round** (A5): new base list, new pre-existing seed, new assertion |
| `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p4_before_p2]` | same | **EDITED this round**, and this is the half that was vacuous — its first genuine run has never happened |

This round added NO new `requires_db` test (see R2-3), so the branch's skip count is unchanged. The branch's other `requires_db` tests were untouched here and carry whatever standing the R16 close gave them (that close ran them: `db 2136/0`) — but the two rows above have NOT been run in their current form by anyone.

**The cards fixture was NOT regenerated.** `tests/fixtures/replay/cards-day.real-shape.json` is byte-for-byte as committed (7,544 B). Only the cutter script changed. Re-cutting needs `scratch/` raw exports and a run rule this session does not have.

**Carried, not fixed this round (from `facts.md`'s own list — nothing dropped silently):**

| row | state |
|---|---|
| **A6** mixed-session fixtures can false-pass F13 | REAL, owned by build-report-2's ESCALATE 3 / ESCALATE 13. Re-cutting membership needs a production DB read this offline run may not do. Untouched. |
| **A7** K3 reads a permitted frozen HOLD as red | REAL, already ESCALATE 2. The desk owes a ruling before the deploy smoke. Untouched. |
| **A8** build-report-2 wording ("16 fund tickers blank" vs 8 distinct tickers, `build-report-2:162` vs `:149`) | REAL, MINOR. The desk's to fix before the memory fold. Untouched. |
| **A9 / A10** | carried by reference / UNVERIFIABLE FROM READS. Untouched. |

RULING OWED (one line each, for the desk to put to Dejan): **(1)** A3 — waive-and-disclose or revert the `pool.py:189` filter (L52), with R2-2's correction in the disclosure; **(2)** A2 — whether bounding synchronous replay/DB work inside the deadline becomes a design item, before or after this deploy.

MEMORY: a reconcile that retires predecessors is keyed on whether the producing step RAN, never on how many rows it produced — a row-count guard silently makes retirement conditional on there being something to insert (S2-P4 `replay/runner.py` formation step, tribunal A1) [stated 2026-09-19 · Code].
MEMORY: on a rebased branch, a test parametrized over "both merge orders" is vacuous unless its base list actually holds the other branch's migrations back — `FORWARD` already contains them, so `< 8` filters produce one case twice (S2-P4 `test_p4_migrations.py`, tribunal A5) [stated 2026-09-19 · Code].

Stop-line sha convention, as at the R16 close on this branch: it names the CODE TIP (`9045260`, step 5), and the docs-only report commits follow it (`1a93416`, then this line's own).

P4 ROUND2 9045260 | offline 1829/0 | A1 A2 A4 A5: FIXED (A2 PINNED — design gap escalated, no behaviour changed) | A3: PINNED, pool.py untouched — L52 ruling owed | RESTARTS: com.cobalt.radar | OWED requires_db: 2 | ESCALATE: 5 | cobalt_dev: untouched

---

# ROUND 2 DB RUN

Seat `p4-db-0919` (Opus 5), worktree `/Users/cobalt/cobalt-wt/s2-p4`, branch `sprint-2/p4`, prompt
`docs/40 - DevDocs/prompts/2026-09-19/37-p4-db-run.md`. Started 12:52 ET. THIS RUN TOUCHES `cobalt_dev`.

**§0 Headline.**
- **DB DEBT CLOSED.** Both owed `requires_db` parametrizations RAN for the first time on real `cobalt_dev` and
  **PASSED**; the captured SQL proves the two merge orders are finally different cases (A5's point).
- `db 2147/0` vs the 2136/0 baseline — **+11, reconciling exactly** with round 2's 11 offline tests; offline
  re-run holds at **1829/0**. `cobalt_dev` 0001–0009, unchanged. `.env` removed and proven gone, twice.
- Nothing built, no code touched (verification run). PREFLIGHT clean, 0 denials; 18/18 rule strings found in
  `13-p4-verify.md`, no new rule. RESTARTS `com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED.
- **ESCALATE: 4 carried open (A2, R2-1, R2-2, R2-3) + 0 new**; A3 RULED 12:34 ET (R23, verified in the record).

## ROUND 2 DB RUN PREFLIGHT

| rule / probe | command | exit | allowed/DENIED |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | allowed — `Sat Sep 19 12:52:11 EDT 2026` |
| `Bash(git status*)` | `git status --porcelain` | 0 | allowed — **EMPTY** |
| `Bash(git status*)` | `git status` (long) | 0 | allowed — quoted below |
| `Bash(git log*)` | `git log --oneline -1` | 0 | allowed — `21bcd5f` (FIRST LAUNCH; the tip the prompt expects) |
| `Bash(git -C /Users/cobalt/cobalt *log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | allowed — `cae2468 docs(desk): 09-19 archiver DB run in (3 red, being classified); ops round 3 clean; P4 folds check + P4 DB run launched; S2 smoke blocked on dev data` (recorded, no equality required) |
| `Bash(ls *)` | `ls -la .env` | 1 | allowed — `ls: /Users/cobalt/cobalt-wt/s2-p4/.env: No such file or directory` — the required answer |
| `Bash(ls *)` | `ls -la /Users/cobalt/cobalt-wt/archiver-append/.env` | 1 | allowed — `ls: /Users/cobalt/cobalt-wt/archiver-append/.env: No such file or directory` — `archiver-db-0919` does not hold the `cobalt_dev` lane |
| `Bash(grep *)` | `grep -c -F '<string>' '…/13-p4-verify.md'` ×18 + the deny-rule string | 0 | allowed — every one printed `1` (AUTHORIZATION below) |
| `Bash(grep *)` | `grep -rln "cobalt_dev" '…/reports/'` + `grep -n cobalt_dev cto-2026-09-19.md` | 0 | allowed — lane check below |
| `Bash(cd *)` | `cd /Users/cobalt/cobalt-wt/s2-p4` | 0 | allowed |
| `Bash(uv run pytest *)` | `uv run pytest --co -q tests/cobalt/test_p4_migrations.py` | 0 | allowed — **18 tests collected in 0.01s**, clean collection |

**NO DENIALS.** Rules not probed at preflight because their first real use is the probe (UNATTENDED-LAUNCH §6):
`Bash(cp …/.env …)`, `Bash(rm …/.env)`, `Bash(COBALT_ENV=dev uv run cobalt db migrate*)`,
`Bash(COBALT_ENV=dev uv run pytest *)`, `Bash(git add *)`, `Bash(git commit *)`, `Bash(git diff *)`,
`Bash(git show*)`, `Bash(uv run cobalt jobs restarts *)`, `Bash(git -C /Users/cobalt/cobalt status*)`.

**`git status`, LONG FORM, IN FULL — first two lines are the whole output:**

```
On branch sprint-2/p4
nothing to commit, working tree clean
```

Branch is `sprint-2/p4`. No "rebase in progress", no "You have unmerged paths".

**Collection — both owed parametrizations present and `requires_db`-marked** (`test_p4_migrations.py:381-383`
carries `@requires_db` then `@pytest.mark.parametrize("order", ["p2_before_p4", "p4_before_p2"])`):

```
tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]
tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p4_before_p2]
```

**`cobalt_dev` LANE — proven free, not assumed.** `archiver-append-build-2026-09-19.md`'s last line, read with
the Read tool and quoted verbatim (leading segment; the full line is that report's own):

> `ARCHIVER DB d2099e1 (code tip; report 2e4ca6f, and this line's own docs-only commit on top) | offline 1866/0 (320 skipped vs round-2 320 — unchanged, and correct: step 3 runs with `.env` removed so all 23 `requires_db` skip again) | db 154/3 … | cobalt_dev: 0001–0009, unchanged (every row count and digest identical to this run's step-0 proof) | .env: removed, proven gone | …`

It starts `ARCHIVER DB ` — the archiver DB run has CLOSED. Later-session check: `grep -rln "cobalt_dev"` over
`/Users/cobalt/cobalt/docs/40 - DevDocs/reports/` returns 37 files; only three carry a 2026-09-19 date, and of
those `harness-round2-2026-09-19.md` (mtime 08:19) and `prod-proof-only-3-2026-09-19.md` (08:19) both predate the
archiver close. The one live file, `cto-2026-09-19.md`, was read at its `cobalt_dev` lines: its LAST two lane
rows are `ARCHIVER DB` closing at 12:5x ("**`cobalt_dev` FREE**", hub `82df2e20` stopped) and then
`- p4-db-0919 LAUNCHED 12:53 (S2-P4): bg 8a46e814, Opus, cwd s2-p4, 37's line verbatim. **HOLDS `cobalt_dev`**` —
which is THIS session. **No session other than this one claimed the lane after `archiver-db-0919` closed it.**

## ROUND 2 DB RUN AUTHORIZATION

Written by the CTO desk (Fable), not by Dejan. Verified by this session before anything ran, 12:52–12:53 ET.

(a) **This launch line adds NO new rule string.** All 18 `Bash(...)` rules were checked one at a time with
`grep -c -F '<string>' '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md'`
(`comm`/process substitution is not on this allowlist, so the set difference was taken string by string —
the same method round 2 used). **Every one printed `1`:**

| # | rule string | `grep -c -F` in `13-p4-verify.md` |
|---|---|---|
| 1 | `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)` | 1 |
| 2 | `Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)` | 1 |
| 3 | `Bash(COBALT_ENV=dev uv run cobalt db migrate*)` | 1 |
| 4 | `Bash(COBALT_ENV=dev uv run pytest *)` | 1 |
| 5 | `Bash(uv run pytest *)` | 1 |
| 6 | `Bash(uv run cobalt jobs restarts *)` | 1 |
| 7 | `Bash(git add *)` | 1 |
| 8 | `Bash(git commit *)` | 1 |
| 9 | `Bash(git diff *)` | 1 |
| 10 | `Bash(git status*)` | 1 |
| 11 | `Bash(git log*)` | 1 |
| 12 | `Bash(git show*)` | 1 |
| 13 | `Bash(git -C /Users/cobalt/cobalt *log*)` | 1 |
| 14 | `Bash(git -C /Users/cobalt/cobalt status*)` | 1 |
| 15 | `Bash(cd *)` | 1 |
| 16 | `Bash(ls *)` | 1 |
| 17 | `Bash(grep *)` | 1 |
| 18 | `Bash(date*)` | 1 |
| deny | `--disallowedTools "AskUserQuestion" "EnterWorktree"` | 1 |

**18 of 18 found in `13-p4-verify.md` alone** — the stricter result than the prompt's bar (found in 13 OR 24b).
Set difference `{this line} \ {13}` = ∅. **No rule is absent from the approved list; no mismatch.**
This launch line is a strict SUBSET of 13's: 13 carries 30 rules, this one 18. Dropped relative to 13, and
therefore never available to this run: `COBALT_ENV=dev uv run cobalt validate*`, the dev-vault replay,
`COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *`, the `COBALT_ENV=production` movers fetch and its
`rm`, the fixture cutter, `claude -p *`, `git rebase main`, `GIT_EDITOR=true git rebase --continue`,
`mkdir -p *`, `touch scratch/*`, `wc *`, `CronCreate`.

(b) **`.env` cp/rm are 13's own strings for THIS worktree's path, byte for byte** (rows 1–2 above; they are also
24b's). Not in this launch line and never typed by this run: production, push, vault write, `bypassPermissions`,
`claude -p`, `git rebase`, the fixture cutter, `CronCreate`, `--allow-prod`.

**AUTHORIZATION HOLDS.**

## ROUND 2 DB RUN — STATE PROBE

`.env` copied by name (`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env`) at 12:54 ET, never
opened, never printed. `COBALT_ENV=dev uv run cobalt db migrate --proof-only` — applies NOTHING. **FULL output,
verbatim:**

```
cobalt db migrate — PROOF ONLY on cobalt_dev (READ ONLY, nothing applied)

table                side    schema   rows         digest                             secs
------------------------------------------------------------------------------------------
aset_sizings         user    user     1            0824685c130da3c7cb7f0e76191a6819   0.02
bars                 system  system   1043443      2769919a57144c7bf8720110061dbf72   6.25
card_dot_taps        user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_dots            user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_stop_edits      user    user     1            7599f9ab6018697c2299e20bbacace54   0.00
card_transitions     user    user     4            f181e76b208a51b503339267865c157c   0.00
cobalt_email_sends   system  system   2            fba8cf9fc07cd6c95b503af272e26639   0.00
cobalt_jobs          system  system   13           8d9b0861615861e343009f33118a4931   0.00
cobalt_kill_switch   system  system   1            2e590e87d4c9576e61d1ee0d5c90bbab   0.00
cobalt_redactions    system  system   126          5c891af77292421e9595daef739c3941   0.00
day_modes            user    user     2            f2ffb4d41ed0a3bbc3dc2a1c7e6112b9   0.00
desk_grade           system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_packet          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_regime          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
missed               user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
movers_daily         system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
picks                user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_membership     system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_pool           system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_receipt  user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_run      system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
session_blocks       system  system   6            b650702dd6fd624548e05ca940662f08   0.00
traders              user    user     1            a64e01480038484676fad3b14eb2489f   0.00
vault_overrides      user    user     6            6a8b05207f55b8e25c253ce990c7a65a   0.01
vault_writes         user    user     184          4a965c69340f112d12e6ca21a8a0602c   0.01
------------------------------------------------------------------------------------------
26 table(s) probed on cobalt_dev; digest excludes user_id, vault_outcome, vault_reason, account_mode, pool_member_id, rank_metric, rank_value; aset_sizings: 25 card column(s) added by 0007. Proof cost: total 6.3 s — and a migration pays it TWICE (before and after), inside the outage.
NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.
```

**Read against the two expectations, plainly:**

| expectation | what the output says | verdict |
|---|---|---|
| `cobalt_dev` holds 0001–0009, all objects | all 26 tables present and probed; the three 0008/0009 relations — `movers_daily` (system), `picks` (user), `missed` (user) — are THERE, 0 rows each (this branch's own round-1 §4.4 created them and §4.7's round trip left them created) | **CONFIRMED** |
| the archiver's 0010/0011 are absent | the table list is EXACTLY the same 26 as this branch's round-1 close (§4.4 "26 table(s) proven"); no archiver relation appears — its own DB run applied 0010/0011 and then reversed them, as its report states | **CONFIRMED** |
| nothing reads `CHANGED` / no error | `--proof-only` completed in a READ ONLY transaction; no `CHANGED` line, no serialization failure, no refusal, exit 0 | **CONFIRMED** |

Only movement since this branch's own last look (§4.5b's third `.env` cycle, 10:48: `cobalt_redactions` 125):
`cobalt_redactions` **125 → 126** rows, digest moved with it. That is the ordinary test-round-trip row every
`requires_db` suite writes (§4.7 already recorded the same +1 between two runs); the archiver's DB run sat on
this database between the two looks. `bars` 1,043,443 — **unchanged**, as is every other row count.
No row of this branch's own tables moved: `radar_membership` and `radar_pool` are still 0.

**`cobalt_dev` holds (before this run): 0001–0009 objects OK, 1,043,443 rows in `bars` (the largest table).**

`.env`: `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` → `ls: /Users/cobalt/cobalt-wt/s2-p4/.env: No
such file or directory`. **Proven gone.**

CONTINUE: step 1

## ROUND 2 DB RUN — WITH-DB SUITE

`.env` re-copied by name at 12:55 ET (step 0 had removed it), never opened, never printed. The command is the
one `24b-p4-fund-rule.md` step 7 ran, verbatim:

```
COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy
```

**FULL summary line, verbatim (exit 0):**

```
2147 passed, 6 skipped, 1 xfailed, 15 warnings in 189.39s (0:03:09)
```

**THE ARITHMETIC, stated plainly — it reconciles exactly, and the prompt's first branch is the one that holds.**

| quantity | value |
|---|---|
| prior with-DB baseline (`24b` step 7, `188.89s`) | `2136 passed, 6 skipped, 1 xfailed` — **2136 passed / 0 failed** |
| this run | **2147 passed / 0 failed** |
| delta | **+11** |
| round 2's own offline delta (`## ROUND 2 (tribunal fold)` close) | 1818 → 1829 = **+11**, all offline, skips unchanged at 324 |

**+11 = +11. It reconciles with "+11 round-2 offline tests", exactly.** The number was not forced: it was read
off the summary line and 2136 subtracted. The reconciliation also settles the question the prompt left open —
the two owed `requires_db` parametrizations **were already counted inside 2136** as their OLD (vacuous) bodies;
round 2 edited those bodies without changing the test count, so the whole +11 is round 2's new offline tests.

Cross-check from the other side, which has to agree and does: offline `1829 + 324 = 2153`; with-DB
`2147 + 6 = 2153`. The same 2153 items either way, so `324 − 6 = 318` tests that skip offline RAN here, and
`2147 − 1829 = 318`. **Every `requires_db` test on this branch ran, none was skipped, and with 0 failed every
one of them passed.**

### The two owed tests — NAMED, and PASSED

The `-q` summary above proves `0 failed` but prints no test names, so the two are named from a per-test report
on the one file (`COBALT_ENV=dev uv run pytest -q tests/cobalt/test_p4_migrations.py -rA`, inside the launch
line's own `Bash(COBALT_ENV=dev uv run pytest *)` — no new rule; `18 passed in 0.41s`). L45 asks these two to be
PROVED on real Postgres, not asserted, so the `PASSED` lines are quoted verbatim rather than inferred:

```
PASSED tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]
PASSED tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p4_before_p2]
```

| owed test | verdict |
|---|---|
| `tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]` | **PASSED** |
| `tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p4_before_p2]` | **PASSED** |

**And the A5 fix is proved to be real, not just green** — the captured stdout shows the two parametrizations
applying genuinely DIFFERENT SQL sequences, which is the whole point of A5 (before the fix they were one case
twice). Verbatim, side by side:

| `[p2_before_p4]` | `[p4_before_p2]` |
|---|---|
| `0001 0002 0003 0004 0005 0006 0007` | `0001 0002 0003 0004 0005` — **0006/0007 HELD BACK** |
| `0008 0009` (first apply) | `0008 0009` (first apply, on a tree that has never seen P2's seam) |
| `0008 0009` (second apply, idempotent) | `0008 0009` (second apply, idempotent) |
| — | **`0006 0007`** — P2's OWN migrations, in the P2 position |
| — | `0008 0009` (re-apply on top of P2) |
| `0009.rollback 0008.rollback` ×2 (repeated reverse = no-op) | `0009.rollback 0008.rollback` ×2 |
| `0008 0009` (final re-apply) | `0008 0009` (final re-apply) |

`p4_before_p2` really does land 0008/0009 BEFORE P2's 0006/0007 and then apply them afterwards —
`_merge_order_bases` works against real Postgres, and the parametrization that "was vacuous" has now had its
first genuine run. Every assertion the round-2 fix kept held: the pre-existing `P4PRE` row (seeded before 0008
existed, via `_membership_insert`'s no-value form) survived the first apply with `(rank_metric, rank_value) ==
(None, None)`; `_content(_probe(...))` equalled `before` across the second apply and across the P2 arrival; the
three digest equalities held; `to_regclass` returned NULL for `system.movers_daily` and `"user".picks` after the
reverse; the repeated reverse was a no-op.

**Any OTHER red in the suite: NONE.** `0 failed` across all 2153 items. Nothing to escalate from this step.

### `cobalt_dev` after the step — measured, not assumed

The prompt asks for this to be stated explicitly rather than assumed, so a second
`COBALT_ENV=dev uv run cobalt db migrate --proof-only` was run while `.env` was still present (same rule,
nothing applied, READ ONLY). Compared line by line against the STATE PROBE above:

| | before this step | after this step | movement |
|---|---|---|---|
| tables probed | 26 | 26 | none — same 26 names, same `side`/`schema` on every one |
| `missed` / `movers_daily` / `picks` | present, 0 rows | present, 0 rows | none |
| `bars` | 1,043,443 rows, digest `2769919a…` | 1,043,443, `2769919a…` | **none** |
| `radar_membership` / `radar_pool` | 0 rows | **0 rows** | none — the merge-order test's `P4PRE`/`P4A` seeds lived in its own transaction and `conn.rollback()` in the `finally` took them back |
| every other table | (see STATE PROBE) | byte-identical row count AND digest | none |
| `cobalt_redactions` | 126 rows, digest `5c891af7…` | **127 rows, digest `0f80638d…`** | **+1 row** |

The ONE movement is `cobalt_redactions` +1 — the ordinary test round-trip row a `requires_db` suite writes on
this database; §4.7 of this same report recorded the identical +1 between two runs in round 1, and the archiver's
own DB run moved it 125 → 126 before this one. It is not this branch's data and not a migration effect: no
schema object changed, no `CHANGED` line appeared, and nothing was applied. **I have no reason to believe
`cobalt_dev` changed in any other way, and the digests are the evidence rather than my assumption.**

`.env`: `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` → `ls: /Users/cobalt/cobalt-wt/s2-p4/.env: No
such file or directory`. **Proven gone**, 12:59 ET.

**`cobalt_dev` holds (after this step): 0001–0009, unchanged | db 2147/0.**

CONTINUE: step 2

## ROUND 2 DB RUN — OFFLINE SUITE + RESTARTS

`.env` re-checked absent before the run (`ls -la .env` → `No such file or directory`, 12:59), so this is a true
offline suite: `uv run pytest -q tests/cobalt tests/taxonomy`. **Summary line, verbatim (exit 0):**

```
1829 passed, 324 skipped, 1 xfailed, 15 warnings in 55.60s
```

Against round 2's own close (`## ROUND 2 (tribunal fold)` stop line, `1829 passed, 324 skipped, 1 xfailed,
15 warnings in 56.11s`):

| | round 2 close | this run | movement |
|---|---|---|---|
| passed | 1829 | **1829** | **none — the required number** |
| failed | 0 | **0** | **none — the bar** |
| skipped | 324 | 324 | none |
| xfailed | 1 | 1 | none |
| duration | 56.11s | 55.60s | −0.51s (wall clock only) |

`passed` reads 1829 and `failed` stays 0, as this step requires. **No mismatch to name** — correct, since this
run touched no code and no offline test: its only committed change is this report.

**RESTARTS — `uv run cobalt jobs restarts a59ee8c..HEAD` (the range since the houses' whole read), VERBATIM:**

```
path	change	rule	restart
configs/cobalt/radar.yaml	M	resident reads	com.cobalt.radar
docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md	M	DOCS	-
src/cobalt/radar/config.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/radar/runner.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/replay/models.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/replay/movers.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/replay/runner.py	M	static import reach	com.cobalt.radar
tests/cobalt/test_p4_asset_type_evidence.py	M	test/documentation; no resident	-
tests/cobalt/test_p4_migrations.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_collector.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_config.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_daily.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_evaluate.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_pool.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_replay.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_runner.py	M	test/documentation; no resident	-
tests/cobalt/test_replay_movers.py	M	test/documentation; no resident	-
tests/cobalt/test_replay_runner.py	M	test/documentation; no resident	-
tests/fixtures/replay/_cut_p4_fixtures.py	M	test/documentation; no resident	-
tests/fixtures/replay/movers-gainers.real-shape.csv	M	test/documentation; no resident	-
tests/fixtures/replay/movers-losers.real-shape.csv	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset com.cobalt.radar
```

**0 UNCLASSIFIED** — every one of the 21 paths carries a rule (`resident reads`, `DOCS`, `static import reach`,
`test/documentation; no resident`). No entry to name.

**It does NOT match round 2's `com.cobalt.radar`-only line, and that is correct, not a drift** — the prompt
warns against assuming it would. Round 2 derived over `2738cdc..HEAD`, its own five commits, whose only source
file was `replay/runner.py`. This range is `a59ee8c..HEAD`, which is wider: it also contains the fund-rule chunk
(`db1c095`, `2738cdc`), so `configs/cobalt/radar.yaml` and `radar/config.py` enter the derivation and bring
`com.cobalt.aset` with them (L42: a config-shape change and every resident that reads that file are one action;
`aset` imports `radar.config`). The result equals the branch's STANDING line from the R16 close —
`RESTARTS: com.cobalt.aset com.cobalt.radar` — so the deploy's restart list is unchanged by this run.

**Tree:** `git status --porcelain` → ` M "docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md"` — this report
only, which the commit below carries. No `.env` (gitignored, so proven by `ls` above, never by `git status`).
No `scratch/`, no `src/`, no `tests/`, no `configs/` path: **this run is a verification run and changed no code**,
exactly as the prompt requires.

CONTINUE: step 3

## ROUND 2 DB RUN ESCALATE

**IS THE BRANCH'S DB DEBT CLOSED? YES.** The debt was named in round 2's own words — "the two edited
`@requires_db` merge-order parametrizations owe their first run to P4's DB step" — and both have now RUN on real
`cobalt_dev` and **PASSED**, named by node id, with the captured stdout showing the two orders applying
genuinely different SQL sequences (the point of the A5 fix). Nothing about the debt is left open: no red on
either owed test, no other red anywhere in the with-DB suite, and the arithmetic reconciles exactly
(2136 + 11 = 2147). **A red on either would NOT have closed the debt; there is none to report.**

**NEW THIS RUN: NOTHING.** No red in either owed test, no other red in the suite, no arithmetic mismatch, no
authorization mismatch, no denial, no `cobalt_dev` surprise. **0 new ESCALATE.**

### Carried from ROUND 2 (4 open, 1 ruled) — none of them mine to close

| # | item | state |
|---|---|---|
| **A2** | deadline scope: the execution deadline bounds only async work and the pre-vault-write check; `formation_source()` and `missed.reconcile()` run synchronously past it. Spec R1-16's bar is met and `runner.py:26-31` documents it; bounding synchronous DB work is a design change, not made | **OPEN** — desk/Dejan to rule before or after deploy |
| **A3** | the union sort key's `None`-filter in `radar/pool.py`, shipped undisclosed in S2-P4 STEP-2 (L52) | **RULED 12:34 ET (`cto-2026-09-19.md` §4 R23, "A"): the None-filter STAYS, disclosed and pinned; no revert.** Verified in the desk report by this session rather than taken from the prompt. `pool.py` is still untouched on this branch |
| **R2-1** | index-card line-number drift (the A1 guard at `runner.py:426`, not `:425`; the pre-vault-write check moved `:440` → `:447`) | **OPEN**, MINOR, recorded not reconciled |
| **R2-2** | the desk's A3 pre-read named the wrong half of the sort key; the branch added ONE filter, in element 1 (`pool.py:189`), and element 2 (`:198`) already carried it on main | **OPEN as an ESCALATE row**, but ANSWERED in the record: `cto-2026-09-19.md` §49 carries the correction and settles the disclosure text as "one filter, element 1, `pool.py:189`", stating R23 needs no re-ask. Retiring the row is the desk's call, not this session's |
| **R2-3** | A1's `requires_db` twin deliberately not written — no per-kind reconcile pattern exists to follow (L3), and `test_missed_rerun_reconciles_in_the_ruled_order_against_the_live_unique_index` already pins the retirement at SQL level | **OPEN** — and worth recording that that test PASSED in this run's suite (quoted in the `-rA` block above), so the pin R2-3 relies on is now proven on real Postgres too |

Round 2 also carried these untouched, and this run touches none of them: **A6** (mixed-session fixtures can
false-pass F13 — needs a membership re-cut from a production DB read) · **A7** (K3 reads a permitted frozen HOLD
as red = round 1's ESCALATE 2; a ruling is owed before the deploy smoke) · **A8** (build-report-2 wording, "16
fund tickers blank" vs 8 distinct tickers) · **A9 / A10** (carried by reference / unverifiable from reads).

### Carried from ROUND 1 — all 16, untouched, by reference

Round 1's `## ESCALATE (16)` section above stands unchanged: items 1–10 new that run (`Asset Type` rule ASKED
and since ruled by R16/R23's lineage, K3 HOLD ambiguity, mixed-session fixtures, `"v": 152` L3 duplicate, the
deploy-prompt list, the `01-p4-build.md` launch shape, the L53 Finviz record, DevDocs gaps, housekeeping, the
`.env` cycle log) plus C5, C7, C9, C10, C11, C12 carried from build2. The fund-rule retry's four (**18** four
out-of-bound test files, **19** L32 real tickers pinned in tests, **20** DevDocs stale for that chunk, **21**
the deploy-prompt consequences of R16) also stand. **This run closed none of them and opened none.**

### `.env` cycles this run

**Two**, both `cp` by exact name, never opened, never printed, never in a command's output:
step 0's state probe (copied 12:54, removed 12:55, `ls` proved gone) and step 1's suite (copied 12:55, removed
12:59, `ls` proved gone). `ls -la .env` re-checked before the offline suite (12:59) and again immediately before
this stop line (13:02): **`No such file or directory` every time.** No builder ran at any point — this run
launched no child session (L36/L41: the DB step is hub-direct, never delegated).

MEMORY: a round that EDITS a `requires_db` test body without adding one moves no count — the with-DB total then
shifts by exactly the offline delta, so the reconciliation to look for is `old_db + offline_delta` (S2-P4:
2136 + 11 = 2147, and `offline_passed + offline_skipped == db_passed + db_skipped` = 2153 both ways, which
proves every `requires_db` test actually ran rather than silently skipping) [stated 2026-09-19 · Code].
MEMORY: A5's merge-order fix is proven on real Postgres — `p4_before_p2` applies `0001–0005 → 0008 0009 → 0008
0009 → 0006 0007 → 0008 0009` while `p2_before_p4` applies `0001–0007 → 0008 0009 → …`, so the two
parametrizations are genuinely different cases now and the pre-existing no-value seed survives the first apply
with `(rank_metric, rank_value) == (None, None)` [stated 2026-09-19 · Code, `pytest -rA` captured stdout].
RULING (proposed, for the desk): none from this run. The two still owed elsewhere are A2 (design gap) and the
formal retirement of R2-2's row now that §49 carries its correction.

**Still owed on this branch after this run, and none of it mine (L67):** the house check (≥3 houses) on the
fold · the A2 design-gap ruling · Dejan's approval · the deploy. This run never merged, never pushed, never
deployed, never touched production, never used `--allow-prod`, and never left this worktree.

P4 DB 9045260 (code tip, UNCHANGED — this run built nothing; report commits 6b42666 + this line's own on top) | offline 1829/0 | db 2147/0 (vs 2136/0 + 2 owed — +11 = round 2's 11 offline tests, reconciles exactly) | owed requires_db: test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4] → PASSED, [p4_before_p2] → PASSED | cobalt_dev: 0001–0009, unchanged (only cobalt_redactions +1, the ordinary suite round-trip row; every other row count and digest byte-identical, bars 1,043,443) | .env: removed, proven gone | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: 4 carried open (A2, R2-1, R2-2, R2-3), A3 RULED 12:34 ET, 0 new | DB DEBT: CLOSED
