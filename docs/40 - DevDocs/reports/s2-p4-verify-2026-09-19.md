# S2-P4 verify — 2026-09-19

## §0 Headline
- DO NOT STOP until READY — reached 10:50 ET: READY, code tip `a59ee8c`, rebased onto main `919d562`; offline 1796/0, dev-DB 2114/0, `cobalt_dev` 0001–0009, rollback round trip ok.
- Built: column set one source + 151-column movers fixtures · plan correction (row 2) · smoke `compare` kind, K8 machine assertion (row 4) · K9 short side (row 8, L52 bound held) · K3 SQL ran on Postgres (row 6).
- **`Asset Type` rule ESCALATED**, not built: 16 fund rows have a blank `Asset Type` (table C ≠ 0); the desk chooses the rule (ESCALATE 1). K3 HOLD ambiguity confirmed on Postgres (ESCALATE 2).
- Found and fixed on the DB run: 3 test-side reds (2 params of a migration test, 1 P2 test vs P4's `FillResult`); no src change.
- ESCALATE: 16. OWED: three-house check, receipt-reference tribunal, deploy prompt.

## AUTHORIZATION
Written by the CTO desk (Fable `318239db`), not by Dejan. Verified by this session, 09:2x ET:
| check | evidence |
|---|---|
| rows committed on main | `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` → `db2dcb1`, `e748c8c`, `8b0a3bb`, `32bbfe8`, `6c582ef`, `920b0a9`, `11960d9`, `4da7429` (all desk commits; the R-rows `cto-2026-09-17.md` R5/R6, `cto-2026-09-18.md` R1/R11 are older, on main) |
| `cto-2026-09-17.md` R6 quote | line 47: 06:11 ET "approve." — the P4 hub list (`_fetch_movers.py` vault-token fetch, `.env` cp/rm by exact path, `COBALT_ENV=dev` migrate/pytest/validate/replay dry-run, read-only real-vault pytest, `claude -p`, git add/commit/rebase main, plain reads, `CronCreate`) |
| rule strings vs committed launch lines (`grep -c -F`, each = 1) | `01-p4-followup.md`: migrate*, replay nightly *, dev pytest/vault pytest triple, cp/rm/migrate/validate quad, `git rebase main`, and the tail `claude -p *` … `CronCreate` — identical except `Bash(grok *)` (DROPPED, as this prompt says); `04-p4-relaunch.md`: `_fetch_movers.py*` run, its `rm`, `_cut_p4_fixtures.py*` — each 1 |
| a rule in neither file | none |
| mismatch | none |

## PREFLIGHT
| rule / probe | command | exit | result |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | `Sat Sep 19 09:22:44 EDT 2026` |
| `Bash(git status*)` | `git status --porcelain` | 0 | empty |
| `Bash(git status*)` | `git status` (long) | 0 | "On branch sprint-2/p4 / nothing to commit, working tree clean" — no rebase in progress |
| `Bash(git log*)` | `git log --oneline -1` | 0 | `2a8784c` (first launch) |
| `Bash(git -C … status*)` | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | ` M docs/40 - DevDocs/reports/seat-usage.md` — desk's, not this session's, untouched |
| `Bash(git -C … *log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | `db2dcb1` (main tip; docs-only desk commits after deploy `2893a7f`/`8d17cee`) — the main this run rebases onto |
| `.env` cp | `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` | 0 | allowed |
| `COBALT_ENV=dev … migrate*` | `COBALT_ENV=dev uv run cobalt db migrate --help` | 0 | allowed (this branch's help lists `--allow-prod --down-to --rollback`; no `--proof-only` yet: main's harness arrives with the rebase) |
| `.env` rm | `rm …/.env` then `ls -la .env` | 0 / 1 | allowed; `ls: .env: No such file or directory` |
| `Bash(claude -p *)` | `claude -p --model claude-opus-5 "Reply with exactly the word OK"` | 0 | `OK`, no usage-limit message (L47 meter clear) |
| `Bash(uv run pytest *)` | `uv run pytest --co -q tests/cobalt/test_tenancy.py` | 0 | 34 collected |
Denials: none. The two script rules (`_fetch_movers.py`, `_cut_p4_fixtures.py`) are probed by first real use.

## §1 Rebase onto main
| item | value |
|---|---|
| main at 1.2 | `919d562` (desk docs-only commit after `db2dcb1`; the deploy tip is unchanged) |
| 1.1 measured by this run | `main..HEAD` = 65 rows; `976528f..HEAD` = 31; `--cherry-mark --right-only main...HEAD` = 34 `=` (976528f and below) + 31 `+` — agrees with the desk |
| `git rebase main` | dropped the 34 patch-identical commits itself ("skipped previously applied commit" ×34); replayed 31 |
| conflict stops | **ONE**, at 6/31 (`7ead430` chunk A), docs only: `docs/40 - DevDocs/cobalt/db_migrations/cli.md` — main's new "no `schema_migrations` ledger" paragraph vs P4's `## 2026-09-17 — S2-P4` section; both kept (main's first). No stop in `placement.py`, `__init__.py`, `cli.py`, tests or configs (the expected zones merged clean). The stop's subject is a P4 commit, not one of the 34 |
| resolver | ONE Opus headless run, brief `scratch/rebase-brief-v1.md`, note `scratch/rebase-resolve-v1.md`; hub-verified: `git diff --check` clean, no marker lines, `tests/cobalt/test_p4_migrations.py` + `test_cards_picks.py` 25 passed 17 skipped; `git add` that one file; `GIT_EDITOR=true git rebase --continue` |
| launch-shape note | the first resolver launch (the `01-p4-build.md` step-2 shape, prompt LAST) exited 1 in 0 s with `Input must be provided either through stdin or as a prompt argument` — `--allowedTools` is variadic and swallowed the trailing prompt as a tool name. Nothing ran, worktree untouched. Relaunched with the prompt FIRST (`claude -p "<prompt>" --model … --permission-mode acceptEdits --allowedTools <the same 17 rules, byte for byte>`), the shape `03-p2-rebase.md` §2.2 used. Same rule (`Bash(claude -p *)`), same list, no denial. **Every builder launch in this run uses the prompt-first shape** (finding for the desk: `01-p4-build.md` step 2's shape cannot run as written) |
| 1.3 count | `git log --oneline main..HEAD` = **31**; the two subject columns are identical row for row (same order: `04112ed docs(plan): S2-P4 plan + report …` at the bottom → `fc61ac2 docs(report): fill the close commit sha …` at the top); none missing, none extra |
| 1.3 harness diff | `git diff main HEAD -- src/cobalt/db_migrations/cli.py placement.py __init__.py` read in full: `__init__.py` = 0008/0009 in `FORWARD` + `REVERSE` (numeric order) + docstring; `cli.py` = `DIGEST_EXCLUDED_COLUMNS` gains `rank_metric`, `rank_value` (+ comment); `placement.py` = `CREATED_TABLES` gains `movers_daily`/`picks`/`missed`, `missed` leaves `DECLARED_TABLES`. Every changed line is P4's; not one line of main's harness (streamed proof, `--proof-only`, REPEATABLE READ, ambiguous-name refusal) altered or missing |
| 1.3 suite | `uv run pytest -q tests/cobalt tests/taxonomy` → **`1753 passed, 322 skipped, 1 xfailed, 15 warnings in 54.68s`**, 0 failed (09-18's offline line was 1714 passed; main added tests: more, not fewer) |
Tip after the rebase: `fc61ac2`.

## §2 Chunk AT — `Asset Type`
### 2.1 The export (hub, one production-token READ)
| item | value |
|---|---|
| FACTS checked before briefing | `radar.yaml` `export.v: 152`, `columns: "0-150"`, `required_headers` carries `Asset Type`; `radar/collector.py:161` builds `c` from `range(151)`; `replay/movers.py:210` `_params` builds the SAME set as a second literal; `radar/throttle.py:180` and `radar/propose.py:180` carry two more `range(151)` (four sites, not the two the desk named — the builder gets all four). The old fixtures were 21 columns: `ls -la` before the run: `scratch/movers-gainers-raw.csv` and `-losers-raw.csv` = 1,428,920 bytes each, Sep 17 06:26 |
| `_params` (quoted, `replay/movers.py:209-210`) | `def _params(self, side): return {"v": self.config.export.v, "c": ",".join(str(v) for v in range(151)), "o": SIDES[side]}` with `SIDES = {"gainers": "-change", "losers": "change"}`; no `f=` |
| script | `scratch/_fetch_movers.py` (not committed; deleted after the run): `resolve_token` + `finviz_get` from `cobalt.archiver.collector`, both silencers first, ONE outer `try/except Exception` → `ERROR <type>` + `sys.exit(2)`, inner retry silent (60 s, once), both bodies validated in memory (151 columns, `Asset Type` in the header, ≥1 row) before either file is written |
| token-safety: checked | script lines: 21–24 `logging.disable(CRITICAL)` then loguru `logger.remove()` BEFORE any cobalt import or credential call; 28 the only transport import; 41 `resolve_token()` inside the retried block; 47–52 `finviz_get(..., on_metrics=…)` — the callback prints an int status only; no `raise_for_status`, no `__cause__`, no `str(e)`, no URL/params/header/token in any print or f-string; 58–64 the retry handler prints nothing; 73–78 the one printing handler prints `type(e).__name__` only. Read against the run prompt paragraph line by line before the run |
| run 09:28–09:29 ET | `cd /Users/cobalt/cobalt` → `COBALT_ENV=production uv run python …/_fetch_movers.py` → tool result, verbatim: `gainers status 200` / `gainers rows 11640 columns 151` / `losers status 200` / `losers rows 11640 columns 151` / `written 2 files`. Exit 0, no retry needed (2 requests). Nothing else was in the tool result |
| cleanup | `cd` back; `rm …/_fetch_movers.py` → `ls scratch/_fetch_movers.py`: `No such file or directory`; `git status --porcelain` empty |
| after | `scratch/movers-gainers-raw.csv` and `-losers-raw.csv` = 11,481,555 bytes each, Sep 19 09:29 (151 columns; the top rows are cut by the cutter, 60 per side) |
| L53 note (for ESCALATE) | two Finviz `/export/screener` requests at ~09:28–09:29 ET on a Saturday, radar `idle:overnight`, archiver not running |

### 2.2 The evidence (builder AT-0, commit `27b1d36` `test(fixtures): Asset Type evidence mode for the P4 cutter`)
Builder report `s2-p4-verify-opus-AT0-2026-09-19.md`; tests-first: 10 red (missing attribute) → 11 green in `tests/cobalt/test_p4_asset_type_evidence.py`; hub re-ran that file (11 passed). Hub ran `uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence` (first real use of that script rule: allowed): stdout `files: 16307 (without the columns: 0); rows: 643998 (without the columns: 0); distinct fund tickers: 5702; distinct Asset Type values: 28; table C total: 16`. Inputs: every `*.csv` under `data/radar-cache/` (5 days, 09-14…09-18; scan snapshots repeat rows, so row counts are not distinct-ticker counts) + the two new raw movers exports.
Table A (non-blank `Asset Type`, with its `Industry`) — every one of the 28 values pairs ONLY with `Industry = Exchange Traded Fund`:
| Asset Type | rows | | Asset Type | rows |
|---|---|---|---|---|
| Bonds | 6176 | | Multi-Asset - Tactical / Active | 278 |
| Carbon Trading | 8 | | Multi-AssetTarget Date - 2030…2070 (9 values, 2 each) | 18 |
| Closed End Funds | 18 | | Preferred Stock | 176 |
| Commodities & Metals | 7890 | | Private Equity | 12 |
| CryptoCurrency | 7283 | | SPAC | 4 |
| Currency | 30 | | Hedge Fund Replication | 36 |
| Equities (Stocks) | 109257 | | MLP | 34 |
| Equities (Stocks) - IPO Based | 12 | | Multi-Asset - Conservative / - Growth-Aggressive / - Moderate / - Spread Between Asset Classes | 30 / 16 / 28 / 22 |
| Freight Futures | 4 | | | |
Table B (`Industry = Exchange Traded Fund`, by `Asset Type`): the same 28 values with the same counts, plus **`<blank>` = 16 rows**.
Table C: **(i) non-blank `Asset Type` with a non-fund `Industry` = 0 rows; (ii) fund `Industry` with a BLANK `Asset Type` = 16 rows (8 distinct tickers, each in both movers exports, none in the radar cache); total 16.**
DECISION RULE, in order: (0) zero funds — no (5702). (i) sample floor — met (5702 ≥ 10 distinct fund tickers, 28 ≥ 2 values). (ii) requires table C = 0 rows — it is 16. (iii) **`Asset Type: ESCALATED`.** Built: 2.3 and 2.5. NOT built: 2.4's rule, `not_equity` unchanged, `REQUIRED_HEADERS`/`unreported` branch untouched (the hub reads "nothing of 2.4's rule" to include them; both are named in 2.4). Reading of the evidence for the desk (no build): "non-blank `Asset Type`" never mislabels a stock (0 rows) but misses 8 real funds that Finviz leaves blank (the movers lists are the whole market and are where they show); `Industry = Exchange Traded Fund` includes every row of table A plus those 16 (table B ⊇ table A: every non-blank value sits on an ETF industry row), so on this sample it is the superset rule. Whether `Industry` could ever label a non-fund is not testable here — no non-fund row carries `Exchange Traded Fund`. Note `not_equity.values: [Exchange Traded Fund]` is already an `Industry` value, but `not_equity.header` is `Asset Type` — pointing the header at `Industry` would be a one-line config change; that is the desk's ruling to make, not built here.

### 2.3 / 2.5 The change (builder AT-1, commit `05ece47`; builder report `s2-p4-verify-opus-AT-2026-09-19.md`)
`fix(s2-p4): movers take the radar's column set from one source; fixture re-cut at 151 columns — the not-equity rule is ESCALATED, not built` (the escalated wording the prompt prescribes). Brief `scratch/at1-brief.md` carried all FOUR `range(151)` sites.
| item | result (hub-verified) |
|---|---|
| 2.3 one source | `cobalt.radar.config.screener_columns()` / `screener_columns_param()` beside `ExportConfig` (parses `a-b` / comma list, anything else crashes, L1); called by `radar/collector.py`, `replay/movers.py`, `radar/propose.py::_columns` and `radar/throttle.py` (`run_probe(columns=…)`, required kwarg; `command()` reads the config). Hub: `grep -rn "range(151)" src/cobalt` → **no output (0 hits)**. No config shape change |
| 2.3 tests | `test_every_finviz_consumer_asks_for_the_same_column_set` (collector = movers gainers = movers losers = throttle probe = the written-out string; passes before and after by design, stated) · `test_only_one_function_in_the_new_core_builds_the_column_set` (RED before: 4 literals) · `test_screener_columns_parses…` / `test_a_malformed_column_declaration_crashes` (RED: ImportError) |
| 2.5 fixture | new cutter mode `movers` (`uv run python tests/fixtures/replay/_cut_p4_fixtures.py movers`) re-cuts ONLY the two movers fixtures — the no-argument mode re-cuts every fixture and needs raw inputs that no longer all exist, so the prompt's bare command was not used (finding; the no-arg mode still works as before). Builder ran it: gainers 60 rows, 151 columns, **29** with a non-blank `Asset Type`; losers 60 rows, 151 columns, **19**; nothing appended (a real fund row sat at gainers rank 5 / losers rank 7); `_anonymize` shifted 23/26 dates; `grep -c "2026-09-"` on both fixtures = 0/0. Header row byte for byte from the raw file up to line endings (raw CRLF → LF, as the P2 cut of `pool-metrics.real-shape.csv` already is; a CRLF header would not equal the radar fixture's — one shape, asserted) |
| 2.5 tests | movers fixture header == radar `pool-metrics.real-shape.csv` header byte for byte · 151 columns with `Asset Type` · ≥1 real non-blank row per side · `parse_movers` reads `Asset Type` from the export · evidence tests now use the movers fixture as a REAL input (tables A/B non-empty; table C = 0 in the top 60, so C's disagreement path is covered by the hub's full-cache run only) |
| leak test | `test_screen_filter_values_live_only_in_approved_radar_fixtures` passes (inside the suite) |
| 2.6 suite | hub: `uv run pytest -q tests/cobalt tests/taxonomy` → **`1783 passed, 322 skipped, 1 xfailed, 15 warnings in 54.59s`**, 0 failed |
| 2.6 `not_equity` | `grep` of `src/cobalt/radar` + `replay`: the exclusion line (`radar/runner.py:144`), `NotEquityConfig`, `benchmark_misses`, `REQUIRED_HEADERS`, the `unreported` branch and `radar.yaml` are UNCHANGED (diff read) |
| 2.6 RESTARTS | `uv run cobalt jobs restarts main..HEAD`: `RESTARTS: com.cobalt.aset com.cobalt.radar` (0 UNCLASSIFIED); this chunk's own `src/cobalt/radar/*` and `replay/movers.py` changes derive radar/aset as before; **`not_equity` unchanged — no RESTARTS from this chunk's config** (no config file touched). The table also lists `D` rows for desk docs — main gained them after this rebase; they are not P4 changes |
Builder findings carried: (a) **the re-fetched exports are a different trading day (09-19 data) from `membership-day.real-shape.json` (the 09-14 anchor day)** — the re-cut forced ticker repairs in `test_replay_movers.py` (8 tests), `test_replay_runner.py` (2) and the evidence tests; each benchmark test now points at a real row that carries its case, but the mover set and the membership set no longer come from one session; re-cutting the membership fixture from the same day needs a production DB read — desk's call (ESCALATE 13). (b) `radar/throttle.py` and `propose.py` also hard-code `v: 152` — the same L3 duplicate one field over, not fixed (ESCALATE 14). (c) the two sibling cutters `_cut_p2_fixtures.py` / `_cut_panel_fixtures.py` mention the anchor-month pattern in comments (a `grep -rlE '2026-09-'` over the fixture dirs lists them; no fixture data leaks) — pre-existing, not mine.

## §3 Chunk TR — triage rows 2 · 4 · 8
Split into two builder runs (TR-A row 4 + the K3 test; TR-B row 8) because rows 4 and 8 both edit `configs/cobalt/smoke/s2.yaml` and `git add -p` is not available: one commit per item needs one run per item. Briefs `scratch/tr-brief.md`, `scratch/tr2-brief.md` (the two STOP sentences of rows 4 and 8 verbatim in the briefs).
| row | result |
|---|---|
| 2 plan text | hub, docs only: `[corrected 2026-09-19 — cto-2026-09-18.md §21 row 2 …]` under BOTH old wordings (STEP-5 Formations bullet, line 184; R1-21 bullet, line 189→190), old text kept. Commit `1bc0dd9` `docs(plan): s2-p4 — the shipped replay entrypoint, corrected in place`. Verified against the tree first: `radar/evaluate_cli.py:146 def replay_formations`, `radar/evaluate.py:132 EVALUATOR_VERSION = "s2p2.1"`. **BUILT** |
| 4 compare kind | TR-A (`s2-p4-verify-opus-TRA-2026-09-19.md`), commit `089b069` `feat(smoke): compare check kind; K8 counts_agree is a machine assertion again`. Design: one optional additive field `result_number` on `sql`/`job_row` names THE number of a check; `kind: compare` (`left/right/op`, only `eq`) is validated at load (unknown id, self-reference, operand below the row, operand with no number, op not eq → loud) and graded at run (equal PASS · unequal FAIL · operand ERROR → ERROR · non-numeric → ERROR); shipped `K8.3` `left: K8.1, right: K8.2, op: eq`, hand comparison text removed from K8.1/K8.2. STOP sentence did not trigger: an old smoke file with no compare row loads and runs byte-identically (a test proves it). Tests first: 6 red (quoted in the builder report) → 6 green. GRANT-reading test from FX kept. Hub: suite `1788 passed, 324 skipped, 1 xfailed, 0 failed`; `git diff` of `s2.yaml` read. **BUILT** |
| 6 (K3 test file) | commit `7b3846c` `test(smoke): K3's SQL runs on Postgres` — `tests/cobalt/test_smoke_k3_sql.py`, 2 `requires_db` tests (skip offline: 2 skipped, quoted by the builder); their run is step 4.6 |
| 8 K9 short side | TR-B (`s2-p4-verify-opus-TRB-2026-09-19.md`), commit `0443112` `fix(s2-p4): K9 compares against the rows the export really had (short side)`. `ReplayResult.movers_by_side` records per side `{exported, top_n, expected = min(top_n, exported)}` (validated as `min`); K9 became six rows (K9.1–K9.6: user-side stored count, system-side job row's `expected`, `compare` eq, per side) — the same tenancy wall as K8, no new grant. **L52 STOP sentence did not trigger** — hub read the diff: the only changed line inside `parse_movers` is the constructor call (`rows=tuple(rows[:top_n])` intact, `+ exported_rows=len(rows)`); `benchmark_misses`, `archive_movers`, `MoversStore`, `MoversCollector.exports`, `retained_exports` have zero changed lines; the runner gains one statement (`export_counts`) before any write; the builder also executed HEAD's `parse_movers` out of git against the new module at 8 caps × 2 sides: identical rows (`scratch/proof_rows.py`, gitignored). Tests first: 5 red (+1 collection error) → 8 green. Hub suite: `1796 passed, 324 skipped, 1 xfailed, 0 failed`. **BUILT** |
Consequences for the deploy prompt (ESCALATE 15): the replay `job.result` shape gains `movers_by_side`; until the first `com.cobalt.replay` run after deploy no job row carries it, so K9.2/K9.5 read FAIL and K9.3/K9.6 ERROR (a smoke run before that night is RED by design). The two new K9 SQL statements have never run on Postgres — step 4.5's `test_committed_queries_run_read_only_on_cobalt_dev` is their first run.

## §4 DB verification — `cobalt_dev` ONLY (10:35–…, `.env` copied in at 10:35 by name, never read)
### 4.2 State found (before any change), `COBALT_ENV=dev uv run cobalt db migrate --proof-only`, 10:35:30 ET
Verbatim facts: 26 tables probed; `aset_sizings` 1 row, **`bars` 1,043,443 rows**, `cobalt_jobs` 13, `vault_writes` 184; the P2 tables (`radar_score`, `radar_score_run`, `radar_score_receipt`, `card_dots`, `card_dot_taps`, `card_stop_edits`, `card_transitions`) present; **`missed`, `movers_daily`, `picks` = `-` (absent)**; `radar_membership`/`radar_pool` 0 rows. Proof cost **total 6.4 s** (`bars` 6.35 s), "a migration pays it TWICE". Last line: `NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.` (Full table is in the tool result; the numbers above are the ones the desk budgets from.)
So `cobalt_dev` held 0001–0007 (P2's re-apply from 09-18 stands) and **NOT** 0008/0009 — the prompt's "probably 0001–0009" was wrong. 4.3 (rollback first) does not apply: nothing of 0008/0009 to reverse.
### 4.4 Forward, `COBALT_ENV=dev uv run cobalt db migrate`, 10:35:43 ET
Because the harness keeps no ledger, this was a FULL forward over the registered list, not P4's alone: `-- applying 0001 … 0009` in order (nine lines). Result table: 23 pre-existing tables `OK` (digests before = after on every one); **`missed` CREATED (0 rows), `movers_daily` CREATED (0), `picks` CREATED (0)**; last lines `26 table(s) proven … content UNCHANGED on every table.` / `proof cost: BEFORE 5.9 s + AFTER 6.0 s = total 11.9 s; slowest table bars (5.8 s before).` No `CHANGED`, no serialization failure, no refusal. Since the forward was a full 0001–0009 pass, its duration is not P4's alone; the deploy number is 4.7's second forward (0008 + 0009 on a database that already holds 0007).
`system.bars` on dev = 1,043,443 rows; production has 8.83 M (≈8.5× more) and its proof took 104 s — dev's 6 s is NOT a prediction for production; 0008/0009 add no rows to `bars`, so the proof cost the deploy budgets is production's own last number (104 s before + 104 s after), plus P4's DDL, which ran here in the same forward pass without a separately measurable cost.

### 4.5 DB suite (10:36–10:39), `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`
**`3 failed, 2111 passed, 6 skipped, 1 xfailed, 15 warnings in 185.15s`.** The four reds of 09-17 ESCALATE 4 / followup ESCALATE 2 are GONE (0006/0007 sit under P4; none of the tenancy/placement tests is red). The 3 that ARE red are new, all test-side:
| test | assertion | cause |
|---|---|---|
| `test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]` and `[p4_before_p2]` | `test_p4_migrations.py:297` `assert _probe(conn, "radar_membership") == before` → the only differing item is `'seconds'` (0.00123 vs 0.00173) | main's harness (rebased under P4) adds the probe's wall time to `_probe`'s dict; P4's test compares whole dicts. Content (`digest`, `rows`) never differed |
| `test_radar_cards_db.py::test_manual_cards_are_unaffected` | `assert len(ids) == 2` → `TypeError: object of type 'FillResult' has no len()` | P2's test (now on main) treats `CardStore.fill` as returning a list; P4's ruled R1-5 returns `FillResult` (`transition_ids`) |
Neither is a live-value assertion on production-mirroring data, and no data was touched. Fix: tests only, builder run FIX-DB after 4.8 (no builder while `.env` sits in the worktree); the DB suite is then re-run in a second `.env` cycle (below).
### 4.6 ROW 6 — K3 on real Postgres (10:38)
`COBALT_ENV=dev uv run pytest -q tests/cobalt/test_smoke_k3_sql.py -rA` → **`2 passed in 0.18s`**: (1) **K3's statement EXECUTED** on `cobalt_dev` through the framework's own loader/renderer/read path, returned exactly one row with exactly the five declared columns (`post_deploy_admitted, metric_missing, value_null, rescanned_admitted, rescanned_metric_missing`); on dev `radar_membership` and `radar_pool` hold 0 rows (the proof table), so K3's baseline counters are all 0 by construction — the check would read KNOWN there (inferred from the empty tables; the test asserts shape, not the zeros). (2) **`test_k3_hold_row_reads_red_documented_ambiguity` — a HOLD-shaped row (pre-deploy `first_seen_at`, `rank_metric` NULL, `last_scan_id` = the pool's post-cutoff scan) made `rescanned_admitted ≥ 1` and `rescanned_metric_missing ≥ 1`, and the framework's grader returned FAIL naming `rescanned_metric_missing`.** So build2 ESCALATE 6 is CONFIRMED on real Postgres: a degraded-source night that HOLDs a pre-deploy episode reads K3 red, indistinguishable by column from a RETAIN defect. The rows lived in the test's own transaction and rolled back (radar tables 0 rows after). Nothing built. Also `test_committed_queries_run_read_only_on_cobalt_dev` → **1 passed**: every committed SQL check of `s2.yaml` (K2, K3, K5.x, K6, K8.2, K9.1, K9.4, K10.2 …) ran read-only on `cobalt_dev` — the two new K9 statements' first execution.
Decision for the desk (ESCALATE 6, unchanged options): accept the loud false positive with the read-against-`degraded_sources` procedure already in K3's `expect_text`; or a row-level discriminator (schema change, its own chunk); or revert `b327221`-lineage (K3 RETAIN grading). Recommendation: accept — it is loud, documented, and only reachable on a night with a degraded source.
### 4.7 Rollback round trip (10:39–10:40)
`COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` (10:39:54) → applied `0009_picks_missed.rollback.sql` then `0008_radar_value_movers.rollback.sql` and NOTHING else; `missed`, `movers_daily`, `picks` **DROPPED**; the other 23 tables `OK`, digests before = after; `26 table(s) proven … content UNCHANGED on every table`; proof cost 12.5 s. Then `COBALT_ENV=dev uv run cobalt db migrate` (10:40:11): the same nine `-- applying` lines, the three relations **CREATED**, 23 `OK`, proof cost **12.2 s** (BEFORE 6.1 + AFTER 6.1) — the number for P4's 0008 + 0009 on a database that already holds 0007 (the harness has no ledger, so the run re-applies the whole list; the proof is what costs, and it scales with `bars`: production's own last figure is the budget, dev's 12 s is not).
Side by side, 4.4 forward vs this second forward: every schema-object line is EQUAL — each of the 23 pre-existing tables `OK` with the same digest before/after, `missed`/`movers_daily`/`picks` `- -> user|system … CREATED` with digest `- -> d41d8cd9` and 0 rows; the nine `applying` lines equal. Differences, all explained (R18): durations (5.83/5.98 vs 6.08/6.02 s for `bars`, etc.); **`cobalt_redactions` 123 → 124 rows (digest `2797ee97` → `19538c9d`)**, the one row the DB suite of 4.5 wrote between the two runs (the digest is identical inside each run: before = after). **Round trip: ok.**
### 4.8 `.env`
`rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env`: `ls: .env: No such file or directory` → `git status --porcelain` shows no `.env` (only the report). `cobalt_dev` now holds **0001–0009**.

### 4.5b The three reds fixed and re-proven (10:43–10:47)
Builder run FIX-DB (brief `scratch/fix-db-reds-brief.md`, report `s2-p4-verify-opus-FIXDB-2026-09-19.md`), tests only — hub read the diff: `test_p4_migrations.py` gains `_content(probe)` (drops ONLY `seconds`, by exclusion, so a key added to `_probe` later is still compared) and the three equalities compare content; `before` also asserts `rows > 0` and `digest` are present (no vacuous "unchanged"); the two `["digest"]` field-wise lines were already fine; `test_radar_cards_db.py::test_manual_cards_are_unaffected` asserts `len(result.transition_ids) == 2` (the same count, the accessor of R1-5's `FillResult`). No `src/`, no fixture, no assertion weakened. Commit `a59ee8c` `fix(s2-p4): two dev-DB reds are test-side …`. Second `.env` cycle: `cp` → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → **`2114 passed, 6 skipped, 1 xfailed, 15 warnings in 190.12s` — 0 failed** (the 3 are green on real Postgres) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (state after the suite: 26 tables, `missed`/`movers_daily`/`picks` present with 0 rows, `bars` 1,043,443, `cobalt_redactions` 125 — the suite's own rows) → `rm .env` → `ls -la .env`: `No such file or directory`; `git status --porcelain`: the FIX-DB files only, no `.env`.
`cobalt_dev` now holds: **0001–0009**.
### 4.9 Real-vault read-only tests (no `.env`)
`COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` → **`33 passed, 63 skipped, 1741 deselected in 0.74s`** (the 63 skips are `requires_db`/DB-env tests — no `.env` here by design). No vault write.
### 4.10 OWED from build2, done here: `cobalt validate` (third `.env` cycle, 10:48)
`COBALT_ENV=dev uv run cobalt validate` → exit 0: 13 trade_defs validated from the vault, **`Jobs (F17): 16 registered — 6 resident, 10 one-shot`, `registry <-> ops/: 16 label(s), exact match`, `registry <-> plists: schedules and COBALT_ENV agree on every job`** (so `com.cobalt.replay`'s registry row, plist and the smoke file load), `reads: 3 config path(s) re-read at runtime by 2 resident(s)` (`radar.yaml` → radar; `tunables.yaml` → aset, radar; `aset.yaml` → aset), `Placement: tree clean`. `.env` removed again and proven (`ls -la .env` no such file; `git status --porcelain` empty).

## §5 Close
| check | result |
|---|---|
| offline suite | `uv run pytest -q tests/cobalt tests/taxonomy` → **`1796 passed, 324 skipped, 1 xfailed, 15 warnings in 56.24s`**, 0 failed |
| leak test | `test_screen_filter_values_live_only_in_approved_radar_fixtures` → **`1 passed, 1836 deselected`** |
| RESTARTS | `uv run cobalt jobs restarts 919d562..HEAD` (P4's own range; `main..HEAD` now also lists 24 desk docs that main gained after this rebase as `D`, none is P4's): 0 UNCLASSIFIED. Non-docs, non-test rows: `configs/cobalt/jobs.yaml M → -` · `configs/cobalt/smoke/s2.yaml A → -` (operator command) · `configs/cobalt/taxonomy/tunables.yaml M → com.cobalt.aset,com.cobalt.radar` · `ops/com.cobalt.replay.plist A → new one-shot, bootstrap once: com.cobalt.replay` · `ops/README.md M → -` · `src/cobalt/**` (archiver, aset, cards, cli, dayopen, db_migrations, db_query, jobs, radar, replay, settings, smoke) → `com.cobalt.aset` and/or `com.cobalt.radar` (no other resident) · `0008`/`0009` `.sql` + rollbacks `→ -`. **`RESTARTS: com.cobalt.aset com.cobalt.radar`** (+ bootstrap of `com.cobalt.replay`). This run added no config-shape change: `radar.yaml` `not_equity` is UNCHANGED. Full per-path table is deterministic: `uv run cobalt jobs restarts 919d562..HEAD` |
| commits `919d562..HEAD` | 31 own (step 1.3) + 12 of this run: `07c6528` report step 1 · `27b1d36` test(fixtures) evidence mode · `1bc0dd9` docs(plan) · `05ece47` fix column source + fixture re-cut · `355580b` report step 2 · `089b069` feat(smoke) compare kind · `7b3846c` test(smoke) K3 SQL · `89532a2` report · `0443112` fix K9 short side · `869780a` report · `5a2e450` report step 4 · `a59ee8c` fix two dev-DB reds test-side — then the close commit(s) |
| main | rebased onto `919d562`; `git -C /Users/cobalt/cobalt log --oneline -1` now `a89105f` (desk docs-only commits: `git diff --stat 919d562 main` = 24 files, all under `docs/`) — still READY; the deploy hub rebases again |
| worktree | clean (only `scratch/`, untracked by the local exclude); every `git show --stat` of this run's commits listed no `scratch/` path; `.env` absent (last `ls -la .env` 10:49) |

## ESCALATE (16)
New this run:
1. **`Asset Type` rule — ASK DESK: which rule — non-blank `Asset Type`, `Industry = Exchange Traded Fund`, or either? [09:37]** Evidence (counts, L32): 16,307 files / 643,998 rows / 5,702 distinct fund tickers / 28 distinct non-blank values; every non-blank `Asset Type` sits on an `Industry = Exchange Traded Fund` row (table C (i) = 0), and **16 fund-industry rows (8 distinct tickers, in both whole-market movers exports) have a BLANK `Asset Type`** (table C (ii)). So today's `not_equity.values: [Exchange Traded Fund]` matches nothing because its header is `Asset Type`; "non-blank `Asset Type`" would still let 8 real funds into the benchmark; `Industry = Exchange Traded Fund` covers the whole sample (one-line change of `not_equity.header` — not built). Safe default taken: NOTHING of 2.4's rule built, `REQUIRED_HEADERS`/`unreported` branch untouched. When built later: config-shape change → RESTARTS `com.cobalt.radar` (L42), 0009 undeployed makes the `inputs_sha256` change (`not_equity_values` → the new rule marker in `gate_detail`) safe only before deploy.
2. **K3 HOLD ambiguity is CONFIRMED on Postgres (row 6)** — 4.6: K3's SQL runs; a HOLD-stamped pre-deploy row reads FAIL. Options unchanged from build2 ESCALATE 6 (accept the loud false positive + the `degraded_sources` procedure already in `expect_text` — recommended; or a row-level discriminator = schema change; or revert `b327221`'s RETAIN grading). Desk's decision; nothing built.
3. **The movers fixtures and `membership-day.real-shape.json` are now different sessions** (movers = 09-19 exports, membership = the 09-14 anchor day): 14 tests across `test_replay_movers.py`/`test_replay_runner.py`/evidence had their pinned tickers re-pointed at real rows that still carry each case (e.g. the "Tesla-class" mover is now a rank-2 gainer with no membership episode at all). Coherence would need a membership re-cut from 09-19 = a production DB read this seat cannot make. Desk's call.
4. **`"v": 152` is the same L3 duplicate** in `radar/throttle.py` and `radar/propose.py` beside `config.export.v`. Not fixed (out of the ruled scope).
5. **For the deploy prompt:** (a) the replay `job.result` shape gains `movers_by_side` (commit `0443112`); until the first `com.cobalt.replay` run after deploy, K9.2/K9.5 read FAIL and K9.3/K9.6 ERROR (RED) — a smoke run before that night is red by design; (b) `radar/throttle.py`'s probe now takes the column declaration from the radar config; (c) `com.cobalt.replay` bootstrap once; (d) restart list = aset + radar; (e) migration proof budget = production's own 104 s × 2 (dev's 12 s is not a prediction; 0008 + 0009 add no `bars` rows).
6. **`01-p4-build.md` step 2's builder launch shape cannot run as written** (`--allowedTools` is variadic: the trailing prompt is swallowed, `claude -p` exits 1 in 0 s — happened once here, nothing ran). Every builder in this run used the prompt-first shape with the same 17-rule list. Also the cutter's bare no-argument run (prompt 2.5) re-cuts every fixture and needs raw inputs that no longer all exist; the new `movers` mode was used instead.
7. **L53 record:** two Finviz `/export/screener` requests at ≈09:28–09:29 ET, Saturday, radar `idle:overnight` (the prompt's statement; archiver not running); status 200 both, no retry. 11,640 rows × 151 columns each. Nothing else touched Finviz.
8. **DevDocs gaps for the close:** no page yet for `tests/cobalt/test_p4_asset_type_evidence.py`, `test_replay_movers.py`, `test_smoke_k3_sql.py` (`docs/40 - DevDocs/tests/` has 40 other test pages). The cutter's page was written.
9. **Housekeeping:** main moved after the rebase by desk docs-only commits (`919d562` → `a89105f`), so `jobs restarts main..HEAD` shows desk docs as `D`; P4's range is `919d562..HEAD`. The two sibling cutters `_cut_p2_fixtures.py`/`_cut_panel_fixtures.py` mention the anchor-month string in comments, so the bare `grep -rlE '2026-09-'` over the fixture dirs lists them (no fixture data; both movers fixtures = 0 hits).
10. **`.env` cycles:** three (step 4 main run; FIX-DB re-proof; `cobalt validate`), each `cp` by name, never read, `rm` + `ls -la .env` proven after each; no builder ran while it was present.
Carried, untouched, one line each (build2 numbering): **C7** FY-3 changes `inputs_sha256` for card misses computed from now on — safe only while `"user".missed` is undeployed; `not_equity_values` was NOT changed here (item 1 not built), so only FY-3's half applies to the deploy prompt · **C9** E2's receipt reference is content-addressed `(membership_id, trade_def_md5)`, not a row id — the receipt-reference tribunal's question (L52 seam clause) · **C5** 0001's schema-wide grant ran before 0002 moved tables: `cobalt_user` has SELECT on none of `bars`, `cobalt_jobs`, `cobalt_kill_switch`, `cobalt_redactions`, `cobalt_email_sends`, `session_blocks` — migration-level design, ops item · **C10** tests that inject a module via `sys.modules` alone can pass for the wrong reason (`import a.b.c as x`) — sweep · **C11** replay window seam (formation/candidate horizons are session close) — design, values session · **C12** L53 gate scores declared windows, not wall-clock — design question.
(That is 10 new + 6 carried = 16.)

MEMORY: the Finviz `Asset Type` column is filled only for funds and even then not always — 16 of ~5,700 fund tickers in the 09-14…09-19 sample were blank while `Industry = Exchange Traded Fund` held for all; a stock never carries a non-blank value [stated 2026-09-19 · Code, evidence `s2-p4-verify-2026-09-19.md` §2.2].
MEMORY: builder launch shape — put the `claude -p` prompt BEFORE `--allowedTools` (variadic option swallows a trailing prompt) [stated 2026-09-19 · Code].
RULING (proposed, for the desk): none.

READY 4cc6859 (code tip a59ee8c; one docs-only commit fills this sha in) | rebased onto 919d562 (main now a89105f, docs-only since) | offline 1796/0 | db 2114/0 | cobalt_dev: 0001–0009 | rollback round-trip ok | Asset Type ESCALATED | rows 2 built · 4 built · 6 built (K3 SQL ran, HOLD ambiguity confirmed → ESCALATE 2) · 8 built | RESTARTS: com.cobalt.aset com.cobalt.radar (+ bootstrap com.cobalt.replay) | OWED: three-house check, receipt-reference tribunal, deploy prompt | ESCALATE: 16

## FUND RULE (R16)
Hub `p4-fund-rule-0919` (Sonnet 5), 11:15–11:18 ET, prompt `prompts/2026-09-19/24-p4-fund-rule.md`. **Headline: FAILED at step 3, before any build — nothing built, no file under `src/`/`configs/`/`tests/` touched, no builder launched, no `.env` copied, no Finviz request, no DB touched. The chunk as written cannot be built inside its own bounds; ESCALATE 17 names the fork.**

### Authorization + preflight
| rule | command | exit | result |
|---|---|---|---|
| launch-line strings vs `13-p4-verify.md` line 1 | compared by hand (`comm`/`sort` are not on this allowlist) | — | every string of this launch line appears byte for byte in 13's; none missing → no mismatch |
| `date*` | `date` | 0 | `Sat Sep 19 11:15:52 EDT 2026` |
| `git status*` | `git status --porcelain` / `git status` | 0 | empty; "nothing to commit, working tree clean"; no rebase |
| `git log*` | `git log --oneline -1` | 0 | `d5947e7` (as the prompt expected) |
| `git -C … status*` / `log*` | main checkout | 0 | ` M docs/40 - DevDocs/reports/seat-usage.md` (desk's, untouched) · tip `5ab083e` |
| `ls *` | `ls -la scratch/movers-{gainers,losers}-raw.csv` | 0 | 11,481,555 bytes each, Sep 19 09:29 — no new Finviz call needed |
| `claude -p *` | Opus meter probe | 0 | `OK` (L47 clear) |
| `uv run pytest *` | `--co -q tests/cobalt/test_replay_movers.py` | 0 | 27 collected |
Denials: none. Reads done: LAWS.md in full, INDEX, `## NOW`, `cto-2026-09-19.md` R16, this report §2.2–§2.5 + ESCALATE 1, `radar.yaml`, `radar/config.py`, `radar/runner.py:144`, `replay/movers.py`, `replay/models.py`, `replay/runner.py:310-360`, migration 0008.

### Facts checked (§1) — all match the prompt except one, the blocker
- `radar.yaml:22-24` and `NotEquityConfig` (`radar/config.py:81-84`) as described; `radar/runner.py:144` inline comparison as described; `movers.py:159` (`config.not_equity.header` picks the raw column), `movers.py:347` (the test), `movers.py:328` (`not_equity_values` param), `movers.py:385` (`not_equity_values` in the stored `inputs`); `REQUIRED_HEADERS` `movers.py:83`; `export.required_headers` has no `Industry`. Also a third reader the prompt did not name: `replay/runner.py:353` passes `deps.radar_config.not_equity.values` into `benchmark_misses`.
- `ls /Users/cobalt/cobalt/data/radar-cache/*/movers-*` → no match (empty). No retained production movers export exists, so the `unreported` label question of step 4 would come out as the prompt expects.
- **BLOCKER (step 3):** the prompt says `MoverRow` gains `industry` "read at parse time … the same way `asset_type` is read", and `benchmark_misses` then applies the one evaluator. But `benchmark_misses` does not run on `MoverRow`; it runs on `StoredMover`, which `MoversStore.active()` reads back from `system.movers_daily` (`movers.py:484-498`, `replay/runner.py:333-353`, `models.py:373-387`). `movers_daily` (migration 0008, lines 17-32) has `asset_type` but **no `industry` column**, and `grep -rn industry` over `src/cobalt/replay` and migrations 0008/0009 returns nothing. On every non-dry-run night the benchmark therefore cannot see `Industry`; adding a field to `MoverRow` alone changes nothing at the test site. Carrying it through the DB needs a NEW migration (0010 + rollback, `FORWARD`/`REVERSE` in `db_migrations/__init__.py`, `placement.py`, `MoversStore._COLUMNS`/`reconcile`, `StoredMover`) plus a dev-DB migrate/validate proof — outside this chunk's stated bound ("touches no migration and no DB-facing code", step 7; the diff-stat bound in step 9 lists no migration path) and outside this launch line, which deliberately dropped `COBALT_ENV=dev uv run cobalt db migrate*` and `… validate*` (13's had them). A half-build would leave the branch tip red (config `values` removed while `movers.py` still reads it), or leave R16 applied by radar but not by the benchmark (the L3 split the chunk exists to close). Stopped per "YOU CAN ALWAYS STOP".

### ESCALATE 17 — how does the benchmark see `Industry`? ASK DESK [11:17] — RESOLVED — option B built (retry `p4-fund-rule-0919b`, see `### Retry (0919b)` below)
The rule R16 "C", the config shape (`asset_type_header` / `industry_header` / `industry_values`), the evaluator, both `required_headers` lists, tests and the counts re-run stay exactly as `24-p4-fund-rule.md` specifies. Only step 3's movers half needs a ruling. Two ways, both keep ONE evaluator (L3):
- **B (recommended; offline-only, fits this launch line): no schema change.** `movers_step` (`replay/runner.py:311-353`) already holds the parsed `exports` in memory on both the live and the retained path. `parse_movers` evaluates the one function on the raw row and `MoverRow` carries `industry` plus the verdict; the step hands `benchmark_misses` the verdicts by `(side, ticker)` (replacing `not_equity_values`), and the receipt `inputs` records each mover's `asset_type`, `industry` and verdict plus a rule marker in place of `not_equity_values` (L57: the receipt is where the deciding inputs are stored; `reconcile`'s idempotence already keys on `export_sha256`, so a changed `Industry` cannot hide behind unchanged stored columns). `system.movers_daily` unchanged. Same permissions as this launch line; nothing new for the deploy.
- **A: persist it.** New migration 0010 adding `system.movers_daily.industry` (`ADD COLUMN IF NOT EXISTS`, idempotent; 0008 is applied on `cobalt_dev` so it must not be edited), `StoredMover`/`MoversStore` carry it, `benchmark_misses` takes `NotEquityConfig` as the prompt wrote. Truer to "the row carries its inputs", but it is a new write-path migration (L29), needs a launch line with 13's `db migrate*` / `validate*` / `.env` cp+rm rules, and adds a migration to P4's deploy plan and to its migration proof budget.
No file was changed for either. Relaunch shape: the same prompt with step 3's movers half replaced by the chosen option (B needs no new permission).

MEMORY: `benchmark_misses` reads `StoredMover` from `system.movers_daily`; any new per-mover input the benchmark must decide on has to be persisted (migration) or passed in memory from the parsed exports [stated 2026-09-19 · Code, `replay/runner.py:333`, migration 0008].
RULING (proposed, for the desk): none — the fork is ESCALATE 17.

Housekeeping: `.env` never copied this run (`ls -la .env` → `No such file or directory`, 11:17); `scratch/` not staged; no Finviz request (L53: none this run).

FAILED: step 3 — `benchmark_misses` reads `StoredMover` from `system.movers_daily`, which has no `industry` column; carrying `Industry` needs a migration (out of this chunk's bound and allowlist) or a redesign the desk has not ruled — ESCALATE 17, nothing built (superseded by the retry below)

### Retry (0919b)
Hub `p4-fund-rule-0919b` (Sonnet 5), 11:23–12:00 ET, prompt `prompts/2026-09-19/24b-p4-fund-rule.md` (step 3 = option B, decide at ingest; steps 1-2 and 4-9 = `24-p4-fund-rule.md`). Builders: two headless Opus 5 runs (`claude -p`, L50/L29): FR-1 (evidence count + fixture row), FR-2 (the rule). **Headline: BUILT. Two commits `db1c095` + `2738cdc`; offline 1818/0, db 2136/0; dropped 131,348, stock rows hit 0; RESTARTS `com.cobalt.aset com.cobalt.radar`; no migration, no Finviz request; ESCALATE 4 new (18-21).**

**Authorization + preflight.** Launch-line strings: `comm -23` of 24b's rule strings against 13's and 24's launch lines prints only 24b's own prompt line (the `Read '…24b…'` argument); every rule string is in both. Preflight, all allowed, 0 denials: `date` (11:23) · `git status --porcelain` empty · `git status` clean, no rebase · `git log --oneline -1` `e79d55d` · main checkout tip `4177649`, its own dirty file = the desk's `seat-usage.md`, untouched · `ls -la scratch/movers-{gainers,losers}-raw.csv` 11,481,555 bytes each · `claude -p` Opus probe `OK` (L47 clear) · `uv run pytest --co -q tests/cobalt/test_replay_movers.py` 27 collected. Read: LAWS.md in full, INDEX, `## NOW`, `UNATTENDED-LAUNCH.md`, `cto-2026-09-19.md` §4 R16, this report §2.2 / ESCALATE 1 / 17, both prompts. Facts re-checked: `ls /Users/cobalt/cobalt/data/radar-cache/*/movers-*` → no match (no retained movers export, so `unreported` keeps a row-level meaning only); `reconcile` keys each side by ticker, so a duplicate (side, ticker) could never have been stored — the new lookup makes it loud instead.

**What was built (old → new).**
| item | change |
|---|---|
| config shape | `not_equity: {header, values}` → `{asset_type_header, industry_header, industry_values}`; the old shape is refused by name (`extra="forbid"`, five keys named in the error); `load_config` requires both headers in `export.required_headers`; `radar.yaml` `required_headers` + `Industry`; comment block rewritten with the evidence counts |
| one evaluator | `is_not_equity(row, config)` in `cobalt.radar.config` = `asset_type non-blank OR industry in industry_values`; called by `radar/runner.py:144` and, through a two-key mapping (`mover_is_not_equity`), by the movers benchmark. `industry_values` is compared in exactly one place |
| movers at ingest | `MoverRow.industry`; `parse_movers` reads both headers unconditionally; `not_equity_verdicts(exports)` builds `(side, ticker) → export row` ONCE in `movers_step` before `export_counts`/`reconcile`/`archive_movers`, passed to `benchmark_misses` on the dry-run AND live paths; `benchmark_misses(…, not_equity=<config>, verdicts=<lookup>)`; a lead with no verdict → `ReplayInputError` naming ticker/side; a duplicate (side, ticker) → `ReplayInputError` |
| receipt (L57) | miss-row `inputs` carry per mover `asset_type` + `industry` (from the verdict, never from the stored row), `not_equity_rule: "asset_type_or_etf_industry"` and `not_equity_industry_values` (the one addition: the marker names the rule, the list is the value it compared — without it a receipt could not replay its own decision if the value changes); `not_equity_values` is gone; `gate_detail` gains `industry` |
| headers | movers `REQUIRED_HEADERS` = `Ticker, Change, Volume, Asset Type, Industry`; a missing column is a FAILED parse naming it; `unreported` kept, meaning a blank CELL on a present column |
| NOT touched | `system.movers_daily`, `StoredMover`, `MoversStore`, every migration, `db_migrations/*` |

**Grep proofs** (builder's, re-read by the hub against the diff): `grep -rn "not_equity" src/cobalt` → the radar call site (`radar/runner.py:144` `is_not_equity(row, self.config.not_equity)`), the movers call (`replay/movers.py:437` → `mover_is_not_equity` → `is_not_equity`), `replay/runner.py:332/365` (verdicts + call); `grep -rn "industry_values" src/cobalt` → exactly the config field (`radar/config.py:97`), the evaluator (`:117`), the receipt line (`replay/movers.py:486`); `grep -rn "not_equity_values" src` → no hit.

**Tests** (all red first; builder reports `scratch/fr1-builder-report.md`, `scratch/fr2-builder-report.md`, not committed). FR-1: 5 new (`test_p4_asset_type_evidence.py`; red = missing attribute / no blank-type fund row in the fixture) + 12 assertions restated for the 61-row fixtures (rank 61 = the appended real row per side; no assertion weakened). FR-2: 17 new + 7 rewritten in the named files — `test_radar_config.py` (6 new: shipped shape, old shape refused naming its keys, both headers required, comment block carries the evidence, evaluator strips/reads both, empty header/value list refused), `test_replay_movers.py` (9 new, 6 rewritten: the four rule cases, the C(i) arm counted and printed (0 of 142 real-shape rows), industry read on every row, either header missing is loud, verdict lookup + duplicate refused, a `StoredMover` list that LACKS `Industry` still drops the blank-type funds (the round-trip case the prior plan would have missed), a staled stored `asset_type` decides nothing, no-verdict is loud, receipt inputs), `test_replay_runner.py` (1 new: dry-run and live reach `benchmark_misses` with the same lookup), `test_radar_replay.py` (1 new, 1 rewritten: acceptance matrix; the radar call site on four verbatim real rows). `test_radar_pool.py::test_inactive_degraded_and_not_equity_precedence` checked, unchanged (never reads the radar config). Rule cases on REAL rows: non-blank type + fund industry; BLANK type + fund industry (the FR-1 rows); blank type + non-fund industry. Non-blank type + non-fund industry has no real row anywhere (0 of 142; 0 on the full sample) — tested as a boolean on a mapping, no row fabricated, and the count is asserted so a future non-zero fails a test.

**Counts (step 6, hub re-ran `uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence` after the fix commit):** `files: 16307 (without the columns: 0); rows: 643998 (without the columns: 0); distinct fund tickers: 5702; distinct Asset Type values: 28; table C total: 16; dropped under the new rule: 131348; stock rows hit: 0`. Table A rows 131,332, table B rows 131,348, table C(i) 0 → A ⊆ B, so A ∪ B = 131,348 = `dropped_under_new_rule`; the 16 extra rows are table C(ii) (8 tickers, blank `Asset Type`). Stock rows hit = 0.

**Suites (step 7).** Offline `uv run pytest -q tests/cobalt tests/taxonomy` → `1818 passed, 324 skipped, 1 xfailed, 15 warnings in 55.92s` (0 failed; 1796 before, +22 = the new tests). DB re-run (`.env` copied by name, never read; no builder was running) `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → `2136 passed, 6 skipped, 1 xfailed, 15 warnings in 188.89s` (0 failed; 2114 before). `.env` removed, `ls -la .env` → `No such file or directory`, `git status --porcelain` empty. `cobalt_dev` after: unchanged, `0001–0009` (no migration ran; the suite creates and drops its own test databases as before).

**RESTARTS (step 8).** `uv run cobalt jobs restarts e79d55d..HEAD` (this chunk): `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED (`configs/cobalt/radar.yaml` → resident reads radar; `radar/config.py`, `radar/runner.py`, `replay/models.py`, `replay/movers.py` → static import reach aset + radar; `replay/runner.py` → radar). `main..HEAD` (whole branch) gives the same line; it also lists `ops/com.cobalt.replay.plist` A → bootstrap once `com.cobalt.replay`, and desk docs as `D` because main moved (P4's range is `919d562..HEAD`).

**Commits.** `db1c095` `test(fixtures): dropped-under-new-rule count in the evidence mode` (6 files: cutter, evidence test, two movers fixtures +1 row each, two test files' 61-row bookkeeping) · `2738cdc` `fix(s2-p4): not-equity for movers decided at ingest from the parsed export, never through the movers_daily round trip (R16 "C", ESCALATE 17 option B)` (16 files, 684+/77−). ONE commit for the whole rule rather than two: the config shape, the evaluator and the movers half cannot be split without a red tip (the movers parse reads the renamed config keys). `git show --stat` of both lists no `scratch/` path. `git diff --stat` bound: the files 24 named plus `src/cobalt/replay/runner.py`; outside it: the four files of ESCALATE 18, plus `tests/cobalt/test_radar_config.py` and `tests/cobalt/test_replay_runner.py`, which 24 does not name but where the new config and runner tests naturally live. The stop line's sha is the code tip; one docs-only commit (this report) follows it.

**ESCALATE (4 new; 17 resolved above).**
18. **Four test files outside the bound edited (accepted by the hub, revert on request):** `test_radar_collector.py`, `test_radar_daily.py`, `test_radar_evaluate.py`, `test_radar_runner.py` — 7 tests whose synthetic screen rows set `Asset Type: "Stock"` (a value Finviz never returns; it leaves the cell blank) and no `Industry`; R16 makes both a red. Only fixture rows changed (blank type + `Industry: Capital Markets`), no assertion weakened, none deleted. The hub also restated two stale docstring sentences (`test_p4_asset_type_evidence.py`, `_cut_p4_fixtures.py`) that still called the rule unbuilt. 
19. **L32 — real tickers pinned in tests.** The rewritten/new movers and radar tests locate real fixture rows by ticker (31 occurrences of the three new pins in `test_replay_movers.py`, 7 in `test_radar_replay.py`, 3 in `test_replay_runner.py`), as the pre-existing tests already did (e.g. the "Tesla-class" mover, 14 quoted ticker literals before this chunk). The prompt says "counts only in anything committed"; this report and the commit messages are counts-only, the test files are not. Desk's call: accept the existing convention (the fixtures are real-shape rows with tickers in them anyway), or a follow-up that finds rows by predicate. Nothing changed for it.
20. **DevDocs stale for this chunk (sprint close):** pages exist for `radar/config`, `replay/models`, `replay/movers`, `replay/runner`, `tests/fixtures/replay/_cut_p4_fixtures` and are now behind (`is_not_equity`, `not_equity_verdicts`, `mover_is_not_equity`, the new config keys, the evidence count). Not regenerated here (CLAUDE.md: DevDocs are generated at sprint close). Still no page for `test_p4_asset_type_evidence.py`, `test_replay_movers.py`, `test_smoke_k3_sql.py` (ESCALATE 8).
21. **For the deploy prompt:** (a) `com.cobalt.radar` restarts (config-shape change + import reach), inside the 20:00–21:00 ET pause or overnight idle (L43); the one config file that changed shape = `configs/cobalt/radar.yaml`, and `com.cobalt.aset` also imports `radar.config`, so it restarts with it (L42, one action); (b) the live radar collector now requires `Industry` in every screen export (`required_headers`) — `export.columns: "0-150"` carries it (evidence: 151 columns, `Industry` present in all 16,307 files), so no config change is owed, but a screen whose columns are narrowed by the vault pool block would fail loud; (c) mover-miss `inputs_sha256` changes for rows computed from now on (`not_equity_values` gone; `not_equity_rule`, `not_equity_industry_values`, per-mover `industry` added) — safe only while `"user".missed` (0009) is undeployed, extends carried C7; (d) the live effect of R16: the radar now drops every non-blank-`Asset Type` row and every `Industry = Exchange Traded Fund` row, i.e. the 5,702-ticker class plus the 8 blank-typed funds; on the evidence sample it drops 0 stock rows.

MEMORY: the movers benchmark decides not-equity from the parsed export rows at ingest (`not_equity_verdicts`), never from `movers_daily` — that table stores no `industry`; any new per-mover input the benchmark must decide on is either passed in memory from `exports` (as here) or persisted by migration [stated 2026-09-19 · Code, `replay/movers.py`, `replay/runner.py`, migration 0008].
MEMORY: R16 "C" is built: not-equity = non-blank `Asset Type` OR `Industry = Exchange Traded Fund`, one `is_not_equity` in `cobalt.radar.config`; 0 stock rows hit on 643,998 rows / 16,307 files; the 8 blank-typed funds are caught by `Industry` [stated 2026-09-19 · Code, `s2-p4-verify-2026-09-19.md` §Retry (0919b)].
RULING (proposed, for the desk): ESCALATE 18 accept or revert the four out-of-bound test files (recommended: accept; each is a fixture value Finviz never returns); ESCALATE 19 accept the ticker-pinning convention.

P4 FUND RULE 2738cdc | offline 1818/0 | db 2136/0 | dropped 131348 kept n/a | stock rows hit 0 | RESTARTS: com.cobalt.aset com.cobalt.radar
