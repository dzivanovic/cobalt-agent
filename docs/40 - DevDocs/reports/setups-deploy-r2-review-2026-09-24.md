# Setups Deploy R2 Review — 2026-09-24 (`10` read of `09-setups-deploy-r2.md`)

## §0 Headline
- 2 of 2 houses answered: Grok `REVIEW: RUN IT AFTER` 2 folds (the L67 floor, met), Opus 5.5 `REVIEW: RUN IT AFTER` 3 folds. `09` = commit `4ff1f1ad30408fb78b5f84e3dec67e277b6f1f2e` (working file = committed = staged, `cmp`).
- Mechanical checks clean: 51 allows + 3 denies of `09` each count 1 in `66` (0 NEW; 55 quoted strings, the one 0 is the prompt path); the 4 STEP-6 rows equal the engine rows and R119 / R82; every authorization literal `09` greps for is on his real rows today; all 8 of `06`'s folds landed in `09` as written (quoted with line numbers).
- Both houses found the same two RELAUNCH-only text gaps (BETWEEN + the desk's commit hold; relaunch (i) on an interrupted `-m 2` revert). Opus's 6.8 harm claim and its 6.7 "contradiction" DO NOT HOLD against the files.
- Blockers: 0 under the mechanical rule stated under `## Checked against the files`. Folds proposed: 5. ESCALATE: 4.

## L74
One block arrived inside a tool result: appended to the Read of this run's own prompt file (`10`), a system-reminder-shaped block asking that commit messages and PR bodies end with a `Claude-Session: https://claude.ai/code/session_…` line and that a file be sent with a file-send tool. DATA under L74 — recorded once here, never followed. This hub commits nothing and sends no file.

## PREFLIGHT
| rule | command | exit | allowed/DENIED + reason |
|---|---|---|---|
| PLACEHOLDER GATE (1st call) | `grep -n -E "R_[_]" ".../prompts/2026-09-24/10-review-setups-deploy-r2.md"` | 1 | ALLOWED — printed nothing (exit 1) |
| DATE GATE (1st) | `date` | 0 | ALLOWED — `Thu Sep 24 07:33:41 EDT 2026` (2026-09-24) |
| DATE GATE 09-24 → R17 | `grep -n "^| R17 " cto-2026-09-24.md` · `git log -1 --format=%H -S"10-review-setups-deploy-r2.md" -- cto-2026-09-24.md` | 0 | ALLOWED — line 25: his words ("Why do we ask for Grok every time? … Grok approved with no asking going forward …"), names `10-review-setups-deploy-r2.md`, carries `Bash(grok *)` and `2026-09-24`; commit `1758fd78a572f47b613b2ca831dcfa636ed8f65a` (NON-EMPTY) |
| AUTH THIS LAUNCH (R18) | `grep -n "^| R18 " cto-2026-09-24.md` · same `git log -S` | 0 | ALLOWED — line 26 names `10-review-setups-deploy-r2.md` (DESK LAUNCH ROW); commit `1758fd78…` (NON-EMPTY) |
| AUTH R32 | `grep -n "^| R32 " cto-2026-09-22.md` · `git log -1 --format=%H -S"Bash(claude -p --model claude-opus-5-5 *)" -- cto-2026-09-22.md` | 0 | ALLOWED — line 131 carries the string; commit `b8a72b5300370e248cd6c7a8a732258fec03e6a0` (NON-EMPTY) |
| AUTH R95 | `grep -n "^| R95 " cto-2026-09-23.md` | 0 | ALLOWED — line 103 prints the seats row |
| `grok --version` | `grok --version` | 0 | ALLOWED — `grok 1.0.25 (f7e67d6988e2) [stable]` |
| OPUS PROBE | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (background, task `b7ag2nenf`) | 0 | ALLOWED — `OK` = UP |
| 09 exists | `ls -la ".../prompts/2026-09-24/09-setups-deploy-r2.md"` | 0 | ALLOWED — 110,220 B, Sep 24 07:15 |
| 09 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -- ".../09-setups-deploy-r2.md"` | 0 | ALLOWED — `<prompt sha>` = `4ff1f1ad30408fb78b5f84e3dec67e277b6f1f2e` (Sep 24 07:21:05 ET). Extra: `git show 4ff1f1ad:<path>` `cmp` the working file → IDENTICAL |
| STAGGER | `grep -n -F "no other house hub is running" cto-2026-09-24.md` | 0 | ALLOWED — lines 13 (R5), 19 (R11), 26 (R18); line 26 also names `10-review-setups-deploy-r2.md`: `stagger: clear (launch row)` |
| RECOVERY | `ls scratch/tribunal-bars-0920/setups-deploy-r2-0924` | 1 | fresh run — "No such file or directory" |
| DATE GATE (2nd) | `date` + R17 grep + `git log -S` | 0 | ALLOWED — `Thu Sep 24 07:41:19 EDT 2026` (2026-09-24); R17 line 25 present; commit `1758fd78…` NON-EMPTY |

Helper commands the hub used outside the launch list, all read-only or scratch-only (no dialog met): `sed`, `awk`, `cut`, `sort`, `comm`, `cmp`, `cat`, `cp` (two report files into the packet folder), `iconv`, `zsh <script in the job tmp dir>`, shell redirection into the packet folder, `git -C … status --short --branch` (main and the three worktrees, read-only), `grep -rn` over `setups-c1/src` and its scratch, `ls`. Nothing under `/Users/cobalt/cobalt` except this report file was written. A mistaken `cd` into `docs/40 - DevDocs/reports` (read-only use) was made once and left no change.

## Packet
Staged in `scratch/tribunal-bars-0920/setups-deploy-r2-0924/` (the first Write, `QUESTIONS.md`, created the folder; no `mkdir` run). DEVIATION FROM THE STAGING RULE, disclosed as `06` did: the parts of `09`, `05`, `66` were cut with `awk` at line boundaries (`LC_ALL=C`, balanced), NOT Read → Write, because `09` part1 lines 1 and 7 (2,476 / 3,309 B), `05` lines 1 and 7 and `66` lines 1 and 6 exceed 2,000 B. Byte identity PROVEN: `cat <parts in order> | cmp - <source>` → IDENTICAL for `09`, `05`, `66`; `cmp` IDENTICAL for `review-06.md` and `draft.md` (`cp`). Every part ≤ 38,000 B.
- `09-setups-deploy-r2.part1.md` 27,454 B (lines 1–81) + `part2` 27,482 (82–200) + `part3` 27,617 (201–297) + `part4` 27,667 (298–426) = 110,220 B = source (110,220 B).
- `05-base.part1.md` 30,792 (1–129) + `part2` 31,279 (130–260) + `part3` 31,467 (261–411) = 93,538 B = source `05-setups-deploy.md` (93,538 B).
- `66-base.part1.md` 27,120 (1–108) + `part2` 26,780 (109–223) + `part3` 27,904 (224–376) = 81,804 B = source `66-stacked-deploy-r4.md` (81,804 B).
- `review-06.md` 23,667 B = source (23,667 B) · `draft.md` 15,084 B = source `setups-deploy-r2-draft-2026-09-24.md` (15,084 B).
- `branches.md` 11,899 B · `stop-lines.md` 18,671 B (the 12 `tail -n 3`s, 3 greps; all files present) · `rulings.md` 29,583 B (09-23 R4, R52, R81, R82, R83, R95, R104, R106, R107, R113, R114, R118; 09-22 R30, R32, R119; `cto-2026-09-24.md` §4 lines 6–27, R1–R18) · `laws.md` 23,055 B (L28, L42, L43, L54, L62, L63, L65, L66, L68, L73, L76) · `rows.md` 15,083 B · `restarts.md` 5,511 B · `clock.md` 5,585 B.
- `greps.txt` 36,312 B (HUB ADDENDUM = the 55 quoted strings of `09`'s launch line one per line, then searches 1–4) · `greps-2.txt` 31,352 (5–7) · `greps-3.txt` 36,001 (8–11) · `greps-4.txt` 32,796 (12–16) · `greps-5.txt` 33,671 (17–20). 20 searches, one `grep -n` call each over the four `09` parts. RULES APPLIED (also stated inside `greps.txt`): a hit line over 1,250 B (six lines of `09`) is named, not reprinted; searches 8, 11, 14, 18, 19 (each over 17 KB in full) are given as `grep -c` per part plus part:line and the first 110 characters of every hit; search 15 (`FAILED`, 97 hits) as `grep -c` and hit line numbers only. Pattern headers: an early `echo` turned `\b` into a backspace in the stored pattern text; repaired and the raw outputs re-run (byte-identical, `cmp`); the greps themselves always ran with `\b`. One search-18 gist line was cut mid-character and repaired with `iconv -c`.
- `QUESTIONS.md` 8,874 B = the prompt's text verbatim (`diff` of `10` lines 145–171, outer quotes removed → identical) plus ONE appended "Files in this folder:" paragraph that names `greps.txt` to open SECOND.
- Packet the houses read: 612,706 B in 25 files (≈153k tokens; the prompt's "≈330 KB" was the five whole sources: 324,613 B). Trailing-whitespace lines (`grep -c " $"`): 0 in `09`, `05`, `66`, `draft`, `review-06`; a few in the generated files, all inside verbatim command output.
- Every house got the identical packet (L44).

Houses (one attempt each, `run_in_background`, both launched 07:41:2x ET right after the second date gate 07:41:19):
- GROK (`grok -p "…" --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)"`, task `bg37zq39v`): notice read 07:56:38 ET (≈15 min, inside the 20-min limit), exit 0, closing `REVIEW: RUN IT AFTER …`. Grok wrote no file; the hub wrote `grok-review.md` (8,920 B) from its printed answer, lines 1–24 of the task output byte for byte (line 1 begins with Grok's own process narration).
- OPUS (`claude -p --model claude-opus-5-5 "…" --permission-mode plan --add-dir … --allowedTools "Read" "Grep" "Glob" --disallowedTools …`, task `b790ixyv4`): notice read 07:49:24 ET (≈8 min), exit 0, closing `REVIEW: RUN IT AFTER …`; written to `opus-review.md` (12,433 B) byte for byte from the printed answer (lines 1–107 of the task output).
- **L67 floor: MET** — Grok (a non-author house) answered in full with a `REVIEW:` line. Sol NOT SEATED (METER, retry after Sep 26th 06:47 ET); Gemini NOT ASKED (R96 / R97, L67 as amended); Astra a design seat. No harness failure, no meter message, no timeout.

## CONTINUE
next: none — the desk commits this report (`09` P9 requires it COMMITTED), folds the rows below it takes (a re-issued `09` under L19), fills `R__L`, re-cuts the gate on the mover tip (`stacked-0923` is at `4e4577c3` now), holds its commits, and launches `09` after his `DONE TRADING` word.

## Per question
Cells are each house's own words (≤30), cited as the house cited them. Grok's citations are `09` part-local lines (part1 +0, part2 +81, part3 +200, part4 +297); Opus cites source lines.
| Q | grok | opus |
|---|---|---|
| Q1 Allowlist | "YES. Every step command matches one of the 51 allow strings … 0 new. No allow string is unused"; cut on the mover tip removes the merge string (`part1.md:4`, `:7`, `:9`; `part2.md:110–113`) | "YES." 51 allows = `66`'s minus 3, same order; no unused string; nit: `09:109` "TWO `-m`" vs the `-m 2` revert, harmless (`09:7`, `:9`, `:109`, `:347`) |
| Q2 L68 gate / two-branch | "YES." rebase → one merge → suites → `.env` removed → one report commit → main merged in → 3.3 → ff at 4.3; 1.2 cannot pass a bad replay (`part3.md:18–87`, `part4.md:12`) | "YES." suites on `<stack>` before the ff; minor: lock not re-read before the `cp` (`09:172`, `:247`); prose: `c212645f` named as the `rules.yaml` commit, `de88c302` is (`09:23`) |
| Q3 Window / migration | "NO — a BETWEEN stop line ends the desk's commit hold, so a held commit moves `main` off `<pre-merge>`"; 4.1 itself deterministic (`part1.md:5`, `part3.md:93`, `part4.md:5`) | "YES." both down before merge + migration, up after; 4.4 complete; gap: STEP-5 "at any hour" may cross 20:30 / 21:10 (`09:303`, `:310–314`, `:343`) |
| Q4 Rollback | "NO — an in-progress `-m 2` revert still has HEAD at `<stack-final>`, so relaunch (i) bootstraps it"; the revert itself, (2), (4), 0013 and the note are safe (`part3.md:93`, `part4.md:49–58`) | "YES for the single-run path; relaunch (i) has a hole": a conflicted / killed STEP-5 revert leaves HEAD at `<stack-final>` (`09:292`, `:298`, `:346–348`) |
| Q5 STEP-6 / R118 | "YES." four rows = his values and the engine rows; no `dist.k.vwap` row; no R107 clause; 6.8 exact; 6.7 fresh-cycle rule; UNVERIFIABLE that `+companion` was at 1 / 0.10 (`part4.md:66–116`) | "YES." rows, 6.8, 6.7 exact; RISK: 6.8 goes red if the companion's values differ from R82 (UNVERIFIABLE); "internal contradiction" 6.7 vs `09:360` (`09:360–413`) |
| Q6 Mover carries | "YES." RESTARTS aset + radar; smoke s2 not run, owed read on the stop line and ESCALATE; seam by 1.3; K17 / `archive_partial_by_side` markers (`part1.md:31`, `part4.md:37`, `:41`) | "YES." same; UNVERIFIABLE whether the 21:10 replay on `s2p2.2` refuses today's `s2p2.1` receipts (`09:16`, `:258–274`, `:338`, `:420`) |
| Q7 Other | "Two sequences, no dialog on the path as written": (1) BETWEEN at 19:56 → desk hold ends → relaunch (ii) ends the run; (2) STEP-5 (2) conflict, session dies, relaunch (i) (`part3.md:92–93`, `part4.md:5`) | DEFECT: DESK LINE ends at the BETWEEN stop line (`09:5`, `:293`, `:303`); minors: relaunch reuses P14 baselines; P11 no seam→ln-base link; P1 docs residual (`09:294–295`, `:183–185`) |
| REVIEW | `REVIEW: RUN IT AFTER DESK LINE: hold commits through BETWEEN relaunch · Relaunch (i): in-progress revert stays DOWN` | `REVIEW: RUN IT AFTER Relaunch (i): git status first; mid-revert → residents stay DOWN · DESK LINE: hold commits through a BETWEEN relaunch · Desk proves R82 values = +companion values before launch` |

Where the houses contradict each other: Q3 and Q4 — Grok "NO" (both on the relaunch cases), Opus "YES" for the single-run path with the same two relaunch defects; settled below (rows 1–2: both HOLD as text, neither on the run's own path). Q5 — Grok calls the companion-value read "not a defect in the text"; Opus calls it a RISK that could red 6.8; settled below (row 3: the values are equal, DOES NOT HOLD).

## Checked against the files
Blocker rule (mine, applied to every row; a mechanical test, as `06` and `69` applied it): `blocks the launch? yes` = the text as written, on the run's OWN single path with the values the packet records (no third-party action, no relaunch, no abnormal input), fails a good deploy, passes a bad one, opens a dialog, or leaves a resident / his vault wrong; `no` = it needs a third party (the desk), a relaunch, an input the measurements do not show, or is wording only.

Files opened: `09` (whole, source), `05`, `66` (launch lines), `06`'s report, `draft.md`, LAWS.md (in full), `cto-2026-09-2{2,3,4}.md` rows, the branches via `git -C /Users/cobalt/cobalt show|log`, `main`'s and the worktrees' `git status`, `setups-c1/tests/cobalt/setups_shapes.py`, `setups-c1/scratch/prints-0923/test_second_chance_print_0924.py`, `docs/60 - Agent Output/inflight-strays-2026-09-23/setups-assumed-values-2026-09-21.md`, `setups-c1/src/cobalt` (grep).

| claim | who | file:line | verdict | blocks? | ≤30 words |
|---|---|---|---|---|---|
| 1. BETWEEN writes a stop line; the desk hold ends at it; a desk commit moves `main` off `<pre-merge>`; relaunch (ii) then ends the run and the 20:00 window is lost | grok Q3, Q7-1; opus Q7 | `09:5`, `:41`, `:293`, `:303`; `draft.md:80`; `git log -5 main` (5 desk docs commits 07:21–07:44 today) | HOLDS (text) | no — needs a relaunch and a desk commit | `09:5` "until `09` writes its stop line"; `:303` BETWEEN writes one, uncommitted; (iv) needs main = `<pre-merge>`; (ii) reads any earlier stop line as "the run already ended" |
| 2. Relaunch (i) matches on HEAD = `<stack-final>` and bootstraps, also for a STEP-5 `-m 2` revert conflicted or killed before `revert --abort` / commit | grok Q4, Q7-2; opus Q4 | `09:291` (the six relaunch calls, no `git status`), `:292`, `:349`, `:298` | HOLDS (text) | no — session death mid-revert plus a relaunch | (i) has no `git status` / REVERT_HEAD read; 349 only handles a live conflict by `revert --abort`; "NEVER a second revert" (298) is then unguarded |
| 3. 6.8 goes red (a good deploy reverted) because `+companion=True` was not at R82's values | opus Q5 | `stop-lines.md:158–162`; `setups-c1/scratch/prints-0923/test_second_chance_print_0924.py:28` `COMPANION = {failed_trap_bars: "1", retest_tolerance_atr: "0.10"}`; companion note `:29–30` (1 bar; 0.10 × `atr_working`); R82 (`rulings.md:11`); `setups_shapes.py:417–418` D6 = 3 / 0.35 | DOES NOT HOLD (as to the values) | no — values are equal | the print's companion = R82's 1 / 0.10; D6's 3 / 0.35 are the constructed test values, a different row. Whether it forms live stays UNPROVEN until 6.8 runs (L70) |
| 4. 6.7 vs `09:360`: a resident reads the note directly, so the rows do reach the radar without `taxonomy load` | opus Q5 | `09:360`, `:406`; grep of `setups-c1/src/cobalt`: callers of `load_vault_trade_defs` / `load_assumed_tunables` only in `taxonomy/cli.py:80,306,565`, `taxonomy/validate.py:93`, `taxonomy/catalyst.py:151,322`, `cards/trail_fit_draft.py:56`; none under `radar/` or `aset/` | DOES NOT HOLD as a contradiction; residual UNVERIFIABLE FROM READS | no | 6.7's clause is conditional ("a resident whose code walks…"); no resident module calls the loader directly. `cobalt validate` (not a resident) does. Indirect reach via `cobalt.cli` import: `grep` cannot prove a runtime call |
| 5. `09:23` names `c212645f` as the `rules.yaml` commit | opus Q2 | `09:23`; `git show --stat c212645f` = `seat-usage.md` only; `git log -- configs/cobalt/rules.yaml` → `de88c302` (09-23 22:05) | HOLDS (wording) | no | no gate uses the sha; P11 pins the 11 paths. The drafter's `draft.md` ESC 2 carries the same wrong sha |
| 6. The `cobalt_dev` lock is read at P7 / P13 and not again before 2.3 (a)'s `cp` | opus Q2 | `09:172`, `:196`, `:247`; DESK LINE `:5` | HOLDS (text) | no | only the desk's hold covers the gap between P7 and the `cp`; today no `.env` exists under any `cobalt-wt` (`ls` "no matches found") |
| 7. STEP-5 "at any hour" can run a code revert across the 20:30 archiver or the 21:10 replay | opus Q3 | `09:343`, `:44`; `clock.md:3–19` | HOLDS as stated by the prompt (a ruled choice, R83), not guarded | no | `09:343` says finish (1)–(5) anyway and let ESCALATE say so; only a failure path reaches it |
| 8. A relaunch resuming at 4.1 under (iii) / (iv) reuses the first run's P14 baselines (`<a0>`, `<ta0>`, `<tr0>`, `<tc0>`) | opus Q7 | `09:207`, `:294–295`, `:327–331` | HOLDS (text) | no — relaunch | a crash or a traceback in the gap would make 4.7 (b) red against a stale count |
| 9. P11 links `<ln base>` `9e775fd6` and `<seam tip>` `51afdad0` by no diff | opus Q7 | `09:183–185` | UNVERIFIABLE FROM READS — `git -C /Users/cobalt/cobalt diff --stat 51afdad0 9e775fd6 -- . ':(exclude)docs'` | no | my `git log` range check sits across a rebase (different shas) and settles nothing; P11's other three diffs and the branch's 74-path count cover the tree |
| 10. Every step command is covered by an allow string; no string is unused | grok, opus | `09:7`; my re-read of `09`'s commands against the 51 strings | HOLDS (no gap) | no | `git -C * status*/log*/diff*/rev-parse*/rev-list*/merge-base*/show*`, `cd *`, the pytest / `COBALT_*` prefixes, `db query *`, `launchctl print gui/501/*`, the curl `\?` all match; `rebase --abort` / `merge --abort` / `revert --abort` / `kickstart` / `reset --soft` each sit on a named branch |
| 11. Cutting the gate on the mover tip removes the need for a `merge --no-edit replay/mover-partial-0924` string | grok, opus | `09:4`, `:193`, `:228`; `grep -c -F 'merge --no-edit replay/mover-partial-0924'`: `66` = 0, `09`'s launch line = 0 | HOLDS | no | the string is in neither pool; P12 requires gate = mover tip, 1.3 fails a fast-forward |
| 12. `09` = `05`'s 50 + one string of `66` | `09:9` | `comm` of the sorted quoted strings of `05` line 7 and `09` line 7 | HOLDS | no | in `09` not `05`: `"Bash(git -C /Users/cobalt/cobalt-wt/stacked-0923 merge --no-edit main)"` (+ the prompt path); in `05` not `09`: only its own prompt path |
| 13. The authorization gates of `09` pass on today's real rows | grok Q7, opus Q7 | `rulings.md`; my `grep -c -F` of each literal `09` P-AUTH names, on the real desk files | HOLDS | no | count 1 each: R83 (`DONE TRADING`, `09-24 only`), R4, R104, R119, R82 (4 strings), R118 (`Let's go with B`, `HOLDS`, `FOUR rows`), R3 (4), R7, R52 (4); `-S` NON-EMPTY: `2028ef79`, `d5b7cf55`, `b33b5634`, `81a226bd`, `54b33bec`, `d327ff17`, `f173328f`; P-HIS regex matches 0 lines today (no word yet); the bare `-S"DONE TRADING"` is non-empty (`5c0a8d3f`) — why fold 4 |
| 14. P5–P8 stop-line gates read true on today's files | grok, opus | `stop-lines.md:1–112` vs `09:156–174` | HOLDS | no | fix-r2 build line equals `09:159`'s exact line; r4 / seam / live-note / mover / blind / devdb lines carry the required fields; `04`, `08` = `YES` ×2 and `HOLD: 0` |
| 15. P1 dirt list against `main` today | grok, opus | `git status --short --branch` (`09:141–151`) | HOLDS | no | first line `## main...origin/main [ahead 44]`; ` M` `rules.yaml`, ` M` `seat-usage.md`; `??` under `docs/40 - DevDocs/{prompts,reports}/` (incl. this report and `.grok-stdout-2026-09-22.tmp`); none staged; none of the ten added report paths; worktrees `setups-c1`, `mover-bars`, `stacked-0923` each one line, no `.env`. Gate at `4e4577c3` (the desk re-cuts before launch: P12 will refuse until then) |
| 16. `09`'s `-m` wording vs the revert | opus Q1 nit | `09:109`, `:347` | HOLDS (wording) | no | line 109 "EVERY commit message carries TWO `-m`" is about commits; the revert spells its own command |

(i) Allow strings — `grep -c -F -e "<string>"` (quotes included) of each quoted string of `09`'s `(3) claude --bg` line (part1 line 7; one per line in `greps.txt`) against `66-stacked-deploy-r4.md`: strings 02–52 (the 51 allows, from `"Bash(git -C /Users/cobalt/cobalt add *)"` to `"Bash(date*)"`) each count = **1**; the three denies `"AskUserQuestion"`, `"EnterWorktree"`, `"Bash(git push*)"` each = **1**; string 01 (the prompt path `"Read '…/09-setups-deploy-r2.md' and follow it exactly."`) = 0, as it is not a rule string. Flags: `--model claude-opus-5-5` = 1, `--permission-mode acceptEdits` = 2, `--add-dir /Users/cobalt/Vault` = 1, `--add-dir /Users/cobalt/cobalt-wt` = 1, `--allowedTools` = 1, `--disallowedTools` = 1. No rule string counts 0 → 0 NEW. `66`'s line 6 has 58 quoted strings (prompt path + 54 allows + 3 denies); `09` = 55; dropped: `s2-smoke-fix rebase main`, `s2-smoke-fix rebase --abort`, `stacked-0923 merge --no-edit s2/smoke-fix-0922` (+ the prompt path), 54 − 3 = 51.

(ii) `grep -c -E "R_[_]" ".../09-setups-deploy-r2.md"` → **5** lines (4, 5, 64, 66, 174: the desk's `R__L`). The desk's fill; not a blocker.

(iii) STEP-6.2's four rows against the engine rows (`rows.md` `tunables.yaml`) and the rulings:
| key | `09` value / unit / scope / consumers (`09:366–400`) | engine row unit / scope / consumers (`rows.md` line) | his ruling |
|---|---|---|---|
| `flat_threshold.ema9` | 0.05 / ratio / `per_indicator(ema9)` / `["fashionably_late"]` | ratio / `per_indicator(ema9)` / `["fashionably_late"]` (393–400) | R119: 0.05 |
| `flat_threshold.vwap` | 0.05 / ratio / `per_indicator(vwap)` / `["fashionably_late"]` | ratio / `per_indicator(vwap)` / `["fashionably_late"]` (402–409) | R119: 0.05 |
| `range_break.failed_trap_bars` | 1 / bars / global / `["Range Break (primitive)", "second_chance"]` | bars / global / `["Range Break (primitive)", "second_chance"]` (157–164) | R82 A-19: 1 bar |
| `range_break.retest_tolerance_atr` | 0.1 / atr / global / `["cobalt.radar.anatomy.range_break (event(retest))"]` | atr / global / `["cobalt.radar.anatomy.range_break (event(retest))"]` (325–332) | R82 A-20: 0.10 × `atr_working` |
All four: `dynamic: true`, `status: proposed` (equal to the engine rows), `source: assumed` (the engine rows say `dwv`, by design: `loader._fills_hole`). `grep -c -F "dist.k.vwap"` of `09` lines 366–400 (the YAML block) = **0**; the whole file = 10 lines, each a prose / expectation line (`hole` expected at 6.1 and 6.6; R118 paragraph; `2.3 (d)`'s pre-STEP-6 AWAITING line). No `KNOWN RED` / `EXPECTED RED` remains: the only `R107` strings are `09:56`, `:361`, `:413` ("superseded"); the only "five" is `09:254` (pre-STEP-6 AWAITING set) and `:59` ("R3 named FIVE rows; the FOUR written here are a subset").

(iv) `review-06.md`'s `## Folds proposed` 1–8 against `09`:
| fold | where it landed in `09` |
|---|---|
| 1 BETWEEN: no report commit | `:303` "On the BETWEEN FAILED write the stop line and do NOT commit the report (`main` must stay `<pre-merge>` for RELAUNCH (iv))"; also `:41`, `:68`, `:108`, `:423` (draft.md's table cites 42 / 111 / 70 / 435: stale numbers, not a defect in `09`) |
| 2 (i) before (vi); 6.4 idempotent | `:292` "…unless (vi)'s condition also holds: then (vi) applies first."; `:403` "no change because the four rows are already there = WRITTEN (relaunch, (vi))"; `:404` 6.5 runs once |
| 3 PAUSE merge clock | `:40` "MERGE CLOCK = **20:28 ET minus P14-M's proof cost in seconds, never later than 20:22 ET**"; `:212` records `<pause merge clock>` |
| 4 P-HIS `-S` with the time | `:135` `-S"DONE TRADING <hh:mm>"`, with the reason ("the bare `-S"DONE TRADING"` is already non-empty … `06` row 6") |
| 5 2.3 (a) waits | `:247` "(a) ONLY after 2.2's `<task-notification>` has arrived and its `tail -n 3` is read and green: `cp …`" |
| 6 STEP-5 (2) HEAD ≠ `<stack-final>` | `:346` "otherwise NO revert: end `FAILED: STEP-5 (2) — HEAD is <HEAD>, not <stack-final>; no revert made …`" |
| 7 STEP-5 (4) includes (f) | `:355` "Re-run 4.7 (a), (b) …, (c), (e), (f) and (g)" |
| 8 6.7 fresh radar cycle | `:406` "FIRST `date`, then `tail -n 12 …/radar.err` → a `radar cycle:` line stamped AFTER `<t write>` (at most THREE tails, as 4.7 (b) … PAUSE provision …)" |
All eight landed; none `NOT FOLDED`.

## Folds proposed
Each HOLDS finding as ONE text change to `09`, for the desk to fold (L19: a whole re-issue). I edit nothing. All five are relaunch, abnormal-input or wording changes; none is a blocker (rows 1, 2, 5, 6, 8).
1. DESK LINE (`09:5`): `from launch until `09` writes its stop line, the desk HOLDS …` → `from launch until `09` writes its stop line — and, after a BETWEEN stop line, until the relaunch's 4.3 has run or the desk re-cuts — the desk HOLDS …`.
2. THE RELAUNCH RULE (`09:291`–`:292`): add `git -C /Users/cobalt/cobalt status` to the opening calls; in (i) add: `If it shows a revert in progress (`You are currently reverting`, `REVERT_HEAD`, unmerged paths), bootstrap nothing: residents stay DOWN and named; go to STEP-5 (2)'s PROVE IT / `revert --abort` and end FAILED naming it.`
3. WHAT SHIPS (`09:23`): `(`c212645f`)` → `(`de88c302`)`.
4. STEP-2.3 (a) (`09:247`): `ONLY after 2.2's `<task-notification>` … `cp …`` → `… then `ls -la /Users/cobalt/cobalt-wt/*/.env` (P7's lock read, again) → "no matches found", then `cp …``.
5. RELAUNCH (iii) / (iv) (`09:294`–`:295`): `resume at 4.1` → `re-read P14's four log baselines (<a0> <ta0> <tr0> <tc0>), record them, then resume at 4.1`.
Not proposed: Opus's 6.8 companion-value risk and its 6.7 "contradiction" (DO NOT HOLD, rows 3–4); STEP-5 at any hour (row 7, a ruled choice named in the prompt); P11's seam→ln-base link (row 9, UNVERIFIABLE FROM READS).

## ESCALATE
1. ASK DESK: fold 1 / the DESK LINE — a BETWEEN ending needs the desk's commit hold to outlive the stop line, or the relaunch at 20:00 ends at (ii). Safe default: fold it. Both houses found it; `06` ESCALATE 2 raised the half `09` folded. [2026-09-24 07:58 ET]
2. ASK DESK: fold 2 / relaunch (i) on an interrupted `-m 2` revert — take it or record that such a relaunch is the desk's manual case. Safe default: fold it. [2026-09-24 07:58 ET]
3. Before launch, the desk's own steps (not this hub's): commit this report (P9); fill `R__L` (5 lines) naming every fold taken; run bare command (1) — `stacked-0923` is at `4e4577c3` now and P12 refuses until it is re-cut on `59532385`; keep its commits off `main` until `09`'s stop line (main is `ahead 44` of origin; `configs/cobalt/rules.yaml` is dirty — a commit of it would be a non-docs move and fail P11).
4. Opus's two UNVERIFIABLE items, for the record (L70, not defects): whether the 21:10 replay on `s2p2.2` code refuses `s2p2.1` receipts and disturbs the owed S2 read (`09:16`; `grep -n "EVALUATOR_VERSION\|refus" /Users/cobalt/cobalt-wt/setups-c1/src/cobalt/replay/runner.py /Users/cobalt/cobalt-wt/setups-c1/src/cobalt/replay/formations.py`); and Opus's Q3 read that the real migrate costs about twice the proof-only run (`grep -n "proof cost" ".../reports/deploy-2026-09-19c.md"`). Grok took ≈15 of its 20 minutes on a 613 KB packet.

SETUPS DEPLOY R2 REVIEW DONE · houses: 2 of 2 · other houses: 1 of 1 · blockers: 0 · folds: 5
