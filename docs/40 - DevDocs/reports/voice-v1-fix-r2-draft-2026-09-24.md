# Voice V1 fix round 2 — classify-first draft (L75) · 2026-09-24

## §0 Headline

- Classified check round 2 (`37`): its 3 HOLDS, C2, the RUN-2 / RUN-4c / RUN-7 reds and every item under its ESCALATE. **FIX 7 · NOT REAL 9 · UNPROVEN 0 · OUT OF SCOPE 6 · OWNER ITEM 0.**
- FIX: A1, D3, DESELECT IDS (report-only), C2 (the registry schema's own path; FINAL `:118` binds the load, and the resident's load is a load), RUN-2, RUN-4c, RUN-7. Each one traces to a FINAL clause, quoted below.
- Wrote `prompts/2026-09-24/54-voice-v1-fix-r2-build.md` (base `137c1928`, the tip read 18:07; red first; three suites under L76) and `55-voice-v1-fix-r2-check.md` (**ROUND 3, THE LAST**; Opus 5.5 + Grok, Sol from Sep 26th, 2026 6:47 AM).
- `comm -3` against `36` / `37` prints nothing for either prompt. New rule strings: 0 (the `.env` pair is R68's). ESCALATE: 9.

## L74

A block attached at session start asked for a `Claude-Session:` commit line and named a file-send tool. I recorded it once here as data and did not follow it. This seat commits nothing and sends no file.

## Classification

Sources: `chk` = `reports/voice-v1-fix-r1-check-2026-09-24.md`; `b1` = `/Users/cobalt/cobalt-wt/voice-v1/docs/40 - DevDocs/reports/voice-v1-fix-r1-build-2026-09-24.md`; `r1d` = `reports/voice-v1-fix-r1-draft-2026-09-24.md`; `FINAL` = `docs/30 - Design/VOICE-v3-FINAL-2026-09-23.md`; `desk` = `reports/cto-2026-09-24.md`. Code lines are on `voice/v1-0923` at `137c1928`.

| # | claim (short) | source line | class | traces to (FINAL clause quoted) | FIX shape or reason | grok's answer |
|---|---|---|---|---|---|---|
| A1 | `exit QRS`, `get out of QRS`, `close out QRS` and `short QRS` reach `answer` / `act` with no refusal. `_ORDER` (`tools.py:53-59`) matches only `short(ing)? \d+`, `get me out`, and `(exit\|close) … position` | chk:140, :157 (row :60) | FIX | FINAL:84 "anything that places, changes or cancels an order or touches his trading platform (no such tool exists; `refuse` with a fixed sentence)". The FINAL has NO exit / close / ½-off card action: §3's table has one ACT on a card, the stop (`:103`), and "Anything else → `unsupported`" (`:105`). FINAL:391 "no order tool exists and code refuses one". So an exit of a ticker is an order, not a card action | Red first: 4 inputs × answer / act → `refuse`. Then ONE `_ORDER` alternative: `short` without a share count, and `exit`, `close out`, `close` or `get out of` followed by an UPPER-CASE 1–5-letter ticker (case-sensitive). `the` / `my` / `a` before `short` are excluded, so `my short XYZ card`, `the XYZ short to 4.50` and `close at` stay readable. One NOT-refused pin is added: `what is the stop on my short XYZ card`. Round 1's platform and phrase rows stay closed, and this row does not re-argue them | CLOSED (chk:60), with no reasoning (chk:164). Dissent recorded; the hub's file-check decides (L35) |
| D3 | `lines = [refused]` and `lines = j.lines \|\| []` in the status callback (`web.py:299`) wipe the RED no-microphone line that `:300` pushes synchronously | chk:141, :158 (row :70) | FIX | FINAL:175 "No microphone / permission denied \| widget red: \"no microphone on this device\" — text box still works"; §7 heading `:167` (L1, L9, L18). A device's red line that a status refresh erases is a silent all-clear. Fix r1's FIX (r1d:38, `r.ok` first) closed the 403 half; the wipe is older (chk:141) | Red first: a static pin (no JS runner) that (a) declares a `device` array, (b) makes `banner()` draw `lines.concat(device, extra \|\| [])`, (c) makes `:300` push to `device`, and (d) makes the status block never assign `device`. So a refused status and an OK status both keep the line. The status fetch stays as it is | CLOSED (chk:70), with no reasoning. Dissent recorded |
| DS | The three carried deselect ids are not named. The build report gives `<the 8 --deselect>` and names only `TestMigrationRoundTrip` | chk:142, :159 (row :97) | FIX (report-only) | L68 GATE EARLY: "every check packet carries" the three executed results. L35: a count the reader cannot trace is not evidence | `54` D8 (d) quotes the command with all eight ids written out, the standing ESCALATE line lists them, and the stop line carries `deselects: <the 8>`. `55` checks the ids at PREFLIGHT and in FOURTH | DESELECTS AS STATED (chk:97). Dissent recorded |
| C2 | The resident's voice-config load skips the backup-source refusal. It was NOT BUILT twice: the shape drafted in r1d:28 (`com.cobalt.aset` under `no_resident_reads` `readers:`) is refused by `jobs/config.py:402-406` | chk:143, :171 (ESC 10); b1:103 (schema proof), :159 (ESC 1); r1d:28; desk R71 | FIX — the schema's own path | FINAL:118 "The config schema REFUSES to load (L1, L10) when `scratch_dir` is relative, or sits under the repo root, under the resolved vault path, under `docs/`, or under any `backup.yaml` source (F11)." The sentence binds the LOAD and exempts no caller. The resident loads (`voice/web.py:57-60`), and that load is the one that governs production. NOT REAL would need a sentence that binds only a suite or an optional argument; `:118` has no such sentence. L42: "config in a resident's `reads:` → that resident". The schema (`jobs/config.py:393-397`, `:402-406`) names the one lawful shape | `load_voice_config` reads `load_backup_config().sources` through the ONE loader when none are passed, and a `BackupConfigError` becomes a `VoiceConfigError`. `jobs.yaml` puts `configs/cobalt/backup.yaml` in `com.cobalt.aset`'s `reads:`, removes it from `no_resident_reads` (the schema refuses both), and keeps the provenance as a comment. The test at `test_voice_config.py:213-217` (`:199` at `28b6b0c6`; r1 cited `:201`) is reversed. `test_jobs_restarts.py:343` becomes a pin on the derived aset restart, and its reader set gains only `cobalt.voice.*` callers. A reader outside `cobalt.voice.*` is FAILED, not widened. No third shape | NOT BUILT — carried (chk:66) |
| R2 | RUN-2 is red: a stop moved between `execute_stop`'s read (`tools.py:228`) and `set_card_stop`'s read (`card_stop.py:43`) is not refused, and the edit is recorded with the second read's `from_stop` | chk:165 (ESC 4); b1:118, :160 | FIX | FINAL:90 `[F-09]` "At execution the expert recomputes it from a fresh read; a mismatch → REFUSED, the new before → after read aloud, re-confirm." A RUN that someone ran and read red is a defect under L70, not UNPROVEN | Red first: remove the strict-xfail mark from the unchanged RUN-2 test. `set_card_stop(…, expect_from_stop=None)` then raises `StopMoved(CardStateError)` with the fresh card BEFORE `record_stop_edit` when the stop differs. `execute_stop` passes `pending.from_stop` and turns `StopMoved` into `TargetChanged(<fresh dry run>)`. The route never passes the keyword, so it is unchanged | RESULT SHOWN — red (chk:111) |
| R4c | RUN-4c is red: a `ValueError` inside `plan_turn` fails loud and RED, but as `turn_error` "Something failed in this turn …", not the Plan-step class | chk:165; b1:122, :161 | FIX | FINAL:178 "Local model down / Plan does not parse \| red: \"Cobalt can't think right now (<class>)\" — nothing executed \| `voice_plan` RED". L1 is already met, but the row names the Plan step's sentence and class | Red first: remove the mark. In `turn.py`'s Plan step (`:293-300`), after `PlanFailed`, any other `Exception` from `deps.plan` becomes `_Fail("voice_plan", "Cobalt can't think right now (<class>).", red=True)` | RESULT SHOWN — red |
| R7 | RUN-7 is red: the CLI `--audio` path transcribes a clip over `max_upload_bytes` and exits 0. Only the route bounds it (`web.py:110`) | chk:165; b1:124, :162 | FIX | FINAL:72 "Length and size bounds are engine tunables (§11 W7)"; :239 W7 "`max_clip_s`, `max_upload_bytes` … are ENGINE TUNABLES"; :188 "One turn function, three callers … the CLI `cobalt voice turn --audio <file>`". A bound that one caller can bypass is not the bound | Red first: remove the mark on the oversize param. `cli.py` reads the bytes once and, over `cfg.max_upload_bytes`, calls `_fail(… exceeds <n> bytes …)` BEFORE the scratch lock and before any turn | RESULT SHOWN — red (chk:116) |
| O1 | RUN-1 timing (a stop write waiting past the 60 s `executing` limit) | chk:110, :122, :165; b1:117, :163 | OUT OF SCOPE | L76. The desk's standing ruling (chk:169 ESC 8, :173 ESC 12) runs it at the deploy's L68 gate | Carried verbatim. It needs `0017` COMMITTED, which is impossible in a build under L76 | RESULT SHOWN — UNPROVEN |
| O2 | Five V1 with-DB tests UNRUN under L76 (store round-trip and reaper, the two X-X13 races, X-E7) | chk:103, :169 (ESC 8); b1:139-145, :169 | OUT OF SCOPE | L76 second clause; desk R64 (1) (b1:169): "the stacked L68 gate" | Carried verbatim; `54`'s D8 (c) proves the reason again | — (not asked) |
| O3 | D6: `test_radar_panel_cards.py` changed outside `43`'s list | chk:170 (ESC 9); b1:170 | OUT OF SCOPE | — (r1d:41: a necessary consequence of the widget on `/radar`; reverting it turns `/radar` red) | The desk rules the boundary; carried verbatim | — |
| O4 | X-E10: the `av` wheel's bundled codecs | chk:170 (ESC 9) | OUT OF SCOPE | FINAL:285 "an unexpected native / GPL package → tribunal round 2" | A tribunal step, not a build (r1d:53) | NO NEW PACKAGE BEYOND THE FIVE (chk:119) |
| O5 | RUN-9: the whole-branch restarts range exits 1 on six UNCLASSIFIED paths | chk:118, :127; b1:153 | OUT OF SCOPE | L42 "unclassified → ESCALATE, never dropped" (r1d:75, O11) | The deploy or the desk declares them. `54`'s RUN-R re-reads the list | RESULT SHOWN — green (chk:118) |
| O6 | Device session (E1 E3 E5 E8 X3) and V1's production prerequisites; D3's runtime on a real device | chk:172, :173 (ESC 11 / 12) | OUT OF SCOPE | FINAL:277 E1; b1:171 | Owed before any ship. `55`'s standing line carries it | — |
| N1 | ESC 1: Opus `DEFECT REMAINS`, quoted | chk:162 | NOT REAL (record) | L35 | Its content is rows A1, D3, DS and C2 above | — |
| N2 | ESC 2: FOR THE CLASSIFIER restated | chk:163 | NOT REAL (record) | — | Rows A1, D3, DS | — |
| N3 | ESC 3: Grok vs Opus contradiction on A1, D3 and the deselects | chk:164 | NOT REAL (record) | L35, L39 | Settled by the file-check rows and recorded as dissent in the column. `55` asks Grok for a reason behind every `CLOSED` on A1, D3 and C2 | — |
| N4 | ESC 5: Sol METER | chk:166 | NOT REAL (record) | L62 R19 | `55` seats Sol from Sep 26th, 2026 6:47 AM | — |
| N5 | ESC 6: process record (classifier halt at staging, "31 paths" vs 29, harness warnings) | chk:167 | NOT REAL (record) | desk R79 | `55` stages every Write under 15,000 B and names its own path count, 18 | — |
| N6 | ESC 7: L74 | chk:168 | NOT REAL | L74 | Recorded once, not followed | — |
| N7 | ESC 11: round-2 standing line | chk:172 | NOT REAL (record) | L39, L75 | Superseded by `55`'s round-3 standing line | — |
| N8 | Opus: "the 0017 absence probe is short by one" (with-DB cell) | chk:95; b1:147 | NOT REAL | L76 | Short by exactly 1 IS the absence proof (b1:147); `54` D8 (e) expects the same | SHOWN (chk:95) |
| N9 | D7 "has never run" | chk:73, :144 | NOT REAL (fact) | — | Deselected under L76; carried as O2 | CLOSED |

Counts: FIX 7 (A1, D3, DS, C2, R2, R4c, R7) · NOT REAL 9 (N1–N9) · UNPROVEN 0 · OUT OF SCOPE 6 (O1–O6) · OWNER ITEM 0. `37`'s twelve ESCALATE items map as: 1 → N1; 2 → N2; 3 → N3; 4 → R2, R4c, R7, O1; 5 → N4; 6 → N5; 7 → N6; 8 → O2; 9 → O3, O4; 10 → C2; 11 → N7; 12 → O6 (with O2). Why round 2 is needed: fix r1 closed the platform names and the order PHRASES (A1), and the 403 half of D3. It did not close the bare exit / close / short OF A TICKER, or the older wipe. C2's r1 shape was refused by the schema, not argued against, so this round re-shapes it and does not re-classify it.

## OWNER ITEMS

none. Every question here is settled by the FINAL's own words (A1 `:84`, D3 `:175`, C2 `:118`, RUN-2 `:90`, RUN-4c `:178`, RUN-7 `:72` / `:239`) or by law (L42, L68, L76). Under L67's owner-items clause these are house questions, not his.

## FOR DEJAN

New strings: NONE. `54` carries `36`'s two `.env` strings byte for byte, and they are already approved (`cto-2026-09-24.md` R68, "All approved."). `55` carries `37`'s fourteen byte for byte, which are R17 / R19 standing.

## ESCALATE

1. **A1's ticker is case-sensitive, by design.** The new alternative refuses `exit QRS` but not a lower-case `exit qrs`. A case-insensitive ticker would also match `close at` and `short to`, both in the NOT-refused set (`test_voice_tools.py:159-162`). Whether transcripts carry tickers in upper case is E2 / E4's measurement in the device session. A wider shape is a house design question and is NOT built (L75: nothing widens past the four inputs round 2 held).
2. **RUN-2 leaves a narrower window, not built.** The fix refuses a move between the two reads. A move between `set_card_stop`'s read and `record_stop_edit`'s `SELECT … FOR UPDATE` (`cards/store.py:677-682`, which does not select `stop`) is not re-compared. No check claim names that window, so it is carried for the desk, not built.
3. **C2 changes the L42 derivation.** From fix r2 on, a `configs/cobalt/backup.yaml` change derives a `com.cobalt.aset` restart, and `jobs.yaml`'s `no_resident_reads` loses its `backup.yaml` entry (the schema refuses a path in both). The aset `reads:` block's heading says "re-reads on every request", while the voice config is read once and cached (`voice/web.py:57-60`). `54` adds a one-line comment rather than rewording the block. The deploy prompt names the restart (`55`'s standing line).
4. **D3 is proven only by a static pin** (no JS runner here, as in fix r1). Its runtime behaviour on a real device belongs to the owed device session.
5. **`54` queues on the L76 lock** behind `42` / `43` / `48` / `51`, in the desk's order (its DESK LINE).
6. **`55` is ROUND 3, THE LAST.** A HOLD there, or a NO with 0 HOLDs, goes to him as ONE message: override (L67 / L73) or a design round, never a round 4. `55` retitles `## FOR THE CLASSIFIER` as `## FOR DEJAN` for that reason and asks Grok to give a reason for every `CLOSED` on A1, D3 and C2. Both are prompt text, not strings.
7. **Placeholder gates** in `54` / `55` are `grep -c -F "R__"` → `1` after the desk fills both `R__` launch-row placeholders (each file holds 3 now). The gate is a fixed string, because a `-E` bracket pattern is not a plain fixed string (09-24 lesson (1)).
8. **Sol METER** until Sep 26th, 2026 6:47 AM. The seats are Opus + Grok, at the floor.
9. **L74:** recorded once above, not followed.

VOICE V1 FIX R2 DRAFTED · FIX: 7 · NOT REAL: 9 · UNPROVEN: 0 · OUT OF SCOPE: 6 · OWNER ITEM: 0 · prompts: 2 · new rule strings: 0 · ESCALATE: 9
