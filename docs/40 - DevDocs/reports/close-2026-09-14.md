# Close — 2026-09-14

§0 Ledger appendix appended (day-open, alert fatigue, C-size, three tribunals, S2-P3 plan,
`cobalt day-open`). `_inflight` pre-merge copy workflow retired — plans/reports commit
directly on the working branch; `docs/_inflight` is README-only, permanently. Found and
fixed a real gap while doing this: `.gitignore` never actually carved out
`docs/_inflight/README.md`, so it was never tracked despite CLAUDE.md/PLACEMENT.md
documenting it as committed — added the carve-out. `COBALT_ENV=production uv run cobalt
validate` exit 0. ESCALATE: 0.

## What changed

| File | Change |
|---|---|
| `docs/00 - Project/PROJECT-LEDGER.md` | Appended 2026-09-14 appendix, one line per ruling (day-open, alert fatigue B, C-size, email retirement, yolo/day-open, tribunal placement, Grok tribunal, analyst tribunal, prebell, S2-P3, tree cleanup, rules.md #11, ops queue), plus the placement fold note and the three proposed law amendments below |
| `docs/PLACEMENT.md` | Rewrote the `_inflight` rule (no pre-merge copies, README-only permanently) and added the tribunal-workspace rule (`Think/0 - Inbox/tribunal-<name>-<date>/`, hub report in `40 - DevDocs/reports/`, never `~/tmp`) |
| `CLAUDE.md` | Vault delivery paragraph replaced with one line pointing at `docs/PLACEMENT.md`'s `_inflight` rule; Documentation standard's `_inflight` tier bullet updated to match (struck in place with a dated pointer, not deleted) |
| `docs/_inflight/README.md` | Rewritten to state the folder is retired to README-only, no copies |
| `.gitignore` | Added missing `!docs/_inflight/` / `!docs/_inflight/README.md` carve-out (see ESCALATE-worthy finding below — not escalated, just fixed, since it's a one-line config fix directly required to make today's ruling true) |
| 6 untracked reports (`aset-c-enable`, `aset-c-gate-locate`, `day-open`, `grok-launcher-diag`, `tribunal-analyst`, `tribunal-grok`, all `-2026-09-14`) + already-modified `configs/cobalt/rules.yaml`, `archiver-runs.md`, `seat-usage.md` | Committed as-is, untouched, per dispatch |

## Findings (not escalated, logged for the record)

- `docs/_inflight/README.md` was never actually git-tracked: `.gitignore`'s `docs/*` rule had
  no negation for it despite both CLAUDE.md and PLACEMENT.md documenting it as "held in git."
  Fixed with the carve-out above; now `git check-ignore` confirms it resolves to tracked.

## Laws fold — PROPOSED, NOT APPLIED (owed Dejan's one-word approval)

Per CLAUDE.md/LAWS.md discipline, no Cobalt session writes LAWS.md directly — these are
listed here for the fold, not applied:

1. **L28 amendment** — trader-run settings CLI (`cobalt settings load --apply`) is exempt
   from HITL, with a ledger trace of the run. Source: 09-14 C-size ruling ("I am already in
   the loop").
2. **L43 amendment** — production radar restarts only inside the 20:00–21:00 pause or
   overnight idle on a trading day. Source: 09-14 alert-fatigue deploy sequencing.
3. **L49 addendum** — the local seat's only write path is a Cobalt command, never the shell.
   Source: 09-14 ops queue / local-seat discipline.

Everything else in the 2026-09-14 ledger appendix is record, not law.

## Verification

- `git status --porcelain` before this run showed only the known machine-written files
  (`configs/cobalt/rules.yaml`, `archiver-runs.md`, `seat-usage.md`) plus today's six
  untracked reports — matched, none touched beyond staging.
- `COBALT_ENV=production uv run cobalt validate` → exit 0, "Placement (docs/PLACEMENT.md):
  tree clean."

CLOSE COMMITTED 6d3bf53 (a commit cannot state its own final hash without changing it — `git log -1` on main is authoritative) · PUSH: Dejan · ESCALATE: none
