AMEND BEFORE BUILD — first-run migration, fill, settings, replay-input and total-demand blockers remain; R1–R7 (including amended R7) are accepted unchanged.
Reviewed working-tree P4 plan on `71b0c16`; P2 plan is absent here, so STEP-1/§7 were read at `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` (P2 below); evidence is static, no DB/production tests run.
ESCALATE 2: sound interim; 3: investigation sound, data-loss conclusion unproven; 4: unsound deferral; 5: conditional scope handling sound, dependencies need correction. RESTARTS: none (this report only; documentation classifier exists).

| id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording |
|---|---|---|---|---|---|
| 1 | BLOCKER | STEP-1; §4 migrations | Adding the two radar columns changes the populated membership digest. The migration harness treats that as content corruption and rolls back the entire forward migration; empty-DB tests miss this. | `src/cobalt/db_migrations/cli.py:49`, `:102`, `:142`, `:223`; P4 plan:87 | Extend the migration proof's shape exclusions for `rank_metric` and `rank_value`, retaining checks on pre-existing data. Prove first apply, second apply and reverse/reapply on populated membership with non-null post-deploy values, in both P2/P4 merge orders. |
| 2 | BLOCKER | STEP-1 DDL; tenancy tests | Sides and user_id declarations are correct, but picks/missed have no explicit side ownership. Movers grants omit REFERENCES for its cross-side FK. Existing defaults grant privileges, not ownership, and do not supply that REFERENCES grant. | `docs/10 - Decisions/ADR-0008-two-layer-data-model.md:82`, `:97`; `src/cobalt/db_migrations/0001_schemas.sql:119`, `:135`; `src/cobalt/db_migrations/0004_radar_pool.sql:61`; P4 plan:105, :108 | Set picks/missed OWNER TO cobalt_user; retain movers OWNER TO cobalt_system; explicitly grant SELECT, REFERENCES on movers to cobalt_user and appropriate sequence privileges. Expand both `user_id …` placeholders to the stated NOT NULL/FK/GUC DDL. Verify ownership, insert/identity use, user reads of system data, missing-GUC rejection and wrong-side rejection through real side-role connections (L32). |
| 3 | AMEND | STEP-1; §7 P2 seam | No duplicate P2 table is proposed: P4's picks/missed/movers are distinct from P2's score/receipt/dot seam. Number gaps are safe because all FORWARD files run every time, not because there is an applied-version ledger. Registry merge order and reverse behavior still need proof. | `src/cobalt/db_migrations/__init__.py:31`; `src/cobalt/db_migrations/cli.py:199`, `:256`; `src/cobalt/db_migrations/placement.py:64`, `:73`; P2:77, :135, :174, :284 | Keep P2 0006/0007 and P4 0008/0009, numerically ordered forward and descending reverse. Move missed out of DECLARED_TABLES; register all three P4 tables in CREATED_TABLES. Test 0005→0008/0009→later 0006/0007 plus rerunning 0008/0009, and the opposite order. P4-before-P2 must have no SQL/import dependency on missing P2 columns or tables. |
| 4 | BLOCKER | STEP-3 SAVEPOINT | `fill()` owns an open transaction only on the multi-hop manual shortcut. Its radar/already-TRIGGERED route calls `transition()` without a connection; that commits before returning. Merely inserting the specified savepoint hook into the existing outer transaction misses this route. | `src/cobalt/cards/store.py:374`, `:417`, `:435`, `:454`; `src/cobalt/cards/store.py:235`, `:307` | Make fill own one USER transaction on every legal route, passing it to every transition. After the FILLED hop and before commit, create the savepoint around all pick reads/insertion, rollback only that savepoint on recoverable pick failure, release it, and commit the fill. Test WATCH/ARMED shortcuts and strict TRIGGERED manual/radar paths with an actual SQL error; also prove a fill-transaction failure never reports success. |
| 5 | AMEND | STEP-3 banner; §4 F3 | The proposed return flag is not propagated by either existing sheet path. `mark_filled()` discards fill's return, returns None, and writes figures separately; the direct route and CLI expect a list of transition ids. Returning a new object without updating these callers risks a post-commit error or a false green banner. A logger line is not automatically a persisted job row. | `src/cobalt/aset/store.py:185`, `:213`, `:226`; `src/cobalt/aset/web.py:1087`, `:1104`, `:1187`, `:1208`; `src/cobalt/cards/cli.py:79` | Define a typed fill result carrying transition ids and pick_recorded; propagate it through mark_filled, both sheet routes and CLI. Preserve successful fill reporting while appending the red pick-not-recorded banner. Name the existing resident log destination for the required log line. Test both HTTP paths, including successful fill-figure persistence after pick failure. |
| 6 | AMEND | STEP-3 picks lookup/rank; STEP-4 stores | ADR-0008 allows USER reads of qualified system tables, but SYSTEM cannot read cards/settings or write picks/missed. Reusing RadarStore would open a separate connection and weaken the in-transaction snapshot. The score cohort is also unspecified at the FILLED boundary: filtering open WATCH/ARMED/TRIGGERED cards after the hop can remove the picked card from its own ranking. | `docs/10 - Decisions/ADR-0008-two-layer-data-model.md:82`, `:89`; `src/cobalt/radar/store.py:19`, `:26`; `src/cobalt/cards/store.py:285`; P4 plan:133 | record_pick uses the passed USER connection and qualified `system.radar_*` reads inside the savepoint; no role switching or independent pick commit. Define a consistent pick-time cohort including the picked card, deterministic tie policy and unavailable-score reasons; store the cohort/score inputs needed to replay the rank (L57). Replay uses USER for settings/cards/missed/vault audits and SYSTEM for movers/bars/job bookkeeping, with explicit transaction boundaries. Test both permissions and tied ranks. |
| 7 | AMEND | STEP-3 CLI; STEP-9 K6 | “Every FILLED card that day” must mean a FILLED transition on that ET date, not current state=FILLED, card creation date or aset_sizings.filled_at. The direct state button does not populate that latter field, and later CLOSED cards still have picks. Pre-deploy gaps conflict with K6's post-deploy-only expectation. | `src/cobalt/cards/store.py:266`, `:285`; `src/cobalt/aset/store.py:229`; `src/cobalt/cards/models.py:103`; P4 plan:142, :203 | Drive picks output from FILLED transitions with ET day bounds, left-join picks, and show MISSING/exit 1 for every missing row, including later CLOSED cards. Record the P4 deployment cutoff for K6 so historical MISSING rows remain visible without falsely failing its post-deploy assertion. Test cross-midnight UTC/ET, older-created cards, CLOSED cards and button fills. |
| 8 | BLOCKER | STEP-1 missed; STEP-5 inputs; §4 L57 | A digest, bar timestamp range and watermark cannot reproduce cf_r or mfe_r. The DDL does not retain consumed OHLC values, created_at/window source, historical stop version, or the state/position evidence behind gates. `system.bars` is mutable. `replay_job_id BIGINT` has no defined source: cobalt_jobs is keyed by label and overwrites its last-run state. | P4 plan:123, :127, :169; `src/cobalt/archiver/store.py:82`; `src/cobalt/cards/models.py:118`; `src/cobalt/jobs/migrations/0001_jobs.sql:44`; P2:178; LAWS L57 | Persist an immutable USER replay receipt containing actual consumed bars and timestamps, entry/stop version, direction, creation time, window text/resolution, horizon, state/position histories, benchmark/settings inputs and formula/rounding policy. Hash that retained payload; never substitute a hash for it. Define a replay-run identifier owned by this receipt rather than an imaginary numeric cobalt_jobs id. Recompute after source bars, settings and card stops change or disappear. Reuse P2's receipt/evaluator seam for formations, not a second scoring implementation. |
| 9 | AMEND | STEP-5 formula; §4 R tests | The gap rule is compatible with using fill_price in the numerator and planned entry-stop risk in the denominator, but the test name “stop … minus one” is only true without an entry gap. For long entry 100, stop 99, gap fill 102, stop exit gives −3R. mfe_r on a stop bar cannot establish whether its high preceded its low. | P4 plan:60, :160, :167, :168; `src/cobalt/archiver/models.py:40`; P4 plan:233 | Name planned_entry and actual counterfactual fill_price separately; retain the ruled gap-open fill and stop-wins tie. Test long/short gap-through plus stop, ordinary −1R stops and half-up rounding. Define exactly which bar extrema feed mfe_r, label it a bar-level observation where within-bar order is unknowable, and store that policy without introducing another cf-R formula. |
| 10 | AMEND | STEP-5 window/horizon | `_resolve_window_end` takes optional prose and returns a time/detail tuple or None, not a timestamp suitable for comparison. The existing public resolver also handles early closes. The plan hardcodes 16:00 and 15:59, which rejects valid half-day data and may use bars beyond a valid horizon. A bar stamped at the horizon can contain prices after it if timestamps denote bar starts. | `src/cobalt/cards/expire.py:92`, `:125`, `:128`, `:141`; P4 plan:159, :163, :166, :167 | Resolve the card's window source explicitly to an aware ET datetime, preserving the public resolver's fallback and calendar close. State bar timestamp convention and use only completed bars within the horizon. Keep the ruled post-window-trigger behavior, bounded by the session close. Define behavior for creation after close/no valid horizon. Test ambiguous prose, early close, trigger at window end, after-window trigger, and missing final or interior bars. |
| 11 | AMEND | STEP-5 gate history | The candidate set includes terminal MISSED/EXPIRED cards, but the gate mapping only covers WATCH, PASSED, ARMED and TRIGGERED. A manual WATCH→MISSED before the later price trigger has no matching state branch. Same-timestamp transitions require id ordering. Rule 10's two-position proxy is narrower than the actual rule text, including its stop-at-card-level exemption. | `src/cobalt/cards/models.py:90`, `:104`; `configs/cobalt/rules.yaml:42`; P4 plan:158, :161 | Define the exhaustive as-of-trigger state mapping, ordered by (at,id), including MISSED/EXPIRED and their recorded reasons. Fetch entry/stop and position state as of the trigger, not mutable current values. Label the two-open-card check as the implemented rule-10 proxy and record uncomputed clauses as unavailable; do not claim to have evaluated the whole rule. Test terminal states, simultaneous transitions, historical stop edits and position close boundaries. |
| 12 | AMEND | STEP-5 stale coverage; STEP-6 archive | “Any i1 bars for that day” is not evidence that a mover is archived through the required horizon. A ticker polled only briefly can skip archival and still set bars_archived=true. F12 manual cards outside both the Lists archive and top movers have no assured bar source; skipping them must remain visibly incomplete, not a zero-miss green run. | `src/cobalt/archiver/config.py:34`; `src/cobalt/archiver/store.py:97`; P4 plan:159, :176, :205, :206 | Check date-scoped coverage before skipping a fetch and verify returned bars cover the requested day before marking archived. Count and expose stale/missing candidate coverage in job.result, miss-line status and smoke. Specify the F12 source coverage prerequisite; any additional fetches must enter the same demand budget. Test partial-day bars, wrong-day exports, missing manual-card bars and genuine complete days with no triggers. |
| 13 | BLOCKER | STEP-6 total demand; §8 ESCALATE 4 | **ESCALATE 4 handling is unsound.** The plan explicitly excludes the archiver from the demand computation; a done precondition separates runs but does not satisfy L53's requirement to account for every consumer. Adding a request count directly to an rpm calculation is dimensionally undefined. `fetch_bars` does not acquire the screener process bucket, so mover archive requests also bypass the stated throttle unless explicitly wrapped. | P4 plan:176, :182, :323; `src/cobalt/radar/notes.py:219`; `src/cobalt/archiver/collector.py:219`; `src/cobalt/archiver/runner.py:27`, `:53`; `configs/cobalt/taxonomy/tunables.yaml:457`; LAWS L53 | One total-demand computation includes radar screens/lists/polls, P2 daily/context consumers when present, nightly archiver targets and replay exports plus mover i1 requests, with explicit time windows and rpm conversion. Non-overlap contributes zero only in a proved non-overlap window; archiver demand is never omitted. Refuse before requests when total exceeds the ruled ceiling, and test a sum-over-ceiling case where each isolated consumer passes, explicitly including archiver and movers. Keep the existing ceiling/cadence unless Dejan rules otherwise; the archiver policy issue blocks declaring this proof complete. |
| 14 | BLOCKER | STEP-6 benchmark settings; §6 D1 | The standalone P4 plan cannot apply its new setting: the current loader reads only two legacy YAML shapes, diffs seven SETTING_KEYS, requires a source argument and has no --sha256 option. Merely adding a Pydantic model does not add the load path. At 20:05 its apply also refuses in market_reset. | `src/cobalt/settings/models.py:135`, `:162`; `src/cobalt/settings/cli.py:68`, `:94`, `:113`, `:159`; `src/cobalt/session/guard.py:142`; P4 plan:174, :276 | Implement or explicitly reuse the shared optional-key loading/validation/hash-review seam, including when P4 merges before P2; do not add radar.benchmark to the required sheet keys. Name the exact reviewed input and complete command. Schedule Dejan's apply outside market_reset and before 21:05, without restarting radar to load a per-run setting. Test optional-key round trip, absent/malformed benchmark, hash mismatch and pause refusal. |
| 15 | AMEND | STEP-4 precondition; §6 D1 dry-run | At 20:05 tonight's scheduled 20:30 archiver cannot be done. Therefore D1's required successful today dry-run conflicts with the same precondition. “Finished done on this ET date” must identify tonight's scheduled full run, not an older invocation that finished after midnight or an earlier manual run. | P4 plan:147, :277; `configs/cobalt/jobs.yaml:188`; `src/cobalt/archiver/runner.py:91`; `src/cobalt/jobs/migrations/0001_jobs.sql:44` | Keep R3's 21:05 job and fail-loud precondition. Require the archiver label, current ET scheduled occurrence, done/exit 0, coherent started_at/finished_at and successful result. At 20:05 prove expected refusal; run the successful dry-run after tonight's archiver completes and before the live replay, or use the already-proven dev fixture. Test previous-day done, same-day early invocation, running, failed, absent row and UTC date crossover. |
| 16 | AMEND | STEP-4 timeout; §6 backup acceptance | 21:05 plus timeout_s=1800 does not guarantee completion by 21:35. The job wrapper emits heartbeats but does not cancel at timeout; watchdog requires both excess runtime and stale heartbeat. A healthy long-running replay can cross the 21:40 backup. | `src/cobalt/jobs/wrapper.py:167`, `:175`; `src/cobalt/jobs/watchdog.py:437`; `configs/cobalt/jobs.yaml:224`; P4 plan:144 | Add an enforced execution deadline with cleanup and no late vault write, leaving a measured margin before 21:40. Refuse work whose bounded demand cannot fit. Record finish time and verify the backup captures both replay rows and the fourth DRC unit. Test a slow collector/live heartbeat, timeout during a step, and failed/late archiver; do not silently wait past R3's schedule. |
| 17 | AMEND | STEP-4 dry-run; §4 sentinels | `as_job(...)` without skip writes schema/registration/state; the plan's wrapper spelling conflicts with “writes nothing.” Collector caching is another write path. Writer-module import bans are not a sufficient side-effect proof. | `src/cobalt/jobs/entrypoint.py:36`, `:43`; `src/cobalt/jobs/wrapper.py:148`; `src/cobalt/radar/collector.py:133`; P4 plan:143, :153, :173 | Use as_job(..., skip=dry_run), read-only stores without ensure_schema, no cache mutation in dry-run, and VaultWriter's dry-run diff path. Test no DDL/DML/job stamps/cache/vault writes for the full CLI path while still printing rows and the line diff. |
| 18 | NOTE | STEP-7 fourth DRC unit; §3/§4 | The fourth-unit design follows L28: upsert_unit refuses an absent note and preserves human edits through the existing writer. The placement must insert after the drc-rules **closing** marker, not wrap or overwrite human content. A requires_vault test “against live DRC shape” must not become a live write test. | `src/cobalt/vaultwrite/writer.py:136`, `:644`, `:669`, `:693`; `src/cobalt/prefill/drc.py:441`; P4 plan:186, :225; LAWS L28/L45 | Retain the existing-note-only writer; never call create_if_absent from replay. Implement zero-width placement after find_section(...).close_line, with the writer's safe append fallback. Hub reads real shape only, then proves writes/diffs/versioning/restore on a dev-vault copy. Add first-unit placement, missing anchor, malformed marker, human override, sync-revert and mtime-conflict tests; assert every human byte outside the unit remains unchanged. |
| 19 | AMEND | STEP-2 value rendering; §1 F2/F19; K3 | P3 has no per-row metric field: the metric name is a view-level session value. Adding only PoolRow.rank_value mislabels screen-override values. MembershipRecord forbids extra fields and also needs the new selected columns. K3 wrongly demands all post-deploy values be non-null even though STEP-2 permits missing values and frozen HOLD keeps old nulls. | `src/cobalt/aset/radar_panel.py:35`, `:92`, `:133`, `:358`, `:521`, `:685`; `src/cobalt/radar/pool.py:133`, `:174`; P4 plan:130, :200 | Carry metric and value through MembershipRecord, row conversion, PoolRow and rendering; show the stored per-row metric including overrides, with explicit unavailable values. Test the whole query→model→HTML path, not only rendering fakes. K3 distinguishes genuine missing metrics/frozen pre-deploy rows from failed writes. Correct F2: Transition is in pool.py:37; _ranked returns ordered tickers as well as ranks/source_for. |
| 20 | AMEND | STEP-6 reruns; --date; §4 | Upserts prevent duplicate keys but do not remove yesterday's selected top-N subjects from a same-date rerun whose ranks/settings changed. A nightly --date also cannot label today's unfiltered export as historical movers. Multiple exclusion episodes need deterministic selection; mover identity collapses both sides into one missed key. | P4 plan:97, :128, :143, :154, :175, :179; `src/cobalt/radar/collector.py:133`; `src/cobalt/archiver/collector.py:229` | Define one dated snapshot/run identity and reconcile the day's active benchmark set transactionally while retaining replay inputs. For historical --date, require retained dated exports/bars or refuse; never relabel a live fetch. Define duplicate-side and multiple-episode selection. Test changed top-N/settings, rerun after partial failure, same ticker on both sides, and historical data unavailable. |
| 21 | AMEND | STEP-5 formations; §7 | Conditional reuse of P2 is correct under L3, but module importability is not a replay contract. P2's replay CLI is writes-nothing; its score receipt has retained observations. The proposed missed identity has no formation timestamp/member id, so repeated formations of the same ticker/definition/day collapse. No corresponding formation integration tests are listed. | P2:178, :193, :284; P4 plan:128, :170, :305 | Specify the adapter's callable, typed results and capability/version check against P2's actual replay artifact. Absence gets exactly the ruled unavailable line; import/runtime defects fail loud. Define stable per-formation identity and card-match time bounds, then test P2 absent/present, two formations, existing-card suppression, and identical cf-R computation from retained inputs. Do not duplicate P2 tables/evaluator. |
| 22 | AMEND | §5 RESTARTS; §6 21:05 | Expected radar/aset restarts are reasonable, and 21:05 replay implies **no radar restart**. L43 now permits radar restarts during overnight idle as well as the pause. However the classifier currently drops a new one-shot plist from its restart labels; “plist new → bootstrap” needs an explicit derived action, not a omitted label. The new smoke YAML is also not a resident reads file and can be UNCLASSIFIED. | `src/cobalt/jobs/restarts.py:167`, `:192`; `configs/cobalt/jobs.yaml:67`, `:167`; `src/cobalt/radar/runner.py:161`; LAWS L42/L43; P4 plan:247 | Attach the actual derivation table and RESTARTS line for the final range; derive replay's install/bootstrap action from its plist and resolve the smoke-config classification before merge. Restart radar/aset for their imported code; bootstrap replay once. The 21:05 run is a one-shot and does not restart radar. Test the classifier's new-plist and smoke-config cases; retain ESCALATE for genuinely unclassified paths. |
| 23 | BLOCKER | §6 rollback; P2 compatibility | The proposed second rollback invocation selects 0009 again, because selection is by number, not applied state. Idempotent SQL may survive that, but “version before 0008” can reverse P2 if interpreted as the old deployment's 0005 after P2 later lands. Residents/replay remain active while their schema is removed. P2 rollback also deletes radar cards now referenced by picks/missed with NO ACTION FKs. A D1 dump cannot restore picks/missed that did not exist at D1. | `src/cobalt/db_migrations/cli.py:249`; P4 plan:291; P2:278, :282; P4 plan:111, :119 | Before schema reversal, stop replay and affected residents at an L43-safe time. Snapshot current P4 tables/receipts and affected audit units. Reverse 0009 then 0008 once with explicit --down-to 0007 --allow-prod (the numeric boundary works even if 0007 is absent), never implicitly reverse P2. Revert only P4 code/config by git revert, verify schema compatibility, then restart. Define/test the coordinated P2 rollback dependency without silently deleting pick/miss evidence. Preserve post-D1 manual data during any restore and prove the full rollback/reapply on populated dev data in both merge orders (L54). |
| 24 | AMEND | STEP-9 smoke; R6 hand fallback | The table is a useful checklist, but it is not executable verbatim: most rows lack commands/SQL and exact comparison rules. K13's flat 30h age contradicts the cadence-aware archiver probe. K8 can pass with missing candidate coverage, and K6 can pass vacuously with no post-deploy fill. The supplied day-open report is C1–C6, not a verbatim S-command report. | P4 plan:194, :203, :205, :210; `src/cobalt/heartbeat/probes.py:261`; `docs/40 - DevDocs/reports/day-open-2026-09-15.md:4`, `:106`; `docs/00 - Project/MVP-CHARTER-v0_2.md:149` | Supply an exact read-only command/query, side, expected shape, date/cutoff and verdict rule for each K-check, usable by hand. Reuse cadence-aware freshness, record coverage/stale inputs and distinguish no sample from a proven post-deploy fill. Preserve the ruled verdict precedence; define nonzero command behavior for non-GREEN. Test command errors, holidays/weekends, missing rows, historical MISSING picks and empty candidate sets. |
| 25 | NOTE | §8 ESCALATE 2 | **Sound interim handling:** unset trade_count_band is recorded and never invented or used to exclude. It is a day-mode proposal tunable today, not an implemented replay gate. Setting the numbers alone will not wire a new gate into excluded_by. | `configs/cobalt/taxonomy/tunables.yaml:316`, `:325`; P4 plan:165, :321 | Keep unset in gate_detail and report this acceptance limitation. When values are ruled, specify their replay-gate semantics, schema and tests explicitly; do not imply merely loading numbers activates the gate. |
| 26 | NOTE | §8 ESCALATE 3; §1 F4 | **Ops investigation is sound; “unexplained data loss” is not established.** Seven days is a maximum retention window, not proof seven days of collection existed. Cache folders use the supplied instant's date, not explicitly ET. Production's quoted two-day inventory was not independently re-proved in this review. | `src/cobalt/radar/collector.py:133`, `:138`; `configs/cobalt/radar.yaml:27`; P4 plan:38, :322 | Open an Ops ticket to establish collection start, prior inventories, clock/date basis and cleanup history; call it a retention/inventory discrepancy until loss is proven. R1 remains independent. Retain replay inputs durably rather than relying on this expiring cache (L57). |
| 27 | NOTE | §8 ESCALATE 5; amended R7 | **Conditional scope handling is sound:** the hand-smoke fallback is ruled, and further reductions require Dejan's ruling. The schedule still says Sol despite amended R7, and dropping mover i1 archival leaves F13 incomplete unless its acceptance is explicitly rescoped. P2 dependence, benchmark loading, successful archiver and one full P4 day are critical-path gates. | P4 plan:66, :263, :324; `docs/00 - Project/MVP-CHARTER-v0_2.md:154`; P2:284 | Replace the stale Sol estimate with the amended Opus headless lane. Keep dates fixed and make each further scope cut a recorded ruling with its resulting acceptance scope. Show the dependent settings, first live replay, fill proof and P2 smoke milestones; do not mark F13/S2 complete after an unruled archival cut. |
