# REBASE D3 — `ops/2026-09-19` + `archiver/append-0919` onto the post-P4 `main` (2026-09-19)

Build hub `rebase-d3-0919`, prompt `docs/40 - DevDocs/prompts/2026-09-19/48-rebase-deploy3.md`, Opus 5.
This lane deploys NOTHING: no production command, no merge into `~/cobalt`, no push, no `launchctl`, no vault write.

## §0 Headline

- **BOTH BRANCHES ARE REBASED onto the post-P4 `main` (`19bb1b2`), and both rebases are KEPT.** `ops/2026-09-19` → `bb996a5`, clean, 16/16 commits, offline **1875/0**. `archiver/append-0919` → `12ef535`, clean, 31/31 commits, **9 conflicted files resolved by rule; `FORWARD` = 0001…0011, `REVERSE` its mirror**.
- **STOPPED at §4.1: the archiver's offline suite is RED — `7 failed, 2131 passed`.** All seven sit in files that merged CLEAN. Five are enumeration pins that each asserted "the registry ends with exactly MY two migrations" (four the archiver's, one P4's, now on `main`); one is the archiver's own L3 range-read pin firing for the fold its spec says is owed once P4 lands; one is real — the combined L53 demand inventory measures **51.00 rpm against `finviz_max_rpm=50`**, a ruling under L53, not an edit.
- The with-DB step was never entered: **`.env` was never copied and `cobalt_dev` was never connected to.** `main`, production and the residents were not touched.
- ESCALATE: 5. Nothing merged, deployed or pushed.

## AUTHORIZATION — verified before anything was touched

| check | command | result |
|---|---|---|
| the row naming THIS file is committed | `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` | newest commit **`fde57c2 docs(desk): 09-19 R33 — 5 new rule strings approved for the deploy-3 rebase hub (48)`** — the row is committed, not working-tree-only |
| the row's content | `grep -n "48-rebase-deploy3" ".../reports/cto-2026-09-19.md"` | line 47 = **R33**, 15:0x ET, "Approved", naming `prompts/2026-09-19/48-rebase-deploy3.md`, its 29 rule strings (24 already approved today; FIVE new: `git rebase --abort`, `git -C /Users/cobalt/cobalt show*`, and the three exact `COBALT_ENV=dev … db migrate` strings replacing `44`'s wildcard) and the NEVER block (production, `--allow-prod`, push, `launchctl`, vault write, `bypassPermissions`, `git rebase --skip`). Line 665 records the launch. |
| standing rows | same file | R25 (REVOKE/DB-1), R26 (dropped twin/DB-2), R31 (narrowed deploy-2 gate), R32 (L43 overruled tonight) present; `cto-2026-09-18.md` R18–R21 (L67) read in the archiver build report's own authorization table |

Verdict: **authorization matches this file.** Proceeding.

## PREFLIGHT

Run 15:18–15:19 ET, before any rebase. Every probe is the bare shape of the rule it probes.

| rule | command | exit | verdict |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | allowed — `Sat Sep 19 15:18:29 EDT 2026` |
| `Bash(git -C /Users/cobalt/cobalt log*)` | `git -C /Users/cobalt/cobalt log --oneline -3` | 0 | allowed — tip `19bb1b2 docs(desk): 09-19 DEPLOY 2 LIVE …` |
| `Bash(git -C /Users/cobalt/cobalt status*)` | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | allowed — two untracked `31-packet/*.yaml` only (the deploy report's ESCALATE 5b; never `git add`ed, untouched here) |
| `Bash(git -C /Users/cobalt/cobalt show*)` (new, R33) | `git -C /Users/cobalt/cobalt show --stat deploy-2026-09-19b` | 0 | allowed — tag resolves to **`5b58b7b`** |
| `Bash(git status*)` | `git status --porcelain` in both worktrees | 0 | allowed — **empty in both** |
| `Bash(git log*)` | `git log --oneline -1` in both worktrees | 0 | allowed — tips below |
| `Bash(ls *)` | `ls -la .env` in both worktrees | 1 | allowed — **"No such file or directory" in both** |
| `Bash(cd *)` | `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` | 0 | allowed |
| (no explicit rule; read-only, allowed by the classifier) | `git rev-list --count main..HEAD`, `git -C /Users/cobalt/cobalt rev-parse --short HEAD` | 0 | allowed — recorded here because they are not named rule strings; both are pure reads |

Not preflighted, first use is the real use: `git rebase main`, `GIT_EDITOR=true git rebase --continue`, `git rebase --abort`, `git add/commit/diff/show`, `uv run pytest`, `uv run cobalt jobs restarts`, the `.env` `cp`/`rm`, and the three `COBALT_ENV=dev` commands — none has a harmless variant inside its own pattern (UNATTENDED-LAUNCH §6).

### 1.1 / 1.2 / 1.3 — the gate

| fact | value |
|---|---|
| `<main>` | **`19bb1b2`** (`git -C /Users/cobalt/cobalt rev-parse --short HEAD`) |
| P4 on main | **YES** — `git -C /Users/cobalt/cobalt log --oneline -40` carries the `sprint-2/p4` range (`d3aa084 fix(s2-p4) …` through `5b58b7b docs(report): s2-p4 round-2 db run stop line — P4 DB 9045260 …`), and `9562dc3 docs(deploy): P4 DEPLOY DONE 5b58b7b …` above them |
| tag `deploy-2026-09-19b` | resolves — commit `5b58b7ba95c48209e8a4419464fe913120708452`, 1 file, `s2-p4-verify-2026-09-19.md` |
| `archiver/append-0919` tip (before) | **`99039f7`** `docs(report): archiver db re-run — the stop line (db 161/0, DB-1 PASSED/PASSED, cobalt_dev back at 0001-0009)` · `git rev-list --count main..HEAD` = **29** |
| `ops/2026-09-19` tip (before) | **`a868a69`** `docs(report): ops 0919 db run — R1/R11 settled on cobalt_dev, with-DB suite green 61/0, ESCALATE 12` · `git rev-list --count main..HEAD` = **16** |
| both worktrees clean | yes, `git status --porcelain` empty in both |
| `.env` | absent in both worktrees — no other lane holds `cobalt_dev` through these trees |
| re-land check (UNATTENDED-LAUNCH §6) | neither branch is an ancestor of `main`: both counts are non-zero (16 and 29), so `git rebase main` has commits to replay. Deploy 2 shipped **P4 alone** (R31), so `ops/2026-09-19` is genuinely unmerged |

**GATE: proven.**

## 2. OPS REBASE — CLEAN, as deploy 2 predicted

| step | result |
|---|---|
| `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` | ok |
| `git rebase main` | **CLEAN** — `Rebasing (1/16)` … `Rebasing (16/16)`, `Successfully rebased and updated refs/heads/ops/2026-09-19.` **No conflict, no stop** |
| new tip | **`bb996a5`** `docs(report): ops 0919 db run — R1/R11 settled on cobalt_dev, with-DB suite green 61/0, ESCALATE 12` (was `a868a69`) |
| `git rev-list --count main..HEAD` | **16 → 16** — no commit dropped, no commit added |
| `git status --porcelain` | **empty** |

### 2.3 Offline suite on the rebased ops branch — GREEN

`cd /Users/cobalt/cobalt-wt/ops-2026-09-19` → `ls -la .env` → `ls: .env: No such file or directory` → `uv run pytest -q tests/cobalt tests/taxonomy` (background, exit 0).

Summary line, verbatim: **`1875 passed, 329 skipped, 1 xfailed, 15 warnings in 57.01s`** — **0 failed**.

Context, not the bar: this branch's own pre-rebase close was `1608/0` (302 skipped). The +267 passed are P4's own tests, which the branch now sits on top of.

## 3. ARCHIVER REBASE — 9 files, 4 commits, every resolution by rule

`cd /Users/cobalt/cobalt-wt/archiver-append` → `git rebase main`. It stopped FOUR times, on four of the branch's own commits. `git rebase --skip` was never typed and is not in this session's allowlist.

| # | commit it stopped on | conflicted files |
|---|---|---|
| 1 | `b623e72 feat(db): 0010 archive_progress + 0011 archive_incidents …` | `db_migrations/__init__.py`, `db_migrations/placement.py`, `test_radar_migration.py`, `test_radar_score_migration.py`, `test_tenancy.py` |
| 2 | `4e3c577 feat(archiver): append path — insert_new_bars, progress and incident writers …` | `archiver/store.py` |
| 3 | `2b4893c feat(archiver): write_mode dispatch — upsert untouched, pre-write shadow compare …` | `archiver/runner.py`, `docs/40 - DevDocs/cobalt/archiver/runner.md` |
| 4 | `f4bdcfa feat(archiver): repairs only in a quiet window (R8 option A) …` | `cli.py` |

**9 distinct files — exactly the 9 deploy 2's gate named** (`deploy-p4-2026-09-19.md` §1.2). Nothing outside that list conflicted.

The merge base is `8838dda` (pre-P4 main), confirmed with `git merge-base main archiver/append-0919`. That is what makes every case below a UNION rather than a choice: P4's lines are `HEAD`'s (already on main), the archiver's are `theirs`, and neither side edited the other's.

### 3.4 (a) `src/cobalt/db_migrations/__init__.py` — rule (a), the registry union

- **HEAD (P4)** ended `FORWARD` at `0008_radar_value_movers.sql`, `0009_picks_missed.sql`; `REVERSE` began `0009…rollback`, `0008…rollback`. Docstring: entries for 0008/0009 + "0006/0007 are S2-P2's, 0008/0009 are S2-P4's."
- **theirs (archiver)** ended `FORWARD` at `0010_archive_progress.sql`, `0011_archive_incidents.sql`; `REVERSE` began `0011…`, `0010…`. Docstring: entries for 0010/0011 + "THE 0008/0009 GAP IS DELIBERATE … the branch that lands second rebases and keeps BOTH sets in numeric order."
- **Kept:** the UNION. `FORWARD` = 0001 … 0011, every entry from both sides, numeric order. `REVERSE` = its exact mirror, 0011, 0010, 0009, 0008, 0007 … 0002 (0001 still deliberately absent). Both docstring blocks kept, entries in numeric order; P4's "0006/0007 … 0008/0009" paragraph verbatim. The archiver's gap paragraph is the ONE piece of prose rewritten: its heading sentence said the gap is deliberate and 0008/0009 "are never used here", which stopped being true the moment P4 landed. It now records that the gap is CLOSED and that this branch did what that paragraph said the second lander would do; its mechanical claims (explicit ordered tuple, `_rollback_paths` selects by numeric prefix) are kept, with the worked example restated for `--down-to 0009`. Agent-authored, per CLAUDE.md.
- **Rule (a).** Nothing present on one side is absent from the result.

### 3.4 (b) `src/cobalt/db_migrations/placement.py` — rule (b), union of registrations

- **HEAD (P4)** added to `CREATED_TABLES`: `movers_daily` (SYSTEM), `picks` (USER), `missed` (USER), each with its own `# db_migrations/000X …` comment.
- **theirs (archiver)** added `archive_progress` (SYSTEM) and `archive_incidents` (SYSTEM) with their four-line L32 comment.
- **Kept:** both blocks verbatim, in the order the file already orders things (by migration number): P4's 0008/0009 entries first, the archiver's 0010/0011 entries after them. No line exists on both sides in different wording.
- `DECLARED_TABLES` did not conflict and was not touched: P4 removed `missed` from it when 0009 built the table; the archiver never edited that line, so git applied P4's removal on its own. `missed` therefore appears exactly once in the file, on the CREATED side. **Rule (b).**

### 3.4 (c) `tests/cobalt/test_radar_migration.py` — rule (d), union of expectations

- **HEAD (P4)**, in `test_rollback_selects_only_newer_files_newest_first`: the full literal list `[0009, 0008, 0007, 0006, 0005, 0004]` and `selected == tuple(p for p in REVERSE if int(p.name[:4]) > 3)`.
- **theirs (archiver)**: the same four names asserted as a SUFFIX (`[-4:]`), plus `not any(p.name.startswith(("0003","0002","0001")))` and the same equality written as a prefix-string filter. Its docstring (auto-merged, already in the file) says why: the property is "everything newer than the bound, newest first", never a fixed tuple length.
- **Kept:** every assertion of both sides, plus a HEAD-slice assertion that carries P4's own names forward — `selected[:4] == [0011, 0010, 0009, 0008]`. That is the union of the two sides' enumerations in numeric order, written in the archiver's slice style so the next migration does not have to rewrite it. **No test deleted, no expectation dropped** (L45, L3).

### 3.4 (d) `tests/cobalt/test_radar_score_migration.py` — rule (d), two hunks

1. `test_0006_and_0007_are_registered_forward_in_order`: HEAD asserted 0006/0007 adjacency by name-and-slice; theirs asserted the same adjacency by index and added a docstring saying `FORWARD` no longer ends at 0007. **Kept both assertion forms and the docstring**, the docstring extended by one clause so it names P4's 0008/0009 as well as the archiver's 0010/0011.
2. The rollback-selection test: HEAD renamed it `test_rollback_selects_every_newer_migration_then_0007_then_0006_newest_first` and enumerated `REVERSE[:4]`, `_rollback_paths("0005")` and `_rollback_paths("0006")` in full; theirs kept the base name and asserted adjacency + suffixes + a prefix filter. **Kept:** HEAD's name (the only rename), theirs' docstring, theirs' adjacency/suffix/negative assertions verbatim, and HEAD's enumerations restated as `[:4] == [0011, 0010, 0009, 0008]` against the union — one shared `newest_four` list so the two sides' expectations cannot drift apart.

### 3.4 (e) `tests/cobalt/test_tenancy.py` — rule (d), same shape

`test_down_to_0004_selects_0009_0008_0007_0006_then_0005_reverse` (HEAD's name, kept): HEAD's `names[-1] == "0005…"` and `all(int(name[:4]) > 4 …)` are kept VERBATIM, theirs' suffix `[-3:]` assertion, docstring and `not any(name.startswith(("0004","0003","0002")))` are kept VERBATIM, and HEAD's literal head entries return as `selected[:4] == [0011, 0010, 0009, 0008]`. `names` is bound to `selected` so both sides' lines read exactly as their authors wrote them.

### 3.4 (f) `src/cobalt/archiver/store.py` — rule (c), different code, both kept

- The conflict itself was the import block: HEAD `from .models import Bar, Interval`; theirs split it in two lines and added `from .reconcile import values_from_row`. **Kept:** one `from .models import Bar, Interval` plus theirs' `values_from_row` import — both names imported, no third path.
- The substantive overlap resolved itself and was verified afterwards: P4's `BarStore.bars_between()` (line 142) and the archiver's `BarStore._bars_in_range()` (line 248) are BOTH present, along with `upsert_bars`, `run_lock`, `target_transaction` and `insert_new_bars`. They are different methods and no statement of one was touched by the other. **The archiver's own docstring names this as the L3 fold owed once both land** — see ESCALATE 1; this rebase does not perform that fold, because merging two read methods is a code change, not a conflict resolution.

### 3.4 (g) `src/cobalt/archiver/runner.py` — rule (c), THE ONE THAT MATTERED

- **HEAD (P4)** added a NEW function `_check_demand(targets, mode)` — L53's one shared total-demand gate (S2-P4 R1-13/R2-5) — and one call to it as the FIRST statement of the old `_run_targets`.
- **theirs (archiver)** rewrote `_run_targets` into the write-mode dispatcher (`_now`, `_default_fetch`, `_Withheld`, the §9 lock, `_upsert_targets` / `_append_targets`).
- **Kept:** `_check_demand` verbatim, in place, AND its call as the first statement of the new `_run_targets` — before the settings load, the store, the lock, the token and therefore before anything that can send a request, which is exactly where P4 put it. The two sides touch different statements: P4 INSERTED a statement the archiver never had; the archiver rewrote the statements below it. Dropping the gate would have silently removed an L53 guard from the nightly run, and no test pins its call site (`grep -rn "_check_demand" tests src` → the two lines in `runner.py` only), so the rule, not a test, decided it. The docstring records the placement and why.

### 3.4 (h) `docs/40 - DevDocs/cobalt/archiver/runner.md` — rule (e), prose union

HEAD's `## 2026-09-17 — S2-P4: the L53 total-demand gate before the first request` section and theirs' `## 2026-09-19 — write_mode dispatch` section are BOTH kept, in date order, separated by the file's own `---` rule, every paragraph of both untouched. One new short section, agent-authored, states that both are live and that `_check_demand` is still the first statement of `_run_targets`.

### 3.4 (i) `src/cobalt/cli.py` — rule (b), union of registrations

HEAD registered `replay_cli.add_parser(sub)` and `smoke_cli.add_parser(sub)` (P4's two command groups); theirs registered `archiver_cli.add_parser(sub)` with its comment. **Kept all three**, in that order, so the archiver's block still sits at the end of the group and reflows none of its neighbours as its comment claims. The imports and the `except (QuietRefused, ArchiveLockError)` clause auto-merged; verified present afterwards (`replay_cli` :72, `smoke_cli` :74, `archiver_cli` :80, both archiver exception imports :81–:82, all three registrations :485–:491).

### 3.3 PROOF after the rebase

| check | value |
|---|---|
| `git status --porcelain` | **empty** |
| new tip | **`dd4cd7c`** `docs(report): rebase d3 — ops/2026-09-19 rebased CLEAN onto main (bb996a5, 16/16), offline 1875/0` |
| `git rev-list --count main..HEAD` | **31 → 31.** No commit dropped. (1.2 recorded 29 before this run's own two report commits; the count immediately before `git rebase main` was 31, and it is 31 after.) |
| `grep -n "0008\|0009\|0010\|0011" src/cobalt/db_migrations/__init__.py` | `FORWARD` (75–78) `…0008_radar_value_movers.sql, 0009_picks_missed.sql, 0010_archive_progress.sql, 0011_archive_incidents.sql` after 0001–0007 → **0001…0011 in numeric order**; `REVERSE` (83–86) `0011…rollback, 0010…rollback, 0009…rollback, 0008…rollback` above 0007…0002 → **its exact mirror** |
| leftover markers | `grep -rn "<<<<<<<\|>>>>>>>" src tests` → **none** |

## 4. SUITES ON THE REBASED ARCHIVER BRANCH

### 4.1 OFFLINE — **RED**, 7 failed. The run stops here.

`cd /Users/cobalt/cobalt-wt/archiver-append` → `ls -la .env` → `ls: .env: No such file or directory` → `uv run pytest -q tests/cobalt tests/taxonomy` (background, exit 1).

Summary line, verbatim: **`7 failed, 2131 passed, 346 skipped, 1 xfailed, 15 warnings in 63.44s (0:01:03)`**.

Context, never the bar: pre-rebase this branch closed `1871/0` (319 skipped) with P4 absent; the rebased tree now carries P4's tests too (2131 + 7 = 2138 collected-and-run vs 1871).

**All seven are in files that did NOT conflict** — they auto-merged cleanly and are false only because the registry union and P4's own code are now present. None of §3.2's five rules reaches them: a rule that resolves a conflict does not authorise editing a test that merged clean, and §4.1 makes a red suite a stop, not a repair. **No red is labelled "known" and continued past** (L68, L70).

| # | test | assertion, verbatim | why it is red |
|---|---|---|---|
| 1 | `tests/cobalt/test_archiver_append_store.py:326::test_there_is_exactly_one_range_read_on_the_store` | `assert reads == ["_bars_in_range"]` → `AssertionError: assert ['_bars_in_ra...bars_between'] == ['_bars_in_range']`, `Left contains one more item: 'bars_between'` | THE ARCHIVER'S OWN L3 PIN, firing exactly as written. Its docstring: "`sprint-2/p4` adds `bars_between()` with its own connection; the two fold into one method at integration. Until then this branch adds exactly one." P4 has landed, so the fold is now due. §3.2 (c) kept both methods — merging two read methods into one is a code change, not a conflict resolution |
| 2 | `tests/cobalt/test_archiver_migrations.py:63::test_forward_ends_0007_0010_0011` | `assert [p.name for p in FORWARD[-3:]] == ["0007_radar_cards.sql", "0010_archive_progress.sql", "0011_archive_incidents.sql"]` → `At index 0 diff: '0009_picks_missed.sql' != '0007_radar_cards.sql'` | pins `FORWARD`'s tail as 0007→0010→0011, i.e. the gap. The union puts 0008/0009 between them |
| 3 | `tests/cobalt/test_archiver_migrations.py:71::test_reverse_begins_0011_0010_0007` | `assert [p.name for p in REVERSE[:3]] == ["0011_…rollback.sql", "0010_…rollback.sql", "0007_radar_cards.rollback.sql"]` → `At index 2 diff: '0009_picks_missed.rollback.sql' != '0007_radar_cards.rollback.sql'` | same pin, mirrored |
| 4 | `tests/cobalt/test_archiver_migrations.py:87::test_rollback_down_to_0007_selects_exactly_the_two_new_files` | `assert [p.name for p in _rollback_paths("0007")] == ["0011_…rollback.sql", "0010_…rollback.sql"]` → `Left contains 2 more items, first extra item: '0009_picks_missed.rollback.sql'` | `--down-to 0007` now correctly also reverses P4's 0008/0009. The archiver's own operator-undo bound is `--down-to 0009` after the rebase (the registry docstring already says so) |
| 5 | `tests/cobalt/test_archiver_migrations.py:98::test_the_registry_is_an_explicit_list_so_the_0008_0009_gap_is_legal` | `assert 8 not in numbers and 9 not in numbers` → `assert (8 not in [1, 2, 3, 4, 5, 6, ...])` | asserts the GAP. The gap is closed by design — this test's premise ("P4 owns 0008/0009 on an unmerged branch") expired when P4 shipped |
| 6 | `tests/cobalt/test_archiver_runner.py:334::test_run_targets_mode_still_means_the_report_scope` | `cobalt.radar.notes.TotalDemandExceeded: REFUSED: total Finviz demand for backfill: peak 51.00 rpm at 04:00 ET over [radar, archiver, replay, backfill], finviz_max_rpm=50 exceeds the ceiling` (raised at `notes.py:283`, through `runner.py:132 _check_demand(targets, mode)` → `runner.py:46`) | **NOT a stale pin — a real integration finding.** §3.2 (c) kept P4's L53 gate as the first statement of `_run_targets`; this offline test drives `_run_targets` with `mode="backfill:TESTARCH"`, so the gate runs and the COMBINED inventory (radar 50 + replay + archiver + a 1 rpm backfill) is 51 against `finviz_max_rpm=50`. The archiver DevDoc already carries this as an "Open deployment gate" (plan §8 item 4) |
| 7 | `tests/cobalt/test_p4_migrations.py:157::test_rollback_down_to_0007_reverses_0009_then_0008_only` | `assert [p.name for p in _rollback_paths("0007")] == ["0009_picks_missed.rollback.sql", "0008_radar_value_movers.rollback.sql"]` → `At index 0 diff: '0011_archive_incidents.rollback.sql' != '0009_picks_missed.rollback.sql'` | **P4's own pin**, the mirror image of #4: it asserts `--down-to 0007` reverses P4's pair ONLY. Both branches wrote "only my two", and after the union both are wrong in the same way |

Rows 2–5 and 7 are one defect class with one shape: five enumeration pins, each written when its own branch was the only unmerged one. Row 1 is a named, owed fold. Row 6 needs a RULING (the ceiling or the pacing), not an edit.

### 4.2 WITH-DB — **NOT RUN**, and nothing of it was touched

The gate at 4.1 is red, so (a)–(f) were never entered. **`.env` was never copied**: `cp /Users/cobalt/cobalt/.env …` was not typed in this run, in any spelling. `cobalt_dev` was never connected to, migrated, rolled back or proved — no `COBALT_ENV=dev` command of any kind ran. `ls -la .env` → "No such file or directory" in BOTH worktrees, proven at 15:29 ET, immediately before this line was written (L4).

### 4.3 RESTARTS — not run

`uv run cobalt jobs restarts <main>..HEAD` belongs after 4.2 and was not reached. No derivation is claimed. (L70: nothing here is carried forward as a defect on the strength of "not run".)

## STATE AT THE STOP — verified, not assumed

| thing | state |
|---|---|
| `ops/2026-09-19` | **REBASED onto `19bb1b2` and GREEN.** Tip `bb996a5`, 16/16 commits, tree clean, offline `1875 passed / 0 failed`. Deploy 3's ops leg is ready |
| `archiver/append-0919` | **REBASED, tree clean, 31/31 commits, registry 0001–0011 correct — but its offline suite is red on 7 tests.** The rebase was NOT aborted: the conflicts are resolved and committed, and the reds are a separate, later gate. `git rebase --abort` would throw away nine correct resolutions to leave a branch that still does not merge |
| `main` / `~/cobalt` | **untouched.** `19bb1b2`, nothing merged, nothing pushed, no tag, no `launchctl`, no production command, no vault write |
| `cobalt_dev` | **untouched and free** — never connected to by this run |
| `.env` | absent in both worktrees, proven at 15:29 ET |
| `git rebase --skip` | never typed (not in the allowlist) |

## ESCALATE

1. **The five enumeration pins (reds 2–5, 7) need ONE fix, and it is a build job, not a rebase one.** Each asserts "the registry ends/begins with exactly my two migrations". After the union both branches' versions of that claim are false, and the correct assertion is the one the conflicted tests already got in §3.4 (c)–(e): name the head/tail as slices over the union, never a fixed-length whole-tuple equality. Rows 2–5 sit in the archiver's `test_archiver_migrations.py`; row 7 sits in P4's `test_p4_migrations.py`, **which is now on `main`** — fixing it is an edit to shipped code and needs the desk's word on which branch carries it.
2. **Red 1 is the L3 fold the archiver's own spec named as owed at integration (spec O-5).** `BarStore.bars_between()` (P4, opens its own connection) and `BarStore._bars_in_range()` (archiver, takes one) are both live. The branch's test says there must be exactly one range read once both land. Folding them into one method with an optional connection is the fix the archiver's docstring already prescribes; it is a code change with its own tests and needs a build run.
3. **Red 6 is a real deployment gate, not a test artifact: the COMBINED demand inventory exceeds the ceiling.** With P4's `replay` consumer, the radar's 50 rpm and the archiver's backfill in the same window, the L53 total reaches **51.00 rpm at 04:00 ET against `finviz_max_rpm=50`**. The archiver's DevDoc has carried this as an open gate since 09-17 ("the archiver's own pacing bound exceeds the ceiling … until the ceiling or the pacing is ruled", plan §8 item 4); the rebase has now made it demonstrable in the offline suite. **L53 says the ceiling and the cadence are Dejan's to set and are never settled silently in a config file** — so this one is a RULING owed, and until it is ruled a nightly `full` archiver run under the combined inventory may refuse. Note the failing case is `backfill`, which counts against every window; whether `full` also refuses was not measured (not run, L70).
4. **The registry docstring is the one piece of prose this run rewrote** (§3.4 (a)). The archiver's "THE 0008/0009 GAP IS DELIBERATE" paragraph asserted something that stopped being true when P4 shipped; it now records the gap as CLOSED and keeps every mechanical claim. Agent-authored, flagged here so the houses read it as a resolution and not as a silent edit.
5. **Deploy 3's two legs are now in different states.** `ops/2026-09-19` is rebased and green and could be gated on its own; `archiver/append-0919` is rebased but not green. If tonight's window matters (R32: before 21:00 ET), the ops leg is the part that is ready — but that is a scope decision for the desk and Dejan, not this run's to take.

FAILED: 4.1 — archiver offline suite red on the rebase — tests/cobalt/test_archiver_append_store.py::test_there_is_exactly_one_range_read_on_the_store, tests/cobalt/test_archiver_migrations.py::test_forward_ends_0007_0010_0011, ::test_reverse_begins_0011_0010_0007, ::test_rollback_down_to_0007_selects_exactly_the_two_new_files, ::test_the_registry_is_an_explicit_list_so_the_0008_0009_gap_is_legal, tests/cobalt/test_archiver_runner.py::test_run_targets_mode_still_means_the_report_scope (TotalDemandExceeded, 51.00 rpm vs ceiling 50), tests/cobalt/test_p4_migrations.py::test_rollback_down_to_0007_reverses_0009_then_0008_only — 7 failed / 2131 passed — rebases KEPT: archiver ac5f8a3 (report tip; the rebase's own 31/31 count was proved at `dd4cd7c`, registry 0001–0011; the two commits above it are this report), ops bb996a5 (16/16, offline 1875/0) — cobalt_dev: untouched, never connected — .env: never copied, proven absent in both worktrees at 15:29 ET

---

# CHUNK 2 — the six fixes

## §0 Headline

Five enumeration pins rewritten and the owed L3 fold DONE; offline GREEN but for item 6 (**1 failed / 2138 passed**, the one failure is item 6 by name). **The with-DB suite is RED beyond item 6: 2 failed / 2477 passed.** The second red is a `requires_db` SIXTH member of the same enumeration-pin class that §4.1's table could not see, because §4.1 was built from an OFFLINE run that SKIPS every `requires_db` test. It is not mine to fix — chunk 2's specification names five pins by name and §2.2 (d)'s gate is "exactly ONE failure" — so the run stops there. cobalt_dev is RESTORED and `.env` removed. ESCALATE: 3.

## 1. GATE (1.1) — one recorded deviation, not a stop

| check | expected | found | verdict |
|---|---|---|---|
| `date` | — | `Sat Sep 19 15:33:01 EDT 2026` | — |
| `git status --porcelain` | empty | **empty** | OK |
| `git log --oneline -1` | `12ef535` | **`ea72fcd`** | **DEVIATION, recorded** |
| `ls -la .env` | "No such file" | `ls: .env: No such file or directory` | OK |

**The deviation, proven rather than assumed:** `git diff --stat 12ef535..ea72fcd` = `docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md | 59 +++---`, **one file, the report above this line**. The two commits are `48`'s OWN report commits (`ac5f8a3` the stop line, `ea72fcd` the sha fill) — the prompt's `12ef535` was written before `48` finished appending the report this prompt names as the index card. **No code, test, config or migration differs from `12ef535`**, so the tree the six fixes act on is the tree the prompt specifies. Recorded here, not treated as a stop.

Authorization verified before anything was touched: `git -C /Users/cobalt/cobalt log --oneline -12 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` → **R33** `fde57c2` ("5 new rule strings approved for the deploy-3 rebase hub (48)") and **R32** `5204535` ("L43 overruled for tonight") both committed, plus R25/R26/R31 standing in `cto-2026-09-19.md` §4. LAWS.md read in full (377 lines), INDEX → `areas/cobalt.md` `## NOW`, `UNATTENDED-LAUNCH.md` §2 + §6, `rebase-d3` §3 and §4.1 in full, the rebased registry.

## 2. ITEMS 2, 3, 4, 5, 7 — the five enumeration pins (1.2)

One commit: **`4ddc5e4`** `test(db-migrate): the five enumeration pins assert the united 0001-0011 registry (rebase-d3 chunk 2)`. Proof: `uv run pytest -q tests/cobalt/test_archiver_migrations.py tests/cobalt/test_p4_migrations.py` → **`67 passed, 14 skipped in 0.08s`**.

Every pin now asserts what is TRUE after the union, and every invariant the pin protected is still asserted. **No test deleted, none weakened** (L45, L3).

| # | was | is now | invariant kept |
|---|---|---|---|
| 2 | `test_forward_ends_0007_0010_0011` | **`test_forward_ends_0008_0009_0010_0011`** — `FORWARD[-4:]` = `0008, 0009, 0010, 0011` | 0010/0011 are still the LAST two registered; nothing of this branch was displaced |
| 3 | `test_reverse_begins_0011_0010_0007` | **`test_reverse_begins_0011_0010_0009_0008`** — `REVERSE[:4]`, the exact mirror | this branch's pair still reverses FIRST |
| 4 | `test_rollback_down_to_0007_selects_exactly_the_two_new_files` | **`test_rollback_down_to_0009_undoes_this_branch_alone_and_0007_also_reaches_p4`** — `_rollback_paths("0009")` = exactly `0011, 0010`; `_rollback_paths("0007")` = `0011, 0010, 0009, 0008` | the operator-undo bound for THIS branch, now correctly `--down-to 0009`, is pinned; `0007`'s wider reach is pinned beside it so neither drifts |
| 5 | `test_the_registry_is_an_explicit_list_so_the_0008_0009_gap_is_legal` | **`test_the_registry_is_an_explicit_contiguous_list_and_reverse_mirrors_it`** — numeric order, **no duplicate**, **`numbers == list(range(1, 12))`**, tail still `[10, 11]`, and `REVERSE` == `FORWARD` reversed | the invariant UNDER the gap: an explicit ordered list selected by numeric prefix, never by position |
| 7 | `test_p4_migrations.py::test_rollback_down_to_0007_reverses_0009_then_0008_only` | **`test_rollback_down_to_0007_reverses_everything_above_p2_newest_first`** — all four, newest first | P4's own bound, twice: `_rollback_paths("0009")` contains NOTHING starting `0008`/`0009`, and `_rollback_paths("0008")` = `0011, 0010, 0009` (0008 itself excluded) |

**Correction to the prompt's wording for row 5, stated rather than silently applied:** §1.2 asks the test to assert "`REVERSE` is exactly `FORWARD` reversed". That is literally FALSE and would fail — `0001` has no rollback and is deliberately never reversed (`db_migrations/__init__.py`: "0001 is deliberately NOT reversed"; FORWARD has 11 entries, REVERSE has 10). The test asserts the true form, `reverse_numbers == [n for n in reversed(numbers) if n != 1]`, with the exception named in the assertion message. L45's spirit: the assertion is fitted to the artifact, never the artifact to the assertion.

Also rewritten (agent-authored, flagged so no house reads it as a silent edit): `test_archiver_migrations.py`'s module docstring paragraph "WHY 0010/0011 AND NOT 0008/0009", which asserted that `0008`/`0009` "are never used here". That stopped being true when P4 shipped. It now records that the gap is closed and that this branch did what the paragraph said the second lander would do; every mechanical claim is kept.

## 3. ITEM 1 — the owed L3 fold (1.3), DONE

One commit: **`0efe5a9`** `refactor(archiver): fold bars_between and _bars_in_range into one range read (L3, owed at P4 integration)`. Proof: `uv run pytest -q tests/cobalt/test_archiver_append_store.py tests/cobalt/test_archiver_runner.py` → **`1 failed, 81 passed, 12 skipped in 2.67s`**, the one failure being item 6. Widened to every caller: `… test_replay_runner.py test_replay_movers.py` → **`1 failed, 151 passed, 14 skipped in 9.50s`**, same single failure.

Both methods were read in full first, as instructed. They were the same SELECT differing on exactly THREE axes, each of which is now a parameter instead of a second copy of the statement:

| axis | archiver (`_bars_in_range`, store.py:298) | P4 (`bars_between`, store.py:173) | folded |
|---|---|---|---|
| connection | the CALLER's transaction | opens and closes its own | `conn`, with `None` meaning "open one here" |
| end bound | `ts <= end`, `[start, end]` | `ts < end`, `[start, end)` | `end_inclusive: bool = True` |
| rendering | `{ts: BarValues}`, normalised | `list[Bar]`, oldest first | `as_bars: bool = False` |

Survivor: **`BarStore.bars_in_range(conn, ticker, interval, start, end, *, end_inclusive=True, as_bars=False)`**. Private → public because the fold gave it a caller in another module (`replay`), and the reason the append branch gave for keeping it private — "the unmerged `sprint-2/p4` adds a public one" — resolved the other way when P4 shipped. **The property the pin actually protects, that it takes the connection, is unchanged and still asserted.**

**NEITHER CALLER'S RESULT CHANGED**, which is the condition §1.3 set for proceeding rather than stopping:
- The archiver's three call sites (`archiver/runner.py:277`, `archiver/shadow.py:155`, `archiver/cli.py:351`) pass their own connection and receive the same `{ts: BarValues}` over the same closed range. This is load-bearing, not style: §8's pre-commit re-check compares against rows read inside the repair's `target_transaction`, and a read on another connection cannot see them.
- Replay's three call sites (`replay/runner.py` ×2, `replay/movers.py`) pass `conn=None` — they own no transaction — and receive the same `list[Bar]` over the same half-open window.
- The SQL now always selects `ticker, interval` as well, so one row shape serves both renderings. **The dict is built from the same columns it was built from before**; the extra two columns are projected away for that caller.
- Considered and REJECTED: making `list[Bar]` the single return and deriving the dict from it. `Bar` carries `@field_validator("ts") _to_utc`, so the dict's keys would change representation (equal as instants, but a different rendering in any incident `detail`) — that IS a change in what a caller receives, and §1.3 says that is a design call, not mine.

Updated to the one surviving name, mechanically and with no expectation changed: the pin (`test_there_is_exactly_one_range_read_on_the_store`, whose docstring now records the fold as DONE), the four other `bars_in_range` pins, the runner's fake store, and the two replay test doubles — the doubles now also ASSERT `(conn, end_inclusive, as_bars) == (None, False, True)`, so replay's half of the contract is pinned where it is consumed. Two DevDocs corrected, since they described a method that no longer exists: `cobalt/archiver/store.md` (new dated section with the three-axis table) and `cobalt/archiver/shadow.md` (one sentence).

## 4. ITEM 6 — NOT TOUCHED, and the measurement the desk asked for (1.4)

`test_archiver_runner.py::test_run_targets_mode_still_means_the_report_scope` **stays RED, by ruling**. `finviz_max_rpm`, every consumer's cadence, every window, the gate and the test are **byte-for-byte unchanged** — `git show --stat` on both commits carries no `radar/notes.py`, no `tunables.yaml`, and the test file's only diff is the `_bars_in_range` → `bars_in_range` rename of the fake store.

**MEASURED — the `full` nightly mode does NOT exceed the ceiling; only `backfill` does.** `rebase-d3` §ESCALATE 3 recorded this as never measured (L70); it is measured now, read-only.

- No `-k "full"` test exists (`uv run pytest -q tests/cobalt/test_archiver_runner.py -k "full"` → `46 deselected`), so the measurement is the shipped gate's own suite plus the code path.
- `runner.py:38` — `if mode == "full": check_scheduled_demand("archiver"); return`. The `full` path adds **no** extra consumer. The non-`full` path adds `DemandConsumer(name="backfill", rpm=…, window=None)`.
- **`window=None` is the whole difference.** `total_demand` counts a windowless consumer against EVERY window, so the backfill lands on the radar's 04:00 peak: radar 50.0 + backfill 1.0 = **51.00 rpm at 04:00 ET**, refused.
- For `full` the subject is `archiver`, whose window is **20:30–21:10**, disjoint from the radar's 04:00–20:00 and from replay's 21:10–21:35. It counts only itself: 60 / 1.2 = **50.00 rpm, exactly the ceiling**, and `peak_rpm > ceiling` is strict, so it PASSES.
- Proof run, green: `uv run pytest -q tests/cobalt/test_replay_demand.py -k "r17"` → **`4 passed, 5 deselected in 0.36s`**, including `test_r17_every_scheduled_subject_passes_against_the_shipped_ceiling_of_50` (`archiver.peak_rpm == 50.0`, `<= 50`) and `test_r17_the_shipped_archiver_and_replay_windows_are_disjoint`.

**Note for the desk, learned at 2.3:** while this chunk ran, Dejan ruled **R34 "B"** (`~/cobalt` `96163c2`, 96163c2 → chunk 3 `50` at `89079ae`) — ceiling stays 50, the backfill is bounded out of 04:00–09:30. That is the L53 ruling item 6 was waiting for, and it matches this measurement: the fix belongs on the backfill's window, not on the ceiling and not on `full`.

## 5. BOTH SUITES

### 5.1 OFFLINE (2.1) — **GATE MET**

`cd /Users/cobalt/cobalt-wt/archiver-append` → `ls -la .env` → `ls: .env: No such file or directory` → `uv run pytest -q tests/cobalt tests/taxonomy` (background, exit 1).

Summary line, verbatim: **`1 failed, 2138 passed, 346 skipped, 1 xfailed, 15 warnings in 62.46s (0:01:02)`**.

Short summary, verbatim: `FAILED tests/cobalt/test_archiver_runner.py::test_run_targets_mode_still_means_the_report_scope - cobalt.radar.notes.TotalDemandExceeded: REFUSED: total Finviz demand for ba...`

**Exactly ONE failure and it is item 6 by name.** Six red at 4.1 → one red here, and the 2131 passing became 2138. Item 6 did NOT vanish, so the L53 gate is still firing as designed.

### 5.2 WITH-DB on `cobalt_dev` (2.2) — **(d) GATE NOT MET**

Production was never touched; `--allow-prod` was never typed in any spelling; `~/cobalt` was never written to, merged into, pushed or tagged.

| step | command | result |
|---|---|---|
| (a) | `cp /Users/cobalt/cobalt/.env …` → `ls -la .env` | `-rw------- 1 cobalt staff 2186 Sep 19 15:43 .env` |
| (b) | BASELINE `--proof-only` | 28 tables on `cobalt_dev`; P4's `movers_daily`/`picks`/`missed` PRESENT; **`archive_progress` and `archive_incidents` ABSENT** (schema `-`, rows `-`). `cobalt_redactions` 129 rows |
| (c) | `COBALT_ENV=dev uv run cobalt db migrate` | applied 0001…0011 in order; **`archive_incidents` CREATED, `archive_progress` CREATED**, every other table `OK`, **no `CHANGED`**; "content UNCHANGED on every table" |
| (d) | `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` | **`2 failed, 2477 passed, 6 skipped, 1 xfailed, 15 warnings in 201.96s (0:03:21)`** — **STOP** |
| (e) | POST-SUITE `--proof-only` | every table present; **no table vanished**; the only movement from (b) is `cobalt_redactions` **129 → 130** (`9b686308` → `333b384e`) — the suite's own write, RECORDED, never a stop (R31 run-3 fold) |
| (f) | `--rollback --down-to 0009` | applied **exactly** `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` **and nothing else**; both tables `DROPPED`, every other table `OK`. Post-restore `--proof-only` compared **against (e)**: every digest identical, `cobalt_redactions` 130/`333b384e` on both sides. **No digest moved ACROSS the restore** |
| (g) | `rm …/.env` → `ls -la .env` | `ls: .env: No such file or directory` — done BEFORE this stop line |

**The two failures at (d):**

| test | assertion, verbatim | reading |
|---|---|---|
| `tests/cobalt/test_archiver_runner.py::test_run_targets_mode_still_means_the_report_scope` | `cobalt.radar.notes.TotalDemandExceeded: REFUSED: … peak 51.00 rpm at 04:00 ET … finviz_max_rpm=50` | **item 6**, expected, red by ruling |
| `tests/cobalt/test_archiver_migrations.py::test_rollback_down_to_0007_drops_exactly_those_two_and_nothing_else` | `AssertionError: picks was dropped by a rollback that does not own it` | **NOT expected — the sixth enumeration pin**, see ESCALATE 1 |

### 5.3 RESTARTS (2.3)

`uv run cobalt jobs restarts 19bb1b2..HEAD` (`19bb1b2` = `git merge-base main HEAD`, the post-P4 `main` this branch was rebased onto and the tip in force at 1.1). 52 paths classified, **0 UNCLASSIFIED** (L42). Last line, verbatim:

`RESTARTS: com.cobalt.aset com.cobalt.radar`

Rules that fired: `resident reads` (`configs/cobalt/taxonomy/tunables.yaml`), `static import reach` (20 `src/` modules), `DOCS → -` (17 paths), `non-Python src asset → -` (the four migration `.sql` files), `test/documentation; no resident → -` (14 test files).

## ESCALATE

1. **THE STOP: there is a SIXTH enumeration pin, and it is `requires_db`, which is why §4.1 never saw it.** `test_archiver_migrations.py::test_rollback_down_to_0007_drops_exactly_those_two_and_nothing_else` applies `FORWARD`, snapshots the surviving tables, applies `_rollback_paths("0007")`, and asserts every non-archiver table survived. After the union `--down-to 0007` correctly ALSO reverses P4's 0008/0009, which drops `"user".picks` and `"user".missed` — so the assertion fires on `picks`. **This is the same defect class and the same shape as rows 2–5 and 7**: a pin written while its own branch was the only unmerged one, asserting "`--down-to 0007` is MY bound". §4.1's seven-row table could not contain it because §4.1 was produced by an OFFLINE run, and every `requires_db` test is SKIPPED there (346 skipped offline vs 6 with the DB). **It is not fixed here**: chunk 2's specification names five pins by name, §2.2 (d)'s gate is "exactly ONE failure", and this lane feeds a production deploy tonight where L67's floor wants a house other than the author on any change nobody specified. The fix is mechanical and is the same rewrite rule §1.2 already gives — the bound this test should now assert is `_rollback_paths("0009")`, with the `--down-to 0007` case kept as a second assertion that P4's pair IS dropped and everything at or below 0007 is not. **A one-line addition to a chunk-3 prompt closes it.** Flagged as a class question, not a one-off: any other `requires_db` test that pins a rollback bound or a registry enumeration is red for the same reason and is invisible to every offline run.
2. **`git rev-list --count main..HEAD` is now measured against a MOVING `main`.** `main` advanced from `19bb1b2` to `89079ae` DURING this run (three desk docs commits: `a12c8df` chunk 2, `96163c2` R34, `89079ae` chunk 3). All three are `docs/` only, so nothing this branch was proved against changed — but the next chunk's 1.1 gate will see a different `main` tip again, and any prompt that hard-codes one will false-stop the way 1.1 did here. **Recommend: gates name `git merge-base main HEAD`, not a literal sha.**
3. **Item 6 is now RULED (R34 "B") and this chunk's measurement agrees with the ruling.** The ceiling stays 50 and the backfill is bounded out of 04:00–09:30 — which is precisely where the 51.00 rpm comes from, and why `full` at 20:30–21:10 was never affected. Chunk 3 (`50`) implements it. Nothing in this chunk needs revisiting for it; the L53 finding in `rebase-d3` §ESCALATE 3 can be closed as measured and ruled.

FAILED: 2.2 (d) — the with-DB suite is red beyond item 6 — `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → `2 failed, 2477 passed, 6 skipped, 1 xfailed, 15 warnings in 201.96s` — the unexpected red is `tests/cobalt/test_archiver_migrations.py::test_rollback_down_to_0007_drops_exactly_those_two_and_nothing_else` (`AssertionError: picks was dropped by a rollback that does not own it`), a `requires_db` SIXTH member of the five-pin enumeration class that §4.1's offline table could not see and that chunk 2's specification does not name — cobalt_dev: RESTORED to 0001–0009 by `--rollback --down-to 0009` (exactly 0011 then 0010, nothing else), post-restore proof identical to the post-suite proof, the only movement across the whole run being the suite's own `cobalt_redactions` 129 → 130 — .env: removed, proven gone
