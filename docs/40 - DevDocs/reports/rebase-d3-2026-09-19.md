# REBASE D3 — `ops/2026-09-19` + `archiver/append-0919` onto the post-P4 `main` (2026-09-19)

Build hub `rebase-d3-0919`, prompt `docs/40 - DevDocs/prompts/2026-09-19/48-rebase-deploy3.md`, Opus 5.
This lane deploys NOTHING: no production command, no merge into `~/cobalt`, no push, no `launchctl`, no vault write.

## §0 Headline

- PREFLIGHT green at 15:18 ET: authorization row R33 committed, P4 proven on `main` (`19bb1b2`, tag `deploy-2026-09-19b` → `5b58b7b`), both worktrees clean, no `.env` anywhere.
- Steps 2–4 pending.

## AUTHORIZATION — verified before anything was touched

| check | command | result |
|---|---|---|
| the row naming THIS file is committed | `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` | newest commit **`fde57c2 docs(desk): 09-19 R33 — 5 new rule strings approved for the deploy-3 rebase hub (48)`** — the row is committed, not working-tree-only |
| the row's content | `grep -n "48-rebase-deploy3" ".../reports/cto-2026-09-19.md"` | line 47 = **R33**, 15:0x ET, "Approved", naming `prompts/2026-09-19/48-rebase-deploy3.md`, its 29 rule strings (24 already approved today; FIVE new: `git rebase --abort`, `git -C /Users/cobalt/cobalt show*`, and the three exact `COBALT_ENV=dev … db migrate` strings replacing `44`'s wildcard) and the NEVER block (production, `--allow-prod`, push, `launchctl`, vault write, `bypassPermissions`, `git rebase --skip`). Line 665 records the launch. |
| standing rows | same file | R25 (REVOKE/DB-1), R26 (dropped twin/DB-2), R31 (narrowed deploy-2 gate), R32 (L43 overruled tonight) present; `cto-2026-09-18.md` R18–R21 (L67) read in the archiver build report's own authorization table |

Verdict: **authorization matches this file.** Proceeding.

## PREFLIGHT

Run 15:18–15:19 ET, before any rebase. Every probe is the bare shape of the rule it probes.

| rule | command | exit | verdict |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | allowed — `Sat Sep 19 15:18:29 EDT 2026` |
| `Bash(git -C /Users/cobalt/cobalt log*)` | `git -C /Users/cobalt/cobalt log --oneline -3` | 0 | allowed — tip `19bb1b2 docs(desk): 09-19 DEPLOY 2 LIVE …` |
| `Bash(git -C /Users/cobalt/cobalt status*)` | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | allowed — two untracked `31-packet/*.yaml` only (the deploy report's ESCALATE 5b; never `git add`ed, untouched here) |
| `Bash(git -C /Users/cobalt/cobalt show*)` (new, R33) | `git -C /Users/cobalt/cobalt show --stat deploy-2026-09-19b` | 0 | allowed — tag resolves to **`5b58b7b`** |
| `Bash(git status*)` | `git status --porcelain` in both worktrees | 0 | allowed — **empty in both** |
| `Bash(git log*)` | `git log --oneline -1` in both worktrees | 0 | allowed — tips below |
| `Bash(ls *)` | `ls -la .env` in both worktrees | 1 | allowed — **"No such file or directory" in both** |
| `Bash(cd *)` | `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` | 0 | allowed |
| (no explicit rule; read-only, allowed by the classifier) | `git rev-list --count main..HEAD`, `git -C /Users/cobalt/cobalt rev-parse --short HEAD` | 0 | allowed — recorded here because they are not named rule strings; both are pure reads |

Not preflighted, first use is the real use: `git rebase main`, `GIT_EDITOR=true git rebase --continue`, `git rebase --abort`, `git add/commit/diff/show`, `uv run pytest`, `uv run cobalt jobs restarts`, the `.env` `cp`/`rm`, and the three `COBALT_ENV=dev` commands — none has a harmless variant inside its own pattern (UNATTENDED-LAUNCH §6).

### 1.1 / 1.2 / 1.3 — the gate

| fact | value |
|---|---|
| `<main>` | **`19bb1b2`** (`git -C /Users/cobalt/cobalt rev-parse --short HEAD`) |
| P4 on main | **YES** — `git -C /Users/cobalt/cobalt log --oneline -40` carries the `sprint-2/p4` range (`d3aa084 fix(s2-p4) …` through `5b58b7b docs(report): s2-p4 round-2 db run stop line — P4 DB 9045260 …`), and `9562dc3 docs(deploy): P4 DEPLOY DONE 5b58b7b …` above them |
| tag `deploy-2026-09-19b` | resolves — commit `5b58b7ba95c48209e8a4419464fe913120708452`, 1 file, `s2-p4-verify-2026-09-19.md` |
| `archiver/append-0919` tip (before) | **`99039f7`** `docs(report): archiver db re-run — the stop line (db 161/0, DB-1 PASSED/PASSED, cobalt_dev back at 0001-0009)` · `git rev-list --count main..HEAD` = **29** |
| `ops/2026-09-19` tip (before) | **`a868a69`** `docs(report): ops 0919 db run — R1/R11 settled on cobalt_dev, with-DB suite green 61/0, ESCALATE 12` · `git rev-list --count main..HEAD` = **16** |
| both worktrees clean | yes, `git status --porcelain` empty in both |
| `.env` | absent in both worktrees — no other lane holds `cobalt_dev` through these trees |
| re-land check (UNATTENDED-LAUNCH §6) | neither branch is an ancestor of `main`: both counts are non-zero (16 and 29), so `git rebase main` has commits to replay. Deploy 2 shipped **P4 alone** (R31), so `ops/2026-09-19` is genuinely unmerged |

**GATE: proven.**

## 2. OPS REBASE — CLEAN, as deploy 2 predicted

| step | result |
|---|---|
| `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` | ok |
| `git rebase main` | **CLEAN** — `Rebasing (1/16)` … `Rebasing (16/16)`, `Successfully rebased and updated refs/heads/ops/2026-09-19.` **No conflict, no stop** |
| new tip | **`bb996a5`** `docs(report): ops 0919 db run — R1/R11 settled on cobalt_dev, with-DB suite green 61/0, ESCALATE 12` (was `a868a69`) |
| `git rev-list --count main..HEAD` | **16 → 16** — no commit dropped, no commit added |
| `git status --porcelain` | **empty** |

### 2.3 Offline suite on the rebased ops branch — GREEN

`cd /Users/cobalt/cobalt-wt/ops-2026-09-19` → `ls -la .env` → `ls: .env: No such file or directory` → `uv run pytest -q tests/cobalt tests/taxonomy` (background, exit 0).

Summary line, verbatim: **`1875 passed, 329 skipped, 1 xfailed, 15 warnings in 57.01s`** — **0 failed**.

Context, not the bar: this branch's own pre-rebase close was `1608/0` (302 skipped). The +267 passed are P4's own tests, which the branch now sits on top of.

## 3. ARCHIVER REBASE — 9 files, 4 commits, every resolution by rule

`cd /Users/cobalt/cobalt-wt/archiver-append` → `git rebase main`. It stopped FOUR times, on four of the branch's own commits. `git rebase --skip` was never typed and is not in this session's allowlist.

| # | commit it stopped on | conflicted files |
|---|---|---|
| 1 | `b623e72 feat(db): 0010 archive_progress + 0011 archive_incidents …` | `db_migrations/__init__.py`, `db_migrations/placement.py`, `test_radar_migration.py`, `test_radar_score_migration.py`, `test_tenancy.py` |
| 2 | `4e3c577 feat(archiver): append path — insert_new_bars, progress and incident writers …` | `archiver/store.py` |
| 3 | `2b4893c feat(archiver): write_mode dispatch — upsert untouched, pre-write shadow compare …` | `archiver/runner.py`, `docs/40 - DevDocs/cobalt/archiver/runner.md` |
| 4 | `f4bdcfa feat(archiver): repairs only in a quiet window (R8 option A) …` | `cli.py` |

**9 distinct files — exactly the 9 deploy 2's gate named** (`deploy-p4-2026-09-19.md` §1.2). Nothing outside that list conflicted.

The merge base is `8838dda` (pre-P4 main), confirmed with `git merge-base main archiver/append-0919`. That is what makes every case below a UNION rather than a choice: P4's lines are `HEAD`'s (already on main), the archiver's are `theirs`, and neither side edited the other's.

### 3.4 (a) `src/cobalt/db_migrations/__init__.py` — rule (a), the registry union

- **HEAD (P4)** ended `FORWARD` at `0008_radar_value_movers.sql`, `0009_picks_missed.sql`; `REVERSE` began `0009…rollback`, `0008…rollback`. Docstring: entries for 0008/0009 + "0006/0007 are S2-P2's, 0008/0009 are S2-P4's."
- **theirs (archiver)** ended `FORWARD` at `0010_archive_progress.sql`, `0011_archive_incidents.sql`; `REVERSE` began `0011…`, `0010…`. Docstring: entries for 0010/0011 + "THE 0008/0009 GAP IS DELIBERATE … the branch that lands second rebases and keeps BOTH sets in numeric order."
- **Kept:** the UNION. `FORWARD` = 0001 … 0011, every entry from both sides, numeric order. `REVERSE` = its exact mirror, 0011, 0010, 0009, 0008, 0007 … 0002 (0001 still deliberately absent). Both docstring blocks kept, entries in numeric order; P4's "0006/0007 … 0008/0009" paragraph verbatim. The archiver's gap paragraph is the ONE piece of prose rewritten: its heading sentence said the gap is deliberate and 0008/0009 "are never used here", which stopped being true the moment P4 landed. It now records that the gap is CLOSED and that this branch did what that paragraph said the second lander would do; its mechanical claims (explicit ordered tuple, `_rollback_paths` selects by numeric prefix) are kept, with the worked example restated for `--down-to 0009`. Agent-authored, per CLAUDE.md.
- **Rule (a).** Nothing present on one side is absent from the result.

### 3.4 (b) `src/cobalt/db_migrations/placement.py` — rule (b), union of registrations

- **HEAD (P4)** added to `CREATED_TABLES`: `movers_daily` (SYSTEM), `picks` (USER), `missed` (USER), each with its own `# db_migrations/000X …` comment.
- **theirs (archiver)** added `archive_progress` (SYSTEM) and `archive_incidents` (SYSTEM) with their four-line L32 comment.
- **Kept:** both blocks verbatim, in the order the file already orders things (by migration number): P4's 0008/0009 entries first, the archiver's 0010/0011 entries after them. No line exists on both sides in different wording.
- `DECLARED_TABLES` did not conflict and was not touched: P4 removed `missed` from it when 0009 built the table; the archiver never edited that line, so git applied P4's removal on its own. `missed` therefore appears exactly once in the file, on the CREATED side. **Rule (b).**

### 3.4 (c) `tests/cobalt/test_radar_migration.py` — rule (d), union of expectations

- **HEAD (P4)**, in `test_rollback_selects_only_newer_files_newest_first`: the full literal list `[0009, 0008, 0007, 0006, 0005, 0004]` and `selected == tuple(p for p in REVERSE if int(p.name[:4]) > 3)`.
- **theirs (archiver)**: the same four names asserted as a SUFFIX (`[-4:]`), plus `not any(p.name.startswith(("0003","0002","0001")))` and the same equality written as a prefix-string filter. Its docstring (auto-merged, already in the file) says why: the property is "everything newer than the bound, newest first", never a fixed tuple length.
- **Kept:** every assertion of both sides, plus a HEAD-slice assertion that carries P4's own names forward — `selected[:4] == [0011, 0010, 0009, 0008]`. That is the union of the two sides' enumerations in numeric order, written in the archiver's slice style so the next migration does not have to rewrite it. **No test deleted, no expectation dropped** (L45, L3).

### 3.4 (d) `tests/cobalt/test_radar_score_migration.py` — rule (d), two hunks

1. `test_0006_and_0007_are_registered_forward_in_order`: HEAD asserted 0006/0007 adjacency by name-and-slice; theirs asserted the same adjacency by index and added a docstring saying `FORWARD` no longer ends at 0007. **Kept both assertion forms and the docstring**, the docstring extended by one clause so it names P4's 0008/0009 as well as the archiver's 0010/0011.
2. The rollback-selection test: HEAD renamed it `test_rollback_selects_every_newer_migration_then_0007_then_0006_newest_first` and enumerated `REVERSE[:4]`, `_rollback_paths("0005")` and `_rollback_paths("0006")` in full; theirs kept the base name and asserted adjacency + suffixes + a prefix filter. **Kept:** HEAD's name (the only rename), theirs' docstring, theirs' adjacency/suffix/negative assertions verbatim, and HEAD's enumerations restated as `[:4] == [0011, 0010, 0009, 0008]` against the union — one shared `newest_four` list so the two sides' expectations cannot drift apart.

### 3.4 (e) `tests/cobalt/test_tenancy.py` — rule (d), same shape

`test_down_to_0004_selects_0009_0008_0007_0006_then_0005_reverse` (HEAD's name, kept): HEAD's `names[-1] == "0005…"` and `all(int(name[:4]) > 4 …)` are kept VERBATIM, theirs' suffix `[-3:]` assertion, docstring and `not any(name.startswith(("0004","0003","0002")))` are kept VERBATIM, and HEAD's literal head entries return as `selected[:4] == [0011, 0010, 0009, 0008]`. `names` is bound to `selected` so both sides' lines read exactly as their authors wrote them.

### 3.4 (f) `src/cobalt/archiver/store.py` — rule (c), different code, both kept

- The conflict itself was the import block: HEAD `from .models import Bar, Interval`; theirs split it in two lines and added `from .reconcile import values_from_row`. **Kept:** one `from .models import Bar, Interval` plus theirs' `values_from_row` import — both names imported, no third path.
- The substantive overlap resolved itself and was verified afterwards: P4's `BarStore.bars_between()` (line 142) and the archiver's `BarStore._bars_in_range()` (line 248) are BOTH present, along with `upsert_bars`, `run_lock`, `target_transaction` and `insert_new_bars`. They are different methods and no statement of one was touched by the other. **The archiver's own docstring names this as the L3 fold owed once both land** — see ESCALATE 1; this rebase does not perform that fold, because merging two read methods is a code change, not a conflict resolution.

### 3.4 (g) `src/cobalt/archiver/runner.py` — rule (c), THE ONE THAT MATTERED

- **HEAD (P4)** added a NEW function `_check_demand(targets, mode)` — L53's one shared total-demand gate (S2-P4 R1-13/R2-5) — and one call to it as the FIRST statement of the old `_run_targets`.
- **theirs (archiver)** rewrote `_run_targets` into the write-mode dispatcher (`_now`, `_default_fetch`, `_Withheld`, the §9 lock, `_upsert_targets` / `_append_targets`).
- **Kept:** `_check_demand` verbatim, in place, AND its call as the first statement of the new `_run_targets` — before the settings load, the store, the lock, the token and therefore before anything that can send a request, which is exactly where P4 put it. The two sides touch different statements: P4 INSERTED a statement the archiver never had; the archiver rewrote the statements below it. Dropping the gate would have silently removed an L53 guard from the nightly run, and no test pins its call site (`grep -rn "_check_demand" tests src` → the two lines in `runner.py` only), so the rule, not a test, decided it. The docstring records the placement and why.

### 3.4 (h) `docs/40 - DevDocs/cobalt/archiver/runner.md` — rule (e), prose union

HEAD's `## 2026-09-17 — S2-P4: the L53 total-demand gate before the first request` section and theirs' `## 2026-09-19 — write_mode dispatch` section are BOTH kept, in date order, separated by the file's own `---` rule, every paragraph of both untouched. One new short section, agent-authored, states that both are live and that `_check_demand` is still the first statement of `_run_targets`.

### 3.4 (i) `src/cobalt/cli.py` — rule (b), union of registrations

HEAD registered `replay_cli.add_parser(sub)` and `smoke_cli.add_parser(sub)` (P4's two command groups); theirs registered `archiver_cli.add_parser(sub)` with its comment. **Kept all three**, in that order, so the archiver's block still sits at the end of the group and reflows none of its neighbours as its comment claims. The imports and the `except (QuietRefused, ArchiveLockError)` clause auto-merged; verified present afterwards (`replay_cli` :72, `smoke_cli` :74, `archiver_cli` :80, both archiver exception imports :81–:82, all three registrations :485–:491).

### 3.3 PROOF after the rebase

| check | value |
|---|---|
| `git status --porcelain` | **empty** |
| new tip | **`dd4cd7c`** `docs(report): rebase d3 — ops/2026-09-19 rebased CLEAN onto main (bb996a5, 16/16), offline 1875/0` |
| `git rev-list --count main..HEAD` | **31 → 31.** No commit dropped. (1.2 recorded 29 before this run's own two report commits; the count immediately before `git rebase main` was 31, and it is 31 after.) |
| `grep -n "0008\|0009\|0010\|0011" src/cobalt/db_migrations/__init__.py` | `FORWARD` (75–78) `…0008_radar_value_movers.sql, 0009_picks_missed.sql, 0010_archive_progress.sql, 0011_archive_incidents.sql` after 0001–0007 → **0001…0011 in numeric order**; `REVERSE` (83–86) `0011…rollback, 0010…rollback, 0009…rollback, 0008…rollback` above 0007…0002 → **its exact mirror** |
| leftover markers | `grep -rn "<<<<<<<\|>>>>>>>" src tests` → **none** |

CONTINUE: step 4
