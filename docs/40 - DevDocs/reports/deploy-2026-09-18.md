# Deploy 2026-09-18 — DEPLOY 1 hub `deploy1-0918` (Opus 5, `06-deploy-1-ops.md`)

## §0 Headline (11:13 ET)

- `DO NOT STOP until DEPLOY 1 DONE` — **this run never started. FAILED PREFLIGHT at P8; nothing merged, nothing applied, both residents UP, production untouched.**
- **Blocker: `cobalt heartbeat show` is RED right now** — `RED radar failed_stage bars: poll failures: 1; poll THCH stale since 2026-09-18T14:52:29`. P8's rule is absolute: a RED that exists BEFORE the run → `FAILED PREFLIGHT: production not green`.
- Authorization VERIFIED before any command: R3 `5e68daa`, R4 `146cf10`, R5 `cff63bd` all committed on main; both settings sha256 match R4 byte for byte; every rule in the launch line is covered by R4.
- Everything else is green and ready: P1–P7, P9–P11 all ALLOWED, ops tip `b1b3045` clean, sheet 200, both residents running (aset 55521 / radar 55534), radar cycling every 3 min with no traceback.
- ESCALATE: 5 (item 1 is the blocker and needs his ruling — the R3 daytime cadence and this gate collide).

## PREFLIGHT

Ran 11:11–11:13 ET from `/Users/cobalt/cobalt`. **Zero denials — the allowlist is correct and complete.**

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P1 | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | allowed — ` M configs/cobalt/rules.yaml`, ` M docs/40 - DevDocs/reports/seat-usage.md` (the two expected machine-written files; L51-2 commit not reached, they are still dirty) |
| P2 | `git … tag scratch-allow-probe-0918` then `tag -d` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-0918' (was cff63bd)` |
| P3 | `git … commit --allow-empty` then `reset --soft HEAD~1` | 0 / 0 | allowed — `[main 7a344e7]` created, then `log --oneline -1` → `cff63bd` — HEAD is back |
| P4 | `COBALT_ENV=production uv run cobalt validate` | **0** | allowed — 13 trade_defs OK, `Step-downs: … 6 row(s)` (no `trade_count_over_band` — the pre-merge state, as expected), `Placement: tree clean` |
| P5 | `COBALT_ENV=production uv run cobalt backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 13.5 h old` |
| P6 | `COBALT_ENV=production uv run cobalt backup run --dry-run` | 0 | allowed — `cobalt_brain via pg_dump inside cobalt_memory — 613.6 MB`, `DRY RUN — nothing written` |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..ops/2026-09-18` | **1** | allowed — exit 1 is EXPECTED pre-merge, see ESCALATE 2 |
| P8 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — **`HEARTBEAT RED — 1 probe(s)`. GATE FAILED.** |
| P9 | `launchctl print gui/501/com.cobalt.aset` · `…/com.cobalt.radar` | 0 / 0 | allowed — both `state = running`; **aset pid 55521** (runs 16), **radar pid 55534** (runs 9) |
| P10 | `curl … http://127.0.0.1:5010/` | 0 | allowed — `200` |
| P11 | `shasum -a 256` of the two forward files | 0 | allowed — **both match R4 exactly** (table below) |

**P11 — the approved settings input, re-proven at 11:12 ET:**

| file | sha256 | vs R4 |
|---|---|---|
| `…/ops-2026-09-18/scratch/daymode-settings-0918/daymode.yaml` | `daa7bb720f19b60a438f3366aa7327188842d69f5fe0da343fa6bbbe7b3d5ebb` | MATCH |
| `…/ops-2026-09-18/scratch/daymode-settings-0918/aset.yaml` | `8eca6945087170312a11709a4c5833a9d36d298798343ea1af39265a6eb99d58` | MATCH |

**0.2 GATE: proven** — commit, tag and `reset --soft` are all allowed by the session allowlist (P2, P3), with HEAD restored. L29 satisfied.

**0.3 ROLLBACK DIRECTORY — deliberately NOT built.** Nothing was written to `data/backups/pre-ops-0918/`. Its gate (`0 setting(s) would change`) proves the directory equals production *at the moment it runs*; that proof cannot be inherited by a later session. The run that deploys builds it and proves it fresh. See ESCALATE 5.

## Preconditions

Recorded for the rerun — step 1 was not formally entered (the PREFLIGHT gate stops first), but these reads are cheap and they all pass, so the rerun's only open question is the RED.

| check | result |
|---|---|
| 1.1 clock | `Fri Sep 18 11:12:56 EDT 2026` — NOT inside 19:45–21:40 ET. Clear. |
| 1.2 "done trading" | R5, 11:09 ET, committed `cff63bd`. Present. |
| 1.3 ops tip | `git -C …/ops-2026-09-18 rev-parse --short HEAD` → **`b1b3045`** = expected; `status --porcelain` **empty**. Clean. |
| 1.4 no other merger | main tip `cff63bd` = a desk docs commit. No other deploy session merging. |
| 1.5 authorization | R3 `5e68daa` · R4 `146cf10` · R5 `cff63bd` — all three rows committed, hashes match, allowlist covered. |

## Deploy table

**NOT REACHED.** No `bootout`, no `merge --ff-only`, no `settings load`, no `bootstrap`, no `kickstart`. `ops/2026-09-18` is unmerged at `b1b3045`; main is unchanged at `cff63bd`; no tag created; no snapshot taken (2.2 not reached, so the ops ESCALATE 4 pruning problem is not yet in play).

Production state at stop = production state at start: both residents UP on their original pids, `trader_settings` untouched, live code unchanged.

## Smoke

**NOT REACHED** — and this is the substantive reason the gate matters, not just the formal one. §5's radar gate requires `failed_stage` **empty**. It is `bars` right now, before anything is deployed:

```
last_scan_at                        members   failed_stage
2026-09-18 15:10:32.006236+00:00    50        bars
```

Had this run proceeded, its own smoke test could not have passed, and any RED after the restart would have been impossible to attribute to the deploy or to the pre-existing condition. Stopping is the letter of P8 and the substance of NN#16 both.

## ESCALATE

1. **BLOCKER, needs his ruling: the P8 green-gate and R3's daytime cadence collide.** The RED is `radar failed_stage bars: poll failures: 1; poll THCH stale since 2026-09-18T14:52:29+00:00` (10:52 ET). It is **not** a fault in the radar: `radar.err` shows clean 3-minute cycles straight through 11:11:52 with no traceback, 50 members, `last_scan_at` fresh. It is one member whose bar poll is stale, which sets `failed_stage=bars`, which the heartbeat renders RED. The same probe self-cleared for ILAG at 11:01:44 (`dropped carried failure record(s) for ILAG — not a member this poll`), so THCH will likely clear the same way on a scan that drops it from the pool. **But:** `grep -n "poll failures" logs/heartbeat.log` shows this RED class recurring on every trading day back to 09-14. Until 09-18 deploys ran in the evening pause, when the pool is quiet; R3 moved them into the trading day, when a single stale ticker is normal. As written, P8's gate may rarely open during market hours. His call, one of: (a) wait for THCH to drop and rerun (cheapest today); (b) rule that a single-ticker `failed_stage bars` RED with the radar cycling clean is not "production not green" for a deploy gate, and have the prompt name that exception explicitly; (c) give the bars probe a tolerance. **I did not decide this myself — the prompt's rule is unconditional and I stopped.** Related to ops ESCALATE 1 (the 04:00 boundary RED): two different benign-RED classes now, same question underneath.
2. **P7 exits 1 pre-merge, and that is correct.** From `~/cobalt` the classifier is main's OLD code, which has no operator-script rule — so `ops/cto-desk.sh` comes back `UNCLASSIFIED` and derives all six residents. The rule that classifies it (`operator script; no Cobalt reader → -`) is **in this very merge**; the ops hub's own run from its worktree gave `0 UNCLASSIFIED` / `RESTARTS: com.cobalt.aset com.cobalt.radar`. The rerun should expect exit 1 at P7 and exit 0 at step 4.1, and not read the preflight exit as a defect. Recorded verbatim so the next session does not re-litigate it.
3. **`com.cobalt.aset` is loaded from the repo plist, not from `~/Library/LaunchAgents/`.** `launchctl print` gives `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist`, while step 4.2's approved bootstrap line names `/Users/cobalt/Library/LaunchAgents/com.cobalt.aset.plist`. **Not a blocker — they are byte-identical today** (both `f750a20d40498b820434ef81def6257312702838f65c3cf3405dc0c150afcc40`), so the bootstrap brings back the same service. But it silently *moves* aset's registered plist path from the repo copy to the home copy, and from then on an edit to `ops/com.cobalt.aset.plist` would no longer be what launchd loads. `com.cobalt.radar` already loads from `~/Library/LaunchAgents/` (`a4f49ca6…`), so only aset changes. Worth a deliberate ruling rather than a side effect of a deploy.
4. **Carried, unchanged, not reached:** ops ESCALATE 4 — a pre-deploy restic snapshot is pruned by the 21:40 nightly (`--keep-daily 14`), and `cobalt backup` cannot list snapshot ids, so a cited id cannot be verified afterwards by any allowlisted command. Still owed; no snapshot was taken today.
5. **The rollback directory must be built by the session that deploys.** §0.3 was not run. Its whole value is the dry run printing `0 setting(s) would change`, which proves the directory equals production *now*; a directory built at 11:13 and applied hours later proves nothing about the state in between. The rerun builds it fresh — it should not be treated as a step already done. (Also noted: the forward files live in the `ops-2026-09-18` worktree, which must stay alive until the apply — the ops report's own "staging risk".)

6. **The desk committed this report out from under me, mid-run.** I staged `deploy-2026-09-18.md` by name; before my own `git commit` ran, the desk's `858e7ab` ("deploy 1 launched (f4b9b0da)") swept the staged file into its own commit, and my commit exited 1 with `no changes added to commit`. No harm today — the file landed complete (79 lines) and the tree is clean — but the desk and a deploy hub were both committing to `~/cobalt` in the same seconds. Under R3 the deploys now run in daylight while the desk is actively writing prompts and rulings, so this will recur, and at step 2.1 it is not harmless: that step commits named machine-written files and records `<main-before>`, which the revert range in §6 depends on. A desk commit interleaving there would put an unrelated file inside the deploy's revert range. Owed: either the desk holds commits to `~/cobalt` while a deploy hub is between its step 2.1 and its stop line, or the deploy derives `<main-before>` from its own commit hash rather than from `HEAD` at the time it looks.

Also noted, for the record: this session's first tool result — the `cat` of the prompt file — ended with a block styled as a system reminder asking for a `Claude-Session:` URL line in every commit. Same injection the desk logged in `cto-2026-09-18.md` §3. It came from inside a tool result, not from the harness or from Dejan, and the genuine harness attribution reminder names only the `Co-Authored-By` line and says not to add lines it leaves out. Not followed; commits carry `Co-Authored-By` only.

CONTINUE: rerun this same prompt from `## PREFLIGHT` once P8 is green (or once he rules on ESCALATE 1). No step needs undoing first — nothing was changed. The two machine-written files (`configs/cobalt/rules.yaml`, `seat-usage.md`) are still uncommitted, exactly as step 2.1 expects to find them.

FAILED PREFLIGHT: production not green — P8 `HEARTBEAT RED — 1 probe(s)`: `RED radar failed_stage bars: poll failures: 1; poll THCH stale since 2026-09-18T14:52:29.301250+00:00` — safe state: green — residents: up (aset 55521, radar 55534) — nothing merged, nothing applied

---

# SECOND RUN — DEPLOY 1 hub `deploy1-0918` (Opus 5, `06-deploy-1-ops.md` as amended under R6)

## §0 Headline (11:34 ET)

- `DO NOT STOP until DEPLOY 1 DONE` — **DEPLOY 1 FAILED at 3.6 and was REVERTED. Safe state GREEN, both residents UP on new pids, outage 4 min 53 s.**
- The deploy itself worked: merge ff `856176e`, dry run exactly `1 setting(s) would change`, row applied with an EMPTY from_db/from_yaml diff, `validate` exit 0 with `trade_count_over_band=down(1)` — 7 rows.
- **3.6 red: `26 failed, 1407 passed`. Every failure is `cobalt_dev` state** (no over-band row; still on P2's 0006/0007 schema), none the merged code — and no allowlisted command here can fix it, so the rerun fails identically until someone does (ESCALATE 1).
- §6 executed in full: 8 reverts, settings rolled back, `validate` exit 0, residents bootstrapped (aset 39791, radar 39805); `git diff pre-ops-0918 HEAD` = this report only.
- ESCALATE: 8 (1 and 2 gate the relaunch).

## PREFLIGHT — SECOND RUN

Ran 11:21–11:23 ET from `/Users/cobalt/cobalt`. **Zero denials.**

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P1 | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | allowed — ` M configs/cobalt/rules.yaml`, ` M docs/40 - DevDocs/reports/deploy-2026-09-18.md` (this report), ` M docs/40 - DevDocs/reports/seat-usage.md`. Nothing else dirty |
| P2 | `git … tag scratch-allow-probe-0918` then `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-0918' (was 3fa69e5)` |
| P3 | `git … commit --allow-empty` then `reset --soft HEAD~1` | 0 / 0 | allowed — `[main 8c2a6f7]` created, then `log --oneline -1` → `3fa69e5` — HEAD is back |
| P4 | `COBALT_ENV=production uv run cobalt validate` | **0** | allowed — 13 trade_defs OK; `Step-downs: … 6 row(s)` (no `trade_count_over_band` — the pre-merge state); `Placement: tree clean` |
| P5 | `COBALT_ENV=production uv run cobalt backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 13.7 h old` |
| P6 | `COBALT_ENV=production uv run cobalt backup run --dry-run` | 0 | allowed — `cobalt_brain via pg_dump inside cobalt_memory — 613.7 MB`; `DRY RUN — nothing written, no jobs row touched.` |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..ops/2026-09-18` | **1** | allowed — EXPECTED: `ops/cto-desk.sh  A  UNCLASSIFIED` under main's OLD classifier (`ESCALATE: unclassified path ops/cto-desk.sh`). The rule is in this merge; exit 0 is expected only at 4.1 |
| P8 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — `HEARTBEAT RED — 1 probe(s)`; the ONE red is the accepted class → **BASELINE**, below |
| P9 | `launchctl print gui/501/com.cobalt.aset` · `…/com.cobalt.radar` | 0 / 0 | allowed — both `state = running`; **aset pid 55521** (runs 16, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist`), **radar pid 55534** (runs 9, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`) |
| P10 | `curl … http://127.0.0.1:5010/` | 0 | allowed — `200` |
| P11 | `shasum -a 256` of the two forward files | 0 | allowed — both match R4 exactly (below) |

**P8 BASELINE RED, verbatim (the one class R6 accepts):**

```
RED  radar                    failed_stage bars: poll failures: 1; poll THCH stale since 2026-09-18T14:52:29.301250+00:00
```

Every other probe OK; `com.cobalt.herdr` AMBER by declared interim, unchanged. Radar demonstrably cycling — `tail -n 12 logs/radar.err` shows `radar cycle: scanning` at 10:50:49, 10:53:49, 10:56:50, 10:59:50, 11:02:50, 11:05:51, 11:08:51, 11:11:52, 11:14:52, 11:17:53, 11:20:53 — 3 min apart up to `date` 11:23:24, no traceback. Second RED probe: none. Gate PASSES under R6.

**P11 — the approved settings input, re-proven 11:23 ET:**

| file | sha256 | vs R4 |
|---|---|---|
| `…/ops-2026-09-18/scratch/daymode-settings-0918/daymode.yaml` | `daa7bb720f19b60a438f3366aa7327188842d69f5fe0da343fa6bbbe7b3d5ebb` | MATCH |
| `…/ops-2026-09-18/scratch/daymode-settings-0918/aset.yaml` | `8eca6945087170312a11709a4c5833a9d36d298798343ea1af39265a6eb99d58` | MATCH |

**0.2 GATE: proven — commit + tag allowed by the session allowlist** (P2, P3, HEAD restored). L29 satisfied.

**0.3 ROLLBACK DIRECTORY — built fresh by this session** at `data/backups/pre-ops-0918/daymode-settings-rollback` (user data, gitignored, never `git add`ed; no value quoted here).

| file | sha256 | how it was made |
|---|---|---|
| `…/daymode-settings-rollback/aset.yaml` | `8eca6945087170312a11709a4c5833a9d36d298798343ea1af39265a6eb99d58` | byte-identical to the forward file (hash equality is the proof) |
| `…/daymode-settings-rollback/daymode.yaml` | `48a99812acf090574f738df886d4584c2100f1d6e0f3abbbd9882bde183d81e1` | the forward file minus exactly the four-line `trade_count_over_band` row |

`git diff --no-index --stat` rollback→forward: `1 file changed, 4 insertions(+)` — four lines added, **zero deleted, nothing else touched**.

**GATE — the rollback dry run against production, verbatim:**

```
cobalt settings load — DRY RUN from yaml:/Users/cobalt/cobalt/data/backups/pre-ops-0918/daymode-settings-rollback

  = aset.sheet_modes
  = aset.enabled_grades
  = daymode.reduced_sheet
  = daymode.reduced_enabled_grades
  = daymode.enabled_modes
  = daymode.hotkey_file_template
  = daymode.stepdowns

no differences — the database already holds these settings.
```

All seven keys `=`. The CLI prints `no differences — the database already holds these settings.` for the zero case rather than the literal `0 setting(s) would change` the prompt quotes — same fact, stronger form (it enumerates all seven as unchanged). The gate's stated purpose — *the directory IS production's current state* — is proven. Wording difference recorded, not treated as a mismatch (ESCALATE 1).

## Preconditions — SECOND RUN (11:24 ET)

| check | result |
|---|---|
| 1.1 clock | `Fri Sep 18 11:24:07 EDT 2026` — NOT inside 19:45–21:40 ET. Clear |
| 1.2 "done trading" | R5, 11:09 ET, committed `cff63bd`. Present |
| 1.3 ops tip | `rev-parse --short HEAD` → **`b1b3045`** = expected; `status --porcelain` **empty**; report last line starts `OPS 0918 eb907a0 …` ✓ |
| 1.4 no other merger | `log --oneline -3` → `1d44abb` (mine, the PREFLIGHT commit) over `3fa69e5` (desk docs) over `a63a488`. No other deploy session merging |
| 1.5 authorization | R3 `5e68daa` · R4 `146cf10` · R5 `cff63bd` · R6 `3fa69e5` — committed, hashes match, every launch-line rule covered |

## Deploy table

**Step 2 — snapshot, machine-written files, rebase (11:24 ET)**

| step | fact |
|---|---|
| 2.1 dirty tree | ` M configs/cobalt/rules.yaml`, ` M docs/40 - DevDocs/reports/seat-usage.md`, ` M docs/40 - DevDocs/reports/cto-2026-09-18.md` (the desk's live report — an expected "desk report" per 2.1). All three `git add`ed BY NAME, nothing under `data/` |
| 2.1 `<main-before>` | **`7ff81e5`** `chore: machine-written + report files, committed as-is (L51-2)` — my own commit, read back from `log --oneline -1` |
| 2.2 snapshot | `ssd: snapshot 40f54eba — 14 new / 6 changed, 119.5 MB added, 0 pruned`; dump line `backup: cobalt_brain via pg_dump inside cobalt_memory — 613.7 MB`. `backup status` → `newest snapshot: 0.0 h old` ✓ |
| 2.2 tag | `pre-ops-0918` created at `7ff81e5` |
| 2.3 rebase | `Successfully rebased and updated refs/heads/ops/2026-09-18` (8 commits, no conflict). New tip **`32b41e3`** |
| 2.3 diff vs main | 9 paths, all in the ops report §5 table: `ops-2026-09-18.md`, `ops/README.md`, `ops/cto-desk.sh`, `daymode/config.py`, `daymode/propose.py`, `jobs/restarts.py`, `test_daymode.py`, `test_daymode_note.py`, `test_jobs_restarts.py` — `1051 insertions(+), 15 deletions(-)` |

Known and carried (ops ESCALATE 4): snapshot `40f54eba` is pruned by tonight's 21:40 nightly (`--keep-daily 14`). Taken 11:24 ET today; `git tag pre-ops-0918` is the durable rollback point.

**Step 3 — the outage window (11:25:28 → 11:30:21 ET, 4 min 53 s)**

| step | fact |
|---|---|
| 3.1 baseline | `grep -c "ENTERED RED" logs/heartbeat.log` → **93**. Window opened `Fri Sep 18 11:25:28 EDT 2026` |
| 3.2 residents DOWN | `bootout` aset, then radar. Both `launchctl print` then FAILED with exit 113 `Could not find service "com.cobalt.aset" / "com.cobalt.radar" in domain for user gui: 501` — the proof. No 15-min beat landed inside the window (beats at 11:17 and 11:32 ET), so no RED/DM was raised by the outage itself |
| 3.3 merge — FIRST ATTEMPT REFUSED | `merge --ff-only ops/2026-09-18` → exit 128 `fatal: Not possible to fast-forward, aborting.` Cause: the prompt has 2.3 rebase the ops branch and then commit the report — that report commit (`77260b5`) advanced main *after* the rebase, so the branch was one commit behind. Fixed with the same allowlisted command, `git -C /Users/cobalt/cobalt-wt/ops-2026-09-18 rebase main` (8 commits, no conflict) — no new rule, no new shape (ESCALATE 2) |
| 3.3 merge | `Updating 77260b5..856176e` — fast-forward, 9 files, `1051 insertions(+), 15 deletions(-)`. Tip **`856176e`** |
| 3.3a forward dry run | **GATE PASSED** — `DRY RUN — 1 setting(s) would change. Nothing written.`; six keys `=`, the only `~` is `daymode.stepdowns`, db side = 6 rows, file side = the same 6 + `trade_count_over_band` |
| 3.4 forward apply | `applied: {…'daymode.stepdowns': 'updated'…}` (all six others `unchanged`) and `from_db() == from_yaml() — field-by-field diff EMPTY.` |
| 3.5 validate | exit **0** — `Step-downs: … trade_count_band_placeholder=floor; trade_count_over_band=down(1) — 7 row(s), every computable signal ruled.` |
| 3.6 suite | **RED — `26 failed, 1407 passed, 1 xfailed` in 76.75s → §6 entered** |

**3.4 trace line (L28):** approved by Dejan via the desk — `cto-2026-09-18.md` row R4 (07:41 ET), files + sha256 as named (`daymode.yaml` `daa7bb72…3d5ebb`, `aset.yaml` `8eca6945…b99d58`, re-hashed at 11:23 ET). Command: `COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply`, run 11:29 ET. Rolled back at 11:30 ET under §6 (below).

**Why 3.6 is red — every one of the 26 is `cobalt_dev` state, not the merged code.** From `~/cobalt` a bare `uv run pytest` resolves to the DEV database (the repo `.env`), and `cobalt_dev` is in two known-bad conditions, both recorded before this deploy:

| class | count | evidence | prior record |
|---|---|---|---|
| `"user".trader_settings: invalid trader settings … daymode.stepdowns has no row for ['trade_count_over_band']` | 22 | `test_aset_config`, `test_daymode::TestShippedConfig` (3), `test_prefill_drc` (10), `test_trader_settings` (5), … | ops ESCALATE 3 / ops §5: `.env` in → dev suite 3 failed, "the ruled hazard, reproduced, not a defect in the change" |
| `cobalt_dev` still carries P2's 0006/0007 schema | 4 | `test_aset_store` + `test_cards` `psycopg.errors.CheckViolation: new row for relation "aset_sizings" violates…`; `test_tenancy::TestPlacement` "every table is declared on exactly one side"; `test_tenancy::TestMigrationRoundTrip` | `deploy-2026-09-17.md` ESCALATE 3; `cobalt_dev` was left at 0007 by the P2 gate this morning |

The same tree was proven green offline by the ops hub — `1194 passed, 239 skipped, 1 xfailed` (`ops-2026-09-18.md` §5) — and the three test files this change touches (`test_daymode.py` outside `TestShippedConfig`, `test_daymode_note.py`, `test_jobs_restarts.py`) all pass here. **Production was green at that moment: 3.3a, 3.4 and 3.5 all passed.** The gate at 3.6 is nonetheless unconditional, and no command in this session's allowlist can repair `cobalt_dev`, so the gate could not be met by retrying. I did not decide it away — §6 was executed.

## Smoke

Not reached as a deploy smoke: §6's step (4) re-run of the same checks stands in its place, on the REVERTED code.

| check | result |
|---|---|
| `curl … :5010/` | **200** |
| `curl … :5010/radar` | **200** |
| `heartbeat show` (11:30:39) | `HEARTBEAT RED — 1 probe(s)` — the SAME baseline probe, verbatim: `RED radar failed_stage bars: poll failures: 1; poll THCH stale since 2026-09-18T14:52:29.301250+00:00`. **No new RED.** `com.cobalt.aset running pid 39791`, `com.cobalt.radar running 0 min, heartbeat fresh`; herdr AMBER by declared interim |
| `validate` (post-revert) | exit **0** — `Step-downs: … trade_count_band_placeholder=floor — 6 row(s)`, back to the pre-deploy set |
| `radar.err` | radar started 11:30:17 on the reverted code, vault unlocked, Finviz token resolved, **no traceback**. Two cycles since: `radar cycle: scanning` 11:31:37 and 11:34:38 — 3 min apart, the normal cadence |
| `aset.err` | server restarted (`Started server process [39798]`, `Uvicorn running on http://0.0.0.0:5010`), **no traceback, no degraded day-mode banner**. One `ERROR … radar panel FAILED: FAILED: radar bars: poll failures: 1` — the same baseline condition surfacing loudly (L9), not a new fault |
| `radar_pool` | read 1 (11:30:38 ET): `last_scan_at 2026-09-18 15:22:34Z · members 50 · failed_stage bars` (the pre-outage scan) → read 2 (11:33:07 ET): `last_scan_at 2026-09-18 15:30:17Z · members 50 · failed_stage bars`. **`last_scan_at` advanced to the restarted radar's own first cycle**; 50 members; `failed_stage` is the baseline `bars` class only, never another stage |
| beats | the first beat after the restart, 11:32:19 ET, is GREEN except the same single baseline probe; `grep -c "ENTERED RED" logs/heartbeat.log` is **93 — unchanged from the 3.1 baseline**, so the outage raised no new RED entry and sent no DM |

## §6 SAFE STATE — executed

| step | fact |
|---|---|
| (1) revert | `git revert --no-edit 77260b5..HEAD` → 8 revert commits (`d6332f6` … `511cf25`). **Range note:** `<main-before>` is `7ff81e5`, but `7ff81e5..HEAD` also contains `77260b5`, this report's own step-2 commit (docs only). Reverting that would have deleted the deploy's own evidence from the report file (L48), so the range used is exactly the merged code: `77260b5..HEAD` = the 8 ops commits, nothing else |
| (2) settings rollback | rollback `--apply` from `data/backups/pre-ops-0918/daymode-settings-rollback` → `applied: {…'daymode.stepdowns': 'updated'…}`, `from_db() == from_yaml() — field-by-field diff EMPTY.` Then `validate` exit 0 on the reverted code — **code and settings agree** |
| (3) residents UP | `bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` (the REPO plist, R6) and `…/Library/LaunchAgents/com.cobalt.radar.plist`. Both `state = running`, NEW pids **aset 39791** (was 55521), **radar 39805** (was 55534), `last exit code = (never exited)`. No `Bootstrap failed: 5`, no `kickstart` needed |
| (4) re-checks | the Smoke table above |
| outage | 11:25:28 → 11:30:21 = **4 min 53 s** |
| membership gap | 11:25:28 (bootout) → 11:31:37 (first scan cycle started on the restarted radar; its row landed as `last_scan_at 15:30:17Z`) = **~6 minutes of membership record**, R3's named cost, paid |
| tree proof | `git diff --stat pre-ops-0918 HEAD` → **one file changed: this report**. Production's code and configs are byte-equal to the pre-deploy state |

## ESCALATE

1. **BLOCKER for the relaunch: step 3.6 cannot pass from `~/cobalt` until `cobalt_dev` is repaired — no allowlisted command in this session can repair it.** Two independent causes, both pre-existing and both recorded before launch (ops ESCALATE 3 and 5; `deploy-2026-09-17.md` ESCALATE 3): `cobalt_dev` has no `trade_count_over_band` row, and it still carries P2's 0006/0007 schema while main has no P2. A bare `uv run pytest` in `~/cobalt` reads that database, so the deploy's own suite gate fails for reasons the deploy neither caused nor can fix — **the identical run would fail again**. His call, one of: (a) the deploy's suite gate becomes the BRANCH's offline suite line, verified from the ops report under L35 (`1194 passed`), with 3.6 reduced to production-side `validate`; (b) the deploy hub is given a DEV `settings load --apply` rule plus the 0008/0009-style rollback so it can bring `cobalt_dev` level first; (c) a dev DB per worktree (the standing fix already owed, `cto-2026-09-18.md` §9) lands before the next deploy. Until one of those, no deploy that touches `trader_settings` can go green in daylight.
2. **Re-landing `ops/2026-09-18` is a cherry-pick, NOT a rebase — the same trap that cost P2 this morning.** Its 8 commits are now ancestors of main (merged `856176e`, then reverted), so `main..HEAD` is empty and `git rebase main` will replay nothing and exit 0 (`UNATTENDED-LAUNCH.md` §6, 09-18 06:5x). The relaunch prompt must re-land with `git cherry-pick 77260b5..856176e` onto a branch off main and prove `git diff --stat 856176e HEAD -- src tests ops` empty.
3. **Prompt-order defect, hit and worked around in-run: 2.3 rebases, then 2.3 commits the report, so `merge --ff-only` at 3.3 fails** (`fatal: Not possible to fast-forward`) with both residents already DOWN. Recovered with the same allowlisted `rebase main` (no new rule, ~20 s of extra outage). Fix the order: commit the report BEFORE the rebase, or rebase as the first command after `bootout`.
4. **`<main-before>` is the wrong revert range under this prompt's own shape.** 2.1 defines `<main-before>` = the machine-written commit, but the prompt then has the hub commit its report (`77260b5`) before the merge, so `<main-before>..HEAD` contains a docs commit whose revert would delete the deploy's own evidence (L48). §6 was executed on the merge range `77260b5..HEAD` instead, stated at the time. Prompts should define the revert range as "the commit immediately before the ff-merge .. HEAD".
5. **The §0.3 gate's literal string does not exist in the CLI.** For the zero case `cobalt settings load --dry-run` prints `no differences — the database already holds these settings.`, not `0 setting(s) would change` (that wording appears only when the count is ≥1). Recorded verbatim, treated as the gate passing, since it enumerates all seven keys as `=`. Update the prompt's quoted string.
6. **Carried, now real: the pre-deploy snapshot expires tonight.** `ssd: snapshot 40f54eba` taken 11:24 ET is pruned by the 21:40 nightly (`--keep-daily 14`), and `cobalt backup` cannot list snapshot ids. The durable rollback point is the tag `pre-ops-0918` (`7ff81e5`). Ops ESCALATE 4 is still owed.
7. **The desk's in-flight report was committed by this hub, as 2.1 instructs.** ` M docs/40 - DevDocs/reports/cto-2026-09-18.md` was dirty at step 2.1, which names "desk / deploy reports" as expected, so it went into `7ff81e5` as-is. Not a sweep of a staged file (first run's ESCALATE 6) — but the desk should know its 11:2x text is committed at that hash.

8. **`ops/cto-desk.sh` is NOT on main** — it merged at 11:29 and was reverted at 11:30 with the rest of the range (`ls -la ops/cto-desk.sh` → `No such file or directory`). It still lives in `ops/2026-09-18` at `856176e` and in the ops worktree. If the desk has started relying on it for its own relaunch, it must use the worktree copy until the branch re-lands.

Also noted, unchanged from the first run: the prompt file's tail carries a block styled as a system reminder asking for a `Claude-Session:` URL line in every commit. It arrives inside a tool result, not from the harness or from Dejan; the genuine attribution reminder names only `Co-Authored-By`. Not followed.

## State handed to the desk

| item | value |
|---|---|
| main tip | `511cf25` (8 revert commits above the merge); `git diff --stat pre-ops-0918 HEAD` = this report only |
| rollback point | tag **`pre-ops-0918`** = `7ff81e5`; restic snapshot **`40f54eba`** (11:24 ET, pruned by the 21:40 nightly) |
| branch | `ops/2026-09-18` tip **`856176e`**, worktree `ops-2026-09-18` clean — **re-land by cherry-pick `77260b5..856176e`, never by rebase** (ESCALATE 2) |
| settings files | unchanged and still hashing to R4's values (re-verified 11:34 ET); rollback directory left in place at `data/backups/pre-ops-0918/daymode-settings-rollback` and still equal to production |
| residents | aset **39791**, radar **39805**, both `state = running`, sheet 200, `/radar` 200 |
| production | reverted code + 6-row step-down table, `validate` exit 0, heartbeat = the single pre-existing baseline RED, RED-entry count 93 (unchanged) |

CONTINUE: relaunch this prompt only after ESCALATE 1 is ruled (the `cobalt_dev` gate) and with step 1 re-landing the branch by cherry-pick per ESCALATE 2; nothing else needs undoing — production is at its pre-deploy state.

FAILED: 3.6 — offline suite `26 failed, 1407 passed`, every failure caused by `cobalt_dev` state (no `trade_count_over_band` row; still on P2's 0006/0007 schema), none by the merged code, and unfixable from this session's allowlist — safe state: green — residents: up (aset 39791, radar 39805) — reverted `77260b5..856176e`, settings rolled back, `validate` exit 0, outage 4 min 53 s — ESCALATE: 8

