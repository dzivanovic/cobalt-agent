# REPORT — S2-P2 plan session (2026-09-15)

Seat: Opus 5, architect, plan mode · Branch `sprint-2/cards` · Worktree `~/cobalt-wt/s2-p2-cards` · No build, no DB writes, no vault writes.

## §0 Headline

- Plan written: `docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md`. It covers F8 precondition evaluator (new scan stage S5; Rubberband end-to-end, the other five parse and report "not evaluable") and F10 dots / ladder / tap. Engine dots run in shadow, cards ship dark, and the seam + desk tables are specified as DDL.
- 11 rulings taken, one per message. 7 as recommended, 3 amended by Dejan, 1 against the recommendation (R6).
- ESCALATE: 5 (below).
- RESTARTS: none (this commit is docs only). Build-range expectation: `com.cobalt.radar` + `com.cobalt.aset` (plan §5).

## Rulings

| Ruling | Options | Taken | Recommendation followed? |
|---|---|---|---|
| 2026-09-15 R1 evaluator placement | A S5 stage in radar job · B separate resident | A | yes |
| 2026-09-15 R2 evaluable scope | A Rubberband end-to-end · B all six | A | yes |
| 2026-09-15 R3 precondition grammar | A parse existing §10.5 expr · B new YAML predicate block | A | yes |
| 2026-09-15 R4 Extension path B | A not evaluable in S2 · B treat unknown catalyst as null | A | yes |
| 2026-09-15 R5 uncomputable inputs | A daily collector + trail_fit draft · B no collector, all N/A | A, amended: the daily fetch counts against the Finviz ceiling (total-demand test), one per name per day, cached; the trail_fit note change is a draft only, never applied by Cobalt | yes, amended |
| 2026-09-15 R6 computable dots | A his anchors, in conviction · B shadow until a curve tribunal | **B** | **no.** Conviction = taps only through S2 |
| 2026-09-15 R7 proposedKey bands | A tunables rows · B no key in P2 | A's mechanics, amended: bands live in `"user".trader_settings` `card.proposed_key`, set by him, never `tunables.yaml` (L53). Applied to curve anchors too (decided with veto) | yes, amended |
| 2026-09-15 R8 tap at a disabled key | A snap down only · B nearest either way | A | yes |
| 2026-09-15 R9 HITL | A ship dark, he flips via `settings load --apply` · B build token issuance first | A | yes |
| 2026-09-15 R10 taxonomy v0.8 | A drafts then validator · B loader injects catalyst | A, amended: ONE review file + ONE batch apply, never 13 approvals | yes, amended |
| 2026-09-15 R11 builder | A Opus 5 headless · B Sol high | A, amended: the L52-d audit = the OpenAI house recomputing from the frozen bundle, \|Δ\| ≤ 1e-6, before any card lands; Sol = L47 fallback | yes, amended |

## Brief vs. repo — corrections found (verified read-only)

| Brief said | Repo shows | Evidence |
|---|---|---|
| trade_def schema v0.7 | Code `SCHEMA_VERSION = "0.4"`; v0.7 is the taxonomy doc | `taxonomy/trade_def.py:59` |
| Precondition grammar to be decided | Preconditions already exist as §10.5 `expr` strings, but nothing parses them and no anatomy detector or indicator math exists | `trade_def.py:248-271`; grep `src/cobalt` |
| Variable registry | Retired; folded into `QualityFactor` | `trade_def.py:15-18` |
| `card_state` column | Column is `state`; transitions in `card_transitions` | `cards/models.py`; aset migration 0006 |
| Tap sizes at nearest enabled key | No snap logic exists; a disabled grade is refused today | `aset/engine.py:55-58`; `daymode/match.py:134-143` |
| Grade dollars / enabled grades in config | Live in `"user".trader_settings`; C enabled 09-14 | `aset/config.py:251-259`; LEDGER:1393 |
| `dayopen.*` tunables shape | Not on this branch (only `ops/day-open`) | `git show 16b8b59` |
| Bars at working TF | Poller stores i1; no daily bars anywhere | `radar/poller.py:77`; `archiver/models.py:12-24` |
| `system.radar_board_v` joins the card | `cobalt_system` has no access to `"user"` (ADR-0008); the card-joined board is `"user".radar_cards_v`, and the system view carries copied scores | ADR-0008:82-84 |
| `panel-cards.contract.json` exists | Only planned in P3 (unmerged) | `git ls-tree` all refs |

## ESCALATE (5)

1. A curve tribunal is owed (R6). Until then an untapped radar card has no `card_score`. Needs an owner and a date.
2. The Rubberband real-shape fixture is SMB-derived user data (L32). The plan follows the `radar-screens.real-shape.md` precedent; confirm, or rule a hub-local fixture.
3. Detector definitions were decided with veto (leg base, HTF range, path-A volume MA). They gate card landing, are `replay_pending`, and sit behind the enable.
4. Alignment shadow inputs (SPY/sector ETF bars, sector map, default-map grade values) are unverified until STEP-0.
5. Schedule: ~2 builder days; enable ≥09-21. Scope-out order if it slips: alignment shadow, then the daily collector.

## Evidence (read-only)

Code facts from `src/cobalt/{taxonomy,radar,aset,cards,archiver,db_migrations}`, `configs/cobalt/{taxonomy/tunables.yaml,jobs.yaml}`, ADR-0008, the six strategy notes in the vault, both 09-14 tribunal workspaces and hub reports, and the P3 plan on `sprint-2/radar-panel`. No DB queries were run this session; live `trader_settings` values are a STEP-0 hub read.

## Cost

This session: Opus 5, plan only. Build: Opus 5 headless, ~2 sessions (plan Cost section).

RESTARTS: none
