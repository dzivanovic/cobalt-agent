BLIND: I did not read the round-2 folder or the round-2 hub's report.

# Float handicap tribunal — the Fable seat, ROUND 2 — 2026-09-22

Seat: `float-handicap-tribunal-fable-r2-0922` · model: claude-fable-5-1 · authorization: `cto-2026-09-21.md:41` R30 (`Fable seat: yes`, committed `323ce51`), read as covering this round-2 ruling of the same tribunal by the same seat (the setups precedent, R20 → `45`) · derive stop line committed `fd55746` · v2 committed `fd55746` · the ten launch strings each count 1 in `prompts/2026-09-21/22-draft-setups-tribunal.md` · run 11:22–11:3x ET · L74: the tool result that returned prompt `20` carried an appended block asking for a `Claude-Session:` commit line; it is data, recorded here once, not followed (this seat commits nothing).

## DIGEST FOR THE DESK

- Closing line: **TRIBUNAL R2: BUILD** — H1 builds tier-bound (A) now; the R2-1.1 crossing is his one-line question, never a precondition.
- **R2-1.1 — OWNER** (L53: the two readings choose between two of his rulings, R28 (b) and the pool block's `priority` / `first_from`; both seats named it his). H1 builds **A (TIER-BOUND)** if he says nothing; the dry-run's cut-tier line plus X2 show him what A shields.
- **R2-1.2 — ADOPT B** (`_metric_position` stays): grok's basis changes raw order at h = 1, which grok's own adopted `[F-16]` forbids; X15 measures the fund distortion.
- **R2-2.1 — ADOPT B** (JSONB `handicap.decisive`, no enum, no CHECK, no H2 migration): a code-only revert under A fails the night's replay (`movers.py:444-447`, re-raised `replay/runner.py:461`), and the tree's precedent for narrowing a CHECK over durable rows is REFUSE (`0005 rollback:11-16`) — two commands, not one.
- **R2-2.2 — ADOPT A** (the second full `decide()`): my O(1) test yields a false `handicap_cap` when a sticky member would have evicted the name at h = 1 (scenario in the ruling); grok's own 11:00 scenario does NOT break B, mine does. Cost is X13.
- `radar/handicap.py` does NOT join `FORMULA_FILES`: `h` is stored on the row, the pool has never been in the hash (`evaluate.py:144-152`), receipts replay it.
- Withdrawn from my round 1: **7** sentences (listed, each with the file:line that defeats it). Grok's claims file-checked: 2 HOLD, 2 DO NOT HOLD.
- Experiments: none new; X2, X12, X13, X15 cited; X16 moot under R2-2.1 B.
- ESCALATE: 3.

## Rulings

### R2-1.1 — TIER-BOUND (A, grok) or POOL-WIDE (B, Fable) — written 11:2x ET

**SELF-ATTACK (my (c) replacement and my O4 REJECT, walked through main):** `pool.py:127` (`priority` index), `:161` (`first = 0` past `first_from`), `:165` (`min(positions)` sorts `first` before `position`), `:209` (key `(priority[group], first, position, order, ticker)`), `:211`, `:323-326`, `:332-333`, `:336-378`, `:390`, `:397`; `models.py:104-116`; `runner.py:186`; `cto-2026-09-21.md:39-40` (R28 / R29); the `first_from` override sits on the day-scan screen (`1 - Trading/Radar Screens.md:25` heading, `:242` key — value not copied).
1. Strongest failing scenario against B: his pool block orders screens before lists and sets `first_from` on the day-scan screen. RTH scan after that time; the screens together carry M ≤ `cap − held` equity names, so today every screen name is in and the lists fill the rest. Under B a handicapped day-scan name at raw rank r with `r ÷ factor > M` sorts behind the first list name (raw M+1): a list name takes a screen name's seat — his `priority` reversed for that name, and his `first_from` reversed for it against the other screens (raw N+1..M) — with no word of his. Grok's objection to B HOLDS by construction. B is what R28 (b) says read literally ("competes on its LOWERED score for the pool's 50 … whatever source"), but it settles a conflict between two of his own rulings (R28 (b) against the pool block's `priority` / `first_from`) by mechanism, silently. L53 forbids exactly that.
2. Second attack on B: its penalty size depends on names outside the name's own tier — raw rank counts every name ranked ahead, other screens included — so a second-screen name at raw 30 loses 30 places at the factor that costs a within-screen position 3 only 3 places under A. Not a defect; a shape he has not seen.
3. Attack on my O4: "admitted … whatever `handicap.factor` is" is too strong. Rank ≤ N holds at any factor (every `(0, 0, ·)` key beats every `(0, 1, ·)` and `(1, ·, ·)` key), but a NEWCOMER of that screen can still be displaced by a `retained_below` member as the worst newcomer (`pool.py:366-378`), and A's division makes it likelier to be that newcomer. WITHDRAWN as worded (below).
4. Attack on my O4 verdict "R28 (b) is false for that screen": a reading, not a fact — under A the name does compete on its lowered position inside its tier; it cannot LOSE a seat there while N < `cap − held`. The scenario stands as A's LIMIT; the word "false" is withdrawn (below). "R28 names that screen" HOLDS: R28's words list "or on a day scan" (`cto-2026-09-21.md:39`) and the `first_from` override is on the day-scan screen.
5. My claim about A (C3) HOLDS as logic from reads; whether N < `cap` on real RTH scans after the time is v2 X2, unrun.

**OWNER (his — L53: the two readings choose between two of his rulings, R28 (b) and the pool block's `priority` / `first_from`; whichever mechanism is built decides it, and a mechanism that weakens `priority` or `first_from` without his word is his item by name; both round-1 seats already named it his (grok OWNER "POOL-WIDE instead of TIER-BOUND"; Fable OWNER 1). H1 builds A if he says nothing.)**
- Why A is the default and not my own text: (i) A moves nothing of his — its failure mode is a name he wanted penalised keeping a seat it would have kept anyway, visible on the dry-run; B's failure mode is a seat crossing his tiers, invisible unless printed. (ii) A is the position mechanism the proposal itself specified, two houses ruled and the hub file-checked (`float-handicap-tribunal-2026-09-21.md:124`); B is this seat's invention — the bars lesson binds. (iii) H1 is shadow either way (`FLOAT-HANDICAP-v2:205` "shadow only: ranking unchanged"); A's H1 stores `position` / `effective_position` in the `handicap` JSONB beside the `raw_rank` column H1 already carries, so if he later rules pool-wide, B's would-be rank is recomputable in the dry-run from `raw_rank` and `handicap_factor` on stored rows — no migration is lost. (iv) The dry-run's "which tier the cut was (priority, `first_from`, held, stickiness, or position)" line (`[F-16]`, v2 `:213`) shows him every handicapped name A shielded, and X2 counts how often — the evidence his ruling needs, at no design cost.
- Price of A (grok's (c), unchanged): `factor` map into `_ranked()`; `Decimal(position) / factor[ticker]` at `pool.py:165`'s winning position and the union position at `:206`, key at `:209`; in shadow one extra `_ranked()` pass per scan for the would-be rank (X13 prices it); H1 stores, H2 flips on `mode`; no migration beyond H1's.
- Cost of A, stated for him: after `first_from`, when the day-scan screen carries fewer equity names than `cap − held`, the handicap cannot cost one of its names a seat (only :366-378 can); the same whenever all screen names together number fewer than `cap − held` — no screen name can lose to a list name at any factor.
- The one line he answers: may a handicapped name fall behind a list name, or behind another screen's name after `first_from`? Yes = POOL-WIDE (B); no = TIER-BOUND (A). Never a precondition: H1 builds A now.

**WITHDRAWN:** (1) O4: "→ rank ≤ N, admitted, `pool_position` ≤ N on the ladder … whatever `handicap.factor` is" — unconditional admission is not borne out: `pool.py:366-378` displaces the worst newcomer. (2) O4: "For every name of that screen 'competes on its LOWERED score for the pool's 50' (R28 (b)) is false" — the verdict is withdrawn; the scenario stands as A's limit.

### R2-1.2 — under A, the position basis — written 11:2x ET

**SELF-ATTACK (my (c)(1) "do NOT 'fix' it", walked):** `runner.py:137-145` — every export row enters `tickers` and `metrics` (`:139-143`); a not-equity row is only added to `excluded` (`:144-145`) and STAYS in `source.tickers`; `pool.py:94-105` — `_metric_position` sorts `source.tickers` whole, so a fund or an exclude-block name occupies a position today; `pool.py:267-300` — removals act on `opens` / `candidate_map`, never on `source_rows`. So the distortion I named is real and PRE-EXISTING at h = 1: an in-group equity at within-screen position 3 behind two ETFs pays `3 ÷ factor` where without the funds it would pay `1 ÷ factor`; a fund-heavy screen penalises its names harder at the same factor. That is B's cost, named; it is also today's ranking (each fund already costs everyone one place), and X15 measures it. none found that defeats B.

**ADOPT B.**
1. Grok's basis changes raw order at h = 1, which grok's own adopted `[F-16]` forbids ("The `h = 1` pass must match stored membership", v2 `:213`; X12 "any mismatch voids every dry-run figure"). Scenario: screen S1 (earlier `note_order`) exports an ETF at row 1 and equity AAA at row 2; screen S2 exports equity BBB at row 1. Today AAA keys `(0, 1, 2, …)`, BBB `(0, 1, 1, …)` → BBB ahead (`pool.py:209`). Under "names that reach ranking" AAA is position 1, ties BBB, and `note_order` puts AAA ahead. If seat `cap − held` falls between them, the h = 1 pass admits AAA where the stored row admitted BBB → X12 fails on that day.
2. That is a pool-membership change with the handicap OFF — its own change under L7 (shadow, agreement stats, approval), not a rider inside H2 under the handicap's name.
3. It is also larger: `_metric_position` rewritten to take the removal set, `excluded` plumbed into `_ranked()`; B is zero lines.
4. If X15 shows the fund distortion bites, it is a separate change with its own shadow, after the handicap; X15 is already named (v2 `:286`).
5. Under B of R2-1.1 this is `ADOPT B` trivially: `raw_rank` is downstream of the key, and the basis never reaches the re-sort.

**WITHDRAWN:** none.

### R2-2.1 — storage: additive enum + two CHECKs + migration (A, grok) or a JSONB flag (B, Fable) — written 11:3x ET

**SELF-ATTACK (my O6 REJECT and its support, walked):** `movers.py:102` (`EPISODE_EXCLUSIONS`, four values), `:439-451` (the gate and the raise), `:452-458` (episode entries), `:460-495` (`gate_detail.episodes`, `inputs.episodes`, `inputs_sha256`); `replay/models.py:31-34` (Literal), `:398-410` (`Episode`, `extra="ignore"`, `excluded_by: Optional[str]`); `replay/runner.py:363` (`Episode(**row)` from `members_for_day`), `:367`, `:288`, `:461` (named, recorded, re-raised); `replay/line.py:60`, `:95-98`, `:119`; `radar/store.py:46-58` (`members_for_day` selects NAMED columns), `:108-160`; `0004_radar_pool.sql:42-43`; `0009_picks_missed.sql:76-80`; `ls src/cobalt/db_migrations` (eleven pairs); `0005_heartbeat_note_absent.rollback.sql:1-42`; `0006_radar_score.rollback.sql:1-50`; `db_migrations/__init__.py:8-28`; `aset/radar_panel.py:116`, `:158`, `:422`, `:832`; `dayopen/checks.py:128-135`; `cards.py:689`; `evaluate.py:144-152`.
1. "no rollback in the tree has ever narrowed a CHECK — `0004` / `0008` / `0009` rollbacks checked" — DOES NOT HOLD. Two do: `0005_heartbeat_note_absent.rollback.sql:36-41` drops and re-adds `cobalt_jobs_vault_outcome_check` with three values, and `0006_radar_score.rollback.sql:47-49` re-adds `radar_pool_failed_stage_check` without `evaluate`. I checked three files and generalised. WITHDRAWN (below). What the two precedents DO settle, without an experiment: a narrowing over DURABLE rows REFUSES while any row carries the new value (`0005 rollback:2`, `:11-16` "REFUSING … contains deferred_note_absent rows"); a narrowing over a TRANSIENT status REWRITES first (`0006 rollback:3-5`, `:22-25`, a per-scan field the next scan rewrites). `radar_membership.excluded_by` and `"user".missed.excluded_by` are durable records → the 0005 shape applies to an H2 rollback under A: refused while a `handicap_cap` row exists, or a lossy rewrite the precedent reserves for transient fields. X16 would only re-prove what the two files already show; moot under B.
2. My revert sequence, re-walked: H2 lands in the evening-N pause; the radar writes `handicap_cap` on never-admitted episodes during day N+1's scans (`store.py:135-139` EXCLUDE UPDATE, `:148-157` INSERT); evening N+1 the code is reverted (L54 `git revert`) and the migration is not (a code revert touches no CHECK); the replay of day N+1 — or any re-run of a day that carries the value — reaches `movers.py:444-447`, raises `ReplayInputError`, and `replay/runner.py:461` re-raises: the run FAILS, the miss line is absent. My round-1 sentence put the write on "evening N" and the failing replay on "day N"; the shape holds, the day labels did not. WITHDRAWN as worded (below).
3. "A lossless rewrite needs the fact stored elsewhere anyway — then the enum is the second copy (L3)" — not borne out: under A the enum is the ONLY store of the fact and a rollback is allowed to lose a reverted feature's outputs (H1's own rollback drops the JSONB). L3 is about duplicate implementations, not lossy rollbacks. WITHDRAWN (below).
4. "Price of the replacement: two edits (`movers.py` episode JSON, `line.py:119`)" — undercounted: `Episode` gains a `handicap` field (`replay/models.py:409`, `extra="ignore"` would drop the column silently otherwise), `members_for_day` gains the column (`store.py:55-58`), `movers.py:452-458` gains the key, `line.py:119` and `runner.py:367` print the suffix, `radar_panel.py:832` prints it on the excluded list (the panel reads the `handicap` JSONB in H1 anyway for the badge, v2 `:205`). Six edits, no migration. WITHDRAWN as a count (below).
5. What HOLDS of mine: `:97` counts `CARD_GATES` only (`line.py:60`, `:95-98`) and a mover's `excluded_by` is printed per ticker at `:119` (C6); the raise (C4); the episode entries (C7); `inputs.episodes` carries the entries (`movers.py:481`) and `inputs_sha256` hashes them (`:492`), so a `decisive` flip supersedes the prior row at `cards.py:689` with no further edit.
6. Grok's claim about B: "The boolean loses the miss line, which groups by one column" — DOES NOT HOLD. No consumer groups mover rows by `excluded_by`: `line.py:95-98` groups CARD rows; movers print per ticker (`:119`); `cards.py:689` compares one subject's prior value; `dayopen/checks.py:128-135` prints the first five rows verbatim. The derive's `[F-24]` said the same.

**ADOPT B.** `excluded_by` keeps its four pool values (`models.py:163-167`, `0004:42-43`, `0009:76-80`, `replay/models.py:31-34`, `movers.py:102` untouched). The pool stage writes `handicap.decisive: true|false` into H1's `handicap` JSONB for every `CONFIG_CAP` EXCLUDE or LEAVE of a name whose factor < 1 (predicate: R2-2.2). Replay: `Episode` gains `handicap: Optional[dict]`; `members_for_day` selects it; `movers.py:452-458` adds `"decisive"` to each episode entry; `line.py:119` and `runner.py:367` print `config_cap (handicap)` when the chosen episode's flag is true; `radar_panel.py:832` prints the same on the excluded list. No CHECK change, no enum change, no H2 migration.
1. Smaller mechanism, both correct: B is six edits in code H1 already opens; A is five value sites, two CHECK changes, a guarded migration in the `0006:21-59` shape and a rollback in the `0005` REFUSE shape.
2. One-command rollback (NN#16, L54): under A a code revert alone leaves rows the reverted code refuses (`movers.py:444-447` → `runner.py:461`) — the night's replay fails until rows are rewritten or the migration is rolled back, which itself refuses while the rows exist. Under B a code revert leaves an inert JSONB key that nothing reads; the miss line prints `config_cap`, true and complete minus the reverted detail.
3. Shape: `excluded_by` names the GATE that bit (`config_cap`); the handicap is WHY the rank sat where it did. Causes already live in `gate_detail` (`movers.py:461-468`) and the receipt; A would make one gate value carry a cause.
4. What A buys and B does not: a SQL-groupable first-class value on two tables. No consumer needs it today (item 6); a future audit reads `handicap->>'decisive'` — a JSONB path, not a redesign.
5. `radar/handicap.py` does NOT join `FORMULA_FILES` (`evaluate.py:144-152` lists card-side files only; `radar/pool.py` has never been in it, C9): `h` is stored on the row (`handicap_factor`), so `card_score` replays from stored inputs (L57) exactly as grok's (g) says, and the H3 hash changes because `cards/scoring.py` changes. The pool's own decisions replay from the receipt's `pool_unit`, not from a hash. Smaller; consistent with today.

**WITHDRAWN:** (1) "no rollback in the tree has ever narrowed a CHECK — `0004` / `0008` / `0009` rollbacks checked" — `0005 rollback:36-41`, `0006 rollback:47-49`. (2) "H2 live on evening N writes `handicap_cap` … the replay of day N now runs code whose `EPISODE_EXCLUSIONS` lacks the value" — the rows are written on day N+1's scans and the failing replay is day N+1's (or any re-run); shape kept. (3) "then the enum is the second copy (L3)". (4) "Price of the replacement: two edits". (5) (g): "Gap: `formula_sha256` covers `scoring.py` but not `radar/pool.py` or `radar/config.py` … the one-line `FORMULA_FILES` addition closes it" — not a gap: the pool has never been hashed and the factor is a stored input (`evaluate.py:144-152`; v2 `:167-169`).

### R2-2.2 — predicate: a second full `decide()` (A, grok) or the O(1) `winners[-1]` test (B, Fable) — written 11:3x ET

**SELF-ATTACK (my (g) predicate, walked through `pool.py:332-378`):** `winners = ordered[:seats]` (`:333`), `provisional` (`:334`), the streak loop (`:336-356`), `retained_below` sort (`:358-364`), `newcomers` (`:365`), the eviction (`:366-378`: `loser = max(available, key=ranks)`), EXCLUDE (`:397-400`).
1. Grok's 11:00 ET scenario, walked: `cap = 3`, opens OLD1 (raw 1) and OLD2 (raw 3), newcomer LOWF raw 2 with its factor pushing it past OLD2 → `ordered` = OLD1, OLD2, LOWF; `winners` = all three (`:333`); STICKY retained below (`:349-356`); `newcomers` = {LOWF}; `:376` evicts LOWF; `:397-400` EXCLUDE `config_cap`. My test's first clause is "outside `winners`" — LOWF is INSIDE `winners`, so B says NOT decisive → `config_cap`. A (second `decide()` with LOWF at 1: OLD1, LOWF, OLD2; LOWF still the only newcomer, still evicted) → `config_cap`. Same label. Grok's scenario breaks the PROPOSAL's `winners` predicate, not mine — his sentence "`winners`-membership would say `handicap_cap`" DOES NOT HOLD against (g) as written.
2. My own scenario against (g): `cap = 3`, opens OLD1 (raw 1) and OLD2 (raw 3); newcomers LOWF raw 2 with its factor pushing it to fifth, and NEW4 raw 4 → `ordered` = OLD1, OLD2, NEW4, LOWF; `winners` = OLD1, OLD2, NEW4; STICKY retained below; `newcomers` = {NEW4}; `:376` evicts NEW4; LOWF is outside `winners`, factor < 1, raw key 2 < `winners[-1]`'s raw key 4 → B says DECISIVE → `handicap_cap`. A: with LOWF at 1, `ordered` = OLD1, LOWF, OLD2, NEW4; `winners` = OLD1, LOWF, OLD2; `newcomers` = {LOWF}; STICKY evicts LOWF → not admitted → `config_cap`. The final pool is the same at both factors (OLD1, OLD2, STICKY); the handicap changed only WHICH newcomer STICKY displaced. B prints `handicap_cap` for a name whose seat the handicap did not cost — a false line on his screen under v2 §4's promise ("lost its seat BECAUSE of the handicap", v2 `:145`). My definition says what it labels ("inside `winners` with its own factor at 1"), and what it labels is not what §4 promises. B falls.
3. "it enters iff its raw key beats `winners[-1]`" — it enters `winners`, not the pool: `:366-378` can still evict it. WITHDRAWN (below).

**ADOPT A.** Grok's O6 predicate, verbatim: relabel (under R2-2.1 B: set `decisive: true`) "only when that name's `h < 1` and a second `decide()` with that name's `h` set to 1, every other input unchanged, admits the name (`ADMIT`, `RETAIN`, or `HOLD`) where the real `decide()` does not. Both calls include stickiness and held seats. Membership in `winners` is not the test."
1. Correct where B is not (item 2); deterministic (`decide()` is a pure sort whose key ends with `ticker`, `pool.py:209-211`, and every input is in memory at `runner.py:186`).
2. Price: `decide()` gains the `factors` map both R2-1 positions already need; a `counterfactual: bool` guard so the second call does not label; one call per `CONFIG_CAP` transition whose name has factor < 1 (`:351`, `:370`, `:397-400`) — the handicapped names outside the cap, not the pool. ~20 lines in `pool.py` (or `radar/handicap.py`), H2, no migration. Its scan-time cost is v2 X13, unrun; grok's own fallback if X13 exceeds the cadence ("stamp it in the post-scan replay, not inline. The O6 predicate stays") is taken with it.
3. Under R2-2.1 B the result lands in `handicap.decisive`; under A it would be the enum value. The predicate is the same either way.

**WITHDRAWN:** (1) (g): "it enters iff its raw key beats `winners[-1]`" — `pool.py:366-378`.

## Withdrawn from round 1

Seven sentences of `reports/float-handicap-tribunal-fable-r1-2026-09-21.md`, each with what defeats it:

1. O4 (`:47`): "rank ≤ N, admitted, `pool_position` ≤ N on the ladder … whatever `handicap.factor` is" — unconditional admission: `pool.py:366-378` displaces the worst newcomer. Rank ≤ N stands.
2. O4 (`:47`): "For every name of that screen 'competes on its LOWERED score for the pool's 50' (R28 (b)) is false" — a verdict, not a fact; under A (`pool.py:161-165`, `:209`) the name competes inside its tier. Kept as A's limit.
3. O6 (`:55`): "no rollback in the tree has ever narrowed a CHECK — `0004` / `0008` / `0009` rollbacks checked" — `0005_heartbeat_note_absent.rollback.sql:36-41`; `0006_radar_score.rollback.sql:47-49`.
4. O6 (`:55`): "H2 live on evening N writes `handicap_cap` on never-admitted episodes; evening N+1 H2 is reverted … the replay of day N now runs code whose `EPISODE_EXCLUSIONS` lacks the value" — the rows are day N+1's scans (`store.py:135-157`); the failing replay is day N+1's or a re-run. Shape kept (`movers.py:444-447`, `replay/runner.py:461`).
5. O6 (`:55`): "A lossless rewrite needs the fact stored elsewhere anyway — then the enum is the second copy (L3)" — L3 concerns duplicate implementations; a reverted feature's outputs may be lost (`0006 rollback:22-25` does exactly that).
6. O6 (`:55`): "Price of the replacement: two edits (`movers.py` episode JSON, `line.py:119`)" — six: `replay/models.py:409`, `store.py:55-58`, `movers.py:452-458`, `line.py:119`, `replay/runner.py:367`, `radar_panel.py:832`.
7. (g) (`:90`): "Gap: `formula_sha256` covers `scoring.py` but not `radar/pool.py` or `radar/config.py` … the one-line `FORMULA_FILES` addition closes it" and "it enters iff its raw key beats `winners[-1]`" — `evaluate.py:144-152` (the pool has never been hashed; the factor is a stored row input); `pool.py:366-378`.

Grok's claims file-checked here: "a pool-wide re-sort lets a small `h` drop a screen name behind a list name" HOLDS (R2-1.1 item 1); "`position` … among names that reach ranking" is a change to raw order at h = 1 — HOLDS as grok states it, and defeats grok's own `[F-16]` (R2-1.2); "the boolean loses the miss line, which groups by one column" DOES NOT HOLD (`line.py:60`, `:95-98`, `:119`); "`winners`-membership would say `handicap_cap`" on the 11:00 scenario DOES NOT HOLD against (g)'s outside-`winners` clause (R2-2.2 item 1) — my own scenario defeats (g) instead.

## Experiments (L70)

- None new. Cited: **X2** (R2-1.1 — the marginal seat's tier on the stored RTH cache; equity names on the `first_from` screen against `cap`) · **X12** (R2-1.2 — the h = 1 pass must reproduce stored membership; grok's basis fails it by construction) · **X15** (R2-1.2 — whether the fund distortion bites; if it does, its own change with its own shadow) · **X13** (R2-2.2 — the cost of one extra `decide()` per `config_cap` name with factor < 1; over the cadence → grok's post-scan stamp) · **X16** — moot under R2-2.1 B; if the derive keeps the enum, the two rollback files already show the REFUSE and REWRITE shapes and the experiment only confirms Postgres validating existing rows.
- Not experiments (logic from reads, stated as such): the raise and its re-raise (`movers.py:444-447`, `replay/runner.py:461`); `inputs_sha256` changing when an episode entry gains a key (`movers.py:481`, `:492`); `Episode` dropping an unselected or unlisted column (`replay/models.py:401`).

## OWNER

1. R2-1.1 — may a handicapped name fall behind a list name, or behind another screen's name after `first_from`? Yes = POOL-WIDE; no = TIER-BOUND. H1 builds TIER-BOUND now; the dry-run's cut-tier line and X2 bring him the evidence.
2. `handicap.factor`, `handicap.combinator`, `handicap.missing`, `handicap.mode` — his keys, read after the tribunal (round-1 OWNER 2–4, unchanged).
3. Shadow length before `shadow → live` and the approve itself (round-1 OWNER 5, unchanged).
4. Whether a `decisive` name is shown apart on the `/radar` excluded list beyond the `config_cap (handicap)` suffix (round-1 OWNER 7, unchanged).

## READING

- `LAWS.md` in full (L1–L74).
- `reports/float-handicap-tribunal-derive-2026-09-21.md` `:1-13`, `:76-87`.
- `prompts/2026-09-22/19-handicap-tribunal-r2.md` `:16-31` (the staging list, for the v2 ranges and the hub rows), `:32` (QUESTIONS-R2 verbatim), `:45` (C1–C9).
- `reports/float-handicap-tribunal-fable-r1-2026-09-21.md` `:46-56`, `:67-72`, `:88-92`, `:119-136`.
- `docs/30 - Design/FLOAT-HANDICAP-v2-2026-09-21.md` `:1-14`, `:63-100`, `:145`, `:161-179`, `:201-213`, `:223-230`, `:237`, `:270-271`, `:273`, `:283-284`, `:286-287`, `:291-304`.
- `reports/float-handicap-tribunal-2026-09-21.md` `:114-117`, `:120`, `:124`.
- `reports/cto-2026-09-21.md` `:39-41` (R28, R29, R30).
- `src/cobalt/radar/pool.py` `:94-212`, `:236-400` · `src/cobalt/radar/models.py` `:75-116`, `:160-195` · `src/cobalt/radar/runner.py` `:120-160`, `:178-192` · `src/cobalt/radar/store.py` `:46-58`, `:108-160` · `src/cobalt/radar/evaluate.py` `:140-170`.
- `src/cobalt/replay/movers.py` `:95-110`, `:402-430`, `:430-495` · `src/cobalt/replay/models.py` `:25-40`, `:398-410` · `src/cobalt/replay/line.py` `:55-62`, `:90-125` · `src/cobalt/replay/runner.py` `:276-300` (plus greps at `:288`, `:363-367`, `:461`).
- `src/cobalt/db_migrations/0004_radar_pool.sql` `:36-50` · `0009_picks_missed.sql` `:74-84` · `0005_heartbeat_note_absent.rollback.sql` `:1-42` · `0006_radar_score.rollback.sql` `:1-50` · `__init__.py` `:8-28` · `ls` of the folder · a grep of every CHECK / constraint line in the folder.
- `src/cobalt/aset/radar_panel.py`, `src/cobalt/dayopen/checks.py` `:122-150` — grep for `excluded_by` (consumers).
- `1 - Trading/Radar Screens.md` — grep for headings and the `first_from` key only (`:25`, `:239`, `:242`); no value and no filter string copied.
- Not read: the round-2 folder, the round-2 hub report, any round-2 house file; `../r1/` raw rulings (the derive's quotes sufficed).

## ESCALATE

1. **R2-1.1 stays his.** This seat's answer is OWNER, not A or B; if grok or gemini also answer OWNER, or split, L39 sends the one-line question above to Dejan — never a vote, never a round 3. H1 is not blocked: it builds TIER-BOUND in shadow with `raw_rank` stored, which keeps POOL-WIDE recomputable.
2. **v2 carries a wrong fact of mine verbatim** (`:295-297`, the dissent: "no rollback in the tree has ever narrowed a CHECK") and X16's premise rests on it (`:287`). The derive (`21`) must strike it and cite `0005 rollback:36-41` / `0006 rollback:47-49` instead; X16 becomes moot under R2-2.1 B.
3. **Position A's basis sentence contradicts its own adopted `[F-16]`** ("`position` is the 1-based place among names that reach ranking" at v2 `:70` against "The `h = 1` pass must match stored membership" at `:213`). If the derive builds A for R2-1.1, that sentence is struck (R2-1.2 B) so the dry-run gate can hold.

## CONTINUE

next: none — all four questions ruled; the desk commits this report; after the hub's `FLOAT HANDICAP TRIBUNAL R2 DONE` line is committed, `21-handicap-tribunal-derive-r2.md` runs (this seat, no longer blind).

FLOAT HANDICAP TRIBUNAL FABLE R2 DONE · R2-1: OWNER/ADOPT B · R2-2: ADOPT B/ADOPT A · owner: 1 · ESCALATE: 3
