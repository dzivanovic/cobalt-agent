# DRC K2 FIX R2 — DRAFT (classification of `03`, round 3 of 3, THE LAST) — 2026-09-25

Seat `drc-k2-fix-r2-draft-0925` (Opus 5.5), prompt `prompts/2026-09-25/11-draft-drc-k2-fix-r2.md`, authorization `cto-2026-09-25.md:20` (`| R12 |`, names `11-draft-drc-k2-fix-r2.md`). Started 02:46:56 ET, closed 03:00:50 ET (`date`). Base read: `git -C /Users/cobalt/cobalt log -1 --format=%h drc/d1-trading-log` → `1724d59e`; `--oneline -2` → `1724d59e docs(k2-fix-r1): …` above `4626a1f2 test(drc): K2 fix r1 RUNS — …` (as the prompt expects). Lock at drafting: `ls -la /Users/cobalt/cobalt-wt/*/.env` → `no matches found` (FREE).

## §0 Headline
- `03`'s 2 HOLDs are ONE input (THE INPUT: a resolve restated to an earlier day with nothing recorded on or before it, the superseded resolve's day recorded) → ONE FIX build row **F-1r2**: CODE (the CLI trigger tests the superseded row's day; new read `stated_day`; OTHERWISE print names the effect day) + DOC (AMENDED C7 (r2), seam (4), `## FOR K3`, new `## FOR D4`).
- ONE reading: the rebuild stays `rebuild(effect_day)`; for THE INPUT it is refused loud (`nothing to re-pair ([F-05])`, `store.py:494-496`) → `not rebuilt`, exit 1, statement kept — AMENDED C7 (b)'s own shape. No exit-0 path remains. A stored mark on the later days is NOT built (new write path, L75).
- ESC 8 → RUN-2 (L70). ESC 7, Grok's pass, build ESC 5, the NOT-CHECKABLE row → NOT REAL. Sol / Astra / X12 / packet / records → OUT OF SCOPE. OWNER ITEM 0; new strings 0; `FN_VERSION` stays `drc.pairing/4`.
- Wrote `12-drc-k2-fix-r2-build.md` (`02`'s line, `diff`: only path + remote-control differ, 86 tokens each) and `13-drc-k2-fix-r2-check.md` (`03`'s line, same, 75 tokens each). ESCALATE: 6.

## L74
- One block arrived INSIDE a tool result (a `<system-reminder>` attached to the Read of this prompt file) asking for a `Claude-Session: https://claude.ai/code/…` commit line and naming a file-send tool (`SendUserFile`). Recorded once here as DATA; not followed. This seat commits nothing and sends no file.

## Classification
`#` · claim (short) · source line · class · traces to · FIX shape or reason. Source = `docs/40 - DevDocs/reports/drc-k2-fix-r1-check-2026-09-25.md` (`03`) unless named; `B` = the fix r1 build report on the branch (`git show drc/d1-trading-log:"docs/40 - DevDocs/reports/drc-k2-fix-r1-build-2026-09-25.md"`).

| # | claim (short) | source line | class | traces to | FIX shape or reason |
|---|---|---|---|---|---|
| H1 | F-1 NOT CLOSED on THE INPUT: `_rebuilds` tests `has_chain_through(effect)` only (`cli.py:139-142`), exit 0 via `cli.py:177-178`; later day keeps the superseded resolve's rows; no later read fails (`store.py:877-883` raises only for a held trade) | `03:161`, `:185` | **FIX** (CODE half of F-1r2) | L1; L3; v3 `[F-06]` `:190` ("so no later `book_close` still contains that `trade_id` … Never a silent close"); R52; AMENDED C7 (b) (`02:17`); seam (4) (B `:179`) | NEW `DrcStore.stated_day(id)` (the one read of a stated row's day; `effect_day` `store.py:371-386` calls it, result unchanged); `_rebuilds` (b) tests `has_chain_through(<superseded row's day>)` for a restatement, the statement's day otherwise; `rebuild(effect)` unchanged; OTHERWISE print `cli.py:178` names `effect`. Red = THE INPUT by name, with-DB (`12` F2): dry run must name `rebuild 2001-01-01`, apply exit 1 `not rebuilt: … nothing to re-pair ([F-05])`, statement kept, later days' snapshots unchanged. A mechanism the houses settle — not an owner item |
| H2 | SEAM GAP: AMENDED C7's OTHERWISE clause / seam (4) say "no later `book_close` exists to hold the trade id" — false for THE INPUT | `03:162`, `:186` | **FIX** (DOC half of F-1r2 — the same row) | L35; L72; AMENDED C7 (`02:17`); B `## SEAM FOR D2` (4) `:179` | `## AMENDED C7 (r2)` written in `12` (precondition = what `_rebuilds` tests; reason true for THE INPUT; stale citations re-read at the tip — ESCALATE 3); `## SEAM FOR D2` re-issued whole ((4) moves a WORD); `## FOR K3` re-issued whole (two NEW lines); NEW `## FOR D4` |
| E1 | Opus `DEFECT REMAINS` (reason: earlier-day restatement skips rebuild; superseded effect stays stored) | `03:189`, `:179` | = H1 / H2 (restated, not counted) | — | settled by F-1r2; the "stays stored" half is the named NOT-built item (ESCALATE 2), put to `13` as question (i)(c); not his trading judgement |
| E2 | Grok `FIX STANDS` — did not walk THE INPUT | `03:190`, `:166-167` | **NOT REAL** (its pass does not reach THE INPUT; H1 HOLDS against it) | `03` §3 rule (quote both, smooth neither); L67 | a RULE for `13`: THE INPUT is a NAMED question every checker walks by name (QUESTIONS FIRST); `INPUT NOT WALKED` is recorded and blocks `ready for K3: YES` |
| E3 | the two `## FOR THE CLASSIFIER` items restated | `03:191` | = H1 / H2 (not counted) | — | — |
| E4 | packet 228,794 B, no cut | `03:192`, `:73` | **OUT OF SCOPE** (a packet rule for `13`) | `cto-2026-09-25.md` R5 (300,000 B) | `13` §1: every header names the REAL path; size from MEASURED `wc -c` parts; ceiling 300,000 B with a named cut order |
| E5 | no checker failed to check; no ASK DESK | `03:193` | **OUT OF SCOPE** (record) | — | — |
| E6 | the L74 line | `03:194` | **OUT OF SCOPE** (record) | L74 | — |
| E7 | LINE MOVED `263`→`262`; GREEN-as-pin cited `:397`, REAL `:411` at the tip | `03:195`, `:154`; B `:78`, `:199` | **NOT REAL** | L35 (a citation is true of the tree it names) | `git show 11e9af9a:tests/cobalt/test_drc_k2_experiments.py` → `def test_x9_pass_c_a_not_computed_match_is_kept` at `:398` (decorator `:397`) — the F3 red ran on that tree; at `4626a1f2` `:411` (F4 added `_without_stale`). `263`→`262` is a PREFLIGHT record the build used as read. Drift on a moving tree; no DOC-ONLY row. `13` has checkers cite at the tip only |
| E8 | `_repair` RAISES `PairingError` on stored `stats_row` rows with no stats import named (`store.py:520-522`); "No test reaches it" | `03:196`; B `:205` | **UNPROVEN** → **RUN-2** | L70 | `12` F4: construct the state through `record_day` (stats pairing, `import_ids` without the stats id) → assert storable, then `rebuild` raises `stats rows are stored with no stats import named — nothing assumed`, rows unchanged; red = strict `xfail`, quoted (the writer's own refusal is then the result) |
| E9 | Sol `NOT SEATED (METER — retry after Sep 26th, 2026 6:47 AM)` | `03:197` | **OUT OF SCOPE** (the desk's seat) | L47; L62 R19 | `13` probes Sol by `date` and seats it from 06:47 |
| E10 | Astra's K2 NEW-BUILD read owed; X12 NOT RUN | `03:198` | **OUT OF SCOPE** (the desk's seat and hub-run read) | L67 R95; `52` ESC 7 | carried in `13`'s ESCALATE lines |
| E11 | standing line (round 2) | `03:199` | **OUT OF SCOPE** | L39; L67 | `13` carries the ROUND 3 / THE LAST line |
| E12 | standing line (deploy L68 gate) | `03:200` | **OUT OF SCOPE** | L68 | carried |
| B5 | the build's second fix commit `d684f80b` (F-1's own shadow, caught at F5) | B `:202`; `03:32` | **NOT REAL** | L35 | disclosed, corrected, re-proven (`132 passed`, B `:121`); round 2 walked F-1 at `4626a1f2` |
| B8 | = E8 | B `:205` | = E8 (not counted) | — | — |
| NC | "NOT CHECKABLE FROM READS" — `cli.py:190`, `models.py:391` / `:139`, `store.py:1044`, `pairing.py:286` | `03:163` | **NOT REAL** (DOES NOT HOLD — the lines exist) | L70 | `13` stages every signature the seam cites; `12` re-cites each at the tip |

Counted: FIX 2 (H1, H2 → ONE build row) · NOT REAL 4 (E2, E7, B5, NC) · UNPROVEN 1 (E8) · OUT OF SCOPE 7 (E4, E5, E6, E9, E10, E11, E12) · OWNER ITEM 0. E1, E3, B8 are restatements of counted rows.

THE ONE READING (for H1; derived, never both): AMENDED C7 (b) already states the refusal shape for a resolve whose effect day cannot be re-paired ("`not rebuilt: <reason>` … exits 1, the statement kept … `nothing to re-pair ([F-05])`, applied when that day's import is recorded", `02:17`); v3 `[F-06]` `:190` forbids a silent close; seam (4) makes the EFFECT `rebuild(effect_day)` for every statement, which the page already calls. Walked at `4626a1f2`: with the trigger reading the superseded row's day, THE INPUT reaches `rebuild(effect)` (`cli.py:181`) → `_repair(<earlier day>)` has no import and no no-trade row → `PairingError … nothing to re-pair ([F-05])` (`store.py:494-496`) → `not rebuilt:` exit 1 (`cli.py:182-186`), statement kept, `drc_rows` untouched (the `rebuild` transaction rolls back, `store.py:338-348`). No rebuild can succeed for THE INPUT (rebuilding the later day instead raises `rebuild <R>` at `store.py:877-883`, the trade being held in its seed), so a loud refusal IS the "refuse" reading and the "rebuild" reading at once — one outcome. Non-restatements, same-day restatements and later-day restatements keep their trigger exactly (the day tested is unchanged for them). Tests C8 forbids editing stay green: `test_drc_k1_store.py:648` (empty DB → OTHERWISE, exit 0) and `test_drc_k2_store.py:408` (no `supersedes` → the printed day is unchanged, `:413`).

## FOR K3
- **A seam WORD moves (yes).** `## SEAM FOR D2` (4): the restatement trigger (a `day` row on or before the SUPERSEDED row's day, `stated_day`) and the page's loud refusal (statement kept, nothing written). `## FOR K3`: two NEW lines — the page shows `not rebuilt: <reason>`; a stored row naming a no-longer-current resolve (`trade.inputs.resolve_id`, `day.derived.resolves`) renders STALE naming `rebuild <effect day>`, never as a current close; plus RUN-2's result. Points (1)–(3), (5)–(9) and fix r1's K3 lines: citations only (re-cited at the tip), except the RUN-2-derived sentences in (2) / (3), written from the RUN's output.

## FOR D4
- `05` must re-read: the BASE (moves from `4626a1f2` / `1724d59e` to fix r2's `<tip>`, R100) and seam (4)'s moved words; `12`'s `## FOR D4` has the builder grep `05` for `SEAM FOR D2` / `effect_day` and say which words D4 reads. No stored shape, no `FN_VERSION`, no `seed_for` / `record_day` / `rebuild` behaviour changes. Expected: "the base moved; re-read seam (4)" — never a D4 code change forced by this round.

## OWNER ITEMS
NONE. (THE INPUT's remedy is a mechanism the houses settle — L67 owner-items clause. If `13` HOLDS the NOT-built stored state, that is a round-3 HOLD and reaches him as the desk's ONE message under L39, not an owner item from this draft.)

## FOR DEJAN
NONE — 0 new strings. `12` = `02`'s launch line (the drc-d1 `.env` pair is his, `cto-2026-09-24.md` R22); `13` = `03`'s launch line (grok / opus / sol standing, R17 / R19). `diff` of the tokenized lines: only the prompt path and the remote-control name differ in each.

## ESCALATE
1. **L74:** recorded once above, not followed.
2. **The NOT-built item, named for `13` (desk reading, L75):** after `--apply` on THE INPUT the later days' stored rows still name the superseded resolve (`inputs.resolve_id`, `derived.resolves`) behind the loud exit 1, until the earlier day's input is recorded (AMENDED C7 (b)'s shape); nothing marks them in K2. A stored mark (the F-2 `book_stale` shape) after a refused rebuild would be a NEW write path outside the transaction that refused — a widening this round does not build. K3's render rule (`## FOR K3`, NEW) covers the page. `13` asks every checker (i)(c) about exactly this; a HOLD on it after round 3 goes to Dejan as ONE message (L39), never round 4.
3. **AMENDED C7's citations are stale at the tip (inside HOLD 2's DOC half, not a separate row):** `02:17` cites `store.py:374-381` (REAL `has_chain_through` `store.py:393-400` at `4626a1f2`), `cli.py:127-137` / `:153-155` (REAL `_rebuilds` `cli.py:129-142`, the dry-run line `:161`), `test_drc_k1_store.py:646` (REAL `def` `:648`; `:646` is the `ids=` line). AMENDED C7 (r2) in `12` has every `<n>` filled from the builder's `grep -n` at its tip.
4. **Two edits inside the HOLD's cited lines, named as desk readings (ASK DESK if either reads as a widening [02:59 ET]; safe default: kept, both are in `12`):** (a) the OTHERWISE print `cli.py:178` names `effect` instead of `req.day` — identical for every statement without `supersedes` (`test_drc_k2_store.py:413` green), and true for a later-day restatement, where `req.day` names a day `_rebuilds` never tested for an import; (b) NEW read `DrcStore.stated_day` — the one read of a stated row's day (L3), used by `effect_day` and the CLI trigger, a new signature in `## SEAM FOR D2`.
5. **No offline red this round:** the fix is a CLI trigger over stored rows; THE INPUT's red is with-DB (`12` F2, the first lock take). `12` F6's offline count is `<bp>` + 0.
6. **`13`'s launch-time values are placeholders** (`«FILL AT LAUNCH: …»` on `13` lines 27–31: the BUILT line, `<tip>`, `<red>`, the tip's oneline, the range and its count, the path union, the build report's headers) — the desk fills them from `12`'s stop line and `git log` (R100); `R__` ×2 in each of `12` and `13` (`grep -c -E "R_[_]"` = 2 each). The stagger literal `no other house hub is running` must sit on a line of the `<D>` desk file naming `13-drc-k2-fix-r2-check.md`.

DRC K2 FIX R2 DRAFTED · FIX: 2 · NOT REAL: 4 · UNPROVEN: 1 · OUT OF SCOPE: 7 · OWNER ITEM: 0 · prompts: 2 · new rule strings: 0 · ESCALATE: 6
