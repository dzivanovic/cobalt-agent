# Ops 2026-09-19 — five small defect fixes (offline build, worktree `ops-2026-09-19`, branch `ops/2026-09-19`)

## §0 Headline
DO NOT STOP until OPS 0919
- ALL FIVE BUILT, tests first, each its own commit: (a) C2 trading-day guard · (b1) trade-count band validator · (c) `lock_timeout` on the migrate transaction · (d) test hygiene A1/A2 · (e) the migrate output names its code tip. Branch `ops/2026-09-19`, build tip `fc1d967` (this report commits above it).
- Offline: `1608 passed, 298 skipped, 0 failed` vs BASELINE `1562/297/0` — +46 passed, +1 skipped, all named below. RESTARTS: `com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED.
- Law step BUILD only: never merged, deployed, pushed; no production, no `cobalt_dev`, no `.env`, no vault write.
- OWED: three-house check · the dev-DB run of 1 new + 1 edited `requires_db` tests, none ever run · the next deploy prompt. [corrected in round 2, R7: this read "1 new + 2 edited"; only `test_the_alter_waits_for_an_open_transaction_and_then_completes` was edited — ESCALATE 8's own text says the second test "shares `JOBS_TABLE` but not the helper"]
- ESCALATE: 9.

## AUTHORIZATION
Verified by this hub at 09:31–09:36 ET, 2026-09-19, before any work.

| what | how verified | result |
|---|---|---|
| Dejan's approval of this job shape | `cto-2026-09-18.md:14` §4 **R1**, 06:46 ET, "Approved" — list **(2)** `02-ops-2026-09-18.md` (Opus, new worktree `ops-2026-09-18`), "its launch-line list as committed `5fd90e8`" | MATCHES the prompt's claim verbatim |
| the rows are committed on main | `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` → `919d562`, `db2dcb1`, `e748c8c`, `8b0a3bb`, `32bbfe8`, `6c582ef`, `920b0a9`, `11960d9` | committed |
| NO NEW RULE — every rule string of today's launch line | `grep -c -F '<rule>' prompts/2026-09-18/02-ops-2026-09-18.md` for all 16 `Bash(...)` rules + `--disallowedTools "AskUserQuestion" "EnterWorktree"` + the three `--add-dir` | **every one counts 1** (16 + 1 + 1 = 18 strings) |
| today's line adds nothing | read `02-ops-2026-09-18.md:1` in full: today's list is a strict SUBSET — `cp`/`rm` of `.env`, every `COBALT_ENV=dev`/`COBALT_ENV=production` command, `launchctl print`, `curl`, `chmod +x ops/cto-desk.sh`, `ops/cto-desk.sh --dry-run`, `shasum`, `git -C … tag --list*` are all DROPPED, none added | confirmed |
| the unattended standard | `cto-2026-09-17.md:46` §4 **R5**, 06:03 ET — "approval list ahead of time and no questions in the middle" | says what is quoted |
| no idle lane / never a skipped law step | `cto-2026-09-19.md:16` **R3** (07:12) + `:19` **R6** (07:38) | say what is quoted |
| the law is followed, not re-asked | `cto-2026-09-19.md:18` **R5** (07:35) | says what is quoted |

Laws read in full before work: `UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `areas/cobalt.md` `## NOW` → `LAWS.md` (L1–L67, in full) → `16-packet/facts.md` (the spec) → `cto-2026-09-19.md` §0, §5a, §8, §14, §15 → `cto-2026-09-18.md` §18 row F2 → `prod-proof-only-3-2026-09-19.md` `## ESCALATE` 2–3.

**Verdict: AUTHORIZED. No rule in this launch line is absent from the committed line Dejan approved 2026-09-18 06:46 ET.**

## PREFLIGHT
Run 09:31–09:33 ET. Every probe sits inside its own rule's pattern.

| # | rule | command | exit | verdict |
|---|---|---|---|---|
| 1 | `Bash(date*)` | `date` | 0 | allowed — `Sat Sep 19 09:31:19 EDT 2026` |
| 2 | `Bash(git -C /Users/cobalt/cobalt log*)` | `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` | 0 | allowed — 8 rows, authorization proof above |
| 3 | `Bash(grep *)` | `grep -c -F '<rule>' …/02-ops-2026-09-18.md` × 18 | 0 | allowed — every count 1 |
| 4 | `Bash(git status*)` | `git status --porcelain` | 0 | allowed — **no output: worktree clean** |
| 5 | `Bash(git status*)` | `git status` (long form) | 0 | allowed — first two lines: `On branch ops/2026-09-19` / `nothing to commit, working tree clean`. Branch correct; no rebase in progress |
| 6 | `Bash(git log*)` | `git log --oneline -1` | 0 | allowed — `4c14712 docs(desk): 09-19 ops small-items build prompt (offline, no new rule) + facts packet` — **first launch**: main's tip at the moment the desk created the worktree |
| 7 | `Bash(git -C /Users/cobalt/cobalt log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | allowed — `4c14712` (main is at the same tip) |
| 8 | `Bash(ls *)` | `ls -la .env` | 1 | allowed — `ls: .env: No such file or directory`. **This run is offline: the suite cannot reach `cobalt_dev`.** |
| 9 | `Bash(uv run pytest *)` | `uv run pytest --co -q tests/cobalt/test_dayopen_checks.py` | 0 | allowed — `26 tests collected` |
| 10 | `Bash(uv run cobalt jobs restarts *)` | `uv run cobalt jobs restarts main..HEAD` | 0 | allowed — empty range, header + `RESTARTS: none` |

**No denials. 0 rules failed.**

## BASELINE
Before any edit, 09:34–09:35 ET.

```
$ uv run pytest -q tests/cobalt tests/taxonomy
1562 passed, 297 skipped, 1 xfailed, 15 warnings in 43.66s
```

```
$ uv run pytest -q tests/cobalt/test_migrate_proof.py
25 passed, 16 skipped in 0.46s
```

**BASELINE = 1562 passed / 0 failed / 297 skipped (+1 xfailed).** `test_migrate_proof.py` alone = 25 passed / 16 skipped (item d's comparison). Not red — nothing to escalate.

## Item a — day-open C2 gets the trading-day guard (`facts.md` §a)

**Status: BUILT.** Tests first.

### Red first
Two new tests written BEFORE the change, run 09:38 ET:

```
$ uv run pytest -q tests/cobalt/test_dayopen_checks.py -k "c2"
2 failed, 6 passed, 21 deselected in 0.11s
FAILED tests/cobalt/test_dayopen_checks.py::test_c2_non_trading_day_with_zero_rows_is_pass[non_trading_day0]
FAILED tests/cobalt/test_dayopen_checks.py::test_c2_non_trading_day_with_zero_rows_is_pass[non_trading_day1]
```

Both failures verbatim, and they are the real defect:

```
E  AssertionError: assert <Verdict.FAIL: 'FAIL'> is <Verdict.PASS: 'PASS'>
E   +  where <Verdict.FAIL: 'FAIL'> = CheckResult(id='C2', …, detail='FAIL — no rows for 2026-09-13', raw='count\t0\npool block metric (premarket): volume').verdict
E   +  where <Verdict.FAIL: 'FAIL'> = CheckResult(id='C2', …, detail='FAIL — no rows for 2026-09-19', raw='count\t0\npool block metric (premarket): volume').verdict
```

`2026-09-19` is the Saturday false alarm of `cto-2026-09-19.md` §0, reproduced offline.

### The change
`src/cobalt/dayopen/checks.py`, `check_c2_radar_membership` ONLY. The calendar read
C3 already uses (`clock.windows_for(report_date)` empty = not a trading day) moves
AHEAD of the `count == 0` FAIL:

```python
    windows = clock.windows_for(report_date)
    if not windows and count == 0:
        return CheckResult(
            "C2", title, Verdict.PASS,
            f"PASS — no session today ({report_date} is not a trading day)",
            raw,
        )

    if count == 0:
        return CheckResult("C2", title, Verdict.FAIL, f"FAIL — no rows for {report_date}", raw)
```

`windows` is now computed once and reused by the on-time branch below it (it was read
there before); no other line of the function changed. `raw` is built above this point,
so the raw block is still produced on the new PASS path — a test asserts it.
`src/cobalt/dayopen/models.py` NOT touched (the unmerged P4 branch changes it).

### Verify
| what | result |
|---|---|
| the two new tests | green (`test_c2_non_trading_day_with_zero_rows_is_pass[SUNDAY]`, `[SATURDAY]`) |
| `test_c2_fail_zero_rows` (MONDAY, existing) | **unedited, still green** — a trading day with zero rows is still FAIL |
| `test_c2_trading_day_zero_rows_still_fails` (new) | green — pins the same rule beside the guard |
| the file's other tests | `29 passed in 0.13s` = BASELINE 26 + 3 new, none edited |
| full offline suite | `1565 passed, 297 skipped, 1 xfailed, 15 warnings in 40.39s` — BASELINE + 3 passed, skipped unchanged, **0 failed** |

### Diff stat
```
 docs/40 - DevDocs/cobalt/dayopen/checks.md | 10 +++++++
 src/cobalt/dayopen/checks.py               | 14 +++++++++-
 tests/cobalt/test_dayopen_checks.py        | 45 ++++++++++++++++++++++++++++++
 3 files changed, 68 insertions(+), 1 deletion(-)
```

DevDoc `docs/40 - DevDocs/cobalt/dayopen/checks.md`: C2's paragraph now states what a
non-trading day returns, that the raw block is still produced, and that a trading day
with zero rows is still FAIL.

**NOT in the bound, ESCALATE instead of built:** a TRADING day before premarket open.
C3 exempts that case (`past_open`), C2 does not, so on a trading day at 03:00 ET C2 still
FAILs on zero rows. Unchanged behaviour; not built here because item a's bound is the
non-trading day. [corrected in round 2, R9: this sentence ended "see `## ESCALATE` 1",
which points at the DevDocs symbol check — an unrelated entry. Round 2 took option (ii),
dropping the dangling pointer and stating the case here instead of renumbering round 1's
ESCALATE list; the case is carried with a number of its own in `## ROUND 2 ESCALATE` 1.]

## Item b1 — the trade-count band refuses what cannot be a band (`facts.md` §b)

**Status: BUILT. NO FACT CHANGED.** This is the day-mode path (L7 / CLAUDE.md HITL),
so the no-change claim is proven, not asserted.

### Red first, in two stages — because "byte-identical" has to be measured, not promised
**Stage 1 (09:44 ET), BEFORE the validator existed:** the accepted-band pin test was
written alone and run against the UNTOUCHED code path. It computes `_facts(...)` for
each of the five valid bands and compares it with a pinned literal dict:

```
$ uv run pytest -q tests/cobalt/test_daymode.py -k "TestABandThatIsValidChangesNothing"
5 passed, 63 deselected in 0.08s
```

Green on untouched code ⇒ **the pinned dicts ARE today's output**, per band. They are
what the rest of the item is measured against.

**Stage 2 (09:46 ET),** the refusal + wiring tests added:

```
$ uv run pytest -q tests/cobalt/test_daymode.py -k "Validator or BothReaders or ABandThatIsValid"
18 failed, 10 passed, 63 deselected in 0.31s
```

- 12 failed on `ImportError: cannot import name 'validate_band' / 'BandError' from 'cobalt.daymode.propose'` — 7 of the refusal class + the 5 `test_the_validator_accepts_it` cases [corrected in round 2, R6: this read "13", which made the breakdown sum to 19 against the 18 above]
- 5 failed `DID NOT RAISE <class 'SystemExit'>` — `_band()` accepted `(6, 2)`, `(2, "6")`, `(2.5, 6)`, `(-1, 6)`, `(True, 6)`, which is the defect of `cto-2026-09-18.md` §18 F2
- 1 failed `assert 'validate_band' in '<source of _cmd_validate>'` — the deploy gate printed the rows and checked nothing
- the **10 that passed** are the five facts pins and the five accepted-band `_band()`
  round-trips: they pass before AND after, which is the no-change proof

### The change — ONE validator, two call sites (L3)
`src/cobalt/daymode/propose.py`, beside `BAND_MIN_KEY`/`BAND_MAX_KEY`: `BandError`
(a `ValueError`) and `validate_band(band_min, band_max) -> None`.

| band | verdict |
|---|---|
| both set, whole ints ≥ 0, `min <= max` | accepted |
| both set, `min > max` | `BandError` — "min must be <= max — nothing can be inside an inverted band" |
| either value not a whole non-negative count (`"6"`, `2.5`, `-1`, `True`) | `BandError` — a `bool` is an `int` to Python and is explicitly not a count here |
| **both** null | accepted, unchanged — unruled PLACEHOLDER, itself adverse |
| **exactly one** null (HALF-SET) | **accepted, unchanged** — b2 is scoped out; the docstring says so and a test pins it |

Every refusal message carries **both keys and both values** plus the file to fix:
`the trade-count band is not a band: daymode.trade_count_band.min=6, daymode.trade_count_band.max=2 — … Fix the two rows in configs/cobalt/taxonomy/tunables.yaml; F6 reads the band from config and has no built-in default (F16).`

Call sites, both of them:
- `src/cobalt/daymode/cli.py` `_band()` — catches `BandError`, re-raises `SystemExit(str(e))`, matching its existing missing-row style.
- `src/cobalt/cli.py` `_cmd_validate` — after printing the two rows it now validates; on a refusal it prints `FAILED: <message>` and `sys.exit(1)`, exactly like its neighbours (the `SheetMode` and missing-row branches above it).

### Verify
| what | result |
|---|---|
| refusals | 5 bands × (`validate_band` refuses, naming both keys/values) + 5 × (`_band()` exits) + 2 message tests = green |
| **no fact changed** | 5 accepted bands, `_facts` dict equals the literal pinned on untouched code; the last three still carry `trade_count_band_placeholder: True` |
| `_band()` round-trip | returns each accepted band unchanged, including `(2, None)` and `(None, 6)` |
| `test_a_day_with_7_trades_is_adverse` | **not edited**, green |
| `test_the_shipped_band_is_the_ruled_2_to_6` (his live band 2–6) | not edited, green |
| `cobalt validate` wiring | proven by unit test + a source check that `_cmd_validate` calls `validate_band` and carries **no second copy** of the rule (L3). The command is NOT run — no rule for it in this launch line, and whether it needs a database is NOT VERIFIED (`facts.md` "Cross-branch facts") |
| file suite | `80 passed, 11 skipped in 0.31s` (BASELINE 52 passed / 11 skipped + 28 new) |
| full offline suite | `1593 passed, 297 skipped, 1 xfailed, 15 warnings in 40.05s` — BASELINE + 31, skipped unchanged, **0 failed** |

L52 check: the band feeds `day_facts` and the day-mode ladder. It does not touch
scoring, ranking, or what reaches a card. Nothing to stop.

### Diff stat
```
 docs/40 - DevDocs/cobalt/daymode/cli.md     |  10 ++
 docs/40 - DevDocs/cobalt/daymode/propose.md |  30 ++++++
 src/cobalt/cli.py                           |  18 +++-
 src/cobalt/daymode/cli.py                   |  19 +++-
 src/cobalt/daymode/propose.py               |  65 ++++++++++++
 tests/cobalt/test_daymode.py                | 152 ++++++++++++++++++++++++++++
 6 files changed, 291 insertions(+), 3 deletions(-)
```

## Item c — `lock_timeout` on the migrate transaction (`facts.md` §c)

**Status: BUILT, with one design point taken as the prompt's named safe default and
escalated (ESCALATE 2).**

### Red first
```
$ uv run pytest -q tests/cobalt/test_migrate_proof.py -k "lock_timeout or thirty_seconds or cannot_get"
9 failed, 1 skipped, 41 deselected in 0.60s
```
- 5 failed `AttributeError: module 'cobalt.db_migrations.cli' has no attribute 'DEFAULT_LOCK_TIMEOUT_S'`
- 3 failed `AssertionError: a connection was opened for a refused lock ceiling` — `--lock-timeout-s 0 / -1 / -30` all reached `_connect`
- 1 failed `AssertionError: got 2 … cobalt: error: unrecognized arguments: --lock-timeout-s 0` — the flag did not exist at the CLI boundary
- **1 skipped** = the `requires_db` test, skipping cleanly offline as required (never an error)

### The change (`src/cobalt/db_migrations/cli.py` only)
- `DEFAULT_LOCK_TIMEOUT_S = 30`, a module constant with its reasoning beside it.
- `--lock-timeout-s N` (`type=int`, default the constant) on the `migrate` parser.
- Refused **before any connection is opened**, beside the `--proof-only`/`--rollback` refusal: a bool, a non-int, or `< 1` → `MigrationError` — *"0 means wait forever — that is the defect this flag closes."*
- `conn.execute(f"SET LOCAL lock_timeout = '{lock_timeout_s}s'")` in the READ-WRITE branch, between `before = _probe_all(conn)` and `_apply(conn, paths)` — so it covers forward **and** rollback, which share that branch.
- `psycopg.errors.LockNotAvailable` wrapped exactly as `SerializationFailure` already was (`rollback()` → `MigrationError`): says **NOTHING WAS APPLIED**, names the timeout that fired, hands over the `pg_locks` query that finds the holder, and states that raising the flag only makes a stuck deploy wait longer.
- `--proof-only` untouched — a test asserts it sends no `lock_timeout` at all.
- `args.lock_timeout_s` is read with `getattr(..., DEFAULT_LOCK_TIMEOUT_S)`: `tests/cobalt/test_radar_migration.py` builds this Namespace by hand and is **outside this item's bound**, so requiring the attribute would have broken a file the prompt does not name. The parser always sets it; there is one number, named once.

**`DIGEST_EXCLUDED_COLUMNS` NOT touched.** The new constant was deliberately moved
down past `TABLE_DIGEST_EXCLUDED_COLUMNS` so it shares no diff context with the tuple
P4 edits — `git diff` confirms two non-adjacent hunks, keeping the rebase trivial for
whichever branch lands second.

### THE SNAPSHOT — what was decided and why
The requirement is that the BEFORE probe and the migration see **ONE** REPEATABLE READ
snapshot. A snapshot is taken once per such transaction, so a statement issued after
the probe cannot move it. **I could not cite the Postgres manual's wording from this
run** — it is offline by design and no fetch tool is in the allowlist — so I took the
step's own named safe default: *"if you cannot cite it, the `SET LOCAL` goes AFTER
`_probe_all`'s BEFORE pass and before `_apply`, where it cannot matter."* The docstring
says exactly that, including that it is unsettled rather than settled.

**The consequence, named not hidden:** the BEFORE probe is not itself under the ceiling.
It takes ACCESS SHARE, which only an ACCESS EXCLUSIVE holder (another DDL — never a
resident's INSERT/UPDATE) can block. → `## ESCALATE` 2.

### Verify
| what | result |
|---|---|
| forward run | `SET LOCAL lock_timeout` recorded BEFORE the first migration file's real text (read off disk by the real `_apply`, L45), carrying `'30s'`; commits once, rolls back zero times |
| rollback run | same, before the first reverse file's text |
| `--proof-only` | sends no statement containing `lock_timeout` |
| `LockNotAvailable` | rollback called once, commit zero, message contains "nothing was applied", the timeout value, and `pg_locks` |
| `0`, `-1`, `-30` | refused with `--lock-timeout-s` named, and `_connect`/`db.connect_migration` both assert if reached |
| CLI boundary | real subprocess: exit 1, `FAILED: MigrationError: ` on **stderr**, flag named |
| file suite | `34 passed, 17 skipped` (BASELINE 25/16 + 9 offline + 1 `requires_db`) |
| full offline suite | `1602 passed, 298 skipped, 1 xfailed in 40.53s` — **0 failed** |

**`requires_db`, WRITTEN AND NOT RUN:**
`test_a_migration_that_cannot_get_its_lock_fails_in_about_a_second` — another session
holds ACCESS EXCLUSIVE on `system.cobalt_jobs`, migrate with `--lock-timeout-s 1` fails
in under `LOCK_CEILING_S` with nothing applied and the table's proof unchanged. It stubs
`_probe_all`, because under the conservative placement a real BEFORE probe would block
on that same holder with no ceiling — the docstring says so. **It has never run.**

### Diff stat
```
 docs/40 - DevDocs/cobalt/db_migrations/cli.md |  56 +++++-
 src/cobalt/db_migrations/cli.py               |  89 +++++++++
 tests/cobalt/test_migrate_proof.py            | 269 ++++++++++++++++++++++++++
 3 files changed, 413 insertions(+), 1 deletion(-)
```

## Item d — test hygiene A1 / A2 (`facts.md` §d) — test file only

**Status: BUILT.** `tests/cobalt/test_migrate_proof.py` ONLY — **no production code**
in this commit (diff stat below is one file).

Origin: `cto-2026-09-19.md` §8, build check round 2, Astra — "A1 test (g) leaves its
worker connection open if a lock wait outlives the 20 s ceiling…; A2 the blocked-lock
probe does not filter by pid/mode"; desk ruling: "statement_timeout on the worker
connection + cleanup on the hung path; pid + `AccessExclusiveLock` in the probe".

### A1 — a hung ALTER can no longer leave a backend waiting on `cobalt_dev`
- New `WORKER_STATEMENT_TIMEOUT_S = 25`, set on the worker connection as
  `SET LOCAL statement_timeout = '25s'` right after the BEFORE probe and before the
  thread starts. `LOCK_CEILING_S` (20 s) only bounds the MAIN thread's `join`; it cannot
  end a statement already running on another thread's connection. The server-side
  ceiling makes a hung ALTER end ITSELF.
- On the hung path the test now calls `conn.cancel()` (psycopg's documented
  cross-thread call), joins again — for `WORKER_STATEMENT_TIMEOUT_S`, the backstop's
  worth of time — and then falls into the existing `finally`, which now rolls back and
  closes on BOTH paths.
- **The assertion is unchanged**: it still fails with the same "did not finish"
  message. The cleanup does not soften the verdict.

### A1's required comment — and a fact that differs from the prompt's assumption
The prompt asked the comment to say how the harness's 30 s `lock_timeout` relates to
this test's 20 s ceiling. Read against the code: **it does not relate at all.** Test (g)
calls `cli._connect` and executes the ALTER itself; it never goes through `cmd_migrate`,
which is the only place the `SET LOCAL lock_timeout` is issued. The docstring says so
explicitly and lists what actually bounds the test, in firing order:
1. `LOCK_CEILING_S` 20 s — the main thread's join gives up **first**, so the message a
   reader sees is this test's own assertion, never a driver error;
2. `WORKER_STATEMENT_TIMEOUT_S` 25 s — deliberately ABOVE (1), so a legitimate wait is
   reported by the test rather than pre-empted by the server.

→ recorded in `## ESCALATE` 2, same root cause as the `SET LOCAL` placement.

### A2 — the blocked-lock probe now looks at the worker's own request
`_wait_for_a_blocked_lock(watcher, table, ceiling, pid)` — a fourth, required
parameter. The `WHERE` gains `l.pid = %s AND l.mode = 'AccessExclusiveLock'` beside the
existing `NOT l.granted`. The pid is `SELECT pg_backend_pid()` on the worker connection,
read from the main thread **before** the thread starts.

Its only caller is test (g), and it passes the pid. **Test (f) (`:1171`) was read
first and does not share the helper** — it starts no thread and takes no lock wait — so
it is untouched, exactly as `facts.md` §d predicted.

### Verify — these are `requires_db`, so there is no offline red-first
| what | result |
|---|---|
| `uv run pytest -q tests/cobalt/test_migrate_proof.py` | `34 passed, 17 skipped in 0.94s` |
| vs BASELINE (25 passed / 16 skipped) | passed = 25 + step 3's 9 offline tests; skipped = 16 + step 3's 1 `requires_db` test |
| errors / failures | **0 / 0** — the file still collects cleanly (a collection error would be a failure) |
| full offline suite | `1602 passed, 298 skipped, 1 xfailed in 40.60s` — unchanged from item c, **0 failed** |

**THE REAL RUN IS OWED.** A1 and A2 change only `requires_db` tests, which cannot run in
this offline lane. Their first real run on `cobalt_dev` is owed after `p4-verify-0919`'s
stop line, before this branch may deploy. This is in the stop line and in `## ESCALATE`.

### Diff stat
```
 tests/cobalt/test_migrate_proof.py | 88 ++++++++++++++++++++++++++++++++++----
 1 file changed, 79 insertions(+), 9 deletions(-)
```

## Item e — the migrate output names the code it ran from (`facts.md` §e)

**Status: BUILT.** No third git helper — the existing one imports cleanly.

### Red first
```
$ uv run pytest -q tests/cobalt/test_migrate_proof.py -k "code_it_ran_from or dirty_tree or git_failure or forward_path_prints or code_root or real_helper"
6 failed, 51 deselected in 0.15s
```
4 × `AttributeError: module 'cobalt.db_migrations.cli' has no attribute 'CODE_ROOT'` /
`'_code_line'`; 1 × `AssertionError: <proof table>` (the last line of `--proof-only`'s
output was the table, not a code line); 1 × `ValueError: list.index(x): x not in list`
(the forward path printed no such line).

### The change (`src/cobalt/db_migrations/cli.py`)
```
code: abc1234 (clean) · /Users/cobalt/cobalt
code: abc1234 (DIRTY: 2 path(s)) · /Users/cobalt/cobalt-wt/ops-2026-09-19
code: UNKNOWN — RestartError: git rev-parse --short HEAD failed: …
```
- `CODE_ROOT = Path(__file__).resolve().parents[3]` — the checkout the RUNNING `cobalt`
  package was imported from, **never** the current directory: a deploy hub `cd`s, and the
  answer must not follow it. A test pins that against `cli.__file__`.
- `_code_line()` is the LAST line of BOTH modes: after `_print_probe` under
  `--proof-only`, and after `_print_proof` on the forward path — deliberately printed
  **before** the `CHANGED` refusal, because a run that rolled back is exactly the run
  whose code an operator most needs named.
- **The git helper is imported, not rewritten (L3):** `cobalt.generated.committer._git`,
  which already takes the repo root explicitly and runs `git -C`. The import is
  function-local, so this module gains no import-time dependency on that package.
  `cobalt.generated.__init__` pulls in only `.config` (yaml + pydantic) — no DB, no
  cycle back to `db_migrations`. The alternative, `jobs/restarts._git`, was rejected: it
  would run `cobalt.jobs.__init__`, which imports `.store`, `.watchdog` and `.wrapper`.
  **No ESCALATE was needed here — the import works, and a test proves it by calling the
  real helper against this checkout.**
- A git failure never fails a migration or a proof: `except Exception` → `code: UNKNOWN
  — <type>: <reason>`. Explicit, and not a plausible value (L1). Refusing an UNKNOWN or
  DIRTY tip is the deploy gate's job — see `## ESCALATE` 4.

### Verify
| what | result |
|---|---|
| `--proof-only`, clean | last line exactly `code: abc1234 (clean) · <CODE_ROOT>` |
| dirty tree | `code: abc1234 (DIRTY: 2 path(s)) · <CODE_ROOT>` — counts porcelain lines, ` M` and `??` alike |
| raising helper | `code: UNKNOWN — RuntimeError: not a git repository`, and `cmd_migrate` **returns** rather than raising (so `--proof-only` still exits 0) |
| forward path | the code line follows the proof table and is the last line |
| `CODE_ROOT` | equals `Path(cli.__file__).resolve().parents[3]`, and that directory really holds `src/cobalt/db_migrations/cli.py` |
| the REAL helper | `cli._code_line()` called for real against this checkout: starts `code: `, names `CODE_ROOT`, reports `(clean)` or `(DIRTY: …)` — proves the import carries no cycle and no side effect |
| file suite | `40 passed, 17 skipped in 1.01s` |
| full offline suite | `1608 passed, 298 skipped, 1 xfailed in 41.53s` — **0 failed** |

### Diff stat
```
 docs/40 - DevDocs/cobalt/db_migrations/cli.md |  32 +++++++
 src/cobalt/db_migrations/cli.py               |  51 +++++++++++
 tests/cobalt/test_migrate_proof.py            | 126 +++++++++++++++++++++++++-
 3 files changed, 206 insertions(+), 3 deletions(-)
```

## Close (offline) — 09:55–09:56 ET

### Suite against BASELINE
```
$ uv run pytest -q tests/cobalt tests/taxonomy
1608 passed, 298 skipped, 1 xfailed, 15 warnings in 40.99s
```
| | BASELINE | now | delta |
|---|---|---|---|
| passed | 1562 | **1608** | **+46** |
| failed | 0 | **0** | 0 |
| skipped | 297 | **298** | **+1** |
| xfailed | 1 | 1 | 0 |

**The +46 passed, by name and step:**

| step | tests | n |
|---|---|---|
| 1 (a) | `test_c2_non_trading_day_with_zero_rows_is_pass[SUNDAY]`, `[SATURDAY]`, `test_c2_trading_day_zero_rows_still_fails` | 3 |
| 2 (b1) | `TestABandThatIsValidChangesNothing::test_the_facts_for_an_accepted_band_are_todays_facts` ×5, `::test_the_validator_accepts_it` ×5; `TestTheBandValidatorRefusesWhatCannotBeABand::test_it_is_refused_naming_both_keys_and_both_values` ×5, `::test_the_inverted_band_says_why`, `::test_a_non_integer_names_the_offending_key`; `TestBothReadersOfTheTwoRowsValidate::test_band_refuses_it` ×5, `::test_band_still_returns_an_accepted_band_unchanged` ×5, `::test_cobalt_validate_calls_the_same_validator` | 28 |
| 3 (c) | `test_the_default_lock_timeout_is_thirty_seconds`, `test_a_forward_run_sets_lock_timeout_before_the_first_migration_file`, `test_a_rollback_run_sets_lock_timeout_too`, `test_proof_only_sends_no_lock_timeout`, `test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied`, `test_a_zero_or_negative_lock_timeout_is_refused_before_any_connection` ×3, `test_the_cli_turns_a_refused_lock_timeout_into_failed_and_exit_1` | 9 |
| 4 (d) | none — A1/A2 edit `requires_db` tests in place | 0 |
| 5 (e) | `test_proof_only_ends_with_the_code_it_ran_from`, `test_a_dirty_tree_says_how_many_paths`, `test_a_git_failure_is_unknown_and_never_fails_the_proof`, `test_the_forward_path_prints_the_same_line_after_its_proof_table`, `test_the_code_root_is_the_package_not_the_current_directory`, `test_the_real_helper_imports_and_produces_a_line` | 6 |
| | | **46** |

**The +1 skipped** (step 3): `test_a_migration_that_cannot_get_its_lock_fails_in_about_a_second`
— `requires_db`, skips cleanly offline, **has never run**.

**Named suite lines:**
- L45 leak test: `uv run pytest -q tests/cobalt/test_radar_notes.py::test_screen_filter_values_live_only_in_approved_radar_fixtures` → `1 passed in 0.09s`.
- **DevDocs symbol check: NOT FOUND in this suite.** → `## ESCALATE` 1.

### RESTARTS (L42 — derived by the command, never by judgement)
`uv run cobalt jobs restarts main..HEAD`, VERBATIM:
```
path	change	rule	restart
docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md	D	DOCS	-
docs/40 - DevDocs/cobalt/daymode/cli.md	M	DOCS	-
docs/40 - DevDocs/cobalt/daymode/propose.md	M	DOCS	-
docs/40 - DevDocs/cobalt/dayopen/checks.md	M	DOCS	-
docs/40 - DevDocs/cobalt/db_migrations/cli.md	M	DOCS	-
docs/40 - DevDocs/prompts/2026-09-19/17-archiver-append-build.md	D	DOCS	-
docs/40 - DevDocs/reports/cto-2026-09-19.md	M	DOCS	-
docs/40 - DevDocs/reports/ops-2026-09-19.md	A	DOCS	-
src/cobalt/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/daymode/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/daymode/propose.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/dayopen/checks.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/cli.py	M	static import reach	com.cobalt.radar
tests/cobalt/test_daymode.py	M	test/documentation; no resident	-
tests/cobalt/test_dayopen_checks.py	M	test/documentation; no resident	-
tests/cobalt/test_migrate_proof.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset com.cobalt.radar
```
**0 UNCLASSIFIED.** Three rows above are NOT this branch's work — see `## ESCALATE` 5.
Against the branch point, `uv run cobalt jobs restarts 4c14712..HEAD` gives the same
`RESTARTS: com.cobalt.aset com.cobalt.radar` from 13 rows, 0 UNCLASSIFIED.

### Commits (one per built item, + this report)
```
fc1d967 feat(db-migrate): the proof names the code it ran from — code: <sha> (clean|DIRTY) as the last output line (09-19 deploy check, known limit)
739b14d test(db-migrate): the lock-wait test cannot leave a waiting backend behind; the blocked-lock probe looks at the worker's own ACCESS EXCLUSIVE request (Astra A1, A2)
f960715 feat(db-migrate): lock_timeout on the migrate transaction — a DDL that cannot get its lock fails in 30 s instead of hanging the deploy (proof-only-3 ESCALATE 3)
86da5a8 fix(daymode): the trade-count band refuses an inverted or non-integer band — one validator for propose and validate (09-18 review F2)
8232dcc fix(dayopen): C2 passes on a non-trading day — no session, no rows, no false AMBER (09-19 Saturday day-open)
```
Every `git add` named explicit paths; `git show --stat HEAD` after each commit listed
only those paths. No `scratch/`, no `-A`, no `.`, no directory.

### Paths
`git diff --stat 4c14712 HEAD` (the branch point) — **12 paths, every one named by the
prompt**:
```
 docs/40 - DevDocs/cobalt/daymode/cli.md       |  10 +
 docs/40 - DevDocs/cobalt/daymode/propose.md   |  30 ++
 docs/40 - DevDocs/cobalt/dayopen/checks.md    |  10 +
 docs/40 - DevDocs/cobalt/db_migrations/cli.md |  88 ++++-
 src/cobalt/cli.py                             |  18 +-
 src/cobalt/daymode/cli.py                     |  19 +-
 src/cobalt/daymode/propose.py                 |  65 ++++
 src/cobalt/dayopen/checks.py                  |  14 +-
 src/cobalt/db_migrations/cli.py               | 140 ++++++++
 tests/cobalt/test_daymode.py                  | 152 ++++++++
 tests/cobalt/test_dayopen_checks.py           |  45 +++
 tests/cobalt/test_migrate_proof.py            | 477 +++++++++++++++++++++++++-
 12 files changed, 1054 insertions(+), 14 deletions(-)
```
(+ this report, in the final commit.) `git diff --stat main HEAD` shows three ADDITIONAL
paths — two DELETIONS and one MODIFICATION [corrected in round 2, R8: this read "all as
DELETIONS"; the RESTARTS table classifies the third as `M`]. The two deletions:
`docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` (−144) and
`docs/40 - DevDocs/prompts/2026-09-19/17-archiver-append-build.md` (−45) — files `main`
GAINED after this worktree was branched at `4c14712`, so a two-dot diff renders them as
deletions. The modification: `docs/40 - DevDocs/reports/cto-2026-09-19.md` (`M`, −60) —
that file exists on both sides and `main`'s copy has simply grown since the branch point.
**None of the three was touched by this run.** → `## ESCALATE` 5.

## ESCALATE

**1. The DevDocs symbol check named in the prompt's step 6 does not exist in this suite.**
Searched `tests/cobalt` and `tests/taxonomy` for a test that walks `src/**/*.py` against
`docs/40 - DevDocs/cobalt/`: `grep -rn "def test.*symbol"`, `grep -rln "DevDoc\|devdoc"`,
`grep -rln "docs/40 - DevDocs/cobalt"`, `grep -rln "rglob\|glob("`. The only source-tree
walkers are `test_finviz_consumers.py` (Finviz consumers) and the placement check
(`test_placement.py`, which validates the D6 TREE, not symbols). **The code wins over the
prompt (index card rule), so this is reported, not invented.** The DevDocs for all four
touched modules were written by hand and name every new public symbol
(`validate_band`, `BandError`, `DEFAULT_LOCK_TIMEOUT_S`, `--lock-timeout-s`, `CODE_ROOT`,
the `code:` line). If a symbol gate is wanted, it is a build item, not a missing run.

**2. `lock_timeout` does not cover the BEFORE probe, and it does not reach test (g) at all.**
Two consequences of one decision, both from item c's conservative `SET LOCAL` placement
(after `_probe_all`'s BEFORE pass), which was the step's own named safe default because
the Postgres manual's wording on whether a `SET` takes the REPEATABLE READ snapshot
**could not be cited from this offline run** — no fetch tool is in the allowlist.
- (a) The BEFORE probe takes ACCESS SHARE with no ceiling. Only an ACCESS EXCLUSIVE
  holder — another DDL, never a resident's INSERT/UPDATE — can block it. Moving the
  `SET LOCAL` into `_connect` would cover it, and is the change to make **once the
  snapshot question is answered against the manual**. That answer is owed; a house with
  network access can settle it in one read.
- (b) Item d's required comment could not say what the prompt assumed. Test (g) calls
  `cli._connect` and runs the ALTER itself, never through `cmd_migrate`, so the 30 s
  `lock_timeout` is not in force there. The docstring states what actually bounds it:
  the 20 s join fires first (so the reader still sees the test's own message) and the
  new 25 s server-side `statement_timeout` is the backstop.

**3. For the DEPLOY PROMPT, always:** a lock timeout at deploy = `FAILED` at the migrate
step with **nothing applied** → the deploy prompt's rollback section; the desk names it
there.

**4. For the desk, always:** proof and deploy prompts — the stop line quotes the `code:`
line, and the deploy gate compares it with the branch tip it was approved for. The gate
also refuses an `UNKNOWN` or a `DIRTY` tip; `cobalt db migrate` deliberately does not.

**5. `main` moved under this branch while it was being built.** Branch point `4c14712`;
`main` is now `3723d86` (`docs(desk): 09-19 R10 …`), three desk commits ahead, all
documentation. `main..HEAD` therefore renders three of main's newer files as deletions
in both the RESTARTS table and the diff stat. Nothing in this branch touched them, and
the derived `RESTARTS:` line is identical either way. Named so no reviewer reads it as
this run deleting a design file.

**6. b2 — a HALF-SET band is DESIGN, the desk's call** *(carried, untouched)*. Today
exactly one null is still "not ruled yet": it fires the placeholder signal and
`com.cobalt.daymode-propose` steps down. Making it fail loud would change that job's
09:00 outcome from *step down* to *job FAILED* — a day-mode outcome, therefore trading
logic (L7). Scoped out deliberately; `validate_band`'s docstring says so and a test pins
it.

**7. Item f — disarming `com.cobalt.archiver` is NOT a build** *(carried, untouched)*.
It is two new `launchctl` rules in the next WEEKDAY deploy's approval list:
`launchctl bootout gui/501/com.cobalt.archiver` before the merge and
`launchctl bootstrap gui/501 <installed archiver plist path>` after, the path read with
`launchctl print gui/501/com.cobalt.archiver` at that time (`facts.md` §f). The plist
has no weekend schedule, so the two weekend deploys are already covered by the clock.

**8. The `requires_db` tests of steps 3 and 4 have NEVER RUN** *(carried, untouched)*.
Their first run on `cobalt_dev` is owed before this branch may deploy:
`test_a_migration_that_cannot_get_its_lock_fails_in_about_a_second` (new, step 3),
`test_the_alter_waits_for_an_open_transaction_and_then_completes` and
`test_ddl_runs_after_another_session_committed_to_the_same_table` (step 4's A1/A2 edits
reach the first; the second shares `JOBS_TABLE` but not the helper).

**9. Cross-branch** *(carried, untouched)*. `sprint-2/p4` also changes
`src/cobalt/db_migrations/cli.py` (the `DIGEST_EXCLUDED_COLUMNS` tuple, +5) and
`src/cobalt/dayopen/models.py`. **Neither was touched here** — `models.py` not at all,
and the new `DEFAULT_LOCK_TIMEOUT_S` was deliberately placed past
`TABLE_DIGEST_EXCLUDED_COLUMNS` so it shares no diff context with that tuple. The branch
that lands second rebases, and the integrated suite on the combined tree is the gate.

No `MEMORY:` or `RULING:` lines: this run produced no new law and no memory-worthy fact
that is not already in the desk's own record.

OPS 0919 fc1d967 | on main 3723d86 | offline 1608/0 (298 skipped; baseline 1562/297) | C2 guard built · band built · lock_timeout built · A1/A2 built · code line built | RESTARTS: com.cobalt.aset com.cobalt.radar | db: OWED — 1 requires_db test written, never run (+1 edited, never re-run) | OWED: three-house check, dev-DB run after p4-verify's stop line, next deploy prompt | ESCALATE: 9

---

# ROUND 2 — folding the three-house check of the round-1 build

Seat `ops-0919-r2` (bg `efa588a7`, Opus 5), same worktree, same branch, OFFLINE.
Prompt: `docs/40 - DevDocs/prompts/2026-09-19/19-ops-round2.md`. Spec for what is REAL:
`19-packet/verdicts.md` `## Verdict table`, rows R2/R3/R5/R6/R7/R8/R9.

## §0 Headline
- ALL SEVEN FOLDED, each item its own commit: R2 (test (g) cleanup into the `finally`) ·
  R3 (the join-vs-probe ordering stated as what is actually guaranteed — **took fix (a)**)
  · R5 (the lock-timeout message points at the HOLDER, red/green offline) · R6–R9 (four
  report-text corrections, marked inline). Round-2 build tip `0ec31f8`.
- Offline suite re-run (R10, settled): `1608 passed, 298 skipped, 1 xfailed, 0 failed` —
  **identical to round 1's close on all three numbers**, no drift.
- R10's second half: `cobalt jobs restarts fc1d967..HEAD` = `com.cobalt.radar`,
  0 UNCLASSIFIED. Narrower than round 1's line because this round touched only
  `db_migrations/cli.py`; the branch-level line is unchanged (see `## ROUND 2 CLOSE`).
- CARRIED, not folded here: R1 + R11 (`SET LOCAL` placement — the owed `cobalt_dev`
  experiment), R4 (NOT REAL, no change).
- ESCALATE: 7. One new finding of substance: R2's and R3's fix cannot be proven offline —
  **THE REAL RUN IS OWED**, and the never-run `requires_db` list is now THREE tests.

## ROUND 2 PREFLIGHT
Run 10:42:08–10:44 ET, 2026-09-19, before any work. **0 denials in the entire run** —
every command matched its bare allowlisted prefix, one command per call.

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | allowed — `Sat Sep 19 10:42:08 EDT 2026` |
| `Bash(git status*)` | `git status --porcelain` | 0 | allowed — **EMPTY**, worktree clean |
| `Bash(git status*)` | `git status` | 0 | allowed — first two lines: `On branch ops/2026-09-19` / `nothing to commit, working tree clean`. Branch correct; no rebase in progress |
| `Bash(git log*)` | `git log --oneline -1` | 0 | allowed — `319eaaa docs(report): ops 0919 — five small fixes built offline…`. **First launch**, matches the prompt's expected tip |
| `Bash(git -C /Users/cobalt/cobalt log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | allowed — `636fb76 docs(desk): 09-19 archiver built (c974fbf), hub ended; ops round 2 launched (efa588a7)`. Recorded, no equality required; `main` has moved past round 1's `3723d86` again (round 1's `## ESCALATE` 5) |
| `Bash(ls *)` | `ls -la .env` | 1 | allowed — `ls: .env: No such file or directory`. **This run never sees the dev database** |
| `Bash(uv run pytest *)` | `uv run pytest --co -q tests/cobalt/test_migrate_proof.py` | 0 | allowed — `57 tests collected in 0.02s`, clean, no code change landed yet |

Rules with no harmless variant inside their own pattern were probed by first real use, all
allowed: `Bash(git add *)`, `Bash(git commit *)`, `Bash(git show*)`, `Bash(git diff *)`,
`Bash(grep *)`, `Bash(tail *)`, `Bash(wc *)`, `Bash(uv run cobalt jobs restarts *)`.
`Bash(cd *)` and `Bash(mkdir -p *)` were never needed and never typed.

## ROUND 2 AUTHORIZATION
Verified by this hub at 10:42–10:44 ET, 2026-09-19, **before any work**. The launch line
claims to add NO rule beyond round 1's. Checked by `grep -c -F` of each rule string
against round 1's committed prompt,
`docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md`. **Every row counts 1.**

| # | string grepped | count |
|---|---|---|
| 1 | `Bash(uv run pytest *)` | 1 |
| 2 | `Bash(uv run cobalt jobs restarts *)` | 1 |
| 3 | `Bash(git add *)` | 1 |
| 4 | `Bash(git commit *)` | 1 |
| 5 | `Bash(git diff *)` | 1 |
| 6 | `Bash(git status*)` | 1 |
| 7 | `Bash(git log*)` | 1 |
| 8 | `Bash(git show*)` | 1 |
| 9 | `Bash(git -C /Users/cobalt/cobalt log*)` | 1 |
| 10 | `Bash(cd *)` | 1 |
| 11 | `Bash(mkdir -p *)` | 1 |
| 12 | `Bash(ls *)` | 1 |
| 13 | `Bash(grep *)` | 1 |
| 14 | `Bash(tail *)` | 1 |
| 15 | `Bash(wc *)` | 1 |
| 16 | `Bash(date*)` | 1 |
| 17 | `--disallowedTools "AskUserQuestion" "EnterWorktree"` | 1 |
| 18 | `--add-dir /Users/cobalt/Vault` | 1 |
| 19 | `--add-dir /Users/cobalt/cobalt ` (trailing space — the bare path, not the `-wt` one) | 1 |
| 20 | `--add-dir /Users/cobalt/cobalt-wt` | 1 |

**16 of 16 Bash rules, the deny pair, and all three `--add-dir` values are byte-for-byte
round 1's. Zero added, zero altered.** No `FAILED: authorization mismatch`.

WORK ITEM, each row checked to say what the prompt quotes:

| what authorizes it | where | verified |
|---|---|---|
| findings are folded before the deploy when the houses say FIX FIRST | `reports/cto-2026-09-19.md:15`, inside `## §4 Rulings 2026-09-19` (section spans :11–:25) | YES — R2, 07:09 ET, Dejan: *"your call was right."* confirming the desk's decided-with-veto call to fold before the deploy although the hub-verified blocker count was 0 |
| this round's exact scope | `reports/cto-2026-09-19.md:347` (§28) | YES — names R2, R3, R5, R6–R9 as ROUND 2, and R1/R11/R10 as carried, R4 NOT REAL |
| L67, the three-house tribunal check being folded | `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-0919/REVIEW.md`, last line | YES, verbatim: `OPS CHECK · grok: 0/2/1 FIX FIRST F2 · gemini: 1/1/1 FIX FIRST 1, 2, 3 · astra: 5/1/0 FIX FIRST 1, 2, 3, 4, 5, 6 · houses that read it: 3 of 3 · hub-verified REAL: 8 (blocks shipping: 0) · NOT REAL: 1 · UNVERIFIABLE: 2` |
| round 1's report, the file appended to | this file, committed at `319eaaa` on `ops/2026-09-19` | YES |

## Item R2 — test (g)'s cleanup runs on every path, not only the hung one
**Status: FIXED.** Commit `543810f`. `tests/cobalt/test_migrate_proof.py` only.

**THE DEFECT, read in the file before touching it.** The `cancel()` + second `join` sat in
the `try` body under `if hung:`. Anything raised between `thread.start()` and that check —
from `_wait_for_a_blocked_lock`, from `other.commit()` — jumped straight to the `finally`,
which closed `conn` only `if not thread.is_alive()`. A worker that was still blocked on
its ACCESS EXCLUSIVE request was therefore never cancelled and never closed, and could sit
in front of every other session on `cobalt_dev` until the interpreter exited.

**THE FIX.** The `finally` now does it unconditionally, in this order: release the other
session (`if not committed: other.commit()`), close `other` and `watcher`, then
`if thread.is_alive(): conn.cancel(); thread.join(timeout=WORKER_STATEMENT_TIMEOUT_S)`,
then the existing `if not thread.is_alive(): conn.rollback(); conn.close()`. The
`if hung:` copy in the `try` body is **removed, not kept beside it** (L3, one path); the
`else` branch survives as `if not hung: after = cli._probe(...)`. The commit releasing the
contended lock still runs before the cancel, so a thread still alive at that point is one
that was not going to get its lock anyway.

**Assertions UNCHANGED**, as the prompt requires: `assert not hung`, `assert blocked`,
`assert outcome[0][0] == "completed"`, `assert cli._verdict(...) == "OK"`. This is
cleanup-path hygiene, not a change to what the test proves. The docstring gained a
`CLEANUP RUNS ON EVERY PATH (R2…)` paragraph replacing the old "On the hung path…"
sentence, which is now false.

**THIS IS A `requires_db` TEST — THE REAL RUN IS OWED.** There is no offline red/green for
this fix: `@requires_db` skips it without a database, exactly as round 1's item d hit the
same wall. It joins the dev-DB list carried in round 1's `## ESCALATE` 8, which is now
**three** tests, not two. What was proved offline today:

| check | result |
|---|---|
| `uv run pytest --co -q tests/cobalt/test_migrate_proof.py` | `57 tests collected in 0.05s` — collects cleanly, count unchanged |
| `uv run pytest -q tests/cobalt/test_migrate_proof.py` | `40 passed, 17 skipped in 1.00s` — **0 errors, 0 failures** |

## Item R3 — the "20 s join fires first" ordering
**Status: FIXED — TOOK FIX (a), the docstring.** Commit `8ed2beb`. Test file only.

**R3: took fix (a), not (b), because** this is the same `requires_db` test as R2 and cannot
be executed in this offline session. Fix (b) moves the join deadline to `thread.start()`,
which changes the test's real wait budget — a slow probe would then *shorten* the join
instead of extending the total run — and it forces a matching edit to the failure message,
which today reads *"did not finish within 20 s **of the other session committing**"*. An
unverifiable timing change to the one test whose whole job is to guard against a hang is
the worse trade. Fix (a) also lands the correction where the defect actually is: the claim
lived in the docstring, and `verdicts.md` R3 calls it *"the docstring claims … unconditionally"*.

**BEFORE** (`test_migrate_proof.py`, numbered item 1 of the ceilings block):

> 1. `LOCK_CEILING_S` (20 s) — the MAIN thread's `join` gives up and the test has its
>    verdict. **This is what fires first, so the message a reader sees is this test's own
>    "did not finish" assertion**, never a driver error.

**AFTER** — item 1 keeps only the part that is unconditionally true ("the MAIN thread's
`join` gives up and the test has its verdict"), and a new paragraph states the guarantee
with its condition:

> WHAT (1)-BEFORE-(2) ACTUALLY GUARANTEES (R3, tribunal round 1; this used to be stated
> unconditionally and was not true in general). The two clocks do not start together: (2)
> starts at the server when the ALTER begins executing, right after `thread.start()`, while
> (1) is only armed once `_wait_for_a_blocked_lock` has returned AND `other.commit()` has
> run. So (1) fires first exactly while those two steps take less than the 5 s of headroom
> between them — which is the case this test drives: the probe polls the catalog every
> 50 ms and returns as soon as the worker's own ungranted `AccessExclusiveLock` request is
> visible, i.e. within milliseconds of the ALTER blocking, and the commit is a local round
> trip.
>
> It is NOT a general guarantee, and no bound beyond the 5 s of headroom is claimed here.
> If the probe itself ran long — its own ceiling is `LOCK_CEILING_S`, and it returns False
> rather than early when the worker's request never appears, e.g. because a THIRD session
> already holds ACCESS EXCLUSIVE — the server's 25 s can fire first. Then the worker's
> ALTER raises, `hung` is False, and the `_probe` on the next line meets an aborted
> transaction: the message a reader sees is a driver error, not this test's own "did not
> finish" assertion.

**No invented bound.** The only number claimed is the 5 s that is arithmetic on the two
constants already in the file (`WORKER_STATEMENT_TIMEOUT_S` 25 s − `LOCK_CEILING_S` 20 s),
and the 50 ms polling interval, read from `_wait_for_a_blocked_lock` at `:1092`
(`time.sleep(0.05)`). The failure mode named in the last sentence is the one
`verdicts.md` R3 describes. Same `requires_db` test — **no offline red/green here either**.
Offline: `40 passed, 17 skipped, 0 failed`, unchanged.

## Item R5 — the lock-timeout message points at the HOLDER
**Status: FIXED, red/green offline.** Commit `be79b74`.
`src/cobalt/db_migrations/cli.py` + `tests/cobalt/test_migrate_proof.py`.

**THE DEFECT.** `cli.py:699` told the operator *"Find the session holding it"* and then
handed them `SELECT pid, mode, granted, relation::regclass FROM pg_locks WHERE NOT
granted;` — a filter that selects **waiting requests**, the opposite of a holder. Worse,
`conn.rollback()` runs one line above the `raise`, so by the time anyone types that query
this migration's own waiting row is gone too: it can return **nothing at all** while a
holder sits on the table.

**TESTS FIRST.** Three assertions added to
`test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied` — `"NOT granted" not
in message`, `"pg_stat_activity" in message`, `"l.granted" in message`.

RED, before the fix (`uv run pytest -q tests/cobalt/test_migrate_proof.py -k
test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied`):

```
1 failed, 56 deselected in 0.10s
E   AssertionError: the hint still filters on ungranted locks, which lists waiters
E   (and after this rollback, not even this one): … Find the session holding it: SELECT
E   pid, mode, granted, relation::regclass FROM pg_locks WHERE NOT granted; then stop …
E   assert 'NOT granted' not in 'the migrati...lock timeout'
```

GREEN, after the fix, same command: `1 passed, 56 deselected in 0.02s`.

**THE FIX**, this one `except` block's message text only:

> BEFORE: `NOTHING WAS APPLIED — the transaction was rolled back. Find the session holding
> it: SELECT pid, mode, granted, relation::regclass FROM pg_locks WHERE NOT granted; then
> stop what is holding the lock …`
>
> AFTER: `NOTHING WAS APPLIED — the transaction was rolled back, so this migration's own
> waiting request is already gone from pg_locks; the HOLDER is still there. Find it: SELECT
> l.pid, l.mode, l.relation::regclass, a.state, a.query FROM pg_locks l JOIN
> pg_stat_activity a USING (pid) WHERE l.granted AND l.relation IS NOT NULL; the list is
> not filtered by mode because an ALTER conflicts with every other lock mode, so any
> granted lock on the table it touches is enough to block it. Then stop what is holding the
> lock …`

Three choices worth naming, because each could have gone the other way:
- **It stays on `pg_locks`**, so `assert "pg_locks" in message` is kept, unedited — the
  prompt's condition for editing it (a fix that drops `pg_locks` entirely) did not arise.
  `pg_stat_activity` is joined on, not substituted for it.
- **No mode filter.** Filtering to granted `AccessExclusiveLock` would have looked tidier
  and been wrong: an ALTER's ACCESS EXCLUSIVE conflicts with *every* mode, so the blocker
  is normally a resident holding ROW EXCLUSIVE, and a mode-filtered query would usually
  return nothing. The message says why it is unfiltered rather than leaving it odd.
- **No `pg_blocking_pids`.** It answers "who is blocking *me*", and after the rollback this
  backend is no longer waiting on anything — the same staleness defect in a new shape. No
  table-scoped query was invented either: the code does not know which table lost the race
  at this point (`verdicts.md` R5's own caution).

Rest of the message untouched: NOTHING WAS APPLIED, the `{lock_timeout_s}` value, the
"raising `--lock-timeout-s` only makes a stuck deploy wait longer" guidance, and
`Postgres said: {e}`. An `R5 …` comment above `conn.rollback()` records why the filter
flipped. Grep confirms the old hint was quoted **nowhere else** in `src/`, `tests/` or
`docs/40 - DevDocs/` (`grep -rn 'NOT granted'` → one hit, the line fixed;
`grep -rn 'Find the session'` → one hit, the same line).

## Items R6–R9 — four report-text corrections, this file, no code
**Status: ALL FOUR FIXED.** Commit `0ec31f8`,
`docs/40 - DevDocs/reports/ops-2026-09-19.md` only. Each correction is marked inline in
round 1's text so it reads as a correction, not a silent rewrite.

**R6** — `:157`, item b1's red-first breakdown.
> BEFORE: `- 13 failed on `ImportError: cannot import name 'validate_band' / 'BandError'…`
> AFTER: `- 12 failed on `ImportError: …` — 7 of the refusal class + the 5
> `test_the_validator_accepts_it` cases [corrected in round 2, R6: this read "13", which
> made the breakdown sum to 19 against the 18 above]`

7 + 5 = 12; 12 + 5 + 1 = 18, which is the `18 failed, 10 passed` at `:154`. The total was
always right; the first line of the breakdown was one over.

**R7** — two places, `:8` and the round-1 stop line at `:569`.
> BEFORE (`:8`): `the dev-DB run of 1 new + 2 edited `requires_db` tests, none ever run`
> AFTER: `1 new + 1 edited` + the reason inline
>
> BEFORE (stop line): `db: OWED — 1 requires_db test written, never run (+2 edited, never re-run)`
> AFTER: `(+1 edited, never re-run)`

Only `test_the_alter_waits_for_an_open_transaction_and_then_completes` was edited.
`ESCALATE` 8's own sentence gives it away: the second test *"shares `JOBS_TABLE` but not
the helper"* — sharing a constant is not an edit.

**R8** — `:488`, the `git diff --stat main HEAD` note.
> BEFORE: `shows three ADDITIONAL paths, all as DELETIONS — … `cto-2026-09-19.md` (−60).
> **None of them was touched by this run**: they are files `main` GAINED …`
> AFTER: `shows three ADDITIONAL paths — two DELETIONS and one MODIFICATION [corrected in
> round 2, R8 …]`, then the two real deletions named with their reason, then the third
> named separately: `docs/40 - DevDocs/reports/cto-2026-09-19.md` (`M`, −60) — that file
> exists on both sides and `main`'s copy has simply grown since the branch point.

The "none of the three was touched by this run" claim is preserved — it was true and is
still true; only the word "deletions" was wrong for one of them.

**R9** — `:130`, item a's dangling pointer. **Took option (ii): the pointer is dropped.**
> BEFORE: `C3 exempts that case (`past_open`), C2 does not — see `## ESCALATE` 1.`
> AFTER: `C3 exempts that case (`past_open`), C2 does not, so on a trading day at 03:00 ET
> C2 still FAILs on zero rows. Unchanged behaviour; not built here because item a's bound
> is the non-trading day. [corrected in round 2, R9 … the case is carried with a number of
> its own in `## ROUND 2 ESCALATE` 1.]`

**Why (ii) and not (i).** Option (i) — a new entry in round 1's own `## ESCALATE` list —
would have made it a tenth entry, and round 1's headline `- ESCALATE: 9.` and its
**committed stop line** (`… | ESCALATE: 9`) would both have become untrue. Renumbering a
stop line the desk has already verified (L35) is worse than moving the item. So the case
is stated plainly where a reader of item a meets it, with no pointer to an unrelated entry,
**and it is carried with a number in this round's own ESCALATE section** — which is the
section the desk reads for this round, so nothing is lost by dropping the pointer.

## ROUND 2 CLOSE
Run 10:47–10:49 ET, 2026-09-19.

**R10 — SETTLED.** It was `UNVERIFIABLE FROM READS` only because the review session's
sandbox lacked two rules. This launch line is a byte-copy of the build's and carries both,
so both commands were run:

```
$ uv run pytest -q tests/cobalt tests/taxonomy
1608 passed, 298 skipped, 1 xfailed, 15 warnings in 41.47s
```

| number | round 1's close | round 2 | drift |
|---|---|---|---|
| passed | 1608 | **1608** | none — as predicted: R2/R3 touch a `requires_db` test, R5 adds assertions to an EXISTING test, **no new test function was added** |
| failed | 0 | **0** | none |
| skipped | 298 | **298** | none |

R10's suite half is therefore **CONFIRMED, not contradicted**: the build report's
`1608 passed, 298 skipped, 0 failed` reproduces exactly on this tree.

```
$ uv run cobalt jobs restarts fc1d967..HEAD
path	change	rule	restart
docs/40 - DevDocs/reports/ops-2026-09-19.md	A	DOCS	-
src/cobalt/db_migrations/cli.py	M	static import reach	com.cobalt.radar
tests/cobalt/test_migrate_proof.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.radar
```

**0 UNCLASSIFIED.** Three changed paths, all three named by the prompt, nothing else.

**The RESTARTS line differs from round 1's `com.cobalt.aset com.cobalt.radar`, and the
reason is the range, not a reclassification** — named rather than waved away. Round 1's
range also carried `src/cobalt/daymode/propose.py`, the only path whose static import
reach includes `com.cobalt.aset`; this round touched none of the day-mode files. The
BRANCH-level line, which is what a deploy plan carries, is unchanged — re-derived to be
sure (R10 asked for `fc1d967..HEAD`; this second range is additional evidence, not a
substitution):

```
$ uv run cobalt jobs restarts 4c14712..HEAD     # the branch point
…
src/cobalt/daymode/propose.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
…
RESTARTS: com.cobalt.aset com.cobalt.radar
```

13 paths, 0 UNCLASSIFIED, every restart derived by the command (L42 — never by judgement).

**Tree and diff.**

```
$ git status --porcelain      (empty)
$ git diff --stat fc1d967 HEAD -- src tests docs
 docs/40 - DevDocs/reports/ops-2026-09-19.md | 577 ++++++++++++++++++++++++++++
 src/cobalt/db_migrations/cli.py             |  23 +-
 tests/cobalt/test_migrate_proof.py          |  92 +++--
 3 files changed, 666 insertions(+), 26 deletions(-)
```

Every path is one this prompt named. (The report shows as `+577` against `fc1d967` because
round 1 committed it at `319eaaa`, one commit later; the round-2 delta to it is the
`0ec31f8` corrections plus this section.)

**Commits, one per item (L46), each verified with `git show --stat HEAD` to list only the
paths named in its own `git add`:**

| commit | item | paths |
|---|---|---|
| `543810f` | R2 | `tests/cobalt/test_migrate_proof.py` |
| `8ed2beb` | R3 | `tests/cobalt/test_migrate_proof.py` |
| `be79b74` | R5 | `src/cobalt/db_migrations/cli.py`, `tests/cobalt/test_migrate_proof.py` |
| `0ec31f8` | R6–R9 | `docs/40 - DevDocs/reports/ops-2026-09-19.md` |

No `git add -A`, no `.`, no directory, nothing under `scratch/`. Never merged, never
pushed, never `~/cobalt`'s tree, never production, never `cobalt_dev`, no `.env`, no vault
write, no `configs/`, no `ops/`, nothing reaching scoring/ranking or a card (L52).

No `MEMORY:` or `RULING:` lines: this round produced no new law, and no fact not already
in the desk's own record.

## ROUND 2 ESCALATE

**1. A TRADING day before premarket open is NOT in item a's bound** *(new number; the case
R9's dangling pointer failed to reach)*. `dayopen` C3 exempts that case through
`past_open`; C2 does not. So on a trading day at, say, 03:00 ET, C2 still FAILs on zero
rows. This is **unchanged behaviour, not a regression** — item a's bound was the
non-trading day and the guard built for it is correct. It is escalated because it is the
one case the round-1 builder identified as outside its brief and never got in front of the
desk, the pointer having gone to an unrelated entry. Ruling owed: leave it, or widen C2 the
way C3 is already widened.

**2. The `requires_db` tests have NEVER RUN — now THREE, not two** *(carried from round 1's
`## ESCALATE` 8, widened by this round)*. **THE REAL RUN IS OWED** for R2's and R3's fix
exactly as it was for round 1's item d: `@requires_db` skips offline, so neither the
`finally`-block restructure nor the docstring's claim has been exercised against a real
Postgres. The list before this branch may deploy:
`test_a_migration_that_cannot_get_its_lock_fails_in_about_a_second` (new, round 1 step 3) ·
`test_the_alter_waits_for_an_open_transaction_and_then_completes` (round 1's A1/A2 edits,
**plus this round's R2 and R3**) · `test_ddl_runs_after_another_session_committed_to_the_same_table`
(shares `JOBS_TABLE`, not the helper — unedited, re-run for safety). R2's fix is the one
that most needs a real run: its whole subject is what a *raising* path leaves behind, and
that path has never executed.

**3. b2 — a HALF-SET band is DESIGN, the desk's call** *(carried from round 1's `##
ESCALATE` 6, untouched by this round)*.

**4. Item f — disarming `com.cobalt.archiver` is NOT a build** *(carried from round 1's
`## ESCALATE` 7, untouched by this round)*.

**5. Cross-branch, now with a second hunk region** *(carried from round 1's `## ESCALATE`
9, widened)*. `sprint-2/p4` also changes `src/cobalt/db_migrations/cli.py`
(`DIGEST_EXCLUDED_COLUMNS`, +5). Round 1 placed `DEFAULT_LOCK_TIMEOUT_S` deliberately clear
of that tuple; **this round edits a second region of the same file** — the
`LockNotAvailable` message at `:687-720`, far from both — so the conflict surface is
unchanged in kind but no longer a single hunk. The branch that lands second rebases, and
the integrated suite on the combined tree is the gate.

**6. Round 1's `## ESCALATE` 1–5 stand, untouched and unrenumbered.** This round changed
nothing in them and did not renumber the list (see R9 above). Note in particular that
round 1's `## ESCALATE` 2 (`lock_timeout` does not cover the BEFORE probe) **is** the
tribunal's R1, which this prompt carries to the owed `cobalt_dev` experiment together with
R11 — it is not a second, separate item.

**7. Two differences between this prompt and what the files say. Reporting both, per the
index card's "where your reading of the code differs from a row, the CODE wins and the
difference is an ESCALATE line."** Neither changed any work.
- The prompt names round 1's file-suite close as `34 passed, 17 skipped`. That is round 1's
  **item c** figure (`:326`). Round 1's *final* file-suite line is `40 passed, 17 skipped`
  (`:389`), after item e added six more. Round 2 measured 40/17 before and after every
  change, so the count is unchanged against the correct baseline.
- The prompt expects `cobalt jobs restarts fc1d967..HEAD` to read the same as round 1's
  `com.cobalt.aset com.cobalt.radar`. It reads `com.cobalt.radar`. Explained above under
  ROUND 2 CLOSE: a narrower changed-path set in a narrower range, not a reclassification;
  the branch-level line still derives `com.cobalt.aset com.cobalt.radar`.

One more note, no ruling owed: **R5's DevDoc needed no edit.**
`docs/40 - DevDocs/cobalt/db_migrations/cli.md:106-107` already said the message *"gives
the `pg_locks` query that finds the session holding the lock"* — which is what the code now
actually does. The DevDoc described the intended behaviour all along; R5 was a code defect,
not a documentation one. The prompt scoped the fix to `cli.py` only, so no DevDoc change
was made; the desk may want the query's new shape spelled out there at sprint close.

OPS 0919 R2 0ec31f8 | on ops/2026-09-19 (round 1 fc1d967) | offline 1608/0 (298 skipped; round-1 close 1608/0/298) | R2 finally-block fixed | R3 ordering fix (a) docstring | R5 holder query fixed | R6 fixed | R7 fixed | R8 fixed | R9 pointer dropped | RESTARTS: com.cobalt.radar (round-2 delta fc1d967..HEAD; branch 4c14712..HEAD unchanged — com.cobalt.aset com.cobalt.radar) | CARRIED: R1 SET LOCAL placement (owed cobalt_dev experiment), R11 (same experiment), R4 no change | OWED: cobalt_dev run of R1/R2/R3's requires_db tests + round 1's two, next three-house check on this diff, then the deploy prompt | ESCALATE: 7
