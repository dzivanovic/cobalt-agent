# S2-P4 follow-up — 2026-09-18

DO NOT STOP until READY FOR MERGE / FAILED. Hub: Sonnet 5, seat `p4-followup-0918`, unattended per R5 (2026-09-17 06:03 ET).

## §0 Headline
- Authorization verified before any risky command: R6 (09-17 06:11 ET), R17 (09-17 08:38 ET, ceiling 50), R1 (09-18 06:46 ET, adds `Bash(grok *)`) all committed on main, allowlist matches launch line.
- **P2 on main: NO.** Prompt's own headline text ("P2 is LIVE dark since the 09-17 evening") is stale/wrong — `cto-2026-09-18.md` §0 states "P2 NOT live (reverted 09-17)" and R1 2026-09-18 itself says "(3)'s E2 step waits for tonight's P2 merge." Step 1.1's built-in check catches this; §3 (E2) is skipped per its own documented contingency, not treated as FAILED.
- ESCALATE: 8 (list below) — none block this session's own work (chunk F1: done, verified, tests-first, suite green); all are pre-existing on the branch from the earlier 09-17 build or carried-forward/architectural, surfaced by full verification (§4) and grok's second-house review (§5), for Dejan's call before P4's D1.

## PREFLIGHT
| check | result |
|---|---|
| `git status --porcelain` (worktree) | clean |
| HEAD | `ec61e65` (expected, matched) |
| `.env` cp → `cobalt db migrate --help` → rm | OK, help printed, `.env` confirmed removed |
| `claude -p --model claude-opus-5 "OK"` | replied `OK` — no usage-limit message |
| `grok --version` | `grok 1.0.25 (f7e67d6988e2) [stable]` |
| `git -C /Users/cobalt/cobalt status --porcelain` | dirty: `configs/cobalt/rules.yaml`, `docs/40 - DevDocs/reports/seat-usage.md` modified — not this session's, not touched, noted only |

## ESCALATE
1. **Pre-existing** `src/cobalt/jobs/wrapper.py:23` / DevDoc `jobs/wrapper.md:18` claim heartbeat stamps "every 5 min" — real value is `timeout_s/heartbeat_fraction`, wrong before and after R17 (30min → ~13min). Left untouched (out of chunk F1's scope, builder's call, hub agrees). One ticket, both files.
2. **Carried, 09-17 ESCALATE 4/E4.** 4 dev-DB tests red on `cobalt_dev` (`test_aset_store.py::test_mark_filled_refuses_the_shortcut_on_a_radar_card`, `test_cards.py::TestOneClickFill::test_a_radar_card_gets_no_shortcut`, `test_tenancy.py::TestPlacement::test_every_table_is_on_its_ruled_side`, `test_tenancy.py::TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips`) — leftover P2 schema objects on the shared dev DB, unrelated to this branch's own migrations. Confirmed still open as of close: main's latest commit (`5e68daa`) itself reports "P2 re-landed, gate red on shared cobalt_dev" and adds a new prompt (`05-p2-gate.md`) for a clean-DB gate — same root cause, independently hit by the parallel P2 re-ship session tonight. Resolves when that gate closes and this branch rebases again before D1.
3. **Design debt, not a regression** — `replay/cards.py` `MissedStore.candidates()` has no seam to pass a card's real window/trade_def into replay (unlike `cards/expire.py`'s injectable `trade_def_ref_for`), so replay's window resolution always falls back to session close. Harmless today (no card anywhere carries a real trade_def yet), but whoever builds the trade_def-attachment detector (deferred to S2/S4 per `expire.py`'s own docstring) must also wire this seam in replay, or replay silently diverges from the live path's window resolution the day the live seam is finally used.
4. **Confirmed real, deferred to F2/E2.** `replay/runner.py:166` `formation_replay` imports `cobalt.radar.evaluate_cli`; the plan (line 184/189) names the entrypoint `cobalt.radar.evaluate`. Zero impact today (P2 absent either way, correct "unavailable" line prints). Fix as part of F2/E2 once P2's real shipped module name is recorded, per the plan's own R1-21 amendment — do not guess a fix now.
5. **Design question, pre-existing architecture.** The L53 total-demand gate (`check_scheduled_demand`/`total_demand`) scores each subject's DECLARED schedule window, never wall-clock "now." The nightly scheduled path is safe (verified: `archiver_precondition` refuses replay outright unless tonight's archiver row is already `done`). A human manually invoking a HISTORICAL replay run (`--date <past-day>`) during live trading hours would pass the gate while firing real concurrent Finviz traffic against the live resident radar — the historical-date precondition branch never checks "now" either. Predates R17; not introduced by this chunk. Needs Dejan's call: teach the gate wall-clock awareness, or treat this as an operational-discipline rule (no manual historical replay during market hours) and document it.
6. **Confirmed real, pre-existing.** `configs/cobalt/smoke/s2.yaml` K8 (`side: user`) subqueries `system.cobalt_jobs` in 5 places; no migration (0001-0009) grants `cobalt_user` SELECT on it — `cobalt_jobs` is system-side by declaration (`0003_heartbeat_vault_outcome.sql:3`), only `movers_daily` was granted to the user role (`0008:42-44`). If `side: user` maps to a real restricted role, K8 errors on permission denial. From the earlier 09-17 build's Chunk C, not this session's chunk.
7. **Confirmed real, pre-existing.** `replay/movers.py:75` `REQUIRED_HEADERS = ["Ticker", "Change", "Volume"]` is missing `Asset Type`, which the plan (line 191) requires. A non-equity top-N mover would be misclassified as `unreported`/counted toward the miss benchmark instead of dropped. From the earlier 09-17 build, not this session's chunk.
8. **Unverified, passed through from grok's review** (time-boxed per L35's BLOCKER-only verification requirement): MAJOR — K3 rank_metric cutoff query keys on `first_seen_at`, may miss RETAIN'd pre-deploy rows (`configs/cobalt/smoke/s2.yaml:76`); MAJOR — K9 `full_sides` check has no short-export tolerance (`s2.yaml:205`); 5 MINOR items in `scratch/review/grok-findings.md` (EXPIRED reason mapping, archiver historical-precondition edge case, picks join key mismatch, cf-R zero-day display, one stale "21:05" comment in `0008_radar_value_movers.sql:12`). Full detail in `scratch/review/grok-findings.md` (not committed — gitignored `scratch/`, per PROMPT §5 staging convention).

## §1 Rebase onto main
- worktree HEAD before rebase: `ec61e65`; main HEAD: `842919f` ("09-18 R1 — three launch allowlists approved 06:46 ET; day-open GREEN").
- **P2 on main: no.** `src/cobalt/db_migrations/0007_radar_cards.sql` absent on main; `deploy-2026-09-17.md` ends `sprint-2/cards DARK n (merged, suite red, reverted...)`, not DARK y. §3 (E2) skipped per step 1.1's own contingency.
- `git rebase main`: **no conflicts** — none of the expected zones (`configs/cobalt/taxonomy/tunables.yaml`, `tests/cobalt/test_radar_*.py`, `db_migrations/placement.py`/`__init__.py`) actually collided; P2's migrations (0006/0007) are not on main to collide with.
- commit count `main..HEAD`: 16 before, 16 after — unchanged.
- `uv run pytest -q tests/cobalt tests/taxonomy`: **1334 passed, 263 skipped, 2 xfailed, 0 failed** (28.26s).

## §2 Chunk F1 (Opus headless) — R17 ceiling 50 + windows
- Builder launch: 3 attempts. Attempt 1 (`claude -p --model claude-opus-5 "<brief>"`, no permission flags) had no write/env access — DENIED every Edit/Write and `COBALT_ENV=` Bash; it did read-only analysis instead and caught test hits my brief's grep missed (`test_replay_demand.py:113`, `test_radar_config.py:42-49`, `test_radar_propose.py:409`, `test_radar_sources.py:24`, two DevDocs files). Attempt 2 (adding `--allowedTools` as trailing args before the prompt) failed at the CLI arg parser — the multi-word brief text got consumed as more `--allowedTools` patterns instead of the prompt; no edits made (verified `git status --porcelain` clean). Attempt 3: brief piped via stdin, `--permission-mode acceptEdits` + explicit `Bash(COBALT_ENV=dev uv run pytest *)`/`Bash(COBALT_ENV=dev uv run cobalt validate*)` — succeeded.
- Commit `ff5c870`, 19 files, +186/-61.
- Tests-first evidence (builder's report, independently re-verified by the hub): 8 targeted assertions run against the unchanged 45/5400/"21:05" values first — all failed (`assert 45 == 50`, window-string mismatches, wrong-boundary refusals) — then passed after the edit. Hub re-ran the full suite independently: **1338 passed, 263 skipped, 2 xfailed** (baseline was 1334 — exactly +4, the new R17 tests; skip/xfail counts identical).
- `git diff --stat` (pre-commit, hub-verified): 19 files, +186/-61 — matches builder's claim exactly.
- `COBALT_ENV=dev uv run cobalt validate`: exits 0, taxonomy/tunables/calendar/session sections OK (13 trade_defs validated); it does not itself print the ceiling (that's `cobalt radar screens validate`'s "transport budget: 36.60/50 rpm" line, asserted by `test_radar_propose.py:409`, hub-confirmed passing) — `validate`'s first line is a pre-existing `DbConfigError` (no Postgres creds in this dev shell — same root cause as the 263 skips), unrelated to R17.
- Two out-of-brief fixture fixes the builder made and the hub reviewed as correct: `test_smoke.py`'s `_job_row()` healthy-run fixture shifted 21:05→21:10 start (was asserting a now-stale schedule, would have gone MISSED under 21:10); `test_radar_propose.py`'s bad-pool-refusal fixture `cap=66`(46.20 rpm, under new ceiling 50, stopped refusing) → `cap=76` (52.20 rpm, same shape). Both keep the original test's intent intact.
- ESCALATE 1 (informational, not a build defect): pre-existing `src/cobalt/jobs/wrapper.py:23` / its DevDoc claim heartbeat stamps "every 5 min" — with `jobs.heartbeat_fraction=3` the real figure was 5400/3=30min and is now 2400/3≈13min, wrong before and after R17. Builder correctly left it untouched (out of this chunk's scope) and flagged it for a separate ticket.

## §3 Chunk F2 (E2) — SKIPPED
Per §1's "P2 on main: no" finding and step 1.1's own documented contingency. No code written for this chunk.

## §4 Full verification
- Offline suite: 1338 passed, 263 skipped, 2 xfailed (unchanged from §2). Named leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures`: **1 passed**.
- `.env` cp → `COBALT_ENV=dev uv run cobalt db migrate` (FORWARD): 0001-0005, 0008, 0009 applied; 0006/0007 (P2's) absent, as expected since P2 is not on this branch (§1/§3). 18 tables proven, content UNCHANGED, all `OK`.
- `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`: **4 failed, 1594 passed, 3 skipped, 2 xfailed** (dev DB has real Postgres, far fewer skips than offline). The 4 failures are the carried 09-17 ESCALATE 4/E4 item, named exactly:
  - `tests/cobalt/test_aset_store.py::test_mark_filled_refuses_the_shortcut_on_a_radar_card`
  - `tests/cobalt/test_cards.py::TestOneClickFill::test_a_radar_card_gets_no_shortcut`
  - `tests/cobalt/test_tenancy.py::TestPlacement::test_every_table_is_on_its_ruled_side`
  - `tests/cobalt/test_tenancy.py::TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips`
  **These are NOT gone** — the prompt's expectation that they'd clear "now that P2 is under this branch" does not hold, because P2 is NOT under this branch (§1: P2 on main: no; the parallel `03-p2-reship.md` session had not landed on main as of this verification). The shared `cobalt_dev` Postgres instance still carries leftover P2 schema objects (`"user".radar_cards_v` view depending on `aset_sizings.pool_member_id`, a CHECK constraint on `aset_sizings`) from an earlier P2 build/test run — this branch's own migration registry (0001-0005, 0008, 0009) neither created nor knows about them. Same root cause as `deploy-2026-09-17.md`'s carried ESCALATE 3 (`db migrate --rollback` dependency failure) and the P2 close report's ESCALATE 12. Resolves itself once P2 re-ships to main and this branch rebases again before D1 — not fixable from this session without touching P2's objects.
- ROLLBACK ROUND-TRIP on `cobalt_dev` (`.env` still in), scoped `--down-to 0005` (only 0008/0009 are this branch's to reverse):
  - `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005`: 0009, 0008 rolled back cleanly; `missed`/`movers_daily`/`picks` DROPPED, all 18 rows verbatim, no error.
  - `COBALT_ENV=dev uv run cobalt db migrate` (forward again): 0001-0005, 0008, 0009 re-applied; `missed`/`movers_daily`/`picks` CREATED back.
  - **Digests match pre-rollback exactly** on every one of the 18 tables (e.g. `aset_sizings eeaf69a3->eeaf69a3`, `bars 2769919a->2769919a`, the three dropped/recreated tables `d41d8cd9->d41d8cd9` both times — empty-content digest, consistent with 0 rows before and after). No denial, no mismatch.
- `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""`: **96 passed**, read-only against the real vault.
- `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly --dry-run`: `FAILED: ReplayError: archiver not done: state 'pending'`. This is the CORRECT fail-loud behavior (R1-16/STEP-4 precondition: replay refuses without tonight's `com.cobalt.archiver` row `done`, exit 0) — the dev DB has no completed archiver run today, so the guard fires as designed. Not a defect.
- `.env` removed after this block.
- `uv run cobalt jobs restarts main..HEAD`: 133 rows, **0 UNCLASSIFIED**. `ops/com.cobalt.replay.plist` classified explicitly `new one-shot, bootstrap once: com.cobalt.replay` (not omitted). `configs/cobalt/smoke/s2.yaml` classified `operator command (cobalt smoke); no job reads` (not UNCLASSIFIED, resolved per R2-6/L42). Last line: **`RESTARTS: com.cobalt.aset com.cobalt.radar`** — matches expectation (aset + radar; replay is bootstrap-once, not a restart).

### ESCALATE 2 — carried, not new
The 4 dev-DB test failures above (09-17 ESCALATE 4/E4, P2 close report ESCALATE 12) remain open. Not caused by this build; will not clear until P2 re-ships to main and this branch is rebased again before P4's D1.

## §5 Second-house review — Grok
Staged `scratch/review/`: `diff.txt` (`git diff main..HEAD -- src configs ops`, 6442 lines), `plan-s2-p4-2026-09-15.md`, `laws-excerpt.md` (L1/L8/L28/L32/L45/L53/L57, extracted from LAWS.md). Launch per `audit-house-2026-09-16.md` §3 grammar: `grok --sandbox cobalt-job --allow "Write(.../scratch/review/**)" -p "..."`, `run_in_background`. Exit 0, `grok-stderr.txt` empty (0 bytes) — no denials, no HARNESS.

Findings (`scratch/review/grok-findings.md`): **3 BLOCKER, 5 MAJOR, 5 MINOR.** Every BLOCKER hub-verified against the code myself (L35); MAJORs spot-checked where the claim was concrete and quick to confirm; MINORs passed through as grok's read, not independently re-verified (time-boxed).

| # | grok severity | Verified | Hub verdict |
|---|---|---|---|
| BLOCKER 1 — `replay/cards.py` `MissedStore.candidates()` never selects/passes `window_ref`, so `resolve_window` always falls back to session close | BLOCKER | Confirmed accurate about current behavior (`cards.py:453` SQL has no window column; `models.py:102` defaults `window_ref=None`) — **but not a regression.** `cards/expire.py`'s OWN live path has the identical default (`trade_def_ref_for=None` → every window is session close) by explicit design: its docstring says no card anywhere carries a `trade_def` yet, "the seam exists now so that S2 attaches the trade_def and nothing in this module changes." Grepped the whole tree: `trade_def_slug`/`trade_def_ref_for` appear ONLY in `expire.py` — nothing attaches a real trade_def to any card yet, live or replay. Downgraded: real design debt (replay's loader has no equivalent injectable seam at all, unlike the live path), not a merge-blocking defect for this release. | Real code, not a regression — ESCALATE as a ticket, not fixed here |
| BLOCKER 2 — `replay/runner.py:166` `formation_replay` imports `cobalt.radar.evaluate_cli`, not the plan's `cobalt.radar.evaluate` | BLOCKER | **Confirmed real.** Plan line 184/189 names the entrypoint `cobalt.radar.evaluate` explicitly. Shipped code tries `cobalt.radar.evaluate_cli` first; its except-clause treats either name's `ModuleNotFoundError` as "absent," but never actually attempts importing `cobalt.radar.evaluate`. **Zero practical impact today** — P2 is not on main, so both names are absent and the code correctly prints `FORMATION_UNAVAILABLE_LINE` either way (verified by name, not by running). Squarely inside the E2/F2 scope this session already deferred (§3): the plan's own R1-21 amendment requires recording "P2's actual shipped replay contract" before wiring the adapter — not yet possible since P2 hasn't shipped. | Real, deferred, zero impact today — ESCALATE, fix as part of F2/E2 when P2's real module name is known |
| BLOCKER 3 — ceiling/window changes (jobs.yaml, tunables.yaml, notes.py, movers.py) let the L53 gate score DECLARED schedule windows, not real concurrent execution | BLOCKER | Two parts. (a) **Scheduled-path overlap: NOT a real risk.** Verified `archiver_precondition` (`replay/runner.py:94-133`): for tonight's date it requires the archiver's row to be `state=done, exit_code=0` AND started at/after tonight's 20:30 occurrence — a still-running (non-done) archiver makes replay's scheduled 21:10 run REFUSE outright (reproduced empirically in §4's dry-run: `FAILED: ReplayError: archiver not done`), so real concurrent Finviz traffic on the nightly scheduled path cannot happen regardless of the declared-window arithmetic. (b) **Ad-hoc/historical manual invocation: confirmed real, but pre-existing.** `check_scheduled_demand`/`total_demand` (`radar/notes.py`) take no "now" argument — they score declared windows only. `archiver_precondition`'s historical branch (`trade_date < today`) only checks that a past night's run was clean, not "now" — so a human manually running `cobalt replay nightly --dry-run --date <past-day>` during live trading hours would pass both the precondition and the L53 gate while firing real HTTP calls concurrently with the live resident radar. This characteristic predates R17 (radar's window 04:00-20:00 has always been declared-disjoint from archiver/replay's evening windows; the gate was never wall-clock-aware) — R17 changed three numbers inside this existing architecture, it did not introduce the gap. Also noted: the "R17" ruling cited in code comments is not in grok's review packet (only the 09-15 plan was staged) — the hub independently verified R17 from `cto-2026-09-17.md` row R17 and `cto-2026-09-18.md` row R1 at session start (see Authorization, §0); this is a review-packet gap, not a code defect. | (a) not real; (b) real, pre-existing architecture, not introduced by this chunk — ESCALATE as a design question (should the gate consider wall-clock time, or is manual-invocation discipline the intended control) |
| MAJOR — `radar/notes.py:421` `load_sources` catches `TotalDemandExceeded` into `pool_error` instead of raising | MAJOR | **Not confirmed as a defect.** `pool_error is not None` drives `ParsedSources.frozen` (`notes.py:62-63`); `runner.py:177` reads `parsed.frozen` and passes `None` instead of live blocks to `decide(...)` when frozen. This is the designed refusal mechanism (freeze-via-field, not raise), and it is wired through to the scan-path caller. | Not confirmed — mechanism works as designed |
| MAJOR — `tunables.yaml` ceiling 45→50 committed, "plan says pending his ruling" | MAJOR | Not a defect — see BLOCKER 3(b) note above: R17 is real, ruled, committed, independently verified by the hub before this chunk ran (§0/§2). Review-packet gap (grok wasn't given the cto reports), not a code or process defect. | Not confirmed — packet gap |
| MAJOR — `configs/cobalt/smoke/s2.yaml` K8 (`side: user`) subqueries `system.cobalt_jobs` | MAJOR | **Confirmed real.** Grepped every migration (0001-0009): `system.cobalt_jobs` is granted to `cobalt_system` only ("`cobalt_jobs` is system-side by declaration," `0003_heartbeat_vault_outcome.sql:3`); no migration ever grants `cobalt_user` SELECT on it. `0008_radar_value_movers.sql:42-44` grants `cobalt_user` only `movers_daily` + its sequence. If `side: user` maps to a real restricted Postgres role (consistent with ADR-0008/L32 tenancy), K8 will error on a permission failure, not merely fail an assertion. | Confirmed real — ESCALATE (smoke K8, pre-existing from the earlier build's Chunk C, not this session's chunk) |
| MAJOR — `replay/movers.py:75` `REQUIRED_HEADERS` missing `Asset Type` | MAJOR | **Confirmed real.** Plan line 191: "required headers `Ticker, Change, Asset Type, Volume`." Shipped: `REQUIRED_HEADERS = ["Ticker", "Change", "Volume"]` (`movers.py:75`) — `Asset Type` absent. | Confirmed real — ESCALATE (pre-existing, not this session's chunk) |
| MAJOR — K3 rank_metric cutoff query keys on `first_seen_at`, misses RETAIN'd pre-deploy rows; MINORs (5) | MAJOR/MINOR | Not independently re-verified (time-boxed per L35's BLOCKER-only requirement) — passed through as grok's read. | Unverified — pass through as ESCALATE items for Dejan/next session to check |

**Grok verified: 3/3 BLOCKER reviewed by hub (1 downgraded-not-a-regression, 1 confirmed-but-zero-impact-deferred, 1 split real/not-real); 2/7 MAJOR spot-checked confirmed real, 2/7 spot-checked not confirmed, 3/7 + 5 MINOR unverified/passed through.**

None of the CONFIRMED-real findings (BLOCKER 2, MAJOR K8, MAJOR REQUIRED_HEADERS) are in this session's own chunk (F1) or in the skipped chunk (F2/E2) — they are pre-existing on this branch from the earlier 09-17 build. Per the "3-tries rule"/token-conservation and this session's own scope (R17 ceiling+windows only), the hub is not attempting speculative fixes to another chunk's code in this pass. All are recorded as ESCALATE below for Dejan's call before D1.

## §6 Close
- Worktree clean: `git status --porcelain` empty (`scratch/` is gitignored by design, holds the F1 briefs/builder transcripts and the grok review packet — not committed).
- main advanced during this session (`842919f` → `5e68daa`, docs-only: desk reports + new prompt files, `git diff --stat` confirmed zero `src`/`configs`/`ops` changes) — not re-rebased; nothing to reconcile.
- Report committed at each step boundary (`36a362d`, `ee6577e`, `c681dc0`, this close commit).

READY FOR MERGE 1b6be4b (code ff5c870) | rebased onto 842919f | RESTARTS: com.cobalt.aset com.cobalt.radar · ceiling 50 · E2: skipped (P2 not on main) · grok: 5/13 hub-verified (3 BLOCKER + 2 MAJOR spot-checked; 2 confirmed real pre-existing defects, 1 not confirmed, 1 packet-gap not-a-defect, 1 split real/not-real) · audit: deferred · ESCALATE: 8
