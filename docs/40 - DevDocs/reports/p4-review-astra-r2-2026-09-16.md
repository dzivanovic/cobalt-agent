AMEND BEFORE BUILD — 2 BLOCKER, 5 AMEND; only unresolved R1 findings or consequences of their amendments below. R1–R7, including amended R7, stand unchanged.
Static review of the amended P4 plan and hub dispositions; P2 STEP-1/§7 read from `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` (absent here). No DB or production tests run. RESTARTS: none (this report only).
ESCALATE 2: sound interim; 3: sound corrected investigation; 4: computation design sound, enforcement/deploy gate still incomplete (row 5); 5: sound conditional scope handling under amended R7, with F13 rescoping required if archival is cut.

| id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording |
|---|---|---|---|---|---|
| 1 | BLOCKER | STEP-1/STEP-4/STEP-6; R1-8 and R1-20 | The immutable receipt amendment and rerun reconciliation still lack a compatible storage contract. `missed` has one unique row per subject/day and an immutable payload on that row; a rerun with changed bars/settings must either overwrite that payload, reject the new result, or store another version somewhere unspecified. Adding only a replay-run identifier does not resolve the unique constraint. The new instruction to remove no-longer-selected movers also hits `missed.mover_id`'s NO ACTION FK for an already-recorded miss. Deleting its miss row to unblock it would destroy the retained receipt. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:114`, `:127`, `:134`, `:164`, `:202`; `src/cobalt/archiver/store.py:82`; LAWS L57 | Specify executable storage for immutable per-run receipt versions and the current reconciled result, including keys, active-run selection and rollback. Retain referenced historical movers; remove them from the active benchmark rather than physically deleting audit parents. A changed same-day run must retain and independently replay both versions, retire stale active misses, and publish only its own counts. Prove this with real FKs and a crash between the SYSTEM and USER commits; keep all receipt content USER-side with the ADR-0008 ownership/GUC/registry contract. |
| 2 | BLOCKER | STEP-2; remaining R1-19 model-path gap | The amendment fixes `MembershipRecord` and `PoolRow`, but STEP-2 also expands `open_members()` and its other consumer constructs `OpenMember(**row)`. `OpenMember` forbids extras and has neither new field. The first resident scan over a populated pool will fail validation if the specified SELECT expansion is implemented literally. The query→HTML test does not exercise this resident path. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:139`, `:142`; `src/cobalt/radar/models.py:190`; `src/cobalt/radar/runner.py:175`; `src/cobalt/radar/store.py:29` | Add nullable `rank_metric`/`rank_value` to OpenMember as well as MembershipRecord, and carry them through the frozen HOLD path. Test a real-shaped open_members row through the resident scan for both pre-deploy NULLs and populated values, including HOLD preserving the prior pair. |
| 3 | AMEND | STEP-4/STEP-6; unaddressed part of R1-6 | The pick SAVEPOINT now correctly stays on one USER connection, but the replay half of R1-6 was not folded. STEP-4 still says each step has its own transaction, although STEP-6 reads USER settings, writes SYSTEM movers/bars, then writes USER misses. No allowed side role can perform that entire transaction. The new transactional reconciliation wording makes the missing commit boundary consequential. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:151`, `:160`, `:191`, `:193`, `:198`, `:202`; `docs/10 - Decisions/ADR-0008-two-layer-data-model.md:82`, `:88`; `src/cobalt/archiver/store.py:78` | Name replay's USER store for settings/cards/missed/receipts/vault audits and SYSTEM stores for movers/bars/job bookkeeping. Define their ordered, separately committed phases and recovery by retained run identity; never claim cross-side atomicity or switch roles inside the fill transaction. Test actual side-role permissions and retry after each commit boundary. |
| 4 | AMEND | STEP-5 horizon; remaining R1-10 | The public resolver handles an early close only on its fallback path. An explicit `preferred_windows_ref` resolving to 16:00 returns 16:00 immediately even on a 13:00 close day. Merely calling the public resolver as amended can therefore keep the old false-stale or beyond-session horizon defect. The amendment also still leaves the source bar timestamp convention implicit. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:178`, `:184`; `src/cobalt/cards/expire.py:128`, `:136`, `:141`; `src/cobalt/archiver/models.py:40` | Resolve the window with window_end_for, separately obtain that day's calendar session close, and bound the replay horizon by that close, preserving the ruled after-window-trigger branch. State the verified i1 timestamp convention and completed-bar cutoff explicitly. Add an early-close fixture with an explicitly resolved later window, not only an ambiguous-prose fallback fixture, and prove the retained timestamps reproduce the same last eligible bar. |
| 5 | AMEND | STEP-6/§8 ESCALATE 4; remaining R1-13 | The all-consumer, window-aware sum and mover throttling are sound amendments. However no pre-request enforcement call is named for replay or the archiver. Today's refusal is in radar load_sources; the archiver calls parse_note through its own loader and then fetches directly. Extending the sum in notes/propose plus a pure refusal test does not demonstrate that an over-budget archiver run is prevented. The measured archiver demand remains above the unchanged ceiling, so leaving the ruling OPEN cannot also mean the proof is deployment-ready. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:200`, `:322`; `src/cobalt/radar/notes.py:151`, `:179`; `src/cobalt/archiver/config.py:49`; `src/cobalt/archiver/runner.py:38`, `:62`; LAWS L53 | Name the shared demand-check entry point and every pre-request caller, and test the actual entry paths refuse before finviz_get when the shared total is over the ceiling. Keep the ceiling/cadence and R3 archiver schedule/carve-out unchanged. ESCALATE 4 stays a deployment gate until a conforming measured demand proof exists or Dejan resolves the policy; serialization alone does not close it. |
| 6 | AMEND | §5 RESTARTS; amendment to R1-22 | The new sentence “this deploy doesn't need one” follows “radar restart” and contradicts both the derived radar restart listed above it and D1's explicit kickstart. The intended conclusion was that the 21:05 replay does not itself restart radar. Read as a deploy exemption, the amendment violates L42 for the changed imported pool/store code. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:271`, `:275`, `:287`; `src/cobalt/jobs/restarts.py:167`, `:177`; LAWS L42/L43 | The P4 deployment restarts radar and aset as derived from the final range, and bootstraps replay once. Only the 21:05 replay invocation and per-run benchmark setting read imply no additional radar restart. Keep the actual derivation table and explicit new-plist bootstrap action in the build report. |
| 7 | AMEND | STEP-5 formation adapter; remaining R1-21 | “Is specified as a named callable + typed result” still names neither a callable nor a result contract. P2 specifies a writes-nothing replay CLI and immutable score receipts, not the assumed formation-returning API. The identity amendment names member/trigger timestamp, but the displayed unique index still omits both. The retained R1 request was for a concrete seam; describing the requirement again leaves the two builders free to implement incompatible interfaces. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:127`, `:182`, `:187`, `:312`; `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md:178`, `:193`, `:284`; LAWS L3/L57 | Before implementing the adapter, record the actual P2 callable, arguments, capability version and typed returned fields, including formation timestamp/member identity, trigger/stop/window inputs and retained receipt reference. Make the formation uniqueness constraint use that identity and specify card-match time bounds. If P2 is absent, preserve the ruled unavailable line; if present but incompatible, fail loudly. Prove two same-day formations survive persistence and use the existing P2 evaluator without duplicating it. |
