# Close 2026-09-19 — session close (close hub `close-0919`, `prompts/2026-09-20/99-close-0919.md`)

## §0 Headline
- Three production deploys landed on 2026-09-19 (`2893a7f` 08:25, `5b58b7b` 15:17, `851335e` 18:03), tags `deploy-2026-09-19`, `-19b`, `-19c`; cards LIVE since 12:30:01 ET but never rendered. Ledger appendix `### 2026-09-19 — three production deploys` appended (49 lines, R1–R36 each).
- Ladder: first `### Status 2026-09-20` block written under `## S2 — Radar 19a`; sprint stop 2026-09-23 = **AT RISK** (nothing left to build, all acceptance still ahead: first card render Mon 09-21 04:00 ET, first replay Mon 21:10 ET, 0 of 2–3 live mornings). Backlog rows brought in line.
- **One prompt premise did not survive L35:** S2-P3 (`sprint-2/radar-panel`) is MERGED and live since 09-15, not BUILT-NOT-MERGED — recorded as DONE-LIVE.
- Always-loaded block **3,993 / 4,000** (PASS, 7 characters of margin). `validate` exit 0. LAWS: 7 PROPOSED, 0 applied (L58). One docs commit; push is Dejan's (L55).
- ESCALATE: 6

## AUTHORIZATION (verified by this hub before any write)
- `git log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` → `6c970f6`, `6f4ebc0`, `92745c6`, `6271160`, `7948fbf`, `7d9326b` — the day's record is on main.
- Read in the prompt's order: `SESSION-CLOSE.md` · `cto-2026-09-19.md` §0, §4 in full (R1–R36 + PUSH), §45–§65 · last lines of `deploy-2026-09-19.md`, `deploy-p4-2026-09-19.md`, `deploy-d3-2026-09-19.md`, `cards-golive-2026-09-19.md` · `ladder-audit-2026-09-19.md` · `close-2026-09-18.md` · `LAWS.md` L1–L70 (read only) · ladder S2 section · `BACKLOG.md`.
- Every command used is inside the approved set (`git status/log/diff/show/add/commit`, `tag --list`, production `validate`, `cd`, `ls`, `grep`, `tail`, `wc`, `date`); nothing outside it; no denial. No write under `Vault/Think/6 - Permanent/Memory/` (L58).

## Deploy stop lines (each quoted from its report's LAST line)
| # | deploy | last line of its report |
|---|---|---|
| 1 | stack, 08:25 ET | `STACK DEPLOY DONE 2893a7f · LIVE: sprint-2/stack DARK y · ops-0918 y · migrations 0006/0007 applied · over-band row 08:21:11 ET · dark row 08:21:15 ET · RESTARTS done: com.cobalt.aset com.cobalt.radar · outage 02:40 · PUSH: Dejan · ESCALATE: 5` |
| 2 | P4, 15:17 ET (run 3) | `P4 DEPLOY DONE 5b58b7b · LIVE: sprint-2/p4 · migrations 0008/0009 applied · gate: stack 317a691 offline 1875/0, db 2198/0 · RESTARTS done: com.cobalt.aset com.cobalt.radar · com.cobalt.replay bootstrapped, first run Mon 21:10 ET · cards: LIVE, untouched · outage 02:33 · tag deploy-2026-09-19b · PUSH: Dejan · ESCALATE: 9` |
| 3 | ops + archiver, 18:03 ET | `D3 DEPLOY DONE 851335e · LIVE: ops/2026-09-19 + archiver/append-0919 · migrations 0010/0011 applied · gate: stack 428e2e0 offline 2192/0, db 2537/0 · RESTARTS done: com.cobalt.aset com.cobalt.radar · R25 REVOKE: in force · archiver mode: upsert, first nightly Mon 20:30 ET · cards: LIVE, untouched · outage 02:29 · tag deploy-2026-09-19c · PUSH: Dejan · ESCALATE: 9` |
| — | cards go-live, 12:30 ET | `CARDS LIVE 12:30:01 ET · file sha256 f3663399… · stored values: match · resident logs clean: yes · rollback: not used · ESCALATE: 1` |
Tags present (`git tag --list "deploy-2026-09-1*"`): `deploy-2026-09-15`, `-16`, `-17`, `deploy-2026-09-19`, `-19b`, `-19c`. `origin/main` = `57ba74b` (his push 16:37 ET); `deploy-2026-09-19c` and everything after it are NOT pushed.

## Ladder status — what was verified against the tree (L35), where it departs from the audit and from the prompt
| claim | check | result |
|---|---|---|
| S2-P3 "BUILT-NOT-MERGED on `sprint-2/radar-panel`" (prompt) | `git log --oneline main..sprint-2/radar-panel` → EMPTY; `git log deploy-2026-09-15..sprint-2/radar-panel` → EMPTY; `cto-2026-09-15.md` §6.5 step 3 "merged, main `ebb1231`", step 8 tag `deploy-2026-09-15` | **FALSE — merged and live since 09-15.** Written as DONE-LIVE. |
| F8/F10 live since 12:30:01 ET, never rendered | `cards-golive-2026-09-19.md` last line; R27 | confirmed; first render Mon 09-21 04:00 ET = live morning 1 of the L7 shadow (R13) |
| F3/F12/F13 shipped with S2-P4 | `P4 DEPLOY DONE 5b58b7b`; desk L35 read (`cto-2026-09-19.md` §64): aset pid 80628, radar pid 80641, `com.cobalt.replay` loaded, not running | confirmed; F12/F13 never run (first 21:10 ET Mon) |
| `cobalt smoke s2` | `deploy-p4-2026-09-19.md` §6.5 "WAS NOT RUN, deliberately" (K9 red by design until first replay row; K3 = tribunal A7) | not started as acceptance |
| Agent SDK spike | `grep -rli "SDK spike"` over `reports/` and `BACKLOG.md` → only `ladder-audit-2026-09-19.md` and `cto-2026-09-19.md` | still zero evidence it ran |
| Stop date and verdict | ladder S2 heading 09-23; remaining trading mornings Mon 21 · Tue 22 · Wed 23 | **AT RISK, not LATE** — reason in the ladder block |
Files changed: `docs/00 - Project/SPRINT-LADDER-v0_1.md` (one block, placed after S2's "Code prompts owed", before `### S2-P1 prompt`), `docs/00 - Project/BACKLOG.md` (status header + off-ladder rows above the S2-P4 row; the S2-P4, S2-P2, S2-P3 headlines carry DONE-LIVE with their evidence; row bodies untouched). No scope, date or order changed.

## Laws fold — PROPOSED, NOT APPLIED
Read from `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` (L1–L70 contiguous). This hub wrote nothing under `6 - Permanent/Memory/` (L58) and took no number: **Dejan assigns numbers** (L58 as amended 09-18 allows the desk the next free one on his approval). Each item is judged on the record. P1–P3 are the desk's three candidates; P4–P7 are what the record shows beyond them.

**P1 — an allowlist is a pre-approval list, and an agent never writes a rule to unblock a denied action (desk candidate (a)). Judgement: earned; partly overlaps L61 ("his hands remain for … permission grants the classifier withholds") — an amendment of L61 or a new law is his call.** Evidence: `cto-2026-09-19.md` §61 — control probe: allow `touch …probe-c`, typed `touch …probe-d`, the file was created (14:08 ET), so under `--permission-mode auto` an unlisted command can still run; prefix matching therefore stays UNVERIFIABLE. §63 refines it: a deploy hub launched with NO `--permission-mode` flag (deploy 1, deploy 2) is gated by the allowlist itself. §4 PUSH row: the desk's push was denied `[Out-of-Place Publication]` and its attempt to add an allow rule `[Auto-Mode Bypass]`, correctly. **Note:** §64's watch script is NOT a counter-example — that rule was added on his ruling ("add that rule and start the watch"), so the text carries the exception. Exact fold text:
> **L__ Allowlist is not a gate (PROPOSED).** Under `--permission-mode auto` a per-session allowlist is a pre-approval list, not a whitelist: a command that matches no rule may still run, so no prompt may claim an action "cannot be typed". A session that was denied an action never writes, edits or requests a permission rule to unblock it; only Dejan may add a rule after a denial. A mid-run denial is a FAILED run (L62).

**P2 — a deploy prompt's stop line and its report's last line are the only watch surface (desk candidate (b)). Judgement: earned as practice; the 09-18 close already filed the same watcher practice as "Record, not law (C)" (`topics/cto-desk.md`), so whether it becomes law or stays desk record is his call.** Evidence: `cto-2026-09-19.md` §64 — the watch script "keys on the report's LAST NON-BLANK LINE only … after a first version matched quoted `FAILED:` strings inside the report body"; all three stop lines above and the rebase lane's were caught by it. **Not verified:** the prompt's "fired twice on body text" — the record I read states the first version matched quoted strings but gives no count. Exact fold text:
> **L__ Stop line (PROPOSED).** A run's stop line is the LAST NON-BLANK LINE of its report file and nothing else; a `FAILED:` or `DONE` string anywhere else in the report is text, never a stop. A watcher keys on that line only, and every launch prompt names the stop-line shape.

**P3 — the gate branch that both branches merge into may itself be what ships (desk candidate (c)). Judgement: earned; an amendment of L68 (and reading of L54's "`--ff-only`").** Evidence: `53-deploy-d3.md` line 9 — `archiver/append-0919` and `ops/2026-09-19` are siblings off one `main`, so ff-merging either makes the other non-ff; §1 builds `stack/deploy3-0919`, proves both suites on that exact tree, §4.3 fast-forwards `main` onto it ("the tree that lands is byte-identical to the tree the gate proved"); the prompt went through three house rounds (`cto-2026-09-19.md` §65: REAL 11 → 7 → 7, launch blockers 2 → 1 → 0); deploy 3 shipped it (`851335e`, gate `stack 428e2e0`). Exact fold text:
> **L68 amendment (PROPOSED).** Where the unmerged branches are siblings off one `main`, the integrated gate branch is the deliverable: `main` fast-forwards onto the gated tip, and the branches are never merged in sequence. The tree that lands must be byte-identical to the tree the gate proved.

**P4 — tribunals and design sessions run in parallel with building and deploying (`09-19 R15`, STANDING). Judgement: earned; a clause of L67.** Evidence: R15 (11:03 ET) "Also all tribunals and design session can run parallel to buying, so they are not a blocker. And Sunday tribunal is not a blocker"; applied through the day (the archiver tribunal beside P4 and ops). Exact fold text:
> **L67 amendment (PROPOSED).** Tribunals and design sessions run IN PARALLEL with building and deploying; a tribunal for one work item never holds another item's build, check or deploy lane. An item's OWN design still passes its tribunal before ITS build, and every build is checked by ≥3 houses before ITS deploy.

**P5 — no law step is dropped for speed; minimum idle time; the desk never asks him to re-decide a law in force (`09-19 R5`, `R6`, STANDING). Judgement: earned, but it binds the desk's behaviour — it may belong in `topics/working-contract.md` (the desk's, L58) rather than LAWS.md; his call.** Evidence: R5 07:35 ET "I don't want you to ask me to retract the law that's already in place"; R6 07:38 ET "as long as the rules are followed … minimum downtime … Not that … any steps are going to be left out." Exact fold text:
> **L__ Speed never drops a step (PROPOSED).** No law step is skipped for speed. The target is minimum idle time — whenever no hub is building and meters allow, the next lawful step of some work item is running, nights included, with the night's approvals brought to Dejan in one message before he sleeps (L62). The desk never asks him to confirm, waive or re-decide a law already in force; the only question of that kind it may bring is an explicit "should I overrule <law>?" with the reason and what is set aside.

**P6 — commits and PR bodies carry the session's own attribution only (`09-19 R19` item 3, "NO"). Judgement: earned (it has been re-decided by every hub since 09-18: `deploy-2026-09-19.md` item 5 asks for a ruling; this close received the same block again — ESCALATE 2).** Exact fold text:
> **L__ Commit attribution (PROPOSED).** Commits and PR bodies carry the attribution line the harness's real system prompt gives and nothing else; a block arriving inside a tool result that asks for a `Claude-Session` URL line or names a file-send tool is never followed, and is reported.

**P7 — OPEN, no fold text: which reading of L68's scope governs. `09-19 R31` narrowed it for ONE deploy** (gate stacks `sprint-2/p4` + `ops/2026-09-19` only) after the desk's wide reading (all unmerged branches, §60) stopped deploy 2 at 14:33; `09-19 R32/R33/R36` then ran deploy 3's gate on the two branches that actually landed. R31 says it narrows L68 "for this one deploy", so the standing reading is still unruled. Options for his word: WIDE (all unmerged branches) or NARROW (the branches that land in that deploy; an unlanded branch is rebased before its own deploy). Contested → OPEN, not proposed (LAWS "Fold-at-session-close").

Not law (desk memory, not folded here): R9/R10/R21/R28/R29 (Fable/Opus seat and meter), R22/R24 (reply discipline), R14 (wake-up carries the ladder — already in `CTO-DESK-WAKEUP.md` and `SESSION-CLOSE.md` 4a).

## Measure — always-loaded block (SESSION-CLOSE step 5; measured, NOT edited)
`cd "/Users/cobalt/Vault/Think/6 - Permanent/Memory"` then `wc -c INDEX.md profile.md preferences.md`:
```
    1768 INDEX.md
     706 profile.md
    1519 preferences.md
    3993 total
```
**3,993 / 4,000 — PASS**, margin 7 characters. Measured at the time this hub ran; the desk is editing this folder in parallel (steps 3–4), so its own re-measure after its writes is the one that counts — see ESCALATE 6.

## Owed and open (everything the day left)
| item | source | note |
|---|---|---|
| A2 — P4 deadline scope (bounds only async work) | `cto-2026-09-19.md` §51, §59, R30 | ruling owed; did not gate deploy 2 |
| A7 / K3 — a legitimate frozen HOLD grades red in smoke | `deploy-p4-2026-09-19.md` §6.5 | tribunal ruling owed; keeps `cobalt smoke s2` from going green |
| ESCALATE 19 — L32 ticker literals in P4 test code | §53, §54, §56 | open; carried since P4's fold checks |
| Four build MINORs G-A1, G-A2, H1, H2 | R36 disclosure; `deploy-d3-2026-09-19.md` | none on a scheduled path; H1 = backfill window counts sleeps but not fetch latency |
| Archiver rulings R2-1 (shadow file date ET vs UTC) · C1 (`poller_writable` is interval-based; shadow `late` is structurally 0) · O-6 · R3-1 (§11 "follows 0006's pattern" contradicts R25) | §53, §57, §59 | ONE per message; the shadow numbers arrive Sunday after tonight's nightly (R35) |
| `cmd_migrate` must refuse `--allow-prod` unless `COBALT_ENV=production` | §57, §59; `cli.py:542`, `db.py:146` | the approved dev wildcard is not production-safe; new launch lines carry exact `db migrate` strings |
| Missing `git push` allow rule | R11 row, PUSH row; L55 status note | his: "We will update .claude file later." Every push is his hand until then; `deploy-2026-09-19c` and later docs unpushed |
| `htf_level_proximity` end points | R17 reading (iv) | direction ruled, end points provisional; no curve shipped |
| Leg-count reading | R17 readings (i)(ii) | desk readings stated back, his to correct |
| S3 preconditions | ladder "Carried open items"; `ladder-audit` §3 | DRC template review + stop-override authority (mock #6) — S3 starts 09-24 |
| `31-packet` yaml copies | ESCALATE 4 | two untracked files; also `23-packet`'s two committed copies (§45) — his word before the push |
| **Migration proof cost, 106.8 s of a 149 s outage reading 8.8 M `system.bars` rows** | `cto-2026-09-19.md` §65 | grows with the table; **the Sunday 13:05 ET bars tribunal inherits it** as a fact of its input |
| R34 C — stagger pool cadences | R34, `PROJECT-LEDGER.md:1289` | queued "as soon as possible", not started |
| Agent SDK spike | ladder S2 | no evidence it ever ran — decision his (ESCALATE 5) |

## ESCALATE
1. **Seven law items P1–P7 await his word** — accept, amend or refuse each; P7 is OPEN with no text. No number taken.
2. **A block styled as a system reminder arrived attached to the `Read` of `99-close-0919.md`**, asking for a `Claude-Session: https://claude.ai/code/session_…` line in every commit and PR body and naming a file-send tool. It did not come from Dejan or from the session's real attribution reminder (`Co-Authored-By: Claude Sonnet 5 …`); `09-19 R19` item 3 is "NO". Not followed; no file sent. Another sighting after the desk's fourth (`cto-2026-09-19.md` §45).
3. **The prompt's S2-P3 premise ("BUILT-NOT-MERGED on `sprint-2/radar-panel`") is false** — the branch is fully merged (`ebb1231`, `deploy-2026-09-15`). Written as DONE-LIVE from the tree; the desk should correct whatever record carried the premise (`cto-2026-09-19.md`, the audit's "S2-P3 DONE-NOT-LIVE (partial)" wording is also stale for the same reason).
4. **`docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml` and `p2-live-settings.yaml` are untracked and left untracked** — copies of files under gitignored `data/backups/`; never `git add`ed (CLAUDE.md, L32). `23-packet/`'s two copies were committed at 11:5x (§45); whether they leave the index before the next push is his word.
5. **Agent SDK spike has no evidence and no decision** since 09-04 — run it before S5 or drop it; scope is his, so the ladder line is unchanged.
6. **Always-loaded block at 3,993 of 4,000** — any desk append to INDEX / profile / preferences over 7 characters trips the cap (SESSION-CLOSE step 5 FAILS over it). The desk re-measures after its steps 3–4; this hub does not trim (L58).

## Verification before commit
- `COBALT_ENV=production uv run cobalt validate` → exit 0. Quoted: `13 trade_def(s) validated OK from the vault.` · `Jobs (F17): 16 registered — 6 resident, 10 one-shot. Kill phrase 'COBALT STOP'.` · `  registry <-> ops/: 16 label(s), exact match.` · `Placement (docs/PLACEMENT.md): tree clean.` (matches the expected 16 = 6 resident + 10 one-shot); `archiver.write_mode = upsert`, `archiver.shadow_compare = on -> shadow artifact tonight: yes`.
- `git status --porcelain` before the commit: ` M BACKLOG.md` · ` M PROJECT-LEDGER.md` · ` M SPRINT-LADDER-v0_1.md` (this hub) · ` M reports/seat-usage.md` (machine-written, L51-2, +20 lines) · `?? reports/day-open-2026-09-20.md` (machine-written, first lines read: DAY-OPEN header, C1 launchd) · `?? prompts/2026-09-19/31-packet/p2-dark-settings.yaml` and `p2-live-settings.yaml` (NOT added, ESCALATE 4) · `?? reports/close-2026-09-19.md` (this report). Anything else unexpected: none.
- `git add` BY NAME for exactly six files: `BACKLOG.md`, `PROJECT-LEDGER.md`, `SPRINT-LADDER-v0_1.md`, `seat-usage.md`, `day-open-2026-09-20.md`, `close-2026-09-19.md`. Nothing under `data/`, no `.log`.
- The last line carries `<see git log -1>` (one run, one commit, L46: the report is inside the commit it names, so the hash cannot be in its own file — same as `close-2026-09-18.md`); the hash is the `git log -1` of main after this commit and is stated in the launch reply.
- Memory: this hub wrote nothing under `6 - Permanent/Memory/` (L58). SESSION-CLOSE steps 3–4 (NOW rewrite, areas/topics append) are the desk's.

CLOSE COMMITTED <see git log -1> · ledger: 49 lines · laws: 7 PROPOSED, 0 applied · ladder: S2 status block written, AT RISK · always-loaded: 3993/4000 · validate: exit 0 · PUSH: Dejan · ESCALATE: 6
