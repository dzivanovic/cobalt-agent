# DRC D1 CHECK — round 1 of at most 3 (hub `drc-d1-check`, Sonnet 5)

## §0 Headline
- WHAT: check of the D1 build (two daily-file parsers, header classifier, FIFO pairing, carried positions, `DrcStore`, migration `0016_drc`) — tip `f6798406`, base `04b05cd4`, 10 commits, worktree `/Users/cobalt/cobalt-wt/drc-d1`.
- STATUS: DONE — 3 of 4 houses checked (grok `BUILD STANDS` · gemini `BUILD STANDS EXCEPT` one weak assertion · opus `FIX`; sol METER until Sep 26 2026 6:47 AM). Ready for the next chunk: 2 of 3 (opus NO).
- FILE-CHECK: 14 claims HOLD (opus's D1-3 short-carry and D1-5 flat-seed items, two `line 1` decode messages, one empty-cell reason, the real E1 date `2026-09-18` in committed files, 5 weak-assertion claims, the registry-pin hunks outside the named files, two minor storage notes). Grok and Gemini did not raise the D1-3 / D1-5 items.
- ESCALATE: 12.

## PREFLIGHT
All commands are bare, one per Bash call; every row ALLOWED (no denial).

| rule | command | exit | result |
|---|---|---|---|
| DATE GATE (row 1) | `date` | 0 | `Wed Sep 23 17:35:59 EDT 2026` → D = 2026-09-23, inside R30 (through 2026-09-23 23:59 ET) |
| AUTH R13 (09-20) | `grep -n "^| R13 " …/cto-2026-09-20.md` | 0 | row present (line 86) |
| AUTH R40 | `grep -n "^| R40 " …/cto-2026-09-21.md` | 0 | carries `ONE EXTRA DOT THAT CANNOT BE TAPPED` |
| AUTH R44 | `grep -n "^| R44 " …/cto-2026-09-21.md` | 0 | carries `ONE BUILD of the whole FINAL` |
| AUTH R46 | `grep -n "^| R46 " …/cto-2026-09-21.md` | 0 | carries `instead of Astra you can use Sol` |
| R46 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"instead of Astra you can use Sol" -- …cto-2026-09-21.md` | 0 | `53e059456750c0c9efcf50222a7a647630dc4b04` |
| AUTH R49 | `grep -n "^| R49 " …/cto-2026-09-21.md` | 0 | carries `"Approved"` |
| R49 Sol string present | `grep -c -F "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" …cto-2026-09-21.md` | 0 | `1` |
| R49 Sol string committed | `git … log -1 --format=%H -S"Bash(codex exec …gpt-5.6-sol -s read-only *)" -- …cto-2026-09-21.md` | 0 | `60147d400b009db5a2518e02b8ab1fe5765db405` |
| AUTH R32 (Opus 5.5 string) | `grep -n "^| R32 " …/cto-2026-09-22.md` | 0 | carries `claude -p --model claude-opus-5-5` (line 131) |
| R32 committed | `git … log -1 --format=%H -S"Bash(claude -p --model claude-opus-5-5 *)" -- …cto-2026-09-22.md` | 0 | `b8a72b5300370e248cd6c7a8a732258fec03e6a0` |
| DATE/EXT R30 | `grep -n "^| R30 " …/cto-2026-09-22.md` | 0 | carries `Bash(grok *) and Bash(agy *) through 2026-09-23 23:59 ET` |
| DATE/EXT R105 | `grep -n "^| R105 " …/cto-2026-09-22.md` | 0 | carries `Bash(grok *) and Bash(agy *) through 2026-10-07` |
| R105 committed | `git … log -1 --format=%H -S"through 2026-10-07" -- …cto-2026-09-22.md` | 0 | `123f7ad5bd2dbfd7610fb13cda36736505e9cea0` |
| LAUNCH ROW | `grep -n "54-drc-d1-check.md" …/cto-2026-09-23.md` | 0 | `\| R90 \|` row present, number filled (not `__`); row is a DESK LAUNCH ROW, "NO WORDS OF HIS BEYOND R59 / R60 (3) / 09-22 R105" |
| launch row committed | `git … log -1 --format=%H -S"54-drc-d1-check.md" -- "…/cto-2026-09-2*.md"` | 0 | `f173dbfe9261afdaf74db9788f394f7f5dad3ba7` |
| THIRTEEN + THREE | 16 × `grep -c -F -e "<rule>" …/08-bars-chunk-e-check.md` (grok, agy, mkdir, git show, git log, s2 show/log/diff, ls, grep, tail, wc, date; denies AskUserQuestion, EnterWorktree, git push) | 0 ×16 | each `1` |
| ASTRA absent | launch line as printed in the prompt | — | no `gpt-6-astra` string in the line |
| `grok --version` | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` → UP |
| `agy --version` | `agy --version` | 0 | `1.2.9` → UP |
| worktree | `ls /Users/cobalt/cobalt-wt/drc-d1` | 0 | present (src, tests, docs, configs …) |
| build report last lines | `tail -n 3 …/drc-d1-build-2026-09-23.md` | 0 | last non-blank line: `DRC D1 BUILT f6798406 (+ this report's commit) \| on 04b05cd4 \| offline 2126/0 \| with-DB 250/0 \| migration: src/cobalt/db_migrations/0016_drc.sql (desk renumbers) \| E1: 1 - Trading/5 - Review/_imports/drc/2026-09-18/ \| tests added: 180 \| .env: removed, proven gone \| ESCALATE: 12` → `<tip>` = `f6798406`, `<base>` = `04b05cd4` |
| commits | `git -C /Users/cobalt/cobalt log --oneline 04b05cd4..f6798406` | 0 | 10 commits (D1-0, D1-1, D1-2a, D1-2, D1-3, D1-4, D1-5 ×3 incl. two `fix(drc)`, D1-6) |
| stat / boundary | `git -C /Users/cobalt/cobalt log --stat --oneline 04b05cd4..f6798406` | 0 | see `## Assertions and boundary` (i) |
| `.env` | `ls /Users/cobalt/cobalt-wt/drc-d1/.env` | 1 | `No such file or directory` (as required) |
| recovery | `ls scratch/tribunal-bars-0920/drc-check/d1` | 1 | `No such file or directory` → fresh run |
| HOUSE-LANE STAGGER | `tail -n 1 …/smoke-only-deploy-review-2026-09-23.md` | 0 | `SMOKE ONLY DEPLOY REVIEW DONE · houses: 2 of 3 · other houses: 1 of 2 · blockers: 1 · folds: 6` → DONE shape, not running |
| CODEX LAUNCH SHAPE | `grep -c -F "Experiment field: **reads started**" …/setups-tribunal-r2-2026-09-21.md` | 0 | `1` → ` < /dev/null` recorded as working (Sol not launched) |
| PROBE Sol | `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "Reply with only the word OK." < /dev/null` | 1 | `ERROR: You've hit your usage limit … try again at Sep 26th, 2026 6:47 AM.` → `sol: METER — retry after Sep 26th, 2026 6:47 AM` — SKIPPED (R13) |
| PROBE Opus | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` | 0 | `OK` → UP |
| PROBE Grok / Gemini | the two `--version` rows above | 0 | UP / UP |
| COUNT | grok UP · gemini UP · opus UP · sol METER | — | 3 of 4 UP ≥ 3 → launch every UP house |

R105 scope record: R105 names the hubs "`53`–`57`" in the 09-22 numbering; this file is `53` re-issued whole as `54`. The launch day's desk file DOES carry his word covering the reading: `cto-2026-09-23.md` R60 — His word: "A" — (3) "READING: 09-22 R105 ("for `48`–`57` only") covers today's whole re-issues `53-drc-d1-build.md` / `54-drc-d1-check.md` (their `.env` pair and grok / agy)". R90 itself is a desk launch row with no words of his. (R30 also stands through 2026-09-23 23:59 ET independently.)

## Packet
Staged in `scratch/tribunal-bars-0920/drc-check/d1/` (absolute `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/d1/`), Read → Write, no `mkdir`. Same 13 files for every house.

| file | bytes | source · check |
|---|---|---|
| `QUESTIONS.md` | 7,589 | the prompt's QUESTIONS text verbatim (proved a substring of `54`'s line 26 by `grep -c -F -f`) + the "Files in this folder:" paragraph |
| `prompt.md.part1` / `.part2` | 36,029 + 12,060 = 48,089 | `prompts/2026-09-23/53-drc-d1-build.md` (95 lines); sum = original 48,089 B; cut at `## STEP-0`; every non-empty line found as a whole line in the original |
| `rulings.md` | 7,568 | R91 R92 R93 R94 R101 R103 R114 R117 (`cto-2026-09-22.md`) + R17 R59 (`cto-2026-09-23.md`) verbatim; every row found as a whole line in its desk file |
| `design.md` | 27,755 | v2 §3 (74–83), §4 (84–99), §9 (169–184), §10 (185–208), §12 (217–234) verbatim under path + line-range headers |
| `build-diff.md.part1` … `.part5` | 27,916 + 33,116 + 31,132 + 28,583 + 37,938 = 158,685 | `git log -p 04b05cd4..f6798406 -- . ":(exclude)docs"` (fixtures included); each part ≤ 38,000 B, cut at `commit` / `diff --git` lines |
| `devdocs.md` | 25,608 | `git log -p 04b05cd4..f6798406 -- "docs/40 - DevDocs/cobalt"`; equals the captured original (25,608 B), whitespace kept |
| `build-report.md.part1` / `.part2` | 30,402 + 11,813 = 42,215 | `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-d1-build-2026-09-23.md` whole; sum = original 42,215 B; cut at `## CLOSE`; `## CLOSE`, `## ESCALATE`, `## CONTINUE` and the stop line in part2 |

**Honest size:** 317,509 B whole packet ≈ **79,377 tokens per checker** (÷4). NOT staged: his real files (vault only).

**Diff completeness (packet check):** `grep -c "^commit "` over the five parts = 3 + 1 + 2 + 1 + 2 = **9** = the 10 commits of `04b05cd4..f6798406` minus the docs-only D1-6 commit `61ef8381`; `grep -c "^diff --git"` = 8 + 6 + 3 + 4 + 10 = **31** = the non-docs file-touches of `--stat` (1 + 1 + 11 + 2 + 2 + 1 + 4 + 3 + 6). No mismatch.

**Disclosure (byte-identity):** the five `build-diff` parts carry 29 whitespace-only lines (commit-message continuation `    ` and diff context ` ` lines) as EMPTY lines: 17 + 7 + 2 + 1 + 2 lines, 26 + 10 + 8 + 4 + 8 = 56 B. Proof: original diff content 158,741 B − 56 = 158,685 B staged; the disclosure is named in `QUESTIONS.md`'s file list. Per-part line counts equal the original's (610 / 826 / 794 / 766 / 953 = 3,949). While staging I mistyped three fixture lines of part5 (`Type` cell `Short` read as `Margin`, and one order-id) — found by `grep -n -v -x -F -f <original> <part>`, corrected, and the check re-run clean; every part was checked that way.

## CONTINUE
DATE GATE second row: `date` → `Wed Sep 23 18:03:03 EDT 2026` (inside R30 / R105). Before-launch listings taken (`ls -la` of the packet folder: 13 files as staged; `ls -la /Users/cobalt/cobalt-wt/drc-d1`: no `.env`, latest mtime 14:19).
LAUNCHED 18:03 EDT, all `run_in_background`, one attempt each, 45-minute clock ends 18:48:
- grok — task `b09seeo5s`: `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" -p "…"` (writes `grok-check.md` itself)
- gemini — task `b2oqsbsf2`: `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="…"` (prints; I write `gemini-check.md`)
- opus — task `bwz3v9zoi`: `claude -p --model claude-opus-5-5 "…" --permission-mode plan --add-dir …/drc-check/d1 --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"` (prints; I write `opus-check.md`)
- sol — NOT launched: `METER — retry after Sep 26th, 2026 6:47 AM` (R13).
RETURNS (each `date` read at its completion notice): gemini 18:05:10 (2 min; printed a CHECK line) · opus 18:08:56 (5 min; printed a CHECK line) · grok 18:16:43 (13 min; wrote `grok-check.md` itself; its stdout was only narration). No checker passed 45 minutes; no TaskStop used.
WRITTEN-NOTHING PROOF: `ls -la` of the packet folder before the launches showed 13 files; after the returns it shows those 13 plus `gemini-check.md` (909 B, written by me), `opus-check.md` (9,059 B, written by me) and `grok-check.md` (13,730 B, Grok's own approved write) — nothing else new, no packet file changed (sizes and mtimes identical). `ls -la /Users/cobalt/cobalt-wt/drc-d1` before and after every return: identical (latest mtime 14:19; no `.env`). `grep -c -i "denied\|not allowed\|permission"` on each check file: gemini 0 · opus 0 · grok 0.
PROCESS NOTE: once during staging I used a command substitution inside a `grep` (a shape outside the bare-command rule); it only re-verified a corrected line and its result (1) was re-proved with a plain `grep -c -v -x -F -f`.
next: none — the report is complete; last line below.

## Per question
`MEETS: n of 3` = the houses that checked (grok · gemini · opus). Sol did not check (METER).

| row | grok | gemini | sol | opus | MEETS |
|---|---|---|---|---|---|
| D1-0 fixtures | MEETS | MEETS | — | MEETS | 3 of 3 |
| D1-1 models | MEETS | MEETS | — | MEETS | 3 of 3 |
| D1-2a header classifier | MEETS | MEETS | — | MEETS | 3 of 3 |
| D1-2 trading-log parser | MEETS | MEETS | — | NEW DEFECT (low) `trading_log.py:~188-190` — a non-UTF-8 byte on data line 16 fails the file as `line 1`; test `:~166` asserts no line | 2 of 3 |
| D1-3 pairing + carry | MEETS | MEETS | — | DOES NOT MEET `pairing.py:~187-188` — a carried SHORT with no seed: today's first `B` opens a phantom LONG, nothing fails; test covers the carried long only | 2 of 3 |
| D1-4 stats-log parser | MEETS | MEETS | — | NEW DEFECT (low) `pairing.py:276`, `stats_log.py:~294` — empty `Open Date` reads "empty: Open Date, Open Time"; undecodable byte named `line 1` | 2 of 3 |
| D1-5 store + migration | MEETS | MEETS | — | NEW DEFECT `store.py:~225-246` with `:~192` — `record_day` writes a `day` row for a pairing-not-computed day; `seed_for(D+1)` then returns `[]` and D+1 pairs as flat | 2 of 3 |
| D1-6 DevDocs | MEETS | MEETS | — | MEETS | 3 of 3 |

Focus items each checker answered (their words are in the check files): partial, date, accounts, stop, header detection, several playbooks, L31 — all three found them met. Opus raised in addition: L57 (`open_position` rows' `inputs` hold only `trading_log_import_id`), `drc_imports.degraded` stores the flag only, a "3 exit times" (report) vs "3 exit fills at 2 Times" (README / fixture) inconsistency (NOT CHECKABLE), and a seam note (`record_import` runs before pairing). Opus and Grok named the date `2026-09-18` in committed files; Gemini did not.

## Assertions and boundary
**grok** — (a) "`tests/cobalt/test_drc_store.py:425` `test_the_system_role_cannot_read_the_drc_tables` — `pytest.raises(Exception)` on `SELECT 1 FROM drc_imports`. Any error passes, including a missing relation. The SQL side is still USER + `OWNER TO cobalt_user`, and the offline placement tests name the side. One weak assertion, not a row miss." (b) "Three registry-order pins, each only inserting `0016_drc.rollback.sql` at the head of an existing newest-first list. Disclosed in the report (ESCALATE 2). Not a behavior change." — `test_p4_migrations.py`, `test_radar_migration.py`, `test_radar_score_migration.py`; "`test_tenancy.py` is named by D1-5 (the `0004` pin went red; `DrcStore` joined `_stores()`). `test_archiver_migrations.py` is named." (c) "NO PATH. … `store.py` calls `daymode.propose.prior_trading_day` — the calendar function D1-3 names for the contiguity check, not a score, rank, grade, or size."
**gemini** — (a) "`tests/cobalt/test_drc_store.py:429` DOES — `with pytest.raises(Exception):` is weak for checking a database privilege error." (b) "changed outside named files: `tests/cobalt/test_p4_migrations.py`, `tests/cobalt/test_radar_migration.py`, `tests/cobalt/test_radar_score_migration.py` (all seam registry pins)." (c) "L52: NO PATH"
**sol** — did not check.
**opus** — (a) "`test_drc_detect.py:~304-312` lists the both-kinds file as failed but never asserts the set's status. The set actually returns `pass`. · `test_drc_store.py:~347` checks only `startswith("PARTIAL — missing: ")` and never checks the column name. · `test_drc_trading_log.py:~166` asserts no line for the undecodable file. · `test_drc_pairing.py:~239` covers the carried long only. · The `_bytes` docstring in `test_drc_stats_log.py:~35` says quoted `""` is written back. `QUOTE_MINIMAL` does not do that. No assertion depends on it." (b) "`test_p4_migrations.py:102,112,118`, `test_radar_migration.py:34-35` and `test_radar_score_migration.py:104-121`: position pins +0016. · `test_tenancy.py:74,84` (`DrcStore` joins `_stores()`) and `:515` (down-to-0004 pin). `test_tenancy.py` is named only "if the one-side assertion needs" the tables, and these edits are for other reasons. · All of these are stated in the report." (c) "NO PATH. `drc/*` only reads `daymode.propose.prior_trading_day`. The `placement.py` change reaches `com.cobalt.radar` as a static map import only."

**Boundary, checked by me (plain facts):**
- (i) `git -C /Users/cobalt/cobalt log --stat --oneline 04b05cd4..f6798406` (PREFLIGHT) names: `src/cobalt/drc/*` (7 files), `src/cobalt/db_migrations/{0016_drc.sql, 0016_drc.rollback.sql, __init__.py, placement.py}`, `tests/cobalt/test_drc_{detect,trading_log,pairing,stats_log,store}.py`, `tests/fixtures/drc/*` (5), the registry-pin tests `test_archiver_migrations.py` (named by D1-5), `test_tenancy.py` (named "only if needed"), `test_p4_migrations.py`, `test_radar_migration.py`, `test_radar_score_migration.py` (NOT among `53`'s D1-5 named files — the report states them under D1-5 C and ESCALATE 2), DevDocs (`cobalt/drc/*` 7, `db_migrations/{__init__,placement}.md`) and the builder's report. Nothing else.
- (ii) `git -C /Users/cobalt/cobalt log --oneline 04b05cd4..f6798406 -- src/cobalt/aset src/cobalt/prefill src/cobalt/replay src/cobalt/cards src/cobalt/radar src/cobalt/settings configs` → EMPTY.
- (iii) `grep -rn -i "das_\|tradezella\|zella" …/src/cobalt/drc …/src/cobalt/db_migrations` → 0 hits (no comment or docstring line either).
- (iv) `git log --name-status --format= 04b05cd4..f6798406 -- src/cobalt/db_migrations` → `A 0016_drc.rollback.sql`, `A 0016_drc.sql`, `M __init__.py`, `M placement.py`: exactly ONE new pair. `grep -c "CREATE TABLE" 0016_drc.sql` → 3. Number `0016` = the number the build report's `## ESCALATE` (ii) states, with its re-derive list ("branches carry `0001`–`0013`, `0014` is reserved by H1 (`47`), and `0015` is reserved by stale score (`46`, conditional)").
- (v) `grep -rn "Trade Risk" …/src/cobalt/drc` → one hit: `src/cobalt/drc/stats_log.py:102:    "Trade Risk",` — inside the `REQUIRED` name tuple; no line derives a price from it.

## Checked against the branch
Original under `/Users/cobalt/cobalt-wt/drc-d1/`. Each row counted once toward `defects that HOLD` when it is a defect / weak-assertion / outside-hunk / L32 claim and HOLDS.

| # | claim | who | file:line | result | note |
|---|---|---|---|---|---|
| 1 | a carried short with no seed: first `B` opens a LONG, nothing fails | opus | `pairing.py:186-189` (`if book is None:` → `ExecSide.BUY` → `_open(e, Direction.LONG)`) | HOLDS | Code path as described; the only no-seed FAIL is the `S` branch (`:190-196`). Grok and Gemini: D1-3 MEETS — contradiction, both quoted above. |
| 2 | `seed_for` seeds flat after a pairing-not-computed day | opus | `store.py:192-201` (`record_day` always appends a `day` row carrying `not_computed`), `:225-246` (`seed_for` reads `kind='day'` rows only for contiguity, returns `open_position` rows) | HOLDS | A `day` row exists → `check_contiguity` passes; `build_day` on a pairing-not-computed file yields no `open_positions` → `[]`. |
| 3 | non-UTF-8 byte on a data line is reported as `line 1` (trading log) | opus | `trading_log.py:188-190` | HOLDS | `_decode(data)` over the whole text; `TradingLogError(1, "not UTF-8 text …")`. |
| 4 | same in the stats log | opus | `stats_log.py:292-294` | HOLDS | `StatsLogError(1, "not UTF-8 text …")`. |
| 5 | empty `Open Date` cell reports "empty: Open Date, Open Time" | opus | `pairing.py:275-279` | HOLDS | `zip(MATCH_INPUTS, (row.symbol, row.side, row.open_time, row.open_time))`; `open_time` is None when either cell is empty, and both names are reported. |
| 6 | real E1 date `2026-09-18` committed | opus (grok noted) | `tests/fixtures/drc/README.md:4`; `tests/cobalt/test_drc_detect.py:318,324`; `src/cobalt/drc/trading_log.py:9`; the build report (`## E1`, its stop line) | HOLDS | The literal is in all four; it is the E1 folder's date (R114 names it). No account id or ticker-and-price pair found in the fixtures. Grok: "That is the E1 folder the prompt's stop line requires, not a fill." |
| 7 | `pytest.raises(Exception)` on the system-role read | gemini, grok | `tests/cobalt/test_drc_store.py:427` | HOLDS | Both cite the wrong number (gemini `:429` is the `ROLLBACK TO SAVEPOINT` line; grok `:425` is the function body's opening); the broad `Exception` is at `:427` in a 429-line file. |
| 8 | both-kinds file in a set: only the failed list asserted, status not | opus | `tests/cobalt/test_drc_detect.py:304-312` | HOLDS | `:312` asserts only `[d.name for d in s.failed] == ["mix.md"]`. |
| 9 | partial reason checked by `startswith` only | opus | `tests/cobalt/test_drc_store.py:347` | HOLDS | `assert status == "partial" and reason.startswith("PARTIAL — missing: ")`. |
| 10 | undecodable-file test asserts no line | opus | `tests/cobalt/test_drc_trading_log.py:166-168` | HOLDS | Asserts `p.result.outcome is Outcome.FAILED` only. |
| 11 | carried-symbol test covers the long case only | opus | `tests/cobalt/test_drc_pairing.py:239-241` | HOLDS | Runs the seed day-2 file (`S` of DDD) with no seed; no carried-short input exists. |
| 12 | `_bytes` docstring says quoted `""` is written back but `QUOTE_MINIMAL` does not | opus | `tests/cobalt/test_drc_stats_log.py:36,39` | NOT CHECKABLE FROM READS | The docstring and the `QUOTE_MINIMAL` flag are as quoted; what `csv.writer` emits for empty cells needs a run (`python -c` writing one row). No assertion is seen depending on it. |
| 13 | edits outside the rows' named files | all three | `tests/cobalt/test_p4_migrations.py`, `test_radar_migration.py`, `test_radar_score_migration.py` (position pins +`0016`); `test_tenancy.py` (`DrcStore` in `_stores()`, the `0004` pin) | HOLDS | `--stat` confirms; each is stated in the report (D1-5 C, ESCALATE 2). |
| 14 | `open_position` rows' `inputs` hold only `trading_log_import_id` (L57, "minor") | opus | `store.py` `record_day` (`for p in pairing.open_positions` → `{"trading_log_import_id": trading_id}`) | HOLDS | No fill lines and no seed reference in that row's `inputs`. |
| 15 | `drc_imports.degraded` stores the flag, not the extra names | opus | `store.py` `record_import` (`result.degraded` only; `result.extras` is not passed) | HOLDS | The extras stay on `ImportResult`, not in the row. |
| 16 | "3 exit times" (report) vs "3 exit fills at 2 Times" (README, fixture) | opus | report `## E1` `structure` row; `tests/fixtures/drc/README.md` (scale-out paragraph); `tests/fixtures/drc/trading_log_e1.csv` (AAA sells at 11:10:40, 10:55:00, 10:55:00) | NOT CHECKABLE FROM READS | The fixture holds 3 exit fills at 2 Times as the README says; whether the REAL E1 has 3 exit Times needs E1 (not staged). |
| 17 | L52 NO PATH | all three | `git diff` boundary (ii) EMPTY; `drc/*` imports only `daymode.propose.prior_trading_day` | no path found | (ii) is empty; no scoring / ranking file is touched. |

Contradictions between checkers, quoted: **D1-3** — grok "`pair_day` … A carried symbol with no prior row fails naming `DDD` (`:242`)" · gemini "D1-3: `tests/cobalt/test_drc_pairing.py:658` MEETS THE ROW" · opus "\"A carried symbol with no prior row FAILs\" only holds for a carried LONG." **L32** — opus "The real E1 date `2026-09-18` is committed in three places" · grok "That is the E1 folder the prompt's stop line requires, not a fill." Gemini's `:658` does not exist in the 295-line `test_drc_pairing.py`.

## Ready for the next chunk
| checker | CHECK D1 line | ready | reason |
|---|---|---|---|
| grok | `CHECK D1: BUILD STANDS` | YES | — |
| gemini | `CHECK D1: BUILD STANDS EXCEPT test_drc_store.py:429 weak exception` | YES | — |
| sol | none (METER — retry after Sep 26th, 2026 6:47 AM) | — | did not check |
| opus | `CHECK D1: FIX D1-3 (carried short with no seed opens a phantom long, `pairing.py:~187`), D1-5 (`seed_for` seeds flat after a pairing-not-computed day, `store.py:~225-246`), D1-2/D1-4 (an undecodable byte is named `line 1`; the Open Date/Time empty-cell reason `pairing.py:276`), L32 (`2026-09-18` in `README.md:4`, `test_drc_detect.py:323`, `trading_log.py:9`)` | NO | "seed paths can invent a carried position; fix D1 first" |

## ESCALATE
1. **L32 — before any merge:** the real E1 date `2026-09-18` is committed in `tests/fixtures/drc/README.md:4`, `tests/cobalt/test_drc_detect.py:318,324`, `src/cobalt/drc/trading_log.py:9` and the builder's report (row 6 HOLDS). No account id or ticker-and-price pair was found by any checker.
2. **Opus `CHECK D1: FIX`** (its full line above).
3. **D1-3 (row 1 HOLDS):** a carried short with no seed pairs as a phantom long (`pairing.py:186-189`); the carried-symbol test covers the long only (row 11). Grok and Gemini marked D1-3 MEETS.
4. **D1-5 (row 2 HOLDS):** `seed_for` returns `[]` for a day whose prior `day` row is pairing-not-computed (`store.py:192-201`, `:225-246`).
5. **D1-2 / D1-4 (rows 3, 4, 5 HOLD):** an undecodable byte is reported as `line 1` in both parsers; an empty `Open Date` cell reads "empty: Open Date, Open Time" (`pairing.py:275-279`).
6. **Weak-assertion claims that HOLD (rows 7–11):** `test_drc_store.py:427` (`pytest.raises(Exception)`), `test_drc_detect.py:304-312` (set status), `test_drc_store.py:347` (`startswith`), `test_drc_trading_log.py:166` (no line asserted), `test_drc_pairing.py:239` (carried long only).
7. **Outside-the-rows hunks that HOLD (row 13):** `test_p4_migrations.py`, `test_radar_migration.py`, `test_radar_score_migration.py` (not among `53`'s D1-5 named files; stated in the report) and `test_tenancy.py`'s two edits (named "only if needed"; edited for other reasons).
8. **Minor storage notes that HOLD (rows 14, 15):** `open_position` `inputs` carry only `trading_log_import_id`; `drc_imports.degraded` stores the flag without the extras.
9. **Checker contradictions:** D1-3 and the L32 date (quoted under `## Checked against the branch`).
10. **A checker that did not check:** sol — `METER — retry after Sep 26th, 2026 6:47 AM` (R13). Three houses checked, so L67's floor is met; no `ASK DESK`.
11. **R105 scope record:** R105 names the hubs "`53`–`57`" (09-22 numbering). The launch day's desk file carries his word covering this file's re-issue — `cto-2026-09-23.md` R60: His word: "A" … "(3) READING: 09-22 R105 ("for `48`–`57` only") covers today's whole re-issues `53-drc-d1-build.md` / `54-drc-d1-check.md` (their `.env` pair and grok / agy)". R90 (this launch row) itself carries no words of his.
12. **NOT CHECKABLE FROM READS carried:** row 12 (`csv.writer` behaviour — run a one-row write); row 16 (real E1's exit Times — needs E1); the byte-identity of the fixture headers to the real files, the `4 of 4` zone proof and the with-DB counts (`250/0`, three deselected live-`cobalt_dev` items) — the builder's claims, unproven by these reads (L70: not defects).

DRC D1 CHECK DONE · grok: CHECK D1: BUILD STANDS · gemini: CHECK D1: BUILD STANDS EXCEPT test_drc_store.py:429 weak exception · sol: METER · opus: CHECK D1: FIX · houses that checked: 3 of 4 · defects that HOLD: 14 · ready for the next chunk: 2 of 3 · ESCALATE: 12
