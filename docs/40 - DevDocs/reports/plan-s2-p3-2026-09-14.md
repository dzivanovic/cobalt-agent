# REPORT — S2-P3 plan session (2026-09-14)

Seat: Opus 5, architect, plan mode · Branch `sprint-2/radar-panel` · Worktree `~/cobalt-wt/s2-p3-radar-panel` · No build, no DB writes, no vault writes.

## §0 Headline

- Plan written: `docs/40 - DevDocs/plans/plan-s2-p3-2026-09-14.md`. It covers a read-only `/radar` route on the ASET sheet: pool view live now, plus a card-ladder shell proven on fixtures that renders empty in production.
- 4 rulings taken, one per message. Dejan approved the plan with 3 amendments, all applied.
- ESCALATE: none (2 raised, both closed by Dejan).
- RESTARTS: none (this commit is docs only). Build-range expectation: `com.cobalt.aset` + `com.cobalt.radar` (plan §5).

## Rulings

| Ruling | Options | Taken | Recommendation followed? |
|---|---|---|---|
| 2026-09-14 R1 scope | A two layers · B wait for P2 | A | yes |
| 2026-09-14 R2 health thresholds (#8) | A per-class, rows at P2 · B uniform 60/40, rows at P3 | A | yes |
| 2026-09-14 R3 detail order (#11) | A levels first, no chart · B mock order, static chart | A | yes |
| 2026-09-14 R4 builder seat | A Opus 5 headless · B Sol high | **B**, launched by the Sonnet hub after Astra, Codex probed first, Opus fallback | no. Dejan's reason: L27's overflow sentence is under routing review; use the full Codex week; Opus kept for P2/P4 write paths |

## Approval amendments (applied)

| # | Amendment | Applied in |
|---|---|---|
| 1 | No `_inflight` copies (`docs/_inflight` is README-only per PLACEMENT.md; `~/cobalt` has another writer today). Plan + report committed on the branch, one commit. | this commit |
| 2 | `com.cobalt.aset` restarts at 16:05 or later; `com.cobalt.radar` only inside the 20:00–21:00 pause, never in aftermarket. | plan §6 STEP-D1/D2 and rollback |
| 3 | ESCALATE 1 closed (PNGs = design of record). ESCALATE 2 closed (S2-P4 carries the rank-metric value column; P2 may carry it if it ships first). | plan §1 F9, §7, §8 |

## Brief vs. repo — corrections found (verified read-only)

| Brief said | Repo shows | Evidence |
|---|---|---|
| Rank-metric value can be shown | Not persisted. `radar_membership` has `last_rank` only; values live only in the gitignored 7-day CSV cache. | information_schema; `configs/cobalt/radar.yaml` |
| Honour `radar.board_refresh` as a tunable | Key does not exist; it is a tribunal value still to rule. The plan reads `radar.scan_interval` (100 s). | `tunables.yaml:400`; tribunal `fable/OPEN.md:17` |
| Mock has `prototype.html` | Absent, and `source/` is empty; PNGs only | `ls` of the mock folder |
| Card table "will exist at P2" | `"user".aset_sizings` already carries `origin` and `pool_member_id`: 119 manual, 0 radar cards. No dot, health or WHY columns. | query |
| ASET "Flask" sheet | FastAPI on :5010 | `aset/web.py:31,73` |
| S2-P1 live facts | Confirmed: `primary` scanning, cap 50, members 50, rth rvol / premarket volume, stickiness 3, scan interval 100 s; 676 membership rows today, 172 left | `radar_pool`, `trader_settings`, `tunables.yaml` |

## Evidence commands (read-only)

- `COBALT_ENV=production uv run cobalt db query --side system --prod` on the information_schema columns for `radar_pool`/`radar_membership`, the `radar_pool` row, today's membership sample and counts.
- `… --side user --prod`: `aset_sizings` origin/state counts and columns; `trader_settings` `radar.pool`; `trade_defs` columns.
- `uv run cobalt jobs restarts 7bca336..3a5fc1a`: docs/tests rows derive no resident; `src/cobalt/cli.py` reaches `com.cobalt.radar`.

## Cost

This session: Opus 5, plan only. Build: Sol high, ~2 h, Codex weekly meter (probe before launch).

RESTARTS: none
