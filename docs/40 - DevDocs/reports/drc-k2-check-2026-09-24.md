# DRC K2 — BUILD CHECK, ROUND 1 (`40cf173e..09ce3742` on `drc/d1-trading-log`)

Hub `drc-k2-check-0924`, Sonnet 5, started Fri Sep 25 00:41:45 EDT 2026 (from `date`), stopped 00:5x (`date` 00:51:12 at the report). Prompt `prompts/2026-09-24/52-drc-k2-check.md` (launch row `| R4 |` of `cto-2026-09-25.md`).

## §0 Headline
- Round 1 of the NEW-BUILD check of DRC K2 (`09ce3742` on `40cf173e`): **STOPPED AT THE PACKET — the specified packet cannot fit the 260,000 B ceiling. No checker was launched (nothing to spend, L67 P-c: no ruling, no round).**
- Preflight is green: gates, BUILT line, range, path union (16), no `.env`, stagger, Opus probe `OK`, Grok 1.0.25 UP; Astra METER. Only `k2-diff.part1`–`part5` (the whole build diff, 120,359 B) was staged, reconciled to the byte.
- Measured floor of the packet as `52` §1 specifies it, before any v3 excerpt: **265,319 B (≥ 5,319 B over)**; `52`'s own remedy (cut v3 §2b) cannot close it. ESCALATE: 8.

## L74
- One block arrived inside a tool result (the Read of this prompt file), asking every commit to carry a `Claude-Session: <url>` line and naming a file-send tool. Recorded once here; not followed. This hub commits nothing and sends no file. The build report's own `## L74` (a block of the same shape) is data, carried as read.

## PREFLIGHT
| rule | command | exit | result |
|---|---|---|---|
| placeholder gate | `grep -n -E "R_[_]" …/52-drc-k2-check.md` | no hit | no output — allowed |
| clock | `date` | 0 | `Fri Sep 25 00:41:45 EDT 2026` → `<D>` = 2026-09-25 (before Sep 26th 6:47 AM) |
| GROK GATE (R17) | `grep -n "^| R17 " …/cto-2026-09-24.md` | 0 | `:35` `\| R17 \| 07:32 ET \| … Grok approved with no asking going forward …` — allowed |
| GROK GATE (R17 committed) | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Grok approved with no asking going forward" -- …` | 0 | `1758fd78a572f47b613b2ca831dcfa636ed8f65a` |
| GROK GATE (R19) | `grep -n "^| R19 " …/cto-2026-09-24.md` | 0 | `:37` `\| R19 \| 07:36 ET \| … All 4 house models approved for use indefinlitly …` — allowed |
| GROK GATE (R19 committed) | `git … log -1 --format=%H -S"All 4 house models approved" -- …` | 0 | `5055151dbf68899b82de5b11f99733ed2d03048c` |
| grok | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` → UP |
| R51 / R52 | `grep -n "^| R51 " …` / `"^| R52 "` | 0 | `:69` R51 `**R2-1 RULED "A"**` (one row); `:70` R52 `**R2-2 RULED "B" ON BOTH HALVES**` (one row) |
| R51 / R52 committed | `git … log -1 --format=%H -S"R2-1 RULED" -- …/cto-2026-09-24.md` | 0 | `4d4e844581c5a8cfb3dc4549d5f09e0d9b32ce20` |
| THIS launch | `grep -n "52-drc-k2-check.md" …/cto-2026-09-25.md` | 0 | `:10` R2 (names `67`'s launch and this file as `67`'s stagger text — not the launch), `:12` `\| R4 \| 00:41 ET \| … DESK LAUNCH ROW for 52-drc-k2-check.md …` (counts), `:28` HANDOVER text |
| R4 committed | `git … log -1 --format=%H -S"52-drc-k2-check.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` | 0 | `e19a85c59ac2bc17b4e84f0631f3cba88207d864` |
| worktree | `ls /Users/cobalt/cobalt-wt/drc-d1` | 0 | listed (`AGENTS.md`, `CLAUDE.md`, `configs`, `docs`, `src`, `tests`, `uv.lock` …) |
| THE BUILT LINE | `tail -n 3 "…/drc-k2-build-2026-09-24.md"` | 0 | last non-blank line: `DRC K2 BUILT 09ce3742 \| on 40cf173e \| migration none \| X: 5 of 6 run, design-changing: 0 \| offline 2463/0 \| with-DB 2902/0 \| live-note 142/0 \| .env: removed \| 0018: rolled back \| RESTARTS: com.cobalt.radar \| tests added: 56 \| ESCALATE: 11` — starts `DRC K2 BUILT `, carries `\| migration none`, `\| X: `, `\| offline `, `\| with-DB `, `\| live-note `, `\| .env: removed`, `\| RESTARTS: `. `<tip>` = `09ce3742`, `<base>` = `40cf173e`. Not `0018: UNPROVEN`; `design-changing: 0`. |
| tip subject | `git … log --oneline -1 09ce3742` | 0 | `09ce3742 feat(drc): K2 — no-trade carry, forward re-pair, R51 rebuild from the earlier close, resolve, the [F-06] reader (v3 §6 K2, R51, R52)` |
| range | `git … log --oneline 40cf173e..09ce3742` | 0 | SEVEN: `09ce3742 feat(drc): K2 …` · `8509a5f9 wip(k2): … red tests, with-DB …` · `82e395b1 wip(k2): … red tests, offline …` · `b505dcb1 wip(k2): … first-gate experiments X7–X11 …` · `a8bfcdab docs(k1-fix-r2): … build report — abfaf290` · `abfaf290 docs(k1-fix-r2): … seam (7) and FOR K2 cite file:line …` · `33557098 docs(k1-fix-r1): … build report — 40cf173e`. Non-docs commits: FOUR. |
| nothing above the tip in code | `git … log --oneline 09ce3742..drc/d1-trading-log -- src tests configs` | 0 | EMPTY |
| path union | `git … log --stat --format=%h 40cf173e..09ce3742` | 0 | SIXTEEN (see `## Scope`) — exactly `51`'s S6 list |
| `.env` | `ls /Users/cobalt/cobalt-wt/drc-d1/.env` | 1 | `No such file or directory` (never read) |
| recovery | `ls scratch/tribunal-bars-0920/drc-check/k2` | 1 | `No such file or directory` → fresh run |
| STAGGER | `grep -n -F "no other house hub is running" …/cto-2026-09-25.md` | 0 | prints `:10` (names `67-handicap-h1-fix-r2-check.md`) and `:12` R4, which also names `52-drc-k2-check.md` — satisfied |
| PROBE Opus | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (background) | 0 | `OK` → UP. The saved output carried one harness notice line before it: `Permission deny rule (../../cobalt/.claude/settings.local.json): Bash(git push*:*) mixes * with the trailing :* prefix syntax …` (a notice; no denial of any call of this hub) |
| PROBE Grok | its `--version` row | — | UP |
| ASTRA | keyed on `date` (2026-09-25 00:41 ET, before 2026-09-26 06:47 ET) | — | recorded `astra: METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — not probed, not launched |
| FAIL CLOSED (checkers) | Opus UP and Grok UP | — | two UP — the checker floor was met; the stop is the packet (below), not the seats |
| GROK GATE, second row | (immediately before the checkers launch) | — | NOT REACHED — no checker was launched |

## Packet
Staged in `scratch/tribunal-bars-0920/drc-check/k2/` (no `mkdir`; the Write tool created the folder). Sources read from the saved `git log -p` outputs and from `/Users/cobalt/cobalt-wt/drc-d1/`.

| file | bytes | status |
|---|---|---|
| `k2-diff.part1.md` | 18,540 | staged — saved-output lines 1-429 (cli, models, pairing) |
| `k2-diff.part2.md` | 34,982 | staged — lines 430-1187 (`store.py`) |
| `k2-diff.part3.md` | 26,506 | staged — lines 1188-1784 (K1 test edits, experiments edit, `test_drc_store.py`, `8509a5f9` = `test_drc_k2_store.py`) |
| `k2-diff.part4.md` | 12,358 | staged — lines 1785-2092 (`82e395b1` = `test_drc_k2.py`) |
| `k2-diff.part5.md` | 27,973 | staged — lines 2093-2708 (`b505dcb1` = `test_drc_k2_experiments.py`) |
| **k2-diff total** | **120,359** | |

Staging checks for the diff (each from a tool result):
- Parts each ≤ 38,000 B (largest 34,982). `grep -c "^commit "` over the five parts = 1 + 0 + 1 + 1 + 1 = **4** = PREFLIGHT's non-docs commit count; `grep -c "^diff --git"` = 3 + 1 + 5 + 1 + 1 = **11** = the non-docs path-touches `--stat` shows (feat commit 8, `8509a5f9` 1, `82e395b1` 1, `b505dcb1` 1).
- Byte reconciliation: the saved output was 119,094 B; its last two lines (a blank and `[exited with code 0]`, 22 B) are the harness's and were not staged. The five headers are 189 + 228 + 302 + 229 + 339 = 1,287 B (each read by `grep -b` on the part file); 120,359 − 1,287 = 119,072 = 119,094 − 22 (exact).
- Trailing whitespace: the saved output has 70 lines ending in a space (line numbers listed by `grep -n " $"`); the five copies hold 35 + 15 + 18 + 1 + 1 = 70, each at the line the original has it (part offset checked line by line). No tab-ending line.

**THE CEILING (`52` §1, HONEST SIZE): NOT STAGED BEYOND THE DIFF — the specified packet does not fit.** Every other file's size below is the byte count of its SOURCE (offsets from `grep -b -n "^## "` on the build report and on `51`, `wc -c` on the code files, offsets of the desk file's rows and the prompt's `QUESTIONS` block), taken BEFORE staging it, because the total crosses the ceiling before `rules.md` is reached:

| file (`52` §1) | source bytes | how measured |
|---|---|---|
| `k2-diff.part1`–`part5` | 120,359 | staged, `wc -c` |
| `code-at-tip.md`: `store.py` from `def record_day` (`:281`) | 36,399 | `wc -c` 47,383 − `grep -b` offset 10,984 |
| … `pairing.py` `def _trade` (`:160`) to the end of `build_day` | 15,038 | offset of `__all__` (`:519`) 20,976 − offset of `def _trade` 5,936, − 2 blank lines |
| … `models.py` from `class OpenPosition` (`:125`) to the end | 12,006 | 16,081 − 4,075 |
| … `cli.py` whole | 8,958 | `wc -c` |
| … `0018_drc_stated_books.sql` whole | 3,353 | `wc -c` |
| … `0016_drc.sql:40-95` | not measured (≈ 2.9 KB; excluded from the floor) | |
| `devdocs-diff.md` | 8,645 | saved output 8,667 − 22 |
| `experiments.md` (`## S2 EXPERIMENTS X7–X12` whole) | 3,959 | offsets 7,254 → 11,213 |
| `suites.md` (S1 827 · S3 1,270 · S4 1,525 · S7 543 · S8 790 · S9 2,962 · RESTARTS 1,382) | 9,299 | offsets; the four deselect ids and their `file:line` (≈ 1 KB) excluded from the floor |
| `build-proof.md` (PREFLIGHT 3,170 · S5 3,555 · S6 2,086 · SEAM FOR D2 4,000 · FOR K3 1,538 · ESCALATE + stop line 5,419) | 19,768 | offsets |
| … `SUPERSEDED BY THE ABOVE` (fix r1 report's `## SEAM FOR D2` 3,032 + `## FOR K2` 2,535) | 5,567 | offsets |
| `rules.md`, the desk rows R51 1,135 + R52 1,275 + R77 1,803 | 4,213 | offsets of `:69`, `:70`, `:71`, `:95`, `:96` in the desk file |
| … `51`'s `## THE K2 CONTRACT` | 11,755 | offsets 36,144 → 47,899 |
| … v3 excerpts (`:84-104` §2b, `:124-143`, `:165-192`, `:208-232`, `:243-258`, `:319-328`) | not measured (≈ 120 of the file's 365 lines; the file is 73,788 B, ≈ 200 B a line ⇒ ≈ 24 KB, ≈ 17 KB without §2b) | EXCLUDED from the floor |
| `QUESTIONS-DRC-K2.md` (the block, without the appended "Files in this folder:" paragraph) | 6,078 (taken as 6,000 in the floor) | offsets of `:85` and `:108` of `52` |

**FLOOR** = 120,359 + 36,399 + 15,038 + 12,006 + 8,958 + 3,353 + 8,645 + 3,959 + 9,299 + 19,768 + 5,567 + 4,213 + 11,755 + 6,000 = **265,319 B**, counting NO v3 excerpt, NO per-file header line, NO deselect list, NO `0016` range and NO appended paragraph. That is **≥ 5,319 B over the 260,000 B ceiling**.
- `52`'s remedy is "cut `rules.md`'s §2b first (say so), then `FAILED` if still over": §2b (`:84-104`, 21 of the excerpt lines) is inside the EXCLUDED v3 excerpts, so cutting it moves the floor by nothing. Cutting every v3 excerpt is not what `52` allows (the QUESTIONS block reads §3, §4, §6 and "Before K2" through `rules.md`).
- With the packet as `52` specifies it (v3 minus §2b, headers, deselect list, `0016` range, paragraph) the total is ESTIMATED at ≈ 290 KB, ≈ 30 KB over. That figure is an estimate; the floor above is measured.

Launches: NONE. No `opus-check.md`, no `grok-check.md`; nothing in `drc-d1` was touched (the hub ran `ls`, `grep`, `wc`, `tail`, `date`, `git log`, the Opus probe and read files only).

## CONTINUE
next: on a relaunch of this file, `ls scratch/tribunal-bars-0920/drc-check/k2` shows `k2-diff.part1.md` … `part5.md` (staged and reconciled; not re-staged, L60). What is left to stage, in `52` §1's order: `code-at-tip.md`, `devdocs-diff.md` (its saved output is in this hub's tasks folder only — re-run `git log -p 40cf173e..09ce3742 -- "docs/40 - DevDocs/cobalt"`), `experiments.md`, `suites.md`, `build-proof.md`, `rules.md`, `QUESTIONS-DRC-K2.md`. Then the launches. BLOCKED on the desk's answer to `ASK DESK` under `## ESCALATE`.

## Per row
not reached — no checker was launched (FAILED at the packet).

## Experiments
not reached — no checker was launched. The build's own X7–X12 table stands in its report (`## S2`, offsets 7,254 → 11,213; X7–X11 carry a GATE and a post-code result each; X12 `NOT RUN — hub-run read`).

## Scope
- Checkers' THIRD answers: none (no checker).
- My PREFLIGHT path-union facts (`git log --stat --format=%h 40cf173e..09ce3742`, read from the tool result): the union is SIXTEEN paths — `src/cobalt/drc/{cli,models,pairing,store}.py`; `tests/cobalt/test_drc_k1.py`, `test_drc_k1_store.py`, `test_drc_k2_experiments.py` (new at `b505dcb1`, edited at `09ce3742`), `test_drc_store.py`, `test_drc_k2_store.py` (new, `8509a5f9`), `test_drc_k2.py` (new, `82e395b1`); `docs/40 - DevDocs/cobalt/drc/{cli,models,pairing,store}.md`; `docs/40 - DevDocs/reports/drc-k1-fix-r1-build-2026-09-24.md` (`33557098`, `abfaf290`) and `docs/40 - DevDocs/reports/drc-k1-fix-r2-build-2026-09-24.md` (`a8bfcdab`). This is exactly `51`'s S6 list. No `db_migrations/` path, no `configs/`, no `aset/`, `prefill/`, `replay/`, `vaultwrite/`, no `src/cobalt/cli.py`. Nothing outside the list to record.

## Suites
- Checkers' FOURTH answers: none (no checker).
- Myself, from the build report `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k2-build-2026-09-24.md` (read whole; not the packet copy — none was staged):
  - **live-note (S7)**, on `09ce3742`: `142 passed, 1 skipped, 15 warnings in 9.77s` (exit 0) → 0 failed, 0 errors. The one SKIPPED line is `tests/cobalt/test_replay_line.py:256: requires_vault: COBALT_TEST_LIVE_DRC …`; no line names `COBALT_LIVE_VAULT_ROOT`.
  - **offline (S8)**, on `09ce3742`: `2463 passed, 449 skipped, 1 xfailed, 15 warnings in 69.15s (0:01:09)` (exit 0) → 0 failed, 0 errors. `.env` absent before the run (`No such file or directory`).
  - **with-DB (S9)**, `cobalt_dev`: `2902 passed, 6 skipped, 4 deselected, 1 xfailed, 15 warnings in 149.32s (0:02:29)` (exit 0) → 0 failed, 0 errors. Three `--deselect` arguments select four tests; the report lists them: `test_tenancy.py:697`, `:710`, `:263`, `test_migrate_proof.py:306`. Absence probe (c2): `1 failed in 5.64s`, `assert 28 == 32` — "short by EXACTLY 4". Lock steps (a)–(d) written; `.env: removed, proven gone` is written for S2, S4 AND S9 (also S5's extra take). The closing line: `0016 + 0018: rolled back — applied only inside the suite's transaction; absent on cobalt_dev (probe short by 4)`.
  - I did NOT re-take the four `grep -n` of the deselect ids (they are the packet's DESELECTS step, not reached).

## Seam
- Checkers' FIFTH / SIXTH answers: none (no checker).
- Myself: the build's `## SEAM FOR D2` (`:209`) and `## FOR K3` (`:231`) exist in the report; not walked against the code (that is the checkers' step, not reached).

## Checked against the branch
No claim to walk (no checker). The facts I took myself, each its own call:
- (i) `grep -rn "drc_stated_books" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt` → hits placed: `drc/store.py` (`:25`, `:27` docstring; `:97` `TABLES`; `:109`, `:405`, `:728`, `:776`, `:1013`, `:1018` reads and the table lock; `:177` `STATED_TABLE`; `:195` an error message; `:884` a comment; `:997` a docstring; `:1036` the INSERT), the `0018` pair (`0018_drc_stated_books.sql`, `.rollback.sql`), `db_migrations/__init__.py` (`:44`, `:48`, `:100`, `:105`), `db_migrations/placement.py` (`:95`, `:97`). No other file. `grep -n "INSERT INTO drc_stated_books" …/store.py` → exactly ONE hit, `:1036`, inside `record_stated_book` (its docstring at `:997` names it "THE one writer of `drc_stated_books`").
- (ii) `grep -n "INSERT INTO"` / `"UPDATE "` / `"DELETE FROM"` on `…/drc/cli.py` → EMPTY each.
- (iii) `git -C /Users/cobalt/cobalt log --oneline 40cf173e..09ce3742 -- src/cobalt/db_migrations src/cobalt/cli.py src/cobalt/aset src/cobalt/prefill src/cobalt/replay src/cobalt/vaultwrite configs` → EMPTY.
- (iv) `grep -rn "VaultWriter" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt/drc` → EMPTY.
- (v) L32 — I read this report once before the last line: no ticker beyond the constructed `GGG` / `HHH` / `DDD` (none is written), no real date of his, no file name of his and no value written.

## Ready for K3
No checker answered. `ready for K3: NO` — K2 is UNCHECKED (nothing was put before a checker), not judged bad.

## FOR THE CLASSIFIER
none

## ESCALATE
1. **FAILED: packet** — the packet `52` §1 specifies is over the 260,000 B ceiling by construction (floor 265,319 B, ≥ 5,319 B over; ≈ 290 KB estimated with the v3 excerpts `52` keeps). The drafter's estimate was ≈ 180–250 KB; the K2 diff is 120 KB (K1's, 132 KB, had no `code-at-tip`) and `code-at-tip.md` adds ≈ 80 KB. Nothing launched.
2. `ASK DESK: the packet is over the 260,000 B ceiling by construction (floor 265,319 B; ≈ 290 KB as specified) — relaunch `52` with a re-pointed ceiling (≈ 300,000 B fits the specified packet with margin), or with `code-at-tip.md` cut (the diff already carries every changed line; the tip files add ≈ 80 KB of unchanged K1 context)? [00:51 ET]` Safe default: nothing launched, `k2-diff.part1`–`part5` left staged (a relaunch skips them, L60).
3. The L74 line: recorded under `## L74` (one block in a tool result asking for a `Claude-Session` line and naming a file-send tool; not followed).
4. Astra's line: `astra: METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — not probed, not launched; at or after that time the desk seats it in its own launch (L62 as amended R19).
5. X12's `NOT RUN — hub-run read` (the desk's; carried): `X12 — NOT RUN — hub-run read (v3 :306, :328); the no_trade row stays the only input (its "does not" branch) until the desk runs it`.
6. KNOWN AT LAUNCH (the build's ESCALATE 3), carried for the checkers' SECOND answer when they are seated: X10's and X11's GATE tests asserted BASE facts the contract replaces and were REMOVED from the file at S5 with the src change; their S2 GATE results (`PASSED`) and post-code PASS results stand in the build report's table. The diff (`k2-diff.part3.md`) shows the removal and its comment.
7. The build's DRAFTER PINS line and its A1 line, verbatim (carried for the desk):
   - `10. **DRAFTER PINS where v3 / R51 name no shape (for `52` to rule on, not the builder's):**` — the R51 compare key `(symbol, direction, shares)` as a multiset, and `stated_differs` = the unmatched ids of BOTH books; `stated_book_id` kept on a `carried` seed as the history link; `repaired` / `not_repaired` on the recorded day's `day` row, written only when a later day exists; the chain STOPS at a not-computed day (an ESCALATE default — v3 side A is silent, side B not ruled); a `failed` current import FAILS the re-pair (v3 `[F-29]` B's clause, A1 silent); a `partial` current import → `not_computed.pairing` = its stored `reason`; a resolve applies on its own day only, is superseded by an export touching its symbol that day or by an earlier export's close, and FAILS otherwise (`rebuild <R>`, `names <trade_id> … does not hold`); the resolved trade's exit leg only with price AND time; the CLI's rebuild after `--apply` (C7); `FN_VERSION` `drc.pairing/3`.
   - `11. **A1 is a DESK READING of R51 (the row says so), not his word; a wording he wants otherwise is one message (R51).**`
8. Standing lines:
   - **"Round 1 covers DRC K2 whole (`<base>..<tip>`), its X7–X12 outputs and its three suites' executed output, checked by Opus 5.5 + Grok under his R95 (a new build: Fable-seat as Opus 5.5 per 09-22 R109; Astra METER until Sep 26th, 2026 6:47 AM, seated by the desk from its return; Gemini out, R96/R97). Two seated houses `ready for K3: YES` with `defects that HOLD: 0` → K2 is checked by the floor L67 allows while Astra is METER (the shrink-to-two clause), and K3's drafter may start; Astra's read of K1 and K2 is recorded when it returns. A HOLD → a fix round classified first (L75), ≤3 rounds (L39)."** — THIS ROUND HAS NOT RUN (packet stop); no round is spent (L67 P-c).
   - **"The deploy's L68 gate re-proves offline, with-DB and the live-note suite on the stacked tree that ships (D1 + K1 + K2 + D4 + D2 + D3); the deploy prompt gets its own house read (L67, R95 seats)."**

FAILED: packet — 265,319 B is the measured floor of the specified packet (no v3 excerpt, no headers), ≥ 5,319 B over the 260,000 B ceiling; cutting v3 §2b cannot close it; no checker launched
