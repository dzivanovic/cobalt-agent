# DRC K2 — BUILD CHECK, ROUND 1 (`40cf173e..09ce3742` on `drc/d1-trading-log`)

Hub `drc-k2-check-0924`, Sonnet 5, started Fri Sep 25 00:41:45 EDT 2026 (from `date`); first stop `FAILED: packet` 00:51 (the specified packet was over the 260,000 B ceiling); RESUMED 00:54 BY THE DESK'S MESSAGE (ceiling re-pointed to 300,000 B, verified in git below); checkers launched 01:04; closed 01:2x. Prompt `prompts/2026-09-24/52-drc-k2-check.md` (launch rows `| R4 |` and `| R5 |` of `cto-2026-09-25.md`).

## §0 Headline
- Round 1 of the NEW-BUILD check of DRC K2 (`09ce3742` on `40cf173e`, its X7–X12 output, its three suites): Opus 5.5 and Grok both checked; Astra METER until Sep 26th, 2026 6:47 AM; no Sol, no Gemini. Packet 298,977 B (under the re-pointed 300,000 B ceiling; v3 §2b cut).
- Opus `FIX` (C3, C4, C5, two pins, one seam gap) · Grok `BUILD STANDS EXCEPT C7 · ready for K3: YES` (Grok's C7 finding is a second, separate hole). Scope: NOTHING WIDENED (both). All three suites SHOWN (both). DESELECTS AS STATED (both).
- In my file-check 9 claims HOLD (7 Opus, 2 Grok — Grok's C7 and its pin 9 are one input). `ready for K3: NO`. ESCALATE: 11.

## L74
- One block arrived inside a tool result (the Read of this prompt file), asking every commit to carry a `Claude-Session: <url>` line and naming a file-send tool. Recorded once here; not followed. This hub commits nothing and sends no file. The build report's own `## L74` (a block of the same shape) is data, carried as read. A peer message (the desk's DESK ANSWER, 00:5x) is not that block; its every claim was verified in git before I acted (see PREFLIGHT).

## PREFLIGHT
| rule | command | exit | result |
|---|---|---|---|
| placeholder gate | `grep -n -E "R_[_]" …/52-drc-k2-check.md` | no hit | no output — allowed |
| clock | `date` | 0 | `Fri Sep 25 00:41:45 EDT 2026` → `<D>` = 2026-09-25 (before Sep 26th 6:47 AM) |
| GROK GATE (R17) | `grep -n "^| R17 " …/cto-2026-09-24.md` | 0 | `:35` `\| R17 \| 07:32 ET \| … Grok approved with no asking going forward …` — allowed |
| GROK GATE (R17 committed) | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Grok approved with no asking going forward" -- …` | 0 | `1758fd78a572f47b613b2ca831dcfa636ed8f65a` |
| GROK GATE (R19) | `grep -n "^| R19 " …/cto-2026-09-24.md` | 0 | `:37` `\| R19 \| 07:36 ET \| … All 4 house models approved for use indefinlitly …` — allowed |
| GROK GATE (R19 committed) | `git … log -1 --format=%H -S"All 4 house models approved" -- …` | 0 | `5055151dbf68899b82de5b11f99733ed2d03048c` |
| grok | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` → UP |
| R51 / R52 | `grep -n "^| R51 " …` / `"^| R52 "` | 0 | `:69` R51 `**R2-1 RULED "A"**` (one row); `:70` R52 `**R2-2 RULED "B" ON BOTH HALVES**` (one row) |
| R51 / R52 committed | `git … log -1 --format=%H -S"R2-1 RULED" -- …/cto-2026-09-24.md` | 0 | `4d4e844581c5a8cfb3dc4549d5f09e0d9b32ce20` |
| THIS launch | `grep -n "52-drc-k2-check.md" …/cto-2026-09-25.md` | 0 | `:10` R2 (`67`'s launch row, names this file only as `67`'s stagger text), `:12` `\| R4 \| 00:41 ET \| … DESK LAUNCH ROW for 52-drc-k2-check.md …` (counts), `:28` HANDOVER text |
| R4 committed | `git … log -1 --format=%H -S"52-drc-k2-check.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` | 0 | `e19a85c59ac2bc17b4e84f0631f3cba88207d864` |
| worktree | `ls /Users/cobalt/cobalt-wt/drc-d1` | 0 | listed (`AGENTS.md`, `CLAUDE.md`, `configs`, `docs`, `src`, `tests`, `uv.lock` …) |
| THE BUILT LINE | `tail -n 3 "…/drc-k2-build-2026-09-24.md"` | 0 | last non-blank line: `DRC K2 BUILT 09ce3742 \| on 40cf173e \| migration none \| X: 5 of 6 run, design-changing: 0 \| offline 2463/0 \| with-DB 2902/0 \| live-note 142/0 \| .env: removed \| 0018: rolled back \| RESTARTS: com.cobalt.radar \| tests added: 56 \| ESCALATE: 11` — starts `DRC K2 BUILT `, carries `\| migration none`, `\| X: `, `\| offline `, `\| with-DB `, `\| live-note `, `\| .env: removed`, `\| RESTARTS: `. `<tip>` = `09ce3742`, `<base>` = `40cf173e`. Not `0018: UNPROVEN`; `design-changing: 0`. |
| tip subject | `git … log --oneline -1 09ce3742` | 0 | `09ce3742 feat(drc): K2 — no-trade carry, forward re-pair, R51 rebuild from the earlier close, resolve, the [F-06] reader (v3 §6 K2, R51, R52)` |
| range | `git … log --oneline 40cf173e..09ce3742` | 0 | SEVEN: `09ce3742 feat(drc): K2 …` · `8509a5f9 wip(k2): … red tests, with-DB …` · `82e395b1 wip(k2): … red tests, offline …` · `b505dcb1 wip(k2): … first-gate experiments X7–X11 …` · `a8bfcdab docs(k1-fix-r2): … build report — abfaf290` · `abfaf290 docs(k1-fix-r2): … seam (7) and FOR K2 cite file:line …` · `33557098 docs(k1-fix-r1): … build report — 40cf173e`. Non-docs commits: FOUR. |
| nothing above the tip in code | `git … log --oneline 09ce3742..drc/d1-trading-log -- src tests configs` | 0 | EMPTY |
| path union | `git … log --stat --format=%h 40cf173e..09ce3742` | 0 | SIXTEEN (see `## Scope`) — exactly `51`'s S6 list |
| `.env` | `ls /Users/cobalt/cobalt-wt/drc-d1/.env` | 1 | `No such file or directory` (never read) |
| recovery | `ls scratch/tribunal-bars-0920/drc-check/k2` | 1 | `No such file or directory` → fresh run |
| STAGGER | `grep -n -F "no other house hub is running" …/cto-2026-09-25.md` | 0 | prints `:10` (names `67-handicap-h1-fix-r2-check.md`) and `:12` R4, which also names `52-drc-k2-check.md` — satisfied |
| PROBE Opus | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (background) | 0 | `OK` → UP. The saved output carried one harness notice line before it (`Permission deny rule (../../cobalt/.claude/settings.local.json): Bash(git push*:*) mixes * with the trailing :* prefix syntax …`) — a notice; no denial of any call of this hub |
| PROBE Grok | its `--version` row | — | UP |
| ASTRA | keyed on `date` (2026-09-25, before 2026-09-26 06:47 ET) | — | recorded `astra: METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — not probed, not launched |
| FAIL CLOSED (checkers) | Opus UP and Grok UP | — | two UP → checkers launched |
| FIRST STOP | `wc` / `grep -b` of every source before staging | — | `FAILED: packet` at 00:51 — floor 265,319 B over the 260,000 B ceiling; report committed by the desk in that state |
| RESUME — the desk's ceiling re-point (verified, not believed) | `grep -n "300,000 B" …/52-drc-k2-check.md` | 0 | `:83` the HONEST SIZE line now carries `CEILING 300,000 B (re-pointed by the desk 2026-09-25 00:5x, cto-2026-09-25.md R5 …)`; `git … log -1 --format=%H -S"CEILING 300,000 B" -- …/52-drc-k2-check.md` → `bdcd2eb5096ed27fb3344d7db29dd7912ff27b36`; `grep -n "^| R5 " …/cto-2026-09-25.md` → `:13` R5 (00:53, `DESK RECORD + DESK READING`, ceiling → 300,000 B); `git … log -1 --format=%H -S"\| R5 \|" -- …/cto-2026-09-25.md` → `bdcd2eb5096ed27fb3344d7db29dd7912ff27b36` |
| GROK GATE, second row (before launch) | R17 and R19 rows and both `git -S` commits, re-run at 01:04:02 | 0 | same rows (`:35`, `:37`), same hashes `1758fd78…` and `5055151d…` |

## Packet
Staged in `scratch/tribunal-bars-0920/drc-check/k2/` (no `mkdir`; the Write tool created the folder). Sources read from the saved `git log -p` outputs and from `/Users/cobalt/cobalt-wt/drc-d1/`.

| file | bytes | what |
|---|---|---|
| `k2-diff.part1.md` | 18,540 | `git log -p 40cf173e..09ce3742 -- . ":(exclude)docs"` saved-output lines 1-429 (cli, models, pairing) |
| `k2-diff.part2.md` | 34,982 | lines 430-1187 (`store.py`) |
| `k2-diff.part3.md` | 26,506 | lines 1188-1784 (K1 test edits, experiments edit, `test_drc_store.py`, `8509a5f9` = `test_drc_k2_store.py`) |
| `k2-diff.part4.md` | 12,358 | lines 1785-2092 (`82e395b1` = `test_drc_k2.py`) |
| `k2-diff.part5.md` | 27,973 | lines 2093-2708 (`b505dcb1` = `test_drc_k2_experiments.py`) |
| `code-at-tip.part1.md` | 36,610 | `store.py` :281-1052 |
| `code-at-tip.part2.md` | 27,504 | `pairing.py` :160-516, `models.py` :125-460 |
| `code-at-tip.part3.md` | 15,674 | `cli.py` whole, then under SCHEMA AS K1 LEFT IT: `0018_drc_stated_books.sql` whole, `0016_drc.sql` :40-95 |
| `devdocs-diff.md` | 8,907 | `git log -p 40cf173e..09ce3742 -- "docs/40 - DevDocs/cobalt"` |
| `experiments.md` | 4,199 | build report `## S2 EXPERIMENTS X7–X12` whole |
| `suites.md` | 10,996 | build report S1, S3, S4, S7, S8, S9, RESTARTS; then the four deselect ids with the hub's own `grep -n` hits |
| `build-proof.md` | 26,021 | build report PREFLIGHT, S5, S6, SEAM FOR D2, FOR K3, ESCALATE, stop line; then the K1 fix r1 report's `## SEAM FOR D2` / `## FOR K2` under SUPERSEDED BY THE ABOVE |
| `rules.md` | 41,864 | desk rows R51, R52, R77; v3 :124-143, :165-192, :208-232, :243-258, :319-328; `51`'s `## THE K2 CONTRACT` whole. **v3 §2b (`:84-104`, 5,121 B) CUT** — with it the packet was ≈ 305 KB, over the 300,000 B ceiling; `52` §1's remedy is "cut §2b first" and the total without it is under |
| `QUESTIONS-DRC-K2.md` | 6,843 | the questions verbatim + the "Files in this folder:" paragraph |
| **total** | **298,977** | ÷ 4 = **≈ 74,744 tokens per checker**; under the 300,000 B ceiling (1,023 B margin) |

Written after the checks: `opus-check.md` 10,147 B (by this hub from stdout), `grok-check.md` 14,750 B (by Grok, told to).

Staging checks (each from a tool result):
- **Diff parts** (before the stop): each ≤ 38,000 B (largest 34,982). `grep -c "^commit "` over the five parts = 1 + 0 + 1 + 1 + 1 = **4** = PREFLIGHT's non-docs commit count; `grep -c "^diff --git"` = 3 + 1 + 5 + 1 + 1 = **11** = the non-docs path-touches `--stat` shows. The saved output was 119,094 B; its last two lines (a blank and `[exited with code 0]`, 22 B) are the harness's; the five headers are 189 + 228 + 302 + 229 + 339 = 1,287 B (`grep -b`); 120,359 − 1,287 = 119,072 = 119,094 − 22 (exact). The saved output has 70 lines ending in a space, at lines the copies hold at the same offsets (35 + 15 + 18 + 1 + 1).
- **Code parts:** the sources have 0 lines ending in a space (`grep -c " $"` on all six files). `code-at-tip.part1`: `def record_day` sits at byte offset 211 = the header, and 36,610 − 211 = 36,399 = 47,383 − 10,984 (exact to the end of `store.py`). `code-at-tip.part2`: `pairing.py` segment 414 → 15,452 = 15,038 B = offset of `__all__` − offset of `def _trade` − 2 blank lines; `models.py` segment 15,498 → 27,504 = 12,006 B = 16,081 − 4,075 (exact). `code-at-tip.part3`: `cli.py` segment 468 → 9,426 = 8,958 B = `wc -c` of `cli.py` (exact); `0018` segment 9,473 → 12,897 = header (71) + 3,353 B.
- **`devdocs-diff.md`:** the saved output's 10 lines ending in a space or tab (`--- a/…` / `+++ b/…` path lines end in a tab, one blank line ` `, the 4-space commit line) are present at the same lines (+1 for the header).
- **Report excerpts:** `grep -n -F -x -v -f experiments.md suites.md build-proof.md <the build report>` lists ONLY lines this packet does not carry by design (§0, L74, AUTHORIZATION, CONTINUE) and blanks. The fix r1 report's lines 248-265 are all present in `build-proof.md`; only blank lines 257 and 266 were listed.
- **`rules.md`:** `grep -n -F -x -v -f <v3> -f <desk file> -f <51> rules.md` lists only its own label lines (`===` headers, the top header) and blanks — every content line is present exactly in a source.
- **`QUESTIONS-DRC-K2.md`:** `grep -n -F -x -v -f <the prompt>` lists only the two lines that carried the prompt's opening / closing quote mark (lines 1, 22, the appended paragraph 24 and the blank 23); every other line is present exactly.
- **Deselect ids** (`52` §1 (5)): `grep -n "def test_twice_is_idempotent_and_the_rollback_round_trips"` → `test_tenancy.py:697`; `"def test_the_proof_table_names_every_ruled_table"` → `:710`; `"def test_every_user_table_carries_user_id_not_null_with_the_guc_default"` → `:263`; `"def test_rows_reach_the_probe_through_a_named_cursor_in_batches"` → `test_migrate_proof.py:306`. No line moved.
- **Placement / order:** `code-at-tip` and `k2-diff` are numbered `.part<k>` sets; the QUESTIONS paragraph names each set as one file read in order.

Launches (both `run_in_background`, independent, one attempt per house, told "Do not open any *-check.md file"; Opus first):
- OPUS at 01:04:12 (bg `b23v82mj7`), `claude -p --model claude-opus-5-5 "You are OPUS. The folder is scratch/tribunal-bars-0920/drc-check/k2/ (absolute path …). Start with QUESTIONS-DRC-K2.md and follow it exactly. Files named <name>.part<k>.md are one file read in order. Do not open any *-check.md file. Read files with the Read, Grep and Glob tools only. Write no file. Print your complete check as your answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/k2 --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"`. Answered by 01:11 (≈ 7 min). Its stdout carried two harness notice lines (the `git push*:*` notice and `Warning: no stdin data received in 3s, proceeding without it.`) then the check. `opus-check.md` written by this hub from stdout, byte for byte (`grep -F -x -v -f opus-check.md <stdout>` lists only the two notice lines, blanks and the 22-byte trailer).
- GROK at 01:04:19 (bg `b4dx0n9l5`), `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" -p "You are GROK. The folder is scratch/tribunal-bars-0920/drc-check/k2/ (absolute path …). Start with QUESTIONS-DRC-K2.md and follow it exactly. … Do not open any *-check.md file. Write your complete check to /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/k2/grok-check.md and reply with only that path."` (never `--always-approve`). Its stdout was progress text and the path; its own file was on disk at 01:18 (`ls -la`). Grok's file is recorded as Grok wrote it (14,750 B, ≈ 14 min).
- Clock: 45-minute deadline ≈ 01:49; both inside it; no HARNESS / METER / TIMEOUT.
- Written-nothing proof: `ls -la` of the packet folder (14 files) and of `/Users/cobalt/cobalt-wt/drc-d1` before the Opus launch (01:04:02) and after it were identical. After both finished the packet folder holds exactly the 14 staged files + `opus-check.md` (this hub's) + `grok-check.md` (told to Grok), and `drc-d1` is unchanged (same entries and mtimes, folder mtime 00:37, no `.env`).

## CONTINUE
done — packet staged (298,977 B), both checkers answered, every claim walked; report closed below.

## Per row
`row · opus · grok · MEETS: n of 2` (each DOES NOT MEET / NEW DEFECT in the checker's words, ≤30 words, its `file:line` as cited — Opus's `part` numbers are packet lines, the parenthesised `store.py` numbers the tip).

| row | opus | grok | MEETS |
|---|---|---|---|
| C1 | MEETS THE ROW (validator `models.py:404-426`; notes the carried-book-names-a-statement arm is stricter than C1, ESCALATE 4) | MEETS THE ROW | 2 of 2 |
| C2 | MEETS THE ROW (reads `DayPairing.resolves` as the day's own outcomes, merged at write time by `_outcomes` — "a contract inconsistency, not a defect") | MEETS THE ROW | 2 of 2 |
| C3 | **NEW DEFECT — `code-at-tip.part1.md:500-509` (store.py:779-788) with `:736-754` (store.py:1015-1033):** two current resolves for one trade on different days make `_with_resolves` raise for every day; `record_stated_book` cannot supersede across days; "every `seed_for`/`rebuild` fails permanently" | MEETS THE ROW | 1 of 2 |
| C4 | **DOES NOT MEET — `code-at-tip.part1.md:277-283` (store.py:556-562) with `_carried` `:569-602`:** after a not-computed day stops the chain, "`seed_for` still FAILS loud" only for the FIRST later day; the next day is paired from the stale close. **Second NEW DEFECT — `part1.md:31-39`:** a no-trade day via `record_day` stores `source: carried` with no `no_trade_id`; `rebuild` relabels it `no_trade_carry` | MEETS THE ROW | 1 of 2 |
| C5 | **NEW DEFECT — `code-at-tip.part1.md:210, 241-247` (store.py:489, 520-526) with `part2:343-349` (pairing.py:500-506):** a day stored not computed keeps `not_computed.match = "not computed — pairing did not run"`; once later paired, `kept_match` writes the stats rows back unmatched and the day stores "pairing did not run" | MEETS THE ROW | 1 of 2 |
| C6 | MEETS THE ROW (`store.py:349-367`, R51's wording, read from the stored seed row) | MEETS THE ROW | 2 of 2 |
| C7 | MEETS THE ROW under ESCALATE 5's narrowing; caveat: a resolve dated a day with no import yet, after an earlier recorded day, prints `on --apply: rebuild …` and then `not rebuilt: … nothing to re-pair`, exit 1 — "loud but misleading" | **DOES NOT MEET — `cli.py:135`:** C7 rebuilds for every `no_trade` / `resolve`; `_rebuilds` is false with no import and no `day` row on or before the day, so apply prints `stated; <day> has no import yet`, exit 0, and `rebuild` is not called | 1 of 2 |
| C8 | MEETS THE ROW (deletions `test_drc_k1_store.py:415`, `:481`; pins `test_drc_k1.py:66`, `test_drc_k1_store.py:497`, `test_drc_store.py:403`; nothing loosened) | MEETS THE ROW | 2 of 2 |

Opus's and Grok's "specific checks" agree on: all or nothing (holds), statement kept as history (holds; only INSERT at `store.py:1036`), A1 current fills only (holds). They differ on: A1 "an unchanged day re-pairs identically" (Opus: fails in three cases — the C5 kept-match, the partial pin, the C4 no-trade relabel; Grok: X9 pass, holds), one path L3 (Opus: holds except the no-trade relabel; Grok: holds).

## Experiments
`X · opus · grok` (SECOND, verbatim ≤30 words).

| X | opus | grok |
|---|---|---|
| X7 | RAN — gate PASSED (one transaction) / pass GREEN at S9. The note half is K3's (X13); not shown here. · design-changing: no | RAN — gate PASSED (executemany after the DELETE leaves the day's rows unchanged) / pass GREEN (S9, 0 failed). The note half is K3, not this run. · no |
| X8 | RAN — gate PASSED / pass GREEN at S9. The pass only exercises the `rebuild` path; the `record_day` path stores `carried` (C4). · no (code fix) | RAN — gate PASSED (offline: empty day returns the same trade id on the new day) / pass GREEN · no |
| X9 | RAN — gate PASSED / 5 passes GREEN. For the partial import (FR14), only a `Price`-less file was tested, where both readings agree. · no | RAN — gate PASSED (both imports keep their fills; the current id is the new one) / five passes GREEN · no |
| X10 | RAN — gate PASSED on the base at S2 and was removed / 3 passes GREEN. · no | RAN — gate PASSED at S2 on the base (the K1 raise), then removed with the src / three passes GREEN · no |
| X11 | RAN — gate PASSED on the base (`IndexError`) and was removed / offline and with-DB passes GREEN. · no | RAN — gate PASSED at S2 (`IndexError` on a CLOSED book with no exits), then removed / passes GREEN (S8 offline, S9 with-DB) · no |
| X12 | NOT RUN BY DESIGN — a hub-run read. | NOT RUN BY DESIGN — its row says hub-run read · no |

Grok: "No run result is a result that changes the design." Neither checker marks any result design-changing.

## Scope
- opus · THIRD: `NOTHING WIDENED.` "16 paths changed, 10 of them K2 src, tests or DevDocs, all inside what the contract allows … No migration, no vault path, no K3 / K4 / D2 / D3 unit."
- grok · THIRD: `NOTHING WIDENED.` "The code diff is `drc/cli.py`, `drc/models.py`, `drc/pairing.py`, `drc/store.py`, and the K1/K2 test files the contract names. No migration file. … No vault path … No K3 unit, no K4 widget, no D2 route, no D3 build."
- My PREFLIGHT path-union facts (`git log --stat --format=%h 40cf173e..09ce3742`): SIXTEEN paths — `src/cobalt/drc/{cli,models,pairing,store}.py`; `tests/cobalt/test_drc_k1.py`, `test_drc_k1_store.py`, `test_drc_k2_experiments.py` (new at `b505dcb1`, edited at `09ce3742`), `test_drc_store.py`, `test_drc_k2_store.py` (new, `8509a5f9`), `test_drc_k2.py` (new, `82e395b1`); `docs/40 - DevDocs/cobalt/drc/{cli,models,pairing,store}.md`; `docs/40 - DevDocs/reports/drc-k1-fix-r1-build-2026-09-24.md` (`33557098`, `abfaf290`) and `docs/40 - DevDocs/reports/drc-k1-fix-r2-build-2026-09-24.md` (`a8bfcdab`). Exactly `51`'s S6 list. No `db_migrations/` path, no `configs/`, no `aset/`, `prefill/`, `replay/`, `vaultwrite/`, no `src/cobalt/cli.py`.

## Suites
`suite · opus · grok`.

| suite | opus | grok |
|---|---|---|
| offline | SHOWN — `2463 passed, 449 skipped, 1 xfailed`, 0 failed, `.env` proven absent before the run; arithmetic adds up (2441 + 22; 417 + 34 − 2) | SHOWN — S8 `2463 passed, 449 skipped, 1 xfailed`, 0 failed; `.env` absent before the run |
| with-DB | SHOWN — `2902 passed, 6 skipped, 4 deselected, 1 xfailed`, 0 failed; `.env` removal proven; absence probe `assert 28 == 32`; 2848 + 56 − 2 = 2902 | SHOWN — S9 `2902 passed, 6 skipped, 4 deselected, 1 xfailed`, 0 failed; `.env` removed, a following `ls` shows it gone; probe `assert 28 == 32` |
| live-note | SHOWN — `142 passed, 1 skipped`, exit 0 | SHOWN — S7 `142 passed, 1 skipped`, 0 failed; vault read only |
| deselects | DESELECTS AS STATED — `test_tenancy.py:697`, `:710`, `:263`, `test_migrate_proof.py:306` | DESELECTS AS STATED — three `--deselect` arguments, four tests, lines as `suites.md` lists them |

Myself, from the build report `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k2-build-2026-09-24.md` (read whole, not the packet copy):
- **live-note (S7)**, on `09ce3742`: `142 passed, 1 skipped, 15 warnings in 9.77s` (exit 0) → 0 failed, 0 errors. The one SKIPPED line is `tests/cobalt/test_replay_line.py:256: requires_vault: COBALT_TEST_LIVE_DRC …`; no line names `COBALT_LIVE_VAULT_ROOT`.
- **offline (S8)**: `2463 passed, 449 skipped, 1 xfailed, 15 warnings in 69.15s (0:01:09)` (exit 0) → 0 failed, 0 errors. `.env` absent before the run.
- **with-DB (S9)**, `cobalt_dev`: `2902 passed, 6 skipped, 4 deselected, 1 xfailed, 15 warnings in 149.32s (0:02:29)` (exit 0) → 0 failed, 0 errors. Three `--deselect` arguments select four tests: `test_tenancy.py:697`, `:710`, `:263`, `test_migrate_proof.py:306` (all four confirmed by my own `grep -n` at those lines). Absence probe (c2): `1 failed in 5.64s`, `assert 28 == 32` — "short by EXACTLY 4" (as expected). `.env: removed, proven gone` is written for S2, S4 AND S9 (also for S5's extra take). The closing line: `0016 + 0018: rolled back — applied only inside the suite's transaction; absent on cobalt_dev (probe short by 4)`.

## Seam
- opus · FIFTH: `CONTRACT GAP.` "All seam line numbers match the tip: store.py:281/327/349/696/962/984, pairing.py:283/480, models.py:136/375, cli.py:184. The K3 section says what K3 reads and re-upserts (`derived.repaired`). The gap: `not_repaired` is stored only on the earlier day's `day` row. Neither D2 nor K3 can cite, from day N's (or N+1's) own rows, that its book is stale. SEAM (1) also states that `seed_for` raises on an uncomputed prior, but the stale chain beyond N does not raise (the C4 finding)."
- grok · FIFTH: `CONTRACT STATED.` "Tip lines match the signature list … `build_day`'s def is `pairing.py:479`; the seam's `:480` is the next line, the first parameter. … Point (4) tells the page to call `rebuild` after the statement; the CLI does that only in the ESCALATE 5 cases, and the seam's builder note says so. FOR K3 says what it reads … and what it re-upserts after commit."
- SIXTH, the drafter's pins:

| pin | opus | grok |
|---|---|---|
| 1 compare key multiset `(symbol, direction, shares)`, ids of both books | SOUND | SOUND |
| 2 `stated_book_id` kept on a carried seed | SOUND | SOUND |
| 3 `repaired` / `not_repaired` only when a later day exists | SOUND (as a record; too weak as a marker — see pin 4) | SOUND |
| 4 the chain STOPS at a not-computed day | **UNSOUND** — D3 is carried from D_NEXT's stale close | SOUND (`store.py:556`; later rows unchanged; `seed_for` still fails) |
| 5 a failed current import FAILS the re-pair | SOUND | SOUND |
| 6 a partial current import → `not_computed.pairing` = its stored reason | **UNSOUND** — a partial file missing only a non-pairing column pairs at its first record; any rebuild un-pairs it, drops its `book_close`, stops the chain | SOUND — "It can un-pair a day the first record paired; that is this pin, and the chain stops loud." |
| 7 resolve on its own day only; superseded by an export touching the symbol or an earlier close; FAILS otherwise | SOUND (the cross-day deadlock is C3's) | SOUND |
| 8 exit leg only with price AND time | SOUND | SOUND |
| 9 the CLI's rebuild after `--apply` (C7) | SOUND (with the C7 caveat) | **UNSOUND** — `no_trade` or `resolve` with no import and no `day` row on or before that day: `rebuild` does not run, exit 0 |
| 10 `FN_VERSION` `drc.pairing/3` | SOUND | SOUND |

## Checked against the branch
For EVERY DOES NOT MEET, NEW DEFECT, CONTRACT GAP and UNSOUND I opened the real files (the code under `/Users/cobalt/cobalt-wt/drc-d1/` and, for the line numbers, the tip's own numbering). `claim · who · file:line · verdict · ≤30 words`.

| # | claim | who | file:line | verdict | walked |
|---|---|---|---|---|---|
| 1 | C3 — two current resolves for one trade on different days make every `seed_for` / `rebuild` fail, and none can be superseded across days | opus | `store.py:779-788` (`by_trade` over ALL current resolve rows, no day filter, raise when a trade has >1); `store.py:1015-1033` (`record_stated_book`'s `current` is filtered `AND day = %s AND kind = %s` and, for a resolve, the trade; `:1029` refuses `supersedes` not in it); no retract kind (`_STATED_KINDS`, `:101`); table append-only (`0018` trigger) | HOLDS | A restatement can only supersede a row of its own day / kind / trade; a cross-day pair stays two current rows; the raise is reached whenever `_seed` returns a book (`:765`). `test_two_current_resolves_for_one_trade_fail_naming_both` is at `test_drc_k2_store.py:313` and builds this setup. C3(a)'s text is the raise; the unrecoverability is what is walked |
| 2 | C4 — after a not-computed day stops the chain, only the FIRST later day's `seed_for` fails; the next later day is paired from a stale close, nothing stored on its rows | opus | `store.py:556-561` (stop: later days keep rows, named only in the earlier day's `not_repaired`); `store.py:848-881` (`_carried` reads only the prior day's own `day` row / `book_close` / `open_position` hash — D_NEXT's untouched rows pass) | HOLDS | For D3 (prior = D_NEXT, unrepaired): D_NEXT's `day` row is still computed (`derived.not_computed` has no `pairing`), its `book_close` hash matches its own rows → a book is returned. `seed_for(D_NEXT)` itself fails (prior D not computed, `:852-858`) |
| 3 | C4 (minor) — a no-trade day written through `record_day` stores its seed as `carried` with no `no_trade_id`; `rebuild` relabels it `no_trade_carry` | opus | `store.py:310-318` (`record_day` passes the caller's `seed` to `_commit`); `store.py:662-669` (`seed_inputs["source"] = seed.source`, `no_trade_id` only if `seed.no_trade_id`); `store.py:499-513` (relabel exists only in `_repair`); `test_drc_k2_store.py:207` (`test_a_day_with_no_trading_log_needs_its_no_trade_row` asserts `day_inputs["no_trade_id"]`, not the seed row) | HOLDS | The seed row's `source` differs by path; the next forward re-pair rewrites it. The day row does name `no_trade_id` on both paths |
| 4 | C5 — a day stored not computed keeps `not_computed.match`; once later paired, the stats rows are written back unmatched and the day stores "pairing did not run" | opus | `pairing.py:498-507` (`"match": "not computed — pairing did not run"`, and the stats rows become `unmatched — no trades were paired`); `store.py:471-490` (`kept_match` read from the stored day row), `:520-526` (rows written back unchanged, `not_computed.match = kept_match`) | HOLDS | `_repair` cannot tell "match stored because the file lacked columns" from "match stored because the day was not paired"; both keep. Reached by `rebuild(D)` after he states the book (SEAM (2)) |
| 5 | pin 4 — "the chain STOPS at a not-computed day" is UNSOUND | opus | the input of #2 | HOLDS | Same input; the pin's own text (`## ESCALATE` item 10) is what `:556-561` builds |
| 6 | pin 6 — a partial current import → `not_computed.pairing` = its stored reason is UNSOUND | opus | `store.py:451-460` (any `Outcome.PARTIAL` → `not_computed={"pairing": reason}`); `trading_log.py:170-171` (a first record sets `not_computed.pairing` only for a MISSING pairing column; other partials pair) | HOLDS | Read `trading_log.py:170-171` myself: `absent = [c for c in PAIRING_INPUTS if c in detection.missing]`. A partial file missing only a non-pairing column is paired at its first record and un-paired by every later `_repair`. Grok read the same pin as SOUND ("that is this pin, and the chain stops loud"); the build report's ESCALATE 7 says the same as Opus |
| 7 | CONTRACT GAP — `not_repaired` lives only on the earlier day's `day` row; day N's (or N+1's) own rows carry no stale marker; SEAM (1)'s "raises on an uncomputed prior" does not reach beyond N | opus | `store.py:551-577` (`extra` is built for `pairing.day` only), `:569` (`self._rows(p, ids, s, nt, {})` — each later day's rows written with an EMPTY `extra`) | HOLDS | Fact walked; K3's `## FOR K3` reads `derived.repaired` of the recorded day only. See #2 for the chain beyond N |
| 8 | C7 — no `no_trade` / `resolve` rebuild when there is no current import and no `day` row on or before the day; apply prints `stated; <day> has no import yet`, exit 0 | grok | `cli.py:127-137` (`_rebuilds`), `:171-173` (`if not rebuilds: print(...); return`); `store.py:374-381` (`has_chain_through`); `store.py:396-409` grep: `_seed` never reads `no_trade` rows (`no_trade` appears only at `:312`, `:403-409`, `:420`, `:462`) | HOLDS | Contract C7 (`rules.md`) says rebuild for EVERY `no_trade` / `resolve`; the build report's ESCALATE 5 discloses the narrowing (two K1 CLI tests C8 forbids editing). A departure from the contract's text, disclosed |
| 9 | pin 9 — the CLI's rebuild after `--apply` is UNSOUND (the same input) | grok | as #8 | HOLDS | Same input as #8 |
| — | C7 caveat — a resolve dated a day with no import, after an earlier recorded day, prints `on --apply: rebuild …` in the dry run and then `not rebuilt: … nothing to re-pair`, exit 1 | opus (labelled MEETS THE ROW, a caveat) | `cli.py:127-137`, `:174-181`; `store.py:466-468` | NOT COUNTED — a caveat, not a claim of defect | Walked: `has_chain_through` true, `_repair` raises `no trading-log import and no no-trade DRC — nothing to re-pair` |
| — | C2 reading — `DayPairing.resolves` from `build_day` holds only that day's outcomes; the seed's `resolve_outcomes` are merged at write time by `_outcomes` | opus (labelled "a contract inconsistency, not a defect") | `store.py:127-134` (`_outcomes`), `pairing.py:508-515` | NOT COUNTED — labelled by its author as not a defect | Walked; the C2 signature carries no `SeedBook` |

Where the checkers contradict each other, both quoted and neither smoothed:
- **C3.** opus: `NEW DEFECT … "every seed_for/rebuild fails permanently"` · grok: `C3 — MEETS THE ROW … two current resolves for one trade, any days, fail naming both ids (store.py:782) … two currents` (asserted by the store tests). Grok read the raise as the contract's own rule; Opus walked what follows from it.
- **C4 / pin 4.** opus: `DOES NOT MEET … only holds for the first day after the stop` · grok: `C4 — MEETS THE ROW … A not-computed day stops the chain … every later day is left byte-unchanged and named in not_repaired, and seed_for of such a later day still fails` / pin 4 `SOUND`. Grok's text is about the later day N; the walked input in #2 is the day after N.
- **C5.** opus: `NEW DEFECT … kept_match` · grok: `C5 — MEETS THE ROW … Match set → stored rows written back unchanged (store.py:520)`. Grok describes the branch; Opus walks a stored-`match` that came from an unpaired day.
- **C7.** opus: `MEETS THE ROW` (caveat above) · grok: `DOES NOT MEET — cli.py:135`. Both cite ESCALATE 5's narrowing; they differ on whether the narrowing is a departure.
- **Pin 6.** opus: `UNSOUND` · grok: `SOUND … it can un-pair a day the first record paired`.
- **Seam.** opus: `CONTRACT GAP` · grok: `CONTRACT STATED`.

Also stated, each its own call:
- (i) `grep -rn "drc_stated_books" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt` → hits placed: `drc/store.py` (`:25`, `:27` docstring; `:97` `TABLES`; `:109`, `:405`, `:728`, `:776`, `:1013`, `:1018` reads and the table lock; `:177` `STATED_TABLE`; `:195` an error message; `:884` a comment; `:997` a docstring; `:1036` the INSERT), the `0018` pair (`0018_drc_stated_books.sql`, `.rollback.sql`), `db_migrations/__init__.py` (`:44`, `:48`, `:100`, `:105`), `db_migrations/placement.py` (`:95`, `:97`); no other file. `grep -n "INSERT INTO drc_stated_books" …/store.py` → exactly ONE hit, `:1036`, inside `record_stated_book`.
- (ii) `grep -n "INSERT INTO"` / `"UPDATE "` / `"DELETE FROM"` on `…/drc/cli.py` → EMPTY each.
- (iii) `git -C /Users/cobalt/cobalt log --oneline 40cf173e..09ce3742 -- src/cobalt/db_migrations src/cobalt/cli.py src/cobalt/aset src/cobalt/prefill src/cobalt/replay src/cobalt/vaultwrite configs` → EMPTY.
- (iv) `grep -rn "VaultWriter" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt/drc` → EMPTY.
- (v) L32 — I read this report once before the last line: no ticker beyond the constructed `GGG` / `HHH` / `DDD` (none is written), no real date of his, no file name of his and no value written.

## Ready for K3
- opus · `CHECK DRC K2: FIX C5-kept-match, C4-stop-chain, C3-resolve-deadlock, C4-no-trade-seed · ready for K3: NO · stale chain carries unflagged; stats never re-matched; resolve deadlock unrecoverable` · ready: NO · reason verbatim: "stale chain carries unflagged; stats never re-matched; resolve deadlock unrecoverable"
- grok · `CHECK DRC K2: BUILD STANDS EXCEPT C7 · ready for K3: YES` · ready: YES · reason: (none given beyond the exception `C7`)
- Mine: `ready for K3: NO` — one checker NO and `defects that HOLD` is 9, not 0.

## FOR THE CLASSIFIER
Round 1 of ≤3 — a HOLD goes to a fix round (L75). One item per claim that HOLDS in my file-check, no class and no recommendation:
1. "C3 — NEW DEFECT: two current resolves for one trade on different days make `_with_resolves` raise for every day; `record_stated_book` finds current rows only for the new row's own day, so no restatement retracts one" · opus · row C3 · `store.py:779-788` with `:1015-1033` (packet `code-at-tip.part1.md:500-509`, `:736-754`) · HOLDS
2. "C4 — DOES NOT MEET: after a not-computed day stops the chain, `seed_for` fails only for the first later day; the next later day is paired from a book its root day's new file contradicts, with nothing stored on its rows" · opus · row C4 · `store.py:556-561` with `:848-881` · HOLDS
3. "C4 (second, minor) — NEW DEFECT: a no-trade day written through `record_day` stores its seed as `carried` with no `no_trade_id`; `rebuild` relabels it `no_trade_carry`, so the next forward re-pair changes that day's rows though its inputs did not" · opus · row C4 · `store.py:310-318`, `:662-669`, `:499-513` · HOLDS
4. "C5 — NEW DEFECT: a day stored not computed with a stats log keeps `not_computed.match = "not computed — pairing did not run"`; when it is later paired, `kept_match` writes the stats rows back unmatched and the day stores 'pairing did not run'" · opus · row C5 / A1 · `store.py:471-490`, `:520-526`; `pairing.py:498-507` · HOLDS
5. "SIXTH pin 4 — the chain STOPS at a not-computed day — UNSOUND" · opus · the pins · the input of item 2 · HOLDS
6. "SIXTH pin 6 — a partial current import → `not_computed.pairing` = its stored reason — UNSOUND: a partial file missing only a non-pairing column pairs at its first record and is un-paired by every rebuild / forward re-pair" · opus · the pins / A1 · `store.py:451-460` with `trading_log.py:170-171` · HOLDS
7. "FIFTH — CONTRACT GAP: `not_repaired` is stored only on the earlier day's `day` row; neither D2 nor K3 can cite, from day N's (or N+1's) own rows, that its book is stale" · opus · the seam · `store.py:551-577`, `:569` · HOLDS
8. "C7 — DOES NOT MEET: C7 rebuilds for every `no_trade` / `resolve`; `_rebuilds` is false with no current import and no `day` row on or before that day, so apply prints `stated; <day> has no import yet`, exits 0 and does not call `rebuild`" · grok · row C7 · `cli.py:127-137`, `:171-173` · HOLDS
9. "SIXTH pin 9 — the CLI's rebuild after `--apply` (C7) — UNSOUND: `no_trade` or `resolve` with no current trading-log import and no `day` row on or before that day: `rebuild` does not run, exit is 0" · grok · the pins · `cli.py:135` · HOLDS

## ESCALATE
1. **opus's `FIX`, quoted in full with my file-check verdict beside it:** `CHECK DRC K2: FIX C5-kept-match, C4-stop-chain, C3-resolve-deadlock, C4-no-trade-seed · ready for K3: NO · stale chain carries unflagged; stats never re-matched; resolve deadlock unrecoverable` — claims 1–7 of `## Checked against the branch` HOLD (items 1–7 under `## FOR THE CLASSIFIER`); the two claims labelled "not a defect" by their author (the C2 reading, the C7 caveat) are recorded and NOT counted. Opus's own ESCALATE (its text): "a ruling on moving or retracting a resolve; the partial-import pin".
2. **grok's `BUILD STANDS EXCEPT C7 · ready for K3: YES`, quoted in full with my file-check verdict beside it:** `CHECK DRC K2: BUILD STANDS EXCEPT C7 · ready for K3: YES` — C7 and pin 9 HOLD (items 8, 9); Grok's `MEETS THE ROW` on C3 / C4 / C5 and `SOUND` on pins 4 / 6 and `CONTRACT STATED` contradict Opus's walked inputs (quoted above under `## Checked against the branch`, neither smoothed).
3. **Every item under `## FOR THE CLASSIFIER`, restated by number:** items 1–9 (opus 1–7, grok 8–9). `defects that HOLD: 9`.
4. **A packet note (a stop-and-resume, not a mismatch):** the first run stopped `FAILED: packet` at 00:51 (floor 265,319 B over the 260,000 B ceiling); the desk re-pointed the ceiling to 300,000 B (verified in `52`'s HONEST SIZE line, commit `bdcd2eb5`, launch row R5, both committed) and resumed this hub by message; the packet is 298,977 B, v3 §2b cut per the ceiling rule, `code-at-tip` staged as three `.part<k>` files and named as one file read in order in the QUESTIONS paragraph. The interim `FAILED` report was committed by the desk in its interim state (R5); this report replaces it. No checker was launched before the resume, so no round was spent (L67 P-c).
5. **A checker that did not check / wrote a file it was not told to / every `ASK DESK`:** none (both answered with a `CHECK DRC K2:` line; Grok wrote only `grok-check.md`, as told; Opus wrote nothing). The `ASK DESK` at 00:51 (ceiling) was answered by the desk's message; no other.
6. **The L74 line:** recorded under `## L74`.
7. **Astra's line:** `astra: METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — not probed, not launched; at or after that time the desk seats it in its own launch (L62 as amended R19).
8. **X12's `NOT RUN — hub-run read` (the desk's; carried):** `X12 — NOT RUN — hub-run read (v3 :306, :328); the no_trade row stays the only input (its "does not" branch) until the desk runs it`. Both checkers: `NOT RUN BY DESIGN`.
9. **KNOWN AT LAUNCH (the build's ESCALATE 3), for the record of the checkers' SECOND answer:** X10's and X11's GATE tests asserted BASE facts the contract replaces and were REMOVED from the file at S5 with the src change; their S2 GATE results (`PASSED`) and post-code PASS results stand in the build report's table. Both checkers answered `RAN — gate PASSED at S2 … then removed / passes GREEN`, design-changing no.
10. **The build's DRAFTER PINS line and its A1 line, verbatim (carried for the desk):**
    - `10. **DRAFTER PINS where v3 / R51 name no shape (for `52` to rule on, not the builder's):**` — the R51 compare key `(symbol, direction, shares)` as a multiset, and `stated_differs` = the unmatched ids of BOTH books; `stated_book_id` kept on a `carried` seed as the history link; `repaired` / `not_repaired` on the recorded day's `day` row, written only when a later day exists; the chain STOPS at a not-computed day (an ESCALATE default — v3 side A is silent, side B not ruled); a `failed` current import FAILS the re-pair (v3 `[F-29]` B's clause, A1 silent); a `partial` current import → `not_computed.pairing` = its stored `reason`; a resolve applies on its own day only, is superseded by an export touching its symbol that day or by an earlier export's close, and FAILS otherwise (`rebuild <R>`, `names <trade_id> … does not hold`); the resolved trade's exit leg only with price AND time; the CLI's rebuild after `--apply` (C7); `FN_VERSION` `drc.pairing/3`.
    - `11. **A1 is a DESK READING of R51 (the row says so), not his word; a wording he wants otherwise is one message (R51).**`
    - The houses' rulings on the pins (`52` asked each): opus UNSOUND on pins 4 and 6 (SOUND on the other eight); grok UNSOUND on pin 9 (SOUND on the other nine).
11. **Standing lines:**
    - **"Round 1 covers DRC K2 whole (`<base>..<tip>`), its X7–X12 outputs and its three suites' executed output, checked by Opus 5.5 + Grok under his R95 (a new build: Fable-seat as Opus 5.5 per 09-22 R109; Astra METER until Sep 26th, 2026 6:47 AM, seated by the desk from its return; Gemini out, R96/R97). Two seated houses `ready for K3: YES` with `defects that HOLD: 0` → K2 is checked by the floor L67 allows while Astra is METER (the shrink-to-two clause), and K3's drafter may start; Astra's read of K1 and K2 is recorded when it returns. A HOLD → a fix round classified first (L75), ≤3 rounds (L39)."** — RESULT: opus `ready for K3: NO`, grok `YES`, `defects that HOLD: 9` → NOT met; a HOLD → a fix round classified first (L75).
    - **"The deploy's L68 gate re-proves offline, with-DB and the live-note suite on the stacked tree that ships (D1 + K1 + K2 + D4 + D2 + D3); the deploy prompt gets its own house read (L67, R95 seats)."**

DRC K2 CHECK DONE · round: 1 · opus: CHECK DRC K2: FIX C5-kept-match, C4-stop-chain, C3-resolve-deadlock, C4-no-trade-seed · ready for K3: NO · stale chain carries unflagged; stats never re-matched; resolve deadlock unrecoverable · grok: CHECK DRC K2: BUILD STANDS EXCEPT C7 · ready for K3: YES · astra: METER retry after Sep 26th, 2026 6:47 AM · houses that checked: 2 of 3 · defects that HOLD: 9 · ready for K3: NO · ESCALATE: 11
