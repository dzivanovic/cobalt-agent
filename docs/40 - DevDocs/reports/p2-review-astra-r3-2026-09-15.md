FINAL: one BLOCKER remains (R2-1/R1-10); one AMEND remains (R2-7/R1-18), recorded as dissent under L29/L39; no fourth round.
R2-2 through R2-6 resolve their findings as plan requirements; all other previously closed R1 findings stay closed, and 2026-09-15 rulings R1–R11 stand.
Evidence alias PLAN = docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md; full plan, R1/R2 reports and hub dispositions reviewed; implementation proofs remain owed. RESTARTS: none.

| id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording |
|---|---|---|---|---|---|
| 1 | BLOCKER | STEP-1 receipt; R2-1/R1-10 retained in part | The user-side table, tenancy, dark-run persistence, evaluation time, ordered cohort and publish gate close substantial gaps. The concrete receipt still has only `pool_unit_sha256`, no pool-unit content or payload content/hash, and no explicit consumed-tunables snapshot. Its observation contract enumerates each datum's hash/locator/timestamps/version/status without requiring the datum's retained value/bytes. Replay is directed exclusively to this receipt, so mutable or expired source content cannot be recovered from the specified metadata. Also, `run_id BIGINT NOT NULL` lacks the requested FK to `system.radar_score_run`; the existing grant names SELECT only for that table. The changed-source replay test is necessary but does not supply these missing storage requirements. | PLAN:94–96,131–132,177,181,223; docs/40 - DevDocs/reports/p2-review-astra-r2-2026-09-15.md:8 | Receipt observations retain the exact consumed values/bytes alongside their metadata; retain the consumed tunable rows and exact pool-unit and collector-payload content with hashes in the immutable user-side receipt or explicitly linked immutable user-side storage. Define `run_id REFERENCES system.radar_score_run(id)` with the required REFERENCES grant and dependent-first rollback. Prove dark-run replay after source mutation and cache removal using only retained inputs, including the frozen policy and cohort evidence. |
| 2 | AMEND | §6 rollback; R2-7/R1-18 retained in part | The single bounded reverse invocation and stop-before-DDL ordering are corrected. However, the ordered step still says “pg_dump restore … selectively” without an executable selective-restore path: pg_dump creates a dump and does not perform a selective merge. It names only sizing/transition restoration, although the expanded manifest includes stop edits, sequences and key-block vault units. Code/tunables revert and settings/loader compatibility remain outside that ordered step, which ends with “restart both residents once compatible.” Thus the exact complete procedure requested in R2 is still unspecified, and replaying D1 rows could conflict with surviving rows or restore stale manual state unless the merge rule is explicit. | PLAN:276–281; docs/40 - DevDocs/reports/p2-review-astra-r2-2026-09-15.md:14 | Replace the superseded domain commands with one complete hub procedure: stop affected residents; reverse 0007 then 0006 once; use a named, implemented selective-restore command with staging and explicit row-conflict rules that preserve surviving/post-D1 manual data, restore required child history, and keep sequences above surviving IDs; reconcile affected vault units through the guarded restore path; restore compatible settings/loader state and revert code/tunables before restart. Specify the exact invocations and verify that complete sequence on populated cobalt_dev before D1. |
