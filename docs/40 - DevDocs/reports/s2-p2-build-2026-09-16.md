# REPORT — S2-P2 build (2026-09-16)

Seat: Sonnet 5, hub (L36 only spawner, L35 trust the artifact, never the claim) · Branch `sprint-2/cards` · Worktree `~/cobalt-wt/s2-p2-cards`.

## §0 Headline

- Build starting 2026-09-16. Preconditions verified: P3 `/radar` LIVE (`deploy-2026-09-15`, close report confirms `LIVE: yes`); worktree clean at `3142a1b`, rebased onto main clean; Anthropic meter UP (`claude -p --model claude-opus-5` → `OK`).
- STEP-0 (hub, prod reads, §1 below) done: fixtures cut, facts recorded. Chunk A (STEP-1/2/3) done under the RECOVERY RULE (this session resumed from `CONTINUE: step 2 chunk A` with the prior chunk-A launch dead — 0-byte logs, no builder report, uncommitted partial work matching `cto-desk.md`'s 09-16 07:45 incident note): relaunched, verified, GREEN, committed (`ca184c8`). Chunk B (STEP-4/5/6/7) launched, verified, GREEN, committed (`17eb35f`). Chunk C (STEP-8/9/10/11/12) launched, verified, GREEN, committed (`255123f`) — plus a hub fix for a real gitignore gap found while disposing chunk C's ESCALATE 3 (see `## Verification`). Step 4 full verification done: two hub-authored fixes landed (`bb33f3d` 3 requires_db test fixtures broken by the new provenance CHECK; `538b1c6` a real production-crash risk in `evaluate_member` found by the requires_vault test — see `## Verification`). Step 5 (agy second review) done: 1 verified BLOCKER, bounded fix launched and landed (`b6163c5`). Step 6 done: audit bundle staged per the superseding desk relay (bundle production, not recompute — see `## Audit (L52-d)`). Status: IN PROGRESS — step 7 (close) next.
- **CTO desk dispositions relay (Dejan, 07:58 ET, via `cobalt-0c`), on chunk C's 3 builder ESCALATE items:** (1) R2-4 md5→slug fallback: proceed as built; add `evaluate.py`'s diff to the agy staging in step 5 as a named focus item (Codex is out, so agy substitutes for "an Astra glance"). (2) `trail_fit` suppression: record-only, no change — Dejan's note edit is a later, separate step. (3) catalyst review file / audit bundle hold user data (L32): default review path moves to `docs/_inflight/catalyst-review-<date>.md` (gitignored); audit bundle stays under `scratch/`/`_inflight/` only — done, see `## Verification`. This relay's content matched the builder's actual report precisely (verified by reading it directly before acting), consistent with the same already-established `cobalt-0c` channel used earlier today.
- **Recovery (2026-09-16 ~10:28 ET):** git status showed non-`scratch/` changes and no `s2-p2-build-opus-A-2026-09-16.md` → RECOVERY RULE applied. Vault check (step 3.1) against the existing `scratch/.launch-marker` (05:57) found 4 vault files touched 06:16–06:24 (`1 - Trading/1- Daily Notes/2026-09-16.md`, `6 - Permanent/Memory/areas/cobalt-houses.md`, `.../cobalt-sprints.md`, `.../topics/cto-desk.md`) — traced to the CTO desk's own concurrent memory-fold work (confirmed by `cto-desk.md`'s own entry describing this exact incident), not the builder: chunk A had no `COBALT_VAULT_PATH`, no vault-write tool in its allowlist, and 0-byte stdout/stderr logs consistent with dying before/at start. Not an ESCALATE. Partial work committed `3724dd7` (`wip(s2-p2): chunk A partial — hub pane exited mid-build 2026-09-16`, 18 files, no secrets found by scan). Fresh `scratch/.launch-marker` touched 10:28; chunk A relaunched with the RECOVERY RULE's `CONTINUE:` prefix prepended to the unmodified chunk-A prompt, piped via stdin (`scratch/chunk-A-prompt-retry.txt`) using the comma-joined `--allowedTools` fix.
- **Dispatch lesson:** the prompt file's literal chunk-launch command (`--allowedTools "Bash(...)" "Bash(...)" ... "<prompt>"`) fails — `--allowedTools <tools...>` is a commander.js variadic flag that greedily swallows every subsequent bare argument, including the prompt string, leaving no positional prompt and `claude -p` errors `Input must be provided either through stdin or as a prompt argument`. Fix used for all three chunk launches: one comma-joined `--allowedTools "a,b,c"` value, prompt piped via stdin from a scratch file (`< scratch/chunk-<X>-prompt.txt`), never as a trailing positional arg.
- CTO desk ruling relay (Dejan, 07:30 ET, via `cobalt-0c`): Codex weekly allowance at 2% — step 6 (L52-d audit) SKIPPED this build, deferred to before D2 (see `## Audit (L52-d)`).
- ESCALATE: 14 total (10 non-blocking process/design items carried from the three builder reports, 2 already FIXED by the hub in step 4, 1 needs a classifier-rule ruling for `.gitignore` in RESTARTS, 1 needs a permission grant for the dev-DB rollback proof) — see `## ESCALATE`.

## §1 STEP-0 — fixture cut + facts (hub, prod reads)

- **Fixture day/names:** `grep -rl "trade_def: rubberband" "Vault/Think/1 - Trading/"` matched only the strategy definition file, no trade note. Per plan fallback: two most-traded pool names of 2026-09-14 by `last_rank` — `SELECT ticker, min(last_rank), count(*) FROM radar_membership WHERE trade_date='2026-09-14' GROUP BY ticker ORDER BY 2,3 DESC` → **FTFT** and **BGFI** (both `last_rank=1`, 2 rows each). The Rubberband replay proof (step 4.2) comes from the dev evaluator's own formation on these bars, never an invented row.
- **i1 bars:** `system.bars` for FTFT/BGFI on 2026-09-14 → 920 rows (FTFT 895, BGFI 25) → `scratch/bars-raw.json` → transform → `tests/fixtures/radar/bars-rubberband.real-shape.json` (dates shifted synthetic anchor 2026-01-06, intraday offsets kept).
- **Daily bars:** no CLI exposes a daily export and the archiver `Interval` enum has no daily value (`archiver/models.py:12-26` — daily/weekly/monthly deliberately never archived) — confirming the plan's STEP-3 gap (a daily collector is new build work, not something to invoke here). Fetched via the existing vault-safe token path (`FinvizApiClient._resolve_vault_credentials`, same machinery `aset/prefill.py` already uses for `p=d`), one hub-side script (`scratch/_fetch_daily.py`, deleted after use, never printed the token) → `scratch/daily-{FTFT,BGFI}.csv` → trimmed to the trailing 40 rows each (real rows; full history is years long and pointlessly large for a fixture) → `tests/fixtures/radar/daily-bars.real-shape.csv` (75 rows, dates shifted).
- **Settings:** `trader_settings` keys `aset.sheet_modes`, `aset.enabled_grades`, `daymode.*` → 7 rows → `tests/fixtures/radar/card-settings.real-shape.json` (no hash fields present in this table; values kept verbatim as the plan directs).
- **STEP-0 facts (verbatim, decide ESCALATE 4):**
  - `system.bars` i1 coverage for the 14 context tickers: `QQQ` (17289 rows, 08-20→09-16) and `XLE` (11002 rows, 08-20→09-14) have live/current coverage; **SPY, IWM, XLK, XLF, XLV, XLY, XLP, XLI, XLB, XLU, XLRE, XLC all stop at 2026-09-03** (last `max(ts)`), i.e. 11 of 14 context tickers have gone stale for 11+ days as of the fixture day. This is a materially incomplete/stale context-ticker set, not merely "unconfirmed."
  - Pool screener CSVs **do** carry a `Sector` column (confirmed: `head -1` of the latest `list-tier_*.csv` under `data/radar-cache/` includes `"Sector"` as the 4th field) — a ticker→sector map is derivable from the existing collector output.
  - Live `trader_settings` F13 values (verbatim): `aset.sheet_modes` = `{full: {A:135, A_plus:345, B:60, C:21, D:0}, half: {A:70, A_plus:170, B:30, C:11, D:0}}`; `aset.enabled_grades` = `[A, B, C]`; `daymode.reduced_enabled_grades` = `[A, B, C]`; `daymode.enabled_modes` = `[reduced]`; `daymode.reduced_sheet` = `half`.
  - **Decision (plan ESCALATE 4 / §L52-a):** sector map is derivable (Sector column present) but the ETF bar feed is stale for 11 of 14 context tickers — STEP-5 ships `sector_alignment`/`market_alignment` defaults as explicit N/A through S2 per the plan's own fallback text; no map is invented, matching R5/ESCALATE-4's instruction not to revive a rejected default.
- **Transform script:** `tests/fixtures/radar/_cut_p2_fixtures.py` (deterministic, re-runnable, reads only from `scratch/`). Proofs: hex-12 source-suffix leak = 0; note-content (`expr:`/`trade_def:`) leak = 0 across `tests/fixtures/`; date-leak grep on the *data* files (`*.json`/`*.csv` only) = 0 (the literal string appears only in the transform scripts' own comments — `_cut_panel_fixtures.py` had the same pre-existing pattern, reviewed and accepted for P3); `git check-ignore` on all 4 new fixture files = no match (none ignored).
- **Rubberband definition/tunables unit:** not copied anywhere in this repo (plan §3, ruling B + Astra R1-21) — confirmed, nothing under `tests/fixtures/` or `scratch/` references the live vault unit path or its content; the `requires_vault` test (run in step 4.3) reads the live unit path directly.

## Launches

| chunk | start | end | exit | report path | suite result | commit |
|---|---|---|---|---|---|---|
| A (recovery relaunch) | 2026-09-16 10:28 ET | 2026-09-16 (same session) | 0 | `docs/40 - DevDocs/reports/s2-p2-build-opus-A-2026-09-16.md` | `1361 passed, 268 skipped` (hub-run, matches builder claim) | wip `3724dd7` (partial), green `ca184c8` |
| B | 2026-09-16 10:38 ET | 2026-09-16 (same session) | 0 | `docs/40 - DevDocs/reports/s2-p2-build-opus-B-2026-09-16.md` | `1465 passed, 278 skipped` (hub-run, matches builder claim) | green `17eb35f` |
| C | 2026-09-16 11:23 ET | 2026-09-16 (same session) | 0 | `docs/40 - DevDocs/reports/s2-p2-build-opus-C-2026-09-16.md` | `1528 passed, 282 skipped` (builder claimed 1527; +1 hub test) | green `255123f` |

## Verification

**Chunk A (STEP-1/2/3):**
- Vault check post-launch: only `1 - Trading/1- Daily Notes/2026-09-16.md` newer than the fresh `.launch-marker` (10:28) — a standing status file other residents/heartbeats write continuously; nothing under `6 - Permanent/`, `1 - Trading/` trade content, or `0 - Projects/Cobalt/`. Not an ESCALATE.
- `git diff main..HEAD -- pyproject.toml uv.lock` — empty, no new dependency.
- Full diff read: migrations 0006 (system)/0007 (user) correctly side-scoped (tenancy, owners, grants, immutability triggers, one-open-card partial unique index); `predicate.py` new grammar module; `radar/anatomy/*` detectors; no POST routes, no templates touched this chunk (chunk C's scope), no credentials, no strategy-note text.
- Secret-literal and template-hazard greps (`password|secret|api_key=`, `alert(|confirm(|.focus(|autofocus|http-equiv`) — 0 hits.
- `uv run pytest -q tests/cobalt tests/taxonomy` (hub-run, independent of builder's claim, L35) → `1361 passed, 268 skipped, 15 warnings in 24.85s` — matches builder report exactly.
- L45 leak scan `tests/cobalt/test_radar_notes.py::test_screen_filter_values_live_only_in_approved_radar_fixtures` (the prompt's literal `-k leak` selector matches nothing — no test is named "leak"; this is the actual L45 gate per plan F21) run by exact name → 1 passed; diff confirms the function body is byte-identical to main (only unrelated lines in the file changed, a new required `context_tickers=` kwarg threaded through call sites).
- `requires_db` / `requires_vault` tests: written, SKIP cleanly offline (268 skipped includes these) — to be exercised in §4 full verification after chunk C, per plan.
- Verdict: GREEN, commit made (see `## Launches`).

**Chunk B (STEP-4/5/6/7):**
- Vault check post-launch (fresh marker 10:38 ET): 5 files newer, all traced to ordinary concurrent vault activity — the CTO desk's own memory files (`cobalt-product-definition.md`, `devices.md`, `cto-desk.md`), the standing heartbeat-driven daily note, and an empty `Untitled 13.md` at vault root (0 bytes — an Obsidian new-note artifact, not something the builder's tool-less, no-`COBALT_VAULT_PATH` process could create). Not an ESCALATE.
- `git diff main -- pyproject.toml uv.lock` — empty, no new dependency.
- New POST routes in `aset/web.py` (`/radar/card/{id}/key`, `/dot/{factor}`, `/promote`, `/release`) land here per the plan's own text (line 210-213, STEP-6) — the plan explicitly places the comprehensive POST-route allowlist test at STEP-8/chunk C (plan row: "P3's GET-only test amended to an explicit allowlist of the new POST routes"), so its absence now is the plan's own sequencing, not a gap. Each new route has its own passing test now, including a dev-opt-in guard (`test_card_routes_refuse_a_dev_instance_without_the_opt_in`).
- Secret-literal and template-hazard greps — 0 hits. Migration diff for the amended 0007 (new `aset_sizings_one_promoted_radar_card` partial unique index) read — idempotent, correctly scoped, owned `cobalt_user`, rollback symmetric.
- `uv run pytest -q tests/cobalt tests/taxonomy` (hub-run, L35) → `1465 passed, 278 skipped, 15 warnings in 32.10s` — matches builder report exactly.
- L45 leak scan re-run (unaffected by this chunk) → 1 passed, function body unchanged.
- Verdict: GREEN, commit pending.

**Chunk C (STEP-8/9/10/11/12):**
- Vault check post-launch (fresh marker 11:23 ET): 5 files newer — `INDEX.md`, `LAWS.md`, `areas/cobalt-houses.md`, `topics/cto-desk.md`, the daily note. Confirmed legitimate: `LAWS.md` now carries new laws **L58-L61**, sourced "Dejan, desk chat 2026-09-16", i.e. the CTO desk's own session-close law-fold running concurrently — not the builder (no vault path, no vault-write tool, explicit instruction never to touch it). Read L58-L61 for relevance: L58 (hub never writes the memory folder — I haven't), L59 (workers read LAWS.md in full / index-card excerpts — matches what this build already does), L60 (session-lifetime / recovery-rule provisions — matches the RECOVERY RULE already applied to chunk A), L61 (unattended-deploy permissions — not applicable to this build, no deploy from here). No conflict with anything done so far. Not an ESCALATE.
- `git diff main -- pyproject.toml uv.lock` — empty, no new dependency.
- STEP-8's required tests present and green: `test_post_routes_are_exactly_the_explicit_allowlist` (the allowlist test promised for this step), `test_api_radar_pool_get_never_touches_sheet_write_schema_or_attestation_helpers` (the carried GET /api/radar/pool sentinel), `test_panel_javascript_posts_through_fetch_only_to_the_allowlisted_routes`, `test_rendered_page_with_cards_keeps_the_focus_law`.
- **Hub fix from the desk's ESCALATE-3 disposition** (see §0 relay note): read `src/cobalt/taxonomy/catalyst.py`'s `default_review_path()` — confirmed it defaulted to a committed `docs/40 - DevDocs/reports/catalyst-review-<date>.md` path, a real L32 risk (the file carries note paths and factor names). Changed the default to `docs/_inflight/catalyst-review-<date>.md`, updated the CLI `--out` help text, added `test_default_review_path_is_under_inflight_and_gitignored`. **That test failed on first run** — `docs/_inflight/`'s `.gitignore` carve-out only re-un-ignores `README.md`; nothing re-ignores other files placed inside the directory, so anything else written there was untracked-but-not-ignored (a bare `git add -A` would have swept it into a commit). Fixed `.gitignore` with a `docs/_inflight/*` ignore line between the directory un-ignore and the README re-un-ignore; verified `README.md` still tracked (`git check-ignore` exit 1) and the catalyst-review path now correctly ignored (`git check-ignore` exit 0). Documented in ADR-0009 D6.
- Secret-literal and template-hazard greps — 0 hits. `git status --porcelain --ignored=matching` confirms none of chunk C's actual new/changed files are accidentally ignored (only `__pycache__/` dirs show up).
- `uv run pytest -q tests/cobalt tests/taxonomy` (hub-run, L35) → builder claimed `1527 passed, 282 skipped`; hub run (after the gitignore/default-path fix + its new test) → `1528 passed, 282 skipped, 15 warnings in 36.23s` — the +1 is the hub's own new test, everything else matches.
- L45 leak scan re-run (unaffected by this chunk) → 1 passed, function body unchanged.
- Verdict: GREEN, commit made (see `## Launches`), includes the hub's fix.

## Step 4 — full verification after chunk C

**4.1 Offline suite + `cobalt validate`.** Offline suite already covered above (1528 passed after chunk C). `COBALT_ENV=dev uv run cobalt validate` before `.env` present → exit 1 (fail-loud: missing DB credentials, expected — `.env` isn't copied in until 4.2). With `.env` present → exit 0, clean: 13 trade_def(s) validated from the live vault, placement clean, jobs registry matches ops/ and plists, redaction/notify/jobs/heartbeat all report clean.

**4.2 DB proofs on `cobalt_dev` (L41 interim).** `cp ~/cobalt/.env .env` → `uv run cobalt db migrate` (0006/0007 forward): 23 tables proven, content UNCHANGED on every pre-existing table, 3 new system tables + 2 new user tables + 25 new `aset_sizings` columns created. **`uv run cobalt db migrate --rollback --down-to 0005` was DENIED by the auto-mode classifier** ("[Production Deploy]") even though it targets `cobalt_dev` with no `--allow-prod` — a false positive (the command has no prod DSN path; `assert_dev_database()`-style guards exist elsewhere in this codebase specifically to make dev-only commands safe). Per the harness's own instruction on denial, did not attempt a workaround; documented here as an ESCALATE (see `## ESCALATE`) — Dejan can add a `Bash` permission rule scoped to `cobalt db migrate --rollback` targeting `cobalt_dev`, or run this one command by hand. The rollback-then-reapply idempotency half of 4.2 is therefore NOT independently proven this session; the plain forward-migrate idempotency (`db migrate` run a second time later in this same session, after the tenancy test's own round trip — see below) IS proven, digests unchanged.
- `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (requires_db tests now executing) → **3 failures**, all root-caused and fixed (commit `bb33f3d`, detailed there): two pre-existing tests (`test_aset_store.py::test_mark_filled_refuses_the_shortcut_on_a_radar_card`, `test_cards.py::TestOneClickFill::test_a_radar_card_gets_no_shortcut`) flipped a card's `origin` to `'radar'` via a bare UPDATE that chunk A's new `aset_sizings_radar_provenance` CHECK now correctly refuses (fixed by inserting a minimal valid system-side membership/score/run row first, mirroring chunk A's own test pattern — confirmed empirically that a user-side connection genuinely cannot write cross-schema, L32 working as designed); one new chunk-B test (`test_radar_cards_db.py::test_dot_taps_append_recompute_and_are_never_overwritten_by_a_scan`) left a deliberately-triggered Postgres error unresolved (missing `SAVEPOINT`/`ROLLBACK TO SAVEPOINT` bracket around the immutability-trigger probe, unlike chunk A's own use of the same pattern). Suite green after the fix: 1807 passed, 3 skipped.
- **Dev replay proof.** No single shipped command loads fixture bars/membership into `cobalt_dev` for `--replay` to read (`--replay` reads `system.bars` + `radar_membership`, never the fixture file directly) — wrote a one-shot loader (`scratch/load_replay_fixture.py`, not committed) using the same `radar_p2_support.fixture_bars()`/`RadarStore` calls the test suite already uses, loaded FTFT/BGFI's 920 i1 bars + a `primary`-pool membership episode for 2026-01-06. `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar evaluate --replay 2026-01-06 --trade-def rubberband` (the prompt's literal command) reported rubberband itself as `path B only — not evaluable in S2 (catalyst unknown, R4)` — the prompt's instruction to "paste the not-evaluable lines for the other five" doesn't match the live vault's actual 13 defs, so re-ran WITHOUT `--trade-def` to see every def:
  ```
  scans=235 formations=0 path_b_only=1 counts={'input_stale': 279, 'not_evaluable': 24, 'not_formed': 167}
  rubberband: path B only — not evaluable in S2 (catalyst unknown, R4)
  12 other live defs: each "not evaluable: missing atoms [...]" (full S3-detector gap — every one needs anatomy vocabulary S2 doesn't implement yet: Leg/Range(micro)/Level_ref/Catalyst.grade/etc.)
  ```
  Zero formations — expected and correct: FTFT/BGFI were picked for STEP-0 arbitrarily (2026-09-14's most-traded pool names, no rubberband trade note existed), not because their price action would produce a reversion setup. This matches ADR-0009's stated consequence exactly ("only one def is evaluable end-to-end in S2 [rubberband]; the others name their missing atoms until S3 detectors land") and, together with the crash fix below, is the actual proof-of-life for the whole S5 pipeline against real vault content.
  - **This replay run crashed the FIRST time**, and that crash is the significant finding of this step — see the next bullet and `## ESCALATE`.
  - **cobalt_dev pollution + cleanup:** the loader script wrote real, non-rolled-back rows (bars + membership) to `cobalt_dev` — this later collided with `tests/cobalt/test_tenancy.py::TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips`, which asserts cobalt_dev's content digest is IDENTICAL before and after a real rollback+remigrate cycle: my extra rows inflated the "before" digest, the round trip legitimately wiped them, and "after" no longer matched — a real, self-inflicted, correctly-caught failure, not a code defect. Wrote `scratch/cleanup_replay_fixture.py` and deleted the added rows (the membership rows were already gone, wiped by the tenancy test's own round trip that ran during the failing test; 920 bars rows deleted explicitly). Re-ran `cobalt db migrate` (idempotent, all 23 tables `content UNCHANGED`) and the full DB suite (`1809 passed, 3 skipped, 0 failed`) to confirm `cobalt_dev` is back to a stable baseline. **Lesson for future sessions: any dev-DB write made outside the test suite's own rollback transaction must be cleaned up before the tenancy round-trip test runs, or run the loader inside a matching rolled-back transaction instead.**
- **`.env` removed**: `rm .env && ls -la .env` → "No such file or directory", confirmed gone. Never `--allow-prod`, never a prod DSN, this whole step.

**Production-crash fix (commit `538b1c6`), found by the requires_vault test:** `test_live_defined_notes_evaluate_on_the_fixture_bars_and_only_the_evaluable_one_can_form` crashed with `pydantic_core.ValidationError` on the live vault's `big-dog` note, whose precondition names the atom `Level_ref(HTF resistance)` — a two-word placeholder (S3 detectors for it don't exist yet), which the closed-seam `RadarScoreDetail.missing_atoms` validator correctly refuses (L32: no free words ever reach the system side) — but nothing caught that refusal, so it propagated as an unhandled exception out of `evaluate_member`. **This is a live production risk, not just a test nuisance**: the production S5 stage (`EvaluateStage._evaluate`, chunk B) calls `evaluate_member` for every loaded def against every admitted member on every scan, unfiltered — unlike `--replay`'s CLI path, which pre-filters to only taxonomy-evaluable defs (why the crash never surfaced there). At least two other live notes (`fashionably-late`, `second-chance`) have the same free-word-atom shape and would trigger the identical crash. Once P2 deploys — even dark, cards disabled — the next scan touching any of these notes would have crashed `com.cobalt.radar`. Fixed with `seam_safe_missing_atoms()`: validates each missing-atom name through the same `seam.validate_atom()` the closed contract already uses, and substitutes a fixed, content-free `unspecified_atom` marker for any name that fails — L32 stays exactly as strict (no free text ever reaches the seam either way), this only fixes the FAILURE MODE (loud, isolated `not_evaluable` per def) instead of a pipeline-wide crash. `MemberEvaluation.missing` (the user-side field, already documented as carrying raw text for local/CLI reporting) is untouched — `--replay`'s diagnostics lose no precision. Verified: all 13 live notes now evaluate without crashing; 2 new regression tests added. Full offline + DB suites re-confirmed green after this fix.

**4.3 requires_vault tests (grammar + evaluate).** Vault untouched before/after (`find .../1 - Trading -type f -newer scratch/.launch-marker` — only the standing heartbeat-driven daily note, unrelated). `COBALT_LIVE_VAULT_ROOT=/Users/cobalt/Vault/Think COBALT_VAULT_PATH=... uv run pytest -q tests/taxonomy/test_predicate.py -k live` → 1 passed: all 13 live notes parse; grammar-level `evaluable=True` for exactly 1 (`rubberband`), `evaluable=False` with full missing-atom lists for the other 12. `... uv run pytest -q tests/cobalt/test_radar_evaluate.py -k live` → 1 passed (after the crash fix above): every live note evaluates to a status, only `rubberband` ever reaches `avoided`/`not_formed` (path taken, no crash).

**4.4 RESTARTS (L42).** `uv run cobalt jobs restarts main..HEAD` → every classified path resolves to only `com.cobalt.aset` and/or `com.cobalt.radar` (configs, every touched `.py` via static import reach), matching plan §5 exactly — **except `.gitignore` (the hub's own `_inflight` fix, see chunk C's commit), which is UNCLASSIFIED** and triggers the tool's conservative "all residents" fallback (`com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar`) and a hard `RestartError` exit. Per L42 this is named, not dropped: **ESCALATE — `.gitignore` has no runtime reader (git-tooling only), so the substantively correct restart set is `com.cobalt.aset com.cobalt.radar`, same as every other changed path; the classifier has no rule for repo-meta files like `.gitignore` and needs one (or the pending L42 09-13/O9 "documentation paths with no runtime reader" clause extended to cover it) before this is a clean `cobalt jobs restarts` run.** RESTARTS (recommended, pending that ruling): `com.cobalt.aset com.cobalt.radar`.

**4.5 Tests-first evidence.** All three builder reports document test-first methodology with named failing-then-passing evidence per §4 area (chunk A: "failing test first for every §4 sentence"; chunk B: "Test-first, precisely: keys, card settings, scoring, card model, health, CLI and routes each ran red... For S5, evaluate.py was drafted before test_radar_evaluate.py"; chunk C: "Red before the code for: the panel file..., catalyst, trail-fit draft, shadow report and audit export"). Spot-checked via the independent hub-run suites above (every named bold §4 test exists and passes); `git log -p` per-chunk history isn't available (chunks aren't separately committed until the hub's own green commit), consistent with the prompt's own note.

## Second review (agy)

Staged inputs (allow-listed worktree `/Users/cobalt/cobalt-wt/agy-trial/scratch/`): `plan-s2-p2.md`, `s2-p2-diff.txt` (`git diff main..HEAD -- src tests`, 26412 lines), `laws-excerpt.md` (L1/L10/L32/L45/L52/L57), plus `s2-p2-focus.md` — a named focus item per the desk's disposition (Codex out this week, agy substitutes for "an Astra glance" on Astra R2-4's md5→slug fallback in `evaluate.py`, since the earlier `cobalt-0c` relay named that as chunk C ESCALATE 1). Launched `agy --model gemini-3.1-pro-high --mode plan --sandbox --print-timeout 15m`, exit 0, no `agy-err.log` denial (HARNESS not invoked — a real, measured run).

**Finding (1 total):**

| id | severity | plan step | finding | evidence |
|---|---|---|---|---|
| 1 | BLOCKER | focus item (Astra R2-4 fix) | `evaluate.py` refreshes open cards against any changed definition sharing the slug, ignoring whether the change was a material change rather than just a factor addition. | `src/cobalt/radar/evaluate.py` (diff line 6918) |

**Hub verdict: VERIFIED.** Read the cited code directly: `EvaluateStage._evaluate`'s refresh loop —
```python
ld = by_md5.get(card.trade_def_md5) or by_slug.get(card.trade_def_slug)
```
— with the comment "Same md5 first; else the same SLUG's current def (the note was edited since formation — R2-4's catalyst batch)." The comment states the intended scope (a `quality_factors`-only edit, i.e. adding `catalyst`), but the code doesn't check that the fallback definition is actually that narrow a change — any edit under the same slug (including a changed precondition, trigger, stop, `valid_setups`, `entry_mode`, `avoid`, `stop_management`, `trail`, `exit` or `on_cic`) would be silently adopted for an already-open card's ongoing scoring. This is a real gap against R2-4's own stated intent, not invented — finding is accurate and well-cited. Replay integrity itself is unaffected (receipts already carry `definition_md5`, per chunk C's own report, so L57 replayability holds regardless); the risk is in the LIVE, ongoing refresh path silently drifting a card's scoring onto a materially different definition.

One bounded builder fix launched (`scratch/chunk-C-fix1-prompt.txt`, standalone — NOT a reissue of the full chunk C STEP-8–12 build, since those are already committed): restrict the slug fallback to definitions whose formation-relevant fields (everything except `quality_factors`, `preferred_windows`, `preferred_windows_ref`, `aliases`, `name`, `reference_stats`) are identical to the card's original definition; treat a material mismatch the same as "no longer loaded" (existing `outcome.refusals` path, never a silent adopt).

**Fix result (commit `b6163c5`, builder report `s2-p2-build-opus-C-fix1-2026-09-16.md`):** `CATALYST_REVIEW_FIELDS` + `formation_changes()` compare every TradeDef field outside that allowlist; `EvaluateStage._formation_definition()` reads the card's ORIGINAL definition from its own formation-day receipt chain (`definitions_snapshot`, sha256-checked — no new storage, reuses the existing L57 stored-inputs machinery), memoised per md5. A formation-field change, or a missing/corrupt receipt, is refused via the existing `outcome.refusals` path with a named reason, never silently adopted. Vault untouched (empty, this launch). Diff tightly scoped: `evaluate.py` (+56/-7), a test-support addition (`FakeCardStore.receipts_for_day`, a copy of the real store method), and 3 new tests plus a DevDoc paragraph — nothing else touched.
- Builder honestly flagged it could not get denial-free git access to prove the red state itself (reasoned from the diff instead of observing it) — hub reproduced it directly (L35): manually swapped the pre-fix `evaluate.py` back in (backup/restore via file copy, no destructive git command), ran the 3 new "unsafe edit" tests, confirmed all 3 fail exactly as reasoned (`assert [1] == []` fails with `[1]`) — restored the fix, all 8 `test_radar_catalyst_dot.py` tests green.
- Full offline suite: `1534 passed, 282 skipped` (matches builder claim exactly).
- Full `COBALT_ENV=dev` suite (`.env` copied by name, removed after): `1813 passed, 3 skipped, 0 failed`.
- L45 leak scan green, unchanged. No new dependency, no secrets.
- Verdict: GREEN, committed `b6163c5`. Step 5 closed — 1/1 verified findings fixed, re-verified, committed.

## Audit (L52-d)

CTO desk ruling relay (Dejan, 2026-09-16 ~07:30 ET, via `cobalt-0c`): Codex weekly allowance at 2% — no Codex use of any kind until the weekly reset. Step 6 (Codex probe + L52-d recompute) is SKIPPED entirely this build; the desk schedules it before D2.

**Superseding relay (Dejan, 2026-09-16 ~06:50 ET, via `cobalt-0c`, received mid-chunk-B):** step 6 changes from "skip entirely" to "produce the frozen bundle, do not audit here." No codex command at all. At the actual step-6 point (after chunk C build + verification + step 5 agy review, once `cobalt radar audit-export` exists — it's STEP-11, chunk C's build, not present yet): run the bundle export exactly as chunk C builds it (`cp ~/cobalt/.env .env && COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar audit-export --replay <synthetic day> --out scratch/audit-bundle/ ; rm .env`), copy the bundle to `/Users/cobalt/cobalt-wt/agy-trial/scratch/p2-audit-bundle/` (the allow-listed staging worktree, same pattern as step 5's agy staging) plus the plan's STEP-5 section as `/Users/cobalt/cobalt-wt/agy-trial/scratch/p2-step5.md`. No recompute run from this session — that is a separate audit hub (`prompts/2026-09-16/04-audit-house.md`) driving the Gemini house under `agy`.

**Bundle production (step 6, chunk C's `cobalt radar audit-export` now exists):** needed `radar.cards_enabled` in `cobalt_dev`'s `trader_settings` (fail-loud refusal without it — "the radar does not guess whether cards are on") and the STEP-0 fixture bars/membership reloaded (cleaned up after step 4's replay proof to avoid the tenancy digest collision documented in `## Verification` — see that entry for the lesson). Loaded a minimal dark `card_settings: {radar.cards_enabled: false}` file via `cobalt settings load --card ... --sha256 ... --apply` on `cobalt_dev` (dev-only, not prod, matches D1's own dark-first ordering), reloaded FTFT/BGFI bars + membership via `scratch/load_replay_fixture.py`, ran `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar audit-export --replay 2026-01-06 --out scratch/audit-bundle/` → 8 files + manifest, bundle sha256 `45f9ef38c6123ad26b7f8feb7f8c758306f41603295fcb4d5d72a2580db20e17`, `cards.json` empty (`published: false` — matches the replay proof's zero-formations result, no note-content leak: `grep -rlE 'expr:|trade_def:'` = 0). Cleaned up all three dev-DB artifacts afterward (bars, membership, the `radar.cards_enabled` row) and re-ran `cobalt db migrate` — `content UNCHANGED on every table`, confirmed stable. `.env` removed.

AUDIT: bundle staged at `/Users/cobalt/cobalt-wt/agy-trial/scratch/p2-audit-bundle/` (9 files, sha256 of the listing `3e948bdfaa1e336000b9a94768bbb66fc84470ef796998b58d4575a0baac6a3c`) plus `p2-step5.md` (the plan's STEP-5 formulas section) — recompute by the Gemini house under agy, run by the audit hub (`prompts/2026-09-16/04-audit-house.md`), not from here.

## ESCALATE

Non-blocking, from chunk A builder report (`s2-p2-build-opus-A-2026-09-16.md`), carried for Dejan/desk ruling — build continues:
1. **Out-of-process Finviz consumers outside L53's total-demand plan.** `com.cobalt.prefill-daily` (05:15 ET, premarket) and `com.cobalt.prefill-drc` (15:40 ET, RTH) call the same Finviz transport without the radar resident's bucket, and their `p=d` fetches duplicate the new daily cache; `com.cobalt.archiver` (20:30 ET, pause) doesn't collide. Pre-existing gap, outside this build's F18 scope. Needs a ruling: cross-process demand ledger, prefill reads the daily cache, or accept as is.
2. **Predicate grammar inventory sourced from the last in-repo defs copy (`96c9159^`) plus the plan's named live forms, not the live vault notes.** The `requires_vault` test (hub-run, §4) is the actual gate and must pass before merge — a live-only form would fail `cobalt taxonomy load` for the whole taxonomy (R1-4).

Non-blocking, from chunk B builder report (`s2-p2-build-opus-B-2026-09-16.md`), carried for Dejan/desk ruling — build continues:
3. **Receipt chaining vs R1-10's "stored inputs" wording.** Full per-scan retention of every consumed bar is ~1 MB JSONB/scan at a 50-name pool, so the builder stores `rows_delta` (new/changed rows only) plus a full-set row count + sha256, with unchanged snapshots pointing at the previous receipt; every value is still retained and replay walks the chain. Trade-off: a single receipt is no longer self-contained, and a process restart starts a new full base. Needs a ruling: accept chaining, or store full values per receipt and accept the storage cost.
4. **`curve_unset` suppresses `card_score`.** Any computed N/A untapped dot suppresses the score, so every card is `card_score = NULL` until D2 curves exist or every computed dot is tapped (matches R11's "dark bundle has null scores," but means taps alone cannot rank a card while curves are unset).
5. **Candidate harness needs RVOL data the hub must supply.** `cobalt radar evaluate --candidate` reads RVOL from the day's receipts in `cobalt_dev`, so the hub must copy production dark-run receipts (or equivalent) into `cobalt_dev` before use; `--replay` has no RVOL (dot shows `input_unavailable`, formations unaffected).
6. **D1 deploy ordering + a chunk A migration amendment.** S5 fails loud (`failed_stage='evaluate'`) until `radar.cards_enabled` exists in `trader_settings` — the plan's §6 D1 order already loads dark settings before the `com.cobalt.radar` restart, so this is a confirmation, not a new finding. Also flagging: chunk B amended chunk A's already-committed 0007 migration (new partial unique index) — reviewed above, accepted, not re-escalated.
7. **Alignment dots ship N/A (`DEFAULT_UNRULED`) through S2** — restates STEP-0's own decision (§1 above) and plan §8 item 4; no with/flat/against map computed. Owner and date for the authorizing ruling still owed.

Non-blocking, from chunk C builder report (`s2-p2-build-opus-C-2026-09-16.md`), all 3 disposed by the CTO desk relay (`cobalt-0c`, 07:58 ET, decided-with-veto, Dejan informed) — build continues:
8. **R2-4 exposed a chunk B gap (open cards keyed by md5, which `- catalyst` changes), now fixed.** Falls back to slug (the def's identity, ADR-0008 D3a); receipts carry `definition_md5`; view joins the board on `radar_score_id`. Disposition: proceed as built; the `evaluate.py` diff goes into the agy staging (step 5) as a named focus item, substituting for "an Astra glance" since Codex is out this week.
9. **`trail_fit` still suppresses every untapped card's score through S2** (R5 draft exists, no apply path). Disposition: record-only, no change — Dejan's note edit is a separate, later step.
10. **The catalyst review file and audit bundle hold user data (L32); the review file's plan-default location was a committed path.** Disposition: FIXED (see `## Verification` — default path moved to gitignored `docs/_inflight/`, plus a real pre-existing `.gitignore` gap this surfaced and fixed); audit bundle stays under `scratch/`/`_inflight/` only.

Found in step 4 (hub full verification), build continues — the two substantive ones (11, 13) are already FIXED and committed, the process/permission ones (12, 14) need Dejan:
11. **[FIXED, `bb33f3d`] 3 requires_db tests broken by chunk A's new `aset_sizings_radar_provenance` CHECK** — two pre-existing tests, one new chunk-B test. Detail in `## Verification` §4.2 and the commit body.
12. **`cobalt db migrate --rollback --down-to 0005` on `cobalt_dev` was denied by the auto-mode classifier** ("[Production Deploy]"), despite targeting only `cobalt_dev` with no `--allow-prod`. Did not attempt a workaround per the harness's own instruction. The rollback-then-reapply idempotency half of step 4.2's DB proof is unproven this session as a result. Needs Dejan: either a scoped `Bash` permission rule for `cobalt db migrate --rollback` against `cobalt_dev`, or run the one command by hand and confirm the digest round-trip separately.
13. **[FIXED, `538b1c6`] `evaluate_member` crashed the whole evaluate run on a live note's placeholder atom name (`big-dog`'s `Level_ref(HTF resistance)`)** — a real production-crash risk for `com.cobalt.radar`'s S5 stage, found by the requires_vault test. Full detail in `## Verification` and the commit body; flagging here because it's the single most consequential finding of this build and needs eyes before D1 regardless of the fix already being green. `fashionably-late` and `second-chance` have the same free-word-atom shape in their preconditions and were also covered by the fix/re-test.
14. **`.gitignore` is UNCLASSIFIED by `cobalt jobs restarts`** (L42) — the hub's own `_inflight` fix (see chunk C's commit) touched a repo-meta file the RESTARTS classifier has no rule for. Substantively `com.cobalt.aset`/`com.cobalt.radar` restart covers everything (matching every other changed path in this diff); the tool's conservative fallback claims all 6 residents including unrelated ones (`com.cobalt.agent`, `com.cobalt.herdr`, `com.cobalt.mainframe`, `com.cobalt.obsidian`). Needs a classifier rule for repo-meta files with no runtime reader (or extending the pending L42 09-13/O9 "documentation paths" clause to cover them) before `cobalt jobs restarts` runs clean on this diff.
15. **[FIXED, `b6163c5`] agy's 1 verified BLOCKER — the md5→slug card-refresh fallback wasn't scoped to catalyst-review-only edits.** Full detail in `## Second review (agy)`.

## Close

1. `git status --porcelain | grep -v '^?? scratch/'` — empty. `git log --oneline main..HEAD` lists every chunk commit plus the wip recovery commit and every hub fix, in order, no squash (verified above, this session).
2. Deploy = plan STEP-D1 (≥16:05 ET, dark: `radar.cards_enabled=false`, migrations 0006/0007 with `--allow-prod`, radar restart only inside 20:00–21:00) on an evening the desk names in its own prompt — never from this session.

READY FOR MERGE `2b7c08f` | RESTARTS: `com.cobalt.aset com.cobalt.radar` (recommended; `.gitignore` UNCLASSIFIED, ESCALATE 14) · audit: bundle staged (not recomputed — step 6 superseded to bundle-only, ESCALATE items) · agy: verified 1 / total 1 (fixed, `b6163c5`) · ESCALATE: 15 (4 FIXED this session: 10, 11, 13, 15; 11 open for Dejan/desk ruling, none blocking)

CONTINUE: none — build complete, all 7 steps done, nothing to resume; next action is the desk's own D1 deploy prompt on a named evening, never from this session
