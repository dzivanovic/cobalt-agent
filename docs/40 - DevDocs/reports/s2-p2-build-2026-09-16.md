# REPORT — S2-P2 build (2026-09-16)

Seat: Sonnet 5, hub (L36 only spawner, L35 trust the artifact, never the claim) · Branch `sprint-2/cards` · Worktree `~/cobalt-wt/s2-p2-cards`.

## §0 Headline

- Build starting 2026-09-16. Preconditions verified: P3 `/radar` LIVE (`deploy-2026-09-15`, close report confirms `LIVE: yes`); worktree clean at `3142a1b`, rebased onto main clean; Anthropic meter UP (`claude -p --model claude-opus-5` → `OK`).
- STEP-0 (hub, prod reads, §1 below) done: fixtures cut, facts recorded. Chunk A (STEP-1/2/3) done under the RECOVERY RULE (this session resumed from `CONTINUE: step 2 chunk A` with the prior chunk-A launch dead — 0-byte logs, no builder report, uncommitted partial work matching `cto-desk.md`'s 09-16 07:45 incident note): relaunched, verified, GREEN, committed (`ca184c8`). Chunk B (STEP-4/5/6/7) launched, verified, GREEN, committed (`17eb35f`). Status: IN PROGRESS — chunk C next.
- **Recovery (2026-09-16 ~10:28 ET):** git status showed non-`scratch/` changes and no `s2-p2-build-opus-A-2026-09-16.md` → RECOVERY RULE applied. Vault check (step 3.1) against the existing `scratch/.launch-marker` (05:57) found 4 vault files touched 06:16–06:24 (`1 - Trading/1- Daily Notes/2026-09-16.md`, `6 - Permanent/Memory/areas/cobalt-houses.md`, `.../cobalt-sprints.md`, `.../topics/cto-desk.md`) — traced to the CTO desk's own concurrent memory-fold work (confirmed by `cto-desk.md`'s own entry describing this exact incident), not the builder: chunk A had no `COBALT_VAULT_PATH`, no vault-write tool in its allowlist, and 0-byte stdout/stderr logs consistent with dying before/at start. Not an ESCALATE. Partial work committed `3724dd7` (`wip(s2-p2): chunk A partial — hub pane exited mid-build 2026-09-16`, 18 files, no secrets found by scan). Fresh `scratch/.launch-marker` touched 10:28; chunk A relaunched with the RECOVERY RULE's `CONTINUE:` prefix prepended to the unmodified chunk-A prompt, piped via stdin (`scratch/chunk-A-prompt-retry.txt`) using the comma-joined `--allowedTools` fix.
- **Dispatch lesson:** the prompt file's literal chunk-launch command (`--allowedTools "Bash(...)" "Bash(...)" ... "<prompt>"`) fails — `--allowedTools <tools...>` is a commander.js variadic flag that greedily swallows every subsequent bare argument, including the prompt string, leaving no positional prompt and `claude -p` errors `Input must be provided either through stdin or as a prompt argument`. Fix used for all three chunk launches: one comma-joined `--allowedTools "a,b,c"` value, prompt piped via stdin from a scratch file (`< scratch/chunk-<X>-prompt.txt`), never as a trailing positional arg.
- CTO desk ruling relay (Dejan, 07:30 ET, via `cobalt-0c`): Codex weekly allowance at 2% — step 6 (L52-d audit) SKIPPED this build, deferred to before D2 (see `## Audit (L52-d)`).
- ESCALATE: 0 so far.

## §1 STEP-0 — fixture cut + facts (hub, prod reads)

- **Fixture day/names:** `grep -rl "trade_def: rubberband" "Vault/Think/1 - Trading/"` matched only the strategy definition file, no trade note. Per plan fallback: two most-traded pool names of 2026-09-14 by `last_rank` — `SELECT ticker, min(last_rank), count(*) FROM radar_membership WHERE trade_date='2026-09-14' GROUP BY ticker ORDER BY 2,3 DESC` → **FTFT** and **BGFI** (both `last_rank=1`, 2 rows each). The Rubberband replay proof (step 4.2) comes from the dev evaluator's own formation on these bars, never an invented row.
- **i1 bars:** `system.bars` for FTFT/BGFI on 2026-09-14 → 920 rows (FTFT 895, BGFI 25) → `scratch/bars-raw.json` → transform → `tests/fixtures/radar/bars-rubberband.real-shape.json` (dates shifted synthetic anchor 2026-01-06, intraday offsets kept).
- **Daily bars:** no CLI exposes a daily export and the archiver `Interval` enum has no daily value (`archiver/models.py:12-26` — daily/weekly/monthly deliberately never archived) — confirming the plan's STEP-3 gap (a daily collector is new build work, not something to invoke here). Fetched via the existing vault-safe token path (`FinvizApiClient._resolve_vault_credentials`, same machinery `aset/prefill.py` already uses for `p=d`), one hub-side script (`scratch/_fetch_daily.py`, deleted after use, never printed the token) → `scratch/daily-{FTFT,BGFI}.csv` → trimmed to the trailing 40 rows each (real rows; full history is years long and pointlessly large for a fixture) → `tests/fixtures/radar/daily-bars.real-shape.csv` (75 rows, dates shifted).
- **Settings:** `trader_settings` keys `aset.sheet_modes`, `aset.enabled_grades`, `daymode.*` → 7 rows → `tests/fixtures/radar/card-settings.real-shape.json` (no hash fields present in this table; values kept verbatim as the plan directs).
- **STEP-0 facts (verbatim, decide ESCALATE 4):**
  - `system.bars` i1 coverage for the 14 context tickers: `QQQ` (17289 rows, 08-20→09-16) and `XLE` (11002 rows, 08-20→09-14) have live/current coverage; **SPY, IWM, XLK, XLF, XLV, XLY, XLP, XLI, XLB, XLU, XLRE, XLC all stop at 2026-09-03** (last `max(ts)`), i.e. 11 of 14 context tickers have gone stale for 11+ days as of the fixture day. This is a materially incomplete/stale context-ticker set, not merely "unconfirmed."
  - Pool screener CSVs **do** carry a `Sector` column (confirmed: `head -1` of the latest `list-tier_*.csv` under `data/radar-cache/` includes `"Sector"` as the 4th field) — a ticker→sector map is derivable from the existing collector output.
  - Live `trader_settings` F13 values (verbatim): `aset.sheet_modes` = `{full: {A:135, A_plus:345, B:60, C:21, D:0}, half: {A:70, A_plus:170, B:30, C:11, D:0}}`; `aset.enabled_grades` = `[A, B, C]`; `daymode.reduced_enabled_grades` = `[A, B, C]`; `daymode.enabled_modes` = `[reduced]`; `daymode.reduced_sheet` = `half`.
  - **Decision (plan ESCALATE 4 / §L52-a):** sector map is derivable (Sector column present) but the ETF bar feed is stale for 11 of 14 context tickers — STEP-5 ships `sector_alignment`/`market_alignment` defaults as explicit N/A through S2 per the plan's own fallback text; no map is invented, matching R5/ESCALATE-4's instruction not to revive a rejected default.
- **Transform script:** `tests/fixtures/radar/_cut_p2_fixtures.py` (deterministic, re-runnable, reads only from `scratch/`). Proofs: hex-12 source-suffix leak = 0; note-content (`expr:`/`trade_def:`) leak = 0 across `tests/fixtures/`; date-leak grep on the *data* files (`*.json`/`*.csv` only) = 0 (the literal string appears only in the transform scripts' own comments — `_cut_panel_fixtures.py` had the same pre-existing pattern, reviewed and accepted for P3); `git check-ignore` on all 4 new fixture files = no match (none ignored).
- **Rubberband definition/tunables unit:** not copied anywhere in this repo (plan §3, ruling B + Astra R1-21) — confirmed, nothing under `tests/fixtures/` or `scratch/` references the live vault unit path or its content; the `requires_vault` test (run in step 4.3) reads the live unit path directly.

## Launches

| chunk | start | end | exit | report path | suite result | commit |
|---|---|---|---|---|---|---|
| A (recovery relaunch) | 2026-09-16 10:28 ET | 2026-09-16 (same session) | 0 | `docs/40 - DevDocs/reports/s2-p2-build-opus-A-2026-09-16.md` | `1361 passed, 268 skipped` (hub-run, matches builder claim) | wip `3724dd7` (partial), green `ca184c8` |
| B | 2026-09-16 10:38 ET | 2026-09-16 (same session) | 0 | `docs/40 - DevDocs/reports/s2-p2-build-opus-B-2026-09-16.md` | `1465 passed, 278 skipped` (hub-run, matches builder claim) | green `17eb35f` |

## Verification

**Chunk A (STEP-1/2/3):**
- Vault check post-launch: only `1 - Trading/1- Daily Notes/2026-09-16.md` newer than the fresh `.launch-marker` (10:28) — a standing status file other residents/heartbeats write continuously; nothing under `6 - Permanent/`, `1 - Trading/` trade content, or `0 - Projects/Cobalt/`. Not an ESCALATE.
- `git diff main..HEAD -- pyproject.toml uv.lock` — empty, no new dependency.
- Full diff read: migrations 0006 (system)/0007 (user) correctly side-scoped (tenancy, owners, grants, immutability triggers, one-open-card partial unique index); `predicate.py` new grammar module; `radar/anatomy/*` detectors; no POST routes, no templates touched this chunk (chunk C's scope), no credentials, no strategy-note text.
- Secret-literal and template-hazard greps (`password|secret|api_key=`, `alert(|confirm(|.focus(|autofocus|http-equiv`) — 0 hits.
- `uv run pytest -q tests/cobalt tests/taxonomy` (hub-run, independent of builder's claim, L35) → `1361 passed, 268 skipped, 15 warnings in 24.85s` — matches builder report exactly.
- L45 leak scan `tests/cobalt/test_radar_notes.py::test_screen_filter_values_live_only_in_approved_radar_fixtures` (the prompt's literal `-k leak` selector matches nothing — no test is named "leak"; this is the actual L45 gate per plan F21) run by exact name → 1 passed; diff confirms the function body is byte-identical to main (only unrelated lines in the file changed, a new required `context_tickers=` kwarg threaded through call sites).
- `requires_db` / `requires_vault` tests: written, SKIP cleanly offline (268 skipped includes these) — to be exercised in §4 full verification after chunk C, per plan.
- Verdict: GREEN, commit made (see `## Launches`).

**Chunk B (STEP-4/5/6/7):**
- Vault check post-launch (fresh marker 10:38 ET): 5 files newer, all traced to ordinary concurrent vault activity — the CTO desk's own memory files (`cobalt-product-definition.md`, `devices.md`, `cto-desk.md`), the standing heartbeat-driven daily note, and an empty `Untitled 13.md` at vault root (0 bytes — an Obsidian new-note artifact, not something the builder's tool-less, no-`COBALT_VAULT_PATH` process could create). Not an ESCALATE.
- `git diff main -- pyproject.toml uv.lock` — empty, no new dependency.
- New POST routes in `aset/web.py` (`/radar/card/{id}/key`, `/dot/{factor}`, `/promote`, `/release`) land here per the plan's own text (line 210-213, STEP-6) — the plan explicitly places the comprehensive POST-route allowlist test at STEP-8/chunk C (plan row: "P3's GET-only test amended to an explicit allowlist of the new POST routes"), so its absence now is the plan's own sequencing, not a gap. Each new route has its own passing test now, including a dev-opt-in guard (`test_card_routes_refuse_a_dev_instance_without_the_opt_in`).
- Secret-literal and template-hazard greps — 0 hits. Migration diff for the amended 0007 (new `aset_sizings_one_promoted_radar_card` partial unique index) read — idempotent, correctly scoped, owned `cobalt_user`, rollback symmetric.
- `uv run pytest -q tests/cobalt tests/taxonomy` (hub-run, L35) → `1465 passed, 278 skipped, 15 warnings in 32.10s` — matches builder report exactly.
- L45 leak scan re-run (unaffected by this chunk) → 1 passed, function body unchanged.
- Verdict: GREEN, commit pending.

## Second review (agy)

(pending)

## Audit (L52-d)

CTO desk ruling relay (Dejan, 2026-09-16 ~07:30 ET, via `cobalt-0c`): Codex weekly allowance at 2% — no Codex use of any kind until the weekly reset. Step 6 (Codex probe + L52-d recompute) is SKIPPED entirely this build; the desk schedules it before D2.

**Superseding relay (Dejan, 2026-09-16 ~06:50 ET, via `cobalt-0c`, received mid-chunk-B):** step 6 changes from "skip entirely" to "produce the frozen bundle, do not audit here." No codex command at all. At the actual step-6 point (after chunk C build + verification + step 5 agy review, once `cobalt radar audit-export` exists — it's STEP-11, chunk C's build, not present yet): run the bundle export exactly as chunk C builds it (`cp ~/cobalt/.env .env && COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar audit-export --replay <synthetic day> --out scratch/audit-bundle/ ; rm .env`), copy the bundle to `/Users/cobalt/cobalt-wt/agy-trial/scratch/p2-audit-bundle/` (the allow-listed staging worktree, same pattern as step 5's agy staging) plus the plan's STEP-5 section as `/Users/cobalt/cobalt-wt/agy-trial/scratch/p2-step5.md`. No recompute run from this session — that is a separate audit hub (`prompts/2026-09-16/04-audit-house.md`) driving the Gemini house under `agy`.

AUDIT: deferred — bundle staging pending chunk C (`cobalt radar audit-export` doesn't exist until then); will read `AUDIT: bundle staged at /Users/cobalt/cobalt-wt/agy-trial/scratch/p2-audit-bundle/ (<n> files, sha256 of the listing <hash>) — recompute by the Gemini house under agy, run by the audit hub (prompts/2026-09-16/04-audit-house.md), not from here` once done. READY line's audit field = `bundle staged`.

## ESCALATE

Non-blocking, from chunk A builder report (`s2-p2-build-opus-A-2026-09-16.md`), carried for Dejan/desk ruling — build continues:
1. **Out-of-process Finviz consumers outside L53's total-demand plan.** `com.cobalt.prefill-daily` (05:15 ET, premarket) and `com.cobalt.prefill-drc` (15:40 ET, RTH) call the same Finviz transport without the radar resident's bucket, and their `p=d` fetches duplicate the new daily cache; `com.cobalt.archiver` (20:30 ET, pause) doesn't collide. Pre-existing gap, outside this build's F18 scope. Needs a ruling: cross-process demand ledger, prefill reads the daily cache, or accept as is.
2. **Predicate grammar inventory sourced from the last in-repo defs copy (`96c9159^`) plus the plan's named live forms, not the live vault notes.** The `requires_vault` test (hub-run, §4) is the actual gate and must pass before merge — a live-only form would fail `cobalt taxonomy load` for the whole taxonomy (R1-4).

Non-blocking, from chunk B builder report (`s2-p2-build-opus-B-2026-09-16.md`), carried for Dejan/desk ruling — build continues:
3. **Receipt chaining vs R1-10's "stored inputs" wording.** Full per-scan retention of every consumed bar is ~1 MB JSONB/scan at a 50-name pool, so the builder stores `rows_delta` (new/changed rows only) plus a full-set row count + sha256, with unchanged snapshots pointing at the previous receipt; every value is still retained and replay walks the chain. Trade-off: a single receipt is no longer self-contained, and a process restart starts a new full base. Needs a ruling: accept chaining, or store full values per receipt and accept the storage cost.
4. **`curve_unset` suppresses `card_score`.** Any computed N/A untapped dot suppresses the score, so every card is `card_score = NULL` until D2 curves exist or every computed dot is tapped (matches R11's "dark bundle has null scores," but means taps alone cannot rank a card while curves are unset).
5. **Candidate harness needs RVOL data the hub must supply.** `cobalt radar evaluate --candidate` reads RVOL from the day's receipts in `cobalt_dev`, so the hub must copy production dark-run receipts (or equivalent) into `cobalt_dev` before use; `--replay` has no RVOL (dot shows `input_unavailable`, formations unaffected).
6. **D1 deploy ordering + a chunk A migration amendment.** S5 fails loud (`failed_stage='evaluate'`) until `radar.cards_enabled` exists in `trader_settings` — the plan's §6 D1 order already loads dark settings before the `com.cobalt.radar` restart, so this is a confirmation, not a new finding. Also flagging: chunk B amended chunk A's already-committed 0007 migration (new partial unique index) — reviewed above, accepted, not re-escalated.
7. **Alignment dots ship N/A (`DEFAULT_UNRULED`) through S2** — restates STEP-0's own decision (§1 above) and plan §8 item 4; no with/flat/against map computed. Owner and date for the authorizing ruling still owed.

CONTINUE: step 2 chunk C
