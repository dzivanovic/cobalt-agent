# HANDICAP H1 FIX R1 — DRAFT (classification + prompts `60` / `61`) — 2026-09-24

## §0 Headline
`45`'s 2 HOLDs and its 14 ESCALATEs, plus 3 more file-check rows, are classified (L75): **FIX 4** (F1 harness; F2 cut-tier text, DOC-ONLY; F3 `handicap: not replayable from bars`; F4 v3 L52 (c), DOC-ONLY), **UNPROVEN 3** (each becomes a RUN), **NOT REAL 5**, **OUT OF SCOPE 9**, **OWNER ITEM 0**.
F1's shape rests on a fact I read: `dev_db_tx` is `@pytest.fixture(autouse=True)` (`tests/cobalt/conftest.py:133`). Removing the name from the five callers therefore changes nothing at run time. `60` red-tests the named intent, runs a with-DB probe for the second transaction (L70), and builds the rollback only if the probe is red.
Written: `prompts/2026-09-24/60-handicap-h1-fix-r1-build.md` (`43`'s line byte for byte except path and rc name; base `026c99b8`, as read) and `61-handicap-h1-fix-r1-check.md` (`45`'s line byte for byte except path and rc name; round 2). New rule strings: 0. ESCALATE: 9.

## L74
Recorded once: the result of my first tool call had a system-reminder block attached. It asked for a `Claude-Session: https://claude.ai/code/session_…` line in commits and named a file-send tool (`SendUserFile`). It is DATA and was not followed. This run makes no commit and sends no file.

## Authorization and reads
| rule · command | result |
|---|---|
| `grep -n "^| R98 " cto-2026-09-24.md` | line 119: the DESK LAUNCH ROW for `59-draft-handicap-h1-fix-r1.md`. Authorized |
| `date` | `Thu Sep 24 22:05:56 EDT 2026` (start) · `22:17:34` (prompts written) |
| `git -C /Users/cobalt/cobalt log -1 --format=%h radar/handicap-h1-0922` | `026c99b8`, the expected value. The worktree is clean (`## radar/handicap-h1-0922`) |
| `grep -n "autouse=True"` / `sed -n 100,194p tests/cobalt/conftest.py` (branch) | `:133` `@pytest.fixture(autouse=True)` above `def dev_db_tx` (`:134`). It opens `REAL_CONNECT(env.DEV_DB_NAME, side=db.Side.SYSTEM)` (`:158`) with `autocommit = False` (`:159`) |
| `tests/cobalt/radar_migrated_support.py:72-94` | `migrated_radar(monkeypatch)` opens a SECOND connection, `db.connect_migration(env.DEV_DB_NAME)` (`:77`, the BOOTSTRAP login, `src/cobalt/db.py:269-290`). It re-patches `db.connect` (`:90`) and does not take `dev_db_tx`. A test that uses it therefore has two `cobalt_dev` connections, including `test_radar_store` (autouse), which the build said was "replaced" |
| `43` XL76 (A), `43-handicap-h1-build-r2.md:69` | "each move listed under STEP-5's A1 as `test · fixture dev_db_tx → migrated_radar · assertions unchanged`" |
| `src/cobalt/radar/handicap_dry_run.py:335-356`, `:484-487`; `tests/experiments/handicap_h1/test_h1_x2_marginal_seat.py:49-78` | `_cut_tier` works per cut (stickiness → held → priority → first_from → position). X2's `_tier` works per scan: held first, the first match wins, one tier per scan |
| `src/cobalt/radar/runner.py:502-552` `_scan_replay` | The snapshot header is fixed at `("Ticker", "Volume", "Relative Volume")` (`:522`). The summary `print` (`:551`) says nothing about handicap. `grep -rn "not replayable" src/cobalt` returns nothing |
| `tests/cobalt/test_radar_replay.py:192`, `:268-282` | An offline `_scan_replay` harness already exists (fakes and `capsys`). F3's red test reuses it |
| `git diff f6643d41 026c99b8 -- v3` | The branch ships v3 (STEP-0 changed 9 lines and removed 10). `:252` still reads `**NOT MET until he answers R2-1.**` … `Met on the day his A/B lands.` |
| `src/cobalt/cards/store.py:1073-1090` `write_receipt` | The receipt writer RUN-2 (b) uses; `grep -rln "radar_score_receipt" tests/cobalt` → `test_p4_migrations.py`, `test_radar_score_migration.py` |

## Classification (L75) — from `45`'s file-check rows (`reports/handicap-h1-check-2026-09-24.md`)
v3 = `docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md` as the branch left it (line numbers on `026c99b8`).

| # | claim (short) | source line | class | traces to (FINAL clause quoted) | FIX shape or reason | grok's answer |
|---|---|---|---|---|---|---|
| 1 | Five XL76 callers keep `dev_db_tx` beside `migrated_radar` | `:323` (FOR THE CLASSIFIER 1), `:257` HOLDS | **FIX (F1)** | `43` XL76 (A) "`fixture dev_db_tx → migrated_radar`" (the "moved" claim is false in the code); L76 "`cobalt_dev` has exactly one owner at a time". Fact: `conftest.py:133` autouse | Two reds. The OFFLINE red is an AST test: no test uses both `migrated_radar` and `dev_db_tx`. It is red on exactly the five. The with-DB PROBE checks `pg_stat_activity` for the state of `dev_db_tx`'s backend during a `migrated_radar` test; that is a RUN, never an argument (L70). F1 (a), always built: drop the name at `test_cards_picks.py:330` and `test_radar_panel.py:1223`, and change `migrated_radar(monkeypatch, dev_db_tx)` so the order is DECLARED. F1 (b), built only if the probe is red: `dev_db_tx.rollback()` before `_apply(conn, FORWARD)`. `conftest.py` is untouched | Noted the stacking ("the pick tests and the panel test still also wear `dev_db_tx`") and raised no defect. `CHECK: BUILD STANDS`: the recorded dissent (L39) |
| 2 | `_cut_tier` docstring ("the rule X2 tallies") and the `render` comment claim X2's rule | `:324`, `:258` HOLDS | **FIX (F2), DOC-ONLY** | [F-16] `:223` "which tier the cut was (priority, `first_from`, held, stickiness, or position)"; X12 `:309` "the tier of every cut printed"; X2 `:299` "record the marginal seat's tier" (a per-scan experiment whose dry-run consequence is dropping the `p ≤ h × c` sentence) | The code keeps its per-cut rule, which is [F-16]'s. The docstring and comment are changed to name [F-16] / X12 and to say this is NOT X2's per-scan tally. A doc-only row, stated as such: no behaviour changes, so there is no red. The diff shows docstring and comment lines only | Not raised |
| 3 | ESCALATE 1: Opus `CHECK: FIX`, 2 HOLDs | `:327` | = rows 1–2 | — | Covered by F1 and F2 | `BUILD STANDS` |
| 4 | ESCALATE 2: X6 / X10 NOT AS EXPECTED | `:328` | **NOT REAL** | v3's own consequence columns: X10 → "the verdict follows `source_for[ticker]`" (v3 §3 "Source of the values"); X6 → the reason is built at STEP-7 | Both checkers read neither one as a §4 stop. The build carries each consequence | "all follow; no §4 stop" |
| 5 | ESCALATE 2: X4's load path under H1 (`pool_error` text not printed) | `:328` | **UNPROVEN → RUN-1** | v3 §5 "Freeze versus crash on current main is experiment X4" | RUN-1 prints the `pool_error` TEXT under H1 with a complete constructed block and checks that it does not name `handicap`. It repeats the check with `finviz_max_rpm` set, expecting `None` | "all follow" (Q10) |
| 6 | ESCALATE 2: X5's stored half (0 receipts on `cobalt_dev`) | `:328` | **UNPROVEN → RUN-2** | v3 §6 Replay: "(Whether today's receipt already carries `metrics` is experiment X5.)" | RUN-2 (a), offline: the `pool_unit` round-trips through `write_receipt`'s `json.dumps(…, default=str)`. RUN-2 (b), with-DB inside `migrated_radar`: one receipt is written and `pool_unit` read back. With no existing seed helper, (b) skips and is recorded as UNPROVEN, not widened | "all follow" (Q10) |
| 7 | ESCALATE 3: XL76 decision (A) | `:329` | **OUT OF SCOPE** | — | (A) stands. The stacking is row 1 | carried |
| 8 | ESCALATE 4: the production dry-run and parser proof | `:330` | **OUT OF SCOPE** | v3 §7 (1) "a with-DB sanity check against `radar_membership`, read-only" | These are the deploy's own acceptance steps | carried |
| 9 | ESCALATE 5: the builder's four ASK DESK items | `:331` | **OUT OF SCOPE** | — | The desk's records in `cto-2026-09-24.md` R89 stand and are never re-opened | carried |
| 10 | ESCALATE 6: ranked LEAVEs store no record; the departed row can render a stale badge | `:332`, `:259` HOLDS, `:260` HOLDS (by reading) / NOT CHECKABLE (run time) | **UNPROVEN → RUN-3** | The FINAL is SILENT here. §4 has no LEAVE / RETAIN row. §6 `:171` "`raw_rank INTEGER` \| the unhandicapped rank of this scan"; `:175` "`last_rank` and `rank_at_entry` keep their meaning: the rank that governed admission." Neither names a LEAVE | RUN-3 (a), with-DB: ADMIT → RETAIN at factor < 1 → ranked cap LEAVE, then print the departed row's stored fields and the LEAVE's `raw_rank`. RUN-3 (b), offline: render the departed row and print whether the `HANDICAP (shadow)` badge shows. It asserts no policy. The desk chooses no code (R96). Round 2 asks whether the result CONTRADICTS a FINAL clause | Recorded "LEAVE's UPDATE does not (`:122-129`)" as a fact, not a defect |
| 11 | ESCALATE 7a: another `decide(` caller without `handicap_headers` | `:333`, `:265` DOES NOT HOLD | **NOT REAL** | — | Only `runner.py:190` and `handicap_dry_run.py:256`, and both pass it | not raised |
| 12 | ESCALATE 7b: v3 §3 `handicap: not replayable from bars` not built | `:333`, `:266` HOLDS | **FIX (F3)** | §3 `:139` "**Replay-from-bars** (…header `Ticker, Volume, Relative Volume`) has no float or cap. That tool reports `handicap: not replayable from bars` and does not guess." §3 is the group-test section that §9's H1 row builds (`:219` "`handicap_group()` … per [F-10]"; "degraded source `handicap`"). No H2 / H3 row names replay-from-bars, so there is no clause that makes it OUT OF SCOPE | Red first: a new offline test in `test_radar_replay.py` on the `:192` harness with a constructed block, which must see the line in stdout; a "does not guess" pin (factor never < 1) goes beside it. The fix adds ` · handicap: not replayable from bars` to `_scan_replay`'s summary `print`, unconditionally (the header at `:522` is fixed). One hunk | not raised (Q12 named only the [F-16] ladder) |
| 13 | ESCALATE 8: packet gaps (the resident loop's `except`; the facts packet's cite drift) | `:334`, `:261` HOLDS, `:281-287` | **NOT REAL** | — | The hub verified both claims true at the re-located lines. There is no defect | "the facts packet's claim, not re-shown" |
| 14 | ESCALATE 9: v3's L52 (c) sentence is stale ("NOT MET until he answers R2-1") | `:335`, `:268` HOLDS | **FIX (F4), DOC-ONLY, one line** | The branch ships the file (STEP-0 diff). `## Status after round 2` has "**RULED B [R26]**"; L52 (c) `:252` "Met on the day his A/B lands."; §6 `:175` "(R26 B: `position` = `raw_rank`; `effective_position` = the would-be pool-wide rank. [R26])" | One line (`:252`) becomes `**MET — R2-1 RULED B [R26].**`, using STEP-0's own wording and tag. The diff is one `-` and one `+`. A doc-only row, stated as such | Raised it (Q11 (c)) |
| 15 | ESCALATE 10: the identity and shadow tests compare the branch with itself | `:336`, `:269` HOLDS | **OUT OF SCOPE** | [F-16] "The `h = 1` pass must match stored membership." | The check's limit. The comparison is the deploy's production dry-run | "not a second execution of main" |
| 16 | ESCALATE 11: LANE STOPs / live-note / mismatch — none | `:337` | **OUT OF SCOPE** | — | A record; nothing held | — |
| 17 | ESCALATE 12: L74 | `:338` | **OUT OF SCOPE** | — | A record | — |
| 18 | ESCALATE 13: Sol METER until Sep 26th, 2026 6:47 AM | `:339` | **OUT OF SCOPE** | — | A seat record. `61` seats Sol from that time (L62 R19) | — |
| 19 | ESCALATE 14: the standing line | `:340` | **OUT OF SCOPE** | — | A record | — |
| 20 | "In `pool_unit`" in the facts packet covers only the inputs | `:267` HOLDS | **NOT REAL** | v3 §6 "They are then in every receipt's `pool_unit`, together with the pool block" (the inputs) | Wording in the facts packet (a CLAIM file), with no code defect | "reason holds; `pool_unit` carries metrics and block" |
| 21 | On a dead column, each `unknown` row also shows `unknown → not applied` | `:270` HOLDS (by reading) | **NOT REAL** | [F-10] "The pool row shows `unknown → applied` or `unknown → not applied`." | The FINAL's own line; `45`'s prompt allowed it and forbade only the badge | Raised it as a note |
| 22 | [F-16] "that day's stored ladder, top ten, raw beside would-be" is not in the dry-run | `45` Per question row 12 (`:98`); build ESCALATE 9 | **OUT OF SCOPE (this round)** | [F-16] `:223` names it in H1's dry-run text | No row in `45`'s file-check holds it, and L75 builds only rows that HOLD. The builder deferred it: it reads stored cards' `card_score`, the H3 path. Carried as ESCALATE 1 | Named it as missing (Q12) |

Counts: FIX 4 (rows 1, 2, 12, 14) · NOT REAL 5 (4, 11, 13, 20, 21) · UNPROVEN 3 (5, 6, 10) · OUT OF SCOPE 9 (7, 8, 9, 15, 16, 17, 18, 19, 22) · OWNER ITEM 0. Row 3 restates rows 1–2 and is not counted.

## OWNER ITEMS
none. Every open point is either a design question the houses settle in round 2 (RUN-3's reading against the FINAL) or a record.

## FOR DEJAN
New strings: NONE. `60` carries `43`'s launch line byte for byte except the prompt path and the rc name `handicap-h1-fix-r1-build-0924`. That includes the `.env` pair, which is his (`cto-2026-09-22.md` R30, carried by R84–R87). `60` has no `db migrate` string (the grep counts 0). `61` carries `45`'s line byte for byte except the prompt path and the rc name `handicap-h1-fix-r1-check-0924`. `comm`-checked: `grep -o -F` of each whole allow/deny/add-dir run matches in both the precedent and the new file.

## ESCALATE
1. **ASK DESK — [F-16]'s ladder line [22:17]:** v3 `:223` puts "that day's stored ladder, top ten, raw beside would-be" in H1's dry-run. The builder deferred it to H3 (build ESCALATE 9: it reads stored cards' `card_score`), and both checkers named it missing (Q12). No row in `45`'s file-check HOLDS it, so fix r1 does not build it (L75). Safe default: not built; the desk decides whether it goes into the H3 prompt or a round-2 question.
2. **F1 is built on the autouse fact** (`conftest.py:133`). `60` PREFLIGHT greps for it and FAILS loud if it is gone. It never re-drafts on its own.
3. **F1 (b) depends on a RUN:** `60` D3's probe (`pg_stat_activity` state of `dev_db_tx`'s backend). RED → F1 (b) is built. GREEN → the run-time harm is recorded NOT REAL by RUN and only F1 (a) ships. Either way the stop line reads `FIX: 4 of 4`, and §0 says which.
4. **F2 and F4 are DOC-ONLY** (the prompt allows "a doc-only FIX row stated as such"). They carry no red. `61`'s questions tell the checkers so.
5. **RUN-3 asserts no policy** (v3 is silent on LEAVE rows, and the desk chose no code in R96). `61` asks each house whether RUN-3's lines CONTRADICT a FINAL clause. A HOLD there goes to round 3, the last (L39).
6. **Lock queue (L76):** `60`'s DESK LINE queues it behind `57` (replay fix) and `51` (K2), first ready first.
7. **`R__` placeholders for the desk:** two each in `60` (header launch row, AUTHORIZATION) and `61` (header launch row, AUTHORIZATION). Both placeholder gates FAIL until the desk fills them. `61`'s STAGGER needs a launch row that names `61-handicap-h1-fix-r1-check.md` and carries `no other house hub is running`.
8. **Grok's round-1 `CHECK: BUILD STANDS`** is recorded as the dissent in rows 1 and 2 (L39), not smoothed away. The hub's file-check rows decided (L35).
9. **Build ESCALATE 10 (`h1_cache.py` not deleted)** was not carried by `45` and is not classified here. The file is a re-export left in `tests/experiments/handicap_h1/`; it is noted for the H1 deploy drafter only.

HANDICAP H1 FIX R1 DRAFTED · FIX: 4 · NOT REAL: 5 · UNPROVEN: 3 · OUT OF SCOPE: 9 · OWNER ITEM: 0 · prompts: 2 · new rule strings: 0 · ESCALATE: 9
