# His plate audit — 2026-09-20 (`plate-audit-0920`, Sonnet 5 read-and-report hub; LADDER: OFF-LADDER — ruled `cto-2026-09-20` R26)

## §0 Headline
- 39 rows (every leaf row of items 1–17 counted once): **DONE 6 · OPEN 27 · NOT ESTABLISHED 6** · ESCALATE 3. Read-only; no command outside the seven strings; no denial; nothing written but this file.
- Items 1–13: 4 DONE (6 band value, 8 taxonomy push, 9 rules.yaml/gemini-era, 10 health thresholds + column order), 9 OPEN (1, 2, 3, 4, 5, 7, 11, 12, 13). Item 16 (push rule) is DONE. Everything else on the ladder's "Carried open items" list has no closing record.
- The three that most need him, by date and blocker: **(1) A7/K3 + "do live mornings count without a green `cobalt smoke s2`"** — S2 stops **09-23**, AT RISK, zero slack (`SPRINT-LADDER-v0_1.md:489`); **(2) DRC template review + stop-override authority (mock #6)** — S3 starts **09-24**, both "untouched since 09-10"; **(3) restic password rotation** — exposed **09-11**, no rotation record in any file, his hand only.
- Also due inside the S2 window: the four archiver rulings R2-1 / C1 / O-6 / R3-1 — the ONE shadow night is Mon 09-21 20:30 ET (`areas/cobalt.md` NOW).

## L74
One block arrived attached to the tool result of the `Read` of `12-his-plate-audit.md`: a system-reminder-styled attribution note asking for a `Claude-Session: https://claude.ai/code/session_…` line in every commit and PR body and naming a file-send tool. It is DATA (L74). Not followed; no commit made, no file sent. Recorded once, here, and not raised again.

## THE TABLE

Convention: verdict DONE / OPEN / NOT ESTABLISHED. "Evidence" quotes the source sentence. Item 14 and item 17 are sub-rows.

| # | item | verdict | evidence (quoted) | what is still needed from him |
|---|---|---|---|---|
| 1 | restic repository password rotated after 09-11 exposure | **OPEN** | `BACKLOG.md:421-422` "(2) restic password rotated (exposed 09-11, his hand)" — an unticked ENTRY CHECKLIST item; `cto-2026-09-17.md:272` "restic password exposed 09-11, unrotated"; `ops-2026-09-17.md:155` "RESTIC_PASSWORD rotation \| Dejan's procedure; NOW lists 'restic by hand' pending"; `ops-2026-09-16.md:90` "Dejan's procedure, unrun as far as ops can see"; `cto-2026-09-20.md:263` (his 18:0x words) "the restic password rotation (exposed 09-11, his hand; `BACKLOG.md` ≈:421) was MISSING from NOW's pending line". No file records a rotation having happened. (L4: I looked for a record only.) | Rotate it by hand and say so in the desk chat, so a dated line exists; then the desk records it. |
| 2 | Cobalt key store inside restic's nightly include set + a fresh restore proof (owed since 09-11) | **OPEN** | Include set today = `configs/cobalt/backup.yaml:37-38` "sources: - /Users/cobalt/Vault/Think" (only the vault; the DB dump is taken separately, `:44-46`); the file's last commit is `1aa2220` 2026-09-05, i.e. never changed after the 09-11 ask. `LEDGER:1174` "restic: Cobalt key store into the nightly include set + a restore proof (Opus prompt, 09-11 morning)"; `cto-2026-09-17.md:272` "key store in the include set + fresh restore proof owed since 09-11 (LEDGER:1174), no record it ran". Restore proofs on file are pre-ask (09-05/06, `SPRINT-LADDER-v0_1.md:654-655`) or one memory-note restore by the desk 09-17 (`cto-2026-09-17.md:284`, not a key-store proof). | Nothing but a go: it is owed BY A HUB (an Opus ops prompt), owner action = approve that prompt. (I could not establish the key store's on-disk path from reads — see NOT ESTABLISHED.) |
| 3 | Second backup copy / B2 offsite leg — decided? armed? | **OPEN** (decided = DEFERRED; not armed) | `LEDGER:1187` "R5 RESTIC DEFERRED, not owed: no cloud storage purchase while the local storage question is open … Revisit only as restic→rclone→Google Drive … only after measuring vault + dump size against the 15 GB free tier. Removed from OWED; re-enters when that sizing is done." `configs/cobalt/backup.yaml:65-68` b2 leg `enabled: false`, `repo: ""`. `BACKLOG.md:422-423` "(3) second backup copy decided (offsite leg deferred 09-11 R5; one local USB disk today)". No sizing measurement recorded. | A decision when he wants one: keep the single local disk, or ask for the size measurement and an offsite leg. Gated only by the macOS-27 upgrade project (no date). |
| 4 | Trading PC joined Tailscale (S4 precondition) | **OPEN** | Last dated observation `SPRINT-LADDER-v0_1.md:87-91` (09-04) "NO Windows peer on the tailnet … The trading PC has not joined — still his"; `:646` "Trading PC joins Tailscale (his) — S4 precondition"; `:603` S4 preconditions "trading PC on Tailscale (his)"; `BACKLOG.md:125-126` 08-25 the PC reaches the sheet over LAN, "not Tailscale". No later record of a join. CLAUDE.md's environment line still lists the tailnet as "Mac + Fedora laptop + phone". **Conflicting desk sentence:** `cto-2026-09-16.md:123` "every device he uses is on the tailnet" — unsourced, not a tailnet read. | Join it (or say it is joined); the desk then reads `tailscale status` read-only to close it. |
| 5 | `.htk` label check (S4 precondition) | **OPEN** | `SPRINT-LADDER-v0_1.md:603-604` "`.htk` hotkey labels checked against `{DAYMODE}-{KEY}-{SIDE}` (his, mock #3)"; `:647` carried item. No file records the check having been done; `ladder-audit-2026-09-19.md:28` only records the F6 attestation refusal (`.htk` self-report), a different thing. | The check itself (compare the DAS hotkey labels to the pattern) and say so. |
| 6 | Trade-count goal band VALUE ruled by him | **DONE** | Value: `configs/cobalt/taxonomy/tunables.yaml:353-368` `daymode.trade_count_band.min` `value: 2` and `.max` `value: 6`, both `status: solidified` `source: ruling`. Ruling: `areas/cobalt-product-definition.md:19` "[stated 2026-09-16 · Dejan] `daymode.trade_count_band` = 2–6 trades/day, tunable (dynamic)"; `LEDGER:1494` "`ops/2026-09-17` → `3f29250` LIVE (… `daymode.trade_count_band` 2–6)"; over-band rule `cto-2026-09-17.md:194` R13 "ruling A." + validator `cto-2026-09-19.md:309` "(b1) the trade-count band refuses an inverted / non-integer band". Note (not a defect claim): it sits in committed `tunables.yaml`, while `LEDGER:785` lists the band among trader-specific settings; whether that placement is intended is not established here. | Nothing. |
| 7 | `sudo crontab -l` (his) — what for; reported back? | **OPEN** | What for: the 09-03 incident audit — `INCIDENT-2026-09-03-notes.md:1340-1344` "`sudo crontab -l` … could **not** be run — this session is non-interactive and `sudo` requires a password … **The root crontab remains unchecked — run `sudo crontab -l` by hand to close that gap.**" Still listed: `SPRINT-LADDER-v0_1.md:658` "`sudo crontab -l` (his)"; `LEDGER:653` "Still his: sudo crontab -l · boot-contract settings · git push". Reported back: no record after 09-05 in any file read (`LEDGER:705` closed the Gemini-era audit's folders, not this line). | Run it once and paste the output (or "empty") in the desk chat. |
| 8 | Push of `taxonomy/trade-defs-v0_3` — on origin, merged, or gone | **DONE** (merged + branch gone) | `seats-followup-2026-09-08.md:55-56` "only `main` remains (all six branch refs are gone — deleted, not just merged), `main` is `fdf79d0` … `origin/main`...`main` shows 0/0 — fully in sync with the remote"; the stack table there (`:23`, `:31`) shows this branch as the base of the fast-forwarded chain. My `git log` on both `taxonomy/trade-defs-v0_3` and `origin/taxonomy/trade-defs-v0_3` in `~/cobalt` → "unknown revision". | Nothing (remove from the ladder list). |
| 9 | Ruling on `rules.yaml` + the gemini-era vault-side commit | **DONE** (resolved by his own commit; no separate ruling row found) | `git show fdf79d0`: subject "ops+docs: mainframe swap (27B, MLX 1.11.0), rules two-position, ledger §1 fold + 09-05 appendix, card mock, reports, gemini-era archive"; its stat contains `configs/cobalt/rules.yaml \| 9 +-` and 100+ `gemini-era-vault-side/…` paths. `seats-followup-2026-09-08.md:52-54` "Dejan (`dzivanovic`) committed the pending working-tree changes as `fdf79d0`". | Nothing. |
| 10 | Health thresholds (#8) + detail-column order (#11), listed "before S2-P3" | **DONE** | `plan-s2-p3-2026-09-14.md:17` "2026-09-14 R2 health thresholds (#8) \| A per-class, rows at P2 … \| A"; `:18` "R3 detail order (#11) \| A levels first, no chart … \| A". Order: `areas/cobalt-product-definition.md:13` "Detail-column order ruled 09-14: levels → rank + WHY → news → notes → chart slot (empty). Health thresholds per class ruled 09-14". Values on disk: `tunables.yaml:635-705` `card.health.participation_warn 0.70` / `_bad 0.45` · `cost_warn 1.5` / `cost_bad 2.5` · `dot_warn_drop 2` · `dot_bad_max 3` · `structural_warn touched` / `structural_bad lost_on_close`. | Nothing. |
| 11 | DRC template review session (before S3-P3) — held? | **OPEN** | `SPRINT-LADDER-v0_1.md:650` "DRC template review session (Dejan + Claude) — before S3-P3"; `:489` "S3 (09-24) also carries two owed preconditions — the DRC template review and stop-override authority (mock #6) — untouched since 09-10"; `ladder-audit-2026-09-19.md:120-121`; `close-2026-09-19.md:82` "DRC template review + stop-override authority (mock #6) — S3 starts 09-24". No 09-20 row touches it. | Hold the sitting (before S3-P3). |
| 12 | Stop-override authority (mock #6, before S3-P2) — ruled? | **OPEN** | `SPRINT-LADDER-v0_1.md:535` "Stop-override authority (mock #6) — before S3-P2."; `:651`; `ladder-audit-2026-09-19.md:84` "stop-override authority (mock #6) still …"; `close-2026-09-19.md:82`. No ruling row anywhere. | One ruling: who may override a stop, and when (before S3-P2). |
| 13 | Agent SDK spike — any evidence it ran; ruling to run/drop | **OPEN** | `close-2026-09-19.md:31` "`grep -rli "SDK spike"` over `reports/` and `BACKLOG.md` → only `ladder-audit-2026-09-19.md` and `cto-2026-09-19.md` \| still zero evidence it ran"; `:93` "Agent SDK spike has no evidence and no decision since 09-04 — run it before S5 or drop it; scope is his"; `SPRINT-LADDER-v0_1.md:487` "NOT STARTED … Needs an explicit decision (run before S5, or drop) — his." No 09-20 row. | One A/B: run it before S5, or drop/replan. |
| 14a | **A2** — P4 replay deadline scope | **OPEN** | Written: `s2-p4-verify-2026-09-19.md:679` and `:709`; `deploy-p4-2026-09-19.md:862`; `cto-2026-09-19.md:569`, `:584`, R30 (`:43`). Source text: "the execution deadline bounds only async work and the pre-vault-write check; `formation_source()` and `missed.reconcile()` run synchronously past it. Spec R1-16's bar is met … Bounding synchronous DB work is a design change — NOT made this round. Desk/Dejan to rule before or after deploy." **Question:** if a replay run is late, must its two database steps also be cut at the deadline, or is "no late vault write" enough? **Options the source gives:** (A) make bounding synchronous replay/DB work a design item ("whether … becomes a design item, before or after this deploy", `s2-p4-verify:709`); (B) leave as pinned and documented (`runner.py:26-31`, two tests assert the current limitation). **Source recommendation:** none. Deploy 2 has since shipped; it never gated it (`cto-2026-09-19.md:43`). | One A/B. |
| 14b | **A7 / K3** — smoke reads a legitimate frozen HOLD as red | **OPEN** | Written: `deploy-p4-2026-09-19.md:831` (§6.5) and `:862`; `s2-p4-verify-2026-09-19.md:150`, `:705`; `close-2026-09-19.md:74`. Source: "a legitimate frozen HOLD grades red, indistinguishable by column from a RETAIN defect — the ruling is owed before the s2 smoke can be read as a verdict"; it "keeps `cobalt smoke s2` from going green". **Question:** when a card is legitimately frozen (HOLD), should the smoke show it red anyway, or should we change the data so it can tell a frozen HOLD from a real defect? **Options (`s2-p4-verify:150`):** (A) accept the loud false positive + the `degraded_sources` procedure already in `expect_text`; (B) a row-level discriminator (a schema change); (C) revert `b327221`'s RETAIN grading. **Source recommendation:** (A), "recommended". | One A/B/C — blocks reading S2's smoke as green before 09-23. |
| 14c | **ESC 19** — L32 ticker literals in P4 test code | **OPEN** | Written: `deploy-p4-2026-09-19.md:862` "(L32: real tickers pinned in test code — accept the convention or move to predicate lookup)"; `close-2026-09-19.md:75`; `cto-2026-09-19.md:590`. **Question:** the P4 tests name real tickers; keep that as accepted practice, or rewrite them to pick tickers by rule? **Options:** (A) accept the convention; (B) move to predicate lookup. **Source recommendation:** none stated. | One A/B. |
| 14d | **archiver R2-1** — shadow file date ET vs UTC | **OPEN** | Written: `cto-2026-09-19.md:583` "R2-1 — shadow artifact date = ET in this build; UTC is a one-line change if he rules it"; `:623`, `:502`; `close-2026-09-19.md:77`. **Question:** should the nightly shadow file be dated by the Eastern date (as built) or the UTC date? **Options:** ET (built) / UTC (one-line change). **Source recommendation:** none ("his or the tribunal's", `:502`). | One A/B; the first shadow file is Mon 09-21 20:30 ET. |
| 14e | **archiver C1** — `poller_writable` is interval-based | **OPEN** | Written: `archiver-append-build-2026-09-19.md:543` and `:638-640`; `cto-2026-09-19.md:563`; `close-2026-09-19.md:77` "poller_writable is interval-based; shadow `late` is structurally 0". **Question:** should the shadow record say "could the poller write this bar at all" (built — decided from the 1-minute interval), or "was this ticker actually a pool member tonight"? **Options:** (A) keep as built; (B) have the archiver read `system.radar_membership` nightly — "a new read of the radar's tables, which this build did not take on its own authority". **Source recommendation:** none; the desk notes it "decides what the shadow nights can show". | One A/B (before he reads the shadow numbers). |
| 14f | **archiver O-6** — shadow's nightly whole-export read | **OPEN** | Written: `archiver-append-build-2026-09-19.md:561` "whether the shadow's whole-export read every night is acceptable to the owner for N nights \| the OWNER, with O-1 \| `archiver.shadow_compare: on`, `status: proposed`; the key turns it off without a deploy of code"; `cto-2026-09-19.md:323`. (O-1's count was answered by R35, "one shadow night" — `cto-2026-09-19.md:49`; O-6 itself was not.) **Question:** is the extra nightly read of the stored window acceptable for that one night? **Options:** on (as shipped) / off via the key. **Source recommendation:** none. | One yes/no, Mon 09-21 before 20:30 ET. |
| 14g | **archiver R3-1** — design §11 contradicts R25 | **OPEN** | Written: `close-2026-09-19.md:77` "R3-1 (§11 'follows 0006's pattern' contradicts R25)"; `cto-2026-09-19.md:623`; `cto-2026-09-19.md:581` (DB-1 origin: "The FINAL design's §11 contradicts itself"). Built: REVOKE on both tables in 0010/0011. **Question:** edit the design doc's §11 sentence to match what was built (revoke), or leave the contradiction? **Options:** the source states none beyond the implied edit-or-leave. **Source recommendation:** none. | One line: edit §11 or leave (doc-only). |
| 14h | **the 4 MINORs** (all four named) | **OPEN** | `deploy-d3-2026-09-19.md:423`: **G-A1** "the backfill window is built from `hour:minute` so sub-minute overshoot is possible (manual `--backfill` only)" · **G-A2** "a stale pre-existing docstring in `db_migrations/__init__.py` about `--rollback` reversing '0003 then 0002'" · **H1** "the declared backfill window counts the 1.2 s sleeps but not fetch latency, so a long backfill can run past its declared end into the window R34 closed; the midnight clause is untested" · **H2** "the registry docstring says 'nothing asserts contiguity' while the new pin asserts it, and the new pins are tail-anchored, so migration 0012 will redden them". Also `close-2026-09-19.md:76` "none on a scheduled path". **Question:** carry all four to the next ops build, or accept as-is? **Source recommendation:** none ("carried, not fixed"). | One A/B (low stakes). |
| 14i | **`htf_level_proximity` curve** | **OPEN** | Written: `cto-2026-09-19.md:30` R17 "`htf_level_proximity` DIRECTION ruled: closer = higher … (iv) level proximity's END POINTS stay provisional — he ruled the direction only"; `:33` R20 the shipped file has "NO `htf_level_proximity` curve — removed after the house check, nothing invented ships"; `close-2026-09-19.md:80`. **Question:** how close to the higher-timeframe level is a 10, and how far is a 1? **Options:** none in the source (the raw unit is "read from the code by the helper"). **Source recommendation:** none. | Two numbers (or a rule). No curve is live today. |
| 14j | **leg counts** | **OPEN** | Written: `cto-2026-09-19.md:30` R17 readings (i)–(ii), stated back "his to correct": "1–2 legs is 6" read as 2 legs = 6 and 1 leg = 10; "3 or below" read as 3 legs = grade 3, 4+ sliding to 1. Shipped in R20's approved file (`:33`): `Extension.leg_count` [[0,10],[1,10],[2,6],[3,3],[5,1]]. `close-2026-09-19.md:81` "desk readings stated back, his to correct"; `cto-2026-09-19.md:504` "leg-count reading (1 leg = 10?)". **Question:** confirm 0–1 legs = 10, 2 = 6, 3 = 3, 5+ = 1 (4 legs in between)? **Options:** confirm / correct. **Source recommendation:** none. Caveat: R20's "Approve cards" covered the file that carries this curve, which could be read as acceptance — no row says so. | Yes, or the corrected numbers. |
| 15a | R34 C — stagger the pool cadences | **OPEN** (queued, not built) | `BACKLOG.md:68` "R34 C — stagger the pool cadences … NOT STARTED, 'as soon as possible'"; `cto-2026-09-19.md:48` R34 "AND C, queued as soon as possible (not tonight)"; `close-2026-09-19.md:85` "queued 'as soon as possible', not started". No build prompt or branch found in reads. | Nothing from him — a hub build is owed; he already ruled it. |
| 15b | `cmd_migrate` off production | **OPEN** | `BACKLOG.md:70` "`cmd_migrate` must refuse `--allow-prod` unless `COBALT_ENV=production`". Code still as described: `src/cobalt/db_migrations/cli.py:662` `dbname = db.PROD_DB_NAME if args.allow_prod else env.resolve_db_name()` (read in `~/cobalt`). | Nothing — a hub build; already approved as an ops item. |
| 15c | `CLAUDE.md:95` correction (ordered 08-22) | **OPEN** | **What the correction IS:** `docs/20 - Assessment/ASSESSMENT.md:186-187` §4 "'5-pillar schema with memory_logs…' — `memory_logs/graph_*/browser_fast_path/hitl_proposals` come from runtime DDL, the 5-Pillar file is `schema.sql` (13 tables); cosine floor 0.3 is only in `search()`" (same paragraph: `dev_utils/wipe_memory.py` / `reset_memory_table.py` "do not exist"). **Does the line carry it:** no — `CLAUDE.md:95-96` in both `~/cobalt` and this worktree still reads "Memory layer: PostgresMemory ("Hippocampus") — 5-pillar schema (memory_logs w/ 1536-dim embeddings, graph_nodes, graph_edges,"; `topics/memory-system.md:13` "unchanged" after 30 days; `cto-2026-09-20.md:148` "correct `CLAUDE.md:95` (ordered 08-22, never done …)". | Nothing but a go for a small doc edit (CLAUDE.md is a repo file; not vault). |
| 16 | `close-2026-09-19` P1 (allowlist is not a gate) — did R12 settle what he wanted | **DONE** (what he wanted); P1's law text stays unruled by his own word | `cto-2026-09-20.md:15` R1 "P1 not for ruling now, when Fable back, we will set up the desk so it can do push itself on my verbal approval."; `:85` R12 A, "Script has run." (13:31) — `settings.local.json` "2 push allow rules + 9 deny present"; `:207` R24 "push" → "PUSHED: `git push origin main` → `8ea21a6..c686311`". So the push-on-verbal-word path works. P1's exact text was deferred, not refused; nothing is owed until he raises it. | Nothing. (R12's residual — every hub launch line carries `--disallowedTools "Bash(git push*)"` and `UNATTENDED-LAUNCH.md` "line owed" — is a desk item.) |
| 17.1 | Laws consolidation sitting | **OPEN** | `cto-2026-09-20.md:32` R10 "we need a sitting where we bring all the laws … Have it prepared for a sitting and put it on a schedule as pending. So it's continuously reminding us"; `areas/cobalt.md:13` "SITTINGS PENDING (nag until held, R10)". Audit prepared (`laws-audit-2026-09-20.md`); sitting not held. | Hold the sitting. |
| 17.2 | Short-term memory / disk sitting | **OPEN** | `areas/cobalt.md:13` "SHORT-TERM MEMORY/disk — nothing pruned without an emptiness proof"; `cto-2026-09-20.md:41` R11 (memory research ordered instead of a sitting; research is in, the sitting is not held). | Hold the sitting (after the research reports are given in chat). |
| 17.3 | Bars swap: the weekend he names after the rehearsal | **OPEN** (future) | `areas/cobalt.md:11` "swap = full outage on a weekend he names after the rehearsal"; rulings R19–R22. | Name the weekend, after the dev rehearsal. |
| 17.4 | Do the live mornings count without a green `cobalt smoke s2`? | **OPEN** | `cto-2026-09-19.md:409` "OPEN: `cobalt smoke s2` cannot be fully green before P4 merges + one replay cycle — whether the live mornings count without it is in no record."; `:504`. P4 has merged; first replay row is Mon 09-21 21:10 ET. No 09-20 row rules it. | One ruling, before Wed 09-23. |
| 17.5 | Recorded-day smoke A/B (mirror one production day into `cobalt_dev` so his curves grade real cards, or let Monday 04:00 be the first render) | **NOT ESTABLISHED** | Asked: `cto-2026-09-19.md:578` "ONE question to him (12:5x) …"; `:594` "re-asked after them — it only needs an answer by Sunday". I found no answer row; `areas/cobalt.md:9` says "Mon 04:00 = the cards' first render (R27)", which suggests the second option, but R27's text was not read (grep for its row start returned nothing). | Confirm which way it went (probably moot). |
| 17.6 | The `31-packet` / `23-packet` yaml copies leaving the index | **NOT ESTABLISHED** | `close-2026-09-19.md:92` "whether they leave the index before the next push is his word"; `cto-2026-09-20.md:233` two untracked `31-packet` files "left for the close". The push happened (R24) and I did not read the tree's current state. | His word on the two committed `23-packet` copies. |
| 17.7 | Hand herdr to launchd | **NOT ESTABLISHED** | `LEDGER:1334` "OWED — HIM … hand herdr to launchd when dropping the agent tabs costs nothing"; no later record read. | Confirm it is done, or do it (low stakes). |
| 17.8 | File the Qwen Code upstream issue | **NOT ESTABLISHED** | `LEDGER:1334` "file the Qwen Code upstream issue"; also `:1177`. No later record read. | His filing, if still wanted. |
| 17.9 | Unset `COBALT_VAULT_PATH` in his interactive shell | **NOT ESTABLISHED** | `LEDGER:1334` "unset `COBALT_VAULT_PATH` from his interactive shell (it points at the production vault)"; `:1168`. No later record read (a shell state can only be read by asking). | Run `unset` / confirm. |
| 17.10 | OAuth Publish / 7-day re-auth "before 09-15" | **DONE** (moot — the email channel it served was retired) | `LEDGER:1334` "OAuth: Publish or the 7-day re-auth fallback, before 09-15"; `cto-2026-09-16.md:123` "outbound retired 09-14 (Mattermost only, L14 one throat) — stays retired"; `LEDGER:1431` `ops/2026-09-15` "email channel retired in code", merged as `8765d62` (`LAWS.md` L42). `cto-2026-09-17.md:208` "nothing by his hand". | Nothing. |
| 17.11 | Ledger §2/§3/§4 — keep / refresh / retire | **NOT ESTABLISHED** | `LEDGER:1178`, `:1188` R6 "ON RADAR, not ruled … keep, refresh, or retire into the appendix". No ruling read; the laws-consolidation sitting may absorb it. | Fold into 17.1. |
| 17.12 | `bars_legacy` disposal | **OPEN** (not yet due) | `areas/cobalt.md:11` "UNRULED by design: disposal of `bars_legacy` (irreversible, after the swap)". | Later, after the swap. |

## STILL HIS, in time order (OPEN rows only)
- **Mon 09-21, before 20:30 ET** — 14f O-6 (yes/no on the shadow read); 14d R2-1 (ET vs UTC file date); 14e C1 (`poller_writable`). The ONE shadow night is that night.
- **Before Wed 09-23 (S2 stop, AT RISK)** — 14b A7/K3 (A/B/C); 17.4 whether live mornings count without a green smoke; 14a A2 (ruling owed since deploy 2, never gated); 14c ESC 19; 14i htf end points; 14j leg-count confirm (the three cards' curves are live now).
- **Wed 09-23 evening** — bars Lists `[i1]` + writer refusal (ruled; the action falls under the desk, L65) — listed only so it is not missed (`areas/cobalt.md:11`).
- **Before S3-P2 / S3-P3 (S3 starts 09-24)** — 12 stop-override authority (mock #6); 11 DRC template review sitting.
- **Before S5 (no date)** — 13 Agent SDK spike: run or drop.
- **S4 precondition (`SPRINT-LADDER-v0_1.md:600` S4 10-08 → 10-14; `:602` text says "must be true on 10-19")** — 4 trading PC on Tailscale; 5 `.htk` label check.
- **Any time, his hand** — 1 restic password rotation (exposed 09-11); 7 `sudo crontab -l`; 17.9 (see NOT ESTABLISHED).
- **When the OS-upgrade project is scheduled (no date)** — 2 key store include + restore proof (a hub does it; he approves the prompt); 3 second copy / offsite decision.
- **Pending sittings (nag until held, R10)** — 17.1 laws consolidation; 17.2 memory/disk.
- **After the swap** — 17.3 weekend to name; 17.12 `bars_legacy` disposal.
- **Low stakes, no date** — 14g R3-1; 14h the four MINORs.
- **Not his (a hub or the desk owes it)** — 15a R34 C, 15b `cmd_migrate`, 15c `CLAUDE.md:95`.

## DONE — remove from the plate
- **6** band value = 2–6 — `tunables.yaml:353-368` + `areas/cobalt-product-definition.md:19`.
- **8** `taxonomy/trade-defs-v0_3` — merged and its ref deleted, `seats-followup-2026-09-08.md:55-56`; both local and origin refs are "unknown revision".
- **9** `rules.yaml` + gemini-era vault-side — committed by his own hand in `fdf79d0` (09-08).
- **10** health thresholds + detail order — `plan-s2-p3-2026-09-14.md:17-18`; values at `tunables.yaml:635-705`.
- **16** push rule — `cto-2026-09-20.md:85` R12 "Script has run." + `:207` R24 push verified `origin/main..main` = 0. (P1's law text remains deferred, nothing owed.)
- **17.10** OAuth Publish — moot; email retired 09-14, merged `8765d62`.

## NOT ESTABLISHED
What I searched, so nobody repeats it:
- **Item 2, key store path:** `grep -i "key store\|keystore\|key-store"` over `reports/`, `PROJECT-LEDGER.md`, `areas/`, `topics/`; read `configs/cobalt/backup.yaml` whole; `git log` on `backup.yaml`, `src/cobalt/backup`, `ops/run_backup.sh`. No file names the key store's on-disk path or says it was added.
- **17.5:** R27's row in `cto-2026-09-19.md` (the `^| R27 ` grep returned nothing — its format differs); `areas/cobalt.md:9` is the only pointer.
- **17.6:** `cto-2026-09-20.md` for `31-packet` / `23-packet`; the current tree state (not read).
- **17.7 / 17.8 / 17.9:** `grep` for each phrase over `PROJECT-LEDGER.md`, `BACKLOG.md`, `areas/`, `topics/` — only the ledger's OWED-HIM lines (`:1176`, `:1234`, `:1334`) matched; no closing line. A shell's environment and an upstream issue can only be confirmed by asking him.
- **17.11:** ledger and reports for a keep/refresh/retire ruling on §2/§3/§4 — none.
- **Item 4 (contradiction):** `cto-2026-09-16.md:123` says "every device he uses is on the tailnet" — I did not run `tailscale status` (not in the allowlist), so I could not settle it.
- **Not searched (out of the card's "nothing more" scope):** the 09-05/09-15 `_imports` chat exports; `PROJECT-LEDGER.md` appendices before 09-10; the old-tree memory logs; `seat-usage.md`.

## ESCALATE
1. **S2 cannot close cleanly on 09-23 without two rulings from him** — A7/K3 (14b) and "do live mornings count without a green `cobalt smoke s2`" (17.4). The smoke is red by design until Mon 09-21 21:10 ET, and A7/K3 keeps it red after that. `SPRINT-LADDER-v0_1.md:489` calls S2 AT RISK with "NO slack".
2. **Item 4 has two irreconcilable statements** (ladder/`BACKLOG.md`: the PC is not on the tailnet; `cto-2026-09-16.md:123`: every device is). Needs one read of `tailscale status` by the desk or a hub. S4's precondition depends on it.
3. **`CLAUDE.md:95-96` has carried a false fact for 30 days** (15c) and it is the repo root operating contract every house loads; the same paragraph also names two destructive utilities (`dev_utils/wipe_memory.py`, `reset_memory_table.py`) that ASSESSMENT.md §4 says do not exist. A hub edit needs his go.

## CONTINUE
None — the audit is complete; nothing partial.

HIS PLATE AUDIT DONE · items: 39 · DONE: 6 · OPEN: 27 · NOT ESTABLISHED: 6 · ESCALATE: 3
