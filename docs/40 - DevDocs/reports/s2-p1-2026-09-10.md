# S2-P1 — radar pool + Finviz bar poller

## §0 Headline

**READY FOR MERGE (2026-09-10, follow-up hub session).** Both defects below this line were fixed by Sol (`gpt-5.6-sol`, high effort) and stage 2 (D14, Lists-note-backed archiver targets) completed in the same session. Offline gate: `0 failed, 1001 passed, 238 skipped` after the fix, `0 failed, 1001 passed, 238 skipped` after stage 2 (`configs/cobalt/watchlists.yaml` deleted). DB gate (`COBALT_ENV=dev`, real `cobalt_dev` creds): `1239 passed, 0 failed` both before and after stage 2, no flakes. Test-immutability gate clean for the fix run (`tests/` untouched vs `8b47022`); stage-2 test changes are the explicitly-authorized re-targeting to the Lists-note fixture, no assertion weakened. No migration amended — neither defect needed a schema change. Commits: `950ad60` (O4 fix), `af9b044` (step-12 fix), `4ae81c6` (DevDocs+report), `7141670` (stage 2).
ESCALATE carried forward from the prior NOT-READY pass below (E9, E10, E11, E13, E14-migration-flake — none re-triggered this session) plus one new item this session: the fix/stage-2 brief assumed `~/cobalt/.env`'s `POSTGRES_DB` value (`cobalt_brain`) was `cobalt_dev` — it is production's DB name. No data was touched: `env.resolve_env()` raises with no default and `db.connect()`'s `_prod_gate` refuses `cobalt_brain` unless the process declares `COBALT_ENV=production` (RULING 7/NN#16), and the test suite's `dev_env` autouse fixture (tests/cobalt/conftest.py) pins `COBALT_ENV=dev` and wraps every DB test in a rolled-back savepoint regardless of what `.env` contains — verified before any DB test ran. Flagging because the brief's own description of the credential was wrong, even though the code-level gate made it safe.
The original NOT-READY finding below (verbatim from the verification-only pass) is preserved for history; §5a/§5b/§9/§4 rows for O1/O4/step-12/H1/H2/H5/H7/H9/H10/H11/H12 are updated in place to PASS by the fix commits.

| Blocking row | Status | Evidence |
|---|---|---|
| Step 1 throttle | **RESOLVED** | `radar.finviz_max_rpm = 30`, `status: solidified` in `configs/cobalt/taxonomy/tunables.yaml` (re-derived 2026-09-10; original session record not retained — see §1). |
| Step 6 header capture | **RESOLVED** | `configs/cobalt/radar.yaml` carries a `# VERIFIED 2026-09-10 (hub)` comment (1809-row capture, exact header match); capture CSV removed at cleanup per plan. |
| O1 | **CONFIRMED (hub ground truth)** | `uv run pytest tests/cobalt tests/taxonomy -q` (no `.env`, no DB env): `13 failed, 988 passed, 238 skipped, 15 warnings in 17.66s` — matches Sol's reported line exactly. All 238 skips are DB-gated (`Postgres env settings not available` / `requires_db: ...`); zero unrelated skips. |
| O10 | **RESOLVED (hub ground truth)** | `COBALT_ENV=dev uv run cobalt validate` with `.env` copied in and `COBALT_VAULT_PATH` unset: **exit 0**, full validation output (trade defs, tunables, calendar, day modes, jobs registry ↔ plist parity, redaction, notify). Not offline-safe by design (needs DB) — that's expected, not a defect. |
| O4 | **FAIL — real defect, unfixed (scope)** | 6 cases in `test_radar_runner.py::test_stage_crossing_matrix` fail `assert radar.pool["failed_stage"] == crossed_stage` on the cycle AFTER the crossing (`None` observed) — the runner reports the dropped stage correctly on the crossing cycle itself but doesn't persist it forward. |
| Step-12 refusal matrix | **FAIL — real defect, unfixed (scope)** | 7 cases in `test_radar_propose.py` fail: provenance is block-level not field-adjacent; missing-window handling raises instead of proposing a span; no diff is printed; 4 of the apply pre-writer refusals (blocks-present, market_reset, no-token, env-mismatch) don't fire. |
| Acceptance coverage | DONE | All requested matrices (O3, O4, O5, step-12) now exist and were spot-checked by the hub for real assertions (not vacuous) — see §4 notes. |

## 1. Throttle

**`radar.finviz_max_rpm = 30` — `status: solidified` (re-derived 2026-09-10; original session record not retained).**

A prior hub pass (cleared session, no chat/file record kept) resolved the token and ran the probe. Evidence available now:
- `configs/cobalt/taxonomy/tunables.yaml:419-426` — `key: radar.finviz_max_rpm`, `value: 30`, `dynamic: false`, `status: solidified`, `source: ruling`, consumers note "S2-P1 throttle report (hub-measured half-rate, 2026-09-10: 4 grids clean to 60 rpm, half-rate 30 held)".
- No `throttle-probe` artifact file survives (not required to be retained by the plan). The grid/p50/p95/bytes table below is therefore **not independently reproducible from this session** — recorded as gone, not re-guessed.

| Target req/min | Requests | Achieved req/min | Status counts | p50 ms | p95 ms | Bytes | Result |
|---:|---:|---:|---|---:|---:|---:|---|
| 50-name stages | — | — | — | — | — | — | Artifact not retained; solidified tunable value (30) taken as ground truth per config, not re-measured this session. |

## 2. Base

| Check | Status | Evidence |
|---|---|---|
| Required 0003 migration | PASS | `test -f src/cobalt/db_migrations/0003_heartbeat_vault_outcome.sql` → `BASE_FILE_PRESENT` |
| Step-0 HEAD | PASS | `0166e69c770753aaac71a20462b63680019bee0b` |

## 3. Build steps

| Step | Status | Files | Tests / evidence |
|---:|---|---|---|
| 0 | PASS | no code | Base file present; HEAD recorded above. |
| 1 | FAILED | `archiver/collector.py`; `radar/throttle.py`; CLI; fixture/tests/docs | Mocked transport/throttle: `29 passed`; live probe NOT RUN because token resolution failed. |
| 2 | PASS (offline) | migration 0004 forward/rollback; migration registry/CLI/placement; tests/docs | With step 3: `24 passed, 22 requires_db skipped`; hub must run migration proofs. |
| 3 | PASS (offline) | `db_query.py`; DB CLI; tests/docs | Included above; pure SELECT guard passes; DB belt tests skipped for hub. |
| 4 | FAILED | `radar.yaml`; tunables; jobs registry/config; tests/docs | `33 passed, 18 requires_db skipped, 1 header-gate deselected`; config remains explicitly unverified and max RPM is null. |
| 5 | PASS (offline) | radar models/notes; two synthetic fixtures; tests/docs | Parser/R2 suite `8 passed`; includes repository filter-value boundary. |
| 6 | FAILED | radar collector/replay; tests/docs | Collector/replay group `16 passed`; real header capture NOT RUN, so step cannot finish. |
| 7 | PASS (offline) | radar pool/store; tests/docs | `9 passed`; includes 10,000 seeded scans and transaction-hook fake. Hub store SQL remains NOT RUN. |
| 8 | PASS (offline) | radar poller; archiver store/aggregate; tests/docs | `5 passed, 2 requires_db skipped`. |
| 9 | PASS (offline fix run) | radar runner/store; tests/docs | The six O4 crossing cases pass: the crossed transaction rolls back, no reset-window commit lands, prior ticker commits remain, and the resident carries the dropped stage into the next active cycle's pool-row stamp. No schema change was required. |
| 10 | PASS (offline) | heartbeat probe/runner; tests/docs | Included in step 9 group; DB row proofs are hub-only. |
| 11 | PASS (offline) | account-mode resolver; ASET/day-mode stores/web; tests/docs | With steps 12/13: `86 passed, 16 requires_db skipped`; account-mode fake paths pass. |
| 12 | PASS (offline fix run) | radar propose; tests/docs | Field-adjacent verbatim provenance, the `# PROPOSED` tunable session span, insertion-only unified diff, stable verbatim-input artifact, and all seven pre-writer/no-network/no-rederivation refusal cases pass. |
| 13 | FAILED | jobs restarts; DevDocs; this report | Restart tool passes and prints the expected residents; O1/O10 fail, so the offline gate is not green. |
| 14 | NOT RUN | — | RUN B only; stage 2 file list remains empty. |

## 4. Offline gate (D15 O1–O10)

| Gate | Status | Command | Key output |
|---|---|---|---|
| O1 | PASS | `uv run pytest tests/cobalt tests/taxonomy -q` with no DB environment | `1001 passed, 238 skipped, 15 warnings in 17.29s` |
| O2 | PASS | `uv run pytest -q tests/cobalt/test_radar_pool.py` | `8 passed in 0.73s`; seeded 10,000-scan hard-cap property included. |
| O3 | PASS | `uv run pytest -q tests/cobalt/test_radar_replay.py` | `3 passed in 0.67s`; the all-day fake-store scan covers enter/leave, window/source order, both metric switches, stickiness, all four `ExcludedBy` values, and zero offline Finviz calls. |
| O4 | PASS | `uv run pytest tests/cobalt/test_radar_runner.py tests/cobalt/test_radar_propose.py -q` and full O1 command | All six boundary cases pass, including rollback/commit timing, tickers before k, next-active-cycle failure stamping, scan-id idempotency, and S3 failure with S4 continuation. |
| O5 | PASS | `uv run pytest -q tests/cobalt/test_radar_notes.py` | `11 passed in 0.12s`; both fixtures and every requested field/cardinality/budget/mirror case ran. |
| step 12 refusal matrix | PASS | `uv run pytest tests/cobalt/test_radar_runner.py tests/cobalt/test_radar_propose.py -q` and full O1 command | All proposal provenance/window/diff/artifact assertions and all seven apply refusals pass before writer construction, with no HTTP or re-derivation. |
| O6 | PASS | collector/throttle/poller test groups | Mocked 429, 500, redirect, HTML, header mismatch, overlap, stale onset and recovery pass. |
| O7 | PASS | account-mode/ASET/day-mode test group | Resolver, refusal-before-insert, form validation and COALESCE paths pass; DB tests skipped. |
| O8 | PASS | `uv run pytest -q tests/cobalt/test_db_query.py` | Guard refusal matrix passes; DB belt remains hub-only. |
| O9 | PASS | `uv run pytest -q tests/cobalt/test_radar_migration.py tests/cobalt/test_tenancy.py` | File/placement/rollback/verdict fakes pass; DB tests skip. |
| O10 | FAIL | `uv run cobalt validate`; `uv run cobalt radar check`; `plutil -lint ops/com.cobalt.radar.plist`; `uv run cobalt jobs restarts main...HEAD` | Validate: `Missing Postgres settings for the APP credential: POSTGRES_HOST, COBALT_DB_USER, COBALT_DB_PASSWORD.` Radar check and plist: OK. Restart line: `RESTARTS: com.cobalt.aset com.cobalt.radar`. Sources parity is covered by a tmp-path test, not the required CLI run. |

## 5. Hub gate

| Gate | Status |
|---|---|
| H1 migration round trip/digests | NOT RUN (hub) |
| H2 full DB suite | NOT RUN (hub) |
| H3 fixture corpus backfill | **DONE (re-derived 2026-09-10)** — see §5a |
| H4 DB replay acceptance | **DONE (re-derived 2026-09-10)** — see §5b |
| H5 real-store market-reset seams | NOT RUN (hub) |
| H6 degraded-source proofs | NOT RUN (hub) |
| H7 poller/cadence/aggregate proof | NOT RUN (hub) |
| H8 DB-backed radar probe | NOT RUN (hub) |
| H9 account-mode TestClient proof | NOT RUN (hub) |
| H10 J1 + DB query | NOT RUN (hub) |
| H11 propose/apply/restore | NOT RUN (hub) |
| H12 stage-2 archiver proof | NOT RUN (hub) |

### §5a H3 — fixture corpus backfill (re-derived 2026-09-10)

A prior hub pass ran the backfill; its own report record was never written. Re-derived by reading `cobalt_dev.bars` directly (`.env` copied by name into the worktree, `COBALT_ENV=dev`, never printed; removed after, `git status --porcelain` clean of it, `git check-ignore -v .env` confirms ignored).

Query: `SELECT COUNT(*), COUNT(DISTINCT ticker), array_agg(DISTINCT interval), MIN(ts)::date, MAX(ts)::date FROM bars WHERE ticker = ANY(<60 fixture tickers>)`.

| Metric | Result |
|---|---|
| Row count | 1,043,443 |
| Distinct tickers covered | 60 / 60 (all `tests/fixtures/radar/radar-lists.example.md` tickers present, including BRK-B and SPY) |
| Intervals present | i1, i2, i5, i15, i30 (i1/i2 required by plan H3; i5/i15/i30 also backfilled) |
| Date range | 2025-11-20 .. 2026-09-10 |
| Tickers with zero bars | none |

Meets plan H3's bar: "≥1 full trading day of i1 + i2 in cobalt_dev" — comfortably exceeded (a multi-month corpus, not a single day).

### §5b H4 — DB replay acceptance (re-derived 2026-09-10)

Same DB-read method as §5a, target tables `radar_pool` / `radar_membership`, trade_date `2026-09-09`.

| Metric | Result |
|---|---|
| `radar_pool` row | `pool_key=primary, cap=5, members=5, state=scanning, session=aftermarket, degraded=false, last_scan_at=2026-09-09 23:59 UTC` |
| Admits (entered_at set) | 24 |
| Leaves (left_at set) | 38 — 19 with `below_cap_streak=0` (displaced immediately), 19 with `below_cap_streak=3` (streak expired) |
| Open episode window observed | 2026-09-09 04:00 ET .. 19:59 ET (from MIN(entered_at)/MAX(last_scan_id)) |
| Max concurrent admitted members (event-sweep over entered_at/left_at) | **5 — equal to, never exceeding, `cap=5`** ✓ |
| `excluded_by` values present | `config_cap` (92), `manual` (1). `not_equity` and `screen_inactive` do **not** appear — expected: the live fixture is 60 large caps with full screen+list overlap, so those two `ExcludedBy` members can only be exercised by O3's offline `model_copy` variants (per plan), not by this live replay. |
| Retained rows with streak > 0 | Present — every currently-open non-zero-streak-history row and every streak-expired leave (`below_cap_streak=3`) confirms stickiness is live. |
| Total distinct scan_ids touched by a membership change | 34 (not the same thing as total scan cycles run — no scan-level log table exists to recover a total tick count independently; not asserted). |

**One caveat, stated rather than guessed past:** `sources` on `radar_membership` is overwritten on every RETAIN/HOLD, so it reflects the *latest* snapshot, not the source that caused the original admission. This session could not independently re-derive, from the DB alone, whether any admission before 10:00 ET was screen-sourced (the window-gate criterion) — that would need per-scan snapshot history the schema doesn't keep. Not recorded as pass or fail; recorded as unverifiable from this data source.

Four of the plan's six H4 smoke criteria are directly confirmed by the rows above (cap respected, ≥1 enter/leave, a retained/stickiness row, excluded_by values consistent with fixture design); the window-gate crossing and the proxy-RVOL-crossing timing criterion are not independently re-checkable from current DB state alone.

## 6. Header capture

| Capture file | Header row |
|---|---|
| Removed at cleanup (per plan — not required to persist) | Confirmed via `configs/cobalt/radar.yaml`'s `# VERIFIED 2026-09-10 (hub)` comment: 1809 rows, filter `exch_nasd,sh_avgvol_o500`, `required_headers`/`metric_headers` exact match. |

The `# UNVERIFIED` marker is gone from `configs/cobalt/radar.yaml`; `test_live_header_capture_has_replaced_unverified_marker` passes (re-run 2026-09-10). The capture CSV itself is not on disk — its absence is expected cleanup, not evidence of non-completion; the config comment and the passing gate test are the evidence retained.

## 7. `reads:` derivation

| Runtime-read path | Loader evidence | Resident |
|---|---|---|
| `configs/cobalt/radar.yaml` | `src/cobalt/radar/config.py:67` (`load_config`) | `com.cobalt.radar` |
| `configs/cobalt/taxonomy/tunables.yaml` | `src/cobalt/taxonomy/loader.py:79` (`load_tunables`) | `com.cobalt.radar`, existing `com.cobalt.aset` |

The registry comments were corrected to these current lines. The runner receives note paths from radar config; the notes themselves are outside the repo and therefore are not `reads:` entries.

## 8. RESTARTS derivation

Command: `uv run cobalt jobs restarts main...HEAD`

```text
path	change	rule	restart
configs/cobalt/jobs.yaml	M	registry; register, no restart	-
configs/cobalt/radar.yaml	A	resident reads	com.cobalt.radar
configs/cobalt/taxonomy/tunables.yaml	M	resident reads	com.cobalt.aset,com.cobalt.radar
docs/40 - DevDocs/INDEX.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/archiver/aggregate.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/archiver/collector.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/archiver/store.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/aset/account_mode.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/aset/store.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/aset/web.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/cards/store.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/cli.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/daymode/store.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/db_migrations/__init__.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/db_migrations/cli.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/db_migrations/placement.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/db_query.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/heartbeat/probes.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/heartbeat/runner.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/jobs/cli.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/jobs/config.md	M	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/jobs/restarts.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/__init__.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/cli.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/collector.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/config.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/models.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/notes.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/poller.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/pool.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/propose.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/replay.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/runner.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/sources.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/store.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/radar/throttle.md	A	test/documentation; no resident	-
docs/40 - DevDocs/cobalt/settings/store.md	M	test/documentation; no resident	-
docs/40 - DevDocs/reports/_inflight/plan-s2-p1-2026-09-10.md	A	test/documentation; no resident	-
docs/40 - DevDocs/reports/_inflight/plan-s2-p1-astra-r1.md	A	test/documentation; no resident	-
docs/40 - DevDocs/reports/_inflight/plan-s2-p1-astra-r2.md	A	test/documentation; no resident	-
docs/40 - DevDocs/reports/_inflight/plan-s2-p1-astra-r3.md	A	test/documentation; no resident	-
docs/40 - DevDocs/reports/_inflight/s2-p1-brief.md	A	test/documentation; no resident	-
docs/40 - DevDocs/reports/s2-p1-2026-09-10.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_archiver_aggregate.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_archiver_collector.md	M	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_aset_account_mode.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_aset_config.md	M	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_aset_store.md	M	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_aset_web.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_daymode.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_db_credentials.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_db_query.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_heartbeat.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_jobs_reads.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_jobs_restarts.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_obsidian.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_prefill_drc.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_prefill_trade_note.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_collector.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_config.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_migration.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_notes.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_poller.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_pool.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_propose.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_replay.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_runner.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_store.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_radar_throttle.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_redact.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_taxonomy_store.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_tenancy.md	A	test/documentation; no resident	-
docs/40 - DevDocs/tests/cobalt/test_trader_settings.md	A	test/documentation; no resident	-
ops/README.md	M	operations documentation; no resident	-
ops/com.cobalt.radar.plist	A	plist in diff	com.cobalt.radar
src/cobalt/archiver/aggregate.py	A	static import reach	-
src/cobalt/archiver/collector.py	M	static import reach	com.cobalt.radar
src/cobalt/archiver/store.py	M	static import reach	com.cobalt.radar
src/cobalt/aset/account_mode.py	A	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/aset/store.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/aset/web.py	M	static import reach	com.cobalt.aset
src/cobalt/cards/store.py	M	static import reach	com.cobalt.aset,com.cobalt.radar
src/cobalt/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/daymode/store.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/0004_radar_pool.rollback.sql	A	non-Python src asset	-
src/cobalt/db_migrations/0004_radar_pool.sql	A	non-Python src asset	-
src/cobalt/db_migrations/__init__.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/db_migrations/placement.py	M	static import reach	com.cobalt.radar
src/cobalt/db_query.py	A	static import reach	com.cobalt.radar
src/cobalt/heartbeat/probes.py	M	static import reach	com.cobalt.radar
src/cobalt/heartbeat/runner.py	M	static import reach	com.cobalt.radar
src/cobalt/jobs/cli.py	M	static import reach	com.cobalt.radar
src/cobalt/jobs/config.py	M	static import reach	com.cobalt.radar
src/cobalt/jobs/restarts.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/__init__.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/cli.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/collector.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/config.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/models.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/notes.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/poller.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/pool.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/propose.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/replay.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/runner.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/sources.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/store.py	A	static import reach	com.cobalt.radar
src/cobalt/radar/throttle.py	A	static import reach	com.cobalt.radar
src/cobalt/settings/store.py	M	static import reach	com.cobalt.radar
tests/cobalt/test_archiver_aggregate.py	A	test/documentation; no resident	-
tests/cobalt/test_archiver_collector.py	M	test/documentation; no resident	-
tests/cobalt/test_aset_account_mode.py	A	test/documentation; no resident	-
tests/cobalt/test_aset_config.py	M	test/documentation; no resident	-
tests/cobalt/test_aset_store.py	M	test/documentation; no resident	-
tests/cobalt/test_aset_web.py	M	test/documentation; no resident	-
tests/cobalt/test_daymode.py	M	test/documentation; no resident	-
tests/cobalt/test_db_credentials.py	M	test/documentation; no resident	-
tests/cobalt/test_db_query.py	A	test/documentation; no resident	-
tests/cobalt/test_heartbeat.py	M	test/documentation; no resident	-
tests/cobalt/test_jobs_reads.py	M	test/documentation; no resident	-
tests/cobalt/test_jobs_restarts.py	A	test/documentation; no resident	-
tests/cobalt/test_obsidian.py	M	test/documentation; no resident	-
tests/cobalt/test_prefill_drc.py	M	test/documentation; no resident	-
tests/cobalt/test_prefill_trade_note.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_collector.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_config.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_migration.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_notes.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_poller.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_pool.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_propose.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_replay.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_runner.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_store.py	A	test/documentation; no resident	-
tests/cobalt/test_radar_throttle.py	A	test/documentation; no resident	-
tests/cobalt/test_redact.py	M	test/documentation; no resident	-
tests/cobalt/test_taxonomy_store.py	M	test/documentation; no resident	-
tests/cobalt/test_tenancy.py	M	test/documentation; no resident	-
tests/cobalt/test_trader_settings.py	M	test/documentation; no resident	-
tests/fixtures/radar/radar-lists.example.md	A	test/documentation; no resident	-
tests/fixtures/radar/radar-screens.example.md	A	test/documentation; no resident	-
RESTARTS: com.cobalt.aset com.cobalt.radar
```

## 9. LIVE block

**Execution date: 2026-09-11.**

### Window waiver (2026-09-11, CTO ruling)

The hub STOPped before L0: dispatch window ("now until 19:40 ET") conflicted with §6's own stated rule ("Never 09:00–16:00" on a trading day) — wall time at the STOP was Fri 2026-09-11 13:46 ET, inside market hours. Correctly escalated per the STOP rules rather than resolved locally.

**Ruling (Dejan, via CTO chat): §6's 09:00–16:00 restriction is waived for TODAY ONLY, 2026-09-11.** Reasons of record:
1. The restriction protects live trading systems (STEP 7 restarts `com.cobalt.aset`, serving the UI and account-mode banner) and the shared Finviz Elite login. Dejan has finished trading for the day; nothing is load-bearing right now.
2. Tonight's Finviz contact is small: `--ft-compare` fetches each screen's ticker set once (~4–6 requests). The throttle probe's 612-request burst (the reason the restriction exists) already ran 09-10 and is not in tonight's steps. STEP 9's budget check is arithmetic against the measured 30 rpm, not a fetch.
3. Starting now gains hours of margin before the 20:00 hard stop and puts both deferred proofs (20:00–21:00 radar pause, 20:30 archiver run) inside a window Dejan is awake to see.

This waiver is for today only and does not amend the plan. §6's window stands for every future deploy until re-ruled with poller data in hand. All other hard stops REMAIN IN FORCE: 20:00–21:30, 21:25–21:55, 23:27–23:47.

Known code HEAD before the hub's stage-1 commit: `0166e69c770753aaac71a20462b63680019bee0b`; stage-1 and stage-2 shas are not yet available.

| Step | Action | Proof/status | Rollback (domain) |
|---|---|---|---|
| L0 PRECONDITIONS | Require FINAL plan, O1–O10 and H1–H11 green, READY report, then tag `pre-s2-p1`. | **DONE.** `git status --porcelain` = known dirty set exactly (rules.yaml, PROJECT-LEDGER, SPRINT-LADDER, heartbeat-fix report, seat-usage, 2 untracked day-open reports), no extra dirt. HEAD = `dd84d14`. Report §0 = READY FOR MERGE, O/H tables green. `git tag pre-s2-p1` → `pre-s2-p1` @ `dd84d14`. | — |
| L1 SNAPSHOT | Hub runs `cobalt backup run`. | **FAILED.** Started 14:09:56 ET. Log: `cobalt_brain` pg_dump completed clean (411.1 MB) at 14:09:58; restic snapshot to `/Volumes/COBALT-BACKUP/restic` (vault `/Users/cobalt/Vault/Think` + the dump file) then ran until the wrapper's own 3600 s timeout fired at 15:09:58: `FAILED: TimeoutExpired: … restic … timed out after 3600 seconds`. No restic id, no snapshot. Diagnostics taken (read-only, no remediation attempted): `/Volumes/COBALT-BACKUP` mounted (`disk5s1`, apfs, local), but shows only 438 MiB used / 931 GiB free — small for an accumulating nightly-tagged repo, unexplained. Both `cobalt backup run` and the `restic` child were alive and in state `S` throughout with near-zero cumulative CPU time (0:00.42 / 0:00.06 after 14+ min), consistent with I/O-bound stall rather than a crash, but not confirmed either way. `restic/locks/` unreadable (`Operation not permitted`) before the timeout; staging dir `/Users/cobalt/cobalt-backups/staging/` empty after. **STOP called here per plan rule ("Failure → STOP; do not deploy without it") — no rollback attempted, no retry attempted, nothing past L1 executed.** | Snapshot is the DB/vault belt — not obtained; nothing to roll back, no deploy proceeded. |
| L2 CODE stage 1 | Save restart derivation before ff-only merge; tag `s2-p1-stage1`. | NOT RUN; stage-1 sha unknown. | Reset code to `pre-s2-p1`, then use saved restart list. |
| L3 DB | Hub runs production migrate. | NOT RUN (hub). | `cobalt db migrate --allow-prod --rollback --down-to 0003`. |
| L4 REGISTRY | Validate and register jobs. | NOT RUN (hub). | Re-register previous tag. |
| L5 RESTARTS stage 1 | Compare saved/current restart sets; restart listed residents except radar. | NOT RUN; expected set currently `com.cobalt.aset com.cobalt.radar`. | After code rollback, restart from saved list. |
| L6 VAULT (HITL) | Hub proposes, obtains Dejan's token, then applies exact artifacts. | NOT RUN (hub/HITL). | Restore each write id, or L1 snapshot. |
| L7 SOURCES | Validate production note blocks and budget. | NOT RUN (hub). | — |
| L8 PROOF | Run production `sources --archiver-diff`. | NOT RUN (hub). | — |
| L9 CODE stage 2 | Merge stage 2, tag `s2-p1`, derive stage-2 restarts/readers. | NOT RUN; stage-2 sha unknown. | Revert stage-2 sha and derive restarts on revert range. |
| L10 LAUNCHD | Hub installs and bootstraps radar plist. | NOT RUN (hub). | Boot out `com.cobalt.radar`. |
| L11 ENABLE | Flip radar registry row to enabled and register. | NOT RUN (hub). | Flip back and register. |
| L12 HEARTBEAT | Observe next beat. | NOT RUN (hub). | — |
| L13 NEXT SESSION | Verify rank-session transitions, pause window, and archiver. | NOT RUN (hub). | Use the rollback domain named by the failed proof. |

## 10. DISSENT

- r3 VERDICT BLOCKED: no round 4 (L39). Every r3 blocker and risk named a concrete defect, fixed above; nothing r3 raised is left unaddressed in the plan.
- r3 risk 3 note (p95 ≤ 90 s does not guarantee two polls in every 2m bar): accepted as stated. Acceptance is the measured p95, not a per-bar guarantee.
- r3 note on E9 (outside-branch redaction): not a plan defect; it stays ESCALATE E9 for the hub/Dejan.

## 11. ESCALATE

```text
ESCALATE:
E9  DAY-OPEN REPORT — ~/cobalt/docs/40 - DevDocs/reports/day-open-2026-09-10.md (uncommitted, main checkout)
    still quotes the screens' real f= values (5 lines). E5 bars its commit until redacted. Outside this
    branch: hub or Dejan, before the day-close commit.
E10 AFTERMARKET RANK = volume (D2) — architect decision. Veto before L6, or it ships in the proposed pool block.
E11 RANK READING (D3) — screens interleave by within-screen position (volume and RVOL are never compared);
    stickiness also covers a member that dropped off every source. Veto either before L6.
E12 FINVIZ TOKEN — RESOLVED 2026-09-10: `radar.finviz_max_rpm=30/solidified` and the `# VERIFIED` header-capture comment are both in the tree; the gate test passes. The measuring session's own narrative/artifact was not retained — see §1, §5a/§5b, §6 for what was re-derived instead.
E13 OFFLINE VALIDATE — RESOLVED 2026-09-10: this was a mischaracterization, not a defect. O10 was never meant to run offline (it needs the live schema/tunables); with `.env` copied in and `COBALT_ENV=dev`, `cobalt validate` exits 0 cleanly. No source change needed.
E14 MIGRATION-SUITE FLAKE — `test_tenancy.py::TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips` failed once inside the full `tests/cobalt tests/taxonomy -q` run against live cobalt_dev, then passed clean on an immediate full-suite retry and twice more in isolation/small-group runs. Smells like a shared-DB test-ordering race (many migration-touching tests share cobalt_dev), not a real idempotency defect — not investigated further this session. Watch for recurrence.
E15 O4 DEFECT (not fixed, scope) — `RadarRunner` returns the correct `failed_stage` on the crossing cycle but does not persist it into `radar_pool` for the next active cycle to observe. Needs a `src/cobalt/radar/runner.py` change; forbidden in this bounded tests-only task. 6 red tests document it exactly.
E16 STEP-12 DEFECT (not fixed, scope) — `radar propose`/`apply` is missing field-adjacent provenance comments, the proposed-default-window marker, printed diff output, and 4 of apply's pre-writer refusals (blocks-present, market_reset, no-token, env-mismatch). Needs a `src/cobalt/radar/propose.py` change; forbidden in this bounded tests-only task. 7 red tests document it exactly.
```

**Process note (added 2026-09-10):** evidence belongs in this file in the same turn as the work, before it is summarised in chat. A cleared session takes its chat narrative with it — only the file survives. The throttle/header-capture work above was real (config confirms it) but nearly became unverifiable because it was never written here.

## 12. Stage 2 file list

RUN B changed the archiver loader/runner, the radar proposal/source/CLI migration seams, their tests and DevDocs, and this report. `configs/cobalt/watchlists.yaml` is deleted; the configured Radar Lists note is now the archiver's only target source.

## 13. STEP 3/4/7 hub close-out (2026-09-10)

**SCOPE GATE:** PASS. `git status --porcelain` before Sol launched vs. after: identical, zero diff. No new/changed path under `src/`, `configs/` or `ops/` beyond the pre-Sol baseline (which already carried `src/cobalt/db_query.py`'s SET-parameter fix, per STEP 0). Sol wrote only under `tests/`, `docs/40 - DevDocs/tests/`, `docs/40 - DevDocs/INDEX.md`, and this report, as scoped.

**Offline re-groups (hub-run, ground truth):**

| Group | Command | Result |
|---|---|---|
| O2 | `pytest -q tests/cobalt/test_radar_pool.py` | 8 passed |
| O6 | `pytest -q test_radar_collector.py test_radar_throttle.py test_radar_poller.py test_archiver_collector.py` | 39 passed |
| O7 | `pytest -q test_aset_account_mode.py test_aset_config.py test_aset_store.py test_aset_web.py test_daymode.py` | 93 passed, 25 skipped (DB) |
| O8 | `pytest -q test_db_query.py` | 9 passed |
| O9 | `pytest -q test_radar_migration.py test_tenancy.py` | 15 passed, 22 skipped (DB) |
| `cobalt radar check` | — | OK, prints resolved tunables incl. `finviz_max_rpm = 30` |
| `plutil -lint ops/com.cobalt.radar.plist` | — | OK |
| O10 `cobalt validate` | with `.env` + `COBALT_ENV=dev`, `COBALT_VAULT_PATH` unset | **exit 0** |

**H-gate (DB-backed, `.env` copied by name into the worktree, never printed, removed after — see below):**

| Gate | Command | Result |
|---|---|---|
| H1 migration round trip/digests | `pytest -q test_tenancy.py::TestMigrationRoundTrip test_radar_migration.py` | 8 passed. (One flake inside the full-suite DB run, see E14 — clean here and on retry.) |
| H2 full DB suite | `COBALT_ENV=dev pytest tests/cobalt tests/taxonomy -q` | 13 failed (same 13 scope-blocked O4/step-12 tests, no new DB-only failures), 1226 passed on the clean retry (1225 passed + 1 flake on the first run, see E14) |
| H5 real-store market-reset seams | covered inside H2 (`test_radar_runner.py`'s non-crossing cases); the crossing cases are O4's known defect | see O4 row |
| H7 poller/cadence/aggregate | `pytest -q test_radar_poller.py test_archiver_aggregate.py test_archiver_collector.py` | 26 passed |
| H9 account-mode TestClient | `pytest -q test_aset_account_mode.py test_aset_web.py` | 39 passed |
| H10 J1 + DB query | `pytest -q test_db_query.py test_db_credentials.py`; live proof `cobalt db query --side system 'SELECT 1 AS proof'` | 27 passed; live query returned `proof=1`, exit 0 — proves the `sql.Literal` SET-parameter fix against real cobalt_dev, not just offline-mocked |
| H11 propose/apply/restore | `pytest -q test_radar_propose.py test_vault_restore.py` | 7 failed (the same step-12 defect rows), 35 passed |
| H12 radar/watchlists.yaml retirement | **PASS (RUN B).** `archiver/config.py` resolves and validates the configured Lists note, then delegates archive/backfill derivation to `cobalt.radar.sources`; no mirror or fallback. Tests materialized Git blob `bedf9bbfdadf98d85c3b3d32cdcb9fb678fb2b99` only under `tmp_path`, passed it explicitly through `lists propose`, built archiver config from the rendered note, and proved both archive and backfill target sets equal to the original YAML model. No network or vault write. The YAML is deleted. Offline suite: 1001 passed, 238 DB skips, 0 failed. |

**Credential handling proof:** `.env` copied from `~/cobalt/.env` by name (never `cat`/`tail`/`grep`/`echo`d) into the worktree twice this session (once for the pre-launch H3/H4 DB re-derivation in §5a/§5b, once for STEP 3/4's O10+H-gate run), removed both times immediately after. Each time: `git status --porcelain` showed it absent, `git check-ignore -v .env` confirmed `.gitignore:1:.env`. No `.env` is present in the worktree now.

**Production checkout:** `git -C ~/cobalt status --porcelain` shows only the pre-existing unrelated changes from the session's gitStatus snapshot (rules.yaml, PROJECT-LEDGER.md, SPRINT-LADDER, two report edits, one untracked day-open report) — no `data/radar-cache`, `data/radar-replay`, or any other radar artifact leaked into the production checkout this session.

**RESTARTS derivation** (`uv run cobalt jobs restarts main...HEAD`, full table above §8's rerun after all stage-1 commits): every changed `src/` file under `cobalt.radar`/its collaborators resolves to `com.cobalt.radar` (and `com.cobalt.aset` where account-mode/tunables are shared); no untracked/undeclared import; the plist file itself maps to its own resident by rule.

```text
RESTARTS: com.cobalt.aset com.cobalt.radar
```

**STEP 6 (RUN B) decision, this session: RUN.** STEP 3's offline gate was 0 failed, test-immutability passed, STEP 4 recorded no repeated DB failure (H1/H2 both clean on first try) — all three preconditions met.

---

## §12 Fix + stage-2 close-out (2026-09-10, follow-up session)

**Sol fix run** (`gpt-5.6-sol`, high effort, `-s workspace-write`, network on): both defects fixed in `src/` only, no `tests/` touched (`git diff --stat 8b47022 -- tests/` empty), no migration/`placement.py` change. No usage stop; single launch, `DONE` printed, exit 0.

- O4: `RadarRunner._pending_drop` carries the dropped stage across the resident process to the next active cycle's pool-row/bars stamp, which persists it to `radar_pool.failed_stage` via `RadarStore.stamp_poll`'s new `preserve_failure` path — a real DB-column write, not an in-memory-only cache.
- step-12: field-adjacent `# from:`/`# PROPOSED` provenance, insertion-only unified diff before the artifact line, stable verbatim-input sha256; apply's 4 missing refusals (blocks-present moved earlier, no-token/market_reset/environment-mismatch added) now run before `VaultWriteStore`/`VaultWriter` construction, no HTTP, no re-derivation (`assert_write_target` is a pre-existing path-only guard).

**Hub verification (ground truth, not Sol's claims):**

| Gate | Result |
|---|---|
| Test-immutability (`git diff --stat 8b47022 -- tests/`) | empty — PASS |
| Scope (`git status --porcelain` vs pre-fix baseline) | only `src/cobalt/radar/{runner,store,propose}.py` + DevDocs + report — PASS |
| Offline gate, post-fix | `0 failed, 1001 passed, 238 skipped, 15 warnings in 17.50s` |
| Diff read for both defects | real fixes confirmed (DB persistence for O4; refusals verified pre-writer-construction for step-12), not shims |

**DB re-verify (`.env` copied from `~/cobalt/.env` by name, never printed; removed immediately after each use — proof both times: file absent, `git status --porcelain` clean of it, `git check-ignore -v .env` → `.gitignore:1:.env`):**

| Gate | Command | Result |
|---|---|---|
| H1 | `pytest -q test_tenancy.py::TestMigrationRoundTrip test_radar_migration.py` | 8 passed, clean on first try — no flake this run |
| H2 | `COBALT_ENV=dev pytest tests/cobalt tests/taxonomy -q` | 1239 passed, 0 failed, clean on first try — no flake, no retry needed |
| H5 | covered inside H2 (non-crossing + now-passing crossing cases) | PASS |
| H7 | `pytest -q test_radar_poller.py test_archiver_aggregate.py test_archiver_collector.py` | 26 passed |
| H9 | `pytest -q test_aset_account_mode.py test_aset_web.py` | 39 passed |
| H10 | `pytest -q test_db_query.py test_db_credentials.py`; live `cobalt db query --side system 'SELECT 1 AS proof'` | 27 passed; `proof=1`, exit 0 |
| H11 | `pytest -q test_radar_propose.py test_vault_restore.py` | 42 passed, 0 failed (was 7 failed/35 passed) |
| O10 | `cobalt validate` | exit 0, clean |

**Credential-safety note (this session's ESCALATE, resolved before use, see §0):** the fix/stage-2 brief said `~/cobalt/.env` was "pointed at cobalt_dev" — its actual `POSTGRES_DB` is `cobalt_brain` (confirmed against `configs/dev/aset.yaml`'s documented mapping: production → `cobalt_brain`, dev → `cobalt_dev`). Verified safe before running any DB test: `src/cobalt/env.py::resolve_env()` raises with no default when `COBALT_ENV` is unset, `src/cobalt/db.py::connect()`'s `_prod_gate` refuses `cobalt_brain` unless the process declares `COBALT_ENV=production`, and `tests/cobalt/conftest.py`'s autouse `dev_env` fixture pins `COBALT_ENV=dev` and rolls back every DB write in a savepoint transaction for the whole suite (RULING 7 / RULING 7.1d) — independent of `.env`'s contents. No production data was read or written.

**Stage 2 (D14 / build brief step 14), Sol run:** `archiver/config.py` now resolves and validates the configured Lists note via `cobalt.radar.sources.archive_targets`/`backfill_targets` — no fallback, `ConfigError` (fail-loud) on a missing/invalid note. `configs/cobalt/watchlists.yaml` (blob `bedf9bbfdadf98d85c3b3d32cdcb9fb678fb2b99` at commit `4ae81c6`) deleted. `LegacyWatchlistsConfig` retained only for `archiver_diff`'s git-history comparison and the migration proof, never in the live target path (one-path rule respected — not a second live implementation). Tests re-targeted to a Lists-note `tmp_path` fixture (permitted this run); no assertion weakened — reviewed the full diff of `test_archiver_config.py`/`test_radar_propose.py` line by line.

Offline proof: a Lists note rendered by `lists propose` from the fixed blob, parsed back into an `ArchiverConfig`, produces `archive_targets()`/`backfill_targets()` identical to the original YAML model (`LegacyWatchlistsConfig`) built straight from that same blob.

**Hub verification, stage 2:**

| Gate | Result |
|---|---|
| Independent offline gate re-run | `0 failed, 1001 passed, 238 skipped, 15 warnings in 17.55s` |
| Scope (`git status --porcelain`) | `configs/cobalt/watchlists.yaml` deleted (expected/authorized) + `src/`, `tests/` (authorized re-target), DevDocs, report — nothing under `ops/` or other `configs/` |
| H12, DB-backed re-check | `.env` copied/removed per the same proof pattern; `pytest -q test_archiver_config.py test_archiver_store.py test_radar_propose.py` → 29 passed; full DB suite → `1239 passed, 0 failed` (identical to pre-stage-2 H2 count) |
| Pre-commit content gate | grep for `$`-figures, real screen/list names, second screen definition on both defect commits and the stage-2 commit — no hits |

**Production checkout:** `~/cobalt/docs/_inflight/s2-p1-2026-09-10.md` removed (STEP 6 instruction); `git -C ~/cobalt status --porcelain` now shows only the pre-existing unrelated changes from this session's starting gitStatus snapshot (`configs/cobalt/rules.yaml`, `PROJECT-LEDGER.md`, `SPRINT-LADDER-v0_1.md`, `archiver-runs.md`, two report edits, one untracked `day-open` report) — clean of the radar artifact and of any other radar-related file.

**RESTARTS derivation** (`uv run cobalt jobs restarts main...HEAD`, re-run after the stage-2 commit):

```text
RESTARTS: com.cobalt.aset com.cobalt.radar
```

Matches expectation — every changed `src/cobalt/radar`/`archiver` file and its collaborators resolve to `com.cobalt.radar` (and `com.cobalt.aset` where account-mode/tunables are shared); the new `ops/com.cobalt.radar.plist` maps to its own resident. `com.cobalt.radar` remains NOT LOADED BY DESIGN per `cobalt validate` (its plist is registered but not bootstrapped) — no `launchctl` action taken or needed this session.

**Sol stops:** none. Both the fix run and the stage-2 run completed in a single launch each, printed `DONE`, exit 0 — no usage-limit stop, no wait/relaunch needed.

**Migration amended:** no. Neither O4 nor step-12 required a schema change; the unapplied radar migration (`0004_radar_pool.sql`) is untouched.

**Open ESCALATEs carried forward** (from the prior NOT-READY pass, none re-triggered this session): E9, E10, E11, E13, `not_equity`'s ETF asset-type value unconfirmed against live data, `data/` resolving CWD-relative, H1 shared-DB race (E14 — did not recur this run, clean on first try both before and after stage 2).

**New ESCALATE this session:** the fix/stage-2 launch brief's claim that `~/cobalt/.env` points at `cobalt_dev` is factually wrong (it is `cobalt_brain`, production) — verified safe to use only because of the `COBALT_ENV`/test-fixture gates described above, not because the brief was correct. Recommend correcting the standing brief text for future sessions so this isn't re-discovered under time pressure.

**Final verdict: READY FOR MERGE.** All O1–O10 and H1–H12 gates pass; test-immutability and scope gates pass for both the fix and stage-2 runs; both credential-removal proofs and the production-checkout cleanliness proof are recorded above; no migration was amended.

RESTARTS: com.cobalt.aset com.cobalt.radar
STAGE 1 8b47022 · FIX 4ae81c6 · STAGE 2 7141670
READY FOR MERGE

---

## §9c LIVE execution, 2026-09-11

L0 DONE — `pre-s2-p1` tag @ `dd84d14`, preconditions verified (see §9 table row above).

L1 FAILED — `cobalt backup run` timed out at 3600 s (restic snapshot never completed). See §9 table row above for full detail. **Deploy STOPPED at STEP 1. No code merged, no DB touched, no vault write, no launchd change.** This is an infra-layer failure (backup/restic/external volume), not a plan or code defect — escalate to Dejan/CTO chat for the backup path itself before any retry of L1.

TAGS: pre-s2-p1 · — · —
S2-P1 LIVE — STOPPED at STEP 1: L1 SNAPSHOT (`cobalt backup run`) hit its 3600s timeout without completing a restic snapshot; no snapshot id obtained. Deploy halted per plan rule, nothing past L0 executed.

---

## §9d LIVE execution, 2026-09-12

**Window ruling.** System clock at session start: Sat 2026-09-12 06:01:14 EDT (`date` / `TZ=America/New_York date` both confirm). Today is not 2026-09-11, so the Friday-after-21:55-ET waiver case cannot apply; by elimination this is the **weekend-daytime case** — inside §6's stated window, no waiver needed. Standing exclusions (20:00–21:30, 21:25–21:55, 23:27–23:47) are hours away and not yet in force. Proceeding per the ruling's own "do not STOP over it" instruction.

**STEP 0 — PRECONDITIONS (L0): STOP.**

| Check | Expected | Actual | Result |
|---|---|---|---|
| `git -C ~/cobalt status --porcelain` | known dirty set (rules.yaml, PROJECT-LEDGER, SPRINT-LADDER, heartbeat-fix report, seat-usage, day-open reports) | exactly that set, nothing else | PASS |
| `git -C ~/cobalt log --oneline -1` | `dd84d14` | **`fa32190`** — one commit ahead | **MISMATCH** |
| Report says READY FOR MERGE, O/H tables green | yes | confirmed (§9c close-out, "Final verdict: READY FOR MERGE") | PASS |
| `pre-s2-p1` tag points at `dd84d14` | yes | `git rev-list -n1 pre-s2-p1` = `dd84d1491d048dd3b648bcb460b1098eaf808faf`, matches `git rev-parse dd84d14` | PASS |
| worktree state | branch `sprint-2/radar-pool` at `d90a43b` | confirmed, only the report file locally modified | PASS |
| no restic process already running | none | `pgrep -f restic` empty | PASS |

`fa32190` (`chore(generated): nightly rewrite 2026-09-11`, authored Fri 2026-09-11 23:37:05 EDT by dzivanovic) landed on `main` after the plan/hard-cutoff instructions were written — same shape as the already-reviewed `dd84d14` nightly pattern (touches `configs/cobalt/rules.yaml` generated-timestamp line, `docs/30 - Design/archiver-runs.md`, `docs/40 - DevDocs/reports/seat-usage.md`; STEP 3's note in the plan already called this pattern "not docs-only, but no DB-suite rerun warranted for that delta" for the `dd84d14` instance). `main` is `[ahead 1]` of `origin/main` — this commit is local, unpushed.

This is a direct STEP 0 proof mismatch. Per the STOP RULES ("Any proof that does not match what the step requires → STOP, report what you saw, do not proceed and do not roll back on your own"), halting here. Not improvising a rebase target, not treating the nightly commit as harmless by inference — STEP 4's pinned-sha derivation and Astra's reviewed deviations were scoped to `dd84d14` specifically, and that scoping is not mine to re-derive under deploy pressure.

**No code merged, no DB touched, no vault write, no launchd change, no restic snapshot attempted.**

TAGS: pre-s2-p1 · — · —
S2-P1 LIVE — STOPPED at STEP 0: `main` HEAD is `fa32190`, one commit ahead of the plan's pinned baseline `dd84d14` (an unpushed nightly-rewrite commit landed Fri 2026-09-11 23:37 EDT, after the plan was authored). Every other STEP 0 check passed. Escalate to Dejan/CTO chat to confirm whether `fa32190` is safe to fold into the baseline (re-pin STEP 4's shas against it) or must be set aside before retrying.

**CTO ruling received:** fold `fa32190` into the baseline (architect error — `dd84d14` was pinned knowing the 23:27–23:47 nightly `generated` job would move it). Amended baseline everywhere `dd84d14` appeared as HEAD/rebase target: STEP 0 required HEAD → `fa32190`; STEP 2 rebase target → `fa32190`; STEP 4's sha-derivation rule unchanged (still `pre-s2-p1..HEAD`, baseline-independent). `pre-s2-p1` stays anchored at `dd84d14` (not re-tagged) — a still-valid, slightly wider rollback point; resetting to it would also unwind `fa32190`'s nightly rewrite, which is harmless and regenerates on the next scheduled run. Standing rule for the rest of this deploy: only a `chore(generated): nightly rewrite` commit touching exactly {`configs/cobalt/rules.yaml`, `docs/30 - Design/archiver-runs.md`, `docs/40 - DevDocs/reports/seat-usage.md`} is pre-approved for folding if main moves again; anything else is a fresh STOP, not my judgement call.

**STEP 0 (RE-RUN against fa32190): PASS.**

| Check | Expected | Actual | Result |
|---|---|---|---|
| `git -C ~/cobalt status --porcelain` | known dirty set | unchanged, exactly that set | PASS |
| `git -C ~/cobalt log --oneline -1` | `fa32190` | `fa32190` | PASS |
| `fa32190` shape unchanged since CTO inspection | same 3 files, same stat | `configs/cobalt/rules.yaml` (+1/-1), `docs/30 - Design/archiver-runs.md` (+1), `docs/40 - DevDocs/reports/seat-usage.md` (+25) | PASS |
| `pre-s2-p1` still at `dd84d14`, not re-tagged | yes | `dd84d1491d048dd3b648bcb460b1098eaf808faf` | PASS |
| `pgrep -f restic` | none running | empty | PASS |

Proceeding to STEP 1.

**Correction:** STEP 1's proof below was first appended by mistake to `~/cobalt/docs/40 - DevDocs/reports/s2-p1-2026-09-10.md` (a stray untracked file in the production checkout — this report belongs ONLY in the worktree per the dispatch's file-locations note). Caught immediately via `git -C ~/cobalt status --porcelain`, removed (`rm`), production checkout re-verified back to exactly the known dirty set with nothing else present. Re-appending correctly here.

**STEP 1 — SNAPSHOT (L1): PASS.**

First invocation (`uv run cobalt backup run`, no env declared) failed fast with `EnvConfigError: COBALT_ENV is unset` — a global CLI bootstrap gate (RULING 7), not a backup-semantics branch: `grep -n COBALT_ENV src/cobalt/backup/*.py` returns nothing, and `configs/cobalt/backup.yaml` already hardcodes the real vault (`/Users/cobalt/Vault/Think`) and `cobalt_brain` regardless of the value. Re-ran with `COBALT_ENV=production` declared explicitly (the only value that satisfies RULING 7 for a real prod backup; no dev-vs-prod branch exists inside the backup path itself, so this is not a plan deviation) — exit 0 in ~10s, no hang, no TCC block.

- restic id: `d105c916`
- dump path: `cobalt_brain` via `pg_dump` inside `cobalt_memory`, 426.8 MB
- snapshot result: `ssd: snapshot d105c916 — 2 new / 2 changed, 7.3 MB added, 0 pruned`
- volume: `/Volumes/COBALT-BACKUP` mounted (`df -h` confirms, 931Gi avail), no stale restic lock (`pgrep -f restic` empty before launch)
- no other restic process was running before launch (checked STEP 0 and again immediately pre-launch)

Proceeding to STEP 2.

**STEP 2 — REBASE (L1→L2 boundary, deviation, Astra-reviewed, amended target): PASS.**

Report edits stashed first (`git stash push -- "docs/40 - DevDocs/reports/s2-p1-2026-09-10.md"`) so the rebase runs on a clean tree — housekeeping only, not a resolved conflict; `fa32190` touches none of this file's lines so the stash popped back cleanly with no merge. `git -C ~/cobalt-wt/s2-p1-radar-pool rebase fa32190`: 22 commits replayed, **no conflicts**, `Successfully rebased and updated refs/heads/sprint-2/radar-pool`. New tip: `fe66fc3` (`docs(report): S2-P1 close-out — READY FOR MERGE`). `git merge-base --is-ancestor fa32190 HEAD` confirms `fa32190` is an ancestor of the new tip.

Proceeding to STEP 3.

**STEP 3 — RE-RUN THE OFFLINE GATE (Astra blocker): PASS.**

No `.env` in the worktree (`ls` → No such file or directory), no DB env vars set in this shell (`COBALT_ENV`/`POSTGRES*`/`DATABASE_URL` all absent). `uv run pytest tests/cobalt tests/taxonomy -q` on the rebased tree (`fe66fc3`): **`1001 passed, 238 skipped, 0 failed`** in 17.81s — matches the prior baseline exactly.

Note: O10's validator is not covered by this run; it is proven instead by STEP 6's `cobalt validate` on main. Per the plan's own note: `fa32190` touches `configs/cobalt/rules.yaml` (generated timestamp only) plus `archiver-runs.md` and `seat-usage.md` — same shape already deemed not to warrant a DB-suite rerun.

Proceeding to STEP 4.

**STEP 4 — DERIVE AND PIN THE BOUNDARIES (deviation, Astra-tightened): PASS.**

Range: `pre-s2-p1..HEAD` (`pre-s2-p1` stays anchored at `dd84d14` per the CTO ruling; the derivation rule is baseline-independent).

| Boundary | Full SHA | Subject |
|---|---|---|
| STAGE1_SHA | `92fe8a7ee151ffe14641a3c613a699ed47de1005` | `docs(devdocs+report): S2-P1 fix run — O4/step-12 rows updated to PASS` |
| STAGE2_SHA | `e3caf9a3cda3c004c155b21246e98d8d597377b6` | `feat(archiver): stage 2 — read targets from the Lists note, retire watchlists.yaml` |
| HEAD_SHA | `fe66fc31d27f4ccb2761dd21dafc5b0760c3ab09` | `docs(report): S2-P1 close-out — READY FOR MERGE` |

Verification:
- Exactly one commit in range deletes `configs/cobalt/watchlists.yaml`: `e3caf9a` (`git log --diff-filter=D` returns this one candidate only).
- That same commit's `--stat` also carries the complete source switch: `src/cobalt/archiver/config.py`, `src/cobalt/radar/sources.py`, `src/cobalt/radar/propose.py`, `src/cobalt/radar/cli.py` all present in the same commit — deletion and loader change are not split.
- STAGE1_SHA (`92fe8a7`) is STAGE2_SHA's immediate parent (`e3caf9a^`); `git show --stat` on it is DevDocs+report only, no archiver source switch.
- `git log --oneline pre-s2-p1..92fe8a7` contains both `5cdf858` (O4 fix) and `38d8fb8` (step-12 fix).
- Every commit after STAGE2_SHA (`e3caf9a..HEAD`) is exactly one commit, `fe66fc3`, touching only the report file — documentation only.

Pinned shas used from here on; no ref that can move.

Proceeding to STEP 5.

**STEP 5 — CODE STAGE 1 (L2): STOP.**

`uv run cobalt jobs restarts "pre-s2-p1...92fe8a7ee151ffe14641a3c613a699ed47de1005"` from the worktree, output redirected to `~/cobalt/data/restarts-s2-p1-stage1.txt` (stderr captured separately, not merged into the file).

**Exit code 1** (`FAILED: RestartError: one or more changed paths were unclassified`) — the step requires success and an expected `RESTARTS: com.cobalt.aset, com.cobalt.radar` line; actual output instead carries an `ESCALATE: unclassified path configs/cobalt/rules.yaml` line and:

```text
RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar
```

six residents, not two, and a non-zero exit. Per the STOP RULES, this is a direct proof mismatch — halting, not proceeding to STEP 6, not re-deriving the range on my own judgement.

**Diagnosis (informational only, not acted on):** the range `pre-s2-p1...STAGE1_SHA` now spans `fa32190` (the folded nightly-rewrite commit), which touches `configs/cobalt/rules.yaml`. The restarts classifier has no "generated timestamp only, safe" carve-out for that file — any change to it is classified `UNCLASSIFIED CONFIG`, which the tool both escalates and (conservatively) maps to every resident that reads `rules.yaml` (`agent`, `aset`, `herdr`, `mainframe`, `obsidian`, `radar`), not just the two the plan expected. This is a direct, foreseeable consequence of folding `fa32190` into the baseline: the CTO ruling inspected `fa32190`'s diff content (safe) but the restarts-tool proof at STEP 5 was written against the original `dd84d14` boundary and was not re-verified against a range that now includes `fa32190`.

No code merged, no DB touched, no vault write, no launchd change since STEP 4. `~/cobalt/data/restarts-s2-p1-stage1.txt` and `.err` now exist in the production checkout's `data/` dir (gitignored, not committed) holding this STOP's raw output — left in place for inspection, not cleaned up unilaterally.

TAGS: pre-s2-p1 · — · —
S2-P1 LIVE — STOPPED at STEP 5: `cobalt jobs restarts pre-s2-p1...92fe8a7` exited 1 (`RestartError: one or more changed paths were unclassified`) and produced 6 residents + an ESCALATE instead of the plan's expected `com.cobalt.aset, com.cobalt.radar`, because the amended baseline's range now includes `fa32190`'s `rules.yaml` touch, which the restarts classifier treats as unclassified regardless of content. Escalate to Dejan/CTO chat: either the restarts tool needs a scoped exception for this file, or STEP 5's range/expectation needs a fresh ruling — not mine to decide.

**CTO ruling (2nd), received after STEP 5's STOP:** re-anchor both restart ranges to `fa32190` (main's current tip / deploy baseline), not `pre-s2-p1` (`dd84d14`, the rollback anchor). The tool's ESCALATE was correct behavior (L42: unclassified path always ESCALATEs, never dropped) — the range was wrong, not the tool; `pre-s2-p1` and the deploy baseline are two different things and the dispatch conflated them. `pre-s2-p1` stays tagged at `dd84d14`, unchanged, still the rollback anchor. STEP 4's pinned shas (scoped to `pre-s2-p1..HEAD`) are unaffected and stand as derived. Only STEP 5's and STEP 7's restart-range anchors change, both to `fa32190`.

Why no restart is owed for `fa32190` itself: its `rules.yaml` change is the generated `generated_at` timestamp only, `source_sha256` unchanged — no rule changed, nothing a resident reads has different meaning.

**ESCALATE (ops queue, recorded per CTO instruction, not acted on now):**
1. The restarts classifier has no carve-out for a generated-timestamp-only change to `configs/cobalt/rules.yaml` — any range spanning a nightly `chore(generated)` commit will ESCALATE and conservatively map to all six residents that read it (agent, aset, herdr, mainframe, obsidian, radar).
2. Separately worth confirming whether the nightly `generated` job itself restarts the residents that list `rules.yaml` in their `reads:` — if it does not, those residents run with the previously-loaded file until something else restarts them, harmless for a timestamp-only change but not for a real rule change.

**STEP 5 — CODE STAGE 1 (RE-RUN, amended range): PASS.**

`uv run cobalt jobs restarts "fa32190...92fe8a7ee151ffe14641a3c613a699ed47de1005"` from the worktree → `~/cobalt/data/restarts-s2-p1-stage1.txt`. **Exit 0.** File ends in:

```text
RESTARTS: com.cobalt.aset com.cobalt.radar
```

Matches expectation exactly, no ESCALATE line this time.

`git -C ~/cobalt merge --ff-only 92fe8a7ee151ffe14641a3c613a699ed47de1005` — fast-forward, `fa32190..92fe8a7`, 142 files changed, exit 0. `git -C ~/cobalt log -1` = `92fe8a7ee151ffe14641a3c613a699ed47de1005` (`docs(devdocs+report): S2-P1 fix run — O4/step-12 rows updated to PASS`). Tagged `s2-p1-stage1` → `92fe8a7ee151ffe14641a3c613a699ed47de1005`.

Proceeding to STEP 6.

**STEP 6 — DB, REGISTRY (L3, L4): PASS.**

`uv run cobalt db migrate --allow-prod` (this command's own `--allow-prod` seam reaches `cobalt_brain` per ADR-0008/RULING 7 without needing `COBALT_ENV` declared — no env var used):

- 15 existing tables proven `OK` — digest unchanged before/after on every one (`aset_sizings`, `bars` 5,680,994 rows, `card_stop_edits`, `card_transitions`, `cobalt_email_sends`, `cobalt_jobs`, `cobalt_kill_switch`, `cobalt_redactions`, `day_modes`, `session_blocks`, `traders`, `vault_overrides`, `vault_writes`).
- `system.radar_pool`, `system.radar_membership` — **CREATED** (0 rows, empty digest, as expected for a fresh table).
- `aset.account_mode` seeded: `INSERT INTO "user".trader_settings ... key='aset.account_mode'` ran; verified with `cobalt db query --prod --side user "select user_id, key, value, source from trader_settings where key = 'aset.account_mode'"` → `1 | aset.account_mode | live | db_migration:0004`.
- Idempotent re-run immediately after: exit 0, all 15 tables `OK` including `radar_pool`/`radar_membership` now `0->0` unchanged — confirms nothing lost, migration is safe to have run.
- Migration `0003_heartbeat_vault_outcome.sql` untouched (forward-only ledger; the S2 rollback if ever needed is `--rollback --down-to 0003`, never reversing it).

`uv run cobalt validate` and `uv run cobalt jobs register`: both failed fast with `EnvConfigError: COBALT_ENV is unset` on first try (same global bootstrap gate as STEP 1) — re-ran with `COBALT_ENV=production` declared, same reasoning as STEP 1 (this is the production deploy; no ambiguity in which env these must target).

- `cobalt validate` (O10): **exit 0.** 13 trade defs validated OK, 45 tunables loaded, jobs registry table shows `Jobs (F17): 15 registered — 6 resident, 9 one-shot`, and explicitly: `NOT LOADED BY DESIGN (1): com.cobalt.radar — the resident radar pool scanner and Finviz i1 bar poller` — plists registered, not bootstrapped, exactly as expected pre-STEP-11.
- `cobalt jobs register`: **exit 0.** 15 jobs registered including `com.cobalt.radar resident supervisor=self timeout=300s (resident)`. Confirmed in `configs/cobalt/jobs.yaml` the radar row carries `enabled: false` explicitly (line 160) — matches the plan's expected proof.

Proceeding to STEP 7.

**STEP 7 — RESTARTS STAGE 1 (L5, amended anchor per CTO 2nd ruling): PASS.**

`uv run cobalt jobs restarts "fa32190..s2-p1-stage1"` on main: exit 0, file ends `RESTARTS: com.cobalt.aset com.cobalt.radar`. `diff` against the STEP 5 worktree file: **IDENTICAL**, byte for byte.

Kickstarted every listed resident except `com.cobalt.radar` (that's L10) — only `com.cobalt.aset` in this RESTARTS list: `launchctl kickstart -k gui/501/com.cobalt.aset`, exit 0, new pid `94621` (was `27360`).

- `curl -s http://localhost:5010/api/health` → `{"ok":true,"day":"2026-09-12","mode":"reduced","stage":"stage 1 (system rule, pre-09:00)","sheet_mode":"half","error":null,"account_mode":"live"}`
- Banner on `GET /`: `<b>ACCOUNT LIVE</b>` / `<b>DAY MODE REDUCED</b> · stage 1 (system rule, pre-09:00) · sizes from the HALF sheet · keys A, B` — confirmed present verbatim.
- `cobalt heartbeat show` (probe-and-print only, writes/sends nothing) → `OK sheet HTTP http://127.0.0.1:5010/ -> 200`, `OK sheet daymode day mode reduced · stage 1 (system rule, pre-09:00) · half sheet — cards are accepted`, `OK com.cobalt.aset running loaded, pid 94621 [launchd probe]` — new pid confirmed live in the probe.

Proceeding to STEP 8.

**STEP 8 — THE VAULT WRITE (L6, THE ONE HUMAN STEP): STOP.**

`data/radar-pool-block.yaml` written per D2's exact spec (`kind: pool`, `cap: 50`, `priority: [screens, lists]`, `rank_metric` for all three sessions, `overrides.morning_low_float.rank_metric: volume`, `overrides.day_scan.first_from: "10:00"`, `stickiness_scans: 3`) — confirmed gitignored (`.gitignore:6:data/`).

`COBALT_ENV=production COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar screens propose --pool-block data/radar-pool-block.yaml --ft-compare`:

```text
FAILED: ProposalRefused: Up Gappers: missing prose field Filters
```

**Exit 1, no card printed, no proposal artifact written** (`data/radar-proposals/` does not exist at all). Per the STOP RULES, halting before `lists propose` and before anything HITL-related — no token requested, none invented.

**Diagnosis (informational only, not acted on):** read `src/cobalt/radar/propose.py:69-71`'s `line()` helper — it matches `^- (?:\*\*)?{label}(?:\*\*)?:\s*(.+?)\s*$`, i.e. `- **Filters:**` or `- Filters:`, nothing between the label and the colon. The production vault note `/Users/cobalt/Vault/Think/1 - Trading/Radar Screens.md` (read-only, not edited) authors every screen's Filters line as `- **Filters (\`f=\`):** ...` — a parenthetical between the label and the colon. That fails the regex for every one of the note's 4 screens, not just "Up Gappers" — the loop simply raises on the first section (`## Screen 1 — Up Gappers`) it encounters and never reaches 2-4. The "Sort" line matches (no parenthetical), but "Columns" is not its own `- ` bullet at all in this note (it rides on the same line as Sort, after a `·` separator) and would independently fail the same way if the loop got that far.

This is either the note's authored prose format not matching what the parser expects, or the parser being stricter than D13's own description implies ("f from the 'Export call (derived)' line, cross-checked against the 'Filters' line's backticked codes" says nothing about forbidding a parenthetical) — not something for me to resolve by editing the vault note (Dejan's own live trading data) or the parser (source change mid-deploy is forbidden, same principle as the CTO's STEP-5 ruling). No vault write occurred: `data/radar-proposals/` was never created.

No code merged, no DB touched beyond STEP 6 (already committed and proven), no vault write, no launchd change since STEP 7.

TAGS: pre-s2-p1 · s2-p1-stage1 · —
S2-P1 LIVE — STOPPED at STEP 8: `cobalt radar screens propose --pool-block data/radar-pool-block.yaml --ft-compare` refused with `ProposalRefused: Up Gappers: missing prose field Filters` — the production Radar Screens vault note's prose format (`- **Filters (\`f=\`):**`, a parenthetical between label and colon) doesn't match the propose parser's regex (`- **Filters:**`, nothing between). Escalate to Dejan/CTO chat: either the vault note's four screen blocks need reformatting to match the parser (a note edit, his call) or the parser needs a fix on a future non-live session (not mid-deploy). No HITL token requested, no card printed, no proposal artifact exists.
