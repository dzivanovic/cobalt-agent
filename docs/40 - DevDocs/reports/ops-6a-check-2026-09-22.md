# OPS 6A CHECK — build check, round 1, of `ops/2026-09-21` (item 6a: the Cobalt key store into restic's include set)

## §0 Headline
Hub `ops-6a-check-0922`, Sonnet 5, 2026-09-21 18:43–19:20 ET (R53). Round 1 check of `ops/2026-09-21` (tip `f6aa4d0`, 6a = `227325c`): **4 of 4 houses checked**, all four inside 11 minutes (no TIMEOUT), nobody wrote anything but Grok's own file.
- CHECK lines: Grok `BUILD STANDS · YES` · Gemini `BUILD STANDS · YES` · Sol `BUILD STANDS EXCEPT 6(a) · NO` · Opus 5 `BUILD STANDS EXCEPT 6a · NO` — both NOs rest on ONE item, the `jobs restarts` classifier gap (`configs/cobalt/backup.yaml` UNCLASSIFIED CONFIG, exit 1). No `FIX FIRST`.
- My file-check: 0 build defects HOLD; the classifier gap HOLDS as a fact (carried, not counted — an ops item); the code circle in the restore path HOLDS as a fact and is pre-existing (`restic.py` is not in the branch's diff), its fresh-host consequence UNPROVEN; the `cli.md` seam is the one place two checkers contradict (Gemini: conflict/contradiction; Grok, Sol, Opus: neither).
- Ready by the COUNT RULE: **yes** (≥3 houses, 0 defects, no FIX FIRST) — a count, not a verdict; two of four lines read NO. ESCALATE: 4.

## PREFLIGHT
Authorization (each its own call; all committed on main):

| rule | command | result |
|---|---|---|
| R43 | `grep -n "^| R43 " cto-2026-09-21.md` | line 54, carries `"App approved"` and `06-ops-0921.md` — PASS |
| R43 committed | `git log -1 --format=%H -S"App approved" -- cto-2026-09-21.md` | `031196fe9a35132e2e453dbfe613f0f33185015e` — PASS |
| R39 | `grep -n "^| R39 " …` | line 50, carries `"All approved"` — PASS |
| R46 | `grep -n "^| R46 " …` | line 57, carries `instead of Astra you can use Sol` — PASS |
| R46 committed | `git log -1 -S"instead of Astra you can use Sol"` | `53e059456750c0c9efcf50222a7a647630dc4b04` — PASS |
| R49 | `grep -n "^| R49 " …` | line 60, carries `"Approved"` — PASS |
| R49 Sol string | `grep -c -F "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" …` | 1 — PASS |
| R49 Opus string | `grep -c -F "Bash(claude -p --model claude-opus-5 *)" …` | 1 — PASS |
| R49 committed (Opus / Sol) | `git log -1 --format=%H -S…` on the desk file | both `60147d400b009db5a2518e02b8ab1fe5765db405` — PASS |
| launch row | `grep -n "69-ops-6a-check.md" cto-2026-09-21.md cto-2026-09-22.md` | `| R53 |` at line 65 of `cto-2026-09-21.md` names this file; `cto-2026-09-22.md` does not exist (ugrep warning, exit 2) — recorded, not fatal |
| launch row committed | `git log -1 --format=%H -S"69-ops-6a-check.md" -- <the two desk files>` | `b72b2047356f550a145f4090db1e4e2b9cc4328e` — PASS |
| the thirteen + three | `grep -c -F -e "<rule>" prompts/2026-09-20/08-bars-chunk-e-check.md`, 16 calls (`Bash(grok *)`, `Bash(agy *)`, `Bash(mkdir -p scratch/tribunal-bars-0920)`, `git -C /Users/cobalt/cobalt show*` / `log*`, `git -C …/s2-p2-cards show*` / `log*` / `diff*`, `ls *`, `grep *`, `tail *`, `wc *`, `date*`, denies `AskUserQuestion`, `EnterWorktree`, `Bash(git push*)`) | each 1 — PASS. No Astra string in this launch line. |

Preflight rows:

| rule | command | exit | allowed/DENIED |
|---|---|---|---|
| `Bash(date*)` — DATE GATE, row 1 | `date` | 0 | allowed — `Mon Sep 21 18:43:04 EDT 2026` → `<D>` = 2026-09-21, inside R39's window; R39 literal `Bash(grok *) and Bash(agy *) through 2026-09-22` found at line 50 and committed (`3ecbb278de133f1838d217e9745728ed6d6337e9`) |
| `Bash(grok *)` | `grok --version` | 0 | allowed — `grok 1.0.25 (f7e67d6988e2) [stable]` |
| `Bash(agy *)` | `agy --version` | 0 | allowed — `1.2.7` |
| `Bash(ls *)` | `ls /Users/cobalt/cobalt-wt/ops-0921` | 0 | allowed — worktree present |
| `Bash(tail *)` | `tail -n 3 …/ops-2026-09-21.md` (branch report) | 0 | allowed — last non-blank line: `OPS 0921 DONE · items: 7 of 7 · commits: 6 · NEW strings used: 1 · ESCALATE: 10` (starts `OPS 0921 DONE ` — PASS) |
| `git -C /Users/cobalt/cobalt log*` | `log --oneline -1 main` | 0 | allowed — `<main tip>` = `b72b204` |
| same | `log --oneline -1 ops/2026-09-21` | 0 | allowed — `<tip>` = `f6aa4d0` (the drafted sha; not moved) |
| same | `log --format="%h %s" b72b204..f6aa4d0` | 0 | allowed — exactly SIX commits, subjects byte for byte as ordered: `f6aa4d0` report · `9dfc9a0` gitignore · **`227325c` = `<6a>`** feat(backup) · `5e2ae4c` backlog · `6193cd7` UNATTENDED-LAUNCH · `e2db970` cli.md |
| same | `log --stat --oneline b72b204..f6aa4d0` | 0 | allowed — eight paths, exactly the expected: `.gitignore`, `configs/cobalt/backup.yaml`, `docs/00 - Project/BACKLOG.md`, `docs/40 - DevDocs/cobalt/backup/config.md`, `docs/40 - DevDocs/cobalt/db_migrations/cli.md`, `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`, `docs/40 - DevDocs/reports/ops-2026-09-21.md`, `tests/cobalt/test_backup.py` |
| same | `log --oneline -1 bars/chunk-1a-0920` | 0 | allowed — `<1a tip>` = `6961ee0` |
| `Bash(ls *)` | `ls /Users/cobalt/cobalt-wt/ops-0921/.env` | 1 | allowed — "No such file or directory" (as required) |
| `Bash(ls *)` | `ls scratch/tribunal-bars-0920` | 0 | allowed — folder present (it holds earlier tribunals' folders) |
| `Bash(ls *)` | `ls scratch/tribunal-bars-0920/ops-6a-check` | 1 | allowed — "No such file or directory" = fresh run |
| stagger (a) | `tail -n 3 …/stale-marker-check-2026-09-22.md` | 1 | no such file — 68 not started, does not block |
| stagger (b) | `tail -n 3` of `setups-one-check-2026-09-22.md`, `stale-score-tribunal-2026-09-21.md`, `stale-score-tribunal-2026-09-22.md` | 1 each | none exist — 66 and 61 "not started", do not block |
| Codex launch shape | `grep -c -F "Experiment field: **reads started**" …/setups-tribunal-r2-2026-09-21.md` | 0 | allowed — `1` → ` < /dev/null` RECORDED AS WORKING; the Sol launch appends it |
| Sol probe | `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "Reply with only the word OK." < /dev/null` (background) | 0 | allowed — `OK`, no usage-limit text → **UP** |
| Opus probe | `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (background) | 0 | allowed — `OK` → **UP** |

Four of four UP → launch every house. No launcher string was denied. No `mkdir` was run, no `s2-p2-cards` string used, no Astra launch.

## Packet
Folder `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/ops-6a-check/` (the first Write created it; no `mkdir` was run). No file is split into parts (every original ≤ 38,000 B). Secrets: none of the four SECRETS paths (`/Users/cobalt/cobalt/data/.cobalt_vault`, `~/.cobalt_key`, any `.env`, `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt`) was opened, listed by content, staged or hashed; the key store is named by path only. `.env` is absent from the worktree (PREFLIGHT).

| staged file | bytes | matched against | result |
|---|---|---|---|
| `QUESTIONS-CHECK.md` (verbatim + the "Files in this folder:" paragraph) | 7,700 | the prompt's text | written by hand from the prompt |
| `branch-diff.md` (`git log -p b72b204..f6aa4d0 -- . ":(exclude)docs/40 - DevDocs/reports/ops-2026-09-21.md"`) | 13,933 | no original — checked differently: `grep -c "^diff --git"` = **7** (expected 7), `grep -c "^commit "` = **5** (expected 5); plus four long lines grep-matched against the worktree originals (the UNATTENDED-LAUNCH sentence, the config.md sentence, the BACKLOG line = 1 each). One transcription slip on a context line (`allowed` for `allowlisted`) was found and fixed by me before any launch. My copy holds 24 trailing-whitespace lines (`grep -c -E "[[:space:]]$"`), so the Write tool did not strip the diff's own trailing blanks/tabs | see left |
| `branch-report.md` | 29,023 | original `ops-2026-09-21.md`, 0 trailing-whitespace lines | `wc -c` equal |
| `ordered-prompt.md` (`06-ops-0921.md` whole) | 29,377 | original, 0 trailing-whitespace lines | `wc -c` equal |
| `desk-record.md` (R43 row `cto-2026-09-21.md:54`; the `- 17:42 ` bullet `:369`) | 3,073 | the two `grep -n` results | written from the tool results |
| `built/configs/cobalt/backup.yaml` | 3,691 | original, 0 trailing ws | equal |
| `built/src/cobalt/backup/__init__.py` · `cli.py` · `config.py` · `pgdump.py` · `restic.py` · `secrets.py` | 406 · 3,014 · 6,736 · 5,072 · 11,490 · 2,228 | originals, 0 trailing ws | each equal |
| `built/src/cobalt/redact/secrets.py` | 11,801 | original, 0 trailing ws | equal |
| `built/src/cobalt/jobs/restarts.py` | 11,632 | original, 0 trailing ws | equal |
| `built/ops/run_backup.sh` · `built/ops/com.cobalt.backup.plist` | 1,467 · 4,601 | originals, 0 trailing ws | each equal |
| `built/tests/cobalt/test_backup.py` | 9,957 | original, 0 trailing ws | equal |
| `built/docs/40 - DevDocs/cobalt/backup/` `__init__.md` · `cli.md` · `config.md` · `pgdump.md` · `restic.md` · `secrets.md` | 791 · 869 · 2,822 · 1,527 · 2,575 · 485 | originals, 0 trailing ws | each equal |
| `seam/cli-ops-0921.md` (`db_migrations/cli.md` on the branch) | 20,199 | original, 0 trailing ws | equal |
| `seam/cli-bars-1a.md` (`git -C /Users/cobalt/cobalt show 6961ee0:"docs/40 - DevDocs/cobalt/db_migrations/cli.md"`, one head line naming `bars/chunk-1a-0920`, `6961ee0`, "NOT MERGED — the text 1a would land") | 30,050 | **no original to `wc -c` against** (the object-store read has no size rule); `grep -c "SET LOCAL"` on my copy = **4** (the ops copy = 3) | see left |

The staging list of PREFLIGHT named eight paths; every code/config path among them (`configs/cobalt/backup.yaml`, `tests/cobalt/test_backup.py`) is staged; nothing extra was needed.

HONEST SIZE: top-level files 133,355 B + `built/` code and config 72,095 B + `built/` DevDocs 9,069 B = **214,519 B**, ≈ **53,600 tokens per checker** (÷ 4; the drafted estimate was 150–200 KB, mine is a little above).

Launches (`run_in_background`, one attempt per house), `date` recorded 18:58:16 ET immediately after the four starts (the four went out inside one call block; the DATE GATE's second row was `date` = Mon Sep 21 18:56:43 EDT 2026, R39 re-proven at line 50 and commit `3ecbb278…`):
- **GROK** — bg `bvkkfzaqb` — `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" -p "You are GROK. The folder is scratch/tribunal-bars-0920/ops-6a-check/ … Write your complete check to …/ops-6a-check/grok-check.md and reply with only that path."` (never `--always-approve`).
- **GEMINI** — bg `b98a8vdc6` — `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You are GEMINI. … Run NO shell command … print your complete check as your answer."`
- **SOL** — bg `bhqpr96jf` — `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "You are SOL. … Do not open any *-check*.md. Print your complete check as your final message; write no file." < /dev/null` (the ` < /dev/null` per PREFLIGHT's Codex-shape row).
- **OPUS 5** — bg `blqz6fz01` — `claude -p --model claude-opus-5 "You are OPUS. … Read files with the Read, Grep and Glob tools only. Write no file. Print your complete check as your answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/ops-6a-check --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"`.

Clock: launched 18:58 ET → the 45-minute mark is 19:43 ET.

WROTE-NOTHING PROOF — BEFORE the launches (`ls -la`, 18:56–18:57):
`scratch/tribunal-bars-0920/ops-6a-check`: `branch-diff.md` 13933 (18:45) · `branch-report.md` 29023 (18:52) · `built/` (18:50) · `desk-record.md` 3073 (18:53) · `ordered-prompt.md` 29377 (18:53) · `QUESTIONS-CHECK.md` 7700 (18:56) · `seam/` (18:56).
`/Users/cobalt/cobalt-wt/ops-0921`: 27 entries, newest mtime `.gitignore` Sep 21 17:41; `.pytest_cache` 17:33, `.venv` 17:32, `src` 17:32, `tests` 17:32, everything else 17:31.
AFTER each return (`ls -la` of both folders, then `grep -c -i "denied\|not allowed\|permission"` on the check file):

| checker | returned (`date`) | folder listing vs before | worktree `ops-0921` vs before | denial grep | note |
|---|---|---|---|---|---|
| GEMINI | 19:00:18 | + `gemini-check.md` (5,095 B, written by me); nothing else | identical (27 entries, same mtimes) | 0 | printed only; ran as ordered |
| OPUS 5 | 19:00:43 | + `opus-check.md` (11,095 B, written by me) | identical | **1** — the word "permission" in its own Q3 table row ("The job's user lacks permission"), not a tool denial; nothing else matched | `--permission-mode plan`, Bash/Write/Edit disallowed (launch line as run above) |
| SOL | 19:07:05 | + `sol-check.md` (6,951 B, final message only, written by me) | identical | final message 0; its full transcript 12 hits on `denied\|not allowed`, all echoed packet or memory text (the UNATTENDED-LAUNCH rules, LAWS wording, a desk memory note its session-start hook loaded), no tool denial | `-s read-only`; the transcript (502 KB) also shows a memory note from its hook — recorded, nothing written |
| GROK | 19:08:56 | + `grok-check.md` (12,402 B, its own file through its approved `--allow`); nothing else | identical | 0 | its stdout was progress text + the path only; I read the file it wrote |

No `WROTE:` mark on any checker. Clock: Gemini 2 min, Opus 2 min, Sol 9 min, Grok 11 min of the 45.

## CONTINUE
step: closed — all four checkers collated, file-checks done, nothing left to run. (Resume rule, if relaunched: `ls scratch/tribunal-bars-0920/ops-6a-check`; the four `<house>-check.md` files exist, no house is re-asked.)

## Per question
Cells are the checkers' own words, ≤20 words. "Challenging" = checkers whose answer differs from the builder's claim or who raise the item against the deploy.

| question | builder's claim | grok | gemini | sol | opus | challenging |
|---|---|---|---|---|---|---|
| 1 exactly the key store | vault file only; 3 lines; no other change (report §6) | HOLDS — one path under `sources`; no glob; no secret value | HOLDS — exactly the vault file, nothing else, no secret value | HOLDS — only the vault file plus the two-line comment; no `~/.cobalt_key` | HOLDS — one single file, not a directory or glob; no value anywhere | 0 |
| 2 real proof or tautology | test loads the COMMITTED config; RED then GREEN (report §6) | HOLDS — same loader, same list into restic; removed/misspelled/other key all fail | HOLDS — same loader; fails if removed, misspelled or under another key | HOLDS — default committed path; Pydantic `extra="forbid"` catches another key | HOLDS — same loader; limit: proves config not snapshot, NOT CHECKABLE FROM READS | 0 |
| 3 can the nightly job get worse | none stated | HOLDS, failure mode NONE; whether restic errors on a file beside a directory NOT CHECKABLE | `DOES NOT HOLD` (i.e. NONE) — a missing vault file already fails loud in `_env` | HOLDS — no new failure mode; same user; excludes do not match the file | NONE — plus a not-a-defect note: restic `forget` groups by path set | 0 (Opus adds a side-effect note) |
| 4 circular restore | none stated | HOLDS (code circle); fresh host UNPROVEN | HOLDS — `restore` fetches the password from the vault it restores | DOES NOT HOLD — CLI restore circular on a fresh host; UNPROVEN drill | circle exists, pre-existing, not created by 6a; UNPROVEN | 4 |
| 5 other five commits | docs / gitignore / BACKLOG only (report §8) | HOLDS, NONE hidden, no runtime reader | HOLDS, NONE | HOLDS, NONE | NONE; one minor note (`16-packet/` whole directory) | 0 |
| 6a restarts | table VERBATIM, exit 1, gap named (ESCALATE 1) | quotes it; gap at `restarts.py:243-244`; unclassified is escalated | quotes it; same gap | DOES NOT HOLD — deploy must not say "none"; add rule or Dejan's override | quotes it; deploy needs a rule + test or a ruling; `NO` | 2 (Sol, Opus: settle before deploy) |
| 6b migration | none | NONE | NONE | NONE | NONE | 0 |
| 6c `cli.md` seam | may contradict at 1a's merge (ESCALATE 8) | no contradiction, no conflict on this span; merge itself not run | contradict entirely; "direct merge conflict" | no conflict after three-way landing; 1a paragraph is the base text | no conflict; bars copy contradicts itself; landing ops fixes it | 1 (Gemini) |
| 7 the suite | 2208→2209, one test added (report §8) | HOLDS, NONE weakened; re-run NOT CHECKABLE | HOLDS — exactly one test added | HOLDS; suite execution itself NOT CHECKABLE | NONE — 23 tests = 12+2+2+5+2 | 0 |

### Question 3 — every checker's answer, in full, unedited
**GROK:**
> ## (3) CAN THE NIGHTLY JOB GET WORSE — HOLDS (failure mode NONE)
>
> Sequence, unchanged except the extra target:
>
> 1. `com.cobalt.backup.plist:71-80` fires 21:40; `:63` runs `run_backup.sh`. No `UserName`.
> 2. `run_backup.sh:23-39` cds to `/Users/cobalt/cobalt`, sources `~/.cobalt_key`, execs `cobalt backup run`.
> 3. `restic.py:164-186` loads the config, refuses an unmounted disk before the dump, then sets `targets` to every source and appends the dump.
> 4. `restic.py:202` calls `_env` → `vault_value` (`secrets.py:35-40`), which reads `VAULT_FILE` (`redact/secrets.py:43` = `<checkout>/data/.cobalt_vault`). On this job that is `/Users/cobalt/cobalt/data/.cobalt_vault`, the same path as the new source (`backup.yaml:41`). A missing or unreadable file already aborts here, before restic.
> 5. `restic.py:204-207` passes excludes, then every target, to `restic backup`. The previous targets are still there. Forget stays count-based (`backup.yaml:80-83`, `config.py:118-122`).
>
> `latest_snapshot_age` reads snapshot `time` only (`restic.py:240-263`). `status` prints the source list and does not reject a shape (`cli.py:58-71`). The freshness assertions in `test_backup.py:191-226` check mount and "NO DESTINATION ARMED", not path count.
>
> NOT CHECKABLE FROM READS — whether the restic binary errors on a file target beside a directory. `test_backup.py:3-4` says the binary is not exercised, and nothing in this folder is a restic transcript. That is not a defect.

**GEMINI:**
> (3) **CAN THE NIGHTLY JOB GET WORSE**
> `DOES NOT HOLD` (`NONE`). Adding the vault file does not create a silent failure. A missing or unreadable vault file at run time fails the run loudly during the `_env` credential setup (`built/src/cobalt/backup/secrets.py:35`) before any dump or snapshot is taken.

**SOL:**
> 3. **HOLDS — no new failure, skip, or reduced-backup mode is visible.** A missing or unreadable `.cobalt_vault` already made the job fail while fetching `RESTIC_PASSWORD`, before this source addition (`built/src/cobalt/backup/secrets.py:28-45`; `built/src/cobalt/backup/restic.py:127-141,201-207`). The job runs as the same user that already reads that file (`built/ops/run_backup.sh:23-39`). The new target is a separate absolute argument; existing vault and dump targets, excludes, retention, freshness, and status paths are unchanged (`built/src/cobalt/backup/restic.py:182-192,201-230,240-263`; `built/src/cobalt/backup/cli.py:58-72`). None of the excludes matches `.cobalt_vault` (`built/configs/cobalt/backup.yaml:53-58`).
> **NOT CHECKABLE FROM READS —** actual restic handling of the mixed directory/file target requires the first post-deploy nightly run.

**OPUS 5:**
> ## (3) Can the nightly job get worse: **NONE**
> | failure mode | why it is not new |
> |---|---|
> | File missing or unreadable at 21:40 | `_env` (`restic.py:202`, `restic.py:137`) already decrypts this file before restic runs, and fails loudly at `backup/secrets.py:35-36,40`. The job would already have failed. |
> | The job's user lacks permission | Same file, same process; it is already read every night (`backup/secrets.py:40`). |
> | restic handling a single file next to a directory | A single-file target is already backed up every night: the dump at `restic.py:184-186`. |
> | An exclude now drops the vault tree or the dump | The excludes are unchanged (`backup.yaml:53-58`). |
> | The freshness or status check reads a different snapshot shape | `latest_snapshot_age` reads only `snap["time"]` (`restic.py:257-260`); `status` only prints `sources` (`cli.py:61`). |
> | The vault is rewritten during a backup | `put_secret` replaces the file atomically (`redact/secrets.py:273-277`); its temp file is not a target. |
>
> **Not a defect — side effect of adding a path:** by default restic's `forget` groups snapshots by host and path set, so the new path set starts a new group:
> - The first run after the deploy has no parent snapshot, so it rescans everything and `files_new` will look inflated.
> - The old group's 14/8/12 snapshots stay unpruned for good (`restic.py:229-230`). This backs up more, never less.
>
> **NOT CHECKABLE FROM READS** — run `restic forget --dry-run` after the first run.

### Question 4 — every checker's answer, in full, unedited
**GROK:**
> ## (4) A CIRCULAR RESTORE — HOLDS (the code circle). Fresh-host result UNPROVEN
>
> `restore` (`restic.py:266-281`) calls `_env` (`:281`). `_env` (`restic.py:136-137`) sets `RESTIC_PASSWORD` from `vault_value(cfg.password_vault_key)`. The key is `RESTIC_PASSWORD` (`backup.yaml:74`). `vault_value` (`built/src/cobalt/backup/secrets.py:28-40`) reads `VAULT_FILE` and raises `VaultUnavailable` if that file is missing (`:35-36`). It uses the constant `VAULT_FILE` (`secrets.py:15`), not `vault_file()`, so `COBALT_VAULT_FILE` is not consulted. An already-exported `RESTIC_PASSWORD` is overwritten (`restic.py:136-137`). So `cobalt backup restore` cannot start on a host whose key store is what the restore is trying to get back.
>
> What the files name outside the repository: the plist says the password was mirrored to `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt` (`built/ops/com.cobalt.backup.plist:11-15`), a sibling of the repo directory `/Volumes/COBALT-BACKUP/restic` (`backup.yaml:63`), not an object inside it. No function in this folder opens that path. `~/.cobalt_key` (`run_backup.sh:26-28`) is the Fernet master key, not the repository password, and it is not in `sources` (`config.md:9`). It does not break this circle.
>
> `restic.py` is not in `227325c`. This password fetch is the pre-existing restore path; 6a did not add it. A fresh-host restore was not run. UNPROVEN. The run that settles it: with `data/.cobalt_vault` absent, `cobalt backup restore` must fail at `VaultUnavailable`; separately, `restic restore` using the README password (not `cobalt backup restore`) must show whether that file actually opens the repo. Launch 2 does not settle it — `ordered-prompt.md:41` restores from `~/cobalt`, which still has the vault.

**GEMINI:**
> (4) **A CIRCULAR RESTORE**
> `HOLDS` (`built/src/cobalt/backup/restic.py:137`, `built/src/cobalt/backup/secrets.py:35`). The automated `cobalt backup restore` command calls `_env`, which calls `vault_value` to fetch `RESTIC_PASSWORD` from the very vault file (`VAULT_FILE`) it is trying to restore. The circle is broken outside the repository by using the mirrored password in `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt` (`built/ops/com.cobalt.backup.plist:11-15`) with a bare restic call. A real restore on a fresh host is `UNPROVEN` — running a host-loss restore drill (6b) would settle it.

**SOL:**
> 4. **DOES NOT HOLD — the Cobalt CLI restore is circular on a fresh host.** `restore()` calls `_env()`, which fetches `RESTIC_PASSWORD` through `vault_value()`; that function requires and decrypts the very `.cobalt_vault` being recovered (`built/src/cobalt/backup/restic.py:127-141,266-282`; `built/src/cobalt/backup/secrets.py:22-56`). The external escape hatch named by the files is `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt`, which permits a raw-restic recovery without first opening the vault (`built/ops/com.cobalt.backup.plist:7-15`). The wrapper’s `~/.cobalt_key` only supplies the master key to an intact host and is deliberately outside this repository (`built/ops/run_backup.sh:26-32`). No packet file names an off-host master-key recovery copy.
> **UNPROVEN —** run a fresh-host drill with `.cobalt_vault` absent, obtain the repository password from the external README, restore through raw restic, then prove the separately retained master key opens the restored vault. The planned live-host 6b run would not settle this circularity.

**OPUS 5:**
> ## (4) A circular restore: **the circle exists, but 6a did not create it; a fresh-host restore is UNPROVEN**
> | what the code does | file:line |
> |---|---|
> | `restore()` calls `_env(cfg, dest)` | `built/src/cobalt/backup/restic.py:281` |
> | `_env` gets `RESTIC_PASSWORD` through `vault_value` | `restic.py:134-137` |
> | `vault_value` needs `COBALT_MASTER_KEY` and an existing, decryptable `data/.cobalt_vault` | `built/src/cobalt/backup/secrets.py:28-40` |
> | So `cobalt backup restore` cannot recover the vault file on a host that has lost it. This was already true before 6a. | — |
> | What breaks the circle: the restic password is mirrored to `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt` (0600), so plain `restic` can open the repository | `built/ops/com.cobalt.backup.plist:11-15` |
> | The restored vault file is still useless without `~/.cobalt_key`, which is deliberately not backed up (`run_backup.sh:26-28`). No file here names an off-host copy of the master key. | UNPROVEN |
>
> What would settle it: on a host without `data/.cobalt_vault`, run `restic -r /Volumes/COBALT-BACKUP/restic restore latest --include /Users/cobalt/cobalt/data/.cobalt_vault` using the README password. Then unlock the file with the off-host master key (for example, a names-only `secret_names()`). 6b as ordered runs on the live host with the vault present (`ordered-prompt.md:40`), so it does not test this.

My file-check of question 4 is in the table below (rows 11–18): the code path HOLDS as read; `restic.py` is not in the branch's diff; the fresh-host consequence, the README's existence, and whether its password opens the repository are NOT CHECKABLE FROM READS (I never opened the README, L4/L41).

## Checked against the branch
Each row opened by me: the ORIGINAL under `/Users/cobalt/cobalt-wt/ops-0921/`, main under `/Users/cobalt/cobalt/`, or the shared object store. Lines are of the original file unless it says "staged copy".

| # | claim | who | file:line | verdict | note |
|---|---|---|---|---|---|
| 1 | 6a adds exactly one `sources` path plus a two-line comment, nothing else | all four | `git show 227325c -- configs/cobalt/backup.yaml` (quoted in (iii)); `backup.yaml:37-41` | HOLDS | 3 `+` lines, 0 `-`, one hunk |
| 2 | no secret value in config, diff, test or report; master key not in the config | all four | `backup.yaml` grep `cobalt_vault\|cobalt_key`: only `:41`; key names only at `:74-76`; test `:74` path only | HOLDS | the report names paths only (I read it whole); `cobalt_key` appears only in `config.md:9` as "deliberately NOT" |
| 3 | test loads the committed config through the job's loader | all four | `test_backup.py:73`; `config.py:18-19,161`; job path `cli.py:49` → `restic.py:164` | HOLDS | — |
| 4 | fails if removed / misspelled / under another key | all four | `test_backup.py:74` exact `Path` membership; `config.py:43,112,130,137` `extra="forbid"`; RED at `report:109-110` | HOLDS (config level) | that restic STORES the file: NOT CHECKABLE FROM READS — `restic ls latest` after one 21:40 run |
| 5 | every source reaches restic unfiltered | all four | `restic.py:182,207` | HOLDS | — |
| 6 | the quoted RED matches the test code | all four | `report:109-113` vs `test_backup.py:74` | HOLDS | I did not re-run it |
| 7 | a missing/unreadable vault file already fails the job before restic | gemini, sol, grok, opus | `restic.py:202,137`; `secrets.py:35-36,40` | HOLDS (as read) | whether the restic binary takes a file target beside a directory: NOT CHECKABLE FROM READS — first nightly run |
| 8 | a single-file target is already used (the dump) | opus | `restic.py:186` `targets.append(str(dump))` | HOLDS | — |
| 9 | restic `forget` groups by host + path set; the new path set starts a new group; old group's snapshots stay unpruned; first run looks inflated | opus | `restic.py:229-230` (no `--group-by` given, filtered by `--tag` only) | NOT CHECKABLE FROM READS | restic's default is not in any file here; settle with `restic snapshots` / `restic forget --dry-run` after the first run |
| 10 | `put_secret` replaces the vault atomically; its temp is not a target | opus | `redact/secrets.py:273-277` | HOLDS | — |
| 11 | `restore()` → `_env` → `vault_value` → needs the vault file | all four | `restic.py:281,134-137`; `secrets.py:28-40` | HOLDS (code) | restore cannot start without `VAULT_FILE` |
| 12 | the circle is pre-existing, not created by 6a | opus, grok | `git log b72b204..f6aa4d0 -- src ops` is EMPTY (ii) | HOLDS | `restic.py`, `secrets.py` are untouched by the branch |
| 13 | `vault_value` uses the constant `VAULT_FILE`, not `vault_file()` | grok | `secrets.py:15,35,40` | HOLDS | so `COBALT_VAULT_FILE` is not consulted on the backup path |
| 14 | an already-exported `RESTIC_PASSWORD` is overwritten | grok | `restic.py:136-137` | HOLDS | — |
| 15 | the plist says the password is mirrored to the README on the SSD | all four | `com.cobalt.backup.plist:11-15` (path split over `:12-13`) | HOLDS (as text) | that the file exists and opens the repository: NOT CHECKABLE FROM READS — never opened; the plist's mirror predates HIS pending rotation |
| 16 | no function opens the README path | grok | `grep -rn RESTIC-PASSWORD-README src ops configs` → only `plist:12` | HOLDS | — |
| 17 | no packet file names an off-host master-key copy | sol, opus, grok | `run_backup.sh:26-28`; `config.md:9` | HOLDS (for the staged files) | beyond the packet: NOT CHECKABLE FROM READS |
| 18 | 6b as ordered runs on the live host with the vault present | opus, grok, sol | `ordered-prompt.md:40` (6b paragraph); grok cited `:41` | HOLDS | grok's cite is off by one line; the claim holds |
| 19 | the other five commits are docs / `.gitignore` / BACKLOG with no runtime reader | all four | (i), (ii) below; `restarts.py:19-28` META | HOLDS | — |
| 20 | `.gitignore` hides nothing already tracked | all four | `branch-diff.md:9-22` (three `+` lines, no removal); `report:129` controls | HOLDS | ignore rules do not untrack; the `check-ignore` proof itself is the builder's claim |
| 21 | `16-packet/` line ignores the whole directory | opus, grok | `branch-diff.md:20`; `ordered-prompt.md:43` | HOLDS | the order named that line |
| 22 | classifier has a one-shot rule for `watchlists.yaml` only; `backup.yaml` falls to UNCLASSIFIED CONFIG and raises | all four | `restarts.py:243-244`, `:251-252`, `:271-272`; (vi) | HOLDS | `jobs.yaml:230-243` registers `com.cobalt.backup` as one-shot with no declared reads |
| 23 | the report's restarts table is what the command printed (exit 1) | all four | `report:135-151` | NOT CHECKABLE FROM READS | I ran nothing (`uv run` is not in my line); the builder's claim, agreeing with the code path in row 22 |
| 24 | main has moved under the branch; the range is re-derived after the rebase | opus | `report:152,184` | HOLDS | main is now `b72b204`, further on |
| 25 | no database migration | all four | (i): eight paths, none under `db_migrations` code | HOLDS | — |
| 26 | 1a's old `SET LOCAL` paragraph is the base text that ops removes | sol, grok, opus | `grep -c -F` of the paragraph's first line: 1 in `seam/cli-bars-1a.md:114` and 1 in the `-` lines of `branch-diff.md` | HOLDS | also `seam/cli-bars-1a.md:79-83` old wording |
| 27 | 1a's appended section says the `SET LOCAL` precedes the BEFORE probe | grok, sol, opus | `seam/cli-bars-1a.md:466-468`, `:502` | HOLDS | consistent with ops `seam/cli-ops-0921.md:114-118` |
| 28 | the bars copy contradicts itself | opus | `seam/cli-bars-1a.md:114-121` vs `:466-468,:502` | HOLDS | the stale sentence `the BEFORE PROBE IS NOT UNDER THE CEILING` is in the 1a copy once, in the ops copy 0 times (`grep -c -F`) |
| 29 | bars only appends after ~`:341`; its edits to the old paragraphs are none | opus | staged copy: section header `:343`, file 548 lines; `git log --stat b72b204..bars/chunk-1a-0920 -- cli.md`: six commits, +60 +59 +57 +26 +5/−1 +4/−1 | HOLDS at file level | the two `−1` edits sit in bars' own section (the old paragraphs are unchanged in the tip copy, row 26) |
| 30 | landing both = "direct merge conflict" and "contradict entirely" | gemini | as rows 26–29 | DOES NOT HOLD as stated for the text; the merge itself NOT CHECKABLE FROM READS | only the bars copy holds the stale sentence; whether git conflicts needs `git merge-tree` on the two tips (no rule of mine); grok, sol, opus say no conflict |
| 31 | test assert at `test_backup.py:77-78` | gemini | built test file: assert is `:74`; `branch-diff.md:77-78` are the `cfg` and `assert` lines | cite wrong | claim (the assert exists) holds |
| 32 | one test added; no assertion removed or weakened; no skip/xfail | all four | (v): no `-` line in any test file; `grep -c "^+.*\(skip\|xfail\)"` = 0 | HOLDS | 23 tests = 12+2+2+5+2 (`test_backup.py:44-226`) |
| 33 | suite `2209 passed` vs baseline `2208` | all four | `report:40,132` | NOT CHECKABLE FROM READS | pytest not run by me; +1 matches the one added test |
| 34 | `f6aa4d0` touches the report file only | grok | PREFLIGHT `--stat` | HOLDS | — |
| 35 | `status` prints sources only; freshness reads `snap["time"]` only | grok, opus | `cli.py:58-71`; `restic.py:257-260` | HOLDS | — |

My own six checks:
- **(i)** `git log --stat --oneline b72b204..f6aa4d0` names exactly eight paths: `.gitignore`, `configs/cobalt/backup.yaml`, `docs/00 - Project/BACKLOG.md`, `docs/40 - DevDocs/cobalt/backup/config.md`, `docs/40 - DevDocs/cobalt/db_migrations/cli.md`, `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`, `docs/40 - DevDocs/reports/ops-2026-09-21.md`, `tests/cobalt/test_backup.py`. HOLDS.
- **(ii)** `git log --oneline b72b204..f6aa4d0 -- src ops` is EMPTY. No code, no launchd change. HOLDS.
- **(iii)** `git show 227325c -- configs/cobalt/backup.yaml` adds exactly three lines and removes none:
    `+    # The encrypted credential vault: a host loss must not take the only`
    `+    # copy of every credential (PROJECT-LEDGER.md:1174, owed since 09-11).`
    `+    - /Users/cobalt/cobalt/data/.cobalt_vault`
  No `-` line. HOLDS.
- **(iv)** `grep -n "cobalt_vault\|cobalt_key" configs/cobalt/backup.yaml` on the branch: one hit, `41:    - /Users/cobalt/cobalt/data/.cobalt_vault` (the path; the comment at `:39-40` does not name it). `cobalt_key`: no hit. HOLDS.
- **(v)** the `-` lines of `branch-diff.md` (`grep -n "^-[^-]"`) are sixteen lines: one in `UNATTENDED-LAUNCH.md` (`:121`) and fifteen in `cli.md` (`:143-145`, `:157-168`, the replaced `SET LOCAL` paragraphs); none is in a test file. `grep -c "^+.*\(skip\|xfail\)"` = 0. HOLDS.
- **(vi)** `grep -n "watchlists.yaml\|backup.yaml" restarts.py`: one hit, `243:        if not rule and path == "configs/cobalt/watchlists.yaml":`. `backup.yaml`: no hit. The gap the report names HOLDS.

The derived table (the builder's, verbatim — `report:135-151`; every line indented so none can read as a stop line):

    FAILED: RestartError: one or more changed paths were unclassified
    path	change	rule	restart
    .gitignore	M	META	-
    configs/cobalt/backup.yaml	M	UNCLASSIFIED CONFIG	com.cobalt.agent,com.cobalt.aset,com.cobalt.herdr,com.cobalt.mainframe,com.cobalt.obsidian,com.cobalt.radar
    ESCALATE: unclassified path configs/cobalt/backup.yaml
    docs/00 - Project/BACKLOG.md	M	DOCS	-
    docs/40 - DevDocs/cobalt/backup/config.md	M	DOCS	-
    docs/40 - DevDocs/cobalt/db_migrations/cli.md	M	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/58-s2-smoke-first-look.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/59-propose-stale-score.md	D	DOCS	-
    docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md	M	DOCS	-
    docs/40 - DevDocs/reports/cto-2026-09-21.md	M	DOCS	-
    docs/40 - DevDocs/reports/ops-2026-09-21.md	A	DOCS	-
    tests/cobalt/test_backup.py	M	test/documentation; no resident	-
    RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar

Where two checkers contradict, both quoted, neither smoothed: on the `cli.md` seam, Gemini — "They contradict entirely on where `SET LOCAL` sits and what lock it covers. Landing them together would result in a direct merge conflict." — against Opus — "**No merge conflict:** the ops hunks and the bars hunk do not touch the same or adjacent lines." and Sol — "A three-way rebase/merge therefore keeps the ops correction without conflict." Grok: "A conflict is not what the two texts show." On the overall verdict, Grok and Gemini `ready: YES`, Sol and Opus `ready: NO` on the classifier gap.

## Ready for the stacked deploy
| checker | CHECK line | ready | reason verbatim |
|---|---|---|---|
| grok | `CHECK: BUILD STANDS · ready for its deploy: YES` | YES | (none given) |
| gemini | `CHECK: BUILD STANDS · ready for its deploy: YES` | YES | (none given) |
| sol | `CHECK: BUILD STANDS EXCEPT 6(a) · ready for its deploy: NO · Classifier gap unresolved; derived all-resident restarts cannot be silently dropped.` | NO | Classifier gap unresolved; derived all-resident restarts cannot be silently dropped. |
| opus | `CHECK: BUILD STANDS EXCEPT 6a · ready for its deploy: NO · classifier exits 1 on backup.yaml; needs rule or ruling` | NO | classifier exits 1 on backup.yaml; needs rule or ruling |

COUNT RULE (not a verdict): houses that checked 4 ≥ 3 · defects that HOLD 0 · no `CHECK: FIX FIRST` → `yes`. The two NO lines are carried under ESCALATE 1.

## ESCALATE
1. **The classifier gap (question 6a), carried as-is — an ops item for the deploy prompt and the desk, not a build defect by me.** `jobs restarts` on this branch exits 1: `configs/cobalt/backup.yaml` `UNCLASSIFIED CONFIG`, derived `RESTARTS:` = all six residents (table above). `restarts.py:243-244` has a one-shot rule for `configs/cobalt/watchlists.yaml` and none for `backup.yaml`; it falls to `:251-252` and raises at `:271-272`. Views: Grok — unclassified is escalated, not dropped; Gemini — names the gap, ready YES; Sol — "the deploy must not replace that result with the judgment 'none': add and check the missing one-shot rule, or carry Dejan's explicit override", ready NO; Opus — "the deploy needs a classifier rule with a test, or Dejan's ruling, before it runs", ready NO. The builder's own reading (report ESCALATE 1): only the one-shot `com.cobalt.backup` reads the file. I add no recommendation.
2. **The `cli.md` seam (question 6c), carried as-is.** Ops text `seam/cli-ops-0921.md:114-118` (`SET LOCAL` first statement after `_connect`, BEFORE the probe, probe under the ceiling) vs the 1a copy `seam/cli-bars-1a.md:114-121` (old paragraph: after the probe, "the BEFORE PROBE IS NOT UNDER THE CEILING"), which is byte-identical to the base text ops removes; 1a's own appended section (`:466-468`, `:502`) says the `SET LOCAL` precedes the probe. Gemini alone reads it as a contradiction and a merge conflict; Grok, Sol, Opus read no conflict; the merge itself is NOT CHECKABLE FROM READS — `git merge-tree` of `6961ee0` and the ops tip would settle it. The builder's ESCALATE 8 stands until then.
3. **Question 4, every answer carried as-is with my file-check (rows 11–18).** The code path is real: `restore()` → `_env` → `vault_value` needs the very `data/.cobalt_vault` a host-loss restore is recovering (`restic.py:281,134-137`; `secrets.py:28-40`); it is in the branch's base code, not in its diff. The plist names a mirrored password README on the SSD (`plist:11-15`) as the way around; nothing in the packet shows it exists, is current after HIS pending rotation, or opens the repository, and no file names an off-host copy of `~/.cobalt_key`. UNPROVEN, not a defect (L70). What settles it (Sol / Opus / Grok, agreeing): a fresh-host drill — `data/.cobalt_vault` absent, raw `restic restore` with the README password, then the separately held master key opening the restored file. Sol, Opus and Grok each note that 6b as ordered runs on the live host with the vault present and does not settle it. The desk schedules 6b.
4. **A restic side effect, carried (Opus, question 3, "not a defect").** By restic's default `forget` groups by host + path set, so the new path set starts a new snapshot group: the first post-deploy run has no parent (inflated `files_new`), and the old group's 14/8/12 snapshots would stay unpruned (`restic.py:229-230` passes `--tag` only). NOT CHECKABLE FROM READS — restic's default is in no file here; `restic snapshots` / `restic forget --dry-run` after the first run settles it.

Not escalated, recorded: no secret value anywhere (L4); no packet mismatch (one transcription slip in `branch-diff.md` was found by me and fixed before any launch, see Packet); every checker checked and none is marked `WROTE:`; no `ASK DESK` line was needed.

OPS 6A CHECK DONE · grok: CHECK: BUILD STANDS · ready for its deploy: YES · gemini: CHECK: BUILD STANDS · ready for its deploy: YES · sol: CHECK: BUILD STANDS EXCEPT 6(a) · ready for its deploy: NO · Classifier gap unresolved; derived all-resident restarts cannot be silently dropped. · opus: CHECK: BUILD STANDS EXCEPT 6a · ready for its deploy: NO · classifier exits 1 on backup.yaml; needs rule or ruling · houses that checked: 4 of 4 · defects that HOLD: 0 · ready for the stacked deploy: yes · ESCALATE: 4
