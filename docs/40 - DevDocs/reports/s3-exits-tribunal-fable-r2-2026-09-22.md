BLIND: I did not read the round-2 folder or the round-2 hub's report.

Seat `s3-exits-tribunal-fable-r2-0922` · Opus 5.5 (`claude-opus-5-5`) holding the Fable seat · round 2 of ≤3 · started 2026-09-22 14:59 ET.
AUTHORIZATION (each its own call): R36 `cto-2026-09-22.md:62` carries `Fable seat: yes` · `derive seat: claude-opus-5-5` for `S3-EXITS-PROPOSAL-2026-09-21.md`, committed `67bc19b` · R62 `cto-2026-09-22.md:36` carries `S3 exits tribunal ROUND 2` and `claude-opus-5-5`, committed `61dd580` · R57 `cto-2026-09-21.md:68` carries `Fable seat: yes` and the proposal name · derive stop line committed `cda7bf1` · v2 committed `cda7bf1` · R36 names `claude-opus-5-5` = this session's model · the seven allow strings and three deny strings each count 1 in `22-draft-setups-tribunal.md`. L32: no ticker or price of his below; figures are hypothetical.

## DIGEST FOR THE DESK

**Closing line: `TRIBUNAL R2: BUILD AFTER derive folds R2-2 wording: CAS running check, locked stop edit, fill-price open risk`.**

- R2-1 — **ADOPT A**. `[F-22]` leaves no second transaction in the text; §5 names the share-count fix. **DO NOT BUILD withdrawn.** Build note: `mark_filled`'s connection must set `autocommit = False` (`db.py:199`). Addendum: a second FILLED-recompute defect (per-share risk from the PLANNED entry) found after R2-1 was written — folded into R2-2 (d), not a DO NOT BUILD.
- R2-2 — **NEITHER**: F's index, greatest-id view, derived running; plus G's head check and G's one-refused tap (via posted `running_before`); stop edit locked, never writes `shares`.
- R2-2 sub-points: (a) F · (b) F + head check · (c) F · (d) neither · (e) F · (f) F · (g) G.
- R2-3 — **ADOPT C**: one `db_migrations` file + `.rollback.sql`; every post-move ALTER of these tables is there (`0007`).
- Withdrawn from my seat's round 1: 5.
- Experiments: X20 (planned-entry recompute), X21 (stop-edit lock), X22 (fresh DB + M1 round trip); cited v2 X4, X7 (expectation changed to REFUSED), X19.
- OWNER: 2 (legacy FILLED share source; close-by-correction) — neither a precondition.
- ESCALATE: 2.

## Rulings

### R2-1 — gemini's DO NOT BUILD against v2 (written 15:00 ET, before any read for R2-2)

SELF-ATTACK (my seat's round-1 `BUILD AFTER derive folds same-transaction fill, leg lock, pct-only warning, 0014 numbering`, against gemini's two defects): the seam half of my verdict rested on my own orchestrator + a new `within=` callback on `fill()` (r1 (b)). v2 took grok's `fill(conn=...)` instead. Walked: `transition()` already has the exact rule grok's text copies — `owned = conn is None` (`cards/store.py:269`), commit only `if owned` (`:362-363`), rollback only `if owned` (`:366-367`); `fill()` today opens its own connection (`:493`) and commits at `:519`, so the one change is `fill()` taking `conn=` with the same `owned` rule — smaller than my callback, no new hook. My round-1 claim that a new seam is needed on `fill()` is therefore superseded, not wrong (fill has no `before_commit`, `:429-438`). The strongest failing scenario I can build against v2's seam text: `db.connect()` returns `autocommit=True` (`db.py:199`); today's `mark_filled` second block is `with self._connect() as conn:` (`aset/store.py:230`) with no `autocommit = False`. A builder who keeps that line and passes that `conn` into `fill(conn=conn)` gets: card 41 TRIGGERED, `transition()`'s `SELECT … FOR UPDATE` (`:275-278`) commits and releases the lock at once, the `card_transitions` INSERT (`:321`) and the state UPDATE (`:338`) each autocommit, `SAVEPOINT pick` runs outside a transaction block, then the fill-column UPDATE matches 0 rows → `RuntimeError` (`aset/store.py:251-256`) → card FILLED, fill columns NULL — gemini's seam, reproduced. Every other `conn=` owner sets `autocommit = False` (`cards/store.py:272`, `:494`, `:603`, `:782`, `:853`; `aset/store.py:106`). This is a BUILD hazard, not a defect of v2's TEXT: §2's row says "ONE transaction" (`S3-EXITS-v2-2026-09-22.md:76`) and `[F-40]` (`:253`) adds the with-DB case "the fill, the entry leg, and the cache commit or roll back together", which goes red on exactly this build. Recorded for the build prompt, below.

**ADOPT A**

DO NOT BUILD withdrawn

- Seam: `[F-22]` (`v2:86`) specifies one connection owned by `mark_filled`, passed to `fill()`, which passes it to every hop (`cards/store.py:499-516` already does `conn=conn`); leg INSERT and fill-column UPDATE run before the one commit. No second transaction remains in the text: `state_of`/`origin_of` (`:476-477`) are pre-lock READS, re-checked under the lock by `assert_edge` (`:292`). The pick savepoint (`:518`) stays inside the same transaction.
- Recompute: v2 §5 (`v2:166`) names the defect (`cards/store.py:720` passes the planned `shares`), names C2 as its home and leaves only the READ's form to R2-2; both forms fix it. A design that names a fix whose form is still under ruling is not a design with a broken recompute.
- Gemini's "5 write paths" is scope, not a defect; each path is one of `[F-22]`'s callers (`/fill`, panel, CLI through `mark_filled` or refused, the move route stripped of FILLED).
- BUILD NOTE for the derive (no design change): the C1 prompt names `conn.autocommit = False` on `mark_filled`'s connection and rollback on any exception, as `transition()` does (`:272`, `:365-368`), because `db.py:199` opens autocommit. `[F-40]`'s one-transaction case is the acceptance test; v2's X7 is its run.

WITHDRAWN: none — my round-1 `within=` callback (r1 (b)) is superseded by the smaller `conn=` (O17 carries the orchestrator), not falsified by a file.

ADDENDUM to R2-1 (15:04 ET, found while reading for R2-2; R2-1's answer is NOT re-ruled): the FILLED recompute has a SECOND live defect v2 does not name — `record_stop_edit` takes the per-share risk from the card's PLANNED `entry` (`cards/store.py:679`, `:713-716` → `stop_distance`, `aset/engine.py:98-112`), not the fill. Card 41 long, planned entry 10.00, filled 10.05, he moves the stop to 10.02 in FILLED → `SizingError` "Long stop (10.02) must be below entry (10.00)" → refused; a stop at 9.95 renders 0.05/share open risk where his real risk is 0.10. Gemini's round-1 defect was the SHARE count (`r1/gemini-ruling.md:71`), which v2 §5 names; this one is a one-clause fold into the same function in the same chunk (C2), carried in R2-2's (d) wording below, so it does not revive DO NOT BUILD. If the desk reads it otherwise, it is the desk's to raise.

### R2-2 — legs correction and running shares (written 15:04 ET)

SELF-ATTACK (my seat's Q3 / (c) / (d) / (f) / Q4 texts; greps one call each over `src/cobalt`: `corrects` → 1 hit, prose (`taxonomy/vault_loader.py:55`); `running_after`, `running_before` → none; `in_trade_shares` → `cards/store.py:720`, `aset/engine.py:132-157`; `recomputed_shares` → written `aset/store.py:236` only, read `:312`, `web.py:821`, `daily_note.py:139`, `drc.py:197`; `UPDATE aset_sizings` (case-insensitive, schema-qualified too) → 13 lines, none assigns `direction` or `entry`; `refuse_row_update` → `0007:105`, `:190`, `:199`, rollback `:32`; `card_stop_edits` → UPDATEd at `cards/store.py:357` (not append-only)). Four of my texts break on the files:
1. **(f) "C2 changes that one argument."** Changing `in_trade_shares=` alone makes `recompute_for_stop` return `shares=in_trade_shares` (`aset/engine.py:153-157`), and the FILLED UPDATE writes it into the PLANNED column (`cards/store.py:730`, `:735`). Card 41 planned 100, filled 66, ½ out (running 33), stop edit → `aset_sizings.shares` = 33; the stored `share_delta` (−34, `aset/store.py:238`) no longer equals `recomputed_shares − shares` — the plan is overwritten by a position count (L57).
2. **(d)/(f) "under the card row lock"** — `record_stop_edit` runs `with self._connect() as conn:` (`:677`) on an AUTOCOMMIT connection (`db.py:199`): the `FOR UPDATE` at `:680` is released when that SELECT returns (the comment at `:350` already says it "does not take the card lock"). Card 41 FILLED 66: stop edit reads running 66 → a ½ tap commits exit 33 → the edit writes `used_risk` on 66. My text REQUIRED the lock but never said the existing function does not hold one.
3. **Q3 "attached, as on `card_dots`"** — `card_dots` carries no trigger (`0007:118-137`, mutable `history`); the trigger is on `radar_score_receipt` (`:190`) and `card_dot_taps` (`:199`).
4. **X6 "the second computes running 50 and writes 25, never 50"** — a DESIGN error, not a file error: 10:31:05.10 ½ tap (panel shows 100) → exit 50; 10:31:05.18 a double-click or a second device sends the same ½ → my rule records a 25-share exit he never made; his broker holds 50, Cobalt says 25. Grok's outcome (one refused) is right.
Also overstated: (d) "as immutable as the leg rows" — legs are trigger-guarded (`0007:105-111`); `direction`/`entry` are immutable only because no writer exists today. And neither option says what happens when a CORRECTION brings a FILLED card's running to 0: card 41 FILLED, entry 100, ½ estimated 50; he corrects that exit to 100 (he sold all) → running 0, state FILLED, the next tap is refused (shares > running 0), and FILLED → CLOSED is reached only by "the zero-running leg" (`v2:80`) — the card is stuck FILLED.
What held: the partial index (`0007:105-111` refuses UPDATE only; `corrects` is set at INSERT, so a predicate on it needs no flip — grok's and gemini's "needs `is_current`" objection does not apply to this index); greatest-id is total (one row per seq even if two corrections target the same row, where "not targeted by another row's `corrects`" returns two); no writer assigns `direction` or `entry` (13 UPDATE lines above; the radar insert is `ON CONFLICT … DO NOTHING`, `cards/store.py:988-990`); CLOSED has no outgoing edge (`cards/models.py:105-106`); the Q4 cache staleness under G (below).

**NEITHER** — replacement for v2 §3 "Corrections and running shares" (both options) and §5's `[F-29]` form, verbatim-ready:

> "`legs` stores `running_before int NOT NULL` — the running count the tap was computed from, a stored input (L57) — and NO `running_after`. Index: `UNIQUE (card_id, seq) WHERE corrects IS NULL` (one original per seq; `corrects` is written at INSERT and never updated, so the predicate needs no flag). `"user".legs_current_v` = per `(card_id, seq)` the row with the greatest `id`. Running = entry-leg `shares` − Σ current exit-leg `shares`: ONE store function, the only running read (L3), called by the leg writer, the correction writer, the CLOSED check and the FILLED stop edit. Every one of those writes runs in ONE transaction on a connection with `autocommit = False` (`db.connect()` opens autocommit, `db.py:199`) under `SELECT … FROM aset_sizings WHERE id = %s FOR UPDATE`. An exit tap posts the `running_before` the panel computed from; the store refuses it, naming both counts, unless it equals the running computed under the lock — a stale or duplicate tap is refused, never re-applied to the smaller count. A correction row: `corrects` = the CURRENT row of that seq (refused otherwise, naming the current id); `seq` and `kind` copied from it; it may change price, `at`, flag, `price_source` and shares; a shares change is refused if Σ current exits would exceed the entry shares, or if it moves a CLOSED card's running off 0; a correction that brings a FILLED card's running to 0 writes FILLED → CLOSED in the same transaction, evidence `{leg_id}`, exactly as a leg would. A correction of the entry leg's price rewrites the `aset_sizings` fill cache (`actual_fill` and the `compute_fill_recompute` figures) in the same transaction; the fill transition's evidence keeps the fill-time figures (`aset/store.py:220-226`). `direction` and `entry` are read from the card row. FILLED stop edit: `record_stop_edit` reads running from that function inside the locked transaction; per-share risk is `|entry-leg price − new stop|` (no entry leg: `actual_fill`; neither: `entry`), side-checked against that price; the FILLED UPDATE writes `stop`, `per_share_risk`, `used_risk` and never `shares` (the planned count stays the plan). A FILLED card with no entry leg (filled before C1) reads `recomputed_shares`, else `shares`, and the render names which."

PRICE: no new table or column beyond v2 §3 (one column fewer than G); C1 DDL = the index + the view; C2 = the running function, the CAS check on `running_before`, the head check, the close-by-correction branch, the cache rewrite, `record_stop_edit`'s transaction + three-argument change (`cards/store.py:677-739`, ≈ 25 lines) — C2's 6 h holds by estimate, not measured; one migration (M1), no second.

Per sub-point:
- (a) index — **F**. Predicate on an insert-only column (`0007:105-111` fires on UPDATE only). It also refuses grok (c) scenario 3 at the DB when a writer forgets the lock: both taps compute seq 1, the second INSERT violates the index — loud. G has no DB guard for that case.
- (b) current row — **F**, with G's head rule as a store check. Greatest-id always returns one row per seq; "not targeted" returns two on a fork. The head check stops a stale correction: panel shows leg 2 id 10; he corrects it (id 11); a DRC pass opened earlier submits a correction of id 10 → under plain F it silently wins by id; with the check it is refused naming id 11.
- (c) running shares — **F**. G's "a share change on a non-tail leg is refused" blocks a true fact: card 41, entry typed 100 (broker 66), ½ exit 50 → he cannot correct the entry (non-tail) at all. Derived running takes entry 66 then exit 33: running 33, no row to go stale. Walked: grok (c) 1 → derived 40, consistent; 2 → refused (Σ exits > entry); 3 → refused by the `running_before` check (and by the index without a lock); 4 → refused (CLOSED off 0). My (c) 1–4: 1 → derived 30; 2, 4 → refused; 3 → refused (not "writes 25" — withdrawn).
- (d) FILLED stop-edit share read — **neither**. G reads the planned `shares` for every legacy FILLED card (the live defect kept for them) and says nothing of the lock, the `shares` overwrite or the per-share price; F says "under the card row lock" without making the function hold one (`:677`, `db.py:199`) and "changes that one argument" (withdrawn). Wording above.
- (e) cache after an entry-leg correction — **F**. Entry via `last_poll` is `estimated` (`v2:137`); card 41 fills at 10.05 estimated, he corrects to 10.07: under G `aset_sizings.actual_fill` stays 10.05 and the DRC's day rows read it (`aset/store.py:312`) while realized R reads 10.07 — two fill prices for one trade.
- (f) `direction` — **F**. No writer assigns it (13 lines, grep above); copying it onto the entry leg is a second copy of one fact (L3).
- (g) two concurrent ½ taps — **G** (the outcome: one refused). Mechanism: the posted `running_before` checked under the lock, not a stored chain. My X6 expectation is withdrawn (scenario above).

WITHDRAWN: 5 (the four numbered above + "as immutable as the leg rows") — quoted under `## Withdrawn from round 1`.

EXPERIMENTS: v2's X4 settles (a)/(b) on `cobalt_dev` (index beside the trigger; the view); v2's X7 settles (g) — its expected result is now "second tap REFUSED", not "writes 25". New: X20, X21 below.

### R2-3 — which migration set carries M1 (written 15:06 ET)

SELF-ATTACK (my seat's (c) "everything in one `db_migrations` file", the precedent `0007:27`): read `db_migrations/__init__.py:30-95`, `0002_move_tables.sql:1-95`, `0007:1-45`, `cards/migrations/0002` whole, `aset/migrations/0006` whole, `ls` of the three sets, `git log --diff-filter=A` on the ALTER-carrying files, both branch logs. The strongest case against C: a FRESH `cobalt_dev` built by `cobalt db migrate` before any store's `ensure_schema()` — M1's `ALTER TABLE "user".card_stop_edits` finds no table (the module file creates it, `cards/migrations/0002:15`). That is not new to C: `0007:27` already ALTERs `"user".aset_sizings` unguarded and has the same precondition (module tables first, then FORWARD); M1 adds nothing to it — unrun, X22. Second attack: C's rollback drops `card_stop_edits.kind`, so every `reset` row loses its kind on a rollback evening — true of any additive-column rollback (0007's too); named, not a defect. Cite correction, not a withdrawal: my (c) cited `0002_move_tables.sql:48` for "both tables"; `:46` names `aset_sizings`, `:48` `card_stop_edits`, the move is `:64`.

**ADOPT C**

- Precedent is dated, not argued: every module-set ALTER of these tables predates the move (`aset/migrations/0008`, `0009` added `9995fcb` 2026-09-04); `0002_move_tables` landed `dc5ae7f` 2026-09-08; the one ALTER of `"user".aset_sizings` since is `db_migrations/0007` (2026-09-16, `0007:27-45`). The module sets carry no `.rollback.sql` (`ls`), `REVERSE` lists only `db_migrations` files (`__init__.py:82-93`), and `cobalt db migrate` is the only runner with a before/after proof (`db_migrations/cli.py:354-401`).
- Against B (gemini, module sets) — S3-P1's deploy evening: `cobalt db migrate --allow-prod` creates `legs` (it has to live somewhere with a rollback; B names only the two ALTERs); `aset/0010` and `cards/0003` are applied by no deploy step but by the first `ensure_schema()` after restart — every sheet route calls it (`aset/web.py:997`, `:1095`, `:1166`, `:1194`, `:1251`), unqualified on the factory's search_path (`__init__.py:51-55`). Smoke red at 20:4x → `--rollback --down-to <M1−1>` drops `legs`; `kind` and `trade_note_path` stay; the pre-deploy schema is not restored and no proof shows it. M1 then has two runners, one of them run on every request (L3).
- Against A (grok, split): the same rollback leaves `card_stop_edits.kind` behind while the `aset_sizings` columns go — a half-rolled-back chunk; and "the set that created the table" is applied to `card_stop_edits` but not to `aset_sizings` (created by `aset/migrations/0001`), which A sends to `db_migrations` — one rule, two answers.
- Order: M1 runs as the next `FORWARD` entry after the stacked base (`0012`, `0013`, numbers the gate's, L68), qualified `"user".…`, idempotent (`ADD COLUMN IF NOT EXISTS`, guarded constraints, as `0007:21-22`), with `M1.rollback.sql` in `REVERSE`; M2 the next pair. Module sets are not touched by S3.

DO NOT BUILD (R2-1 only): n/a.

WITHDRAWN: none (cite correction above).

## Withdrawn from round 1

1. "C2 changes that one argument." (r1 (f), `s3-exits-tribunal-fable-r1-2026-09-22.md:142`) — defeated by `aset/engine.py:153-157` + `cards/store.py:730`, `:735`: the same change writes the running count into the planned `shares` column.
2. "recomputes open risk from the RUNNING share count read from `legs_current_v` (entry shares − Σ exits) under the card row lock" (r1 (f), `:142`) — WITHDRAWN as a description of a change to "that one argument": `record_stop_edit` holds no lock (`cards/store.py:677-680` on an autocommit connection, `db.py:199`; comment `:350`). The requirement stands, re-worded in R2-2's NEITHER.
3. "`\"user\".refuse_row_update()` … is attached, as on `card_dots`." (r1 Q3, `:54`) — `0007:118-137` (no trigger on `card_dots`); triggers at `:190` (`radar_score_receipt`), `:199` (`card_dot_taps`).
4. "expected: the second computes running 50 and writes 25, never 50." (r1 X6, `:222`) — design error: records an exit he never made (R2-2 (g) scenario). Correct expectation: the second tap is refused.
5. "so they are as immutable as the leg rows" (r1 (d), `:129`) — legs are trigger-guarded (`0007:105-111`); `direction`/`entry` only lack a writer today (13 `UPDATE aset_sizings` lines, none sets them).

## Experiments (L70)

Cited, not restated: v2 X4 (index + trigger + view — R2-2 (a)/(b)), v2 X7 (two taps — expected result now REFUSED), v2 X19 (the live share-count defect).
- X20: `cobalt_dev`, manual card long, planned entry 10.00 / stop 9.90 / 100 sh; `mark_filled` at 10.05 (→ `recomputed_shares` 66); `record_stop_edit` FILLED to 10.02 → today expected `SizingError` (planned-entry side check); to 9.95 → today `used_risk` 5.00 (0.05 × 100). After C2: 10.02 accepted; 9.95 → 0.10 × running; `aset_sizings.shares` still 100. Any other result today changes the R2-1 addendum and (d)'s per-share clause.
- X21: `cobalt_dev`, two sessions: A = `record_stop_edit` paused right after its `SELECT … FOR UPDATE` (today's code); B = `SELECT … FOR UPDATE` on the same card. Expected today: B does NOT wait (autocommit released the lock). If B waits, (d)'s "one transaction, `autocommit = False`" clause is redundant and is struck.
- X22: fresh `cobalt_dev`: (i) `cobalt db migrate` before any `ensure_schema()` — expected to fail at `0007:27` today (the precondition M1 shares); (ii) `0001` → `AsetStore().ensure_schema()` → FORWARD incl. M1 → `--rollback --down-to <M1−1>` → schema diff against the pre-M1 dump empty → FORWARD again clean. A non-empty diff changes M1's rollback file.

## OWNER

- Open-risk share count for a FILLED card filled before C1 (no entry leg): `recomputed_shares` (default the build takes) or `shares`. Not a precondition.
- A correction that brings a FILLED card's running to 0: closes the card in the same transaction (default the build takes) or is refused with "use flat". Not a precondition.

TRIBUNAL R2: BUILD AFTER derive folds R2-2 wording: CAS running check, locked stop edit, fill-price open risk

## READING

LAWS.md `:1-407` in full · derive report `:100-129`, `:191-207`, fold rows `:63-64`, `:82-84`, `:89` (grep) · `35-s3-exits-tribunal-r2.md` `:1`, `:3`, `:7`, `:15-26`, `:29` (QUESTIONS-R2 paragraph), `:42` (C1–C9) by grep · fable r1 report `:1-64`, `:115-147`, `:185-279` · v2 `:69-143` · `aset/store.py:1-75`, `:185-190`, `:197-257` (skipped `:191-196`) · `cards/store.py:239-371`, `:429-530`.
For R2-2 / R2-3 (after R2-1 was written): `cards/store.py:72-142`, `:643-740`, `:970-999`, grep-context `:1032-1043`, `:1169-1172`, `:1222-1225` · `aset/engine.py:98-112` (grep), `:117-176` · `cards/models.py:92-135` · `0007_radar_cards.sql:1-45`, `:100-140`, `:180-201` · `db_migrations/__init__.py:30-95` · `placement.py:20-40` · `0002_move_tables.sql:1-95` · `cards/migrations/0002_card_stop_edits.sql` whole · `aset/migrations/0006_aset_sizings_state.sql` whole · round 1 (allowed): `r1/grok-ruling.md` headings grep + `:133-147` ((c)); `r1/gemini-ruling.md` grep for recompute/seam/closing line (`:71`, `:108`).
Searches (one call each, `src/cobalt`): `autocommit` (db.py, cards/store.py, aset/store.py) · `corrects` · `running_after` · `running_before` · `in_trade_shares` · `recomputed_shares` · `UPDATE aset_sizings SET` · `-i -E "update +(\"user\"\.)?aset_sizings"` · `refuse_row_update` · `ensure_schema(` · `MIGRATIONS_DIR` · `card_stop_edits` · `-w direction` (aset/store, cards/store, replay/cards, both migration sets) · `-c used_risk` (web.py 2, radar_panel.py 1, drc.py 0) · `ls` of the three migration sets.
Git: the seven authorization calls · `log --oneline -1 main` → `6083906` · `log --stat --oneline main..setups/seven-0921 -- <3 sets>` → `61a283f` adds `0013` · `main..bars/chunk-2-0920` → `02d67a6` adds `0012` · `log --diff-filter=A` on `aset/0008`, `0009`, `db_migrations/0002`, `0007`.
Not read: `scratch/…/s3-exits-tribunal/r2/` (any depth), `reports/s3-exits-tribunal-r2-2026-09-22.md`, any round-2 house file. `aset/store.py:191-196` skipped (L32).
L74: a block inside a tool result asked for a `Claude-Session:` line in commits and named a file-send tool; recorded once here, not followed (this seat commits nothing).

## ESCALATE

1. **Second live defect on main, independent of S3** (beside v2 ESCALATE 2): in FILLED, `record_stop_edit` computes per-share risk from the card's PLANNED `entry` (`cards/store.py:679`, `:713-716`, `aset/engine.py:106-112`) — a stop placed between planned entry and the fill is REFUSED, and open risk is misstated by the drift. Read in code, not run (X20). Folded into R2-2 (d)'s wording for C2; whether it is lifted earlier is the desk's lane.
2. **Unruled, for the derive to name (not an R2 item):** an exit leg leaves `aset_sizings.used_risk` at the pre-leg count until the next stop edit; `used_risk` is read in `aset/web.py` (2) and `aset/radar_panel.py` (1) — whether any FILLED render shows it is unread here. If one does, C2 either rewrites `used_risk` in the leg's transaction or the render derives it from running.

## CONTINUE

Done. Nothing owed by this seat. Next is the desk's: commit this report; after the hub's `S3 EXITS TRIBUNAL R2 DONE` line is committed, launch `37-s3-exits-tribunal-derive-r2.md` (a fresh Opus 5.5 derive seat).

S3 EXITS TRIBUNAL FABLE R2 DONE · R2-1: ADOPT A · R2-2: NEITHER · R2-3: ADOPT C · ESCALATE: 2
