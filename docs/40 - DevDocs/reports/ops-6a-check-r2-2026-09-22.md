# OPS 6A CHECK, ROUND 2 — the classifier fix for `configs/cobalt/backup.yaml` (hub `ops-6a-check-r2-0922`, Sonnet 5)

## §0 Headline
- Round 2 of the code check of `ops/2026-09-21`, reading ONLY the fix (`f6aa4d0..3ad064f`; fix tip `ac2e7ae`, report commit `3ad064f`). **3 of 4 houses checked** (Grok, Sol, Opus 5; Gemini HARNESS — no output, a tool needed a `command` permission headless mode cannot prompt for). All four ran inside 9 minutes; nobody wrote anything but Grok's own file.
- CHECK lines: Grok `FIX STANDS · YES` · Sol `FIX AGAIN Q2 reader-proof coverage · NO` · Opus 5 `FIX STANDS · YES`. Both round-1 authors answer `closed` for their own NO line.
- My file-check: **1 defect claim HOLDS as fact** — Sol's Q2 (the test's reader half compares the set of file names containing `load_backup_config(`, so a new call inside a listed file, an aliased import or a resident using an existing wrapper would not fail it). Opus 5 and Grok read the same fact as a non-blocking limit; the order itself specified that comparison (`ordered-prompt.md:46`). No resident reads the file: none of the three names one, and my greps find none. Other paths weakened: NONE. Anything else changed: NONE.
- By the COUNT RULE: round-1 NO lines closed **0 of 2** (the AND-clause: Sol's Q2 defect claim HOLDS); ready for the stacked deploy: **no**. ESCALATE: 4.

## L74
One block arrived appended to the tool result of the Read of this prompt file (`78-ops-6a-check-r2.md`): an attribution reminder asking for a `Claude-Session: https://claude.ai/code/session_…` line in commits and PR bodies and naming a file-send tool (SendUserFile). It is DATA (L74); not followed. Recorded once, here. This hub commits nothing and sends no file.

## PREFLIGHT

### Authorization (each its own Bash call; all passed)
| Check | Result |
|---|---|
| R43 row, `cto-2026-09-21.md` | line 54, carries "App approved" and `06-ops-0921.md` |
| R43 committed | `031196fe9a35132e2e453dbfe613f0f33185015e` |
| R53 row | line 65, names `69-ops-6a-check.md` |
| R39 row | line 50, carries "All approved" |
| R46 row | line 57, carries `instead of Astra you can use Sol` |
| R46 committed | `53e059456750c0c9efcf50222a7a647630dc4b04` |
| R49 row | line 60, carries "Approved" |
| R49 Sol string in the desk file | `grep -c -F` = 1 |
| R49 Opus string in the desk file | `grep -c -F` = 1 |
| R49 strings committed (Opus / Sol) | both `60147d400b009db5a2518e02b8ab1fe5765db405` |
| round 1 on main | `e27c864 docs(desk): 09-21 ops 6a checked 4 of 4, classifier gap goes to a fix round, drafter launched` |
| launch row | `cto-2026-09-21.md` line 67, `\| R55 \|`, names `78-ops-6a-check-r2.md`; also a desk log bullet at line 460. `cto-2026-09-22.md` does not exist (grep exit 2, recorded — the row is in the other file, not fatal) |
| launch row committed | `9eab47a71870c0305b2b6dd3d1c9fbe5350ab890` |
| R39 literal `Bash(grok *) and Bash(agy *) through 2026-09-22` | line 50 of the desk file; committed `3ecbb278de133f1838d217e9745728ed6d6337e9` |
| 13 allow + 3 deny strings in `08-bars-chunk-e-check.md` | each `grep -c -F -e '"<rule>"'` = 1 (16 of 16); no Astra string in this hub's launch |

### Rows
| Rule | Command | Exit | Result |
|---|---|---|---|
| date (DATE GATE, row 1) | `date` | 0 | allowed — `Mon Sep 21 19:36:15 EDT 2026` → `<D>` = 2026-09-21, R39 proven above |
| grok | `grok --version` | 0 | allowed — `grok 1.0.25 (f7e67d6988e2) [stable]` |
| agy | `agy --version` | 0 | allowed — `1.2.7` |
| worktree | `ls /Users/cobalt/cobalt-wt/ops-0921` | 0 | allowed — present |
| fix report last line | `tail -n 3 …ops-classifier-fix-2026-09-22.md` | 0 | allowed — LAST NON-BLANK line: `OPS CLASSIFIER FIX BUILT ac2e7ae \| on f6aa4d0 \| offline 2210/0 (baseline 2209/0) \| backup.yaml classified: no resident reads (one-shot: com.cobalt.backup,com.cobalt.heartbeat) \| jobs restarts main..HEAD: exit 0, RESTARTS: none \| other paths untouched: empty diff \| ESCALATE: 6` — `<fix tip>` = `ac2e7ae`; the `jobs restarts main..HEAD:` field = `exit 0` |
| main tip | `git -C /Users/cobalt/cobalt log --oneline -1 main` | 0 | allowed — `<main tip>` = `c130519 docs(desk): 09-21 ops 6a round-2 check launched` |
| branch tip | `git -C … log --oneline -1 ops/2026-09-21` | 0 | allowed — `<tip>` = `3ad064f docs(report): ops classifier fix — backup.yaml declared one-shot-only, jobs restarts exit 0 on the branch` |
| fix commits | `git -C … log --format="%h %s" f6aa4d0..3ad064f` | 0 | allowed — three: `3ad064f docs(report): …`, `ac2e7ae docs(devdocs): jobs/restarts.md …` (Z2), `d669dfa fix(jobs): configs/cobalt/backup.yaml declared one-shot-only …` (Z1); no `wip`, none unexpected |
| staging list | `git -C … log --stat --oneline f6aa4d0..3ad064f` | 0 | allowed — exactly four paths: `configs/cobalt/jobs.yaml` (+9), `tests/cobalt/test_jobs_restarts.py` (+52), `docs/40 - DevDocs/cobalt/jobs/restarts.md` (1+/1−), `docs/40 - DevDocs/reports/ops-classifier-fix-2026-09-22.md` (+361) |
| src / ops / backup.yaml touched | `git -C … log --oneline f6aa4d0..3ad064f -- src ops configs/cobalt/backup.yaml` | 0 | allowed — EMPTY, as expected |
| `.env` in worktree | `ls /Users/cobalt/cobalt-wt/ops-0921/.env` | 1 | allowed — "No such file or directory" (as required; never read) |
| scratch folder | `ls scratch/tribunal-bars-0920` | 0 | allowed — present (many older subfolders) |
| recovery | `ls scratch/tribunal-bars-0920/ops-6a-check-r2` | 1 | allowed — "No such file or directory" = fresh run |
| stagger 68 | `tail -n 3 …stale-marker-check-2026-09-22.md` | 1 | allowed — "No such file" = not started, does not block |
| stagger 61 | `tail -n 3 …stale-score-tribunal-2026-09-21.md` / `…-2026-09-22.md` | 1 / 1 | allowed — both "No such file" = not started, does not block |
| stagger 73 | `tail -n 3 …s3-exits-tribunal-2026-09-21.md` / `…-2026-09-22.md` | 1 / 1 | allowed — both "No such file" = not started, does not block |
| Codex launch shape | `grep -c -F "Experiment field: **reads started**" …setups-tribunal-r2-2026-09-21.md` | 0 | allowed — `1` → ` < /dev/null` RECORDED AS WORKING; the Sol launch appends it |
| SOL probe | `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "Reply with only the word OK." < /dev/null` (background) | 0 | allowed — answered `OK`, no usage-limit text → **UP** (2,442 tokens) |
| OPUS probe | `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (background) | 0 | allowed — answered `OK`, no usage-limit text → **UP** |
| GROK probe | `--version` row above | 0 | **UP** |
| GEMINI probe | `--version` row above | 0 | **UP** |

**Meter count: 4 of 4 UP.** No launcher string was denied. No `mkdir` run, no `s2-p2-cards` string run, no Astra launch.

## Packet
Folder `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/ops-6a-check-r2/` (the first Write created it; no `mkdir` was run; the folder holds `built/` with the repository paths kept). No file is split into parts (every original ≤ 38,000 B; the largest, `probes.py`, is 36,966 B). Secrets: none of the four SECRETS paths (`/Users/cobalt/cobalt/data/.cobalt_vault`, `~/.cobalt_key`, any `.env`, `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt`) was opened, listed by content, staged or hashed; each is named by path only in staged code and comments (the plist and `run_backup.sh` are the branch's own files). `.env` is absent from the worktree (PREFLIGHT).

| staged file | bytes | matched against | result |
|---|---|---|---|
| `QUESTIONS-CHECK.md` (verbatim + the "Files in this folder:" paragraph) | 6,978 | the prompt's text | written by hand from the prompt |
| `fix-diff.md` (`git -C /Users/cobalt/cobalt log -p f6aa4d0..3ad064f -- . ":(exclude)docs/40 - DevDocs/reports/ops-classifier-fix-2026-09-22.md"`) | 6,459 | no original to `wc -c` against — checked differently: `grep -c "^diff --git"` = **3** (expected 3: the staging list's four paths minus the report), `grep -c "^commit "` = **2** (expected 2: Z1 + Z2, no wip). My copy holds 11 trailing-whitespace lines (`grep -c -E "[[:space:]]$"`) — the Write tool did not strip the diff's own blank context lines | see left |
| `fix-report.md` | 25,877 | original `ops-classifier-fix-2026-09-22.md`, **1 trailing-whitespace line** in the original and 1 in my copy | `wc -c` equal |
| `ordered-prompt.md` (`77-ops-classifier-fix.md` whole) | 33,005 | original, 0 trailing-whitespace lines | `wc -c` equal |
| `round1-verdicts.md` (`ops-6a-check-2026-09-22.md` lines 104-105 + 111, 193-194 + 216, 263-271, 274, each headed by its real path and lines) | 3,329 | written from the tool result of my Read of the whole round-1 report | verbatim extracts |
| `built/configs/cobalt/jobs.yaml` | 16,533 | original, 0 trailing ws | equal |
| `built/src/cobalt/jobs/restarts.py` · `config.py` · `cli.py` | 11,632 · 20,969 · 8,419 | originals, 0 trailing ws | each equal |
| `built/tests/cobalt/test_jobs_restarts.py` · `test_jobs_reads.py` | 10,354 · 8,965 | originals, 0 trailing ws | each equal |
| `built/src/cobalt/backup/config.py` · `cli.py` · `restic.py` | 6,736 · 3,014 · 11,490 | originals, 0 trailing ws | each equal |
| `built/src/cobalt/heartbeat/probes.py` · `runner.py` · `cli.py` | 36,966 · 20,290 · 1,625 | originals, 0 trailing ws | each equal |
| `built/src/cobalt/cli.py` | 23,830 | original, 0 trailing ws | equal |
| `built/ops/com.cobalt.backup.plist` · `run_backup.sh` · `com.cobalt.heartbeat.plist` | 4,601 · 1,467 · 3,390 | originals, 0 trailing ws | each equal |
| `built/docs/40 - DevDocs/cobalt/jobs/restarts.md` | 2,795 | original, 0 trailing ws | equal |
| `built/READERS-GREP.md` (four greps of `/Users/cobalt/cobalt-wt/ops-0921/src` (+`ops`, `configs` for the last), verbatim, each headed by the command as run) | 4,317 | no original to `wc -c` against — the tool results as printed | written from the tool results; its note names `src/cobalt/backup/secrets.py` as listed by the fourth grep but not staged (not among the files the order named) |

HONEST SIZE: whole packet = **273,041 B**, ≈ **68,300 tokens per checker** (÷ 4). Above the prompt's drafted 120–170 KB: the two whole documents (`ordered-prompt.md` 33,005 B and `fix-report.md` 25,877 B) alone are 58,882 B.

WROTE-NOTHING PROOF — BEFORE the launches (`ls -la`, 19:51 ET):
`scratch/tribunal-bars-0920/ops-6a-check-r2`: `built/` (19:50) · `fix-diff.md` 6459 (19:40) · `fix-report.md` 25877 (19:40) · `ordered-prompt.md` 33005 (19:41) · `QUESTIONS-CHECK.md` 6978 (19:51) · `round1-verdicts.md` 3329 (19:42).
`/Users/cobalt/cobalt-wt/ops-0921`: 27 entries, newest mtime `.gitignore` Sep 21 17:41; `.pytest_cache` 17:33, `.venv` 17:32, `src` 17:32, `tests` 17:32, everything else 17:31.

DATE GATE, second row (immediately before the launches): `date` = `Mon Sep 21 19:51:37 EDT 2026` → `<D>` = 2026-09-21; R39 literal re-proven at line 50 of `cto-2026-09-21.md`, committed `3ecbb278de133f1838d217e9745728ed6d6337e9`.

Launches (`run_in_background`, one attempt per house), all four started in one call block; `date` recorded **19:52:45 ET** immediately after (Mon Sep 21 19:52:45 EDT 2026). Clock: launched 19:52 ET → the 45-minute mark is **20:37 ET**. Launch lines as run (the sentence names the house and folder; `-s read-only` / `--permission-mode plan` / `--disallowedTools` visible):
- **GROK** — bg `bbj16crs5` — `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" -p "You are GROK. The folder is scratch/tribunal-bars-0920/ops-6a-check-r2/. Start with QUESTIONS-CHECK.md and follow it exactly. Files under built/ keep their paths. Do not open any other *-check*.md. Write your complete check to /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/ops-6a-check-r2/grok-check.md and reply with only that path."` (never `--always-approve`).
- **GEMINI** — bg `b2y95xuvl` — `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You are GEMINI. The folder is scratch/tribunal-bars-0920/ops-6a-check-r2/ (absolute path /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/ops-6a-check-r2/). Read ONLY the packet files in that folder; do NOT open any *-check*.md. Start with QUESTIONS-CHECK.md and follow it exactly. Files under built/ keep their paths. Read every file with your file viewer only. Run NO shell command - not cat, grep, ls or any pipe: a shell command is denied in this headless run and a denial ends your answer with no output. Do NOT write any file: print your complete check as your answer."`
- **SOL** — bg `b9f5d1pmk` — `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "You are SOL. The folder is scratch/tribunal-bars-0920/ops-6a-check-r2/. Start with QUESTIONS-CHECK.md and follow it exactly. Files under built/ keep their paths. Do not open any *-check*.md. Print your complete check as your final message; write no file." < /dev/null` (the ` < /dev/null` per PREFLIGHT's Codex-shape row).
- **OPUS 5** — bg `bh8e71m0w` — `claude -p --model claude-opus-5 "You are OPUS. The folder is scratch/tribunal-bars-0920/ops-6a-check-r2/. Start with QUESTIONS-CHECK.md and follow it exactly. Files under built/ keep their paths. Do not open any *-check*.md. Read files with the Read, Grep and Glob tools only. Write no file. Print your complete check as your answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/ops-6a-check-r2 --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"`

AFTER each return (`ls -la` of both folders, the denial grep `grep -c -i "denied\|not allowed\|permission" <check file>`):

| checker | returned (first `date` that saw it) | clock | folder listing vs before | worktree `ops-0921` vs before | denial grep | note |
|---|---|---|---|---|---|---|
| GEMINI | 19:53:25 | ≤ 1 min | + `gemini-check.md` (325 B, written by me) | identical (27 entries, same mtimes) | 1 — the word "permission" in its own harness line | HARNESS: `jetski: no output produced — a tool required the "command" permission that headless mode cannot prompt for, so it was auto-denied. Add an allow-rule under permissions.allow in settings.json (e.g. command(<target>)). Alternatively, re-run with --dangerously-skip-permissions to auto-approve all tools.` — exit 0. My copy holds that text plus the tool's own `[exited with code 0]` trailer line. It tried a shell command despite the sentence; recorded, no answer. ONE attempt per house: no relaunch by me |
| OPUS 5 | 19:54:54 | ≤ 2 min | + `opus-check.md` (8,127 B, written by me from its stdout, the tool's `[exited with code 0]` trailer omitted so the CHECK line is the last line) | identical | 0 | `--permission-mode plan`, Bash/Write/Edit disallowed (launch line above) |
| GROK | 19:59:15 | ≤ 6.5 min | + `grok-check.md` (8,974 B, its own file through its approved `--allow`); nothing else | identical | 0 | stdout was progress text + the path only; I read the file it wrote |
| SOL | 20:01:50 | ≤ 9 min | + `sol-check.md` (6,937 B, final message only, written by me; one transcription slip of mine — a wrong filename and a stray `...` in the last sentence of its Q5 — was found and fixed by me before any use) | identical | final message 0; its full transcript (648,768 B) 16 hits on `denied\|not allowed`, all echoed packet or memory-hook text (`no viewer if the attach is denied`, the prompt's "DENIED in the line" rules), no tool denial | `-s read-only`; the session-start hook loaded a desk memory note, recorded, nothing written |

No `WROTE:` mark on any checker. No TIMEOUT (the 45-minute mark was 20:37 ET; the last return was 20:01).

## Per question
Cells are the checkers' own words, ≤ 20 words. "Challenging" = checkers whose answer differs from the builder's claim or who raise a defect.

| question | builder's claim | grok | gemini | sol | opus | challenging |
|---|---|---|---|---|---|---|
| 1 rule right | only the two one-shots read it; report ESCALATE 1 corrects "only backup" to backup + heartbeat | HOLDS — only opener is the loader; two one-shots; no resident reads | no check (HARNESS) | UNPROVEN — call graph supports the row; `secrets.py` unstaged, not itself a defect | HOLDS — every caller traced to a one-shot or operator command; importing is not reading | 0 (Sol's UNPROVEN is a packet limit) |
| 2 real proof or tautology | real `classify` on the real diff shape, plus a source-tree check of callers and the registry row | HOLDS — real `classify`; limit: a resident using a wrapper is not caught | no check | DOES NOT HOLD — reader half compares file names only; a new call in a listed file passes | HOLDS — with a non-blocking limit: same file-set blindness; comment overclaims | 1 (Sol; Opus, Grok state the same fact as a limit) |
| 3 other paths weakened | none (`src/` untouched) | NONE — exact-path match, other arms unchanged | no check | NONE — resident `reads:` first, later handling unchanged | NONE — conservative set still seeds the row; `readers` prints RESTARTS: none | 0 |
| 4 round-1 NO lines | GREEN table exit 0, `RESTARTS: none` | both CLOSED; GREEN consistent; exit NOT CHECKABLE | no check | both CLOSED (Sol's own line first); "separate test-completeness defect in Q2" | both CLOSED; GREEN consistent; exit NOT CHECKABLE | 0 on the lines; 1 on Q2 |
| 5 anything else changed | nothing beyond the four paths | NONE | no check | NONE | NONE | 0 |

### Question 1 — every checker's answer, in full, unedited (a resident that reads the file and is not restarted is the failure this fix exists to prevent, L42)
**GROK:**
> ## (1) IS THE RULE RIGHT — HOLDS
>
> The new row is `configs/cobalt/jobs.yaml:317-325` (`fix-diff.md:39-47`). `readers:` is exactly `com.cobalt.backup` and `com.cobalt.heartbeat`. Both are `kind: one-shot` (`jobs.yaml:230-231`, `jobs.yaml:245-246`). No resident `reads:` names `configs/cobalt/backup.yaml`: aset `jobs.yaml:67-69`, radar `jobs.yaml:167-169`, and the other four are empty (`jobs.yaml:80`, `:95`, `:127`, `:158`). The schema refuses a resident reader and refuses a path any resident `reads:` (`src/cobalt/jobs/config.py:393-408`).
>
> The only opener is `load_backup_config` (`src/cobalt/backup/config.py:161`), default path `CONFIG_PATH` (`config.py:19`), `path.read_text` at `config.py:167`. Repo-wide caller grep (`READERS-GREP.md:3-16`) is the definition, the `__init__.py` re-export (`READERS-GREP.md:7-8`, no call), and these calls:
>
> | Call | Reached from | Who |
> |---|---|---|
> | `backup/restic.py:164` `snapshot` (`cfg or load_backup_config()`) | `backup/cli.py:49` `_run`, no cfg passed (`cli.py:45-49`) | `cobalt backup run`. Plist `ops/com.cobalt.backup.plist:63` → `ops/run_backup.sh:39` `exec … cobalt backup run`. Job `com.cobalt.backup`, one-shot. A hand `backup run` is the same command (`as_job` of that label, `cli.py:46-49`). |
> | `backup/cli.py:59` `_status` | `cli.py:34` | Operator `cobalt backup status`. Not a job. |
> | `backup/restic.py:269` `restore` | `backup/cli.py:76` `_restore`, no cfg passed | Operator `cobalt backup restore`. Not a job. |
> | `backup/restic.py:248` `latest_snapshot_age` | `backup/cli.py:70` and `heartbeat/probes.py:445`, both pass an already-loaded cfg | Fallback not taken on either staged caller. |
> | `heartbeat/probes.py:431` inside `backup_freshness` (`probes.py:416`) | The only call is `heartbeat/runner.py:144` inside `take_beat` (`runner.py:126`). `READERS-GREP.md:18-21`. | `run_beat` → `take_beat` (`runner.py:486-493`) ← `heartbeat/cli.py:27` `cobalt heartbeat beat` ← plist `ops/com.cobalt.heartbeat.plist:30-43` (`jobs run com.cobalt.heartbeat` then `heartbeat beat`). Job `com.cobalt.heartbeat`, one-shot. Same `take_beat` from operator `heartbeat show` (`heartbeat/cli.py:34`). |
>
> `backup_freshness` / `take_beat` / `run_beat` do not appear under any resident module (`READERS-GREP.md:18-36`; `jobs/store.py:197` is a string, not a call). `src/cobalt/cli.py:66-68` imports `backup.cli` and `heartbeat.cli` at import, and `com.cobalt.radar` imports `cobalt.cli` (`jobs.yaml:170`). Import does not call the loader. The loader runs only inside the functions above. Radar's `reads:` does not include this file.
>
> `because:` (`jobs.yaml:319-325`), line by line: loader `:161` and path `:19` — true. Called from `restic.py:164,248,269` and `cli.py:59` — those are the call sites (`:248` is the cfg-less fallback; staged callers pass cfg). One-shot `com.cobalt.backup` via `run_backup.sh:39`, plus status/restore as operator commands — true. `probes.py:431` from `runner.py:144`, one-shot `com.cobalt.heartbeat` — true (`show` uses that same `:144` call; it is an operator, not a resident). "No resident calls either" — true on the greps and on every resident `reads:`.
>
> Other readers, not residents, so they do not make the row wrong: `backup status` (`cli.py:59`), `backup restore` (`cli.py:76` → `restic.py:269`), `heartbeat show` (`heartbeat/cli.py:34` → `runner.py:144`). The `readers:` field is job labels; an unregistered label is refused (`config.py:398-403`).
>
> `src/cobalt/backup/secrets.py` contains the substring `backup.yaml` (`READERS-GREP.md:39-42`) and is not in this packet. NOT CHECKABLE FROM READS — the matching line. It is not a `load_backup_config` caller (`READERS-GREP.md:3-16`). Whether any module outside `backup/` and `heartbeat/` calls `snapshot` / `restore` / `latest_snapshot_age` is NOT CHECKABLE FROM READS — the hub grep of those names was not repo-wide. Neither is a shown resident.

**GEMINI:** no check (HARNESS — see the return table).

**SOL:**
> 1. UNPROVEN — IS THE RULE RIGHT
>
> The supplied call graph supports the declaration:
>
> - `load_backup_config()` opens the single path at `built/src/cobalt/backup/config.py:18-19,161-173`.
> - `com.cobalt.backup` reaches it through `snapshot()` at `built/src/cobalt/backup/restic.py:162-164`, from `built/src/cobalt/backup/cli.py:45-50`, launched by `built/ops/run_backup.sh:39` from `built/ops/com.cobalt.backup.plist:58-66`.
> - The operator-only paths are `status` at `built/src/cobalt/backup/cli.py:58-70` and `restore` at `built/src/cobalt/backup/cli.py:75-76` → `built/src/cobalt/backup/restic.py:266-269`. These are accurately disclosed by the row, although they are not job labels.
> - `com.cobalt.heartbeat` reaches it through `backup_freshness()` at `built/src/cobalt/heartbeat/probes.py:416-445`, called from `take_beat()` at `built/src/cobalt/heartbeat/runner.py:126-145`; its launchd command is `built/ops/com.cobalt.heartbeat.plist:30-43`.
> - Both named jobs are one-shots at `built/configs/cobalt/jobs.yaml:230-256`.
> - None of the six resident `reads:` declarations names `backup.yaml`: `built/configs/cobalt/jobs.yaml:67-70,73-81,84-96,98-129,131-170`.
> - The row and its `because:` text match those paths at `built/configs/cobalt/jobs.yaml:317-325`.
>
> No supplied path shows a resident executing the loader. Importing the top-level command modules at `built/src/cobalt/cli.py:61-80` does not execute their command handlers.
>
> However, exhaustive direct-path proof is not available from this packet: `built/READERS-GREP.md:37-47` reports that `src/cobalt/backup/secrets.py` contains `backup.yaml` and explicitly says that file was not staged. The loader-caller grep is complete for the symbol (`built/READERS-GREP.md:3-16`), but it cannot exclude a direct non-loader opener in that omitted file.
>
> NOT CHECKABLE FROM READS — inspect `src/cobalt/backup/secrets.py`, or supply a complete grep for `CONFIG_PATH`, `open`, `read_text`, and YAML loads involving the backup path. This packet limitation is not itself a fix defect.

**OPUS 5:**
> ## (1) Is the rule right — **HOLDS**
>
> **Every caller of `load_backup_config`, traced to its job**
>
> | Caller (file:line) | Reached from | Job | Kind |
> |---|---|---|---|
> | `built/src/cobalt/backup/restic.py:164` (`snapshot`) | `backup/cli.py:45-49` `_run` ← `ops/run_backup.sh:39` `uv run cobalt backup run` ← `ops/com.cobalt.backup.plist:63` | `com.cobalt.backup` | one-shot (`jobs.yaml:230-231`) |
> | `restic.py:248` (`latest_snapshot_age`, a fallback) | `backup/cli.py:70` and `heartbeat/probes.py:445`. Both pass an already-loaded `cfg`, so neither hits the fallback. | operator / heartbeat | — |
> | `restic.py:269` (`restore`) | `backup/cli.py:75-76` `cobalt backup restore` | operator command, no job | — |
> | `backup/cli.py:59` (`_status`) | `cobalt backup status` | operator command, no job | — |
> | `heartbeat/probes.py:431` (`backup_freshness`) | `heartbeat/runner.py:144` inside `take_beat` ← `run_beat` `:493` ← `heartbeat/cli.py:27` ← `ops/com.cobalt.heartbeat.plist:30-43` (`cobalt jobs run com.cobalt.heartbeat … cobalt heartbeat beat`) | `com.cobalt.heartbeat` | one-shot (`jobs.yaml:245-246`) |
> | `heartbeat/cli.py:34` (`cmd_show` → `take_beat`) | `cobalt heartbeat show` | operator command, no job | — |
>
> **No resident reaches it**
> - No resident's `reads:` names the file: `jobs.yaml:67-69, 80, 95, 127, 158, 167-169`. The schema would refuse the new row if one did (`config.py:393-397`).
> - The greps over `src/cobalt/radar` and `src/cobalt/aset` are empty (`fix-report.md:190`).
> - `READERS-GREP.md:3-16` finds no caller under `src/cobalt_agent`, so the old-tree `com.cobalt.agent` does not call the loader.
> - Radar's static import reach does include backup and heartbeat code (`jobs.yaml:170` `imports: [cobalt.cli]`; `cli.py:66,68`). But importing is not reading:
>   - `CONFIG_PATH` (`backup/config.py:19`) is only a path constant; the file is read only when the loader is called (`:167`).
>   - The loader is called only when `cli.py:518-519` dispatches to the `backup` or `heartbeat` subcommand.
>
> **`because:` text, line by line (`jobs.yaml:320-325`, i.e. `fix-diff.md:42-47`)**
> - Line 1, `config.py:161` and `:19`: true.
> - Line 2, `restic.py:164,248,269` and `cli.py:59`: true as call sites. Attributing `:248` to "backup run / status / restore" is loose (see the table above), but nothing stated is false.
> - Line 3, `run_backup.sh:39`: true.
> - Lines 4-5, `probes.py:431` from `runner.py:144`: true.
> - Line 6, "No resident calls either": true on the greps.
> - Minor omission: `cobalt heartbeat show` is also an operator reader and isn't named. This doesn't affect restarts.
> - The date "Added 2026-09-22" was dictated by the order (`ordered-prompt.md:48`). The commit itself is dated 2026-09-21 19:30 ET (`fix-diff.md:24`). Cosmetic.
>
> **Not checkable from reads**
> - `src/cobalt/backup/secrets.py` contains the string `backup.yaml` (`READERS-GREP.md:41`) but isn't staged. It holds no quoted `"backup.yaml"` literal (`fix-report.md:145-147`), and it is reached from `restic.py:134`. To settle whether anything outside backup/heartbeat reaches it, run `grep -rn "backup.secrets\|backup import secrets" src`.
> - Radar's plist `ProgramArguments` aren't staged. Settling this needs a read of `ops/com.cobalt.radar.plist`.

The full text of all three answers is also in `grok-check.md`, `sol-check.md`, `opus-check.md` in the packet folder.

## Round-1 NO lines
| line | its author's round-2 words verbatim | the other houses' readings | verdict |
|---|---|---|---|
| Sol: `Classifier gap unresolved; derived all-resident restarts cannot be silently dropped.` | Sol: "Sol, my line first: CLOSED. The classifier gap is no longer silently replaced by judgment. `backup.yaml` now reaches the explicit declaration arm at `built/src/cobalt/jobs/restarts.py:201-208`; unknown configs still escalate at `built/src/cobalt/jobs/restarts.py:251-272`." CHECK line: `closed sol`. Also: "Round 2 nevertheless finds the separate test-completeness defect in question 2." | Grok: closed — rule + test, not a deploy sentence. Opus: closed — a declared, schema-validated rule with a red-first test. Gemini: no check. | **NOT CLOSED** (count rule) — the author answers `closed`, but Sol's Q2 defect claim HOLDS in my file-check (row 15 below) |
| Opus 5: `classifier exits 1 on backup.yaml; needs rule or ruling` | Opus 5: "Opus 5 (mine), 'classifier exits 1 on backup.yaml; needs rule or ruling': CLOSED. The fix is a declared, schema-validated rule (`jobs.yaml:317-325`) with a red-first test (`test_jobs_restarts.py:154-203`). No code path was added and no 'none' came from judgement." CHECK line: `closed opus`. | Grok: closed — the row keeps `escalate` false so the raise does not fire. Sol: closed — a registry rule and a regression test exist. Gemini: no check. | **NOT CLOSED** (count rule) — same reason: the AND-clause fails on Sol's Q2 defect claim |

**Round-1 NO lines closed: 0 of 2** (mechanical: both authors said `closed`; the second condition — none of question 1–3's defect claims HOLDS — fails on one).

## Checked against the branch
Each row opened by me: the ORIGINAL under `/Users/cobalt/cobalt-wt/ops-0921/` or the shared object store, by Read or grep. Lines are of the original file unless said otherwise.

| # | claim | who | file:line | verdict | note |
|---|---|---|---|---|---|
| 1 | the new row's readers are exactly `com.cobalt.backup` and `com.cobalt.heartbeat`, both `one-shot` | grok, sol, opus | `jobs.yaml:317-318`; `:230-231`, `:245-246` | HOLDS | `grep -n backup.yaml jobs.yaml` = one hit, `:317` |
| 2 | no resident's `reads:` names `backup.yaml` | grok, sol, opus | `jobs.yaml:67-69, 80, 95, 127, 158, 167-169` | HOLDS | the only `backup.yaml` in `jobs.yaml` is `:317`; the schema refuses a resident reader (`config.py:393-408`) |
| 3 | the callers of `load_backup_config` are `probes.py:431`, `backup/cli.py:59`, `restic.py:164,248,269` (+ definition, re-exports) | grok, sol, opus | `READERS-GREP.md:3-16` (my grep of the branch `src`) | HOLDS | same lines as the builder's `fix-report.md` READERS |
| 4 | `restic.py:248`'s fallback is not taken from `backup/cli.py:70` or `probes.py:445` (both pass `cfg`) | grok, opus | `cli.py:70` `latest_snapshot_age(cfg)`; `probes.py:445` `latest_snapshot_age(cfg)` | HOLDS | grep on the two files |
| 5 | `backup run`: `restic.py:164` ← `cli.py:49` (no cfg) ← `run_backup.sh:39` ← plist `:63` | all three | `cli.py:49`; `run_backup.sh:39`; `com.cobalt.backup.plist:63` | HOLDS | read |
| 6 | heartbeat: `probes.py:431` ← `runner.py:144` in `take_beat` ← `run_beat` `:493` ← `heartbeat/cli.py:27`; plist `:30-43` | all three | `runner.py:126,144,486,493`; `heartbeat/cli.py:27,34`; `com.cobalt.heartbeat.plist:30-43` | HOLDS | read |
| 7 | importing `cobalt.cli` (which radar imports) mounts `backup.cli` and `heartbeat.cli` but calls no loader | grok, sol, opus | `cli.py:66,68`; `jobs.yaml:170`; `backup/cli.py:18,59` (call is inside `_status`) | HOLDS | `backup/__init__.py` not opened: the grep shows only an import (`:3`) and an export (`:12`) |
| 8 | no resident module or plist reaches the loader or a beat entrypoint | all three | my grep of `src/cobalt/radar`, `src/cobalt/aset` for `cobalt.backup\|load_backup_config\|take_beat\|run_beat\|backup_freshness`: one hit, `radar/notes.py:160` `BACKUP_LABEL = "com.cobalt.backup"` (a label); `grep -n -i "backup\|heartbeat" ops/com.cobalt.radar.plist`: no output | HOLDS | round 1's `READERS-GREP` for `src` finds no `cobalt_agent` caller |
| 9 | no module outside `backup/` and `heartbeat/` imports from `cobalt.backup` except `cobalt/cli.py` | (grok: NOT CHECKABLE) | my grep of `src` for `from cobalt.backup\|import cobalt.backup\|from cobalt import backup\|backup.secrets`: `cli.py:66`, `probes.py:426-427`, `restic.py:134` | HOLDS | relative imports inside `backup/` are not covered |
| 10 | `src/cobalt/backup/secrets.py` holds the string `backup.yaml` — an opener? | grok, sol, opus | `secrets.py:54` | HOLDS (a string) | `grep -n backup.yaml secrets.py` = one hit, inside an error message: `"in a plist, or in configs/cobalt/backup.yaml."`; the file was not opened whole |
| 11 | the row's `because:` is true line by line | grok, opus | `jobs.yaml:319-325` vs `config.py:19,161`; `restic.py:164,248,269`; `cli.py:59`; `run_backup.sh:39`; `probes.py:431`; `runner.py:144` | HOLDS | every cited line matches its file |
| 12 | `because:` omits `cobalt heartbeat show`, also an operator reader | opus, grok | `heartbeat/cli.py:34`; `jobs.yaml:320-325` | HOLDS | an operator command, not a job and not a resident |
| 13 | "Added 2026-09-22" while the commit is dated 2026-09-21 | opus | `jobs.yaml:325`; `fix-diff.md` `commit d669dfa` date `Mon Sep 21 19:30:57 2026 -0400` | HOLDS | the date was ordered (`ordered-prompt.md:48`); cosmetic |
| 14 | the test runs the real `classify` with no registry on `Change("configs/cobalt/backup.yaml","M")` | all three | `test_jobs_restarts.py:168-171`; `restarts.py:172-173` | HOLDS | read |
| 15 | the test's reader half compares only the SET OF FILE NAMES that contain `load_backup_config(` / the literal `"backup.yaml"`; a new call inside a listed file, a loader import under an alias, or a resident using `take_beat` / `backup_freshness` would not change it | **sol** (defect: DOES NOT HOLD, FIX AGAIN), opus and grok (a non-blocking limit) | `test_jobs_restarts.py:177-195`: `callers = sorted(str(p.relative_to(repo)) for p in … if "load_backup_config(" in p.read_text())`, `assert callers == [4 file paths]` `:184-189`; `assert literal == ["src/cobalt/backup/config.py"]` `:195` | HOLDS (as a fact) | I run nothing; by read the assertion is a file-name set, and `from x import load_backup_config as f` contains no `load_backup_config(`. The order specified this comparison (`ordered-prompt.md:46`: "the source files that CALL the loader … EQUAL the list"). Sol reads it as a defect, Opus and Grok as a limit — both quoted in `## Per question`, neither smoothed |
| 16 | the test's comment overclaims: "Both halves of that claim are checked here, so the rule stops being true the moment a new reader appears" | sol, opus | `test_jobs_restarts.py:162-163` | HOLDS (the text) | the comment says exactly that; what the assertions catch is row 15 |
| 17 | removing or misspelling the row falls to `UNCLASSIFIED CONFIG` and fails `:172` | all three | `restarts.py:251-253`; `test_jobs_restarts.py:172` | HOLDS (by read) | not run |
| 18 | dropping a reader fails `:175` and `:199`; a resident reader or a resident `reads:` naming the file crashes registry load | all three | `test_jobs_restarts.py:175,197-199`; `config.py:393-408` | HOLDS (by read) | not run |
| 19 | the quoted RED is consistent with the test and round 1's table | all three | `fix-report.md:205-212`; `test_jobs_restarts.py:172`; `fix-report.md:62` | HOLDS (consistency) | the RED and the pytest lines themselves: NOT CHECKABLE FROM READS — `uv run pytest -k backup_yaml` |
| 20 | 38 = 15 + 23 tests | opus | `test_jobs_restarts.py` has 15 `def test_`, `test_jobs_reads.py` 23 | HOLDS (the count) | that they passed is the builder's claim (`fix-report.md:222`) |
| 21 | no other path classifies differently: exact-path lookup after `readers_of`; `:217-218` and `:251-256` unchanged | all three | `config.py:441-446`; `restarts.py:201-208, 217-218, 251-256`; `fix-diff.md` touches no `src/` | HOLDS | check (ii) below is EMPTY |
| 22 | `cobalt jobs readers configs/cobalt/backup.yaml` now prints `RESTARTS: none`, exit 0 | opus, sol | `jobs/cli.py:100-107` | HOLDS (by read) | not run |
| 23 | the conservative set is empty on this branch, so the `backup.yaml` restart column reads `-` | opus | `restarts.py:190,257,265`; `jobs.yaml:70,81,96,128,159,170` (all six residents declare `imports:`); `fix-report.md:103,284` (`tests/cobalt/test_backup.py … -`) | NOT CHECKABLE FROM READS | an unresolved dynamic import comes from an AST scan (`restarts.py:114-155`) I did not run; the table is the builder's claim |
| 24 | `jobs/store.py:197` is a string, not a call | grok | `store.py:197` (grep): `"beat detail and green_summary_date belong to run_beat"` | HOLDS | — |
| 25 | `fix-diff.md` changes nothing the order did not name | all three | checks (i)–(iv) below | HOLDS | — |

My own checks, stated plainly:
- **(i)** `git -C /Users/cobalt/cobalt log --stat --oneline f6aa4d0..3ad064f` names exactly four paths: `configs/cobalt/jobs.yaml` (+9), `tests/cobalt/test_jobs_restarts.py` (+52), `docs/40 - DevDocs/cobalt/jobs/restarts.md` (1+/1−), `docs/40 - DevDocs/reports/ops-classifier-fix-2026-09-22.md` (+361). HOLDS.
- **(ii)** `git -C … log --oneline f6aa4d0..3ad064f -- src ops configs/cobalt/backup.yaml` is EMPTY. HOLDS.
- **(iii)** the `jobs.yaml` hunk of `fix-diff.md` is `@@ -314,6 +314,15 @@ no_resident_reads:` with NINE `+` lines and NO `-` line, all inside `no_resident_reads:`. Every `+` line:
  `+  - path: configs/cobalt/backup.yaml` · `+    readers: [com.cobalt.backup, com.cobalt.heartbeat]` · `+    because: >` · `+      load_backup_config() (backup/config.py:161; the one path :19) is called` · `+      by backup/restic.py:164,248,269 and backup/cli.py:59 — the one-shot` · `+      com.cobalt.backup (`cobalt backup run`, ops/run_backup.sh:39) plus` · `+      status/restore, operator commands — and by heartbeat/probes.py:431` · `+      (backup_freshness, from runner.py:144), the one-shot com.cobalt.heartbeat.` · `+      No resident calls either. Added 2026-09-22 (6a range; round-1 check).` HOLDS.
- **(iv)** `grep -n "^-[^-]" fix-diff.md` = ONE line, `:17`, the DevDoc's replaced sentence (`-A config path with no resident reader … (ruled 2026-09-15; first row `configs/cobalt/notify.yaml`).`); no `-` line in the test file; `grep -c "^+.*\(skip\|xfail\)" fix-diff.md` = **0**. The new test adds 52 `+` lines and no assertion is removed. HOLDS.
- **(v)** `grep -n "reads:" configs/cobalt/jobs.yaml` = `:67 reads:` · `:80 reads: []` · `:95 reads: []` · `:127 reads: []` · `:158 reads: []` · `:167 reads:` · `:301 no_resident_reads:`. Exactly six lines START with `reads:` after stripping indentation (one per resident; `test_jobs_reads.py:88-89` counts them); `:301` is the table header and starts `no_resident_reads:`; none is inside the new row. HOLDS.
- **(vi)** `grep -n "backup.yaml" configs/cobalt/jobs.yaml` = ONE hit, `:317: - path: configs/cobalt/backup.yaml`, inside `no_resident_reads:`. HOLDS.
- **(vii)** the builder's GREEN `jobs restarts main..HEAD` (`fix-report.md:238-286`): its `backup.yaml` row (`:242`) `configs/cobalt/backup.yaml	M	no resident reads (one-shot: com.cobalt.backup,com.cobalt.heartbeat)	-`; its `jobs.yaml` row (`:243`) `configs/cobalt/jobs.yaml	M	registry; register, no restart	-`; its `RESTARTS:` line (`:286`) `RESTARTS: none`; its exit **0** (`:238`). Each agrees with the code as read: rule string built at `restarts.py:206-207` from `readers:` in declared order (`jobs.yaml:318`), `registry; register, no restart` at `:217-218`, `RESTARTS: none` at `:270`, no raise when nothing escalates at `:271-272`. The table and its exit are NOT CHECKABLE FROM READS (I run no `uv`) — the builder's claim.

Where two checkers contradict, both quoted, neither smoothed: on the test's reader half, Sol — "The reader-proof half is insufficient. It compares only the set of filenames containing the literal text `load_backup_config(` … It does not count call sites, map those callers to entrypoints, scan plists, or prove runtime reachability from resident jobs." — against Opus 5 — "The builder did exactly what the order specified (`ordered-prompt.md:46`), which copies the smoke test's pattern (`:238-243`). I don't count this as a defect of the fix." — and Grok — "Limit, not a miss against the ordered test: a resident that reached the file only by calling `take_beat` / `snapshot` / `restore`, without `load_backup_config(` or `"backup.yaml"` in its own source, would not fail `:184-195`. No such caller is in the greps."

## Ready for the stacked deploy
| checker | CHECK line | ready | reason verbatim |
|---|---|---|---|
| grok | `CHECK: FIX STANDS · round-1 NO lines: closed sol, closed opus · ready for the stacked deploy: YES` | YES | (none given) |
| gemini | NO CHECK LINE — HARNESS (no output) | — | — |
| sol | `CHECK: FIX AGAIN Q2 reader-proof coverage · round-1 NO lines: closed sol, closed opus · ready for the stacked deploy: NO · reader-proof misses transitive resident paths through existing loader wrapper calls` | NO | reader-proof misses transitive resident paths through existing loader wrapper calls |
| opus | `CHECK: FIX STANDS · round-1 NO lines: closed sol, closed opus · ready for the stacked deploy: YES` | YES | (none given) |

COUNT RULE (not a verdict): houses that checked 3 ≥ 3 · defects that HOLD 1 · a checker's line reads `CHECK: FIX AGAIN` · round-1 NO lines closed 0 of 2 → `no`.

## ESCALATE
1. **Sol's `CHECK: FIX AGAIN Q2` — a defect claim whose facts HOLD in my file-check (row 15).** The new test's reader half is a comparison of file-name sets (`test_jobs_restarts.py:177-195`); it does not fail on a new call inside a listed file, an aliased loader import, or a resident starting to use an existing wrapper (`take_beat`, `backup_freshness`, `latest_snapshot_age()` with no cfg). Sol asks for a test tied to caller/entrypoint reachability. Opus 5 and Grok state the same fact as a non-blocking limit, name no resident reader, and read `FIX STANDS`; the order named this exact comparison (`ordered-prompt.md:46`, copying the smoke test's shape at `test_jobs_restarts.py:238-243`). No checker and none of my greps finds a resident that reads the file. I add no recommendation.
2. **Both round-1 NO lines are NOT CLOSED by the count rule** (0 of 2): Sol and Opus 5 each answer `closed` for their own line, but Sol's Q2 defect claim HOLDS in my file-check, which fails the rule's second condition for both. The classifier gap itself (exit 1, `UNCLASSIFIED CONFIG`) is answered as read by all three checkers; the exit-0 table is the builder's claim, NOT CHECKABLE FROM READS.
3. **ASK DESK: gemini did not check (HARNESS: `jetski: no output produced — a tool required the "command" permission that headless mode cannot prompt for, so it was auto-denied …`) — relaunch it alone? [20:03 ET]** Safe default taken: no retry by me; three houses checked (L67's floor met), the last line counts who checked.
4. **ALWAYS — this was round 2 of at most 3 (L67): what it leaves open goes to a round-3 fix only on the desk's call, and what round 3 would leave open goes to Dejan.** Round-1 ESCALATE 2 (the `cli.md` seam), 3 (the circular restore → 6b) and 4 (restic `forget` grouping) carried by id, not re-asked.

Not escalated, recorded: no resident reader named or found (rows 2, 7–9); no weaker assertion, no `skip`/`xfail` (iv); no secret value anywhere (L4 — none of the four SECRETS paths was opened; `secrets.py` was grepped for one line only); no packet mismatch (the diff and every original matched, see Packet); no checker marked `WROTE:`; the drafter's ESCALATE (registry form vs `restarts.py`) was raised by no checker.

## CONTINUE
step: closed — all checkers returned, file-checks done, nothing left to run. (Resume rule, if relaunched: `ls scratch/tribunal-bars-0920/ops-6a-check-r2`; the four `<house>-check.md` files exist, no house is re-asked.)

OPS 6A CHECK R2 DONE · grok: CHECK: FIX STANDS · round-1 NO lines: closed sol, closed opus · ready for the stacked deploy: YES · gemini: HARNESS · sol: CHECK: FIX AGAIN Q2 reader-proof coverage · round-1 NO lines: closed sol, closed opus · ready for the stacked deploy: NO · reader-proof misses transitive resident paths through existing loader wrapper calls · opus: CHECK: FIX STANDS · round-1 NO lines: closed sol, closed opus · ready for the stacked deploy: YES · houses that checked: 3 of 4 · round-1 NO lines closed: 0 of 2 · defects that HOLD: 1 · ready for the stacked deploy: no · ESCALATE: 4
