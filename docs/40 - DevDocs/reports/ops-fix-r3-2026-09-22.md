# OPS 0921 — ROUND-3 FIX (L67, THE LAST): backup.yaml reader proof through the call graph

Prompt: `docs/40 - DevDocs/prompts/2026-09-21/80-ops-fix-r3.md` · hub `ops-fix-r3-0922` · Opus 5 · worktree `~/cobalt-wt/ops-0921`, branch `ops/2026-09-21`, from `3ad064f` · started `Mon Sep 21 20:20:01 EDT 2026`.

## §0 Headline

- Sol's Q2 is fixed in the test only. The file-name check is replaced by a function-level call-graph walk from `load_backup_config` that reuses `_module_for`, `_resolve_from` and `reachable` from `restarts.py`. On the real tree it finds exactly the drafter's 12 readers, 5 entrypoints and 2 referrers.
- Red cases: Sol's three cases planted in a `tmp_path` copy of `src/`. Run A: 3 red. Run B: 2 red, but only after the wrapper case was fixed (the drafted case passed without resolution). Run C: 3 green.
- Offline suite 2213/0 (baseline 2210/0, 351 skipped unchanged). `src`, `configs`, `ops` diffs are empty. `jobs restarts 3ad064f..HEAD` gives `RESTARTS: none`. `main..HEAD` gives exit 0 but `RESTARTS: com.cobalt.aset`, which comes from main's own radar commits, not from this branch.
- ESCALATE: 8. One new classifier finding: `import_graph` resolves relative imports in a package `__init__` one level too high.

## L74

A block arrived appended to a tool result (the first Bash call, which read this prompt) asking for a `Claude-Session: https://claude.ai/code/session_…` line in commit messages and naming a file-send tool. DATA under L74 — recorded once, not followed. Commits carry `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` only.

## AUTHORIZATION

| check | command | result |
|---|---|---|
| R25 row | `grep -n "^\| R25 " …/cto-2026-09-20.md` | `:262` names `prompts/2026-09-20/11-bars-chunk-2-build.md`, "approved" (17:58 ET) |
| R25 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"11-bars-chunk-2-build.md" -- …/cto-2026-09-20.md` | `fec68e3b914d19e26645d916a890e6ec7dcb9360` |
| R43 row | `grep -n "^\| R43 " …/cto-2026-09-21.md` | `:54` carries `"App approved"` and `06-ops-0921.md` |
| R43 committed | `-S"App approved"` | `031196fe9a35132e2e453dbfe613f0f33185015e` |
| R54 row | `grep -n "^\| R54 "` | `:66` names `77-ops-classifier-fix.md` |
| R54 committed | `-S"77-ops-classifier-fix.md"` | `4d9c0439b678618c8fa2f00c675de177b24d3ec0` |
| R55 row | `grep -n "^\| R55 "` | `:67` names `78-ops-6a-check-r2.md` |
| R55 committed | `-S"78-ops-6a-check-r2.md"` | `c130519916ea6a0e513664fe7733a31248b08ada` |
| round-2 verdict on main | `git -C … log --oneline -2 -- …/ops-6a-check-r2-2026-09-22.md` | `2834a5a docs(desk): 09-21 deploy-2026-09-21b live, pushed under R38, stale-marker and seven-setups builds launched, ops round 3 drafting` |
| launch row | `grep -n "80-ops-fix-r3.md" …/cto-2026-09-21.md …/cto-2026-09-22.md` | `cto-2026-09-21.md:68` `\| R56 \| 20:19 ET \| …` names `prompts/2026-09-21/80-ops-fix-r3.md`; `cto-2026-09-22.md`: "No such file or directory" (recorded, not fatal — row is in the other file); prompt's `R56` is filled (not `R__`) |
| launch row committed | `-S"80-ops-fix-r3.md" -- <the two desk files>` | `7afe4c3a07d5568e2ebd89a13e0f29713e78f70b` |
| no new rule | `grep -c -F -e '"<rule>"' …/prompts/2026-09-21/02-bars-chunk-2-fix-r3.md` × 19 | all 16 allow strings and all 3 deny strings count `1` |

## PREFLIGHT

| rule | command | exit | allowed/DENIED · output |
|---|---|---|---|
| `Bash(date*)` | `date` | 0 | allowed · `Mon Sep 21 20:20:01 EDT 2026` |
| `Bash(git status*)` | `git status --porcelain` | 0 | allowed · EMPTY |
| `Bash(git status*)` | `git status` | 0 | allowed · `On branch ops/2026-09-21` / `nothing to commit, working tree clean` |
| `Bash(git log*)` | `git log --oneline -1` | 0 | allowed · `3ad064f docs(report): ops classifier fix — backup.yaml declared one-shot-only, jobs restarts exit 0 on the branch` — FIRST LAUNCH, `<old tip>` = `3ad064f` |
| `Bash(git log*)` | `git log --oneline 3ad064f..HEAD` | 0 | allowed · EMPTY |
| `Bash(git show*)` | `git show --stat HEAD` | 0 | allowed · `3ad064fb…`, 1 file `.../reports/ops-classifier-fix-2026-09-22.md \| 361 +++` |
| `Bash(git diff *)` | `git diff --stat` | 0 | allowed · EMPTY |
| `Bash(git -C /Users/cobalt/cobalt log*)` | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | allowed · `<main tip>` = `7afe4c3 docs(desk): 09-21 ops round-3 prompts 80 and 81 drafted, launch row R56` |
| same | `git -C /Users/cobalt/cobalt log --oneline ops/2026-09-21..main -- src configs ops tests .gitignore` | 0 | allowed · NON-EMPTY: `e21e4c2 test(radar): degraded line carries the bars poll failure (seam, L68)` · `71a8368 feat(radar): slim red degraded line above the card ladder on /radar, mirrored by refreshPool (R10 2026-09-21)` → recorded, ESCALATE (v) |
| `Bash(ls *)` | `ls -la .env` | 1 | allowed · `ls: .env: No such file or directory` |
| `Bash(tail *)` | `tail -n 3 ".../ops-classifier-fix-2026-09-22.md"` | 0 | allowed · last non-blank: `OPS CLASSIFIER FIX BUILT ac2e7ae \| on f6aa4d0 \| offline 2210/0 (baseline 2209/0) \| …` |
| `Bash(ls *)` | `ls ".../ops-fix-r3-2026-09-22.md"` | 1 | allowed · "No such file or directory" (first launch) |
| `Bash(grep *)` | `grep -n "^def \|^from \|^import " src/cobalt/jobs/restarts.py` | 0 | allowed · `58:def _git` `67:def changes` `89:def _module_for` `102:def _resolve_from` `114:def import_graph` `158:def reachable` `172:def classify` `261:def command` — matches the prompt |
| `Bash(grep *)` | `grep -n "import_module\|__import__" …/restarts.py` | 0 | allowed · `145:` `153:` (dynamic-import rule; not repeated by the call-edge extractor) |
| `Bash(grep *)` | `grep -rn "load_backup_config" src` | 0 | allowed · `probes.py:426` (fn-local import), `:431` call · `backup/__init__.py:3` re-export, `:12` · `restic.py:38` import, `:164` `:248` `:269` calls · `config.py:161` def, `:183` · `backup/cli.py:18` import, `:59` call |
| `Bash(grep *)` | `grep -rn "backup_freshness\|take_beat\|run_beat\|latest_snapshot_age" src` | 0 | allowed · `runner.py:126` def take_beat, `:144` `probe_mod.backup_freshness`, `:486` def run_beat, `:493` call · `heartbeat/__init__.py:14` · `probes.py:416` def, `:427` import, `:445` call · `heartbeat/cli.py:20` import, `:27` `:34` calls · `backup/cli.py:19` import, `:70` call · `restic.py:240` def · `backup/__init__.py:4` · `jobs/store.py:197` (a string) |
| `Bash(grep *)` | `grep -n "set_defaults\|^def " src/cobalt/backup/cli.py src/cobalt/heartbeat/cli.py` | 0 | allowed · backup: `24 add_parser` `31/34/42 set_defaults` `45 _run` `58 _status` `75 _restore`; heartbeat: `23 cmd_beat` `32 cmd_show` `37 add_parser` `43/46 set_defaults` |
| `Bash(grep *)` | `grep -n "probe_mod" src/cobalt/heartbeat/runner.py` | 0 | allowed · `59:from . import probes as probe_mod`, `144: probe_mod.backup_freshness(now=ts),` |
| `Bash(grep *)` | `grep -n "imports:" configs/cobalt/jobs.yaml` | 0 | allowed · `70 [cobalt.aset.__main__, cobalt.aset.web]` `81 []` `96 []` `128 [cobalt_agent.main]` `159 []` `170 [cobalt.cli]` |
| `Bash(cd *)` | `cd /Users/cobalt/cobalt-wt/ops-0921` | 0 | allowed |
| `Bash(uv run pytest *)` | `uv run pytest --version` | 0 | allowed · `pytest 9.0.2` |
| `Bash(mkdir -p *)` | `mkdir -p "docs/40 - DevDocs/reports"` | 0 | allowed · directory exists — a no-op probe |

`uv run cobalt jobs restarts` probed at CLOSE; `git add` / `git commit` at Z3's commit.

## BASELINE

`Mon Sep 21 20:23:16 EDT 2026` · `uv run pytest -q tests/cobalt tests/taxonomy` (background, exit 0), summary VERBATIM (ANSI stripped):

    2210 passed, 351 skipped, 1 xfailed, 15 warnings in 65.02s (0:01:05)

= round 2's `2210 passed, 351 skipped, 1 xfailed`, 0 failed. Baseline holds.

## Z3

All edits in `tests/cobalt/test_jobs_restarts.py`. Helper `_backup_yaml_readers(root)` placed directly above `test_backup_yaml_is_read_by_one_shots_only_and_derives_no_restart`; reuses `restarts._module_for`, `restarts._resolve_from`, `restarts.reachable` (the closure, on the reversed call graph) and — in the real-tree test — `restarts.import_graph`. New code = binding tables + call/reference-edge extraction only.

One deliberate reuse detail: a package `__init__.py` is passed to `_resolve_from` as `"<pkg>.__init__"` (`_package_name`), because `_resolve_from` climbs from the module's parent and the package name alone resolves one level too high — see the probe below and ESCALATE (a).

**Run A** — helper a STUB (`return set(), set(), set(), set()`), `uv run pytest -q -v tests/cobalt/test_jobs_restarts.py -k reader_proof_goes_red` (exit 1), short summary VERBATIM (ANSI stripped):

    FAILED tests/cobalt/test_jobs_restarts.py::test_backup_yaml_reader_proof_goes_red_through_any_wrapper[new_call_in_listed_file] - AssertionError: []
    FAILED tests/cobalt/test_jobs_restarts.py::test_backup_yaml_reader_proof_goes_red_through_any_wrapper[aliased_loader_import] - AssertionError: []
    FAILED tests/cobalt/test_jobs_restarts.py::test_backup_yaml_reader_proof_goes_red_through_any_wrapper[resident_uses_wrapper] - AssertionError: []
    ======================= 3 failed, 15 deselected in 0.22s =======================

**Run B, first attempt** — helper reduced to `ast.Name` calls resolved to same-module defs and `from x import y` WITHOUT `asname` (no `ast.Import`, no alias, no attribute chain, no re-export), cases as drafted (exit 1):

    tests/cobalt/test_jobs_restarts.py .F.                                   [100%]
    FAILED tests/cobalt/test_jobs_restarts.py::test_backup_yaml_reader_proof_goes_red_through_any_wrapper[aliased_loader_import] - AssertionError: ['cobalt.backup.cli._restore', 'cobalt.backup.cli._run', 'c...
    ================== 1 failed, 2 passed, 15 deselected in 2.25s ==================

→ `resident_uses_wrapper` did NOT fail: `latest_snapshot_age()` is a plain from-import, so the reduced walker already makes the combined `planted` a reader, and the case never tested the `take_beat` / `hb.backup_freshness` chains. Per the prompt ("fix the CASES until they do, never loosen the resolver") the CASE was fixed: the planted module keeps `planted()` calling all three wrappers exactly as drafted AND adds `planted_take_beat`, `planted_backup_freshness`, `planted_latest_snapshot_age` (one wrapper each); all four must be readers. (Case diff = the `else:` branch as committed.)

**Run B, corrected case** (exit 1):

    tests/cobalt/test_jobs_restarts.py .FF                                   [100%]
    E         Extra items in the left set:
    E         'cobalt.radar._planted_beat.planted_backup_freshness'
    E         'cobalt.radar._planted_beat.planted_take_beat'
    FAILED tests/cobalt/test_jobs_restarts.py::test_backup_yaml_reader_proof_goes_red_through_any_wrapper[aliased_loader_import] - AssertionError: ['cobalt.backup.cli._restore', 'cobalt.backup.cli._run', 'c...
    FAILED tests/cobalt/test_jobs_restarts.py::test_backup_yaml_reader_proof_goes_red_through_any_wrapper[resident_uses_wrapper] - AssertionError: ['cobalt.backup.cli._restore', 'cobalt.backup.cli._run', 'c...
    ================== 2 failed, 1 passed, 15 deselected in 2.26s ==================

= `new_call_in_listed_file` PASSES, `aliased_loader_import` and `resident_uses_wrapper` FAIL (the alias; `hb.backup_freshness` and, through `probe_mod.backup_freshness` at `runner.py:144`, `take_beat`).

**Run C** — full resolver (exit 0):

    tests/cobalt/test_jobs_restarts.py ...                                   [100%]
    ======================= 3 passed, 15 deselected in 2.30s =======================

Only the final helper is committed (`grep -n "RUN B"` → no output).

**The real tree — what the walk PRINTED** (`uv run pytest -q -s … -k test_backup_yaml_is_read_by_one_shots_only`, temporary print, exit 0):

    READERS ['cobalt.backup.cli._restore', 'cobalt.backup.cli._run', 'cobalt.backup.cli._status', 'cobalt.backup.config.load_backup_config', 'cobalt.backup.restic.latest_snapshot_age', 'cobalt.backup.restic.restore', 'cobalt.backup.restic.snapshot', 'cobalt.heartbeat.cli.cmd_beat', 'cobalt.heartbeat.cli.cmd_show', 'cobalt.heartbeat.probes.backup_freshness', 'cobalt.heartbeat.runner.run_beat', 'cobalt.heartbeat.runner.take_beat']
    ENTRYPOINTS ['cobalt.backup.cli._restore', 'cobalt.backup.cli._run', 'cobalt.backup.cli._status', 'cobalt.heartbeat.cli.cmd_beat', 'cobalt.heartbeat.cli.cmd_show']
    REFERRERS ['cobalt.backup.cli.add_parser', 'cobalt.heartbeat.cli.add_parser']
    UNRESOLVED []

Line by line against the drafter's claim: readers 12 = 12, identical names; entrypoints 5 = 5, identical; referrers 2 = 2, identical; unresolved empty. **No difference.** Pinned as `EXPECTED_BACKUP_YAML_READERS`.

**Probe (temporary, reverted):** `_package_name` changed to `return m` (the way `import_graph` names a package `__init__`) → same readers, but `UNRESOLVED ['cobalt.backup.<module>']` — `backup/__init__.py:3` `from .config import … load_backup_config` resolves to `cobalt.config.load_backup_config` (`src/cobalt/config.py`: "No such file or directory"). The L1 guard catches it; ESCALATE (a).

**Tests:**
- `uv run pytest -q tests/cobalt/test_jobs_restarts.py` → `18 passed in 8.27s`
- `uv run pytest -q tests/cobalt/test_jobs_restarts.py tests/cobalt/test_jobs_reads.py` → `41 passed in 9.64s`
- `uv run pytest -q -v tests/cobalt/test_jobs_restarts.py -k "smoke_suite_file or declared_one_shot_only or generated_rules_yaml or unknown_dotfile"` → `4 passed, 14 deselected in 1.35s` (`test_a_smoke_suite_file_derives_no_restart_and_no_job_runs_the_smoke`, `test_a_declared_one_shot_only_config_derives_no_restart`, `test_generated_rules_yaml_derives_no_restart`, `test_an_unknown_dotfile_still_escalates` — unchanged, passing)
- `git diff 3ad064f -- tests/cobalt/test_jobs_restarts.py` read WHOLE: `-` lines only in the `:162-163` comment (2 lines) and the `:177-195` file-name half (19 lines); every other hunk is `+` only (imports, helper, planted test). Half (a) `:168-175` and registry asserts `:197-203` unchanged.

Committed: `5562863 test(jobs): backup.yaml reader proof walks the call graph — any wrapper, alias or new call turns it red (ops fix r3 Z3)`; `git show --stat HEAD` → `tests/cobalt/test_jobs_restarts.py | 321 ++++++++++++++++++++++++++++++++++---` / `1 file changed, 300 insertions(+), 21 deletions(-)` — only the named path.

## Z4

`docs/40 - DevDocs/cobalt/jobs/restarts.md`: ONE sentence appended to the `:7` paragraph (the `backup.yaml` reader claim is a function-level call-graph walk reusing `_module_for`, `_resolve_from`, `reachable`; 2026-09-22, round-3 fix; module reach cannot say it because radar imports `cobalt.cli`). `git diff --stat` → `docs/40 - DevDocs/cobalt/jobs/restarts.md | 2 +-` / `1 file changed, 1 insertion(+), 1 deletion(-)`. Committed `af77d6b docs(devdocs): jobs/restarts.md — backup.yaml's reader proof is a call-graph walk (ops fix r3 Z4)`; `git show --stat HEAD` → the DevDoc only.

## CLOSE

`Mon Sep 21 20:27:02 EDT 2026`

**Suite** — `uv run pytest -q tests/cobalt tests/taxonomy` (background, exit 0), VERBATIM (ANSI stripped):

    2213 passed, 351 skipped, 1 xfailed, 15 warnings in 68.74s (0:01:08)

Compared with BASELINE: failed 0, passed 2213 = 2210 + 3 (Z3's three planted cases; the rewritten real-tree test is the same one test), skipped 351 unchanged, 1 xfailed unchanged.

**`uv run cobalt jobs restarts main..HEAD`** — exit 0 (no `RestartError`), table VERBATIM:

    path	change	rule	restart
    .gitignore	M	META	-
    configs/cobalt/backup.yaml	M	no resident reads (one-shot: com.cobalt.backup,com.cobalt.heartbeat)	-
    configs/cobalt/jobs.yaml	M	registry; register, no restart	-
    docs/00 - Project/BACKLOG.md	M	DOCS	-
    docs/30 - Design/DRC-SITTING-PACKET-2026-09-21.md	D	DOCS	-
    docs/30 - Design/S3-EXITS-PROPOSAL-2026-09-21.md	D	DOCS	-
    docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/cobalt/aset/radar_panel.md	M	DOCS	-
    docs/40 - DevDocs/cobalt/backup/config.md	M	DOCS	-
    docs/40 - DevDocs/cobalt/db_migrations/cli.md	M	DOCS	-
    docs/40 - DevDocs/cobalt/jobs/restarts.md	M	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/58-s2-smoke-first-look.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/59-propose-stale-score.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/60-draft-stale-score-tribunal.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/61-stale-score-tribunal.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/62-stale-score-tribunal-fable-seat.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/63-stale-score-tribunal-derive.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/64-draft-setups-one-build.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/65-setups-one-build.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/66-setups-one-check.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/67-draft-tuesday-checks.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/68-stale-marker-check.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/69-ops-6a-check.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/70-propose-s3-exits.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/71-draft-drc-sitting-packet.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/72-draft-s3-exits-tribunal.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/73-s3-exits-tribunal.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/74-s3-exits-tribunal-fable-seat.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/75-s3-exits-tribunal-derive.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/76-draft-ops-classifier-fix.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/77-ops-classifier-fix.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/78-ops-6a-check-r2.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/79-draft-ops-fix-r3.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/80-ops-fix-r3.md	D	DOCS	-
    docs/40 - DevDocs/prompts/2026-09-21/81-ops-6a-check-r3.md	D	DOCS	-
    docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md	M	DOCS	-
    docs/40 - DevDocs/reports/cto-2026-09-21.md	M	DOCS	-
    docs/40 - DevDocs/reports/degraded-line-build-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/degraded-line-rebase-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/deploy-2026-09-21b.md	D	DOCS	-
    docs/40 - DevDocs/reports/drc-sitting-packet-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/ops-2026-09-21.md	A	DOCS	-
    docs/40 - DevDocs/reports/ops-6a-check-2026-09-22.md	D	DOCS	-
    docs/40 - DevDocs/reports/ops-6a-check-r2-2026-09-22.md	D	DOCS	-
    docs/40 - DevDocs/reports/ops-classifier-fix-2026-09-22.md	A	DOCS	-
    docs/40 - DevDocs/reports/ops-classifier-fix-draft-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/ops-fix-r3-2026-09-22.md	A	DOCS	-
    docs/40 - DevDocs/reports/ops-fix-r3-draft-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/s3-exits-design-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/s3-exits-tribunal-draft-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/setups-one-build-draft-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/stale-score-design-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/stale-score-tribunal-draft-2026-09-21.md	D	DOCS	-
    docs/40 - DevDocs/reports/tuesday-checks-draft-2026-09-21.md	D	DOCS	-
    src/cobalt/aset/radar_panel.py	M	static import reach	com.cobalt.aset
    tests/cobalt/test_backup.py	M	test/documentation; no resident	-
    tests/cobalt/test_jobs_restarts.py	M	test/documentation; no resident	-
    tests/cobalt/test_radar_panel.py	M	test/documentation; no resident	-
    RESTARTS: com.cobalt.aset

`configs/cobalt/backup.yaml` still `no resident reads (one-shot: com.cobalt.backup,com.cobalt.heartbeat)` `-`; `tests/cobalt/test_jobs_restarts.py` `test/documentation; no resident`; NO `UNCLASSIFIED` row. `RESTARTS:` is NOT `none`. The one restart comes from `src/cobalt/aset/radar_panel.py`, which is main's own change: `git log --oneline main..HEAD -- src/cobalt/aset/radar_panel.py tests/cobalt/test_radar_panel.py` has no output, and `git log --oneline HEAD..main -- …` shows `e21e4c2 test(radar): degraded line carries the bars poll failure (seam, L68)` and `71a8368 feat(radar): slim red degraded line above the card ladder on /radar, mirrored by refreshPool (R10 2026-09-21)`, the two commits PREFLIGHT flagged. The two-dot range shows main's side reversed, and the `D` doc rows are the same effect. ESCALATE 7.

**`uv run cobalt jobs restarts 3ad064f..HEAD`**: exit 0, VERBATIM:

    path	change	rule	restart
    docs/40 - DevDocs/cobalt/jobs/restarts.md	M	DOCS	-
    docs/40 - DevDocs/reports/ops-fix-r3-2026-09-22.md	A	DOCS	-
    tests/cobalt/test_jobs_restarts.py	M	test/documentation; no resident	-
    RESTARTS: none

(The report shows as `A` because the range ends at HEAD and includes untracked files. It was written before the report commit.)

**Untouched:** `git diff 3ad064f -- src` no output · `git diff 3ad064f -- configs` no output · `git diff 3ad064f -- ops` no output · `git diff 3ad064f -- tests/cobalt/test_jobs_reads.py` no output. The classifier `restarts.py` did not change, and the `jobs.yaml` row from `d669dfa` stands.

**`git diff --stat 3ad064f HEAD`** (before the report commit): `docs/40 - DevDocs/cobalt/jobs/restarts.md | 2 +-` · `tests/cobalt/test_jobs_restarts.py | 321 ++++++++++++++++++++++++++++--` · `2 files changed, 301 insertions(+), 22 deletions(-)`. Only the named paths appear; the report joins them in the report commit.

**L3, no second traversal:** `grep -c "while \|for .* in stack\|\.pop()" tests/cobalt/test_jobs_restarts.py` gives `3`. `grep -n` shows:

    248:        while dotted not in funcs and dotted not in modules and dotted not in seen:
    264:        while isinstance(node, ast.Attribute):
    271:        while enclosing.startswith(m + "."):  # nested defs, innermost first

None of the three is a graph traversal. `:248` follows ONE re-export chain to its fixed point. It never branches, and the prompt calls it the "fixed point". `:264` unwinds one dotted name. `:271` climbs the enclosing scopes of one name. The reachability closure is `restarts.reachable([_LOADER], callers, set())[0]`, and the resident tie is `restarts.reachable(job.imports, graph, unknown)`.

**`git log --oneline 3ad064f..HEAD`** (before the report commit): `af77d6b` (Z4) · `5562863` (Z3). No wip commits.

`git status --porcelain` runs after the report commit, once this file is frozen. Just before the commit, the long-form `git status` showed only this report as untracked, and nothing modified.

## ESCALATE

1. **(i) Walk against the drafter's list: no difference.** Readers are the same 12 by name, entrypoints the same 5, referrers the same 2, and unresolved is empty (Z3).
2. **NEW FINDING, classifier (`src/cobalt/jobs/restarts.py`, NOT touched):** `import_graph` (`:122`, `:131`) passes a package `__init__.py` to `_resolve_from` under the package's own name (`_module_for` `:97-98` drops `__init__`). `_resolve_from` (`:105-108`) climbs from the parent, so a relative import inside any `__init__.py` resolves one package too high. Example: `backup/__init__.py:3` `from .config import …` gives `cobalt.config`, and `src/cobalt/config.py` does not exist, so the edge `cobalt.backup → cobalt.backup.config` is silently missing. PROVEN by a reverted probe in this run (Z3): the walk run with `import_graph`'s naming reports `UNRESOLVED ['cobalt.backup.<module>']`. Possible effect: a resident whose only route to a module runs through a package `__init__`'s relative import would under-derive its restart. No such case was measured; which residents are affected is UNPROVEN (L70). The test works around this by passing `"<pkg>.__init__"` (`_package_name`), which reuses `_resolve_from` and adds no second resolver. The fix belongs in `restarts.py`, is a `src/` change that derives `com.cobalt.radar`, and needs its own build and check. It is not round 3's work.
3. **Planted case widened, as the prompt directs ("fix the CASES").** Drafted as written, `resident_uses_wrapper` passed run B, because `latest_snapshot_age()` is a plain from-import. The case keeps the drafted `planted()` and adds one function per wrapper, and all four must be readers. Run B's output before and after the change is under Z3.
4. **(ii) Sol's items that are NOT the three ruled cases. Listed, not built:**
   - A resident PLIST invoking an existing operator command (`cobalt backup status|restore`, `cobalt heartbeat show`). This needs a registry/plist check, which a call graph cannot give. It would be settled by a test that no `ops/com.cobalt.*.plist` of a RESIDENT names `backup` or `heartbeat show`.
   - `src/cobalt/backup/secrets.py:54`: a `backup.yaml` string inside an error message (round-2 row 10). It is not a read. The dropped `"backup.yaml"` literal check had pinned the one path constant in `config.py`.
   - A SECOND call inside a function that is already a reader. It adds no reader, so by design it is not a red case.
   - Dynamic calls the static walk cannot see: `getattr(mod, "load_backup_config")`, a function stored in a dict and called later, and `args.func(args)` dispatch in `cobalt/cli.py`. A string-named reference would be settled by a grep guard for the literal `"load_backup_config"` outside `config.py`'s `__all__`. Dict-stored callables are covered in part: a reference to a reader shows up as a REFERENCE edge, so `referrers` goes red.
5. **(iii)** Round-2 builder ESCALATE 5: the hard-coded `configs/cobalt/watchlists.yaml` `if` (`restarts.py:243-244`). Carried, untouched.
6. **(iv)** Round-1 ESCALATE 2 (the `cli.md` seam with `bars/chunk-1a-0920`), 3 (the circular restore → 6b) and 4 (restic `forget` grouping). Carried by id. Not round 3's.
7. **(v) The branch is behind main.** `main..HEAD` derives `RESTARTS: com.cobalt.aset` only because of main's own `71a8368` and `e21e4c2` (`src/cobalt/aset/radar_panel.py`). This branch has no commit on that path, and `3ad064f..HEAD` gives `RESTARTS: none`. The deploy's rebase-then-ff is the desk's job. After the rebase, `main..HEAD` should lose that row; that is not proven here.
8. **(vi) This is round 3 of 3 (L67).** Whatever `81` leaves open goes to Dejan. There is no round 4.

(vii) No `ASK DESK`. (viii) No `MEMORY:` / `RULING:` lines.

## CONTINUE

None. The run is complete, and the report commit is the last step.

OPS FIX R3 BUILT af77d6b | on 3ad064f | offline 2213/0 (baseline 2210/0) | reader proof: code-derived through wrappers, red case proven | jobs restarts main..HEAD: exit 0, RESTARTS: com.cobalt.aset | src diff: empty | ESCALATE: 8
