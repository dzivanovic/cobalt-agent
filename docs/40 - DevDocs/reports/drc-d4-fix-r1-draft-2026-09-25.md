# DRC D4 FIX R1 — CLASSIFICATION (L75) + THE FIX BUILD AND ITS CHECK

Drafter `drc-d4-fix-r1-draft-0925` (Opus 5.5), prompt `prompts/2026-09-25/20-draft-drc-d4-fix-r1.md`. Started Fri Sep 25 06:47:35 EDT 2026; last `date` read Fri Sep 25 07:01:16 EDT 2026, before this report was written. Authorization: `grep -n "^| R31 " …/cto-2026-09-25.md` → `39:| R31 | 06:4x ET | … DESK LAUNCH ROW for prompts/2026-09-25/20-draft-drc-d4-fix-r1.md …` (names `20`). Base read: `git -C /Users/cobalt/cobalt log -1 --format=%h drc/d1-trading-log` → `8b6518a8`; `--oneline -3` → `8b6518a8 docs(drc-d4): DRC D4 build report — 5d4f8201` · `facdfc93 wip(drc-d4): DRC D4 build report — FAILED at E7 (E3 fixture), fix 5d4f8201 unproven` · `5d4f8201 wip(drc-d4): E3 fixture writes …` — as the card expects (two docs-only commits above the code tip). Lock at 06:52: `ls /Users/cobalt/cobalt-wt/*/.env` → `no matches found`.

## §0 Headline
- 39 rows classified (`06`'s 12 HOLDs + its 12 ESCALATEs, ESC 5 split into its four claims + the build's 11, ESC 9 split in two). Counted 28 (8 restatement rows take the class of what they restate; 3 standing lines take none): **FIX 10 · NOT REAL 10 · UNPROVEN 3 · OUT OF SCOPE 5 · OWNER ITEM 0**.
- The 10 FIX rows build as **8 rows** in `21`: F-1 the card apply through `apply_settings` (L3) · F-2 X12 `/size` reads `cobalt_dev` · F-3 a failure inside the one `put` · F-4 the one-reader walk · F-5 the typed value · F-6 the read-back sentence · F-7 report-only corrections · F-8 no value and NO digest in the log line (HOLD 10 + ESC 4, L32). F-1 and F-8 are red first; F-2…F-6 are PINS with negative controls (the code is right; the assertion could not fail). RUN-1…RUN-3 = ESC 5's unproven claims.
- HOLD 4 ("history row") = NOT REAL: `05`'s D4-4 named a table the store never had; `21` carries AMENDED D4-4 TRACE (row `source` + `updated_at` + a keys-only log line; no `0019`). HOLDs 11–12 = NOT REAL (R17 / R16).
- Seam: the route contract and the store's shape do NOT move; `## SEAM FOR D2` gains ONE amended bullet (the one apply's callers + its log line); `## FOR D3` stands. Prompts: `21-drc-d4-fix-r1-build.md`, `22-drc-d4-fix-r1-check.md`; launch lines = `05`'s / `06`'s byte for byte but path + remote-control name (token diff run); new rule strings 0. ESCALATE: 6.

## L74
- A block arrived inside a tool result (a system reminder attached to this session's first tool result, the prompt-file read) asking commits to carry a `Claude-Session:` line and naming a file-send tool. Recorded once; not followed. This seat commits nothing and sends no file.

## Classification
`# · claim (short) · source line · class · traces to · FIX shape or reason`. Source lines: `C` = `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-d4-check-2026-09-25.md` (the file-check column `:122`–`:133`, `## FOR THE CLASSIFIER` `:161`–`:172`, `## ESCALATE` `:175`–`:186`); `B` = the D4 build report `git show 8b6518a8:"docs/40 - DevDocs/reports/drc-d4-build-2026-09-25.md"` (`## ESCALATE` `:211`–`:221`). Code lines are the tip `5d4f8201` (read by `git -C /Users/cobalt/cobalt show 5d4f8201:<path>`). v2 = `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` at its REAL lines (§9 D4 `:178`, order `:181`, E10 `:200`, X12 `:204`).

| # | claim (short) | source line | class | traces to | FIX shape or reason |
|---|---|---|---|---|---|
| H1 | `cli.py:10`–`:11`, `:79`–`:81` claim every apply goes through `apply_settings`; `settings/card.py:323` `cmd_load_card` calls `store.put` directly (no read-back through the apply, no log line) | C `:122`, `:161` | **FIX → F-1** | L3 ("the second copy is killed on sight"); `05` D4-4 `:22` ("writes through the SAME function `cobalt settings load --apply` uses for its `put` … ONE write path, L3; the CLI and the page are callers") — `--card --apply` IS a `cobalt settings load --apply` form (`cli.py:242`–`:251` dispatches it); the build's own ESCALATE 8 (B `:218`) named it an L3 follow-up | `cmd_load_card` `:322`–`:323` → ONE call `settings_cli.apply_settings(new_rows, source=…, actor="settings.load.card", store=store, delete=deletes)` (module reference at call time; `cli.py:249` imports `card` lazily — no cycle); its own typed round trip `:324`–`:331` kept. `cli.py:10`–`:11` names every caller and the one other writer (`radar/notes.py:747`, outside D4). Red: a spy test + a `.put(` walk of `settings/*.py`. Named SETUP change `test_card_settings.py:172`, `:188` (the gate patch retargeted to `settings_cli`; assertions byte for byte — `:177`, `:179`, `:181`, `:191` hold through `apply_settings`, `cli.py:96`–`:97`). `card.py` + `test_card_settings.py` listed under D4-4 (the R16 / R17 shape) |
| H2 | X12 `/size` test patches `load_sheet_modes_config` to the offline config (`test_drc_settings_db.py:111`) — `/size` never reads `cobalt_dev` | C `:123`, `:162` | **FIX → F-2** (PIN) | v2 X12 `:204` ("cobalt_dev without the DRC key rows → `/size` still serves and sizes"); `05` D4-6 `:24`; L35 | `:111` patch removed; the coherent constructed set (sheets + day-mode rows, `change_page`'s `:185`–`:196`, factored into ONE helper) written with a constructed `full.B` ≠ the offline config's; the unpatched loader asserted to return it (negative control: an offline loader fails there); then the POST and `:125`–`:127` byte for byte. `_daymode_state` stays patched (the day's attestation, not X12's input) |
| H3 | the "one transaction" test fails validation (`drc.py:258`) before any `put` (`test_drc_settings_db.py:251`/`:255`) | C `:124`, `:163` | **FIX → F-3** (PIN) | `05` D4-4 `:22` ("a partial write … is impossible — one transaction"); `store.py:85`–`:116` | NEW with-DB test: `TraderSettingsStore.put` wrapped to pass `before_commit` raising AFTER both upserts (`store.py:109`–`:110`) → page FAILED, `store.rows() == before`; negative control: the same apply without the raise lands both keys. The `:251` test stays (it proves "nothing written on a validation failure"), docstring points to the new one |
| H4 | "the history row exists" checked only as `source` / `updated_at` on an overwritten row (`test_drc_settings_db.py:226`–`:228`); no history table | C `:125`, `:164` | **NOT REAL** (row TEXT amended) | v2 §9 D4 `:178` (DB: "none if `trader_settings.value` takes a new payload without a migration — E10 proves"); E10 `:200` PASS → "D4 stays migration-free"; R96 / R102 name no history (read: `grep -n "^| R96 "` / `"^| R102 "` on `cto-2026-09-22.md`); `store.py:94`–`:104` upserts, one row per `(user_id, key)`; B `:218`; desk R17 / R20 (not built, `0019` if ever wanted) | `05`'s "the settings history row the store keeps (versioned)" named a table the store never had — a drafting error, not a build defect. `21` carries AMENDED D4-4 TRACE: each row's `source` (`aset.change_line@sha256:<hash>`) + `updated_at` + one keys-only log line (F-8's shape). A versioned history is a new design (its own migration), not D4's; the `:226`–`:228` assertions assert the amended contract and stay |
| H5 | the one-reader walk greps only `daily_stop_full` / `daily_stop_half` (`test_drc_settings.py:249`) | C `:126`, `:165` | **FIX → F-4** (PIN) | `05` D4-2 `:20` ("the ONLY function on any DRC or daily-note path that returns those numbers"); L3; L35 | Helper `_second_readers(root)`: key tokens (`daily_stop_full`, `daily_stop_half`, `DAILY_STOP_KEYS`, `account.daily_stop`) only under `settings/`; reader calls (`daily_risk_values(`, `load_drc_settings(`) only under `settings/` + `prefill/daily.py` + `aset/web.py`; `sheet_modes` in no `drc/` file. `:258`–`:259` kept. Negative control on a constructed `tmp_path` tree. The hub's walk (C `:147` (iii)) placed every hit inside that set → expected green |
| H6 | E2 table counts D4-4 as 11 tests; there are 12 (sums 48 ≠ 49) | C `:127`, `:166`; B `:85` | **FIX → F-7** (report-only) | L35 (every count is tool output) | A correction line in `21`'s `## CORRECTIONS TO THE D4 BUILD REPORT`, the builder re-counting from `git show 5d4f8201:tests/cobalt/test_drc_settings.py`. No code; the committed D4 report is not edited |
| H7 | B ESC 7 ("X12 reader NOT yet run with-DB") and ESC 9 (iii) ("RESTARTS not run") stale | C `:128`, `:167`; B `:217`, `:219` | **FIX → F-7** (report-only) | L35; L48 | A correction line quoting the superseding E7 (B `:150`–`:170`, X12 reader PASSED `:166`) and `## RESTARTS` (B `:171`–`:187`) |
| H8 | `test_drc_settings.py:156` accepts a good value by not raising; no assert on the typed result | C `:129`, `:168` | **FIX → F-5** (PIN) | `05` D4-1 `:19` ("each a Pydantic model"); L10; L35 | Each parametrize row gains the TYPED value the model declares (read from `models.py`, never fitted); assert the field and the round trip `from_rows(model.row()) == model`. Inputs unchanged |
| H9 | `:469` asserts only the success banner absent, not the read-back sentence | C `:130`, `:169` | **FIX → F-6** (PIN) | `05` D4-4 `:22` ("`saved` only after the read-back equals the payload"); `cli.py:101`–`:105` | Two assertions added: `"differs from what was applied"` and the key name on the page (`_failed` → `html.escape`, `web.py:745`–`:746`) |
| H10 | `:491` asserts the stop figure absent from the log, not the grade figure | C `:131`, `:170` | **FIX → F-8** (with ESC 4) | `05` D4-4 `:22` ("never the values in the log (values are his user data, L32: log keys only)") | Helper `_leaks(message, figures)` (ISO timestamps masked; each figure, and any 64-hex run); both constructed figures (the grade figure ≥ 4 digits, ≠ the stop's); negative control. THE ONE NAMED REVERSAL `:489` `sha in` → `sha not in` (ESC 4) |
| H11 | WIDENED `tests/cobalt/test_radar_panel_cards.py:667` (`POST_ALLOWLIST`) | C `:132`, `:171` | **NOT REAL** | desk R17 (`cto-2026-09-25.md:25`, committed `c48ed315`): D4-4's own guard, listed under the row, never a widening | Stands as the desk's reading |
| H12 | WIDENED `tests/cobalt/test_prefill_daily.py:191` (fixture literal → constructed value) | C `:133`, `:172` | **NOT REAL** | desk R16 (`cto-2026-09-25.md:24`, committed `b2ad3b7a`): D4-3's 0-hit rule; the row's own fixture (L32 / L45 / L69) | Stands as the desk's reading |
| E1 | the two checker lines (Opus EXCEPT three items; Grok EXCEPT two widened files) | C `:175` | as H1–H3, H11–H12 (restatement) | — | — |
| E2 | every FOR THE CLASSIFIER item restated | C `:176` | as H1–H12 (restatement) | — | — |
| E3 | prompt line drift: `05` / `06` cite v2 `:92` / `:95` / `:114` / `:118`; the file reads `:178` / `:181` / `:200` / `:204` | C `:177` | **NOT REAL** (a prompt citation, not a build defect) | L35 (every claim `file:line`) | `21` and `22` cite the real lines; `22` stages v2 at `:176`–`:178`, `:181`, `:200`, `:204` under REAL-line headers |
| E4 | `cli.py:106`–`:110` logs the payload's sha256; for a daily-stop-only payload the value space is small | C `:178`; walked C `:136` | **FIX → F-8** | L32 + `05` D4-4 `:22` ("never the values in the log … log keys only"): a sha256 over `{"account.daily_stop_full":"<n>"}` with `<n>` a few-digit number is inverted by enumerating `<n>` — the digest IS the value to anyone holding the log. The `source` label passed to the log carries the same digest (`aset.change_line@sha256:<hash>`; the CLI's `optional:` / `card:` labels carry a FILE digest of the same small space) | ONE reading, mechanism = NO DIGEST IN THE LOG: `digest = payload_sha256(rows)` removed from `apply_settings` (`:95`, read nowhere else); the log line names keys, deleted keys, the source KIND (`source.split("@sha256:", 1)[0]`) and the time. Not a salt (a salt needs a secret the log reader cannot hold, and the reviewed hash must still match the page); not a length (a truncated digest is still enumerable). The reviewed hash stays on HIS screen (review page, saved banner) and in each row's `source` beside the value it hashes (no new exposure). `web.py:1268`–`:1270` docstring and `cli.py:87`–`:90` follow. Never an owner item: a mechanism the houses settle (L67 as amended 09-24) |
| E5a | "`store.put` is one transaction — NOT CHECKABLE" | C `:179`; walked C `:137` | **NOT REAL** | L35: the hub read it (`store.py:60`–`:117`: `autocommit = False`, one `try`, `commit` `:111`, `rollback` `:112`–`:113`) | Readable, walked; F-3 now proves it with-DB through the apply |
| E5b | whether the with-DB `TraderSettingsStore()` joins the `dev_db_tx` rollback (`_connect` not walked) | C `:179`; C `:138` | **UNPROVEN → RUN-1** | L70 | Drafter's read: `store.py:34`–`:35` → `db.connect`, replaced by `dev_db_tx` with a savepoint proxy on ONE connection rolled back at teardown (`tests/cobalt/conftest.py:134`–`:175`). A runtime property → RUN-1: two ordered with-DB tests (a write lands inside its test; the next test sees no row with that `source`) |
| E5c | whether `_failed` logs its text (page errors carry typed values) | C `:179` | **UNPROVEN → RUN-2** | L70; L32 | `_failed` itself calls no logger (`web.py:745`–`:746`, read); RUN-2 proves the whole request path: a refused field with a distinctive constructed figure through both routes → no captured log message carries it |
| E5d | a committed file outside `configs/`, `src/`, `tests/` with the old literal (not run) | C `:179`; C `:140` | **UNPROVEN → RUN-3** | L70; R95 ("the committed daily template stops carrying the number"); L32 | The old line read once from `git show 00262e45:configs/cobalt/templates/daily.md.j2`; the GREP TOOL (the literal may carry a `$` — R16) over the tracked tree; hit COUNT and PATHS only. A hit is a result for the desk's redaction (L45 companion ruling), never fixed in the build |
| E6 | builder's ESC 6 / 8: `account.sheet_mode_default` left out; no history table; `cmd_load_card` via `store.put` | C `:180` | as B6, H4, H1 (restatement) | — | — |
| E7 | `daily.md.j2` UNCLASSIFIED → the six-label `RESTARTS:`; a `no_resident_reads` entry in `jobs.yaml` classifies it | C `:181` | **OUT OF SCOPE** | desk R20 (`cto-2026-09-25.md:28`): the DRC deploy-prompt drafter's RESTARTS item; L42 (derived, never dropped) | `jobs.yaml` is not D4's file; the fix range does not touch the template (its RESTARTS carries no UNCLASSIFIED) |
| E8 | his daily-stop VALUE must be loaded by him before the first deploy | C `:182` | **OUT OF SCOPE** | desk R30 (`cto-2026-09-25.md:38`): the DRC deploy drafter's `## FOR DEJAN`; L28 (amended 09-15: his trader-run `settings load --apply`, no HITL token) | Not a fix item and not an owner item here |
| E9 | the `Bash(git push*:*)` settings-syntax notice | C `:183` | **OUT OF SCOPE** | desk R30 (an ops note) | No action in this round |
| E10 | Astra METER until Sep 26th, 2026 6:47 AM | C `:184` | no class (standing) | L62 as amended R19; L67 | The desk seats Astra for the D4 NEW-BUILD read from its return; a fix round is never Astra-seated (`22` ESCALATE line) |
| E11 | standing line: round 1 / the floor | C `:185` | no class (standing) | L39, L67 | Carried in `22`'s standing line |
| E12 | standing line: the deploy's L68 gate | C `:186` | no class (standing) | L68 | Carried in `21` / `22` |
| B1 | R22 scope read as the drc-d1 pair | B `:211` | **OUT OF SCOPE** | `cto-2026-09-24.md` R22; R61 / R65 (1) / 09-25 R9 | The desk's reading; `21` records it the same way |
| B2 | `LINE MOVED: def assert_writable` → `session/guard.py:123` | B `:212` | **NOT REAL** | L35 | A fact; `21` greps no `writer.py` symbol |
| B3 | the 03:46 dialog (a `$` in a double-quoted grep) | B `:213` | **NOT REAL** | desk R16 (resumed by message) | Its lesson is carried: `21` / `22`'s pattern rule adds "no `$` in a double-quoted argument; the GREP TOOL for a literal read from a file" |
| B4 | `ASK DESK` on R16 | B `:214` | **NOT REAL** | answered: R16 committed `b2ad3b7a` | — |
| B5 | E6 red on `POST_ALLOWLIST` / `ASK DESK` | B `:215` | as H11 (restatement) | R17 | — |
| B6 | key names added; `account.sheet_mode_default`, `grades.*`, `sleep_trigger.*` left out | B `:216` | **NOT REAL** | L3 (a second source for the day's sheet beside `daymode.*`); desk-confirmed R17 / R20; `test_drc_settings.py:133` pins its absence | Stands |
| B7 | E10 / X12 results; "X12 reader NOT yet run with-DB" | B `:217` | as H7 (restatement) | — | — |
| B8 | no history table; `cmd_load_card` via `store.put` | B `:218` | as H4 + H1 (restatement) | — | — |
| B9a | stale cite: `jobs.yaml:313` names `prefill/daily.py:465`, now `:482` | B `:219` | **OUT OF SCOPE** | `jobs.yaml` is not D4's file (`05` E4 list) | The DRC deploy drafter's `jobs.yaml` item (with E7's entry) |
| B9b | (iv) his daily-stop VALUE; (iii) "RESTARTS not run"; the standing line | B `:219` | as E8, H7 (restatement); standing line no class | — | — |
| B10 | E7 red on the E3 fixture / `ASK DESK` | B `:220` | **NOT REAL** | answered: R19 (resumed; E7 GREEN 2973/0 on `5d4f8201`) | — |
| B11 | RESTARTS: `daily.md.j2` UNCLASSIFIED | B `:221` | as E7 (restatement) | — | — |

Counted: FIX 10 (H1, H2, H3, H5, H6, H7, H8, H9, H10, E4) · NOT REAL 10 (H4, H11, H12, E3, E5a, B2, B3, B4, B6, B10) · UNPROVEN 3 (E5b, E5c, E5d) · OUT OF SCOPE 5 (E7, E8, E9, B1, B9a) · OWNER ITEM 0. Restatements 8 (E1, E2, E6, B5, B7, B8, B9b, B11); standing 3 (E10, E11, E12), no class. 28 + 8 + 3 = 39 table rows.

Build rows in `21`: F-1 = H1 · F-2 = H2 · F-3 = H3 · F-4 = H5 · F-5 = H8 · F-6 = H9 · F-7 = H6 + H7 · F-8 = H10 + E4 → `FIX: 8`, `RUNS: 3`.

## SEAM
- **The route contract: NO move.** `POST /settings/daily`, `POST /settings/daily/apply`, the block's place directly after `/attest` and before `/card/{card_id}/move`, and "D2 reads no settings key; D2 edits nothing in D4's block" all stand (`05` THE SEAM; `web.py:1184`–`:1295` at `5d4f8201`). F-8 edits one docstring line inside the block (`:1268`–`:1270`); no name, route or helper changes.
- **The settings store's shape: NO move.** `store.py` is untouched (`21` F4 proves `git diff 5d4f8201 -- src/cobalt/settings/store.py` EMPTY); no migration; one row per key; AMENDED D4-4 TRACE states it.
- **`## SEAM FOR D2`: ONE bullet amended** (L72 — the D2 drafter cites the FIX report's section, which supersedes `05`'s). Amended text, as `21` has the builder write it with its own `file:line`s: "The one apply: `apply_settings(rows, *, source, actor="settings.load", store=None, delete=())` — `src/cobalt/settings/cli.py:<line>`; callers: `cmd_load_optional` and `cmd_load` (same file), `cmd_load_card` (`src/cobalt/settings/card.py:<line>`, D4 fix r1 F-1) and `settings_daily_apply` (`web.py:<line>`); its log line names keys, deleted keys, source kind and time — no value, no digest (F-8)." D2 calls none of these; the bullet is a fact for the seam, not a D2 dependency.
- **`## FOR D3`: stands** (`daily_risk_values` / `load_drc_settings` untouched; `21` adds one line saying so). K3 cites neither.
- `07` (D2) needs no text change: its seam paragraph is `05`'s, unchanged. `22`'s NEXT STEP tells the desk `07` cites the fix report's `## SEAM FOR D2`.

## OWNER ITEMS
NONE. (ESC 4 — is a guessable hash of his value a leak of his value — is a mechanism under his L32 already in force, settled by the houses: F-8. HOLD 4 — a history table — is a design question the houses settle: NOT REAL with the row text amended; building one would be a new design and a `0019`, not asked by R96 / R102.)

## FOR DEJAN
NONE — new rule strings: 0. `21`'s line = `05`'s byte for byte but the path and `--remote-control drc-d4-fix-r1-build-0925` (token diff: 86 tokens each, 2 differ); `22`'s line = `06`'s byte for byte but the path and `--remote-control drc-d4-fix-r1-check-0925` (75 tokens each, 2 differ). The drc-d1 `.env` pair is his R22; the house strings are his standing R17 / R19.

## ESCALATE
1. F-1's file list: `src/cobalt/settings/card.py` is listed under D4-4 as the card directs (the R16 / R17 shape); `tests/cobalt/test_card_settings.py` is ALSO listed under D4-4 — its two `assert_writable` patches (`:172`, `:188`) must follow the gate into `apply_settings` or the two tests read the real clock. Named SETUP change, assertions byte for byte. The desk states both paths in `21`'s launch row (a reading of the same shape, not a new ruling).
2. A second writer found by the drafter's sweep, OUTSIDE D4 and NOT built: `src/cobalt/radar/notes.py:747` `mirror_sources` calls `TraderSettingsStore.put` directly (the radar's mirror of his radar notes, `radar.note.*` keys, S2-P4 `589a1a31`; its own market-reset refusal `:731`–`:732`). Reading used in `21`: a machine mirror of vault notes is a different job from a settings load, not a second copy of it (L3 governs duplicate implementations of the same job); `cli.py`'s docstring names it so the one-apply claim is true. `src/cobalt/radar` is outside D4 (L52 boundary). If the desk reads it as an L3 defect, it is a BACKLOG line for the radar lane, not this fix.
3. `21`'s red is honest by kind: F-1 and F-8 are red on the base; F-2…F-6 are ASSERTION rows whose code is right — each is a PIN expected GREEN, carrying a NEGATIVE CONTROL that proves the assertion can fail (L35). A pin that goes red is a RESULT for round 2 (not a code change in this round, L75). The stop line's `red <hash>` is the offline red commit.
4. AMENDED D4-4 TRACE replaces `05`'s "the settings history row the store keeps (versioned)". If the desk wants a versioned history of `trader_settings` (his settings changes replayable, L57), that is a new design for the tribunal lane (a `0019` migration, the store's write path, L29) — not D4, not an owner item.
5. Lock and order at drafting (06:52 ET): the `cobalt_dev` lock was FREE; `57` (RUNNING, `cto-2026-09-25.md` R29) takes it at its D7 — first ready first; `21` waits at its own `.env` gate otherwise. `07` D2 waits for `22` (same worktree, L46).
6. `06`'s line map, for the desk's launch-row fill: its file-check rows 1–12 are file lines `:122`–`:133` and its ESCALATE 1–12 are `:175`–`:186` (read by `grep -n`); `21` / `22` cite those.

DRC D4 FIX R1 DRAFTED · FIX: 10 · NOT REAL: 10 · UNPROVEN: 3 · OUT OF SCOPE: 5 · OWNER ITEM: 0 · prompts: 2 · new rule strings: 0 · ESCALATE: 6
