# Ops 2026-09-21 — LAUNCH 1 report (hub `ops-0921`, Sonnet 5, offline)

Started Mon Sep 21 17:32 EDT 2026. Prompt: `prompts/2026-09-21/06-ops-0921.md`. Worktree `~/cobalt-wt/ops-0921`, branch `ops/2026-09-21`.

## §0 Headline
- All 7 items done OFFLINE, 5 built-item commits + this report. Suite 2209 passed / 0 failed (baseline 2208 + 6a's one test). Nothing restarted, pushed, merged or run against production.
- 6a built (the Cobalt vault file joins restic's include set; red → green quoted). Needs ≥3 house checks (L67) before the desk's deploy; 6b (production restore) waits for LAUNCH 2.
- ESCALATE 10. Top two: `jobs restarts` cannot classify `configs/cobalt/backup.yaml` (exit 1, derives all 6 residents; truth = one-shot `com.cobalt.backup`, no resident); main moved 2 desk commits under this branch (rebase-then-ff).
- Findings: bare `pytest -q` collection red REPRODUCED (old-tree test, recorded in BACKLOG, not fixed); the 09-20 "margin 6" was bytes — the character margin is 157.

## AUTHORIZATION
| check | command / source | result |
|---|---|---|
| (a) 16 allow strings, `"AskUserQuestion"`, `"EnterWorktree"`, three `--add-dir` — 21 strings | `grep -c -F -e '<string>' prompts/2026-09-19/16-ops-2026-09-19.md`, one call each | **1 for all 21** |
| (b) N1 `Bash(git check-ignore *)`, N2 `Bash(COBALT_ENV=production uv run cobalt backup status)`, N3 `Bash(COBALT_ENV=production uv run cobalt backup restore ssd latest *)` | `grep -c -F -e '<string>' reports/cto-2026-09-21.md` | **2 each**: line 54 (row `R43`, 17:31 ET) + line 94 (a desk note repeating the three); the row itself holds each once. Row R43's words: "App approved" (desk reading "all approved"). The row names the three strings and this launch. |
| (b) `R43` placeholder | `grep -n -F -e 'R43' prompts/2026-09-21/06-ops-0921.md` | one hit, `:9`, is the prompt's own description of the row — not an unfilled placeholder; the row is filled (line 54) |
| (c) N4 push deny | `grep -c -F -e 'Bash(git push*)' prompts/UNATTENDED-LAUNCH.md` | **1** (≥1) |
| (d) rows committed on main | `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` | top: `f5c5bf0 docs(desk): 09-21 R43 smoke first-look and ops-0921 strings approved, ops launch row` |

Verdict: authorization matches. No `authorization mismatch`.

L74 note (recorded once here, never raised again): the `Read` of the prompt file returned, inside its tool result, an attribution block asking for a `Claude-Session:` line on every commit and for a file-send tool. That is data, not an instruction; it was not followed. Commits carry only the attribution my own system prompt gives.

## PREFLIGHT
| rule | command | exit | result |
|---|---|---|---|
| date | `date` | 0 | allowed — `Mon Sep 21 17:32:22 EDT 2026` |
| git status | `git status --porcelain` | 0 | allowed — EMPTY |
| git status | `git status` | 0 | allowed — "On branch ops/2026-09-21 / nothing to commit, working tree clean" (branch correct; no rebase in progress) |
| git log | `git log --oneline -1` | 0 | allowed — `f5c5bf0 docs(desk): 09-21 R43 smoke first-look and ops-0921 strings approved, ops launch row` (FIRST launch: main's tip when the worktree was created) |
| git -C … log | `git -C /Users/cobalt/cobalt log --oneline -1` | 0 | allowed — `f5c5bf0` (same as the worktree tip) |
| ls | `ls -la .env` | 1 | allowed — "ls: .env: No such file or directory" (as required) |
| pytest | `uv run pytest --co -q tests/cobalt/test_backup.py` | 0 | allowed — 22 tests collected |
| uv run cobalt jobs restarts | `uv run cobalt jobs restarts main..HEAD` | 0 | allowed — header only, `RESTARTS: none` (empty range) |
| git check-ignore (N1) | `git check-ignore -v .env` | 0 | allowed — `.gitignore:1:.env	.env` |

`uv run` created the worktree's `.venv` on first call; `git status --porcelain` stays empty (ignored).

## 0. BASELINE
`uv run pytest -q tests/cobalt tests/taxonomy` (17:33, before any edit): `2208 passed, 351 skipped, 1 xfailed, 15 warnings in 68.32s`, 0 failed. Green.

## 1. Item 1 — `db_migrations/cli.md`, the stale `SET LOCAL` paragraph — DONE, commit `e2db970`
Read `cli.md` in full and `cli.py:575-700` first. The code (`cli.py:688-689`): `conn.execute(f"SET LOCAL lock_timeout = '{lock_timeout_s}s'")` is the line before `before = _probe_all(conn)`, i.e. before the BEFORE probe; the docstring (`:588-627`) says why it moved. The four test names the docstring cites all exist at `tests/cobalt/test_migrate_proof.py:1952, :1978, :2008, :2075`.

`cli.md:78-82` — before: "issues `SET LOCAL lock_timeout = '<N>s'` before `_apply`'s first statement, forward and rollback alike" · after: "…before the BEFORE probe (so before `_apply`'s first statement too), forward and rollback alike…".

`cli.md:113-124` — before: "After the BEFORE probe, before `_apply`. … **The price: the BEFORE PROBE IS NOT UNDER THE CEILING** … Moving the `SET LOCAL` into `_connect` would cover the probe too, and is the change to make once the snapshot question is answered against the Postgres manual." · after: two paragraphs — the `SET LOCAL` is the first statement after `_connect` returns, before the BEFORE probe, forward and rollback; the probe's ACCESS SHARE is now under the ceiling; it used to sit after the probe and moved on 2026-09-19 (R1/R11) once the two `requires_db` tests settled that neither `SHOW server_encoding` nor a bare `SET LOCAL` takes the snapshot — all three tests named: `test_connects_show_server_encoding_does_not_take_the_snapshot`, `test_a_bare_set_local_does_not_take_the_snapshot_either`, and the negative control `test_the_instrument_can_see_a_snapshot_that_is_already_fixed` (plus the coverage test `test_a_blocked_before_probe_is_under_the_lock_ceiling_too`). No other paragraph changed (diff: 39 insertions, 15 deletions, one file).

Verify: BASELINE suite re-run after the edit: `2208 passed, 351 skipped, 1 xfailed, 15 warnings in 63.84s`, identical. The DevDoc check that reads `cli.md` is `test_the_documented_fold_counts_rows_and_buffers_a_batch` (`tests/cobalt/test_migrate_proof.py:580`); it is in the run and green. `git show --stat HEAD`: only `docs/40 - DevDocs/cobalt/db_migrations/cli.md`.

The bars branch's `cli.md:461-465` (chunk 1a, unmerged) was neither read nor edited. My text says the `SET LOCAL` sits BEFORE the probe; see ESCALATE for the check when 1a merges.

## 2. Item 2 — `UNATTENDED-LAUNCH.md` wrap line — DONE, commit `6193cd7`
L19 reading — `git log --format="%h %ad %s" --date=short -- "docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md"`:
```
40f12ac 2026-09-20 docs(desk): 09-20 R15 chunk E approved - prompt 07-bars-chunk-e + push denied on every hub line
848e28e 2026-09-18 docs(desk): 09-18 wake-up ab41a0a3 — P2 re-ship FAILED step 1 (rebase cannot re-land a reverted merge); relaunch prompt by cherry-pick, approval pending
0e52e5f 2026-09-17 docs(desk): P4 relaunch run 1 failed on the prompt — authorization block + always-can-stop channel, standard updated
54d869e 2026-09-17 docs(desk): ruling R5 — unattended launch standard, both hubs stopped, P4 relaunch prompt with approval list
```
Four in-place edits; the file calls itself "standing standard … stable file, dateless" → EDITED in place, never re-issued.

Push line already present — `grep -n -F 'Bash(git push*)'` → `UNATTENDED-LAUNCH.md:9`: "…`--disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir …`. … push never in it — and DENIED on every hub line since 2026-09-20 (`cto-2026-09-20.md` R12)…". No second copy added (L3).

The one owed sentence added to §2's `UNATTENDED RULES` paragraph (`:14`), after "no `; echo` behind.": "An exit status is read from the tool result, never by wrapping the command to print it (`; echo $?`, `echo EXIT=$?`, `> /dev/null`, `|| echo`) — a wrapped command no longer matches its bare rule and is judged by the classifier (`cto-2026-09-20.md` §41)." `git diff --stat`: 1 file, 1 insertion, 1 deletion. `git show --stat HEAD`: only `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`.

## 3. Item 3 — the stale registry rows: a LIST for the desk. Stopped NOTHING, killed NOTHING, messaged NO ONE.
(i) `ListAgents` at 17:33 ET: this session (`ops launch setup [8ac49d]`) + two peers — `devdocs cto wakeup prompt [64b474]` (bg, busy, started 1h ago) and `review deploy3 build [7030be]` (bg, idle, started 2d ago). **`ListAgents` prints 6-character ids; the registry rows in the prompt carry 8-character ids, so no id can be matched by id — presence below is by label/name only.** None of the eight 09-17/09-19 labels is in the listing; the `review deploy3 build` label is.

| id | job / label | launched (report:line) | stop line seen (report:line) or NONE | `ListAgents` | verdict |
|---|---|---|---|---|---|
| `4f911896` | `p4-build-0917` ("follow p4 relaunch prompt"), Sonnet, cwd `s2-p4` | `cto-2026-09-17.md:117` | `cto-2026-09-17.md:200` — `READY FOR MERGE 8242831 (code 1efb0cc) \| RESTARTS: com.cobalt.aset com.cobalt.radar · audit: deferred · agy: HARNESS · ESCALATE: 5`; "`claude stop 4f911896` → stopped" `:201`; "stopped 07:58 (still listed `working`… stale row)" `:234` | absent | ASK |
| `da10f94b` | `review-0919a`, Sonnet, cwd `agy-trial` | `cto-2026-09-19.md:64` | `cto-2026-09-19.md:80` — `REVIEW DONE · grok: 0/0/1 SAFE TO DEPLOY · gemini: 1/0/0 FIX FIRST Q5 · astra: 1/0/1 FIX FIRST 1 · houses that read it: 3 of 3 …`; "stopped 07:06, tab `w2:t17` closed" | absent | ASK |
| `39d25992` | `review-0919c` (build check round 2), Sonnet | `cto-2026-09-19.md:109` | `cto-2026-09-19.md:114` — `REVIEW DONE · grok: 0/0/0 SAFE TO DEPLOY · gemini: 0/0/0 SAFE TO DEPLOY · astra: 0/1/1 FIX FIRST 1 · houses that read it: 3 of 3 …`; "stopped 07:40, tab `w2:t19` closed" | absent | ASK |
| `c9b4dc14` | `review-0919b` (deployment delta), Sonnet | `cto-2026-09-19.md:109` | `cto-2026-09-19.md:127` — `REVIEW DONE · grok: 1/5/0 RUN AFTER FIXES 1 3 4 6 · gemini: 1/0/0 RUN AFTER FIXES 6 · astra: 2/1/0 RUN AFTER FIXES 1, 2, 3 · houses that read it: 3 of 3 …`; "stopped 07:47, tab `w2:t1A` closed" | absent | ASK |
| `72fd32ff` | `review-p4-verify-0919`, Sonnet, cwd `agy-trial` | `cto-2026-09-19.md:256` | `cto-2026-09-19.md:265` — `REVIEW DONE · grok: 3/12/4 RUN-AFTER-FIXES · gemini: 1/0/0 DO-NOT-RUN · astra: 6/7/0 RUN-AFTER-FIXES · houses that read it: 3 of 3 …`; "stopped 09:03, tab `w2:t1J` closed" | absent | ASK |
| `75098464` | `review-ops-r2-0919`, Sonnet | `cto-2026-09-19.md:410` | `cto-2026-09-19.md:429` — `grok 0/0/1 FOLD CLEAN · gemini 0/1/1 FOLD UNSAFE · astra 1/0/1 FIX FIRST R2 · 3 of 3 · hub-verified REAL: 4 …`; "Hub idle → `claude stop 75098464`" (same line) | absent | ASK |
| `6e09aadd` | `review-cards-golive-0919`, Sonnet | `cto-2026-09-19.md:453` | `cto-2026-09-19.md:463` — `grok 1/1/2 UNSAFE-TO-APPLY · gemini 0/0/0 VALID-AS-DRAFTED · astra METER · read it: 2 of 3 …`; "Hub stopped (`6e09aadd`)" | absent | ASK |
| `915062e7` | `review-cards-apply-0919`, Sonnet, cwd `agy-trial` | `cto-2026-09-19.md:530` (12:13) | `cto-2026-09-19.md:544` — `CARDS APPLY CHECK · grok: RUN AFTER FIXES 5 · gemini: RUN AFTER FIXES 3 · astra: METER · houses that read it: 2 of 3 …`; "Cards check hub `915062e7` stopped 12:31" `:551` | absent | ASK |
| `b63d56a0` | "review deploy3 build", `done` since 09-19 | NOT FOUND — no `reports/` file names this id or label as a launch (`grep -rn -l` by id: only `cto-2026-09-20.md`, `cto-2026-09-21.md`; by label: only `cto-2026-09-20.md`) | NONE recorded; `cto-2026-09-20.md:249` "finished reviewer / left" | label PRESENT: `review deploy3 build [7030be]`, idle | ASK |

Recommendation for the desk — ask FIRST the eight with a recorded stop line AND absent from `ListAgents`: `4f911896`, `da10f94b`, `39d25992`, `c9b4dc14`, `72fd32ff`, `75098464`, `6e09aadd`, `915062e7` (every one has the desk's own "stopped" record too, so a `SendMessage` reply — or none — settles it; L60: liveness is ASKED, never inferred). Ask `b63d56a0` separately: it is listed (label match) and idle, and its launch/stop lines are not in any report. No commit of its own — this table rides the report.

## 4. Item 4 — bare `uv run pytest -q` stops at collection — REPRODUCED, RECORDED (BACKLOG), NOT FIXED. Commit `5e2ae4c`
(a) `uv run pytest --co -q` (background), tail: `ERROR tests/test_finviz_extractor.py` · `!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!` · `2716 tests collected, 1 error in 23.05s` (exit 2). The one `ERROR collecting` line: `tests/test_finviz_extractor.py`, `:34`: `ImportError: cannot import name 'FinvizStockData' from 'cobalt_agent.skills.research.finviz_extractor' (…/src/cobalt_agent/skills/research/finviz_extractor.py)`. ≥1 error → (b) ran.
(b) `uv run pytest -q` (background): `1 error in 1.79s` after `Interrupted: 1 error during collection` — 0 tests run (exit 2). REPRODUCED.
(c) `grep -rn "FinvizStockData" src/cobalt_agent/` → NO output: the name exists nowhere in `src/cobalt_agent/`; `git log --oneline -S FinvizStockData -- src/cobalt_agent/` → NO output: it never appears in any commit's diff of that path either. `grep -rn "class Finviz" src/cobalt_agent/skills/research/` → `finviz_api.py:38: class FinvizApiClient` · `finviz_extractor.py:39: class FinvizExtractor`. (`FinvizExtractionResult`, the third import at `test_finviz_extractor.py:34-38`, was not checked.) So the test imports a name the old tree has not defined in the history reachable from this branch; the test is stale, not the source.
DISPOSITION: the failing file is `tests/test_finviz_extractor.py` — old tree (`cobalt_agent.*`), outside `tests/cobalt/` and `tests/taxonomy/` — so no fix, no `pyproject` change (strangler rule; `pyproject.toml:91` `testpaths = ["tests"]`). ONE line added under `## STANDING FOLLOW-UPS` in `docs/00 - Project/BACKLOG.md` (end of section, after the `CLAUDE.md "Current phase"` line), text exactly as the prompt gives it with the file:line and error filled in. Why the gates missed it: every build prompt's suite names `tests/cobalt tests/taxonomy` (`cto-2026-09-20.md:256`); only a bare `pytest -q` collects `tests/test_finviz_extractor.py`. `git show --stat HEAD`: only `docs/00 - Project/BACKLOG.md`, 1 insertion.

## 5. Item 5 — the always-loaded block: MEASURED, PROPOSED (nothing in the memory folder written)
`wc -c` (bytes): INDEX.md 1838 · profile.md 706 · preferences.md 1450 · **total 3,994** (matches `close-2026-09-20.md`).
`wc -m` (characters): INDEX.md 1730 · profile.md 691 · preferences.md 1422 · **total 3,843**.
Character margin under L58's cap: 4000 − 3843 = **157**. The 09-20 "margin 6" was a BYTE margin (`→ — ·` are 3 bytes, 1 character each; 151 bytes of multibyte overhead across the three files). It overstated the pressure by 151; L58 says characters. SESSION-CLOSE step 5 (`SESSION-CLOSE.md:18`) still measures with `wc -c`, which is why the block looked full.

Proposals (for the desk only; counts measured with `wc -m` on scratch copies, newline excluded):
MEMORY: INDEX.md:3 — now 77 chars — proposed text "description: Map of the memory folder." — saves 39
MEMORY: INDEX.md:32-34 — now 97 chars (three lines) — proposed text "- closed: [[trading-copilot-os]] superseded · [[2025-tax-filings]] · [[subscription-audit]]" — saves 8
MEMORY: preferences.md:18 — now 103 chars — proposed text "- [stated 2026-09-12 · chat] equal access, meter ≠ router, one memory — LAWS L44 L47 L56" — saves 15 (the rules stay in LAWS L44, L47, L56; the always-loaded line becomes a pointer)
MEMORY: SESSION-CLOSE.md:18 — measures with `wc -c` (bytes, 3,994) while L58's cap is characters (`wc -m`, 3,843) — proposed text: replace "wc -c INDEX.md profile.md preferences.md" with "wc -m INDEX.md profile.md preferences.md" — saves n/a (measure only)
Noted, not proposed: `preferences.md:15` ("his task = push") predates L55's 2026-09-19 amendment (the desk pushes on his word); a staleness item for the memory sitting, not a shortening. None of the three edits is needed to stay under the cap (margin 157).

## 6. Item 6 — restic include set: 6a DONE (LAUNCH 1), commit `227325c`. 6b is LAUNCH 2's — NOT run here.
**ASK DESK (not answered, non-blocking) — which files are "the Cobalt key store"?** The vault file only, or also `~/.cobalt_key` (the master key, `ops/run_backup.sh:26-28`)? A repository holding both lets the restic password alone open every secret. SAFE DEFAULT TAKEN: the vault file ONLY. [17:38 ET, `date` 17:36–17:41]

What I changed (three paths, nothing else): `configs/cobalt/backup.yaml` — under `sources:` after `/Users/cobalt/Vault/Think`, a two-line comment ("The encrypted credential vault: a host loss must not take the only copy of every credential (PROJECT-LEDGER.md:1174, owed since 09-11).") + `- /Users/cobalt/cobalt/data/.cobalt_vault`; `tests/cobalt/test_backup.py` — one test, `TestConfig::test_the_cobalt_vault_file_is_in_the_include_set` (loads the COMMITTED `configs/cobalt/backup.yaml` through `load_backup_config()`, asserts the vault path is in `cfg.sources`); `docs/40 - DevDocs/cobalt/backup/config.md` — one sentence (new `## What the include set holds`). Facts checked first: `sources: list[Path] = Field(min_length=1)` (`src/cobalt/backup/config.py:139`), no per-path validation; `restic.py:182` hands `[str(p) for p in cfg.sources]` to restic as targets, so a file path is a valid target.

RED before the edit (`uv run pytest -q tests/cobalt/test_backup.py -k test_the_cobalt_vault_file_is_in_the_include_set`):
```
E   AssertionError: assert PosixPath('/Users/cobalt/cobalt/data/.cobalt_vault') in [PosixPath('/Users/cobalt/Vault/Think')]
tests/cobalt/test_backup.py:74: AssertionError
FAILED tests/cobalt/test_backup.py::TestConfig::test_the_cobalt_vault_file_is_in_the_include_set
1 failed, 22 deselected in 0.06s
```
GREEN after the edit (`uv run pytest -q tests/cobalt/test_backup.py`): `23 passed in 0.03s` (22 collected at PREFLIGHT + 1). Full suite `2209 passed, 351 skipped, 1 xfailed, 15 warnings in 64.35s` = BASELINE 2208 + 1.

`uv run cobalt jobs restarts main..HEAD` — see §8 for the table VERBATIM: **`configs/cobalt/backup.yaml` is `UNCLASSIFIED CONFIG` (exit 1, derives all six residents)** — the prompt said UNCLASSIFIED → ESCALATE; ESCALATE 1. The truth per the FACTS table: the config is read at every 21:40 run by the one-shot `com.cobalt.backup` (`ops/com.cobalt.backup.plist:71-80`), no resident; the classifier has a one-shot shape for `configs/cobalt/watchlists.yaml` (`src/cobalt/jobs/restarts.py:243-244`) and none for `backup.yaml`.
L67: this is a BUILD — ≥3 houses check it before the desk's deploy prompt carries it; not merged, not pushed. `git show --stat HEAD`: `configs/cobalt/backup.yaml` 3 +, `docs/40 - DevDocs/cobalt/backup/config.md` 3 +, `tests/cobalt/test_backup.py` 11 + — only the three named paths.
6b was NOT attempted (LAUNCH 2, Opus 5, after his "rotated" + the 6a deploy + one 21:40 run).

## 7. Item 7 — three never-added settings-packet paths: `.gitignore` — DONE, commit `9dfc9a0`
**ASK DESK (not answered, non-blocking) — does R40's "from now on" want a standing pattern** (e.g. `docs/40 - DevDocs/prompts/*/*-packet/*settings*.yaml`) for future packets? SAFE DEFAULT TAKEN: the three named lines only. [17:41 ET]
Files confirmed on `~/cobalt` (`ls -la`): `2026-09-19/31-packet/p2-dark-settings.yaml` (44 B), `p2-live-settings.yaml` (362 B), and `2026-09-20/16-packet/p2-live-settings-htf.yaml` (405 B). Edit — directly after the `!docs/40 - DevDocs/**` carve-out (`.gitignore:21-22`), before `!docs/50 - Roles/`: the comment `# 09-20 R40: a settings packet yaml never enters git (sha256 in the report; loadable copy in data/backups/)` (`:23`) and exactly the three named lines (`:24-26`). No glob, no `!` line touched.
PROOF (`git check-ignore -v`, one call each):
```
.gitignore:24:docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml	docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml   (exit 0)
.gitignore:25:docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-live-settings.yaml	docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-live-settings.yaml   (exit 0)
.gitignore:26:docs/40 - DevDocs/prompts/2026-09-20/16-packet/	docs/40 - DevDocs/prompts/2026-09-20/16-packet/p2-live-settings-htf.yaml   (exit 0)
```
NEGATIVE CONTROLS: `git check-ignore -v "docs/40 - DevDocs/prompts/2026-09-19/31-packet/facts.md"` → no output, **exit 1**; `git check-ignore -v "docs/40 - DevDocs/prompts/2026-09-20/17-cards-htf-apply.md"` → no output, **exit 1**. `git log --oneline -1 -- "docs/40 - DevDocs/prompts/2026-09-19/23-packet/"` → `d6bbdd5 docs(desk): 09-19 cards go-live check done (2 houses, no code blocker); file cut to ruled values (sha256 f3663399…); P4 check C launched (e1072ecd)` — the two committed copies are still tracked, no line touches them. The three files do not exist in this worktree; `check-ignore` tests the path, which is what the merge will meet. `git show --stat HEAD`: `.gitignore` only, 4 insertions.

## 8. CLOSE
`uv run pytest -q tests/cobalt tests/taxonomy` (17:41): `2209 passed, 351 skipped, 1 xfailed, 15 warnings in 64.27s`, 0 failed — vs BASELINE `2208 passed, 351 skipped, 1 xfailed` = **+1 passed (6a's new test)**, skipped and xfailed unchanged.

`uv run cobalt jobs restarts main..HEAD` — VERBATIM (exit 1; the `RESTARTS:` line is the command's, L42):
```
FAILED: RestartError: one or more changed paths were unclassified
path	change	rule	restart
.gitignore	M	META	-
configs/cobalt/backup.yaml	M	UNCLASSIFIED CONFIG	com.cobalt.agent,com.cobalt.aset,com.cobalt.herdr,com.cobalt.mainframe,com.cobalt.obsidian,com.cobalt.radar
ESCALATE: unclassified path configs/cobalt/backup.yaml
docs/00 - Project/BACKLOG.md	M	DOCS	-
docs/40 - DevDocs/cobalt/backup/config.md	M	DOCS	-
docs/40 - DevDocs/cobalt/db_migrations/cli.md	M	DOCS	-
docs/40 - DevDocs/prompts/2026-09-21/58-s2-smoke-first-look.md	D	DOCS	-
docs/40 - DevDocs/prompts/2026-09-21/59-propose-stale-score.md	D	DOCS	-
docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md	M	DOCS	-
docs/40 - DevDocs/reports/cto-2026-09-21.md	M	DOCS	-
docs/40 - DevDocs/reports/ops-2026-09-21.md	A	DOCS	-
tests/cobalt/test_backup.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar
```
Reading of the table: the command diffs `main`'s TIP against `HEAD` (two-dot), so the `D` rows for `58-…`/`59-…` and the `M` on `cto-2026-09-21.md` are two desk commits that landed on main AFTER this worktree was cut (`git log --oneline -4 main`: `7497586 docs(desk): 09-21 stale-score proposal drafter 59 launched`, `031196f docs(desk): 09-21 ops-0921 launched, S2 smoke first-look prompt 58, night plan and sessions table`, then `f5c5bf0`, this branch's base) — not changes of mine; all `DOCS → -`. The `ops-2026-09-21.md A` row is this report. The ONE non-doc, non-test row is `configs/cobalt/backup.yaml`.

`git log --oneline main..HEAD` (before the report commit):
```
9dfc9a0 chore(gitignore): settings packet yaml stays out of git — three named paths (09-20 R40)
227325c feat(backup): the Cobalt vault file joins restic's include set (LEDGER:1174, owed since 09-11)
5e2ae4c docs(backlog): bare pytest collection red on the old-tree tests — recorded, not fixed (strangler rule)
6193cd7 docs(standard): UNATTENDED-LAUNCH — an exit status is read from the tool result, never by wrapping (09-20 §41)
e2db970 docs(devdocs): db_migrations/cli.md — the SET LOCAL sits before the BEFORE probe, which is under the ceiling (09-20 1a-fix S2)
```
One commit per built item (1, 2, 4, 6a, 7; items 3 and 5 are report-only by the prompt) + the report commit below.

`git diff --stat main...HEAD` (three-dot: this branch's own changes; every path is one the prompt names):
```
 .gitignore                                     |  4 ++
 configs/cobalt/backup.yaml                     |  3 ++
 docs/00 - Project/BACKLOG.md                   |  1 +
 docs/40 - DevDocs/cobalt/backup/config.md      |  3 ++
 docs/40 - DevDocs/cobalt/db_migrations/cli.md  | 54 +++++++++++++++++++-------
 docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md |  2 +-
 tests/cobalt/test_backup.py                    | 11 ++++++
 6 files changed, 58 insertions(+), 16 deletions(-)   [+ this report, committed last]
```
The prompt's literal `git diff --stat main HEAD` (two-dot) ALSO lists `prompts/2026-09-21/58-…` (−16), `59-…` (−15) and `reports/cto-2026-09-21.md` (−17/+): main's two later desk commits shown reversed — the same cause as above, ESCALATE 7. Every path the branch itself changed is a named one.

## ESCALATE
1. **`jobs restarts` cannot classify `configs/cobalt/backup.yaml` (6a).** Exit 1, `UNCLASSIFIED CONFIG`, derived `RESTARTS:` = all six residents. Real effect per the code and plist: none — only the one-shot `com.cobalt.backup` reads it, each 21:40 run (`ops/com.cobalt.backup.plist:71-80`), so the correct deploy line is "`com.cobalt.backup` (one-shot; reads the config fresh); no resident restart". The gap: `src/cobalt/jobs/restarts.py:243-244` has a one-shot rule for `configs/cobalt/watchlists.yaml` and none for `backup.yaml`. Not fixed here (a `src/` change outside this run's named paths; it would itself need house checks). Desk's call: add the rule (+ a test) as a small build BEFORE the 6a deploy prompt, or carry an explicit `RESTARTS:` override with his word (L42 says unclassified → ESCALATE, never dropped).
2. **(i) 6a is a BUILD** — ≥3 houses check it (L67), then the desk's deploy prompt. No resident restart is needed (`com.cobalt.backup` is a one-shot calendar job); see ESCALATE 1 for the classifier gap. Not merged, not pushed.
3. **(ii) 6b waits** for his "rotated <time>" (`grep -n "rotated"` in `cto-2026-09-21.md`) + the 6a deploy + one 21:40 run after both — then LAUNCH 2 (Opus 5, `--remote-control ops-0921-6b`, its two production strings N2/N3, both approved in R43). Not attempted here.
4. **(iii) The dump reload into a scratch database is OWED** by a DB-capable prompt (no rule in this launch line has a DB shape); 6b proves only that the restored `cobalt_brain.sql` is present and sized.
5. **ASK DESK — the Cobalt key store: vault file only, or also `~/.cobalt_key`?** Default taken: the vault file only. [17:38 ET]
6. **ASK DESK — item 7: a standing pattern for future settings packets, or the three named lines only?** Default taken: the three named lines. [17:41 ET]
7. **This branch is 2 commits behind main.** `031196f`, `7497586` (docs only) landed after the worktree was cut; the deploy needs the desk's rebase-then-ff (L54) and a fresh `jobs restarts <range>` on the rebased range. The literal `git diff --stat main HEAD` shows main's two later files reversed (see §8).
8. **Bars branch (chunk 1a, unmerged) `cli.md:461-465`** — not read, not edited. Main's `cli.md` now says "It is the first statement `cmd_migrate` issues after `_connect` returns — BEFORE the BEFORE probe". If 1a's copy still says "After the BEFORE probe, before `_apply`" / "the BEFORE PROBE IS NOT UNDER THE CEILING", it will contradict main when it merges; the desk/1a fix must carry the same correction.
9. **Item 3 needs the desk's asks.** Nine rows in the table, verdict `ASK` for all; `ListAgents` prints 6-character ids and the registry has 8-character ones, so ids cannot be matched — the desk asks by NAME/label (`SendMessage`), eight first (recommendation in §3); `b63d56a0` separately (its launch is in no report).
10. **Item 4 — the old-tree test collection red** is recorded in BACKLOG, not fixed (strangler rule); desk's call: exclude the old-tree tests from the default collection, or leave the red. Unchecked by me: whether `FinvizExtractionResult` (the test's third import) exists.

MEMORY: INDEX.md:3 — now 77 chars — proposed text "description: Map of the memory folder." — saves 39
MEMORY: INDEX.md:32-34 — now 97 chars (three lines) — proposed text "- closed: [[trading-copilot-os]] superseded · [[2025-tax-filings]] · [[subscription-audit]]" — saves 8
MEMORY: preferences.md:18 — now 103 chars — proposed text "- [stated 2026-09-12 · chat] equal access, meter ≠ router, one memory — LAWS L44 L47 L56" — saves 15
MEMORY: SESSION-CLOSE.md:18 — measures with `wc -c` (bytes, 3,994) while L58's cap is characters (`wc -m`, 3,843, margin 157) — proposed text: "wc -m INDEX.md profile.md preferences.md" — measure only
RULING: none proposed. (His R43 words were "App approved", recorded by the desk as "all approved"; the row names the strings and this launch, and I read it as covering N1–N3.)

## CONTINUE
step 9 — all seven items done offline; report committed by path; nothing further for LAUNCH 1. LAUNCH 2 (6b only) waits on ESCALATE 3.

OPS 0921 DONE · items: 7 of 7 · commits: 6 · NEW strings used: 1 · ESCALATE: 10
