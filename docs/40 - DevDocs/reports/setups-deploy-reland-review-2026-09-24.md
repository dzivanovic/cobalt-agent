# Setups deploy re-land review (32) — 2026-09-24

## §0 Headline
- ONE-ROUND DELTA READ of `32-setups-deploy-reland.md` (committed `c70b4971`; the desk's `R__L` token still present in `32`, 4 lines, expected before the desk fills it). Houses that answered: Grok `REVIEW: RUN IT` · Opus 5.5 `REVIEW: RUN IT AFTER` 3 folds. The L67 floor (Grok) is met.
- File-check: 0 blockers. 7 folds proposed (none blocks the launch as the hub read them; the desk decides). ESCALATE: 3 (Grok ran ≈14 min against a 12-min clock; `R__L` unfilled; hub deviations from the UNATTENDED RULES).
- Both houses answer NO to Q1 (worse than now), Q2 (tree identity), Q3 (strings, 0 new: 51 of 51 strings count 1 in `09`, and the whole allow/deny/add-dir segment counts 1 in `32`). They differ on Q4 and Q5 (Opus: PAUSE-window false RED; (h) narrow gap; Grok: NO to both). The file-check does not confirm Opus's PAUSE chain (see C1).
- This report replaces the 12:44 report of an earlier launch of this prompt (`FAILED: placeholder — lines 31, 50`; that file was untracked, never committed).

## L74
No tool result asked for a `Claude-Session:` line or named a file-send tool during this run. Nothing recorded, nothing followed.

## PREFLIGHT
| rule | command | exit | result |
|---|---|---|---|
| PLACEHOLDER GATE | `grep -n -E "R_[_]" "…/prompts/2026-09-24/33-review-setups-deploy-reland.md"` | 1 | allowed; printed NOTHING |
| DATE GATE (row 1) | `date` | 0 | `Thu Sep 24 12:45:35 EDT 2026` → 2026-09-24 |
| grok | `grok --version` | 0 | allowed; `grok 1.0.25 (f7e67d6988e2) [stable]` |
| OPUS PROBE | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (run_in_background, `bhycyuuzz`) | 0 | allowed; `OK` = UP |
| prompt files | `ls -la` of `32-setups-deploy-reland.md` and `reports/setups-deploy-reland-draft-2026-09-24.md` | 0 | present: 81,233 B (12:25) and 19,148 B (12:28) |
| PROMPT IS COMMITTED | `git -C /Users/cobalt/cobalt log -1 --format=%H -- "docs/40 - DevDocs/prompts/2026-09-24/32-setups-deploy-reland.md"` | 0 | `<prompt sha>` = `c70b4971b684a7a9a660bc1a136afdb8efd6868a` |
| STAGGER | `grep -n -F "no other house hub is running" …/cto-2026-09-24.md` | 0 | line 59 (`| R49 |`) names `33-review-setups-deploy-reland.md` and carries the literal → `stagger: clear (launch row)` |
| scratch folder | `ls scratch/tribunal-bars-0920/setups-deploy-reland-0924` | 1 | "No such file or directory" = fresh run |
| DATE GATE (row 2) | `date` | 0 | `Thu Sep 24 12:57:00 EDT 2026` → 2026-09-24 |

AUTHORIZATION (each its own call): R32 `cto-2026-09-22.md:131` carries `Bash(claude -p --model claude-opus-5-5 *)`, `log -S` → `b8a72b5300370e248cd6c7a8a732258fec03e6a0` · R19 (Grok gate) `cto-2026-09-24.md:29` carries `All 4 house models approved` · R45 `:55` carries `right now` and `re-land` · R95 `cto-2026-09-23.md:103` printed · R49 `:59` names `33-review-setups-deploy-reland.md`, `log -S` → `1055421cd3e676796b9adb2740018a4f8fb6fbbc`.

## Packet
Staged in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-deploy-reland-0924/` (Read → Write; every part ≤ 38,000 B; `wc -c` vs the source by `grep -b` offsets, header bytes counted and subtracted):
| file | bytes | source check |
|---|---|---|
| `delta.md` | 12,773 | = draft lines 10–58 (offsets 1132→13905), 12,773 exact |
| `32-delta-sections.part1.md` | 30,244 | 32 lines 1–12, 22–28, 33–43, 127–176, 177–197 = 29,891 + 353 B of range headers (each section checked by offset) |
| `32-delta-sections.part2.md` | 19,544 | 32 lines 198–223, 224–252, 253–271, 337–339 = 19,285 + 259 B headers |
| `launch-09.md` | 3,310 | `09` line 7, offsets 3936→7246, exact |
| `incident.md` | 7,091 | `deploy-2026-09-24.md` lines 289, 291, 263–282, 306 = 6,825 + 266 B headers |
| `what-happened.md` | 1,852 | `31` line 5 (single line; not offset-checked) |
| `tree.md` | 13,020 | commands' output; the two `scoring.py` cuts came from `grep -n -B5 -A5 "na_reason"` on the worktree `stacked-0923` (HEAD `a0ba0098`, clean) and on `~/cobalt`'s working file, disclosed in the file; the header of `show --stat` is its last 5 lines plus a separate `--no-patch` call; one extra `grep -A3 "^NaReason = Literal"` |
| `greps.txt` | 3,641 | counts (`grep -c`) and line numbers, said so: full outputs are lines of 200–3,300 B |
| `32-extra-authorization-rules-step6.md` | 20,923 | ADDED by the hub (not in the staging list): 32 lines 44–126 (14,912 B) and 316–327 (5,520 B) + 491 B headers, because Q6 asks about the AUTHORIZATION, the launch-row literal, STEP-6.8 and the UNATTENDED RULES |
| `QUESTIONS.md` | 6,394 | the prompt's text verbatim + the "Files in this folder:" paragraph (names `greps.txt` SECOND) |
| `grok-review.md` / `opus-review.md` | 6,896 / 8,444 | the printed answers byte for byte (Grok's line 1 begins with its own process narration); hub-written |
Deviation from the staging list, disclosed: `32-delta-sections.md` is two files (part1 / part2); the `THE SHAPE` paragraph is lines 22–28 and `THE WINDOW` 33–43 as named (lines 29–31, the L66'S SHAPE / DOWNTIME / DO NOT STOP paragraphs, are not staged; delta rows 10 carries them). Trailing whitespace in `32`: `grep -c "[[:space:]]$"` = 0.

## CONTINUE
next: none — the run ended at the last line below. (History: preflight → packet → houses launched 12:57 ET → Opus notice read 13:03:18 (≈6 min) → Grok notice read 13:12:00 (output file mtime 13:11, ≈14 min) → collation.)

## Per question
Each cell is the house's own words, ≤30 words, with the file:line it cites. Grok's cites are `part1`/`part2` = `32-delta-sections.part1.md`/`part2.md`.
| Q | grok | opus |
|---|---|---|
| Q1 worse than now | "NO — no listed path ends worse than today's degraded production… Migrate is `--proof-only` only (`part2.md:16–18`)… The only DOWN endings name the resident." | "NO … nothing writes to the DB… `--allow-prod` fenced three times (part1:12, part2:16, part1:103)." Residual: interrupted STEP-5 revert → relaunch (i) leaves both residents DOWN (part1:97, part2:67). Not a blocker. |
| Q2 tree identity | "NO — the revert restores `a0ba0098`'s non-docs tree… `4e625f6a`'s only parent is `a0ba0098` (`tree.md:8–10`)… 4.3 requires `HEAD^1 = <main0>` and an empty diff (`part2.md:13–14`)." | "NO. `4e625f6a` has the single parent `a0ba0098` (tree.md:8-9)… P4's diff EMPTY outside docs (part1:60; tree.md:32-33)." A dirty tracked docs file among the 128 would refuse the revert — UNVERIFIABLE FROM READS. |
| Q3 strings | "NO — every commanded spelling matches `09`'s allow list; new strings are 0… (`part1.md:10–12`, `delta.md:9`)." | "NO. Every command … covered … 0 new" (compared by eye: part1:10 vs launch-09.md:1). |
| Q4 tails | "NO — the 78 s cycle cannot false-red this rule, and the waits are just `date` plus `heartbeat show`… (`part2.md:43`, `:33`)." | "YES, in the PAUSE window": BETWEEN relaunch → 20:01 → no cycle until 21:00 → the old `failed_stage evaluate` record stays → (e)/(h) RED (part2:43,46,51; extra:91). DAY window: no false RED. |
| Q5 readback (h) | "NO — (h) plus (b) prove this incident's failure is gone, and (e)'s allowance does not hide it… (`part2.md:30`, `:49–53`)." | "YES, a narrow gap": (h)'s heartbeat may hide an evaluate failure behind the carried `bars` kind (incident.md:16); the grep counts only the panel spelling (incident.md:23). Fold: count `input_value='ASSUMED'` on both logs. |
| Q6 dialog / harm | "NO — no dialog, no false fail of a good re-land, no pass of a bad one, no harm beyond a named stop… 6.8 requires the gate worktree at `a0ba0098`." UNVERIFIABLE: whether `^` globs in the tool's shell (fails closed). | No dialog path. 6.8's pre-checks state no outcome on mismatch (extra:93, :98); RELAUNCH (ii) does not re-run R.2's snapshot (part1:98); UNVERIFIABLE: R45's `re-land`, 4.4's proof-only after 0013, gate-worktree status. |
| closing line | `REVIEW: RUN IT` | `REVIEW: RUN IT AFTER (h)/6.7 require unchanged input_value='ASSUMED' counts on both logs · PAUSE: stale evaluate record is not RED without a cycle · 6.8 pre-check mismatch → NOT RUN + ESCALATE, not STEP-5` |
Where the houses contradict each other, quoted: Q4 — Grok "the 78 s cycle cannot false-red this rule" vs Opus "PAUSE window false RED, concrete sequence: BETWEEN relaunch → HARD CLOCK 20:01 → PAUSE → re-land → the radar idles until 21:00". Q5 — Grok "(e)'s allowance does not hide it" vs Opus "(h)'s heartbeat check assumes the radar line reports the evaluate failure … a cycle failing both bars and evaluate could hide evaluate".
Timing: Opus launched 12:57, notice read 13:03:18 (≈6 min). Grok launched 12:57, exited 0 at 13:11 (output file mtime), notice read 13:12:00 — ≈14 min against the 12-min clock. The hub had no wake-up between 13:04:32 and the notice (no poll, no Monitor, by rule), so it did not stop it; the answer, with its closing `REVIEW:` line, is kept (see ESCALATE).

## Checked against the files
Notation: `claim · who · file:line · status · blocks the launch? — why · ≤30 words`.

| # | claim · who · file:line | status | blocks? — why |
|---|---|---|---|
| C1 | The PAUSE window false-REDs a good re-land: no cycle before 21:00, the old `failed_stage evaluate` record stays, so (e) read 2 / (h) / 6.7 go RED → STEP-5. · opus · opus-review.md Q4; `32` part2 lines 241, 246; extra 319 | DOES NOT HOLD as written. `heartbeat/probes.py:88-89` (`radar` probe) returns OK `paused (market_reset)` when the session is MARKET_RESET, BEFORE it reads the pool row (`:92`, `:106-107`); `session/__init__.py:4` names market_reset 20:00–21:00 = the PAUSE window. The stale record is not shown then. | no — the chain does not occur; PAUSE is also reachable only by a BETWEEN relaunch after 20:00 |
| C1b | (hub note, no house claim) In the PAUSE window (h)'s and 6.7's heartbeat leg cannot see `failed_stage evaluate` at all (`probes.py:88-89` returns before the row is read); the `ASSUMED` count and the `radar panel FAILED` count remain. · `32` part2 line 246 | HOLDS (fact) | no — same reason; recorded for the desk |
| C2 | (h)'s heartbeat check can miss an evaluate failure hidden behind the carried `bars` kind. · opus · opus-review.md Q5; incident.md:16 | DOES NOT HOLD for one cycle: `radar/runner.py:296` stamps bars, then `:336-337` `stamp_failure(failed_stage="evaluate")` runs after it and `store.py:229` overwrites unconditionally. BUT `runner.py:375` writes `failed_stage` None / `bars` at each cycle's S2 and the S5 stamp comes ~70 s later (comment `:357-359`), so a heartbeat sample between them can read no evaluate finding. UNVERIFIABLE FROM READS — repeated `COBALT_ENV=production uv run cobalt heartbeat show` across a cycle on the live radar. | no — (c)'s `/radar` curl (before tail 1) makes the panel read the three `ASSUMED` rows; a failure logs `radar panel FAILED` (`deploy-2026-09-24.md:278`) and (h) compares that count |
| C3 | (h)'s grep counts only `radar panel FAILED` in `aset.err`; `radar pool refresh FAILED` (a different string) and radar-side failures are not counted. · opus · incident.md:23 | HOLDS: `deploy-2026-09-24.md:278` shows `api_radar_pool:883 - radar pool refresh FAILED` beside `radar:866 - radar panel FAILED`; `32` part2 line 247 greps only the second. | no — the pool refresh runs only on a page/API request; the panel count is the tied detector |
| C4 | 6.8's pre-check mismatch (HEAD ≠ `a0ba0098`, status ≠ `## deploy/stacked-0923`, `.env` present) has no stated outcome; a red row → STEP-5 would roll back a good re-land after the note is written. · opus · extra:93, :98 | HOLDS as text: `32` lines 321 and 326 state the pre-checks and the RED rule for test output only. `git -C /Users/cobalt/cobalt-wt/stacked-0923 status --short --branch` printed exactly `## deploy/stacked-0923` at 13:0x and `rev-parse` `a0ba0098`. | no — the gate worktree is clean and at `a0ba0098` now; only a later dirt would trigger it |
| C5 | R45's row must carry `re-land`; his quoted words do not. · opus (UNVERIFIABLE) · extra:5 | DOES NOT HOLD as a risk: `cto-2026-09-24.md:55` carries `right now` (count 1) and `re-land` (count 1); P00's grep passes. | no |
| C6 | The `-S"RE-LAND: a0ba0098 by revert of 4e625f6a"` check passes on any earlier commit quoting it. · opus · extra:27 | DOES NOT HOLD now: `git -C /Users/cobalt/cobalt log -S"RE-LAND: a0ba0098 by revert of 4e625f6a" -- "docs/40 - DevDocs/reports/cto-2026-09-24.md"` printed nothing; the R49 row carries the literal 0 times. The row must be written and committed by the desk. | no — the gate fails closed until the desk's launch row exists |
| C7 | 4.4's `--proof-only` after 0013 is applied has never run; `CHANGED` or a non-zero exit → STEP-5 on a good re-land. · opus (UNVERIFIABLE) · part2:15-18 | DOES NOT HOLD for `CHANGED`: at `a0ba0098` `db_migrations/cli.py:666-677` the proof-only path only prints `_print_probe` (`:435-468`, one state, no verdict column); `CHANGED` is computed only on the forward path (`:692-693`). `32` line 30 records `09`'s P14-M proof-only at 78.8 s. A non-zero exit remains possible. | no |
| C8 | RELAUNCH (ii) re-runs R.3 if the tag is missing but not R.2's snapshot. · opus · part1:98 | HOLDS as text: `32` line 189 "STEP-R's tag and snapshot stand if recorded; if `pre-reland-0924` is absent, run R.3 first". | no — a missing snapshot does not stop the re-land; the tag `pre-reland-0924` is the durable rollback point (line 182) |
| C9 | An interrupted STEP-5 revert → relaunch (i) → `revert --abort` → HEAD = `<reland>` ≠ `<main0>` → "Anything else" → both residents DOWN, while STEP-5's own conflict rule calls that tree "safe state: NEW code kept" and brings residents up. · opus · part1:97, part2:67 | HOLDS as text: `32` line 188 ("Anything else → bootstrap nothing … aset: DOWN — radar: DOWN") vs line 261. | no — requires a session interruption inside a single `git revert`; the DOWN ending is named and the desk escalates at once |
| C10 | (b)'s `Traceback` / `TaxonomyConfigError` counts are read before the first cycle and never recounted after tail 3. · opus · part2:32 | HOLDS as text: `32` lines 233–237 (before tail 1); (h) has no recount. | no — tails 1–3 and the `S5 evaluate FAILED` string cover the failure inside their windows |
| C11 | (e) says the incident kind "is judged by (h), not here" and also allows it "on the FIRST read only". · opus · part2:46 | HOLDS as text: `32` line 241 carries both. | no — both readings end at (h)'s RED test |
| C12 | A dirty tracked docs file among the 128 paths (e.g. `seat-usage.md`) would make the revert refuse. · opus (UNVERIFIABLE) · part1:46 | DOES NOT HOLD for the current dirt: `git diff --stat a0ba0098 4e625f6a -- "docs/40 - DevDocs/reports/seat-usage.md"` empty (not in the 128); `git show --stat 4e625f6a -- configs/cobalt/rules.yaml` empty; `git diff --stat 4e625f6a main -- "docs/40 - DevDocs/cobalt" "docs/10 - Decisions"` empty and the ten re-added reports empty; `git status --porcelain` (13:0x): ` M rules.yaml`, ` M seat-usage.md`, four `??` docs incl. this report. | no |
| C13 | Untracked paths the revert adds back fail P1 or the 4.2 error path. · grok · part1:52 | HOLDS: the ten reports of `32` line 144 = the ten `D` report paths of `git show --name-status --format= 4e625f6a -- docs`; that commit also deletes 27 non-report docs (`docs/40 - DevDocs/cobalt/radar/anatomy/*.md`, `formation/*.md`, `ADDING-A-SETUP.md`) that P1 does not list; none is untracked now. A collision falls to 4.2's "any other error" path (line 206). | no |
| C14 | The heartbeat/`failed_stage` model: `failed_stage` persists on `system.radar_pool` until the next row write. · opus (implied) · incident.md:16 | HOLDS (`radar/store.py:184-190`, `:229`; `runner.py:375`). | no |
| C15 | Whether `HEAD^1` globs in the session's zsh. · grok / opus | UNVERIFIABLE FROM READS — `git -C /Users/cobalt/cobalt rev-parse --short HEAD^1` run once in that shell; `09` P3 ran `HEAD~1` under the same allow string. | no — 4.3 fails closed |
| C16 | (hub note, no house claim) `32` line 95 says this run "touches no `cobalt_dev`", line 333 lists "`cobalt_dev` untouched by this run", but STEP-6.3 (line 315) runs `COBALT_ENV=dev … taxonomy assumed write … --apply` and says its `vault_writes` rows "go to `cobalt_dev`". · `32` lines 95, 315, 333 (delta rows 15, 42) | HOLDS as text | no — no `.env` copy, the desk holds every other with-DB run (line 7), the dev write is the L28 dev proof; the sentence in the report would be untrue |

(i) Allow strings of `32`'s `(3) claude --bg` line, `grep -c -F -e '<string>'` (quotes included) against `09-setups-deploy-r2.md`: all 51 count **1** — in the order of the line: `git … add *` 1 · `commit *` 1 · `reset --soft HEAD~1` 1 · `tag *` 1 · `merge --ff-only deploy/stacked-0923` 1 · `revert --no-edit *` 1 · `revert --abort` 1 · `setups-c1 rebase main` 1 · `setups-c1 rebase --abort` 1 · `stacked-0923 merge --no-edit setups/seven-0921` 1 · `stacked-0923 merge --no-edit main` 1 · `stacked-0923 merge --abort` 1 · `git -C * status*` 1 · `log*` 1 · `diff*` 1 · `rev-parse*` 1 · `rev-list*` 1 · `merge-base*` 1 · `show*` 1 · `cd *` 1 · `uv run pytest *` 1 · `COBALT_ENV=dev uv run pytest *` 1 · `COBALT_LIVE_VAULT_ROOT=… uv run pytest *` 1 · `cp …/.env …/stacked-0923/.env` 1 · `rm …/stacked-0923/.env` 1 · `COBALT_ENV=dev … db migrate` 1 · `… db migrate --proof-only` 1 · `COBALT_ENV=production … validate` 1 · `jobs restarts *` 1 · `heartbeat show*` 1 · `backup run*` 1 · `backup status*` 1 · `db migrate --allow-prod --proof-only` 1 · `db migrate --allow-prod` 1 · `db query *` 1 · dev `taxonomy assumed write … --apply` 1 · production `… --dry-run` 1 · production `… --apply` 1 · `taxonomy tunables --assumed` 1 · `launchctl bootout` aset 1 / radar 1 · `bootstrap` aset 1 / radar 1 · `kickstart -k` aset 1 / radar 1 · `launchctl print gui/501/*` 1 · `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*` 1 · `grep *` 1 · `tail *` 1 · `ls *` 1 · `date*` 1. No string counts 0. Also `grep -c -F -e '<the whole segment --allowedTools … --add-dir /Users/cobalt/cobalt-wt>'` on `32` = **1** (the 51 strings, the 3 denies and the `--add-dir` pair, in order, byte for byte).
(ii) `grep -c -E "R_[_]" 32` = **4** lines (7, 64, 66, 158; `grep -n -o -E "R_[_]L"` → 7, 64 ×2, 66, 158): the desk's launch-row token, unfilled at the time of the read; expected, not a blocker.
(iii) `grep -c -F "revert --no-edit -m 2" 32` = **0**; `grep -n -o -F "revert -m 2" 32` → line 3 only (WHAT HAPPENED, history). `grep -n -F "merge --ff-only deploy/stacked-0923" 32` → lines 9 (launch line), 11 (NEVER RUN HERE list), and **61** (twice; the R52 approval-row check text in the AUTHORIZATION section, not a command to run); line 205 names `merge --ff-only` as "NEVER RUN HERE".
(iv) `delta.md` rows against `32` (the "after" landed at): 1 → 1 (`MODEL: Opus 5.5 … RE-LAND`, "NO gate re-run") · 2 → 3 (`WHAT HAPPENED`) · 3 → 6 (`(1) NO GATE RE-CUT`) · 4 → 7 (`DESK LINE … R__L`) · 5 → 9 · 6 → 11 (13 NEVER RUN HERE strings) · 7 → 13 · 8 → 15–20 · 9 → 22–27 · 10 → 29–30 · 11 → 33–42 · 12 → 44–67 · 13 → 68 · 14 → 70–72 · 15 → 74–98 · 16 → 100–106 · 17 → 108–125 · 18 → 129, 45 · 19 → 136–144 · 20 → 149–157 · 21 → 159 (P13 only) · 22 → 158 · 23 → 160–172 · 24 → 173 · 25 → 177–194 · 26 → 187–194 · 27 → 199–202 · 28 → 203–206 · 29 → 207–210 · 30 → 211–214 · 31 → 215–216 · 32 → 227–229, 238 · 33 → 241 · 34 → 242 · 35 → 244–248 · 36 → 250–251 · 37 → 253–270 · 38 → 275 · 39 → 277 · 40 → 319 · 41 → 320–321 · 42 → 331–335 · 43 → 338–339. NOT LANDED: none.

## Folds proposed
For the desk, each HOLDS finding as one text change to `32` (the hub edits nothing; none was marked as blocking the launch):
1. STEP-6.8 (C4, `32` line 321): after "`ls -la …/stacked-0923/.env` → "No such file"." → add "Any of the three differing → record `R119 live-note: NOT RUN — gate worktree <line>` and go to STEP-7; never STEP-5."
2. RELAUNCH (i) (C9, line 188): "Anything else → bootstrap nothing, end `FAILED: relaunch — revert state unresolved …`" → "HEAD = `<reland>` and `diff --stat a0ba0098 HEAD -- . ':(exclude)docs'` EMPTY → bootstrap residents, end `FAILED: relaunch — STEP-5 revert interrupted; new code kept …`; anything else → as written".
3. RELAUNCH (ii) (C8, line 189): "if `pre-reland-0924` is absent, run R.3 first" → "if no snapshot id is recorded run R.2, and if `pre-reland-0924` is absent run R.3, first".
4. 4.7 (e) (C11, line 241): "EXCEPT that the incident kind (`failed_stage evaluate`) is judged by (h), not here" → "EXCEPT that the incident kind (`failed_stage evaluate`) is allowed on the FIRST read only and judged by (h)".
5. 4.7 (h) (C10, line 244+): add a fifth call after the curl: "`grep -c "Traceback" …/radar.err` = `<tr0>` and `grep -c "TaxonomyConfigError" …/radar.err` = `<tc0>`" (both `grep *`).
6. 4.7 first call / (h) (C3, lines 225, 247): "`grep -c "radar panel FAILED"` → `<rp_up>`" → also `grep -c "radar pool refresh FAILED" …/aset.err` → `<rpr_up>` at `<t up>`, and equal in (h).
7. STEP-7 item 3 / L76 line (C16, lines 95, 333): "`cobalt_dev` untouched by this run" → "`cobalt_dev` written only by STEP-6.3's dev-vault proof (`vault_writes` rows)".
Opus's own three folds (its closing line) are recorded verbatim in the Per question table; the hub's file-check did not confirm the first (C1) and rates the second (C2) UNVERIFIABLE, so they are not among the 7 above.

## ESCALATE
- ASK DESK: Grok ran ≈14 min (launched 12:57, exit 0 at 13:11) against the 12-min clock, and the hub could not stop it (no poll, no Monitor by rule). Count it as answered? [13:14] Safe default: counted as answered — `houses: 2 of 2`, the L67 floor met with its closing `REVIEW: RUN IT`; the overrun is recorded here.
- `R__L` is unfilled in `32` (lines 7, 64, 66, 158) at 13:1x; the desk fills it, and writes `RE-LAND: a0ba0098 by revert of 4e625f6a` on the launch row (the desk file's `log -S` for that literal is empty now).
- HUB DEVIATIONS from the UNATTENDED RULES (facts, not judgement): several calls used a pipe or a loop (`| cut`, `| head`, `| wc -l`, `| cat`, one `for … do … done` count loop), one `cd` into the scratch folder persisted (the launches ran after a `cd` back to `/Users/cobalt/cobalt-wt/agy-trial`), the packet's greps are counts (not full outputs) and its `scoring.py` cuts come from `grep -n -B5 -A5` on two files. The hub ran no production command, no `launchctl`, no `curl`, no database call, no `pytest`, no `uv run`, no git write, and wrote nothing but this report and the scratch folder.
- The hub read the desk file `cto-2026-09-24.md` and `32`/`09`/`31`/`deploy-2026-09-24.md`, LAWS.md in full, and the code (`radar/runner.py`, `radar/store.py`, `heartbeat/probes.py`, `db_migrations/cli.py`) at `a0ba0098` via the worktree `stacked-0923` (its `status` printed only `## deploy/stacked-0923`).

SETUPS DEPLOY RELAND REVIEW DONE · houses: 2 of 2 · blockers: 0 · folds: 7
