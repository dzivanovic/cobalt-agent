BLIND: I did not read the round-2 folder or the round-2 hub's report.
RUN DATE: 2026-09-23 (started 10:13 EDT)

## DIGEST FOR THE DESK

- `TRIBUNAL R2: BUILD`
- R2-1: NEITHER — only the voice module deletes: `finally:` + a delete-ALL sweep on `com.cobalt.aset` start. The heartbeat probe only counts (B's per-turn sweep dropped; B's age-gated start sweep missed a fresh crash).
- R2-2: NEITHER — S's span binding and span refusal kept; "takes no retry" moves to design only; "remains his" made operational (never merged, no merge baseline).
- R2-3: ADOPT A — new slice **V5 — trading-logic drafts** (after V1, parallel to V2/V3, 4 h): owner-drafted settings file + sha256 → `send_dm`; applied only by `cobalt settings load … --sha256 … --apply`. Before V5: `unsupported`, not `refuse`.
- Withdrawn from round 1: 4 (R2-1: 1 · R2-2: 2 · R2-3: 1).
- Experiments: X20 (engine re-opens a scratch file by path? — decides nothing under this ruling; would matter only under A), X21 (the O1 op writes no merge baseline), plus an E7 sharpening (kill < `scratch_max_age_s` after upload).
- OWNER: 1 (optional L7/L28 question on voice-applied settings; not a precondition).
- ESCALATE: 2.

## Rulings

### R2-1 — who deletes crash-leftover scratch audio (written 10:1x EDT, before any R2-2 read)

SELF-ATTACK (my seat's T8, `voice-v3-tribunal-fable-r1-2026-09-23.md:73-74`):
- Walked: every deleter under `src/cobalt` (`unlink`: `archiver/shadow.py:234`, `redact/secrets.py:280`, `vaultwrite/writer.py:478`, `radar/collector.py:108`, `backup/restic.py:197`; `rmtree`: 0; `scratch`: one comment, `generated/cli.py:100`). Each deletes ONLY its own module's staging/cache (`restic.py:193-197` its own dump; `collector.py:95-109` its own cache). The heartbeat deletes nothing and imports no voice/scratch module (`heartbeat/runner.py:41-60`). `com.cobalt.heartbeat` is a ONE-SHOT job every 900 s (`heartbeat.plist:6, :60-61`), not a resident; `com.cobalt.aset` is `RunAtLoad` + `KeepAlive` (`aset.plist:6, :40-43`).
- STRONGEST FAILING SCENARIO against my own B: 14:02:00 turn `t-A` writes `t-A.webm`; 14:02:10 `com.cobalt.aset` dies (`kill -9`) mid-transcribe; launchd `KeepAlive` respawns it ≈14:02:20; B's start sweep deletes only files older than `scratch_max_age_s`, which the cross-key forces ABOVE `stt_timeout_s`, so the 20-second-old leftover SURVIVES the start sweep. B's next deleter is "the start of every turn" — he does not press the widget until the next morning, so the audio of a turn that never transcribed sits ≈18 h. B as written misses exactly the crash it exists for.
- The fix needs no second process: §5 names the upload handler "the voice module's one file writer" (`VOICE-v3-derived-2026-09-23.md:107`) and the turn's `finally:` unlinks on every in-process exit (`:107`), so at a fresh `com.cobalt.aset` start NO file in `scratch_dir` can belong to a live turn → the start sweep deletes EVERY file, age unused. With that, B's per-turn sweep covers nothing the `finally:` does not (its only residue — an unlink that already failed RED — fails again the same way) → dropped as the larger mechanism.
- My round-1 line "the turn fails for a reason nobody can see" is not borne out → WITHDRAWN below.

NEITHER — replace §5's "Crash leftovers" bullet (`VOICE-v3-derived-2026-09-23.md:108`) with, verbatim:
> **Crash leftovers:** the voice module is the ONLY deleter of scratch files (L40): the turn's `finally:` unlink above, and on every `com.cobalt.aset` start, before the first request is served, a sweep that deletes EVERY file in `scratch_dir` — the upload handler is the directory's one writer and no turn of a new process is live, so any file present is a leftover; one AMBER line per file deleted (a turn died mid-way), an unlink that fails is RED (L9). One unlink function serves the turn and the sweep (L3). The heartbeat's `voice_scratch` probe (V4) only COUNTS: files older than `scratch_max_age_s` → AMBER with the count and the oldest age; none → OK; `scratch_dir` unreadable → RED unknown; it never deletes. The probe resolves `scratch_dir` through the voice config's one loader; the production override is carried in `ops/com.cobalt.heartbeat.plist` exactly as `COBALT_VAULT_PATH` is (`:20-21`) and in `ops/start_aset.sh` (`:32`), and the probe's detail line names the directory it read. The config schema refuses `scratch_max_age_s ≤ stt_timeout_s` (L10).

- (a) No. The heartbeat is a separate launchd job (`heartbeat.plist:6`) and would be DOING the side effect, not asking the owner (L38/L40: "others reach it by asking, never by doing"); one function called from two processes is two actors of one side effect, not one expert — L40 counts roles, not functions.
- (b) Required under A (a foreign process can only judge "dead" by age: a clip written 14:02:00 and still transcribing when a beat fires at 14:15:00 is unlinked if its age exceeds `scratch_max_age_s`; whether the turn then fails is X20). Under this ruling it guards no deletion — the only deleter in a live process is the turn's own `finally:` — it stops a config that would AMBER every long live clip (L10, L9 false alarm). Kept, one validator.
- (c) Under this ruling a leftover exists only after `com.cobalt.aset` died mid-turn; `KeepAlive` (`aset.plist:42-43`) respawns it and the start sweep deletes the file within the respawn time, whether or not he presses the widget for a day. R18 (b) binds it by its purpose ("live only minimal time"), and this meets it. If `com.cobalt.aset` stays DOWN (booted out, crash-looping), the file sits — and `sheet_http` is already RED (`runner.py:133`) and `voice_scratch` AMBER. Under A the heartbeat would need what it has none of today: the voice config loader, the scratch-dir resolver and its production override in its own plist, an unlink, and a guess of liveness by age (`runner.py:41-60` imports; greps: no deleter in `heartbeat/`).
- (d) V4's `voice_scratch` probe COUNTS only (text above). §9's V4 row: "`voice_stt` / `voice_plan` / `voice_scratch` probes (`voice_scratch` COUNTS files older than `scratch_max_age_s`, never deletes — §5); the production `scratch_dir` override in `ops/com.cobalt.heartbeat.plist`; …" · Depends on: "V1" (drop "R2-1 for …") · RESTARTS: `com.cobalt.heartbeat` reload (plist in the diff, L42) — it is a 900 s one-shot job (`heartbeat.plist:60-61`), not a resident · h: 2, unchanged. V1's sweep becomes "delete all on start" — the same lines as an age sweep, minus the age test; V1 h unchanged.
- Reasoning: every deleter on main deletes only its own module's files (`restic.py:193-197`, `collector.py:95-109`); the heartbeat deletes none (`runner.py:41-60`). The owner process knows no turn is live at its own start; a foreign process can only guess by age, and age is exactly what fails for a fresh crash (scenario above) or a long clip (b). Price vs the derived bullet: V1 ±0 lines; V4 ≈ +1 plist key (it needs the path under either position), no deleter in `heartbeat/`.

WITHDRAWN (R2-1): "and the turn fails for a reason nobody can see" (`voice-v3-tribunal-fable-r1-2026-09-23.md:74`) — the sweep text in the same bullet (`:73`) logs AMBER per deleted file, so the deletion IS seen; whether an unlink mid-transcribe fails the turn at all depends on whether the engine re-opens the file by path, which no file shows (X20).

### R2-2 — O1's fold MECHANICS (scope is his, not asked)

SELF-ATTACK (my seat's (g) / T6, `voice-v3-tribunal-fable-r1-2026-09-23.md:62-64, :161-162`):
- Walked: `_commit` (`writer.py:457-479`, the only caller `:527`), `_write_with_retry` (`:530-554`; callers `:773` upsert_unit, `:974` upsert_region, `:1250`), `_seed_baselines` (`:611, :621-641`), `upsert_unit` (`:644-805`), `_baseline` (`:807-810`) → `store.last_after` (`vaultwrite/store.py:91-103`: `SELECT unit_after … WHERE note, section, unit`), `_merge_base` (`:817-892`), `upsert_region` (`:895-989`), `merge3` (`merge.py:15-17, :100-121`).
- ATTACK 1, "takes no retry" (strongest against my text): the retry re-snapshots and re-runs `build(snapshot)` (`writer.py:535-537`). If the span check (the span still equals the confirmed before bytes) runs INSIDE `build`, attempt 2 either writes exactly the span he heard or refuses — never "a diff he never heard". Scenario: 16:40:12 "yes"; the writer snapshots at `:536`; Obsidian autosaves his typing in the DRC's risk paragraph before `:464`; attempt 1 aborts `NoteChangedOnDisk`. With "no retry" the confirmed edit is REFUSED and re-read for nothing; with the in-`build` span check the retry lands the confirmed span and carries his new risk line verbatim. So "takes no retry" is one lawful mechanism (the derived `[F-09]`, settled, not re-opened), not an invariant — it does not belong in the LAW. My T6 sentence is conditional on the check sitting outside `build`, and is withdrawn as a law requirement (below).
- ATTACK 2, "The replaced text remains his: no Cobalt ownership of it is created" — is it inert? NO, and my round-1 text did not say how: every `VaultWriter` versioning row keyed by (note, section, unit) IS the next merge's baseline (`writer.py:963-969` `unit_after` → `store.py:91-103` → `_baseline` `:807-810`). Scenario with an O1 write done through `upsert_region`'s merge: 10:00 voice edit A→B confirmed (row `unit_after = B`); 12:00 he types C over it by hand; 16:00 he says "change it to D", read-back "C → D", "yes"; `merge3(base=B, human=C, cobalt=D)` keeps C and records an override "human kept" (`merge.py:16-17, :110-121`) — he heard C → D, confirmed, nothing changed, and the audit says he insisted on C. And past 30-day retention the baseline is gone (`store.py:96-97`) and the same call lands D — behaviour depends on the age of an earlier voice edit. My sentence is RIGHT in intent and too vague to build: the fold must say "never a three-way merge; records no merge baseline".
- ATTACK 3, "refused if the span changed since the read-back": `_commit`'s guard is whole-file sha + mtime over the writer's own snapshot → commit window only (`:463-464`), milliseconds, not read-back → confirm. So the span refusal is needed for the confirm window and holds; tightened to "when the writer reads the note to write" so it rides inside the guarded attempt.

NEITHER — the fold text below: S's span binding and span refusal kept; "takes no retry" moved out of the law (design `[F-09]` keeps it); "remains his" made operational (replace, never merge; no merge baseline).

- (a) Span. 16:40:05 read-back of `lesson` in the DRC; 16:40:07–:09 he types a line in the risk paragraph and Obsidian saves; 16:40:12 "yes". P (whole diff / note) refuses and re-reads — and a unified diff carries `@@` line numbers, so a line typed ABOVE the target changes "that one diff" too; every confirm while he types is refused. Span: writes, his risk line untouched. He edits the TARGET itself at 16:40:08 → the span no longer equals the before bytes → refused under both. Span-bound is correct and costs nothing: `[F-09]` already builds it for every confirmed act.
- (b) "Span changed", not "note changed". `_commit` (`writer.py:463-464`) refuses ANY byte or mtime change, but only between the writer's snapshot (`:536`) and its commit (`:527`), then re-builds once (`:547-552`); it does not see read-back → confirm. The law's refusal covers that window at span level, checked in the writer's `build` on each attempt.
- (c) Design only (§2.6 `[F-09]`). The law states the invariant — only the confirmed after bytes, only over the confirmed before bytes, checked under the mtime guard — and the retry is a mechanism under it (Attack 1).
- (d) NOT inert. Every versioning row keyed (note, section, unit) becomes the next merge's base (`writer.py:963-969`, `store.py:91-103`, `:807-810`); through a merging op a later confirmed edit loses to his intermediate hand edit and is logged as HIS override (Attack 2). The sentence changes behaviour only once it says "never merged; no merge baseline". The versioning row then carries before/after with no (section, unit) key that `last_after` reads — as `create_if_absent`'s row does (`writer.py:603-604`, `section=None, unit=None`). X21 proves it.
- (e) The law may state LESS than the design builds (the retry, `target_sha256`'s exact inputs, the TTL stay in §2.6), but it must state every invariant a later design could break without it: span binding, span refusal, no merge / no baseline. P states none of the three; S states the first two and the third vaguely.
- Reasoning: `merge.py:16-17` (human-only change kept) plus `writer.py:963-969` (a baseline recorded on every region write) is the failing path P leaves open, and S's vague wording does not close it. Price: the O1 op in V3 = an `expect_before` mode on `upsert_region` (refuse inside `build` unless the span equals `expect_before`; write the after bytes with no `merge3`; versioning row with no unit key) ≈ 25 lines + 3 tests (edit-elsewhere lands, edit-the-target refuses, X21) in `vaultwrite/writer.py`, +1 h on V3, no new writer (L3). Only if he rules A or B.

FOLD A: [amended <date>] VOICE-ORDERED EDIT. Cobalt may replace text outside marker units — his text included — only when he orders that exact edit in a turn of Cobalt's voice / text widget and confirms it: the widget reads back note, field and before → after; his confirmation (a word from the closed confirm list matched by code, or a tap) is bound by sha256 to that target span's before and after bytes; the write goes through `VaultWriter` with the mtime guard and `_session_gate`, replaces only that span with the confirmed after bytes — never a three-way merge — and is refused if, when the writer reads the note to write, the span no longer equals the confirmed before bytes; it is versioned with before, after and the turn id, and records no merge baseline: the replaced text remains his, and every later write treats it as his. The trader is in the loop (as for `cobalt settings load --apply`, 2026-09-15); this is not a model-judged approval (L37). Nothing else in this law changes.

FOLD B: [amended <date>] VOICE-ORDERED EDIT. Cobalt may replace text HE wrote — outside marker units, or inside a unit whose body is his (his voice units) — only when he orders that exact edit in a turn of Cobalt's voice / text widget and confirms it: the widget reads back note, field and before → after; his confirmation (a word from the closed confirm list matched by code, or a tap) is bound by sha256 to that target span's before and after bytes; the write goes through `VaultWriter` with the mtime guard and `_session_gate`, replaces only that span with the confirmed after bytes — never a three-way merge — and is refused if, when the writer reads the note to write, the span no longer equals the confirmed before bytes; it is versioned with before, after and the turn id, and records no merge baseline: the replaced text remains his, and every later write treats it as his. The trader is in the loop (as for `cobalt settings load --apply`, 2026-09-15); this is not a model-judged approval (L37). Nothing else in this law changes.

WITHDRAWN (R2-2): (1) "takes no retry", as LAW text (`voice-v3-tribunal-fable-r1-2026-09-23.md:161`) — `writer.py:535-537`: the retry re-runs `build`, so with the span check inside `build` it cannot write bytes he did not hear; it stays a design choice (`[F-09]`), not law. (2) "and keep the text his, so a later Cobalt merge never treats it as Cobalt's baseline" (`:162`) — as a description of what the (g) fold text DOES: the fold said nothing that stops a baseline being recorded, and `writer.py:963-969` + `store.py:91-103` record one on every region write.

### R2-3 — which slice builds the trading-logic draft-and-send tool

SELF-ATTACK (my seat's T4 (2), `voice-v3-tribunal-fable-r1-2026-09-23.md:53, :55`):
- Walked: `grep hitl` → `radar/propose.py:1, :740-741, :750`, `radar/cli.py:99`, `settings/card.py:15`, `cards/shadow_report.py:20, :132`, `daymode/propose.py:118`, `db_migrations/placement.py:135`; `grep trading_logic` → 0; `send_dm` → defined `notify/mattermost.py:130`, only caller the heartbeat (`heartbeat/runner.py:299-304, :372, :483`); `trader_settings` → 19 files, write path `settings/cli.py` (`load --card|--optional <file> --sha256 <hash> --dry-run|--apply`, `:4-7, :84-105, :182-209, :304-319`).
- AGAINST R18 (a): 11:30 he says "turn radar cards on". My text returns `refuse` — the HARD-REFUSAL kind (derived `:72`) — "until a slice builds the HITL-card tool", and my round-1 named NO slice, so the refusal is permanent by construction: a class refusal. Not an answer open to me; it is not re-proposed.
- AGAINST ITS OWN LAW CITE: my refusal sentence "naming L7" is wrong for the very keys a voice request would touch first. The five card keys (`settings/card.py:4-8`) are applied today by HIS OWN hand, sha-exact, EXEMPT from the HITL token (`settings/card.py:10-16`; L28 amended 2026-09-15). Refusing them "per L7" tells him a law blocks what his own command does lawfully every day. WITHDRAWN below.
- What holds from the files: §9 builds no draft tool (`VOICE-v3-derived-2026-09-23.md:158-161`; `trading_logic` 0 hits); no V1–V3 tool writes `trader_settings`, `configs/`, rules, strategies or setup definitions (§9 rows).

ADOPT A — slice **V5 — trading-logic drafts** (depends on V1; runs in parallel with V2/V3, L72; no dependency on O1):
- Targets: the `"user".trader_settings` keys that the existing sha-exact files load — `--card` (`settings/card.py:4-8`) and `--optional` (`settings/cli.py:7, :84`). Each is a registry tool marked `trading_logic: true` (L16) with a closed candidate list of keys and a deterministic parser per value type. A nested value (`card.curves`) is addressed one anchor at a time, else `clarify`.
- The act (a confirmed ACT, §2.6: read-back "draft <key>: <before> → <after> and send it to you — say yes"): voice ASKS the owner — a new `settings` function writes the draft file in the owner's own format to `data/voice-drafts/` (gitignored, `.gitignore:6`; the `data/radar-proposals` precedent, `radar/propose.py:433-438`) and returns (path, sha256 of the file's bytes, the per-key diff its `--dry-run` computes). Voice then ASKS `notify.send_dm` (F19-redacted, `mattermost.py:13-15, :140`) with the per-key before → after, path, sha256 and the owner's exact command `cobalt settings load --card|--optional <path> --sha256 <hash> --apply`. `send_dm` returning not-sent (`mattermost.py:147-152`) → the turn says so, AMBER (L9). Nothing is applied.
- After: the change is applied ONLY by the owner's existing command, under its existing gates (the byte-exact hash before parsing, a per-key diff, one `put`, a round-trip read — `settings/card.py:10-16`; refused in `market_reset`, L66). Either he runs it by his own hand (trader-run, token-exempt, L28 amended 2026-09-15), or he says "approve" in the desk chat for that file and sha256 and the desk runs it (L7 as it stands). Voice runs neither.
- Every other `trading_logic` target (setup definitions, rules, strategies, configs in git; radar notes) — until its own registry entry names an owner draft function: Plan kind `unsupported` (NOT `refuse`). The reply is code-rendered and names the owner path (the radar `propose` → `apply --hitl --sha256`, `radar/propose.py:711-760`; a config change = a build and a deploy with his L7 approve, as `cto-2026-09-23.md` R11 (c) did for setup definitions today). A new target is a registry entry plus that owner's draft function — no voice code change.
- Files: `src/cobalt/settings/` (the draft function; ≈40 lines reusing the load/diff path), `configs/cobalt/agents/voice.yaml` (the entries), the voice tool module (≈60 lines: candidates, parsers, the two asks), tests (the draft's sha256 equals what `--dry-run` prints for the same file; the draft writes no DB row; `--apply` with that sha on `cobalt_dev` lands the diff; `send_dm` disabled → AMBER, nothing applied; a nested curve without an anchor → `clarify`). Migration: none. **Hours: 4 (GUESS, same factor as §9).** **RESTARTS (L42, derived at build):** `com.cobalt.aset`, plus every resident whose entrypoint imports `cobalt.settings` by static AST walk (`aset/config.py`, `radar/evaluate.py` touch it → `com.cobalt.radar` unless the walk proves otherwise).
- BEFORE V5 SHIPS (V1–V4): a trading-logic request gets Plan kind `unsupported` with the same code-rendered reply naming the owner command. That is the same kind every function not yet built gets in an earlier slice (e.g. a DRC request in V1), and §9 commits the slice. It is not the permanent `refuse` class that R18 (a) forbids. Orders and the trading platform stay `refuse` (`:72`).
- Replace the derived `[F-07 — OPEN, R2-3 …]` note (`:72`) with: "Built in V5 (§9); before V5, `unsupported` with the owner's command named." Add §9 row: "| **V5 — trading-logic drafts** | `trading_logic: true` registry tools for the `--card` / `--optional` settings keys; owner-drafted file in `data/voice-drafts/` + sha256 + per-key diff; `send_dm` card with the owner's exact apply command; applied only by `cobalt settings load … --sha256 … --apply` (his hand, or the desk on his L7 approve) | no (a gitignored draft file; the owner's own apply is unchanged) | — | V1 | `com.cobalt.aset` (+ residents importing `cobalt.settings`, AST walk) | 4 |"; totals 26 h, ≈36 h at ×1.4 (UNVERIFIED).
- Reasoning: the owner's sha-exact apply already exists (`settings/cli.py:4-7`; `settings/card.py:10-16`), and so does the outbound card (`mattermost.py:130`). V5 adds only a draft function in the owner module and registry entries — no new gate and no new approval path (L3, L37). It is the smallest slice that makes "any command" true for the settings he actually changes.

L40: APPLYING a trading-logic change is NEVER a voice act. The voice module only asks the owner to draft, and asks `notify` to send. Owners: `"user".trader_settings` → `settings/cli.py` `cmd_load` / `cmd_load_optional` (`cobalt settings load … --sha256 … --apply`, `settings/card.py:10-16`); radar notes → `radar/propose.py:711-760` `apply`; setup definitions, rules and configs in git → a build and a deploy (the desk's lane, his L7 approve).

WITHDRAWN (R2-3): "is `refuse`d aloud with a fixed sentence naming L7" (`voice-v3-tribunal-fable-r1-2026-09-23.md:53`), as applied to `"user".trader_settings` — `settings/card.py:15-16` + L28 (amended 2026-09-15): his own sha-exact apply of those keys is token-exempt, so L7 is not the reason such a request cannot run by voice.

## Withdrawn from round 1

Four sentences, counted. The two round-1 WITHDRAWN lines (`voice-v3-tribunal-fable-r1-2026-09-23.md:190-191`) stay withdrawn.
1. "and the turn fails for a reason nobody can see" (`:74`) — the same bullet's sweep logs AMBER per deleted file (`:73`). Whether an unlink mid-transcribe fails the turn at all is X20, not a fact.
2. "takes no retry", as LAW text (`:161`) — `vaultwrite/writer.py:535-537`: the retry re-runs `build(snapshot)`, so with the span check inside `build` it never writes bytes he did not hear. It stays design (`[F-09]`).
3. "and keep the text his, so a later Cobalt merge never treats it as Cobalt's baseline" (`:162`), as a description of what the (g) fold text does — the fold stated nothing that stops a baseline being recorded. `writer.py:963-969` → `vaultwrite/store.py:91-103` → `writer.py:807-810` record and read one on every region write.
4. "is `refuse`d aloud with a fixed sentence naming L7" (`:53`), for `"user".trader_settings` — `settings/card.py:15-16`, L28 (amended 2026-09-15): his own sha-exact apply of those keys is token-exempt.

## Experiments (L70)

Cited, not restated: E1–E11, X1–X5, X12, X13 (derived `:210-241`).
- E7 (sharpened, R2-1): include a `kill -9` of `com.cobalt.aset` LESS than `scratch_max_age_s` after the upload, with no later turn. Pass: the respawned process's start sweep deletes the file, and one AMBER line names it. Fail (file survives) → the start sweep is age-gated, contrary to R2-1.
- X20 (R2-1, informational): on the home server, the chosen engine transcribing a long synthetic clip, `unlink` the scratch file mid-transcribe. Does the transcript complete (the engine holds an fd) or fail (it re-opens by path)? It changes nothing under this ruling, which has no foreign deleter. It would set the cost of position A.
- X21 (R2-2, only if he rules O1 A or B): dev vault, through the O1 op the builder ships — voice edit A→B confirmed; B→C by hand; voice edit "C → D" confirmed. Pass: D on disk, no `vault_overrides` row, and `_baseline(note, section, region)` returns None for the O1 rows. Fail (C kept + override, or a baseline returned) → the op merges, contrary to the fold text.

## OWNER

- L7 / L28, a law he owns, not a precondition (V5 builds draft-and-send either way): may his code-matched voice "yes" on a read-back of a `"user".trader_settings` change APPLY it through the owner's sha-exact `cobalt settings load … --apply`, exactly as his own-hand apply does today (token-exempt, L28 amended 2026-09-15)? Today: no — the draft goes to him and he or the desk applies it. No house can decide this, because it changes where an L7 approval happens.

## READING

- Authorization (10:13 EDT): FABLE ROW count 1 · `cto-2026-09-22.md:56` R109 carries `Make all Opus 5.5 for now` + `claude-opus-5-5` · committed `75b2aa5` · `cto-2026-09-23.md` R109 hits :4, :23, :24 (R21 "09-22 R109 … STANDS"), :36, :38, :40 — all cite, none ends or changes it · session model `claude-opus-5-5` = R109 · R18 `:21` carries `VOICE v3 DIRECTIVE` + `Solve this` · R36 `:39` carries `needs round 2: 3` · derive stop + derived design committed `fc82bfa` · launch row R37 `:40` names `25` · 7 allow + 3 deny strings each count 1 in `22-draft-setups-tribunal.md`.

- LAWS.md 1–442 (full).
- `prompts/2026-09-23/25-…fable-seat.md` (whole); `24-voice-v3-tribunal-r2.md` :128–197 (§1 (2)–(8), including the `QUESTIONS-R2.md (verbatim)` paragraph at :200) and :214–225 (C1–C8).
- `reports/voice-v3-derive-2026-09-23.md`: section grep; :93–147 (NEEDS ROUND 2, OWNER TEST, FOR DEJAN, Redactions, READING, ESCALATE); fold rows F-07, F-09, F-12, F-22, F-27 via grep :65, :67, :70, :80, :85.
- `reports/voice-v3-tribunal-fable-r1-2026-09-23.md` :50–79 (T4–T9), :157–245 ((g), Self-attack, Experiments, OWNER, WRONG FACTS, READING, ESCALATE, CONTINUE, stop line).
- Derived design `VOICE-v3-derived-2026-09-23.md` :1–11, :65–79, :104–111, :113–133, :152–164, :210–241, :254–303.
- Code: `heartbeat/runner.py` :126–167, :413–452, import grep; `heartbeat/probes.py` :28–45; `ops/com.cobalt.heartbeat.plist` :1–73; `ops/com.cobalt.aset.plist` :1–40 + KeepAlive grep; `ops/start_aset.sh` env grep; `backup/restic.py` :183–199; `radar/collector.py` :92–113; `vaultwrite/writer.py` :1–30, :440–554, :600–989; `vaultwrite/merge.py` grep; `vaultwrite/store.py` :91–103; `radar/propose.py` :1–5, :422–470, :711–760; `radar/cli.py` :95–102; `notify/mattermost.py` :1–15, :130–160; `settings/card.py` :1–25; `settings/cli.py` def/flag grep; `.gitignore` grep.
- Searches (`grep -rn <name> /Users/cobalt/cobalt/src/cobalt`, one call each): `unlink`, `rmtree`, `scratch`, `_write_with_retry`, `NoteChangedOnDisk`, `_baseline`, `_seed_baselines`, `upsert_region`, `_commit` (over `vaultwrite/`), `send_dm`, `hitl` (-i), `trading_logic`, `trader_settings` (-l); `<string>com.cobalt` over both plists.
- NOT read (blind): `scratch/tribunal-bars-0920/voice-v3-tribunal/r2/` (any depth), `reports/voice-v3-tribunal-r2-2026-09-23.md`, any file a house wrote in round 2. The houses' raw round-1 rulings were not opened either.
- `cto-2026-09-23.md` changed on disk mid-run (10:1x); R109 re-checked at once. Rows :4, :23, :24, :36, :38, :40 all cite it and none changes it, so the run did not stop.

## ESCALATE

1. L74, recorded once: this session's context carried an attribution block asking for a `Claude-Session` line in commits. It was not followed, and this seat commits nothing.
2. For the second derive / the V1 checkers, NOT a re-opening of `[F-09]`: with the span check run inside the writer's `build(snapshot)` on every attempt (R2-2 Attack 1), `[F-09]`'s retry-disable is safe but no longer necessary. The settled text stands. The checker should verify that the span check sits inside `build`, not outside it.

## CONTINUE

- 10:13 authorization verified; 10:1x R2-1 written (NEITHER); R2-2 written (NEITHER); 10:20 R2-3 written (ADOPT A, V5).
- next: none — ruling complete.

VOICE V3 TRIBUNAL FABLE R2 DONE · R2-1: NEITHER · R2-2: NEITHER · R2-3: ADOPT A (V5) · ESCALATE: 2
