# Ops — 2026-09-15

MODEL: Opus 5 (`claude-opus-5`) · SEAT: ops builder · SESSION: fresh · auto mode on · METER: Anthropic medium
Branch `ops/2026-09-15` in `/Users/cobalt/cobalt-wt/ops-2026-09-15`, off main 0ed37f5 (main has since moved +1 docs-only commit, bc6508a — rebase at merge). Prompt: `docs/40 - DevDocs/prompts/2026-09-15/02-ops-2026-09-15.md`. Revised after Dejan's five rulings of 09-15 (§R); one amended commit (L46).

## §0 Headline

- LANDED: 1 radar flap fix · 2 Rules.md glued-tag tolerance · 3 email channel retired (source removed; git history keeps it) · 4 `.claude/settings.json` committed + proven · 6 RESTARTS `DOCS` rule (L42 O9) · 9 Qwen private stores → pointers · `no_resident_reads` table in jobs.yaml (ruling 2). REPORT-ONLY: 7, 8, 10, 11.
- OUT: 5 — Grok still running (`52628 grok`) at the ruled check, 09:28.
- Suite: `uv run pytest tests/cobalt tests/taxonomy -q` → **1059 passed, 238 skipped**.
- RESTARTS: `com.cobalt.aset com.cobalt.radar` (derived, no ESCALATE — matches ruling 2).
- ESCALATE: 1.

## §R Rulings applied (Dejan, 2026-09-15)

| # | ruling | done |
|---|---|---|
| 1 | `git rm` the email files moved under `docs/_archive/…`; keep only the README section | `git rm -r docs/_archive/email-channel-retired-2026-09-14` (3 .py sources, 1 test, 3 DevDoc pages). `_archive/README.md` section kept, reworded: source is never archived under docs/; `git show 0ed37f5:<path>`. Every reference to the archive path now says that (BACKLOG, `ops/README.md`, `heartbeat/runner.md`, `heartbeat/__init__.md`, `notify/__init__.md`). |
| 2 | RESTARTS = aset + radar; add notify.yaml to the jobs.yaml reads table | A per-job `reads:` is resident-only by schema — a one-shot row is refused (`config.py`, "`reads` belongs to a RESIDENT"). So the table is a registry-level **`no_resident_reads`** list (`path`, `readers`, `because`). The schema refuses unregistered labels, resident readers, paths a resident already reads, and absolute/`..` paths. `cobalt jobs readers` → exit 0 + `RESTARTS: none`; `cobalt jobs restarts` → `no resident reads (one-shot: …)`. The row names **`com.cobalt.heartbeat` only**, because code shows backup no longer loads notify config: callers are `notify/mattermost.py:137` (send_dm ← `heartbeat/runner.py:304`) and `cobalt validate` (`cli.py:261`). Tests first (below). |
| 3 | Qwen idle; guard `pgrep -fl qwen \| grep -vE 'pgrep\|lmstudio\|mlx\|mainframe'`; run 9; run 5 only if Grok idle | Check at 09:28: Qwen → `38672 node /opt/homebrew/bin/qwen --yolo` (+38673/38682) still listed, plus the unfiltered lines of the mainframe `bash -c` script (grep filters line by line; the multi-line cmdline leaks). Item 9 run on the ruling (Qwen idle since 08:23). Grok → `52628 grok` → item 5 OUT. |
| 4 | commit `.claude/settings.json` (Dejan-written) + proof | committed as written. Proof below. It needed a classifier rule: `.claude/` → `HARNESS; no Cobalt reader`, since it derived UNCLASSIFIED → all residents. Test first. |
| 5 | amend to one commit, re-run suite + derivation, re-issue READY | §Close |

## 1. Radar poll-failure flap — LANDED

| step | evidence |
|---|---|
| tests | (a) `test_radar_runner.py::test_carried_poll_failure_never_blinks_clean_between_put_pool_and_stamp_poll` — fixture = live `radar_pool` row, cto §1.2 (it carries no source ids). Asserts the row passed to `put_pool` has `failed_stage='bars'`, `failed_detail='poll failures: 1'` and the IMCC record; plus `test_clean_previous_row_still_writes_a_clean_pool_row`. (b) `test_radar_poller.py::test_carried_record_for_a_non_member_is_dropped_and_logged_once`. (c) `…_for_a_member_is_kept_until_its_fetch_clears_it` |
| RED before | `AssertionError: assert None == 'bars'` · `assert [PollFailure(ticker='IMCC', reason='stale', …)] == []` |
| change | `runner.py::_pool_row(…, existing_pool)` carries `poll_failures` and, when non-empty, `failed_stage='bars'` / `failed_detail='poll failures: N'`; both call sites pass it. Membership / pending-drop / mirror stages still override afterwards. `poller.py::BarPoller.poll` drops carried records whose ticker ∉ `members`, one INFO line per ticker: `radar poll: dropped carried failure record(s) for IMCC — not a member this poll (stale since 2026-09-14T13:38:07.287155+00:00)` |
| GREEN after | `40 passed in 0.61s` (radar runner + poller + restarts + rules_gen files) |
| DevDocs / BACKLOG | `radar/runner.md`, `radar/poller.md`; closed line added |

## 2. Rules.md parser tolerance — LANDED

| step | evidence |
|---|---|
| tests | `test_prefill_rules_gen.py::TestSplitTrailingTags` +4 — real rule 11 text in its glued form (`…never break-even.#process`) is peeled; `trade#process` → none; `break-even.#banana` → none; glued rule 11 parses |
| RED before / GREEN after | `2 failed, 18 passed` → `20 passed` |
| change | `rules_gen.py`: `_GLUED_TAG_RE = ^(.*[^\w\s#])#(\w+)$`, applied once to the last token and only for a tag ∈ `RECOGNIZED_TAGS` |
| Rules.md | untouched (read-only: line 14 now has Dejan's space) |
| DevDocs / BACKLOG | `prefill/rules_gen.md`; BACKLOG.md:258 ticket closed |

## 3. Email channel — RETIRED — LANDED

| scope item | done |
|---|---|
| `notify/email.py`, `notify/store.py`, `notify/cli.py`, `tests/cobalt/test_notify_email.py`, DevDocs `notify/cli.md` / `email.md` / `store.md` | removed from the tree (ruling 1); readable at `0ed37f5` |
| `notify/config.py` | `EmailConfig`, `NotifyConfig.email`, `GOOGLE_OAUTH_*` constants removed; docstring says why |
| `configs/cobalt/notify.yaml` | `email:` block removed. The comment says: retired 09-14 because Google Publish for `gmail.send` is gated on restricted-scope verification (Testing → 7-day refresh token). A leftover `email:` block now fails the load (`extra="forbid"`). |
| CLI | `cobalt notify` registration and the email lines of the `validate` sweep removed |
| probe / `out_of_band` | `email` probe removed; `out_of_band` and both call sites (transition, FINALIZE corrective) removed — alerts are DM-only; docstrings rewritten |
| tunables | `notify.email.auth_port`, `notify.email.timeout_s` removed |
| `cobalt_email_sends` | migration `src/cobalt/notify/migrations/0001_cobalt_email_sends.sql` and the `placement.py` side entry stay (the table exists); drop = separate HITL |
| tests amended | `test_heartbeat_runner.py` (stubs removed, asserts on `dm`, replay oracle = 1 message per event → 5 / 2), `test_sheet_daymode_probe.py` (also stubs `radar`: unstubbed, it read Postgres inside a scanning session — clock-dependent failure, same on main), `test_tenancy.py` |
| DevDocs | `heartbeat/runner.md`, `heartbeat/__init__.md`, `notify/__init__.md`, `notify/config.md`, `notify/result.md`, `notify/mattermost.md`, `redact/guard.md`; `ops/README.md` token-clock section → retired note |
| not changed | `configs/cobalt/redact.yaml` Google OAuth shape patterns stay while the values are still in the vault |

Evidence:

- `grep -rn 'email' src/cobalt --include='*.py'` → only `src/cobalt/db_migrations/placement.py:39:    "cobalt_email_sends": Side.SYSTEM,` (the table's side registration). Non-.py: the migration SQL named above and the `0002_move_tables*.sql` history.
- DM body: production `heartbeat show` was refused by the classifier (`[Production Reads]`), so the before/after below is a code render (`Beat.dm_body()`) using the real 04:12 beat. The hub reads the live shape after the merge.

```
=== BEFORE (main) ===                                  === AFTER (branch) ===
🔴 COBALT HEARTBEAT RED — 1 probe(s)                    🔴 COBALT HEARTBEAT RED — 1 probe(s)
2026-09-15 04:12:22 EDT                                2026-09-15 04:12:22 EDT
RED:                                                   RED:
  • radar: failed_stage bars: poll failures: 1; …        • radar: failed_stage bars: poll failures: 1; …
Green:                                                 Green:
  • database: cobalt_brain reachable                     • database: cobalt_brain reachable
ENTERED RED: radar                                     ENTERED RED: radar
Email sent — message 1a0a41fdaf775907                  (no channel-outcome line)
```

- Secrets — Dejan runs; nothing printed. Copy first: the old `VaultManager._save_vault` rewrites the file non-atomically.
  ```
  cd ~/cobalt && cp -p data/.cobalt_vault data/.cobalt_vault.pre-0915
  uv run python dev_utils/manage_vault.py      # COBALT_MASTER_KEY in env
  #   [4] → GOOGLE_OAUTH_CLIENT_ID → y · [4] → GOOGLE_OAUTH_CLIENT_SECRET → y · [4] → GOOGLE_OAUTH_REFRESH_TOKEN → y
  #   [1] names only, confirm gone. Never [2]. [5] exit.
  ```
- `~/.cobalt/google-oauth-client.json`: **ABSENT**.
- Ticket filed: `redact/secrets.py` `read_secret` / `put_secret` / `secret_names` have no caller left.

## 4. `.claude/settings.json` — LANDED (Dejan-written)

| step | evidence |
|---|---|
| file | committed as written: `additionalDirectories` `/Users/cobalt/cobalt-wt`, `/Users/cobalt/Vault`; `allow` = db query / heartbeat show (each also with a `COBALT_ENV=production` prefix), jobs list / restarts / readers, `launchctl print`, `git status` / `log` / `diff`. No `git push`, no `bypassPermissions`. |
| proof: listed read runs without a prompt | `claude -p "Use the Bash tool to run exactly …: git log --oneline -1 …" --model claude-haiku-4-5-20251001 --output-format json` from the worktree → `result: f72e71b ops(2026-09-15): …` · `permission_denials: []` · `is_error: False` |
| proof: `git push` still asks | same with `git push --dry-run` → `result: DENIED` · `permission_denials: [{'tool_name': 'Bash', …, 'tool_input': {'command': 'git push --dry-run', …}}]` (headless -p turns the ask into a denial) |
| classifier | `.claude/settings.json A HARNESS; no Cobalt reader -`. RED before: `test_claude_harness_settings_derive_no_restart` → `escalate=True` with all six residents |

## 5. Grok sandbox deny profile — OUT

| check | result |
|---|---|
| 08:42, 09:00, 09:28 (ruled filter) | `52628 grok` every time — not started; `~/.grok/sandbox.toml` untouched; no `ops/grok-sandbox.toml` |

## 6. RESTARTS classifier — documentation paths — LANDED

| step | evidence |
|---|---|
| test | `test_jobs_restarts.py::test_documentation_paths_derive_no_restart_and_are_labelled_docs` (`QWEN.md` + a docs/ report → `DOCS`, `()`, no escalate; `src/cobalt/cli.py` → still `com.cobalt.radar`) |
| RED before / GREEN after | `AssertionError: assert 'UNCLASSIFIED' == 'DOCS'` → green |
| change | `restarts.py`: `ROOT_DOCS`; a `docs/` path or root doc with no reader → `DOCS`, no restart (not even the conservative set) |
| report line | **L42 O9 clause now effective — hub folds the PENDING marker out of LAWS.md at close.** |

### 6b. `no_resident_reads` (ruling 2)

| step | evidence |
|---|---|
| tests | `test_jobs_reads.py::TestNoResidentReads` (notify.yaml declared with one-shot readers only; `./` prefix; resident reader refused; unregistered label refused; resident-read path refused) + `TestTheCommand::test_a_one_shot_only_path_exits_zero_with_RESTARTS_none`; `test_jobs_restarts.py::test_a_declared_one_shot_only_config_derives_no_restart`. The unknown-path tests now use an unlisted fake path instead of notify.yaml. |
| RED before | `AttributeError: 'JobRegistry' object has no attribute 'no_resident_read'` · `no_resident_reads Extra inputs are not permitted [type=extra_forbidden…]` |
| fix-along | `test_the_other_residents_declare_it_empty_deliberately` counted the substring `reads:`, which `no_resident_reads:` also matched → it now counts per-job `reads:` key lines (still 6) |
| live | `COBALT_ENV=dev uv run cobalt jobs readers configs/cobalt/notify.yaml` → `…has NO resident reader (jobs.yaml \`no_resident_reads\`)` · `re-read on every run by one-shot(s): com.cobalt.heartbeat` · `RESTARTS: none` · rc=0 |
| DevDocs | `jobs/config.md`, `jobs/cli.md`, `jobs/restarts.md` |

## 7. Phantom `*_scan_id` columns — PROPOSAL ONLY

Facts: `db_migrations/0004_radar_pool.sql:18` `radar_pool.last_scan_id BIGINT`; `:45-47` `radar_membership.opened_scan_id BIGINT NOT NULL`, `last_scan_id BIGINT NOT NULL`, `closed_scan_id BIGINT`; `:50` `UNIQUE (pool_key, opened_scan_id, ticker)`. No FK, no scans table. The id is `max(epoch_ms, last_scan_id + 1)` (`runner.py:171-173`): monotonic per pool, and it dates itself.

| option | for | against |
|---|---|---|
| **A — opaque ids** (document in `radar/store.md` + `db_migrations/placement.md`) | tribunal-grok settled "opaque scan ids, no scans table"; the id orders and dates itself; the UNIQUE key already makes an episode idempotent; zero added writes per 189 s cycle | nothing can answer "what did scan N see" — only the last cycle survives on `radar_pool` |
| B — `system.radar_scan` with the P2 seam tables | per-scan audit (sources, degraded, ms, poll failures) — L57 replayability | ~130 writes/day for a table with no consumer yet; mid-sprint schema change |

**Recommend A.** B stays open for later: a `system.radar_scan(scan_id BIGINT PRIMARY KEY, …)` keyed by the same bigint can take FKs without renaming a column. Put it in P2 only if a seam table needs per-scan inputs. No migration.

## 8. aset.err sheet-modes error 09-14 10:50 — trace

| check | result |
|---|---|
| `grep -n "2026-09-14 10:4\|2026-09-14 10:5" logs/aset.err` | 10 matches, all INFO: vault unlock + finviz token (10:48:38, 10:49:02); `3182 [WRITE] updated … unit=card-20260914T104905 · write_id=2561` (INTC SHORT B, `sheet_mode: half`, 193 sh); `3205 [WRITE] updated … unit=fill-20260914T104905 · write_id=2563` (fill 96.63 → 157 sh) |
| non-INFO lines dated 09-14 | none |
| heartbeat 10:53:33 | `OK sheet daymode day mode reduced · stage 2 (proposed, undecided — floor holds) · half sheet — cards are accepted`; RED were `radar` and `com.cobalt.herdr` |
| recurrence after 10:50 | one ERROR: `2026-09-15 07:16:59.534 | ERROR | cobalt.daymode.note:write:148 - daymode note: …/2026-09-15.md does not exist — the sheet-mode unit was NOT written.` — fail-loud downstream of prefill-daily's 05:15 `RulesSourceError` (fixed by item 2) |
| verdict | no error at 10:4x–10:5x; no bug, no ticket |

## 9. L56 private stores — Qwen — LANDED (ruling 3)

Backed up (`<name>.pre-0915`, sha256 verified) and replaced with the one-line L56 pointer (same text as the Claude Code MEMORY.md line, plus "copied into reports/ops-2026-09-15.md §9 on 2026-09-15"):

| file (under `~/.qwen/`) | was | Cobalt-relevant content for the close hub |
|---|---|---|
| `memories/MEMORY.md` | 156 B, index of one personal note | none |
| `memories/user/location.md` | 368 B, personal (user location) | **not Cobalt; not copied into this committed report.** The hub reads `memories/user/location.md.pre-0915` and files it in the personal layer if it is not already there. |
| `projects/-private-tmp-claude-501-qwen-proof/memory/MEMORY.md` | 0 B | none |
| `projects/-Users-cobalt-cobalt/memory/MEMORY.md` | 154 B, index: `- [Day-open sweep routine](project/day-open-routine.md) — Morning read-only heartbeat/market-reset diagnostic checklist; report goes to docs/40 - Dev…` | index only |
| `projects/-Users-cobalt-cobalt/memory/project/day-open-routine.md` | 1462 B | verbatim below |

```
name: Day-open sweep routine
description: Morning read-only heartbeat/market-reset diagnostic checklist; report goes to docs/40 - DevDocs/reports/day-open-<date>.md
type: project

Cobalt runs a "day-open" sweep each morning (~07:00 ET, local Qwen seat): a fixed checklist of VERBATIM commands — heartbeat row in `system.cobalt_jobs`, `system.session_blocks` refusal count (baseline 8 as of 2026-09-11, all from the 09-08/09-09 incidents), heartbeat.log/.err greps around the 20:00–21:00 ET market-reset window, `launchctl print`, archiver log, tree-state git checks — then a report to `docs/40 - DevDocs/reports/day-open-<date>.md` (the only file written that day).

**Why:** market_reset is a hard no-vault-write block 20:00–21:00 ET; on 09-08/09 the heartbeat died every in-window beat (SessionBlocked refusals, ~75-min holes). Fix 16847eb (09-10) makes beats ask the session and log `deferred_market_reset` instead. The sweep verifies it holds; anomalies become ESCALATE lines, not fixes — the sweep is strictly read-only (no commits, deploys, restarts, vault writes).

**How to apply:** when the user opens a day-open session, run each command verbatim; adapt only with disclosure (e.g. `\d <table>`, `ls logs/`); never write a figure not seen in output; `system.cobalt_jobs` holds one row per label (latest beat only) — never read overnight behaviour off it; `archiver.log` is empty by design, real archiver output is in `archiver.err`.
```

No vault memory file was written from here.

## 10. Qwen day-open allowlist — prepared, NOT run

`~/.qwen/settings.json` keys: `$version env hooks model modelProviders permissions security tools ui`; `permissions` = `{allow: [10]}`. Run after `ops/day-open` merges:

```
python3 -c 'import json,shutil,pathlib; p=pathlib.Path.home()/".qwen"/"settings.json"; shutil.copy2(p,p.with_name("settings.json.pre-0915b")); d=json.loads(p.read_text()); a=d.setdefault("permissions",{}).setdefault("allow",[]); r="Bash(uv run cobalt day-open *)"; (r in a) or a.append(r); p.write_text(json.dumps(d,indent=2,ensure_ascii=False)+"\n"); print("allow entries:",len(a))'
```

Expected: `allow entries: 11`.

## 11. RESTIC_PASSWORD rotation — procedure only (not run; no value printed)

Facts: `configs/cobalt/backup.yaml` → `ssd` (local `/Volumes/COBALT-BACKUP/restic`, `requires_mount`, enabled) and `b2` (`repo: ""`, disabled); `password_vault_key: RESTIC_PASSWORD`; `backup/restic.py:137` passes it as env `RESTIC_PASSWORD`.

```
0. Hub present. Not mid-backup, repo mounted:
   launchctl print gui/$(id -u)/com.cobalt.backup | grep -E "state|last exit"   → not running
   ls /Volumes/COBALT-BACKUP >/dev/null
   cd ~/cobalt && export COBALT_ENV=production   # COBALT_MASTER_KEY already in env, never echoed
   vault_get() { uv run python -c 'import sys; from cobalt.redact.secrets import read_secret; v=read_secret(sys.argv[1]); sys.exit(sys.argv[1]+" absent") if not v else sys.stdout.write(v)' "$1"; }
   R=/Volumes/COBALT-BACKUP/restic
1. Mint the new password into VaultManager, never displayed:
   uv run python -c 'import secrets; from cobalt.redact.secrets import put_secret; put_secret("RESTIC_PASSWORD_NEW", secrets.token_urlsafe(32))'
2. restic -r $R --password-file <(vault_get RESTIC_PASSWORD) key list            → note the * key id (OLD_ID)
3. restic -r $R --password-file <(vault_get RESTIC_PASSWORD) key add --new-password-file <(vault_get RESTIC_PASSWORD_NEW)
4. restic -r $R --password-file <(vault_get RESTIC_PASSWORD_NEW) snapshots       → same snapshots
   restic -r $R --password-file <(vault_get RESTIC_PASSWORD_NEW) key list        → two keys
5. restic -r $R --password-file <(vault_get RESTIC_PASSWORD_NEW) key remove <OLD_ID>
6. restic -r $R --password-file <(vault_get RESTIC_PASSWORD) snapshots           → must fail (wrong password / no key found)
7. cp -p data/.cobalt_vault data/.cobalt_vault.pre-restic-0915
   uv run python -c 'from cobalt.redact.secrets import read_secret, put_secret; put_secret("RESTIC_PASSWORD", read_secret("RESTIC_PASSWORD_NEW"))'
   restic -r $R --password-file <(vault_get RESTIC_PASSWORD) snapshots           → works
8. uv run python dev_utils/manage_vault.py → [4] RESTIC_PASSWORD_NEW → y → [5]
9. LastPass: update the restic password item (reveal only inside LastPass).
10. Proof: next com.cobalt.backup exit 0 + heartbeat backup probe fresh (or `uv run cobalt backup run` with the hub).
   If b2 is ever enabled: repeat 2–6 against it before step 7.
```

## Out of scope today

Rank-metric value column (S2-P4), FinancialJuice collector (S3 design), `COBALT_ENV` defaults (ruled), anything touching L29's routing substance — none touched.

## Close

| check | result |
|---|---|
| `uv run pytest tests/cobalt tests/taxonomy -q -p no:cacheprovider` | `1059 passed, 238 skipped, 15 warnings in 23.65s` |
| `uv run pytest -q` (whole repo) | `ERROR tests/test_finviz_extractor.py` — `ImportError: cannot import name 'FinvizStockData' …` → `Interrupted: 1 error during collection`. Old tree, present before any edit; neither file changed by the branch. |
| `uv run cobalt jobs restarts main..HEAD` | no FAILED / ESCALATE. Non-doc rows: `.claude/settings.json HARNESS -` · `configs/cobalt/jobs.yaml registry; register, no restart -` · `configs/cobalt/notify.yaml no resident reads (one-shot: com.cobalt.heartbeat) -` · `configs/cobalt/taxonomy/tunables.yaml resident reads com.cobalt.aset,com.cobalt.radar` · every changed `src/` file → `com.cobalt.radar` or `-` · **`RESTARTS: com.cobalt.aset com.cobalt.radar`** |
| commit | one amended commit (L46) |

## ESCALATE

1. **Item 5 OUT** — Grok (`52628 grok`) running at the ruled check. Runs in the next ops seat once the Grok pane is idle.

READY FOR MERGE — commit = tip of `ops/2026-09-15` (a hash cannot sit inside its own commit; it is in the chat reply and `git log -1 ops/2026-09-15`) | RESTARTS: com.cobalt.aset com.cobalt.radar · ESCALATE: 1

## Rebase 2026-09-16

Rebased onto main 757b2ac (after `deploy-2026-09-15` = ebb1231: `ops/day-open` + S2-P3 `/radar`). One conflicted file, `src/cobalt/cli.py`; everything else auto-merged.

| hunk | main (day-open) | this branch (email retired) | resolution |
|---|---|---|---|
| module docstring, command list (`cli.py:24`) | added `cobalt day-open [--date YYYY-MM-DD] [--json] / cobalt day-open verdict "<line>"` | removed `cobalt notify email-auth/email-test/email-status` | day-open line kept exactly; notify line stays removed |
| `main()` subparser mounts (`cli.py:471`) | added `dayopen_cli.add_parser(sub)` | removed `notify_cli.add_parser(sub)` | day-open mount kept exactly; notify mount stays removed |

`git diff main -- src/cobalt/cli.py` after the resolution = the branch's original cli.py hunks, nothing else (notify import, email sweep lines of `validate`, notify mount, docstring line).

Added in this rebase: `configs/cobalt/rules.yaml` derived `UNCLASSIFIED CONFIG` → ESCALATE (proven on 4a6658d, the generated commit that touched it). Now declared in `jobs.yaml` `no_resident_reads` with readers `com.cobalt.prefill-daily`, `com.cobalt.prefill-drc` (`prefill/daily.py:465`, `prefill/drc.py:381` → `regenerate_rules_config()`; `load_rules_config()` has no caller). Test first: `test_jobs_restarts.py::test_generated_rules_yaml_derives_no_restart` failed, then passed. The file is generated config read only by one-shots → `RESTARTS: none` for it. `QWEN.md` → `DOCS` (branch's own rule, test present).

| check | result |
|---|---|
| `uv run pytest -q tests/cobalt tests/taxonomy` | `1172 passed, 239 skipped` (09-15: 1059/238; +112 passed/+1 skipped = main's day-open + radar-panel tests; +1 = the rules.yaml test) |
| `COBALT_ENV=dev uv run cobalt validate` | exit 0 (with `.env` copied from `~/cobalt`, never printed, removed after; without it: `DbConfigError`, same on main) |
| `uv run cobalt notify --help` | `invalid choice: 'notify'` — the group is gone, no `email-*` command |
| `uv run cobalt jobs restarts 4a6658d~1..4a6658d` | `configs/cobalt/rules.yaml M no resident reads (one-shot: …)` → `RESTARTS: none` |
| `uv run cobalt jobs restarts main..HEAD` | no ESCALATE · **`RESTARTS: com.cobalt.aset com.cobalt.radar`** |
| commit | the branch's one commit, amended (L46) |

READY FOR MERGE (rebased) · RESTARTS: com.cobalt.aset com.cobalt.radar
