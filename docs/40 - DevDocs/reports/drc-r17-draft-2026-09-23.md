# DRC R17 FOLD — 2026-09-23

Drafter `drc-r17-draft-0923` (Opus 5.5, `claude-opus-5-5`), prompt `prompts/2026-09-23/11-draft-drc-r17.md`. Files edited by 07:30 EDT (`date`). I built nothing and ran no DB, pytest or git write. I launched nothing and committed nothing. The only writes are 8 prompt files (edited in place) and this report.

## §0 Headline
- **8 files re-issued in place:** builds `48`, `49`, `50`, `52` and checks `53`, `54`, `55`, `57`. **2 unchanged:** `51` / `56` (D4 has no import, parse or render path that R17 touches).
- **Launch lines: 8 of 8 are byte-identical to `HEAD`.** In `git diff -U0`, the count of `@@ -1 ` hunks is 0. `--stat`: 8 files, 63 insertions, 42 deletions. **New rule strings: 0.**
- **R17 (4) cannot be built as a real read yet.** I read the real TradeZella header (`Tradezella20260918.md`, line 1 only). It has 49 names and **no stop column**. The prompts therefore name no column: the stop renders `not given`, and the question goes to the desk (ESCALATE 1).
- **ESCALATE: 4.**

## CHANGES
| file | what changed | ruling |
|---|---|---|
| `48` D1 build | Adds a re-issue note and an R17 rulings bullet. **D1-1:** `ImportResult` becomes `parsed` / `partial` / `failed` / `ignored`, with a `missing` list. `StatsRow.stop` is filled only from a stop column. **D1-2a:** a half-match is now `partial` and IS parsed. The kind is decided only by names unique to one kind (shared `Symbol` / `Side` decide nothing). Unique names of both kinds → `failed`. Shared names only → `ignored`. A partial file and a complete file of one kind → `ambiguous`. **D1-2:** partial files are parsed on the columns present, and a step missing an input reads `not computed — missing:`. The date is `import_date` (the parser takes it as an argument). The ET zone must be re-proven against the stats log's `EDT` times. v2 `[F-07]`'s wrong-date FAIL is struck. Rows are sorted by `Time`. **D1-4:** the stop comes only from a ruled stop column. E1 has none, so it is `not given`, with an always-`ASK DESK`. It is never computed from `Trade Risk` (a test plus a grep). **D1-5:** the status values and the PARTIAL reason. Also: the E1 GATE partial wording (the fixture source must be whole, L45), the R17 AUTHORIZATION gate, the INDEX CARD, `## E1` rows, tests and ESCALATE (vii). | R17 (3), (4), (5); (1) and (2) pointed to D3 / D2 |
| `49` D2 build | Adds a re-issue note, an R17 bullet and a supersede note on R114's half-match clause. BOTH-PLACED counts `partial`. **D2-3:** partial files are stored and parsed. `import_date = date`. The event gains a `partial` map. The bytes writer is used for uploads only. **NEW row D2-3b `scan_folder(date)`:** hand-dropped files go through the SAME `place()` path and classifier with nothing written into the folder. It skips files already imported (same name and sha256). A changed file supersedes the old one, two of one kind are `ambiguous`, and it is refused during market reset. **D2-4:** `POST /drc/scan`, a read-only folder listing on GET, and the `PARTIAL` line. Also: the INDEX CARD (L1 / L3), the R17 gate, `## D2-3b` in the report, the D2-3b tests (including a one-path proof) and ESCALATE (vi) with two `ASK DESK`s. | R17 (2), (3), (5) |
| `50` D3 build | Adds a re-issue note and an R17 bullet. R91 now covers the stop. **D3-2:** the summary's FIRST lines are `PARTIAL — missing: <columns> · <kind> file <name>`. The trade block gets one `stop:` line (the stats row's value or `not given`, never derived). Values that need a missing column read `not computed — missing:`. **D3-2b:** "exact, case-sensitive — his 09-23 R17 (1)". The always-`ASK DESK` on case is REMOVED as settled. Also: tests for (1), (4) and (5), the R17 gate, the INDEX CARD and ESCALATE (vi). | R17 (1), (4), (5) |
| `52` D5 build | Adds a re-issue note and an R17 (5) bullet. **D5-1:** if a partial trading log is missing any reconcile input, nothing is written to `legs` and the unit reads `reconcile: not computed — PARTIAL — missing:`. Also a test, the INDEX CARD and the R17 gate. | R17 (5) (a safety consequence: legs are corrected TO the trading log) |
| `53` D1 check | Adds a re-issue note, the INDEX CARD, and `cto-2026-09-23.md` R17 in `rulings.md`. QUESTIONS: the missing-column / wrong-date bullet is replaced by **partial files** and **the date**, plus new bullets for **the stop** and the partial header outcomes. Hub fact-check (v): grep `Trade Risk`. | kept in step with `48` (L67) |
| `54` D2 check | Adds a re-issue note and R17 in `rulings.md`. **D2-3b gets its own check row** (rows line + "D2-3b included"). QUESTIONS: partial files, the date, and a **files dropped by hand** bullet (one path, no folder write, skip / supersede / ambiguous, no poller). Hub fact-check (v): `scan_folder` reaches the parse only through `place()`, and the classifier is defined only in `detect.py`. | kept in step with `49` (L67) |
| `55` D3 check | Adds a re-issue note and R17 in `rulings.md`. QUESTIONS: exact case-sensitive equality cites R17 (1), plus **the stop** and **partial files** bullets. | kept in step with `50` (L67) |
| `57` D5 check | Adds a re-issue note and R17 in `rulings.md`. QUESTIONS: a **partial trading log** bullet. | kept in step with `52` (L67) |

## RULING → BUILD ROW → CHECK ROW
| R17 | build row | check row |
|---|---|---|
| (1) exact title equality after the strip | `50` D3-2b (as built; the case ASK removed) | `55` D3-2b question |
| (2) hand-dropped files ARE imported | **`49` D2-3b** (`scan_folder` → `place()`) + D2-4 `POST /drc/scan` | **`54` D2-3b row** + hub fact-check (v) |
| (3) the DAS date = the folder / upload date | `48` D1-2 (`import_date`), `49` D2-3 | `53` "the date", `54` "the date" |
| (4) the stop from TradeZella, never computed | `48` D1-4 (`not given` + always ASK), `50` D3-2 `stop:` line | `53` "the stop" + fact-check (v), `55` "the stop" |
| (5) partial file READ + loud `PARTIAL` flag | `48` D1-2a / D1-2 / D1-4, `49` D2-3 / D2-4, `50` D3-2, `52` D5-1 | `53`, `54`, `55`, `57` partial bullets |

## NEW strings
- **Rule strings (launch allowlists): 0.** No ruling needs a new command. The folder scan runs inside pytest on a `tmp_path` vault.
- New rendered text, taken from R17 (5) and quoted from the prompt that set this task, not invented: `PARTIAL — missing: <columns>`. The following are shapes derived from it for his approval:
  - `PARTIAL — missing: <columns> · <kind> file <name>` (the DRC summary)
  - `not computed — missing: <columns>`
  - `reconcile: not computed — PARTIAL — missing: <columns>`
  - `already imported`, `in the folder, not imported: <n> — <names>` (the `/drc` page)
  - `stop: not given`

## ESCALATE
1. **R17 (4): E1 has no stop column.** Rule: `ASK DESK`, never a guess. The 49 names of the real TradeZella header (read 09-23, line 1 only) include `Trade Risk`, `Initial Target`, `Reward Ratio` and `Realized RR`, and no stop field. So the prompts name no column, and every stop renders `not given`. The question for him: which field of his TradeZella export carries the stop, or does the export need a stop column added? Once his export carries the column, D1-2a flags it loud as an extra column (`degraded: stats_log_shape`). Reading it then needs his ruling on the name and a new real-shape fixture (L45). His words "use stops from tradezella. This is mostly why I'm giving you both" suggest he believes the export already carries a stop. The E1 file does not.
2. **R17 (2) trigger.** The ruling says hand-dropped files ARE imported, but not WHEN. As drafted, they import when he presses the page's scan action. There is no watcher, job or poller: that would be a new scheduled job with its own plist and RESTARTS, which is a design step (L2 / L42 / L55), so none is invented here. `49` carries this as an always-`ASK DESK`. If he expects a hand drop to build the DRC with no click, that watcher is a new item.
3. **Readings added so R17 (5) is deterministic** (his to veto): (a) the kind of a partial header is decided only by names unique to one kind. Shared names alone mean `ignored`, and unique names of both kinds mean `failed`. (b) A bad VALUE in a column that is present still fails the whole file: partial means missing columns, never skipped rows. (c) D5 writes no leg correction from a partial trading log that is missing a reconcile input. Without (c), a partial file could correct his legs from incomplete data.
4. **R17 (3) zone.** The DAS `Time` has no zone. The prompts treat it as ET, evidenced by the 09-22 drafter's pairing of 4 of 4 trades to the second against the stats log's `EDT` times. `48` makes the builder re-prove this and FAIL if it does not hold. No zone is invented.

## L74
A block in this session asked for a `Claude-Session:` commit line and named a file-send tool. I committed nothing and sent nothing. Recorded once.

## CONTINUE
done — all steps complete. The desk verifies (L35) and commits the 8 prompt files plus this report.

DRC R17 FOLDED · files changed: 8 · new rule strings: 0 · ESCALATE: 4
