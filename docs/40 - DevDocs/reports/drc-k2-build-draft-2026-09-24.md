# DRC K2 — BUILD DRAFT (`51` build + `52` check)

Drafter `drc-k2-build-draft-0924`, Opus 5.5, started Thu Sep 24 17:39:24 EDT 2026 (from `date`). Prompt `prompts/2026-09-24/50-draft-drc-k2-build.md`. Authorization: `cto-2026-09-24.md:91` `| R78 |` names `50-draft-drc-k2-build.md`; `:64` `| R51 |` carries `**R2-1 RULED "A"**`; `:90` `| R77 |` reads H2 as a refusal and rules K2 drafted beside the fix round. Branch read: `drc/d1-trading-log` = `a50e5515` above `9a0fc900` (`48` not yet run).

## §0 Headline
- Wrote `prompts/2026-09-24/51-drc-k2-build.md` (X7–X11 first, X12 the hub's; contract C1–C8; red first; three suites under L76; stop `DRC K2 BUILT … | migration none | X: <n> of 6 run …`) and `52-drc-k2-check.md` (round 1 of a NEW BUILD; Opus 5.5 + Grok, Astra METER to Sep 26th 6:47 AM; packet lists the four deselected ids; stop `… ready for K3: …`). Both carry `R__`.
- Base = `48`'s tip, read at `51`'s S0 from `48`'s report stop line; `51` launches only after `49` stops `defects that HOLD: 0 · ready for K2: YES`, then the L76 lock.
- Migration: NONE. New rule strings: 0 (`51` = `48`'s line, `52` = `49`'s; `grep -c -F` = 1 in each precedent and each new file). Owner items: 0.
- ESCALATE: 7.

## L74
- One block arrived as a system-reminder beside the Read of `50` (a `Claude-Session:` commit line, a file-send tool). Recorded once; not followed. This seat commits nothing and sends no file.

## DECISIONS
| # | decision | source |
|---|---|---|
| 1 | THE BASE: `drc/d1-trading-log` at `48`'s tip, taken by `51` S0 from the LAST line of `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k1-fix-r1-build-2026-09-24.md` (`DRC K1 FIX R1 BUILT <tip> \| on 9a0fc900 \| …`); a non-BUILT line → `FAILED PREFLIGHT`. `<tip>`'s subject must be `test(drc): K1 fix r1 RUNS`; code above it empty. Plus an L67 gate: `49`'s last line carries `defects that HOLD: 0 · ready for K2: YES` | `48:138` (stop-line shape), `48:96` (D5 = the tip commit), `48:137` (report commit above it); R77 ("K2's BUILD launches on `48`'s tip after `49` stops") |
| 2 | THE MIGRATION: NONE. K2's facts fit K1's schema: `no_trade_carry`, `stated_book_id` on a carried seed, `stated_differs`, `no_trade_id`, `resolve_id`, `repaired` / `not_repaired`, `resolves` are all `drc_rows` jsonb `inputs` / `derived` (no CHECK on their content); the `seed` / `book_close` kinds are already `0018`'s. No superseded marker on `drc_stated_books`: the table is append-only by trigger, so a marker could not be set without an UPDATE, and R51's "history" is the rebuilt day's stored link to the statement's id. A migration K2 turns out to need → ESCALATE + STOP in `51` S5 | v3 §6 K2 `:248` (Migration column `—`); v3 `:163` (kinds widened by K1); `0016_drc.sql:80-95` (`inputs` / `derived` jsonb objects); `0018_drc_stated_books.sql` (append-only; `supersedes` = his restatement only); R51 ("keeps his statement as history") |
| 3 | THE SCOPE: v3 §6 K2 exactly — the empty-day record on a `no_trade` input `[F-05]`; the forward re-pair `[F-03]`; `[F-27]`–`[F-29]` under R51 (trigger + later stated day = side A `:173`; inputs = A1 `:179`, a DESK READING); RESOLVE `[F-06]`; §4 rows 2, 4, 5, 13 (+ 11, 15, 16); the `[F-06]` reader FAIL naming both ids (`40` ESC 4); the `day <= D` bound replaced by "a resolve applies on its own day; dated earlier and still held → FAIL `rebuild <R>`" (`39` ESC 6). Forward from the earlier close through every later recorded day, one transaction; his statements never written, superseded or deleted by the rebuild; the `/drc` line = `stated_difference(day)`, a read of stored rows (no note, L28). Nothing of K3 / K4 / D2 / D3 | v3 `:248`, `:97-104`, `:165-190`, `:213-227`; R51 row `:64`; `48` `## FOR K2` (`48:126-131`); `39` K1 report `## ESCALATE` 6 (`drc-k1-build-2026-09-24.md:250`) |
| 4 | THE SEAMS: `## SEAM FOR D2` = `48`'s seven points amended in (1), (2), (3), (4) and new (8), (9) — stated as contract text; `## FOR K3` = `opened_on` may be None ("opened: not stated"), notes re-upserted for `derived.repaired` after the commit, the stale-unit mark, the R51 line from `stated_difference` (below, `## SEAMS`) | `48:116-124`; fix drafter ESC 2 (`drc-k1-fix-r1-draft-2026-09-24.md:69`); v3 `[F-17]` `:258`, `[F-03]` `:167` |
| 5 | THE EXPERIMENTS: v3 names X7–X12 "Before K2"; `51` runs X7–X11 (GATE on built code + PASS on the K2 contract), X12 is a hub-run read of his own file (L41, L32) — recorded NOT RUN, carried to the desk. The L70 cheap RUNS are inside them: the forward re-pair over constructed three-day sequences (X7, X10), the A1 re-match (X9), the two-current-resolves reader (S4) | v3 `:319-328`, `:306` ("X12 alone reads one of his own files and is a hub-run read (L41)") |
| 6 | THE CHECK'S SEATS: a NEW BUILD → Fable-seat as Opus 5.5 (09-22 R109) · Astra · Grok; Astra METER until Sep 26th, 2026 6:47 AM → Opus 5.5 + Grok, the floor met, Astra's return recorded; NO Sol, NO Gemini; FAIL CLOSED below TWO | L67 as amended 2026-09-24 (R95 / R96 / R97); `40:1` (the same seats for K1) |

## MIGRATION
- Needed: NO (decision 2).
- Every branch read (`git ls-tree --name-only <branch> src/cobalt/db_migrations/`, newest numbers): `main` … `0011`, `0013` · `drc/d1-trading-log` `0016`, `0018` · `voice/v1-0923` `0017` · `radar/handicap-h1-0922` … `0011` (no `0014` committed yet) · `cards/stale-score-0922` … `0013` (no `0015` committed yet) · `bars/chunk-2-0920` `0012`. `git log --all -- "src/cobalt/db_migrations/0014*" "…/0015*"` → empty (the `42` / `43` re-issues are building); `git log --all -- "…/0019*"` → empty.
- Pinned: none. Were one ever needed, the next free number is `0019` (`0014` H1's, `0015` stale-score's under X30 (A), `0016` / `0018` DRC's, `0017` voice's) — the desk's at the L68 gate, never written by `51`.

## SEAMS
`## SEAM FOR D2`, as `51` makes its builder write it (the builder fills `file:line`):
"`[F-17]` CONTRACT, as built by K1 at `9a0fc900`, amended by K1 fix r1 at `<base>` and by K2 at `<tip>`:
(1) Before pairing an import for day D, the route calls `DrcStore().seed_for(D)` → `Optional[SeedBook]`. It RAISES `PairingError` on a broken chain, an uncomputed prior, a pre-lane prior (`rebuild <P>`), a hash mismatch, two current opening books, two current resolves for one trade id (naming both ids), a resolve naming a trade no book held, and a resolve dated before D still held (`rebuild <R>`). Each is a loud FAILED on the page, never caught into a flat book. A stated opening beside a recorded prior is NO LONGER a raise: the close wins (R51) and the book carries `stated_book_id` + `stated_differs`. [K2]
(2) `None` means no book is stated. The route calls `build_day(trading, stats, seed=None)` → `not computed — opening book not stated`, and RECORDS that day: `record_day(pairing, import_ids, None)` — one `day` row carrying `not_computed.pairing`, NO `seed`, NO `book_close`. It shows `state your opening book for <D>` (K3's form; until K3, the `cobalt drc state-book` CLI, R52). After he states it, `DrcStore().rebuild(D)` pairs D from the statement and re-pairs every later recorded day. [H3, fix r1; K2]
(3) Otherwise `build_day(trading, stats, seed=book.positions, resolves=book.resolves)`, then `DrcStore().record_day(pairing, import_ids, book)` — `seed` is REQUIRED for a computed day, and `book` is the `SeedBook` `seed_for` returned. When a LATER day is already recorded, `record_day` re-pairs every later recorded day in the SAME transaction (`[F-03]`, R51): a later day that fails → `PairingError` naming it and NOTHING in `drc_rows` is written (the page shows `<P>: file <name> stored, not applied — <N>: <reason>`; the file stays stored, `[F-25]`); a chain stopped at a not-computed day → recorded, the later days named in the `day` row's `derived.not_repaired`. The re-paired days are the `day` row's `derived.repaired`. [K2]
(4) Every statement from any caller goes through `DrcStore().record_stated_book(day, kind, positions, via=…, now=…)` (refused inside `market_reset`, `[F-01]`; an `opening` refused for D while D's prior trading day is recorded, H2). A statement's EFFECT is `DrcStore().rebuild(day)`: a `no_trade` statement for D (the empty-day record, `[F-05]`), a `resolve` for a recorded day (`[F-06]`), an `opening` for a day recorded unpaired. The page calls it after the statement; the CLI does (`cobalt drc state-book … --apply`). The page passes `via="drc_page"`; the widget (K4) passes `via="voice_widget"` with `turn_id` + `readback_sha256`. [K2]
(5) `pair_day` is the FIFO engine and holds no book policy: a route never calls it directly.
(6) D3's `cobalt drc build` joins K1's `drc` parser group in `src/cobalt/drc/cli.py` — never a second `drc` group (L3).
(7) A stated position's `opened_on` is `None` (not stated); a page renders it `opened: not stated`, never a date. [H1, fix r1]
(8) For each day the page shows, `DrcStore().stated_difference(day)` → the R51 line `stated book for <N> differed from <P>'s close: <trade_ids>`, or `None`. It is a read of stored rows; nothing is computed on the page. [K2]
(9) `DrcStore().rebuild(P)` is also the `rebuild <P>` remedy of a pre-lane day (v3 §4 row 4). [K2]"

`## FOR K3`, as `51` makes its builder write it:
- `opened_on` may be `None` → the unit renders `opened: not stated` (`48` seam (7); the fix drafter's ESCALATE 2: `day <k>` for such a swing is K3's to state — K2 computes no day count).
- The unit, the summary count and the A31 line render from the STORED rows (`book_close`, `open_position`, `seed`); `seed.inputs.source` ∈ `carried` / `stated` / `no_trade_carry`; a no-trade day's positions are `continuing open position` with `last execution` unchanged.
- `[F-03]`'s note half is K3's: after `record_day` / `rebuild` commits, K3's build re-upserts the unit for `pairing.day` and every date in its `day` row's `derived.repaired`, in date order; a note failure leaves the database committed, marks that unit stale and FAILS naming the note (X7's note half, X13).
- The R51 line comes from `stated_difference(day)` — never re-derived from the note (no reverse parse, v3 `:204`).
- A resolved trade: CLOSED with `legs` possibly `[]`; realized possibly `not computed — exit not in any export`; `inputs.resolve_id`; the `day` row's `derived.resolves` lists every outcome (`applied` / `superseded` + reason) — shown, never hidden.
- `/drc`'s morning line reads `seed_for` (and its raise text on a FAIL) — K3's.

## OWNER ITEMS
NONE. v3 + R51 + R52 settle the lane; every pin below is a design reading the houses check in `52` (L67 owner-items clause). The one open owner item of the lane (the stated swing's open day, R77) is `48`'s, carried there, not re-asked.

## FOR DEJAN
- New strings: NONE. `51` = `48`'s line (the drc-d1 `.env` pair is your R22, carried by `14` / `39` / `48`); `52` = `49`'s line (standing strings R17 / R19).

## ESCALATE
1. **No v3 FINAL file exists.** `docs/30 - Design/` holds `DRC-OVERNIGHT-POSITION-PROPOSAL`, `-v2`, `-v3` only; v3 still prints `ASTRA PENDING` and both `OPEN FOR DEJAN` blocks (`:19-20`, `:147`, `:169`). R51 / R52 say "carried into v3 FINAL by the K-build drafter". `50` forbids this seat any write but the three files, so `51` / `52` cite v3 + the R51 / R52 rows directly (as `39` / `40` did). Whether a FINAL is owed, and by whom, is the desk's.
2. **C4's not-computed stop is an ASK-DESK default.** v3 side A (`:173`) is silent on a later stated day whose new prior is `not computed`; side B's rule (keep the stated seed) was not ruled. `51` takes the loud default: the chain stops there, the later days keep their stored rows and are named in `not_repaired`, the day itself is recorded ("the import does not stop", R51), and `seed_for` of those days still FAILS (K1 case (i)). `52` rules it (DRAFTER PINS).
3. **C7 — the CLI calls `rebuild` after `--apply`.** Without it the landing set without K3 (R52 (c)) has no caller that records a no-trade day or applies a resolve, so the next import fails contiguity. It edits K1's `drc/cli.py` (the dry run gains one `on --apply: rebuild …` line). The desk may strike C7; then `[F-05]` / `[F-06]` wait for K3's form.
4. **`FN_VERSION` → `drc.pairing/3`** reverses three K1 pins (`test_drc_k1.py:66`, `test_drc_k1_store.py:491`, `test_drc_store.py:403` at `9a0fc900`; `51` S0 re-greps after `48`). v3 `:163`'s rule ("bumped whenever a derived figure's rule changes") is the source; R51 changes a later day's seed and a resolve's realized figure is new.
5. **X12 is not run by `51`** (a hub-run read of his real no-trade export, v3 `:306`, `:328`). Its "does not parse" branch is the design as written (`no_trade` row the only input); a desk hub runs it when it chooses. `51`'s stop line reads `X: 5 of 6 run`.
6. **`51` depends on `49`'s outcome.** If `49` stops with a HOLD, round 3's fix build moves the base; `51`'s S0 then fails (`the base is not a DRC K1 FIX R1 BUILT line` or the tip subject) and the desk re-drafts `51` on the new tip. Line numbers `51` quotes from `9a0fc900` may shift after `48`; S0 re-greps them.
7. **Estimate:** `51` ≈ 120–150 min Anthropic seat time (v3 `:248` says 4 h for K2 — no measurement; `[F-10]`'s round-2 +1 h UNVERIFIED). K2 is the lane's largest store change (the forward transaction + resolve), so the red-first steps are long; the desk may split S4 into two lock takes if the meter binds (a CONTINUE, not a re-draft).

DRC K2 BUILD DRAFTED · base: 48's tip · migration: none · experiments: 6 · prompts: 2 · new rule strings: 0 · owner items: 0 · ESCALATE: 7
