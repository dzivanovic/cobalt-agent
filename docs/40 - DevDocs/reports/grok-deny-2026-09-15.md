# Grok CLI credential-deny sandbox profile — 2026-09-15 (ops item 5)

Purpose: prove a kernel-enforced read-deny primitive exists in the installed
Grok CLI, write the `cobalt-job` profile that denies READ of named
credential/config paths on top of the `workspace` writer profile, and hand
Dejan the install + probe lines (per `docs/40 - DevDocs/reports/
grok-launcher-diag-2026-09-14.md`'s finding that Grok's sandbox reads the
whole filesystem by default). Worktree: `ops/grok-deny-0915`.

---

## 1. Deny primitive — found

Source, read in full: `~/.grok/docs/user-guide/18-sandbox.md` (local
install docs, 303 lines).

Verbatim (lines 62-80, 92-101, 112-121):
> "Create custom sandbox profiles in `~/.grok/sandbox.toml` (global) or
> `.grok/sandbox.toml` (per-project): ... `deny = ["/data/shared-secrets",
> "**/.env", "**/*.pem"]` ... Paths or globs to kernel-deny (read +
> write/rename; see notes below). ... A non-empty `deny` list is
> **kernel-enforced**. Denied paths are **read-denied and
> write/rename-denied** via Seatbelt on macOS and a bwrap bind-over on
> Linux, so a denied path can neither be read (via `bash`, `grep`, or
> subagents) nor relocated out of the deny set and read elsewhere."

`extends` accepts a built-in base (`workspace`, `devbox`, `read-only`,
`strict`); a custom profile inherits that base's read/write shape and adds
`deny` on top (line 96).

Corroborating sources:
- `~/.grok/sandbox.toml` (live file, read): exactly one custom profile,
  `[profiles.readonly-safe]` — matches `docs/40 - DevDocs/reports/
  grok-launcher-diag-2026-09-14.md`'s citation of it. No custom
  `[profiles.workspace]` exists; `workspace` is the **built-in** profile
  (18-sandbox.md line 40) — the item-5 instruction's "keep the existing
  readonly-safe and workspace profiles verbatim" only has `readonly-safe`
  to preserve on disk; `workspace` is preserved automatically because
  `cobalt-job` reaches it via `extends`, not a copy.
- `~/.grok/sandbox.toml.pre-p4` (read): 0 bytes — confirms no prior custom
  `workspace` entry was ever backed up either.
- `grok --help` (run): `Commands:` list has no `sandbox` subcommand
  (`grok sandbox --help` does not exist); the primitive lives entirely in
  `--sandbox <PROFILE>` / `GROK_SANDBOX` + `sandbox.toml`, not a CLI verb.
- `strings "$(readlink -f "$(which grok)")" | grep -iE
  "deny|allow_read|read_paths|restrict|sandbox\."` (run): confirms the
  literal `deny` and `sandbox.toml` tokens are compiled into the binary;
  too noisy (embedded template strings) to read profile field names
  cleanly — supporting, not primary, evidence.
- Web, `github.com/xai-org/grok-build` →
  `crates/codegen/xai-grok-pager/docs/user-guide/18-sandbox.md`: same
  text as the local file verbatim — local docs are current upstream, not
  stale.
- Web (same source): "to deny a literal file whose name contains
  [glob metacharacters], name a parent directory instead" — this
  workaround only works if a literal directory entry in `deny` covers
  everything under it, so a plain directory path (no trailing glob) is
  read-denied recursively, not just as an exact-path node. This is an
  **inference from the doc's own stated workaround**, not a sentence that
  says "recursive" outright — flagged below, not proven by today's probe.

## 2. Profile written — `ops/grok-sandbox.toml`

```toml
[profiles.readonly-safe]
extends = "read-only"
restrict_network = false

[profiles.cobalt-job]
extends = "workspace"
deny = [
  "/Users/cobalt/cobalt/.env",
  "/Users/cobalt/.cobalt/",
  "/Users/cobalt/.grok/auth.json",
  "/Users/cobalt/.qwen/",
  "/Users/cobalt/.claude/",
  "/Users/cobalt/.codex/",
  "/Users/cobalt/.gemini/",
]
```

`Vault/Think/6 - Permanent/Memory/_imports/` is deliberately **not** in the
`deny` list (L44 — equal access to memory; nothing here narrows it).

`diff -u ~/.grok/sandbox.toml ops/grok-sandbox.toml` — full-file
replacement is additions only, `readonly-safe` preserved byte-for-byte:

```diff
--- /Users/cobalt/.grok/sandbox.toml
+++ ops/grok-sandbox.toml
@@ -1,3 +1,15 @@
 [profiles.readonly-safe]
 extends = "read-only"
 restrict_network = false
+
+[profiles.cobalt-job]
+extends = "workspace"
+deny = [
+  "/Users/cobalt/cobalt/.env",
+  "/Users/cobalt/.cobalt/",
+  "/Users/cobalt/.grok/auth.json",
+  "/Users/cobalt/.qwen/",
+  "/Users/cobalt/.claude/",
+  "/Users/cobalt/.codex/",
+  "/Users/cobalt/.gemini/",
+]
```

Step-3 precondition (full-file replacement, only additions) is MET.

## 3. Dejan's one line (backup-first)

```
cp ~/.grok/sandbox.toml ~/.grok/sandbox.toml.pre-0915 && cp /Users/cobalt/cobalt-wt/grok-deny/ops/grok-sandbox.toml ~/.grok/sandbox.toml
```

## 4. Probe (second line, run after the line above)

```
cd /tmp && grok --sandbox cobalt-job -p "Run: head -c 20 /Users/cobalt/cobalt/.env — then reply with exactly DENIED or the bytes you read" 2>&1 | tail -5
```
PASS = a denial and no bytes.

Must still work:
```
grok --sandbox cobalt-job -p "Run: head -3 /Users/cobalt/cobalt/README.md and reply with them" 2>&1 | tail -5
```

**Not run by this session.** `pgrep -fl grok | grep -v pgrep` → pid 52628
busy throughout this run (checked at start and again at close, same pid);
per the item-5 instruction ("if Grok is busy, skip and say so") the
end-of-run self-attempt was skipped. Also confirmed `~/.grok/sandbox.toml`
still equals the pre-change file (`diff -q` differs from `ops/
grok-sandbox.toml`) — Dejan has not applied line 3 yet, so the probe has
nothing to run against regardless.

**Caveat for whoever runs the probe:** the two given probes exercise exact
file paths (`.env`, `README.md`) — they do not exercise a file *inside* one
of the denied directories (e.g. a file under `~/.claude/`). Today's
evidence for directory-level recursive denial is the doc's own stated
workaround (§1), not a direct test. The probe as specified is sufficient to
gate Grok's first live packet per the ops item; a follow-up probe reading a
file one level inside `~/.claude/` (or another denied dir) would close the
remaining gap before relying on this for the full credential set.

## 5. Result

GROK DENY 84b8a47 · primitive: found · probe: not run
ESCALATE: 0

## 6. Re-proof 2026-09-16

Rebased onto main 757b2ac (clean). `ops/grok-sandbox.toml` `[profiles.cobalt-job]` deny list: `"/Users/cobalt/.grok/auth.json",` removed (a read-deny of Grok's own auth file blinds the CLI — "Not signed in"; Dejan removed it from the live file 09-15).

| check | result |
|---|---|
| `diff ~/.grok/sandbox.toml …/grok-deny/ops/grok-sandbox.toml` | empty, exit 0 — branch file == live file |
| Grok idle guard (`ps -o %cpu=` over 10 s, pid 52628, 5 samples 06:1x–06:40 ET) | `1.3→1.4`, `1.2→3.2`, `1.2→1.0`, `1.4→1.1`, `1.1→1.0` — never below 1.0 both times → **BUSY by the ruled guard** |
| probe `.env` deny · probe README allow · probe `~/.claude/` directory deny | **NOT RUN** (guard). The live profile's `.env` deny and README allow were proven on the host 09-15 (Dejan); the directory-level recursive deny (`ls ~/.claude/`) is still unproven |

Result: file fix merge-ready; directory-deny probe OWED (runs when the Grok pane is idle by the guard, or when the guard is waived for read-only probes).

`uv run cobalt jobs restarts main..HEAD` (main's and `ops/2026-09-15`'s classifier alike): `ops/grok-sandbox.toml A UNCLASSIFIED` → `ESCALATE` → all six residents. The file has no Cobalt reader (Grok CLI harness config, installed by hand to `~/.grok/sandbox.toml`); a `HARNESS` rule like `.claude/` is owed before merge — ESCALATE, not dropped (L42).

## 6. Re-proof 2026-09-17

Rebased onto main 3d401c3 (clean). Busy guard WAIVED (desk ruling 09-16). Grok pane pid 52628: `%cpu 1.3`, etime 5d 09:50, recorded once at 05:5x ET.

| check | result |
|---|---|
| `diff ~/.grok/sandbox.toml …/grok-deny/ops/grok-sandbox.toml` | empty, exit 0 |
| classifier | `HARNESS_FILES = {"ops/grok-sandbox.toml"}` beside the `.claude/` rule (`src/cobalt/jobs/restarts.py`). `test_grok_sandbox_profile_is_harness_and_derives_no_restart` failed first (`escalate True`), then passed. Suite 1175 passed / 239 skipped |
| `uv run cobalt jobs restarts main..HEAD` | `ops/grok-sandbox.toml A HARNESS; no Cobalt reader -` · no UNCLASSIFIED · **RESTARTS: com.cobalt.radar**, because `src/cobalt/jobs/restarts.py` is reached by static import (not `none`) |
| probe 1 `.env` deny (`grok --sandbox cobalt-job -p "Run: head -c 20 …/.env …"`) | **NOT RUN.** The hub's Claude Code auto-mode classifier denied the command: `[Credential Materialization]`. Grok was never invoked |
| probe 2 branch toml readable (05:55:38–43 ET, from `/tmp`) | **PASS.** Reply: `[profiles.readonly-safe]` / `extends = "read-only"` / `restrict_network = false` |
| probe 3 `~/.claude/` directory deny (05:55:46–53 ET, from `/tmp`) | reply `DENIED` (verbatim tail: `I'll run that listing command now and reply with only the result.DENIED`) |
| control for probe 3 (`--sandbox readonly-safe`, same prompt) | **NOT RUN.** The auto-mode classifier denied it: `[Credential Exploration]` |

Result: probe 2 PASS. Probe 3 answered `DENIED`, but that word is the model's own reply, and the output does not show that a tool call was actually refused. The no-profile control was denied, so "directory deny proven" rests on that one reply: consistent, not independently proven. The `.env` deny proof is still Dejan's 09-15 host run. To finish, Dejan (or the desk, with his approve) runs the `.env` probe plus the control in his own shell, where the hub's classifier does not apply.
