# Deploy 2026-09-17 — evening hub `evening-0917` (Opus 5)

## §0 Headline
- DO NOT STOP until EVENING DONE
- ops-0917 + grok-deny + agy-trial-rebuilt MERGED and green. **S2-P2 FAILED and is REVERTED** — §2.4 integrated suite red: ops-0917's new ceiling-45 tests call `load_sources()` without P2's newly required `context_tickers` kwarg (a semantic ops×P2 conflict `git merge-tree` cannot see).
- Safe state GREEN: nothing migrated (0006/0007 never ran on prod), no settings row written, prod `radar_pool` clean, heartbeat GREEN, sheet 200.
- INCIDENT: production `com.cobalt.radar` crash-restarted 3× on merged P2 code at 19:18–19:21 ET (outside the L43 pause) before the revert; every prod write it attempted was REFUSED by the 0005-era CHECK. It now runs reverted code.
- LIVE `7e4841e`, tag `deploy-2026-09-17`; RESTARTS done 20:02 ET inside the pause (aset pid 55521, radar pid 55534), smoke green. ESCALATE: 5.

## PREFLIGHT
Authorization: `git log --oneline -6 -- reports/cto-2026-09-17.md` → `9aef12a docs(desk): ruling R12 — evening approval list + dark settings load (file, sha256) approved 07:30 ET`. R12 names file `/Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml`, sha256 `945e42f8…ca7cd3ca`, content `radar.cards_enabled: false` only — matches this prompt. Prompt file last touched by `5d3b565` (no later commit, not dirty). Laws read: LAWS.md in full (L7 note, L28, L29, L42, L43, L51, L54, L55, L61).

| # | rule | command | exit | result |
|---|---|---|---|---|
| P1 | `git -C * status*` | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | allowed — ` M configs/cobalt/rules.yaml`, ` M docs/40 - DevDocs/reports/seat-usage.md` |
| P2 | `git -C /Users/cobalt/cobalt tag *` | `tag scratch-allow-probe-0917` then `tag -d scratch-allow-probe-0917` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-0917' (was 9aef12a)` |
| P3 | `commit *` + `reset --soft HEAD~1` + `log*` | empty commit → reset → `log --oneline -1` | 0 / 0 / 0 | allowed — `[main 4b76640] scratch: allowlist probe…`; HEAD back at `9aef12a` |
| P4 | `COBALT_ENV=production uv run cobalt validate*` | `… validate` | 0 | allowed — 13 trade_defs OK, 15 jobs registered, `Placement: tree clean`; `daymode.trade_count_band.min/max: PLACEHOLDER (unruled)` (main, pre-ops-0917 — expected) |
| P5 | `… backup status*` | `… backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 9.9 h old` |
| P6 | `… backup run*` | `… backup run --dry-run` | 0 | allowed — `cobalt_brain … 566.8 MB`, `DRY RUN — nothing written, no jobs row touched.` |
| P7 | `… jobs *` | `… jobs restarts main..ops/2026-09-17` | 0 | allowed — `RESTARTS: com.cobalt.aset com.cobalt.radar`; 0 UNCLASSIFIED (3 docs `D` rows are main-side files newer than the branch base — vanish on rebase) |
| P8 | `… db query *` | `… db query --side system --prod "SELECT count(*) FROM radar_pool"` | 0 | allowed — `count 1` |
| P9 | `… heartbeat show*` | `… heartbeat show` | 0 | allowed — `HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red`; radar `scanning (premarket), members 50`; herdr AMBER (declared interim) |
| P10 | `launchctl print gui/501/*` | `launchctl print gui/501/com.cobalt.radar` | 0 | allowed — `state = running`, `pid = 28199` |
| P11 | `curl … 127.0.0.1:5010/*` | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` | 0 | allowed — `200` |
| P12 | `shasum -a 256 *` | `shasum -a 256 …/p2-dark-settings.yaml` | 0 | allowed — `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` = R12; Read: `card_settings:` / `  radar.cards_enabled: false`, `wc -c` = 44 bytes = exact expected bytes |

Not preflighted (no harmless variant on main): `merge --ff-only`, `rebase`, `revert`, `db migrate --allow-prod`, `settings load --card`, `kickstart`, `radar evaluate --replay`.

GATE: proven — commit + tag allowed by the session allowlist

Wake-ups (CronCreate, one-shot, session-only): `c2ef4eb9` 19:15 phase 1 · `a5927239` 20:02 phase 2 · `78d8f878` 21:05 §7+§8.

## Preconditions (19:15 ET)

| check | answer |
|---|---|
| 1.1 prompt unchanged | `98-evening.md` last commit `5d3b565`, no later commit, not dirty; sha256 `bd4cdd95…78a71f6`. R12 re-read from disk — same file + hash, status column now `APPLIED: launch recorded in §17` (desk bookkeeping, not a term change) |
| 1.2 clock | `Thu Sep 17 19:15:04 EDT 2026` — weekday, inside 19:10–19:40 |
| 1.3 tips + clean | `ops-2026-09-17` `b0a351e` (`ops/2026-09-17`) · `grok-deny` `d1260d5` (`ops/grok-deny-0915`) · `agy-trial-rebuilt` `e03d8d5` (`ops/agy-trial-0915-rebuilt`) · `s2-p2-cards` `2f22e70` (`sprint-2/cards`) — all four exactly as expected, all four `status --porcelain` empty |
| 1.4 P2 rides | **yes** — (a) 1.3 passed; (b) READY line verbatim at `s2-p2-rebase-2026-09-17.md:64`: `READY FOR MERGE `a54d04a` \| rebased onto `857b9df` \| RESTARTS: com.cobalt.aset com.cobalt.radar \| ESCALATE: 3`; (c) the three absence tests exist (`test_card_settings.py:122`, `test_radar_evaluate.py:337`, `:628`); (d) hash still `945e42f8…ca7cd3ca`; (e) clock OK |

## Deploy table

`<main-before>` = `91f11a1` (after the L51-2 machine-file commit: `configs/cobalt/rules.yaml` generated_at bump, `seat-usage.md` +27 lines).
Snapshot (2.2): `cobalt_brain via pg_dump inside cobalt_memory — 581.2 MB` · `ssd: snapshot de4bfaa8 — 14 new / 10 changed, 156.8 MB added, 0 pruned`; `backup status` → `newest snapshot: 0.0 h old`. Tag `pre-s2-p2` created.

| branch | tip | status | notes |
|---|---|---|---|
| `ops/2026-09-17` | `3f29250` | MERGED, ff-only | rebase clean; Finviz ceiling 45, `daymode.trade_count_band` 2–6 |
| `ops/grok-deny-0915` | `15ae7fc` | MERGED, ff-only | rebase clean (4 commits); `ops/grok-sandbox.toml` + classifier rule |
| `ops/agy-trial-0915-rebuilt` | `475417f` | MERGED, ff-only | rebase clean; docs only, RESTARTS none. Old `ops/agy-trial-0915` `6b7c5a4` untouched |
| `sprint-2/cards` | `2e22ff7` | **MERGED then REVERTED** | rebase clean (21 commits, no conflict); suite red → `git revert --no-edit 475417f..2e22ff7`, 22 revert commits, HEAD `b20f369`. `git diff 475417f HEAD` = empty, tree clean |

**2.4 integrated suite (P2 merged):** `4 failed, 1797 passed, 3 skipped, 2 xfailed, 15 warnings in 94.74s`.
- `tests/cobalt/test_radar_notes.py::test_dejan_budget_ceiling_45_refuses_only_above_45[71-False]` and `[73-True]` — `TypeError: load_sources() missing 1 required keyword-only argument: 'context_tickers'`. **New, and caused by the combination:** main's `src/cobalt/radar/notes.py:151` `load_sources(...)` has no `context_tickers`; the P2 worktree's `notes.py:153` adds it as a required keyword-only argument. ops-0917's own new tests call `load_sources(...)` directly (`tests/cobalt/test_radar_notes.py:170,190,216,245`) without it. P2's rebase updated the call sites that existed at its branch point, not the ones ops-0917 added to main afterwards — no textual conflict, so the desk's 07:20 `git merge-tree` dry-check could not see it.
- `test_tenancy.py::TestPlacement::test_every_table_is_on_its_ruled_side` and `::TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips` — **pre-existing on main, not caused tonight**: `git diff 91f11a1 HEAD -- tests/cobalt/test_tenancy.py tests/cobalt/test_aset_store.py tests/cobalt/test_cards.py src/cobalt/db_migrations/placement.py src/cobalt/db_migrations/cli.py` = empty. They fail because `cobalt_dev` still carries P2's 0006/0007 schema from the 09-16/17 build work.

**After the revert:** `4 failed, 1417 passed, 2 xfailed in 77.19s` — all four are the `cobalt_dev` schema-state failures above (`test_aset_store.py::test_mark_filled_refuses_the_shortcut_on_a_radar_card`, `test_cards.py::TestOneClickFill::test_a_radar_card_gets_no_shortcut`, both `test_tenancy.py` cases): `CheckViolation: … violates check constraint "aset_sizings_radar_provenance"` and `user.card_dots / system.radar_score / … : not in the placement map`. `-m "not requires_db"` does not deselect them. No code failure remains.

**2.5 validate:** exit 0 after the revert — prints `Tunable daymode.trade_count_band.min: 2` / `.max: 6` (was `PLACEHOLDER (unruled)` at preflight), `Placement (docs/PLACEMENT.md): tree clean`, `Jobs (F17): 15 registered`. §3 was never reached: **no `db migrate` ran, no `settings load` ran.**

## Smoke
§6(4) re-run at 19:24–19:26 ET, after the revert:

| check | result |
|---|---|
| sheet HTTP | `curl … http://127.0.0.1:5010/` → `200` |
| heartbeat | `HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red (2026-09-17 19:24:53 EDT)`; `radar scanning (aftermarket), members 50`; herdr AMBER as declared |
| validate | exit 0, band 2/6 live |
| prod schema | `SELECT count(*) FROM information_schema.tables WHERE table_name IN ('radar_score','radar_score_run','card_dots','card_dot_taps','radar_cards_v')` → `0` — migrations 0006/0007 never applied |
| prod settings | `SELECT key, value FROM trader_settings ORDER BY key` → 14 rows, **no `radar.cards_enabled`, no `card.*`** — nothing written |
| prod pool | `SELECT * FROM radar_pool` → `primary / scanning / degraded False / failed_stage (empty) / members 50 / last_scan_at 2026-09-17 23:24:17+00` — clean, cycling |

### Phase 2 — 20:02 ET, restarts inside the pause

`jobs register` → `registered 15 job(s)`, no plist change. `COBALT_ENV=production uv run cobalt jobs restarts 91f11a1..HEAD`, verbatim:

```
path	change	rule	restart
configs/cobalt/taxonomy/tunables.yaml	M	resident reads	com.cobalt.aset,com.cobalt.radar
docs/40 - DevDocs/reports/agy-headless-research-2026-09-15.md	A	DOCS	-
docs/40 - DevDocs/reports/agy-trial-2026-09-15.md	A	DOCS	-
docs/40 - DevDocs/reports/deploy-2026-09-17.md	M	DOCS	-
docs/40 - DevDocs/reports/grok-deny-2026-09-15.md	A	DOCS	-
docs/40 - DevDocs/reports/ops-2026-09-17.md	A	DOCS	-
docs/40 - DevDocs/reports/seat-usage.md	M	DOCS	-
ops/grok-sandbox.toml	A	HARNESS; no Cobalt reader	-
src/cobalt/jobs/restarts.py	M	static import reach	com.cobalt.radar
tests/cobalt/test_daymode.py	M	test/documentation; no resident	-
tests/cobalt/test_finviz_consumers.py	A	test/documentation; no resident	-
tests/cobalt/test_jobs_restarts.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_config.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_notes.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_propose.py	M	test/documentation; no resident	-
tests/cobalt/test_radar_sources.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset com.cobalt.radar
```

**0 UNCLASSIFIED** — the `.gitignore` gap the prompt pre-ruled never arose: that path was P2's, and P2 is reverted. Restarts run at 20:02:0x ET, inside 20:00–20:55 (L43): `launchctl kickstart -k gui/501/com.cobalt.aset`, then `…/com.cobalt.radar`.

| check | result |
|---|---|
| sheet `/` | `200` |
| sheet `/radar` | `200` |
| `com.cobalt.aset` | `state = running`, **pid 55521** (was 28187), `runs = 16`; `aset.err`: `Started server process [55528]` → `Uvicorn running on http://0.0.0.0:5010` |
| `com.cobalt.radar` | `state = running`, **pid 55534** (was 28199), `runs = 9` |
| heartbeat | `HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red (2026-09-17 20:02:23 EDT)`; `radar paused (market_reset)`; no new RED |
| radar log | `20:02:15 F17: com.cobalt.radar RUNNING` → `20:02:15 radar cycle: paused_market_reset scan_id=None` — clean start, correct pause behaviour, no traceback, no `CardSettingsError` |
| validate | exit 0 · `daymode.trade_count_band.min: 2` / `.max: 6` · `radar.finviz_max_rpm: 45` (`tunables.yaml:455-456`) · `Placement … tree clean` |
| email channel | `cobalt notify --help` **does not exist** (`invalid choice: 'notify'`) — see ESCALATE 5. Equivalent evidence from validate: `Notify: mattermost enabled, DM -> @dejan_z` — no email channel |
| radar RED count (pre-restart baseline for §7) | `grep -c "ENTERED RED: radar" logs/heartbeat.log` → **88** |

P2 smoke (§5.2) does not apply: nothing of `sprint-2/cards` is on main. No `radar_score_run` opens tonight in any case — 20:00–21:00 is outside every scanning session (plan STEP-D1 correction). The first COMPLETE `radar_score_run` is proven at the 04:00 premarket open by tomorrow's day-open, not tonight.

`git tag deploy-2026-09-17` → `7e4841e`.

**LIVE `7e4841e` · RESTARTS done: com.cobalt.aset com.cobalt.radar · ESCALATE: 5**

## ESCALATE
1. **The ops×P2 `load_sources(context_tickers)` conflict is a merge-order defect, not a P2 bug.** Neither branch is red alone. Fix before P2 is re-shipped: rebase `sprint-2/cards` on today's main and update ops-0917's four `load_sources(...)` call sites, then re-run `tests/cobalt`. A textual `git merge-tree` dry-check is not sufficient evidence for a same-evening two-branch ship; the integrated suite is.
2. **INCIDENT — production radar ran merged P2 code for ~3 minutes.** `logs/radar.err`: at 19:18:34, 19:19:55 and 19:21:16 the resident (launchd `KeepAlive`, not a kickstart by this hub) restarted into the just-merged code and logged, verbatim: `radar lifecycle polling: lifecycle card read failed: column "trade_def_slug" does not exist`; `radar S5 evaluate FAILED: CardSettingsError: "user".trader_settings has no 'radar.cards_enabled' row — the radar does not guess whether cards are on.`; `F17: com.cobalt.radar FAILED — CheckViolation: new row for relation "radar_pool" violates check constraint "radar_pool_failed_stage_check"`. Every write was refused by the prod CHECK, so no row changed; at 19:22:37 the next respawn imported the reverted code and logged `radar cycle: scanning scan_id=1789687276914`. Consequences: (a) the prompt's premise "nothing is restarted in phase 1" is false — merging into `~/cobalt` puts new code under any resident that crashes or is restarted by launchd at that moment; (b) this was a radar restart outside the 20:00–21:00 window (L43), caused by the merge, not by a kickstart; (c) it is also the proof that P2's fail-loud guards work exactly as designed against a prod DB without its migrations. A deploy shape that stops the residents before the merge, or merges into a staged checkout, is owed.
3. **`cobalt_dev` still holds P2's 0006/0007 schema**, so four DB-backed tests are red on main independent of tonight. Either reset `cobalt_dev` to main's schema or the §2.4 gate stays permanently red and cannot be used as a deploy gate. The known `db migrate --rollback` dependency failure (`cannot drop … because view "user".radar_cards_v depends on it`) is the same open item as the P2 close report's ESCALATE 12.
4. **`cobalt notify --help` is not a command.** The CLI's groups are `vault, session, cards, daymode, db, taxonomy, settings, jobs, heartbeat, backup, day-open, seat-usage, generated, radar, stop, resume, validate`; `notify` is among them in neither this release nor the prompt's assumption. The smoke step that checks "no email channel" needs rewriting against `cobalt validate`'s `Notify:` line. Prompt defect, not a production one — R12 also approved a `day-open --help` probe that was not needed tonight.
5. **Carried, not re-opened tonight:** ops-0917's own ESCALATE 1–6 (7-trades signal cost, grok directory-deny single probe, old `ops/agy-trial-0915` retirement, `GOOGLE_OAUTH_*`, context-ETF i1, Finviz cross-process gate) are unchanged by this run.

Rollback evidence (§6): snapshot `de4bfaa8` · tag `pre-s2-p2` at `91f11a1` · reverted range `475417f..2e22ff7` · nothing applied to the prod schema, so no schema rollback exists to undo (§6(5) not triggered) · residents were never kickstarted by this hub.

CONTINUE: §7 at 21:05 ET — ops-0917 + grok-deny + agy-trial-rebuilt LIVE `7e4841e`, restarts done; S2-P2 FAILED at §2.4 and REVERTED (§0, ESCALATE 1); safe state green. Session alive, do not relaunch.

