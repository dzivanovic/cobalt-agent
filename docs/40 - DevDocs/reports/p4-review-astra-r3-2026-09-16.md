FINAL ROUND 3 — 1 BLOCKER and 1 AMEND remain below; other prior findings are discharged by the amendments. R1–R7, including amended R7, stand; final dissent is recorded here, the architect's plan stands under L29/L39, and there is no fourth round.
Static review against the amended plan, R1/R2 reports, hub dispositions and cited code; P2 STEP-1/§7 read from the specified sibling worktree because absent here. No DB or production tests run. RESTARTS: none (this report only).
ESCALATE 2: sound interim unset handling; 3: sound corrected inventory investigation; 4: sound shared enforcement design with deployment explicitly gated on a conforming demand proof or Dejan's ruling; 5: sound conditional scope handling under amended R7, requiring explicit F13 rescoping if mover archival is cut.

| id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording |
|---|---|---|---|---|---|
| 1 | BLOCKER | STEP-1/STEP-6, R2-1 amendment; remaining R1-8/R1-20 | The new versioning contract still cannot execute its stated rerun. It says to insert the replacement and then set the prior row's `superseded_by`, but both rows satisfy the immediate partial unique index until that update: the replacement INSERT fails first. Reversing those two operations with the stated immediate self-FK instead references a row that does not yet exist. There is also no retirement representation when a prior miss disappears entirely: corrected bars can remove a card trigger, or a changed threshold can remove a mover miss while that mover remains active in top-N. Neither case has a successor miss id, so `superseded_by IS NULL` continues to identify the stale miss as current. Deactivating only `movers_daily` does not solve this. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:135`, `:174`, `:196`, `:204`, `:205`; `src/cobalt/archiver/store.py:82`; L57 | Add an explicit `is_current BOOLEAN NOT NULL` on USER-side missed versions and make the subject unique index partial on `is_current`. In one serialized USER reconciliation transaction, mark prior current rows non-current, insert replacements, then link replaced predecessors through `superseded_by`; retain immutable receipt payloads throughout. Retire a disappeared subject with `is_current=false` and no successor, recording the retiring run identity. Publish counts and the miss line from the successfully reconciled run only. Test the actual replacement INSERT with real uniqueness/FKs, a trigger disappearing after bar correction, a mover falling below the threshold while remaining top-N, identical rerun idempotency, and retry after the SYSTEM commit but before USER publication. |
| 2 | AMEND | STEP-5 horizon, R2-4 amendment; remaining R1-10 | The amendment now declares start-stamped bars but still admits any bar whose **start** is at or before the horizon and whose interval “has closed,” without saying closed **by the horizon**. At the 21:05 replay, a 10:30 bar is completed but contains prices through 10:31; including it for a 10:30 horizon can invent a later stop or use a later exit close. The new “min … in every case” wording also needs the explicit after-window branch to avoid resolving a horizon before the trigger while claiming to preserve that branch. | `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md:180`, `:181`, `:186`; `src/cobalt/cards/expire.py:128`, `:136`, `:141`; `src/cobalt/archiver/models.py:40` | Define `W = min(resolved_window_end, actual_session_close)` and `H = W` for `trigger_ts <= W`, otherwise `H = actual_session_close`, preserving the ruled after-window behavior. Exit/observation bars must satisfy `bar_start + 1 minute <= H`, not merely be complete at nightly evaluation time. Specify loud handling when a boundary trigger leaves no eligible completed bar; never borrow a post-horizon close. Retain the selected bar timestamps and cutoff policy in the receipt. Test a stop occurring only in the bar starting at H, an explicit half-day window, an after-window trigger, and a trigger exactly at H, asserting the precise eligible bars and exit timestamp. |
