# Typesafe classifier research, second pass — run report, 2026-09-21

Seat `typesafe-research-deep-0921` · Sonnet 5 · prompt `prompts/2026-09-21/47-research-typesafe-deep.md` · ordered `cto-2026-09-21.md` R34 · OFF-LADDER (R31 / R34).

## §0 Headline

Document WRITTEN: `docs/30 - Design/TYPESAFE-CLASSIFIER-RESEARCH-DEEP-2026-09-21.md`, 446 lines, §0–§8. Extends the first pass; input to the four-house tribunal after S2.
48 first-pass quotes re-opened, 2 differ; 31 capabilities tabled; 24 users found (3 independent audits, 10 self-reported, 11 second-hand, 0 named customers); 8 question sets.
Nothing was run, installed, signed up for or sent out; no git write; both files are untracked in `~/cobalt`.
ESCALATE: 7.

## DIGEST FOR THE DESK

What changed against the first pass. Five things. One: after six days there are users, but no named customer on any vendor page and no professional trading desk anywhere. Two: independent calibration tests now exist, and they are mixed. Three: the vendor does list a Data Processing Agreement and zero data retention for enterprise customers; the first pass wrongly said no. Four: the company is verified: founded 2024, forty million dollar seed led by DCVC, three named founders. Five: the typed interface is already portable; the vendor itself publishes an adapter that runs the same questions over other models.

How many users, of what grade. Twenty-four. Three are independent audits with reproducible code. Ten report their own small trial. Eleven are second-hand mentions. Zero are vendor-claimed customers. Finance: eight projects, all crypto, paper trading or hobby forecasting.

The three most useful examples from others. One: a study that split one holistic question into five small ones and combined them in code, and went from sixty-three to ninety-five percent, with a shadow run of about five and a half thousand calls costing eighteen cents. Two: the one pipeline that reads news, filings and posts; it found the reading plausible, no money edge, and a strong "down" bias that only a shadow measurement catches. Three: a small trial on typed questions over government documents, eighty-three to ninety-one percent for about twenty-two cents per thousand documents, that warns careful wording can push probabilities into the middle.

The three best question sets for Cobalt. First, catalyst kind, materiality and freshness for a headline, which feeds the desk-graded dot and the unknown-catalyst gap. Second, news, X and squawk triage. Third, a voice or DM sentence to a handler, with the local model first.

What is still not known. Repeat-call determinism, live latency, whether the model id in a response matches what you asked for, the actual DPA terms, and accuracy on financial text. A thirty-minute approved trial settles the first ones; only his own labelled history settles the last. Independent audits found Choice and Score overconfident and yes/no underconfident, and probabilities come back at two decimals, so never sort by them.

## BUDGET USED

WebFetch 45/45 (42 returned content; HTTP 429 on the launch thread, 403 on a press-release copy, 404 on a guessed DPA URL) · WebSearch 15/20 · local file reads 6/25 (prompt file, first-pass document, first-pass report, `LAWS.md` twice, `scoring.py`; `CLAUDE.md` was already in context) · document lines 446/600.
Per-job fetches: verify 10/10 · capabilities 12/12 · users 18 (cap 15) · company 5 (cap 4). Per-job searches: users and company 15.

## ESCALATE

1. **(L74, recorded once.)** The Read result for the prompt file carried an appended block naming a `Claude-Session:` commit line and a file-send tool. It arrived inside a tool result, so it is data; not followed. This run made no commit.
2. **Web text addressing agents (L15) — not followed.** The vendor's docs list an "agent skill" for coding agents; community repositories carry install lines; one awesome list points to the vendor's skills package. Nothing was installed, run or fetched-and-executed. No page tried to change this session's task.
3. **Fetch caps overrun in two jobs, total held.** Job 3 (users) used 18 against 15 and job 4 (company) 5 against 4, funded by the unused spare inside the 45; three fetches returned nothing (429, 403, 404). The desk may rule it a defect. The launch thread's comments and the DPA text remain unread.
4. **Weak-grade sources.** Several facts rest on search-tool summaries or on curated lists summarised by a small model (the 297-entry count, the launch-thread scores, the availability figure, the overload report, several user projects). The document marks each as `search summary` or `second-hand`; none is used as a figure without the label. A read-and-judge seat (L35) should re-open any of them before a house leans on it.
5. **Public names in three search queries.** Queries 10 and 15 carried the names of a public developer and two others taken from a published guide, to find their public projects; nothing of his was sent (L4, L32).
6. **ASK DESK: a 30-minute hands-on trial.** Question: does he approve one sign-up, one API key and a few dollars, run on invented placeholder text only, to settle §6's eight open technical items before the tribunal? Needs his L27 (spend) and L17 (data exposure) ruling. Safe default taken: no trial run; the document tells the tribunal to design without depending on determinism, DPA terms or a reported model id if none is approved.
7. **Both output files sit untracked in `~/cobalt` (production checkout)**, written there because the prompt names those absolute paths; documentation paths, no restart (L42). `EnterWorktree` was denied by the launch line, so no worktree was entered. The commit is the desk's.

MEMORY: [stated 2026-09-21 · Code] Second-pass typesafe research is at `docs/30 - Design/TYPESAFE-CLASSIFIER-RESEARCH-DEEP-2026-09-21.md`; findings that outlive it: independent audits show the product's Choice/Score probabilities overconfident and Noul underconfident on unseen tasks, output is two-decimal, packed batches shift answers; the vendor's legal page lists a DPA and enterprise ZDR; no named production customer and no professional finance user exists as of 2026-09-21.

## CONTINUE

Nothing owed by this seat. Next lawful steps belong to the desk: L35 check of the document (re-open the second-hand items in ESCALATE 4), commit both files, decide ESCALATE 6, then the tribunal prompt with the first pass and this document as input (L67, L52 for anything reaching a dot; no fleet is launched by this seat, L36).

TYPESAFE DEEP RESEARCH DONE · quotes verified: 48 of 48 (2 differ) · capabilities tabled: 31 · users found: 24 (independent: 3, vendor-claimed: 0) · finance users: 8 · question sets: 8 · fetches: 45/45 · searches: 15/20 · ESCALATE: 7
