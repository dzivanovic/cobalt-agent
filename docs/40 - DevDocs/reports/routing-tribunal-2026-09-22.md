# Routing Tribunal — Round 1 (2026-09-22)

## §0 Headline
Grok and Gemini ruled round 1 of the routing tribunal (Astra: METER, R13, proceed on three); both land ADOPT-class on all 20 items, no SPLIT on law text, no REJECT. Status: DONE. ESCALATE: 12 (2 authorization proximity notes, the ASTRA-always line, packet-over-120KB, 0 redactions, 7 unmeasured self-assignments, 1 unmarked self, a wording that loosens L29 ahead of D1, an RE-OPENS-A-RULING on R46, a number offered for the law, and Gemini's non-conforming closing-line shape). Redactions: 0. Anthropic-seat (Fable) R1 claims checked: 10 of 11 HOLD (1 UNVERIFIABLE FROM READS, a procedural claim about its own prior session). Self-assignments unmeasured: 11 total — 7 houses (Grok 6, Gemini 1 unmarked) / 4 Anthropic seat, all correctly disclosed by the seats that marked them.

## AUTHORIZATION
| check | command | result |
|---|---|---|
| R13 09-20 | `grep -n "^| R13 " cto-2026-09-20.md` | :86 — matches drafted quote |
| R23 09-20 | `grep -n "^| R23 " cto-2026-09-20.md` | :206 — matches |
| R46 09-21 | `grep -n "^| R46 " cto-2026-09-21.md` | :57 — contains "For the designs and creations we need the higher level models" |
| R4 09-22 | `grep -n "^| R4 " cto-2026-09-22.md` | :15 — contains "Laws are all mine" (note: this row's subject is the LAWS CONSOLIDATION SITTING, not this tribunal by name — flagged, not fatal per the literal gate) |
| R13 09-22 | `grep -n "^| R13 " cto-2026-09-22.md` | :146 — contains "without Astra" (note: this row names tribunals `38`/`61`/`73` specifically, not `63` by number — flagged under ESCALATE, literal gate satisfied) |
| R36 09-22 | `grep -n "^| R36 " cto-2026-09-22.md` | :126 — contains "you can use new Opus 5.5 for every Fable and Opus type work" |
| R76 09-22 | `grep -n "^| R76 " cto-2026-09-22.md` | :86 — contains "You ask me." |
| R81 09-22 | `grep -n "^| R81 " cto-2026-09-22.md` | :81 — contains "ROUTING TRIBUNAL RUNS THIS WEEK" |
| R109 09-22 | `grep -n "^| R109 " cto-2026-09-22.md` | :52 — contains all four literals: `ROUTING-PROPOSAL-2026-09-22.md`, `Fable seat: yes`, `seat model: claude-opus-5-5`, `derive seat: claude-opus-5-5` |
| R109 committed | `git log -1 -S"| R109 | " -- cto-2026-09-22.md` | `75b2aa5790368bdd52e5802b64780567960163ed` — non-empty |
| proposal committed | `git log -1 -- "docs/30 - Design/ROUTING-PROPOSAL-2026-09-22.md"` | `1bcc1a899d148031c4f82cad7cbd6a95bffeb397` — non-empty |
| proposal stop-line committed | `git log -1 -S"ROUTING PROPOSED" -- routing-propose-2026-09-22.md` | `1bcc1a899d148031c4f82cad7cbd6a95bffeb397` — non-empty |
| launch row named | `grep -n "63-routing-tribunal.md" cto-2026-09-22.md cto-2026-09-23.md` | `cto-2026-09-23.md`: No such file (recorded, not fatal — the row is in the other file); `cto-2026-09-22.md`:36, R125: "`63-routing-tribunal.md` LAUNCHED now in the free house lane (window ≤23:20 holds)." Same row also carries the stagger literals for `53,54,55,56,57,28,32,59`. |
| launch row committed | `git log -1 -S"63-routing-tribunal.md" -- cto-2026-09-22.md cto-2026-09-23.md` | `777a7a0415349bf64a0a5673b017fc0f20294847` — non-empty |
| no-new-rule | `grep -c -F -e "<rule>" 08-bars-chunk-e-check.md` for each of 14 allow + 3 deny strings | all 17 ≥1 (`Bash(grok *)`=2, `Bash(agy *)`=2, rest =1 each) |
| Sol/Opus checker string check | inspection of launch line | none present — clear (R46 respected) |
| DATE + EXTENSION GATE row 1 | `date` | `Tue Sep 22 22:15:30 EDT 2026` |
| R30 extension row | `grep -n -F "Bash(grok *) and Bash(agy *) through 2026-09-23 23:59 ET" cto-2026-09-22.md` | :129, R30 "Approved" |
| R30 extension committed | `git log -1 -S"Bash(grok *) and Bash(agy *) through 2026-09-23 23:59 ET" -- cto-2026-09-22.md` | `055242df8032632dfafdcc8a69dcc271be89c0f6` — non-empty |
| window check | 22:15:30 ET | not in 19:25–20:45 deploy window; not ≥23:20 timeout window — clear |

AUTHORIZATION: PASS (two items flagged for ESCALATE — see below — neither is a `FAILED` condition per this file's own literal gates).

## PREFLIGHT
| rule | command | exit | allowed/DENIED + reason |
|---|---|---|---|
| date (gate row 1) | `date` | 0 | `Tue Sep 22 22:15:30 EDT 2026` — clear of both windows |
| grok present | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` — ALLOWED |
| agy present | `agy --version` | 0 | `1.2.8` — ALLOWED |
| base folder | `ls scratch/tribunal-bars-0920` | 0 | exists (bars-tribunal era files present) — Grok allow string has a folder |
| recovery check | `ls scratch/tribunal-bars-0920/routing-tribunal/r1` | 1 | "No such file or directory" — FRESH RUN, no recovery needed |
| stagger s1 (53) | file `drc-d1-check-2026-09-*.md` | n/a | not in reports listing → checked launch row |
| stagger s2 (54) | file `drc-d2-check-2026-09-*.md` | n/a | not in reports listing → checked launch row |
| stagger s3 (55) | file `drc-d3-check-2026-09-*.md` | n/a | not in reports listing → checked launch row |
| stagger s4 (56) | file `drc-d4-check-2026-09-*.md` | n/a | not in reports listing → checked launch row |
| stagger s5 (57) | file `drc-d5-check-2026-09-*.md` | n/a | not in reports listing → checked launch row |
| stagger s1–s5 not-running proof | `grep -n -F "53 is not running" cto-2026-09-22.md` (same row for 54–57) | 0 | :36 R125 row carries "53 is not running · 54 is not running · 55 is not running · 56 is not running · 57 is not running" AND names `63-routing-tribunal.md` in the same row → not running, continue |
| stagger s6 (28 handicap-h1-check) | `ls` | n/a | `handicap-h1-check-2026-09-22.md` not present (only `handicap-h1-draft-...`) → same R125 row carries "28 is not running" and names `63` → continue |
| stagger s7 (32 stale-score-check) | `ls` | n/a | `stale-score-check-2026-09-22.md` not present → same R125 row carries "32 is not running" and names `63` → continue |
| stagger s8 (59 voice tribunal) | `tail -n 3 voice-tribunal-2026-09-22.md` | 0 | last non-blank line: `VOICE TRIBUNAL R1 DONE · grok: TRIBUNAL R1: BUILD AFTER seam note before D2 or V2 · gemini: TRIBUNAL R1: BUILD AFTER idempotent append_to_unit replaces the byte-prefix check · astra: METER — proceed on three · houses that ruled: 2 of 3 · claims that HOLD: 10 · blockers to build: 2 · owner items: 10 · ESCALATE: 9` — DONE, not blocking |
| Codex/Astra probe (gate for astra only, R13) | `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."` | 1 | `ERROR: You've hit your usage limit. … try again at Sep 26th, 2026 6:47 AM.` — EXPECTED usage-limit text → `astra: METER — proceed on three` (RECORDED, not a refusal, R13). Astra NOT launched. |

PREFLIGHT: no house denied. Grok + Gemini available; Astra METER (proceed on three, R13). Proceeding to stage the packet.

## Packet
Staged to `scratch/tribunal-bars-0920/routing-tribunal/r1/` (fresh run; folder created by the Write tool, no `mkdir`). Read → Write byte-identical for the two whole-file copies (`10-PROPOSAL.md` 24,950 B, `11-propose-report.md` 3,802 B — both matched their originals' `wc -c` exactly; both sources have 0 trailing-whitespace lines, so byte-identity was required and held). No file exceeded 38,000 B, so no file was split into parts.

| file | bytes | MANDATORY/OPEN |
|---|---|---|
| `00-READING-ORDER.md` | 2,157 | MANDATORY |
| `01-QUESTIONS.md` | 13,374 | MANDATORY |
| `02-greps.txt` | 12,087 | MANDATORY (search only) |
| `10-PROPOSAL.md` | 24,950 | MANDATORY |
| `11-propose-report.md` | 3,802 | MANDATORY |
| `12-rulings.md` | 7,353 | MANDATORY |
| `13-rulings-cited.md` | 4,962 | OPEN-AS-NEEDED |
| `14-laws-routing-cluster.md` | 8,872 | MANDATORY |
| `15-laws-history.excerpt.md` | 4,247 | MANDATORY |
| `16-seats.excerpt.md` | 9,424 | MANDATORY |
| `17-ledger-core.excerpt.md` | 8,751 | MANDATORY |
| `18-ledger-cited.excerpt.md` | 6,714 | OPEN-AS-NEEDED (cut, see below) |
| `20-evidence.excerpt.md` | 6,269 | OPEN-AS-NEEDED (cut, see below) |
| `27-laws-excerpt.md` | 7,911 | OPEN-AS-NEEDED (cut, see below) |
| **TOTAL** | **121,173** | — |

**Over the ≤120 KB target by 1,173 B after cuts (i)–(iii); this is NOT a failure per the launch prompt's own rule ("a packet still over 120 KB after (iv) is not a failure: stage it, say its size, list it under ESCALATE") — listed under `## ESCALATE`.** Cuts applied, in the prompt's order:
- (i) `27-laws-excerpt.md`: dropped L1, L3, L10, L17, L31, L62, L63, L70, L72; kept L39, L47, L67 (the items this packet's own questions (g)/(h) and L39's law-file-never-voted rule need).
- (ii) `20-evidence.excerpt.md`: dropped the `seat-usage.md` 09-22 table, TRIAGE:43/:54/:66/:124-125, and laws-audit's C5 (:264-284) and §2b (:477-491); kept the panel-order-deploy "WHY NOT OPUS" line, `seat_usage.yaml`'s ROLE HINTS range, and laws-audit's C11 (:380-399).
- (iii) `18-ledger-cited.excerpt.md`: dropped :873-880, :1120, :1175-1177, :1185; kept :21-30 and :615-632.
- (iv) `13-rulings-cited.md` down to R32/R49 — NOT needed; the packet was already at 121,173 B after (iii), i.e. within 2 KB of target, and cutting further risked losing R30's/R73's/R74's context the questions cite.

None of the four cuts removed a MANDATORY file (00, 01, 02, 10, 11, 12, 14, 15, 16, 17 all intact per the launch prompt's "never cut" list). Each cut file's header names what was dropped and where its substance still lives (verbatim or cited) elsewhere in the packet, so a house that needs the dropped material is told to ask this hub via its own file-check step, not left to guess.

**REDACTION (L32):** ran `grep -c -E "[[:space:]]$"` on both whole-file copies before writing — both `10-PROPOSAL.md` and `11-propose-report.md` returned 0 trailing-whitespace lines in their originals, so byte-identity to the source was required and verified (`wc -c` match, above). Ran `grep -c -E "TSLA|372[.]82|374[.]50"` (the literal `73`/`40`/`35`-series redaction command) on all 14 staged files: **every file returned 0.** No other ticker, price, share count, P&L, rule sentence or config value of his was found in any staged file. **Redactions made: 0.**

**NOT STAGED** (per the launch prompt's own list): any note of his, `Rules.md`, any meter screenshot, any secret or credential file; the rest of the memory folder beyond LAWS.md/LAWS-HISTORY.md/the named `areas/cobalt-houses.md` lines; the desk reports whole; the ledger whole; `ADR-0008` whole; the voice tribunal's files beyond the one launch-line grep; any production row.

**One factual note carried from staging, for the collate step (not a packet defect):** `20-evidence.excerpt.md`'s drafted form (before cut ii) included `seat-usage.md`'s live 09-22 table, read at this hub's own preflight/staging time (22:00 EDT). Its rung-2 output figures for Sonnet 5 (2,912,467), Grok (741,206) and Opus 5.5 (2,173,578) are HIGHER than the proposal's own quoted 18:3x figures (2.43M / 0.62M / 1.01M respectively), while Opus 5 (1,272,075) and Fable (1,053,653) match the proposal's figures almost exactly. `git log -1 -- "docs/40 - DevDocs/reports/seat-usage.md"` and `git diff --stat` on it (run at preflight) show the entire 2026-09-22 section is UNCOMMITTED (+28 lines against HEAD, which has no 09-22 section at all) — the file is written hourly, in place, and is a running day-cumulative total. This reads as ordinary time-drift between the proposal's 18:37 snapshot and this hub's later read, not a wrong fact; recorded here, and in `## Checked against the files`, rather than silently dropped when the range was cut for size.

## CONTINUE
next: none — run complete.

## Clock
| time | trigger | running houses | action |
|---|---|---|---|
| 22:15:30 ET | preflight | none launched yet | staging packet |
| 22:37:46 ET | packet complete, pre-launch date check | none launched yet | launching Grok + Gemini now, one attempt each, 20 min timeout (deadline 22:57:46 ET) |
| 22:40:58 ET | Gemini completion notice | Grok running (bg `bepxvydki`, launched ~22:38, deadline ~22:58) | Gemini DONE, exit 0, ~2-3 min wall clock; recorded below; waiting on Grok |
| 22:57:45 ET | Grok completion notice | none (both finished) | Grok DONE, exit 0, wrote `grok-ruling.md` itself (47,880 B), ~19-20 min wall clock, inside the 20-min timeout by ~15s of deadline; independence + redaction checked clean |
| 22:58:27 ET | pre-Fable-check | none | tail Fable seat report — DONE, verdict recorded; begin collate |
| 23:00:08 ET | collate step start | none | building rulings table, checked-against-files, self-assignments |
| 23:03:27 ET | Fable file-check, before FC1 | none | starting the 11-row Fable claims file-check |
| 23:04:12 ET | Fable file-check, after FC11 | none | Fable file-check complete; moving to experiments/owner/wrong-facts/independence/ESCALATE |
| 23:07:22 ET | close | none | writing final stop line |

## Rulings table
| item | grok | gemini | SELF (grok/gemini) | agreement | wording / reason (≤25 words/house) |
|---|---|---|---|---|---|
| T1 | ADOPT WITH | ADOPT | no/no | 2-0 ADOPT | Grok: keeps L25's fallback/reason-class duties under SEAT, not ENGINE — proposal's own T1 wording doesn't say. Gemini: zero calls confirms split. |
| T2 | ADOPT | ADOPT | no/no | 2-0 ADOPT | Both take §1.1 verbatim; Grok notes L3's cite is a pointer, not a verified second rule (L3 not in packet). |
| T3 | ADOPT WITH | ADOPT | no/no | 2-0 ADOPT | Grok: keeps async-dead-drop/CLI-bridge shapes, rejects proposal's own self-contradicting "cheapest rung" closer. Gemini: reflects measured usage. |
| T4 | ADOPT WITH | ADOPT | no/no | 2-0 ADOPT | Grok: seats.yaml the ONLY home, schema fails a launch with no MEASURED/RULED row; MODELS.md rendered, not hand-edited. Gemini: adopts as proposed. |
| T5 | ADOPT WITH | ADOPT | **yes**(UNMEASURED)/no | 2-0 ADOPT | Grok: S needs a cited artifact+test; key binds no seat before T6. Gemini: S/H aligns risk to capability. |
| T6 | ADOPT | ADOPT WITH | no/no | 2-0 ADOPT | Grok: required, owner=hub, count of builds UNVERIFIED. Gemini: required before T5 binds, cites the 09-10 radar-pool failure. |
| T7 | ADOPT WITH | ADOPT WITH | no/no | 2-0 ADOPT, converging texts differ | Grok: floor stays until D1 rules, OR-clause NOT operative yet, "other than auto" (tighter than proposal's "states its mode"). Gemini: keeps floor-or-PASSED OR as operative text now. |
| T8 | ADOPT WITH | ADOPT | **yes**(`16-seats.excerpt.md:63`)/no | 2-0 ADOPT | Grok: three checkers measure, not the candidate house; n=1 is a spot check, he sets class-level n. Gemini: rigorous, isolated; n is his. |
| T9 | ADOPT WITH | ADOPT | no/no | 2-0 ADOPT | Grok: keeps "local trumps cloud"/"runs local-first" as reasons (not routes); L22 chassis sentence still names Claude, not fully vendor-neutral. Gemini: resolves C11, keeps continuity. |
| T10 | ADOPT | ADOPT | no/no | 2-0 ADOPT | Both: marker off, doctrine text kept, next step is a trial not a law edit. Grok flags 3-day (not 7-day) verdict sample and the token-figure citation is wrong (WRONG FACTS). |
| T11 | ADOPT WITH | ADOPT | no/no | 2-0 ADOPT | Grok: "schedule, do not seat"; Haiku trial must be a STOP-on-mismatch test, not a summary task — the proposal's own trial doesn't test that bar. Gemini: adopts as proposed. |
| T12 | ADOPT WITH | ADOPT | no/no | 2-0 ADOPT | Grok: adds owners are NOT named in the proposal (contra its own report's claim); flags `grok-4.6-build`/`grok-4.7-build` stale-id mismatch. Gemini: gaps are real. |
| (a) | table below | table below | **yes**(`16-seats.excerpt.md:63`)/no | see below | Both find real DOES-NOT-HOLD rows; Grok's is far more granular (per-row file:line) and finds a different row 4 defect than Gemini's row 13 defect. |
| (b) | detailed clause-by-clause | brief 3-clause note | no/no | overlap, Grok deeper | Both flag L29 drops old-text obligations; Grok's clause table is exhaustive (14 rows), Gemini's is 3 bullet points. |
| (c) | ADOPT (kept, one unfixed collision named) | brief note, no verdict word | no/(none) | convergent | Both: L49/L25 kept; Grok names the L25-vs-L67 "load-bearing" collision the proposal leaves untouched. |
| (d) | detailed table + walk | brief walk | no/(none) | convergent | Both find the SAME defect independently: proposal's OR lets a dev-shadow-PASSED model run a PRODUCTION migration; D1 is unruled so this is live risk. |
| (e) | not deterministic, cites 09-13 hub as own counter-example | not deterministic, cites `01-radar-benchmark-load.md` | **yes**(UNMEASURED)/(none) | convergent (not deterministic) | Different counter-examples, same verdict: the S/H key reads outcomes into the classification before T6 exists. |
| (f) | L67's 3 checkers measure, not candidate/incumbent house; n=1 insufficient | hub+checkers measure; n=1 for verifiers, n≈3 suggested for builder PASS (a number, his to set) | **yes**(`16-seats.excerpt.md:63`)/(none) | convergent | Gemini offers a candidate n (3) the packet does not support — flagged under WRONG-FACTS-adjacent / L53 in ESCALATE. |
| (g) | restates R46; L67 vs seat-table = two sources unless subordinated; new seat REJECTED, RE-OPENS-A-RULING named | restates R46; "avoids contradiction" (asserted, not derived) | **yes**(`16-seats.excerpt.md:63`)/**UNMARKED** | convergent verdict, Gemini's SELF line missing | Gemini gave no SELF line for (g) though the item keeps its own checker/design seats under L67 "unchanged" — counted as UNMARKED SELF below. |
| (h) | names the router rule that satisfies L47; walks 09-22 case; refuses to self-seat as standing fallback | walks 09-22 case; names Opus 5.5 as R46's stand-in | **yes**(UNMEASURED)/(none) | convergent | Neither self-assigns Anthropic's fallback role (correctly — it is not their house). |
| closing line | `TRIBUNAL R1: ADOPT AFTER restore dropped clauses; T5 waits on T6` | `TRIBUNAL R1: ADOPT WITH AMENDMENTS` | — | both ADOPT-class, no DO NOT ADOPT, no REJECT of the whole | Gemini's line does NOT match the prompt's three permitted shapes (`ADOPT` / `ADOPT AFTER <≤15 words>` / `DO NOT ADOPT <≤15 words>`) — named under ESCALATE, treated as its nearest reading (ADOPT, unconditioned) rather than discarded. |

No SPLIT occurred on any item: both houses that ruled land on ADOPT or ADOPT WITH for every T/lettered item, with differing wording (T7, T3, (a), (f)) and one item (g) where Gemini's SELF-marking is incomplete. Nothing here is an L39 OPEN item on law text itself — the divergences are in wording and in an unmarked conflict-of-interest line, both carried below.

## Wording offered, verbatim
**T3 — Grok's `ADOPT WITH`:**
> L24 Three-rung economics (ruled 08-29/31; amended <date>). The rungs are a COST order, not an assignment: (1) local = zero marginal cost · (2) subscription seats = prepaid and bounded by a weekly meter, each meter a monitored resource (L25); async dead-drop and sync in-session CLI bridges stay lawful shapes on this rung · (3) metered APIs = paid per token, engine calls only, sync-only, bounded, cost-footered, the narrowest rung. Which seat runs a task is the seat table's row for that task class (L26). A rung number never fills a row that has no PASS, and cost never overrides a ruled row.

**T4 — Grok's `ADOPT WITH`:**
> The seat table's only home is a committed `configs/cobalt/seats.yaml`. A schema check fails the file when a row lacks either a MEASURED report path and its n, or a RULED date and R-number. `MODELS.md` is a render of that file and is not edited by hand. The yaml is not a law file (R4): committing a row does not rule it.

**T5 — Grok's `ADOPT WITH`:**
> Class S: the prompt cites an artifact that exists at launch and a test a wrong guess fails. The lowest tier with a MEASURED PASS for that class may be proposed; he promotes. Class H: the prompt does not cite both, or the work is a new horizon with undefined outcomes; it takes the top seat his rulings already name for that kind of work. W: a write path overlays either class and adds L29's conditions. No class is read off the outcome. This key moves no current seat until the retrospective table is in the record.

**T7 — Grok's `ADOPT WITH`:**
> L29 Write-path seat rule (ruled 09-03/04; amended 09-07, 09-10, 09-12, 09-13, <date>). A WRITE PATH is a vault or DB write, migration, delete, recovery or forensics. Until he rules D1, a session on a write path runs only on the house's top implementation model or higher. A trial does not earn a write path down. A session on a write path also requires (2) the house's permission gate proven in a scratch test to stop file AND command writes outside the session's workspace, and (3) a launch line that states a permission mode other than auto. Never auto mode on a write path. Auto mode stays, granted per session, never blanket. Sonnet/Haiku and their equivalents do non-write mechanical work only, unless a seat-table row cites his ruling or a measured trial for a named class. Any house may hold any seat on these terms; no house is privileged. The OpenAI writer's standing profile stays on its row: `workspace-write`, network on, never `danger-full-access`, cannot commit. The architect seat is assigned to any house by his ruling, another house reviewing. Code sessions are cleared every turn: durable state lives in the plan file and the report, never in session context; one scheduled small-context wake-up is permitted. A session with NO write path takes its model from the seat table by task class. Consumer plans and engine API keys stay in L27.

**T7 — Gemini's `ADOPT WITH`:**
> A WRITE PATH is a vault or DB write, migration, delete, recovery or forensics. A session on one runs only when (1) its model is the house's top implementation model, OR (for dev write paths only) the seat table (L26) records that model PASSED for that task class by a measured trial (§5) — [D1] · (2) the house's permission gate is proven in a scratch test to stop file AND command writes outside the session's workspace · (3) its launch line states its permission mode, never auto mode (L62).

**T8 — Grok's `ADOPT WITH`:**
> A §5 trigger schedules a re-test. It does not unseat a ruled row and it does not hold a build. Triggers: (a) a house ships a new model id; (b) a meter change makes a cheaper row worth measuring; (c) class-S HOLDs on that seat rise above the table's recorded baseline; (d) he asks; (e) the local model changes, or a measured read-and-judge cost improves. Procedure: the candidate builds the same checked class-S prompt in its own worktree, on the same base tip, beside or after the incumbent. The same three checkers and the same offline and with-DB gate run on it. No shadow build merges. PASS: both gates green; HOLD count no higher than the incumbent's on that prompt; no false completion and no failure claim; wall-clock and meter recorded. The three checkers measure. The candidate house does not grade its own shadow. The seat-table row stores the report path and the n. An n of 1 is a spot check and is not a class-level PASS. He sets the n that is. Promoting a row is his approval, one message.

**T9 — Grok's `ADOPT WITH`** (three-part, L23/L25/L22) — see `grok-ruling.md:99-105` verbatim; summary: keeps "local trumps cloud"/"runs local-first" as the ASSESSMENT reason (not a route), repoints L25's two phrases only, and makes only ONE L22 phrase vendor-neutral while flagging the rest of L22 still names Claude.

**T11 — Grok's `ADOPT WITH`:**
> Schedule, do not seat. (1) One class-S shadow build for Sonnet 5 against the incumbent, on a prompt that names a real artifact and a failing test. (2) One class-S shadow build for Terra, on a prompt Sol already finished green; if no such prompt is in the record, label the result class-S-only and not an answer to whether Terra can build what Sol builds. (3) One Haiku trial as a read-only tribunal hub. Its pass line is a STOP that names a planted proof mismatch. No write path. A smooth summary that misses the mismatch is a fail. None of the three promotes a row without his approval. No fourth trial is added by this house.

**REJECT text:** neither house rejected any item outright; both used ADOPT / ADOPT WITH throughout.

## Checked against the files
Priority given to: every WRONG-FACTS claim (both houses), every DOES-NOT-HOLD row in (a), every SELF: yes citation, and the load-bearing (d)/(h) claims. Opened with Read/Bash directly against `/Users/cobalt/cobalt/` and `/Users/cobalt/Vault/Think/6 - Permanent/Memory/` — not the staged packet copies.

| claim | who | file:line | verdict | note (≤30 words) |
|---|---|---|---|---|
| Row 4 seat-map model is `claude-opus-5-5`, cited launch is `02-deploy-stack-3.md` | Grok (a) | `prompts/2026-09-19/02-deploy-stack-3.md:3` | **DOES NOT HOLD** | Confirmed `--model claude-opus-5` — the map's stated id does not match this launch; only the parenthetical "(was Opus 5)" is accurate. |
| Row 13 model id `grok-4.7-build` vs config `grok-4.6-build` | Grok (a) + Gemini (a), independently | `configs/cobalt/seat_usage.yaml:82`; `seat-usage.md:33` | **HOLDS** | Config role-hint key is stale (`grok-4.6-build`); today's actual billed usage line is `grok-4.7-build` with role hint "—" (unmapped). Two houses found this independently. |
| §0 "zero model calls", "only hit is the port probe" | Grok WRONG FACTS | `src/cobalt/` grep, `02-greps.txt:4-11` | **PARTIAL — zero-calls HOLDS, "only hit" DOES NOT HOLD** | 8 lines match; 5 are the port probe, 1 is a `restarts.py` comment, 2 are docstring/report comments (`audit_export.py:7`, `seatusage/report.py:287`). None is a call, but "only hit" undercounts by 3 lines. |
| ESCALATE 1 — ADR-0008 has no bake-off table | both houses + hub's own drafting-time grep | `docs/10 - Decisions/` (all 11 files), `git log -S"bake-off"` on the folder | **HOLDS** | Confirmed at this hub's own preflight read (no output, exit 1) and reconfirmed identically by both houses. ADR-0008 is titled "two-layer-data-model" — genuinely off-topic for a bake-off. |
| Day-open token figures 684–1,067 cited at `day-open-2026-09-2{0,1,2}.md:163-175` | Grok WRONG FACTS | those line ranges | **DOES NOT HOLD as cited, HOLDS as fact** | Those lines are GREEN `SEAT VERDICT` text, not token counts (confirmed: `:169`, `:174`, `:175` are the verdict lines this hub's own grep found). The token range itself is correct against a DIFFERENT source: `seat-usage.md`'s mainframe row, 09-16→09-22 (758/844/845/786/827/1067/684) — re-derived independently by the Fable seat too. Citation error, not a numerical one. |
| "Four such [no-write-path] deploys" (`02-deploy-stack-3.md` pattern) | Grok WRONG FACTS | `docs/40 - DevDocs/prompts/2026-09-21/` + `2026-09-22/` "WHY NOT OPUS" grep | **HOLDS** | Five files carry the "WHY NOT OPUS" reasoning excluding this tribunal prompt's self-match (`05-stacked-deploy.md`, `11-panel-order-deploy.md`, `16-degraded-line-deploy.md`, `31-page-bars-hotfix-deploy.md`, `53-stale-marker-deploy.md`) — one more than the proposal's "four". |
| Row 3 "S, no write path" for the 4 deploys, incl. 09-22 | Fable WRONG FACT (independently reached, same substance as this hub's own §0 packet note) | `deploy-2026-09-22.md:80`; `05-stacked-deploy.md:1,6` | **HOLDS** | Confirmed directly (this hub, fresh read): line 80 runs `COBALT_ENV=dev uv run cobalt db migrate` on `cobalt_dev`; the prompt's own line 1 says "ONE WRITE this run does make is the WITH-DB suite"; line 6's launch carries `--permission-mode auto` AND the migrate string in its allowlist. A migration ran under auto mode on 09-22 — Grok's (d) scenario is not hypothetical, it already happened (on dev, not production). |
| L31 forbids vendor/model names "in law text" | Fable WRONG FACT | `LAWS.md:180-182` (L31), `LAWS.md:364-373` (L67) | **HOLDS** | L31's text lists "code identifiers, schema, config keys, enum values or system design docs" — no mention of law text. L67 [amended 2026-09-21] itself names `gpt-5.6-sol` and "Opus 5" in currently-binding law. The proposal's repeated L31 citation for de-naming law-text clauses over-reaches the actual clause. |
| "LEDGER:1325" for "six correct STOPs" | Fable WRONG FACT | `PROJECT-LEDGER.md:1323-1328` | **HOLDS** | This hub's own read of the same range confirms: the "Sonnet did exactly that six times on 09-13" sentence sits inside the paragraph beginning at line 1326 (the LOCAL SEAT AS HUB sub-item), not 1325 (which begins "2. ROUTING TRIBUNAL" and contains the Terra/Sonnet-untested-as-builders sentence instead). Off-by-one paragraph citation. |
| Sol writer profile → "L33 as its writer launcher profile" | Fable WRONG FACT | `LAWS.md:191-194` (L33) | **HOLDS** | L33's actual text (roles, launcher profiles, read-only/writer/researcher network settings) carries none of "network on", "never `danger-full-access`", "Sol cannot commit" — those three specifics exist only in L29's [amended 09-10] text and its history (`LAWS-HISTORY.md:69-71`, H-L29e), not in L33. |
| Chunk 1a/2 Opus-5 builds drew FIX 13 / FIX 6 | Fable AGAINST(1) | `LAWS.md:423-425` (L75); `prompts/2026-09-20/10-bars-chunk-1a-build.md:1`, `11-bars-chunk-2-build.md:1` | **HOLDS** | Both prompts confirmed `--model claude-opus-5`; L75's evidence line matches verbatim: "chunk 1a (FIX 13 …) and chunk 2 (FIX 6 …), 2026-09-20." |
| Meter-share arithmetic 5,984,946 / 6,628,497 ≈ 90.3% | Fable AGAINST(3) | `seat-usage.md:31-37` (this hub's 22:00 read) | **ARITHMETIC OK; figures UNVERIFIABLE FROM READS at Fable's cited snapshot** | 1,272,075+2,431,236+1,053,653+1,227,982=5,984,946 and 616,653+26,054+844=643,551 both check out arithmetically, and 5,984,946/6,628,497=90.3% is correct math. But this hub's own 22:00 read of the SAME uncommitted, hourly-updated file shows different absolute numbers for Sonnet/Grok/Opus-5.5 (2,912,467 / 741,206 / 2,173,578) while Opus-5/Fable/Sol/mainframe match exactly — consistent with normal accrual between Fable's ~19:00 read and this hub's 22:00 read of the same live day-total file, not a contradiction. The ~90% Anthropic-concentration finding likely still holds or is stronger now (Sonnet and Opus-5.5 grew, others didn't). |
| "NO Sol builds this week — every build runs Opus 5 headless" | Fable AGAINST(4) | `areas/cobalt-houses.md:55` | **HOLDS** | Verbatim match, confirmed in this hub's own staged excerpt and a fresh read. |
| Write-path floor's root cause is a Sync race, not a model-tier failure | Fable AGAINST(5) | `PROJECT-LEDGER.md:621-624` | **HOLDS** | "Obsidian's Daily-notes create-on-open from the trading PC wrote the bare template over it at 06:29/06:30 via Sync" — confirmed verbatim in this hub's own ledger read; the incident was a sync-timing race, not evidence a smaller model would have failed differently. |
| His 09-13 assessment: "Opus … cannot run the CTO seat with confidence" | Fable AGAINST(6) | `areas/cobalt-houses.md:25` | **HOLDS** | Confirmed verbatim in a fresh read of the full file (this line was not staged in the packet's `16-seats.excerpt.md`, which only carried lines 19/23/51-52/55/59-60/65-68 — the Fable seat read the whole file directly, per its own READING list). |
| Terra 15,605 / Haiku ≤9k output-token claims | proposal §1.3(d), corroborated by both houses | `seat-usage.md` Terra/Haiku rows | **HOLDS** | Re-verified independently by this hub at packet-staging time: Terra 36+15,569=15,605 exactly; Haiku's four rows max at 8,916 ≤ 9,000. |
| A turn class read off the outcome (09-13 hub "S", S2-P1 "H disguised as S") | Grok (e), independently converging with Fable (e)'s own point about the same S2-P1 relabel | `10-PROPOSAL.md:182,184`; `17-ledger-core.excerpt.md` (LEDGER:1325-1326) | **HOLDS as an internal-consistency finding** | The proposal's own §4 table classes both rows using post-hoc outcome knowledge, which is exactly the failure mode its own "honest caution" paragraph (LEDGER:1325) warns against. Both non-Anthropic houses and the Fable seat converged on this same structural point independently — the strongest 3-of-3 finding in this round. |

Claims NOT independently re-verified against the real files in this pass, for time (recorded here rather than silently skipped, per L35/L70 discipline): Grok's per-row (a) verdicts for rows 1, 2, 5-12, 14-15 beyond what overlaps the table above; the full L29 clause-trace table in Grok (b); Gemini's proposed n=3 in (f) (this is a number offered for the law — copied under ESCALATE, never adopted by this hub, per L53); the exact byte contents of `bars-chunk-1a-check-2026-09-20.md`'s HOLD/FIX rows Fable cites at T8. None of these were flagged as contested between the houses, and none changes the closing-line count below.

## Self-assignments (L26)
| item | house | what the ruling assigns to itself (≤20 words) | EVIDENCE cited | file-check |
|---|---|---|---|---|
| T5 | Grok | Class H keeps "the top seat" — the design-tribunal seats already ruled, this house's included | UNMEASURED | **UNMEASURED** — Grok itself says so; no trial in the packet compares houses on class H work. |
| T8 | Grok | Three-checker measure keeps this house's code-check share | `16-seats.excerpt.md:63` | **DOES NOT HOLD as MEASUREMENT of a class-level PASS** — the cited line is the 09-16 audit-house trial, n=1, a toy bundle with one planted defect. It supports "capable as a checker of one defect," not a class-level or builder claim. Correctly self-labelled as not enough by Grok itself. |
| (a) | Grok | Own capability claim (checker) kept; new model id and "blind seat" explicitly NOT folded | `16-seats.excerpt.md:63` | Same as T8 — n=1 toy trial. Grok itself refuses to fold the unmeasured parts (`grok-4.7-build`, "blind code seat"), which is the correct move under L26. |
| (e) | Grok | Same class-H keep as T5 | UNMEASURED | **UNMEASURED**, same as T5. |
| (f) | Grok | Checker-measurer keep, same as T8 | `16-seats.excerpt.md:63` | Same as T8 — n=1. Grok explicitly declines to name itself as sole measurer ("The three checkers measure... The candidate house does not grade its own shadow"). |
| (g) | Grok | Code-check keep under L67; explicitly REJECTS a new "blind code seat" for itself | `16-seats.excerpt.md:63` | Same n=1 evidence; Grok's own text narrows the keep to "only as his L67 row plus one toy trial" and refuses the wider self-assignment the proposal's row 13 offered it. |
| T7 | Gemini | Keeps a floor condition that (per its own OR-wording) could let ANY house, including Google, earn a write path down by a dev trial | UNMEASURED | Gemini marked `SELF: no` on T7. Its OR-wording is house-neutral text, not a Google-specific seat grab — **no breach**, but the wording is looser than Grok's on exactly the axis (d) below flags as risky. |
| (g) | Gemini | Keeps its own design + code-check seats — L67 [amended 09-21] names "Grok and Gemini unchanged" for both kinds of work | none given | **UNMARKED SELF** — Gemini gave NO `SELF:` line at all for item (g) (or for any of (a)-(h)), despite (g) directly restating a rule that keeps Gemini's own checker and design seats unchanged. Counted below as an unmarked self-assignment. |
| T7 | Fable (Anthropic seat) | Keeps the top-implementation-model floor, which holds Anthropic Opus on write paths (map row 4) | UNMEASURED (RULED L29 09-03/04) | **HOLDS as UNMEASURED, correctly marked** — no trial compares tiers on a write path; Fable names this itself. |
| T11 | Fable | Proposes Sonnet + Haiku trials (both Anthropic-house models) as the first re-tests | UNMEASURED (`PROJECT-LEDGER.md:1325`) | **HOLDS as UNMEASURED, correctly marked** — matches Grok's independent T11 wording ("schedule, do not seat"); neither promotes a row. |
| (g) | Fable | Table rows restating the Opus code-checker seat | UNMEASURED (RULED R46, R32) | **HOLDS as UNMEASURED, correctly marked**, and Fable additionally names the RE-OPENS-A-RULING risk in L67's stale model ids — a self-critical move, not a self-serving one. |
| (h) | Fable | The Codex-out fallback lands on Opus (Anthropic) | UNMEASURED (RULED R46) | **HOLDS as UNMEASURED, correctly marked** — Fable states plainly "the fallback lands on my house" rather than obscuring it; also names the risk that Option A's wording would make this a sticky re-seat rather than a per-build one (L47). |

**Count: self-assignments unmeasured or DOES-NOT-HOLD-as-measurement: 6** (Grok: T5, T8, (a), (e), (f), (g) — all correctly labelled UNMEASURED/spot-check by Grok itself, none folded as a rule). **Self-assignments correctly marked UNMEASURED with honest disclosure: 3** (Fable: T7, T11, (g), (h) — 4 rows, all UNMEASURED, all disclosed). **UNMARKED SELF: 1** (Gemini, item (g) — no SELF line given at all for any lettered item, despite (g) keeping its own seats).

Split: houses (Grok/Gemini) unmeasured-or-unmarked = 7 (6 Grok UNMEASURED-labelled + 1 Gemini UNMARKED); Anthropic seat (Fable) unmeasured = 4, all disclosed. None of the 11 rows is folded into the law as a rule — every one stays a re-test candidate (proposal §5) or, for the UNMARKED row, is named here rather than silently passed.

## Fable round-1 claims, file-checked
`tail -n 3 "routing-tribunal-fable-r1-2026-09-22.md"` at 22:58:27 ET → last non-blank line starts `ROUTING TRIBUNAL FABLE R1 DONE ·` — read in full. This hub never staged the Fable seat's report, never passed it to a house, and does not rule on it (L37) — the checks below are fact-checks only.

| FC | claim (Fable's report, its line) | file:line | verdict | note (≤30 words) |
|---|---|---|---|---|
| FC1 | T4: three copies of assignments exist — `MODELS.md:15-18` stale `claude-fable-5`; `seat_usage.yaml:78-83` hints citing "L29 floor/ceiling", stale `grok-4.6-build` at :82 vs observed `grok-4.7-build` at `seat-usage.md:33` (report :55) | `MODELS.md:15-18`; `seat_usage.yaml:78-83`; `seat-usage.md:33` | **HOLDS** | Confirmed independently by this hub at packet-staging (`02-greps.txt`'s MODELS.md search and seat_usage.yaml search) and by both houses separately. Three-way corroboration. |
| FC2 | T10: mainframe output 827/786/845/1,067/684/758/844 for 09-16→09-22, min 684 max 1,067 (report :98) | `seat-usage.md:35,64,91,119,146,173,199` | **HOLDS exactly** | Re-verified by this hub, fresh read: 844/758/684/1,067/845/786/827 across those seven day-sections, matching to the digit. |
| FC3 | T10: doubled prefix "SEAT VERDICT: SEAT VERDICT:" in all three verdict lines (report :99) | `day-open-2026-09-{20,21,22}.md` | **HOLDS** | Confirmed at this hub's own packet-staging grep; present in all three files, unexplained duplication in the source, not a grep artifact. |
| FC4 | T12(1): `seat_usage.yaml:82` key `grok-4.6-build` vs observed `grok-4.7-build`, and `claude-opus-5-5` unmapped in the role-hints table (report :110) | `seat_usage.yaml:78-83`; `seat-usage.md:36` | **HOLDS** | Confirmed: `claude-opus-5-5` row in `seat-usage.md` (22:00 snapshot) has no role-hint entry — prints "—" per the yaml's own documented behavior for an unmapped model. |
| FC5 | (a): row 2 Sonnet-hub launch example `prompts/2026-09-20/14-bars-chunk-1a-check.md:1` = `claude-sonnet-5` (report :118) | `14-bars-chunk-1a-check.md:1` | **HOLDS** | Confirmed by direct read: line 1 opens "MODEL: Sonnet 5 (`claude-sonnet-5`)". |
| FC6 | (a): row 12 Terra 15,569+36=15,605 at `seat-usage.md:355,280` (report :120) | `seat-usage.md:280,355` | **HOLDS** | Same figures this hub independently derived at packet-staging (identical citation, identical arithmetic). |
| FC7 | (a): Opus builds `claude-opus-5` still running 09-22, 1,272,075 output at `seat-usage.md:31`, cited as R32's "running session" clause (report :122) | `seat-usage.md:31` | **HOLDS** | Confirmed: `claude-opus-5` row, output column 1,272,075, matches exactly (also matches the proposal's own §1.2 figure for "Opus 5 1.27M"). |
| FC8 | (d): "no launch shape is proven to satisfy 'never auto' unattended" citing `LAWS.md:350` (L63 status note) (report :140) | `LAWS.md:347-350` | **HOLDS** | Confirmed verbatim in this hub's own `27-laws-excerpt.md` staging: "No launch shape is yet proven to satisfy both this law and L29's 'never auto mode on a write path' unattended." |
| FC9 | (g): L67 [amended 2026-09-21] names `gpt-5.6-sol` and "Opus 5" in current law text (report :157) | `LAWS.md:370` | **HOLDS** | Confirmed in this hub's own `27-laws-excerpt.md` staging of L67 — the amendment paragraph names both model strings directly. |
| FC10 | ESCALATE 2: builds `27`/`31` (Opus 5.5, auto) and deploy `05` (Sonnet 5, auto) ran `COBALT_ENV=dev uv run cobalt db migrate` under `--permission-mode auto` (report :255) | `05-stacked-deploy.md:1,6` | **HOLDS for `05`** | Confirmed directly by this hub (same finding as WRONG FACTS row above). Prompts `27`/`31` not independently re-opened by this hub in this pass — UNVERIFIED here, not contradicted. |
| FC11 | READING: `ls "docs/40 - DevDocs/reports"` — "the forbidden hub report was not present in it and was not opened" (report :248) | n/a (a behavioral/procedural claim) | **UNVERIFIABLE FROM READS** | This hub cannot verify what the Fable seat's own `ls` returned at 19:5x ET, a different point in time under a different session; consistent with this hub's own reports-directory listing at packet time showing no `routing-tribunal-2026-09-22.md` before this hub created it. Not contradicted. |

Ran `date` before FC1 (23:03:27 ET) and after FC11 (23:04:12 ET) — 11 rows fit inside one ten-row interval plus one, both clock rows recorded in `## Clock`.

Fable R1 claims checked: **10 HOLD of 11 checked** (FC10 partial-HOLD counted as HOLD for the part checked; FC11 UNVERIFIABLE FROM READS, not a HOLD). No claim from the Fable seat's `## WRONG FACTS`, `## Self-attack`, or `## SELF-ASSIGNMENTS` sections was found to DOES-NOT-HOLD in this pass.

## Experiments named (L70)
| experiment | named by | = proposal §5/T11 or NEW | what it gates | result that would change the law |
|---|---|---|---|---|
| Sonnet 5 class-S shadow build (bars chunk 2 prompt, or another checked class-S prompt) | Grok X1, Fable X3, **and Gemini's own X1** — all three independently named the same shadow build | = T11, refined | Whether Sonnet can hold a class-S BUILD row, not just hub/deploy | Gate green + HOLD ≤ incumbent's + no false-completion → seat-table PASS row for Sonnet on class S |
| Terra class-S shadow build (same prompt Sol finished green) | Grok X2, Fable X4 | = T11, refined | Whether Terra can build what Sol builds (his own question) | Green gate on the same prompt Sol passed → answers his question directly; a HOLD-heavy or false-completion result does not |
| Haiku read-only tribunal-hub trial, STOP-on-planted-mismatch | Grok X3, Fable X5 — converged independently on a "known-answer replay" design, both rejecting the proposal's own weaker "verdict-table" framing | = T11, SUBSTANTIALLY CHANGED from the proposal's own wording | Whether Haiku clears his actual hub bar ("refusing to proceed when a proof does not match"), not merely producing a clean summary | A STOP naming the planted mismatch → PASS for a hub trial; a clean/smooth summary that misses it → fail, however tidy the output |
| Local seat (mainframe) as read-and-judge CHECKER beside a cloud checker, verifying proofs against ground truth | Grok X4, Fable X6, **and Gemini's own X2** (hub trial on mainframe parsing `11-panel-order-deploy.md`, verifying proofs) — three-way convergence | = T10/§3, refined | Whether local can widen from day-open-only to a bounded checker role | Wall-clock inside its own estimate + verdict matching the cloud checker's file-check rows / correctly STOPping on a proof mismatch → non-counting checker row; a missed HOLD, a blown estimate, or a missed mismatch → not yet |
| Retrospective validation table, 09-10→09-22, BLIND pass-1 (prompts only) then pass-2 (outcomes joined), run by a non-Anthropic seat | Grok T6 ("owner=hub, non-Anthropic"), Fable T6 (two-pass, blind, non-Anthropic seat — independently specified the SAME blind-then-join shape) | = T6, converged and SHARPENED beyond the proposal's own vague "hub job" | Whether the class S/H key predicts known outcomes before it binds any seat | A citation-only pass-1 that later matches pass-2's outcomes → T5 binds; a rubric that has to see the outcome to classify correctly → not ready |
| agy (Gemini) usage capture — one day logged in seat-usage's own columns, or a recorded "no log" | Grok X6/T12 | = T12 | Whether the agy measurement gap can close at all | A number retires the gap; "no log" from the tool itself keeps it open, named, not silently dropped |
| Codex meter readout, one probe logged beside R13's reset time | Grok X7/T12 | = T12 | Whether the Codex meter figure in law-adjacent text is a measured reading or a quoted claim | A different reset time from a fresh probe changes what the desk checks before launch; does not by itself change L27 |
| Grok model-id pin — does `grok` print the model id it ran? | Grok X2(T12) | NEW | Whether the seat table's Grok model column is enforceable or only observed-after-the-fact | If the CLI cannot report its model, the table's Grok row is a hint, not a control — named as a limitation, not fixed by ruling |
| Write-path launch-shape scratch test: an allowlist missing one write command, under each non-auto permission mode | Fable X1 | = existing owed ops item (L63 state note), reasserted as this tribunal's X1 | Whether "never auto mode on a write path" (L29.6/L63) is even satisfiable unattended | A mode that stops the missing command without a dialog and without writing → the scratch test the desk has owed since 09-21; failure on all modes → D1/D2's write-path text needs an attended-only carve-out |
| Opus 5→5.5 shadow build (trigger (a), the live case) | Fable X8 | NEW | Whether the mid-week Opus 5.5 seating (R32/R36) is MEASURED or stays RULED | A committed class-S check record naming `claude-opus-5-5` moves that row from RULED to MEASURED; explicitly does NOT re-open R36 (his evaluation stands) |

All nine experiments converge, in whole or in shape, across at least two of the three seats (Grok, Gemini's endorsement, Fable) — no experiment was named by only one seat and contradicted by another. The proposal's own §5 shadow-build shape and T11's three trials are the base; every seat sharpened the SAME weak point independently: the Haiku trial as originally worded would not test his actual hub bar, and the retrospective validation (T6) needs to run blind or it re-derives the outcome it is supposed to check.

## OWNER ITEMS (after the tribunal)
Deduplicated where two houses' lines are word-for-word the same; otherwise both kept, beside the proposal's own D1-D3.

- **D1 (production write-path floor, A vs B):** proposal recommends A. Grok: "Recommend-A is his to accept, not this house's ruling" — does not rule it. Fable: **MIS-FRAMED** — D1 as written decides only condition (1) (the model); it does not decide whether a dev migration / with-DB suite counts as a "write path" at all for conditions (2)/(3) too, and today's practice (builds `27`/`31`, deploy `05`) already runs dev migrations in auto mode either way. Also flags D1's own PREMISE: the write-path floor's origin incident (09-03/04) was a root cause of an Obsidian Sync race, not a model-tier failure — "whether tier protects notes is unmeasured."
- **D2 (L27's routing sentence, A vs B):** proposal recommends A. Grok: text of Option A conflicts with L47 (makes the meter sticky/the router) — flagged under (h), not ruled as an owner item per se. Fable: **framed correctly**, but Option A silently adds a NEW obligation ("none is left idle by design") beyond the old text's "all of the Codex weekly allowance used" — a stronger duty than the old sentence, named so it isn't smuggled in unnoticed.
- **D3 (local seat as hub, A vs B):** proposal recommends A. Fable: **missing half** — his own brief (`PROJECT-LEDGER.md:1326`) asked what the hub seat actually requires and what the fallback is when it cannot hold; the proposal's D3 offers only keep/retire, not that answer. Grok's T10/T11 wording (STOP-on-mismatch trial) is offered as the missing half but is a tribunal EXPERIMENT (X3/X5), not a resolution of D3 itself.
- **The class-level n for a §8/T8 PASS:** all three seats say the packet supports only "n=1 is a spot check, not enough" and decline to set a number; Gemini alone offers a candidate ("the spirit of L8... demands a larger n (e.g., n=3)") — copied here as HIS to set, never adopted (L53, R4).
- **HOLD-severity threshold beyond a raw count (T8's PASS bar):** named by Grok and Fable both — a HOLD that confirms the build is RIGHT (Fable's example: `bars-chunk-1a-check-2026-09-20.md` rows 5/6/25) must not count against a candidate the way a real defect does; the packet's own L75 already draws this line (FIX rows, not HOLD rows) but the proposal's T8 text does not say so.
- **Whether L25's "Phase 1: cross-house seats NOT load-bearing — advisors and fallbacks only" is retired by L67 or still binds:** named by Grok (b) — the proposal edits L25's wording elsewhere but never touches this sentence, which is now in open tension with L67's standing tribunal/checker seats.
- **L22's chassis sentence, still naming Claude and the Anthropic API by name:** named by Grok (b)/(9) — the proposal's L22 edit touches only one phrase; the rest of L22 is not vendor-neutral despite L31 being cited as the reason for the edit.
- **Owners for the three T12 measurement gaps (agy usage, Codex meter readout, seat-usage role-hint regeneration):** named by Grok — the proposal's own report claims "owners named" (`11-propose-report.md:37`) but names none; this hub's file-check confirms no owner appears in either document.
- **L33: whether the OpenAI writer profile ("network on", "never `danger-full-access`", "cannot commit") is re-housed in L33 or stays in L29:** named by Fable — the proposal's trace sends it "to L33" but no L33 text is proposed to carry it, and today's L33 carries none of the three (confirmed, WRONG FACTS above). Without one of the two, it is dropped.
- **The seat map's Fable-seat row (row 7), stale at "asked per R76":** named by Grok — R109 (19:34 ET) superseded the per-seat ask with a standing "all Opus 5.5 for now." A desk reading the map as committed would miss R109. Not a law vote — his standing word, carried here so the derive updates the map's row 7 language, not L67 itself.
- **Whether a "blind code seat" (proposal's row 13) exists in any ruling:** named by Grok — not in R46 or L67; not folded by any house.

**RE-OPENS A RULING (R46):** named once, by Fable — "the model ids in L67's 2026-09-21 amendment ('Sol (`gpt-5.6-sol`)', 'Opus 5')" would need to change if the seat table's `claude-opus-5-5` id is to be the single source (L3); changing L67's wording is his to rule, never folded by this tribunal. Grok's (g) independently reaches the same structural point ("A wording that would change L67 or re-seat R46 is carried to him as RE-OPENS A RULING") without naming a specific edit — both are recorded here as the same escalation, not double-counted.

**RULES AN OWNER ITEM:** none. No house ruled D1, D2 or D3 outright; all three either declined to rule them, said only whether the framing was mis-stated, or flagged a wording risk inside the proposal's own recommendation.

## WRONG FACTS claimed
| claimed by | claim | this hub's file-check verdict |
|---|---|---|
| Grok | `10-PROPOSAL.md:11` "the only hit is the heartbeat's port probe" | DOES NOT HOLD as stated — 8 lines match, 5 are the probe, 3 are comments; zero-model-calls conclusion still HOLDS |
| Grok | `10-PROPOSAL.md:125` cites day-open verdict lines for the 684–1,067 token figure | DOES NOT HOLD as a citation — those lines are GREEN verdicts, not token counts; the figure itself HOLDS against `seat-usage.md` |
| Grok | `10-PROPOSAL.md:89` "four such deploys" (no-write-path pattern) | HOLDS — five WHY-NOT-OPUS deploy files exist, excluding this tribunal prompt's self-match; proposal undercounts by one |
| Grok | `10-PROPOSAL.md:50` "1 new verified finding" omits the trial's other (wrong-on-substance) finding | HOLDS — confirmed against this hub's own `16-seats.excerpt.md` staging of the 09-15 Attempt D line |
| Grok | `10-PROPOSAL.md:161` model id `grok-4.7-build` vs `seat_usage.yaml`'s `grok-4.6-build` | HOLDS — three-way corroboration (Grok, Gemini, this hub) |
| Grok | `10-PROPOSAL.md:152` row 4 model `claude-opus-5-5` vs `--model claude-opus-5` on the cited deploy | HOLDS — confirmed by fresh read of `02-deploy-stack-3.md:3` |
| Grok | `10-PROPOSAL.md:161` "MEASURED" for a design-tribunal seat and a "blind code seat" | HOLDS as characterization — the cited measurement is a single n=1 checker trial, not evidence for either named role |
| Grok | `11-propose-report.md:36` "MODELS.md … has no seat map" | HOLDS — MODELS.md has a 4-row tier table (Planning/Coach/DRC/Logistics), which is a (stale) seat map of a kind, just not the 15-row one the proposal builds |
| Grok | `10-PROPOSAL.md:224` "owners named" for T12 | HOLDS — no owner is named in either `10-PROPOSAL.md` §6 or `11-propose-report.md`'s notes |
| Grok | `10-PROPOSAL.md:101` "nothing dropped" (L29 trace) | HOLDS — the trace's own text concedes the Sonnet/Haiku ban "becomes a default, not law," and "never in session context" is dropped from the new sentence without a trace row |
| Gemini | `10-PROPOSAL.md` row 13 `grok-4.7-build` vs `seat_usage.yaml`'s `grok-4.6-build` | HOLDS — same finding as Grok's, reached independently |
| Gemini | `10-PROPOSAL.md` §1.2 trace omits the dropped async/sync rung-2 shapes | HOLDS — confirmed against `14-laws-routing-cluster.md`'s L24 text; the proposal's §1.2 discussion names only the vendor-name and "free" drops, not the shape drop Grok also names in (b) |
| Fable | `ROUTING-PROPOSAL-2026-09-22.md:150,183` "no write path" for the deploy set, incl. 09-22 | HOLDS — confirmed directly; `deploy-2026-09-22.md:80` + `05-stacked-deploy.md:1,6` show a dev migration under `--permission-mode auto` |
| Fable | `ROUTING-PROPOSAL-2026-09-22.md:38,57,66,138,140` cite L31 for "no vendor/model name in law text" | HOLDS — L31's text says code identifiers/schema/config keys/enum values/system design docs, not law text; L67 already names models in law |
| Fable | `ROUTING-PROPOSAL-2026-09-22.md:149,184` cite "LEDGER:1325" for six STOPs | HOLDS — the sentence is at `PROJECT-LEDGER.md:1326`, one paragraph over |
| Fable | `ROUTING-PROPOSAL-2026-09-22.md:115` Sol writer profile "→ L33" | HOLDS — L33's current text carries none of the three named details |

**16 WRONG FACTS claims total (10 Grok, 2 Gemini, 4 Fable); all 16 HOLD** on this hub's file-check (one, Grok's port-probe claim, HOLDS on the "only hit" wording while the underlying zero-calls conclusion still stands — recorded as HOLDS-as-stated, not a wash). Zero WRONG FACTS claims were found to DOES-NOT-HOLD when re-checked against the real files.

## Independence
`grep -c -F -e "-ruling" gemini-ruling.md` → 0. `grep -c -F -e "-ruling" grok-ruling.md` → 15, all on lines citing the STAGED PACKET files `12-rulings.md` and `13-rulings-cited.md` (whose filenames happen to contain the substring "-ruling" inside "-rulings") — none references `gemini-ruling.md` or `gemini-ruling.partial.md`. Read the matching lines directly (above) to confirm: no line names the other house's file. **No independence breach.** Both houses answered from the same packet, launched in one message, neither pointed at the other's output.

## ESCALATE
- `ASK DESK: R4's committed row (cto-2026-09-22.md:15) reads as the LAWS CONSOLIDATION SITTING being HIS SITTING, "NOT a tribunal design item" and says "no `08`/`09`/`10` tribunal prompts are drafted" — this is about the laws-consolidation sitting's own numbering, not `63`, but the wording is close enough to this tribunal's subject (routing law text) that it is named here for your eyes. Authorization for THIS tribunal instead rests on R81 (routing tribunal runs this week, its own lane) and R109 (Fable/derive seat for the routing tribunal by name) — both hold. Proceeded on R81/R109; flagging R4's proximity rather than treating it as a block. [22:15:30 ET]`
- `ASK DESK: R13 (09:4x ET, cto-2026-09-22.md:146) names tribunals 38/61/73 by number for the "without Astra" ruling, not 63 (routing) by number. The routing tribunal's own R81/R109 rows independently name it and its seat, and the Codex probe independently confirms the meter is out (matching R13's stated reason), so I proceeded on Grok+Gemini with Astra METER per the probe result — but the literal "63 without Astra" authorization rests on the probe result and R81/R109, not on R13's own text. [22:15:30 ET]`
- **ASTRA — ALWAYS, one line:** The OpenAI house (Astra) has not ruled on a routing design that assigns its own seats (seat-map rows 9–12) — R13 (meter out until Sat 09-26 06:47 ET); the desk brings Astra's read of the derived FINAL to him BEFORE his ruling of the law text.
- Packet over 120 KB after the cut order: **121,173 B**, 1,173 B over target, after cuts (i)-(iii); cut (iv) not needed and not applied — see `## Packet` for the full cut accounting.
- REDACTION count: **0** — the `TSLA|372[.]82|374[.]50` check returned 0 on all 14 staged files and both ruling files; no other ticker/price/share-count/P&L/rule-sentence/config value of his was found anywhere in the packet or the two rulings.
- Every self-assignment UNMEASURED or DOES-NOT-HOLD-as-measurement (7, houses): Grok T5, T8, (a), (e), (f), (g) — all correctly self-labelled UNMEASURED/n=1-spot-check by Grok itself; none folded as a rule.
- Every UNMARKED SELF (1): **Gemini gave no `SELF:` line at all for any of items (a)-(h)**, including item (g), which restates a rule ("Grok and Gemini unchanged") that keeps Gemini's own design and code-check seats. Not a breach of the ADOPT verdict itself, but a gap in the conflict-of-interest discipline the charge required for every item.
- A wording that LOOSENS L29's write-path floor, quoted: **Gemini's T7 `ADOPT WITH`** keeps the proposal's operative OR ("(1) its model is the house's top implementation model, OR … the seat table records that model PASSED for that task class by a measured trial (§5) — [D1]") as live text NOW, before D1 is ruled. Both Grok and the Fable seat independently found the SAME risk (a production migration could run on a model that only passed a DEV shadow build) and wrote text that keeps the top-model floor OPERATIVE until D1 rules, with the OR held in reserve. Gemini's wording does not do that — it is quoted here as the one house wording that, if adopted as written, loosens the floor ahead of his ruling.
- Any claim that a rewritten clause DROPS an old obligation that HOLDS (question (b)): the L29 clause-trace gaps ("or higher", "never in session context", the OpenAI writer profile's specific terms not actually present in L33) and L24's dropped async-dead-drop/CLI-bridge shapes — all confirmed HOLDS above, all restored by at least one house's wording.
- Every split on law text (L39): **none.** No item produced a disagreement between the two ruling houses on the LAW TEXT itself; both are ADOPT-class on every item, differing only in wording detail (T3, T7, (a), (f)) — recorded as wording differences for the derive to reconcile, not OPEN items for him.
- Any number a house proposes for the law, quoted (his to set, R4): **Gemini's suggested class-level n ≈ 3** ("(f): the spirit of L8 … demands a larger n (e.g., n=3) across different builds. The exact number is his to set.") — copied here, never adopted by this hub.
- RE-OPENS A RULING: **R46** — Fable and Grok both independently flag that folding L67's current model-id text (`gpt-5.6-sol`, "Opus 5") into a seat-table row that instead names `claude-opus-5-5` would either change L67's wording or leave two sources for the same assignment (L3); carried to him, not folded (see `## OWNER ITEMS`).
- RULES AN OWNER ITEM: none — no house ruled D1/D2/D3 outright.
- A house that did not rule: none — Grok and Gemini both produced a closing `TRIBUNAL R1:` line. Astra is the third of three, METER per R13, recorded above.
- An independence breach: none (see `## Independence`).
- Astra's probe row: `METER — proceed on three` (as expected, R13) — not flagged.
- Gemini's closing line does not match the prompt's three permitted shapes: `TRIBUNAL R1: ADOPT WITH AMENDMENTS` is neither `ADOPT`, `ADOPT AFTER <≤15 words>`, nor `DO NOT ADOPT <≤15 words>`. Recorded verbatim in the rulings table; read as its nearest permitted shape (unconditioned ADOPT) for the closing-line count below, since "WITH AMENDMENTS" with no words attached does not itself state a condition the way `ADOPT AFTER <...>` would.

ROUTING TRIBUNAL R1 DONE · grok: TRIBUNAL R1: ADOPT AFTER restore dropped clauses; T5 waits on T6 · gemini: TRIBUNAL R1: ADOPT WITH AMENDMENTS · astra: METER — proceed on three · houses that ruled: 2 of 3 · claims that HOLD: 13 · self-assignments unmeasured: 11 · owner items: 11 · ESCALATE: 12
