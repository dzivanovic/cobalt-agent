BLIND: I did not read the round-3 folder or the round-3 hub's report.
RUN DATE: 2026-09-23 (started 11:10 EDT)

## DIGEST FOR THE DESK

- `TRIBUNAL R3: BUILD`
- R2-1: **ADOPT B**. MOVED from my N1 to grok's and gemini's B. Delete-ALL at start is not proven safe: `AbandonProcessGroup` (`aset.plist:29-30`) + `exec uv run` (`start_aset.sh:69`) can leave a live server on the same `scratch_dir` (X22). B's age gate stays off every live clip.
- R2-2: **ADOPT S**. MOVED from my N2 to grok's S. In this code "no Cobalt ownership" = no `last_after` baseline (`writer.py:828-829`). No-retry = the settled `[F-09]` and the smaller mechanism. My own row shape was unrestorable (`writer.py:1102-1106`).
- R2-2 (d): a confirmed voice edit of a Cobalt unit goes through the same O1 span op, never `upsert_unit`. The producer's next write keeps his bytes and logs one override (L28), and the change after that applies (X23).
- R2-3: **ADOPT A-V5** — HELD. Slice **V5 — trading-logic drafts**: after V1, parallel to V2/V3, 4 h, RESTARTS `com.cobalt.aset` (+ changed-`cobalt.settings` importers, AST walk). B leaves §2.4 permanently unbuilt (R18 (a)). A-V1 rests on G10 (does not hold) and grows the settled V1.
- Withdrawn from round 2: 5 (R2-1: 2 · R2-2: 2 · R2-3: 1).
- Experiments: X22 (does `uv run`'s child outlive a `kill -9` of `uv`?), X23 (his confirmed bytes survive one producer write), X21 sharpened (restore-by-content round trip). Cited: E1–E11, X1–X5, X12, X13, E7 sharpening, X20, G2/K5.
- OWNER: none.
- ESCALATE: 4.

## Rulings

### R2-1 — who deletes crash-leftover scratch audio: the START sweep (written 11:13 EDT, before any R2-2 read)

SELF-ATTACK (my seat's N1, `voice-v3-tribunal-fable-r2-2026-09-23.md:21-22, :26, :30`):
- Walked: `grep unlink` → 5 deleters, none voice, none in `heartbeat/` (`archiver/shadow.py:234`, `redact/secrets.py:280`, `vaultwrite/writer.py:478`, `radar/collector.py:108`, `backup/restic.py:197`); `grep scratch` → one comment (`generated/cli.py:100`). `ops/com.cobalt.aset.plist:29-30` `AbandonProcessGroup` TRUE; `:40-43` `RunAtLoad` + `KeepAlive` true; no `ThrottleInterval` key (grep). `ops/start_aset.sh:69` `exec uv run python -m cobalt.aset` — the PID launchd tracks is `uv`, and the server is its child. `aset/__main__.py:50` `uvicorn.run(...)` with no `workers=` (one server process per start). `heartbeat/probes.py:128-144` `sheet_http` = HTTP 200 on `127.0.0.1:5010`, nothing about which PID answers.
- STRONGEST FAILING SCENARIO against N1: 14:02:00 turn `t-A` writes `t-A.webm`; 14:02:10 the `kill -9` hits the PID launchd tracks — `uv` (`start_aset.sh:69`). With `AbandonProcessGroup` true (`aset.plist:29-30`) launchd does not reap the group, so the python child can live on, still bound to :5010 and still transcribing `t-A`. `KeepAlive` starts a new `uv` → python; N1's sweep runs "before the first request is served", i.e. before the bind that would fail on the busy port, and deletes EVERY file — `t-A.webm` of a LIVE turn in the orphan — and logs AMBER "a turn died mid-way", which is false. The new process then exits on the bind, `KeepAlive` respawns it, and each respawn's sweep deletes whatever the orphan's next turn just uploaded. `sheet_http` stays GREEN throughout (the orphan answers 200, `probes.py:140-141`), so nothing says so. Whether `uv run`'s child outlives a `kill -9` of `uv` is behaviour → X22. The same path runs on a second instance started by hand (the plist's own comment, `:31-34`, records ASET being started by hand). N1's premise — "no turn of a new process is live, so any file present is a leftover" — proves only that the NEW process has no live turn. It does not prove the DIRECTORY has none. Delete-ALL is not proven safe → WITHDRAWN below.
- My residue claim, "its only residue — an unlink that already failed RED — fails again the same way" (`…fable-r2…:22`), is a claim about OS behaviour that no file shows (L70) → WITHDRAWN. So B's per-turn sweep is not proven redundant: it re-tries that residue and ages out the fresh-crash leftover. Under N1, both sit until the next `com.cobalt.aset` start.
- FROM GROK'S SIDE ("B is the smaller fix that makes §5 `:108` and §9 V4 `:161` both true", K1–K4 HOLD): N1 has fewer call sites, but it is smaller only if it is correct, and the scenario above leaves that unproven. B's age gate is exactly what keeps B's start sweep off a co-live process's files.
- FROM GEMINI'S SIDE ("a sweep with max-age ≤ STT timeout could delete a clip still transcribing"): N1's start sweep has NO age test at all, so gemini's own hazard lands on it whenever a second process shares `scratch_dir`. B + the settled refusal `scratch_max_age_s ≤ stt_timeout_s` keeps every sweep off a clip younger than the STT timeout, in any process.
- What stays true against B, stated once: the 14:02:10 leftover outlives B's start sweep, but under B it is COUNTED (AMBER, V4 probe, within one beat of passing `scratch_max_age_s`) and deleted by the next turn's sweep. That is a delay, loudly visible. N1's failure is a live command's audio deleted, logged under a false reason, with the page green. The smaller failure plus the mechanism two houses already hold wins.

**ADOPT B**

- 14:02:10 walk, B: `kill -9`; `KeepAlive` respawn (delay = launchd behaviour, E7 sharpening); the start sweep sees `t-A.webm` younger than `scratch_max_age_s` (> `stt_timeout_s` by the refusal) → kept, no line. The first beat after it passes `scratch_max_age_s` → `voice_scratch` AMBER, count 1 + oldest age. Next morning, his first press → the per-turn sweep deletes it, one AMBER line; an unlink that fails → RED. Orphan case (X22): the orphan's `t-A` is young → never touched.
- 14:02:10 walk, N1: the respawn's start sweep deletes `t-A.webm` within the respawn time, one AMBER line — correct when the python PID died. When the kill hit `uv` and the child lived on (X22): the same line fires on a LIVE turn, and repeats on every crash-loop respawn.
- Fresh start, can a file belong to a live turn? NOT PROVEN NO: `AbandonProcessGroup` (`aset.plist:29-30`) + `uv` as the tracked PID (`start_aset.sh:69`), or a hand-started second instance. So delete-ALL is not proven safe. B's age gate makes it irrelevant.
- Does the per-turn sweep delete anything `finally:` does not? Yes: a crash leftover younger than `scratch_max_age_s` at the last start, once it ages; and a file whose `finally:` unlink failed (RED), retried each turn.

§5: **Crash leftovers:** The voice module sweeps: on `com.cobalt.aset` start and at the start of every turn it deletes scratch files older than `scratch_max_age_s` (AMBER line per file; a failed unlink RED). The heartbeat's `voice_scratch` probe only COUNTS such files and never deletes (L40 — the voice module owns its scratch). The config schema refuses `scratch_max_age_s ≤ stt_timeout_s`.

V4: | **V4 — ops** | `voice_stt` / `voice_plan` / `voice_scratch` probes (`voice_scratch` COUNTS files older than `scratch_max_age_s` in the `scratch_dir` it resolves through the voice config's one loader, and names that directory in its detail line: some → AMBER with the count and the oldest age, none → OK, unreadable → RED unknown; it never deletes — §5); the production `scratch_dir` override in `ops/com.cobalt.heartbeat.plist` (as `COBALT_VAULT_PATH`, `:20-21`); model-fetch deploy step; the `tailscale serve` deploy step (§11 W6); DevDocs | no (probes read) | — | V1 | `com.cobalt.heartbeat` (plist in the diff → reload, L42; a 900 s one-shot job, `heartbeat.plist:60-61`) | 2 |

- Reasoning: `aset.plist:29-30` + `start_aset.sh:69` leave room for a second live process on the same `scratch_dir`; only an age gate above `stt_timeout_s` (the settled refusal) keeps a sweep off a live clip in ANY process. B's residue — the fresh-crash leftover that waits for the next turn — is counted AMBER by V4 and bounded by his next press. V1's sweep = one function, two call sites (start, turn), one unlink (L3); V1 h unchanged.

WITHDRAWN (R2-1): 2 — see `## Withdrawn from round 2` (W1, W2).

### R2-2 — O1's fold MECHANICS under SCOPE-R39 (written 11:16 EDT)

SELF-ATTACK (my seat's N2, `…fable-r2…:40-51, :55`):
- Walked: `grep _write_with_retry` → def `writer.py:530`, callers `:773` (upsert_unit), `:974` (upsert_region), `:1250`; `grep last_after` → `store.py:91`, `writer.py:810, :828`; `grep unit_after` → written by `upsert_unit` `:766`, `upsert_region` `:969`, `_seed_baselines` `:634`, restore `:1175, :1218, :1241`, read by `_locate_region` `:1032`; `grep merge3` → `writer.py:706, :717, :733, :951`; `grep vault_overrides` → INSERT `store.py:214`, never purged (`:248-250`); `grep sync_revert_of` → `writer.py:732, :889, :958, :970`, `store.py:188-206`, `cli.py:108-110`; `grep upsert_region` → producers `prefill/trade_note.py:162`, `taxonomy/cli.py:334`, `taxonomy/trade_note_migration.py:298`, `taxonomy/note_migration.py:888`, `seatusage/runner.py:147`. Read `writer.py:457-606, :644-893, :895-989, :1002-1049, :1098-1165`; `merge.py:1-171`; `store.py:1-15, :88-109, :245-256`.
- ATTACK 1, on my own mechanism: my round-2 said the O1 versioning row carries "no (section, unit) key that `last_after` reads — as `create_if_absent`'s row does (`writer.py:603-604`, `section=None, unit=None`)" (`…fable-r2…:49`). `restore` refuses a row with no section: "is a whole-file create (no section) … Refusing" (`writer.py:1102-1106`). So my N2 price would ship an O1 write that `cobalt vault restore` cannot undo. That breaks L28's versioning in practice (upsert_region's own contract: "the same rollback through `restore`", `:916-917`) → WITHDRAWN (W4). A row CAN be restorable and still never be a baseline: a section key no producer writes, the turn id as the unit, and `unit_before` / `unit_after` = the span bytes. `restore` finds no such section and locates the span by content (`:1145-1162` → `_locate_region` `:1020-1038`), and `last_after` never matches that key (`store.py:102-104`). That detail is design, not law.
- ATTACK 2, on my objection to S: I said "S's vague wording does not close it" (`…fable-r2…:50-51`). The code defines Cobalt's ownership of a span as exactly one thing — being the merge base: "the base is `last_after` — what Cobalt wrote here last — and anything else on disk is the human's, which wins" (`writer.py:828-829`; `store.py:92-93` "The BODY Cobalt last wrote into this unit — the `base` leg"). So S's "no Cobalt ownership of it is created", read against the code, forbids a `unit_after` row under a producer's (note, section, unit) key — the operational sentence I asked for. X21 tests it. → WITHDRAWN (W3).
- ATTACK 3, retry: my N2 moved "takes no retry" out of the law because the span check COULD sit inside `build` and make attempt 2 safe (`writer.py:535-537`). That is correct, but it is a SECOND attempt added to a mechanism that works with one. It has no failing scenario against S. The only cost of no-retry: 16:40:12 "yes", Obsidian saves his typing elsewhere inside the snapshot → commit window (`:536` → `:463-464`), `NoteChangedOnDisk`, the confirmed edit is refused and read back again. That is one re-confirm, and it is exactly what the settled `[F-09]` already builds. With law and design saying the same thing, the law is the smaller mechanism.
- FROM GROK'S SIDE (S; "a note-level refusal refuses his 16:40 typing elsewhere … `_commit` guards only writer-read → write; 'takes no retry' belongs in the law because `_write_with_retry` rebuilds once"; K9, K11 HOLD): every clause holds against the files, and none is larger than mine. S is correct.
- FROM GEMINI'S SIDE (P): "refused if the note changed since the read-back" refuses the 16:40 case — he types in the risk paragraph and the lesson span is untouched. "Bound … to that one diff" changes whenever a line typed above the target shifts the `@@` offsets. G4 and G5 DO NOT HOLD (`_commit`'s window is `:536` → `:463-464`, not the read-back). And P says nothing that stops a baseline, which is the failure in (c). P fails (a).
- (d) walked on SCOPE-R39 — Cobalt's OWN units — through the real merge code: see (d) below. S's two sentences are exactly the rule that makes a confirmed voice edit of a Cobalt unit behave like his hand edit of it, and so satisfy L28's "a Cobalt line he changed → human wins, logged as override".

**ADOPT S**

- (a) SPAN. 16:40:05 read-back of the lesson span; 16:40:07–:09 he types in the risk paragraph and Obsidian saves; 16:40:12 "yes" → the writer's snapshot (`writer.py:536`) takes in his typing, the span still equals the confirmed before bytes → written, his risk line untouched. At 16:40:08 he edits the lesson span ITSELF → the span ≠ the before bytes → REFUSED, new read-back. A note- or diff-bound refusal (P) refuses the first case too.
- (b) IN THE LAW (S). One attempt: the span check at build, then `_commit`'s guard over snapshot → commit (`:463-464`), together cover read-back → write. `NoteChangedOnDisk` → refused and read back again, no rebuild. The O1 op does not call `_write_with_retry` (`:530-554`) as it stands; it uses a single-attempt path in the same writer (L3). = `[F-09]`, settled.
- (c) "Remains his: no Cobalt ownership of it is created" is OPERATIONAL, read against `writer.py:828-829`: no `unit_after` row under any producer's (note, section, unit) key. Seat scenario: 10:00 voice A → B confirmed (no baseline); 12:00 he types C by hand; 16:00 read-back "C → D", "yes" → the span equals C → D is written by replacement, never `merge3` → no override row. Under a baseline-recording op (`upsert_region` as it stands, `:963-969`), `merge3(base B, human C, cobalt D)` keeps C and logs HIS override (`merge.py:23-24, :109-123`) — S's sentence forbids that op. 30-day purge (`store.py:247-256`, `last_after` → None, `:96-97`): no baseline is stored, so there is nothing for the purge to change. The O1 row itself expires at 30 days like every `vault_writes` row.
- (d) A confirmed voice edit of a Cobalt-owned unit is written by the SAME O1 span op (S), never by `upsert_unit`: `upsert_unit(body=his bytes)` would record them as `unit_after` under the producer's key (`writer.py:766`). The producer's next write would then see base = human = his bytes and replace them silently with no override (`merge.py:14-15`), which breaks L28 human-wins. Under S: 10:00 his confirmed span lands in unit `u-7`, and the versioning row is keyed by a section no producer writes plus the turn id. 10:15 the producer's `upsert_unit(…, u-7, body)` → `_merge_base` returns its own 09:45 `unit_after` (`:857-861`); on disk ≠ base → `merge3` keeps his lines and records one `vault_overrides` row (`merge.py:18-19, :23-24`). That is L28's "a Cobalt line he changed → human wins, logged as override", byte-for-byte his hand edit. From then on it follows today's code for his hand edit: the override run advances the baseline to the merged body (`writer.py:766, :771-773`). So the producer's NEXT change to that line applies with no second override (`merge.py:14-15`, base = human) — his confirmed bytes in a unit the producer rewrites survive ONE producer write (X23). Named false positive, unchanged: his confirmed bytes byte-identical to one of the unit's last 10 `unit_after` values → treated as a SYNC REVERT, the producer's text wins, `sync_revert_of` set (`writer.py:863-890`; L28 2026-09-09).

§6: | A Cobalt-owned unit | on his confirmed read-back (SCOPE-R39): the O1 span op — the same op as his own text: replaces only the confirmed span inside the unit body, single attempt, under the mtime guard and `_session_gate`, never `merge3`, never `upsert_unit` (which would record his bytes as the producer's `unit_after` baseline, `writer.py:766`); versioning row with before, after, `unit_before` / `unit_after` = the span bytes and the turn id, keyed by a section label no producer writes, so `last_after` never returns it (`store.py:102-104`) and `restore` still locates it by content (`writer.py:1145-1162, :1020-1038`). For a computed value, the reply also names that value's own correction path (R90 resolve action / A28, v2 §2) as the lasting fix, because the unit's producer rewrites the unit. | L28 as amended (O1, R39): his confirmed bytes are his — the producer's next `upsert_unit` merges against its own last `unit_after`, the human wins and one `vault_overrides` row is logged (`merge.py:18-24`) |

FOLD: [amended <date>] VOICE-ORDERED EDIT. Cobalt may replace any field of any note in his vault — his own text, his voice units and Cobalt's own units alike — only when he orders that exact edit in a turn of Cobalt's voice / text widget and confirms it: the widget reads back note, field and before → after; his confirmation (a word from the closed confirm list matched by code, or a tap) is bound by sha256 to that target span's before and after bytes; the write replaces only that span, goes through `VaultWriter` with the mtime guard and `_session_gate`, takes no retry, is versioned with before, after and the turn id, and is refused if the span changed since the read-back. The replaced text remains his: no Cobalt ownership of it is created. The trader is in the loop (as for `cobalt settings load --apply`, 2026-09-15); this is not a model-judged approval (L37). Nothing else in this law changes.

- Reasoning: `writer.py:828-829` makes "Cobalt ownership" = the `last_after` merge base, so S's last sentence is the no-baseline rule. `:536` → `:463-464` + a build-time span check with one attempt covers read-back → write. `:1102-1106` sank my own row shape. S = my N2 minus a retry and minus a clarification the code already supplies → the smaller correct text. Price (V3, unchanged from the derived `[F-09]` + O1 op): one single-attempt span op in `vaultwrite/writer.py` (≈25 lines + tests: edit-elsewhere lands, edit-the-target refuses, X21, restore-by-content round trip), +1 h on V3; no second writer (L3).

WITHDRAWN (R2-2): 2 — W3, W4.

### R2-3 — which slice builds the trading-logic draft-and-send tool (written 11:18 EDT)

SELF-ATTACK (my seat's A-V5, `…fable-r2…:67-75`):
- Walked: `grep send_dm` → def `notify/mattermost.py:130`, re-export `notify/__init__.py:26`, only caller `heartbeat/runner.py:299-304, :372, :483`; `grep -i hitl` → `settings/card.py:15`, `radar/propose.py:1, :740-741, :750`, `radar/cli.py:99`, `cards/shadow_report.py:20, :132`, `daymode/propose.py:118`, `db_migrations/placement.py:135`; `grep trading_logic` → 0; `grep -l trader_settings` → 19 files; `grep cobalt.settings` → importers incl. `aset/config.py:264`, `aset/web.py:69-70`, `aset/radar_panel.py:36`, `radar/evaluate.py:90`, `radar/runner.py:22`, `radar/notes.py:17`, `radar/audit_export.py:57`, `heartbeat/probes.py:74`, `dayopen/checks.py:29`, `daymode/config.py:321`, `cards/scoring.py:60`, `cards/shadow_report.py:37`, `replay/*`, `cli.py:76`. Read `settings/card.py:1-25, :242-331`; `settings/cli.py:84-110, :182-212, :289-326`; `notify/mattermost.py:1-15, :130-168`; `radar/propose.py:422-438`; `.gitignore:1-25`; derived `:65-79`, `:152-164`.
- ATTACK 1, the draft file: `cmd_load_card` DELETES every card key absent from the file (`settings/card.py:301-305`, `put(…, delete=deletes)` `:323`). Scenario: 10:41 "turn radar cards on" → a draft holding only `radar.cards_enabled` → sha256 → DM → `--apply` deletes the other four card keys. My text says "owner-drafted file … in the owner's own format" + "per-key diff" and does NOT say the draft starts from the current rows. What holds it: the settled §2.6 read-back IS the owner's dry-run result (derived `:76`), and the dry-run prints every delete (`card.py:305`). So the deletes are spoken before his "yes", and nothing is sent unheard. My text is incomplete there, not wrong. A check note for V5's checkers → ESCALATE 2.
- ATTACK 2, my RESTARTS prose: "`aset/config.py`, `radar/evaluate.py` touch it → `com.cobalt.radar` unless the walk proves otherwise" (`…fable-r2…:72`) names 2 importers of ≈20. It misses `heartbeat/probes.py:74` (`com.cobalt.heartbeat`), among others → WITHDRAWN (W5). The §9 row's own RESTARTS cell ("`com.cobalt.aset` (+ residents importing `cobalt.settings`, AST walk)") is correct as a derivation rule (L42). A NEW module under `settings/` that no existing module imports derives `com.cobalt.aset` only. An edit to `settings/__init__.py` or `card.py` derives every importer's resident.
- ATTACK 3, from GROK'S SIDE ("no V1–V4 row builds a drafter; the request gets `unsupported` and names the existing hand gate, which is not the class refusal"): is no slice smaller than a new slice? In hours, yes. But B's §2.4 then describes a tool that no row of the plan he approves ever builds. Scenario: V4 shipped (22 h); 10:41 "turn radar cards on" → `unsupported`, a reply naming `cobalt settings load --card <file> --sha256 <hash> --apply`. No file exists: he must write the YAML by hand with all five keys (Attack 1), hash it and run it — the manual edit R18 ("you keep asking me …") rules out. It stays so in every later slice, because none is committed. `unsupported` vs `refuse` is a label; the outcome for the class is permanent → R18 (a) ("any command") is not met. B is smaller but not correct.
- ATTACK 4, from GEMINI'S SIDE ("V1 builds it, since the voice agent is not live before V1"): why not V1? (i) G10 DOES NOT HOLD — no new-core inbound approve handler exists (`notify/mattermost.py:6-11` says so; `send_dm` posts only, `:154-164`). The only inbound one is the old tree's (strangler rule: no new-core path through it). After his "approve", the applier is the desk relaying under L7 as it stands, or his own hand — not "the existing chat bot". (ii) G11: the settings owner takes a card FILE + `--sha256` of its bytes (`card.py:242-251`), not the radar JSON artifact (`radar/propose.py:422-438`). (iii) V1 is settled as the smallest end-to-end conversation, gated on E1–E8, E10 and X1/X3/X5/X12/X13 (`[F-17]`, derived `:164`). Adding the drafter makes V1 12 h + 9 h in gemini's pricing, and the first conversation waits on it. V5 depends only on V1 and runs beside V2/V3 (L72), so it can land the same evening V1 does. A-V1 as worded carries a claim that does not hold → it cannot be adopted.
- What holds in A-V5 from the files: `.gitignore:6` `data/`; the `data/radar-proposals` precedent (`radar/propose.py:433-438`); `--card` / `--optional` with `--sha256`, required on `--apply` (`settings/cli.py:6-7`, `:102-107`; `card.py:285-288`); token exemption for a trader-run apply (`card.py:15-16`); `send_dm` not-sent on a disabled channel (`mattermost.py:147-152`); the aset WorkingDirectory is the repo (`aset.plist:27-28`), so `data/voice-drafts/` is repo-relative and gitignored.

**ADOPT A-V5**

- Slice **V5 — trading-logic drafts**. Depends on V1 only (no O1 dependency); runs in parallel with V2/V3 (L72).
- Exact change: `trading_logic: true` registry tools for the `--card` and `--optional` settings keys (`settings/card.py:4-8`, `:69`; `settings/models.py:57`). A new owner draft function in `src/cobalt/settings/` writes the draft file in the owner's format to `data/voice-drafts/` and returns (path, sha256 of the bytes, the owner's dry-run per-key diff). That diff is the §2.6 read-back. After his "yes", voice asks `notify.send_dm` with the diff, path, sha256 and the exact `cobalt settings load --card|--optional <path> --sha256 <hash> --apply`.
- Applied ONLY by that owner command — by his hand (trader-run, token-exempt, `card.py:15-16`), or by the desk on his "approve" in the desk chat for that file and sha256 (L7 as it stands). Voice applies nothing (L40).
- Files: `src/cobalt/settings/<draft module>` (new), `configs/cobalt/agents/voice.yaml` (entries), the voice tool module, tests. Migration: none. Hours: 4 (GUESS). RESTARTS: `com.cobalt.aset` (+ every resident whose entrypoint imports a CHANGED `cobalt.settings` module, AST walk at build, L42).
- Before V5 ships: Plan kind `unsupported`, a code-rendered reply naming the owner's command. Every other trading-logic target (sheet / day-mode keys loaded `--from`, rules, strategies, setup definitions, configs in git, radar notes) gets `unsupported` naming its owner path until its owner has a draft function. Orders and the trading platform stay `refuse` (derived `:72`).

§9: | **V5 — trading-logic drafts** | `trading_logic: true` registry tools for the `--card` / `--optional` settings keys; owner-drafted file in `data/voice-drafts/` + sha256 + per-key diff; `send_dm` card with the owner's exact apply command; applied only by `cobalt settings load … --sha256 … --apply` (his hand, or the desk on his L7 approve) | no (a gitignored draft file; the owner's own apply is unchanged) | — | V1 | `com.cobalt.aset` (+ residents importing `cobalt.settings`, AST walk) | 4 |

F-07: Built in V5 (§9); before V5, `unsupported` with the owner's command named.

- Reasoning: the owner's sha-exact apply (`settings/cli.py:6-7, :102-107`; `card.py:242-251, :281-331`) and the outbound card (`mattermost.py:130-168`) exist, so V5 adds one owner draft function plus registry entries: no new gate and no new approval path (L3, L37). B leaves §2.4 permanently unbuilt (Attack 3). A-V1 rests on G10 and grows the settled smallest V1 (Attack 4). Totals: 26 h, ≈36 h at ×1.4 (GUESS / UNVERIFIED).

WITHDRAWN (R2-3): 1 — W5.

TRIBUNAL R3: BUILD

## Withdrawn from round 2

- W1 (R2-1): "the upload handler is the directory's one writer and no turn of a new process is live, so any file present is a leftover" (`voice-v3-tribunal-fable-r2-2026-09-23.md:26`; the same premise at `:22`, "at a fresh `com.cobalt.aset` start NO file in `scratch_dir` can belong to a live turn"), offered as proof that delete-ALL is safe. Defeated by `ops/com.cobalt.aset.plist:29-30` (`AbandonProcessGroup` true) and `ops/start_aset.sh:69` (`exec uv run …`: launchd tracks `uv`, and the server is its child). A process that outlives the kill, or a second instance started by hand (`aset.plist:31-34`), shares the directory. The premise covers the new process's turns only. Whether the child survives is X22.
- W2 (R2-1): "(its only residue — an unlink that already failed RED — fails again the same way)" (`…fable-r2…:22`). This is OS behaviour, and no file shows it (L70). It was the only ground for calling B's per-turn sweep redundant.
- W3 (R2-2): "P states none of the three; S states the first two and the third vaguely" (`…fable-r2…:50`) and "S's vague wording does not close it" (`:51`). Defeated by `vaultwrite/writer.py:828-829` and `vaultwrite/store.py:92-93`: in this code Cobalt's ownership of a span IS its `last_after` merge base. So "no Cobalt ownership of it is created" is the no-baseline rule, stated operationally.
- W4 (R2-2): "The versioning row then carries before/after with no (section, unit) key that `last_after` reads — as `create_if_absent`'s row does (`writer.py:603-604`, `section=None, unit=None`)" (`…fable-r2…:49`), and its price "versioning row with no unit key" (`:51`). Defeated by `vaultwrite/writer.py:1102-1106`: `restore` refuses any row with no section ("a whole-file create … Refusing"), so that O1 write could not be rolled back.
- W5 (R2-3): "`aset/config.py`, `radar/evaluate.py` touch it → `com.cobalt.radar` unless the walk proves otherwise" (`…fable-r2…:72`), offered as the importer set. `grep -rn cobalt.settings src/cobalt` prints ≈20 importers, among them `heartbeat/probes.py:74` (`com.cobalt.heartbeat`), `radar/runner.py:22` and `aset/web.py:69-70`. Which residents restart depends on which settings module V5 CHANGES, derived at build (L42). The row's own RESTARTS cell stands.

## Experiments (L70)

Cited, not restated: derived E1–E11, X1–X5, X12, X13; round 2's E7 sharpening, X20, X21, G2 / K5.
- X22 (R2-1): on the home server, with a throwaway launchd job shaped like `ops/com.cobalt.aset.plist` (`AbandonProcessGroup` true, `KeepAlive` true, ProgramArguments → a script that ends `exec uv run python -m <a stub that binds a port and sleeps>`), `kill -9` the PID launchd reports. Does the python child survive, still holding the port, while launchd respawns? Repeat with a second instance started by hand. Result that changes the design: the child survives → a delete-ALL start sweep deletes a live turn's audio (N1 unsafe, B's age gate required). This also bears on ops beyond voice: an orphan answering :5010 keeps `sheet_http` GREEN while launchd crash-loops.
- X23 (R2-2 (d)): dev vault + `cobalt_dev`. A Cobalt unit written by `upsert_unit`; the O1 span op writes a confirmed edit of one line in it; the producer's `upsert_unit` runs twice with a new value on that line. Expected from the code (`merge.py:14-24`; `writer.py:766, :771-773`): write 1 keeps his line and logs one `vault_overrides` row; write 2 applies the producer's value with no override. Other result → §6's L28 basis and its reply note change.
- X21 sharpened (R2-2): add `cobalt vault restore <id>` of the O1 row. Pass: the span's before bytes come back, located by content (`writer.py:1145-1162`), and `last_after(note, <producer section>, <unit>)` is unchanged by both writes.

## OWNER

- none. O1's scope is RULED (R39). My round-2 L7 / L28 line (voice-applied settings) is not restated: no text of this design depends on it, and I cannot show it passes the owner test for this design.

## READING

- Authorization, 11:10 EDT: FABLE ROW count 1; `cto-2026-09-22.md:56` R109 (both literals); R109 commit `75b2aa5`; `cto-2026-09-23.md` R109 hits :4, :23, :24 (R21 restates "Fable seat runs on Opus 5.5 STANDS"), :36, :38, :40, :44, :48 — none ends or changes it; seat model `claude-opus-5-5` = this session; R18 :21, R39 :42, R43 :46 carry their literals; second-derive stop line `82bd946`; derived design `fc82bfa`; launch row R45 `cto-2026-09-23.md:48`; 10 rule strings each count 1 in `22-draft-setups-tribunal.md`.

- LAWS.md 1–442 (full).
- `reports/voice-v3-derive-r2-2026-09-23.md`: section grep; :11–35 (DIGEST), :55–144 (Fold table, NEEDS ROUND 3, OWNER TEST, FOR DEJAN), :158–180 (ESCALATE, CONTINUE, stop line).
- `prompts/2026-09-23/33-voice-v3-tribunal-r3.md`: a grep for its section lines, the SCOPE-R39 line (:61), the `QUESTIONS-R3.md (verbatim)` paragraph (:195), and :211–225 (the C9–C22 list). Nothing else in it.
- `reports/voice-v3-tribunal-r2-2026-09-23.md` (round 2): section grep; :155–304 (GROK, GEMINI, Anthropic-seat R1 claims file-checked, Checked against the files, Experiments named).
- `reports/voice-v3-tribunal-fable-r2-2026-09-23.md`: whole (:1–125).
- Derived design `VOICE-v3-derived-2026-09-23.md`: :1–11, :65–79, :104–127, :152–164. NOT opened: :210–241, :254–262, :269–303 (no ruling here turned on them; the experiments are cited by name).
- Code on main: `ops/com.cobalt.aset.plist` :1–49; `ops/com.cobalt.heartbeat.plist` :1–73; `ops/start_aset.sh` :15–70; `heartbeat/runner.py` :126–167; `heartbeat/probes.py` `sheet_http` (:128–145, via grep -A); `vaultwrite/writer.py` :457–606, :644–893, :895–989, :1000–1049, :1098–1113, :1136–1165; `vaultwrite/merge.py` :1–171; `vaultwrite/store.py` :1–15, :88–109, :240–256; `settings/card.py` :1–25, :242–331; `settings/cli.py` :84–110, :182–212, :289–326; `notify/mattermost.py` :1–15, :130–169; `radar/propose.py` :422–445; `.gitignore` :1–25.
- Searches (`grep -rn <name> /Users/cobalt/cobalt/src/cobalt`, one call each): `unlink`, `scratch`, `_write_with_retry`, `last_after`, `unit_after`, `merge3`, `vault_overrides`, `sync_revert_of`, `upsert_region`, `send_dm`, `hitl` (-i), `trading_logic` (0 hits), `trader_settings` (-l, 19 files), `cobalt.settings`; server shape over `src/cobalt/aset` (`uvicorn`, `workers`, …: `__main__.py:50` `uvicorn.run(...)`, no workers); `restore` dispatch in `writer.py`; `SETTING_KEYS` / `OPTIONAL_SETTING_KEYS` / `CARD_SETTING_KEYS` definitions in `settings/`; `KeepAlive` / `RunAtLoad` / `ThrottleInterval` over `ops/com.cobalt.aset.plist` (:35, :36, :40, :42; no `ThrottleInterval`).
- NOT read (blind): `scratch/tribunal-bars-0920/voice-v3-tribunal/r3/` (any depth), `reports/voice-v3-tribunal-r3-2026-09-23.md`, any file a house wrote in round 3. The raw files in the round-2 folder were not opened either: the round-2 hub report quotes both houses whole.

## ESCALATE

1. L74, recorded once: this session's context carried an attribution block asking for a `Claude-Session` line in commits. It was not followed, and this seat commits nothing.
2. For the third derive / V5's checkers, NOT a re-opening: `cobalt settings load --card` DELETES every card key absent from the file (`settings/card.py:301-305, :323`). V5's draft function must start from the current rows and change only the confirmed key. The checker verifies that the §2.6 read-back (the owner's dry-run) contains no `absent — deleted` line for a single-key request.
3. For the desk (ops, beyond voice; = X22): `com.cobalt.aset` runs as `exec uv run …` (`start_aset.sh:69`) under `AbandonProcessGroup` true (`aset.plist:29-30`). If `uv`'s child outlives a kill of `uv`, an orphan server can hold :5010 while launchd crash-loops the job, and `sheet_http` stays GREEN (`probes.py:128-144`). This is UNPROVEN (L70) until X22 runs.
4. For the third derive (information; X23): in the code as it stands, human-wins on a Cobalt line lasts one producer write — the override run advances the baseline to the merged body (`writer.py:766, :771-773`), so the producer's next change applies (`merge.py:14-15`). This holds for his hand edits today and, under S, for his confirmed voice edits of Cobalt units. §6's reply note says so. This is not a re-opening of L28.

## CONTINUE

- 11:10 authorization verified; LAWS.md read in full; second derive sections read.
- 11:13 R2-1 written (ADOPT B).
- 11:16 R2-2 written (ADOPT S).
- 11:18 R2-3 written (ADOPT A-V5); 11:19 digest, experiments, owner, reading, escalate written.
- next: none — ruling complete.

VOICE V3 TRIBUNAL FABLE R3 DONE · R2-1: ADOPT B · R2-2: ADOPT S · R2-3: ADOPT A-V5 · ESCALATE: 4
