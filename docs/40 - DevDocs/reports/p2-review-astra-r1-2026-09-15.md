AMEND BEFORE BUILD: first-run, tenancy, replay and deployment gaps remain; 2026-09-15 R1–R11 are accepted without reopening.
Read-only code/source review; no DB, production, vault or plan changes. RESTARTS: none (documentation classifier: src/cobalt/jobs/restarts.py:186–187).
Evidence aliases: PLAN = docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md; FINAL = /Users/cobalt/Vault/Think/0 - Inbox/tribunal-grok-2026-09-14/fable/FINAL.md; MERGED = /Users/cobalt/Vault/Think/0 - Inbox/tribunal-analyst-2026-09-14/grok/MERGED.md; VAULT = /Users/cobalt/Vault/Think.

| id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording |
|---|---|---|---|---|---|
| 1 | BLOCKER | STEP-1, STEP-4; tenancy | The system seam's unrestricted `detail` carries evaluated trade predicates and missing atoms, which can disclose trade_def content despite the opaque md5. `desk_shadow` also needs a boundary between generic observations and authored trade context. The accepted system seam and nullable score copy do not authorize arbitrary user content in system JSON. This is a payload-classification defect under L32, not a request to move the settled seam tables. | PLAN:102–105,176–180; docs/10 - Decisions/ADR-0008-two-layer-data-model.md:18–21,82–99; MERGED:82,235 | Keep the named system seam and permitted copied score; define a closed system payload schema. Store authored predicates, resolved per-trade thresholds, trade-specific WHY and snapshots on the user side with user_id NOT NULL, identity FK and GUC default. Test that system payloads contain neither authored trade content nor trader settings. Any disputed payload classification remains OPEN for Dejan under L32. |
| 2 | BLOCKER | STEP-1 DDL | New tables/views lack explicit side ownership. Default grants in 0001 cover later objects created by the migration role, so this is not a blanket missing-SELECT finding; ownership remains a separate ADR requirement. The two user tables have the correct explicit user_id/FK/GUC shape, and aset_sizings inherits it. | docs/10 - Decisions/ADR-0008-two-layer-data-model.md:82–99; src/cobalt/db_migrations/0001_schemas.sql:135–160; src/cobalt/db_migrations/0004_radar_pool.sql:27,53,59–61; PLAN:87–171 | Assign every new table and view to its side role, including associated identity-sequence privileges. Retain system SELECT and the precise cross-side REFERENCES grant. Prove owner, SELECT, INSERT/sequence use and wrong-side refusal through actual side-role connections on cobalt_dev. |
| 3 | BLOCKER | STEP-1; §4 Tenancy | The DDL is not repeatable: unconditional CREATE TABLE, ADD COLUMN and ADD CONSTRAINT fail on the second migrate. FORWARD is reapplied in full on every invocation; there is no applied-version registry. The user views are still `AS ...`, and the placement test enumerates views too, while the proposed list omits all three views. | src/cobalt/db_migrations/__init__.py:31–46; src/cobalt/db_migrations/cli.py:190–201; tests/cobalt/test_tenancy.py:87–96,111–127,657–667; PLAN:170–171,232 | Make 0006/0007 idempotent with schema-shape assertions; supply executable view definitions and add all tables and all three views to placement. Register forward and reverse files explicitly; replace positional migration-test references with named paths. Test migrate twice on a populated dev database and bounded reverse/reapply. |
| 4 | BLOCKER | STEP-2; §4 Grammar | The enumerated grammar cannot parse existing notes, including the five required by R2. Missing forms include IN cfg(...) min, slope_norm(...), named arguments/division, touched, on/after/inside, nested calls and !=. Parsing every TradeDef at load makes this a whole-taxonomy first-run failure rather than a clean missing-detector result. R3 also requires note + line, but STEP-2/tests promise only slug + expression. | VAULT/1 - Trading/4 - Strategies/Hitchhiker.md:37; Fashionably Late.md:41,66; Second Chance Scalp.md:108,152–153; 9 EMA Scalp.md:44–46; src/cobalt/taxonomy/trade_def.py:248–271; PLAN:173,225 | Inventory every expr-bearing position in all 13 live definitions and implement its actual syntax without editing notes. Preserve source path and line in AST errors. Parsing success and unavailable detector support are separate results; test every real syntax form plus a malformed expression with exact note/line diagnostics. |
| 5 | BLOCKER | STEP-5/6; STEP-D1/D2 | No build step extends the settings loader. Today it reads two YAML files into seven fixed keys; it cannot load card.* or radar.cards_enabled, and `--sha256` is not an accepted option. The enable/kill gate therefore cannot be executed as planned. | src/cobalt/settings/models.py:29–43,134–171,174–201; src/cobalt/settings/cli.py:68–94,153–169; PLAN:185–197,252–254 | Add typed schemas and the one settings-load path for every new key, including reviewed-artifact SHA verification, diff and round-trip proof. Specify the input file format and complete D1/D2/kill commands. Test bad hash, unknown/malformed settings, absent dark-only optional curves/bands, atomic apply, and per-cycle/request rereads. |
| 6 | BLOCKER | STEP-4/6; §4 F10 | The existing move endpoint does not refuse null grade: CardStore locks and checks only state, edge and reason. Once nullable radar sizing is introduced, an unsized WATCH can become ARMED. Separately, the existing legal WATCH stop edit divides a null risk_budget, causing a first-use exception. | src/cobalt/aset/web.py:1137–1163; src/cobalt/cards/store.py:240–259,587–605; src/cobalt/aset/engine.py:144–153; PLAN:197 | Add the sized-card ARM invariant inside the locked CardStore transition, covering every caller. Extend stop editing for unsized WATCH: preserve null sizing while recording/recomputing valid stop-distance and score inputs. Test unsized ARM refusal, stop edit before first key, key/ARM race, and sized/manual/FILLED regressions. |
| 7 | AMEND | STEP-1/4/6 | WATCH creation names the existing stores but not their required mapping. AsetStore.save consumes a fully sized result, including inp.grade.value; the DDL still requires entry, stop, per_share_risk, direction, session and state. New trigger_price/structural_stop columns also create two price representations that can drift after edits. | src/cobalt/aset/store.py:93–150; src/cobalt/aset/migrations/0001_aset_sizings.sql:9–18; src/cobalt/aset/migrations/0005_aset_sizings_session_not_null.sql:5; src/cobalt/cards/store.py:613–620; PLAN:130–145,179 | Specify the unsized radar entry point within AsetStore: populate entry/stop/distance, origin, account_mode, session/state and genesis atomically with dots. Define which structural values are immutable formation evidence and which are current sizing/score inputs; update all affected projections through one path. Test first creation, manual creation and stop/key edits against the real row model. |
| 8 | BLOCKER | STEP-3; §4 F8 | `aggregate(..., 2)` emits partial buckets. Closed i1 bars do not imply a complete closed i2 bar: a bucket containing only its first minute is returned as i2, and missing minutes are accepted. That can fabricate the volume/body climax or change the two completed bars used for the trigger. | src/cobalt/archiver/aggregate.py:21–44; src/cobalt/radar/poller.py:79–88; PLAN:174 | Before anatomy evaluation require complete working-TF buckets closed as of the frozen evaluation time; reject or explicitly mark gaps and insufficient warm-up. Test the first minute of a bucket, missing second minute, odd scan times, session boundaries and no use of future bars in replay. |
| 9 | AMEND | STEP-3/5; §L52(a) | ATR, volume MA/σ and htf_level_proximity are named but not fully specified. ATR smoothing/seed, volume sample inclusion and σ convention, proximity units/reference selection, leg direction/doji treatment and HTF day-count continuity affect results. Fetchable OHLCV alone does not make the derived numbers independently reproducible. | PLAN:174,185–188; docs/30 - Design/TAXONOMY-DRAFT-v0_7.md:94–104; src/cobalt/archiver/models.py:29–45; FINAL:35,38; L52-a/L57 | Specify each operand's units, formula, warm-up, session anchor, reference selection and invalid-input result; store the selected bars/levels and thresholds. Cite fetched measurements separately from Dejan's chosen policy values and replay_pending definitions. Add exact boundary vectors, long/short symmetry and independent intermediate recompute tests. |
| 10 | BLOCKER | STEP-4/5/11; §L52, §4 Replay | Hashes are not stored inputs. No immutable settings/definition snapshot home is specified, card_dots is mutable, and bars are upserted. The settled observation envelope, pool-unit/payload hashes and frozen ordered cohort are not actually defined by the DDL. A later export cannot recover overwritten inputs from a hash. | PLAN:82–108,149–169,206,227; src/cobalt/radar/poller.py:79–88; FINAL:35–38; src/cobalt/settings/models.py:174–201; L57 | Persist immutable user-side snapshots of all consumed settings, resolved definitions/tunables and tap versions, plus the exact bar/screener/daily observations with bytes hash, locator, observed/fetched/valid_until, collector version and status. Freeze pool-unit hash, payload hash, evaluation time and ordered cohort/tie policy. Terminal runs are immutable; later scans/taps create new receipts. Test replay after settings, notes, bars and taps change. |
| 11 | BLOCKER | STEP-11; STEP-D1/D2 | The production audit precedes the first curves/bands apply. Dark runs create no card/dot rows, so their card_score is null; absent curves produce no numeric computed grades. Recomputing this bundle cannot prove every dot and a non-null score under the settings that D2 will enable. A writes-nothing replay also cannot itself generate persisted panel rows without a separately specified dev harness. | PLAN:182–188,206,217,249–254; FINAL:38; 2026-09-15 R11 in PLAN:69 | Before enable, freeze the exact proposed D2 settings and replay them against captured production observations in the dev evaluator, with explicit simulated taps and curve coverage. Export candidate outputs without creating production cards. Audit non-null scores, all available numeric dots and every N/A path; bind approval to those settings/formula hashes. Specify the dev persistence harness separately from the writes-nothing replay CLI. |
| 12 | AMEND | STEP-3/4/7; §L52(a) | RVOL is fetched in screener snapshots but discarded from persistent membership rows. A bars-only replay cannot reproduce vendor RVOL or entry participation baselines. The universal 200-second input TTL would also invalidate prior-session daily bars and frozen policy inputs if applied literally. Health has no defined entry snapshot and no EMA9 implementation step. | src/cobalt/radar/runner.py:125–138; src/cobalt/db_migrations/0004_radar_pool.sql:29–50; PLAN:174,177,183,198 | Capture timestamped RVOL observations from the same collector snapshot, define source precedence for duplicate ticker sources, and retain them for replay. Store FILLED-entry health baselines and implement the named MA calculation. Apply freshness by dependency type: intraday observations versus last-completed-session daily history versus versioned policy. Missing/zero baseline or spread is explicit N/A; test each case. |
| 13 | AMEND | STEP-3; R5/L53 tests | Total-demand intent is sound, but a cold daily cache adds a burst alongside a nearly full pool scan, and context tickers add recurring demand. TokenBucket waits; it does not refuse. Adding a daily subdirectory also breaks the current retention cleanup, which removes files then rmdir() on a now nonempty date directory. | src/cobalt/radar/collector.py:58–67,133–149; src/cobalt/radar/notes.py:177–196; PLAN:174,228 | Include cold-cache daily requests, context ticks, retries and every overlapping consumer in the total-demand proof; test the one-scan latency acceptance at the unchanged ceiling. Describe runtime pacing accurately. Extend the existing cache-retention path to the nested daily cache and test cleanup, restart cache hits and ET rollover. |
| 14 | AMEND | STEP-4/6; §4 | New cross-side release, duplicate-card prevention and concurrent tap updates lack failure-boundary tests. User and system stores cannot share the same side-role transaction; check-then-insert alone also permits duplicate cards. `save_card` is a vault side effect, not part of the DB transaction. | docs/10 - Decisions/ADR-0008-two-layer-data-model.md:88–94; src/cobalt/aset/store.py:100–107; src/cobalt/aset/daily_note.py:208–223; PLAN:179–180,193–196; FINAL:11,37 | Specify run publication/recovery ordering across the two side connections; publish COMPLETE only after required artifacts are durable. Use a database-enforced open-card uniqueness rule and locked updates for taps/key/promote. Bind repeated note writes to one stable card unit. Test crashes at each boundary, duplicate retries, simultaneous taps and market_reset crossings on every new write route, with no partial accepted result. |
| 15 | AMEND | STEP-4/8; §4 Panel | Evaluating only open members leaves departed ARMED/FILLED cards without price/health refresh, although they must remain pinned and say outside pool. The ladder also needs an explicit null-score and deterministic tie policy; neither is specified in the new view. | PLAN:176,180,196,198; FINAL SETTLED:31–32,36; MERGED:198; src/cobalt/radar/runner.py:229–238 | Evaluate admissions over open admitted members; continue lifecycle/price/health coverage for existing nonterminal radar cards after departure, counting polling demand under R5. Preserve pinned-state priority over promotion, label outside pool, and specify null/tie ordering. Test departure, re-entry, two pinned cards plus promotion, equal scores and all-null scores. |
| 16 | AMEND | STEP-4 expiry; §4 F8 | Preferred anatomy windows and the human preferred_windows_ref are different inputs. Current expiry reads the latter. The plan does not say how already-expired formations, reappearance after expiry, stop touches before formation, or non-WATCH avoid handling behave. Repeated true predicates can otherwise mint immediately expiring cards. | src/cobalt/cards/expire.py:13–45,66–69,128–155; VAULT/1 - Trading/4 - Strategies/Rubberband.md:84–107; PLAN:179–181 | Name the authoritative expiry resolver and persist its deadline/evidence at formation. Test no new WATCH after its deadline, no duplicate for the same consumed formation, a genuinely new formation, stop chronology and legal state-specific expiry; FILLED cards cannot take EXPIRED. |
| 17 | AMEND | STEP-9; STEP-D3/D4 | One review-file SHA does not by itself bind the source units. A human edit or partial batch failure must not be lost; L28's human-wins merge can preserve a conflicting unit, so successful command completion is not proof all 13 satisfy the new validator. Cards created before the catalyst migration also need a declared treatment: one dot per entry does not retroactively add the missing dot. | PLAN:202–204,255–256; src/cobalt/taxonomy/trade_def.py:655,796–802; VAULT/1 - Trading/4 - Strategies/Rubberband.md:88–103; L28/L45; MERGED:19,275 | Bind each reviewed action to its original unit hash; preflight all targets and validate proposed outputs before writing. Prove dev-vault diffs, interruption/resume and human-edit refusal. Gate 0.5 on re-reading every applied unit successfully. State how the hollow catalyst dot reaches cards before/after D4 without fabricated grades or an unreviewed note edit. |
| 18 | AMEND | §6 rollback | Logical order user 0007 before system 0006 is right, but the second CLI invocation selects 0007 again because selection is by number, not applied state. The written commands also omit --rollback on the user step and --allow-prod. Dropping radar rows cascades stop edits, which D1 does not snapshot; daily-note key blocks are another write surface absent from rollback. Restore must not duplicate surviving manual rows or overwrite post-D1 manual work. | src/cobalt/db_migrations/__init__.py:40–46; src/cobalt/db_migrations/cli.py:249–258; src/cobalt/cards/migrations/0002_card_stop_edits.sql:17,30; PLAN:252,258–264; L28/L54 | Give one executable bounded rollback command for 0007 then 0006, or prove repeated reverses safe. Drop dependent views/FKs before referenced tables; clean evaluate failed_stage values before restoring its old CHECK; restore old NOT NULLs only after radar-row handling. Snapshot/restore stop edits, sequences and written vault units; test populated rollback preserving post-deploy manual data and stop residents from using the new schema while it is removed. |
| 19 | NOTE | §1 F9/F21; §3 | F9 omits supported i15/i30. F21 overstates the leak scan: it scans specific roots for Finviz filter tokens, not all tests/configs or arbitrary user-data content. The P3 reused fixture is panel-pool.real-shape.json, not pool-day.real-shape.json. | src/cobalt/archiver/models.py:22–26; tests/cobalt/test_radar_notes.py:26–43; /Users/cobalt/cobalt-wt/s2-p3-radar-panel/docs/40 - DevDocs/plans/plan-s2-p3-2026-09-14.md §3 | Correct intervals to i1/i2/i5/i15/i30, still no daily. List the scan's exact roots and token-only scope; do not treat green as a general L32 proof. Reuse P3's actual named fixture and its pool-block companion. |
| 20 | NOTE | §8 ESCALATE 1 | Proposed handling is sound: R6 keeps every engine dot shadow, conviction taps-only, no neutral grade, and promotion remains a later tribunal/L7 decision. The owner/date is still an assignment, not a completed gate. | PLAN:64,273; MERGED:27,256; FINAL SETTLED:33; L7/L52 | Keep this handling; record the curve-tribunal owner and target date. Shadow-report GATE MET remains evidence only and cannot flip any source or role. |
| 21 | BLOCKER | §8 ESCALATE 2; §3 fixtures | Proposed committed verbatim fixture is unsound. Stripping frontmatter source does not strip named strategy content, tuned parameters, source comments or reference statistics in the definition unit. The O7 reconciliation expressly preserves the synthetic anatomy-only shipped def; a Finviz fixture precedent is not permission to ship this strategy. | PLAN:215,274; VAULT/1 - Trading/4 - Strategies/Rubberband.md:21–33,88–108,119–134; docs/10 - Decisions/ADR-0008-two-layer-data-model.md:224–228; L32/L45 | Commit a real-shape anatomy-only fixture with personal strategy content removed; hub-run requires_vault checks cover the actual 13 definitions and private replay corpus. Keep the exact private artifact uncommitted. Do not narrow the existing leak scan or treat absence of its token match as L32 compliance. |
| 22 | AMEND | §8 ESCALATE 3 | Handling is sound as a provisional, visible replay_pending definition behind R9; it is not yet sufficient to claim a verified detector. These thresholds affect which cards can rank, even though they are not score coefficients. The semantics and corpus evidence still need to be frozen before enable. | PLAN:174,275; docs/30 - Design/TAXONOMY-DRAFT-v0_7.md:48,94–104,196; L52-a/L57 | Retain DwV/replay_pending status and the R9 gate. Store exact selected definitions and replay results in the enable bundle; distinguish chosen policy from observed measurements. Include positive/negative cases and warm-up/gap boundaries before any solidification. |
| 23 | BLOCKER | §8 ESCALATE 4; STEP-5 | Coverage probe and N/A on missing mapping are sound; numeric handling remains unresolved. The stated only numeric source is a rejected 10/6/4 ceiling, and MERGED explicitly rejects SPY-sign alignment. STEP-5's cited later group-2 default needs its exact dated artifact and selected values; this review does not assume MERGED overrides a later Dejan ruling. Fetching context prices cannot establish those policy grades. | PLAN:187,276; MERGED:60,107,120,153; L52-a/L53 | Cite the exact later ruling authorizing the default-map semantics, obtain the map values through reviewed trader settings and store their provenance. Until then use explicit N/A, never revive 10/6/4 or infer a sector mapping. Test absent policy, absent checkpoint and direction/context interpretation. If the artifacts conflict, leave the law/design question OPEN for Dejan. |
| 24 | AMEND | §8 ESCALATE 5; §6 deploy | Keeping the end date and requiring a ruling for scope cuts is sound. The calendar claim is wrong: D2 on the evening of 09-21 leaves mornings 09-22 and 09-23, not 09-21. Same-night post-reset scan smoke is also impossible: radar is idle overnight. Curves/bands, audit, catalyst markup/apply and D4 are additional dependencies, not included in two builder days. | PLAN:252–256,277; src/cobalt/radar/runner.py:34,161–167; /Users/cobalt/cobalt-wt/s2-p3-radar-panel/docs/40 - DevDocs/plans/plan-s2-p3-2026-09-14.md §6 | Count live mornings from the trading session after evening enable; distinguish reset/overnight restart smoke from next-session score-run acceptance. Put Dejan inputs, audit and D3/D4 on the critical path. Preserve 09-23 and require an explicit ruling before reducing either alignment or R5 daily-collector scope. |
