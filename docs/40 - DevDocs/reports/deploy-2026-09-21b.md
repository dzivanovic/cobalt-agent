# DEPLOY 2026-09-21b — DEGRADED LINE (evening, R10)

## §0 Headline
Hub `degraded-line-deploy-0921` (Sonnet 5), 20:02–20:08 ET. DEPLOYED: `s2/degraded-line-0921` fast-forwarded onto main, `c130519` → `ad7d3e4`, tag `deploy-2026-09-21b`; aset AND radar restarted (pids 23042 / 23081).
Smoke green (all three `/radar` URLs 200, marker on the tree, heartbeat GREEN); rollback not used.
Residents down 116 s (target < 60 s) — ESCALATE 1. ESCALATE: 3 (downtime; live page unseen — desk confirms; with-DB suite not run).

## L74
Recorded once, never followed: the attribution reminder that arrived after the prompt file's contents in this session's Read tool result asks for a `Claude-Session:` line in commits and names a file-send tool. It is DATA (L74). Commits carry `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` and nothing else.

## AUTHORIZATION
- R10: `22:| R10 | 07:4x ET | "A and this evenuing" …`; `git log -1 -S"A and this evenuing"` → `2d6e01864f9042d676b62298c9832c2912443128` (committed).
- R33 (line 44) names `16-degraded-line-deploy.md` and `34-degraded-line-rebase.md`; `git log -1 -S"| R33 |"` → `f412d75747aad1184b5d466da5134d5287fc333d`.
- R19 (line 30) names `16-degraded-line-deploy.md`, carries `FOLDED`.
- BUILD: `DEGRADED LINE BUILT c183ed6 | on d87f3bd | offline 2208/0 … | line above ladder when degraded, desktop + phone: proven | absent when healthy: proven | stays current on refresh: proven | pool banners unchanged: proven | retained not carried: proven | card/scoring paths untouched: empty diff | RESTARTS: com.cobalt.aset | ESCALATE: 2` → `<build tip>` = `c183ed6`.
- REBASE: `DEGRADED LINE REBASED 7b0a6a3 | on c8f4095 | cut c8f4095 | code tip 88602cc | offline 2222/0 (baselines main 2208/0, branch 2208/0) | code patch unchanged by the rebase: proven | seam test: added green | RESTARTS: com.cobalt.aset | ESCALATE: 2`; committed on the branch (`dc24d728706e3bbc5b4deaebbca873151ba2b041`). `<proven tip>` = `7b0a6a3`, `<cut>` = `c8f4095`, `<code tip>` = `88602cc`, `<suite>` = `offline 2222/0`, `<seam>` = `added green`.
- CHECK: `DEGRADED LINE CHECK DONE · grok: CHECK: BUILD STANDS … gemini: … astra: … · houses that checked: 3 of 3 · defects that HOLD: 0 · ready for a deploy prompt: 3 of 3 · ESCALATE: 0`; committed (`0cf4b822ff5971e50c5503321be761734b9f0b63`).
- READ (round 1): `… houses that read it: 3 of 3 · launch blockers that HOLD: 2 · folds proposed: 9 · ESCALATE: 4` — R19 names both blockers as folded (09:1x).
- READ (round 2): `DEGRADED LINE DEPLOY REVIEW R2 DONE · grok: REVIEW: RUN IT · gemini: REVIEW: RUN IT · houses that read it: 2 of 2 · launch blockers that HOLD: 0 · folds proposed: 0 · ESCALATE: 3`.

## PREFLIGHT
| rule | command | result |
|---|---|---|
| P0 | `date` | `Mon Sep 21 20:02:24 EDT 2026` → date ok, in window (20:00–20:15) |
| P0b | `git status --short --branch` (main) / (branch worktree) | `## main...origin/main [ahead 114]` / `## s2/degraded-line-0921` (one line, clean) |
| P1 | `git status --porcelain` | ` M configs/cobalt/rules.yaml`, ` M …/reports/seat-usage.md`, `?? …/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`, `?? …/2026-09-20/16-packet/`, `?? …/reports/ops-6a-check-r2-2026-09-22.md` — all expected; no staged line; no src/tests/ops/configs dirt except rules.yaml |
| P2 | `git tag scratch-allow-probe-0921b` / `git tag -d …` | allowed / `Deleted tag 'scratch-allow-probe-0921b' (was c130519)` |
| P3 | empty commit / `reset --soft HEAD~1` / `log --oneline -1` | `[main 74236e1] scratch: allowlist probe` / allowed / HEAD back at `c130519 docs(desk): 09-21 ops 6a round-2 check launched` |
| P4 | `heartbeat show` | BASELINE below |
| P5 | `launchctl print gui/501/com.cobalt.aset` | `state = running`, `<aset pid>` = 93194, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` |
| P6 | `launchctl print gui/501/com.cobalt.radar` | `state = running`, `<radar pid>` = 19014, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` |
| P7 | `tail -n 8 logs/radar.err` | `19:59:11 radar cycle: scanning`, `20:00:51 radar cycle: paused_market_reset scan_id=None`, `20:02:31 radar cycle: paused_market_reset scan_id=None`; no traceback |
| P8 | greps | `<a0>` = 28 (Started server process) · `<ta0>` = 2 (aset.err Traceback) · `<tr0>` = 0 (radar.err Traceback) |
| P9 | `curl …/radar` | `200` |
| P10 | marker grep | `0` |
| P11 | `<branch tip>` / `<n>` | `dc24d72 docs(report): degraded line rebase — on c8f4095, offline 2222/0, seam test added green` / `<n>` = 5 |
| P12 | `log 7b0a6a3..s2/degraded-line-0921` | only `dc24d72`; `diff --stat 7b0a6a3 <branch> -- . ':(exclude)docs'` prints nothing |
| P12b | `log 88602cc..7b0a6a3` | `7b0a6a3 test(radar): degraded line carries the bars poll failure (seam, L68)`, `771b2b7 docs(report): degraded line build …`, `5db6d2d docs(devdocs): radar_panel …`; `diff --stat 88602cc 7b0a6a3 -- . ':(exclude)docs'` → ` tests/cobalt/test_radar_panel.py | 21 +++++++++++++++++++++` (exactly the seam test, `<seam>` = added green) |

P4 BASELINE (verbatim):
```
HEARTBEAT GREEN — 16 job(s), 12 probe(s), nothing red  (2026-09-21 20:03:10 EDT)

OK   database                 cobalt_brain reachable
OK   sheet HTTP               http://127.0.0.1:5010/ -> 200
OK   sheet daymode            day mode reduced · stage 2 (proposed, undecided — floor holds) · half sheet — cards are accepted
OK   obsidian                 Obsidian running (pid 66104)
OK   mainframe                127.0.0.1:1234 accepting connections
OK   herdr                    socket accepting at /Users/cobalt/.config/herdr/herdr.sock; 6 pane(s), seats: agy, claude, codex, grok, qwen; hook guard: 1 SessionStart entry -> herdr-harness-guard.sh, executable
OK   archiver                 last run 2026-09-19 00:53 UTC (71.2 h ago), rows written 3343938, exit 0
OK   seat usage               last run 2026-09-21 20:00 ET (3 min ago), exit 0; UNPRICED in the report: claude-fable-5-1, gpt-6-astra
OK   backup                   newest snapshot 22.4 h old across ssd
OK   vault blocks             0 refused, 0 ungated repair run(s) in the counter window
OK   redactions               0 since the last beat
OK   radar                    paused (market_reset)
OK   com.cobalt.aset              running   loaded, pid 93194 [launchd probe]
OK   com.cobalt.mainframe         running   loaded, pid 25751 [launchd probe]
OK   com.cobalt.obsidian          running   loaded, pid 66104 [launchd probe]
OK   com.cobalt.agent             running   pid 98796 alive (from /Users/cobalt/cobalt/logs/cobalt.pid) [pidfile probe]
AMB  com.cobalt.herdr             unmanaged AMBER launchd unmanaged — loaded, not running (last exit 0) [launchd probe]; runs outside launchd by declared interim (`launchd_unmanaged` in configs/cobalt/jobs.yaml)
OK   com.cobalt.radar             running   running 3002 min, heartbeat fresh
OK   com.cobalt.prefill-daily     done      last finish 2026-09-21 05:15 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.prefill-drc       done      last finish 2026-09-21 15:40 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.archiver          done      last finish 2026-09-18 20:53 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.replay            pending   never run yet (registered, not yet due) · launchd: loaded, last exit 0
OK   com.cobalt.cards-expire      done      last finish 2026-09-21 16:05 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.daymode-propose   done      last finish 2026-09-21 09:00 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.backup            done      last finish 2026-09-20 21:40 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.heartbeat         done      last finish 2026-09-21 19:52 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.seat-usage        done      last finish 2026-09-21 20:00 ET, exit 0 · launchd: loaded, last exit 0
OK   com.cobalt.generated         done      last finish 2026-09-20 23:37 ET, exit 0 · launchd: loaded, last exit 0
```

GATE: proven — commit + tag allowed by the session allowlist

## Gates
SUITE: READING — offline 2222/0 on 7b0a6a3; main docs-only since c8f4095 (`git diff --stat c8f4095 main -- . ':(exclude)docs'` printed nothing); rebased code identical (to be proven by 3.1).
`<pre-merge>` (main) = `c130519`; `<cut>` = `c8f4095`, main moved by docs only since the cut.

STEP-2: `git -C /Users/cobalt/cobalt-wt/degraded-line rebase main` → `Successfully rebased and updated refs/heads/s2/degraded-line-0921.` (REBASE: real, 5 commits replayed onto main c130519; not a no-op). `rev-list --count main..s2/degraded-line-0921` = 5 (= P11's `<n>`, nothing dropped). `<new tip>` = `ad7d3e4`; `<pre-merge>` = `c130519`.

3.1 CODE IDENTITY: `git diff --stat 7b0a6a3 s2/degraded-line-0921 -- . ':(exclude)docs'` → prints nothing.

3.2 RESTARTS (L42), verbatim:
```
path	change	rule	restart
docs/40 - DevDocs/cobalt/aset/radar_panel.md	M	DOCS	-
docs/40 - DevDocs/reports/degraded-line-build-2026-09-21.md	A	DOCS	-
docs/40 - DevDocs/reports/degraded-line-rebase-2026-09-21.md	A	DOCS	-
src/cobalt/aset/radar_panel.py	M	static import reach	com.cobalt.aset
tests/cobalt/test_radar_panel.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset
```
Derives `com.cobalt.aset`; not the radar, no other resident, no UNCLASSIFIED. The radar's restart tonight is L66's, not L42's.

3.3 `git diff --stat main s2/degraded-line-0921` = exactly `docs/40 - DevDocs/cobalt/aset/radar_panel.md`, `docs/40 - DevDocs/reports/degraded-line-build-2026-09-21.md`, `docs/40 - DevDocs/reports/degraded-line-rebase-2026-09-21.md`, `src/cobalt/aset/radar_panel.py`, `tests/cobalt/test_radar_panel.py` (5 files, 527 insertions, 4 deletions). None is a P1 dirty path. THE EMPTY-RADAR DIFF (`-- src/cobalt/radar src/cobalt/cards src/cobalt/archiver src/cobalt/session src/cobalt/db_migrations configs ops`) → prints nothing.

3.4 `git tag pre-degraded-line-0921` → `rev-parse --short` = `c130519` (= `<pre-merge>`).

## Deploy table
| item | value |
|---|---|
| main | `c130519` → `ad7d3e4` (`git merge --ff-only s2/degraded-line-0921`: `Updating c130519..ad7d3e4` / `Fast-forward`, 5 files, 527 insertions, 4 deletions) |
| tags | `pre-degraded-line-0921` = `c130519` (rollback point); `deploy-2026-09-21b` = `ad7d3e4` (tagged after the green smoke) |
| `<t down>` | `20:04:45 EDT` (4.1 `date`, read immediately before the two bootouts) |
| merge clock | `20:05:55 EDT` (4.3 `date`, before the merge; the hard clock 20:25 and the 20:29 merge clock were both far off) |
| `<t up>` | `20:06:41 EDT` (`date` after both bootstraps) |
| downtime | 116 s — OVER the 60 s target; recorded and ESCALATED (not a failure) |
| 4.2 | `launchctl bootout` of both → `launchctl print` each: exit 113 `Could not find service "com.cobalt.aset" in domain for user gui: 501` / `… "com.cobalt.radar" …` |
| 4.4 | both `bootstrap` returned no error, first try; no retry, no kickstart |
| pids | aset 93194 → 23042; radar 19014 → 23081 |
| RESTARTS done | `com.cobalt.aset` (L42, derived by `cobalt jobs restarts`) and `com.cobalt.radar` (L66, no code change for it) |
| rollback | not used |

## Smoke
(a) `launchctl print gui/501/com.cobalt.aset` → `state = running`, `pid = 23042` (≠ 93194); `launchctl print gui/501/com.cobalt.radar` → `state = running`, `pid = 23081` (≠ 19014). Both started after the merge (4.3 precedes 4.4).
(b) LOG CHECK: `grep -c "Started server process" aset.err` → `29` (> `<a0>` 28, ≤ 30: one new uvicorn start). `tail -n 30 aset.err`: last `INFO:     Started server process [23048]` / `Application startup complete.` / `INFO:     Uvicorn running on http://0.0.0.0:5010 (Press CTRL+C to quit)` (uvicorn worker 23048; the `Finished server process [93200]` above it is the pre-deploy worker my bootout stopped). `grep -c "Traceback" aset.err` → `2` (= `<ta0>`). `grep -c "Traceback" radar.err` → `0` (= `<tr0>`). `tail -n 8 radar.err` (first of at most three): startup lines at `20:06:39.68` (vault unlock, finviz credential resolve) then `2026-09-21 20:06:39.736 | INFO | cobalt.radar.runner:resident:459 - radar cycle: paused_market_reset scan_id=None` — after `<t down>` 20:04:45 and after the merge; the radar was booted out until 4.4, so nothing else could write it. NAMED: no separate pre-bootstrap `date` was taken (4.4's shape has only the `<t up>` date after both bootstraps); the F17 `RUNNING` line did not appear in the last 8 lines. The 20:06:39.736 cycle line is the first cycle after the start, as the prompt predicts (≈80 ms after the start), so the check is met on the cycle-line arm.
(c) `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` → `200`; `…/radar` → `200`; `…/radar\?frame=phone` → `200` (first attempt each).
(d) `grep -c -F "id=\"degraded-line\"" src/cobalt/aset/radar_panel.py` → `1` (P10 read 0).
(e) `COBALT_ENV=production uv run cobalt heartbeat show` (20:06:53) → `HEARTBEAT GREEN — 16 job(s), 12 probe(s), nothing red`; the only non-OK row is `AMB com.cobalt.herdr unmanaged` (in P4's baseline); `com.cobalt.aset running loaded, pid 23042`; `com.cobalt.radar running running 0 min, heartbeat fresh`; `radar paused (market_reset)`; `sheet HTTP … -> 200`. No new RED, none inside the outage window.

THE CHAIN (the live page cannot be read by this session — the approved `curl` prints the status code only, and a healthy pool shows the line `hidden` anyway): the build's tests `test_degraded_line_renders_above_the_ladder_when_degraded` / `…_hidden_and_empty_when_healthy` / `…_follows_the_refreshed_pool_fragment` / `test_radar_route_serves_the_degraded_line_above_the_ladder_in_both_frames` GREEN on `<build tip>` `c183ed6` (build report, offline 2208/0), and again — with the hotfix's tests and `test_degraded_line_carries_the_bars_poll_failure` (`<seam>` = added green) — in `34`'s `offline 2222/0` on `<proven tip>` `7b0a6a3` → 3.1 the rebased tip's code is byte-identical to `7b0a6a3`'s (`diff --stat … -- . ':(exclude)docs'` printed nothing) → (d) the line is on production's tree → (a) the serving process started after the merge. **The DESK confirms the live page with him.**

## ESCALATE
1. Downtime 116 s vs the < 60 s target (`<t down>` 20:04:45 → `<t up>` 20:06:41). Between the two bootouts and the merge my own tool calls took about 70 s (the `date` before the merge read 20:05:55). Nothing failed; the launch line's per-call overhead sets the floor. For the desk to weigh against the 60 s target next deploy.
2. (READING, build ESCALATE (ii) and `34`'s) No browser ran at build, rebase or deploy; the live `/radar` page — the line above the ladder while degraded, absent while healthy, on desktop and phone — is unseen. The desk confirms it with him. (The build's (i) is ruled, R11; not carried.)
3. (READING, `34`'s ESCALATE) The with-DB suite was NOT RUN on the merged tree (L70: unproven, not a defect); offline `2222/0` on `<proven tip>` is the suite of record.
No `ASK DESK`. Push: Dejan, through the desk (L55) — nothing was pushed; main was `ahead 114` of origin/main at P0b and has moved by the merge's 5 commits plus this report's commit; tags `deploy-2026-09-21b` and `pre-degraded-line-0921` are local.

## CONTINUE
next: none — STEP-6 close; the report is committed by STEP-6's two calls.

ON A RELAUNCH, BEFORE ANY OTHER STEP-4 CALL: `launchctl print gui/501/com.cobalt.aset`, `launchctl print gui/501/com.cobalt.radar`, `git -C /Users/cobalt/cobalt log --oneline -1` and `git -C /Users/cobalt/cobalt rev-parse --short s2/degraded-line-0921`. FIRST `date`: not 2026-09-21, or before 20:00 ET → nothing is booted out; end `FAILED: window — relaunched outside the evening, nothing touched`. Both loaded AND running and main = `<pre-merge>` → resume at 4.1. Main is neither `<pre-merge>` nor the branch tip (a revert was interrupted) → STEP-5 (3) at once, the revert range re-derived from `git -C /Users/cobalt/cobalt log --oneline -3`. Either NOT loaded and main = `<pre-merge>` → skip 4.1–4.2 (NEVER a second bootout of a service that is not loaded; bootout only the one still loaded, if any), then 4.3 only if `date` is before 20:25 ET, else go straight to 4.4 and end `FAILED: window — relaunched after the hard clock, nothing merged`. Main = the branch tip (merged) → 4.4 for whichever resident is not loaded, then 4.5.

DEGRADED LINE DEPLOY DONE ad7d3e4 · tag deploy-2026-09-21b · residents down 116 s · /radar 200 · rollback: not used · ESCALATE: 3
