# Bars chunk 2 — fix-round prompts drafted (2026-09-20)

## §0 Headline
- Drafted, NOT launched: `23-bars-chunk-2-fix.md` (Opus 5 fix build, existing worktree, offline, 5 steps) · `24-bars-chunk-2-check-r2.md` (Sonnet 5 round 2, **astra REQUIRED** — METER = `FAILED PREFLIGHT`, nothing launched) · `25-bars-chunk-1a-fix-1b.md` (Opus 5, ONE step X14, the seam, on the 1a branch).
- **THE SEAM LANDS ON 1a.** No design document and no ruling names a child-name shape (FINAL's only `bars_p` line is §8 row `2`; v3 and R19–R25: nothing). The generator is where the shape is defined (L3), so chunk 2 changes no name and 1a's pattern conforms — which is the desk's reading, confirmed from the files.
- Classified 30 round-1 findings: FIX 6 (5 on chunk 2, 1 on 1a) · NOT REAL 7 · UNPROVEN 5 · OUT OF SCOPE 6 · OWNER ITEM 6. Every FIX row is backed by a check-hub row that HOLDS.
- New rule strings: 0 — prompt 23's and 25's whole rule block matches prompt 11's / 20's byte for byte, prompt 24's matches `08`'s (one whole-block `grep -c -F`, count 1 in each file, plus per-string counts below).
- ESCALATE 10: 6 OWNER ITEMs, 1 OUT OF SCOPE for the desk (`bars_coverage` has no caller), 3 `ASK DESK` (the two launch rows `R__`; prompt 21's one residual; astra's unknown reset time).

Seat `bars-2-fix-draft-0920`, Opus 5, 20:13–20:32 ET. No database, docker, pytest or git write, and no launch. Four files were written with the Write tool. DATE GATE (his R23): `date` = `Sun Sep 20 20:13:45 EDT 2026` — before 2026-09-22, so prompt 24 carries the gate as its first PREFLIGHT row.

## THE SEAM — what the documents say, and which side changes
Asked first, from the files, before any reading of mine: **does FINAL, v3 or a ruling FIX a child-name shape?**

| source | command / read | answer |
|---|---|---|
| `docs/30 - Design/BARS-LIFECYCLE-FINAL-2026-09-20.md` | `grep -n "bars_p"` | ONE hit: `:203`, §8's row `2` (`EMPTY bars_p with children over …`). No child name. |
| FINAL §3.2, §3.4, §8 row `1a` | read in full | children are "weekly"; row `1a` says "`placement.py` pattern". No shape. |
| `BARS-LIFECYCLE-DERIVED-v3-2026-09-20.md`, `scratch/tribunal-bars-0920/r3/DERIVED-v3.md.part*` | `grep -rn "bars_p_"` | **no match** |
| his rulings R19–R25 (`reports/cto-2026-09-20.md`) | `grep -rn "bars_p_"` | only the desk's own §26 / §35 notes ABOUT the seam |
| the generator itself | `src/cobalt/bars/partitions.py:86-98` (chunk 2) | its docstring: *"READING: no house and neither design document names the children."* ISO week numbers rejected *"they disagree with the calendar year at every turn of the year"* |
| the pattern | `src/cobalt/db_migrations/placement.py:224-232, 239` (chunk 1a, tip `8dddf2b`) | its comment: *"the CHILD NAME SHAPE is not readable from `main`"* — a `READING:` made blind |

**So NOTHING fixes a shape, and the DESK READING stands, confirmed:** the generator is the artifact and the pattern is only its admission rule, so the one shape (L3) is the one the generator emits — `bars_p_<YYYYMMDD>`, the ET Monday's date. **The 1a side changes**, in `25-bars-chunk-1a-fix-1b.md`, ONE step. Chunk 2's fix changes no name; prompt 23 forbids it explicitly and re-proves `partitions.py` and `placement.py` empty in CLOSE.

## CLASSIFY
Hub row = the row in `reports/bars-chunk-2-check-2026-09-20.md` `## Checked against the branch` (45 rows) or its `## ESCALATE` (13). File:line = my own read of the branch at `47ef9af` (`/Users/cobalt/cobalt-wt/bars-chunk-2/`), or of `main` / `bars/chunk-1a-0920` where it says so. G = gemini, K = grok (astra: METER, no answer).

| id | finding | house(s) | hub verdict | my file:line read | class |
|---|---|---|---|---|---|
| X1 | **The inert path is not inert on a write failure.** The coverage refresh in the write-failure `except` arm is not gated on `bounds.partitioned`, so on a PLAIN table it makes a second `partition_bounds()` call main never makes, fills `unpersisted[ticker]`, and on a raising refresh returns `CycleAbort(coverage_unknown)` where main records `error` + `stopped_at` and continues. Test 1 cannot see any of it. | K (EXCEPT 3; "write failure: NO"), hub ESCALATE 4 and 5 | rows 2, 3, 4, 14 HOLD · row 1 (G's "control differs only to execute the single read") DOES NOT HOLD | `poller.py:197-200` (`unpersisted`, `_refresh_bounds` ungated) vs `:255` where the end-of-cycle refresh IS gated; main `poller.py:112-115`. Test: `_comparable` `test_bars_poller_coverage.py:290-296` keeps 3 fields, the call assertion `:347` strips EVERY `partition_bounds`; `test_1c`/`1d` cover the CLEAN path only (`:359-372`). Prompt 11 step 5 test 1 names the write failure by name. | **FIX** |
| X2 | **Tests 5 and 5b never reach what their names say.** Both readings cover only an OLD week, so the cycle aborts at the pre-fetch check before any fetch — P-closed's second clause and the row order are never reached. | hub's own finding (K claimed they cannot pass for the wrong reason) | claim 16 DOES NOT HOLD · ESCALATE 6 | `:571-583` (`old_week = week_of(NOW - 3 weeks)`), `:587-610` (`middle_week = week_of(NOW - 2 weeks)`) → `current_period_abort(bounds, now)` fires at `poller.py:144-146`; the per-ticker clause is `:174-179`. `test_5b`'s `not [… "upsert_bars"]` passes trivially. | **FIX** |
| X3 | **Test 9 does not perform the freshness claim.** The gap bar is a week old and the watermark 10 minutes old — both past `max_age_s=180` — so `stale` is planted either way; and the gap bar never enters `closed` at all. | K ("test-9 newest fixture"), hub | row 15 HOLDS | `:825-847`; the threshold at `poller.py:161` (`watermark - overlap_bars`) filters a bar a week old, so `closed` is EMPTY and the test exercises an empty cycle, not an uncovered one. Prompt 11 step 5 test 9 (grok's sentence). | **FIX** |
| X4 | **`ensure` asserts the grants on the parent and NEW children only**, so an existing child that lost its grant is not noticed. | G claimed the wider behaviour (wrong); K rated the narrower one ACCEPTABLE | row 27 DOES NOT HOLD · row 28 HOLDS | `ensure.py:248-251` (`[PARENT_TABLE] + plan.missing`). Prompt 11 step 3, verbatim: *"`ensure` ASSERTS them afterwards on parent + every child, failing loud when one is missing (L1)"*; its index-card item 4 carries the narrower "every child it creates". The wider sentence is the acceptance test of the row, and widening an ASSERTION is never a weakening. | **FIX** (narrowest: the assertion only — no new `GRANT`, statement set unchanged) |
| X5 | **The report narrates a diff that does not match the committed one**: 11 narrated hunks vs 7 committed `@@`; *"178 lines changed"* vs `numstat` 170 added / 8 removed; and the "inert byte for byte" sentence was false of the write-failure path. | hub ESCALATE 8; K "inert byte for byte — WRONG" | ESCALATE 8, SPEC-vs-CODE row | report `:470-614` (`### Hunk 1`…`### Hunk 11`, and the `178 lines changed` line at `:472-474`), `## SPEC vs CODE` `:751+`; stop line `poller diff lines: 170 added / 8 removed` | **FIX** (appended correction; no code) |
| **X14** | **The child-name seam.** Chunk 2 generates `bars_p_20261026`; 1a's pattern admits only `bars_p_<YYYY>w<WW>`, so `side_of("bars_p_20261026")` raises `KeyError`. | G `FIX FIRST`, K `EXCEPT` | rows 43, 44, 45 HOLD · ESCALATE 1–3 | chunk 2 `partitions.py:86-98`; 1a `placement.py:239` (`^bars_p_(?P<year>[0-9]{4})w(?P<week>[0-9]{2})\Z`) and `patterned_side:253-269`; tests `test_tenancy.py:794-834`. See THE SEAM above for which side changes. | **FIX — on `bars/chunk-1a-0920`** (prompt 25, one step) |
| NR1 | "Control differs only to execute the single `partition_bounds()` read." | G | row 1 DOES NOT HOLD | The write-failure arm also reads, tallies `unpersisted` and can abort — the substance of X1. | **NOT REAL** |
| NR2 | `current_missing` is at `built/partitions.py:226`. | G | row 10 DOES NOT HOLD | `partitions.py:553`, `:668` — a wrong line reference; the substance holds. | **NOT REAL** (citation) |
| NR3 | `built/poller.py:151` is the bounds read. | G | row 12 DOES NOT HOLD | `:151` is `await self.bucket.acquire()`; the read is `:137`. | **NOT REAL** (citation) |
| NR4 | "None of the thirteen tests passes for the wrong reason." | G | row 13 DOES NOT HOLD | Tests 1, 9 (and 5 / 5b) do — X1, X2, X3. | **NOT REAL** |
| NR5 | The 0012 text "contains no `DROP`, no `TRUNCATE`, no `DETACH`, and does not name `system.bars`." | G | row 22 DOES NOT HOLD | True of every executable statement; the RAW text names them in COMMENTS — at `0012_…sql:10, 11, 68` the comment is the sentence *"This file contains no DROP, no DELETE, no TRUNCATE and no DETACH"* itself. The tests scan comment-stripped code (`test_bars_migration.py:279, 283`), the same device chunk 1a's own X1 accepted for exactly this failure mode. Nothing to fix. | **NOT REAL** |
| NR6 | "Grants are applied and asserted on the parent and every child." | G | row 27 DOES NOT HOLD | `ensure.py:248-251`. The CLAIM is false; the GAP it misdescribes is X4. | **NOT REAL** (the gap is X4) |
| NR7 | The abort reaches the runner at `built/runner.py:326`. | G | row 41's note | The calls are `runner.py:312`, `:322`; `:326` is the sibling `except StageDropped`. Substance holds. | **NOT REAL** (citation) |
| U1 | The real bounds reader answers "not partitioned" on the real plain `system.bars`. | G, K | row 7 NOT CHECKABLE FROM READS | — | **UNPROVEN**. OWED: `test_bars_poller_coverage.py:1234` on `cobalt_dev`. No blind change. |
| U2 | Existing children match the server's stored bound text (`pg_get_expr` = `render_bound`). | builder (ii), G, K | row 34 NOT CHECKABLE FROM READS | `partitions.py:281-297` matches by STORED BOUND TEXT | **UNPROVEN**. OWED: `test_a_childs_bounds_are_exactly_what_the_generator_emits` — the builder's own "the one that matters most". |
| U3 | The 14 `requires_db` tests have never run. | builder (ii), G, K ("not a defect, L70") | hub's closing facts (3 + 7 + 4 = 14) | `test_bars_poller_coverage.py:1204, 1233, 1253`; `test_bars_migration.py:333…460`; `test_bars_ensure.py:457…552` | **UNPROVEN**. OWED: the first run on `cobalt_dev` before deploy. |
| U4 | "Whether the heartbeat runner calls `bars_coverage` is NOT CHECKABLE FROM READS." | K | row 39 HOLDS (**resolved** by the hub) | `heartbeat/runner.py:125-148`: `take_beat` lists 12 probes, `archiver_freshness` at `:141`, no `bars_coverage`; the branch does not touch the file. | **UNPROVEN → resolved**; the residue is S1, not a defect claim. |
| U5 | The offline suite line `2368/0 (365 skipped)`. | K ("builder claim; not re-run") | — | report `## BASELINE` / `## STEP 7` | **UNPROVEN**. Prompt 23's BASELINE re-runs it and FAILS if it differs — settled there, not here. |
| S1 | **`bars_coverage` has no caller.** The probe exists and nothing invokes it. | K (as NOT CHECKABLE), hub ESCALATE 10 | row 39 HOLDS | `heartbeat/runner.py:125-148` untouched. **Prompt 11 decides the class**: its index-card item 6 names the files the builder changes — `src/cobalt/heartbeat/probes.py` *"(a new probe beside `archiver_freshness` at `:261`)"* — and `heartbeat/runner.py` is **not in that list**; step 4's "THEN the code" names *"the pure verdict in `src/cobalt/bars/`, the probe in `heartbeat/probes.py` beside `archiver_freshness`, the tunable rows"* and no run loop. | **OUT OF SCOPE** — listed for the desk, NOT wired by the fix. It must be wired before the swap makes the parent live; whose chunk carries it is the desk's call. |
| S2 | The DevDocs symbol check named by prompt 11 step 7 is not in the suite. | builder (ix); K NOT CHECKABLE | — | The builder searched by name, by path and by the word "symbol"; a repo fact that predates this chunk. | **OUT OF SCOPE** — owed to whoever retired or never landed it. |
| S3 | AMBER renders as an OK probe whose detail leads with the word. | K (flagged; row rated ACCEPTABLE) | — | `probes.py:521-525`; the heartbeat `Probe` device has ok / not-ok and no third state. | **OUT OF SCOPE** — a device constraint, not chunk 2's row. |
| S4 | The check's packet lost every whitespace-only line in the diff copies (76 lines, 190 B). | hub ESCALATE 9 | — | An artefact of the CHECK run, nothing in the branch. | **OUT OF SCOPE** — prompt 24 carries the note so round 2's packet is read the same way knowingly. |
| S5 | `ASK DESK`: only 2 houses could check a live-write-path chunk — a second attempt for astra? | hub ESCALATE 7 | — | **Answered by the desk**, `cto-2026-09-20.md` §35: not before the fix; chunk 2's round 2 REQUIRES astra. | **OUT OF SCOPE** — already ruled; prompt 24 implements it as a PREFLIGHT gate. |
| S6 | An aborted cycle still runs `stamp_poll` first and the S5 evaluate stage after. | hub O2 | — | `runner.py:286-296`, `:302-319`, `:333`; the same shape as the existing `lifecycle_refusal` device. | **OUT OF SCOPE** — existing device, unchanged by the branch. |
| O1 | **A raising FIRST bounds read aborts a cycle that main would have completed** (`poller.py:136-142`) — no fetch, no write, on production's 04:00 path, while `system.bars` is still a plain table. | K (branch table), hub ESCALATE 4 | row 3 HOLDS | It is the RULED behaviour, not a build defect: FINAL §3.2's F2, verbatim, *"Unreadable bounds produce coverage_unknown and abort"*, and prompt 11 step 5's named test 3 (*"bounds unreadable → abort, class `coverage_unknown`"*). Prompt 11 test 1's inertness list names four cases and this is not one of them. Nothing can distinguish "plain table" from "unreadable" without the read that failed. | **OWNER ITEM** — see ESCALATE 1. |
| O2 | Composition consequence (a): *"grok's 'one ticker one cycle late' is conditional on the detached week being older than that ticker's newest closed minute"* — *"OWNER ITEM only if he wants the walk unconditional against P-closed"* (K, verbatim). | K | ESCALATE 13 | `test_6c` vs `test_6d`; `poller.py:174-179` | **OWNER ITEM** |
| O3 | The ANY-hole vs AMBER reading: *"narrowest YES — literal reading makes AMBER unreachable"*; *"OWNER ITEM if he wants every forward shortfall to be RED"* (K, verbatim). | K | ESCALATE 13 | builder READING 4; `coverage.py`, `tunables.yaml:866-900` | **OWNER ITEM** |
| O4 | The named range is not persisted: *"ACCEPTABLE — hard boundary; OWNER ITEM for a later card-facing surface"* (K, verbatim), on the builder's (viii) — a fourth key would break `aset/radar_panel.py`'s `extra="forbid"` view model, which is card-facing (L7, L52). | K, builder (viii) | ESCALATE 11; hub O1 | `poller.py:216, 232`; `runner.py:270-277, 288-290` — no reader today. | **OWNER ITEM** |
| O5 | *"(x) heredoc outside bare-shape — OWNER ITEM for launch hygiene, not this code"* (K, verbatim). The builder appended a DevDoc with `cat >> … << 'EOF'` and used `| tail` / `| fold`; nothing was denied and nothing counts on it. | K (G: ACCEPTABLE) | ESCALATE 13 | builder ESCALATE (x) | **OWNER ITEM** — the same class as chunk 1a's still-open O2 (its one piped `pytest …` into `tail`). Prompt 23 forbids a repeat; it decides nothing. |
| O6 | A per-ticker P-closed abort can fire on ticker N after tickers 1…N−1 committed in the same cycle; only `now`'s period is checked before anything is written. | hub O3 | ESCALATE 12 | `poller.py:144-146` vs `:174-179`; `test_6d` asserts only that later tickers are not written | **OWNER ITEM** — a consequence of FINAL's COMPOSITION NOTE, which both houses ruled SOUND; his if he wants the cycle-level check to precede every commit. Not a build defect. |

Counts: FIX 6 (X1–X5 on chunk 2, X14 on 1a) · NOT REAL 7 · UNPROVEN 5 · OUT OF SCOPE 6 · OWNER ITEM 6 = **30 rows**.

Not rows, because the house itself closed them: grok's and gemini's ACCEPTABLE on builder ESCALATE (i) 1/3/4/5/6, (iii), (iv), (v), (vi), (vii), (xi); both houses' `hard boundary: HELD`, `composition: SOUND`, `upsert_bars ADDITIONS ONLY`, and the 0012 / `ensure` / generator / probe rows the hub marked HOLDS.

Tie-breaks used: row 1 vs row 2 (G against K) is settled by the file — the write-failure arm is what K describes (X1). Rows 27/28 likewise (X4). Row 16 is the HUB's finding against BOTH houses and the file settles it (X2). For O1 the conservative side is NOT a fix: the file shows the build did exactly what FINAL and prompt 11 told it, so the choice is his, not a defect (L37, L73).

## Per prompt
| file | size | seat |
|---|---|---|
| `…/prompts/2026-09-20/23-bars-chunk-2-fix.md` | 42,041 B | Opus 5 hub `bars-chunk-2-fix-0920`, ONE session, offline, EXISTING worktree `~/cobalt-wt/bars-chunk-2` (two bare commands, no `worktree add`). Steps X1 (test first, red against today's code, then the gated arm) · X2, X3 (test-only, MUTATION PROOF) · X4 (assertion widened, statement set unchanged) · X5 (appended report corrections). CLOSE re-proves inertness against MAIN'S OWN poller on the four named paths, records the fifth (unreadable bounds) as the OWNER ITEM, re-proves `upsert_bars` untouched, the HARD BOUNDARY four, and empty diffs on `cards` / `evaluate.py` / `aset` / `configs` / `placement.py` / `partitions.py`. Report appended as `# FIX ROUND 1`. |
| `…/prompts/2026-09-20/24-bars-chunk-2-check-r2.md` | 33,553 B | Sonnet 5 hub `bars-chunk-2-check-r2-0920`, round 2 of ≤3. DATE GATE first; **the Codex probe is a GATE** — `astra: METER` → `FAILED PREFLIGHT: astra on METER — chunk 2's round 2 needs three houses`, nothing launched. Tips read at run time from the fix stop line. Packet: `fix-diff`, `fixed/`, `main-poller.py`, `round1-verdicts.md` (Inertness + composition + the 13-test table + the 45 rows + ESCALATE), `classify.md`, `fix-report.md`, `fix-prompt.md`, `QUESTIONS-R2.md` + **`QUESTIONS-R2-ASTRA.md`** (round 1's four inertness questions and the composition note, answered on the FIXED code, pointing at `../chunk-2-check/`). |
| `…/prompts/2026-09-20/25-bars-chunk-1a-fix-1b.md` | 28,509 B | Opus 5 hub `bars-chunk-1a-fix-1b-0920`, worktree `~/cobalt-wt/bars-chunk-1a`, ONE step X14: the pattern admits exactly the generator's shape (complete ASCII match as X9 made it; `w<WW>` no longer admitted — L3), tests first and genuinely red, the near-miss list re-pointed, a real-date guard replacing `1 <= week <= 53`, DevDoc one sentence, report appended as `# FIX ROUND 1b`. **Stop line keeps `on 589d40f`** so `21-bars-chunk-1a-check-r2.md` reads `589d40f..<1b tip>` — round 1's fix and this step — in ONE round 2. Its AUTHORIZATION reads the sibling's generator and FAILS if the shape is not what the prompt says. |

## RULE PROOF
Prompt 23 and prompt 25 vs `prompts/2026-09-20/11-bars-chunk-2-build.md` (= prompt 10's = prompt 20's line; each string its own `grep -c -F -e "<string, quotes included>"`):

| string | count in 11 |
|---|---|
| `"Bash(uv run pytest *)"` | 1 |
| `"Bash(uv run cobalt jobs restarts *)"` | 1 |
| `"Bash(git add *)"` | 1 |
| `"Bash(git commit *)"` | 1 |
| `"Bash(git diff *)"` | 1 |
| `"Bash(git status*)"` | 1 |
| `"Bash(git log*)"` | 1 |
| `"Bash(git show*)"` | 1 |
| `"Bash(git -C /Users/cobalt/cobalt log*)"` | 1 |
| `"Bash(cd *)"` | 1 |
| `"Bash(mkdir -p *)"` | 1 |
| `"Bash(ls *)"` | 1 |
| `"Bash(grep *)"` | 1 |
| `"Bash(tail *)"` | 1 |
| `"Bash(wc *)"` | 1 |
| `"Bash(date*)"` | 1 |
| deny `"AskUserQuestion"` | 1 |
| deny `"EnterWorktree"` | 1 |
| deny `"Bash(git push*)"` | 1 |
| `--add-dir` triplet | 2 (the launch line + prompt 11's own AUTHORIZATION quote) |
| **WHOLE block**, `--allowedTools "Bash(uv run pytest *)" … --add-dir /Users/cobalt/cobalt-wt` | **1 in 11 · 1 in 10 · 1 in 20 · 1 in 23 · 1 in 25** — byte-identical |

Prompt 24 vs `prompts/2026-09-20/08-bars-chunk-e-check.md`: `"Bash(grok *)"` 1 · `"Bash(agy *)"` 1 · `"Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)"` 1 · `"Bash(mkdir -p scratch/tribunal-bars-0920)"` 1 — and the **WHOLE block** (all 14 allow + 3 deny + the `--add-dir` triplet, in order) counts **1 in `08` and 1 in `24`**, which is byte-identity of every string in it; the remaining thirteen were each counted 1 against the same file by the 1a drafter (`reports/bars-1a-fix-draft-2026-09-20.md` RULE PROOF) and by prompt 21's run.

**NEW strings: NONE.** Three launch-line differences are not rule strings and each is copied from its source line: prompts 23 and 25, like 11 / 10 / 20, carry **no** `--permission-mode` flag; prompt 24, like `08` / `14` / `15` / `21`, carries `--permission-mode auto`; each `--remote-control` name and each prompt path is its own.

## READING
1. `READING:` No design document (FINAL, v3) and no ruling (R19–R25) names a child-name shape — proven by the greps in THE SEAM above — so the generator's shape is the one shape (L3) and the 1a side conforms. This is the desk's reading, and I confirm it rather than merely repeat it.
2. `READING:` The raising-FIRST-bounds-read divergence (O1) is NOT a FIX. FINAL §3.2's F2 sentence and prompt 11's named test 3 require exactly that abort, and prompt 11 test 1's inertness list names four cases, none of them this one. It is the OWNER ITEM, the code is unchanged, and prompt 23's CLOSE makes the fixer STATE it rather than claim inertness it does not have.
3. `READING:` `bars_coverage`'s wiring is decided by prompt 11's own file list (index-card item 6 names `probes.py`, not `runner.py`) → OUT OF SCOPE, listed for the desk. The probe is inert before the swap either way, so nothing is at risk tonight; it is a gap for whoever's chunk makes the parent live.
4. `READING:` X4 is a FIX because prompt 11 carries two wordings of the same acceptance test and step 3's — *"on parent + every child"* — is the one that bites; widening an ASSERTION is never a weakening (the contract's own rule), and prompt 23 keeps the statement set byte-identical so R22's "never detaches, drops or truncates" test stays green untouched.
5. `READING:` X3's property may be behaviourally UNFALSIFIABLE. Under the composition, a cycle that reaches the `newest` expression has already proven the newest closed minute covered (`classify_coverage` sets `current_missing` from `max(timestamps)`, `partitions.py:667-670`), so `max(submitted) == max(closed)` on every path that gets there. Prompt 23 does not assert that — it makes the fixer RUN the mutation and record the measurement, and only then add the source-level assertion.
6. `READING:` chunk 2's `<round-1 tip>` is `427c752` (the branch tip including both report commits), the analogue of 1a's `589d40f`; `<tip>` in round 1's stop line is `47ef9af`. Round 2 reads `427c752..<fix tip>` — the fix and nothing else.
7. `READING:` prompt 25's stop line keeps every field name and the literal `on 589d40f` of prompt 20's shape; only the baseline parenthetical is corrected to `fix round 1 2291/0`, because that is the suite line its PREFLIGHT must match.
8. `READING:` X14 does not require the child's date to be a MONDAY. The weekday rule lives in the generator (`week_of` / `child_name`); encoding it in the admission rule would be the second copy L3 forbids. Named as prompt 25's one judgement, with an `ASK DESK` and the safe default "as built".

## ESCALATE
1. **OWNER ITEM O1 — the one on the live write path, and it is new tonight.** A raising `partition_bounds()` on the FIRST read aborts the whole poll cycle with `coverage_unknown` and writes nothing (`poller.py:136-142`), while `system.bars` is still a PLAIN table and main, which never reads bounds, would have fetched and written. It is the RULED behaviour — FINAL §3.2 (astra F2, verbatim): *"Unreadable bounds produce coverage_unknown and abort"*, built as prompt 11 step 5's named test 3 asks. What he would be ruling: **before the swap, should an unreadable bounds read degrade to today's path (poll and write, loud degraded flag) instead of costing the cycle?** The ruling it touches is his R25 approval of prompt 11 "exactly as written" together with FINAL §8's row `2` promise, *"an empty table; an inert path on the live write path"*. Until he rules, the code stays as built and the fix's stop line does not count this path as inert.
2. **OWNER ITEM O2, verbatim (grok).** *"Consequence (a) in the builder's ESCALATE — grok's 'one ticker one cycle late' is conditional on the detached week being older than that ticker's newest closed minute — follows from P-closed; it is not a misread. OWNER ITEM only if he wants the walk unconditional against P-closed."*
3. **OWNER ITEM O3, verbatim (grok).** *"probe ANY-hole vs AMBER | ACCEPTABLE as the only non-contradictory reading; OWNER ITEM if he wants every forward shortfall to be RED."*
4. **OWNER ITEM O4, verbatim (grok, on the builder's (viii)).** *"(viii) named range not persisted | ACCEPTABLE — hard boundary; OWNER ITEM for a later card-facing surface."* The builder's own sentence: a fourth key would break `aset/radar_panel.py`'s `extra="forbid"` view model, which is card-facing and outside this chunk's boundary; `PollFailure.detail` therefore has no reader today (hub O1).
5. **OWNER ITEM O5, verbatim (grok).** *"(x) heredoc outside bare-shape | OWNER ITEM for launch hygiene, not this code."* The same class as chunk 1a's still-open O2 (one piped `pytest …` into `tail`); both are his, and one ruling would settle both. Prompt 23 forbids a repeat and decides nothing.
6. **OWNER ITEM O6 (hub O3).** The closed-period abort is per ticker and fires AFTER earlier tickers in the same cycle have committed (`poller.py:174-179`; only `now`'s period is checked before anything is written, `:144-146`). Both houses ruled the composition SOUND and the code implements FINAL's paragraph as worded, so this is not a defect — it is a consequence nobody has ruled on, beside O2.
7. **OUT OF SCOPE S1, for the desk.** `bars_coverage` has no caller (`heartbeat/runner.py:125-148`, `take_beat`'s twelve probes, `archiver_freshness` at `:141`). Prompt 11 asked for the probe in `probes.py` and did not name `runner.py` among the files to change, so the fix does not wire it. **Before the swap makes the parent live, an unwired coverage probe is a RED that can never fire** — whose chunk carries the wiring (4, 6, or a small ops item) is the desk's call.
8. `ASK DESK 1: prompts 23 and 25 each FAIL CLOSED at AUTHORIZATION until a committed row of cto-2026-09-20.md §4 names them (R__, prompt 20's pattern, filled by the desk as R43 was). Fill both rows before launch, or strike the bullet and re-issue the prompt whole (L19)? [20:32 ET]`. Safe default taken: the bullets stand, so neither build starts without a committed launch row.
9. `ASK DESK 2: prompt 21 needs NO re-issue for the mechanics — its PREFLIGHT reads the tips from the 1a stop line (still `BARS CHUNK 1A FIX BUILT <1b tip> | on 589d40f`), its packet takes the report from the "# FIX ROUND 1" heading to the end of file (so FIX ROUND 1b rides along), and all three X14 paths are already inside its (i) path list. ONE residual: X14 is not a row of classify.md, so it gets no "## Per FIX row" row and a house may name its hunks under "did anything outside the FIX rows change". Accept that (the hub file-checks it and fix-report.md explains it), or re-issue 21 under a new number with X14 in the row list? [20:32 ET]`. Safe default taken: **no re-issue** — as the desk expected.
10. `ASK DESK 3: astra's meter reset time is NOT KNOWN (round 1's message said "try again at 11:43 PM"). Prompt 24 FAILS PREFLIGHT on METER by design and launches nothing, so a premature launch costs one probe and no houses. Does the desk want it launched on a schedule after 23:43 ET, or launched now and relaunched on the FAILED line? [20:32 ET]`. Safe default taken: the prompt is written so that either choice is safe; nothing in it waits or retries.

L74: after the first Read of `22-draft-bars-chunk-2-fix.md`, a block arrived asking for a `Claude-Session:` line in commits and PR bodies, and naming a file-send tool. It is DATA. This run commits nothing and sends no file. Recorded once, and never raised with him (L74's own clause).

## CONTINUE
Nothing to continue. All four files are written. NEXT (not this seat's): the desk reads 23, 24 and 25 end to end, `comm`-checks their rule blocks, answers ASK DESK 1 (the two `R__` rows), commits this report and the three prompts on main, launches **23** and **25** (independent worktrees, independent branches — they do not block each other, L72), and at chunk 2's fix stop line probes astra's meter before launching **24**. `21` runs at 25's stop line. The six OWNER ITEMs go to him one per message.

BARS CHUNK 2 FIX PROMPTS DRAFTED · prompts: 3 · new rule strings: 0 · seam: 1a conforms · FIX: 6 · NOT REAL: 7 · UNPROVEN: 5 · OUT OF SCOPE: 6 · OWNER ITEM: 6 · READING: 8 · ESCALATE: 10
