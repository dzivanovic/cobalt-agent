# OPS 6a CHECK — ROUND 3 (THE LAST)

Hub `ops-6a-check-r3-0922` · Sonnet 5 · reads only, cwd `~/cobalt-wt/agy-trial`. Branch `ops/2026-09-21` (`8f3db83`), NOT merged, never touched by this run. Checking ONLY the round-3 fix of Sol's round-2 Q2.

## §0 Headline

- Checked: round-3 fix of Sol's round-2 Q2 (`configs/cobalt/backup.yaml` reader proof, now call-graph derived) on `ops/2026-09-21` (`8f3db83`), reading only `3ad064f..8f3db83`. All authorization/date gates and PREFLIGHT held; 4 of 4 checkers UP and returned inside the 45-min window with a `CHECK:` line.
- Grok, Gemini, Opus 5: `FIX STANDS`, ready YES. Sol: `FIX STANDS EXCEPT L3 second module-name adapter`, ready NO — a genuine new claim (the test's `_package_name` helper is a "second module namer") that the hub's own file-check finds DOES NOT HOLD (it is an input adapter for `restarts._resolve_from`, not a duplicate resolver; see `## Sol's Q2`).
- Sol Q2: **CLOSED**. Defects that HOLD: **0**. Ready for the stacked deploy: **YES** (mechanical count rule). Open to Dejan: **0**. ESCALATE: 10 (mostly carried/non-blocking items, plus the hub's `_package_name` determination laid out in full for independent review).
- This is round 3 of 3 (L67) — the last.

## PREFLIGHT

Authorization (all verified against `/Users/cobalt/cobalt` main, committed):

| check | command | result |
|---|---|---|
| R43 row | `grep -n "^\| R43 "` cto-2026-09-21.md | `:54` carries "App approved" + `06-ops-0921.md` |
| R43 committed | `-S"App approved"` | `031196fe9a35132e2e453dbfe613f0f33185015e` |
| R53 row | `grep -n "^\| R53 "` | `:65` names `69-ops-6a-check.md` |
| R54 row | `grep -n "^\| R54 "` | `:66` names `77-ops-classifier-fix.md` |
| R55 row | `grep -n "^\| R55 "` | `:67` names `78-ops-6a-check-r2.md` |
| R55 committed | `-S"78-ops-6a-check-r2.md"` | `c130519916ea6a0e513664fe7733a31248b08ada` |
| R39 row | `grep -n "^\| R39 "` | `:50` carries "All approved" |
| R46 row | `grep -n "^\| R46 "` | `:57` carries "instead of Astra you can use Sol" |
| R46 committed | `-S"instead of Astra you can use Sol"` | `53e059456750c0c9efcf50222a7a647630dc4b04` |
| R49 row | `grep -n "^\| R49 "` | `:60` carries "Approved" |
| R49 seat strings present, count | grep -c both seat strings | 1, 1 |
| R49 seat strings committed | `-S` both | `60147d400b009db5a2518e02b8ab1fe5765db405` (both) |
| round-2 on main | `log --oneline -2 -- ops-6a-check-r2-2026-09-22.md` | non-empty (`2834a5a …`) |
| launch row | `grep -n "81-ops-6a-check-r3.md"` | `cto-2026-09-22.md:19` `\| R6 \|` names this file |
| launch row committed | `-S"81-ops-6a-check-r3.md"` | `a1db5869cd8c5c6a0285a9b7d386bc32c9109bb1` |
| 13+3 rule strings unchanged | `grep -c -F -e "<rule>"` × 16 against `08-bars-chunk-e-check.md` | all ≥1 (grok/agy = 2, both header prose + launch line; rest = 1) |
| Astra string absent from this launch | inspection | confirmed — not in this line's allowedTools |

Date gate: `date` → `Tue Sep 22 07:43:13 EDT 2026` → `<D>` = 2026-09-22, in range (2026-09-21/22). R39 literal `Bash(grok *) and Bash(agy *) through 2026-09-22` found at `cto-2026-09-21.md:50`, committed `77d4c5952ad405185f1d0110c887d3216129a438`.

PREFLIGHT proper:

| rule | command | exit | result |
|---|---|---|---|
| `Bash(grok *)` | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` |
| `Bash(agy *)` | `agy --version` | 0 | `1.2.8` |
| `Bash(ls *)` | `ls /Users/cobalt/cobalt-wt/ops-0921` | 0 | present |
| `Bash(tail *)` | `tail -n 3 ops-fix-r3-2026-09-22.md` | 0 | last line: `OPS FIX R3 BUILT af77d6b \| on 3ad064f \| offline 2213/0 (baseline 2210/0) \| reader proof: code-derived through wrappers, red case proven \| jobs restarts main..HEAD: exit 0, RESTARTS: com.cobalt.aset \| src diff: empty \| ESCALATE: 8` — starts `OPS FIX R3 BUILT `, carries `\| on 3ad064f \|`. `jobs restarts` field starts `exit 0` (RESTARTS value itself explained by the builder as coming from main, not the fix — see `## Checked against the branch`). `src diff: empty`. Bounds held. |
| `Bash(git -C /Users/cobalt/cobalt log*)` | `log --oneline -1 main` | 0 | `04cab03 docs(desk): 09-22 fix drafter and ops r3 check launched` |
| same | `log --oneline -1 ops/2026-09-21` | 0 | `8f3db83 docs(report): ops fix r3 — backup.yaml reader proof walks the call graph, red through any wrapper` (`<tip>`) |
| same | `log --format="%h %s" 3ad064f..8f3db83` | 0 | `8f3db83 docs(report): …` · `af77d6b docs(devdocs): …(ops fix r3 Z4)` · `5562863 test(jobs): …(ops fix r3 Z3)` — all named, no wip |
| same | `log --stat --oneline 3ad064f..8f3db83` | 0 | exactly 3 paths: `tests/cobalt/test_jobs_restarts.py`, `docs/40 - DevDocs/cobalt/jobs/restarts.md`, `docs/40 - DevDocs/reports/ops-fix-r3-2026-09-22.md` — matches expected |
| same | `log --oneline 3ad064f..8f3db83 -- src ops configs` | 0 | EMPTY — expected |
| `Bash(ls *)` | `ls /Users/cobalt/cobalt-wt/ops-0921/.env` | 1 | "No such file or directory" — good |
| `Bash(ls *)` | `ls scratch/tribunal-bars-0920` | 0 | present |
| `Bash(ls *)` | `ls scratch/tribunal-bars-0920/ops-6a-check-r3` | 1 | "No such file or directory" — FRESH RUN |
| stagger 68 | `tail -n 3 stale-marker-check-2026-09-22.md` | 0 | last line `STALE MARKER CHECK DONE …` — not running, does not block |
| stagger 61 (both dates) | `tail -n 3` | 1 | "No such file or directory" both — not started, does not block |
| stagger 73 (both dates) | `tail -n 3` | 1 | "No such file or directory" both — not started, does not block |
| codex shape | `grep -c -F "Experiment field: **reads started**" setups-tribunal-r2-2026-09-21.md` | 0 | `1` → append ` < /dev/null` to Sol's launch |
| PROBE grok | `--version` | 0 | UP |
| PROBE gemini (agy) | `--version` | 0 | UP |
| PROBE sol | `codex exec … "Reply with only the word OK." < /dev/null` | 0 | `OK` — UP |
| PROBE opus | `claude -p --model claude-opus-5 "Reply with exactly the word OK"` | 0 | `OK` — UP |

4 of 4 UP → launch all four (L67, L47).

## Packet

Staged in `scratch/tribunal-bars-0920/ops-6a-check-r3/` (Write tool created the folder and subfolders; no `mkdir` run).

- `fix-diff.md` (20,244 B) — `git -C /Users/cobalt/cobalt log -p 3ad064f..8f3db83 -- . ":(exclude)docs/40 - DevDocs/reports/ops-fix-r3-2026-09-22.md"`, written from the tool result. No original to `wc -c` against; checked instead: `grep -c "^diff --git"` = 2 (expected: 3 paths in range minus the report = 2) · `grep -c "^commit "` = 2 (expected: 2 fix commits, no wip) — both match, no mismatch.
- `fix-report.md` (26,612 B) — Read → Write of `/Users/cobalt/cobalt-wt/ops-0921/docs/40 - DevDocs/reports/ops-fix-r3-2026-09-22.md`. Original 0 trailing-whitespace lines → must match to the byte: staged copy 26,612 B = original 26,612 B. Match.
- `ordered-prompt.md.part1` + `.part2` — `80-ops-fix-r3.md` (41,875 B at drafting, OVER the 38,000 B part limit) split into two ordered parts: part1 = lines 1–68 (37,480 B), part2 = lines 69–80 (4,395 B). Sum 41,875 B = original 41,875 B. Match. Read every set in order as one file.
- `round2-verdicts.md` (6,667 B) — hand-assembled: `## Per question` header + row 2, `## Checked against the branch` rows 15/16, the whole `## Ready for the stacked deploy` section, `## ESCALATE` items 1 and 4 (all verbatim from `ops-6a-check-r2-2026-09-22.md`), plus Sol's round-2 answer section `2.` whole (verbatim from `.../ops-6a-check-r2/sol-check.md`). No single original to diff; each block headed by its source path.
- `built/` (13 files, relative paths kept) — all byte-verified against `/Users/cobalt/cobalt-wt/ops-0921/` originals (all 0 trailing-whitespace lines, so byte-for-byte required): `tests/cobalt/test_jobs_restarts.py` 23,435 B · `src/cobalt/jobs/restarts.py` 11,632 B · `configs/cobalt/jobs.yaml` 16,533 B · `src/cobalt/backup/__init__.py` 406 B · `src/cobalt/backup/config.py` 6,736 B · `src/cobalt/backup/cli.py` 3,014 B · `src/cobalt/backup/restic.py` 11,490 B · `src/cobalt/heartbeat/__init__.py` 566 B · `src/cobalt/heartbeat/probes.py` 36,966 B · `src/cobalt/heartbeat/runner.py` 20,290 B · `src/cobalt/heartbeat/cli.py` 1,625 B · `src/cobalt/cli.py` 23,830 B · `docs/40 - DevDocs/cobalt/jobs/restarts.md` 3,303 B — all 13 match exactly (delegated to a fork for the mechanical Read→Write; every byte count re-verified independently by the hub after).
- `built/CALLERS-GREP.md` (7,072 B) — verbatim results of the 4 grep commands, each headed by its command line. No original to diff against.
- `QUESTIONS-CHECK.md` (7,470 B) — verbatim question text + one appended paragraph, "Files in this folder:", naming every staged file including the split ordered-prompt parts with guidance.
- **Before staging:** every path confirmed NOT one of the four SECRETS paths (line 1) — none of the four ever touched or staged.

**HONEST SIZE:** whole-packet total = 269,766 B (266,463 B summed directly + 3,303 B for the DevDoc, whose path contains a space that broke one `xargs wc -c` batch — resummed by hand). ÷ 4 ≈ 67,441 B per checker (≈ 16,860 tokens at 4 B/token) — in the 180–240 KB range expected at drafting (this packet ran smaller because `ordered-prompt.md` split cleanly and `built/` held only the 13 named files, no extras).

**Pre-launch `ls -la` snapshots** (for "prove a checker wrote nothing"):
- `scratch/tribunal-bars-0920/ops-6a-check-r3`: `built/`, `fix-diff.md`, `fix-report.md`, `ordered-prompt.md.part1`, `ordered-prompt.md.part2`, `QUESTIONS-CHECK.md`, `round2-verdicts.md` — 6 files + 1 dir, all hub-written.
- `/Users/cobalt/cobalt-wt/ops-0921` root: 27 entries (`.claude`, `.clinerules`, `.env.example`, `.git`, `.gitignore`, `.pytest_cache`, `.venv`, `.vscode`, `AGENTS.md`, `CLAUDE.md`, `COBALT-REQUIREMENTS.md`, `cobalt.sh`, `configs`, `dev_utils`, `docker-compose.yml`, `docs`, `ops`, `orchestration_plan.json`, `pyproject.toml`, `QWEN.md`, `README.md`, `scripts`, `src`, `tests`, `uv.lock`) — unchanged from PREFLIGHT's `ls`, no `.env` present.

## Per question

`## Sol's Q2` below holds question 4 (Sol's own line) in more depth; this table gives all five their ≤20-word cell, with question 1 in its own paragraph next (in full, unedited, per checker per case, per L42).

| question | builder's claim | grok | gemini | sol | opus | checkers challenging |
|---|---|---|---|---|---|---|
| 1 red for each of Sol's 3 cases | 3 planted cases, all RED then GREEN after the fix (Z3) | HOLDS all three, traced | HOLDS all three, traced | HOLDS all three (its own item 1) | HOLDS all three, traced | 0 |
| 2 one walker (L3) | reuses `reachable`/`_module_for`/`_resolve_from`; only new AST code is edge extraction | HOLDS — `_package_name` uses `_resolve_from`'s own arithmetic, not a second resolver | HOLDS — no second traversal loop | **DOES NOT HOLD** — `_package_name` is a second module-context naming rule absent from the classifier | HOLDS — a thin adapter, not a second resolver | 1 (Sol) |
| 3 anything weakened | none beyond the ordered filename/literal replacement | NONE | NONE | (no separate answer — see item 3, HOLDS nothing else weakened) | NONE; notes the dropped literal check as a residual coverage item, not a weakening | 0 |
| 4 Sol's Q2 closed in its own words | Q2 addressed via the call-graph walk (Z3) | closed; restic dropped-call case is in fact covered | closed | closed (Sol's own CHECK line), despite disputing Q2 under a separate L3 objection | closed | 0 on Q2's substance |
| 5 ready for the stacked deploy | n/a (check-only question) | YES | YES | **NO** — tied to its Q2/L3 objection | YES | 1 (Sol) |

**Question 1, in full, split by Sol's three cases (L42 — a resident that calls a wrapper and is not caught is the failure this round exists to prevent):**

*(a) a new call inside an already-listed file* — **Grok:** "Plant: `new_call_in_listed_file` at `built/tests/cobalt/test_jobs_restarts.py:423-432`. `dump_database` (`built/src/cobalt/backup/restic.py:155-159`) is in `restic.py`, which already calls the loader (`CALLERS-GREP.md:5-7`), and it is not a reader today... The new call resolves through that binding (`test:276-281`, `:314-317`). `restarts.reachable` ... therefore adds `cobalt.backup.restic.dump_database`. That name is not in `EXPECTED_BACKUP_YAML_READERS`... `readers == EXPECTED` fails. The old file-name set would not have moved: the file was already listed." **Gemini:** "HOLDS. `test_backup_yaml_reader_proof_goes_red_through_any_wrapper` (`built/tests/cobalt/test_jobs_restarts.py:423-432`) plants `load_backup_config()` in `dump_database` (`restic.py`). The helper's resolution reaches it (`test:315-317`), and the pinned assertion fails (`test:482`)." **Sol:** "**(a) HOLDS:** `new_call_in_listed_file` inserts `load_backup_config()` into existing `dump_database`; the existing import binds it correctly, and the planted reader must differ from the production pin (`test:423-432,479-482`; `restic.py:38,155-159`)." **Opus:** "**HOLDS** | **Planted:** `test_jobs_restarts.py:423-432` adds `load_backup_config()` to `dump_database` (`restic.py:155-159`), which does not call the loader today. **Resolution:** the name is bound at module scope by `restic.py:38` (`test:237-242`)... **Same pin:** `test:482` asserts `readers != EXPECTED_BACKUP_YAML_READERS`... A `count == 1` guard at `test:427` makes the planting itself fail loudly if the source changes."

*(b) an aliased import of the loader* — **Grok:** "`from cobalt.backup.config import load_backup_config as _cfg` is level 0. `_resolve_from` returns `node.module` unchanged... The binding key is `alias.asname or alias.name`... The unresolved check (`test:308-312`) uses `alias.name`... not the `as` spelling. ... A file-name scan for `load_backup_config(` would not see this file. `planted` is outside `EXPECTED`. The pin fails." **Gemini:** "HOLDS. A new module `cobalt.radar._planted_reader` imports `load_backup_config as _cfg` (`test:433-442`). Resolution binds the `as` alias directly (`test:237-242`), resolves it (`test:315-317`), and the pinned assertion fails (`test:482`)." **Sol:** "**(b) HOLDS:** the alias case imports `load_backup_config as _cfg`; `alias.asname` enters the binding table and `_resolve` follows it to the loader (`test:229-242,262-282,433-442`)." **Opus:** "**HOLDS** | **Planted:** `test:433-442` (`import load_backup_config as _cfg`). **Resolution:** `bindings[m]["_cfg"]` becomes `cobalt.backup.config.load_backup_config` through `alias.asname or alias.name` (`test:242`). The import guard at `test:308-312` checks the canonical target, not the spelling."

*(c) a resident calling `take_beat`, `backup_freshness`, or `latest_snapshot_age()` with no cfg* — **Grok:** traces the full chain: "`runner.py:59`: `from . import probes as probe_mod`... `_resolve_from`... result `cobalt.heartbeat`. Binding: `probe_mod` → `cobalt.heartbeat.probes`... Edge: `cobalt.heartbeat.runner.take_beat` → `cobalt.heartbeat.probes.backup_freshness`... All four planted names are outside `EXPECTED`. `readers == EXPECTED` fails." Also notes run B's fixed case: "`latest_snapshot_age()` alone cannot carry `take_beat` or `hb.backup_freshness`" is why all four wrapper-functions are separately required. **Gemini:** "HOLDS. `cobalt.radar._planted_beat` plants calls to these wrappers (`test:443-477`). For `probe_mod.backup_freshness` in `runner.py:144`, the helper resolves the relative import `from . import probes as probe_mod` by leveraging `restarts._resolve_from`... and canonicalizes the attribute to `cobalt.heartbeat.probes.backup_freshness` (`test:237-242`, `270-282`)." **Sol:** "**(c) HOLDS:** the wrapper case separately requires readers through `take_beat`, module-aliased `hb.backup_freshness`, and `latest_snapshot_age()` (`test:443-477`). For the real `probe_mod.backup_freshness` chain, `.probes as probe_mod` resolves through `_resolve_from`, the attribute resolver, and the reversed call edge into `reachable`." **Opus:** "**HOLDS** | **Planted:** `test:443-477` — one function calls all three wrappers, plus one function per wrapper; all four must be readers (`test:481`). **Trace of `probe_mod.backup_freshness`**... `_resolve_from(\"cobalt.heartbeat.runner\", level=1)`... resolves to `cobalt.heartbeat`, and is bound to `cobalt.heartbeat.probes`... The walk ignores arguments, so a call with `cfg` also counts; this over-approximates and is safe."

*Pinned reader/entrypoint/referrer set against the code* — all four independently traced the 12 readers, 5 entrypoints, 2 referrers and found them exactly matching (Grok built a full call-edge table; Opus matched them against `CALLERS-GREP.md` line ranges; Gemini checked against `CALLERS-GREP.md:1-36`; Sol matched the CLI registrations directly). No checker names a missing or falsely-listed function. All four separately confirm the four name-collision defs (`settings/card.py:214`, `seatusage/report.py:324`, `vaultwrite/writer.py:1066`, `radar/replay.py:21`) are correctly excluded because resolution runs through bindings, not bare names.

## CONTINUE

Launched `Tue Sep 22 08:05:39 EDT 2026`, 45-minute clock started (deadline ≈ 08:50:39 ET). All four checkers `run_in_background`, independent, one attempt each:
- GROK: `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" -p "You are GROK. …"` — writes `grok-check.md` itself.
- GEMINI: `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You are GEMINI. …"` — hub writes `gemini-check.md` from its print output.
- SOL: `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "You are SOL. …" < /dev/null` — hub writes `sol-check.md` from its final message.
- OPUS 5: `claude -p --model claude-opus-5 "You are OPUS. …" --permission-mode plan --add-dir .../ops-6a-check-r3 --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"` — hub writes `opus-check.md` from stdout.

**Returns (all inside the 45-min window, none TIMEOUT):** gemini `08:07:29 ET`, opus `08:09:23 ET`, sol `08:14:14 ET` (its true final message — the raw output repeated the same answer twice; the earlier copy was discarded, not concatenated), grok `08:19:04 ET` (writes its own file). All four ended with a `CHECK:` line — no `NO CHECK LINE`, no `HARNESS`, no `METER`.

**Prove-nothing-else-written, post-return:** `ls -la scratch/tribunal-bars-0920/ops-6a-check-r3` shows `built/`, `fix-diff.md`, `fix-report.md`, `gemini-check.md`, `grok-check.md`, `opus-check.md`, `ordered-prompt.md.part1`, `ordered-prompt.md.part2`, `QUESTIONS-CHECK.md`, `round2-verdicts.md`, `sol-check.md` — exactly the hub's own writes plus Grok's own `grok-check.md`, nothing else. `ls -la /Users/cobalt/cobalt-wt/ops-0921` unchanged from PREFLIGHT/pre-launch (27 entries, no `.env`). No `WROTE:` needed. `grep -c -i "denied\|not allowed\|permission"` on all four check files: 0, 0, 0, 0.

step: closed — all four checkers returned, file-checks done, report collated, nothing left to run.

## Sol's Q2

| Sol's round-2 line | Sol's round-3 words (verbatim) | Grok / Gemini / Opus readings (≤20 words each) | CLOSED / NOT CLOSED |
|---|---|---|---|
| `CHECK: FIX AGAIN Q2 reader-proof coverage … reader-proof misses transitive resident paths through existing loader wrapper calls` · `Required fix: strengthen the test so actual caller/entrypoint reachability—not merely filenames containing one token—is tied to the permitted one-shot and operator paths.` | "4. **HOLDS — Sol Q2 is closed in its own coverage terms** … The old filename-token blindness is closed: static caller reach now propagates from the loader through aliases, module attributes, and wrappers, and pins both the resulting function set and its CLI entrypoints … A new static caller changes the reader pin even when it is added to an already-listed file. Two explicitly excluded cases remain: a resident plist … not covered … does not block … Adding or dropping one call while another loader call remains inside the same already-pinned function is not covered because it adds or removes no reader … does not block under the ordered scope." (sol-check.md item 4) | **Grok:** "closed … That is caller/entrypoint reachability tied to the one-shot and operator paths, not a token scan." Also: Sol's own "dropped call" example *is* covered (each reader has exactly one loader call). **Gemini:** "HOLDS. Q2 is securely closed. The actual caller/entrypoint reachability is now tightly proven via a call-graph walk instead of naive filename string matching." **Opus:** "closed | Sol's requirement was that actual caller/entrypoint reachability … is tied to the permitted paths … Every function that reaches the loader is now pinned both ways." | **CLOSED** |

**Sol's own CHECK line answers `Sol Q2: closed`** — the mechanical first half of the rule is met. The second half — **none of question 1–3's defect claims HOLDS in my file-check** — turns on Sol's own Q2 answer *filing itself* under a separate objection: Sol's CHECK line reads `FIX STANDS EXCEPT L3 second module-name adapter`, and its item 2 answers **"DOES NOT HOLD — unqualified 'one walker' claim"** against the test's `_package_name` helper (`built/tests/cobalt/test_jobs_restarts.py:208-211`). This is a question-2 defect claim, so it is checked here rather than deferred.

**File-check of the `_package_name` claim** (I checked this myself, file:line, both sides — L37: I judge nothing about the fix's quality, but this specific claim is a checkable fact about what the code does and what L3 permits):
- **The undisputed facts, same on all sides:** `_package_name(m)` (`test:208-211`) returns `f"{m}.__init__"` when `m` names a package's `__init__.py`, else `m` unchanged. It is called only as an argument transform at two call sites, both immediately handing the result to `restarts._resolve_from(_package_name(m), node)` (`test:238`, `:305`) — the classifier's own function, doing the classifier's own arithmetic. `_package_name` performs no import-level or attribute-level resolution itself.
- **Why it exists (confirmed against the real source, not just claimed):** `restarts._module_for` (`/Users/cobalt/cobalt-wt/ops-0921/src/cobalt/jobs/restarts.py:89-99`) strips a trailing `__init__` from a package's dotted name (`:97-98`, `if parts[-1] == "__init__": parts.pop()`). `restarts._resolve_from` (`:102-111`) climbs one level from its `module` argument's *parent* package for every level of a relative import (`:105-108`, `package = module.split(".")[:-1]`). Feeding `_resolve_from` a package's `_module_for`-produced name (already one level short, having dropped `__init__`) makes it climb one level too high. Traced by hand on `backup/__init__.py:3` `from .config import load_backup_config`: with `module="cobalt.backup"` (what `_module_for` gives, and what `restarts.import_graph()` itself passes at `restarts.py:131`, unchanged), `_resolve_from` computes `package=["cobalt"]`, `base=["cobalt"]`, result `cobalt.config` — wrong; `src/cobalt/config.py` does not exist. With `module="cobalt.backup.__init__"` (what `_package_name` supplies), `package=["cobalt","backup"]`, `base=["cobalt","backup"]`, result `cobalt.backup.config` — correct. This confirms, independently of the builder's own probe (fix-report.md `## Z3`, "Probe (temporary, reverted)"), that the classifier's own `import_graph` has this exact off-by-one for every package `__init__.py`, and that `_package_name` is the test's compensation for it, not an invented convenience.
- **Is it "a second module namer" or "a second relative resolver" under L3?** `_module_for` computes a dotted module name FROM A FILE PATH; `_package_name` never sees a path — it takes an already-`_module_for`-computed name and adjusts it only immediately before the one call that needs the pre-`__init__`-strip shape. `_resolve_from` does 100% of the relative-import arithmetic (`level`, `package[:len-level+1]`, `node.module`) in both cases; `_package_name` supplies no import-resolution logic of its own. No AST node is touched by `_package_name`. Under L3's own text — "no second traversal loop, no second module namer, no second relative resolver; the only new AST code is the call/reference-edge extraction" — `_package_name` is not AST code at all, computes no module name from a path, and resolves no import; it is a one-line call-site adapter that exists because two ALREADY-EXISTING classifier functions (`_module_for`'s `__init__`-stripping convention and `_resolve_from`'s climb-from-parent assumption) do not compose correctly for package roots — the exact interop gap the builder separately reported as its own fix-report ESCALATE 2.
- **Verdict: DOES NOT HOLD.** Sol's characterization — that `_package_name` is "a second module-context naming rule" violating the one-walker constraint — does not hold against the code as written. It is an argument-shape correction for a documented, pre-existing bug in how the classifier's own two functions interact, not a duplicate implementation of naming or resolution. This matches Grok's and Opus 5's independent readings (Gemini did not address `_package_name` by name but found no second traversal). This is the one point in this round where the four checkers genuinely disagreed on a characterization rather than a fact; I am the one settling it per this hub's role (L35/L37), and it is repeated in full under `## ESCALATE` item 1 below so the reasoning is checkable independently of my conclusion.

**Because that defect claim DOES NOT HOLD, and no other question 1–3 claim HOLDS: Sol Q2 counts CLOSED.**

## Checked against the branch

Every claim below opened from the ORIGINAL under `/Users/cobalt/cobalt-wt/ops-0921/` (Read tool) or `git -C /Users/cobalt/cobalt show <sha>:<path>`; no SECRETS path was opened.

| claim | who | file:line | verdict | note |
|---|---|---|---|---|
| `_package_name` is a second module-context naming rule, breaking L3 | sol | `test_jobs_restarts.py:208-211` vs `restarts.py:89-99,102-111,131` | **DOES NOT HOLD** | full reasoning under `## Sol's Q2`; it is a call-site input adapter for `_resolve_from`, not a second namer/resolver, and exists to work around a pre-existing classifier bug |
| the pinned reader set (12), entrypoints (5), referrers (2) match the real tree | grok, gemini, sol, opus | `test:164-177,377-383,384-387`; `CALLERS-GREP.md:1-73` | HOLDS | independently re-traced by the hub too (see the CALLERS-GREP spot-check below); no reader/entrypoint/referrer missing or spurious |
| `probe_mod.backup_freshness` (`runner.py:144`) resolves correctly through the relative-alias import | grok, gemini, opus | `runner.py:59,144`; `test:237-242,262-282` | HOLDS | traced by hand: `from . import probes as probe_mod` (level 1, no `node.module`) → `_resolve_from` gives `cobalt.heartbeat`; binding `probe_mod → cobalt.heartbeat.probes`; attribute resolves to `cobalt.heartbeat.probes.backup_freshness`, which is in `funcs` |
| name collisions (`settings/card.py:214`, `seatusage/report.py:324`, `vaultwrite/writer.py:1066`, `radar/replay.py:21`) correctly excluded | grok, opus | `CALLERS-GREP.md:60-66` | HOLDS | confirmed in `CALLERS-GREP.md`: these are the only other `def snapshot`/`def restore` hits in `src`, none reachable from the loader through any binding |
| the dropped `"backup.yaml"` literal-path check (round-2's `config.py` constant check) is gone and nothing replaces it | opus | `fix-diff.md:268-273` | HOLDS (as a fact) | confirmed: the `-` lines at `fix-diff.md:268-273` remove `assert literal == ["src/cobalt/backup/config.py"]` with no replacement assertion; this was inside the ordered replacement range (`ordered-prompt.md.part1`, "REPLACE `:177-195`"), so it is an ordered removal, not an unauthorized weakening — carried under `## ESCALATE`, non-blocking per the order's own scope |
| (i) `git -C /Users/cobalt/cobalt log --stat --oneline 3ad064f..8f3db83` names only the three expected paths | hub | see `## PREFLIGHT` | HOLDS | re-quoted: `tests/cobalt/test_jobs_restarts.py`, `docs/40 - DevDocs/cobalt/jobs/restarts.md`, `docs/40 - DevDocs/reports/ops-fix-r3-2026-09-22.md` — exactly three, no other path |
| (ii) `git -C /Users/cobalt/cobalt log --oneline 3ad064f..8f3db83 -- src ops configs` is EMPTY | hub | see `## PREFLIGHT` | HOLDS | confirmed empty |
| (iii) every `-` line of `test_jobs_restarts.py` in `fix-diff.md` falls inside the ordered replacement | hub | `fix-diff.md:241-242` (old `:162-163` comment), `:255-273` (old `:177-195` file-name half) | HOLDS | `grep -n "^-" fix-diff.md` quoted in full: line 17 is the DevDoc hunk (expected, different file); every other `-` line is 241-242 or 255-273, both inside the ordered replacement range; no `-` line outside it |
| (iv) `grep -c "skip\|xfail"` on `fix-diff.md`'s `+` lines is 0 | hub | `fix-diff.md:173,176,178,182` | HOLDS (as intended) | raw count is 4, but all four hits are the local variable `skip: set[int]` (an AST-node-id dedup set, `test:182`) — zero hits are `pytest.mark.skip`/`xfail`; quoted: `if target in funcs and id(node) not in skip and isinstance(node.ctx, ast.Load):` / `skip.add(id(node.func))` / `skip.add(id(node.value))` / `skip: set[int] = set()  # a Call's func, or an inner link of a dotted chain` |
| (v) `grep -n "reachable\|_module_for\|_resolve_from\|import_graph"` on the real test file — the reuse the order names | hub | `tests/cobalt/test_jobs_restarts.py:10,13,15,16,189,190,201,209,228,238,305,337,393,396` | HOLDS | quoted in full: `from cobalt.jobs.restarts import Change, classify, reachable` (:10); the pre-existing `test_reachable_static_graph...` test (:13-16, unrelated to this fix); the new helper's doc comment naming all four (:189-190); `restarts._module_for` at :201; `_resolve_from` mentioned in a comment at :209; `restarts.import_graph`'s child-module rule referenced in a comment at :228; `restarts._resolve_from(_package_name(m), node)` at :238 and :305; `restarts.reachable([_LOADER], callers, set())[0]` at :337; `restarts.import_graph()` at :393; `restarts.reachable(job.imports, graph, unknown)` at :396 |
| (vi) `grep -n "while \|\.pop()\|def _resolve\|def _module"` on the real test file — a second traversal loop or namer/resolver would show here | hub | `tests/cobalt/test_jobs_restarts.py:248,262,264,271` | HOLDS (none is a second traversal) | quoted in full: `:248` `while dotted not in funcs and dotted not in modules and dotted not in seen:` (one re-export chain to a fixed point, no branching); `:262` `def _resolve(m: str, scope: str, node: ast.AST) -> str \| None:` (the new call/reference-edge extractor the order explicitly permits — resolves a call target to a qualname using bindings/toplevel/enclosing-scope, NOT a relative-import resolver); `:264` `while isinstance(node, ast.Attribute):` (unwinds one dotted attribute chain); `:271` `while enclosing.startswith(m + "."):` (climbs one name's enclosing scopes). No `.pop()` in the test file (the classifier's own `reachable` has the only stack `.pop()`, at `restarts.py:163`, called not reimplemented); no `def _module_for` in the test |
| (vii) the builder's RED runs A/B/C and CLOSE tables | builder (fix-report.md) | `fix-report.md` `## Z3`, `## CLOSE` | **NOT CHECKABLE FROM READS** (the hub runs no `uv`) | quoted as the builder's claim: Run A all 3 planted cases FAIL on a stub helper; Run B (reduced resolver) 1 fail/2 pass, then case widened, 2 fail/1 pass; Run C (full resolver) 3 pass; CLOSE suite `2213 passed, 351 skipped, 1 xfailed` (baseline `2210/0`); `jobs restarts main..HEAD` exit 0 `RESTARTS: com.cobalt.aset` (explained as main's own commits, not this branch's — see PREFLIGHT); `jobs restarts 3ad064f..HEAD` exit 0 `RESTARTS: none`. None of this is independently re-run this round; it is the same claim status as round 2's builder tables |
| (viii) the pinned reader set against `CALLERS-GREP.md` line by line | hub | `CALLERS-GREP.md:1-73` vs `test:164-177` | HOLDS | hub's own spot-check (independent of all four checkers): every `load_backup_config` call site (`CALLERS-GREP.md` block 1: `probes.py:431`, `restic.py:164,248,269`, `backup/cli.py:59`) and every wrapper call site (block 2: `probes.py:445`, `runner.py:144,493`, `heartbeat/cli.py:27,34`, `backup/cli.py:49,70,76`) is a member of, or a caller into, the pinned 12; the two `def snapshot`/`def restore` name-collision blocks (block 4) are excluded correctly; `jobs/store.py:197` is a string literal in an error message, not a call — correctly excluded |

Where checkers contradicted each other (Sol vs. Grok/Gemini/Opus on `_package_name`), both are quoted in full above and under `## Sol's Q2`; neither side is smoothed.

## Ready for the stacked deploy

| checker | CHECK line | ready | reason verbatim |
|---|---|---|---|
| grok | `CHECK: FIX STANDS · Sol Q2: closed · ready for the stacked deploy: YES` | YES | (none given) |
| gemini | `CHECK: FIX STANDS · Sol Q2: closed · ready for the stacked deploy: YES` | YES | (none given) |
| sol | `CHECK: FIX STANDS EXCEPT L3 second module-name adapter · Sol Q2: closed · ready for the stacked deploy: NO · test adds synthetic module naming, violating the ordered one-walker constraint` | NO | test adds synthetic module naming, violating the ordered one-walker constraint |
| opus | `CHECK: FIX STANDS · Sol Q2: closed · ready for the stacked deploy: YES` | YES | (none given) |

COUNT RULE (not a verdict): houses that checked 4 ≥ 3 ✓ · defects that HOLD 0 (Sol's `_package_name` claim, the only defect claim raised this round, DOES NOT HOLD per `## Sol's Q2`/`## Checked against the branch`) ✓ · no checker's line reads `CHECK: STILL OPEN` ✓ · Sol Q2 closed = yes ✓ → **`ready for the stacked deploy: yes`**.

## FOR DEJAN

none — Sol's Q2 is CLOSED (mechanically, per the rule: Sol answered `closed` in its own CHECK line, and the one question-2 defect claim raised this round, `_package_name`, does not hold in the hub's file-check); no other defect claim HOLDS; no checker's line reads `STILL OPEN`; Sol's items the order left out (the resident-plist route, an intra-function dropped call) are both explicitly named as NOT BUILT by the order itself, and every checker who addressed them (grok, opus) says neither BLOCKS. Round-1 ESCALATE 2/3/4 and round-2 builder ESCALATE 5 are carried by id under `## ESCALATE`, not restated here (L67, L39: this is round 3, the last — nothing is left open to him from this round's own work).

## ESCALATE

1. **Sol's `_package_name` claim (`CHECK: FIX STANDS EXCEPT L3 second module-name adapter`) — checked, DOES NOT HOLD.** Full reasoning under `## Sol's Q2`: `_package_name` (`test:208-211`) is an input-shape adapter for `restarts._resolve_from`, not a second module namer or resolver — it exists because `restarts._module_for`'s `__init__`-stripping convention and `restarts._resolve_from`'s climb-from-parent arithmetic do not compose correctly for package roots, a pre-existing bug in the classifier itself (confirmed independently by the hub, by hand, on `backup/__init__.py:3`). This is the one point where the four checkers genuinely disagreed on characterization rather than fact; three (grok, gemini, opus) read it as compliant, one (sol) as a violation. The hub's determination is recorded here in full so it can be independently checked or overturned.
2. **The pre-existing classifier bug itself (`import_graph`/`_resolve_from` interaction on package `__init__.py` files) is real and unfixed — not this round's work.** First reported by the builder (fix-report.md ESCALATE 2); independently reproduced by the hub (see `## Sol's Q2`) and separately traced by grok and opus. `src/cobalt/jobs/restarts.py` is untouched by this branch (`git diff 3ad064f -- src` empty, confirmed in PREFLIGHT), so this branch's own restart derivation does not depend on it. Fixing it is a `src/` change that would itself derive `com.cobalt.radar` and needs its own build and check — carried, not built here, per the order's own scope.
3. **Sol's items the order excluded, listed, not built, neither BLOCKS (per grok and opus, unrebutted):** a resident plist invoking an existing operator command (`cobalt backup status|restore`, `cobalt heartbeat show`) — not covered, would need a registry/plist test; a second/dropped call inside a function that is already a reader — by design not a red case (adds/removes no reader), and grok notes Sol's own restic example (dropping one of three single-call functions) *is* in fact covered, since each of those three functions has exactly one loader call.
4. **The dropped `"backup.yaml"` literal-path check (round-2's `config.py` path-constant assertion) has no replacement.** Raised by opus, confirmed by the hub (`## Checked against the branch`): `fix-diff.md:268-273` removes `assert literal == ["src/cobalt/backup/config.py"]` with nothing in its place. This was inside the order's own replacement range, so it is not an unauthorized weakening, but it is a real coverage gap the order itself chose not to keep: a module that opens `backup.yaml` by its own path string (not through the loader) would no longer be caught. Non-blocking per every checker who raised or read it.
5. **No secret value found anywhere (L4).** The four SECRETS paths of line 1 were never opened, listed, hashed, or staged; `.env` confirmed absent in `ops-0921` at both PREFLIGHT and post-return.
6. **No packet mismatch.** `fix-diff.md` diff/commit counts matched (2/2); `fix-report.md` byte-matched (26,612 B both sides); the two `ordered-prompt.md` parts summed to the original's 41,875 B exactly (after a one-byte trailing-newline fix caught before staging closed); all 13 `built/` files byte-matched their originals (delegated to a fork for the mechanical copy, independently re-verified by the hub after).
7. **Every checker checked; none marked `WROTE:`.** 4 of 4 returned inside the 45-minute window with a `CHECK:` line; post-return `ls -la` on both the scratch folder and `/Users/cobalt/cobalt-wt/ops-0921` showed only the hub's own writes and Grok's own `grok-check.md`; `grep -c -i "denied\|not allowed\|permission"` was 0 on all four check files.
8. **No `ASK DESK` this round** — no step required a desk answer to continue; four checkers were UP at PREFLIGHT and all four checked.
9. **ALWAYS: this was round 3 of 3 (L67) — what it leaves open is under `## FOR DEJAN` (which reads `none`), never a round 4.**
10. **Carried by id, not re-asked:** round-1 ESCALATE 2 (the `cli.md` seam with `bars/chunk-1a-0920`), 3 (the circular restore → 6b), 4 (restic `forget` grouping); round-2 builder ESCALATE 5 (`configs/cobalt/watchlists.yaml`'s hard-coded `if`, `restarts.py:243-244`, untouched this round).

No recommendation is added.

## 4. Close

OPS 6A CHECK R3 DONE · grok: CHECK: FIX STANDS · Sol Q2: closed · ready for the stacked deploy: YES · gemini: CHECK: FIX STANDS · Sol Q2: closed · ready for the stacked deploy: YES · sol: CHECK: FIX STANDS EXCEPT L3 second module-name adapter · Sol Q2: closed · ready for the stacked deploy: NO · test adds synthetic module naming, violating the ordered one-walker constraint · opus: CHECK: FIX STANDS · Sol Q2: closed · ready for the stacked deploy: YES · houses that checked: 4 of 4 · Sol Q2 closed: yes · defects that HOLD: 0 · ready for the stacked deploy: yes · open to Dejan: 0 · ESCALATE: 10
