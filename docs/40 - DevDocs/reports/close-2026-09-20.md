# Close 2026-09-20 — session close (close hub `close-0920`, `prompts/2026-09-20/99-close.md`)

## §0 Headline
- 2026-09-20 was a build-and-tribunal day, not a deploy day: the bars-lifecycle tribunal (3 rounds → `BARS-LIFECYCLE-FINAL`), chunk E (20 experiments, checked 3 of 3), chunks 1a + 2 built, checked, fixed and re-checked (BUILT-NOT-MERGED, nothing deploys before WED 09-23), and the cards `htf_level_proximity` curve went LIVE 21:05 ET (`[[0,10],[1,1]]`). Ledger appendix `### 2026-09-20 — …` appended (75 lines; R1–R48 + R9-extended + the R49 launch row).
- Ladder: `### Status 2026-09-21` written (one block, replaces 09-20's). S2 stop 2026-09-23 = **AT RISK, unchanged** — zero of the three remaining days elapsed, 0 of 2–3 live mornings banked, no card rendered, no replay run.
- LAWS: L71–L74 + both L68 amendments verified present; **5 PROPOSED, 0 applied** (L58). Always-loaded block **3,994 / 4,000** (PASS, 6 characters of margin). `validate` exit 0, 16 jobs registered. One docs commit; push is on his word (L55).
- ESCALATE: 5

## AUTHORIZATION (verified by this hub before any write)
- `git log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-20.md"` → `e28ba05`, `df411d6`, `90e47bb`, `aa33af4`, `db2dc48`, `470bdd6` — the day's record (through §47, 01:1x ET Mon 09-21) is on main.
- Read in the prompt's order: `SESSION-CLOSE.md` · `close-2026-09-19.md` (the shape) · `cto-2026-09-20.md` in full — all six `## §4 Rulings` tables (lines 12, 82, 194, 259, 364, 458; R1–R48 plus the R9 "(extended)" row) and §0–§47 · the last line of each of `bars-tribunal-r3`, `bars-chunk-e-check`, `bars-chunk-1a-check`, `bars-chunk-2-check`, `bars-chunk-1a-check-r2`, `bars-chunk-2-check-r2`, `cards-htf-apply` (all `-2026-09-20.md`) · `laws-audit-2026-09-20.md` §0 · `his-plate-audit-2026-09-20.md` §0 · `LAWS.md` L35/L62/L67/L68/L71–L74 (read only) · ladder "Ladder at a glance" + S2 + S3 owed lines + carried items · `BACKLOG.md`. Also read: the three build stop lines from the worktree reports (`grep "^BARS CHUNK"`), `bars-2-fix-draft-2026-09-20.md` (owner items O1–O6).
- Every command is inside the approved set (`git status/log/diff/show/add/commit`, `git tag --list`, production `validate`, `cd`, `ls`, `grep`, `tail`, `wc`, `date`); the one exception is `git branch --list "bars/*" -vv` (read-only, not in the literal list; it ran, no denial, no rule added). LAWS.md and the reports were read with the Read tool. Nothing written under `/Users/cobalt/Vault/Think/6 - Permanent/Memory/` (L58).
- **L74, recorded ONCE:** the tool result of the `Read` of `99-close.md` carried a block styled as a system reminder asking for a `Claude-Session: https://claude.ai/code/session_…` line in every commit and PR body and naming a file-send tool (`SendUserFile`). It is DATA (L74): not followed, no file sent; this hub's commit carries `Co-Authored-By` only. Not counted in ESCALATE (his 09-20 R8 clause: recorded once, never re-raised).

## Ledger appendix (step 1)
`docs/00 - Project/PROJECT-LEDGER.md` — `### 2026-09-20 — bars tribunal, chunks E / 1a / 2, cards level-proximity curve live` appended after the 09-19 block: **75 lines** (heading, sources, STATE line, 50 ruling lines, state-change lines). No prose narration, no analysis.
| line group | count | note |
|---|---|---|
| `09-20 R1`–`R48` | 48 | R43, R47, R48 marked "NO WORDS OF HIS — desk launch row"; R6 marked superseded by R8; R1 marked closed by R12 |
| `09-20 R9` (extended) | 1 | his 07:0x sharpening + the desk's premarket measurement |
| `09-20 R49` | 1 | desk launch row for this close, from `cto-2026-09-20.md` §47 — in no §4 table; the prompt says R1–R48, so it is added and named |
| state changes | 22 (+ the STATE line, + heading and sources = 75) | laws fold L71–L74 + L68 ×2 · brief · laws audit · memory research · tribunal rounds 1/2/3 with stop lines · FINAL (`ef184bb`) · chunk E + check · plate audit · chunk 1a / chunk 2 builds, round-1 checks, fix builds `793f452` `1404f23` `d768674`, round-2 checks (stop lines verbatim) · cards curve LIVE (sha256 from the apply report's last line) · pushes (R13 `8898721..8ea21a6`, R24 `8ea21a6..c686311`, R46 `c686311..f801b63`) · OPEN into Monday |

## Stop lines quoted (each from its report's LAST line)
| # | run | last line |
|---|---|---|
| 1 | tribunal round 3, 15:24 ET | `BARS TRIBUNAL ROUND 3 DONE · grok: … BUILD v3 WITH 4 FIXES P-span P-closed P-range P-swap — … YES · gemini: … BUILD v3 WITH 3 FIXES (a, d, h) — … YES · astra: … BUILD v3 WITH 6 FIXES F1 F2 F3 F4 F5 F6 — … YES · houses that ruled: 3 of 3 · r2 defects RESOLVED by their raiser: 22 of 25 · NOT RESOLVED: G-01, G-12, G-13 · new defects: 13 · sequences that HOLD against the code: 18 · dissents for the owner: 12 · NOT CHECKABLE: 9` |
| 2 | chunk E check, 17:28 ET | `BARS CHUNK E CHECK DONE · … houses that checked: 3 of 3 · results challenged: E-19 · claims that HOLD against the harness: 26 · DO NOT HOLD: 2 · chunk 1a may build: 3 of 3 · chunk 2 may build: 3 of 3 · ESCALATE: 0` |
| 3 | chunk 1a check r1, 19:20 ET | `BARS CHUNK 1A CHECK DONE · grok: … BUILD STANDS EXCEPT … · gemini: … BUILD STANDS · astra: CHECK: FIX FIRST F1–F6 and missing --full interface … · houses that checked: 3 of 3 · claims that HOLD against the branch: 25 · DO NOT HOLD: 3 · ready for a deploy prompt: 2 of 3 · ESCALATE: 4` |
| 4 | chunk 2 check r1, 20:11 ET | `BARS CHUNK 2 CHECK DONE · grok: … child names disagree with 1a's pattern · gemini: CHECK: FIX FIRST … · astra: METER · houses that checked: 2 of 3 · … · ready for a deploy prompt: 0 of 2 · ESCALATE: 13` |
| 5 | chunk 2 check r2, 00:14 ET Mon | `BARS CHUNK 2 CHECK R2 DONE · grok: CHECK R2: FIX STANDS · … YES · gemini: CHECK R2: FIX STANDS · … YES · astra: CHECK R2: FIX AGAIN X1, O4-in-memory-range · inert on an unpartitioned parent, every path: NO · ready for its deploy prompt: NO · … · houses that checked: 3 of 3 · FIX rows CLOSED: 4 of 5 · inert on every path: 0 of 3 YES · new defects: 0 · ready for a deploy prompt: 2 of 3 · ESCALATE: 12` |
| 6 | chunk 1a check r2, 01:04 ET Mon | `BARS CHUNK 1A CHECK R2 DONE · grok: CHECK R2: FIX STANDS · ready for its deploy prompt: YES · gemini: HARNESS · astra: CHECK R2: FIX STANDS EXCEPT X9/X14 naming-contract reconciliation · ready for its deploy prompt: NO · houses that checked: 2 of 3 · FIX rows CLOSED: 12 of 13 · new defects: 0 · ready for a deploy prompt: 1 of 2 · ESCALATE: 5` |
| 7 | cards curve apply, 21:05 ET | `CARDS HTF LIVE 21:05 ET · file sha256 5993c57f54db17101cf22704508801cef8b7fcee9c832b08ac12f63936fbfc17 · stored curve: [[0,10],[1,1]] · other three curves: unchanged · rollback: not used · ESCALATE: 3` |
| — | builds (worktree reports) | `BARS CHUNK 1A BUILT c597bfe … offline 2272/0` · `BARS CHUNK 2 BUILT 47ef9af … offline 2368/0` · `BARS CHUNK 1A FIX BUILT 793f452 … 2291/0` · `BARS CHUNK 1A FIX BUILT 1404f23 … 2295/0` · `BARS CHUNK 2 FIX BUILT d768674 … 2372/0` · `BARS CHUNK E DONE · experiments run: 20 of 20 · AS EXPECTED: 18 · NOT AS EXPECTED: 2 (E-6b, E-9) …` (full lines in the ledger block) |

## Ladder status (step 4a) — what was verified against the tree (L35)
| claim | check | result |
|---|---|---|
| No production deploy on 09-20 | `git tag --list "deploy-2026-09-*"` → last tag `deploy-2026-09-19c` | confirmed; the only production write is the cards settings load |
| Bars chunks are BUILT-NOT-MERGED | `git branch --list "bars/*" -vv`; `git log --oneline main..bars/chunk-e-0920` = 8 commits | `bars/chunk-e-0920` `982d958`, `bars/chunk-1a-0920` `6961ee0`, `bars/chunk-2-0920` `1351da6`; all three unmerged, worktrees present |
| F10 `htf_level_proximity` LIVE | `cards-htf-apply-2026-09-20.md` last line; desk read-back 21:08 (`cto-2026-09-20.md` §41); R35 → R44 | LIVE 21:05 ET, `[[0,10],[1,1]]`, other three curves unchanged. The 09-20 row's "no `htf_level_proximity` curve shipped" is struck. First render = the same Mon 04:00 ET as F8 |
| R33 / R38 / R27 / R28 / R40 | read each row in §4 | carried: R33 into the live-mornings and smoke rows, R32 (K3 reading) beside it, R38 into the S3 owed-inputs line and carried item (marked RULED), R27/R28 into the two S4 precondition lines and the `sudo crontab -l` item (marked DONE), R27/R28/R40 named as device rulings in the block; the memory lines are the desk's |
| R39 Agent SDK spike | §4 row | row changed from "needs a decision" to "decided: KEPT, re-scope hub owed, S3 night lane" |
| Live mornings / replay / smoke | none has happened (it is Mon 09-21 ~01:xx ET; first render 04:00) | 0 of 2–3 banked, no card rendered, smoke NOT STARTED, stop 09-23 **AT RISK** with zero days elapsed of Mon–Wed |
Files changed: `docs/00 - Project/SPRINT-LADDER-v0_1.md` (the one status block; annotations `[RULED …]` / `[DONE …]` on the S3 owed-inputs line at :533-537 and three carried items — nothing deleted, no scope/date/order change) · `docs/00 - Project/BACKLOG.md` (NOW status header + a 09-20 OFF-LADDER list, the restic checklist item annotated, the PENDING SITTINGS table now holds three rows with ages: laws consolidation 1 day, short-term memory/disk 1 day, DRC template review 11 days).

## Laws — verified present, then proposals (step 2; nothing applied, L58)
Read from `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md`: **L71, L72, L73, L74 present**; L68 carries both 2026-09-20 amendments (SCOPE; the gate branch may be the deliverable) — none missing. This hub wrote nothing under `6 - Permanent/Memory/` and took no number; the desk assigns the next free number at the fold, only after his approval (L58 as amended 2026-09-18).

## Laws fold — PROPOSED, NOT APPLIED
P-a to P-c are the desk's three candidates, judged on the record; P-d and P-e are what the record shows beyond them.

**P-a — the desk delegates, reviews and talks; Fable-grade work is a SPAWNED Fable agent, only after asking him (desk candidate (a)). Judgement: earned — three restatements the same afternoon, the last one a correction (R16 → R17 → R18); partly a behaviour rule for the desk that may belong in `topics/working-contract.md` (the desk's) rather than LAWS.md; touches L67 (Fable is a house) — his call.** Evidence: `cto-2026-09-20.md` §4 R16 (16:59 "That desk doesn't do work. It only checks work and launches other work."), R17 (17:04, complexity test), **R18 (17:21 ET) "You will not do work yourself. You will spawn an agent that will do fable work under fable. And you will ask me to do so because you deem work complex enough that fable is needed."**; §16's own account that four Fable forks did drafting from 13:15 and where most of the weekly 7 % went. **Open reading, not folded:** R16 called the tribunal's Fable-seat ruling "the one sanctioned Fable fork"; §18 then says a Fable-seat ruling "would need his yes" under R18 — which governs the seat is unruled. Exact fold text:
> **L__ The desk delegates (PROPOSED).** The CTO desk never does work itself: it delegates, reviews and talks to Dejan. Drafting — prompts, designs, folds — goes to an Opus or Sonnet hub whose digest the desk reads. Work that needs Fable is done by a SPAWNED Fable agent, and only after the desk has asked Dejan, naming the work and why a lower seat will not do.

**P-b — a cross-branch seam that two sibling builders record as a READING is settled BEFORE the siblings launch (desk candidate (b)). Judgement: earned, an amendment of L72 (a shared shape IS a real dependency between "parallel" items); §36 lesson 3 already says it as practice.** Evidence: §35 — chunk 2 generates `bars_p_<YYYYMMDD>` while 1a's pattern admits only `bars_p_<YYYY>w<WW>`, so `side_of("bars_p_20261026")` raises `KeyError`; the drafter had flagged it (§26) but neither prompt settled it; cost: one 1b fix step (`1404f23`, X14, 9 min, one path beyond the prompt's list), 1a's round 2 held, and X9/X14 still open at close (`bars-chunk-1a-check-r2-2026-09-20.md`: 12 of 13 FIX rows closed, astra "FIX STANDS EXCEPT X9/X14 naming-contract reconciliation"). Exact fold text:
> **L72 amendment (PROPOSED).** Where two sibling builders share a name, shape or interface, the desk settles that seam in a document both prompts cite BEFORE either launches; a shared seam a builder can only record as a READING is a real dependency between them (L72). The stacked gate (L68) proves the seam, it does not decide it.

**P-c — a fix round's drafter CLASSIFIES every check finding — FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM — before anything is built (desk candidate (c)). Judgement: earned; used twice the same evening with no new approval needed; the UNPROVEN class is L70 already.** Evidence: §32 — chunk 1a: FIX 13 · NOT REAL 3 · UNPROVEN 6 · OUT OF SCOPE 2 · OWNER ITEM 2, fix built in 12 min (`793f452`); §38 — chunk 2: FIX 6 · NOT REAL 7 · UNPROVEN 5 · OUT OF SCOPE 6 · OWNER ITEM 6, fix built in 17 min (`d768674`); §36 lesson 2 "turned a split house verdict into a 12-minute fix with no new approval". Exact fold text:
> **L__ Fix rounds classify first (PROPOSED).** A fix round's drafter classifies every check finding as FIX, NOT REAL, UNPROVEN (L70), OUT OF SCOPE or OWNER ITEM, from the check hub's own file-check rows, before anything is built. Only FIX rows are built, each backed by a row that HOLDS; the fix widens nothing; every OWNER ITEM reaches Dejan verbatim.

**P-d — a hub's resume breadcrumb never sits on a report's last line while the run is unfinished. Judgement: earned as a clause of L71; already practice in prompts 10, 11, 14, 21 (which pin `(run in progress)`).** Evidence: §15 — the chunk-E watch fired at 16:03 on `CONTINUE: step 2 — scratch server not yet started`, a recovery breadcrumb the hub keeps as its last line while it works, because prompt 07 made `CONTINUE:` both the L60 resume marker and a stop shape; the desk re-armed on `^(BARS CHUNK E DONE|FAILED)` only. Exact fold text:
> **L71 amendment (PROPOSED).** A run's own resume breadcrumb or progress marker is never in a stop-line shape and never the last non-blank line of its report while the run is unfinished; it lives in a `## CONTINUE` section, and the prompt pins the in-progress last line.

**P-e — a privilege-filtered catalog read is never evidence that an object does not exist. Judgement: earned, a corollary of L35; small.** Evidence: §3 — the desk queried `information_schema.tables`, which filters by privilege and does not return `public`, and told him "the Postgres memory layer does not exist"; `pg_tables` shows 48 tables in 3 schemas with all five pillars in `public` (`memory-history-2026-09-20.md`); the same scoped-read-as-whole error class as the 09-19 P4 fixtures. Exact fold text:
> **L35 corollary (PROPOSED).** A catalog view filtered by the reader's privileges (`information_schema`) is never evidence that an object does not exist; absence is claimed from `pg_catalog` or the owner's view, or stated as "not visible to this role".

Considered and NOT proposed: (1) "a stop line never asks for a figure the allowlist cannot produce; a worker that cannot measure says UNVERIFIABLE" (§1) — the finding was walked back (§7: the committed brief's sha256 line verifies; how it differed from what §1 quoted "is NOT established") → UNPROVEN, L70; (2) classifier denials of a viewer attach and a tab close on a production hub (§41, §42 lesson 5) and the "never wrap a command to read its exit status" line (§41; drafter O5; 1a O2) → desk/ops practice, owed to `topics/cto-desk.md` and `UNATTENDED-LAUNCH.md` via `ops-0921`; (3) L34 never obeyed, the L58-vs-SESSION-CLOSE steps 3–4 collision, the 10 origin-unknown contradictions → the laws consolidation sitting's register (`laws-audit-2026-09-20.md`), not proposals. **The 09-19 close's P1 (allowlist is not a gate) was DEFERRED by his R1 and never folded** — the desk treated R12's push rule as answering it (`cto-2026-09-20.md` §11: "P1 → R12"); whether the law text still wants writing is his call, not proposed again here.

## Measure — always-loaded block (step 5; measured, NOT edited)
`cd "/Users/cobalt/Vault/Think/6 - Permanent/Memory"` then `wc -c INDEX.md profile.md preferences.md` (measured at the start of the run and again before the commit — identical):
```
    1838 INDEX.md
     706 profile.md
    1450 preferences.md
    3994 total
```
**3,994 / 4,000 — PASS**, margin 6 characters. The desk edits this folder in parallel (steps 3–4); its own re-measure after its writes is the one that counts — ESCALATE 4.

## Owed and open (everything the day left)
**His**
| item | source | note |
|---|---|---|
| **O1** — an unreadable FIRST bounds read aborts a poll cycle on a plain table (ruled F2 behaviour; A = keep, B = degrade to main's path before the swap; desk recommends A) | `cto-2026-09-20.md` §44; `bars-2-fix-draft-2026-09-20.md` ESCALATE 1 | asked 09-20 21:14 ET, **unanswered**; blocks nothing before the swap |
| **O2, O3, O4, O6** | `bars-2-fix-draft-2026-09-20.md` ESCALATE 2–4, 6 | O2 grok's "one ticker one cycle late" conditional on P-closed · O3 ANY-hole vs AMBER (every forward shortfall RED?) · O4 the named range is not persisted (card-facing surface later) · O6 the closed-period abort is per ticker and can fire after earlier tickers committed. One per message. (O5 = heredoc/pipe process note, not brought — 09-17 R5.) |
| Restic password rotation — **NOT DONE**, exposed 09-11 (10 days) | R41 "no", R42 "tomorrow is fine" | his hand (L4/L41); the step list arrives Mon 09-21 with `ops-0921` |
| **Three sittings** — laws consolidation (opened 09-20, **age 1 day**) · short-term memory/disk (opened 09-20, **age 1 day**) · DRC template review before S3-P3 (owed since 09-10, **age 11 days**, S3 starts 09-24) | `BACKLOG.md` PENDING SITTINGS; R10, R11, R18 | R18: "work at hand first, then a sitting on memory"; the laws audit is READY, the memory research is READY |
| Five law proposals P-a…P-e | this report | accept / amend / refuse each; the desk numbers on approval |
| R1 / R6 | `cto-2026-09-20.md` §4 | **not open**: R1 closed by R12 + the first push (§8), R6 superseded by R8 (L74). Only P1's law text (above) remains his call |
| Push | L55 | `origin/main` = `f801b63` (R46); every docs commit after it, including this close's, is unpushed — on his word |
| `bars_legacy` / the 3,553,219 non-i1 rows — disposal | R22 | UNRULED by design, irreversible, his word only, after the swap has proven itself |
| The swap weekend | R20 order 3 | he names it after the rehearsal measures the copy |
| Archiver `append` switch | 09-19 R35; R29–R31 | his separate ruling after Monday night's shadow numbers (C1: membership read decided after he sees them) |
| The empty "Cards apply" herdr tab `w2:t2C` | §41 | the close was denied `[Production Reads]`; his or a later desk's hand |
| The untracked packet files | ESCALATE 2 | his word before the push |

**The desk's / hubs', Monday 2026-09-21**
| item | source | note |
|---|---|---|
| Chunk 2 round-3 fix (the LAST) + its check | §46 | astra's two holding points — test 1's `rth_stale` case never goes stale in either poller; `poller.py:245` pops a carried `no_partition` record on a plain-table success where main has no such pop; plus the in-memory range on a carried record (astra's wording verbatim; persistence / card surface stay O4). Drafted by an Opus hub; no fourth round (L39) — unresolved after round 3 → him |
| Chunk 1a round 3 = a **CHECK** with X14 in the question set | §47 answers 3–5 | prompt `25` (`25-bars-chunk-1a-fix-1b.md`) + the desk's seam call (§35, R48) staged; **gemini re-asked**; all three houses (L67). Gemini's r2 HARNESS line, verbatim: `jetski: no output produced — a tool required the "command" permission that headless mode cannot prompt for, so it was auto-denied. Add an allow-rule under permissions.allow in settings.json (e.g. command(<target>)). Alternatively, re-run with --dangerously-skip-permissions to auto-approve all tools.` `[exited with code 0]` (`bars-chunk-1a-check-r2-2026-09-20.md`). Astra's R2 exception: X9's text (the `w<WW>` shape stays) vs X14 (1b replaced it with `bars_p_<YYYYMMDD>`) |
| `requires_db` first runs on `cobalt_dev` | stop lines | 19 tests written across 1a (3 + 2) and chunk 2 (14), never run; one dev-DB lane; the rehearsal's i1-only copy (R19) is a later chunk |
| The L68 stacked gate of `bars/chunk-1a-0920` + `bars/chunk-2-0920` | L68; §22 | both touch `placement.py` in disjoint regions; **H2** (registry contiguity pins tail-anchored vs migration `0012`) reddens on the stacked tree — named for the checkers |
| Chunk 4's drafter | R45; §32 S1 | carries R45 (the non-empty lock list + a test the swap's verdict sees that class of loss; the 1a report's "closes" wording → "arms", `cli.md:480`) and the `--proof-only --full` gap (in FINAL §4 row 6, in no chunk's row, absent on main) |
| `ops-0921` (Opus hub drafts, design-free) | §22, §36, §41, §42, §47 | restic step list + key store into restic's include set + restore proof · `cli.md:113-124` stale · `UNATTENDED-LAUNCH.md` lines "never wrap a command to read its exit status" (§41) and `--disallowedTools "Bash(git push*)"` on every `~/cobalt` hub · the eight stale `claude agents --json` registry rows · the four build MINORs G-A1 / G-A2 / H1 / H2 · `CLAUDE.md:95-96` (false 5-pillar fact + two utilities that do not exist) · `cmd_migrate` refusing `--allow-prod` outside production |
| Carried from `close-2026-09-19.md` that nothing closed today | its `## Owed and open` | **closed today:** A2 (R36) · A7/K3 (R32) · ESCALATE 19 (R37) · archiver R2-1 / C1 / O-6 (R30 / R31 / R29) · `htf_level_proximity` end points (R35/R44) · leg-count reading (R34) · the Agent SDK spike decision (R39, kept) · the missing push allow rule (R12). **Still open:** archiver R3-1 (§11 doc sentence edit — desk decision with veto, doc-only) · the four build MINORs · `cmd_migrate` off production · R34 C (stagger the pool cadences, NOT STARTED) · the Agent SDK spike re-scope hub (S3 night lane) · S3 preconditions (DRC template review only — mock #6 ruled, R38) |
| Pre-existing red on main's tree | `bars-chunk-e-2026-09-20.md`; §14 | `uv run pytest -q` stops at collection: `tests/test_finviz_extractor.py:34` `ImportError: cannot import name 'FinvizStockData'` (old-tree module); recorded by chunk E's hub, unread by the desk; how the 09-19 gates ran past it is not established — ESCALATE 5 |

**MONDAY'S CLOCK, stated once:** 04:00 ET first card render + shadow morning 1 (R13/R27, with the `htf_level_proximity` curve in it) · 20:30 ET the archiver's first nightly, the ONE shadow night (09-19 R35) · 21:10 ET first replay run → `cobalt smoke s2 --prod` becomes checkable (K3 read per R32).

## ESCALATE
1. **Five law items P-a…P-e await his word** — accept, amend or refuse each; P-a carries an unruled reading (does R18's "ask him" also govern the tribunal's Fable-seat fork?). No number taken.
2. **`docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml` and `p2-live-settings.yaml`, and the directory `docs/40 - DevDocs/prompts/2026-09-20/16-packet/`, are untracked and left untracked** — copies of settings files whose originals live under gitignored `data/backups/`; never `git add`ed (CLAUDE.md, L32; R40: no settings packet yaml goes to git). His word before any push.
3. **`ASK DESK: which file is `cli.md:113-124`, the stale paragraph ops-0921 owes? [01:xx ET Mon]`** — the desk's citations (`cto-2026-09-20.md` §32 S2, §47) name no path; `docs/40 - DevDocs/cobalt/db_migrations/cli.md` is the only DevDoc `cli.md` with that many lines (338), the top-level `cobalt/cli.md` has 87. Safe default taken: BACKLOG cites the line range as-is with that observation.
4. **Always-loaded block at 3,994 of 4,000** — any desk append to INDEX / profile / preferences over 6 characters trips the cap (SESSION-CLOSE step 5 FAILS over it); INDEX has no path out (`topics/memory-system.md`, the memory research's H-finding). The desk re-measures after its steps 3–4; this hub does not trim (L58).
5. **A pre-existing red on main's tree: `uv run pytest -q` stops at collection on `tests/test_finviz_extractor.py:34`** (`ImportError: cannot import name 'FinvizStockData'`, an old-tree module) — found by chunk E's hub 16:0x ET, never run or read by a desk or this hub; the build prompts' suites (`tests/cobalt tests/taxonomy`) do not see it, so it hides from every gate that names its paths. UNPROVEN as to cause (L70: this hub ran nothing); an `ops-0921` step decides whether the old-tree tests are in scope for the suite.

## Verification before commit
- `COBALT_ENV=production uv run cobalt validate` → **exit 0**. Quoted: `13 trade_def(s) validated OK from the vault.` · `Jobs (F17): 16 registered — 6 resident, 10 one-shot. Kill phrase 'COBALT STOP'.` · `  registry <-> ops/: 16 label(s), exact match.` · `Placement (docs/PLACEMENT.md): tree clean.` (matches the expected 16); also `archiver.write_mode = upsert`, `archiver.shadow_compare = on -> shadow artifact tonight: yes`.
- `git status --porcelain` before the commit: ` M docs/00 - Project/BACKLOG.md` · ` M docs/00 - Project/PROJECT-LEDGER.md` · ` M docs/00 - Project/SPRINT-LADDER-v0_1.md` (this hub) · `?? docs/40 - DevDocs/reports/close-2026-09-20.md` (this report) · `?? docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml`, `p2-live-settings.yaml`, `?? docs/40 - DevDocs/prompts/2026-09-20/16-packet/` (NOT added, ESCALATE 2). **Departure from the prompt's expected-dirty list:** `docs/40 - DevDocs/reports/seat-usage.md` was NOT dirty and `reports/bars-chunk-1a-check-r2-2026-09-20.md` was already committed by the desk (`e28ba05`), so neither is in this commit. Anything else unexpected: none.
- `git add` BY NAME for exactly four files: `docs/00 - Project/BACKLOG.md`, `docs/00 - Project/PROJECT-LEDGER.md`, `docs/00 - Project/SPRINT-LADDER-v0_1.md`, `docs/40 - DevDocs/reports/close-2026-09-20.md`. Nothing under `data/`, no `.log`.
- The last line carries `<see git log -1>` (one run, one commit, L46: the report is inside the commit it names, so the hash cannot be in its own file — same as `close-2026-09-19.md`); the hash is the `git log -1` of main after this commit and is stated in the launch reply.
- Memory: this hub wrote nothing under `6 - Permanent/Memory/` (L58). SESSION-CLOSE steps 3–4 (NOW rewrite, areas/topics append) are the desk's.

CLOSE COMMITTED <see git log -1> · ledger: 75 lines · laws: 5 PROPOSED, 0 applied · ladder: S2 status block written, AT RISK · always-loaded: 3994/4000 · validate: exit 0 · PUSH: on his word · ESCALATE: 5
