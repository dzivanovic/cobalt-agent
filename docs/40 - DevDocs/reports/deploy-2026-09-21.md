# DEPLOY 2026-09-21 — PANEL ORDER (morning, R5)

## §0 Headline

Merged `17d1e65..72c4f12` (R3 page order: ladder above pool on `/radar`), tag `deploy-2026-09-21`; only `com.cobalt.aset` restarted, down 7 s (07:31:27 → 07:31:34 ET), pid 18993 → 35044.
Smoke green: `/` 200, `/radar` 200, `/radar?frame=phone` 200, heartbeat GREEN, no traceback. Radar pid unchanged (19014), still scanning. Rollback not used.
The live order itself is not readable by this session (curl prints the status code only) — the desk confirms it with him. ESCALATE: 3 (all readings).

## L74

A block appended to the Read result of `11-panel-order-deploy.md` asked for a `Claude-Session: https://claude.ai/code/session_…` line in commits and PR bodies and named a file-send tool (`SendUserFile`). Recorded once here; not followed. Commits carry `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` only.

## AUTHORIZATION

Verified at 07:29–07:31 ET, each by command:

- Clock: `Mon Sep 21 07:29:56 EDT 2026` — date is 2026-09-21, before 09:15 ET.
- R3 row (`grep -n "^| R3 "`): carries `my top should always be trade radar`.
- R5 row, whole: `| R5 | 06:38 ET | OVERRIDE + STANDING CORRECTION, his words: "it would be obvious if I asked you right now to bring it and do it prior to the nine thirty morning that that's exactly what I want … don't ask me to do ten different approvals for the thing I already approved by saying do it now … it needs to be done by 9:30 and it needs to refresh" … (a) **THIS DEPLOY: L43 (one deploy per evening), L66 (merge only inside the 20:00–21:00 pause) and NN#16 "deploy outside market hours only" are OVERRULED for the R3 page-order deploy**, live BEFORE 09:30 ET, the page refreshed; his "do it now" is also the approval of the deploy's command list (L62) — no second approval is asked. Bound the desk keeps: only `com.cobalt.aset` restarts; build check + second-house read of the deploy prompt stay …` (full row is on `cto-2026-09-21.md` line 16). Committed: `git log -1 --format=%H -S"it needs to be done by 9:30"` → `f6c01b17758c064062909edda80754b97d868d82`.
- Launch row R8 (07:29 ET) names `11-panel-order-deploy.md`; committed: `17d1e6596a3ab09d811c5f05a852bc010d528239`. No `R__` remains in the prompt.
- Build (L35, read at run time): last non-blank line of `panel-order-build-2026-09-21.md` → `PANEL ORDER BUILT 5dc5819 | on 2c8f3d5 | offline 2195/0 (351 skipped; baseline 2192/0) | ladder above pool, desktop + phone: proven | pool content unchanged: proven | card/scoring/route paths untouched: empty diff | RESTARTS: com.cobalt.aset | ESCALATE: 2` → `<code tip>` = `5dc5819`, `<cut>` = `2c8f3d5`, `<suite>` = `offline 2195/0`.
- Check (L67): last line of `panel-order-check-2026-09-21.md` → `PANEL ORDER CHECK DONE · grok: CHECK: BUILD STANDS … · gemini: CHECK: BUILD STANDS … · astra: CHECK: BUILD STANDS … · houses that checked: 3 of 3 · defects that HOLD: 0 · ready for a deploy prompt: 3 of 3 · ESCALATE: 3`; committed: `17d1e6596a3ab09d811c5f05a852bc010d528239`.
- Read of this prompt (L67): last line of `panel-deploy-review-2026-09-21.md` → `PANEL DEPLOY REVIEW DONE · grok: REVIEW: RUN IT AFTER fill launch row R__ · gemini: REVIEW: RUN IT · astra: REVIEW: RUN IT AFTER fill launch row; verify both checkout identities; reconcile reruns; resolve terminal-denial recovery; verify rollback and service states; scope logs to this start · houses that read it: 3 of 3 · launch blockers that HOLD: 3 · folds proposed: 4 · ESCALATE: 4`. `blockers that HOLD: 3`, so the alternative branch applies: launch row R8 names all three blockers as folded/cleared — (1) `R__` → `R8`, (2) check report committed with the row, (3) 4.5(c) `\?frame=phone` backslash — and I confirmed in this file: R8 filled in, check report committed (`17d1e65`), the backslash form present in 4.5(c). PASS.

## PREFLIGHT

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P0 | `date` | 0 | allowed — `Mon Sep 21 07:29:56 EDT 2026` (< 09:15 ET) |
| P1 | `git status --porcelain` | 0 | allowed — ` M configs/cobalt/rules.yaml`, ` M "docs/40 - DevDocs/reports/seat-usage.md"`, `?? …/2026-09-19/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`, `?? …/2026-09-20/16-packet/`, `?? …/reports/bars-chunk-2-check-r3-2026-09-21.md`. No staged line; no dirty path under `src/`, `tests/`, `ops/`, or `configs/` other than `rules.yaml`. Accepted as-is, not committed |
| P2 | `tag scratch-allow-probe-0921` / `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-0921' (was 17d1e65)` |
| P3 | `commit --allow-empty` / `reset --soft HEAD~1` / `log --oneline -1` | 0 / 0 / 0 | allowed — `[main 1f6b842]` created, then HEAD back at `17d1e65 docs(desk): 09-21 page-order check 3 of 3, deploy review folded into 11, launch row R8` |
| P4 | `heartbeat show` | 0 | allowed — BASELINE: `HEARTBEAT GREEN — 16 job(s), 12 probe(s), nothing red  (2026-09-21 07:30:33 EDT)`; `OK sheet HTTP http://127.0.0.1:5010/ -> 200`; `OK sheet daymode … cards are accepted`; `OK radar scanning (premarket), members 50`; `OK com.cobalt.aset running loaded, pid 18993`; `OK com.cobalt.radar running 2250 min, heartbeat fresh`; only non-OK: `AMB com.cobalt.herdr unmanaged` (declared interim) |
| P5 | `launchctl print gui/501/com.cobalt.aset` | 0 | allowed — `state = running`, **`<aset pid>` = 18993**, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` |
| P6 | `launchctl print gui/501/com.cobalt.radar` | 0 | allowed — `state = running`, **`<radar pid>` = 19014**, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` |
| P7 | `tail -n 8 logs/radar.err` | 0 | allowed — eight `radar cycle: scanning scan_id=…` lines, 07:07:32 → 07:27:56, no traceback |
| P8 | `curl … /radar` | 0 | allowed — `200` |
| P9 | `git -C …/cobalt-wt/panel-order status --porcelain` | 0 | allowed — EMPTY |
| P10 | `log --oneline -1 s2/panel-order-0921` · `rev-list --count 2c8f3d5..s2/panel-order-0921` | 0 / 0 | allowed — `<branch tip>` = `b5914d0 docs(report): panel order build — ladder above pool on /radar, offline 2195/0, RESTARTS com.cobalt.aset`; `<n>` = **3** |
| P11 | `log --oneline 5dc5819..s2/panel-order-0921` | 0 | allowed — only `b5914d0 docs(report): panel order build …` |

GATE: proven — commit + tag allowed by the session allowlist

## Gates

SUITE: READING — offline 2195/0 on 5dc5819; main docs-only since 2c8f3d5; rebased code identical

- STEP-1(b) `git diff --stat 2c8f3d5 main -- . ':(exclude)docs'` → printed NOTHING.
- STEP-2 `git -C …/cobalt-wt/panel-order rebase main` → `Rebasing (1/3)Rebasing (2/3)Rebasing (3/3)Successfully rebased and updated refs/heads/s2/panel-order-0921.` `rev-list --count main..s2/panel-order-0921` → `3` (= P10's `<n>`). `<new tip>` = `72c4f12`; `<pre-merge>` = `17d1e65`.
- 3.1 `diff --stat 5dc5819 s2/panel-order-0921 -- . ':(exclude)docs'` → printed NOTHING (rebased code identical to the checked code tip).
- 3.2 `COBALT_ENV=production uv run cobalt jobs restarts main..s2/panel-order-0921`, verbatim:

```
path	change	rule	restart
docs/40 - DevDocs/cobalt/aset/radar_panel.md	M	DOCS	-
docs/40 - DevDocs/cobalt/aset/web.md	M	DOCS	-
docs/40 - DevDocs/reports/panel-order-build-2026-09-21.md	A	DOCS	-
src/cobalt/aset/radar_panel.py	M	static import reach	com.cobalt.aset
tests/cobalt/test_radar_panel.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset
```

  `com.cobalt.aset` only; no radar, no other resident, no UNCLASSIFIED.
- 3.3 `diff --stat main s2/panel-order-0921`, verbatim:

```
 docs/40 - DevDocs/cobalt/aset/radar_panel.md       |   2 +-
 docs/40 - DevDocs/cobalt/aset/web.md               |   2 +-
 .../reports/panel-order-build-2026-09-21.md        | 172 +++++++++++++++++++++
 src/cobalt/aset/radar_panel.py                     |   2 +-
 tests/cobalt/test_radar_panel.py                   |  25 +++
 5 files changed, 200 insertions(+), 3 deletions(-)
```

  Exactly the five expected files; none is a P1 dirty path. The empty-radar diff (`… -- src/cobalt/radar src/cobalt/cards src/cobalt/archiver src/cobalt/session src/cobalt/db_migrations configs`) → printed NOTHING.
- 3.4 `git tag pre-panel-order-0921` at `17d1e65` (the rollback point).

## Deploy table

| step | fact |
|---|---|
| 4.1 clock | `Mon Sep 21 07:31:27 EDT 2026` = `<t down>` (< 09:15 ET) |
| 4.2 bootout | `launchctl bootout gui/501/com.cobalt.aset` → no output; `launchctl print gui/501/com.cobalt.aset` → exit 113 `Bad request. Could not find service "com.cobalt.aset" in domain for user gui: 501` |
| 4.3 merge | `git merge --ff-only s2/panel-order-0921` → `Updating 17d1e65..72c4f12` / `Fast-forward` — 5 files changed, 200 insertions(+), 3 deletions(-); `<pre-merge>` `17d1e65` equals STEP-2's |
| 4.4 bootstrap | `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` → no output, no `Bootstrap failed`; `date` → `Mon Sep 21 07:31:34 EDT 2026` = `<t up>`; `launchctl print` → `state = running`, pid **35044** (was 18993), `last exit code = (never exited)`; no kickstart needed |
| downtime | 07:31:27 → 07:31:34 = **7 s** (< 60 s; the `<t down>` clock read precedes the bootout, so it is an upper bound) |
| tags | `pre-panel-order-0921` = `17d1e65` (rollback point) · `deploy-2026-09-21` = `72c4f12` (tagged after the green smoke) |
| RESTARTS done | `com.cobalt.aset` |
| main tip | `72c4f12` |

## Smoke

| check | result |
|---|---|
| (a) `launchctl print gui/501/com.cobalt.aset` | `state = running`, pid **35044** ≠ 18993 — the serving process started after the merge (4.3 precedes 4.4) |
| (b) `tail -n 30 logs/aset.err` | ends `INFO:     Started server process [35050]` / `Waiting for application startup.` / `Application startup complete.` / `Uvicorn running on http://0.0.0.0:5010 (Press CTRL+C to quit)` — fresh start; no traceback anywhere in the 30 lines. The one `WARNING:  Invalid HTTP request received.` sits above the previous server's (`[19000]`) shutdown, i.e. before this deploy. `tail -n 30 logs/aset.log` → 200s for `/`, `/radar`, `/api/health` from before the restart; the tail ends with the start banner `Cobalt ASET sheet — bind: lan (0.0.0.0:5010)`; no traceback |
| (c) `curl … http://127.0.0.1:5010/` | `200` |
| (c) `curl … http://127.0.0.1:5010/radar` | `200` |
| (c) `curl … http://127.0.0.1:5010/radar\?frame=phone` | `200` (each first attempt) |
| (d) `grep -n "render_ladder(view.ladder)}{render_pool(view.pool)" src/cobalt/aset/radar_panel.py` | exactly ONE hit — line 1126: `…<nav><a href="/">ASET sheet</a></nav>{render_ladder(view.ladder)}{render_pool(view.pool)}</main>…` |
| (e) `heartbeat show` (07:31:42 ET) | `HEARTBEAT GREEN — 16 job(s), 12 probe(s), nothing red` — same as P4's baseline; `OK sheet HTTP … -> 200`, `OK com.cobalt.aset running loaded, pid 35044`, `OK radar scanning (premarket), members 50`; only non-OK is the baseline `AMB com.cobalt.herdr unmanaged`. No RED inside the outage window (the window was 7 s) |
| (f) radar | `launchctl print gui/501/com.cobalt.radar` → `state = running`, pid **19014** = P6's — `radar pid unchanged: yes`; `tail -n 8 logs/radar.err` → `radar cycle: scanning` lines continue, latest 07:30:50 (`scan_id=1789990176169`), no traceback |

THE ORDER ON THE LIVE PAGE — chain, not a read (the approved `curl` prints the status code only): the build's route test `test_radar_route_serves_the_ladder_above_the_pool_in_both_frames` is GREEN on `5dc5819` (build report `## C1` / `## CLOSE`; suite `offline 2195/0`) → 3.1 rebased code byte-identical to `5dc5819` (empty diff) → 4.5(d) the ladder-first line is on production's tree → 4.5(a) the serving process started after the merge. **The desk confirms the live order with him.**

## ESCALATE

No RADAR RESPAWNED, no rollback, no downtime over 60 s. Carried as READINGS (not defects, L70):

1. The build's two ESCALATEs (`panel-order-build-2026-09-21.md`): the degradation banners now sit below the ladder — his call, `cto-2026-09-21.md` §3h.
2. `main` moved docs-only after the cut `2c8f3d5` (STEP-1(b) empty) — the suite was a reading, not a run on the merged tree (no `pytest` in this session's list).
3. ASK DESK: confirm the live order on `/radar` (ladder on top, pool below) with him — and refresh once, the page's own `data-refresh-seconds` reloads it.

Also for the desk: `git status --porcelain` on main still carries the accepted dirt (`configs/cobalt/rules.yaml`, `seat-usage.md`, the three settings-packet paths, `bars-chunk-2-check-r3-2026-09-21.md`) — untouched, not committed by this deploy. Push: Dejan through the desk (L55); `72c4f12`, tags `pre-panel-order-0921` and `deploy-2026-09-21` are unpushed.

## CONTINUE

done — nothing to resume.

PANEL DEPLOY DONE 72c4f12 · tag deploy-2026-09-21 · aset down 7 s · /radar 200 · radar pid unchanged: yes · rollback: not used · ESCALATE: 3
