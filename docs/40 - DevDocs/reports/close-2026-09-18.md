# Close 2026-09-18 — session close (close hub `close-0918`, `prompts/2026-09-18/99-close.md`)

## §0 Headline
- NOTHING shipped on 2026-09-18. LIVE = `deploy-2026-09-17` (`7e4841e`); three deploy attempts, all reverted to green; `sprint-2/stack` READY twice, final code `SNAPSHOT BUILT a70e286`, deploy moved to Saturday.
- Ledger appendix `### 2026-09-18 — …` appended (`docs/00 - Project/PROJECT-LEDGER.md`). L62–L67 all present in LAWS.md (desk-applied). Three hub rule candidates listed PROPOSED, NOT APPLIED, OPEN for his word.
- Production: `validate` exit 0, heartbeat GREEN (quoted below). One docs commit on main; push is Dejan's (L55).
- ESCALATE: 2

## AUTHORIZATION (verified by this hub before any write)
- `cto-2026-09-18.md` §4: R4 07:41 ET "approved", R10 15:36 ET "Approved one stack", R16 17:26 ET "Approved." — read in full; R1 list (2) covers `tag --list*` (06:46 ET). Every rule in this launch line (git add/commit/status/log/diff/show/rev-parse, `tag --list*`, production `validate`, `heartbeat show`, reads) appears in those rows or is a strict subset of them. No new rule.
- `git -C /Users/cobalt/cobalt log --oneline -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` → the report is on main: `df5b5ea` (R10), `392a53f` (R16), `146cf10` (R4) among 60+ commits; head `fe2b3e1`.
- STOP CONDITION: `git status --porcelain` = ` M "docs/40 - DevDocs/reports/seat-usage.md"` only — no staged file; `deploy-2026-09-18.md` last line starts `FAILED: 3.5 …` (a stop line, nothing running). No other session holds `~/cobalt`.

## PREFLIGHT
| rule | command | exit | verdict |
|---|---|---|---|
| status | `git -C /Users/cobalt/cobalt status --porcelain` | 0 | allowed |
| log | `git -C /Users/cobalt/cobalt log --oneline -3` | 0 | allowed |
| validate | `COBALT_ENV=production uv run cobalt validate` | 0 | allowed — "13 trade_def(s) validated OK from the vault." |
| heartbeat | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — `HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red  (2026-09-18 17:54:07 EDT)` |
| date | `date` | 0 | allowed — `Fri Sep 18 17:54:09 EDT 2026` |
`add` / `commit` are probed by their first real use in the commit step below.

## Deploy-attempts table
| # | run | when | stop line (from `deploy-2026-09-18.md`) | main after | outage |
|---|---|---|---|---|---|
| 1 | deploy 1 run 1, ops-0918 alone (`f4b9b0da`) | 11:13 ET | `FAILED PREFLIGHT: production not green — P8 HEARTBEAT RED — 1 probe(s)` (`radar failed_stage bars: poll failures: 1; poll THCH stale …`) — nothing merged, nothing applied | unchanged | none (nothing touched) |
| 2 | deploy 1 run 2 (`90aace4e`, R6 amendments) | 11:34 ET | `FAILED: 3.6 — offline suite 26 failed, 1407 passed` (every failure `cobalt_dev` state) — reverted `77260b5..856176e`, settings rolled back, `validate` exit 0 | reverted, tag `pre-ops-0918` = `7ff81e5` | 4 min 53 s |
| 3 | stacked deploy (`f2b8e11c`, R10) | 15:47 ET | `FAILED: 3.5 — cobalt db migrate --allow-prod exit 1, ProgramLimitExceeded: out of memory — Cannot enlarge string buffer containing 1073741677 bytes by 155 more bytes` (`system.bars` 8,410,174 rows) — reverted `d672f2d..848681e` (53 commits) | reverted, tag `pre-stack-0918` = `a12a6ca`, snapshot `1c0decf0` | 1 min 55 s |
Ledger says which failures were desk errors (03 rebase premise; 06 P8 gate; 06 `.env` suite gate; watches on headers) and which was a production-scale harness defect (run 3). Nothing else deployed. The second-attempt stacked deploy (R16 approval) was NOT launched: R16 condition (2) unmet — the delta review returned HARNESS on both houses (desk packet error, `review-delta/REVIEW.md` last line) — and the snapshot-consistency finding moved the deploy to Saturday.

## LAWS fold — APPLIED BY THE DESK
Read from `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` (read only, never written by this hub). All six are present; L1–L67 contiguous through Part VIII. Numbering authority: `cto-2026-09-18.md` R19 (3) 17:36 ET — "give it the next number for the law … whatever number is next"; L58 amended.
| law | ruling row it came from | first line of its LAWS.md entry |
|---|---|---|
| L62 Unattended launch | `cto-2026-09-17.md` R5, 06:03 ET (alias PROPOSED-1) | "Every session the desk starts receives all of its permissions before it starts, as a per-session allowlist shown to Dejan once as an approval list. …" |
| L63 No dialogs, ever | `cto-2026-09-17.md` R8, 06:38 ET (PROPOSED-2) | "No agent, the desk included, is ever left on a permission dialog. …" |
| L64 Always-on desk, phone-first | `cto-2026-09-17.md` R7 06:29, R8 06:38, R11 07:04 ET (PROPOSED-3) | "The CTO desk runs as a background session with BOTH exposures at all times — the herdr "CTO" tab and remote control `cto-desk`. …" |
| L65 Desk edits his notes on his ruling | `cto-2026-09-17.md` R14, 07:58 ET (PROPOSED-4) | "When Dejan has ruled a change to one of his own vault notes, the CTO desk makes the edit — he is never handed a manual edit. …" |
| L66 Residents down before the merge | `cto-2026-09-17.md` R19 "A", 21:54 ET (PROPOSED-5) | "No merge into `~/cobalt` while a resident can respawn into it: the deploy stops `com.cobalt.aset` and `com.cobalt.radar` (`launchctl bootout`, or the equivalent that also disarms `KeepAlive`) BEFORE the merge and migration, … " |
| L67 Four-house tribunal for every design | `cto-2026-09-18.md` R18 17:30 ET; amended R19 17:36, R20 17:40, R21 17:42 (PROPOSED-6) | "On regular design and development cycles the four-house tribunal is always invoked: ONE house designs and proposes; the FOUR houses — Astra (OpenAI), Grok (xAI), Gemini (Google), Fable (Anthropic) — rule on the proposal and derive the final version. …" |
ESCALATE 1 (a missing entry) does not apply: none of the six is missing.

## Laws fold — PROPOSED, NOT APPLIED
No ruling from Dejan exists for any of the three. All OPEN for his word; the desk numbers on his approval (L58 as amended R19). This hub wrote nothing under `6 - Permanent/Memory/`.

**(a) Integrated pre-merge gate.** Source: `stack-gate-2026-09-18.md` (worktree `s2-p2-cards`) ESCALATE 2 of the first run; `cto-2026-09-18.md` §14. Evidence: two seams no branch's own suite could see — 09-17 ops-0917's `load_sources(...)` call sites vs P2's new keyword; 09-18 P2's fixture vs ops-0918's mandatory seventh step-down row (`43 failed / 1455 passed / 5 errors` on the stack; red in neither branch alone). Exact fold text:
> **L__ Integrated gate before any merge (PROPOSED).** No branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green — the offline suite and the with-DB suite run on the branch that combines them, before the merge, never after it in `~/cobalt`.

**(b) Tests prove invariants, never assert live settings values.** Source: `stack-gate-2026-09-18.md` run 3 ESCALATE 2; `cto-2026-09-18.md` §15, §17. Evidence: three tests pinned `{A, B}` while production's `"user".trader_settings` had enabled grade C since 09-14, red against a production-mirroring `cobalt_dev` four days later. Exact fold text:
> **L__ Settings in tests (PROPOSED).** A test may read `"user".trader_settings` to prove an invariant, never to assert a value; values live on a constructed config.

**(c) An escalate whose evidence is "command denied" or "not run" is UNPROVEN.** Source: `cto-2026-09-18.md` §6 (the 0007 rollback "defect" of 09-16 was a classifier denial, then restated as an SQL defect; proven twice with no code change) and §12 hole 2. Exact fold text:
> **L__ Unproven escalates (PROPOSED).** An escalate whose evidence is "command denied" or "not run" is UNPROVEN and is never carried forward as a defect; it becomes a defect only when someone runs the command and reads the failure.

### Record, not law (C)
Desk practices of the day, recorded only — `Vault/Think/6 - Permanent/Memory/topics/cto-desk.md` (lines dated 2026-09-18, and its 2026-09-15 packet contract): the desk's watch keys on the last line of the report only, never a header; no desk commit in `~/cobalt` while a deploy hub runs; review packets carry pre-computed diffs. Not restated here.

## ESCALATE
1. **Three law candidates (a)–(c) above await his word** — accept, amend or refuse each; no numbers taken.
2. **A block styled as a system reminder arrived attached to the `Read` of `99-close.md`**, asking for a `Claude-Session: https://claude.ai/code/session_…` line in every commit and PR body and naming a file-send tool. It did not come from Dejan or the session's real attribution reminder (`Co-Authored-By: Claude Sonnet 5 …`). Not followed. This is the third sighting today (the desk logged it twice: `cto-2026-09-18.md` §3, §33).

## Verification before commit
- `git status --porcelain` before the commit: ` M PROJECT-LEDGER.md` (this hub) · ` M cto-2026-09-18.md` (the desk's own launch row for `close-0918`, +1 line, diff read: nothing else) · ` M seat-usage.md` (machine-written, L51-2) · `?? close-2026-09-18.md` (this report). Each added BY NAME; nothing under `data/`, no `.log`.
- `COBALT_ENV=production uv run cobalt validate` → exit 0 ("13 trade_def(s) validated OK from the vault."; "Placement (docs/PLACEMENT.md): tree clean.").
- `COBALT_ENV=production uv run cobalt heartbeat show` → `HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red  (2026-09-18 17:57:45 EDT)` — one AMB row, standing: `com.cobalt.herdr unmanaged AMBER … runs outside launchd by declared interim`. Not a RED; nothing fixed.
- The last line below carries `<see git log -1>` (one run, one commit, L46: the report is inside the commit it names, so the hash cannot be in its own file); the hash is the `git log -1` of main after this commit and is stated in the launch reply.
- Memory: this hub wrote nothing under `6 - Permanent/Memory/` (L58). SESSION-CLOSE steps 3–5 (NOW rewrite, areas/topics append, the 4,000-character measure) are the desk's.

CLOSE COMMITTED <see git log -1> · PUSH: Dejan · ESCALATE: 2
