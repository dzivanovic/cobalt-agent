# Ops 2026-09-16

## §0 Headline
- **Item 0 done:** `ops/2026-09-15` rebased onto main, `cli.py` conflict resolved (day-open mount kept, email removal kept), `rules.yaml` classifier row added with a test. 1172 passed / 239 skipped, validate 0. READY (RESTARTS aset radar).
- **Branches:** grok-deny: fix committed, probes NOT RUN (Grok busy by the CPU guard), `ops/grok-sandbox.toml` derives UNCLASSIFIED. agy-trial: rebase NOT clean → stopped, restored, NOT READY.
- **This branch:** README written (120 lines); `cobalt day-open` never overwrites a report and `--json` writes nothing (tests first); Qwen's 09-15 report recovered. Traces: radar cache = no loss; context-ETF i1 = by design (tier_b archives i5/i30 only). Pricing: no newer ccusage exists.
- Morning: 39/39 beats GREEN since 21:02, 0 radar RED. Neither fix is live yet (ops-0915 not merged), so this does not prove them.
- **ESCALATE: 5**

## 0. `ops/2026-09-15` — rebase + `cli.py` resolution

| check | result |
|---|---|
| `git rebase main` (757b2ac) | one conflicted file, `src/cobalt/cli.py`, two hunks |
| hunk 1, docstring (`cli.py:24`) | main's `cobalt day-open …` line kept; `cobalt notify email-*` line stays removed |
| hunk 2, `main()` mounts (`cli.py:471`) | `dayopen_cli.add_parser(sub)` kept; `notify_cli.add_parser(sub)` stays removed |
| `git diff main -- src/cobalt/cli.py` | exactly the branch's original hunks (notify import, email sweep lines, docstring, mount) |
| `rules.yaml` classifier | derived `UNCLASSIFIED CONFIG` → ESCALATE (proven on 4a6658d). Added a `jobs.yaml` `no_resident_reads` row: readers `com.cobalt.prefill-daily`, `com.cobalt.prefill-drc` (`prefill/daily.py:465`, `prefill/drc.py:381` → `regenerate_rules_config()`). Test `test_generated_rules_yaml_derives_no_restart` failed first, then passed. Now `RESTARTS: none` for that path |
| `QWEN.md` | `DOCS` (branch rule + existing test) |
| `uv run pytest -q tests/cobalt tests/taxonomy` | **1172 passed, 239 skipped** (09-15: 1059/238). The delta is main's day-open + radar-panel tests, plus the one new rules.yaml test |
| `COBALT_ENV=dev uv run cobalt validate` | exit 0. Needs `.env`: it was copied from `~/cobalt`, never printed, and removed after. Without it: `DbConfigError`, same on main |
| `uv run cobalt notify --help` | `invalid choice: 'notify'`: the group is gone, so no `email-*` command |
| `uv run cobalt jobs restarts main..HEAD` | no ESCALATE · **RESTARTS: com.cobalt.aset com.cobalt.radar** |
| history | `main..HEAD` = one commit, the ops commit rebased and amended (resolution + classifier row + `## Rebase 2026-09-16` in `ops-2026-09-15.md`) |

READY: `ops/2026-09-15` a502c23 · RESTARTS: com.cobalt.aset com.cobalt.radar

## 1. `ops/grok-deny-0915`

| check | result |
|---|---|
| `git rebase main` | clean |
| auth.json deny line | removed (exactly `"/Users/cobalt/.grok/auth.json",`) |
| `diff ~/.grok/sandbox.toml ops/grok-sandbox.toml` | **empty, exit 0** |
| CPU guard, pid 52628 (5 samples, 10 s each) | `1.3→1.4` · `1.2→3.2` · `1.2→1.0` · `1.4→1.1` · `1.1→1.0`. Never below 1.0 on both readings → busy |
| 3 probes (`.env` deny, README allow, `~/.claude/` dir deny) | **NOT RUN** (guard) |
| `cobalt jobs restarts main..HEAD` | `ops/grok-sandbox.toml A UNCLASSIFIED` → ESCALATE → all residents (same under ops-0915's classifier) |
| commit | 95cca23, `## 6. Re-proof 2026-09-16` appended |

READY: file only (branch = live). Blocked on the directory-deny probe and a classifier rule, see ESCALATE 2–3.

## 2. `ops/agy-trial-0915`

| check | result |
|---|---|
| `status --porcelain` | empty; `scratch/` ignored (`check-ignore` exit 0) |
| `git rebase main` | **NOT clean.** Stopped at pick 3/8: `untracked working tree files would be overwritten by merge`. a3aa29f adds `scratch/s2-p3-diff.txt` (19,851 lines) + `agy-err.log` + `agy-out.md`, which bd8a5d4 later untracks. Aborted; branch back at 6b7c5a4, tree clean |
| `git diff --stat main...HEAD` (net) | reports x2 **plus `.gitignore` +3** (`scratch/`, `agy-out.md`, `agy-err.log`, from bd8a5d4) → not reports-only |

NOT READY. See ESCALATE 1.

## 3. `README.md`

| fact | source (run read-only) |
|---|---|
| 120 lines; what/boundaries/layout/run/dev-prod/law/landing | `CLAUDE.md`, `docs/PLACEMENT.md` |
| top-level commands | `uv run cobalt --help` on main (notes that `notify` goes when ops-0915 merges) |
| `./cobalt.sh {start\|stop\|status\|restart}` | `./cobalt.sh` with no arg → usage line |
| `uv run prefill {daily,drc}`, `uv run archiver [--backfill TICKER]` | `--help` |
| 6 residents / 9 one-shots | `configs/cobalt/jobs.yaml` labels |
| `cobalt_brain` / `cobalt_dev`, no default | `src/cobalt/env.py:1-44` |

## 4. Radar-cache retention (P4 ESCALATE 3)

| fact | evidence |
|---|---|
| configured | `configs/cobalt/radar.yaml:25-27` `retention_days: 7` |
| enforced | `src/cobalt/radar/collector.py:133-150` `_cache()`: on each write, removes date dirs `< today − 7` (keeps today + 7 prior). No prune job in `jobs.yaml` |
| on disk | `2026-09-14` (mtime Sep 14 19:57), `2026-09-15` (Sep 15 19:59), `2026-09-16` (Sep 16 06:30). No other dirs |
| first production run | S2-P1 LIVE 09-13 (`areas/cobalt-sprints.md:28`). `logs/radar.err`: 09-13 11:47 onward is only `idle:overnight`; first `scanning` cycle **2026-09-14 04:02:28** |
| prune log lines | `grep -iE "prune\|retention\|removed" logs/radar.err`: none (the collector does not log prunes) |

**Verdict: cache younger than 7 days, no loss.** Close the ticket: oldest dir 09-14 = first scanning day; the first prune can fire on 09-22.

## 5. Seat-usage pricing pin (proposal, not applied)

| fact | evidence |
|---|---|
| pin | `configs/cobalt/seat_usage.yaml` `version: "20.0.20"`, `offline: true`. `ccusage --version` → `ccusage 20.0.20` |
| table location | the pricing table is compiled into the native binary `node_modules/@ccusage/ccusage-darwin-arm64/bin/ccusage`. `strings` finds `claude-opus-4-5…4-8`, `claude-sonnet-4-6`, `gpt-5`, `gpt-5.5`, and no `claude-fable-5-1` or `gpt-6-astra` |
| newer versions | `npm view ccusage versions` → last `20.0.20` (published 2026-08-15); dist-tags `latest: 20.0.20` only (the arm64 package too) |

**No pin bump exists.** 20.0.20 is the newest published version, so there is no yaml diff to propose and no rate rows. Options for Dejan: (A) wait for an upstream release and re-check with `npm view ccusage versions` (recommended, no change); (B) a local rate-override block in `seat_usage.yaml`. That is new code plus hand-entered prices (needs verbatim source quotes), so it is a build item, not a pin.

## 6. Carried, report-only

| item | state |
|---|---|
| ops-0915 item 7: phantom `*_scan_id` columns | ruling owed; proposal A (opaque ids) recommended |
| ops-0915 item 11: RESTIC_PASSWORD rotation | Dejan's procedure, unrun as far as ops can see |
| `GOOGLE_OAUTH_*` VaultManager removal | **not done.** The prescribed `cobalt vault list` does not exist (`cobalt vault` = restore/writes/overrides; exit 2, so `grep -c` = 0 is meaningless). Evidence instead: `data/.cobalt_vault` mtime **Sep 14 09:09**, and no `.cobalt_vault.pre-0915` backup (step 1 of the §3 procedure) |
| P4 ESCALATE 4: archiver vs `radar.finviz_max_rpm` 40 | last run `2026-09-16T00:30:07Z full cobalt_brain 210 tickers · 975 requests · 0 failures · 23m20s` → **41.8 rpm**. Ruling owed (L53 total demand) |
| P4 ESCALATE 2: `trade_count_band` | unset, Dejan's value |

## 6b. `cobalt day-open` overwrote the seat's report — fixed

| check | result |
|---|---|
| tests first | `test_a_second_run_never_replaces_the_days_report`, `test_a_colliding_timestamped_report_is_refused_not_replaced`, `test_json_alone_writes_nothing_to_disk`: 3 failed before the fix. `test_write_report_overwrites_its_own_days_file` (it encoded the bug) replaced |
| `report.py` `write_report` | the dated file is never replaced. A later run writes `day-open-<date>-<HHMMSS>.md` (ET, `generated_at`), exclusive create (`"x"`); a colliding stamp → `ReportPathError` |
| `cli.py` `cmd_run` | `--json` prints and returns, writes nothing; a refused write → `FAILED:` exit 1; a stamped write prints `NOTE: … NOT replaced` |
| recovery | `day-open-2026-09-15-qwen.md` ← `git show ebb1231:` (8,706 B, 112 lines; byte-identical to d77affd). `day-open-2026-09-15.md` → `git mv` → `day-open-2026-09-15-smoke.md` (7,228 B, 230 lines, `Generated: 2026-09-16 00:20:22 UTC`) |
| DevDocs | `cobalt/dayopen/report.md`, `cli.md` updated |
| `pytest -q tests/cobalt tests/taxonomy` (with `.env`, DB tests ran) | **1435 passed**, 0 skipped |
| `COBALT_ENV=dev cobalt validate` | exit 0, placement tree clean |
| RESTARTS | not `none`. `src/cobalt/dayopen/*` → **`com.cobalt.radar`** by static import reach (`jobs.yaml` radar `imports: [cobalt.cli]` → `cobalt.dayopen.cli`). On main's classifier `README.md` is UNCLASSIFIED (all residents); after ops-0915 merges it derives `DOCS`. Merge ops-0915 first. The radar restart is already in the ops-0915 set |

## 7. Morning proof (read-only, `~/cobalt`)

| check | result |
|---|---|
| `grep "2026-09-16" logs/heartbeat.log \| grep -c "ENTERED RED: radar"` | 0 |
| beats since 21:00 (`logs/heartbeat.err` `run_beat:603`) | **39 × `GREEN — 15 job(s), 13 probe(s), nothing red`**, 2026-09-15 21:02:08 → 2026-09-16 06:34:22 |
| last radar RED | around 2026-09-15 19:16 EDT (`HEARTBEAT RED — 1 probe(s)`), before the deploy |
| `Email sent` last | `2026-09-15 19:31:46` (before deploy 20:10). The email channel is still **live**: probe `email OK … last send OK 2026-09-15 23:31 UTC` |
| `cobalt heartbeat show` | `HEARTBEAT GREEN — 15 job(s), 13 probe(s), nothing red (2026-09-16 06:38:16 EDT)`; radar `scanning (premarket), members 50`; herdr AMBER (declared interim) |
| `launchctl print … com.cobalt.radar` | `state = running`, `pid = 8355` |
| `com.cobalt.prefill-drc` | pending its 15:40 run today, not judged |

Caveat: the flap fix and the email retirement are both in the unmerged ops-0915, so a quiet night is not proof. The proof moves to the morning after tonight's merge. Nothing RED beyond what the close carried.

## 7b. Context-ETF i1 bars stale since 09-03

| fact | evidence |
|---|---|
| tier | `Think/1 - Trading/Radar Lists.md` `list: tier_b` "Context ETFs / indexes" (25 tickers incl. all 12 + QQQ, XLE) · `archive: [i5, i30]`, `backfill_default: false` |
| since when | `tier_b intervals: [i5, i30]` since the archiver's first commit 63a71c6 (2026-08-27) in `watchlists.yaml`, carried into the Lists note by 001b712 (09-10). No change on or after 09-03 |
| where the i1 rows came from | `archiver-runs.md` 2026-09-03 11:47–12:10Z: `backfill:<T>` for every ticker, 5 requests each (tier_a's intervals, incl. i1): `backfill:SPY … 20462 rows`, `backfill:XLRE … 10941` |
| nightly runs | 09-04 → 09-16: `210` tickers, `975` requests, 0 failures, stable (185 × 5 + 25 × 2 = 975) |
| fetch health | `logs/archiver.err` e.g. `2026-09-14 20:48:54 [787/975] SPY/i5: 3072 rows`; no SPY i1 request exists |
| DB (`cobalt db query --prod`, read) | SPY i1 `2026-09-03` / i5 `2026-09-15`; IWM, XLK same; QQQ i1 `2026-09-16`; XLE i1 `2026-09-14` |
| QQQ/XLE current | written by the radar `BarPoller` (`radar/poller.py:71` fetches i1 for pool members), not the archiver |

**Verdict: neither dropped nor failing. By design, tier_b never archived i1.** P2's consumer expects i1 for context ETFs. The fix is a Lists-note (vault) edit, `tier_b archive: [i1, i5, i30]`: +25 requests/night, 975 → 1000, ≈42.9 rpm at today's pace, which is further over the 40 ceiling. That makes it a vault write and an L53 decision, not a config fix. RESTARTS if applied: none (the archiver is a one-shot and reads the note fresh; the radar resident reads the Lists note too, so re-check with `cobalt jobs restarts`). See ESCALATE 5.

## ESCALATE

1. **agy-trial not rebasable clean + `.gitignore` in its net diff.** Recommended: rebuild as one commit from main with only the two reports (`git checkout -B ops/agy-trial-0915 main && git checkout 6b7c5a4 -- "docs/40 - DevDocs/reports/agy-*.md"`, `scratch/` via `info/exclude`). This rewrites history, so it's the desk's call. Also check whether the P2 build is using `agy-trial/scratch/` before touching that worktree.
2. **grok-deny probes not run.** Grok pid 52628 sat at 1.0–3.2 % CPU in every sample (06:1x–06:40), so the guard's "<1.0" threshold never clears for this TUI. Rule A: waive the guard for these three read-only probes (recommended) / B: run after `/exit` of the Grok pane.
3. **`ops/grok-sandbox.toml` derives UNCLASSIFIED** (all residents). It has no Cobalt reader; recommend a `HARNESS` rule beside `.claude/` (one line in `restarts.py` + test) before merge.
4. **`GOOGLE_OAUTH_*` removal not done**, and the prompt's check command does not exist (`cobalt vault list`). The vault file has been unchanged since 09-14 09:09.
5. **Context-ETF i1 (7b):** adding i1 to tier_b = Lists-note write + archiver 975 → 1000 req over a 40 rpm ceiling it already exceeds (41.8). Ruling owed together with P4 ESC 4.

OPS 0916 (tip of ops/2026-09-16) · READY: ops-0915 a502c23 (RESTARTS aset radar) · grok-deny 95cca23 (file only; probes + classifier owed) · agy-trial 6b7c5a4 NOT READY · ops-0916 tip of ops/2026-09-16 (RESTARTS radar) · ESCALATE: 5
