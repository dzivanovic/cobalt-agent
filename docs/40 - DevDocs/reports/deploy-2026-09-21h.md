# DEPLOY 2026-09-21h — PAGE BARS HOTFIX (market hours, R21)

## §0 Headline

Merged `5cc189d..1ae9e94` (his R21: a per-ticker bars poll failure no longer fails `/radar`; `BARS POLL FAILED` banner), tag `deploy-2026-09-21h`; only `com.cobalt.aset` restarted, down 10 s (11:46:03 → 11:46:13 ET), pid 35044 → 93194.
Smoke green: `/` 200, `/radar` 200, `/radar?frame=phone` 200, pool fragment 200, no new FAILED line, no new traceback; radar pid unchanged (19014), still scanning. Rollback not used.
The banner on the live page is not readable by this session (curl prints the status code only) — the desk confirms it with him. ESCALATE: 3 (all readings).

## L74

A block appended to the Read result of `31-page-bars-hotfix-deploy.md` asked for a `Claude-Session: https://claude.ai/code/session_…` line in commits and PR bodies and named a file-send tool (`SendUserFile`). Recorded once here; not followed. Commits carry `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` only.

## AUTHORIZATION

Verified 11:44 ET, each by command:

- Clock: `Mon Sep 21 11:44:27 EDT 2026` — date is 2026-09-21, before 15:45 ET.
- R21 row (`grep -n "^| R21 "`, line 32), whole: `| R21 | 10:43 ET | "A" — to the desk's A/B after his 10:40 "radar is down now" (`/radar` = `FAILED: radar bars: poll failures: 3`; three thin tickers' stale bars fail the WHOLE page, `radar_panel.py:477`): **A — HOTFIX NOW, in market hours**: a per-ticker bars poll failure no longer fails the page — the page renders and shows the failure as a degraded state naming the tickers; every other failed stage still fails the page. **SET ASIDE by his ruling, this deploy only (L73 as amended): L43 (one deploy per evening), L66 (merge inside the 20:00–21:00 pause), NN#16 "deploy outside market hours only".** BOUND the desk keeps: ONLY `com.cobalt.aset` restarts (≈7 s, he was told), the radar resident is NOT touched (proven by `cobalt jobs restarts` or the deploy stops); build → house check → second-house read of the deploy prompt all KEPT; his "A" = the approval of the deploy's command list (same strings as this morning's `11`, plus the branch-named merge / rebase strings of the new branch). NOT widened: tonight's degraded-line deploy (`16`, R10 "this evening") stays tonight. | APPLIED: this row is the record; …` — carries `HOTFIX NOW` and `SET ASIDE by his ruling, this deploy only`. Committed: `git log -1 --format=%H -S"HOTFIX NOW"` → `017d7dfa2e1260cc59d47c6f1b377459c9ef07ea`.
- Launch row R25 (line 36) names `31-page-bars-hotfix-deploy.md`; no `R__` left in the prompt. Committed: `git log -1 --format=%H -S"31-page-bars-hotfix-deploy.md"` → `5cc189d72e567ef8e8686e7aa24111d355a3f40b`.
- Build (L35, read at run time): last non-blank line of `page-bars-hotfix-build-2026-09-21.md` → `PAGE BARS HOTFIX BUILT 64447b3 | on 912e382 | offline 2208/0 (baseline 2195/0) | bars failure renders degraded, tickers named: proven | other stages still FAIL: proven | healthy output unchanged: proven | RESTARTS: com.cobalt.aset | ESCALATE: 2` → `<code tip>` = `64447b3`, `<cut>` = `912e382`, `<suite>` = `offline 2208/0`.
- Check (L67): last non-blank line of `page-bars-hotfix-check-2026-09-21.md` → `PAGE BARS HOTFIX CHECK DONE · grok: CHECK: BUILD STANDS · ready for its deploy prompt: YES · gemini: CHECK: BUILD STANDS · ready for its deploy prompt: YES · all requirements met · houses that checked: 2 of 2 · defects that HOLD: 0 · ready for a deploy prompt: 2 of 2 · ESCALATE: 2`; committed: `241f620eba511052169daaecc91c2619ec53ef68`.
- Read of this prompt (L67): last non-blank line of `page-bars-hotfix-deploy-review-2026-09-21.md` → `PAGE BARS HOTFIX DEPLOY REVIEW DONE · grok: REVIEW: RUN IT AFTER (d) count new FAILED only after new process pid; no STEP-5 bootout at or after 15:45; relaunch neither-HEAD: finish or abort revert first · gemini: REVIEW: DO NOT RUN — Line 45 forces rollback on 503, contradicting 4.5(d). · houses that read it: 2 of 2 · launch blockers that HOLD: 1 · folds proposed: 3 · ESCALATE: 2`. `blockers that HOLD: 1`, so the alternative branch applies: launch row R25 names the blocker and all three folds as folded into this file (4.5(d) new-process-only count; STEP-5(1) no bootout at or after 15:45; relaunch with main at neither head → `revert --abort` first), and I confirmed each is present in this file. Gemini's line-45 concern: this file's line 45 RED list names `/`, `/radar` and `/radar\?frame=phone` only — the pool fragment `503` is not a RED there (4.5(c) routes it to (d)); R25 records the claim as not holding.

## PREFLIGHT

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P0 | `date` | 0 | allowed — `Mon Sep 21 11:44:27 EDT 2026` (2026-09-21, < 15:45 ET) |
| P0b | `git status --short --branch` (main) / (worktree) | 0 / 0 | allowed — main: first line `## main...origin/main [ahead 55]`; worktree: exactly one line `## s2/page-bars-hotfix-0921` |
| P1 | `git status --porcelain` | 0 | allowed — ` M configs/cobalt/rules.yaml`, ` M "docs/40 - DevDocs/reports/seat-usage.md"`, `?? …/2026-09-19/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`, `?? …/2026-09-20/16-packet/`. No staged line; no dirty path under `src/`, `tests/`, `ops/` or `configs/` other than `rules.yaml`. Accepted as-is, not committed |
| P2 | `tag scratch-allow-probe-0921h` / `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-0921h' (was 5cc189d)` |
| P3 | `commit --allow-empty` / `reset --soft HEAD~1` / `log --oneline -1` | 0 / 0 / 0 | allowed — `[main 3e9caff]` created, then HEAD back at `5cc189d docs(desk): 09-21 hotfix deploy review 2 of 2, three folds into 31, launch row R25` |
| P4 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — BASELINE `HEARTBEAT RED — 1 probe(s)  (2026-09-21 11:44:51 EDT)`: `OK sheet HTTP http://127.0.0.1:5010/ -> 200`; `OK sheet daymode … cards are accepted`; `OK com.cobalt.aset running loaded, pid 35044`; `OK com.cobalt.radar running 2504 min, heartbeat fresh`; **`RED radar failed_stage bars: poll failures: 2; poll GURE stale since 2026-09-21T15:29:45.168175+00:00; poll PFAI stale since 2026-09-21T15:38:34.487659+00:00`** — a radar probe, not the aset / sheet probe: the bug being fixed, baseline (named), not a stop; only other non-OK: `AMB com.cobalt.herdr unmanaged` (declared interim) |
| P5 | `launchctl print gui/501/com.cobalt.aset` | 0 | allowed — `state = running`, **`<aset pid>` = 35044**, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` |
| P6 | `launchctl print gui/501/com.cobalt.radar` | 0 | allowed — `state = running`, **`<radar pid>` = 19014**, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` |
| P7 | `tail -n 8 logs/radar.err` | 0 | allowed — eight `radar cycle: scanning scan_id=…` lines, 11:22:12 → 11:42:47, no traceback |
| P8 | five log-baseline greps (each its own call) | — | allowed — `Started server process` `<a0>` = **27** · aset `Traceback` `<ta0>` = **2** · radar `Traceback` `<tr0>` = **0** · `radar panel FAILED` `<f0>` = **9** · `radar pool refresh FAILED` `<g0>` = **10** |
| P9 | `curl … /radar` | 0 | allowed — `200` |
| P10 | `grep -c -F "BARS POLL FAILED" src/cobalt/aset/radar_panel.py` | — | allowed — `0` (marker absent before) |
| P11 | `log --oneline -1 s2/page-bars-hotfix-0921` · `rev-list --count 912e382..s2/page-bars-hotfix-0921` | 0 / 0 | allowed — `<branch tip>` = `0079740 docs(report): page bars hotfix build — …`; `<n>` = **3** |
| P12 | `log --oneline 64447b3..s2/page-bars-hotfix-0921` · `diff --stat 64447b3 s2/page-bars-hotfix-0921` | 0 / 0 | allowed — only `0079740 docs(report): page bars hotfix build …`; stat: one file, `docs/40 - DevDocs/reports/page-bars-hotfix-build-2026-09-21.md` (142 insertions) |

GATE: proven — commit + tag allowed by the session allowlist

## Gates

SUITE: READING — offline 2208/0 on 64447b3; main docs-only since 912e382; rebased code identical

- STEP-1(b) `git diff --stat 912e382 main -- . ':(exclude)docs'` → printed NOTHING.
- STEP-2 `git -C …/cobalt-wt/page-bars-hotfix rebase main` → `Rebasing (1/3)Rebasing (2/3)Rebasing (3/3)Successfully rebased and updated refs/heads/s2/page-bars-hotfix-0921.` `rev-list --count main..s2/page-bars-hotfix-0921` → `3` (= P11's `<n>`). `<new tip>` = `1ae9e94`; `<pre-merge>` = `5cc189d`.
- 3.1 `diff --stat 64447b3 s2/page-bars-hotfix-0921 -- . ':(exclude)docs'` → printed NOTHING (rebased code identical to the checked code tip).
- 3.2 `COBALT_ENV=production uv run cobalt jobs restarts main..s2/page-bars-hotfix-0921`, verbatim:

```
path	change	rule	restart
docs/40 - DevDocs/cobalt/aset/radar_panel.md	M	DOCS	-
docs/40 - DevDocs/reports/page-bars-hotfix-build-2026-09-21.md	A	DOCS	-
src/cobalt/aset/radar_panel.py	M	static import reach	com.cobalt.aset
tests/cobalt/test_radar_panel.py	M	test/documentation; no resident	-
RESTARTS: com.cobalt.aset
```

  `com.cobalt.aset` only; no radar, no other resident, no UNCLASSIFIED.
- 3.3 `diff --stat main s2/page-bars-hotfix-0921`, verbatim:

```
 docs/40 - DevDocs/cobalt/aset/radar_panel.md       |   2 +-
 .../reports/page-bars-hotfix-build-2026-09-21.md   | 142 +++++++++++++++++++++
 src/cobalt/aset/radar_panel.py                     |  13 +-
 tests/cobalt/test_radar_panel.py                   | 113 ++++++++++++++++
 4 files changed, 268 insertions(+), 2 deletions(-)
```

  Exactly the four expected files; none is a P1 dirty path. The empty-radar diff (`… -- src/cobalt/radar src/cobalt/cards src/cobalt/archiver src/cobalt/session src/cobalt/db_migrations configs ops`) → printed NOTHING.
- 3.4 `git tag pre-page-bars-hotfix-0921` at `5cc189d` (the rollback point).

## Deploy table

| step | fact |
|---|---|
| 4.1 clock | `Mon Sep 21 11:46:03 EDT 2026` = `<t down>` (< 15:45 ET); last readings of the old code: `radar panel FAILED` `<f1>` = 10, `radar pool refresh FAILED` `<g1>` = 10 |
| 4.2 bootout | `launchctl bootout gui/501/com.cobalt.aset` → no output; `launchctl print gui/501/com.cobalt.aset` → exit 113 `Bad request. Could not find service "com.cobalt.aset" in domain for user gui: 501` |
| 4.3 merge | `git merge --ff-only s2/page-bars-hotfix-0921` → `Updating 5cc189d..1ae9e94` / `Fast-forward` — 4 files changed, 268 insertions(+), 2 deletions(-); `<pre-merge>` `5cc189d` equals STEP-2's |
| 4.4 bootstrap | `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` → no output, no `Bootstrap failed`; `date` → `Mon Sep 21 11:46:13 EDT 2026` = `<t up>`; `launchctl print` → `state = running`, pid **93194** (was 35044), `last exit code = (never exited)`; no kickstart needed |
| downtime | 11:46:03 → 11:46:13 = **10 s** (< 60 s; `<t down>` was read before the bootout and `<t up>` after the bootstrap, so it is an upper bound) |
| tags | `pre-page-bars-hotfix-0921` = `5cc189d` (rollback point) · `deploy-2026-09-21h` = `1ae9e94` (tagged after the green smoke) |
| RESTARTS done | `com.cobalt.aset` |
| radar pid | P6 `19014` → 4.5(g) `19014` — unchanged |
| main tip | `1ae9e94` |

## Smoke

| check | result |
|---|---|
| (a) `launchctl print gui/501/com.cobalt.aset` | `state = running`, pid **93194** ≠ 35044 — the serving process started after the merge (4.3 precedes 4.4) |
| (b) start count | `grep -c "Started server process"` → **28** (`<a0>` 27 → +1, not a crash loop) |
| (b) `tail -n 30 logs/aset.err` | ends `INFO:     Finished server process [35050]` / `INFO:     Started server process [93200]` / `Waiting for application startup.` / `Application startup complete.` / `Uvicorn running on http://0.0.0.0:5010 (Press CTRL+C to quit)` — the last start is followed by uvicorn running; `[93200]` is uvicorn's worker (launchd pid 93194). The last `radar panel FAILED: … poll failures: 2` line in that tail is stamped `11:45:00` — OLD process, before the bootout, never RED |
| (b) `grep -c "Traceback"` aset.err | **2** = `<ta0>` — no new traceback |
| (c) `curl … http://127.0.0.1:5010/` | `200` |
| (c) `curl … http://127.0.0.1:5010/radar` | `200` |
| (c) `curl … http://127.0.0.1:5010/radar\?frame=phone` | `200` |
| (c) `curl … http://127.0.0.1:5010/api/radar/pool\?since=2026-09-21T13:30:00%2B00:00` | `200` (each first attempt) |
| (d) `grep -c "radar panel FAILED"` / `"radar pool refresh FAILED"` | `<f2>` = **10** = `<f1>`, `<g2>` = **10** = `<g1>` → GREEN: the new process served `/radar`, the phone frame and the pool fragment with no FAILED line and no `radar bars: poll failures:` line after the last `Started server process`. `LIVE: <f1> − <f0>` = **1** page FAILED line under the old code since P8 (the `11:45:00` line) — the pool carried a bars failure minutes before the bootout, so (d) GREEN with the pool still in that state is a live proof |
| (e) `grep -c -F "BARS POLL FAILED" src/cobalt/aset/radar_panel.py` | **1** (P10 read 0) — production's tree holds the banner |
| (f) `heartbeat show` (11:46:26 ET) | `HEARTBEAT RED — 1 probe(s)` — the ONE RED is `radar failed_stage bars: poll failures: 2; poll GURE stale since …15:29:45…; poll PFAI stale since …15:38:34…`, byte-identical to P4's baseline (the radar's own probe of the failing pool, the state the hotfix makes the page survive — not new, not the aset / sheet probe). `OK sheet HTTP … -> 200`, `OK com.cobalt.aset running loaded, pid 93194`, `OK com.cobalt.radar running 2505 min, heartbeat fresh`; only other non-OK: baseline `AMB com.cobalt.herdr unmanaged`. No RED inside the 10 s window |
| (g) radar | `launchctl print gui/501/com.cobalt.radar` → `state = running`, pid **19014** = P6's — `radar pid unchanged: yes`; `grep -c "Traceback" logs/radar.err` → **0** = `<tr0>`; `tail -n 8 logs/radar.err` → `radar cycle: scanning` lines continue, latest `11:45:43.554` (`scan_id=1790005467180`) |

THE BANNER ON THE LIVE PAGE — chain, not a read (the approved `curl` prints the status code only): the build's tests `test_bars_poll_failures_render_the_page_degraded_with_tickers_named` / `…_render_the_routes_and_the_refresh_fragment` / `test_every_other_failed_stage_still_fails_the_page` are GREEN on `64447b3` (build report `## C1` / `## CLOSE`; suite `offline 2208/0`) → 3.1 rebased code byte-identical to `64447b3` (empty diff) → 4.5(e) the banner is on production's tree → 4.5(a) the serving process started after the merge → 4.5(d) no bars FAILED line from it. **The desk confirms the live page with him.**

## ESCALATE

No RADAR RESPAWNED, no rollback, no downtime over 60 s. Carried as READINGS (not defects, L70):

1. The build's ESCALATE (i) — until tonight's `16` lands, the `BARS POLL FAILED` banner sits in the pool section below the ladder.
2. The build's ESCALATE (ii) — a stale member's pool row and card carry no bars-stale marker of their own (the desk's item). The radar probe stays RED (`failed_stage bars`) by design while GURE / PFAI are stale; only the page survives it now.
3. **TONIGHT'S `16`** — `main` moved OUTSIDE docs after `s2/degraded-line-0921`'s cut (`src/cobalt/aset/radar_panel.py` +13/−2, tests +113 here), so `16`'s STEP-1(b) and STEP-3.1 identity gates will refuse as written; the desk re-issues `16` before 20:00 (see `reports/page-bars-hotfix-draft-2026-09-21.md` `## ESCALATE`).

ASK DESK: confirm the live `/radar` with him — it should now render with a `BARS POLL FAILED` banner naming GURE and PFAI instead of `FAILED: radar bars: poll failures`. The page's own `data-refresh-seconds` reloads it.

Also for the desk: `git status --porcelain` on main still carries the accepted dirt (`configs/cobalt/rules.yaml`, `seat-usage.md`, the three settings-packet paths) — untouched, not committed by this deploy. Push: Dejan through the desk (L55); `1ae9e94`, tags `pre-page-bars-hotfix-0921` and `deploy-2026-09-21h` are unpushed.

## CONTINUE

done — nothing to resume.

PAGE BARS HOTFIX DEPLOY DONE 1ae9e94 · tag deploy-2026-09-21h · aset down 10 s · /radar 200 · radar pid unchanged: yes · rollback: not used · ESCALATE: 3
