# DRC D4 CHECK — ROUND 1 OF ≤3 — 2026-09-25

Prompt: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-25/06-drc-d4-check.md` · hub `drc-d4-check-0925` (Sonnet 5, auto, read-only) · range `00262e45..5d4f8201` on `drc/d1-trading-log`. No code, no git write, no DB, no vault write by this run.

## §0 Headline
Checked DRC D4 whole (`00262e45..5d4f8201`), E10 / X12 and the three suites' executed output; packet 227,481 B, no cut. OPUS 5.5 and GROK both answered `BUILD STANDS EXCEPT … · ready for D2: YES`; Astra METER (retry after Sep 26th, 2026 6:47 AM).
File-check: 12 claims HOLD (0 row failures: 7 rows × 2 checkers all MEETS; 3 weak-assertion items each from OPUS and GROK; 1 false one-apply claim at `cli.py:10`/`:79`–`:81`; 2 desk-ruled widened test files; 2 build-report staleness nits) → `ready for D2: NO` by the mechanical rule (§4), for the classifier.
ESCALATE: 12.

## L74
- None arrived. (A harness stderr notice on the OPUS runs — `Permission deny rule (../../cobalt/.claude/settings.local.json): Bash(git push*:*) mixes * with the trailing :* prefix syntax …` — and a `no stdin data received in 3s` warning are settings / stdin notices, not a `Claude-Session:` or file-send request; recorded as notices.)

## PREFLIGHT
`<D>` = 2026-09-25. Every call was allowed; none denied.

| rule | command | exit | result |
|---|---|---|---|
| date | `date` | 0 | `Fri Sep 25 06:04:52 EDT 2026` |
| placeholder `R_[_]` | `grep -n -E "R_[_]" …/06-drc-d4-check.md` | 1 | (nothing) |
| placeholder `FILL AT LAUNCH` | `grep -n -F "FILL AT LAUNCH" …/06-drc-d4-check.md` | 0 | lines `1` (SEAT prose) and `12` (the gate's own line) only |
| GROK GATE R17 | `grep -n "^| R17 " cto-2026-09-24.md` | 0 | `35:` `| R17 |` "Grok approved with no asking going forward" |
| R17 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Grok approved with no asking going forward" -- …/cto-2026-09-24.md` | 0 | `1758fd78a572f47b613b2ca831dcfa636ed8f65a` |
| R19 | `grep -n "^| R19 " cto-2026-09-24.md` | 0 | `37:` `| R19 |` "All 4 house models approved for use indefinlitly" |
| R19 committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"All 4 house models approved" -- …/cto-2026-09-24.md` | 0 | `5055151dbf68899b82de5b11f99733ed2d03048c` |
| R95 / R96 / R101 / R102 | `grep -n "^| R<n> " cto-2026-09-22.md` (one call each) | 0 | `70:` / `69:` / `64:` / `63:` — ONE row each |
| rulings committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"1.A 2. A 3.A 4. B" -- …/cto-2026-09-22.md` | 0 | `aa8ffbe94795f59e322a9e9edf17b508355502f4` |
| THIS launch | `grep -n "06-drc-d4-check.md" …/cto-2026-09-25.md` | 0 | `23:` (R15, `05`'s launch row), `28:` (`| R20 |`, this launch), `82:` (HANDOVER text) — R20 is neither R9 nor `05`'s row |
| launch row committed | `git -C /Users/cobalt/cobalt log -1 --format=%H -S"06-drc-d4-check.md" -- …/cto-2026-09-25.md` | 0 | `a994a5ddd213c83478a30e328e1fe828bee368cb` |
| grok | `grok --version` | 0 | `grok 1.0.25 (f7e67d6988e2) [stable]` |
| worktree | `ls /Users/cobalt/cobalt-wt/drc-d1` | 0 | listed |
| THE BUILT LINE | `tail -n 3 …/drc-d4-build-2026-09-25.md` | 0 | last non-blank: `DRC D4 BUILT 5d4f8201 \| on 00262e45 \| migration none \| E10: PASS \| X12: PASS \| offline 2516/0 \| with-DB 2973/0 \| live-note 142/0 \| .env: removed \| 0018: rolled back \| RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar \| tests added: 55 \| ESCALATE: 11` — every required token present; `0018: rolled back` (not UNPROVEN) |
| tip subject | `git -C /Users/cobalt/cobalt log --oneline -1 5d4f8201` | 0 | `5d4f8201 wip(drc-d4): E3 fixture writes a coherent constructed sheet + day-mode set (E7 red cause; with-DB unproven)` |
| range | `git -C /Users/cobalt/cobalt log --oneline 00262e45..5d4f8201` | 0 | EXACTLY the 7 lines the prompt names; non-docs commits = 6 (`db14325c` docs-only, 1 file) |
| nothing above tip | `git -C /Users/cobalt/cobalt log --oneline 5d4f8201..drc/d1-trading-log -- src tests configs` | 0 | (empty) |
| path union | `git -C /Users/cobalt/cobalt log --stat --format=%h 00262e45..5d4f8201` | 0 | 16 paths, EXACTLY the prompt's list (10 non-docs, 5 DevDocs, the build report). No `store.py`, no migration, no `drc/`, no `cli.py`, no `engine.py` |
| report headers | `grep -n "^## " …/drc-d4-build-2026-09-25.md` | 0 | the 17 headers in the prompt's order; no `(run 2)` section |
| `.env` | `ls /Users/cobalt/cobalt-wt/drc-d1/.env` | 1 | `No such file or directory` |
| recovery | `ls scratch/tribunal-bars-0920/drc-check/d4-0925` | 1 | `No such file or directory` — fresh run |
| stagger | `grep -n -F "no other house hub is running" cto-2026-09-25.md` | 0 | the R20 row (line `28`) carries the literal AND names `06-drc-d4-check.md` |
| probe OPUS | `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (background) | 0 | `OK` |
| Astra | keyed on `date` (2026-09-25 06:04 ET, before 2026-09-26 06:47) | — | `astra: METER — retry after Sep 26th, 2026 6:47 AM (v3 :10)` — never probed or launched |

## Packet
`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/d4-0925/` — 35 files staged by Write from Read of the real files (git outputs taken `run_in_background` and Read from the saved stdout); no `mkdir`.

| file set | parts | bytes (measured `wc -c`) | proof |
|---|---|---|---|
| `d4-diff.part1…8.md` | 8 | 74,892 (max part 11,271) | headers 197–330 B each; segment bytes sum 72,959 = the `git log -p` output exactly (the saved file's 72,981 = 72,959 + the harness's trailing newline and `[exited with code 0]`); `grep -c "^commit "` = 6 = non-docs commit count; `grep -c "^diff --git"` = 14 = the distinct non-docs path-touches `--stat` shows (1+1+9+1+1+1); trailing-blank lines 49 = 49; `grep -F -x -v -f` of every part against the original: only headers and empty lines |
| `devdocs-diff.md` | 1 | 11,147 | header 258 B + 10,889 = the git output exactly; 199 lines; blank/tab-trailing lines 11 = 11 |
| `code-at-tip.part1…4.md` | 4 | 31,438 | REAL-line slices from `/Users/cobalt/cobalt-wt/drc-d1/` (`drc.py` 1–286; `models.py` 58–222; `web.py` 1141–1300; `cli.py` 61–111, 224–239, 304–316; `daily.py` 166–179, 527–546; `daily.md.j2` 1–30); every line verified against the source file |
| `experiments.md` | 1 | 1,472 | `## E1` verbatim (REAL 67–75) |
| `suites.part1…3.md` | 3 | 15,965 | E0, E2, E3, E5 / E6, E7 / RESTARTS verbatim; the four deselect ids listed with the hub's own greps |
| `build-proof.part1…4.md` | 4 | 19,110 | PREFLIGHT, E4, SEAM FOR D2 + FOR D3, ESCALATE + stop line verbatim |
| `rules.part1…3.md` | 3 | 18,211 | R95 / R96 / R101 / R102 rows; v2 D4 row, order paragraph, E10, X12; §5 whole; §12 whole |
| `prompt.part1…8.md` | 8 | 47,636 (segments sum 45,827 = the original exactly) | `05-drc-d4-build.md` whole |
| `QUESTIONS-DRC-D4.md` | 1 | 7,610 | verbatim + the one "Files in this folder:" paragraph |

**HONEST SIZE:** measured sum of the 35 staged files = **227,481 B** → ÷ 4 ≈ **56,870 tokens** per checker; ceiling 300,000 B not reached; no cut (`rules.md` §12 and §5 kept whole). No part exceeds 15,000 B (largest 11,271). Same folder for both checkers (L44); the checkers' own `opus-check.md` (10,081 B) and `grok-check.md` (8,065 B) came after.
Packet facts for the checkers: (1) the prompt cites v2 rows at `:92` / `:95` / `:114` / `:118`; the file at that path reads them at REAL lines `:178` / `:181` / `:200` / `:204` (`rules.part1.md` headers say so). (2) `experiments.md` (E1) carries the gate results; the after-code results of E10 / X12 sit in `suites.part1.md` (E3) and `suites.part2.md` (E7); the QUESTIONS file says so.

## CONTINUE
DONE. Launched OPUS and GROK 06:23:45 ET; OPUS returned before 06:29; GROK wrote its file by 06:41 (both inside the 45-minute clock); collated; no relaunch.
Written-nothing proof: `ls -la` of the packet folder after each checker — the only new files are `opus-check.md` (written by this hub from OPUS's stdout) and `grok-check.md` (written by GROK, as its launch line told it to); `ls -la /Users/cobalt/cobalt-wt/drc-d1` before (05:58 dir mtime) and after — unchanged. No checker wrote any other file.
Harness lines recorded, not copied into `opus-check.md`: the deny-rule syntax notice and the `no stdin data received in 3s` warning on OPUS's stdout; `[exited with code 0]`. GROK's stdout carried only its progress sentences and the path.

## Per row
Both checkers wrote one verdict per row. `MEETS: n of checked`:

| row | opus | grok | MEETS |
|---|---|---|---|
| D4-1 keys + schema | MEETS THE ROW (`models.py:91`, `:159`, `:222`; `drc.py:82`) | MEETS THE ROW (`models.py:222`; `drc.py:82`) | 2 of 2 |
| D4-2 one reader | MEETS THE ROW (`drc.py:114`–`:120`) | MEETS THE ROW (`drc.py:114`, `:120`) | 2 of 2 |
| D4-3 daily note | MEETS THE ROW (`daily.md.j2:18`; `daily.py:166`, `:537`) | MEETS THE ROW (`daily.md.j2:18`; `daily.py:177`, `:537`) | 2 of 2 |
| D4-4 change line | MEETS THE ROW (`web.py:1184`–`:1295`; `cli.py:92`, `:97`) — caveats: EXCEPT-1, trace not versioned | MEETS THE ROW (`web.py:1243`, `:1258`, `:1275`, `:1280`; `cli.py:71`, `:92`, `:97`, `:99`) | 2 of 2 |
| D4-5 dry-run | MEETS THE ROW (`cli.py:351`–`:355`) | MEETS THE ROW (`cli.py:352`, `:354`) | 2 of 2 |
| D4-6 E10 + X12 | MEETS THE ROW (`test_drc_settings_db.py:54`, `:74`, `:131`) — see weak item (a)1 | MEETS THE ROW (same lines) | 2 of 2 |
| D4-7 DevDocs | MEETS THE ROW (`settings/drc.md` new + four dated sections) | MEETS THE ROW (same) | 2 of 2 |

No DOES NOT MEET, no NEW DEFECT from either checker.

## Experiments
| X | opus | grok |
|---|---|---|
| E10 | RAN — "gate PASS at E1 (`1 failed, 2 passed`; the file was not skipped) / pass: still green at E3 and in the E7 full run (`2973 passed`)"; design-changing: no | RAN — "gate PASSED … / after-code PASSED (still green at E3; inside the E7 `2973 passed`, 0 failed)"; design-changing: no |
| X12 | RAN — "gate: `/size` pin PASS on the base, reader half red (`ModuleNotFoundError`) as designed / pass: reader half PASSED in the E7 runs on `705fba6f` and `5d4f8201`"; design-changing: no | RAN — "gate `/size` PASSED (green pin on the base), reader FAILED `ModuleNotFoundError` / after-code both PASSED"; design-changing: no |

## Scope
| checker | THIRD answer |
|---|---|
| opus | "WIDENED, both by desk ruling: `tests/cobalt/test_radar_panel_cards.py:667` (POST allowlist, R17); `tests/cobalt/test_prefill_daily.py:191` (fixture stop literal → constructed value, R16). Nothing else widened." |
| grok | "WIDENED — `tests/cobalt/test_prefill_daily.py:191` … and `tests/cobalt/test_radar_panel_cards.py:667` (`POST_ALLOWLIST` names the two new routes). No migration. No `src/cobalt/drc/*`. No `src/cobalt/cli.py`. …" |

My PREFLIGHT path-union facts: the range touches EXACTLY 16 paths (10 non-docs: `configs/cobalt/templates/daily.md.j2`, `src/cobalt/aset/web.py`, `src/cobalt/prefill/daily.py`, `src/cobalt/settings/{cli,drc,models}.py`, `tests/cobalt/test_drc_settings.py`, `tests/cobalt/test_drc_settings_db.py`, `tests/cobalt/test_prefill_daily.py`, `tests/cobalt/test_radar_panel_cards.py`; 5 DevDocs; the build report); the last two tests are the prompt-listed rows' widening named by the checkers (R16 committed `b2ad3b7a`, R17 committed `c48ed315` per the build's ESCALATE 4 / 5; rows `| R16 |` and `| R17 |` each exist once in `cto-2026-09-25.md`). No `store.py`, no `db_migrations`, no `drc/`, no `cli.py`, no `engine.py`, `cards/`, `radar/`.

## Suites
| suite | opus | grok |
|---|---|---|
| offline | SHOWN — `2516 passed, 467 skipped, 1 xfailed, 15 warnings in 68.56s` on `5d4f8201`, 0 failed | SHOWN — `2516 passed, 467 skipped, 1 xfailed, 15 warnings in 68.56s` (0 failed) on tip `5d4f8201` |
| with-DB | SHOWN — `2973 passed, 6 skipped, 4 deselected, 1 xfailed, 15 warnings in 149.80s`, 0 failed; `.env` removed; absence probe `assert 28 == 32`, short by 4 | SHOWN — same summary; `.env` removed and proven gone; probe short by exactly 4; `0016` + `0018` rolled back |
| live-note | SHOWN — `142 passed, 1 skipped, 15 warnings in 9.39s` on `5d4f8201` (only skip `test_replay_line.py:256`) | SHOWN — same; only skip the known `COBALT_TEST_LIVE_DRC`, none naming `COBALT_LIVE_VAULT_ROOT` |
| deselects | DESELECTS AS STATED (`test_tenancy.py:697`, `:710`, `:263`; `test_migrate_proof.py:306`) | DESELECTS AS STATED (same four) |

Myself, from the build report (not the packet copy) `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-d4-build-2026-09-25.md`:
- offline (E6 re-run on `705fba6f`, report line 148): `2516 passed, 467 skipped, 1 xfailed, 15 warnings in 68.17s` — 0 failed, "0 errors" written; and again on `5d4f8201` (E7, line 152): `2516 passed, 467 skipped, 1 xfailed, 15 warnings in 68.56s` → 2516/0. The FIRST E6 (line 142, on `0075db31`) was `1 failed, 2515 passed …` — the red `test_post_routes_are_exactly_the_explicit_allowlist` (`test_radar_panel_cards.py:679`), the cause of the R17 line.
- with-DB (E7 FINAL, line 153): `2973 passed, 6 skipped, 4 deselected, 1 xfailed, 15 warnings in 149.80s` — 0 failed, "0 errors" written, deselected 4. An EARLIER E7 on `705fba6f` (line 159) was `3 failed, 2970 passed …` (the three E3 tests, a fixture defect, fixed by `5d4f8201`).
- live-note: E5 on `0075db31` (line 139): `142 passed, 1 skipped, 15 warnings in 10.33s`; re-run on `5d4f8201` (line 157): `142 passed, 1 skipped, 15 warnings in 9.39s`; the only SKIPPED line names `test_replay_line.py:256` (`COBALT_TEST_LIVE_DRC`); no SKIPPED line names `COBALT_LIVE_VAULT_ROOT` (expected none). 0 failed / 0 errors.
- deselected ids as the build names them (line 156) = the four ids at `:697`, `:710`, `:263`, `:306`; my own greps in `/Users/cobalt/cobalt-wt/drc-d1/tests/cobalt/` read the same four lines.
- absence probe (line 154): `1 failed in 5.69s`, `E       assert 28 == 32` — short by exactly 4, the KNOWN shape → `0016 + 0018: rolled back`.
- `.env: removed, proven gone` is written for E1 (line 68), E3 (line 92) and E7 (lines 155 and 159).

## Seam
| checker | FIFTH answer |
|---|---|
| opus | SEAM STATED — `/attest` `web.py:1141`–`:1181`; block `:1184`–`:1295`; next route `:1298`; `drc.py:82` / `:114`; `cli.py:71`; the D3 section names both readers. Two disclosures, neither blocking D2: `_daymode_banner` calls `_settings_daily_form()` from outside both blocks; two new imports at `web.py:70`–`:71`. |
| grok | SEAM STATED — block `web.py:1184`–`:1295` directly after `/attest` at `:1141`, next route `/card/{card_id}/move` at `:1298`; readers at `drc.py:82` / `:114`; `apply_settings` at `cli.py:71`; FOR D3 names both readers. |

## Checked against the branch
Files read under `/Users/cobalt/cobalt-wt/drc-d1/` (Read tool / greps) at tip `5d4f8201`; `git -C /Users/cobalt/cobalt show` for the base.

| # | claim · who · file:line | verdict | ≤30 words |
|---|---|---|---|
| 1 | "`cli.py:10` and `:79`–`:81` claim every apply goes through `apply_settings` … `cmd_load_card` still calls `store.put` directly … a second write path, without the read-back or the log line" · opus (EXCEPT-1) | HOLDS (second-path part); read-back part DOES NOT HOLD | `cli.py:10`–`:11` and `:79`–`:81` say so; `settings/card.py:323` is a direct `store.put`. But `card.py:322` has its own `assert_writable` and `:325`–`:331` a read-back; no log line there. |
| 2 | X12 `/size` test patches `load_sheet_modes_config` to an offline config, `/size` never reads `cobalt_dev` settings · opus (a)1 · `test_drc_settings_db.py:111` | HOLDS | `:111` `monkeypatch.setattr(web_module, "load_sheet_modes_config", _offline_sheet_modes_config)`; `:112`–`:123` also patches `_daymode_state`; the test then posts `/size` (`:124`). |
| 3 | "one transaction" test fails validation before any `put` · opus (a)2 · `test_drc_settings_db.py:251`/`:255` | HOLDS | `:254` form has `full.B = -1`, `:255` posts a zero hash; `drc.py:258` raises `DailyChangeInvalid` before `apply_settings` (`web.py:1280`); `store.put` never runs. |
| 4 | "the history row exists" checked only as `source` / `updated_at` on an overwritten row · opus (a)3 · `test_drc_settings_db.py:226` | HOLDS | `:226`–`:228` assert `proposal.sha256 in row["source"]` and `updated_at is not None`; `store.put` upserts (`store.py:94`–`:104`); no history table (build ESCALATE 8). |
| 5 | one-reader walk greps only the two key-name strings · opus (a)4 · `test_drc_settings.py:249` | HOLDS | `:249` loops only `"daily_stop_full"`, `"daily_stop_half"`. Today's readers of `DAILY_STOP_KEYS` / `load_drc_settings` / `daily_risk_values` are all in `settings/drc.py`, `prefill/daily.py:537`, `aset/web.py:1200`. |
| 6 | "E2 table counts D4-4 as 11 tests; it has 12, so the table sums to 48, not 49" · opus (report nit) · build report line 85 | HOLDS | `test_drc_settings.py` D4-4 tests: `:366`, `:375`, `:391`, `:398`, `:419` (5 param), `:428`, `:464`, `:472` = 12; table rows 26+3+3+4+11+1 = 48 vs 49 collected. |
| 7 | ESCALATE 7 says the X12 reader is "NOT yet run with-DB" and ESCALATE 9 says "RESTARTS not run" — out of date · opus (report nit) · build report lines 217 / 219 | HOLDS | Both lines say so; E7 (lines 150–170) and RESTARTS (lines 171–187) show both run. |
| 8 | `test_drc_settings.py:156` accepts a good value by not raising, never checks the typed result · grok (a) | HOLDS | `:156` is `OPTIONAL_SETTING_MODELS[key].from_rows({key: value}).row()` — no assert on the returned value. |
| 9 | `test_drc_settings.py:469` only asserts the success banner absent, not the read-back sentence · grok (a) | HOLDS | `:468` `"FAILED" in r.text`, `:469` `"Settings saved" not in r.text`; the read-back is `cli.py:98`–`:105`. |
| 10 | `test_drc_settings.py:491` asserts one constructed stop figure absent from the log, not the grade figure · grok (a) | HOLDS | `:491` asserts the constructed stop figure typed at `:478` is not in any log message; the grade figure typed at `:478` is not checked. The log call `cli.py:106`–`:110` passes keys, hash, source, time. |
| 11 | WIDENED `tests/cobalt/test_radar_panel_cards.py:667` · opus + grok | HOLDS (as a fact; desk R17) | `:667` `"/settings/daily", "/settings/daily/apply",` in `POST_ALLOWLIST`; not a row path; `| R17 |` of `cto-2026-09-25.md` exists once. |
| 12 | WIDENED `tests/cobalt/test_prefill_daily.py:191` · opus + grok | HOLDS (as a fact; desk R16) | `:191` carries a constructed figure in the existing-note fixture (his literal no longer in the file); not a row path; `| R16 |` exists once. |

Not claimed as defects, walked anyway (facts):
- "hash in the log is a stand-in for the value" · opus ESCALATE · `cli.py:106`–`:110`: the line logs `sorted(rows)`, `delete`, `digest`, `source`, a UTC time — keys and hash, no value; the hash of a small-value payload is what the claim names (a design question per opus, not a build defect).
- "`store.put` is one transaction — NOT CHECKABLE" · opus + grok: read `store.py:60`–`:117` — `conn.autocommit = False`, all upserts and deletes in one `try`, `conn.commit()` at `:111`, `conn.rollback()` on any exception (`:112`–`:113`). Readable from the file; not in the packet.
- "`TraderSettingsStore()` joins conftest's `dev_db_tx` rollback — NOT CHECKABLE FROM READS" · opus: `tests/cobalt/conftest.py:133`–`:134` `dev_db_tx` is `autouse=True` and routes every `db.connect` through one `cobalt_dev` transaction; I did not walk `TraderSettingsStore._connect` — NOT CHECKABLE FROM READS beyond that.
- "`SheetModesConfig` rejects a sheet not in `order`" · opus NOT CHECKABLE: `aset/config.py:206`–`:219` `_order_covers_every_sheet` raises when a declared sheet is unordered (or an ordered sheet undeclared) or duplicates; the change line builds `sheets` only from `risk.sheet_modes.order` (`drc.py:227`).
- "any committed file outside `configs/`, `src/`, `tests/` still carries the old literal" · opus NOT CHECKABLE FROM READS — not run (the prompt's gate is those three roots).

Where the checkers differ: OPUS found the card-path second writer and the four weak proofs; GROK did not look at `card.py` and found three other weak assertions; neither contradicts the other. Both call the two widened test paths desk-ruled (OPUS) / list them as the EXCEPT (GROK).

Own calls:
- (i) `git -C /Users/cobalt/cobalt log --oneline 00262e45..5d4f8201 -- src/cobalt/cli.py src/cobalt/drc src/cobalt/db_migrations src/cobalt/aset/engine.py src/cobalt/cards src/cobalt/radar` → EMPTY.
- (ii) `old literal: 0 hits` over `/Users/cobalt/cobalt-wt/drc-d1/configs`, `src`, `tests` (fixed-string grep, exit 1, no output). The base template's daily-stop line was read once from the saved output of `git show 00262e45:configs/cobalt/templates/daily.md.j2` (line 18); the grep command is not quoted here.
- (iii) ONE READER hits placed: `sheet_modes` — the settings package (`settings/models.py`, `settings/drc.py`, `settings/cli.py`), the sizing code that already read it (`aset/web.py` lines 337–1436, `aset/engine.py`, `aset/radar_panel.py:733`, `aset/config.py`, `daymode/config.py`, `src/cobalt/cli.py:167`–`:184`), and `prefill/daily.py:59`/`:481` (the existing sizing-rule text, one `load_sheet_modes_config()` call) and `prefill/drc.py:49`/`:380`/`:408` (the OLD DRC prefill, untouched by D4). `daily_stop_full` hits: `settings/models.py:64`, `:97`, `:173` only; `daily_stop_half`: `settings/models.py:98`, `:174` only; consumers go through `DAILY_STOP_KEYS` (`settings/drc.py:119`, `:149`, `:211`); `daily_risk_values`: `settings/drc.py:114`, `:206`, `prefill/daily.py:73`/`:537`, `aset/web.py:1200`; `load_drc_settings`: `settings/drc.py:82`, `:118`.
- (iv) SEAM: `grep -n -F "/attest" aset/web.py` → `613`, `1141`, `1187`; `grep -n -F "/settings/daily" aset/web.py` → `1186`, `1211`, `1237`, `1243`, `1258`; D4's first route (`:1243`) follows `/attest`'s function (`:1141`–`:1181`); the next existing route is `/card/{card_id}/move` at `:1298`.
- (v) L32: no ticker, no real date of his, no file name of his and no value written in this report (the old literal, the grep that finds it, and the constructed test figures are not quoted; the run's own timestamps are not his dates).

## Ready for D2
| checker | CHECK DRC D4 line | ready | reason verbatim |
|---|---|---|---|
| opus | `CHECK DRC D4: BUILD STANDS EXCEPT cli.py:10/:81 one-apply claim false (card path), X12 /size pin not DB-backed, E3 one-transaction test never reaches put · ready for D2: YES` | YES | (EXCEPT items as in the line) |
| grok | `CHECK DRC D4: BUILD STANDS EXCEPT widened tests/cobalt/test_prefill_daily.py:191 and tests/cobalt/test_radar_panel_cards.py:667 · ready for D2: YES` | YES | (EXCEPT items as in the line) |

Both checkers say YES; `defects that HOLD` = 12 (not 0), so by §4 the hub's own field reads `ready for D2: NO` — the classifier's input, not a hub verdict (L37).

## FOR THE CLASSIFIER
Round 1 of ≤3 (L75). No `old literal` hit (0). One item per claim that HOLDS in my file-check; I add no class and no recommendation.
1. "`cli.py:10` and `:79`–`:81` claim that every apply goes through `apply_settings` and 'nothing else'. But `settings/card.py`'s `cmd_load_card` still calls `store.put` directly" — opus (EXCEPT-1) — D4-4 (one write path, L3) — `settings/cli.py:10`–`:11`, `:79`–`:81`; `settings/card.py:323` — HOLDS (its read-back and `assert_writable` sub-claim does not: `card.py:322`, `:325`–`:331`).
2. "the X12 `/size` test patches `load_sheet_modes_config` to an offline config, so `/size` never reads cobalt_dev's settings" — opus (a)1 — E10 / X12, D4-6 — `tests/cobalt/test_drc_settings_db.py:111` — HOLDS.
3. "the 'one transaction' test fails validation before any `put`, so it never exercises the atomicity of `store.put`" — opus (a)2 — D4-4 / E3 — `tests/cobalt/test_drc_settings_db.py:251`/`:255`; `settings/drc.py:258` — HOLDS.
4. "'the history row exists' is only checked as `source` and `updated_at` on an overwritten row" — opus (a)3 — D4-4 (trace) — `tests/cobalt/test_drc_settings_db.py:226`–`:228` — HOLDS.
5. "the one-reader walk greps only the two key-name strings" — opus (a)4 — D4-2 (one reader) — `tests/cobalt/test_drc_settings.py:249` — HOLDS.
6. "the E2 table counts D4-4 as 11 tests; it has 12, so the table sums to 48, not 49" — opus — build report — build report line 85 vs `tests/cobalt/test_drc_settings.py` (12 D4-4 tests) — HOLDS.
7. "ESCALATE 7 says the X12 reader is 'NOT yet run with-DB' and ESCALATE 9 says 'RESTARTS not run'; both are out of date" — opus — build report — build report lines 217, 219 vs E7 / RESTARTS sections — HOLDS.
8. "`test_drc_settings.py:156` accepts a good value by not raising and never checks the typed result" — grok (a) — D4-1 — `tests/cobalt/test_drc_settings.py:156` — HOLDS.
9. "`test_drc_settings.py:469` only asserts the success banner is absent when the double drops the key; it does not require the read-back sentence" — grok (a) — D4-4 (read-back) — `tests/cobalt/test_drc_settings.py:468`–`:469` — HOLDS.
10. "`test_drc_settings.py:491` asserts one constructed stop figure is absent from the log and does not assert the grade figure" — grok (a) — D4-4 (no value logged) — `tests/cobalt/test_drc_settings.py:491` — HOLDS.
11. "WIDENED — `tests/cobalt/test_radar_panel_cards.py:667` (`POST_ALLOWLIST` names the two new routes)" — opus + grok — scope — `tests/cobalt/test_radar_panel_cards.py:667` — HOLDS (a fact; desk R17).
12. "WIDENED — `tests/cobalt/test_prefill_daily.py:191` (the existing-note skeleton's daily-stop line)" — opus + grok — scope / D4-3 — `tests/cobalt/test_prefill_daily.py:191` — HOLDS (a fact; desk R16).

## ESCALATE
1. Checker lines, no FIX: opus `CHECK DRC D4: BUILD STANDS EXCEPT cli.py:10/:81 one-apply claim false (card path), X12 /size pin not DB-backed, E3 one-transaction test never reaches put · ready for D2: YES` — my file-check: items 1–3 above HOLD (item 1's read-back sub-claim does not); grok `CHECK DRC D4: BUILD STANDS EXCEPT widened tests/cobalt/test_prefill_daily.py:191 and tests/cobalt/test_radar_panel_cards.py:667 · ready for D2: YES` — items 11–12 HOLD as facts, desk-ruled.
2. Every item under `## FOR THE CLASSIFIER` (1–12), restated by number.
3. Prompt line drift: `06` and `05` cite v2 §9 D4 / order paragraph / E10 / X12 at `:92` / `:95` / `:114` / `:118`; `DRC-AUTOMATION-v2-2026-09-22.md` reads them at `:178` / `:181` / `:200` / `:204` (staged under the real lines). No packet mismatch, no packet cut, no checker that did not check, no checker that wrote a file it was not told to (GROK wrote only `grok-check.md`).
4. Opus's design question (not a build defect): `settings/cli.py:106`–`:110` logs the payload's sha256; for a daily-stop-only payload the value space is small.
5. NOT CHECKABLE FROM READS carried from the checkers, each with my read above: the `store.put` transaction (readable, `store.py:60`–`:117`, one transaction); whether the with-DB `TraderSettingsStore()` joins the `dev_db_tx` rollback (`conftest.py:133`–`:134` autouse; `_connect` not walked); whether `_failed` logs its text (page errors carry typed values — not read); a committed file outside `configs/`, `src/`, `tests/` with the old literal (not run).
6. Builder's ESCALATE 6 / 8 (desk-confirmed, not built): `account.sheet_mode_default` left out; the store keeps no history table (the trace is each row's `source` = `aset.change_line@sha256:<hash>` + `updated_at`, plus the log line; a versioned history row would need migration `0019`); `settings/card.py`'s `cmd_load_card` writes via `store.put`, not `apply_settings` (my item 1).
7. Builder's RESTARTS item (iii)/11: `configs/cobalt/templates/daily.md.j2` UNCLASSIFIED in `configs/cobalt/jobs.yaml` → the conservative six-label `RESTARTS:` value; derived residents `com.cobalt.aset` and `com.cobalt.radar` (radar restart inside the pause, L43 / L66); a `no_resident_reads` entry beside `rules.yaml`'s (`jobs.yaml:309`) would classify it — the deploy-prompt drafter's item.
8. Builder's (iv), verbatim: "His daily-stop VALUE must be loaded by him before the first deploy (change line or `cobalt settings load --optional`) — until then the new daily note says `not given`."
9. Harness note for the desk: the `claude -p` runs print `Permission deny rule (../../cobalt/.claude/settings.local.json): Bash(git push*:*) mixes * with the trailing :* prefix syntax … Use Bash(git push*) for wildcard matching.` — a settings-syntax notice, not an instruction.
10. Astra: `astra: METER — retry after Sep 26th, 2026 6:47 AM` (v3 `:10`); at `date` 2026-09-25 06:4x ET it was not yet due; not probed, not launched; the desk seats it from that time in its own launch (L62 as amended R19).
11. Standing line: **"Round 1 covers DRC D4 whole (`00262e45..5d4f8201`), its E10 / X12 outputs and its three suites' executed output, checked by Opus 5.5 + Grok under his R95 (a new build: Fable-seat as Opus 5.5 per 09-22 R109; Astra METER until Sep 26th, 2026 6:47 AM, seated by the desk from its return; Gemini out, R96/R97). Two seated houses `ready for D2: YES` with `defects that HOLD: 0` → D4 is checked by the floor L67 allows while Astra is METER (the shrink-to-two clause), and D2 (`07`) may stack on this tip; Astra's read is recorded when it returns. A HOLD → a fix round classified first (L75), ≤3 rounds (L39)."** — this round: two houses YES, `defects that HOLD: 12`.
12. Standing line: **"The deploy's L68 gate re-proves offline, with-DB and the live-note suite on the stacked tree that ships (D1 + K1 + K2 + D4 + D2 + D3); the deploy prompt gets its own house read (L67, R95 seats)."**

DRC D4 CHECK DONE · round: 1 · opus: CHECK DRC D4: BUILD STANDS EXCEPT cli.py:10/:81 one-apply claim false (card path), X12 /size pin not DB-backed, E3 one-transaction test never reaches put · ready for D2: YES · grok: CHECK DRC D4: BUILD STANDS EXCEPT widened tests/cobalt/test_prefill_daily.py:191 and tests/cobalt/test_radar_panel_cards.py:667 · ready for D2: YES · astra: METER retry after Sep 26th, 2026 6:47 AM · houses that checked: 2 of 3 · defects that HOLD: 12 · ready for D2: NO · ESCALATE: 12
