# Stale marker — FIX ROUND (round 2) draft, 2026-09-22

## §0 Headline
- Classified every round-1 finding of `68` (4 checkers + hub ESCALATE 1–6): **FIX 5 · NOT REAL 4 · UNPROVEN 4 · OUT OF SCOPE 0 · OWNER ITEM 2**. Every FIX is a test or one report line. No code change expected.
- Drafted `prompts/2026-09-22/09-stale-marker-fix.md` (Sonnet 5, offline, tests only, a mutation proof per test row) and `prompts/2026-09-22/10-stale-marker-check-r2.md` (4 seats, reads the fold only, staggers after `81`).
- New rule strings: 0. Two strings dropped from `51`'s list for the fix. ESCALATE: 3.

## Sources read (drafter, 2026-09-22 07:41–07:46 ET, `date`)
LAWS.md L35, L45, L48, L53, L60, L62, L63, L67, L70, L71, L73 · `reports/stale-marker-check-2026-09-22.md` whole · `sol-check.md` and `opus-check.md` whole, plus the `CHECK:` sections of `grok-check.md` and `gemini-check.md` (`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/stale-marker-check/`) · `prompts/2026-09-21/51-stale-marker-build.md` · `prompts/2026-09-21/68-stale-marker-check.md` · `prompts/2026-09-20/21-bars-chunk-1a-check-r2.md` · `cto-2026-09-22.md` R3, R5, R6 and the 07:39 line (:56) · `cto-2026-09-21.md` R36, R39, R46, R49 · branch `s2/stale-marker-0921`: `git log --oneline main..s2/stale-marker-0921` = `ca9566f` (report) · `ead43a0` (D1) · `0d961e6` (C1); `--stat`: C1 = `radar_panel.py` +59/−14, `test_radar_panel.py` +95, `test_radar_panel_cards.py` +176; D1 = DevDoc +2; report +225 · the code at the branch tip: `radar_panel.py:829-842, 880-887, 1066-1076`, `grep -n bars_stale` (hits :193, :613-618, :647, :829-836, :874-875, :884, :908, :983, :1047-1073, :1185) · `test_radar_panel.py:94-122, 414-430, 532-620` · `test_radar_panel_cards.py:120-187, 313-327, 386-545, 591-631` · `radar_p2_support.py:36-64`, `web.py:857-884`. Worktree `git status --short`: empty; `.env`: "No such file or directory".

## CLASSIFICATION TABLE
The file-check column quotes the round-1 hub's own `## Checked against the branch` verdict. A finding counts as FIX only where that row says HOLDS (the rule of this round). Where the hub wrote no row of its own, the column says so, and the drafter's own read is marked as a claim for round 2 to check (L35).

| id | checker(s) | finding (≤2 lines) | file-check row | CLASS | what the fix is (FIX rows only) |
|---|---|---|---|---|---|
| F1 | sol (Q1), hub ESC 1 | The exactness test has ONE `current` row, and that row is the stale one. A version that badges EVERY current row whenever any poll fails would still pass. | "`_small_snapshot()` returns exactly ONE `current` row" — `test_radar_panel.py:122` — **HOLDS** | **FIX** | `09` X1: two REAL `current` rows (the second taken from `POOL_FIXTURE`). Stale A with healthy B gives a badge on A only. Then swap: stale B with healthy A gives a badge on B only. The healthy row's ticker cell is exactly `<td class="ticker">T</td>`. Mutation proof M1: at `:836`, badge every current row. |
| F2 | sol (Q1), hub ESC 1 | All 6 evaluated cards share ONE ticker (`FTFT`). A version that badges EVERY active card whenever the pool is degraded would still pass. | "the `evaluated` card fixture uses ONE ticker, `FTFT`" — `test_radar_panel_cards.py:123` — **HOLDS** | **FIX** | `09` X3: the ladder gets a second active card of a real fixture ticker (the `tc:315` precedent `copy.deepcopy(rows[0]) \| {"card_id": 7, "ticker": "BGFI"}`). Stale FTFT with healthy BGFI gives badges on the FTFT cards only, and the BGFI article is byte-identical to its unbadged render. Then the reverse. Mutation proof M3: at `:1069`, badge every active card. |
| F3 | sol (Q1 + the reason on its CHECK line), opus (Q1 gap 2) | The card badge is never proven through the real `/radar` route. The route test (e) uses an empty `FakeCardStore()`, and card placement is tested only through a direct `render_radar_page`. | No hub row of its own. It is carried in Sol's `FIX FIRST … stale-card route tests`, which ESCALATE 1 holds. Drafter's read: `test_radar_panel.py` (e), inside `:570-606`, passes `card_store=FakeCardStore()` — **HOLDS by drafter read (claim — r2 checks it)** | **FIX** | `09` X4: `GET /radar` and `/radar?frame=phone` through the REAL route and the REAL `render_radar_page`. Only `web_module.build_radar_panel` is monkeypatched, returning `RadarPanelView(pool=<stale pool>, ladder=_ladder(<FTFT + BGFI rows>))`, as `tc:622-631` does for a view. The response carries the FTFT strip badges and the row badge, and no badge on BGFI. Mutation proof M3. |
| F4 | opus (Q1 gap 1; CHECK line "departed/excluded no-badge untested (tc:400-401,436-439)"), hub ESC 1 | No test can fail if a departed or excluded row gets the badge. FTFT and GURE are asserted to be in NO pool category, so no stale ticker ever has a departed or excluded row. | "`card_ticker`/`GURE` asserted absent from every pool category" — `test_radar_panel_cards.py:398-402` — **HOLDS** | **FIX** | `09` X2: the stale set holds the current row's ticker plus the `_small_snapshot()` departed ticker and excluded ticker (all real). The badge lands on the current row only. The departed and excluded `<tr>` rows hold no `bars-stale`, and the banner names all three. Sub-case: a current episode and a departed episode of the SAME real `POOL_FIXTURE` ticker, badge on the current one only. Mutation proof M2: pass the map to the departed / excluded table at `:885` / `:886`. |
| F5 | opus (Q1; CHECK line), hub ESC 2 | The build report's §0 says "8 new tests RED on main's code". Its own T1 section shows 5 failed and 3 guards green (`5 failed, 3 passed`, dots `F.FF..FF`). | "report §0 overclaims … confirmed against `build-report.md`" — `build-report.md:5` vs `:70-75` — **HOLDS** | **FIX** | `09` X5: correct the §0 line of `stale-marker-build-2026-09-21.md` in place to "5 RED then GREEN, 3 guards GREEN on both", with a dated line naming this round. The report's last line is untouched. |
| N1 | grok (Q1 rebuttal) | GURE's tooltip text differs from the row and card tips, so test (a)'s title-set assertion does distinguish it. | `test_radar_panel_cards.py:468-470` — HOLDS, "but only narrows the gap for GURE … Sol/Opus's gap stands" | **NOT REAL** | — (not a defect finding, and it does not close F1–F4, per the hub's own row) |
| N2 | all 4 (Q6a); opus adds "Whether to mark it too is his call" | A lifecycle-polled ticker's departed row carries no badge while its card does. | "Row badge passed only to the `current` table" — `radar_panel.py:884-886` — HOLDS (as designed) | **NOT REAL** | — (all four answer RIGHT. The proposal's Q3, "open question (checker / tribunal)", `STALE-MARKER-PROPOSAL-2026-09-21.md:36, :74`, was put to the checkers and all four closed it "never". Opus's aside is quoted for the desk, ESCALATE 3.) |
| N3 | gemini, sol, grok, opus (Q6c) | `mirrorStale` keys a card by the `.strip b` text node. It would be fragile if a node were ever inserted before the ticker text. | No hub row. All four checkers: safe for the markup the file emits; `data-ticker` would break the healthy pins | **NOT REAL** | — (hypothetical future markup; no defect in the emitted HTML) |
| N4 | sol (Q3) | A failed refresh keeps the old layer and does not call `mirrorStale`. Rows and strip can lag the backend. | No hub row. Sol: "They remain mutually consistent with the retained fragment", and `REFRESH FAILED` shows loudly | **NOT REAL** | — (by design: loud `REFRESH FAILED`, consistent state) |
| U1 | opus (Q2) | A ticker with both an `error` and a `stale` entry gets a tooltip of two fragments joined by `"; "`, not one banner substring. No test covers it. | **Not checked** by the hub (no row) | **UNPROVEN** | — (the join is `51` C1 item 1's prescribed behaviour. It is not a HOLD this round, so it is not built. Listed for the desk, ESCALATE 2.) |
| U2 | opus (Q3) | In theory: if `mirrorStale` threw inside the `try`, the catch would mark a detached layer `REFRESH FAILED`. | No hub row; Opus: "not realistic" (input is the server's own `json.dumps`) | **UNPROVEN** (L70) | — (no browser ran; never a defect) |
| U3 | opus (Q5) | Whether the golden pins were captured on main's code is NOT CHECKABLE FROM READS (someone must run `tc:475` against `5b208a0`'s `radar_panel.py`). Also, the API pin hashes `sort_keys=True` output, so it cannot see a change in key order. | No hub row | **UNPROVEN** (L70) | — (the build report quotes the pin run on main's code — a claim; a run settles it. Not a defect.) |
| U4 | hub ESC 5 (all 4) | No browser ran. Live `mirrorStale` behaviour and the badge's fit in the 90 px / 70 px strip column are unproven. | "NOT CHECKABLE FROM READS (L70)" | **UNPROVEN** (L70) | — (the desk confirms on the live page with him after the deploy) |
| O1 | hub ESC 3 (all 4, Q7) | THE TWO CLOCKS. The badge follows the poller: 180 s, RTH only, plus `error`. The evaluator's `input_stale` (200 s, all sessions) and its `no_daily_bars` path can leave a card with NO badge, most clearly outside RTH. Whether `score_card` fills `score_suppressed` in that case is NOT CHECKABLE FROM READS. | Hub: "This window is HIS, per L53 — not a build defect" | **OWNER ITEM** | — (quoted verbatim below; never resolved by this round) |
| O2 | hub ESC 4 (all 4, Q3) | IN-CARD BADGE LAG. The ARMED `last` and LEVELS `last` badges update only on the next `/radar` load or card action. A card's strip and its expanded detail can disagree about staleness, in either direction. | Hub: "The proposal already flags this as assumed-acceptable; every checker confirms the mechanism, none finds a defect" | **OWNER ITEM** | — (ASSUMED by the proposal, Q2; his acceptance is owed) |
| — | hub ESC 6 | Packet mismatches: none. `WROTE:` none. `ASK DESK` none. | — | n/a (records none; not counted) | — |

Counts: FIX 5 (F1–F5) · NOT REAL 4 (N1–N4) · UNPROVEN 4 (U1–U4) · OUT OF SCOPE 0 · OWNER ITEM 2 (O1, O2).

### OWNER ITEMS — verbatim, for the desk to carry to him (never resolved here)
- **O1 (ESCALATE 3 of `stale-marker-check-2026-09-22.md`):** "**Question 7, HIS clocks question (L53), quoted from all four, not a build defect:** the badge follows the poller's 180s/RTH-only/+error clock; the evaluator's 200s/all-sessions `input_stale` (and its separate `no_daily_bars` path) can leave a card with no badge, most clearly outside RTH. All four checkers agree this can be silent elsewhere on the page for a fully-tapped, stale-untapped-free card; whether `score_card` populates `score_suppressed` in that exact case is NOT CHECKABLE FROM READS (`score_card`'s body is outside the packet). This is the proposal's own ESCALATE 2, his to close, never a checker verdict on the build."
- **O2 (ESCALATE 4 of the same report):** "**In-card badge lag (Q2, all four, ASSUMED by the proposal):** the two in-card badges (ARMED `last`, LEVELS `last`) only update on the next `/radar` load or card action, not on a pool refresh, so a card's strip and its expanded detail can disagree about staleness in either direction for a window. The proposal already flags this as assumed-acceptable; every checker confirms the mechanism, none finds a defect in it."

## The two prompts — what they carry
| file | seat · mode | launch strings | stop line (L71) |
|---|---|---|---|
| `prompts/2026-09-22/09-stale-marker-fix.md` | Sonnet 5 · `--permission-mode auto` · EXISTING worktree `/Users/cobalt/cobalt-wt/stale-marker`, branch `s2/stale-marker-0921` from `ca9566f` · offline | `51`'s 16 allow strings minus TWO → 14, byte for byte and in `51`'s order. **Dropped:** `"Bash(uv run cobalt jobs restarts *)"` (tests + one report line restart nothing; the stacked deploy derives RESTARTS over its whole range, L42) and `"Bash(mkdir -p *)"` (the reports directory exists, and Write creates files). The 3 denies are unchanged. `--add-dir` = `/Users/cobalt/Vault` + `/Users/cobalt/cobalt` (the pair the drafter prompt names; `51`'s third `--add-dir /Users/cobalt/cobalt-wt` is the fixer's own cwd and is not carried). | `STALE MARKER FIX R2 BUILT <tip> \| on ca9566f \| offline <p>/0 \| tests added: <n> \| code changed: <yes\|no> \| report headline corrected: yes \| ESCALATE: <n>` |
| `prompts/2026-09-22/10-stale-marker-check-r2.md` | Sonnet 5 hub · `--permission-mode auto` · cwd `~/cobalt-wt/agy-trial` · 4 seats Grok · Gemini · Sol · Opus 5 | `68`'s launch line BYTE FOR BYTE. Only the prompt path and `--remote-control stale-marker-check-r2-0922` change. Stages into `scratch/tribunal-bars-0920/stale-marker-check/r2/` (the first Write creates it). | `STALE MARKER CHECK R2 DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the stacked deploy: <r> of <n> · ESCALATE: <n>` |

**NOTE FOR THE DESK — `05` P6:** once `10` has run, `05` P6 must read the LAST NON-BLANK LINE of `reports/stale-marker-check-r2-2026-09-22.md` (its `defects that HOLD: 0` and `ready for the stacked deploy: n of n`), not round 1's `stale-marker-check-2026-09-22.md`. The desk re-points `05` P6 to that file, re-issued whole (L19). This drafter does not edit `05`.

**NOTE FOR THE DESK — launch rows:** both prompts grep `cto-2026-09-22.md` for a `| R__ |` launch row naming their own filename, committed. The desk fills in the two numbers before each launch. `09` needs nothing of his beyond R3 and R36 (a tests-only round on an already-checked lane). `10`'s strings are 09-21 R49 + R39 + the 09-20 R13 read-only set, as in `68`.

## Verification (drafter, `date` 07:51 ET)
- The rule segment of `10`, from `--allowedTools` through the `--add-dir` triplet, taken as one string: `grep -c -F` = 1 in `68-stale-marker-check.md` and 1 in `10-stale-marker-check-r2.md`. It is byte-identical to `68`. The prefix `… --model claude-sonnet-5 --permission-mode auto --remote-control stale-marker-check-r2-0922 --allowedTools` appears once in `10`.
- `09`'s 14 allow strings and 3 denies: `grep -c -F` of each quoted string in `51-stale-marker-build.md` = 1, all seventeen.
- Desk literals the two prompts gate on, both committed: `-S"the stale branch takes a FIX ROUND"` → `a1db586`; `-S"we will deploy today after trading day ends at 11 am"` → `dd6713a`. `reports/ops-6a-check-r3-2026-09-22.md` exists (the `10` stagger file).

## ESCALATE
1. **The mutation proofs in `09` edit `radar_panel.py` temporarily.** Each X row makes ONE named edit (M1–M3), shows the new test RED, reverts the edit with the Edit tool, and shows `git diff -- src/cobalt/aset/radar_panel.py` EMPTY. `radar_panel.py` is never `git add`-ed, and CLOSE proves `git diff ca9566f -- src` EMPTY. This is how a GREEN-as-pin test is shown to be green for the right reason (the X-row precedent of `21`'s "MUTATION PROOF"). If the desk wants a strictly read-only-source fixer, strike the M steps. The tests then stand as pins with no proof that they would catch anything.
2. **U1 (the joined-tooltip coverage gap Opus raised) is not built.** No hub row checked it, so this round does not count it as a HOLD. `ASK DESK: add U1's test (a ticker with an `error` and a `stale` entry → tooltip `"<a>; <b>"` on its row and card) to `09` as X6, or leave it? [07:46 ET]` Safe default: left out, listed.
3. **N2: Opus's aside is quoted for him, not ruled.** "Whether to mark it too is his call." It concerns the departed row of a lifecycle-polled ticker. All four checkers answer RIGHT, and the proposal's Q3 was put to them. The desk decides whether that aside reaches him. This drafter classes it NOT REAL on the four RIGHT answers.

STALE MARKER FIX PROMPTS DRAFTED · FIX: 5 · NOT REAL: 4 · UNPROVEN: 4 · OUT OF SCOPE: 0 · OWNER ITEM: 2 · code change expected: no · new rule strings: 0 · ESCALATE: 3
