# SETUPS FIX ROUND — drafter report (`14`, Opus 5, 2026-09-22 10:16–10:3x ET)

## §0 Headline
- Classified every round-1 finding of `setups-one-check-2026-09-22.md`: **FIX 6** (HOLD 1 by a blind seat; F1–F5 in code/tests/doc), the rest NOT REAL / UNPROVEN / OUT OF SCOPE / OWNER ITEM (table below).
- Wrote `15-setups-fix.md` (Opus 5, `auto`, `65`'s 19+3 strings byte for byte), `16-setups-check-r2.md` (`66`'s line byte for byte), `17-setups-blind-committed-day.md` (`13`'s line byte for byte). **New rule strings: 0** (proven by `grep -c -F` and `cmp`, below).
- Beyond the desk's expected FIX set (HOLD 2 + HOLD 3): F3 (closure, check ESC 6), F4 (rollback doc, ESC 5), F5 (AWAITING_A_RULING pin, ESC 8) — each on file evidence, each strikable by the desk before launch.
- ESCALATE: 11.

## CLASSIFICATION TABLE
Ids: `Qn` = a `## Per question` row (`.x` = a distinct claim inside it), `Cn` = a `## Checked against the branch` row in table order (C1 = pins … C15 = the n/a pill), `En` = a `## ESCALATE` item. "Drafter read" = a fact I read by tool this session (L35), NOT a hub file-check; it never turns a "not checked" row into HOLDS.

| id | checker(s) | finding (≤2 lines) | file-check row | CLASS | what the fix is (FIX rows only) | closed by |
|---|---|---|---|---|---|---|
| E1 · C1 · Q5.1 | Grok, Opus, build ESC (ii) | `DEF_WRITTEN_*` pins are the engine's own output; [F-16] (1) blind derivation never done | **HOLDS** (build report's own words) | **FIX** (a seat, not code) | a blind house re-derives the 17 existing pins on the committed day; the cut days' pins by `13` | **17** (+ `13`) |
| C2 · Q5.2 | Gemini | "expected values ARE the definition evaluated blind" (`50-build-report.md:186`) | **DOES NOT HOLD** (mis-cited; :187 says the opposite) | NOT REAL | — | none |
| E2 · C3 · Q3.5 · Q4 · Q4.b | Opus | undocumented `ANCHORS` (`anchors.py:73-90`); def outside 4 families → `not_formed: no formation anchor` (`evaluate.py:1080-1081`); `evaluability` calls it evaluable; doc and drift test silent | **HOLDS** | **FIX F1** | `evaluability` reads `anchor_for` → NOT EVALUABLE `anchor:none`; doc "Anchors" section; drift test covers `ANCHORS`; T RED on `60ddac4` = Opus's worked def | **15** |
| Q4.a | Opus | a with-trend def naming `Extension.state` is forced against the Extension (A-01), undocumented | not checked (FINAL §1 behaviour) | **FIX F1** (doc part) | `A-01` named in the doc's Anchors section, in the FINAL's words | **15** |
| E3 · C4 · Q3.2 · Q12.5 (sequence) | Opus | `sequence` serves one hardcoded tuple (`triggers.py:219-236`) vs FINAL §2.2 `:148` "each step is a predicate evaluated by the same interpreter". Drafter read: another step list is reported `trigger:sequence` (honest, not silent) | **HOLDS** | **FIX F2** | steps through the interpreter's dispatch, walked in bar order; `close_through`, `close_above(prior_bar)` served per FINAL §4; second-chance = one instance, its tests unchanged; safe default stops F2 only | **15** |
| E6 · Q6.1 · Q10(a) | Grok, Opus (Gemini "complete" DOES NOT HOLD) | closure misses the trigger resolver's reads: vwap-continuation reads `pivot.n` + `range.micro.*` undeclared; latent until the assumed note fills a null hole | hub: Gemini's contrary claim **DOES NOT HOLD**; not counted as a headline HOLD (latent). Drafter read: the build's own guard pins the gap (`test_setups_lego.py:305-311`); `range.micro.touch_tolerance_atr` / `bound_flat_slope_atr` are `value: null` (`tunables.yaml:215-217`, `:226-228`) | **FIX F3** | resolvers declare their keys; closure unions them; `TRIGGER_READ_UNDECLARED` → empty (A1 by design); T RED: an assumed fill of a `range.micro.*` key must reach `assumed_keys` | **15** |
| Q6.1b | Grok, Opus | the always-read `extension.path_a_*` / `path_b_atr` keys are also undeclared | not checked; build guard `test_setups_lego.py:333` asserts none is assumed | NOT REAL (non-null engine rows; hole-fill can never make them assumed — Opus Q6) | — | none |
| E5 · Q11.1 | Opus | rollback after the assumed note is written and loaded breaks old code (no `assumed` in old `TunableSource`); no runbook | carried, not re-verified. Drafter read: `~/cobalt` main `tunables.py:63-66` = `RULING`, `SHEET`, `DWV` only | **FIX F4** (doc only) | `ADDING-A-SETUP.md` § "Rolling back after the assumed note": order + why; premise quoted by `git show main:…` | **15** (+ the deploy prompt's rollback section reads it) |
| Q11.2 | Grok | 0013 applied then rolled back with no NULL-slug row restores NOT NULL; the new global-assumed sync breaks until reapplied | not checked | **FIX F4** (same doc section) | the order covers it | **15** |
| E8.b · Q5.11 · Q9 (gate 2) | Opus | `AWAITING_A_RULING` shapes skipped by gate 2 with no pin (`test_setups_lego.py:45`, `:72`) | carried, not re-verified. Drafter read: confirmed at those lines | **FIX F5** (test only, GREEN-as-pin) | each member evaluable + forms on no committed scan; no value of his needed | **15** |
| Q5.12 | Opus | vwap-continuation's F1 status lives only in the report; its test fills `dist.k.vwap` | not checked | **FIX F5** (same pin) | the shape with its fill removed forms on no scan | **15** |
| E4 · C5 · Q3.3–3.4 · Q12.5 (relations) | Opus | `after`, `inside`, `on that RangeBreak`, `on`, `between` each fit exactly one def | **not re-verified** | NOT REAL (narrow by FINAL §4 "only what the seven need"; drafter read: `atoms.py` `_on_gaps` / `_after_gaps` / `_inside_gaps` return `Unsupported(<word>:…)` → reported NOT EVALUABLE, named — honest) | — (coverage pin P1, GREEN-as-pin) | **15** (P1) |
| E7 · Q5.3 | Opus | nine-ema can form LONG on a falling day: legs alternate so `Leg(opening_drive OR impulse).direction == trade_direction` always holds; `touched` true for a leg wholly below EMA9 | **NOT CHECKABLE FROM READS** | **OWNER ITEM** (root = build Found 15 / ASK DESK 15, the impulse size rule, O4) + UNPROVEN for the `touched` reading | test that would prove it: a constructed falling day (bounce above EMA21, dip wholly below EMA9, up bar closing above EMA9) → the shape forms LONG at 60ddac4; whether that is wrong needs O4 | none |
| C6 · Q5.4 · Q12.4 (Opus ESC 3) | Opus | `indicator_rejection` reads `frame.run[-1]` without a bucket-completeness check; `micro_range` does check | **not re-verified** ("plausible, not run") | UNPROVEN (L70) | test: a half-built 2-min bucket as the last bar → the trigger must not fire | none |
| Q3.1 | Opus | `_between` branches on `trigger.type == "indicator_cross"` (step5 part2 `:454-456`); Lego (ii) scans only four names | not checked | UNPROVEN | test: a def using `between` with a non-`indicator_cross` trigger — evaluability and formation outcome | none |
| Q5.5 | Opus | hitchhiker stale anchor: `instantiated_ts` recomputed; `formation_consumed` keyed on the formed bar could admit a second card | NOT CHECKABLE FROM READS (Opus) | UNPROVEN | test: a range whose longest window moves between scans after the first card expired | none |
| Q5.6 | Opus | fashionably-late `cross_index` takes the latest cross, no freshness bound | not checked | UNPROVEN (the def is AWAITING_A_RULING anyway: X10, F1) | test: a stale cross far before the scan | none |
| Q5.7 | Opus | vwap sloped trendline can arm after price is already through it | not checked | UNPROVEN | test: last close above the sloped line at arming | none |
| Q5.8 | Opus | second-chance `event(stop_hit)` uses the global `stop.buffer`, not the def's own | not checked | UNPROVEN | test: a def with its own buffer row; which buffer the event reads | none |
| Q5.9 | Opus | rubberband sticky culmination could form long after the culminating bar (X3) | X3 UNPROVEN, runs at the deploy | OUT OF SCOPE (the deploy's X3, build ESC iii) | — | none (deploy) |
| Q5.10 · Q9 (live-note) | Opus | live-note gate accepts `avoided` as a pass; §9 (3) says `formed` | not checked | NOT REAL (`65`'s gate text: "`formed` in its outcome set UNLESS its own avoid is `avoided`") | — | none |
| Q3.6 | Opus | the eighth def forms only with the test's fills of two null engine holes, and only because it names `Range(micro)` | not checked | NOT REAL as a defect (L69 constructed config is `65`'s design; the anchor dependency is F1's doc) | — | none |
| Q3.7 · Q4 (Gemini) · Q8 (Gemini) | Gemini | no leak; `NO FAILURE`; nothing live worse | contradicted by C3 / C4 HOLDS | NOT REAL | — | none |
| C7 | Grok | trade-name comment "Second Chance step 2…" at `trade_def.py:637-639` | **HOLDS as a comment, DOES NOT HOLD as a leak** | NOT REAL (immaterial; in the Lego baseline) | — | none |
| Q1 | all three | R2-3 = B by X20; predicate exact; 0013 bounded | agreed | NOT REAL (confirmation) | — | none |
| Q1.a | Grok, Gemini, Opus | `taxonomy/migrations/0001_trade_defs.sql:63` still `slug NOT NULL`; a rebuilt DB where 0013 runs before the table exists keeps it | **HOLDS**, low severity (rebuilt DB only) | OUT OF SCOPE (protected path, `65` CLOSE empty-diff list; no production effect) | — | none (deploy-prompt notes) |
| Q1.b | Opus | a duplicate-supplier refusal beyond B's text (Astra's A clause, in the loader) | not checked | NOT REAL (harmless, Opus's word) | — | none |
| Q2 · Q2.a | Grok, Opus | `refresh_card` gets the published `ev`, not `by_side[card.direction]`; same result; build ESC (vi) says "Conflicts: none" | consensus, not re-derived | NOT REAL (no functional defect; a report-honesty note) | — | none |
| Q2.b | Opus | the card's `radar_score_id` points at an `avoided` row | not checked | NOT REAL (cosmetic, Opus's word) | — | none |
| Q6 | all three | tap refused before any write; NO PATH today | **HOLDS** (C12, writer set) | NOT REAL (confirmation) | — | none |
| Q6.2 | Opus | X22 cannot see a missing CONVENTION (never a tunable read) | not checked | UNPROVEN (no instance named) | test: a resolver consulting an undeclared `A-nn` convention | none |
| Q6.3 | Grok | after a ruling, the audit export's fresh closure can drop the mark the open card still carries | not checked | UNPROVEN | test: export after a `source: ruling` row replaces an assumed one, vs the card's dot | none |
| Q6.4 | Opus | whether the page shows the refusal message (X24 PASS claimed) | NOT CHECKABLE FROM READS | OUT OF SCOPE (the deploy's NN#16 smoke) | — | none (deploy) |
| Q7 · C10 | all three | every added `tunables.yaml` row `value: null`; no number of his | **HOLDS** | NOT REAL (confirmation) | — | none |
| Q7.a | Opus | a companion value in a test literal was removed (build Found 11) | not checked | NOT REAL (the build's own hygiene fix) | — | none |
| E9 · Q7 (ON/OFF) | all three + build ESC (viii) | no per-setup "discoverable or not" dial — R44 requirement (c) | agreed by all | **OWNER ITEM** (O1) | — | none (the desk → him) |
| E8.a | build ESC (vii), Found 12 | `AWAITING_A_RULING`: backside, fashionably-late (X10 — neither fix chosen), vwap-continuation (F1 — three `per_indicator` holes null) | carried as the design | **OWNER ITEM** (O2 X10, O3 F1) | — | none (the desk → him) |
| C14 · Q8.1 | Opus | nightly replay refuses pre-deploy receipts after the version bump | **by design** (FINAL point 5) | NOT REAL | — | none |
| C15 · Q8.2 | Opus | a FILLED card gains an extra `n/a` pill for `assumed_formation` | **NOT CHECKABLE FROM READS** | **OWNER ITEM** (O5; build ASK DESK 4) | — | none (the desk → him) |
| Q8.3 | Opus | the suppression reason drops "(tap to grade)" when every blocker is the assumed dot | not checked | NOT REAL (Grok Q8: "that is B's sentence") | — | none |
| Q8.4 · Q12.2 | Opus | frames rebuilt per def, not once per (member, scan, side) as FINAL §3; production-pool latency unknown | not checked | UNPROVEN | the deploy's stored-day X5 (build ESC iii) | none (deploy) |
| Q9.1 | Opus | the R1-21 "two real formations" fixture now runs on a synthetic copy | not checked (build Found 3 admits it) | OUT OF SCOPE (closed by `12`'s DB-backed cut, build ESC i) | — | none (`12`) |
| Q9.2 · Q9.3 | Opus, Grok | card / golden pins normalise away `engine_inputs`, `engine_why`, `tunables_sha256`, E9 names, `by_side`, anchor, wrappers — those fields could change unseen | C13 **HOLDS on the sample** (every removal paired) | UNPROVEN (named A1 normalisations by design) | test: mutate `engine_why`; show no pin fails | none |
| Q9.4 | Opus | whether the committed day or any constructed literal is his | NOT CHECKABLE FROM READS | UNPROVEN (the day is the P2 synthetic re-date, `_cut_p2_fixtures.py:36-38`) | — | none |
| C8–C13 | hub | (i) paths · (ii) protected paths empty · (iii) null rows · (iv) no setup names beyond baseline · (v) score writers · (vi) no weakened assert (sample) | **HOLDS** ×6 | NOT REAL (confirmations, no defect) | — | none |
| Q10 | all three | L52 (a)–(d) met (with the X22 latent gap → F3) | — | NOT REAL (confirmation) | — | none |
| Q11.3 | Opus | the writer is proven on `tmp_path` only; X23 on the real dev vault UNPROVEN | — | OUT OF SCOPE (L28 dev-vault proof owed at the assumed-note write, after the reader ships — build ESC ix) | — | none |
| Q12.1 | Opus | "rubberband only can form in production" is unsupported: the tested nine-ema shape reads no null engine key | NOT CHECKABLE FROM READS | UNPROVEN | the deploy's live-note run (gate 3, `COBALT_LIVE_VAULT_ROOT` set) | none (deploy) |
| Q12.3 | Opus | missing: X3, gate 4, live-note, stored X5 | by design at the deploy | OUT OF SCOPE (build ESC iii) | — | none (deploy) |
| E10 | hub | Grok did not self-write `grok-check.md` | recorded | OUT OF SCOPE (harness) | — | none |
| E11 | hub | ASK DESK: none | — | NOT REAL (no finding) | — | none |

**Counts** (one per row above; a row that carries two classes is counted under its FIRST): FIX **9 rows** covering **6 fixes** (HOLD 1, F1–F5) · NOT REAL **19** · UNPROVEN **12** · OUT OF SCOPE **7** · OWNER ITEM **4 rows** covering **5 items** (O1–O5). The stop line counts FIXES and OWNER ITEMS as items (6, 5) and the other classes as rows.

### OWNER ITEMS — quoted verbatim, never resolved here; the desk brings each to him
- **O1 — the per-setup ON/OFF dial (R44 requirement (c)).** Round 1 `## ESCALATE` 9: "**The missing per-setup ON/OFF ("discoverable or not") dial, carried as-is.** All three checkers and the build report agree: none exists; the build says so plainly rather than inventing one. This was R44's explicit requirement (c) and remains open for the owner/tribunal." Build ESC (viii): "No per-setup "discoverable or not" switch was built; the drafter's (A) holds, and none exists. The only switch is the global `radar.cards_enabled`. The smallest candidate is a design question for the owner / tribunal (`ADDING-A-SETUP.md` § Discoverable or not)."
- **O2 — X10's fix.** Build Found 12: "`ASK DESK: X10 failed — which fix, Grok's or Fable's? [05:1x]`. Safe default: neither is built."
- **O3 — F1's three holes.** Build ESC (vii): "**F1: the three `per_indicator` holes stay null** (fashionably-late's two flat thresholds; vwap-continuation's `dist.k.vwap`). These cannot form at production defaults: **fashionably-late** and **vwap-continuation** (`AWAITING_A_RULING: F1`)."
- **O4 — the impulse / pullback size rule** (the root of round 1 ESC 7). Build Found 15: "`ASK DESK: should impulse / pullback carry a size rule (a new A-nn key) before cards? [06:0x]` — safe default: none."
- **O5 — the health pill.** Build Found 4: "`ASK DESK: should the health dot class skip assumed_formation? [20:44]` — safe default: unchanged."

## RULE PROOF
- `15`: each of the 19 allow strings and 3 deny strings `grep -c -F` against `prompts/2026-09-21/65-setups-one-build.md` → every count ≥1 (17 strings `1`; `"Bash(COBALT_ENV=dev uv run pytest *)"` `2`; `"Bash(rm /Users/cobalt/cobalt-wt/setups-c1/.env)"` `2`). Only the prompt path, `--remote-control setups-fix-0922` and `--permission-mode auto` (the desk's ruling in `14`) differ from `65`'s line.
- `16`: the span `--permission-mode auto … --add-dir /Users/cobalt/cobalt-wt` with the remote-control name removed, `cmp` against `66`'s → `IDENTICAL` (714 B).
- `17`: the same span, `cmp` against `13`'s → `IDENTICAL13`.
- New rule strings: **0**.

## DIGEST
- Built nothing; four files written: `15`, `16`, `17`, this report.
- `15` (Opus 5, `auto`, cwd `~/cobalt-wt/setups-c1`, rc `setups-fix-0922`): F1 anchors (evaluability `anchor:none` + doc + drift test), F2 generic `sequence`, F3 closure, F4 rollback doc, F5 AWAITING_A_RULING pin, P1 relations pin. Needs row R__, `05`'s stop line, `12` NOT started in the same worktree. Stop: `SETUPS FIX R2 BUILT <tip> | on 60ddac4 | …`.
- `16` (Sonnet 5, rc `setups-check-r2-0922`, staging `setups-check/r2/`): needs row R__, `15` BUILT, `38`'s and `17`'s stop lines, and — if run after 2026-09-22 23:59 ET — a committed row of HIS extending grok/agy.
- `17` (Sonnet 5, rc `setups-blind-committed-0922`, staging `setups-blind-committed/`): needs row R__ and `38`'s stop line; Gemini through today 23:59, else Opus 5 (same-house, recorded). Runs on `60ddac4`'s pins, before/beside/after `15`.
- New rule strings: 0. Code change expected: yes (F1, F2, F3).
- Launch order: `17` when the house lane frees → `15` after `05`'s stop → `16` after `15` BUILT and `17` DONE → the desk re-points `12` to the fix tip (L19 re-issue, strings unchanged) → `12` → `13` → the deploy-prompt drafter.

## ESCALATE
1. **`16`'s date.** `grok` / `agy` stand through 2026-09-22 23:59 ET (R39). `16` waits for `05` → `15` (two baselines + two closes ≈ 72 min of suites, plus code) → `17`. If it launches after midnight it needs a committed row of HIS: `Bash(grok *) and Bash(agy *) through <D>` (`66`'s gate, carried unchanged). `ASK DESK: bring the extension to him with tonight's list? [10:3x]` — safe default: `16` refuses with `FAILED: authorization expired …`.
2. **`17`'s house lane.** The desk's lane order is `38` → `40` (derive) → `17`. `17` gates only on `38`'s stop line (as `14` asked). If `40` puts a Grok / Gemini call in the lane, the desk holds `17` until `40` stops — no gate in the file does it. `ASK DESK [10:3x]` — safe default: the desk launches `17` after `40`.
3. **FIX set is wider than the desk expected** (HOLD 2 + HOLD 3). F3 rests on the build's own guard test and two null rows; F4 on main's enum (drafter read); F5 is a pin with no value of his. All three are small. The desk may strike any before launch — a full re-issue of `15` (L19).
4. **F2 carries a real design risk.** A generic step walk needs a bar-indexed resolution per step shape. The FINAL gives one for `close_through` / `close_above(prior_bar)` ("relation resolvers over working-TF closes") and nothing for other shapes. `15`'s safe default stops F2 alone (the tuple stays; `ASK DESK`), so round 2 may see F2 not built. Drafter read: today's failure mode is honest (`trigger:sequence`, NOT EVALUABLE), so HOLD 3 is a FINAL-conformance gap, not a silent never-form like HOLD 2.
5. **F1 changes a published row for a def with no anchor.** It becomes `not_evaluable` (`anchor:none`) instead of `not_formed: no formation anchor`. All eight corpus shapes have an anchor. His other six definitions (R15's 13) are not built, and whether any of them reaches that branch today is UNPROVEN (L70). The deploy's live-note run shows it.
6. **`15`'s with-DB command** is the build report's `## CLOSE` set, written out with paths: that line names the files without paths. I checked each path exists in the worktree (`ls`, no MISSING). The baseline must reproduce `540 passed, 1 skipped` before any edit.
7. **`15` and `12` share `setups-c1`.** `15`'s PREFLIGHT refuses if `12`'s report exists. NOTE FOR THE DESK: **`12-setups-fixture-cut.md` PREFLIGHTs on tip `60ddac4`** (`git log -1 --format=%h` = `60ddac4`, its RECOVERY `60ddac4..HEAD`, its pin comment `at 60ddac4`), so it must be re-pointed to the fix tip before it runs. That is launch mechanics only, with the strings unchanged (L19). `15` writes the same note into its report.
8. **`16`'s staging folder is `setups-check/r2/`**, as `14` names it, not `setups-one-check/r2/`. Round 1's answers are therefore `../../setups-one-check/<house>-check.md`. Opus's `--add-dir` names two folders, both inside R49's wildcard (a spelling inside an approved string, not a new string).
9. **`17` rubberband pin semantics.** Its constants are read at ONE scan (`2026-01-06T16:30:00+00:00`, FTFT, the `mixed` shape without HTF avoid), not at the first formation, and include `_LAST`. `17` gives the house that scan instant as a grid input. It never stages `setups_shapes.py:40-42` (the culmination-bar comment).
10. **`17` side spelling.** If a pin spells the side other than `long|short` (e.g. a direction), the row is `MISMATCH` with the note "spelling": `13`'s normalisation stands and there is no translation (L37). The desk reads it before routing.
11. **UNDERIVABLE is expected in `17`** for EMA / VWAP / ATR-resting values (nine-ema, vwap-continuation, the rubberband Extension path), because neither seat runs code. A code-capable blind seat (Sol read/write, back Sat 2026-09-26 06:47 ET) would compute the series on the staged bars. That needs a NEW string (fixture-cut drafter ESC 7).

SETUPS FIX PROMPTS DRAFTED · FIX: 6 · NOT REAL: 19 · UNPROVEN: 12 · OUT OF SCOPE: 7 · OWNER ITEM: 5 · code change expected: yes · new rule strings: 0 · ESCALATE: 11
