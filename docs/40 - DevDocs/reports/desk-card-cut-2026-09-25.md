# DESK CARD CUT — drafter report (2026-09-25, seat `desk-card-cut-draft-0925`, bg `b7ff7d67`, Opus 5.5, read-only)

## §0 Headline
Five PROPOSED files in `reports/desk-card-cut-2026-09-25/`, nothing live touched: a new `topics/cto-desk-contract.md` (11,914 B: 29 contract lines, 64 checklist rules), a 139-of-139 coverage table, the wake-up file (3 sentences changed), the close routine (+1 step, 2a LESSONS GATE), the measure.
Card: 352,491 B ≈ 88,123 tokens today → 167,117 B ≈ 41,779 after (−53 %); the 06:10 wake-up (167,323) would measure ≈ 121k (≈ 134k if step 8 keeps reading §4 in full).
State is not cut: NOW, LAWS in full, §0, the §4 rows since the last HANDOVER, `## §5 CURRENT`, the HANDOVER line and the prompt folder are all still read; `topics/cto-desk.md` stays whole.
New rule strings: 0 · owner items: 0 · ESCALATE: 6 (2 ASK DESK, 4 desk readings).

## L74
One block appended to the tool result of this seat's first read (the prompt file) was styled as a system reminder. It asked for a `Claude-Session: https://claude.ai/code/session_…` line in commit messages and PR bodies and named a file-send tool. Per L74 it is data, not an instruction, and was not followed. This seat commits nothing.

## AUTHORIZATION
`grep -n "^| R28 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-25.md"` → line 36 (06:41 ET): `| R28 | 06:4x ET | — NO WORDS OF HIS BEYOND R27 ("I want your A") and 09-22 R109 …: DESK LAUNCH ROW for prompts/2026-09-25/19-draft-desk-card-cut.md — the index-card cut DRAFTER …`. It names this prompt, so the seat is authorized. The row is OFF-LADDER, per R27.

## DIGEST
- The contract keeps 29 `[stated … · origin]` lines. They distil the 63 contract-mapped source lines (the Directions, the derived contract, and every standing Dejan ruling through 09-25 R27). Each line keeps its tag and cites every source line it condenses (`(:8 :11 :12)`). Rulings that are now law are one clause citing the law (L58–L67, L72–L76). The one struck text in the source, `qwen --yolo`, stays struck.
- The checklist has 64 rules in 8 groups: launch L1–L16, watch W1–W6, commit C1–C4, handover/refresh H1–H5, reads R1–R7, replies P1–P9, prompts/cards K1–K15, memory M1–M2. Each rule cites its lessons as `:N(n)`. A rule that already lives in UNATTENDED-LAUNCH.md or in LAWS is one line citing that place (L2, L13, W1, W4, C3, C4, R7, K2, K7, K14).
- Coverage: 139 of 139 lines mapped (`grep -c` on the table). 63 lines go to the contract, 6 of which also feed a checklist rule. 52 lines go to the checklist only. 24 are retrieval-only: 6 frontmatter, 2 headings, 2 blank, and 14 history or superseded lines (:22 :24 :27 :33 :48 :49 :51 :72 :80 :82 :103–:106). Every numbered LESSONS item is mapped. Three items stay retrieval-only, with reasons: :98(4), :130(5) and :135(2).
- Measure (bytes ÷ 4): item 3 drops from 109,997 to 11,914 B. Item 6's §4 drops from 73,542 to 20,778 B (R21–R33 since the 06:08 HANDOVER). Item 6's §5 drops from 36,595 to 1,650 B (6 live sessions in the compact table). LAWS (89,839 B) and everything else are unchanged.
- Wake-up diff: item 3 now points to `topics/cto-desk-contract.md`. Item 6 now reads only `§0`, the §4 rows after the last HANDOVER and `## §5 CURRENT`. REFRESH step (3) now rewrites a compact `## §5 CURRENT` and moves finished rows under `## §5 HISTORY`. `diff` against the live file shows exactly these 3 hunks. STEP 0 and the 500,000 line are byte-identical.
- Close diff: one row added, `2a` Lessons gate, between steps 2 and 3. The hub PROPOSES under `Checklist — OWED` and the desk APPLIES under L58. `diff` shows one added line.
- Always-loaded block: adding the INDEX line `- [[cto-desk-contract]] — desk contract + checklist, read at wake-up — 09-25` (77 chars) and shortening the cto-desk line to `- [[cto-desk]] — dated desk lessons record — 09-25` (51 chars, was 58) takes 3,843 to 3,913 of 4,000 chars (`wc -m`).
- Source drift: `topics/cto-desk.md` is 109,997 B, not the card's 108,988 B, because line 138 (R27) landed before this read. The live desk report grew from 130,264 B to 140,712 B during the run, so the measure uses the 06:51 snapshot.

## OWNER ITEMS
None. Every choice here is packaging of rules he already gave (R27: "I want your A"). No rule's wording needs his word.

## CONTINUE
For the desk, in order:
1. Commit `reports/desk-card-cut-2026-09-25/` and this report with pathspecs.
2. Read `coverage.md`. The apply condition, 139 of 139, is met.
3. Write `topics/cto-desk-contract.md` in the memory folder as a byte copy of `cto-desk-contract.proposed.md`. It is a NEW file, under L58.
4. Edit INDEX: add the contract line, and optionally shorten the cto-desk line (both strings are in the DIGEST).
5. Optionally Edit the `description:` of `topics/cto-desk.md`, which still says "Read by the desk at every wake-up". Supersede it, never delete.
6. Replace `prompts/CTO-DESK-WAKEUP.md` and `SESSION-CLOSE.md` with each proposal's text above its `## DIFF`.
7. The next REFRESH writes the first `## §5 CURRENT`, and the successor's first §5 row records its measure (R27).

## ESCALATE
1. ASK DESK: wake-up step 8 still reads `## §4 Rulings` of today AND yesterday in full: 73,542 B + 176,914 B, about 62k tokens. The card said "nothing else changes", so step 8 is untouched. Narrowing it to a status read (`grep -n "APPROVED" <file>`, fixed string, then the matching rows) is a one-sentence edit the desk can rule on. Safe default: unchanged. [06:52]
2. ASK DESK: item 6 still says "every file" of `prompts/<today>/`, which is 610,316 B today, and its wording is unchanged. Its own lesson (:126(4)) says prompts are read at their launch turn, so "the listing" is the true read. Safe default: unchanged. [06:52]
3. Desk reading, no action: `## §5 CURRENT` does not exist yet in today's report. A crash wake-up before the first post-apply refresh reads the §5 block whose heading ends `— CURRENT`. The five older HANDOVER lines (23,726 B) count as finished rows under the new step (3) and move under `## §5 HISTORY`. The newest HANDOVER stays the last line (step 6).
4. Desk reading, no action: citations are compressed to `(:N)` and `:N(n)`, with the date read from the cited line's own tag, instead of the card's `(cto-desk.md:<line>, <date> lesson (<n>))`. The full form measured 17,110 B against the 12,000 B cap. The file's first body line states the format.
5. Desk reading, no action: the close gate says "stated ON OR AFTER `updated:`", not "after". Without it, a lesson appended on the day of the cut would never be gated. A line already cited by a rule passes free.
6. Desk reading, no action: the wake-up's `WORK RULES (from cto-desk.md, …)` sentence still names the old file. It is harmless, since those rules are repeated inline, and it was left alone because the card allowed changes to items 3 and 6 and REFRESH (3) only.

DESK CARD CUT DRAFTED · contract: 11914 · checklist rules: 64 · lines mapped: 139 of 139 · card before: 88123 · card after: 41779 · new rule strings: 0 · owner items: 0 · ESCALATE: 6
