# DEPLOY 2026-09-19 (2) — S2-P4

Hub `deploy-p4-0919` (Opus 5, `claude-opus-5`), background session, cwd `/Users/cobalt/cobalt`.
Prompt: `docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md` (sha256 `0a65607c4c64f798880054520bf5eeb0f6a2f5db907eae10bf78c92b1388e5fd`).
Authorization: `cto-2026-09-19.md` §4 **R30** (14:23 ET) + `cto-2026-09-18.md` §4 R3/R5/R6/R18–R21 + `cto-2026-09-19.md` §4 R16/R19/R23. Verified below before anything ran.

## §0 Headline

- **STOPPED AT THE L68 GATE, STEP 1.2 — `archiver/append-0919` CONFLICTS with the P4+ops stack in 9 files.** This is the integration seam L68 exists to surface. It is reported, never resolved here. Merge aborted.
- **NOTHING WAS DEPLOYED. Production is exactly as it was found**: main still `23aedbf`, no merge, no migration, no settings write, both residents UP on their original pids (aset 72854, radar 72867), no `.env` ever copied anywhere.
- PREFLIGHT was clean: **16 rows, ZERO denials**; authorization R30 verified (names this file, sha256 MATCHES, committed `f1780f5`); heartbeat GREEN; P13 proof exit 0 with no `CHANGED`.
- **P15 MET** (`ARCHIVER DB2 f5df6c1 … db 161/0`) — the archiver's three known reds are gone. The blocker is no longer test redness; it is a textual merge conflict on the shared migration registry and the archiver/CLI files.
- **FAILED: gate — conflict merging archiver/append-0919.** ESCALATE: 5. Gate artifacts left on disk for the desk (see ESCALATE 1 — a relaunch will fail at `worktree add` until they are removed).

### Authorization, verified before anything ran

| what | where | verdict |
|---|---|---|
| the row that names THIS file | `cto-2026-09-19.md` §4 **R30**, 14:23 ET, "Approve" | PRESENT — names `docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md`, approves its launch line VERBATIM (55 `Bash(` rules, 32 byte-identical to R7's, 23 differing) |
| the file is the approved text | R30 carries the FINAL sha256 `0a65607c…88e5fd` (superseding the pre-row `9919e856…` of `d630707`) | `shasum -a 256` of the prompt = **`0a65607c4c64f798880054520bf5eeb0f6a2f5db907eae10bf78c92b1388e5fd`** — **EQUAL** |
| the row is committed on main | `git log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` | **`f1780f5`** (R30 with the FINAL sha) over `4e4e901` (R30 as first written). Also: `git status` shows `cto-2026-09-19.md` is NOT modified, so the row read IS the committed text |
| R30's conditions | the row itself | start ≤ 18:30 ET today — **MET at 14:27** · tag `deploy-2026-09-19b` · RESTARTS aset + radar + first bootstrap of `com.cobalt.replay` · **no settings write of any kind** · never push, never `bypassPermissions`, never a production schema rollback |
| not this deploy's approval | R7 (07:49 ET) approved `02-deploy-stack-3.md` | acknowledged — NOT read as an approval of this file |
| standing rows | `cto-2026-09-18.md` §4 R3/R5 (weekend push through 09-20), R6 (accepted baseline-RED class; aset bootstraps from the REPO plist), R18–R21 (L67); `cto-2026-09-19.md` §4 R16 (fund rule "C"), R19 (L68/L69/L70 folded; push on his word only), R23 (`pool.py:189` None-filter stays, disclosed) | all found committed |

No mismatch anywhere.

## PREFLIGHT

Ran 14:27–14:30 ET from `/Users/cobalt/cobalt`. **Zero denials — every allowlisted shape probed.**

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P1 | `git -C /Users/cobalt/cobalt status --porcelain` and `git … status` | 0 / 0 | allowed — ` M docs/40 - DevDocs/reports/seat-usage.md`, `?? …/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`. LONG form: `On branch main`, ahead of origin/main by 69 commits, **no rebase in progress, no unmerged paths**. `cto-2026-09-19.md` NOT dirty |
| P2 | `git … tag scratch-allow-probe-p4-0919` then `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-p4-0919' (was f1780f5)` |
| P3 | `git … commit --allow-empty` then `reset --soft HEAD~1` | 0 / 0 | allowed — `[main d35ada7]` created, then `log --oneline -1` → **`f1780f5`** — HEAD is back |
| P4 | `date` | 0 | allowed — **`Sat Sep 19 14:27:40 EDT 2026`** — 14:27 ET ≤ 18:30. Read for the record; enforced at §2.1 |
| P5 | `COBALT_ENV=production uv run cobalt validate` | **0** | allowed — 13 trade_defs OK; **`Jobs (F17): 15 registered — 6 resident, 9 one-shot.`** (the pre-merge state; `com.cobalt.replay` not there yet); `registry <-> ops/: 15 label(s), exact match`; `registry <-> plists: schedules and COBALT_ENV agree on every job`; `Step-downs: … — 7 row(s)`; `Placement: tree clean` |
| P6 | `COBALT_ENV=production uv run cobalt backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 6.2 h old` |
| P6 | `COBALT_ENV=production uv run cobalt backup run --dry-run` | 0 | allowed — `backup: cobalt_brain via pg_dump inside cobalt_memory — 645.2 MB`; `DRY RUN — nothing written, no jobs row touched.` |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..sprint-2/p4` | **1** | allowed — see the P7 note below. EXPECTED class: main's OLD classifier |
| P8 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — **`HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red  (2026-09-19 14:28:39 EDT)`** |
| P9 | `launchctl print gui/501/com.cobalt.aset` · `…/com.cobalt.radar` | 0 / 0 | allowed — both `state = running`; **aset pid 72854**, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` · **radar pid 72867**, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`. Both paths are the expected ones — no plist moved |
| P9 | `launchctl print gui/501/com.cobalt.replay` | **113** | allowed — **`Could not find service "com.cobalt.replay" in domain for user gui: 501`**. Correct: it does not exist yet, nobody bootstrapped it |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` | 0 | allowed — **`200`** |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` | 0 | allowed — **`200`** |
| P11 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(…) … HAVING count(*) > 1"` | 0 | allowed — header only (`tablename	array_agg`), **ZERO rows**. No duplicate table name; 4.5 cannot hit the harness's ambiguity refusal |
| P12 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN ('movers_daily','picks','missed')"` | 0 | allowed — header only, **ZERO rows**. None of 0008/0009's three new names exists anywhere in production |
| P13 | `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` (`timeout` 600000) | **0** | allowed — proof table below. **No `CHANGED`, no tool timeout, nothing applied** |
| P14 | `ls -la <path>/.env` × 4 (`s2-p4`, `ops-2026-09-19`, `archiver-append`, `stack-deploy2-0919`) | 1 × 4 | allowed — **every one "No such file or directory"**. No other session holds `cobalt_dev` |
| P15 | archiver precondition — read of `archiver-append-build-2026-09-19.md` | — | **MET** — see below |
| P16 | ops + P4 branch stop lines | — | **both `0` failed** — see below |

**P7 — recorded, not judged. Exit 1, ONE UNCLASSIFIED path, and the replay row reads differently than the prompt's expectation. Both are main's OLD classifier, exactly deploy 1's P7 class** (whose `.gitignore UNCLASSIFIED` was classified only by rules inside that merge). Verbatim:

```
FAILED: RestartError: one or more changed paths were unclassified
configs/cobalt/smoke/s2.yaml	A	UNCLASSIFIED CONFIG	com.cobalt.agent,com.cobalt.aset,com.cobalt.herdr,com.cobalt.mainframe,com.cobalt.obsidian,com.cobalt.radar
ops/com.cobalt.replay.plist	A	plist in diff	-
RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar
```

`configs/cobalt/smoke/s2.yaml` is a NEW config file arriving WITH P4; main's classifier has no rule for it, so it fans out to all six residents and raises the error. `ops/com.cobalt.replay.plist` classifies as `plist in diff` with restart `-` rather than the prompt's expected `new one-shot, bootstrap once: com.cobalt.replay` — again main's classifier, which does not know the label. Everything else classified cleanly (`DOCS -`, `resident reads`, `static import reach`, `test/documentation; no resident`, `registry; register, no restart`). **§5.1 re-runs `jobs restarts <pre-merge>..HEAD` on the MERGED classifier; that run's `RESTARTS: com.cobalt.aset com.cobalt.radar` with 0 UNCLASSIFIED is the gate, and this row is not a stop here.**

**P8 — NO BASELINE RED TO RECORD.** Every probe `OK`; the only non-OK line is `AMB  com.cobalt.herdr  unmanaged  AMBER launchd unmanaged — loaded, not running (last exit 0) [launchd probe]; runs outside launchd by declared interim`, the standing AMBER, not a RED. R6's accepted-RED gate is therefore not needed; the smoke's rule at 6.1 becomes "no RED at all". `radar` probe reads `idle (overnight)` and `com.cobalt.radar running 367 min, heartbeat fresh` — Saturday is outside every scanning session, so `idle:overnight` is the correct shape. `sheet daymode … cards are accepted`.

**P13 — the production proof, exit 0, nothing applied.** Gate checked term by term: **exit 0**, **no table marked `CHANGED`**, **no tool timeout**. As the prompt states (round-1 finding 14), this probe runs from `/Users/cobalt/cobalt` = main's code, whose static list is 23 tables; `movers_daily`/`picks`/`missed` are simply not in its world, and their absence is proven by P12, not here.

```
cobalt db migrate — PROOF ONLY on cobalt_brain (READ ONLY, nothing applied)

table                side    schema   rows         digest                             secs
------------------------------------------------------------------------------------------
aset_sizings         user    user     146          baac6ac19fd231057ebe2cbe7443c933   0.03
bars                 system  system   8834532      d21fc0b31265b2565c71432a6ebf6979   47.22
card_dot_taps        user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_dots            user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_stop_edits      user    user     1            cbae670bbd7acd13749ac41d99f25861   0.00
card_transitions     user    user     293          3986eb3f96571498667bbfbc3d80aa8c   0.00
cobalt_email_sends   system  system   463          c8fc112af72900bb260196353fea82bd   0.00
cobalt_jobs          system  system   15           324c22d076dada0f52a6c6afec730d2e   0.00
cobalt_kill_switch   system  system   1            310655940a3a0ce10031f77a390733b6   0.00
cobalt_redactions    system  system   2            cf42d85ee49244ca40d419683eb3ca51   0.00
day_modes            user    user     10           d9139ce7290afb41d88404c32c59736b   0.00
desk_grade           system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_packet          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_regime          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_membership     system  system   3334         72b94dfe689084e8e96cd880d031e3f3   0.04
radar_pool           system  system   1            88537229826d4da833be389abd8367be   0.00
radar_score          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_receipt  user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_run      system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
session_blocks       system  system   8            ddf2a773f48403300cbff196fe9aa419   0.00
traders              user    user     1            cf9aec4d006d82fb0f12be2d9afa10b9   0.00
vault_overrides      user    user     29           1f529ee789a2e27f1b73010bbae4cdd7   0.00
vault_writes         user    user     1328         d4dd2c3365b291f7388b4c1fd6519c51   0.13
------------------------------------------------------------------------------------------
23 table(s) probed on cobalt_brain; digest excludes user_id, vault_outcome, vault_reason, account_mode, pool_member_id; aset_sizings: 25 card column(s) added by 0007. Proof cost: total 47.5 s — and a migration pays it TWICE (before and after), inside the outage.
NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.
```

**This deploy's outage budget: 47.5 s × 2 ≈ 95 s of proof**, in line with deploy 1's measured 94.1 s. Movement since deploy 1's 08:2x table, recorded not judged: `vault_writes` 1322 → 1328 rows (digest moved accordingly) and `cobalt_jobs`' digest moved at an unchanged 15 rows — ordinary production activity between two proofs, on tables 0008/0009 do not touch.

**P15 — THE ARCHIVER PRECONDITION: MET.** The line required is LATER than `ARCHIVER DB d2099e1` (which sits at line **1600** of `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` and carried `db 154/3`). The later line is at **line 2864** of the same file, quoted in the part that matters:

> `ARCHIVER DB2 f5df6c1 (report tip; NO code commit this run — the code tip is unchanged at round 3b's `ecb817c`, and this line's own docs-only commit sits on top) | offline 1871/0 (319 skipped …) | db 161/0 (the 22 archiver requires_db tests + the three files' 139 offline tests, run WHILE 0010/0011 were applied; zero failures) | DB-1 tests: PASSED/PASSED — test_the_user_role_has_no_grant_on_either_table[archive_progress] PASSED, [archive_incidents] PASSED, both RED in the first DB run … | DB-2: CLOSED, the twin confirmed ABSENT from the tree and the collection (grep exit 1) | DB-3: CLOSED … | cobalt_dev: 0001–0009, unchanged …`

**`db 161/0` — zero failed.** All three reds P15 names are resolved: DB-1's two tests now PASS after the `REVOKE` fix, and DB-2's test is confirmed absent from the tree. The L68 stacked with-DB gate is not blocked. That line also states `cobalt_dev: 0001–0009, unchanged`, which is the baseline §1.4 (b) expects.

**P16 — the other two branches, recorded not judged.**

| branch | last line's counts | verdict |
|---|---|---|
| `ops/2026-09-19` (`ops-2026-09-19.md`) | `OPS 0919 DB be00506 \| code d860bf7 \| offline 1608/0 (302 skipped) \| db 61/0 … \| cobalt_dev: 0001–0009, unchanged` | **0 failed** |
| `sprint-2/p4` (`s2-p4-verify-2026-09-19.md:1124`) | `P4 DB 9045260 … \| offline 1829/0 \| db 2147/0 … \| owed requires_db: test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4] → PASSED, [p4_before_p2] → PASSED \| cobalt_dev: 0001–0009, unchanged … \| DB DEBT: CLOSED` | **0 failed**, both owed tests PASSED |

### 0.2 GATE

**GATE: proven — commit + tag allowed by the session allowlist.** P2 created and deleted a tag; P3 created and reset an empty commit; HEAD proven back at `f1780f5`. (L62's launch proof.)

### 0.3 Not preflighted

No harmless variant exists inside their own pattern (UNATTENDED-LAUNCH §6), so each is probed by its first real use: `worktree add`, the stack `merge --no-edit`, `rebase`, `bootout`, `bootstrap`, `kickstart`, `merge --ff-only`, `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.

CONTINUE: step 1

## L68 GATE — NOT GREEN, and not reached past 1.2

Ran 14:31–14:34 ET. The gate never reached its suites: the stacked tree could not be built.

### 1.1 The four tips, as found

| ref | tip | vs the facts packet |
|---|---|---|
| `main` (`<main-at-gate>`) | **`23aedbf`** (my own PREFLIGHT commit, over `f1780f5`) | packet said `33462d8`; main has moved by desk/deploy docs commits since |
| `sprint-2/p4` | **`d9c51b4`** `docs(report): s2-p4 round-2 db run stop line — P4 DB 9045260 …` | MATCHES the packet's round-1 correction (`d9c51b4`, docs-only above code `9045260`) |
| `ops/2026-09-19` | **`a868a69`** `docs(report): ops 0919 db run …` | MATCHES the packet |
| `archiver/append-0919` | **`99039f7`** `docs(report): archiver db re-run — the stop line (db 161/0, DB-1 PASSED/PASSED, cobalt_dev back at 0001-0009)` | packet said `193a2ca`; the branch has ADVANCED (the DB2 re-run's report commits). Recorded, not a stop — §1.1's rule |

`git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` → **empty**. The `s2-p4` worktree is clean.

### 1.2 BUILD THE STACK — two merges clean, the third CONFLICTED

| step | result |
|---|---|
| `worktree add -b stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919 main` | **SUCCEEDED** — `Preparing worktree (new branch 'stack/deploy2-0919')`, `HEAD is now at 23aedbf`. No rerun artifact was on disk |
| `merge --no-edit sprint-2/p4` | **CLEAN** — `Merge made by the 'ort' strategy.` 163 files, 46,336 insertions(+), 220 deletions(−). Tip `9201bff` |
| `merge --no-edit ops/2026-09-19` | **CLEAN** — auto-merged `src/cobalt/cli.py`, `src/cobalt/db_migrations/cli.py`, `docs/40 - DevDocs/cobalt/db_migrations/cli.md`. 13 files, 3,351 insertions(+), 21 deletions(−). Tip **`feca2cf`** = `<stack>` as far as it got |
| `merge --no-edit archiver/append-0919` | **CONFLICT, exit 1** — 9 conflicted files, below |
| `merge --abort` | ran; `status --porcelain` on the gate worktree → **empty**. The worktree is back at `feca2cf` |

**The conflict, verbatim from the merge output:**

```
Auto-merging configs/cobalt/taxonomy/tunables.yaml
Auto-merging docs/40 - DevDocs/cobalt/archiver/runner.md
CONFLICT (content): Merge conflict in docs/40 - DevDocs/cobalt/archiver/runner.md
Auto-merging docs/40 - DevDocs/cobalt/archiver/store.md
Auto-merging docs/40 - DevDocs/cobalt/db_migrations/__init__.md
Auto-merging docs/40 - DevDocs/cobalt/db_migrations/placement.md
Auto-merging src/cobalt/archiver/runner.py
CONFLICT (content): Merge conflict in src/cobalt/archiver/runner.py
Auto-merging src/cobalt/archiver/store.py
CONFLICT (content): Merge conflict in src/cobalt/archiver/store.py
Auto-merging src/cobalt/cli.py
CONFLICT (content): Merge conflict in src/cobalt/cli.py
Auto-merging src/cobalt/db_migrations/__init__.py
CONFLICT (content): Merge conflict in src/cobalt/db_migrations/__init__.py
Auto-merging src/cobalt/db_migrations/placement.py
CONFLICT (content): Merge conflict in src/cobalt/db_migrations/placement.py
Auto-merging tests/cobalt/test_finviz_consumers.py
Auto-merging tests/cobalt/test_radar_migration.py
CONFLICT (content): Merge conflict in tests/cobalt/test_radar_migration.py
Auto-merging tests/cobalt/test_radar_score_migration.py
CONFLICT (content): Merge conflict in tests/cobalt/test_radar_score_migration.py
Auto-merging tests/cobalt/test_tenancy.py
CONFLICT (content): Merge conflict in tests/cobalt/test_tenancy.py
Automatic merge failed; fix conflicts and then commit the result.
```

**The 9 conflicted paths:** `src/cobalt/db_migrations/__init__.py` · `src/cobalt/db_migrations/placement.py` · `src/cobalt/cli.py` · `src/cobalt/archiver/runner.py` · `src/cobalt/archiver/store.py` · `tests/cobalt/test_radar_migration.py` · `tests/cobalt/test_radar_score_migration.py` · `tests/cobalt/test_tenancy.py` · `docs/40 - DevDocs/cobalt/archiver/runner.md`.

**Five of those are exactly the files the prompt named in advance** (`§1.2` "EXPECT the conflict here if anywhere", and the archiver build's own cross-branch ESCALATE (iii)): `db_migrations/__init__.py`, `placement.py`, `cli.py`, `archiver/runner.py`, `archiver/store.py`. Three test files and one DevDoc page conflicted on top of that list. `configs/cobalt/taxonomy/tunables.yaml` — also on the predicted list — auto-merged clean.

**What the registry seam actually is, read from both branches (a read, not a resolution).** The two migration registries are *semantically* complementary — the archiver deliberately left the 0008/0009 gap for P4 — but both branches rewrote the same tuple lines and the same docstring, so git cannot text-merge them:

| branch | `FORWARD` tail | `REVERSE` head | docstring |
|---|---|---|---|
| `sprint-2/p4` | `…0007_radar_cards.sql`, `0008_radar_value_movers.sql`, `0009_picks_missed.sql` | `0009…rollback`, `0008…rollback`, `0007…rollback` | "0006/0007 are S2-P2's, 0008/0009 are S2-P4's." |
| `archiver/append-0919` | `…0007_radar_cards.sql`, `0010_archive_progress.sql`, `0011_archive_incidents.sql` | `0011…rollback`, `0010…rollback`, `0007…rollback` | "**THE 0008/0009 GAP IS DELIBERATE.** Those two numbers belong to the …" |

A correct union is `0001…0011` in numeric order with `REVERSE` its mirror — which is what §1.2's post-merge `grep` gate would have required. **Producing that union is a code change on a branch, which this deploy hub does not make** (§1.2: "reported, never resolved here"; the desk resolves it in a build run).

### What was NOT run, and why

Everything from **1.3 onward**. The offline suite, the with-DB suite, the `cobalt_dev` forward/rollback cycle and all three `--proof-only` verdicts were never reached, because 1.2 never produced a stacked tree to run them on. Per §1.6 this makes the gate section incomplete by definition and the gate **NOT GREEN**.

- **`.env` was never copied.** §1.4 (a) was never reached. `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` → **"No such file or directory"**, proven after the abort. The §1.4 (h) red-branch restore does not apply: no `COBALT_ENV=dev` command of any kind was typed, and **`cobalt_dev` was never connected to, migrated, rolled back or read by this session.**
- No production command beyond the read-only PREFLIGHT probes was typed. No `bootout`, no `rebase`, no `merge --ff-only`, no `db migrate --allow-prod` (without `--proof-only`), no `settings load` in any spelling, no tag beyond P2's probe.

### Production state at the stop — verified, not assumed

| check | value |
|---|---|
| main tip | **`23aedbf`**, unchanged since my PREFLIGHT commit. Nothing was merged |
| `com.cobalt.aset` | `state = running`, **pid 72854** — the SAME pid P9 recorded. Never booted out |
| `com.cobalt.radar` | `state = running`, **pid 72867** — the SAME pid P9 recorded. Never booted out |
| `com.cobalt.replay` | never bootstrapped — still `Could not find service` |
| migrations | **none applied.** Production is at 0001–0007; 0008/0009 are NOT in `cobalt_brain` |
| `trader_settings` | **untouched.** No settings command exists in this session's allowlist; cards remain LIVE exactly as R20 left them at 12:30:01 ET |
| gate worktree | clean at `feca2cf`, no `.env` |

**§8 was NOT entered, and correctly so:** it is triggered by a red at 4.3–4.6, a red smoke, a failed resident bootstrap, or a mid-run denial *after* 4.2. None of those happened. Nothing is merged and no resident is down, so there is no safe state to restore — the safe state is the one production never left.

## ESCALATE

1. **GATE ARTIFACTS ARE ON DISK AND BLOCK A RELAUNCH — the desk owns their removal.** Branch **`stack/deploy2-0919`** and worktree **`/Users/cobalt/cobalt-wt/stack-deploy2-0919`**, left at `feca2cf` (main + P4 + ops, the archiver merge aborted). Per §1.2's RERUN CASE this is deliberate: no `worktree remove` / `branch -D` rule is in this session's allowlist, so I cannot clean them, and **the next launch of this prompt will fail at `worktree add` until they are gone.** Remove both before relaunching.
2. **THE BLOCKER, and who owns it: `archiver/append-0919` does not merge with P4+ops.** Nine conflicted files, five of them predicted. This is not a test failure and not a P4 defect — it is the integration seam L68 was written to catch, and it needs a build run on the archiver branch (or a rebase of it onto the P4+ops stack) before deploy 2 can be relaunched. **Deploy 2 cannot proceed on the current branch tips under L68 as written.** The desk's two paths, neither of which is mine to choose: (a) resolve the seam in a build run and re-gate; (b) bring Dejan the narrower L68 reading (gate only what is about to land = P4 on main) — the same choice the facts packet §12 item 3 already flagged as OPEN for the desk.
3. **P15's precondition is satisfied but no longer sufficient.** The desk's gating question for this deploy was the archiver's three red `requires_db` tests; those are genuinely resolved (`db 161/0`, DB-1 PASSED/PASSED, DB-2 CLOSED). The prompt's P15 therefore passed and sent the run forward into a conflict it did not screen for. **A future deploy prompt should add a mergeability precondition beside the redness one** — a dry `merge --no-commit` of each leg, or a check that each unmerged branch is rebased onto the current main — so this class stops at PREFLIGHT with nothing built, instead of at 1.2 with a worktree to clean up.
4. **P7 recorded, unresolved by design.** `configs/cobalt/smoke/s2.yaml` reads `UNCLASSIFIED CONFIG` and `ops/com.cobalt.replay.plist` reads `plist in diff  -` under main's OLD classifier, exactly deploy 1's P7 class. Both are expected to classify correctly once P4 merges (§5.1 is their gate) — but that has now NOT been demonstrated in production, and remains unproven until deploy 2 actually runs.
5. **Production reads worth keeping.** (a) P13's proof shows `vault_writes` 1322 → 1328 rows and a moved digest, and `cobalt_jobs`' digest moved at an unchanged 15 rows, both since deploy 1's 08:2x table — ordinary activity on tables 0008/0009 do not touch, recorded so a later proof does not read it as a surprise. (b) The two untracked `docs/40 - DevDocs/prompts/2026-09-19/31-packet/*.yaml` copies are still untracked and were never `git add`ed, as instructed. (c) Carried P4 escalates still owed a desk ruling, unchanged by this run: **A2** (deadline scope), **A7/K3** (the HOLD false positive), **ESCALATE 19** (ticker pinning).

FAILED: gate — conflict merging archiver/append-0919 — src/cobalt/db_migrations/__init__.py, src/cobalt/db_migrations/placement.py, src/cobalt/cli.py, src/cobalt/archiver/runner.py, src/cobalt/archiver/store.py, tests/cobalt/test_radar_migration.py, tests/cobalt/test_radar_score_migration.py, tests/cobalt/test_tenancy.py, docs/40 - DevDocs/cobalt/archiver/runner.md — safe state: green — residents: up

---

# SECOND RUN

Hub `deploy-p4-0919` (Opus 5, `claude-opus-5`), background session, cwd `/Users/cobalt/cobalt`.
Prompt: the SAME file, refolded for R31 — `docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md`, run-2 sha256 `0174f9ae57d7b300063cdf59c41a7f7f6e3e7ed7657e2b1e64a3db83733cb22e`.
Authorization: `cto-2026-09-19.md` §4 **R30** (14:23 ET) **and R31** (14:4x ET, "A" — the narrowed L68 scope, and the row that carries the run-2 sha) + `cto-2026-09-18.md` §4 R3/R5/R6/R18–R21 + `cto-2026-09-19.md` §4 R16/R19/R23.
Run 1 (14:26–14:33 ET) stands above, unedited: it stopped at the L68 gate with `FAILED: gate — conflict merging archiver/append-0919`. Nothing above this line is rewritten.

### Authorization, verified before anything ran

| what | where | verdict |
|---|---|---|
| the row that names THIS file | `cto-2026-09-19.md` §4 **R30**, 14:23 ET, "Approve" | PRESENT — names `docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md`, approves its launch line VERBATIM (55 `Bash(` rules, 32 byte-identical to R7's, 23 differing) |
| **R31, the run-2 row** | `cto-2026-09-19.md` §4 **R31**, 14:4x ET, "A" | PRESENT — narrows the L68 gate to `sprint-2/p4` + `ops/2026-09-19` for this one deploy (archiver NOT stacked), and states **"launch line and all 55 rules UNCHANGED, so R30's 23 approved strings stand — a typed command was removed, no rule"** |
| the file is the approved text | R31 carries the **FINAL sha256 for RUN 2**: `0174f9ae…cb22e` (superseding R30's run-1 `0a65607c…e5fd` in writing, and the intermediate `4473d248…e46c`) | `shasum -a 256` of the prompt = **`0174f9ae57d7b300063cdf59c41a7f7f6e3e7ed7657e2b1e64a3db83733cb22e`** — **EQUAL** |
| the rows are committed on main | `git log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` | **`74a0555`** (R31 carries the run-2 sha) over `bfe9849` (R31 "A"), over `11bf4c2`, over `f1780f5` / `4e4e901` (R30). Also: `git status` shows `cto-2026-09-19.md` is **NOT modified**, so the rows read ARE the committed text |
| R30's conditions | the row itself | start ≤ 18:30 ET today — **MET at 14:43** · tag `deploy-2026-09-19b` · RESTARTS aset + radar + first bootstrap of `com.cobalt.replay` · **no settings write of any kind** · never push, never `bypassPermissions`, never a production schema rollback |
| not this deploy's approval | R7 (07:49 ET) approved `02-deploy-stack-3.md` | acknowledged — NOT read as an approval of this file |
| standing rows | `cto-2026-09-18.md` §4 R3 (07:14), R5 (11:09), R6 (11:18), R18–R21 (17:30–17:42); `cto-2026-09-19.md` §4 R16 (11:05, fund rule "C"), R19 (11:47, L68/L69/L70 folded; push on his word only), R23 (12:34, `pool.py:189` None-filter stays, disclosed) | all found committed (`8f2597a` and older for 09-18; the list above for 09-19) |

No mismatch anywhere.

## PREFLIGHT (second run)

Ran 14:42–14:47 ET from `/Users/cobalt/cobalt`. **Zero denials — every allowlisted shape probed.** Every command was RE-RUN; nothing was carried over from run 1.

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P1 | `git -C /Users/cobalt/cobalt status --porcelain` and `git … status` | 0 / 0 | allowed — ` M docs/40 - DevDocs/reports/seat-usage.md`, `?? …/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`. LONG form: `On branch main`, ahead of origin/main by **75 commits**, **no rebase in progress, no unmerged paths**. `cto-2026-09-19.md` NOT dirty |
| P2 | `git … tag scratch-allow-probe-p4-0919` then `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-p4-0919' (was 74a0555)` |
| P3 | `git … commit --allow-empty` then `reset --soft HEAD~1` | 0 / 0 | allowed — `[main 121275c]` created, then `log --oneline -1` → **`74a0555`** — HEAD is back |
| P4 | `date` | 0 | allowed — **`Sat Sep 19 14:43:21 EDT 2026`** — 14:43 ET ≤ 18:30. Read for the record; enforced at §2.1 |
| P5 | `COBALT_ENV=production uv run cobalt validate` | **0** | allowed — 13 trade_defs OK; **`Jobs (F17): 15 registered — 6 resident, 9 one-shot.`** (the pre-merge state; `com.cobalt.replay` not there yet); `registry <-> ops/: 15 label(s), exact match`; `registry <-> plists: schedules and COBALT_ENV agree on every job`; `Step-downs: … — 7 row(s)`; `Placement: tree clean` |
| P6 | `COBALT_ENV=production uv run cobalt backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 6.4 h old` |
| P6 | `COBALT_ENV=production uv run cobalt backup run --dry-run` | 0 | allowed — `backup: cobalt_brain via pg_dump inside cobalt_memory — 645.2 MB`; `DRY RUN — nothing written, no jobs row touched.` |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..sprint-2/p4` | **1** | allowed — see the P7 note below. EXPECTED class: main's OLD classifier |
| P8 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — **`HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red  (2026-09-19 14:43:51 EDT)`** |
| P9 | `launchctl print gui/501/com.cobalt.aset` · `…/com.cobalt.radar` | 0 / 0 | allowed — both `state = running`; **aset pid 72854**, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` · **radar pid 72867**, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`. Both paths are the expected ones — no plist moved. Same pids run 1 recorded: nothing restarted in between |
| P9 | `launchctl print gui/501/com.cobalt.replay` | **113** | allowed — **`Could not find service "com.cobalt.replay" in domain for user gui: 501`**. Correct: it does not exist yet, nobody bootstrapped it |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` | 0 | allowed — **`200`** |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` | 0 | allowed — **`200`** |
| P11 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(…) … HAVING count(*) > 1"` | 0 | allowed — header only (`tablename	array_agg`), **ZERO rows**. No duplicate table name; 4.5 cannot hit the harness's ambiguity refusal |
| P12 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN ('movers_daily','picks','missed')"` | 0 | allowed — header only, **ZERO rows**. None of 0008/0009's three new names exists anywhere in production |
| P13 | `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` (`timeout` 600000) | **0** | allowed — proof table below. **No `CHANGED`, no tool timeout, nothing applied** |
| P14 | `ls -la <path>/.env` × 4 (`s2-p4`, `ops-2026-09-19`, `archiver-append`, `stack-deploy2-0919`) | 1 × 4 | allowed — **every one "No such file or directory"**. No other session holds `cobalt_dev` |
| P15 | archiver precondition — RECORDED, no longer a gate (R31) | — | recorded — see below |
| P16 | ops + P4 branch stop lines | — | **both `0` failed** — see below |

**P7 — recorded, not judged. Exit 1, ONE UNCLASSIFIED path, identical in class to run 1 and to deploy 1's P7.** Verbatim, the rows that matter:

```
FAILED: RestartError: one or more changed paths were unclassified
path	change	rule	restart
configs/cobalt/jobs.yaml	M	registry; register, no restart	-
configs/cobalt/radar.yaml	M	resident reads	com.cobalt.radar
configs/cobalt/smoke/s2.yaml	A	UNCLASSIFIED CONFIG	com.cobalt.agent,com.cobalt.aset,com.cobalt.herdr,com.cobalt.mainframe,com.cobalt.obsidian,com.cobalt.radar
ESCALATE: unclassified path configs/cobalt/smoke/s2.yaml
…
RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar
```

`configs/cobalt/smoke/s2.yaml` is a NEW config file arriving WITH P4; main's classifier has no rule for it, so it fans out to all six residents and raises the error. Everything else classified cleanly (`DOCS -`, `resident reads`, `static import reach`, `test/documentation; no resident`, `registry; register, no restart`). **§5.1 re-runs `jobs restarts <pre-merge>..HEAD` on the MERGED classifier; that run is the gate, and this row is not a stop here.**

**HONEST LIMIT on P7 this run (not present in run 1):** the tool truncated ≈13 KB from the MIDDLE of this table, so the `ops/com.cobalt.replay.plist` row was **not read by this session**. Run 1 read it as `plist in diff  -` and recorded it verbatim; I am not restating that as my own observation. What I did read is quoted above. Neither the truncation nor the row changes this step's disposition: P7 is recorded, not judged, and §5.1 is its gate.

**P8 — NO BASELINE RED TO RECORD.** Every probe `OK`; the only non-OK line is `AMB  com.cobalt.herdr  unmanaged  AMBER launchd unmanaged — loaded, not running (last exit 0) [launchd probe]; runs outside launchd by declared interim`, the standing AMBER, not a RED. R6's accepted-RED gate is therefore not needed; the smoke's rule at 6.1 becomes "no RED at all". `radar` probe reads `idle (overnight)` and `com.cobalt.radar running 382 min, heartbeat fresh` — Saturday is outside every scanning session, so `idle:overnight` is the correct shape. `sheet daymode … cards are accepted`.

**P13 — the production proof, exit 0, nothing applied.** Gate checked term by term: **exit 0**, **no table marked `CHANGED`**, **no tool timeout**. Per round-1 finding 14 this probe runs from `/Users/cobalt/cobalt` = main's code, whose static list is 23 tables; `movers_daily`/`picks`/`missed` are not in its world, and their absence is proven by P12, not here.

```
cobalt db migrate — PROOF ONLY on cobalt_brain (READ ONLY, nothing applied)

table                side    schema   rows         digest                             secs
------------------------------------------------------------------------------------------
aset_sizings         user    user     146          baac6ac19fd231057ebe2cbe7443c933   0.03
bars                 system  system   8834532      d21fc0b31265b2565c71432a6ebf6979   47.70
card_dot_taps        user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_dots            user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_stop_edits      user    user     1            cbae670bbd7acd13749ac41d99f25861   0.00
card_transitions     user    user     293          3986eb3f96571498667bbfbc3d80aa8c   0.00
cobalt_email_sends   system  system   463          c8fc112af72900bb260196353fea82bd   0.00
cobalt_jobs          system  system   15           bb072e56f5a158574c31de5937dbb6b0   0.00
cobalt_kill_switch   system  system   1            310655940a3a0ce10031f77a390733b6   0.00
cobalt_redactions    system  system   2            cf42d85ee49244ca40d419683eb3ca51   0.00
day_modes            user    user     10           d9139ce7290afb41d88404c32c59736b   0.00
desk_grade           system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_packet          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_regime          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_membership     system  system   3334         72b94dfe689084e8e96cd880d031e3f3   0.04
radar_pool           system  system   1            88537229826d4da833be389abd8367be   0.00
radar_score          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_receipt  user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_run      system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
session_blocks       system  system   8            ddf2a773f48403300cbff196fe9aa419   0.00
traders              user    user     1            cf9aec4d006d82fb0f12be2d9afa10b9   0.00
vault_overrides      user    user     29           1f529ee789a2e27f1b73010bbae4cdd7   0.00
vault_writes         user    user     1328         d4dd2c3365b291f7388b4c1fd6519c51   0.12
------------------------------------------------------------------------------------------
23 table(s) probed on cobalt_brain; digest excludes user_id, vault_outcome, vault_reason, account_mode, pool_member_id; aset_sizings: 25 card column(s) added by 0007. Proof cost: total 47.9 s — and a migration pays it TWICE (before and after), inside the outage.
NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.
```

**This deploy's outage budget: 47.9 s × 2 ≈ 96 s of proof**, in line with deploy 1's measured 94.1 s and run 1's 47.5 s. Movement since run 1's table 15 minutes ago, recorded not judged: **`cobalt_jobs`' digest moved `324c22d0…` → `bb072e56…` at an unchanged 15 rows** (the jobs registry updates its own rows as one-shots run — the 14:38 heartbeat ran inside that gap). `vault_writes` is unchanged at 1328. Both are tables 0008/0009 do not touch.

**P15 — THE ARCHIVER PRECONDITION: RECORDED, NOT A GATE (R31).** Quoted from `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`, last line, the part that matters:

> `ARCHIVER DB2 f5df6c1 (report tip; NO code commit this run — the code tip is unchanged at round 3b's `ecb817c`…) | offline 1871/0 (319 skipped…) | db 161/0 (the 22 archiver requires_db tests + the three files' 139 offline tests, run WHILE 0010/0011 were applied; zero failures) | DB-1 tests: PASSED/PASSED … | DB-2: CLOSED … | DB-3: CLOSED … | cobalt_dev: 0001–0009, unchanged …`

**`db 161/0` — zero failed**, the expected line, unchanged since run 1 read it. Branch tip: `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` → **`99039f7`** `docs(report): archiver db re-run — the stop line (db 161/0, DB-1 PASSED/PASSED, cobalt_dev back at 0001-0009)` — the same tip run 1 found; the branch has not moved. Under R31 this branch is NOT stacked in tonight's gate, so neither fact gates anything here; both are recorded because the desk asked for them.

**P16 — the other two branches, recorded not judged.**

| branch | last line's counts | verdict |
|---|---|---|
| `ops/2026-09-19` (`ops-2026-09-19.md`) | `OPS 0919 DB be00506 \| code d860bf7 \| offline 1608/0 (302 skipped vs round-2 298; +4 named) \| db 61/0 (tests/cobalt/test_migrate_proof.py only) … \| cobalt_dev: 0001–0009, unchanged` | **0 failed** |
| `sprint-2/p4` (`s2-p4-verify-2026-09-19.md:1124`) | `P4 DB 9045260 … \| offline 1829/0 \| db 2147/0 (vs 2136/0 + 2 owed — +11 …) \| owed requires_db: test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4] → PASSED, [p4_before_p2] → PASSED \| cobalt_dev: 0001–0009, unchanged … \| DB DEBT: CLOSED` | **0 failed**, both owed tests PASSED |

### 0.2 GATE

**GATE: proven — commit + tag allowed by the session allowlist.** P2 created and deleted a tag; P3 created and reset an empty commit; HEAD proven back at `74a0555`. (L62's launch proof.)

### 0.3 Not preflighted

No harmless variant exists inside their own pattern (UNATTENDED-LAUNCH §6), so each is probed by its first real use: `worktree add`, the stack `merge --no-edit`, `rebase`, `bootout`, `bootstrap`, `kickstart`, `merge --ff-only`, `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.

CONTINUE: step 1

## L68 GATE (second run) — BOTH SUITES GREEN, stopped at 1.4 (f) on a `cobalt_dev` digest

Ran 14:47–14:53 ET. Under R31 the stacked tree is `sprint-2/p4` + `ops/2026-09-19`; `archiver/append-0919` is NOT stacked.

### 1.1 The four tips, as found

| ref | tip | vs run 1 / the facts packet |
|---|---|---|
| `main` (`<main-at-gate>`) | **`2a71ace`** (my own second-run PREFLIGHT commit, over `74a0555`) | run 1 had `23aedbf`; main has moved by desk commits (R31) + my own |
| `sprint-2/p4` | **`d9c51b4`** `docs(report): s2-p4 round-2 db run stop line — P4 DB 9045260 …` | UNCHANGED from run 1 |
| `ops/2026-09-19` | **`a868a69`** `docs(report): ops 0919 db run …` | UNCHANGED from run 1, MATCHES the packet |
| `archiver/append-0919` | **`99039f7`** (RECORDED ONLY — not stacked, R31) | UNCHANGED from run 1 |

`git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` → **empty**. The `s2-p4` worktree is clean.

### 1.2 BUILD THE STACK — both merges CLEAN

| step | result |
|---|---|
| `worktree add -b stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919 main` | **SUCCEEDED** — `Preparing worktree (new branch 'stack/deploy2-0919')`, `HEAD is now at 2a71ace`. **The desk's removal of run 1's artifacts held**; the RERUN CASE branch was not taken |
| `merge --no-edit sprint-2/p4` | **CLEAN** — `Merge made by the 'ort' strategy.` **163 files, 46,336 insertions(+), 220 deletions(−)** — byte-for-byte the same counts run 1 got. Tip `6533457` |
| `merge --no-edit ops/2026-09-19` | **CLEAN** — auto-merged `src/cobalt/cli.py`, `src/cobalt/db_migrations/cli.py`, `docs/40 - DevDocs/cobalt/db_migrations/cli.md`. **13 files, 3,351 insertions(+), 21 deletions(−)** — again the same counts. Tip **`e5c0695`** = `<stack>` |
| archiver merge | **NOT RUN — removed by R31**, not skipped silently. Run 1's 9-file conflict stands recorded above as deploy 3's seam |

`git … log --oneline -6` on the stack: `e5c0695` (merge ops) over `6533457` (merge P4) over `2a71ace` over `74a0555` over `4377c84` over `bfe9849`.

**MERGED MIGRATION REGISTRY — GATE PASSED.** `grep -n "0008\|0009\|0010\|0011" src/cobalt/db_migrations/__init__.py` plus the full tuples read at `:50-72`:

```
FORWARD = (0001_schemas, 0002_move_tables, 0003_heartbeat_vault_outcome, 0004_radar_pool,
           0005_heartbeat_note_absent, 0006_radar_score, 0007_radar_cards,
           0008_radar_value_movers, 0009_picks_missed)
REVERSE = (0009…rollback, 0008…rollback, 0007…rollback, 0006…rollback, 0005…rollback,
           0004…rollback, 0003…rollback, 0002…rollback)      # 0001 deliberately NOT reversed
```

**0001…0009 in numeric order, `REVERSE` its mirror, and 0010/0011 ABSENT** — exactly what R31's narrowed stack requires.

### 1.3 OFFLINE SUITE ON THE STACK — GREEN

`ls -la …/stack-deploy2-0919/.env` → "No such file or directory" (proven offline) → `cd` → `uv run pytest -q tests/cobalt tests/taxonomy`, exit 0. Summary line verbatim:

```
1875 passed, 329 skipped, 1 xfailed, 15 warnings in 60.98s (0:01:00)
```

**`0 failed` — GATE MET.** (Context only, never the bar: P4 alone 1829/0, ops-0919 1608/0.)

### 1.4 WITH-DB SUITE ON THE STACK — SUITES GREEN, RESTORE COMPARISON RED

**(a)** `cp /Users/cobalt/cobalt/.env …/stack-deploy2-0919/.env` (by name, never read, never printed) → `ls -la` printed `-rw------- 1 cobalt staff 2186 Sep 19 14:48 .env`.

**(b) BASELINE** — `COBALT_ENV=dev uv run cobalt db migrate --proof-only`, **26 tables on `cobalt_dev`**, `code: e5c0695 (clean)`. `cobalt_dev` at **0001–0009**: P4's `movers_daily` / `picks` / `missed` PRESENT at 0 rows; the archiver's `archive_progress` / `archive_incidents` **ABSENT — they are not even enumerated**, because the stacked registry stops at 0009 and the static table list is this stack's own 26. Row counts/digests recorded in full (the table below carries both proofs side by side).

**(c) FORWARD** — `COBALT_ENV=dev uv run cobalt db migrate`, exit 0. All nine walked (`-- applying 0001…0009`), **NOTHING CREATED**, every table `OK`, every digest unchanged, closing line `content UNCHANGED on every table.` and `proof cost: BEFORE 6.4 s + AFTER 6.3 s = total 12.7 s`. No `CHANGED`. `cobalt_redactions` read **127 -> 127, `0f80638d -> 0f80638d  OK`** here — it had not moved yet.

**(d) SUITE** — `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`, exit 0. Summary line verbatim:

```
2198 passed, 6 skipped, 1 xfailed, 15 warnings in 200.46s (0:03:20)
```

**`0 failed` — GATE MET.** Reconciles exactly: offline `1875 + 329 = 2204` and with-DB `2198 + 6 = 2204`, both ways — so every `requires_db` test genuinely RAN rather than silently skipping.

**(e) END STATE, stated plainly before the restore:** `cobalt_dev holds after the suite: 0001–0009, archiver tables ABSENT` — **the gate added nothing this time (R31)**, which is itself the thing to say plainly.

**(f) RESTORE — reversed nothing, as expected; then the comparison went RED.**

`COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → **exit 1**, verbatim:

```
FAILED: MigrationError: no registered migrations are newer than 0009
```

**NOTHING was applied and nothing numbered ≤ 0009 was reversed** — which is the outcome (f) predicts. The harness spells "there is nothing above 0009 to reverse" as a loud error rather than a quiet no-op (L1's fail-loud shape). Recorded as ESCALATE 6; it is **not** the reason this gate stops.

Closing `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → exit 0, 26 tables. **Compared row for row against (b): 25 of 26 tables byte-identical in row count AND digest. ONE moved:**

| table | (b) baseline, before (c)/(d) | (c) forward, after apply | (f) closing proof |
|---|---|---|---|
| `cobalt_redactions` | **127** rows, `0f80638def1d0d8e46457a59b2771fe7` | 127 → 127, `0f80638d -> 0f80638d  OK` | **128** rows, `a93ce24ff053320346d529539a22be59` |

Every other table — `aset_sizings` 1 / `bars` 1043443 (`2769919a`) / `card_dot_taps` 0 / `card_dots` 0 / `card_stop_edits` 1 / `card_transitions` 4 / `cobalt_email_sends` 2 / `cobalt_jobs` 13 / `cobalt_kill_switch` 1 / `day_modes` 2 / `desk_grade` 0 / `desk_packet` 0 / `desk_regime` 0 / `missed` 0 / `movers_daily` 0 / `picks` 0 / `radar_membership` 0 / `radar_pool` 0 / `radar_score` 0 / `radar_score_receipt` 0 / `radar_score_run` 0 / `session_blocks` 6 / `traders` 1 / `vault_overrides` 6 / `vault_writes` 184 — identical in both proofs, and both archiver tables still absent.

**§1.4 (f)'s rule is categorical and I am applying it as written:** "a moved digest on ANY other table → `.env` cleanup, then `FAILED: gate — cobalt_dev digest moved on <table>: <before> → <after>`", with the ONLY exception being the two archiver tables — which do not exist in this stack. The clause was narrowed deliberately by round-1 finding 20, and it explicitly forecloses the one benign class it names. **This is not mine to widen at runtime on a production deploy**, so the gate stops here. What I can hand the desk is the evidence, below.

**(g)** `rm …/stack-deploy2-0919/.env` → `ls -la` → **"No such file or directory"**. Removed and proven gone BEFORE this stop line was written. Then `cd /Users/cobalt/cobalt` → `ls -la .env` lists the file (cwd is back; contents never read, never `cat`ed, never `grep`ed).

**(h)** Not applicable as a cleanup: (c) and (d) were both GREEN, and (f) was already run in full. Belt-and-braces proof stands — `cobalt_dev` is at **0001–0009**, the state it started in, with the single `cobalt_redactions` row noted.

### 1.5 / 1.6

`git -C /Users/cobalt/cobalt log --oneline -1` = **`2a71ace`** = `<main-at-gate>` — nobody committed while the gate ran. Clock at the stop: `Sat Sep 19 14:53:53 EDT 2026`.

**Gate completeness:** every step 1.1 → 1.4 (g) was RUN; none was skipped. Both suite lines are quoted verbatim, all three `--proof-only` verdicts are above (baseline / post-forward / post-rollback), the forward table's verdict line is quoted, the `.env` proof is recorded at both ends, and P15's archiver line is quoted with `db 161/0` in PREFLIGHT. The gate is **NOT GREEN** on one term only: (f)'s digest comparison.

## Production state at the stop — verified, not assumed

| check | value |
|---|---|
| main tip | **`2a71ace`**, unchanged since my second-run PREFLIGHT commit. **Nothing was merged** |
| `com.cobalt.aset` | never booted out — `state = running`, **pid 72854**, as P9 recorded |
| `com.cobalt.radar` | never booted out — `state = running`, **pid 72867**, as P9 recorded |
| `com.cobalt.replay` | never bootstrapped — still `Could not find service` |
| migrations | **none applied to production.** `cobalt_brain` is at 0001–0007; 0008/0009 are NOT in it. The only `--allow-prod` command typed all run was P13's `--proof-only`, which applies nothing |
| `trader_settings` | **untouched.** No settings command exists in this session's allowlist; cards remain LIVE exactly as R20 left them at 12:30:01 ET |
| `cobalt_dev` | 0001–0009, `archive_progress`/`archive_incidents` absent, `cobalt_redactions` at 128 rows (was 127 at the gate's own baseline) |
| gate worktree | clean at `e5c0695`, **no `.env`** |

**§8 was NOT entered, and correctly so:** it is triggered by a red at 4.3–4.6, a red smoke, a failed resident bootstrap, or a mid-run denial *after* 4.2. None happened. Nothing is merged and no resident is down — the safe state is the one production never left.

## ESCALATE (second run)

1. **THE STOP, and the one-line fold that clears it: `cobalt_redactions` gains exactly one row when the with-DB suite runs, and §1.4 (b)'s baseline is taken BEFORE that suite.** The movement is provably internal to this gate, not foreign: **(c)'s forward table printed `cobalt_redactions 127 -> 127 … OK`**, so the row appeared during **(d)**, the suite itself — the only step between (c) and (f). The P4 DB run documented the identical footprint in its own stop line: *"cobalt_dev: 0001–0009, unchanged (only `cobalt_redactions` +1, the ordinary suite round-trip row; every other row count and digest byte-identical, bars 1,043,443)"* — the same table, the same +1, accepted there. **As written, §1.4 (f) can never pass**, because the suite always writes that row between the two proofs it compares. The desk's fix is one line, and it is the desk's to take, not mine: either (i) name `cobalt_redactions` beside the two archiver tables as the accepted suite-footprint exception, or (ii) move the comparison's baseline to a proof taken AFTER (d). P14 held at preflight — no other lane was holding `cobalt_dev` — so the "another lane" class the clause forecloses is not what happened here.
2. **Everything L68 actually exists to prove is GREEN on the stacked tree.** `stack/deploy2-0919` = main + `sprint-2/p4` + `ops/2026-09-19` at **`e5c0695`**: both merges clean at the same file counts run 1 measured (163 / 13), the merged registry is `0001…0009` with 0010/0011 absent, **offline `1875 passed, 0 failed`**, **with-DB `2198 passed, 0 failed`**, the forward migrate created nothing and changed nothing, and the two suite totals reconcile at 2204 both ways. R31's narrowed scope is vindicated: P4 and ops have no seam.
3. **GATE ARTIFACTS ARE ON DISK AND WILL BLOCK THE NEXT RELAUNCH — the desk owns their removal, again.** Branch **`stack/deploy2-0919`** and worktree **`/Users/cobalt/cobalt-wt/stack-deploy2-0919`**, left at **`e5c0695`** (P4 + ops merged, no `.env`). No `worktree remove` / `branch -D` rule is in this session's allowlist by design, so I cannot clean them, and **§1.2's `worktree add` will fail until they are gone.** It also now carries a built `.venv` from the two suite runs.
4. **P7's table was TRUNCATED by the tool this run (~13 KB from the middle), so `ops/com.cobalt.replay.plist`'s classifier row was not read by this session.** Run 1 read it as `plist in diff  -`; I am not restating that as my own observation. `configs/cobalt/smoke/s2.yaml  A  UNCLASSIFIED CONFIG` and the `RESTARTS:` fan-out to all six residents WERE read and are quoted in PREFLIGHT. Unchanged in disposition (recorded, not judged; §5.1 is the gate) — recorded because a future prompt may want the derivation written to a file rather than read from a tool result.
5. **Production reads worth keeping.** (a) P13's proof moved `cobalt_jobs`' digest (`324c22d0…` → `bb072e56…`) at an unchanged 15 rows in the ~15 minutes since run 1's table — the jobs registry updating its own rows as one-shots run (the 14:38 heartbeat sits in that gap). `vault_writes` steady at 1328. Both are tables 0008/0009 do not touch. (b) Production outage budget re-measured: **47.9 s × 2 ≈ 96 s**, in line with deploy 1's 94.1 s. (c) The two untracked `31-packet/*.yaml` copies are still untracked and were never `git add`ed, as instructed.
6. **`db migrate --rollback --down-to <n>` exits 1 when nothing is above `<n>`** (`FAILED: MigrationError: no registered migrations are newer than 0009`). Correct fail-loud behaviour and the predicted "nothing applied" outcome, but §1.4 (f) is worded as though it were a quiet no-op, so a future hub could read the non-zero exit as a failure. Worth one word in the prompt.
7. **Carried P4 escalates still owed a desk ruling, unchanged by this run:** **A2** (deadline scope), **A7 / K3** (the frozen-HOLD false positive), **ESCALATE 19** (L32 ticker pinning in test code).

FAILED: gate — cobalt_dev digest moved on cobalt_redactions: 127 rows / 0f80638def1d0d8e46457a59b2771fe7 → 128 rows / a93ce24ff053320346d529539a22be59 (the with-DB suite's own round-trip row, written between (b) and (f); both stacked suites GREEN — offline 1875/0, db 2198/0, stack e5c0695) — safe state: green — residents: up

---

# THIRD RUN

Hub `deploy-p4-0919` (Opus 5, `claude-opus-5`), background session, cwd `/Users/cobalt/cobalt`.
Prompt: the SAME file, refolded again for the run-3 `(d2)` rule — `docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md`, run-3 sha256 `0b2516e6c7b9fe10c1fd046b9ae3034948ed623b0ca619b1ceb5ddd85a51fbad`.
Authorization: `cto-2026-09-19.md` §4 **R30** (14:23 ET), **R31** (14:4x ET, "A") and the **R31 (run 3)** desk-fold row (14:5x ET, which carries the run-3 sha) + `cto-2026-09-18.md` §4 R3/R5/R6/R18–R21 + `cto-2026-09-19.md` §4 R16/R19/R23.
Runs 1 and 2 stand above, unedited. Run 1 stopped at `FAILED: gate — conflict merging archiver/append-0919`; run 2 reached BOTH stacked suites GREEN and then FALSE-STOPPED at §1.4 (f) on the suite's own `cobalt_redactions` round-trip row. Nothing above this line is rewritten.

### §0 Headline (third run)

- **P4 DEPLOY DONE. Main `5b58b7b`, tagged `deploy-2026-09-19b`, smoke GREEN, both residents UP on new pids, outage 2 min 33 s.** §8 was never entered.
- **L68 gate GREEN on every term** before anything merged: stack `317a691` (main + `sprint-2/p4` + `ops/2026-09-19`, R31's narrow scope), offline **1875/0**, with-DB **2198/0**, registry `0001…0009` with 0010/0011 absent. The run-3 `(d2)` post-suite proof cleared run 2's false stop: `cobalt_redactions` moved 128 → 129 during the SUITE, and nothing at all moved across the restore.
- **Migrations 0008/0009 APPLIED** — 26 tables, **0 `CHANGED`**, exactly **3 `CREATED`** (`system.movers_daily`, `"user".picks`, `"user".missed`), `radar_membership` `OK` with its two new columns; 105.9 s of proof.
- **`com.cobalt.replay` bootstrapped and armed** (Mon–Fri 21:10, `RunAtLoad false`) — runs nothing tonight; first run **Monday 2026-09-21 21:10 ET**. **Cards LIVE and untouched** (read back `True`); no settings command exists in this session.
- PREFLIGHT 16 rows, **zero denials**; heartbeat GREEN before and after, RED-entry count **93 → 93**. ESCALATE: **9** (none blocks anything shipped).

### Authorization, verified before anything ran

| what | where | verdict |
|---|---|---|
| the row that names THIS file | `cto-2026-09-19.md` §4 **R30**, 14:23 ET, "Approve" | PRESENT — names `docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md`, approves its launch line VERBATIM (55 `Bash(` rules, 32 byte-identical to R7's, 23 differing) |
| **R31** | §4 **R31**, 14:4x ET, "A" | PRESENT — narrows the L68 gate to `sprint-2/p4` + `ops/2026-09-19` (archiver NOT stacked); states **"launch line and all 55 rules UNCHANGED"** |
| **R31 (run 3)** — the row that carries THIS run's hash | §4 row `R31 (run 3)`, 14:5x ET | PRESENT — a desk fold under R31, no new ruling asked of him: the `(d2)` post-suite proof becomes (f)'s comparison basis, and the empty rollback's `exit 1` is stated as the EXPECTED outcome. **"No rule string changed; the launch line is untouched."** |
| the file is the approved text | that row's applied column: **run-3 sha256 `0b2516e6…1fbad` — "this is the hash the hub verifies from here"** (superseding R30's run-1 `0a65607c…e5fd` and R31's run-2 `0174f9ae…cb22e` in writing) | `shasum -a 256` of the prompt = **`0b2516e6c7b9fe10c1fd046b9ae3034948ed623b0ca619b1ceb5ddd85a51fbad`** — **EQUAL** |
| the rows are committed on main | `git log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` | **`d304d2a`** (R31 run-3 row + sha) under `5204535` (R32), over `74a0555`, `bfe9849`, `11bf4c2`, `f1780f5`, `4e4e901`. `git status` shows `cto-2026-09-19.md` **NOT modified**, so the rows read ARE the committed text |
| R30's conditions | the row itself | start ≤ 18:30 ET today — **MET at 14:59** · tag `deploy-2026-09-19b` · RESTARTS aset + radar + first bootstrap of `com.cobalt.replay` · **no settings write of any kind** · never push, never `bypassPermissions`, never a production schema rollback |
| not this deploy's approval | R7 (07:49 ET) approved `02-deploy-stack-3.md` | acknowledged — NOT read as an approval of this file |
| read but not acted on | **R32** (`5204535`, "A" — L43 overruled for tonight: the archiver may be a SECOND production deploy if its lane is green before 21:00 ET) | recorded. It concerns deploy 3, not this file; nothing in it changes this run's scope, and `archiver/append-0919` is neither stacked nor merged here |

No mismatch anywhere.

## PREFLIGHT (third run)

Ran 14:58–15:01 ET from `/Users/cobalt/cobalt`. **Zero denials — every allowlisted shape probed.** Every command was RE-RUN; nothing was carried over from runs 1 or 2.

| rule | command | exit | allowed / DENIED |
|---|---|---|---|
| P1 | `git -C /Users/cobalt/cobalt status --porcelain` and `git … status` | 0 / 0 | allowed — ` M docs/40 - DevDocs/reports/seat-usage.md`, `?? …/31-packet/p2-dark-settings.yaml`, `?? …/31-packet/p2-live-settings.yaml`. LONG form: `On branch main`, ahead of origin/main by **80 commits**, **no rebase in progress, no unmerged paths**. `cto-2026-09-19.md` NOT dirty |
| P2 | `git … tag scratch-allow-probe-p4-0919` then `tag -d …` | 0 / 0 | allowed — `Deleted tag 'scratch-allow-probe-p4-0919' (was 5204535)` |
| P3 | `git … commit --allow-empty` then `reset --soft HEAD~1` | 0 / 0 | allowed — `[main 65ce22c]` created, then `log --oneline -1` → **`5204535`** — HEAD is back |
| P4 | `date` | 0 | allowed — **`Sat Sep 19 14:59:17 EDT 2026`** — 14:59 ET ≤ 18:30. Read for the record; enforced at §2.1 |
| P5 | `COBALT_ENV=production uv run cobalt validate` | **0** | allowed — 13 trade_defs OK; **`Jobs (F17): 15 registered — 6 resident, 9 one-shot.`** (the pre-merge state; `com.cobalt.replay` not there yet); `registry <-> ops/: 15 label(s), exact match`; `registry <-> plists: schedules and COBALT_ENV agree on every job`; `Step-downs: … — 7 row(s)`; `Placement (docs/PLACEMENT.md): tree clean` |
| P6 | `COBALT_ENV=production uv run cobalt backup status` | 0 | allowed — `ssd local ARMED`, `newest snapshot: 6.7 h old` |
| P6 | `COBALT_ENV=production uv run cobalt backup run --dry-run` | 0 | allowed — `backup: cobalt_brain via pg_dump inside cobalt_memory — 645.2 MB`; `DRY RUN — nothing written, no jobs row touched.` |
| P7 | `COBALT_ENV=production uv run cobalt jobs restarts main..sprint-2/p4` | **1** | allowed — see the P7 note below. EXPECTED class: main's OLD classifier |
| P8 | `COBALT_ENV=production uv run cobalt heartbeat show` | 0 | allowed — **`HEARTBEAT GREEN — 15 job(s), 12 probe(s), nothing red  (2026-09-19 14:59:36 EDT)`** |
| P9 | `launchctl print gui/501/com.cobalt.aset` · `…/com.cobalt.radar` | 0 / 0 | allowed — both `state = running`; **aset pid 72854**, `path = /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` · **radar pid 72867**, `path = /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`. Both paths are the expected ones — no plist moved. Same pids runs 1 and 2 recorded: nothing restarted in between |
| P9 | `launchctl print gui/501/com.cobalt.replay` | **113** | allowed — **`Could not find service "com.cobalt.replay" in domain for user gui: 501`**. Correct: it does not exist yet, nobody bootstrapped it |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` | 0 | allowed — **`200`** |
| P10 | `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` | 0 | allowed — **`200`** |
| P11 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(…) … HAVING count(*) > 1"` | 0 | allowed — header only (`tablename	array_agg`), **ZERO rows**. No duplicate table name; 4.5 cannot hit the harness's ambiguity refusal |
| P12 | `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN ('movers_daily','picks','missed')"` | 0 | allowed — header only, **ZERO rows**. None of 0008/0009's three new names exists anywhere in production |
| P13 | `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` (`timeout` 600000) | **0** | allowed — proof table below. **No `CHANGED`, no tool timeout, nothing applied** |
| P14 | `ls -la <path>/.env` × 4 (`s2-p4`, `ops-2026-09-19`, `archiver-append`, `stack-deploy2-0919`) | 1 × 4 | allowed — **every one "No such file or directory"**. No other session holds `cobalt_dev` |
| P15 | archiver precondition — RECORDED, no longer a gate (R31) | — | recorded — see below |
| P16 | ops + P4 branch stop lines | — | **both `0` failed** — see below |

**P7 — recorded, not judged. Exit 1, ONE UNCLASSIFIED path, identical in class to runs 1 and 2 and to deploy 1's P7.** Verbatim, the rows that matter:

```
FAILED: RestartError: one or more changed paths were unclassified
path	change	rule	restart
configs/cobalt/jobs.yaml	M	registry; register, no restart	-
configs/cobalt/radar.yaml	M	resident reads	com.cobalt.radar
configs/cobalt/smoke/s2.yaml	A	UNCLASSIFIED CONFIG	com.cobalt.agent,com.cobalt.aset,com.cobalt.herdr,com.cobalt.mainframe,com.cobalt.obsidian,com.cobalt.radar
ESCALATE: unclassified path configs/cobalt/smoke/s2.yaml
configs/cobalt/taxonomy/tunables.yaml	M	resident reads	com.cobalt.aset,com.cobalt.radar
…
RESTARTS: com.cobalt.agent com.cobalt.aset com.cobalt.herdr com.cobalt.mainframe com.cobalt.obsidian com.cobalt.radar
```

`configs/cobalt/smoke/s2.yaml` is a NEW config file arriving WITH P4; main's classifier has no rule for it, so it fans out to all six residents and raises the error. Everything else classified cleanly (`DOCS -`, `resident reads`, `static import reach`, `test/documentation; no resident`, `registry; register, no restart`). **§5.1 re-runs `jobs restarts <pre-merge>..HEAD` on the MERGED classifier; that run is the gate, and this row is not a stop here.**

**HONEST LIMIT on P7, the same as run 2's:** the tool truncated ≈13 KB from the MIDDLE of this table, so the `ops/com.cobalt.replay.plist` row was **not read by this session**. Run 1 read it as `plist in diff  -`; I am not restating that as my own observation. What I did read is quoted above and in the tail (`src/…` static-import-reach rows and the full `tests/…` block). Disposition unchanged: recorded, not judged; §5.1 is its gate.

**P8 — NO BASELINE RED TO RECORD.** Every probe `OK`; the only non-OK line is `AMB  com.cobalt.herdr  unmanaged  AMBER launchd unmanaged — loaded, not running (last exit 0) [launchd probe]; runs outside launchd by declared interim`, the standing AMBER, not a RED. R6's accepted-RED gate is therefore not needed; the smoke's rule at 6.1 becomes "no RED at all". `radar` probe reads `idle (overnight)` and `com.cobalt.radar running 398 min, heartbeat fresh` — Saturday is outside every scanning session, so `idle:overnight` is the correct shape. `sheet daymode … cards are accepted`.

**P13 — the production proof, exit 0, nothing applied.** Gate checked term by term: **exit 0**, **no table marked `CHANGED`**, **no tool timeout**. Per round-1 finding 14 this probe runs from `/Users/cobalt/cobalt` = main's code, whose static list is 23 tables; `movers_daily`/`picks`/`missed` are not in its world, and their absence is proven by P12, not here.

```
cobalt db migrate — PROOF ONLY on cobalt_brain (READ ONLY, nothing applied)

table                side    schema   rows         digest                             secs
------------------------------------------------------------------------------------------
aset_sizings         user    user     146          baac6ac19fd231057ebe2cbe7443c933   0.03
bars                 system  system   8834532      d21fc0b31265b2565c71432a6ebf6979   46.89
card_dot_taps        user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_dots            user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
card_stop_edits      user    user     1            cbae670bbd7acd13749ac41d99f25861   0.00
card_transitions     user    user     293          3986eb3f96571498667bbfbc3d80aa8c   0.00
cobalt_email_sends   system  system   463          c8fc112af72900bb260196353fea82bd   0.00
cobalt_jobs          system  system   15           debc3a079c76ad85692328078ebf2af0   0.00
cobalt_kill_switch   system  system   1            310655940a3a0ce10031f77a390733b6   0.00
cobalt_redactions    system  system   2            cf42d85ee49244ca40d419683eb3ca51   0.00
day_modes            user    user     10           d9139ce7290afb41d88404c32c59736b   0.00
desk_grade           system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_packet          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
desk_regime          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_membership     system  system   3334         72b94dfe689084e8e96cd880d031e3f3   0.04
radar_pool           system  system   1            88537229826d4da833be389abd8367be   0.00
radar_score          system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_receipt  user    user     0            d41d8cd98f00b204e9800998ecf8427e   0.00
radar_score_run      system  system   0            d41d8cd98f00b204e9800998ecf8427e   0.00
session_blocks       system  system   8            ddf2a773f48403300cbff196fe9aa419   0.00
traders              user    user     1            cf9aec4d006d82fb0f12be2d9afa10b9   0.00
vault_overrides      user    user     29           1f529ee789a2e27f1b73010bbae4cdd7   0.00
vault_writes         user    user     1328         d4dd2c3365b291f7388b4c1fd6519c51   0.13
------------------------------------------------------------------------------------------
23 table(s) probed on cobalt_brain; digest excludes user_id, vault_outcome, vault_reason, account_mode, pool_member_id; aset_sizings: 25 card column(s) added by 0007. Proof cost: total 47.1 s — and a migration pays it TWICE (before and after), inside the outage.
NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.
```

**This deploy's outage budget: 47.1 s × 2 ≈ 94 s of proof**, in line with deploy 1's measured 94.1 s, run 1's 47.5 s and run 2's 47.9 s. Movement since run 2's table ~15 minutes ago, recorded not judged: **`cobalt_jobs`' digest moved `bb072e56…` → `debc3a07…` at an unchanged 15 rows** — the same benign class run 2 recorded (the jobs registry updates its own rows as one-shots run; the 14:53 heartbeat sits in that gap). `vault_writes` steady at 1328, `radar_membership` at 3334. Both moving tables are ones 0008/0009 do not touch.

**P15 — THE ARCHIVER PRECONDITION: RECORDED, NOT A GATE (R31).** Quoted from `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`, last line, the part that matters:

> `ARCHIVER DB2 f5df6c1 (report tip; NO code commit this run — the code tip is unchanged at round 3b's ecb817c…) | offline 1871/0 (319 skipped…) | db 161/0 (the 22 archiver requires_db tests + the three files' 139 offline tests, run WHILE 0010/0011 were applied; zero failures) | DB-1 tests: PASSED/PASSED … | DB-2: CLOSED … | DB-3: CLOSED … | cobalt_dev: 0001–0009, unchanged …`

**`db 161/0` — zero failed**, the expected line, unchanged since runs 1 and 2 read it. Branch tip: `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` → **`99039f7`** `docs(report): archiver db re-run — the stop line (db 161/0, DB-1 PASSED/PASSED, cobalt_dev back at 0001-0009)` — the same tip both earlier runs found; the branch has not moved. Under R31 this branch is NOT stacked in tonight's gate, so neither fact gates anything here; both are recorded because the desk asked for them.

**P16 — the other two branches, recorded not judged.**

| branch | last line's counts | verdict |
|---|---|---|
| `ops/2026-09-19` (`ops-2026-09-19.md`) | `OPS 0919 DB be00506 \| code d860bf7 \| offline 1608/0 (302 skipped vs round-2 298; +4 named) \| db 61/0 (tests/cobalt/test_migrate_proof.py only) … \| cobalt_dev: 0001–0009, unchanged` | **0 failed** |
| `sprint-2/p4` (`s2-p4-verify-2026-09-19.md`, last line) | `P4 DB 9045260 … \| offline 1829/0 \| db 2147/0 (vs 2136/0 + 2 owed — +11 …) \| owed requires_db: …[p2_before_p4] → PASSED, [p4_before_p2] → PASSED \| cobalt_dev: 0001–0009, unchanged (only cobalt_redactions +1, the ordinary suite round-trip row…) \| DB DEBT: CLOSED` | **0 failed**, both owed tests PASSED |

Note for §1.4: that P4 DB stop line already documents `cobalt_redactions +1` as "the ordinary suite round-trip row" — the exact footprint run 2 stopped on and the run-3 `(d2)` fold now handles.

### 0.2 GATE

**GATE: proven — commit + tag allowed by the session allowlist.** P2 created and deleted a tag; P3 created and reset an empty commit; HEAD proven back at `5204535`. (L62's launch proof.)

### 0.3 Not preflighted

No harmless variant exists inside their own pattern (UNATTENDED-LAUNCH §6), so each is probed by its first real use: `worktree add`, the stack `merge --no-edit`, `rebase`, `bootout`, `bootstrap`, `kickstart`, `merge --ff-only`, `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.

CONTINUE: step 1

## L68 GATE (third run) — GREEN, every step run

Ran 15:01–15:10 ET. Under R31 the stacked tree is `sprint-2/p4` + `ops/2026-09-19`; `archiver/append-0919` is NOT stacked.

### 1.1 The four tips, as found

| ref | tip | vs run 2 / the facts packet |
|---|---|---|
| `main` (`<main-at-gate>`) | **`fde57c2`** `docs(desk): 09-19 R33 — 5 new rule strings approved for the deploy-3 rebase hub (48)` | run 2 had `2a71ace`; main has moved by **desk commits only** (`d304d2a`, `5204535`, `4d0b9a5`, `fde57c2`) — see ESCALATE 1 |
| `sprint-2/p4` | **`d9c51b4`** `docs(report): s2-p4 round-2 db run stop line — P4 DB 9045260 …` | UNCHANGED from runs 1 and 2 |
| `ops/2026-09-19` | **`a868a69`** `docs(report): ops 0919 db run …` | UNCHANGED from runs 1 and 2, MATCHES the packet |
| `archiver/append-0919` | **`99039f7`** (RECORDED ONLY — not stacked, R31) | UNCHANGED from runs 1 and 2 |

`git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` → **empty**. The `s2-p4` worktree is clean.

### 1.2 BUILD THE STACK — both merges CLEAN

| step | result |
|---|---|
| `worktree add -b stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919 main` | **SUCCEEDED** — `Preparing worktree (new branch 'stack/deploy2-0919')`, `HEAD is now at fde57c2`. **The desk removed run 2's artifacts** (ESCALATE 3 of the second run); the RERUN CASE branch was not taken |
| `merge --no-edit sprint-2/p4` | **CLEAN** — `Merge made by the 'ort' strategy.` **163 files, 46,336 insertions(+), 220 deletions(−)** — byte-for-byte the counts runs 1 and 2 got. Tip `19483cb` |
| `merge --no-edit ops/2026-09-19` | **CLEAN** — auto-merged `src/cobalt/cli.py`, `src/cobalt/db_migrations/cli.py`, `docs/40 - DevDocs/cobalt/db_migrations/cli.md`. **13 files, 3,351 insertions(+), 21 deletions(−)** — again the same counts. Tip **`317a691`** = `<stack>` |
| archiver merge | **NOT RUN — removed by R31**, not skipped silently. Run 1's 9-file conflict stands recorded above as deploy 3's seam (and is now `48`'s rebase job, R32/R33) |

`git … log --oneline -6` on the stack: `317a691` (merge ops) over `19483cb` (merge P4) over `fde57c2` over `4d0b9a5` over `5204535` over `d304d2a`.

**MERGED MIGRATION REGISTRY — GATE PASSED.** `grep -n "0008\|0009\|0010\|0011" src/cobalt/db_migrations/__init__.py` plus the full tuples read at `:49-71`:

```
FORWARD = (0001_schemas, 0002_move_tables, 0003_heartbeat_vault_outcome, 0004_radar_pool,
           0005_heartbeat_note_absent, 0006_radar_score, 0007_radar_cards,
           0008_radar_value_movers, 0009_picks_missed)
REVERSE = (0009…rollback, 0008…rollback, 0007…rollback, 0006…rollback, 0005…rollback,
           0004…rollback, 0003…rollback, 0002…rollback)      # 0001 deliberately NOT reversed
```

**0001…0009 in numeric order, `REVERSE` its mirror, and 0010/0011 ABSENT** — exactly what R31's narrowed stack requires.

### 1.3 OFFLINE SUITE ON THE STACK — GREEN

`ls -la …/stack-deploy2-0919/.env` → "No such file or directory" (proven offline) → `cd` → `uv run pytest -q tests/cobalt tests/taxonomy`, exit 0. Summary line verbatim:

```
1875 passed, 329 skipped, 1 xfailed, 15 warnings in 60.92s (0:01:00)
```

**`0 failed` — GATE MET.** (Context only, never the bar: P4 alone 1829/0, ops-0919 1608/0. Identical to run 2's stack numbers.)

### 1.4 WITH-DB SUITE ON THE STACK — GREEN, ALL OF (a)–(h) RUN

**(a)** `cp /Users/cobalt/cobalt/.env …/stack-deploy2-0919/.env` (by name, never read, never printed) → `ls -la` printed `-rw------- 1 cobalt staff 2186 Sep 19 15:04 .env`.

**(b) BASELINE** — `COBALT_ENV=dev uv run cobalt db migrate --proof-only`, exit 0, **26 tables on `cobalt_dev`**, `code: 317a691 (clean)`. `cobalt_dev` at **0001–0009**: P4's `movers_daily` / `picks` / `missed` PRESENT at 0 rows; the archiver's `archive_progress` / `archive_incidents` **ABSENT — not even enumerated**, because the stacked registry stops at 0009 and the static table list is this stack's own 26. No `CHANGED`. Full row counts in the comparison table below.

**(c) FORWARD** — `COBALT_ENV=dev uv run cobalt db migrate`, exit 0. All nine walked (`-- applying 0001…0009`), **NOTHING CREATED**, every table `OK`, every digest unchanged, closing line `content UNCHANGED on every table.` and `proof cost: BEFORE 6.4 s + AFTER 6.2 s = total 12.7 s; slowest table bars (6.4 s before).` No `CHANGED`. `cobalt_redactions` read **128 -> 128, `a93ce24f -> a93ce24f  OK`** here — it had not moved yet, which is what makes the movement below provably the suite's.

**(d) SUITE** — `cd` + `ls -la .env` (printed, as the with-DB step requires) → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`, exit 0. Summary line verbatim:

```
2198 passed, 6 skipped, 1 xfailed, 15 warnings in 200.30s (0:03:20)
```

**`0 failed` — GATE MET.** Reconciles exactly: offline `1875 + 329 = 2204` and with-DB `2198 + 6 = 2204`, both ways — so every `requires_db` test genuinely RAN rather than silently skipping.

**(d2) POST-SUITE PROOF — the run-3 addition, and (f)'s comparison basis.** `COBALT_ENV=dev uv run cobalt db migrate --proof-only`, exit 0, 26 tables. Compared row for row against (b): **25 of 26 byte-identical in row count AND digest. ONE moved, and it is the suite's own:**

| table | (b) baseline | (c) forward | **(d2) post-suite** | (f) closing proof |
|---|---|---|---|---|
| `cobalt_redactions` | 128 rows, `a93ce24ff053320346d529539a22be59` | 128 → 128, `a93ce24f -> a93ce24f  OK` | **129 rows, `9b686308cea3badb9483869c73cfd43e`** | 129 rows, `9b686308cea3badb9483869c73cfd43e` |

Every other table — `aset_sizings` 1 (`0824685c`) / `bars` 1043443 (`2769919a`) / `card_dot_taps` 0 / `card_dots` 0 / `card_stop_edits` 1 (`7599f9ab`) / `card_transitions` 4 (`f181e76b`) / `cobalt_email_sends` 2 (`fba8cf9f`) / `cobalt_jobs` 13 (`8d9b0861`) / `cobalt_kill_switch` 1 (`2e590e87`) / `day_modes` 2 (`f2ffb4d4`) / `desk_grade` 0 / `desk_packet` 0 / `desk_regime` 0 / `missed` 0 / `movers_daily` 0 / `picks` 0 / `radar_membership` 0 / `radar_pool` 0 / `radar_score` 0 / `radar_score_receipt` 0 / `radar_score_run` 0 / `session_blocks` 6 (`b650702d`) / `traders` 1 (`a64e0148`) / `vault_overrides` 6 (`6a8b0520`) / `vault_writes` 184 (`4a965c69`) — **identical across (b), (d2) and (f)**, and both archiver tables absent throughout.

**(d2)'s own gate, checked as written:** no table PRESENT at (b) is missing here, and no table appeared that a stacked migration does not create. The `cobalt_redactions` +1 is RECORDED, named, and is **not** a stop — it is the with-DB suite's round-trip row, written between (c) and (d2), exactly as the P4 DB run's own stop line documents it (`only cobalt_redactions +1, the ordinary suite round-trip row`) and exactly what run 2 false-stopped on.

**(e) END STATE, stated plainly before the restore:** `cobalt_dev holds after the suite: 0001–0009, archiver tables ABSENT` — **the gate added nothing this time (R31)**, which is itself the thing to say plainly. The only change the gate made to `cobalt_dev` is its own suite's one `cobalt_redactions` row.

**(f) RESTORE — reversed nothing, exit 1, and the comparison is CLEAN.**

`COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → **exit 1**, verbatim:

```
FAILED: MigrationError: no registered migrations are newer than 0009
```

**This is the EXPECTED outcome** the run-3 fold states (run 2 measured it): the harness is fail-loud about an empty rollback rather than quietly succeeding. **NOTHING was applied and nothing numbered ≤ 0009 was reversed.** Not a gate failure.

Closing `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → exit 0, 26 tables, **IDENTICAL to (d2) row for row — all 26 tables, every row count and every digest, `cobalt_redactions` included at 129 / `9b686308…`.** The restore acted on (d2)'s state and changed nothing, which is exactly what an empty rollback must do. **No digest moved across the restore → (f) GREEN.**

For the record, as (f) requires: the difference between (b) and (d2) is the single `cobalt_redactions` row above, caused by the suite, never a stop. P14 held at preflight — no other lane was holding `cobalt_dev` — so the foreign-lane class the clause forecloses is excluded by preflight, not by this comparison.

**(g)** `rm …/stack-deploy2-0919/.env` → `ls -la` → **"No such file or directory"**. Removed and proven gone. Then `cd /Users/cobalt/cobalt` → `ls -la .env` lists the file (cwd is back; contents never read, never `cat`ed, never `grep`ed).

**(h) RED-BRANCH RESTORE — not triggered, and said plainly:** (c) and (d) were both GREEN, so (h)'s red path was never entered. Its belt-and-braces claim is nevertheless proven by (f), which ran in full: with the archiver leg gone the stack has no migration above 0009, the rollback found nothing to reverse, and `cobalt_dev` is at **0001–0009** — the state it started in.

### 1.5 / 1.6

`git -C /Users/cobalt/cobalt log --oneline -1` = **`fde57c2`** = `<main-at-gate>` — nobody committed while the gate itself ran (the desk's four commits all landed during PREFLIGHT, before 1.1).

**Gate completeness — every step 1.1 → 1.4 (h) was RUN, none skipped.** Both suite lines are quoted verbatim; all FOUR `--proof-only` verdicts are above (baseline (b) / post-forward (c) table / post-suite (d2) / post-rollback (f)); the forward table's verdict line is quoted; the `.env` proof is recorded at both ends; P15's archiver line is quoted with `db 161/0` in PREFLIGHT. **The gate is GREEN on every term.**

**GATE ARTIFACTS, cleanup owed (ESCALATE):** branch **`stack/deploy2-0919`** and worktree **`/Users/cobalt/cobalt-wt/stack-deploy2-0919`**, left at **`317a691`** with a built `.venv` and no `.env`. They are never merged, never pushed, never rebased. No `worktree remove` / `branch -D` rule is in this session's allowlist by design, so the desk removes them — and must, before any further `worktree add` of that name.

CONTINUE: step 2

## Preconditions (third run, 15:10 ET)

| check | result |
|---|---|
| 2.1 WINDOW | `date` → **`Sat Sep 19 15:10:36 EDT 2026`** — **15:10 ET ≤ 18:30**, the run may start. Saturday, not a trading day; radar reads `idle:overnight` at every hour, so L43's trading-day restart window does not bind and L66's own requirement is met by §4. The 18:30 cap is margin against the 21:40 nightly backup (all seven days); `com.cobalt.archiver` (20:30) and `com.cobalt.replay` (21:10) are Mon–Fri and cannot fire tonight |
| 2.2 (a) P4 DB gate | `s2-p4-verify-2026-09-19.md` last line: `P4 DB 9045260 … \| offline 1829/0 \| db 2147/0 … \| owed requires_db: test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4] → PASSED, [p4_before_p2] → PASSED \| cobalt_dev: 0001–0009, unchanged … \| DB DEBT: CLOSED`. **Both owed tests PASSED, 0 failed — MET** |
| 2.2 (b) L67 house check | `cto-2026-09-19.md` §52 launched `review-p4-folds-0919`; §54 and §56 record its results. §56 verbatim: `P4 FOLDS GROK 13:2x: grok: 0/0/0 FOLD CLEAN (both folds) · houses that have read both folds (with 36's Gemini): 2 of 3 · hub-verified REAL: 2 (blocks shipping: 0; both known — L32 tickers in test code ESCALATE 19, cut_cards_day wiring MINOR) · UNVERIFIABLE: 4`, and **"P4's BUILD IS CHECKED (whole build 3 of 3 at `a59ee8c`; both later folds 2 of 3, Astra METER → L67's meter floor)"**. **`blocks shipping: 0` — MET** |
| 2.3 branch | `git -C …/s2-p4 log -1 --format=%D` → **`HEAD -> sprint-2/p4`** |
| 2.3 clean | `status --porcelain` → **empty** |
| 2.3 byte-identity | `<gated>` = **`9045260`**, the code tip both §2.2 gates name. `diff --stat 9045260 HEAD -- . ':(exclude)docs'` → **printed NOTHING**. Only report commits sit above the gated code |
| 2.3 `<n>` | `rev-list --count main..HEAD` → **58** |
| 2.4 no other merger | `log --oneline -3` → `d2f0013` (my own gate commit) over `fde57c2` / `4d0b9a5` (the desk's deploy-3 docs). All docs commits; **no build or deploy session is merging** |
| 2.4 merge-base | `merge-base main sprint-2/p4` → **`919d562`** (the packet's number), and `diff --stat 919d562 main -- . ':(exclude)docs'` → **printed NOTHING** — main has moved by DOCS ONLY since P4 branched, re-proven after the desk's four commits. 4.1's rebase cannot change code; its own byte-identity proof remains the real gate |

## Deploy table (third run)

**Step 3 — machine-written files, snapshot, tag (15:10–15:11 ET)**

| step | fact |
|---|---|
| 3.1 dirty tree | ` M docs/40 - DevDocs/reports/seat-usage.md` plus the two untracked `31-packet/*.yaml` — **exactly the expected set**; `cto-2026-09-19.md` was NOT dirty (the desk had committed it). `seat-usage.md` `git add`ed **BY NAME**; the two `.yaml` copies (of files under gitignored `data/backups/`) were **NOT `git add`ed** and remain untracked — ESCALATE |
| 3.1 commit | **`59dd819`** `chore: machine-written + report files, committed as-is (L51-2)` — 1 file, +9/−8 |
| 3.2 snapshot | `ssd: snapshot 2139fe9a — 1 new / 8 changed, 15.0 MB added, 1 pruned`; dump line `backup: cobalt_brain via pg_dump inside cobalt_memory — 645.2 MB`. `backup status` → `newest snapshot: 0.0 h old` ✓ |
| 3.2 tag | **`pre-p4-0919`** created at `59dd819` |
| 3.2 rollback directory | **NONE, by design** — no settings file is read or written in this deploy, so there is nothing to stage |

Known and carried (ops ESCALATE 4): snapshot `2139fe9a` (15:11 ET) is pruned by tonight's 21:40 nightly (`--keep-daily 14`). **The durable rollback point is the tag `pre-p4-0919`.**

CONTINUE: step 4

**Step 4 — the outage window (15:11:51 → 15:14:24 ET, 2 min 33 s)**

| step | fact |
|---|---|
| 4.1 rebase | `git -C …/s2-p4 rebase main` → `Successfully rebased and updated refs/heads/sprint-2/p4` (58/58, no conflict). **PROOF:** `diff --stat 9045260 HEAD -- . ':(exclude)docs'` **printed NOTHING** — what was merged is byte-identical to what §1 gated and §2.2 proved. The named `configs/cobalt/rules.yaml` exception was NOT needed (that file was never dirty today). `rev-list --count main..HEAD` → **58** = `<n>` of 2.3 ✓ |
| 4.1 `<pre-merge>` | **`866819f`** (my own step-3.3 report commit) |
| 4.2 baseline | `grep -c "ENTERED RED" logs/heartbeat.log` → **93**. Clock re-check `Sat Sep 19 15:11:51 EDT 2026` — 15:11 ET, far inside the window; the outage opened here |
| 4.2 residents DOWN | `bootout` aset, then radar. Both `launchctl print` then exit **113** `Could not find service … in domain for user gui: 501` — the proof, first attempt, no retry needed. The 15:08 and 15:23 beats sit outside the window, so **no beat landed inside the outage** |
| 4.3 no foreign commit | `rev-parse --short HEAD` = **`866819f`** ✓ (the desk's four commits all landed during PREFLIGHT; nothing committed between the rebase and the merge) |
| 4.3 merge | `merge --ff-only sprint-2/p4` → `Updating 866819f..5b58b7b`, fast-forward, **163 files, 46,336 insertions(+), 220 deletions(−)** — the same counts the gate measured. Tip **`5b58b7b`**. **POINT OF NO RETURN passed: from here the run finishes forward** |
| 4.4 | nothing — no settings dry-run in this deploy; the `radar.yaml` config-shape change rode in with 4.3 |
| 4.5 migration | **GREEN, exit 0** — the proof table below |
| 4.6 validate | **exit 0** — `Jobs (F17): 16 registered — 6 resident, 10 one-shot.`, `registry <-> ops/: 16 label(s), exact match.`, `registry <-> plists: schedules and COBALT_ENV agree on every job.`, `Placement (docs/PLACEMENT.md): tree clean`. 13 trade_defs OK; `61 engine tunable(s)` (was 60); step-downs still 7 rows. **Already 16 at this point** — the merged `configs/cobalt/jobs.yaml` carries `com.cobalt.replay`, so validate saw it before 5.1 re-registered |

**4.5 — THE MIGRATION, exit 0.** All nine FORWARD files walked (`-- applying 0001…0009`); 0001–0007 re-ran idempotently by design. Proof table verbatim:

```
table                side    schema before -> after     rows            probe secs      digest before -> after verdict
----------------------------------------------------------------------------------------------------------------------
aset_sizings         user    user -> user               146 -> 146      0.04 -> 0.01    baac6ac1 -> baac6ac1  OK
bars                 system  system -> system           8834532 -> 8834532 52.75 -> 52.71  d21fc0b3 -> d21fc0b3  OK
card_dot_taps        user    user -> user               0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
card_dots            user    user -> user               0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
card_stop_edits      user    user -> user               1 -> 1          0.00 -> 0.00    cbae670b -> cbae670b  OK
card_transitions     user    user -> user               293 -> 293      0.01 -> 0.00    3986eb3f -> 3986eb3f  OK
cobalt_email_sends   system  system -> system           463 -> 463      0.00 -> 0.00    c8fc112a -> c8fc112a  OK
cobalt_jobs          system  system -> system           15 -> 15        0.00 -> 0.00    1713a30f -> 1713a30f  OK
cobalt_kill_switch   system  system -> system           1 -> 1          0.00 -> 0.00    31065594 -> 31065594  OK
cobalt_redactions    system  system -> system           2 -> 2          0.00 -> 0.00    cf42d85e -> cf42d85e  OK
day_modes            user    user -> user               10 -> 10        0.00 -> 0.00    d9139ce7 -> d9139ce7  OK
desk_grade           system  system -> system           0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
desk_packet          system  system -> system           0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
desk_regime          system  system -> system           0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
missed               user    - -> user                  - -> 0          0.00 -> 0.00    - -> d41d8cd9         CREATED
movers_daily         system  - -> system                - -> 0          0.00 -> 0.00    - -> d41d8cd9         CREATED
picks                user    - -> user                  - -> 0          0.00 -> 0.00    - -> d41d8cd9         CREATED
radar_membership     system  system -> system           3334 -> 3334    0.04 -> 0.05    72b94dfe -> 72b94dfe  OK
radar_pool           system  system -> system           1 -> 1          0.00 -> 0.00    88537229 -> 88537229  OK
radar_score          system  system -> system           0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
radar_score_receipt  user    user -> user               0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
radar_score_run      system  system -> system           0 -> 0          0.00 -> 0.00    d41d8cd9 -> d41d8cd9  OK
session_blocks       system  system -> system           8 -> 8          0.00 -> 0.00    ddf2a773 -> ddf2a773  OK
traders              user    user -> user               1 -> 1          0.00 -> 0.00    cf9aec4d -> cf9aec4d  OK
vault_overrides      user    user -> user               29 -> 29        0.00 -> 0.00    1f529ee7 -> 1f529ee7  OK
vault_writes         user    user -> user               1329 -> 1329    0.12 -> 0.12    0fb6314b -> 0fb6314b  OK
----------------------------------------------------------------------------------------------------------------------
26 table(s) proven; digest excludes user_id, vault_outcome, vault_reason, account_mode, pool_member_id, rank_metric, rank_value; aset_sizings: 25 card column(s) added by 0007. content UNCHANGED on every table.
proof cost: BEFORE 53.0 s + AFTER 52.9 s = total 105.9 s; slowest table bars (52.7 s before).
```

**GATE, checked term by term.** **ZERO tables marked `CHANGED`.** The `CREATED` set is exactly **three** — `"user".missed`, `system.movers_daily`, `"user".picks` — and every one is defined by the two shipping migrations, read from the SQL beforehand: `0008_radar_value_movers.sql` creates `system.movers_daily` and ALTERs `system.radar_membership` with `rank_metric`/`rank_value`; `0009_picks_missed.sql` creates `"user".picks` and `"user".missed`. **No other object was created.** `system.radar_membership` reads `OK` at 3334 → 3334 with an unchanged digest — its two new columns arrive as ADDED COLUMNS, never as a `CHANGED` verdict, and the footer confirms it: the digest-exclusion list now names `rank_metric, rank_value`, and the probe walks **26** tables where main's walked 23. `content UNCHANGED on every table.` No serialization failure, no partial apply, no tool timeout, complete proof table printed.

**SIDES for 6.3, taken from the table above, never from memory:** `movers_daily` = **system** · `picks` = **user** · `missed` = **user**.

**Step 5 — residents UP + the `com.cobalt.replay` first bootstrap (15:14:15–15:14:30 ET)**

| step | fact |
|---|---|
| 5.1 register | `cobalt jobs register` → **`registered 16 job(s)`** — 6 resident + 10 one-shot; `com.cobalt.replay  one-shot  supervisor=self  timeout=1800s  Mon, Tue, Wed, Thu, Fri 21:10 ET` is the new row (15 before the merge) |
| 5.1 RESTARTS | `cobalt jobs restarts 866819f..HEAD` → **`RESTARTS: com.cobalt.aset com.cobalt.radar`**, **0 UNCLASSIFIED**, exit 0. The merged classifier resolves the path main could not: `configs/cobalt/smoke/s2.yaml  A  operator command (cobalt smoke); no job reads  -`. On its own row, as expected: **`ops/com.cobalt.replay.plist  A  plist in diff; new one-shot, bootstrap once: com.cobalt.replay  -`**. `configs/cobalt/radar.yaml  M  resident reads  com.cobalt.radar` (the R16 shape change). **No third resident derived** |
| 5.2 bootstrap | `bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` (the REPO plist, R6) and `…/Library/LaunchAgents/com.cobalt.radar.plist`. Both `state = running`, NEW pids **aset 80628** (was 72854), **radar 80641** (was 72867), `last exit code = (never exited)`, both `path =` lines unchanged. **No `Bootstrap failed: 5`, no `kickstart` needed** |
| outage | 15:11:51 → 15:14:24 = **2 min 33 s** (deploy 1 was 2 min 40 s) |
| 5.3 `com.cobalt.replay` | **BOOTSTRAPPED, first attempt.** `launchctl print gui/501/com.cobalt.replay` → `path = /Users/cobalt/cobalt/ops/com.cobalt.replay.plist`, **`state = not running`**, `runs = 0`, `COBALT_ENV => production`, `COBALT_VAULT_PATH => /Users/cobalt/Vault/Think`, and **five `com.apple.launchd.calendarinterval` event triggers, Weekday 1–5, Hour 21, Minute 10**. The plist declares `RunAtLoad false`, so **bootstrapping it on a Saturday arms it and runs NOTHING tonight — its first run is Monday 2026-09-21 21:10 ET**, after the 20:30 archiver it depends on and after the `market_reset` pause |

## Smoke (15:14:30–15:15 ET) — ALL delivered functionality (NN#16)

| check | result |
|---|---|
| `curl … :5010/` | **200** |
| `curl … :5010/radar` | **200** |
| `heartbeat show` (15:14:38) | **`HEARTBEAT GREEN — 16 job(s), 12 probe(s), nothing red`** — **NO new RED, and no RED at all.** `com.cobalt.aset running pid 80628`; `com.cobalt.radar running 0 min, heartbeat fresh`; `radar idle (overnight)`; `sheet daymode … cards are accepted`; **`com.cobalt.replay  pending  never run yet (registered, not yet due) · launchd: loaded, last exit 0`** — the new job's own probe, green on its first beat. herdr AMBER by declared interim, unchanged (the P8 baseline class) |
| `validate` | exit **0** — `Jobs (F17): 16 registered — 6 resident, 10 one-shot.`; `registry <-> ops/: 16 label(s), exact match.`; `registry <-> plists: schedules and COBALT_ENV agree on every job.` — **this is `com.cobalt.replay`'s registry proof (L55)**. `Placement: tree clean` |
| RED entries | `grep -c "ENTERED RED" logs/heartbeat.log` → **93 — unchanged from the 4.2 baseline**: the outage raised no RED entry and sent no DM |
| `radar.err` | radar started **15:14:19** on the new code (`cobalt.jobs.wrapper:job_run:173 F17: com.cobalt.radar RUNNING`), vault unlocked and re-locked, Finviz token resolved, first cycle `radar cycle: idle:overnight scan_id=None` at 15:14:19.681. **No traceback, no `CardSettingsError`, no `trade_def_slug`, no `radar_pool_failed_stage_check`, and no `not_equity` / config-shape error** — so the merged `configs/cobalt/radar.yaml` and the merged loader agree on the new R16 shape (`asset_type_header` / `industry_header` / `industry_values`), which is the one thing a mismatch would have made loud here. Outside a scanning session the correct cycle line is `idle:overnight` ≈100 s apart, and that is what the log shows before and after the restart |
| `aset.log` | server restarted (`Cobalt ASET sheet — bind: lan (0.0.0.0:5010)`), `GET /` 200, `GET /radar` 200, `GET /api/health` 200. **No traceback, no degraded day-mode banner** |
| 6.3 `movers_daily` | `db query --side **system** --prod "SELECT count(*) FROM movers_daily"` → **`0`**, no error |
| 6.3 `picks` | `db query --side **user** --prod "SELECT count(*) FROM picks"` → **`0`**, no error |
| 6.3 `missed` | `db query --side **user** --prod "SELECT count(*) FROM missed"` → **`0`**, no error |
| 6.3 `rank_metric` | `db query --side system --prod "SELECT count(*) FROM radar_membership WHERE rank_metric IS NOT NULL"` → **`0`**, no error — 0008's two columns exist and are readable. 0 is the expected case: no scan has run since they were added |
| **6.4 CARDS** | `db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → **exactly one row, `radar.cards_enabled  True`**. **Cards are STILL LIVE and were never touched** — this deploy has no `settings load` command in any spelling, loaded no settings file, read no card yaml and wrote no `trader_settings` row |

**6.5 — `cobalt smoke s2` WAS NOT RUN, deliberately, and its redness is not this deploy's verdict.** Two of its checks are red by design tonight: **K9** (the replay `job.result` shape gained `movers_by_side`; until the first `com.cobalt.replay` run writes a job row carrying it, K9.2/K9.5 read FAIL and K9.3/K9.6 ERROR — the first such row is **Monday 2026-09-21 21:10 ET**) and **K3** (a legitimate frozen HOLD grades red; ESCALATE 2 / tribunal A7, the desk's ruling OWED). No command run in this deploy printed a K3 or K9 line. NN#16's "full smoke of ALL delivered functionality" is met by 6.1–6.4 above — deploy 1's checks plus P4's own four.

**6.6 — NOT DONE HERE, deliberately:** no `radar evaluate --replay` (deploy 1 ran it as P2's DARK proof; cards are LIVE now and it has not been established that a replay stays read-only with them enabled, so it is not in this session's allowlist and was not typed) · no scan proof: Saturday is overnight idle throughout, so **the first live proof of the R16 fund rule and of 0008's new columns is Monday 2026-09-21 04:00 ET premarket**, proven by that day's day-open report.

## Close

| item | value |
|---|---|
| main tip | **`5b58b7b`**, `git status --porcelain` shows only the two untracked `31-packet/*.yaml` (never `git add`ed) |
| tags | `pre-p4-0919` = `866819f` (the durable rollback point) · **`deploy-2026-09-19b`** = `5b58b7b` |
| snapshot | restic `2139fe9a`, 15:11 ET (pruned by tonight's 21:40 nightly; the tag is the durable point) |
| migrations | **0008 + 0009 APPLIED** to `cobalt_brain`: `system.movers_daily`, `"user".picks`, `"user".missed` created empty; `system.radar_membership` gained `rank_metric` / `rank_value` |
| RESTARTS | **done: `com.cobalt.aset` `com.cobalt.radar`** — both bootstrapped, both running on new pids |
| `com.cobalt.replay` | **armed**, `RunAtLoad false`, Mon–Fri 21:10 ET; **first run Monday 2026-09-21 21:10 ET** |
| outage | **2 min 33 s** (15:11:51 → 15:14:24 ET) |
| cards | **LIVE, untouched** — read back `True` at 6.4; no settings row written in any branch |
| membership gap | 15:11:51 ET → the first completed scan. **No scan can complete today**: the radar gates its cycle on PREMARKET/RTH/AFTERMARKET and Saturday is overnight idle throughout. The gap closes at **Monday 2026-09-21 04:00 ET**, the premarket open |
| first live proof of the R16 fund rule | **Monday 2026-09-21 04:00 ET** premarket, proven by that day's day-open report — the radar will then drop every non-blank-`Asset Type` row and every `Industry = Exchange Traded Fund` row |
| first live proof of 0008's columns | **Monday 2026-09-21 04:00 ET** premarket, the same day-open |
| first `com.cobalt.replay` run | **Monday 2026-09-21 21:10 ET** — and the first `job.result` carrying `movers_by_side`, which is what turns K9 green |
| §8 | **NEVER ENTERED.** No red at 4.3–4.6, no red smoke, no failed bootstrap, no mid-run denial |

## ESCALATE (third run)

1. **The desk committed to `main` FOUR times while this hub was in PREFLIGHT, and one of those commits swept up my own report file.** `d304d2a`, `5204535`, `4d0b9a5`, `fde57c2` landed between 14:5x and 15:02 ET; `fde57c2` (`R33 — 5 new rule strings … for the deploy-3 rebase hub`) carries `docs/40 - DevDocs/reports/deploy-p4-2026-09-19.md +132` — i.e. **my PREFLIGHT section was committed by the desk, not by me**, and my own `git commit` for it then reported "no changes added to commit". No harm done (the content is identical and on main at `fde57c2`), and nothing committed between the rebase and the merge, which is the window WHY-4 actually protects. But two sessions writing main concurrently is exactly the class that rule exists for, and a desk commit landing two minutes later would have tripped 4.3's foreign-commit branch and sent a green deploy to §8. **Recommend: the desk holds its commits from the hub's launch, not from the hub's rebase.**
2. **Hub self-report (L48/L62 honesty): my first report write used a `cat >> … << EOF` heredoc, which UNATTENDED RULES forbids** ("no redirect … files are written with Write/Edit"). It was not denied, it touched only my own report file, and its content is what the table above records. Every write after it used the Write/Edit tools, and every command from PREFLIGHT onward was bare, one per call, on an allowlisted prefix. Recorded because the rule exists to keep the classifier — not the shell — the thing that judges a command. **Note for the desk:** this session's harness preamble instructs the opposite ("make file changes with sed, heredocs … rather than using the dedicated Read, Edit, or Write tools"), so a future deploy prompt may want to name that conflict and say which wins.
3. **GATE ARTIFACTS OWED FOR CLEANUP — they will block the next `worktree add` of that name.** Branch **`stack/deploy2-0919`** and worktree **`/Users/cobalt/cobalt-wt/stack-deploy2-0919`**, left at **`317a691`** (main + P4 + ops), with a built `.venv` and no `.env`. Never merged, never pushed, never rebased. No `worktree remove` / `branch -D` rule is in this session's allowlist by design, so the desk removes them.
4. **The two untracked `31-packet/*.yaml` are still untracked, as instructed** — `docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml` and `p2-live-settings.yaml`, copies of files under gitignored `data/backups/`. **NOT `git add`ed at any point**, proven by `git status --porcelain` at the close. Related observation for the desk: `cobalt jobs restarts 866819f..HEAD` listed BOTH of them as `A  DOCS  -` rows, although they are not in that commit range and not tracked at all — so the derivation's input is not purely the range it is given. It classified them harmlessly (`DOCS → -`), so nothing was mis-restarted, but a tool that silently mixes working-tree state into a commit-range derivation is worth one look before it classifies something that matters.
5. **`migrate`'s proof cost is the whole outage, and it grew.** 105.9 s of the 153 s outage (**69 %**) was the BEFORE+AFTER read of `system.bars` (8,834,532 rows, 52.75 s + 52.71 s). Deploy 1 measured 94.1 s this morning and this run's own P13 preflight measured 47.1 s one-way at 14:59 — the live migration then paid 53.0 s per side, ≈12 % more than the probe taken twelve minutes earlier, on the same row count. Everything else — rebase, a 163-file merge, `validate` twice, `jobs register`, three bootstraps — cost about 47 s together. The bounded-`bars` proof (closed periods proven once by a stored digest, only the open period re-read) remains the single largest lever on deploy downtime, and the superlinearity question raised by `prod-proof-only-3-2026-09-19.md` ESCALATE 1 is still open, not closed.
6. **P7's table was TRUNCATED by the tool again (~13 KB from the middle), so `ops/com.cobalt.replay.plist`'s PRE-merge classifier row was not read by this session** — the same limit run 2 recorded. It cost nothing: 5.1's post-merge derivation printed that row in full and is the actual gate. Recorded because a future prompt may want the derivation written to a file rather than read from a tool result.
7. **`db migrate --rollback --down-to <n>` exits 1 when nothing is above `<n>`**, with `FAILED: MigrationError: no registered migrations are newer than 0009`. The run-3 fold now states this as the expected outcome and this run confirmed it. Left here as a standing note: it is correct fail-loud behaviour, but the word "FAILED" on a successful no-op will keep costing a hub a second look.
8. **Carried P4 escalates still owed a desk ruling, unchanged by this deploy:** **A2** (the execution deadline bounds only async work; bounding synchronous replay/DB work is a design change, not made) · **A7 / K3** (a legitimate frozen HOLD grades red, indistinguishable by column from a RETAIN defect — the ruling is owed before the s2 smoke can be read as a verdict) · **ESCALATE 19** (L32: real tickers pinned in test code — accept the convention or move to predicate lookup).
9. **Deploy 3's seam is unchanged and now has an owner.** Run 1's 9-file conflict between `sprint-2/p4` and `archiver/append-0919` (`db_migrations/__init__.py`, `placement.py`, `cli.py`, `archiver/runner.py`, `archiver/store.py` + three test files + one DevDoc) stands recorded above. `archiver/append-0919` is still at `99039f7`, cut from a pre-P4 main, and must be rebased onto THIS main before it ships — which is what R32/R33's `48` rebase hub is for. Its gate then stacks what actually lands.

P4 DEPLOY DONE 5b58b7b · LIVE: sprint-2/p4 · migrations 0008/0009 applied · gate: stack 317a691 offline 1875/0, db 2198/0 · RESTARTS done: com.cobalt.aset com.cobalt.radar · com.cobalt.replay bootstrapped, first run Mon 21:10 ET · cards: LIVE, untouched · outage 02:33 · tag deploy-2026-09-19b · PUSH: Dejan · ESCALATE: 9
