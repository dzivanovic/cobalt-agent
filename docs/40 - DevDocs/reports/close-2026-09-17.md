# Close 2026-09-17 — session close (evening hub `evening-0917`, `98-evening.md` §8)

## §0 Headline
- Ledger appendix `### 2026-09-17` appended; BACKLOG `## GATED (post-MVP)` carries the host-OS-upgrade project (R18) with its six-point checklist.
- LIVE `7e4841e`, tag `deploy-2026-09-17`: ops-0917 + grok-deny + agy-trial-rebuilt. **S2-P2 merged then REVERTED** on a red integrated suite; nothing migrated, no settings row written, R12's dark-load approval UNUSED.
- 6 LAWS items PROPOSED, NOT APPLIED — the desk applies, Dejan assigns every number (L58). This hub wrote nothing under `6 - Permanent/Memory/`.
- ESCALATE: 6.

## Deploy table

| branch | tip | status |
|---|---|---|
| `ops/2026-09-17` | `3f29250` | LIVE — Finviz ceiling 45, `daymode.trade_count_band` 2–6 |
| `ops/grok-deny-0915` | `15ae7fc` | LIVE — `ops/grok-sandbox.toml` + HARNESS classifier rule |
| `ops/agy-trial-0915-rebuilt` | `475417f` | LIVE — docs only; old `ops/agy-trial-0915` `6b7c5a4` never merged |
| `sprint-2/cards` | `2e22ff7` → reverted | **NOT LIVE** — §2.4 suite red, `git revert --no-edit 475417f..2e22ff7` |

Restarts 20:02 ET inside the pause: `com.cobalt.aset` (pid 55521), `com.cobalt.radar` (pid 55534), 0 UNCLASSIFIED. Flap proof 21:05: `ENTERED RED: radar` 88 → 88, 0 new. Snapshot `de4bfaa8`, tag `pre-s2-p2` at `91f11a1`. Full evidence: `deploy-2026-09-17.md`.

## LAWS fold — PROPOSED, NOT APPLIED
The desk applies these; Dejan assigns every number (L58). Each carries his words and the time.

**(a) UNATTENDED LAUNCH — candidate L61 corollary.** R5, 06:03 ET: "I want all your sessions to have all the necessary permissions ahead of starting and I want them to run unattended by you all the way through until the task is finished. … You ask me to approve ahead of time and you run the tasks yourself. You close the task yourself. … I want the approval list ahead of time and no questions in the middle. If the question is in the middle, your task has failed, needs to rerun with the correct information."
*Fold text:* Every session the desk starts receives all of its permissions before it starts, as a per-session allowlist shown to Dejan once as an approval list. No questions mid-run: a mid-run question or a mid-run denial means the run FAILED and is rerun with corrected information, never patched from inside. The session's report file is its always-open stop channel — a written `FAILED: <step> — <reason>` line is always a correct ending. Standard: `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`.

**(b) NO DIALOGS, EVER — candidate law.** R8, 06:38 ET: "if dialog boxes are a problem, I never want to see them again, and we always want to have a chat version of approvals and directions for any of the agents, including yourself."
*Fold text:* No agent, the desk included, is ever left on a permission dialog. Every launch line denies the dialog tools (`AskUserQuestion`, `EnterWorktree`); approvals and directions are chat text in the desk chat. A session found on a dialog means the launch was wrong: stop it, fix the list, rerun. Evidence: two `--bg` hubs sat blocked 05:35–06:00 on dialogs nobody could see, while `claude agents --json` reported them `busy`.

**(c) ALWAYS-ON DESK + PHONE-FIRST — candidate law.** R8 06:38 ET: "I want you to be able to run in the background, relaunch yourself, and attach to the herder pane and also launch yourself as remote control. … When you decide that the session is heavy and you need to refresh yourself, you do a restart of your session … So this way, you're always on and we save tokens." R11 07:04 ET: "I suggest we always have you running in Herder and on remote control. And you relaunch yourself and attach to those two all the time." R7 06:29 ET: "I want to be able to tell you through the phone to start the tasks and also approve everything on the phone and not need to copy and paste anything between your child sessions."
*Fold text:* The CTO desk runs as a background session with BOTH exposures at all times — the herdr "CTO" tab and remote control `cto-desk`. It refreshes itself by HANDOVER instead of `/clear`; the successor ends the predecessor (no session stops itself). A crash is answered by the same wake-up file, which IS the crash routine. The desk is the only session Dejan talks to, and he reaches it from the phone. Proven limits to carry with it: outside `~/cobalt` the `Bash(claude --bg *)` allow rule does not apply, so the desk relaunches only from `~/cobalt`; whether a dropped remote-control link can be re-bound on a running session is NOT KNOWN.

**(d) DESK EDITS HIS NOTES — candidate law; flag L58 and L28.** R14, 07:58 ET: "I don't want to touch radar list. It looks very complicated. And I don't want to mess something up. … Don't make me do manual adds anywhere. This files are accessible by you and I want you to edit them."
*Fold text:* When Dejan has ruled a change to one of his own vault notes, the CTO desk makes the edit — he is never handed a manual edit. Smallest possible diff, only the value he ruled, before/after in the desk report, and a read-only production-parser proof afterwards. Never a value he has not ruled. **Flag for the desk:** this widens L58 (which today scopes desk vault writes to `6 - Permanent/Memory/`) and meets L28's human-note boundary (Cobalt owns marked units, not human text) — the fold must say explicitly that this is a DESK edit on his ruling, not a Cobalt write path, and that L28's human-wins rule is untouched.

**(e) L43 amendment candidate — OPEN for Dejan, desk finding, not yet his ruling.** A deploy that writes `"user".trader_settings` must run its merge / migrate / settings phase BEFORE 20:00 ET, because `assert_writable` refuses every Cobalt write during `market_reset` (20:00–21:00); only the `com.cobalt.radar` restart sits inside the pause. Tonight proved the shape works (phase 1 at 19:15, restarts at 20:02) but did not exercise the settings write — P2 never reached it.

**(f) Record, not law.** `radar.finviz_max_rpm` 50, ruled R17 08:38 ET ("50 is approved.", L53 — his number), effective with P4's D1, not tonight. Tonight's ops-0917 shipped 45.

**(g) New, from tonight — OPEN for Dejan, no number proposed.** The merge that reaches `~/cobalt` is live to any resident that restarts at that instant: production `com.cobalt.radar` respawned three times into merged P2 code at 19:18–19:21 ET under launchd `KeepAlive`, outside the L43 window, before the revert. Every write it attempted was refused by the prod CHECK, so nothing was corrupted, but "phase 1 restarts nothing" is not true as written. Candidate remedies for his ruling: stop the residents before a merge, or merge into a staged checkout that production only follows on the restart step.

## ESCALATE
1. **ops×P2 `load_sources(context_tickers)` merge-order defect** — neither branch is red alone; the integrated suite is the gate, `git merge-tree` is not. Fix before P2 re-ships: rebase `sprint-2/cards` on today's main and update ops-0917's four call sites.
2. **Deploy shape vs. running residents** — proposal (g) above; needs his ruling before the next multi-branch evening.
3. **Shared `cobalt_dev` carries P2's 0006/0007** — four tenancy/aset tests are red on main independent of any branch (identical finding as P4's E4). Until `cobalt_dev` is reset, `§2.4`-style suite gates cannot be read cleanly.
4. **`cobalt notify --help` does not exist** — the evening prompt's smoke step needs rewriting against `cobalt validate`'s `Notify:` line.
5. **Carried from ops-0917 (1–6)** — over-band step-down build owed (R13); grok directory-deny rests on a single model reply; retire `ops/agy-trial-0915` + both trial worktrees; ETF i1 closed by R14; `GOOGLE_OAUTH_*` closed by R15; Finviz cross-process gate = a P4 acceptance check.
6. **Carried from P4 (E1–E5)** and P2's 11 triaged items; P2's audit recompute before D2 ENABLE is blocked until the Codex weekly reset.

## Verification before commit
- `git -C /Users/cobalt/cobalt status --porcelain` — the three close files plus machine-written/report files only, added BY NAME (L51-2); nothing under `data/`.
- `COBALT_ENV=production uv run cobalt validate` — exit 0.
- This hub wrote nothing under `6 - Permanent/Memory/` (L58). SESSION-CLOSE steps 3–5 (NOW rewrite, areas/topics append, the 4,000-character measure) are the desk's.

CLOSE COMMITTED <see git log -1> · PUSH: Dejan · ESCALATE: 6
