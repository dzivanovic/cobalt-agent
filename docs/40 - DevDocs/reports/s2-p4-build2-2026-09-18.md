# S2-P4 build 2 — 2026-09-18

DO NOT STOP until BUILT / FAILED. Hub: Sonnet 5, seat `p4-e2-0918`, unattended (R5 09-17 06:03, R6 09-17 06:11, R17 09-17 08:38, R1 list (3) 09-18 06:46, R7 09-18 11:24). OFFLINE by design: no `.env`, no database, no migration.

## §0 Headline
- In progress — step 1 (rebase onto P2) DONE; step 2 (E2) next.
- **`sprint-2/cards` MOVED during this run**: at 1.1/1.2 it was `976528f` (rebased onto exactly that); by 14:48 ET it is `db950b5` — the desk rewrote it into the STACK (ops-0918 + P2, R8/R9 plan; stack gate second run). P4 sits on the OLD `976528f` lineage. See ESCALATE 1.

## AUTHORIZATION (verified by this session)
| check | evidence |
|---|---|
| R5 / R6 / R17 committed on main | `cto-2026-09-17.md` rows R5 (06:03), R6 (06:11 "approve."), R17 (08:38 "50 is approved.") |
| R1 (09-18 06:46) list (3) | `cto-2026-09-18.md` R1 |
| the ONE new rule `Bash(git rebase sprint-2/cards)` | `cto-2026-09-18.md` R7 (11:24 "Approved", item (1)) — committed `5476410` on main |
| proof of commit | `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` → top `5476410`, `77b1a08` |
| mismatch | none |

## PREFLIGHT
| probe | result |
|---|---|
| `git status --porcelain` | empty |
| `claude -p --model claude-opus-5 "Reply with exactly the word OK"` | `OK` — no usage-limit message (L47 meter probe) |
| `git -C /Users/cobalt/cobalt status --porcelain` | ` M docs/40 - DevDocs/reports/seat-usage.md` — not this session's, not touched |
| `uv run pytest --co -q tests/cobalt/test_replay_runner.py` | 23 collected (file exists) |

## §1 Rebase onto P2
| item | value |
|---|---|
| HEAD before | `2d954a5` (expected) |
| `git log --oneline main..HEAD` before | 23 |
| P2 tip at 1.1 | `976528f` (expected) |
| P2 tip at 14:48 ET (after the rebase) | `db950b5` — rewritten stack, merge-base with HEAD now `e12417d` |
| own commits above `976528f` after rebase | 23 (= before; `git log --oneline 976528f..HEAD` read in full) |
| HEAD after | `f699478` |

Four conflict stops, each ONE Opus headless run (briefs `scratch/rebase-brief-{1,1b,2,3,4}.md`, notes `scratch/rebase-resolve-*.md`), hub-verified (`git diff --check` empty, no markers in src/tests/configs/docs, touched tests run):
| stop | replaying | files | resolution |
|---|---|---|---|
| 1 (4/23) | `2dba001` wip chunk A | 7: `aset/radar_panel.py`, `cards/{cli,store}.py`, `db_migrations/{__init__,placement}.py`, `test_radar_migration.py`, `test_tenancy.py` | both sets kept: 0006/0007 (P2) + 0008/0009 (P4), numeric order, one `_named()` helper (L3), merged `fill()` = P4's single-transaction pick-under-SAVEPOINT + P2's `before_commit` on `transition()`. Resolver's 3 union-broken tests (`test_p4_migrations`, 2× `test_radar_score_migration`) fixed by name-based union asserts in run 1b; suite then 1564 passed / 0 failed |
| 2 (6/23) | `0c95cd1` chunk A | 5 DevDocs pages | both sections kept, chronological; symbols grepped |
| 3 (8/23) | `b8311a9` chunk B | `radar/notes.py`, `radar/propose.py`, `settings/cli.py`, DevDocs `radar/notes.md` (+ one call site `test_replay_demand.py`) | L53: ONE demand computation — P2's `plan_transport_demand` feeds P4's gate via `rpm=demand.steady_rpm`; both refusals survive, no second sum (L3); `--card` and `--optional` loaders both kept; DevDocs prose corrected where the union made it false |
| 4 (10/23) | `1424e98` chunk C | `BACKLOG.md` | both items kept |

**Suite after the rebase (offline, `uv run pytest -q tests/cobalt tests/taxonomy`): `6 failed, 1673 passed, 305 skipped, 2 xfailed`.** The prompt's 1.3 expects 0 failed; the six are ALL `tests/cobalt/test_replay_runner.py` and ALL one cause, P4's own known E2: `ReplayError: S2-P2 formation replay is present but incompatible: missing ['membership_id', 'trade_def_md5']` — the adapter written for an absent P2 now meets P2's shipped `ReplayFormation`. That is exactly step 2's subject; proceeding (not a rebase defect, and no conflict resolution touched `replay/`). Named: `test_nightly_report_lists_misses_with_the_gate_or_variable_that_excluded_them`, `test_r2_3_a_retry_after_a_commit_boundary_resumes_idempotently`, `test_archive_failures_are_counted_and_fail_the_job_at_the_end`, `test_drc_note_absent_fails_the_line_step_and_creates_nothing`, `test_formation_replay_unavailable_logs_exact_line_no_rows`, `test_dry_run_through_the_cli_writes_nothing_and_prints_rows_and_the_line_diff`.

## ESCALATE
1. **`sprint-2/cards` was rewritten under this run** (stack: ops-0918 + P2, tip `db950b5`, commits `f907f1d`/`30f3402` etc.). P4 is rebased onto the OLD `976528f`. P2's code is unchanged by the stack (only a card-settings fixture row + the ops commits), so E2 built here binds to the same shipped contract. The next rebase must be onto MAIN after the stack lands (a re-rebase onto the new branch would drop the patch-identical old P2 commits and re-hit the ops-side files) — desk's call.
2. (from the rebase resolver, for step 2/verification) `test_radar_score_migration.py::test_card_checks_index_and_receipt_immutability_on_cobalt_dev` (`requires_db`) rolls back to 0005 and re-applies only 0006/0007; with 0008/0009 registered that rollback also drops P4's objects and the re-apply does not restore them. Inside a rolled-back transaction, assertions pass — needs confirming on `cobalt_dev` in the DB-verification run (this run is offline).

CONTINUE: step 2 (chunk E2)
