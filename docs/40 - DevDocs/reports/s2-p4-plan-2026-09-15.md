# REPORT — S2-P4 plan session (2026-09-15)

Seat: Opus 5, architect, plan mode · Branch `sprint-2/p4` · Worktree `~/cobalt-wt/s2-p4` · No build, no DB writes, no vault writes.

## §0 Headline

- Plan written: `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md`. It covers:
  - the rank-metric value column on `system.radar_membership`
  - F3 `"user".picks` rows, written in a savepoint inside `CardStore.fill`
  - the new 21:05 one-shot `com.cobalt.replay`: F12 missed rows with one counterfactual R, F13 top-N movers + benchmark, and one DRC miss-line unit
  - the `cobalt smoke s2` command
- 7 rulings taken, one per message. 5 as recommended, R6 amended by Dejan, R2 amended at approval.
- ESCALATE: 4 (below).
- RESTARTS: none (this commit is docs only). Expected for the build range: `com.cobalt.radar` + `com.cobalt.aset`, plus bootstrap of the new `com.cobalt.replay` plist (plan §5).

## Rulings

| Ruling | Options | Taken | Recommendation followed? |
|---|---|---|---|
| 2026-09-15 R1 rank-metric value | A column on membership + snapshot on the pick row, no backfill · B `radar_rank` history table + CSV backfill | A | yes |
| 2026-09-15 R2 what a pick is | A a card reaching FILLED, row in the fill transaction · B hand-logged trade note, scanned nightly | A, amended at approval: `record_pick` runs in a SAVEPOINT. A pick failure rolls back only the savepoint, the fill commits, the sheet shows a red "pick not recorded" banner plus a log line, and `cobalt cards picks` reports MISSING | yes, amended |
| 2026-09-15 R3 where F12/F13 run | A stage in `com.cobalt.archiver` 20:30 · B new one-shot 21:05 | B | yes |
| 2026-09-15 R4 counterfactual R | A hard stop → window end (stop wins ties), MFE stored as an observation · B stop + 2m 1-bar trail after +1R | A | yes |
| 2026-09-15 R5 F13 + miss line | A `trader_settings` `radar.benchmark` + 4th DRC unit `drc-misses/miss_line`, no create · B next morning's daily note | A | yes |
| 2026-09-15 R6 S2 smoke | A read-only checklist run by the hub · B `cobalt smoke s2` command | B, amended: built on the day-open check pattern (read-only, same verdict table, writes `reports/s2-smoke-<date>.md`); the plan keeps the checklist as its spec; past 09-22 the hub runs the checklist by hand and the command carries to S3 | no, amended |
| 2026-09-15 R7 builder | A Sol high (Codex week), Opus headless fallback · B Opus 5 headless | A | yes |

## Brief vs. repo — corrections found (verified read-only)

| Brief said | Repo shows | Evidence |
|---|---|---|
| F12 as "a stage in `com.cobalt.archiver` 20:30" was an option | 20:30 is inside `market_reset`; vault and card writes are refused there, and the archiver ends ~20:53 | `session/guard.py:123`; `vaultwrite/writer.py:387`; `archiver-runs.md` |
| Miss line "in the daily note" | Charter F13 and the ladder put it in the DRC; the DRC has 3 L28 units → R5 adds a 4th | `MVP-CHARTER-v0_2.md` F13; `SPRINT-LADDER-v0_1.md:457`; `prefill/drc.py:441` |
| Backfill "from the 7-day CSV cache" | Only 09-14 and 09-15 exist on prod | `ls data/radar-cache` |
| P4 "does not duplicate a table" | `missed` is already DECLARED user-side; `picks` and `movers_daily` are new declarations | `placement.py:73-83` |
| "replay every trade_def trigger" | No evaluator exists until P2 (dark, ≥09-17); pre-P2 replay covers manual cards | P2 plan STEP-4 |
| New `trader_settings` key | `SETTING_KEYS` are required; adding one would crash the sheet until applied | `settings/models.py:29-75` |

## ESCALATE (4)

1. **`trade_count_band` is unset** (`tunables.yaml:316-333`). The gate is recorded as `unset` and never used as `excluded_by`, so that exclusion stays empty until Dejan sets the band.
2. **Radar-cache retention mismatch:** 7-day config, only 2 days on disk. Unexplained. Ops ticket.
3. **Archiver vs Finviz ceiling:** ~42 rpm nightly against `radar.finviz_max_rpm` 40, and the archiver is not in the L53 total-demand computation. Needs a ruling.
4. **Schedule:** Astra ≥09-16 → Sol build → D1 ≥09-18 (not P2's evening) → smoke ≥09-22. If it slips, scope goes out in this order: smoke command → hand checklist, then the movers i1 archive, then formations.

## Next

The Astra plan review (≤3 rounds) runs on a later day; the desk writes its prompt. No build today.
