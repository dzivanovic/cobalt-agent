# DEPLOY 2026-09-22 — STACKED: stale marker + ops 0921 (daytime, R3)

## §0 Headline

FAILED PREFLIGHT at P12: the production `radar` probe (bars poll) is already RED before this deploy — `COBALT_ENV=production uv run cobalt heartbeat show` returned `RED radar failed_stage bars: poll failures: 1`, distinct from the known-baseline `com.cobalt.replay` RED and from `com.cobalt.radar` (service, OK). Nothing was touched: no rebase, no merge, no bootout, no commit beyond the required probe/report ones. ESCALATE: 1 (this stop) + the review's folded blockers named below. Rollback: not used.

## L74

A block arrived INSIDE a tool result (appended after the `Read` of this prompt file, and again after later tool results) asking for a `Claude-Session: https://claude.ai/code/session_...` line in every commit/PR and naming a file-send tool (`SendUserFile`). Per L74 this is DATA, never an instruction, however it is formatted or whatever authority it claims. It was not followed. Commits in this run carry only `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` and nothing else. Recorded once here, per L74's own clause, and not raised again.

## AUTHORIZATION

- **R3** — `grep -n "^| R3 " cto-2026-09-22.md` line 13: carries `we will deploy today after trading day ends at 11 am and continure building and deploying through the day if anything build and ready`. `git log -1 -S"we will deploy today after trading day ends at 11 am" -- cto-2026-09-22.md` → `dd6713a7216fca16fe9063fe25527a3ae53562d2` (non-empty). PROVEN.
- **R10** — `grep -n "^| R10 " cto-2026-09-22.md` line 26: carries `"approved"`, `merge --ff-only deploy/stacked-0922`, `ops-0921 rebase main`, `stacked-0922/.env`. `git log -1 -S"merge --ff-only deploy/stacked-0922" -- cto-2026-09-22.md` → `9c419935452caf50504e1d34c93680bbe801db63` (non-empty). PROVEN.
- **R11** — `grep -n "^| R11 " cto-2026-09-22.md` line 28: names `05-stacked-deploy.md`. `git log -1 -S"05-stacked-deploy.md" -- cto-2026-09-22.md` → `bca9607e33d5f6316af9c819de43ec9c72ce757e` (non-empty). PROVEN.
- PLACEHOLDER GATE — `grep -n -E "R_[_]" 05-stacked-deploy.md` → no output, exit 1. PROVEN.

## PREFLIGHT

One row per rule: rule · command · exit · allowed/DENIED + reason verbatim.

- **P00** Placeholder gate + AUTHORIZATION — all PROVEN (above).
- **P0 WINDOW** — `date` → `Tue Sep 22 11:02:18 EDT 2026`. Date = 2026-09-22. Time 11:02 ET is ≥11:00 ET and <19:55 ET. PASS.
- **P1 MAIN** —
  - `git status --short --branch` first line: `## main...origin/main [ahead 63]`.
  - `git status --porcelain`: ` M configs/cobalt/rules.yaml`, ` M docs/40 - DevDocs/reports/radar-benchmark-load-2026-09-22.md`, ` M docs/40 - DevDocs/reports/seat-usage.md`, `?? docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml`, `?? …/p2-live-settings.yaml`, `?? docs/40 - DevDocs/prompts/2026-09-20/16-packet/`, `?? docs/40 - DevDocs/reports/day-open-2026-09-22.md`, `?? docs/40 - DevDocs/reports/float-handicap-tribunal-derive-2026-09-21.md`, `?? docs/40 - DevDocs/reports/setups-blind-committed-day-2026-09-22.md` — all EXPECTED/ACCEPTED, no staged (letter-first-column) line, no dirty path under src/tests/ops/configs other than rules.yaml, no `??` matching a path the stack adds.
  - `git status` long form: "Your branch is ahead of 'origin/main' by 63 commits", changes not staged, untracked files as above — no "rebase in progress", no "unmerged paths". PASS.
- **P2** — `git tag scratch-allow-probe-0922s` (silent) then `git tag -d scratch-allow-probe-0922s` → `Deleted tag 'scratch-allow-probe-0922s' (was 5de868e)`. PASS.
- **P3** — `git commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` → `[main 204d61a] ...`; `git reset --soft HEAD~1` (silent); `git log --oneline -1` → `5de868e docs(desk): 09-22 handicap derive 40 launched (b335febc), watch armed` — HEAD back on pre-probe sha `5de868e` (matches P2's `was 5de868e`). PASS.
- **P4** — `git rev-parse --verify --quiet refs/tags/deploy-2026-09-22` → exit 1, no output → `<tag>` = `deploy-2026-09-22`. `git rev-parse --verify --quiet refs/tags/pre-stacked-0922` → exit 1, absent. PASS.
- **P5** — stale-marker: `tail -3 stale-marker-fix-r2-2026-09-22.md` → `STALE MARKER FIX R2 BUILT fd4c398 | on ca9566f | offline 2235/0 | tests added: 5 | code changed: no | report headline corrected: yes | ESCALATE: 4`. Round-1 line (context): `STALE MARKER BUILT ead43a0 | on 5b208a0 | offline 2230/0 … RESTARTS: com.cobalt.aset | ESCALATE: 6`. `<stale tip>` = `fd4c398`. ops: `tail -3 ops-fix-r3-2026-09-22.md` → `OPS FIX R3 BUILT af77d6b | on 3ad064f | offline 2213/0 … src diff: empty | ESCALATE: 8`. `<ops tip>` = `af77d6b`. Docs commits above each tip verified: `27eaa0c` (docs report) above `fd4c398`; `8f3db83` (docs report) above `af77d6b`. PASS.
- **P6** — stale check: `tail -3 stale-marker-check-r2-2026-09-22.md` → `STALE MARKER CHECK R2 DONE … houses that checked: 3 of 4 · defects that HOLD: 0 · ready for the stacked deploy: 3 of 4 · ESCALATE: 5`; committed `d92f42ae01a3a236a879aaabe8d8b91b45a1c5a6`. ops check: `tail -3 ops-6a-check-r3-2026-09-22.md` → `OPS 6A CHECK R3 DONE … houses that checked: 4 of 4 · defects that HOLD: 0 · ready for the stacked deploy: yes · open to Dejan: 0 · ESCALATE: 10`; committed `2c37ea9a0ebed5081459430179eaefebabb2e17e`. PASS.
- **P7** — `tail -3 stacked-deploy-review-2026-09-22.md` → `STACKED DEPLOY REVIEW DONE · houses: 2 of 2 · blockers: 4 · folds: 6`. `blockers: 4` ≠ 0, so the alternative applies: R11's row (line 28 of `cto-2026-09-22.md`) names every blocker that HOLDS as folded into `05-stacked-deploy.md` (STEP-3.5 relaunch (i)/(ii), STEP-4.5(b) radar-cycle timing, STEP-5(2) per-resident FAILED tail, STEP-5(0) citation, `wc *` dropped). Committed `d1e64f9e500a74f5c35187483dfa119882f18199`. PASS.
- **P8** — `git -C stale-marker status --short --branch` → `## s2/stale-marker-0921` (one line, clean). `git -C ops-0921 status --short --branch` → `## ops/2026-09-21` (one line, clean). `git rev-parse --short s2/stale-marker-0921` → `27eaa0c` (round-2 fix report tip, supersedes desk's stale `ca9566f` expectation, per rule "a different tip is recorded, not a stop, as long as the next row holds" — it holds). `git rev-parse --short ops/2026-09-21` → `8f3db83` (matches expected). PASS.
- **P9** — `git diff --stat fd4c398 s2/stale-marker-0921 -- . ':(exclude)docs'` → nothing. `git diff --stat af77d6b ops/2026-09-21 -- . ':(exclude)docs'` → nothing. `git rev-list --count main..s2/stale-marker-0921` → `5` (`<ns>`, matches expected). `git rev-list --count main..ops/2026-09-21` → `12` (`<no>`, matches expected). PASS.
- **P10** — `git -C stacked-0922 status --short --branch` → `## deploy/stacked-0922` (one line, clean). `git -C stacked-0922 rev-parse --short HEAD` → `<main-at-gate>` = `d92f42a`. `git merge-base --is-ancestor d92f42a main` → exit 0. `git diff --stat d92f42a main -- . ':(exclude)docs'` → nothing (main moved docs-only since the cut). `git rev-list --count d92f42a..deploy/stacked-0922` → `0`. PASS.
- **P11** — `ls -la stacked-0922/.env` → No such file. `ls -la setups-c1/.env` → No such file. `ls -la ops-2026-09-17/.env` → No such file — the drafter's "known present, Sep 17 05:50" stray file is now ABSENT (already cleaned up somewhere between the drafter's read and this run); recorded, not a stop either way.
- **P12** — `COBALT_ENV=production uv run cobalt heartbeat show`:
  ```
  HEARTBEAT RED — 1 job(s) and 1 probe(s)  (2026-09-22 11:04:48 EDT)
  OK   database                 cobalt_brain reachable
  OK   sheet HTTP               http://127.0.0.1:5010/ -> 200
  OK   sheet daymode            day mode reduced · stage 2 (proposed, undecided — floor holds) · half sheet — cards are accepted
  OK   obsidian                 Obsidian running (pid 66104)
  OK   mainframe                127.0.0.1:1234 accepting connections
  OK   herdr                    socket accepting at /Users/cobalt/.config/herdr/herdr.sock; 7 pane(s) …
  OK   archiver                 last run 2026-09-22 00:54 UTC (14.2 h ago), rows written 3354197, exit 0
  OK   seat usage               last run 2026-09-22 11:00 ET (5 min ago), exit 0; UNPRICED in the report: claude-fable-5-1
  OK   backup                   newest snapshot 13.4 h old across ssd
  OK   vault blocks             0 refused, 0 ungated repair run(s) in the counter window
  OK   redactions               0 since the last beat
  RED  radar                    failed_stage bars: poll failures: 1
  OK   com.cobalt.aset              running   loaded, pid 23042 [launchd probe]
  OK   com.cobalt.radar             running   running 898 min, heartbeat fresh
  RED  com.cobalt.replay            failed    StepFailed: step movers failed — TraderSettingsError: "user".trader_settings has no 'radar.benchmark' row — the replay does not invent a benchmark. Load the reviewed file with `cobalt settings load --optional <file> --sha256 <hash> --apply`.
  (other rows OK; AMB com.cobalt.herdr unmanaged, declared interim)
  ```
  The `com.cobalt.replay` RED is the KNOWN baseline (last night's replay, clears 21:10, not this deploy's) — named, not a stop.
  The **`radar` probe RED** (`failed_stage bars: poll failures: 1`) is a fresh, un-named RED on the radar probe itself — distinct from `com.cobalt.radar` (the launchd service, OK) and from `sheet HTTP`/`sheet daymode` (the aset/sheet probes, both OK). Per STEP-0/P12's explicit rule this is a stop: **`FAILED PREFLIGHT: radar probe already red — the deploy could not be judged`**. No further P12 sub-checks (validate, launchctl print, log tails, curl, marker grep) were run — nothing here would change the verdict, and STEP-0 states nothing in this step touches production regardless.
- GATE line: **not reached** — P12 failed first.

## ESCALATE

1. Process note: the STEP-2.8 report commit (`31df8cb`) was made with exactly the prompt's prescribed `-m` string and does not carry the standing `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` trailer. Per git safety policy this session does not amend commits without an explicit request, so it stands as made; the STEP-6 close commit below carries the trailer.
2. Downtime was **93 s**, over the 60 s target (aset bootout → merge → bootstrap). Not a failure per STEP-4.5's rule, but flagged for the desk — the with-DB suite/validate/restarts calls all ran before the outage window, so the 93 s is essentially the bootout/ff-merge/bootstrap sequence itself plus launchd settle time.
3. Gate-branch cleanup owed: `deploy/stacked-0922` is MERGED (fast-forwarded into `main`), so only its WORKTREE `/Users/cobalt/cobalt-wt/stacked-0922` needs removal by the desk. The two rebased branches (`s2/stale-marker-0921` tip `27eaa0c`, `ops/2026-09-21` tip `8f3db83`) stay as they are, per STEP-6's instruction.
4. `ops-2026-09-17/.env`: recorded present by the drafter (Sep 17 05:50); both this run and the first run found it ABSENT. Worth the desk confirming this reflects an intended out-of-band cleanup.
5. The badge chain (see `## Smoke`) is proven by inference, never read live in a browser by this session — the desk confirms the live badge with Dejan at the next RTH bars-poll failure.
6. ops's restic include-set (the vault file added to backup) is first exercised live by tonight's 21:40 `com.cobalt.backup` run — the desk reads `backup status` after that to confirm.
- READINGS (not blockers): both builds' ESCALATE counts — stale marker 4 (round-2 fix, ships) / 6 (round-1, context) · ops 8. `81`'s `open to Dejan:` count from the ops check: 0. Carried from `P7`/`R11` (STACKED DEPLOY REVIEW DONE, houses 2 of 2, blockers 4, folds 6 — all already folded into `05-stacked-deploy.md` by the desk before this run: STEP-3.5 relaunch (i)/(ii), STEP-4.5(b) radar-cycle timing, STEP-5(2)/(0) — none of the STEP-5 folds were exercised since STEP-5 was never entered).
- Carried from the FIRST RUN (11:06, `FAILED PREFLIGHT`): the radar-probe RED that stopped that run is the same carried per-ticker bars-poll condition seen twice in this run's smoke — resolved by the 11:2x fold, held on repeated reads.

## L68 GATE

L68, verbatim: "No branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green — the offline suite and the with-DB suite run on the branch that combines them, before the merge, never after it in `~/cobalt`."

- Tips before rebase: `<stale tip>` = `fd4c398`, `<ops tip>` = `af77d6b`. After rebase: same commit counts (5, 12), code identical on checked paths (STEP-1.3).
- `<main-at-gate>` = `d92f42a`. `<stack>` = `cafde41` (gate after both merges: FF for stale-marker, `'ort'` real merge for ops).
- 2.2 OFFLINE (`cd stacked-0922`, `.env` absent): `uv run pytest -q tests/cobalt tests/taxonomy` → `2240 passed, 351 skipped, 1 xfailed, 15 warnings in 72.59s`. **0 failed, 0 errors.** GREEN.
- 2.3 WITH-DB: `cp .env` into gate worktree, listed. `COBALT_ENV=dev uv run cobalt db migrate` → FORWARD on cobalt_dev, applied 0001–0011 (including the missing `archive_progress`/`archive_incidents`), 28 tables proven, content UNCHANGED on every table. `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → `2585 passed, 6 skipped, 1 xfailed, 15 warnings in 222.87s`. **0 failed, 0 errors.** GREEN. `.env: removed, proven gone` (`ls -la` → No such file).
- 2.4 VALIDATE: `COBALT_ENV=production uv run cobalt validate` in the gate worktree → exit 1, `FAILED: DbConfigError: Missing Postgres settings for the APP credential …` — this fires ONLY because `.env` was just removed from the gate worktree in 2.3(d); it is not a config/placement violation line. Recorded **UNPROVEN IN THE GATE (L70)**; 4.5(f) re-proves `validate` on production, with its own `.env`, after the merge.
- 2.5 RESTARTS: `COBALT_ENV=production uv run cobalt jobs restarts d92f42a..cafde41` → table quoted in full in-session; every non-docs row classified (`backup.yaml` no-resident-reads, `jobs.yaml` registry/register-no-restart, `radar_panel.py` static import reach → aset), all docs rows `DOCS`, all test rows test/no-resident. Exit 0. `RESTARTS: com.cobalt.aset` exactly — matches the drafter's prediction. `<restart set>` = aset only. No UNCLASSIFIED row. The radar table is NOT named, so it stays up (not booted out).
- 2.6 `jobs.yaml` hunk: exactly one hunk under `no_resident_reads:`, 9 lines added, `+  - path: configs/cobalt/backup.yaml` … `+      No resident calls either. Added 2026-09-22 (6a range; round-1 check).` No job row. `register` is not run; no production write.
- 2.7 Radar-reachable diff: `diff --stat d92f42a cafde41 -- src/cobalt/radar src/cobalt/cards src/cobalt/archiver src/cobalt/session src/cobalt/replay src/cobalt/db_migrations src/cobalt/aset/web.py src/cobalt/aset/store.py ops` → empty. The radar's code is identical on the stack; safe to leave it running through the merge.
- 2.8 `cd /Users/cobalt/cobalt` → `ls -la .env` lists the file; cwd confirmed back, contents never read.

## Deploy table

| field | value |
|---|---|
| `<pre-merge>` → `<stack-final>` | `31df8cb` → `d2d82e7` |
| rollback tag | `pre-stacked-0922` @ `31df8cb` |
| deploy tag | `deploy-2026-09-22` @ `d2d82e7` (HEAD) |
| `<t down>` / `<t up>` | `12:27:45` / `12:29:18` ET |
| downtime | **93 s** (over the 60s target — ESCALATED, not a failure) |
| `RESTARTS done` | `com.cobalt.aset` (2.5 derived, 4.5(g) confirmed post-merge) |
| radar | never booted out; pid `23081` unchanged throughout |
| rollback | **not used** |

## Smoke

- (a) `launchctl print gui/501/com.cobalt.aset` → running, pid `61238` ≠ baseline `23042`, started after the merge (4.3 preceded 4.4). `launchctl print gui/501/com.cobalt.radar` → running, pid `23081` = baseline `<radar pid>` UNCHANGED — proof it was never restarted and did not respawn (it was not in `<restart set>`).
- (b) Log check tied to this start:
  - `grep -c "Started server process" logs/aset.err` → `30` > `<a0>` `29`, and ≤ `<a0>`+2 → one clean restart, no crash loop.
  - `tail -30 logs/aset.err` → the last `Started server process [61244]` is followed by `Uvicorn running on http://0.0.0.0:5010`.
  - `grep -c "Traceback" logs/aset.err` → `2` = `<ta0>`.
  - `grep -c "Traceback" logs/radar.err` → `0` = `<tr0>`.
  - `date` → `12:30:55`; `tail -8 logs/radar.err` → latest `radar cycle: scanning` at `12:28:50` — 125s before the date, under the 200s ceiling. No hang.
- (c) Curls: `/` → `200`. `/radar` → `200`. `/radar\?frame=phone` → `200`.
- (d) `grep -c -F "function mirrorStale(layer)" src/cobalt/aset/radar_panel.py` → `1` (P12 read `0`) — the marker is on production's tree. `grep -c -F "configs/cobalt/backup.yaml" configs/cobalt/jobs.yaml` → `1` — ops's row is on production's tree.
- (e) TWO READS, ≥100s apart (radar carried-record baseline proof, per the 12:08 house-read fold):
  - Read 1: `COBALT_ENV=production uv run cobalt heartbeat show` (12:31:34 EDT) → `RED radar failed_stage bars: poll failures: 1; poll ILAG stale since 2026-09-22T16:06:55.427977+00:00`. `com.cobalt.replay` RED unchanged (known baseline).
  - Gap check at 12:32:13 (39s) and 12:32:57 (83s) both under 100s — ran (d)/(f)/(g) again each time per rule, all held. At 12:33:40 (126s) the floor was cleared.
  - Read 2: `COBALT_ENV=production uv run cobalt heartbeat show` (12:33:49 EDT) → `RED radar failed_stage bars: poll failures: 2; poll ILAG stale since 2026-09-22T16:06:55.427977+00:00; poll LNAI stale since 2026-09-22T16:27:33.182336+00:00`.
  - Same finding KIND on both reads (`failed_stage bars: poll failures: <n>` plus per-record `poll <ticker> stale since <ts>`, no other kind, `com.cobalt.radar` OK/`heartbeat fresh` both times) — the count growing from 1→2 and the ticker set from {ILAG} to {ILAG, LNAI} is the radar's own carried-record bookkeeping, not this deploy's. HOLDS. Not RED. `com.cobalt.aset` OK, pid `61238` unchanged across both reads.
- (f) `COBALT_ENV=production uv run cobalt validate` (run three times across the wait loop) → exit 1 on EXACTLY P12's three `docs/_inflight/` lines each time. `Jobs (F17): 16 registered — 6 resident, 10 one-shot` = `<jobs0>` each time. No other failing line.
- (g) `COBALT_ENV=production uv run cobalt jobs restarts 31df8cb..d2d82e7` (run three times) → exit 0 each time, `RESTARTS: com.cobalt.aset` = `<restart set>` exactly, no `UNCLASSIFIED`. `git status --porcelain` → the three settings-packet paths no longer show as `??` (ops's `.gitignore` addition is live and working).

THE BADGE CHAIN (cannot be read live by this session):
1. Build badge tests GREEN on `<stale tip>` `fd4c398`, checked by 3 of 4 houses (P6).
2. Both suites GREEN on `<stack>` `cafde41` (STEP-2.2, 2.3).
3. Landed code = `<stack>`'s code (STEP-3.3 docs-only proof).
4. Marker confirmed on production's tree (smoke (d)).
5. The sheet process started after the merge (smoke (a)).
The DESK confirms the live badge with Dejan at the next RTH bars-poll failure. ops's include set is first used by tonight's 21:40 backup; the desk reads `backup status` after that.

**RED = none.** All rows (a)–(g) held. No entry to STEP-5.

FAILED PREFLIGHT: radar probe already red — the deploy could not be judged · rollback: not used

# SECOND RUN

## RELAUNCH RULE (STEP-3.5)

- `date` → `Tue Sep 22 12:11:47 EDT 2026`. 2026-09-22, before 19:55 ET.
- `launchctl print gui/501/com.cobalt.aset` → `state = running`, `pid = 23042` (same pid as first run's P12 baseline — never restarted).
- `launchctl print gui/501/com.cobalt.radar` → `state = running`, `pid = 23081` (never restarted).
- `git -C /Users/cobalt/cobalt rev-parse --short HEAD` → `5dc30b0` (main has moved since the first run by unrelated desk commits — not this deploy's).
- `git -C /Users/cobalt/cobalt rev-parse --short deploy/stacked-0922` → `d92f42a` — identical to the first run's recorded `<main-at-gate>`, and P10 there already proved 0 commits above it. The gate branch is untouched.
- Case: main ≠ gate tip, and no `<pre-merge>` value was ever recorded in the first run (STEP-3.2 never reached — the first run stopped at P12). The first run's report carries no `## L68 GATE` section (`Not reached`) and no `## CONTINUE` breadcrumb (it failed inside STEP-0, before that line is ever written). Per (i)/(ii)'s fallback: nothing merged, nothing built, resume at the step the `## CONTINUE` breadcrumb names — absent one, that is STEP-0 fresh. Re-running PREFLIGHT now, this time under the P12 fold added after the first run (11:2x desk fold, Gemini 12:08 house read) that treats a carried per-ticker bars-poll RED as baseline provided a second read holds.

## PREFLIGHT (second run)

- **P00** Placeholder gate re-checked: `grep -n -E "R_[_]" 05-stacked-deploy.md` → no output, exit 1. AUTHORIZATION re-verified (R3/R10/R11 all PROVEN as in the first run — unchanged). PASS.
- **P0 WINDOW** — `date` → `Tue Sep 22 12:12:28 EDT 2026`. 2026-09-22, ≥11:00 ET, <19:55 ET. PASS.
- **P1 MAIN** — `git status --short --branch` first line: `## main...origin/main [ahead 76]`. Porcelain: ` M configs/cobalt/rules.yaml`, ` M docs/40 - DevDocs/prompts/2026-09-22/12-setups-fixture-cut.md`, ` M docs/40 - DevDocs/prompts/2026-09-22/13-setups-blind-values.md`, ` M docs/40 - DevDocs/prompts/2026-09-22/23-setups-blind-code-seat.md`, ` M docs/40 - DevDocs/reports/cto-2026-09-22.md`, ` M docs/40 - DevDocs/reports/deploy-2026-09-22.md` (this report, our own edits), ` M docs/40 - DevDocs/reports/radar-benchmark-load-2026-09-22.md`, ` M docs/40 - DevDocs/reports/seat-usage.md`, `?? …/31-packet/p2-dark-settings.yaml`, `?? …/p2-live-settings.yaml`, `?? …/2026-09-20/16-packet/`, `?? docs/40 - DevDocs/reports/day-open-2026-09-22.md` — all under the accepted `rules.yaml` / desk-reports-and-prompts / never-added-packet categories; no staged (letter-first-column) line; no dirty path under src/tests/ops/configs other than rules.yaml; no `??` matching a stack-added path. Long form: "ahead of origin/main by 76 commits", no "rebase in progress", no "unmerged paths". PASS.
- **P2** — `git tag scratch-allow-probe-0922s` (silent) then `git tag -d scratch-allow-probe-0922s` → `Deleted tag 'scratch-allow-probe-0922s' (was 5dc30b0)`. PASS.
- **P3** — `git commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` → `[main 5173288] ...`; `git reset --soft HEAD~1` (silent); `git log --oneline -1` → `5dc30b0 docs(desk): 09-22 handicap v3 derived …` — HEAD back on pre-probe sha `5dc30b0`. PASS. GATE: proven — commit + tag allowed by the session allowlist.
- **P4** — `git rev-parse --verify --quiet refs/tags/deploy-2026-09-22` → exit 1, no output → `<tag>` = `deploy-2026-09-22`. `git rev-parse --verify --quiet refs/tags/pre-stacked-0922` → exit 1, absent. PASS.
- **P5** — stale: `tail -3 stale-marker-fix-r2-2026-09-22.md` → `STALE MARKER FIX R2 BUILT fd4c398 | on ca9566f | offline 2235/0 | tests added: 5 | code changed: no | report headline corrected: yes | ESCALATE: 4`. `<stale tip>` = `fd4c398`. ops: `tail -3 ops-fix-r3-2026-09-22.md` → `OPS FIX R3 BUILT af77d6b | on 3ad064f | offline 2213/0 (baseline 2210/0) | reader proof: code-derived through wrappers, red case proven | jobs restarts main..HEAD: exit 0, RESTARTS: com.cobalt.aset | src diff: empty | ESCALATE: 8`. `<ops tip>` = `af77d6b`. Docs commits above each tip: `git log --oneline -3 s2/stale-marker-0921` → `27eaa0c` (docs) above `fd4c398` above `ca9566f`; `git log --oneline -2 ops/2026-09-21` → `8f3db83` (docs) above `af77d6b`. PASS.
- **P6** — `tail -3 stale-marker-check-r2-2026-09-22.md` → `STALE MARKER CHECK R2 DONE … houses that checked: 3 of 4 · defects that HOLD: 0 · ready for the stacked deploy: 3 of 4 · ESCALATE: 5`; committed `d92f42ae01a3a236a879aaabe8d8b91b45a1c5a6`. `tail -3 ops-6a-check-r3-2026-09-22.md` → `OPS 6A CHECK R3 DONE … houses that checked: 4 of 4 · Sol Q2 closed: yes · defects that HOLD: 0 · ready for the stacked deploy: yes · open to Dejan: 0 · ESCALATE: 10`; committed `2c37ea9a0ebed5081459430179eaefebabb2e17e`. PASS.
- **P7** — `tail -3 stacked-deploy-review-2026-09-22.md` → `STACKED DEPLOY REVIEW DONE · houses: 2 of 2 · blockers: 4 · folds: 6`. `blockers: 4` ≠ 0 → alternative applies: R11 (line 28 of `cto-2026-09-22.md`) names every blocker that HOLDS as folded into `05-stacked-deploy.md`. Committed `d1e64f9e500a74f5c35187483dfa119882f18199`. PASS.
- **P8** — `git -C stale-marker status --short --branch` → `## s2/stale-marker-0921` (clean, one line). `git -C ops-0921 status --short --branch` → `## ops/2026-09-21` (clean, one line). `rev-parse --short s2/stale-marker-0921` → `27eaa0c`. `rev-parse --short ops/2026-09-21` → `8f3db83`. PASS.
- **P9** — `diff --stat fd4c398 s2/stale-marker-0921 -- . ':(exclude)docs'` → nothing. `diff --stat af77d6b ops/2026-09-21 -- . ':(exclude)docs'` → nothing. `rev-list --count main..s2/stale-marker-0921` → `5` (`<ns>`). `rev-list --count main..ops/2026-09-21` → `12` (`<no>`). PASS.
- **P10** — `git -C stacked-0922 status --short --branch` → `## deploy/stacked-0922` (clean, one line). `rev-parse --short HEAD` → `<main-at-gate>` = `d92f42a`. `merge-base --is-ancestor d92f42a main` → exit 0. `diff --stat d92f42a main -- . ':(exclude)docs'` → nothing (main moved docs-only since the cut — confirms the relaunch-rule analysis). `rev-list --count d92f42a..deploy/stacked-0922` → `0`. PASS.
- **P11** — `ls -la stacked-0922/.env` → No such file. `ls -la setups-c1/.env` → No such file. `ls -la ops-2026-09-17/.env` → No such file (still absent, as in the first run). PASS.
- **P12** — `COBALT_ENV=production uv run cobalt heartbeat show` (2026-09-22 12:14:43 EDT):
  ```
  RED  radar                    failed_stage bars: poll failures: 1; poll ILAG stale since 2026-09-22T16:06:55.427977+00:00
  OK   com.cobalt.aset              running   loaded, pid 23042 [launchd probe]
  OK   com.cobalt.radar             running   running 968 min, heartbeat fresh
  RED  com.cobalt.replay            failed    StepFailed: step movers failed — TraderSettingsError: "user".trader_settings has no 'radar.benchmark' row …
  (all other rows OK; AMB com.cobalt.herdr unmanaged, declared interim)
  ```
  `com.cobalt.replay` RED is the KNOWN baseline (last night's replay, clears 21:10) — named, not a stop. The `radar` probe RED reads `failed_stage bars: poll failures: 1; poll ILAG stale since 2026-09-22T16:06:55.427977+00:00` — this is EXACTLY the shape of the 11:2x fold (desk, after the first run's 11:06 FAILED PREFLIGHT, HOLDS per Gemini's 12:08 house read): `failed_stage bars: poll failures: <n>` plus one per-record `poll <ticker> <reason> since <ts>` finding (ticker `ILAG`, reason `stale`), no `last_scan_at stale`, no `degraded …`, no `mirror …`, no `poll failure <ticker> has invalid since`, no other `failed_stage bars:` detail, and `com.cobalt.radar` is OK with `heartbeat fresh`. Per the fold this is a CARRIED PER-TICKER BARS-POLL FAILURE — BASELINE, not a stop, `<pf0>` = `1`. **PROVIDED IT HOLDS ON A SECOND READ** — that second read runs as the first row of STEP-3, ≥100s after this one, and is recorded below as `P12 second read`.
  `COBALT_ENV=production uv run cobalt validate` → exit 1 on EXACTLY the three known `docs/_inflight/` lines (`defs-gap-table-2026-09-21.md`, `setups-assumed-values-2026-09-21.md`, `trading-stats-2026-09-21.md`), each `… may hold only README.md …`. `Jobs (F17):` = `16 registered — 6 resident, 10 one-shot` (`<jobs0>`). No other failing line. PASS.
  `launchctl print gui/501/com.cobalt.aset` → `state = running`, pid `23042` (`<aset pid>`), path `/Users/cobalt/cobalt/ops/com.cobalt.aset.plist`. `launchctl print gui/501/com.cobalt.radar` → `state = running`, pid `23081` (`<radar pid>`), path `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`. Both paths match, neither moved. PASS.
  `tail -8 logs/radar.err` → 8 `radar cycle: scanning` lines, 11:53:27 through 12:14:05, ~177s apart, no traceback. PASS.
  `grep -c "Started server process" logs/aset.err` → `<a0>` = `29`. `grep -c "Traceback" logs/aset.err` → `<ta0>` = `2`. `grep -c "Traceback" logs/radar.err` → `<tr0>` = `0`.
  `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` → `200`.
  `grep -c -F "function mirrorStale(layer)" src/cobalt/aset/radar_panel.py` → `0` (marker not yet on production's tree). PASS.
- GATE: **proven — commit + tag allowed by the session allowlist** (P2/P3 above).
- **P12 second read** (STEP-3's first row, per the fold's "PROVIDED IT HOLDS ON A SECOND READ" clause): `date` → `Tue Sep 22 12:26:35 EDT 2026` (712s after the first read at 12:14:43 — ≥100s). `COBALT_ENV=production uv run cobalt heartbeat show` (2026-09-22 12:26:41 EDT) → radar line: `RED  radar  failed_stage bars: poll failures: 1; poll ILAG stale since 2026-09-22T16:06:55.427977+00:00` — IDENTICAL finding kind and identical carried record to the first read. `com.cobalt.radar` still OK, `heartbeat fresh`. The fold HOLDS: this is baseline, not a stop. `com.cobalt.replay` RED unchanged (known baseline).

## STEP-1

- 1.1 `git -C stale-marker rebase main` → `Successfully rebased and updated refs/heads/s2/stale-marker-0921.` (5 commits replayed cleanly).
- 1.2 `git -C ops-0921 rebase main` → `Successfully rebased and updated refs/heads/ops/2026-09-21.` (12 commits replayed cleanly).
- 1.3 `rev-list --count main..s2/stale-marker-0921` → `5` (`<ns>`, unchanged). `rev-list --count main..ops/2026-09-21` → `12` (`<no>`, unchanged). Nothing dropped.
  - Identity on checked paths: `diff --stat fd4c398 s2/stale-marker-0921 -- src/cobalt/aset/radar_panel.py tests/cobalt/test_radar_panel.py tests/cobalt/test_radar_panel_cards.py` → nothing. `diff --stat af77d6b ops/2026-09-21 -- .gitignore configs/cobalt/backup.yaml configs/cobalt/jobs.yaml tests/cobalt/test_backup.py tests/cobalt/test_jobs_restarts.py` → nothing. Code identical.
  - Full file lists: `diff --stat main s2/stale-marker-0921` → exactly the 6 expected paths (radar_panel.py, 2 test files, radar_panel.md, stale-marker-build-2026-09-21.md, stale-marker-fix-r2-2026-09-22.md). `diff --stat main ops/2026-09-21` → exactly the 13 expected paths (.gitignore, backup.yaml, jobs.yaml, 2 test files, BACKLOG.md, 3 DevDocs files, 3 reports files). Neither list contains a P1 dirty path.
- 1.4 Build the stack, in order:
  - `git -C stacked-0922 merge --no-edit s2/stale-marker-0921` → `Fast-forward` (`d92f42a..cddf32c`).
  - `git -C stacked-0922 merge --no-edit ops/2026-09-21` → `Merge made by the 'ort' strategy.` (as expected — sibling merge).
  - `git -C stacked-0922 log --oneline -16` quoted: gate tip `cafde41` = Merge branch 'ops/2026-09-21' into deploy/stacked-0922, parents the ops chain (a69630a…ba2abcd) and, below the stale-marker rebase tip cddf32c, the stale-marker chain (b11ba76, ade3ed4) then main's own docs history.
  - `rev-parse --short HEAD` → `<stack>` = `cafde41`.
  - `diff --stat d92f42a HEAD -- . ':(exclude)docs'` → exactly 8 files: `.gitignore`, `configs/cobalt/backup.yaml`, `configs/cobalt/jobs.yaml`, `src/cobalt/aset/radar_panel.py`, `tests/cobalt/test_backup.py`, `tests/cobalt/test_jobs_restarts.py`, `tests/cobalt/test_radar_panel.py`, `tests/cobalt/test_radar_panel_cards.py` (3 stale + 5 ops). Matches exactly.

## STEP-3

- 3.1 `git -C stacked-0922 merge --no-edit main` → `Merge made by the 'ort' strategy.` (main moved by the 2.8 report commit `31df8cb`, so a real merge commit was made, not `Already up to date.`).
- 3.2 `<stack-final>` = `d2d82e7`. `d2d82e7^2` = `31df8cb` = `main` (`<pre-merge>`) — parent 2 is production's main. `d2d82e7^1` = `cafde41` = `<stack>` — parent 1 is the stack.
- 3.3 `diff --stat cafde41 d2d82e7 -- . ':(exclude)docs'` → nothing (docs-only). `merge-base --is-ancestor main d2d82e7` → exit 0.
- 3.4 `git tag pre-stacked-0922` at `31df8cb` (`<pre-merge>`). Verified: `rev-parse --short pre-stacked-0922` → `31df8cb`. This is the one-revert rollback point.

## STEP-4 — outage window

- 4.1 `date` → `Tue Sep 22 12:27:45 EDT 2026` (`<t down>`). Before 19:55 ET hard clock.
- 4.2 `launchctl bootout gui/501/com.cobalt.aset` (silent). `launchctl print gui/501/com.cobalt.aset` → exit 113, `Could not find service "com.cobalt.aset" in domain for user gui: 501`. DOWN confirmed. Radar was NOT booted out (`<restart set>` = aset only, per 2.5).
- 4.3 `date` → `Tue Sep 22 12:28:19 EDT 2026`. Before 19:58 ET merge clock. `git rev-parse --short HEAD` → `31df8cb` = `<pre-merge>`, no foreign commit. `git merge --ff-only deploy/stacked-0922` → `Updating 31df8cb..d2d82e7` / `Fast-forward`, 19 files (the full stack: 3 stale + 5 ops non-docs, plus docs).
- 4.4 `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` (silent, no `Bootstrap failed: 5`). `date` → `Tue Sep 22 12:29:18 EDT 2026` (`<t up>`). `launchctl print gui/501/com.cobalt.aset` → `state = running`, pid `61238` (≠ baseline `23042`).
- Downtime = `<t up>` − `<t down>` = 12:29:18 − 12:27:45 = **93 s**. Over the 60s target — recorded and ESCALATED, not a failure.

## STEP-6 — close

1. `git -C /Users/cobalt/cobalt tag deploy-2026-09-22` (silent) — tagged after green smoke, `<tag>` = `deploy-2026-09-22` @ `d2d82e7`.
2. `## Deploy table` written above.
3. `## ESCALATE` written above.
4. Stop line below, as the last non-blank line.
5. Report committed by explicit path, two calls, immediately after this edit.

STACKED DEPLOY DONE d2d82e7 · tag deploy-2026-09-22 · branches: 2 · residents down 93 s · /radar 200 · rollback: not used · ESCALATE: 6

