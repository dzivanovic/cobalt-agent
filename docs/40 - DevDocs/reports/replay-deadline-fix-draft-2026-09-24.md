# Replay deadline fix — drafter report, 2026-09-24

## §0 Headline
- Cause (reads only): the 09-24 formations step re-ran `evaluate_member` 82,250 times (11,750 member-instants × 7 evaluable defs; 09-23: × 1). Every call rebuilt the member's own work (bars, working bars, both frames and their lazy atoms). The step is synchronous and uncut, so it ran ≈29.4 min and the `line` step refused at 21:40:06.
- FIX 3: (1) one member prep per scan, shared by every def, byte-identical output; (2) the formations step is cut between scans before the deadline with a named PARTIAL, the line lands, and the job fails at the end; (3) one bar read per ticker in `formation_misses`.
- Prompts: `57-replay-deadline-fix-build.md` (Opus 5.5, `acceptEdits`, GATE EARLY + timing row) and `58-replay-deadline-fix-check.md` (Sonnet 5 hub, Opus 5.5 + Grok). New rule strings: 57 = 3 (the re-pathed `.env` pair + one timing string), 58 = 0.
- No owner item. Whether FIX 1 alone makes the 09-25 run fit is UNPROVEN. FIX 2 makes sure the miss line lands either way.
- ESCALATE: 9. Run 21:52–22:0x ET (`date`).

## Classification (L75)
| # | item | class | file:line | the test that holds it / why |
|---|---|---|---|---|
| 1 | Member-level work rebuilt per def: `_closed_i1`, `bar_row` tuple, `working_bars`, `rth_only`, `ExtensionParams`, `_daily_ok`, `_build_frames` (two `Frame`s whose `LazyAtoms` memoize per frame) → ONE `prepare_member` per (member, instant), frames memoized per `bind_side`, `evaluate_member(…, prep=None)` builds its own through the same function (L3) | **FIX** (FIX 1) | `src/cobalt/radar/evaluate.py:907–970`, `src/cobalt/radar/evaluate_cli.py:196–205`; frames are frozen and their lazy caches take only frame inputs + tunables (`anatomy/frame.py:169–190`, `:229–248`) | T1 (every committed def × member × 3 instants, output byte-identical with and without a shared prep), T2 (`_build_frames` runs once per `bind_side`, whatever the def count: the structural guard against a future setup count), T3 (a wrong-member prep is refused), T4 (`replay_formations` unchanged vs a per-def reference loop) |
| 2 | The formations step is synchronous and never cut; the deadline is only checked between steps, so the run does all the work and then refuses at `line` → cut BETWEEN scans at `deadline − replay.formations_reserve_s`, named in the log, job row (`formation_cut`) and line (`PARTIAL: cut before …`); line lands; job fails at the END, as with archive failures | **FIX** (FIX 2) | `runner.py:26–31`, `:427–438`, `:478–495`; `evaluate_cli.py:185–205`; `line.py:129–138`; `models.py:281–300`, `:451–490`; new tunable beside `tunables.yaml:948` | T5 (the cut stops before the named scan and prints it), T7 (the cut reaches `FormationOutcome`), T8 (the PARTIAL clause; uncut output byte-identical), T9 (cut → reconcile + line written + end-of-run `ReplayError`, `failed_step` None), T10 (no deadline → no cut), T11 (missing reserve → refuses, L1); `test_r1_16_a_synchronous_step_is_not_cut_mid_flight…` keeps every assertion (reconcile stays uncut) |
| 3 | `formation_misses` reads `context.bars_for(ticker)` once per CANDIDATE (197 DB bar-window reads on 09-24) → once per ticker | **FIX** (FIX 3) | `src/cobalt/replay/formations.py:330–332` | T6 (`bars_for` called once per ticker, rows unchanged). Worth ≈2 s at most (21:40:04.138 → 21:40:06.498 covers it plus the reconcile) |
| 4 | FIX 1 alone brings the 09-25 run inside the ≈24 min budget | **UNPROVEN** (L70) | — | Needs a timing run. Upper bound: if the member prep were all of 09-23's 8.4 ms/call, sharing it saves ≤ 6/7 × 7 × 8.4 ms × 11,750 ≈ 830 s → ≈15.6 min. The share of that 8.4 ms that is prep is unknown. `57` D7 (c2) measures it on `cobalt_dev` if that DB holds 2026-09-24 |
| 5 | Lazy `inputs_sha`, or skip building `MemberEvaluation` (with its `consumed_bars` tuple, twice per call) for non-formed results in the replay | **UNPROVEN** | `evaluate.py:919–929`, `:1126` | Size unknown. It would change `evaluate_member`'s result contract that the resident and receipts read, so it widens. Not built |
| 6 | A wall-clock budget test in the suite | **UNPROVEN** | — | Machine-dependent, not provable in CI time. T2's structural count is the provable guard (e) |
| 7 | "Skip a (member, def) after it has formed — only the missed set reaches the line" (b) | **NOT REAL** | `formations.py:19–28`; `evaluate_cli.py:208–211` | A later evaluation can form a NEW formation (new `formed_bar_ts`), which is a separate miss row under the extended key. Every evaluation the step does is needed for the miss set; it is not waste |
| 8 | The job wrapper's `timeout_s: 1800` as a second cause | **NOT REAL** | `configs/cobalt/jobs.yaml:207–212` | The run ended by the deadline check at 21:40:06 (`replay.err:68`), not by the wrapper. After the fix the run ends by 21:35 |
| 9 | K7, K10.1, K10.2 as separate defects | **NOT REAL** (consequences) | `s2-smoke-look-2026-09-24.md` | They are the same failed run and the unwritten line |
| 10 | Smoke look ESCALATE 4 (`replay.log:109` "tonight's 20:30 ET run: done …") | **NOT REAL** | `runner.py:143`, `:291` | It is the archiver PRECONDITION record, printed the same way on 09-22 and 09-23 (desk R95) |
| 11 | Read the resident's stored live evaluations (per-scan `system.radar_score` rows / receipts) instead of re-evaluating the day | **OUT OF SCOPE** | `runner.py:176–236` (the R4 / R1-21 binding: "P2's OWN read-only replay … replay never re-derives") | A design change to a ruled binding. It goes to the design question in ESCALATE 4, never into this fix (L75) |
| 12 | The resident's `EvaluateStage` adopts `prepare_member` too | **OUT OF SCOPE** | `evaluate.py:1571`, `:1796` | Its per-scan cost (≈50 members × 7 defs) is not failing anything. Changing the resident widens the fix |
| 13 | Recover the lost 09-24 miss line by a rerun | **OUT OF SCOPE** | `runner.py:161` (a run started after the backup has no deadline); `archiver_precondition` historical branch `runner.py:144` | It is a production write, so it is the desk's call. On the unfixed tree it takes ≈30 min, uncut |
| 14 | Move the 21:40 backup later | **OWNER ITEM** — not raised | `jobs.yaml` (backup `at`), 09-19 R32 | Only he can move this edge, and the engine-side fix makes it unnecessary |
| 15 | Start the replay earlier than 21:10 | **OWNER ITEM** — not raised | `jobs.yaml:201–213` (21:10 = the end of the archiver's declared Finviz window, R17) | It would collide with the archiver's window. Not needed |
| 16 | Shrink `replay.backup_margin_s` (300 s, `source: ruling`) | **OWNER ITEM** — not raised | `tunables.yaml:948–956` | This is a ruled value. Not needed |

Answers (a)–(e): **(a)** per member-instant × def. The multiplier is the number of evaluable defs (1 → 7), and each call rebuilds the member's work. It is not per formation (197 formations, 3,225 `formed` evaluations) and not in the counterfactual-R reads (≈2 s). **(b)** No unneeded evaluations (row 7). The waste is inside each evaluation (row 1). **(c)** Yes, one: the bar read per formation (row 3). `replay_formations` already caches bars per ticker (`evaluate_cli.py:187–190`). **(d)** Row 2. **(e)** T1/T2/T4 pin equivalence and the structural "once per scan" count. T5/T9–T11 pin the cut. A timing budget is not provable in CI (row 6).

## Where the time goes
- Step order `STEPS = ("movers", "cards", "formations", "line")` (`src/cobalt/replay/runner.py:82`). The deadline is checked only between steps and just before the vault write (`runner.py:293–295`, `:472`, `:480`). `formation_source(...)` is a synchronous call (`runner.py:435`).
- Timeline 09-24 (`logs/replay.err`): movers + cards end with the cards step's last `resolve_window` fallback line at 21:10:37.900 (`:66`). The formations step's only log line is `formation_misses`' own `resolve_window(None, …)` (`formations.py:327`), which runs right after `replay_formations` returns, at 21:40:04.138 (`:67`). The ERROR at the `line` step follows at 21:40:06.498 (`:68`). → `replay_formations` ≈ **1,766 s**.
- Timeline 09-23: cards end 21:10:40.410, the formations `resolve_window` line is 21:12:19.649, DONE 21:12:19.759 (`replay.err:18–39`). → `replay_formations` ≈ **99 s**.
- The counts (`replay.log`): 09-23 `counts={'avoided': 294, 'input_stale': 291, 'not_evaluable': 7182, 'not_formed': 3983}` = **11,750** evaluations (`:95`), with 12 of 13 defs "not evaluable: missing atoms" (`:35–46`) → **1 evaluable def**. 09-24 `counts={'avoided': 1007, 'formed': 3225, 'input_stale': 672, 'not_evaluable': 14242, 'not_formed': 63104}` = **82,250** (`:467`), with 6 not evaluable (`:149–154`) → **7 evaluable defs**. 82,250 / 7 = **11,750** member-instants both nights (235 scans; a 50-name pool). The per-evaluation cost rose from ≈8.4 ms to ≈21.5 ms, because the new defs run their full precondition / anchor / trigger / stop path.
- The multiplier (`src/cobalt/radar/evaluate_cli.py:198–205`): `for member in admitted_at(...)` → `MemberInput(...)` → `for ld in evaluable: evaluate_member(ld, inp, …)`. Inside `evaluate_member` (`evaluate.py:907–970`), before any def-specific line, every call runs `_closed_i1`, `bar_row` over every consumed bar, `canonical_sha256` over bars + daily (per def by design: `def_md5` is in the payload), `working_bars`, `rth_only`, `ExtensionParams.from_tunables`, `_daily_ok` and `_build_frames`. `_build_frames` builds two `Frame`s whose atoms are lazy and memoized PER FRAME (`anatomy/frame.py:243–248`), so each of the 7 defs recomputes ATR / EMA / VWAP / ranges / legs on fresh frames.
- 09-25 exposure: the same 7 defs and pool → the same ≈29 min unless fixed. Tomorrow's 21:10 run fails the same way if the fix is not deployed in the 20:00–21:00 pause.

## Prompts written
| file | seat | launch-line strings vs precedent (`comm -3`, launch line only) | new strings |
|---|---|---|---|
| `docs/40 - DevDocs/prompts/2026-09-24/57-replay-deadline-fix-build.md` | Opus 5.5, `acceptEdits`, worktree `/Users/cobalt/cobalt-wt/replay-deadline`, branch `fix/replay-deadline-0924` from `main` at launch | vs `07`: 24 → 25 tokens (20 allow + 3 deny + mode → 21 + 3 + mode). Only in `07`: `"Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/mover-bars/.env)"`, `"Bash(rm /Users/cobalt/cobalt-wt/mover-bars/.env)"`. Only in `57`: `"Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/replay-deadline/.env)"`, `"Bash(rm /Users/cobalt/cobalt-wt/replay-deadline/.env)"`, `"Bash(COBALT_ENV=dev uv run cobalt radar evaluate --replay 2026-09-24)"`. Mode `acceptEdits` and the `--add-dir` triplet are identical | **3** (ESCALATE 1) |
| `docs/40 - DevDocs/prompts/2026-09-24/58-replay-deadline-fix-check.md` | Sonnet 5 hub, `auto`, cwd `/Users/cobalt/cobalt-wt/agy-trial`, Opus 5.5 + Grok | vs `08`: 18 = 18 tokens (14 allow + 3 deny + mode), `comm -3` EMPTY | **0** |

Placeholders left for the desk: `57` has `R__` ×1 (the SEAT line). `58` has `R__` ×2 (the launch sentence and the THIS-launch gate) and `<tip>` (filled at the BUILT line). The literal STAGGER gate `no other house hub is running` is in `58`, worded as in `45`. `58`'s Grok gate is `45`'s standing R17 / R19 gate, not `08`'s dated extension.

## FOR THE DEPLOY
- Branch `fix/replay-deadline-0924` (worktree `/Users/cobalt/cobalt-wt/replay-deadline`), cut from `main`. It joins the 09-25 stacked set with `cards/stale-score-0922` and `radar/handicap-h1-0922` only if BUILT (`57`) and CHECKED (`58`, `ready … YES`) by then (L43 / L68).
- SEAM: `cards/stale-score-0922` edits the head of `evaluate_member` (it moves `intraday_stale` up and adds it to `base`), `SUPPORTED_EVALUATORS` in `replay/formations.py`, and `tests/cobalt/test_replay_runner.py`. `57` leaves those lines alone, and its T1 and T2 re-run on the stacked tree are the proof. The H1 branch touches none of these files (`git diff --stat main...radar/handicap-h1-0922` over them: empty).
- RESTARTS by rule (L42), to be quoted from the build's `cobalt jobs restarts` table:
  - `com.cobalt.replay` is a one-shot. It picks the change up at its next 21:10 run, so nothing to kickstart.
  - `com.cobalt.radar` restarts: it imports `cobalt.radar.evaluate`, and `configs/cobalt/taxonomy/tunables.yaml` is in its reads. Inside the 20:00–21:00 pause (L43 / L66). The stale-score set already restarts it.
  - Every other resident whose `reads:` names `tunables.yaml` restarts too, named from the table.
- First live proof: the 2026-09-25 21:10 replay logs `F17: com.cobalt.replay DONE` before 21:35:00 ET. `DRC-2026-09-25.md` carries the `drc-misses` unit. Smoke K7 / K10.1 / K10.2 are green.
  - A CUT night instead lands `PARTIAL: cut before …` in the line and ends FAILED. That means FIX 2 worked and FIX 1 alone did not fit: ESCALATE 4's design question.
- Rollback (L54): `git revert` of the merge range, or `git revert -m 2` of the gate merge if the stack ships as a gate branch. There is no migration and no data to undo. The new tunable row leaves with the revert.

## FOR DEJAN
no owner item — the fix is engine-side

## ESCALATE
1. **NEW RULE STRINGS for the desk's ONE list (`57` only):** `"Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/replay-deadline/.env)"` and `"Bash(rm /Users/cobalt/cobalt-wt/replay-deadline/.env)"` (`07`'s R7 pair with only the worktree path changed; every new worktree has needed his word, e.g. R22, R68). Plus `"Bash(COBALT_ENV=dev uv run cobalt radar evaluate --replay 2026-09-24)"`, the timing proof: read-only, `cobalt_dev`, one exact argument. Without the third, `57` records `NOT RUN (string not approved)`, and the only timing evidence is the 09-25 live night. I launch none of them.
2. `ASK DESK: base — `57` is cut from `main` as ordered, but `cards/stale-score-0922` (same set) edits the same `evaluate_member` head. Cut from its tip instead? [22:04 ET]` Safe default taken: `main`, with the seam lines left untouched and named in `57` FIX 1 and `## FOR THE DEPLOY`.
3. 09-19 A2 (`s2-p4-verify-2026-09-19.md:398–420`) called bounding synchronous work "a DESIGN CHANGE". FIX 2 bounds ONLY the formations loop, between scans, as `56` (d) orders. `missed.reconcile` stays uncut, and the pinned test keeps every assertion (only one docstring sentence changes). The safe default is to draft it as a FIX. The check (`58` FOURTH) reads it.
4. UNPROVEN (row 4): FIX 1's saving is not measured. DESIGN QUESTION for the desk if a night still CUTS (or when the 6 non-evaluable defs gain their atoms and the multiplier grows to 13): should the replay read the resident's own stored per-scan evaluations instead of re-evaluating the day (row 11)? That changes the R4 / R1-21 binding, so it goes through a tribunal (L67), never a fix round.
5. New engine tunable `replay.formations_reserve_s` = 120 s (`status: proposed`, `source: dwv`), my proposal. It is not a ruled value (L53 does not cover it). The first nights are the measurement.
6. The 09-24 miss line is lost (K10.1 / K10.2). It can be recovered by a desk-run production rerun of 2026-09-24 started after the day's 21:40 backup (no deadline, `runner.py:161`). That is OUT OF SCOPE here: a gated production write, and ≈30 min on the unfixed tree.
7. If the fix misses the 09-25 deploy, the 09-25 21:10 run fails the same way and 09-25's line is lost too (desk R95).
8. `57` touches `src/cobalt/radar/evaluate.py`, which is shared with the live resident. The resident's path is unchanged by construction (`prep=None`), and T1 proves the outputs byte-identical. It still restarts `com.cobalt.radar` by rule, inside the pause.
9. The smoke's K7 stays red on a CUT night by design (L1): the line lands, the job fails. No smoke change is drafted.

REPLAY DEADLINE FIX DRAFTED · cause: the formations step re-evaluates every scan × member × def (82,250 calls, 7 defs) rebuilding the member's work per def, synchronously and uncut past 21:35 · FIX: 3 · NOT REAL: 4 · UNPROVEN: 3 · OUT OF SCOPE: 3 · OWNER ITEM: 3 · prompts: 2 · new rule strings: 3 · ESCALATE: 9
