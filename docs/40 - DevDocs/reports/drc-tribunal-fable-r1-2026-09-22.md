BLIND: I did not read the houses' folder or the hub's report.
RUN DATE: 2026-09-22 (first `date` 16:22:22 EDT; report written 16:33–16:4x EDT)

# drc-tribunal-fable-r1-2026-09-22 — the Fable seat, round 1 (seat `drc-tribunal-fable-0922`, `claude-fable-5-1`, R73)

## DIGEST FOR THE DESK

- Closing line: **TRIBUNAL R1: BUILD AFTER E1's real export is in hand and vendor names leave every identifier.**
- Authorization proven: R73 (`cto-2026-09-22.md:37`, committed `938f1f5`) carries all three literals; R66 `ONE IMPORT PLACE`, R72 `THE DIFF MODEL IS RULED`, 09-21 R46 seat rule; proposal + §13 committed `c52133c`; ten launch-line strings each count 1 in `22-draft-setups-tribunal.md`; this session runs `claude-fable-5-1`.
- T1 ADOPT WITH — the `_imports/` bytes have no writer in the design: `VaultWriter` is text-only and refuses to create folders; the import store owns them, write-once.
- T2 ADOPT — synchronous in the request, event state on the day row; a one-shot job needs a poller nobody has.
- T3 ADOPT WITH — the 21:10 run stores the RENDERED line body in its result; the build writes that body through `replay.line` with the replay's writer name. No re-render, E6 dissolves.
- T4 ADOPT WITH — no vendor name in any identifier (L31): `drc_executions` not `drc_fills`/`das.py`; source value `trading_log`; the event is the DAY row of `drc_rows` (three tables hold).
- T5 ADOPT WITH — DRC keys join `OPTIONAL_SETTING_KEYS`, never `SETTING_KEYS`; a rule binding carries the bound rule text's hash; text changed → `untested`, loud.
- T6 ADOPT WITH — one unit per trade (`trade:<id>`), not one `tickers` unit; a vanished trade's unit is untouched and marked orphaned.
- T7 ADOPT WITH — a C2 refusal never fails the build: recorded as an unresolved leg mismatch (A31), shown, carried; S-C1 rides C1 with a nullable `import_id` (no FK) and the `trading_log` source value.
- T11 ADOPT WITH — the carry seeds only from a CONTIGUOUS import chain; a missing prior trading day = FAILED naming the day; he backfills by the form date.
- T8 ADOPT — two `{{date:YYYY-MM-DD}}` lines in his template, nothing else; repo copy dies.
- T9 ADOPT WITH — ONE source = `"user".trader_settings`: grade dollars from the existing `aset.sheet_modes` row, the daily stop from one new optional key family; `daily.md.j2:18` becomes a variable rendered from it.
- T10 ADOPT WITH — K10.1/K10.2 keyed to "DRC built for that day, else `line: pending`"; K12.2 replaced by an SQL check on the day row (`failed` = red, absent row = R66 fact).
- (a) ADOPT WITH — F1–F34 hold; §1 missed `smoke/checks.py:122-129,:566` (reader), `prefill/cli.py:22,:49` + `pyproject.toml:64` (the `prefill drc` entry D3 must remove), `ops/README.md:30`.
- (b) ADOPT WITH — dispositions right; second writer/template gone after D3 IF `prefill/cli.py` loses `drc`; a PRE draft never writes a line the 09:00 reader's `Grade:`/`Goal:` regex matches.
- (c) ADOPT WITH — orphaned screenshot bindings on a superseding CSV listed, never silently re-bound; LAN reach = ops item, not a precondition.
- (d) ADOPT WITH — `cobalt drc build --date D` refuses when no inputs-placed day row exists; uploads in `market_reset` refused (O14 default).
- (e) ADOPT WITH — missing/renamed column = FAILED + `degraded`; extra column = parse + `degraded`; boundary: NONE.
- (f) ADOPT WITH — same text as T7; "diff shown, adjustment pending" before C2 is L1-clean.
- (g) ADOPT WITH — image check by magic bytes, no `pillow` import (transitive); `python-multipart` direct at the locked 0.0.22; embed = X9.
- (h) ADOPT WITH — same text as T5; nothing flips a checkbox or reaches the 09:00 proposal, a grade or a size.
- (i) ADOPT WITH — `drc_rows.inputs` SNAPSHOTS card fields, typed cells, the `missed` row ids and the leg row ids it used; nothing referenced by id alone.
- (j) ADOPT WITH — D3 adds `prefill/cli.py`, `ops/README.md`, `smoke/checks.py`; `jobs.yaml:310`'s `no_resident_reads` row for `rules.yaml` moves to `com.cobalt.aset`'s `reads:`; migration number not assumed; hours UNVERIFIED.
- Experiments named: 12 (X1–X12; E1–E5, E7 kept; E6 and E8 changed; X9–X12 new).
- Owner items: O1–O18 as framed (O2 and O5 sharpened) + O20 (a CLOSED card the export shows open), O21 (gross vs net on the summary line).
- WITHDRAWN sentences: 3. WRONG FACTS: 2 (+1 moved line). ESCALATE: 4.

## Rulings

### T1 — import place `/drc` on the ASET app + files in the vault's `_imports/`
**ADOPT WITH** — replace §2 "Files land byte-for-byte under the vault's `_imports/drc/<YYYY-MM-DD>/` (Cobalt-owned by R3, F30; home = O2), sha256 recorded in `"user".drc_imports`" with: "Files land byte-for-byte under the vault's `_imports/drc/<YYYY-MM-DD>/` (Cobalt-owned by R3, F30; home = O2). They are written by the import store, not by `VaultWriter`: write-once (an existing path whose bytes differ is refused, never overwritten), atomic tmp+rename in the same directory, the `_imports/drc/<date>/` folder created by the import store (R3 ownership; `VaultWriter.create_if_absent` refuses to create folders, `writer.py:583-587`, and writes text only, `:470`), gated by the same `assert_writable` call the vault writer makes (`writer.py:409-414`). The `"user".drc_imports` row (date, kind, name, size, sha256, trade key, parse status, supersedes) is its L28 version record."
1. The proposal names no writer for the bytes. `create_if_absent(path, template: str)` opens the file `"w", encoding="utf-8"` (`writer.py:470`) — a PNG cannot pass through it; `upsert_unit` is markers-only.
2. `_imports/` does not exist in the vault (`ls` → no such directory, F30 holds), and `create_if_absent` raises "refusing to create vault structure" on a missing parent (`:583-587`). Without a folder-creating owner the first drop fails.
3. Failing scenario: 2026-09-23 16:02 he drops `<export>.csv` for the form date 2026-09-23; the handler calls the vault writer with the bytes → `TypeError`/refusal at `:583`; nothing recorded, page shows FAILED with a Python error, not a reason.
4. `_imports/` is inside the vault, so `assert_write_target`'s production guard (`writer.py:178-215`) applies as-is to a text path; the import store reuses `assert_writable` for the `market_reset` gate (O14) and the production declaration is the process's (`ops/start_aset.sh` exports `COBALT_ENV=production`).
5. Price: inside D2's `src/cobalt/drc/imports.py`, already listed; no new file, no chunk change. L40: one expert (the import store) for the side effect "import bytes"; the DRC note stays `VaultWriter`'s.

### T2 — build synchronous in the request vs a one-shot job
**ADOPT** (synchronous, bounded by a timeout, result shown; the CLI runs the same function).
1. A one-shot job would need a trigger: launchd has no "an HTTP upload happened" event, so it would be a poller — a plist, a `jobs.yaml` row, a `cobalt_jobs` receipt and a smoke row, for one trader dropping two files. The request already holds the event.
2. L18 is met by the state on the day row (T4): `pending → running → done | failed`, written before the build starts and finalized in the same handler.
3. L34 ("every spawn is a job row") is about spawns; an in-request function spawns nothing. The smoke keys on the day row (T10).
4. The ASET app already writes the vault in-request through `VaultWriter("aset.daily_note")` (`aset/daily_note.py:151-154`, :184-185) — the pattern exists.
5. Consequence for RESTARTS: the build imports `prefill.rules_gen` (`regenerate_rules_config`, `rules_gen.py:128`) into `com.cobalt.aset` — see (j).

### T3 — the 21:10 miss line on an absent note → `pending`, rendered later by the build
**ADOPT WITH** — replace §8's "the DRC build, when it runs later, calls the SAME `render_line` + `write_miss_line` for that date from the replay's persisted rows (`missed.current`, run result) — whether every `render_line` input is persisted is UNPROVEN → E6" with: "`line_step` on an absent note (`DrcNoteAbsent`, `line.py:155-163`) renders the body as today, stores it on the run result as `line_body` with `line_action = 'pending_no_drc'` (`ReplayResult`, `replay/models.py:453-454`, persisted in the job receipt), and ends green. The DRC build, when it runs later for that date, reads that stored body and writes it through `replay.line.write_miss_line(path, body, writer=VaultWriter(replay.line.WRITER))` — the replay's own function and writer name (`line.py:54`, :167-172), no re-render. A run whose receipt carries no `line_body` (failed before the line step) → the build upserts `miss line: not available — replay run <id> failed at <step>`."
1. `render_line` takes eight inputs (`line.py:82-92`): `card_rows`/`mover_rows`/`formation_rows` are `missed.current(...)` rows (persisted), but `settings` (`BenchmarkSettings`, his hand can change it), `formation_replay`, `input_stale`, `formation_suppressed`, `formation_input_stale` live on `ReplayResult` only. Storing the derived string removes the question.
2. Failing scenario for re-render: 2026-09-24 21:10 replay runs, no DRC; 2026-09-25 09:30 he runs `cobalt settings load --optional` changing `radar.benchmark.min_move_pct`; 2026-09-25 16:10 he drops 09-24's files; a re-render prints a movers segment the 21:10 run never produced — a figure with no run behind it (L57).
3. One writer identity for `drc-misses/miss_line` (`replay.nightly`) whatever process writes it — question (b)'s "a unit written by two writers" is answered: one function, one name, two clocks.
4. Price: one field on `ReplayResult` (no migration — the result is JSON in the receipt), ~10 lines in `runner.py:443-455`, the build's call in D3. Smaller than E6.

### T4 — table names and the `public.trades` collision
**ADOPT WITH** — replace §9 D1's "`"user".drc_imports` (+ event state), `"user".drc_fills` (DAS executions; name vs declared `fills` = T4), `"user".drc_rows`" with: "`"user".drc_imports` (one row per dropped file), `"user".drc_executions` (the trading-log export's execution rows — not `fills`: v3 keeps `fills` declared for him, O16, and a second fills-named table would be a second concept under one name), `"user".drc_rows` (one row per trade and ONE per day; the day row carries the build event: state `pending → running → done | failed`, the import ids and sha256s, started/finished, failure text). No fourth table. No vendor name in any identifier, schema, enum or config key (L31): module `src/cobalt/drc/trading_log.py`, `legs.source` value `trading_log`, degraded flag `trading_log_shape`, column `import_id`."
1. L31 (LAWS.md:173-175): "Person or vendor names never in code identifiers, schema, config keys, enum values or system design docs". The proposal's `das.py`, `das_import_id`, source `das`, `degraded: das_export_shape` are vendor names in identifiers. Today's code has the vendor only in comments and messages (`grep -w DAS`: `cli.py:215`, `daymode/match.py:23`, `aset/web.py:1142`… — strings, no identifier).
2. `public.trades` and `order_fills` are frozen old-tree names (`placement.py:143`, `:147`); `PLACEMENT` and `OLD_TREE_PUBLIC_TABLES` are keyed by bare name, so a new `trades` would collide in the placement test. `drc_*` names avoid it — holds.
3. §3 says the event goes into "`drc_imports`' event table" while the design counts three tables (§9, the hub's question): the day row of `drc_rows` already exists for the streak (§7) — the event state belongs on it. Zero new files; one table fewer than the ambiguous reading.
4. `DECLARED_TABLES` (`placement.py:106-112`) names `drc_rows` USER already; `drc_imports` and `drc_executions` join it in D1 as the proposal says.

### T5 — the rule engine as an L7 shadow beside his checkboxes
**ADOPT WITH** — append to §5 "Rule engine": "The SPEC §7 key families (`account.*`, `grades.*`→see T9, `windows.*`, `limits.*`, `goal.*`, `sleep_trigger.*`) and the `rule → checker` binding register in `OPTIONAL_SETTING_KEYS` (`settings/models.py:44-56`), never in `SETTING_KEYS`: a missing DRC key fails the DRC build naming the key and touches no sheet reader. Each binding row carries `rule_id`, `checker_id` and `text_sha256` of the rule text at binding time (the dry-run prints them for him). At build, `regenerate_rules_config()` re-parses `Rules.md`; a bound rule whose current text hash differs renders `untested: rule <n> text changed since binding <date>` — never re-bound silently."
1. `_parse_rules_md` numbers rules by ORDER (`rules_gen.py:88-108`: `rule_number += 1`, `id=f"rule_{rule_number:02d}"`). An inserted line shifts every later id.
2. Failing scenario without the hash: 2026-09-26 he inserts a new rule as line 3 of `Rules.md`; the binding `rule_04 → checker:re_entry_written_info` now points at yesterday's rule 3's text; the engine renders `pass` for a rule that says something else. His checkboxes are unaffected (they are text), so the shadow disagreement reads as HIS error.
3. `SETTING_KEYS` is required by `TraderSettings._build` (`models.py:120-127`): a new required key with no row breaks `load_sheet_modes_config()` (`aset/config.py:251-275`) and with it every `/size` request until his hand loads — the `BENCHMARK_KEY` comment (`models.py:44-49`) records exactly why optional keys exist.
4. Failing scenario without the registry: D4 deploys 2026-09-25 20:30 with `drc.limits` in `SETTING_KEYS`; 2026-09-26 07:10 `/size` → `TraderSettingsError: missing setting(s)`; no card can be sized until he runs `cobalt settings load`.
5. No path flips a checkbox: `format_rules_checkbox_block` (`prefill/daily.py:165`) writes `- [ ]` lines inside `drc-rules/rules_check`; the engine's `drc-rules/rule_engine` is a new unit id inside the same section (`upsert_unit`: "New id -> appended inside the section", `writer.py:651-653`). The 09:00 reader reads `Grade:`/`Goal:` only (`daymode/drc.py:112-113`).
6. Price: ~20 lines in D4 (hash column in the binding model + one comparison); zero elsewhere.

### T6 — his answers inside a Cobalt unit across rebuilds
**ADOPT WITH** — replace §6 "`drc-trades/tickers` REBUILT: one block per DAS trade" with: "`drc-trades/tickers` retires; one unit per trade, id `trade:<trade_id>` (`NAME_RE` admits `:` and `-`, `markers.py:31`), each carrying the derived table, matched card, legs, flags, the screenshot embed, then his blank voice lines (per R71's writers). A rebuild upserts each trade's unit by id; a trade absent from the superseding export is NOT deleted — its unit is upserted with a first line `not in the trading-log export of <date> (import <id>)` and its body otherwise unchanged, so his text inside it stays where he typed it. The day's counts line is its own unit `drc-trades/counts`."
1. Today his voice lines live INSIDE the one `tickers` unit (`_render_entry`, `prefill/drc.py:196-214`); a rebuild replaces the whole unit body and relies on the three-way merge to carry his insertions. E8 is the proposal's own UNPROVEN.
2. Failing scenario with one unit: 2026-09-24 16:05 build lists trades A, B (two blocks); 17:00 he types a lesson under B's `Trade Notes:`; 17:20 he drops a corrected export in which B's opening execution carries a different time → B's block moves/changes → the merge sees a Cobalt change and a human change in the same lines → override row and his line may land under A's block or be listed as orphaned — position lost.
3. With per-trade units the merge is per unit: A's unit is untouched, B's unit (same `trade_id` if pairing keeps it; a new id if not) is marked, his text stays in B's unit. `upsert_unit` touches only the named unit (`writer.py:644-680`).
4. Price: zero new files; the unit loop in D3 iterates trades; E8 shrinks to one unit (X8).

### T7 — the DAS-vs-legs reconcile per R67; seams S-C1 / S-C2 / S-C2b
**ADOPT WITH** — (i) replace §3 step (4)'s "A refusal from C2's writer (e.g. a CLOSED card moved off 0) = build FAILED naming the card — never forced" with: "A refusal from C2's writer is never forced and never fails the build: it is recorded as an UNRESOLVED LEG MISMATCH (a `drc_rows` row: card id, leg ids, the export rows, the refusal text), rendered in the reconcile unit as `unresolved: card <id> — <refusal>`, and carried forward on every later DRC (A31) until resolved; how a CLOSED card the export shows still open is reopened is his (O20)." (ii) S-C1 reads: "C1's `legs` DDL adds the value `trading_log` to the `source` CHECK and a nullable `import_id bigint` (no FK — the DRC's table may land after C1; the store checks the id exists when it writes), so a correction names its source (L57, R67 (3))." S-C2 / S-C2b: ADOPT as written with the same rename.
1. R67 (3): "everything should be adjusted to what Cobalt thinks to actually what DAS says" — and R67's desk reading B = the Fable seat's N, in which "a shares change is refused if … it moves a CLOSED card's running off 0" (v3 :150). Both are his; the proposal makes their meeting point a permanent build failure.
2. Failing scenario as written: 2026-09-24 10:15 he taps flat on card 812 (running 100 → 0, CLOSED) while the export later shows exits totalling 60; 16:05 he drops the file; the correction would put running at 40 on a CLOSED card → refused → `build FAILED: card 812` → no DRC-2026-09-24; re-dropping fails identically; no path in the design can ever build that day.
3. With (i) the DRC builds, loud: the mismatch is a fact on the note and a row he can act on; L1 is met (nothing plausible-empty — the refusal text is printed), R66 is met (inputs placed → DRC).
4. S-C1 is R67's own consequence, not a change to the S3 FINAL: v3 + R67 IS the FINAL, and R67 orders "the DAS source named". v3's `price_source` CHECK ∈ {last_poll, typed, dm} and `source` ∈ {panel, sheet, dm} (v3 §3) have no slot for it. One migration set carries `legs` (v3 :139, R2-3 CONVERGED) — so the slot lives in C1's file. A FK to `drc_imports` would order C1 after D1; a nullable id without FK does not.
5. "Holding 50" stated, export 60: the statement row stays (history), a correction row `corrects` the current exit leg with source `trading_log`, `import_id` = the drop; before/after rendered. An exit Cobalt never recorded: a new exit leg posting `running_before` = the running computed under the card lock (N's rule); if it brings running to 0 the card CLOSES in the same transaction (N) — the export closes the card; that is R67. A Cobalt leg with no export execution: a correction to 0 shares is a shares change — under N it is refused unless Σ stays consistent → (i) applies. A card filled before C1: entry leg from the export, `stop_in_force` from the card's stop (source named). Before C2 is merged: "diff shown, `adjustment pending (legs writer not built)`" is loud and L1-clean.

### T11 — the open-position carry
**ADOPT WITH** — replace §3 "The NEXT day's pairing SEEDS each symbol with the carried position before reading that day's executions" with: "The next import for date D seeds each symbol from the open positions in the `drc_rows` day row of the PRIOR TRADING DAY (exchange calendar) ONLY IF that day has a `done` build; a prior trading day with no import → `FAILED: open-position carry broken — no trading-log import for <D-1>`, and the page says `drop <D-1>'s export first`. He may drop any date (the form date). A day whose prior day carried nothing open needs no chain. If E1 shows the export lists overnight positions itself, the file is the seed and the carried row is the cross-check."
1. Failing scenario as written: 2026-09-23 close, 50 shares open in symbol S (carried); 2026-09-24 he closes S at 09:45 but drops no files (lawful, R66); 2026-09-25 he drops files with no S executions → the seed says 50 open → the DRC shows a "continuing open position" he closed yesterday, forever. If 09-25 has new S buys they stack on a phantom 50 and the FIFO exits pair wrong.
2. A DAS day-file starting flat is UNPROVEN (E1/X1) — hence both branches.
3. One `trade_id` across days: holds — the trade opened 09-23 keeps its id while the carried row names it; realized P&L per day; `unrealized: not computed` — no mark invented (L57, L53).
4. The calendar exists: the smoke uses `last_trading_day` (`s2.yaml:384`), so "prior trading day" is a function the codebase already has (UNVERIFIED — its module not opened).
5. Price: one check before seeding, in D1; the page message in D2.

### T8 — the template reader: date token only, repo copy dies
**ADOPT.**
1. His template has exactly two `{{` lines (count 2), both `{{date:YYYY-MM-DD}}` (`5 - Templates/DRC.md:7`, `:17`) — F5 holds; anything else `{{` = loud failure, as proposed.
2. His template carries no `### Cobalt Rules Check` heading (headings :5–:175; the repo copy has it at `drc.md.j2:132`) — F4 holds; `RULES_PLACEMENT = None` lands the rules unit at the end (`prefill/drc.py:347`), `TRADES_PLACEMENT` anchors `### Catalyst + Set Up + Trades` (`:343`, his template :58), `_risk_span` anchors a `Risk Parameters:` line (one in his template) or the PnL heading (his :49) — E3 proves the landing on the dev vault.
3. One template, one creator: `create_if_absent` from `drc.md.j2` at `prefill/drc.py:427` dies with the create step; `TEMPLATES_DIR` keeps `daily.md.j2` for the daily prefill (`prefill/daily.py:220-221`) — not a DRC path.
4. Obsidian's `this.file.cday` dataview (`drc.md.j2:9-14` mirrors his) on a note created the morning after = X10, an experiment, not a defect.

### T9 — one source for the daily stop / grade dollars (mechanism; O5's value is his)
**ADOPT WITH** — the design states: "ONE source = `"user".trader_settings`, loaded only by his hand (`cobalt settings load`, `settings/cli.py:1-7`). Grade dollars: the existing `aset.sheet_modes` row (`SheetModesConfig.dollars_for`, `aset/config.py:222`; read from the DB, `:251-275`) — no `grades.*` key is created. Daily stop: one new optional key family for SPEC §7 `account.*` (`OPTIONAL_SETTING_KEYS`). `configs/cobalt/templates/daily.md.j2:18`'s literal becomes a template variable rendered from that key by the 05:15 prefill (it already loads sheet modes, `prefill/daily.py:202`); an absent key = the daily prefill FAILS loud. `Rules.md` rules 2/12 stay his text, displayed by the checkbox block; no engine reads a number out of a rule sentence."
1. Four sources today (the author's ESCALATE 1): `daily.md.j2:18` (a committed literal — L32 user data in git, L53 a cap in a config file), Rules.md text, `aset.sheet_modes` (already in `trader_settings`), SPEC §7. Only one is a DB row his hand loads.
2. No daily-stop key exists in code today (`grep -i daily_stop` in `aset/config.py`, `prefill/daily.py`, `settings/models.py` → one comment, `aset/config.py:9`): the key family is new; `grades.*` would duplicate `aset.sheet_modes` (L3).
3. Failing scenario without one source: he changes his daily stop in `Rules.md` on 2026-09-26; the 05:15 note still prints `daily.md.j2:18`'s literal; the engine's rule-2 checker reads SPEC §7's value from wherever D4 put it — three numbers on one day, two of them stale.
4. Price: D4 adds the key model and the template variable (`prefill/daily.py` joins D4's file list — one file); the literal leaves git.

### T10 — S2 / S3 smoke re-keyed to the input-driven build
**ADOPT WITH** — replace §8's "S2 smoke `s2.yaml:383-398` (miss unit present): keyed to a built DRC, else `pending`" and "`s2.yaml:436-438` replaced by a `drc build` check" with: "K10.1 (`vault_unit`, `s2.yaml:381-387`) and K10.2 (`sql`, `:389-398`) run only when the `drc_rows` day row for `last_trading_day` is `done`; otherwise they expect the replay receipt's `line_action = pending_no_drc` (T3). K12.2 (`job_row com.cobalt.prefill-drc`, `:436-438`) is replaced by an `sql` check on the day row: `failed` = red; `done` = green; no row = `DRC: no inputs for <day> (R66)` reported as a fact, not a pass. Charter/ladder `15:41` wording = O18."
1. `s2.yaml`'s kinds today are `vault_unit`, `sql`, `job_row`; a conditional check needs either a new kind or two rows with a precondition — `smoke/checks.py` joins D3's file list ((j)).
2. Failing scenario as written: 2026-09-24 no inputs (lawful); 21:10 replay stores `pending`; the S2 smoke that night runs K10.1 → unit absent → RED; the deploy gate (L68/L43) reads a red smoke for a lawful day.
3. `smoke/checks.py:122-129` resolves the DRC path from the same two config values (`prefill.yaml:9-10`) — stays valid.

### (a) THE FACT BASE
**ADOPT WITH** — §1 holds (F1–F34 checked against the files, below); add to §1: "F35 `smoke/checks.py:122-129`, `:566` reads the DRC note for K10.1 (a reader) · F36 `prefill/cli.py:22`, `:49` calls `run_drc_prefill`; `pyproject.toml:64` registers the `prefill` console script — the hand path `uv run prefill drc` (D3 removes the `drc` subcommand) · F37 `ops/README.md:30`, `:145`, `:157` document the 15:40 job (D3 edits)."
1. Verified: F1 (`plist:24-29`, `:50-54`) · F2 (`drc.py:270-272`, `:427-432`; `config.py:30`) · F3 (`:12-16`, `:441-449`) · F4 (his template headings end at :175 with no rules-check heading; `drc.md.j2:132`; note :228) · F5 (2 token lines; 1 `Risk Parameters:` line) · F6 (`:321-347`) · F7 (`:559-619`, `:669-673`) · F8 (`:387-416`) · F9 (`:457-528`, `:644-661`) · F10 (`line.py:28-34`, `:155-172`; `replay.plist:38-42` = 21:10) · F11 (`runner.py:443-466`) · F12 (`daymode/drc.py:85-139`; `daymode-propose.plist:51-55` = 09:00) · F13 (`web.py:1-4`, `:83`) · F14 (`:947`, `:1048`; also `:1147`, `:1190`, `:1247`, `:1282`) · F15 (`uv.lock:2879` under `mcp` :2866, `:2789` under `matplotlib` :2779; no multipart in `pyproject.toml`; no `pyobjc`/`pytesseract` in the lock) · F16 (`aset.yaml:39-41`; `aset.local.yaml:28` `bind: lan`; `__main__.py:28-50`) · F17 (`ladder:610`) · F18 (`store.py:292-322`, `:268`) · F20 (`rules_gen.py:37-107`, `:128`) · F21 (`daily.md.j2:11-13`, `:16`) · F22 (`daily.md.j2:18` — a dollar line exists; value not recorded here) · F23 (`placement.py:106-112`, `:143`, `:147`) · F24 (`0012` on `bars/chunk-2-0920` `02d67a6`; `0013` on `setups/seven-0921` `61a283f`; main ends `0011`; main head `49c0871`) · F25/F26 (v3 `:17-34`, `:110-170`, `:228`, `:237` "Nothing reads DAS") · F27 (`s2.yaml:381-398`, `:436-438`; `jobs.yaml:181-186`) · F28 (`charter:171`; `ladder:612`, `:616`) · F29 (`app.py:10`, `:21-31`, `:47-68`; `index.html:63`, `:73`; `app.js` not opened — UNVERIFIED) · F30 (`vault.py:99-101`; `ls _imports` → none) · F33 (R67 now at `cto-2026-09-22.md:43`, not :35 — rows were inserted above) · F34 (v3 §3 table).
2. Not checkable from reads: F19 (his daily note), F31 (absence on the host), F32 (E1).
3. §8 names `jobs.yaml:310` — holds (`:310` `readers: [com.cobalt.prefill-daily, com.cobalt.prefill-drc]`).

### (b) L3 — ONE DRC WRITER
**ADOPT WITH** — append to §8: "`prefill/cli.py` loses its `drc` subcommand (`:22`, `:25`, `:49`); `DRC_JOB` goes with it. A PRE draft (A5, A7, A16, A18, A24) never writes a line the 09:00 reader's regexes match (`daymode/drc.py:112-113`: a line beginning `Grade:` or `Goal:`); the reader stays unchanged."
1. Dispositions right: the 15:40 job's create step dies (R66 b); with no note `upsert_unit` refuses (`writer.py:669-673`) so the plist retires whole; the 09:00 reader is untouched; the miss line per T3.
2. After D3: one creator (`drc/build.py` via `create_if_absent` from HIS template), one template (`drc.md.j2` deleted), one writer per unit (`drc-misses/miss_line` = `replay.nightly`, T3). `radar/propose.py:753` and `aset/daily_note.py:184` call `create_if_absent` on other notes — not DRC paths.
3. Failing scenario without the `prefill/cli.py` change: 2026-09-25 16:00 someone runs `uv run prefill drc` (pyproject.toml:64 still ships it); `run_drc_prefill` → `_render_template` → `TemplateNotFound` (the j2 is deleted) — loud, but a dead command shipping in the console script is the second path L3 kills on sight.
4. The 09:00 reader's goal test is "contains a digit" (`daymode/drc.py:129-130`). Failing scenario: A5 PRE writes `Goal: <1% goal text with a digit>` under `### My GOAL for today:` on 2026-09-24; he never edits; 2026-09-25 09:00 the reader finds a filled goal → no step-down for a DRC he did not write. The constraint keeps `Grade`/`Goal` lines his.
5. Night with no DRC / DRC built after 21:10: walked in T3 — the 21:10 run stores the body; the build writes it with the replay's writer name; stored inputs = the receipt.

### (c) THE IMPORT PLACE ON THE ASET APP
**ADOPT WITH** — (T1's writer text) and append to §3 "Re-import": "A superseding export re-validates every screenshot binding by trade key: a binding whose trade key no longer exists is listed as `orphaned screenshot: <file> (was trade <key>)` and counts as NOT bound — never silently re-bound to another trade, never deleted. LAN reach from the trading PC (E7) is an ops item after the build; the page is used from the Mac until proven."
1. L3 one web surface — `aset/web.py:83` is the one FastAPI app, already writing the vault (`aset/daily_note.py:151-154`). L40: import store owns `_imports/` bytes + `drc_imports` rows; `drc/build.py` owns the note through `VaultWriter`; C2's writer owns `legs`. Three side effects, three owners.
2. Multipart: `request.form()` is used six times (`web.py:947` …); `python-multipart` 0.0.22 is in the lock (`uv.lock:4460`) via `mcp` — making it direct changes `pyproject.toml` only; E5 proves it in the dev app.
3. Day binding: form date + filename date + every execution's date must agree, else FAILED naming the file — holds. Failing scenario for bindings: 09-24 16:05 export v1 → trades A, B; he binds `b.png` to B; 16:20 export v2 changes B's opening time → B's key changes → `b.png`'s binding dangles; without the sentence the page could show `2 of 2 bound` (stale) and build.
4. Half-imported day: a FAILED parse leaves a `drc_imports` row with `parse status = failed` and no day-row state change → `waiting for: trading-log export` — holds.
5. Production loads `configs/dev/aset.yaml` unless `aset.local.yaml` exists (`aset/config.py:41-42`; `start_aset.sh` sets only vault/env) — which one production has is E7/X7.

### (d) THE TRIGGER
**ADOPT WITH** — append to §3: "`cobalt drc build --date D [--dry-run]` REFUSES (`no inputs-placed day row for D`) when the day row is absent or not `pending|failed`; it never creates a note from nothing. The `market_reset` refusal applies to the upload itself (the same `assert_writable`), with the reason on the page."
1. "Both placed" is deterministic code after each upload (L2); the state lives on the day row (T4). It is a request side effect with typed state, not a second watcher process — smaller, and L18-complete.
2. Paths that still create a note: the 15:40 job until D3 lands (an interval — L43's one-deploy shape lands D2 and D3 together); `uv run prefill drc` (killed in (b)); the miss line never creates (`line.py:167-172`); the CLI per the sentence above.
3. Re-import idempotent under L28: `create_if_absent` → `skipped_exists` (`writer.py:568-578`); units by id; human wins + override rows (`:644-661`). Vanished trade = T6.
4. `market_reset`: refuse (O14 default) — accept-and-defer would need something to fire after 21:00, and no resident polls; a queue is a bigger mechanism than a refusal with a reason. L1-clean: the page says why.
5. Failing scenario for the CLI as written ("runs the SAME function on the stored event"): 2026-09-27 (Saturday) a smoke or a hand runs `cobalt drc build --date 2026-09-26` with no drop that Friday; if the CLI computes "placed" itself from the folder, an empty folder could build an empty DRC — the refusal sentence closes it.

### (e) THE DAS EXPORT PARSER
**ADOPT WITH** — replace §4's fail-loud cell wording "a header change = `degraded: das_export_shape` flag (L9)" with: "a MISSING or RENAMED column = the import FAILED naming file and line (L1) AND the `trading_log_shape` degraded flag raised (L9); an ADDED column = parsed by header name AND the flag raised; the flag clears on the next import whose header set equals the fixture's." Boundary answer: **NONE.**
1. Columns by header NAME, FIFO per symbol/direction, close at 0, partials, mixed-date = FAILED — holds. A position through 0 in one execution = FAILED until E1 says the export books it; if it does, the failing scenario is a lawful reversal (long 100 → sell 200) blocking the day's DRC — X1 decides, not this seat.
2. L45: fixture = the real export's shape, values stripped (companion ruling); consent O15; a header change must fail a test — the fixture's header set is asserted exactly.
3. CLAUDE.md boundary: every path reads a file he exported and dropped by hand (R66 a); the code today reaches no platform (`grep -w DAS` → comments and messages only). NONE.
4. Carry: T11.

### (f) THE RECONCILE AGAINST S3's LEGS
**ADOPT WITH** — T7's text (i) and (ii).
1. Walked in T7: stated 50 / export 60; an exit Cobalt never recorded; a Cobalt leg with no execution; a pre-C1 card; a CLOSED card the export shows open (the deadlock and its resolution).
2. S-C1 changes no ruled behaviour of v3 + R67; it gives R67 (3) its slot. S-C2 / S-C2b describe what C2's writer must accept — the same lock, the same refusals (N). Whose migration: C1's set (v3 :139).
3. Until C2: "diff shown, `adjustment pending (legs writer not built)`" is loud and L1-clean; before C1 the unit says `legs: not built` and compares export trades to cards only.

### (g) THE TRADEZELLA SCREENSHOT
**ADOPT WITH** — append to §4's screenshot row: "`not an image` is decided by the file's magic bytes (PNG `89 50 4E 47`, JPEG `FF D8 FF`) — no image library is imported; `pillow` stays transitive." 
1. MVP reads nothing from the image; B26/B25/C12 name TradeZella as the source HE TYPES FROM (typed cells, O1) — consistent with the diff rows as ruled (R71/R72).
2. New dependency: none — holds for `python-multipart` (direct at the locked 0.0.22; lock unchanged). `pillow` is in the lock only under `matplotlib` (`uv.lock:2779-2789`): importing it would make the design depend on a package it does not declare. Magic bytes are deterministic and dependency-free.
3. Embedding `![[_imports/drc/<date>/<file>]]`: whether Obsidian resolves a wikilink into an underscore-prefixed folder under his attachment settings = X9.

### (h) THE RULE ENGINE IN SHADOW (L7)
**ADOPT WITH** — T5's text; and: "No engine result is read by `daymode/drc.py`, `daymode/propose`, `cards/scoring.py`, `radar/evaluate.py` or `aset/engine.py`; the DRC's units are the only consumer this slice."
1. Rules as data through the existing parser (`rules_gen.py:128`, holds); thresholds from `trader_settings` (T9); ids stable via the binding + hash (T5).
2. A checker encodes a rule's LOGIC — that is the shadow's purpose (L7: engine computes beside his hand); it is not a copy of his NUMBER (the number is a key) nor of his TEXT (displayed from `Rules.md`). The hash keeps his text authoritative over the checker.
3. No pre-tick: the checkbox block is text he ticks; the engine unit is separate (T5 point 5). Nothing reaches a grade or a size: the 09:00 reader reads `Grade:`/`Goal:` (`daymode/drc.py:112-113`) and `aset_sizings` warnings (`:73-84`) only.

### (i) EVERY LEDGER AND ADDITION WITH ITS n (L8) AND ITS STORED INPUTS (L57)
**ADOPT WITH** — append to §5 "Every derived value…": "`drc_rows.inputs` SNAPSHOTS every value read: the card fields used (`aset_sizings` id, `grade`, `entry`, `stop`, `shares`, `actual_fill`, `recomputed_shares`, `state`, `created_at`), the typed cells (stored here, nothing else stores them), the `missed` row ids and `cf_r` used for C3, the `legs` row ids and their prices/shares used for realized R, and the import ids. Nothing is referenced by id alone."
1. L8: every C row states its n rule; `MIN_N_FOR_AVERAGE = 30` exists once (`line.py:59`) — the DRC imports that constant, never a second literal (L3).
2. Card fields change after the DRC: `mark_filled` UPDATEs the card row (`aset/store.py:185-257`); under N an entry-leg correction rewrites `actual_fill` (v3 :157). Failing scenario: DRC-2026-09-24 built 16:05 shows planned/actual risk from `actual_fill`; 17:30 the reconcile corrects the entry price; a replay of the row on 09-25 reads the new `actual_fill` → a different figure with the same `fn_version`.
3. Typed cells have no named store in the proposal — they are form inputs; without the sentence they exist only in the rendered note.
4. Realized R "is not stored" (v3 :166) — replayable only if the leg rows used are named; legs are append-only, so ids suffice.
5. The 21:10 replay figures are persisted in `missed` (`runner.py:399-403`); `grep actual_fill|recomputed_shares` in `cards/`, `radar/`, `replay/` → none: the replay does not read the fill cache, so a later reconcile does not change a 21:10 figure.

### (j) THE CHUNKS FOR THIS WEEK
**ADOPT WITH** — amend §9: "D3 files + `prefill/cli.py` (drop `drc`), `ops/README.md`, `smoke/checks.py` (T10's conditional check); D3's `jobs.yaml` edit MOVES the `configs/cobalt/rules.yaml` entry out of `no_resident_reads` (`jobs.yaml:308-315`) into `com.cobalt.aset`'s `reads:` — the build calls `regenerate_rules_config()` inside the resident. D4 files + `prefill/daily.py` (T9's variable); D4's keys are OPTIONAL (T5). RESTARTS: D2 `com.cobalt.aset`; D3 `com.cobalt.prefill-drc` (retired), `com.cobalt.replay`, `com.cobalt.aset`; D4 every resident that imports `settings.models` by static AST walk (`cobalt jobs restarts`, L42) — `com.cobalt.radar` included, so D4 lands inside the 20:00–21:00 pause (L43)."
1. Seats: Opus 5 floor, never auto mode on a write path — §9 says so; L29 holds for D1–D5.
2. Migration: one set (`db_migrations` + `.rollback.sql`), number the desk's at L68 (≥ 0013 on the stacked branches: `0012` bars `02d67a6`, `0013` setups `61a283f`; `0014`/`0015` claims are text) — the proposal assumes no number. Holds.
3. Failing scenario for RESTARTS as written: 2026-09-26 he edits `Rules.md`; `cobalt jobs restarts` derives no resident for `rules.yaml` (the `no_resident_reads` row says none); `com.cobalt.aset` keeps the old generated rules until its next restart — the `no_resident_reads` claim is now false (L42 unclassified → ESCALATE).
4. D4 ∥ D2: D2 touches `aset/web.py`, `drc/imports.py`, `pyproject.toml`; D4 touches `settings/models.py`, `prefill/daily.py` — no shared file. D1 blocked on E1 (L45) — holds. D5 after C2 — holds.
5. Hours: 7+7+9+4+5 = 32; ×1.4 = 44.8 ≈ 45 — arithmetic holds; feasibility beside S3 C1–C4 in 09-24 → 10-07 is UNVERIFIED — the desk's sequencing (the author's ESCALATE 4).

## Self-attack

Greps run (one call each; every writer and reader listed by the hits):
- `create_if_absent` / `upsert_unit`: writers `prefill/drc.py:427,:447`, `prefill/daily.py:523,:558`, `prefill/trade_note.py:151`, `daymode/note.py:159`, `heartbeat/runner.py:205`, `taxonomy/catalyst.py:404`, `taxonomy/cli.py:223`, `taxonomy/note_migration.py:872,:880`, `replay/line.py:172`, `radar/propose.py:753,:756`, `aset/daily_note.py:184-185`, `seatusage/runner.py:107,:126`; DRC-note writers among them: `prefill/drc.py`, `replay/line.py` only.
- `run_drc_prefill`: `prefill/drc.py:375` (def), `prefill/cli.py:22,:49`. `prior_day_inputs`: `daymode/drc.py:150`, `daymode/cli.py:26,:151`. `DrcNoteAbsent` / `drc_note_path`: `replay/line.py:64,:155,:163,:171`, `replay/cli.py:50,:98`, `smoke/checks.py:85,:122,:151,:566`. `prefill drc` / `prefill-drc`: `prefill/cli.py:1,:25`, `jobs.yaml:181,:186,:310`, `s2.yaml:436,:438`, `ops/README.md:30,:145,:157`, the plist, `session/migrations/0001_session_blocks.sql:6`.
- `drc-risk|drc-trades|drc-rules|drc-misses|drc_filename_pattern|review_dir`: `prefill/drc.py:14-16,:418-419,:442-444`, `prefill/config.py:111-112`, `daymode/drc.py:96-97`, `replay/line.py:1,:32,:51,:53,:151,:156,:161`, `smoke/checks.py:123,:129`, `s2.yaml:385,:387,:395`, `prefill.yaml:9-10`, `drc.md.j2:51,:55,:64,:68,:134,:138`.
- `format_risk_parameters` :248,:408 · `format_rules_check_block` :285,:415 · `format_card_reconcile_block` :230,:411 · `format_tickers_unit` :297,:416 · `format_tickers_block` :220,:409 — all `prefill/drc.py`, no other caller. `format_rules_checkbox_block` / `apply_mode_aware_sizing` / `regenerate_rules_config`: `prefill/daily.py:161,:165,:202,:209,:465`, `prefill/drc.py:54-55,:381,:405,:410`, `rules_gen.py:128`.
- `find_trade_note_for_card` :101,:159 · `parse_fill_updates` :70,:396 (`prefill/drc.py` only).
- `render_line` / `write_miss_line`: `replay/line.py:82,:167`, `replay/runner.py:61,:444,:452`.
- `request.form`: `aset/web.py:947,:1048,:1147,:1190,:1247,:1282`; `UploadFile`/`multipart`: none in `src/`, none in `pyproject.toml`.
- `ensure_schema(`: 60 hits; `aset/web.py:508,:729,:997,:1095,:1166,:1194,:1251` in request handlers (the build's `ensure_schema` in-request follows the existing pattern).
- `for_date` / `counts_for_date`: `aset/store.py:268,:292`; callers `prefill/drc.py:387-388`, `prefill/daily.py:378,:507`, `aset/web.py:490,:509,:542` (day-mode store's `for_date` is a different method).
- `trader_settings`: readers `settings/*`, `aset/config.py:251-275`, `aset/account_mode.py:32`, `aset/radar_panel.py:729`, `radar/evaluate.py:1184`, `cards/shadow_report.py:15,:150`, `dayopen/checks.py:151,:159`, `daymode/config.py:49,:314`; writers `settings/store.py:96,:106`, `db_migrations/0004_radar_pool.sql:73`.
- `_imports`: `vault.py:101` (comment) only — no code path. `market_reset`: `vaultwrite/writer.py:36,:388,:401`, `aset/web.py:950`. `TEMPLATES_DIR` / `drc.md.j2` / `_render_template`: `prefill/config.py:30`, `prefill/drc.py:53,:270-272,:427`, `prefill/daily.py:68,:220-221,:523`, `daymode/drc.py:121` (comment).
- `legs|fills|drc_rows|drc_imports|prediction_records|DECLARED_TABLES` in `db_migrations`: `placement.py:11,:106-112,:125,:143,:170`, `0006_radar_score.sql:132` (comment). `realized_r|legs_current_v|price_source`: no S3 legs code on main; `price_source` is the CARD's column (`aset/*`, `cards/store.py:978`). `MIN_N_FOR_AVERAGE|insufficient data`: `replay/line.py:5,:24,:59,:107,:110,:176` only. `-w DAS|Lightspeed|TradeStation|CenterPoint`: comments and messages only.
- `git log --all -- db_migrations`: `0012` `02d67a6` (bars), `0013` `61a283f` (setups); `main` head `49c0871`; `ls db_migrations` ends `0011`.

WITHDRAWN (my own round-1 text, struck before finalizing):
- WITHDRAWN: "the tickers unit's `- Grade: <card grade> · Direction …` lines are read by the 09:00 reader as his self-grade" — `daymode/drc.py:142-147` takes the FIRST match and `:129` accepts only a ladder value; his `Grade:` prompt line (note :38) precedes the tickers block (:62), and the card line's group is `A · Direction: …`, not a ladder value.
- WITHDRAWN: "the in-request build is wrapped by `jobs/wrapper.py`'s `job_run` so the smoke keeps its `job_row` kind" — `jobs/wrapper.py:125` `job_run(` exists but I did not open its signature or its spec model; an unwalked mechanism. T10 keys on the day row instead.
- WITHDRAWN: "`VaultWriter` grows a `create_bytes_if_absent` so `_imports/` stays on the one writer" — `_commit` opens `"w", encoding="utf-8"` (`writer.py:470`) and `_persist_and_write` stores `after_section=text` (`:495-521`); a bytes method would be a second commit path inside the writer. T1 gives the bytes to the import store.

## Experiments (L70)

- X1 (= E1, widened): one real trading-log export from him (any recent day) → header set, side codes, time format + zone, account column(s), whether order-log rows share the file, whether a day-file starts flat or lists overnight positions, and whether a single execution can cross 0 (reversal). Changes: the fixture; T11's seed branch; §4's through-0 rule.
- X2 (= E2): one real TradeZella screenshot → which SPEC §1.1 row 2 fields are legible; decides O1's typed-cell list.
- X3 (= E3): dev vault — render his template with only the date token substituted, run the three existing placements + the per-trade units (T6); every unit under its heading, his lines byte-identical, no other `{{` token; his `Tomorrow’s` heading (curly apostrophe, template :173) anchors nothing.
- X4 (= E4): one card's `created_at` vs the same trade's first execution time → skew and zone; `card_lead_seconds` trustworthy or rule 1 needs a tolerance key (his).
- X5 (= E5): multipart `UploadFile` in the dev ASET app with `python-multipart` 0.0.22 made direct — works, or the dependency is added knowingly.
- X6 (replaces E6): on cobalt_dev, run the replay for a date with no DRC → the receipt's result carries `line_action = pending_no_drc` and a `line_body`; then `cobalt drc build --date` writes that body into a built DRC byte-identical; a run failed before the line step → the `not available` line.
- X7 (= E7): which aset config production loads (`aset.local.yaml` present?) and a >1 MB PNG POSTed from the trading PC over the LAN — ops, after the build.
- X8 (replaces E8): build twice on the dev vault with a trade removed from the second export, his text typed inside that trade's unit → the unit stays, first line marked orphaned, his text in place, no override row for his lines.
- X9 (new): Obsidian, dev vault — `![[_imports/drc/<date>/<file>.png]]` renders under his attachment/link settings for an underscore-prefixed folder; if not, the embed shape changes (still a display, O2).
- X10 (new): a DRC created on D+1 morning for date D — `this.file.cday` dataview (`drc.md.j2:9-14`, his template :9) lists D+1's trade notes, not D's; if so, the tickers dataview line is an OWNER item (his template), not a build defect.
- X11 (new): cobalt_dev after S3 C2 — a CLOSED card whose export exits total less than Cobalt's → C2 refuses; the build writes the unresolved-mismatch row, renders it, and the DRC is built (T7 (i)).
- X12 (new): cobalt_dev without the DRC key rows → `/size` still serves and sizes; `cobalt drc build` fails naming the missing key (T5's `OPTIONAL_SETTING_KEYS` placement).

## OWNER (after the tribunal)

O1 TradeZella figures: typed cells (MVP) vs OCR (new dependency; shadow vs typed) · O2 file home: vault `_imports/drc/<date>/` (every screenshot then syncs to every device) vs a non-synced data directory · O3 a no-trade day: what "both placed" means (SPEC §9 streak vs R66) — default until he rules: no DRC · O4 which accounts in the export count · O5 the daily stop and grade dollar VALUES (the one-source mechanism is T9, not his to build) · O6 config home: `trader_settings` (T9's mechanism) vs a keyed unit in his note · O7 rule number → checker binding (T5 adds the text hash; the binding itself is his) · O8 his template's example blocks (keep, or the desk removes on his word, L65) · O9 his per-trade answers inside Cobalt's per-trade unit vs his own block (T6 makes either safe) · O10 voice channel and timing · O11 premarket keys that do not exist (meditated, tone, hotkey, sleep hours vs `Sleep:`) · O12 the packet questions still his (Q2, Q4–Q8, Q10–Q14) · O13 shadow length N + the agreement definition · O14 inputs dropped inside `market_reset` (default refuse) · O15 consent to a redacted real-shape export fixture in git · O16 the PnL heading placeholder: his, or Cobalt fills it · O17 `trades.jsonl` / `days.jsonl` beside Postgres · O18 Charter F14 / S3-smoke "15:41" wording (`charter:171`, `ladder:612,:616`) · O19 spent (R69–R72) · **O20 (missing)** how a CLOSED card the export shows still open is reopened — a CLOSED→FILLED edge does not exist (v3 :150, `cards/models.py:106` per v3, not opened here); adding one is a trading-logic change (L7 + HITL) · **O21 (missing)** the summary line's P&L: gross or net (SPEC §2 carries both; the proposal writes net).

## WRONG FACTS

- Proposal §8 "the three placements (`prefill/drc.py:248-347`)" as the range holding `format_risk_parameters`, `format_rules_checkbox_block`, `format_card_reconcile_block`: `format_card_reconcile_block` is at `prefill/drc.py:230-247` (outside the range) and `format_rules_checkbox_block` is defined in `prefill/daily.py:165` (imported at `drc.py:54`); the DRC's own is `format_rules_check_block` at `:285`.
- Proposal F33 / report: R67 cited at `cto-2026-09-22.md:35`; R67 is at `:43` today (`grep -n "^| R67 "`) — rows were inserted above it; content unchanged.

## READING

Files opened (line ranges): `LAWS.md` (full, 407 lines) · `DRC-AUTOMATION-PROPOSAL-2026-09-22.md` (full, 298) · `reports/drc-design-2026-09-22.md` (full, 73) · `reports/cto-2026-09-22.md` rows R60, R65–R73 (`grep -n "^| R.. "`) · `reports/cto-2026-09-21.md` row R46 · `prompts/2026-09-22/40-drc-tribunal.md` :3 (heading line via grep), :15–47 (§1 staging list + the question paragraph through `TRIBUNAL R1:`) · `prompts/2026-09-22/41-drc-tribunal-fable-seat.md` (this seat's prompt) · `src/cobalt/prefill/drc.py` :1–67, :68–247, :248–350, :375–454 · `prefill/rules_gen.py` :37–130 · `prefill/config.py` :25–35, :105–115 · `prefill/cli.py` (grep lines) · `replay/line.py` :20–70, :82–142, :143–178 · `replay/runner.py` :330–474 · `replay/models.py` (grep :425, :453–454) · `daymode/drc.py` :40–84, :85–141, :142–147 · `vaultwrite/writer.py` :178–215, :380–420, :457–528, :559–620, :644–680 · `vaultwrite/markers.py` :31, :79–93 · `vault.py` :92–116 · `aset/web.py` :1–24, :80–84, :940–960 · `aset/store.py` :51–75, :185–190, :197–257, :268–322 · `aset/__main__.py` (whole) · `aset/config.py` :40–50, :251–275 · `configs/dev/aset.yaml` :35–45 · `configs/dev/aset.local.yaml` (grep `bind`) · `settings/models.py` :40–94, :95–120, :120–175 · `db_migrations/__init__.py` :60–95 · `db_migrations/placement.py` :100–154 · `configs/cobalt/jobs.yaml` (grep drc; :300–316) · `configs/cobalt/smoke/s2.yaml` (grep drc; :378–400, :434–440) · `ops/com.cobalt.prefill-drc.plist` (whole) · `ops/com.cobalt.replay.plist`, `ops/com.cobalt.daymode-propose.plist` (grep Hour/Minute) · `ops/start_aset.sh` (whole) · `configs/cobalt/templates/drc.md.j2` (whole) · `configs/cobalt/templates/daily.md.j2` (grep keys; :18 located, value not recorded) · `pyproject.toml` (grep multipart, prefill) · `uv.lock` (grep; :2770–2790, :2860–2880) · `docs/30 - Design/S3-EXITS-v3-2026-09-22.md` :17–34, :110–170, :226–238 · `docs/90 - References/trade-reporter/app.py` (whole), `templates/index.html` :1–86 · `MVP-CHARTER-v0_2.md`, `SPRINT-LADDER-v0_1.md` (grep `15:41`; ladder :610) · `jobs/wrapper.py` (def list only) · shape-only (L32): coach spec headings (`grep -n -E "^#+ "`, 15 lines) and key names (`grep -n -o -E "^[[:space:]]*[a-z_]+(\.[a-z_]+)*:"`, 36 keys); `DRC-2026-09-21.md` headings + `cobalt:` markers; `5 - Templates/DRC.md` headings, `{{` count and tokens, `^Risk Parameters:` count.
Searches: the `02-greps.txt` list of `40` §1 re-run against the real tree (listed under Self-attack), plus `grep -rn actual_fill|recomputed_shares` (cards, radar, replay), `replay_runs`/`replay_run_id`, `daily_stop`, `VaultWriter` in `aset/`, `drc` in `cli.py` (none), `structural_stop|card_stop_edits|card_transitions|"picks"|"missed"` in `placement.py`, `ls cards/picks.py`, `ls Vault/Think/_imports` (none). Git: `log -1 main`, `log --all --oneline -- db_migrations`, `log --stat main..bars/chunk-2-0920`, `log --stat main..setups/seven-0921`, the authorization `log -S` calls.
Not read: the houses' folder, the hub's report, any house file about this proposal, `Rules.md`, any body line of the coach spec, his DRC note or his template, any daily note, `trade-reporter/static/js/app.js`, `utils/drc_builder.py`.

## ESCALATE

1. **L31 in the proposal's identifiers.** `das.py`, `das_import_id`, source value `das`, `degraded: das_export_shape` are vendor names in identifiers/enums (LAWS.md:173-175). The derive applies T4's names; the design prose may cite "the trading-log export" without the vendor.
2. **R67 (3) meets N's CLOSED invariant.** As written, a CLOSED card whose export shows an open remainder fails the DRC build forever (T7 scenario). T7 (i) keeps the build; the state edge is O20 — his, and a trading-logic change.
3. **D4's key placement is a deploy seam.** A DRC key in `SETTING_KEYS` breaks every sheet reader until his hand loads (`settings/models.py:120-127`); T5 puts the keys in `OPTIONAL_SETTING_KEYS`. X12 proves it before D4 merges.
4. **`jobs.yaml:308-315` becomes false at D3.** `configs/cobalt/rules.yaml` gains a resident reader (`com.cobalt.aset`) when the build runs in-request; the `no_resident_reads` entry must move to `reads:` or L42's derivation drops a restart.

## CONTINUE

none — the ruling is complete; the desk commits this file; the hub `40` file-checks these claims at its collate step; the derive (`42`) follows on `claude-fable-5-1`.

DRC TRIBUNAL FABLE R1 DONE · verdict: BUILD AFTER E1's real export is in hand and vendor names leave every identifier · adopt: 2 · adopt with wording: 19 · reject: 0 · experiments named: 12 · ESCALATE: 4
