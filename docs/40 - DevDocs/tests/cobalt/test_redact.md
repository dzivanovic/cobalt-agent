# `tests/cobalt/test_redact.py`

Exercises F19 redaction across literals, URLs, logs, and persisted output. S2-P1
keeps its ordinary-URL example filter-neutral so the only committed radar screen
filter remains the synthetic fixture required by R2.
