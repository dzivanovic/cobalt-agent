# `tests/cobalt/test_radar_notes.py`

Tests both synthetic note fixtures, exact screen count, fence-only parsing, hashes, and the R2/E5 repository-content boundary. O5 adds per-field cases for missing and duplicate pool blocks with mirrored status, unknown `kind`, absent/present `ft`, unknown override keys, and over-budget refusal text containing every computed number.
