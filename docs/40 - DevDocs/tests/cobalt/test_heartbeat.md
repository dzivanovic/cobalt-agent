# `tests/cobalt/test_heartbeat.py`

Adds fake-row coverage for every principal radar probe state: disabled, absent, paused, idle, healthy scanning, failed stage, invalid mirror, and aged poll failure.

