MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `jev-check-reissue-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/36-reissue-jev-check.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control jev-check-reissue-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO network, NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic small · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps.

# SPLIT THE JEV CODE CHECK IN TWO (L19 re-issue of `29`) — THE PACKET IS 317 KB AGAINST A 230 KB CEILING

LAW STEP: `29` stopped `FAILED: packet — 316,683 B measured … ≥ 86,683 B over the 230,000 B ceiling — nothing staged, no house launched, probe gate NOT READY` (`reports/jev-trial-check-2026-09-23.md`). L67: every build is checked by three houses — a SPLIT keeps that for every line of the build; it never drops a part. His R42 condition: the keyed probe runs only after the houses have checked the KEY HANDLING.

INDEX CARD: `LAWS.md` in full (L59) — L4 / L41, L19, L35, L67, L71. Files: `prompts/2026-09-23/29-jev-trial-check.md` (whole), its FAILED report, `prompts/2026-09-23/28-jev-trial-build.md`, `prompts/2026-09-23/31-jev-trial-probe.md`, the build report `/Users/cobalt/cobalt-wt/jev-trial/docs/40 - DevDocs/reports/jev-trial-build-2026-09-23.md`, `git -C /Users/cobalt/cobalt log --stat 47fafe5..jev/trial-0923` (the file list and sizes).

DO (whole files):
(1) Re-issue `29-jev-trial-check.md` in place as **CHECK A — THE SECRET AND NETWORK PATH**: only the files through which the key or a network call passes (the key read / `read_secret` use, guards, ledger / cap, the systemone door request + parse, the probe subcommand, the wrapper `ops/run_classify_trial.sh`, their config and their tests + DevDocs), packet ≤ 230,000 B MEASURED before staging (list each file with `wc -c`). Its stop line keeps `29`'s DONE shape and adds `part: A (secret + network path)`; `31`'s probe gate reads THIS stop line (confirm `31`'s gate text matches; if not, re-issue `31`'s gate line only).
(2) Write `37-jev-trial-check-b.md` — **CHECK B — EVERYTHING ELSE** of `47fafe5..45f647a` (runner, plan / trial sets, CLI glue, remaining tests / DevDocs), same shape, ≤ 230,000 B measured, launched after A in the house lane; B gates any MERGE of `jev/trial-0923` and the trial RUNS (N3), never the probe.
(3) Keep every rule string byte for byte (`29`'s); stagger literals + `R__` for the desk.

REPORT (write LAST): `docs/40 - DevDocs/reports/jev-check-split-2026-09-23.md` — §0 ≤5 lines · the file split table with byte counts (A total, B total; every file of the range in exactly one part) · `## ESCALATE` · last non-blank line: `JEV CHECK SPLIT · A: <bytes> B · B: <bytes> B · files unassigned: 0 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
