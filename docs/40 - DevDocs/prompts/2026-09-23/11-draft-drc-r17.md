MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `drc-r17-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/11-draft-drc-r17.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-r17-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, 09-20 R13) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write (L58) — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue with the safe default. Never end a turn between steps.

# FOLD HIS 09-23 R17 DRC ANSWERS INTO THE DRC BUILD + CHECK PROMPTS (L19 full re-issue of every file that changes)

LADDER: S3 · F14 / F15 (DRC on radar cards; S3 starts 09-24, D1 launches 09-24). LAW STEP: his R17 (`reports/cto-2026-09-23.md` §4, his words verbatim there) answers the five DRC owner items of `reports/open-items-2026-09-23.md` 1–5.

INDEX CARD
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59). Binding: L1 (loud), L3 (one parse path), L19, L28 (vault writes), L35, L45 (real-shape fixtures), L57, L62 / L63, L67 (each changed build keeps its check prompt in step), L71, L75.
2. `reports/cto-2026-09-23.md` §4 R17 (THE RULINGS), `reports/open-items-2026-09-23.md` items 1–5, `reports/drc-reissue-draft-2026-09-22.md` (the last re-issue and its ESCALATE — the source of items 1–5), `areas/cobalt-product-definition.md` lines dated 2026-09-22 / 2026-09-23 (DRC).
3. The prompts: `prompts/2026-09-22/48-drc-d1-build.md` … `52-drc-d5-build.md` and `53-drc-d1-check.md` … `57-drc-d5-check.md`, whole, plus the DRC design FINAL they cite.

THE FIVE RULINGS TO FOLD: (1) exact title equality after the Long/Short strip — as built, confirm and cite R17; (2) files dropped BY HAND into `1 - Trading/5 - Review/_imports/drc/<date>/` ARE imported — same header classifier, one parse path with the `/drc` upload (L3); name the build row + the check row that prove it; (3) the DAS rows' date = the `<date>` folder / upload date — no date column exists; (4) the STOP PRICE comes from the TradeZella export's stop field (read its real header from the committed real-shape fixture / E1 — never invent a column name; if the TradeZella file carries no stop column, that is `ASK DESK`, not a guess); never computed from `Trade Risk`; `not given` only when TradeZella has none for that trade; (5) a file with SOME of a kind's required columns is READ (what is there), and the DRC renders a LOUD `PARTIAL — missing: <columns>` flag for that file (L1 is met by the flag; nothing silent).

DO: re-issue every prompt that changes, whole, in place (same paths); keep every rule string byte for byte unless a ruling needs a new one — list any NEW string for his approval. Keep each check prompt in step with its build (a new row → a check row).

REPORT (write LAST): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-r17-draft-2026-09-23.md` — §0 ≤5 lines · one row per file changed (what, why, which ruling) · NEW strings · `## ESCALATE` · last non-blank line: `DRC R17 FOLDED · files changed: <n> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
