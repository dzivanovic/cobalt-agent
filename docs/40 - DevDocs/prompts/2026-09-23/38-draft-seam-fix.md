MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `seam-fix-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/38-draft-seam-fix.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control seam-fix-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps. **TIME MATTERS: today's deploy must restart ≤19:55 ET (R5) — draft in ≤25 min.**

# THE L68 SEAM: CLASSIFY (L75) AND DRAFT THE SMALLEST FIX + ITS CHECK + THE DEPLOY RE-ISSUE

LAW STEP: `07` stopped `FAILED: 2.2 — offline suite red on the stack — tests/cobalt/test_radar_panel_cards.py::test_bars_stale_badge_absent_and_output_unchanged_when_healthy[False], [True] (ladder SHA pin 0ac9b5d0… ≠ e617c53c…) · rollback: not used` (`reports/deploy-2026-09-23.md`, whole: §0, ESCALATE 2–5, CONTINUE). Production untouched. Both branches REBASED onto `d327ff1` (`setups/seven-0921` now `b007ce2e`, `s2/smoke-fix-0922` now `4706e219`, code identical to the checked tips); the gate branch `deploy/stacked-0923` sits at `f2dd42cb`.

INDEX CARD: `LAWS.md` in full (L59) — L35, L45, L67 (every build checked by three; a fix round is checked), L68 (the gate proves the seam; it does not decide it), L70, L72 P-b, L73, L75 (classify first; only FIX rows build; the fix widens nothing). Files: the deploy report; `tests/cobalt/test_radar_panel_cards.py` around `:377` on main and on the stack (`git -C /Users/cobalt/cobalt show f2dd42cb:tests/cobalt/test_radar_panel_cards.py`); the 09-22 stale-marker build / check reports that set the pin; the setups ladder-change reports (`setups-fix-r4-*`, `setups-check-r4-2026-09-22.md`).

DECIDE (L75, from file evidence only): is the new ladder HTML the INTENDED output of the setups ladder change (the pin must move, with the new SHA derived and the test's intent kept: "stale badge absent and output unchanged when healthy" = healthy bars add nothing to the ladder) — or does the stack's healthy-path output differ in a way the stale-marker change forbids (a real defect)? Name the exact bytes that differ if a read can show it; else say UNPROVEN and design the fix step to print the diff of the two HTML outputs before any pin moves.

WRITE (whole files): `prompts/2026-09-23/39-seam-fix-build.md` (Opus 5.5 builder in `/Users/cobalt/cobalt-wt/setups-c1` on `setups/seven-0921` at `b007ce2e`; offline; its allowlist = `72`'s / `33`'s shared strings byte for byte + any NEW listed; it proves the diff first, then the smallest change; the test's INTENT stays; stop `SEAM FIX BUILT <tip> | on b007ce2e | offline <p>/<f> | …`), `prompts/2026-09-23/40-seam-fix-check.md` (L67, `17`'s shape; Opus 5.5 · Grok · Gemini; house lane — stagger literals incl. `19` (PAUSED protocol) and `29`), and the deploy re-issue `prompts/2026-09-23/41-stacked-deploy-r2.md` = `07` with ONLY: the new setups tip, the gate re-cut (the desk runs `git -C /Users/cobalt/cobalt branch -f deploy/stacked-0923 main` or a new gate branch name — pick the one that needs no new string, else name the string), P6 also reading `40`'s DONE line, placeholders `R__A` / `R__L` (R52's approval carries; name any string that changes). Deadline gate in `41`: before 19:55 ET.

REPORT (write LAST): `docs/40 - DevDocs/reports/seam-fix-draft-2026-09-23.md` — §0 ≤5 lines · the classification (FIX | NOT REAL | UNPROVEN) with its evidence · NEW strings · `## ESCALATE` · last non-blank line: `SEAM FIX DRAFTED · class: <FIX|UNPROVEN> · prompts: 3 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
