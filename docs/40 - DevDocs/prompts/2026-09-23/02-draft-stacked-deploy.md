MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — a read-and-write drafting job of a PRODUCTION DEPLOY prompt (two branches, one stacked gate, residents down and up); no code, no DB, no launch · SEAT: drafter `stacked-deploy-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/02-draft-stacked-deploy.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control stacked-deploy-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `prompts/2026-09-22/04-draft-stacked-deploy.md`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault / memory write (L58) — you write exactly the THREE files named below with the Write tool, the REPORT LAST · METER: Anthropic medium · nobody sits at this terminal; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps.

# Draft TODAY's stacked deploy — `setups/seven-0921` + `s2/smoke-fix-0922`, on HIS "done trading" word (2026-09-23)

LADDER: S2 (stop 2026-09-23, AT RISK) — F8/F10 ladder change (all setups at defaults, R44) + F12/F13 smoke fix; S2 closes on tonight's green smoke night after this deploy.

HIS RULING (`reports/cto-2026-09-23.md` §4 **R5**, 06:2x ET, verbatim): "I want the work done after I am done trading." → TODAY's ONE deploy event starts on his "done trading" word in the desk chat. OVERRIDDEN for this one deploy: L43's radar-restart window and L66's "inside the 20:00–21:00 pause" timing. NOT set aside: L66's SHAPE (residents down BEFORE the merge, up after), L67 (this prompt is read by ≥1 other house before it runs), L68 (integrated gate on the stacked tree BEFORE the merge, offline AND with-DB), L42 (`cobalt jobs restarts`), L54 (rollback = ONE `git revert -m 2` of the gate merge), L35, L62 / L63, L71. Also R4(a): the setups r4 HOLD (test cutter) SHIPS, cutter fix → BACKLOG.

## INDEX CARD
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59); one line each in the prompt you write for L35, L42, L43, L51, L54, L55 (no push), L62, L63, L65, L66, L67, L68 (both amendments), L70, L71, L73.
2. THE PRECEDENT — YESTERDAY'S SAME-SHAPE DEPLOY, which RAN GREEN (`STACKED DEPLOY DONE d2d82e7 · tag deploy-2026-09-22 · branches: 2 · residents down 93 s · /radar 200 · rollback: not used`): `prompts/2026-09-22/04-draft-stacked-deploy.md` (its drafter brief — reuse its structure), `05-stacked-deploy.md` (the deploy prompt), `06-review-stacked-deploy.md` + `25-review-stacked-deploy-r2.md` (the house reads) and their reports `stacked-deploy-review-2026-09-22.md`, `stacked-deploy-review-r2-2026-09-22.md` (every defect they found — carry each fix forward), `reports/deploy-2026-09-22.md` (what actually ran, every lesson / ESCALATE). Start from `05`, change only what today differs in, and list each difference.
3. THE BRANCHES: (a) `setups/seven-0921`, worktree `/Users/cobalt/cobalt-wt/setups-c1`, code tip `f5aaeb4`, report tip `f78bf4d`; build `setups-fix-r4-…`; check `reports/setups-check-r4-2026-09-22.md` (`SETUPS CHECK R4 DONE … ready: YES` ×3, the ONE HOLD shipped under R4(a)); owed before this deploy: the blind values of `23` (re-issued today as `prompts/2026-09-23/05-setups-blind-code-seat.md` by the sibling drafter `01`) and 09-22 R119's three `Assumed Defaults` rows (A-09 / A-10 0.05, A-16 0.5 × ATR, A-24 null — "written by the setups deploy"; L65 shape: smallest diff, before/after, read-only parser proof). (b) `s2/smoke-fix-0922`, worktree `/Users/cobalt/cobalt-wt/s2-smoke-fix`, stop line `S2 SMOKE FIX BUILT b510b65 | on 6f4da5e | offline 1970/0 | with-DB: OWED (68) | RESTARTS: com.cobalt.aset com.cobalt.radar | F3: built | tests added: 12 | ESCALATE: 9` (report `docs/40 - DevDocs/reports/s2-smoke-fix-build-2026-09-23.md` in that worktree — read its ESCALATE); its L67 check = `prompts/2026-09-23/06-s2-smoke-fix-check.md` (sibling drafter). Prove what lands and how far `main` moved with `git -C /Users/cobalt/cobalt log --oneline --stat`.
4. WITH-DB: `cobalt_dev` is BROKEN until `prompts/2026-09-22/68-devdb-repair.md` lands (`cto-2026-09-22.md` R110, R125, R127). The deploy's PREFLIGHT reads `68`'s report last line (DONE shape from `68`) and FAILS if the dev DB is not repaired — never a with-DB gate skipped (L73).
5. RESTARTS (L42): predict from both branches' `--stat`; the smoke fix names aset + radar. R5 permits the radar restart outside the pause today; the residents-down window stays as short as yesterday's (93 s).

## WRITE THREE FILES
(a) `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/07-stacked-deploy.md` — the deploy prompt, whole (L19), yesterday's `05` shape. PREFLIGHT gates, each its own row: (i) HIS WORD — the launch row in `reports/cto-2026-09-23.md` carries the literal `DONE TRADING <time>` (the desk writes it from his chat word; absent → `FAILED PREFLIGHT: no done-trading word`); (ii) `date` = 2026-09-23 and before 19:55 ET (the 20:30 archiver / 21:10 replay / 21:40 backup are never crossed by a residents-down window); (iii) both checks' last lines READY (setups r4 per R4(a); smoke-fix check `06`'s DONE shape with ready from ≥3 houses or the L67 floor as metered); (iv) `68` DONE; (v) `05` (blind values) DONE; (vi) the desk created the gate worktree `/Users/cobalt/cobalt-wt/stacked-0923` (branch `deploy/stacked-0923` off `main`) — the hub never runs `worktree add`. Then the gate, the R119 rows, residents down → ff merge → up, `/radar` 200, heartbeat, tag `deploy-2026-09-23`, report, rollback string. List every rule string; mark each NEW vs a precedent file (byte for byte). Stop line: `STACKED DEPLOY DONE <sha> · tag deploy-2026-09-23 · branches: <n> · residents down <s> s · /radar 200 · rollback: not used|USED · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
(b) `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/08-review-stacked-deploy.md` — the L67 house read of `07`, shape `prompts/2026-09-22/06-review-stacked-deploy.md` (Grok + Gemini, Opus 5.5 seat if the shape allows without new strings), a STAGGER gate with the `<nn> is not running` literals, launch row `R__`.
(c) `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/09-s2-smoke-look.md` — tonight's read-only smoke look after the 21:10 replay (shape `prompts/2026-09-22/03-s2-smoke-look.md`, window at/after 21:40 ET, reads the post-deploy replay + `cobalt smoke s2`), stating the S2 close verdict line.

REPORT (write LAST): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stacked-deploy-draft-2026-09-23.md` — §0 ≤5 lines · differences vs `05` (table) · NEW strings (for his approval list — one line each) · the desk's launch order · `## ESCALATE` · last non-blank line = `STACKED DEPLOY DRAFTED · prompts: 3 · new rule strings: <n> · restarts predicted: <list> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
