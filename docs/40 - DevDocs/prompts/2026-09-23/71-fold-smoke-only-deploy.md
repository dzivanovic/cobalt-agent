MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat (fold of a production deploy prompt) · SEAT: drafter `smoke-deploy-fold-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/71-fold-smoke-only-deploy.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control smoke-deploy-fold-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic small · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps. **≤15 min: the deploy must start ≤19:55 ET.**

# FOLD THE L67 READ OF `68` INTO A FULL RE-ISSUE OF `68` (L19) — THE ONE BLOCKER FIRST

LAW STEP: `69` stopped `SMOKE ONLY DEPLOY REVIEW DONE · houses: 2 of 3 · other houses: 1 of 2 · blockers: 1 · folds: 6` (`reports/smoke-only-deploy-review-2026-09-23.md`, `## Folds proposed` 1–6 and `## Checked against the files`). The blocker = fold 2 (STEP-5 (2): "any other revert error → (3)" brings both residents UP on a half-reverted tree — must run the PROVE IT diff and keep them DOWN when non-empty). One round only; folds are not re-read (its own rule).

INDEX CARD: `LAWS.md` — L19, L35, L62 / L63, L66, L67, L71. Files: `prompts/2026-09-23/68-smoke-only-deploy.md` (whole — re-issue IN PLACE), the review report (whole), `prompts/2026-09-23/21-fold-stacked-deploy.md` (this morning's fold of `07`, the shape to follow).

DO: fold 2 (the blocker) exactly as proposed; folds 1, 3, 4, 5, 6 as proposed, each the smallest text change backed by its `## Checked against the files` row; NO rule string added or widened (fold 1 uses `rev-parse*`, fold 4 uses `ls *` and the existing `rm` of the gate `.env` — confirm each is already on `68`'s line); keep `R__L` for the desk; the desk-side note (hold every other with-DB / `.env` until `68` stops) goes into `68`'s header as a desk line, not a hub step.

REPORT (write LAST): `docs/40 - DevDocs/reports/smoke-deploy-fold-2026-09-23.md` — one row per fold (before → after, line) · `## ESCALATE` · last non-blank line: `SMOKE DEPLOY FOLDED · folds applied: <n> · blocker folded: yes · strings widened or added: 0 · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
