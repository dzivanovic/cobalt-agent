DERIVE ROW: R109

MODEL: `claude-opus-5-5` — the DERIVE SEAT his STANDING R109 names (`cto-2026-09-22.md` §4, 19:34 ET, "Make all Opus 5.5 for now": every Fable-type seat — the tribunal seat, the derive, forensics — on `claude-opus-5-5` until his next word; filled at drafting, nothing for the desk to fill). This is the Anthropic house's SECOND JOB on this tribunal, as on the bars, setups, float-handicap, stale-score, S3-exits, DRC, voice-v2 and routing tribunals; you derive the version the round-1 rulings produce; you build, launch and commit nothing · SEAT: `voice-v3-derive-0923`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `13`; the Anthropic seat `14`). YOUR POSITION, STATED: the Anthropic house ruled round 1 through its seat (`14`, on `claude-opus-5-5` by R109), and the PROPOSAL is also the Anthropic house's (seat `voice-v3-draft-0923`) — you are that house in a fresh session; you HOLD A SIDE twice over, so you recommend nothing to him (L37), the Anthropic seat's wordings pass the same filter as every house's, and the proposal's text is never preferred over a house's equally correct wording. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/15-voice-v3-derive.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control voice-v3-derive-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `prompts/2026-09-22/61-voice-tribunal-derive.md` and `prompts/2026-09-21/22-draft-setups-tribunal.md`, 09-20 R13 — nothing new) · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic Opus 5.5 — honest estimate: the hub report `13` ≈ 25–35k tokens, the raw rulings of the houses that ruled ≈ 25–35k (19 items each), the Anthropic seat's round-1 report ≈ 12–16k, the proposal ≈ 8k (read whole — the derived version is the proposal amended), its author's report ≈ 2–3k, R18 / R92 / R93 / R99 / R100 / R109 ≈ 3k, LAWS.md ≈ 20.5k (82,174 B measured at drafting), code and DRC build prompts re-opened only where a fold turns on it ≈ 5–10k; the derived design ≈ 10–13k tokens of output plus the report ≈ 6–9k; peak context ≈ 110–140k — read the raw rulings item by item, not all at once, if the peak nears your window. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE THE VOICE v3 DESIGN from the proposal and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: S3 design lane (`cto-2026-09-22.md` R100; `cto-2026-09-23.md` R18). LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini via hub `13`; Astra `METER — proceed on three`, 09-22 R13; the Anthropic seat via `14`) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (empty → the tribunal closes and the derived design is the FINAL's input) → build → ≥3 checkers → deploy. HIS R18 (d) IS THE BAR OF THIS DERIVE: he receives ONE finished design and ONE approval; an item reaches him ONLY if it is his money, his data, his time or a law he owns, AND no house can decide it — each such item says which of the four and why. L1, L2, L3, L4, L7, L9, L23, L25, L28, L32, L37, L40, L41 and L45 are the bar the derived design must meet; the proposal's L52 finding is re-answered. DO NOT STOP until the report's last line is `VOICE V3 DERIVED …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** Each its own Bash call: `grep -c -x -E "DERIVE ROW: R[0-9]+" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/15-voice-v3-derive.md"` — must print **1**; read `<nn>` from line 1 (`109`) · `grep -n "^| R109 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (exactly ONE row carrying BOTH literals `Make all Opus 5.5 for now` · `claude-opus-5-5`, his words in quotes — else `FAILED: authorization mismatch — row R109 does not name the derive seat`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R109 | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — row R109 is not committed`) · **R109 STILL STANDS:** `grep -n "R109" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (and `cto-<run date>.md` on a later run date) — a `| R` row carrying his words that ENDS or CHANGES R109's seat model → `FAILED: authorization — R109 superseded by <row>; the desk re-issues this file` · **YOU ARE THE SEAT THE ROW NAMES:** `claude-opus-5-5`, the `--model` value on this file's launch line, and the model id your own system prompt states you run as must be the SAME id — else `FAILED: seat mismatch — R109 names claude-opus-5-5; this session runs <id>`, stop · **THE LAUNCH ROW:** `grep -n "15-voice-v3-derive.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-<run date>.md"` — a `| R` row naming this file (no row → `FAILED: authorization mismatch — no launch row names 15`). **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-tribunal-2026-09-23.md"` (the LAST NON-BLANK line must start `VOICE V3 TRIBUNAL R1 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE V3 TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/voice-v3-tribunal-2026-09-23.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-tribunal-fable-r1-2026-09-23.md"` (must start `VOICE V3 TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Anthropic-seat round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE V3 TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/voice-v3-tribunal-fable-r1-2026-09-23.md"` (EMPTY → `FAILED: refused — the Anthropic-seat round-1 stop line is not committed on main`) · **A HOUSE MUST HAVE RULED:** the hub's stop line shows `houses that ruled: 0 of 3`, or neither `grok:` nor `gemini:` in it carries a `TRIBUNAL R1:` line → `FAILED: no house ruled round 1 — the desk relaunches 13 or brings it to him` (the Anthropic house alone never settles an item: it proposed AND ruled, L37) · **ASTRA (09-22 R13):** the hub's `astra:` field is `METER — proceed on three` or `SKIPPED — R13 (probe UP, recorded)` — RECORDED, not a refusal: derive from the seats that ruled, mark every question only Astra would have answered `ASTRA PENDING (R13)`, mark the derived design's header `ASTRA PENDING (R13)`, and name in §0 that Astra reads it when its meter returns (Sat 09-26). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L2 · L3 · L4 / L41 · L5 / L22 / L23 / L25** (the routing cluster is FROZEN — the derived design cites it, never rewrites it) **· L7 · L9 · L10 · L14 · L15 · L16 · L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L18 · L28 · L29 · L31 · L32** (both files are COMMITTED: no user data — no audio, no transcript, no line of his notes, no value of the coach spec, no ticker, price or P&L of his) **· L35 · L36 · L37** (you derive; a SPLIT is carried, never settled by vote; a seat that holds a side recommends nothing to him) **· L38 / L40 · L42 · L43 / L66 · L44 · L45 · L52 · L53** (read BY ITS TEXT: the Finviz ceiling, scan cadence and pool settings are his; an engine tunable is not) **· L57** (set aside for audio by R18) **· L67 · L68** (migration NUMBERS are the gate's) **· L70 · L71 · L72** (P-b: a seam is ONE document both lanes cite) **· L73 · L74**. CLAUDE.md's absolute boundary: nothing in the derived design reads, writes or infers from his trading platform; no order is ever placed; the trading PC is a browser tab at most.
2. THE ROUND-1 RULINGS, whole: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-tribunal-2026-09-23.md` — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Anthropic-seat round-1 claims, file-checked` (the FC rows, or its one line saying the seat's report was not done at collate), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/voice-v3-tribunal/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line (the raw file is what "verbatim" means, but you never copy a user-data span out of it) · the Anthropic seat's round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-tribunal-fable-r1-2026-09-23.md` — including its `## Self-attack` (its `WITHDRAWN:` lines bind you: a withdrawn sentence is never folded).
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/VOICE-v3-proposal-2026-09-23.md` in full; its author's report `reports/voice-v3-draft-2026-09-23.md` (§0, `## v2 → v3`, `## Owner items`, `## ESCALATE`); `cto-2026-09-23.md` §4 row **R18** and `cto-2026-09-22.md` rows **R92, R93, R99, R100, R109** (`grep -n "^| R18 "` etc.); the earlier design `docs/30 - Design/DRC-VOICE-v2-2026-09-22.md` only where the proposal's §12 cites it; the DRC build prompts `prompts/2026-09-22/49-drc-d2-build.md`, `50-drc-d3-build.md`, `52-drc-d5-build.md` by their anchors only where a seam fold turns on them.
4. Only where a fold turns on it: the real file the hub's file-check cites (code on main, migrations, configs, the lock file, the heartbeat, the device facts in `topics/devices.md` :8, :11, :12, :17, :21, :30, :31) — read to check, never to copy.

THE RULES OF THE DERIVE — `61`'s rules (the bars tribunal's lesson: everything a derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding, with this tribunal's substitutions:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into the derived design word for word, or it is not taken. You may choose BETWEEN seats' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check, or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **An Anthropic-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's FC rows exist, they are your filter for that seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the seat's report as not done at collate, every seat claim is UNCHECKED: you may take a seat wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure. Where the Anthropic seat's wording, or the proposal's text, competes with a house's wording that is equally correct, the house's is taken — your own house's text is never preferred.
- **His R18, R92, R93, R99, R100 are NEVER re-opened.** A wording that would keep audio after transcription, put audio in the vault, the database or git, add a second widget, limit voice to fewer functions than R18 (a) names, withhold the record button from any house, let a re-drop or re-build rewrite, reorder or remove a byte of his unit, or drop the no-trade DRC, is NOT folded: it is carried in `## Dissents, verbatim` and named in `## ESCALATE` as `RE-OPENS A RULING (<R-row>)` — his to rule again if he chooses, never yours.
- **THE OWNER TEST (R18 (d)) — the filter on everything that reaches him.** An item goes to `## FOR DEJAN` ONLY if it is his MONEY, his DATA, his TIME or a LAW HE OWNS, AND no house can decide it; its entry names which of the four and why no house can decide it. An item a seat called his that FAILS the test is NEVER sent to him: it is settled in the derived design by a seat's wording or the proposal's text (the smaller mechanism that meets the laws, per the rule below), or — when the seats disagree on whether a mechanism is CORRECT — listed under `## NEEDS ROUND 2` for the houses. Record each such item in `## OWNER TEST` (item · who raised it · which of the four it claimed · PASSES / FAILS · where it went).
- **The DRC FINAL is not rewritten here.** A seat's ruling that the DRC design or its build prompts must change — the landing function D3 calls, a `VaultWriter` op, the A31 line in D5-3 — is stated in the derived design as SEAM S2 (what this design needs from which DRC chunk, and when) and carried in `## ESCALATE` for the desk, never folded as a change to them. Likewise SEAM S1 (the one model-access module): the derived design states what the rulings leave; which lane creates the module first is the desk's ordering, stated as a fact.
- **The routing cluster is FROZEN** (L5, L21–L27, L29, L49). A wording that would rewrite routing law or assign a routing seat is not folded; the derived design cites the laws as they stand.
- **Prefer the simpler mechanism.** Where two seats offer different mechanisms for one item and both meet the laws above, take the smaller one — and settle it: a trade-off between two lawful mechanisms is the HOUSES' choice, never an owner item (R18 (d)); record the other's cost in the fold row. Two seats disagreeing on whether a mechanism is CORRECT → `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` beside the proposal's E1–E11, placed before the slice it gates, with the result that would change the design — never an argument in the design's text, never a defect.
- **Invent nothing.** The derived design adds no mechanism, number, rule, key, column, table, route, engine, model, tool or slice that neither the proposal nor a seat's ruling contains. A gap no ruling fills stays a named gap (an experiment, or a round-2 item) — never your filler, and never an owner item unless it passes the test.
- **Engine tunables are the houses'.** A clip length, timeout, TTL, scratch age or history length is set by the builder from the named measurement, committed with its source line; a seat's proposed VALUE is recorded as `<key>: value proposed by <seat> — <file>:<line>` and taken only with its measurement.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT BUILD` that the derived design does not follow goes into `## Dissents, verbatim` word for word, with its house and item.
- **No user data in either file** (L32): a span of his quoted anyway is taken with that span replaced by `[user data: <file:line>]`; count every redaction.

WRITE (Write tool, TWO new files, these names WHATEVER the date of the run):
- `/Users/cobalt/cobalt/docs/30 - Design/VOICE-v3-derived-2026-09-23.md` — the proposal, whole (§1–§14 and `## OWNER ITEMS`), with each amended paragraph REPLACED in place and tagged `[F-nn]` (so a reader can diff it against the proposal section by section); a header naming the proposal, R18 / R92 / R93 / R99 / R100 / R109 and the derive row, the rulings of every seat that ruled (paths; Astra `METER — ASTRA PENDING (R13)`) and the fold table's path; `## First-gate experiments (L70)` = the proposal's E1–E11 and UNPROVEN rows F12–F13 as the rulings leave them, plus every experiment the seats named that the fold takes, each before the slice it gates; `## Seam` (S1, S2 per the rules above); `## Dissents, verbatim`; `## L52 and the bar` — the proposal's §14 re-answered for the derived design, with L1, L2, L3, L7, L23, L25, L28, L37 each stated; `## OWNER ITEMS` = exactly the items that PASS the owner test (it may be EMPTY). Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-derive-2026-09-23.md` — §0 Headline ≤5 lines (folds, round 2 or not, owner items, Astra pending) → `## DIGEST FOR THE DESK` (≤40 lines: what the derived design changed from the proposal, item by item in one line each; which seats ruled round 1; how many Anthropic-seat claims were hub-checked, how many UNCHECKED; what goes to round 2 and why; the owner items; the slices, seats and RESTARTS; what waits on the DRC build (D2 / D3) and on the model-access seam; the hours beside the DRC build and S3, a GUESS marked as one) → `## Fold table` (`F-nn · item (T1–T12, (a)–(g), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (seat, or "proposal kept") · adopted verbatim? yes / no / not taken · why (≤25 words, citing the hub's `Checked` row or `FC<n>` where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`, or `RE-OPENS A RULING (<R-row>)`, or `DRC FINAL — carried as a seam`)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT BUILD`, or two seats disagree on whether a mechanism is CORRECT; each with both positions quoted and the question round 2 must answer; EMPTY = the tribunal closes) → `## OWNER TEST` (every item any seat or the proposal called his, PASSES / FAILS, where it went) → `## FOR DEJAN` — ONLY the items that PASS, each in HIS terms (what he would say to the widget, what he would hear back, what changes in which note), as ONE yes / no or A/B question, naming which of money / data / time / law it is and why no house could decide it, ≤4 lines each, NO recommendation (L37 — the Anthropic house proposed and ruled); then, as the LAST entry, the ONE approval R18 (d) promises: `APPROVE: the VOICE v3 design (docs/30 - Design/VOICE-v3-derived-2026-09-23.md, sha256 taken by the desk at commit) for build — yes / no`, and nothing else → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` (every `RE-OPENS A RULING`, every DRC-FINAL change a seat asked for, the S1 model-access seam, every item that failed the owner test and was settled or sent to round 2, every DOES-NOT-HOLD wording a seat still presses, every `ASK DESK`) → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `VOICE V3 DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both file paths, reads its own report first and continues from `## CONTINUE`; the derived design, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the derive report, L71): `VOICE V3 DERIVED · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes`, `owner items` = entries of `## FOR DEJAN` before the APPROVE line — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits both files, brings him `## FOR DEJAN` (the owner items that passed, then the ONE approval), and the build prompts are drafted (the experiments first, then V1, ordered against the DRC build and the model-access seam at the desk's L68 gate); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a run on 2026-09-24 or later needs his row carrying `VOICE V3 TRIBUNAL: Bash(grok *) and Bash(agy *) through <date>` — R30 ends 2026-09-23 23:59 ET); in every case Astra reads the FINAL on Sat 09-26 (09-22 R13).
