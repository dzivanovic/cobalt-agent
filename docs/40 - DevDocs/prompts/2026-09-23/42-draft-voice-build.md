MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `voice-build-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/42-draft-voice-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control voice-build-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps.

# DRAFT THE VOICE v3 BUILD — SLICE V1 FIRST (the first end-to-end conversation), then the later slices as a queue

LADDER: S3 design lane → build (09-22 R100 "as soon as possible"; R18; R57 APPROVED). LAW STEP: `reports/cto-2026-09-23.md` §4 R18, R39, R56, R57 (his words there); `LAWS.md` L28 as amended 2026-09-23 (the VOICE-ORDERED EDIT paragraph — read it verbatim). The design is `docs/30 - Design/VOICE-v3-FINAL-2026-09-23.md` (approved at `43a11a1`, sha256 prefix `8c469b9544282b55`) with his item-2 = B and item-3 = B + his clause (R56) — the FINAL's text for item 3 still says "refused if the span changed"; the LAW (L28 as amended) governs: written even if changed in the interim, before / after versioned.

INDEX CARD: `LAWS.md` in full (L59). The FINAL whole (slices §9, experiments owed per slice, seams S1 — the new core's first model call module — and S2, R2-3's HITL-draft slice V5). `docs/40 - DevDocs/UNATTENDED-LAUNCH.md`. Precedent build + check prompts: `prompts/2026-09-22/76-s2-smoke-fixes-build.md`, `prompts/2026-09-23/28-jev-trial-build.md` (a new-core module), `prompts/2026-09-23/06-s2-smoke-fix-check.md`.

WRITE (whole files):
(a) the SEAM S1 document the FINAL requires before V1 (`docs/30 - Design/VOICE-v3-SEAM-S1-2026-09-23.md`): the ONE module V1 creates for the new core's model access (local model via the LiteLLM route; routing cluster frozen — cite, never rewrite L5 / L22), its interface, and what later callers (JEV's collector, the typed-classifier lane) share.
(b) `prompts/2026-09-23/43-voice-v1-build.md` — Opus 5.5 builder, new worktree `~/cobalt-wt/voice-v1` (branch `voice/v1-0923` off main; the desk runs `worktree add`), the V1 slice exactly as the FINAL defines it, experiments the FINAL gates V1 on run FIRST (measured on this Mac), offline tests + the with-DB suite if V1 touches the DB, DevDocs; every rule string listed, NEW ones marked for his ONE approval list (a vault write in V1 = a write path → Opus + `acceptEdits` + full allowlist, L29 / L62 / L63 interim practice).
(c) `prompts/2026-09-23/44-voice-v1-check.md` — L67 code check (`06`'s shape).
(d) In the report, the QUEUE for V2–V5 (one line each: what, hours, gate) — not drafted today.

REPORT (write LAST): `docs/40 - DevDocs/reports/voice-build-draft-2026-09-23.md` — §0 ≤5 lines · NEW strings (his approval list, one line each) · the V2–V5 queue · `## ESCALATE` · last non-blank line: `VOICE BUILD DRAFTED · slice: V1 · experiments first: <n> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
