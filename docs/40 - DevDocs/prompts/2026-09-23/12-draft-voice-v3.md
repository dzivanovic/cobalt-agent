MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat (design lane) · SEAT: drafter `voice-v3-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/12-draft-voice-v3.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control voice-v3-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, 09-20 R13) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write (L58) — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue with the safe default. Never end a turn between steps.

# VOICE v3 — RE-PROPOSE UNDER HIS 09-23 DIRECTIVE, AND DRAFT THE TRIBUNAL THAT DERIVES IT (L67), SO HE RECEIVES ONE FINISHED DESIGN AND ONE APPROVAL

LADDER: S3 design lane (09-22 R100: "a per-trade VOICE CAPTURE becomes its own design item NOW … B as soon as possible"), parallel to every build (L72). LAW STEP: his R18 (`reports/cto-2026-09-23.md` §4 — READ HIS WORDS THERE VERBATIM FIRST) supersedes the v2 scope and the v2 audio defaults. He was asked 11 design questions (v2 W2–W12) that the houses should have settled; he called that a failure ("You did not do it properly. Otherwise this would not come to me as questions."). v3 must not repeat it: every design choice is made by the houses; ONLY a true owner decision (his money, his data, his time, a law he owns) may reach him, and each such item must say why no house could decide it.

THE DIRECTIVE (R18, binding, not reopened):
(a) ONE press-to-talk widget inside Cobalt; BIDIRECTIONAL — Cobalt answers by voice; a Jarvis-like conversational agent that EXECUTES what he asks, including knowing which field in which file to update from his words; it covers EVERY speech function discussed (per-trade answers, per-day block, no-trade reason, DRC, and any command).
(b) Audio is EPHEMERAL: local agent scratch only, deleted once transcribed and the command has run; NEVER in the vault, NEVER in the database (no audio rows, no audio blobs), NEVER in git (tests use synthetic audio in the real file format, L45 / L32).
(c) Every house has the same permissions as the desk — the record button included (L44).
Set aside by his instruction (L73, already recorded): L57's "replayable" for audio — the transcript + the executed command are the stored inputs.

INDEX CARD
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59). Binding for the design: L1, L2 (no LLM in a watch loop — a press-to-talk turn is an event, not a loop), L4 / L41 (no secret to a model), L5 / L22 / L23 (routing: local speech-to-text and text-to-speech first; the routing cluster is frozen — cite, never rewrite), L7 (a voice command that changes trading logic still needs his approval), L9 (loud degradation), L14 (one throat — the voice agent speaks as Cobalt's chief of staff, not a war room), L16 / L40 (who owns each side effect — the voice agent ASKS the owning expert, never writes itself), L28 (vault writes: marker-bounded units / blank template cells, versioned; a voice-ordered edit of his notes is a Cobalt vault write — design how it satisfies L28, or name the exact L28 amendment as ONE owner item), L32, L37, L44, L45, L52 if anything reaches the card, L57 (as set aside above), L67.
2. `reports/cto-2026-09-23.md` §4 R17 / R18; `areas/cobalt-product-definition.md` (2026-09-22 / 2026-09-23 lines); `reports/drc-voice-propose-2026-09-22.md`, `reports/voice-tribunal-2026-09-22.md`, `reports/voice-tribunal-fable-r1-2026-09-22.md`, `reports/voice-tribunal-derive-2026-09-22.md` (v2 — reuse what still holds, mark every superseded clause), `topics/devices.md` (the Mac, the local model, the voice stack facts), the ASET web code layout (`src/cobalt/aset/`) by `ls` / `grep` only.
3. Tribunal precedent prompts: `prompts/2026-09-22/58-draft-voice-tribunal.md`, `59-voice-tribunal.md`, `60-voice-tribunal-fable-seat.md`, `61-voice-tribunal-derive.md`.

WRITE (Write tool, whole files):
(a) `/Users/cobalt/cobalt/docs/30 - Design/VOICE-v3-proposal-2026-09-23.md` — the proposal: architecture (widget, capture, local STT, the conversational agent and its tool boundary, TTS reply, confirmation of side effects by voice, the ephemeral-audio lifecycle with the exact scratch path and deletion point, failure modes and their loud states), how each speech function maps onto it, how "which field in which file" is resolved deterministically (tools fetch, agents reason; numbers are code), the L28 path for voice-ordered edits, a slice plan (first slice = the smallest end-to-end conversation that runs a real command), experiments owed (measured on his devices), and a `## OWNER ITEMS` section that is EMPTY unless an item passes the test above.
(b) `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/13-voice-v3-tribunal.md` + `14-voice-v3-tribunal-fable-seat.md` + `15-voice-v3-derive.md` — the L67 tribunal on (a), shape = 09-22 `59` / `60` / `61` (Grok · Gemini · the Anthropic seat, Opus 5.5 per R109; Astra METER until Sat 2026-09-26 06:47 ET — 09-22 R13 override, Astra reads the FINAL Saturday); the derive's `## FOR DEJAN` may hold only items that pass the owner test; stagger literals and launch rows `R__` for the desk; rule strings byte-identical to `59` / `60` / `61` — list any NEW one.

REPORT (write LAST): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-draft-2026-09-23.md` — §0 ≤5 lines · what v3 keeps / supersedes from v2 · owner items (count, each with why no house could decide it) · NEW strings · `## ESCALATE` · last non-blank line: `VOICE V3 DRAFTED · owner items: <n> · prompts: 3 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
