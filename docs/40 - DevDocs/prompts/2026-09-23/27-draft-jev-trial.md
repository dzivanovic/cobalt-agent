MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `jev-trial-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/27-draft-jev-trial.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control jev-trial-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, 09-20 R13) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO network, NO git write, NO launch (L36), no vault / memory write (L58) — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps.

# DRAFT THE JEV (typesafe.ai typed classifier) HANDS-ON TRIAL — THROUGH OPENROUTER ON COBALT'S FUNDED KEY

LADDER: OFF-LADDER — ruled `cto-2026-09-21.md` R31 / R34 / R35 / R37 (research ordered; trial APPROVED; the design tribunal after S2 closes, never dropped) and `cto-2026-09-23.md` R34 + R38 (his words: "No, JEV is https://typesafe.ai/ type safe llm clasifier, we worked on draft for this assesment" and "when we need to review JEV, openrouter has access and it is financed there. We can do test using cobalt's openrouter api key"). L72: parallel lane, holds nothing.

INDEX CARD
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59). Binding: L1, L4 / L41 (the OpenRouter key lives in VaultManager under `OPENROUTER_API_KEY`; NO session ever receives it — a Cobalt command fetches it on the host at call time; never printed, logged, in `.env`, in a command line or in a report), L5 / L22 / L27 (routing cluster frozen; this metered use is his per-case override, R34 — cite it, rewrite nothing), L9 (the collector interface), L10, L35, L45 (real-shape fixtures), L57 (every classification stored with its inputs, model id, latency, confidence), L62 / L63, L67, L70, L71.
2. The research: `docs/30 - Design/TYPESAFE-CLASSIFIER-RESEARCH-2026-09-21.md`, `…-DEEP-2026-09-21.md` (esp. §0, §2, §5 question sets S1–S8, the latency facts, the confidence / three query types of R37), `TYPED-DECISIONS-PATTERNS-RESEARCH-2026-09-21.md`; `reports/cto-2026-09-21.md` §4 R31, R34, R35, R37 (his words — WHY: "blazing fast … multi question hard selective prompt with several layers of deep selections and then derive the actual scoring in milliseconds"); `areas/cobalt-product-definition.md` (2026-09-21 line).
3. Code: `src/cobalt_agent/security/vault.py` (`VaultManager.get_secret`), the new core's one connection / secrets pattern (`src/cobalt/` — grep `VaultManager`, `COBALT_MASTER_KEY`), `src/cobalt_agent/config.py` + `configs/config.yaml` (the OLD tree's OpenRouter settings — reference only, never imported, L15 / strangler), the new core's CLI entry (`src/cobalt/cli.py` or equivalent).

WRITE (Write tool, whole files):
(a) `docs/30 - Design/JEV-TRIAL-PLAN-2026-09-23.md` — the trial: which question sets (S1–S8 from the research, real-shape inputs from Cobalt's own stored data, L45), what is measured per call (latency ms end-to-end, the answer, the confidence field, cost per call from the response's usage, the model id as returned), the comparison (the local model on the same typed questions; a Claude seat as a reference grader where the research used one), the pre-registered success bar (fixed before any call), the spend cap for the trial (propose a dollar figure; his to approve), and what the trial does NOT decide (the design tribunal decides adoption, R34).
(b) `docs/40 - DevDocs/prompts/2026-09-23/28-jev-trial-build.md` — an Opus 5.5 BUILD (`acceptEdits` is NOT needed: offline code + tests, no DB write; `--permission-mode auto`, the dev lane) in a new worktree `~/cobalt-wt/jev-trial` (branch `jev/trial-0923` off main; the desk runs `worktree add`): the SMALLEST new-core module `src/cobalt/classify/` with ONE collector class behind an interface (L9) that (i) fetches `OPENROUTER_API_KEY` from VaultManager at call time, (ii) lists the OpenRouter models whose id matches the Jev / typesafe product and FAILS LOUD if none (the exact id is NOT KNOWN to the desk — the build discovers it from OpenRouter's own model list, never guesses), (iii) sends one typed question and returns a Pydantic result (answer, confidence, latency, usage, model id); a CLI `cobalt classify trial --set <S#> --dry-run|--run` that runs the plan's sets and writes a local results file (NOT the vault, NOT the DB, NOT git — `~/cobalt-wt/jev-trial/scratch/`); offline tests with a recorded real-shape response (the first live call's response, secrets-free, becomes the fixture — L45); DevDoc per file. List the build's full allowlist; mark every NEW string (network calls happen only inside `uv run cobalt classify …`, never `curl`).
(c) `docs/40 - DevDocs/prompts/2026-09-23/29-jev-trial-check.md` — the L67 code check (shape `prompts/2026-09-23/06-s2-smoke-fix-check.md`: Opus 5.5 · Grok · Gemini; Sol METER) — secrets handling is the first question every checker answers.

REPORT (write LAST): `docs/40 - DevDocs/reports/jev-trial-draft-2026-09-23.md` — §0 ≤5 lines · the proposed spend cap · NEW strings (his ONE approval list, each one line) · `## ESCALATE` · last non-blank line: `JEV TRIAL DRAFTED · question sets: <n> · spend cap proposed: $<x> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
