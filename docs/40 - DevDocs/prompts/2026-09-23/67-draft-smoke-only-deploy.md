MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `smoke-only-deploy-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/67-draft-smoke-only-deploy.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control smoke-only-deploy-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps. **TIME: the deploy must start ≤19:55 ET and a house must read it first — draft in ≤20 min.**

# TONIGHT'S DEPLOY WITHOUT THE SETUPS BRANCH (L43 / L68 SCOPE) — `s2/smoke-fix-0922` ALONE

LAW STEP: the live-note fix build `64` stopped `FAILED: D2 — 1 live def(s) do not form where the branch's gate 2 says they form — second-chance: ftft=['not_formed'] … class (f) DEFECT/UNPROVEN` (`/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/live-note-fix-build-2026-09-23.md`). The setups branch turns the combined gate red against his live notes → L43 (as amended 09-21): "a branch waits only when it is not built, not checked, or turns the combined gate red — then it is dropped from the set, named, and the rest lands". `cto-2026-09-23.md` §4 R78. S2 closes on tonight's green smoke night, which needs the smoke fix live before the 21:10 replay.

INDEX CARD: `LAWS.md` in full (L59) — L42, L43, L51, L54, L62 / L63, L66, L67, L68 (SCOPE: the stack = only the branches that land), L70, L71, L73. Files: `prompts/2026-09-23/59-stacked-deploy-r3.md` (the base to re-issue from) and its report `reports/deploy-2026-09-23-r3.md` (ESCALATE 2–4: branch rebases, the gate branch at the stack, `cobalt_dev` migrated to `0013` — a migration main does NOT carry); `prompts/2026-09-22/05-stacked-deploy.md` + `reports/deploy-2026-09-22.md` (a recent deploy that shipped NO migration); `reports/cto-2026-09-23.md` §4 R5, R52, R53, R78; the smoke-fix branch `s2/smoke-fix-0922` (tip per `git log`), its check `reports/s2-smoke-fix-check-r2-2026-09-23.md`.

WRITE `prompts/2026-09-23/68-smoke-only-deploy.md` — `59` re-issued for ONE branch: no setups merge, NO production migration, NO R119 vault write, NO live-note step unless the smoke branch touches a `@requires_vault` path (say which); the gate = main + the smoke branch (a single-branch merge: rebase-then-ff, L54 — or the gate-branch shape if a string forces it; name which and why); RESTARTS per `cobalt jobs restarts` (the smoke build said aset + radar); THE DEV DB: `cobalt_dev` is at `0013`, which main + the smoke branch do not know — decide from the with-DB suite's own tests whether that is harmless or a guaranteed red, and if red, what lawful step restores it (a dev rollback string would be NEW — name it for his word); a fresh report `reports/deploy-2026-09-23-r5.md`, remote-control `smoke-only-deploy-0923`, `R__L` for the desk; every rule string BYTE FOR BYTE a subset of `07`'s (R52) — list any string not in `07` as NEW. Deadline gate ≤19:55 ET. Also write `prompts/2026-09-23/69-review-smoke-only-deploy.md` — the L67 house read of `68` (`06-review-stacked-deploy.md`'s shape of 09-22; Grok + Gemini + Opus reader; house lane; stagger literals incl. `62`, `65`, `19` PAUSED).

REPORT (write LAST): `docs/40 - DevDocs/reports/smoke-only-deploy-draft-2026-09-23.md` — §0 ≤5 lines · the delta from `59` · the dev-DB decision · NEW strings · `## ESCALATE` · last non-blank line: `SMOKE ONLY DEPLOY DRAFTED · migration: none · dev db: <harmless|needs <step>> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
