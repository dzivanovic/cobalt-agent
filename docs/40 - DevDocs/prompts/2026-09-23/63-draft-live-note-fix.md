MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `live-note-fix-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/63-draft-live-note-fix.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control live-note-fix-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps. **TIME: today's deploy must restart ≤19:55 ET — draft in ≤20 min.**

# THE LIVE-NOTE GATE: CLASSIFY (L75) THE TWO RED `@requires_vault` TESTS, DRAFT THE SMALLEST FIX + ITS CHECK + THE DEPLOY RE-ISSUE

LAW STEP: `59` stopped `FAILED: 2.3 (d) — live-note proof red — tests/cobalt/test_radar_evaluate.py:735 (backside ['not_formed']), tests/taxonomy/test_predicate.py:278 (nine-ema-scalp evaluable) · rollback: not used` (`reports/deploy-2026-09-23-r3.md`, whole — §0, ESCALATE 1–6). Offline 2494 and with-DB 2849 were GREEN; production untouched. The two tests run ONLY with `COBALT_LIVE_VAULT_ROOT` set (his real strategy notes, read-only) — no build or check suite ran them. `test_predicate.py:278` still asserts Rubberband is the ONLY evaluable def (the setups ladder change, his R15 / R17 / R44, makes more defs evaluable at defaults); `test_radar_evaluate.py:735` expects Backside `['not_formed']`.

INDEX CARD: `LAWS.md` in full (L59) — L35, L45 (the real artifact is the test's input; the note is NEVER edited to fit — every divergence is fixed in code or in the test's stale expectation), L67, L68, L70, L73, L75. Files: the deploy report; both test files at the stack (`git -C /Users/cobalt/cobalt show c7b098e7:<path>`) and at main; his notes under `/Users/cobalt/Vault/Think/1 - Trading/4 - Strategies/` (READ only — Backside, 9 EMA Scalp, Rubberband); the setups FINAL `[F-16]`; `reports/setups-check-r4-2026-09-22.md`; `prompts/2026-09-23/39-seam-fix-build.md` + `40` + `59-stacked-deploy-r3.md` (shapes to reuse).

DECIDE per test (L75, file evidence): STALE EXPECTATION (the test asserts the pre-ladder-change world — the fix moves the expectation, the test's intent kept) | CODE DEFECT (the engine mis-reads his real note — the fix is in code) | UNPROVEN (the build step prints the evaluation of each def against his live note before any change).

WRITE (whole files): `prompts/2026-09-23/64-live-note-fix-build.md` (Opus 5.5 in `/Users/cobalt/cobalt-wt/setups-c1` on `setups/seven-0921` at its current tip; runs the live-note suite with `COBALT_LIVE_VAULT_ROOT=/Users/cobalt/Vault/Think uv run pytest *` — an approved string (R52) — READ-only on the vault; `39`'s allowlist + that string; stop `LIVE NOTE FIX BUILT <tip> | … | live-note <p>/<f> | …`), `prompts/2026-09-23/65-live-note-fix-check.md` (`40`'s shape, house lane, Grok · Gemini · Opus 5.5; it also reads `66`), and `prompts/2026-09-23/66-stacked-deploy-r4.md` = `59` with ONLY: the new setups tip, a fresh report `reports/deploy-2026-09-23-r4.md`, its own name + remote-control `stacked-deploy-r4-0923`, P6 also reading `65`'s DONE line, `R__L` for the desk (R52 carries as approval). Deadline gate ≤19:55 ET.

REPORT (write LAST): `docs/40 - DevDocs/reports/live-note-fix-draft-2026-09-23.md` — the classification per test with evidence · NEW strings · `## ESCALATE` · last non-blank line: `LIVE NOTE FIX DRAFTED · classes: <a>/<b> · prompts: 3 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
