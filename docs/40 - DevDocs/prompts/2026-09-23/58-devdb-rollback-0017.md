MODEL: Opus 5.5 (`claude-opus-5-5`, R32 / R109) — a DEV-DB WRITE (one migration reversed on `cobalt_dev`), so the Opus floor and NOT auto mode (L29) · SEAT: hub `devdb-rollback-0017`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/voice-v1` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/58-devdb-rollback-0017.md' and follow it exactly." --model claude-opus-5-5 --permission-mode acceptEdits --remote-control devdb-rollback-0017 --allowedTools "Bash(date*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/voice-v1/.env)" "Bash(rm /Users/cobalt/cobalt-wt/voice-v1/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0011)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" "Bash(*cobalt_brain*)" "Bash(*--allow-prod*)" "Bash(*--prod*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · `acceptEdits` + this exact allowlist; an unlisted command = do not call it; never `bypassPermissions`; NO production anything, NO git write, NO launch (L36), no vault / memory write; the report via the Write tool · METER: Anthropic small · nobody sits at this terminal: `FAILED: <step> — <reason>` as the last line is always a correct ending (L62).

# REVERSE `0017_voice_turns` ON `cobalt_dev` — AND NOTHING ELSE

WHY: `41` (the stacked deploy) stopped `FAILED: 2.3 — with-DB suite red on the stack … test_tenancy.py::TestPlacement::test_every_table_is_on_its_ruled_side (user.voice_turns: not in the placement map) …` because the voice V1 build applied `0017_voice_turns` to `cobalt_dev` and left it (`voice-v1-build-2026-09-23.md` :376). HIS APPROVAL: `reports/cto-2026-09-23.md` §4 **R72** ("approved", the NEW string `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0011`). This worktree (`voice/v1-0923`) holds `src/cobalt/db_migrations/0017_voice_turns.rollback.sql`; main does not — that is why it runs HERE.

INDEX CARD: `LAWS.md` — L1 (loud), L4 / L41 (`.env` copied by name, never read or printed, removed on every path), L29, L35 (every claim is tool output), L48 (clock times from `date`), L62 / L63, L70, L71.

STEPS, one Bash call each, each quoted VERBATIM in the report the same turn:
1. AUTHORIZATION: `grep -n "^| R72 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` → the row carries `--rollback --down-to 0011` and his `approved`; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"--rollback --down-to 0011" -- "docs/40 - DevDocs/reports/cto-2026-09-23.md"` NON-EMPTY. Else `FAILED: authorization mismatch`.
2. LANE: `ls -la /Users/cobalt/cobalt-wt/*/.env` → MUST list nothing (another build holds `cobalt_dev`) — else `FAILED: PREFLIGHT — dev-DB lane busy: <path>`. `ls src/cobalt/db_migrations/0017_voice_turns.rollback.sql` → exists.
3. `date` · `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/voice-v1/.env`
4. `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → BEFORE: `voice_turns` present (quote its row and the table count).
5. `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0011` → quote whole; it must reverse ONLY `0017`; any other migration reversed or any error → step 7 then `FAILED: ROLLBACK — <line>`.
6. `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → AFTER: no `voice_turns`; every other table's row count and digest EQUAL to step 4's (L35 — compare row by row in the report).
7. `rm /Users/cobalt/cobalt-wt/voice-v1/.env` then `ls -la /Users/cobalt/cobalt-wt/voice-v1/.env` → "No such file" (on EVERY path, the failures included).

REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/devdb-rollback-0017-2026-09-23.md` — §0 ≤3 lines · each step · last non-blank line: `DEVDB 0017 ROLLED BACK · voice_turns: gone · other tables: <n> unchanged · .env: removed, proven gone` or `FAILED: <step> — <reason>`.
