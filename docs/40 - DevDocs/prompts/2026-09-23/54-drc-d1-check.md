MODEL: Sonnet 5 (`claude-sonnet-5`) — stage ONE packet before FOUR checkers, file-check what they say, tabulate; no verdict of your own (L37) · SEAT: build-check hub `drc-d1-check`, launched by the CTO desk in the background · LADDER `S3-P3 · F14` · LAW STEP (L67): v2 derived → his rulings R90–R103 → build `prompts/2026-09-23/53-drc-d1-build.md` → **THIS = CHECK ROUND 1 of at most 3, the WHOLE D1 build** → a fix round (L75: classify first) if a defect HOLDS → D4 / D2 stack. RULE STRINGS: `16-setups-check-r2.md`'s launch line BYTE FOR BYTE (its fifteen allow strings, three denies, `--add-dir` triplet — the four it names as carried-unused stay carried-unused); only the prompt path and the remote-control name differ; NO NEW STRING. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/54-drc-d1-check.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control drc-d1-check --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/tribunal-bars-0920)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" "Bash(claude -p --model claude-opus-5-5 *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode (a read-only seat, L62 as amended R80: stated here and on the line); never `bypassPermissions`. NO production command, NO `cobalt_dev`, NO database, NO `docker`, NO `pytest` (the suites are the builder's artifact), NO git write, no vault write, no memory-folder write (L58) · METER: Anthropic small for you; per checker one headless check — Sol (`gpt-5.6-sol`, probed; METER until Sat 2026-09-26 06:47 ET per R13 → recorded, skipped), Opus 5.5 (`claude -p --model claude-opus-5-5`, R32 / R36 — the Anthropic code-check seat of R46), Grok, Gemini (`agy`) — ≥3 must check (L67; R46's seats) · nobody sits at this terminal — the report is your channel (`FAILED:` stops safely). DESK: watch the report's LAST NON-BLANK LINE (L71), `^(DRC D1 CHECK DONE|FAILED)`.

# BUILD CHECK, ROUND 1 — DRC D1: THE TWO DAILY-FILE PARSERS, PAIRING, CARRIED POSITIONS, STORE, ONE MIGRATION. Four checkers read the SAME packet independently, from READS ALONE; you stage, launch, then walk every checker claim through the branch's own files (file:line) and tabulate. NOTHING is built, run, merged, rebased or deleted by this run. DO NOT STOP until the report ends `DRC D1 CHECK DONE …` or `FAILED …`.

RE-ISSUED 2026-09-22 (L19, whole file) by the Opus 5.5 prompt seat `drc-reissue-draft-0922`: the QUESTIONS gain R114's header-detection and several-playbooks checks and R117's constructed names; `rulings.md` carries R114 and R117 so the checkers can read them.

RE-ISSUED 2026-09-23 (L19, whole file) by the Opus 5.5 prompt seat `drc-r17-draft-0923`, in step with `48`'s R17 re-issue (L67): the QUESTIONS gain his 09-23 R17 (3) the date from the import, (4) the stop only from the journal's own stop field, (5) a partial header READ and flagged `PARTIAL`; `rulings.md` carries `cto-2026-09-23.md` §4 R17.

RE-ISSUED 2026-09-23 (L19, whole file, NEW NUMBER `54` — `prompts/2026-09-22/53-drc-d1-check.md` is superseded and never launched) by the Opus 5.5 prompt seat `drc-d1-reissue-0923`, in step with the build's re-issue as `prompts/2026-09-23/53-drc-d1-build.md` (L67; his 09-23 R59): every pointer to the build prompt re-pointed to `53`; the build is now on `main` (not D4's tip) and carries the registry seam files D1-5 now names (`db_migrations/__init__.py`, `test_archiver_migrations.py`, `test_tenancy.py` only if needed); the date gate names 09-22 R105; `rulings.md` carries `cto-2026-09-23.md` §4 R59. The launch line differs from `53`'s only in the prompt path.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (drafted by the Opus 5.5 prompt seats `drc-build-draft-0922` and `drc-d1-reissue-0923`), not by Dejan. Each its own Bash call:
- `16`'s AUTHORIZATION gates UNCHANGED for every string of the line (copy them from `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/16-setups-check-r2.md`, with its failure strings: R13 of 09-20 · R46 (`instead of Astra you can use Sol`) · R49 (`"Approved"`) for the Sol string · R32 for the Opus 5.5 string · the thirteen + three `grep -c -F` counts on `08-bars-chunk-e-check.md`), EXCEPT its round-2 rows (the fix-round call, its own launch row) which are replaced by the two below.
- **THE DATE + EXTENSION GATE (`66`'s wording, the FIRST PREFLIGHT row AND again immediately before the checkers launch):** `Bash(grok *)` / `Bash(agy *)` stand through 2026-09-23 23:59 ET on his R30 (`cto-2026-09-22.md`), and through 2026-10-07 23:59 ET for the DRC check hubs on his R105 (`cto-2026-09-22.md` §4: `grep -n "^| R105 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` carrying `Bash(grok *) and Bash(agy *) through 2026-10-07`, and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"through 2026-10-07" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY). R105 names the hubs "`53`–`57`" in the 09-22 numbering; this file is `53` re-issued whole under a new number (L19) — RECORD `R105 scope: read as 53 re-issued` under `## ESCALATE` (the desk put that reading on his ONE list before launch, `reports/drc-d1-reissue-2026-09-23.md`) unless the launch day's desk file carries a row of his extending R105 to `2026-09-23/54` (quote it). On a date past 2026-10-07 ONLY a committed row of HIS in `cto-<date>.md` §4 carrying `Bash(grok *) and Bash(agy *) through <D>` (or a later date) and his words in quotes counts — a DESK LAUNCH ROW with "NO WORDS OF HIS" does NOT; missing → `FAILED: authorization expired — no committed row of his extends grok/agy to <D>`, launch nothing.
- **THIS launch** is row **R90** in the launch day's desk file (still `__` → `FAILED: authorization mismatch — launch row not filled in`): `grep -n "54-drc-d1-check.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-2<n>.md"` → a `| R` row; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"54-drc-d1-check.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` NON-EMPTY (desk files ONLY).
YOU CAN ALWAYS STOP with `FAILED: <step> — <concern>`.

INDEX CARD (you; the checkers get the packet only): (1) `16` — its UNATTENDED RULES, staging rules, seat launch spellings (`66` §2 by reference), 45-minute clock, written-nothing proof, collation rules and ASK DESK / RECOVERY paragraphs bind you UNCHANGED, with `scratch/tribunal-bars-0920/drc-check/d1/` in every path and output files `grok-check.md`, `gemini-check.md`, `sol-check.md`, `opus-check.md`. (2) `66-setups-one-check.md` (a ROUND-1 whole-build check: its §1 staging of a build diff in parts). (3) The build prompt `prompts/2026-09-23/53-drc-d1-build.md` (rows D1-0 … D1-6, `## WITH-DB`, CLOSE). (4) v2 `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` §3, §4, §9 D1, §10 E1 / E4, §12. (5) `cto-2026-09-22.md` §4 R91–R94, R101, R103, R114, R117; `cto-2026-09-23.md` §4 R17, R59. (6) LAWS.md in full (L59): **L1**, **L9**, **L31**, **L32**, **L35**, **L37**, **L40**, **L44**, **L45**, **L47**, **L57**, **L67**, **L70**, **L71**, **L74** bind you here as `16` states them. Do NOT pre-read the build's code for defects before the checkers answer.

PREFLIGHT (METER, L47), one row each (rule · command · exit · allowed/DENIED + reason verbatim), in this order: `date` (DATE GATE) · `grok --version` · `agy --version` · `ls /Users/cobalt/cobalt-wt/drc-d1` (absent → `FAILED PREFLIGHT: drc-d1 worktree missing`) · `tail -n 3 "/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-d1-build-2026-09-23.md"` — **the LAST NON-BLANK line must start `DRC D1 BUILT `; else `FAILED PREFLIGHT: D1 is not built — last line: <verbatim>`, stop.** Take `<tip>` and `<base>` FROM IT · `git -C /Users/cobalt/cobalt log --oneline <base>..<tip>` · `git -C /Users/cobalt/cobalt log --stat --oneline <base>..<tip>` (your staging list AND the boundary: `53`'s CLOSE list) · `ls /Users/cobalt/cobalt-wt/drc-d1/.env` MUST be "No such file" (present → ESCALATE as L41, continue; never read it) · `ls scratch/tribunal-bars-0920/drc-check/d1` (RECOVERY: "No such file" = fresh) · THE HOUSE-LANE STAGGER: `tail -n 1` of every other check / tribunal report the desk's §5 sessions table lists as RUNNING a Grok / Gemini hub — the desk writes the path here: **`__`** (unfilled → `FAILED PREFLIGHT: stagger row not filled in`); its line must start with its DONE shape or `FAILED`, else `FAILED PREFLIGHT: <job> is running — the desk staggers` · THE CODEX LAUNCH SHAPE row (`16`'s `grep -c -F "Experiment field: **reads started**"` row) · THE FOUR PROBES exactly `16`'s (Sol expected METER → recorded, skipped). **FAIL CLOSED ON THE COUNT:** fewer than THREE up → `FAILED PREFLIGHT: fewer than three checkers — <each down house with its line and retry time>`, launch nothing.

REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-d1-check-2026-09-2<n>.md` (the run day; Write tool; you commit nothing). Sections: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## CONTINUE` → `## Per question` → `## Assertions and boundary` → `## Checked against the branch` → `## Ready for the next chunk` → `## ESCALATE` → last line. Last line while running EXACTLY `(run in progress — next step under ## CONTINUE)`; fallback path on a refused Write: `scratch/tribunal-bars-0920/drc-check/d1/CHECK-REPORT.md`, refusal named in §0.

## 1. Stage the packet in `scratch/tribunal-bars-0920/drc-check/d1/` — `16` / `66` §1 staging rules (no `mkdir`; Read → Write byte-identical; parts ≤ 38,000 B cut at `commit` / `diff --git` lines; `wc -c` each copy vs original)
(1) `build-diff.md` = `git -C /Users/cobalt/cobalt log -p <base>..<tip> -- . ":(exclude)docs"` (fixtures included — they are real SHAPE, stripped; a checker checks that they carry no value of his by reading them, L32). Commit and `diff --git` counts checked against PREFLIGHT's `--stat`; mismatch → `FAILED: packet — the build diff is incomplete`. (2) `devdocs.md` = the same range `-- "docs/40 - DevDocs/cobalt"`. (3) `build-report.md` = the builder's report, whole (parts at `## ` boundaries). (4) `design.md` = v2 §3, §4, §9, §10, §12 VERBATIM, headed by path + line ranges. (5) `rulings.md` = the §4 rows R91, R92, R93, R94, R101, R103, R114, R117 of `cto-2026-09-22.md` VERBATIM, then the §4 rows R17 and R59 of `cto-2026-09-23.md` VERBATIM (headed by its path). (6) `prompt.md` = `prompts/2026-09-23/53-drc-d1-build.md` whole. (7) `QUESTIONS.md`, verbatim below, plus the "Files in this folder:" paragraph. HONEST SIZE into `## Packet` (bytes, ÷4 tokens per checker). NOT staged: his real files (they are his user data and live only in the vault; the checker judges the fixtures' SHAPE claims against the build report's `## E1` table).

QUESTIONS.md (verbatim): "You are one of four checkers of ONE BUILD CHUNK (round 1 of at most 3). The sentence that launched you says which house you are. Read ONLY the files in this folder. You cannot run commands. The chunk parses a trader's two daily files — a broker trading-log export (CSV) and a trade-journal daily log — into executions, trades (FIFO per symbol and direction, a trade closes at position 0), exit legs, open positions carried to the next day, and journal rows (entry, stop, target, assumed and realized R:R, MAE / MFE when present), matched to the trades; it stores them in three tables through one store and one migration. The design is `design.md`, the owner's rulings `rulings.md`, the build prompt `prompt.md`, the build `build-diff.md` + `devdocs.md`, and the builder's claims `build-report.md` (a CLAIM — check it against the diff). The fixtures are REAL-SHAPE copies of one real day with every value replaced; the builder's `## E1` table says what shape the real files have. The contract: *a red test is a RESULT; a test is fixed ONLY when IT is wrong; nothing may be invented where a source is missing (render `not given`); no value of the owner's may enter the repository*. For EACH row D1-0 … D1-6 of `prompt.md` (D1-2a included), exactly one of, each with `file:line`: `MEETS THE ROW` (the tests assert what the row says and go red on the regression they name) / `DOES NOT MEET — <file:line, the concrete input that shows it>` / `NEW DEFECT — <file:line>`. Look hardest at: **fail-loud** — one bad row: does the WHOLE file fail with zero rows stored, naming the line? an added column parses with the `trading_log_shape` flag? **partial files** (the owner's 09-23 R17 (5): a file with only some of its kind's columns is READ and shown loudly as PARTIAL) — a required column removed: is the file READ as `partial`, its rows parsed, that field `not given`, every step that needs the column `not computed — missing: <column>`, the flag `PARTIAL — missing: <columns>` on the result — never a default, never silent, never a skipped row? **the date** (R17 (3): the trading log has no date column; its date is the folder / upload date) — does every execution take its date from the import date the caller passes, with no date read from the file or inferred? is the time zone the builder used proven the way the report's `## E1` claims? **accounts** — two accounts are ONE bucket (the owner's R94), never a failure? **pairing** — partial exits, scale-ins, a position open at file end (`open`, held shares, not a failure), the next-day seed (a carried symbol with no prior row FAILs; a contradicting file FAILs), a reversal through 0 handled the way the builder's E1 row says the export books it? **the journal log** — columns read by name; a missing MAE / MFE column renders `not given`, never 0; an unmatched journal row is shown, never dropped or guessed; no tolerance literal in code? **the stop** (R17 (4)) — is the stop read ONLY from a stop column of the journal log, by name (the real header has none, so every stop must be `not given`), and NEVER computed from the dollar-risk column, a target, a ratio, prices or shares? **header detection** (the owner's R114: his files carry CSV content under a `.md` extension and a new name every day) — is every file identified by its HEADER LINE alone, through ONE classifier, with the name and extension never read (the same bytes under a `.md`, a `.csv` and no extension → the same kind)? a header matching neither kind → ignored and listed, never parsed? two files of one kind → FAILED naming both, neither picked? a header carrying only some of a kind's names → that kind as `partial`, decided ONLY by names unique to one kind (a name both headers share decides nothing), READ and flagged loudly (R17 (5)); unique names of both kinds → FAILED; only shared names → ignored; a partial file plus a complete one of the same kind → FAILED naming both? a folder reader never takes the `_reference/` folder as input? do the committed fixtures keep the real header line byte-identical (compare each fixture's first line with the build report's `## E1` header names and its HEADER PROOF rows)? does any identifier, test literal or source line carry the one vendor-named column name (it may live ONLY in the fixture files)? **several playbooks on one trade** (R114) — does a trade keep EVERY playbook name of its journal row, in the row's order, with nothing picking one, nothing de-duplicated, an empty cell → none (not a guess)? is the multi-playbook fixture trade built in the structure the report describes (an add before the first setup closes, several exits) with CONSTRUCTED names only (R117)? are the overnight-carry fixtures marked CONSTRUCTED (the real day had no carry)? **store / migration** — three tables only (`drc_imports` with the event-state columns, `drc_fills`, `drc_rows`), `user_id NOT NULL` + FK, one-side tenancy, a working `.rollback.sql`, NOT `fills` / `order_fills`, every write through `DrcStore`? the migration registry (`db_migrations/__init__.py` `FORWARD` / `REVERSE`) carries the new pair in order, and the registry test is re-pinned to this tree only (the numbers the sibling branches hold are named, not faked)? **names** — no vendor or person name in any identifier, enum value, table, column or config key (L31: `trading_log`, `stats_log`, `import_id`)? **L32** — do the fixtures, tests, DevDocs or report carry anything that looks like a real account id, a real ticker-and-price pair, or a real date of the owner's? **L57** — does every derived value land with its inputs and `fn_version`? THEN over the whole diff: (a) any assertion that is weak for what it claims (`NONE` or each), (b) anything changed outside the rows' named files (`NONE` or each hunk), (c) L52: can anything here reach a score, rank, grade or size (`NO PATH` or `<file:line> DOES — <how>`). Anything you cannot settle from reads: `NOT CHECKABLE FROM READS — <what would have to be run>` — not a defect. 'I would have written it differently' is not a finding. End with EXACTLY one line: `CHECK D1: BUILD STANDS` or `CHECK D1: BUILD STANDS EXCEPT <items>` or `CHECK D1: FIX <items>` — followed on the SAME line by ` · ready for the next chunk: YES|NO · <ten words of reason if NO>`."

## 2. Launch the checkers — `date` first (the DATE GATE's second row); `16` §2 / `66` §2 EXACTLY, with `drc-check/d1/` in every path, `QUESTIONS.md` as the start file, output files as named above, Opus's `--add-dir` naming ONE folder `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-check/d1`, Sol METER → not launched, recorded. HARNESS / METER / TIMEOUT recorded verbatim, never looped; fewer than three `CHECK D1:` lines → `ASK DESK: <house> did not check (<reason>) — relaunch it alone before D4 / D2 launch? [<time>]` (safe default: no retry by you).

## 3. Collate — you judge NOTHING (L37), you check facts (L35)
`## Per question`: one row per D1-0 … D1-6 (D1-2a included): `row · grok · gemini · sol · opus · MEETS: n of <checked>`, each DOES NOT MEET / NEW DEFECT in the checker's words ≤30 words with its `file:line`. `## Assertions and boundary`: per checker (a), (b), (c) verbatim. `## Checked against the branch`: EVERY DOES NOT MEET, NEW DEFECT, weak-assertion, outside-the-rows and L52 claim opened YOURSELF in `/Users/cobalt/cobalt-wt/drc-d1/` (Read tool) or `git -C /Users/cobalt/cobalt show <base>:<path>` → `claim · who · file:line · HOLDS / DOES NOT HOLD / NOT CHECKABLE FROM READS · ≤30 words` (L70: never restated as a defect when unproven). Also, yourself, plainly: (i) `git -C /Users/cobalt/cobalt log --stat --oneline <base>..<tip>` names only `53`'s CLOSE paths (D1-5's registry files included); (ii) `git -C /Users/cobalt/cobalt log --oneline <base>..<tip> -- src/cobalt/aset src/cobalt/prefill src/cobalt/replay src/cobalt/cards src/cobalt/radar src/cobalt/settings configs` EMPTY; (iii) `grep -rn -i "das_\|tradezella\|zella" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt/drc /Users/cobalt/cobalt-wt/drc-d1/src/cobalt/db_migrations` → only comment / docstring lines, each quoted; (iv) exactly ONE new migration pair, and `grep -c "CREATE TABLE"` in it = 3; its number = the one the build report's `## ESCALATE` (ii) states, with its re-derive list; (v) R17 (4): `grep -rn "Trade Risk" /Users/cobalt/cobalt-wt/drc-d1/src/cobalt/drc` → each hit quoted and placed — none derives a price. Where two checkers contradict, quote both. `## Ready for the next chunk`: `checker · CHECK D1 line · ready · reason`. `## ESCALATE`: every `FIX`; every DOES NOT MEET / NEW DEFECT / weak assertion / outside hunk that HOLDS; any L52 path; any L32 leak claim that HOLDS (FIRST, marked `L32 — before any merge`); a packet mismatch; a checker that did not check; every `ASK DESK`; the R105 scope record. You add no recommendation.

## 4. Close
Last non-blank line (L71): `DRC D1 CHECK DONE · grok: <CHECK D1 line|NO CHECK LINE|HARNESS|METER|TIMEOUT> · gemini: <…> · sol: <…> · opus: <…> · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the next chunk: <r> of <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`. `defects that HOLD` counts the claims that HOLD in your file-check (L32 leaks included). Then stop. NEXT, not yours: `houses that checked` ≥ 3, `defects that HOLD: 0` → the desk launches D4 (`51`, re-pointed onto this tip) and then D2 (`49`); a defect → a fix round drafted by classification first (L75), at most 3 rounds (L67 / L39).
