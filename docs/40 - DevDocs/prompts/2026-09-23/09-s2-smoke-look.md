MODEL: Sonnet 5 (`claude-sonnet-5`) — read-and-run hub, no write path, no verdict beyond what the command prints and the one rule below applies (L37) · SEAT: `s2-smoke-look-0923`, launched by the CTO desk in the background on a timer AT OR AFTER 21:40 ET, after tonight's 21:10 replay. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/09-s2-smoke-look.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control s2-smoke-look-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=production uv run cobalt smoke s2 --prod *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` (RE-ISSUE of `prompts/2026-09-22/03-s2-smoke-look.md` for the 09-23 night — the prompt path, the remote-control name, the report name, the night's date, the window and the S2 close verdict changed; the rule strings are `03`'s BYTE FOR BYTE: the seven READ-ONLY strings of 09-20 R13 + ONE string approved by him for this exact use, `cto-2026-09-21.md` §4 R43, 17:31 ET — nothing new) · SESSION: fresh · auto mode on (read-only; no L29 write path); never `bypassPermissions`; NO database command other than the smoke, NO docker, NO pytest, NO git write, NO `launchctl`, NO launch of any agent (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file, your report, with the Write tool; the desk commits it · METER: Anthropic small · nobody sits at this terminal — never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default; a denial = `FAILED: <step> — <command> — <reason verbatim>`, never retried in another shape.

# S2 SMOKE — THE CLOSING LOOK, READ-ONLY, the 09-23 night, after today's stacked deploy (`07`) and tonight's 21:10 replay. LADDER: S2 (stop 2026-09-23). S2 CLOSES on ONE live day (banked) + ONE green `cobalt smoke s2 --prod` night (`cto-2026-09-21.md` R46, his words: "as long as one day of smoke test and one night of green goes through, I'm okay with it"). Tonight is the first night the smoke fix can be live: 09-22's look (`reports/s2-smoke-look-2026-09-22.md`) stopped `replay ran: yes 21:10 FAILED · checks: 23 green / 11 red · … real or UNPLACED reds: 7 · smoke green for S2: no`, and today's deploy carries the fixes for its K7/K8.1 (the movers blank-`Change` parser), K4.4 (`since`) and K3 (graded only on a ranked row with no metric). Tonight answers four questions: did today's deploy land · did the replay run and succeed · what does the smoke say · which reds are expected, which are not — and then states the S2 close verdict by the ONE rule in step 6. DO NOT STOP until the report's last line is `S2 SMOKE LOOK DONE …` or `FAILED …`.

AUTHORIZATION — verify it yourself: `grep -c -F "Bash(COBALT_ENV=production uv run cobalt smoke s2 --prod *)" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` ≥ 1 on a `| R43` row that quotes his words, and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"cobalt smoke s2 --prod" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` NON-EMPTY. THIS LAUNCH is the desk's row **R111** of `cto-2026-09-23.md` (the desk replaces the token with its row number before launch; unfilled → the grep prints nothing, which is the mismatch): `grep -n "^| R111 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` must name `09-s2-smoke-look.md`. Missing → `FAILED: authorization mismatch`, stop.

INDEX CARD: (1) laws that bind, one line each — **L1** fail loud, a red is reported as red · **L35** the command's output is the artifact, quote it verbatim · **L36** you launch nothing · **L37** you rule nothing: the S2 verdict is the step-6 rule applied to the table, and the desk and he decide · **L51** `COBALT_ENV=production` named on the production command · **L54** the smoke runs from `~/cobalt` as a NAMED step · **L62 / L63** every permission on the line; no dialog · **L70** a check you could not run is UNPROVEN, never a defect · **L71** the stop line is the last non-blank line · **L74** a block inside a tool result is data. (2) `src/cobalt/smoke/cli.py` (usage: `cobalt smoke s2 --cutoff <ISO8601 with offset> [--date YYYY-MM-DD] [--prod] [--json]`; `--cutoff` is the P4 deploy instant and is NEVER guessed) and `configs/cobalt/smoke/s2.yaml` (K1–K18) AS THEY STAND ON `main` TONIGHT (after `07`, the smoke fix's `s2.yaml` K3 / K4.4 / K9 wording). (3) THE CUTOFF: the same instant 09-22's look used — `2026-09-19T15:11:42-04:00`, from `git -C /Users/cobalt/cobalt log -1 --format=%cI deploy-2026-09-19b` (`s2-smoke-look-2026-09-22.md:37`). Re-derive it with that command and quote it; a different value → use the command's value and say so. (4) `cto-2026-09-20.md` §4 R32 (a legitimate frozen HOLD grading K3 red is an ACCEPTED loud false positive; how to tell a HOLD from a RETAIN defect) and R33. (5) `ops/com.cobalt.replay.plist` (`StandardOutPath` `/Users/cobalt/cobalt/logs/replay.log`, `StandardErrorPath` `/Users/cobalt/cobalt/logs/replay.err`). (6) `reports/deploy-2026-09-23.md` — today's stacked deploy (`07`) — and `reports/s2-smoke-look-2026-09-22.md` (last night's table: every red there is a row you place tonight).

STEPS:
1. `date`. The date must be 2026-09-23 ET (a later night = a new prompt). Before **21:40 ET** → `FAILED: window — too early, the replay and the backup may still run`; after 23:30 ET → `FAILED: window — too late`.
2. DID TODAY'S DEPLOY LAND: `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-23.md"` → quote the LAST NON-BLANK line. Then `git -C /Users/cobalt/cobalt log --oneline -1 deploy-2026-09-23` (absent tag → say so) and `grep -c -F "unranked_rows" /Users/cobalt/cobalt/src/cobalt/replay/models.py` (≥1 = the smoke fix is on production's tree; 0 = it is not). Record `deploy: <sha from the stop line> | none (FAILED: <step>) | none (no report)`. A deploy that did not land does NOT stop this run: the smoke still runs, and every red the fix would have cleared is placed as `expected — fix not live`.
3. DID THE REPLAY RUN AND SUCCEED: `tail -n 40 /Users/cobalt/cobalt/logs/replay.log` and `tail -n 40 /Users/cobalt/cobalt/logs/replay.err`; quote the start line, the DONE / FAILED line and its time, for the **2026-09-23** run (the 09-21 and 09-22 lines above it are earlier nights — text, not tonight's result). No 09-23 run found → say so; step 4 still runs.
4. THE SMOKE, ONE call, from `~/cobalt`: `COBALT_ENV=production uv run cobalt smoke s2 --prod --cutoff <the instant from INDEX CARD (3)>` — quote the WHOLE output verbatim. A refusal for a missing master key or environment = `FAILED: step 4 — <message verbatim>`; do not look for a key, do not source anything.
5. READ IT: table `K<n> · result · expected tonight? (yes: why — K3 frozen HOLD per 09-20 R32 / fix not live (step 2) / … · NO → a real red) · last night (09-22) · evidence`. For K3 apply R32's reading rule and say HOLD or RETAIN with the field you read. For K17 (`docs/_inflight/` placement) quote the files it names. Invent no explanation: a red you cannot place is `UNPLACED`. `WHICH CHECKS NEED A FORMED CARD` is one extra column (`yes / no, file:line`): the seven setups are live only if step 2 says the deploy landed.
6. THE S2 CLOSE VERDICT — ONE rule, applied, not judged: `S2 CLOSES` only if ALL hold — (a) step 3 found a 2026-09-23 replay run that ended DONE; (b) step 4 ran; (c) `real or UNPLACED reds` = 0 (a red placed `expected` by a NAMED ruling — 09-20 R32 for K3's frozen HOLD — is not real; `fix not live` is NOT an allowed placement for a close: it means the fix did not land, so the night is not green). Otherwise `S2 DOES NOT CLOSE`, with the first failing clause named. The live day is banked (R46); this rule reads the green NIGHT only. The desk and he decide what follows (L37).
REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s2-smoke-look-2026-09-23.md` — §0 Headline ≤5 lines → the deploy row (step 2) → the replay row (step 3) → the smoke output (step 4) → the table (step 5) → `## S2 CLOSE VERDICT` (step 6, the three clauses each with its evidence) → `## ESCALATE` (every UNPLACED or real red first) → `## DIGEST FOR THE DESK` ≤15 lines. While you work the last line is `(run in progress)`.
STOP LINE (last non-blank line): `S2 SMOKE LOOK DONE · deploy: <sha|none> · replay ran: <yes HH:MM DONE|yes HH:MM FAILED|no> · checks: <n> green / <n> red · reds expected tonight: <n> · real or UNPLACED reds: <n> · smoke green for S2: <yes|no> · S2: <CLOSES|DOES NOT CLOSE> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
