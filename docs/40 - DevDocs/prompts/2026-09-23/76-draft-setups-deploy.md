MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `setups-deploy-draft-0923`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/76-draft-setups-deploy.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control setups-deploy-draft-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `67`'s byte for byte) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps.

# THE 09-24 DEPLOY OF THE SETUPS BRANCH (`setups/seven-0921`) + ITS HOUSE READ

LAW STEP: setups branch BUILT + CHECKED — `2026-09-24/01` BUILT `c9a11e14` → `02` round 1 (HOLD 1) → `03` fix r2 `df7817a6` (with-DB SKIPPED by his ruling, `cto-2026-09-23.md` R104) → `04` round 2 `SETUPS FIX R2 CHECK DONE · … defects that HOLD: 0` (R106). The deploy runs 09-24 on his `DONE TRADING` (R83), gated on a `DONE TRADING <time>` row in `cto-2026-09-24.md`.

INDEX CARD: `LAWS.md` in full (L59) — L28 (the Assumed Defaults note write), L35, L41, L42, L43, L51, L54, L55, L61, L62, L63, L65, L66, L68, L71, L73; `topics/cto-desk.md` last five lines (R81 GATE EARLY, R95–R98). THE DELTA SOURCES: `reports/second-chance-fix-draft-2026-09-23.md` (the deploy delta rows 1–20) and `reports/setups-fix-r2-draft-2026-09-23.md` `## FOR THE DEPLOY` 1–7 (it amends that delta). PRECEDENTS to copy the shape and strings from: `prompts/2026-09-23/68-smoke-only-deploy.md` (the one that landed, `deploy-2026-09-23`, report `deploy-2026-09-23-r5.md`) and `66-stacked-deploy-r4.md` (the stacked shape with the setups branch). HIS RULINGS carried: 09-22 R82 (A-19/A-20 ASSUMED), 09-22 R119 (three Assumed-Defaults rows), 09-23 R83 (DONE TRADING gate), **09-23 R107 "A" (below)**.

HIS R107 "A" (`cto-2026-09-23.md`): STEP-6 writes ALL THREE R119 rows, `dist.k.vwap` included. The post-STEP-6 live-note run is EXPECTED RED on vwap-continuation's `assert forms` — recorded as `R119 live-note: RED (expected, R107) — <assertion>`, NEVER a rollback trigger; every other assertion of that run must pass (any OTHER red = ESCALATE and the normal rollback rule). The deploy report names it as a KNOWN RED that every later build's GATE EARLY and every later deploy's L68 live-note leg will show until the 09-25 vwap fix round lands.

THE WITH-DB READ (R104's open item): `03` could not read its with-DB suite output — its `Monitor` wait was denied `[Credential Leakage]`. Find, from the precedent deploy reports (`grep -n "with-DB\|DB suite" reports/deploy-2026-09-2*.md`), EXACTLY how the landed deploys ran and read their with-DB suite (command, background or not, how the summary was read) and use THAT shape verbatim — never `Monitor`. If no precedent read exists, say so as ESCALATE 1 and give the safest read shape (run in the foreground with `-q`, summary from the tool result).

WRITE (whole files): (1) `prompts/2026-09-24/05-setups-deploy.md` — the deploy, the L68 gate on the tree that ships (offline, with-DB, live-note BEFORE STEP-6), residents down before the merge inside the 20:00–21:00 pause (L66) — OR, if his `DONE TRADING` row names an earlier time, the precedent's daytime-deploy clauses (radar restart window, L43 per-case override recorded) — then STEP-6's three rows through the note path, the post-STEP-6 live-note run per R107, restarts per `cobalt jobs restarts`, rollback shape named (L54). `R__` launch-row placeholder for the desk. NEW RULE STRINGS: prefer ZERO (copy `68`'s / `66`'s approved strings); list any new one with the reason. (2) `prompts/2026-09-24/06-review-setups-deploy.md` — the house read of `05` per HIS R95: **Opus 5.5 + Grok** (Sol METER until Sat 09-26 06:47; NO Gemini, NO Astra); copy `prompts/2026-09-23/69-review-smoke-only-deploy.md`'s shape minus `Bash(agy *)`; its grok date gate needs a committed row of HIS naming `06-review-setups-deploy.md` if it launches on 09-24 — carry that as ASK DESK.

REPORT (LAST): `docs/40 - DevDocs/reports/setups-deploy-draft-2026-09-23.md` — §0 ≤5 lines → the delta table (every row of both sources: CARRIED / CHANGED-by-R107 / DROPPED with reason) → `## WITH-DB READ` → `## FOR DEJAN` (every string or window he must approve, ONE list) → `## ESCALATE` → last line `SETUPS DEPLOY DRAFTED · prompts: 2 · new rule strings: <n> · window: <…> · restarts: <…> · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `SETUPS DEPLOY DRAFTED` or `FAILED`.
