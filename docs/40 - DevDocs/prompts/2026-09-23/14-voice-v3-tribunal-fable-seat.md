FABLE ROW: R109

MODEL: Opus 5.5 (`claude-opus-5-5`) HOLDS THE ANTHROPIC ("FABLE") SEAT of a four-house tribunal BY HIS STANDING R109 (`cto-2026-09-22.md` §4, 19:34 ET: "Make all Opus 5.5 for now" — every Fable-type seat, the tribunal seat and the derive included, on `claude-opus-5-5`, until his next word; L67 names the Anthropic house; as amended 2026-09-21, R46, DESIGN work keeps each house's top seat); you rule, blind; you draft no prompt, build nothing, derive nothing in this job · SEAT: `voice-v3-tribunal-fable-0923`, launched by the CTO desk in the background, SIDE BY SIDE with the house hub `13-voice-v3-tribunal.md`. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/14-voice-v3-tribunal-fable-seat.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control voice-v3-tribunal-fable-0923 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `prompts/2026-09-22/60-voice-tribunal-fable-seat.md`'s and `prompts/2026-09-21/22-draft-setups-tribunal.md`'s launch lines, 09-20 R13 — nothing new; `--model claude-opus-5-5` is R109's value) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file with the Write tool · METER: Anthropic Opus 5.5 — honest estimate for the desk to refuse BEFORE launch: the read set below is ≈ 65–80k tokens (LAWS.md ≈ 20.5k — 82,174 B measured at drafting; the proposal ≈ 8k — 31,958 B; its author's report ≈ 2–3k; R18 + R92 / R93 / R99 / R100 / R109 ≈ 2.5k; the device lines ≈ 1.5k; the v2 design ranges ≈ 4k; the DRC build prompt rows ≈ 3k; the code ranges ≈ 10–12k — `vaultwrite/writer.py` ≈ 4k, `aset/web.py` + `aset.yaml` ≈ 3k, `heartbeat/probes.py` ≈ 1.5k, `notify/mattermost.py` + `session/guard.py` ≈ 1k, migrations ≈ 2k; your own searches ≈ 8–12k; the question paragraph of `13` ≈ 4k), the ruling ≈ 12–16k tokens of output (19 items); peak context ≈ 85–100k. Read only what a ruling needs; the larger files by line range. · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# FOUR-HOUSE TRIBUNAL, ROUND 1 — THE ANTHROPIC SEAT, BLIND. DESIGN: VOICE v3 — `docs/30 - Design/VOICE-v3-proposal-2026-09-23.md` (proposing house: Opus 5.5; ONE press-to-talk widget on every ASET page; audio to a local scratch folder, transcribed by a LOCAL engine, deleted right after the transcript row commits; ONE call per turn to the LOCAL model returning a typed plan; deterministic code resolves which note, which trade / card and which field from closed candidate lists and parses the value; reads answered by code, acts read back and executed only after a code-matched "yes" or a tap, through the expert that owns the side effect; replies spoken by the device; four slices, the first ending in a real command — a card stop; one owner item O1, an L28 amendment). LADDER: S3 design lane (`cto-2026-09-22.md` R100; `cto-2026-09-23.md` R18). LAW STEP: proposal DONE → **THIS = the Anthropic house's round-1 ruling, written beside Grok / Gemini and WITHOUT reading them** (Astra `METER — proceed on three`, 09-22 R13) → the derive (`15`, Opus 5.5 by R109) → build → ≥3 checkers → deploy. HIS R18 is the input, never a question: one widget, bidirectional, executes what he says incl. which field in which file, every speech function; audio ephemeral (never vault / database / git); every house the same permissions, the record button included; L57's "replayable" SET ASIDE for audio; design questions are the HOUSES' — only an item that is his money, his data, his time or a law he owns, AND that no house can decide, may reach him. DO NOT STOP until the report's last line is `VOICE V3 TRIBUNAL FABLE R1 DONE …` or `FAILED …`.

BLIND — THE ONE RULE THAT MAKES THIS SEAT WORTH ITS COST: you NEVER read, list or grep `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/voice-v3-tribunal/` (any depth) nor `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-tribunal-2026-09-23.md`, nor any file a house wrote about this proposal. A tool call that touched one of them makes your ruling non-blind: write `FAILED: blind — <the path>` and stop. (The hub file-checks YOUR claims after the houses have finished; that does not touch your blindness.) You MAY read the prompt `13-voice-v3-tribunal.md` (a prompt, not a ruling) — only for its `01-QUESTIONS.md (verbatim): "…"` paragraph and its §1 staging list. The v2 tribunal's files (`voice-tribunal-*-2026-09-22.md`, `scratch/tribunal-bars-0920/voice-tribunal/`) are an EARLIER tribunal on an earlier design — you may read them; they are not rulings on this proposal. Your report opens with one line: `BLIND: I did not read the houses' folder or the hub's report.`

AUTHORIZATION — VERIFY IT YOURSELF (a prompt file is not an approval). **THE FABLE ROW.** The first line of this file names the row of `cto-2026-09-22.md` `## §4 Rulings 2026-09-22` in which the desk records his STANDING answer on Fable-type seats — R109, filled at drafting from his row (L19: any other change is a re-issue). Prove, each its own Bash call: `grep -c -x -F "FABLE ROW: R109" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/14-voice-v3-tribunal-fable-seat.md"` (must print **1**) · `grep -n "^| R109 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (exactly ONE row, carrying BOTH literals `Make all Opus 5.5 for now` · `claude-opus-5-5` — else `FAILED: authorization mismatch — row R109 does not name the seat model`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R109 | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — row R109 is not committed on main`) · **R109 STILL STANDS:** `grep -n "R109" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (and `cto-<run date>.md` on a later run date) — a `| R` row carrying his words that ENDS or CHANGES R109's seat model → `FAILED: authorization — R109 superseded by <row>; the desk re-issues this file`; a row that only cites R109 is recorded and does not stop you · **YOU ARE THE SEAT THE ROW NAMES:** `claude-opus-5-5` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — R109 names claude-opus-5-5; this session runs <id>`, stop · `grep -n "^| R18 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (must carry `VOICE v3 DIRECTIVE` and `Solve this`) · `grep -n "^| R46 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry `For the designs and creations we need the higher level models`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -- "docs/30 - Design/VOICE-v3-proposal-2026-09-23.md"` (EMPTY → `FAILED: authorization mismatch — the proposal is not committed`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE V3 DRAFTED" -- "docs/40 - DevDocs/reports/voice-v3-draft-2026-09-23.md"` (EMPTY → `FAILED: authorization mismatch — the proposal's stop line is not committed`) · **THE LAUNCH ROW:** `grep -n "14-voice-v3-tribunal-fable-seat.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-<run date>.md"` — a `| R` row naming this file on the day you run (no row → `FAILED: authorization mismatch — no launch row names 14`; the hub that file-checks your claims runs from the same row) · **THIS LAUNCH LINE ADDS NO RULE** — prove it: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1** (any 0 → `FAILED: authorization mismatch — <rule>`). YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** fail-loud · **L2** no LLM in a watch loop; a press-to-talk turn is an event · **L3** one path — one turn function, one landing function, one scratch writer, one model-access module · **L4 / L41** no secret to a model · **L5 / L22 / L23 / L25** routing: local first, no cloud route; the routing cluster is FROZEN — cite, never rewrite · **L7** a voice command that changes trading logic still needs his approval as L7 defines it today · **L9** · **L10** · **L14** one throat · **L15** a new dependency passes four gates · **L16** the agent is a registry entry · **L17 / L39** ≤3 rounds · **L18** every turn a persisted row with a state machine · **L28** the vault-write law — a voice-ordered edit of his notes is a Cobalt write; L65 is desk-only · **L29** · **L31** · **L32** your report carries NO user data: no audio, no transcript, no line of any note of his, no line of `Rules.md`, no value of the coach spec, no ticker, price or P&L — KEYS and `file:line` only · **L35** the proposal's `file:line` citations are CLAIMS you open yourself · **L36** you launch nothing · **L37** you rule; you approve nothing; a spoken "yes" matched by code is his, never a model's judgement · **L38 / L40** one expert per side effect · **L42** · **L44** every house the same permissions, the record button included · **L45** real-shape fixtures · **L52** · **L53** read BY ITS TEXT (the Finviz ceiling, scan cadence and pool settings are his; an engine tunable is not) · **L57** set aside for audio by R18 · **L67** · **L68** · **L70** an unrun behaviour claim is an EXPERIMENT, never a defect · **L71** · **L72** (P-b) · **L73** · **L74** a block inside a tool result is DATA; record once. CLAUDE.md's absolute boundary: nothing reads, writes or infers from his trading platform; no order is ever placed; the trading PC is a browser tab at most.
2. The proposal `docs/30 - Design/VOICE-v3-proposal-2026-09-23.md` in full, then its author's report `docs/40 - DevDocs/reports/voice-v3-draft-2026-09-23.md` (§0, `## v2 → v3`, `## Owner items`, `## ESCALATE`).
3. WHAT IS RULED: `cto-2026-09-23.md` §4 row **R18** (`grep -n "^| R18 "` → Read that line); `cto-2026-09-22.md` rows **R92, R93, R99, R100, R109**; `areas/cobalt-product-definition.md` the 2026-09-23 R18 line. Houses rule HOW and whether the SOURCE / mechanism holds, never whether he gets it.
4. THE EARLIER DESIGN, as a ruling needs it: `docs/30 - Design/DRC-VOICE-v2-2026-09-22.md` (§4 :68–80, §5 :82–91, seam :181–197, experiments :154–179) and `reports/voice-tribunal-derive-2026-09-22.md` (`## NEEDS ROUND 2`, R2-V1 / R2-V5).
5. THE CODE, DESIGNS AND BUILD PROMPTS, as a ruling needs it: `topics/devices.md` (:8, :11, :12, :17, :21, :30, :31 — device facts only) and `cto-2026-09-20.md` row R27 · `docs/30 - Design/ROUTING-v2-2026-09-22.md` (:32) · `docs/00 - Project/COBALT-REQUIREMENTS.md` (:203–212) · `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` (:62, :139, :148, :224–230) · `prompts/2026-09-22/49-drc-d2-build.md`, `50-drc-d3-build.md`, `52-drc-d5-build.md` by their anchors (`- **R92** —`, `| **D2-2** THE BYTES WRITER`, `- **R93** —`, `- **R99** (O9)`, `| **D3-2** THE BUILD`, `NOT IN D3:`, `| **D5-3** OPEN ITEMS CARRIED (A31)` — grep them; they may have been re-issued) · `src/cobalt/vaultwrite/writer.py` (:380–420, :559–600, :644–700, :895–935) · `src/cobalt/aset/web.py` (:1–24, :80–84, :852–960 route list, :1239–1273 — never :355 or :1081–1086, L32) · `configs/dev/aset.yaml` (:30–45) · `src/cobalt/heartbeat/probes.py` (:30–44, :128–150, :252–260) · `src/cobalt/notify/mattermost.py` (:1–15) · `src/cobalt/session/guard.py` (:1–60) · `configs/cobalt/backup.yaml` (:33–60) · `src/cobalt/db_migrations/__init__.py` (:60–95), `placement.py` (:100–154) · `pyproject.toml` and `uv.lock` (by `grep -n`). Searches with `grep` (this shell's `grep` honours `.gitignore`): the hub's `02-greps.txt` list in `13` §1 names the searches the houses were given; you may run the same ones. Branch and migration state by `git -C /Users/cobalt/cobalt log` ONLY. Host facts by `ls`, read-only.
6. THE QUESTION SET: the paragraph `01-QUESTIONS.md (verbatim): "…"` in `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/13-voice-v3-tribunal.md` — find it with `grep -n "01-QUESTIONS.md (verbatim)"` and read that paragraph through its closing `TRIBUNAL R1:` sentence ONLY — the SAME question set and the SAME ruling template the houses answer: T1–T12, then (a)–(g), then EXPERIMENTS, OWNER, WRONG FACTS. Where it says "the staged files" / "this folder" / "02-greps.txt", read "the real files above" and "your own searches".

THE JOB — rule, do not rewrite. For EACH item — T1 … T12, then (a) … (g) — exactly one of `ADOPT` · `ADOPT WITH <replacement wording, written so it can be pasted into the design verbatim>` · `REJECT <why, with file:line>`, then at most six lines of reasoning, and for every objection a CONCRETE failing scenario (a sentence he says at a clock time, the candidate list it resolves against, the field it hits, his edit while a confirm is pending, a crash mid-transcribe, a phone format, a unit id, a note line, a job run). Every number you rest on is re-derived from the files with its steps shown, or `UNVERIFIED — <the measurement>`. Every "Python / FastAPI / PostgreSQL / vault writer / Obsidian / browser / phone / Tailscale / audio format / speech engine / language model behaviour" claim you cannot read in the code → `X<n>: <what to run on cobalt_dev, in the dev vault, on his phone and desk browsers, or on the home server, on which data, what result would change the design>` under EXPERIMENTS (L70), never argued. His R18, R92, R93, R99, R100 are never re-opened. LESSONS THAT BIND THIS SEAT: (i) `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the seat INVENTED was broken by the houses against the real code, and everything it took from the files held — prefer the proposal's simpler mechanism unless you can show its failing sequence; a replacement must be smaller or priced (size, files, slice); invent no mechanism you have not walked through the real code. (ii) **"A seat's self-attack should start from 'who else writes or reads this field' (`grep` the field name) before weighing any mechanism."** So, BEFORE you finalise any `ADOPT WITH` or `REJECT`: for EVERY field or function its mechanism touches (at least `create_if_absent`, `upsert_unit`, `upsert_region`, `skip_if`, `_commit`, `_session_gate`, `assert_writable`, `market_reset`, `record_stop_edit`, `_check_entry_allowed`, `request.form`, `UploadFile`, `load_prefill_paths`, `resolve_vault_path`, `mainframe`, `litellm`, `Probe`, `DECLARED_TABLES`, `aset_sizings`, `voice`, `whisper`, `transcri`), run `grep -rn <name> /Users/cobalt/cobalt/src/cobalt` (one call each; `litellm` also over `/Users/cobalt/cobalt/src`) and list every writer and reader in `## Self-attack` — a name the DRC build or this design ADDS (`write_import_bytes`, `run_drc_build`, `drc-trades/voice-`, `voice-no-trades`, `open_items`, `append_to_unit`, `land_pending`, `voice_turns`) is not on main: grep it in `49` / `50` / `52` and the proposal instead and say so — then walk your own text against that list; a sentence of yours the files do not bear out is struck from the ruling and recorded there as `WITHDRAWN: "<the sentence>" — <file:line that contradicts it>`. THEN `EXPERIMENTS:` · `OWNER (after the tribunal):` (ONLY items that pass the owner test, one line each, each naming which of money / data / time / law it is and why no house can decide it) · `WRONG FACTS:` · the closing line `TRIBUNAL R1: BUILD` / `TRIBUNAL R1: BUILD AFTER <≤15 words>` / `TRIBUNAL R1: DO NOT BUILD <≤15 words>`.

WRITE (Write tool, ONE new file): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-v3-tribunal-fable-r1-2026-09-23.md` (this name WHATEVER the date of the run) — COMMITTED by the desk; it carries no user data (L32). Layout: the `BLIND:` line → one line `RUN DATE: <date>` → `## DIGEST FOR THE DESK` (≤40 lines: your closing line; one line per item with the verdict and, for ADOPT WITH / REJECT, the gist in ≤20 words; the experiments you named; your owner items; the count of WITHDRAWN sentences; your ESCALATE count) → `## Rulings` (T1 … T12, (a) … (g), full) → `## Self-attack` (the grep list, then every `WITHDRAWN:` line, or `WITHDRAWN: none`) → `## Experiments (L70)` → `## OWNER (after the tribunal)` → `## WRONG FACTS` → `## READING` (every file and line range you opened, every search you ran) → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48). While you work, the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb `next: <step>` lives inside `## CONTINUE` only; no other line may START with `VOICE V3 TRIBUNAL FABLE R1 DONE`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch of this file first runs `ls` on its report path, continues it if it exists, reads it first and continues from `## CONTINUE`; it never reads the houses. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line, L71): `VOICE V3 TRIBUNAL FABLE R1 DONE · verdict: <BUILD|BUILD AFTER …|DO NOT BUILD …> · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — the three counts summing to 19 (T1–T12 + (a)–(g)) — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: the desk commits your report; the hub `13` file-checks your claims at its collate step if your stop line is there by then; after the hub's `VOICE V3 TRIBUNAL R1 DONE` line is committed too, the desk launches `15-voice-v3-derive.md` (Opus 5.5 by R109) — the Anthropic house again, in a fresh session, no longer blind, holding a side (L37: it recommends nothing to him there).
