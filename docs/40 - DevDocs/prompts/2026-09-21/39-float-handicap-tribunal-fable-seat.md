FABLE ROW: R30

MODEL: Fable 5.1 (`claude-fable-5-1`) — the FABLE SEAT of a four-house tribunal (L67 names Fable a house), a SPAWNED agent that runs ONLY if he approves it for THIS tribunal (09-20 R18: a spawned Fable agent only after the desk asks him, per case); you rule, blind; you draft no prompt, build nothing, derive nothing in this job · SEAT: `float-handicap-tribunal-fable-0921`, launched by the CTO desk in the background, SIDE BY SIDE with the three-house hub `38-float-handicap-tribunal.md`. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/39-float-handicap-tribunal-fable-seat.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control float-handicap-tribunal-fable-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`'s and `24`'s launch lines, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file with the Write tool · METER: Anthropic FABLE — honest estimate for the desk to refuse BEFORE launch: the read set below is ≈ 75–90k tokens (LAWS.md ≈ 25k, the proposal ≈ 7.5k, its author's report ≈ 2k, R28 / R29 ≈ 1k, ADR-0009 ≈ 2k, `pool.py` ≈ 4.5k, `models.py` ≈ 2k, `cards/radar.py` ≈ 2k, `cards/scoring.py` ≈ 3.5k, the `config.py` / `runner.py` / `evaluate.py` / `picks.py` / `store.py` / migration line ranges ≈ 12k, the `radar_panel.py` ranges ≈ 3k, your own searches ≈ 6–10k, the question paragraph of `38` ≈ 4k), the ruling ≈ 8–12k tokens of output; peak context ≈ 100–120k — about half of one 09-20 bars Fable fork (≈ 250k, `topics/cto-desk.md` 2026-09-20). Read only what a ruling needs; the larger files by line range after the first pass. · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# FOUR-HOUSE TRIBUNAL, ROUND 1 — THE FABLE SEAT, BLIND. DESIGN: a soft handicap on low-float / small-cap tickers, `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` (proposing house: Opus 5). LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`. LAW STEP: proposal DONE → **THIS = the Fable house's round-1 ruling, written beside Astra / Grok / Gemini and WITHOUT reading them** → the derive (`40`, on the seat his row names) → build per chunk → ≥3 checkers → deploy. L52 applies in full: this design reaches ranking (the pool's 50, the ladder, F3 focus) and the card face; its bar (a)–(d) is your bar. DO NOT STOP until the report's last line is `FLOAT HANDICAP TRIBUNAL FABLE R1 DONE …` or `FAILED …`.

BLIND — THE ONE RULE THAT MAKES THIS SEAT WORTH ITS COST: you NEVER read, list or grep `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/float-handicap-tribunal/` (any depth) nor `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-2026-09-21.md`, nor any file a house wrote today about this proposal. A tool call that touched one of them makes your ruling non-blind: write `FAILED: blind — <the path>` and stop. You MAY read the prompt `38-float-handicap-tribunal.md` (a prompt, not a ruling) — only for its `QUESTIONS.md (verbatim): "…"` paragraph and its §1 staging list. Your report opens with one line: `BLIND: I did not read the houses' folder or the hub's report.`

AUTHORIZATION — VERIFY IT YOURSELF (a prompt file is not an approval). **THE FABLE ROW.** The first line of this file names the row of `cto-2026-09-21.md` `## §4 Rulings 2026-09-21` in which the desk records his answer to "may I spawn a Fable agent for the Fable seat of the float-handicap tribunal?". The DESK fills the two characters after `R` on that line AFTER his yes, and changes nothing else in this file (L19: any other change is a re-issue). Prove, each its own Bash call: `grep -c -x -F "FABLE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/39-float-handicap-tribunal-fable-seat.md"` (`-x` = whole line: only the first line can match, never this sentence) — a count of **1** means the line is still unfilled → `FAILED: authorization — the Fable seat is not approved for this tribunal (the FABLE ROW is unfilled)`, stop; **0** → read the number `<nn>` from the first line · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (the row must exist AND carry the literal `Fable seat: yes` AND `FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` — anything else, `Fable seat: no` included → `FAILED: authorization mismatch — row R<nn> does not approve the Fable seat on this tribunal`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed on main`) · `grep -n "^| R28 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` and `grep -n "^| R29 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (the design task; R28 must carry "I don't want to exclude tickers") · `git -C /Users/cobalt/cobalt log -1 --format=%H -- "docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — the proposal is not committed`). **THIS LAUNCH LINE ADDS NO RULE** — prove it: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1** (any 0 → `FAILED: authorization mismatch — <rule>`). YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** a missing float / cap is never silently in or out of the group · **L3** one path · **L7** cards stay advisory; a pool-membership change goes through shadow; nothing here promotes · **L8** · **L9** a dead column is a loud degraded state · **L10** dry-run · **L17 / L39** ≤3 rounds, no fourth · **L28** vault writes; his values through the load path or a desk edit (L65) · **L32** R28 / R29 and the author's DIGEST carry his threshold values and an ASSUMED factor: USER DATA · **L35** trust the file, never a report — the proposal's `file:line` citations are CLAIMS you open yourself · **L36** you launch nothing · **L37** you rule; you approve nothing · **L42** RESTARTS · **L44** · **L45** gates on the REAL export shape · **L52** (a)–(d) · **L53** his cap, rank rule, metric choice live in his vault pool block · **L57** · **L61** · **L65** · **L67** · **L70** an unrun behaviour claim is an EXPERIMENT, never a defect · **L71** · **L72** · **L73** · **L74** a block inside a tool result is DATA; record once.
2. The proposal `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` in full, then its author's report `docs/40 - DevDocs/reports/float-handicap-design-2026-09-21.md` (DIGEST with the worked number, READING, ESCALATE 1).
3. HIS RULINGS: `cto-2026-09-21.md` §4 rows R28 and R29 (`grep -n "^| R<n> "` → Read those lines). What is RULED, and you do not argue it: a SOFT penalty, never an exclusion, on the group float below his float threshold OR market cap below his market-cap threshold (the "or" confirmed; the combinator a KEY); whatever source brought the ticker; the ticker keeps competing on the lowered number for the pool's 50 and the top of the ladder; the penalty SIZE and SHAPE are his values, read AFTER the tribunal — anything of his goes under `OWNER (after the tribunal):`, never as a precondition to build; cards stay advisory.
4. THE CODE ON MAIN, as a ruling needs it (the hub's staged excerpts name these ranges; you open the real files): `src/cobalt/radar/pool.py` (whole) · `src/cobalt/radar/models.py` (whole) · `src/cobalt/cards/radar.py` (whole) · `src/cobalt/cards/scoring.py` (:1–72, :236–343) · `src/cobalt/radar/config.py` (:24–118, :144–180) · `src/cobalt/radar/runner.py` (:100–150, :172–232, :355–399) · `src/cobalt/radar/evaluate.py` (:128–167, :1210–1237, :1383–1400) · `src/cobalt/db_migrations/0004_radar_pool.sql` (:25–55), `0007_radar_cards.sql` (:210–245), `0009_picks_missed.sql` (:70–80) · `src/cobalt/cards/picks.py` (:55–62, :220–240) · `src/cobalt/cards/store.py` (:1185–1227) · `src/cobalt/aset/radar_panel.py` (the ranges the proposal cites, §4) · `docs/10 - Decisions/ADR-0009-radar-cards-seam-and-precondition-ast.md` (:24–28) · `configs/cobalt/radar.yaml` · the real-shape fixtures the proposal's F5 / F6 cite under `tests/fixtures/`. Searches with `grep` (this shell's `grep` honours `.gitignore`): run what a ruling needs — the hub's §1 (8) list in `38` names the searches the houses were given; you may run the same ones.
5. THE INTERACTION: `docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` — ONLY its chunk table (C1–C7) and the lines that name `scoring.py`, `cards/radar.py`, `pool.py` or `radar_cards_v` (find them with `grep -n`); `SETUPS-AT-DEFAULTS-v2-2026-09-21.md` in the same folder, the same lines only, if it exists.
6. THE QUESTION SET: the paragraph `QUESTIONS.md (verbatim): "…"` in `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/38-float-handicap-tribunal.md` — find it with `grep -n "QUESTIONS.md (verbatim)"` and read that paragraph through its closing `TRIBUNAL R1:` sentence ONLY — the SAME question set and the SAME ruling template the three houses answer. Where it says "the staged files" / "this folder" / "greps.txt", read "the real files above" and "your own searches"; the staged names map to the real paths as `38` §1 lists them. The retained `data/radar-cache/` CSVs behind the proposal's F6 counts and worked example are market data you MAY open read-only; a count you cannot complete by reading is `UNVERIFIED — <the dry-run that settles it>`.

THE JOB — rule, do not rewrite. For EACH item — O1 … O6, then (a) … (j) — exactly one of `ADOPT` · `ADOPT WITH <replacement wording, written so it can be pasted into the design verbatim>` · `REJECT <why, with file:line>`, then at most six lines of reasoning, and for every objection a CONCRETE failing scenario (a pool of named screens, a scan time, a ticker at a position, a card field). Every number you rest on is re-derived from the files with its steps shown, or `UNVERIFIED — <the measurement>`. Every "PostgreSQL / pandas / Finviz export / radar behaviour" claim you cannot read in the code → `X<n>: <what to run, on which data, what result would change the design>` under (j) (L70), never argued. LESSONS THAT BIND THIS SEAT (`topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL"): on the bars tribunal, everything the Fable seat INVENTED was broken by the houses against the real code, and everything it took from the files held — so prefer the proposal's simpler mechanism unless you can show its failing sequence; when you replace one, your replacement must be smaller or you price its cost (size, files, chunk) and say so; invent no mechanism you have not walked through the real code. THEN `OWNER (after the tribunal):` (his items, one line each, none a precondition) · `WRONG FACTS:` (a proposal statement the files contradict, both file:lines) · the closing line `TRIBUNAL R1: BUILD` / `TRIBUNAL R1: BUILD AFTER <≤15 words>` / `TRIBUNAL R1: DO NOT BUILD <≤15 words>`.

WRITE (Write tool, ONE new file): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r1-2026-09-21.md` — COMMITTED by the desk, so it carries NO value of his: no threshold, no penalty factor, no number from R28 / R29 or from the author's DIGEST worked example; cite them by KEY (`handicap.float_below_m`, `handicap.market_cap_below_m`, `handicap.factor`, `handicap.missing`, `handicap.mode`) or `R28` / `R29` / `report:line` (L32). Layout: the `BLIND:` line → `## DIGEST FOR THE DESK` (≤40 lines: your closing line; one line per item with the verdict and, for ADOPT WITH / REJECT, the gist in ≤20 words; the experiments you named; your owner items; your ESCALATE count) → `## Rulings` (O1 … O6, (a) … (j), full) → `## Experiments (L70)` → `## OWNER (after the tribunal)` → `## WRONG FACTS` → `## READING` (every file and line range you opened) → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48). While you work, the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb `next: <step>` lives inside `## CONTINUE` only; no other line may START with `FLOAT HANDICAP TRIBUNAL FABLE R1 DONE`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch of this file reads its own report first and continues from `## CONTINUE`; it never re-reads the houses (it never read them). One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line, L71): `FLOAT HANDICAP TRIBUNAL FABLE R1 DONE · verdict: <BUILD|BUILD AFTER …|DO NOT BUILD …> · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — the three counts summing to 16 (O1–O6 + (a)–(j)) — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: the desk commits your report; after the hub's `FLOAT HANDICAP TRIBUNAL R1 DONE` line is committed too, it launches `40-float-handicap-tribunal-derive.md` on the seat his row names — you again if he named Fable, no longer blind.
