MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `ops-classifier-fix-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/76-draft-ops-classifier-fix.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control ops-classifier-fix-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `50` / `67`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) TWO prompts: the FIX ROUND for branch `ops/2026-09-21` and its ROUND-2 check. OFF-LADDER (`cto-2026-09-20.md` R41 / R42; `cto-2026-09-21.md` R43, R53). LAW STEP (L67): built → checked round 1 by FOUR checkers (`reports/ops-6a-check-2026-09-22.md`: `houses that checked: 4 of 4 · defects that HOLD: 0`, but **Sol and Opus 5 both say `ready for its deploy: NO`** on question 6a) → **THIS = drafting the fix the two dissenting checkers ask for, and the round-2 check of that fix.** DESK CALL (L37 is the desk's here, 09-19 lesson "adopt the dissent when it is the conservative side and costs nothing"): the branch does NOT join a stacked deploy while `cobalt jobs restarts` exits 1 on it. DO NOT STOP until the report's last line is `OPS CLASSIFIER FIX PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): the ONE gap of the check's ESCALATE 1 — `src/cobalt/jobs/restarts.py` has a one-shot rule for `configs/cobalt/watchlists.yaml` (`:243-244` on the branch) and NONE for `configs/cobalt/backup.yaml`, so the classifier falls to `:251-252`, raises at `:271-272`, exits 1 and derives ALL SIX residents for a file only the one-shot `com.cobalt.backup` reads. L42: restarts are derived by rule, never by judgement; unclassified → ESCALATE, never dropped — so the fix is A RULE WITH A TEST, never a deploy-prompt sentence saying "none". The fix proves WHO READS `backup.yaml` from the code (every reader, `file:line`; if a RESIDENT reads it, the rule names that resident instead — say so and stop the one-shot reading). NOT part of this: the circular-restore question (check ESCALATE 3 → 6b, his restic rotation first), restic's forget grouping (ESCALATE 4 → a 6b question), the `cli.md` seam with `bars/chunk-1a-0920` (ESCALATE 2 → settled by that branch's own gate), any other classifier gap you happen to see (list it under ESCALATE, fix nothing else).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** · **L10** · **L19** · **L29** `jobs/restarts.py` is a read-only classifier, no write path → say so with `file:line`, Sonnet-eligible if so · **L35** · **L36** · **L42** · **L45** the test runs the REAL classifier on the REAL diff shape (a change to `configs/cobalt/backup.yaml`) · **L46 / L54 / L60** the existing worktree, RECOVERY RULE · **L62 / L63** · **L67** as amended 09-21 (code check = Grok · Gemini · Sol · Opus 5) · **L70** · **L71** · **L74**.
2. `reports/ops-6a-check-2026-09-22.md` — `## Per question` question 6, `## Checked against the branch`, ESCALATE 1–4 · the branch's `docs/40 - DevDocs/reports/ops-2026-09-21.md` (Read under `/Users/cobalt/cobalt-wt/ops-0921`; its ESCALATE 1) · on the branch: `src/cobalt/jobs/restarts.py` whole, its tests (`grep -l restarts tests/cobalt/*.py`), the watchlists one-shot rule's own test as the pattern, `src/cobalt/backup/*.py` + `ops/com.cobalt.backup.plist` + `ops/run_backup.sh` for the readers of `backup.yaml`, the DevDoc `docs/40 - DevDocs/cobalt/jobs/restarts.md`.
3. THE SHAPES TO COPY, byte for byte wherever the same sentence serves: the FIX prompt `prompts/2026-09-21/02-bars-chunk-2-fix-r3.md` (a fix round in an EXISTING worktree, offline, its 16 + 3 strings and triplet = 09-20 R25; FIX rows, RED quoted first, full suite, `git diff --stat` limited to named paths, `uv run cobalt jobs restarts main..HEAD` VERBATIM at CLOSE — here it must now EXIT 0 and name the one-shot only) · the CHECK prompt `prompts/2026-09-21/69-ops-6a-check.md` (its launch line byte for byte; round 2 reads ONLY the fix: a pre-computed diff of the fix commits + round 1's verdict table, 09-19 lesson).

WRITE (Write tool, NEW files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `77-ops-classifier-fix.md` — hub `ops-classifier-fix-0922`, seat per L29 (expected Sonnet 5), OFFLINE, EXISTING worktree `/Users/cobalt/cobalt-wt/ops-0921`, branch `ops/2026-09-21`; FIX rows: Z1 the classifier rule for `configs/cobalt/backup.yaml` (test FIRST, RED quoted: today's exit 1 / UNCLASSIFIED; then green: classified to its proven reader, exit 0) · Z2 the DevDoc sentence · nothing else; `git diff --stat <old tip>..HEAD` = `restarts.py`, its test file, its DevDoc, the report ONLY. AUTHORIZATION greps committed rows R43, R53 and a launch row `R__` the desk fills (in `cto-2026-09-22.md` or `cto-2026-09-21.md`). Stop line: `OPS CLASSIFIER FIX BUILT <tip> | on <old tip> | offline <p>/<f> (baseline 2209/0) | backup.yaml classified: <reader> | jobs restarts main..HEAD: exit 0, RESTARTS: <list> | other paths untouched: empty diff | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `78-ops-6a-check-r2.md` — Sonnet 5 hub `ops-6a-check-r2-0922`; `69`'s launch line and seats byte for byte; date gate as `69` (R39, through 2026-09-22); PREFLIGHT: `77`'s report ends `OPS CLASSIFIER FIX BUILT …`; stagger vs `68`, `61`, `73`. QUESTIONS (round 2 reads the fix only): (1) is the rule right — does ONLY the named reader read `backup.yaml` (`file:line` for any other); (2) is the test a real proof on the real diff shape or a tautology; (3) does the rule weaken the classifier for any OTHER path (`NONE` or `file:line`); (4) round 1's two NO lines — does the fix close each, in that checker's own words; (5) anything else changed. Stop line: `OPS 6A CHECK R2 DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · round-1 NO lines closed: <n> of 2 · defects that HOLD: <n> · ready for the stacked deploy: <yes|no> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/ops-classifier-fix-draft-2026-09-21.md`: §0 Headline ≤5 lines → the L29 seat finding → the readers of `backup.yaml` (`file:line`) → RULE PROOF (`grep -c -F -e` per string: `77` vs `02`, `78` vs `69`, each **1**) → **`NEW approvals:`** (`none` expected) → `READING:` → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤20 lines**. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `OPS CLASSIFIER FIX PROMPTS DRAFTED · prompts: 2 · builder seat: <Sonnet 5|Opus 5> · readers of backup.yaml: <n> · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
