MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `setups-tribunal-r1b-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/26-draft-setups-tribunal-r1b.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-tribunal-r1b-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `22`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-1 COMPLETION of the setups tribunal: the TWO houses that did not rule. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R15 / R17 / R18`. LAW STEP (L67): tribunal round 1, still open. DO NOT STOP until the report's last line is `SETUPS TRIBUNAL R1B PROMPTS DRAFTED …` or `FAILED …`.

WHAT HAPPENED (read, do not re-derive): `23-setups-tribunal.md` ran 09:28–10:1x ET; its report `reports/setups-tribunal-2026-09-21.md` ends `SETUPS TRIBUNAL R1 DONE · grok: … BUILD AFTER … · gemini: HARNESS · astra: METER · houses that ruled: 1 of 3 …`. Astra probed UP at 09:29, then hit its usage limit mid-ruling after 135,471 tokens of file reads ("try again at 11:12 AM"), no ruling written. Gemini exited 0 with no ruling: a tool asked for the `command` permission that headless `agy` auto-denies (it tried a shell command despite the file-viewer-only sentence). Grok ruled; the blind Fable seat ruled (`reports/setups-tribunal-fable-r1-2026-09-21.md`, committed `edaea58`). The packet of 44 files is STAGED and byte-checked at `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-tribunal/r1/` — it is REUSED, never restaged. Astra was REQUIRED by `23` (drafter's L47 finding, `reports/setups-tribunal-draft-2026-09-21.md`): that stands — the derive does not run on Grok + Fable alone.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L19** a changed prompt is re-issued whole · **L33** headless agy / Grok answer from reads alone · **L35** · **L36** · **L37** · **L47** the meter is a precondition: PROBE before launch, and size the ask to the meter · **L62 / L63** · **L67** ≤3 rounds per house — a house that produced NO ruling has not used a round; say so · **L70** · **L71** · **L74**.
2. `prompts/2026-09-21/23-setups-tribunal.md` in full (the shape, the 14 + 3 strings, §2's per-house spellings, QUESTIONS, the collate + file-check sections, the L32 redaction rule) · `25-setups-tribunal-derive.md` in full · `reports/setups-tribunal-2026-09-21.md` §0, `## CONTINUE`, `## ESCALATE` · `reports/setups-tribunal-draft-2026-09-21.md` (PACKET table, ESCALATE 3).
3. For the two failures, the precedents: every earlier `gemini: HARNESS` and `astra: METER` in `docs/40 - DevDocs/reports/` of 09-19 → 09-21 (`grep -l`), and what made the next attempt succeed (spelling of the file-viewer-only sentence, packet size, the order of files named first). State what you found with `file:line`; invent no new CLI flag and no new rule string — `--dangerously-skip-permissions` and any settings edit are FORBIDDEN (L55, another seat's config).

WRITE (Write tool, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `27-setups-tribunal-r1b.md` (new) — Sonnet 5 hub `setups-tribunal-r1b-0921`, cwd `~/cobalt-wt/agy-trial`, launch line = `23`'s with only the file name and the remote-control name changed (the 14 + 3 strings byte for byte). GATES: DATE GATE (2026-09-22 or later → FAILED); TIME GATE `date` before 11:15 ET → `FAILED: window — astra's meter returns 11:12` (the desk launches on a timer), at or after 23:20 ET → FAILED; packet present (`ls` of `r1/`, the 44 names; a missing file → FAILED, never restage). Codex PROBE = a gate for ASTRA ONLY: METER → astra is skipped and named, GEMINI STILL RUNS (the two are independent now). ASTRA'S METER IS SMALL: it spent 135k tokens on reads alone — give astra a READING ORDER that reaches a ruling inside one window (QUESTIONS.md → the proposal's two parts → the design report → the proof → `evaluate.py` parts → the rest only where a ruling turns on it), and tell it to WRITE ITS RULING INCREMENTALLY is NOT possible under `-s read-only` — so instead tell it to print the ruling item by item as it goes, so a second meter stop leaves a partial ruling the hub keeps as `astra-ruling.partial.md` (labelled PARTIAL, items ruled counted). GEMINI: the spelling that worked in the precedents you found, plus one sentence naming the exact failure ("a shell or `command` tool call ends this run with no output — open files with the file viewer only"). One attempt per house, no retry; 20-minute timeout unchanged. COLLATE + FILE-CHECK exactly as `23` for the two houses' claims; L32 redaction as `23`. Report `reports/setups-tribunal-r1b-2026-09-21.md`; stop line `SETUPS TRIBUNAL R1B DONE · gemini: … · astra: … · houses that ruled: <n> of 2 · round 1 total: <n> of 3 + Fable · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- `25-setups-tribunal-derive.md` — RE-ISSUED WHOLE (L19; you MAY overwrite this one file — Read it first): identical but (a) it refuses unless `reports/setups-tribunal-r1b-2026-09-21.md` ALSO ends on its DONE line and is committed; (b) it reads BOTH hub reports and the raw rulings of all houses that ruled (`grok-ruling.md` in `r1/`, the r1b rulings where `27` puts them, a PARTIAL astra ruling counted only for the items it ruled); (c) the astra condition becomes: astra ruled in full OR in part, else `FAILED: astra has not ruled — the desk brings him the two-house question`; (d) its report states per item how many of the four seats ruled it. Nothing else changes; its launch line and strings stay byte-identical.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-r1b-draft-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤25 lines → the HARNESS / METER precedents found → RULE PROOF (`grep -c -F -e` = 1 for `27`'s whole rule span against `23`; `25`'s against `22`) → `NEW strings:` (expected none) → a word-level summary of what changed in `25` (section by section, so the desk does not re-read it) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `SETUPS TRIBUNAL R1B PROMPTS DRAFTED · prompts: 2 · gemini spelling: <≤8 words> · astra reading order: <n> files first · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
