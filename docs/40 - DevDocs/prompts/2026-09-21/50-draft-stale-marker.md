MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `stale-marker-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/50-draft-stale-marker.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control stale-marker-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `28` / `33` / `49`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) FOUR prompts for ONE front-end change: the STALE STAMP on `/radar` — a small red `STALE` badge on the pool row AND on the card of every ticker whose bars poll is failing. LADDER: `S2 · Radar panel (§5 host)` + `F8 Card at precondition`. LAW STEP (L67): the design decision IS his ruling (`cto-2026-09-21.md` §4 R36, 16:06 ET, "A"); the proposal `docs/30 - Design/STALE-MARKER-PROPOSAL-2026-09-21.md` established that BOTH marks are a second RENDERING of the stored `radar_pool.poll_failures` state — no field, route, column, score or order changes (desk lane call, `cto-2026-09-21.md` §13, 16:12 bullet) → **THIS = drafting its build, the ≥3-house check of that build, its EVENING deploy (TUESDAY 2026-09-22) and the houses' read of that deploy prompt** → the desk reads your DIGEST, asks him ONE approval list, launches. DO NOT STOP until the report's last line is `STALE MARKER PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): the proposal's two chunks, built as ONE branch in ONE run (chunk 1 the derived `bars_stale_tickers` set + the row badge by the ticker — the pool row has no `last` cell; chunk 2 the card badges + `mirrorStale()` beside `mirrorDegraded()`), wording / 9 px mono / red `.badge` geometry ASSUMED and said so in the DevDoc sentence. The badge follows the POLLER's clock only (the state the `BARS POLL FAILED` banner already names) — the three-clocks question (proposal ESCALATE 2) is a CHECKER question, never a build decision. NOT part of this: the proposal's ESCALATE 1 (an open card can score on a stale `last`) — a separate scoring item with its own lane; the build must leave `src/cobalt/cards`, `src/cobalt/radar`, scoring and `evaluate.py` with an EMPTY diff. The banner and tonight's red degraded line stay byte-identical. A refresh must never move the ladder (his 06:32 complaint).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** · **L9** · **L19** · **L28** the panel READS · **L29** front-end only with no write path = Sonnet-eligible (the proposal's finding; re-state it with `file:line`) · **L35** · **L36** · **L42** RESTARTS derived, never judged · **L43 / L66** evening deploy, residents down BEFORE the merge, inside the 20:00–21:00 ET pause · **L45** tests on the real rendered page · **L46 / L54 / L60** own worktree off main, RECOVERY RULE · **L52** nothing here changes scoring or ranking; what a card SHOWS changes by exactly one badge, ruled R36 · **L62 / L63** · **L67** · **L68** say which other unmerged branches exist and which, if any, could land the same evening (the desk decides) · **L71** · **L73** · **L74**.
2. `docs/30 - Design/STALE-MARKER-PROPOSAL-2026-09-21.md` in full + `reports/stale-marker-design-2026-09-21.md` (fact table, §7 checker questions).
3. THE TREE THE BUILD WILL START FROM: tonight at ≈20:02 ET `16-degraded-line-deploy.md` lands `s2/degraded-line-0921` (rebased tip `7b0a6a3`, code tip `88602cc`) on main — read `src/cobalt/aset/radar_panel.py`, `tests/cobalt/test_radar_panel.py`, `tests/cobalt/test_radar_panel_cards.py` and `docs/40 - DevDocs/cobalt/aset/radar_panel.md` AS THEY ARE ON THAT BRANCH (`git -C /Users/cobalt/cobalt show s2/degraded-line-0921:<path>`), not only on main: `mirrorDegraded()` exists there only. The build prompt's PREFLIGHT refuses unless main carries tag `deploy-2026-09-21b` (= `16` landed) — if `16` FAILED tonight the desk re-issues; say so under `## CONTINUE`.
4. THE SHAPES TO COPY, byte for byte wherever the same sentence serves — today's prompts for the SAME file, all of which ran clean: `prompts/2026-09-21/14-degraded-line-build.md` (build; its 16 + 3 strings and triplet = his 09-20 R25 approval) · `15-degraded-line-check.md` (14 + 3 strings of 09-20 R13 / R23) · `16-degraded-line-deploy.md` AS RE-ISSUED AND FOLDED (the newest clean evening shape: nine folds of R19, the identity gates reading tips from a stop line) · `35-review-degraded-line-deploy-r2.md` (one-round read by grok + gemini). `Bash(grok *)` / `Bash(agy *)` END TODAY 23:59 ET (09-20 R23): `52` and `54` will run on 2026-09-22, so their AUTHORIZATION greps `cto-2026-09-22.md` (or `cto-2026-09-21.md`) for a row of HIS extending those two strings to the launch date — write that gate and put the two strings on your `NEW approvals:` list in plain words.
5. TUESDAY'S CLOCK — establish from `ops/*.plist`: 20:00–21:00 ET pause · `com.cobalt.archiver` 20:30 · `com.cobalt.replay` 21:10 · `com.cobalt.backup` 21:40. Expected: launch 20:02, hard clock 20:25.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `51-stale-marker-build.md` — hub `stale-marker-build-0921`, `14`'s shape: OFFLINE, branch `s2/stale-marker-0921` off main, worktree `/Users/cobalt/cobalt-wt/stale-marker` (created by the desk as for `14`; say the two commands), seat per L29. TEST FIRST on the REAL rendered page, desktop AND `phone_frame=True`, RED quoted: (a) a view with `poll_only` failures → the row badge on exactly the named tickers' `current` rows and the card badges on exactly their cards (strip, ARMED `last`, LEVELS `last`), tooltip `<reason> since HH:MM ET`; (b) a healthy view → NO badge element, rows and cards byte-identical to main; (c) a lifecycle-polled card outside the pool is marked; (d) refreshed fragment stale → fresh → stale keeps rows current, `mirrorStale()` toggles the strip badge with no re-sort / re-fetch / open-close change; (e) the API JSON is unchanged (`exclude=True`); (f) banner + degraded line byte-identical. CLOSE as `14` (full suite, `git diff --stat main` limited to the four named paths, EMPTY diffs for `src/cobalt/cards`, `src/cobalt/radar`, `src/cobalt/aset/store.py`, `src/cobalt/aset/web.py`, `configs`; `uv run cobalt jobs restarts main..HEAD` VERBATIM). AUTHORIZATION greps `cto-2026-09-21.md` for row R36 (committed) and a launch row `R__` the desk fills. STOP LINE: `STALE MARKER BUILT <tip> | on <main tip> | offline <p>/<f> (<s> skipped; baseline <p>/<f>) | row badge on stale tickers only: proven | card badges on stale tickers only: proven | absent when healthy, output unchanged: proven | stays current, ladder does not move: proven | API JSON unchanged: proven | card/scoring paths untouched: empty diff | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `52-stale-marker-check.md` — Sonnet 5 hub `stale-marker-check-0922`, `15`'s shape and strings byte for byte, its own staging folder under `scratch/tribunal-bars-0920/`, three houses TARGETED (astra with ` < /dev/null` only if `reports/setups-tribunal-r2-2026-09-21.md` records that experiment as working; the hub runs `date` at every completion notice and stops a house past 20 min with its own task-stop tool). QUESTIONS: badge on exactly the failing tickers, both frames, rows AND cards · one state (L3) or a second computation (`file:line`) · can a badge go stale or move the ladder · anything in scoring, ranking, order, payload, route, refresh interval changed (`NONE` or `file:line`) · any assertion weaker · the proposal's §7 four (departed rows · terminal rows · JS ticker key · `exclude=True` vs a JSON key) · the three clocks: does following the poller's clock only ever leave a card the evaluator holds `input_stale` with no badge, and is that loud elsewhere · closing line as `15`. Stop line `STALE MARKER CHECK DONE · grok: … · gemini: … · astra: … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>` — or `FAILED …`.
- `53-stale-marker-deploy.md` — Sonnet 5 hub `stale-marker-deploy-0922`, cwd `/Users/cobalt/cobalt`, `16` AS RE-ISSUED byte for byte wherever the sentence serves: PREFLIGHT date `2026-09-22`, window 20:00–20:15, hard clock 20:25, branch-identity gate, AUTHORIZATION (R36 · build stop line · check `defects that HOLD: 0` committed · review `launch blockers that HOLD: 0` or folds named in launch row `R__` · his approval row for the three branch-named strings), gates BEFORE anything goes down, residents down per L66, `merge --ff-only`, residents up, smoke (the three curls with `\?`, the badge's marker on production's tree, new pids, heartbeat, log check tied to this start), ONE rollback path, every failure branch ends with the residents' bootstrap attempt. Tag `deploy-2026-09-22`. Report `reports/deploy-2026-09-22.md`. Stop line `STALE MARKER DEPLOY DONE <tip> · tag deploy-2026-09-22 · residents down <n> s · /radar 200 · rollback: <not used|used> · ESCALATE: <n>` — or `FAILED …`.
- `54-review-stale-marker-deploy.md` — `35`'s shape and strings byte for byte, ONE round, gate: refuses after 19:30 ET on 2026-09-22. Stop line `STALE MARKER DEPLOY REVIEW DONE · … · houses that read it: <n> of <n> · launch blockers that HOLD: <n> · folds proposed: <n> · ESCALATE: <n>`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-marker-draft-2026-09-21.md`: §0 Headline ≤5 lines → the L29 seat finding → worktree creation → per prompt: file, size, seat → RULE PROOF (`grep -c -F -e` = 1 per string against the approved source line, per prompt) → **`NEW approvals:`** — every string not byte-identical to an approved one (the three branch-named merge / rebase strings) AND the two date-expired house strings, as ONE list the desk can show him verbatim → Tuesday's window and the clock evidence → other unmerged branches (L68) → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤40 lines** (what each prompt does step by step; the desk reads this, not the four prompts — `cto-2026-09-21.md` R7). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `STALE MARKER PROMPTS DRAFTED · prompts: 4 · builder seat: <Sonnet 5|Opus 5> · new rule strings: <n> · house strings to extend: 2 · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
