MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `page-bars-hotfix-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/28-draft-page-bars-hotfix.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control page-bars-hotfix-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `22` / `26`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. **SPEED MATTERS: production's `/radar` page is dead while he trades. Copy shapes, do not re-invent; stop as soon as the four prompts and the report are right.**

# DRAFT (do NOT launch) FOUR prompts for ONE HOTFIX: a per-ticker BARS POLL FAILURE must no longer fail the whole `/radar` page. LADDER: `S2 · Radar panel (§5 host)`. LAW STEP (L67): the design decision IS his ruling (`cto-2026-09-21.md` §4 R21, 10:43 ET, "A") → **THIS = drafting its build, the house check of that build, its DAYTIME deploy and the houses' read of that deploy prompt**. DO NOT STOP until the report's last line is `PAGE BARS HOTFIX PROMPTS DRAFTED …` or `FAILED …`.

THE INCIDENT (desk forensics, `cto-2026-09-21.md` §11 "10:40 ET" bullet — read it): the radar RESIDENT is up and scanning. `src/cobalt/aset/radar_panel.py:477` `if pool.failed_stage: raise RadarPanelError(…)` → `src/cobalt/aset/web.py:866` renders the whole page `FAILED: radar bars: poll failures: 3`. `failed_stage='bars'` is set whenever ANY pool member carries a poll failure (`src/cobalt/radar/store.py:267-273`, `runner.py:375-376`); production's `radar_pool.poll_failures` at 10:42 = three thin tickers, each `{"ticker", "reason": "stale", "since"}`. 3 of 50 members blank the ladder and the pool view; it recurs whenever a thin name sits in the pool.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): (1) when `failed_stage == 'bars'`, the page and its refresh fragments RENDER; the bars failure is shown as a DEGRADED state (L9: red banner, never a dead page, never silent) that NAMES the tickers and their reason, through the pool view's EXISTING banner mechanism (L3 one path — establish from the file how the pool view banners DEGRADED / STALE / REFRESH FAILED today and add the bars state THERE, so that tonight's red degraded line, which mirrors the pool banners, carries it with no further change — say with `file:line` whether that holds against branch `s2/degraded-line-0921`). (2) EVERY OTHER `failed_stage` still fails the page exactly as today (L1) — assert it. (3) A member whose bars are stale must not look healthy: say from the files what a stale member's pool row and any card of it show today once the page renders, and whether the evaluator already refuses a stale member (expected: `evaluate.py` names it not evaluable) — if a card of a stale ticker could render as fresh, that is an ESCALATE, not your fix. (4) NO change in `src/cobalt/radar/`, `store.py`, `runner.py`, the poller, the DB, scoring, cards, settings: the fix is inside `src/cobalt/aset/` (expected `radar_panel.py` only) + its tests + the DevDoc sentence. RESTARTS expected `com.cobalt.aset` ONLY; anything else → the deploy stops. (5) TONIGHT'S `16-degraded-line-deploy.md` (branch `s2/degraded-line-0921`, same file) rebases on main at ≈20:02 ET: keep the hotfix diff SMALL and AWAY from the lines that branch touches (`git -C /Users/cobalt/cobalt show` / `log -p` of `c183ed6`), and state in your report whether the rebase will conflict (`CONFLICT EXPECTED: yes|no — <lines>`); a `yes` is an ESCALATE for the desk with the smallest remedy.

HIS OVERRIDE (R21; L73 as amended 09-21): L43, L66's pause window and NN#16's "deploy outside market hours" are SET ASIDE for this deploy only. Bound kept: aset only (down ≈7 s), radar resident untouched and proven untouched, build → check → second-house read all kept. His "A" approves the deploy's command list = this morning's `11-panel-order-deploy.md` list with the branch name changed.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1** · **L3** · **L9** · **L19** · **L29** front-end, no write path = Sonnet-eligible (say if any touched line sits on a write path → Opus) · **L35** · **L36** · **L42** · **L45** tests on the REAL rendered page with a real-shape `poll_failures` row · **L46 / L54 / L60** own worktree, RECOVERY RULE · **L52** nothing reaches scoring or what a card shows · **L62 / L63** · **L67** EMERGENCY clause: production's page is out → fewer checkers allowed; ASTRA IS ON METER until 11:12 ET and its next window is RESERVED for the setups tribunal (`27`) → the check and the deploy read TARGET grok + gemini, astra NOT launched (say so in each prompt; two houses = the floor holds) · **L68** only this branch lands · **L71** · **L73** · **L74**.
2. THE SHAPES TO COPY byte for byte wherever the same sentence serves — this morning's chain for the SAME file, which ran clean in 56 minutes: `prompts/2026-09-21/08-panel-order-build.md` (build; 16 + 3 strings, 09-20 R25) · `09-panel-order-check.md` (check; 14 + 3 strings — Gemini's sentence as IMPROVED in `27-setups-tribunal-r1b.md`: file viewer only, the failure named, the pre-computed greps file opened second) · `11-panel-order-deploy.md` AS FOLDED (the DAYTIME aset-only deploy: `\?` curl, relaunch rule, branch-identity gate) PLUS the folds of `reports/degraded-line-deploy-review-2026-09-21.md` that apply to an aset-only deploy (P12 docs-commits wording, exact branch match, log check tied to this start) · `12-review-panel-deploy.md`.
3. The code on MAIN: `src/cobalt/aset/radar_panel.py` (`:440-500` and the banner rendering, `render_pool`, `render_failed_page`, `PANEL_JS` refresh), `src/cobalt/aset/web.py:850-900` (page + fragment routes share `build_radar_panel`), tests `tests/cobalt/test_radar_panel.py`, DevDoc `docs/40 - DevDocs/cobalt/aset/radar_panel.md`.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `29-page-bars-hotfix-build.md` — hub `page-bars-hotfix-build-0921`, `08`'s shape and strings, OFFLINE, branch `s2/page-bars-hotfix-0921` off main, worktree `/Users/cobalt/cobalt-wt/page-bars-hotfix` (created by the desk, as for `08`). TEST FIRST, RED quoted, desktop AND phone frame: (a) a pool row with `failed_stage='bars'` + three `poll_failures` → the page renders ladder and pool, and the banner names each ticker and reason; (b) the refresh fragments render too; (c) any OTHER failed stage → still the FAILED page; (d) no failure → output byte-identical to main. CLOSE as `08`. AUTHORIZATION greps `cto-2026-09-21.md` for R21 (`-S"HOTFIX NOW"`) and a launch row `R__` the desk fills. STOP LINE: `PAGE BARS HOTFIX BUILT <tip> | on <main tip> | offline <p>/<f> (baseline <p>/<f>) | bars failure renders degraded, tickers named: proven | other stages still FAIL: proven | healthy output unchanged: proven | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED …`.
- `30-page-bars-hotfix-check.md` — Sonnet hub `page-bars-hotfix-check-0921`, `09`'s shape and strings, folder `scratch/tribunal-bars-0920/page-bars-hotfix-check/`, DATE GATE, grok + gemini ONLY (no Codex probe, astra named `NOT ASKED — reserved, L67 emergency`). QUESTIONS: does any other failed stage still fail the page · can a stale ticker look healthy anywhere on the rendered page · is anything outside `src/cobalt/aset/` + tests + DevDoc touched · did any assertion get weaker · does it reach scoring or a card (L52) · will tonight's degraded-line rebase conflict. Stop line `PAGE BARS HOTFIX CHECK DONE · grok: … · gemini: … · houses that checked: <n> of 2 · defects that HOLD: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>`.
- `31-page-bars-hotfix-deploy.md` — Sonnet hub `page-bars-hotfix-deploy-0921`, cwd `/Users/cobalt/cobalt`, `11`'s structure: DATE `2026-09-21`; NO evening window (R21) but a HARD CLOCK: not at or after 15:45 ET (never into the close) → `FAILED: window`; AUTHORIZATION (R21 · build stop line · check `defects that HOLD: 0` committed · review blockers 0 or folds named in launch row `R__`); gates BEFORE anything goes down (rebased code identical, RESTARTS = `com.cobalt.aset` ONLY verbatim, branch carries exactly its paths, EMPTY diff for `src/cobalt/radar src/cobalt/cards configs ops src/cobalt/db_migrations`); aset down → `merge --ff-only` → aset up; radar pid UNCHANGED before/after (record both); smoke (three curls with `\?` = 200 — and since production's pool may STILL carry bars failures, that is the live proof: `/radar` 200 AND `grep -c "radar panel FAILED"` in `aset.err` not growing after the start); ONE rollback path; tag `deploy-2026-09-21h`; report `reports/deploy-2026-09-21h.md`. Stop line `PAGE BARS HOTFIX DEPLOY DONE <tip> · tag deploy-2026-09-21h · aset down <n> s · /radar 200 · radar pid unchanged: <yes|no> · rollback: <not used|used> · ESCALATE: <n>`.
- `32-review-page-bars-hotfix-deploy.md` — `12`'s shape and strings, ONE round, grok + gemini only, folder `scratch/tribunal-bars-0920/page-bars-hotfix-deploy-review/`. Stop line `PAGE BARS HOTFIX DEPLOY REVIEW DONE · … · houses that read it: <n> of 2 · launch blockers that HOLD: <n> · folds proposed: <n> · ESCALATE: <n>`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/page-bars-hotfix-draft-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines → WHERE THE BANNERS LIVE (`file:line`) and the narrowest change → the stale-member finding of RULED (3) → `CONFLICT EXPECTED:` line → RULE PROOF (`grep -c -F -e` whole span = 1 against `08` / `09` / `11` / `12`) → **`NEW strings:`** (expected: the branch-named `merge --ff-only` / `rebase main` / `rebase --abort` only) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `PAGE BARS HOTFIX PROMPTS DRAFTED · prompts: 4 · builder seat: <Sonnet 5|Opus 5> · change: <≤10 words> · conflict with tonight: <yes|no> · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
