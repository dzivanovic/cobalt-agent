MODEL: Opus 5 (`claude-opus-5`) — the PROPOSING house of a four-house tribunal (L67: "ONE house designs and proposes") · SEAT: `float-handicap-design-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/36-propose-float-handicap.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control float-handicap-design-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `21` / `28` / `33`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the TWO files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PROPOSE THE DESIGN: a SOFT HANDICAP (a score penalty, never an exclusion) on LOW-FLOAT / SMALL-CAP tickers in the trade radar. LADDER: OFF-LADDER — ruled `docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 R28 (touches `S2 · F2` pool rank, `F3` focus rank, `F10` score). LAW STEP (L67 / L52): **THIS = the proposal one house writes; four houses then rule on it and derive the final version; nothing is built from your file.** He asked three things, and your proposal answers exactly them: WHAT IS NEEDED · WHERE IT FITS · HOW. DO NOT STOP until the report's last line is `FLOAT HANDICAP PROPOSED …` or `FAILED …`.

HIS RULING (read row R28 verbatim in the desk report — it is spoken text and he corrected the numbers inside the message; the LAST values stand): he almost never trades low floats but wants them to STAY on the radar and sometimes takes them; "they don't fall under the same guidelines". THE GROUP: **float below 50 million OR market cap below 500 million** (the desk's reading of his final numbers; carried as HIS values, by KEY, tunable — if your reading of R28 differs, say so under `## READING`, do not substitute). It applies whatever brought the ticker in — any Finviz screen (up-gapper, down-gapper, low-float morning, day scan) or any list. THE EFFECT: "a lower score comparing to what it would have now" — the ticker keeps competing on the LOWERED score: still in the pool's 50 if it earns it, still top five / top ten if it earns it — "if it goes inside there with the handicap, that's when I want to look at it". NOT ruled: the penalty's SIZE and SHAPE (you propose ONE conservative default, marked ASSUMED, plus the alternatives; L53 makes the number his). NOT wanted: exclusion (the Lists note's `exclude` block already exists and stays the only hard tool).

INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1** missing float / market cap for a ticker is never silently "not in the group" and never silently "in the group" — say what it is and what he sees · **L3** ONE penalty mechanism, not one per surface, unless you prove two are needed · **L7** cards stay advisory; nothing here promotes · **L8** · **L9** a dead source for float / market cap = a degraded state, never a silent un-handicap · **L10** schema + dry-run ("what would this penalty have done to yesterday's pool and ladder") · **L28** any write of his values goes through the existing load path (`cobalt settings load`) or is a desk edit of his note on his ruling (L65) — you propose NO edit yourself · **L32** his thresholds and penalty are USER data: never in the committed design file as values, only by key · **L45** tests against the REAL export shape · **L52 (a)–(d)** answer each explicitly — (b) exactly ONE ranking authority reaches the card: say which number the penalty enters and prove nothing else re-ranks around it · **L53** cap, rank rule, metric choice live in the vault pool block, never settled in committed config · **L57** the penalised score is replayable: raw score, the group test's inputs, the penalty and the result are all stored or derivable from stored inputs · **L67 · L68 · L70 · L72 · L73 · L74**.

THE FACT BASE — establish from the files, every claim with `file:line` ("not found" beats a plausible fill): (1) THE POOL: `src/cobalt/radar/pool.py`, `models.py` (`PoolBlock`, `RankMetric`, `RankMetricName`, `PoolOverride`, `ExcludeBlock`, `ExcludedBy`), `sources.py`, `collector.py`, `config.py`, `notes.py`, `configs/cobalt/` — how the 50 are chosen today (rank metric per source, cap, stickiness / `below_cap_streak`), and what per-ticker data the pool holds at that moment; (2) THE DATA: does the Finviz export Cobalt already fetches carry float (`Shares Float`) and `Market Cap` for EVERY pool candidate, from screens AND from static lists (`config.py` `required_headers`, `metric_headers`, `columns`; the real-shape fixtures under `tests/`), in which units, and where is it stored (`radar_pool` / membership rows / a cache) — is it there at pool-rank time, at card-scoring time, in the replay; (3) THE SCORE: `src/cobalt/cards/` (scoring, curves, `proposed_key`), `src/cobalt/radar/evaluate.py`, the F3 focus rank (conviction × proximity) and where it is computed — what "score" and "rank" mean at each surface he named: pool membership (top 50), ladder order (top five / ten), the card's dots and proposed key; whether float already feeds a scored dot today (F10 lists float among screener fields) — a handicap on top of an existing float dot must not double-count silently: say what happens; (4) WHAT HE SEES: `src/cobalt/aset/radar_panel.py` — pool row, card face, owner badges, the `DEFAULT_UNRULED` / assumed marking; (5) WHERE HIS VALUES LIVE TODAY: the vault pool block in his Radar Lists / Screens notes (through `notes.py`) and `"user".trader_settings` (`card.*` keys, `cobalt settings load`) — which of the two is the lawful home for the group thresholds and the penalty, and why; (6) the nightly replay / miss line (`replay.py`) — a penalised ticker that fell out of the 50 BECAUSE of the penalty must be tellable from one that fell out on merit (`excluded_by` today has `config_cap`, `not_equity`, `screen_inactive`, `manual`).

WHAT THE DESIGN MUST SETTLE — each with the mechanism, the files it touches, what reaches the card, and its tests:
1. **WHERE THE PENALTY ENTERS** — the fewest places that give him what he said: competing for the pool's 50 on a lowered rank AND for the ladder's top on a lowered score. Candidates to weigh, not to assume: (a) the pool rank only; (b) the focus rank / card score only; (c) one "handicap factor" computed once per ticker per scan and applied at both. Pool rank is per-source metric ranks today, not one score — say honestly what "lower score" can mean there and price each option.
2. **THE SHAPE** — multiplier, rank offset, or point deduction; flat for the whole group or graded by how small the float / cap is. ONE conservative default (ASSUMED, by key), chosen so that a genuinely top name still surfaces — show with a worked example on REAL stored numbers' SHAPE (no values of his in the committed file) what it takes for a handicapped ticker to stay top ten.
3. **THE GROUP TEST** — float below X OR market cap below Y, both his keys; units; missing data (item L1 above); ETFs / not-equity already dropped before it.
4. **VISIBLE** — he wants to LOOK when a handicapped name makes it in: a marker on the pool row and the card ("handicapped: float / cap", raw vs penalised score), using the existing badge mechanism; nothing hidden.
5. **TUNING** — how he changes thresholds and penalty later (normal load path, sha256, L28 / L61), and how a value stops being ASSUMED.
6. **REPLAY + MISS LINE** — stored inputs so the replay reproduces the penalised ranks (L57); whether `excluded_by` needs a way to say "cap, after handicap" (additive, never a rename).
7. **SHADOW / PROMOTION (L7)** — cards are advisory today; say whether a change to pool membership needs a before/after dry-run on stored days (L10) before it goes live, and what that report shows him.
8. **INTERACTION with today's other design** — `docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` (chunks C1–C7, in tribunal now): does any chunk touch the same scoring lines; which lands first; no mechanism here may need tearing up for swing / options later (anti-rigidity).
9. **CHUNKS** — each independently buildable offline in its own worktree, checkable by ≥3 houses, deployable on its own evening (L43 / L66): size S/M/L, builder seat by L29 (a scoring / ranking path → say if Opus), what restarts (L42), migration yes/no, what it needs from `cobalt_dev`. State plainly the evenings from "tribunal ruled" to "he sees a handicapped name marked on `/radar`".

WRITE (Write tool, new files only):
- `/Users/cobalt/cobalt/docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` — the proposal, COMMITTABLE: none of his values (thresholds appear as KEYS; R28 is cited, not quoted with numbers). Sections 1–9 above, then `## L52 (a)–(d)`, `## What this does NOT do`, `## Open to the tribunal` (the ≤6 points where you are least sure, each with the alternative you rejected and why), `## Owner items` (what is his by L53, each with your ASSUMED default by key — read by him AFTER the tribunal, not a list of questions that blocks it).
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-design-2026-09-21.md` — §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines: WHAT IS NEEDED / WHERE IT FITS / HOW in plain words he could be read aloud, the data answer of FACT BASE (2) in one line, the chunk table, evenings → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
RULES: prefer the simpler mechanism and price it; every claim carries `file:line`; nothing here is built or launched by you. One command per Bash call, bare shape, no pipe, no redirect.
STOP LINE (last non-blank line): `FLOAT HANDICAP PROPOSED · enters at: <≤6 words> · shape: <≤6 words> · float + cap data present for every candidate: <yes|no|partly> · chunks: <n> · evenings to visible: <n> · migration: <yes|no> · open to the tribunal: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
