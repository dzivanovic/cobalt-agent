MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `float-handicap-tribunal-reissue-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/41-reissue-handicap-tribunal-q17.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control float-handicap-tribunal-reissue-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `22` / `33` / `36` / `37`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. **SMALL JOB: one question added, counts made consistent, nothing else touched.**

# RE-ISSUE (L19, whole files, in place — precedent: `25` by `26`, `16` by `33` today) `38-float-handicap-tribunal.md` and `40-float-handicap-tribunal-derive.md` with ONE MORE QUESTION in the houses' set, BEFORE `38` has ever launched. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`. LAW STEP: tribunal prompts drafted (`reports/float-handicap-tribunal-draft-2026-09-21.md`), `38` NOT yet launched (it waits behind other house work) → **THIS = the fold of a question the set lacks**, per the desk lesson of 09-20 (`topics/cto-desk.md` 2026-09-21 "LESSONS OF THE 09-20 BUILD NIGHT" item 5: a question known to be missing is added to the question set and the packet BEFORE launch — launching without it cost a round). DO NOT STOP until the report's last line is `FLOAT HANDICAP TRIBUNAL REISSUED …` or `FAILED …`.

BLINDNESS — BINDING: the blind Fable seat has already ruled (`reports/float-handicap-tribunal-fable-r1-2026-09-21.md`). **YOU DO NOT OPEN THAT FILE, and nothing you write may carry, hint at or paraphrase a position of any seat.** The question below is the desk's neutral wording; stage it as written. `39` is NOT touched (it has run).

THE QUESTION TO ADD, as item **(k)**, after (j), in the same ruling template (`ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>` against the proposal's text as it stands): "**(k) TIERS.** Pool admission is tiered before rank decides: establish from `src/cobalt/radar/pool.py` and `models.py` (`priority`, `first_from`, and any other mechanism that admits a source's names ahead of the ranked competition) every tier that exists today, with `file:line`. The proposal divides a within-source position by `h`. Say for EACH tier: does the handicap change which names are admitted there, or is it inert for that tier's admission? Then rule between the two readings — TIER-BOUND (the handicap reorders only inside a tier; his ruled `priority` / `first_from` keep their full force) and POOL-WIDE (a handicapped name competes on its lowered number against the whole candidate set, across tiers) — against R28's words ('if it still falls within the top 50 it should still be there') and L53 (his pool-block values are his; a mechanism that silently weakens one is an owner item, named). Does the proposal say which reading it implements? If it names only some of the tiers, name the ones it misses." Add whatever pre-cut code excerpt and `greps.txt` search item (k) turns on (expected: the `first_from` / `priority` lines of `pool.py` and `models.py`, ≤6 KB) to `38`'s packet list, staged the same way as the others.

WHAT CHANGES, AND ONLY THIS: in `38` — the question set gains (k); every count that says 16 becomes 17 and every id span that says `(a)–(j)` becomes `(a)–(k)` (the ruling template, astra's per-item print and PARTIAL `<k> of 16` rule, the collate table, the stop-line counts if any, the packet list + its size / token estimate, `greps.txt`'s search count); astra's READING ORDER gains the new excerpt where it belongs. In `40` — the same counts and id spans, and its fold table covers (k); its first line `DERIVE ROW: R30` STAYS as it is (the desk filled it; `grep -c -x -F "DERIVE ROW: R__"` must stay 0). NOTHING ELSE: every gate, window, stagger, AUTHORIZATION grep, launch line and rule string byte for byte. Read each old issue FIRST from `git -C /Users/cobalt/cobalt show HEAD:"<path>"` and prove at the end that the launch line (the whole first-line span from `claude --bg` to the last `--add-dir /Users/cobalt/cobalt-wt`) is unchanged by `grep -c -F -e` = the same count in old and new. Add NO header line to either file (`40`'s first line is grepped whole-line, and line-number references must not move); the re-issue is recorded in your report only.

INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59); binding here **L19 · L35 · L36 · L37 · L52 · L53 · L62 / L63 · L67 · L71 · L74**. Then `reports/float-handicap-tribunal-draft-2026-09-21.md` (DIGEST + PACKET), `prompts/2026-09-21/38-…` and `40-…` end to end, `src/cobalt/radar/pool.py` + `models.py` for the excerpt, the proposal `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` (to see what it says about tiers — for the excerpt choice only, you judge nothing).

WRITE (Write tool): `prompts/2026-09-21/38-float-handicap-tribunal.md` and `40-float-handicap-tribunal-derive.md` re-issued whole in place · your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-reissue-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤15 lines → EVERY CHANGED SENTENCE (old → new, per file) → RULE PROOF (launch-line span count old = new, both files; `grep -c -x -F "DERIVE ROW: R30"` on `40` = 1) → **`NEW strings:`** (expected: none) → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `FLOAT HANDICAP TRIBUNAL REISSUED · questions: 17 · files: 2 · sentences changed: <n> · launch lines unchanged: <proven|NOT> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
