DERIVE ROW: R57

MODEL: the DERIVE SEAT that row names — Fable 5.1 (`claude-fable-5-1`) when he approved the Fable seat (then this is the Fable seat's SECOND JOB, as on the bars, setups, float-handicap and stale-score tribunals), else the seat he named with his "no"; you derive the version the round-1 rulings produce; you build, launch and commit nothing · SEAT: `s3-exits-tribunal-derive-0922`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `73`; the Fable seat `74` ONLY if he approved it). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/75-s3-exits-tribunal-derive.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control s3-exits-tribunal-derive-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` — THE DESK replaces the value of `--model` (and ONLY that value) with the model id the row names after `derive seat:`; it must be a Claude model id, since this file is launched by `claude --bg` (a non-Anthropic derive seat cannot run this file: the desk drafts another). The allow and deny strings are the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `39`, `40`, `62`, `63` and `74`, 09-20 R13 — nothing new · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic, the derive seat's house — honest estimate: the hub report `73` ≈ 25–35k tokens, the raw rulings of the houses that ruled ≈ 36–54k (26 items each), the Fable round-1 report ≈ 12–16k (only if that seat ruled), the proposal ≈ 7k, its author's report ≈ 2k, LAWS.md ≈ 16–17k, code re-opened only where a fold turns on it ≈ 5–15k; v2 ≈ 12–16k tokens of output; peak context ≈ 120–150k — the largest derive of the week so far; read the raw rulings item by item, not all at once, if the peak nears your window · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v2 of the S3 EXITS DESIGN from the proposal and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: `S3-P1 · F11` + `S3-P2 · F22` (S3 opens 09-24). LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini, Astra via hub `73`; Fable via `74` if he approved that seat) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (empty → the tribunal closes and v2 is the FINAL's input) → build per chunk → ≥3 checkers (Sol + Opus 5 + Grok + Gemini, R46) → deploy. L1, L3, L28, L32, L53 and L57 are the bar v2 must meet, and the proposal's §9 L52 finding is re-answered for v2. DO NOT STOP until the report's last line is `S3 EXITS DERIVED v2 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The first line of this file names the row of the desk report of the day he answered — `cto-2026-09-21.md` or `cto-2026-09-22.md`, section `## §4 Rulings` — in which the desk records his answer to the Fable-seat question for THIS tribunal — yes, or no and the seat he names for the derive. The DESK fills the two characters after `R` on that line and the `--model` value, and changes nothing else in this file (L19: any other change is a re-issue). The row must carry, as literal text: `S3-EXITS-PROPOSAL-2026-09-21.md`, `Fable seat: yes` or `Fable seat: no`, and `derive seat: <model id>`. Each its own Bash call: `grep -c -x -F "DERIVE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/75-s3-exits-tribunal-derive.md"` (`-x` = whole line: only the first line can match) — **1** → `FAILED: authorization — the DERIVE ROW is unfilled; the desk has not recorded his answer on the Fable seat`, stop; **0** → read `<nn>` from the first line · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (a missing `cto-2026-09-22.md` is recorded, not fatal; exactly ONE `R<nn>` row carrying `S3-EXITS-PROPOSAL-2026-09-21.md`, with the three literals above all present — any missing, or two such rows → `FAILED: authorization mismatch — row R<nn> does not name the Fable seat's status and the derive seat for this tribunal`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "<the desk file the row is in>"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — row R<nn> names <id>; this session runs <id>`, stop. **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE.** The round-1 reports were named from their writers' `date`, so each has TWO candidate names; `ls` both, and exactly one must exist (both → `FAILED: refused — two hub reports; the desk says which stands`): the hub `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-2026-09-21.md` / `…-2026-09-22.md` → `tail -n 3 "<the one that exists>"` (the LAST NON-BLANK line must start `S3 EXITS TRIBUNAL R1 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"S3 EXITS TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/<its name>"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · ONLY when the row says `Fable seat: yes`: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-fable-r1-2026-09-21.md` / `…-2026-09-22.md` → `tail -n 3` the one that exists (must start `S3 EXITS TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Fable round 1 not done — last line: <line verbatim>`) and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"S3 EXITS TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/<its name>"` (EMPTY → `FAILED: refused — the Fable round-1 stop line is not committed on main`); when the row says `Fable seat: no`, the tribunal is the three houses and no Fable report is read · **ASTRA WAS REQUIRED** (`reports/s3-exits-tribunal-draft-2026-09-21.md`, the L47 finding; `73` fails closed without it): the hub's stop line must show `astra: TRIBUNAL R1: …` (ruled in full) or `astra: PARTIAL <k> of 26 — …` with k ≥ 1 (ruled in part) — anything else (`METER`, `HARNESS`, `HARNESS — redirect denied`, `TIMEOUT`, `NO RULING LINE`) → `FAILED: astra has not ruled — the desk relaunches 73 for astra or brings him the question`. Gemini or Grok not having ruled does NOT refuse the derive: the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L2 · L3 · L8 · L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L28 · L29 · L32** (v2 and your report are COMMITTED: no user data — a traded ticker and prices of his sit in the Charter's example DM line and two code comments; write `[TICKER]` / `[PRICE]`, never the values) **· L35 · L36 · L37** (you derive; a SPLIT between houses is carried, never settled by vote; a seat that holds a side recommends nothing to him) **· L42 · L43 / L66 · L52 · L53** (every threshold, rounding rule, preset fraction and poll interval is his) **· L57 · L67 · L68 · L70 · L71 · L72 · L73 · L74**. CLAUDE.md's absolute boundary: nothing in v2 reads, writes or infers from his trading platform.
2. THE ROUND-1 RULINGS, whole: the hub report — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Fable round-1 claims, file-checked` (the FC rows, or its one line saying the Fable report was not done at collate), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/s3-exits-tribunal/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line; `astra-ruling.md` if astra ruled in full, else `astra-ruling.partial.md` — a PARTIAL astra ruling is counted ONLY for the items its first line lists as ruled; an item it did not reach is not ruled by astra · the Fable round-1 report, ONLY if the row says `Fable seat: yes` — including its `## Self-attack` (its `WITHDRAWN:` lines).
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/S3-EXITS-PROPOSAL-2026-09-21.md` in full; its author's report `reports/s3-exits-design-2026-09-21.md` (the UNPROVEN rows, ESCALATE 1–6, DIGEST); `cto-2026-09-20.md` §4 row R38 (his stop becomes the plan until he resets it; Cobalt's structural stop stays visible; the gap is recorded on the trade); the Charter's F11 / F22 texts (`docs/00 - Project/MVP-CHARTER-v0_2.md` :135–147, :205–207, §5 :245–255) and the ladder's S3 block (`docs/00 - Project/SPRINT-LADDER-v0_1.md` :586–603).
4. Only where a fold turns on it: the real file the hub's file-check cites (code on main, migrations, configs, the Charter, the ladder, `reports/drc-sitting-packet-2026-09-21.md` :48–49; his templates by headings and frontmatter keys ONLY) — read to check, never to copy.

THE RULES OF THE DERIVE — the bars tribunal's lesson (`topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into v2 word for word, or it is not taken. You may choose BETWEEN houses' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check, or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **A Fable-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's `## Fable round-1 claims, file-checked` has its FC rows, they are your filter for the Fable seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the Fable report as not done at collate, every Fable claim is UNCHECKED: you may take a Fable wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure, and where you cannot settle it by reading it is an experiment, not a fold.
- **Prefer the simpler mechanism.** Where two houses offer different mechanisms for one item, take the smaller one when both meet the laws above, and write the other's cost and trade-off as an OWNER ITEM (after the tribunal) — unless the two disagree on whether a mechanism is CORRECT, which is `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in v2's experiment list, placed before the chunk it gates, with the result that would change the design — never an argument in v2's text, never a defect.
- **Invent nothing.** v2 adds no mechanism, number, rule, key, column, table, clock or chunk that neither the proposal nor a house's ruling contains. A gap no ruling fills stays a named gap (an experiment, an owner item, or a round-2 item) — never your filler.
- **Nothing of his before build.** Every value and choice that is his — the proposal's O1–O8 (the ⅓ / ½ rounding, the drift pct threshold P, the ATR floor K, manual notes at sizing or at FILLED, which of his note keys Cobalt may fill while blank, `trade_def` on a radar note, one tap or two before the S4 detector, the DM line in S3 or after), every threshold and poll interval, and every choice the houses name as his (the reading of Charter §5 #16 if a house calls it his, the `strategy` / `trade_def` fix in his template) — is HIS, read by him AFTER the tribunal; nothing in v2 may make a chunk's BUILD depend on one of them (tests use constructed settings, L69); a house that wrote one as a precondition to build is carried as an OWNER ITEM (after the tribunal) and named in `## ESCALATE`. Where the proposal makes a missing setting REFUSE, v2 states what the rulings decide about that refusal; it never fills a value.
- **The author's ESCALATE 2 and 3** (the hard-coded drift constant `aset/engine.py:43` vs the Charter's >20 %, and the ASSUMED ATR term with no source text) and **ESCALATE 6** (Charter §5 #16 "single fill at MVP" vs scale-out legs): v2 places each exactly as the rulings decide it — inside this design, an owner item, or its own item; a split on whether a mechanism is correct is `## NEEDS ROUND 2`, never your call. **ESCALATE 1** (radar fills record no price today) and **ESCALATE 4 / 5** (the DRC prefill's nearest-timestamp search; the `strategy` vs `trade_def` column): v2 states what the rulings decide, and a seam for F14 is named as a seam, never designed here (the DRC is designed BY HIM, R48).
- **The DM line (C5, owner item O8)**: v2 carries it exactly as the rulings leave it — in S3, after S3, or split out — with the cost each house named; the kill-phrase coupling, if a house raised it, is carried as the rulings decide it.
- **The order against the seven-setups one-build and the stale-score fix** (both on `cards/store.py`), **the migration numbers and sets** (`0012` / `0013`, `db_migrations/` vs `cards/migrations/` vs `aset/migrations/`), and **the parallel C3 ∥ C4 ∥ C5 claim**: v2 states what the rulings decide; the deploy order and the numbering at build are the desk's lane (L68), stated as a fact, never as your ruling.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT BUILD` that v2 does not follow goes into v2's `## Dissents, verbatim` word for word, with its house and item.
- **No user data in either file** (L32): the packet had none after the hub's REDACTION; if a house's text quotes a value of his anyway, it is taken with that span replaced by `[user data: <file:line>]`, and a house's proposed VALUE for one of his keys is recorded as `<key>: value proposed by <house> — scratch/tribunal-bars-0920/s3-exits-tribunal/r1/<house>-ruling.md:<line>` (a pointer, never the value) and listed under OWNER ITEMS; count every redaction.

WRITE (Write tool, TWO new files), both named from your first `date`, `<D>`:
- `/Users/cobalt/cobalt/docs/30 - Design/S3-EXITS-v2-<D>.md` — the proposal, whole, with each amended section REPLACED in place and tagged `[F-nn]` at the amended paragraph (so a reader can diff v2 against the proposal section by section); a header naming the proposal, the rulings of every seat that ruled (paths, astra's marked PARTIAL where it is, the Fable seat's status from row R<nn>) and the fold table's path; `## First-gate experiments (L70)` = the proposal's UNPROVEN rows (F25, F30, F32, F33) and its §11 with-DB and dev-vault tests as the rulings leave them, plus every experiment the houses named that the fold takes, each before the chunk it gates, each runnable on `cobalt_dev` or in the dev vault (a production read stays a hub-run read, L41); `## Dissents, verbatim`; `## L52 and the bar` — the proposal's §9 L52 finding re-answered for v2, with L1, L3, L28, L57 each stated for v2. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-derive-<D>.md` — §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` (≤40 lines: what v2 changed from the proposal, item by item in one line each; which seats ruled round 1 (in full / in part / not / not approved); how many Fable claims were hub-checked, how many UNCHECKED; what goes to round 2 and why; the owner items in one line each; the chunks, seats and RESTARTS under v2; the hours under v2 against S3's 09-24 → 10-07, a GUESS marked as one) → `## Fold table` (`F-nn · item (Q1–Q12, (a)–(n), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (house, or "proposal kept") · adopted verbatim? yes / no / not taken · why (≤25 words, citing the hub's file-check row — `Checked` row or `FC<n>` — where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT BUILD`, or two houses disagree on whether a mechanism is correct; each with both houses' positions quoted and the question round 2 must answer; EMPTY = the tribunal closes) → `## OWNER ITEMS (after the tribunal)` (deduplicated, each with who raised it — the proposal's O1–O8 first; none a precondition to build) → `## FOR DEJAN` (each owner item in HIS terms — what he would see on `/radar`, on the IN-TRADE card, in his trade note or on his phone, at which moment of the trade — as ONE A/B question per item: A = what v2 builds by default, B = the alternative a house named, each with the houses that hold it; NO recommendation from a seat that holds a side (L37) — you ruled round 1 if you are the Fable seat, so you recommend nothing; ≤4 lines per item) → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `S3 EXITS DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both candidate names of each file, reads its own report first and continues from `## CONTINUE`; v2, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line of the derive report, L71): `S3 EXITS DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits v2 as the FINAL's input, brings him `## FOR DEJAN`, and the build prompts are drafted (C1 first, per v2, ordered against the seven-setups one-build and the stale-score fix per the desk's L68 gate); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a run after 2026-09-22 23:59 ET needs his word for `grok` / `agy` — R39 ends then).
