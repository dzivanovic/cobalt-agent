MODEL: Fable 5.1 (`claude-fable-5-1`) — the FABLE SEAT's CLOSING JOB on the setups tribunal (`cto-2026-09-21.md` §4 R20: "blind ruling (+ the derive step, as on the bars tribunal)"); you derive the FINAL from v2 and the round-2 rulings; you build, launch and commit nothing · SEAT: `setups-tribunal-derive-r2-0921`, launched by the CTO desk in the background ONLY after BOTH round-2 stop lines are committed (hub `44`, the Fable seat `45`). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/46-setups-tribunal-derive-r2.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control setups-tribunal-derive-r2-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `24`, `25`, `45`, 09-20 R13 — nothing new) · SESSION: fresh (the round-2 seat's context is NOT carried; its report is on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic FABLE — honest estimate: the round-2 hub report ≈ 10–20k tokens, the raw round-2 rulings of the houses that ruled ≈ 8–20k, your round-2 report ≈ 8k, v2 ≈ 17k (read whole — the FINAL is v2 amended), the derive report ≈ 7k, LAWS.md ≈ 25k, code re-opened only where a fold turns on it ≈ 5–15k; the FINAL ≈ 17–20k tokens of output (v2 whole + the folds); peak context ≈ 120–160k. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE THE FINAL of the SETUPS DESIGN from v2 and the round-2 rulings. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R15 / R17 / R18`. LAW STEP (L67): proposal → round 1 → derive v2 (`25`) → round 2 (hub `44`: Grok, Gemini, Astra — targeted, none required, R32; the Fable seat `45`) → **THIS = the second derive; it CLOSES the tribunal** → C1's build (≥3 checkers) → deploy. L52 (a)–(d) is the bar the FINAL must meet; **R2-2 gates C1**. DO NOT STOP until the report's last line is `SETUPS FINAL DERIVED …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). Each its own Bash call: `grep -n "^| R20 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry "yes you may spawn" and "derive step") · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"yes you may spawn" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — R20 is not committed`) · **BOTH ROUND-2 STOP LINES, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-r2-2026-09-21.md"` (the LAST NON-BLANK line must start `SETUPS TRIBUNAL R2 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 2 not done — last line: <line verbatim>`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-fable-r2-2026-09-21.md"` (must start `SETUPS TRIBUNAL FABLE R2 DONE ` — else `FAILED: refused — Fable round 2 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"SETUPS TRIBUNAL R2 DONE" -- "docs/40 - DevDocs/reports/setups-tribunal-r2-2026-09-21.md"` and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"SETUPS TRIBUNAL FABLE R2 DONE" -- "docs/40 - DevDocs/reports/setups-tribunal-fable-r2-2026-09-21.md"` (either EMPTY → `FAILED: refused — <report> stop line is not committed on main`) · **R2-2 NEEDS A HOUSE:** the hub's stop line field `R2-2:` is `NO HOUSE`, or its `## Rulings table` shows no house (Grok, Gemini or Astra, in full or PARTIAL with R2-2.1 ruled) choosing `GROK'S` / `FABLE'S` / `NEITHER` on R2-2.1 → `FAILED: R2-2 has no house ruling — the desk brings it to him` (the Fable seat alone never settles the item that gates C1 — it holds one side of it). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L3 · L7 · L17 / L39** (≤3 rounds; no fourth; an item unresolved after round 2 goes to Dejan — NO round 3 is drafted unless a house said `DO NOT BUILD`) **· L28 / L65** (who writes the assumed note: answered by the desk 15:16 — the Cobalt L28 command, +S on C2 — not re-opened) **· L32** (the FINAL and your report are COMMITTED: no user data) **· L35 · L36 · L37** (you derive; a SPLIT is carried to him, never settled by count) **· L45 · L52** (a)–(d) **· L53 · L57 · L70 · L71 · L72 · L73 · L74**.
2. THE ROUND-2 RULINGS: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-r2-2026-09-21.md` whole — `## Rulings table`, `## Wording offered, verbatim`, `## Fable round-1 claims, file-checked` (C1–C9), `## Checked against the files`, `## Experiments named (L70)`, `## Astra stdin experiment`, `## Independence`, `## ESCALATE` — its HOLDS / DOES NOT HOLD tables are your filter · the raw round-2 rulings of every house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-tribunal/r2/` (`grok-ruling-r2.md`, `gemini-ruling-r2.md`, `astra-ruling-r2.md` or `astra-ruling-r2.partial.md` — a PARTIAL counts ONLY for the questions its first line lists; the raw file is what "verbatim" means, but you never copy a user-data span out of it) · your own round-2 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-fable-r2-2026-09-21.md` (its SELF-ATTACK and WITHDRAWN lines bind you: a withdrawn sentence of yours is never folded).
3. v2 `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-v2-2026-09-21.md` whole, and the derive report `reports/setups-tribunal-derive-2026-09-21.md` (`## NEEDS ROUND 2`, `## OWNER ITEMS`, `## ESCALATE`).
4. Only where a fold turns on it: the real file a file-check cites — read to check, never to copy.

OWNER RULINGS THAT BIND: R15 / R17 (nothing of his before cards) · **R24** (`cto-2026-09-21.md:35`: his trade log is NEVER pass/fail truth; no gate, fixture or expected value from a day he traded or tagged — a round-2 wording that rests on one is NOT taken, and is named in `## ESCALATE`).

THE RULES OF THIS DERIVE — `25`'s rules, UNCHANGED, plus the closing rule:
- **Take a wording VERBATIM** — a house's `NEITHER` replacement, or the round-1 text (Grok's or yours) a `GROK'S` / `FABLE'S` choice names, word for word, or not at all; you choose, you never blend, edit or improve. A wording whose supporting claim DOES NOT HOLD in the round-2 hub's file-check (or is WITHDRAWN in your round-2 report) is NOT taken.
- **When is a question SETTLED.** Count the SEATS that ruled it: the houses (Grok, Gemini, Astra — in full, or PARTIAL for the questions it lists) plus the Fable seat. SETTLED = (i) every seat that ruled it chose the same verdict (for `NEITHER`, the same replacement text, or texts the hub's file-check shows are the same mechanism — otherwise not the same), **with at least one HOUSE among them**; or (ii) every HOUSE that ruled it chose the same verdict and the Fable seat's contrary choice rests on a claim that DOES NOT HOLD in the hub's C1–C9 or file-check table, or that the Fable seat itself WITHDREW. The Fable seat's choice alone never settles a question (it is one of the two sides; R32's single-seat clause, read strictly — the desk confirmed that reading at 15:16). Everything else is STILL SPLIT. An ITEM is settled when each of its questions is.
- **STILL SPLIT → `## FOR DEJAN`, never settled by you (L37, L39).** ONE A/B per item (not per question): `A — <one side, whose>` / `B — <the other, whose>`, each side's case in ONE sentence, and the COST of each in HIS terms (a card that shows a score on an assumed value · every card scoreless and WATCH ordered by pool rank · a migration · an extra deploy evening · two of the seven never forming · a load that fails for the whole Strategies folder), plus which chunk waits on his answer. The FINAL carries that item as `[FOR DEJAN — R2-n]` with both texts quoted, and is NOT buildable for the chunk it gates until he answers. No round 3 is drafted for a split — the ONLY path to a round 3 is a house's `TRIBUNAL R2: DO NOT BUILD`, which you list under `## NEEDS ROUND 3` with its text verbatim (L39: round 3 is the last).
- **Prefer the smaller mechanism** only BETWEEN texts the seats agree are correct; a disagreement about correctness is a split.
- **Every "not checkable from reads" claim → a first-gate experiment** in the FINAL's list, placed before the chunk it gates (L70). The astra stdin experiment is NOT a design experiment: record its outcome in your report only.
- **Invent nothing. Nothing of his before cards. Dissents carried VERBATIM** (`## Dissents, verbatim` gains every round-2 choice the FINAL does not follow, with house and question). **NO USER DATA** (L32): spans replaced by `[user data: <KEY or note:line>]`, counted.

WRITE (Write tool, TWO new files):
- `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` — v2, whole, with each `[F-nn · NOT SETTLED → R2-n]` block REPLACED in place by the settled wording, tagged `[R2F-nn]` (a row of your fold table), or by `[FOR DEJAN — R2-n]` with both texts quoted; `## Not settled` becomes `## Status after round 2` (per item: settled / FOR DEJAN, and the chunk each gates); the header names v2, the round-2 hub report, the raw rulings of every seat that ruled round 2 (astra PARTIAL marked), your round-2 report and the fold table's path; `## First-gate experiments (L70)` = v2's X1–X17 plus every round-2 experiment the fold takes; `## Dissents, verbatim` extended; `## L52 (a)–(d)` re-answered for the FINAL — (a) is MET only if R2-2 is settled. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-derive-r2-2026-09-21.md` — §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` (≤30 lines: per item, settled or FOR DEJAN and whose text; C1 unblocked or not and why; seats that ruled round 2; owner items after cards; evenings to first card and to all seven UNDER THE FINAL) → `## Fold table` (`R2F-nn · question (R2-2.1 … R2-4.3) · seats that ruled it (<n>, named) · verdicts (per seat) · whose wording · adopted verbatim? yes / no / not taken · why (≤25 words, citing the round-2 hub's file-check row or C<n>)`) → `## FOR DEJAN` (the A/Bs; empty = none) → `## NEEDS ROUND 3` (only a `DO NOT BUILD`; else `none`) → `## OWNER ITEMS (after cards)` (v2's twelve, updated, deduplicated, each with who raised it; none a precondition) → `## Redactions` → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `SETUPS FINAL DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch reads its own report first and continues from `## CONTINUE`; the FINAL, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line of the report, L71): `SETUPS FINAL DERIVED · folds: <n> · C1 unblocked: <yes|no — <why in ≤10 words>> · for Dejan: <n> · owner items (after cards): <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `for Dejan` = A/Bs in `## FOR DEJAN`, and `C1 unblocked: yes` ONLY when R2-2 is settled AND nothing else in C1's row of §10 is `FOR DEJAN` (R2-1 gates C1's registry check: if R2-1 is FOR DEJAN, C1 is buildable only without that check, and the stop line says so in its ≤10 words) — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `for Dejan: 0` and C1 unblocked → the desk commits the FINAL and an Opus seat drafts C1's build prompt; otherwise the desk brings him the A/Bs, one per message.
