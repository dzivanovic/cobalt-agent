MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: packet drafter `drc-sitting-packet-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/71-draft-drc-sitting-packet.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control drc-sitting-packet-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `50` / `64`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write outside the two files below, no memory-folder write (L58) — you write exactly the TWO files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PREPARE (do NOT design, do NOT decide) THE PACKET FOR HIS DRC TEMPLATE SITTING. LADDER: `S3-P3 · F14` precondition ("DRC template review session … before S3-P3", `docs/00 - Project/SPRINT-LADDER-v0_1.md` "Rulings/inputs owed before S3", untouched since 09-10). RULED (`cto-2026-09-21.md` §4 **R48**, his words: "the DRC needs to be designed by me … not by the tribunal"; **R52**: he names the time, the packet is ready when he is): the DRC is designed BY HIM in one sitting with the desk. **THIS = everything that sitting needs on ONE short page, so he decides and nobody researches while he waits.** You propose NO template, NO field he has not already asked for, NO wording of his note. DO NOT STOP until the report's last line is `DRC SITTING PACKET READY …` or `FAILED …`.

HOW HE READS IT (`/Users/cobalt/Vault/Think/6 - Permanent/Memory/preferences.md`, `topics/cto-desk.md` 09-19 / 09-21 verbosity rulings): the packet is LISTENED to on a phone. Plain sentences, no tables wider than three columns, no law numbers in the body, no file paths in the body (paths go in an appendix). Every decision is ONE question he can answer with a letter or a word, with what each answer costs or unlocks in one clause. Order = what blocks S3-P3's build first.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L11** the human-only variable stays his · **L28** Cobalt writes only marker-bounded units, create-if-absent, human wins — so the sitting must settle WHICH parts of the DRC note are Cobalt's units and which are his free text · **L30** friction that makes the trader stays; friction that moves no needle goes · **L32** DRC content is USER data — copy NO personal content (no P&L, no ticker he traded, no psychology text) into either file; describe sections by their HEADINGS only · **L35** · **L45** the reader / writer is tested against HIS real template's shape · **L65** · **L70** · **L71** · **L74**.
2. Memory: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/areas/daily-report-card.md` in full (the DRC contract and patterns as he stated them) · `areas/cobalt-product-definition.md` (grep `DRC`, `F14`, `F15`, `excitement`, `cost-of-discipline`, `pick-vs-rank`, `miss line`).
3. The ladder's F14 row + S3 smoke (`:586-604`) and the "Rulings/inputs owed before S3" paragraph (`:538` region) · `docs/00 - Project/MVP-CHARTER-v0_2.md` F14, F15, F6 (the 09:00 proposal that quotes the prior DRC), §12.
4. WHAT EXISTS TODAY, every claim with a path (and `file:line` for code): HIS DRC template(s) and recent DRC notes under `/Users/cobalt/Vault/Think/` (find them: `ls` the Templates folder and `1 - Trading/`; record the HEADINGS and the frontmatter KEYS only) · the SMB-format reference under `docs/90 - References/` (what the SMB DRC's sections are) · `src/cobalt/prefill/` (what the 15:40 / 05:15 prefill writes today, into which note, which units, which ids — `ops/com.cobalt.prefill-*.plist` for the clock) · `src/cobalt/vaultwrite/` (what a unit is; frontmatter site) · `docs/40 - DevDocs/cobalt/prefill/` DevDocs · what S2 already stores that F14 must render: `"user".picks` (pick-vs-rank), the MISSED rows + miss line (`src/cobalt/replay/`, `archiver`), cards written / taken, the L7 shadow taps.
5. What S3's exit side will add for the DRC to read — by NAME only, from the ladder's F11 / F22 rows (legs, estimated vs confirmed, the stop-override gap of `cto-2026-09-20.md` R38); tonight's S3 exits proposal (`prompts/2026-09-21/70-propose-s3-exits.md`) runs beside you — do not wait for it, do not read its output.

WRITE (Write tool, exactly two new files):
- `/Users/cobalt/cobalt/docs/30 - Design/DRC-SITTING-PACKET-2026-09-21.md`, ≤140 lines, in this order: (1) **WHY THIS SITTING, 3 sentences** — what S3-P3 cannot build until he has decided; (2) **WHAT EXISTS TODAY, ≤12 sentences** — his template's sections by heading, what Cobalt already fills and when, what S2 already stores that has no place on the note yet; (3) **WHAT THE LADDER ALREADY PROMISED** for the DRC note, as a plain list of the F14 items (cards written / trades taken, the taken / passed / discarded ask for every unfilled card, pick-vs-rank, miss line, cost-of-discipline tally, adherence checkboxes, excitement audit, estimated legs to confirm, time blocks, PnL, the 3-line stub in the daily note, next morning's quote) — each marked `has a home in his template today` · `no home yet` · `NOT ESTABLISHED`; (4) **THE DECISIONS, numbered, ≤15**, each ONE question answerable by a letter or a word, the blocking ones first: e.g. one DRC note per day beside the daily note or inside it · which template is THE template · for each "no home yet" item: where it goes or that it is dropped · which sections are his free text that Cobalt never touches · what Cobalt asks him at 15:40 and through which surface · what the morning proposal quotes · whether the SMB PDF builder is wanted at all in S3; NEVER a recommended ANSWER on content — the DRC is his design — but where a choice has a build cost, say the cost in one clause; (5) **WHAT HE SHOULD HAVE OPEN** during the sitting (his template, one recent DRC, the card mock); (6) APPENDIX — paths and `file:line` for everything above; UNPROVEN rows named.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-sitting-packet-2026-09-21.md`: §0 Headline ≤5 lines → the exists-today fact table (`claim · path or file:line · PROVEN | UNPROVEN`) → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤25 lines** (the decisions in one line each, which ones block S3-P3, how long the sitting should take). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line of the report): `DRC SITTING PACKET READY · decisions: <n> · blocking S3-P3: <n> · ladder items with no home yet: <n> · NOT ESTABLISHED: <n> · sitting length: <n> min · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
