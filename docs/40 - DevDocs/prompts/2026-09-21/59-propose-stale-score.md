MODEL: Opus 5 (`claude-opus-5`) — DESIGN-PROPOSAL seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: proposer `stale-score-design-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/59-propose-stale-score.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control stale-score-design-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `36` / `49`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the TWO files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PROPOSE (do NOT build, do NOT draft build prompts) what a CARD'S SCORE does while the bars under it are STALE. LADDER: `S2-P2 · F10 Dots / ladder` (a defect in what is live). ORIGIN: `reports/stale-marker-design-2026-09-21.md` ESCALATE 1 (found 16:12 ET, told to him 16:1x, he raised no objection to queuing it): an OPEN card is refreshed with no staleness guard — `refresh_card` runs for every open card (`evaluate.py:1329-1349`), takes proximity from `ev.last_price`, the last STORED bar however old (`:776-777`), writes that price as the card's `last` (`:803`); stale computed dots suppress `card_score` only while UNTAPPED (`scoring.py:248`) → a card whose computed dots are all tapped shows a live `card_score` computed on a stale `last`. The round-2 setups hub proved the same mechanism from the other side (`reports/setups-tribunal-r2-2026-09-21.md` ESCALATE 3: a score comes back after a tap or a refresh because every path recomputes from dots). LAW STEP: this touches SCORING and what reaches the card → L52 / L67: **proposal (THIS) → four-house tribunal → build → ≥3 checkers → deploy**. The tribunal PRODUCES the design; your proposal is its input — state options honestly, recommend ONE, and price each. DO NOT STOP until the report's last line is `STALE SCORE PROPOSED …` or `FAILED …`.

WHAT IS FIXED AROUND YOU (you change none of it): (1) the STALE STAMP on the row and the card (his R36, prompts `51`–`54`, `30 - Design/STALE-MARKER-PROPOSAL-2026-09-21.md`) is a RENDERING and ships on its own — your design must sit beside it, never replace it; (2) chunk C1 of `30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` (his R40 = B) adds ONE `assumed_formation` dot that CANNOT be tapped, so the existing `suppression()` holds on all five recompute paths — read how it does that (`reports/setups-c1-draft-2026-09-21.md`, the five paths with `file:line`; `reports/setups-tribunal-fable-r2-2026-09-21.md` R2-2.1) BEFORE you invent a mechanism: L3 says ONE path, and "a dot that cannot be tapped while its input is stale" may already be that path — or may not fit, because staleness LIFTS and an assumed value does not; say which, from the files; (3) thresholds are HIS (L53): "stale" has three clocks today (poller 180 s RTH-only + `error` · evaluator 2 × scan_interval = 200 s every session, `input_stale` · the pool banner 2 × interval since the last scan) — you propose WHICH clock the score obeys and why, never a new number; (4) cards are ADVISORY under the L7 shadow; his hand grade is never touched; (5) R24: nothing he traded or tagged is pass/fail truth.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** a score on stale input is a plausible-wrong artifact — the loud state wins · **L3** · **L8** · **L9** · **L32** no value of his in either file · **L35** · **L36** · **L52 (a)–(d)** answer each in one line for your recommended option · **L53** · **L57** a suppressed or frozen score must replay: what is stored, when · **L67** · **L70** every claim `PROVEN file:line` or `UNPROVEN` · **L71** · **L74**.
2. The code on MAIN (Read tool under `/Users/cobalt/cobalt`): `src/cobalt/cards/evaluate.py` (`refresh_card`, the `input_stale` evaluation state `:532` region, `:776-803`, `:1329-1349`), `scoring.py` (`suppression()` `:247-251`, `refresh_dots` `:201-235`), `store.py` (`tap_dot` `:1215-1225`, the five writers of a card row), `cards/radar.py` (WATCH order: `card_score`, then `pool_position`), `audit_export.py`, `replay/` (what the nightly replay recomputes and whether a stale window would replay the same way — L57), `src/cobalt/radar/poller.py` `:121` and `store.py` `stamp_poll` (the known S4-over-S3 stamp overwrite, carried — name it if your design depends on the stamp). Tests for each. ADR-0009 (D4: `card_score` alone orders WATCH cards).
3. `reports/stale-marker-design-2026-09-21.md` (fact table, ESCALATE 1–2) · `reports/setups-tribunal-r2-2026-09-21.md` ESCALATE 2–3 · `cto-2026-09-21.md` §4 R36, R40.

WRITE (Write tool, exactly two new files):
- `/Users/cobalt/cobalt/docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md`, ≤250 lines: (1) FACT BASE — every path that writes `last`, `card_score`, a dot or the ladder order, and for each whether it knows the bars are stale (`claim · file:line · PROVEN | UNPROVEN`); the exact sequence that produces a live score on a stale `last` today, and whether it can also ARM, EXPIRE or REORDER a card on stale input (proximity, levels, terminal transitions) — that last part decides the size of the problem; (2) OPTIONS, at least three, each with mechanism, files, what he SEES on the card, what is stored (L57), what the replay does, cost, and how it meets C1's untappable-dot path: e.g. (A) the score is suppressed while input is stale and returns by itself when fresh · (B) the card freezes whole — no refresh of `last`, dots, state — until fresh · (C) the refresh runs but every value computed on stale input is marked and excluded from the score; (3) YOUR RECOMMENDATION and its L52 (a)–(d) lines; (4) which clock, and the L53 sentence; (5) what is deliberately NOT changed; (6) chunks, seats per L29 (`store.py` writers = a write path on a user table → Opus floor), migration yes / no, RESTARTS expected (a guess is marked as one), evenings to live, and whether it can ride C2 of the setups plan or must not wait for it; (7) first-gate experiments (L70), each runnable on `cobalt_dev`; (8) ≤10 questions for the tribunal, the ones where you are least sure first.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-score-design-2026-09-21.md`: §0 Headline ≤5 lines → the fact table → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤30 lines** (the problem's true size in his terms; the options in one line each; the recommendation; anything that is HIS). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line of the report): `STALE SCORE PROPOSED · can score on stale input: <yes|no> · can arm, expire or reorder on stale input: <yes|no|UNPROVEN> · options: <n> · recommended: <≤8 words> · migration: <yes|no> · chunks: <n> · open to the tribunal: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
