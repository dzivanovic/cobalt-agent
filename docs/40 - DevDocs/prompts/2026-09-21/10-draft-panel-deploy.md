MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `panel-deploy-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/10-draft-panel-deploy.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control panel-deploy-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `01` / `05` / `07`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO `launchctl`, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. **SPEED MATTERS: this deploy must be LIVE BEFORE 09:30 ET TODAY (his R5). Draft the SMALLEST correct prompts — cut every step of the old shapes that this deploy does not need, and say what you cut.**

# DRAFT (do NOT launch, do NOT run) the PRODUCTION DEPLOY of ONE branch, `s2/panel-order-0921` (the R3 radar page order: ONE line in `src/cobalt/aset/radar_panel.py` `render_radar_page`, two tests, two DevDoc sentences, a report), and the ONE-HOUSE-OR-MORE read of that deploy prompt. LADDER: `S2 · Radar panel (§5 host)`. LAW STEP (L67): his R3 = the design → build `08-panel-order-build.md` (running / built — read its stop line) → its ≥3-house check `09-panel-order-check.md` (the desk launches it) → **THIS = the deploy prompt + its review prompt** → the desk launches the review, folds, launches the deploy. DO NOT STOP until the report's last line is `PANEL DEPLOY PROMPTS DRAFTED …` or `FAILED …`.

HIS OVERRIDE, RECORDED (`docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 **R5**, 06:38 ET — quote it in the deploy prompt's AUTHORIZATION, greppable and committed): **L43 (one deploy per evening), L66's "inside the 20:00–21:00 pause" and NN#16's "deploy outside market hours only" are OVERRULED for THIS deploy; it is LIVE BEFORE 09:30 ET on 2026-09-21; his "do it now" is also the approval of this deploy's command list (L62, LAWS L73 as amended 2026-09-21) — NO second approval is asked.** THE BOUND THE DESK KEEPS, and the prompt enforces: **ONLY `com.cobalt.aset` is stopped and restarted. `com.cobalt.radar` is NEVER booted out, kickstarted or restarted by this deploy — it is SCANNING PREMARKET NOW** (L43's radar clause is NOT overruled). The gate for that: `COBALT_ENV=production uv run cobalt jobs restarts <main tip>..<branch tip>` (or the offline spelling the build used) must derive `com.cobalt.aset` and must NOT derive `com.cobalt.radar` nor any `UNCLASSIFIED` → otherwise `FAILED: RESTARTS — <table>`, nothing merged. WHY IT IS SAFE TO MERGE WITH THE RADAR UP (state it, and have the hub PROVE it, not assume it): L66 exists because launchd can respawn a resident into a half-merged tree; here `git diff --stat main <branch tip> -- src/cobalt/radar src/cobalt/cards src/cobalt/archiver src/cobalt/session src/cobalt/db_migrations configs` must be EMPTY (a radar respawn would load byte-identical radar code) — non-empty → `FAILED`, nothing merged. A HARD CLOCK in PREFLIGHT and again before the merge: `date` at or after **09:15 ET** → `FAILED: window — too close to the open; tonight's pause instead`, nothing merged (the desk then re-issues for the evening).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1** · **L19** · **L35** · **L36** · **L42** RESTARTS line · **L46 / L54** rebase-then-ff, `--ff-only`, rollback = `git revert`, never `reset --hard` · **L51** the three pre-approvals · **L55** push NEVER in the list · **L61 / L62 / L63** per-session allowlist, the gate PROVEN at launch (reverted tag + empty commit — read how `02-deploy-stack-3.md` / `38-deploy-p4.md` did it and keep the smallest form) · **L66** aset DOWN before the merge, UP after (`launchctl bootout` … `bootstrap`; the radar untouched — above) · **L67** this prompt is read by ≥1 other house before it runs · **L68** ONE branch lands, no sibling → no stacked gate; say so · **L70** · **L71** · **L73** (amended 2026-09-21) · **L74**.
2. THE SHAPES (copy byte for byte what serves, CUT what does not — no migration, no settings load, no backup run, no replay, no second branch): `docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md` (33 KB — the launch line's rule strings are the APPROVED deploy vocabulary; use ONLY strings that appear there, each byte for byte, EXCEPT the three that must name this branch/worktree: `Bash(git -C /Users/cobalt/cobalt merge --ff-only s2/panel-order-0921)`, `Bash(git -C /Users/cobalt/cobalt-wt/panel-order rebase main)`, `Bash(git -C /Users/cobalt/cobalt-wt/panel-order rebase --abort)` — list those three under `BRANCH-NAMED strings` in your report; they are covered by R5, not a new approval) and its report `docs/40 - DevDocs/reports/deploy-2026-09-19.md` (what actually happened, the smoke it ran on `:5010`); the review shape `docs/40 - DevDocs/prompts/2026-09-19/41-review-deploy-p4.md` (11 KB) with today's `prompts/2026-09-21/04-bars-chunk-1a-check-r3.md` for the house launch spellings, the gemini file-viewer-only sentence, the 14 + 3 strings and the DATE GATE.
3. THE BUILD: `prompts/2026-09-21/08-panel-order-build.md`; its report in the worktree `/Users/cobalt/cobalt-wt/panel-order/docs/40 - DevDocs/reports/panel-order-build-2026-09-21.md` (if its last line is not yet `PANEL ORDER BUILT …`, draft from the prompt; the deploy prompt reads the tip and the suite line from that stop line AT RUN TIME, never from this file); `ops/com.cobalt.aset.plist` and `ops/start_aset.sh` (how aset starts, its port, `COBALT_VAULT_PATH`).
4. `docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 R3, R4, R5 and §3b (why no card can form before 09:30 ET).

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `11-panel-order-deploy.md` — Sonnet 5 hub `panel-deploy-0921` (no write-path CODE is written by it; it merges a checked branch — if `02`'s seat was Opus for a reason that applies here, say so and use Opus), cwd per `02`'s shape. Body, in order, smallest form: AUTHORIZATION (R3, R5 committed; a launch row `R__` the desk fills; the build's `PANEL ORDER BUILT` stop line present; the check report `reports/panel-order-check-2026-09-21.md` last line `PANEL ORDER CHECK DONE …` with `defects that HOLD: 0` — else `FAILED: not checked`) → PREFLIGHT (one row per rule, the HARD CLOCK, main clean but the known paths: ` M configs/cobalt/rules.yaml` + the three never-added settings-packet paths — L51(2) says how a machine-written file is handled; read how `02` treated a dirty tree and keep main's dirt OUT of the merge) → PROVE THE GATE (L61) → rebase the branch on main in ITS worktree (docs-only commits landed on main since the cut) → the offline suite on the rebased tip ONLY IF an approved string allows it from that worktree (if none does, say so: the build's own suite line + a docs-only `git diff --stat <cut>..main` is the proof, and list it as a READING) → the RESTARTS gate and the EMPTY-radar-diff gate (above) → tag the pre-deploy main (`pre-panel-order-0921`) → `launchctl bootout gui/501/com.cobalt.aset` → `merge --ff-only` → tag `deploy-2026-09-21` → `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` → SMOKE: `launchctl print gui/501/com.cobalt.aset` = running; `curl … http://127.0.0.1:5010/` = 200 and `…/radar` = 200 (the approved curl string prints the status code only — so the ORDER on the live page cannot be read by curl with that string: say so, and have the hub prove the order with the Read-free fact that the running process started AFTER the merge (`launchctl print` pid / start) plus the build's own route test; the DESK confirms the live order with him); `COBALT_ENV=production uv run cobalt heartbeat show` not red; the radar's pid UNCHANGED from PREFLIGHT (`launchctl print gui/501/com.cobalt.radar`) — a changed pid → ESCALATE loudly → ROLLBACK, one command path, proven shape: aset down → `git revert --no-edit <range>` → aset up → smoke; triggered by any red smoke row. TOTAL aset downtime target < 60 s; record the bootout and bootstrap times from `date`. STOP LINE: `PANEL DEPLOY DONE <main tip> · tag deploy-2026-09-21 · aset down <s> s · /radar 200 · radar pid unchanged: <yes|NO> · rollback: not used|USED · ESCALATE: <n>` — or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`; in-progress last line pinned, breadcrumb under `## CONTINUE`. Report `docs/40 - DevDocs/reports/deploy-2026-09-21.md` on main, committed by the hub by explicit path (`02`'s shape).
- `12-review-panel-deploy.md` — Sonnet 5 hub `panel-deploy-review-0921`, `41`'s shape + `04`'s launch line (14 + 3 strings of `08` of 09-20, byte for byte), folder `scratch/tribunal-bars-0920/panel-deploy-review/`, DATE GATE, ONE round, targets THREE houses, TWO answering is enough, ONE is the floor (L67) — it runs WHEN NO OTHER CHECK HUB RUNS (PREFLIGHT refuses on an in-progress last line of `bars-chunk-2-check-r3-2026-09-21.md` or `panel-order-check-2026-09-21.md`; the desk staggers). Packet = `11` whole (parts), R3 / R5 rows, `02`'s launch line for the string comparison, the plist. QUESTIONS, from reads alone: can any step touch, stop or restart `com.cobalt.radar` · can the merge land anything but the branch's five paths + report (main's dirty files, another branch) · is every rule string in the launch line from `02` or one of the three BRANCH-NAMED ones · does the rollback leave production on the pre-deploy tree with aset UP on every failure branch (name any branch that ends with aset DOWN) · does any step wait on a human or a dialog · closing line `REVIEW: RUN IT` / `RUN IT AFTER <folds>` / `DO NOT RUN <why>`. Collation + hub file-check of every claim as `41` / `04`. Stop line `PANEL DEPLOY REVIEW DONE · grok: … · gemini: … · astra: … · houses that read it: <n> of 3 · launch blockers that HOLD: <n> · folds proposed: <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/panel-deploy-draft-2026-09-21.md`: §0 Headline ≤5 lines → what you CUT from `02`'s shape and why → the launch line's strings, each with `grep -c -F -e` = 1 against `02-deploy-stack-3.md`, then `BRANCH-NAMED strings: 3` (or the list), then `OTHER NEW strings: NONE` (or the list — each one is a blocker the desk must see) → the suite-on-rebased-tip finding → per prompt: file, size, seat → `READING:` list → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `PANEL DEPLOY PROMPTS DRAFTED · prompts: 2 · deploy seat: <Sonnet 5|Opus 5> · strings from 02: <n> · branch-named: <n> · other new: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
