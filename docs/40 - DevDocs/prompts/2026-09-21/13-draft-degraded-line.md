MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `degraded-line-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/13-draft-degraded-line.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control degraded-line-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `01` / `05` / `07` / `10`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) FOUR prompts for ONE front-end change: on `/radar`, a SLIM RED DEGRADED LINE ABOVE THE CARD LADDER, shown ONLY while a source is degraded. LADDER: `S2 · Radar panel (§5 host)` (`docs/00 - Project/SPRINT-LADDER-v0_1.md`). LAW STEP (L67): the design decision IS his ruling (`cto-2026-09-21.md` §4 R10, 07:4x ET, "A and this evenuing", to the A/B the three houses raised as question 6 of `reports/panel-order-check-2026-09-21.md`) → **THIS = drafting its build, the ≥3-house check of that build, its EVENING deploy and the houses' read of that deploy prompt** → the desk reads, asks him ONE approval list, launches. DO NOT STOP until the report's last line is `DEGRADED LINE PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): since this morning's deploy (`deploy-2026-09-21`, `72c4f12`) the ladder renders above the pool view, so the pool view's degraded banners sit BELOW the ladder and he may not see a dead source while he works the cards (L9: "red banner … never silent staleness"). RULED: a slim red line ABOVE `#ladder-layer`, desktop AND phone frame, present ONLY when at least one source is degraded, naming what is degraded in the fewest words the existing banner text allows; when nothing is degraded it takes NO space. The pool view's own banners STAY where they are, unchanged. NOT ordered: no restyle, no new label elsewhere, no change to what a card or a pool row shows, no new degraded COMPUTATION — the line is a second RENDERING of the state the pool banners already render (L3: one path; if the files show two different degraded states on the page, name them and ESCALATE which one the line carries — safe default: every state the pool view banners today). It MUST stay current: the page refreshes its layers by id (`refreshPool` / `refreshLadder` in `PANEL_JS`), so a line rendered once at page load would go stale silently — establish from the files the NARROWEST way the line refreshes with the state it shows (expected: it lives inside, or is replaced together with, a fragment that already refreshes; a new API route or payload field is an ESCALATE, not your decision). It must NOT move the ladder when it refreshes unchanged (his 06:32 complaint: the page jumped on every refresh).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** · **L9** · **L19** · **L28** no vault/DB write path is touched — the panel READS · **L29** front-end only with no write path = Sonnet-eligible; say whether ANY line the build must touch sits on a write path — if one does, the builder seat becomes Opus 5 · **L35** · **L36** · **L42** RESTARTS derived, never judged · **L43 / L66** evening deploy: residents down BEFORE the merge, inside the 20:00–21:00 ET pause · **L45** tests against the real rendered page · **L46 / L54 / L60** own worktree off main, RECOVERY RULE · **L52** nothing here changes scoring, ranking, or what a card SHOWS · **L62 / L63** · **L67** build checked by ≥3 houses; the deploy prompt read by houses other than its author · **L68** (only this branch lands tonight unless the desk says otherwise — say what other unmerged branches exist and that none lands) · **L71** · **L73** · **L74**.
2. The code, on MAIN (Read tool under `/Users/cobalt/cobalt`): `src/cobalt/aset/radar_panel.py` — where the pool view's degraded banners are rendered and from which view fields, `render_ladder`, `render_pool`, `render_radar_page` (`:1126` now ladder-first), `render_failed_page`, `PANEL_JS` (`refreshPool` / `refreshLadder`), `PANEL_CSS`; `src/cobalt/aset/web.py` (`/radar` routes and the fragment routes — expected untouched). Tests: `tests/cobalt/test_radar_panel.py`, `tests/cobalt/test_radar_panel_cards.py`. DevDocs: `docs/40 - DevDocs/cobalt/aset/radar_panel.md`, `web.md`.
3. THE SHAPES TO COPY, byte for byte wherever the same sentence serves — this morning's four prompts for the SAME page, all of which ran clean: `prompts/2026-09-21/08-panel-order-build.md` (build; its 16 + 3 strings and triplet = his 09-20 R25 approval; how its worktree was created and by whom) · `09-panel-order-check.md` (round-1 build check, 14 + 3 strings of 09-20 R13 / R23 — `Bash(grok *)` / `Bash(agy *)` stand THROUGH TODAY 23:59 ET, DATE GATE) · `11-panel-order-deploy.md` AS FOLDED (the `\?` curl, the relaunch rule at `## CONTINUE`) · `12-review-panel-deploy.md`. For the EVENING shape that `11` did not have (his R5 set the window aside this morning ONLY): `prompts/2026-09-19/02-deploy-stack-3.md` and the 09-19/09-20 evening deploy prompts it points to — aset AND radar down before the merge, restarted after, inside the pause (L66), every string from an approved line.
4. The reports: `reports/panel-order-check-2026-09-21.md` (question 6) · `reports/panel-deploy-review-2026-09-21.md` (`## Checked against the files` rows 5, 6, 7, 11 — fold ALL FOUR lessons into the new deploy prompt: the escaped `?`, a branch-identity gate at PREFLIGHT, the relaunch rule, a log check tied to the new pid or start time) · `reports/deploy-2026-09-21.md` · `cto-2026-09-21.md` §4 R3, R5, R8, R10.
5. TONIGHT'S CLOCK, which the deploy must not collide with — establish each from `ops/*.plist` and `COBALT_ENV`-free reads only, and state the window you chose and why: 20:00–21:00 ET market_reset pause · `com.cobalt.archiver` 20:30 (its FIRST nightly, the ONE shadow night) · `com.cobalt.replay` 21:10 (first run) · `com.cobalt.backup` 21:40. Expected: launch 20:02, hard clock 20:25, done before 20:30; if the files say otherwise, say so under `## ESCALATE`.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `14-degraded-line-build.md` — hub `degraded-line-build-0921`, `08`'s shape: OFFLINE, branch `s2/degraded-line-0921` off main, worktree `/Users/cobalt/cobalt-wt/degraded-line`, seat per the L29 finding. TEST FIRST on the REAL rendered page, desktop AND `phone_frame=True`, RED quoted: (a) with a degraded view the line is present, occurs BEFORE `id="ladder-layer"`, and names the degraded source(s); (b) with a healthy view NO line element with visible box exists; (c) the pool view's banners are byte-identical to main in both cases; (d) whatever refresh mechanism you specified keeps the line current — a test that renders the refreshed fragment for degraded → healthy → degraded. THE CHANGE in the narrowest form; DevDoc sentence(s); CLOSE as `08` (full suite, `git diff --stat main` limited to the named paths, EMPTY diffs for `src/cobalt/cards`, `src/cobalt/radar`, `src/cobalt/aset/store.py`, `configs`, and for `web.py` unless your refresh finding required a line there — then named; `uv run cobalt jobs restarts main..HEAD` VERBATIM). AUTHORIZATION greps `cto-2026-09-21.md` for row R10 (`-S"A and this evenuing"`, committed) and a launch row `R__` the desk fills. STOP LINE: `DEGRADED LINE BUILT <tip> | on <main tip> | offline <p>/<f> (<s> skipped; baseline <p>/<f>) | line above ladder when degraded, desktop + phone: proven | absent when healthy: proven | stays current on refresh: proven | pool banners unchanged: proven | card/scoring paths untouched: empty diff | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `15-degraded-line-check.md` — Sonnet 5 hub `degraded-line-check-0921`, `09`'s shape and strings byte for byte, folder `scratch/tribunal-bars-0920/degraded-line-check/`, DATE GATE (2026-09-22 or later → FAILED). QUESTIONS: is the line above the ladder in BOTH frames and ONLY when degraded · does it carry the SAME state as the pool banners (one path, L3) or a second computation (`file:line`) · can it go stale between refreshes, or move the ladder on an unchanged refresh (name the mechanism) · is any pool content, label, route, payload or refresh interval changed (`NONE` or `file:line`) · did any assertion get weaker · does anything reach scoring, ranking or what a card shows (L52) · closing line as `09`. Stop line `DEGRADED LINE CHECK DONE · grok: … · gemini: … · astra: … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>` — or `FAILED …`.
- `16-degraded-line-deploy.md` — Sonnet 5 hub `degraded-line-deploy-0921`, cwd `/Users/cobalt/cobalt`, `11`'s structure with the EVENING shape of item 3: PREFLIGHT date `2026-09-21`, time inside the window of item 5 (before it → `FAILED: window — too early`, the desk launches on a timer; after the hard clock → `FAILED: window — too late`, NOTHING is down), branch-identity gate, AUTHORIZATION (R10 · build stop line · check `defects that HOLD: 0` committed · review `launch blockers that HOLD: 0` or folds named in launch row `R__`), gates BEFORE anything goes down (rebased code identical to the checked tip, RESTARTS derivation verbatim, branch carries exactly its paths), residents down per L66 exactly as the approved evening prompts do it, `merge --ff-only`, residents up, smoke (the three curls with `\?`, the line's marker on production's tree, new pids, heartbeat, log check tied to this start), ONE rollback path, every failure branch ends with the residents' bootstrap attempt. Tag `deploy-2026-09-21b`. Stop line `DEGRADED LINE DEPLOY DONE <tip> · tag deploy-2026-09-21b · residents down <n> s · /radar 200 · rollback: <not used|used> · ESCALATE: <n>` — or `FAILED …`. Report: APPEND a new `#`-level run section to `reports/deploy-2026-09-21.md` is NOT wanted (its last line is this morning's stop line, L71) — write `reports/deploy-2026-09-21b.md`.
- `17-review-degraded-line-deploy.md` — `12`'s shape and strings byte for byte, ONE round, folder `scratch/tribunal-bars-0920/degraded-line-deploy-review/`, gate: refuses after 19:30 ET. Stop line `DEGRADED LINE DEPLOY REVIEW DONE · … · houses that read it: <n> of 3 · launch blockers that HOLD: <n> · folds proposed: <n> · ESCALATE: <n>`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/degraded-line-draft-2026-09-21.md`: §0 Headline ≤5 lines → WHERE THE DEGRADED STATE LIVES (`state · view field · file:line · rendered where today`) → THE REFRESH FINDING (the mechanism you specified, `file:line`, why it is the narrowest) → the L29 seat finding → worktree creation → per prompt: file, size, seat → RULE PROOF (`grep -c -F -e` = 1 per string against the approved source line, per prompt) → **`NEW strings:`** — every string not byte-identical to an approved one (branch-named merge / rebase strings included), as ONE list the desk can show him verbatim → the evening window you chose and the clock evidence → `READING:` list → `## ESCALATE` → `## CONTINUE` → last line. ALSO a `## DIGEST FOR THE DESK` of ≤40 lines: what each prompt does step by step, so the desk does not read the four prompts end to end (`cto-2026-09-21.md` R7). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `DEGRADED LINE PROMPTS DRAFTED · prompts: 4 · builder seat: <Sonnet 5|Opus 5> · refresh mechanism: <≤8 words> · new rule strings: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
