MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `ops-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/05-draft-ops-0921.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control ops-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `prompts/2026-09-20/22-draft-bars-chunk-2-fix.md` and today's `01`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58), NO restic command of any kind, NO read of any file that holds a secret (`.env`, the key store, keychain) — you write exactly the files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch, do NOT run) (A) the restic password ROTATION STEP LIST for Dejan's own hand and (B) the `ops-0921` hub prompt. LADDER: `OFF-LADDER — ruled cto-2026-09-20 R41, R42` (the step list) and standing ops hygiene (`cto-2026-09-20.md` "DESK DECISIONS WITH HIS VETO", §48). A lane beside the bars round 3 (L72) — it blocks nothing and nothing blocks it. DO NOT STOP until the report's last line is `OPS 0921 DRAFTED …` or `FAILED …`.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L4 / L41** secrets are held by processes, never by models: no secret value, no password, no key material appears in ANY file you write or read; the step list has HIM type every secret · **L19** · **L35** · **L36** · **L43 / L66** nothing restarts a resident outside its window · **L54** `~/cobalt` IS production · **L55** push never in an allowlist · **L62 / L63** · **L67** a desk-written prompt that touches production is read by another house before it runs — say in your report which items of (B) need that · **L70** · **L71** · **L74**.
2. His words and the desk's decision: `docs/40 - DevDocs/reports/cto-2026-09-20.md` §4 R41, R42 (and R12 for the push deny) · `docs/00 - Project/BACKLOG.md:419-422` (host-OS-upgrade entry checklist item 2) · `docs/00 - Project/PROJECT-LEDGER.md:1174` (the Cobalt key store in restic's include set + a fresh restore proof, owed since 09-11) · `/Users/cobalt/Vault/Think/6 - Permanent/Memory/areas/cobalt.md` `## NOW` (the restic and OPS lines).
3. How the backup actually runs, from the FILES (quote `file:line` for every fact you rely on; what the files do not show is `NOT KNOWN`, never guessed): `src/cobalt/backup/` and its DevDocs under `docs/40 - DevDocs/cobalt/`, `configs/cobalt/` backup config, `ops/com.cobalt.backup*.plist`, `cobalt backup --help` text in the source, `logs/backup.log` (last 40 lines), and the 09-17 restore the desk ran (`topics/cto-desk.md` line "21:48 ET the desk TRUNCATED" — the working `cobalt backup restore ssd latest --into … --include …` shape). Establish: where the repository password is READ from at run time (VaultManager key name / env / file — the NAME only), how many repositories exist (`ssd`, any second copy), when the job fires (21:40 daily per the desk's notes — verify).
4. The shape of a past ops prompt and its checks: `docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md`, `19-ops-round2.md`, and the standard `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`.

WRITE (Write tool, new files only):
- **(A)** `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/restic-rotation-steps-2026-09-21.md` — FOR HIM, read on a phone: ≤25 numbered steps, each ONE command he runs in Local Terminal or one thing he types, in order, every command safe to run at once and copied from restic's own key interface (`restic key list` / `key add` / `key remove` / `key passwd` — state which one the step list uses and why: add the new key and PROVE it opens the repository BEFORE the old key is removed, so a typo never locks the repository); where the NEW value must be stored so the 21:40 job finds it (the store and key NAME from item 3, never a value); the proof after (a `cobalt backup` read-only command or `restic snapshots` that succeeds with the new value, then one restore of one small file into a temp folder); when NOT to do it (never between 21:30 and the backup's end; never while a deploy runs); what to do if any step fails (the old key still works until the remove step — stop there, tell the desk). A closing section `OWED WITH IT, BY A HUB` = the key store in the include set + the fresh restore proof (LEDGER:1174) as items of (B). Anything item 3 left `NOT KNOWN` that the list depends on is an `ASK DESK` AND a clearly marked blank in the list — never a guess.
- **(B)** `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/06-ops-0921.md` — the hub prompt (Sonnet 5 hub; Opus 5 headless builder only where a write path is touched, L29), launch line first, index card second, `UNATTENDED-LAUNCH.md` shape, own worktree `~/cobalt-wt/ops-0921` off main, launch-line strings taken ONLY from `16-ops-2026-09-19.md` / `19-ops-round2.md` — list every string you use with the file it was approved in, and list separately (`NEW strings: …`) any the items cannot do without, so the desk can bring him ONE approval list (L62). ITEMS: (1) `docs/40 - DevDocs/cobalt/db_migrations/cli.md:113-124` stale paragraph on main (`reports/bars-1a-fix-draft-2026-09-20.md` row S2, its ESCALATE 4); (2) two lines in `UNATTENDED-LAUNCH.md`: never wrap a command to read its exit status; `--disallowedTools "Bash(git push*)"` on every hub whose cwd is `~/cobalt` (09-20 R12) — L19: is `UNATTENDED-LAUNCH.md` a prompt (re-issue) or a standard (edit)? read how it was changed before and follow that; (3) the stale registry rows — eight `working` rows of 09-17/09-19 in `claude agents --json` whose sessions are long finished, + `b63d56a0` done: the hub LISTS them with evidence each is dead (L60: a session's liveness is asked, never inferred — so the item is a list for the desk, the hub stops NOTHING); (4) main's `uv run pytest -q` stopping at collection on `tests/test_finviz_extractor.py:34` — UNPROVEN (L70): the hub RUNS it offline in its worktree, reads the failure, and reports; a fix only if it is a test-collection defect inside the new tree's scope, else a BACKLOG line (old tree untouched — strangler rule); (5) the always-loaded block margin of 6 characters (`close-2026-09-20.md` "Measure") — MEASURE and propose which line could shorten; the hub never writes the memory folder (L58); (6) the restic include-set + restore proof of (A)'s closing section — drafted as a step the hub runs ONLY AFTER the desk relays that he has rotated, with `COBALT_ENV=production` named (L51) and no secret on any command line; (7) the three never-added settings-packet paths on main (`prompts/2026-09-19/31-packet/p2-*.yaml`, `prompts/2026-09-20/16-packet/`) — 09-20 R40's desk decision says no settings packet yaml enters git: propose the `.gitignore` line (named folder, never a widened exception) and prove it with `git check-ignore`. Stop line `OPS 0921 DONE · items: <n> of 7 · commits: <n> · NEW strings used: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`; in-progress last line pinned, breadcrumb under `## CONTINUE`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/ops-draft-2026-09-21.md`: §0 Headline ≤5 lines → the backup FACTS table (`fact · file:line · value or NOT KNOWN`) → per file written: path, size, seat → RULE PROOF for (B) (`grep -c -F` = 1 per string against the file it came from; `NEW strings: NONE` or the list) → which items need a second house before they run → `READING:` list → `## ESCALATE` (every `ASK DESK`) → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `OPS 0921 DRAFTED · files: 2 · restic steps: <n> · NOT KNOWN facts: <n> · new rule strings: <n> · items needing a second house: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
