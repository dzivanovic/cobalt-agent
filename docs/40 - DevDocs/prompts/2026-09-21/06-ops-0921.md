MODEL: Sonnet 5 (`claude-sonnet-5`) for LAUNCH 1 (items 1–5, 6a, 7 — docs, `.gitignore`, one config line, reads; no vault/DB write path, L29) · Opus 5 (`claude-opus-5`) for LAUNCH 2 = step 6b ONLY (a production restore of the backup = recovery, L29 floor; L50: the same prompt file, relaunched on the other model — no headless builder, so no `claude -p` rule) · SEAT: ops hub `ops-0921`, launched by the CTO desk in the background; drafted by `ops-draft-0921` (Opus 5), not by the desk and not by Dejan.
LAUNCH 1 — three bare commands, run by the desk: `git -C /Users/cobalt/cobalt worktree add -b ops/2026-09-21 /Users/cobalt/cobalt-wt/ops-0921 main` then `cd /Users/cobalt/cobalt-wt/ops-0921` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/06-ops-0921.md' and follow it exactly — LAUNCH 1." --model claude-sonnet-5 --remote-control ops-0921 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(git check-ignore *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`
LAUNCH 2 (step 6b only; NOT before the three conditions at the top of step 6b hold; same worktree, it already exists — do NOT `git worktree add`) — two bare commands: `cd /Users/cobalt/cobalt-wt/ops-0921` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/06-ops-0921.md' and follow it exactly — LAUNCH 2, step 6b only." --model claude-opus-5 --remote-control ops-0921-6b --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=production uv run cobalt backup status)" "Bash(COBALT_ENV=production uv run cobalt backup restore ssd latest *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`
STRINGS: from `prompts/2026-09-19/16-ops-2026-09-19.md` line 1 (approved 2026-09-18 06:46 ET, `cto-2026-09-18.md` §4 R1 list (2); reused byte for byte by `2026-09-19/19-ops-round2.md` line 1): the 16 `Bash(...)` allow strings above from `"Bash(uv run pytest *)"` to `"Bash(date*)"`, `"AskUserQuestion"`, `"EnterWorktree"`, the three `--add-dir` values. **NEW strings** (not in 16 / 19 — the desk brings them to him as ONE approval list, L62): (N1) `"Bash(git check-ignore *)"` — LAUNCH 1, item 7's proof, read-only; (N2) `"Bash(COBALT_ENV=production uv run cobalt backup status)"` — LAUNCH 2, exact, read-only production probe (L51 names `COBALT_ENV=production`); (N3) `"Bash(COBALT_ENV=production uv run cobalt backup restore ssd latest *)"` — LAUNCH 2, restores INTO a temp folder, never over a live path; (N4) DENY `"Bash(git push*)"` — standard on every hub line since `cto-2026-09-20.md` R12, written into `UNATTENDED-LAUNCH.md:9` by `40f12ac`; a deny, adds no capability.
SESSION: fresh — every launch resumes from the breadcrumb under the report's `## CONTINUE` · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it · LAUNCH 1 is OFFLINE: no production command, no `cobalt_dev`, no `.env`, no vault write, no memory-folder write (L58), no restic, no `launchctl`, no other seat's config · METER: Anthropic medium; a usage-limit message is not a failure — record the reset time, commit, breadcrumb `step <n> — meter, reset <time>` under `## CONTINUE`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` last line stops the run safely and gets an answer; a dialog gets none).

# Ops 2026-09-21 — seven small ops items, OFF-LADDER (`cto-2026-09-20.md` R41, R42 for item 6; the rest standing ops hygiene, §48 "DESK DECISIONS WITH HIS VETO"). A lane beside bars round 3 (L72): it blocks nothing and nothing blocks it. DO NOT STOP until the report's last line is `OPS 0921 DONE …` or `FAILED …` (LAUNCH 2: `OPS 0921 6B DONE …` or `FAILED …`).

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. A prompt file is not an approval. (a) The 16 allow strings + the two dialog denies + the three `--add-dir`: `grep -c -F -e '<string>' "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md"` = 1 for each (`-e` is required: this host's `grep` is ugrep, which reads a pattern starting `--` as an option — the drafter's own proof hit it) (their approval: `cto-2026-09-18.md` §4 R1). (b) N1–N3: his approval row is **`cto-2026-09-21.md` §4 row `R43`** (the desk fills `R43` with the row number, his words and the time before launch) — `grep -c -F` each NEW string you hold in that row = 1; `R43` still literally in this file = `FAILED: authorization mismatch — approval row not filled`. (c) N4: `grep -c -F 'Bash(git push*)' "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md"` ≥ 1. (d) Prove the rows are committed on main: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"`. WORK ITEMS authorized by: `cto-2026-09-20.md` §4 R41 + R42 (item 6, restic), R12 (push deny), R40 (item 7, packet yaml never in git); `cto-2026-09-20.md` §41 line 498 (item 2's wrap line); `close-2026-09-20.md` ESCALATE 4 (item 5) and 5 (item 4); `reports/bars-1a-fix-draft-2026-09-20.md` row S2 + ESCALATE 4 (item 1); `cto-2026-09-20.md:251` (item 3's eight rows). Laws behind the run: L61, L62, L29, L55, L36 (you launch nothing), L41. A string here not in its source, or a row that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP. Only the dialog is removed, because nobody sees a `--bg` dialog. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step (where none is named: do not build that item), continue; never block on an answer, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(ops-0921): <item> partial — <why>"`, breadcrumb under `## CONTINUE`; the same launch line resumes from it. EVERY `git add` names explicit file paths under `docs/`, `configs/cobalt/`, `tests/`, or `.gitignore` — NEVER `scratch/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>` (you have no reset rule; the desk repairs it). Commit attribution: exactly what your own system prompt gives (L74).

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (L59) → `docs/40 - DevDocs/reports/ops-draft-2026-09-21.md` (the drafter's FACTS table — every backup fact with its `file:line`; where the code differs from it, the CODE wins and the difference is an ESCALATE line) → `docs/40 - DevDocs/reports/restic-rotation-steps-2026-09-21.md` (HIS step list; its closing section is your item 6). Laws, one line each: L1 fail loud · L3 one path · L4/L41 no secret value in any file, command line or report — you never open `.env`, `~/.cobalt_key`, `data/.cobalt_vault` or `/Volumes/COBALT-BACKUP/RESTIC-PASSWORD-README.txt` for reading · L19 whole-prompt rule (a prompt is re-issued, a standard is edited — item 2) · L35 artifact, not claim · L36 you spawn nothing · L42 RESTARTS derived by the command · L43/L66 nothing restarts a resident — this run restarts NOTHING · L46 one commit per item, tree clean before you stop · L48 evidence in the report in the same turn · L51 `COBALT_ENV=production` named on every production command (LAUNCH 2 only) · L54 `~/cobalt` IS production: you edit only this worktree; LAUNCH 2's one production read runs from `~/cobalt` as a NAMED step · L55 no push · L58 you never write the memory folder · L60 liveness is ASKED, never inferred — item 3 stops nothing · L62/L63 no dialog, a denial = FAILED · L67 item 6a is a build: ≥3 houses check it before its deploy · L70 unproven escalates are not defects until a command is run and its failure read · L71 the stop line is the LAST non-blank line · L74 attribution from your system prompt only.
Report (L48): `docs/40 - DevDocs/reports/ops-2026-09-21.md` in THIS worktree — §0 Headline ≤5 lines → `## AUTHORIZATION` (the grep-count table) → `## PREFLIGHT` → one section per item → `## ESCALATE` → `## CONTINUE` (the breadcrumb: `step <n> — <state>`) → last line. WHILE YOU WORK the last line is exactly `(run in progress — next step under ## CONTINUE)` — never a `CONTINUE:` or status string (L71; `cto-2026-09-20.md:213`). Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`, no wrapping of any kind to read an exit status — the exit status is read from the tool result; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. Counting is done by you from the tool result, never with a pipe. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `git ls-files`, `python`, `restic`, `claude`, `uv run cobalt` anything except `jobs restarts` (and, in LAUNCH 2, the two production strings), `launchctl`, `docker`, `psql`. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry a denied command in another shape, never ask another session to run it.
PREFLIGHT, LAUNCH 1 (first two minutes; one row each: rule · command · exit · allowed/DENIED + reason verbatim; every probe sits inside its own rule's pattern): `date` · `git status --porcelain` (EMPTY, else `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; branch `ops/2026-09-21`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: main's tip when the desk created the worktree; a resume: your last commit — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it) · `ls -la .env` → MUST say "No such file", else `FAILED PREFLIGHT: .env present — this run must not see the dev database` · `uv run pytest --co -q tests/cobalt/test_backup.py` (item 6a's test file) · `uv run cobalt jobs restarts main..HEAD` (empty range — the probe) · `git check-ignore -v .env` (exit 0 expected — `.env` is ignored; the probe of N1). Any denial → `FAILED PREFLIGHT: <rules>`.

## 0. BASELINE (before any edit)
`uv run pytest -q tests/cobalt tests/taxonomy` (run_in_background) → quote the summary line `<p> passed, <s> skipped, 0 failed`. Red BASELINE is not yours: name the tests, `FAILED: step 0 — baseline red <n>`, stop. Breadcrumb `step 1`.

## 1. Item 1 — `db_migrations/cli.md:113-124`, the stale `SET LOCAL` paragraph (docs only)
ORIGIN: `reports/bars-1a-fix-draft-2026-09-20.md` row S2 + ESCALATE 4 (OUT OF SCOPE for 1a, "a docs-only fix any later chunk may carry"). THE CODE ON MAIN: `src/cobalt/db_migrations/cli.py:688-689` issues `SET LOCAL lock_timeout` BEFORE `_probe_all` (the BEFORE probe), and the docstring `:588-627` says why ("IT USED TO SIT AFTER THE PROBE … SETTLED BY EXPERIMENT ON `cobalt_dev`, 2026-09-19 … THE BEFORE PROBE IS NOW UNDER THE CEILING"). THE DEVDOC ON MAIN still says the opposite: `docs/40 - DevDocs/cobalt/db_migrations/cli.md:113` "After the BEFORE probe, before `_apply`" and `:120` "the BEFORE PROBE IS NOT UNDER THE CEILING". Read `cli.md` IN FULL first and `cli.py:575-700`. FIX: rewrite `cli.md:113-124` so it says what the code does — the `SET LOCAL` is the first statement after `_connect`, before the BEFORE probe, forward and rollback; the probe's ACCESS SHARE is now under the ceiling; it used to sit after the probe and moved once the two `requires_db` tests named in the docstring settled that neither `SHOW server_encoding` nor a bare `SET LOCAL` takes the snapshot (name all three tests the docstring names, including the negative control). Then check `cli.md:78-82` (`SET LOCAL lock_timeout = '<N>s'` "before `_apply`'s first statement"): still true but incomplete — make it say "before the BEFORE probe" if that is what the code does; quote before/after. No other paragraph. NOT on main and NOT yours: the bars branch's `cli.md:461-465` (chunk 1a, unmerged) — if your text would contradict it, ESCALATE the sentence, do not read or edit the branch. Verify: BASELINE's suite re-run (the DevDocs symbol check is in it — name its line). Commit `docs(devdocs): db_migrations/cli.md — the SET LOCAL sits before the BEFORE probe, which is under the ceiling (09-20 1a-fix S2)`. Breadcrumb `step 2`.

## 2. Item 2 — `UNATTENDED-LAUNCH.md`, the "never wrap to read an exit status" line (a STANDARD — edited in place)
L19 READING, from the file's own history (`git log --format="%h %ad %s" --date=short -- "docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md"` — quote it): four in-place edits (`54d869e`, `0e52e5f`, `848e28e`, `40f12ac`), the file calls itself "standing standard … stable file, dateless" — so it is EDITED, never re-issued; a prompt is re-issued. THE PUSH LINE IS ALREADY THERE: `UNATTENDED-LAUNCH.md:9` carries `"Bash(git push*)"` in `--disallowedTools` since `40f12ac` (`cto-2026-09-20.md:192`, "R12's owed line — closed") — verify with `grep -n -F 'Bash(git push*)'` and quote the line; do NOT add a second copy (L3). If it is NOT there, add it to §1 item 4's launch-line template and say so. THE ONE LINE OWED (origin `cto-2026-09-20.md:498`; drafter O5, 1a O2): in §2's `UNATTENDED RULES` paragraph (`:14`), after "no `; echo` behind.", add one sentence: "An exit status is read from the tool result, never by wrapping the command to print it (`; echo $?`, `echo EXIT=$?`, `> /dev/null`, `|| echo`) — a wrapped command no longer matches its bare rule and is judged by the classifier (`cto-2026-09-20.md` §41)." Nothing else in the file changes. Commit `docs(standard): UNATTENDED-LAUNCH — an exit status is read from the tool result, never by wrapping (09-20 §41)`. Breadcrumb `step 3`.

## 3. Item 3 — the stale registry rows: a LIST for the desk, you stop NOTHING
The rows (`cto-2026-09-20.md:249`, `:251`): eight `working` rows of 09-17/09-19 in `claude agents --json` — `4f911896`, `da10f94b`, `39d25992`, `c9b4dc14`, `72fd32ff`, `75098464`, `6e09aadd`, `915062e7` — "absent from `ListAgents` = stale" (the desk's inference), and `b63d56a0` "review deploy3 build", `done` since 09-19. You have no `claude` rule and must not want one. EVIDENCE YOU CAN GATHER: (i) call the `ListAgents` tool (load it with ToolSearch `select:ListAgents` first) and quote, for each of the nine ids, present / absent and its state; (ii) for each id, `grep -rn -l "<id>" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/"` → the report that launched it and the stop line that report records for it (quote the stop line and its file:line). WRITE a table `id · job/label · launched (report:line) · stop line seen (report:line) or NONE · ListAgents state` and a verdict column that only ever says `ASK` — L60: liveness is ASKED (`SendMessage`, by the desk), never inferred, and you message no one. Recommendation line for the desk: which ids have a recorded stop line AND are absent from `ListAgents` (the desk asks those first). You STOP NOTHING, kill nothing, remove nothing. No commit of its own — it rides the report. Breadcrumb `step 4`.

## 4. Item 4 — main's `uv run pytest -q` stopping at collection (UNPROVEN, L70 — you RUN it)
ORIGIN: `close-2026-09-20.md:116` ESCALATE 5 and `cto-2026-09-20.md:213` — chunk E's hub saw `uv run pytest -q` stop at collection on `tests/test_finviz_extractor.py:34` (`ImportError: cannot import name 'FinvizStockData'`); never run by a desk. WHY THE GATES MISSED IT (fact, not guess): every build prompt's suite names `tests/cobalt tests/taxonomy` (`cto-2026-09-20.md:256`), while `pyproject.toml:91` sets `testpaths = ["tests"]`, so only a bare `uv run pytest -q` collects `tests/test_finviz_extractor.py`. SAFE ORDER — collection first, because the old tree's tests may reach the network: (a) `uv run pytest --co -q` (run_in_background) → quote the tail: the error count and every `ERROR collecting` line. (b) ONLY IF (a) shows ≥1 collection error: `uv run pytest -q` (run_in_background) → it must stop at collection having run 0 tests; quote its summary line (`Interrupted: <n> error(s) during collection`). If (a) collects clean → do NOT run (b); report `NOT REPRODUCED` with (a)'s line, and stop the item. (c) Read the failure, two calls: `grep -rn "FinvizStockData" src/cobalt_agent/` and `grep -rn "class Finviz" src/cobalt_agent/skills/research/` → where the names live today, or that they do not. DISPOSITION: `tests/test_finviz_extractor.py` imports `cobalt_agent.*` — the OLD tree (CLAUDE.md strangler rule: old tree untouched, KILL code dies in place). So: NO FIX in the old tree and no `pyproject` change (a test-collection change that hides an old-tree test is a scope call for the desk, not yours). Write ONE BACKLOG line under `## STANDING FOLLOW-UPS` in `docs/00 - Project/BACKLOG.md`: `- [2026-09-21 · ops-0921] Bare \`uv run pytest -q\` stops at collection on <each erroring file:line> (<error>) — old-tree tests under \`testpaths = ["tests"]\` (pyproject.toml:91); every gate runs \`tests/cobalt tests/taxonomy\` and never sees it. Desk's call: exclude the old-tree tests from the default collection, or leave the red. Evidence: reports/ops-2026-09-21.md item 4.` — only if (b) reproduced. If the failing file is inside `tests/cobalt/` or `tests/taxonomy/` (new tree) instead: that IS yours only if it is a pure test-collection defect (an import of a renamed symbol in a test) — fix it, red-first from (a), else ESCALATE. Commit `docs(backlog): bare pytest collection red on the old-tree tests — recorded, not fixed (strangler rule)`. Breadcrumb `step 5`.

## 5. Item 5 — the always-loaded block's 6-character margin: MEASURE and PROPOSE (you never write the memory folder, L58)
ORIGIN: `close-2026-09-20.md:70-78` — `wc -c INDEX.md profile.md preferences.md` = 1838 + 706 + 1450 = **3,994 / 4,000**, margin 6. THE CAP IS IN CHARACTERS (LAWS L58: "the always-loaded block under 4,000 characters"); `wc -c` counts BYTES, and every `→`, `—`, `·` is 3 bytes. Measure both, absolute paths, one call each: `wc -c "/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md" "/Users/cobalt/Vault/Think/6 - Permanent/Memory/profile.md" "/Users/cobalt/Vault/Think/6 - Permanent/Memory/preferences.md"` and the same with `wc -m`. Quote both tables. State the character margin (`4000 − wc -m total`) and whether the 09-20 measure overstated the pressure. Then read the three files and PROPOSE, for the desk only, at most THREE lines that could shorten without losing a rule (e.g. a long citation that can move behind a `[[link]]`, a duplicated sentence), each as `MEMORY: <file>:<line> — now <n> chars — proposed text "<…>" — saves <n>`; plus, if the two measures differ, ONE `MEMORY:` line proposing that SESSION-CLOSE step 5 measure with `wc -m`. No edit to the Memory folder, no commit of its own — it rides the report. Breadcrumb `step 6`.

## 6. Item 6 — restic: the Cobalt key store into the include set (6a, LAUNCH 1) + a fresh restore proof (6b, LAUNCH 2 only)
ORIGIN: `PROJECT-LEDGER.md:1174` ("Cobalt key store into the nightly include set + a restore proof", owed since 09-11); `BACKLOG.md:420-422` entry-checklist item 1; the closing section of `reports/restic-rotation-steps-2026-09-21.md`. FACTS (`reports/ops-draft-2026-09-21.md` FACTS table): the include set is `configs/cobalt/backup.yaml:37-38` `sources: [/Users/cobalt/Vault/Think]` + a fresh `cobalt_brain` dump (`:44-46`); the vault file the backup reads its password from is `REPO_ROOT/data/.cobalt_vault` (`src/cobalt/redact/secrets.py:43`, used by `src/cobalt/backup/secrets.py:35-40`) — NOT in the include set; `sources` is `list[Path]`, min 1 (`src/cobalt/backup/config.py:139`), a file path is a valid restic target; the job reads the config at every 21:40 run (`ops/com.cobalt.backup.plist:71-80`), a one-shot, not a resident.
**6a (LAUNCH 1, OFFLINE — a config line, no production effect until a deploy merges it):** ASK DESK — which files ARE "the Cobalt key store": the vault file only, or also `~/.cobalt_key` (the master key; `ops/run_backup.sh:26-28`)? A repository that holds both lets the restic password alone open every secret. SAFE DEFAULT: the vault file ONLY. Add `- /Users/cobalt/cobalt/data/.cobalt_vault` under `sources:` in `configs/cobalt/backup.yaml` with a two-line comment (why: a host loss must not take the only copy of every credential; LEDGER:1174), and nothing else in the file. A TEST, red-first: in `tests/cobalt/test_backup.py` (the existing backup test file) add one test that loads the COMMITTED `configs/cobalt/backup.yaml` and asserts the vault path is in `sources` — RED before the edit, GREEN after, quote both. DevDoc `docs/40 - DevDocs/cobalt/backup/config.md`: one sentence on what the include set now holds. `uv run pytest -q tests/cobalt tests/taxonomy` vs BASELINE (+1 passed). `uv run cobalt jobs restarts main..HEAD` → table VERBATIM (expect the config classified to `com.cobalt.backup` or `-`; UNCLASSIFIED → ESCALATE). Commit `feat(backup): the Cobalt vault file joins restic's include set (LEDGER:1174, owed since 09-11)`. L67: this is a BUILD — ≥3 houses check it before the desk's deploy prompt carries it; you do not merge. Breadcrumb `step 7`.
**6b (LAUNCH 2 ONLY — Opus 5; production READ; runs NOTHING until all three hold, each quoted in the report):** (1) the desk has written in `cto-2026-09-21.md` §4 (or its successor's) that he said "rotated <time>" — `grep -n "rotated" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"`; (2) 6a is merged: `git -C /Users/cobalt/cobalt log --oneline -20 -- configs/cobalt/backup.yaml` shows 6a's commit subject; (3) a backup ran AFTER both: `tail -n 6 /Users/cobalt/cobalt/logs/backup.err` shows a `com.cobalt.backup DONE` timestamped after the merge and after the rotation. Any one missing → `FAILED: 6b — precondition <n> not met`, stop (not a defect). THEN, as a NAMED production step (L54): `cd /Users/cobalt/cobalt` (the backup code resolves the vault from the package's REPO_ROOT, so it runs from `~/cobalt`, never from the worktree) → `COBALT_ENV=production uv run cobalt backup status` (quote it; a `COBALT_MASTER_KEY is not set` refusal = `FAILED: 6b — master key not in this session's environment` — NOT KNOWN at drafting whether a hub's environment carries it; do not look for it, do not source anything) → `COBALT_ENV=production uv run cobalt backup restore ssd latest --into <your job's tmp directory, named in your system prompt>/restore-0921` (a FULL restore of the newest snapshot; none named → `FAILED: 6b — no job tmp directory`) → PROOFS, each quoted: `ls -la <into>/Users/cobalt/cobalt/data/` shows `.cobalt_vault`; `git diff --no-index --stat <into>/Users/cobalt/cobalt/data/.cobalt_vault /Users/cobalt/cobalt/data/.cobalt_vault` → EMPTY output = byte-identical (binary: git prints no content, only a `Bin` line if they differ — never open either file otherwise); the same `git diff --no-index --stat` for `6 - Permanent/Memory/LAWS.md` restored vs live (a vault note; a difference is fine if the live file changed after the snapshot — say so); `ls -la <into>/Users/cobalt/cobalt-backups/staging/` shows `cobalt_brain.sql` with a size within 1% of the `dumped, <n> MB` of the same night's `backup.err` line. NOT IN THIS LAUNCH, → ESCALATE as OWED: reloading that dump into a scratch database (needs a DB rule no line here has). `cd /Users/cobalt/cobalt-wt/ops-0921` back. The restored tree stays in your job's tmp (cleaned with the job) — say its size (`ls -la <into>`), never copy it anywhere. Report under `## 6b`, commit the report by path. Stop line for LAUNCH 2: `OPS 0921 6B DONE · key store restored byte-identical: <yes|no> · vault note: <identical|changed since snapshot> · dump present: <MB> · dump reload: OWED · ESCALATE: <n>` or `FAILED: 6b — <reason>`.

## 7. Item 7 — the three never-added settings-packet paths: `.gitignore`
ORIGIN: `cto-2026-09-20.md:323` R40 DESK DECISION WITH HIS VETO: "from now on a settings packet yaml is NOT added to git — the report's sha256 identifies it and the loadable copy lives in gitignored `data/backups/`"; the two committed `2026-09-19/23-packet/` copies STAY. Untracked on `~/cobalt` at drafting (09-21, the drafter's `git -C /Users/cobalt/cobalt status --porcelain`; you have no `git -C … status` rule — confirm the files with `ls -la "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/31-packet/"` and `ls -la "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/16-packet/"`): `prompts/2026-09-19/31-packet/p2-dark-settings.yaml`, `prompts/2026-09-19/31-packet/p2-live-settings.yaml`, `prompts/2026-09-20/16-packet/` (holds `p2-live-settings-htf.yaml`). FIX: in `.gitignore`, directly after the `!docs/40 - DevDocs/**` carve-out (`.gitignore:21-22`) and in the shape of the existing re-ignore lines (`:27`, `:67`, `:89`), add a comment `# 09-20 R40: a settings packet yaml never enters git (sha256 in the report; loadable copy in data/backups/)` and exactly three NAMED lines — `docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml`, `docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-live-settings.yaml`, `docs/40 - DevDocs/prompts/2026-09-20/16-packet/`. Never a glob, never a widened exception, never an edit to a `!` line. ASK DESK: does R40's "from now on" want a standing pattern (e.g. `docs/40 - DevDocs/prompts/*/*-packet/*settings*.yaml`) for future packets? SAFE DEFAULT: the three named lines only. PROOF (N1), one call each, quote every result: `git check-ignore -v "docs/40 - DevDocs/prompts/2026-09-19/31-packet/p2-dark-settings.yaml"` · the same for `p2-live-settings.yaml` · `git check-ignore -v "docs/40 - DevDocs/prompts/2026-09-20/16-packet/p2-live-settings-htf.yaml"` → each exit 0 naming YOUR `.gitignore` line; NEGATIVE CONTROLS → exit 1: `git check-ignore -v "docs/40 - DevDocs/prompts/2026-09-19/31-packet/facts.md"` (a tracked sibling) and `git check-ignore -v "docs/40 - DevDocs/prompts/2026-09-20/17-cards-htf-apply.md"`; and `git log --oneline -1 -- "docs/40 - DevDocs/prompts/2026-09-19/23-packet/"` shows the two committed copies are still tracked (no line touches them). The files themselves do not exist in your worktree; `check-ignore` tests the path, which is what the merge will meet. Commit `chore(gitignore): settings packet yaml stays out of git — three named paths (09-20 R40)`. Breadcrumb `step 8`.

## 8. CLOSE (LAUNCH 1, offline)
`uv run pytest -q tests/cobalt tests/taxonomy` → the line vs BASELINE (+1 passed from 6a, else say why) · `uv run cobalt jobs restarts main..HEAD` → table VERBATIM, the `RESTARTS:` line is the command's (L42) · `git log --oneline main..HEAD` (one commit per built item + the report) · `git diff --stat main HEAD` → every path is one this prompt names (`docs/40 - DevDocs/cobalt/db_migrations/cli.md`, `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`, `docs/00 - Project/BACKLOG.md`, `configs/cobalt/backup.yaml`, one backup test file, `docs/40 - DevDocs/cobalt/backup/config.md`, `.gitignore`, your report) — any other → ESCALATE · `git status --porcelain` empty · report committed by explicit path (`docs(report): ops 0921 — <one line>`). `## ESCALATE` carries ALWAYS: (i) 6a is a build — ≥3 houses check it, then the desk's deploy prompt (no resident restart: `com.cobalt.backup` is a one-shot calendar job); (ii) 6b waits for his "rotated" + the 6a deploy + one 21:40 run, then LAUNCH 2; (iii) the dump reload into a scratch database is OWED by a DB-capable prompt; (iv) every `ASK DESK` of this run; plus `MEMORY:` / `RULING:` lines (L48, L58). Last line, exactly this shape:
`OPS 0921 DONE · items: <n> of 7 · commits: <n> · NEW strings used: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. (Item 6 counts as done for LAUNCH 1 when 6a is committed; 6b is LAUNCH 2's own stop line.) Then stop. The desk verifies the artifact (L35), stops this session, and routes 6a to its check.
