MODEL: Opus 5 (`claude-opus-5`) — DESIGN-PROPOSAL seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: proposer `stale-marker-design-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/49-propose-stale-marker.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control stale-marker-design-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `21` / `36`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the TWO files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PROPOSE (do NOT build, do NOT draft build prompts) the STALE STAMP: a ticker whose bars went stale gets a small STALE mark on its POOL ROW and on its CARD on `/radar`. LADDER: `S2 · Radar panel (§5 host)` + `F8 Card at precondition` (`docs/00 - Project/SPRINT-LADDER-v0_1.md`). LAW STEP: his ruling (`cto-2026-09-21.md` §4 R36, 16:06 ET, "A") is the design decision for the ROW mark; the CARD mark changes what a card SHOWS, so the desk needs the FACTS before it names the card side's lane (L52 / L67) — **THIS = the facts + a short proposal**. DO NOT STOP until the report's last line is `STALE MARKER PROPOSED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): since the 11:47 hotfix (`deploy-2026-09-21h`, `1ae9e94`) a per-ticker bars poll failure renders `/radar` with a red `BARS POLL FAILED` banner that NAMES the stale tickers — but each such ticker's pool row and card still show `last` as if it were fresh (owner item, `reports/page-bars-hotfix-check-2026-09-21.md` ESCALATE 1; L9 "never silent staleness"). RULED: a SMALL stale stamp on that ticker's row and on its card; the banner stays. NOT ruled: size, wording, colour (propose ONE, mark it ASSUMED). NOT ordered: no restyle, no new banner, no change to scoring, ranking, admission, the proposed key or any dot; no new threshold of his (L53 — if "stale" needs a number that is not already in the code or his settings, that is an ESCALATE, never your value).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** one path — the stamp is a second RENDERING of a staleness state that already exists, or you say plainly that no such per-ticker state exists yet · **L9** · **L28** the panel READS · **L29** say whether any line a build would touch sits on a write path · **L32** no value of his in either file · **L35** · **L36** · **L52** (the bar (a)–(d); say exactly what on the card changes) · **L57** · **L67** · **L70** a claim you could not check from reads is UNPROVEN, said so · **L71** · **L74**.
2. The code, on MAIN (Read tool under `/Users/cobalt/cobalt`): `src/cobalt/aset/radar_panel.py` — `build_pool_view` (`poll_only`, the named stale rows, `:477` / `:482` / `:589` region), `render_pool` (where a row's `last` renders), `render_ladder` and the card renderers (where a card's `last` renders), `render_radar_page`, `PANEL_JS` (`refreshPool` / `refreshLadder` — his 06:32 complaint: a refresh must not move the ladder), `PANEL_CSS`, the phone frame; `src/cobalt/radar/store.py` (`stamp_poll`, `:270-273` — the known S4-over-S3 stamp overwrite, carried, NOT yours to fix) and `runner.py` / `poller.py` for what "stale" means per ticker and where it is stored; `src/cobalt/cards/` — what a card payload carries about its bars' freshness (`evaluate.py`, `radar.py`, `models`), and WHETHER A CARD CAN FORM, SCORE OR STAY ON THE LADDER ON STALE BARS (answer from the files with `file:line`; if yes that is a SEPARATE finding → `## ESCALATE`, not part of the stamp). Tests: `tests/cobalt/test_radar_panel.py`, `test_radar_panel_cards.py`. DevDocs: `docs/40 - DevDocs/cobalt/aset/radar_panel.md`.
3. Today's neighbours on the same page, for shape and for what must not be disturbed: `reports/page-bars-hotfix-build-2026-09-21.md` (or the build report on branch `s2/page-bars-hotfix-0921`), `reports/page-bars-hotfix-check-2026-09-21.md`, `reports/degraded-line-rebase-2026-09-21.md`; branch `s2/degraded-line-0921` (`git -C /Users/cobalt/cobalt log main..s2/degraded-line-0921`) LANDS TONIGHT at ≈20:02 ET and touches `radar_panel.py` — say where the stamp's change would sit relative to it and that any build branches off main AFTER tonight's deploy.
4. `cto-2026-09-21.md` §4 R10, R11, R21, R36.

WRITE (Write tool, exactly two new files):
- `/Users/cobalt/cobalt/docs/30 - Design/STALE-MARKER-PROPOSAL-2026-09-21.md`, ≤200 lines: (1) FACT BASE, every row `claim · file:line · PROVEN | UNPROVEN`: where per-ticker staleness is known today, in which view field, how long it lives, whether the pool row and the card can each reach it without a new query, route, payload field or stored column; (2) THE ROW MARK — the narrowest change, what it looks like (ASSUMED wording / colour), desktop + phone, how it stays current on refresh without moving the ladder; (3) THE CARD MARK — the same, PLUS the L52 finding in one of exactly three words: `RENDERING` (a second rendering of an existing state, nothing computed, nothing scored, nothing reordered) · `NEW STATE` (a field, payload or column must be added — name it) · `TOUCHES SCORING` (say how); (4) what is deliberately NOT changed; (5) chunks, seats per L29, RESTARTS expected (derive from the touched paths, L42 — a guess is marked as one), evenings to live; (6) test plan on the REAL rendered page (L45): stale ticker → both marks present and named; healthy → no mark, byte-identical rows and cards; refresh stale → fresh → stale; (7) open questions for a checker or a tribunal, ≤6.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-marker-design-2026-09-21.md`: §0 Headline ≤5 lines → the fact table → the L52 finding with its evidence → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤30 lines** (the desk reads this, not the proposal: what the two marks are, where they hook in, the L52 word and why, chunks, evenings, anything that is his). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line of the report): `STALE MARKER PROPOSED · row mark: <≤8 words> · card mark: <RENDERING|NEW STATE|TOUCHES SCORING> · new route or stored field: <yes|no> · card can score on stale bars: <yes|no|UNPROVEN> · chunks: <n> · evenings to live: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
