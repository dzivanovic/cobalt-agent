MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `setups-c1-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/55-draft-setups-c1.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-c1-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `43` / `50`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) TWO prompts for CHUNK C1 of the seven daily setups at defaults — "RUBBERBAND CAN FORM": its BUILD and the ≥3-house CHECK of that build. LADDER: `S2-P2 · F8 Card at precondition` (the proven S2 defect: Rubberband can never form a card — `areas/cobalt-sprints`, proof run `20`) + the ladder change he ruled (`cto-2026-09-21.md` §4 R15 / R17 / R18: S3's detectors pulled forward, his seven daily setups first). LAW STEP (L67): design → four-house tribunal, TWO ROUNDS, CLOSED: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` (`SETUPS FINAL DERIVED`, `reports/setups-tribunal-derive-r2-2026-09-21.md`) + his answer to the one split that gated C1 (R40, 17:01 ET: R2-2 = **B, one extra dot that cannot be tapped**) → **THIS = drafting C1's build and its check**. C1's DEPLOY prompt is NOT yours: it is drafted after the build is checked (the desk decides the evening and what it stacks with, L43 / L68). DO NOT STOP until the report's last line is `SETUPS C1 PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING; every sentence of scope comes from the FINAL with its tag, never from your own design):
- C1's row of FINAL §10, word for word: direction from anatomy (§1) · the `unclassified` setup token [F-07] · NO unbound-direction refusal [R2F-01: not built] · the geometry guard (§2.3, §9 (5)) [F-16 · X17] · gates 1–5 and points (1)–(5) of §9 for rubberband · the `A-01` mark under R2-2 **B** — the Fable seat's round-2 texts for R2-2.1–R2-2.5 as the FINAL quotes them: ONE `assumed_formation` dot made UNTAPPABLE in `tap_dot` (a tap is refused with a message), so the existing `suppression()` holds on all five recompute paths; a static key closure computed once at formation (`iter_cfg_tokens(def)` + detector `TUNABLE_KEYS` + `ASSUMED_CONVENTIONS`); in C1 the dot marks `A-01` from a module constant — NO store, column, closure table or migration in C1; `AtomOutcome` stays closed, the key rides the dot; X8 re-worded to include a tap; ORDER BY unchanged. Where the FINAL still prints A's or Astra's variant beside B for C1, B is what is built — quote R40.
- C1 needs NOTHING from C2 [F-18]: no registries, no assumed note, no mirrored frame. Nobody writes `Assumed Defaults.md` anywhere (FINAL [F-09]; `cto-2026-09-21.md` §12 15:16 bullet).
- R24 BINDS EVERY TEST (`cto-2026-09-21.md` §4 R24): his trade log, tags and retro labels are NEVER pass/fail truth. Acceptance fixtures are written from the DEFINITION evaluated on stored bars (FINAL [F-21], §9); his tagged ticker-days may only be REPLAYED to LOOK, never asserted. No value of his in a prompt, a fixture name, a report or a commit message (L32) — the Rubberband definition's numbers live in his note and the loaded DB row, tests construct their own config (L69).
- The first card under C1 shows NO score for its life (every seat's text): say so in the build's DevDoc sentence and in the check's questions — it is the design, not a defect.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** · **L7** cards stay ADVISORY under the shadow; C1 is a trading-logic change → its DEPLOY needs his chat "approve" (L61) — say so under `## CONTINUE`, it is not a build gate · **L8** · **L10** · **L11** the human-only variable Rubberband keeps (FINAL "L11") · **L19** · **L28** no vault write · **L29** `cards/store.py` `tap_dot` is a write path on a user table → the builder is **Opus 5** (FINAL §10; Fable R2 ESCALATE 2) · **L32** · **L35** · **L36** · **L41** the build uses `cobalt_dev` only, through Cobalt commands that fetch their own credentials; `.env` never printed · **L42** RESTARTS derived (`cobalt jobs restarts`), expected `com.cobalt.radar` (+ aset only if the files say the dot's rendering needs it — establish, do not guess) · **L45** real-shape fixtures · **L46 / L54 / L60** own worktree off main, RECOVERY RULE, wip-commit never discard · **L52** (a)–(d) — quote the FINAL's own L52 section; (a) is exactly what the untappable dot enforces · **L57** · **L62 / L63** · **L67** ≥3 checkers · **L68** name every other unmerged branch (`s2/degraded-line-0921` lands tonight ≈20:02; `s2/stale-marker-0921` is built tonight after it and deploys TUE ≈20:02; the `bars/*` three) and which files C1 shares with each — the build branches off main AFTER tonight's `16` has landed (PREFLIGHT: tag `deploy-2026-09-21b` on main, as `51` does) · **L69** · **L70** every first-gate experiment the FINAL assigns to C1 is RUN by the build or named UNPROVEN, never argued · **L71** · **L73** · **L74**.
2. THE DESIGN: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` — `## Status after round 2`, §1, §2.3, §7 (the parts R2-2 B settles), §9, §10 (C1's row), `## L52 (a)–(d)`, `## First-gate experiments (L70)` (C1's list: X3, X9, X17, X8's tap half, X24, X27 — read each one's exact wording), `## L11`, `## What this does NOT do`, `## Dissents, verbatim`. Then `reports/setups-tribunal-derive-r2-2026-09-21.md` (DIGEST, fold table rows for R2-1 and R2-2, ESCALATE 5) · `reports/setups-tribunal-fable-r2-2026-09-21.md` (`## Rulings` R2-2.1–R2-2.5 = the texts B means, and its ESCALATE 2: `tap_dot` + the four `compute_dots` call sites incl. `audit_export.py`) · `reports/setups-tribunal-r2-2026-09-21.md` (`## Checked against the files` — every row about R2-2 that HOLDS is a test the build owes; ESCALATE 2 and 3).
3. THE DEFECT, as proven: `/Users/cobalt/cobalt-wt/rubberband-proof/docs/40 - DevDocs/reports/rubberband-card-proof-2026-09-21.md` (run `20`, in its own worktree) and `/Users/cobalt/cobalt/docs/_inflight/defs-gap-table-2026-09-21.md` (E1, E8, E9; gitignored, local) — why S2-P2's suite and the 09-19 replay were green while no card could form; the build's FIRST test reproduces that RED on main.
4. THE CODE on main (Read tool under `/Users/cobalt/cobalt`): `src/cobalt/cards/` (`evaluate.py`, `seam.py`, `scoring.py`, `store.py` — `tap_dot` `:1215-1224`, `radar.py`, `audit_export.py`, models), `src/cobalt/radar/` detectors the §1 mechanism names (`extension.py` and what it reads), the taxonomy loader for `setup_ref` / the `unclassified` token, `src/cobalt/aset/radar_panel.py` only for how a dot renders and how a refused tap's message reaches the page. Tests: `tests/cobalt/` files for cards, scoring, store, seam. DevDocs of every file the build will touch.
5. THE SHAPES TO COPY, byte for byte wherever the same sentence serves: for an Opus build on a write path WITH `cobalt_dev` — find the newest approved one (`prompts/2026-09-19/` S2-P4 / archiver builds, `prompts/2026-09-20/10-…1a-build.md`, `11-…2-build.md`, today's `02-bars-chunk-2-fix-r3.md`) and say which line you copied and whose approval row covers its strings; for the check — today's `44-setups-tribunal-r2.md` / `52-stale-marker-check.md` (14 + 3 strings; astra launched with ` < /dev/null`, the hub runs `date` at every completion notice and stops a house past 20 min with its own task-stop tool; date gate 2026-09-22 on HIS row `cto-2026-09-21.md` §4 R39, literal `Bash(grok *) and Bash(agy *) through 2026-09-22`).

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `56-setups-c1-build.md` — Opus 5 hub `setups-c1-build-0921`, branch `setups/c1-rubberband-0921` off main, worktree `/Users/cobalt/cobalt-wt/setups-c1` (created by the desk; say the two commands). PREFLIGHT (tag `deploy-2026-09-21b` on main; `cobalt_dev` reachable if the experiments need it — say from the FINAL's "needs from `cobalt_dev`" cell exactly which steps do; nothing else holds the dev DB). TEST FIRST, RED quoted, in this order: the defect's reproduction → §9 gates 1–5 / points (1)–(5) for rubberband from a DEFINITION-written fixture [F-21] → the geometry guard (X17) → the `unclassified` token → the untappable dot: present on a formed Rubberband card naming `A-01`; `tap_dot` on it REFUSED with the message and the row unchanged; `card_score` stays null through ALL FIVE recompute paths (name each `file:line`; tap of every OTHER dot, `refresh_card`, `refresh_dots`, the audit export, the replay) = X8's tap half, X24, X27 as the FINAL words them; ORDER BY unchanged → healthy non-Rubberband output byte-identical. THE CHANGE in the narrowest form, file by file. DevDoc per touched file. CLOSE: full offline suite + the with-DB suite if any touched test `requires_db`, `git diff --stat main` limited to the named paths, EMPTY diffs for `configs/`, migrations, `src/cobalt/aset/web.py`, the archiver, the vault writers; `uv run cobalt jobs restarts main..HEAD` VERBATIM; every C1 experiment's result line (`X<n>: PASS | FAIL | UNPROVEN — <why>`). AUTHORIZATION greps `cto-2026-09-21.md` for rows R15, R40 (committed) and a launch row `R__` the desk fills; every rule string `grep -c -F` = 1 against the approved line you copied. STOP LINE: `SETUPS C1 BUILT <tip> | on <main tip> | offline <p>/<f> (baseline <p>/<f>) | with-DB <p>/<f> or not needed | rubberband forms on a definition-written fixture: proven | untappable dot, score null on all five paths: proven | other setups' output unchanged: proven | experiments: <n> PASS / <n> UNPROVEN | migration: none | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `57-setups-c1-check.md` — Sonnet 5 hub `setups-c1-check-0922`, `52`'s shape and strings byte for byte, its own staging folder under `scratch/tribunal-bars-0920/`, THREE houses, astra REQUIRED if its probe says UP (a build that reaches the card; PREFLIGHT fails closed on METER and names the retry time), staggered against `52` / `54` (same seats). Packet ≤120 KB: the diff, the touched files, the tests, the FINAL's C1 row + §1 + §7 + §9 + L52 section + C1's experiments, R40's row, the Fable R2 rulings R2-2.1–.5, the build report. QUESTIONS: does direction now come from anatomy exactly as §1 words it · can Rubberband form, and ONLY when the definition says so (name a bar sequence where it forms wrongly or fails to) · is the dot untappable on every path, and is there ANY path where a score appears on an assumed value (`file:line`) — tap, refresh, export, replay, a second writer of the row · does anything outside Rubberband change · did any assertion get weaker, does any fixture rest on a day he traded or tagged (R24) · L52 (a)–(d) one line each · is anything of C2 smuggled in, or anything of C1 missing · closing line as `52`. The hub file-checks EVERY claim, the Fable-seat texts included. Stop line `SETUPS C1 CHECK DONE · grok: … · gemini: … · astra: … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-c1-draft-2026-09-21.md`: §0 Headline ≤5 lines → C1's scope as a table (`item · FINAL tag · file(s) · test`) → the five recompute paths with `file:line` → which experiments need `cobalt_dev` → per prompt: file, size, seat → RULE PROOF per string → **`NEW approvals:`** every string not byte-identical to an approved one, as ONE list the desk can show him verbatim (`none` is a fine answer) → other unmerged branches and shared files (L68) → what C1's DEPLOY will need (restarts, his L7 / L61 "approve", whether it can stack with `53` on TUE or takes WED — facts only, the desk decides) → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤40 lines**. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `SETUPS C1 PROMPTS DRAFTED · prompts: 2 · builder seat: Opus 5 · needs cobalt_dev: <yes|no> · new rule strings: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
