MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `s3-exits-tribunal-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/72-draft-s3-exits-tribunal.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control s3-exits-tribunal-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `37` / `43` / `60`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-1 prompts of the FOUR-HOUSE TRIBUNAL (L67) on `docs/30 - Design/S3-EXITS-PROPOSAL-2026-09-21.md`. LADDER: `S3-P1 · F11` + `S3-P2 · F22` (S3 opens 09-24). LAW STEP: proposal DONE (Opus, `reports/s3-exits-design-2026-09-21.md`: `S3 EXITS PROPOSED · chunks: 6 · write-path chunks: 5 · migrations: 2 · inbound DM path exists: no · trade-note writer exists: partial · reaches scoring: no · build estimate: 50 h · open to the tribunal: 12 · owner items: 8 · ESCALATE: 6`) → **THIS = drafting its tribunal** → the tribunal rules and derives the FINAL → build per chunk → ≥3 checkers (Sol + Opus 5 + Grok + Gemini, R46) → deploy. It is a DESIGN tribunal: the OpenAI seat is ASTRA, the Anthropic seat is FABLE (LAWS L67 `[amended 2026-09-21 18:05]`; R49's Sol / Opus strings are NOT used here). DO NOT STOP until the report's last line is `S3 EXITS TRIBUNAL PROMPTS DRAFTED …` or `FAILED …`.

THIS IS TODAY'S STALE-SCORE TRIBUNAL AGAIN, ON A LARGER DESIGN. Your template is `prompts/2026-09-21/60-draft-stale-score-tribunal.md` and what it produced — `61-stale-score-tribunal.md` · `62-stale-score-tribunal-fable-seat.md` · `63-stale-score-tribunal-derive.md` (the newest clean copies: astra with ` < /dev/null`, `date` at every completion notice, the 20-minute task-stop, the hub file-checks the blind Fable seat's claims too, the `FABLE ROW: R__` / `DERIVE ROW: R__` first lines). Copy those shapes byte for byte wherever the same sentence serves; every rule of `60` binds here unchanged, with these substitutions:
- (2') THE FABLE SEAT IS NOT YET APPROVED for THIS tribunal (09-20 R18: asked per case; `cto-2026-09-21.md` R51 approved the stale-score tribunal's seat ONLY). First lines `FABLE ROW: R__` / `DERIVE ROW: R__`, the same gates as `62` / `63`, row literals `Fable seat: yes|no` · `derive seat: <model id>` · `S3-EXITS-PROPOSAL-2026-09-21.md`.
- (5') `Bash(grok *)` / `Bash(agy *)` through 2026-09-22 23:59 ET (`cto-2026-09-21.md` §4 R39 — copy `61`'s gate). DATE GATE: 2026-09-23 or later → FAILED (a later run needs his extension row; say so under `## CONTINUE`).
- (7) STAGGER (same houses): the house-running hub refuses while the report of `38`, `61`, `66`, `68` or `69` is in progress without a stop line (`61`'s own rule for a report that does not exist), and refuses 19:45–20:45 ET (the production deploy window). Tuesday's order is the desk's: the two code checks (`68`, `69`) and `61` go first.
- (8) PACKET ≤140 KB, MANDATORY + OPEN-AS-NEEDED parts in reading order (`66`'s layout): the proposal whole; the report's DIGEST, UNPROVEN rows and ESCALATE 1–6; the ladder's S3 table + S3 smoke; Charter F11 / F22 / §5 #16; `cto-2026-09-20.md` R38 verbatim; the code the proposal cites as pre-cut excerpts with `file:line` headers (`aset/web.py` `/card/{id}/move`, `/fill`, `/size`; `aset/engine.py` the drift constant `:43` and `compute_fill_recompute`; `cards/store.py` `fill()` / `mark_filled` / the edge table / stop edits; `vaultwrite/writer.py` `create_if_absent` / `upsert_unit`; the slice-2 `trade_note.py`; `prefill/drc.py:101-121`; `daymode` attestation; `notify/` to show no inbound path; `db_migrations/__init__.py` numbering); his templates by HEADINGS and frontmatter KEYS only — NO personal content, no P&L, no traded ticker (L32: check and say so). State the total size and an honest token estimate per house.
- ASTRA: REQUIRED if its probe says UP (five write-path chunks on a user table + a vault write path); PREFLIGHT fails closed on METER and names the retry time; a partial ruling is KEPT.

QUESTION SET for each house — ruling per item `ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>`: the proposal's 12 "open to the tribunal" items in ITS order, plus: (a) does the FACT BASE hold — name any writer of the card's state, `stop`, `actual_fill` or a trade note it missed (`file:line`), and is "no new edge is needed" TRUE; (b) L3: is the radar fill REALLY the same `fill()` + `mark_filled` + `compute_fill_recompute` path as the sheet, or does `SizingResult.from_card` create a second path (`file:line`); (c) the `legs` table: append-only corrections, the estimated / confirmed flag, running shares, CLOSED in the same transaction — any state in which running shares and the card state disagree; placement per ADR-0008 (`"user"`, `user_id NOT NULL`), migration numbering against `0012` / `0013` (L68); (d) realized R: the unit (planned vs actual), comparability with F12's counterfactual R, `provisional` while any leg is estimated, L57 replayability, L8's n wherever it is aggregated; (e) the drift warning: the pct reading of "27 % past plan", the hard-coded 25 vs the Charter's 20, the ASSUMED ATR term — is any number smuggled in that is HIS (L53), and does a missing setting refuse loudly (L1); (f) stop override per R38: does "his stop is the live stop, the structural stop shown beside it, ↺ = a stop edit marked reset" lose anything the DRC needs; (g) the DM line: a NEW resident poller under the watcher standard (L2) — poll vs websocket, the ambiguity rule, NOT RECORDED replies, and whether it belongs in S3 at all or after (owner item O8 — say what each costs); (h) F22: the note written synchronously right after the DB commit — what happens when the vault write fails after the row commits (L1, L28), one marker unit per leg, human wins, no reverse parse; the `strategy` vs `trade_def` dataview mismatch (ESCALATE 5) — whose fix; (i) the attestation soft check at FILL: record vs refuse on a mismatch; (j) Charter §5 #16 "single fill at MVP" vs scale-out legs — state the reading; (k) CLAUDE.md's absolute boundary: does ANY path read from, write to or infer from a trading platform — `NONE` or `file:line`; (l) chunks, seats by L29, the parallel C3 ∥ C4 ∥ C5 claim (shared files?), RESTARTS, the order against the seven-setups build and the stale-score fix on `cards/store.py`, and the HONEST hours; (m) the F14 / F15 seams the DRC sitting will need (`trade_note_path` on the card; the miss line's 21:10 clock vs the 15:41 note — `reports/drc-sitting-packet-2026-09-21.md` ESCALATE 1); (n) what is NOT checkable from reads → a named experiment on `cobalt_dev` (L70) · closing line per house `TRIBUNAL R1: BUILD | BUILD AFTER <≤15 words> | DO NOT BUILD <≤15 words>`.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `73-s3-exits-tribunal.md` — Sonnet 5 hub `s3-exits-tribunal-0922`, cwd `~/cobalt-wt/agy-trial`, `61`'s shape, its 14 + 3 strings byte for byte, folder `scratch/tribunal-bars-0920/s3-exits-tribunal/r1/` (Write-created), DATE GATE, STAGGER (7), Codex PREFLIGHT. Report `reports/s3-exits-tribunal-2026-09-22.md` (the hub names it from `date`); stop line `S3 EXITS TRIBUNAL R1 DONE · grok: … · gemini: … · astra: … · houses that ruled: <n> of 3 · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- `74-s3-exits-tribunal-fable-seat.md` — MODEL Fable 5.1 (`claude-fable-5-1`), seat `s3-exits-tribunal-fable-0922`, `62`'s shape and strings; AUTHORIZATION per (2'); BLIND to the houses' folder and to `73`'s report; ONE file; stop line `S3 EXITS TRIBUNAL FABLE R1 DONE · verdict: … · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — or `FAILED …`. METER line: an honest token estimate.
- `75-s3-exits-tribunal-derive.md` — `63`'s shape; refuses unless the round-1 stop lines it needs are present and committed; a wording whose supporting claim DOES NOT HOLD or is WITHDRAWN is never folded; writes `docs/30 - Design/S3-EXITS-v2-<date>.md` and its report (fold table, `## NEEDS ROUND 2`, `## OWNER ITEMS`, `## FOR DEJAN` in his terms, one A/B per item, no recommendation from a seat that holds a side — L37). Stop line `S3 EXITS DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-draft-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (what each prompt does, the launch order with clocks) → the L47 finding → PACKET (source → staged path, sizes, token estimate per house) → RULE PROOF (`grep -c -F -e` per string = 1: `73` vs `61` · `74`, `75` vs `62`) → **`NEW strings:`** (expected: none) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `S3 EXITS TRIBUNAL PROMPTS DRAFTED · prompts: 3 · astra: <REQUIRED|targeted> · questions: <n> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
