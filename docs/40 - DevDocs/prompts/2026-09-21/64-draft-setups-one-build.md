MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `setups-one-build-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/64-draft-setups-one-build.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-one-build-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `55` / `60`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic medium-to-large (the FINAL is 67 KB and you read ALL of it this time) · nobody sits at this terminal — never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) TWO prompts: ONE BUILD of the WHOLE seven-setups design, and ONE CHECK of that one build by the houses. LADDER: `S2-P2 · F8` defect (Rubberband cannot form) + the ladder change he ruled (`cto-2026-09-21.md` §4 R15 / R17 / R18). DO NOT STOP until the report's last line is `SETUPS ONE BUILD PROMPTS DRAFTED …` or `FAILED …`.

HIS RULING, WHICH REPLACES THE PACKAGING AND NOTHING ELSE (`cto-2026-09-21.md` §4 **R44**, 17:50 ET — read the row, his words are in it): "I want not eight separate chunks. I want all seven designs to be one build, and I want the houses to then check it as one build of seven different setups … a standard setup routine and rubber band … should all be following the standard routine … like Legos … you can make tomorrow another setup that does not require an intense build but just requires us to take the pieces and put it together with different parameters … Everything in those setups needs to be tunable … all the necessary dials to tune it down and up and make it discoverable or not." → the DESIGN stands: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` (four-house tribunal, two rounds, closed) + R40 (R2-2 = B, the untappable `assumed_formation` dot). SET ASIDE by him: §10's eight chunks / eight deploy evenings, "C3a deploys alone" [F-18], and the desk's C1-only prompts `56` / `57` (never launched — `56` is now your STEP-1 source, reuse its text byte for byte wherever the same sentence serves). KEPT: build → ≥3 checkers → ONE deploy (its prompt is NOT yours); every FINAL tag, experiment, withdrawn sentence and dissent; R24 (nothing he traded or tagged is pass/fail truth); L32 (no value of his in a prompt, fixture name, report or commit message).

WHAT THE ONE BUILD IS: ONE branch `setups/seven-0921` off main in ONE worktree — the path MUST be `/Users/cobalt/cobalt-wt/setups-c1` (his R41 approved `cp` / `rm` of `.env` for exactly that path; a new path = a new approval) — built by ONE Opus 5 seat (L29: `cards/store.py` `tap_dot`, the tunables load path), NO auto mode, as an ORDERED SEQUENCE OF STEPS inside the one prompt, in the FINAL's strict code-dependency order (C1 → C2 → C3a → C3b → {C4, C5, C6} → C7, §10's last paragraph), each step = its tests FIRST (RED quoted) → the narrowest change → DevDoc per touched file → the step's first-gate experiments (L70) run on `cobalt_dev` or named UNPROVEN → the suite → ONE commit by explicit paths → breadcrumb under `## CONTINUE`. The run WILL outlive one context and may meet the meter: the SAME launch line resumes from the breadcrumb (L60 RECOVERY RULE: wip-commit, never discard; a usage-limit message = record the reset time, commit, stop — not a failure). "One build" is what he, the houses and the deploy see — ONE branch, ONE stop line, ONE check, ONE deploy; the steps are the builder's own discipline, never separate deliverables. THE LEGO TEST IS AN ACCEPTANCE GATE OF THE BUILD, his words made checkable: after the last step, (i) NO source line under `src/cobalt/` names a setup (`grep` for each of the seven slugs + rubberband outside tests, fixtures and DevDocs = 0, as `registry.py:5-8` already requires); (ii) Rubberband forms through the SAME registries with byte-identical output on the committed day (FINAL §2.2–2.3 "re-registered"); (iii) a test builds an EIGHTH, synthetic definition purely from data — existing atoms, an existing trigger type and stop placement with different parameters — and it evaluates and forms on a constructed bar series WITHOUT any source change; (iv) `registry.evaluability` reports all seven + Rubberband evaluable, or names the exact missing atom per def (FINAL §2.5) — a def still not evaluable at the end is an ESCALATE with its atom, never silent.

THREE THINGS YOU MUST SETTLE FROM THE FILES BEFORE YOU WRITE (each gets a section in your report):
(A) THE DIALS. For each of the seven + Rubberband list every tunable the FINAL gives it (key · default source: engine / ASSUMED `A-nn` with the cheat-sheet page · where he changes it) and answer: does a per-setup "discoverable or not" switch exist TODAY (`trade_def` schema status / enabled field, `cards_enabled`, a tunable, the loader) — `file:line`. If NONE: name the smallest lawful way (a field of the def's own note read by the existing loader? a row in the tunables note?) WITHOUT inventing design: put it in the build ONLY if it is a pure use of an existing mechanism; otherwise it is `ASK DESK` + an `## OWNER / TRIBUNAL` line, and the build ships without it, saying so.
(B) THE TWO OPEN SPLITS that gate C2's part (FINAL [FOR DEJAN — R2-3] and [— R2-4]; `reports/setups-tribunal-derive-r2-2026-09-21.md` `## FOR DEJAN`). He is not asked before he sees cards (R15 / R17). R2-3: experiment **X20** answers the fact — write it as a STEP of the build that RUNS X20 on `cobalt_dev` and then builds the side the result supports, both sides' texts quoted in the prompt, the result line in the report. R2-4 (what the one seam row publishes when neither side forms; where observations are computed): lay out, for the desk, each live text (Grok A · Fable B · Astra's) with ONLY its file-checked support from the hub tables (`HOLDS` / `DOES NOT HOLD` / `WITHDRAWN`), say which text has NO claim that fails and NO withdrawn sentence, and draft the build to take a line `R2-4 = <A|B|ASTRA>` from the desk's launch row (the prompt refuses while the row does not carry it). Make BOTH the first questions of the ONE check.
(C) FIXTURES (the C1 drafter's finding, `reports/setups-c1-draft-2026-09-21.md` ESCALATE 1–2): for each of the eight defs say whether a committed real-shape day exists on which it forms; where none exists the build proves the path it can on definition-written fixtures [F-21] and pins `AWAITING_A_DAY` BY NAME — the list of pins is printed in the stop line; a DB-backed fixture-cut job + a blind expected-values seat close them BEFORE the deploy (not yours to draft; say exactly what that job needs from the built branch).

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: L1 · L3 · L7 (cards advisory; the deploy needs his "approve") · L8 · L10 · L11 (each setup keeps its human-only variable — FINAL "L11") · L19 · L28 (nobody writes `Assumed Defaults.md` before its reader ships, FINAL [F-09]; the build writes NO vault note — say how the assumed rows reach `cobalt_dev` for tests without his vault) · L29 · L32 · L35 · L36 · L41 · L42 · L45 · L46 / L54 / L60 · L52 (a)–(d) — the FINAL's own section · L53 · L57 · L62 / L63 · L67 · L68 (other unmerged branches and the files each shares with this build: `s2/stale-marker-0921`, `ops/2026-09-21`, the `bars/*` three, and the queued stale-score design on the same `evaluate.py` / `scoring.py` / `store.py` functions) · L69 · L70 · L71 · L73 · L74.
2. THE DESIGN, ALL OF IT: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` §1–§11, L52, experiments, L11, "does NOT do", dissents. Then `reports/setups-tribunal-derive-r2-2026-09-21.md`, `reports/setups-tribunal-fable-r2-2026-09-21.md` (`## Rulings`, ESCALATE 1–2), `reports/setups-tribunal-r2-2026-09-21.md` (`## Checked against the files`, ESCALATE 1–3).
3. `prompts/2026-09-21/56-setups-c1-build.md` + `reports/setups-c1-draft-2026-09-21.md` — STEP 1's text, the five recompute paths, E3 (`replay/formations.py:81` takes the new evaluator version — INSIDE the build), E4, E5; and its launch line: the 16 + 3 strings of 09-20 R25 + `Bash(COBALT_ENV=dev uv run pytest *)` (09-19 R18) + R41's two — THE ONE BUILD'S LAUNCH LINE IS `56`'s, byte for byte, with only the remote-control name and the prompt path changed; any further string the whole build needs (a migration under R2-3 B: `COBALT_ENV=dev uv run cobalt db migrate*`? a replay?) goes on `NEW approvals:` — find the approved precedent line for each first.
4. The code on main the steps touch (Read tool under `/Users/cobalt/cobalt`): `src/cobalt/radar/` (`evaluate.py`, `registry.py`, `structure.py`, `extension.py`, `indicators.py`, `daily.py`, `predicate.py`, `runner.py`), `src/cobalt/cards/`, `src/cobalt/taxonomy/` (`trade_def.py`, `tunables.py`, the loader), `src/cobalt/replay/formations.py`, `configs/cobalt/taxonomy/tunables.yaml`; his seven sheets and Rubberband's by PATH ONLY as the FINAL cites them (you may read them; nothing of theirs is copied into a committed file).
5. For the check's shape: `52-stale-marker-check.md` / `57-setups-c1-check.md` (14 + 3 strings; astra ` < /dev/null`; `date` at every completion notice; TaskStop past the timeout; R39's literal for 2026-09-22).

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `65-setups-one-build.md` — as above. PREFLIGHT: tag `deploy-2026-09-21b` on main (tonight's deploy landed); worktree clean; `.env` copied by R41's string, removed at EVERY stop (done, meter, FAILED). AUTHORIZATION greps `cto-2026-09-21.md` (and `cto-2026-09-22.md`) for R15, R40, R41, R44 and a launch row `R__` carrying `R2-4 = …`. CLOSE: full offline + with-DB suites vs BASELINE; `git diff --stat main` = only named paths; migration: none, or exactly the one R2-3's result requires (named, with its rollback); `uv run cobalt jobs restarts main..HEAD` VERBATIM; the LEGO TEST (i)–(iv); every experiment's result line; the `AWAITING_A_DAY` pins. STOP LINE: `SETUPS ONE BUILD BUILT <tip> | on <main tip> | steps: <n> of <n> | offline <p>/<f> (baseline …) | with-DB <p>/<f> | defs evaluable: <n> of 8 | forms on a definition-written fixture: <n> of 8 (pins: <list or none>) | rubberband output unchanged through the registries: proven | eighth def from data only: proven | no setup name in src: proven | untappable dot, score null on all paths: proven | R2-3: <A|B> by X20 | R2-4: <…> per row | experiments: <n> PASS / <n> FAIL / <n> UNPROVEN | migration: <none|name> | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED: <step> — <reason>`; while it works the last line is `(run in progress — step <n> of <n>, next under ## CONTINUE)`.
- `66-setups-one-check.md` — Sonnet 5 hub `setups-one-check-0922`, ONE check of the ONE build, three houses, astra REQUIRED if UP (fails closed on METER, names the retry time; a PARTIAL astra ruling is KEPT), date gate per R39, staggered against `52` / `54` / `61`. The packet will exceed one window: stage it in the houses' folder as a READING ORDER of parts (design excerpts → the diff by step → tests → the LEGO-test evidence → the build report), each house told which parts are mandatory and that the rest is there to open; honest token estimate per house; if astra cannot hold it, say how the question set is split across TWO astra runs of the same check rather than shrinking the evidence. QUESTIONS, in this order: R2-3 as built (does X20's result support it) · R2-4 as taken · THE LEGO TEST — is the next setup data only, name any place a setup's identity leaks into code (`file:line`) · can each of the eight form, and ONLY when its definition says so (name a bar sequence where one forms wrongly or fails to) · the untappable dot on every path · is every default a dial he can turn, marked ASSUMED until ruled, and is there a per-setup on / off · does anything live today change for the worse (Rubberband's output, the ladder order, latency — X5's number) · weaker assertions, R24 · L52 (a)–(d) · migration and rollback if any · closing line `CHECK: BUILD STANDS | FIX <≤15 words> | DO NOT DEPLOY <≤15 words>`. The hub file-checks EVERY claim. Stop line `SETUPS ONE CHECK DONE · grok: … · gemini: … · astra: … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-one-build-draft-2026-09-21.md`: §0 Headline ≤5 lines → (A) the dials table + the on / off finding → (B) R2-3 / R2-4 as laid out → (C) the fixture table → the step list (`step · FINAL tags · files · tests · experiments · needs cobalt_dev`) → an HONEST duration estimate for the build (hours of Opus run time, number of expected resumes) and for the check → RULE PROOF per string → **`NEW approvals:`** as ONE list the desk can show him verbatim (`none` is fine) → L68 shared-file table → what the ONE deploy will need (restarts, migration, his "approve", whether it can stack with `53`) → `READING:` → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤40 lines**. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `SETUPS ONE BUILD PROMPTS DRAFTED · prompts: 2 · steps: <n> · on/off dial exists: <yes|no> · R2-4 text with no failed claim: <A|B|ASTRA|none> · build estimate: <n> h · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
