MODEL: Fable 5.1 (`claude-fable-5-1`) — the FABLE SEAT's SECOND JOB on the setups tribunal (`cto-2026-09-21.md` §4 R20: "blind ruling (+ the derive step, as on the bars tribunal)"); you derive the version the four houses' rulings produce; you build, launch and commit nothing · SEAT: `setups-tribunal-derive-0921`, launched by the CTO desk in the background ONLY after ALL THREE round-1 stop lines are committed (hub `23`, the Fable seat `24`, hub `27` — the round-1 completion for Gemini and Astra). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/25-setups-tribunal-derive.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control setups-tribunal-derive-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md` and `24`, 09-20 R13 — nothing new) · SESSION: fresh (the round-1 seat's context is NOT carried; your own round-1 report is on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic FABLE — honest estimate: the raw rulings of the houses that ruled ≈ 15–30k tokens, the two hub reports ≈ 20–35k, your round-1 report ≈ 8k, the proposal ≈ 11k, LAWS.md ≈ 25k, code re-opened only where a fold turns on it ≈ 10–30k; v2 ≈ 13–16k tokens of output; peak context ≈ 130–185k · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v2 of the SETUPS DESIGN from the proposal and the round-1 rulings of EVERY SEAT THAT RULED (of the four). LADDER: `OFF-LADDER — ruled cto-2026-09-21 R15 / R17 / R18`. LAW STEP (L67): proposal DONE → round 1 DONE (Grok via hub `23`; Gemini and Astra via hub `27`, the round-1 completion; Fable via `24`) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (empty → the tribunal closes and v2 is the FINAL's input) → build per chunk → ≥3 checkers → deploy. L52's bar (a)–(d) is the bar v2 must meet. DO NOT STOP until the report's last line is `SETUPS DERIVED v2 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). Each its own Bash call: `grep -n "^| R20 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry "yes you may spawn" and "derive step") · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"yes you may spawn" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — R20 is not committed`) · **ALL THREE ROUND-1 STOP LINES, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-2026-09-21.md"` (the LAST NON-BLANK line must start `SETUPS TRIBUNAL R1 DONE ` — anything else, the in-progress line and `FAILED` included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-fable-r1-2026-09-21.md"` (must start `SETUPS TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Fable round 1 not done — last line: <line verbatim>`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-r1b-2026-09-21.md"` (must start `SETUPS TRIBUNAL R1B DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1B not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"SETUPS TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/setups-tribunal-2026-09-21.md"`, `git -C /Users/cobalt/cobalt log -1 --format=%H -S"SETUPS TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/setups-tribunal-fable-r1-2026-09-21.md"` and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"SETUPS TRIBUNAL R1B DONE" -- "docs/40 - DevDocs/reports/setups-tribunal-r1b-2026-09-21.md"` (any EMPTY → `FAILED: refused — <report> stop line is not committed on main`) · **ASTRA WAS REQUIRED** (`reports/setups-tribunal-draft-2026-09-21.md`, L47 finding; restated for round 1B by the desk, `cto-2026-09-21.md:238`): the R1B hub's stop line must show `astra: TRIBUNAL R1: …` (ruled in full) or `astra: PARTIAL <k> of 17 — …` with k ≥ 1 (ruled in part) — anything else (`SKIPPED — METER at probe`, `METER`, `HARNESS`, `TIMEOUT`, `NO RULING LINE`) → `FAILED: astra has not ruled — the desk brings him the two-house question`. Gemini not having ruled does NOT refuse the derive: the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L3 · L7** (cards advisory; nothing promotes) **· L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L28 · L32** (v2 and your report are COMMITTED: no user data) **· L35 · L36 · L37** (you derive; a SPLIT between houses is carried, never settled by vote) **· L45 · L52** (a)–(d) **· L53 · L57 · L65 · L67 · L70 · L71 · L72 · L73 · L74**.
2. THE ROUND-1 RULINGS, whole: BOTH hub reports — `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-2026-09-21.md` (Grok) and `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-r1b-2026-09-21.md` (Gemini, Astra) — each: its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` — the HOLDS / DOES NOT HOLD / UNVERIFIABLE tables of BOTH are your filter —, `## Experiments named (L70)`, `## OWNER ITEMS (after cards)`, `## ESCALATE` (and R1B's `## Independence`) · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-tribunal/r1/`: `grok-ruling.md`; `gemini-ruling.md` only if the R1B stop line shows `gemini: TRIBUNAL R1: …`; `astra-ruling.md` if astra ruled in full, else `astra-ruling.partial.md` — a PARTIAL astra ruling is counted ONLY for the items its first line lists as ruled; an item it did not reach is not ruled by astra (the hubs' copies are redacted; the raw file is what "verbatim" means — you may READ the raw file, but you never copy a user-data span out of it) · your own round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-fable-r1-2026-09-21.md`.
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` in full; its author's report `reports/setups-design-2026-09-21.md` (DIGEST, ESCALATE).
4. Only where a fold turns on it: the real file the hubs' file-checks cite (code on main, the taxonomy, his notes, the companion `docs/_inflight/setups-assumed-values-2026-09-21.md`, the gap table) — read to check, never to copy.

OWNER RULING THAT BINDS THE ACCEPTANCE SECTION (`cto-2026-09-21.md` §4 R24, 11:13 ET, his words: "Do not assume days and look alikes. This is not known at the time"): his trade log (`docs/_inflight/trading-stats-2026-09-21.md`, USER DATA, read it) is NEVER pass/fail truth — his tags, grades and retro labels are hindsight. In v2, no gate, test or `--expect-formed` day asserts "formed" / "not formed" from a day he traded or tagged, and no "negative / lookalike" fixture is built from his labels; the proposal's acceptance gate (d) is re-worded accordingly (expected values are written from the DEFINITION evaluated on the bars, by a checker house); his tagged ticker-days may only be REPLAYED to look at the engine beside him (L7 shadow, agreement stats he reads after cards show).

THE RULES OF THE DERIVE — the bars tribunal's lesson (`topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into v2 word for word, or it is not taken. You may choose BETWEEN houses' wordings; you do not blend them, edit them or improve them. A wording whose supporting claim is `DOES NOT HOLD` in either hub's file-check is NOT taken — say so in the fold table.
- **Prefer the simpler mechanism.** Where two houses offer different mechanisms for one item, take the smaller one when both meet L52, and write the other's cost and trade-off as an OWNER ITEM (after cards) — unless the two disagree on whether a mechanism is CORRECT, which is `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in v2's experiment list, placed before the chunk it gates, with the result that would change the design — never an argument in v2's text, never a defect.
- **Invent nothing.** v2 adds no mechanism, number, rule or chunk that neither the proposal nor a house's ruling contains. A gap no ruling fills stays a named gap (an experiment, an owner item, or a round-2 item) — never your filler.
- **Nothing of his before cards** (R15 / R17): nothing in v2 may require a per-setup ruling from him before cards show; a house that wrote one as a precondition is carried as an OWNER ITEM (after cards) and named in `## ESCALATE`.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT BUILD` that v2 does not follow goes into v2's `## Dissents, verbatim` word for word, with its house and item.
- **NO USER DATA in either file** (L32): no value, quote or wording from his notes, the gap table, the companion or the cheat sheets. A house's text that carries one is taken with that span replaced by `[user data: <KEY or note:line>]`; a house's proposed VALUE for an assumed key is recorded as `A-nn: value change proposed by <house> — scratch/tribunal-bars-0920/setups-tribunal/r1/<house>-ruling.md:<line>` (a pointer, never the value), and listed under OWNER ITEMS; count every redaction.

WRITE (Write tool, TWO new files):
- `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-v2-2026-09-21.md` — the proposal, whole, with each amended section REPLACED in place and tagged `[F-nn]` at the amended paragraph (so a reader can diff v2 against the proposal section by section); a header naming the proposal, the rulings of every seat that ruled (paths, astra's marked PARTIAL where it is) and the fold table's path; `## First-gate experiments (L70)` = the proposal's X1–X5 plus every experiment the houses named that the fold takes, each before the chunk it gates; `## Dissents, verbatim`; `## L52 (a)–(d)` re-answered for v2. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-derive-2026-09-21.md` — §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` (≤40 lines: what v2 changed from the proposal, item by item in one line each; which seats ruled round 1 (in full / in part / not); what goes to round 2 and why; the owner items in one line each; evenings to first card and to all seven UNDER v2) → `## Fold table` (`F-nn · item (O1–O8, (a)–(i), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (house, or "proposal kept") · adopted verbatim? yes / no / not taken · why (≤25 words, citing the R1 or R1B hub's file-check row where it decides)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT BUILD`, or two houses disagree on whether a mechanism is correct; each with both houses' positions quoted and the question round 2 must answer; EMPTY = the tribunal closes) → `## OWNER ITEMS (after cards)` (deduplicated, each with who raised it; none a precondition) → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `SETUPS DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch reads its own report first and continues from `## CONTINUE`; v2, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line of the derive report, L71): `SETUPS DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits v2 as the FINAL's input and the build prompts are drafted (chunk C1 first, per v2); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a later day's `grok` / `agy` needs his word — 09-20 R23 ends 2026-09-21 23:59 ET).
