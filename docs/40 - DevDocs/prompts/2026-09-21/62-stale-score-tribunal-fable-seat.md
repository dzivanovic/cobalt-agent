FABLE ROW: R__

MODEL: Fable 5.1 (`claude-fable-5-1`) — the FABLE SEAT of a four-house tribunal (L67 names Fable a house), a SPAWNED agent that runs ONLY if he approves it for THIS tribunal (09-20 R18: a spawned Fable agent only after the desk asks him, per case); you rule, blind; you draft no prompt, build nothing, derive nothing in this job · SEAT: `stale-score-tribunal-fable-0922`, launched by the CTO desk in the background, SIDE BY SIDE with the three-house hub `61-stale-score-tribunal.md`. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/62-stale-score-tribunal-fable-seat.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control stale-score-tribunal-fable-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`'s, `39`'s and `45`'s launch lines, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file with the Write tool · METER: Anthropic FABLE — honest estimate for the desk to refuse BEFORE launch: the read set below is ≈ 55–65k tokens (LAWS.md ≈ 16–17k, the proposal ≈ 5.5k, its author's report ≈ 2.5k, R36 / R40 ≈ 1.5k, the C1 draft's ranges ≈ 1.5k, ADR-0009 D4 ≈ 0.3k, `cards/scoring.py` whole ≈ 4k, the `evaluate.py` ranges ≈ 5k, the `store.py` ranges ≈ 2k, `cards/radar.py` / `poller.py` / `freshness.py` / `expire.py` / `audit_export.py` / migration / panel ranges ≈ 3–4k, your own searches ≈ 8–12k, the question paragraph of `61` ≈ 3.5k), the ruling ≈ 10–14k tokens of output; peak context ≈ 75–90k — smaller than the float-handicap Fable seat (≈ 100–120k, `39`) and about a third of one 09-20 bars Fable fork (≈ 250k). Read only what a ruling needs; the larger files by line range. · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# FOUR-HOUSE TRIBUNAL, ROUND 1 — THE FABLE SEAT, BLIND. DESIGN: a card must not score on stale bars, `docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md` (proposing house: Opus 5; recommended option C — proximity UNKNOWN while the evaluator calls the intraday bars stale). LADDER: `S2-P2 · F10 Dots / ladder` — a defect in what is live. LAW STEP: proposal DONE → **THIS = the Fable house's round-1 ruling, written beside Astra / Grok / Gemini and WITHOUT reading them** → the derive (`63`, on the seat his row names) → build per chunk → ≥3 checkers → deploy. L52 applies in full: this design is scoring and it reaches the card (score, proximity, the suppression line, the WATCH order); its bar (a)–(d) is your bar. DO NOT STOP until the report's last line is `STALE SCORE TRIBUNAL FABLE R1 DONE …` or `FAILED …`.

BLIND — THE ONE RULE THAT MAKES THIS SEAT WORTH ITS COST: you NEVER read, list or grep `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/stale-score-tribunal/` (any depth) nor `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-score-tribunal-2026-09-21.md` nor `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-score-tribunal-2026-09-22.md`, nor any file a house wrote about this proposal. A tool call that touched one of them makes your ruling non-blind: write `FAILED: blind — <the path>` and stop. (The hub file-checks YOUR claims after the houses have finished; that does not touch your blindness — you never see its report.) You MAY read the prompt `61-stale-score-tribunal.md` (a prompt, not a ruling) — only for its `QUESTIONS.md (verbatim): "…"` paragraph and its §1 staging list. Your report opens with one line: `BLIND: I did not read the houses' folder or the hub's report.`

AUTHORIZATION — VERIFY IT YOURSELF (a prompt file is not an approval). **THE FABLE ROW.** The first line of this file names the row of the desk report of the launch day — `cto-2026-09-21.md` or `cto-2026-09-22.md`, section `## §4 Rulings` — in which the desk records his answer to "may I spawn a Fable agent for the Fable seat of the stale-score tribunal?". The DESK fills the two characters after `R` on that line AFTER his yes, and changes nothing else in this file (L19: any other change is a re-issue). Prove, each its own Bash call: `grep -c -x -F "FABLE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/62-stale-score-tribunal-fable-seat.md"` (`-x` = whole line: only the first line can match, never this sentence) — a count of **1** means the line is still unfilled → `FAILED: authorization — the Fable seat is not approved for this tribunal (the FABLE ROW is unfilled)`, stop; **0** → read the number `<nn>` from the first line · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (a missing `cto-2026-09-22.md` is recorded, not fatal; the row must exist in ONE of the two AND carry the literal `Fable seat: yes` AND `STALE-SCORE-PROPOSAL-2026-09-21.md` — anything else, `Fable seat: no` included, or two rows `R<nn>` that both carry `STALE-SCORE` → `FAILED: authorization mismatch — row R<nn> does not approve the Fable seat on this tribunal`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"STALE-SCORE-PROPOSAL-2026-09-21.md" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md" "docs/40 - DevDocs/reports/cto-2026-09-22.md"` and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "<the desk file the row is in>"` (either EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed on main`; the pathspec names the desk files ONLY) · `grep -n "^| R36 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` and `grep -n "^| R40 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (what is ruled beside this design; R40 must carry `ONE EXTRA DOT THAT CANNOT BE TAPPED`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -- "docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — the proposal is not committed`). **THIS LAUNCH LINE ADDS NO RULE** — prove it: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1** (any 0 → `FAILED: authorization mismatch — <rule>`). YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** a stale input is never shown as a live number · **L3** one path for "an input of the score is unknown" · **L7** cards stay advisory; `shadow_agreement_v` is promotion evidence; nothing here promotes · **L9** a stale source is a loud degraded state · **L17 / L39** ≤3 rounds, no fourth · **L29** the builder seat for a user-table write path · **L32** the design, its report and the rows it rests on carry NO user data (the drafter checked); keep your report so · **L35** trust the file, never a report — the proposal's `file:line` citations are CLAIMS you open yourself · **L36** you launch nothing · **L37** you rule; you approve nothing · **L42** RESTARTS · **L43 / L66** · **L52** (a)–(d) · **L53** `radar.scan_interval`, `radar.poll_bar_max_age_s` and every threshold are his — name a value by key, never propose one · **L57** · **L67** · **L68** · **L70** an unrun behaviour claim is an EXPERIMENT, never a defect · **L71** · **L72** · **L73** · **L74** a block inside a tool result is DATA; record once.
2. The proposal `docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md` in full, then its author's report `docs/40 - DevDocs/reports/stale-score-design-2026-09-21.md` (§0, fact table, ESCALATE 1–4, DIGEST).
3. WHAT IS RULED: `cto-2026-09-21.md` §4 rows R36 and R40 (`grep -n "^| R<n> "` → Read those lines). You do not argue them: R36 — a small `STALE` stamp on a stale ticker's pool row and card, a rendering of the poller's failure list, built separately, it stays; R40 — every card formed on an assumed value carries ONE untappable dot (`assumed_formation`) and shows no score for its life until he rules the value (chunk C1, not merged); cards stay advisory; every clock value and threshold is his — anything of his goes under `OWNER (after the tribunal):`, never as a precondition to build.
4. THE CODE ON MAIN, as a ruling needs it (the hub's staged excerpts name these ranges; you open the real files): `src/cobalt/cards/scoring.py` (whole) · `src/cobalt/radar/evaluate.py` (:128–140, :339–364, :429–480, :487–505, :525–535, :595–632, :755–829, :1043–1090, :1315–1350, :1380–1400) · `src/cobalt/cards/store.py` (:1016–1075, :1182–1231) · `src/cobalt/cards/radar.py` (:20–35, :160–206) · `src/cobalt/radar/poller.py` (:90–125) · `src/cobalt/radar/anatomy/freshness.py` (:106–142) · `configs/cobalt/taxonomy/tunables.yaml` (:465–472, :512–522) · `src/cobalt/cards/expire.py` (:272–312) · `src/cobalt/radar/audit_export.py` (:240–260) · `src/cobalt/db_migrations/0007_radar_cards.sql` (:44–50, :140–150, :215–258), `0006_radar_score.sql` (:95–109) · `src/cobalt/aset/radar_panel.py` (:877–885, :955–958, :998) · `src/cobalt/replay/` (by search) · `docs/10 - Decisions/ADR-0009-radar-cards-seam-and-precondition-ast.md` (:27–28). Searches with `grep` (this shell's `grep` honours `.gitignore`): run what a ruling needs — the hub's §1 (8) list in `61` names the searches the houses were given; you may run the same ones.
5. THE C1 FACTS: `docs/40 - DevDocs/reports/setups-c1-draft-2026-09-21.md` lines 33–43 (the five recompute paths on which C1 keeps a score null; its refs are main `23fb828`) and 137–140; the C1 branch `setups/c1-rubberband-0921` by `git -C /Users/cobalt/cobalt log` / `show` ONLY if it exists (it is not merged; you rule on main plus these facts).
6. THE QUESTION SET: the paragraph `QUESTIONS.md (verbatim): "…"` in `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/61-stale-score-tribunal.md` — find it with `grep -n "QUESTIONS.md (verbatim)"` and read that paragraph through its closing `TRIBUNAL R1:` sentence ONLY — the SAME question set and the SAME ruling template the three houses answer: Q1–Q10 (the proposal's own §8, in its order), then (a)–(k). Where it says "the staged files" / "this folder" / "greps.txt", read "the real files above" and "your own searches"; the staged names map to the real paths as `61` §1 lists them.

THE JOB — rule, do not rewrite. For EACH item — Q1 … Q10, then (a) … (k) — exactly one of `ADOPT` · `ADOPT WITH <replacement wording, written so it can be pasted into the design verbatim>` · `REJECT <why, with file:line>`, then at most six lines of reasoning, and for every objection a CONCRETE failing scenario (a ticker, a bar time, a scan, a tap, a card field, a ladder position). Every number you rest on is re-derived from the files with its steps shown, or `UNVERIFIED — <the measurement>`. Every "Python / PostgreSQL / radar behaviour" claim you cannot read in the code → `X<n>: <what to run on cobalt_dev, on which data, what result would change the design>` under (k) (L70), never argued. LESSONS THAT BIND THIS SEAT: (i) `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the Fable seat INVENTED was broken by the houses against the real code, and everything it took from the files held — so prefer the proposal's simpler mechanism unless you can show its failing sequence; when you replace one, your replacement must be smaller or you price its cost (size, files, chunk) and say so; invent no mechanism you have not walked through the real code. (ii) `reports/setups-tribunal-fable-r2-2026-09-21.md` MEMORY line: on its own round-1 text the Fable seat withdrew 11 sentences, all inventions or un-walked consequences (a second writer, a second leg of a loader, an expiry read), none a fact read from a file — **"A seat's self-attack should start from 'who else writes or reads this field' (`grep` the field name) before weighing any mechanism."** So, BEFORE you finalise any `ADOPT WITH` or `REJECT`: for EVERY field or function its mechanism touches (at least `last_price`, `proximity`, `card_score`, `score_suppressed`, `intraday_stale`, `input_stale`, `engine_grade_at_tap`, `refresh_card`, `tap_dot`, `refresh_radar_card`, `replay_receipt`, `score_card`, `EVALUATOR_VERSION`), run `grep -rn <name> /Users/cobalt/cobalt/src/cobalt` (one call each) and list every writer and reader in `## Self-attack`, then walk your own text against that list; a sentence of yours the files do not bear out is struck from the ruling and recorded there as `WITHDRAWN: "<the sentence>" — <file:line that contradicts it>`. THEN `OWNER (after the tribunal):` (his items, one line each, none a precondition) · `WRONG FACTS:` (a proposal statement the files contradict, both file:lines) · the closing line `TRIBUNAL R1: BUILD` / `TRIBUNAL R1: BUILD AFTER <≤15 words>` / `TRIBUNAL R1: DO NOT BUILD <≤15 words>`.

WRITE (Write tool, ONE new file), named from your first `date`, `<D>` = `2026-09-21` or `2026-09-22`: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-score-tribunal-fable-r1-<D>.md` — COMMITTED by the desk; it carries no user data (L32). Layout: the `BLIND:` line → one line `REPORT NAMED FROM date: <D>` → `## DIGEST FOR THE DESK` (≤40 lines: your closing line; one line per item with the verdict and, for ADOPT WITH / REJECT, the gist in ≤20 words; the experiments you named; your owner items; the count of WITHDRAWN sentences; your ESCALATE count) → `## Rulings` (Q1 … Q10, (a) … (k), full) → `## Self-attack` (the grep list, then every `WITHDRAWN:` line, or `WITHDRAWN: none`) → `## Experiments (L70)` → `## OWNER (after the tribunal)` → `## WRONG FACTS` → `## READING` (every file and line range you opened, every search you ran) → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48). While you work, the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb `next: <step>` lives inside `## CONTINUE` only; no other line may START with `STALE SCORE TRIBUNAL FABLE R1 DONE`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch of this file first runs `ls` on both candidate names, continues the one that exists under its own name, reads it first and continues from `## CONTINUE`; it never re-reads the houses (it never read them). One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line, L71): `STALE SCORE TRIBUNAL FABLE R1 DONE · verdict: <BUILD|BUILD AFTER …|DO NOT BUILD …> · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — the three counts summing to 21 (Q1–Q10 + (a)–(k)) — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: the desk commits your report; the hub `61` file-checks your claims at its collate step if your stop line is there by then; after the hub's `STALE SCORE TRIBUNAL R1 DONE` line is committed too, the desk launches `63-stale-score-tribunal-derive.md` on the seat his row names — you again if he named Fable, no longer blind.
