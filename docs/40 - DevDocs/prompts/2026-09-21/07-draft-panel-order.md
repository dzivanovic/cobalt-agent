MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `panel-order-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/07-draft-panel-order.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control panel-order-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `prompts/2026-09-20/22-draft-bars-chunk-2-fix.md` and today's `01` / `05`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the BUILD prompt and the BUILD-CHECK prompt for ONE front-end change: on `/radar` the CARD LADDER is ALWAYS on top, the POOL VIEW below it. LADDER: `S2 · Radar panel (§5 host)` (`docs/00 - Project/SPRINT-LADDER-v0_1.md:455`). LAW STEP (L67): the design decision IS his ruling (`cto-2026-09-21.md` §4 R3, 06:26 ET, his words: *"my top should always be trade radar and the cards that should be always on top of the page because that's what I'm working on"* … *"the section with the three working on environments is below the trade radar … So I can visually see what the current system is working on and why something is in or out of the radar"*) → **THIS = drafting its build and the ≥3-house check of that build** → after them, not yours: the deploy prompt (evening, L43 / L66, read by another house before it runs). DO NOT STOP until the report's last line is `PANEL ORDER PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): the section `#ladder-layer` (CARD LADDER / TRADE RADAR, its `collapse all` / `top 2` buttons, its `TERMINAL` fold-out) renders FIRST on the radar page, in the desktop page AND the phone frame; the section `#pool-layer` (POOL VIEW: current admitted table, `Departed admitted`, `Never-admitted exclusions`, its refresh status) renders BELOW it, still visible, NOT collapsed, NOT removed, content unchanged. He said he is "not sure that the UI design is appropriate" beyond this — a wider redesign is NOT ordered: no restyle, no new fold-out, no renamed label, no change to what a card or a pool row shows, no change to any API route, payload, refresh interval or store call. If the files show that the order cannot change without one of those, that is an ESCALATE in your report, not a decision you take.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** · **L19** · **L28** no vault/DB write path is touched — the panel READS · **L29** front-end only with no write path = Sonnet-eligible (ladder `:455`, `:468`); say in your report whether ANY line the build must touch sits on a write path (the tap / grade POST handlers live in the same package) — if one does, the builder seat becomes Opus 5 · **L35** · **L36** · **L45** tests against the real rendered page, not an invented fixture · **L46 / L54 / L60** own worktree off main, RECOVERY RULE · **L52** nothing here changes scoring, ranking, or what a card SHOWS — order on the page only; the check proves it with empty diffs · **L62 / L63** · **L67** a build is checked by ≥3 houses · **L71** · **L73** · **L74**.
2. The code, on MAIN (Read tool under `/Users/cobalt/cobalt`): `src/cobalt/aset/radar_panel.py` — `render_pool` (`:834-858`), `render_ladder` (`:995-1043`), `PANEL_JS` (the `refreshPool` / `refreshLadder` functions, which replace `#pool-layer` / `#ladder-layer` BY ID — establish from the file whether anything depends on DOM ORDER: a `firstElementChild`, a `:first-child` / sibling CSS selector in `PANEL_CSS`, a sticky header, the `<1150 px` detail-column rule, the phone frame), `render_radar_page` (`:1123-1126`, where the two layers are concatenated pool-first), `render_failed_page`; `src/cobalt/aset/web.py` (the `/radar` routes and the `/api/radar/pool` / ladder fragment routes — READ ONLY, expected untouched). Tests: `tests/cobalt/test_radar_panel.py`, `tests/cobalt/test_radar_panel_cards.py` (find every assertion that fixes the order or would break on it). DevDocs: `docs/40 - DevDocs/cobalt/aset/radar_panel.md`, `web.md`.
3. THE SHAPES TO COPY, byte for byte wherever the same sentence serves: the offline build prompt `prompts/2026-09-21/02-bars-chunk-2-fix-r3.md` (launch line, AUTHORIZATION, UNATTENDED RULES, RECOVERY RULE, PREFLIGHT, BASELINE, report-append and stop-line discipline — its 16 allow strings + 3 denies + `--add-dir` triplet are his 09-20 R25 approval) and the build-check prompt `prompts/2026-09-21/04-bars-chunk-1a-check-r3.md` with `prompts/2026-09-20/14-bars-chunk-1a-check.md` (a ROUND-1 build check: packet, QUESTIONS, three houses, the gemini file-viewer-only sentence, collation, stop line; the 14 + 3 strings of `08`).
4. The rulings: `docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 R3 (this change) · `cto-2026-09-20.md` §4 R13, R23, R25 (the strings; `Bash(grok *)` / `Bash(agy *)` stand THROUGH MONDAY 2026-09-21 23:59 ET).

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `08-panel-order-build.md` — hub `panel-order-build-0921`, ONE session writes the change AND runs its tests, OFFLINE (`.env` absent, no `cobalt_dev`), seat per item 1's L29 finding (Sonnet 5 if no write-path line is touched; else Opus 5 — say which and why on line 1). THREE bare commands because the worktree does not exist yet — BUT `git worktree add` is NOT among the approved strings: read how `prompts/2026-09-20/10-bars-chunk-1a-build.md` / `11-bars-chunk-2-build.md` created theirs (which command, run by whom — the desk or the hub) and copy THAT, saying which; the `claude --bg …` line carries exactly prompt `02`'s 16 + 3 strings and triplet, byte for byte, NO NEW RULE STRING (any string the build cannot do without is listed under `NEW strings:` in your report for ONE approval list, L62). Branch `s2/panel-order-0921` off main, worktree `/Users/cobalt/cobalt-wt/panel-order`. Body: PREFLIGHT → BASELINE (`uv run pytest -q tests/cobalt tests/taxonomy`, the line recorded) → TEST FIRST: a test on the REAL rendered page (`render_radar_page` with the suite's existing view fixtures, desktop AND `phone_frame=True`) asserting `id="ladder-layer"` occurs BEFORE `id="pool-layer"` and that BOTH are present with the pool's three parts intact (current admitted table, `Departed admitted`, `Never-admitted exclusions`) — RED first, quoted → THE CHANGE, the narrowest form the files allow (expected: the two f-string calls in `render_radar_page` swap; anything more only if item 2 found an order dependency, each named) → any existing assertion that pinned the OLD order is re-pointed, never weakened, each with its diff → DevDoc `radar_panel.md` one sentence → CLOSE: full suite, `git diff --stat main` = only `src/cobalt/aset/radar_panel.py`, the two test files, the DevDoc, the report; EMPTY diffs for `src/cobalt/cards`, `src/cobalt/radar`, `src/cobalt/aset/web.py`, `src/cobalt/aset/store.py`, `configs`; `uv run cobalt jobs restarts main..HEAD` VERBATIM (expect `com.cobalt.aset`; L42). Report `docs/40 - DevDocs/reports/panel-order-build-2026-09-21.md` in the worktree; in-progress last line pinned, breadcrumb under `## CONTINUE`; AUTHORIZATION greps `cto-2026-09-21.md` for row R3 (`-S"my top should always be trade radar"`, committed) and a launch row `R__` the desk fills. STOP LINE: `PANEL ORDER BUILT <tip> | on <main tip> | offline <p>/<f> (<s> skipped; baseline <p>/<f>) | ladder above pool, desktop + phone: proven | pool content unchanged: proven | card/scoring/route paths untouched: empty diff | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `09-panel-order-check.md` — Sonnet 5 hub `panel-order-check-0921`, `14`'s ROUND-1 shape with this build's paths, folder `scratch/tribunal-bars-0920/panel-order-check/` (say in your report whether Grok's approved `--allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)"` string is WHY the folder sits under that name — it is; a new folder root would be a new string), the 14 + 3 strings of `08` byte for byte, DATE GATE (2026-09-22 or later → FAILED), shas read at run time from the build's stop line, targets THREE houses (two answering = continue + `ASK DESK`; astra on METER = continue with two), PREFLIGHT refuses while `03` or `04`'s report ends on the in-progress line (same three houses, the desk staggers). QUESTIONS: is the ladder above the pool in BOTH frames · is any pool content, label, fold-out, route, payload or refresh behaviour changed (`NONE` or `file:line`) · does any JS / CSS rule depend on the old order and now misbehave (name it) · did any assertion get weaker · does anything reach scoring, ranking or what a card shows (L52) · closing line `CHECK: BUILD STANDS` / `BUILD STANDS EXCEPT <items>` / `FIX FIRST <items>` + ` · ready for its deploy prompt: YES|NO`. Collation + hub file-check as `14`. Stop line `PANEL ORDER CHECK DONE · grok: … · gemini: … · astra: … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/panel-order-draft-2026-09-21.md`: §0 Headline ≤5 lines → ORDER DEPENDENCIES found in JS / CSS / tests (`what · file:line · does the swap break it`) → the L29 seat finding → how the worktree gets created and by whom → per prompt: file, size, seat → RULE PROOF (`grep -c -F -e` = 1 per string against `02` for `08`, against `prompts/2026-09-20/08-bars-chunk-e-check.md` for `09`; `NEW strings: NONE` or the list) → `READING:` list → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `PANEL ORDER PROMPTS DRAFTED · prompts: 2 · builder seat: <Sonnet 5|Opus 5> · order dependencies: <n> · new rule strings: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
