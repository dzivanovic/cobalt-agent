MODEL: Opus 5 (`claude-opus-5`) — the PROPOSING house of a four-house tribunal (L67: "ONE house designs and proposes") · SEAT: `setups-design-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/21-propose-setups-design.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-design-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `18` / `19`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool · METER: Anthropic medium-to-large · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PROPOSE THE DESIGN: his SEVEN daily setups evaluable on the radar at DEFAULT values, Rubberband's defect fixed FIRST. LADDER: a LADDER CHANGE ruled by him — S3's detectors pulled forward (`docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 R15, R17, R18). LAW STEP (L67 / L52): **THIS = the proposal one house writes; four houses then rule on it and derive the final version; nothing is built from your file.** DO NOT STOP until the report's last line is `SETUPS DESIGN PROPOSED …` or `FAILED …`.

HIS RULINGS (read the three rows verbatim in the desk report): all definitions at defaults; a missing default is ASSUMED from the cheat sheets and his own trading findings, MARKED assumed, tuned AFTER he has seen cards ("I need to see the cards before I rule on anything"); he rules on NO setup beforehand — so your design may NOT end in a list of owner questions: where a choice is his by law (L53), you pick the conservative default, mark it ASSUMED, and list it in ONE table he reads after cards show. THE SHORT LIST (R18), built first: **Hitchhiker · Backside · Rubberband · Second Chance · Fashionably Late · 9 EMA (Scalp) · VWAP Continuation**; the other six definitions later (design them OUT of scope, but never in a way that must be torn up for them).

THE FACT BASE — read, do not redo: (1) `docs/_inflight/defs-gap-table-2026-09-21.md` (gitignored; Read tool under `/Users/cobalt/cobalt`) — THE THIRTEEN, THE ATOMS, THE DEFAULTS, HOW "ASSUMED" IS SHOWN, BUILD ORDER, ESCALATE E1–E11; (2) `/Users/cobalt/cobalt-wt/rubberband-proof/docs/40 - DevDocs/reports/rubberband-card-proof-2026-09-21.md` — PROVEN by a run: a definition whose `valid_setups` mix countertrend + with_trend never forms (`src/cobalt/radar/evaluate.py:635-645`), `setup_ref` is an order-dependent label not a detection (`:647`), why every test missed it (single-relation fixture; the live-note test never asserts `formed`; a replay with `formations=0` was accepted), a second gate (the def's own day-1 avoid) stands behind it; (3) `docs/10 - Decisions/ADR-0009-radar-cards-seam-and-precondition-ast.md`; (4) the code it cites, on main; (5) his seven definition notes (found through the loader) and their cheat sheets `docs/90 - References/*.pdf`.

INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L2 · L3 · L7** (cards stay advisory beside his hand grade; nothing here promotes) **· L8 · L9 · L10 · L11** (each definition keeps ≥1 human-only variable — name it; gap table E11 says only 3 of 13 carry an explicit one) **· L28** (any write of a definition value or setting goes through the existing load path; the desk edits his notes only on his ruling, L65 — you propose NO edit to his notes; divergences E10 are reported) **· L32** (sheet- and note-derived VALUES are user data: never in the committed design file — see WRITE) **· L45** (tests against the REAL definition shapes) **· L52** (a)–(d) — answer each explicitly **· L53 · L57** (every derived value replayable from stored inputs) **· L67 · L68 · L70** (every "PostgreSQL / pandas / evaluator behaviour" claim you cannot read is a first-gate experiment, not an argument) **· L72 · L73 · L74**.

WHAT THE DESIGN MUST SETTLE — each with the mechanism, the files it touches, what reaches the card, and its tests:
1. **DIRECTION (Rubberband first, its own deployable chunk).** How a definition with setups of both relations gets a direction per INSTANCE: what detects which setup an instance is, from stored data, deterministically; what the card shows; what happens when both or neither qualify (fail-loud, never a guess). Simplest mechanism that is correct; price its cost honestly.
2. **THE FORMATION STAGE**, generalised beyond Extension (gap table E2): the trigger types and stop placements the seven need, as data the definition names, not seven hard-coded paths (anti-rigidity; the other six and future swing / options must extend it without a refactor).
3. **THE DETECTOR GROUPS the seven need**, in build order by setups unlocked per unit of size: Extension lifecycle incl. `reverting` / `backside` (E8 — the registry must stop reporting served-but-never-true) · Range(micro) · levels + sequence · trendline · shared indicators. Each = typed event / atom, inputs from stored i1 + daily + pool data only.
4. **THE INTERPRETER GAPS** (E9: arithmetic, units, `IN cfg(band)`) — only what the seven need.
5. **WARM-UP** (E7): seeding indicators from stored premarket i1 so a 09:35–09:45 setup can evaluate; what that does to "RTH bars only" (`evaluate.py:527`).
6. **9 EMA's CATALYST** (E3): the definition requires a catalyst reference Cobalt has no data for, while its sheet says the setup needs no strong catalyst. Propose ONE default (a human tap field, or the sheet's reading with the gate relaxed and marked ASSUMED) — no owner question.
7. **ASSUMED, VISIBLE**: how an assumed value is marked where he sees it — on the card face and on the settings surface — using what exists (owner badges, `DEFAULT_UNRULED`), and how a value stops being assumed (his later ruling through the normal load path).
8. **WHERE ASSUMED VALUES LIVE** (L32 / L53; gap table digest item 12): not in committed config. Propose the store and the load path.
9. **ACCEPTANCE THAT WOULD HAVE CAUGHT TODAY'S DEFECT**: per setup, a test on the REAL note's shape asserting `formed` on a stored day chosen to form; a replay gate that is RED when a day known to contain the setup forms zero; the registry's `evaluable` flag proven by formation, not by atom presence.
10. **CHUNKS**: an ordered list where EVERY chunk is independently buildable offline in its own worktree, checkable by ≥3 houses and deployable on its own evening (L43 / L66), and unlocks at least one named setup or removes one named defect; chunk 1 = Rubberband can form. For each: size S/M/L, builder seat by L29, what restarts (L42), what it needs from `cobalt_dev`. State plainly how many evenings the seven take at one deploy per evening, and which setups he sees first.
11. **WHAT YOU WOULD USE FROM HIS TRADING STATS** if he pastes them (he offered): the exact figures per setup that would replace a LOW-confidence assumed default — a short list he can ask his Claude chat for in one message.

WRITE (Write tool, new files only):
- `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` — the proposal, COMMITTABLE: no value, quote or wording from his notes or the sheets; it refers to them by `file:line` / PDF page and to assumed values by KEY. Sections 1–11 above, then `## L52 (a)–(d)`, `## What this does NOT do`, `## Open to the tribunal` (the ≤8 points where you are least sure, each with the alternative you rejected and why).
- `/Users/cobalt/cobalt/docs/_inflight/setups-assumed-values-2026-09-21.md` — gitignored companion: per setup, every ASSUMED value by key, the value, source (PDF page or note line, ≤25-word quote), confidence; the ONE table he reads after cards show.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-design-2026-09-21.md` — §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤40 lines (the mechanism of each of 1–9 in one or two sentences; the chunk table; evenings to first card and to all seven) → `## STATS TO ASK HIM FOR` (item 11, ready to paste into chat) → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
RULES: prefer the simpler mechanism and price it; every claim carries `file:line`; "not found" beats a plausible fill; nothing here is built or launched by you. One command per Bash call, bare shape, no pipe, no redirect.
STOP LINE (last non-blank line): `SETUPS DESIGN PROPOSED · setups: 7 · chunks: <n> · evenings to first card: <n> · evenings to all seven: <n> · assumed values: <n> · open to the tribunal: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
