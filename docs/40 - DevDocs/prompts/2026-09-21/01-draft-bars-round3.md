MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `bars-r3-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/01-draft-bars-round3.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control bars-r3-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `09`, `13`, `16`, `19` and `22-draft-bars-chunk-2-fix.md` of 2026-09-20, a subset of the approved line of `06-bars-tribunal-r3.md`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) ROUND 3 — THE LAST — for both bars chunks: chunk 2's ROUND-3 FIX and its ROUND-3 CHECK; chunk 1a's ROUND-3 CHECK (no fix). LADDER: `OFF-LADDER — ruled cto-2026-09-18 R17 / cto-2026-09-19 R10, R15 / cto-2026-09-20 R9`. LAW STEP (L67): design CLOSED → chunk E checked 3 of 3 → both chunks BUILT (09-20 R25) → round 1 checked → fix round 1 BUILT (chunk 2 `d768674`, branch tip `1351da6`; chunk 1a `793f452` + 1b `1404f23`, branch tip `6961ee0`) → ROUND 2 checked Mon 00:14 / 01:04 ET (`cto-2026-09-20.md` §46, §47) → DESK CALLS, both committed: chunk 2 = "a ROUND-3 FIX — the LAST — is the next lawful step" (§46), chunk 1a = "1a's ROUND 3 is a CHECK, not a fix" (§47) → **THIS = drafting those three prompts.** L67 caps a house at THREE rounds: what is unresolved after round 3 goes to Dejan, never to a fourth round — every prompt you draft says so in its collation. DO NOT STOP until the report's last line is `BARS ROUND 3 PROMPTS DRAFTED …` or `FAILED …`.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** · **L19** a changed prompt = a whole new file under a new number, never an edit · **L33** · **L35** a house's claim counts only where the check hub's file-check says HOLDS · **L36** · **L37** · **L46/L60** the fix commits on its OWN branch in its OWN existing worktree · **L52** nothing reaches a card · **L62/L63** · **L67** ≤3 rounds a house; round 3 reads the FOLD only · **L68** · **L70** a claim nobody ran is UNPROVEN, never fixed blind · **L71** · **L72 practice** (a step added to a branch after its check prompt was drafted joins that prompt's question set and packet BEFORE launch — the X14 lesson) · **L73** · **L74**.
2. THE SHAPE TO COPY, byte for byte wherever the same sentence serves: your predecessor's prompt `prompts/2026-09-20/22-draft-bars-chunk-2-fix.md` (its CLASSIFY classes bind you UNCHANGED: FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM; you widen NOTHING) and what it produced: `prompts/2026-09-20/23-bars-chunk-2-fix.md` (ran clean, `BARS CHUNK 2 FIX BUILT d768674 …` — copy its launch line, AUTHORIZATION, UNATTENDED RULES, RECOVERY RULE, THE MUTATION PROOF, the inertness harness, report-append and stop-line discipline), `24-bars-chunk-2-check-r2.md` (the chunk-2 check shape: Codex probe as a PREFLIGHT GATE, astra's addendum) and `21-bars-chunk-1a-check-r2.md` (the chunk-1a check shape), plus its report `reports/bars-2-fix-draft-2026-09-20.md`.
3. THE ROUND-2 RESULTS, whole: `reports/bars-chunk-2-check-r2-2026-09-20.md` and `reports/bars-chunk-1a-check-r2-2026-09-20.md` — §0, every per-FIX-row section, the hub's own file-check rows, the FINAL `## ESCALATE` (12 and 5 items), every `ASK DESK`. The houses' full texts and packets: `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/chunk-2-check-r2/` (`astra-check-r2.md`, `grok-check-r2.md`, `gemini-check-r2.md`, `classify.md`, `QUESTIONS-R2.md`, `QUESTIONS-R2-ASTRA.md`) and `…/chunk-1a-check-r2/` (`astra-check-r2.md`, `grok-check-r2.md`, `classify.md`, `QUESTIONS-R2.md`).
4. THE DESK'S CALLS AND ANSWERS that bind you: `reports/cto-2026-09-20.md` §35 (the seam call), §46 (chunk 2: what HOLDS, the round-3 fix's three points), §47 (chunk 1a: the answers to the check hub's three `ASK DESK` — gemini is re-asked, the stripped-whitespace copy is ACCEPTED as disclosed, X14 joins the question set) and §4 R9, R19–R23, R25, R45, R47, R48.
5. WHAT WAS BUILT: worktree `/Users/cobalt/cobalt-wt/bars-chunk-2` (branch `bars/chunk-2-0920`, tip `1351da6`; Read tool + `git -C /Users/cobalt/cobalt log -p 47ef9af..bars/chunk-2-0920`), worktree `/Users/cobalt/cobalt-wt/bars-chunk-1a` (branch `bars/chunk-1a-0920`, tip `6961ee0`; the whole fix = `589d40f..1404f23`), their reports there, the prompts that built them (`23`, `20`, `25`), the spec `docs/30 - Design/BARS-LIFECYCLE-FINAL-2026-09-20.md` §3.1–§3.4, §8, and main's `src/cobalt/radar/poller.py` (the inertness baseline).
6. DATE GATE (his 09-20 R23: `Bash(grok *)` / `Bash(agy *)` stand THROUGH MONDAY 2026-09-21 23:59 ET) on BOTH checks you draft: `date` is 2026-09-22 or later → FAILED, first PREFLIGHT row. Say in your report, in one line, the latest wall-clock time each check can start and still finish today, from round 2's measured durations (28 min and 49 min).

CHUNK 2 — THE THREE POINTS OF THE ROUND-3 FIX (all astra's, all HOLD in the check hub's own file-check, none seen by grok or gemini — classify them from the FILES like every other row; the desk's reading is a pointer, not a verdict): **(1) test 1's `rth_stale` case never goes stale** (newest = NOW−1m, age 60 s < 180 — a copy of `clean`), so plain-table stale planting is unproven; astra says the production change at `poller.py:201` is CORRECT — the gap is the PROOF. FIX = the fixture really goes stale in BOTH pollers (main's own and the built one, the builder's inertness harness), by MUTATION PROOF: the test fails when the stale branch is removed. **(2) `poller.py:245` pops a carried `no_partition` record on a plain-table success where main has no such pop** — FIX = the pop is gated so a plain (unpartitioned) table stays main's, call for call; the inertness harness proves it on a path that CARRIES such a record. **(3) `setdefault` at `:222` / `:240` keeps a carried `detail=None`, so the in-memory range of a carried `no_partition` record is lost** — astra disputes O4's class for THIS part only ("requires neither a persisted fourth key nor a card-schema change"); grok + gemini agreed with the class. FIX = astra's replacement wording, taken VERBATIM from `astra-check-r2.md` (quote it with `file:line` in your CLASSIFY row; desk lesson 09-20: adopt a house's wording, invent nothing); PERSISTENCE of the range and any CARD SURFACE for it stay **O4 — his, OWNER ITEM, not built**. Line numbers above are round 2's: re-read them on tip `1351da6` and correct them in your rows. Anything else in round 2's `## ESCALATE` is classified too; the fix widens NOTHING beyond FIX rows that HOLD. O1–O4, O6 are copied verbatim as OWNER ITEMs with what he would be ruling; you decide none.

CHUNK 1a — ROUND 3 IS A CHECK, NOT A FIX: round 2 closed 12 of 13 FIX rows with 0 new defects; the ONE open row is X9 vs X14 — X9's text said the `w<WW>` child-name shape stays, X14 (fix 1b, 09-20 R48, prompt `25`) replaced it with the generator's `bars_p_<YYYYMMDD>` on the desk's seam call (§35), but round 2's `classify.md` had no X14 row and prompt `25` was not staged, so both houses reported X14 as outside the rows. Round 3 stages what was missing: an X14 row in `classify.md`, prompt `25`, the seam call (§35 + R48, quoted), the X14 hunks named in the question set with ONE question the houses answer from reads alone — does X14, as built, SUPERSEDE X9's naming sentence so that the repo holds ONE child-name shape, generated in one place and admitted by one pattern (L3)? GEMINI IS RE-ASKED: its round-2 failure line is verbatim in `bars-chunk-1a-check-r2-2026-09-20.md` ESCALATE 3 (`jetski: no output produced — a tool required the "command" permission that headless mode cannot prompt for`) — read how `24` / `15` / `08` launched `agy` when gemini DID answer, find the difference from `21`'s launch, and say it in your report; if the files do not show one, write `UNPROVEN` (L70) and keep `21`'s shape. This check TARGETS three houses: two answer → it continues and writes `ASK DESK`; astra on METER there = continue with two.

WRITE (Write tool, new files only, all under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `02-bars-chunk-2-fix-r3.md` — Opus 5 hub `bars-chunk-2-fix-r3-0921`, ONE session, OFFLINE exactly as prompt `23`. TWO bare commands, no `worktree add`: `cd /Users/cobalt/cobalt-wt/bars-chunk-2` then the `claude --bg …` line carrying **exactly the 16 allow strings + 3 denies + `--add-dir` triplet of prompt `23`'s launch line, byte for byte — only the prompt path and the remote-control name change; NO NEW RULE STRING**. Body = prompt `23`'s shape: PREFLIGHT (branch `bars/chunk-2-0920`, clean, tip read and recorded as `<round-2 tip>`, `.env` absent), BASELINE = fix round 1's own offline count read from its stop line, ONE step per FIX row (failing test or MUTATION PROOF first, then the change, one commit each, explicit-path `git add`), the inertness re-proof against main's own poller on EVERY path (clean, fetch failure, write failure, RTH stale that REALLY goes stale, a raising bounds read, a carried `no_partition` record on a plain table), `upsert_bars` still byte-identical to main, the HARD BOUNDARY re-checked, empty diffs for `src/cobalt/cards`, `src/cobalt/radar/evaluate.py`, scoring; report APPENDED as `# FIX ROUND 2` to the chunk-2 report, nothing above rewritten; in-progress last line pinned, breadcrumb under `## CONTINUE` (L71); AUTHORIZATION greps `cto-2026-09-20.md` for the committed desk call (`-S"a ROUND-3 FIX — the LAST — is the next lawful step"`) and a launch row `R__` in `cto-2026-09-21.md` the desk fills. STOP LINE: prompt `23`'s exact shape with `on <round-2 tip>` and `round-2` in place of `round-1`.
- `03-bars-chunk-2-check-r3.md` — Sonnet 5 hub `bars-chunk-2-check-r3-0921`, `24`'s shape, folder `scratch/tribunal-bars-0920/chunk-2-check-r3/`, the 14 + 3 strings of `08` byte for byte, DATE GATE, shas read at run time from the fix stop line, **astra REQUIRED** (Codex probe = PREFLIGHT GATE: METER → `FAILED PREFLIGHT: astra on METER — chunk 2's round 3 needs three houses`, nothing launched). The houses read ONLY round 3's fold (the fix diff, the touched files, main's poller, round 2's verdict table, your CLASSIFY). THE PATH CORRECTION (round-2 hub ESCALATE 9a): the spec copy astra's addendum names lives under `../chunk-e-check/spec-final-s3.md`, NOT `../chunk-2-check/` — verify with `ls` and write the path that exists. Collation ends the matter: no fourth round; a row still open after this check is listed for Dejan verbatim. Stop line = `24`'s shape with `R3` in place of `R2`.
- `04-bars-chunk-1a-check-r3.md` — Sonnet 5 hub `bars-chunk-1a-check-r3-0921`, `21`'s shape, folder `scratch/tribunal-bars-0920/chunk-1a-check-r3/`, the same 14 + 3 strings, DATE GATE, range `589d40f..1404f23` unchanged, the staging described above. Stop line = `21`'s shape with `R3` in place of `R2` and one added field `· X9/X14 one shape: <n> of <n> YES`. The two checks use the SAME three houses: `04` says in its PREFLIGHT that it runs when `03` is NOT running (the desk staggers them; `04` does not depend on the chunk-2 fix and may run FIRST).
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/bars-r3-draft-2026-09-21.md`: §0 Headline ≤5 lines → the CLASSIFY table (chunk 2, then chunk 1a: `finding · house(s) · the check hub's file-check verdict · YOUR file:line read · class`) → the gemini launch difference (or UNPROVEN) → per prompt: file, size, seat → RULE PROOF (`grep -c -F` = 1 per string against prompt `23` for `02`, against `08` for `03` and `04`; `NEW strings: NONE` or the list) → the latest-start line of item 6 → `READING:` list → `## ESCALATE` (every OWNER ITEM verbatim, every `ASK DESK`) → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `BARS ROUND 3 PROMPTS DRAFTED · prompts: 3 · new rule strings: <n> · chunk 2 FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · chunk 1a staged: X14 row + prompt 25 + seam call · gemini launch difference: <found|UNPROVEN> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
