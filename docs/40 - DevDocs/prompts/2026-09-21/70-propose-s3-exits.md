MODEL: Opus 5 (`claude-opus-5`) — DESIGN-PROPOSAL seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: proposer `s3-exits-design-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/70-propose-s3-exits.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control s3-exits-design-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `21` / `36` / `49` / `59`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the TWO files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PROPOSE (do NOT build, do NOT draft build prompts) the design of S3's EXIT SIDE: **F11 fill recompute + fill / exit capture** and **F22 trade-note auto-creation** on RADAR cards. LADDER: `S3-P1 · F11` + `S3-P2 · F22` (`docs/00 - Project/SPRINT-LADDER-v0_1.md` `## S3`, 09-24 → 10-07). LAW STEP (L67): ONE house proposes → the four houses rule and derive → build → ≥3 checkers → deploy. **THIS = the proposal**, run beside tonight's builds (L72) so the tribunal can sit the day S3 opens. NOT in this proposal: F14 (the DRC note — its template is designed BY HIM in a sitting he has not yet held, `cto-2026-09-21.md` R48 / R52) and F15 (S3-P3); name only the seams they will need from F11 / F22. DO NOT STOP until the report's last line is `S3 EXITS PROPOSED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING):
- The ladder's S3 table rows F11 and F22, its S3 smoke and its "Code prompts owed" S3-P1 / S3-P2 — acceptance tests verbatim; the 09-04 soft ATTESTATION check as written there.
- **Stop-override authority** (`cto-2026-09-20.md` R38, "B with your addition"; ladder `### Status 2026-09-21` + the "Rulings/inputs owed before S3" line): when he edits the stop, HIS stop is the plan until he resets it; Cobalt's structural stop stays visible beside it; the gap is recorded on the trade for the DRC / Friday review.
- CLAUDE.md absolute boundaries: Cobalt NEVER touches a trading platform and NEVER executes or automates a trade — a fill is what HE tells Cobalt (a tap, a typed price, a DM line), never read from DAS.
- L7 / L8: nothing here flips a human-fed value to engine-fed; realized R and any statistic carries its n. L28: the trade note is a vault write — create-if-absent, marker-bounded units, versioned, human wins. L32: legs, fills, notes are USER data (`"user"` schema, `user_id NOT NULL`). L53: every threshold of his (the 27 % / >20 % drift wording, ATR scaling, expiry) is HIS value — if it is not already in the ladder, the Charter, his settings or the code, it is an ESCALATE marked ASSUMED, never your number. R24 (`cto-2026-09-21.md`): his trade log is never pass / fail truth.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** one path — the ASET sheet's existing fill-recompute is either REUSED or named as the copy that dies · **L7 / L8** · **L9** · **L10** · **L11** · **L28** · **L29** every write path = Opus 5 floor; say which chunks are write paths · **L32** · **L35** · **L40** one expert role per side effect · **L42** RESTARTS derived · **L45** real-artifact fixtures · **L52** say plainly whether anything proposed reaches scoring, ranking or what a card's score shows (if yes, the bar (a)–(d)) · **L57** every derived number (drift, R, running shares) stored with its inputs · **L66 / L68** · **L67** · **L70** · **L71** · **L74**.
2. The ladder `## S3` (`:586-604`), `### Status 2026-09-21` (`:472-503`), the "Rulings/inputs owed before S3" paragraph (`:538` region) · `docs/00 - Project/MVP-CHARTER-v0_2.md` F11, F22, §5 (the panel), §7 metrics, §12 acceptance · `docs/30 - Design/TRADE-RADAR-CARD-MOCK-v0_1` (mock #6 stop editing, the FILLED / IN-TRADE states, the ½ · ⅓ · flat strip) · `docs/10 - Decisions/` ADR-0008 (two-layer data model) and ADR-0009 (cards).
3. THE CODE, on MAIN (Read tool under `/Users/cobalt/cobalt`), every claim with `file:line`: `src/cobalt/cards/` (models, `store.py` — the card state machine as built: which states exist, which transitions, `tap_dot`, what FILLED means today) · `src/cobalt/aset/` (`web.py`, `store.py`, the sheet's fill-recompute and sizing — the ladder says radar cards fill "via the existing fill-recompute" in S2; `radar_panel.py` for where an ARMED / IN-TRADE card renders) · `src/cobalt/vaultwrite/` (`writer.py`: `create_if_absent`, `upsert_unit`, frontmatter site — what F22 can reuse as is) · `src/cobalt/prefill/` and `src/cobalt/daymode/` (the `day_modes` row the attestation persists on; whether slice-2's trade-note version exists — the ladder asks exactly that) · `src/cobalt/notify/` (whether an INBOUND DM path exists at all for "TSLA filled 372.82 10"; if only outbound exists, say so — an inbound listener is a new resident, L2 watcher standard) · `src/cobalt/db_migrations/` + `placement` (next free migration number; NOTE the unmerged branches that also add migrations: `bars/chunk-2-0920` `0012`, the seven-setups build's possible `0013` — L68 seam) · `src/cobalt/replay/` (what counterfactual R already computes from bars, for the realized-R seam).
4. Branches that touch the same files and land first (`git -C /Users/cobalt/cobalt log main..<branch> --name-only`): `setups/seven-0921` (building from tonight; `cards/store.py`, `evaluate.py`, `scoring.py`) · the stale-score design (`docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md`, same functions) · `s2/stale-marker-0921` (`radar_panel.py`). Say what S3-P1 must rebase over and what it must not re-decide.

WRITE (Write tool, exactly two new files):
- `/Users/cobalt/cobalt/docs/30 - Design/S3-EXITS-PROPOSAL-2026-09-21.md`, ≤350 lines: (1) FACT BASE, every row `claim · file:line · PROVEN | UNPROVEN`: the card state machine as built, what the sheet's fill path does today, what vaultwrite offers, whether an inbound DM path exists, what the mock shows; (2) THE STATE MACHINE proposed — TRIGGERED → FILLED | PASS | EXPIRED → legs → CLOSED, each transition with its actor (always HIM), its stored row, its inputs (L57); (3) THE `legs` TABLE — columns, side (`"user"`), keys, the estimated / confirmed flag, compounding off running shares, the rounding rule for ⅓ (a number of shares is HIS broker's reality — propose, mark ASSUMED); (4) DRIFT WARNING — the formula scaled to ATR, which ATR (the stored input), where the thresholds live as HIS values; (5) STOP OVERRIDE per R38 — fields, what renders beside what, what the DRC later reads; (6) THE DM LINE — grammar, the inbound path or its absence, the fail-loud reply on a line it cannot parse; the panel tap path FIRST if the DM path needs a new resident; (7) F22 TRADE NOTE — the note's path and frontmatter from HIS existing trade-note template / dataview table (find them under `/Users/cobalt/Vault/Think/1 - Trading/` and the templates folder; cite paths, copy NO personal content), units and ids, the update-in-place rule for legs, what happens when he edited the note (L28 human wins); (8) ATTESTATION soft check as the ladder words it; (9) what is deliberately NOT changed (scoring, ranking, admission, sizing keys) and the L52 finding in one sentence; (10) CHUNKS in dependency order, seat per L29, migrations, RESTARTS expected (a guess is marked as one), which chunks can build in parallel, an HONEST build estimate in hours; (11) TEST PLAN on real artifacts (L45) incl. the Charter's two acceptance tests verbatim; (12) OPEN QUESTIONS for the tribunal, ≤12, and OWNER ITEMS (values or behaviours only HE can give), ≤8, each one sentence he could answer A / B.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-design-2026-09-21.md`: §0 Headline ≤5 lines → the fact table's UNPROVEN rows → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤40 lines** (the desk reads this, not the proposal — `cto-2026-09-21.md` R7: what is proposed in plain words, what exists already, chunks + hours, what is HIS, what the tribunal must settle). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line of the report): `S3 EXITS PROPOSED · chunks: <n> · write-path chunks: <n> · migrations: <n> · inbound DM path exists: <yes|no> · trade-note writer exists: <yes|no|partial> · reaches scoring: <yes|no> · build estimate: <n> h · open to the tribunal: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
