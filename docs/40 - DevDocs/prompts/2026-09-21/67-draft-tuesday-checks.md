MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `tuesday-checks-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/67-draft-tuesday-checks.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control tuesday-checks-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `50` / `64`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) TWO CODE-CHECK prompts for TUESDAY 2026-09-22, both on the NEW checker seats of his R46 / R49. OFF-LADDER for the ops one (`cto-2026-09-20.md` R41 / R42; `cto-2026-09-21.md` R43); LADDER `S2 · Radar panel (§5 host)` + `F8` for the stale stamp (R36). LAW STEP (L67): each is the ≥3-house CHECK of a finished build, before its deploy. DO NOT STOP until the report's last line is `TUESDAY CHECK PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): `cto-2026-09-21.md` §4 **R46** (18:05) + LAWS **L67** `[amended 2026-09-21 18:05]`: a CODE CHECK of a finished build seats the OpenAI house as **Sol** (`gpt-5.6-sol`) and the Anthropic house as **Opus 5**, beside Grok and Gemini; Astra and Fable sit on DESIGN work only; three checkers required, the meter is a preflight (L47; Sol draws on the Codex allowance). **R49** (18:14, "Approved") = the two seat strings, used BYTE FOR BYTE: `Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)` · `Bash(claude -p --model claude-opus-5 *)`. **R39**: `Bash(grok *)` / `Bash(agy *)` run through 2026-09-22 23:59 ET — both prompts run TUESDAY and refuse on any later date. **R47**: nothing deploys alone — each checked branch joins Tuesday evening's ONE stacked deploy, whose prompt is drafted by another seat after the checks; you draft NO deploy prompt and no deploy-prompt read (`53` / `54` are superseded).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L19** a re-issue is a whole file · **L33** reviewer seats answer from reads; hashing and file-checks belong to the hub · **L35** the hub file-checks every claim · **L36** · **L37** · **L44** the hub STAGES every input inside each seat's allowed workspace · **L47** · **L62 / L63** · **L67** as amended today · **L70** · **L71** · **L74**.
2. THE SHAPE TO COPY, byte for byte wherever the same sentence serves: `prompts/2026-09-21/66-setups-one-check.md` — the FIRST check prompt on the R49 seats (its line 1 rule strings, its seat launch shapes for Sol and Opus 5, the 45-minute clock, `date` at every completion notice, the task-stop rule, the hub writes what each seat rules, ` < /dev/null` after a `codex exec` prompt). Your two launch lines carry EXACTLY `66`'s rule strings — no string added, none changed; prove it per string.
3. FOR THE STALE STAMP: `prompts/2026-09-21/52-stale-marker-check.md` in full (drafted before R46, astra TARGETED — its questions, packet layout, staging folder and AUTHORIZATION stay; only the seats, the seat launch shapes and the rule strings change) · `reports/stale-marker-draft-2026-09-21.md` (DIGEST, ESCALATE 1, 2, 5) · `docs/30 - Design/STALE-MARKER-PROPOSAL-2026-09-21.md` §7 · `51-stale-marker-build.md` for the build's stop-line shape (`STALE MARKER BUILT …`; the build launches TONIGHT after `deploy-2026-09-21b` in worktree `/Users/cobalt/cobalt-wt/stale-marker`, branch `s2/stale-marker-0921` — if its report does not exist yet when you read, say so; the check prompt's AUTHORIZATION reads the stop line at ITS launch, never now).
4. FOR OPS 6a: branch `ops/2026-09-21` (worktree `/Users/cobalt/cobalt-wt/ops-0921`, tip `f6aa4d0`, UNMERGED): `git -C /Users/cobalt/cobalt log main..ops/2026-09-21`, the branch's `docs/40 - DevDocs/reports/ops-2026-09-21.md` (Read it under the worktree; §0, item 6a, ESCALATE 1–10), commit `227325c` (6a: the Cobalt key store — the vault file — joins restic's include set, red → green, suite 2209/0) and the other five commits (`e2db970` · `6193cd7` · `5e2ae4c` · `9dfc9a0` · `f6aa4d0`) by `git show --stat`; `prompts/2026-09-21/06-ops-0921.md` for what was ordered; `cto-2026-09-21.md` §13 17:42 bullet for the desk's L35 and the carried items (the `jobs restarts` classifier cannot classify `configs/cobalt/backup.yaml`; `cli.md:461-465` vs bars chunk 1a).

WRITE (Write tool, NEW files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `68-stale-marker-check.md` — Sonnet 5 hub `stale-marker-check-0922`; `52` RE-ISSUED WHOLE on the R49 seats (Grok + Gemini + Sol + Opus 5, THREE required); every question of `52` kept verbatim; line 1 says `52` is superseded by this file and why (R46 / R49). PREFLIGHT refuses after 2026-09-22 23:59 ET and unless the build's report ends `STALE MARKER BUILT …`. AUTHORIZATION greps committed rows R36, R39, R46, R49 and a launch row `R__` the desk fills (in `cto-2026-09-22.md` or `cto-2026-09-21.md`). Stop line: `STALE MARKER CHECK DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the stacked deploy: <n> of <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `69-ops-6a-check.md` — Sonnet 5 hub `ops-6a-check-0922`; the same seats, clock and staging shape; packet = the six commits' diffs + the branch report, staged by the hub. QUESTIONS, each answered `HOLDS | DOES NOT HOLD | UNPROVEN` with `file:line`: (1) does `227325c` put exactly the vault key-store file into restic's include set and nothing else (no secret VALUE in config, diff, test or report — L4; path by name only); (2) is the red → green test a real proof on the real config shape (L45) or a tautology; (3) can the change make `com.cobalt.backup` (21:40, one-shot) fail, skip, or back up less than before — name the failure mode or `NONE`; (4) restore side: does anything in the restore path now need the key store to read the key store (a circular restore) — L70 if not checkable from reads; (5) the other five commits: docs / `.gitignore` / BACKLOG only, no runtime reader (L42) — `NONE` or `file:line`; (6) what the stacked deploy must carry for this branch: RESTARTS as DERIVED (the classifier gap named, not judged), any migration (`NONE` expected), the `cli.md` seam with `bars/chunk-1a-0920`; (7) anything weaker, removed or skipped in the suite. AUTHORIZATION greps committed rows R43 (of `cto-2026-09-21.md`), R39, R46, R49 and a launch row `R__`. Stop line: `OPS 6A CHECK DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the stacked deploy: <yes|no> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- STAGGER: both hubs use the same four houses — each prompt's PREFLIGHT refuses while the other's report exists without a stop line (copy `66`'s / `52`'s stagger gate if one exists; else write one keyed on the other report's LAST line, L71).
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/tuesday-checks-draft-2026-09-21.md`: §0 Headline ≤5 lines → per prompt: file, size, seat → RULE PROOF (`grep -c -F -e` per string against `66`'s line 1, each **1**) → **`NEW approvals:`** (`none` expected; anything else as ONE list the desk can show him verbatim) → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤30 lines** (the desk reads this, not the prompts — `cto-2026-09-21.md` R7). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `TUESDAY CHECK PROMPTS DRAFTED · prompts: 2 · new rule strings: <n> · questions: <n> + <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
