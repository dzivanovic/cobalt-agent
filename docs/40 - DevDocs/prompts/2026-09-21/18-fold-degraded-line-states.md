MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt fold `degraded-line-fold-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/18-fold-degraded-line-states.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control degraded-line-fold-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `13`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you re-issue exactly the files named below with the Read then Write tools · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# FOLD ONE RULING into the drafted, NOT-YET-LAUNCHED prompts `14-degraded-line-build.md` and `15-degraded-line-check.md` (and `16` / `17` ONLY where a sentence of theirs names the states) — a full re-issue of each touched file (L19), nothing else changed. LADDER: `S2 · Radar panel (§5 host)`. LAW STEP (L67): his ruling = the design decision; this is still the drafting step of `13`. DO NOT STOP until the report's last line is `DEGRADED LINE STATES FOLDED …` or `FAILED …`.

THE RULING (`docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 R11, 08:21 ET, his "a" to the A/B your predecessor raised as ESCALATE 1 of `reports/degraded-line-draft-2026-09-21.md`): the red line above the ladder carries **DEGRADED, STALE and REFRESH FAILED ONLY. RETAINED PRIOR-DAY DATA is NOT carried** — it is amber, normal every night until the first scan; the red line must always mean "do not trust what you see right now" and must be ABSENT (hidden, no space) while the ONLY pool-view state is RETAINED. The pool view's own banners, RETAINED included, stay byte-identical.

INDEX CARD:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L3** (still ONE computation — the line filters the SAME `view.pool.banners` by `level`, it computes nothing) · **L9** · **L19** full re-issue · **L35** · **L45** · **L52** · **L62** NO rule string changes — the launch lines of all four prompts stay byte-identical · **L71** · **L74**.
2. `reports/degraded-line-draft-2026-09-21.md` (`## WHERE THE DEGRADED STATE LIVES`, `## THE REFRESH FINDING`, ESCALATE 1) · the four prompts `14`–`17` · the code on main `src/cobalt/aset/radar_panel.py` (`BannerView.level`, `build_pool_view` `:577-599`, `render_pool` `:837-851`, `refreshPool` `:1103-1116`).

WHAT CHANGES, and you establish the narrowest form from the files: (a) the server renderer of the line filters `view.pool.banners` to `level in {degraded, stale}`; (b) the JS mirror must NOT copy a retained banner out of the pool layer — establish from `render_pool`'s real markup how a banner's level is distinguishable in the DOM (a class per level? if the markup carries NO level marker, say so: the narrowest fix that keeps `render_pool` byte-identical wins; if none exists, a level class on the pool banner is a pool-markup change → name it, make it the ONE permitted pool diff, and tell the check to look at exactly that), REFRESH FAILED still mirrored; (c) TESTS: `14`'s T1 gains a case "RETAINED only → the line is hidden, identical to healthy" and "RETAINED + DEGRADED → the line carries DEGRADED only", in both frames and through the refresh-fragment sequence; the parity test (d) compares the line with the FILTERED banners; (d) `14`'s stop line gains ` | retained not carried: proven`; its standing ESCALATE (i) "which states" is REMOVED and replaced by a one-line citation of R11; AUTHORIZATION also greps R11 (`-S"DEGRADED, STALE and REFRESH FAILED only"`, committed); (e) `15` QUESTIONS gain: "is RETAINED ever shown in the red line, server-rendered OR mirrored (`file:line`)" and, if (b) needed a pool-markup change, "is that the ONLY pool diff"; (f) `16` / `17`: touch ONLY a sentence that names the carried states or `14`'s stop-line fields they parse; if none, leave the file byte-identical and say so.

WRITE: the re-issued files in place under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/` (Read each first, then Write the whole file) and your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/degraded-line-fold-2026-09-21.md`: §0 Headline ≤5 lines → THE DOM FINDING for (b) (`file:line`, the mechanism chosen, whether `render_pool` stays byte-identical) → per file: touched or byte-identical, bytes before / after, the sentences changed (old → new, ≤25 words each) → RULE PROOF (`grep -c -F -e` of each file's whole `--allowedTools … --add-dir /Users/cobalt/cobalt-wt` segment against `git -C /Users/cobalt/cobalt show HEAD:"docs/40 - DevDocs/prompts/2026-09-21/13-draft-degraded-line.md"`-era drafts is not possible — the drafts are uncommitted; instead prove each launch line against the SAME approved source the draft report's RULE PROOF names, count 1 per string) → `## DIGEST FOR THE DESK` ≤15 lines → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `DEGRADED LINE STATES FOLDED · files re-issued: <n> · render_pool byte-identical: <yes|no — the one class> · new rule strings: 0 · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
