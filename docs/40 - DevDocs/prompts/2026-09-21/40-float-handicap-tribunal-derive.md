DERIVE ROW: R30

MODEL: the DERIVE SEAT that row names — Fable 5.1 (`claude-fable-5-1`) when he approved the Fable seat (then this is the Fable seat's SECOND JOB, as on the bars and setups tribunals), else the seat he named with his "no"; you derive the version the round-1 rulings produce; you build, launch and commit nothing · SEAT: `float-handicap-tribunal-derive-0921`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `38`; the Fable seat `39` ONLY if he approved it). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/40-float-handicap-tribunal-derive.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control float-handicap-tribunal-derive-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` — THE DESK replaces the value of `--model` (and ONLY that value) with the model id the row names after `derive seat:`; it must be a Claude model id, since this file is launched by `claude --bg` (a non-Anthropic derive seat cannot run this file: the desk drafts another). The allow and deny strings are the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `24` and `39`, 09-20 R13 — nothing new · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic, the derive seat's house — honest estimate: the hub report `38` ≈ 12–20k tokens, the raw rulings of the houses that ruled ≈ 12–20k, the Fable round-1 report ≈ 6k (only if that seat ruled), the proposal ≈ 7.5k, its author's report ≈ 2k, LAWS.md ≈ 25k, code re-opened only where a fold turns on it ≈ 5–15k; v2 ≈ 9–12k tokens of output; peak context ≈ 90–120k · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v2 of the FLOAT HANDICAP DESIGN from the proposal and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`. LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini, Astra via hub `38`; Fable via `39` if he approved that seat) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (empty → the tribunal closes and v2 is the FINAL's input) → build per chunk → ≥3 checkers → deploy. L52's bar (a)–(d) is the bar v2 must meet. DO NOT STOP until the report's last line is `FLOAT HANDICAP DERIVED v2 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The first line of this file names the row of `cto-2026-09-21.md` `## §4 Rulings 2026-09-21` in which the desk records his answer to the Fable-seat question for THIS tribunal — yes, or no and the seat he names for the derive. The DESK fills the two characters after `R` on that line and the `--model` value, and changes nothing else in this file (L19: any other change is a re-issue). The row must carry, as literal text: `FLOAT-HANDICAP-PROPOSAL-2026-09-21.md`, `Fable seat: yes` or `Fable seat: no`, and `derive seat: <model id>`. Each its own Bash call: `grep -c -x -F "DERIVE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/40-float-handicap-tribunal-derive.md"` (`-x` = whole line: only the first line can match) — **1** → `FAILED: authorization — the DERIVE ROW is unfilled; the desk has not recorded his answer on the Fable seat`, stop; **0** → read `<nn>` from the first line · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (the three literals above must all be present — any missing → `FAILED: authorization mismatch — row R<nn> does not name the Fable seat's status and the derive seat for this tribunal`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — row R<nn> names <id>; this session runs <id>`, stop. **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-2026-09-21.md"` (the LAST NON-BLANK line must start `FLOAT HANDICAP TRIBUNAL R1 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"FLOAT HANDICAP TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/float-handicap-tribunal-2026-09-21.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · ONLY when the row says `Fable seat: yes`: `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r1-2026-09-21.md"` (must start `FLOAT HANDICAP TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Fable round 1 not done — last line: <line verbatim>`) and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"FLOAT HANDICAP TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r1-2026-09-21.md"` (EMPTY → `FAILED: refused — the Fable round-1 stop line is not committed on main`); when the row says `Fable seat: no`, the tribunal is the three houses and no Fable report is read · **ASTRA WAS REQUIRED** (`reports/float-handicap-tribunal-draft-2026-09-21.md`, the L47 finding): the hub's stop line must show `astra: TRIBUNAL R1: …` (ruled in full) or `astra: PARTIAL <k> of 17 — …` with k ≥ 1 (ruled in part) — anything else (`SKIPPED — METER at probe`, `METER`, `HARNESS`, `TIMEOUT`, `NO RULING LINE`) → `FAILED: astra has not ruled — the desk relaunches 38 for astra or brings him the question` — **EXCEPT under his R13 of 2026-09-22 (`cto-2026-09-22.md` §4, "A", 09:43 ET: this week's design tribunals run on Grok · Gemini · Fable without Astra, the Codex meter being out until Sat 09-26 06:47 ET): an astra `METER` / `SKIPPED — METER` mark is then RECORDED, not a refusal — derive from the seats that ruled, mark every question only Astra would have answered `ASTRA PENDING (R13)`, and name in §0 that Astra reads this v2 when its meter returns.** [re-issued by the desk 2026-09-22 09:5x] Gemini or Grok not having ruled does NOT refuse the derive: the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L3 · L7** (cards advisory; a pool-membership change goes through shadow; nothing promotes) **· L9 · L10 · L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L28 · L32** (v2 and your report are COMMITTED: no value of his) **· L35 · L36 · L37** (you derive; a SPLIT between houses is carried, never settled by vote) **· L42 · L45 · L52** (a)–(d) **· L53 · L57 · L61 · L65 · L67 · L70 · L71 · L72 · L73 · L74**.
2. THE ROUND-1 RULINGS, whole: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-2026-09-21.md` — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/float-handicap-tribunal/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line; `astra-ruling.md` if astra ruled in full, else `astra-ruling.partial.md` — a PARTIAL astra ruling is counted ONLY for the items its first line lists as ruled; an item it did not reach is not ruled by astra (the hub's copies are redacted; the raw file is what "verbatim" means — you may READ the raw file, but you never copy a value span out of it) · the Fable round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r1-2026-09-21.md`, ONLY if the row says `Fable seat: yes`.
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` in full; its author's report `reports/float-handicap-design-2026-09-21.md` (DIGEST, ESCALATE 1); `cto-2026-09-21.md` §4 rows R28 and R29 (what is ruled: a soft penalty, never an exclusion; the group by float OR cap, the combinator a key; the size and shape his, read AFTER the tribunal).
4. Only where a fold turns on it: the real file the hub's file-check cites (code on main, migrations, ADR-0009, the setups proposal's chunk table) — read to check, never to copy.

THE RULES OF THE DERIVE — the bars tribunal's lesson (`topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into v2 word for word, or it is not taken. You may choose BETWEEN houses' wordings; you do not blend them, edit them or improve them. A wording whose supporting claim is `DOES NOT HOLD` in the hub's file-check is NOT taken — say so in the fold table. (A Fable-seat wording has no hub file-check: open the file it cites yourself before taking it, and say so.)
- **Prefer the simpler mechanism.** Where two houses offer different mechanisms for one item, take the smaller one when both meet L52, and write the other's cost and trade-off as an OWNER ITEM (after the tribunal) — unless the two disagree on whether a mechanism is CORRECT, which is `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in v2's experiment list, placed before the chunk it gates, with the result that would change the design — never an argument in v2's text, never a defect.
- **Invent nothing.** v2 adds no mechanism, number, rule, key or chunk that neither the proposal nor a house's ruling contains. A gap no ruling fills stays a named gap (an experiment, an owner item, or a round-2 item) — never your filler.
- **Nothing of his before build** (R28 (c)): the thresholds, the factor, the missing rule, the combinator and the mode are his values, read by him AFTER the tribunal; nothing in v2 may make a chunk's BUILD depend on one of them (tests use constructed blocks, L69); a house that wrote one as a precondition to build is carried as an OWNER ITEM (after the tribunal) and named in `## ESCALATE`. What his values gate is the shadow → live flip, which is his approve by design.
- **The author's ESCALATE 1** (ADR-0009 D4 against `ladder_order`): v2 places its reconciliation — ADR text or code, and which chunk — exactly as the rulings decide it; a split on it is `## NEEDS ROUND 2`, never your call.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT BUILD` that v2 does not follow goes into v2's `## Dissents, verbatim` word for word, with its house and item.
- **NO VALUE OF HIS in either file** (L32): no threshold, factor or number from R28 / R29 or from the author's DIGEST worked example. A house's text that carries one is taken with that span replaced by `[user data: <KEY>]`; a house's proposed VALUE for one of his keys is recorded as `handicap.<key>: value proposed by <house> — scratch/tribunal-bars-0920/float-handicap-tribunal/r1/<house>-ruling.md:<line>` (a pointer, never the value), and listed under OWNER ITEMS; count every redaction.

WRITE (Write tool, TWO new files):
- `/Users/cobalt/cobalt/docs/30 - Design/FLOAT-HANDICAP-v2-2026-09-21.md` — the proposal, whole, with each amended section REPLACED in place and tagged `[F-nn]` at the amended paragraph (so a reader can diff v2 against the proposal section by section); a header naming the proposal, the rulings of every seat that ruled (paths, astra's marked PARTIAL where it is, the Fable seat's status from row R<nn>) and the fold table's path; `## First-gate experiments (L70)` = the proposal's dry-run (§7) plus every experiment the houses named that the fold takes, each before the chunk it gates; `## Dissents, verbatim`; `## L52 (a)–(d)` re-answered for v2. Committable: no value of his.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-derive-2026-09-21.md` — §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` (≤40 lines: what v2 changed from the proposal, item by item in one line each; which seats ruled round 1 (in full / in part / not / not approved); what goes to round 2 and why; the owner items in one line each; evenings to a marked name on `/radar` and to full effect UNDER v2) → `## Fold table` (`F-nn · item (O1–O6, (a)–(k), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (house, or "proposal kept") · adopted verbatim? yes / no / not taken · why (≤25 words, citing the hub's file-check row where it decides)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT BUILD`, or two houses disagree on whether a mechanism is correct; each with both houses' positions quoted and the question round 2 must answer; EMPTY = the tribunal closes) → `## OWNER ITEMS (after the tribunal)` (deduplicated, each with who raised it; none a precondition to build) → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `FLOAT HANDICAP DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch reads its own report first and continues from `## CONTINUE`; v2, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line of the derive report, L71): `FLOAT HANDICAP DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits v2 as the FINAL's input and the build prompts are drafted (H1 first, per v2); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a later day's `grok` / `agy` needs his word — 09-20 R23 ends 2026-09-21 23:59 ET).
