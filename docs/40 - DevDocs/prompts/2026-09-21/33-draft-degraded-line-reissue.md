MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `degraded-line-reissue-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/33-draft-degraded-line-reissue.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control degraded-line-reissue-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `13` / `22` / `26` / `28`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. **Copy shapes, do not re-invent; the chain you draft must be finished and read by a house before 19:30 ET tonight.**

# DRAFT (do NOT launch) THREE prompts so that TONIGHT'S degraded-line deploy can run after this morning's hotfix moved main outside docs. LADDER: `S2 · Radar panel (§5 host)`. LAW STEP (L67 / L68): the build is BUILT and CHECKED 3 of 3 (`c183ed6`), its deploy prompt `16` was READ 3 of 3 and folded (`cto-2026-09-21.md` §4 R19) → **THIS = drafting (a) the rebase + suite run that is the L68 seam proof on the tree that actually lands, (b) `16` RE-ISSUED whole (L19) against that run's stop line, (c) ONE round of a second-house read of the re-issue.** DO NOT STOP until the report's last line is `DEGRADED LINE REISSUE PROMPTS DRAFTED …` or `FAILED …`.

WHY (read `reports/page-bars-hotfix-draft-2026-09-21.md` `## ESCALATE` item 1, and `cto-2026-09-21.md` §11 the 10:56 and 11:47 bullets): the hotfix `s2/page-bars-hotfix-0921` LANDED on main at 11:47 ET (`deploy-2026-09-21h`, code `961970c` + DevDoc `64447b3`; `git diff --stat d87f3bd main -- src tests` = `src/cobalt/aset/radar_panel.py` +13 −1 · `tests/cobalt/test_radar_panel.py` +113). `16-degraded-line-deploy.md` as written REFUSES: its STEP-1(b) docs-only gate (`git diff --stat <cut> main -- . ':(exclude)docs'` must print nothing) and its STEP-3.1 code identity against `<code tip>` both fail now. Branch `s2/degraded-line-0921` = `c183ed6` (code + tests) · `df0011a` (DevDoc) · `5c31876` (report), merge-base with main `d87f3bd`, worktree `/Users/cobalt/cobalt-wt/degraded-line` clean. The hotfix drafter measured `CONFLICT EXPECTED: no` (every hunk gap > 30 lines, same three files) — RE-PROVE it from `git show`, do not trust it.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): R10 (slim red line above the ladder, this evening) · R11 (the line carries DEGRADED, STALE, REFRESH FAILED only; RETAINED PRIOR-DAY DATA not carried) · R12 (his "approved" of `16`'s command list, the three branch-named strings `Bash(git -C /Users/cobalt/cobalt merge --ff-only s2/degraded-line-0921)` · `Bash(git -C /Users/cobalt/cobalt-wt/degraded-line rebase main)` · `Bash(git -C /Users/cobalt/cobalt-wt/degraded-line rebase --abort)`) · R19 (the desk's launch row with the nine folds — ALL NINE STAY, verbatim) · R21 (the hotfix: a per-ticker bars poll failure renders the page with a `BARS POLL FAILED` degraded banner naming the tickers, through the pool view's existing banner mechanism). NO change to `src/` by this chain: the rebase run changes NO source line. THE SEAM QUESTION you must answer from the files with `file:line`, on the combined tree as you can read it (`git show main:<path>` beside `git show c183ed6`): does the red line above the ladder CARRY the hotfix's `BARS POLL FAILED` state once both are in one tree (the hotfix drafter expected yes because the line mirrors the pool banners)? If YES → the rebase run adds ONE test (tests only, in `tests/cobalt/test_radar_panel.py`, real-shape `poll_failures` row, L45) proving it on the rebased tree, RED-then-GREEN not required (it should be green at once; RED = `ESCALATE`, the run still finishes and the desk decides). If NO or NOT KNOWN → no test is added; it is `ESCALATE 1` for the desk with the smallest remedy, and the deploy of the line as checked is NOT held by it (the banner still shows in the pool section, L9 holds).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1** · **L3** · **L9** · **L19** a re-issue is the WHOLE prompt, never a patch list · **L35** · **L36** · **L42** RESTARTS re-derived on the rebased range · **L43 / L66** tonight's window 20:00–21:00 ET, aset AND radar down before the merge (unchanged from `16`) · **L45** · **L46 / L54 / L60** own worktree, commit clean before stopping, RECOVERY RULE · **L62 / L63** every command in the launch allowlist, a denial = FAILED · **L67** the re-issued deploy prompt is read by at least one other house before it runs: TARGET grok + gemini, ONE round; astra `NOT ASKED — its window is on the setups tribunal (27)`, say so · **L68** the gate is the offline suite on the tree that lands = this rebase run; with-DB suite: say from `16` / `14` what the build ran and keep the same scope, never less · **L70** · **L71** · **L73** no step dropped · **L74**.
2. THE SHAPES TO COPY byte for byte wherever the same sentence serves: `prompts/2026-09-21/16-degraded-line-deploy.md` AS FOLDED (the thing you re-issue — read it end to end, keep every gate, every fold, every rule string) · `prompts/2026-09-21/14-degraded-line-build.md` (the offline build hub's shape, 16 + 3 strings of 09-20 R25, RECOVERY RULE, CLOSE) · `prompts/2026-09-21/32-review-page-bars-hotfix-deploy.md` (ONE round, grok + gemini only, 14 + 3 strings, Gemini's improved sentence: file viewer only, the failure named, `greps.txt` opened second) · `reports/degraded-line-deploy-review-2026-09-21.md` (the nine folds and why) · `reports/deploy-2026-09-21h.md` (what the hotfix deploy left on main: tips, tag).
3. The code: `git -C /Users/cobalt/cobalt show main:src/cobalt/aset/radar_panel.py` beside `git -C /Users/cobalt/cobalt show c183ed6` (and `961970c`), tests `tests/cobalt/test_radar_panel.py`, DevDoc `docs/40 - DevDocs/cobalt/aset/radar_panel.md`.

WRITE (Write tool, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `34-degraded-line-rebase.md` (NEW) — Sonnet 5 hub `degraded-line-rebase-0921`, `14`'s shape, OFFLINE, cwd / worktree `/Users/cobalt/cobalt-wt/degraded-line`, branch `s2/degraded-line-0921`. Its launch allowlist = `14`'s 16 + 3 strings PLUS the two R12 worktree strings (`rebase main`, `rebase --abort`) and NOTHING else — if the run needs any further string (for example a `rebase --continue`), you do NOT add it: a conflict = `rebase --abort` + `FAILED: rebase conflict — <files>`; list under `NEW strings:` anything you could not avoid. STEPS: PREFLIGHT (long-form `git status` clean, branch exact, main tip recorded, TIME GATE: refuses at or after 19:00 ET) → `rebase main` → prove the rebased branch's diff against main touches EXACTLY the branch's own paths and that `git range-diff`-equivalent reading holds (patch content of the code commit unchanged: compare `git show --stat` and the hunks; say how with the allowed strings) → the offline suite, SAME command and scope as `14` ran, result beside the baselines (main after the hotfix: 2208/0 per `page-bars-hotfix-build-2026-09-21.md`; the branch before the rebase: 2208/0 — state what the combined number should be and why) → the ONE seam test if the seam answer above is YES (explicit `git add <path>`, one commit `test(radar): degraded line carries the bars poll failure (seam, L68)`) → `cobalt jobs restarts main..HEAD` or `14`'s equivalent → RESTARTS line → report `reports/degraded-line-rebase-2026-09-21.md` committed in the worktree with explicit paths. AUTHORIZATION greps `cto-2026-09-21.md` for R12 and a launch row `R__` the desk fills. STOP LINE: `DEGRADED LINE REBASED <tip> | on <main tip> | cut <new cut> | code tip <new code tip> | offline <p>/<f> (baselines main <p>/<f>, branch <p>/<f>) | code patch unchanged by the rebase: proven | seam test: <added green|added RED|not added> | RESTARTS: <list> | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `16-degraded-line-deploy.md` (RE-ISSUED WHOLE, in place, L19 — precedent: `25` re-issued by `26` today). Every gate, all nine folds of R19, every rule string UNCHANGED. What changes, and only this: its AUTHORIZATION also requires `34`'s stop line (`DEGRADED LINE REBASED …`, `offline … /0`, `code patch unchanged by the rebase: proven`) committed, and the review `35`'s stop line with `launch blockers that HOLD: 0` or the folds named in a NEW desk launch row `R__` (keep R12 / R19 as they are); its `<cut>` / `<code tip>` / identity gates read the NEW values FROM `34`'s stop line instead of literals where you can, so a further docs-only move of main does not refuse it again; STEP-1(b)'s docs-only gate runs against the NEW cut; if the branch is already rebased on main's tip at 20:02 the prompt's own `rebase main` is a no-op and must not be treated as an error; the extra seam-test commit (tests only) is inside the identity check. Head the file with one line `RE-ISSUED 2026-09-21 <time> after the page-bars hotfix (deploy-2026-09-21h) — supersedes the 09:15 issue; diff of intent: <≤3 lines>`. Window, hard clock 20:25, tag, report path `reports/deploy-2026-09-21b.md`, stop line `DEGRADED LINE DEPLOY DONE|FAILED …` — all unchanged. Prove in your report, by `grep -c -F -e`, that every rule string of the old issue (read it FIRST from `git -C /Users/cobalt/cobalt show HEAD:"docs/40 - DevDocs/prompts/2026-09-21/16-degraded-line-deploy.md"`) is in the new one and that no new string entered.
- `35-review-degraded-line-deploy-r2.md` (NEW) — Sonnet 5 hub `degraded-line-deploy-review-r2-0921`, `32`'s shape and strings (14 + 3), ONE round, grok + gemini only, folder `scratch/tribunal-bars-0920/degraded-line-deploy-review-r2/`, packet = the re-issued `16` · a pre-computed word diff old-vs-new `16` · `34`'s report and the rebased diff · round 1's verdict table (`degraded-line-deploy-review-2026-09-21.md`) · `greps.txt`. STAGGER GATES: refuses while `setups-tribunal-r1b-0921` (`reports/setups-tribunal-r1b-2026-09-21.md`) has no stop line (same Gemini seat) and until `34`'s stop line is committed; TIME GATE: refuses at or after 19:30 ET (`FAILED: window` — the desk then takes the floor question to him). QUESTIONS: does the re-issue drop or weaken any gate or fold of the old issue · do the identity gates now read the right tips and still refuse a tree that is NOT the proven one · can the no-op rebase, the seam-test commit or a docs-only move of main after `34` make it refuse or, worse, pass wrongly · is the rollback path still ONE path and correct on the new history · RESTARTS still aset + radar handled per L66. Stop line `DEGRADED LINE DEPLOY REVIEW R2 DONE · grok: … · gemini: … · houses that read it: <n> of 2 · launch blockers that HOLD: <n> · folds proposed: <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/degraded-line-reissue-draft-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (the order the desk launches in, with clocks) → THE SEAM ANSWER (`file:line`, YES / NO / NOT KNOWN) → `CONFLICT EXPECTED:` line re-proven → WHAT CHANGED IN `16` (every changed sentence, old → new) → RULE PROOF (`grep -c -F -e` whole span = 1: `34` vs `14` plus the two R12 strings · `16` new vs old · `35` vs `32`) → **`NEW strings:`** (expected: none) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `DEGRADED LINE REISSUE PROMPTS DRAFTED · prompts: 3 · seam: <yes|no|not known> · conflict expected: <yes|no> · 16 rule strings unchanged: <proven|NOT> · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
