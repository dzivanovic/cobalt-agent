MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `setups-tribunal-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-tribunal-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `01` / `05` / `07` / `10` / `13` / `21`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-1 prompts of the FOUR-HOUSE TRIBUNAL (L67) on `docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md`. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R15 / R17 / R18` (S3's detectors pulled forward: his seven daily setups — Hitchhiker, Backside, Rubberband, Second Chance, Fashionably Late, 9 EMA Scalp, VWAP Continuation — evaluable on the radar at ASSUMED defaults; he rules on no setup before cards show). LAW STEP: proposal DONE (Opus, `reports/setups-design-2026-09-21.md`) → **THIS = drafting its tribunal** → tribunal rules and derives the FINAL → build per chunk → ≥3 checkers → deploy. L52 binds: the design reaches the card; its bar (a)–(d) is the tribunal's bar. DO NOT STOP until the report's last line is `SETUPS TRIBUNAL PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): (1) the tribunal = Astra, Grok, Gemini + the Fable seat; ≤3 rounds per house while meters allow, never below two houses (L67). (2) The Fable seat is a SPAWNED Fable agent, approved by him 09:14 ET (`cto-2026-09-21.md` §4 R20, "yes you may spawn") — BLIND to the three houses' folder in round 1; the desk itself rules nothing and derives nothing. (3) The derive step (FINAL from the proposal + the four rulings) is the same spawned Fable seat's second job, launched by the desk only after BOTH round-1 stop lines; lessons that bind it, from `topics/cto-desk.md` 2026-09-20 "THE FABLE SEAT ON A TRIBUNAL": take a house's replacement wording VERBATIM, prefer the simpler mechanism and price its cost as an owner item, push every "not checkable from reads" claim into a first-gate experiment (L70), invent nothing. (4) Owner items are COLLECTED, never asked mid-run; R15 / R17 stand: nothing in the FINAL may require a per-setup ruling from him before cards show — a house that wants one writes it as "after cards". (5) `Bash(grok *)` / `Bash(agy *)` stand THROUGH TODAY 23:59 ET (09-20 R23) → DATE GATE in every house-running prompt (2026-09-22 or later → FAILED). (6) User data (L32): `docs/_inflight/setups-assumed-values-2026-09-21.md` and `docs/_inflight/defs-gap-table-2026-09-21.md` are gitignored and are READ by every house (L44, L17 privacy default) but never copied into a committed file; staged copies live only under `scratch/` in `~/cobalt-wt/agy-trial` (say whether `scratch/` is ignored there — `git -C /Users/cobalt/cobalt-wt/agy-trial` is NOT in your strings; read its `.gitignore` with the Read tool).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L7** cards stay advisory under the shadow · **L17 / L39** ≤3 rounds, no fourth · **L19** · **L32** · **L33** house launcher profiles; headless Grok / agy answer from reads alone, the HUB stages every input inside the allowed workspace · **L35** · **L36** · **L37** the hub takes no side on a split · **L44** · **L45** · **L47** Codex meter probed at PREFLIGHT; a tribunal that REQUIRES astra fails closed on METER, one that targets it continues with two and writes `ASK DESK` — say which this is and why (design reaching the card → expected: REQUIRED) · **L52** · **L62 / L63** · **L67** · **L70** · **L71** · **L72** this tribunal holds no other lane · **L73** · **L74**.
2. THE SHAPES TO COPY, byte for byte wherever the same sentence serves: `prompts/2026-09-20/04-bars-tribunal.md` + `04-packet/` (round 1: the Sonnet hub in `~/cobalt-wt/agy-trial`, its 14 allow + 3 deny strings = his 09-20 R13 approval, the packet staging, the per-house launch commands, the ruling template, the stop line with counts) · `05-bars-tribunal-r2.md` (how a later round reuses the SAME strings by staging into a subfolder the Write tool creates — no new `mkdir` rule) · today's `09-panel-order-check.md` / `17-review-degraded-line-deploy.md` (the newest clean shape of the same 14 + 3 strings, folders under `scratch/tribunal-bars-0920/`) · `cto-2026-09-20.md` §7–§12 for how the Fable seat ran beside the hub (then a fork; now a spawned agent with its own launch line).
3. THE INPUT: `docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` in full (its "Open to the tribunal" list of 8 is the core of the question set) · `reports/setups-design-2026-09-21.md` (DIGEST, ESCALATE 1–7) · `docs/_inflight/setups-assumed-values-2026-09-21.md` · `docs/_inflight/defs-gap-table-2026-09-21.md` · `~/cobalt-wt/rubberband-proof/docs/40 - DevDocs/reports/rubberband-card-proof-2026-09-21.md` · `docs/10 - Decisions/` ADR-0009 · `cto-2026-09-21.md` §4 R13, R15, R17, R18, R20.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `23-setups-tribunal.md` — Sonnet 5 hub `setups-tribunal-0921`, cwd `~/cobalt-wt/agy-trial`, `04`'s shape, the 14 + 3 strings byte for byte, folder `scratch/tribunal-bars-0920/setups-tribunal/r1/` (Write-created), DATE GATE, Codex PREFLIGHT per your L47 finding. QUESTION SET for each house — ruling per item `ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>`: the proposal's 8 open items, plus: (a) direction from anatomy vs `relation` — is the root-cause reading right against `evaluate.py` and the live notes · (b) the mirrored-frame side binding — any detector or level where price → −price is NOT an equivalence (name it) · (c) L52 (a)–(d), item by item: every number traceable or marked modelled/ASSUMED and degrading the score · ONE ranking authority · the seam a real artifact · auditable by another house · (d) ASSUMED visibility and `assumed_formation` score suppression — can an assumed value reach a score or a size silently · (e) the store (`Assumed Defaults.md`, `source: assumed`, hole-fill merge) vs L28 / L32 / L65 — and the desk-writes-it question of ESCALATE 7 · (f) acceptance gates (a)–(e) of the DIGEST — would they have caught the Rubberband miss; what still passes green while forming nothing · (g) premarket EMA seeding and the RTH-only session objects — does any existing number move · (h) chunk order and size C1–C7, the first-gate experiments X1–X5, scan latency at 2× evaluation · (i) what is NOT checkable from reads → named as an experiment (L70) · closing line per house `TRIBUNAL R1: BUILD | BUILD AFTER <≤15 words> | DO NOT BUILD <≤15 words>`. The hub file-checks every claim a house makes (`## Checked against the files`, HOLDS / DOES NOT HOLD / UNVERIFIABLE FROM READS), takes no side (L37), collects `## OWNER ITEMS (after cards)`. Report `reports/setups-tribunal-2026-09-21.md`; stop line `SETUPS TRIBUNAL R1 DONE · grok: … · gemini: … · astra: … · houses that ruled: <n> of 3 · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- `24-setups-tribunal-fable-seat.md` — MODEL Fable 5.1 (`claude-fable-5-1`), seat `setups-tribunal-fable-0921`, cwd `~/cobalt-wt/agy-trial`, the seven READ-ONLY strings of THIS file's launch line + the same 3 denies, nothing else; AUTHORIZATION greps `cto-2026-09-21.md` for R20 (`-S"yes you may spawn"`, committed). BLIND: it never reads `scratch/tribunal-bars-0920/setups-tribunal/` nor `reports/setups-tribunal-2026-09-21.md`. Same question set, same ruling template, real files opened by itself. It writes ONE file, `reports/setups-tribunal-fable-r1-2026-09-21.md`, ≤40-line `## DIGEST FOR THE DESK` on top; stop line `SETUPS TRIBUNAL FABLE R1 DONE · verdict: <BUILD|BUILD AFTER …|DO NOT BUILD …> · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — or `FAILED …`. METER line: state an honest token estimate for the read set, so the desk can refuse it before launch.
- `25-setups-tribunal-derive.md` — the SAME Fable seat, second job (`setups-tribunal-derive-0921`), same strings; refuses unless BOTH round-1 stop lines are present and committed. Reads the four rulings + the hub's file-check table; writes `docs/30 - Design/SETUPS-AT-DEFAULTS-v2-2026-09-21.md` (= the proposal + amended sections, dissents carried VERBATIM, no user data) and `reports/setups-tribunal-derive-2026-09-21.md` with a fold table (`item · whose wording · adopted verbatim? · why`), `## NEEDS ROUND 2` (only items where a house said DO NOT BUILD or two houses disagree on a mechanism; empty = the tribunal closes) and `## OWNER ITEMS (after cards)`. Stop line `SETUPS DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-draft-2026-09-21.md`: §0 Headline ≤5 lines → per prompt: file, size, seat → the L47 finding (astra REQUIRED or targeted) → PACKET (every staged file, source path → staged path; which carry user data) → RULE PROOF (`grep -c -F -e` = 1 per string against the approved source line, per prompt) → **`NEW strings:`** (expected: none; anything listed goes to him as ONE list) → `READING:` list → `## ESCALATE` → `## CONTINUE` → last line. ALSO a `## DIGEST FOR THE DESK` of ≤40 lines: what each prompt does step by step, so the desk does not read the three prompts end to end (`cto-2026-09-21.md` R7). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `SETUPS TRIBUNAL PROMPTS DRAFTED · prompts: 3 · astra: <REQUIRED|targeted> · questions: <n> · new rule strings: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
