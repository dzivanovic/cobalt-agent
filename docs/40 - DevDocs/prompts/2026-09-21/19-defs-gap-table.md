MODEL: Opus 5 (`claude-opus-5`) — forensics + design INPUT, reads only (`cto-2026-09-20.md` R16–R18: the desk does no work) · SEAT: `defs-gap-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/19-defs-gap-table.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control defs-gap-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `01` / `05` / `07` / `10` / `13`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file, your report, with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# THE GAP TABLE: what it takes to make ALL THIRTEEN setup definitions EVALUABLE on the radar at DEFAULT values. LADDER: a LADDER CHANGE ruled by him — S3's detectors pulled forward (`docs/40 - DevDocs/reports/cto-2026-09-21.md` §4 R15, 08:29 ET). LAW STEP (L67): **THIS = the fact base a design proposal will be written from; it is NOT the design and builds nothing.** After it, not yours: one house proposes the design → four-house tribunal (L52's bar (a)–(d) applies: it reaches the card) → build → ≥3 checkers → deploy. DO NOT STOP until the report's last line is `DEFS GAP TABLE DONE …` or `FAILED …`.

HIS RULING, verbatim (R15): "13 definitions is the correct number … I don't want to rule on anything. I want 13 default definitions with defaults put in place if the default does not exist for something I want the default that is most … reasonable for when you read the … actual PDF cheat sheet I want the default to be assumed I also want to know that defaults are assumed and we want to start from there and tune them we don't have time to now rule on 13 setups before we saw them firing and running through anything". Why he ruled it: today only ONE definition is evaluable end to end (`docs/10 - Decisions/ADR-0009-radar-cards-seam-and-precondition-ast.md` Consequences: "Only one def is evaluable end-to-end in S2; the others name their missing atoms until S3 detectors land"), the 09-18 replay formed 0 cards in a full day, and "I can't know if I need to tune something if they're not there."

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1** · **L3** · **L7** cards stay advisory, engine beside his hand · **L8** · **L9** · **L10** · **L11** every playbook keeps ≥1 human-only variable — NAME it per definition, never propose computing it · **L32** cheat-sheet-derived values and trade_def content are USER DATA: they live in his vault notes / `"user".trader_settings`, never in the repo — your report may quote them (it is gitignored? — CHECK: `docs/40 - DevDocs/reports/` IS committed; so quote the SOURCE LOCATION and the ≤25-word verbatim line needed to justify a number, nothing longer, and say in §0 that the report carries user-data excerpts so the desk decides where it lives) · **L35** · **L45** · **L52** · **L53** a cap / rank rule / cadence is his, never settled in a config file — an ASSUMED default is marked ASSUMED · **L57** no derived value without stored inputs · **L70** · **L71** · **L74**.
2. The engine: `docs/10 - Decisions/ADR-0009-…md` whole · `src/cobalt/cards/` (the precondition AST, the atom registry, `evaluate.py` — note `:527` RTH bars only — and whatever reports a definition's MISSING atoms) · `src/cobalt/radar/` only as far as the evaluator's inputs go · `configs/cobalt/taxonomy/` · `docs/90 - References/TAXONOMY-DRAFT-v0_8.md` (§0 laws, the definition anatomy).
3. The thirteen definitions as they are LOADED today: find where the loader reads them (vault notes under `/Users/cobalt/Vault/Think/1 - Trading/…` and/or the repo's synthetic one — establish it from the loader, do not guess), read all thirteen.
4. The cheat sheets and education material: `docs/90 - References/*.pdf` (one PDF per setup, plus `back$ide_cheat_sheet.pdf`, `Daily_Stop_Model_Card.pdf`) and `docs/90 - References/assets/` — Read tool, PDFs by page range. LICENSED, local-only: never copied, quoted ≤25 words per number.
5. What data exists to feed a detector: `system.bars` is i1 ONLY since 09-20 R9 (i2 and session segments derived from i1; no premarket in i15/i30), the radar pool's Finviz fields, daily levels / ATR as the card already uses them (`htf_level_proximity`), NO Level 2, NO Time & Sales (tape = the human-only frontier, L11).

WRITE ONE FILE, `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/defs-gap-table-2026-09-21.md`:
- §0 Headline ≤5 lines (how many of 13 evaluable today · how many DISTINCT missing atoms in total · how many of those are computable from data Cobalt already stores · how many defaults are missing · ESCALATE count).
- `## THE THIRTEEN` — one row per definition: `slug · name · source note path · evaluable today Y/N · atoms it names (n) · atoms MISSING (list) · defaults present (n) · defaults MISSING (list) · its human-only variable`.
- `## THE ATOMS` — one row per DISTINCT missing atom: `atom · which definitions need it · what it must compute, in one sentence, from the definition's own words (file:line) · inputs it needs · do those inputs exist in Cobalt today (Y / N / PARTIAL — name the store and the gap) · deterministic from i1 bars + daily data? (Y/N) · rough size S/M/L · anything that needs data Cobalt does NOT have (then it cannot be built now — say so plainly)`. Group atoms that are one detector with different parameters.
- `## THE DEFAULTS` — one row per MISSING default value: `definition · parameter · proposed ASSUMED value · source (PDF file, page, ≤25-word verbatim quote) · confidence HIGH / MEDIUM / LOW · if the source gives no number: say "NO SOURCE NUMBER — judgement", give the most conservative reasonable value and why`. A value already present in his notes / settings is NEVER re-proposed.
- `## HOW "ASSUMED" IS SHOWN` — what exists today that could carry an ASSUMED mark to the card face and to the settings surface (owner badges `COBALT` / `YOU` / `LEDGER` in `"user".radar_cards_v`, ADR-0009 D5; the settings load path, L28) — findings only, `file:line`, no design.
- `## BUILD ORDER A DESIGN COULD USE` — the atoms sorted by (definitions unlocked per unit of size), so the first build unlocks the most setups; which definitions become evaluable after each step; which definitions CANNOT be made evaluable from today's data at all, and why.
- `## WHAT THIS DISPLACES` — read `docs/00 - Project/SPRINT-LADDER-v0_1.md` S3 / S4 sections: which ladder lines already contain these detectors, what S3 work it would run beside or ahead of (facts, no recommendation on dates — scope and dates are his).
- `## DIGEST FOR THE DESK` ≤30 lines · `## READING` · `## ESCALATE` · `## CONTINUE` · last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
RULES: every number carries its source `file:line` or PDF page; "not found" beats a plausible fill (L1); nothing is invented — a definition whose words do not determine an atom's computation is an ESCALATE row, not a guess. One command per Bash call, bare shape, no pipe, no redirect.
STOP LINE (last non-blank line): `DEFS GAP TABLE DONE · definitions: 13 · evaluable today: <n> · distinct missing atoms: <n> · buildable from stored data: <n> · not buildable now: <n> · missing defaults: <n> (HIGH <n> / MEDIUM <n> / LOW <n>) · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
