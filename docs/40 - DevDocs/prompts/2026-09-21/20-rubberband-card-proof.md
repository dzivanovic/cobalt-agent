MODEL: Sonnet 5 (`claude-sonnet-5`) — a PROOF by tests, no production code change, no write path (L29: Sonnet-eligible) · SEAT: hub `rubberband-proof-0921`, launched by the CTO desk in the background, OFFLINE, own worktree. The desk creates the worktree first: `git -C /Users/cobalt/cobalt worktree add -b s2/rubberband-proof-0921 /Users/cobalt/cobalt-wt/rubberband-proof main`, then two bare commands: `cd /Users/cobalt/cobalt-wt/rubberband-proof` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/20-rubberband-card-proof.md' and follow it exactly." --model claude-sonnet-5 --remote-control rubberband-proof-0921 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (byte-identical to the launch segment of today's `08` and `14` = his 09-20 R25 strings — nothing new) · SESSION: fresh · auto mode is NOT set on this line (same as `08` / `14`); never `bypassPermissions`; NO database (`.env` absent in the worktree), NO docker, NO production command, NO `src/` edit, no vault write, no memory-folder write (L58), no agent launch (L36) · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue. A permission denial = `FAILED`, a red test or a mismatch = ESCALATE.

# PROVE OR REFUTE BY A RUN (L70): "the ONE evaluable definition, Rubberband, can never form a card". LADDER: `S2-P2 · F8 Card at precondition` — S2's stop date is 09-23 and its acceptance assumes a card CAN form. LAW STEP: a defect claim from a code read (`docs/_inflight/defs-gap-table-2026-09-21.md` ESCALATE E1, gitignored, Read tool under `/Users/cobalt/cobalt`) is UNPROVEN until someone runs it — THIS is that run. You fix NOTHING. DO NOT STOP until the report's last line is `RUBBERBAND CARD PROOF DONE …` or `FAILED …`.

THE CLAIM: Rubberband's `valid_setups` mix `countertrend` and `with_trend` setups (his note `Rubberband.md:37-41`, found via the definition loader), and the formation step `src/cobalt/radar/evaluate.py:635-645` returns `not_evaluable: Setup(relation)` for a mixed relation — so even with a perfect culminating Extension on RTH bars, no formation and no card. Counter-evidence to weigh: S2-P2's own suite and the dev replay of 09-19 shipped cards "proven" — find how THOSE tests form a card (which definition fixture, which relation) and say why they pass if the claim is true (L45: is the fixture the real definition's shape?).

INDEX CARD: 1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59); binding **L1 · L32** (his note's content is user data: your tests build the definition's SHAPE — the same `valid_setups` relation mix — with neutral names, never his note's text or numbers; the report cites `file:line`, quotes ≤25 words) **· L35 · L45 · L46 / L54 / L60 · L52** (you change nothing that reaches the card) **· L69 · L70 · L71 · L74**. 2. `src/cobalt/radar/evaluate.py` (`:527` RTH only, `:600-700` formation), `src/cobalt/radar/anatomy/registry.py:27-38`, the trade_def loader + `trade_def.py`, the Extension detector `extension.py`, the existing card tests under `tests/cobalt/` (`grep -l "formation\|radar_board\|evaluate"`). 3. The gap table's E1, E8 rows and its `## THE THIRTEEN` Rubberband row.

STEPS: PREFLIGHT (`git status` long form clean on `s2/rubberband-proof-0921`, `.env` absent, `date`) → BASELINE `uv run pytest -q tests/cobalt tests/taxonomy` (line recorded) → READ the path end to end and write the call chain (`file:line` per hop) from "scan produced bars" to "card row published" → TEST A (new file `tests/cobalt/test_rubberband_card_proof.py`): the REAL loader's definition shape with Rubberband's relation mix + a bar series the existing suite already uses to produce a culminating Extension on RTH bars → assert what the evaluator RETURNS, verbatim (formation? card? `not_evaluable` with which reason?) — the test PINS the observed behaviour, whichever it is, with a docstring saying which; TEST B: the same series with a single-relation definition (the shape the existing green tests use) → what differs → TEST C, only if A shows a formation: does anything downstream (dots, score suppression `trail_fit` N/A, publish gate) still stop a card row → COMMIT the test file + your report by explicit path (never `-A`, never `.`) → CLOSE: full suite line, `git diff --stat main...HEAD` = the test file + the report only.
REPORT `docs/40 - DevDocs/reports/rubberband-card-proof-2026-09-21.md` (in the worktree): §0 Headline ≤5 lines (CLAIM HOLDS / DOES NOT HOLD / PARTLY, in one sentence a trader can read) → the call chain → tests A/B/C with the verbatim evaluator output → why the S2-P2 suite and the 09-19 dev replay were green → IF THE CLAIM HOLDS: the narrowest candidate fixes as FACTS for a design (where the relation gate lives, what a per-setup relation would touch, whether it reaches scoring — L52), NO recommendation built → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`. RECOVERY RULE: partial work is wip-committed; a relaunch with the same line resumes from `## CONTINUE`.
STOP LINE (last non-blank line): `RUBBERBAND CARD PROOF DONE · claim: <HOLDS|DOES NOT HOLD|PARTLY> · evaluator says: <≤12 words verbatim> · a card can form today: <yes|no> · offline <p>/<f> (baseline <p>/<f>) · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
