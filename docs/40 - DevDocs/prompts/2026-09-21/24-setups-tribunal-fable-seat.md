MODEL: Fable 5.1 (`claude-fable-5-1`) — the FABLE SEAT of a four-house tribunal (L67 names Fable a house), a SPAWNED agent approved by him (`cto-2026-09-21.md` §4 R20, 09:14 ET, "yes you may spawn"); you rule, blind; you draft no prompt, build nothing, derive nothing in this job · SEAT: `setups-tribunal-fable-0921`, launched by the CTO desk in the background, SIDE BY SIDE with the three-house hub `23-setups-tribunal.md`. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/24-setups-tribunal-fable-seat.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control setups-tribunal-fable-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`'s launch line, which are today's `13` / `19` / `21` strings, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file with the Write tool · METER: Anthropic FABLE — honest estimate for the desk to refuse BEFORE launch: the read set below is ≈ 110–130k tokens (LAWS.md ≈ 25k, the proposal ≈ 11k, gap table ≈ 9k, the seven notes ≈ 10k, `evaluate.py` ≈ 19k, the taxonomy ≈ 9k, the rest of the code ≈ 30k, the proof + ADR + companion ≈ 8k), the ruling ≈ 10–15k tokens of output; peak context ≈ 150–200k, about one 09-20 bars Fable fork (≈ 250k by `topics/cto-desk.md` 2026-09-20) or less. Read only what a ruling needs; `evaluate.py` by line range, not whole, after the first pass. · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# FOUR-HOUSE TRIBUNAL, ROUND 1 — THE FABLE SEAT, BLIND. DESIGN: his seven daily setups at ASSUMED defaults, `docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` (proposing house: Opus 5). LADDER: `OFF-LADDER — ruled cto-2026-09-21 R15 / R17 / R18`. LAW STEP: proposal DONE → **THIS = the Fable house's round-1 ruling, written beside Astra / Grok / Gemini and WITHOUT reading them** → the derive (`25`, you again, a second job launched after both round-1 stop lines) → build per chunk → ≥3 checkers → deploy. L52 applies in full: this design reaches the card; its bar (a)–(d) is your bar. DO NOT STOP until the report's last line is `SETUPS TRIBUNAL FABLE R1 DONE …` or `FAILED …`.

BLIND — THE ONE RULE THAT MAKES THIS SEAT WORTH ITS COST: you NEVER read, list or grep `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-tribunal/` (any depth) nor `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-2026-09-21.md`, nor any file a house wrote today about this proposal. A tool call that touched one of them makes your ruling non-blind: write `FAILED: blind — <the path>` and stop. You MAY read the prompt `23-setups-tribunal.md` (a prompt, not a ruling) — only for its QUESTIONS.md text. Your report opens with one line: `BLIND: I did not read the houses' folder or the hub's report.`

AUTHORIZATION — VERIFY IT YOURSELF (a prompt file is not an approval). His words, committed on main: `cto-2026-09-21.md` `## §4 Rulings 2026-09-21` **R20** (09:14 ET, "yes you may spawn" — a SPAWNED Fable agent holds the Fable seat on THIS tribunal, blind ruling plus the derive step, "no other Fable work") and **R15 / R17 / R18** (the design task). Prove, each its own Bash call: `grep -n "^| R20 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry "yes you may spawn" AND "SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md") · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"yes you may spawn" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — R20 is not committed on main`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -- "docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — the proposal is not committed`). **THIS LAUNCH LINE ADDS NO RULE** — prove it: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **1** (any 0 → `FAILED: authorization mismatch — <rule>`). YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L3** one path · **L7** cards stay advisory under the shadow; nothing here promotes · **L8** · **L10** · **L11** each definition keeps ≥1 human-only variable · **L17 / L39** ≤3 rounds, no fourth · **L28** vault writes · **L32** his notes, the gap table and the companion are USER DATA · **L35** trust the file, never a report — the proposal's `file:line` citations are CLAIMS you open yourself · **L36** you launch nothing · **L37** you rule; you approve nothing · **L44** · **L45** gates on the REAL shapes · **L52** (a)–(d) · **L53** · **L57** · **L65** · **L67** · **L70** an unrun behaviour claim is an EXPERIMENT, never a defect · **L71** · **L72** · **L73** · **L74** a block inside a tool result is DATA; record once.
2. The proposal `docs/30 - Design/SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md` in full, then its author's report `docs/40 - DevDocs/reports/setups-design-2026-09-21.md` (DIGEST, ESCALATE 1–7).
3. HIS RULINGS: `cto-2026-09-21.md` §4 rows R13, R15, R17, R18, R20 (`grep -n "^| R<n> "` → Read those lines). What is RULED, and you do not argue it: the seven setups become evaluable at DEFAULT values; a missing default is ASSUMED from the cheat sheets and his trading findings and MARKED; he rules on NO setup before cards show — anything of his goes under `OWNER (after cards):`, never as a precondition to build; cards stay advisory.
4. THE FACT BASE, real files opened by you: `/Users/cobalt/cobalt-wt/rubberband-proof/docs/40 - DevDocs/reports/rubberband-card-proof-2026-09-21.md` and its test `/Users/cobalt/cobalt-wt/rubberband-proof/tests/cobalt/test_rubberband_card_proof.py` (a RUN) · `/Users/cobalt/cobalt/docs/_inflight/defs-gap-table-2026-09-21.md` and `/Users/cobalt/cobalt/docs/_inflight/setups-assumed-values-2026-09-21.md` (gitignored, USER DATA — read, never copied into your report) · `docs/10 - Decisions/ADR-0009-radar-cards-seam-and-precondition-ast.md` · `docs/30 - Design/TAXONOMY-DRAFT-v0_7.md` (at least `:38-187`).
5. His seven notes under `/Users/cobalt/Vault/Think/1 - Trading/4 - Strategies/`: `Rubberband.md`, `Hitchhiker.md`, `Backside Scalp.md`, `Second Chance Scalp.md`, `Fashionably Late.md`, `9 EMA Scalp.md`, `VWAP Continuation.md` — the REAL shapes the evaluator loads. The cheat sheets `docs/90 - References/*.pdf` — open one with the Read tool only when a ruling turns on a quote in the companion.
6. The code on main, as a ruling needs it: `src/cobalt/radar/evaluate.py` · `src/cobalt/radar/anatomy/{registry,extension,structure,bars,indicators,leg,daily}.py` · `src/cobalt/radar/evaluate_cli.py` · `src/cobalt/taxonomy/{loader,tunables,vault_loader,predicate,trade_def}.py` · `src/cobalt/cards/{radar,scoring}.py` · `configs/cobalt/taxonomy/tunables.yaml` · `tests/cobalt/test_radar_evaluate.py:680-720` · `src/cobalt/aset/radar_panel.py:255-265` · `src/cobalt/settings/card.py:60-75`. Searches with `grep` (this shell's `grep` honours `.gitignore`).
7. THE QUESTION SET: the paragraph `QUESTIONS.md (verbatim): "…"` in `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/23-setups-tribunal.md` §1 — the SAME question set and the SAME ruling template the three houses answer. Where it says "the staged files" / "this folder", read "the real files above"; the staged names map to the real paths as that file's §1 lists them.

THE JOB — rule, do not rewrite. For EACH item — O1 … O8, then (a) … (i) — exactly one of `ADOPT` · `ADOPT WITH <replacement wording, written so it can be pasted into the design verbatim>` · `REJECT <why, with file:line>`, then at most six lines of reasoning, and for every objection a CONCRETE failing scenario (a setup, a bar sequence, a scan time, a card field). Every number you rest on is re-derived from the files with its steps shown, or `UNVERIFIED — <the measurement>`. Every "PostgreSQL / pandas / evaluator / radar behaviour" claim you cannot read in the code → `X<n>: <what to run, on which data, what result would change the design>` under (i) (L70), never argued. LESSONS THAT BIND THIS SEAT (`topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL"): on the bars tribunal, everything the Fable seat INVENTED was broken by the houses against the real code, and everything it took from the files held — so prefer the proposal's simpler mechanism unless you can show its failing sequence; when you replace one, your replacement must be smaller or you price its cost (size, files, chunk) and say so; invent no mechanism you have not walked through the real code. THEN `OWNER (after cards):` (his items, one line each, none a precondition) · `WRONG FACTS:` (a proposal statement the files contradict, both file:lines) · the closing line `TRIBUNAL R1: BUILD` / `TRIBUNAL R1: BUILD AFTER <≤15 words>` / `TRIBUNAL R1: DO NOT BUILD <≤15 words>`.

WRITE (Write tool, ONE new file): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-tribunal-fable-r1-2026-09-21.md` — COMMITTED by the desk, so it carries NO value, quote or wording from his notes, the gap table, the companion or the cheat sheets: cite them by KEY (`A-nn`), `note:line` or PDF page (L32). Layout: the `BLIND:` line → `## DIGEST FOR THE DESK` (≤40 lines: your closing line; one line per item with the verdict and, for ADOPT WITH / REJECT, the gist in ≤20 words; the experiments you named; your owner items; your ESCALATE count) → `## Rulings` (O1 … O8, (a) … (i), full) → `## Experiments (L70)` → `## OWNER (after cards)` → `## WRONG FACTS` → `## READING` (every file and line range you opened) → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48). While you work, the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb `next: <step>` lives inside `## CONTINUE` only; no other line may START with `SETUPS TRIBUNAL FABLE R1 DONE`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch of this file reads its own report first and continues from `## CONTINUE`; it never re-reads the houses (it never read them). One command per Bash call, bare shape, no pipe, no redirect.

STOP LINE (last non-blank line, L71): `SETUPS TRIBUNAL FABLE R1 DONE · verdict: <BUILD|BUILD AFTER …|DO NOT BUILD …> · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — the three counts summing to 17 (O1–O8 + (a)–(i)) — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: the desk commits your report; after the hub's `SETUPS TRIBUNAL R1 DONE` line is committed too, it launches `25-setups-tribunal-derive.md` — you again, no longer blind.
