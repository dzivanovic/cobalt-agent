MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `stale-score-tribunal-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/60-draft-stale-score-tribunal.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control stale-score-tribunal-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `37` / `43` / `59`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-1 prompts of the FOUR-HOUSE TRIBUNAL (L67) on `docs/30 - Design/STALE-SCORE-PROPOSAL-2026-09-21.md`. LADDER: `S2-P2 · F10 Dots / ladder` — a defect in what is live (a card can show a score computed on a stale `last`, and WATCH ranks by it; a ticker with no bar today reads MAXIMUM proximity). LAW STEP: proposal DONE (Opus, `reports/stale-score-design-2026-09-21.md`: `can score on stale input: yes · can arm, expire or reorder on stale input: yes · options: 4 · recommended: C — proximity unknown while bars stale · migration: no · chunks: 2 · open to the tribunal: 10`) → **THIS = drafting its tribunal** → the tribunal rules and derives the FINAL → build per chunk → ≥3 checkers → deploy. L52 binds: it is scoring and it reaches the card; bar (a)–(d) is the tribunal's bar. DO NOT STOP until the report's last line is `STALE SCORE TRIBUNAL PROMPTS DRAFTED …` or `FAILED …`.

THIS IS TODAY'S HANDICAP TRIBUNAL AGAIN, ON A SMALLER DESIGN. Your template is `prompts/2026-09-21/37-draft-float-handicap-tribunal.md` and what it produced — `38-float-handicap-tribunal.md` AS RE-ISSUED · `39-float-handicap-tribunal-fable-seat.md` · `40-float-handicap-tribunal-derive.md` AS RE-ISSUED — plus what the afternoon taught, which you fold in from the newest clean copies `44-setups-tribunal-r2.md` and `52-stale-marker-check.md`: astra is launched with ` < /dev/null` after its sentence (`reports/setups-tribunal-r2-2026-09-21.md` `## Astra stdin experiment`: accepted by the rule, reads started within 2 min, 12 questions ruled in ≈6 min); the hub runs `date` at EVERY completion notice and stops a house past 20 min with its own task-stop tool; THE HUB FILE-CHECKS THE BLIND FABLE SEAT'S CLAIMS TOO when that seat's report exists at its collate step (else the derive treats them as unchecked, never as HOLDS). Copy those shapes byte for byte wherever the same sentence serves; every WHAT IS RULED item of `37` binds here unchanged, with these substitutions:
- (2') THE FABLE SEAT IS NOT YET APPROVED for this tribunal (09-20 R18: asked per case; he is away until late evening 09-21). Draft its prompt; its AUTHORIZATION greps `cto-2026-09-21.md` AND `cto-2026-09-22.md` §4 for a row `R__` the DESK fills after his yes, and refuses while the two characters are `__`. The derive names its seat from that row. If he says no: three houses, and the derive goes to the seat he names.
- (5') `Bash(grok *)` / `Bash(agy *)`: through TODAY 23:59 ET under 09-20 R23, and through 2026-09-22 23:59 ET under `cto-2026-09-21.md` §4 R39 (literal `Bash(grok *) and Bash(agy *) through 2026-09-22`, the gate `52` wrote — copy it). DATE GATE: 2026-09-23 or later → FAILED.
- (7) STAGGER (same houses): the house-running hub refuses while `38`'s, `52`'s, `54`'s or `57`'s report is in progress without a stop line (a report that does not exist = not running ONLY if the desk's launch row says so — `38`'s own rule), and refuses 19:45–20:45 ET on either date (the production deploy window).
- (8) PACKET ≤110 KB so astra finishes inside one window: the proposal, the report's DIGEST + fact table, the code the proposal cites as pre-cut excerpts with `file:line` headers (`cards/evaluate.py` `refresh_card` / the `input_stale` lines `:599`, `:624`, `:629` / `:463-477` / `:776-803` / `:1079` / `:1329-1349`; `scoring.py` `suppression()` + `refresh_dots`; `store.py` `tap_dot`, `:1029-1037`, `:1209`; `cards/radar.py` WATCH order; `radar/poller.py:121`; migration `0007` `shadow_agreement_v` `:255`), ADR-0009 D4, the C1 facts that bound it (`reports/setups-c1-draft-2026-09-21.md` five recompute paths; `cto-2026-09-21.md` §4 R40), `greps.txt`, R36 row verbatim. NO user data is involved — say so after checking (L32). State the total size and an honest token estimate per house.
- ASTRA: REQUIRED if its probe says UP (scoring that reaches the card); PREFLIGHT fails closed on METER and names the retry time; a partial ruling is KEPT.

QUESTION SET for each house — ruling per item `ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>`: the proposal's 10 "open to the tribunal" items in ITS order, plus: (a) does the FACT BASE hold — name any writer of `last`, `card_score`, a dot or the ladder order it missed (`file:line`), and is "cannot arm, create or wrongly expire on stale bars; an expiry can only come late" TRUE; (b) option C against L3: is "proximity = unknown while stale" really the ONE existing null-proximity branch on the tap, refresh AND replay paths, or does any path treat null differently (`file:line`); why C1's untappable-dot carrier is or is not the better path; (c) ESCALATE 1 (no bar today → proximity 1.0): does the claim HOLD, and is deleting the fallback safe for a card created premarket; (d) the clock: evaluator's 2 × scan_interval in bar time, every session — sound premarket and in the 20:00–21:00 pause, and is ANY new number smuggled in (L53); (e) the two meanings of `evaluation="input_stale"` (intraday stale vs daily missing) — does the guard key on the right flag; (f) L57: what is stored so a suppressed score replays, and does the nightly replay reproduce it; (g) L7: `engine_grade_at_tap` recorded from a stale price feeds `shadow_agreement_v` — exclude, flag, or leave, and is it an OWNER ITEM; (h) what he SEES: `—` + "bars stale — last bar HH:MM" beside the R36 STALE stamp — one message or two, any contradiction; should a stale card hold its WATCH slot or sink (owner item or engineering?); (i) ESCALATE 4 (the racing tap / refresh inconsistency on FRESH input) — inside this design or its own item; (j) chunks, seat by L29, RESTARTS, order against C1 / C2 / the stale stamp on the same functions, and whether C must be live BEFORE his first `A-01` ruling; (k) what is NOT checkable from reads → a named experiment on `cobalt_dev` (L70) · closing line per house `TRIBUNAL R1: BUILD | BUILD AFTER <≤15 words> | DO NOT BUILD <≤15 words>`.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `61-stale-score-tribunal.md` — Sonnet 5 hub `stale-score-tribunal-0922`, cwd `~/cobalt-wt/agy-trial`, `38`'s shape with `44`'s improvements, the 14 + 3 strings byte for byte, folder `scratch/tribunal-bars-0920/stale-score-tribunal/r1/` (Write-created), DATE GATE, STAGGER (7), Codex PREFLIGHT. Report `reports/stale-score-tribunal-2026-09-22.md` (or `-09-21` if launched today — the hub names it from `date`, and says so in line 1); stop line `STALE SCORE TRIBUNAL R1 DONE · grok: … · gemini: … · astra: … · houses that ruled: <n> of 3 · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- `62-stale-score-tribunal-fable-seat.md` — MODEL Fable 5.1 (`claude-fable-5-1`), seat `stale-score-tribunal-fable-0922`, `39`'s shape, the seven READ-ONLY strings + the same 3 denies; AUTHORIZATION per (2'); BLIND to the houses' folder and to `61`'s report; its self-attack starts from "who else writes or reads this field" (`grep` the field name) before weighing any mechanism (`reports/setups-tribunal-fable-r2-2026-09-21.md` MEMORY line). ONE file; stop line `STALE SCORE TRIBUNAL FABLE R1 DONE · verdict: … · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — or `FAILED …`. METER line: an honest token estimate.
- `63-stale-score-tribunal-derive.md` — `40`'s shape AS RE-ISSUED; refuses unless the round-1 stop lines it needs are present and committed; a wording whose supporting claim DOES NOT HOLD or is WITHDRAWN is never folded; writes `docs/30 - Design/STALE-SCORE-v2-<date>.md` and its report (fold table, `## NEEDS ROUND 2`, `## OWNER ITEMS`, `## FOR DEJAN` in his terms, one A/B per item, no recommendation from a seat that holds a side — L37). Stop line `STALE SCORE DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/stale-score-tribunal-draft-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (what each prompt does, the launch order with clocks) → the L47 finding → PACKET (source → staged path, sizes, token estimate per house) → RULE PROOF (`grep -c -F -e` per string = 1: `61` vs `38` · `62`, `63` vs `39`) → **`NEW strings:`** (expected: none) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `STALE SCORE TRIBUNAL PROMPTS DRAFTED · prompts: 3 · astra: <REQUIRED|targeted> · questions: <n> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
