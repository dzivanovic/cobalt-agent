MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `ops-fix-r3-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/79-draft-ops-fix-r3.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control ops-fix-r3-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `76`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-3 FIX — THE LAST (L67: three rounds) — for branch `ops/2026-09-21` and its round-3 check. OFF-LADDER (`cto-2026-09-20.md` R41 / R42; `cto-2026-09-21.md` R43, R53–R55). LAW STEP: round 2 (`reports/ops-6a-check-r2-2026-09-22.md`): Grok + Opus 5 `FIX STANDS`, Sol `FIX AGAIN Q2` with a claim the hub's file-check says HOLDS (row 15), Gemini HARNESS → **THIS = drafting the round-3 fix of exactly Sol's Q2 and its check; whatever round 3 leaves open goes to Dejan, never a round 4.** DO NOT STOP until the report's last line is `OPS FIX R3 PROMPTS DRAFTED …` or `FAILED …`.

WHAT IS RULED, AND NOTHING MORE (you widen NOTHING): Sol's Q2, in the hub's file-check words: the new test's READER HALF (`tests/cobalt/test_jobs_restarts.py:177-195` on the branch, tip `3ad064f`) compares FILE-NAME SETS — it does not go red on a new call inside an already-listed file, on an aliased import of the loader, or on a RESIDENT starting to use an existing wrapper (`take_beat`, `backup_freshness`, `latest_snapshot_age()` with no cfg). The fix = a reader proof that is code-derived through the call graph (a static AST walk from `configs/cobalt/backup.yaml`'s loader and its wrappers to every entrypoint, the same shape L42 already uses for `src/` changes — reuse the classifier's own walker if one exists, `file:line`; never a second walker, L3), so that a resident reaching the file through ANY wrapper turns the test red. NOTHING ELSE: the `jobs.yaml` row (`d669dfa`) stands; `restarts.py` stays untouched unless the walker reuse REQUIRES a helper to be importable (say so, smallest diff); the `watchlists.yaml` hard-coded `if` (round-2 ESCALATE 5 of the builder) is NOT touched; round-1 ESCALATE 2–4 carried by id.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L3** · **L19** · **L29** (test + possibly a classifier helper: no write path → Sonnet-eligible, say so) · **L35** · **L42** static AST walk is the ruled derivation · **L45** · **L46 / L54 / L60** existing worktree, RECOVERY RULE · **L62 / L63** · **L67** round 3 is the LAST · **L70** · **L71** · **L74**.
2. `reports/ops-6a-check-r2-2026-09-22.md` — Sol's Q2 verbatim, the hub's row 15, ESCALATE 1–4 · `reports/ops-6a-check-2026-09-22.md` ESCALATE 1 (what round 1 asked) · the branch's `reports/ops-classifier-fix-2026-09-22.md` (Read under `/Users/cobalt/cobalt-wt/ops-0921`) · on the branch: `tests/cobalt/test_jobs_restarts.py` whole, `src/cobalt/jobs/restarts.py` (its AST walk for `src/` changes — the walker to reuse), `configs/cobalt/jobs.yaml` `no_resident_reads`, `src/cobalt/backup/*.py`, `src/cobalt/heartbeat/probes.py` `:431` region, `heartbeat/runner.py:144`, `ops/run_backup.sh`.
3. SHAPES TO COPY byte for byte wherever the sentence serves: `prompts/2026-09-21/77-ops-classifier-fix.md` (the round-2 fix in the same worktree — its launch line = 09-20 R25's strings, its AUTHORIZATION, CLOSE, stop-line shape) · `78-ops-6a-check-r2.md` (its launch line byte for byte; round 3 reads ONLY the round-3 diff + round 2's verdict table).

WRITE (Write tool, NEW files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `80-ops-fix-r3.md` — hub `ops-fix-r3-0922`, seat per L29, OFFLINE, EXISTING worktree `/Users/cobalt/cobalt-wt/ops-0921`, branch `ops/2026-09-21`, from `3ad064f`; FIX row Z3 = Sol's Q2 (test FIRST: a RED case that plants a resident call through an existing wrapper in a `tmp_path` copy or a monkeypatched module and expects red; then green on the real tree) · Z4 DevDoc sentence if the DevDoc describes the proof; `git diff 3ad064f..HEAD` limited to the test file (+ the one helper if required) + DevDoc + report. AUTHORIZATION greps committed rows R54, R55 and a launch row `R__` the desk fills. Stop line `OPS FIX R3 BUILT <tip> | on 3ad064f | offline <p>/<f> (baseline 2210/0) | reader proof: code-derived through wrappers, red case proven | jobs restarts main..HEAD: exit 0, RESTARTS: none | src diff: <empty|helper only: file> | ESCALATE: <n>` — or `FAILED …`.
- `81-ops-6a-check-r3.md` — Sonnet 5 hub `ops-6a-check-r3-0922`, `78`'s launch line and seats byte for byte, date gate R39 (through 2026-09-22), stagger vs `68`, `61`, `73`; QUESTIONS (round 3 reads the round-3 diff only): (1) does the reader proof now go red for a new wrapper call by a resident — Sol's three cases, each; (2) is it ONE walker (L3) — `file:line`; (3) anything weakened; (4) Sol's own line: is Q2 closed in its own words; (5) `ready for the stacked deploy`. Stop line `OPS 6A CHECK R3 DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · Sol Q2 closed: <yes|no> · defects that HOLD: <n> · ready for the stacked deploy: <yes|no> · open to Dejan: <n> · ESCALATE: <n>` — or `FAILED …`. It states in its preamble: THIS IS ROUND 3 — what stays open is listed under `## FOR DEJAN`, one A/B per item.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/ops-fix-r3-draft-2026-09-21.md`: §0 Headline ≤5 lines → the walker finding (reuse or not, `file:line`) → RULE PROOF (`80` vs `77`, `81` vs `78`, each string **1**) → **`NEW approvals:`** (`none` expected) → `READING:` → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤15 lines**. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `OPS FIX R3 PROMPTS DRAFTED · prompts: 2 · builder seat: <Sonnet 5|Opus 5> · walker reused: <yes|no> · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
