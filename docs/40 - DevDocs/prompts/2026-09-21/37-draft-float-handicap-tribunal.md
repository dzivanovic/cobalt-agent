MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `float-handicap-tribunal-draft-0921`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/37-draft-float-handicap-tribunal.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control float-handicap-tribunal-draft-0921 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `22` / `26` / `28` / `33` / `36`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-1 prompts of the FOUR-HOUSE TRIBUNAL (L67) on `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md`. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29` (a SOFT score penalty, never an exclusion, on tickers with float below his threshold OR market cap below his threshold; they keep competing for the pool's 50 and the ladder's top on the lowered number). LAW STEP: proposal DONE (Opus, `reports/float-handicap-design-2026-09-21.md`: `enters at: pool position and card_score · shape: flat multiplier h, ASSUMED · data: partly · chunks: 3 · migration: yes`) → **THIS = drafting its tribunal** → tribunal rules and derives the FINAL → build per chunk → ≥3 checkers → deploy. L52 binds: the design reaches ranking and the card; its bar (a)–(d) is the tribunal's bar. DO NOT STOP until the report's last line is `FLOAT HANDICAP TRIBUNAL PROMPTS DRAFTED …` or `FAILED …`.

THIS IS TODAY'S SETUPS TRIBUNAL AGAIN, ON A SMALLER DESIGN. Your template is `prompts/2026-09-21/22-draft-setups-tribunal.md` and what it produced and what the day then taught: `23-setups-tribunal.md` · `24-setups-tribunal-fable-seat.md` · `25-setups-tribunal-derive.md` AS RE-ISSUED · `27-setups-tribunal-r1b.md` (Gemini's improved sentence: file viewer only, the failure named, `greps.txt` opened second; astra's READING ORDER and the partial-ruling keep, because astra hit its usage limit mid-read this morning after 135k tokens). Copy those shapes byte for byte wherever the same sentence serves; every WHAT IS RULED item (1)–(6) of `22` binds here unchanged, with these substitutions and additions:
- (2') THE FABLE SEAT IS NOT YET APPROVED for this tribunal (09-20 R18: a spawned Fable agent only after the desk asks him, per case). Draft its prompt; its AUTHORIZATION greps `cto-2026-09-21.md` §4 for a row `R__` the DESK fills after his yes, and refuses while the two characters are `__`. If he says no, the tribunal runs with the three houses and the derive goes to the seat he names — write `25'`'s AUTHORIZATION so that it names the seat from that row, not a literal.
- (5') `Bash(grok *)` / `Bash(agy *)` end TODAY 23:59 ET → DATE GATE in the house-running prompt.
- (7) STAGGER (same houses all day): the house-running hub refuses while `reports/setups-tribunal-r1b-2026-09-21.md` or `reports/degraded-line-deploy-review-r2-2026-09-21.md` is in progress without a stop line (the second may not exist yet: not existing = not running ONLY if `prompts/2026-09-21/35-…`'s hub is not listed — say how the hub tells with its allowed strings, else the safe default is to require the desk's launch row to state "35 has stopped or will not run"), and refuses between 19:45 and 20:45 ET (tonight's production deploy window: no house traffic the desk must watch then).
- (8) KEEP THE PACKET SMALL so astra can finish inside one window: the proposal (29 KB), the report's DIGEST, the code the proposal cites as pre-cut excerpts with `file:line` headers (`src/cobalt/radar/pool.py` rank / cap / `_metric_position`, `models.py` pool + exclude blocks, `config.py` export headers, `src/cobalt/cards/radar.py:174-183` `ladder_order`, the scoring entry, `0007_radar_cards.sql:227`), ADR-0009 D4, `greps.txt`, R28 + R29 rows verbatim (they carry his thresholds: user data, L32 — staged under `scratch/` only, never into a committed file). State the packet's total size and an honest token estimate per house.
- (9) USER DATA: the committed proposal names his thresholds and `h` by KEY only; keep it so in every file you write outside `scratch/`.

QUESTION SET for each house — ruling per item `ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>`: the proposal's 6 "Open to the tribunal" items, plus: (a) ONE factor `h` at two places (pool position ÷ h, `card_score` × h) — is it one mechanism (L3) or two rank authorities (L52 b); (b) the proposal's ESCALATE 1: ADR-0009 D4 says `card_score` alone orders WATCH cards while `ladder_order` sorts by (`card_score`, `pool_position`) and every card is born untapped with a null score, so the morning ladder is ordered by pool rank — does the claim HOLD against the code, and which is reconciled, the ADR or the code, and in which chunk; (c) dividing a within-screen POSITION by `h` — is it sound when not-equity rows occupy positions (`pool.py:94-106`), when a ticker sits on several screens, against stickiness / `below_cap_streak`; is there a simpler lawful meaning of "a lower score" at the pool; (d) missing float / cap (~1 % blank; static lists) — "unknown" state, what he sees, L1 / L9; (e) does float already feed a scored dot — double counting; (f) where his values live (pool block vs `"user".trader_settings`), the any / all combinator as a key (R29), shadow → live as a one-word flip vs L7 / L61 approval and L28's load path; (g) L57: can the replay reproduce penalised ranks from stored inputs; the `handicap_cap` miss reason additive to `ExcludedBy`; (h) what reaches the card face (H chip, raw → penalised) and that taps, proposed key and size are untouched — prove from the code that nothing sizes off `card_score`; (i) chunks H1–H3: order, size, migration per chunk, seat by L29, RESTARTS, interaction with the setups chunks C1–C7 on the same scoring lines, what H1's dry-run must show him before the flip; (j) what is NOT checkable from reads → named as an experiment (L70) · closing line per house `TRIBUNAL R1: BUILD | BUILD AFTER <≤15 words> | DO NOT BUILD <≤15 words>`.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/`):
- `38-float-handicap-tribunal.md` — Sonnet 5 hub `float-handicap-tribunal-0921`, cwd `~/cobalt-wt/agy-trial`, `23`'s shape with `27`'s improvements, the 14 + 3 strings byte for byte, folder `scratch/tribunal-bars-0920/float-handicap-tribunal/r1/` (Write-created), DATE GATE, STAGGER (7), Codex PREFLIGHT per your L47 finding (REQUIRED or targeted — say which and why; a partial astra ruling is KEPT). Report `reports/float-handicap-tribunal-2026-09-21.md`; stop line `FLOAT HANDICAP TRIBUNAL R1 DONE · grok: … · gemini: … · astra: … · houses that ruled: <n> of 3 · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- `39-float-handicap-tribunal-fable-seat.md` — MODEL Fable 5.1 (`claude-fable-5-1`), seat `float-handicap-tribunal-fable-0921`, `24`'s shape, the seven READ-ONLY strings + the same 3 denies; AUTHORIZATION per (2'); BLIND to `scratch/tribunal-bars-0920/float-handicap-tribunal/` and to `38`'s report. ONE file, `reports/float-handicap-tribunal-fable-r1-2026-09-21.md`; stop line `FLOAT HANDICAP TRIBUNAL FABLE R1 DONE · verdict: … · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — or `FAILED …`. METER line: an honest token estimate for the read set.
- `40-float-handicap-tribunal-derive.md` — `25`'s shape AS RE-ISSUED (astra in full or PARTIAL k ≥ 1; a missing Gemini does not refuse); refuses unless the round-1 stop lines it needs are present and committed; writes `docs/30 - Design/FLOAT-HANDICAP-v2-2026-09-21.md` (no user data) and `reports/float-handicap-tribunal-derive-2026-09-21.md` (fold table, `## NEEDS ROUND 2`, `## OWNER ITEMS`). Stop line `FLOAT HANDICAP DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-draft-2026-09-21.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (what each prompt does, the launch order with clocks) → the L47 finding → PACKET (source path → staged path, sizes, which carry user data, token estimate per house) → RULE PROOF (`grep -c -F -e` whole span = 1: `38` vs `27` · `39`, `40` vs `24`) → **`NEW strings:`** (expected: none) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `FLOAT HANDICAP TRIBUNAL PROMPTS DRAFTED · prompts: 3 · astra: <REQUIRED|targeted> · questions: <n> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
