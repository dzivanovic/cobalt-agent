MODEL: Opus 5 (`claude-opus-5`) · SEAT: architect, plan mode — a fresh Claude Code pane in herdr started with ONE line, nothing pasted: `cd ~/cobalt && git worktree add -b sprint-2/cards /Users/cobalt/cobalt-wt/s2-p2-cards main; cd /Users/cobalt/cobalt-wt/s2-p2-cards && claude --model claude-opus-5 --permission-mode plan --remote-control p2-plan-0915 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-15/03-p2-plan.md' and follow it exactly."` (create the worktree first: `cd ~/cobalt && git worktree add -b sprint-2/cards /Users/cobalt/cobalt-wt/s2-p2-cards main`) · SESSION: fresh · auto mode on (plan mode: reads and the plan file only; no build) · METER: Anthropic medium (plan session, ~$4–6 API-equivalent, same shape as the P3 plan)

# S2-P2 PLAN — F8 precondition evaluator + F10 dots / ladder / accept-up-down, from the two 09-14 tribunal designs. Plan only, no build.

You are the ARCHITECT for this plan (L29 amended 09-12: the seat is assigned by ruling). You write the plan; you build nothing; a Sol/Opus builder runs it on a later day after Astra's review. Rulings go to Dejan one per message as A/B with your recommendation, in this pane; you continue after each answer. State lives in the plan file, never in this session (L48).

## 0. Wake-up and inputs (read-only, in this order)
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `/Users/cobalt/Vault/Think/6 - Permanent/Memory/areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full. L52 binds this plan: scoring/ranking that reaches the card needs its tribunal BEFORE build — the two tribunals below ARE that tribunal; your plan shows how each of L52's four bars (a)–(d) is met, or names the gap as ESCALATE.
2. `docs/00 - Project/MVP-CHARTER-v0_2.md` §3 F8 (line 107) and F10 (line 128) — the acceptance criteria are the tests.
3. `docs/00 - Project/SPRINT-LADDER-v0_1.md` line 467 (S2-P2 as owed) and the S2 block around it; S2 = 09-10 → 09-23, dates never move (scope out, never dates in).
4. Tribunal 1, settled constraints (ranking/seam): `/Users/cobalt/Vault/Think/0 - Inbox/tribunal-grok-2026-09-14/fable/FINAL.md` §SETTLED, `.../astra/FINAL.md`, `.../grok/FINAL.md`, and the hub report `docs/40 - DevDocs/reports/tribunal-grok-2026-09-14.md`. Dejan's verdict: SUPERSEDED as a design, KEPT as constraints — `card_score = round(conviction × proximity × 100)` is the sole card authority; pool rank = membership only; seam = `system.radar_score_run` / `system.radar_score` (+ view `system.radar_board_v`) beside `radar_pool`/`radar_membership`; opaque scan ids, no `scans` table; one letter vocabulary (`proposedKey`); deterministic evaluator, zero tokens, scan-job stage; hollow judgment dots excluded until tapped, no neutral 5, empty filled set → conviction null never 0; modelled values weight zero and badged; cross-house recompute |Δ| ≤ 1e-6.
5. Tribunal 2, design of record (dots/desk): `/Users/cobalt/Vault/Think/0 - Inbox/tribunal-analyst-2026-09-14/grok/MERGED.md` (APPROVED 09-14; Astra dissents verbatim inside), `.../grok/OPEN.md`, and the hub report `docs/40 - DevDocs/reports/tribunal-analyst-2026-09-14.md`. Rulings that bind P2: dots are one per entry variable of each `trade_def` (`quality_factors[]`) plus the standard trio (`setup_relation`, `market_alignment`, `sector_alignment`) plus the NEW standard `catalyst` dot; `tier: judgment` label; the three desk-graded dots run in SHADOW through S2 (engine computes and stores, the card shows the human/hollow value; one HITL flip in S3 — L7); dots display 1–10 with WHY regardless of n (L8's n is for EV); an expired required input SUPPRESSES the fresh grade (old value survives only as labelled history); structure = caps/warnings, not a dot; desk tables (`system.desk_packet`, `desk_grade`, `desk_regime`) are declared now beside the seam, filled in S3.
6. P3's carry-ins, `docs/40 - DevDocs/plans/plan-s2-p3-2026-09-14.md` §7 and §R R2: the 8 `card.health.*` tunables rows (the R2 threshold table is already ruled — copy it, do not re-rule it) with the health computer; replace `tests/fixtures/radar/panel-cards.contract.json` with real rows; wire the card adapter; enable promote ↑ and the tap strip. Also F3 line: the pool stores `last_rank` only — the rank-metric VALUE column is S2-P4 unless P2 ships first and carries it (say which).
7. Card mock: `docs/30 - Design/TRADE-RADAR-CARD-MOCK-v0_1/card-spec.md` §6 (card fields), §7 (tokens), `decisions.md`; the PNGs are the design of record.
8. Code facts to verify yourself before writing a step (cite file:line): `src/cobalt/taxonomy/` (trade_def schema v0.7, `quality_factors`, variable registry; what a "precondition" is today and where Rubberband's live), `src/cobalt/radar/{runner,store,poller}.py` (scan-job stages, bars in `system.bars`), `src/cobalt/aset/` (sheet, `"user".aset_sizings` columns, `card_state` + transitions from S1-P2, fixed-dollar keys per sheet mode, `enabled_grades` / `daymode.reduced_enabled_grades`), `configs/cobalt/taxonomy/tunables.yaml` (`radar.scan_interval`, `dayopen.*` shape for new rows), `docs/10 - Decisions/ADR-0008*` (tenancy: every new table on exactly one side, `user_id NOT NULL` + FK on user tables, the suite asserts it).

## 1. What the plan must decide (each an A/B to Dejan, one per message, your recommendation first; ≤ 12 rulings, mechanical ones first)
- Evaluator placement: a new stage in the radar scan job (deterministic, after S4 bars) vs. a separate resident. (Tribunal 1 says scan-job stage — default A unless you find a reason.)
- Precondition grammar: how a `trade_def`'s preconditions are expressed as config-as-code (L10: Pydantic schema, dry-run command "what would this trade_def have fired on yesterday's bars") — Rubberband first, then the other five defined defs; a def with no evaluable precondition renders "not evaluable", never a fake card (L1).
- Card landing: which `aset_sizings` columns/rows a WATCH card writes (origin=radar, pool_member_id, trigger, structural stop, proposed key, dots+WHY JSON, `card_score`, `conviction`, `proximity`), the `user_id` FK, and the L28-style provenance (formula hash, tunables hash, scan id) so every number replays (L57).
- Dot scoring: computable dots 1–10 with WHY from stored inputs; judgment dots hollow until tapped; the three shadow dots stored in `system.radar_score` but not in `conviction` until the S3 flip; expired-input suppression rule; no neutral 5.
- Ladder sizing + tap: keys A+/A/B/C/pass with fixed dollars per sheet mode (full B $60 / A $135, half B $30 / A $70; A+ and C greyed with would-be dollars, C enabled since 09-14 by his settings — read the live `enabled_grades` / `daymode.reduced_enabled_grades` values, do not assume); a tap records grade + sizes at the nearest enabled key, loud (F10 test: an A+ tap on a half day records A+ and sizes at A $70 with the notice). Human-only tape dot stays human (L11).
- Health computer: the 8 `card.health.*` rows from P3 R2, computed deterministically, ok/warn/bad only.
- Taxonomy v0.8 bump: what changes in the schema (catalyst standard factor, precondition block, tier label), migration of the 13 loaded defs, the vault-note → DB reload path (note is truth).
- HITL: card landing and dot scoring are TRADING-LOGIC changes → the plan names the tokenized HITL step before anything engine-fed reaches the sheet (L7; status note in LAWS: no token mechanism exists — say how the plan satisfies the law without pretending `--hitl <label>` does).
- Shadow proof: how agreement stats are stored and reported (ten sessions / thirty pairs / median |Δ| ≤ 1 bar from tribunal 2) so the S3 flip has its evidence.
- Sample-size law: EV never renders in P2 (n<30 everywhere) — state it; dots do.
- Builder + reviewer: Sol high after Astra (as P3) vs. Opus headless; Codex probed at launch (L47).

## 2. The plan file
`docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` on branch `sprint-2/cards`, following `docs/40 - DevDocs/PLAN-TEMPLATE.md` and the exact shape of `plan-s2-p3-2026-09-14.md`: DEPLOY WINDOW line (no production deploy 09:00–16:00 ET on a trading day), §0 Headline, §1 Facts (verified read-only, file:line), §R Rulings (Dejan's answers, verbatim), §2 Steps (STEP-n numbering, never L-n), §3 Fixtures (L45: real-shape rows cut by the hub from prod reads — name the queries; personal attribution stripped; the trader's note never edited to fit a parser), §4 Tests (F8/F10 acceptance sentences become test names; tenancy assertions; L45 leak scan unchanged; DB tests marked for the hub), §5 RESTARTS (derived by rule; expected `com.cobalt.radar` + `com.cobalt.aset`; any tunables change restarts every reader), §6 Build/deploy/acceptance/rollback (one deploy per evening; radar restart only in the 20:00–21:00 pause; rollback = revert of the merge range + one migration-down per rollback domain — one domain per write surface, each with its own snapshot step), §7 Carried (to P4/S3), §8 ESCALATE, Cost. Every number in a formula is either traceable to a source Cobalt fetches or marked MODELLED with weight zero (L52-a); exactly one ranking authority reaches the card (L52-b, `card_score`); the seam tables are specified as DDL in the plan (L52-c); the recompute audit by another house is a named step (L52-d).

## 3. Close
1. Plan committed on the branch with a short report `docs/40 - DevDocs/reports/s2-p2-plan-2026-09-15.md` (§0 → rulings table → ESCALATE) in ONE commit: `docs(plan): S2-P2 cards plan + report (rulings R1-Rn)`.
2. Reply to Dejan in ≤10 lines: commit hash, ESCALATE count, the plan path; next step = Astra review (≤3 rounds) on the next build day, then the builder — never today.
