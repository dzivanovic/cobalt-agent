MODEL: Opus 5 (`claude-opus-5`) · SEAT: architect, plan mode — SHELL tab: `cd ~/cobalt && git worktree add -b sprint-2/p4 /Users/cobalt/cobalt-wt/s2-p4 main; cd /Users/cobalt/cobalt-wt/s2-p4 && claude "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-15/05-p4-plan.md' and follow it exactly." --model claude-opus-5 --permission-mode plan --remote-control p4-plan-0915 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt` · SESSION: fresh · auto mode on (plan mode) · METER: Anthropic medium · ATTENDED: Dejan answers ≤8 A/B rulings in the pane; do not start it while he is trading

# S2-P4 PLAN — F3 pick-vs-rank rows · rank-metric value column · F12 nightly replay + counterfactual R · F13 top-N archive + benchmark + miss line · S2 smoke. Plan only, no build.

INDEX CARD (read in this order, nothing more until a step needs it): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (you are the architect) → `docs/00 - Project/MVP-CHARTER-v0_2.md` §3 F3 (line 77), F12 (149), F13 (154) → `docs/00 - Project/SPRINT-LADDER-v0_1.md` S2 block (line ~462–472: P4 = F3 pick-vs-rank rows · F12 · F13 · S2 smoke; S2 ends 09-23, dates never move) → `docs/40 - DevDocs/plans/plan-s2-p3-2026-09-14.md` §7 (P4 carries the rank-metric VALUE column: a new `radar_membership` column + write path, Opus floor) → `docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` §7 (S2-P4 line) and its seam DDL (STEP-1) so P4 does not duplicate a table → code facts you verify yourself (cite file:line): `src/cobalt/radar/{runner,store,poller,models}.py`, `src/cobalt/archiver/` (nightly job, `archiver-runs.md`), `configs/cobalt/jobs.yaml` (`com.cobalt.archiver` 20:30, `com.cobalt.generated`), `src/cobalt/db_migrations/` (next free numbers after P2's 0006/0007 — P4 takes 0008+), `docs/10 - Decisions/ADR-0008*` (tenancy).

Rulings to bring Dejan, one per message, A/B with your recommendation, ≤8, mechanical first: (1) where the rank-metric value lives (new numeric column on `radar_membership` written at S2 vs a `radar_rank` history table) and its backfill from the 7-day CSV cache; (2) F3 pick-vs-rank: what a "pick" is before P2 cards exist (his hand-logged trade note vs a tapped card) and the row shape; (3) F12 nightly replay: a stage in `com.cobalt.archiver` at 20:30 vs a new job; counterfactual R definition from the archived bars (one deterministic formula, inputs stored, L57); (4) F13 top-N archive + benchmark query + the miss line in the daily note (which marker unit, create-if-absent, L28); (5) S2 smoke: the checklist that proves P1–P4 together on the prod install (NN#16), run by the hub after the last P4 deploy; (6) builder: Sol high on the Codex week vs Opus headless (L29 floor for the write path = the value column and the daily-note unit; probe at launch, L47).

Plan file `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` on `sprint-2/p4`, PLAN-TEMPLATE shape exactly as P2/P3: DEPLOY WINDOW, §0, §1 Facts (file:line), §R, §2 Steps (STEP-n), §3 Fixtures (L45 real-shape, hub-cut from prod reads; no user strategy note leaves the vault), §4 Tests (acceptance sentences → test names), §5 RESTARTS (derived; archiver + radar expected), §6 build/deploy/acceptance/rollback (one domain per write surface with its snapshot; radar restart only in the 20:00–21:00 pause; one deploy per evening), §7 Carried, §8 ESCALATE, Cost. Sequencing fact to state: P4 rebases after P2 merges if P2 lands first; if P4 is smaller, P4 may ship first and carry the value column (P3 §7 ruling).

Close: plan + short report `docs/40 - DevDocs/reports/s2-p4-plan-2026-09-15.md` in ONE commit `docs(plan): S2-P4 plan + report (rulings R1-Rn)`; reply ≤10 lines: hash, ESCALATE count, path; next = Astra review on a later day (the desk writes it). No build today.
