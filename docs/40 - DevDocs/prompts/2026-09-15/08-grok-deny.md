MODEL: Sonnet 5 (`claude-sonnet-5`) · SEAT: ops researcher/preparer — SHELL tab: `cd ~/cobalt && git worktree add -b ops/grok-deny-0915 /Users/cobalt/cobalt-wt/grok-deny main; cd /Users/cobalt/cobalt-wt/grok-deny && claude "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-15/08-grok-deny.md' and follow it exactly." --model claude-sonnet-5 --remote-control grok-deny --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/.grok` · SESSION: fresh · auto mode on · METER: Anthropic small; Grok = one read probe on the SuperGrok plan; nobody watching — never ask a question

# Grok CLI credential-deny sandbox profile (ops item 5, carried from `02-ops-2026-09-15.md`) — prepare, prove, hand Dejan one line

INDEX CARD: read `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` → `.../areas/cobalt-houses.md` (Grok lines) → `.../topics/devices.md` (Grok line) → LAWS.md L41, L44, L55 only → `docs/40 - DevDocs/reports/grok-launcher-diag-2026-09-14.md` (what was measured: unscoped filesystem READ by default; `readonly-safe` custom profile; `workspace` profile writes CWD only). Report (L48): `docs/40 - DevDocs/reports/grok-deny-2026-09-15.md` in this worktree, committed here. You READ `~/.grok/` freely; you do not edit `~/.grok/sandbox.toml` (a hub was refused such edits today — self-modification class); the install is Dejan's one-liner that you write out exactly, backup-first.

1. **Find the deny primitive** (read-only): `grok --help`, `grok sandbox --help` if it exists, `~/.grok/docs/` (grep `sandbox`, `deny`, `read`, `profile`, `extends`), `~/.grok/sandbox.toml` and `sandbox.toml.pre-p4` (current profiles), `~/.grok/README.md`, `strings "$(readlink -f "$(which grok)")" | grep -iE "deny|allow_read|read_paths|restrict|sandbox\." | head -80`, and the web (Grok Build CLI sandbox docs, GitHub issues) for the exact TOML keys that restrict READ paths. Cite each source.
2. **Write the profile** to `ops/grok-sandbox.toml` in this worktree (versioned copy of what Dejan's file will contain): keep the existing `readonly-safe` and `workspace` profiles verbatim, add `[profiles.cobalt-job]` extending `workspace` that DENIES READ of `/Users/cobalt/cobalt/.env`, `/Users/cobalt/.cobalt/`, `/Users/cobalt/.grok/auth.json`, `/Users/cobalt/.qwen/`, `/Users/cobalt/.claude/`, `/Users/cobalt/.codex/`, `/Users/cobalt/.gemini/`, and `/Users/cobalt/Vault/Think/6 - Permanent/Memory/_imports/` is NOT denied (L44 equal access to memory). If the installed Grok has NO read-deny primitive, stop here: report `ESCALATE: no read-deny primitive in grok <version>` with the evidence and the nearest alternative (e.g. running Grok jobs as a separate macOS user, or a launcher that bind-mounts a scrubbed tree), no probe.
3. **Dejan's one line** (in the report and in your final reply): `cp ~/.grok/sandbox.toml ~/.grok/sandbox.toml.pre-0915 && cp /Users/cobalt/cobalt-wt/grok-deny/ops/grok-sandbox.toml ~/.grok/sandbox.toml` — only if step 2 produced a full-file replacement that preserves every existing profile byte-for-byte (prove with `diff` against the current file showing only additions).
4. **Probe, only after Dejan's line is applied** — you cannot apply it, so write the probe as the SECOND line for him, and ALSO try it yourself once at the end in case he applied it while you worked (`pgrep -fl grok | grep -v pgrep` must be empty first; if Grok is busy, skip and say so): `cd /tmp && grok --sandbox cobalt-job -p "Run: head -c 20 /Users/cobalt/cobalt/.env — then reply with exactly DENIED or the bytes you read" 2>&1 | tail -5` — PASS = a denial and no bytes. Add a second probe that must still WORK: `grok --sandbox cobalt-job -p "Run: head -3 /Users/cobalt/cobalt/README.md and reply with them" 2>&1 | tail -5`.
5. Commit in this worktree: `git add ops/grok-sandbox.toml "docs/40 - DevDocs/reports/grok-deny-2026-09-15.md" && git commit -m "ops(grok): cobalt-job credential-deny sandbox profile + read probe (item 5)"`. Last report line `GROK DENY <hash> · primitive: <found|none> · probe: <PASS|FAIL|not run> · ESCALATE: <n>`; reply ≤6 lines with Dejan's two lines and stop.
