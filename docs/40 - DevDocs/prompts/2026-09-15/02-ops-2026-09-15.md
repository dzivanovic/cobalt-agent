MODEL: Opus 5 (`claude-opus-5`) · SEAT: ops builder, a fresh Claude Code pane in herdr started with ONE line, nothing pasted: `cd ~/cobalt && git worktree add -b ops/2026-09-15 /Users/cobalt/cobalt-wt/ops-2026-09-15 main; cd /Users/cobalt/cobalt-wt/ops-2026-09-15 && claude --model claude-opus-5 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-15/02-ops-2026-09-15.md' and follow it exactly."` (create the worktree first: `cd ~/cobalt && git worktree add -b ops/2026-09-15 /Users/cobalt/cobalt-wt/ops-2026-09-15 main`) · SESSION: fresh · auto mode on (write path → Opus floor, L29; never bypassPermissions) · METER: Anthropic medium (one build session, ~2 h)

# Ops 2026-09-15 — the queue from `## NOW`, in value order, one branch, READY FOR MERGE before 20:00 ET

Branch `ops/2026-09-15` in `/Users/cobalt/cobalt-wt/ops-2026-09-15`, off `main` (L54). No production process is touched from here: no restarts, no `~/cobalt` writes, no prod DB writes; reads through `cobalt db query --prod` are fine. Merge and restarts happen in the 20:05 pause by the deploy hub (prompt `01-p3-review-build.md` §7) — your job ends at `READY FOR MERGE` with a `RESTARTS:` line. Report from the first step, updated every step (L48): `docs/40 - DevDocs/reports/ops-2026-09-15.md` (§0 Headline ≤5 lines → one table per item → ESCALATE). Each item: failing test first where a test is possible, then the change, then the evidence line in the report. If an item cannot be finished, leave it OUT of the branch entirely (no half-work) and say so in the report.

## 0. Wake-up (read-only)
Read `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md`, then `## NOW` at the top of `/Users/cobalt/Vault/Think/6 - Permanent/Memory/areas/cobalt.md`, then `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full. Then `docs/40 - DevDocs/reports/cto-2026-09-15.md` §1 (the overnight forensics: numbers, mechanism, code lines) — it is the spec for item 1.

## 1. Radar poll-failure flap — bounded fix (ruled 09-15, option A; RESTARTS: com.cobalt.radar in the pause)
Mechanism (verified, `cto-2026-09-15.md` §1.2): `src/cobalt/radar/runner.py::_pool_row` writes the pool row with `failed_stage=None, failed_detail=None, poll_failures=[]` at stage S2 of every cycle; `RadarStore.stamp_poll` (`store.py:208-240`) re-stamps `failed_stage='bars'` and the carried `poll_failures` at S4 ~70 s later; `heartbeat.probes.radar` reads the row once per beat and flapped 14 times since 16:25 09-14 (27/27 beats fit the phase rule). Second half: `poller.py:96-107` pops a `stale` record only when that ticker is polled again, so IMCC (not a member since 09-14) is stuck.
Change, exactly this and nothing wider:
- `_pool_row` carries the previous row's `poll_failures` and, when that list is non-empty, `failed_stage='bars'` + `failed_detail='poll failures: N'` (the same strings `stamp_poll` writes), so the row never blinks clean between S2 and S4. Membership/mirror failure stages keep precedence exactly as today.
- `BarPoller.poll` drops carried failure records whose ticker is not in `members` for this poll (a name outside the pool has no bars to be stale about); the drop is logged once per ticker at INFO with the record's `since`. Records for current members are unchanged.
- Tests first (`tests/cobalt/test_radar_runner.py` / `test_radar_poller.py`, whichever holds the cycle tests): (a) a cycle with a carried failure never produces a clean pool row between `put_pool` and `stamp_poll` — assert on the row passed to `put_pool`; (b) a carried record for a non-member is dropped and logged; (c) a carried record for a member is kept until the member's fetch clears it. Replay the real shape: the fixture row is the live `radar_pool` row quoted in `cto-2026-09-15.md` §1.2 with the 12-hex source suffixes zeroed (L45).
- DevDocs for `radar/runner.py`, `radar/poller.py` updated; BACKLOG.md ticket line closed with the commit.
Evidence in the report: the failing test output before, green after; `uv run cobalt jobs restarts main..HEAD` → `com.cobalt.radar`.

## 2. Rules.md parser tolerance (TICKET 09-14, BACKLOG.md:258; L45 companion: fix the parser, never the note)
`src/cobalt/prefill/rules_gen.py::_split_trailing_tags` peels only whitespace-delimited `#tag` tokens. Accept a recognized tag glued to the final punctuation (`…break-even.#process`) — and only that: a tag glued to a word (`…tradeprocess`) is still "none". Fixture = the real-shape line quoted in `cto-2026-09-15.md` §1.4 (personal attribution none; keep the text). Failing test first in `tests/cobalt/test_prefill_rules_gen.py` (or the existing rules_gen test file). No change to Rules.md. RESTARTS: none (prefill-* are launchd one-shots; they pick the code up at their next run).

## 3. Email channel — RETIRE (ruled 09-14, final; executed today)
Scope, all of it, one path (L3): remove the email second channel — `src/cobalt/notify/email.py`, its config in `notify/config.py` (`EmailConfig`, the `email:` block of `configs/cobalt/notify.yaml`), the `notify email-auth|email-test|email-status` CLI, the `email` heartbeat probe (`heartbeat/probes.py`), `out_of_band` in `heartbeat/runner.py` (the transition alert becomes DM-only; the DM body no longer carries an email line), tunables rows `notify.email.auth_port` / `notify.email.timeout_s`, the `cobalt_email_sends` migration is left in place (history; note it in the report — dropping a table is a separate HITL), tests (`test_notify_email.py` removed; heartbeat/probe/tenancy tests amended), DevDocs pages, `docs/40 - DevDocs/cobalt/notify/` index. The runner's docstring and `notify.yaml` comments are rewritten to say the second channel was retired 09-14 and why (Google Publish gated on restricted-scope verification). Secrets: the three `GOOGLE_OAUTH_*` VaultManager entries are removed by Dejan with the command you name in the report (never run it from here, never print a value); `~/.cobalt/google-oauth-client.json` — report whether it exists, do not delete. RESTARTS: derived — heartbeat is an interval job (no resident); anything else the derivation names is reported. Evidence: `uv run cobalt heartbeat show` from `~/cobalt` (read-only, before/after shape of the DM body in the report), full suite green, `grep -rn "email" src/cobalt --include=*.py` shows only the archived migration path.

## 4. `.claude/settings.json` — worktree directories + production-read allowlist
Repo file `.claude/settings.json` (create if absent; commit it): `permissions.additionalDirectories` = `["/Users/cobalt/cobalt-wt", "/Users/cobalt/Vault"]`; `permissions.allow` gets the read-only prefixes every hub uses: `Bash(uv run cobalt db query *)`, `Bash(uv run cobalt heartbeat show*)`, `Bash(uv run cobalt jobs list*)`, `Bash(uv run cobalt jobs restarts *)`, `Bash(uv run cobalt jobs readers *)`, `Bash(uv run cobalt notify email-status*)` (drop this one after item 3), `Bash(launchctl print *)`, `Bash(git status*)`, `Bash(git log*)`, `Bash(git diff*)`. Never `Bash(git push *)` (L55). Prove with a fresh `claude -p` from the worktree that a listed read runs without a prompt and `git push --dry-run` still asks (record the transcript lines). No `bypassPermissions` anywhere.

## 5. Grok sandbox — credential-deny profile + read probe (before any live Grok packet)
GUARD: run this item only when `pgrep -fl grok | grep -v pgrep` is empty (the Grok pane is idle); otherwise do it last, and if Grok is still busy at the end, leave the item OUT and say so.
`~/.grok/sandbox.toml` (outside the repo; capture the final file verbatim under `ops/grok-sandbox.toml` in the branch so it is versioned): add profile `[profiles.cobalt-job]` extending `workspace` that denies READ of `/Users/cobalt/cobalt/.env`, `/Users/cobalt/.cobalt/`, `/Users/cobalt/.grok/auth.json`, `/Users/cobalt/.qwen/`, `/Users/cobalt/.claude/`, `/Users/cobalt/.codex/` (use the deny key the installed Grok version documents — `grok --help` / `~/.grok/docs`; if the version has no read-deny primitive, STOP this item and ESCALATE with the evidence). Read probe, run by you, headless: `cd /tmp && grok --sandbox cobalt-job -p "Run: head -c 20 /Users/cobalt/cobalt/.env; then reply with only DONE or DENIED"` — the report quotes the denial. Keep the backup `~/.grok/sandbox.toml.pre-0915`.

## 6. RESTARTS classifier — documentation paths (L42 amendment O9, PENDING until this ships)
`src/cobalt/jobs/restarts.py::classify`: a path under `docs/`, or a root markdown file (`README.md`, `CLAUDE.md`, `AGENTS.md`, `QWEN.md`), with no runtime reader derives NO restart and is labelled `DOCS` in the table, never `UNCLASSIFIED`. Everything else unchanged. Failing test first (`tests/cobalt/test_jobs_restarts.py`): `QWEN.md` in a range → no ESCALATE; `src/cobalt/cli.py` in the same range → still `com.cobalt.radar`. Report line: "L42 O9 clause now effective — hub folds the PENDING marker out of LAWS.md at close."

## 7. Phantom `*_scan_id` columns — PROPOSAL ONLY, nothing applied
`radar_membership.opened_scan_id / last_scan_id / closed_scan_id` reference no table (`system.scans` was never created; tribunal-grok settled "opaque scan ids, no scans table"). Write the proposal into the report: keep the columns as opaque ids (documented in the DevDoc of `radar/store.py` and the placement map) vs. add a `system.radar_scan` run table later with the P2 seam tables. Recommend one. No migration.

## 8. aset.err sheet-modes error, 09-14 10:50 — trace (read-only)
`grep -n "2026-09-14 10:4\|2026-09-14 10:5" /Users/cobalt/cobalt/logs/aset.err` — quote the lines, name the code path and whether it recurs in the log after 10:50. If it is a bug, a failing test + fix is in scope only when it fits inside this session; otherwise a BACKLOG ticket line.

## 9. L56 private stores — Qwen
GUARD: run this item only when `pgrep -fl qwen | grep -v pgrep` is empty (the Qwen day-open may be running); otherwise do it last, and if Qwen is still busy at the end, leave the item OUT and say so.
Claude Code's private auto-memory was folded into the vault on 09-15 (only a pointer remains). Do the same for Qwen: list `~/.qwen/` memory files (`QWEN.md`-style auto notes, `memory*`), copy any Cobalt-relevant line into `docs/40 - DevDocs/reports/ops-2026-09-15.md` §9 for the close hub to file into `6 - Permanent/Memory/` (you never write the vault memory folder), and replace each private note with the one-line pointer used in `/Users/cobalt/.claude/projects/-Users-cobalt-cobalt/memory/MEMORY.md`. Backup each file as `<name>.pre-0915` first.

## 10. Qwen allowlist rule for day-open — prepared, applied after the merge
Prepare the one-line command that adds `Bash(uv run cobalt day-open *)` to `permissions.allow` in `~/.qwen/settings.json` (Python one-liner with a `.pre-0915b` backup). Put it in the report for the deploy hub to run after `ops/day-open` merges tonight; do not run it now (the command does not exist on main yet).

## 11. RESTIC_PASSWORD rotation — procedure only
Write the exact step list (restic `key add` with the new password from VaultManager → verify `restic snapshots` with the new key → `key remove` the old → update the VaultManager entry → LastPass) as a numbered block in the report for Dejan to run in step-by-step mode with a hub present. Do not run any of it; do not print any value.

## Out of scope today (say so in the report): rank-metric value column (S2-P4), FinancialJuice collector (S3 design), `COBALT_ENV` defaults (ruled), anything touching L29's routing substance.

## Close of this session
1. `uv run pytest -q` full offline suite green in the worktree; `uv run cobalt jobs restarts main..HEAD` → the `RESTARTS:` line (expected: `com.cobalt.radar` from item 1 + whatever item 3 derives; UNCLASSIFIED → ESCALATE).
2. One commit (L46): `git add -A && git commit -m "ops(2026-09-15): radar flap fix, rules-tag tolerance, email retired, settings allowlist, grok deny profile, restart classifier docs rule"` — body lists items done/skipped and the RESTARTS line.
3. Report's last line: `READY FOR MERGE <hash> | RESTARTS: <line> · ESCALATE: <n>` — then reply to Dejan in ≤10 lines with the hash, the items landed, the items left out and why.
