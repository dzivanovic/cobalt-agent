> CONTINGENCY ONLY (desk, 2026-09-15 14:35): the P3 hub pane (`p3-hub-0915` / peer `cobalt-ce`) is ALIVE and holds cron `3483781f` for 20:05 — the desk's earlier "dead pane" reading was wrong (`~/.claude/sessions/<pid>.json` is not a liveness signal; `ListAgents`/a peer message is). Launch this file ONLY if that pane is gone before 19:45 ET (check: `ListAgents` from the desk, or the pane itself). Never run both: two hubs deploying the same evening is the failure.

MODEL: Sonnet 5 (`claude-sonnet-5`) · SEAT: evening deploy hub — in the SHELL tab, contingency (any time before 19:45 ET), one line: `cd ~/cobalt && claude "Read 'docs/40 - DevDocs/prompts/2026-09-15/09-evening-deploy.md' and follow it exactly." --model claude-sonnet-5 --remote-control evening-0915 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — this pane REPLACES the P3 hub pane (`p3-hub-0915`), whose 20:05 cron `3483781f` died when that pane was exited at 13:28; DO NOT `/exit` this pane until it prints `EVENING DONE` · auto mode on · METER: Anthropic small · nobody watching — never ask a question; a step that cannot proceed becomes an ESCALATE line in the report and is skipped

# Evening 2026-09-15 — pre-flight now, wait by yourself, deploy at 20:05 ET, Qwen allowlist, close. Supersedes `98-evening.md` (same job, dead pane).

INDEX CARD (read in this order, nothing more until a step needs it): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (you are the hub on a production deploy) → `docs/40 - DevDocs/prompts/2026-09-15/01-p3-review-build.md` §7 (the ruled sequence; this file restates it with two pre-flight fixes) → `docs/40 - DevDocs/prompts/2026-09-15/99-close.md` (steps 1–4 are yours tonight) → `docs/40 - DevDocs/SESSION-CLOSE.md` → evidence: `docs/40 - DevDocs/reports/cto-2026-09-15.md` (§5, §6), `/Users/cobalt/cobalt-wt/s2-p3-radar-panel/docs/40 - DevDocs/reports/s2-p3-2026-09-15.md`, `/Users/cobalt/cobalt-wt/ops-2026-09-15/docs/40 - DevDocs/reports/ops-2026-09-15.md`, `/Users/cobalt/cobalt-wt/ops-day-open/docs/40 - DevDocs/reports/ops-day-open-2026-09-15.md`. Laws that bind tonight: L43 (one deploy per evening, radar restart only inside 20:00–21:00 ET), L54 (rebase-then-ff from `~/cobalt`, merge = deploy), L42 (restarts derived, UNCLASSIFIED → ESCALATE), L51 (pre-approved: `COBALT_ENV=production` on a refusing prod command; committing machine-written files; extracting a stage-2-retired file), L46 (clean tree), L55 (push is Dejan's), L48 (evidence in the report file first). Report from the first step: `docs/40 - DevDocs/reports/deploy-2026-09-15.md` — §0 Headline ≤5 lines → tables → ESCALATE. Under `6 - Permanent/Memory/` you touch ONLY `LAWS.md` and `LAWS-HISTORY.md`, exactly as `99-close.md` step 2 authorizes; every other memory file is the desk's.

## 1. Pre-flight — NOW, before you wait (all safe outside the pause; no restart, no prod DB write)

1. **Schedule yourself first.** One-shot cron (CronCreate) for 20:05 ET today with the prompt `Run 09-evening-deploy.md step 2 onward`. Confirm in one line what you scheduled (id + time). Do not poll.
2. **P3 worktree is dirty** (the P3 hub edited its report after committing `a77a7fe`; `git rebase` refuses on unstaged changes): `git -C /Users/cobalt/cobalt-wt/s2-p3-radar-panel status --porcelain` → if it lists `docs/40 - DevDocs/reports/s2-p3-2026-09-15.md` (and nothing else), commit it there: `git -C /Users/cobalt/cobalt-wt/s2-p3-radar-panel add -A && git -C /Users/cobalt/cobalt-wt/s2-p3-radar-panel commit -m "docs(report): s2-p3 — final headline and commit section (build a77a7fe)"`. Anything else listed → ESCALATE, touch nothing, and P3 is skipped tonight. Record the new tip hash.
3. **main carries two machine-written files** (`configs/cobalt/rules.yaml`, regenerated by `prefill-daily` at 08:31 from Rules.md; `docs/40 - DevDocs/reports/seat-usage.md`, the seat-usage job): `git -C ~/cobalt status --porcelain` must show exactly those two (the desk committed its own docs). If so: `cd ~/cobalt && git add configs/cobalt/rules.yaml "docs/40 - DevDocs/reports/seat-usage.md" && git commit -m "chore: machine-written files, committed as-is (L51-2) — rules.yaml regenerated 2026-09-15 08:31, seat-usage report"`. Any other path → ESCALATE and leave it out of the commit.
4. **Conflict forecast (read-only):** for each of `ops/day-open`, `sprint-2/radar-panel`, `ops/2026-09-15`: `mb=$(git -C ~/cobalt merge-base main <branch>)`; `comm -12 <(git -C ~/cobalt diff --name-only $mb..<branch> | sort) <(git -C ~/cobalt diff --name-only $mb..main | sort)` → the files both sides touched. Table in the report. (The desk checked at 14:00: no overlap on the two machine-written files.)
5. **Other worktrees stay untouched tonight:** `ops/agy-trial-0915`, `ops/grok-deny-0915`, `sprint-2/cards`, `sprint-2/p4` are tomorrow's; `heartbeat/*` and `_tmp_main_check` are not yours.
6. Write §0 + the pre-flight table to the report, reply in one line, and wait for your cron. If a later turn arrives after 20:20 ET and the deploy has not run, run step 2 on that turn at once — the pause ends 21:00.

## 2. 20:05 ET — deploy (inside the 20:00–21:00 market_reset pause ONLY; if the current time is past 20:45, deploy day-open + ops only and leave P3 for tomorrow's pause: its radar restart must stay inside the pause, L43)

1. `git -C ~/cobalt status --porcelain` → empty (else: machine-written only → commit as in 1.3; anything else → ESCALATE and stop). Record `main_before=$(git -C ~/cobalt rev-parse --short HEAD)`.
2. `ops/day-open` (84912db): `git -C /Users/cobalt/cobalt-wt/ops-day-open rebase main` → `cd ~/cobalt && git merge --ff-only ops/day-open`.
3. `sprint-2/radar-panel` (a77a7fe + the report commit from 1.2): `git -C /Users/cobalt/cobalt-wt/s2-p3-radar-panel rebase main` → `cd ~/cobalt && git merge --ff-only sprint-2/radar-panel`.
4. `ops/2026-09-15` (b2e33d5; its report ends `READY FOR MERGE`): `git -C /Users/cobalt/cobalt-wt/ops-2026-09-15 rebase main` → `cd ~/cobalt && git merge --ff-only ops/2026-09-15`.
   Conflict rule for 2–4: a rebase that stops with conflicts → `git rebase --abort` in that worktree, main stays exactly as before that branch, ESCALATE with the conflicting paths, and CONTINUE with the next branch (the three are independent: day-open = `dayopen/` + cli; P3 = `aset/web.py`, `aset/radar_panel.py`, `radar/store.py` read layer, tests, fixtures; ops = radar runner/poller, prefill rules_gen, notify/heartbeat email removal, jobs.yaml, `.claude/settings.json`, docs).
5. `cd ~/cobalt && COBALT_ENV=production uv run cobalt validate` → exit 0; `cd ~/cobalt && COBALT_ENV=production uv run cobalt jobs register` (day-open adds tunables rows; ops adds the `no_resident_reads` registry rows to `jobs.yaml`). A non-zero exit here = stop before any restart, ESCALATE, and roll back the merges with `git revert` of `$main_before..HEAD` (never `reset --hard`).
6. Restarts, derived: `cd ~/cobalt && uv run cobalt jobs restarts $main_before..HEAD` → the three reports agree on `com.cobalt.aset com.cobalt.radar`. Then, inside the pause: `launchctl kickstart -k gui/$(id -u)/com.cobalt.aset` and `launchctl kickstart -k gui/$(id -u)/com.cobalt.radar`. Every label the derivation names and nothing it does not; UNCLASSIFIED → ESCALATE, never guess.
7. Smoke (all from `~/cobalt`): `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5010/` = 200 · `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5010/radar` = 200 and `curl -s http://127.0.0.1:5010/radar | grep -ciE "ladder|radar"` ≥ 1 · `COBALT_ENV=production uv run cobalt day-open --json | head -c 400` runs · `COBALT_ENV=production uv run cobalt heartbeat show` has no new RED (prefill-drc's state is recorded, not judged: it clears at its next 15:40 run) · `launchctl print gui/$(id -u)/com.cobalt.radar | grep -E "state =|pid ="` = running · email retired: the first heartbeat beat after the restart writes no `Email sent` line to `logs/heartbeat.err` (the beat before the deploy may still show one) · after 21:00 the first post-reset radar scan lands: `COBALT_ENV=production uv run cobalt db query --side system --prod --format table "SELECT last_scan_at, members, failed_stage FROM radar_pool WHERE pool_key='primary'"` (if your turn is over by then, record "not yet observed"; tomorrow's day-open proves it).
8. Tag: `git -C ~/cobalt tag deploy-2026-09-15`. Rollback = `git revert` of the merge range + `com.cobalt.aset` restart at once, `com.cobalt.radar` in the next pause.
9. Deploy table in the report (branch · before/after hash · rebased? · merged? · RESTARTS · smoke lines), last line of that section `LIVE <hash> · RESTARTS done: <labels> · ESCALATE: <n>`.

## 3. Qwen day-open allowlist — only if `ops/day-open` merged
Run the one-liner from `docs/40 - DevDocs/reports/ops-2026-09-15.md` §10 verbatim (it backs up `~/.qwen/settings.json` first); expected output `allow entries: 11`. Record it.

## 4. Close — `99-close.md` steps 1–4, yourself, tonight
Ledger appendix (step 1) must also carry the afternoon (from `cto-2026-09-15.md` §5–§6): P2 plan REVIEWED `3142a1b` (Astra 3 rounds, 31 folded, 0 dissent); P4 plan `5312d3d` (R1–R7, ESCALATE 4); Gemini/agy Attempt D clean → RULED second reviewer; Grok `cobalt-job` deny profile proven live; Codex meter ruling 09-15→09-19 (reviews only, builds Opus headless); the P3-hub cron incident and this hub's replacement of it; tonight's deploy table. LAWS folds (a)–(d) APPLY exactly as `99-close.md` step 2 says, (e) stays PROPOSED. Validate, one commit on main `close: 2026-09-15 appendix, LAWS folds applied, deploy table`.

## 5. Last line of the close report: `EVENING DONE <hash> · LIVE: <yes/no per branch> · PUSH: Dejan · ESCALATE: <n>`. Reply in this pane in ≤6 lines and stop. Dejan reads it tomorrow morning; the push is his.
