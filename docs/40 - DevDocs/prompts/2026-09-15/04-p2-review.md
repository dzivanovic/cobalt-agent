MODEL: Sonnet 5 (`claude-sonnet-5`) · SEAT: review hub — in the SHELL tab, after the plan pane has `/exit`ed (two sessions never share a working tree, L46): `cd /Users/cobalt/cobalt-wt/s2-p2-cards && claude "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-15/04-p2-review.md' and follow it exactly." --model claude-sonnet-5 --remote-control p2-review --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt` · SESSION: fresh · auto mode on · METER: Anthropic small (hub); Codex probed in STEP 1 — if the window is spent, exit with `RELAUNCH AFTER RESET` and stop

# S2-P2 plan — Astra review (≤3 rounds), amendments in place, REVIEWED — ready for the builder. No build.

INDEX CARD: you are the HUB (L36 only spawner, L35 trust the artifact). Read only: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full → the plan `docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` (committed on `sprint-2/cards`; if it is not there, stop and report) → `docs/40 - DevDocs/reports/s2-p2-plan-2026-09-15.md`. Rulings R1–R11 in the plan are Dejan's and are not reopened; Dejan's answer to the plan's ESCALATE 2 is recorded in NOW (fixture = hub-local, uncommitted, `requires_vault` test, unless NOW says otherwise). Report from the first step (L48): `docs/40 - DevDocs/reports/s2-p2-review-<today>.md` — §0 Headline ≤5 lines → probe → per-round disposition tables → ESCALATE.

## 0. Preconditions (read-only)
`git -C /Users/cobalt/cobalt-wt/s2-p2-cards status --porcelain` empty; `git log --oneline -1` shows the plan commit; `codex exec --help` confirms the flags below.

## 1. Codex meter probe (L47)
```
cd /Users/cobalt/cobalt-wt/s2-p2-cards && wc -l "docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md"
cd /Users/cobalt/cobalt-wt/s2-p2-cards && codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Run: wc -l 'docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md' -- reply with only the number." < /dev/null
```
Match + exit 0 + no usage-limit text = UP. Otherwise write `RELAUNCH AFTER RESET — Codex window spent at <time>` as the report's last line, tell Dejan in one line, and stop. Never wait, never substitute another house for the reviewer (L29: plan review = architect + Astra).

## 2. Astra rounds (L29 ≤3, L39 no fourth)
Round 1:
```
cd /Users/cobalt/cobalt-wt/s2-p2-cards && codex exec --skip-git-repo-check -m gpt-6-astra -s workspace-write "You are GPT-6 Astra, the cross-house reviewer (LAWS L29). Working directory = git worktree of branch sprint-2/cards. Read in order: /Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md in full; 'docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md'; 'docs/10 - Decisions/' ADR-0008 (tenancy); every file:line the plan's §1 Facts cite (taxonomy/trade_def.py, radar/runner.py, radar/poller.py, archiver/migrations, aset/migrations, cards/models.py, cards/store.py, aset/engine.py, settings/models.py, placement.py, tests/cobalt/test_tenancy.py, tests/cobalt/test_radar_notes.py); the two tribunal designs at '/Users/cobalt/Vault/Think/0 - Inbox/tribunal-grok-2026-09-14/fable/FINAL.md' §SETTLED and '/Users/cobalt/Vault/Think/0 - Inbox/tribunal-analyst-2026-09-14/grok/MERGED.md'. Rulings R1-R11 are Dejan's and are not under review. Review for: (1) factual errors against the code (file:line); (2) a step that would break a law or a settled tribunal constraint (cite L-number or the SETTLED bullet); (3) the DDL in STEP-1 against ADR-0008 tenancy (side per table, user_id NOT NULL + FK + GUC default, grants, migration registry, rollback order); (4) a formula or number whose source Cobalt cannot fetch (L52-a); (5) a missing test for a stated behaviour, or a first-run failure; (6) the five ESCALATE items — say for each whether the plan's proposed handling is sound. Write ONE file 'docs/40 - DevDocs/reports/p2-review-astra-r1-<today>.md': 3-line summary, then a numbered table id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording. No rewrite of the plan, no alternatives that contradict R1-R11. Reply in chat with only the path." < /dev/null
```
Disposition (you are the clerk, not the architect): (a) factual correction of a name, path, column, line or DDL detail → amend the plan in place, tag `[amended <today> per Astra R1-<id>]`; (b) a change contradicting R1–R11 or a SETTLED constraint → not applied, recorded verbatim under `## Dissent (Astra)` in the plan, the plan stands; (c) a missing test / first-run failure / tenancy defect → amend §2/§4 accordingly. Write the disposition table into your report. Round 2 = same launch, `r2` file name, prepend: "Round 2. Read your R1 file and the hub's dispositions in 'docs/40 - DevDocs/reports/s2-p2-review-<today>.md'; only findings you still hold, or new ones caused by the amendments." Round 3 only if R2 still holds a BLOCKER; after it the plan stands and every remaining dissent is recorded verbatim.

## 3. Close
1. `cd /Users/cobalt/cobalt-wt/s2-p2-cards && git add -A && git commit -m "docs(plan): S2-P2 plan Astra-reviewed (<n> rounds, <b> blockers folded, <d> dissents recorded)"` — one commit (L46). No source files touched; if any were, revert them and say so.
2. Report's last line: `REVIEWED <hash> | rounds <n> · dissent <d> · ESCALATE <k>` — then tell Dejan in ≤10 lines. The desk writes the build prompt (`05-p2-build.md`) from this report; you do not build.
