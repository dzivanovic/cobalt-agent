MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat · SEAT: drafter `setups-deploy-reland-draft-0924`, launched by the CTO desk in the background on HIS word (`cto-2026-09-24.md` R45, 12:14 ET: the re-land, "right now"). Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/31-draft-setups-deploy-reland.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control setups-deploy-reland-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `09-draft-setups-deploy-r2.md`'s byte for byte; `git -C /Users/cobalt/cobalt diff*` is NOT on this line — every tree fact you need is read with `show` / `log`) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue with the safe default. Never end a turn between steps. **≤20 min: production is DEGRADED while you draft (radar card stage RED every cycle); the house read of your output is a DELTA read and the launch follows it.**

# RE-ISSUE `09-setups-deploy-r2.md` AS THE RE-LAND OF THE SAME STACK (L19) — `a0ba0098`'s TREE BACK ONTO `main` BY ONE `git revert` OF THE ROLLBACK COMMIT `4e625f6a`, SMOKE TAILS SPACED, A REVERT-READBACK ROW, THEN STEP-6 AND THE TAG — PLUS ITS DELTA HOUSE READ

WHAT HAPPENED (read it in `reports/deploy-2026-09-24.md` §0, `## Smoke`, `## ESCALATE` 0 / 2 / 4 / 5 / 6 / 7, `## CONTINUE`'s recorded values, and the stop line — the FIRST thing you read): `09` ran 11:04–11:40 on his `DONE TRADING 11:01` (R39). The L68 gate was GREEN on `<stack>` `5e62ea26` (offline 2501/0 · with-DB 2856/0 · live-note 131/0 + 5 AWAITING); the merge landed `<stack-final>` `a0ba0098` at 11:32 (residents down 188 s; migration `0013` APPLIED, stays applied — additive, never rolled back); smoke 4.7 (b) read three radar tails within 52 s of `<t up>` 11:35:20 and saw no post-restart `radar cycle:` line → RED by the prompt's letter → STEP-5 reverted with ONE `git revert -m 2` → `4e625f6a` (code proven identical to `<pre-merge>` `1118c3d4`), residents UP 11:37:45. The cycle came at 11:36:38 — 20 s after tail 3 — a FALSE RED from tail pacing, NOT a code defect (L70; the hub's own ESC 2). Every other smoke row reached was GREEN on the new code ((a), (b) counts, (c) 200 ×3, (d) all four markers, (e) read 1, (f), (g)). **PRODUCTION IS DEGRADED NOW:** the deployed radar's one cycle wrote 3 rows into `"user".card_dots` (ids 16 / 32 / 48, cards 377–379, factor `assumed_formation`, `na_reason 'ASSUMED'`); the OLD `Dot.na_reason` literal lacks `ASSUMED` (the new `src/cobalt/cards/scoring.py:75` adds it), so on `4e625f6a` the radar panel, the pool refresh and the radar's S5 evaluate FAIL on every read (`ValidationError … input_value='ASSUMED'`), heartbeat `RED radar failed_stage evaluate`; residents UP, pages 200. HIS RULING R45 (12:14 ET, `cto-2026-09-24.md` §4): re-land the same stack NOW — the new code reads those rows; a rollback would not have been needed. L43: this re-land is the SAME deploy event continued (desk reading, R42); the day window is his per-case word (R45, as R83 / R39 were).

LAW STEP (L43 / L54 / L66 / L67 / L68): the set (`setups/seven-0921` rebased `126383ea` + `replay/mover-partial-0924` `59532385`) was BUILT, CHECKED (`04` HOLD 0, `08` HOLD 0), stacked and GATED GREEN on `5e62ea26`, its deploy prompt `09` read by `10` (blockers 0), merged as `a0ba0098` and reverted on a false red. NOTHING is rebuilt and NO gate re-runs: the tree that ships is `a0ba0098`'s, byte for byte, restored by reverting the revert — `git -C /Users/cobalt/cobalt revert --no-edit 4e625f6a` (its string is on `09`'s approved line: `"Bash(git -C /Users/cobalt/cobalt revert --no-edit *)"`). L54's rollback shape for the re-land: ONE `git -C /Users/cobalt/cobalt revert --no-edit <reland sha>` (the same string), named in the prompt and in the report. L66: residents DOWN before the revert commit lands on `main`, UP after `validate`. L67: the re-issued prompt is read by houses other than its author BEFORE it runs — a DELTA read (`33`, below), one round.

INDEX CARD: `LAWS.md` in full (L59) — L1, L19 (a re-issue is WHOLE), L28 (STEP-6's note write, unchanged), L35, L42, L43, L51, L54, L61, L62, L63, L66, L67, L68, L70, L71, L73, L76; `topics/cto-desk.md` last eight lines (the 09-24 midday lessons: tails ≥90 s, revert-readback, the desk's `cd` as its own call, a watch names the report path the prompt names). SOURCES: (1) `prompts/2026-09-24/09-setups-deploy-r2.md` WHOLE — the base text (its launch line = the string pool; ADD NOTHING; every step, gate, string and rule stays unless a source below changes it); (2) `reports/deploy-2026-09-24.md` (above); (3) `cto-2026-09-24.md` R42 (the incident + the desk's A/B), R44 (the desk's current state: the overnight tribunal round 2 runs in `agy-trial` — no DB, no house lane conflict with a deploy), R45 (his word); (4) `reports/setups-deploy-r2-draft-2026-09-24.md` (what `09` folded — do not undo any of it); (5) `prompts/2026-09-24/10-review-setups-deploy-r2.md` (the house-read shape and strings to copy for `33`) and `reports/setups-deploy-r2-review-2026-09-24.md` (its findings: `09` stands with blockers 0 — the delta read covers ONLY what this file changes).

WRITE (whole files, L19): **(1) `prompts/2026-09-24/32-setups-deploy-reland.md` — the re-land.** `09` re-issued whole with EXACTLY these changes, each a row of your `## DELTA FROM 09` table (before → after, `09`'s line):
- HEADER / LAW STEP / AUTHORIZATION: this is the re-land of `a0ba0098` after the 11:40 false red; the P-HIS gate reads `cto-2026-09-24.md` for the committed row **R45** carrying his words `right now` and `re-land` (instead of `09`'s `DONE TRADING` row R39 — keep R39 as the day-window precedent it cites; R83's `for 09-24 only` stands); the launch-row grep expects the desk's row naming `32-setups-deploy-reland.md` and `RE-LAND: a0ba0098 by revert of 4e625f6a`; `R__L` placeholders as `09` has them, for the desk. THE DESK'S BARE COMMANDS: NO gate re-cut (command (1) of `09` is DELETED — no gate worktree is touched; `deploy/stacked-0923` stays at `a0ba0098`, `stacked-0923`'s worktree untouched); (2) `cd /Users/cobalt/cobalt`; (3) the launch line = `09`'s launch line BYTE FOR BYTE except the prompt path and the remote-control name `setups-deploy-reland-0924` (`comm` of the sorted allow strings must be EMPTY both ways — your `## RULE STRINGS` section proves it; the strings never run here — the gate worktree's `cp`/`rm .env`, `pytest`, `merge`, `rebase`, `tag` shapes — are CARRIED so the list is byte-identical, and each is named `NEVER RUN HERE` in the prompt). DESK LINE unchanged (the desk holds every with-DB run and every commit of its own on `main` from launch to the stop line).
- STEP-0 PREFLIGHT: rewritten for the re-land — `date`; `git -C /Users/cobalt/cobalt status` (known dirt at drafting, RE-READ IT YOURSELF with `git -C /Users/cobalt/cobalt status` … you cannot: `status` is not on your line — take it from `## KNOWN DIRT` below, written by the desk at 12:15: modified `configs/cobalt/rules.yaml` (`generated_at`, the 05:15 generator) and `docs/40 - DevDocs/reports/seat-usage.md`; untracked under `docs/40 - DevDocs/reports/`: `.grok-stdout-2026-09-22.tmp`, `day-open-2026-09-24.md`, `drc-overnight-tribunal-fable-r2-2026-09-24.md`, `drc-overnight-tribunal-r2-2026-09-24.md` (two LIVE hub reports being written by the round-2 tribunal sessions — NEVER added, never touched), `routing-x5-retro-2026-09-23.md`, `voice-v1-check-2026-09-23.md`; anything else dirty at run time → recorded, and a dirty file under `src/`, `configs/` (other than `rules.yaml`'s `generated_at`) or `tests/` → `FAILED: preflight — unexpected dirt <path>`); `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<main0>` (a docs-only descendant of `4e625f6a`: prove with `git -C /Users/cobalt/cobalt diff --stat 4e625f6a HEAD -- . ':(exclude)docs'` → EMPTY, else `FAILED: preflight — main carries non-docs changes above the revert`); `git -C /Users/cobalt/cobalt log --oneline -1 4e625f6a` must read `Revert "Merge branch 'main' into deploy/stacked-0923"`; `git -C /Users/cobalt/cobalt rev-parse --short deploy/stacked-0923` = `a0ba0098`; both residents `running`; P14's four log baselines re-read and recorded (`<a0>` `<ta0>` `<tr0>` `<tc0>`) plus `<k17_0>`; the slug read (attnotnull → `f`/`False`: 0013 is applied — if `t`/`True` → `FAILED: preflight — migration 0013 not applied; the desk decides`); the cards read `<cards0>`; `COBALT_ENV=production uv run cobalt heartbeat show` recorded as the DEGRADED baseline (radar `failed_stage evaluate` EXPECTED here — it is the incident; any OTHER new red kind → recorded, not a stop); the three `card_dots` rows read (`COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT id, card_id, factor, na_reason FROM \"user\".card_dots WHERE na_reason = 'ASSUMED' ORDER BY id"` — the `db query` string is on the line; expected 3 rows, ids 16 / 32 / 48; a different count → recorded, not a stop; 0 rows → `ASK DESK` + continue: the re-land is still his word). L76: `ls -la /Users/cobalt/cobalt-wt/stacked-0923/.env` → must NOT exist (the deploy's lock is free); the desk's `ls ~/cobalt-wt/*/.env` at launch is the row. WINDOW: his R45 word (day window, per-case) — the prompt's window clause names R45 and drops `09`'s `DONE TRADING <hh:mm>` wait.
- STEP-1, STEP-2, STEP-3 (rebase, stack, gate, `main` INTO the gate, docs-only proof): DELETED — replaced by ONE section `## STEP-R — the tree that ships is a0ba0098's (nothing is down)`: snapshot `COBALT_ENV=production uv run cobalt backup run …` (as `09`'s 3.x, a fresh snapshot — record its id) and `backup status`; the rollback tag: `09`'s `pre-setups-0924` already exists at `1118c3d4` — DO NOT re-tag it (a `tag` that exists fails); instead record `pre-reland-0924` = `<main0>` with the same `tag` string (`git -C /Users/cobalt/cobalt tag pre-reland-0924 <main0>`); `git -C /Users/cobalt/cobalt merge-base --is-ancestor a0ba0098 HEAD` (exit 0 — the revert is on main's history); `RESTARTS:` re-derived for the re-land range with `COBALT_ENV=production uv run cobalt jobs restarts 4e625f6a..a0ba0098` … NO: derive on the range that LANDS — after STEP-4.3 the range is `<main0>..<reland>`; at STEP-R record the expected line from `deploy-2026-09-24.md` `## RESTARTS` (`com.cobalt.aset com.cobalt.radar`, nothing UNCLASSIFIED) and re-run at 4.5 / 4.7 (f) on `<main0>..<reland>`.
- STEP-4 (the outage window, L66): 4.1 bootout both (as `09`); 4.2 → `git -C /Users/cobalt/cobalt revert --no-edit 4e625f6a` (replaces `merge --ff-only`; a conflict → `git -C /Users/cobalt/cobalt revert --abort`, residents UP, `FAILED: 4.2 — revert conflict <files>`); 4.3 PROVE THE TREE: `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<reland>`; `git -C /Users/cobalt/cobalt diff --stat a0ba0098 HEAD -- . ':(exclude)docs'` → EMPTY (else `FAILED: 4.3 — the re-landed tree differs from a0ba0098: <stat>` → STEP-5 at once); `git -C /Users/cobalt/cobalt diff --stat 4e625f6a HEAD -- . ':(exclude)docs'` → the stack's file list (recorded); 4.4 migration: `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` ONLY (0013 is applied; the `--allow-prod` migrate WITHOUT `--proof-only` is NEVER run here — write that in capitals; the proof must show 0013 applied and nothing pending); 4.5 `validate` + `jobs restarts <main0>..<reland>` (= both residents, nothing UNCLASSIFIED — a different line → `FAILED: 4.5 — RESTARTS <line>` → STEP-5); 4.6 bootstrap both (as `09`), `<t up>` from `date`.
- STEP-4.7 SMOKE — `09`'s rows (a)–(g) verbatim EXCEPT (b)'s radar-cycle bullet and a NEW row (h): **(b) THE TAILS ARE SPACED:** the first `tail -n 12 /Users/cobalt/cobalt/logs/radar.err` is run ONLY when `date` reads `<t up>` + 90 s or later — until then run (c), (d), (f), (g) and (e)'s first read, each preceded by `date`, and record the clock; the second tail at `<t up>` + 180 s or later, the third at `<t up>` + 300 s or later (between tails: (e)'s second read at ≥100 s from the first, then `COBALT_ENV=production uv run cobalt heartbeat show` and `date` reads as the clock — never a wait or sleep command, none is on the line); RED only after the THIRD tail at ≥ 300 s shows no `radar cycle:` line stamped after `<t up>`. Record every tail's `date` in `## Smoke`. In the PAUSE window `09`'s idle clause stands. **(h) REVERT-READBACK (the 09-24 lesson): the new code reads the old code's and its own writes.** After (a)–(g): `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT count(*) FROM \"user\".card_dots WHERE na_reason = 'ASSUMED'"` → the STEP-0 count (the rows are still there — the new code READS them, nothing deletes them); `COBALT_ENV=production uv run cobalt heartbeat show` → the radar line carries NO `failed_stage evaluate` (the incident kind is GONE — it is the row that proves the re-land fixed production; still present after the second (e) read → RED → STEP-5 and `ESCALATE: the ASSUMED rows are not readable by a0ba0098's code either — the desk decides`); `grep -c "radar panel FAILED" /Users/cobalt/cobalt/logs/aset.err` recorded before (STEP-0) and after (`<t up>` + 300 s): the after-count must not grow after `<t up>` (a growing count → RED); `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` = `200`. RED rules as `09` plus (h).
- STEP-5 ROLLBACK, one path: residents DOWN → `git -C /Users/cobalt/cobalt revert --no-edit <reland>` → PROVE IT (`diff --stat 4e625f6a HEAD -- . ':(exclude)docs'` EMPTY) → residents UP → `FAILED: <row> … · rollback: used — migration 0013 applied: yes — aset: <UP|DOWN> — radar: <UP|DOWN>`; say in the prompt, in capitals, that this rollback RETURNS production to the degraded state (the 3 rows unreadable again) and that the desk then decides at once — never a second revert, never a DB write.
- STEP-6 (his four rows, L28 note write, 6.8 live-note GREEN expected) and STEP-7 close (tag `deploy-2026-09-24` at `<reland>` … NOT at `<reland>`: the tag goes on the commit that carries the report — keep `09`'s STEP-7 order and `reset --soft` shape exactly) UNCHANGED except every `<stack-final>` → `<reland>` and every `<pre-merge>` → `<main0>`; the S2 smoke line stays `S2 smoke: owed after the 21:10 replay (07 ESCALATE 1)`.
- REPORT: `reports/deploy-2026-09-24-reland.md` (a NEW file; `deploy-2026-09-24.md` is the failed run's record and is never appended); stop line `SETUPS DEPLOY RELAND DONE · reland: <sha> · tag: deploy-2026-09-24 · rows: 4 · live-note: <n passed> · S2 smoke: owed 21:10 · ESCALATE: <n>` or `FAILED: … · rollback: <used|not used> — migration 0013 applied: yes — aset: … — radar: …`; the RELAUNCH RULE re-derived for THIS run's states (main = `<main0>` untouched / `<reland>` landed with residents down or up / a revert in progress / STEP-5 completed → never re-entered); the in-progress last line as `09`. Its `## L74` section as `09`'s.
**(2) `prompts/2026-09-24/33-review-setups-deploy-reland.md` — the DELTA house read of `32`** per his R95: Opus 5.5 + Grok (Sol METER to Sat 09-26 06:47; no Gemini), `10`'s shape and its 9 allow strings / 3 denies / `--add-dir` triplet BYTE FOR BYTE, remote-control `setups-deploy-reland-review-0924`; the PACKET = your `## DELTA FROM 09` table + `32`'s STEP-0, STEP-R, STEP-4, STEP-4.7, STEP-5 and RELAUNCH RULE whole + `deploy-2026-09-24.md` `## ESCALATE` 0 / 2 + this file's WHAT HAPPENED paragraph — the houses read the DELTA for: a step that could leave production worse than now, a tree-identity gap, a string not on `09`'s line, a tail rule that can still false-red, a readback row that proves nothing; `09`'s unchanged text was read by `10` (blockers 0) and is NOT re-read; report `reports/setups-deploy-reland-review-2026-09-24.md`, stop `SETUPS DEPLOY RELAND REVIEW DONE · houses: <n> of 2 · blockers: <n> · folds: <n>|FAILED`; the grok gate = the STANDING row (`grep -n "^| R19 " cto-2026-09-24.md` carrying `All 4 house models approved`, L62 — never a dated row).

REPORT (LAST): `docs/40 - DevDocs/reports/setups-deploy-reland-draft-2026-09-24.md` — §0 ≤5 lines (what `32` does in one sentence, strings added: 0 expected, the readback row, the tail rule) → `## DELTA FROM 09` (every change: before → after, `09`'s line, why) → `## KNOWN DIRT` → `## RULE STRINGS` (`comm` of the sorted allow strings of `32` against `09`'s: EMPTY both ways; `33`'s against `10`'s: EMPTY) → `## FOR DEJAN` (every NEW string or window he must approve — expected: NONE) → `## ESCALATE` → last line `SETUPS DEPLOY RELAND DRAFTED · new rule strings: <n> · readback rows: <n> · tail rule: 90/180/300 · window: R45 now · restarts: com.cobalt.aset com.cobalt.radar · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `SETUPS DEPLOY RELAND DRAFTED` or `FAILED`.
