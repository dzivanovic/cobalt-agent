MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: drafter `devdb-builds-reissue-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/41-reissue-devdb-builds-r2.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control devdb-builds-reissue-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `45-reissue-devdb-builds.md`'s byte for byte; NO new string) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default, continue. Never end a turn between steps. ≤35 min.

# RE-ISSUE FOR 2026-09-24 THE DEV-DB LANE UNDER L76 + GATE EARLY — `46` STALE-SCORE (a CONTINUE), `47` HANDICAP H1, THEIR CHECKS `50` / `51`, AND TONIGHT'S S2 SMOKE LOOK `09` (L19)

LAW STEP: both builds were ruled by him 09-23 13:2x ET, "yes start both" (`cto-2026-09-23.md` R58); stale-score `46` launched, ran STEP-1 and part of STEP-2 (worktree `/Users/cobalt/cobalt-wt/stale-score`, branch `cards/stale-score-0922`, tip `57925f3b` = `wip(stale-score): STEP-2 partial — RED tests recorded; LANE HELD by drc-d1/.env before the with-DB baseline`, on `51afdad0`), was STOPPED by the desk 09-23 15:1x (R73) to give `cobalt_dev` to the deploy, and never resumed; H1 `47` never launched (its worktree `/Users/cobalt/cobalt-wt/handicap-h1` sits at `be4c79b5`, a 09-22 `main` commit — nothing built). Since then TWO LAWS changed what a build must do — **L76** (ruled 2026-09-24 06:38, `cto-2026-09-24.md` R10: one owner, one lock for `cobalt_dev`; a migration is applied ONLY inside the suite's own rollback transaction or rolled back before the stop line; **`"Bash(COBALT_ENV=dev uv run cobalt db migrate)"` — on BOTH `46`'s and `47`'s launch lines — is now a forbidden step**, 09-23 R71 is why) and **L68 GATE EARLY** (ruled 06:31, R9: offline + with-DB + the READ-ONLY live-note suite on the build's own tree before BUILT; the stop line quotes all three; check packets carry them) — and the setups stack LANDED on `main` today (`a2d320b8` = `deploy-2026-09-24`, migration `0013` applied in production; `cobalt_dev` at `0013`), which `46`'s base (`51afdad0`, the pre-merge setups tip) and `47`'s "after tonight's deploy" gate both waited for. THIS = re-issue the four prompts as full files (L19), plus tonight's read-only smoke look. Nothing is launched by you; the desk launches ONE with-DB build at a time (L76), in the order the lane frees: the voice fix build `36` and the K1 build `39` are ahead in the queue — say so in each DESK LINE.

LADDER: `46` = S2 · F10 carry-over (stale-score, `cto-2026-09-22.md` R37–R45 his nine owner rulings); `47` = S2 · F10 carry-over (float handicap H1, `cto-2026-09-22.md` R26 / R52–R57); the smoke look = S2 · F12 / F13 (the 21:10 replay; K9.4 after today's mover-bars fix). S2 is CLOSED (09-23 R113, his override) — these are its carried build items, not its close.

AUTHORIZATION (verify before you read anything else): `grep -n "^| R62 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print a row naming `41-reissue-devdb-builds-r2.md`; `grep -n "^| R58 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` must print his "yes start both". Either missing → `FAILED: authorization mismatch — <which>` as the last line of your report and stop.

INDEX CARD: `LAWS.md` in full (L59) — L19 (a changed file is a full re-issue; a CONTINUE relaunch of an UNCHANGED file is one `CONTINUE:` line — `46` changes, so it is a re-issue that RESUMES from `57925f3b`), L29 (write paths → Opus 5.5; the mode each precedent states, quoted), L41 (the `.env` pair), L46 / L54 (`46`'s branch rebases onto `main` before it continues — rebase-then-ff; the desk runs no rebase, the BUILD's first step does, and PREFLIGHT proves `main` is its base afterwards; a conflict in the ≈1,000 setups lines of `evaluate.py` = `FAILED: rebase — <files>`, never resolved blind), L60 (recovery rule: the wip stays, nothing discarded), L62 (STANDING STRINGS R17 / R19 — grok / agy / codex / `claude -p` need no approval; the `.env` pairs of `46` / `47` are his already: 09-22 R64 and R30 — cite the rows), L67 (checker seats as amended 2026-09-24: these checks are fix-round / code checks → Opus 5.5 · Sol · Grok; Sol METER until Sat 09-26 06:47 ET → Opus 5.5 + Grok; NO Gemini — 09-23 R97 — so `50` / `51`'s `"Bash(agy *)"` is REMOVED, a strict subset), L68 (GATE EARLY: the live-vault READ string `"Bash(COBALT_LIVE_VAULT_ROOT=/Users/cobalt/Vault/Think uv run pytest *)"` — approved 09-23 R52, on `03`'s line — and its step, copied from `03` / `14`), L70, L72 P-b (the MIGRATION SEAM stays as `reports/devdb-builds-reissue-2026-09-23.md` `## MIGRATION SEAM` settled it: `0013` setups (on main now), **`0014` H1**, **`0015` stale-score's R40 view, only under X30 (A)**, `0016` DRC D1, `0017` voice; the K1 drafter running beside you pins `0018` — do not touch it), L73, **L76**. Files: `prompts/2026-09-23/46-stale-score-build.md`, `47-handicap-h1-build.md`, `50-stale-score-check.md`, `51-handicap-h1-check.md` (WHOLE — the texts you re-issue), `reports/devdb-builds-reissue-2026-09-23.md` (the 09-23 re-issue's decisions; its `## MIGRATION SEAM`), the stale-score wip: `/Users/cobalt/cobalt-wt/stale-score/docs/40 - DevDocs/reports/stale-score-build-2026-09-23.md` (what STEP-1 and STEP-2 did; its `## CONTINUE`) and `git -C /Users/cobalt/cobalt log --oneline -5 cards/stale-score-0922`; `prompts/2026-09-24/03-setups-fix-r2-build.md` + `14-drc-d1-fix-r1-build.md` (GATE EARLY + L76 steps and strings — the shapes you graft in) and `04-setups-fix-r2-check.md` + `15-drc-d1-fix-r1-check.md` (the check strings; the grok gate as R17's standing row `grep -n "^| R17 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"`, NEVER a dated row); `cto-2026-09-23.md` R71–R75 (why `46` stopped, the `0017` lesson); `cto-2026-09-24.md` R9 / R10 / R17 / R19 / R54 / R56 / R59 (today's laws, the deploy, the taxonomy load); for the smoke look: `prompts/2026-09-23/09-s2-smoke-look.md` (WHOLE), `reports/s2-smoke-look-2026-09-23.md` (the 09-23 look: AMBER 32/34, K9.4 `archive_incomplete: 1`, K17 five `_inflight` files), `prompts/2026-09-24/07-mover-bars-fix-build.md` `## ESCALATE` 1 (what tonight's look proves) and `reports/mover-bars-fix-build-2026-09-24.md` or as `07` names it, `docs/00 - Project/SPRINT-LADDER-v0_1.md` `### Status 2026-09-24` (S2 CLOSED R113; K17 leaves the smoke for good R114 "yes A").

DECIDE, before writing (each a line of your report with its source): (1) `46`'s RESUME — the build starts with `git rebase main` in its worktree (L54; the wip commits `1a5c6928`, `57925f3b` ride along), PREFLIGHT proves `git merge-base --is-ancestor main HEAD` and re-locates every `v2 cite → base line` on the rebased tree (the setups lines are now in `main`), BASELINE runs the three suites on the rebased tip (the RED tests of STEP-2 are EXPECTED red — name them so the baseline is not mis-read as a failure), then STEP-2 continues from the wip's `## CONTINUE`; (2) `47`'s BASE — the desk's re-cut line in its header (`git -C /Users/cobalt/cobalt-wt/handicap-h1 checkout -B radar/handicap-h1-0922 main` — nothing built on the branch, be4c79b5 is a `main` commit; confirm with `git -C /Users/cobalt/cobalt log --oneline -3 radar/handicap-h1-0922` and `git -C /Users/cobalt/cobalt merge-base --is-ancestor radar/handicap-h1-0922 main` reasoning from `show`), migration `0014` directly on `0013` (now on main); (3) MIGRATIONS UNDER L76 — every `cobalt db migrate` step and string REMOVED from both builds; the migration file pair is written, registered in `__init__.py` `FORWARD` / `REVERSE` and in `placement.py`, and proven ONLY by the with-DB suite's own rollback transaction (`test_migrate_proof`, `test_tenancy`, the archiver rollback tests — name the tests the suite already has that exercise a new migration, `file:line`); `cobalt_dev` is at `0013` before and after — the stop line says `<nnnn>: rolled back` / `cobalt_dev: 0013`; (4) GATE EARLY — the live-note step and string grafted into both builds (`03`'s step text; READ-ONLY, `COBALT_LIVE_VAULT_ROOT=/Users/cobalt/Vault/Think`), the stop lines carry `offline <p>/<f> | with-DB <p>/<f> | live-note <p>/<f>`; the KNOWN state of the live-note suite after today's rows (R56: `131 passed, 0 failed`, vwap-continuation pinned, second-chance pin lifted) is the expectation — a different set → ESCALATE, never "expected red"; (5) CHECK SEATS — `50` / `51` re-issued with Opus 5.5 + Grok (Sol METER, return `Sep 26th, 2026 6:47 AM` recorded), `"Bash(agy *)"` removed, the packet carrying the three suites' executed output (L68), fail closed below TWO; (6) THE SMOKE LOOK — `09` re-issued for 2026-09-24 (path, rc `s2-smoke-look-0924`, report `reports/s2-smoke-look-2026-09-24.md`, the date gate, the window 21:40–23:30 ET unchanged, the strings BYTE FOR BYTE: the seven read-only + `"Bash(COBALT_ENV=production uv run cobalt smoke s2 --prod *)"` of `cto-2026-09-21.md` R43); its VERDICT RULE changes: S2 is CLOSED (R113) — the look no longer decides S2; it reports (a) the 21:10 replay's outcome, (b) **K9.4 after today's mover-bars fix** (`07` ESC 1: what value proves the fix — quote it), (c) K17 no longer counted (R114), (d) every other check's colour, (e) `smoke green: yes|no` with the first failing clause; stop line `S2 SMOKE LOOK DONE · deploy: deploy-2026-09-24 · replay ran: <yes hh:mm DONE|FAILED|no> · checks: <g> green / <r> red · K9.4: <value> (<PASS|FAIL>) · real reds: <n> · smoke green: <yes|no> · ESCALATE: <n>` or `FAILED: window — …` / `FAILED: <step> — …`.

WRITE (whole files, L19; new numbers today): (1) `prompts/2026-09-24/42-stale-score-build-r2.md` — `46` re-issued per (1), (3), (4): the launch line = `46`'s MINUS `"Bash(COBALT_ENV=dev uv run cobalt db migrate)"` PLUS `"Bash(COBALT_LIVE_VAULT_ROOT=/Users/cobalt/Vault/Think uv run pytest *)"` (say so in a RULE STRINGS sentence the desk can `comm`: `46`'s line − 1 + 1, the `.env` pair of R64 kept), rc `stale-score-r2-0924`, mode as `46` states (quote it), report `reports/stale-score-build-2026-09-24.md` in the worktree (the 09-23 report stays as history, cited), stop `STALE SCORE BUILT <tip> | on <main tip> | rebased: yes | red <hash> | offline <p>/<f> | with-DB <p>/<f> | live-note <p>/<f> | migration: <0015|none> rolled back | cobalt_dev: 0013 | .env: removed | experiments <n>/<as expected>/<not> | ESCALATE: <n>` or `FAILED: …`; DESK LINE: launched only while no `~/cobalt-wt/*/.env` exists and no with-DB run is in flight — after `36` / `39` as the lane frees. (2) `prompts/2026-09-24/43-handicap-h1-build-r2.md` — `47` re-issued per (2), (3), (4): the same string arithmetic on `47`'s line (− `db migrate` + the live-vault read; the `.env` pair of R30 kept), rc `handicap-h1-r2-0924`, the desk's re-cut line in the header, stop `HANDICAP H1 BUILT <tip> | on <main tip> | experiments <n>/<as>/<not> | offline <p>/<f> | with-DB <p>/<f> | live-note <p>/<f> | migration: 0014 rolled back | cobalt_dev: 0013 | h=1 identity: proven | .env: removed | ESCALATE: <n>` or `FAILED: …`; DESK LINE as above, after `42`. (3) `prompts/2026-09-24/44-stale-score-check-r2.md` and (4) `45-handicap-h1-check-r2.md` — `50` / `51` re-issued per (5), `15`'s strings byte for byte (state the precedent line), the grok gate = R17's standing row, round 1 of ≤3, stop lines in `15`'s shape (`STALE SCORE CHECK DONE · round: 1 · opus: … · grok: … · defects that HOLD: <n> · ESCALATE: <n>` / `HANDICAP H1 CHECK DONE · …`). (5) `prompts/2026-09-24/46-s2-smoke-look.md` — `09` re-issued per (6). Every prompt carries `R__` launch-row placeholders for the desk, its ladder line, and a RULE STRINGS sentence naming the precedent line and the exact ± strings.

REPORT (LAST): `docs/40 - DevDocs/reports/devdb-builds-reissue-2026-09-24.md` — `## §0 Headline` ≤5 lines → `## L74` (one line if the block appears) → `## DECISIONS` (the six above, each with its source) → `## DELTA` per prompt (`46 → 42`, `47 → 43`, `50 → 44`, `51 → 45`, `09 → 46`: every changed sentence, old → new, and the string arithmetic) → `## OWNER ITEMS` (verbatim, for him — expected NONE) → `## FOR DEJAN` (new strings: expected 0 — every string is on an approved line already; any string you could not source to a row of his goes here) → `## ESCALATE` → last line `DEVDB BUILDS REISSUED · 42: ready (rebase first) · 43: ready (re-cut first) · 44: ready · 45: ready · 46: ready (window 21:40) · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `DEVDB BUILDS REISSUED` or `FAILED`.
