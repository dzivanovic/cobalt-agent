MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat · SEAT: drafter `setups-deploy-r2-draft-0924`, launched by the CTO desk in the background AFTER `08`'s stop line. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/09-draft-setups-deploy-r2.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control setups-deploy-r2-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `76`'s byte for byte) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps. **≤40 min: the house read of your output must be done well before 15:00 ET.**

# RE-ISSUE `05` AS THE 09-24 STACKED DEPLOY: SETUPS + MOVER-BARS, WITH `06`'s EIGHT FOLDS, HIS R118 (FOUR ROWS, NO KNOWN RED) AND `07`'s DEPLOY CARRIES — PLUS ITS HOUSE READ

LAW STEP (L43 / L68 / L67): TWO branches are BUILT + CHECKED for tonight's one deploy event — `setups/seven-0921` (`03` fix r2 `df7817a6`, `04` round 2 HOLD 0, `cto-2026-09-23.md` R106) and `replay/mover-partial-0924` (`07` BUILT `b69a6681` on `4cc6811a`, red `8030d3cf`, offline 2258/0 · with-DB 2603/0 · live-note 142/0; `08` round 1 — READ ITS STOP LINE FIRST, below). `05-setups-deploy.md` was written for ONE branch (its line 18: "A second ready branch means a stacked re-issue from `66` (L19), never this") and was read by `06` (`SETUPS DEPLOY REVIEW DONE · houses: 2 of 2 · blockers: 0 · folds: 8`). This is that stacked re-issue, whole (L19), and its house read.

GATE ON `08` (first thing, one `tail -n 3` of `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/mover-bars-fix-check-2026-09-24.md`): its LAST NON-BLANK line must start `MOVER BARS FIX CHECK DONE` and carry `defects that HOLD: 0` with both `ready` = `YES`. If it does → TWO branches (the stacked shape). If it does NOT (a HOLD, a FAILED, or no stop line) → write the re-issue for ONE branch (`05` + the folds + R118 only, `05`'s own rebase-then-ff shape kept), name the mover branch as DROPPED FROM THE SET with the line you read (L43: "dropped from the set, named, and the rest lands"), and say so in §0 and the stop line. Never guess the check's outcome.

INDEX CARD: `LAWS.md` in full (L59) — L28 (the Assumed Defaults note write), L35, L42, L43, L51, L54 (the gate-branch exception: `main` merged INTO the gate branch, rollback = ONE `git revert -m 2`), L61, L62, L63, L66, L67, L68 (its 2026-09-24 GATE EARLY clause), L71, L73, **L76** (one lock for `cobalt_dev` — the deploy's with-DB gate takes it alone); `topics/cto-desk.md` last six lines. SOURCES: (1) `prompts/2026-09-24/05-setups-deploy.md` (whole — the base text; every step, gate and string it carries stays unless a source below changes it); (2) `reports/setups-deploy-review-2026-09-24.md` `## Folds proposed` 1–8 and `## Checked against the files` (fold ALL EIGHT, each the smallest text change backed by its row; fold 1 is TAKEN — the desk's answer to its ESC 2; its ESC 1 is answered: the desk's launch shell has NEITHER `COBALT_ALLOW_DEV_ENTRY` nor `COBALT_VAULT_PATH` set — `printenv | grep -c` = 0 — so the dev proof cannot reach his real note, `vault.py:196-208`); (3) `cto-2026-09-23.md` R118 (below); (4) `reports/mover-bars-fix-build-2026-09-24.md` (§0, `## RESTARTS`, `## ESCALATE` 1, 2, 5) and `reports/mover-bars-fix-draft-2026-09-23.md` `## DESIGN` (what the branch changes: `src/cobalt/replay/movers.py`, `runner.py`, `configs/cobalt/smoke/s2.yaml` K9.7–K9.12 + K17 removed, tests, docs); (5) the STACKED PRECEDENT `prompts/2026-09-23/66-stacked-deploy-r4.md` (the two-branch shape: gate worktree `/Users/cobalt/cobalt-wt/stacked-0923`, gate branch `deploy/stacked-0923` re-cut on `main` by the desk, siblings merged into it, `main` merged INTO it, the combined tree proved — offline, with-DB under L76's lock, live-note — then `--ff-only`, rollback `git revert -m 2`) and its landed cousin `68-smoke-only-deploy.md` (`deploy-2026-09-23`, report `deploy-2026-09-23-r5.md`) for the resident stop/start, migration and validate steps that ran GREEN; (6) `prompts/2026-09-24/06-review-setups-deploy.md` (the house-read shape to copy for `10`).

HIS R118 (`cto-2026-09-23.md`, 22:3x ET, "Let's go with B …"): the deploy HOLDS `dist.k.vwap` (A-16). STEP-6 writes **FOUR** rows — `flat_threshold.ema9` (A-09) 0.05, `flat_threshold.vwap` (A-10) 0.05 (R119), `range_break.failed_trap_bars` (A-19) 1, `range_break.retest_tolerance_atr` (A-20) 0.10 (R82) — and NO `dist.k.vwap` row; vwap-continuation stays `AWAITING ITS ENGINE FILL: vwap-continuation (dist.k.vwap null)`. R107 is SUPERSEDED: the post-STEP-6 live-note run is expected GREEN; there is NO known red; delete every `KNOWN RED` / `R119 live-note: RED (expected, R107)` clause and every "five rows" count (the rows file, the dry-run's "EXACTLY the five rows", the `AWAITING` pins, the report title, the close). R3's approval of `05` covered "FIVE rows incl. `dist.k.vwap`"; four of those five is a SUBSET — no new approval for the rows; say so in `## FOR DEJAN`.

`07`'s DEPLOY CARRIES: (ESC 5) RESTARTS for the mover branch = `com.cobalt.aset` + `com.cobalt.radar` (static import reach of `cobalt.replay.*`) — the same two residents the setups branch restarts; the combined `cobalt jobs restarts <range>` line must name both and nothing UNCLASSIFIED. (ESC 1) The first `cobalt smoke s2 --prod` after this deploy must read a POST-DEPLOY replay night (a pre-deploy job row lacks `archive_partial_by_side` → K9.8 / K9.11 FAIL, K9.9 / K9.12 ERROR): the deploy's own smoke step (if `05` runs one) must either skip K9.7–K9.12 by name with that reason or state that the smoke verdict is read tonight after the 21:10 replay — copy `05`'s own smoke rule and amend it minimally. (ESC 2) `tests/cobalt/test_replay_runner.py` is touched by BOTH branches (setups ~418/~472, mover appended at the end) and by `cards/stale-score-0922` (not shipping): the stacked gate proves the seam (L68); the merge of the two siblings into the gate branch must be conflict-free or FAILED, named — write the check (`git merge` exit status, `git diff --check`) as the precedent `66` does.

WRITE (whole files): (1) `prompts/2026-09-24/09-setups-deploy-r2.md` — the deploy. Base = `05` folded ×8 + R118; shape = `66`'s stacked set when `08` is clean (else `05`'s single-branch shape), branches named with their tips read at PREFLIGHT from the two stop lines; the L68 gate on the COMBINED tree (offline, with-DB under L76 — one `.env` copy into the gate worktree, removed and proven gone before the stop line, no other with-DB run in flight — and live-note BEFORE STEP-6); residents down before the merge (L66), `merge --ff-only`, migration `0013`, `validate`, residents up; then STEP-6's FOUR rows through the note path and the post-STEP-6 live-note run expected GREEN; restarts per `cobalt jobs restarts`; rollback shape named (L54: `git revert -m 2` of the gate merge for the two-branch shape). The WINDOW and P-HIS (`DONE TRADING <hh:mm>` in `cto-2026-09-24.md`, R83) unchanged. `R__L` placeholders for the desk, as `05` has them; the AUTHORIZATION grep for the launch row must expect `TWO BRANCHES: setups/seven-0921 + replay/mover-partial-0924` (or `ONE BRANCH: setups/seven-0921` on the drop path) instead of `05`'s `ONE BRANCH` literal. RULE STRINGS: `66`'s 54 approved allows, 3 denies and `--add-dir` pair are the pool — take from it, ADD NOTHING; if the mover worktree needs a string `66` lacks (it should not: the gate worktree does the merging), list it under `## FOR DEJAN` with the reason and mark the prompt NOT LAUNCHABLE until his word. (2) `prompts/2026-09-24/10-review-setups-deploy-r2.md` — the house read of `09-setups-deploy-r2.md` per his R95: Opus 5.5 + Grok (Sol METER; no Gemini, R96/R97), `06`'s shape and strings byte for byte, report `reports/setups-deploy-r2-review-2026-09-24.md`, stop `SETUPS DEPLOY R2 REVIEW DONE|FAILED`; its grok date gate reads the launch day's desk file for a committed row of HIS naming `10-review-setups-deploy-r2.md` and `Bash(grok *)` — carry that as ASK DESK (the desk brings it to him with the ONE list).

REPORT (LAST): `docs/40 - DevDocs/reports/setups-deploy-r2-draft-2026-09-24.md` — §0 ≤5 lines (branches in the set, `08`'s line quoted, rows count, known red: none) → `## FOLDS` (one row per `06` fold: before → after, line) → `## R118` (every "five" → "four" change, every R107 clause removed, by line) → `## MOVER CARRIES` (ESC 1 / 2 / 5, how each landed) → `## RULE STRINGS` (`comm` of the sorted allow strings of `09` against `66`'s: must be empty on the left) → `## FOR DEJAN` (every string or window he must approve, ONE list; expected: only Grok for `10`) → `## ESCALATE` → last line `SETUPS DEPLOY R2 DRAFTED · branches: <1|2> · folds: 8 · rows: 4 · known red: none · new rule strings: <n> · window: DONE TRADING · restarts: com.cobalt.aset com.cobalt.radar · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `SETUPS DEPLOY R2 DRAFTED` or `FAILED`.
