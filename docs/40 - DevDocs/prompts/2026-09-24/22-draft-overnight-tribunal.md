MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `drc-overnight-tribunal-draft-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/22-draft-overnight-tribunal.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-overnight-tribunal-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven read-only strings of `12` / `21` byte for byte; NO new string) · SESSION: fresh · auto mode on · METER: Anthropic medium; stop within 35 minutes. Nobody sits at this terminal: never ask; `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, safe default, continue.

# DRAFT (do NOT launch) the ROUND-1 prompts of the TRIBUNAL (L67) on `docs/30 - Design/DRC-OVERNIGHT-POSITION-PROPOSAL-2026-09-24.md` — the overnight-position lane of the DRC. LADDER: `S3-P3 · F14`. LAW STEP: proposal DONE (Opus 5.5, `reports/drc-overnight-draft-2026-09-24.md`: `OVERNIGHT LANE PROPOSED · already built: 8 items · gap items: 5 · first-day options: 3 · chunks: 4 · hours: 16 · migrations: 1 · write paths: 4 · open to the tribunal: 11 · owner items: 0 · ESCALATE: 3`) → THIS = the tribunal's round-1 prompts → round 1 → derive → (round 2 if needed) → build.

THIS IS 09-22's DRC TRIBUNAL AGAIN, ON THE LANE PROPOSAL. Your template is `prompts/2026-09-22/39-draft-drc-tribunal.md` and what it produced — `40-drc-tribunal.md` · `41-drc-tribunal-fable-seat.md` · `42-drc-tribunal-derive.md`. Copy those shapes byte for byte wherever the same sentence serves; every rule of `39` binds here unchanged, with these substitutions:
- (1') WHAT HE RULED, and the tribunal does NOT re-open (the design's inputs, verbatim in the packet from the desk files): `cto-2026-09-24.md` R22 (07:56 ET — the lane itself: "We need to design the lane in which last nights DRC informs the next day that there is swing position. You will know night before that position is left open and this is already improted"; O1 = A "for now": the first import is flat UNTIL this lane lands — the proposal's first-day option A retires it on his own words, which is a design consequence, not a re-opening); `cto-2026-09-22.md` R67 (open positions carry; his held-count statement wins mid-trade; DAS reconcile is the truth), R65–R66 (the DRC's content and inputs), R90 (RESOLVE), R93 (a no-trade DRC exists); 09-23 R39 / L28's voice-ordered-edit clause (the widget's confirm shape, for option A's "state your book").
- (2') THE ANTHROPIC SEAT: 09-22 R109 ("Make all Opus 5.5 for now") — the Fable seat and the derive run on **`claude-opus-5-5`**; first lines `FABLE ROW: R109` / `DERIVE ROW: R109`; the row literals the gates grep, from the DESK's launch row for the tribunal (`cto-2026-09-24.md`, the row that names `23-drc-overnight-tribunal.md`): `DRC-OVERNIGHT-POSITION-PROPOSAL-2026-09-24.md` · `Fable seat: yes` · `derive seat: claude-opus-5-5`. NEVER `--model claude-fable-5-1` unless the desk row says so.
- (3') ASTRA: METER until Sat 09-26 06:47 ET (`54`'s probe) — proceed on the Anthropic seat · Grok · Gemini (R96 "A": Gemini is the optional FOURTH seat on a NEW DESIGN tribunal, allowed here; R97 keeps it out of code checks only); the astra string CARRIED on the line, its probe run and recorded, then SKIPPED on METER (L67: a house that produces no ruling spends no round); Astra reads the FINAL Sat. FLOOR: the Anthropic seat + Grok (two houses) — fewer → `FAILED`.
- (5') NO DATED GROK / AGY GATE: `Bash(grok *)` and `Bash(agy *)` are STANDING strings (L62 as amended 2026-09-24; `cto-2026-09-24.md` R17 / R19). The gate is `grep -n "^| R19 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` printing the row with `All 4 house models approved` — never a "through <date>" row (R17: the dated shape was desk practice, retired).
- (7') STAGGER (one Grok / Gemini hub at a time — the house lane): the house hub refuses while any of these reports exists WITHOUT a stop line: `reports/drc-d1-fix-r1-check-2026-09-24.md` (`15`), `reports/voice-v1-check-a-2026-09-24.md` … `-d-` (`16`–`19`), `reports/jev-check-final-2026-09-23.md` (`74`); `35`'s rule incl. "a report that does not exist = not running only if the desk's launch row says `<nn> is not running`". NO deploy-window refusal: this hub reads and writes scratch only, no DB, no vault write.
- (8') PACKET ≤140 KB, MANDATORY + OPEN-AS-NEEDED in reading order (`40`'s layout): the proposal WHOLE; the proposer's report DIGEST, UNPROVEN, ESCALATE; his rulings above VERBATIM from the desk files; `DRC-AUTOMATION-v2-2026-09-22.md` § "Open position carry" and the "Carried positions" row, the `open_positions` unit (≈ `:141`, `:179`) and the E1 row ONLY (by heading, not the whole file); the built code EXCERPTS the proposal cites — `pairing.py:143-174`, `store.py:177-179`, `:220-250`, the B8 pin test — from the worktree `/Users/cobalt/cobalt-wt/drc-d1/` at tip `d1342595` (its branch's report commit `38a70947` is docs only); `S3-EXITS-v3-2026-09-22.md`'s R67 clause; his DRC note's HEADINGS and PROMPT LABELS only (L32: no trade content, no ticker, no price, no P&L, no share count of his in any staged file). Packet under `scratch/tribunal-bars-0920/drc-overnight/r1/` (Write-created; the `mkdir -p scratch/tribunal-bars-0920` string is carried as in `40`).
- QUESTION SET for each house — per item `ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>`: the proposal's 11 "open to the tribunal" items in ITS order, then: (a) FACT BASE — does `## 1. What v2 + D1 already do` hold at `d1342595` (file:line), anything it missed; (b) L1 — is every row of `## 4. Failure modes` fail-loud, is any "assume" left (the proposal's option C is named "not fail-loud" — confirm or refute); (c) L57 — is every carried trade replayable from `carried_from` + `book_sha256` alone; (d) L28 / L40 — the `open_positions` unit: one writer, marker-bounded, create-if-absent, and the morning "state your book" write goes through the voice/L28 confirm shape or through the `/drc` form — which, and is there exactly ONE write path per side effect; (e) L43 / L68 — the landing set: the proposal's ESCALATE 1 says K1–K3 must land WITH D2 + D3 or production runs the flat assumption; is that a real dependency (L72) or can K1 land alone as a no-op until D2/D3; (f) EXPERIMENTS — any claim about `drc_rows` re-pairing all-or-nothing, or the hash's stability across an Obsidian Sync revert (L28's sync-revert clause), that only a run can settle → name it as a first-gate experiment (L70), never argue it.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/`):
- `23-drc-overnight-tribunal.md` — Sonnet 5 hub `drc-overnight-tribunal-0924`, cwd `~/cobalt-wt/agy-trial`, `40`'s shape and its strings byte for byte (the astra string carried; `--model` per seat), folder `scratch/tribunal-bars-0920/drc-overnight/r1/`, gates (5') (7'), the Codex PREFLIGHT probe recorded and skipped on METER (3'), `date` at every completion notice, the 20-minute task-stop, the hub file-checks the Anthropic seat's claims too (its report is read only AFTER the houses finish). Report `reports/drc-overnight-tribunal-2026-09-24.md`; stop line `DRC OVERNIGHT TRIBUNAL R1 DONE · grok: … · gemini: … · astra: METER · houses that ruled: <n> of 3 · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `24-drc-overnight-tribunal-fable-seat.md` — MODEL Opus 5.5 (`claude-opus-5-5`, R109 — say so in the MODEL line), seat `drc-overnight-fable-0924`, `41`'s shape and strings with `--model claude-opus-5-5`; AUTHORIZATION per (2'); BLIND to the houses' folder and to `23`'s report; ONE file; stop line `DRC OVERNIGHT FABLE R1 DONE · verdict: … · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — or `FAILED …`. METER line: an honest token estimate.
- `25-drc-overnight-tribunal-derive.md` — `42`'s shape with `--model claude-opus-5-5` (R109) and `DERIVE ROW: R109`; refuses unless both round-1 stop lines are present and committed; a wording whose supporting claim DOES NOT HOLD or is WITHDRAWN is never folded; his rulings of (1') are never re-opened; writes `docs/30 - Design/DRC-OVERNIGHT-POSITION-v2-2026-09-24.md` and its report `reports/drc-overnight-tribunal-derive-2026-09-24.md` (fold table, `## NEEDS ROUND 2`, `## OWNER ITEMS` — only decisions that are HIS, L67 as amended 2026-09-24; expected none — `## FOR DEJAN` in his terms as ONE approval, no recommendation from a seat that holds a side, L37). Stop line `DRC OVERNIGHT DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-overnight-tribunal-draft-2026-09-24.md`: `## §0 Headline` ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (what each prompt does, the launch order, the stagger reads the desk must make) → PACKET (source → staged path, sizes, token estimate per house) → RULE PROOF (`grep -c -F -e` per string: `23` vs `40`, `24` vs `41`, `25` vs `42`; expected differences: paths, remote-control names, `--model claude-opus-5-5` on `24` / `25`, the folder name) → **`NEW strings:`** (expected: none) → `## L74` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `OVERNIGHT TRIBUNAL PROMPTS DRAFTED · prompts: 3 · astra: METER — proceed on three · questions: <n> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write. L32: no value, rule sentence, ticker, price or P&L of his in any file you write. You commit nothing.
