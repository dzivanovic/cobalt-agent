MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: drafter `handicap-h1-fix-r1-draft-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/59-draft-handicap-h1-fix-r1.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control handicap-h1-fix-r1-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `53-draft-voice-v1-fix-r2.md`'s byte for byte; NO new string) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default, continue. Never end a turn between steps. ≤40 min. Grep patterns carry no backtick, no `\|`, no alternation — plain fixed strings, one per call, or the Read tool.

# FLOAT HANDICAP H1 — FIX ROUND 1: CLASSIFY THE 2 HOLDS OF CHECK ROUND 1 (`45`) PLUS ITS CARRIED ESCALATES (L75), THEN WRITE THE FIX BUILD AND ITS CHECK (ROUND 2 OF ≤3)

LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29` (float handicap, carried under S2's F10; S2 CLOSED 09-23; blocks nothing, L72). LAW STEP (L67 / L75 / L39): design v3 FINAL `docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md` (tribunal closed) → H1 BUILT `27df13f1` on `f6643d41` (`prompts/2026-09-24/43-handicap-h1-build-r2.md`; worktree `/Users/cobalt/cobalt-wt/handicap-h1`, branch `radar/handicap-h1-0922`, tip of the branch = the report commit `026c99b8` above `27df13f1`; migration `0014` proven only inside the with-DB suite's transaction, `cobalt_dev` at `0013`) → check round 1 (`45`, `reports/handicap-h1-check-2026-09-24.md`) STOPPED 22:02 `HANDICAP H1 CHECK DONE · round: 1 · opus: CHECK: FIX — 1) remove dev_db_tx from the five XL76 callers that now also use migrated_radar … or prove the two fixtures are ordered correctly; 2) correct _cut_tier's docstring claim ("the rule X2 tallies") and the render comment, or align the rule with X2's · grok: CHECK: BUILD STANDS · … · defects that HOLD: 2 · ready for a deploy prompt: 1 of 2 · ESCALATE: 14` — **2 HOLDS** (Opus; Grok `BUILD STANDS` with no reasoning against them; the hub's file-check rows decide, L35) → THIS = classify (L75) + write fix r1's build and its check (round 2). H1 is NOT in the 09-25 deploy set until round 2 is clean; the deploy's own acceptance (the production dry-run, `45` ESCALATE 4) stays the deploy's.

AUTHORIZATION (verify before you read anything else): `grep -n "^| R98 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print a row naming `59-draft-handicap-h1-fix-r1.md`. Missing → `FAILED: authorization mismatch — R98` as the last line of your report and stop.

INDEX CARD: `LAWS.md` in full (L59) — **L1**, **L7** (nothing of the factor reaches an order or a card number — H1 is SHADOW), **L29** (the fix touches `src/cobalt/radar/*` and tests on a DB write path — the floor is Opus), **L32** (nothing of his in any file you write), **L35** (every claim `file:line`), **L42** (restarts by rule), **L45**, **L62** (STANDING STRINGS R17 / R19; `43`'s `.env` pair for `handicap-h1` is APPROVED — `cto-2026-09-24.md` R84–R87 launch rows; carry `43`'s two `.env` strings byte for byte, they are NOT new), **L67** (a fix round = Opus 5.5 · Sol · Grok; Sol METER until Sat 09-26 06:47 ET → Opus + Grok), **L68** (GATE EARLY: offline + with-DB + live-note on the build's own tree before BUILT; the check packet carries the three executed results), **L70**, **L75** (FIX rows only, nothing widens), **L76** (one lock; `0014` applied ONLY inside the suite's own rollback transaction — never `cobalt db migrate` on `cobalt_dev` in any spelling; the `.env` copied by name and removed before the stop line). Files: `reports/handicap-h1-check-2026-09-24.md` WHOLE — `## FOR THE CLASSIFIER` (the two HOLDS: (1) five XL76 callers keep `dev_db_tx` beside `migrated_radar` — `tests/cobalt/test_cards_picks.py:330,332,343,353,363`, `tests/cobalt/test_radar_panel.py:1223,1225`, "harm NOT CHECKABLE FROM READS — with-DB suite ran green in this state"; (2) `_cut_tier`'s docstring and the `render` comment claim X2's rule — `src/cobalt/radar/handicap_dry_run.py:335-337`, `tests/experiments/handicap_h1/test_h1_x2_marginal_seat.py` `_tier`), `## Checked against the branch`, `## Ready for a deploy`, `## ESCALATE` (items 2 X4 / X5 partial results Opus reads as UNPROVEN; 3 XL76 (A); 6 ranked LEAVEs store no record — HOLDS in the hub's file-check, Opus's question "keep the `rank_metric` precedent, or write / clear the three columns on ranked LEAVEs"; 7 v3 §3 "replay-from-bars reports `handicap: not replayable from bars`" NOT BUILT — HOLDS; 9 a stale L52 (c) sentence in the shipped v3 file; 10 the identity / shadow tests compare branch vs branch); the H1 build report `/Users/cobalt/cobalt-wt/handicap-h1/docs/40 - DevDocs/reports/handicap-h1-build-2026-09-24.md` WHOLE (its XL76 section, `## ESCALATE` 1–12, the four ASK DESK items and the desk's records `cto-2026-09-24.md` R89); `docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md` (every FIX traces to a FINAL clause — quote §3's replay sentence, §4's row for LEAVE / RETAIN / the three columns, X2's marginal-seat rule and the dry-run's tally, §9's H1 row); `prompts/2026-09-24/43-handicap-h1-build-r2.md` + `45-handicap-h1-check-r2.md` (the shapes you copy BYTE FOR BYTE except the round, the base, the rows and the paths — `43`'s GATE EARLY steps, XL76 / L76 steps, allow strings and permission mode quoted verbatim in the SEAT prose; `45`'s 14 strings, probes, STAGGER literal `no other house hub is running`, ≤15,000-byte Write parts); `prompts/2026-09-24/48-drc-k1-fix-r1-build.md` + `49-drc-k1-fix-r1-check.md` (the most recent FIX-ROUND pair: red-first FIX rows, the RUN shape for UNPROVEN rows, the `git log --stat` path gate, "the fix widened" preflight); the desk's readings in `cto-2026-09-24.md` R89 (the four ASK DESK answers: the builder's safe defaults STAND — they are records, not rulings; never re-open them) and R96 (this round's launch record).

DECIDE (L75, the two HOLD rows and every carried ESCALATE of `45`): FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM, from the hub's own file-check column, each row citing its source (`<report>:<line>`) and the FINAL clause or check rule it traces to. Expected shapes, to be confirmed by the rows themselves: **HOLD 1 (`dev_db_tx` stacked beside `migrated_radar` in five callers)** = FIX if the two fixtures open two transactions on `cobalt_dev` or the prompt's "moved" claim is false in the code — red first: a test (or a fixture assertion) that a membership test runs on ONE harness, then the five callers moved to `migrated_radar` alone; if a read shows the stacking is harmless by construction (one fixture yields the other's connection), NOT REAL with the lines quoted — never "prove ordering" by argument (L70: a RUN inside the build proves it, or it is a FIX). **HOLD 2 (`_cut_tier` docstring / `render` comment claim X2's tally)** = FIX: either the docstring and comment state the rule the code implements, or the rule is aligned with X2's — choose from v3's X2 clause quoted; a docstring fix is still red first (a test that the dry-run's tier tally equals the rule the FINAL names, or a doc-only FIX row stated as such). **ESCALATE 7 (v3 §3 `handicap: not replayable from bars` not built)** = FIX if v3 §9's H1 row or §3 assigns it to H1 (quote), else OUT OF SCOPE for H2 / H3 with the clause quoted. **ESCALATE 6 (ranked LEAVEs keep the previous scan's record)** = classify from v3 §4's row for LEAVE: FIX (write or clear the three columns on ranked LEAVEs, red first) if the FINAL says a LEAVE row carries no record; NOT REAL if the FINAL keeps the `rank_metric` precedent; UNPROVEN → a RUN if the FINAL is silent — the desk chooses no code (R96). **ESCALATE 2 (X4's `pool_error` text not printed; X5's stored half has 0 receipts on `cobalt_dev`)** = UNPROVEN → named RUNS inside the build whose output the report quotes, never a FIX. **ESCALATE 9 (stale L52 (c) sentence in the shipped v3 file)** = FIX, docs-only, one line, if the branch ships that file; else OUT OF SCOPE. **ESCALATE 3, 4, 5, 10** = OUT OF SCOPE (XL76 (A) stands; the production dry-run is the deploy's; the four ASK DESK records stand; branch-vs-branch identity is the check's limit, the deploy's dry-run is the comparison). Nothing widens beyond the rows that HOLD; a row you cannot settle from the report's and the FINAL's own words is UNPROVEN or OWNER ITEM, never guessed. Grok's `BUILD STANDS` is recorded in the table as the dissent, not smoothed (L39).

WRITE (whole files, L19): (1) `prompts/2026-09-24/60-handicap-h1-fix-r1-build.md` — `43`'s shape and launch line BYTE FOR BYTE except path, remote-control `handicap-h1-fix-r1-build-0924`, the base `radar/handicap-h1-0922` on its tip (read it: `git -C /Users/cobalt/cobalt log -1 --format=%h radar/handicap-h1-0922` must print `026c99b8` — the build-report commit above the build tip `27df13f1`, read by the desk 22:04; a different tip → `ASK DESK`, write the tip you read), the same two `.env` strings (R84–R87), the permission mode `43` states quoted verbatim in the SEAT prose; NO `db migrate` string of any spelling, NO `uv add`, NO scratch python; FIX rows only, red first, the UNPROVEN rows as named RUNS whose output the report quotes; ALL THREE suites (L68) under L76 before its stop, `0014` inside the suite's transaction only, the absence probe after; every grep pattern a plain fixed string; "never end a turn between steps"; stop `HANDICAP H1 FIX R1 BUILT <tip> | on 026c99b8 | red <hash> | offline <p>/<f> | with-DB <p>/<f> | live-note <p>/<f> | .env: removed | 0014: rolled back | cobalt_dev: 0013 | FIX: <n> of <n> | RUNS: <n> | ESCALATE: <n>` or `FAILED: <step> — <reason>`. DESK LINE in its header: the desk launches it only while no `~/cobalt-wt/*/.env` exists and no with-DB run is in flight (L76) — it QUEUES on the lock behind `57` (replay fix) and `51` (K2) in the desk's order, first ready first. (2) `prompts/2026-09-24/61-handicap-h1-fix-r1-check.md` — `45`'s shape, ROUND 2 OF ≤3, ONE packet (the fix diff `026c99b8..<tip>`, the RUN outputs, the build's executed output of all three suites), checkers Opus 5.5 + Grok (Sol METER → record its return `Sep 26th, 2026 6:47 AM`; the desk seats it from then), fail closed below TWO; grok gate = `grep -n "^| R17 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` (his standing word), NEVER a dated row; `45`'s strings byte for byte; the STAGGER literal `no other house hub is running` on a launch row naming `61-handicap-h1-fix-r1-check.md`; the hub stages every Write part under 15,000 bytes (R79); stop `HANDICAP H1 FIX R1 CHECK DONE · round: 2 · opus: … · grok: … · defects that HOLD: <n> · ready for a deploy prompt: <r> of <n> · ESCALATE: <n>` or `FAILED: …`. Both prompts carry `R__` launch-row placeholders for the desk and a `comm`-checkable RULE STRINGS sentence naming the precedent line each copies (`43` / `45`). `## FOR THE DEPLOY` in `60`: what changes for the stale-score + H1 deploy drafter (nothing new expected beyond the fix tip).

REPORT (LAST): `docs/40 - DevDocs/reports/handicap-h1-fix-r1-draft-2026-09-24.md` — `## §0 Headline` ≤5 lines → `## L74` (one line if the block appears) → the classification table (the 2 HOLDS + ESCALATE 2, 6, 7, 9 + every other `45` ESCALATE: `# · claim (short) · source line · class · traces to (FINAL clause quoted) · FIX shape or reason · grok's answer`) → `## OWNER ITEMS` (verbatim, for him, one per line — only decisions that are HIS, L67 owner-items clause; expected: none) → `## FOR DEJAN` (new strings: expected NONE — the `.env` pair is `43`'s) → `## ESCALATE` → last line `HANDICAP H1 FIX R1 DRAFTED · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · prompts: 2 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `HANDICAP H1 FIX R1 DRAFTED` or `FAILED`.
