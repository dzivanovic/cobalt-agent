MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (fold of a production deploy prompt) · SEAT: drafter `setups-deploy-r2-fold-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/20-fold-setups-deploy-r2.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control setups-deploy-r2-fold-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `71`'s byte for byte) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic small · never ask: `ASK DESK: <question> [<time>]` in the report, continue. Never end a turn between steps. **≤15 min.**

# FOLD THE L67 READ OF `09` INTO A FULL RE-ISSUE OF `09` IN PLACE (L19) — FIVE FOLDS, ZERO BLOCKERS, ZERO STRINGS WIDENED

LAW STEP: `10` stopped `SETUPS DEPLOY R2 REVIEW DONE · houses: 2 of 2 · other houses: 1 of 1 · blockers: 0 · folds: 5` (`reports/setups-deploy-r2-review-2026-09-24.md`, `## Folds proposed` 1–5 and `## Checked against the files`). The desk took ALL FIVE (`cto-2026-09-24.md` R24): ESC 1 → fold 1, ESC 2 → fold 2, folds 3–5 as proposed. One round only; folds are not re-read (`10`'s own rule).

INDEX CARD: `LAWS.md` — L19, L35, L54, L62 / L63, L66, L67, L68, L71, L76. Files: `prompts/2026-09-24/09-setups-deploy-r2.md` (whole — re-issue IN PLACE), the `10` report (whole), `prompts/2026-09-23/71-fold-smoke-only-deploy.md` (the fold shape to follow).

DO: each fold exactly as proposed, the smallest text change backed by its `## Checked against the files` row, at the line the fold names (re-locate by text if the line moved): (1) the DESK LINE (`09:5`) hold extended past a BETWEEN stop line; (2) THE RELAUNCH RULE (`09:291`–`:292`): `git -C /Users/cobalt/cobalt status` added to the opening calls, and (i)'s interrupted-revert branch (residents DOWN, named; PROVE IT / `revert --abort`; end FAILED); (3) WHAT SHIPS (`09:23`): `(`c212645f`)` → `(`de88c302`)`; (4) STEP-2.3 (a) (`09:247`): the second lock read (`ls -la /Users/cobalt/cobalt-wt/*/.env` → "no matches found") before `cp`; (5) RELAUNCH (iii) / (iv) (`09:294`–`:295`): re-read and record P14's four log baselines before resuming at 4.1. NO rule string added or widened (`git -C /Users/cobalt/cobalt status*` and `ls *` are on `09`'s line — confirm each by `grep -c -F`); keep every `R__L` for the desk; keep the `TWO BRANCHES: setups/seven-0921 + replay/mover-partial-0924` literal and every authorization grep as they are.

REPORT (write LAST): `docs/40 - DevDocs/reports/setups-deploy-r2-fold-2026-09-24.md` — one row per fold (before → after, line) · `## RULE STRINGS` (`grep -o '"Bash([^"]*)"' 09 | sort -u | wc -l` before and after = equal; the two prefixes used by folds 2 and 4 quoted from `09`'s own line) · `## ESCALATE` · last non-blank line: `SETUPS DEPLOY R2 FOLDED · folds applied: 5 · strings widened or added: 0 · ESCALATE: <n>` or `FAILED: <step> — <reason>`.
