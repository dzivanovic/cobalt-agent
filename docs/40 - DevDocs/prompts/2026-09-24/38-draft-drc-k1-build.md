MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: drafter `drc-k1-build-draft-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/38-draft-drc-k1-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-k1-build-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `12-draft-drc-d1-fix-r1.md`'s byte for byte; NO new string) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default, continue. Never end a turn between steps. ≤45 min.

# DRC OVERNIGHT-POSITION LANE — WRITE THE K1 BUILD (BOOK RECORDS + THE `cobalt drc state-book` CLI) AND ITS NEW-BUILD CHECK, FROM v3 + HIS R51 + R52

LADDER: `S3-P3 · F14` (DRC). LAW STEP (L67): PROPOSED (`21`) → TRIBUNAL round 1 (`23` / `24` / `25` → v2) → round 2 (`27` / `28` / `29` → v3, `docs/30 - Design/DRC-OVERNIGHT-POSITION-v3-2026-09-24.md`) → the two `OPEN FOR DEJAN` blocks RULED by him: **R51** (R2-1 = "A": an earlier day's first file after a later stated day REBUILDS the later days from the earlier close, his statement kept as history in `drc_stated_books`, the difference shown on `/drc`; sub-item (iii) = A1, the newest un-superseded file's fills — desk reading) and **R52** (R2-2 = "B" on both halves: `via` literal **`cli`** — `text CHECK IN (drc_page, voice_widget, cli)`, one `DrcStore.record_stated_book(…, via=…)` behind every caller (L3); the `cobalt drc state-book` CLI covers `opening` / `no_trade` / `resolve`, is DRY-RUN BY DEFAULT printing the row and its `book_sha256`, writes only with `--apply --sha256 <hash>`, logged under L7's interim clause). THE TRIBUNAL IS CLOSED (R52: "v3 + R51 + R52 = the lane's FINAL input"; Astra reads the FINAL Sat 09-26 06:47 ET as an after-check, `ASTRA PENDING` marks kept). → **THIS = write the K1 BUILD prompt and its CHECK prompt.** K1 is the first chunk (v3 §6: `drc_stated_books`, `drc_rows.kind` + `seed` / `book_close`, `record_stated_book` refusing inside `market_reset` `[F-01]`, `seed_for`, `record_day` writing `seed` / `book_close`, `carried_from`, first import without a statement → `not computed`, `FN_VERSION` → `drc.pairing/2`, the B8 pin test replaced, the CLI `[F-10]`; ONE new migration). K2 (`[F-27]`–`[F-29]` under R51) is the NEXT drafter's, after K1 is BUILT — not this file's.

AUTHORIZATION (verify before you read anything else): `grep -n "^| R61 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print a row naming `38-draft-drc-k1-build.md`; `grep -n "^| R51 \|^| R52 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print two rows. Either missing → `FAILED: authorization mismatch — <which>` as the last line of your report and stop.

INDEX CARD: `LAWS.md` in full (L59) — **L1** (a missing carried position is a loud FAILED, never a silent flat), **L3** (ONE `record_stated_book` behind every caller), **L7** (the CLI's `--apply --sha256` is the interim HITL shape — dry-run prints, his chat word + the hash approve, the same as `settings load --apply`), **L28** (K1 writes NO vault note — the `open_positions` unit is K3's; a K1 step that touches a note is OUT OF SCOPE), **L29** (a migration + a DB store = write path; the floor is Opus; the mode is the one `14` / `53` state — quote it verbatim, prose and line never disagree, L62), **L32** (nothing of his — ticker, price, position — in any file you write; fixtures real-shape with dates and attribution stripped, L45), **L35** (every claim `file:line`; the build's claims are proven by its own executed output), **L57** (every derived value ships with its stored inputs — `seed.source`, `from_book_sha256`, `stated_book_id`), **L67** (a NEW BUILD's check seats Fable · Astra · Grok — the Anthropic seat runs as 09-22 R109 pins it, Opus 5.5; Astra on METER until Sat 09-26 06:47 ET → Opus 5.5 + Grok, floor met, Astra's return recorded), **L68** (GATE EARLY: offline + with-DB + live-note on the build's own tree before BUILT, the stop line quotes all three; the check packet carries them), **L70** (the first-gate experiments X1–X6 RUN before the chunk's code is written; a result that changes the design is an ESCALATE with the row's own words, never argued), **L72** (this lane holds no other lane; the seam with D2 / D3 — `pair_day` called from the route — is `[F-17]`'s: the build prompt states it as a contract, never a located call), **L76** (one lock for `cobalt_dev`: `.env` copied only when no other `~/cobalt-wt/*/.env` exists and no with-DB run is in flight, removed and proven gone before the stop line; the new migration applied ONLY inside the suite's own rollback transaction, never `cobalt db migrate` on `cobalt_dev` — 09-23 R71 is why). Files: v3 WHOLE (`§3 Data model and write paths` — the table shapes, the CHECK literals, the hash; `§4 Failure modes`; `§6 Chunks`; `§7`; `First-gate experiments` X1–X6 with their pass conditions; `[F-01]`, `[F-02]`, `[F-04]`, `[F-05]`, `[F-09]`, `[F-10]`, `[F-17]`, `[F-31]`); his two rows R51 / R52 verbatim (they resolve v3's two `OPEN FOR DEJAN` blocks — carry their text into the build's rules; you do not edit v3); the D1 code the chunk extends, IN THE WORKTREE `/Users/cobalt/cobalt-wt/drc-d1` at its tip (read it: `git -C /Users/cobalt/cobalt log -1 --format=%h drc/d1-trading-log`; expected `38a70947` after fix round 1) — `src/cobalt/drc/store.py`, `pairing.py`, `models.py`, `src/cobalt/db_migrations/0016_drc.sql` + `.rollback.sql`, `src/cobalt/db_migrations/placement.py`, `tests/cobalt/test_drc_*.py`; `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` (the parent design; `:171`, `:175-181`, `:183`); the shapes you copy: `prompts/2026-09-23/53-drc-d1-build.md` (the D1 build — its worktree-add line, PREFLIGHT, BASELINE, migration + rollback steps, report shape, stop-line shape) and `prompts/2026-09-24/14-drc-d1-fix-r1-build.md` (the newest build on this worktree — its GATE EARLY and L76 steps and its allow strings, which already carry the `drc-d1` `.env` pair, approved R22); for the check, `prompts/2026-09-23/54-drc-d1-check.md` (a NEW-BUILD check on this lane — its packet shape and strings) and `prompts/2026-09-24/15-drc-d1-fix-r1-check.md` (the newest check strings; the grok gate as R17's standing row); `docs/40 - DevDocs/reports/drc-d1-fix-r1-build-2026-09-24.md` (in the worktree — what the tip holds, its ESCALATE 2 and 4).

DECIDE, before writing (each a line of your report with its `file:line` or v3 `[F-nn]`): (1) THE BRANCH — K1 continues on `drc/d1-trading-log` in `/Users/cobalt/cobalt-wt/drc-d1` (v3 §6 "D1 merged" is the deploy order, not the branch; D1 is BUILT + CHECKED and unmerged — building K1 on its tip keeps ONE branch for the DRC stack's deploy, L46 / L68 SCOPE) unless a v3 clause forbids it — state which you chose and why; if a new worktree is needed, its `worktree add` is the DESK's line in the header (as `53` did), never the builder's. (2) THE MIGRATION NUMBER — the next free across EVERY branch: `main` holds `0013` as its highest, `drc/d1-trading-log` `0016`, `voice/v1-0923` `0017`, `bars/chunk-2-0920` `0012`, `radar/handicap-h1-0922` claims `0014` in `47`'s prompt, `cards/stale-score-0922` may claim one (read `prompts/2026-09-23/46-stale-score-build.md`) — read each with `git -C /Users/cobalt/cobalt ls-tree --name-only <branch> src/cobalt/db_migrations/` and the two prompts, then pin the number (`0018` expected) and list what you read; X5 gates any fold into `0016` (`[F-02]`: unchanged CHECK → a new numbered file, never the fold — so the new file is the default). (3) THE SCOPE — K1 exactly per v3 §6 row K1 + `[F-10]`'s CLI + R52's `cli` literal and dry-run shape + R51's history rows (`drc_stated_books` keeps his statement when a rebuild differs; the rebuild itself is K2); nothing of K2 / K3 / K4; the B8 pin test replaced; `FN_VERSION` bumped. (4) THE SEAM — `[F-17]`: D2's route calls `pair_day` / `record_day`; the build prompt names the contract (signature, the `seed` argument, the refusal inside `market_reset`) as text D2's builder will cite; no D2 file is touched. (5) THE EXPERIMENTS — X1–X6 run FIRST on `cobalt_dev` under the lock (X5 on a `cobalt_dev` where `0016` is applied inside the suite's transaction), each with v3's pass condition quoted; a design-changing result = `ESCALATE` + the build STOPS at that row (`FAILED: X<n> — <v3's "result that changes the design">`), never re-designed by the builder. (6) THE CHECK'S SEATS — a NEW BUILD: Opus 5.5 (the Anthropic seat as R109 pins it) + Grok; Astra METER (return `Sep 26th, 2026 6:47 AM`, the desk seats it from then); NO Gemini (09-23 R97: no code check); fail closed below TWO.

WRITE (whole files, L19): (1) `prompts/2026-09-24/39-drc-k1-build.md` — `14`'s launch line BYTE FOR BYTE except path and remote-control `drc-k1-build-0924` (the `drc-d1` `.env` pair is already his, R22; if you chose a new worktree, the re-pathed pair = TWO NEW STRINGS for his ONE list and the desk's `worktree add` line in the header); the permission mode `14` states, quoted; Opus 5.5; the DESK LINE (launch only while no `~/cobalt-wt/*/.env` exists and no with-DB run is in flight, L76); PREFLIGHT (the tip, the migration number free on every branch, the lock, `cobalt_dev` at production's head), BASELINE (D1's three suites green on the tip — the numbers), EXPERIMENTS X1–X6 (their outputs quoted), then the build red-first per v3 §6 K1 (a failing test per behaviour, then the code), the migration + rollback pair (`[F-02]`; `placement.py` row), the CLI (`[F-10]`, R52: dry-run default, `--apply --sha256`), `FN_VERSION`, the B8 replacement; ALL THREE suites (L68) under L76 before the stop; `cobalt jobs restarts <base>..<tip>` line (L42); stop `DRC K1 BUILT <tip> | on <base> | migration <nnnn> | X: <n> of 6 run, design-changing: <n> | offline <p>/<f> | with-DB <p>/<f> | live-note <p>/<f> | .env: removed | <nnnn>: rolled back | RESTARTS: <line> | tests added: <n> | ESCALATE: <n>` or `FAILED: <step> — <reason>`; report `/Users/cobalt/cobalt-wt/<worktree>/docs/40 - DevDocs/reports/drc-k1-build-2026-09-24.md` committed on the branch, with `## SEAM FOR D2` (the `[F-17]` contract, verbatim for D2's drafter) and `## FOR K2` (what K2 reads from this build). (2) `prompts/2026-09-24/40-drc-k1-check.md` — `54`'s shape with `15`'s strings (byte for byte; grok gate = `grep -n "^| R17 " …` R17's standing row, NEVER a dated row), round 1 of ≤3 of a NEW BUILD, ONE packet: the diff `<base>..<tip>`, the migration pair, the X1–X6 outputs, the three suites' executed output, v3 §3 / §6 K1 / `[F-01]` `[F-02]` `[F-04]` `[F-05]` `[F-10]` and R51 / R52's text as the rules; checkers Opus 5.5 + Grok (Astra METER, recorded); fail closed below TWO; `## FOR THE CLASSIFIER` for round 2 (L75); stop `DRC K1 CHECK DONE · round: 1 · opus: … · grok: … · astra: METER … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for K2: <YES|NO> · ESCALATE: <n>` or `FAILED: …`. Both prompts carry `R__` launch-row placeholders for the desk and a `comm`-checkable RULE STRINGS sentence naming the precedent line each copies.

REPORT (LAST): `docs/40 - DevDocs/reports/drc-k1-build-draft-2026-09-24.md` — `## §0 Headline` ≤5 lines → `## L74` (one line if the block appears) → `## DECISIONS` (the six above, each with its source) → `## MIGRATION NUMBER` (every branch read, the number pinned) → `## SEAM` (`[F-17]` as the build prompt states it) → `## OWNER ITEMS` (verbatim, for him — expected NONE: v3 §8 and R51 / R52 settle the lane; a decision that is genuinely HIS goes here, one per line, never a design question) → `## FOR DEJAN` (new strings: expected 0 — the `drc-d1` pair is his R22) → `## ESCALATE` → last line `DRC K1 BUILD DRAFTED · branch: <name> · migration: <nnnn> · experiments: 6 · prompts: 2 · new rule strings: <n> · owner items: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `DRC K1 BUILD DRAFTED` or `FAILED`.
