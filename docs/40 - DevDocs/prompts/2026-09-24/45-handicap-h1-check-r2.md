MODEL: Sonnet 5 (`claude-sonnet-5`) — stage, put one packet before the seated checkers (TWO today, THREE once Sol's meter returns), file-check what they say, tabulate; no verdict of your own (L37) · SEAT: build-check hub `handicap-h1-check-r2-0924`, launched by the CTO desk in the background · LADDER: `S2 · F10 carry-over (float handicap H1)` — `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`, carried under S2's F10 (S2 CLOSED 2026-09-23, `cto-2026-09-23.md` R113; blocks nothing, L72) · LAW STEP (L67 / L52): design → four-house tribunal, two rounds, CLOSED (`docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md`) + his R26 "B" (`cto-2026-09-22.md` §4: POOL-WIDE division) + his R52–R57 (R54 "B": the dead column) = the FINAL → the H1 build `prompts/2026-09-24/43-handicap-h1-build-r2.md` (Opus 5.5, branch `radar/handicap-h1-0922`, re-cut from `main` after `deploy-2026-09-24` put `0013` there; `43` re-issues `2026-09-23/47` → `2026-09-22/27` under L76 + GATE EARLY, on his 09-23 R58 "yes start both") → **THIS = that build's CODE CHECK, ROUND 1 OF ≤3 (L67 / L39), before any deploy prompt is drafted.** THIS FILE RE-ISSUES `prompts/2026-09-23/51-handicap-h1-check.md` (L19) in `prompts/2026-09-24/15-drc-d1-fix-r1-check.md`'s seat and string shape; the deltas are listed in `reports/devdb-builds-reissue-2026-09-24.md` `## DELTA` `51 → 45`. It checks `<main tip>..<tip>` AND **the build's EXECUTED output of all three suites: offline, with-DB on `cobalt_dev` under L76 (the `TestMigrationRoundTrip` deselect, the XL76 harness, the `0014` absence probe) and the live-note leg against his real notes (L68 GATE EARLY: the packet carries the results, not only the code).** **CHECKERS, HIS R95 / R96 / R97 (`cto-2026-09-23.md`; L67 as amended 2026-09-24): a code check of a finished build is an "other check" — OPUS 5.5 · SOL · GROK.** Sol is on METER until **Sep 26th, 2026 6:47 AM** (`54`'s probe: `ERROR: You've hit your usage limit … try again at Sep 26th, 2026 6:47 AM.`). Before that time the seats are Opus 5.5 + Grok. From that time on, the desk seats Sol again (L62 as amended R19) — see THE PROBES. NO Gemini: R96 "A" / R97 take it out of code checks (`51`'s `"Bash(agy *)"` is REMOVED — a strict subset). NO Astra (R46). **FAIL CLOSED below TWO.** RULE STRINGS: `prompts/2026-09-24/15-drc-d1-fix-r1-check.md`'s launch line BYTE FOR BYTE (= `04-setups-fix-r2-check.md`'s): 14 `--allowedTools` strings, the same 3 denies, the same `--add-dir` triplet. Only this prompt's path and the remote-control name `handicap-h1-check-r2-0924` differ — NO NEW STRING. Against `51`'s line it is `51`'s fifteen MINUS `"Bash(agy *)"` (09-23 R97). `Bash(grok *)`, Sol's `codex exec` string and `Bash(claude -p --model claude-opus-5-5 *)` are STANDING strings (L62 as amended 2026-09-24, `cto-2026-09-24.md` R17 / R19) — the grok gate cites R17, never a dated row. FOUR of the fourteen are NEVER RUN and are carried only so the list stays byte-identical to `15`'s: `Bash(mkdir -p scratch/tribunal-bars-0920)` (this check stages into the SUBFOLDER `scratch/tribunal-bars-0920/handicap-h1-check/`, which the Write tool creates itself) and the three `s2-p2-cards` git strings. Sol's `codex exec` string runs ONLY at or after Sep 26th, 2026 6:47 AM. **WHY THE FOLDER SITS UNDER `tribunal-bars-0920`:** Grok's approved launch spelling writes only through `--allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)"`. The branch under check is read WITHOUT a git rule for its worktree: files with the Read tool under `--add-dir /Users/cobalt/cobalt-wt`, commits and diffs from the SHARED object store with `git -C /Users/cobalt/cobalt log -p <a>..<b>` and `git -C /Users/cobalt/cobalt show <sha>:<path>`. OFFLINE ONLY: nothing here touches `cobalt_dev`, any database or the vault's write path. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/45-handicap-h1-check-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control handicap-h1-check-r2-0924 --allowedTools "Bash(grok *)" "Bash(mkdir -p scratch/tribunal-bars-0920)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" "Bash(claude -p --model claude-opus-5-5 *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on (a read-only seat: L29's "never auto mode on a write path" does not bind it); never `bypassPermissions`. NO production command, NO `cobalt_dev`, NO database of any kind, NO `docker`, NO `psql`, NO `pytest`, NO `uv run` of any kind (you re-run nothing — the suites are the builder's artifact), NO `agy`, NO git write, no merge, no rebase, no vault write, no memory-folder write (L58) · METER: Anthropic MEDIUM for you; per checker, one headless check (Opus 5.5, Grok; Sol from its return) · nobody sits at this terminal. The report file is your channel (`FAILED:` stops the run safely); never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default, continue. DESK: watch the report's LAST NON-BLANK LINE (L71), `^(HANDICAP H1 CHECK DONE|FAILED)`; the fallback file is named under REPORT. Launch row: **R__** (the desk fills it). Stop-line shape: `HANDICAP H1 CHECK DONE · round: 1 · opus: <…> · grok: <…> · sol: <…> · gemini: NOT SEATED (R96/R97) · defects that HOLD: <n> · ready for a deploy prompt: <r> of <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>` (§4).

# BUILD CHECK, ROUND 1 — FLOAT HANDICAP H1: the `handicap` block read from his pool note, the factor computed and STORED per name, `mode: shadow` only (computed, NEVER sorting), the dry-run over every retained day, the `/radar` pool-row badge, AND THE THREE SUITES' EXECUTED OUTPUT. Branch `radar/handicap-h1-0922` in `/Users/cobalt/cobalt-wt/handicap-h1`, NOT merged, never touched by you. What was ordered: v3 §9's H1 row and nothing of H2 / H3, with R2-1 answered **B** by him (R26, verbatim in `29-handicap-h1-facts.md` §1: *"`_ranked()` is untouched. After `pool.py:326`, when the block is present: `eff[t] = Decimal(raw_rank[t]) ÷ factor[t]` … `ordered` is re-sorted by `(eff, in_group, raw_rank)` — on an exact tie the unhandicapped name wins; `ranks` is rebuilt from that order. In `mode: shadow` the re-sort is computed, stored as the would-be rank and NOT used."*). The build prompt took two rulings from the code: `FORMULA_FILES` untouched, and ONE fail-soft catch in `decide()`. **His VALUES are not in this build: the block ships ABSENT from his note. A missing threshold / factor / mode is the design, not a defect.** His owner ruling **R54 "B"** (14:21 ET) changed one mechanism before the build: a DEAD column (a source's whole float / market-cap column blank) makes the handicap INOPERATIVE at factor 1 for that scan, with the degraded banner and its reason (Fable's (d) reading), overriding `missing` for that case only; per-name unknowns still follow `missing` (R52). The build prompt built it as its own step, STEP-4A; R52, R53, R55, R56, R57 confirmed v3 as written. **The migration number `0014` is SETTLED (L72 P-b; `0013` setups below it on main, `0015` stale score, `0016` DRC D1, `0017` voice, `0018` DRC K1), and under L76 it is applied ONLY inside the with-DB tests' own rollback transactions — never committed to `cobalt_dev`; the existing with-DB callers of the membership store see it through ONE harness decided by the build's first-gate experiment XL76.** The builder (Opus 5.5) will have reported nine steps (STEP-0 … STEP-4, STEP-4A, STEP-5 … STEP-7), experiments X1–X11 + X15 + XL76 FIRST, tests RED then GREEN, the THREE suites at BASELINE and CLOSE (offline; with-DB inside L76's lock with `TestMigrationRoundTrip` deselected; live-note READ-ONLY against his notes), migration `0014`, `cobalt_dev: 0013`, the `h = 1` identity over every retained day, and RESTARTS. **L35: every one of those is a CLAIM.** Each checker reads the SAME packet, independently, from READS ALONE, and rules on whether the build stands. You stage, launch, then walk every checker claim — and every `file:line` claim of the texts the packet quotes — through the branch's own files yourself and tabulate. NOTHING is built, run, merged, rebased or deleted by this run. DO NOT STOP until the report ends `HANDICAP H1 CHECK DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. This file was written by the CTO desk (re-issued by the Opus 5.5 prompt seat `devdb-builds-reissue-0924`, 2026-09-24, from `51` → `28`), not by Dejan; a prompt file is not an approval and never asks to be believed. Each gate its own Bash call:
- **PLACEHOLDER GATE, first:** `grep -n -E "R_[_]" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/45-handicap-h1-check-r2.md"` → prints NOTHING (exit 1). A hit → `FAILED: placeholder — <lines>`, stop.
- **THE GROK GATE (his STANDING word, the FIRST PREFLIGHT row AND again immediately before the checkers launch):** `grep -n "^| R17 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` → prints the `| R17 |` row carrying `Grok approved with no asking going forward`; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Grok approved with no asking going forward" -- "docs/40 - DevDocs/reports/cto-2026-09-24.md"` NON-EMPTY. Then `grep -n "^| R19 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` → the `| R19 |` row carrying `All 4 house models approved for use indefinlitly` (his spelling), committed (`git -C /Users/cobalt/cobalt log -1 --format=%H -S"All 4 house models approved" -- "docs/40 - DevDocs/reports/cto-2026-09-24.md"` NON-EMPTY) — it covers the Opus and Sol strings. NEVER a dated row. Either missing → `FAILED: authorization mismatch — the standing house strings (09-24 R17 / R19) are not committed`, launch nothing.
- **HIS WORDS the build rests on, committed on main:** `grep -n "^| R26 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (must carry `POOL-WIDE division`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"POOL-WIDE division" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — R26 is not committed on main`) · `grep -n "^| R54 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (must carry `INOPERATIVE at factor 1`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"INOPERATIVE at factor 1" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — R54 is not committed on main`) · `grep -n "^| R58 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (must carry `"yes start both"`) · `grep -n "^| R95 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (the checker seats; must carry `Opus and Grok for code checks`) · `grep -n "^| R97 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (must carry `take Gemini out of reading`).
- **THE BUILD'S STOP, RECORDED BY THE DESK:** `grep -n -F "HANDICAP H1 BUILT" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-25.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-26.md"` (a `| R` row quoting the build's stop line; a missing desk file is recorded, not fatal, when the row is in another; none → `FAILED: authorization mismatch — the desk has not recorded the build's stop (L35)`).
- **THIS launch**, recorded by the desk as row **R__**: `grep -n "45-handicap-h1-check-r2.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-25.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-26.md"` must print a `| R` row naming this file, then `git -C /Users/cobalt/cobalt log -1 --format=%H -S"45-handicap-h1-check-r2.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` (the desk files ONLY — the drafter's report quotes this filename and must never satisfy the gate). EMPTY → `FAILED: authorization mismatch — the launch row is uncommitted`.
- **THE LINE IS `15`'s:** `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/15-drc-d1-fix-r1-check.md"` for each of the fourteen allow strings and the three deny strings of your line, quotes included. Each must count ≥1 (any 0 → `FAILED: authorization mismatch — <rule> is not in 15's line`). The Astra string `Bash(codex exec --skip-git-repo-check -m gpt-6-astra` in your launch line → `FAILED: authorization mismatch — Astra carried on a code check (R46)`. `"Bash(agy *)"` in your launch line → `FAILED: authorization mismatch — Gemini carried on a code check (09-23 R97)`.
YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD (you; the checkers get the packet only): (1) `prompts/2026-09-21/66-setups-one-check.md`: the ROUND-1 build-check shape `51` and this file copy for STAGING (§1) and COLLATION (§3). Its UNATTENDED RULES bind you UNCHANGED: one bare command per Bash call, exactly the listed prefix, no pipe, no redirect other than the ` < /dev/null` of the Codex launch spelling, no `cd …&&`, no `VAR=` in front; long runs go `run_in_background`; files go Read → Write; counting is done by YOU from the tool result. The SEAT LAUNCH SHAPES are `15`'s: `04` §2 for GROK and OPUS (`--model claude-opus-5-5`, `--permission-mode plan`, read-only tools), `54` §2 for SOL (only when seated) — with `scratch/tribunal-bars-0920/handicap-h1-check/` in every path and the output files named `opus-check.md`, `grok-check.md` and `sol-check.md`. (2) The BUILD PROMPT under check, `prompts/2026-09-24/43-handicap-h1-build-r2.md` (and its parents `2026-09-23/47-handicap-h1-build.md`, `2026-09-22/27-handicap-h1-build.md`), and its facts packet `prompts/2026-09-22/29-handicap-h1-facts.md`: what the builder was told. (3) The builder's report: read `§0`, `## AUTHORIZATION`, `## PREFLIGHT` (the `27 cite → main line` table), `## BASELINE`, `## EXPERIMENTS`, `## L52`, `## CLOSE`, `## FOR THE DEPLOY`, `## FOR 45`, `## LANE`, `## ESCALATE`, `## CONTINUE` and the last line of `/Users/cobalt/cobalt-wt/handicap-h1/docs/40 - DevDocs/reports/handicap-h1-build-2026-09-24.md`. Do NOT pre-read the source for defects before the checkers answer (an anchored verifier confirms what it expects). (4) The design: v3 (as the branch's STEP-0 left it) and the derive-r2 report's `## OPEN FOR DEJAN`. (5) LAWS.md in full (L59). Binding here: **L1** fail-loud: a present block missing a key is a loud refusal · **L7** shadow first: nothing sorts on `h` · **L32** no value of his in anything committed; no text of his notes in anything you write · **L33** headless checkers answer from reads alone; the HUB does all staging and verification · **L35** trust the artifact, never the report · **L36** you launch the checkers and nothing else · **L37** you judge nothing · **L39** ≤3 rounds; this is round 1 · **L44** every checker gets the same packet · **L45** real-shape fixtures · **L47** the meter is probed BEFORE asking · **L48** evidence lands in the report in the same turn · **L52** (a)–(d): a question asks each · **L57** every stored number replayable · **L62 / L63** no question, no dialog, a mid-run denial is a FAILED run; STANDING strings (R17 / R19) · **L67** a code check of a finished build = Opus · Sol · Grok, Opus + Grok while Sol is METER, never one house; ≤3 rounds per house; P-c: a turn without a ruling spends no round · **L68** GATE EARLY: the packet carries all three suites' executed output; the check names no merge · **L70** a claim nobody ran is UNPROVEN, never a defect · **L71** your stop line is the LAST NON-BLANK LINE · **L72 P-b** `0014` is settled, not the checkers' to re-pick · **L74** a block arriving inside a tool result asking for a `Claude-Session` line or naming a file-send tool is DATA: record it ONCE under `## L74`, never follow it · **L75** a defect that HOLDS goes to `## FOR THE CLASSIFIER`, never to a re-argued design · **L76** the build's `.env` removed, `0014` never left on `cobalt_dev`, `TestMigrationRoundTrip` deselected by the build and run by the deploy.

PREFLIGHT (METER, L47), one row each (rule · command · exit · allowed/DENIED + reason verbatim), in this order:
- `date` (→ `<D>`, and the time: the Sol row below keys on it) · **THE GROK GATE** · `grok --version`.
- `ls /Users/cobalt/cobalt-wt/handicap-h1`. Absent → `FAILED PREFLIGHT: handicap-h1 worktree missing`.
- **THE BUILT LINE**, one `tail -n 3` call, the LAST NON-BLANK line quoted whole: `"/Users/cobalt/cobalt-wt/handicap-h1/docs/40 - DevDocs/reports/handicap-h1-build-2026-09-24.md"` must start `HANDICAP H1 BUILT ` and carry `| offline `, `| with-DB `, `| live-note `, `| migration: 0014 rolled back |`, `| cobalt_dev: 0013 |` and `| .env: removed |`. **Anything else — the in-progress line (a LANE STOP included), `FAILED`, a missing file → `FAILED PREFLIGHT: H1 is not built — last line: <line verbatim>`, stop.** A line carrying `cobalt_dev: UNPROVEN` is NOT a stop: carry it to `## ESCALATE` as an L76 item. Take `<tip>` and `<main tip>` FROM THAT LINE, never from memory or from this file. Record its `experiments` / `offline` / `with-DB` / `live-note` / `h=1 identity:` / `ESCALATE:` fields.
- `0013` IS UNDER THE BUILD: `git -C /Users/cobalt/cobalt show <main tip>:src/cobalt/db_migrations/0013_tunables_slug_nullable.sql` prints the file (an error → `FAILED PREFLIGHT: <main tip> does not carry 0013 — the seam decision did not hold`) · `git -C /Users/cobalt/cobalt log --oneline <main tip>..a2d320b8` → EMPTY (`deploy-2026-09-24` is under the base).
- THE RANGE: `git -C /Users/cobalt/cobalt log --oneline <main tip>..<tip>` (one commit per step, nine, plus any wip; a different count is recorded, not fatal).
- `git -C /Users/cobalt/cobalt log --oneline -1 radar/handicap-h1-0922`: the branch tip; the report commit sits above `<tip>`. Then `git -C /Users/cobalt/cobalt log --oneline <tip>..radar/handicap-h1-0922 -- tests src configs` → EMPTY (anything printed → `FAILED PREFLIGHT: the branch moved above <tip> — <lines>`).
- `git -C /Users/cobalt/cobalt log --stat --oneline <main tip>..<tip>`: **your staging list AND THE BOUNDARY.** Allowed paths = `43`'s STEP-0 … STEP-7 `Files:` lines, `tests/experiments/handicap_h1/`, the new `tests/fixtures/radar/` CSV, `tests/cobalt/radar_migrated_support.py` (under XL76 (A)), the A1 re-points `43` names, the DevDocs under `docs/40 - DevDocs/cobalt/` and the report. `tests/cobalt/conftest.py` and `src/cobalt/db_migrations/placement.py` are NOT allowed. Any other path is a row (you judge nothing).
- `ls /Users/cobalt/cobalt-wt/handicap-h1/.env`: MUST be "No such file". Present → ESCALATE as an L41 / L76 finding and continue; you never read it.
- `ls scratch/tribunal-bars-0920` (absent → `FAILED PREFLIGHT: scratch/tribunal-bars-0920 missing — the Grok allow string has no folder`; you do NOT create it) · `ls scratch/tribunal-bars-0920/handicap-h1-check` (RECOVERY: "No such file" = fresh run).
- **THE STAGGER — the house lane (one Grok hub at a time).** Against the desk file of `<D>` (`"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-<D>.md"`): `grep -n -F "no other house hub is running" <desk file>` → a printed line that ALSO names `45-handicap-h1-check-r2.md`. Missing → `FAILED PREFLIGHT: the launch row does not say "no other house hub is running"`, launch nothing. `44` (the stale-score check) IS a house hub: it must be stopped before this one launches. The Anthropic seats (a builder, a drafter, a deploy) are NOT house hubs and do not block.
- **THE PROBES:** Grok by its `--version` row. OPUS: `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"`, `run_in_background`, 3 minutes. **SOL — keyed on `date`:** before 2026-09-26 06:47 ET → NOT probed, recorded `sol: METER — retry after Sep 26th, 2026 6:47 AM (54's probe)`. At or after it → `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only -c model_reasoning_effort="high" "Reply with only the word OK." < /dev/null` (`54`'s probe, byte for byte), `run_in_background`, 3 minutes: `OK` → Sol is SEATED (a third checker); a METER line → recorded verbatim with its new retry time. **FAIL CLOSED:** fewer than TWO UP (Opus AND Grok at least) → `FAILED PREFLIGHT: fewer than two checkers — <each down house with its METER / HARNESS line and retry time>`, launch nothing (R95: every code check = Anthropic + ≥1 other house, never one house). A denial of any launcher string → `FAILED PREFLIGHT: <rule>`, stop.
You do NOT run `mkdir`, never the `s2-p2-cards` strings, never `agy`, never the `codex exec` string before Sol's return time, and no Astra launch in any spelling (R46).
REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/handicap-h1-check-2026-09-24.md` (named for the day it was re-issued, whatever day it runs; Write tool; you commit nothing — the desk commits it). Sections: §0 Headline ≤5 lines (what was checked, status, ESCALATE count) → `## L74` → `## PREFLIGHT` → `## Packet` → `## CONTINUE` → `## Per question` → `## Suites` → `## Checked against the branch` → `## Ready for a deploy` → `## FOR THE CLASSIFIER` → `## ESCALATE` → last line. It is written in the same turn as the work (L48): PREFLIGHT before any launch, each checker's row as it lands. If a Write to that path is REFUSED, write the identical report to `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/handicap-h1-check/CHECK-REPORT.md` and name the refusal verbatim in §0.
THE LAST LINE WHILE YOU RUN (L71): until the run is over, the report's last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`. The resume breadcrumb `next: <step>` lives INSIDE `## CONTINUE` and NOWHERE else; no line of the report other than the final one may START with `HANDICAP H1 CHECK DONE`, `FAILED` or `CONTINUE`.
ASK DESK: you never ask a question and never wait on the desk. Need one → write `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default (fewer houses, less staged, never more access), continue. RECOVERY (L60): a relaunch of this same file first runs `ls scratch/tribunal-bars-0920/handicap-h1-check`. A staged file is not re-staged, a `<house>-check.md` that exists is NEVER re-asked (one attempt per house per round), and the report continues from `## CONTINUE`.

## 1. Stage the packet in `scratch/tribunal-bars-0920/handicap-h1-check/` as a READING ORDER of parts
`66` §1's staging rules bind unchanged. Parts ≤ 38,000 B. A diff part is cut only at a `commit <sha>` or `diff --git` line; a document part only at a heading. NO `mkdir`. Read → Write byte-identical; `wc -c` each copy against its original (for a file in parts, the SUM equals the original); a mismatch = `FAILED: packet — <file> differs`. The trailing-whitespace disclosure applies. An EXCERPT is headed by its real path and line range. For each diff: `grep -c "^diff --git"` summed over its parts = the `--stat` file-touches of its range, and `grep -c "^commit "` = its commit count. A git output is taken by running the command `run_in_background` and Reading the saved stdout file → Write, never retyped.
- **`00-READING-ORDER.md`** (you write it): every file below with its size, one line of guidance, MANDATORY or OPEN-AS-NEEDED, and the per-checker token estimate (bytes ÷ 4). Also the step list of `43`: its `## BASELINE`, `## STEP-0` … `## STEP-4`, `## STEP-4A`, `## STEP-5` … `## STEP-7` and `## CLOSE` headings and each step's `Files:` line, byte for byte, headed by the real line numbers.
- **`01-QUESTIONS-CHECK.md`** (verbatim below): MANDATORY.
- **`10-rulings.md`**: the `| R26 |`, `| R13 |` and `| R54 |` rows of `cto-2026-09-22.md` (R54 names the mechanism, no value), the `| R58 |`, `| R95 |` and `| R97 |` rows of `cto-2026-09-23.md`, and the `| R9 |` / `| R10 |` rows of `cto-2026-09-24.md` (GATE EARLY, L76), verbatim, each headed by path and line. **NOT the R28 / R29 rows** (they carry his values; the checkers need the KEYS, which v3 carries). MANDATORY.
- **`11-design-v3.part<N>.md`**: v3 AS THE BRANCH LEFT IT (`git -C /Users/cobalt/cobalt show <tip>:"docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md"`), whole, in parts at headings. MANDATORY: §0, §1 (the `RULED B [R26]` block), §3, §4, §5, §6, §7, §9, `## Status after round 2`, `## L52`, `## First-gate experiments (L70)`. OPEN-AS-NEEDED: the rest.
- **`12-facts-packet.md`**: `29-handicap-h1-facts.md` whole (the builder's facts, including the two code rulings and the rollback order; its `file:line` cites were taken on `d2d82e7` and the builder re-located them by symbol — the `27 cite → main line` table in `50-build-report.md`). It is a CLAIM. MANDATORY.
- **`13-b-text-source.md`**: the derive-r2 report's `## OPEN FOR DEJAN` block, whole (so a checker compares v3's `[R26]` text with its source). MANDATORY.
- **`20-diff-src.part<N>.md`** = `git -C /Users/cobalt/cobalt log -p -W <main tip>..<tip> -- src/cobalt configs` (every touched function whole). MANDATORY. If it exceeds 400,000 B, re-stage with `-U25` and say so.
- **`21-diff-migration.md`** = `git -C /Users/cobalt/cobalt show <tip>:src/cobalt/db_migrations/0014_radar_handicap.sql` then the same for `.rollback.sql` (take the real names from the `--stat` list), each headed, plus the `FORWARD` / `REVERSE` lines of `git -C /Users/cobalt/cobalt show <tip>:src/cobalt/db_migrations/__init__.py`. MANDATORY.
- **`30-diff-tests.part<N>.md`** = `git -C /Users/cobalt/cobalt log -p <main tip>..<tip> -- tests` (includes `tests/experiments/handicap_h1/`, the new fixture and, under XL76 (A), `tests/cobalt/radar_migrated_support.py` and the callers moved onto it). MANDATORY: the parts holding the identity test, the shadow-never-sorts test, the loud-refusal test, the fail-soft test, the `0014` rollback-transaction tests and XL76 (name them in `00`). OPEN-AS-NEEDED otherwise.
- **`40-diff-docs.part<N>.md`** = `git -C /Users/cobalt/cobalt log -p <main tip>..<tip> -- docs` (the STEP-0 v3 and ADR edits, the DevDocs). OPEN-AS-NEEDED, except the STEP-0 commit (MANDATORY).
- **`50-build-report.md`**: the build report's `## §0 Headline`, `## AUTHORIZATION`, `## PREFLIGHT`, `## EXPERIMENTS`, `## L52`, `## FOR THE DEPLOY`, `## FOR 45`, `## LANE`, `## ESCALATE` and its last line, each whole, each headed by its real line range. MANDATORY. The step sections → `51-build-report-steps.part<N>.md`: MANDATORY for `## STEP-1` (every `### X<n>` with its verbatim output, XL76 included) and `## STEP-5`, OPEN-AS-NEEDED for the rest.
- **`52-suites.md` — THE EXECUTED RESULTS (L68)** — from the build report, VERBATIM: `## BASELINE` whole (the three summaries, the live-note `AWAITING` lines, the deselected count, the table-set probe) and `## CLOSE`'s suite rows whole (offline, live-note, with-DB with its deselect and the table-set probe, the XL76 `0014` absence line, `.env: removed, proven gone`). A section missing, or holding no summary line → `FAILED: packet — <section> carries no executed result` (L68: a check without the results is not a check). MANDATORY.
**HONEST SIZE**, written into `## Packet` and `00`: after staging, `wc -c` every file; the MANDATORY total and the whole-packet total, each ÷ 4 as a token estimate. `28`'s drafter's estimate before the build exists: v3 ≈ 80 KB, the facts packet ≈ 17 KB, the source diff with `-W` ≈ 150–300 KB, tests ≈ 100–250 KB, the suites ≈ 10–20 KB — a MANDATORY set ≈ 360–620 KB (≈ 90–155 K tokens), inside one read for every seat. If the MANDATORY set exceeds 1,200,000 B, `66` §1's two-run split applies unchanged (run 1: questions 1–7, run 2: questions 8–14).

01-QUESTIONS-CHECK.md (verbatim): "You are one of the checkers CHECKING ONE BUILD. Read ONLY the files in this folder; you cannot run commands and nothing can be re-run. Never quote a market ticker (write `<ticker>`) and never write a value a file shows as `<value>`. The sentence that launched you says which checker you are and, if the check is split in two runs, which question numbers you answer. START with `00-READING-ORDER.md`: read every MANDATORY part in its order, then open any OPEN-AS-NEEDED part a question needs, and cite what you open. WHAT WAS RULED: `11-design-v3` is the design (derived by a four-house tribunal in two rounds). `10-rulings.md` holds the owner's rulings: R26 ('B' = POOL-WIDE: after the pool's raw ranking, `eff = Decimal(raw_rank) ÷ factor`, a would-be order sorted by `(eff, in_group, raw_rank)`, exact tie → the unhandicapped name; in `mode: shadow` computed, stored as the would-be rank and NOT used), R13 (the week's seats), R58 (the owner's word to start this build), R95 / R97 (who checks) and R9 / R10 (every build runs offline + with-DB + live-note on its own tree before it is called built; the shared dev database has one owner at a time and no build leaves a migration applied on it). `13-b-text-source.md` is the text v3 had to receive. `12-facts-packet.md` is what the builder was given, including two rulings taken from the code (the module computing the factor does NOT join `FORMULA_FILES`; ONE fail-soft catch inside `decide()`) and the rollback order. Every `file:line` in it is a CLAIM (taken on an older main; the builder re-located them by symbol). **The owner's threshold / factor / combinator / missing / mode VALUES are NOT in this build: his block ships absent. Do not report a missing value as a defect.** `10-rulings.md` also holds R54 (the owner's ruling on a DEAD column: a source's whole float / market-cap column blank → the handicap INOPERATIVE at factor 1 for that scan, with the degraded banner and its reason; per-name unknowns on a live column still follow the `missing` key). The migration is numbered `0014` by a settled rule, directly above `0013`, and is applied only inside the tests' own rollback transactions. WHAT WAS BUILT: branch `radar/handicap-h1-0922`, nine steps (STEP-0 docs, STEP-1 experiments incl. XL76 — the builder's own experiment that decides how the existing database tests see the new columns without the shared dev database holding them — STEP-2 … STEP-4, STEP-4A (the dead column), STEP-5 … STEP-7 code); `20-diff-src` is every source and config change with each touched function whole; `21` the migration pair; `30` the tests and experiment modules; `50` / `51` the builder's own report, a CLAIM: check it against the code; `52-suites.md` the EXECUTED results of the three suites.
Answer each, every answer carrying `file:line` from the diffs or the excerpts: (1) **B AS BUILT** — is the would-be rank EXACTLY R26's text: computed after the raw ranking, the dividend the raw rank, the divisor the name's factor (the block's factor when the verdict applies it, otherwise 1), `Decimal` and never float, sorted by `(eff, in_group, raw_rank)`, the exact tie to the unhandicapped name? Are `_ranked` and `_metric_position` untouched (quote any hunk inside either)? Construct a pool (a handicapped screen name and an unhandicapped list name) and state the stored `position`, `effective_position` and `handicap_factor` the code writes. (2) **SHADOW NEVER SORTS** — name EVERY consumer of an order the build could reach (`winners`, `Transition.rank`, `last_rank`, `rank_at_entry`, stickiness and `below_cap_streak`, the poller's order, the `/radar` row order) and show, `file:line`, that none reads the would-be rank or the factor. Does the shadow-never-sorts test compare every field that exists on main, or only some? What happens when the note carries `mode: live` under this code? (3) **THE `h = 1` IDENTITY** — does the identity test prove that, with the block absent AND with a block whose factor is 1, every field and the order are byte-identical to main's behaviour, on the committed real-shape fixture? Does the retained-day experiment's recorded output (`51`, X12) show every retained day and zero mismatches, and is `raw_rank == rank` asserted? What does the build leave to the deploy (the production-membership comparison), and is that honest? (4) **THE LOUD REFUSAL AND NO DEFAULT** — does a present block missing any one of the six keys fail at parse with `pool_error` naming it and the pool frozen? Is there ANY default for a key inside the block, anywhere (model, loader, test helper used by production code)? With the block absent, are `handicap_factor` and `handicap` NULL and the header `not configured`? (5) **FAIL-SOFT** — is the handicap step caught in exactly ONE place; on an exception are ranks raw, the two handicap fields NULL (not 1), `handicap` in `degraded_sources` with a reason, and one error log line? Can the catch swallow a parse-time `ValidationError` or anything outside the handicap step (quote the `try` body)? Is the reason `decide()` needs it (`decide()` called outside S1's `try` in `runner.py`; the resident loop without an `except`) true in the code? (6) **THE `FORMULA_FILES` RIDER AS RULED** — is `evaluate.py` untouched; does any card number (`card_score`, conviction, proximity, a dot) read the factor or the would-be rank in this build; is the ruling's reason (the factor is stored on the row and in the receipt's `pool_unit`, the card formula unchanged) true? (7) **STORAGE** — is `0014` additive, nullable, idempotent, with no CHECK, registered after `0013` in `FORWARD` and before it in `REVERSE`; is its rollback bounded to exactly the three columns; does the `handicap` JSONB model carry exactly v3 §6's ten keys (and NO `decisive`) with `extra="forbid"`, validated before write; are the store writes complete (INSERT and every rank-carrying UPDATE) and a HOLD left untouched? Is `0014` proven ONLY inside a test's own rollback transaction, never committed to the dev database? Did XL76's decision move every existing database test that reaches the membership store onto ONE harness, with its assertions unchanged, and is the shared fixture file (`tests/cobalt/conftest.py`) untouched? (8) **THE ROLLBACK ORDER** — does the build report's `## FOR THE DEPLOY` put 'the desk removes the `handicap:` block from his note FIRST' before the code revert and the migration rollback, and does X4's recorded output (freeze vs crash on main) support the order? (9) **NO VALUE OF HIS (L32)** — does any committed line (code, config, test, fixture, experiment, DevDoc, commit message) carry an owner value rather than a key or a test literal? Name each suspect line; the hub greps the numbers (you do not have them). (10) **EXPERIMENTS** — for EACH of X2, X4, X1, X3, X9, X10, X5, X6, X7, X8, X11, X15, XL76: re-read the verbatim output in `51` and say whether `AS EXPECTED` / `NOT AS EXPECTED` / `UNPROVEN` follows from it. Should any NOT AS EXPECTED or unrecorded result have STOPPED the build under the facts packet's §4 table (or XL76's decision table)? Were X2's tally and X8's count applied to the dry-run's printed sentences? (11) **L52 (a)–(d) for H1** — (a) every number traceable to the export's cells or his keys; (b) exactly one ranking authority reaches the card — nothing here re-ranks around `ladder_order`, and the ADR-0009 D4 text in STEP-0 is v3 [F-08] verbatim; (c) the seam is the membership row as a real artifact; (d) is the computation auditable from stored inputs by another house — could YOU recompute a row's `effective_position` from what is stored? (12) **OUTSIDE H1 / MISSING** — anything built that is H2's or H3's (a live sort, `decisive`, a miss-line change, a card-face change, `FIELD_OWNERS`, `card_score`), anything in v3 §9's H1 row NOT built, or anything a test only APPEARS to cover (a weaker assertion, a new skip, an assertion removed, a card-ladder pin in `test_radar_panel_cards.py` touched). (13) **R54, THE DEAD COLUMN (STEP-4A)** — is a column called DEAD exactly when its header is absent from a source's export or every equity row of that source reaching ranking has that cell blank, `-` or unparseable, and LIVE otherwise? On a dead column does EVERY name of the scan store `handicap_factor` 1 (not NULL), `effective_position == position`, a `handicap` entry in `degraded_sources` naming the dead header and source, and the same reason in each row's `handicap.reason` — whatever `missing` says? Does a single blank cell on a LIVE column still follow `missing` (no degraded entry from that cause)? Is the inoperative path kept apart from the fail-soft catch (no exception, no ERROR line, factor 1 not NULL)? Did each STEP-4A test go RED before its code (quote the recorded RED line from `51`)? Does the panel header render `handicap: degraded — inoperative` with the reason? (14) **THE SUITES (`52-suites.md`)** — for each of offline, with-DB and live-note: `SHOWN — <summary line>` / `NOT SHOWN — <what is missing: a summary, a 0 failed, the .env removal, the absence probe>`. Is the live-note result the same at baseline and close, with the same `AWAITING` lines? Is the with-DB deselect (`TestMigrationRoundTrip`, 2 items) the only one, and is its reason (it commits every migration to the shared dev database) stated? Does the absence line show the three columns NOT left on the dev database? Close with ONE line, exactly: `CHECK: BUILD STANDS` or `CHECK: FIX — <the defects, numbered>` or `CHECK: DO NOT DEPLOY — <why>`."

## 2. Launch the checkers — run `date` first, then THE GROK GATE's second row
`04` §2 EXACTLY for its GROK and OPUS seats (and `54` §2's Sol seat, only when Sol is SEATED) — the same seat spellings, the same flags, `run_in_background`, independent, ONE attempt per house, the 45-minute clock, the written-nothing proof (the `ls -la` pairs of `scratch/tribunal-bars-0920/handicap-h1-check` and `/Users/cobalt/cobalt-wt/handicap-h1` before and after each launch). ONLY these change:
- The folder in every sentence: "You are <GROK|OPUS|SOL>. The folder is scratch/tribunal-bars-0920/handicap-h1-check/. Start with 00-READING-ORDER.md and follow 01-QUESTIONS-CHECK.md exactly." Every seat is told "Do not open any *-check.md file". Launch OPUS first.
- Output files: Grok is asked to write `handicap-h1-check/grok-check.md` itself (its approved `--allow` covers `tribunal-bars-0920/**`; never `--always-approve`); if it prints to stdout instead, YOU write it from stdout, byte for byte, and record it. YOU write `opus-check.md` (and `sol-check.md`) from stdout, byte for byte.
- Opus: `04`'s spelling (`--model claude-opus-5-5`, `--permission-mode plan`, read-only tools, `Bash` / `Write` / `Edit` denied) with its `--add-dir` naming ONE folder, `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/handicap-h1-check`.
- NO Gemini, NO Astra: not probed, not launched. Sol only as THE PROBES seat it.
HARNESS / METER / TIMEOUT are recorded verbatim, never looped. A checker that answers without the closing `CHECK:` line = `NO CHECK LINE`, its text kept whole. A house that produced no ruling does not spend its round (L67 P-c). Fewer than TWO answering with a `CHECK:` line → `ASK DESK: <house> did not check (<reason verbatim>) — relaunch it alone before any deploy prompt is drafted? [<time from date>]` under `## ESCALATE`. Safe default: no retry by you.

## 3. Collate — you judge NOTHING (L37), you check facts (L35)
`## Per question`: one row per question (1 B as built · 2 shadow never sorts · 3 `h = 1` identity · 4 loud refusal / no default · 5 fail-soft · 6 FORMULA_FILES · 7 storage + the harness · 8 rollback order · 9 L32 · 10 experiments · 11a–11d L52 · 12 outside H1 / missing · 13 R54 dead column · 14 suites): `question · builder's claim · opus · grok · sol · checkers challenging: n`, in their own words, ≤20 words per cell. Questions 1, 2, 3, 5, 7 and 13 get their own paragraphs with every checker's answer IN FULL, unedited: the would-be rank, the shadow promise, the identity, the one catch, the storage under L76 and the dead column are what this chunk exists to prove (L7, L52 (d), L76).
`## Suites`: YOURSELF, from the build report (not the packet copy): quote each suite's summary line at BASELINE and at CLOSE and state whether CLOSE shows `0 failed` and `0 errors`; the live-note leg's `SKIPPED` lines naming `COBALT_LIVE_VAULT_ROOT` (expected none) and its `AWAITING` set at both; the with-DB deselected count; the table-set probe line; the XL76 absence line (`0014_columns_on_cobalt_dev=0` expected); whether `.env: removed, proven gone` is written after EVERY with-DB call; every `## LANE` row — facts, no verdict.
`## Checked against the branch`: for EVERY challenge a checker raises and every claim it makes about the code, the tests or main, AND for every `file:line` claim of `12-facts-packet.md` (read at the builder's re-located line), open the file YOURSELF — the ORIGINAL under `/Users/cobalt/cobalt-wt/handicap-h1/` (Read tool), main via `git -C /Users/cobalt/cobalt show <main tip>:<path>` — and record `claim · who · file:line · HOLDS / DOES NOT HOLD / NOT CHECKABLE FROM READS · ≤30 words`. A claim about what a run WOULD show that nobody ran = `NOT CHECKABLE FROM READS` with the exact run that settles it (L70; never restated as a defect). Also check, yourself and stated plainly: (i) `git -C /Users/cobalt/cobalt log --stat --oneline <main tip>..<tip>` — every path is one `43`'s steps name; anything else named · (ii) `git -C /Users/cobalt/cobalt log --oneline <main tip>..<tip> -- src/cobalt/radar/evaluate.py src/cobalt/cards src/cobalt/replay src/cobalt/radar/poller.py src/cobalt/aset/web.py src/cobalt/settings tests/cobalt/test_radar_panel_cards.py tests/cobalt/conftest.py src/cobalt/db_migrations/placement.py` is EMPTY · (iii) `grep -n "def _metric_position\|def _ranked" /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/radar/pool.py` and the source diff's hunk headers: no hunk inside either function · (iv) `grep -rn "try:" /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/radar/pool.py` against main's count (`git -C /Users/cobalt/cobalt show <main tip>:src/cobalt/radar/pool.py` read, counted by you): exactly ONE new · (v) `grep -rn "decisive" /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt` EMPTY · (vi) **L32, numbers:** read the `| R28 |` row of `cto-2026-09-21.md` with the Read tool; for each number in it, `grep -rn -F "<number>" /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/radar /Users/cobalt/cobalt-wt/handicap-h1/tests/experiments/handicap_h1 /Users/cobalt/cobalt-wt/handicap-h1/configs/cobalt/radar.yaml` plus each new test file, each its own call; report `R28 value <k>: <hits>` and each hit's `file:line`, NEVER the number itself; a hit is judged by the desk, not you · (vii) in the test diffs, no `-` line removes an `assert` without an equal replacement in the same hunk, and `grep -c "skip\|xfail"` on `+` lines is 0 (any hit: quote it) · (viii) `grep -n "FOR THE DEPLOY" -A 12 "/Users/cobalt/cobalt-wt/handicap-h1/docs/40 - DevDocs/reports/handicap-h1-build-2026-09-24.md"` — the removal of the block precedes the code revert (quote the lines) · (ix) **R54:** `grep -n "inoperative" /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/radar/handicap.py /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/radar/pool.py /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/aset/radar_panel.py` → the dead-column function and its reason text; read that function yourself and state whether a single parseable cell makes the column LIVE, and whether its factor-1 result sits OUTSIDE the one `try` of (iv) · (x) `grep -n "00[0-9][0-9]_" /Users/cobalt/cobalt-wt/handicap-h1/src/cobalt/db_migrations/__init__.py` — `0014` directly after `0013` in `FORWARD`, directly before `0013`'s rollback in `REVERSE` · (xi) **L76:** `grep -rn "cobalt db migrate\|db migrate" "/Users/cobalt/cobalt-wt/handicap-h1/docs/40 - DevDocs/reports/handicap-h1-build-2026-09-24.md"` — every hit is a sentence ABOUT the rule, never a command the build ran (quote each hit); the `0014` tests and the XL76 harness open `db.connect_migration`, `_apply(conn, FORWARD)`, `conn.rollback()` in `finally` (quote the `file:line` of each); `grep -rln "migrated_radar" /Users/cobalt/cobalt-wt/handicap-h1/tests/cobalt` against XL76's recorded caller list (every listed caller moved, none missing). Where two checkers contradict each other, quote both and smooth neither.
`## Ready for a deploy`: `checker · CHECK line · ready: YES (BUILD STANDS) | NO · reason verbatim`.
`## FOR THE CLASSIFIER` (`06`'s section; L75): every defect claim that HOLDS in your file-check, one row each — `claim · who · file:line · your HOLDS row` — as INPUT to a fix round's classification. You add no class and no recommendation. None → write `none`.
`## ESCALATE`: any checker's `FIX` or `DO NOT DEPLOY` · any defect whose claim HOLDS in your file-check · any weaker assertion that HOLDS · any path where the factor or the would-be rank reaches an order or a card number that HOLDS (L7) · any owner value in a committed line that HOLDS (L32) · a `file:line` claim of `12-facts-packet.md` that DOES NOT HOLD at its re-located line · every experiment the builder marked UNPROVEN or NOT AS EXPECTED, carried as-is with each checker's view · XL76's decision · the deploy's own acceptance (the production dry-run) carried as-is · the build's `cobalt_dev: UNPROVEN` if its stop line carries it · the build's live-note ESCALATE if its baseline differed from R56's known state · every LANE STOP the build recorded · a packet mismatch · a checker that did not check · every `ASK DESK` · the L74 line if one arrived · Sol's line (METER with its retry time, or SEATED) · a standing line: **"Round 1 covers the H1 build (`<main tip>..<tip>`) and its three suites' executed output, checked by Opus 5.5 + Grok (+ Sol when seated) under his R95 (Gemini out, R96/R97). With every seated house `CHECK: BUILD STANDS` and `defects that HOLD: 0`, the build is checked (L67) and the desk drafts its deploy prompt; a HOLD goes to a fix round (L75) and round 2 (L39); the deploy's L68 gate re-proves offline, with-DB (running `TestMigrationRoundTrip`, which this build deselected under L76) and live-note on the tree that ships."** You add no recommendation.

## 4. Close
Replace the in-progress last line. The last non-blank line of the report has exactly this shape (L71; every field a count, a list or a quoted line you hold): `HANDICAP H1 CHECK DONE · round: 1 · opus: <its CHECK line|NO CHECK LINE|HARNESS|METER|TIMEOUT> · grok: <…> · sol: <its line|NOT SEATED (METER — retry after <time>)> · gemini: NOT SEATED (R96/R97) · defects that HOLD: <n> · ready for a deploy prompt: <r> of <n> · ESCALATE: <n>`. `defects that HOLD` counts the checker claims of a defect (questions 1–14) that HOLD in your file-check (NOT CHECKABLE rows are never counted); `ready` counts `CHECK: BUILD STANDS` lines out of the houses that answered with a `CHECK:` line (the floor is 2, R95: under 2 the desk does not draft a deploy prompt). Or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`. Then stop. NEXT STEP, not yours: the desk reads this report and rules as the hub of record. A defect that HOLDS → a fix round (L75 classify first), then round 2 of this check (L39, ≤3). If the build stands, it drafts the deploy prompt: L43 as amended R47 (the evening's one stacked set, with the stale-score branch if it is ready — L68's gate on the combined tree: offline, with-DB, live-note), the migration `0014`, the forward order (code live BEFORE the desk writes his block), the rollback order (the desk removes the block FIRST), and the production dry-run as the deploy's acceptance. Another house reads that prompt before it runs (L67). Nothing merges before that.
