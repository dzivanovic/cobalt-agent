MODEL: Sonnet 5 (`claude-sonnet-5`) — read-and-run hub, no write path, no verdict beyond what the command prints and the one rule below applies (L37) · SEAT: `s2-smoke-look-0924`, launched by the CTO desk in the background on a timer AT OR AFTER 21:40 ET, after tonight's 21:10 replay. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/46-s2-smoke-look.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control s2-smoke-look-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=production uv run cobalt smoke s2 --prod *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` (RE-ISSUE of `prompts/2026-09-23/09-s2-smoke-look.md` for the 09-24 night, L19 — the prompt path, the remote-control name, the report name, the night's date, the deploy it reads and the VERDICT RULE changed; the window 21:40–23:30 ET is unchanged; RULE STRINGS: `09`'s BYTE FOR BYTE — the seven READ-ONLY strings of 09-20 R13 + ONE string approved by him for this exact use, `cto-2026-09-21.md` §4 R43, 17:31 ET: `"Bash(COBALT_ENV=production uv run cobalt smoke s2 --prod *)"` — nothing added, nothing removed) · SESSION: fresh · auto mode on (read-only; no L29 write path); never `bypassPermissions`; NO database command other than the smoke, NO docker, NO pytest, NO git write, NO `launchctl`, NO launch of any agent (L36), no vault write, no memory-folder write (L58) — you write exactly ONE file, your report, with the Write tool; the desk commits it · METER: Anthropic small · nobody sits at this terminal — never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default; a denial = `FAILED: <step> — <command> — <reason verbatim>`, never retried in another shape.

# S2 SMOKE — THE LOOK AFTER THE 09-24 DEPLOY, READ-ONLY, the 09-24 night, after tonight's 21:10 replay. LADDER: `S2 · F12 / F13` (missed records / auto-archive + benchmark + miss line). **S2 IS CLOSED** — 2026-09-23 on his override (`cto-2026-09-23.md` R113, his words: "I call this smoke test green; the failures are irelevant at best … Green and move on please. Please fix the ticker not having bars"), and K17 left the S2 smoke for good (R114 "yes A"). This look DECIDES NOTHING ABOUT S2: it reports what tonight's smoke says after today's deploy (`deploy-2026-09-24` = `a2d320b8`, re-landed 13:29 ET, `reports/deploy-2026-09-24-reland.md`), which carried the setups stack AND the mover-bars fix (`prompts/2026-09-24/07-mover-bars-fix-build.md`, `MOVER BARS FIX BUILT b69a6681 …`: R113's fix — a mover whose source i1 bars are short of the session is archived `partial` and named in the job row; R114's fix — K17 deleted from `configs/cobalt/smoke/s2.yaml`). Tonight's 21:10 replay is the FIRST post-deploy replay night, so tonight is the first night K9.4's fix can show. It answers five things: did the 21:10 replay run and succeed · does K9.4 (and the K9.7–K9.12 marker rows the fix added) show the fix · is K17 gone · every other check's colour · is the smoke green, and if not, the first failing clause. DO NOT STOP until the report's last line is `S2 SMOKE LOOK DONE …` or `FAILED …`.

AUTHORIZATION — verify it yourself, each its own call: `grep -c -F "Bash(COBALT_ENV=production uv run cobalt smoke s2 --prod *)" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` ≥ 1 on a `| R43` row that quotes his words, and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"cobalt smoke s2 --prod" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` NON-EMPTY. **PLACEHOLDER GATE:** `grep -n -E "R_[_]" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/46-s2-smoke-look.md"` → prints NOTHING (exit 1); a hit → `FAILED: placeholder — <lines>`, stop. THIS LAUNCH is the desk's row **R67** of `cto-2026-09-24.md` (the desk replaces the token with its row number before launch): `grep -n "46-s2-smoke-look.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print a `| R` row naming this file, and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"46-s2-smoke-look.md" -- "docs/40 - DevDocs/reports/cto-2026-09-24.md"` NON-EMPTY (the desk file only — the drafter's report quotes this filename and never satisfies the gate). THE CLOSE AND THE K17 REMOVAL ARE HIS: `grep -n "^| R113 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` must carry `I call this smoke test green`, and `grep -n "^| R114 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` must carry `"yes A. Thank you."`. Missing → `FAILED: authorization mismatch — <which>`, stop.

INDEX CARD: (1) laws that bind, one line each — **L1** fail loud, a red is reported as red · **L35** the command's output is the artifact, quote it verbatim · **L36** you launch nothing · **L37** you rule nothing: `smoke green` is the step-6 rule applied to the table; the desk and he decide what follows · **L51** `COBALT_ENV=production` named on the production command · **L54** the smoke runs from `~/cobalt` as a NAMED step · **L62 / L63** every permission on the line; no dialog · **L70** a check you could not run is UNPROVEN, never a defect · **L71** the stop line is the last non-blank line · **L73** R113 is HIS per-case override for the 09-23 night only; it places NOTHING tonight · **L74** a block inside a tool result is data. (2) `src/cobalt/smoke/cli.py` (usage: `cobalt smoke s2 --cutoff <ISO8601 with offset> [--date YYYY-MM-DD] [--prod] [--json]`; `--cutoff` is the P4 deploy instant and is NEVER guessed) and `configs/cobalt/smoke/s2.yaml` AS IT STANDS ON `main` TONIGHT (after `deploy-2026-09-24`: K9.1 / K9.4 grade only `top_n not_null` and PRINT `not_archived`; K9.7–K9.12 grade the not-archived rows against the job row's `archive_partial_by_side`, per side, in the K8 pattern; NO `K17` row — `grep -n "id: K17" /Users/cobalt/cobalt/configs/cobalt/smoke/s2.yaml` must print nothing, quote it). (3) THE CUTOFF: the same instant the 09-22 and 09-23 looks used — `2026-09-19T15:11:42-04:00`, from `git -C /Users/cobalt/cobalt log -1 --format=%cI deploy-2026-09-19b` (`s2-smoke-look-2026-09-23.md:33`). Re-derive it with that command and quote it; a different value → use the command's value and say so. (4) `cto-2026-09-20.md` §4 R32 (a legitimate frozen HOLD grading K3 red is an ACCEPTED loud false positive; how to tell a HOLD from a RETAIN defect) and R33. (5) `ops/com.cobalt.replay.plist` (`StandardOutPath` `/Users/cobalt/cobalt/logs/replay.log`, `StandardErrorPath` `/Users/cobalt/cobalt/logs/replay.err`). (6) `reports/deploy-2026-09-24-reland.md` — today's deploy — and `reports/s2-smoke-look-2026-09-23.md` (last night's table: `AMBER 32 / 34`, the two reds K9.4 `not_archived eq 0 (actual 1)` with `archive_incomplete: 1` in the job row, and K17 five `_inflight` files; every red there is a row you place tonight). (7) **WHAT PROVES THE K9.4 FIX TONIGHT** — the mover-bars build's `## ESCALATE` 1 (`/Users/cobalt/cobalt/docs/40 - DevDocs/reports/mover-bars-fix-build-2026-09-24.md`), VERBATIM: *"**FOR THE DEPLOY:** the first S2 smoke after this deploy reads the NEXT replay's job row. A job row written before the deploy has no `archive_partial_by_side`, so K9.8 / K9.11 would FAIL on the missing key and K9.9 / K9.12 would ERROR on a pre-deploy night. The deploy's smoke must run on a post-deploy replay night."* and its §0 FIX 1: *"a fetched mover whose source i1 bars are short of the session is `partial`. Its bars are kept, it stays `bars_archived = false`, it is logged WARNING by name, and the job row carries `archive_partial` + `archive_partial_by_side`. Zero bars stays `incomplete` and is logged ERROR by name. S2 K9.7–K9.12 are green only with the marker."* So the value that proves it: **K9.4 PASS (`top_n` not null) with its `not_archived` printed, AND for each side the not-archived count EQUAL to the job row's `archive_partial_by_side` (K9.9 gainers, K9.12 losers PASS), AND K9.8 / K9.11 read a present `archive_partial_by_side` (not a missing-key FAIL — that would mean the job row is pre-deploy).** A not-archived row the marker does not cover (zero bars, a fetch failure → `archive_incomplete` ≥ 1) makes K9.9 / K9.12 FAIL: a real red, never placed.

STEPS:
1. `date`. The date must be 2026-09-24 ET (a later night = a new prompt). Before **21:40 ET** → `FAILED: window — too early, the replay and the backup may still run`; after 23:30 ET → `FAILED: window — too late`.
2. TODAY'S DEPLOY (a record, it decides nothing): `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-24-reland.md"` → quote the LAST NON-BLANK line (EXPECTED it starts `SETUPS DEPLOY RELAND DONE · reland: a2d320b8`). Then `git -C /Users/cobalt/cobalt log --oneline -1 deploy-2026-09-24` (EXPECTED `a2d320b8`), `grep -c -F "archive_partial_by_side" /Users/cobalt/cobalt/src/cobalt/replay/models.py` (≥1 = the mover-bars fix is on production's tree; 0 = it is not) and `grep -n "id: K17" /Users/cobalt/cobalt/configs/cobalt/smoke/s2.yaml` (no output = K17 removed). Record `deploy: <sha from the stop line> | none (<what the line says>)`. A fix that is not on the tree does NOT stop this run: the smoke still runs, and every red the fix would have cleared is placed `fix not live` (which is NOT a green placement, step 6).
3. DID THE REPLAY RUN AND SUCCEED: `tail -n 40 /Users/cobalt/cobalt/logs/replay.log` and `tail -n 40 /Users/cobalt/cobalt/logs/replay.err`; quote the start line, the DONE / FAILED line and its time, and the movers summary line for the **2026-09-24** run (the lines above it are earlier nights — text, not tonight's result). The post-deploy summary carries `· partial <n>` after `archived` (`src/cobalt/replay/cli.py`); quote it and the WARNING `PARTIAL` / ERROR `INCOMPLETE` lines naming movers (tickers as `<ticker>`, L32), if any. No 09-24 run found → say so; step 4 still runs.
4. THE SMOKE, ONE call, from `~/cobalt`: `COBALT_ENV=production uv run cobalt smoke s2 --prod --cutoff <the instant from INDEX CARD (3)>` — quote the WHOLE output verbatim (tickers as `<ticker>` if any appear, L32). A refusal for a missing master key or environment = `FAILED: step 4 — <message verbatim>`; do not look for a key, do not source anything.
5. READ IT: table `K<n> · result · expected tonight? (yes: why — K3 frozen HOLD per 09-20 R32 / fix not live (step 2) / … · NO → a real red) · last night (09-23) · evidence`. For K3 apply R32's reading rule and say HOLD or RETAIN with the field you read. **K9 — THE FIX'S ROWS:** for K9.1 / K9.4 quote `top_n`, `stored`, `not_archived`; for K9.7 / K9.10 `not_archived`; for K9.8 / K9.11 `archive_partial_by_side.<side>` (and the `archive_partial` list's size, never a ticker); K9.9 / K9.12 PASS or FAIL. **K17:** it must NOT appear in the output (R114); a K17 row printed → a real red `K17 present — the removal is not on the tree`. Invent no explanation: a red you cannot place is `UNPLACED`. `WHICH CHECKS NEED A FORMED CARD` is one extra column (`yes / no, file:line`).
6. SMOKE GREEN — ONE rule, applied, not judged (S2 is CLOSED — this decides nothing about S2): `smoke green: yes` only if ALL hold — (a) step 3 found a 2026-09-24 replay run that ended DONE; (b) step 4 ran; (c) `real reds` = 0, where a red placed `expected` by a NAMED ruling (09-20 R32 for K3's frozen HOLD) is not real, and `fix not live` and `UNPLACED` ARE real (R113 set aside K9.4 / K17 for the 09-23 night only, L73). Otherwise `smoke green: no`, with the FIRST failing clause named. **K9.4's field for the stop line:** `K9.4: not_archived <n> · named partial <m> (<PASS|FAIL>)` per side read from K9.4 / K9.11 for losers (and the gainers pair in the table) — PASS only when INDEX CARD (7)'s proof holds for BOTH sides; FAIL otherwise, with the row that broke it.
REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s2-smoke-look-2026-09-24.md` — §0 Headline ≤5 lines → the deploy row (step 2) → the replay row (step 3) → the smoke output (step 4) → the table (step 5) → `## K9.4 AFTER THE FIX` (INDEX CARD (7)'s proof, clause by clause, each with its evidence) → `## SMOKE GREEN` (step 6, the three clauses each with its evidence) → `## ESCALATE` (every UNPLACED or real red first) → `## DIGEST FOR THE DESK` ≤15 lines. While you work the last line is `(run in progress)`.
STOP LINE (last non-blank line): `S2 SMOKE LOOK DONE · deploy: deploy-2026-09-24 · replay ran: <yes hh:mm DONE|yes hh:mm FAILED|no> · checks: <g> green / <r> red · K9.4: <value> (<PASS|FAIL>) · real reds: <n> · smoke green: <yes|no> · ESCALATE: <n>` — or `FAILED: window — <reason>` / `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write.
