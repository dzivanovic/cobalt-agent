MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: drafter `drc-k2-build-draft-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/50-draft-drc-k2-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-k2-build-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `38-draft-drc-k1-build.md`'s byte for byte; NO new string) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default, continue. Never end a turn between steps. ≤45 min. Grep patterns carry no backtick, no `\|`, no alternation — plain fixed strings, one per call, or the Read tool.

# DRC OVERNIGHT-POSITION LANE — WRITE THE K2 BUILD (THE REBUILD FROM AN EARLIER DAY'S CLOSE, `[F-27]`–`[F-29]` UNDER HIS R51) AND ITS NEW-BUILD CHECK

LADDER: `S3-P3 · F14` (DRC). LAW STEP (L67): v3 FINAL `docs/30 - Design/DRC-OVERNIGHT-POSITION-v3-2026-09-24.md` + his R51 / R52 (`cto-2026-09-24.md`, 12:55 / 12:57 ET — the tribunal CLOSED) → K1 BUILT `9a0fc900` on `38a70947` (`39`; worktree `/Users/cobalt/cobalt-wt/drc-d1`, branch `drc/d1-trading-log`, migration `0018`; report `a50e5515` on the branch) → K1 CHECK round 1 (`40`, `reports/drc-k1-check-2026-09-24.md`: Opus + Grok `ready for K2: YES`, 3 HOLDs in the hub's file-check) → K1 FIX ROUND 1 classified (`reports/drc-k1-fix-r1-draft-2026-09-24.md`: FIX 3 — H1 `opened_on` Optional/None for a stated position, H2 `record_stated_book` REFUSES an `opening` for D while D's prior trading day is recorded, H3 the route RECORDS the unpaired day; `## FOR K2`: THE SEAM MOVES on all three) → the fix build `48-drc-k1-fix-r1-build.md` (QUEUED on the `cobalt_dev` lock behind `42` / `43`) and its check `49`. → **THIS = write the K2 BUILD prompt and its CHECK prompt, BESIDE the K1 fix round (L72: the seam is settled in documents both cite — `48`'s `## SEAM FOR D2` and `## FOR K2` sections, which supersede `39`'s; the desk's ruling, `cto-2026-09-24.md` R77).** K2's BUILD launches on `48`'s tip after `49` — never before; your build prompt's PREFLIGHT reads `48`'s report stop line for its base. K3 / K4 are NOT this file's.

AUTHORIZATION (verify before you read anything else): `grep -n "^| R78 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print a row naming `50-draft-drc-k2-build.md`; `grep -n "^| R51 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-24.md"` must print his R2-1 "A" row; `grep -n "^| R77 " …` (same file) must print the desk's row reading H2 as a refusal and ruling K2 drafted beside the fix. Any missing → `FAILED: authorization mismatch — <which>` as the last line of your report and stop.

INDEX CARD: `LAWS.md` in full (L59) — **L1** (a rebuild that cannot pair is a loud FAILED row, never a silent flat; a difference between his statement and the earlier close is SHOWN, never smoothed), **L3** (ONE rebuild path; ONE `record_stated_book` behind every caller — R52), **L7** (his statement stays history; no engine value replaces a human-fed one silently), **L28** (K2 writes NO vault note — the `open_positions` unit is K3's; the `/drc` line is a PAGE render, not a note write), **L29** (store + pairing on user tables = write path; the floor is Opus; the mode `48` / `14` state — quote it verbatim, prose and line never disagree, L62), **L32** (nothing of his in any file you write; fixtures real-shape, dates and attribution stripped, L45), **L35**, **L57** (every rebuilt value ships with its stored inputs — `from_book_sha256`, the superseded `stated_book_id`, the file that seeded it), **L62** (STANDING STRINGS R17 / R19; the drc-d1 `.env` pair is his R22 — ZERO new strings expected), **L67** (a NEW BUILD's check seats Fable · Astra · Grok — the Anthropic seat runs as 09-22 R109 pins it, Opus 5.5; Astra on METER until Sat 09-26 06:47 ET → Opus 5.5 + Grok, floor met, Astra's return recorded; NO Gemini), **L68** (GATE EARLY: offline + with-DB + live-note on the build's own tree before BUILT, the stop line quotes all three; the check packet carries them), **L70** (v3's first-gate experiments for K2, if §7 names any, RUN before the chunk's code; a design-changing result = ESCALATE + the build STOPS at that row), **L72** (this lane holds no other lane; the seam with D2 / D3 and with K3 is stated as a contract in the build report's `## SEAM FOR D2` / `## FOR K3`, never a located call), **L75** (nothing of K1's open fix rows is re-done here: H1 / H2 / H3 land in `48`; K2 BUILDS ON THEM), **L76** (one lock for `cobalt_dev`; `.env` copied only when no other `~/cobalt-wt/*/.env` exists and no with-DB run is in flight, removed and proven gone before the stop line; any new migration applied ONLY inside the suite's own rollback transaction — never `cobalt db migrate` on `cobalt_dev`). Files: v3 WHOLE — `[F-27]`, `[F-28]`, `[F-29]` (the R2-1 texts, resolved "A" by R51), §3 (tables, hash, `drc_stated_books` history rows), §4 (failure modes — the two-current-resolves FAIL naming both ids, `40` ESCALATE 4, is K2's `[F-06]` reader), §6 row K2, §7, the experiments named for K2; R51 verbatim (`| R51 |` row: rebuild the later days from the earlier day's close, the statement kept as history in `drc_stated_books`, the difference on `/drc` as `stated book for <N> differed from <P>'s close: <trade_ids>`, the import does not stop; sub-item (iii) = A1 — the newest un-superseded file's fills, stored `stats_row` rows re-matched — a DESK READING, cite it as such); R52 (the `cli` literal, one `record_stated_book`, dry-run default); **`48-drc-k1-fix-r1-build.md` WHOLE** — its `## SEAM FOR D2` and `## FOR K2` section texts (the contract as amended by H1 / H2 / H3: `opened_on` Optional, `seed_for` case (iv) reached only in R51's order — D stated first, P recorded after — "exactly what K2's rebuild replaces", the unpaired-day `day` row with `not_computed.pairing`), its D4 edit blocks (what `pairing.py` / `store.py` / `models.py` look like after the fix — K2 is written against THAT shape), its stop-line shape (`48`'s report is where the K2 build reads its base `<tip>`); `39-drc-k1-build.md` (the K1 build — `## FOR K2` original text, the CLI, the migration `0018` pair, the experiments' shape) and its report `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k1-build-2026-09-24.md` (`## FOR K2`, `## SEAM FOR D2`, `## ESCALATE` — items 6 (the `day <= D` bound) and the DRAFTER PINS); `reports/drc-k1-check-2026-09-24.md` `## ESCALATE` items 4 and 6–10 (carried to K2 or classified NOT REAL — read the fix drafter's classification table for each); `reports/drc-k1-fix-r1-draft-2026-09-24.md` WHOLE; the D1 / K1 code IN THE WORKTREE `/Users/cobalt/cobalt-wt/drc-d1` at `9a0fc900` (`src/cobalt/drc/store.py`, `pairing.py`, `models.py`, `cli.py` or where `39` put the CLI, `src/cobalt/db_migrations/0016_drc.sql`, `0018_*.sql` + rollbacks, `placement.py`, `tests/cobalt/test_drc_*.py`); the shapes you copy: `39` (a chunk build on this worktree: PREFLIGHT, BASELINE, EXPERIMENTS, red-first steps, migration + rollback pair, the three suites under L76, report shape, stop line) and `48` (the NEWEST build on this worktree — its lock moves, its "grep patterns carry no backtick" rule, its allow strings = `14`'s line with the drc-d1 `.env` pair, R22); for the check, `40-drc-k1-check.md` (the NEW-BUILD check on this lane — packet shape, `ready for K3` stop field) and `49-drc-k1-fix-r1-check.md` (the newest check strings = `15`'s; the grok gate as R17's standing row; the packet LISTS the four deselected test ids).

DECIDE, before writing (each a line of your report with its `file:line` or v3 `[F-nn]`): (1) THE BASE — K2 builds on `drc/d1-trading-log` at `48`'s tip (`DRC K1 FIX R1 BUILT <tip>`), read by the K2 build's PREFLIGHT from `48`'s report stop line in the worktree (`/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k1-fix-r1-build-2026-09-24.md`); a base that is not a `DRC K1 FIX R1 BUILT` line → `FAILED PREFLIGHT`. (2) THE MIGRATION — does K2 need one (`[F-27]`–`[F-29]`: a superseded-by column on `drc_stated_books`, a rebuild provenance column on `drc_rows`, or NONE if v3 §3 already holds them)? If yes: the next free number across every branch (read `git -C /Users/cobalt/cobalt ls-tree --name-only <branch> src/cobalt/db_migrations/` for `main`, `drc/d1-trading-log`, `voice/v1-0923`, `radar/handicap-h1-0922`, `cards/stale-score-0922`, `bars/chunk-2-0920`; `0018` is K1's, `0015` is stale-score's under its X30 (A), `0014` H1's — `0019` expected), a new numbered file never a fold into `0016` / `0018` (`[F-02]`), applied only inside the suite's rollback (L76). (3) THE SCOPE — K2 exactly per v3 §6 row K2 + `[F-27]`–`[F-29]` under R51 + the `[F-06]` two-current-resolves FAIL (v3 §4; `40` ESCALATE 4 "belongs to K2") + the `day <= D` bound (`39` ESCALATE 6) if v3 places it in K2; the rebuild is FORWARD from the earlier day's close through every later recorded day (re-pair, re-seed, re-close), his statements kept as history rows (never deleted, never overwritten — `drc_stated_books` gains a superseded marker if v3 §3 lacks one), the `/drc` difference line rendered from stored rows (no note write, L28); `stats_row` re-match under A1 (desk reading — cite R51's row as such); nothing of K3 (the vault unit) / K4. (4) THE SEAMS — `## SEAM FOR D2` (does the rebuild change the route's contract? if D2's import route must call the rebuild when an earlier day's file lands, state the contract as text, never a located call, `[F-17]`) and `## FOR K3` (what K3 reads: `opened_on` may be None — "opened: not stated", the fix drafter's ESCALATE 2). (5) THE EXPERIMENTS — v3 §7's first-gate experiments for K2, if any (quote their pass conditions); if v3 names none for K2, the build's experiments are the cheap RUNS L70 asks for on every "not checkable from reads" claim — the two-current-resolves reader, the forward re-pair over a constructed three-day sequence, the A1 re-match. (6) THE CHECK'S SEATS — a NEW BUILD: Opus 5.5 + Grok; Astra METER (return `Sep 26th, 2026 6:47 AM`, the desk seats it from then); NO Gemini; fail closed below TWO.

WRITE (whole files, L19): (1) `prompts/2026-09-24/51-drc-k2-build.md` — `48`'s launch line BYTE FOR BYTE except path and remote-control `drc-k2-build-0924` (the `drc-d1` `.env` pair is his R22 — NO new string); the permission mode `48` states, quoted; Opus 5.5; the DESK LINE (launch only while no `~/cobalt-wt/*/.env` exists, no with-DB run is in flight, AND `49` has stopped — L76 and L67 in order); PREFLIGHT (the base from `48`'s stop line; the migration number free on every branch if one is needed; the lock; `cobalt_dev` at production's head `0013`), BASELINE (the three suites on the base — the numbers, `48`'s CLOSE numbers as the expectation), EXPERIMENTS / RUNS, then the build red-first per v3 §6 K2 (a failing test per behaviour, then the code; the constructed three-day sequence — P recorded, N stated, P's earlier file dropped later — is the fixture, real-shape, no date of his), the migration + rollback pair if any (`[F-02]`; `placement.py` row), the `/drc` difference line, the `[F-06]` reader FAIL; ALL THREE suites (L68) under L76 before the stop, the `TestMigrationRoundTrip` deselect and the table-set probe as `39` / `48` ran them, `0018` (and `0019`) rolled back / applied inside the transaction only; `cobalt jobs restarts <base>..<tip>` line (L42); stop `DRC K2 BUILT <tip> | on <base> | migration <nnnn|none> | X: <n> of <m> run, design-changing: <n> | offline <p>/<f> | with-DB <p>/<f> | live-note <p>/<f> | .env: removed | <nnnn>: rolled back | RESTARTS: <line> | tests added: <n> | ESCALATE: <n>` or `FAILED: <step> — <reason>`; report `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-k2-build-2026-09-24.md` committed on the branch, with `## SEAM FOR D2` and `## FOR K3`. (2) `prompts/2026-09-24/52-drc-k2-check.md` — `40`'s shape with `49`'s strings (byte for byte; grok gate = R17's standing row, NEVER a dated row; the packet LISTS the deselected test ids), round 1 of ≤3 of a NEW BUILD, ONE packet: the diff `<base>..<tip>`, the migration pair if any, the experiment / RUN outputs, the three suites' executed output, v3 §3 / §4 / §6 K2 / `[F-27]`–`[F-29]` / `[F-06]` and R51's text as the rules; checkers Opus 5.5 + Grok (Astra METER, recorded); fail closed below TWO; `## FOR THE CLASSIFIER` for round 2 (L75); stop `DRC K2 CHECK DONE · round: 1 · opus: … · grok: … · astra: METER … · houses that checked: <n> of 3 · defects that HOLD: <n> · ready for K3: <YES|NO> · ESCALATE: <n>` or `FAILED: …`. Both prompts carry `R__` launch-row placeholders for the desk and a `comm`-checkable RULE STRINGS sentence naming the precedent line each copies.

REPORT (LAST): `docs/40 - DevDocs/reports/drc-k2-build-draft-2026-09-24.md` — `## §0 Headline` ≤5 lines → `## L74` (one line if the block appears) → `## DECISIONS` (the six above, each with its source) → `## MIGRATION` (needed or not; every branch read; the number pinned) → `## SEAMS` (`## SEAM FOR D2` and `## FOR K3` as the build prompt states them) → `## OWNER ITEMS` (verbatim, for him — expected NONE: v3 + R51 / R52 settle the lane; only a decision that is genuinely HIS, one per line, never a design question) → `## FOR DEJAN` (new strings: expected 0) → `## ESCALATE` → last line `DRC K2 BUILD DRAFTED · base: 48's tip · migration: <nnnn|none> · experiments: <n> · prompts: 2 · new rule strings: <n> · owner items: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then no line starts with `DRC K2 BUILD DRAFTED` or `FAILED`.
