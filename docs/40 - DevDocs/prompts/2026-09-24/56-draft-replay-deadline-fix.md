MODEL: Opus 5.5 (`claude-opus-5-5`, 09-22 R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: drafter `replay-deadline-fix-draft-0924`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-24/56-draft-replay-deadline-fix.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control replay-deadline-fix-draft-0924 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven precedented READ-ONLY strings, `50-draft-drc-k2-build.md`'s byte for byte; NO new string) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO git write, NO launch (L36), no vault / memory write — Write tool only, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default, continue. Never end a turn between steps. ≤45 min. Grep patterns carry no backtick, no `\|`, no alternation — plain fixed strings, one per call, or the Read tool.

# NIGHTLY REPLAY — CLASSIFY THE 09-24 DEADLINE FAILURE AND WRITE THE FIX BUILD + ITS CHECK

LADDER: `S2-P4 · F12` (Missed records, nightly replay — DONE-LIVE since 09-23; this is a DEFECT on a live feature, not new scope). LAW STEP (L67 / L75): a FIX ROUND — you classify first, then write ONE build prompt and ONE check prompt; the build runs in its own worktree under L76's lock with the GATE EARLY suite (L68: offline + with-DB + read-only live-note before its stop line); the check seats Opus 5.5 + Grok ("every other check", L67 as amended 2026-09-24; Sol is on METER until 2026-09-26 06:47 ET); the fix rides the 09-25 evening deploy set with stale-score + H1 if it is BUILT and CHECKED by then (L43). You launch nothing; the desk launches from your files.

## THE EVIDENCE (the desk's reads, `cto-2026-09-24.md` R95 — verify each line yourself before you rely on it)
1. `logs/replay.err:68` — `2026-09-24 21:40:06.498 | ERROR | cobalt.replay.runner:run_nightly:484 - replay 2026-09-24 step line FAILED — DeadlineExceeded: line: past the deadline 21:35:00 ET`; `:69` the F17 wrapper's `com.cobalt.replay FAILED — StepFailed: step line failed …`.
2. `src/cobalt/replay/runner.py:82` — `STEPS = ("movers", "cards", "formations", "line")`; `:150` `replay_deadline` = the backup's `at` minus `replay.backup_margin_s`; `:293` `check_deadline`; `:478–484` the step loop.
3. `logs/replay.err:52–66` — movers and cards ended `21:10:37.900`; the next line is `:67` at `21:40:04.138`; no line in between. The formations step therefore ran ≈29.5 minutes.
4. `logs/replay.log:467` — `replay 2026-09-24: scans=235 formations=197 path_b_only=115 counts={'avoided': 1007, 'formed': 3225, 'input_stale': 672, 'not_evaluable': 14242, 'not_formed': 63104} writes: none`. Against `:95` — `replay 2026-09-23: scans=235 formations=0 path_b_only=48 counts={'avoided': 294, 'input_stale': 291, 'not_evaluable': 7182, 'not_formed': 3983}`, a run that took 2 min 19 s (`replay.err:18` RUNNING `21:10:00.563` → `:39` DONE `21:12:19.759`).
5. What changed between the two nights: `deploy-2026-09-24` (`a2d320b8`, the setups re-land — all 13 setup definitions at defaults, 09-21 R44; the mover-bars fix; read `reports/deploy-2026-09-24-reland.md` §0 and `git -C /Users/cobalt/cobalt log --oneline deploy-2026-09-23..deploy-2026-09-24`).
6. `configs/cobalt/jobs.yaml:201–230` (the replay and backup jobs, `at` times) and `configs/cobalt/taxonomy/tunables.yaml:948` (`replay.backup_margin_s`). The 21:40 backup is HIS hard edge (09-19 R32; `topics/cto-desk.md`): moving it is an OWNER ITEM, never your fix.
7. Consequences already recorded, not separate defects: smoke K10.1 (no `drc-misses` unit in `DRC-2026-09-24.md`) and K10.2 (`vault_writes` 0) — the 09-24 miss line was never written. Tomorrow's 21:10 run fails the same way if the formations step is unchanged.

## INDEX CARD (read in this order; nothing else until a step needs it)
`/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (you draft a build prompt — L59) → `docs/40 - DevDocs/reports/cto-2026-09-24.md` rows R95, R88, R89 and the CURRENT `## §5` table (what runs where tonight; the lock and the house lane) → `reports/s2-smoke-look-2026-09-24.md` in full → `src/cobalt/replay/runner.py` in full, then every module the formations step calls (`formation_replay`, the detectors, the counterfactual-R / archived-bars reads, `cobalt.cards.expire`) → `src/cobalt/replay/` tests that pin the step's behaviour and timing → `docs/40 - DevDocs/prompts/2026-09-24/07-mover-bars-fix-build.md` + `08-mover-bars-fix-check.md` (the precedent PAIR: a same-day fix build with GATE EARLY and its Opus + Grok check — your two files copy their launch lines, rule strings and section shapes byte for byte, changing only the names, paths, base, FIX rows and questions) → `prompts/2026-09-24/48-drc-k1-fix-r1-build.md` §PREFLIGHT (the L76 lock check and `.env` copy-by-name shape) → `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` → `docs/00 - Project/BACKLOG.md` (grep `replay`).

## STEP 1 — CLASSIFY (L75), BY READS
Write a classification table in your report BEFORE any prompt file: for the cause and for every remedy you consider — `FIX` (engine-side, provable by a test, widens nothing), `NOT REAL`, `UNPROVEN` (L70 — needs a measurement no read can give), `OUT OF SCOPE`, `OWNER ITEM` (a schedule or budget only he can move: the backup time, the replay start time if it collides with the archiver's 20:30 run and its ~20:55 finish, the margin tunable's ruled value). Answer from the code: (a) where the ≈29.5 minutes go — per formation (3,225 formed) or per scan (235) or in the counterfactual-R reads of archived bars, and which call is the multiplier the 13 setups introduced; (b) whether the step does work it does not need for the miss line (e.g. evaluating every formed formation when only the missed set reaches the line); (c) whether a DB round trip per formation or per bar window exists that a batched read removes; (d) what a bounded run looks like — the step must END inside the deadline with a loud, named partial (L1: never a plausible-empty miss line; a cut run says so in the DRC unit and the log) rather than raise at the `line` step after doing all the work; (e) what the offline / with-DB tests must pin so a future setup count cannot silently outrun the deadline again (a timing budget test is UNPROVEN unless it runs in CI time — say what is provable). Every FIX row names file:line and the test that will hold it. If the honest answer is "the engine cannot form 3,225 formations inside 25 minutes without a design change", say so in one ESCALATE line, still draft the bounded-run FIX (d) so the miss line is written for what was evaluated, and list the design question for the desk — never widen the fix into a redesign (L75).

## STEP 2 — WRITE THE TWO PROMPTS
`docs/40 - DevDocs/prompts/2026-09-24/57-replay-deadline-fix-build.md` (Opus 5.5, `--permission-mode acceptEdits` + `07`'s allowlist; worktree `/Users/cobalt/cobalt-wt/replay-deadline`, branch `fix/replay-deadline-0924` cut from `main` at its tip on launch — the build's PREFLIGHT reads the tip; migration: none expected — a migration makes this an ESCALATE, not a fix) and `58-replay-deadline-fix-check.md` (Sonnet 5 hub, cwd `/Users/cobalt/cobalt-wt/agy-trial`, Opus 5.5 + Grok, `08`'s shape; STAGGER gate = the literal `no other house hub is running` on a launch row of the desk file, as `44` / `45` word it; its `<tip>` placeholder filled by the desk at the BUILT line). Both: launch line first, index card second, ONE complete block, tags MODEL / SEAT / SESSION / auto mode / METER; the build's stop line = `REPLAY DEADLINE FIX BUILT <sha> | on <main tip> | offline <n>/<n> | with-DB <n>/<n> | live-note <n>/<n> | .env: removed | FIX: <n> of <n> | replay dry-run 2026-09-24: <duration> | ESCALATE: <n>` (GATE EARLY — the three suites quoted; the dry-run row is the build's own timing proof of the formations step against `cobalt_dev`'s archived bars for 2026-09-24 if they exist there, else `NOT MEASURABLE ON cobalt_dev` — never against production); the check's stop line = `REPLAY DEADLINE FIX CHECK DONE · round: 1 · opus: … · grok: … · ready for a deploy prompt: YES|NO · ESCALATE: <n>`. The build's AUTHORIZATION row: `R__` (the desk fills it on a committed launch row); `git add` names explicit paths; "never end a turn between steps"; the RECOVERY RULE; the L76 lock check (`ls ~/cobalt-wt/*/.env` = no matches; the `.env` copied by name, removed and proven gone before the stop line); no `cobalt_dev` migration left applied. Rule strings: `comm` your two launch lines against `07`'s and `08`'s in your report — expected NEW strings: 0; a new string is an ESCALATE line for the desk's ONE list, never launched by you.

## STEP 3 — `## FOR THE DEPLOY` and `## FOR DEJAN`
`## FOR THE DEPLOY`: what the 09-25 deploy prompt's drafter needs — the branch, restarts by rule (L42: `com.cobalt.replay` is a one-shot; name every reader of a changed config), the first live proof (the 09-25 21:10 run ending DONE inside the deadline, the miss line in `DRC-2026-09-25.md`), the rollback shape. `## FOR DEJAN`: at most ONE owner item, in his terms (what he gets / loses), only if an engine-side FIX cannot make the 21:10 run fit; otherwise the line `no owner item — the fix is engine-side`.

## REPORT (written LAST): `docs/40 - DevDocs/reports/replay-deadline-fix-draft-2026-09-24.md`
§0 Headline ≤5 lines → the classification table → `## Where the time goes` (file:line, the multiplier, the numbers) → `## Prompts written` (paths, `comm` results, new strings) → `## FOR THE DEPLOY` → `## FOR DEJAN` → `## ESCALATE` → the last non-blank line EXACTLY `REPLAY DEADLINE FIX DRAFTED · cause: <one clause> · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · prompts: 2 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Until then the last line is `(run in progress — next step under ## CONTINUE)`.
