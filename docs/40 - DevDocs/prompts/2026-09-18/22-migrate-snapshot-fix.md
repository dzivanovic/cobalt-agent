MODEL: Opus 5 (`claude-opus-5`) — the migration harness IS the production write path (L29 floor) · SEAT: build hub `migrate-snapshot-0918`, launched by the CTO desk in the background. NO NEW RULE: the launch line is `15-migrate-harness-fix.md`'s, unchanged — a subset of R1 list (1) of 2026-09-18 for this worktree; NO production command exists in it. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/22-migrate-snapshot-fix.md' and follow it exactly." --model claude-opus-5 --remote-control migrate-snapshot-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; `COBALT_ENV=production` NEVER appears, never `--allow-prod`, no vault write, no push, no merge, no rebase, no cherry-pick · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Migration harness — make the proof SNAPSHOT-CONSISTENT. Today a write by ANY other session between the BEFORE and the AFTER probe makes a good migration read `CHANGED` and roll back. Tests first. `cobalt_dev` is YOURS ALONE while you run. DO NOT STOP until the report ends `SNAPSHOT BUILT …` or `FAILED …`.

THE DEFECT (desk finding 2026-09-18 17:3x ET, from `prod-proof-only-2-2026-09-18.md` ESCALATE 2 and `src/cobalt/db_migrations/cli.py` `cmd_migrate`): the migrate transaction runs at Postgres's default READ COMMITTED — `before = _probe_all(conn)` → `_apply(conn, paths)` → `after = _probe_all(conn)` → any `CHANGED` verdict → `conn.rollback()`. Under READ COMMITTED each probe statement sees whatever OTHER sessions committed in the meantime. Until today the two probes were seconds apart; with production `system.bars` at 8.6M rows each probe takes ≈46 s, so the window is ≈100 s — and production's scheduled one-shots keep writing while a deploy has only the two RESIDENTS down: `com.cobalt.heartbeat` every 15 minutes and `com.cobalt.seat-usage` hourly update their rows in `system.cobalt_jobs` (in the proof: 15 rows); `cobalt_redactions` is append-only telemetry (it drifted 118 → 120 rows between two probes on dev this afternoon); `vault_writes` grows with any vault write. One such commit inside the window → that table reads `CHANGED` → the whole migration rolls back → the deploy fails at its migrate step with the residents down (a clean failure, but an outage for nothing, at roughly a 1-in-6 chance per attempt tonight).

DESK DESIGN (architect; build to it; a reason it cannot work → `FAILED: design — <why>`, do not substitute your own): the migrate transaction — and the `--proof-only` transaction — run at **REPEATABLE READ**: the snapshot is taken at the transaction's first statement; BEFORE and AFTER both read THAT snapshot plus this transaction's OWN changes, so the proof answers exactly the question it was written for — "did THIS migration change existing content?" — and other sessions' commits are invisible to both probes. Set it where `_connect` configures the connection (the driver's isolation-level setting, before the first statement; `--proof-only` = REPEATABLE READ + READ ONLY). Known consequence, and it is the right one: if another session commits a change to a row/table the migration itself then modifies, Postgres raises a serialization failure (`could not serialize access…`) → the existing `except BaseException: conn.rollback(); raise` path → nothing applied, loud error; say so in the DevDoc and make the error message name it. Rejected, say why in the DevDoc: excluding job/telemetry tables from the proof (a weaker proof, and the list would rot); stopping every scheduled job for a deploy (a bigger outage surface, and it does not cover an ad-hoc writer).

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 17:4x ET), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** list (1) (this worktree's dev rules), **R3** + **R5** (the weekend push), **R18** (every build is checked by at least three tribunal members before it ships — that check is the desk's next run, Saturday, not yours). Compare this launch line rule by rule with R1 list (1); a rule not in it → `FAILED: authorization mismatch — <rule>`. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves the rows are committed. Laws: L1, L3, L10, L29, L35, L41 interim (`.env` by name only, never printed, removed after), L46, L54, L55, L57, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, remove any `.env`, wip-commit, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → `src/cobalt/db_migrations/cli.py` IN FULL (`_connect`, `cmd_migrate`, `_probe_all`, the `--proof-only` branch) + `src/cobalt/db.py` (`_open`, `connect_migration`: autocommit at connect, `SET search_path` / `set_config` run before any transaction opens) → `tests/cobalt/test_migrate_proof.py` (today's tests) → this worktree's `docs/40 - DevDocs/reports/migrate-harness-fix-2026-09-18.md`, `reland-gate-2026-09-18.md`, `prod-proof-only-2-2026-09-18.md` (ESCALATE 2) → `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-migrate-fix/REVIEW.md` (F1: both houses already pointed at the two-snapshot problem inside ONE probe; this is the same problem BETWEEN the probes). STATE OF THIS BRANCH: `sprint-2/stack`, tip `15ec09d` (report commits above the gated stack tip `d72ece4`), a branch OFF main — you only ADD commits on top. Report (L48): NEW file `docs/40 - DevDocs/reports/migrate-snapshot-fix-2026-09-18.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → tables per step → `## ESCALATE` → last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, NO pipe, no redirect; outputs read from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git rev-parse --abbrev-ref HEAD` (= `sprint-2/stack`, else `FAILED: wrong branch`), `git log -1`, `date`, `uv run pytest --co -q tests/cobalt/test_migrate_proof.py`, `.env` cp → `COBALT_ENV=dev uv run cobalt db migrate --help`. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove the `.env`, wip-commit, stop.

## 1. Tests first (each new test FAILS before the change — quote the failing lines — and passes after), all against `cobalt_dev` with a SECOND connection playing the other session
(a) THE DEFECT, reproduced: open the migrate connection as the harness does, take the BEFORE probe, then from the second connection INSERT (and commit) a row into a small proof table that the test owns or can clean up exactly (prefer a scratch table created and dropped by the test inside the `system` side and registered for the probe through the harness's own table discovery — if discovery is a fixed list, use `cobalt_redactions`, insert one row and DELETE that exact row by id in teardown), take the AFTER probe → today `CHANGED`; after the fix `UNCHANGED` (rows and digest identical). (b) the transaction's OWN change is still seen: an UPDATE made through the migrate connection between the probes reads `CHANGED`. (c) `--proof-only`'s transaction is REPEATABLE READ and READ ONLY at the server (`SHOW transaction_isolation`, `SHOW transaction_read_only` through the harness's connection). (d) the migrate transaction is REPEATABLE READ and read-write. (e) a concurrent committed UPDATE to a row the migrate transaction then UPDATEs raises the serialization error and leaves nothing applied (the harness's except-path rolls back) — if this cannot be made deterministic in a test, say why and cover it in the DevDoc only. Every test cleans up exactly what it created; none may leave `cobalt_dev` different (prove it: the no-op `COBALT_ENV=dev uv run cobalt db migrate` proof table before step 1 and after step 2 lists the same rows + digests for every table except the append-only `cobalt_redactions` if a redaction fired — name it if so). Commit the red tests: `test(db-migrate): the proof must not see other sessions' commits — red`.

## 2. Build, then prove on `cobalt_dev`
Implement per the DESK DESIGN — the smallest change that satisfies the tests. Then: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quoted, nothing applied · `COBALT_ENV=dev uv run cobalt db migrate` → no-op, no `CHANGED` · ROUND TRIP `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → `COBALT_ENV=dev uv run cobalt db migrate` → digests identical before and after, 0006/0007 back (DDL under REPEATABLE READ must work — this is the proof that it does) · `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed (the re-land gate: 1845 passed) · `COBALT_ENV=dev uv run cobalt validate` exit 0 · `rm` the `.env`, `ls -la` proves it gone · `uv run pytest -q tests/cobalt tests/taxonomy` (offline) → 0 failed · `uv run cobalt jobs restarts d72ece4..HEAD` → table verbatim. DevDoc for `db_migrations/cli.py` updated (why REPEATABLE READ; the serialization-failure consequence; the two rejected designs; the 2026-09-18 finding in two lines). Commit: `fix(db-migrate): run the proof at REPEATABLE READ — other sessions' commits cannot fail a good migration (deploy-2026-09-18)`. Last line `CONTINUE: step 3`.

## 3. Close
`git status --porcelain` empty · `git diff --stat 15ec09d HEAD -- src tests docs` listed · §0 Headline · last line:
`SNAPSHOT BUILT <tip> on sprint-2/stack | migrate + --proof-only at REPEATABLE READ | defect reproduced red, green after | dev round trip 0005↔0007: identical | with DB: <b> passed, 0 failed | offline: <a> passed, 0 failed | OWED: checked by ≥3 tribunal members (R18), production --proof-only on the final code, then the deploy | ESCALATE: <n>`
Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production.
