MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50; migrations ride this branch, L29) · SEAT: P4 build hub `p4-e2-0918`, launched by the CTO desk in the background after Dejan's approval of the ONE new rule in this launch line (`Bash(git rebase sprint-2/cards)`); every other rule is a subset of the list he approved 2026-09-17 06:11 ET (R6) and 2026-09-18 06:46 ET (R1 list (3)) for this worktree and this job shape — not brought to him again (`prompts/UNATTENDED-LAUNCH.md` §1.3). This launch line DROPS every `.env`, database, replay and `grok` rule: this run is OFFLINE by design. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p4` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/08-p4-e2-fixes.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-e2-0918 --allowedTools "Bash(claude -p *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git rebase sprint-2/cards)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(touch scratch/*)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" "CronCreate" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every launch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61), never `bypassPermissions`, push never in it · METER: Anthropic medium–large (two or three Opus headless chunks + conflict resolves); Codex = NONE; Grok = NONE in this run · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# S2-P4 build 2 — put P4 on top of P2 (`sprint-2/cards`), build E2 (R1-21's positive half against P2's REAL contract), fix the three defects the Grok review confirmed, settle its eight unverified findings. OFFLINE ONLY: `cobalt_dev` belongs to another session today — no `.env`, no `COBALT_ENV=dev` command, no migration. DO NOT STOP until the report ends `BUILT …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan; a prompt file is not an approval. His words, with times, are committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-17.md` rows **R6** (06:11 "approve." — the P4 hub allowlist incl. `claude -p`, git add/commit/rebase, `CronCreate`), **R5** (unattended standard), **R17** (ceiling 50, already shipped on this branch); `cto-2026-09-18.md` rows **R1** (06:46, list (3)), **R3** + **R5** (the weekend push: build hard today through Sunday) and the row approving **`Bash(git rebase sprint-2/cards)`** for this launch (absent → `FAILED: authorization mismatch`). `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves they are committed. Laws: L61, L50, L29, L55, L36 (you launch builders; builders launch nothing). Mismatch → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (recovery rule), stop. The desk watches that line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (hub, L59) → this worktree's `docs/40 - DevDocs/reports/s2-p4-followup-2026-09-18.md` (§3 why E2 was skipped, §5 the Grok table, `## ESCALATE` 1–8) and `scratch/review/grok-findings.md` (the findings in Grok's words) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/01-p4-followup.md` §3 (THE E2 SPEC — binding here verbatim) and its §1.2 (the conflict procedure) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md` (builder launch shape in its step 2, RECOVERY RULE) → `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` (R1-21/R2-7 block ≈ line 189; line 184/189 the entrypoint name; line 191 the required headers) → `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/s2-p2-reship-2026-09-18.md` §0 (P2 is re-landed, gate green 1801/0; tip `976528f`). Laws, one line each: L46 one run one commit, clean before any wait · L54 you rebase, the desk merges · L29 Opus floor for every code chunk · L35 artifact, not claim · L45 real-shape fixtures, never edited to fit · L53 the refusal proof stays · L57 every derived number replayable · L32 no user data in the repo; a wrong-side query fails loud and STAYS failing loud · L3 one path · L60 recovery rule. Report (L48): `docs/40 - DevDocs/reports/s2-p4-build2-2026-09-18.md` — §0 Headline ≤5 lines (line 1 `DO NOT STOP until BUILT`) → `## PREFLIGHT` → tables → `## ESCALATE` → last line = `CONTINUE:` / stop line / `FAILED`.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs read from the tool result, files written with Write/Edit; long runs (`claude -p`) go `run_in_background`. MID-RUN DENIAL = `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry a denied command in another shape, never ask another session to run it. PREFLIGHT (rows in the report, every probe inside its own rule's pattern): `git status --porcelain` (empty or `?? scratch/`) · `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (also the L47 meter probe; a usage-limit message → record the reset time, `CronCreate` a wake-up at reset + 5 min, wait) · `git -C /Users/cobalt/cobalt status --porcelain` · `uv run pytest --co -q tests/cobalt/test_replay_runner.py` (or the nearest replay test file — `ls tests/cobalt` first). Any denial → `FAILED PREFLIGHT: <rules>`.

## 1. Rebase onto P2
1.1 Record `git rev-parse --short HEAD` (expected `2d954a5`), `git log --oneline main..HEAD` count, and P2's tip `git log --oneline -1 sprint-2/cards` (expected `976528f`; a later report-only commit is fine, anything else → record it and continue). 1.2 `git rebase sprint-2/cards`. Conflicts: `01-p4-followup.md` §1.2's procedure exactly — brief in `scratch/rebase-brief-<n>.md`, ONE Opus headless run per conflict stop, keep BOTH intents, hub verifies (`git diff --check`, no markers, touched tests pass), `git add <files>`, `GIT_EDITOR=true git rebase --continue`; three failed resolves on one conflict = `FAILED`. Expected zones: `src/cobalt/db_migrations/placement.py` / `__init__.py` (P2's 0006/0007 entries vs P4's 0008/0009 — BOTH sets stay, numeric order), `tests/cobalt/test_radar_*.py`, `configs/cobalt/taxonomy/tunables.yaml`. 1.3 After: `git log --oneline sprint-2/cards..HEAD` count = P4's own count from 1.1 · `uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (offline). Last line `CONTINUE: step 2`.

## 2. Chunk E2 (Opus headless) — `01-p4-followup.md` §3, verbatim, now that P2 is under the branch
Brief `scratch/e2-brief.md` = that §3 text + this addition: Grok BLOCKER 2 is part of this chunk — `replay/runner.py:166` imports `cobalt.radar.evaluate_cli` while the plan (lines 184/189) names `cobalt.radar.evaluate`; the builder records P2's SHIPPED entrypoint first (module, callable, arguments, capability/version marker, typed returned fields — from the code under this branch, not from the plan) and binds to THAT; if the shipped name differs from the plan's, the code follows the shipped contract and the difference is an ESCALATE line for the plan, never a shim with two names (L3). `test_r1_21_a_present_but_incompatible_p2_fails_loud` still passes; the "formation replay unavailable" line still logs when P2 is absent. A P2-side field that cannot be populated without a schema change = STOP the chunk, ESCALATE the exact field — never a nullable shortcut that weakens 0009's CHECK. Hub verifies as chunk B was verified (tests-first evidence in the builder's report, suite line, `git diff --stat`). Commit `feat(s2-p4): E2 — formation replay binds to P2's shipped contract (R1-21 positive half)`. Last line `CONTINUE: step 3`.

## 3. Chunk FX (Opus headless) — the confirmed defects, tests FIRST (each new test fails before the edit, passes after)
Brief `scratch/fx-brief.md`: (a) **smoke K8** (`configs/cobalt/smoke/s2.yaml`, `side: user`) reads `system.cobalt_jobs`, which `cobalt_user` is never granted. DESK RULING (architect, L32): NO cross-side grant is added — the tenancy wall stays; the check is split so the `system.cobalt_jobs` part runs on the SYSTEM side and the user-side part reads only user tables; a test proves no `side: user` smoke query names a `system.` relation (and the reverse). (b) **`replay/movers.py:75` `REQUIRED_HEADERS`** gains `Asset Type` (plan line 191), with the non-equity classification it exists for; fixture = the REAL-SHAPE movers export already in the repo (L45) — if that fixture has no `Asset Type` column, STOP this item and ESCALATE with the fixture path and its real header row (the plan or the fixture is wrong; you do not invent a column). (c) **`src/cobalt/jobs/wrapper.py:23`** + DevDoc `jobs/wrapper.md:18`: the "every 5 min" heartbeat claim → the real rule (`timeout_s / heartbeat_fraction`), docstring + DevDoc only. Commit `fix(s2-p4): smoke K8 stays inside the tenancy wall; movers requires Asset Type; wrapper heartbeat doc`. Last line `CONTINUE: step 4`.

## 4. The eight findings nobody verified (followup ESCALATE 8: 3 MAJOR + 5 MINOR from Grok) — settle each, by the HUB, from the code (L35)
One table row per finding: Grok's claim · file:line read · verdict `REAL` / `NOT REAL` / `DESIGN QUESTION` with the evidence in one sentence. `REAL` and small (≤ ~30 lines, no schema change, no ranking/scoring behaviour — L52) → one more Opus headless run, tests first, its own commit. `REAL` and larger, or anything touching what reaches the card → ESCALATE with a one-line proposed fix; do not build it. Followup ESCALATE 3 (replay window seam) and 5 (L53 gate scores declared windows) are DESIGN QUESTIONS already — restate each in two lines for the desk, build nothing. Last line `CONTINUE: step 5`.

## 5. Close (offline)
`uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote) · the leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures` passes · `uv run cobalt jobs restarts sprint-2/cards..HEAD` → table verbatim, UNCLASSIFIED → ESCALATE · worktree clean (only `?? scratch/`), report committed. Last line:
`BUILT <tip> | on sprint-2/cards <P2 tip> | offline: <n> passed, 0 failed | E2: built/STOPPED | fixes: K8 <y/n> · Asset Type <y/n/ESCALATED> · wrapper doc <y/n> | findings: <r> real (<f> fixed) / <n> not real / <d> design | OWED: DB verification + rollback round-trip + second-house re-review, after P2 is on main | ESCALATE: <n>`
Then stop. The desk verifies, stops this session, and — once deploy 2 has put P2 on main — launches the verification run (rebase onto main, `cobalt_dev` 0008/0009, Grok re-review) that ends READY FOR MERGE.
