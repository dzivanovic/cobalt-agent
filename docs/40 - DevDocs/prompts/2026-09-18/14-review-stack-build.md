MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two reviewers, verify their findings; no write path · SEAT: review hub `review-0918b`, launched by the CTO desk in the background under Dejan's R9 of 2026-09-18 14:33 ET (`Bash(grok *)` + `Bash(agy *)`, standing for the weekend's review runs). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/14-review-stack-build.md' and follow it exactly." --model claude-sonnet-5 --remote-control review-0918b --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/review-stack-build)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write of any kind, no vault write · METER: Anthropic small–medium; Grok = one headless review; Gemini (agy) = one headless review · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Second-house review of the STACKED BUILD (R8: "did a build and need to deploy" → second opinion first). What ships today is `sprint-2/cards` = S2-P2 (radar cards, shipped DARK) + ops-0918 (over-band day-mode step-down, RESTARTS classifier rules, `ops/cto-desk.sh`) + three seam fixes. P2's PLAN was reviewed by Astra (3 rounds); P2's BUILD and all of ops-0918 have been read by no house but the one that wrote them. Grok and Gemini read the code diff independently; you verify every BLOCKER and MAJOR against the real files (L35). DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R8** (14:29) and **R9** (14:33 "Approved" — `Bash(grok *)`, `Bash(agy *)`, standing for the weekend's review runs). `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` proves it. Mismatch → `FAILED: authorization mismatch — <what>`. Laws: L33, L35, L36, L37 (reviewers FIND; the desk and Dejan decide), L44, L47, L32.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, stop.

INDEX CARD (you; the reviewers get the packet only): `prompts/UNATTENDED-LAUNCH.md` §2 → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` — the FIRST review run of today, same shape: its §1 staging rules, its §2 launch spellings (BOTH worked at 14:5x: `grok --sandbox cobalt-job --allow "Write(<dir>/**)" -p "…"`, and `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="…"` with a printed answer that YOU save), its §3 verification rules and table shape BIND you here unchanged → `scratch/review-deploy-stack/REVIEW.md` in this worktree (what that run looked like). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-stack-build/REVIEW.md` (gitignored scratch; you commit nothing) — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Verdict table` → last line.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no pipe, no redirect, no `cd`; long runs go `run_in_background`; outputs read from the tool result; files via Write. PREFLIGHT (METER, L47): `grok --version` · `agy --version` · `date`. A usage-limit or login message → that house = `METER`, continue with the other; both unavailable → `FAILED: no second house reachable`.

## 1. Stage the packet in `scratch/review-stack-build/` (`mkdir -p` first; Read → Write, byte-identical; nothing under `data/`, no `.env`, no settings file — L32, L41)
(1) `diff-src.txt` = the output of `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff main..HEAD -- src configs ops` (tests and fixtures excluded — they are most of the volume; say the line count) · (2) `diff-seam-tests.txt` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff main..HEAD -- tests/cobalt/test_trader_settings.py tests/cobalt/test_aset_config.py tests/cobalt/test_daymode.py tests/cobalt/test_daymode_note.py tests/fixtures/radar/card-settings.real-shape.json` (the tests that CHANGED at the seam today — were they fixed or weakened?) · (3) `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` (P2's reviewed plan — the contract the build answers to) · (4) `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/stack-gate-2026-09-18.md` (all runs — what the gate found and how it was fixed) · (5) `/Users/cobalt/cobalt-wt/ops-2026-09-18/docs/40 - DevDocs/reports/ops-2026-09-18.md` §1 only (the over-band spec and build, R13's words) — Write that section alone · (6) `laws-excerpt.md` — LAWS.md entries L1, L3, L7, L8, L10, L11, L28, L32, L45, L53, L57, verbatim · (7) `QUESTIONS.md`, verbatim from the block below. If the stack gate's third run is still committing test fixes while you stage, take (2) again just before launch and say which tip you staged (`git -C … log --oneline -1`).

QUESTIONS.md (verbatim): "You are an independent reviewer from another house. `diff-src.txt` is production code about to be deployed to a LIVE trading-support system used by one discretionary day trader: the system scans for stocks in play, grades setups and recommends size; the trader always executes by hand. Two features ship together. (A) S2-P2 'radar cards': an evaluator turns radar pool members into scored trade cards — it ships DARK: a settings row `radar.cards_enabled = false` must make it write NO user-visible card and change NOTHING the trader sees. (B) ops-0918 'over-band step-down': a day whose trade count is ABOVE the trader's ruled band maximum is an adverse signal that steps the next day's risk mode DOWN one rung; at or below the maximum nothing happens; below the minimum nothing happens; an unset band must still be safe. Read only the files in this folder. For every finding give SEVERITY (BLOCKER = wrong behaviour that reaches the trader, corrupts or silently loses data, or breaks a law in `laws-excerpt.md` / MAJOR = likely defect or a plan clause not met / MINOR), FILE + LINE in the diff, the LAW or PLAN CLAUSE broken, and a concrete FAILING SCENARIO with inputs. Do not pad; say 'none found' for a category rather than inventing. Check specifically: (Q1) DARK means dark — trace every write path in (A): with `radar.cards_enabled` false, or the row MISSING, or the row malformed, can any code path write a user card, alter `aset_sizings`, or change what the sheet renders? What happens on a missing row — loud failure (L1) or a silent default? (Q2) (B)'s boundary arithmetic: count == max, count == max+1, count == min-1, count == 0, band unset, band with min > max; is the reason line replayable from stored inputs (L57)? Is there exactly ONE path computing it (L3) — note `trade_count_band_placeholder` still exists beside `trade_count_over_band`: is that a second path or inert? (Q3) Scoring and ranking in (A): any number that reaches a card without its stored inputs (L57) or without its sample size / with n<30 rendered as a number (L8)? Any value hard-coded that the laws say must be config or the trader's ruling (L10, L53)? (Q4) Migrations 0006/0007 and their rollbacks: additive? idempotent on re-run? anything that rewrites or locks an existing production table? the view/table drop order in the rollback files? tenancy — every new table on exactly one side, user tables with `user_id NOT NULL` (L32)? (Q5) Fail-loud (L1): any `except` that swallows, any default that papers over missing data, any path that produces a plausible-empty artifact. (Q6) `diff-seam-tests.txt`: three test files were changed TODAY to get the stacked branch green. For each: was a real assertion weakened or deleted, or was a test that wrongly pinned the trader's live settings moved onto invariants + a constructed config as the gate report claims? (Q7) The RESTARTS classifier change in `src/cobalt/jobs/restarts.py`: can the new META / operator-script rules ever classify a path that DOES have a runtime reader as 'no restart'? (Q8) Anything else that would make you refuse to ship this. End with one line: `VERDICT: SHIP DARK AS IS / SHIP AFTER FIXES <finding numbers> / DO NOT SHIP`."

## 2. Launch both reviewers — `11-review-deploy-stack.md` §2 EXACTLY, with `scratch/review-stack-build/` in place of `scratch/review-deploy-stack/` in every path, `grok-findings.md` / `gemini-findings.md` in the new folder, `--print-timeout 20m` for agy (the diff is large). ONE attempt per house; HARNESS / METER recorded verbatim, never looped. Do not pre-read the diff for defects yourself (an anchored verifier confirms what it expects).

## 3. Verify — `11-review-deploy-stack.md` §3 EXACTLY (every BLOCKER and MAJOR against the REAL files in `/Users/cobalt/cobalt-wt/s2-p2-cards`, one row each, REAL / NOT REAL / UNVERIFIABLE FROM READS, agreement between houses = one row naming both). For each REAL finding add one column: `blocks a DARK ship? yes/no — why` (a defect that can only fire once cards are ENABLED does not block today's dark deploy but blocks P2 D2; a defect in (B), in the migrations, or in the dark gate itself blocks today). You fix NOTHING.

## 4. Close
Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER> · hub-verified REAL: <k> (blocks dark ship: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u> · staged tip <hash>`. Then stop.
