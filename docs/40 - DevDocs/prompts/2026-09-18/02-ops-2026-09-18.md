MODEL: Opus 5 (`claude-opus-5`) — item 1 is trading logic (day-mode step-down) · SEAT: ops hub `ops-0918`, launched by the CTO desk in the background on 2026-09-18 after the day-open is GREEN and after Dejan's approval of the list in this launch line (`prompts/UNATTENDED-LAUNCH.md`), three bare commands: `git -C /Users/cobalt/cobalt worktree add -b ops/2026-09-18 /Users/cobalt/cobalt-wt/ops-2026-09-18 main` then `cd /Users/cobalt/cobalt-wt/ops-2026-09-18` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/02-ops-2026-09-18.md' and follow it exactly." --model claude-opus-5 --remote-control ops-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-18/.env)" "Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-18/.env)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(COBALT_ENV=production uv run cobalt jobs list*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(chmod +x ops/cto-desk.sh)" "Bash(ops/cto-desk.sh --dry-run)" "Bash(shasum -a 256 *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt tag --list*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; no production restart, no production write, no vault write, no other seat's config · METER: Anthropic medium; Codex = NONE · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Ops 2026-09-18 — morning proof of `deploy-2026-09-17` (ops branches LIVE; P2 REVERTED, not live), over-band trade-count signal (ruled R13), `ops/cto-desk.sh` bring-back script (R11), repo-meta classifier rule for RESTARTS, radar real-cadence facts. DO NOT STOP until the report ends `OPS 0918 …` or `FAILED`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `fcb31caa`, 2026-09-17), not by Dejan; a prompt file is not an approval. His words are in committed files on main: `docs/40 - DevDocs/reports/cto-2026-09-17.md` rows **R5** (unattended standard), **R13** (07:37 ET "ruling A." — over the trade-count band = adverse, `down 1`; under the band does nothing), **R11** (always-on desk; the bring-back script is owed), and in `cto-2026-09-18.md` the row approving THIS launch line's allowlist. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves they are committed. Laws: L61, L41 interim (`.env` by name only, never printed, removed after), L29, L55, L7 (a step-down is trading logic — nothing you build goes live without his approval of the settings file by path + sha256, which the DESK obtains; you only produce the file and the hash). Mismatch → `FAILED: authorization mismatch — <what>`.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 → Memory `INDEX.md` → `## NOW` → LAWS.md L1, L3, L7, L8, L10, L28, L32, L42, L45, L46, L53, L54, L57 (hub on a trading-logic item: read those entries in full) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/ops-2026-09-17.md` §1b + ESCALATE 1 (on main since last night) → `deploy-2026-09-17.md` (what went LIVE, the dark row, ESCALATE) → `cto-2026-09-17.md` §15 findings + R11 + the 07:59 "OBSERVED" line. Report (L48): `docs/40 - DevDocs/reports/ops-2026-09-18.md` in this worktree — §0 Headline ≤5 lines → `## PREFLIGHT` → one table per item → `## ESCALATE` → last line.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect; `cd` is its own call; read outputs from the tool result; files via Write/Edit. PREFLIGHT first (one harmless variant per rule you will use; any denial → `FAILED PREFLIGHT: <rules>`). MID-RUN DENIAL = `FAILED: …`, wip-commit, stop; never another shape, never another session.

## 0. Morning proof of `deploy-2026-09-17` (read-only, from `/Users/cobalt/cobalt`) — AMENDED 2026-09-17 21:5x by the desk: P2 is NOT live (merged then REVERTED, `deploy-2026-09-17.md` §2.4 + ESCALATE 1–2); LIVE = `7e4841e` = ops-0917 + grok-deny + agy-trial-rebuilt only. No P2 table, no dark row, no `radar_score_run` exists in production — do not query for them.
`git -C /Users/cobalt/cobalt tag --list 'deploy-2026-09-17'` → `7e4841e` · `COBALT_ENV=production uv run cobalt heartbeat show` (no RED) · `tail -n 80 /Users/cobalt/cobalt/logs/radar.err` → premarket cycles running on the ops-0917 code (ceiling 45, band 2–6), no traceback; the 19:18–19:21 P2 incident lines (`CardSettingsError`, `trade_def_slug`, `radar_pool_failed_stage_check`) are HISTORY above the 20:02 restart — confirm none after 20:02 · `grep -c "ENTERED RED: radar" /Users/cobalt/cobalt/logs/heartbeat.log` = 88 (the 20:02 and 21:05 count) → still 88, else ESCALATE · `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT last_scan_at, members, failed_stage FROM radar_pool"` → cycling in premarket, `failed_stage` empty · archiver: `COBALT_ENV=production uv run cobalt jobs list` (the jobs table; `logs/archiver.log` is 0 bytes and stale) → last night's run 20:30–20:54, exit 0, requests = 1000 (tier_b gained `i1`, desk edit R14), 0 failures · BACKUP ID RECONCILE (L35): the deploy report cites snapshot `de4bfaa8` for the 19:15 `backup run`; `cobalt backup restore ssd de4bfaa8 …` said "no matching ID" at 21:48 and `logs/backup.log` never lists it, while `latest` restored fine — run `COBALT_ENV=production uv run cobalt backup status` and read `logs/backup.log` (never `restic` with the password in your hands, L41) → name the real 19:15 snapshot id and say where `de4bfaa8` came from. Anything RED the deploy report did not carry → ESCALATE first line of the headline.

## 1. Over-band trade count — RULED R13 (tests first; his words are the spec)
Spec: a day whose trade count is ABOVE `daymode.trade_count_band.max` (6) is ADVERSE with effect `down 1`; BELOW `.min` (2) does nothing; inside the band unchanged. Build, in `src/cobalt/daymode/`: a new signal id `trade_count_over_band` in `SIGNAL_IDS`, computed in `propose.py` where `trade_count_band_placeholder` sits today (replace the placeholder — one path, L3); its reason line cites the band and the count (L57). `test_a_day_with_7_trades_is_adverse` flips from strict xfail to PASS; add: exactly 6 = not adverse, 7 = `down 1`, 1 trade = no signal, unset band still pins to the floor (existing regression stays green). THE DEPLOY HAZARD you must design for, not around: `load_daymode_config` REFUSES a `SIGNAL_IDS` entry with no row in `"user".trader_settings` `daymode.stepdowns` (`daymode/config.py:248-257`) → if the code goes live without the row, `com.cobalt.daymode-propose` FAILS at 09:00. So you also produce the SETTINGS CHANGE as a reviewed file: read how `daymode.stepdowns` is loaded today (`src/cobalt/settings/` — `settings load --from <dir>` and its two YAMLs; quote the format from its tests), write the COMPLETE new input under gitignored `/Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918/` = production's current values + the ONE new step-down row (`trade_count_over_band: down 1`) — current values come from a read-only `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key LIKE 'daymode.%'"`; user data (L32): never `git add` it, never paste values into the report beyond the ONE new row; prove it on dev with `COBALT_ENV=dev uv run cobalt settings load … --dry-run` (shows exactly one added row, zero deletions, zero other changes); print `shasum -a 256` of each file. Report: the exact `--apply` command the evening hub will run, file paths, hashes, the dry-run diff, and the ORDER for the evening prompt (row loaded BEFORE 20:00 and before anything imports the new code at 09:00 — same shape as `98-evening.md` of 09-17 §3). `RESTARTS:` line derived. Commit `feat(daymode): trade_count_over_band step-down — down 1 above the band (ruled 2026-09-17 R13)`.

## 2. `ops/cto-desk.sh` — bring the desk back with one command (R11)
Bash, `set -euo pipefail`, no secrets, idempotent, `--dry-run` prints every action and runs none. Logic: (1) `claude agents --json` → a LIVE desk = a background row with cwd `/Users/cobalt/cobalt` whose name/remote-control is the desk's (match on the `--remote-control cto-desk` launch, record in the report exactly which JSON field you matched on, from a real sample of the output); (2) desk alive → ensure ONE herdr tab labelled "CTO" whose pane runs `claude attach <id>` (`herdr tab list`, `herdr pane list`; create the tab with `herdr tab create --workspace w2 --cwd /Users/cobalt/cobalt --label "CTO" --no-focus` and `herdr pane run <pane> "claude attach <id>"` only if missing or its viewer is dead), print `desk <id> alive — viewer ok/re-attached`; (3) no desk → `cd /Users/cobalt/cobalt`, run the launch line from `docs/40 - DevDocs/prompts/CTO-DESK-WAKEUP.md` line 1 (parse it from the file between the first `claude --bg` and the closing backtick — never a second copy of the line, L3), then (2). Never stops or kills anything. You run ONLY `ops/cto-desk.sh --dry-run` (a desk is alive while you work — the dry run must say so and propose no launch). README line in `ops/README.md`. A login LaunchAgent is NOT built (his later ruling). Commit `ops: cto-desk.sh — re-attach or relaunch the always-on desk`.

## 3. RESTARTS classifier: repo-meta files with no runtime reader (P2 ESC 14, carried three times)
`cobalt jobs restarts` classifies `.gitignore` (and `.claude/settings.json`, `.gitattributes`) as UNCLASSIFIED. Add the rule beside the 09-15 DOCS rule: repo-meta paths → `META → -` (no restart), explicit list, not a glob on dotfiles. Tests first (`.gitignore` alone → no restart, 0 UNCLASSIFIED; an unknown dotfile stays UNCLASSIFIED). Commit.

## 4. Radar real cadence — facts only, no change (feeds the 09-19 values session)
The desk saw resident cycles start ≈189 s apart on 09-17 07:50–08:00 with `radar.scan_interval: 100`. From `src/cobalt/radar/runner.py` + `poller.py` + `collector.py` (TokenBucket) and 30 minutes of `logs/radar.err` timestamps: is 100 s the sleep BETWEEN cycles or the period? what bounds a cycle's duration (bucket rate, per-request latency, member count)? measured requests/min vs `planned rpm 36.60`? One table, file:line for each claim, measured numbers with their n. State what cap/interval pairs fit under ceiling 45 and 50 by the code's own formula AND by the measured rate — as facts for his ruling, no recommendation on the trading side, nothing edited.

## 5. Close
Offline suite green (`uv run pytest -q tests/cobalt tests/taxonomy`), `.env` in → `COBALT_ENV=dev uv run pytest -q tests/cobalt/test_daymode.py` + `COBALT_ENV=dev uv run cobalt validate` → `.env` out (`ls -la .env` gone); `uv run cobalt jobs restarts main..HEAD` table verbatim; worktree clean. Last line: `OPS 0918 <tip> (RESTARTS <labels>) · over-band: READY + settings file sha256 <…> · cto-desk.sh: READY · meta rule: READY · cadence: facts in §4 · ESCALATE: <n>`.
