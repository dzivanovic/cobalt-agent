MODEL: Opus 5 (`claude-opus-5`) — the migration harness against production, even read-only, is the L29 floor · SEAT: proof hub `prod-proof-0918b`, launched by the CTO desk in the background ONLY AFTER Dejan's approval of the ONE new rule in this launch line: `Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only)` — an exact line, no wildcard. Every other rule is R1 list (1) of 2026-09-18 for this worktree. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/20-prod-proof-only-2.md' and follow it exactly." --model claude-opus-5 --remote-control prod-proof-0918b --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; the ONLY production command is the exact `--proof-only` line; NO other `db migrate` spelling exists in this launch line on purpose (no bare `migrate`, no `--rollback`); no code edit, no vault write, no push · METER: Anthropic small · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Production `--proof-only` — the first time the fixed migration harness meets production data. READ-ONLY: it runs the BEFORE probe over every table inside a `READ ONLY` transaction, prints rows + digest + seconds, applies nothing. Purpose: prove the 1 GB failure of 15:4x is gone on the real 8.4M-row `system.bars`, and MEASURE the probe's wall time so the next deploy's outage is budgeted from a fact. DO NOT STOP until the report ends `PROD PROOF DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** list (1) (this worktree's `.env` cp/rm/ls, git, reads), **R8** (second opinion first — done: §24 of that report, Grok AND Gemini `VERDICT: SAFE TO RUN --proof-only ON PRODUCTION`, review file `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-migrate-fix/REVIEW.md`), and row **R16** approving a SECOND run of **the exact production line above** (R12 approved the first) (absent, or naming a different line → `FAILED: authorization mismatch`). `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` proves the rows are committed. Laws: L29, L35, L41 interim (`.env` by name only, never printed, removed after — it is removed even if the run fails), L28 (nothing is written: no trace line is owed, say so), L55, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, remove the `.env`, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → this worktree's `docs/40 - DevDocs/reports/migrate-harness-fix-2026-09-18.md` (§0, BASELINE, the `--proof-only` section, ESCALATE 1) → `src/cobalt/db_migrations/cli.py` (`cmd_migrate`'s `if proof_only:` branch, `_connect`, `_probe`) → the review file above (Verdict table: F1 two-statement count/digest — expect a `rows` value that can be off by the rows inserted between the two statements on a live table; that is a known cosmetic, not a failure). Report (L48): NEW file `docs/40 - DevDocs/reports/prod-proof-only-2-2026-09-18.md` in this worktree — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Dev` → `## Production` → `## ESCALATE` → last line. Commit the report after each section.
UNATTENDED RULES: one bare command per Bash call, exactly the listed spelling, no `cd … &&`, no pipe, no redirect; outputs read from the tool result; files via Write. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove the `.env`, commit, stop; never another spelling.

## 0. PREFLIGHT (read-only)
THIS IS THE SECOND PRODUCTION PROOF (the first, 16:5x, passed on `511bff0`: `prod-proof-only-2026-09-18.md`). Since then review finding F1 was folded (`0e1f768`: the row count is taken inside the streamed fold — one statement, one pass) and the stack was re-landed on `sprint-2/stack`; this run points the FINAL code at production once, read-only, so that nothing about the harness is first seen inside the deploy's outage, and replaces the ≈94 s outage estimate with a measured number. `git status --porcelain` empty · `git rev-parse --abbrev-ref HEAD` = `sprint-2/stack` · `git rev-parse --short HEAD` = `20add4f` (report-only commits above stack tip `d72ece4`: `git log --oneline -4`) — anything else → `FAILED: unexpected tip — <sha>` · `git log --oneline --grep="review F1" -1` shows the F1 commit on this branch · `date` → NOT inside 20:25–21:15 ET (the archiver's bulk upsert into `bars`, then the replay) → inside it: `FAILED: window — wait for the desk` · `grep -n "proof_only\|READ ONLY\|read_only" src/cobalt/db_migrations/cli.py` → the flag and the read-only connection exist in THIS checkout.

## 1. Dev first — the flag does what it says in this checkout
`cp` the `.env` in · `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → table quoted: 23 tables, each with rows + digest + seconds, NO `-- applying` line, exit 0 (no comparison against an older pinned BASELINE — the append-only `cobalt_redactions` drifts by design; the gate run already compared BEFORE with AFTER inside one command). Anything else → `FAILED: dev proof-only — <what>` (remove the `.env` first). Record dev `bars` rows + seconds.

## 2. Production — ONE command, once
`date` · the exact allowlisted line `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` (run it in the FOREGROUND with the Bash call's `timeout` parameter set to 600000 — the default 120 000 ms is too close; expect ≈1 minute) · `date`. Quote the WHOLE output verbatim. PASS = exit 0, every table has rows + digest + seconds, `system.bars` ≈ 8.4M rows completes (no `ProgramLimitExceeded`), NO `-- applying` line, the run's own last line says nothing was applied. It is run ONCE: a failure is recorded verbatim and NOT retried (`FAILED: production proof-only — <error class>`; nothing can have been written — the transaction is READ ONLY at the server — say what the error implies for the deploy).
`rm` the `.env` · `ls -la` proves it gone (do this even on failure, BEFORE writing the FAILED line).

## 3. Close
In the report: per-table seconds for every table over 100,000 rows, the TOTAL, and the implied outage cost of a BEFORE + AFTER pair; whether `0006`/`0007`'s tables are absent (they must be — the proof table lists what exists); heartbeat-visible impact is the desk's to check, not yours. `git status --porcelain` empty. Last line:
`PROD PROOF DONE | <n> tables | system.bars <rows> rows in <s> s | total <s> s → BEFORE+AFTER ≈ <s> s of outage | nothing applied, nothing written | 0006/0007 absent: yes | ESCALATE: <n>`
Never merge, never touch `~/cobalt`'s tree, never push, never run any other production command.
