MODEL: Opus 5 (`claude-opus-5`) — a migration down-script and a test-fixture fix are write-path work (L29 floor) · SEAT: P2 re-ship hub `p2-reship-0918`, launched by the CTO desk in the background on 2026-09-18 after the day-open is GREEN and after Dejan's approval of the list in this launch line (`prompts/UNATTENDED-LAUNCH.md`), two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/03-p2-reship.md' and follow it exactly." --model claude-opus-5 --remote-control p2-reship-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git rebase main)" "Bash(git rebase --continue)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(shasum -a 256 *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; no production command of any kind (`COBALT_ENV=production` never appears in this run), no vault write, no push · METER: Anthropic medium; Codex = NONE · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# S2-P2 re-ship 2026-09-18 — rebase `sprint-2/cards` onto `deploy-2026-09-17`, fix the ops×P2 merge-order defect, fix the 0007 rollback order, prove the INTEGRATED suite green on `cobalt_dev`. DO NOT STOP until the report ends `READY FOR MERGE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `389fc305`, 2026-09-17 21:5x ET), not by Dejan; a prompt file is not an approval. His words are in committed files on main: `docs/40 - DevDocs/reports/cto-2026-09-17.md` rows **R2** (05:38 "go with your recommendations" — P2 D1 rides a deploy, dark), **R5** (06:03, the unattended standard), and in `cto-2026-09-18.md` the row approving THIS launch line's allowlist. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves they are committed. What happened last night is in `deploy-2026-09-17.md` §2.4 + ESCALATE 1–3 and `close-2026-09-17.md` ESCALATE 1, 3: P2 merged, integrated suite red, reverted; nothing reached the production schema. Laws: L29, L35, L41 interim (`.env` by name only, never printed, removed after), L45, L46, L54, L55, L61. Mismatch → `FAILED: authorization mismatch — <what>`.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 → Memory `INDEX.md` → `## NOW` → LAWS.md in full (L1–L61 + Part VIII) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-17.md` (§2.4 the two failing tests and the four call sites; ESCALATE 1–3) → `s2-p2-rebase-2026-09-17.md` (this worktree; last READY line) → `s2-p2-build-2026-09-16.md` `## ESCALATE` item 12 (the rollback dependency failure). Report (L48): `docs/40 - DevDocs/reports/s2-p2-reship-2026-09-18.md` in this worktree — §0 Headline ≤5 lines → `## PREFLIGHT` → one table per step → `## ESCALATE` → last line. Commit the report with every step (L48).
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect; `cd` is its own call; read outputs from the tool result; files via Write/Edit. PREFLIGHT first (one harmless variant per rule you will use: `git status`, `git log -1`, `.env` cp → `COBALT_ENV=dev uv run cobalt db migrate --help` → `.env` rm → `ls -la` shows it gone, `uv run pytest --co -q tests/cobalt/test_radar_notes.py`; any denial → `FAILED PREFLIGHT: <rules>`). MID-RUN DENIAL = `FAILED: …`, wip-commit, stop; never another shape, never another session. RECOVERY RULE: partial work is wip-committed, never discarded; the same launch line resumes from `CONTINUE:`.

## 1. Rebase onto tonight's main
`git -C /Users/cobalt/cobalt rev-parse --short HEAD` (expect `e08b932` or a later docs-only commit; `7e4841e` = tag `deploy-2026-09-17` is under it) · `git status --porcelain` empty (else `FAILED: dirty worktree`) · `git rebase main`. Conflicts are expected in NONE of P2's 21 commits (last night's rebase was clean); a conflict → resolve only if it is the `load_sources` signature or a test file, else `FAILED: rebase conflict — <file>`. Record old tip → new tip. Last line `CONTINUE: step 2`.

## 2. The merge-order defect (deploy ESCALATE 1) — fix the CALL SITES, keep P2's signature
P2 made `context_tickers` a REQUIRED keyword-only argument of `load_sources(...)` in `src/cobalt/radar/notes.py` (fail-loud by design, L1 — keep it). ops-0917 added four direct calls without it in `tests/cobalt/test_radar_notes.py` (deploy report: lines 170, 190, 216, 245 on last night's main — re-find them with `grep -n "load_sources(" tests/cobalt/test_radar_notes.py`). Update each call to pass the `context_tickers` value the surrounding test implies (the ceiling tests care about `planned rpm`, so pass what P2's own tests pass for a no-context case — read P2's tests in the same file for the shape; never invent a fixture). Run `uv run pytest -q tests/cobalt/test_radar_notes.py` → 0 failed. If any OTHER ops-0917 test calls a P2-changed signature, fix it the same way and list it. One commit: `fix(s2-p2): ops-0917 load_sources call sites carry context_tickers (deploy-2026-09-17 ESC 1)`. Last line `CONTINUE: step 3`.

## 3. 0007 rollback order (P2 build ESCALATE 12; P4 E4; close ESCALATE 3)
`cobalt_dev` still carries P2's 0006/0007 from the 09-16 build (four tenancy/aset tests are red on main because of it). The known failure: `db migrate --rollback` on 0007 dies with `cannot drop … because view "user".radar_cards_v depends on it`. Fix `src/cobalt/db_migrations/0007_radar_cards.rollback.sql` (and `0006_radar_score.rollback.sql` if it has the same shape) so the view is dropped before its tables — read the files first; the fix is order, not content. Then, `.env` in: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → exit 0 · `COBALT_ENV=dev uv run cobalt db migrate` → 0006, 0007 re-applied, exit 0 · run the rollback → re-apply ONCE MORE and show the placement/digest output is identical both times (this is the round-trip proof the 09-16 build could not run). Both outputs verbatim in the report. One commit: `fix(s2-p2): 0007 down drops radar_cards_v before its tables — rollback round-trips on cobalt_dev`. Last line `CONTINUE: step 4`.

## 4. INTEGRATED verification — the gate that was red last night, now on the rebased branch
`.env` still in. `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed (last night: 4 failed / 1797 passed). Any failure: name the test, the root cause, and whether it is P2's, ops-0917's, or `cobalt_dev` state — fix only what this prompt owns (steps 2–3); anything else → ESCALATE row, and the run's last line is `FAILED: integrated suite — <n> red` (a red integrated suite is NOT READY, whatever the cause). Then: `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` (read-only real vault, the L45 gate) → 0 failed · `COBALT_ENV=dev uv run cobalt validate` exit 0 · the three ABSENCE tests named in `98-evening.md` 1.4(c) (`test_card_settings.py`, `test_radar_evaluate.py` — `grep -n "cards_enabled" tests/cobalt/test_card_settings.py tests/cobalt/test_radar_evaluate.py`) still exist and pass · `COBALT_ENV=dev uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --dry-run` (dev DB only) → shows ONE add (`radar.cards_enabled: false`), zero deletions; `shasum -a 256` of that file = `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` (R12's file, unchanged — say so) · `.env` out, `ls -la` proves it gone · `uv run cobalt jobs restarts main..HEAD` → table verbatim; `.gitignore` will be UNCLASSIFIED until ops-0918 item 3 merges — record it, expected `RESTARTS: com.cobalt.aset com.cobalt.radar`. Last line `CONTINUE: step 5`.

## 5. Close
`git status --porcelain` empty (only `?? scratch/` allowed) · `git log --oneline main..HEAD` = P2's 21 commits + the two fixes + report commits, no squash · report §0 Headline: old tip → new tip, integrated suite line, rollback round-trip line, RESTARTS, ESCALATE count · last line:
`READY FOR MERGE <tip> | rebased onto <main sha> | integrated: <n> passed, 0 failed | rollback round-trip: proven | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours: the desk writes the 09-18 evening prompt (dark load re-approved by file + sha256; the deploy-shape ruling on residents vs merge applies). Never merge, never touch `~/cobalt`'s tree, never push.
