MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two reviewers, verify their findings; no write path · SEAT: review hub `review-0918d`, launched by the CTO desk in the background under Dejan's R9 of 2026-09-18 14:33 ET (`Bash(grok *)` + `Bash(agy *)`, standing for the weekend's review runs). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/21-review-delta.md' and follow it exactly." --model claude-sonnet-5 --remote-control review-0918d --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/review-delta)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; Grok = one headless review; Gemini (agy) = one headless review · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely). TIME MATTERS: the deploy this review gates must START by 19:00 ET — keep the packet small and do not wait on a house longer than 12 minutes (then record it `TIMEOUT` and close with the other).

# Second-house DELTA review (R8) — only what changed since today's three earlier reviews: (1) review finding F1 folded into the migration harness (`0e1f768`), (2) the second-attempt deploy prompt `19-deploy-stack-2.md` versus the reviewed `10-deploy-stack.md`. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R8** (14:29) and **R9** (14:33 "Approved" — `Bash(grok *)`, `Bash(agy *)`, standing for the weekend's review runs). `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` proves it. Mismatch → `FAILED: authorization mismatch — <what>`. Laws: L33, L35, L36, L37, L44, L47.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` — its §1 staging rules, §2 launch spellings, §3 verification rules and table shape BIND you here unchanged; `scratch/review-migrate-fix/REVIEW.md` in this worktree = the last run (both houses worked on a small packet). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-delta/REVIEW.md` (gitignored scratch; you commit nothing) — §0 Headline ≤5 lines → `## Packet` → `## Grok` → `## Gemini` → `## Verdict table` → last line.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no pipe, no redirect, no `cd`; long runs go `run_in_background`; files via Write. PREFLIGHT (METER, L47): `grok --version` · `agy --version` · `date`.

## 1. Stage the packet in `scratch/review-delta/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `f1.diff` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show 0e1f768` (if that hash is not found on this worktree's branches, find it: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards log --oneline --all --grep="review F1" -2` and use the one on `sprint-2/stack`) · (2) `cli.py` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show sprint-2/stack:src/cobalt/db_migrations/cli.py` · (3) `10-deploy-stack.md` and (4) `19-deploy-stack-2.md` from `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/` · (5) `incident.md` — from `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` `# THIRD RUN`: its §0 Headline, the step 3.5 rows, its §6 SAFE STATE section and ESCALATE 1–4, verbatim · (6) `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/reland-gate-2026-09-18.md` · (7) `QUESTIONS.md`, verbatim from the block below.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house. Earlier today you (or a peer) reviewed `10-deploy-stack.md`, an UNATTENDED deploy prompt for a live trading-support system; it then ran and failed at its step 3.5 on a defect in the migration tool, rolling back cleanly (`incident.md`). The tool was fixed and reviewed; one review finding, F1, was folded afterwards (`f1.diff`; `cli.py` is the module after it). `19-deploy-stack-2.md` is the SECOND-ATTEMPT prompt: a copy of the first with a new branch name (`sprint-2/stack`), new tag / rollback-directory names (`pre-stack-0918b`), a new session name, a rewritten step 3.5 (the command now runs ≈2–3 minutes; a tool-timeout instruction was added), and updated authorization rows. Read only the files in this folder. Give SEVERITY (BLOCKER = will fail mid-deploy or can leave production broken / MAJOR / MINOR), the STEP or FILE+LINE, the EVIDENCE, and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) `f1.diff`: is the row count now taken from the same single statement as the digest? any off-by-one between rows counted and rows folded (first row, batch boundaries, zero rows)? does anything still run a separate `count(*)` for the content proof? is the digest byte-identical to before F1 (separator placement unchanged)? (Q2) DIFF the two prompts yourself, line by line: every place `19` differs from `10` — is each rename applied CONSISTENTLY between the launch line's allowlist and the steps that use it (branch name in `merge --ff-only`, the worktree rebase lines, the `mkdir -p` rule versus the directory the steps write and the two `settings load --from …rollback` lines, the tag name)? One inconsistent spelling = a denied command with the residents down. (Q3) Anything that was true at the first attempt and is FALSE now: tags or directories that already exist from the failed run (`pre-stack-0918` exists; does `19` ever try to create it again? does any step assume a directory is absent?), the deploy report file already carrying three runs, a rollback directory built by the first attempt. (Q4) Step 3.5 as rewritten: is the tool-timeout instruction sufficient, and is the described behaviour on a client-side timeout right (transaction rolled back, nothing applied)? what if the timeout hits AFTER commit but before the client prints? (Q5) Anything else that changed for the worse. End with: `VERDICT: RUN AS IS / RUN AFTER FIXES <numbers> / DO NOT RUN`."

## 2. Launch both reviewers — `11-review-deploy-stack.md` §2 EXACTLY, with `scratch/review-delta/` in every path. ONE attempt per house; HARNESS / METER / TIMEOUT recorded verbatim, never looped.
## 3. Verify — `11-review-deploy-stack.md` §3 EXACTLY, against the REAL files (`sprint-2/stack` in `/Users/cobalt/cobalt-wt/s2-p2-cards`; the two prompts on main). One extra column per REAL finding: `blocks tonight's deploy? yes/no — why`. You fix NOTHING.
## 4. Close
Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · hub-verified REAL: <k> (blocks the deploy: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
