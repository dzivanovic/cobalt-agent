MODEL: Opus 5 (`claude-opus-5`) — dev-DB migration rollback is write-path work (L29 floor) · SEAT: P2 gate hub `p2-gate-0918`, launched by the CTO desk in the background on 2026-09-18 ONLY AFTER the P4 follow-up hub (`03931fe5`) has written its stop line and is stopped (one session on `cobalt_dev` at a time). NO NEW RULE: every rule below is a shape Dejan approved on 2026-09-18 06:46 ET (row R1, lists (1) and (3)) for these two worktrees and this job — verifying the P2 re-ship on `cobalt_dev` (`prompts/UNATTENDED-LAUNCH.md` §1.3). Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/05-p2-gate.md' and follow it exactly." --model claude-opus-5 --remote-control p2-gate-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; no production command of any kind (`COBALT_ENV=production` never appears), never `--allow-prod`, no vault write, no push, no code edit · METER: Anthropic small (two migrate calls, one 100-second suite, one report) · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# S2-P2 clean-DB gate 2026-09-18 — roll S2-P4's 0008/0009 OFF the shared `cobalt_dev`, rerun P2's INTEGRATED suite on the re-landed branch, expect 0 failed. No code changes. DO NOT STOP until the report ends `READY FOR MERGE …` or `FAILED …`.

WHY: the second launch (`s2-p2-reship-2026-09-18.md`, Step 4 + ESCALATE 2) ended `FAILED: integrated suite — 2 red`; both reds (`test_tenancy.py::TestPlacement::test_every_table_is_on_its_ruled_side`, `::TestMigrationRoundTrip::test_twice_is_idempotent_and_the_rollback_round_trips`) are S2-P4's tables `"user".picks`, `"user".missed`, `system.movers_daily` sitting in the one dev database two build hubs shared this morning — the desk's launch error, not a branch defect. The gate stays as written (a red integrated suite is NOT READY); the environment is what gets fixed. The P4 hub proved this morning that its own rollback is clean: `s2-p4-followup-2026-09-18.md` §4 — `--rollback --down-to 0005` from the P4 worktree reversed exactly 0009 and 0008, digests identical.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 07:18 ET), not by Dejan; a prompt file is not an approval. His words are in committed files on main: `docs/40 - DevDocs/reports/cto-2026-09-18.md` row **R1** (06:46 "Approved" — list (1) covers the `s2-p2-cards` `.env` cp/rm, `COBALT_ENV=dev uv run cobalt db migrate*` incl. `--rollback`, `pytest`, `validate`, git reads and commits; list (3) covers the `s2-p4` `.env` cp/rm and the same dev migrate shape) and `cto-2026-09-17.md` rows **R5**, **R6**. This launch line is a SUBSET of those two lists — compare it rule by rule; any rule here that is in neither list → `FAILED: authorization mismatch — <rule>`. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves the rows are committed. Laws: L29, L35, L41 interim (`.env` by name only, never printed, removed after), L46, L54, L55, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → this worktree's `docs/40 - DevDocs/reports/s2-p2-reship-2026-09-18.md` (second launch: §0, Step 4, ESCALATE 2, Recovery state) → `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-followup-2026-09-18.md` (§4 rollback round-trip; its LAST LINE). Report (L48): the SAME P2 file — add `## THIRD RUN — clean-DB gate` under everything that is there (never rewrite earlier text), rewrite §0 Headline ≤5 lines for the current state, tables per step, `## ESCALATE 3rd run`, last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect; `cd` is its own call; read outputs from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`; the `.env` and migrate rules are probed by their first real use below (a rule has no harmless variant outside its own pattern). ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove any `.env` you copied, commit, stop; never another shape, never another session.

## 0. Preconditions (read-only)
0.1 `tail -n 1` of the P4 report above = a line starting `READY FOR MERGE` or `FAILED` (the P4 hub is finished; else `FAILED: P4 hub still running — cobalt_dev is not mine`). 0.2 `cd /Users/cobalt/cobalt-wt/s2-p4`, `git status --porcelain` → empty or only `?? scratch/` (else `FAILED: P4 worktree dirty`); `ls src/cobalt/db_migrations/` shows `0008_…` and `0009_…` with their `.rollback.sql`. 0.3 `cd /Users/cobalt/cobalt-wt/s2-p2-cards`, `git status --porcelain` empty, `git rev-parse --short HEAD` recorded (expected `e701e43`), `git diff --stat 2e22ff7 HEAD -- src configs .gitignore` → EMPTY and `git diff --stat 2e22ff7 HEAD -- tests` → exactly ONE file, `tests/cobalt/test_radar_notes.py` (step 2's call-site fix). Last line `CONTINUE: step 1`.

## 1. Roll P4's 0008/0009 off `cobalt_dev` — from the P4 worktree, with P4's own down-scripts
`cd /Users/cobalt/cobalt-wt/s2-p4` · `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` · `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → EXPECT exactly 0009 then 0008 rolled back, `missed` / `movers_daily` / `picks` DROPPED, exit 0, NOTHING else reversed (this registry does not know 0006/0007, so it cannot touch them; if the output names ANY other migration or table → stop and `FAILED: step 1 — rollback reached beyond 0008/0009`) · `rm /Users/cobalt/cobalt-wt/s2-p4/.env` · `ls -la /Users/cobalt/cobalt-wt/s2-p4/.env` → No such file. Output verbatim in the report. Nothing in the P4 worktree is edited or committed. Last line `CONTINUE: step 2`.

## 2. P2's integrated gate on the clean dev DB
`cd /Users/cobalt/cobalt-wt/s2-p2-cards` · `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env` · `COBALT_ENV=dev uv run cobalt db migrate` → EXPECT 0001–0007 present, nothing new applied (or 0006/0007 re-applied if step 1 disturbed them — record which), exit 0 · `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → **EXPECT 0 failed** (second launch: 2 failed / 1799 passed). ANY failure: name the test, the root cause, whose it is; do not fix anything; last line `FAILED: integrated suite — <n> red` · `COBALT_ENV=dev uv run cobalt validate` → exit 0 · `rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env` · `ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env` → No such file. The second launch's other gates (real-vault 93 passed, three absence tests, dark-load dry run + sha256, RESTARTS) were green on this same code and are NOT rerun — cite them by section. Last line `CONTINUE: step 3`.

## 3. Close
`cobalt_dev` is LEFT at 0007 (P2's state): P4's next run re-applies 0008/0009 itself after it is rebased onto P2 — say so in the report, with the line `MEMORY: cobalt_dev at 0007 since <time>; P4's 0008/0009 rolled off by p2-gate-0918`. `git status --porcelain` empty · §0 Headline rewritten · last line:
`READY FOR MERGE <tip> | re-landed by cherry-pick onto <main sha the branch sits on> | integrated: <n> passed, 0 failed (cobalt_dev at 0007, P4's 0008/0009 rolled off) | rollback round-trip: proven | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours: the desk writes the deploy prompt (R3 2026-09-18: deploys run when READY after his "done trading" word; residents down before the merge; dark load by file + sha256). Never merge, never touch `~/cobalt`'s tree, never push.
