MODEL: Opus 5 (`claude-opus-5`) — a change to the migration harness + the dev-DB gate (L29 floor) · SEAT: build + gate hub `reland-gate-0918`, launched by the CTO desk in the background ONLY AFTER Dejan's approval of the ONE new rule in this launch line: `Bash(git switch -c sprint-2/stack main)` — an exact line. Every other rule was approved on 2026-09-18 for this worktree (R1 list (1), R2 `git cherry-pick *`, R7). Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/18-reland-gate.md' and follow it exactly." --model claude-opus-5 --remote-control reland-gate-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git switch -c sprint-2/stack main)" "Bash(git cherry-pick *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(shasum -a 256 *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command of any kind, never `--allow-prod`, no vault write, no push, no merge · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Re-land + gate — (A) fold the one REAL review finding into the migration harness, (B) re-land the whole stack on a NEW branch off main by cherry-pick, (C) run the full integrated gate on it. `cobalt_dev` is YOURS ALONE while you run. DO NOT STOP until the report ends `STACK READY …` or `FAILED …`.

STATE YOU INHERIT: branch `sprint-2/cards` in this worktree, tip `123f0b0` = the gated stack (`d672f2d..848681e`, 53 commits: P2 + ops-0918 + seam fixes) + the harness fix (`fd13970` red tests, `511bff0` fix) + report commits. The 53 were ff-merged to main at 15:42 and REVERTED at 15:44 (`deploy-2026-09-18.md` `# THIRD RUN`), so they are ANCESTORS of main with their content reverted: a rebase replays nothing; the ONLY re-land is a cherry-pick onto a branch that starts at main (`UNATTENDED-LAUNCH.md` §6 — third occurrence today). The harness fix was reviewed by Grok and Gemini (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-migrate-fix/REVIEW.md`: both `SAFE TO RUN`, one REAL finding F1) and proven read-only against production (`docs/40 - DevDocs/reports/prod-proof-only-2026-09-18.md`: `system.bars` 8,591,339 rows in 46.73 s, nothing written). `cobalt_dev` is at 0007 with the over-band row and production-mirroring settings.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** list (1), **R2** (`git cherry-pick *` in this worktree), **R7**, **R3** + **R5**, **R8**, and the row approving **`Bash(git switch -c sprint-2/stack main)`** (absent → `FAILED: authorization mismatch`). Compare this launch line rule by rule. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves the rows are committed. Laws: L1, L3, L29, L35, L41 interim, L32, L45, L46, L54, L55, L57, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, remove any `.env`, wip-commit, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → the review file above (Verdict table F1, F3a; the MINOR list of untested cases) → `docs/40 - DevDocs/reports/migrate-harness-fix-2026-09-18.md` and `prod-proof-only-2026-09-18.md` (its ESCALATE 1: the BASELINE gate must not include the append-only `cobalt_redactions`) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/09-stack-gate.md` (its step 2's gates BIND part C) → `src/cobalt/db_migrations/cli.py`. Report (L48): NEW file `docs/40 - DevDocs/reports/reland-gate-2026-09-18.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → tables per part → `## ESCALATE` → last line. Commit the report with every part (on whichever branch is checked out at that moment — say which).
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, NO pipe, no redirect; outputs read from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`, `git cherry-pick -h` (exit 129 = allowed), `uv run pytest --co -q tests/cobalt/test_migrate_proof.py`. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove any `.env`, wip-commit, stop.

## A. Fold review finding F1 — ONE statement, ONE pass (on `sprint-2/cards`, before the re-land)
`_probe` runs `SELECT count(*)` and then the streamed digest as two READ COMMITTED statements: on a table being written to, `rows` and `digest` can describe different snapshots, and `bars` is read twice. FIX: the row count is taken INSIDE the fold (count the rows the cursor yields), so one statement yields both and `bars` is read once. Tests FIRST (red before, green after, quoted): rows == the number of row texts folded; a table that grows between two probes yields a (rows, digest) pair that is self-consistent; no `SELECT count(*)` remains in the SQL `_probe` constructs for the content proof; the byte-compatibility test against the old SQL oracle still passes; the DevDoc loses its "exactly one row at a time" wording (a batch of `PROBE_BATCH_SIZE` rows is buffered — say that). Add the two cheap untested cases the review listed: `--proof-only --rollback` is a usage error; an exception inside the probe leaves the connection rolled back. `uv run pytest -q tests/cobalt/test_migrate_proof.py` → 0 failed. ONE commit: `fix(db-migrate): count rows inside the streamed fold — one statement, one pass (review F1)`. Record `<T_old>` = `git rev-parse --short HEAD` after this commit and the report commit. Last line `CONTINUE: part B`.

## B. Re-land on a new branch off main
B.1 `git status --porcelain` empty · `git log --oneline d672f2d..<T_old>` → record the count `<n>` (the 53 + everything above them) · `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = main's tip, recorded. B.2 `git switch -c sprint-2/stack main` (the exact allowlisted line; the branch must not exist yet — if git says it exists: `FAILED: branch exists`, do not improvise). B.3 `git cherry-pick d672f2d..<T_old>` — oldest first, original messages, no squash. The reverts on main restored each patch's exact pre-image, so NO conflict is expected; a conflict in a file under `docs/` that main's desk/deploy commits also touched (the reports) may be resolved by keeping BOTH texts, `git add`, `git cherry-pick --continue`; a conflict in ANY other path, or an empty-commit stop → `git cherry-pick --abort`, `FAILED: cherry-pick — <file or commit> — <git's message>`. B.4 PROOF, all three or `FAILED: re-land proof — <which>`: `git log --oneline main..HEAD` count = `<n>` · `git diff --stat <T_old> HEAD -- src tests configs ops .gitignore` prints NOTHING (byte-identical to what part A left) · `ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env` → No such file, then `uv run pytest -q tests/cobalt tests/taxonomy` (genuinely offline) → 0 failed. Last line `CONTINUE: part C`.

## C. The gate, with the database (on `sprint-2/stack`)
`cp` the `.env` in · `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quoted with its seconds; nothing applied · `COBALT_ENV=dev uv run cobalt db migrate` → 0001–0007 present, P4's tables ABSENT (present → `FAILED: cobalt_dev is not clean`), proof table: no `CHANGED` EXCEPT the append-only telemetry table `cobalt_redactions` if this very run redacted something between BEFORE and AFTER — name it if it happens; any other `CHANGED` → `FAILED` · `COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --dry-run` → the CLI's zero-change line (the dev apply was done at 14:5x; it is NOT in this launch line and is not run again) · `COBALT_ENV=dev uv run cobalt validate` → exit 0, `trade_count_over_band=down(1)` · **`COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed** (any failure: name it, whose it is, fix NOTHING, `FAILED: integrated suite — <n> red`) · `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m "" -p no:randomly` → 0 failed · `COBALT_ENV=dev uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --dry-run` → ONE add, zero deletions; `shasum -a 256` of that file = `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` · `shasum -a 256` of the two over-band files = the hashes in `cto-2026-09-18.md` R4 · `rm` the `.env`, `ls -la` proves it gone · `uv run cobalt jobs restarts main..HEAD` → table verbatim, expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED. `git status --porcelain` empty. Last line:
`STACK READY <tip> on sprint-2/stack | <n> commits re-landed by cherry-pick onto main <sha>, byte-identical to <T_old> | harness: streamed proof + --proof-only + F1 | offline: <a> passed, 0 failed | integrated with DB: <b> passed, 0 failed | real-vault: <c> passed | dark file sha256 945e42f8…ca7cd3ca unchanged | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours. Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production.
