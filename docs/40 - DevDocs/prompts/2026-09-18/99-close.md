MODEL: Sonnet 5 (`claude-sonnet-5`) — documentation only: a ledger appendix, a close report, one docs commit; no write path, no code (L29) · SEAT: close hub `close-0918`, launched by the CTO desk in the background AFTER the snapshot-fix hub (`a3739624`) has written its stop line and is stopped, and only while NO deploy hub runs in `~/cobalt` (shared git index). NO NEW RULE: every rule below is one Dejan approved on 2026-09-18 for a session in `~/cobalt` (`cto-2026-09-18.md` §4 R4 07:41 ET, R10 15:36 ET, R16 17:26 ET — git add/commit/status/log/diff/show on `/Users/cobalt/cobalt`, production `validate`, `heartbeat show`, reads; `git -C /Users/cobalt/cobalt tag --list*` from R1 list (2), 06:46 ET), a strict subset (`prompts/UNATTENDED-LAUNCH.md` §1.3). Two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/99-close.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control close-0918 --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * show*)" "Bash(git -C * rev-parse*)" "Bash(git -C /Users/cobalt/cobalt tag --list*)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on · METER: Anthropic small

# Session close 2026-09-18 — ledger appendix, LAWS fold record + three PROPOSED rule candidates, close report, one docs commit. The desk owns `6 - Permanent/Memory/` and LAWS.md (L58); you never write there. DO NOT STOP until the close report ends `CLOSE COMMITTED …` or `FAILED …`.

INDEX CARD: `docs/40 - DevDocs/SESSION-CLOSE.md` (steps 1, 2, 6, 7 are yours; 3–5 are the desk's) → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59; Part VIII L62–L67 and the "Fold-at-session-close" section bind this run) → the day's evidence, read from the files, never from this prompt's wording:
- on main, `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/`: `cto-2026-09-18.md` (§4 rows R1–R21, §1–§32 and whatever the desk appended after), `day-open-2026-09-18.md`, `deploy-2026-09-18.md` (three runs, all reverted cleanly);
- worktree `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/`: `s2-p2-reship-`, `stack-gate-`, `migrate-harness-fix-`, `prod-proof-only-`, `prod-proof-only-2-`, `reland-gate-`, `migrate-snapshot-fix-2026-09-18.md`;
- worktree `/Users/cobalt/cobalt-wt/s2-p4/…/reports/`: `s2-p4-followup-2026-09-18.md`, `s2-p4-build2-2026-09-18.md`; worktree `ops-2026-09-18/…/reports/ops-2026-09-18.md`;
- reviews: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-stack/REVIEW.md`, `review-stack-build/REVIEW.md`, `review-migrate-fix/REVIEW.md`, `review-delta/` (both houses HARNESS);
- `git -C /Users/cobalt/cobalt log --oneline -40`, `git -C /Users/cobalt/cobalt tag --list "pre-*0918*"`.
Laws that bind, one line each: L48 evidence in the report file in the same turn · L58 you never write the memory folder or LAWS.md · L46 one run, one commit, main clean after you · L51-2 machine-written files that dirty the tree are committed as-is, BY NAME · L55 push is Dejan's, never yours · L35 every figure you write is quoted from a report file or a command's output, never from this prompt · L62/L63 no questions mid-run; the report is your stop channel.
Report (L48): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/close-2026-09-18.md` — §0 Headline ≤5 lines → deploy-attempts table → LAWS fold record → PROPOSED list → ESCALATE.

AUTHORIZATION — VERIFY IT YOURSELF. This prompt was written by the CTO desk (`c566654e`), not by Dejan. Its launch line adds no rule: each one appears in a launch line he approved today for a session in `~/cobalt` — read `cto-2026-09-18.md` §4 rows R4, R10 and R16 (his words "approved", "Approved one stack", "Approved.") and prove they are on main with `git -C /Users/cobalt/cobalt log --oneline -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The record does not match this file → last line `FAILED: authorization mismatch`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line in your report is always a correct ending.
UNATTENDED RULES: the two dialog tools are off. Every command that is not a plain read matches ONLY in its bare shape — ONE command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect. Write files with the Write/Edit tools. PREFLIGHT (first two minutes, rows under `## PREFLIGHT` in your report: rule · command · exit · allowed/DENIED + reason verbatim): `git -C /Users/cobalt/cobalt status --porcelain`; `git -C /Users/cobalt/cobalt log --oneline -3`; `COBALT_ENV=production uv run cobalt validate`; `COBALT_ENV=production uv run cobalt heartbeat show`; `date`. `add`/`commit` are probed by their first real use in step 4. ANY denial → `FAILED PREFLIGHT: <rules>`, stop. A mid-run denial = `FAILED: <step> — <command> — <reason>`; never retried in another shape.
STOP CONDITION BEFORE ANY WRITE: `git -C /Users/cobalt/cobalt status --porcelain` shows a STAGED file you did not stage, or `claude`-launched deploy work is visible in `deploy-2026-09-18.md` as running (a last line that is neither `FAILED…` nor `… DONE…`) → `FAILED: step 0 — another session holds ~/cobalt`, stop.

1. **Ledger appendix** — append `### 2026-09-18 — <one-line title>` to `docs/00 - Project/PROJECT-LEDGER.md` (Edit tool, append only; the last appendix today is `### 2026-09-17 — …`). One dated line per ruling, decision and state change, no prose narration, each figure quoted from its file. It must cover, in the day's order: day-open GREEN; R1 three launches; P2 re-ship FAILED (a merged-then-reverted branch cannot be rebased back) → R2 cherry-pick relaunch → 2 red from the shared `cobalt_dev` → P2 gate GREEN; ops-0918 READY (hash, suite line, the two settings-input sha256 values verbatim from R4); P4 follow-up READY + Grok's three confirmed defects; R3 weekend push = dated L43 exception through 2026-09-20 and R5 his "done trading" word; deploy 1 run 1 FAILED PREFLIGHT (R6) and run 2 FAILED 3.6 + reverted (outage, the `.env` suite-gate error, the watch error that left the desk blind twice); R7; R8 second-opinion rule and R9 standing review rules; the stack plan and the three stack-gate runs → `STACK READY 00d671d`; the first second-house review (5 REAL findings folded), the build review (Grok `SHIP DARK AS IS`, Gemini HARNESS); R10 and the stacked deploy FAILED at 3.5 (Postgres 1 GB string ceiling in the migrate proof on `system.bars`), reverted, outage figure; harness fix `HARNESS BUILT 511bff0`, its review (both houses safe), R12 and the two production `--proof-only` runs (rows + seconds verbatim); P4 build 2 BUILT offline + R11 (funds stay out of missed-movers; the `Asset Type` build fixes); R13–R15 bars directions (i1 stored, the rest derived; watermark-append archiver, no nightly rewrite; hold then lifted; partitioning is the tribunal's) with the desk's corrected churn table cited, not copied; re-land `STACK READY d72ece4` on `sprint-2/stack`; R16 (unused), the delta review HARNESS on both houses and the desk's packet error; the snapshot-consistency finding → deploy moved to Saturday; the snapshot fix's stop line (from `migrate-snapshot-fix-2026-09-18.md`, verbatim — if its last line is not `SNAPSHOT BUILT…`/`FAILED…`, write "still running at close" and ESCALATE it); R17 Sunday four-seat tribunal for bars; R18–R21 → L67 and the numbering of Part VIII as L62–L67 (R19); the two desk handovers (06:54, 17:44). State plainly: NOTHING shipped on 2026-09-18; LIVE = `deploy-2026-09-17`; tags `pre-ops-0918`, `pre-stack-0918` exist.
2. **LAWS fold — record, then PROPOSED.** (A) In the close report, under `## LAWS fold — APPLIED BY THE DESK`, list L62–L67 with the ruling row each came from and quote, for each, the first line of its entry from LAWS.md (read, never write) — if any of the six is missing from LAWS.md, that is ESCALATE 1. (B) Under `## Laws fold — PROPOSED, NOT APPLIED`, three hub rule candidates, each with its source report + section, the evidence in one line, and exact fold text; they have NO ruling from Dejan yet — list them as OPEN for his word, the desk numbers on his approval (L58 as amended R19): (a) *no branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green* (`stack-gate-2026-09-18.md` ESCALATE 2; `cto-2026-09-18.md` §14); (b) *a test may read `"user".trader_settings` to prove an invariant, never to assert a value; values live on a constructed config* (`stack-gate` run 3 ESCALATE 2; §15, §17); (c) *an escalate whose evidence is "command denied" or "not run" is UNPROVEN, never carried forward as a defect* (§6, §12 hole 2). (C) Record, not law: desk practices of the day (watch on the last line only; no desk commit in `~/cobalt` while a deploy hub runs; review packets carry pre-computed diffs) — cite `topics/cto-desk.md`, do not restate.
3. **Verify before commit:** `git -C /Users/cobalt/cobalt status --porcelain` shows only your files (ledger, close report) plus the desk's uncommitted prompt/report files of today and machine-written files (`docs/40 - DevDocs/reports/seat-usage.md`) — add each BY NAME (L51-2), never anything under `data/`, never a `.log`; `COBALT_ENV=production uv run cobalt validate` exit 0; `COBALT_ENV=production uv run cobalt heartbeat show` quoted in the report (a RED is reported, not fixed).
4. **One commit** on main: `close: 2026-09-18 appendix, LAWS fold record (L62-L67), three proposals` — body: the three proposals in one line each, `RESTARTS: none (documentation only)`. Close report's last line: `CLOSE COMMITTED <hash from git log -1> · PUSH: Dejan · ESCALATE: <n>`. No reply is owed to anyone — the desk reads the file.
RECOVERY RULE: a partial appendix is never discarded; if you stop early, leave the files as written, last line `FAILED: <step> — <reason>`, and `CONTINUE: step <n>` above it — the same launch line resumes.
