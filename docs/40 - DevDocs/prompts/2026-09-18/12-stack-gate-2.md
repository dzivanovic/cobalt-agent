MODEL: Opus 5 (`claude-opus-5`) — a fixture fix on a build branch, dev-DB migration + a dev settings write (L29 floor) · SEAT: stack gate hub, second run `stack-gate-0918b`, launched by the CTO desk in the background. NO NEW RULE: the launch line is `09-stack-gate.md`'s, unchanged — every rule approved by Dejan on 2026-09-18 for this worktree (R1 list (1), R2, R7); the ONE file edit this run makes is inside its own worktree with the Edit tool and needs no rule. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/12-stack-gate-2.md' and follow it exactly." --model claude-opus-5 --remote-control stack-gate-0918b --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git rebase main)" "Bash(git rebase --abort)" "Bash(git cherry-pick *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(shasum -a 256 *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command of any kind, never `--allow-prod`, no vault write, no push · METER: Anthropic small–medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Stack gate, second run — fix the ONE seam defect the first run found, then finish the gate (`09-stack-gate.md` steps 1.4 → 3, unchanged). DO NOT STOP until the report ends `STACK READY …` or `FAILED …`.

STATE YOU INHERIT (first run `693c6fd1`, `docs/40 - DevDocs/reports/stack-gate-2026-09-18.md` in this worktree — read it first, all of it): the stacked branch IS assembled and clean (P2's 35 commits + ops-0918's 8 on main; all three step-1.3 proofs PASS; desk re-verified 14:42 ET that ops-0918's code is byte-identical to `856176e`). It stopped at 1.4: offline suite `43 failed, 1455 passed, 5 errors`, ONE root cause — ops-0918 `fe195ab` made a seventh step-down signal `trade_count_over_band` mandatory (`src/cobalt/daymode/config.py` completeness check), and P2's fixture `tests/fixtures/radar/card-settings.real-shape.json` carries six (`grep -n '"signal"'` → lines 47–76). Red in neither branch alone: the second cross-branch seam defect in two days, and the first one caught BEFORE a merge. Step 2 never ran: no `.env`, `cobalt_dev` untouched (at 0007, P4's 0008/0009 absent).

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 14:4x ET), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** list (1), **R2**, **R7**, **R3** + **R5**, **R8**; the value you write into the fixture is HIS ruling, `cto-2026-09-17.md` **R13** (07:37 ET "ruling A." — over the trade-count band = adverse, `down 1`). Compare this launch line rule by rule with those rows; a rule in none of them → `FAILED: authorization mismatch — <rule>`. Laws: L29, L35, L41 interim, L32, L45 (the fixture mirrors the REAL shape of `"user".trader_settings` — after this deploy production carries exactly this row), L46, L54, L55, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → the first run's report (above) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/09-stack-gate.md` (its steps 1.4, 2 and 3 BIND you verbatim — expectations, gates, stop line) → `/Users/cobalt/cobalt-wt/ops-2026-09-18/docs/40 - DevDocs/reports/ops-2026-09-18.md` §1.1–§1.2 (the new row, in full, as the ops hub shipped it). Report (L48): the SAME file — add `# SECOND RUN` under everything there (never rewrite earlier text), §0 Headline ≤5 lines for the current state, tables per step, `## ESCALATE 2nd run`, last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, NO pipe (the first run self-reported one — do not repeat it), no redirect; `cd` is its own call; outputs read from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`, `uv run pytest --co -q tests/cobalt/test_radar_keys.py`. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove any `.env` you copied, commit, stop.

## F. The fixture — the ONLY code change this run may make
F.1 `git status --porcelain` empty; `git rev-parse --short HEAD` recorded. Read `tests/fixtures/radar/card-settings.real-shape.json` (the `daymode.stepdowns` list, lines ≈45–80), `tests/fixtures/radar/_cut_p2_fixtures.py` (how the fixture was cut — do NOT run it), and `tests/cobalt/test_daymode.py` ≈ line 83 (how ops-0918 spells the new row in its own test table). F.2 With the Edit tool add ONE element to the fixture's `daymode.stepdowns` list, after the last existing row, in the fixture's own key order and style: `signal: trade_count_over_band`, `effect: down`, `rungs: 1`, and the `because` text exactly as the ops branch spells it (ops report §1.2 / `test_daymode.py`). Nothing else in the file changes — `git diff --stat` = 1 file, insertions only. If the fixture carries its own hash/digest or a row count asserted anywhere (`grep -rn "card-settings.real-shape" tests`), update ONLY what that one added row forces, and list each such line. Any OTHER file that needs a change to get green → STOP: `FAILED: fixture fix — a second file is involved — <path> — <why>` (the desk decides; you do not widen the fix). F.3 `uv run pytest -q tests/cobalt tests/taxonomy` (this worktree has no `.env` — `ls -la …/.env` → No such file, first — so this is genuinely offline) → EXPECT 0 failed, 0 errors. Still red → name what remains, `FAILED: offline suite — <n> red after the fixture fix`. F.4 ONE commit: `fix(s2-p2): card-settings fixture carries the trade_count_over_band step-down (ops-0918 seam, ruled 2026-09-17 R13)`. Last line `CONTINUE: step 2`.

## 2 → 3. `09-stack-gate.md` step 2 (dev DB: migrate → over-band dry run (3 changed keys, KEYS only) → hashes → the DEV `--apply` → `validate` → **full integrated suite with the DB, EXPECT 0 failed** → real-vault subset → dark card dry run + sha256 → `.env` out → RESTARTS) and step 3 (close, MEMORY line), EXACTLY as written there, with one addition to the stop line — the fixture commit:
`STACK READY <tip> | P2 (<n> commits) + ops-0918 (8) + fixture fix <hash> on main <sha> | offline: <a> passed, 0 failed | integrated with DB: <b> passed, 0 failed | real-vault: <c> passed | dark file sha256 945e42f8…ca7cd3ca unchanged | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours. Never merge, never touch `~/cobalt`'s tree, never push.
