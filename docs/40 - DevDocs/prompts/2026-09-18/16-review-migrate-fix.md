MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two reviewers, verify their findings; no write path · SEAT: review hub `review-0918c`, launched by the CTO desk in the background under Dejan's R9 of 2026-09-18 14:33 ET (`Bash(grok *)` + `Bash(agy *)`, standing for the weekend's review runs). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/16-review-migrate-fix.md' and follow it exactly." --model claude-sonnet-5 --remote-control review-0918c --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/review-migrate-fix)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; Grok = one headless review; Gemini (agy) = one headless review · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Second-house review of the MIGRATION HARNESS FIX (R8) — before this code is pointed at production for the first time. Commits `fd13970` (red tests) + `511bff0` (fix) on `sprint-2/cards`: the content proof of `cobalt db migrate` is now STREAMED client-side, and a read-only `--proof-only` mode exists. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R8** (14:29) and **R9** (14:33 "Approved" — `Bash(grok *)`, `Bash(agy *)`, standing for the weekend's review runs). `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` proves it. Mismatch → `FAILED: authorization mismatch — <what>`. Laws: L33, L35, L36, L37, L44, L47.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, stop.

INDEX CARD (you; the reviewers get the packet only): `prompts/UNATTENDED-LAUNCH.md` §2 → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` — today's first review run: its §1 staging rules, §2 launch spellings, §3 verification rules and table shape BIND you here unchanged → this worktree's `scratch/review-deploy-stack/REVIEW.md` and `scratch/review-stack-build/REVIEW.md` (what the two earlier runs looked like; note that Gemini worked on the SMALL packet at 14:5x and went HARNESS on the large one at 15:1x — this packet is deliberately small). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-migrate-fix/REVIEW.md` (gitignored scratch; you commit nothing) — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Verdict table` → last line.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no pipe, no redirect, no `cd`; long runs go `run_in_background`; outputs read from the tool result; files via Write. PREFLIGHT (METER, L47): `grok --version` · `agy --version` · `date`. A usage-limit or login message → that house = `METER`, continue with the other; both unavailable → `FAILED: no second house reachable`.

## 1. Stage the packet in `scratch/review-migrate-fix/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `diff.txt` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff 848681e 511bff0 -- src tests` · (2) `cli.py` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show 511bff0:src/cobalt/db_migrations/cli.py` (the whole module after the fix) · (3) `cli_before.py` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show 848681e:src/cobalt/db_migrations/cli.py` (before) · (4) `__init__.py` = `… show 511bff0:src/cobalt/db_migrations/__init__.py` · (5) `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/migrate-harness-fix-2026-09-18.md` (the build hub's report: BASELINE, red-before/green-after, timings, ESCALATE) · (6) `incident.md` — YOU write it: from `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` `# THIRD RUN`, step 3.5's rows and ESCALATE 1–2, verbatim · (7) `QUESTIONS.md`, verbatim from the block below.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house. `cli.py` is the database-migration harness of a LIVE trading-support system (Postgres). Before and after it applies migrations — all inside ONE transaction — it proves that no existing table's content changed, by a per-table row count and content digest. Today that proof killed a production deploy: it built one `md5(string_agg(row_json, '|' ORDER BY pk))` string per table, and `system.bars` (8.4 million rows) passed Postgres's 1 GB string limit (`incident.md`). `diff.txt` is the fix: the digest is now folded client-side from a server-side cursor, and a new `--proof-only` flag runs the BEFORE probe alone, read-only. The NEXT thing that happens to this code is a `--proof-only` run against the production database, from an unmerged branch, with the live services running. Read only the files in this folder. For every finding give SEVERITY (BLOCKER = can write to / lock / corrupt production, or gives a WRONG proof verdict in either direction / MAJOR = likely failure or a guarantee weaker than the module's docstring promises / MINOR), FILE + LINE, and a concrete FAILING SCENARIO. Do not pad; say 'none found' rather than inventing. Check specifically: (Q1) BYTE-COMPATIBILITY: does the streamed fold produce exactly the bytes `string_agg((row_json)::text, '|' ORDER BY <order>)` would have — separator placement (between rows only), zero rows vs `coalesce(…, '')`, a NULL row text, encoding of non-ASCII (server encoding vs client decoding and re-encoding — can a round trip change bytes?), and above all ORDER: is the cursor's `ORDER BY` the identical expression, and is it a TOTAL order (ties between rows with equal sort keys would make the digest nondeterministic — was that already true before, and is it worse now)? (Q2) `--proof-only` can NEVER write: trace every statement it can execute; is the transaction really read-only at the SERVER (not just by convention), what happens to it on exit paths (exception, Ctrl-C, flag combinations such as `--proof-only --rollback`, `--proof-only` without `--allow-prod` on production), and can any code path reach the apply loop with the flag set? (Q3) The server-side cursor inside the MIGRATION transaction (the non-proof-only path): named cursors and DDL in the same transaction — can an open cursor on a table block or be invalidated by an `ALTER`/`CREATE` on that table later in the same transaction? are cursors closed before `-- applying` starts? cursor name collisions across tables? (Q4) PRODUCTION IMPACT of the long read: which lock does the probe take on each table, can it block the live writers (a radar resident inserting, an archiver bulk upsert) or be blocked by them, how long does it hold a snapshot open, memory on the client (batch size × row width), and what a statement/lock timeout does to the verdict. (Q5) The proof's VERDICT logic: can a table that legitimately changes during a deploy (a heartbeat or job row written by a scheduled one-shot while the residents are down) now read `CHANGED` and fail a good migration, or is that handled (excluded columns / tables)? did the fix change that behaviour? (Q6) The TESTS in `diff.txt`: does the byte-compatibility test really compare against the OLD SQL expression on real tables, or against a re-implementation of the new fold (a test that cannot fail)? is the 'cursor is server-side and batched' assertion meaningful? what is NOT tested that should be? (Q7) Anything else that would make you refuse to point this at production. End with one line: `VERDICT: SAFE TO RUN --proof-only ON PRODUCTION / SAFE AFTER FIXES <finding numbers> / DO NOT RUN`."

## 2. Launch both reviewers — `11-review-deploy-stack.md` §2 EXACTLY, with `scratch/review-migrate-fix/` in place of `scratch/review-deploy-stack/` in every path and `grok-findings.md` / `gemini-findings.md` in the new folder. ONE attempt per house; HARNESS / METER recorded verbatim, never looped. Do not pre-read the diff for defects yourself.

## 3. Verify — `11-review-deploy-stack.md` §3 EXACTLY, against the REAL files in `/Users/cobalt/cobalt-wt/s2-p2-cards` at `511bff0`. For each REAL finding add one column: `blocks the production --proof-only run? yes/no — why` and one: `blocks a production MIGRATE? yes/no — why`. You fix NOTHING.

## 4. Close
Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER> · hub-verified REAL: <k> (blocks proof-only: <p>, blocks migrate: <g>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
