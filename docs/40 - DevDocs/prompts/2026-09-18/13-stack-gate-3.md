MODEL: Opus 5 (`claude-opus-5`) — test-support fixes on a build branch + the dev-DB gate (L29 floor) · SEAT: stack gate hub, third run `stack-gate-0918c`, launched by the CTO desk in the background. NO NEW RULE: the launch line is `09-stack-gate.md`'s, unchanged — every rule approved by Dejan on 2026-09-18 for this worktree (R1 list (1), R2, R7); file edits are inside this worktree with the Edit tool. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/13-stack-gate-3.md' and follow it exactly." --model claude-opus-5 --remote-control stack-gate-0918c --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git rebase main)" "Bash(git rebase --abort)" "Bash(git cherry-pick *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(shasum -a 256 *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command of any kind, never `--allow-prod`, no vault write, no push · METER: Anthropic small–medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Stack gate, third run — two TEST-SIDE fixes the second run's DB gate exposed, then the full integrated gate again. No file under `src/`, `configs/` or `ops/` changes in this run. DO NOT STOP until the report ends `STACK READY …` or `FAILED …`.

STATE YOU INHERIT (`docs/40 - DevDocs/reports/stack-gate-2026-09-18.md` in this worktree — read BOTH runs, the second one's Group A / Group B tables and ESCALATE 1–3 in full): the stack is assembled (P2 + ops-0918's 8 + fixture fix `f907f1d`), offline suite GREEN `1535 passed, 0 failed`; `cobalt_dev` is at `0007`, carries the over-band row and now MIRRORS production's settings (the DEV apply of step 2.5 is done — do NOT repeat it; its dry run must now print the CLI's zero-change line). With the database the suite is `8 failed, 1805 passed`:
- **Group A — 5 × `tests/cobalt/test_trader_settings.py`**: the revision-3 proof seeds `"user".trader_settings` from YAML frozen in git history (six step-downs); ops-0918's completeness check refuses it. No database state can fix it. This was ops-0918's own defect, invisible to its hub because its DB run was already red on the missing dev row.
- **Group B — 3 tests** (`test_aset_config.py::TestSheetModesConfig::test_committed_config_is_valid` `:175`, `::test_is_enabled_reflects_committed_config` `:199`, `test_daymode.py::TestShippedConfig::test_the_real_config_loads_and_reads_as_ruled` `:128`) read the LIVE `trader_settings` rows and assert the trader's grade ladder as `{A, B}` / `["A", "B"]` / "C is not enabled".

DESK RULINGS (architect; you build to them, you do not re-open them):
- **Group B is NOT a question for Dejan — he already ruled it.** The record: `areas/cobalt-sprints.md` line "C-size RULED (source: 09-14 weekly review …): grade C re-enabled; … applied by him 07:55 (`aset.enabled_grades`) and 07:59 (`daymode.reduced_enabled_grades` …)" and `/Users/cobalt/cobalt/docs/00 - Project/PROJECT-LEDGER.md` ≈ line 1393; production's two rows carry `source: yaml:/Users/cobalt/tmp/aset-c`, `updated_at` 2026-09-14 11:55 / 11:59 UTC (desk read-only query, 14:5x ET). Production is RIGHT; the three tests are STALE. And they are wrong in KIND: a test that reads the live settings table and pins the trader's current choice turns red every time he changes his own settings (L32: his values are user data, not system behaviour). FIX: each of the three asserts the INVARIANT it was protecting, against whatever ladder the rows hold — enabled grades are a non-empty subset of the declared `Grade` members, in ladder order; the reduced ladder is a subset of the account ladder (that is the S1-P2 re-ruling's actual content: "A+ is out because the ACCOUNT ladder does not enable it" — keep that sentence, as the invariant `reduced ⊆ account`); `is_enabled(g)` is true exactly for members of `enabled_grades`. The VALUE-pinning behaviour tests move onto a CONSTRUCTED config (the file already builds `COMPLETE_SHEET_MODES` with `enabled_grades: [A, B]` for that purpose — a grade outside a constructed ladder is still proven refused). Each rewritten test's comment cites the 09-14 C-size ruling and says why no live value is pinned. No test is deleted; no assertion about refusal behaviour is weakened.
- **Group A**: shape (a) of the second run's ESCALATE 1 — the `seeded` fixture appends a ruled row for every `SIGNAL_IDS` entry the frozen git-history text does not name, exactly the pattern ops-0918 used for its own note fixture in `382c862` (read that commit: `git show 382c862`). The frozen revision-3 text itself is never edited (it is history); the appended rows use `effect: none` unless the signal is `trade_count_over_band`, which takes R13's `down` / `1` — say so in the fixture's comment. The five tests then pass unchanged.
- ANY file under `src/`, `configs/` or `ops/` that would have to change to get green → STOP: `FAILED: test-side fix is not enough — <path> — <why>`. The desk decides; you do not widen the fix.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 15:0x ET), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** list (1), **R2**, **R7**, **R3** + **R5**, **R8**; `cto-2026-09-17.md` **R13**; the 09-14 C-size ruling as cited above (verify both citations yourself before you touch Group B — a citation that does not say what this prompt says → `FAILED: authorization mismatch — C-size record`). Compare this launch line rule by rule with those rows. Laws: L29, L32, L35, L41 interim, L45, L46, L54, L55, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → this worktree's gate report (both runs) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/09-stack-gate.md` (its steps 2 and 3 BIND you: expectations, gates, stop line). Report (L48): the SAME file — add `# THIRD RUN` under everything there (never rewrite earlier text), §0 Headline ≤5 lines, tables per step, `## ESCALATE 3rd run`, last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, NO pipe, no redirect; `cd` is its own call; outputs read from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`, `uv run pytest --co -q tests/cobalt/test_trader_settings.py`. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove any `.env` you copied, commit, stop.

## T. The two test-side fixes (tests FIRST is the wrong shape here — the tests ARE the change; the evidence is red-before / green-after per group)
T.1 `git status --porcelain` empty; tip recorded. `cp` the `.env` in. RED BEFORE: `COBALT_ENV=dev uv run pytest -q tests/cobalt/test_trader_settings.py tests/cobalt/test_aset_config.py tests/cobalt/test_daymode.py -p no:randomly` → EXPECT exactly the 8 named failures (another set → record it and continue only if it is a subset of the 8 plus nothing new; anything new → `FAILED: the red set moved`). T.2 Group A per the ruling → rerun `tests/cobalt/test_trader_settings.py` with the DB → 0 failed → commit `test(settings): the revision-3 seed appends a ruled row for every SIGNAL_IDS entry its frozen text predates (ops-0918 seam)`. T.3 Group B per the ruling → rerun the two files with the DB → 0 failed; then prove the rewritten tests do not depend on the ladder: they must also pass OFFLINE-equivalent logic — state in the report, per test, which invariant it now asserts and that no literal grade list from live rows remains (`grep -n '"A", "B"\|Grade.A, Grade.B' ` on the three test bodies → only constructed-config uses) → commit `test(aset,daymode): live-settings tests assert invariants, not the trader's current ladder (C-size ruled 2026-09-14)`. T.4 `git diff --stat <tip at T.1> HEAD` → ONLY files under `tests/`. `rm` the `.env`, `ls -la` proves it gone. `uv run pytest -q tests/cobalt tests/taxonomy` (offline) → 0 failed. Last line `CONTINUE: step 2`.

## 2 → 3. `09-stack-gate.md` step 2 and step 3 EXACTLY as written there, with two stated differences: the over-band dev dry run now EXPECTS the zero-change line (`no differences — the database already holds these settings.`) and the `--apply` is NOT run again; the stop line names the fix commits:
`STACK READY <tip> | P2 + ops-0918 (8) + seam fixes f907f1d <A-hash> <B-hash> on main <sha> | offline: <a> passed, 0 failed | integrated with DB: <b> passed, 0 failed | real-vault: <c> passed | dark file sha256 945e42f8…ca7cd3ca unchanged | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours. Never merge, never touch `~/cobalt`'s tree, never push.
