MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two reviewers, verify their findings; no write path · SEAT: review hub `review-0918a`, launched by the CTO desk in the background under Dejan's R9 of 2026-09-18 14:33 ET (`Bash(grok *)` + `Bash(agy *)`, standing for the weekend's review runs). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md' and follow it exactly." --model claude-sonnet-5 --remote-control review-0918a --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/review-deploy-stack)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write of any kind, no vault write · METER: Anthropic small; Grok = one headless review; Gemini (agy) = one headless review · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Second-house review of the STACKED DEPLOY prompt (R8: no deploy without a second opinion). Two independent readers — Grok and Gemini — each find what is wrong with `10-deploy-stack.md` BEFORE it runs against production; you verify every BLOCKER and MAJOR against the real files (L35) and hand the desk a verdict table. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan. His words are committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R8** (14:29 — "asking for second opinion whenever we create or design new features, or did a build and need to deploy"; Grok and Gemini named) and **R9** (14:33 "Approved" — `Bash(grok *)`, `Bash(agy *)` for a read-only review hub in a staged folder). `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` proves it. Mismatch → `FAILED: authorization mismatch — <what>`. Laws: L33 (reviewer = read-only, answerable from reads alone), L35, L36 (you launch the two reviewers; they launch nothing), L37 (no model-judged approval: reviewers FIND, the desk and Dejan decide), L44, L47 (meter = precondition), L32.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, stop.

INDEX CARD (you; the reviewers get the packet only): `prompts/UNATTENDED-LAUNCH.md` §2 → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/audit-house-2026-09-16.md` §2–§3 (the launch spellings that worked and the ones that did not: `grok --sandbox cobalt-job --allow "Write(<dir>/**)" -p "…"`; `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 10m --add-dir <dir> --print="…"`; agy went HARNESS three times on 09-16 because its task needed to RUN a command — this task needs only reads and a printed answer) → this worktree's `scratch/` is the agy-known workspace. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-stack/REVIEW.md` (gitignored scratch — you commit nothing; the desk folds your verdict table into its own report on main) — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Verdict table` → last line.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no pipe, no redirect, no `cd`; long runs (`grok`, `agy`) go `run_in_background`; outputs read from the tool result / the reviewers' output file; files via Write. PREFLIGHT (METER, L47): `grok --version` · `agy --version` (or `agy --help` if it has no `--version`; either matches the rule) · `date`. A usage-limit or login message from either house → record it verbatim, that house = `METER`, continue with the other; both unavailable → `FAILED: no second house reachable`.

## 1. Stage the packet — `mkdir -p scratch/review-deploy-stack`, then Read each source and Write it into that folder under the SAME basename (byte-identical; no summarising). Nothing else goes in.
(1) `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/10-deploy-stack.md` — THE FILE UNDER REVIEW · (2) `…/prompts/2026-09-18/09-stack-gate.md` (the pre-merge gate that feeds it) · (3) `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` (today's two failed runs, in the deploy hub's own words) · (4) `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-17.md` (last night's failure) · (5) `…/prompts/UNATTENDED-LAUNCH.md` · (6) `laws-excerpt.md` — YOU write it: LAWS.md entries L1, L7, L28, L35, L42, L43, L46, L51, L54, L55, L61 and Part VIII PROPOSED-5, verbatim · (7) the code the prompt's gates depend on, from the STACKED branch so the reviewers can check every flag and every quoted string: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show HEAD:src/cobalt/settings/cli.py`, `…:src/cobalt/settings/card.py`, `…:src/cobalt/settings/models.py`, `…:src/cobalt/settings/store.py`, `…:src/cobalt/daymode/config.py`, `…:src/cobalt/db_migrations/cli.py`, `…:src/cobalt/db_migrations/0007_radar_cards.sql`, `…:src/cobalt/session/guard.py`, `…:src/cobalt/jobs/restarts.py`, `…:ops/com.cobalt.aset.plist` — each saved as `code__<path with / replaced by __>`; if the stack gate has not finished its cherry-pick yet and `src/cobalt/daymode/config.py` lacks `trade_count_over_band`, take `config.py` and `restarts.py` from `git -C /Users/cobalt/cobalt show 856176e:<path>` and say so in `## Packet` · (8) `QUESTIONS.md` — YOU write it, verbatim from the block below. `ls -la scratch/review-deploy-stack` + `wc -c` of each → `## Packet` table. NO settings file, no `.env`, nothing under `data/` ever enters the packet (L32, L41).

QUESTIONS.md (verbatim): "You are an independent reviewer from another house. A deploy prompt, `10-deploy-stack.md`, is about to be executed UNATTENDED by an AI agent against a LIVE production trading-support system (two launchd residents, a Postgres database, a settings table that drives trading-day logic). Its three predecessors failed — read `deploy-2026-09-17.md` and `deploy-2026-09-18.md` to see how. Your job is to find what will make THIS one fail or, worse, leave production in a broken state. Read only the files in this folder. For every finding give: SEVERITY (BLOCKER = will fail or can leave production broken / MAJOR = likely failure or an unrecoverable ambiguity / MINOR), the STEP number in `10-deploy-stack.md`, the EVIDENCE as file + line or quoted text from this folder, and the concrete FAILING SCENARIO. Do not pad; ten real findings beat forty guesses; say 'none found' for a category rather than inventing. Check specifically: (Q1) Every command in the launch line's allowlist versus every command the steps actually ask for — is any step's command NOT covered by an allowlisted prefix, or covered only in a different spelling (argument order, quoting, an extra flag)? An uncovered command kills the run mid-deploy with the residents down. (Q2) Every CLI flag and every quoted output string the prompt relies on (`--card`, `--sha256`, `--from`, `--dry-run`, `--apply`, `--allow-prod`, `--rollback`, the text '1 setting(s) would change', 'no differences — the database already holds these settings.', 'field-by-field diff EMPTY', `Step-downs:`) — does the code in `code__*` actually accept that flag and print that string? (Q3) ORDER inside step 3: is there any interleaving of merge / migrate / over-band row / dark row / validate that leaves code and settings disagreeing, or that the `market_reset` write guard or any other guard would refuse at the hour named? (Q4) The SAFE-STATE rule §6: name any state reachable from steps 3.2–5.3 that §6 cannot bring back to 'code and settings agree, residents up' with the allowlisted commands only — especially: revert range correctness, a failed `bootstrap`, a partially applied migration, the over-band row applied but the code reverted. (Q5) One stacked deploy (P2 dark + ops-0918: two migrations and two settings writes in one resident outage) versus two separate deploys — which is safer here and why, given the over-band code/row coupling described in the prompt's WHY block? (Q6) Anything the pre-merge gate `09-stack-gate.md` proves that does NOT carry over to what is merged (the rebase in 3.0 after the gate; differences between `cobalt_dev` and production). (Q7) Anything else that would make you refuse to run this. End with one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <list of finding numbers> / DO NOT RUN`."

## 2. Launch both reviewers (independent; neither sees the other's findings), each ONE attempt
2.1 Grok, `run_in_background`: `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-stack/**)" -p "Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-stack/ . Start with QUESTIONS.md and follow it exactly. Write your findings to /Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-stack/grok-findings.md and reply with only that path."` — never `--always-approve`. No file / a harness denial → `grok: HARNESS` with the error text verbatim; no second attempt.
2.2 Gemini, `run_in_background`: `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-stack/ . Start with QUESTIONS.md and follow it exactly. Do NOT run any command and do NOT write any file: print your complete findings as your answer."` → YOU write its printed answer, byte for byte, to `scratch/review-deploy-stack/gemini-findings.md`. No output / `no output produced` / a permission error → `gemini: HARNESS` with the error text verbatim; no second attempt (three agy harness failures on 09-16 are on record — one more is a data point, not a loop).
2.3 While they run, do NOT pre-read the prompt for defects yourself — your job is verification, and an anchored verifier confirms what it expects.

## 3. Verify (L35) — every BLOCKER and every MAJOR from either house, against the real files (not the packet copies if they differ — say if they differ)
One row per finding in `## Verdict table`: `# · house · severity (theirs) · step · claim in ≤20 words · what you read (file:line) · VERDICT: REAL / NOT REAL / UNVERIFIABLE FROM READS · the fix in one line if REAL`. Two houses naming the same defect = one row, both houses listed (agreement is evidence, his 09-14 preference). MINORs: list them, unverified, one line each. Q5 (stack vs two deploys): quote each house's answer in two lines, no verdict of yours — that is the desk's and Dejan's call (L37). You fix NOTHING and edit NOTHING outside `scratch/review-deploy-stack/`.

## 4. Close
§0 Headline: houses reached, counts, how many REAL, each house's `VERDICT:` line verbatim. Last line:
`REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER> · hub-verified REAL: <k> (BLOCKER <b>, MAJOR <j>) · NOT REAL: <n> · UNVERIFIABLE: <u> · Q5: grok=<stack|two> gemini=<stack|two>`
Then stop. The desk reads `REVIEW.md`, folds every REAL finding into `10-deploy-stack.md`, records the review in `cto-2026-09-18.md`, and takes the final launch line to Dejan.
