MODEL: Opus 5 (`claude-opus-5`) — Opus floor: a production `trader_settings` write ships (L29 write path; a day-mode step-down is trading logic, L7) · SEAT: deploy hub `deploy1-0918`, launched by the CTO desk in the background ONLY AFTER (a) Dejan's approval of the list in this launch line and of the settings files by path + sha256, and (b) his "done trading" word — both rows in `cto-2026-09-18.md` §4 (`prompts/UNATTENDED-LAUNCH.md`). Two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/06-deploy-1-ops.md' and follow it exactly." --model claude-opus-5 --remote-control deploy1-0918 --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C /Users/cobalt/cobalt reset --soft HEAD~1)" "Bash(git -C /Users/cobalt/cobalt tag *)" "Bash(git -C /Users/cobalt/cobalt merge --ff-only ops/2026-09-18)" "Bash(git -C /Users/cobalt/cobalt revert --no-edit *)" "Bash(git -C /Users/cobalt/cobalt-wt/ops-2026-09-18 rebase main)" "Bash(git -C /Users/cobalt/cobalt-wt/ops-2026-09-18 rebase --abort)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * rev-parse*)" "Bash(git -C * rev-list*)" "Bash(git -C * merge-base*)" "Bash(git -C * show*)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt jobs *)" "Bash(COBALT_ENV=production uv run cobalt backup run*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt/data/backups/pre-ops-0918/daymode-settings-rollback --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt/data/backups/pre-ops-0918/daymode-settings-rollback --apply)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(uv run pytest *)" "Bash(launchctl bootout gui/501/com.cobalt.aset)" "Bash(launchctl bootout gui/501/com.cobalt.radar)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.aset.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist)" "Bash(launchctl kickstart -k gui/501/com.cobalt.aset)" "Bash(launchctl kickstart -k gui/501/com.cobalt.radar)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(mkdir -p /Users/cobalt/cobalt/data/backups/pre-ops-0918/daymode-settings-rollback)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; the allowlist is per session (L55, L61), never `bypassPermissions`, push never in it · METER: Anthropic small (one Opus session, no wake-ups) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# DEPLOY 1 of 2026-09-18 (daytime, under R3) — `ops/2026-09-18` ALONE: over-band step-down (R13) + its settings row, `ops/cto-desk.sh`, the META/operator-script RESTARTS rules. Shape A (LAWS PROPOSED-5): both residents DOWN before the merge, UP after. No migration. DO NOT STOP this session until its report ends `DEPLOY 1 DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 07:5x ET), not by Dejan; a prompt file is not an approval. His words, with times, are in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` `## §4`: row **R3** (07:14 ET — the weekend push: "No need to wait for 9pm to restart. Deploy and restart systems every time we have something to deploy." = the dated exception to L43's evening-only clause, 09-18 → 09-20); the row approving **THIS launch line's allowlist AND the over-band settings load by file + sha256** (`…/scratch/daymode-settings-0918/daymode.yaml` = `daa7bb720f19b60a438f3366aa7327188842d69f5fe0da343fa6bbbe7b3d5ebb`, `aset.yaml` = `8eca6945087170312a11709a4c5833a9d36d298798343ea1af39265a6eb99d58`, ONE new row `trade_count_over_band: down 1`, and the rollback directory defined in §0.3); and the row carrying his **"done trading"** word for today. `cto-2026-09-17.md` row **R13** (07:37 "ruling A." — over the band = adverse, `down 1`) is the ruling being shipped. Confirm the rows are committed: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. Any of the three rows absent, a different hash, a rule in your launch line the approval row does not cover → `FAILED: authorization mismatch — <what>`, commit the report, stop. Laws: L61, L7 (+ its interim approval clause), L28, L43 as excepted by R3, PROPOSED-5, L54, L42, L51, L29, L55, L32 (the settings files are user data: never `git add`, never quote a value beyond the one new row).
YOU CAN ALWAYS STOP. Only the dialog is removed. Doubt → `FAILED: <step> — <concern>` as the last line, commit the report, stop. ONE ASYMMETRY: from the first `bootout` on, production's residents are DOWN — "stop" then means finish §6's safe-state rule first. Production is never left with residents down and no loud line saying so (NN#16).

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 (binds you) → Memory `INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (production deploy) → `/Users/cobalt/cobalt-wt/ops-2026-09-18/docs/40 - DevDocs/reports/ops-2026-09-18.md` (§0, §1.2, §1.3, §5, ESCALATE 2–5, last line) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-17.md` (last night's shape and its ESCALATE 2 — why the residents go down first). Report (L48), from the first step: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` — §0 Headline ≤5 lines (line 1 `DO NOT STOP until DEPLOY 1 DONE`) → `## PREFLIGHT` → `## Preconditions` → `## Deploy table` → `## Smoke` → `## ESCALATE` → last line = `CONTINUE:` / stop / `FAILED`.
UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`, no pipe, no redirect, no `; echo`, no `$(…)`; your cwd stays `/Users/cobalt/cobalt`; git always as `git -C <path> …`; read outputs from the tool result; files via Write/Edit. MID-RUN DENIAL = THE RUN FAILED: §6 if anything is merged or any resident is down, then `FAILED: <step> — <command> — <reason string>`, commit, stop. Never another shape, never another session.

THE COUPLING YOU ARE DEPLOYING AROUND (desk-verified in `src/cobalt/daymode/config.py` on main): the LIVE code refuses a `daymode.stepdowns` row whose signal is not in `SIGNAL_IDS` (line ≈97, "unknown step-down signal"), and the NEW code refuses to load when a `SIGNAL_IDS` entry has no row (line ≈252, "has no row for"). Neither order works with a resident running. So: residents down → merge → row → validate → residents up; and a code revert ALWAYS travels with the rollback settings load (§6).

## 0. PREFLIGHT + the rollback directory (nothing here changes production)
0.1 One row each (rule · command · exit · allowed/DENIED + reason verbatim): P1 `git -C /Users/cobalt/cobalt status --porcelain` · P2 `git -C /Users/cobalt/cobalt tag scratch-allow-probe-0918` then `git -C /Users/cobalt/cobalt tag -d scratch-allow-probe-0918` · P3 `git -C /Users/cobalt/cobalt commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` then `git -C /Users/cobalt/cobalt reset --soft HEAD~1`, `git -C /Users/cobalt/cobalt log --oneline -1` proves HEAD is back · P4 `COBALT_ENV=production uv run cobalt validate` → exit 0 · P5 `COBALT_ENV=production uv run cobalt backup status` · P6 `COBALT_ENV=production uv run cobalt backup run --dry-run` · P7 `COBALT_ENV=production uv run cobalt jobs restarts main..ops/2026-09-18` · P8 `COBALT_ENV=production uv run cobalt heartbeat show` (no RED; a RED that exists BEFORE you start → `FAILED PREFLIGHT: production not green`) · P9 `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar` → running, record both pids · P10 `curl … http://127.0.0.1:5010/` → 200 · P11 `shasum -a 256` of the two forward files → equal the hashes in the approval row. The FORWARD dry run is NOT run here: main's live code rejects the new row at parse time ("unknown step-down signal"), so from `~/cobalt` it can only run after the merge (step 3.3a). Before the outage the forward files are proven against production transitively: 0.3's rollback directory equals production (its dry run says 0 changes) and differs from the forward files by exactly the four-line row. NOT preflighted (no harmless variant): `bootout`, `bootstrap`, `kickstart`, `merge --ff-only`, `rebase`, `revert`, the two `--apply` lines — a denial at run time follows §6.
0.2 `GATE: proven — commit + tag allowed by the session allowlist` (L29), or the FAILED line.
0.3 ROLLBACK DIRECTORY (user data, gitignored `data/`, never `git add`, no value quoted in the report): `mkdir -p /Users/cobalt/cobalt/data/backups/pre-ops-0918/daymode-settings-rollback`; Read the two forward files; Write `aset.yaml` there BYTE-IDENTICAL; Write `daymode.yaml` there = the forward file MINUS exactly the four-line `trade_count_over_band` row (`- signal:` / `effect:` / `rungs:` / `because:`) and nothing else. `shasum -a 256` both → record. The rollback dry run (exact allowlisted line) → GATE: prints `0 setting(s) would change` — that proves the directory IS production's current state, which is the only thing its `--apply` can ever restore. Anything else → `FAILED PREFLIGHT: rollback file does not equal production`. Last line `CONTINUE: step 1`, report committed by name.

## 1. Preconditions (record each in `## Preconditions`)
1.1 `date` → NOT inside 19:45–21:40 ET (the `market_reset` hard block refuses settings writes 20:00–21:00; the archiver runs 20:30, the replay 21:05) → inside it: `FAILED: window — wait for the desk`. 1.2 The "done trading" row exists and is committed (AUTHORIZATION). 1.3 `git -C /Users/cobalt/cobalt-wt/ops-2026-09-18 rev-parse --short HEAD` = `b1b3045` and `status --porcelain` empty (ignoring `?? scratch/`); its report's last line starts `OPS 0918`. A moved tip: docs/report-only commits by that branch's own hub are fine (say so); anything else → `FAILED: ops tip moved`. 1.4 NO other deploy or build session is merging: `git -C /Users/cobalt/cobalt log --oneline -3` — main's tip is a desk docs commit.

## 2. Snapshot, machine-written files, rebase
2.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `configs/cobalt/rules.yaml`, `docs/40 - DevDocs/reports/seat-usage.md`, desk / deploy reports. `git add` them BY NAME (never `-A`, never anything under `data/`), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`. Record `<main-before>`.
2.2 `COBALT_ENV=production uv run cobalt backup run` → quote the `cobalt_brain` dump size line and the restic snapshot id; `backup status` shows it newest. No snapshot id → `FAILED: snapshot`. `git -C /Users/cobalt/cobalt tag pre-ops-0918`. (Known, ops ESCALATE 4: tonight's 21:40 nightly prunes this snapshot — record the id and the time; the fix is a later ops item, not yours.)
2.3 `git -C /Users/cobalt/cobalt-wt/ops-2026-09-18 rebase main` → conflict: `… rebase --abort`, `FAILED: rebase conflict — <paths>` (nothing is down yet, just stop). Record the new tip; `git -C /Users/cobalt/cobalt-wt/ops-2026-09-18 diff --stat main HEAD` touches only the paths in the ops report §5 table. Last line `CONTINUE: step 3`, report committed.

## 3. Residents DOWN → merge → settings row → validate → suite (keep this window short: no reading, no report prose between these commands beyond the outputs)
3.1 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → record the baseline. `date`.
3.2 `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print gui/501/com.cobalt.aset` and `…radar` → each must now FAIL with "Could not find service" (that failure is the proof; a service still printed as running → repeat the `print` once after a `date` call; still there → §6). A 15-minute heartbeat beat landing in this window will go RED on the sheet/radar probes and DM him — expected, say so in the report with the beat time.
3.3 `git -C /Users/cobalt/cobalt merge --ff-only ops/2026-09-18` → record the tip. 3.3a The FORWARD dry run (exact allowlisted line), now on the merged code → GATE: prints exactly `1 setting(s) would change` and the only `~` is `daymode.stepdowns`. Anything else → do NOT apply; §6 with step (2) skipped (no settings were touched — say so).
3.4 The forward `--apply` (exact allowlisted line) → quote its `applied: {…}` line and the `from_db() == from_yaml() — field-by-field diff EMPTY.` line. Beside it: `approved by Dejan via the desk — cto-2026-09-18.md row <n>, files + sha256 as named`, the command, the hashes, the time (L28's trace line).
3.5 `COBALT_ENV=production uv run cobalt validate` → exit 0 AND the `Step-downs:` line carries `trade_count_over_band=down(1)` (7 rows). 3.6 `uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote the line). Red at 3.4–3.6 → §6.

## 4. Residents UP
4.1 `COBALT_ENV=production uv run cobalt jobs register` (no plist change expected — say so) → `COBALT_ENV=production uv run cobalt jobs restarts <main-before>..HEAD` verbatim; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED (the META rule is in this very merge). A third resident derived → ESCALATE, do not restart it today.
4.2 `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.aset.plist` · `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` · `launchctl print` each → `state = running`, NEW pids. `Bootstrap failed: 5` (the old instance not fully gone): one `date`, retry ONCE. A service that prints as loaded but not running → `launchctl kickstart -k gui/501/<label>` ONCE. Still not running → §6 step (4) wording: `FAILED: residents DOWN — <label> — <launchctl output verbatim>` (the desk pushes Dejan at once). `date` → record the outage length (3.2 → 4.2).

## 5. Smoke — ALL delivered functionality (NN#16), each quoted in `## Smoke`
`curl … :5010/` = 200 · `curl … :5010/radar` = 200 · `COBALT_ENV=production uv run cobalt heartbeat show` → no RED that is newer than 4.2 (a RED stamped inside the outage window is the expected one — name it) · `COBALT_ENV=production uv run cobalt validate` exit 0 · `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` → started on the new code, no traceback, and — a scanning session is OPEN at this hour — a scan cycle completes: `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT last_scan_at, members, failed_stage FROM radar_pool"` twice, ≥3 minutes apart by `date`: `last_scan_at` advances, `failed_stage` empty · `tail -n 30 /Users/cobalt/cobalt/logs/aset.err` (or the aset log the plist names — `launchctl print` shows the path) → no traceback, no degraded day-mode banner line · `ls -la /Users/cobalt/cobalt/ops/cto-desk.sh` → executable. Green → `git -C /Users/cobalt/cobalt tag deploy-2026-09-18-1`. Deploy table (branch | tip | status | notes), `RESTARTS done: <labels>`, outage length, the membership-gap window (3.2 → first completed scan) stated as a fact for R3's named cost. First live proof of the step-down itself = Monday 09:00 `com.cobalt.daymode-propose` — write that sentence. Report committed by name. Last line:
`DEPLOY 1 DONE <hash> · LIVE: ops-0918 y · over-band row applied <time> · RESTARTS done: com.cobalt.aset com.cobalt.radar · outage <mm:ss> · PUSH: Dejan · ESCALATE: <n>`

## 6. SAFE-STATE RULE — red at 3.4–3.6, red smoke, a failed bootstrap, or a mid-run denial after 3.2
Goal: code and settings agree, residents UP, before you stop. (1) If 3.3 merged: `git -C /Users/cobalt/cobalt revert --no-edit <main-before>..HEAD` (L54 — never `reset`). (2) If 3.4 applied (or you cannot prove it did not): the ROLLBACK `--apply` (exact allowlisted line) → then `COBALT_ENV=production uv run cobalt validate` → exit 0 on the reverted code. Code reverted + row still present, or row removed + new code still merged, are BOTH broken states — never leave either. (3) Bring the residents up exactly as 4.2. (4) Re-run §5's curl / heartbeat / validate / radar_pool lines → quote them. (5) `ESCALATE` with the snapshot id, tag `pre-ops-0918`, the reverted range, which settings directory is live; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. Residents DOWN at the end is the one outcome the desk escalates by push immediately.
