STATUS: DRAFT FOR SECOND-HOUSE REVIEW (R8, 2026-09-18) — NOT APPROVED, NOT LAUNCHABLE. The desk launches it only after (1) `stack-gate-2026-09-18.md` ends `STACK READY`, (2) the review findings are folded in, (3) Dejan approves the final launch line + both settings loads by file + sha256.

MODEL: Opus 5 (`claude-opus-5`) — Opus floor: production migrations 0006/0007 and two production `trader_settings` writes ship (L29 write path; L7) · SEAT: deploy hub `deploy-stack-0918`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/10-deploy-stack.md' and follow it exactly." --model claude-opus-5 --remote-control deploy-stack-0918 --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C /Users/cobalt/cobalt reset --soft HEAD~1)" "Bash(git -C /Users/cobalt/cobalt tag *)" "Bash(git -C /Users/cobalt/cobalt merge --ff-only sprint-2/cards)" "Bash(git -C /Users/cobalt/cobalt revert --no-edit *)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase main)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase --abort)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * rev-parse*)" "Bash(git -C * rev-list*)" "Bash(git -C * merge-base*)" "Bash(git -C * show*)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt jobs *)" "Bash(COBALT_ENV=production uv run cobalt backup run*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt/data/backups/pre-stack-0918/daymode-settings-rollback --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt/data/backups/pre-stack-0918/daymode-settings-rollback --apply)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --apply)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(COBALT_ENV=production uv run cobalt radar evaluate --replay *)" "Bash(launchctl bootout gui/501/com.cobalt.aset)" "Bash(launchctl bootout gui/501/com.cobalt.radar)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist)" "Bash(launchctl kickstart -k gui/501/com.cobalt.aset)" "Bash(launchctl kickstart -k gui/501/com.cobalt.radar)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(mkdir -p /Users/cobalt/cobalt/data/backups/pre-stack-0918/daymode-settings-rollback)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61), never `bypassPermissions`, push never in it, NO `pytest` in it (see WHY) · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely and gets an answer; a dialog gets none).

# STACKED DEPLOY 2026-09-18 (daytime, under R3) — ONE ff-merge of `sprint-2/cards`, which carries S2-P2 (cards, DARK) + ops-0918 (over-band step-down R13, `ops/cto-desk.sh`, META/operator-script RESTARTS rules). Shape A (LAWS PROPOSED-5): both residents DOWN before the merge, UP after. Ships: production migrations 0006/0007, the over-band `daymode.stepdowns` row, the dark row `radar.cards_enabled: false`. DO NOT STOP this session until its report ends `STACK DEPLOY DONE …` or `FAILED …`.

WHY THIS SHAPE (each line is a failure already paid for): (1) 09-17: two branches green alone, red together → the integrated gate runs on the STACKED branch BEFORE the merge (`stack-gate-2026-09-18.md`), not after. (2) 09-18 11:34: `uv run pytest` in `~/cobalt` is NOT offline — `~/cobalt/.env` points it at `cobalt_dev`, whose state has nothing to do with production → this prompt runs NO pytest; after the merge it runs production `validate` + smoke only. (3) 09-17 19:18: launchd respawned the radar into merged code, and the merged CONFIG landed under the OLD resident first → residents are `bootout`ed before the merge. (4) 09-18: a rebase-then-report-commit order broke `--ff-only` with the residents down → the rebase is the LAST thing before `bootout`, and NOTHING commits on main between the rebase and the merge (you hold your report writes; the desk holds its commits). (5) The over-band change is a hard pair (`src/cobalt/daymode/config.py`): live code REFUSES a step-down row for a signal it does not know; new code REFUSES to load without the row → code and row change together, residents down; a code revert ALWAYS travels with the settings rollback. (6) P2's dark row is INERT for old code (desk-verified: `TraderSettings.from_rows` checks only that the seven `SETTING_KEYS` are present and already ignores an eighth row, `aset.account_mode`) and migrations 0006/0007 are additive → a code revert leaves them in place; the schema is NEVER rolled back unattended.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18), not by Dejan; a prompt file is not an approval. His words, with times, are in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` `## §4`: **R3** + **R5** (the weekend push; "Deploy and restart systems every time we have something to deploy"; done trading 11:09), **R6** (the accepted baseline RED class; aset bootstraps from the REPO plist), **R8** (second opinion before every deploy — the row recording the Grok/Gemini review of THIS file and what was folded in), and the row approving **THIS launch line AND both settings loads by file + sha256**: over-band `daymode.yaml` = `daa7bb720f19b60a438f3366aa7327188842d69f5fe0da343fa6bbbe7b3d5ebb`, `aset.yaml` = `8eca6945087170312a11709a4c5833a9d36d298798343ea1af39265a6eb99d58` (production's values + ONE row `trade_count_over_band: down 1`, `cto-2026-09-17.md` R13); dark card file sha256 `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` (exactly `radar.cards_enabled: false`, `cto-2026-09-17.md` R2/R12). Confirm the rows are committed: `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. A missing row, a different hash, a rule the approval row does not cover, or this file still carrying its `STATUS: DRAFT` line → `FAILED: authorization mismatch — <what>`, commit the report, stop. Laws: L61, L7 (+ interim clause), L28, L43 as excepted by R3, PROPOSED-5, L54, L42, L51, L29, L55, L32 (settings files are user data: never `git add`, never quote a value beyond the two new rows).
YOU CAN ALWAYS STOP. Doubt → `FAILED: <step> — <concern>` as the last line, commit the report, stop. ONE ASYMMETRY: from the first `bootout` on, production's residents are DOWN — "stop" then means finish §6's safe-state rule first (NN#16).

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → `LAWS.md` IN FULL → `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/stack-gate-2026-09-18.md` (§0, last line `STACK READY <tip> …`) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` (both earlier runs: what worked — bootout/bootstrap, the rollback directory, 4 min 53 s outage — and ESCALATE 1–8 of the second run) → `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` §6 (STEP-D1, rollback) → `/Users/cobalt/cobalt-wt/ops-2026-09-18/docs/40 - DevDocs/reports/ops-2026-09-18.md` §1.3. Report (L48): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` — append `# THIRD RUN — STACKED DEPLOY` under everything there (never rewrite earlier text), rewrite nothing above it; inside it §0 Headline ≤5 lines → PREFLIGHT → Preconditions → Deploy table → Smoke → ESCALATE → last line.
UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`, no pipe, no redirect, no `; echo`, no `$(…)`; cwd stays `/Users/cobalt/cobalt`; git always as `git -C <path> …`; outputs read from the tool result; files via Write/Edit. MID-RUN DENIAL = THE RUN FAILED: §6 if anything is merged or any resident is down, then `FAILED: <step> — <command> — <reason string>`, commit, stop. Never another shape, never another session.

## 0. PREFLIGHT + the rollback directory (nothing here changes production)
0.1 One row each (rule · command · exit · allowed/DENIED + reason verbatim): P1 `git -C /Users/cobalt/cobalt status --porcelain` · P2 `git -C /Users/cobalt/cobalt tag scratch-allow-probe-0918` then `… tag -d scratch-allow-probe-0918` · P3 `git -C /Users/cobalt/cobalt commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` then `… reset --soft HEAD~1`, `… log --oneline -1` proves HEAD is back · P4 `COBALT_ENV=production uv run cobalt validate` → exit 0 · P5 `… backup status` · P6 `… backup run --dry-run` · P7 `COBALT_ENV=production uv run cobalt jobs restarts main..sprint-2/cards` (EXPECT exit 1: main's OLD classifier reads `ops/cto-desk.sh` and `.gitignore` as UNCLASSIFIED; the rules that classify them are in this merge) · P8 `COBALT_ENV=production uv run cobalt heartbeat show` — R6's gate: a PRE-EXISTING RED is accepted ONLY if it is the `radar` probe reading `failed_stage bars: poll failures: <n>; poll <TICKER> stale since …` AND `tail -n 12 /Users/cobalt/cobalt/logs/radar.err` shows `radar cycle: scanning` lines ≤4 minutes apart with no traceback (outside a scanning session — after 20:00 or a weekend — "cycling" reads `idle:<session>` lines instead; say which); record it verbatim as the BASELINE; any other RED → `FAILED PREFLIGHT: production not green` · P9 `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar` → running, record both pids and both `path =` lines (aset = `/Users/cobalt/cobalt/ops/com.cobalt.aset.plist`, radar = `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`; a different path → `FAILED PREFLIGHT: plist path moved`) · P10 `curl … http://127.0.0.1:5010/` → 200 · P11 `shasum -a 256` of the two over-band files and of the dark card file → equal the hashes in the approval row. NOT preflighted (no harmless variant): `bootout`, `bootstrap`, `kickstart`, `merge --ff-only`, `rebase`, `revert`, `db migrate --allow-prod`, the `--apply` lines, `--card` (the option exists only after the merge), `radar evaluate --replay`.
0.2 `GATE: proven — commit + tag allowed by the session allowlist` (L29), or the FAILED line.
0.3 ROLLBACK DIRECTORY, built fresh by YOU (user data, gitignored `data/`, never `git add`, no value quoted): `mkdir -p /Users/cobalt/cobalt/data/backups/pre-stack-0918/daymode-settings-rollback`; Read the two forward files; Write `aset.yaml` there BYTE-IDENTICAL; Write `daymode.yaml` there = the forward file MINUS exactly the four-line `trade_count_over_band` row. `shasum -a 256` both → record. The rollback dry run (exact allowlisted line) → GATE: prints `no differences — the database already holds these settings.` (the CLI's zero-change wording; all seven keys listed `=`). Anything else → `FAILED PREFLIGHT: rollback file does not equal production`. (The FORWARD dry run cannot run here: main's live code rejects the new row at parse time; it is step 3.4.) Report committed by name. Last line `CONTINUE: step 1`.

## 1. Preconditions
1.1 `date` → NOT inside 19:30–21:40 ET (settings writes are refused 20:00–21:00; archiver 20:30, replay 21:05) → inside it: `FAILED: window — wait for the desk`. 1.2 `stack-gate-2026-09-18.md` ends `STACK READY <tip> …`; `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rev-parse --short HEAD` = that tip or report-only commits above it; `status --porcelain` empty. 1.3 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; NO build or deploy session is merging (the desk launches you alone).

## 2. Machine-written files, snapshot, tags
2.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `configs/cobalt/rules.yaml`, `docs/40 - DevDocs/reports/seat-usage.md`, desk / deploy reports. `git add` BY NAME (never `-A`, never `data/`), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`.
2.2 `COBALT_ENV=production uv run cobalt backup run` → quote the `cobalt_brain` dump size line and the restic snapshot id; `backup status` shows it newest. No snapshot id → `FAILED: snapshot`. `git -C /Users/cobalt/cobalt tag pre-stack-0918`. (Known: the 21:40 nightly prunes this snapshot, ops ESCALATE 4 — record id + time; the durable rollback point is the tag.)
2.3 Report committed by name NOW — this is your LAST commit before the merge. Last line `CONTINUE: step 3`.

## 3. Rebase → residents DOWN → merge → migrate → rows → validate (no report prose between these commands; record outputs, write them up after 4.2)
3.0 `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase main` (brings the stack above your own commits; a conflict → `… rebase --abort`, `FAILED: rebase conflict — <paths>`; nothing is down yet). PROOF before going on: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff --stat <gated tip> HEAD -- src tests configs ops .gitignore` prints NOTHING (what you merge is byte-identical to what the gate proved) and `git -C /Users/cobalt/cobalt-wt/s2-p2-cards log --oneline main..HEAD` count = the gate's count. Record `<pre-merge>` = `git -C /Users/cobalt/cobalt rev-parse --short HEAD`.
3.1 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → baseline. `date`.
3.2 `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print` each → must FAIL with "Could not find service" (still printed → one `date`, print once more; still there → §6). A heartbeat beat landing in this window goes RED on the sheet/radar probes and DMs him — expected; note the beat time afterwards.
3.3 `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<pre-merge>` (else `FAILED: foreign commit on main — <hash>` → §6 steps 3–5 only: nothing merged, bring the residents up) → `git -C /Users/cobalt/cobalt merge --ff-only sprint-2/cards` → record the tip.
3.4 The over-band FORWARD dry run (exact line) → GATE: exactly `1 setting(s) would change`, the only `~` is `daymode.stepdowns`; else do NOT apply → §6 (settings untouched — say so).
3.5 `COBALT_ENV=production uv run cobalt db migrate --allow-prod` → quote the proof table (`0006_radar_score`, `0007_radar_cards` FORWARD, nothing else). Non-zero exit → §6; output showing a migration PARTIALLY applied → run NO rollback, quote it, `FAILED: migration partial` after §6's code revert + residents up.
3.6 The over-band `--apply` (exact line) → quote `applied: {…}` and the `field-by-field diff EMPTY` line + L28's trace line (`approved by Dejan via the desk — cto-2026-09-18.md row <n>`, command, hashes, time).
3.7 The dark card `--dry-run` (exact line) → GATE: ONE add `radar.cards_enabled` = false, ZERO deletions (any existing `card.*` row listed for deletion → do not apply; §6) → the `--apply` line → quote it + the round trip `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → one row, false + the L28 trace line.
3.8 `COBALT_ENV=production uv run cobalt validate` → exit 0; `Step-downs:` carries `trade_count_over_band=down(1)`; ceiling / band lines unchanged. Red anywhere in 3.4–3.8 → §6.

## 4. Residents UP
4.1 `COBALT_ENV=production uv run cobalt jobs register` (no plist change expected — say so) → `COBALT_ENV=production uv run cobalt jobs restarts <pre-merge>..HEAD` verbatim; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED. A third resident derived → ESCALATE, not restarted today.
4.2 `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` · `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` · `launchctl print` each → `state = running`, NEW pids. `Bootstrap failed: 5` → one `date`, retry ONCE. Loaded but not running → `launchctl kickstart -k gui/501/<label>` ONCE. Still not running → `FAILED: residents DOWN — <label> — <launchctl output verbatim>` after attempting §6 (1)–(3). `date` → outage length (3.2 → 4.2).

## 5. Smoke — ALL delivered functionality (NN#16), each quoted in `## Smoke`
5.1 `curl … :5010/` = 200 · `curl … :5010/radar` = 200 · `COBALT_ENV=production uv run cobalt heartbeat show` → no NEW RED (only the P8 baseline class may remain; a RED stamped inside the outage window is the expected one — name it) · `COBALT_ENV=production uv run cobalt validate` exit 0 · `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` → started on the new code, no traceback, NO `CardSettingsError`, no `trade_def_slug`, no `radar_pool_failed_stage_check` (the 09-17 incident signatures) · if a scanning session is open: `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT last_scan_at, members, failed_stage FROM radar_pool"` twice ≥3 min apart by `date` → `last_scan_at` advances, `failed_stage` empty or `bars` (baseline class only); outside a session → `idle:<session>` lines are correct, say so · `tail -n 30 /Users/cobalt/cobalt/logs/aset.log` → no traceback, no degraded day-mode banner · `ls -la /Users/cobalt/cobalt/ops/cto-desk.sh` → executable.
5.2 P2 DARK proof: `COBALT_ENV=production uv run cobalt radar evaluate --replay 2026-09-18` (writes nothing) → formation lines quoted, cards reported disabled/dark · radar-origin card rows = 0: read `src/cobalt/db_migrations/0007_radar_cards.sql` for the provenance column the `aset_sizings_radar_provenance` CHECK names, then ONE `… db query --side user --prod "SELECT count(*) FROM aset_sizings WHERE <that column marks radar origin>"` → 0 (take the column from the migration file, never guess) · `… db query --side system --prod "SELECT count(*) FROM radar_score_run"` → a number, no error (the table exists; rows appear only in a scanning session — say which case you are in).
5.3 Green → `git -C /Users/cobalt/cobalt tag deploy-2026-09-18`. Deploy table, `RESTARTS done`, outage length, the membership-gap window (3.2 → first completed scan). First live proof of the step-down = Monday 09:00 `com.cobalt.daymode-propose`; first COMPLETE `radar_score_run` in premarket = Monday 04:00, proven by that day-open — write both sentences. Report committed by name. Last line:
`STACK DEPLOY DONE <hash> · LIVE: sprint-2/cards DARK y · ops-0918 y · migrations 0006/0007 applied · over-band row <time> · dark row <time> · RESTARTS done: com.cobalt.aset com.cobalt.radar · outage <mm:ss> · PUSH: Dejan · ESCALATE: <n>`

## 6. SAFE-STATE RULE — red at 3.4–3.8, red smoke, a failed bootstrap, or a mid-run denial after 3.2
Goal: code and settings agree, residents UP, before you stop. (1) If 3.3 merged: `git -C /Users/cobalt/cobalt revert --no-edit <pre-merge>..HEAD` — the range starts at the commit IMMEDIATELY BEFORE the ff-merge, so it holds the stack's commits and nothing of yours (L54 — never `reset`). (2) If 3.6 applied, or you cannot prove it did not: the over-band ROLLBACK `--apply` (exact line) → then `COBALT_ENV=production uv run cobalt validate` → exit 0 on the reverted code. Code reverted + over-band row present, or row removed + new code merged, are BOTH broken states — never leave either. (3) The dark row and migrations 0006/0007 STAY (inert for old code / additive); the schema is never rolled back unattended — PROVE it with `validate` exit 0 and the sheet 200 after (4). (4) Bring the residents up exactly as 4.2. (5) Re-run 5.1's curl / heartbeat / validate / radar lines → quote them. (6) `ESCALATE` with the snapshot id, tag `pre-stack-0918`, the reverted range, which settings are live, what stays applied; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. Residents DOWN at the end is the one outcome the desk escalates by push at once.
