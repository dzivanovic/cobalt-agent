MODEL: Opus 5 (`claude-opus-5`) — dev-DB migration + a dev settings write are write-path work (L29 floor) · SEAT: stack gate hub `stack-gate-0918`, launched by the CTO desk in the background. NO NEW RULE: every rule below was approved by Dejan on 2026-09-18 for THIS worktree — R1 list (1) (06:46), R2 (`git cherry-pick *`, 07:01), R7 (`git rebase --abort` and the ONE dev settings write, 11:24) (`prompts/UNATTENDED-LAUNCH.md` §1.3). SUPERSEDES `07-p2-integrate.md` (never launched: its precondition, a DONE deploy 1, no longer exists). Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/09-stack-gate.md' and follow it exactly." --model claude-opus-5 --remote-control stack-gate-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git rebase main)" "Bash(git rebase --abort)" "Bash(git cherry-pick *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(shasum -a 256 *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command of any kind (`COBALT_ENV=production` never appears), never `--allow-prod`, no vault write, no push, no code edit · METER: Anthropic small–medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Stack gate 2026-09-18 — build ONE deployable branch in `s2-p2-cards`: P2 (`sprint-2/cards`) rebased onto main, with ops-0918's 8 commits cherry-picked ON TOP, and prove the INTEGRATED suite green WITH the dev database. This is the pre-merge gate both 09-17 and today's deploy 1 lacked. No code changes. `cobalt_dev` is YOURS ALONE while you run (the desk serializes dev-DB sessions; the P4 build running beside you is offline by its launch line). DO NOT STOP until the report ends `STACK READY …` or `FAILED …`.

WHY A STACK: ops-0918 was merged to main at 11:29 and REVERTED at 11:30 (`deploy-2026-09-18.md`, second run: its step 3.6 ran `uv run pytest` in `~/cobalt`, which reads `cobalt_dev` through `~/cobalt/.env` — 26 failures from dev-DB state, none from the code). So ops-0918's commits `77260b5..856176e` are now ANCESTORS of main with their content reverted — exactly P2's shape this morning: they can only be re-landed by cherry-pick (`UNATTENDED-LAUNCH.md` §6). Desk-verified read-only at 14:33 ET: the 9 paths those 8 commits touch (`src/cobalt/daymode/config.py`, `propose.py`, `src/cobalt/jobs/restarts.py`, `ops/README.md`, `ops/cto-desk.sh`, three test files, the ops report) do not overlap ANY path of P2's 21 commits — a conflict is not expected anywhere.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 14:3x ET), not by Dejan; a prompt file is not an approval. His words are in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4, committed on main: **R1** (list (1)), **R2** (`git cherry-pick *` in this worktree), **R7** (`git rebase --abort`; the DEV `settings load --from …daymode-settings-0918 --apply`), **R3** + **R5** (the weekend push is open), **R8** (second opinion before every deploy — this gate's output goes to Grok and Gemini before anything ships). Compare this launch line rule by rule; a rule in none of those rows → `FAILED: authorization mismatch — <rule>`. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves the rows are committed. Laws: L29, L35, L41 interim (`.env` by name only, never printed, removed after), L32 (the settings files are user data: never `git add`, no value in the report), L45, L46, L54, L55, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → this worktree's `docs/40 - DevDocs/reports/s2-p2-reship-2026-09-18.md` (§0 and the THIRD RUN's READY line: 1801 passed, 0 failed, `cobalt_dev` at 0007) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` (SECOND RUN: §0, ESCALATE 1–2, "State handed to the desk") → `/Users/cobalt/cobalt-wt/ops-2026-09-18/docs/40 - DevDocs/reports/ops-2026-09-18.md` §1.2, §5, ESCALATE 3, 5. Report (L48): a NEW file in this worktree, `docs/40 - DevDocs/reports/stack-gate-2026-09-18.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → one table per step → `## ESCALATE` → last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect; `cd` is its own call; read outputs from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`, `git cherry-pick -h` (usage, exit 129 = allowed), `uv run pytest --co -q tests/cobalt/test_radar_notes.py`; the `.env`, migrate and settings rules are probed by their first real use (every probe must match its own rule's pattern). ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove any `.env` you copied, commit, stop.

## 0. Preconditions
`git status --porcelain` empty · `git rev-parse --short HEAD` = `<old-tip>` (expected `976528f` or a report-only commit above it) · `git log --oneline main..HEAD` → record the count `<n>` and the list · `git -C /Users/cobalt/cobalt log --oneline -12` shows main's tip is a desk docs commit above `77b1a08` and the 8 `Revert "…"` commits of ops-0918 · `git log --oneline 77260b5..856176e` = exactly the 8 ops commits (`fe195ab` … `856176e`). Last line `CONTINUE: step 1`.

## 1. P2 onto main, then ops-0918 on top
1.1 `git rebase main` — P2's commits are NEW since this morning's cherry-pick, so they replay normally. ANY conflict → `git rebase --abort`, `FAILED: rebase conflict — <file>`. After: `git log --oneline main..HEAD` count = `<n>` (a dropped commit → `FAILED: rebase dropped <k>`; `<old-tip>` is in your report). 1.2 `git cherry-pick 77260b5..856176e` — 8 commits, oldest first, original messages, no squash. ANY conflict or an empty-commit stop → `git cherry-pick --abort`, `FAILED: cherry-pick — <file or commit> — <git's message>` (the branch returns to 1.1's tip). 1.3 PROOF, all three or `FAILED: stack proof — <which>`: `git log --oneline main..HEAD` count = `<n>` + 8 · `git diff --stat 856176e HEAD -- src/cobalt/daymode src/cobalt/jobs/restarts.py ops/README.md ops/cto-desk.sh tests/cobalt/test_daymode.py tests/cobalt/test_daymode_note.py tests/cobalt/test_jobs_restarts.py` prints NOTHING (ops-0918's code, byte-identical to what merged at 11:29) · `git diff --stat <old-tip> HEAD -- src tests configs ops .gitignore` lists ONLY those same ops paths (P2's code untouched by the stacking). 1.4 `uv run pytest -q tests/cobalt tests/taxonomy` — this worktree has NO `.env`, so this run is genuinely offline (`ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env` → No such file, first) → 0 failed. Last line `CONTINUE: step 2`.

## 2. The dev database learns the over-band row, then the full gate
`cp` the `.env` in · `COBALT_ENV=dev uv run cobalt db migrate` → 0001–0007 present, exit 0; P4's tables (`picks`, `missed`, `movers_daily`) must be ABSENT — present → `FAILED: cobalt_dev is not clean — another session migrated it` · `COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --dry-run` → EXPECT 3 changed keys (`daymode.stepdowns` + the two known dev↔production drift rows, ops ESCALATE 5) — record KEYS only, never values (L32) · `shasum -a 256` of the two files = the hashes in `cto-2026-09-18.md` R4 · the `--apply` line (exact allowlisted shape; DEV database only) → quote its `applied:` line and its field-by-field-diff-empty line · `COBALT_ENV=dev uv run cobalt validate` → exit 0, `Step-downs:` carries `trade_count_over_band=down(1)` · **`COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed.** ANY failure: name the test, root cause, whose it is (P2's / ops-0918's / environment); fix NOTHING; last line `FAILED: integrated suite — <n> red`. Then `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m "" -p no:randomly` → 0 failed · `COBALT_ENV=dev uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --dry-run` → ONE add (`radar.cards_enabled: false`), zero deletions; its `shasum -a 256` = `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` · `rm` the `.env`, `ls -la` proves it gone · `uv run cobalt jobs restarts main..HEAD` → table verbatim; EXPECT `RESTARTS: com.cobalt.aset com.cobalt.radar` and 0 UNCLASSIFIED (the META + operator-script rules are in this stack; anything UNCLASSIFIED → ESCALATE row, not a failure). Last line `CONTINUE: step 3`.

## 3. Close
`git status --porcelain` empty · §0 Headline · `MEMORY: cobalt_dev at 0007 + over-band row since <time>; dev↔production settings drift closed by stack-gate-0918` · last line:
`STACK READY <tip> | P2 (<n> commits) + ops-0918 (8 commits) on main <sha> | offline: <a> passed, 0 failed | integrated with DB: <b> passed, 0 failed | real-vault: <c> passed | dark file sha256 945e42f8…ca7cd3ca unchanged | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours: the stacked-deploy prompt goes to a second-house review first (R8), then to Dejan for its approval list. Never merge, never touch `~/cobalt`'s tree, never push.
