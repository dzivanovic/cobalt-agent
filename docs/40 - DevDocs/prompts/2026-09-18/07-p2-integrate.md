MODEL: Opus 5 (`claude-opus-5`) — dev-DB migration + a dev settings write are write-path work (L29 floor) · SEAT: P2 integrate hub `p2-integrate-0918`, launched by the CTO desk in the background ONLY AFTER `deploy-2026-09-18.md` ends `DEPLOY 1 DONE …` (ops-0918 is on main) and after Dejan's approval of the TWO new rules in this launch line (`Bash(git rebase --abort)`, and the ONE dev settings write `Bash(COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)`); every other rule is R1 list (1) of 2026-09-18, same job, not brought to him again (`prompts/UNATTENDED-LAUNCH.md` §1.3). Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/07-p2-integrate.md' and follow it exactly." --model claude-opus-5 --remote-control p2-integrate-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run cobalt settings load *--dry-run*)" "Bash(COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git rebase main)" "Bash(git rebase --abort)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(shasum -a 256 *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; NO production command of any kind (`COBALT_ENV=production` never appears), never `--allow-prod`, no vault write, no push, no code edit · METER: Anthropic small · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# S2-P2 integrate 2026-09-18 — put `sprint-2/cards` on top of main-WITH-ops-0918 and prove the INTEGRATED suite green with the dev database. This is the gate that was skipped on 09-17 (two branches each green alone, red together). No code changes. `cobalt_dev` is YOURS ALONE while you run (the desk serializes dev-DB sessions today). DO NOT STOP until the report ends `READY FOR MERGE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18, time in the desk report), not by Dejan; a prompt file is not an approval. His words are in committed files on main, `docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** (06:46, list (1) — this worktree's dev rules), **R3** + **R5** (the weekend push is open), and the row approving THIS launch line's two new rules (absent → `FAILED: authorization mismatch`). `cto-2026-09-17.md` **R2**, **R5**. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves they are committed. Laws: L29, L35, L41 interim (`.env` by name only, never printed, removed after), L32 (the settings files are user data: never `git add`, no value in the report), L45, L46, L54, L55, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → this worktree's `docs/40 - DevDocs/reports/s2-p2-reship-2026-09-18.md` (§0, THIRD RUN, its READY line) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` (§0, last line) → `/Users/cobalt/cobalt-wt/ops-2026-09-18/docs/40 - DevDocs/reports/ops-2026-09-18.md` §1.2 + ESCALATE 3, 5. Report (L48): the SAME P2 file — add `## FOURTH RUN — integrate on main-with-ops` under everything there (never rewrite earlier text), rewrite §0 Headline ≤5 lines, tables per step, `## ESCALATE 4th run`, last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect; `cd` is its own call; read outputs from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`, `uv run pytest --co -q tests/cobalt/test_radar_notes.py`; the `.env`, migrate and settings rules are probed by their first real use (every probe must match its own rule's pattern). ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove any `.env` you copied, commit, stop.

## 0. Preconditions
`tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md"` starts `DEPLOY 1 DONE` (else `FAILED: deploy 1 not done`) · `git -C /Users/cobalt/cobalt log --oneline -12` shows the ops-0918 commits (`feat(daymode): trade_count_over_band …`) on main · `git status --porcelain` empty · `git rev-parse --short HEAD` recorded (= `<old-tip>`) · `git log --oneline main..HEAD` → record the count `<n>` and the list. Last line `CONTINUE: step 1`.

## 1. Rebase onto main-with-ops
`git rebase main`. P2's 21 commits are NEW commits since the cherry-pick, so they replay normally. A conflict in ANY file → `git rebase --abort` and `FAILED: rebase conflict — <file>` (the desk decides; nothing is lost). After: `git log --oneline main..HEAD` count = `<n>` exactly (a dropped commit → `FAILED: rebase dropped <k> commits`; `<old-tip>` stays in the reflog and in your report) · `git diff --stat <old-tip> HEAD -- src tests configs ops .gitignore` lists ONLY paths that `git diff --stat <old-tip's merge-base with main> main -- src tests configs ops .gitignore` also lists (= what ops-0918 and nothing else brought in) — record both outputs. `uv run pytest -q tests/cobalt tests/taxonomy` (offline) → 0 failed. Last line `CONTINUE: step 2`.

## 2. The dev database learns the over-band row, then the full gate
`cp` the `.env` in · `COBALT_ENV=dev uv run cobalt db migrate` → 0001–0007 present, nothing unexpected, exit 0 (P4's 0008/0009 must be ABSENT — if `picks`/`missed`/`movers_daily` appear → `FAILED: cobalt_dev is not clean — another session migrated it`) · `COBALT_ENV=dev uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --dry-run` → EXPECT 3 changes (`daymode.stepdowns` + the two known dev↔production drift rows, ops ESCALATE 5) — record the KEYS only, never values (L32) · `shasum -a 256` of the two files = `daa7bb72…3d5ebb` / `8eca6945…b99d58` (full hashes in `cto-2026-09-18.md` R4) · the `--apply` line (exact allowlisted shape; DEV database only — it also ends the dev↔production settings drift) → quote `applied:` and the `diff EMPTY` line · `COBALT_ENV=dev uv run cobalt validate` → exit 0, `Step-downs:` carries `trade_count_over_band=down(1)` · **`COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed.** ANY failure: name the test, root cause, whose it is (P2's / ops-0918's / environment); fix NOTHING; last line `FAILED: integrated suite — <n> red`. Then `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m "" -p no:randomly` → 0 failed · `COBALT_ENV=dev uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --dry-run` → ONE add (`radar.cards_enabled: false`), zero deletions; its `shasum -a 256` = `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` · `rm` the `.env`, `ls -la` proves it gone · `uv run cobalt jobs restarts main..HEAD` → table verbatim; EXPECT `RESTARTS: com.cobalt.aset com.cobalt.radar` and 0 UNCLASSIFIED (the META rule is on main now; `.gitignore` still UNCLASSIFIED → ESCALATE row, not a failure). Last line `CONTINUE: step 3`.

## 3. Close
`git status --porcelain` empty · §0 Headline rewritten · `MEMORY: cobalt_dev at 0007 + over-band row since <time>; dev↔production settings drift closed by p2-integrate-0918` · last line:
`READY FOR MERGE <tip> | rebased onto <main sha> (ops-0918 LIVE under it) | integrated with DB: <n> passed, 0 failed | rollback round-trip: proven (2nd + 3rd run) | dark file sha256 945e42f8…ca7cd3ca unchanged | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>`
Deploy is NOT yours: the desk writes deploy 2 (P2 dark: production migrations 0006/0007, the dark settings row by file + sha256, shape A). Never merge, never touch `~/cobalt`'s tree, never push.
