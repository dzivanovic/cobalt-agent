MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50) · SEAT: P4 follow-up hub `p4-followup-0918`, launched by the CTO desk in the background on the morning of 2026-09-18 AFTER the day-open is GREEN and AFTER Dejan's approval of the ONE new rule (`Bash(grok *)`; every other rule is the list he approved 2026-09-17 06:11 ET, row R6, same job shape — not brought to him again, `prompts/UNATTENDED-LAUNCH.md` §1.3), two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p4` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/01-p4-followup.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-followup-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly *)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(claude -p *)" "Bash(grok *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git rebase main)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(touch scratch/*)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" "CronCreate" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every launch resumes from the report's last `CONTINUE:` line · auto mode on; the allowlist is per session (L55, L61), never `bypassPermissions`, push never in it · METER: Anthropic medium (two Opus headless chunks + conflict resolves); Codex = NONE (out until the weekly reset); Grok = one headless review · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# S2-P4 follow-up — rebase onto main (P2 is LIVE dark since the 09-17 evening), ceiling 50 + disjoint archiver/replay windows (ruled R17), R1-21's positive half against P2's real contract (E2), second-house review by Grok, READY FOR MERGE. No deploy from here: P4 D1 = the evening the desk names (target 2026-09-18). DO NOT STOP until the report ends `READY FOR MERGE` or `FAILED`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. This prompt was written by the CTO desk (Fable, session `fcb31caa`, 2026-09-17), not by Dejan; a prompt file is not an approval. Dejan's own words, with times, are in files committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-17.md` rows **R6** (06:11 ET "approve." — the P4 hub allowlist: `.env` cp/rm by exact path, `COBALT_ENV=dev` migrate/pytest/validate/replay dry-run, read-only real-vault pytest, `claude -p`, git add/commit/rebase main, plain reads, `CronCreate`), **R5** (unattended standard) and **R17** (08:38 ET "50 is approved." — `radar.finviz_max_rpm` 45 → 50 with the archiver/replay window numbers), and in `cto-2026-09-18.md` the row approving **`Bash(grok *)`** for this launch. Confirm the rows are committed: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"`. Laws behind each sensitive step: L61, L41 interim (`.env` by name only, never printed, removed after), L50, L29, L55, L53 (a ceiling is his ruling — R17 is that ruling). This launch line DROPS the production reads and the vault-token fetch of the 09-17 list (not needed) and ADDS only `grok` and `git rebase --continue`. Record does not match → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP. Only the dialog is removed, because nobody sees a `--bg` dialog. If any step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (recovery rule), stop. The desk watches that line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 (binds you) → Memory `INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (hub, L59) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md` (the builder launch shape in its step 2, the RECOVERY RULE, the verification steps 3–4 — reused here unchanged) → this worktree's `docs/40 - DevDocs/reports/s2-p4-build-2026-09-17.md` (§ESCALATE E1–E5, step 4) and `s2-p4-build-opus-B-2026-09-17.md` §5 (E1, E2 in the builder's words) → `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` STEP for formations (the `[amended 2026-09-16 per Astra R1-21; corrected by R2-7]` block, line ≈189) and §8 item 4 → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-17.md` "P4 E1 — the desk's fix" → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-17.md` last line (is `sprint-2/cards DARK y`?). Laws, one line each: L46 one run one commit, clean before any wait · L54 you rebase, the desk's evening merges · L29 Opus floor (migrations ride this branch) · L35 artifact, not claim · L45 real-shape fixtures, never edited to fit · L53 total demand across every consumer, refusal test stays · L57 every derived number replayable · L32 no user data in the repo · L60 recovery rule. Report (L48): `docs/40 - DevDocs/reports/s2-p4-followup-2026-09-18.md` — §0 Headline ≤5 lines (line 1 `DO NOT STOP until READY FOR MERGE`) → `## PREFLIGHT` → tables → `## ESCALATE` → last line = `CONTINUE:` / stop line / `FAILED`.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; long runs (`claude -p`, `grok`) go `run_in_background`. MID-RUN DENIAL = `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry a denied command in another shape, never ask another session to run it.
PREFLIGHT (first two minutes, rows in the report): `git status --porcelain` · `cp` the `.env` → `COBALT_ENV=dev uv run cobalt db migrate --help` → `rm` the `.env`, `ls -la .env` shows it gone · `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (also the L47 meter probe; a usage-limit message → record the reset time, `CronCreate` a wake-up at reset + 5 min, wait) · `grok --version` · `git -C /Users/cobalt/cobalt status --porcelain`. Any denial → `FAILED PREFLIGHT: <rules>`.

## 1. Rebase onto main
1.1 Record `git rev-parse --short HEAD` (expected `ec61e65`), `git -C /Users/cobalt/cobalt log --oneline -1`, and whether P2 is on main: `ls /Users/cobalt/cobalt/src/cobalt/db_migrations/0007_radar_cards.sql` exists AND `deploy-2026-09-17.md` ends `EVENING DONE … sprint-2/cards DARK y`. Write `P2 on main: yes/no`. If NO: skip §3 entirely (E2 cannot be built against an absent P2; the adapter's "formation replay unavailable" line stays) and say so in the headline.
1.2 `git rebase main`. Conflicts: exactly the P2-rebase procedure (`prompts/2026-09-17/03-p2-rebase.md` §2.2–2.3): brief in `scratch/rebase-brief-<n>.md`, ONE Opus headless run per conflict stop, keep BOTH intents, hub verifies (`git diff --check`, no conflict markers, the touched tests pass), `git add <files>`, `GIT_EDITOR=true git rebase --continue`; three failed resolves on one conflict = `FAILED`. Expected conflict zones: `configs/cobalt/taxonomy/tunables.yaml` and `tests/cobalt/test_radar_*.py` (ops-0917 moved the ceiling 40 → 45 in the same fixtures P4 touched), `src/cobalt/db_migrations/placement.py` / `__init__.py` (P2's 0006/0007 entries vs P4's 0008/0009 — BOTH sets stay, numeric order).
1.3 After: commit count unchanged (`git log --oneline main..HEAD` before = after), `uv run pytest -q tests/cobalt tests/taxonomy` green → record the line. Last line `CONTINUE: step 2`.

## 2. Chunk F1 (Opus headless) — R17: ceiling 50, disjoint windows
Builder launch = `01-p4-build.md` step 2's `claude -p` shape, same allowlist, brief written first to `scratch/f1-brief.md`. The change, tests FIRST (each new/changed test must fail before the edit and pass after — evidence in the builder's report): (a) `configs/cobalt/taxonomy/tunables.yaml` `radar.finviz_max_rpm` 45 → 50, the `consumers:` note gains `ruled 50 on 2026-09-17 (Dejan, cto-2026-09-17.md R17: = the archiver's pacing bound 60/1.2)`; every test pinned at 45 moves to 50 KEEPING its shape (a pass just under, a refusal just over — L53's refusal proof is never deleted); (b) `configs/cobalt/jobs.yaml` `com.cobalt.archiver` `timeout_s` 5400 → 2400 with the comment rewritten (normal run 23–24 min; 40 min = the declared demand window's end, 21:10; the watchdog flags ZOMBIE only past it AND with a stale heartbeat); (c) `com.cobalt.replay` `at` "21:05" → "21:10" in `jobs.yaml` AND `ops/com.cobalt.replay.plist` (Hour 21 / Minute 10), comments and DevDocs lines that say 21:05 updated; (d) NEW tests against the SHIPPED registry + tunables (no mocks of the numbers): `check_scheduled_demand("radar", radar_rpm=<planned>)`, `("archiver")`, `("replay", replay_top_n=<n>)` all pass at ceiling 50; the archiver and replay windows are disjoint (`20:30–21:10`, `21:10–21:35`); at ceiling 49 the archiver is REFUSED; with replay `at` forced back to 21:05 the overlap is REFUSED. `GENTLE_SLEEP_SECONDS` is NOT changed. Hub verifies: suite line, `git diff --stat`, `COBALT_ENV=dev uv run cobalt validate` prints ceiling 50. Commit `feat(s2-p4): R17 — finviz ceiling 50, archiver window to 21:10, replay 21:10 (total-demand gate passes for all three consumers)`. Last line `CONTINUE: step 3`.

## 3. Chunk F2 (Opus headless) — E2: R1-21's positive half against P2's REAL contract (only if `P2 on main: yes`)
Brief `scratch/f2-brief.md`: read the plan's R1-21/R2-7 block and P2's shipped replay entrypoint on main (`cobalt.radar.evaluate` — record the concrete callable, arguments, capability/version marker, typed returned fields in the builder report BEFORE any code). Build: `ReplayFormation` gains what migration 0009's `missed` CHECK needs — `membership_id`, the trade_def md5, the retained score-receipt reference — populated from data P2 already holds (no new table, no second seam, L3); then the binding's positive half: two formations under the extended key, existing-card suppression, cf-R through P2's evaluator. Tests first, real-shape fixtures only (L45); `test_r1_21_a_present_but_incompatible_p2_fails_loud` must still pass for an incompatible contract; the "formation replay unavailable" line must still log when P2 is absent. A P2-side field that cannot be populated without a schema change = STOP the chunk, `ESCALATE` with the exact field and why — never a nullable shortcut that weakens 0009's CHECK. Hub verifies as chunk B was verified. Commit `feat(s2-p4): E2 — formation replay binds to P2's shipped contract (R1-21 positive half)`. Last line `CONTINUE: step 4`.

## 4. Full verification (as `01-p4-build.md` step 4)
Offline suite + the named leak test (`test_screen_filter_values_live_only_in_approved_radar_fixtures`) · `.env` in → `COBALT_ENV=dev uv run cobalt db migrate` → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → `.env` out: the 4 failures of 09-17 ESCALATE 4 (P2's 0006/0007 unknown to P4's `placement.py`) must be GONE now that P2 is under this branch — if any remains, name it · ROLLBACK ROUND-TRIP on `cobalt_dev` only (P2 build ESCALATE 12, unproven since 09-16; covered by the launch line's `COBALT_ENV=dev uv run cobalt db migrate*` rule, never `--allow-prod`): with the `.env` still in, `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → `COBALT_ENV=dev uv run cobalt db migrate` → the placement/digest check equals the pre-rollback one; both outputs verbatim in the report; a denial or a digest mismatch = ESCALATE row, the run continues · `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` · `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly --dry-run` · `uv run cobalt jobs restarts main..HEAD` → table verbatim (expected aset + radar + the new one-shot's explicit bootstrap; UNCLASSIFIED → ESCALATE). Last line `CONTINUE: step 5`.

## 5. Second-house review — Grok (audit-house 2026-09-16: grok CAPABLE, agy HARNESS twice → agy is not used)
Stage INSIDE `/Users/cobalt/cobalt-wt/s2-p4/scratch/review/` (Write tool): `diff.txt` = `git diff main..HEAD -- src configs ops` (tests and fixtures excluded — the 09-17 diff was 37k lines of fixtures), the plan, a LAWS excerpt (L1, L8, L28, L32, L45, L53, L57). Launch (spelling per `audit-house-2026-09-16.md` §3, read it first): `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/s2-p4/scratch/review/**)" -p "<read only scratch/review/; list BLOCKER / MAJOR / MINOR findings as file:line + the law or plan clause broken + a failing scenario; write scratch/review/grok-findings.md; reply with only that path>"`, `run_in_background`, never `--always-approve`. You verify every BLOCKER against the code yourself (L35): real → fix through one more Opus headless run + suite, or ESCALATE; not real → say why. No output / a harness denial = `grok: HARNESS` (one attempt, no loop) and ESCALATE — the desk decides whether P4's D1 waits. Last line `CONTINUE: step 6`.

## 6. Close
Worktree clean (only `?? scratch/`), report committed. Last line: `READY FOR MERGE <hash> (code <hash>) | rebased onto <main sha> | RESTARTS: <line> · ceiling 50 · E2: built/skipped · grok: <verified n / total m | HARNESS> · audit: deferred · ESCALATE: <n>`. Then stop. The desk verifies, stops this session, and writes P4's D1 into the evening prompt (plan §6 STEP-D1: snapshot, `pre-s2-p4` tag, migrations 0008/0009 `--allow-prod` BEFORE 20:00, restarts inside the pause, `com.cobalt.replay` bootstrap, Dejan's `radar.benchmark` load by file + sha256, first live replay 21:10).
