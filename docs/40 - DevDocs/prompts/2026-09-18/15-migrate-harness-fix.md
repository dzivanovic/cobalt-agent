MODEL: Opus 5 (`claude-opus-5`) — the migration harness IS the production write path (L29 floor) · SEAT: build hub `migrate-fix-0918`, launched by the CTO desk in the background. NO NEW RULE: the launch line is a subset of the list Dejan approved on 2026-09-18 06:46 ET for this worktree (R1 list (1): `.env` cp/rm/ls by path, `COBALT_ENV=dev uv run cobalt db migrate*` incl. `--rollback`, dev `pytest`/`validate`, `uv run pytest`, git add/commit/diff/status/log/show/rev-parse, reads); code edits are inside this worktree with the Edit/Write tools. NO production command exists in this launch line — the production proof is a separate, later run with its own approval. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/15-migrate-harness-fix.md' and follow it exactly." --model claude-opus-5 --remote-control migrate-fix-0918 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61); never `bypassPermissions`; `COBALT_ENV=production` NEVER appears in this run, never `--allow-prod`, no vault write, no push, no merge · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Migration harness fix — `cobalt db migrate` cannot run against production-sized data. Make its content proof STREAMED (no size ceiling, same digest values) and give it a read-only `--proof-only` mode. Tests first. `cobalt_dev` is YOURS ALONE while you run. DO NOT STOP until the report ends `HARNESS BUILT …` or `FAILED …`.

WHAT HAPPENED (read it in the deploy hub's words: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md`, `# THIRD RUN`, step 3.5 + ESCALATE 1–3): today's stacked deploy — every gate green up to the migration — died at `COBALT_ENV=production uv run cobalt db migrate --allow-prod`: `ProgramLimitExceeded: out of memory — Cannot enlarge string buffer containing 1073741677 bytes by 155 more bytes`. The harness's BEFORE proof runs `md5(coalesce(string_agg((<row_json>)::text, '|' ORDER BY <pk>), ''))` per table (`src/cobalt/db_migrations/cli.py` ≈ line 109, `_probe`); production `system.bars` holds 8,410,174 rows, so the aggregated string passes Postgres's 1 GB varlena ceiling before the first `-- applying`. One transaction, rolled back, nothing applied; the hub reverted the merge, residents back in 1 min 55 s. `cobalt_dev`'s bars table is tiny — no gate could see this. EVERY production migration is blocked until this is fixed. There is also no probe-only mode, so the defect could only be discovered with the residents already down.

DESK DESIGN (architect; build to it; a reason it cannot work → `FAILED: design — <why>`, do not substitute your own):
1. **Streamed digest, byte-compatible.** `_probe` keeps `count(*)` in SQL but computes the digest CLIENT-SIDE: a server-side (named) cursor over `SELECT (<the same row_json expression>)::text FROM <schema>.<table> ORDER BY <the same order expression>`, fetched in batches (≈10,000 rows), folded into ONE `hashlib.md5()` — `update(row_text.encode("utf-8"))` per row with `update(b"|")` BETWEEN rows (never after the last; zero rows = `md5(b"")`), i.e. exactly the bytes `string_agg(…, '|' ORDER BY …)` would have produced, so **every digest value equals the old SQL's value** and old proof tables stay comparable. Constant memory; no ceiling. No `string_agg` of row content remains anywhere in the module. Rejected, and say why in the DevDoc: a digest of per-row md5s (one 32-byte-per-row string: 269 MB at today's 8.4M rows, past 1 GB again near 33M — not durable); exempting bulk tables (a weaker proof exactly where the most data lives).
2. **`--proof-only`.** New flag on `cobalt db migrate`: runs the BEFORE probe over every table the harness knows, prints the same proof-table columns (rows + digest) plus **wall seconds per table and in total**, applies NOTHING, exits 0. It runs inside a transaction opened `READ ONLY` (`SET TRANSACTION READ ONLY` / the driver's read-only option) so that not even a bug in this flag's handling can write. Mutually exclusive with `--rollback` / `--down-to` (loud usage error). On a production environment it still requires `--allow-prod` (same guard as today — a production connection is never opened by default).
3. **Timing is a deliverable**, because this probe runs INSIDE a resident outage, twice (BEFORE and AFTER): the normal `migrate` run prints the same per-table and total seconds.
4. One path (L3): the old SQL digest expression is deleted, not kept behind a switch.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `ab41a0a3`, 2026-09-18 15:5x ET), not by Dejan. His words, committed on main in `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4: **R1** list (1) (this worktree's dev rules), **R3** + **R5** (the weekend push), **R8** (second opinion — this build is reviewed by another house BEFORE any production use; that review and the production proof are the desk's next two runs, not yours). Compare this launch line rule by rule with R1 list (1); a rule not in it → `FAILED: authorization mismatch — <rule>`. `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves the rows are committed. Laws: L1, L3, L10, L28, L29, L35, L41 interim (`.env` by name only, never printed, removed after), L46, L54, L55, L57, L60, L61.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, wip-commit (recovery rule), stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → the deploy report's THIRD RUN (above) → `src/cobalt/db_migrations/cli.py` IN FULL (module docstring: what the proof promises; `_probe`, `_probe_all`, the proof-table renderer, the CLI flags ≈ lines 262–285) + `src/cobalt/db_migrations/__init__.py` (`FORWARD`) + the tests that cover the proof today (`grep -rln "db_migrations\|digest" tests/cobalt`) + the DevDoc `docs/40 - DevDocs/cobalt/db_migrations/cli.md` (or wherever `ls "docs/40 - DevDocs"` shows this module's DevDoc). STATE OF THIS BRANCH: `sprint-2/cards` = the gated stack (P2 + ops-0918 + seam fixes), tip `848681e` + report commits; it was merged to main at 15:42 and REVERTED at 15:44, so its commits are ancestors of main — you only ADD commits on top; you never rebase, never cherry-pick, never touch main (the re-land is the deploy run's job). Report (L48): NEW file `docs/40 - DevDocs/reports/migrate-harness-fix-2026-09-18.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → one table per step → `## ESCALATE` → last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, NO pipe, no redirect; `cd` is its own call; outputs read from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git log -1`, `date`, `uv run pytest --co -q tests/cobalt/test_tenancy.py`, `.env` cp → `COBALT_ENV=dev uv run cobalt db migrate --help` → keep the `.env` for step 1. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove the `.env`, wip-commit, stop.

## 1. Pin today's behaviour BEFORE touching it (`.env` in)
`COBALT_ENV=dev uv run cobalt db migrate` (a no-op on `cobalt_dev` at 0007) → save its whole proof table VERBATIM in the report as `BASELINE` — every table's rows + digest as the OLD SQL computes them. These values are the byte-compatibility oracle for step 3. Last line `CONTINUE: step 2`.

## 2. Tests first (each new test FAILS before the change — quote the failing lines — and passes after)
(a) unit, no DB: the streaming fold over an iterator of row texts equals `md5("|".join(rows))`, for 0 rows (`md5("")`), 1 row, many rows, and rows containing `|`, non-ASCII and very long text; (b) DB: for every table in the proof, the new digest equals the value the OLD SQL expression returns for the same table in the same transaction (the test may run the old expression itself as its oracle — on `cobalt_dev` it is small enough; this is the only place that SQL survives, inside the test); (c) regression guard: no SQL string built by `cli.py` contains `string_agg` — asserted on the constructed queries, not by grepping source; (d) `--proof-only`: prints the table with the seconds columns, applies nothing (`schema_migrations` — or whatever the harness records applied migrations in — identical before and after; no `-- applying` line), exit 0; refuses `--rollback` / `--down-to` with a usage error; the transaction is read-only (a write attempted inside it raises — prove it with a test that tries); (e) memory shape: the cursor is server-side and batched — assert on the cursor's construction (named cursor / `itersize` or the driver's equivalent), since a 1 GB table cannot be built in a test. Commit the red tests: `test(db-migrate): streamed digest + --proof-only — red`.

## 3. Build, then prove on `cobalt_dev`
Implement per the DESK DESIGN. Then: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → table quoted; **every digest equals step 1's BASELINE**, nothing applied · `COBALT_ENV=dev uv run cobalt db migrate` → no-op, digests = BASELINE, seconds printed · ROUND TRIP: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → `COBALT_ENV=dev uv run cobalt db migrate` → digests identical before and after, 0006/0007 back, both outputs verbatim · `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed (the stack gate's third run: 1815 passed) · `COBALT_ENV=dev uv run cobalt validate` exit 0 · `rm` the `.env`, `ls -la` proves it gone · `uv run pytest -q tests/cobalt tests/taxonomy` (offline) → 0 failed · `uv run cobalt jobs restarts 848681e..HEAD` → table verbatim (expected: no resident — `db_migrations/cli.py` is an operator command; whatever it derives, quote it). DevDoc for `db_migrations/cli.py` updated (the streamed proof, `--proof-only`, the two rejected designs and why, the 2026-09-18 incident in two lines). Commit: `fix(db-migrate): stream the content proof (no 1 GB string ceiling, same digests) + read-only --proof-only with timings (deploy-2026-09-18 3.5)`. Last line `CONTINUE: step 4`.

## 4. Close
`git status --porcelain` empty · `git diff --stat 848681e HEAD -- src tests docs` listed · §0 Headline · last line:
`HARNESS BUILT <tip> | digests byte-compatible with BASELINE: <n>/<n> tables | --proof-only: read-only, applies nothing | dev round trip 0005↔0007: identical | with DB: <b> passed, 0 failed | offline: <a> passed, 0 failed | OWED: second-house review, then production --proof-only (read-only, its own approval) | ESCALATE: <n>`
Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production.
