MODEL: Opus 5.5 (`claude-opus-5-5`) — the DERIVE SEAT HIS 09-22 R109 names (`cto-2026-09-22.md` §4, 19:34 ET: "Make all Opus 5.5 for now" — STANDING until his next word: every Fable-type seat, the derive included, on `claude-opus-5-5`) and the desk's round-2 launch row records (`VOICE-TTS-DRAFT-FINAL-2026-09-25.md` · `Fable seat: yes` · `derive seat: claude-opus-5-5`); this is the Anthropic seat's THIRD JOB on the voice-TTS tribunal (round-1 ruling blind `17`, the round-1 derive `18`, round-2 ruling blind `25`, and now THIS: the round-2 derive); you derive the version the round-2 rulings produce — **the FINAL** (`docs/30 - Design/VOICE-TTS-FINAL-2026-09-26.md`), because Astra (L67's OpenAI design seat) has now ruled; you build, launch and commit nothing · SEAT: `voice-tts-derive-r2-0926`, launched by the CTO desk in the background ONLY after every round-2 stop line this file needs is committed (hub `24`; the Anthropic seat `25`). YOUR POSITION, STATED: you are the same SEAT that ruled rounds 1 and 2 blind (`17`, `25`) and derived the DRAFT FINAL (`18`), in a fresh session — you HOLD A SIDE on all four round-2 items (your round-1 wording is Side B under R2-1, Side C under R2-2, Side B under R2-3 and R2-4), so the only recommendation you carry to him is none (L37), and your own round-1 and round-2 wordings pass the same filter as every house's: where a house's wording is equally correct, the house's is taken. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-25/26-voice-tts-derive-r2.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control voice-tts-derive-r2-0926 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the `--model` value is the one the launch row names after `derive seat:`, already filled; the SAME seven READ-ONLY strings and three denies as `prompts/2026-09-23/12-draft-voice-v3.md`'s launch line, 09-20 R13, and so as `18-voice-tts-derive.md`'s — nothing new) · SESSION: fresh (the round-2 seat's context is NOT carried; its report is on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO `uv`, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic medium — honest estimate: the round-2 hub report ≈ 20–30k tokens (22 Astra items, four seats, file-check tables), the raw round-2 rulings of the houses that ruled ≈ 25–40k (Astra's 22 items ≈ 15–20k; Grok's and Gemini's four items plus Part B ≈ 6–10k), your round-2 report ≈ 6–9k, the DRAFT FINAL ≈ 16k (63,651 B, read whole — the FINAL is it amended), the round-1 derive report ≈ 6.4k (25,432 B), LAWS.md ≈ 22k (89,839 B at round 1), the FINAL's cited ranges ≈ 3.6k, V1 code re-opened only where a fold turns on it ≈ 3–8k; the FINAL ≈ 16–19k tokens of output plus the report ≈ 6–9k; peak context ≈ 130–150k — read the raw rulings item by item, not all at once, if the peak nears your window. It depends on no house string (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` in the report and continue with the safe default.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 / L9** (every fallback hop loud) **· L3** (one Synthesizer, one engine by config, one model-fetch command, one config loader, one status route) **· L15** (the new dependency: four gates; licence + native status are experiment rows until VT-X4 runs — L70; never folded as fact; whether a separate process changes a licence's reach is a legal claim, never folded as fact) **· L17 / L39** (≤3 rounds, no fourth; round 3 is the LAST; unresolved after it → Dejan; a design item is never voted) **· L23 · L27 · L28** (VT writes nothing to the vault) **· L31** (`LAWS.md:182`; V1's `faster-whisper` rename is a V1 BACKLOG ticket, `cto-2026-09-25.md` R25 reading (1) — never a VT chunk, never folded here) **· L32** (the FINAL and your report are COMMITTED: no user data — no audio, no transcript, no stored `reply` value, no note of his) **· L35 · L36 · L37** (you derive; a SPLIT between seats is carried, never settled by vote; you hold a side) **· L42 · L43 / L66** (VT lands in a later deploy, R23) **· L45 · L52** (VT reaches no score) **· L57 · L67** (as amended 2026-09-24: OWNER ITEMS are only HIS decisions — a derive that lists a design question as an owner item is returned, not delivered; each derive states whether a Gemini finding held, 09-23 R97) **· L70 · L71 · L72** (the `## SEAM` with V1 and V4 is the document the later build prompt cites) **· L73 · L74**. CLAUDE.md's absolute boundary: nothing in the FINAL reads, writes or infers from his trading platform; the trading PC is a browser tab at most.
2. THE ROUND-2 RULINGS: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-r2-2026-09-26.md` whole — `## Rulings table`, `## Astra disagreement items (Part B)`, `## Composition item C-1`, `## Wording offered, verbatim`, `## Checked against the files` (the `AS` / `GK` / `GM` rows — the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Anthropic-seat round-2 claims, file-checked` (the FR rows, or its one line saying the seat's report was not done at collate), `## Experiments named (L70)`, `## OWNER answers`, `## WRONG FACTS claimed`, `## Independence`, `## NEW STRINGS`, `## ESCALATE` · the raw rulings of EVERY house that ruled round 2, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/voice-tts/r2/`: `astra-ruling-r2.md` (REQUIRED — the hub's stop line's `astra:` field must carry a `TRIBUNAL R2:` line), `grok-ruling-r2.md` and `gemini-ruling-r2.md` only if the hub's stop line shows that house's `TRIBUNAL R2:` line, and `20-ASTRA-DISAGREES.md` (the raw file is what "verbatim" means, but you never copy a user-data span out of it) · your own round-2 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-fable-r2-2026-09-26.md` — including its `## Self-attack` (its `WITHDRAWN:` lines bind you: a withdrawn sentence is never folded).
3. THE DRAFT FINAL `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-DRAFT-FINAL-2026-09-25.md` WHOLE (the FINAL is it amended) and the round-1 derive report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-derive-2026-09-25.md` (`## Fold table` — its `F-01`…`F-57` numbering you CONTINUE from `F-58`; `## NEEDS ROUND 2`; `## ESCALATE` 1–9); the proposal `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-PROPOSAL-2026-09-25.md` and the round-1 raw rulings `…/r1/grok-ruling.md`, `…/r1/gemini-ruling.md` and your round-1 report `reports/voice-tts-tribunal-fable-r1-2026-09-25.md` ONLY where a fold turns on a round-1 wording (the DRAFT FINAL's `## OPEN TO ROUND 2` quotes the four sides verbatim). HIS RULINGS, never re-opened: `cto-2026-09-25.md` **R23**, and the desk's records **R21, R22, R25, R45**; `cto-2026-09-24.md` **R107**; `cto-2026-09-23.md` **R95, R96, R97**; `cto-2026-09-22.md` **R109** — each by `grep -n "^| R<nn> "`. THE FINAL VT AMENDS: `/Users/cobalt/cobalt/docs/30 - Design/VOICE-v3-FINAL-2026-09-23.md` — §2 diagram (:63), §2.5 (:86), §7 (:167–185), §9 (:190–224), `## First-gate experiments` (:269 on; E5 :281, E10 :285), `## Seam` (:311–322), `## L52 and the bar` (:369) — anchors by `grep -n`, never trusted.
4. Only where a fold turns on it: the real file the hub's file-check cites (V1's worktree `/Users/cobalt/cobalt-wt/voice-v1/`, code `d319e4f3` — `git -C /Users/cobalt/cobalt show d319e4f3:<path>` for committed bytes; main; the FINAL by the ranges above) — read to check, never to copy.
WRITES (named here, the only two): `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-FINAL-2026-09-26.md` and `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-derive-r2-2026-09-26.md`. STOP LINE: `VOICE TTS DERIVED · FINAL · …` or `VOICE TTS DERIVED · DRAFT FINAL v2 · …` (below).

DERIVE ROW: R109

# DERIVE THE FINAL of VOICE SLICE VT (server-side local reply voice) from the DRAFT FINAL and the round-2 rulings of EVERY SEAT THAT RULED. LADDER: OFF-LADDER (`cto-2026-09-25.md` R23), a SIDE LANE (L72). LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini, the Anthropic seat; Astra `METER`) → round-1 derive DONE (`18`: the DRAFT FINAL, `needs round 2: 4`) → round 2 DONE (hub `24`: Astra on all 22 items, Grok and Gemini on Part A and Part B; the Anthropic seat `25` on Part A) → **THIS = the round-2 derive → the FINAL** (or, for an item no wording converges, a NEEDS ROUND 3 — the last round, L39) → build per chunk (VT-0 first: the experiments) → ≥3 checkers → a later deploy. L1, L3, L9, L15, L23, L31, L32, L52, L57, L70 and L72 are the bar the FINAL must meet. DO NOT STOP until the report's last line is `VOICE TTS DERIVED · …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The line `DERIVE ROW: R109` above names the row of `cto-2026-09-22.md` `## §4 Rulings 2026-09-22` that pins the Fable-type seat, the derive included — filled at drafting (L19: any other change is a re-issue). Each its own Bash call: `grep -c -x -F "DERIVE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-25/26-voice-tts-derive-r2.md"` (`-x` = whole line: only that line can match) — **1** → `FAILED: authorization — the DERIVE ROW is unfilled`, stop; **0** → read `<nn>` from that line (`109`) · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (exactly ONE row, carrying `Make all Opus 5.5 for now` and `derive` — else `FAILED: authorization mismatch — row R<nn> does not pin the derive seat`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **THE LAUNCH ROWS FOR THIS ROUND** (the desk's row **`R__`** that launched `24` and `25` (FILL AT LAUNCH), and the desk's row **`R__`** that launches THIS derive (FILL AT LAUNCH) — both recorded in `## AUTHORIZATION`, the grep decides): `grep -n -F "24-voice-tts-tribunal-r2.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-25.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-26.md"` (and `cto-<D>.md` on a later run date) → exactly ONE `| R` LAUNCH row carrying, as literal text, `VOICE-TTS-DRAFT-FINAL-2026-09-25.md`, `Fable seat: yes` and `derive seat: claude-opus-5-5` (a missing desk file is recorded, not fatal, when the row is in another; any literal missing, or two such rows → `FAILED: authorization mismatch — no single launch row names the seats for round 2`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"24-voice-tts-tribunal-r2.md" -- "docs/40 - DevDocs/reports/cto-2026-09-25.md" "docs/40 - DevDocs/reports/cto-2026-09-26.md"` (EMPTY → `FAILED: authorization mismatch — the launch row is not committed`; the pathspec names the desk files ONLY) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — the launch row names <id>; this session runs <id>`, stop. **EVERY ROUND-2 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-r2-2026-09-26.md"` (the LAST NON-BLANK line must start `VOICE TTS TRIBUNAL DONE · round: 2` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 2 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE TTS TRIBUNAL DONE · round: 2" -- "docs/40 - DevDocs/reports/voice-tts-tribunal-r2-2026-09-26.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-fable-r2-2026-09-26.md"` (must start `VOICE TTS FABLE R2 DONE ` — else `FAILED: refused — Anthropic-seat round 2 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE TTS FABLE R2 DONE" -- "docs/40 - DevDocs/reports/voice-tts-tribunal-fable-r2-2026-09-26.md"` (EMPTY → `FAILED: refused — the Anthropic-seat round-2 stop line is not committed on main`) · **THE FLOOR — ASTRA AND GROK MUST HAVE RULED ROUND 2:** the hub's stop line's `astra:` field must carry a `TRIBUNAL R2:` line (Astra is the reason round 2 exists; a `FAILED`, `METER`, `PARTIAL` or missing field → `FAILED: floor — astra did not rule round 2; the desk relaunches 24`) and its `grok:` field must carry a `TRIBUNAL R2:` line (else `FAILED: floor — grok did not rule round 2; the desk relaunches 24`; your seat alone never settles an item: you would be deriving from your own ruling, L37). Gemini not having ruled does NOT refuse the derive (the optional fourth, R97): the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/12-draft-voice-v3.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. PATTERN RULE (`topics/cto-desk.md` 09-25 third-desk lesson (1)): no backtick, no `\|`, no alternation inside one pattern, no `$` inside a double-quoted argument. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

THE RULES OF THE DERIVE — `18`'s rules and `prompts/2026-09-24/29-drc-overnight-tribunal-derive-r2.md`'s round-2 substitutions (the bars tribunal's lesson, `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in the next round; everything ADOPTED from a house held), made binding, with this tribunal's substitutions:
- **Every split ADOPTS a house's wording VERBATIM.** An `ADOPT WITH` / `ASTRA RIGHT WITH` / `NEITHER` text goes into the FINAL word for word, or it is not taken. You may choose BETWEEN seats' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check (`AS` / `GK` / `GM` rows), or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **An Anthropic-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's `## Anthropic-seat round-2 claims, file-checked` has its FR rows, they are your filter for your seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the seat's report as not done at collate, every seat claim is UNCHECKED: you may take a seat wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure, and where you cannot settle it by reading it is an experiment, not a fold. Where your seat's wording competes with a house's wording that is equally correct, the house's is taken — you never prefer your own text.
- **When is an ITEM CONVERGED.** Count the SEATS that ruled it: Astra, Grok, Gemini (the houses) plus the Anthropic seat. For R2-1 … R2-4: CONVERGED = (i) every seat that ruled it gave the same answer (the same side letter, or `ADOPT WITH` texts word-for-word the same or that the hub's file-check shows are the same mechanism), **with at least one HOUSE among them**; or (ii) every HOUSE that ruled it gave the same answer and the Anthropic seat's contrary answer rests on a claim that DOES NOT HOLD in the hub's FR / file-check tables, or that the seat itself WITHDREW. The seat's answer alone never settles an item. For an item Part B put to Grok and Gemini (Astra says the DRAFT FINAL's mechanism is NOT CORRECT): CONVERGED = Astra's failing sequence HOLDS in the hub's file-check and every house that ruled Part B answered `ASTRA RIGHT` (its wording taken verbatim) or the same `ASTRA RIGHT WITH` / `NEITHER` text; or every house that ruled Part B answered `DRAFT FINAL CORRECT` and Astra's failing sequence DOES NOT HOLD in the file-check (the DRAFT FINAL text stands, Astra's text goes to `## Dissents, verbatim`); anything else is NOT converged — **never a count, never a vote (L39)**. The Anthropic seat did not rule Part B (blind): it counts on Part A only. An `OWNER` answer is never converged into a design choice by you: if any HOUSE answered `OWNER` on a design item, test it against L67 as amended 2026-09-24 (his money, his data, his laws, his own trading judgement, or his own taste in what he hears — AND no house can decide it): truly his → `## OWNER ITEMS`; a design question → it is NOT an owner item, the item is STILL SPLIT, and it is named in `## ESCALATE` as `OWNER ANSWER ON A DESIGN QUESTION — <house>`.
- **ASTRA'S ROUND-1 ITEMS (Q1 … Q10, (a) … (g)) on the DRAFT FINAL.** An Astra `ADOPT` leaves the DRAFT FINAL's text standing. An Astra `ADOPT WITH <wording>` on an item whose line reads `DRAFT FINAL: CORRECT` is an IMPROVEMENT offered: its wording replaces the DRAFT FINAL's text for that item VERBATIM only when the hub's file-check supports it (HOLDS), no other seat's file-checked ruling contradicts it, and it is not larger than the text it replaces at equal correctness (the smaller wins, priced); otherwise it is carried in `## Dissents, verbatim` word for word. An Astra `REJECT` or `DRAFT FINAL: NOT CORRECT` is settled ONLY by the Part B rules above; where Part B was `NONE` because Astra marked no item NOT CORRECT, an Astra `REJECT` that stands with its failing sequence HOLDING in the file-check is a NOT-converged item (→ `## NEEDS ROUND 3`) and one whose sequence DOES NOT HOLD goes to `## Dissents, verbatim`. Astra's `## WRONG FACTS` rows the hub file-check HOLDS are corrected in place with a fold row.
- **C-1 — THE COMPOSITION OF THE DRAFT FINAL (the round-1 derive's `ASK DESK` 7; desk record `cto-2026-09-25.md` R45: a design-shape reading, checked by Astra's round-2 read, never settled by the desk).** Astra's answer is in the hub's `## Composition item C-1`. `C-1: HOLDS` → the composed folds stand as they are (F-05/F-06, F-21/F-22, F-23/F-24, F-27–F-30, F-35, F-41–F-43) and the fold table says so. `C-1: DOES NOT HOLD — <the fold rows F-nn that blend, and for each the ONE seat's whole wording (or the proposal's text) that stands instead>` → each named row is REPLACED in the FINAL by exactly the ONE wording Astra names, verbatim from that seat's ruling or the proposal, nothing else moved; a row Astra names without naming the wording that stands is carried NOT taken in `## ESCALATE` (a gap, never your filler). Whichever it is, the rule "choosing between wordings, never blending" binds the FINAL's own new folds: one seat's whole wording per item, or a per-line adoption named line by line.
- **His R23 (the order; §2.5's "only if E5 fails" set aside), R107 (V1 = A) and the seat rulings are NEVER re-opened.** A wording that would drop VT, make it conditional on E5 again, pull it into the 09-25 deploy set, pull V1's `faster-whisper` rename or V1's A1 fix into a VT chunk, or change V1's CHECKED contract as a precondition is NOT folded: it is carried in `## Dissents, verbatim` and named in `## ESCALATE` as `RE-OPENS A RULING (<R-row>)`. A seat's ruling that L31 binds V1's identifiers is RECORDED as a finding for the V1 BACKLOG ticket, never a VT fold.
- **Prefer the simpler mechanism, and price it honestly** (09-20 tribunal lesson) — BETWEEN texts the seats agree are correct; a disagreement about correctness is NOT converged, never a size contest. Price the mechanism not taken in the fold row (size, files, which chunk, hours — a GUESS marked as one).
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in the FINAL's experiment list beside VT-X1–X9, placed before the chunk it gates, with the result that would change the design — never an argument in the text, never a defect; round-2 experiments that repeat VT-X1–X9 are merged into that row, attributed, never duplicated; new ones continue VT-X10 onward. The new dependency's licence and native status, whether a separate process changes a licence's reach, the ONNX load, browser autoplay on his devices, the first-audio latency, the resident's RSS beside LM Studio and the output's determinism are experiments unless a run already settled them (none has, at drafting).
- **Invent nothing.** The FINAL adds no mechanism, number, rule, key, route, probe, file or chunk that neither the DRAFT FINAL nor a seat's round-2 ruling contains. A gap no ruling fills stays a named gap (an experiment or a NEEDS ROUND 3 item) — never your filler. Every engine tunable (`tts_timeout_s`, `tts_max_chars`, `tts_rss_amber_mb`, `tts_speed`) keeps NO number until its experiment sets it; a seat's proposed VALUE for a tunable without its measurement is recorded as `<key>: value proposed by <seat> — <file>:<line>` (a pointer, never folded).
- **OWNER ITEMS are only HIS decisions (L67 as amended 2026-09-24).** `## FOR DEJAN` carries **ONE approval — "approve the VT FINAL?"** — plus ONLY an owner item that passes L67's owner test WITH the seat's words quoted (round 1 left none: grok ruled both proposal items settleable by measurement, the derive followed it; Astra's (g) and any OWNER line of round 2 are read against that test). A seat that rules an owner item NOT his (a house can settle it from measurement) has it folded as the houses' mechanism and the item stays out; a seat that rules one his enters `## OWNER ITEMS` only if no file-checked seat ruling shows a house can settle it — if the seats split on whether it passes, it is a NOT-converged item, never an owner item by default. Nothing in the FINAL may make a chunk's BUILD depend on an owner item: the proposal's OWNER ITEM 1 (voice and speed) is heard in the device session he already owes (R25 reading (3)); tests use a constructed config (L69).
- **THE FINAL AMENDMENTS FOR THE DESK'S FOLD.** The DRAFT FINAL's `## FINAL AMENDMENTS (for the desk's fold)` (its blocks A–F and the E5 result cell, each tagged with its fold row) are the text the desk folds into `VOICE-v3-FINAL-2026-09-23.md` — the desk's fold, not yours (you never write the FINAL of the voice design). The FINAL carries `## FINAL AMENDMENTS (for the desk's fold)` AS THE ROUND-2 RULINGS LEAVE THEM: each block kept, or replaced whole by ONE seat's verbatim wording, or amended by a converged R2 wording (amendment B's read-back sentence follows R2-1; amendment C's busy / error rows follow R2-4; amendment D's engine value follows R2-3), each tagged with its fold row; the §2.5 block keeps his R23 sentence. A seat that says an amendment changes a FINAL clause VT does not need → that amendment is dropped or narrowed only by that seat's verbatim wording, never yours.
- **THE SEAM (L72).** The FINAL's `## SEAM` = the DRAFT FINAL's as the round-2 rulings leave it: every V1 file VT touches (`voice/web.py`, `voice/config.py`, `configs/cobalt/voice.yaml`, `models.py` docstring only, `store.py` read-only, `configs/cobalt/jobs.yaml` `reads:`, `pyproject.toml` / `uv.lock`, the `POST_ALLOWLIST` guard test), the V4 seam (`heartbeat/probes.py`; the ONE model-fetch command — first to build creates it), each line citing its `file:line` at `d319e4f3`, and "VT branches off `main` AFTER V1 merges" kept. The DRAFT FINAL's UNSETTLED lines (the key count, eight or nine — R2-1; which of V4 and VT adds `Probe`'s level field; what the fetch command writes for voices — R2-3; the sidecar resident, only if R2-2 chooses one; the `voice.yaml` reads line, UNPROVEN on VT's range until VT-X9) are settled ONLY by a converged round-2 wording; a seam a seat can only state as a READING stays named UNSETTLED (L72 P-b), never smoothed — and the FINAL's `## Status after round 2` names each.
- **Dissents carried VERBATIM**: a seat's REJECT, `DO NOT BUILD` or NOT-taken wording that the FINAL does not follow goes into `## Dissents, verbatim` word for word, with its seat and item, extending the DRAFT FINAL's list.
- **Gemini's findings (09-23 R97):** for each item, the fold row says whether a Gemini finding was folded and whether its supporting claim HELD in the hub's file-check; §0 and the stop line count `Gemini findings held: <n> of <n>` for round 2 and state plainly whether a Gemini finding held — his evidence for keeping or removing the seat (round 1: 3 of 6 held, none unique).
- **NEEDS ROUND 3 — ONLY for a non-convergence (L39).** An item that did not converge (per the rules above) is listed under `## NEEDS ROUND 3` with the positions VERBATIM (`A — <text>, whose` …), each seat's reason in ONE sentence with its `file:line`, and the ONE question round 3 must answer; round 3 is the LAST round: an item still unresolved after it goes to Dejan (L39), and it reaches him as an owner item only if it passes L67's owner test. NO round 3 is listed for a preference: only for an item on which the seats disagree on whether a mechanism is CORRECT, or on which an Astra failing sequence HOLDS and no wording converged, or a `DO NOT BUILD`. Everything converged is folded; the FINAL's header says so honestly: **`FINAL`** when `needs round 3 = 0`; **`DRAFT FINAL v2 — ROUND 3 PENDING (<ids>)`** otherwise (the file name stays `VOICE-TTS-FINAL-2026-09-26.md`; the stop line says which).
- **No user data in either file** (L32): the packet had none after the hub's REDACTION; if a seat's text quotes a value of his anyway, it is taken with that span replaced by `[user data: <file:line>]`; count every redaction. The houses' constructed scenario values (`12.5`, clock times, `af_sarah`) are the houses' own, not his.

WRITE (Write tool, TWO new files, these names WHATEVER the date of the run):
- `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-FINAL-2026-09-26.md` — the DRAFT FINAL, whole (§1–§11, `## OPEN TO ROUND 2`, the experiments, the amendments, the seam, dissents, the bar, owner items, L52 / L31), with each `[R2-n]` paragraph and each amended section REPLACED in place by the converged wording, verbatim, tagged `[F-nn]` CONTINUING the DRAFT FINAL's numbering (`[F-58]` onward, a row of your fold table; the DRAFT FINAL's own `[F-01]`…`[F-57]` tags are KEPT), or by an `OPEN TO ROUND 3` block; `## OPEN TO ROUND 2` becomes `## RESOLVED IN ROUND 2` (each item, its converged text or its pointer to `## NEEDS ROUND 3`); the header names it `FINAL` or `DRAFT FINAL v2 — ROUND 3 PENDING (<ids>)` (above), the proposal, the DRAFT FINAL, R23 / R25 / R45 / R107 / R95–R97 / R109, the rulings of EVERY seat that ruled round 2 (paths; Astra `RULED` with its `TRIBUNAL R2:` line; the Anthropic seat's status from R109 and the launch row) and the fold table's path; a `## Status after round 2` section (per R2 item and per Part-B item: converged / NEEDS ROUND 3, and the chunk each gates — R2-1 gates VT-2's route guard and VT-3's widget, R2-2 gates VT-1 only if VT-X4 finds a native / GPL step, R2-3 gates VT-1's keys and the fetch command, R2-4 gates VT-1's lock and VT-3's widget); `## First-gate experiments (L70)` = VT-X1–X9 as the rulings leave them plus every experiment the seats named that the fold takes (VT-X10 onward), each before the chunk it gates, each runnable in dev, on his devices in the owed device session, or in a scratch worktree; `## FINAL AMENDMENTS (for the desk's fold)` (above); `## SEAM` (above); `## Dissents, verbatim`; `## The bar` — L1 / L9 (every hop loud), L3, L15, L23, L31, L52, L57 each stated for the FINAL; `## OWNER ITEMS`. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-derive-r2-2026-09-26.md` — §0 Headline ≤5 lines (folds, converged or NEEDS ROUND 3, owner items, whether a Gemini finding held and `Gemini findings held: <n> of <n>`, Astra ruled, C-1's answer) → `## AUTHORIZATION` (every gate result) → `## DIGEST FOR THE DESK` (≤40 lines: what the FINAL changed from the DRAFT FINAL, item by item in one line each; which seats ruled round 2 (in full / not); how many Anthropic-seat claims were hub-checked, how many UNCHECKED; C-1; what goes to round 3 and why; the chunks, seats and RESTARTS under the FINAL; the hours under it, a GUESS marked as one) → `## Fold table` (`F-nn · item (R2-1 … R2-4, Q1–Q10, (a)–(g), Part-B item, C-1, WRONG FACT, FINAL amendment A–F, experiment) · seats that ruled it (<n> of 4, named) · whose wording (seat, or "DRAFT FINAL kept") · adopted verbatim? yes / no / not taken · Gemini finding held? yes / no / n/a · why (≤25 words, citing the hub's file-check row — AS<n> / GK<n> / GM<n> / FR<n> — where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`, or `RE-OPENS A RULING (<R-row>)`, and the cost of the mechanism NOT taken)`) → `## NEEDS ROUND 3` (per the rule above; `none` is valid) → `## OWNER ITEMS` (with why) → `## FOR DEJAN` (in HIS terms — what he hears after he speaks, on the phone and on the trading-PC browser, when the server voice is down or busy, and what the device session asks of him — ONLY the owner items, each ONE A/B with the seat's words quoted; then ONE approval: "approve the VT FINAL?" — asked only when `needs round 3 = 0`; otherwise "No decision for you yet: round 3 (the last) runs first, then one approval.") → `## SEAM` (a pointer to the FINAL's section + any UNSETTLED seam line) → `## Redactions` (count, and where) → `## READING` → `## L74` → `## ESCALATE` (every `RE-OPENS A RULING`, every V1-contract change a seat asked for, every precondition-to-build, every DOES-NOT-HOLD wording a seat still presses, every seat-proposed number without its measurement, every `OWNER ANSWER ON A DESIGN QUESTION`, every C-1 row named without a wording, every `ASK DESK`) → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `VOICE TTS DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both file paths, reads its own report first and continues from `## CONTINUE`; the FINAL, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the derive report, L71): `VOICE TTS DERIVED · FINAL · folds: <n> · verbatim: <n> · needs round 3: 0 · owner items: <n> · gemini findings held: <n> of <n> · astra: ruled · ESCALATE: <n>` — or, when `needs round 3` is above 0, `VOICE TTS DERIVED · DRAFT FINAL v2 · folds: <n> · verbatim: <n> · needs round 3: <n> · owner items: <n> · gemini findings held: <n> of <n> · astra: ruled · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes`, `owner items` = the items in `## FOR DEJAN` other than the one approval — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: the desk commits both files; `needs round 3: 0` → the FINAL is named, the desk folds `## FINAL AMENDMENTS` into `VOICE-v3-FINAL-2026-09-23.md` and brings him ONE approval ("approve the VT FINAL?") plus any owner item, then the VT-0 experiments, then the build per chunk (VT-0 first), ≥3 checkers, a later deploy (R23); `needs round 3: n` → the desk drafts round 3 (the LAST round, L39: the houses that hold a side on the listed items only), and whatever it does not settle goes to Dejan.
