MODEL: Opus 5.5 (`claude-opus-5-5`) — the DERIVE SEAT HIS 09-22 R109 names (`cto-2026-09-22.md` §4, 19:34 ET: "Make all Opus 5.5 for now" — STANDING until his next word: every Fable-type seat, the derive included, on `claude-opus-5-5`) and the desk's launch row for THIS tribunal records (`VOICE-TTS-PROPOSAL-2026-09-25.md` · `Fable seat: yes` · `derive seat: claude-opus-5-5`); this is the Anthropic seat's SECOND JOB, as on the bars, setups, float-handicap, stale-score, DRC, voice and overnight tribunals; you derive the version the round-1 rulings produce — a **DRAFT FINAL**, because Astra (L67's OpenAI design seat) has not sat (METER until Sat 2026-09-26 06:47 ET; it sits in round 2, `16` §5); you build, launch and commit nothing · SEAT: `voice-tts-derive-0925`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `16`; the Anthropic seat `17`). YOUR POSITION, STATED: you are the same SEAT that ruled round 1 blind (`17`), in a fresh session — you HOLD A SIDE, so the only recommendation you carry to him is the PROPOSAL's, attributed, beside which seats held each side (L37), and your own round-1 wordings pass the same filter as every house's. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-25/18-voice-tts-derive.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control voice-tts-derive-0925 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the `--model` value is the one the launch row names after `derive seat:`, already filled; the SAME seven READ-ONLY strings and three denies as `prompts/2026-09-23/12-draft-voice-v3.md`'s launch line, 09-20 R13 — nothing new) · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO `uv`, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic medium — honest estimate: the hub report `16` ≈ 18–28k tokens, the raw rulings of the houses that ruled ≈ 15–30k (17 items each), your seat's round-1 report ≈ 9–13k, the proposal ≈ 6.2k (24,961 B, read whole — the DRAFT FINAL is the proposal amended), its author's report ≈ 2.2k, the rulings ≈ 3.7k, LAWS.md ≈ 22k (89,839 B measured at drafting), the FINAL's §2 / §2.5 / §7 / §9 / first-gate / Seam ranges ≈ 3.6k, V1 code re-opened only where a fold turns on it ≈ 3–8k; the DRAFT FINAL ≈ 9–12k tokens of output plus the report ≈ 5–8k; peak context ≈ 100–130k — read the raw rulings item by item, not all at once, if the peak nears your window. It depends on no house string (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` in the report and continue with the safe default.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 / L9** (every fallback hop loud) **· L3** (one Synthesizer, one engine by config, one model-fetch command, one config loader, one status route) **· L15** (the new dependency: four gates; licence + native status are experiment rows until VT-X4 runs — L70; never folded as fact) **· L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L23** (local first; the fallback order is the houses') **· L27 · L28** (VT writes nothing to the vault) **· L31** (the VT identifiers are the tribunal's; V1's `faster-whisper` rename is a V1 BACKLOG ticket, `cto-2026-09-25.md` R25 reading (1) — never a VT chunk, never folded here) **· L32** (the DRAFT FINAL and your report are COMMITTED: no user data — no audio, no transcript, no stored `reply` value, no note of his) **· L35 · L36 · L37** (you derive; a SPLIT between seats is carried, never settled by vote; you hold a side) **· L42 · L43 / L66** (VT lands in a later deploy, R23) **· L45 · L52** (VT reaches no score) **· L57** (Q10) **· L67** (OWNER ITEMS are only HIS decisions — amended 2026-09-24: a derive that lists a design question as an owner item is returned, not delivered; `## FOR DEJAN` carries ONLY the proposal's two owner items unless a seat shows a third that passes the owner test; each derive states whether a Gemini finding held, 09-23 R97) **· L70 · L71 · L72** (the `## SEAM` with V1 and V4 is the document the later build prompt cites) **· L73 · L74**. CLAUDE.md's absolute boundary: nothing in the DRAFT FINAL reads, writes or infers from his trading platform; the trading PC is a browser tab at most.
2. THE ROUND-1 RULINGS, whole: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-2026-09-25.md` — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Anthropic-seat round-1 claims, file-checked` (the FC rows, or its one line saying the seat's report was not done at collate), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## Round 2 — Astra (not this run)`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/voice-tts/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line (the raw file is what "verbatim" means, but you never copy a user-data span out of it) · your seat's round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-fable-r1-2026-09-25.md` — including its `## Self-attack` (its `WITHDRAWN:` lines bind you: a withdrawn sentence is never folded).
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-PROPOSAL-2026-09-25.md` in full; its author's report `reports/voice-tts-propose-2026-09-25.md` (DIGEST, ESCALATE 1–3); HIS RULINGS, never re-opened: `cto-2026-09-25.md` §4 **R23** (06:20 ET — "Do A, We will build Kokoro next on a side lane so A and drafter for Kokoro, but it can land in later deploy": the server voice is ORDERED; the FINAL §2.5 condition "only if E5 fails" is SET ASIDE) and the desk's records **R21, R22, R25**; `cto-2026-09-24.md` **R107** (V1 = A); `cto-2026-09-23.md` **R95, R96, R97**; `cto-2026-09-22.md` **R109** — each by `grep -n "^| R<nn> "`. THE FINAL VT AMENDS: `/Users/cobalt/cobalt/docs/30 - Design/VOICE-v3-FINAL-2026-09-23.md` — §2 diagram (:63), §2.5 (:86), §7 (:167–185), §9 (:190–224), `## First-gate experiments` (:269 on; E5 :281, E10 :285), `## Seam` (:311–322), `## L52 and the bar` (:369) — anchors by `grep -n`, never trusted.
4. Only where a fold turns on it: the real file the hub's file-check cites (the V1 worktree `/Users/cobalt/cobalt-wt/voice-v1/`, code `d319e4f3` — `git -C /Users/cobalt/cobalt show d319e4f3:<path>` for committed bytes; main; the FINAL by the ranges above) — read to check, never to copy.
WRITES (named here, the only two): `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-DRAFT-FINAL-2026-09-25.md` and `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-derive-2026-09-25.md`. STOP LINE: `VOICE TTS DERIVED · DRAFT FINAL · …` (below).

DERIVE ROW: R109

# DERIVE THE DRAFT FINAL of VOICE SLICE VT (server-side local reply voice) from the proposal and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: OFF-LADDER (`cto-2026-09-25.md` R23), a SIDE LANE (L72). LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini as the optional fourth via hub `16`; Astra `METER`; the Anthropic seat via `17`) → **THIS = the round-1 derive → a DRAFT FINAL** (never named FINAL while Astra is unseated) → ROUND 2 with Astra (`16` §5) for Astra's first ruling and for what `## NEEDS ROUND 2` lists → the round-2 derive names the FINAL → build per chunk (VT-0 first) → ≥3 checkers → a later deploy. L1, L3, L9, L15, L23, L31, L32, L52, L57, L70 and L72 are the bar the DRAFT FINAL must meet. DO NOT STOP until the report's last line is `VOICE TTS DERIVED · DRAFT FINAL …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The line `DERIVE ROW: R109` above names the row of `cto-2026-09-22.md` `## §4 Rulings 2026-09-22` that pins the Fable-type seat, the derive included — filled at drafting (L19: any other change is a re-issue). Each its own Bash call: `grep -c -x -F "DERIVE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-25/18-voice-tts-derive.md"` (`-x` = whole line: only that line can match) — **1** → `FAILED: authorization — the DERIVE ROW is unfilled`, stop; **0** → read `<nn>` from that line (`109`) · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (exactly ONE row, carrying `Make all Opus 5.5 for now` and `derive` — else `FAILED: authorization mismatch — row R<nn> does not pin the derive seat`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **THE LAUNCH ROW FOR THIS TRIBUNAL** (the desk's row **`R__`** that launched `16`, FILL AT LAUNCH; and the desk's row **`R__`** that launches THIS derive, FILL AT LAUNCH — both recorded in `## AUTHORIZATION`, the grep decides): `grep -n -F "16-voice-tts-tribunal.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-25.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-26.md"` → exactly ONE `| R` LAUNCH row carrying, as literal text, `VOICE-TTS-PROPOSAL-2026-09-25.md`, `Fable seat: yes` and `derive seat: claude-opus-5-5` (a missing `cto-2026-09-26.md` is recorded, not fatal, when the row is in the other file; any literal missing, or two such rows → `FAILED: authorization mismatch — no single launch row names the seats for this tribunal`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"16-voice-tts-tribunal.md" -- "docs/40 - DevDocs/reports/cto-2026-09-25.md" "docs/40 - DevDocs/reports/cto-2026-09-26.md"` (EMPTY → `FAILED: authorization mismatch — the launch row is not committed`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — the launch row names <id>; this session runs <id>`, stop. **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-2026-09-25.md"` (the LAST NON-BLANK line must start `VOICE TTS TRIBUNAL DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE TTS TRIBUNAL DONE" -- "docs/40 - DevDocs/reports/voice-tts-tribunal-2026-09-25.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-tribunal-fable-r1-2026-09-25.md"` (must start `VOICE TTS FABLE R1 DONE ` — else `FAILED: refused — Anthropic-seat round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE TTS FABLE R1 DONE" -- "docs/40 - DevDocs/reports/voice-tts-tribunal-fable-r1-2026-09-25.md"` (EMPTY → `FAILED: refused — the Anthropic-seat round-1 stop line is not committed on main`) · **THE FLOOR — GROK MUST HAVE RULED (L67: the Anthropic seat + Grok):** the hub's stop line's `grok:` field must carry a `TRIBUNAL R1:` line → else `FAILED: floor — grok did not rule round 1; the desk relaunches 16` (your seat alone never settles an item: you would be deriving from your own ruling, L37) · **ASTRA:** the hub's `astra:` field is `METER` or `SKIPPED — probe UP, recorded` — RECORDED, not a refusal: derive from the seats that ruled, mark every question only Astra would have answered `ASTRA PENDING`, mark the DRAFT FINAL's header `DRAFT FINAL — ASTRA PENDING (round 2 not before 2026-09-26 06:47 ET)`, and name in §0 that Astra rules round 2 on this DRAFT FINAL (`16` §5). Gemini not having ruled does NOT refuse the derive (it is the optional fourth, R97): the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-23/12-draft-voice-v3.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. PATTERN RULE (`topics/cto-desk.md` 09-25 third-desk lesson (1)): no backtick, no `\|`, no alternation inside one pattern, no `$` inside a double-quoted argument. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

THE RULES OF THE DERIVE — `25`'s rules (`prompts/2026-09-24/25-drc-overnight-tribunal-derive.md`; the bars tribunal's lesson, `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding, with this tribunal's substitutions:
- **Every split ADOPTS a house's wording VERBATIM.** An `ADOPT WITH` text goes into the DRAFT FINAL word for word, or it is not taken. You may choose BETWEEN seats' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check, or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **An Anthropic-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's `## Anthropic-seat round-1 claims, file-checked` has its FC rows, they are your filter for your seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the seat's report as not done at collate, every seat claim is UNCHECKED: you may take a seat wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure, and where you cannot settle it by reading it is an experiment, not a fold. Where your seat's wording competes with a house's wording that is equally correct, the house's is taken — you never prefer your own text.
- **His R23 (the order; §2.5's "only if E5 fails" set aside), R107 (V1 = A) and the seat rulings are NEVER re-opened.** A wording that would drop VT, make it conditional on E5 again, pull it into the 09-25 deploy set, pull V1's `faster-whisper` rename or V1's A1 fix into a VT chunk, or change V1's CHECKED contract as a precondition is NOT folded: it is carried in `## Dissents, verbatim` and named in `## ESCALATE` as `RE-OPENS A RULING (<R-row>)` — his to rule again if he chooses, never yours. A seat's ruling that L31 binds V1's identifiers is RECORDED as a finding for the V1 BACKLOG ticket (R25 reading (1)), never a VT fold.
- **Prefer the simpler mechanism, and price it honestly** (09-20 tribunal lesson). Where two seats offer different mechanisms for one item, take the smaller one when both meet the laws above, and record the other's cost and trade-off in the fold row (size, files, which chunk, hours — a GUESS marked as one) — unless the two disagree on whether a mechanism is CORRECT, which is `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in the DRAFT FINAL's experiment list beside the proposal's VT-X1–X6, placed before the chunk it gates, with the result that would change the design — never an argument in the text, never a defect. The new dependency's licence and native status, the ONNX load, browser autoplay on his devices, the first-audio latency, the resident's RSS beside LM Studio and the output's determinism are experiments unless a run already settled them (none has, at drafting).
- **Invent nothing.** The DRAFT FINAL adds no mechanism, number, rule, key, route, probe, file or chunk that neither the proposal nor a seat's ruling contains. A gap no ruling fills stays a named gap (an experiment or a round-2 item) — never your filler. Every engine tunable (`tts_timeout_s`, `tts_max_chars`, `tts_rss_amber_mb`, `tts_speed`) keeps NO number until its experiment sets it.
- **OWNER ITEMS are only HIS decisions (L67 as amended 2026-09-24).** `## FOR DEJAN` carries ONLY the proposal's two owner items — (1) voice and speed, (2) on the phone, server voice or device voice first — each as ONE A/B WITH the proposal's recommendation, attributed ("the proposal recommends B"), and the seats that held each side; a seat that ruled one of the two is NOT his (a house can settle it from measurement) is folded as the houses' mechanism and the item leaves `## FOR DEJAN`, named in the fold table; a THIRD item enters `## FOR DEJAN` only if a seat shows it passes the owner test (his money, his data, his laws, his own trading judgement or his taste in what he hears — AND no house can decide it) with the seat's words quoted. Nothing in the DRAFT FINAL may make a chunk's BUILD depend on an owner item: OWNER ITEM 1 is heard in the device session he already owes (R25 reading (3)); tests use a constructed config (L69).
- **The author's ESCALATE 1–3** (the L31 shape on V1; `voice.yaml` absent from `com.cobalt.aset`'s `reads:`; VT-X3 needing his hands): the DRAFT FINAL places each exactly as the desk's R25 readings and the rulings leave it — a V1 BACKLOG pointer, a SEAM line with its UNPROVEN mark (L70) until `cobalt jobs restarts` runs, a device-session row — never re-decided by you.
- **THE FINAL AMENDMENTS FOR THE DESK'S FOLD.** The proposal's §8 A–F (the §2 diagram line, §2.5 whole, §7 rows, §9 row, the `Before VT` experiment block, the L52 line) are the text the desk folds into `VOICE-v3-FINAL-2026-09-23.md` — the desk's fold, not yours (you never write the FINAL). The DRAFT FINAL carries them in `## FINAL AMENDMENTS (for the desk's fold)` AS THE RULINGS LEAVE THEM: each block verbatim from the proposal, or replaced whole by a seat's verbatim `ADOPT WITH` wording, tagged with its fold row; the §2.5 block keeps his R23 sentence. A seat that says an amendment changes a FINAL clause VT does not need → that amendment is dropped or narrowed only by that seat's verbatim wording, never yours.
- **THE SEAM (L72).** The DRAFT FINAL's `## SEAM` = the proposal's `## 10. SEAM` as the rulings leave it: every V1 file VT touches (`voice/web.py`, `voice/config.py`, `configs/cobalt/voice.yaml`, `models.py` docstring only, `store.py` read-only, `configs/cobalt/jobs.yaml` `reads:`, `pyproject.toml` / `uv.lock`, the `POST_ALLOWLIST` guard test), the V4 seam (`heartbeat/probes.py`; the ONE model-fetch command — first to build creates it), V1's A1 fix (no seam), DRC D2 / D3 (nothing) — each line citing its `file:line` at `d319e4f3`, and "VT branches off `main` AFTER V1 merges" kept. The later VT build prompt cites this section; a seam a seat can only state as a READING is named UNSETTLED (L72 P-b), never smoothed.
- **Dissents carried VERBATIM**: a seat's REJECT or `DO NOT BUILD` that the DRAFT FINAL does not follow goes into `## Dissents, verbatim` word for word, with its seat and item.
- **Gemini's findings (09-23 R97):** for each item, the fold row says whether a Gemini finding was folded and whether its supporting claim HELD in the hub's file-check; §0 and the stop line count `Gemini findings held: <n> of <n>` and state plainly whether a Gemini finding held — his evidence for keeping or removing the seat.
- **No user data in either file** (L32): the packet had none after the hub's REDACTION; if a seat's text quotes a value of his anyway, it is taken with that span replaced by `[user data: <file:line>]`; a seat's proposed VALUE for an engine tunable without its measurement is recorded as `<key>: value proposed by <seat> — <file>:<line>` (a pointer, never folded); count every redaction.

WRITE (Write tool, TWO new files, these names WHATEVER the date of the run):
- `/Users/cobalt/cobalt/docs/30 - Design/VOICE-TTS-DRAFT-FINAL-2026-09-25.md` — the proposal, whole (§1–§11, OWNER ITEMS, L52 / L31), with each amended section REPLACED in place and tagged `[F-nn]` at the amended paragraph (so a reader can diff the DRAFT FINAL against the proposal section by section); a header naming it `DRAFT FINAL — ASTRA PENDING (round 2 not before 2026-09-26 06:47 ET)`, the proposal, R23 / R25 / R107 / R95–R97 / R109, the rulings of every seat that ruled (paths; Astra `METER — ASTRA PENDING`; the Anthropic seat's status from R109 and the launch row) and the fold table's path; `## First-gate experiments (L70)` = VT-X1–X6 as the rulings leave them, plus every experiment the seats named that the fold takes, each before the chunk it gates, each runnable in dev, on his devices in the owed device session, or in a scratch worktree; `## FINAL AMENDMENTS (for the desk's fold)` (above); `## SEAM` (above); `## Dissents, verbatim`; `## The bar` — L1 / L9 (every hop loud), L3, L15, L23, L31, L52, L57 each stated for the DRAFT FINAL; `## OWNER ITEMS` (the two, as `## FOR DEJAN` states them). Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tts-derive-2026-09-25.md` — §0 Headline ≤5 lines (folds, round 2 or not, owner items, Astra pending, whether a Gemini finding held and `Gemini findings held: <n> of <n>`) → `## DIGEST FOR THE DESK` (≤40 lines: what the DRAFT FINAL changed from the proposal, item by item in one line each; which seats ruled round 1 (in full / not / Astra METER); how many Anthropic-seat claims were hub-checked, how many UNCHECKED; what goes to round 2 and why — Astra's full first ruling always, plus `## NEEDS ROUND 2`; the chunks, seats and RESTARTS under the DRAFT FINAL; the hours under it, a GUESS marked as one) → `## Fold table` (`F-nn · item (Q1–Q10, (a)–(g), WRONG FACTS, FINAL amendment A–F) · seats that ruled it (<n> of 4, named) · whose wording (seat, or "proposal kept") · adopted verbatim? yes / no / not taken · Gemini finding held? yes / no / n/a · why (≤25 words, citing the hub's file-check row — `Checked` row or `FC<n>` — where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`, or `RE-OPENS A RULING (<R-row>)`, and the cost of the mechanism NOT taken)`) → `## NEEDS ROUND 2` (ONLY items where a seat said `DO NOT BUILD`, or two seats disagree on whether a mechanism is CORRECT; each with both positions quoted and the question round 2 must answer; Astra rules round 2 regardless — an EMPTY list still means round 2 runs for Astra, `16` §5) → `## OWNER ITEMS` (the two, and any third that passed the test, with why) → `## FOR DEJAN` (in HIS terms — what he hears after he speaks, on the phone and on the trading-PC browser, when the server voice is down, and what the device session asks of him — ONLY the owner items: each ONE A/B, the proposal's recommendation attributed, the seats that hold each side; then ONE approval to come AFTER round 2: "approve the VT FINAL?" — not asked now) → `## SEAM` (a pointer to the DRAFT FINAL's section + any UNSETTLED seam line) → `## Redactions` (count, and where) → `## READING` → `## L74` → `## ESCALATE` (every `RE-OPENS A RULING`, every V1-contract change a seat asked for, every precondition-to-build, every DOES-NOT-HOLD wording a seat still presses, every seat-proposed number without its measurement, every `ASK DESK`) → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `VOICE TTS DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both file paths, reads its own report first and continues from `## CONTINUE`; the DRAFT FINAL, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the derive report, L71): `VOICE TTS DERIVED · DRAFT FINAL · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · gemini findings held: <n> of <n> · astra: round 2 after 2026-09-26 06:47 · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes`, `owner items` = the items in `## FOR DEJAN` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: the desk commits both files; the round-2 hub is drafted on `16` §5 (Astra seated, the probe a fail-closed PREFLIGHT gate, launched not before 2026-09-26 06:47 ET; the other seats on `## NEEDS ROUND 2` only); the round-2 derive names the FINAL, the desk folds `## FINAL AMENDMENTS` into `VOICE-v3-FINAL-2026-09-23.md` and brings him the owner items + ONE approval; then the VT-0 experiments, then the build per chunk, ≥3 checkers, a later deploy (R23).
