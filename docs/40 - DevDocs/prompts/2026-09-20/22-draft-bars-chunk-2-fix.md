MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `bars-2-fix-draft-0920`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/22-draft-bars-chunk-2-fix.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control bars-2-fix-draft-0920 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `09`, `13`, `16` and `19-draft-bars-1a-fix.md`, a subset of the approved line of `06-bars-tribunal-r3.md`, R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the FIX ROUND for bars chunk 2, its ROUND-2 check, and the ONE cross-branch step chunk 1a owes because of it. LADDER: `OFF-LADDER — ruled cto-2026-09-18 R17 / cto-2026-09-19 R10, R15 / cto-2026-09-20 R9`. LAW STEP (L67): design CLOSED → chunk E checked 3 of 3 → chunk 2 BUILT (R25, `BARS CHUNK 2 BUILT 47ef9af …`) → CHECKED round 1 at 20:11 ET by **2 of 3** houses (astra: METER): grok `BUILD STANDS EXCEPT` three items, deploy prompt NO · gemini `FIX FIRST` the child-name shape, deploy prompt NO · hub file-check 36 HOLD / 7 DO NOT HOLD, ESCALATE 13 (`reports/bars-chunk-2-check-2026-09-20.md`) → DESK CALL (`cto-2026-09-20.md` §35): chunk 2 is NOT ready for a deploy prompt; **THIS = drafting its fix build and the round-2 check that reads ONLY the fix**. This is the chunk on the LIVE RADAR WRITE PATH: FINAL §8 asks THREE checkers for it, so the round-2 check you draft REQUIRES astra UP (probe says METER → `FAILED PREFLIGHT: astra on METER — chunk 2's round 2 needs three houses`, nothing launched; the desk relaunches when the meter is back). DO NOT STOP until the report's last line is `BARS CHUNK 2 FIX PROMPTS DRAFTED …` or `FAILED …`.

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** · **L3** one path (ONE child-name shape in the repo, generated in ONE place) · **L19** · **L33** · **L35** a house's claim counts only where the check hub's file-check says HOLDS · **L36** · **L37** · **L46/L60** each fix commits on its OWN branch in its OWN existing worktree · **L52** nothing reaches a card · **L62/L63** · **L67** ≤3 rounds a house; round 2 reads the FOLD only · **L68** the two branches are SIBLINGS that will land together: the seam between them is proven on the stacked tree, but a KNOWN mismatch is fixed before the gate, not discovered by it · **L70** a claim nobody ran is UNPROVEN, never fixed blind · **L71** · **L73** · **L74**.
2. THE SHAPE TO COPY, byte for byte wherever the same sentence serves: the drafter prompt `prompts/2026-09-20/19-draft-bars-1a-fix.md` (its CLASSIFY rules and classes bind you UNCHANGED: FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM; you widen NOTHING) and what it produced tonight: `20-bars-chunk-1a-fix.md` (ran clean 19:36–19:48, `BARS CHUNK 1A FIX BUILT 793f452 | on 589d40f | …` — copy its AUTHORIZATION, UNATTENDED RULES, RECOVERY RULE, THE MUTATION PROOF, report-append and stop-line discipline), `21-bars-chunk-1a-check-r2.md` (the round-2 shape) and your predecessor's report `reports/bars-1a-fix-draft-2026-09-20.md`.
3. THE ROUND-1 RESULT, whole: `reports/bars-chunk-2-check-2026-09-20.md` — §0, `## Inertness`, `## The composition note`, `## The thirteen poller tests`, `## Checked against the branch`, the FINAL `## ESCALATE` (13 items). The houses' full texts: `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/chunk-2-check/` (`grok-check.md`, `gemini-check.md`).
4. WHAT WAS BUILT AND WHAT IT WAS TOLD: `prompts/2026-09-20/11-bars-chunk-2-build.md` (the contract — its step 5, the thirteen named poller tests, the HARD BOUNDARY, *never weaken an assertion*), the worktree `/Users/cobalt/cobalt-wt/bars-chunk-2` (branch `bars/chunk-2-0920`, `e15d03e..47ef9af` + report commits; Read tool + `git -C /Users/cobalt/cobalt log -p e15d03e..bars/chunk-2-0920`), its report there, the spec `docs/30 - Design/BARS-LIFECYCLE-FINAL-2026-09-20.md` §3.1–§3.4, §8 row `2`, main's `src/cobalt/radar/poller.py` (the inertness baseline), and his rulings `reports/cto-2026-09-20.md` §4 R9, R19–R22, R25, **R45** (chunk E's ESCALATE 6 is closed by chunk 4 — not yours, do not reopen it).
5. DATE GATE (his R23) on the check you draft: `date` is 2026-09-22 or later → FAILED, first PREFLIGHT row.

THE FINDINGS THE DESK READ AS MOST SERIOUS — classify them from the FILES like every other row; the desk's reading is a pointer, not a verdict: **(a) INERTNESS IS NOT BYTE FOR BYTE ON A FAILURE PATH** (check ESCALATE 4: the coverage refresh at `poller.py:198` is not gated on `bounds.partitioned`; a raising first bounds read or refresh aborts a cycle where main continues, `poller.py:136-142, 198-200`) — this is the one claim the whole chunk rests on, on production's 04:00 write path; if the file-check HOLDS it is FIX, with a test that runs MAIN'S OWN poller beside the built one on the failure fakes (the builder's existing inertness harness) and fails on any difference in calls, order or `PollResult`. **(b) WRONG-REASON TESTS the hub's file-check HOLDS** (tests 1 and 9; tests 5 / 5b abort at `poller.py:144-146` before any fetch, so P-closed's second clause and the row order are never reached) — FIX = the test is made to reach what its name says, by MUTATION PROOF. **(c) `bars_coverage` HAS NO CALLER** (`heartbeat/runner.py:131-148` untouched) — read prompt 11's step for the probe: if it told the builder to WIRE the probe, FIX; if it told it to build the probe only, OUT OF SCOPE with the sentence quoted. **(d) the narrated diff has 11 hunks, the committed diff 7** — a report correction (appended, never an edit in place). **(e) THE CHILD-NAME SHAPE** — see the next block.

THE CROSS-BRANCH SEAM (check ESCALATE 1–2, both houses, hub-confirmed from both branches): chunk 2 GENERATES `bars_p_<YYYYMMDD>` (the ET Monday's date, `partitions.py:86-98`, asserted `bars_p_20261026`); chunk 1a's placement pattern ADMITS only `bars_p_<YYYY>w<WW>` (`placement.py` `_PARTITION_CHILD`, branch tip `8dddf2b`) and recorded that shape as a `READING:` made blind ("the child name shape is not readable from `main`"). FIRST establish from the files whether FINAL, v3 (`scratch/tribunal-bars-0920/r3/DERIVED-v3.md.part*`) or any ruling R19–R22 FIXES a shape (quote `file:line`). If one does → the branch that departs from it changes. If NONE does (the chunk-2 builder's own READING says no document names the children) → DESK READING, yours to confirm or to ESCALATE against with a reason: **the generator's shape stands and chunk 1a's pattern conforms to it** — a Monday date is unambiguous, while a year+week label has two defensible numberings at a year boundary (ISO week-year vs calendar year) and would need its own rule; the generator is the artifact, the pattern is only its admission rule (L3: one shape, defined where it is generated). Then the 1a side is ONE step on the 1a branch, drafted as its own small prompt (below); chunk 2's fix changes NO name.

CLASSIFY FIRST (in your report, one row per round-1 finding — every item of the check's final `## ESCALATE`, every `DO NOT HOLD`-adjacent house claim, grok's three EXCEPT items, gemini's FIX FIRST, the houses' OWNER ITEMs): `finding · house(s) · the check hub's file-check verdict · YOUR file:line read · class`. OWNER ITEMs the houses already named (composition (a) "one ticker one cycle late" is conditional · ANY-hole vs AMBER · the named range for a later card surface · the builder's heredoc) are copied verbatim with what he would be ruling; you decide none of them.

WRITE (Write tool, new files only):
- `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/23-bars-chunk-2-fix.md` — Opus 5 hub `bars-chunk-2-fix-0920`, ONE session, OFFLINE exactly as prompt 11. TWO bare commands, no `worktree add`: `cd /Users/cobalt/cobalt-wt/bars-chunk-2` then the `claude --bg …` line carrying **exactly the 16 allow strings + 3 denies + `--add-dir` triplet of prompt 11's launch line (= prompt 10's = prompt 20's), byte for byte — NO NEW RULE STRING**. Body = prompt 20's shape: PREFLIGHT (branch `bars/chunk-2-0920`, clean, tip read and recorded as `<round-1 tip>`, `.env` absent), BASELINE = chunk 2's own `2368/0 (365 skipped)`, ONE step per FIX row (failing test or MUTATION PROOF first, then the change, one commit each, explicit-path `git add`), the inertness re-proof against main's own poller on EVERY path (clean, fetch failure, write failure, RTH stale, a raising bounds read), `upsert_bars` still byte-identical to main, the HARD BOUNDARY four re-checked, empty diffs for `src/cobalt/cards`, `src/cobalt/radar/evaluate.py`, scoring; report APPENDED as `# FIX ROUND 1` to the chunk-2 report, nothing above rewritten; AUTHORIZATION greps `cto-2026-09-20.md` for the committed desk call (`-S"chunk 2 is NOT ready for a deploy prompt"`) and a launch row `R__` the desk fills. STOP LINE: `BARS CHUNK 2 FIX BUILT <tip> | on <round-1 tip> | offline <p>/<f> (<s> skipped; round-1 2368/0) | fixed: <X ids> | not fixed (listed): <n> | inert on an unpartitioned parent, every path: proven | upsert_bars byte-identical to main: proven | card/scoring paths untouched: empty diff | db: OWED — <n> requires_db tests written, never run | ESCALATE: <n>` — or `FAILED: <step> — <reason>`.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/24-bars-chunk-2-check-r2.md` — Sonnet 5 hub `bars-chunk-2-check-r2-0920`, `21-bars-chunk-1a-check-r2.md`'s shape with chunk 2's paths, folder `scratch/tribunal-bars-0920/chunk-2-check-r2/`, the 14 + 3 strings of `08` byte for byte, DATE GATE, shas read at run time from the fix stop line, **astra REQUIRED** (above); astra did NOT read round 1, so ITS packet sentence also names round 1's folder `../chunk-2-check/` (QUESTIONS-CHECK.md, THE-POLLER-DIFF.md, `built/`) and asks it for round 1's inertness and composition answers on the FIXED code as well as the round-2 questions — grok and gemini answer round 2 only. Stop line `BARS CHUNK 2 CHECK R2 DONE · grok: … · gemini: … · astra: … · houses that checked: <n> of 3 · FIX rows CLOSED: <n> of <n> · inert on every path: <n> of <n> YES · new defects: <n> · ready for a deploy prompt: <n> of <n> · ESCALATE: <n>`.
- ONLY IF the seam lands on the 1a side: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/25-bars-chunk-1a-fix-1b.md` — Opus 5 hub `bars-chunk-1a-fix-1b-0920`, prompt 20's launch line with only the prompt path and the remote-control name changed (same 16 + 3 strings), worktree `~/cobalt-wt/bars-chunk-1a`, ONE step X14: the placement pattern admits EXACTLY the generator's shape (complete ASCII match, as X9 made it; the old `w<WW>` shape no longer admitted — L3), tests first (the near-miss list re-pointed at the new shape; a name chunk 2's test asserts, e.g. `bars_p_20261026`, is admitted; `bars_p_2026w44` raises), DevDoc `placement.md` one sentence, report APPENDED as `# FIX ROUND 1b` under round 1's section, and **its stop line keeps prompt 20's exact shape with `on 589d40f`** (`BARS CHUNK 1A FIX BUILT <1b tip> | on 589d40f | …`) so that `21-bars-chunk-1a-check-r2.md`, UNCHANGED, reads the whole fix `589d40f..<1b tip>` in one round 2. Say in your report whether prompt 21 needs any re-issue for this (L19: a change = a whole re-issue under a new number, never an edit) — the desk expects NONE.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/bars-2-fix-draft-2026-09-20.md`: §0 Headline ≤5 lines → THE SEAM (what the documents fix, the side that changes, why) → the CLASSIFY table → per prompt: file, size, seat → RULE PROOF (`grep -c -F` = 1 per string against prompt 11 / prompt 10 / `08`; `NEW strings: NONE` or the list) → `READING:` list → `## ESCALATE` (every OWNER ITEM verbatim, every `ASK DESK`) → `## CONTINUE` → last line.
STOP LINE (last non-blank line): `BARS CHUNK 2 FIX PROMPTS DRAFTED · prompts: <2|3> · new rule strings: <n> · seam: <1a conforms|chunk 2 conforms|OWNER ITEM> · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · READING: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect.
