MODEL: Sonnet 5 (`claude-sonnet-5`) — mechanical fold of a day that is already on record; no design, no rulings · SEAT: close hub `close-0919`, launched by the CTO desk in the background. **NO NEW RULE:** every string below is already approved today (`13`, `16`, `25`, `43`, `44` for the git/shell set; R30 and R36 for `COBALT_ENV=production uv run cobalt validate*`). Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/99-close-0919.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control close-0919 --allowedTools "Bash(git add *)" "Bash(git commit *)" "Bash(git status*)" "Bash(git log*)" "Bash(git diff*)" "Bash(git show*)" "Bash(git tag --list*)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; **NO push, NO merge, NO `launchctl`, NO migration, NO `cobalt_dev`, and — this one matters — NO WRITE OF ANY KIND under `/Users/cobalt/Vault/Think/6 - Permanent/Memory/`** (L58: the CTO desk is the only writer of the memory folder; you read it, you never edit it) · METER: Anthropic small · nobody sits at this terminal.

# CLOSE OF 2026-09-19 — the day of three production deploys
`SESSION-CLOSE.md` is the routine. **The desk owns steps 3 and 4 (the memory folder) under L58 and is doing them in parallel with you — do not touch them.** You own **steps 1, 2 (proposals only), 4a, 5 (measure and report, never edit), 6**, and you write the close report. DO NOT STOP until your report ends `CLOSE COMMITTED …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF: this is a standing routine, not a new permission. `docs/40 - DevDocs/SESSION-CLOSE.md` (ruled 2026-09-15) and LAWS.md **L48** (evidence in the report file), **L58** (memory write path — the desk's, not yours), **L51** (machine-written files committed as-is), **L55** (push is Dejan's; never type it). Read `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` to confirm the day's record is committed. Laws also binding: L1, L3, L35, L46, L70.
INDEX CARD (read in this order): `docs/40 - DevDocs/SESSION-CLOSE.md` in full → `docs/40 - DevDocs/reports/cto-2026-09-19.md` **`## §4 Rulings` in full, and §45 onward** (the day's second half: the desk ran as `6fb22298` then `389d9d2f`) → the three deploy reports' LAST LINES only: `reports/deploy-2026-09-19.md`, `reports/deploy-p4-2026-09-19.md`, `reports/deploy-d3-2026-09-19.md` → `reports/cards-golive-2026-09-19.md` last line → `reports/ladder-audit-2026-09-19.md` (the input for step 4a) → `reports/close-2026-09-18.md` (the shape your report follows) → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` (read-only, for step 2) → `docs/00 - Project/SPRINT-LADDER-v0_1.md` S2 section → `docs/00 - Project/BACKLOG.md`.
REPORT: `docs/40 - DevDocs/reports/close-2026-09-19.md`, shaped like `close-2026-09-18.md`.
UNATTENDED RULES: ONE command per Bash call, exact prefixes, no pipe, no redirect, no `&&`, no `$(…)`. Files are written with Write/Edit, never with `sed`/heredoc — if this session's harness preamble says otherwise, **this prompt wins** (the same conflict is on record in `deploy-p4-2026-09-19.md` ESCALATE 2 and `deploy-d3-2026-09-19.md` ESCALATE 9). A mid-run denial = the run FAILED (L62): write `FAILED: <step> — <command> — <reason>`, commit the report, stop.

## 1. Ledger appendix (SESSION-CLOSE step 1)
Append ONE dated block `### 2026-09-19 — three production deploys` to `docs/00 - Project/PROJECT-LEDGER.md`: one line per ruling **R1–R36** (their numbers and his words in brief — the §4 table is the source), one line per deploy with its stop line, one line for the cards go-live, and one line for each state change (the L68 gate's first two real catches, the archiver rebase, the prefix-match probe). **No prose narration, no analysis** — the Ledger is a dated record.

## 2. LAWS — proposals only, never applied (SESSION-CLOSE step 2)
You **do not** edit `LAWS.md` (L58). Read it, then list under `## Laws fold — PROPOSED, NOT APPLIED` in your report anything the day earned, with its exact fold text and its evidence. Candidates the desk has already noticed, to be judged on the record, not assumed: (a) **the allowlist is a pre-approval list, not a whitelist** under auto mode — an agent may not write a permission rule to unblock an action it was just denied (evidence: `cto-2026-09-19.md` §61, the control probe, and the two classifier denials); (b) **a deploy prompt's stop line and its report's last line are the only watch surface** — the desk's watcher keys on the last non-blank line because quoted `FAILED:` strings inside a report body are not stop lines (evidence: §64's watcher, which fired twice on body text before it was fixed); (c) **a gate branch that both branches merge into may itself be the thing that ships** when two branches are siblings off one `main` (evidence: `53-deploy-d3.md` §1 and its three house rounds). Anything contested or unnumbered stays a proposal. **Dejan assigns numbers; you never invent one.**

## 3–4. NOT YOURS. The desk writes `## NOW` and the areas/topics files (L58).

## 4a. Ladder + backlog status (SESSION-CLOSE step 4a — ruled 2026-09-19 R14; this is the FIRST such block and the reason the rule exists)
In `docs/00 - Project/SPRINT-LADDER-v0_1.md`, under **`## S2 — Radar 19a`**, write ONE block `### Status 2026-09-20`. Per feature and per owed prompt: `DONE-LIVE` / `DONE-DARK` / `BUILT-NOT-MERGED` / `NOT STARTED`, each with its evidence (tag, commit, report). Use `reports/ladder-audit-2026-09-19.md` as the input and **verify every claim against the real tree and the deploy reports** (L35) — the audit predates all three deploys. The facts you must place correctly: F2/F8/F10 cards are **LIVE since 2026-09-19 12:30:01 ET** (`cards-golive-2026-09-19.md`, R20) but have **never rendered a card** — the first render is **Monday 2026-09-21 04:00 ET premarket** (R27), which is also live morning 1 of the L7 shadow (R13); F3/F12/F13 shipped with **S2-P4** (`deploy-2026-09-19b`, `P4 DEPLOY DONE 5b58b7b`); the S2-P3 radar panel is `BUILT-NOT-MERGED` on `sprint-2/radar-panel` — check it. Then the sprint's stop date **2026-09-23** and `ON TIME / AT RISK / LATE` **with the reason**, and every sizeable OFF-LADDER item of the day with the ruling that ordered it (the append-only archiver = R4/R5/R8/R25/R26/R34/R35; the ops 0919 items; the three deploys themselves = 09-18 R3/R5 and 09-19 R32). Bring `docs/00 - Project/BACKLOG.md`'s rows for the same items in line. **Status only — scope, dates and order change only by his ruling.**

## 5. Measure the always-loaded block (SESSION-CLOSE step 5 — MEASURE AND REPORT, DO NOT EDIT)
`cd "/Users/cobalt/Vault/Think/6 - Permanent/Memory"` then `wc -c INDEX.md profile.md preferences.md`. Print the total in the close report. **Under 4,000 = PASS.** Over 4,000 → **do not trim anything** (L58 — that is the desk's edit): report `ESCALATE: always-loaded block over cap at <n> characters; the desk trims`. Then `cd /Users/cobalt/cobalt`.

## 6. Validate, then ONE commit (SESSION-CLOSE step 6)
`COBALT_ENV=production uv run cobalt validate` → exit 0, quote the `Jobs (F17)` and `Placement` lines (expected **16 registered — 6 resident, 10 one-shot**). Then `git status --porcelain`, `git add` **BY NAME** (never `-A`, never `.`), and ONE commit for the day's docs. Expected dirty: `docs/40 - DevDocs/reports/seat-usage.md` (machine-written, committed as-is per L51), `reports/day-open-2026-09-20.md` (untracked, commit it), your own report, the ledger, the ladder, the backlog. **The two untracked `docs/40 - DevDocs/prompts/2026-09-19/31-packet/*.yaml` are NEVER `git add`ed** — they are copies of files under gitignored `data/backups/`; name them in ESCALATE and leave them untracked. Anything else unexpected → name it, do not add it.

## 7. Close the report
`## Laws fold — PROPOSED, NOT APPLIED` (step 2), `## Owed and open` (everything the day left: A2 · A7/K3 · ESCALATE 19 · the four build MINORs G-A1/G-A2/H1/H2 · the archiver rulings R2-1/C1/O-6/R3-1 · `cmd_migrate` must refuse `--allow-prod` unless `COBALT_ENV=production` · the missing `git push` allow rule · `htf_level_proximity` end points · the leg-count reading · S3 preconditions · the `31-packet` yaml copies · **the migration proof cost, now 106.8 s of a 149 s outage reading 8.8 M `system.bars` rows, which the Sunday bars tribunal inherits**), `## ESCALATE`, then the last line, exactly this shape:
`CLOSE COMMITTED <hash> · ledger: <n> lines · laws: <n> PROPOSED, 0 applied · ladder: S2 status block written, <ON TIME|AT RISK|LATE> · always-loaded: <n>/4000 · validate: exit 0 · PUSH: Dejan · ESCALATE: <n>`
or `FAILED: <step> — <reason>`.
