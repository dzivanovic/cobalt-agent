MODEL: Opus 5 (`claude-opus-5`) — historical forensics across three memory concepts, four data sources and seven weeks; a wrong date here produces a wrong story and the story is the deliverable (L29: forensics) · SEAT: research hub `memory-history-0920`, launched by the CTO desk in the background, two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/03-memory-history.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control memory-history-0920 --allowedTools "Bash(git add *)" "Bash(git commit *)" "Bash(git status*)" "Bash(git log*)" "Bash(git diff*)" "Bash(git show*)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(find *)" "Bash(du *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · **ONE NEW RULE CLASS, both read-only and both approved by Dejan 2026-09-20 for this job: `Bash(find *)` and `Bash(du *)`** (inventory without opening files) · SESSION: fresh · auto mode on; never `bypassPermissions`; **READ-ONLY EVERYWHERE THAT MATTERS: you write exactly one report file and commit it. No write under `6 - Permanent/Memory/` (L58), no production write, no `cobalt_dev`, no migration, no merge, no push, no deletion of anything, ever** · METER: Anthropic small · nobody sits at this terminal.

# THE MEMORY HISTORY — what we said we would build, what we built, and when each happened
Ordered by Dejan 2026-09-20 (`cto-2026-09-20.md` §4 R11) after the desk found that `CLAUDE.md:95` states a five-pillar Postgres memory layer as an environment FACT while production holds **17 tables in 4 databases and none of the five**, with pgvector installed and wired to nothing. His framing, and it is the brief: *"the biggest issue here is not disc clutter, but memory that all houses rely on that is forgetful."* And: *"we designed our memory not to ever forget, but it seems like it never actually adds any information to it other than just simple stuff, and on close of CTO desk."*
**THIS IS RESEARCH. You decide nothing, propose no law, build nothing, delete nothing.** He has explicitly refused a knee-jerk sitting; the point is to know the true history first. DO NOT STOP until the report ends `MEMORY HISTORY READY …` or `FAILED …`.

## THE THREE THREADS HE NAMED — trace each one separately, then cross-reference them
**THREAD 1 — HIPPOCAMPUS / GRAPH / VECTOR / THE FIVE PILLARS.** His recollection to test: it is a Gemini-era leftover; early in this project we either changed direction, adopted parts of it, or said we would build something that never got built. **The Obsidian memory was supposed to have a SIBLING in Hippocampus, reachable by graph and vector.** Also promised in that thread: **before-and-after state kept for any file change or update**. Known anchors to start from, not to stop at: `docs/20 - Assessment/TRIAGE.md` **§2.1 "Memory / Hippocampus"**, which dispositions `PostgresMemory` core as **KEEP-CONCEPT / REBUILD** — find every other row of that section and what became of each; `src/cobalt_agent/memory/` (136 KB: `base.py`, `core.py`, `postgres.py`) — **read it and say what it actually does, whether anything still CALLS it, and whether the running production agent touches it at all**; `CLAUDE.md:95-99`. His closing words, which are the question this thread answers: *"yes, I know that there is a hook in Cobalt that does hippocampus stuff, but it's part of the old Gemini structure. It was supposed to be reused, but I don't see where we reused it."*
**THREAD 2 — THE HERMES-SHAPED INDEX, AND THE PATH OUT OF IT.** His recollection: we chose a Hermes-agent-like memory structure for speed and for its ability to prune and decide what stays in the index — **but what Hermes does is force the agent to decide what leaves the 4,000-character index, and what leaves must NOT become a forgotten memory; it must have a PATH, a way to retrieve it.** He believes that second half was never built and that it is why the index is being lost. Test it: find where the 4,000-character cap was ruled, what was said at the time about what happens to evicted material, and whether any retrieval path for evicted content was ever designed or built. Measure today's reality: the always-loaded block is at **3,993 / 4,000 characters** — a zero-sum surface — and `INDEX.md` is a hand-maintained list of links. **Where does an evicted pointer go today? Answer with evidence, and if the answer is "nowhere", say so plainly.**
**THREAD 3 — DREAMING, COMPRESSION, PRUNING, AND A TIME MACHINE.** His recollection: dreaming was to take the day's material, structure it, combine it into one coherent record, write it to memory, make it referenceable, **and then deal with it — prune or condense**. Plus an ability to compress what is not needed and clean the tree, e.g. gzip on a cadence, with a **"time machine" shape: the last month fresh and reachable through the index, and the index pointing to the deeper archive for a deep dive.** Test which parts were said, designed, built. Measure the reality he is reacting to: **a `cto-<date>.md` report for every day the desk has run** (count them and total their bytes), plus `docs/40 - DevDocs/prompts/` (271 files) and `reports/` (121 files), `docs/30 - Design/`, tests, and the vault `0 - Inbox` (181 files, 11 MB, **6 MB of it the `tribunal-routing-2026-09-13` folder that NOTHING in memory references**). **For a representative sample of these — at least the last five `cto-*.md`, two close reports and two tribunal folders — answer the question he actually asked: IS ANYTHING IN THEM THAT MATTERS ACTUALLY IN MEMORY?** Pick concrete facts out of them and grep the memory folder for each. Report the hit rate as a number.

## THE METHOD — time-bound, because the order of decisions is the point
**Every finding carries a DATE and a SOURCE.** His instruction: *"this has to be time bound … we need to know which decisions happened before which other decisions."*
**§1 THE TIMELINE.** One dated line per event that touched memory, 2026-08-21 → today, in order: what was said, decided, designed, built, deployed or abandoned, who said it, and the file+line it comes from. A claim without a date does not go in the timeline; it goes in `## Undated`.
**§2 THE MATURITY LADDER, per thread and per component.** Six rungs: **SAID** (proposed in conversation) → **DECIDED** (a ruling) → **DESIGNED** (an ADR or design doc) → **BUILT** (code exists) → **DEPLOYED** (it runs in production) → **USED** (it has rows, or logs, or any evidence of use). For each component give the date and the evidence at every rung it reached, and **name the exact rung where it stopped and what stopped it**. This is the spine of the answer to "what we said we're going to do and what we're missing".
**§3 THE CROSS-REFERENCE.** Where the three threads agree, duplicate each other, or contradict each other — and in particular whether the Obsidian memory was ever meant to REPLACE Hippocampus or to SIT BESIDE it. He believes the latter. Say what the record says, with dates, and if the record is silent or changed its mind, say when it changed and on whose word.
**§4 THE HOLES.** Every promise not kept, with the ruling that made it, its date, and what has been done instead. Order by how much it costs us today.
**§5 THE RECONSTRUCTION PROPOSAL.** How to rebuild a historical record of these discussions so this can never be asked again: what to index, where the index lives, what makes a fact reachable, and how the three threads would be closed. **Options with costs, as the bars brief did — not a single plan.** Nothing here is a decision; it is what he will choose from.

## WHERE TO LOOK — read in this order, and say what you could not read
`/Users/cobalt/Vault/Think/6 - Permanent/Memory/_imports/anthropic-2026-09-05/` and `_imports/anthropic-2026-09-15/` — **the frozen chat-side history 08-21 → 09-15, which is the ONLY place the original discussions survive verbatim; grep it hard for memory, hippocampus, vector, graph, embedding, Hermes, index, prune, dream, compress, gzip, archive, retention, forget** · `docs/00 - Project/PROJECT-LEDGER.md` in full (the dated record) · `docs/20 - Assessment/TRIAGE.md` §2.1 and `ASSESSMENT.md` · `docs/10 - Decisions/ADR-*.md` (all eleven) · `docs/30 - Design/` · `LAWS.md` + `LAWS-HISTORY.md` (L28, L56, L57, L58, L59, L6 and anything else touching memory) · `INDEX.md`, `areas/*.md`, `topics/*.md` · `src/cobalt_agent/memory/` and `src/cobalt/` (is there a new-core memory module at all?) · the database: `cobalt db query` the catalog for what exists, and state what the five pillars would have needed · the vault `0 - Inbox` folders · `docs/40 - DevDocs/reports/cto-*.md` and `close-*.md`.
**L35: verify, never infer.** **L70: a claim you could not check is `UNVERIFIED — <the exact command or file that would settle it>`, never smoothed over.** Where his recollection and the record disagree, **say so plainly and show both** — he asked for the true history, not a comfortable one.

REPORT: `docs/30 - Design/memory-history-2026-09-20.md`. Shape: `# MEMORY — the true history` → §0 Headline ≤8 lines → §1 Timeline → §2 Maturity ladder → §3 Cross-reference → §4 The holes → §5 Reconstruction options → `## UNVERIFIED` → `## What I could not read`. Commit it by name.
Last line, exactly: `MEMORY HISTORY READY · timeline events: <n> · components tracked: <n> · reached USED: <n> · stopped at SAID/DECIDED: <n> · holes: <n> · memory hit rate on sampled reports: <n>% · UNVERIFIED: <n>` or `FAILED: <step> — <reason>`.
