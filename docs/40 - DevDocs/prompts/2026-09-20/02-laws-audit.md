MODEL: Opus 5 (`claude-opus-5`) — you are auditing the file that binds every house; a missed contradiction becomes a law nobody can follow (L29 floor: this is forensics) · SEAT: audit hub `laws-audit-0920`, launched by the CTO desk in the background, two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/02-laws-audit.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control laws-audit-0920 --allowedTools "Bash(git add *)" "Bash(git commit *)" "Bash(git status*)" "Bash(git log*)" "Bash(git diff*)" "Bash(git show*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · **NO NEW RULE:** this is today's close-hub list minus the `cobalt validate` string · SESSION: fresh · auto mode on; never `bypassPermissions`; **READ-ONLY on law: you never edit `LAWS.md`, `LAWS-HISTORY.md` or anything under `6 - Permanent/Memory/` (L58 — the CTO desk is the only writer, and this audit changes no law). No production command, no `cobalt_dev`, no merge, no push.** · METER: Anthropic small · nobody sits at this terminal.

# THE LAWS AUDIT — build the contradiction register for a consolidation sitting
Ruled by Dejan 2026-09-20 (`cto-2026-09-20.md` §4 R10) after the desk reported **74 laws, 46 of them carrying amendments**. His words: *"we need a sitting where we bring all the laws and we look at summarizing everything into a correct set of laws that don't contradict each other and don't need to override each other or amend each other … if there's clutter there needs to be a cleanup so the models that are looking at it are not getting confused."*
**You produce a REGISTER, not new law.** Nothing is rewritten, nothing is folded, no number is invented or retired. The sitting rules; you only make it possible to hold one. DO NOT STOP until your report ends `LAWS AUDIT READY …` or `FAILED …`.

**WHY THIS IS DELICATE, and it is the reason the register must be clause-level:** consolidation has already silently dropped clauses TWICE, and the file says so itself — `[restored 09-13, O1]` four clauses of L28 "were dropped without comment in the 09-08 fold paste", and `[restored 09-13, O12]` L29's "never auto mode on a write path" was "silently absent from the 09-10 fold wording". A consolidation that cannot PROVE nothing was lost is worse than the clutter it removes.

AUTHORIZATION — VERIFY IT YOURSELF: `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-20.md"` and read `cto-2026-09-20.md` §4 R10. Laws that bind you: **L58** (you never write the memory folder), **L35** (verify against the file, never a summary), **L48** (evidence in the report), **L70** (an unproven claim is not a finding), **L37** (you do not decide — Dejan rules each conflict), **L39** (an unresolved disagreement about law text is an OPEN item for him, never a vote).
INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` **in full, twice — once to read, once to index** → `LAWS-HISTORY.md` in full → `INDEX.md` → `topics/cto-desk.md` → `docs/40 - DevDocs/SESSION-CLOSE.md` → `docs/40 - DevDocs/reports/close-2026-09-1*.md` (the fold lists) → `docs/00 - Project/PROJECT-LEDGER.md` **only where a law cites it by line**.
REPORT: `docs/40 - DevDocs/reports/laws-audit-2026-09-20.md`. Commit it by name.

## 1. THE CLAUSE INVENTORY — the spine of the whole thing
Every law L1–L74 gets a row: number · title · ruled date · every `[amended …]` / `[restored …]` / `[struck …]` / `[clarified …]` / `[resolved …]` tag it carries with its date · its cited sources · **and a CLAUSE COUNT**: break each law into its distinct normative clauses (an imperative that could be obeyed or broken on its own) and number them `L<n>.1`, `L<n>.2`, … **This inventory is what a future rewrite must be diffed against**, so a clause split too coarsely is a clause that can go missing. Report the total clause count.

## 2. THE CONTRADICTION REGISTER — the point of the exercise
For every pair of clauses that cannot both be obeyed, one row: the two clause ids, both texts VERBATIM, the situation where they collide, which one current practice actually follows (with evidence from a report or the code), and **the question Dejan must answer, phrased so it can be ruled with one word**. Sort by how often the collision is actually met.
**Four the desk has already hit — confirm, refute or sharpen each, and do not treat this list as complete:**
(a) **L58 vs `SESSION-CLOSE.md` steps 3–4** — L58: the memory folder is written "only by the CTO desk"; the close routine has the close HUB rewrite `## NOW` and append the areas files. The desk split the steps on 2026-09-20 to run the close at all (`cto-2026-09-20.md` §0) and the texts still disagree.
(b) **L29's "never auto mode on a write path"** (restored 09-13, O12) vs practice: 2026-09-19's build hubs `48`/`49`/`50` rebased branches, edited source and migrated `cobalt_dev` under `--permission-mode auto`, while the deploy hubs `38`/`53` carried NO `--permission-mode` flag at all. Read those launch lines and say which practice the law actually forbids and whether the deploy shape is the compliant one.
(c) **L23 local-first** vs **L49** (the local seat reads and judges, never composes) vs **L29**'s model floors — the file already flags this cluster "under review, routing tribunal" and has since 09-13.
(d) **L68 as amended 2026-09-20** vs **L46** (one run one commit) and **L54** (`--ff-only`, merge = deploy) — the gate branch may now BE the deliverable; check the three texts agree.
Also list every law still marked **"under review, routing tribunal"** (L5, L24, L26, L27, and L29's routing sentence) with how long it has been open, and every **status note that is not law text** but reads like one.

## 3. THE CLUTTER REGISTER — what confuses a reader that is not a contradiction
Laws that would be clearer merged (candidates the desk has noticed, judge them yourself: L55/L61/L62/L63 all govern launches and permissions; L35/L70 are both "trust what was run"; L46/L54/L68 are all merge discipline; L7/L8 both gate what may render). Laws whose CURRENT text still argues with a superseded version. Entries where the `— source` line is longer than the law. Aliases still in use (`PROPOSED-1…6`). Anything a small local model would plausibly misread — **state the misreading**, since Dejan's stated reason for the sitting is "so the models that are looking at it are not getting confused".

## 4. WHAT A SMALL MODEL CANNOT BE EXPECTED TO OBEY — read this as its own finding
For each law, mark whether compliance needs **judgement** (the agent must notice the situation is covered) or is **mechanical** (a permission rule, an allowlist, a missing capability, a deterministic wrapper enforces it). The desk's position, for you to test rather than accept: a law that only binds by judgement does not bind a 27B local model at all, and the local seat is safe today not because of L49's text but because L49 removed its shell write path. **Produce the list of laws that currently rely on judgement alone and could be made mechanical**, with the mechanism named for each.

## 5. CLOSE
`## Sitting agenda` — the conflicts in the order he should rule them, each a one-word-answerable question, with the desk's recommendation where there is one · `## Proposed shape of the consolidated file` (structure only, NO new law text) · `## What a rewrite must prove` (the clause-level trace: every clause of the current file appears in the new one or is explicitly retired by a ruling of his) · `## ESCALATE`.
Last line, exactly: `LAWS AUDIT READY · laws: <n> · clauses: <n> · contradictions: <n> (met in practice: <n>) · clutter items: <n> · judgement-only laws: <n> · open since 09-13: <n>` or `FAILED: <step> — <reason>`.
