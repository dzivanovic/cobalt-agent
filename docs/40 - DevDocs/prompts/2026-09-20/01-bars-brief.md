MODEL: Opus 5 (`claude-opus-5`) — this is a DESIGN brief that will be ruled on by four houses and then built against `system.bars`, the largest table in production (L29) · SEAT: design hub `bars-brief-0920`, launched by the CTO desk in the background, two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/01-bars-brief.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control bars-brief-0920 --allowedTools "Bash(git add *)" "Bash(git commit *)" "Bash(git status*)" "Bash(git log*)" "Bash(git diff*)" "Bash(git show*)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · **NO NEW RULE:** the git/shell set is today's close-hub list; `COBALT_ENV=production uv run cobalt db query *` is **read-only in code** (`src/cobalt/db_query.py` `guard_select` accepts exactly one `SELECT`/`WITH` and opens `BEGIN READ ONLY`) and is approved standing in `.claude/settings.json` · SESSION: fresh · auto mode on; never `bypassPermissions`; **NO migration, NO write of any kind to production, NO `cobalt_dev`, NO merge, NO push, NO `launchctl`, NO vault write** · METER: Anthropic small · nobody sits at this terminal.

# WRITE THE BARS-LIFECYCLE TRIBUNAL BRIEF — the proposing house's design, for a FOUR-HOUSE tribunal at 13:05 ET today
**The tribunal's job on this brief (L67):** the four houses rule on it and derive the final version — so where the brief cannot settle something, it must hand them a QUESTION SHARP ENOUGH TO RULE ON, never a shrug. His instruction, verbatim: "the tribunal needs to either come up with questions or results, or we need to figure out how to design this problem and make sure it's appropriate for what we're trying to do right now."
You are the PROPOSING house (L67: one house designs and proposes, four rule on it and derive the final version). **You write a brief, not a build.** Nothing is implemented today. DO NOT STOP until your file ends `BARS BRIEF READY …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/cto-2026-09-20.md"` and read `cto-2026-09-20.md` §4 in full. The tribunal itself is `cto-2026-09-19.md` §4 **R10** (scheduled 13:05 ET, after the Fable meter reset) and **R15** (it blocks nothing); the hold on this redesign was lifted by `cto-2026-09-18.md` §4 **R15** as a TRIBUNAL design task. Laws that bind the brief: **L52** (a design that touches anything reaching the card must have every number traceable to a source Cobalt can fetch and verify, ONE named ranking authority, a real integration seam, and be auditable by another house), **L67** (four houses, ≤3 rounds each), **L1** fail-loud, **L3** one path, **L9** loud degradation, **L10** config-as-code, **L28** vault-write law and its `com.cobalt.archiver` append-only carve-out, **L42** restarts by rule, **L45** real artifacts, **L53** (**ceilings and cadences are DEJAN'S — the brief may state a number's consequences and must never settle one**), **L57** explainability, **L68** as amended today, **L70** unproven escalates.

INDEX CARD (read in this order): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → **`LAWS.md` in full** → `areas/cobalt-product-definition.md`'s 2026-09-18 and 2026-09-19 lines (the bars hold-lift and the whole append-only archiver tribunal, three rounds) → `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-archiver-r3-0919/TRIBUNAL-R3.md` (the last archiver tribunal round, including the dissents carried FORWARD to this one) → `docs/40 - DevDocs/prompts/2026-09-19/12-packet/` (the archiver's FINAL design — §11 in particular) → `docs/40 - DevDocs/reports/deploy-d3-2026-09-19.md` ESCALATE 4 (the migration proof cost) → `src/cobalt/archiver/` (`runner.py`, `store.py`, `shadow.py`, `settings.py`) and `src/cobalt/db_migrations/` (the `bars` DDL and every index on it) → `src/cobalt/radar/collector.py` (the intraday poller — the ONLY writer that can see a forming minute).
REPORT / DELIVERABLE: `docs/30 - Design/bars-lifecycle-brief-2026-09-20.md`. Commit it by name.

## THE MEASURED GROUND (the desk read these from production at 06:3x ET today; RE-DERIVE THEM YOURSELF with `cobalt db query` and say so if any differs)
`system.bars`: **8,834,532 rows, 1,372 MB total — 862 MB heap + 509 MB indexes, and exactly ONE index, `bars_pkey`.** By interval:
`i1` 5,281,313 rows · 584 tickers · 2026-07-30 → 2026-09-18 · `i2` 1,780,528 · 210 · 2026-07-30 → 2026-09-18 · `i5` 1,025,864 · 210 · 2026-06-05 → 2026-09-18 · `i30` 585,783 · 210 · **2025-08-08** → 2026-09-18 · `i15` 161,044 · 210 · 2026-06-05 → 2026-09-18.
**The number that forces this tribunal:** the production migration's BEFORE+AFTER proof read of `bars` cost **106.8 s of a 149 s outage — 72 %** (`deploy-d3-2026-09-19.md` ESCALATE 4; it was 59 % on 09-19 morning and 94.1 s on deploy 1). **It grows with the table, so every future deploy's outage is a function of how `bars` is stored.**

## WHAT THE BRIEF MUST CONTAIN — and what it must NOT
**It must answer SEVEN questions, each with its evidence. Q6 he has already RULED (i1 only) — there it is HOW and WHAT BREAKS, not whether. Q7 is his too:**
1. **PARTITIONING.** His words (`cto-2026-09-18.md` R15): "we also need to figure out how to partition that table and make it really highly accessible in the future." Propose the scheme (by interval? by time range? both?), state what it does to the three real access patterns — the intraday poller's writes, the nightly archiver's append, and the replay/backfill reads — and **say plainly what it does to the migration proof cost**, which is the only number that has already hurt us.
2. **RETENTION AND DERIVATION.** `09-18 R13`: **i1 stored, the other intervals derived.** But the measured ground says i30 reaches back to **2025-08-08** while i1 reaches back only to **2026-07-30** — ≈13 months versus ≈7 weeks — so the archiver tribunal's finding stands: **≈9.6 months of i30 cannot be rebuilt from ≈14 days of i1**. Reconcile the ruling with the data: what is derivable going forward, what is historical and must be kept as stored rows, and where the boundary sits. Also carry forward, and do not bury: **i1/i5 include extended hours, i15/i30 do not** — a derivation that ignores that produces wrong bars.
3. **THE FORMING-BAR QUESTION.** The nightly archiver runs when the market and the database are quiet, so it needs no forming-bar special case; **the radar's intraday poller is the only writer that can see a forming minute** (`radar/collector.py`). State what that means for a partitioned table and for any derivation that runs while the poller is live.
4. **THE REPAIR SEAM, carried from the archiver tribunal as an OPEN dissent.** `09-19 R8` ruled **A**: repairs run only in a QUIET WINDOW (radar idle, ≥5 min after the last poll cycle, ≥10 min before the next session opens), poller untouched. Gemini and Astra dissented for **Option B** — a per-target repair marker the poller checks inside its write transaction — and that dissent was explicitly carried to THIS tribunal. Put both on the table with their evidence; **do not re-rule A, and do not pretend B was rejected on merit.**
5. **THE MIGRATION PROOF ITSELF.** Whatever the scheme, the proof that runs inside every production migration currently reads the whole table twice. Propose how it stays honest (L57: every number replayable) while not costing 72 % of every outage.

## THE THING THAT MAKES THIS BRIEF USEFUL, and the reason a first draft was stopped to add it (his words, 2026-09-20)
*"Need proposals and estimates within that write-up, not just something that comes to me for ruling. I want to know if we partition it one way, what is the consequence, what is the benefit? If we do something else, what is the consequence, what is the benefit? If we drop something and decide to do a lower retention period, what is the benefit, what is the downside? I need to have multiple proposals to be able to say this is what I like versus this is not what I like. Not just dry, this is what it is, rule it in or out."*

**So: every one of the five questions is answered with TWO TO FOUR NAMED OPTIONS, each costed.** A question that arrives as "here are the considerations, you decide" has failed this brief. For EACH option, in a compact table plus two or three sentences of prose:
- **What it is**, in one sentence a non-DBA can act on.
- **BENEFIT, quantified.** Use the measured ground: 8,834,532 rows · 1,372 MB (862 heap + 509 index) · one index `bars_pkey` · the proof read costing **106.8 s of a 149 s outage**. Say what the option does to *that* number and to disk, as a figure with its arithmetic shown — "the proof reads only the live partition, ≈X of Y rows, so ≈Z s" beats "faster".
- **COST / DOWNSIDE, equally concrete:** build effort in CHUNKS (a chunk = one prompt, one hub, one suite run — say how many), migration risk on a 1.4 GB table (is it a rewrite? does it hold a lock? how long with the residents down?), what it breaks or complicates for the poller, the archiver, replay and backfill, and what it makes irreversible.
- **WHAT IT COSTS TO BE WRONG** — if we pick it and regret it in a month, what is the way back and what does that cost.
- **YOUR RECOMMENDATION** among the options, in one sentence, with the single reason that decides it.
**Retention specifically:** give him REAL SCENARIOS, not a policy question — e.g. keep i1 for 30 / 90 / 365 days and keep i30 forever; for each, the MB saved (computed from the per-interval row counts above), what analysis stops being possible, what the replay and the missed-record counterfactual R lose, and whether it is reversible (it is not — deleted bars do not come back from the vendor beyond its own window; **check and state that window rather than assuming it**). Same treatment for any proposal to stop storing an interval and derive it.
**Estimates may be rough, but they are never absent and never unsourced:** an estimate carries the arithmetic or the measurement it came from, and an unknown is written `UNKNOWN — <the measurement that would settle it>` (L70), never smoothed over. Where two options differ by less than the error in your own estimate, say so — that is a real finding.

## TWO MORE QUESTIONS HE ADDED, 2026-09-20 — treat them as Q6 and Q7, same costed-option treatment
**Q6 — ANSWERED BY HIM, 2026-09-20: i1 ONLY. This is now a RULED INPUT, not an open question** (`cto-2026-09-20.md` §4 R9). His words: *"i30 on its own does not mean anything to our use case and it's irrelevant if it exists and the one minute bars cannot be created. Any data that cannot be folded into two minute bars that I'm using … is irrelevant if one or two minute bars cannot be created out of it. So it can be completely dropped out of the database … all we need to keep in a database is one minute bars or two minute bars, whichever one does longer period … I would just keep one minute bars, remove everything else, and go forward with only one minute bars."* **The standing test he set: an interval that cannot be dissected into i1/i2 is irrelevant, however far back it reaches** — so the ~13 months of i30 is droppable, not precious, and the "9.6 months cannot be rebuilt from 14 days of i1" finding stops being an objection. His span check is correct: i1 and i2 both run 2026-07-30 → 2026-09-18. Volume released: **i2+i5+i15+i30 = 3,553,219 of 8,834,532 rows, 40 % of the table.** The desk has already read the supporting evidence: the live engine reads i1 and nothing else (`src/cobalt/radar/replay.py:136`, `src/cobalt/radar/store.py:333`, both `interval = 'i1'`).
**AND THE MEASUREMENT THAT SETTLES IT, taken by the desk from production at 07:0x ET — re-derive it and quote the query.** Extended-hours coverage per interval, timestamps in ET:
`i1` **04:00:00 → 19:59:00**, **1,136,902 premarket rows + 847,248 after-hours rows** · `i2` 04:00 → 19:58, 440,493 + 350,889 · `i5` 04:00 → 19:55, 283,104 + 228,455 · **`i15` 09:30 → 15:45 and `i30` 09:30 → 15:30 — ZERO premarket rows, ZERO after-hours rows, in either.**
So the two intervals with the longest history are **blind to the premarket**, which is the session the radar runs in and the one Monday's first live cards render in. His point, 2026-09-20: *"Even more reason to remove all but 1 minute. We need to derive pre and post market from 1 minute too."* → **RULED, as part of R9: i1 is the only stored interval AND the only source of session segmentation.** Premarket (04:00–09:30), regular (09:30–16:00) and after-hours (16:00–20:00) are all DERIVED from i1 by timestamp — never from a higher interval, which cannot represent them at all.
**So the brief must also answer:** (f) **is i1's extended-hours coverage complete enough to derive on** — per-minute row density inside 04:00–09:30 and 16:00–20:00, gaps, and whether a missing minute is a vendor gap or a poller gap (name how to tell them apart); (g) **where session segmentation lives today** — is it in the collector, in config, in the card engine, in SQL? — with file:line, and what it costs to make i1-derived segmentation the one path (L3); (h) whether any derived-i2 or session-boundary edge case (a bar straddling 09:30, a half day, a holiday) changes an answer, with the real dates to test against.

**So Q6 is no longer "should we" — it is HOW, and WHAT BREAKS. Answer these, costed as above:** (a) **Find every reader of a non-i1 interval before a single row is deleted** — `src/`, `configs/`, the archiver's backfill defaults in the Lists note, `configs/cobalt/smoke/s2.yaml`, the DRC and replay paths, any DevDoc that promises one. Name each with file:line, or state plainly that there are none. **A reader you miss is a production break with no way back** (deleted bars do not return past the vendor's window — check and state that window). (b) **Does a derived i2 equal the vendor's i2 exactly?** Compare stored i2 rows against i2 built from stored i1 over the overlapping range and give the row-by-row result — this is the one measurement that can still qualify his ruling, because i1/i5 carry extended hours while i15/i30 do not, and if a derived bar differs, say where and by how much. If the comparison needs a command you do not have, write `UNVERIFIABLE FROM READS — <the exact query>` (L70). (c) **The deletion itself:** options for removing 3.55 M rows from a 1.4 GB table — in-place `DELETE` (locks, bloat, `VACUUM`), a partition drop if partitioning lands first, or a table rewrite — each costed, each with its outage, and each saying whether it happens before, during or after the partitioning change. (d) **The poller and the archiver stop writing the other four** — name the config or code that decides which intervals are fetched, and what the change costs. (e) **What the freed 40 % does to the migration proof cost**, with the arithmetic.

**Q7 — GROWTH. THE DESIGN IS FOR THE TABLE WE WILL HAVE, NOT THE ONE WE HAVE.** His words: *"this is current state based on 520 some tickers in the database but that is not the number of tickers that's going to be there in the future, that number is growing, so the growth of the database structure is exponential and not linear … we need to account for the growth of the number of tickers that are coming in daily and also give it room to grow even more."* Today: **584 distinct tickers in i1, 210 in every other interval.** **MEASURE THE ACTUAL ARRIVAL RATE FIRST** — from `bars` (first `ts` per ticker, bucketed by week) and from `system.radar_membership` (distinct tickers per day since it started) — and quote the queries. Then model the table at **1, 3, 6, 12 and 24 months** under at least three ticker-growth scenarios (the measured rate continued; a faster rate he might want; and a deliberate ceiling), giving rows, GB and — the number that matters — **the migration proof cost in seconds** at each point, with the arithmetic shown. State plainly at which point each partitioning option stops working and what the warning sign will be. **Growth is a DESIGN INPUT here, not an afterthought**: an option that is fine at 8.8 M rows and unworkable at 50 M is not a candidate, and say so. If the evidence does not support "exponential", say what it does support and show it — his instruction is to account for growth, not to assume a particular curve.

**It must NOT:** settle any ceiling, cadence or retention PERIOD (L53 — those are his, and the brief states consequences and options, never a chosen number); propose anything that changes what the archiver WRITES while it is in `upsert` mode (R35's one shadow night is still ahead — Monday 20:30); or assume a number it did not read. **Every figure carries its source.** Where a claim needs a command that is not in your allowlist, write `UNVERIFIABLE FROM READS — <the exact command>` (L70) rather than asserting it.

## SHAPE
`# BARS LIFECYCLE — tribunal brief 2026-09-20` → **§0 The proposal in ten lines** (a house must be able to disagree with it from §0 alone) → §1 Measured ground, re-derived, with every query quoted → §2 The seven questions, one section each, each carrying its TWO TO FOUR COSTED OPTIONS (benefit / cost / cost-of-being-wrong / your recommendation) and ending with the one you propose → §3 What this costs to build, in chunks, with what must be ruled before each → §4 What is HIS to rule (L53 numbers, retention periods, anything touching the card) → §5 Open dissents carried in (the A/B repair seam) → §6 `UNVERIFIABLE FROM READS`.
Last line, exactly: `BARS BRIEF READY <file sha256> · questions answered: 7/7 · options costed: <n> · his rulings owed: <n> · carried dissents: <n> · UNVERIFIABLE: <n>` or `FAILED: <step> — <reason>`.
