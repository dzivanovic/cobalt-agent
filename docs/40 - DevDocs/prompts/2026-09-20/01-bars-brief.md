MODEL: Opus 5 (`claude-opus-5`) — this is a DESIGN brief that will be ruled on by four houses and then built against `system.bars`, the largest table in production (L29) · SEAT: design hub `bars-brief-0920`, launched by the CTO desk in the background, two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-20/01-bars-brief.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control bars-brief-0920 --allowedTools "Bash(git add *)" "Bash(git commit *)" "Bash(git status*)" "Bash(git log*)" "Bash(git diff*)" "Bash(git show*)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · **NO NEW RULE:** the git/shell set is today's close-hub list; `COBALT_ENV=production uv run cobalt db query *` is **read-only in code** (`src/cobalt/db_query.py` `guard_select` accepts exactly one `SELECT`/`WITH` and opens `BEGIN READ ONLY`) and is approved standing in `.claude/settings.json` · SESSION: fresh · auto mode on; never `bypassPermissions`; **NO migration, NO write of any kind to production, NO `cobalt_dev`, NO merge, NO push, NO `launchctl`, NO vault write** · METER: Anthropic small · nobody sits at this terminal.

# WRITE THE BARS-LIFECYCLE TRIBUNAL BRIEF — the proposing house's design, for a FOUR-HOUSE tribunal at 13:05 ET today
You are the PROPOSING house (L67: one house designs and proposes, four rule on it and derive the final version). **You write a brief, not a build.** Nothing is implemented today. DO NOT STOP until your file ends `BARS BRIEF READY …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/cto-2026-09-20.md"` and read `cto-2026-09-20.md` §4 in full. The tribunal itself is `cto-2026-09-19.md` §4 **R10** (scheduled 13:05 ET, after the Fable meter reset) and **R15** (it blocks nothing); the hold on this redesign was lifted by `cto-2026-09-18.md` §4 **R15** as a TRIBUNAL design task. Laws that bind the brief: **L52** (a design that touches anything reaching the card must have every number traceable to a source Cobalt can fetch and verify, ONE named ranking authority, a real integration seam, and be auditable by another house), **L67** (four houses, ≤3 rounds each), **L1** fail-loud, **L3** one path, **L9** loud degradation, **L10** config-as-code, **L28** vault-write law and its `com.cobalt.archiver` append-only carve-out, **L42** restarts by rule, **L45** real artifacts, **L53** (**ceilings and cadences are DEJAN'S — the brief may state a number's consequences and must never settle one**), **L57** explainability, **L68** as amended today, **L70** unproven escalates.

INDEX CARD (read in this order): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → **`LAWS.md` in full** → `areas/cobalt-product-definition.md`'s 2026-09-18 and 2026-09-19 lines (the bars hold-lift and the whole append-only archiver tribunal, three rounds) → `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-archiver-r3-0919/TRIBUNAL-R3.md` (the last archiver tribunal round, including the dissents carried FORWARD to this one) → `docs/40 - DevDocs/prompts/2026-09-19/12-packet/` (the archiver's FINAL design — §11 in particular) → `docs/40 - DevDocs/reports/deploy-d3-2026-09-19.md` ESCALATE 4 (the migration proof cost) → `src/cobalt/archiver/` (`runner.py`, `store.py`, `shadow.py`, `settings.py`) and `src/cobalt/db_migrations/` (the `bars` DDL and every index on it) → `src/cobalt/radar/collector.py` (the intraday poller — the ONLY writer that can see a forming minute).
REPORT / DELIVERABLE: `docs/30 - Design/bars-lifecycle-brief-2026-09-20.md`. Commit it by name.

## THE MEASURED GROUND (the desk read these from production at 06:3x ET today; RE-DERIVE THEM YOURSELF with `cobalt db query` and say so if any differs)
`system.bars`: **8,834,532 rows, 1,372 MB total — 862 MB heap + 509 MB indexes, and exactly ONE index, `bars_pkey`.** By interval:
`i1` 5,281,313 rows · 584 tickers · 2026-07-30 → 2026-09-18 · `i2` 1,780,528 · 210 · 2026-07-30 → 2026-09-18 · `i5` 1,025,864 · 210 · 2026-06-05 → 2026-09-18 · `i30` 585,783 · 210 · **2025-08-08** → 2026-09-18 · `i15` 161,044 · 210 · 2026-06-05 → 2026-09-18.
**The number that forces this tribunal:** the production migration's BEFORE+AFTER proof read of `bars` cost **106.8 s of a 149 s outage — 72 %** (`deploy-d3-2026-09-19.md` ESCALATE 4; it was 59 % on 09-19 morning and 94.1 s on deploy 1). **It grows with the table, so every future deploy's outage is a function of how `bars` is stored.**

## WHAT THE BRIEF MUST CONTAIN — and what it must NOT
**It must answer five questions, each with its evidence:**
1. **PARTITIONING.** His words (`cto-2026-09-18.md` R15): "we also need to figure out how to partition that table and make it really highly accessible in the future." Propose the scheme (by interval? by time range? both?), state what it does to the three real access patterns — the intraday poller's writes, the nightly archiver's append, and the replay/backfill reads — and **say plainly what it does to the migration proof cost**, which is the only number that has already hurt us.
2. **RETENTION AND DERIVATION.** `09-18 R13`: **i1 stored, the other intervals derived.** But the measured ground says i30 reaches back to **2025-08-08** while i1 reaches back only to **2026-07-30** — ≈13 months versus ≈7 weeks — so the archiver tribunal's finding stands: **≈9.6 months of i30 cannot be rebuilt from ≈14 days of i1**. Reconcile the ruling with the data: what is derivable going forward, what is historical and must be kept as stored rows, and where the boundary sits. Also carry forward, and do not bury: **i1/i5 include extended hours, i15/i30 do not** — a derivation that ignores that produces wrong bars.
3. **THE FORMING-BAR QUESTION.** The nightly archiver runs when the market and the database are quiet, so it needs no forming-bar special case; **the radar's intraday poller is the only writer that can see a forming minute** (`radar/collector.py`). State what that means for a partitioned table and for any derivation that runs while the poller is live.
4. **THE REPAIR SEAM, carried from the archiver tribunal as an OPEN dissent.** `09-19 R8` ruled **A**: repairs run only in a QUIET WINDOW (radar idle, ≥5 min after the last poll cycle, ≥10 min before the next session opens), poller untouched. Gemini and Astra dissented for **Option B** — a per-target repair marker the poller checks inside its write transaction — and that dissent was explicitly carried to THIS tribunal. Put both on the table with their evidence; **do not re-rule A, and do not pretend B was rejected on merit.**
5. **THE MIGRATION PROOF ITSELF.** Whatever the scheme, the proof that runs inside every production migration currently reads the whole table twice. Propose how it stays honest (L57: every number replayable) while not costing 72 % of every outage.

**It must NOT:** settle any ceiling, cadence or retention PERIOD (L53 — those are his, and the brief states consequences and options, never a chosen number); propose anything that changes what the archiver WRITES while it is in `upsert` mode (R35's one shadow night is still ahead — Monday 20:30); or assume a number it did not read. **Every figure carries its source.** Where a claim needs a command that is not in your allowlist, write `UNVERIFIABLE FROM READS — <the exact command>` (L70) rather than asserting it.

## SHAPE
`# BARS LIFECYCLE — tribunal brief 2026-09-20` → **§0 The proposal in ten lines** (a house must be able to disagree with it from §0 alone) → §1 Measured ground, re-derived, with every query quoted → §2 The five questions, one section each, each ending in a PROPOSED ANSWER and the alternatives you rejected with the reason → §3 What this costs to build, in chunks, with what must be ruled before each → §4 What is HIS to rule (L53 numbers, retention periods, anything touching the card) → §5 Open dissents carried in (the A/B repair seam) → §6 `UNVERIFIABLE FROM READS`.
Last line, exactly: `BARS BRIEF READY <file sha256> · questions answered: 5/5 · his rulings owed: <n> · carried dissents: <n> · UNVERIFIABLE: <n>` or `FAILED: <step> — <reason>`.
