MODEL: Opus 5 (`claude-opus-5`) — same seat and shape as `35-archiver-db-run.md` (and, behind it, `25-ops-db-run.md`): L29 floor on the migration/DB-suite harness, and the DB step itself is ALWAYS hub-direct, never delegated to a builder child. SEAT: `p4-db-0919`, worktree `/Users/cobalt/cobalt-wt/s2-p4` (it already exists, tip `21bcd5f`, round-2 code tip `9045260` — do NOT create it, do NOT `git worktree add`). THIS RUN TOUCHES `cobalt_dev`: it is a `.env`-present, `COBALT_ENV=dev` run. `cobalt_dev` is FREE only once `archiver-db-0919`'s report ends a line starting `ARCHIVER DB …` (PREFLIGHT proves this yourself — do not launch on the strength of this prompt's header alone). Two bare commands, run by the desk: `cd /Users/cobalt/cobalt-wt/s2-p4` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/37-p4-db-run.md' and follow it exactly." --model claude-opus-5 --remote-control p4-db-0919 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh — every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `--allow-prod`, no vault write, no other seat's config; `cobalt_dev` and `.env` ARE this session's to touch, scoped exactly to the rules above · METER: Anthropic medium; a usage-limit message is not a failure — record the reset time, and BEFORE stopping remove `.env` if present and prove it gone (RECOVERY RULE applies to the meter stop too), commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

# S2-P4 2026-09-19, DB RUN — round 2's owed with-DB proof: the branch's FIRST REAL RUN of its 2 owed `requires_db` tests (the both-merge-orders migration test, `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]` and `[p4_before_p2]`, edited under tribunal A5 but never executed against Postgres — `s2-p4-verify-2026-09-19.md`'s `## ROUND 2 (tribunal fold)` section), plus a fresh with-DB suite total against the branch's own prior with-DB baseline (`db 2136/0`, prompt `24b-p4-fund-rule.md`). LAW STEP of this work item (L67): this run CLOSES the branch's DB debt. After it, none of it yours: the branch still needs its house check (≥3 houses) · the A2 design-gap ruling, still owed (A3's L52 disclosure is RULED — `cto-2026-09-19.md` §4 R23, 12:34 ET "A": the None-filter stays, disclosed and pinned) · Dejan's approval · deploy. You never merge, never deploy, never push, never touch production, never `--allow-prod`. DO NOT STOP until the report ends `P4 DB …`, `CONTINUE: … — meter …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable), not by Dejan; a prompt file is not an approval.
(a) Every `Bash(...)` rule in the launch line above is copied byte for byte from `13-p4-verify.md` and/or `24b-p4-fund-rule.md` — both already approved for THIS worktree and THIS job shape (13: `cto-2026-09-17.md` §4 R6 + `cto-2026-09-18.md` §4 R1; 24b: identical to `24-p4-fund-rule.md`'s own list, itself `13`'s subset, per that file's own LAUNCH-LINE PROOF). This run adds NO new rule string: `comm -23 <(sort this file's strings) <(sort 13's ∪ 24b's strings)` must print NOTHING. If ANY rule here is not found in either file (`grep -c -F '<string>' '<file>'` must print `1` for at least one of the two) → `FAILED: authorization mismatch — <string>`, commit nothing, stop before PREFLIGHT.
(b) `.env` cp/rm are `13`'s and `24b`'s own strings for THIS worktree's path, unchanged. NEVER: production, push, vault write, `bypassPermissions`, `claude -p`, `git rebase`, the fixture cutter, `CronCreate` — none of these are in this launch line and none may be added without a new approval row; a step that seems to need one is not built, it is named in `## ESCALATE` instead.
YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line — but FIRST run `.env` cleanup (see below) — then wip-commit, stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(p4-db-0919): <item> partial — <why>"`, last line `CONTINUE: step <n>`. EVERY `git add` names explicit paths under `docs/40 - DevDocs/` (this run changes no `src/`/`tests/` — it is a verification run only; if a real code defect surfaces, name it under `## ESCALATE`, do not fix it here) — NEVER `scratch/` (review round 1, R11: `scratch/` is NOT gitignored on this branch, holds raw exports with tickers from his screens — L32), never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named. `.env` DISCIPLINE (L4, L41 — the one rule that overrides every other stop order): on ANY stop — `FAILED`, `CONTINUE`, meter, or the normal end — `ls -la .env` FIRST; if it exists, `rm /Users/cobalt/cobalt-wt/s2-p4/.env` THEN `ls -la .env` again to prove it gone, THEN write whatever stop line you were going to write. A stop line written while `.env` still exists on disk is itself a `FAILED` condition — go back and remove it, then re-open the report and fix the line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you touch `cobalt_dev` directly, L4/L41/L67) → this worktree's `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` IN FULL, esp. `## ROUND 2 (tribunal fold)` (the two owed tests, named, and the branch's offline 1829/0 baseline) and its close section 5 (`4.4`–`4.9`, the shape of a with-DB step on this branch) → `24b-p4-fund-rule.md` step 7 and its report section (`db 2136/0`, the command it ran, verbatim, 188.89s) → `tests/cobalt/test_p4_migrations.py` (esp. `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership`, both parametrizations, and `_merge_order_bases`/`_membership_insert`, tribunal A5's own text in the verify report) → `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`'s last line (read-only evidence that `cobalt_dev` is free and what it holds — you do not re-derive this by any other means) → `35-archiver-db-run.md` (the shape this run's PREFLIGHT/state-probe pattern copies). Laws, one line each: L1 fail loud · L3 one path — you verify, you do not build a second implementation of anything · L4 secrets discipline — `.env` by name only, never `cat`/printed · L29 Opus floor on the with-DB harness · L35 artifact, not claim — the desk verifies via `git show`/`git diff`, never your report's prose alone · L41 cross-house credential law — this session runs the DB-backed proof directly (hub-shaped, no worker) · L45 real-shape evidence — the two owed tests' pass/fail is proved on real Postgres, never asserted from the SQL text alone · L46 one run, one commit boundary; small commits within it · L48 evidence in the report file, same turn · L52 you build nothing that touches scoring, ranking or what reaches the card — this run verifies only · L54 you work in this worktree only · L57 every printed value replayable · L60 you hold this session until the stop line · L67 the branch still needs its house check and two tribunal rulings after you stop — not yours to chase. Report (L48): APPEND to `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` in THIS worktree — a new `## ROUND 2 DB RUN` top-level section, AFTER the existing `## ROUND 2 (tribunal fold)` section's own final line (`P4 ROUND2 9045260 | …`); never delete or restructure existing sections: §0 Headline ≤5 lines → `## ROUND 2 DB RUN PREFLIGHT` → `## ROUND 2 DB RUN AUTHORIZATION` → `## ROUND 2 DB RUN — STATE PROBE` → `## ROUND 2 DB RUN — WITH-DB SUITE` → `## ROUND 2 DB RUN — OFFLINE SUITE + RESTARTS` → `## ROUND 2 DB RUN ESCALATE` → last line. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `claude -p`, `uv run cobalt` anything except `jobs restarts` and `db migrate` (and `db migrate` ONLY with `COBALT_ENV=dev` in front, never bare, never `--allow-prod`), `uv run python` anything (no fixture cutter), any `COBALT_ENV=production` command, `launchctl`, `CronCreate`, `cat`/`less`/`more`/anything that could print `.env`'s contents. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, `.env` cleanup, wip-commit, stop. Never retry a denied command in another shape.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim):
`date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; branch must be `sprint-2/p4`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: `21bcd5f`, else the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file" (if it exists → `FAILED PREFLIGHT: .env present before this run started — remove it and say who left it, this is not yours to inherit silently`) · `ls -la /Users/cobalt/cobalt-wt/archiver-append/.env` → MUST say "No such file" (the signal that `archiver-db-0919` finished cleanly and does not still hold the `cobalt_dev` lane; if it EXISTS → `FAILED PREFLIGHT: archiver-db-0919's .env is present — another session may hold cobalt_dev, do not proceed`) · Read (not Bash) `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`'s last line and quote it verbatim — it MUST start `ARCHIVER DB `; anything else (still `ARCHIVER APPEND R2 …`, a `CONTINUE:`, or a `FAILED:`) → `FAILED PREFLIGHT: archiver DB run has not closed — <line found>`, stop, nothing run · `grep -rn "cobalt_dev" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/"` dated after that line's timestamp (by filename/section date) and read (Read tool) any later hit to confirm no session claimed the lane after `archiver-db-0919` closed it — a hit that claims `.env`/`cobalt_dev` and is NOT `archiver-db-0919`'s own report → `FAILED PREFLIGHT: a later session may have touched cobalt_dev — <what you found>` · `uv run pytest --co -q tests/cobalt/test_p4_migrations.py` (collects cleanly — no code change has landed yet; note the collected count and confirm both `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership` parametrizations are present, `requires_db`-marked). Any denial → `FAILED PREFLIGHT: <rules>`.

## 0. STATE PROBE — what `cobalt_dev` holds before this run (L62: probe the rules with no harmless variant, once, before real use)
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (applies NOTHING; quote the FULL output — every table this branch's harness knows, placement, row count, digest, trailing `code:` line) → `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` proves it gone. Expected, per the archiver report's close and this branch's own round-1 with-DB step: `cobalt_dev` holds 0001–0009, ALL `OK` — 0008/0009 are this branch's own and were applied there in round 1 (`s2-p4-verify-2026-09-19.md` §4.4); the archiver's 0010/0011 are absent (its own DB run applies and then reverses them, leaving 0001–0009). State the terminal line for this step: `cobalt_dev holds (before this run): 0001–0009 objects OK, <n> rows in <largest table>`. If ANYTHING here reads `CHANGED`, errors, or an object outside 0001–0009 exists → STOP and ESCALATE (do not assume a prior partial run cleaned up; name what you found) — `.env` cleanup, `FAILED: step 0 — <line>`, stop. Last line `CONTINUE: step 1`.

## 1. THE WITH-DB SUITE — the branch's owed `requires_db` tests, real Postgres, `cobalt_dev`
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` (re-copy; step 0 removed it) → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` — the SAME command `24b-p4-fund-rule.md` step 7 ran verbatim (quoted there: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → `2136 passed, 6 skipped, 1 xfailed, 15 warnings in 188.89s`) — quote your FULL summary line. EXPECTED ARITHMETIC, stated plainly, not assumed: the prior with-DB baseline is `2136 passed, 0 failed`; round 2 (this branch's tip since that baseline) added 11 offline-passing tests to `tests/cobalt`/`tests/taxonomy` (1818→1829, none of them `requires_db` — ROUND 2's own skip count stayed at 324 unchanged) and EDITED but never ran the two owed `requires_db` parametrizations named below — so `passed` should read `2136 + 11 = 2147` at minimum if those two parametrizations were already counted inside 2136 as their OLD (vacuous) bodies, or some other total if they were not — DO NOT force the arithmetic to fit; state the actual number, subtract 2136, name the delta, and say explicitly whether it reconciles with "+11 round-2 offline tests" or not. Independent of the total: NAME the two owed tests by their exact node ids and state PASSED/FAILED/not found for each:
- `tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]`
- `tests/cobalt/test_p4_migrations.py::test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p4_before_p2]`
(use the summary/verbose output already produced by the suite run above — no separate `-k` rerun is authorized by this launch line beyond what `uv run pytest *`/`COBALT_ENV=dev uv run pytest *` already covers). A red on EITHER: name the test, quote the assertion, state whether it is a TEST-SIDE defect in what round 2 wrote (do not fix it here — this run verifies, it does not build, L52/L1; name it under `## ROUND 2 DB RUN ESCALATE` instead) or a genuine finding about the harness/`cobalt_dev`'s real state. Any OTHER red anywhere in the suite (not the two named tests): name it, do not build a fix, ESCALATE. `cobalt_dev` must be UNCHANGED in content after this step beyond ordinary test round-trips (every test that writes cleans up; the migration-harness test calls `_merge_order_bases`/`_membership_insert` inside its own transaction/round trip per the verify report's own description) — if you have reason to believe otherwise, say so explicitly, do not assume. `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` proves it gone. State the terminal line: `cobalt_dev holds (after this step): 0001–0009, unchanged | db <p>/<f>`. Last line `CONTINUE: step 2`.

## 2. OFFLINE SUITE + RESTARTS
With `.env` confirmed absent (re-check `ls -la .env` if any doubt): `uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 2's own close, `1829 passed, 324 skipped, 1 xfailed, 15 warnings in 56.11s` (`s2-p4-verify-2026-09-19.md`'s `## ROUND 2 (tribunal fold)` stop line) — `passed` MUST still read `1829` and `failed` MUST stay `0` (this step touches no code and no offline test); a mismatch is named plainly, not waved off. `uv run cobalt jobs restarts a59ee8c..HEAD` (the range since the houses' whole read) → the table VERBATIM; name any UNCLASSIFIED entry, do not assume it matches round 2's own `com.cobalt.radar`-only line. `git status --porcelain` empty · report committed by explicit path: `docs(report): s2-p4 round-2 db run — <one line>` (paths: only under `docs/40 - DevDocs/`). Last line `CONTINUE: step 3`.

## 3. CLOSE
`## ROUND 2 DB RUN ESCALATE` carries forward, untouched, everything round 1's and round 2's ESCALATE sections already carried (A2, R2-1, R2-2 — still open, none of them yours to close; A3/L52 is CLOSED by R23 — carry it as "RULED 12:34 ET: stays, disclosed, pinned", not as open), PLUS whatever this run found new (a real red in either owed test, any arithmetic mismatch from step 1, an authorization mismatch). State plainly whether the branch's DB debt is now CLOSED (it should be, after step 1, one way or another) — a red on either owed test does NOT close the debt; say so and ESCALATE it by name. `MEMORY:`/`RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, exactly this shape:
`P4 DB <sha> | offline <p>/<f> | db <p>/<f> (vs 2136/0 + 2 owed) | owed requires_db: <names → PASSED/…> | cobalt_dev: <state> | .env: removed, proven gone | RESTARTS: <line> | ESCALATE: <n>`
or `FAILED: <step> — <reason> | cobalt_dev: <state>` (with `.env` cleanup proven first, always). Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or with `--allow-prod`.
