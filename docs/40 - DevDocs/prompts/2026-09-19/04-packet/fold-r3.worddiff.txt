diff --git a/Users/cobalt/.claude/jobs/c566654e/tmp/02.before-fold3.md b/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md
index 35f585c..61713fd 100644
--- a/Users/cobalt/.claude/jobs/c566654e/tmp/02.before-fold3.md
+++ b/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md	
@@ -18,7 +18,7 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`,
0.3 ROLLBACK DIRECTORY, built fresh by YOU (user data, gitignored `data/`, never `git add`, no value quoted): `mkdir -p /Users/cobalt/cobalt/data/backups/pre-stack-0919/daymode-settings-rollback`; Read the two forward files; Write `aset.yaml` there BYTE-IDENTICAL; Write `daymode.yaml` there = the forward file MINUS exactly the four-line `trade_count_over_band` row. `shasum -a 256` both → record. The rollback dry run (exact allowlisted line) → GATE: prints `no differences — the database already holds these settings.` (the CLI's zero-change wording; all seven keys listed `=`). Anything else → `FAILED PREFLIGHT: rollback file does not equal production`. (The FORWARD dry run cannot run here: main's live code rejects the new row at parse time; it is step 3.4.) Report committed by name. Last line `CONTINUE: step 1`.

## 1. Preconditions
1.1 `date` → the run STARTS only when now ≤ 18:30 ET or ≥ 21:40 ET (every Cobalt settings `--apply` is refused by the `market_reset` guard 20:00–21:00; archiver 20:30, replay 21:05; a run that starts by 18:30 has ninety minutes of margin, so §6 [-can never-]{+is not expected to+} be entered inside the refused hour {+(and §6 (2) says what to do if the clock gets there anyway)+} — deployment check 2026-09-19 V6) → otherwise `FAILED: window — wait for the desk`. THE CLOCK IS RE-CHECKED with `date` immediately before 3.2 (bootout), before 3.6 and before 3.7: at or after 19:40 ET and before 21:00 → do NOT proceed — before 3.2 that is a plain `FAILED: window`; after 3.2 go to §6 and read its CLOCK RULE (review finding 4). 1.2 `harness-round2-2026-09-19.md` ends `ROUND2 BUILT ad9b07c …` and `prod-proof-only-3-2026-09-19.md` ends `PROD PROOF DONE …` (EITHER one missing or ending differently → `FAILED: gate — <which>`; and the proof[-report-] must [-itself name-]{+be BOUND to+} the gated [-code: `grep -c ad9b07c`-]{+code, not merely mention it: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards log --oneline ad9b07c..HEAD -- "docs/40 - DevDocs/reports/prod-proof-only-3-2026-09-19.md"` lists at least one commit — the proof report was committed+} on [-`prod-proof-only-3-2026-09-19.md` ≥ 1, else-]{+THIS branch AFTER the gated code, and the diff proof below shows no code changed since, so the proof ran on `ad9b07c`'s code; no such commit →+} `FAILED: gate — proof [-ran on other code`-]{+not bound to ad9b07c`+} — deployment check [-V7);-]{+V7, tightened by round-2 finding R3);+} `git -C /Users/cobalt/cobalt-wt/s2-p2-cards log -1 --format=%D` shows `HEAD -> sprint-2/stack` (V8); `<gated code>` = `ad9b07c`: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff --stat ad9b07c HEAD -- . ':(exclude)docs'` prints NOTHING (only report commits, all under `docs/`, sit above the gated code — the whole tree outside `docs/` is compared, not a list of folders: V2); `status --porcelain` empty; record `<n>` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rev-list --count main..HEAD`. 1.3 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; NO build or deploy session is merging (the desk launches you alone).

## 2. Machine-written files, snapshot, tags
2.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `configs/cobalt/rules.yaml`, `docs/40 - DevDocs/reports/seat-usage.md`, desk / deploy reports. `git add` BY NAME (never `-A`, never `data/`), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`.
@@ -38,7 +38,7 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`,

## 4. Residents UP
4.1 `COBALT_ENV=production uv run cobalt jobs register` (no plist change expected — say so) → `COBALT_ENV=production uv run cobalt jobs restarts <pre-merge>..HEAD` verbatim; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED. A third resident derived → ESCALATE, not restarted today.
4.2 `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` · `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` · `launchctl print` each → `state = running`, NEW pids. `Bootstrap failed: 5` → one `date`, retry ONCE. Loaded but not running → `launchctl kickstart -k gui/501/<label>` ONCE. Still not running → `FAILED: residents DOWN — <label> — <launchctl output verbatim>` after [-attempting-]{+running §6 from its step (0) through (3) — step (0) boots out the resident that DID start, so nothing runs while code and settings are reverted — and then ONE bootstrap attempt of both on the reverted pair (this step's commands, the same single retry / single `kickstart`). Whatever is not running after that stays down: that is the terminal state;+} §6 [-(1)–(3).-]{+is never entered a second time (round-2 finding R1).+} `date` → outage length (3.2 → 4.2).

## 5. Smoke — ALL delivered functionality (NN#16), each quoted in `## Smoke`
5.1 (every `curl` in this prompt is typed in its FULL allowlisted spelling, never the abbreviation printed here: `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` and `…/radar`) `curl … :5010/` = 200 · `curl … :5010/radar` = 200 · `COBALT_ENV=production uv run cobalt heartbeat show` → no NEW RED (only the P8 baseline class may remain; a RED stamped inside the outage window is the expected one — name it) · `COBALT_ENV=production uv run cobalt validate` exit 0 · `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` → started on the new code, no traceback, NO `CardSettingsError`, no `trade_def_slug`, no `radar_pool_failed_stage_check` (the 09-17 incident signatures) · if a scanning session is open: `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT last_scan_at, members, failed_stage FROM radar_pool"` TWICE — run it FIRST in 5.1 and LAST in 5.2, with every other smoke check in between, and prove the gap with `date` beside each (there is NO sleep or wait command in your allowlist and you never ask for one — a denied wait would fail a green deploy; if the two reads turn out <3 min apart, run `date` and re-read once more after the report's Smoke table is written) (review finding 2) → `last_scan_at` advances, `failed_stage` empty or `bars` (baseline class only); outside a session → `idle:<session>` lines are correct, say so · `tail -n 30 /Users/cobalt/cobalt/logs/aset.log` → no traceback, no degraded day-mode banner · `ls -la /Users/cobalt/cobalt/ops/cto-desk.sh` → executable.
@@ -48,4 +48,4 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`,

## 6. SAFE-STATE RULE — red at 3.4–3.8, red smoke, a failed bootstrap, or a mid-run denial after 3.2
STEP (0), FIRST, ALWAYS (V5 — inherited from `10`, never exercised: both 09-18 uses entered §6 with the residents already down): `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar`; if EITHER is loaded (you came from 4.2 or from a red smoke in §5) → `launchctl bootout` both and require "Could not find service" for both BEFORE you touch code or settings. A resident must never be running while its code is reverted or its row removed under it; `bootstrap` in (4) is only ever run on a service that is not loaded.
Goal: code and settings agree, residents UP, before you stop. THE TWO CONSISTENT PAIRS are {old code + six step-down rows} and {new code + seven rows}; either is a safe place to stop, a MIXED pair never is. CLOCK RULE (review finding 4): `date` first — inside 19:40–21:00 ET no settings `--apply` can run, so you do NOT change the row: if the over-band row IS applied, KEEP the new code (skip (1)–(2), go to (3)); if it is NOT applied, revert the code (1) and skip (2). (1) If 3.3 merged: `git -C /Users/cobalt/cobalt revert --no-edit <pre-merge>..HEAD` — the range starts at the commit IMMEDIATELY BEFORE the ff-merge, so it holds the stack's commits and nothing of yours (L54 — never `reset`). A CONFLICT during the revert (not expected on a linear range nothing was committed above): `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → make THAT pair consistent instead: if the over-band row is not applied yet and the clock allows, apply it (3.6) and the dark row (3.7), `validate`; then (3)–(6) with `safe state: NEW code kept — revert conflicted` (review finding 1). IF THE CLOCK DOES NOT ALLOW that apply (V6 — unreachable on a run that started by 18:30, written down anyway): leave BOTH residents DOWN (new code without the row refuses to load, so bringing them up would only crash-loop them), skip (4), and end `FAILED: <step> — revert conflicted inside the refused hour — safe state: red — residents: DOWN`; the desk escalates by push at once and completes the pair after 21:00. (2) If 3.6 applied, or you cannot prove it did not: {+`date` AGAIN, immediately before this apply — at or after 19:40 ET and before 21:00, or if the apply below is REFUSED by the `market_reset` guard, the pair {old code + six rows} cannot be restored now: leave BOTH residents DOWN (old code refuses the seventh row and would not load), skip (4), end `FAILED: <step> — settings rollback refused — safe state: red — residents: DOWN`; the desk pushes at once and completes the pair after 21:00 (round-2 finding R2). Otherwise:+} the over-band ROLLBACK `--apply` (exact line) → then `COBALT_ENV=production uv run cobalt validate` → exit 0 on the reverted code. Code reverted + over-band row present, or row removed + new code merged, are BOTH broken states — never leave either. (3) The dark row and migrations 0006/0007 STAY (inert for old code / additive); the schema is never rolled back unattended — PROVE it with `validate` exit 0 and the sheet 200 after (4). (4) Bring the residents up [-exactly as 4.2.-]{+with 4.2's commands and 4.2's single retry / single `kickstart` rule ONLY; a failure here is terminal (`residents: DOWN`), never a second pass through §6.+} (5) Re-run 5.1's curl / heartbeat / validate / radar lines → quote them. (6) `ESCALATE` with the snapshot id, tag `pre-stack-0919`, the reverted range, which settings are live, what stays applied; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. Residents DOWN at the end is the one outcome the desk escalates by push at once.
