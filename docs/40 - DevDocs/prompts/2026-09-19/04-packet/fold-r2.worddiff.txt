diff --git a/Users/cobalt/.claude/jobs/c566654e/tmp/02.before-fold.md b/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md
index d7c0c1d..35f585c 100644
--- a/Users/cobalt/.claude/jobs/c566654e/tmp/02.before-fold.md
+++ b/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md	
@@ -18,7 +18,7 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`,
0.3 ROLLBACK DIRECTORY, built fresh by YOU (user data, gitignored `data/`, never `git add`, no value quoted): `mkdir -p /Users/cobalt/cobalt/data/backups/pre-stack-0919/daymode-settings-rollback`; Read the two forward files; Write `aset.yaml` there BYTE-IDENTICAL; Write `daymode.yaml` there = the forward file MINUS exactly the four-line `trade_count_over_band` row. `shasum -a 256` both → record. The rollback dry run (exact allowlisted line) → GATE: prints `no differences — the database already holds these settings.` (the CLI's zero-change wording; all seven keys listed `=`). Anything else → `FAILED PREFLIGHT: rollback file does not equal production`. (The FORWARD dry run cannot run here: main's live code rejects the new row at parse time; it is step 3.4.) Report committed by name. Last line `CONTINUE: step 1`.

## 1. Preconditions
1.1 `date` → the run STARTS only when now ≤ [-19:00-]{+18:30+} ET or ≥ 21:40 ET (every Cobalt settings `--apply` is refused by the `market_reset` guard 20:00–21:00; archiver 20:30, replay 21:05; a run that starts by [-19:00-]{+18:30+} has [-an hour-]{+ninety minutes+} of [-margin)-]{+margin, so §6 can never be entered inside the refused hour — deployment check 2026-09-19 V6)+} → otherwise `FAILED: window — wait for the desk`. THE CLOCK IS RE-CHECKED with `date` immediately before 3.2 (bootout), before 3.6 and before 3.7: at or after 19:40 ET and before 21:00 → do NOT proceed — before 3.2 that is a plain `FAILED: window`; after 3.2 go to §6 and read its CLOCK RULE (review finding 4). 1.2 `harness-round2-2026-09-19.md` ends `ROUND2 BUILT ad9b07c …` and `prod-proof-only-3-2026-09-19.md` ends `PROD PROOF DONE …` [-(neither-]{+(EITHER one missing or ending differently+} → `FAILED: gate — [-<which>`);-]{+<which>`; and the proof report must itself name the gated code: `grep -c ad9b07c` on `prod-proof-only-3-2026-09-19.md` ≥ 1, else `FAILED: gate — proof ran on other code` — deployment check V7); `git -C /Users/cobalt/cobalt-wt/s2-p2-cards log -1 --format=%D` shows `HEAD -> sprint-2/stack` (V8);+} `<gated code>` = `ad9b07c`: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff --stat ad9b07c HEAD -- [-src tests configs ops .gitignore`-]{+. ':(exclude)docs'`+} prints NOTHING (only report [-commits-]{+commits, all under `docs/`,+} sit above the gated [-code);-]{+code — the whole tree outside `docs/` is compared, not a list of folders: V2);+} `status --porcelain` empty; record `<n>` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rev-list --count main..HEAD`. 1.3 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; NO build or deploy session is merging (the desk launches you alone).

## 2. Machine-written files, snapshot, tags
2.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `configs/cobalt/rules.yaml`, `docs/40 - DevDocs/reports/seat-usage.md`, desk / deploy reports. `git add` BY NAME (never `-A`, never `data/`), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`.
@@ -26,12 +26,12 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`,
2.3 Report committed by name NOW — this is your LAST commit before the merge. Last line `CONTINUE: step 3`.

## 3. Rebase → residents DOWN → merge → migrate → rows → validate (no report prose between these commands; record outputs, write them up after 4.2)
3.0 `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase main` (brings the stack above your own commits; a conflict → `… rebase --abort`, `FAILED: rebase conflict — <paths>`; nothing is down yet). PROOF before going on: `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff --stat ad9b07c HEAD -- [-src tests configs ops .gitignore`-]{+. ':(exclude)docs'`+} prints NOTHING (what you merge is byte-identical to what the gate proved) {+— WITH ONE NAMED EXCEPTION (V1; it happened on 09-18): if your own step 2.1 committed main's machine-written `configs/cobalt/rules.yaml`, the rebase carries that file onto the branch and this diff lists exactly that ONE path; accept it ONLY when `git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff --stat main HEAD -- configs/cobalt/rules.yaml` prints nothing (the branch holds main's copy, unchanged). ANY other path in the diff → `FAILED: rebase changed code — <paths>` (nothing is down yet; do not go on)+} and `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rev-list --count main..HEAD` = `<n>` of 1.2 (the rebase dropped nothing). Record `<pre-merge>` = `git -C /Users/cobalt/cobalt rev-parse --short HEAD`.
3.1 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → baseline. `date`.
3.2 `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print` each → must FAIL with "Could not find service" (still printed → one `date`, print once more; still there → §6). A heartbeat beat landing in this window goes RED on the sheet/radar probes and DMs him — expected; note the beat time afterwards.
3.3 `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<pre-merge>` (else `FAILED: foreign commit on main — <hash>` → §6 steps 3–5 only: nothing merged, bring the residents up) → `git -C /Users/cobalt/cobalt merge --ff-only sprint-2/stack` → record the tip.
3.4 The over-band FORWARD dry run (exact line) → GATE: its output CONTAINS `1 setting(s) would change` (the CLI's full line is `DRY RUN — 1 setting(s) would change. Nothing written.`) AND the only key marked `~` is `daymode.stepdowns`, the other six `=`; else do NOT apply → §6 (settings untouched — say so) (review finding 3).
3.5 (WHAT CHANGED SINCE THE FIRST ATTEMPT: this step is where it died at 15:4x — the old harness built a >1 GB string over `system.bars`. The harness on this branch streams its proof and runs the whole migrate transaction at REPEATABLE READ: both probes read ONE snapshot plus the transaction's own changes, so a commit by another session between them (heartbeat and seat-usage write `system.cobalt_jobs`; `cobalt_redactions` and `vault_writes` append) can no longer read as `CHANGED`. It was proven READ-ONLY against production three times — `prod-proof-only-2026-09-18.md`, `prod-proof-only-2-2026-09-18.md`, and `prod-proof-only-3-2026-09-19.md` on the final code — at ≈46 s per pass over ≈8.6M bars. STATED FINDING (the build hub's ESCALATE 1, not to be re-derived): under REPEATABLE READ only a concurrent UPDATE or DELETE of a row this migration ALSO updates can raise `could not serialize access`; the registered set holds three row-level statements, `0002`'s UPDATE matches zero rows on production and the two seed INSERTs touch tables no job updates — so the exposure today is effectively nil. If it happens anyway the harness names it, exits non-zero, nothing is applied: that is a failed 3.5 → §6, say `serialization failure`, and do NOT rerun the command. EXPECT this command to sit SILENT for ≈1 minute in its BEFORE probe and again in its AFTER probe; that is the proof reading `bars`, not a hang — run it in the FOREGROUND, do not interrupt it, do not background it; it prints seconds per table. Budget ≈2–3 minutes for this step. **THE BASH TOOL'S DEFAULT TIMEOUT IS 120 000 ms — LESS THAN THIS COMMAND NEEDS: set the call's `timeout` parameter to 600000 for THIS call (and for 2.2's `backup run`).** A tool timeout here would kill the client mid-transaction: Postgres rolls the transaction back (nothing applied), but you must then treat it as a failed 3.5 → §6, and say `tool timeout`, not `migration [-error`.)-]{+error`. ONE CAVEAT (V4): the harness commits BEFORE it prints its proof table, so a timeout or a dropped connection in the instant after the commit leaves 0006/0007 APPLIED with no table printed. Whenever 3.5 ends without a complete proof table, do NOT write "nothing applied" from the absence of output: read it back first — `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT to_regclass('system.radar_score_run')"` → empty/NULL = not applied, a name = applied and committed — then go to §6 (the same path either way: the code is reverted, the additive schema stays) and label the FAILED line `migrations applied: yes/no` from that read.)+} `COBALT_ENV=production uv run cobalt db migrate --allow-prod` {+(THIS call: `timeout` 600000)+} → the runner walks ALL of `FORWARD` = 0001…0007 and prints `-- applying` for each; 0001–0005 re-run idempotently by design. GATE = its proof table: NO table marked `CHANGED`, and the ONLY `CREATED` objects are the ones `0006_radar_score` and `0007_radar_cards` define; quote the whole table (review finding 5). Non-zero exit → §6; output showing a migration PARTIALLY applied → run NO rollback, quote it, `FAILED: migration partial` after §6's code revert + residents up.
3.6 The over-band `--apply` (exact line) → quote `applied: {…}` and the `field-by-field diff EMPTY` line + L28's trace line (`approved by Dejan via the desk — cto-2026-09-19.md row <n>`, command, hashes, time).
3.7 The dark card `--dry-run` (exact line) → GATE: ONE add `radar.cards_enabled` = false, ZERO deletions (any existing `card.*` row listed for deletion → do not apply; §6) → the `--apply` line → quote it + the round trip `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → one row, false + the L28 trace line.
3.8 `COBALT_ENV=production uv run cobalt validate` → exit 0; `Step-downs:` carries `trade_count_over_band=down(1)`; ceiling / band lines unchanged. Red anywhere in 3.4–3.8 → §6.
@@ -47,4 +47,5 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd`,
`STACK DEPLOY DONE <hash> · LIVE: sprint-2/stack DARK y · ops-0918 y · migrations 0006/0007 applied · over-band row <time> · dark row <time> · RESTARTS done: com.cobalt.aset com.cobalt.radar · outage <mm:ss> · PUSH: Dejan · ESCALATE: <n>`

## 6. SAFE-STATE RULE — red at 3.4–3.8, red smoke, a failed bootstrap, or a mid-run denial after 3.2
{+STEP (0), FIRST, ALWAYS (V5 — inherited from `10`, never exercised: both 09-18 uses entered §6 with the residents already down): `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar`; if EITHER is loaded (you came from 4.2 or from a red smoke in §5) → `launchctl bootout` both and require "Could not find service" for both BEFORE you touch code or settings. A resident must never be running while its code is reverted or its row removed under it; `bootstrap` in (4) is only ever run on a service that is not loaded.+}
Goal: code and settings agree, residents UP, before you stop. THE TWO CONSISTENT PAIRS are {old code + six step-down rows} and {new code + seven rows}; either is a safe place to stop, a MIXED pair never is. CLOCK RULE (review finding 4): `date` first — inside 19:40–21:00 ET no settings `--apply` can run, so you do NOT change the row: if the over-band row IS applied, KEEP the new code (skip (1)–(2), go to (3)); if it is NOT applied, revert the code (1) and skip (2). (1) If 3.3 merged: `git -C /Users/cobalt/cobalt revert --no-edit <pre-merge>..HEAD` — the range starts at the commit IMMEDIATELY BEFORE the ff-merge, so it holds the stack's commits and nothing of yours (L54 — never `reset`). A CONFLICT during the revert (not expected on a linear range nothing was committed above): `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → make THAT pair consistent instead: if the over-band row is not applied yet and the clock allows, apply it (3.6) and the dark row (3.7), `validate`; then (3)–(6) with `safe state: NEW code kept — revert conflicted` (review finding 1). {+IF THE CLOCK DOES NOT ALLOW that apply (V6 — unreachable on a run that started by 18:30, written down anyway): leave BOTH residents DOWN (new code without the row refuses to load, so bringing them up would only crash-loop them), skip (4), and end `FAILED: <step> — revert conflicted inside the refused hour — safe state: red — residents: DOWN`; the desk escalates by push at once and completes the pair after 21:00.+} (2) If 3.6 applied, or you cannot prove it did not: the over-band ROLLBACK `--apply` (exact line) → then `COBALT_ENV=production uv run cobalt validate` → exit 0 on the reverted code. Code reverted + over-band row present, or row removed + new code merged, are BOTH broken states — never leave either. (3) The dark row and migrations 0006/0007 STAY (inert for old code / additive); the schema is never rolled back unattended — PROVE it with `validate` exit 0 and the sheet 200 after (4). (4) Bring the residents up exactly as 4.2. (5) Re-run 5.1's curl / heartbeat / validate / radar lines → quote them. (6) `ESCALATE` with the snapshot id, tag `pre-stack-0919`, the reverted range, which settings are live, what stays applied; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. Residents DOWN at the end is the one outcome the desk escalates by push at once.
