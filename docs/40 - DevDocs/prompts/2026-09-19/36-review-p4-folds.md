MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-p4-folds-0919`, launched by the CTO desk in the background, SAME worktree as round 1 (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `18-review-ops-0919.md`'s launch line, with exactly ONE string changed — the `mkdir` path, from `scratch/review-ops-0919` to `scratch/review-p4-folds-0919` — because this round needs no rule `18` did not already carry (same repository, same branch, same `git -C /Users/cobalt/cobalt show*`/`log*` reach; the unused `git -C .../s2-p2-cards *` rules stay, unused, for the same reason they stayed in `18`). Proof: sorted `comm -3` between `18`'s rule-string list and this prompt's rule-string list yields exactly one line on each side, both the `mkdir` string. If this round turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/36-review-p4-folds.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-folds-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-folds-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# P4 FOLDS CHECK — ONE fold check covering BOTH unread folds on `sprint-2/p4` (worktree `/Users/cobalt/cobalt-wt/s2-p4`), since the whole-build tribunal check all three houses already read (`REVIEW-C.md`, tip `a59ee8c`, `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/REVIEW-C.md`). Fold 1 = the fund rule, Dejan's ruling R16, commits `d5947e7..2738cdc`. Fold 2 = round 2 of the tribunal fixes (A1/A3/A4/A5), commits `2738cdc..9045260`. This is a FOLD-ONLY round: neither house re-reads the whole build; each reads the two folds' pre-staged diffs plus the context files the packet already gives them. DO NOT STOP until the report ends `P4 FOLDS CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Standing law: LAWS.md L67, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, R19-R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. This round's own debt: Dejan's R16 (`cto-2026-09-19.md` §4) named a "small tests-first chunk on `sprint-2/p4` … read by the houses as a fold of P4's build check" as NEXT; his R23 (`cto-2026-09-19.md` §4) named "the round-2 builder re-confirms with file:line or STOPS (then it comes back to him)" and the round-2 report's own stop line (`P4 ROUND2 9045260 …`) shows both folds landed and unread — prove with `git -C /Users/cobalt/cobalt-wt/s2-p4 log --oneline -6` showing `9045260` beneath the report commits, and `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` showing R16 and R23 committed. A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the packet's own `facts.md` (it is the hub's own claim too — verify it against the diffs, don't just relay it) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet for a given bucket · L47 a meter is checked BEFORE a house is counted on · L52 nothing in either fold may touch scoring, ranking or a card value without disclosure — fold 2's A3 is exactly this, already ruled (R23) · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out — Astra's meter is known OUT this round (see §4 below), so the floor is Grok + Gemini.

INDEX CARD (you; the reviewers get their assigned bucket only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-p4-folds-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. SECOND LESSON (carried from `18`/`22`): the Write tool strips trailing whitespace from copied diffs — a unified diff's blank context lines are a single space, Write turns them into empty lines; state the byte delta once in `## Packet`, never chase it. THIRD LESSON (carried from `29`, the Gemini harness lesson): packets over ~150 KB, or ones naming files not present in the folder, print nothing from Gemini — this packet's two combined folds run to ~188 KB total, over that line, so BOTH houses read it as TWO SEPARATE BUCKET CALLS, never one combined call (see §2-3 below). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-folds-0919/REVIEW.md` — same gitignored-scratch pattern, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/36-packet/`; copy each into `scratch/review-p4-folds-0919/` (`mkdir -p` first; Read → Write, byte-identical where the tool allows — see SECOND LESSON above)

Files, with byte counts to verify with `wc -c` after copying (re-stage ONLY a file whose count differs):

| file | bytes | for |
|---|---|---|
| `fund-rule.diff` | 86,951 | fund-rule bucket |
| `round2.diff` | 30,600 | round-2 bucket |
| `round2-report-section.md` | 45,593 | round-2 bucket |
| `verdicts.md` | 8,430 | both buckets |
| `rulings.md` | 2,377 | both buckets |
| `facts.md` | 6,367 | both buckets |
| `QUESTIONS.md` | 7,326 | both buckets |

`fund-rule.diff` is `git diff d5947e7..2738cdc -- src tests` from `/Users/cobalt/cobalt-wt/s2-p4`, WITH `tests/fixtures/replay/movers-gainers.real-shape.csv` and `tests/fixtures/replay/movers-losers.real-shape.csv` EXCLUDED — those two files are raw Finviz export rows (real tickers, real figures) and are barred from this packet under L32; `facts.md` names the exclusion and why nothing about the rule's correctness depends on reading those rows. `round2.diff` is `git diff 2738cdc..9045260 -- src tests`, complete, nothing excluded (checked clean of raw-export content). Verify both independently with `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*` is NOT the right remote — this branch is `sprint-2/p4`, reachable only through the pre-staged files here and, for spot verification by YOU (not the reviewers), `git -C /Users/cobalt/cobalt show sprint-2/p4:<path>` if that ref is fetched to `/Users/cobalt/cobalt`, else Read/`grep` under `/Users/cobalt/cobalt-wt/s2-p4/` (covered by `--add-dir /Users/cobalt/cobalt-wt`).

**BUCKETS** — the two folds sized together exceed a safe single-call packet, so each house reads them as two separate calls, never combined:
- **fund-rule bucket** (~111.5 KB): `fund-rule.diff`, `verdicts.md`, `rulings.md`, `facts.md`, `QUESTIONS.md`.
- **round-2 bucket** (~100.7 KB): `round2.diff`, `round2-report-section.md`, `verdicts.md`, `rulings.md`, `facts.md`, `QUESTIONS.md`.

`QUESTIONS.md` is shared and answerable from either bucket alone or both — each call answers what its bucket's files can address and marks the rest `N/A THIS CALL`; you combine both calls' answers per house into one set of Q1–Q9 tags in `## Verdict table`.

## 2. Grok — TWO calls, one per bucket, no shell, headless
Grok cannot run a shell in this mode — everything it needs is in the staged files. For EACH bucket, one `grok` call, `run_in_background`, one attempt, never a retry on the same bucket: point it at the bucket's files under `scratch/review-p4-folds-0919/` and have it follow `QUESTIONS.md` exactly, verbatim, answering only what that bucket's files support (`N/A THIS CALL` for the rest). Record each call's verbatim output as `grok-findings-fund-rule.md` / `grok-findings-round2.md`.

## 3. Gemini — TWO calls, one per bucket, one attempt each, never a retry
Same bucket split as Grok, same rule (a bucket that HARNESSes — any tool-permission denial, any "no output produced" — is recorded as `gemini bucket <fund-rule|round2>: HARNESS` and you move on; NEVER retried). Open each call with "You have NO tools for this task except reading local files already present in one folder. Never run a command, never list a directory, never fetch a URL, never search the web — if answering a question would require any of those, write UNVERIFIABLE FROM PACKET for that item and continue." then name the EXPLICIT file list for that bucket only (from the table above — none named that is absent), then "Follow QUESTIONS.md exactly, answering only the questions your files can address (mark the rest N/A THIS CALL). Do NOT write any file: print your complete findings as your answer." Write each call's printed answer, byte for byte, to `gemini-findings-fund-rule.md` / `gemini-findings-round2.md`.

## 4. Astra — probe once, do not retry
Astra's Codex meter is reported OUT until roughly 16:54 ET today; its last P4 read (the whole-build check) cost 331k tokens on this same branch. PROBE FIRST, `run_in_background`, one attempt: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."` — record verbatim. If METER (as expected): record `astra: METER` with the verbatim message and END for Astra — this is a correct, non-failing ending, never a second probe, never a loop, never a self-scheduled wait. If the probe unexpectedly returns UP: launch ONCE, `run_in_background`, both buckets in one call is acceptable for Astra only if their combined size is what a single Codex call can take (Astra has handled the full ~188 KB combined build before at higher cost; if in doubt, run it as the same two bucket calls as Gemini) — read `QUESTIONS.md` and the relevant bucket file(s), print findings, write to `astra-findings.md`, record token usage if the output states one.

## 5. Verify — using ONLY this launch line's rules, nothing new
Every REAL finding from Grok and/or Gemini (and Astra if it ran) against the REAL files: a diff claim against `git -C /Users/cobalt/cobalt-wt/s2-p4` via Read/`grep` under `--add-dir /Users/cobalt/cobalt-wt` (no Bash rule needed for a plain read), and against `rulings.md`/`verdicts.md`'s own quoted text for anything about what R16/R23/REVIEW-C actually say. If a verification genuinely needs a command this launch line does not cover (an independent re-derivation of either diff with a fresh `git diff` against `/Users/cobalt/cobalt-wt/s2-p4`, or `uv run pytest`/`uv run cobalt jobs restarts` to re-check the offline counts or RESTARTS claims in `facts.md`) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks shipping? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-p4-folds-0919/`.

## 6. Close
`## Verdict table` combines both houses' Q1–Q9 answers against the real files, one row per question, naming which house(s) answered it and from which bucket, plus any finding that duplicates something `verdicts.md` (REVIEW-C) or `rulings.md` (R16/R23) already settled — cite it, don't re-litigate it. Last line, beginning EXACTLY `P4 FOLDS CHECK`:
`P4 FOLDS CHECK · grok: <verdict|HARNESS|METER|TIMEOUT> (fund-rule: …, round2: …) · gemini: <verdict|HARNESS(n of 2 buckets)|METER|TIMEOUT> (fund-rule: …, round2: …) · astra: METER|<verdict>|not run · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>` or, on any authorization/harness failure severe enough to stop entirely, `FAILED: <step> — <concern>`. Then stop.
