STATUS: DEPLOY 2 of the weekend (R3/R5 of 09-18: three production deploys by Monday). Deploy 1 ran this morning and is on record: `02-deploy-stack-3.md` → `reports/deploy-2026-09-19.md`, `STACK DEPLOY DONE 2893a7f`, outage 2 min 40 s, 5 ESCALATE, §6 never entered. THIS file lands `sprint-2/p4` ONLY (S2-P4 · F3/F12/F13). `ops/2026-09-19` and `archiver/append-0919` are DEPLOY 3 (Sunday) and are NOT merged here — they enter this prompt only as the L68 gate's other legs. LAUNCHABLE ONLY with Dejan's approval row in `docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4` that names THIS file, committed on main, and only after `37-p4-db-run.md` has ended `P4 DB …` and `36-review-p4-folds.md` has ended with no shipping blocker (both verified in §2, both `FAILED: gate` if absent).

MODEL: Opus 5 (`claude-opus-5`) — Opus floor: production migrations 0008/0009 ship and this session runs a production write path (L29; 02 ran the same seat on the same floor) · SEAT: deploy hub `deploy-p4-0919`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md' and follow it exactly." --model claude-opus-5 --remote-control deploy-p4-0919 --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C /Users/cobalt/cobalt reset --soft HEAD~1)" "Bash(git -C /Users/cobalt/cobalt tag scratch-allow-probe-p4-0919)" "Bash(git -C /Users/cobalt/cobalt tag -d scratch-allow-probe-p4-0919)" "Bash(git -C /Users/cobalt/cobalt tag pre-p4-0919)" "Bash(git -C /Users/cobalt/cobalt tag deploy-2026-09-19b)" "Bash(git -C /Users/cobalt/cobalt merge --ff-only sprint-2/p4)" "Bash(git -C /Users/cobalt/cobalt revert --no-edit *)" "Bash(git -C /Users/cobalt/cobalt revert --abort)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p4 rebase main)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p4 rebase --abort)" "Bash(git -C /Users/cobalt/cobalt worktree add -b stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919 main)" "Bash(git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --no-edit *)" "Bash(git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --abort)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * rev-parse*)" "Bash(git -C * rev-list*)" "Bash(git -C * merge-base*)" "Bash(git -C * show*)" "Bash(cd *)" "Bash(uv run pytest *)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env)" "Bash(rm /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt jobs register)" "Bash(COBALT_ENV=production uv run cobalt jobs restarts *)" "Bash(COBALT_ENV=production uv run cobalt backup run*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(launchctl bootout gui/501/com.cobalt.aset)" "Bash(launchctl bootout gui/501/com.cobalt.radar)" "Bash(launchctl bootout gui/501/com.cobalt.replay)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.replay.plist)" "Bash(launchctl kickstart -k gui/501/com.cobalt.aset)" "Bash(launchctl kickstart -k gui/501/com.cobalt.radar)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it, NO production `pytest` (see WHY 2), NO `settings load` in any spelling (see WHY 6), NO PRODUCTION `--rollback`/`--down-to` — **and round 1 of the house check (finding 1, a BLOCKER) is why this sentence is now backed by the allowlist instead of by hope: `COBALT_ENV` does NOT decide the database when `--allow-prod` is present.** `src/cobalt/db_migrations/cli.py:542` (main `:537`) reads `dbname = db.PROD_DB_NAME if args.allow_prod else env.resolve_db_name()`, and `src/cobalt/db.py:146`'s `_prod_gate` only refuses when `allow_prod` is FALSE — so a rule like `Bash(COBALT_ENV=dev uv run cobalt db migrate*)` would have admitted `COBALT_ENV=dev uv run cobalt db migrate --allow-prod --rollback --down-to 0007`, i.e. a production schema rollback wearing a dev prefix. That wildcard is GONE: the three dev commands §1.4 types are allowlisted as exact strings with no `*`, and the only `--allow-prod` rules are the bare one and `--proof-only`. **SAID HONESTLY (desk probe, 2026-09-19 14:0x ET, `cto-2026-09-19.md` §61):** under `--permission-mode auto` the allowlist is a PRE-APPROVAL list, not a whitelist — a control session whose only rule was `Bash(touch /private/tmp/cobalt-probe-c)` still ran `touch /private/tmp/cobalt-probe-d`, a command no rule matched. So no rule list can make a command impossible to type; what keeps a production rollback out of this run is that this prompt never instructs one, forbids it in four places, and L62 makes any mid-run denial a FAILED run. Type only what a step tells you to type. The ONE rollback this session may type is the DEV-lane `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` of §1.4, on `cobalt_dev`, which the gate is required to run · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely and gets an answer; a dialog gets none).

# P4 DEPLOY 2026-09-19 (Saturday, daytime, weekend push R3/R5 — no scanning session today, no trading day) — ONE ff-merge of `sprint-2/p4`: S2-P4 (F3 movers/value · F12 picks/missed · F13 benchmark), production migrations **0008** (`system.radar_membership.rank_metric/rank_value` + `system.movers_daily`) and **0009** (`"user".picks`, `"user".missed`), the R16 fund rule as a `configs/cobalt/radar.yaml` shape change, and the NEW one-shot job `com.cobalt.replay` (`ops/com.cobalt.replay.plist`, Mon–Fri 21:10 ET, `RunAtLoad false`). Shape A (L66): both residents DOWN before the merge, UP after, plus a first bootstrap of `com.cobalt.replay`. STEP 1 is the L68 integrated gate — offline AND with-DB, on a branch stacking every unmerged branch, GREEN BEFORE the merge, never after it in `~/cobalt`. Expected outage ≈3–4 minutes (≈95 s of it is the migration's BEFORE+AFTER read of `system.bars`, deploy 1's measured number). NO settings write of any kind. DO NOT STOP this session until its report ends `P4 DEPLOY DONE …` or `FAILED …`.

WHY THIS SHAPE (each line is a failure already paid for, or a law that already cost one): (1) 09-17 and 09-18: two branches green alone, red together, twice — hence **L68**, and hence §1's gate runs on the STACKED tree BEFORE the merge, never after it in `~/cobalt`. (2) 09-18 11:34: `uv run pytest` in `~/cobalt` is NOT offline — `~/cobalt/.env` points it at `cobalt_dev` — so this prompt runs pytest ONLY inside a worktree, never with `/Users/cobalt/cobalt` as cwd, and after the merge it runs production `validate` + smoke only. (3) 09-17 19:18: launchd respawned the radar into merged code → residents are `bootout`ed BEFORE the merge (L66). (4) 09-18: a rebase-then-report-commit order broke `--ff-only` with the residents down → the rebase is the LAST thing before `bootout`, and NOTHING commits on main between the rebase and the merge (you hold your report writes; the desk holds its commits). (5) THE HARD PAIR IS AUTOMATIC HERE: P4's config-shape change (`not_equity: {header, values}` → `{asset_type_header, industry_header, industry_values}`, `extra="forbid"` on the old shape) lives in `configs/cobalt/radar.yaml`, a REPO file inside the merge — so a `git revert` of the merge range takes code and config back together in one action, and there is no settings row to travel with it (L42's "one action" is satisfied by the revert itself). (6) **CARDS ARE LIVE** since 12:30:01 ET today (`reports/cards-golive-2026-09-19.md`, R20): `radar.cards_enabled = true` in `"user".trader_settings`. This deploy **loads NO settings file, reads no card yaml, writes no `trader_settings` row, and its rollback NEVER turns the cards dark** — deploy 1's six `settings load` strings are DROPPED from the launch line above for exactly that reason, and the dark file is not reachable from this session. Cards are proven STILL ENABLED at 6.4 as a smoke check, never touched as a deploy action. (7) Migrations 0008/0009 are ADDITIVE (two nullable columns + three new tables) and P4's code is inert for the P2 code already in production, so a code revert leaves the schema in place; the schema is NEVER rolled back unattended (§8 (3)).

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval. His words, with times, are in the report files, never in this one.
- THE ROW: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4 Rulings`, rows **R30** (his "Approve", 2026-09-19 14:23 ET — the 23 rule strings, the window, the tag) **and R31** (his "A", 14:4x ET — the narrowed L68 scope, and the row that carries THIS FILE'S FINAL sha256 for run 2), which together must name THIS file (`38-deploy-p4.md`) and approve THIS launch line and the file's sha256 as recorded there. Verify: `shasum -a 256 "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md"` equals the hash in **R31** (R30's hash is run 1's and is superseded there in writing), and `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` shows the commit that carries it. **No committed row naming THIS file, a different hash, a rule string this prompt types that R30 does not carry, or a missing R30 or R31 → `FAILED: authorization mismatch — <what>`, commit the report, stop before PREFLIGHT.** R7 of today approved a DIFFERENT file (`02-deploy-stack-3.md`) and is NOT an approval of this one.
- STANDING ROWS that this deploy also rests on, each to be found committed (`git log --oneline -10 -- …`): `cto-2026-09-18.md` §4 **R3/R5** (the weekend push through 09-20), **R6** (the accepted baseline-RED class; aset bootstraps from the REPO plist), **R18–R21** (L67: a build checked by ≥3 tribunal members, a deployment by ≥1 other house). `cto-2026-09-19.md` §4 **R16** (the fund rule "C" — a radar behaviour change: non-blank `Asset Type` OR `Industry = Exchange Traded Fund`), **R19** (L68/L69/L70 folded; the desk pushes only on his word — push is not in your list at all), **R23** (the `pool.py:189` None-filter STAYS, disclosed — as corrected in §51: ONE filter, element 1, `pool.py:189`; `:198` was already on main).
- Laws that bind you: L1, L4, L28, L29, L32 (P4's fixtures and any Finviz value are user data — never quoted, never `git add`ed beyond the paths §3.1 names), L35, L42, L43, L46, L48, L51 (1–3 pre-approved: `COBALT_ENV=production`, committing a machine-written file, extracting a stage-2-retired blob), L54, L55, L57, L61, L62, L63, L66, **L67**, **L68**, L70.
YOU CAN ALWAYS STOP. Doubt → `FAILED: <step> — <concern>` as the last line, commit the report, stop. TWO ASYMMETRIES: (a) from the first `bootout` on, production's residents are DOWN — "stop" then means finishing §8's safe-state rule first (NN#16) — with ONE named exception, §4.2's terminal branch, which repairs the resident whose bootout took and stops WITHOUT entering §8 (entering it there would loop on the bootout that just failed); that exception is written out at the step and nowhere else; (b) while `.env` sits in the gate worktree, "stop" means removing it and proving it gone FIRST (L4), then writing the stop line.

INDEX CARD (read in this order, before PREFLIGHT): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` **IN FULL** (you run a production write path; L68 is new today) → **this prompt's facts packet** `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/38-packet/facts.md` (the desk's own pre-computed measurements — a CLAIM to check, never settled fact, L35) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-19.md` **IN FULL** (deploy 1, this morning: what worked — bootout/bootstrap, the tag as the durable rollback point, the 94.1 s migration proof, the §6 safe-state rule never needed — and all 5 ESCALATE) → `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md`: §0, `## ESCALATE` item **5 (a)–(e)** (the five deploy-prompt consequences), item **2** (K3/HOLD), the `## FUND RULE (R16)` section's ESCALATE **21 (a)–(d)**, `## ROUND 2`'s §0 + `## ROUND 2 ESCALATE` + its last line `P4 ROUND2 9045260 …` → `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/` the P4 DB run's own report section (last line `P4 DB …`, written by `37-p4-db-run.md`) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cards-golive-2026-09-19.md` (last line only: `CARDS LIVE 12:30:01 ET …`) → `/Users/cobalt/cobalt-wt/s2-p4/ops/com.cobalt.replay.plist` IN FULL (the job you bootstrap) → `/Users/cobalt/cobalt-wt/s2-p4/src/cobalt/db_migrations/0008_radar_value_movers.sql` and `0009_picks_missed.sql` (what CREATED is allowed to mean at 4.5).
REPORT (L48): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-p4-2026-09-19.md` — **it ALREADY EXISTS: run 1 (14:26–14:33 ET) stopped at the L68 gate with `FAILED: gate — conflict merging archiver/append-0919`, nothing deployed, production untouched. You ARE the rerun: append `# SECOND RUN` under everything there and rewrite NOTHING above it.** Read run 1's PREFLIGHT and §1.2 sections first — its 16 preflight rows and its `feca2cf` stack tip are evidence you may cite, but every command is RE-RUN, never assumed. Inside it: §0 Headline ≤5 lines → PREFLIGHT → L68 GATE → Preconditions → Deploy table → Smoke → Close → ESCALATE → last line.

WILDCARD AUDIT (round-1 finding 5, done after finding 1 showed the class is real — every `*` rule in the launch line, asked one question: does it admit a spelling that reaches PRODUCTION outside the step that needs it?). NARROWED, wildcard removed: `db migrate*` on the dev lane → three exact commands (finding 1: `--allow-prod` beats `COBALT_ENV`) · `tag *` → the four exact tag names this run types · `worktree add *` → the one exact command · `jobs *` → `jobs register` + `jobs restarts *`, because `cobalt jobs run <label> -- <cmd>` exists (`src/cobalt/jobs/cli.py:189`, "Run a command wrapped as a registered job") and would have wrapped ANY command under a `COBALT_ENV=production` prefix. KEPT, with the reason: the seven `git -C * <read>` rules and `heartbeat show*`, `validate*`, `backup status*` are read-only · `db query *` is read-only in code, not merely by convention (`src/cobalt/db_query.py` `guard_select` accepts exactly one `SELECT`/`WITH` and opens `BEGIN READ ONLY`) · `backup run*` writes only a snapshot, which is the point · `revert --no-edit *` and the stack `merge --no-edit *` are git-only and path-scoped · `cd *`, `uv run pytest *`, `COBALT_ENV=dev uv run pytest *` cannot reach `cobalt_brain` (pytest has no `--allow-prod` path and `requires_db` skips without `POSTGRES_*`) · `grep/tail/ls/wc/date/shasum` are reads · `launchctl print gui/501/*` is a read · the `curl` rule is a localhost GET. STILL OPEN, and the desk owns it, not you (round-1 finding 6, UNVERIFIABLE FROM READS): whether an UNSTARRED rule such as `Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)` matches by prefix on this host — if it did, it would admit the production rollback by itself. It is byte-identical to the rule Dejan approved for deploy 1 (R7) which ran clean this morning; you never type a longer spelling of it, and the desk settles the matcher question by experiment outside a deploy.

UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no pipe, no redirect, no `; echo`, no `$(…)`, no `&&`. `cd <path>` is its OWN call and is used ONLY to enter a worktree for a pytest run and to return; every git command is `git -C <absolute path> …` so cwd never decides a git answer. **CWD DISCIPLINE (the one thing that can silently ruin this run):** the session starts in `/Users/cobalt/cobalt`; the only place pytest may run is `/Users/cobalt/cobalt-wt/stack-deploy2-0919` or `/Users/cobalt/cobalt-wt/s2-p4`; before every pytest call, the immediately preceding call is `cd <that worktree>` and then `ls -la .env` (in the stack worktree during the with-DB step it must PRINT the file; everywhere else it must say "No such file"); after the last pytest call, `cd /Users/cobalt/cobalt` then `ls -la .env` — in `~/cobalt` that file EXISTS, and seeing it listed is your proof that cwd is back (never print its contents, never `cat`, never `grep` it). Outputs are read from the tool result; files are written with Write/Edit. MID-RUN DENIAL = THE RUN FAILED (L62): `.env` cleanup if present, then §8 if anything is merged or any resident is down — except inside §4.2's terminal branch, which is the one place that repairs and stops without §8 — then `FAILED: <step> — <command> — <reason string>`, commit, stop. Never another shape, never another session, never a retry of a denied command in a different spelling.

## 0. PREFLIGHT — nothing here changes production (residents stay UP)
0.1 One row each (rule · command · exit · allowed/DENIED + reason verbatim):
· **P1** `git -C /Users/cobalt/cobalt status --porcelain` AND `git -C /Users/cobalt/cobalt status` (LONG form, quoted in full — no "rebase in progress", no "unmerged paths", branch `main`).
· **P2** `git -C /Users/cobalt/cobalt tag scratch-allow-probe-p4-0919` then `git -C /Users/cobalt/cobalt tag -d scratch-allow-probe-p4-0919`.
· **P3** `git -C /Users/cobalt/cobalt commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` then `git -C /Users/cobalt/cobalt reset --soft HEAD~1`; `git -C /Users/cobalt/cobalt log --oneline -1` proves HEAD is back.
· **P4** `date` → the WINDOW rule of §2.1 is read here for the record, not enforced yet.
· **P5** `COBALT_ENV=production uv run cobalt validate` → exit 0. Record the `Jobs (F17):` line verbatim (expected **15 registered — 6 resident, 9 one-shot**: the pre-merge state; `com.cobalt.replay` is not there yet).
· **P6** `COBALT_ENV=production uv run cobalt backup status` and `COBALT_ENV=production uv run cobalt backup run --dry-run`.
· **P7** `COBALT_ENV=production uv run cobalt jobs restarts main..sprint-2/p4` — record VERBATIM. Expected `RESTARTS: com.cobalt.aset com.cobalt.radar` with `ops/com.cobalt.replay.plist A → new one-shot, bootstrap once: com.cobalt.replay` and 0 UNCLASSIFIED. A non-zero exit or any UNCLASSIFIED path → record it; it is a §2 gate, not a stop here.
· **P8** `COBALT_ENV=production uv run cobalt heartbeat show` — deploy 1 found GREEN at 08:16 with only `AMB com.cobalt.herdr unmanaged` (declared interim). EXPECT GREEN. R6's gate still stands if a RED is there: a PRE-EXISTING RED is accepted ONLY if it is the `radar` probe reading `failed_stage bars: poll failures: <n>` AND `tail -n 12 /Users/cobalt/cobalt/logs/radar.err` shows `radar cycle: idle:<session>` lines ≤4 minutes apart with no traceback (Saturday = outside every scanning session, so `idle:overnight` is the correct shape — say so); record it verbatim as the BASELINE. Any other RED → `FAILED PREFLIGHT: production not green`.
· **P9** `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → both running; record both pids and both `path =` lines (aset = `/Users/cobalt/cobalt/ops/com.cobalt.aset.plist`, radar = `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`; a different path → `FAILED PREFLIGHT: plist path moved`). Also `launchctl print gui/501/com.cobalt.replay` → MUST fail "Could not find service" (it does not exist yet; if it IS loaded, someone bootstrapped it already → `FAILED PREFLIGHT: com.cobalt.replay already loaded — <output>`).
· **P10** `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` → 200 and `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` → 200.
· **P11** `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(schemaname ORDER BY schemaname) FROM pg_tables WHERE schemaname IN ('public','user','system') GROUP BY tablename HAVING count(*) > 1"` → ZERO rows (the harness REFUSES a table name that exists in more than one of those schemas; a duplicate would fail 4.5 with the residents down). Any row → `FAILED PREFLIGHT: duplicate table name — <name> in <schemas>` (a human decision, never a retry).
· **P12** (new for P4, same class as P11) `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN ('movers_daily','picks','missed')"` → ZERO rows. A row means one of 0008/0009's three new names already exists somewhere in production and the migration would either collide or create the ambiguity P11 tests for → `FAILED PREFLIGHT: table name already present — <schema>.<table>`.
· **P13** `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only` (the exact line R7 approved this morning for `03-prod-proof-only-3.md`, run here as a preflight read). **THIS CALL: set the Bash tool's `timeout` parameter to 600000** — the default 120 000 ms is LESS than it needs; it sits silent ≈50 s per probe reading `system.bars` (8.8 M rows). It applies NOTHING and ends `NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.` Quote the whole proof table and the `proof cost:` line — that number is this deploy's outage budget. **(Round-1 finding 14, corrected:) do NOT expect this probe to mention 0008/0009's tables at all.** It runs from `/Users/cobalt/cobalt`, i.e. main's code, and `_probe_all` (`src/cobalt/db_migrations/cli.py:280-282`) enumerates the STATIC table list of the code it runs from — main proves 23 tables (deploy 1's table), P4's tree proves 26. So `movers_daily`/`picks`/`missed` are simply not in this probe's world; their absence is proven by P12's catalog query, not here. This step's gate is exactly three things: exit 0, no table marked `CHANGED`, no tool timeout. A `CHANGED` verdict on ANY table, a non-zero exit, or a tool timeout → `FAILED PREFLIGHT: proof-only <what>` (nothing was applied; production is untouched; do NOT rerun).
· **P14** `.env` LANE CHECK — `ls -la /Users/cobalt/cobalt-wt/s2-p4/.env`, `ls -la /Users/cobalt/cobalt-wt/ops-2026-09-19/.env`, `ls -la /Users/cobalt/cobalt-wt/archiver-append/.env`, `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` → every one MUST say "No such file". Any hit means another session may hold `cobalt_dev` → `FAILED PREFLIGHT: .env present in <path> — another session may hold cobalt_dev`.
· **P15 — THE ARCHIVER PRECONDITION: RECORDED, NO LONGER A GATE (R31).** §1's gate no longer stacks `archiver/append-0919`, so its `requires_db` state cannot make this gate red. Two things are still recorded, because the desk wants them in this report: (1) read `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` and quote its LAST line — it should read `ARCHIVER DB2 f5df6c1 … db 161/0 …`, i.e. the three reds that once blocked this gate are GONE; (2) `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` for the branch tip. Neither is a stop: a different line, or none, is RECORDED and the run continues. Run 1 of this prompt found this precondition MET and then failed on the merge instead (`deploy-p4-2026-09-19.md`).
· **P16 — the other two branches' own state, recorded not judged:** Read the last line of `/Users/cobalt/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md` (expected `OPS 0919 DB … | db 61/0 | cobalt_dev: 0001–0009, unchanged`) and of `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` (`P4 DB …`, the §2.2 gate). A non-zero `failed` in either → `FAILED PREFLIGHT: <branch> carries red requires_db tests — L68 gate cannot be green`.
0.2 `GATE: proven — commit + tag allowed by the session allowlist` (L62's launch proof), or the FAILED line.
0.3 NOT preflighted, because no harmless variant exists inside their own pattern (UNATTENDED-LAUNCH §6) — each is probed by its first real use: `worktree add`, the stack `merge --no-edit`, `rebase`, `bootout`, `bootstrap`, `kickstart`, `merge --ff-only`, `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.
0.4 Report written and committed by explicit name. Last line `CONTINUE: step 1`.

## 1. THE L68 INTEGRATED GATE — green BEFORE anything is merged
**The law, verbatim (LAWS.md L68, ruled today):** *"No branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green — the offline suite and the with-DB suite run on the branch that combines them, before the merge, never after it in `~/cobalt`."* **SCOPE RULED BY DEJAN 2026-09-19 14:4x ET (`cto-2026-09-19.md` §4 R31 "A"), after run 1 of this prompt stopped here:** the stacked tree combines `sprint-2/p4` and `ops/2026-09-19` — **`archiver/append-0919` is NOT stacked in this gate**. Why: run 1 merged P4 clean (163 files) and ops clean (13 files) to tip `feca2cf`, then hit a 9-file CONFLICT on the archiver branch, five files of which this prompt had named in advance; that branch was cut from a pre-P4 `main`, its migration registry deliberately leaves the 0008/0009 gap for P4, and it must be REBASED before deploy 3 ships it whatever happens tonight. It is not landing tonight, so its seam is proven at deploy 3's own gate, on the tree that actually lands then. This gate proves the seam between what is merging now and what merges next; the merge at 4.3 still ships `sprint-2/p4` ALONE.
1.1 `git -C /Users/cobalt/cobalt log --oneline -1` (main's tip, record as `<main-at-gate>`) · `git -C /Users/cobalt/cobalt-wt/s2-p4 log --oneline -1` · `git -C /Users/cobalt/cobalt-wt/ops-2026-09-19 log --oneline -1` · `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` (RECORDED ONLY — that branch is not stacked, R31) · `git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` empty. Record all four tips in the report; the facts packet names the tips the desk saw, and a DIFFERENT tip is recorded, not a stop (a branch may have advanced) — but a dirty `s2-p4` worktree IS a stop: `FAILED: gate — s2-p4 not clean`.
1.2 BUILD THE STACK: `git -C /Users/cobalt/cobalt worktree add -b stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919 main`. **RERUN CASE (round-1 finding 17; run 1 of this prompt WAS such a case — the desk removed `stack/deploy2-0919` and its worktree at 14:3x ET before relaunching, verified by `git worktree list`):** this command FAILS if the branch or the path already exists, which is exactly what a second launch after a failed gate looks like — no `worktree remove` / `branch -D` rule exists in your list, deliberately. So on that failure: `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919` to show what is there, then `FAILED: gate — stack artifact from an earlier run is still on disk (branch `stack/deploy2-0919` and/or its worktree); the desk removes it before the next launch`. Do not reuse the existing worktree (its content is from the earlier branch tips), do not pick another name. If `worktree add` SUCCEEDED → then, in this order, TWO separate calls (R31 — the archiver merge that run 1 typed here is REMOVED, not skipped silently): `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --no-edit sprint-2/p4` · `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --no-edit ops/2026-09-19`. Run 1 got both of these CLEAN (163 files, then 13 files, tip `feca2cf`); a conflict on either one now is NEW and is reported, never resolved here. A CONFLICT on any of them → `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --abort`, then `FAILED: gate — conflict merging <branch> — <paths>` (NOTHING is down, production is untouched, the desk resolves it in a build run, never here). **RUN 1 ALREADY PROVED THIS SEAM, which is why the archiver leg is gone:** P4 and the archiver branch both edit `src/cobalt/db_migrations/__init__.py` (the `FORWARD`/`REVERSE` tuples — P4 adds 0008/0009, the archiver adds 0010/0011) and both touch `src/cobalt/db_migrations/placement.py`, `src/cobalt/cli.py`, `src/cobalt/archiver/runner.py` and `store.py`; run 1's third merge conflicted in exactly those five plus three test files and one DevDoc page (`deploy-p4-2026-09-19.md` §1.2, verbatim output). That is a REAL integration seam, it is recorded, and it is deploy 3's to resolve by rebasing the archiver branch. Between P4 and ops there is no such overlap and run 1 proved it. Any conflict you hit now is reported, never resolved here. Record the stack tip as `<stack>` and `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 log --oneline -6`; also `grep -n "0008\|0009\|0010\|0011" /Users/cobalt/cobalt-wt/stack-deploy2-0919/src/cobalt/db_migrations/__init__.py` → with the archiver leg gone (R31) the merged `FORWARD` must list **0001…0009 in numeric order** with `REVERSE` its mirror, and **0010/0011 must be ABSENT** (they live only on the unstacked archiver branch); anything else → `FAILED: gate — merged migration registry is wrong — <what you read>`.
1.3 OFFLINE SUITE ON THE STACK: `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` → "No such file" (a pytest run with `.env` present is NOT offline) → `cd /Users/cobalt/cobalt-wt/stack-deploy2-0919` → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`; read the summary from the tool result). GATE: **`0 failed`**. Quote the whole summary line. For context only, never as a pass/fail rule: P4 alone closed at `1829 passed, 324 skipped`; ops-0919 at `1608/0`; the archiver at `1866 passed, 320 skipped` — the stack's own number is its own, and only `failed == 0` is the bar. Any failure → name each test, quote each assertion, `FAILED: gate — offline suite red on the stack — <tests>`.
1.4 WITH-DB SUITE ON THE STACK (`cobalt_dev` only — never production, never `--allow-prod`). **THE STEP ORDER IS PART OF THE GATE.** With the archiver leg gone (R31) the stacked registry is **0001–0009** and `cobalt_dev` is ALREADY at 0001–0009, so the forward step creates NOTHING and the restore step reverses NOTHING — both are run anyway, as proofs that the lane is what it claims to be, and their EXPECTATIONS below are rewritten accordingly. The order is kept because it is the order every other lane uses and because a surprise in it is exactly what this gate is for. Historical reason for the order, from the archiver's own DB run: with `cobalt_dev` at 0001–0009 and the archiver's two tables dropped, **20 of its 23 `requires_db` tests could not run at all** — its run 1 went `12 failed, 145 passed` purely on `relation … does not exist`, because only 3 of them are self-contained round trips; the rest need the tables to EXIST (seven via the `real_connect` fixture, thirteen via `BarStore().ensure_schema()`, which creates neither). That is the shape this order was built for. In this exact order:
  (a) `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` (by name, never read, never printed, L4) → `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` prints it.
  (b) BASELINE: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote the table. EXPECT `cobalt_dev` at **0001–0009** with `archive_progress` and `archive_incidents` ABSENT (`-`) and P4's `movers_daily`/`picks`/`missed` PRESENT — that is where the archiver and P4 DB runs each left it, and run 1 of this prompt did not touch it (it never reached §1.4). Record every row count and digest: this table is what (f) is compared against. A `CHANGED` verdict → `.env` cleanup, then `FAILED: gate — cobalt_dev proof CHANGED before the gate — <line>`.
  (c) FORWARD: `COBALT_ENV=dev uv run cobalt db migrate` (the stacked registry is **0001–0009**, R31; all nine re-run idempotently by design) → quote the full table. EXPECT **NOTHING CREATED** — `archive_progress` and `archive_incidents` stay ABSENT, because 0010/0011 are not in this stack — every table `OK` with its digest unchanged, `content UNCHANGED on every table`, no `CHANGED`. A `CREATED` line here would mean the stack carries a migration it must not. Anything else → **go to (h) and follow it in order**, ending `FAILED: gate — dev forward migrate <what>`.
  (d) SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 failed`** — quote the summary line. For context only, never as the bar: P4's own DB close was `2136/0` plus the two owed tests `37` ran; the archiver's scoped run was `154/3`, and **those three reds must already be gone by P15 or you never reached this step**. A red → **go to (h) and follow it in order**, ending `FAILED: gate — with-DB suite red on the stack — <test names + assertions verbatim>`. **Never label a red "known" and continue** (L68 admits no exception; L70: a red someone actually ran is a fact, not an escalate to carry).
  (e) STATE THE END STATE PLAINLY, in the report, before you run the restore: `cobalt_dev holds after the suite: 0001–0009, archiver tables ABSENT` — the gate added nothing this time (R31), which is itself the thing to say plainly.
  (f) RESTORE — run it even though it has nothing to reverse, as the proof that the lane behaves: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → EXPECT **NOTHING applied** (the stack's registry stops at 0009, so `_rollback_paths` finds no file above it). P4's 0008/0009 are NOT touched — if the output reverses anything numbered ≤ 0009, that is a serious finding: quote it, `.env` cleanup, `FAILED: gate — dev rollback went below 0009 — <what>` → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → compare row for row against (b)'s baseline: **every table identical, row count and digest, and the two archiver tables still ABSENT** — this gate should end exactly where it started. A table whose digest moved is NAMED with both values, and (round-1 finding 20) it is also a STOP unless it is one of the two archiver tables this step just dropped: a moved digest on ANY other table → `.env` cleanup, then `FAILED: gate — cobalt_dev digest moved on <table>: <before> → <after>`. The one benign class the archiver's own run documents — rows another lane committed between two proofs — is still a stop HERE, because no other lane is supposed to be holding `cobalt_dev` while this gate runs (P14 proved that at preflight).
  (g) `rm /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` → `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` says "No such file". **The `.env` cleanup happens before ANY stop line, including every red branch above** — a stop line written while `.env` is on disk is itself a failure condition: go back, remove it, prove it, then fix the line.
  **(h) RED-BRANCH RESTORE (round-1 finding 4; premise corrected round 2, finding N2).** With the archiver leg gone (R31) neither (c) nor (d) can leave `cobalt_dev` above 0009 at all — the stack has no migration above it — so this branch is now a belt-and-braces proof rather than a cleanup; run it anyway and say what you proved. A red (c) usually leaves NOTHING applied — `cmd_migrate` applies, probes and commits in ONE transaction and rolls it back on `CHANGED` or error — so there (f) is a no-op restore: its `--rollback --down-to 0009` finds 0010/0011 absent and its two rollback files are `DROP TABLE IF EXISTS`. Run it anyway, because you cannot tell from the output alone how far a partial (c) got, and a no-op restore is free. So on a red at (c) or (d): FIRST run (f) in full — the `--rollback --down-to 0009` and its closing `--proof-only` — THEN (g)'s `.env` cleanup, THEN the FAILED line, and end that line with `cobalt_dev: <state you proved>`. This is a `cobalt_dev` command on a dev database with production untouched; it is always safe to run and it is never skipped because the gate went red. If the restore ITSELF errors, do not retry it: quote the error and write `FAILED: gate — <original reason> — cobalt_dev: <the last state you PROVED — (b)'s baseline 0001–0009 when (c) printed a rolled-back run and (f) printed nothing applied; 0001–0011 when (c) or (d) got that far; UNKNOWN when you cannot tell from what was printed>, restore failed: <error>` so the desk knows exactly what to clean (round-3 finding R1: this line used to assert 0001–0011 unconditionally, which the corrected premise above makes false for a red (c)).
1.5 `cd /Users/cobalt/cobalt` → `ls -la .env` (the file IS listed here — cwd is back; contents never read) → `git -C /Users/cobalt/cobalt log --oneline -1` = `<main-at-gate>` (nobody committed while you ran; if main moved, record it and go on — §4.2 re-checks it where it matters).
1.6 Write the gate into the report as its own section: the four tips, `<stack>`, the merged `FORWARD` line, both suite lines VERBATIM, all THREE `--proof-only` verdicts (baseline / post-forward table / post-rollback), the forward and rollback tables, the `.env` proof, and P15's quoted archiver line with its date. If any part of the gate was not run, say WHICH and WHY in that section — a gate section with a missing step is a `FAILED: gate — incomplete`, never a green one. **The gate branch `stack/deploy2-0919` and its worktree are a GATE ARTIFACT, not work to land: they are never merged, never pushed, never rebased, and they stay on disk for the desk to remove at the close — record them in ESCALATE as cleanup owed.** Report committed by name. Last line `CONTINUE: step 2`.

## 2. Preconditions
2.1 **WINDOW.** `date` → the run STARTS only when now is **≤ 18:30 ET on Saturday 2026-09-19**. WHY THIS IS LAWFUL AND WHY THE HOUR MATTERS: L43 binds `com.cobalt.radar` restarts to the 20:00–21:00 ET `market_reset` pause "or during overnight idle … **on a trading day**; never while a scanning session is open" — today is Saturday, not a trading day, and the radar reads `idle:overnight` at every hour, so no scanning session can be open and L43's trading-day window does not bind; L66's own requirement (residents down before the merge and up after) is met by §4 regardless of hour. This is the same basis deploy 1 ran on at 08:19 ET this morning under 09-18 R3/R5. The 18:30 cap is not L43 — it is margin against **the nightly backup, `com.cobalt.backup`, which runs at 21:40 ET on ALL SEVEN days** (`configs/cobalt/jobs.yaml:224` `weekdays: [0,1,2,3,4,5,6]`, `ops/com.cobalt.backup.plist:72-79`) and must never fire into a half-deployed tree. **Round-1 finding 13b, corrected here:** `com.cobalt.archiver` (20:30) and `com.cobalt.replay` (21:10) are **Mon–Fri only** (`ops/com.cobalt.archiver.plist:56-57`, Weekday 1–5) and therefore cannot fire on a Saturday at all — the earlier wording named the archiver as tonight's risk and was wrong. Besides the backup, the only thing running today is `com.cobalt.heartbeat` (every interval, all week). Now > 18:30 → `FAILED: window — wait for the desk` (the desk re-approves a new window; you never pick one).
**THE CLOCK IS RE-CHECKED with `date` ONCE, immediately before 4.2 (bootout), and nowhere else. At or after 19:40 ET at that check → `FAILED: window`, nothing is down, nothing is merged, stop.**
**THE POINT OF NO RETURN (round-1 finding 2, a BLOCKER in the previous wording — read this twice).** The earlier text re-checked the clock "again at 5.2" and sent a late run to §8; that would have taken a GREEN merge and a GREEN migration and REVERTED them for no reason but the hour, leaving 0008/0009 applied over reverted code, the replay job unarmed and a second outage. It is deleted. The rule is now: **once 4.3's `merge --ff-only` has returned success, this run FINISHES FORWARD.** From that instant the clock is a MEASUREMENT, never a trigger — the `date` calls at 4.2 and 5.2 exist to compute the outage length and to stamp the report, and no hour, however late, sends a successful merge+migration to §8. §8 is entered ONLY for a RED result (a failed step, a red smoke, a mid-run denial), never for a late clock. If the run is still going at 21:40 and the backup fires beside it, that is an ESCALATE line in the report, not a revert. Conversely nothing after 4.2 may be ABANDONED for the hour either: finish §5's residents-up, finish §6's smoke, write the stop line. Said once more without the shorthand (round-2 finding N4): once 4.3 returns success the CLOCK never sends this run to §8 — a RED result at 4.3–4.6 or in §6's smoke still does, and §8 is then run as the step that sent you there specifies (4.3's foreign-commit branch says steps 3–5 only; 5.2's says (0) through (3); everything else enters at (0) and each of §8's steps is conditional on its own trigger).
2.2 **THE TWO P4 GATES** (L67 + L68's companions): (a) the P4 DB run's report section, written by `37-p4-db-run.md` in `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md`, ends `P4 DB <sha> | … | owed requires_db: <names → PASSED> | …` with both owed tests PASSED and `0` failed — missing, or ending `FAILED`, or either owed test not passed → `FAILED: gate — P4 DB run <what>`; (b) the houses' fold read `36-review-p4-folds.md` has landed and the desk's §4/§51-class section in `cto-2026-09-19.md` records it with NO blocker for shipping — absent → `FAILED: gate — L67 house check not recorded`. Neither is re-derived by you; you read the stop line and the desk's recorded verdict, and quote both.
2.3 **THE GATED CODE.** `<gated>` = the `sprint-2/p4` tip named in the approval row. `git -C /Users/cobalt/cobalt-wt/s2-p4 log -1 --format=%D` shows `HEAD -> sprint-2/p4`; `git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` empty; `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat <gated> HEAD -- . ':(exclude)docs'` prints NOTHING (only report commits may sit above the gated code — the whole tree outside `docs/` is compared, never a hand-listed set of folders); record `<n>` = `git -C /Users/cobalt/cobalt-wt/s2-p4 rev-list --count main..HEAD`.
2.4 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; no build or deploy session is merging (the desk launches you alone). `git -C /Users/cobalt/cobalt merge-base main sprint-2/p4` → record it (the desk saw `919d562`, and `git -C /Users/cobalt/cobalt diff --stat 919d562 main -- . ':(exclude)docs'` printed NOTHING — main has moved by DOCS ONLY since P4 branched, which is why 4.1's rebase cannot change code; re-prove that with the merge-base you actually find, and a NON-EMPTY diff there → record it and treat 4.1's proof as the real gate, not this one).

## 3. Machine-written files, snapshot, tag
3.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `docs/40 - DevDocs/reports/seat-usage.md`, the desk's live `cto-2026-09-19.md`, your own report, and the two untracked `docs/40 - DevDocs/prompts/2026-09-19/31-packet/*.yaml` copies the desk left (those are copies of files under gitignored `data/backups/` — **do NOT `git add` them**, name them in ESCALATE and leave them untracked). `git add` BY NAME (never `-A`, never `.`, never `data/`, never a directory), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`.
3.2 `COBALT_ENV=production uv run cobalt backup run` (**THIS CALL: `timeout` 600000**) → quote the `cobalt_brain` dump size line and the restic snapshot id; `COBALT_ENV=production uv run cobalt backup status` shows it newest. No snapshot id → `FAILED: snapshot`. `git -C /Users/cobalt/cobalt tag pre-p4-0919`. (Known, ops ESCALATE 4: the 21:40 nightly prunes this snapshot — record id + time; **the durable rollback point is the tag `pre-p4-0919`**.) There is NO rollback DIRECTORY in this deploy: no settings file is read or written, so there is nothing to stage (deploy 1's `mkdir -p …/daymode-settings-rollback` and its four `settings load --from` strings are absent from your allowlist by design).
3.3 Report committed by name NOW — **this is your LAST commit before the merge**. Last line `CONTINUE: step 4`.

## 4. Rebase → residents DOWN → merge → migrate → validate (no report prose between these commands; record outputs, write them up after 5.2)
4.1 `git -C /Users/cobalt/cobalt-wt/s2-p4 rebase main` (brings P4 above your own commits; a conflict → `git -C /Users/cobalt/cobalt-wt/s2-p4 rebase --abort` then `FAILED: rebase conflict — <paths>`; nothing is down yet). **PROOF before going on:** `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat <gated> HEAD -- . ':(exclude)docs'` prints NOTHING (what you merge is byte-identical to what §1 gated and §2.2 proved). ONE NAMED EXCEPTION (it happened on 09-18): if your own 3.1 committed a machine-written `configs/cobalt/rules.yaml`, the rebase carries that file onto the branch and this diff lists exactly that ONE path — accept it ONLY when `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat main HEAD -- configs/cobalt/rules.yaml` prints nothing. ANY other path → `FAILED: rebase changed code — <paths>` (nothing is down; do not go on). Then `git -C /Users/cobalt/cobalt-wt/s2-p4 rev-list --count main..HEAD` = `<n>` of 2.3 (the rebase dropped nothing). Record `<pre-merge>` = `git -C /Users/cobalt/cobalt rev-parse --short HEAD`.
4.2 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → baseline. `date` (the 2.1 re-check). Then **residents DOWN (L66)**: `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → each must FAIL with "Could not find service" (still printed → one `date`, print once more; **still loaded after that one retry → the run STOPS — but not before you put the OTHER resident back. REPAIR FIRST (round-2 finding N1, all three reading calls): `bootout` is two separate commands, so one of them may have SUCCEEDED; for EACH label whose `launchctl print` now says "Could not find service" (i.e. the bootout that DID take), run 5.2's bootstrap for that label ONCE — `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` for aset, `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for radar — then `launchctl print gui/501/<label>` once more and record `state`. ONE attempt per label, no `kickstart`, no second pass. THEN write `FAILED: residents not stopped — <label> — <launchctl output verbatim>`, and say plainly that NOTHING was merged and nothing was migrated, so production's CODE is untouched; state the services from what you just printed, as `residents: aset <up/DOWN>, radar <up/DOWN>`, NEVER as "as they were". Do NOT enter §8** — §8's step (0) is a bootout, which is the thing that just failed; entering it would loop). A heartbeat beat landing in this window goes RED on the sheet/radar probes and DMs him — expected; note the beat time afterwards.
4.3 `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<pre-merge>` (else `FAILED: foreign commit on main — <hash>` → §8 steps 3–5 only: nothing merged, bring the residents up) → `git -C /Users/cobalt/cobalt merge --ff-only sprint-2/p4` → record the tip and the file/insertion counts.
4.4 (nothing here — there is no settings dry-run in this deploy; the config-shape change rode in with 4.3.)
4.5 **THE MIGRATION.** `COBALT_ENV=production uv run cobalt db migrate --allow-prod` — **THIS CALL: set the Bash tool's `timeout` parameter to 600000.** EXPECT it to sit SILENT ≈50 s in its BEFORE probe and again in its AFTER probe (that is the proof reading `system.bars`, not a hang): run it in the FOREGROUND, do not interrupt it, do not background it. Budget ≈2–3 minutes. The runner walks ALL of `FORWARD` = 0001…0009 and prints `-- applying` for each; 0001–0007 re-run idempotently by design. **GATE = its proof table:** NO table marked `CHANGED`, and the only `CREATED` objects are the ones `0008_radar_value_movers.sql` and `0009_picks_missed.sql` define — read those two files (index card) and hold the output to them: `system.movers_daily`, `"user".picks`, `"user".missed`, plus `system.radar_membership` gaining `rank_metric`/`rank_value` as ADDED COLUMNS on an `OK` table, never as a `CHANGED` verdict. Quote the whole table and the `proof cost:` line. Take each new table's SIDE from the printed table, never from memory — 6.3 queries them with that side. STATED FINDINGS, not to be re-derived: (i) the whole migrate transaction runs at REPEATABLE READ, so a concurrent commit cannot read as `CHANGED`; only a concurrent UPDATE/DELETE of a row this migration also updates can raise `could not serialize access` — if it happens the harness names it, exits non-zero and applies NOTHING: that is a failed 4.5 → §8, say `serialization failure`, and do NOT rerun the command. (ii) The harness COMMITS BEFORE it prints its proof table, so a timeout or dropped connection in the instant after the commit leaves 0008/0009 APPLIED with no table printed. **Whenever 4.5 ends without a complete proof table, do NOT write "nothing applied" from the absence of output: read it back first** — `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT to_regclass('system.movers_daily')"` → empty/NULL = not applied, a name = applied and committed — then go to §8 (the same path either way: the code is reverted, the additive schema stays) and label the FAILED line `migrations applied: yes/no` from that read. A tool timeout is labelled `tool timeout`, never `migration error`. Non-zero exit → §8; output showing a migration PARTIALLY applied → run NO rollback, quote it, `FAILED: migration partial` after §8's code revert + residents up.
4.6 `COBALT_ENV=production uv run cobalt validate` → exit 0. Record the `Jobs (F17):` line (expected **16 registered — 6 resident, 10 one-shot** once 5.1 registers; at this point it may still read 15 — say which), `registry <-> ops/` and `registry <-> plists` lines, and `Placement: tree clean`. Red anywhere in 4.3–4.6 → §8.

## 5. Residents UP + the `com.cobalt.replay` first bootstrap
5.1 `COBALT_ENV=production uv run cobalt jobs register` → quote the count (expected `registered 16 job(s)`; 15 before the merge — the new one is `com.cobalt.replay`). Then `COBALT_ENV=production uv run cobalt jobs restarts <pre-merge>..HEAD` VERBATIM; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED, with `ops/com.cobalt.replay.plist A → new one-shot, bootstrap once: com.cobalt.replay` on its own row. A THIRD resident derived → ESCALATE, not restarted today. Any UNCLASSIFIED path → ESCALATE by name, never dropped (L42).
5.2 `date` (**MEASUREMENT ONLY** — it stamps the outage length; it is never a gate and never sends this run to §8, §2.1's point of no return) → `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` (the REPO plist, R6) · `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` · `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → `state = running`, NEW pids, `path =` lines unchanged. `Bootstrap failed: 5` → one `date`, retry ONCE. Loaded but not running → `launchctl kickstart -k gui/501/<label>` ONCE. Still not running → `FAILED: residents DOWN — <label> — <launchctl output verbatim>` after running §8 from its step (0) through (3) — step (0) boots out the resident that DID start, so nothing runs while the code is reverted — and then, unless §8 itself ended in one of its terminal residents-DOWN branches (those stand), ONE bootstrap attempt of both on the reverted, validated tree (this step's commands, the same single retry / single `kickstart`). Whatever is not running after that stays down: that is the terminal state; §8 is never entered a second time. `date` → outage length (4.2 → 5.2).
5.3 **`com.cobalt.replay` FIRST BOOTSTRAP (L55: Code installs and proves every scheduled job it ships).** `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.replay.plist` → `launchctl print gui/501/com.cobalt.replay` → quote `state` and the calendar entries. The plist declares `RunAtLoad false` and `StartCalendarInterval` **Mon–Fri 21:10 local (ET)**, so bootstrapping it on a SATURDAY arms it and runs NOTHING tonight — its first run is **Monday 2026-09-21 21:10 ET**, after the 20:30 archiver it depends on and after the `market_reset` pause. Say that sentence in the report. `Bootstrap failed: 5` → one `date`, retry ONCE; still failing → this is an ESCALATE, **not** a §8 trigger and **not** a deploy failure: the two residents are up, the merge stands, the job is simply unarmed — record `com.cobalt.replay: NOT bootstrapped — <output verbatim>` and carry it into the stop line's ESCALATE count. A failed bootstrap of a job that runs Monday never justifies reverting a green deploy.

## 6. Smoke — ALL delivered functionality (NN#16), each quoted in `## Smoke`
6.1 (every `curl` is typed in its FULL allowlisted spelling, never the abbreviation printed here) `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` = 200 · `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` = 200 · `COBALT_ENV=production uv run cobalt heartbeat show` → **no NEW RED, and a NEW RED sends this run to §8** (round-1 finding 7 — §8's own header lists "red smoke" but this line did not say so). TWO named exceptions, neither of which is a new RED: the P8 BASELINE class recorded at preflight, and a RED whose timestamp falls INSIDE the outage window 4.2→5.2 (the 15-minute beat landing while the residents were down) — name either verbatim and go on. Anything else red → §8 · `COBALT_ENV=production uv run cobalt validate` exit 0, with the `Jobs (F17)` / `registry <-> ops/` / `registry <-> plists` lines quoted (this is `com.cobalt.replay`'s registry proof) · `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` against 4.2's baseline.
6.2 LOGS: `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` → started on the new code, no traceback, **no `CardSettingsError`, no `trade_def_slug`, no `radar_pool_failed_stage_check`** (the 09-17 incident signatures), and **no `not_equity` / config-shape error** — the R16 rule renamed `radar.yaml`'s `not_equity` keys and the loader refuses the OLD shape by name, so a parse error here means the merged config and the merged code disagree → §8. Outside a scanning session the correct cycle line is `radar cycle: idle:overnight scan_id=None` ≈100 s apart — say so. · `tail -n 30 /Users/cobalt/cobalt/logs/aset.log` → no traceback, no degraded day-mode banner.
6.3 P4's OWN surfaces, read-only: for each of `movers_daily`, `picks`, `missed`, ONE `COBALT_ENV=production uv run cobalt db query --side <the side 4.5's proof table printed for that table> --prod "SELECT count(*) FROM <table>"` → a number, no error (0 rows is the expected and correct case: nothing has written them yet) · `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT count(*) FROM radar_membership WHERE rank_metric IS NOT NULL"` → a number, no error (0008's two columns exist and are readable; 0 is expected — no scan has run since they were added). Never guess a side; take it from the table you quoted.
6.4 **CARDS ARE STILL LIVE** (the one card check in this deploy, and it is a READ): `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → **one row, True**. Anything else — a missing row, `False`, more than one row — means something in this deploy touched a settings row it must never touch → `FAILED: cards state changed — <what>` and §8 (whose (3) leaves every settings row exactly as it is). This is a verification, never a repair: you have no `settings load` command of any kind.
6.5 **`cobalt smoke s2` IS NOT RUN, and its redness is not a deploy verdict.** Two of its checks are RED BY DESIGN tonight and neither is a defect: (a) **K9** — the replay `job.result` shape gained `movers_by_side`, and until the first `com.cobalt.replay` run writes a job row carrying it, K9.2/K9.5 read FAIL and K9.3/K9.6 ERROR; the first such row is **Monday 2026-09-21 21:10 ET** (`s2-p4-verify-2026-09-19.md` ESCALATE 15 / 5(a)); (b) **K3** — a legitimate frozen HOLD grades red: a pre-deploy episode held with `rank_metric` NULL and a post-cutoff `last_scan_id` makes `rescanned_admitted ≥ 1` and `rescanned_metric_missing ≥ 1`, and the framework's grader returns FAIL naming `rescanned_metric_missing`, indistinguishable by column from a RETAIN defect — confirmed on real Postgres (`s2-p4-verify-2026-09-19.md` §4.6), carried as **ESCALATE 2 / tribunal A7**, and the desk's ruling on it is OWED and is NOT this hub's to take. **Therefore: do not run `cobalt smoke s2`, do not add it to any check list, and never write "smoke red" as this deploy's verdict.** NN#16's "full smoke of ALL delivered functionality" is met by 6.1–6.4, which are deploy 1's checks plus P4's own three. If some other command's output happens to print a K3 or K9 line, record it verbatim under `## Smoke` labelled `RED BY DESIGN — ESCALATE 2 / ESCALATE 15, not a deploy failure`, and continue.
6.6 NOT DONE HERE, deliberately: no `radar evaluate --replay` (deploy 1 ran it as P2's DARK proof; cards are LIVE now and this hub has not established that a replay stays read-only with them enabled — it is not in your allowlist, so it cannot be typed) · no scan proof: Saturday is overnight idle throughout, so the first live proof of the R16 fund rule and of the new columns is **Monday 2026-09-21 04:00 ET premarket**, proven by that day's day-open; write that sentence.

## 7. Close
Green → `git -C /Users/cobalt/cobalt tag deploy-2026-09-19b`. **The tag name is fixed at approval and is never re-derived by you**: `deploy-2026-09-19` is already taken by this morning's deploy, so this one is `deploy-2026-09-19b` — the second deploy of the same date. If `date` says the day is no longer 2026-09-19 when you reach §2.1, that is `FAILED: window` at 2.1 and you never reach here; you do NOT silently rename the tag to another day. Then: the Deploy table, `RESTARTS done`, the outage length, `com.cobalt.replay` armed with its first run time, the membership gap (4.2 → first completed scan = Monday 04:00 ET), and the two "first live proof" sentences of 6.6. ESCALATE section: the gate artifacts (`stack/deploy2-0919` + its worktree) owed for cleanup, the two untracked `31-packet/*.yaml`, the carried P4 escalates the desk still owes a ruling on (A2 deadline scope, A7/K3, ESCALATE 19 ticker pinning), plus anything you found. Report committed by name. Last line, exactly this shape:
`P4 DEPLOY DONE <hash> · LIVE: sprint-2/p4 · migrations 0008/0009 applied · gate: stack <stack> offline <p>/<f>, db <p>/<f> · RESTARTS done: com.cobalt.aset com.cobalt.radar · com.cobalt.replay <bootstrapped, first run Mon 21:10 ET | NOT bootstrapped: <reason>> · cards: LIVE, untouched · outage <mm:ss> · tag deploy-2026-09-19b · PUSH: Dejan · ESCALATE: <n>`
(Round-1 finding 18: that slot is filled from what 5.3 actually printed. The line never says "bootstrapped" when 5.3 recorded `NOT bootstrapped` — a stop line that contradicts its own report is worse than a failed bootstrap.)
or `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`.

## 8. SAFE-STATE RULE — red at 4.3–4.6, red smoke, a failed resident bootstrap, or a mid-run denial after 4.2
STEP (0), FIRST, ALWAYS: `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar`; if EITHER is loaded (you came from 5.2 or from a red smoke in §6) → `launchctl bootout` both and require "Could not find service" for both BEFORE you touch code. A resident must never be running while its code is reverted under it; `bootstrap` in (4) is only ever run on a service that is not loaded. **IF `com.cobalt.replay` WAS BOOTSTRAPPED AT 5.3, BOOT IT OUT TOO** (round-1 finding 3): `launchctl bootout gui/501/com.cobalt.replay` → `launchctl print gui/501/com.cobalt.replay` must fail "Could not find service". WHY this changed: the whole `src/cobalt/replay` package arrives WITH P4 and is absent from main, so after a §8 revert a job left armed would fire `uv run cobalt replay nightly` on Monday 21:10 ET against a tree that has no `replay` module — a loud Monday-night failure nobody is watching for. Record it in the ESCALATE line either way (`com.cobalt.replay: booted out` / `never bootstrapped`). If the bootout does not take after one retry, say so plainly in the ESCALATE and in the FAILED line — it is not worth a second pass through this section, but the desk must know a dead job is armed for Monday.
GOAL: code and config agree, residents UP, before you stop. **THIS DEPLOY HAS NO SETTINGS LEG** — no `trader_settings` row was written, so there is no second half to restore and no `market_reset` clock to beat. CLOCK NOTE: the 19:40–21:00 ET refusal that bound deploy 1 applied to `settings load --apply`; nothing here is refused by it, and **no hour ever sends a green result into this section** (§2.1's point of no return). `com.cobalt.archiver` and `com.cobalt.replay` are Mon–Fri and cannot fire on a Saturday; the only job that fires tonight is the 21:40 backup (all seven days). If §8 is still running as 21:40 approaches, finish (0)–(5) anyway — a half-finished safe state is worse than a snapshot taken beside one — and let the ESCALATE say so.
(1) **If 4.3 merged:** `git -C /Users/cobalt/cobalt revert --no-edit <pre-merge>..HEAD` — the range starts at the commit IMMEDIATELY BEFORE the ff-merge, so it holds P4's commits and nothing of yours (L54 — **never `reset --hard`**, which is not in your allowlist in any spelling). This reverts P4's code AND `configs/cobalt/radar.yaml` together, which is the whole hard pair (WHY 5). A CONFLICT during the revert (not expected on a linear range nothing was committed above): `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → that pair is ALREADY consistent (code + its own config, both from the merge), so make THAT your safe state: `COBALT_ENV=production uv run cobalt validate` exit 0, then (3)–(6) with `safe state: NEW code kept — revert conflicted`.
(2) (no settings step — see the GOAL note above.)
(3) **Migrations 0008/0009 STAY.** They are additive (two nullable columns on `system.radar_membership`; three new empty tables) and the pre-P4 code neither reads nor writes them, so reverted code runs unchanged over them. **The schema is NEVER rolled back unattended** — no PRODUCTION rollback is instructed anywhere in this prompt and none is allowlisted as its own rule: `--allow-prod` is allowlisted only bare and with `--proof-only`, and you never type `--allow-prod --rollback --down-to …` in any branch (the desk's probe above is why this sentence says "never typed" rather than "cannot be typed") (the dev-lane rollback of §1.4 is a different command against `cobalt_dev` and has nothing to do with production); the rollback files (`0009_picks_missed.rollback.sql`, `0008_radar_value_movers.rollback.sql`, reversed by `COBALT_ENV=production uv run cobalt db migrate --allow-prod --rollback --down-to 0007`) exist and are the DESK's documented path with Dejan, run as its own approved job, never from inside a failing deploy. PROVE the schema is harmless where it stands: `validate` exit 0 and the sheet 200 after (4). Likewise **no settings row is touched** — cards stay LIVE through every branch of this section.
(4) Bring the residents up with 5.2's commands and 5.2's single retry / single `kickstart` rule ONLY; a failure here is terminal (`residents: DOWN`), never a second pass through §8.
(5) Re-run 6.1's curl / heartbeat / validate lines and 6.4's cards read → quote them.
(6) `ESCALATE` with the snapshot id, the tag `pre-p4-0919`, the reverted range, what stays applied (0008/0009), the state of `com.cobalt.replay`, and the gate artifacts; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. **Residents DOWN at the end is the one outcome the desk escalates by push at once.**
