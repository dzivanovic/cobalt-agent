MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-ops-r2-0919`, launched by the CTO desk in the background, SAME worktree as round 1 (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `18-review-ops-0919.md`'s launch line, with exactly ONE string changed — the `mkdir` path, from `scratch/review-ops-0919` to `scratch/review-ops-r2-0919` — because this round needs no rule `18` did not already carry (same repository, same branch, same `git -C /Users/cobalt/cobalt show*`/`log*` reach; the unused `git -C .../s2-p2-cards *` rules stay, unused, for the same reason they stayed in `18`). Proof: sorted `comm -3` between `18`'s rule-string list and this prompt's rule-string list yields exactly one line on each side, both the `mkdir` string. If this round turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/22-review-ops-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-ops-r2-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-ops-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of branch `ops/2026-09-19`, ROUND 2 of ≤3 (LAWS L67) — verifying the FOLD, not the whole build again. Round 1 (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-0919/REVIEW.md`) ended 3 of 3 FIX FIRST, hub-verified 8 REAL (R2, R3, R5, R6, R7, R8, R9; R1 disclosed-and-carried, R4 NOT REAL, R10/R11 UNVERIFIABLE FROM READS at the time). A round-2 build session then folded fixes for R2/R3/R5/R6/R7/R8/R9 into the branch (report tip `d860bf7`, its own stop line names `0ec31f8` — that is round 1's own report-vs-tip-commit pattern repeating, not a discrepancy; see `facts.md`). R1 and R11 remain CARRIED (owed a `cobalt_dev` experiment another session holds); R4 got no change (it was NOT REAL). **The houses read ONLY THE FOLD** — the pre-computed diff of what changed since round 1's build tip, plus round 1's own verdict table — not the whole branch again. DO NOT STOP until the report ends `OPS CHECK R2 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Same standing law as round 1: LAWS.md L67, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, R19-R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The fold's own debt: round 1's report (`docs/40 - DevDocs/reports/ops-2026-09-19.md`, committed on `ops/2026-09-19`) named "three-house check" as OWED, this branch's round-2 report ends "next three-house check on this diff, then the deploy prompt" — prove with `git -C /Users/cobalt/cobalt log --oneline -4 ops/2026-09-19 -- "docs/40 - DevDocs/reports/ops-2026-09-19.md"` (LESSON, carried from `18`: the same command with NO branch named returns EMPTY on `/Users/cobalt/cobalt` — the report is committed only on the branch; name the ref explicitly or the check reads as a false negative). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the fold's own report's claims (`facts.md` in the packet is the hub's own claim too — verify it against the diffs, don't just relay it) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-ops-r2-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. SECOND LESSON (carried from `18`): the Write tool strips trailing whitespace from copied diffs — a unified diff's blank context lines are a single space, Write turns them into empty lines. `facts.md` in this packet already counts how many such lines `fold.diff` and `fold-report.diff` each have; when you copy them into `scratch/`, re-run the same `grep -c "^ $"` check on your copies and note the byte delta in `## Packet` as a fidelity note, not a defect. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-r2-0919/REVIEW.md` — same gitignored-scratch pattern, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/22-packet/`; copy each into `scratch/review-ops-r2-0919/` (`mkdir -p` first; Read → Write, byte-identical where the tool allows — see SECOND LESSON above)
(1) `fold.diff` = `22-packet/fold.diff` — `git -C /Users/cobalt/cobalt diff fc1d967..ops/2026-09-19 -- src tests`, the hub's pre-computed diff of everything that changed under `src/` and `tests/` across the WHOLE range since round 1's build tip (this necessarily includes R2/R3/R5's fixes; it does not include anything from `configs/` or `ops/`, which the range does not touch at all — worth confirming, not assuming) · (2) `fold-report.diff` = `22-packet/fold-report.diff` — the same commit range, filtered to `docs/40 - DevDocs/reports/ops-2026-09-19.md` only, covering R6-R9's text corrections and the new `## ROUND 2` section round 1's report gained · (3) `verdicts-r1.md` = `22-packet/verdicts-r1.md`, verbatim copy of round 1's `## Verdict table` (`19-packet/verdicts.md`) — what round 1 found REAL and what fix each finding was owed; QUESTIONS.md calls this the SPEC for what "fixed" means, not a claim to re-litigate unless the code disagrees · (4) `facts.md` = `22-packet/facts.md`, the hub's own pre-computed measurements: commit list, diff-stat, per-item file:line matched against round 1's "fix in one line" column, the offline-count (1608/1608) explanation, and the ROUND 2 ESCALATE carry-forward — a CLAIM to spot-check against the diffs, same standing as `build-report.md` had in round 1, never settled fact on its own · (5) `QUESTIONS.md`, verbatim below. If a file is too large for one Write call, split `name.part1`, `name.part2` and say so in `## Packet`.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — everything you need is already here as a file. This is ROUND 2 of the three-house check of branch `ops/2026-09-19`. Round 1 (`verdicts-r1.md`, the `## Verdict table` from `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-0919/REVIEW.md`) found 8 REAL findings (R2, R3, R5-R9; R4 was NOT REAL; R10-R11 were UNVERIFIABLE FROM READS at the time). A build session then folded fixes for R2, R3, R5, R6, R7, R8, R9 — R1 and R11 are CARRIED (they ride with an owed `cobalt_dev` experiment another session holds) and R4 got no change (it was NOT REAL). `fold.diff` is `git diff fc1d967..ops/2026-09-19 -- src tests` — the code and test changes since round 1's build tip, covering R2/R3/R5. `fold-report.diff` is the same commit range for the report file only, covering R6-R9's wording corrections and the new `## ROUND 2` section. `facts.md` is the hub's own pre-computed measurements (commit list, diff-stat, per-item file:line, the offline-count explanation, the ESCALATE carry-forward) — treat facts.md as a claim to spot-check against the diffs, same as you would the build's own report, not as settled truth you may skip verifying. For each finding give SEVERITY (BLOCKER = the fix does not do what round 1 asked, breaks an existing invariant, or contradicts a LAW (L1 fail-loud, L3 one-path, L45 real-shape fixtures, L52 nothing here may touch scoring/ranking/cards) / MAJOR / MINOR), FILE + LINE (from the diff), the EVIDENCE you read (quote it), and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) R2 — `test_the_alter_waits_for_an_open_transaction_and_then_completes`'s cleanup. Does `fold.diff` move `conn.cancel()` + `thread.join(...)` into the `finally` block UNCONDITIONALLY (not gated on `if hung:` anymore), ahead of the `if not thread.is_alive(): conn.rollback(); conn.close()` line? Is the old `try`-body copy of `cancel`/`join` actually REMOVED (one copy only, L3), or does the diff leave a second copy behind? Does the fix change what the test's own assertions check — it should not; this is cleanup-path hygiene only. (Q2) R3 — the '20 s join fires first' docstring. Does `fold.diff` show the docstring's claim made CONDITIONAL (naming the 5 s headroom / probe-speed dependency it actually relies on), rather than removed or left unconditional? Is this a docstring-only change (fix (a) per `facts.md`), or does the diff also touch the join/timeout CODE (fix (b)) — if both appear, is that consistent with round 1's verdict table's 'your call, name which you chose,' or is a mix undisclosed? (Q3) R5 — the lock-timeout message. Does `fold.diff` show the `except psycopg.errors.LockNotAvailable` hint text in `db_migrations/cli.py` changed away from `pg_locks WHERE NOT granted`? Does the new text query for a GRANTED lock (or use `pg_blocking_pids`/`pg_stat_activity`), i.e. does it now identify a HOLDER rather than a waiter? Does the rest of the message (NOTHING WAS APPLIED, the lock_timeout value, the retry guidance, `Postgres said: {e}`) stay untouched? Does `fold.diff` show a NEW or CHANGED assertion in `test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied` that is FALSE against the OLD message text and TRUE against the new one? Is the existing `assert 'pg_locks' in message` removed — if so, is that consistent with the fix, and if not removed, is `pg_locks` really still present in the new message text? (Q4) R6-R9 — read `fold-report.diff` for the corrected lines only (ignore the ~430-line new `## ROUND 2` section for this question). For each of R6, R7, R8, R9: does the diff show the exact correction the verdict table asked for? Quote before/after from the diff. Is each correction marked INLINE as a correction, not a silent rewrite? (Q5) R9 specifically — `facts.md` says the fix took 'option (ii): pointer dropped,' carrying the premarket-before-open case in a NEW `## ROUND 2 ESCALATE` section instead of round 1's own `## ESCALATE` list (to avoid invalidating round 1's already-committed 'ESCALATE: 9' stop line). Does `fold-report.diff` support that account — is round 1's `## ESCALATE` section left untouched, and does a genuinely new numbered item appear in a `## ROUND 2 ESCALATE` section instead? Is this an honest resolution, or does it just move the same gap somewhere a reader of round 1's list alone would still miss it — say which, and if dishonest or inadequate, say exactly why. (Q6) THE OFFLINE COUNT. `facts.md` claims the suite stayed at '1608 passed, 298 skipped, 0 failed' in both rounds because no new test FUNCTION was added. Does `fold.diff` support this — does any `+def test_` line appear anywhere in the diff? Is the test R5 modifies NOT decorated `@requires_db` (does it really run offline)? Is the test R2/R3 modify decorated `@requires_db` (confirm the decorator sits directly above it)? If any of this doesn't hold, say so — it would mean the round-2 report's RED/GREEN claim for R5 is unverified too. (Q7) DID THE FOLD BREAK ANYTHING new. Does `fold.diff` touch anything outside `src/cobalt/db_migrations/cli.py` and `tests/cobalt/test_migrate_proof.py`? Does it touch `configs/` or `ops/`? Does anything in the diff reach scoring, ranking, or a value that lands on a trading card (L52)? Any secret, credential, path, or connection string visible in either diff? Does the diff introduce a second copy of a rule/helper that already exists elsewhere (L3), beyond what R2's own comment claims ('only one copy')? (Q8) CARRIED ITEMS — R1, R4, R11. Does `fold.diff` show ANY change touching R1's or R4's code (the `_probe_all`/`SET LOCAL lock_timeout` placement at the top of `cmd_migrate`, or the `code:`/`FAILED:` ordering at the bottom)? It should show none — if it shows any, name exactly what changed and whether it was authorized. End with exactly one line per round-1 finding, in this exact order: `R2: FIXED / NOT FIXED / REGRESSED`, `R3: FIXED / NOT FIXED / REGRESSED`, `R5: FIXED / NOT FIXED / REGRESSED`, `R6: FIXED / NOT FIXED / REGRESSED`, `R7: FIXED / NOT FIXED / REGRESSED`, `R8: FIXED / NOT FIXED / REGRESSED`, `R9: FIXED / NOT FIXED / REGRESSED`. Then, on its own final line: `VERDICT: FOLD CLEAN / FIX FIRST <finding numbers> / FOLD UNSAFE`."

## 2-4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-ops-r2-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: `fold.diff`/`fold-report.diff` against `git -C /Users/cobalt/cobalt show ops/2026-09-19:<path>` (the branch's tip content — reachable under the SAME `show*` rule, no worktree `cd` needed) and against the real files under `/Users/cobalt/cobalt-wt/ops-2026-09-19/` with Read and `grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (for example, an independent re-derivation of `fold.diff` itself with a fresh `git diff` against `/Users/cobalt/cobalt-wt/ops-2026-09-19` rather than reading the pre-staged file, or re-running `uv run pytest`/`uv run cobalt jobs restarts` to re-check R10's numbers) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-ops-r2-0919/`. Last line, with 18's fields plus, per round-1 finding R2/R3/R5/R6/R7/R8/R9, a FIXED / NOT FIXED / REGRESSED tag: `OPS CHECK R2 · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u> · R2: <FIXED|NOT FIXED|REGRESSED> · R3: <FIXED|NOT FIXED|REGRESSED> · R5: <FIXED|NOT FIXED|REGRESSED> · R6: <FIXED|NOT FIXED|REGRESSED> · R7: <FIXED|NOT FIXED|REGRESSED> · R8: <FIXED|NOT FIXED|REGRESSED> · R9: <FIXED|NOT FIXED|REGRESSED>`. Then stop.
