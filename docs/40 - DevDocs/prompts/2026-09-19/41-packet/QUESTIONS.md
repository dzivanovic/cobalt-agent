You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — everything you need is already here as a file. This packet may reach you as TWO calls (bucket A and bucket B); `38-deploy-p4.md` is in both. Answer the questions you can from the files in FRONT of you; for a question whose evidence sits in the other bucket, say `OTHER BUCKET` rather than guessing.

`38-deploy-p4.md` is an UNATTENDED production deploy prompt for a live trading-support system. It has not run and has not been approved. It will merge one branch (`sprint-2/p4`) into `/Users/cobalt/cobalt`, which IS production, apply two schema migrations (0008, 0009) to a live Postgres 16 database holding 8.8 M rows, stop and restart the two resident services, and bootstrap a new scheduled job. Nobody will be watching it run. `02-deploy-stack-3.md` is its direct precedent: the same desk's deploy prompt from this morning, approved by the owner and already run — `deploy-1-report.md` is exactly what it printed. `facts.md` is the desk's own pre-computed measurements — treat every line in it as a CLAIM to check, never as settled fact. `approval-list.md` names the 14 rule strings that differ from the approved precedent. `laws.md` carries the four laws that bind this deploy, verbatim. `archiver-db-reds.md` is the archiver branch's own DB-run evidence.

For every finding give SEVERITY (**BLOCKER** = it will fail mid-deploy, or can leave production broken, down, or in a mixed state / **MAJOR** / **MINOR**), the STEP number in `38-deploy-p4.md`, the EVIDENCE you read (quote the line), and the FAILING SCENARIO in one sentence. Say "none found" rather than inventing. A finding that only a running command could settle is `UNVERIFIABLE FROM READS` — name the exact command, do not assume the answer.

**(Q1) ALLOWLIST ↔ STEPS, both directions.** The launch line is line 3 of the prompt. (a) Does every command any step tells the session to run appear in the allowlist, character for character, including flags and paths? Look hardest at: §1.2's `worktree add -b stack/deploy2-0919 … main`; §1.4's four `COBALT_ENV=dev … db migrate` spellings (`--proof-only`, bare, `--rollback --down-to 0009`); §0 P13's `--allow-prod --proof-only`; §5.3's replay `bootstrap`; every `git -C` path (does `Bash(git -C * status*)` really cover `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 log --oneline -6`?); the `grep -n …` of the merged migration registry in §1.2; `ls -la .env` in several places. One mismatch = a DENIED command, and after §4.2 that means denied with production's services DOWN. (b) The reverse: is any rule BROADER than the step that needs it, in a way that matters here — name each and say what else it would permit (e.g. `Bash(cd *)`, `Bash(uv run pytest *)`, `Bash(COBALT_ENV=dev uv run cobalt db migrate*)`, `Bash(git -C /Users/cobalt/cobalt worktree add *)`, `Bash(git -C * diff*)`). (c) Is there any spelling by which this session could reach a production schema rollback, a `push`, a force flag, or a `reset`?

**(Q2) CARDS — the one thing that must not move.** Production has `radar.cards_enabled = true` since 12:30:01 ET today. Read every step: is there ANY path — normal, smoke-red, mid-run denial, or §8 safe-state — by which this prompt reads, writes, deletes or reverts a `"user".trader_settings` row, loads a settings yaml, or turns the cards dark? Is the claim that the dropped `settings load` strings make this impossible actually true from the allowlist, or only from the prose? Does §6.4's read-back (`SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'`) use a rule that exists, and is its FAILED branch (`FAILED: cards state changed`) reachable and correct?

**(Q3) ROLLBACK COMPLETENESS.** §8 reverts the merge with `git revert --no-edit <pre-merge>..HEAD` (L54 forbids `reset --hard`). (a) Is `<pre-merge>..HEAD` the right range given that §3.3 says the report commit is the LAST commit before the merge — does that range hold P4's commits and none of the hub's own? (b) Migrations 0008/0009 STAY applied on a revert. Is "additive, therefore inert for the reverted code" actually true — two nullable columns added to `system.radar_membership` plus three new empty tables? Could pre-P4 code break on a table or column it does not know about? (c) The revert takes `configs/cobalt/radar.yaml`'s `not_equity` shape change back with the code because it is a repo file — is that reasoning right, and is there any window in which merged code and merged config disagree? (d) If §5.3 already bootstrapped `com.cobalt.replay` and §8 then reverts the code, the plist stays loaded and the job is scheduled for Monday 21:10 ET against reverted code — what happens, and does the prompt handle it?

**(Q4) L66 ORDERING.** Quote L66 from `laws.md`, then check §4.2 → §4.3 → §4.5 → §5.2 against it. Are both residents proven DOWN before the merge AND before the migration? Is the "Could not find service" proof sufficient, or can a service be booted out and still respawn? Is the `com.cobalt.replay` bootstrap in the right place (after the residents are up, §5.3) — or should it be inside the outage window? Does anything run between the rebase (§4.1) and the merge (§4.3) that could break `--ff-only`?

**(Q5) THE L68 GATE — is it sound?** Read `laws.md`'s L68 and its closing note, then §0 P15/P16 and §1. (a) Does §1 actually prove what L68 asks, given that the stack carries code (ops + archiver) that will NOT ship — can a green superset hide a red in the shipping tree, and does §4.1's byte-identity proof close that hole? (b) `archiver-db-reds.md`: three `requires_db` tests are RED on `archiver/append-0919` (DB-1 ×2, the `cobalt_user` grant; DB-2, the unobservable commit). P15 makes a later `db <p>/0` line a hard precondition and refuses "green except known reds" — is that the right call, and does the prompt leave any way to reach §1.4 with those reds still red? (c) The step ORDER in §1.4 (baseline proof → forward apply 0001–0011 → suite → state → rollback to 0009 → proof): is it right, and does it leave `cobalt_dev` where other lanes expect it (0001–0009)? Would anything be wrong if the deploy's own P4 migrations (0008/0009) were touched by the `--down-to 0009` restore? (d) Is the `.env` discipline airtight — copied by name, never printed, removed before EVERY stop line including each red branch?

**(Q6) THE NEW SCHEDULED JOB.** §5.3 bootstraps `com.cobalt.replay` for the first time. Is `launchctl bootstrap` idempotent if the job is somehow already loaded — and does §0 P9's check (it must NOT be loaded) catch that? The plist declares `RunAtLoad false` and `StartCalendarInterval` Mon–Fri 21:10: is the prompt's claim right that bootstrapping on a Saturday runs nothing tonight? Is treating a failed replay bootstrap as an ESCALATE rather than a §8 trigger correct, or should it revert the deploy? Does anything prove the job actually works, or only that it is loaded?

**(Q7) THE WINDOW, on a weekend.** §2.1 starts only when `date` ≤ 18:30 ET today and re-checks before §4.2 and at §5.2. The argument is that L43's restart window binds "on a trading day", Saturday is not one, no scanning session can be open, and 18:30 is margin against the 20:30 archiver and 21:40 backup. Is that argument sound against L43's and L66's actual words? What happens if the run starts at 18:29 and the gate (§1) takes an hour — is any step then unsafe, and does the prompt catch it? Is there any hour today at which the merge would be unlawful rather than merely awkward? If the desk instead launched on Sunday, what in the prompt breaks (the tag name `deploy-2026-09-19b` is fixed at approval)?

**(Q8) MID-RUN FAILURE, STEP BY STEP.** For each of these, say exactly what production is left in, and whether the prompt's instruction gets it there: a denial at §1.4's `cp .env`; a conflict at §1.2; a red suite at §1.3 or §1.4; a rebase conflict at §4.1; a `bootout` that does not take at §4.2; a non-`--ff-only`-able main at §4.3; a serialization failure inside §4.5; a TOOL TIMEOUT at §4.5 **after** the harness committed but before it printed (§4.5 (ii) names this — is its read-back instruction correct?); a `Bootstrap failed: 5` at §5.2 on one resident but not the other; a red `heartbeat` at §6.1. Name any path that ends with production DOWN, or with code and config disagreeing, that the prompt does not name.

**(Q9) AGAINST THE PRECEDENT** (bucket B). `02-deploy-stack-3.md` ran this morning and `deploy-1-report.md` is what it printed. (a) For every step `38` copies from `02`, did anything change for the worse? (b) `02`'s step 3.5 and `38`'s §4.5 are the migration step — compare them line by line: the 600000 ms timeout instruction, the "do NOT rerun after a serialization failure" rule, the commit-before-print caveat, the proof-table gate. Is `38`'s version at least as safe? (c) `deploy-1-report.md` shows the migration cost 94.1 s of a 2 min 40 s outage; `38` adds a read-only `--proof-only` probe at P13 (another ~95 s, residents UP) — is that a real preflight or wasted time? (d) Does `38` carry forward everything `deploy-1-report.md`'s five ESCALATE items said the next deploy would need?

**(Q10) ANYTHING ELSE.** Any claim in `facts.md` you cannot confirm from the files given (mark `UNVERIFIABLE FROM READS`); any secret, credential, connection string or personal/user data visible anywhere in the packet (L4/L32); any place the prompt would produce a plausible-looking but empty or fabricated artifact instead of failing loudly (L1); any instruction that asks the session to judge rather than to check.

End with exactly one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <finding numbers> / DO NOT RUN`.
