MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify; no write path · SEAT: review hub `review-cards-apply-0919`, launched by the CTO desk in the background. NO NEW RULE: `18-review-ops-0919.md`'s `--allowedTools` list, copied byte for byte, with exactly ONE string changed — the `mkdir` path, from `scratch/review-ops-0919` to `scratch/review-cards-apply-0919` (this check needs no rule `18` did not already carry: the packet is files-only, read with `Read`/`grep`/`ls`/`wc`/`tail`, inside the same `agy-trial` worktree `18` already ran in; the unused `git -C .../s2-p2-cards *` / `git -C /Users/cobalt/cobalt show*`/`log*` rules stay, unused, because they are not the string that changed and no branch/commit lookup is needed for a drafted apply prompt and two already-hashed settings files). If this check turns out to need something those rules cannot do, the missing rule is NAMED in the report, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/31-review-cards-apply.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-cards-apply-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-cards-apply-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house). NOTE ON METER PRIORITY: `review-p4-verify-0919` (P4's own house check) has priority on Astra's Codex meter today — probe Astra as usual, but if it returns METER, record `astra: METER` and end with two houses; do NOT retry, do NOT wait for P4's check to finish first.

# Second-house check of a DESK PROMPT before it runs (09-18 R8; LAWS L67 floor: the desk's own deploy/rebase/re-land prompts — and by the same logic any desk prompt with a production write — are checked by another house) — `30-cards-golive-apply.md`, an UNATTENDED Opus prompt that will write to production `"user".trader_settings` (the cards switch-on: `radar.cards_enabled` + `card.proposed_key` + `card.curves` + `card.shadow_promotion_bar`), approved by Dejan (R20, `cto-2026-09-19.md` §4, 11:57 ET) by file + sha256. This is the LAW STEP R20 itself names as still owed before that prompt runs. Would `30-cards-golive-apply.md` FAIL or DO HARM as written? Round 1 of at most 3; small packet on purpose. DO NOT STOP until the report ends `CARDS APPLY CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. His words on main: `cto-2026-09-19.md` §4 R20 (11:57 ET, "Approve cards") names the law step owed ("the apply PROMPT — a desk prompt with a production write — is read by at least one other house") as NOT re-asked of him; `cto-2026-09-18.md` §4 R8 (14:29 ET, the second-opinion rule, the desk's own write-path prompts included), R9 (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20), R18–R21 (the tribunal, L67). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED:` line; `ASK DESK: <question> [<time>]` in the report for a question that does not stop you — then continue with the safe default, never wait on an answer.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — laws, UNATTENDED RULES, PREFLIGHT (Astra: probe as usual; a usage-limit message → `astra: METER`, continue with two houses — SEE the METER-PRIORITY note above), LESSON, §2 launch spellings, and `2026-09-18/11-review-deploy-stack.md` §3 bind you UNCHANGED, with `scratch/review-cards-apply-0919/` in every path. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-cards-apply-0919/REVIEW.md`.

## 1. Packet in `scratch/review-cards-apply-0919/` (`mkdir -p` first; Read → Write, byte-identical) — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/31-packet/`
(1) `30-cards-golive-apply.md` = `31-packet/30-cards-golive-apply.md` — THE FILE UNDER REVIEW · (2) `p2-live-settings.yaml` = `31-packet/p2-live-settings.yaml` — the file the apply prompt loads, sha256 `f3663399b1cbb5ba94edadc7b929900a09be02439c84ffc24f89c12ea81c65f0` · (3) `p2-dark-settings.yaml` = `31-packet/p2-dark-settings.yaml` — the rollback target, sha256 `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` · (4) `r20-row.md` = `31-packet/r20-row.md` — Dejan's approval row, verbatim · (5) `four-strings.md` = `31-packet/four-strings.md` — the four approved rule strings, verbatim, with the note on which two already ran in production today · (6) `02-settings-load-excerpt.md` = `31-packet/02-settings-load-excerpt.md` — `02-deploy-stack-3.md`'s launch line and its settings-load steps (3.4/3.6/3.7/3.8) and safe-state clause for the card row · (7) `deploy-report-excerpt.md` = `31-packet/deploy-report-excerpt.md` — the ACTUAL dry-run/apply output `02`'s hub produced today for the dark row, the only precedent for this loader's print format · (8) `facts.md` = `31-packet/facts.md` — the desk's schema/arithmetic proof-by-reading for the live file (copied unchanged from `23-packet/facts.md`) · (9) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): see `31-packet/QUESTIONS.md` — copy it byte-for-byte into `scratch/review-cards-apply-0919/QUESTIONS.md` as part of staging step (9) above; it is the same text every house receives (L44).

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-cards-apply-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: `30-cards-golive-apply.md` on main (`git -C /Users/cobalt/cobalt show main:"docs/40 - DevDocs/prompts/2026-09-19/30-cards-golive-apply.md"` if committed, else Read the working-tree file directly — say which), `card.py`'s enable-gate and deletion semantics via `git -C /Users/cobalt/cobalt show main:src/cobalt/settings/card.py` and Read/`grep` against `/Users/cobalt/cobalt/src/`, and `02-deploy-stack-3.md` / `deploy-2026-09-19.md` directly under `/Users/cobalt/cobalt` with Read (no Bash rule needed for a plain read; `--add-dir /Users/cobalt/cobalt` already covers it). A claim only a live `--dry-run` could settle (Q3's float-noise question, Q4's deletion-semantics question if the loader's own deletion code is not conclusive from a read) = `UNVERIFIABLE FROM READS`, name the exact missing command (`uv run cobalt settings load ... --dry-run`, none of you may run it). One extra column per REAL finding: `blocks the launch? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-cards-apply-0919/`. Last line, matching `14-review-p4-verify.md`'s field shape exactly: `CARDS APPLY CHECK · grok: <RUN AS IS|RUN AFTER FIXES <n>|DO NOT RUN|HARNESS|METER|TIMEOUT> · gemini: <same shape> · astra: <same shape> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
