# facts.md — measured by the CTO desk, read-only, 2026-09-19 08:36–08:45 ET. Every line is a command's output or a quoted file line; nothing here is an opinion.

## A. The branch the prompt rebases (`/Users/cobalt/cobalt-wt/s2-p4`, branch `sprint-2/p4`)
- Tip `2a8784c`, `git status --porcelain` empty.
- `git log --oneline main..HEAD` = 65 rows. `git log --oneline 976528f..HEAD` = 31 rows (P4's own commits).
- `git log --oneline --cherry-mark --right-only main...HEAD`: 34 rows marked `=` (patch-identical to a commit already on main), 31 rows marked `+`. The newest `=` row is `976528f`; the two rows directly above it are `+ b2f751f` and `+ ccad079` (P4's first commits). `git cherry main HEAD` gives the same split: 34 `-`, 31 `+`.
- Why 34 commits are patch-identical: P2 (`sprint-2/cards`, old tip `976528f`) was re-landed on a stack branch by `git cherry-pick`, and that stack was merged to main by today's deploy (`deploy-2026-09-19.md` last line: `STACK DEPLOY DONE 2893a7f · LIVE: sprint-2/stack DARK y · ops-0918 y · migrations 0006/0007 applied …`). P4 has never been merged to main and never reverted.
- main at 08:45 ET: `920b0a9` (a docs-only desk commit on top of the deploy). The desk makes more docs-only commits on main while hubs run.
- main's migrations directory: `0001`–`0007` + `cli.py` (the harness: streamed proof, `--proof-only`, REPEATABLE READ, ambiguous-table-name refusal), `placement.py`, `__init__.py`. P4 adds `0008_radar_value_movers.sql` / `.rollback.sql` and `0009_picks_missed.sql` / `.rollback.sql`. `git log` on those four files in P4: `2533098` (09-17, created) and `402bfc0` (09-18, "0008 comment names the 21:10 replay … comment only, no runner checksum").
- `cli.py` flags on main: `--allow-prod`, `--down-to`, `--rollback` ("--rollback requires --down-to NNNN"), `--proof-only` ("takes the proof and applies NOTHING").

## B. What earlier reports say about `cobalt_dev` (NOT verified by the desk today)
- `s2-p4-followup-2026-09-18.md` (07:3x ET 09-18): "`COBALT_ENV=dev uv run cobalt db migrate` (FORWARD): 0001-0005, 0008, 0009 applied; 0006/0007 (P2's) absent"; rollback `--down-to 0005` reversed 0009, 0008 cleanly; forward again re-applied them. It left 0008/0009 APPLIED.
- 09-18 afternoon: the stack gates (another worktree) applied / rolled back / re-applied 0006/0007 on the same `cobalt_dev`.
- No session is using `cobalt_dev` now (`ListAgents` 08:34: no hub alive).

## C. `Asset Type`, from the code and the data
- `/Users/cobalt/cobalt/configs/cobalt/radar.yaml`: `export: {v: 152, columns: "0-150", required_headers: [Ticker, Volume, Relative Volume, Asset Type], …}`; `not_equity: {header: Asset Type, values: [Exchange Traded Fund]}`. Comment above it (09-10): the value "Exchange Traded Fund" did NOT appear in that capture's `Asset Type` set; seen: Bonds, Commodities & Metals, CryptoCurrency, Equities (Stocks), Multi-Asset, Preferred Stock.
- main `src/cobalt/radar/config.py:42-45`: `class NotEquityConfig(BaseModel): model_config = ConfigDict(extra="forbid"); header: str = Field(min_length=1); values: list[str] = Field(min_length=1)`.
- main `src/cobalt/radar/runner.py:144`: `if row.get(self.config.not_equity.header) in self.config.not_equity.values: excluded.add(ticker)` — the LIVE radar's rule.
- main `src/cobalt/radar/collector.py:158-162`: `_params` returns `{"v": self.config.export.v, "c": ",".join(str(value) for value in range(151))}`.
- P4 `src/cobalt/replay/movers.py:209-210`: `_params(side)` returns `{"v": self.config.export.v, "c": ",".join(str(v) for v in range(151)), "o": SIDES[side]}`; `SIDES = {"gainers": "-change", "losers": "change"}`; line 75 `REQUIRED_HEADERS = ["Ticker", "Change", "Volume"]` with the comment "`Asset Type` is read when present"; line 313 `if lead.asset_type is not None and lead.asset_type in not_equity_values: continue`; line 338 `"asset_type": lead.asset_type if lead.asset_type is not None else "unreported"`.
- P4's movers fixture `tests/fixtures/replay/movers-gainers.real-shape.csv` and its raw source `scratch/movers-gainers-raw.csv` have the SAME 21-column header: `"No.","Ticker","Industry","Country","Exchange","Market Cap","P/E","Shares Float","Insider Ownership","Institutional Ownership","Short Float","Short Interest","Performance (5 Minutes)","Average True Range","Shortable","Gap","Average Volume","Relative Volume","Volume","Price","Change"` — no `Asset Type`. The 09-17 script that fetched it (`scratch/_fetch_movers.py`) was deleted as ordered; what parameters it sent is NOT known.
- A live radar export (`/Users/cobalt/cobalt/data/radar-cache/2026-09-18/`, first `*.csv`): 151 header columns; `"Industry"` is column 5, `"Asset Type"` column 101. `cto-2026-09-18.md` §22: in `screen-morning_low_float-195628.csv` (16 rows) `Asset Type` is blank for all 16 stocks.
- His ruling, `cto-2026-09-18.md` §4 R11, 16:02 ET: "Keep as designed." — funds and ETFs stay out of the nightly missed-movers benchmark; "the movers request must carry Finviz's `Asset Type` column (the radar's column set), and not-equity = any NON-BLANK `Asset Type` (Finviz fills it only for funds) — one rule serving radar and movers".
- P4 has never been deployed: production has no `"user".missed`, no `com.cobalt.replay`, no retained `movers-*` export.

## D. The launch line's rules against what he approved (computed by the desk with `grep -o` + `comm`)
- Rules in `13-p4-verify.md`'s launch line: 31.
- In `13` but in NEITHER approved launch line (`2026-09-18/01-p4-followup.md` line 1 = 09-18 R1 list (3); `2026-09-17/04-p4-relaunch.md` line 1 = 09-17 R6): NONE.
- Taken from the 09-17 line only: `"Bash(COBALT_ENV=production uv run python /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py*)"`, `"Bash(rm /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py)"`, `"Bash(uv run python tests/fixtures/replay/_cut_p4_fixtures.py*)"`.
- 09-18 rules NOT carried: `"Bash(grok *)"`.
- The 09-18 line, every rule (sorted): "Bash(cd *)" "Bash(claude -p *)" "Bash(COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(date*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git log*)" "Bash(git rebase main)" "Bash(git show*)" "Bash(git status*)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(grep *)" "Bash(grok *)" "Bash(ls *)" "Bash(mkdir -p *)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(touch scratch/*)" "Bash(uv run cobalt jobs restarts *)" "Bash(uv run pytest *)" "Bash(wc *)" "CronCreate"
- HOW A RULE MATCHES (`prompts/UNATTENDED-LAUNCH.md` §2, §5): a rule matches ONE bare command per call, exactly the listed prefix — no `cd … &&` in front, no pipe, no redirect, no `; echo` behind. A command that matches no rule is judged by the auto-mode classifier, which has denied, in these runs: production reads, credential-file copies, dev-DB rollbacks (`[Production Deploy]`), git history operations (`[Git Destructive]`). A mid-run denial = the run FAILED.
