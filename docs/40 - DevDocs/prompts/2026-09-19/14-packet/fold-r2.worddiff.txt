diff --git a/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md b/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md
index d33c0f8..24fc341 100644
--- a/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md	
+++ b/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md	
@@ -7 +7 @@ YOU CAN ALWAYS STOP. Only the dialog is removed, because nobody sees a `--bg` di
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(s2-p4): <chunk> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit paths under `src/`, `configs/`, `tests/`, `ops/`, `docs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory that contains `scratch/` (review round 1, R11: `scratch/` is NOT gitignored on this branch, and it holds the raw exports, the fetch script and the evidence file with tickers from his screens — L32). After every commit: `git show --stat HEAD` lists no `scratch/` path, else `FAILED: <step> — scratch path committed <path>` (the desk repairs it; you have no reset rule). BEFORE ANY STOP LINE, on every path: if `/Users/cobalt/cobalt-wt/s2-p4/.env` may exist, `rm` it and prove it with `ls -la .env` (the file is gitignored, so `git status` can never prove its absence — only `ls` does); same for `scratch/_fetch_movers.py`. BUILDERS (review round 1, R2): every {+BUILDER+} `claude -p` launch {+(NOT PREFLIGHT's one-word meter probe, which is typed exactly as written there, N6)+} is the shape of `01-p4-build.md` step 2 with its `--permission-mode acceptEdits --allowedTools …` list byte for byte, as ONE bare command WITHOUT the leading `cd … &&` (you are already in the worktree); a builder runs offline tests only — YOU run every `COBALT_ENV=dev` command; NO builder runs while `.env` is in the worktree (its list carries `cat *`): builders finish before 4.1 or start after 4.8. A killed builder's uncommitted edits are wip-committed by you before any relaunch. ONE exception: a rebase stopped on a conflict you could not resolve is left EXACTLY as it stands (no commit is possible, `git rebase --abort` is not in your list): write `git status` verbatim into the report, then the `FAILED` line; the desk aborts it.
@@ -12 +12 @@ UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd 
PREFLIGHT (first two minutes; one row each: rule · command · exit · allowed/DENIED + reason verbatim; every probe sits inside its own rule's pattern): `date` · `git status --porcelain` (empty or `?? scratch/` — ANYTHING else,[-a rebase in progress included (`git status` says so),-] → `FAILED PREFLIGHT: worktree not clean — <lines>`, R14) · `git {+status` (LONG form — `--porcelain` shows a paused clean rebase as empty; quote its first two lines; "rebase in progress" → `FAILED PREFLIGHT: rebase in progress`, N7) · `git+} log --oneline -1` (first launch: `2a8784c`; a resume after a `CONTINUE:` line: the tip that line's commit left — say which) · `git -C /Users/cobalt/cobalt status --porcelain` · `git -C /Users/cobalt/cobalt log --oneline -1` (record it: this is the main you rebase onto) · `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` → `COBALT_ENV=dev uv run cobalt db migrate --help` → `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` shows it gone (if the `migrate --help` probe is DENIED, the `rm` + `ls` still run before the `FAILED PREFLIGHT` line, R3) · `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (also the L47 meter probe; a usage-limit message → record the reset time, `CronCreate` a wake-up at reset + 5 min, commit the report, wait) · `uv run pytest --co -q tests/cobalt/test_tenancy.py`. The two script rules have no harmless variant and are probed by their first real use (`UNATTENDED-LAUNCH.md` §6). Any denial → `FAILED PREFLIGHT: <rules>`.
@@ -21 +21 @@ FACTS (desk, read from the files 08:40 ET — verify each before you brief): the
2.1 THE EXPORT (you, not a builder — `04-p4-relaunch.md` B3's shape): Write `scratch/_fetch_movers.py`: `FinvizApiClient._resolve_vault_credentials`, the token never printed, never logged, never written; TWO requests to `/export/screener` with `v` read from `/Users/cobalt/cobalt/configs/cobalt/radar.yaml` `export.v`, `c` = `0,1,…,150`, `o` = `-change` (gainers) and `change` (losers) — the exact parameters `replay/movers.py` `_params` builds (quote that function in the report); no `f=` string; one second apart; the script itself writes `/Users/cobalt/cobalt-wt/s2-p4/scratch/movers-gainers-raw.csv` and `…/movers-losers-raw.csv` (overwriting the 21-column files — first `ls -la scratch/movers-*-raw.csv` and record sizes) and prints ONLY: status code, row count, column count, per file. TOKEN SAFETY (review round 1, R1 — three houses; the repo's own Finviz client builds `…&auth=<token>` into the URL and an httpx error's text CONTAINS that URL): the script goes through the transport the movers collector already uses — `from cobalt.archiver.collector import resolve_token, finviz_get, scrub` (`src/cobalt/archiver/collector.py:74, :78, :175`; see `src/cobalt/replay/movers.py:48, :222`): `resolve_token()` fetches the credential, `finviz_get(path, params, token, on_metrics=…)` is the sole transport — both are `async` (`asyncio.run`), the transport adds `auth` to the request ITSELF and raises a `CollectorError` whose text is already scrubbed, and the status code comes from its `on_metrics` callback (`FetchMetrics.status`) — never the script's own `httpx`/`requests` call, never the token placed in `params`, a URL or an f-string by the script; it NEVER calls `raise_for_status` itself and never touches `e.__cause__` (the chained httpx error is NOT scrubbed); the whole body sits in ONE `try/except Exception as e` whose handler prints exactly `ERROR <type(e).__name__>` and nothing of `str(e)`, no traceback (`sys.exit(2)`, never a bare re-raise); it never prints, logs or writes a URL, a header, a params dict or the token; it configures no [-logger and, if-]{+logger; BEFORE `resolve_token()` is called it silences BOTH logging systems (review round 2, N1 — three houses): `logging.disable(logging.CRITICAL)` for+} the [-client-]{+standard library (that is what silences `httpx`, which+} logs {+the request line at INFO) AND loguru's `logger.remove()` (which silences loguru only). THE RETRY SHAPE (N5): `finviz_get` raises `CollectorError`+} on [-its own, removes-]{+a non-200, so+} the [-loguru/stdlib handlers for-]{+retry is an INNER `try` that catches, prints NOTHING, waits and retries once;+} the [-run (`logger.remove()`) before-]{+ONE printing handler described above is+} the [-first request.-]{+OUTER one and wraps everything, `asyncio.run(...)` included.+} THE RETRY IS INSIDE THE SCRIPT (you have no `sleep` rule): a non-200, a token error or a column count other than 151 → the script waits 60 s itself and tries ONCE more, then exits 2. BOTH responses are validated in memory first; the two raw files are written only after BOTH are good (a failed run leaves the old 21-column files untouched). Before the run, YOU read your own script once against this paragraph, line by line, and write `token-safety: checked` with the line numbers into the report. Run: `cd /Users/cobalt/cobalt` → `COBALT_ENV=production uv run python /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py` → `cd /Users/cobalt/cobalt-wt/s2-p4` → `rm /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py` → `ls scratch/_fetch_movers.py` shows it gone — the `rm` + `ls` run on EVERY path, the failed one included, BEFORE any `FAILED` line is written. Expected: 151 columns, header contains `Asset Type`. Exit 2 → `FAILED: step 2.1 — exit 2, <the status code / row count / column count lines only>`; you NEVER copy anything else from that tool result into the report, and if the tool result shows anything that looks like a URL or a key, the report says only `FAILED: step 2.1 — output withheld, possible credential in tool result` (the desk reads it and rotates). Known and accepted (R19, unverifiable from reads): `uv run` in the production checkout may touch its `.venv`; the same shape ran on 09-17 and the checkout was synced by this morning's deploy. Record under `## ESCALATE` for L53: two Finviz requests at <time>, radar `idle:overnight`, archiver not running.
@@ -23 +23 @@ FACTS (desk, read from the files 08:40 ET — verify each before you brief): the
   DECISION RULE, yours, mechanical, in THIS order (review round 1, R7 — a sample of one or two funds proves nothing): {+(0) ZERO FUNDS — tables A and B both empty → `Asset Type: ESCALATED — no fund row in the sample`: the rule cannot be proven from real data (L45); build 2.3, and 2.5 WITHOUT the fund row (see 2.5), nothing of 2.4; continue with step 3.+} (i) SAMPLE FLOOR[-FIRST-] — the evidence file also counts DISTINCT fund tickers (rows of table A or B) and DISTINCT non-blank `Asset Type` values; fewer than 10 distinct fund tickers, or fewer than 2 distinct `Asset Type` values → `Asset Type: ESCALATED — sample too thin (<n> funds, <v> values)`, build 2.3 and 2.5 only, nothing of 2.4's rule, continue with step 3. (ii) floor met AND table C has 0 rows → "non-blank `Asset Type`" and "is a fund" agree on every real row → build 2.3–2.5. (iii) Table C has ≥1 row → `Asset Type: ESCALATED` — commit the evidence mode, write the C value pairs + counts under `## ESCALATE` with `ASK DESK: which rule — non-blank Asset Type, Industry = Exchange Traded Fund, or either? [<time>]`, build NOTHING of 2.4's rule (2.3's column source and 2.5's fixture are still built — they do not depend on the rule), continue with step 3. [-A sample with no fund row at all (tables A-]{+(The zero-fund case is branch (0) above+} and [-B both empty) → the same ESCALATE: the rule cannot be proven from real data (L45), build 2.3 only.-]{+is decided there, once — review round 2, N2.)+}
@@ -27 +27 @@ FACTS (desk, read from the files 08:40 ET — verify each before you brief): the
   2.5 THE FIXTURE (L45 + companion): `uv run python tests/fixtures/replay/_cut_p4_fixtures.py` re-cuts `movers-{gainers,losers}.real-shape.csv` from the new 151-column raw files — real header row byte for byte, the rows the cutter already takes, PLUS at least one real row whose `Asset Type` is non-blank (if the top rows hold none, the cutter takes the first such row further down and says so in its header [-comment);-]{+comment; if the raw exports hold NO non-blank `Asset Type` row at all, the cutter cuts what exists and says so — a fund row is never fabricated, N2);+} attribution, dates and preset ids stripped as the cutter already does; the fixture is never edited by hand. A test asserts the movers fixture's header row equals the radar real-shape export fixture's header row (the collector receives ONE shape). The leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures` still passes.
@@ -30 +30 @@ FACTS (desk, read from the files 08:40 ET — verify each before you brief): the
## 3. Chunk TR — triage rows 2, 4, 8 of `cto-2026-09-18.md` §21 (builder run TR, Opus headless; brief `scratch/tr-brief.md`; tests first — the brief carries the two STOP sentences of rows 4 and 8 below VERBATIM, so the builder meets the L52 guard where it works, R15; the SAME builder run also writes step 4.6's K3 test file, test only, so that no builder runs while `.env` is in the [-worktree).-]{+worktree; that ONE file is exempt from the red-first rule offline — it is marked `requires_db`, must SKIP cleanly offline, and its red/green is 4.6's run, N4).+} Read build2 `## ESCALATE` 2, 4, 8 and its §4 row 3 before briefing.
@@ -38 +38 @@ WHAT `cobalt_dev` PROBABLY HOLDS (from the reports, NOT verified by the desk): `
4.1 `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` (by name; never `cat`, `grep`, Read or print it). 4.2 STATE: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (main's harness: applies NOTHING) — output verbatim, its duration in seconds. 4.3 If 0008/0009 are already applied: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` FIRST (verbatim; it must reverse 0009 then 0008 and nothing else), so that the forward run below is the honest one. If 0006 or 0007 is ABSENT on `cobalt_dev` → do not invent an order. The harness keeps NO ledger of applied migrations (review round 1, R5b): a forward run re-runs the whole registered list 0001–0009 in order, every file idempotent, and `--rollback --down-to 0007` reverses the registered files numbered above 0007 (0009, then 0008) whatever was applied. So in that state 4.4 is a FULL forward over 0006–0009 and its duration is not P4's alone — say so; the number the deploy prompt budgets from is then 4.7's second forward run (0008 + 0009 on a database that already holds 0007). 4.4 FORWARD: `COBALT_ENV=dev uv run cobalt db migrate` → 0008 then 0009 applied ON TOP OF 0007; every pre-existing table `OK` unchanged; the three new relations (`system.movers_daily`, `"user".picks`, `"user".missed` — names as the migration files say) created; RECORD THE PROOF'S DURATION (BEFORE + AFTER seconds, and `system.bars` row count on dev) — production's was 104 s for 8.83 M rows and the deploy prompt budgets from your number. A `CHANGED` on a table 0008/0009 do not touch, a serialization failure, or a refusal → verbatim, `FAILED: step 4.4 — <line>` after 4.8's cleanup. 4.5 DB SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote). The four reds of 09-17 ESCALATE 4 / followup ESCALATE 2 must be GONE now that 0006/0007 sit under P4 — any that remains: name the test, quote the assertion, ESCALATE, continue. A red that reads a live `"user".trader_settings` VALUE on a production-mirroring dev DB is the known 09-18 class (tests prove invariants, never assert live values): name it, do not "fix" the data. 4.6 ROW 6 — K3 ON REAL POSTGRES: run K3's SQL through the smoke framework's own test path against `cobalt_dev` (a test that loads `configs/cobalt/smoke/s2.yaml`, executes K3's statement, and asserts it PARSES and returns the declared shape — WRITTEN in step 3 by the TR builder, test only, its own commit `test(smoke): K3's SQL runs on Postgres`; here YOU run it with `COBALT_ENV=dev uv run pytest -q <that file>` — no builder is launched inside step 4); then report, for the desk's decision and building NOTHING more: did it execute · what it returned on dev · whether a HOLD row with a pre-deploy NULL `last_scan_id` makes it read red (build2 ESCALATE 6's scenario, reproduced with a constructed row inside the test's own transaction, rolled back). 4.7 ROLLBACK ROUND TRIP: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` → reverses 0009 then 0008, drops only its own three relations, everything else `OK` → `COBALT_ENV=dev uv run cobalt db migrate` → re-applied; compare 4.4's forward output with this second forward run LINE BY LINE (side by side in the report): the lines naming schema objects — every table's placement/`OK` line and the three new relations — must be EQUAL; a line that differs only in a duration, or in a row count of a table the DB suite of 4.5 writes, is listed and explained, not a mismatch (R18); all three outputs verbatim. A MISMATCH = ESCALATE row + `rollback round-trip ESCALATED` in the stop line; the run continues. A DENIAL of any step-4 command is NOT an escalate — it is `FAILED: step 4.<n> — <command> — <reason>` like every mid-run denial, written AFTER 4.8's cleanup (R12). If the rollback ran and the forward after it FAILED: run the forward ONCE more, record both outputs, and state in the stop line exactly what `cobalt_dev` holds (`cobalt_dev: 0001–0009` or `cobalt_dev: 0001–0007, forward failed`) — the next run must be able to start from your line. Normal ending: `cobalt_dev` is LEFT with 0001–0009 applied. {+EVERY terminal line written after step 4 has begun — `FAILED` included — ENDS with ` | cobalt_dev: <what it holds | unknown>` (review round 2, N3).+} The stop line's `db [-<n>/0`-]{+<passed>/<failed>`+} reports what actually [-ran;-]{+ran, reds included (4.5 lets a named red ESCALATE and continue — then the line says so, e.g. `db 1790/2`);+} a suite that did not run is `db NOT RUN`, never a number. 4.8 ALWAYS, even on a failure above: `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` shows it gone (quote) → `git status --porcelain` shows no `.env`. 4.9 Without the `.env`: `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` (read-only real vault) — quote the line. Last line `CONTINUE: step 5`.
@@ -42 +42 @@ WHAT `cobalt_dev` PROBABLY HOLDS (from the reports, NOT verified by the desk): `
`READY <tip> | rebased onto <main sha> | offline <n>/0 | db [-<n>/0-]{+<passed>/<failed>+} | cobalt_dev: <what it holds> | rollback round-trip <ok/ESCALATED> | Asset Type <built/ESCALATED> | rows 2 4 6 8 <built|ESCALATED each> | RESTARTS: <line> | OWED: three-house check, receipt-reference tribunal, deploy prompt | ESCALATE: <n>`
