MODEL: Opus 5 (`claude-opus-5`) — same tier as the round-1 build (this fold touches the append write path and the production `cobalt` entry point, L29 floor) · SEAT: archiver build hub `archiver-append-r2`, launched by the CTO desk in the background, SAME worktree as round 1 (it already exists — do NOT create it, do NOT `git worktree add`, do NOT rebase). NO NEW RULE: every rule below is copied byte for byte from `docs/40 - DevDocs/prompts/2026-09-19/17-archiver-append-build.md`'s launch line (the round-1 build this session is folding findings into) — zero added, zero altered; dropping was considered and none were dropped, because this round's re-run of `## 8. Close` needs the same `Bash(uv run pytest *)` and `Bash(uv run cobalt jobs restarts *)` rules round 1 already had. THIS RUN IS OFFLINE BY DESIGN, same as round 1: `cobalt_dev` is not this session's to touch — every `requires_db` test written this round joins round 1's OWED list, never run here. Two bare commands, run by the desk:

```
cd /Users/cobalt/cobalt-wt/archiver-append
claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/26-archiver-round2.md' and follow it exactly." --model claude-opus-5 --remote-control archiver-append-r2 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt
```

SESSION / REMOTE-CONTROL: `archiver-append-r2` — fresh session, every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `cobalt_dev`, no `.env`, no vault write, no other seat's config · METER: Anthropic LARGE, shared with any other Opus seat running today; a usage-limit message is not a failure — record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

LAUNCH-LINE RULE PROOF (this session's rule strings vs 17's, sorted, `comm -3`): both sides are the 16 `Bash(...)` allow strings + `AskUserQuestion` + `EnterWorktree` + the three `--add-dir` values (21 strings each). `comm -3 <(sort 17-rules.txt) <(sort 26-rules.txt)` → **empty output** — the sets are identical, nothing added, nothing altered, nothing dropped. (Every rule this round's steps need — `git add` by explicit path, `git commit`, `uv run pytest`, `uv run cobalt jobs restarts`, `grep`/`Read` for the tests-first RED quotes — is already in 17's set; no command this prompt asks for falls outside it.)

# Archiver append-only — ROUND 2, fold the three-house check of the round-1 build (`archiver/append-0919`, code tip `c974fbf`, report tip `7c972ee`). Tests FIRST for every code change (each new test RED on the current tip, then GREEN), OFFLINE, one commit per item. DO NOT STOP until the report ends `ARCHIVER APPEND R2 …` or `FAILED …`.

CONTEXT. The round-1 check (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-0919/REVIEW.md`, ONE house read it — Grok; Gemini = harness failure, Astra = meter) found 7 REAL findings (hub-verified), 0 NOT REAL, 4 UNVERIFIABLE. ONE blocks shipping (F1). The full verdict table (verbatim, including the UNVERIFIABLE rows) is staged at **`docs/40 - DevDocs/prompts/2026-09-19/26-packet/verdicts.md`** — READ IT IN FULL before touching any file; it binds you exactly as `19-packet/verdicts.md` bound the ops round-2 build. Where your reading of the code differs from a row, the CODE wins and the difference is an `## ESCALATE` line, never a silent substitution.

CARRIED, NOT YOURS THIS ROUND: nothing is carried out of scope silently — every one of the 7 REAL findings (F1–F7, F3=Q10) is assigned below. The three-house re-check after this round reads Gemini + Astra fresh (whole updated build) and Grok reads only the fold (desk's plan, not yours to change).

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval. `grep -c` each of the 16 `Bash(...)` rule strings above, plus `--disallowedTools "AskUserQuestion" "EnterWorktree"` and the three `--add-dir` values, against `docs/40 - DevDocs/prompts/2026-09-19/17-archiver-append-build.md` — every one must count 1 (17's own AUTHORIZATION section did the identical check against `02-ops-2026-09-18.md`; same shape, one file back). The WORK ITEM is authorized by: the round-1 build's own report `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` (its `OWED:` line names "three-house check" as the very next step, which this fold answers); LAWS L67 (the tribunal check whose findings you are folding — `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-0919/REVIEW.md`, 1 of 3 houses read it, hub-verified 7 REAL / 1 blocks shipping / 0 NOT REAL / 4 UNVERIFIABLE — the L67 three-checker bar is NOT met this round, but the floor of two houses total (builder + Grok) holds, R20); the design itself, `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` (still FINAL — nothing here reopens it; §8, §14, §15 bind the fixes below). A rule here not in 17's committed line, or a row that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.

YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded scope: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step (or in the design's §14 OPEN table), continue; never block on an answer, never message anyone. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Anything RED that this prompt expected GREEN, or any number that does not match what a cited row or file says → `ESCALATE`, never silently reconciled, never asked about mid-run.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(archiver-r2): <item> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit file paths under `src/cobalt/archiver/`, `src/cobalt/cli.py`, `tests/cobalt/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `data/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>`.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL → **`docs/40 - DevDocs/prompts/2026-09-19/26-packet/verdicts.md`** (THE SPEC for what is REAL and what fix is owed this round — every row binds you) → the FINAL design, `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md`: **§8** (V3-9, option A — the quiet window, Q1–Q4, the pre-commit re-check, the two named boundary tests, the refusal text — binds F1/F2/F3/F6's siblings), **§9** (the run-level lock and failure semantics — binds F1's "same connection" requirement and the lock error), **§14 O-4** (a repair's audit trail — binds F1's incident-write-inside-the-transaction shape), **§14 O-7** (heartbeat wording — unaffected this round, cite only if a fix touches it), **§15** ("Quiet window (§8)" bullet — binds F2, F3/Q10, F7; "Migrations" bullet — binds F7) → the round-1 build's own report `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` on branch `archiver/append-0919` (the file you APPEND to: a new `## ROUND 2` section after its existing last line) → the full review, for context the table compresses, at `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-0919/REVIEW.md` (reachable under your `--add-dir /Users/cobalt/cobalt-wt`) → `src/cobalt/archiver/cli.py` IN FULL (`_cmd_restate`, `_cmd_backfill_missing`, `_apply_restate`, `HANDLERS`, `MUTATING_COMMANDS`/`READ_ONLY_COMMANDS`, `main`) → `src/cobalt/archiver/store.py` (`upsert_bars` at `:95-141`, `insert_new_bars` at `:206+`, `target_transaction`, `ArchiveLockError`, `try_acquire_run_lock`) → `src/cobalt/archiver/quiet.py` (`QuietRefused`, `_Guard.check_before_commit`, `guarded_repair`) → `src/cobalt/archiver/runner.py:130,152` (the `upsert` night's `store.upsert_bars(bars)` call — UNTOUCHED by this round — and the shadow `night=clock().date()` line) → `src/cobalt/cli.py:493-503` (`main`'s dispatch — the production entry point F5 binds) → `src/cobalt/session/clock.py:262` (`SessionClock.to_et` — the ET-conversion tool already in the codebase, for F6) → `tests/cobalt/test_archiver_quiet.py` (all of it — `FakeBars` at `:290-310`, the two named boundary tests at `:333-433`, the pre-commit rollback test near `:476-490`, the four tests F4 replaces at `:246-247`, `:498-507`, `:510-519`, `:553-561`) → `tests/cobalt/test_archiver_migrations.py:190` (`test_incidents_has_the_partial_unique_index_on_unresolved_rows`) → `tests/cobalt/test_archiver_runner.py` (`FETCH_AT`, the mode-isolation pinned-list test). Laws, one line each: L1 fail loud — a failed re-check leaves ZERO changed rows, never a plausible partial write · L3 one path — one write method per write shape, no second `upsert_bars`-alike · L29 Opus floor, this write path and the production CLI entry both qualify · L35 your claims are checked against your artifacts (RED-then-GREEN quoted, diffs quoted) · L45 tests against the real command path and the suite's existing fakes — no invented Postgres behaviour where `requires_db` is the honest answer · L46 one commit per item, tree clean before you stop · L48 evidence in the report file in the same turn · L54 you work in this worktree only · L57 the counting identities are untouched by this round — if a fix looks like it would change one, STOP and ESCALATE, do not silently absorb it · L60 recovery: partial work is wip-committed, never discarded · L62/L63 no questions mid-run, no dialogs; the report is your stop channel · L67 this fold exists because the tribunal found a real blocker — the next check reads your diff, not your word.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `uv run cobalt` anything except `jobs restarts`, any `COBALT_ENV=…` command, `launchctl`, `claude`.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim): `date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form, quote it in full — the branch must be `archiver/append-0919`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: `7c972ee`, else the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file"; if it exists → `FAILED PREFLIGHT: .env present — this run must not see the dev database` · `uv run pytest --co -q tests/cobalt/test_archiver_quiet.py tests/cobalt/test_archiver_migrations.py tests/cobalt/test_archiver_runner.py` (collects cleanly — no code change has landed yet). Any denial → `FAILED PREFLIGHT: <rules>`.

DESIGN CONSTRAINT THAT BINDS EVERY STEP BELOW (state it, do not just keep it): `runner.py:130`'s `upsert` night — `store.upsert_bars(bars)` with no connection argument — stays byte-identical in behaviour apart from the run-level advisory lock already built in round 1 (spec §5's "COMPLETE LIST … pinned by a test that fails if the list grows" stands; this round adds nothing to that list). `git diff main -- src/cobalt/radar/` must stay EMPTY — quote it at close, same as round 1. You never touch `src/cobalt/radar/`.

## 1. F1 — `restate --apply` and `backfill-missing` write on the SAME connection/transaction as the pre-commit re-check
Read `verdicts.md` row 1 and `cli.py:266-410` in full before touching anything. Today: `_apply_restate` (`cli.py:391-399`) calls `store.upsert_bars(rows)`, which opens its OWN connection in `store.py:95-141` (`self._connect()` … its own `with` commit) — separate from the `conn` that `_cmd_restate` opened via `store.target_transaction()` for the incident write and the re-check. A failed `guard.check_before_commit()` (`cli.py:287`) rolls back the incident write on `conn`; the bars `upsert_bars` already committed on its own connection survive. `backfill-missing` does NOT have this shape: `_cmd_backfill_missing` (`cli.py:313-315` region) already calls `store.insert_new_bars(conn, …)` — the connection-taking method — inside the same `target_transaction()` block as its re-check, so its write already rolls back together with the re-check. CONFIRM this by reading `insert_new_bars`'s signature (`store.py`, takes `conn` as its first argument, no internal connect/commit) and quote the confirmation in your report; do not silently assume it.
THE FIX (restate only): give `restate --apply`'s bar write the same shape `backfill-missing` already has. `upsert_bars` must be reachable from `_apply_restate` in a form that writes ON `conn` — either add an optional connection parameter to `BarStore.upsert_bars` that, when given, executes the `INSERT … ON CONFLICT DO UPDATE` on that connection without opening or committing one of its own (the nightly call site, `runner.py:130`, passes no such argument and MUST be unaffected — quote `git diff main -- src/cobalt/archiver/runner.py` showing that line untouched), or add a sibling connection-taking method that `upsert_bars` and it both call for the SQL (L3: no second copy of the statement). Whichever shape you take, name it in the report. The incident write and the bar write must both be inside `_cmd_restate`'s one `target_transaction()` block, and `guard.check_before_commit()` must run AFTER both, so a raise there rolls both back together.
TESTS FIRST (each RED on the current tip before the fix, GREEN after — quote both):
- A test through the REAL command path (`archiver_cli._cmd_restate` or `HANDLERS["restate"]`, wired with a fake store, a fake clock, and a pool reader that makes the pre-commit re-check fail) that a repair whose re-check fails leaves ZERO changed bar rows — assert against the fake store's own snapshot, using the fakes the suite already has (`FakeBars` in `test_archiver_quiet.py`, extended with a connection-taking `upsert_bars` if your fix adds one — extend the fake, do not build a second one, L3).
- The same shape for `backfill-missing` through its real command path — this should already pass today (no code change there) since it already writes on `conn`; if it does NOT pass, that is a NEW finding, not something to paper over — quote it and treat it as an F1 sibling.
- A `requires_db` twin of both (written, skipped offline, following `tests/cobalt/test_archiver_store.py`'s pattern) — its first run is OWED, same as every other `requires_db` test this branch carries; say so plainly.
Commit: `fix(archiver): restate --apply writes bars on the repair's own transaction so a failed pre-commit re-check rolls them back (tribunal round 1, F1)`. Last line `CONTINUE: step 2`.

## 2. F2 — the two named §8 boundary tests drive the COMMAND path, not only `quiet_verdict`
Read `verdicts.md` row 2. `test_gemini_close_boundary_poller_lag` and `test_astra_open_boundary_repair_crosses_open` (`test_archiver_quiet.py:333-384`, `:387-433`) construct a `QuietObservation` and call `quiet_verdict()` directly — they never go through `_cmd_restate`/`_cmd_backfill_missing`, so F1's bug (a write escaping the transaction) could not have been caught by them. Extend (do not delete) both named tests, or add a sibling per one named identically plus a suffix (say which you chose), so that at least one scenario in each drives the real command entry point (`archiver_cli._cmd_restate` or `_cmd_backfill_missing`, via `HANDLERS` or `archiver_cli.main`) with a fake clock, a constructed pool row, and the fake store from step 1 — keeping every existing assertion (`store.snapshot() == before`, the failure-line assertions) and adding the command-path drive. TESTS FIRST: the new/extended assertions must be RED on the current tip (they exercise machinery — `guarded_repair`, `target_transaction`, the incident write — that is not wired together the same way through `quiet_verdict()` alone) and GREEN after step 1's fix lands (do this step's tests as part of step 1 if they share a RED/GREEN boundary — say so if you combine the commits, otherwise keep it a separate commit). Commit: `test(archiver): the two named §8 boundary tests drive the command path, not only quiet_verdict (tribunal round 1, F2)`. Last line `CONTINUE: step 3`.

## 3. F3 / Q10 — the differing-value and equal-value race tests §15 names
Read `verdicts.md` row 3 and spec §15's "Quiet window (§8)" bullet ("differing-value and equal-value races") and its "Concurrency" bullet ("a poller insert between the range read and the insert is a counted conflict, never an error; a second concurrent run or repair refuses on the lock"). Grok's shape: same key, poller writes X and a repair independently wants Y (differing-value race) — assert which value survives and why; same key, poller writes X and the repair also computes X (equal-value race) — assert nothing spurious happens (no incident, no wasted write, or whatever the surviving-value assertion honestly is once you trace the code). Write these OFFLINE with the suite's existing fakes (`FakeBars`, the pool-reader/clock fakes in `test_archiver_quiet.py`) if a fake can express the ordering honestly — the existing boundary tests already script a sequence of instants and mid-flow writes, which is the same shape. If genuine concurrent-transaction semantics (which value a real Postgres commit race resolves to) cannot be expressed honestly by a fake, write the `requires_db` twin instead and say so plainly — do not stretch a fake to claim a guarantee only a real transaction can prove (L45). Name which of the two you took, and why, for each of the two tests. Commit: `test(archiver): differing-value and equal-value race tests (spec §15, tribunal round 1, F3/Q10)`. Last line `CONTINUE: step 4`.

## 4. F4 — replace the four name/attribute tests with tests that invoke the command
Read `verdicts.md` row 4. The four tests, each replaced (not just supplemented) with an assertion on behaviour:
- `test_the_refusal_exit_code_is_two` (`:246-247`, today: `assert QuietRefused("x").exit_code == 2`) → invoke `archiver_cli.main` (or the handler) with a setup that makes the quiet-window check fail, assert the REAL exit code your call site returns is 2.
- `test_both_mutating_commands_are_refused_in_every_scanned_session` (`:498-507`, today: computes `quiet_verdict` then checks list membership) → actually invoke `_cmd_restate`/`_cmd_backfill_missing` (or the dispatch through `HANDLERS`) for both commands across the four scanned sessions with a fake store, asserting each raises `QuietRefused` (or exits 2) AND that the fake store recorded no write.
- `test_previews_always_run` (`:510-519`, today: monkeypatches `require_quiet`, asserts `calls == []` with nothing invoked) → actually call the preview path (`_cmd_restate`/`_cmd_backfill_missing` with `--apply` absent, or their preview helper) and assert it runs to completion (prints, reads) without ever calling `require_quiet`/`guarded_repair` — a real invocation, not an unexercised monkeypatch.
- `test_backfill_missing_never_calls_upsert_bars` (`:553-561`, today: calls `store.insert_new_bars(None, …)` directly) → invoke `_cmd_backfill_missing` through its real entry point with a fake store, asserting `upsert_bars` is never called and only `insert_new_bars` is, on the SAME fake step 1 built.
Keep `test_backfill_missing_can_only_do_nothing`'s source-scan (`:545-551`) as-is — it is not one of the four cited, it is the sibling the hub called "weak but not wrong." TESTS FIRST for whichever RED/GREEN boundary applies (some of these will already be GREEN once steps 1–3 land, since they exercise the same command paths — if a rewritten test is GREEN before you'd expect it, say so and explain why rather than force an artificial RED). Commit: `test(archiver): four quiet-window tests assert on command behaviour, not names or attributes (tribunal round 1, F4)`. Last line `CONTINUE: step 5`.

## 5. F5 — map `QuietRefused` and `ArchiveLockError` to exit code 2 on the production `cobalt` entry
Read `verdicts.md` row 5 and `src/cobalt/cli.py:493-503` in full. Today: `main()`'s `try: args.func(args) except JobStopped … except Exception as e: print(f"FAILED: {type(e).__name__}: {e}", file=sys.stderr); sys.exit(1)` — `QuietRefused` and `ArchiveLockError` are both plain `RuntimeError`s and fall into the generic branch, exit 1. `ArchiveLockError` (`src/cobalt/archiver/store.py:39`) has NO `exit_code` attribute at all today — add `exit_code = 2` to it, matching `QuietRefused`'s own (`quiet.py:73`).
THE FIX: add a NEW except-clause in `cobalt/cli.py`'s `main()`, BEFORE the generic `except Exception`, catching `(quiet_mod.QuietRefused, ArchiveLockError)` (import both — `from cobalt.archiver.quiet import QuietRefused` beside the existing `from cobalt.archiver import cli as archiver_cli` import block, and `from cobalt.archiver.store import ArchiveLockError`), printing to stderr and calling `sys.exit(e.exit_code)`. Keep the message SHAPE consistent with today's other exceptions (`FAILED: {type(e).__name__}: {e}`) unless you have a specific reason to match `archiver_cli.main`'s own bare `str(refused)` instead — say which you chose and why. Do not touch `cobalt/cli.py`'s other lines; this file is shared with `ops/2026-09-19` (`:214-221`) — your edit is a new except-clause added to the existing `try/except` block at `:493-503`, never a reflow of a neighbour's lines. Keep every OTHER exception's behaviour (generic `Exception` → `FAILED:`/exit 1, `JobStopped` → `NOT RUN`/exit 0) untouched.
TESTS FIRST: a subprocess test (RED today — quote the exit code and stderr text `cobalt` actually returns for a refused repair or a locked run, showing exit 1) that invokes the real `cobalt` CLI entry point (however the existing CLI subprocess tests in this suite do it — follow that pattern, do not invent a new harness) with a scenario that raises `QuietRefused` or `ArchiveLockError`, asserting BOTH the stderr text and the exit code (2) after the fix. If a subprocess test would need `cobalt_dev` to construct the refusing scenario, build it against fakes/monkeypatches the way the CLI's existing subprocess tests already do (name that pattern in your report) rather than reaching for the database. Commit: `fix(cli): QuietRefused and ArchiveLockError exit 2 on the production cobalt entry, every other exception unchanged (tribunal round 1, F5)`. Last line `CONTINUE: step 6`.

## 6. F6 — the shadow artifact's night = the ET trading date
Read `verdicts.md` row 6, spec §5's shadow bullet (the `data/archiver-shadow/<YYYY-MM-DD>.jsonl` line — it names no timezone, which is the ambiguity), and `runner.py:33-42,152`. Today `night=clock().date()` takes the `.date()` of a UTC instant (`clock.now_utc()`); a run that actually happens at 20:30 ET lands on a different UTC calendar day than the ET trading night it is naming, and the test suite's own `FETCH_AT` (frozen at 20:30 **UTC**, not ET) never exercises the mismatch. FIX per the verdict row: derive `night` from the ET calendar date, using the ET-conversion tool already in the codebase (`cobalt.session.clock.SessionClock.to_et`, `clock.py:262`, or the module-level equivalent it wraps) rather than a raw `.date()` on the UTC instant. Do NOT redesign the artifact naming or retention scheme — this is the one-line date-derivation fix the verdict names, nothing more. TESTS FIRST: a test frozen at a UTC instant that is a genuinely different ET calendar date (e.g. 00:30 or 01:30 UTC, which is 20:30 ET the PRECEDING day in EDT) — RED today (asserts the naive `.date()` gives the wrong, later day), GREEN after (asserts the artifact's night is the ET trading date). Record explicitly in the report, as an `## ESCALATE` line, that spec §5's `<YYYY-MM-DD>` is ambiguous on timezone and this round resolved it to ET per the verdict row — this is a fix-per-row, not a design change, and the desk/owner may still rule differently. Commit: `fix(archiver): the shadow artifact's night is the ET trading date, not the UTC date of the run instant (tribunal round 1, F6)`. Last line `CONTINUE: step 7`.

## 7. F7 — pin `NULLS NOT DISTINCT` in the migration test
Read `verdicts.md` row 7 and `tests/cobalt/test_archiver_migrations.py:190` (`test_incidents_has_the_partial_unique_index_on_unresolved_rows`) plus `0011_archive_incidents.sql:84-92` (the `NULLS NOT DISTINCT` clause already in the SQL). TESTS FIRST: add an assertion to the existing test that is RED today (the test currently does not check for the clause — quote that it passes without the assertion, then add it) and GREEN once added (`assert "NULLS NOT DISTINCT" in <the SQL text the test reads>`), without removing any existing assertion in that test. Commit: `test(db-migrate): pin NULLS NOT DISTINCT on the archive_incidents partial unique index (tribunal round 1, F7)`. Last line `CONTINUE: step 8`.

## 8. Close (offline) — re-run the suite, append the report, stop
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 1's close (`1850 passed, 315 skipped, 0 failed`): failed stays 0, passed grows by this round's new/rewritten tests (list them by step), skipped grows only by this round's new `requires_db` tests (list them by name — expect at least the two from step 1 and whichever race test from step 3 needed the database twin) · `uv run cobalt jobs restarts c974fbf..HEAD` → the table VERBATIM (branch point for this round is round 1's CODE tip, `c974fbf`, not the report commit `7c972ee`) — expect only `src/cobalt/archiver/*`, `src/cobalt/cli.py`, `tests/cobalt/*` among the changed paths; 0 UNCLASSIFIED or ESCALATE why · `git diff main -- src/cobalt/radar/` EMPTY (quote it) · `git status --porcelain` empty · `git diff --stat c974fbf HEAD -- src tests docs` listed, every path one this prompt named · append `## ROUND 2` to `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` on THIS branch — a new top-level section AFTER the existing report's last line, containing: §0 headline ≤5 lines, one subsection per step (1–7) with its RED/GREEN quotes and commit, the CLOSE numbers above, and `## ROUND 2 ESCALATE` carrying forward everything round 1's `## ESCALATE` already had (untouched) PLUS: the F6 timezone-ambiguity line, whether `backfill-missing` needed any F1-shaped fix (state the finding either way), the still-OWED `requires_db` first run, and anything else this round found. Commit the report by explicit path: `docs(report): archiver append round 2 — <one line>`.
LAST LINE, exactly this shape:
`ARCHIVER APPEND R2 <tip> | on archiver/append-0919 (round 1 c974fbf) | offline <passed>/<failed> (<skipped> skipped; round-1 close 1850/0/315) | F1 restate <fixed|FAILED> · backfill-missing shape <already-safe|fixed|FAILED> | F2 command-path tests <added|FAILED> | F3/Q10 race tests <offline|requires_db|FAILED> | F4 four tests rewritten <fixed|FAILED> | F5 exit code 2 on cobalt <fixed|FAILED> | F6 shadow night = ET date <fixed|FAILED> | F7 NULLS NOT DISTINCT pinned <fixed|FAILED> | radar diff: empty <yes|no> | write_mode ships: upsert (unchanged) | RESTARTS: <line> | CARRIED: <n> requires_db tests never run (now includes this round's) | ESCALATE: <n>`
or `FAILED: <step> — <reason>`. Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or `cobalt_dev`.
