MODEL: Sonnet 5 (`claude-sonnet-5`) — stage, put one packet before three houses, collate; no verdict of your own (L37) · SEAT: tribunal hub `tribunal-0919b`, launched by the CTO desk in the background. NO NEW RULE: `01-review-harness.md`'s allowlist with one `mkdir` path changed (R9 of 2026-09-18; R1 of 2026-09-19). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/10-tribunal-archiver-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control tribunal-0919b --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/tribunal-archiver-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless ruling per house.

# FOUR-HOUSE TRIBUNAL, ROUND 2 of at most 3 — DESIGN: the append-only Bar Archiver. Round 1: three houses ruled `BUILD WITH MY AMENDMENTS`, no point 3-0. The fourth house (Fable, the proposer) derived `DERIVED-v2.md` from their amendments. Each house now ACCEPTS or OBJECTS, point by point. DO NOT STOP until the report ends `TRIBUNAL ROUND 2 DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words on main: `cto-2026-09-18.md` §4 R14, R15, R9, R18–R21; `cto-2026-09-19.md` §4 R1 and R5 ("The design needs to be reviewed by a tribunal and then built and then deployed."). Prove with `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED:` line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/08-tribunal-archiver.md` — round 1's prompt: its INDEX CARD, staging list, §2 launch rule and §3 collation rules bind you UNCHANGED, with `scratch/tribunal-archiver-r2-0919/` in every path, output files `grok-ruling-r2.md`, `gemini-ruling-r2.md`, `astra-ruling-r2.md`, report `TRIBUNAL-R2.md`; Astra answered four times today → `astra: UP (carried)`. Round 1's report: `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-archiver-0919/TRIBUNAL.md`.

## 1. Packet in `scratch/tribunal-archiver-r2-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `DERIVED-v2.md` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/10-packet/DERIVED-v2.md` · (2) `round1-amendments.md` = round 1's `## Rulings table` + `## Amendments, verbatim` sections, verbatim · (3)–(9) the SAME code and evidence files as round 1, copied from round 1's folder: `facts.md`, `store.py`, `runner.py`, `poller.py`, `0001_bars.sql`, `test_archiver_store.py`, `finviz-windows.md`, `owner-rulings.md` · (10) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): "You are one of four houses sitting as a design tribunal; this is ROUND 2 of at most 3. Read ONLY the files in this folder; you cannot run commands. In round 1 you and two other houses ruled on a proposal (`round1-amendments.md` holds all three rulings, yours included). `DERIVED-v2.md` is the version the fourth house derived from them; each point says whose amendment it took and why it declined the others. For EACH point V2-1 … V2-9, and for the closing list of tests and tools, give exactly one of ACCEPT / OBJECT. An OBJECT must carry (a) a CONCRETE failing sequence — tickers, times, the order of events — checked against the staged code files, and (b) the exact replacement wording. 'I preferred my own wording' is not an objection; 'this sequence loses a bar, here is the sequence' is. Where the derived version declined YOUR round-1 amendment, say plainly whether its stated reason holds against the code — V2-2 in particular rests on one claim about `poller.py` lines 96-105 (the poller cannot move `max(ts)` past a hole because it re-reads the whole export and keeps every closed bar newer than its watermark minus five minutes): verify that claim line by line and try to break it (a poll that fails for one ticker, a ticker that leaves and re-enters the pool, a budget cut-off mid-cycle, a first contact, a day the radar was down, two writers in the same minute). Then judge the desk's addition in V2-4 (a read-only audit run on production before the switch, stopping the deploy above 1 % differing targets): is the threshold meaningful, is the sample sound, what would you measure instead? Say whether anything in V2 departs from the owner's words in `owner-rulings.md`. End with exactly one line: `RULING: BUILD V2 / BUILD V2 WITH <V2-numbers> CHANGED AS I WROTE / DO NOT BUILD — <why in ten words>`."

## 2. Launch the three houses — round 1's launch rule EXACTLY, with this folder and the three `-r2` output names. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) verbatim, never looped.
## 3. Collate — round 1's §3 EXACTLY: `## Rulings table` (point · grok · gemini · astra · agreement · each objection's failing sequence in ≤30 words), `## Objections, verbatim`, `## Checked against the files` — for EVERY failing sequence a house gives, walk it through the staged code yourself and mark it `HOLDS / DOES NOT HOLD / NOT CHECKABLE FROM READS` with file:line. You judge no design question (L37); you do check code facts (L35).
## 4. Close
Last line: `TRIBUNAL ROUND 2 DONE · grok: <RULING line|HARNESS|METER|TIMEOUT> · gemini: <…> · astra: <…> · houses that ruled: <n> of 3 · points 3-0 ACCEPT: <list> · points with an OBJECT: <list> · failing sequences that HOLD against the code: <n>`. Then stop.
