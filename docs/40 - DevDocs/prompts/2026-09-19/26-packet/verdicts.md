## Verdict table
Verified (L35) against the branch tip `7c972ee` in `/Users/cobalt/cobalt-wt/archiver-append/` (`Read`/`grep`), not against the packet copies; where a packet copy differs I say so — none did. Two or three houses naming one defect would be one row; only Grok read, so one house per row.

| # | house | sev (theirs) | claim (≤20 words) | what I read | VERDICT | fix in one line (not applied) | blocks the branch shipping? |
|---|---|---|---|---|---|---|---|
| 1 | Grok (F1) | BLOCKER | `restate --apply` writes bars via `upsert_bars` on its own connection; failed pre-commit re-check cannot unwrite them | `cli.py:266-267` `with store.target_transaction() as conn: written = _apply_restate(store, conn, …)`; `cli.py:391-399` `_apply_restate` never uses `conn`, calls `store.upsert_bars(rows)`; `store.py:95-141` `upsert_bars` opens `self._connect()` and returns after its own `with` commit, `before_commit` hook exists (`:99,139-140`) but is not passed; `cli.py:287` `guard.check_before_commit()` only rolls back the incident write. The same `conn`-unused shape is absent in `backfill-missing` (`cli.py:313-315` writes on `conn`) | **REAL** | pass the re-check into the write (`before_commit=guard.check_before_commit`) or do the overwrite on `conn` inside the one transaction, and add the test the design asks for | **yes** — design §8's core guarantee ("a failed re-check = ROLLBACK", "no bar row surviving") is not met for the only command that may overwrite bars; the branch ships the CLI even in `upsert` mode. Nothing runs it automatically, so the deploy path is unaffected — the exposure is an operator running `restate --apply` |
| 2 | Grok (F2) | MAJOR | the two named §8 tests only call `quiet_verdict`; the rollback-on-recheck test uses `insert_new_bars`, not the restate shape | `test_archiver_quiet.py:333-384`, `:387-433` call only `quiet_verdict`; `grep` of the file shows `guarded_repair`/`check_before_commit` appear only at `:476`/`:481` (the pre-commit test), which writes with `store.insert_new_bars` (`:479`) — `upsert_bars` appears only as setup/assertions | **REAL** | drive `_cmd_restate`/`guarded_repair` with an `upsert_bars`-shaped write in the two named tests | no — a test gap, not runtime behaviour; but it is exactly why #1 escaped (fix together with #1) |
| 3 | Grok (F3) = **Q10** | MAJOR | no differing-value vs equal-value race test exists | `grep -rn -i "def test_.*race|differing.value|equal.value race" tests/cobalt` → only `test_vaultwrite.py` (unrelated); `test_archiver_quiet.py` `grep` for `race`/`differing`/`equal` → no match. The only poller-adjacent test is Known limit 1 (`:600-655`), a NEW key beside old-basis, not a same-key race | **REAL — the desk's named gap is CONFIRMED, not refuted** | write the two race tests §15 lists (same key: poller X vs repair Y, and poller X vs repair X) with the surviving value asserted | no — it changes nothing that runs in `upsert` mode; it is a shortfall against the design's §15 "must exist" list, so the desk may still make it a gate for the switch ruling (L37: desk's call) |
| 4 | Grok (F4) | MAJOR | four tests the report cites assert names/class attributes or never invoke the command | `test_archiver_quiet.py:246-247` `assert QuietRefused("x").exit_code == 2`; `:498-507` computes `quiet_verdict` then `assert command in MUTATING_COMMANDS`; `:510-519` monkeypatches `require_quiet` then `assert calls == []` with nothing invoked; `:553-561` calls `FakeBars.insert_new_bars` directly (the source scan at `:545-550` is the real proof) | **REAL** | assert on behaviour (invoke the handler with a refusing observer) instead of on names | no — test-quality debt; the source-scan siblings cover the same property weakly |
| 5 | Grok (F5) | MAJOR | exit code 2 for refusal / lock not proven on the production `cobalt` entry; `ArchiveLockError` never mapped | **Grok called this UNVERIFIABLE; I verified it.** `cobalt/cli.py:493-503`: `args.func(args)` inside `try … except JobStopped … except Exception as e: print(f"FAILED: {type(e).__name__}: {e}", file=sys.stderr); sys.exit(1)`. `QuietRefused`/`ArchiveLockError` are both `RuntimeError` → **exit 1**, message prefixed `FAILED: QuietRefused:`. The only `return refused.exit_code` is `archiver/cli.py:main` (`:510-518`), which no entry point in this diff calls | **REAL** | catch `QuietRefused`/`ArchiveLockError` where `cobalt.cli.main` calls `args.func` (or return the code from the handler) and test it through the real dispatch | no — the refusal is still loud and nothing is written; only the design's exit-code contract (§8/§9) is missed |
| 6 | Grok (F6) | MINOR | shadow artifact is named by the UTC date of an 20:30 ET run, one calendar day later than the ET night | `runner.py:152` `night=clock().date()`; `runner.py:33-42` `_now` = `clock.now_utc()`; tests freeze `FETCH_AT` at 20:30 **UTC** (`test_archiver_runner.py`, `datetime(2026, 9, 18, 20, 30, tzinfo=UTC)`) | **REAL** | derive the night from the ET date (the design's `<YYYY-MM-DD>` is ambiguous — desk to rule which) | no |
| 7 | Grok (F7) | MINOR | the incident unique-index test does not pin `NULLS NOT DISTINCT` | `grep -n "NULLS NOT DISTINCT\|NOT DISTINCT" tests/cobalt/test_archiver_migrations.py` → no match; the SQL has it (`0011…sql:92`) | **REAL** | assert the clause in `test_incidents_has_the_partial_unique_index_on_unresolved_rows` | no |

MINORs unverified beyond the above: none other reported. Grok's Q3 remark that the *shadow-failure summary is not byte-identical* (it still gains `shadow`, `shadow_errors == 1`) is the design's own counted `shadow_errors`, not a finding; I did not verify it further.

**UNVERIFIABLE FROM READS (4):** (a) the build's suite numbers (`1562/297` baseline, `1850/315/0` close, +288, +18 `requires_db`) — no pytest output is readable; re-derivation needs `Bash(uv run pytest *)`, which the launch line does NOT carry; (b) the `RESTARTS: com.cobalt.aset com.cobalt.radar` line — needs `Bash(uv run cobalt jobs restarts *)`, not in the launch line; (c) the report's AUTHORIZATION `grep -c` table against `prompts/2026-09-18/02-ops-2026-09-18.md` — readable with `grep`, but I did not re-run it (outside this check's question set; the builder itself reported 5 of 7 strings do not match literally, every difference a narrowing); (d) the report's chunk-P `148 insertions` vs the final store.py `+167` — `git diff --stat` needs `Bash(git diff*)` against the branch worktree, which this launch line has only for `s2-p2-cards` (`Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)`). Exact rule strings missing: `Bash(uv run pytest *)`, `Bash(uv run cobalt jobs restarts *)`, `Bash(git -C /Users/cobalt/cobalt diff*)`. I did not invent any.

Allowlist claims, rule beside command: `Bash(git -C /Users/cobalt/cobalt show*)` covered `git -C /Users/cobalt/cobalt show archiver/append-0919:"docs/…/archiver-append-build-2026-09-19.md"` (its output was a plain `tail`-shaped read, not a pipe: the command was the one bare `git … show`, output read from the tool result); plain reads of `/Users/cobalt/cobalt-wt/archiver-append/**` used `Read`/`grep` under `--add-dir /Users/cobalt/cobalt-wt`.
