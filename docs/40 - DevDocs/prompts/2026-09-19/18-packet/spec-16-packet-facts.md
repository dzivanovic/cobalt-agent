# Ops small items 2026-09-19 — facts read by the desk (fork of `318239db`, 09:10–09:27 ET, read-only; main `919d562`)

Source list: `docs/40 - DevDocs/reports/cto-2026-09-19.md` §17 owed item 4. Law step of the work item: BUILD. Every line below was read from the file named; nothing here was run against `cobalt_dev` or production.

## Verdict per item
| # | item | verdict | one reason |
|---|---|---|---|
| a | day-open C2 trading-day guard | BUILD | pure check logic; C3 already carries the same guard |
| b1 | `_band()` refuses an inverted band (both set, min > max) and a non-integer / negative value | BUILD | config validation (L1, L10); changes NO outcome for any valid band; his live band 2–6 passes |
| b2 | `_band()` "both-or-neither" (a HALF-SET band fails loud) | SCOPED OUT — DESIGN (desk) | today a null is deliberately an ADVERSE SIGNAL, not a crash (`daymode/cli.py:52-55` docstring, `propose.py:170`); turning a half-set band into a crash changes what `com.cobalt.daymode-propose` does at 09:00 from "step down" to "job FAILED" — a day-mode outcome (trading logic, L7 / CLAUDE.md HITL) |
| c | `lock_timeout` for deploy-time DDL | BUILD | harness safety, no schema, no data; the value is an engine tunable (not an L53 ceiling) — desk default 30 s, decided-with-veto |
| d | test hygiene A1 / A2 | BUILD (test file only) | two named test defects, production code untouched |
| e | the proof output carries the executed code tip | BUILD | one printed line; closes the known limit of `cto-2026-09-19.md` §14 |
| f | disarm `com.cobalt.archiver` in the deploy shape | SCOPED OUT — DEPLOY PROMPT + HIS APPROVAL | there is no deploy script in `ops/`; the "deploy shape" is the desk's deploy prompt and its launch line. Disarming = two NEW launch-line rules (`launchctl bootout gui/501/com.cobalt.archiver`, `launchctl bootstrap gui/501 <archiver plist path>`) → they go into the NEXT deploy's approval list, not into a build |

## a — C2 (`src/cobalt/dayopen/checks.py`)
- Origin: `cto-2026-09-19.md` §0 — 09-19 (Saturday) day-open AMBER on ONE check: C2 "no `radar_membership` rows for 2026-09-19"; no scanning session exists on a Saturday; C1, C3–C6 PASS.
- Code: `check_c2_radar_membership` `:109-194`. `:170-171` `if count == 0: return CheckResult("C2", title, Verdict.FAIL, f"FAIL — no rows for {report_date}", raw)` — evaluated BEFORE any calendar read. The calendar is only consulted afterwards (`:174` `windows = clock.windows_for(report_date)`; `:180-181` `on_time = True`, `window_note = "n/a (not a trading day)"`), so a non-trading day WITH rows already passes and one WITHOUT rows fails.
- The guard already exists one check down: C3 `:242-245` `windows = clock.windows_for(report_date)` · `trading_day = bool(windows)` · `exempt = not trading_day or not past_open`, and its test `tests/cobalt/test_dayopen_checks.py:249` `test_c3_weekend_red_is_idle` (constant `SUNDAY = date(2026, 9, 13)` at `:18`).
- Bound: `checks.py` C2 only + its tests + DevDoc `docs/40 - DevDocs/cobalt/dayopen/checks.md`. `clock.windows_for(report_date)` empty AND `count == 0` → `Verdict.PASS`, detail `PASS — no session today (<date> is not a trading day)`, the raw block still printed. A TRADING day with zero rows stays FAIL (`test_c2_fail_zero_rows`, `:170`, MONDAY — must stay green unchanged). NOT in the bound: the "before premarket open on a trading day" case (C3 exempts it, C2 does not; day-open runs after 04:00) — if the builder thinks it matters: ESCALATE line, not built.
- Red first: `test_c2_non_trading_day_with_zero_rows_is_pass` (SUNDAY, and Saturday 2026-09-19 — the real day) → FAIL today ("no rows"), PASS after. Plus `test_c2_trading_day_zero_rows_still_fails` = the existing test, untouched.
- P4 overlap: P4 changes `src/cobalt/dayopen/models.py` (adds `Verdict.KNOWN`, `overall_verdict` over a Protocol, empty list raises) — NOT `checks.py`. Do not touch `models.py`.

## b — the trade-count band (`src/cobalt/daymode/`)
- Origin: `cto-2026-09-18.md` §18 F2 (Grok/Gemini build review): "the over-band fact has no guard for an inverted (min > max) or half-set band (`propose.py:153,170-171`; `daymode/cli.py:52-66`) — REAL, MINOR in effect — his live band 2–6 is valid and `solidified`".
- Code: `daymode/cli.py:52-66` `_band()` returns `(row.value, row.value)` from `load_tunables().by_key` for `BAND_MIN_KEY` / `BAND_MAX_KEY` (`propose.py:85-86`); a missing ROW is `SystemExit` (F16); a null VALUE is "not ruled yet" and is returned as `None`. `propose.py:153` `band_min, band_max = band`; `:170` `"trade_count_band_placeholder": band_min is None or band_max is None`; `:171` `"trade_count_over_band": band_max is not None and prior_filled > band_max`. `band_min` is never compared with anything. SECOND reader of the same two rows: `src/cobalt/cli.py:214-221` (`cobalt validate` prints them, validates nothing).
- Live values: `configs/cobalt/taxonomy/tunables.yaml:353-368` min 2, max 6, `status: solidified`, `source: ruling`.
- Bound (b1): ONE validation function in `cobalt.daymode` (beside `BAND_MIN_KEY`), called by `_band()` AND by `cobalt validate` (`cli.py:214`) so a bad band is caught at the deploy's `validate`, not at 09:00 (L10, L3): both values set → each an `int` ≥ 0 (a `bool` is not an int here) and `min <= max`, else fail loud naming both keys and both values. both null → unchanged (placeholder, adverse). EXACTLY one null → UNCHANGED today (b2 is scoped out): still the placeholder signal; the function says so in its docstring and a test pins it.
- Red first: inverted `(6, 2)` → refusal; `(2, "6")`, `(2.5, 6)`, `(-1, 6)`, `(True, 6)` → refusal; `(2, 6)`, `(3, 3)`, `(None, None)`, `(2, None)`, `(None, 6)` → accepted and `day_facts` output byte-identical to today's (one parametrized test over the five facts dicts). `test_a_day_with_7_trades_is_adverse` (`tests/cobalt/test_daymode.py:655`) stays green untouched.
- This IS the day-mode path (trading logic). The chunk changes no fact, no step-down, no settings row, and needs no settings load. If the builder finds that it cannot validate without changing a fact's value for some VALID band → STOP the item, ESCALATE.

## c — `lock_timeout` (`src/cobalt/db_migrations/cli.py`)
- Origin: `prod-proof-only-3-2026-09-19.md` ESCALATE 3 ("Nothing in `cli.py` sets a `lock_timeout` or a statement timeout … a deploy whose `0003` ALTER meets a heartbeat write waits indefinitely rather than failing fast"); `cto-2026-09-19.md` §5a last bullet ("bounded today only by the Bash tool's 600 s timeout"); review U1.
- Code: `_connect` `:440-497` — `db.connect_migration(...)`, then `conn.read_only`, `conn.isolation_level = REPEATABLE_READ`, `conn.autocommit = False`, `_assert_utf8(conn)` (`:421-437`, runs `SHOW server_encoding` — the first statement, which opens the transaction). `cmd_migrate` `:536-590`: read-write path = `_probe_all` → `_apply` (`:407-418`, whole files via `conn.execute(path.read_text())`) → `_probe_all` → commit / rollback; `SerializationFailure` is already wrapped into a `MigrationError` with rollback (`:563-577`) — the shape to copy. `grep -n "lock_timeout\|statement_timeout" cli.py` → nothing.
- Bound: `cli.py` + `tests/cobalt/test_migrate_proof.py` + DevDoc `docs/40 - DevDocs/cobalt/db_migrations/cli.md`. The READ-WRITE migrate transaction (forward AND rollback) issues `SET LOCAL lock_timeout = '<n>s'` before `_apply`'s first statement; `<n>` = module constant `DEFAULT_LOCK_TIMEOUT_S = 30` with a CLI flag `--lock-timeout-s <int>` (0 refused: "0 means wait forever — that is the defect this flag closes"); `psycopg.errors.LockNotAvailable` → rollback → `MigrationError` that says NOTHING WAS APPLIED, names the timeout, and tells the operator to find the session holding the lock (`pg_locks`) — same shape as the serialization wrapper. `--proof-only` is NOT changed (it takes ACCESS SHARE only and must not start failing on a busy evening). THE SNAPSHOT: the builder states in the docstring, from the Postgres manual, whether `SET LOCAL` takes the REPEATABLE READ snapshot (it must not move the snapshot relative to `_probe_all`; if in doubt the `SET LOCAL` goes AFTER the BEFORE probe and before `_apply`).
- Do NOT touch `DIGEST_EXCLUDED_COLUMNS` (`cli.py:≈52-57`): P4 (`sprint-2/p4`) adds two entries there (+5 lines, `rank_metric`, `rank_value`) — the only lines of `cli.py` P4 changes; keep the rebase trivial for whichever branch lands second.
- Red first, OFFLINE (the file already has fakes: `_RecordingConn` `:254`, `_RecordingConnection` `:599`): the recorded statement list of a forward run contains `SET LOCAL lock_timeout` BEFORE the first migration file's text and AFTER nothing that writes; a fake `_apply` raising `LockNotAvailable` → rollback called, `MigrationError`, exit 1 through the CLI boundary test's shape (`:1144`); `--lock-timeout-s 0` refused before any connection; `--proof-only` sends no `lock_timeout`. WRITTEN BUT NOT RUNNABLE TODAY (`requires_db`, `:85`): a second session holds `ACCESS EXCLUSIVE` on `system.cobalt_jobs`, migrate with `--lock-timeout-s 1` → FAILED in ≈1 s, table unchanged. Interaction to check on the DB run: test (g) `:1203` deliberately makes the ALTER WAIT behind an open transaction — its wait is sub-second, far under 30 s, and must stay green.

## d — A1 / A2 (`tests/cobalt/test_migrate_proof.py`, test file ONLY)
- Origin: `cto-2026-09-19.md` §8, build check round 2, Astra: "A1 test (g) leaves its worker connection open if a lock wait outlives the 20 s ceiling …; A2 the blocked-lock probe does not filter by pid/mode"; desk ruling there: "statement_timeout on the worker connection + cleanup on the hung path; pid + `AccessExclusiveLock` in the probe".
- Code: test (g) `test_the_alter_waits_for_an_open_transaction_and_then_completes` `:1203-1272`; `LOCK_CEILING_S = 20.0` `:1001`. A1: `:1246-1249` `if not thread.is_alive(): conn.rollback(); conn.close()` — on the hung path `conn` is never closed and the daemon thread keeps a backend waiting for ACCESS EXCLUSIVE on `system.cobalt_jobs` in `cobalt_dev` until the interpreter exits. A2: `_wait_for_a_blocked_lock` `:1048-1071` counts ANY ungranted lock on the table (`WHERE c.relname = %s AND n.nspname = %s AND NOT l.granted`) — another session's wait satisfies it.
- Bound: A1 — the worker connection gets a server-side ceiling (`SET statement_timeout` a little above `LOCK_CEILING_S`) so a hung ALTER ends itself; after `thread.join`, the hung path cancels and closes (`conn.cancel()` from the main thread is psycopg's documented cross-thread call, then join again, then close) and the assertion message still says "hung". A2 — the probe takes the worker's backend pid (`SELECT pg_backend_pid()` on `conn` before the thread starts) and filters `l.pid = %s AND l.mode = 'AccessExclusiveLock' AND NOT l.granted`. Same two fixes for test (f) `:1171` only if it shares the helper (it does not start a thread — read it first).
- These are `requires_db`: offline they SKIP. Offline proof today = the file still collects and skips cleanly (`uv run pytest -q tests/cobalt/test_migrate_proof.py` → same passed count as before + the same skips, 0 errors). Their real run is OWED on `cobalt_dev` after `p4-verify-0919`'s stop line.

## e — the proof names its code (`src/cobalt/db_migrations/cli.py`)
- Origin: `cto-2026-09-19.md` §14: "the git-history binding proves the proof report was committed after `ad9b07c` on the branch, not the executed tip as a field — the desk verified by hand … → ops item: the proof's last line carries the executed tip".
- Code: `--proof-only` prints `cobalt db migrate — PROOF ONLY on {dbname} (READ ONLY, nothing applied)` (`:542`), the table (`_print_probe` `:371-404`), `NOTHING WAS APPLIED: …` (`:404`). Forward prints `cobalt db migrate — {direction} on {dbname}` (`:553`). Neither names the checkout. Two git helpers already exist: `src/cobalt/jobs/restarts.py:58` `_git(*args)` and `src/cobalt/generated/committer.py:60` `_git(repo_root, *args, check=True)`.
- Bound: BOTH modes print, as the LAST line of their output, `code: <short sha> (<clean | DIRTY: n path(s)>) · <repo root>` for the checkout the running `cobalt` package was imported from. NO third copy of a git helper (L3): import an existing one; if neither can be imported without a side effect or a cycle → STOP the item, ESCALATE the import problem. A git failure never fails a migration: the line reads `code: UNKNOWN — <reason>` (explicit, never a plausible value, L1) and the DEPLOY GATE — the desk's prompt — refuses an UNKNOWN or a DIRTY tip. Decided-with-veto by the desk.
- Red first (offline, fakes + `monkeypatch` of the helper): proof-only output ends with `code: <sha> (clean)`; a dirty tree says `DIRTY: 2 path(s)`; a raising helper → `code: UNKNOWN — …` and exit 0 for proof-only; the forward path prints the same line after the proof table.

## f — the archiver in the deploy shape (facts for the desk's next deploy prompt; nothing built)
- `ops/com.cobalt.archiver.plist:54-63`: `StartCalendarInterval` Weekday 1–5 at 20:30, `RunAtLoad` false, no `KeepAlive` → it cannot run on a Saturday or a Sunday; a weekday deploy inside the 20:00–21:00 pause (L43, L66) DOES overlap its 20:30 start.
- `prompts/2026-09-19/02-deploy-stack-3.md` launch line: `launchctl bootout` / `bootstrap` / `kickstart -k` rules exist for `com.cobalt.aset` and `com.cobalt.radar` ONLY.
- What the desk owes instead of a build: in the next WEEKDAY deploy prompt, `launchctl bootout gui/501/com.cobalt.archiver` before the merge and `launchctl bootstrap gui/501 <the installed archiver plist path>` after — two new rules in that deploy's approval list (L62), installed-plist path to be read with `launchctl print gui/501/com.cobalt.archiver` at that time. For the two weekend deploys the clock rule already covers it (no weekend schedule).

## Cross-branch facts (for the three-house check and the deploy)
- `sprint-2/p4` (unmerged, being verified today) touches `src/cobalt/db_migrations/cli.py` (+5, the excluded-columns tuple), `__init__.py`, `placement.py`, `src/cobalt/dayopen/models.py`, `ops/README.md`. This ops branch touches `cli.py` (other regions), `dayopen/checks.py`, `daymode/`, `src/cobalt/cli.py`, two test files, three DevDocs. Two unmerged branches → whichever lands second rebases, and the integrated suite on the combined tree is the gate (`close-2026-09-18.md` proposed law (a); 09-17 P2 revert lesson).
- NOT VERIFIED by this desk: whether `jobs/restarts._git` can be imported into `db_migrations/cli.py` without a cycle; the exact Postgres rule for when `SET LOCAL` takes a snapshot; whether `cobalt validate` (`cli.py:162-221`) runs with no DB (the 09-18 close ran it with the `.env` in) — so `validate` is NOT run in this offline build, its band check is proven by unit test only.
