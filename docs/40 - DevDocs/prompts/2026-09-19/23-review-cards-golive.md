MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-cards-golive-0919`, launched by the CTO desk in the background. NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `18-review-ops-0919.md`'s launch line, with exactly ONE string changed — the `mkdir` path, from `scratch/review-ops-0919` to `scratch/review-cards-golive-0919` — because this check needs no rule `18` did not already carry (this packet is files-only, read with `Read`/`grep`/`ls`/`wc`, inside the same `agy-trial` worktree `18` already ran in; the unused `git -C .../s2-p2-cards *`/`git -C /Users/cobalt/cobalt show*`/`log*` rules stay, unused, because they are not the string that changed and no branch/commit lookup is needed for a drafted settings file that has never been committed or applied). If this check turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/23-review-cards-golive.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-cards-golive-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-cards-golive-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of an ARTIFACT, not a build (LAWS L67: every design, development or DEPLOYMENT is checked by at least THREE members of the tribunal before it ships; Fable is NOT one of the three checkers today — the checkers are Astra (OpenAI), Grok (xAI), Gemini (Google), per R9 2026-09-19). Item under check: `docs/40 - DevDocs/prompts/2026-09-19/23-packet/p2-live-settings.yaml` — a DRAFTED go-live card-settings file (never applied to any database; not yet reviewed by anyone; the desk's own read already found it likely INVALID against the schema — the reviewers confirm or contradict that independently from the same files). This is the artifact R12 (10:56 ET) and R13 (11:00 ET) require before `radar.cards_enabled` can flip true for Monday 2026-09-21 premarket. Round 1 of at most 3 (L67: ≤3 rounds per house while meter allows, never fewer than 2 houses total). DO NOT STOP until the report ends `CARDS GOLIVE CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. L67 ("any design, development or DEPLOYMENT is checked by at least ONE house other than its author" / "every DEVELOPMENT (build) is checked by at least THREE members of the tribunal") — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. This artifact's own debt: `docs/40 - DevDocs/reports/ladder-audit-2026-09-19.md` §7(2) names "L67 ≥1 other house checks every deployment … NOT YET MET for the go-live settings file itself — a new artifact, not yet reviewed" — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/ladder-audit-2026-09-19.md"`. R12/R13 themselves: `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the desk's own `facts.md` claims (treat it as a CLAIM, exactly like a builder's report) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-cards-golive-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff/hash is PRE-COMPUTED into a file, never asked of a reviewer by command. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-cards-golive-0919/REVIEW.md` — the same gitignored-scratch pattern `01`/`14`/`18` use, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/23-packet/`; copy each into `scratch/review-cards-golive-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `p2-live-settings.yaml` = `23-packet/p2-live-settings.yaml` — the DRAFT go-live file, never applied · (2) `p2-dark-settings.yaml` = `23-packet/p2-dark-settings.yaml` — byte-identical copy of the file currently loaded in production (sha256 `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca`, the rollback target) · (3) `golive.diff` = `23-packet/golive.diff` — `diff -u` between them, pre-computed · (4) `facts.md` = `23-packet/facts.md` — the desk's pre-computed reading of the schema, the loader, the restart/market-reset gate, the rollback, and what Monday looks like, all with file:line · (5) `src-settings-card.py` = `23-packet/src-settings-card.py`, full copy of `src/cobalt/settings/card.py` (the schema + loader this file must pass) · (6) `src-radar-evaluate-desk_shadow.py`, `src-session-guard-assert_writable.py`, `src-session-clock-windows_for.py`, `src-cards-shadow_report-excerpt.py`, `src-settings-store.py` = the matching `23-packet/` excerpts, verbatim `sed` cuts of the real files at the line ranges named in `facts.md` · (7) `plan-s2-p2-excerpt-step5-10.md` = `23-packet/plan-s2-p2-excerpt-step5-10.md`, verbatim excerpt of the design-of-record plan's STEP-5/STEP-10/Dejan's-open-items sections · (8) `QUESTIONS.md`, verbatim below. If a file is too large for one Write call, split `name.part1`, `name.part2` and say so in `## Packet`.

QUESTIONS.md (verbatim): see `23-packet/QUESTIONS.md` — copy it byte-for-byte into `scratch/review-cards-golive-0919/QUESTIONS.md` as part of staging step (8) above; it is the same text every house receives (L44).

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-cards-golive-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: `src-settings-card.py`/etc. against `git -C /Users/cobalt/cobalt show main:src/cobalt/settings/card.py` (reachable under the SAME `show*` rule) and against the real files under `/Users/cobalt/cobalt` with Read and `grep` (no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (for example, actually running `cobalt settings load --card ... --dry-run` to watch it fail, which is a `cobalt` command none of you may run) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks the switch-on? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-cards-golive-0919/`. Last line: `CARDS GOLIVE CHECK · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks switch-on: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
