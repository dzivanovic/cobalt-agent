MODEL: Opus 5 (`claude-opus-5`) — the session builds itself, no headless builders (this build adds a write path, two migrations and repair commands: Opus floor, L29) · SEAT: archiver build hub `archiver-append-0919`, launched by the CTO desk in the background. NO NEW RULE: every rule below is copied byte for byte from the launch line of `prompts/2026-09-19/16-ops-2026-09-19.md`, which is itself the 2026-09-18 06:46 ET approval (`cto-2026-09-18.md` §4 R1 list (2)) as committed in `prompts/2026-09-18/02-ops-2026-09-18.md` with every sensitive rule DROPPED — same job shape (a dated worktree off main, Opus, editing, testing and committing inside its own worktree, OFFLINE). THIS RUN IS OFFLINE BY DESIGN: `cobalt_dev` belongs to `p4-verify-0919` today. Three bare commands, run by the desk AFTER the design file is committed on main: `git -C /Users/cobalt/cobalt worktree add -b archiver/append-0919 /Users/cobalt/cobalt-wt/archiver-append main` then `cd /Users/cobalt/cobalt-wt/archiver-append` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/17-archiver-append-build.md' and follow it exactly." --model claude-opus-5 --remote-control archiver-append-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every launch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `cobalt_dev`, no `.env`, no vault write, no other seat's config · METER: Anthropic LARGE (seven chunks), and the Opus meter is SHARED today with `p4-verify-0919`'s headless builders and with `ops-0919` — a usage-limit message is not a failure: record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches; you have no `CronCreate` rule); Codex = NONE; Grok = NONE · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# Archiver append-only — BUILD of the tribunal's FINAL design, tests FIRST, OFFLINE, one commit per chunk. THE SPEC is one file: `/Users/cobalt/cobalt/docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` (also in this worktree at the same relative path). LAW STEP of this work item (L67): the DESIGN is closed (three tribunal rounds + the owner's ruling R8 "A on archiver") → this is the BUILD. After it, none of it yours: at least three houses check the build · the `requires_db` tests you write get their first real run on `cobalt_dev` once `p4-verify-0919` has released it · the desk writes the deploy prompt, which deploys `archiver.write_mode: upsert` · his approval · shadow nights · HIS ruling to switch to `append`. You never merge, never deploy, never push, never touch production, `cobalt_dev` or a `.env`. You NEVER touch `src/cobalt/radar/poller.py` or any line of the radar's write path (R8). DO NOT STOP until the report ends `ARCHIVER APPEND BUILT …`, `CONTINUE: … — meter …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable `318239db`, 2026-09-19), not by Dejan; a prompt file is not an approval. His words, with times, are committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4 **R1** (06:46 "Approved" — list (2), the ops hub's launch line as committed in `prompts/2026-09-18/02-ops-2026-09-18.md`), **R14/R15** (his words on the nightly overwrite — the purpose of this build), **R18–R21** (L67: every design to the four houses, every build checked by three); `cto-2026-09-17.md` §4 **R5** (the unattended standard); `cto-2026-09-19.md` §4 **R5** (the law is followed, not re-asked), **R3 + R6** (no idle lane, never a skipped law step) and **R8** (09:30 "A on archiver" — repairs only in a quiet window, the live poller untouched, the switch to `append` NOT covered). This launch line adds NO rule: `grep -c` each rule string above in `prompts/2026-09-18/02-ops-2026-09-18.md` — every one must count 1. Prove the rows are committed: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"`. The design file must be committed on main: `git -C /Users/cobalt/cobalt log --oneline -2 -- "docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md"` (empty → `FAILED: authorization mismatch — the spec is not on main`). Laws behind it: L61, L62, L67, L29, L55, L36 (you launch nothing). A rule here that is not in that committed line, or a row that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP. Only the dialog is removed, because nobody sees a `--bg` dialog. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. The desk watches that line. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the spec's §14 OPEN table (where none is named: do not build that item), continue; never block on an answer, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(archiver-append): <chunk> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit file paths under `src/`, `tests/`, `configs/cobalt/taxonomy/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `data/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>` (you have no reset rule; the desk repairs it).

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you build a write path, L59) → **THE SPEC** `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md`, every section (§2 = the code facts with file:line, §3–§9 = behaviour, §10 = config keys, §11 = tables + migrations, §14 = OPEN items with your safe default, §15 = THE TEST LIST — binding; where your reading of the code differs from §2, the CODE wins and the difference is an ESCALATE line) → the code it binds to: `src/cobalt/archiver/{store,runner,collector,config,models,report}.py`, `src/cobalt/radar/poller.py:96-105` and `src/cobalt/radar/runner.py:35-48, 170-176, 279-296` (READ ONLY — you change neither), `src/cobalt/radar/store.py:39, 161-173, 227-257` (read only), `src/cobalt/session/clock.py:196-254`, `src/cobalt/db_migrations/{__init__,placement}.py` + `0006_radar_score.sql` (the pattern for a system table's ownership and grants), `src/cobalt/heartbeat/probes.py:261-320`, `configs/cobalt/taxonomy/tunables.yaml:460-500` (the row shape) + `src/cobalt/taxonomy/tunables.py` → for the houses' reasoning when the spec is terse: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/12-packet/DERIVED-v3.md`, `10-packet/DERIVED-v2.md`, and `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-archiver-r3-0919/TRIBUNAL-R3.md` (the sequences that become dated tests, in the houses' words) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md` (the sister run in `ops-2026-09-19`, for the cross-branch note only). Laws, one line each: L1 fail loud — a bad `write_mode` crashes, a missing pool row REFUSES, never a plausible default · L3 one path — one settings model, one comparison, one quiet-window function, one range read · L7 the switch to `append` is a shadow run + numbers + HIS ruling: you ship `upsert` and you never flip it · L10 config validated on load, printed by `validate` · L28/L40 the archiver owns writes to `system.bars` outside the poller; repairs are gated · L29 Opus floor · L32 both new tables are SYSTEM side; no ticker from his lists in a committed fixture or report (construct names like `TESTARCH`, or the houses' public tickers from the tribunal text) · L35 artifact, not claim · L42 RESTARTS derived by the command · L45 real-shape fixtures: every CSV a test parses has the vendor's real header and row shape, taken from the collector tests' existing fixtures — never an invented format · L46 one commit per chunk, tree clean before you stop · L48 evidence in the report file in the same turn · L52 nothing here touches scoring, ranking or what reaches a card — if a chunk turns out to, STOP it and ESCALATE · L54 you work in this worktree only · L57 every counter reconciles by the spec's identities · L66 (for your ESCALATE) a deploy stops the residents before the merge. Report (L48): `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` in THIS worktree — §0 Headline ≤5 lines (line 1 `DO NOT STOP until ARCHIVER APPEND BUILT`) → `## AUTHORIZATION` → `## PREFLIGHT` → `## BASELINE` → one section per chunk (red-first evidence, diff stat, suite line) → `## SPEC vs CODE` (every difference you found) → `## ESCALATE` → last line = `CONTINUE:` / stop line / `FAILED`. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. Counting is done by you from the tool result, never with a pipe. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `uv run archiver`, `uv run cobalt` anything except `jobs restarts`, any `COBALT_ENV=…` command, `launchctl`, `claude`. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry a denied command in another shape, never ask another session to run it.
PREFLIGHT (first two minutes; one row each: rule · command · exit · allowed/DENIED + reason verbatim; every probe sits inside its own rule's pattern): `date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; the branch must be `archiver/append-0919`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: main's tip when the desk created the worktree; a resume: the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it) · `ls -la .env` → MUST say "No such file" (a suite that finds a `.env` reads `cobalt_dev`, 09-18's lesson); if it exists → `FAILED PREFLIGHT: .env present — this run must not see the dev database` · `ls "docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md"` (the spec is in your worktree) · `uv run pytest --co -q tests/cobalt/test_archiver_store.py` · `uv run cobalt jobs restarts main..HEAD` (empty range on a first launch — the probe of the rule). Any denial → `FAILED PREFLIGHT: <rules>`.

MIGRATION NUMBERS — how this prompt handles the unmerged P4 branch: `0008` and `0009` exist ONLY on `sprint-2/p4` and are NEVER used here. You create **`0010_archive_progress.sql`** and **`0011_archive_incidents.sql`** (+ their `.rollback.sql`) and register them in `FORWARD` / `REVERSE` after `0007` — a gap in the tuple is legal (explicit list, no contiguity test; if you FIND a test that asserts contiguity, STOP chunk 2 and ESCALATE it, change nothing). The branch that lands second rebases and keeps BOTH sets in numeric order; say so under `## ESCALATE`. Same for the OTHER files P4 also changes — `db_migrations/__init__.py`, `placement.py`, `configs/cobalt/taxonomy/tunables.yaml`, `src/cobalt/cli.py`, `archiver/runner.py` (P4 adds a `_check_demand(targets, mode)` call at the top of `_run_targets`), `archiver/store.py` (P4 adds `bars_between`) — and the one `ops-0919` changes (`src/cobalt/cli.py` `:214-221`, `db_migrations/cli.py`): keep your edits in those files to NEW blocks (a new subparser block, new tuple entries, new rows at the END of the radar/archiver group), never reflow a neighbour's lines.

STRUCTURE THAT MAKES THIS BUILD TESTABLE OFFLINE (binding): every DECISION of the spec is a PURE function with no I/O — eligibility (V2-3), the candidate range (§4), the four-way comparison and normalisation (§6), regression / gap / empty decisions (§7), the counting identities (§7), the quiet-window verdict and its refusal text (§8) — in NEW modules under `src/cobalt/archiver/` (suggested: `settings.py`, `reconcile.py`, `quiet.py`, `shadow.py`, `progress.py`, `incidents.py`, `cli.py`); stores and the runner only fetch, call them, and write. The pure functions get the whole of §15's offline list; everything that needs Postgres is a `requires_db` test in the existing pattern (`tests/cobalt/test_archiver_store.py:25-30`), WRITTEN, must SKIP cleanly offline (a collection error is a failure), its first run OWED.

## 0. BASELINE (before any edit)
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line `<p> passed, <s> skipped, 0 failed`. Every later suite line is compared with it: passed grows only by your tests, skipped only by your `requires_db` tests, failed stays 0. A red BASELINE is not yours to fix: `FAILED: step 0 — baseline red <n>`, stop. Also `uv run pytest -q tests/cobalt/test_archiver_store.py tests/cobalt/test_radar_poller.py tests/cobalt/test_archiver_report.py` alone → its own line (the differential baseline for chunk 5). Last line `CONTINUE: step 1`.

## 1. Chunk S — settings and the mode flag (spec §10)
Tests FIRST: `ArchiverSettings` resolves the seven keys from the shipped `tunables.yaml`; `archiver.write_mode` accepts EXACTLY `upsert` / `append` — `Append`, ` upsert`, `""`, `both`, a missing row each fail validation NAMING THE KEY; the shipped value is `upsert` (a test pins it: the default can only change by a reviewed commit after his ruling); `shadow_compare` on/off; the three repair minutes are positive integers. Then: the seven rows in `configs/cobalt/taxonomy/tunables.yaml` (row shape of `radar.poll_overlap_bars`; `status: proposed` exactly where the spec says PROPOSED, `source: ruling` with the citation `cto-2026-09-19.md §16 / R8` where it says the desk's number), ONE model `cobalt.archiver.settings.ArchiverSettings` read through the existing tunables loader (L3 — no second YAML reader), and `cobalt validate` printing it — proven by a unit test of the function `validate` calls (you do not run `cobalt validate`). If the tunables schema cannot carry a `label` enum without changing `src/cobalt/taxonomy/tunables.py` → STOP the chunk, ESCALATE the exact limit. DevDoc `docs/40 - DevDocs/cobalt/archiver/settings.md`. Commit `feat(archiver): ArchiverSettings — write_mode upsert|append (ships upsert), shadow and quiet-window keys, validated on load`. Last line `CONTINUE: step 2`.

## 2. Chunk M — the two additive tables (spec §11)
Tests FIRST (offline): `FORWARD` ends `…0007, 0010, 0011` and `REVERSE` begins `0011, 0010, 0007…`; each new file has its rollback; `placement.side_of("archive_progress")` and `("archive_incidents")` are SYSTEM; the SQL text carries the PK, the `CHECK (archived_through <= export_newest)`, the five incident kinds, the partial unique index `WHERE resolved_at IS NULL`, and NO grant to `cobalt_user`. `requires_db` (written, skipped): forward → both tables exist on the system side; rollback `--down-to 0007` drops exactly those two and nothing else; forward again; the tenancy suite's one-side assertion still holds. Then the four SQL files, the registry entries, the placement entries, following `0006_radar_score.sql`'s ownership/grant pattern line for line. DevDoc `docs/40 - DevDocs/cobalt/db_migrations/__init__.md` (the list). Commit `feat(db): 0010 archive_progress + 0011 archive_incidents — additive, system side, rollback = drop own table`. Last line `CONTINUE: step 3`.

## 3. Chunk R — the pure core: `reconcile.py` (spec §3 V2-3, §4, §6, §7)
Tests FIRST — this chunk carries most of spec §15: the TIME list (regular close, 13:00 half-day incl. the i30 12:30 bar, extended hours, Thanksgiving Thursday + Friday half-day, weekend, 09-07 → 09-08 holiday, both DST dates — assert selected ET dates AND UTC boundaries; a fetch crossing a bar close; unknown interval fails loud); the RANGE (union of `ts > archived_through` and the two most recent completed export-present sessions + today; bootstrap = whole export); the four ROUND-2 SEQUENCES as dated tests in the houses' own dates; REGRESSION / ILLIQUID (THIN unchanged three sessions = success; strict regression = failure with evidence; IMCC future-dated row then valid export; half-day is not truncation); GAP (`export_oldest > archived_through`); COUNTERS (Grok's 100-key case, Astra's AAPL/i5 case, MSFT 19:59 new-not-late, MSFT 19:59 equal = 1/1/0, complete withholding) — every result object asserts the spec's identities in its own validator, so a non-reconciling count cannot be constructed (L57, L1). Normalisation to `NUMERIC(14,4)` / integer volume before equality. No I/O, no clock read (the instant is an argument), no DB. DevDoc `archiver/reconcile.md`. Commit `feat(archiver): reconcile — eligibility, candidate range, four-way comparison, regression/gap rules, reconciling counters (pure)`. Last line `CONTINUE: step 4`.

## 4. Chunk P — store methods on ONE connection (spec §3 V2-1, §4, §9, §11)
Tests FIRST. Offline, with a recording fake connection (the shape `tests/cobalt/test_migrate_proof.py` uses): `insert_new_bars(conn, bars)` sends `ON CONFLICT (ticker, interval, ts) DO NOTHING`, counts ACTUAL inserts, never opens or commits a connection; the range read, the progress upsert (`GREATEST`, only on an accepted outcome) and the incident open-or-refresh all take `conn`; `upsert_bars` is BYTE-IDENTICAL to main (`git diff main -- src/cobalt/archiver/store.py` shows additions only — quote it). `requires_db` (written, skipped): the append path never modifies an existing row; a poller-style insert between the range read and the insert is a counted conflict, not an error; crash after inserts before progress / before commit → neither is durable, retry idempotent; the advisory lock — a second holder refuses. The lock: ONE constant key, session-level, helper in the store module. O-5 of the spec: your range read is ONE private method taking a connection; name the P4 `bars_between` overlap under `## ESCALATE`. DevDocs `archiver/store.md`, `progress.md`, `incidents.md`. Commit `feat(archiver): append path — insert_new_bars, progress and incident writers on the target's one transaction; run-level advisory lock`. Last line `CONTINUE: step 5`.

## 5. Chunk N — the runner: MODE ISOLATION, the shadow compare, the append night (spec §5, §6, §7, §9)
Tests FIRST, offline, with a fake store and a fake `fetch` (there is NO runner test file today — you create `tests/cobalt/test_archiver_runner.py`): DIFFERENTIAL `upsert` tests against today's behaviour — night 1 AND night 2 send the WHOLE export to `upsert_bars`, keep the incomplete latest bar, write no progress and no incident, keep today's failure on an empty export, keep the submitted-row count and the run-report row byte for byte; the COMPLETE LIST of upsert-mode additions (lock, shadow read, `job.result["shadow"]` + the artifact) is pinned by one test that fails if a fourth appears; SHADOW — runs after the fetch and BEFORE the write, labelled `pre-write`; an exception, a timeout and a slow read inside it each leave the target's upsert and the summary IDENTICAL; it calls no write method of the store (the fake records every call); artifact schema, both scopes, field-level differences; `shadow_compare: off` skips it; APPEND night — bootstrap, steady state, withheld target (no insert, progress frozen, incident after rollback, target FAILED), gap (incident BEFORE progress advances, DEGRADED), regression, `inserted = 0` = SUCCESS, two failed nights then success, the run continues after a failed target, FAILED/DEGRADED → the job result is not healthy; `_run_targets(mode=…)` still means the report scope. Then the code: dispatch on `write_mode` BEFORE any append-specific step; the `upsert` branch is today's lines untouched; `shadow.py` writes `data/archiver-shadow/<date>.jsonl` (`data/` is gitignored — never `git add` it) with retention; the run report gains the append columns ONLY in `append` mode (the file's existing SCHEMA_BREAK device, `report.py`, for the column change). DevDocs `archiver/runner.md`, `shadow.md`, `report.md`. Commit `feat(archiver): write_mode dispatch — upsert untouched, pre-write shadow compare, the append night`. Last line `CONTINUE: step 6`.

## 6. Chunk Q — the quiet window and the commands (spec §8, §6, §3 V3-7)
Tests FIRST, offline, fake clock + a constructed pool row through an injected reader: Q1–Q3 each refuse alone; `MARKET_RESET` is not quiet; a missing pool row / NULL `last_scan_at` REFUSES ("cannot prove the radar is quiet"); the refusal text carries session, last scan start + age + need, next scanning open + distance + need, and the earliest allowed start; exit code 2, nothing written; the PRE-COMMIT re-check rolls back; NO `--force` exists (a test asserts the parser rejects it); the two NAMED tests exactly as the spec titles them — `test_gemini_close_boundary_poller_lag` (normal 20:00, early-close 17:00, and the literal 16:00) and `test_astra_open_boundary_repair_crosses_open` (03:59:50 refused at start; 03:49:59 start rolled back at the 03:50:05 commit) — both assert NO bar row changed; `restate --apply` AND `backfill-missing` refused in every scanned session; previews always run; `backfill-missing` can only DO NOTHING (the fake store proves `upsert_bars` is never called by it); `restate --apply` needs `--reason` and leaves its audit on an incident row (spec O-4); every read-only command (`audit`, `incidents`, `progress`, `shadow-report`, both previews) calls no write method; `incidents resolve` is explicit and audited; the one test that DEMONSTRATES Known limit 1 (an open `restated` incident, the unchanged poller still writes) — it imports the poller, it does not edit it. Then the code: `quiet.py` (pure verdict + the pool-row reader), `cobalt.archiver.cli` and ONE new `archiver` subparser block in `src/cobalt/cli.py`. `git diff main -- src/cobalt/radar/` must be EMPTY — quote it. DevDocs `archiver/quiet.md`, `archiver/cli.md`. Commit `feat(archiver): repairs only in a quiet window (R8 option A) — restate, backfill-missing, audit, incidents, progress, shadow-report`. Last line `CONTINUE: step 7`.

## 7. Chunk H — heartbeat and docs (spec §11, O-7)
Tests FIRST, offline with the probe's existing fake store pattern: the archiver probe is not green while an unresolved incident exists, its detail names the count and the oldest kind; zero incidents → exactly today's result; a missing `archive_incidents` table → a LOUD probe failure naming the migration, never green, never a crash of the heartbeat run. Then the probe change (`heartbeat/probes.py`, `archiver_freshness` only), DevDoc `heartbeat/probes.md`, and `docs/40 - DevDocs/cobalt/archiver/__init__.md` gains the operator's page: the modes, what a shadow night writes, how to read `shadow-report`, the quiet window in plain words, the five known limits of spec §12 verbatim. Commit `feat(heartbeat): the archiver probe sees unresolved archive incidents; archiver operator docs`. Last line `CONTINUE: step 8`.

## 8. Close (offline)
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the line; against BASELINE: failed 0, passed = BASELINE + your tests (list them by file with counts per chunk), skipped = BASELINE + your `requires_db` tests (list them by name) · the DevDocs symbol check and the leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures` are in that suite — name their lines · `uv run cobalt jobs restarts main..HEAD` → the table VERBATIM; UNCLASSIFIED → ESCALATE; the `RESTARTS:` line is what the command derives (L42) · `git log --oneline main..HEAD` (one commit per chunk + the report; list them) · `git diff --stat main HEAD` → every path is under `src/cobalt/archiver/`, `src/cobalt/db_migrations/`, `src/cobalt/heartbeat/probes.py`, `src/cobalt/cli.py`, `configs/cobalt/taxonomy/tunables.yaml`, `tests/cobalt/`, `docs/40 - DevDocs/` — any other path, and ANY path under `src/cobalt/radar/` → ESCALATE with why · `git diff main -- src/cobalt/radar/` EMPTY (quote) · `git status --porcelain` empty · report committed by explicit path (`docs(report): archiver append build — <one line>`). `## ESCALATE` carries, besides what you found, ALWAYS and untouched: (i) the spec's §14 OPEN items O-1 … O-7, one line each, with what you built as the safe default; (ii) the `requires_db` tests have NEVER run — their first run on `cobalt_dev` is owed before this branch may deploy; (iii) cross-branch: the files shared with `sprint-2/p4` and `ops/2026-09-19` (the list above), migration numbers 0010/0011 beside P4's 0008/0009, the second lander rebases and the integrated suite on the combined tree is the gate; (iv) for the deploy prompt: TWO migrations ship (`--allow-prod` inside the pause, the proof budget applies), `archiver.write_mode` ships `upsert`, the shadow's first night and its artifact path, `data/archiver-shadow/` must exist or be created by the run, the residents that read `tunables.yaml` per the RESTARTS table; (v) the five known limits of spec §12 go to the owner WITH the switch ruling. `MEMORY:` / `RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, exactly this shape:
`ARCHIVER APPEND BUILT <tip> | on main <sha> | offline <passed>/<failed> (<skipped> skipped; baseline <p>/<s>) | settings <built|ESCALATED> · migrations 0010/0011 <built|ESCALATED> · reconcile <built|ESCALATED> · store <built|ESCALATED> · runner+shadow <built|ESCALATED> · quiet window+commands <built|ESCALATED> · heartbeat+docs <built|ESCALATED> | write_mode ships: upsert | radar diff: empty | RESTARTS: <line> | db: OWED — <n> requires_db tests written, never run | OWED: three-house check, dev-DB run after p4-verify's stop line, deploy prompt (upsert), his approval, shadow nights, his switch ruling | ESCALATE: <n>`
Then stop. The desk verifies the artifact (L35), stops this session, and launches the three-house check of the build.
