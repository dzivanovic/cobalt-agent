MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch ONE reviewer (Grok) across FOUR bucketed whole-build calls, verify its findings; no write path · SEAT: review hub `review-archiver-grokwhole-0919`, launched by the CTO desk in the background, SAME worktree as rounds 1–2 (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `29-review-archiver-r2.md`'s launch line, with exactly THREE strings changed — the prompt filename (`29-review-archiver-r2.md` → `33-review-archiver-grok-whole.md`), the `--remote-control` name (`review-archiver-r2-0919` → `review-archiver-grokwhole-0919`), and the `mkdir` path inside `--allowedTools` (`scratch/review-archiver-r2-0919` → `scratch/review-archiver-grokwhole-0919`) — because this run needs no rule `29` did not already carry (same repository, same branch, same `git -C /Users/cobalt/cobalt show*`/`log*` reach; the now-unused `Bash(agy *)`, `Bash(codex exec …)`, and both `git -C .../s2-p2-cards *` rules stay, unused, for the same reason unused rules stayed in `29` and `20` — nothing is removed, only the three named strings change). Proof: sorted `comm -3` between `29`'s `--allowedTools` rule-string list and this prompt's rule-string list yields exactly one line on each side, both the `mkdir` string (the bare `--bg "Read '<file>' …"` string also differs, as it always does between rounds — that is the filename change already named above, not a second rule):
```
"Bash(mkdir -p scratch/review-archiver-r2-0919)"
	"Bash(mkdir -p scratch/review-archiver-grokwhole-0919)"
```
If this round turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/33-review-archiver-grok-whole.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-archiver-grokwhole-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-archiver-grokwhole-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; ONE headless review per bucket, one attempt each, never retried (L47 — probe before counting on a house; `grok --version` is the probe here).

# Grok WHOLE-BUILD read of `archiver/append-0919` (code tip `9056e73`, report tip `fb825df`), OFF-LADDER (LAWS L67; ruled `cto-2026-09-18.md` R13–R17 and `cto-2026-09-19.md` R8) — the third checker's third read. Round 2 (`scratch/review-archiver-r2-0919/REVIEW.md`) closed with the build read WHOLE by only ONE house (Gemini, 4 of 4 buckets, clean) and Astra METER before it could read at all; Grok read only the round-2 FOLD (`fold.diff`, `VERDICT: FOLD CLEAN`) — it has never read this build whole. L67 wants three checkers; Grok has meter (Astra does not), so this run gives Grok the WHOLE build, bucketed exactly as Gemini's round-2 read was bucketed (`29-packet/build-{A,B,C,D}-*.diff` + matching `spec-excerpt-*`), so each call stays small enough for a headless Grok session to answer without reaching for a tool. The code has NOT moved since round 2 (`git log 9056e73..archiver/append-0919` = only the report commit `fb825df`) — this is the SAME code Gemini already cleared, read by a different house. DO NOT STOP until the report ends `ARCHIVER GROK WHOLE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Standing law: LAWS.md L67, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, R19–R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. THIS run's specific authorization: `cto-2026-09-18.md` R13–R17 (17:05–17:26 ET — the archiver becomes watermark-append, and "every development gets checked by at least three members of the tribunal … at any time the code has to be checked at least by one more house … more than one additional house if possible, if the meter allows") and `cto-2026-09-19.md` R8 (09:30 ET, "A on archiver" — quiet-window repairs, build, three-house check, deploy in `upsert`, shadow nights) — prove both with the same `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` / `"…/cto-2026-09-19.md"` (both files carry many rows; R13–R17 and R8 are rows within the commits these prove exist, not separate commits). The branch's own debt: round 2's stop line and `REVIEW.md` ESCALATE both name "Astra whole-build read is still OWED" and leave Grok's whole-build read implicit in the same L67 gap — prove the branch state with `git -C /Users/cobalt/cobalt log --oneline -4 archiver/append-0919 -- "docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md"` (**name the branch ref** — this report lives ONLY on `archiver/append-0919`, same lesson every prior round of this check recorded). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the report's own claims (`facts.md` and `build-report.md` in the packet are claims — verify them against the diffs) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets a complete, sufficient packet for its assigned read (Grok's four bucket packets are the SAME six-file shape Gemini's round-2 buckets were — not a narrower read than the house that already cleared this code) · L47 a meter is checked BEFORE a house is counted on · L52 nothing in this build may touch scoring, ranking or a card value · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; Grok gets its assigned bucket packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close), `2026-09-18/11-review-deploy-stack.md` §3 (verify), and `27-review-p4-gemini-astra.md` §3.1 (the split-call, "you have NO tools" pattern) BIND you here UNCHANGED, with `scratch/review-archiver-grokwhole-0919/` in every path. LESSONS carried into this run: (a) Read→Write strips trailing whitespace — state it once in `## Packet` if observed, never chase it; a `cp` byte-copy avoids it entirely and is preferred, as round 2 recorded; (b) `git log -- <path>` for a branch-only file needs the branch ref named explicitly; (c) **headless Grok auto-denies shell** — a `grok` session with only a scoped `Write` permission has no `Bash`/`Read-elsewhere`/network tool, so ANY question it cannot answer purely from the files named in its own call must be marked `UNVERIFIABLE FROM PACKET`, never attempted as a command — the same failure shape that HARNESSed Gemini on over-scoped or cross-referential packets (`27-review-p4-gemini-astra.md` §3). This round's whole-build packet is the SAME four ~76–112 KB area buckets (A/B/C/D) round 2 built for Gemini, reused here for exactly this reason — give Grok ONE bucket per call, one attempt per call, NEVER a retry on the same bucket (if a bucket HARNESSes, record it and move to the next bucket; do not re-attempt). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-grokwhole-0919/REVIEW.md` — same gitignored-scratch pattern, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok (whole build, 4 calls)` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/33-packet/`; copy each into `scratch/review-archiver-grokwhole-0919/` (`mkdir -p` first; `cp`, byte-identical — round 2's preferred method, not Read→Write). The desk's SIZE TABLE (verify with `ls`/`wc -c`; re-stage ONLY a file that is missing or whose byte count differs — these are the SAME 12 files round 2 staged for Gemini's whole-build read, verified byte-identical against `29-packet/` because the branch has not moved since (only the report-only commit `fb825df` sits above round 2's code tip `9056e73`)):

| file | bytes | for |
|---|---|---|
| `verdicts-r1.md` | 7,212 | Grok, every call (context) |
| `facts.md` | 14,992 | Grok, every call |
| `build-report.md` | 81,852 | Grok, every call (the branch's current report, round 1 + round 2) |
| `QUESTIONS-whole.md` | 6,760 | Grok, every call |
| `spec-excerpt-A-settings-heartbeat-store.md` | 9,970 | Grok call A |
| `spec-excerpt-B-migrations-reconcile.md` | 15,453 | Grok call B |
| `spec-excerpt-C-runner-shadow-report.md` | 3,666 | Grok call C |
| `spec-excerpt-D-quiet-cli.md` | 9,881 | Grok call D |
| `build-A-settings-heartbeat-store.diff` | 97,907 | Grok call A |
| `build-B-migrations-reconcile.diff` | 111,567 | Grok call B |
| `build-C-runner-shadow-report.diff` | 76,100 | Grok call C |
| `build-D-quiet-cli.diff` | 88,636 | Grok call D |

The four `build-*.diff` files ARE, together, the WHOLE non-docs three-dot diff `main...archiver/append-0919 -- src tests configs` (374,210 bytes total) — re-confirm with `wc -c` across the four rather than trusting this table blind. `ops` and the rest of `configs` are unchanged beyond the one `tunables.yaml` block already in bucket A (round 2 re-confirmed this with `git -C /Users/cobalt/cobalt log --oneline --stat main..archiver/append-0919 -- ops configs src/cobalt/radar` → exactly one commit, `54e98f7`, touching only `configs/cobalt/taxonomy/tunables.yaml`) — worth re-running, not assuming, since it is one bare `git log*` command already covered by this launch line's rules.

`QUESTIONS-whole.md` is written for "an independent reviewer from another house" reading a WHOLE-build bucket — it is the same file round 2 gave Gemini, unedited, and needs no Grok-specific rewrite.

## 2. Grok — the WHOLE build, as FOUR SEPARATE calls, one attempt each, never a retry
For each bucket (A, B, C, D), ONE `grok` call, `run_in_background`, no `--always-approve`, sandboxed with write access scoped to ONLY this run's scratch directory (the `Write(<dir>/**)` spelling is `01-review-harness.md` §2's, the one proven on this host — desk fold 12:2x; a single-file `Write(...)` pattern has never been run here):

`grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-grokwhole-0919/**)" -p "You have NO tools for this task except reading the local files named below and writing ONE output file. Never run a command, never list a directory, never fetch a URL, never search the web — if answering a question would require any of those, write UNVERIFIABLE FROM PACKET for that item and continue. Read ONLY these six files, all in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-grokwhole-0919/: QUESTIONS-whole.md, verdicts-r1.md, facts.md, build-report.md, spec-excerpt-<X-full-name>.md, build-<X-full-name>.diff. Follow QUESTIONS-whole.md exactly, answering only the questions your files can address (mark the rest N/A THIS CALL). Write your complete findings to grok-findings-<X>.md and reply with only that path."`

(`<X>` = A/B/C/D; `<X-full-name>` = the matching packet filename's own suffix, e.g. `A-settings-heartbeat-store`.) A call that HARNESSes (any tool-permission denial, any empty/no output) is recorded as `grok bucket <X>: HARNESS` and you move to the next bucket — NEVER retried, NEVER split further (the bucket is already sized for this, matching round 2's Gemini rule). Combine all four calls' answers into one `## Grok (whole build, 4 calls)` section, one row per bucket (file, bytes, F-tags verbatim, verdict-this-call verbatim), same table shape round 2 used for Gemini. Your close-line `grok:` field aggregates across whichever buckets answered.

## 3. Verify — using ONLY this launch line's rules, nothing new
Every BLOCKER and MAJOR from Grok against the REAL files: a diff claim against `git -C /Users/cobalt/cobalt show archiver/append-0919:<path>` (reachable under the SAME `show*` rule, no worktree `cd` needed) and against `/Users/cobalt/cobalt-wt/archiver-append/` with Read/`grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (an independent re-derivation of a diff with a fresh `git diff`, or `uv run pytest`/`uv run cobalt jobs restarts` to re-check a suite or RESTARTS claim) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. If a Grok finding duplicates one round 1's Grok, round 2's Grok (fold), or round 2's Gemini already raised, say so and cite it rather than re-litigating — carry it forward by reference. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-archiver-grokwhole-0919/`.

## 4. Close
`## Verdict table` shows a row per round-1 finding (F1–F7) with Grok's this-round tag (FIXED / NOT FIXED / REGRESSED / N/A THIS CALL, per bucket) plus your own hub-verified read of the real files (reuse round 2's own file:line citations where the code has not moved — say so rather than re-deriving them from scratch), plus any NEW finding this run surfaces. Last line, beginning EXACTLY `ARCHIVER GROK WHOLE`:
`ARCHIVER GROK WHOLE · grok: <b>/<m>/<n> <BUILD CLEAN|FIX FIRST n|HARNESS|METER|TIMEOUT> (buckets answered k of 4) · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
