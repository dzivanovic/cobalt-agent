MODEL: Opus 5 (`claude-opus-5`) — same tier as rounds 1/2 and the DB run (this chunk edits the migrations that carve `cobalt_user`'s read boundary on two production system tables, L29 floor) · SEAT: archiver build hub `archiver-round3-0919`, launched by the CTO desk in the background, SAME worktree as rounds 1/2/the DB run (it already exists — do NOT create it, do NOT `git worktree add`, do NOT rebase). NO NEW RULE: every rule below is copied byte for byte from `docs/40 - DevDocs/prompts/2026-09-19/26-archiver-round2.md`'s launch line — zero added, zero altered; dropping was considered and none were dropped, because this chunk's steps need exactly the same `Bash(uv run pytest *)`, `Bash(git add *)`/`commit`, `Bash(uv run cobalt jobs restarts *)` and read-only shell rules 26 already had, and nothing this chunk does needs a tool outside that set. THIS RUN IS OFFLINE BY DESIGN, same as rounds 1/2: `cobalt_dev` is not this session's to touch. The with-DB re-run that actually flips the two red tests green is a SEPARATE later step — the desk re-launches `35-archiver-db-run.md`'s shape, with the corrected apply → suite → roll back order the DB run's own report named (DB-3). Two bare commands, run by the desk:

```
cd /Users/cobalt/cobalt-wt/archiver-append
claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/40-archiver-round3.md' and follow it exactly." --model claude-opus-5 --remote-control archiver-round3-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt
```

SESSION / REMOTE-CONTROL: `archiver-round3-0919` — fresh session, every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `cobalt_dev`, no `.env`, no vault write, no other seat's config · METER: Anthropic LARGE, shared with any other Opus seat running today; a usage-limit message is not a failure — record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

LAUNCH-LINE RULE PROOF (this session's rule strings vs 26's, sorted, `comm -3`): both sides are the 16 `Bash(...)` allow strings + `AskUserQuestion` + `EnterWorktree` + the three `--add-dir` values (21 strings each). `comm -3 <(sort 26-rules.txt) <(sort 40-rules.txt)` → **empty output** — the sets are identical, nothing added, nothing altered, nothing dropped. (Every rule this chunk's steps need — `git add` by explicit path, `git commit`, `uv run pytest`, `uv run cobalt jobs restarts`, `grep`/`Read` for the tests-first RED quotes — is already in 26's set; no command this prompt asks for falls outside it. If a step below needs a tool not in this list, name it in the report as an `## ESCALATE` line — do not invent the rule and do not run the command anyway.)

# Archiver append-only — ROUND 3, REVOKE `cobalt_user` off `system.archive_progress` and `system.archive_incidents` (Dejan's ruling, `cto-2026-09-19.md` §4 R25, "B"). Tests FIRST for every SQL change (each new offline test RED on the current tip, then GREEN), OFFLINE, one commit per item. DO NOT STOP until the report ends `ARCHIVER R3 …` or `FAILED …`.

CONTEXT. The DB run (`docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`, section `# DB RUN`, finding **DB-1**) ran all 23 owed `requires_db` tests for the first time on real `cobalt_dev` and found two RED: `tests/cobalt/test_archiver_migrations.py::test_the_user_role_has_no_grant_on_either_table[archive_progress]` and `[archive_incidents]`. Cause, quoted from DB-1: `cobalt_user` CAN read both new tables because `0001_schemas.sql:124`'s blanket `GRANT SELECT ON ALL TABLES IN SCHEMA system TO cobalt_user;` and its default-privilege twin at `:149-151` (`ALTER DEFAULT PRIVILEGES FOR ROLE cobalt_system, %I IN SCHEMA system GRANT SELECT ON TABLES TO cobalt_user`) apply to every table created later in schema `system`, including 0010's and 0011's — while the FINAL design's §11 says outright "`cobalt_user` is granted nothing on them". DB-1 gave the desk two options: (a) add an explicit `REVOKE` to 0010/0011 so §11's sentence becomes true; (b) amend §11 to match `0006_radar_score.sql`'s pattern (which grants `cobalt_user` explicit read on the radar tables it creates, `:198-200`) and relax the two tests. **Dejan ruled "B" at 13:10 ET (R25): take (a).** §11 stands as written — do not edit the design doc unless your own reading finds it now reads wrong (see step 3). The two tests stay exactly as written; this chunk's job is to make the database match them, and to prove the SQL says so before the next DB run gets to prove the database does.

CARRIED, NOT YOURS THIS CHUNK: **DB-2** (`test_the_own_connection_upsert_survives_another_transactions_rollback`, unobservable under the suite's autouse single-transaction fixture) is NOT ruled and NOT touched here — carry it forward in `## ESCALATE` untouched, by name, with its status unchanged ("ruling owed"). **R2-1** (the shadow artifact's timezone ruling) and **DB-4** (the two index-card drifts, already settled) are also untouched — carry forward, do not re-litigate. You do not touch `src/cobalt/archiver/quiet.py`, `runner.py`, `cli.py`, or any test file outside `tests/cobalt/test_archiver_migrations.py`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval. `grep -c` each of the 16 `Bash(...)` rule strings above, plus `--disallowedTools "AskUserQuestion" "EnterWorktree"` and the three `--add-dir` values, against `docs/40 - DevDocs/prompts/2026-09-19/26-archiver-round2.md` — every one must count 1 (26's own launch-line-rule-proof did the identical check against `17-archiver-append-build.md`; same shape, one file back). The WORK ITEM is authorized by: `cto-2026-09-19.md` §4 row R25 (quoted above, "B" approved 13:10 ET — the ONLY ruling this chunk executes; it does not extend to DB-2 or to anything else in the branch's ESCALATE list); the DB run's own report, section `# DB RUN`, finding DB-1 (the desk's two options, his choice between them); the FINAL design `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` §11 (the sentence this chunk makes true) and §15's "Migrations" bullet ("`cobalt_user` has no grant" — a test that must exist, already written, currently red). A rule here not in 26's committed line, or a citation that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.

YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded scope: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Anything RED that this prompt expected GREEN, or any number that does not match what a cited row or file says → `ESCALATE`, never silently reconciled, never asked about mid-run.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(archiver-r3): <item> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit file paths under `src/cobalt/db_migrations/`, `tests/cobalt/test_archiver_migrations.py`, `docs/30 - Design/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `data/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>`.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL → `cto-2026-09-19.md` §4 row R25 (THE ruling this chunk executes) → the DB run's report, section `# DB RUN`, finding DB-1 in full (the cause, both options, his choice) → the FINAL design §11 (the sentence made true) and §15's "Migrations" bullet → `src/cobalt/db_migrations/0001_schemas.sql:99-162` IN FULL (section 3 ownership, section 4 grants — read `:115-132` for the blanket grants and `:141-162` for the `ALTER DEFAULT PRIVILEGES` block; do not requote from memory, quote the file) → `src/cobalt/db_migrations/0006_radar_score.sql:191-200` (the pattern §11 also names — contrast only, you do not copy it) → `src/cobalt/db_migrations/0010_archive_progress.sql` IN FULL and its `.rollback.sql` → `src/cobalt/db_migrations/0011_archive_incidents.sql` IN FULL (note `:99-101`, the sequence grant already made explicit there, "NOTHING is granted to `cobalt_user`" written but not yet enforced by a REVOKE — see step 2) and its `.rollback.sql` → `tests/cobalt/test_archiver_migrations.py:1-46` (imports, `_code()` helper, `PROGRESS_SQL`/`INCIDENTS_SQL`/`NEW_TABLES`) and `:375-398` (`test_the_correct_table_is_owned_by_cobalt_system`, `test_the_user_role_has_no_grant_on_either_table` — READ, do not rewrite these two `requires_db` tests, they stay as written). Laws, one line each: L1 fail loud — a REVOKE that silently no-ops on the wrong object name is not a fix, prove the object name against the file · L3 one path — do not invent a second grants-migration pattern; 0010/0011 diverge from 0006 on purpose, per R25, and you say so once, not by accident elsewhere · L35 your claims are checked against your artifacts (RED-then-GREEN quoted, `grep`/file-read quotes for every SQL line you cite) · L45 tests against real file text and the suite's own helpers, no invented Postgres behaviour where `requires_db` is the honest answer for what only a live grant check can prove · L46 one commit per item, tree clean before you stop · L48 evidence in the report file in the same turn · L54 you work in this worktree only · L57 the counting identities are untouched by this chunk — you are not near them; if anything you touch looks like it would change one, STOP and ESCALATE · L60 recovery: partial work is wip-committed, never discarded · L62/L63 no questions mid-run, no dialogs; the report is your stop channel.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `psql`, any `uv run cobalt` other than `jobs restarts`, any `COBALT_ENV=…` command, `launchctl`, `claude`.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim): `date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form, quote it in full — branch must be `archiver/append-0919`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: `c14e6f9`, else the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file"; if it exists → `FAILED PREFLIGHT: .env present — this run must not see the dev database` · `uv run pytest --co -q tests/cobalt/test_archiver_migrations.py` (collects cleanly — confirms the two named `requires_db` tests are present and unchanged before you touch anything; they will SKIP, not run, since `.env` is absent — that is expected, they are OWED to the DB re-run, not to this chunk). Any denial → `FAILED PREFLIGHT: <rules>`.

DESIGN CONSTRAINT THAT BINDS EVERY STEP BELOW: this chunk changes ONLY grant/revoke statements in 0010/0011 and adds offline text-pinning tests for them in `tests/cobalt/test_archiver_migrations.py`. It does not touch `CREATE TABLE`, any column, any constraint, any index, either `.rollback.sql`'s `DROP TABLE`, or any file outside `src/cobalt/db_migrations/00{10,11}*.sql` and the one test file. `git diff main -- src/cobalt/archiver/ src/cobalt/radar/` must stay EMPTY — quote it at close.

## 1. Read and confirm the cause, quote it
Read `0001_schemas.sql:115-132` and `:141-162` in full and quote, verbatim, the two lines DB-1 named: the blanket grant (`:124`, `GRANT SELECT ON ALL TABLES IN SCHEMA system TO cobalt_user;`) and the default-privilege grant that fires on every table `system` gets AFTER 0001 runs (`:149-151` inside the `DO $$ ... $$` block, `ALTER DEFAULT PRIVILEGES FOR ROLE cobalt_system, %I IN SCHEMA system GRANT SELECT ON TABLES TO cobalt_user`). Confirm from the file, not from this prompt, that these are the ONLY two routes by which `cobalt_user` reaches SELECT on a table it never got an explicit grant for — name any third route if you find one (there should not be one; if there is, ESCALATE, do not silently fold it into the fix). Also read `0001_schemas.sql:152-154` (the matching default-privilege grant for SEQUENCES) and check it against `0011_archive_incidents.sql:99-101`: `archive_incidents_id_seq` is created inside 0011 and, by the same default-privilege mechanism, `cobalt_user` gets SELECT on it too UNLESS the sequence is also revoked — 0011's own comment ("NOTHING is granted to `cobalt_user`") is currently aspirational for the sequence exactly as it was for the table. Decide and state in the report whether the sequence needs the same treatment: R25's ruling and the two named tests are about the TABLES only, but leaving the sequence's default SELECT unrevoked while the comment says "NOTHING is granted" is the same class of gap DB-1 found — the honest options are (i) revoke it too, in the same commit as `archive_incidents`'s table REVOKE, matching the comment's own claim, or (ii) leave it and correct the comment to say what is actually true. Pick one, say why, and do it — do not leave the comment claiming something the SQL does not do. No commit yet; this step is reading and deciding, folded into step 3's commit.

## 2. Read 0006's contrasting pattern, confirm it is a contrast and not a template
Read `0006_radar_score.sql:191-200`. It explicitly `GRANT SELECT, REFERENCES ON system.radar_score TO cobalt_user;` (and similarly for `radar_score_run`, `radar_board_v`) — those tables are MEANT to be read by the user side (radar scores feed ASET sizing). §11 says the opposite for `archive_progress`/`archive_incidents`: `cobalt_user` reads NOTHING. Confirm in the report, in one sentence, that 0010/0011 diverging from 0006's pattern is deliberate (R25) and not an inconsistency to "fix" toward 0006 — the two patterns coexist because the two sets of tables have opposite tenancy answers, not because one is wrong.

## 3. Write the offline pinning tests FIRST (RED), then the REVOKE statements (GREEN)
In `tests/cobalt/test_archiver_migrations.py`, near the existing `test_the_user_role_has_no_grant_on_either_table` (`:391-398`, do not modify it), ADD new OFFLINE (no `requires_db` marker) tests using the file's own `_code()` helper (`:47-52`) and `PROGRESS_SQL`/`INCIDENTS_SQL` constants (`:38-41`):
- One test per migration file (or parametrized over `NEW_TABLES` the way the existing test is, your call — say which) asserting the file's code (via `_code()`, so a REVOKE mentioned only in a `--` comment does not count) contains an explicit `REVOKE ... FROM cobalt_user` statement naming the correct fully-qualified table (`system.archive_progress` / `system.archive_incidents`), AND that the REVOKE's string position is AFTER the `CREATE TABLE IF NOT EXISTS system.<table>` statement's position (Postgres would error revoking privileges on a table that does not exist yet in the same script — pin the ORDER, not just presence, by comparing `.index()` results on the `_code()` string).
- Decide and pin the exact spelling: R25's cause quotes `cobalt_user`'s ONLY route to these tables as SELECT (table default-privilege) and, for `archive_incidents_id_seq`, SELECT (sequence default-privilege) — nothing else is default-granted to it in schema `system` (you confirmed this in step 1). Between `REVOKE SELECT` and `REVOKE ALL`, take **`REVOKE ALL ON <object> FROM cobalt_user`**: it is the spelling that actually matches §11's word — "nothing" — rather than "no SELECT specifically", and it costs nothing today (no other privilege is granted to revoke) while remaining correct if a later migration ever adds a different default grant to `cobalt_user` in schema `system` that this design never intended for these two tables. State this reasoning in the report; do not silently pick one.
- If step 1 decided the sequence also needs a REVOKE, its test and statement follow the identical shape (`REVOKE ALL ON SEQUENCE system.archive_incidents_id_seq FROM cobalt_user`, ordered after the sequence exists — a `BIGSERIAL`/`GENERATED` column creates its sequence as part of the `CREATE TABLE`, so ordering after `CREATE TABLE system.archive_incidents` suffices, confirm this is true of the actual column definition you read in step 1 rather than assuming it).
- RUN the new tests BEFORE adding the SQL — quote the RED failure (missing REVOKE). Add the REVOKE statement(s) to `0010_archive_progress.sql` (after its `ALTER TABLE ... OWNER TO cobalt_system;` at `:75`) and to `0011_archive_incidents.sql` (in its existing grants section, `:98-101`, beside the sequence grant already there). Re-run — quote GREEN.
- IDEMPOTENCY (apply twice): state explicitly, as a fact about Postgres semantics rather than something you can test offline — `REVOKE` on a privilege the grantee does not currently hold succeeds as a no-op (no error), so re-running 0010/0011 against a database where the REVOKE already applied is safe, exactly as idempotent as the file's existing `CREATE TABLE IF NOT EXISTS`. This is NOT proven by an offline test (only a live `cobalt db migrate` run twice proves it) — name it as OWED to the DB re-run alongside the two `requires_db` tests, do not claim it as tested here.
- Do NOT touch either `.rollback.sql` — dropping the table removes the REVOKE along with everything else on it; state this in the report as the reason no rollback change is needed.
Commit: `fix(db-migrate): REVOKE cobalt_user off system.archive_progress and system.archive_incidents (cto-2026-09-19 R25 "B", DB-1)`. Last line `CONTINUE: step 4`.

## 4. §11 — confirm, do not silently edit
Re-read FINAL design §11's first sentence: "`cobalt_user` is granted nothing on them." Quote it. This chunk makes that sentence TRUE in the database (pending the DB re-run); the sentence itself was already correct as written — R25 said so explicitly ("the FINAL design's §11 'nothing granted' stands as written"). Do NOT edit `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` unless your own reading in steps 1–3 found a sentence in §11 or §15 that now reads wrong (for example, if you decided in step 1 that the sequence needs its own explicit callout and §11's prose doesn't currently mention sequences at all — that is a possible textual gap, not a wrongness, and is NOT grounds to edit; only edit if something in §11/§15 as written is now FALSE). If you find nothing wrong, say so in the report in one line and do not touch the file. If you do find something wrong, quote the exact sentence before and after your edit, and make that its own commit: `docs(design): §11 <one line> (archiver round 3)`. Last line `CONTINUE: step 5`.

## 5. Close (offline) — re-run the suite, restarts, append the report, stop
`uv run pytest -q tests/cobalt/test_archiver_migrations.py` → quote the summary line, confirm the two `requires_db` tests still SKIP (not run, not xfail) and every offline test including your new ones is green · `uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against the DB run's own offline close (**1866 passed / 0 failed / 320 skipped**): failed stays 0, passed grows by exactly the new tests you added this chunk (name them), skipped stays 320 (you added no new `requires_db` test this chunk — the two that matter already existed) · `uv run cobalt jobs restarts c14e6f9..HEAD` → the table VERBATIM (branch point for this chunk is the DB run's own report-fill commit, `c14e6f9`) — expect only `src/cobalt/db_migrations/00{10,11}*.sql`, `tests/cobalt/test_archiver_migrations.py`, and `docs/40 - DevDocs/` (and, only if step 4 fired, one `docs/30 - Design/` path) among the changed paths; 0 UNCLASSIFIED · `git diff main -- src/cobalt/archiver/ src/cobalt/radar/` EMPTY (quote it) · `git status --porcelain` empty · `git diff --stat c14e6f9 HEAD -- src tests docs` listed, every path one this prompt named · append a new top-level section to `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` on THIS branch, AFTER the existing `## DB RUN ESCALATE` section's last line, headed `# ROUND 3 — REVOKE cobalt_user off the two archiver tables (cto-2026-09-19 R25 "B", DB-1)`, containing: a `## §0 Headline` (≤5 lines), one subsection per step above with its RED/GREEN quotes and commit, the CLOSE numbers above, and a `## ROUND 3 ESCALATE` carrying forward EVERYTHING the DB run's `## DB RUN ESCALATE` already had (untouched: round 1's 8, round 2's R2-1/R2-2/R2-4, DB-2, DB-4) PLUS: whether the sequence needed its own REVOKE (state the finding either way), the still-OWED two `requires_db` tests and the idempotency-on-real-Postgres fact, both owed to the next DB re-run, and anything else this chunk found. Commit the report by explicit path: `docs(report): archiver append round 3 — <one line>`.

LAST LINE, exactly this shape:
`ARCHIVER R3 <sha> | offline <p>/<f> | DB-1: REVOKE in 0010 + 0011 (offline pin green; 2 requires_db OWED to the DB re-run) | DB-2: not touched — ruling owed | RESTARTS: <line> | ESCALATE: <n> | cobalt_dev: untouched`
or `FAILED: <step> — <reason> | cobalt_dev: untouched`. Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or `cobalt_dev`.
