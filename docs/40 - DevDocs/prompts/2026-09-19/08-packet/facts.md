# facts.md — staged by the CTO desk 2026-09-19 from the code on main (read-only; the archiver is byte-identical on `main` and on the ship branch `sprint-2/stack`). Gathered by a read-only agent, the key items spot-checked by the desk against the files. The hub stages the real files beside this one; where this file and a real file differ, the real file wins — say so.

- WRITE PATH `src/cobalt/archiver/store.py`: `BarStore.upsert_bars(bars, *, before_commit=None)` lines 49-95; one `executemany` of `INSERT INTO bars (…) VALUES (…) ON CONFLICT (ticker, interval, ts) DO UPDATE SET open, high, low, close, volume = EXCLUDED.*`; no chunking; one connection + one transaction PER CALL; returns `len(rows)` = rows SENT. `watermark(ticker, interval="i1")` lines 97-104 = `SELECT max(ts) FROM bars WHERE ticker=%s AND interval=%s`, own connection per call; its only caller is the radar poller.
- RUN FLOW `src/cobalt/archiver/runner.py` `_run_targets` lines 30-55: sequential over sorted (ticker, interval) targets; `fetch_bars` then `store.upsert_bars(bars)` — the whole export, no filter; 1.2 s sleep between targets; a failing target is recorded and the run continues; the nightly wrapper raises after the run if any target failed.
- FETCH `src/cobalt/archiver/collector.py`: `GET https://elite.finviz.com/export/stock` with `t`, `p`, `auth` only; no date parameter has any effect (`docs/30 - Design/DATA-SOURCE-MEMO.md` §2a/2b, staged as `finviz-windows.md`). Finviz emits bare ET wall-clock; the collector localizes to America/New_York (DST-aware) and stores UTC. `ts` = bar OPEN (`aggregate.py` buckets by the floor of the ET minute).
- TABLE `src/cobalt/archiver/migrations/0001_bars.sql`: ticker TEXT, interval TEXT, ts TIMESTAMPTZ, open/high/low/close NUMERIC(14,4), volume BIGINT, all NOT NULL; PRIMARY KEY (ticker, interval, ts); no other index. Lives in schema `system`.
- SECOND WRITER `src/cobalt/radar/poller.py` lines 94-105: i1 only; keeps bars with `ts + 1 min <= now` AND `ts > watermark − overlap_bars minutes` (`radar.poll_overlap_bars = 5`), then the SAME `upsert_bars` (DO UPDATE).
- READERS of `system.bars` in the new core: `src/cobalt/radar/replay.py` lines 134-139 (i1 only, day−14d … day+1d) and the store's own `max(ts)` / `count(*)`. None reads i2/i5/i15/i30.
- TARGETS come from the trader's vault note `1 - Trading/Radar Lists.md`: tier_a ≈185 tickers `archive: [i1, i2, i5, i15, i30]`, tier_b ≈25 tickers `archive: [i1, i5, i30]`, tier_c none → ≈1,000 targets, 210 tickers.
- SCHEDULE `ops/com.cobalt.archiver.plist` + `configs/cobalt/jobs.yaml`: 20:30 ET, weekdays, one-shot, timeout 5400 s; recent runs ≈23-24 min.
- LAST FOUR RUNS (`docs/30 - Design/archiver-runs.md`): 975 / 975 / 1000 / 1000 requests; rows written 3,164,833 / 3,164,706 / 3,339,864 / 3,343,938; 0 failures. Production `system.bars` ≈8.6M rows; ≈390,000 net new rows per trading day (`cto-2026-09-18.md` §25).
- TESTS: `tests/cobalt/test_archiver_store.py` `test_upsert_is_idempotent_and_refreshes_on_conflict` pins the DO UPDATE refresh; no test file exists for the archiver RUNNER; `tests/cobalt/test_radar_poller.py` `test_overlap_reupserts_closed_bars_and_excludes_open_bar` pins the poller's filter.
- The archiver has NO bar-is-complete rule today.
