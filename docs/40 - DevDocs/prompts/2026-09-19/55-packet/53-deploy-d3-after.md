STATUS: DEPLOY 3 of the weekend, and the SECOND production deploy of tonight — `cto-2026-09-19.md` §4 **R32**, in which Dejan OVERRULED **L43**'s one-production-deploy-per-evening for tonight only, on his own question ("why are we not redeploying Archiver right after merge and right after deploy and continuing with build and do another deploy tonight?"), with one condition: **the lane is green before 21:00 ET**, else it goes to Sunday. Deploy 1 (`02-deploy-stack-3.md`, 08:25 ET, `STACK DEPLOY DONE 2893a7f`) and deploy 2 (`38-deploy-p4.md`, 15:17 ET, `P4 DEPLOY DONE 5b58b7b`, outage 02:33) are both on record and LIVE. THIS file lands `ops/2026-09-19` AND `archiver/append-0919` together. LAUNCHABLE ONLY with Dejan's approval row in `docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4` naming THIS file, committed on main.

MODEL: Opus 5 (`claude-opus-5`) — Opus floor: production migrations 0010/0011 ship and this session runs a production write path (L29; deploys 1 and 2 ran the same seat on the same floor) · SEAT: deploy hub `deploy-d3-0919`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/53-deploy-d3.md' and follow it exactly." --model claude-opus-5 --remote-control deploy-d3-0919 --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C /Users/cobalt/cobalt reset --soft HEAD~1)" "Bash(git -C /Users/cobalt/cobalt tag scratch-allow-probe-d3-0919)" "Bash(git -C /Users/cobalt/cobalt tag -d scratch-allow-probe-d3-0919)" "Bash(git -C /Users/cobalt/cobalt tag pre-d3-0919)" "Bash(git -C /Users/cobalt/cobalt tag deploy-2026-09-19c)" "Bash(git -C /Users/cobalt/cobalt merge --ff-only stack/deploy3-0919)" "Bash(git -C /Users/cobalt/cobalt revert --no-edit *)" "Bash(git -C /Users/cobalt/cobalt revert --abort)" "Bash(git -C /Users/cobalt/cobalt worktree add -b stack/deploy3-0919 /Users/cobalt/cobalt-wt/stack-deploy3-0919 main)" "Bash(git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --no-edit *)" "Bash(git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --abort)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * rev-parse*)" "Bash(git -C * rev-list*)" "Bash(git -C * merge-base*)" "Bash(git -C * show*)" "Bash(cd *)" "Bash(uv run pytest *)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env)" "Bash(rm /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt jobs register)" "Bash(COBALT_ENV=production uv run cobalt jobs restarts *)" "Bash(COBALT_ENV=production uv run cobalt backup run*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(launchctl bootout gui/501/com.cobalt.aset)" "Bash(launchctl bootout gui/501/com.cobalt.radar)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist)" "Bash(launchctl kickstart -k gui/501/com.cobalt.aset)" "Bash(launchctl kickstart -k gui/501/com.cobalt.radar)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode NOT set (no `--permission-mode` flag, exactly as `02` and `38` ran); allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it, NO production `pytest`, **NO `settings load` in any spelling**, **NO PRODUCTION `--rollback`/`--down-to`** — never instructed here and never typed, in any branch (the desk's 2026-09-19 probe, `cto-2026-09-19.md` §61: under auto mode an allowlist is a pre-approval list rather than a whitelist, so what keeps a production rollback out of a run is the instruction, not the rule list; this session carries no auto mode and no such instruction) · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely and gets an answer; a dialog gets none).

# DEPLOY 2026-09-19 (3) — `ops/2026-09-19` + `archiver/append-0919`, ONE ff-merge of the gated stack
WHAT SHIPS: (1) **ops 0919** — the `cobalt day-open` C2 trading-day guard, `_band()` min ≤ max validation, a 30 s `lock_timeout` on the read-write migrate transaction, test hygiene A1/A2, and the migrate output's `code: <sha> (clean|DIRTY)` line. (2) **the append-only archiver** — production migrations **0010** (`system.archive_progress`) and **0011** (`system.archive_incidents`), both carrying an EXPLICIT `REVOKE` so `cobalt_user` cannot read them (Dejan's R25 "B"); the archiver's own per-(ticker, interval) progress, a persistent incidents table, a read-only SHADOW COMPARE, and **`upsert` mode — today's behaviour byte for byte** (the switch to `append` is HIS later ruling on the shadow numbers, R35: **ONE shadow night**, never automatic). Plus R34 "B": the archiver's backfill is bounded out of **04:00–09:30 ET** and `radar.finviz_max_rpm` **stays 50**.
Shape A (L66): both residents DOWN before the merge, UP after. NO new scheduled job is installed — `com.cobalt.archiver` already exists (Mon–Fri 20:30) and `com.cobalt.replay` was armed by deploy 2. NO settings write of any kind; the cards stay LIVE and are only READ. Expected outage ≈3–4 minutes (≈95 s of it is the migration's BEFORE+AFTER read of `system.bars`, measured twice today). **DO NOT STOP this session until its report ends `D3 DEPLOY DONE …` or `FAILED …`.**

WHY THIS SHAPE, each line a failure already paid for: (1) **ONE ff-merge of the GATE BRANCH, not two sequential merges.** Both branches are rebased onto the same `main`, so they are siblings: ff-merging one makes the other non-ff. §1 builds `stack/deploy3-0919` from `main`, merges both into it, and proves the SUITES ON THAT EXACT TREE; §4.3 then fast-forwards `main` to it. The tree that lands is byte-identical to the tree the gate proved, which is stronger than merging two branches in sequence where only their combination was ever tested (L68). Deploy 2's own gate branch was a throwaway because only one branch was landing; here it is the deliverable, and that difference is deliberate. (2) 09-18 11:34: `uv run pytest` in `~/cobalt` is NOT offline (`~/cobalt/.env` points it at `cobalt_dev`), so pytest runs ONLY inside a worktree. (3) 09-17 19:18: launchd respawned the radar into merged code → residents are `bootout`ed BEFORE the merge (L66). (4) 09-18: a commit on main between a rebase and an `--ff-only` broke the merge. Here the run's OWN commits move main (§1.6, §3.1, §3.3), which is lawful and expected — 4.1 absorbs them by merging `main` into the stack and proving the result docs-only, and **the desk still holds ITS commits from the moment this session launches until its stop line**. What must never happen is a THIRD party committing between 4.1 and 4.3; 4.3 re-checks `<pre-merge>` for exactly that. (5) Migrations 0010/0011 are ADDITIVE (two new tables) and nothing in the pre-archiver code reads them, so a code revert leaves the schema in place; **the schema is NEVER rolled back unattended** (§8 (3)). (6) **CARDS ARE LIVE** since 12:30:01 ET (`reports/cards-golive-2026-09-19.md`, R20): this deploy loads no settings file, writes no `trader_settings` row, and its rollback never turns the cards dark — proven STILL ENABLED at 6.4 as a smoke check, never touched as an action.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval.
- THE ROW: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4 Rulings`, the row that names THIS file (`53-deploy-d3.md`), approves THIS launch line, and carries this file's sha256. Verify: `shasum -a 256 "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/53-deploy-d3.md"` equals the hash in that row, and `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` shows the commit that carries it. **No committed row naming THIS file, or a different hash → `FAILED: authorization mismatch — <what>`, commit the report, stop before PREFLIGHT.** R7 and R30 approved DIFFERENT files and are not approvals of this one.
- STANDING ROWS, each to be found committed: `cto-2026-09-19.md` §4 **R32** (L43 overruled tonight, green-before-21:00), **R25** (the REVOKE), **R26** (the dropped twin), **R34** (ceiling 50, backfill bounded), **R35** (ONE shadow night), **R19** (push is the desk's on his word only — push is not in your list at all); `cto-2026-09-18.md` §4 **R18–R21** (L67).
- Laws that bind you: L1, L3, L4, L9, L28, L29, L32, L35, L42, L43 **as overruled for tonight by R32, and only that clause** — its restart-window clause still holds and is met because Saturday is not a trading day and the radar reads `idle:overnight` at every hour — L46, L48, L51, L53 (**you change no ceiling and no cadence**), L54, L55, L57, L61, L62, L63, L66, L67, L68, L70.
YOU CAN ALWAYS STOP. Doubt → `FAILED: <step> — <concern>` as the last line, commit the report, stop. TWO ASYMMETRIES: (a) from the first `bootout` on, production's residents are DOWN — "stop" then means finishing §8's safe-state rule first (NN#16) — with ONE named exception, §4.2's terminal branch, which repairs the resident whose bootout took and stops WITHOUT entering §8 (entering it there would loop on the bootout that just failed); that exception is written out at the step and nowhere else; (b) while `.env` sits in the gate worktree, "stop" means removing it and proving it gone FIRST (L4), then writing the stop line.

INDEX CARD (read in this order, before PREFLIGHT): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` **IN FULL** → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-p4-2026-09-19.md` **IN FULL** (tonight's deploy 2: its three runs, what its L68 gate caught, the false stop its (d2) fold fixed, and all 9 ESCALATE) → `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md` §0 + the CHUNK 2 and CHUNK 3 sections + its last line → `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md` §0 + `## Verdict table` (the houses' read of this exact code: 0 blockers, 4 MINOR — **G-A1** window built from `hour:minute`, **G-A2** a stale pre-existing docstring, **H1** the declared backfill window counts sleeps but not fetch latency, **H2** a docstring/pin contradiction; none is yours to fix) → `/Users/cobalt/cobalt-wt/archiver-append/src/cobalt/db_migrations/0010_archive_progress.sql` and `0011_archive_incidents.sql` IN FULL (what CREATED is allowed to mean at 4.5) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cards-golive-2026-09-19.md` (last line only).
REPORT (L48): `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-d3-2026-09-19.md` — a NEW file, title `# DEPLOY 2026-09-19 (3) — ops + archiver`; if it already exists you are a rerun: append `# SECOND RUN` under everything there and rewrite nothing above it. Inside: §0 Headline ≤5 lines → PREFLIGHT → L68 GATE → Preconditions → Deploy table → Smoke → Close → ESCALATE → last line.

UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no pipe, no redirect, no `; echo`, no `$(…)`, no `&&`. `cd <path>` is its OWN call and is used ONLY to enter a worktree for pytest and to return. Every git command is `git -C <absolute path> …` so cwd never decides a git answer. **CWD DISCIPLINE:** the session starts in `/Users/cobalt/cobalt`; the only place pytest may run is `/Users/cobalt/cobalt-wt/stack-deploy3-0919`; before every pytest call the immediately preceding call is `cd <that worktree>` then `ls -la .env` (during the with-DB step it must PRINT the file; everywhere else it must say "No such file"); after the last pytest call, `cd /Users/cobalt/cobalt` then `ls -la .env` — in `~/cobalt` that file EXISTS and seeing it listed is your proof that cwd is back (never print its contents, never `cat`, never `grep` it). Outputs are read from the tool result; files are written with Write/Edit. MID-RUN DENIAL = THE RUN FAILED (L62): `.env` cleanup if present, then §8 if anything is merged or any resident is down — except inside §4.2's terminal branch, which is the one place that repairs and stops without §8 — then `FAILED: <step> — <command> — <reason string>`, commit, stop. Never another shape, never another session, never a retry of a denied command in a different spelling.

## 0. PREFLIGHT — nothing here changes production (residents stay UP)
0.1 One row each (rule · command · exit · allowed/DENIED + reason verbatim):
· **P1** `git -C /Users/cobalt/cobalt status --porcelain` AND `git -C /Users/cobalt/cobalt status` (LONG form, quoted in full — no "rebase in progress", no "unmerged paths", branch `main`).
· **P2** `git -C /Users/cobalt/cobalt tag scratch-allow-probe-d3-0919` then `git -C /Users/cobalt/cobalt tag -d scratch-allow-probe-d3-0919`.
· **P3** `git -C /Users/cobalt/cobalt commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` then `git -C /Users/cobalt/cobalt reset --soft HEAD~1`; `git -C /Users/cobalt/cobalt log --oneline -1` proves HEAD is back.
· **P4** `date` → the WINDOW rule of §2.1 is read here for the record, not enforced yet.
· **P5** `COBALT_ENV=production uv run cobalt validate` → exit 0. Record the `Jobs (F17):` line verbatim (expected **16 registered — 6 resident, 10 one-shot**: deploy 2's state, with `com.cobalt.replay` present).
· **P6** `COBALT_ENV=production uv run cobalt backup status` and `COBALT_ENV=production uv run cobalt backup run --dry-run`.
· **P7 — INFORMATIONAL ONLY** (round-1 finding F7: the previous wording called it "a §2 gate" and §2 has no such gate; **5.1 is the real derivation**, on the merged tree with the new classifier). Run BOTH: `COBALT_ENV=production uv run cobalt jobs restarts main..archiver/append-0919` and `COBALT_ENV=production uv run cobalt jobs restarts main..ops/2026-09-19` — record both VERBATIM. Expected `RESTARTS: com.cobalt.aset com.cobalt.radar`. These run on MAIN's classifier, which is the old one, so a non-zero exit or UNCLASSIFIED paths are EXPECTED here and are recorded, never a stop (deploy 2's P7 exited 1 for exactly this reason).
· **P8** `COBALT_ENV=production uv run cobalt heartbeat show` — EXPECT GREEN, 16 jobs (deploy 2 added `com.cobalt.replay`). A PRE-EXISTING RED is accepted ONLY if it is the `radar` probe reading `failed_stage bars: poll failures: <n>` AND `tail -n 12 /Users/cobalt/cobalt/logs/radar.err` shows `radar cycle: idle:<session>` lines ≤4 minutes apart with no traceback (Saturday = outside every scanning session, so `idle:overnight` is the correct shape — say so); record it verbatim as the BASELINE. Any other RED → `FAILED PREFLIGHT: production not green`.
· **P9** `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → both running; record both pids and both `path =` lines (aset = `/Users/cobalt/cobalt/ops/com.cobalt.aset.plist`, radar = `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`; a different path → `FAILED PREFLIGHT: plist path moved`). Deploy 2 left aset at pid 80628 and radar at 80641 — a DIFFERENT pid is recorded, not a stop (launchd may have respawned), but say so.
· **P10** `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` → 200 and `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` → 200.
· **P11** `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(schemaname ORDER BY schemaname) FROM pg_tables WHERE schemaname IN ('public','user','system') GROUP BY tablename HAVING count(*) > 1"` → ZERO rows. Any row → `FAILED PREFLIGHT: duplicate table name — <name> in <schemas>`.
· **P12** `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN ('archive_progress','archive_incidents')"` → ZERO rows. A row means one of 0010/0011's names already exists in production → `FAILED PREFLIGHT: table name already present — <schema>.<table>`.
· **P13** `COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only`. **THIS CALL: set the Bash tool's `timeout` parameter to 600000** — it sits silent ≈50 s per probe reading `system.bars`. It applies NOTHING and ends `NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.` Quote the whole proof table and the `proof cost:` line — that number is this deploy's outage budget. It runs from `/Users/cobalt/cobalt`, i.e. main's code, which now includes P4: expect **26** tables and do NOT expect it to mention `archive_progress`/`archive_incidents` (their absence is proven by P12, not here). Gate = exactly three things: exit 0, no table marked `CHANGED`, no tool timeout. Otherwise → `FAILED PREFLIGHT: proof-only <what>` (nothing was applied; do NOT rerun).
· **P14** `.env` LANE CHECK — `ls -la /Users/cobalt/cobalt-wt/archiver-append/.env`, `ls -la /Users/cobalt/cobalt-wt/ops-2026-09-19/.env`, `ls -la /Users/cobalt/cobalt-wt/s2-p4/.env`, `ls -la /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env` → every one MUST say "No such file". Any hit → `FAILED PREFLIGHT: .env present in <path> — another session may hold cobalt_dev`.
· **P15 — THE TWO BRANCH GATES.** Read the last line of `/Users/cobalt/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md` (expected `OPS 0919 DB … | db 61/0 …`) and of `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md` (expected `BACKFILL WINDOW … | offline 2146/0 | db 2486/0 | registry 0001-0011 | … | .env: removed, proven gone`). A non-zero `failed` in either, or either line absent → `FAILED PREFLIGHT: <branch> carries red tests — L68 gate cannot be green`. Quote both lines and their position in the file.
· **P16 — THE L67 GATE.** Read `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md`'s LAST line (`D3 BUILD CHECK · … blocks deploy 3: <d> …`) and, if it exists, `/Users/cobalt/cobalt-wt/agy-trial/scratch/astra-d3-0919/REVIEW.md`'s last line (`ASTRA D3 · …`). **`blocks deploy 3` must be 0 in each line that exists.** Non-zero → `FAILED PREFLIGHT: house check blocks deploy 3 — <line>`. The desk owns how many houses read it; you only read the numbers.
0.2 `GATE: proven — commit + tag allowed by the session allowlist` (L62's launch proof), or the FAILED line.
0.3 NOT preflighted, because no harmless variant exists inside their own pattern: `worktree add`, the stack `merge --no-edit`, `merge --ff-only`, `bootout`, `bootstrap`, `kickstart`, `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.
0.4 Report written and committed by explicit name. Last line `CONTINUE: step 1`.

## 1. THE L68 INTEGRATED GATE — and it builds the branch that ships
**The law, verbatim (LAWS.md L68):** *"No branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green — the offline suite and the with-DB suite run on the branch that combines them, before the merge, never after it in `~/cobalt`."* Both remaining unmerged branches are in this deploy, so the stacked tree combines exactly what lands and nothing else. R31's narrowing was for deploy 2 and does not apply here.
1.1 `git -C /Users/cobalt/cobalt log --oneline -1` (record as `<main-at-gate>`) · `git -C /Users/cobalt/cobalt-wt/ops-2026-09-19 log --oneline -1` (expected `bb996a5`) · `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` (expected `7117fc3`, code tip `85b6d7c`) · `git -C /Users/cobalt/cobalt-wt/ops-2026-09-19 status --porcelain` and the same for `archiver-append` — both EMPTY. A different tip is RECORDED, not a stop (a branch may have advanced); a dirty worktree IS a stop: `FAILED: gate — <worktree> not clean`.
1.2 BUILD THE STACK: `git -C /Users/cobalt/cobalt worktree add -b stack/deploy3-0919 /Users/cobalt/cobalt-wt/stack-deploy3-0919 main`. **RERUN CASE:** this FAILS if the branch or the path already exists, which is what a second launch after a failed gate looks like; no `worktree remove` / `branch -D` rule is in your list, deliberately. On that failure: `ls -la /Users/cobalt/cobalt-wt/stack-deploy3-0919` to show what is there, then `FAILED: gate — stack artifact from an earlier run is still on disk (branch `stack/deploy3-0919` and/or its worktree); the desk removes it before the next launch`. Do not reuse it, do not pick another name.
If `worktree add` SUCCEEDED → then, in this order, TWO separate calls: `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --no-edit ops/2026-09-19` · `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --no-edit archiver/append-0919`. A CONFLICT on either → `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --abort`, then `FAILED: gate — conflict merging <branch> — <paths>` (NOTHING is down, production is untouched; the desk resolves it in a build run, never here). **EXPECT the overlap here if anywhere:** both branches touch `src/cobalt/cli.py` (ops +18 lines, the archiver +28) — different registrations, so it should auto-merge; ops alone touches `src/cobalt/db_migrations/cli.py`. Record the stack tip as `<stack>` and `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 log --oneline -8`; also `grep -n "0008\|0009\|0010\|0011" /Users/cobalt/cobalt-wt/stack-deploy3-0919/src/cobalt/db_migrations/__init__.py` → the merged `FORWARD` must list **0001…0011 in numeric order** with `REVERSE` its mirror; anything else → `FAILED: gate — merged migration registry is wrong — <what you read>`.
1.3 OFFLINE SUITE ON THE STACK: `ls -la /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env` → "No such file" → `cd /Users/cobalt/cobalt-wt/stack-deploy3-0919` → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 failed`**. Quote the whole summary line. Context only, never the bar: the archiver branch alone closed `2146/0`, ops alone `1875/0`. Any failure → name each test, quote each assertion, `FAILED: gate — offline suite red on the stack — <tests>`.
1.4 WITH-DB SUITE ON THE STACK (`cobalt_dev` only — never production, never `--allow-prod`). The stacked registry is **0001–0011** and `cobalt_dev` sits at 0001–0009, so the forward step CREATES the archiver's two tables and the restore drops them again. In this exact order:
  (a) `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env` (by name, never read, never printed, L4) → `ls -la …/.env` prints it.
  (b) BASELINE: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote the table. EXPECT `cobalt_dev` at **0001–0009** with `archive_progress`/`archive_incidents` ABSENT and P4's `movers_daily`/`picks`/`missed` PRESENT. A `CHANGED` verdict → go to (h), ending `FAILED: gate — cobalt_dev proof CHANGED before the gate — <line>`.
  (c) FORWARD: `COBALT_ENV=dev uv run cobalt db migrate` → quote the full table. EXPECT `archive_progress` and `archive_incidents` **CREATED**, every pre-existing table `OK` with its digest unchanged, no `CHANGED`. Anything else → (h), ending `FAILED: gate — dev forward migrate <what>`.
  (d) SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 failed`**; quote the summary. Context only: the archiver branch's own with-DB close was `2486/0`. A red → (h), ending `FAILED: gate — with-DB suite red on the stack — <test names + assertions verbatim>`. **Never label a red "known" and continue** (L68, L70).
  (e) **POST-SUITE PROOF:** `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote it. **The suite WRITES to `cobalt_dev` by design** (deploy 2 run 2 false-stopped on exactly this — `cobalt_redactions` 127 → 128), so differences between (b) and here are RECORDED table by table and are NOT a stop. A table PRESENT at (b) and missing now, or one no stacked migration creates → (h), ending `FAILED: gate — cobalt_dev table set changed during the suite — <what>`.
  (f) RESTORE: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → EXPECT exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` applied and **nothing else** (if anything numbered ≤ 0009 is reversed: quote it, (h), `FAILED: gate — dev rollback went below 0009 — <what>`) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → compare **against (e), not (b)**: the two archiver tables ABSENT again and every other table identical to (e). A digest that moved ACROSS the restore is a stop → (h), `FAILED: gate — cobalt_dev digest moved across the restore on <table>: <before> → <after>`.
  (g) `rm /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env` → `ls -la …/.env` says "No such file". **The `.env` cleanup happens before ANY stop line** — a stop line written while `.env` is on disk is itself a failure condition.
  (h) RED-BRANCH RESTORE (trigger widened, round-1 finding F6): on a red at ANY of (b) through (f), FIRST run (f) in full — it is always safe, it is a dev-lane command, and after a red (b) or (c) it usually has nothing to reverse — THEN (g), THEN the FAILED line, ending it with `cobalt_dev: <the last state you PROVED>`. Compare (f)'s closing proof **against (e) if (e) ran, else against (b)**. If the restore ITSELF errors, do not retry it: quote the error and write `FAILED: gate — <original reason> — cobalt_dev: <last proven state, or UNKNOWN>, restore failed: <error>`.
1.5 `cd /Users/cobalt/cobalt` → `ls -la .env` (the file IS listed here — cwd is back; contents never read) → `git -C /Users/cobalt/cobalt log --oneline -1`. Your OWN report commits are expected here and are fine (4.1 absorbs them). **What is not fine is a commit that is not yours or the desk's** — `git -C /Users/cobalt/cobalt log --oneline <main-at-gate>..HEAD` and read every subject: anything that is not this run's report, the desk's docs, or a machine-written file commit → `FAILED: gate — foreign commit on main during the gate — <hash> <subject>` (round-1 finding F5: stop HERE, before §3's production snapshot and before the `pre-d3-0919` tag, which has no delete rule on a rerun).
1.6 Write the gate into the report as its own section: the three tips, `<stack>`, the merged `FORWARD` line, both suite lines VERBATIM, all THREE `--proof-only` verdicts, the forward and rollback tables, the `.env` proof, and both P15 lines quoted. A gate section with a missing step is `FAILED: gate — incomplete`, never a green one. **`stack/deploy3-0919` is NOT a throwaway this time — it is the branch §4.3 fast-forwards `main` to.** Report committed by name. Last line `CONTINUE: step 2`.

## 2. Preconditions
2.1 **WINDOW.** `date` → the run STARTS only when now is **≤ 20:15 ET on Saturday 2026-09-19**. WHY: R32 overruled L43's one-deploy-per-evening for tonight and set its own condition — the lane green before 21:00 — and the hard edge is `com.cobalt.backup`, which runs at **21:40 ET on ALL SEVEN days** (`configs/cobalt/jobs.yaml:242` `schedule: {at: "21:40", weekdays: [0, 1, 2, 3, 4, 5, 6]}` — round-1 finding F11 corrected the stale `:224`; `ops/com.cobalt.backup.plist:73-79`) and must never fire into a half-deployed tree. 20:15 leaves the measured ≈3–4 minute outage plus smoke plus margin. `com.cobalt.archiver` (20:30) and `com.cobalt.replay` (21:10) are **Mon–Fri only** and cannot fire on a Saturday. L43's restart-window clause is met independently: Saturday is not a trading day and the radar reads `idle:overnight` at every hour, so no scanning session can be open. Now > 20:15 → `FAILED: window — wait for the desk` (the desk re-approves a new window; you never pick one).
**THE CLOCK IS RE-CHECKED with `date` ONCE, immediately before 4.2 (bootout), and nowhere else. At or after 21:00 ET at that check → `FAILED: window`, nothing is down, nothing is merged, stop.**
**THE POINT OF NO RETURN: once 4.3's `merge --ff-only` has returned success, this run FINISHES FORWARD.** From that instant the clock is a MEASUREMENT, never a trigger; the `date` calls at 4.2 and 5.2 compute the outage length and stamp the report, and no hour sends a successful merge+migration to §8. §8 is entered ONLY for a RED result (a failed step, a red smoke, a mid-run denial), never for a late clock. If the run is still going at 21:40 and the backup fires beside it, that is an ESCALATE line, not a revert. Conversely nothing after 4.2 may be ABANDONED for the hour either: finish §5's residents-up, finish §6's smoke, write the stop line.
2.2 **THE GATES ALREADY READ AT P15/P16** are quoted again in this section as the record: both branch stop lines, the `D3 BUILD CHECK` line, and the `ASTRA D3` line if it exists. Neither is re-derived by you.
2.3 **THE GATED CODE.** The approval row R36 binds this FILE by sha256 and does not list branch tips, so the tips are named HERE (round-1 finding F4) and are what you check: ops CODE tip **`954cabb`** (its branch tip `bb996a5` is a docs commit), archiver CODE tip **`85b6d7c`** (its branch tip `7117fc3` is a docs commit). `<gated>` = those two. `git -C /Users/cobalt/cobalt-wt/archiver-append diff --stat 85b6d7c HEAD -- . ':(exclude)docs'` prints NOTHING, and `git -C /Users/cobalt/cobalt-wt/ops-2026-09-19 diff --stat 954cabb HEAD -- . ':(exclude)docs'` prints NOTHING. Only report commits may sit above the gated code; anything else → `FAILED: gate — <branch> moved since approval — <paths>`.
2.4 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; no other session is merging (the desk launches you alone).

## 3. Machine-written files, snapshot, tag
3.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `docs/40 - DevDocs/reports/seat-usage.md`, the desk's live `cto-2026-09-19.md`, your own report, and the two untracked `docs/40 - DevDocs/prompts/2026-09-19/31-packet/*.yaml` copies (copies of files under gitignored `data/backups/` — **do NOT `git add` them**, name them in ESCALATE and leave them untracked). `git add` BY NAME (never `-A`, never `.`, never a directory), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`.
3.2 `COBALT_ENV=production uv run cobalt backup run` (**THIS CALL: `timeout` 600000**) → quote the `cobalt_brain` dump size line and the restic snapshot id; `COBALT_ENV=production uv run cobalt backup status` shows it newest. No snapshot id → `FAILED: snapshot`. `git -C /Users/cobalt/cobalt tag pre-d3-0919`. (The 21:40 nightly prunes this snapshot — record id + time; **the durable rollback point is the tag `pre-d3-0919`**.) There is NO rollback DIRECTORY: no settings file is read or written.
3.3 Report committed by name NOW — **this is your LAST commit before the merge**. Last line `CONTINUE: step 4`.

## 4. Residents DOWN → merge → migrate → validate (no report prose between these commands; record outputs, write them up after 5.2)
4.1 **BRING THE STACK UP TO `main` — round-1 finding F1, a launch BLOCKER in the previous wording, found by both reading houses and by the hub.** `main` MOVES between §1.2 and here ON EVERY LAUNCH, and by your own hand: §1.6 and §3.3 commit the report and §3.1 commits the machine-written files. The old text then demanded `HEAD` = `<main-at-gate>` and would have hard-stopped every run. The fix, which needs no rule you do not already have: `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --no-edit main` → record its output. **PROVE IT IS DOCS-ONLY:** `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 diff --stat <stack> HEAD -- . ':(exclude)docs'` prints NOTHING — if it prints anything, code entered the stack after the suites ran and the gate no longer covers what ships: `FAILED: 4.1 — non-docs change entered the stack after the gate — <paths>` (nothing is down). A CONFLICT here: `git -C /Users/cobalt/cobalt-wt/stack-deploy3-0919 merge --abort`, then `FAILED: 4.1 — conflict merging main into the stack — <paths>` (nothing is down). Record the new stack tip as `<stack-final>`. Then the ff test, in its CORRECT form: `git -C /Users/cobalt/cobalt merge-base --is-ancestor main <stack-final>` → **exit 0** (a non-zero exit means the fast-forward at 4.3 cannot happen: `FAILED: 4.1 — main is not an ancestor of the stack — <output>`, nothing is down). Record `<pre-merge>` = `git -C /Users/cobalt/cobalt rev-parse --short HEAD` — it is main's CURRENT tip, not `<main-at-gate>`.
4.2 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → baseline. `date` (the 2.1 re-check). Then **residents DOWN (L66)**: `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → each must FAIL with "Could not find service" (still printed → one `date`, print once more; **still loaded after that one retry → the run STOPS, but not before you put the OTHER resident back: for EACH label whose `launchctl print` now says "Could not find service" (i.e. the bootout that DID take), run 5.2's bootstrap for that label ONCE, print `state` once more, and record it. ONE attempt per label, no `kickstart`, no second pass. THEN write `FAILED: residents not stopped — <label> — <launchctl output verbatim>`, say plainly that NOTHING was merged and nothing was migrated, and state the services as `residents: aset <up/DOWN>, radar <up/DOWN>` from what you just printed, NEVER as "as they were". Do NOT enter §8** — §8's step (0) is a bootout, which is the thing that just failed). A heartbeat beat landing in this window goes RED on the sheet/radar probes and DMs him — expected; note the beat time afterwards.
4.3 `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<pre-merge>` as recorded at 4.1 (else `FAILED: foreign commit on main — <hash>` → §8 steps 3–5 only — nothing merged, bring the residents up) → `git -C /Users/cobalt/cobalt merge --ff-only stack/deploy3-0919` (the branch, now at `<stack-final>`) → record the tip and the file/insertion counts. A non-fast-forward error here means main moved: `FAILED: merge not a fast-forward — <output>` → §8 steps 3–5 only (nothing merged, bring the residents up).
4.4 (nothing here — no settings step in this deploy.)
4.5 **THE MIGRATION.** `COBALT_ENV=production uv run cobalt db migrate --allow-prod` — **THIS CALL: `timeout` 600000.** EXPECT it to sit SILENT ≈50 s in its BEFORE probe and again in its AFTER probe: run it in the FOREGROUND, do not interrupt it, do not background it. The runner walks ALL of `FORWARD` = 0001…0011 and prints `-- applying` for each; 0001–0009 re-run idempotently by design. **GATE = its proof table:** NO table marked `CHANGED`, and the only `CREATED` objects are the ones `0010_archive_progress.sql` and `0011_archive_incidents.sql` define — read those two files (index card) and hold the output to them: `system.archive_progress`, `system.archive_incidents`, and the sequence `archive_incidents_id_seq`. Quote the whole table and the `proof cost:` line. STATED FINDINGS, not to be re-derived: (i) the whole migrate transaction runs at REPEATABLE READ, so a concurrent commit cannot read as `CHANGED`; a `could not serialize access` error means the harness applied NOTHING — that is a failed 4.5 → §8, say `serialization failure`, and do NOT rerun. (ii) The harness COMMITS BEFORE it prints its proof table, so a timeout in the instant after the commit leaves 0010/0011 APPLIED with no table printed. **Whenever 4.5 ends without a complete proof table, do NOT write "nothing applied" from the absence of output: read it back first** — `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT to_regclass('system.archive_progress')"` → empty/NULL = not applied, a name = applied and committed — then go to §8 and label the FAILED line `migrations applied: yes/no` from that read. A tool timeout is labelled `tool timeout`, never `migration error`. Non-zero exit → §8; output showing a migration PARTIALLY applied → run NO rollback, quote it, `FAILED: migration partial` after §8's code revert + residents up.
4.6 `COBALT_ENV=production uv run cobalt validate` → exit 0. Record the `Jobs (F17):` line, `registry <-> ops/` and `registry <-> plists` lines, and `Placement: tree clean`. (Round-1 finding F10: `Placement: tree clean` is `check_tree()` over `docs/PLACEMENT.md`, NOT a check that the placement map names 0010/0011's tables — the earlier sentence claiming that was wrong and is deleted. The table-placement invariant is asserted by the suite, which §1.3 and §1.4 already ran.) Red anywhere in 4.3–4.6 → §8.

## 5. Residents UP
5.1 `COBALT_ENV=production uv run cobalt jobs register` → quote the count (expected `registered 16 job(s)` — this deploy adds NO new job; a different number is recorded and named). Then `COBALT_ENV=production uv run cobalt jobs restarts <pre-merge>..HEAD` VERBATIM; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 UNCLASSIFIED. A THIRD resident derived → ESCALATE, not restarted today. Any UNCLASSIFIED path → ESCALATE by name, never dropped (L42).
5.2 `date` (**MEASUREMENT ONLY**) → `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` (the REPO plist, R6) · `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` · `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar` → `state = running`, NEW pids, `path =` lines unchanged. `Bootstrap failed: 5` → one `date`, retry ONCE. Loaded but not running → `launchctl kickstart -k gui/501/<label>` ONCE. Still not running → `FAILED: residents DOWN — <label> — <launchctl output verbatim>` after running §8 from (0) through (3), then ONE bootstrap attempt of both on the reverted, validated tree. Whatever is not running after that stays down: terminal state; §8 is never entered twice. `date` → outage length (4.2 → 5.2).

## 6. Smoke — ALL delivered functionality (NN#16), each quoted in `## Smoke`
6.1 (every `curl` typed in its FULL allowlisted spelling) `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` = 200 · `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` = 200 · `COBALT_ENV=production uv run cobalt heartbeat show` → **no NEW RED, and a NEW RED sends this run to §8**. TWO named exceptions, neither a new RED: the P8 BASELINE class, and a RED whose timestamp falls INSIDE the outage window 4.2→5.2. Anything else red → §8 · `COBALT_ENV=production uv run cobalt validate` exit 0 with its `Jobs (F17)` / `registry <-> ops/` / `registry <-> plists` / `Placement` lines quoted · `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` against 4.2's baseline.
6.2 LOGS: `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` → started on the new code, no traceback, **no `CardSettingsError`, no `trade_def_slug`, no `radar_pool_failed_stage_check`**, and **no `not_equity` / config-shape error**. Outside a scanning session the correct cycle line is `radar cycle: idle:overnight scan_id=None` ≈100 s apart — say so. · `tail -n 30 /Users/cobalt/cobalt/logs/aset.log` → no traceback, no degraded day-mode banner.
6.3 **THE ARCHIVER'S OWN SURFACES, read-only.** (a) the two new tables exist and are readable on the system side: `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT count(*) FROM archive_progress"` and the same for `archive_incidents` → a number, no error (**0 rows is the expected and correct case** — no nightly run has happened yet; the first is Monday 20:30, Mon–Fri). (b) **THE REVOKE (R25), the one thing this deploy adds that is a PERMISSION and not a table:** `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT has_table_privilege('cobalt_user','system.archive_progress','SELECT')"` and the same for `archive_incidents` → **both must return FALSE**. A TRUE means 0001's blanket grant won and R25 did not take → that is not a crash but it is a defect against an explicit ruling: record it verbatim, finish the smoke, and put it in ESCALATE as `R25 NOT IN FORCE` — **do not revert for it and do not try to fix it** (a `REVOKE` typed by hand is a production DDL nobody approved). If the query itself errors, quote the error and say the check could not run (L70) rather than reporting a pass.
6.4 **CARDS ARE STILL LIVE** (a READ): `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → **one row, True**. Anything else → `FAILED: cards state changed — <what>` and §8 (whose (3) leaves every settings row exactly as it is). This is a verification, never a repair: you have no `settings load` command in any spelling.
6.5 **`cobalt smoke s2` IS NOT RUN**, and its redness is not a deploy verdict — K9 and K3 are red by design until Monday's first replay row and until the desk's A7/K3 ruling (`s2-p4-verify-2026-09-19.md` ESCALATE 2 / 15). Do not run it, do not add it to any check list, never write "smoke red" as this deploy's verdict. NN#16 is met by 6.1–6.4. If some other command's output prints a K3 or K9 line, record it verbatim labelled `RED BY DESIGN — not a deploy failure`, and continue.
6.6 NOT DONE HERE, deliberately: no `cobalt archiver` run of any kind (the nightly is Mon–Fri 20:30 and the first live run is **Monday 2026-09-21**; R35 makes that ONE shadow night before his ruling on the numbers) · no backfill (R34 bounds it out of 04:00–09:30 and it is operator-initiated anyway) · no scan proof: Saturday is overnight idle throughout, so the first live proof of the ops day-open guard is **Sunday 2026-09-20's `cobalt day-open`** and of the archiver **Monday 20:30**. Write those two sentences.

## 7. Close
Green → `git -C /Users/cobalt/cobalt tag deploy-2026-09-19c`. **The tag name is fixed at approval and is never re-derived by you**: `deploy-2026-09-19` and `deploy-2026-09-19b` are taken by tonight's first two deploys, so this one is `deploy-2026-09-19c`. If `date` says the day is no longer 2026-09-19 when you reach §2.1, that is `FAILED: window` at 2.1 and you never reach here; you do NOT silently rename the tag. Then: the Deploy table, `RESTARTS done`, the outage length, the two "first live proof" sentences of 6.6, and the R25 REVOKE result from 6.3 (b) stated explicitly either way. ESCALATE section: the gate artifacts (`stack/deploy3-0919` + its worktree — **this time the branch IS merged, so only the WORKTREE is cleanup owed**), the two untracked `31-packet/*.yaml`, the four MINOR findings the houses left open (G-A1, G-A2, H1, H2 — carried, not fixed), and the P4 escalates the desk still owes a ruling on (A2, A7/K3, ESCALATE 19), plus anything you found. Report committed by name. Last line, exactly this shape:
`D3 DEPLOY DONE <hash> · LIVE: ops/2026-09-19 + archiver/append-0919 · migrations 0010/0011 applied · gate: stack <stack> offline <p>/<f>, db <p>/<f> · RESTARTS done: com.cobalt.aset com.cobalt.radar · R25 REVOKE: <in force | NOT IN FORCE> · archiver mode: upsert, first nightly Mon 20:30 ET · cards: LIVE, untouched · outage <mm:ss> · tag deploy-2026-09-19c · PUSH: Dejan · ESCALATE: <n>`
or `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`.

## 8. SAFE-STATE RULE — red at 4.3–4.6, red smoke, a failed resident bootstrap, or a mid-run denial after 4.2
STEP (0), FIRST, ALWAYS: `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar`; if EITHER is loaded → `launchctl bootout` both and require "Could not find service" for both BEFORE you touch code. A resident must never be running while its code is reverted under it; `bootstrap` in (4) is only ever run on a service that is not loaded. **`com.cobalt.replay` is NOT booted out here** — it arrived with deploy 2, it is not in this deploy's range, and reverting this range leaves its module in place; `com.cobalt.archiver` likewise already existed. Neither can fire on a Saturday.
GOAL: code and config agree, residents UP, before you stop. **THIS DEPLOY HAS NO SETTINGS LEG** — no `trader_settings` row was written, so there is no second half to restore. CLOCK NOTE: **no hour ever sends a green result into this section** (§2.1's point of no return). The only job that fires tonight is the 21:40 backup; if §8 is still running as it approaches, finish (0)–(5) anyway and let the ESCALATE say so.
(1) **If 4.3 merged — and read this twice, it is round-1 finding F2, a launch BLOCKER in the previous wording.** The range `<pre-merge>..HEAD` **CONTAINS MERGE COMMITS** — §1.2 made two (`ops` and `archiver` into the stack) and 4.1 made a third (`main` into the stack) — and `git revert` REFUSES a merge commit without `-m`. Deploy 2's range was linear; this one is not. So: `git -C /Users/cobalt/cobalt log --oneline --merges <pre-merge>..HEAD` → list them, then revert **newest first, one command per commit**: `git -C /Users/cobalt/cobalt revert --no-edit -m 1 <sha>` for each merge commit, and `git -C /Users/cobalt/cobalt revert --no-edit <sha>` for each non-merge commit in the same descending order (`-m 1` = keep the first parent, which is `main`'s side — the side production was on). Both spellings are covered by the approved `revert --no-edit *` rule. Quote every command and its exit. **If a revert errors for any reason other than a conflict** (a bad `-m`, an empty revert, anything): stop reverting, do NOT improvise another spelling, and go straight to (3)–(6) with `safe state: NEW code kept — revert failed: <error>` — a live tree on new code that passed its own suites is safer than a half-reverted one. A CONFLICT during the revert: `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → that pair is ALREADY consistent, so make THAT your safe state: `COBALT_ENV=production uv run cobalt validate` exit 0, then (3)–(6) with `safe state: NEW code kept — revert conflicted`.
(2) (no settings step.)
(3) **Migrations 0010/0011 STAY — IF 4.5 RAN** (round-1 finding F9: on the 4.3 non-ff or foreign-commit branch nothing was ever applied, and the report must say `migrations applied: no` rather than that they stay). When they were applied: they are additive (two new tables, nothing else reads them) so reverted code runs unchanged over them. **The schema is NEVER rolled back unattended** — no production rollback is instructed anywhere in this prompt and none is typed in any branch; the rollback files exist and are the DESK's documented path with Dejan, run as its own approved job, never from inside a failing deploy. PROVE the schema is harmless where it stands: `validate` exit 0 and the sheet 200 after (4). Likewise **no settings row is touched** — cards stay LIVE through every branch of this section.
(4) Bring the residents up with 5.2's commands and 5.2's single retry / single `kickstart` rule ONLY; a failure here is terminal (`residents: DOWN`), never a second pass through §8.
(5) Re-run 6.1's curl / heartbeat / validate lines and 6.4's cards read → quote them.
(6) `ESCALATE` with the snapshot id, the tag `pre-d3-0919`, the reverted range, what stays applied (0010/0011), and the gate artifacts; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. **Residents DOWN at the end is the one outcome the desk escalates by push at once.**
