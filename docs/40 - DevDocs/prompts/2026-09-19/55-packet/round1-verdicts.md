# ROUND 1 — headline and the complete verdict table (verbatim)

## §0 Headline
- **NOT ZERO. Two launch blockers, both real, both named independently by two houses and re-derived by the hub: (1) the run's own commits move `main` after the stack is cut, so 4.1 hard-stops (or 4.3 cannot fast-forward) on EVERY launch; (2) §8's `git revert <pre-merge>..HEAD` has no `-m`, and the range now holds two merge commits, so the code-revert safe state cannot run.** Both fail SAFE (nothing down at 4.1; §8 steps 3–5 at 4.3) but deploy 3 cannot complete as written.
- **Neither fix has to change a RULE STRING** — stack `merge --no-edit *` and `revert --no-edit *` already cover the spellings needed (details in the table). A rebase route WOULD need new rules (rebase rules were dropped) → Sunday. Desk's call; folding either fix changes the file R36's sha256 carries.
- Houses that read it: Gemini (both buckets), Grok (bucket B; bucket A = HARNESS), Astra METER — L67's "one other house" floor met by two. 9 further REAL findings are MINOR/MAJOR text (none blocks); 5 claims NOT REAL; 3 UNVERIFIABLE FROM READS.
- Q2 checked by hub: 51 rules = 41 identical + 10 new, 14 dropped, no step types an uncovered command, both `launchctl bootstrap` strings exist character for character, `push` in no rule. Q6(c) plists checked directly: archiver Mon–Fri, replay Mon–Fri, backup all seven days.
- Not counted (per the prompt): P15, the wide-vs-narrow L68 scope, and the prefix-match question (Grok's M2 is exactly that).


## Verdict table
Two houses naming the same defect = one row. `blocks the launch?` only for REAL rows.

| # | house(s) | sev (theirs) | where | claim (≤20 words) | what I read | VERDICT | blocks the launch? | fix |
|---|---|---|---|---|---|---|---|---|
| **F1** | Grok B (B1), Gemini A (Q1b/Q8), Gemini B (Q7 1–2); **also found by the hub while staging** | BLOCKER | §1.2 vs 1.6, 3.1, 3.3, 4.1, 4.3, WHY (4) | Report/machine-file commits on `main` after the stack is cut make 4.1 fail and 4.3 non-ff on every launch | `53:47` stack from `main`; `:60`, `:71`, `:73` commits on `main`; `:76` 4.1 `FAILED` if moved; `:78` 4.3; `:9` WHY (4) contradicts; only commit rule is `git -C /Users/cobalt/cobalt commit *`; 1.5 (`:59`) says "go on" | **REAL** | **yes** — no launch reaches 4.3; fails safe (nothing down at 4.1; residents up via §8 3–5 at 4.3) | Text-only, no new rule: (i) hold 1.6/3.1/3.3 commits until after 4.3 (and say 3.1's dirty tree is allowed through the ff); or (ii) after 3.3 `git -C …/stack-deploy3-0919 merge --no-edit main` (covered by the existing `merge --no-edit *`; docs-only, prove with `diff --stat <stack-before> HEAD -- . ':(exclude)docs'`) and make 4.1's test `merge-base --is-ancestor main <stack>`. A rebase route needs rules that were dropped → Sunday |
| **F2** | Grok B (B2), Gemini B (Q8) | BLOCKER | §8 (1) | `git revert --no-edit <pre-merge>..HEAD` has no `-m`; the range holds merge commits, so the code revert cannot run | `main` is not an ancestor of `ops/2026-09-19` (exit 1) and both branches fork at `19bb1b2`, so `merge --no-edit ops` and `merge --no-edit archiver` are TWO true merge commits inside the range; the diff DELETED deploy 2's qualifier "(not expected on a linear range nothing was committed above)"; the only named abort is "A CONFLICT" | **REAL** (structure verified by reads; git's exact error string not run — see U4 note) | **yes** — the safe state after any red at 4.3–4.6 / smoke cannot execute; the hub would improvise or keep a red tree live (L70) | Text-only: `revert --no-edit -m 1 <sha>` per merge, newest first (covered by `revert --no-edit *`; NOT-REAL note below on Grok's "allowlist cannot say -m"), or another merge-aware spelling; name the non-conflict error branch. If F1 (ii) is chosen a third merge joins the range |
| F3 | Grok B (M1) | MAJOR | TWO ASYMMETRIES (`:15`), UNATTENDED RULES (`:20`) vs 4.2 (`:77`) | Global stop rules lost the named 4.2 exception; they now say "stop = §8" while 4.2 says "Do NOT enter §8" | worddiff shows both deploy-2 exceptions removed; 4.2 text unchanged | **REAL** (MAJOR by theirs; hub: MINOR-to-MAJOR — the step is explicit and more specific) | no — 4.2 says "Do NOT enter §8 — §8's step (0) is a bootout"; fold anyway (class deploy 2's checks kept finding) | Restore the exception sentence in both places |
| F4 | hub | MAJOR | 2.3 (`:67`) | "`<gated>` = the two branch tips named in the approval row" — R36 names none | `grep -c` of `7117fc3|85b6d7c|bb996a5` in `cto-2026-09-19.md` = **0** (deploy 2's row carried its tip: `9045260|a59ee8c` = 6); 1.1 (`:46`) names `bb996a5` / `7117fc3` / `85b6d7c` "expected" | **REAL** | no — 1.1 carries the tips and R36 binds the file's sha256; a strict hub false-stops at 2.3 with nothing down | Name the tips in 2.3 itself (ops **code** tip too — `bb996a5` is a docs commit) |
| F5 | Grok B (N1) | MINOR | 1.5 (`:59`) vs 4.1 (`:76`) | 1.5 "main moved → go on" then 4.1 fails after 3.2's production snapshot and `pre-d3-0919` tag | read; a rerun's `git tag pre-d3-0919` has no delete rule | **REAL** | no | 1.5: same FAILED as 4.1, before §3 |
| F6 | Grok B (N2), Gemini A (Q5c "MAJOR") | MINOR / MAJOR | 1.4 (b), (e), (h), (f) (`:52-58`) | (b)/(e) route to (h), whose trigger is "(c) or (d)"; (f)'s "compare against (e)" has no (e) on a red before it | read; deploy 2's (f)-after-red-(d) had the same gap | **REAL** (hub: MINOR — dev DB only, `.env` removed by (g), FAILED line ends with the proved state) | no | Extend (h)'s trigger to (b)–(f); say "against (e) if it ran, else (b)" |
| F7 | Grok B (N3) | MINOR | P7 (`:30`) | P7 derives only `main..archiver/append-0919` (not ops, not the stack), runs on main's old classifier, and calls its result "a §2 gate" — §2 has none | read; deploy 2's P7 exited 1 on the old classifier (report lines 54-63) | **REAL** | no — 5.1 is the real derivation | Say P7 is informational; add `main..ops/2026-09-19` |
| F8 | Gemini A (Q2) | MINOR | launch line | `wc *`, `rev-list*`, `show*` rules no step uses | `grep` of the real file: only on line 3 | **REAL** | no | Leave (part of the approved line) |
| F9 | Gemini B (Q8) | MINOR | §8 (3), (6) (`:105`, `:108`) | On the 4.3 non-ff / foreign-commit branch the FAILED text still says 0010/0011 "STAY" though 4.5 never ran | read | **REAL** | no | Qualify with "if 4.5 ran" |
| F10 | hub | MINOR | 4.6 (`:81`) | "`Placement: tree clean` … the placement map must already name [0010/0011]" — false: that line is `check_tree()` on `docs/PLACEMENT.md` | `src/cobalt/cli.py:430-439`; no `CREATED_TABLES`/`db_migrations` reference under `src/cobalt/placement/` | **REAL** (a wrong rationale, harmless check) | no | Delete the sentence |
| F11 | hub | MINOR | 2.1 (`:63`) | Cites `configs/cobalt/jobs.yaml:224` for the 21:40 backup | main's line 224 is `com.cobalt.daymode-propose`'s `kind`; the schedule is `:242`; plist `:73-79` confirms the fact | **REAL** (stale line number) | no | Cite `:242` |
| — | Gemini A | MINOR | 1.4 (h) | `.env` not removed if the restore errors | (g) `:57`, asymmetry (b) `:15`, (h) "THEN (g)" | **NOT REAL** | n/a | — |
| — | Gemini A | MINOR | §8 (5) | `validate` goes red on reverted code because "the placement map lacks the new tables" | `check_tree` is a docs-tree check (F10) | **NOT REAL** | n/a | — |
| — | Grok B | (in B2) | launch line | "allowlist cannot say `-m`" | `Bash(git -C /Users/cobalt/cobalt revert --no-edit *)` matches `revert --no-edit -m 1 <sha>` | **NOT REAL** (the fix is text-only) | n/a | — |
| — | Grok B (M3) | MAJOR | 2.1 / 4.2 | R32 "lane green before 21:00" vs a 4.2 check that only refuses a bootout at/after 21:00 | R32 (`cto-2026-09-19.md:46`): "if its lane is green before 21:00 … must START early enough"; R36 (3) start ≤ 20:15; a 20:59 bootout ends ≈ 21:05, 35 min before 21:40 | **NOT REAL** | n/a | — |
| — | Grok B | (aside) | launch line | "auto mode NOT set … exactly as 02 and 38 ran" is false on its face | neither `02` nor `38`'s launch line has `--permission-mode` (`grep -c`: 0 / prose only); no `defaultMode` in `~/.claude/settings.json` or the project settings; the prose "auto mode on" in 02/38 was the desk's own error | **NOT REAL** | n/a | — |
| — | Grok B (M2) | MAJOR | launch line | WILDCARD AUDIT deleted; unstarred `… migrate --allow-prod` might admit `--rollback` | this IS the prefix-match question | **OUT OF SCOPE** (desk's experiment; not counted) | n/a | — |

**UNVERIFIABLE FROM READS (3)** — the missing command is named; none invented:
1. **U1 (carried from round 2, #6):** whether an UNSTARRED rule matches by prefix on this host — fresh scratch session with `--allowedTools "Bash(echo hi)"`, type `echo hi there`, expect DENIED. If it matches, `… db migrate --allow-prod` would admit `--rollback --down-to …` on production; this deploy has no auto mode.
2. **U2 (carried from round 2, #22a):** reverted code runs unchanged over the additive 0010/0011 — on `cobalt_dev`, start pre-archiver reads after `COBALT_ENV=dev uv run cobalt db migrate` (a build-lane run, never here).
3. **U3:** the stacked merges are conflict-free and both suites are green on the stack — the gate's own job at 1.2–1.4 (`git -C …/stack-deploy3-0919 merge --no-edit …`, `uv run pytest -q tests/cobalt tests/taxonomy`, both with and without `cobalt_dev`); the build check's numbers `2146/0`, `2486/0` are evidence, not verification.
(F2's one running confirmation, not counted twice: in a scratch repo, `git revert --no-edit <base>..HEAD` over a range holding a merge commit — expect `error: commit <sha> is a merge but no -m option was given`.)

D3 PROMPT CHECK · grok: DO NOT RUN 2 (call B; call A HARNESS) · gemini: no verdict line (A: 1 BLOCKER; B: 1 BLOCKER + 3 Q7 breaks) · astra: METER · hub-verified REAL: 11 (blocks the launch: 2) · NOT REAL: 5 · UNVERIFIABLE: 3
