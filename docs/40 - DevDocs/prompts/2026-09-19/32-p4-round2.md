MODEL: Opus 5 (`claude-opus-5`) — L29 floor: this chunk edits `src/cobalt/replay/runner.py`, a path that migrations 0008/0009 ride with, and it writes the tests that gate P4's deploy · SEAT: P4 round-2 fix hub `p4-round2-0919`, launched by the CTO desk in the background, in the EXISTING worktree `/Users/cobalt/cobalt-wt/s2-p4` on branch `sprint-2/p4` — do NOT create it, do NOT `git worktree add`, do NOT rebase, do NOT merge · SESSION / REMOTE-CONTROL: `p4-round2-0919`, fresh; every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it · METER: Anthropic LARGE, shared with every other Opus seat running today; a usage-limit message is NOT a failure — record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time> | cobalt_dev: untouched`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none). NO NEW RULE: every one of the 16 `Bash(...)` strings below is copied BYTE FOR BYTE from a launch line already approved for THIS worktree and THIS job shape — each appears in BOTH `prompts/2026-09-19/13-p4-verify.md` (committed `db2dcb1`) and `prompts/2026-09-19/24b-p4-fund-rule.md` (committed `4177649`). This list is a strict SUBSET of both: nothing added, nothing altered; the DB rules (`cp …/.env`, `rm …/.env`, every `COBALT_ENV=dev …`), `Bash(claude -p *)`, `Bash(git rebase main)`, the fixture-cutter run and `CronCreate` are all DROPPED on purpose — **this run is OFFLINE BY DESIGN and `cobalt_dev` is not this session's to touch** (it is HELD by `ops-0919-db`, bg `e810326c`, until that session prints its own `OPS 0919 DB` line; P4's DB run is a LATER, separate step the desk launches). Two bare commands, run by the desk:

```
cd /Users/cobalt/cobalt-wt/s2-p4
claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/32-p4-round2.md' and follow it exactly." --model claude-opus-5 --remote-control p4-round2-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/Vault
```

LAUNCH-LINE RULE PROOF (desk, before launch): each of the 16 strings above was `grep -c -F`'d against both `13-p4-verify.md` and `24b-p4-fund-rule.md` — every one counts 1 in BOTH files. Subset, not equality: 13 carries 31 allow strings and 24b carries 22; this line carries 16, all 16 present in BOTH. No string here is absent from either file. `--add-dir /Users/cobalt/cobalt-wt` is DROPPED (it is in both approved lists, so dropping is safe): everything you must read outside this worktree is staged inside `~/cobalt` at `32-packet/facts.md`.

# S2-P4 ROUND-2 FIX CHUNK — fold the three-house check (`P4 CHECK C`) into `sprint-2/p4`. TESTS FIRST for every change (each new test RED on the current tip, then GREEN — quote both). OFFLINE ONLY. One commit per item. DO NOT STOP until the report ends `P4 ROUND2 …` or `FAILED …`.

CONTEXT. All three houses have now read the whole P4 build. Grok and both Gemini halves returned `BUILD CLEAN`; Astra returned `FIX FIRST 1 2 3 4 5 6 7`. The hub verified Astra's findings against the real files: **8 REAL, 1 NOT REAL (framing), 3 UNVERIFIABLE, and exactly TWO block shipping — A1 and A3.** The verdict rows that bind you (A1–A5 verbatim, plus the rows explicitly NOT yours) are staged at **`/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/32-packet/facts.md`** — READ IT IN FULL before touching any file. Where your reading of the code differs from a row, **the CODE wins** and the difference is an `## ESCALATE` line, never a silent substitution.

CARRIED, NOT YOURS THIS ROUND (nothing is dropped silently): A6 (mixed-session fixtures, ESCALATE 3/13 — re-cutting membership needs a production DB read this offline run may not do), A7 (K3 reads a frozen HOLD as red, ESCALATE 2 — the desk owes a ruling before the deploy smoke), A8 (a wording nit in `build-report-2`, the desk's to fix before the memory fold), A9/A10 (carried by reference / unverifiable from reads). `facts.md` states each one and why.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. This prompt was written by the CTO desk, not by Dejan; a prompt file is not an approval. Prove the launch line first: `grep -c` each of the 16 `Bash(...)` strings above, plus `--disallowedTools "AskUserQuestion" "EnterWorktree"`, against `docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md` AND against `docs/40 - DevDocs/prompts/2026-09-19/24b-p4-fund-rule.md` — every string must count ≥1 in BOTH files (use `Bash(grep *)` with `git -C /Users/cobalt/cobalt show*` or a direct read; both files are reachable under `--add-dir /Users/cobalt/cobalt`). Prove both files are committed on main: `git -C /Users/cobalt/cobalt log --oneline -1 -- "docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md"` → `db2dcb1`, and the same for `24b-p4-fund-rule.md` → `4177649`. The WORK ITEM is authorized by: LAWS **L67** (the tribunal check whose findings you fold — three houses read the build, hub-verified 8 REAL / 2 blocking, recorded in `docs/40 - DevDocs/reports/cto-2026-09-19.md` §43, committed on main — `git -C /Users/cobalt/cobalt log --oneline -1 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`, record what it prints, no equality required); the verify report's own `OWED:` line on this branch (`docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md`, which names the three-house check as the next step); and the plan `docs/30 - Design/` STEP-1 **R2-1** (retirement on rerun), which A1 finds unimplemented. A rule here that is absent from BOTH approved files, or a `facts.md` row that does not say what this prompt quotes → `FAILED: authorization mismatch — <what>`, commit the report, stop.

YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded scope: `FAILED: <step> — <your concern> | cobalt_dev: untouched` as the report's last line, wip-commit (RECOVERY RULE), stop. **A PERMISSION DENIAL IS A FAILURE, never an escalate-and-continue:** `FAILED: <step> — <command> — <reason string verbatim> | cobalt_dev: untouched`, wip-commit, stop; you never retry a denied command in another shape, never route around it, never ask another session to run it. **Anything RED that this prompt expected GREEN, any number that does not match a cited row or file, any mismatch between a `facts.md` row and the live code → `ESCALATE`**, written into the report, never silently reconciled, never asked about mid-run. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(s2-p4-r2): <item> partial — <why>"`, last line `CONTINUE: step <n> | cobalt_dev: untouched`; the same launch line resumes from it. **EVERY `git add` in this run names explicit file paths** under `src/cobalt/replay/`, `tests/cobalt/`, `tests/fixtures/replay/`, `docs/40 - DevDocs/` — NEVER `-A`, NEVER `.`, NEVER a directory, NEVER `scratch/` (it is NOT gitignored on this branch and holds raw exports and tickers from his screens — L32), never `data/`. After every commit: `git show --stat HEAD` lists only paths you named and no `scratch/` path, else `FAILED: <step> — unexpected path committed <path>` (the desk repairs it; you have no reset rule).

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (**L52** especially — it owns step 3) → **`docs/40 - DevDocs/prompts/2026-09-19/32-packet/facts.md`** (THE SPEC for what is REAL and what fix is owed; every row binds you) → on THIS branch: `src/cobalt/replay/runner.py` IN FULL, with attention to the module docstring lines 26–31 (the stated deadline design), `check_deadline`/`run_async` at `:292-310`, `movers_step`'s reconcile at `:370`, `cards_step`'s at `:399`, `formations_step` at `:404-430` (**the `if rows and not dry_run:` guard at `:425` is A1**), `formation_replay`'s docstring at `:180-196` (it defines `unavailable` as the P2-absent status) and the vault-write deadline check at `:440` → `src/cobalt/radar/pool.py:160-212` (`_ranked`, the `union` sort key at `:179-205`, its two `is not None` filters at `:189` and `:198`, and `position = union.index(ticker) + 1` at `:206`) — **READ ONLY, you never edit this file** → `tests/cobalt/test_replay_runner.py:345-372` (the two formation tests; `:371` asserts `"missed.reconcile:formation" not in calls`, which A1 says is the wrong assertion) and its `fake_deps` helper → `tests/cobalt/test_p4_migrations.py`: the module docstring at `:1-15` (STALE per A5 — it still says "P2's 0006/0007 are not in this tree"), `_seed_membership` at `:279`, and `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership` at `:293-328` (`@requires_db`, `base = [p for p in FORWARD if _migration_version(p) < 8]` at `:301`) → `tests/fixtures/replay/_cut_p4_fixtures.py` (`_anonymize` at `:154-155`, `_cast_row`, `cut_cards_day` at `:174+`) and the committed `tests/fixtures/replay/cards-day.real-shape.json` → the branch's own report `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` (the file you APPEND to). Laws, one line each: **L1** fail loud — no plausible-empty artifact, a missing input is a loud failure · **L3** one path — extend the suite's existing fakes, never build a second · **L29** Opus floor, this branch carries migrations · **L35** your claims are checked against your artifacts: every RED and every GREEN is quoted · **L45** tests run against the real command path with the suite's existing fakes — never invent Postgres behaviour where `requires_db` is the honest answer · **L46** one commit per item, tree clean before you stop · **L48** evidence lands in the report in the same turn · **L52** ranking is not yours to change — see step 3 · **L54** you work in this worktree only · **L60** partial work is wip-committed · **L67** the next check reads your diff, not your word.

UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no `cd … &&` in front, no pipe, no `>`/`2>` redirect, no `; echo` behind; `cd <path>` is its own call; outputs are read from the tool result and files are written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so NEVER typed: `cp`, `rm`, `touch`, `python`, `uv run python`, `claude -p`, `agy`, `grok`, `codex`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git merge`, `git rev-parse`, `git worktree`, any `COBALT_ENV=…` command, `uv run cobalt` anything except `jobs restarts`, `launchctl`, `docker`, `psql`.

**THE DB LINE, STATED ONCE AND BINDING EVERY STEP:** `cobalt_dev` is HELD by `ops-0919-db` (bg `e810326c`) until that session's own `OPS 0919 DB` stop line. You do not connect to it, migrate it, roll it back, or read it. Every `requires_db` test you write this round is WRITTEN AND SKIPPED — its first run is OWED to P4's later DB step, and you say so plainly by name in the report. **Every terminal line you ever write — `CONTINUE:`, `FAILED:`, or the stop line — ends with `| cobalt_dev: untouched`.**

PREFLIGHT (one row each in the report under `## ROUND 2 PREFLIGHT`: rule · command · exit · allowed/DENIED + the classifier's reason string verbatim):
- `date`
- `git status --porcelain` → EMPTY; anything else → `FAILED PREFLIGHT: worktree not clean — <lines> | cobalt_dev: untouched`
- `git status` → **the LONG form, quoted IN FULL in the report**; branch must be `sprint-2/p4`; "rebase in progress" or "You have unmerged paths" → `FAILED PREFLIGHT`
- `git log --oneline -3` → first launch: expect `c40f03c` on top (the R16 retry's report commit) with the CODE tip `2738cdc` beneath it. **If the top commit is anything else, that is not automatically a failure** — say which tip you are on, whether `2738cdc` is an ancestor (`git log --oneline -20`), and continue only if `sprint-2/p4` is the branch; otherwise `FAILED PREFLIGHT: unexpected tip — <what>`
- `ls -la .env` → MUST print "No such file". If `.env` EXISTS → `FAILED PREFLIGHT: .env present — this run must not see the dev database | cobalt_dev: untouched` (you have no `rm` rule; the desk removes it and relaunches you)
- `ls -la scratch` → record what is there; you never add any of it
- `git -C /Users/cobalt/cobalt log --oneline -1` → record it, no equality required
- `uv run pytest --co -q tests/cobalt/test_replay_runner.py tests/cobalt/test_p4_migrations.py` → collects cleanly, no code change has landed yet
- `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`) → **the BASELINE, quoted verbatim**; the R16 close was `1818 passed, 0 failed`. `failed` must be 0 here; if it is not, `FAILED PREFLIGHT: baseline red — <summary line> | cobalt_dev: untouched`

Any denial → `FAILED PREFLIGHT: <rules> | cobalt_dev: untouched`, commit the report, stop.

STANDING CONSTRAINT FOR EVERY STEP: `git diff main -- src/cobalt/radar/` must be **byte-identical to what it is at PREFLIGHT** — quote `git diff --stat main -- src/cobalt/radar/` at PREFLIGHT and again at close and show they match. You add NOT ONE LINE to `src/cobalt/radar/`. (The existing P4 diff under that path is the branch's own shipped work, including the A3 edit — it stays exactly as it is.)

## 1. A1 — the formation reconcile must run whenever P2's replay was AVAILABLE, even with zero rows (BLOCKER 1 of 2)

Read `facts.md` row A1, then `src/cobalt/replay/runner.py:404-430` in full and quote the guard line verbatim. Today `formations_step` ends:

```
if rows and not dry_run:
    result.reconcile["formation"] = deps.missed.reconcile(
        run_id=..., trade_date=trade_date, kind="formation", rows=rows)
    state["formation_rows"] = deps.missed.current(trade_date, "formation")
```

`movers_step` (`:370`) and `cards_step` (`:399`) reconcile unconditionally. `reconcile`'s own contract is "retire every predecessor this run"; plan STEP-1 **R2-1** requires that a subject whose miss disappears on rerun is retired. With the `rows and` guard, a rerun whose formation list is now EMPTY leaves yesterday's rows `is_current` forever. The ONLY case the guard legitimately covers is `formation_replay` returning `status="unavailable"` (P2 absent) with no rows — and the guard cannot tell that from "available, zero rows".

**THE FIX:** reconcile when the formation outcome was AVAILABLE and this is not a dry run — i.e. key the guard on `outcome.status` (the `unavailable` marker `formation_replay`'s docstring defines at `:180-196`), NOT on `rows` being non-empty. Read how `status` is actually spelled at the source before you write the condition; if `unavailable` is produced in more than one spelling or reachable in a shape your test cannot construct, STOP and `ESCALATE` rather than guessing. Change nothing else in the step; `result.formation_misses` and the `MISS formation …` print lines keep their current behaviour.

**TESTS FIRST** (each RED on the current tip, GREEN after — quote both, with the failure text):
- A new test in `tests/cobalt/test_replay_runner.py`, driving `run_nightly` through the existing `fake_deps` (extend it, do not build a second — L3): P2 present and COMPATIBLE, the day's formation list EMPTY, `dry_run=False` → assert `deps.missed.reconcile` WAS called for `kind="formation"` with zero rows, and that a predecessor row seeded as `is_current` in the fake is retired. This is the R2-1 case the plan names.
- A second test pinning the legitimate skip: P2 ABSENT (`status == "unavailable"`, no rows) → `missed.reconcile:formation` is NOT called. This must be GREEN both before and after; say so.
- **`test_an_open_radar_card_for_the_formations_subject_suppresses_it_in_the_run` (`:363-372`) asserts `"missed.reconcile:formation" not in calls` with an AVAILABLE P2 and everything suppressed. Under the fix that assertion becomes wrong**: an available run with zero surviving rows MUST reconcile. Update that one assertion to assert the reconcile IS called with zero rows, keep every other assertion in that test byte-identical (the counts tuple, the `formations: 0 not taken …` line), and quote the before/after of the changed line in the report. If you believe the old assertion was right and the plan wrong, do NOT change it — `ESCALATE` and stop at this step.
- A `requires_db` twin if and only if the suite's existing pattern has one for the sibling kinds; written, SKIPPED offline, first run OWED — name it.

Commit (explicit paths): `fix(s2-p4): the formation reconcile runs whenever P2's replay was available, so an empty rerun retires its predecessors (R2-1, tribunal A1)`. Last line `CONTINUE: step 2 | cobalt_dev: untouched`.

## 2. A2 — pin the deadline's ACTUAL scope and escalate the gap; change no behaviour

Read `facts.md` row A2. The hub's verdict: **REAL as a limitation, severity overstated** — `check_deadline` runs only between steps and immediately before the vault write, only `run_async` work is under `wait_for`, and `formation_source(...)`/`missed.reconcile(...)` are synchronous. The module docstring at `:26-31` states exactly that design, and spec R1-16's stated bar ("no late vault write", a measured margin) is met. **Bounding the synchronous work is a DESIGN CHANGE and is NOT yours this round.** Do not add a timeout around a DB commit; do not touch `run_async`.

**TESTS FIRST, then nothing else:**
- A test that pins what IS guaranteed: a run whose clock crosses the deadline during the synchronous part still performs `check_deadline("line (before the vault write)")` at `:440` and **raises `DeadlineExceeded` rather than writing the vault file** — assert the writer fake recorded NO write. If a test of exactly this shape already exists, say so with its name and line, add no duplicate (L3), and instead add the missing half if one is missing.
- A test pinning that a synchronous step is NOT cut mid-flight (the documented limitation, asserted as the current contract, with a comment naming it as such and pointing at the ESCALATE line below). If a fake cannot express this honestly, say so and skip this one rather than stretch it (L45).

Write an `## ROUND 2 ESCALATE` line, verbatim shape: `A2 (deadline scope): the execution deadline bounds only async work and the pre-vault-write check; formation_source() and missed.reconcile() run synchronously past it. Spec R1-16's stated bar is met (no late vault write, margin held) and runner.py:26-31 documents the design. Bounding synchronous DB work is a design change — NOT made this round. Desk/Dejan to rule before or after deploy.`

Commit: `test(s2-p4): pin the deadline's real scope — no late vault write, sync steps not cut (tribunal A2, design gap escalated)`. Last line `CONTINUE: step 3 | cobalt_dev: untouched`.

## 3. A3 — the ranking sort key: PIN IT, DO NOT TOUCH IT (BLOCKER 2 of 2, and it is Dejan's to rule)

Read `facts.md` row A3 and its "Desk's pre-read of A3" section, then read `src/cobalt/radar/pool.py:160-212` YOURSELF. **`src/cobalt/radar/pool.py` is READ-ONLY for this entire run. You do not revert the None-filter, you do not re-word it, you do not add a comment to it.** L52 puts ranking behind a tribunal and the disclosure behind Dejan's word; the hub recommended disclose-and-rule, and that ruling is not yours and not the desk's.

**ESTABLISH THE FACT, with quotes:** does the `is not None` filter change WHAT REACHES THE POOL or its ORDER, or does it only remove a `TypeError`? The desk's pre-read (in `facts.md`) says: both `max(...)` calls in the key tuple carry the filter (`:189`, `:198`); the pre-edit element 2 `-(max(<unfiltered>, default=0))` raised on ANY `None` in the generator, including the single-`None` case element 1 survives; therefore every input that did not raise had an all-non-None or empty generator, for which filtered and unfiltered `max` are equal and the key tuple is identical; and `union` feeds only `position = union.index(ticker) + 1` (`:206`), never the `candidates` set. **Confirm or contradict each of those three claims with a `file:line` quote in your report.**

- **If you confirm it (crash-removal only):** write the PINNING TEST — in the existing radar pool test module (find it; do not create a new one), a test that for candidates whose list metrics are ALL non-None the `_ranked` order and the returned rank map are EXACTLY what they are today (build the expectation from the code's documented ordering, not from a golden blob you cannot explain), plus a second case that a candidate with NO value on any list sorts after every candidate that has one, deterministically by name. Quote RED/GREEN honestly: a pure pinning test may be GREEN on first run — **if it is, say so and explain why** rather than forcing an artificial RED (there is no code change here to make it red).
- **If you contradict it — if the filter changes ranking or membership for any input that previously ran — STOP.** Write `ESCALATE: A3 — the None-filter DOES change ranking: <your quotes>`, make NO test asserting either order, wip-commit, and set the stop line's `A3:` field to `RANKING CHANGE — TRIBUNAL (L52)`. Do not proceed to step 4; the desk takes it from there.

Either way, write this `## ROUND 2 ESCALATE` line: `A3 (L52): the union sort key's None-filter in radar/pool.py:189,198 was shipped undisclosed in S2-P4 STEP-2. This chunk did not touch it. Effect per this round's read: <crash-removal only | RANKING CHANGE>. Dejan's ruling owed — waive-and-disclose or revert.`

Commit: `test(s2-p4): pin the radar pool union ordering unchanged for non-None keys (tribunal A3, pool.py untouched — L52)`. Last line `CONTINUE: step 4 | cobalt_dev: untouched`.

## 4. A4 — the fixture cutter's two missing transforms (the BLOCKER framing is NOT REAL; the drift is)

Read `facts.md` row A4. The hub verified the COMMITTED fixture `tests/fixtures/replay/cards-day.real-shape.json` and found **no `user_id` and no reason/note field exists in it to leak** — sizings keys `created_at, direction, entry, id, state, stop, ticker` (11 rows), transitions keys `at, card_id, from_state, to_state` (37 rows). **Nothing personal shipped.** The residual, which IS real: `_anonymize` (`tests/fixtures/replay/_cut_p4_fixtures.py:154-155`) is a hex-suffix scrub plus a date shift only, so spec-01:249's `user_id` → `1` and free-text reason → `"<reason>"` transforms are not implemented; the cutter stays correct only because today's raw reads happen to omit those columns.

**FIRST, RE-VERIFY THE HUB'S READ ON THE CURRENT TIP** and quote it: `grep -c user_id tests/fixtures/replay/cards-day.real-shape.json` and the same for the reason/note spellings; `wc -c` the fixture. **If any personal field IS present, that is a NEW finding — `ESCALATE` it loudly, name the field, and treat the whole item as blocking rather than cleanup.**

**TESTS FIRST:**
- A test asserting the committed fixture carries no `user_id` and no free-text reason/note key in either array — RED today only if the fixture is dirty (it should not be); if GREEN on first run, say so: it is a REGRESSION PIN, and that is its stated purpose.
- A unit test of `_anonymize` (or of the row transform you add) over a SYNTHETIC row that DOES carry `user_id` and a free-text reason — RED today (the transforms do not exist), GREEN after.

**THE FIX:** implement the two spec-01:249 transforms in the cutter so they apply if those columns ever appear — `user_id` → `1`, free-text reason → the literal `"<reason>"` — keeping the existing hex-suffix scrub and date shift byte-identical, and fix the docstring narrative Astra flagged. **DO NOT RE-RUN THE CUTTER**: it reads `scratch/` raw exports and its run rule is not in your allowlist. The committed fixture is NOT regenerated this round, and you say so in the report.

Commit: `test(fixtures): the P4 cards cutter implements spec §3's user_id and reason transforms; the committed fixture is pinned clean (tribunal A4 residual)`. Last line `CONTINUE: step 5 | cobalt_dev: untouched`.

## 5. A5 — the "both merge orders" migration test is vacuous after the rebase

Read `facts.md` row A5 and `tests/cobalt/test_p4_migrations.py:293-328` plus the module docstring at `:1-15`. Today `base = [p for p in FORWARD if _migration_version(p) < 8]` (`:301`) is applied in BOTH parametrizations; on the rebased tree `FORWARD` already contains P2's 0006/0007, so `_simulate_p2_card_columns` (`ADD COLUMN IF NOT EXISTS`) is a NO-OP in the `p4_before_p2` case and the two parametrizations are the same test twice. The module docstring still claims "P2's 0006/0007 are not in this tree" — stale. Separately, `_seed_membership` runs only AFTER the first `_apply([0008, 0009])`, so the digest equality does not prove what the docstring says it proves about PRE-EXISTING rows.

**NOTE THE OFFLINE BOUNDARY, STATED IN THE REPORT:** this test is `@requires_db`. You may EDIT it, but you may NOT RUN it — `cobalt_dev` is held. Its verification is OWED to P4's later DB step, and the stop line says so.

**TESTS FIRST** — the RED/GREEN you CAN prove offline:
- A new OFFLINE test asserting the vacuity is gone at the level the registry can show: that `FORWARD` on this tree DOES contain 0006/0007 (so the docstring's premise is false), and that the two parametrizations of the merge-order test now construct DIFFERENT base lists (assert against the helper you extract, not against a DB). RED today (the helper/behaviour does not exist), GREEN after.
- Then fix the `@requires_db` test itself so `p4_before_p2` genuinely installs 0008/0009 BEFORE P2's card columns — exclude 0006/0007 from `base` in that parametrization and apply them in the P2 position — and move `_seed_membership` so the populated-membership claim is about rows that exist BEFORE the first P4 apply. Every existing assertion stays; nothing is deleted.
- Rewrite the stale module docstring to say what is true on the rebased tree.

Commit: `test(s2-p4): the both-merge-orders migration test is no longer the same case twice; stale docstring corrected (tribunal A5)`. Last line `CONTINUE: step 6 | cobalt_dev: untouched`.

## 6. Close (OFFLINE) — re-run the suite, append the report, stop

In order, each quoted verbatim in the report:
- `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`) → the summary line. Compare against YOUR PREFLIGHT BASELINE (expected `1818 passed, 0 failed`): **`failed` stays 0**; `passed` grows by this round's new tests (LIST THEM BY NAME, per step); `skipped` grows only by this round's new `requires_db` tests (LIST THEM BY NAME). Any other movement → `ESCALATE` with the numbers.
- `uv run cobalt jobs restarts 2738cdc..HEAD` → the table VERBATIM (branch point = the R16 CODE tip `2738cdc`, not the report commit). Expect only `src/cobalt/replay/*`, `tests/cobalt/*`, `tests/fixtures/replay/*`, `docs/40 - DevDocs/*` among the changed paths; **0 UNCLASSIFIED**, or `ESCALATE` why.
- `git diff --stat main -- src/cobalt/radar/` → byte-identical to the PREFLIGHT capture; quote both and state they match. `src/cobalt/radar/pool.py` appears in your own `git diff --stat 2738cdc HEAD` **zero times** — prove it.
- `git diff --stat 2738cdc HEAD -- src tests docs` → every path is one this prompt named.
- `git status --porcelain` → empty. `ls -la .env` → "No such file" (quote it; the file is gitignored, so `git status` can never prove its absence).
- APPEND a new top-level `## ROUND 2 (tribunal fold)` section to `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` on THIS branch, AFTER its existing last line — never a rewrite of what is there. It contains: §0 headline ≤5 lines (what changed, status, ESCALATE count); `## ROUND 2 PREFLIGHT`; one subsection per step 1–5 with its RED/GREEN quotes, its `facts.md` row confirmation-or-contradiction, and its commit sha; the CLOSE numbers above; and `## ROUND 2 ESCALATE` carrying the A2 line, the A3 line, **the still-OWED first run of every `requires_db` test this branch carries (now including this round's, by name)**, the fact that the cards fixture was NOT regenerated, the rows carried but not fixed (A6, A7, A8), and anything else this round found. Commit by explicit path: `docs(report): s2-p4 round-2 tribunal fold — <one line>`.

LAST LINE, exactly this shape:

`P4 ROUND2 <sha> | offline <pass>/<fail> | A1 A2 A4 A5: <FIXED|…> | A3: <PINNED, pool.py untouched — L52 ruling owed|RANKING CHANGE — TRIBUNAL (L52)|…> | RESTARTS: <the restarts line> | OWED requires_db: <n> | ESCALATE: <n> | cobalt_dev: untouched`

or `FAILED: <step> — <reason> | cobalt_dev: untouched`.

Never merge, never rebase, never push, never touch `~/cobalt`'s working tree, never run anything against production or `cobalt_dev`.
