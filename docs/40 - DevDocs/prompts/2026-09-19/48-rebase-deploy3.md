STATUS: DEPLOY 3's BUILD LANE (R32 15:0x ET — Dejan overruled L43's one-deploy-per-evening for tonight: the archiver + ops ship as a SECOND production deploy tonight IF this lane is green before 21:00 ET, else Sunday). You do NOT deploy anything. You rebase two branches onto the post-P4 `main`, resolve the ONE predictable conflict set, and prove both suites. LAUNCHABLE ONLY after `main` carries P4 (verified in §1) and with Dejan's approval row in `docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4` naming THIS file, committed.

MODEL: Opus 5 (`claude-opus-5`) — Opus floor: you resolve a MIGRATION REGISTRY conflict and run a with-DB suite (L29) · SEAT: build hub `rebase-d3-0919`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt-wt/archiver-append` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/48-rebase-deploy3.md' and follow it exactly." --model claude-opus-5 --remote-control rebase-d3-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rebase main)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(git rebase --abort)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session; never `bypassPermissions`; **NO production command of any kind, NO `--allow-prod` in any spelling, NO push, NO merge into `~/cobalt`, NO `launchctl`, no vault write** · METER: Anthropic small · nobody sits at this terminal — your channel is the report file.

# REBASE `ops/2026-09-19` AND `archiver/append-0919` ONTO THE POST-P4 `main`, RESOLVE THE REGISTRY SEAM, PROVE BOTH SUITES
WHY THIS EXISTS, precisely: deploy 2's L68 gate (run 1, `reports/deploy-p4-2026-09-19.md` §1.2) merged `sprint-2/p4` CLEAN and `ops/2026-09-19` CLEAN onto `main`, then CONFLICTED on `archiver/append-0919` in **9 files** — five of them named in advance. The cause is not a defect: `archiver/append-0919` was cut from a pre-P4 `main`, its migrations are **0010/0011** and it deliberately left the 0008/0009 gap that P4 fills, but both branches rewrote the same tuple lines and the same docstrings, so git cannot text-merge them. Dejan then ruled (R31) that deploy 2's gate would not stack the archiver at all, and P4 shipped alone. **This job makes the archiver branch mergeable again.** DO NOT STOP until the report ends `REBASE D3 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. THE ROW: `cto-2026-09-19.md` `## §4`, the row that names THIS file (`48-rebase-deploy3.md`) and its rule strings; verify it is committed (`git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`). No committed row naming THIS file → `FAILED: authorization mismatch`, stop before anything. STANDING ROWS: `cto-2026-09-19.md` §4 **R25** (the REVOKE, DB-1), **R26** (the dropped twin, DB-2), **R31** (the narrowed deploy-2 gate), **R32** (L43 overruled tonight); `cto-2026-09-18.md` §4 **R18–R21** (L67). Laws that bind you: L1, L3, L4, L28, L29, L35, L45, L46, L48, L54, L57, L60, L62, L63, L67, L68, L69, L70.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, commit the report, stop. ONE ASYMMETRY: while `.env` sits in a worktree, "stop" means removing it and proving it gone FIRST (L4), then writing the stop line.

INDEX CARD (read in this order, before step 1): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you run a write path on two branches) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-p4-2026-09-19.md` **§1.2 of the FIRST RUN in full** (the verbatim conflict output and the desk's read of the registry seam — this is your map) → `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` §0 + its last line → `/Users/cobalt/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md` last line → `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-folds-0919/REVIEW.md` §0 (the houses' read of the archiver's last code).
REPORT (L48): `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md` — a NEW file. §0 Headline ≤5 lines → PREFLIGHT → OPS REBASE → ARCHIVER REBASE (a section per conflicted file) → SUITES → ESCALATE → last line. Commit it in the archiver worktree by explicit name.

UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no pipe, no redirect, no `; echo`, no `$(…)`, no `&&`. `cd <path>` is its OWN call. You are launched in `/Users/cobalt/cobalt-wt/archiver-append`; the only other cwd you may enter is `/Users/cobalt/cobalt-wt/ops-2026-09-19`. **NEVER `cd /Users/cobalt/cobalt`** — every look at production's tree is `git -C /Users/cobalt/cobalt <read>`. Before every pytest call the immediately preceding call is `cd <the worktree>` then `ls -la .env` (it must print the file only during the with-DB step, and say "No such file" everywhere else). A MID-RUN DENIAL = THE RUN FAILED (L62): `.env` cleanup if present, then `FAILED: <step> — <command> — <reason>`, commit, stop. Never a retry in another spelling.

## 1. PREFLIGHT — and the P4 gate
1.1 `date`. `git -C /Users/cobalt/cobalt log --oneline -3` and `git -C /Users/cobalt/cobalt status --porcelain`. **P4 MUST BE ON MAIN:** `git -C /Users/cobalt/cobalt log --oneline -40` must contain the `sprint-2/p4` commits, and `git -C /Users/cobalt/cobalt show --stat deploy-2026-09-19b` must resolve. Absent → `FAILED: gate — P4 is not on main yet; this lane runs after deploy 2` (nothing has been touched).
1.2 `git status --porcelain` in BOTH worktrees → empty. Dirty → `FAILED: gate — <worktree> not clean`. Record both tips (`git log --oneline -1` in each) and `git -C /Users/cobalt/cobalt rev-parse --short HEAD` as `<main>`.
1.3 `ls -la .env` in both worktrees → "No such file" in both (another lane holding `cobalt_dev` would poison the with-DB step). A hit → `FAILED: gate — .env present in <path>`.
1.4 `GATE: proven` line, report written and committed. Last line `CONTINUE: step 2`.

## 2. OPS REBASE — expected clean
2.1 `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` → `git rebase main`. Deploy 2's gate merged this branch onto a tree already carrying P4 with NO conflict, so a conflict here is NEW: `git rebase --abort`, then `FAILED: ops rebase conflict — <paths>` (nothing else has been touched).
2.2 PROOF: `git log --oneline -1` (new tip), `git rev-list --count main..HEAD` (the same commit count as before the rebase — record both numbers), `git status --porcelain` empty.
2.3 `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` → `ls -la .env` ("No such file") → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 failed`**; quote the summary line. Red → `FAILED: ops offline suite red — <tests>`.
2.4 Report section committed. Last line `CONTINUE: step 3`.

## 3. ARCHIVER REBASE — the seam, resolved by RULE, never by taste
3.1 `cd /Users/cobalt/cobalt-wt/archiver-append` → `git rebase main`. EXPECT it to stop on conflicts. For EVERY conflicted commit: read BOTH sides in full before you touch anything (`git diff`, `git show :2:<path>`, `git show :3:<path>`), then resolve by the rules below, `git add <path>` BY NAME, and `GIT_EDITOR=true git rebase --continue`. **You never `git rebase --skip`** (it is not in your allowlist and it silently drops a commit).
3.2 **THE RESOLUTION RULES — a conflict that does not fit one of them is a STOP, not a judgement call.**
  (a) `src/cobalt/db_migrations/__init__.py` — the `FORWARD` tuple must end up listing **0001…0011 in numeric order** and `REVERSE` must be its exact mirror. P4 contributes 0008/0009, the archiver contributes 0010/0011, and the archiver deliberately left that gap: the resolution is the UNION in numeric order, never a choice between sides. Any entry present on one side and absent from your result → STOP.
  (b) `src/cobalt/db_migrations/placement.py`, `src/cobalt/cli.py` — UNION of both sides' added entries/registrations, each kept verbatim, ordering as the surrounding file already orders things. A line that exists on both sides in DIFFERENT wording is a STOP.
  (c) `src/cobalt/archiver/runner.py`, `src/cobalt/archiver/store.py` — these are the archiver's own modules; where `main` (post-P4) changed them and the branch also changed them, keep BOTH changes if they touch different code, and STOP if they touch the same statement.
  (d) `tests/cobalt/test_radar_migration.py`, `tests/cobalt/test_radar_score_migration.py`, `tests/cobalt/test_tenancy.py` — UNION of both sides' tests and of any shared table/expectation list (these are the files that enumerate migrations and table placements, so they move for the same reason as (a)). **Never delete a test to resolve a conflict** (L45, L3); a test that genuinely cannot hold both sides is a STOP.
  (e) `docs/40 - DevDocs/cobalt/archiver/runner.md` — prose; UNION, both sides' paragraphs kept, agent-authored (CLAUDE.md: DevDocs prose is never generated).
  A STOP in any of these: `git rebase --abort`, then `FAILED: archiver rebase — <path> — <the two sides quoted>`; the branch is back where it started.
3.3 WHEN THE REBASE FINISHES: `git status --porcelain` empty; `git log --oneline -12`; `git rev-list --count main..HEAD` — **the same commit count as 1.2 recorded** (a rebase that dropped a commit is a STOP: `FAILED: archiver rebase dropped a commit — <before> vs <after>`). Then `grep -n "0008\|0009\|0010\|0011" src/cobalt/db_migrations/__init__.py` → quote it: `FORWARD` = 0001…0011 in order, `REVERSE` its mirror. Anything else → `FAILED: archiver rebase — registry wrong after resolution — <what you read>`.
3.4 **WRITE DOWN WHAT YOU RESOLVED**, one section per conflicted file: what each side had, what you kept, and the rule (a)–(e) you applied. The houses read exactly this section next, so a resolution you cannot state in two sentences is one you should have stopped on.
3.5 Report committed. Last line `CONTINUE: step 4`.

## 4. SUITES ON THE REBASED ARCHIVER BRANCH
4.1 OFFLINE: `cd /Users/cobalt/cobalt-wt/archiver-append` → `ls -la .env` ("No such file") → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE **`0 failed`**; quote the summary. For context only, never as the bar: before the rebase this branch closed `1871/0`. Red → name each test and each assertion, `FAILED: archiver offline suite red on the rebase — <tests>`.
4.2 WITH-DB (`cobalt_dev` ONLY — never production, never `--allow-prod`), in this exact order:
  (a) `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env` (by name, never read, never printed, L4) → `ls -la .env` prints it.
  (b) BASELINE `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote the table. EXPECT `cobalt_dev` at **0001–0009** with `archive_progress`/`archive_incidents` ABSENT. A `CHANGED` verdict → go to (g), ending `FAILED: gate — cobalt_dev proof CHANGED before the suite — <line>`.
  (c) FORWARD `COBALT_ENV=dev uv run cobalt db migrate` → quote the full table. EXPECT `archive_progress` and `archive_incidents` **CREATED** (0010/0011), every pre-existing table `OK`, no `CHANGED`. Anything else → (g), ending `FAILED: gate — dev forward migrate <what>`.
  (d) SUITE `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE **`0 failed`**; quote the summary. Context only: the branch's own DB close was `161/0` scoped, the P4+ops stack ran `2198/0`. Red → (g), ending `FAILED: gate — with-DB suite red — <tests + assertions verbatim>`. **Never label a red "known" and continue** (L68, L70).
  (e) **POST-SUITE PROOF** `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote it. **The suite writes to `cobalt_dev` by design**, so rows and digests legitimately move between (b) and here (deploy 2 run 2 false-stopped on exactly this — `cobalt_redactions` 127 → 128); record every difference table by table, and it is NOT a stop. A table that VANISHED, or one no stacked migration creates, IS: → (g), ending `FAILED: gate — cobalt_dev table set changed during the suite — <what>`.
  (f) RESTORE `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → EXPECT exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` applied and **nothing else**; anything numbered ≤ 0009 reversed → quote it, (g), `FAILED: gate — dev rollback went below 0009 — <what>`. Then `COBALT_ENV=dev uv run cobalt db migrate --proof-only` and compare **against (e), not against (b)**: the two archiver tables ABSENT again and every other table identical to (e). A digest that moved ACROSS the restore is a STOP → (g), `FAILED: gate — cobalt_dev digest moved across the restore on <table>: <before> → <after>`.
  (g) `rm /Users/cobalt/cobalt-wt/archiver-append/.env` → `ls -la .env` says "No such file". **This happens before ANY stop line, including every red branch above**; a stop line written while `.env` is on disk is itself a failure.
4.3 `uv run cobalt jobs restarts <main>..HEAD` → record VERBATIM, 0 UNCLASSIFIED (L42); an UNCLASSIFIED path is named in ESCALATE, never dropped.
4.4 Report committed by name. Last line, exactly this shape:
`REBASE D3 <archiver tip> | ops <ops tip> | conflicts resolved: <n> files | offline <p>/<f> (ops) , <p>/<f> (archiver) | db <p>/<f> | registry 0001–0011 | cobalt_dev: <state you proved> | .env: removed, proven gone | RESTARTS: <line> | ESCALATE: <n>`
or `FAILED: <step> — <reason> — cobalt_dev: <state> — .env: <state>`.
