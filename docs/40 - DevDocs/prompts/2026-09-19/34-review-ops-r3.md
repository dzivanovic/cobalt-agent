MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-ops-r3-0919`, launched by the CTO desk in the background, SAME worktree as round 1 (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `22-review-ops-r2.md`'s launch line, with exactly ONE string changed — the `mkdir` path, from `scratch/review-ops-r2-0919` to `scratch/review-ops-r3-0919` — because this round needs no rule `22` did not already carry (same repository, same branch, same `git -C /Users/cobalt/cobalt show*`/`log*` reach; the unused `git -C .../s2-p2-cards *` rules stay, unused, for the same reason they stayed in `22`). Proof: sorted `comm -3` between `22`'s rule-string list and this prompt's rule-string list yields exactly one line on each side, both the `mkdir` string. If this round turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/34-review-ops-r3.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-ops-r3-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-ops-r3-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of branch `ops/2026-09-19`, ROUND 3 of ≤3 (LAWS L67, the LAST round on this fold) — verifying ONLY the code the DB run added on top of what round 2 reviewed. Round 2 (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-r2-0919/REVIEW.md`) reviewed the fold up to its own report tip `d860bf7` and ended `OPS CHECK R2 · grok: 0/0/1 FOLD CLEAN · gemini: 0/1/1 FOLD UNSAFE (its per-item lines contradict its own token, hub-verified NOT REAL) · astra: 1/0/1 FIX FIRST R2 · hub-verified REAL: 4 (F1 MINOR residual, F3/F4/F5 MINOR disclosed trade-offs; none blocked shipping) · R2-R9 all FIXED`. A DB-run session on `cobalt_dev` then (1) nested F1's residual cleanup, (2) settled carried items R1 and R11 by experiment — `SET LOCAL lock_timeout` does NOT take the REPEATABLE READ snapshot, so it now runs ahead of the BEFORE probe, backed by three new experiment tests and one new probe-coverage test — and (3) fixed a test-side defect (test (j) probed through an autocommit connection). Item (2) is a CODE change no house has read; L67 says the changed code is read again, this fold only, not the whole branch. **The houses read ONLY THE FOLD** — the pre-computed diff of code+tests since round 2's reviewed tip, plus round 2's own verdict table and the report's own DB-RUN section — not the whole branch again. DO NOT STOP until the report ends `OPS CHECK R3 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Same standing law as rounds 1 and 2: LAWS.md L67, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, R19-R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The fold's own debt: the DB-run report's own stop line ends `OWED: three-house check on this diff, next deploy prompt` — prove with `git -C /Users/cobalt/cobalt log --oneline -4 ops/2026-09-19 -- "docs/40 - DevDocs/reports/ops-2026-09-19.md"` (LESSON, carried from `18`/`22`: the same command with NO branch named returns EMPTY on `/Users/cobalt/cobalt` — the report is committed only on the branch; name the ref explicitly or the check reads as a false negative). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the fold's own report's claims (`facts.md` in the packet is the hub's own claim too — verify it against the diff, don't just relay it) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on — Astra's Codex meter is expected OUT until ~16:54 ET today; probe it anyway, and if it reports out, `astra: METER` is a NORMAL ending, not a retry · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out — this round runs on Grok + Gemini with Astra probed-and-recorded; 2 houses reading satisfies the floor.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-ops-r3-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. SECOND LESSON (carried from `18`/`22`): the Write tool strips trailing whitespace from copied diffs — a unified diff's blank context lines are a single space, Write turns them into empty lines. `facts.md` in this packet already counts how many such lines `fold.diff` has (15); when you copy it into `scratch/review-ops-r3-0919/`, re-run the same `grep -c "^ $"` check on your copy and note the byte delta in `## Packet` as a fidelity note, not a defect. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-r3-0919/REVIEW.md` — same gitignored-scratch pattern, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/34-packet/`; copy each into `scratch/review-ops-r3-0919/` (`mkdir -p` first; Read → Write, byte-identical where the tool allows — see SECOND LESSON above)
(1) `fold.diff` = `34-packet/fold.diff` — `git -C /Users/cobalt/cobalt diff d860bf7..ops/2026-09-19 -- src tests`, the hub's pre-computed diff of everything that changed under `src/` and `tests/` since round 2's own reviewed tip (this is the R1/R11 `SET LOCAL` move, F1's nesting, and the (j) test fix; it does not include the report file, `configs/`, or `ops/`, which the range does not touch at all — worth confirming, not assuming) · (2) `verdicts-r2.md` = `34-packet/verdicts-r2.md`, verbatim copy of round 2's own `## Verdict table` (F1 MINOR residual, F2 NOT REAL, F3/F4/F5 MINOR disclosed trade-offs, R2-R9 all FIXED) — the SPEC for what F1's fix must satisfy, not a claim to re-litigate unless the code disagrees · (3) `db-run-section.md` = `34-packet/db-run-section.md`, the ops report's own `## DB RUN …` section verbatim (headline through close and stop line) — the hub's own account of this work; a CLAIM to spot-check against `fold.diff`, same standing as any build report · (4) `facts.md` = `34-packet/facts.md`, the hub's own pre-computed measurements: commit list, diff-stat, the four new test names, the scope check, and which of the report's claims (the coverage test's red-first run, the RESTARTS command) cannot be independently confirmed from a diff alone — a CLAIM to spot-check, never settled fact on its own · (5) `QUESTIONS.md`, verbatim below. If a file is too large for one Write call, split `name.part1`, `name.part2` and say so in `## Packet`.

QUESTIONS.md (verbatim): see `34-packet/QUESTIONS.md` — copy it into `scratch/review-ops-r3-0919/QUESTIONS.md` byte-identical (Read → Write) and hand it to each house exactly as staged; do not retype it.

## 2-4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-ops-r3-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. Houses this round: Grok + Gemini as full reviewers; Astra is probed exactly as L47 requires, and if the probe shows the meter out, record `astra: METER` and move on — never retry, never treat it as a denial or a FAILED step. You verify each BLOCKER and MAJOR against the REAL files: `fold.diff` against `git -C /Users/cobalt/cobalt show ops/2026-09-19:<path>` (the branch's tip content — reachable under the SAME `show*` rule, no worktree `cd` needed) and against the real files under `/Users/cobalt/cobalt-wt/ops-2026-09-19/` with Read and `grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (for example, re-running `uv run pytest`/`uv run cobalt jobs restarts` to independently re-derive the RESTARTS claim, or re-running the coverage test's red-before-the-move) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-ops-r3-0919/`. Last line: `OPS CHECK R3 · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>` or `FAILED: …`. Then stop.
