You are an independent reviewer from another house, one of two reading this packet separately (a third house, Astra, is probed once and may not answer — do not assume a third read exists). Read ONLY the files in this folder; you cannot run commands — everything you need is already here as a file. This packet checks TWO folds onto branch `sprint-2/p4` since the whole-build check `REVIEW-C.md` (`a59ee8c`) that all three houses already read: (1) the fund rule (Dejan's ruling R16), commits `d5947e7..2738cdc`, in `fund-rule.diff`; (2) round 2 of the tribunal fixes (A1/A3/A4/A5), commits `2738cdc..9045260`, in `round2.diff` plus the full prose section `round2-report-section.md`. `verdicts.md` is REVIEW-C's own verdict table (A1–A10) — the SPEC for what each fold claims to fix, not a claim to re-litigate unless a diff disagrees with it. `rulings.md` quotes Dejan's R16 and R23 rulings verbatim — the two rulings these folds must match. `facts.md` is the hub's own pre-computed measurements (diff-stats, counts, RESTARTS, stop lines, the two OWED `requires_db` test names) — treat it as a claim to spot-check against the diffs, same as you would a build report's own claims, not as settled truth. Two files are deliberately excluded from `fund-rule.diff` (raw Finviz export rows carrying real tickers, L32) — `facts.md` names them and says why; if a question below genuinely needs that row's content, answer `UNVERIFIABLE FROM PACKET — raw export row excluded, L32` rather than asking for it.

For each finding give SEVERITY (BLOCKER = the fix does not do what it claims, breaks an existing invariant, or contradicts a LAW (L1 fail-loud, L3 one-path, L45 real-shape fixtures, L52 nothing here may touch scoring/ranking/cards, L32 no raw exports/screen tickers) / MAJOR / MINOR), FILE + LINE (from the diff), the EVIDENCE you read (quote it), and the FAILING SCENARIO; say "none found" rather than inventing.

(Q1) A1's fix, `round2.diff`'s change to `src/cobalt/replay/runner.py`. Is the guard now keyed on the formation outcome's STATUS (`outcome.status != FORMATION_UNAVAILABLE`), not on the row count? Is `unavailable` spelled exactly the way the constant is defined and the way `formation_replay`/`formation_source` actually produce it (check `FORMATION_UNAVAILABLE = "unavailable"` if it appears in the diff context, or take `facts.md`'s claim and flag if the diff can't confirm the spelling)? Does the diff leave the P2-absent case (no rows, status `unavailable`) as the ONLY case that still skips the reconcile?

(Q2) Do the new tests in `round2.diff` (A1's two new `test_replay_runner.py` tests, A3's two new `test_radar_pool.py` tests, A4's three new `test_p4_asset_type_evidence.py` tests, A5's two new `test_p4_migrations.py` tests) each assert something that is FALSE against the pre-fix code and TRUE against the post-fix code — i.e., would each fail if its corresponding fix were reverted? Say which, if any, look like they'd pass either way (a vacuous pin).

(Q3) Read the A3 pinning tests in `round2.diff` (`test_radar_pool.py`) and their surrounding comment. Is this a TRUE order pin — does it assert today's `union` sort order and ranks for inputs that already ran before the fix — or does it (intentionally or not) also settle the ONE disputed input (a name with a value on one list and `None` on another) that `rulings.md`'s R23 note says was deliberately left unasserted? If it does assert that disputed input either way, say so plainly — that would settle Dejan's L52 ruling from inside the suite, which the comment in the diff says it does not do.

(Q4) Read `fund-rule.diff`. Does the shipped rule (`is_not_equity` in `cobalt.radar.config`, wired into `radar/runner.py` and the movers benchmark via `not_equity_verdicts`) do EXACTLY R16's "C" (`rulings.md`) — drop a row when `Asset Type` is non-blank OR `Industry` = `Exchange Traded Fund` — and nothing more (no additional condition, no additional dropped class)? Based on the diff and `facts.md`'s counts (5,702 fund tickers, 8 blank-`Asset Type` funds, 131,348 dropped, 0 stock rows hit), is there any real stock row visible in the diff's test fixtures/assertions that this rule's logic COULD drop under a plausible input it doesn't test for (e.g., a stock whose `Industry` cell happens to read "Exchange Traded Fund" by data error, or a blank `Asset Type` cell on a real stock)? `facts.md` explains two CSV files with real export rows are excluded — do not ask for them; answer from the evaluator's LOGIC and the counts given.

(Q5) A5's restructured migration test. Read `round2.diff`'s changes to `test_p4_migrations.py`: do `_merge_order_bases(order)` and `_membership_insert(...)` do what `round2-report-section.md`'s "THE FIX" section (A5) claims — do the two `@requires_db` parametrizations (`p2_before_p4`, `p4_before_p2`) now build genuinely DIFFERENT base migration lists (not the same list twice, which was the original defect)? Is every pre-existing assertion in that test kept (the report claims none were deleted or weakened) — spot-check against the diff.

(Q6) The RESTARTS claims in `facts.md`: fold 1 = `com.cobalt.aset com.cobalt.radar`, fold 2 = `com.cobalt.radar` only. Does this match what each diff actually touches (fold 1 touches `configs/cobalt/radar.yaml`-shape code + `radar/config.py`/`radar/runner.py`/`replay/*`; fold 2 touches only `src/cobalt/replay/runner.py`)? Is there anything in either diff that should have triggered a DIFFERENT resident restart that `facts.md` doesn't name?

(Q7) Did either fold touch, reopen, or silently contradict anything `verdicts.md` (REVIEW-C) had already marked CLEARED or out of scope — specifically A2 (PINNED, no behaviour change — confirm `round2.diff` changes no runtime behaviour for A2, only adds tests) and A6/A7/A8/A9/A10 (all explicitly carried, untouched)? Flag anything that looks like scope creep beyond A1, A3, A4, A5, and A2's disclosure.

(Q8) Does `round2.diff` support R23's ruling and its correction (`rulings.md`'s note on R2-2)? Confirm from the diff itself: is `src/cobalt/radar/pool.py` genuinely absent from `round2.diff` (untouched this round)? Does the round-2 report's own correction — "one added filter, in element 1, at `pool.py:189`," not two — read as consistent with what you can independently see of the fund-rule fold's earlier pool.py change (if `fund-rule.diff` shows any radar/pool.py touch — it should show none, since the None-filter predates this branch's chunk boundary per the report)?

(Q9) Any secret, credential, connection string, `.env` content, settings-yaml value, or raw screen/export data (beyond the two named-and-excluded CSV rows) visible in EITHER diff? Does either diff touch anything outside `src/` and `tests/` (no `configs/`, no `ops/`, no `scratch/`)? Does either diff introduce a second copy of a rule/helper that already exists elsewhere (L3)?

End with exactly one line per question, in this exact order: `Q1: REAL / NOT REAL / UNVERIFIABLE`, `Q2: …`, `Q3: …`, `Q4: …`, `Q5: …`, `Q6: …`, `Q7: …`, `Q8: …`, `Q9: …` (where REAL means "a genuine problem/gap found," NOT REAL means "the claim holds, no problem," UNVERIFIABLE means the packet cannot answer it). Then, on its own final line: `VERDICT: FOLD CLEAN / FIX FIRST <question numbers> / FOLD UNSAFE`.
