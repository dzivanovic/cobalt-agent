Hub's own pre-computed measurements for both folds — a CLAIM to spot-check against the diffs in this packet, same standing as any build report's own claims (L35): never settled fact on its own.

## Fold 1 — fund rule (R16), commits `d5947e7..2738cdc`

- Branch `sprint-2/p4`, worktree `/Users/cobalt/cobalt-wt/s2-p4`.
- `fund-rule.diff` in this packet = `git diff d5947e7..2738cdc -- src tests`, WITH TWO FILES EXCLUDED: `tests/fixtures/replay/movers-gainers.real-shape.csv` and `tests/fixtures/replay/movers-losers.real-shape.csv` (each +1 row this range — a raw Finviz export row with real ticker/financial data, excluded from this packet under L32; see `## Excluded from this packet` below). Everything else under `src/` and `tests/` in that range is included, byte-identical to `git diff`.
- Full (unscrubbed) diff-stat for the range, `src tests` only, 17 files, 920 insertions(+) / 140 deletions(-). The two excluded CSVs are 1 line each (+1/+0), already counted in that stat; nothing else was removed.
- Config shape change: `not_equity: {header, values}` → `{asset_type_header, industry_header, industry_values}` in `configs/cobalt/radar.yaml`, `extra="forbid"` on the old shape.
- Rule implemented: `is_not_equity(row, config)` = `asset_type` non-blank OR `industry` in `industry_values` (one evaluator, `cobalt.radar.config`), called from `radar/runner.py` and (via `not_equity_verdicts`/`mover_is_not_equity`) from the movers benchmark at ingest — never through `movers_daily`.
- Counts (hub re-ran the evidence cutter after the fix): 643,998 rows / 16,307 files; 5,702 distinct fund tickers; 28 distinct `Asset Type` values; table C (rows this rule newly drops that the old rule didn't) = 16, all fund rows (8 distinct tickers, blank `Asset Type`, caught by `Industry`); **dropped under the new rule: 131,348; stock rows hit: 0.**
- Offline suite after the fix: `1818 passed, 324 skipped, 1 xfailed, 15 warnings` (0 failed; was 1796 before this chunk, +22 new tests). DB suite (`COBALT_ENV=dev`, run then reverted): `2136 passed, 6 skipped, 1 xfailed` (0 failed; was 2114 before). `cobalt_dev` unchanged at `0001–0009` after — no migration ran.
- RESTARTS this chunk: `com.cobalt.aset com.cobalt.radar` (config-shape change, static import reach).
- Stop line, verbatim: `P4 FUND RULE 2738cdc | offline 1818/0 | db 2136/0 | dropped 131348 kept n/a | stock rows hit 0 | RESTARTS: com.cobalt.aset com.cobalt.radar`
- Escalates opened by this chunk (18–21, none in this packet's question scope except as background): #18 four test files outside the chunk's own file bound edited (accepted by the hub, revert on request) — fixture-value-only changes (`Asset Type: "Stock"` → blank, a value Finviz never returns); #19 L32 — real tickers pinned in the rewritten/new test files as row-lookup keys (31/7/3 occurrences across three files) — an OPEN item Dejan has not yet ruled; not new to this fold-check packet, and not reproduced here beyond what the diff itself already carries in `.py` test code (no raw export rows are in this packet — see exclusion above).

## Fold 2 — round 2 tribunal fixes, commits `2738cdc..9045260`

- `round2.diff` in this packet = `git diff 2738cdc..9045260 -- src tests`, complete, nothing excluded (checked: no `.csv`/raw-export content in range).
- `radar/` untouched in this range: `git diff --stat 2738cdc..9045260 -- src/cobalt/radar/` returns nothing.
- Files touched (diff-stat): `src/cobalt/replay/runner.py` (+9/−0 net lines shown, one guard line changed), `tests/cobalt/test_p4_asset_type_evidence.py`, `tests/cobalt/test_p4_migrations.py`, `tests/cobalt/test_radar_pool.py`, `tests/cobalt/test_replay_runner.py`, `tests/fixtures/replay/_cut_p4_fixtures.py`, plus the report file itself (excluded from the diff files in this packet — see `round2-report-section.md` instead, the relevant prose section in full).
- Offline suite after: `1829 passed, 324 skipped, 1 xfailed` (0 failed; was 1818 before this round, +11 named new tests). Skips unchanged at 324 — this round added no `@requires_db` test.
- RESTARTS this round: `com.cobalt.radar` only (`src/cobalt/replay/runner.py`, static import reach) — already inside the branch's standing restart line, adds nothing new to the deploy.
- Stop line, verbatim: `P4 ROUND2 9045260 | offline 1829/0 | A1 A2 A4 A5: FIXED (A2 PINNED — design gap escalated, no behaviour changed) | A3: PINNED, pool.py untouched — L52 ruling owed | RESTARTS: com.cobalt.radar | OWED requires_db: 2 | ESCALATE: 5 | cobalt_dev: untouched`
- The 5 ESCALATE lines this round (full text in `round2-report-section.md`'s `## ROUND 2 ESCALATE`): A2 (deadline scope — desk/Dejan to rule), A3 (L52 — Dejan's ruling now given, R23 above), R2-1 (a line-number drift, MINOR), R2-2 (the desk's A3 pre-read named the wrong half of the sort key — correction above), R2-3 (no `requires_db` twin written for A1, with reason given).
- The 2 OWED `requires_db` tests (edited this round, NOT run — `cobalt_dev` was held by another session the whole round): `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p2_before_p4]` and `test_0008_0009_apply_twice_reverse_and_reapply_on_populated_membership[p4_before_p2]`, both in `tests/cobalt/test_p4_migrations.py`.
- The A3 pinning tests (`tests/cobalt/test_radar_pool.py`) deliberately do NOT exercise the one input whose behaviour the None-filter changed (a name with a value on one list and `None` on another) — both tests pass under the filtered AND the unfiltered key, so they pin today's *order for every input that already ran*, not the disputed input itself.

## Excluded from this packet (L32)

`tests/fixtures/replay/movers-gainers.real-shape.csv` and `tests/fixtures/replay/movers-losers.real-shape.csv` (fold 1 only, +1 row each) are raw Finviz export rows — real tickers, real financial figures, real headlines — and are excluded from `fund-rule.diff` under L32 ("no raw exports, tickers from his screens ... in the packet"). Their presence in fold 1's real diff is a one-row-per-file append the offline evidence counts already summarize numerically above; nothing about the rule's correctness depends on reading that row's content. If a house's answer to Q4 genuinely needs to see that row, mark it `UNVERIFIABLE FROM PACKET — raw export row excluded, L32` rather than asking for it.
