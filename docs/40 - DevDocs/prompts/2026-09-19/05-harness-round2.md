MODEL: Opus 5 (`claude-opus-5`) — the migration harness IS the production write path (L29 floor) · SEAT: build hub `harness-r2-0919`, launched by the CTO desk in the background. NO NEW RULE: the ALLOWLIST is `22-migrate-snapshot-fix.md`'s, rule for rule (only the session name differs, and `--permission-mode auto` is spelled out) (= `15-migrate-harness-fix.md`'s, a subset of R1 list (1) of 2026-09-18 for this worktree; the same job shape — a harness build in `s2-p2-cards`, dev only — so nothing is brought to Dejan again, `UNATTENDED-LAUNCH.md` §1.3); NO production command exists in it. Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p2-cards` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/05-harness-round2.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control harness-r2-0919 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(ls -la /Users/cobalt/cobalt-wt/s2-p2-cards/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rev-parse *)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt rev-parse *)" "Bash(cd *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62); never `bypassPermissions`; `COBALT_ENV=production` NEVER appears, never `--allow-prod`, no vault write, no push, no merge, no rebase, no cherry-pick · METER: Anthropic small-to-medium · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Migration harness, ROUND 2 — fold what the three-house tribunal check of 2026-09-19 found (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/REVIEW.md`: Grok `SAFE TO DEPLOY`, Gemini `FIX FIRST Q5`, Astra `FIX FIRST 1`; hub-verified REAL 3, blocks today's deploy 0, UNVERIFIABLE 1). Three small things, tests first, NOTHING else: (R1) `_schema_of` must refuse an ambiguous table name instead of picking one; (R3) test (e) must be able to fail; (U1) the one thing no house could settle from reads — DDL under REPEATABLE READ after ANOTHER session committed to the same table — becomes a test. `cobalt_dev` is YOURS ALONE while you run. DO NOT STOP until the report ends `ROUND2 BUILT …` or `FAILED …`.

DESK RULINGS ON THE REVIEW (architect; build to them; a reason one cannot work → `FAILED: design — <why>`, never your own substitute):
- **R1 (Astra, BLOCKER as filed; hub: REAL, latent):** `_schema_of()` (`src/cobalt/db_migrations/cli.py`, the `pg_tables … schemaname = ANY(%s) LIMIT 1` query) finds a table by name across `SEARCHED_SCHEMAS` and takes whichever row comes first; if one name existed in two of those schemas the proof could digest the untouched copy and print `OK`. Production has NO such duplicate today (desk, read-only query 2026-09-19 06:57 ET: 0 rows), so this is not today's failure — it is a verifier that can lie, on the production write path, and L1 says it fails loud instead. FIX: fetch EVERY match; more than one → `MigrationError` naming the table and all its schemas ("… exists in <a> and <b>; the proof cannot know which one the migration touches"); exactly one → as today; none → as today. Remove `LIMIT 1`; make the comment above `SEARCHED_SCHEMAS` true (it says "in look-up order" — there is no order any more, say "the schemas searched; a name found in more than one is an error").
- **R2 (Gemini BLOCKER / Grok MINOR; hub: REAL as a stated limit, no such statement exists in the applied set):** NO CODE. `OK` means "the content that existed at the transaction's snapshot, plus this transaction's own writes, is unchanged except where the migration meant to change it". A FUTURE migration that back-fills rows of a table other sessions insert into cannot rely on the proof to notice rows inserted after the snapshot. Write exactly that, in plain words, into the module's DevDoc under the REPEATABLE READ section and into `cmd_migrate`'s docstring, with the rule for migration authors: a back-filling migration either runs with every writer of that table stopped, or is followed by a second idempotent pass.
- **R3 (Astra, MINOR; hub: REAL):** test (e) `test_a_concurrent_update_to_a_row_the_migration_updates_fails_loud` passes even if the explicit rollback is removed, because the migration's only write is the one Postgres rejects. FIX the TEST: the migrate transaction makes a migration-owned write FIRST (a row the test owns), then provokes the conflict; assert that first write is ABSENT afterwards. ADD a CLI-boundary test: `main()` turns a `MigrationError` into `FAILED: …` on stderr and exit status 1 (the hub read `cli.py` `main` and found it correct but untested).
- **U1 (Astra Q2 + the hub; UNVERIFIABLE FROM READS):** the runner re-applies `0003` on every run, whose `ALTER TABLE system.cobalt_jobs ADD COLUMN IF NOT EXISTS …` touches the table `com.cobalt.heartbeat` (every 15 min) and `com.cobalt.seat-usage` (hourly) update — AFTER the BEFORE probe has read it under REPEATABLE READ. Yesterday's dev round trip had no concurrent writer. TWO TESTS, against `cobalt_dev`, second connection = the other session (`db.connect_migration`, autocommit): (f) migrate connection takes the BEFORE probe → the other session commits an UPDATE to a `system.cobalt_jobs` row (a no-change `SET col = col` on one existing row, so nothing needs cleaning) → the migrate connection runs the SAME `ALTER TABLE … ADD COLUMN IF NOT EXISTS` statement `0003` runs (take it from the file, do not retype it) → AFTER probe → EXPECT: no serialization error, the table reads unchanged. (g) the same, but the other session holds an OPEN transaction that updated the row while the ALTER starts: the ALTER must WAIT (ACCESS EXCLUSIVE) and complete once the other session commits — drive it with a thread and a hard 20 s ceiling inside the test so a hang fails the test instead of hanging the suite; EXPECT: completes, no error. IF (f) or (g) RAISES or hangs: that is a FINDING, not a test to bend — quote the exact Postgres error, write `FAILED: U1 — <error>` and stop; the desk decides (it changes today's deploy).
- **MINORS folded, no test needed:** `_connect` assigns `read_only = False` explicitly on the migrate path (today it relies on the server default — Astra Q1); the module docstring's list of proof columns no longer says `count(*)` (Grok Q7). NOT in scope, do not touch: a `lock_timeout`, any change to the digest, the probe order, the migrations themselves.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words, committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4 **R1** list (1) (this worktree's dev rules — compare this launch line with it rule by rule; a rule not in it → `FAILED: authorization mismatch — <rule>`), **R3** + **R5** (the weekend push through 2026-09-20), **R18–R21** (LAWS L67: the tribunal check whose findings you are folding; a round 2 of that check on YOUR diff is the desk's next run, not yours). `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"` proves the rows are committed. Laws, one line each: L1 fail loud · L3 one path · L29 Opus floor on a write path · L35 your claims are checked against your artifacts · L41 interim: `.env` by name only, never printed, removed after — even on failure · L45 tests against the real artifact (the ALTER statement comes from `0003`'s file) · L46 commit as you go, tree clean at the end · L54 you work only in this worktree · L55 never push · L60 recovery: partial work is wip-committed, never discarded · L62/L63 no questions mid-run; the report is your stop channel.
YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` as the last line, remove any `.env`, wip-commit, stop.

INDEX CARD: `prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → LAWS.md in full → the review file above IN FULL (Verdict table, the two experiments, "claims checked that are not findings") plus `astra-findings.md` beside it (finding 1 and 2, Q2) → `src/cobalt/db_migrations/cli.py` IN FULL → `tests/cobalt/test_migrate_proof.py` group 8 (yesterday's five snapshot tests — your new tests join that group and reuse its second-connection helpers) → `src/cobalt/db_migrations/0003_heartbeat_vault_outcome.sql` → this worktree's `docs/40 - DevDocs/reports/migrate-snapshot-fix-2026-09-18.md` (how yesterday's run proved `cobalt_dev` unchanged; do the same). STATE OF THIS BRANCH: `sprint-2/stack`, tip `704e802` (a report commit above the gated code `a70e286`), a branch OFF main — you only ADD commits on top. Report (L48): NEW file `docs/40 - DevDocs/reports/harness-round2-2026-09-19.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → tables per step → `## ESCALATE` → last line. Commit the report with every step.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no `cd … &&`, NO pipe, no redirect (yesterday's hub slipped twice with `| tail` and `2>/dev/null` — do not); outputs read from the tool result; files via Write/Edit. PREFLIGHT: `git status`, `git rev-parse --abbrev-ref HEAD` (= `sprint-2/stack`, else `FAILED: wrong branch`), `git log -1` (= `704e802`, else `FAILED: unexpected tip — <sha>`), `date`, `uv run pytest --co -q tests/cobalt/test_migrate_proof.py`, `.env` cp → `COBALT_ENV=dev uv run cobalt db migrate --help`. ANY classifier denial → `FAILED: <step> — <command> — <reason string>`, remove the `.env`, wip-commit, stop.

## 1. Tests first (each new or changed test FAILS before the code change where a code change exists — quote the failing line — and passes after)
R1: a test that makes ONE name exist in two searched schemas inside a transaction it rolls back (create the duplicate through the SAME connection it hands to `_schema_of` / `_probe`, so nothing is ever committed to `cobalt_dev`) → today returns a schema silently (RED: assert it raises) → after the fix raises `MigrationError` naming both schemas; plus the two unchanged cases (one match, no match). R3: the strengthened test (e) — prove it CAN fail: run it once against a local edit that removes the rollback in the except-path, quote the failure, RESTORE the file (`git diff` on `cli.py` empty before you go on), and say so; the CLI exit-status test. U1: tests (f) and (g) — these may pass at once (they test Postgres + the existing code, there is no code change behind them); say so plainly, as yesterday's test (b). Every test cleans up exactly what it created; none leaves `cobalt_dev` different — prove it as yesterday's run did (the no-op `COBALT_ENV=dev uv run cobalt db migrate` proof table before step 1 and after step 2: same rows + digests for every table, `cobalt_redactions` named if a redaction fired). Commit the red tests: `test(db-migrate): ambiguous table names, a rollback test that can fail, DDL after a concurrent commit — red`.

## 2. Build, then prove on `cobalt_dev`
The smallest change that satisfies the DESK RULINGS. Then: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quoted, nothing applied · `COBALT_ENV=dev uv run cobalt db migrate` → no-op, no `CHANGED` · ROUND TRIP `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → `COBALT_ENV=dev uv run cobalt db migrate` → digests identical before and after, 0006/0007 back · `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy --tb=short -p no:randomly` → EXPECT 0 failed (yesterday: 1850 passed) · `COBALT_ENV=dev uv run cobalt validate` exit 0 · `rm` the `.env`, `ls -la` proves it gone · `uv run pytest -q tests/cobalt tests/taxonomy` (offline) → 0 failed (yesterday: 1561) · `uv run cobalt jobs restarts a70e286..HEAD` → table verbatim. DevDoc for `db_migrations/cli.py` updated (R1's rule; R2's meaning of `OK` and the rule for back-filling migrations; U1's result in two lines). Commit: `fix(db-migrate): refuse an ambiguous table name; say what OK means; prove DDL after a concurrent commit (tribunal round 1, 2026-09-19)`. Last line `CONTINUE: step 3`.

## 3. Close
`git status --porcelain` empty · `git diff --stat a70e286 HEAD -- src tests docs` listed · the report names `<code>` = the fix commit and `<tip>` = the last commit · §0 Headline · last line:
`ROUND2 BUILT <code> on sprint-2/stack (tip <tip>) | R1 ambiguous name refused | R3 test (e) can fail + exit-status test | U1 DDL after a concurrent commit: (f) <pass/FAIL> (g) <pass/FAIL> | dev round trip 0005↔0007: identical | with DB: <b> passed, 0 failed | offline: <a> passed, 0 failed | OWED: tribunal round 2 on this diff, production --proof-only on the final code, then the deploy | ESCALATE: <n>`
Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production.
