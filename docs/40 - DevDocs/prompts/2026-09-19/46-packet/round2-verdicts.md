# ROUND 2 — §0 Headline, the 11-row Q1 table, the new-findings table, and the stop line (verbatim from `scratch/review-deploy-p4-r2-0919/REVIEW.md`)

## §0 Headline

- **Two of three houses read it (Grok, Gemini); Astra = `METER`** (same Codex usage limit, retry after 4:54 PM — a normal ending, L67's floor met twice). Both houses got the packet as bucket A and bucket B; all four calls exit 0, no HARNESS/TIMEOUT.
- **Round-1 fixes: 10 of 11 FIXED, row 4 PARTIAL.** Both launch blockers (row 1 dev `migrate*` wildcard, row 2 the 5.2 clock revert) are FIXED, verified against the real file and code — no `migrate*` rule survives, only prose mentions; one clock check, before 4.2.
- **Hub-verified REAL: 4 new, of which 1 blocks the launch:** (N1) the new §4.2 "Do NOT enter §8 … services are as they were" leaves **aset DOWN** if its bootout succeeded and radar's did not, and contradicts the untouched "from the first `bootout` on … stop means finishing §8's safe-state rule first" (all three reading houses found it independently); (N2) §1.4 (c)/(d) still say "`.env` cleanup, `FAILED`" against the new (h) "restore FIRST" — row 4 only PARTIAL (dev DB only); (N3, N4) two wording splices, MINOR.
- The desk's open items stand and are not counted: P15's archiver precondition, the wide-vs-narrow L68 scope, and whether an unstarred rule matches by prefix (now also on the new bare dev `migrate` rule).
- Verdicts: Grok `RUN AFTER FIXES 1, 3` (A) / `RUN AFTER FIXES B1 B2 B3` (B) · Gemini `RUN AS IS` (A) / no VERDICT line, 1 BLOCKER (B) · Astra `METER`. Hub read: **RUN AFTER FIXES N1, N2** — one sentence in §4.2, one re-pointing of (c)/(d) to (h), then re-approval on the new sha.


### Q1 — the 11 round-1 REAL rows

| row | claim | verdict | evidence read |
|---|---|---|---|
| 1 | dev `migrate*` reaches production via `--allow-prod` | **FIXED** | real file: `db migrate*` occurs twice, both in prose (SEAT line, WILDCARD AUDIT), never as a rule; the launch line carries the three exact dev strings. All three houses agree. |
| 2 | 5.2 clock re-check reverts a green deploy | **FIXED** | §2.1 "re-checked … ONCE, immediately before 4.2 … nowhere else"; the only `19:40` outside that check is §8's CLOCK NOTE, which now says "no hour ever sends a green result into this section"; §5.2 "MEASUREMENT ONLY". |
| 3 | replay armed after a §8 revert | **FIXED** | §8 (0) boots it out; new rule present exactly. Residual (bootout does not take) is named in ESCALATE and the FAILED line — deliberate; see N7 below. |
| 4 | red (c)/(d) skips the restore | **PARTIAL** | (h) exists, but (c)/(d) still say `.env` cleanup → FAILED and were not rewritten — see N2. |
| 5 | wildcards broader than steps | **FIXED** | WILDCARD AUDIT + four tag / worktree-add / jobs narrowings; the 23-string list counts to 23 and 55 rules (57 `Bash(` occurrences = 55 rules + 2 prose mentions). Five of the "byte-identical to `02`" strings spot-checked present in `02`. |
| 7 | NEW RED has no §8 arrow | **FIXED** | §6.1 "a NEW RED sends this run to §8"; the two named exceptions are checks. |
| 13b | archiver named as Saturday risk | **FIXED** | plist reads above: archiver Mon–Fri 20:30, replay Mon–Fri 21:10, backup all seven days 21:40 (`jobs.yaml:224`). |
| 14 | P13 expects tables it cannot list | **FIXED** | P13 gate = exit 0, no `CHANGED`, no timeout; absence proven by P12. |
| 17 | rerun `worktree add` | **FIXED** (stop present; wording splice N3) | §1.2 RERUN CASE → `ls` → `FAILED: gate`; nothing is down, no `.env`, `cobalt_dev` untouched at that point (P14 proves no `.env`), so it stops cleanly. |
| 18 | DONE line says "bootstrapped" regardless | **FIXED** | §7 slot `<bootstrapped … \| NOT bootstrapped: <reason>>`. |
| 20 | moved digest had no stop | **FIXED** | §1.4 (f) "a moved digest on ANY other table → … `FAILED: gate — cobalt_dev digest moved on <table>`". |

### New findings this round

| # | house(s) · sev (theirs) | step | claim (≤20 words) | what I read | VERDICT | fix in one line | blocks the launch? |
|---|---|---|---|---|---|---|---|
| N1 | Grok A #3 (MAJOR), Grok B B1 (BLOCKER), Gemini B Q4 #2 (BLOCKER) — **all three reading calls**; hub had it first | §4.2 vs preamble asymmetry (a) | if `bootout` aset succeeds and radar's does not, the new terminal branch stops with aset DOWN and says "services are as they were" | real file: 4.2 "still loaded after that one retry → `FAILED: residents not stopped` … NOTHING was merged … production is untouched and the services are as they were. Do NOT enter §8"; untouched preamble: "(a) from the first `bootout` on, production's residents are DOWN — "stop" then means finishing §8's safe-state rule first" and "then §8 if … any resident is down". §8 (4) is what would have brought aset back; no other sentence does. `bootstrap` rules for both residents exist, so it is a text gap, not a rule gap | **REAL** | in 4.2's terminal branch: "if either `bootout` succeeded, `bootstrap` that label ONCE with 5.2's command before the FAILED line; still do not enter §8 (0)"; and say "residents: <up/DOWN>" from a `print`, not "as they were" | **yes — it can leave production with a resident down under a sentence that says it is untouched; the approval binds the file's sha256, so fix it in the same fold** |
| N2 | Grok A #1 (MAJOR), Grok B B2 (MAJOR), Gemini B Q4 #1 (MAJOR); hub found the same and one more angle | §1.4 (c), (d), (g), (h) | (c)/(d) still say `.env` cleanup then FAILED, contradicting (h)'s "restore FIRST, then cleanup" — a literal session removes the `.env` (f) needs | real file: (c) "Anything else → `.env` cleanup, `FAILED: gate — dev forward migrate <what>`", (d) "A red → `.env` cleanup, then `FAILED: gate — with-DB suite red …`", (g) "cleanup before ANY stop line, including every red branch above", (h) "on a red at (c) or (d): FIRST run (f) … THEN (g)'s `.env` cleanup". Hub adds: (h)'s premise "every FAILED branch of (c) … leaves `cobalt_dev` at 0001–0011" is FALSE for (c) — `cmd_migrate` applies, probes and commits in ONE transaction (`cli.py:560-568`, `conn.rollback()` on `CHANGED`/error), so a red (c) leaves nothing applied; running (f) there only executes `DROP TABLE IF EXISTS` (`0010`/`0011` rollback SQL) — harmless, but the sentence is wrong. It is true for a red (d) | **REAL (dev DB only; production untouched)** | point (c) and (d)'s arrows at (h); say (h) is a no-op restore after a red (c) | no — `cobalt_dev` only, and today P15 stops the run before §1.4; but it leaves round-1 row 4 PARTIAL, so fold it with N1 |
| N3 | Grok A #2 (MINOR), Grok B B4 (MINOR) | §1.2 | "Do not pick another name. Then → then, in this order, three separate calls" reads as a continuation after the rerun FAILED line | real file: the string `Then → then` occurs once, right after the rerun paragraph; UNATTENDED RULES and L62 make FAILED terminal | **REAL (wording, MINOR)** | "If `worktree add` succeeded → then, in this order, …" | no |
| N4 | Grok A #4 (MINOR); hub | §2.1 | PONR is named at 4.3 merge success, but "nothing after 4.2 may be ABANDONED for the hour" starts at bootout, and a RED after 4.3 still reverts via §8 — "FINISHES FORWARD" overstates | real file §2.1 paragraphs; §8 header still lists reds at 4.3–4.6. The next sentence ("§8 is entered ONLY for a RED result") resolves it, so no session is sent backwards by the clock | **REAL (wording, MINOR)** | "once 4.3 returns success the CLOCK never sends this run to §8; a RED still does" | no |
| N5 | Grok A #5 (MINOR) | launch line | stack `merge --no-edit *` could merge any ref into the gate worktree | git-only, scoped to `stack-deploy2-0919`, never `~/cobalt`; the prompt types three named branches; disclosed in `approval-list.md` | **NOT REAL** (optional narrowing, not a hole) | — | — |
| N6 | Grok B B3 (MAJOR) | §1.4 (h) | (h)'s restore-failed FAILED line has no `.env` cleanup | (g) already says cleanup before ANY stop line "including every red branch", and STOP (b) says it again; (h) orders cleanup before the FAILED line | **NOT REAL** (covered by (g)) | — | — |
| N7 | Gemini B Q4 #3 (MAJOR) | §8 (0) | a replay `bootout` that does not take leaves a dead job armed for Monday | real file: "If the bootout does not take after one retry, say so plainly in the ESCALATE and in the FAILED line … the desk must know" — the round-1 row-3 alternative the hub itself allowed; nothing fires before Monday 21:10 | **NOT REAL** as a defect (named residual) | — | — |


DEPLOY P4 CHECK R2 · grok: RUN AFTER FIXES 1,3|B1,B2,B3 · gemini: RUN AS IS|no verdict line (B: 1 BLOCKER) · astra: METER · round-1 fixes FIXED: 10 of 11 · hub-verified REAL: 4 (blocks the launch: 1) · NOT REAL: 3 · UNVERIFIABLE: 2
