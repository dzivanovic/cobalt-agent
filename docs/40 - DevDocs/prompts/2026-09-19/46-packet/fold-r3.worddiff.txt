diff --git a/docs/40 - DevDocs/prompts/2026-09-19/45-packet/38-deploy-p4-after.md b/docs/40 - DevDocs/prompts/2026-09-19/46-packet/38-deploy-p4-after.md
index 455b26f..bcd971e 100644
--- a/docs/40 - DevDocs/prompts/2026-09-19/45-packet/38-deploy-p4-after.md	
+++ b/docs/40 - DevDocs/prompts/2026-09-19/46-packet/38-deploy-p4-after.md	
@@ -44,24 +44,24 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no pipe,
## 1. THE L68 INTEGRATED GATE — green BEFORE anything is merged
**The law, verbatim (LAWS.md L68, ruled today):** *"No branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green — the offline suite and the with-DB suite run on the branch that combines them, before the merge, never after it in `~/cobalt`."* Three unmerged branches exist right now — `sprint-2/p4`, `ops/2026-09-19`, `archiver/append-0919` — so the stacked tree combines ALL THREE, not only the one that lands. This gate proves the seam; the merge at 4.3 still ships `sprint-2/p4` ALONE.
1.1 `git -C /Users/cobalt/cobalt log --oneline -1` (main's tip, record as `<main-at-gate>`) · `git -C /Users/cobalt/cobalt-wt/s2-p4 log --oneline -1` · `git -C /Users/cobalt/cobalt-wt/ops-2026-09-19 log --oneline -1` · `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` · `git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` empty. Record all four tips in the report; the facts packet names the tips the desk saw, and a DIFFERENT tip is recorded, not a stop (a branch may have advanced) — but a dirty `s2-p4` worktree IS a stop: `FAILED: gate — s2-p4 not clean`.
1.2 BUILD THE STACK: `git -C /Users/cobalt/cobalt worktree add -b stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919 main`. **RERUN CASE (round-1 finding 17):** this command FAILS if the branch or the path already exists, which is exactly what a second launch after a failed gate looks like — no `worktree remove` / `branch -D` rule exists in your list, deliberately. So on that failure: `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919` to show what is there, then `FAILED: gate — stack artifact from an earlier run is still on disk (branch `stack/deploy2-0919` and/or its worktree); the desk removes it before the next launch`. Do not reuse the existing worktree (its content is from the earlier branch tips), do not pick another name. [-Then-]{+If `worktree add` SUCCEEDED+} → then, in this order, three separate calls: `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --no-edit sprint-2/p4` · `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --no-edit ops/2026-09-19` · `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --no-edit archiver/append-0919`. A CONFLICT on any of them → `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 merge --abort`, then `FAILED: gate — conflict merging <branch> — <paths>` (NOTHING is down, production is untouched, the desk resolves it in a build run, never here). **EXPECT the conflict here if anywhere:** P4 and the archiver branch both edit `src/cobalt/db_migrations/__init__.py` (the `FORWARD`/`REVERSE` tuples — P4 adds 0008/0009, the archiver adds 0010/0011) and both touch `src/cobalt/db_migrations/placement.py`, `src/cobalt/cli.py`, `configs/cobalt/taxonomy/tunables.yaml`, `src/cobalt/archiver/runner.py` and `store.py` (the archiver build's own cross-branch ESCALATE (iii) names that list). A conflict in those files is a REAL integration seam — exactly what L68 exists to surface — and it is reported, never resolved here. Record the stack tip as `<stack>` and `git -C /Users/cobalt/cobalt-wt/stack-deploy2-0919 log --oneline -6`; also `grep -n "0008\|0009\|0010\|0011" /Users/cobalt/cobalt-wt/stack-deploy2-0919/src/cobalt/db_migrations/__init__.py` → the merged `FORWARD` must list **0001…0011 in numeric order** with `REVERSE` its mirror; anything else → `FAILED: gate — merged migration registry is wrong — <what you read>`.
1.3 OFFLINE SUITE ON THE STACK: `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` → "No such file" (a pytest run with `.env` present is NOT offline) → `cd /Users/cobalt/cobalt-wt/stack-deploy2-0919` → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`; read the summary from the tool result). GATE: **`0 failed`**. Quote the whole summary line. For context only, never as a pass/fail rule: P4 alone closed at `1829 passed, 324 skipped`; ops-0919 at `1608/0`; the archiver at `1866 passed, 320 skipped` — the stack's own number is its own, and only `failed == 0` is the bar. Any failure → name each test, quote each assertion, `FAILED: gate — offline suite red on the stack — <tests>`.
1.4 WITH-DB SUITE ON THE STACK (`cobalt_dev` only — never production, never `--allow-prod`). **THE STEP ORDER IS PART OF THE GATE, and it was learned the hard way:** the archiver's own DB run proved that with `cobalt_dev` left at 0001–0009 and the archiver's two tables dropped, **20 of its 23 `requires_db` tests cannot run at all** — its run 1 went `12 failed, 145 passed` purely on `relation … does not exist`, because only 3 of them are self-contained round trips; the rest need the tables to EXIST (seven via the `real_connect` fixture, thirteen via `BarStore().ensure_schema()`, which creates neither). The stacked tree carries migrations **0008–0011**, so the gate applies FORWARD first, runs the suite, then restores. In this exact order:
  (a) `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` (by name, never read, never printed, L4) → `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` prints it.
  (b) BASELINE: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote the table. EXPECT `cobalt_dev` at **0001–0009** with `archive_progress` and `archive_incidents` ABSENT (`-`) and P4's `movers_daily`/`picks`/`missed` PRESENT — that is where the archiver and P4 DB runs each left it. Record every row count and digest: this table is what (f) is compared against. A `CHANGED` verdict → `.env` cleanup, then `FAILED: gate — cobalt_dev proof CHANGED before the gate — <line>`.
  (c) FORWARD: `COBALT_ENV=dev uv run cobalt db migrate` (the stacked registry, 0001…0011; 0001–0009 re-run idempotently by design) → quote the full table. EXPECT `archive_progress` and `archive_incidents` **CREATED**, every pre-existing table `OK` with its digest unchanged, `content UNCHANGED on every table`, no `CHANGED`. Anything else → [-`.env` cleanup,-]{+**go to (h) and follow it in order**, ending+} `FAILED: gate — dev forward migrate <what>`.
  (d) SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 failed`** — quote the summary line. For context only, never as the bar: P4's own DB close was `2136/0` plus the two owed tests `37` ran; the archiver's scoped run was `154/3`, and **those three reds must already be gone by P15 or you never reached this step**. A red → [-`.env` cleanup, then-]{+**go to (h) and follow it in order**, ending+} `FAILED: gate — with-DB suite red on the stack — <test names + assertions verbatim>`. **Never label a red "known" and continue** (L68 admits no exception; L70: a red someone actually ran is a fact, not an escalate to carry).
  (e) STATE THE END STATE PLAINLY, in the report, before you restore it: `cobalt_dev holds after the suite: 0001–0011 (the stack's registry), <the archiver's two tables present>` — the deploy ships only 0008/0009, so 0010/0011 are on `cobalt_dev` ONLY because the gate put them there.
  (f) RESTORE, because other lanes expect 0001–0009: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → EXPECT exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` applied and **nothing else** (P4's 0008/0009 are NOT touched — if the output reverses anything numbered ≤ 0009, that is a serious finding: quote it, `.env` cleanup, `FAILED: gate — dev rollback went below 0009 — <what>`) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → compare row for row against (b)'s baseline: the two archiver tables ABSENT again, every other table's row count and digest identical. A table whose digest moved is NAMED with both values, and (round-1 finding 20) it is also a STOP unless it is one of the two archiver tables this step just dropped: a moved digest on ANY other table → `.env` cleanup, then `FAILED: gate — cobalt_dev digest moved on <table>: <before> → <after>`. The one benign class the archiver's own run documents — rows another lane committed between two proofs — is still a stop HERE, because no other lane is supposed to be holding `cobalt_dev` while this gate runs (P14 proved that at preflight).
  (g) `rm /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` → `ls -la /Users/cobalt/cobalt-wt/stack-deploy2-0919/.env` says "No such file". **The `.env` cleanup happens before ANY stop line, including every red branch above** — a stop line written while `.env` is on disk is itself a failure condition: go back, remove it, prove it, then fix the line.
  **(h) RED-BRANCH RESTORE (round-1 finding [-4).** Every-]{+4; premise corrected round 2, finding N2).** A+} `FAILED: gate` branch of[-(c) and-] (d)[-above-] leaves `cobalt_dev` at 0001–0011, where no other lane expects it. {+A red (c) usually leaves NOTHING applied — `cmd_migrate` applies, probes and commits in ONE transaction and rolls it back on `CHANGED` or error — so there (f) is a no-op restore: its `--rollback --down-to 0009` finds 0010/0011 absent and its two rollback files are `DROP TABLE IF EXISTS`. Run it anyway, because you cannot tell from the output alone how far a partial (c) got, and a no-op restore is free.+} So on a red at (c) or (d): FIRST run (f) in full — the `--rollback --down-to 0009` and its closing `--proof-only` — THEN (g)'s `.env` cleanup, THEN the FAILED line, and end that line with `cobalt_dev: <state you proved>`. This is a `cobalt_dev` command on a dev database with production untouched; it is always safe to run and it is never skipped because the gate went red. If the restore ITSELF errors, do not retry it: quote the error and write `FAILED: gate — <original reason> — cobalt_dev: 0001–0011, restore failed: <error>` so the desk knows exactly what to clean.
1.5 `cd /Users/cobalt/cobalt` → `ls -la .env` (the file IS listed here — cwd is back; contents never read) → `git -C /Users/cobalt/cobalt log --oneline -1` = `<main-at-gate>` (nobody committed while you ran; if main moved, record it and go on — §4.2 re-checks it where it matters).
1.6 Write the gate into the report as its own section: the four tips, `<stack>`, the merged `FORWARD` line, both suite lines VERBATIM, all THREE `--proof-only` verdicts (baseline / post-forward table / post-rollback), the forward and rollback tables, the `.env` proof, and P15's quoted archiver line with its date. If any part of the gate was not run, say WHICH and WHY in that section — a gate section with a missing step is a `FAILED: gate — incomplete`, never a green one. **The gate branch `stack/deploy2-0919` and its worktree are a GATE ARTIFACT, not work to land: they are never merged, never pushed, never rebased, and they stay on disk for the desk to remove at the close — record them in ESCALATE as cleanup owed.** Report committed by name. Last line `CONTINUE: step 2`.

## 2. Preconditions
2.1 **WINDOW.** `date` → the run STARTS only when now is **≤ 18:30 ET on Saturday 2026-09-19**. WHY THIS IS LAWFUL AND WHY THE HOUR MATTERS: L43 binds `com.cobalt.radar` restarts to the 20:00–21:00 ET `market_reset` pause "or during overnight idle … **on a trading day**; never while a scanning session is open" — today is Saturday, not a trading day, and the radar reads `idle:overnight` at every hour, so no scanning session can be open and L43's trading-day window does not bind; L66's own requirement (residents down before the merge and up after) is met by §4 regardless of hour. This is the same basis deploy 1 ran on at 08:19 ET this morning under 09-18 R3/R5. The 18:30 cap is not L43 — it is margin against **the nightly backup, `com.cobalt.backup`, which runs at 21:40 ET on ALL SEVEN days** (`configs/cobalt/jobs.yaml:224` `weekdays: [0,1,2,3,4,5,6]`, `ops/com.cobalt.backup.plist:72-79`) and must never fire into a half-deployed tree. **Round-1 finding 13b, corrected here:** `com.cobalt.archiver` (20:30) and `com.cobalt.replay` (21:10) are **Mon–Fri only** (`ops/com.cobalt.archiver.plist:56-57`, Weekday 1–5) and therefore cannot fire on a Saturday at all — the earlier wording named the archiver as tonight's risk and was wrong. Besides the backup, the only thing running today is `com.cobalt.heartbeat` (every interval, all week). Now > 18:30 → `FAILED: window — wait for the desk` (the desk re-approves a new window; you never pick one).
**THE CLOCK IS RE-CHECKED with `date` ONCE, immediately before 4.2 (bootout), and nowhere else. At or after 19:40 ET at that check → `FAILED: window`, nothing is down, nothing is merged, stop.**
**THE POINT OF NO RETURN (round-1 finding 2, a BLOCKER in the previous wording — read this twice).** The earlier text re-checked the clock "again at 5.2" and sent a late run to §8; that would have taken a GREEN merge and a GREEN migration and REVERTED them for no reason but the hour, leaving 0008/0009 applied over reverted code, the replay job unarmed and a second outage. It is deleted. The rule is now: **once 4.3's `merge --ff-only` has returned success, this run FINISHES FORWARD.** From that instant the clock is a MEASUREMENT, never a trigger — the `date` calls at 4.2 and 5.2 exist to compute the outage length and to stamp the report, and no hour, however late, sends a successful merge+migration to §8. §8 is entered ONLY for a RED result (a failed step, a red smoke, a mid-run denial), never for a late clock. If the run is still going at 21:40 and the backup fires beside it, that is an ESCALATE line in the report, not a revert. Conversely nothing after 4.2 may be ABANDONED for the hour either: finish §5's residents-up, finish §6's smoke, write the stop line. {+Said once more without the shorthand (round-2 finding N4): once 4.3 returns success the CLOCK never sends this run to §8 — a RED result at 4.3–4.6 or in §6's smoke still does, and §8 is then run in full.+}
2.2 **THE TWO P4 GATES** (L67 + L68's companions): (a) the P4 DB run's report section, written by `37-p4-db-run.md` in `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md`, ends `P4 DB <sha> | … | owed requires_db: <names → PASSED> | …` with both owed tests PASSED and `0` failed — missing, or ending `FAILED`, or either owed test not passed → `FAILED: gate — P4 DB run <what>`; (b) the houses' fold read `36-review-p4-folds.md` has landed and the desk's §4/§51-class section in `cto-2026-09-19.md` records it with NO blocker for shipping — absent → `FAILED: gate — L67 house check not recorded`. Neither is re-derived by you; you read the stop line and the desk's recorded verdict, and quote both.
2.3 **THE GATED CODE.** `<gated>` = the `sprint-2/p4` tip named in the approval row. `git -C /Users/cobalt/cobalt-wt/s2-p4 log -1 --format=%D` shows `HEAD -> sprint-2/p4`; `git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` empty; `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat <gated> HEAD -- . ':(exclude)docs'` prints NOTHING (only report commits may sit above the gated code — the whole tree outside `docs/` is compared, never a hand-listed set of folders); record `<n>` = `git -C /Users/cobalt/cobalt-wt/s2-p4 rev-list --count main..HEAD`.
2.4 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; no build or deploy session is merging (the desk launches you alone). `git -C /Users/cobalt/cobalt merge-base main sprint-2/p4` → record it (the desk saw `919d562`, and `git -C /Users/cobalt/cobalt diff --stat 919d562 main -- . ':(exclude)docs'` printed NOTHING — main has moved by DOCS ONLY since P4 branched, which is why 4.1's rebase cannot change code; re-prove that with the merge-base you actually find, and a NON-EMPTY diff there → record it and treat 4.1's proof as the real gate, not this one).
@@ -73,7 +73,7 @@ UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no pipe,

## 4. Rebase → residents DOWN → merge → migrate → validate (no report prose between these commands; record outputs, write them up after 5.2)
4.1 `git -C /Users/cobalt/cobalt-wt/s2-p4 rebase main` (brings P4 above your own commits; a conflict → `git -C /Users/cobalt/cobalt-wt/s2-p4 rebase --abort` then `FAILED: rebase conflict — <paths>`; nothing is down yet). **PROOF before going on:** `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat <gated> HEAD -- . ':(exclude)docs'` prints NOTHING (what you merge is byte-identical to what §1 gated and §2.2 proved). ONE NAMED EXCEPTION (it happened on 09-18): if your own 3.1 committed a machine-written `configs/cobalt/rules.yaml`, the rebase carries that file onto the branch and this diff lists exactly that ONE path — accept it ONLY when `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat main HEAD -- configs/cobalt/rules.yaml` prints nothing. ANY other path → `FAILED: rebase changed code — <paths>` (nothing is down; do not go on). Then `git -C /Users/cobalt/cobalt-wt/s2-p4 rev-list --count main..HEAD` = `<n>` of 2.3 (the rebase dropped nothing). Record `<pre-merge>` = `git -C /Users/cobalt/cobalt rev-parse --short HEAD`.
4.2 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → baseline. `date` (the 2.1 re-check). Then **residents DOWN (L66)**: `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → each must FAIL with "Could not find service" (still printed → one `date`, print once more; **still loaded after that one retry → {+the run STOPS — but not before you put the OTHER resident back. REPAIR FIRST (round-2 finding N1, all three reading calls): `bootout` is two separate commands, so one of them may have SUCCEEDED; for EACH label whose `launchctl print` now says "Could not find service" (i.e. the bootout that DID take), run 5.2's bootstrap for that label ONCE — `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` for aset, `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for radar — then `launchctl print gui/501/<label>` once more and record `state`. ONE attempt per label, no `kickstart`, no second pass. THEN write+} `FAILED: residents not stopped — <label> — <launchctl output verbatim>`, and say plainly that NOTHING was merged and nothing was migrated, so [-production-]{+production's CODE+} is [-untouched and-]{+untouched; state+} the services [-are-]{+from what you just printed, as `residents: aset <up/DOWN>, radar <up/DOWN>`, NEVER+} as {+"as+} they [-were.-]{+were".+} Do NOT enter §8** — §8's step (0) is a bootout, which is the thing that just failed; entering it would loop). A heartbeat beat landing in this window goes RED on the sheet/radar probes and DMs him — expected; note the beat time afterwards.
4.3 `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<pre-merge>` (else `FAILED: foreign commit on main — <hash>` → §8 steps 3–5 only: nothing merged, bring the residents up) → `git -C /Users/cobalt/cobalt merge --ff-only sprint-2/p4` → record the tip and the file/insertion counts.
4.4 (nothing here — there is no settings dry-run in this deploy; the config-shape change rode in with 4.3.)
4.5 **THE MIGRATION.** `COBALT_ENV=production uv run cobalt db migrate --allow-prod` — **THIS CALL: set the Bash tool's `timeout` parameter to 600000.** EXPECT it to sit SILENT ≈50 s in its BEFORE probe and again in its AFTER probe (that is the proof reading `system.bars`, not a hang): run it in the FOREGROUND, do not interrupt it, do not background it. Budget ≈2–3 minutes. The runner walks ALL of `FORWARD` = 0001…0009 and prints `-- applying` for each; 0001–0007 re-run idempotently by design. **GATE = its proof table:** NO table marked `CHANGED`, and the only `CREATED` objects are the ones `0008_radar_value_movers.sql` and `0009_picks_missed.sql` define — read those two files (index card) and hold the output to them: `system.movers_daily`, `"user".picks`, `"user".missed`, plus `system.radar_membership` gaining `rank_metric`/`rank_value` as ADDED COLUMNS on an `OK` table, never as a `CHANGED` verdict. Quote the whole table and the `proof cost:` line. Take each new table's SIDE from the printed table, never from memory — 6.3 queries them with that side. STATED FINDINGS, not to be re-derived: (i) the whole migrate transaction runs at REPEATABLE READ, so a concurrent commit cannot read as `CHANGED`; only a concurrent UPDATE/DELETE of a row this migration also updates can raise `could not serialize access` — if it happens the harness names it, exits non-zero and applies NOTHING: that is a failed 4.5 → §8, say `serialization failure`, and do NOT rerun the command. (ii) The harness COMMITS BEFORE it prints its proof table, so a timeout or dropped connection in the instant after the commit leaves 0008/0009 APPLIED with no table printed. **Whenever 4.5 ends without a complete proof table, do NOT write "nothing applied" from the absence of output: read it back first** — `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT to_regclass('system.movers_daily')"` → empty/NULL = not applied, a name = applied and committed — then go to §8 (the same path either way: the code is reverted, the additive schema stays) and label the FAILED line `migrations applied: yes/no` from that read. A tool timeout is labelled `tool timeout`, never `migration error`. Non-zero exit → §8; output showing a migration PARTIALLY applied → run NO rollback, quote it, `FAILED: migration partial` after §8's code revert + residents up.
