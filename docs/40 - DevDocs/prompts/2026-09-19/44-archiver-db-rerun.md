MODEL: Opus 5 (`claude-opus-5`) — same seat and shape as `25-ops-db-run.md` (the ops with-DB run): L29 floor on the migration harness, and the DB step itself is ALWAYS hub-direct, never delegated to a builder. SEAT: `archiver-db2-0919`, worktree `/Users/cobalt/cobalt-wt/archiver-append` (it already exists, round-2 code tip `9056e73`, report commit `fb825df` on top — do NOT create it, do NOT `git worktree add`). THIS RUN TOUCHES `cobalt_dev`: it is a `.env`-present, `COBALT_ENV=dev` run. `cobalt_dev` is FREE now — `ops-0919-db`'s stop line: `cobalt_dev: 0001–0009, unchanged`, `.env` removed there, verified (PREFLIGHT proves it yourself). Two bare commands, run by the desk: `cd /Users/cobalt/cobalt-wt/archiver-append` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/44-archiver-db-rerun.md' and follow it exactly." --model claude-opus-5 --remote-control archiver-db2-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh — every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `--allow-prod`, no vault write, no other seat's config; `cobalt_dev` and `.env` ARE this session's to touch, scoped exactly to the two rules above · METER: Anthropic medium; a usage-limit message is not a failure — record the reset time, and BEFORE stopping remove `.env` if present and prove it gone (RECOVERY RULE applies to the meter stop too), commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

# Archiver 2026-09-19, DB RE-RUN — the with-DB proof, RE-RUN after round 3 (DB-1's REVOKE, cto-2026-09-19 R25 "B") and round 3b (DB-2's twin dropped, R26): migrations 0010/0011 applied on real `cobalt_dev` WHILE the with-DB suite runs against them, in the corrected order the first DB run's own report named wrong (DB-3), THEN reversed. LAW STEP of this work item (L67, 09-18 R13–R17 / 09-19 R8): the branch's DB debt was CLOSED by the first DB run, but two of its findings (DB-1, DB-2) are what this branch now needs re-proved on real Postgres before deploy. After it, none of it yours: the branch still needs its whole-build house check closed (Astra's read is owed after its meter resets; Grok's whole-build read is queued separately) · Dejan's approval · deploy 3, Sunday, after P4. You never merge, never deploy, never push, never touch production, never `--allow-prod`. DO NOT STOP until the report ends `ARCHIVER DB2 …`, `CONTINUE: … — meter …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable), not by Dejan; a prompt file is not an approval.
(a) The 16 base `Bash(...)` rules + `Bash(uv run cobalt jobs restarts *)` + the two `COBALT_ENV=dev` rules + `--disallowedTools`/`--add-dir` above are `25-ops-db-run.md`'s own launch line, already run once on `cobalt_dev` (`ops-0919-db`, stop line `OPS 0919 DB be00506 …`) and again by `archiver-db-0919` (this file's own predecessor, `35-archiver-db-run.md`, stop line `ARCHIVER DB d2099e1 …`) — same strings, this worktree's path only where the `.env` rules name it. These need no new approval beyond what covered `25`/`35`.
(b) The TWO rules new to THIS worktree — `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)`, `Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)` — unchanged from `35`, Dejan's R18 strings for this worktree. Quote it exactly, verbatim from `docs/40 - DevDocs/reports/cto-2026-09-19.md`, `## §4 Rulings`, row **R18** (11:33 ET, "approve env"): *"FOUR new rule strings, exactly: `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)` · `Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)` · `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)` · `Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)` — a by-name copy of `.env` into that worktree for the dev-DB test run, never printed, removed at the end with its absence proven in the terminal line; disclosed with it as REUSED from 13's approved line: `Bash(COBALT_ENV=dev uv run cobalt db migrate*)`, `Bash(COBALT_ENV=dev uv run pytest *)`. NEVER: production, push, vault write, `bypassPermissions`. Covers: `25-ops-db-run.md` (launch when `cobalt_dev` is free) and the archiver's DB run after its round 2."* This run is a RE-RUN under the same R18 grant — no new `.env` rule is needed or requested. If that row is missing, reworded, or its APPLIED marker absent → `FAILED: authorization mismatch — no R18 row for the archiver .env pair`, commit nothing, stop before PREFLIGHT.
(c) The WORK ITEM (re-running with the corrected step order, and re-proving DB-1/DB-2 on real Postgres) is authorized by: `35`'s own close report, section `# DB RUN`, finding **DB-3** ("`35`'s step order is wrong and the next prompt of this shape should not repeat it... The safe default taken (re-apply, run, roll back again) delivered both steps' stated outcomes.") — this prompt IS that next prompt, and step 1 below is written to that finding, not to `35`'s original order; round 3's report, section `# ROUND 3`, DB-1 closed in the SQL; round 3b's report, section `# ROUND 3b`, DB-2's twin dropped per R26. A citation that does not say what is quoted → `FAILED: authorization mismatch — <what>`, stop.
YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line — but FIRST run `.env` cleanup (see below) — then wip-commit, stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(archiver-db2-0919): <item> partial — <why>"`, last line `CONTINUE: step <n>`. EVERY `git add` names explicit paths under `src/`, `tests/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named. `.env` DISCIPLINE (L4, L41 — the one rule that overrides every other stop order): on ANY stop — `FAILED`, `CONTINUE`, meter, or the normal end — `ls -la .env` FIRST; if it exists, `rm /Users/cobalt/cobalt-wt/archiver-append/.env` THEN `ls -la .env` again to prove it gone, THEN write whatever stop line you were going to write. A stop line written while `.env` still exists on disk is itself a `FAILED` condition — go back and remove it, then re-open the report and fix the line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you touch `cobalt_dev` directly, L4/L41/L67) → `docs/40 - DevDocs/reports/cto-2026-09-19.md` rows **R18** (quoted above — the `.env` authorization), **R25** (DB-1's "B", REVOKE) and **R26** (DB-2's "A", twin dropped) → `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` §4/§11 (the migrations' own spec — binding; where your reading of the code differs from it, the CODE wins and the difference is an ESCALATE line) → `src/cobalt/db_migrations/__init__.py` IN FULL (the `FORWARD`/`REVERSE` tuples) → `src/cobalt/db_migrations/0010_archive_progress.sql` and `0011_archive_incidents.sql` IN FULL (round 3's REVOKE statements, now part of forward migration — confirm they are there before you rely on the grant tests flipping) → `src/cobalt/db_migrations/cli.py`, esp. `cmd_migrate` (≈:497-660) and its `--rollback`/`--down-to`/`--proof-only` flags → `tests/cobalt/test_archiver_migrations.py` IN FULL — the 0010/0011 `requires_db` half is **10 tests, not ≈17** (DB-4's own correction; confirm honestly from your own `--co`, do not requote the old figure), including `test_forward_creates_both_tables_on_the_system_side`, `test_migrate_twice_is_idempotent_for_the_two_new_tables`, `test_rollback_down_to_0007_drops_exactly_those_two_and_nothing_else`, and the two parametrized `test_the_user_role_has_no_grant_on_either_table[archive_progress]` / `[archive_incidents]` (currently RED, this run's reason for existing) → `tests/cobalt/test_archiver_append_store.py` §§ marked `requires_db` (13 as of round 3's tip, 12 after round 3b's drop of `test_the_own_connection_upsert_survives_another_transactions_rollback` — confirm it is genuinely GONE from this file, not merely skipped, before you rely on the 22 count) → `tests/cobalt/test_archiver_quiet.py` — **holds ZERO `requires_db` tests** (DB-4's other correction; the F3/Q10 twin lives in `test_archiver_append_store.py`, not here — do not go looking for one) → `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` in THIS worktree, sections `# DB RUN`, `# ROUND 3`, `# ROUND 3b` IN FULL (the file you APPEND to — a new `## DB RE-RUN` top-level section, AFTER `# ROUND 3b`'s existing final line; never delete or restructure existing sections) → `35-archiver-db-run.md` (the shape this run corrects — same seat, same `.env`/`COBALT_ENV=dev` command shape, same PREFLIGHT pattern; its own finding DB-3 is what step 1 below fixes) and its close report section `# DB RUN`'s last line (read-only evidence of what `cobalt_dev` held after the first run — you do not re-derive it by any other means). Laws, one line each: L1 fail loud · L3 one path — the FORWARD/REVERSE tuples are the only registry, never a second one · L4 secrets discipline — `.env` by name only, never `cat`/printed · L29 Opus floor on the migration harness · L35 artifact, not claim — the desk verifies via `git show`/`git diff`, never your report's prose alone · L41 cross-house credential law — this session runs the DB-backed proof directly (hub-shaped, no worker) · L45 real-shape evidence — the REVOKE's effect is on real Postgres, never asserted from the SQL text alone · L46 one run, one commit boundary; small commits within it (CLAUDE.md) · L48 evidence in the report file, same turn · L54 you work in this worktree only · L57 every printed value replayable · L60 you hold this session until the stop line · L67 this build still needs its house check closed after you stop (Astra's whole-build read is owed separately — not yours to chase). Report (L48): APPEND to `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` in THIS worktree — a new `## DB RE-RUN` section: §0 Headline ≤5 lines → `## DB RE-RUN PREFLIGHT` → `## DB RE-RUN AUTHORIZATION` → `## DB RE-RUN — MIGRATIONS 0010/0011 + SUITE WHILE APPLIED + REVERSED` → `## DB RE-RUN — OFFLINE SUITE + RESTARTS` → `## DB RE-RUN ESCALATE` → last line. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `psql`, `uv run cobalt` anything except `jobs restarts` and `db migrate` (and `db migrate` ONLY with `COBALT_ENV=dev` in front, never bare, never `--allow-prod`), any `COBALT_ENV=production` command, `launchctl`, `claude`, `cat`/`less`/`more`/anything that could print `.env`'s contents. There is NO rule for `psql` in this allowlist — the sequence-privilege check in step 1 is bounded by that: if it needs a command outside this list, it is an `ESCALATE` line, never an invented one. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, `.env` cleanup, wip-commit, stop. Never retry a denied command in another shape.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim):
`date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; branch must be `archiver/append-0919`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (record the tip your last `CONTINUE:` commit left, or the branch's current tip on first launch) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file" (if it exists → `FAILED PREFLIGHT: .env present before this run started — remove it and say who left it, this is not yours to inherit silently`) · Read (not Bash) `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`'s LAST LINE and quote it verbatim — it MUST start `ARCHIVER R3B `; anything else → `FAILED PREFLIGHT: round 3b not closed` · `grep -rn "cobalt_dev" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/"` dated after the `ARCHIVER DB` line's timestamp (by filename/section date) and read (Read tool) any later hit to confirm no session claimed the lane since the first DB run closed it — a hit that claims `.env`/`cobalt_dev` and is NOT this branch's own report → `FAILED PREFLIGHT: a later session may have touched cobalt_dev — <what you found>` · `git -C /Users/cobalt/cobalt-wt/archiver-append diff --stat 8838dda..HEAD -- src tests docs` (quote it — records what round 3 + round 3b added on top of the first DB run: expect `src/cobalt/db_migrations/00{10,11}*.sql`, `tests/cobalt/test_archiver_migrations.py`, `tests/cobalt/test_archiver_append_store.py`, `docs/40 - DevDocs/`; a surprise path → ESCALATE, not silently accepted) · `uv run pytest --co -q tests/cobalt/test_archiver_migrations.py tests/cobalt/test_archiver_append_store.py tests/cobalt/test_archiver_quiet.py` (collects cleanly; state the collected `requires_db`-marked count HONESTLY from this `--co` — expected **22** per DB-4's corrected 10 + round-3b's 12 + quiet's 0 — name any drift, do not assume). Any denial → `FAILED PREFLIGHT: <rules>`.

## 0. STATE PROBE — allowlist proof + the "what does `cobalt_dev` hold" line (L62: probe the rules with no harmless variant, once, before real use)
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env` → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (applies NOTHING; quote the FULL output — every table THIS branch's harness knows, its placement, row COUNT, digest, and the trailing `code:` line) → `rm /Users/cobalt/cobalt-wt/archiver-append/.env` → `ls -la .env` proves it gone. Expected: 0001-0009 objects `OK`; 0010/0011's two tables ABSENT/MISSING (the first DB run rolled them back and nothing since has re-applied them). State the terminal line: `cobalt_dev holds (before this run): 0001–0009 objects OK, <n> rows in <largest table>; 0010/0011 objects: absent (expected — first DB run rolled them back, nothing has re-applied since)`. If ANYTHING here reads `CHANGED`, errors, or the two new tables already exist → STOP and ESCALATE (do not assume a prior partial run cleaned up; name what you found) — `.env` cleanup, `FAILED: step 0 — <line>`, stop. Last line `CONTINUE: step 1`.

## 1. MIGRATIONS 0010/0011 APPLIED → PROOF → SUITE RUN WHILE APPLIED → ROLLED BACK → PROOF — the order DB-3 says is correct
This is the ONE change to `35`'s shape that matters: the first DB run rolled the two tables back BEFORE running the with-DB suite, and 20 of the 23 tests need the tables — DB-3 named this in full (`12 failed, 145 passed` with ten missing-table reds). This run does apply → proof → SUITE → rollback → proof, in that order, so the suite runs against tables that exist.
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env` (re-copy; step 0 removed it) → `COBALT_ENV=dev uv run cobalt db migrate` (forward: 0001-0009 idempotent no-ops, 0010 and 0011 apply fresh, now carrying round 3's REVOKE statements; quote the FULL output) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (prove every table 0001-0011 now reads `OK`, none `CHANGED`; quote it) →
**THE WITH-DB SUITE, WHILE APPLIED.** SCOPE, deliberately narrow, same discipline `25`'s step 4 and `35`'s step 2 used: this branch's OWN `requires_db` tests are `tests/cobalt/test_archiver_migrations.py`, `tests/cobalt/test_archiver_append_store.py`, and `tests/cobalt/test_archiver_quiet.py` (which PREFLIGHT confirmed holds none). Repo-wide `requires_db` tests this branch merely touched in passing (`test_radar_score_migration.py`, `test_tenancy.py`) are OUT OF SCOPE here — leave them skipped, do not run the whole `tests/cobalt` tree with `.env` present. `COBALT_ENV=dev uv run pytest -q tests/cobalt/test_archiver_migrations.py tests/cobalt/test_archiver_append_store.py tests/cobalt/test_archiver_quiet.py` → quote the FULL summary line. EXPECTED: `22 passed, 0 failed` — both `test_the_user_role_has_no_grant_on_either_table[archive_progress]` and `test_the_user_role_has_no_grant_on_either_table[archive_incidents]` now PASS (round 3's REVOKE proved live; name them explicitly and quote PASS), and `test_the_own_connection_upsert_survives_another_transactions_rollback` does not appear in the collection at all (round 3b dropped it — confirm its absence, do not just note it "skipped"). Any OTHER red: name the test, quote the assertion, state whether it is a TEST-SIDE defect (fix it here, tests-first, its own commit) or a genuine finding about `cobalt_dev`'s real state (ESCALATE, do not "fix" real data). SEQUENCE PRIVILEGE CHECK: grep the just-collected test list (or `tests/cobalt/test_archiver_migrations.py`'s own source) for a `requires_db` test that checks, on real Postgres, whether `cobalt_user` holds ANY privilege on `system.archive_incidents_id_seq` (round 3 added an OFFLINE text-pin for this — `test_the_incident_sequence_is_also_revoked_from_cobalt_user` — but that is not a live check). **If such a live `requires_db` test exists, its result is already in the summary line above — name it and quote its PASS/FAIL.** If no such test exists, state that plainly and STOP THERE — there is no `psql` rule in this session's allowlist, and no other Bash-allowed command proves a live grant; do not invent one. `cobalt_dev` must be UNCHANGED in content beyond the migration state itself (every test that writes cleans up; the migration-harness tests that call `_apply`/`_rollback_paths` directly are self-contained round trips) — if you have reason to believe otherwise, say so explicitly, do not assume. State the terminal line, feeding the stop line's `db` field: `db <p>/<f> (22 archiver requires_db tests, scoped as above; the two grant tests named above)`.
`COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` (per `_rollback_paths`' numeric-prefix selection this reverses exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` and stops — 0001-0009 are NOT touched; quote the FULL output) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (prove `cobalt_dev` is back at 0001-0009, BYTE-IDENTICAL to step 0's baseline — 0010/0011's two tables absent again, every 0001-0009 object unchanged; quote it, compare explicitly against step 0's proof rather than asserting equality) → `rm /Users/cobalt/cobalt-wt/archiver-append/.env` → `ls -la .env` proves it gone. If EITHER proof-only reads `CHANGED` where it should not, the with-DB suite finds a genuine red beyond the two named, or the rollback leaves either new table or the sequence's REVOKE behind → `.env` cleanup, `FAILED: step 1 — <line>`, stop. Commit: `test(db-migrate): DB-1's REVOKE and DB-2's drop proved live on cobalt_dev — corrected apply/suite/rollback order (archiver DB re-run, DB-3)` (report-only commit if no code changed; if the proof surfaces a code defect, fix it tests-first, its own commit, named plainly). Last line `CONTINUE: step 2`.

## 2. OFFLINE SUITE + RESTARTS
With `.env` confirmed absent (re-check `ls -la .env` if any doubt): `uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 3b's own close (`docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`'s `# ROUND 3b` stop line, offline `<p>/<f> (<s> skipped)`) — `passed` MUST match round 3b's figure exactly (this step touches no offline test), `failed` MUST stay 0; `skipped` should be unchanged from round 3b's own offline close (this step runs with `.env` removed, so all 22 `requires_db` tests skip again, same as before) — name the arithmetic, do not wave off a mismatch. `uv run cobalt jobs restarts 8838dda..HEAD` (the branch's merge-base with `main`) → the table VERBATIM; name any UNCLASSIFIED entry, do not assume it matches. `git status --porcelain` empty · `git diff --stat 8838dda HEAD -- src tests docs` listed, every path one this prompt or the branch's own prior commits named · report committed by explicit path: `docs(report): archiver db re-run — <one line>`. Last line `CONTINUE: step 3`.

## 3. DB RE-RUN CLOSE
`## DB RE-RUN ESCALATE` carries forward, untouched, everything the first DB run's, round 3's and round 3b's ESCALATE sections already carried, by name, PLUS whatever this run found new (a real red beyond the two named grant tests, any drift from step 2's expected counts, an authorization mismatch, the sequence-privilege finding either way). State plainly whether DB-1 and DB-2 are now CLOSED on real Postgres (they should be, one way or another, after step 1) and correct, explicitly, the two index-card drifts DB-4 named (quiet.py holds 0 `requires_db` tests; the 0010/0011 half is 10, not ≈17) if this report is the first place a later reader would see them corrected. `MEMORY:`/`RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, EXACTLY this shape — the P4 deploy prompt's PREFLIGHT P15 looks for a later line on this branch reading `db <p>/0`, so the `db` field below MUST contain that exact token shape (`db <passed>/0`, zero failed, or the run is not done):
`ARCHIVER DB2 <sha> | offline <p>/<f> | db <p>/0 | DB-1 tests: PASSED/… | cobalt_dev: 0001–0009, unchanged | .env: removed, proven gone | RESTARTS: … | ESCALATE: <n>`
or `FAILED: <step> — <reason> | cobalt_dev: <state>` (with `.env` cleanup proven first, always). Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or with `--allow-prod`.
