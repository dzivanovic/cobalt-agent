MODEL: Opus 5 (`claude-opus-5`) — L29 write-path floor: a production `"user".trader_settings` write (four new/changed keys) · SEAT: apply hub `cards-golive-0919`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/30-cards-golive-apply.md' and follow it exactly." --model claude-opus-5 --remote-control cards-golive-0919 --allowedTools "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/cards-live-2026-09-19/p2-live-settings.yaml --sha256 f3663399b1cbb5ba94edadc7b929900a09be02439c84ffc24f89c12ea81c65f0 --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/cards-live-2026-09-19/p2-live-settings.yaml --sha256 f3663399b1cbb5ba94edadc7b929900a09be02439c84ffc24f89c12ea81c65f0 --apply)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --apply)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61), never `bypassPermissions`, no `--permission-mode` flag (this is a write path — L29's "never auto mode on a write path" means no `--permission-mode auto`, exactly as `02-deploy-stack-3.md` carries none), push never in it, NO `uv run pytest`, NO `cobalt` command outside this list · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely; a dialog gets no answer).

# CARDS SWITCH-ON — APPLY (2026-09-19) — ONE settings load: `radar.cards_enabled` true + `card.proposed_key` + `card.curves` + `card.shadow_promotion_bar`, to production `"user".trader_settings`. No git write, no merge, no migration, no restart (settings are re-read every call — L43/L66's residents-down rule does not apply here, there is no code change). DO NOT STOP until the report ends `CARDS LIVE …` or `FAILED …`.

INDEX CARD (read before anything else): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` → `LAWS.md` IN FULL. Laws that bind THIS run, one line each: **L7** (interim clause) — no engine-fed flip without HITL; Dejan's typed "Approve cards" (`cto-2026-09-19.md` §4 R20, 11:57 ET) IS that approval, for the exact file + sha256 named there, nothing else. **L28** (amended 2026-09-15) — a trader-run `cobalt settings load --apply` with reviewed sha256 needs no separate token; trace it as one line: command, hash, time, "approved by Dejan via the desk — `cto-2026-09-19.md` row R20". **L29** — Opus floor for this write path; never `--permission-mode auto` / `bypassPermissions` on it (see SEAT line). **L32** — these are user-data settings files: never `git add` them, never quote a value beyond what R20 already approved verbatim (the four bands, three curves, four promotion-bar numbers, and `radar.cards_enabled` — nothing invented, nothing extra). **L43 / L66** — `market_reset` pause 20:00–21:00 ET refuses a `"user".trader_settings` write; this run has no resident restart to sequence, but the write itself is still refused inside that hour. **L61 / L62 / L63** — this session got every permission it needs before it started; a mid-run denial or a mid-run question = FAILED, never patched from inside; no dialog tool. **L67** — this prompt is a desk prompt with a production write; it is read by ≥1 other house BEFORE it runs — that check is `31-review-cards-apply.md` / hub `review-cards-apply-0919`; its report `blocks the launch` count must be 0 (checked in PREFLIGHT below, not re-asked of Dejan).

R12/R13/R17/R20 (`cto-2026-09-19.md` §4) are the ruling chain: R12 sets Monday 2026-09-21 premarket as the target; R13 rules the three live mornings ARE the L7 shadow (advisory only — Dejan keeps grading by hand); R17 is Dejan's own verbatim card-value ruling; R20 is his HITL approval of the exact file below. `docs/40 - DevDocs/prompts/2026-09-19/23-packet/facts.md` is the schema/arithmetic proof-by-reading (bands are fractions of conviction, curves are piecewise-linear anchors, `Extension.leg_count` is always an integer, `htf_level_proximity` is deliberately OMITTED — Grok's BLOCKER from the go-live check, folded: no invented magnitude ships).

## 0. PREFLIGHT (nothing here changes production)
P1 `date` → refuse if now is inside 20:00–21:00 ET (L43/L66's `market_reset` pause: the settings write will be refused there anyway) → `FAILED: window — wait for the desk`. Record ET time and margin to 20:00.
P2 `shasum -a 256 /Users/cobalt/cobalt/data/backups/cards-live-2026-09-19/p2-live-settings.yaml /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml` → must equal, in order, `f3663399b1cbb5ba94edadc7b929900a09be02439c84ffc24f89c12ea81c65f0` and `945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca` exactly. Any mismatch → `FAILED: hash mismatch — <file> — <got>`.
P3 `ls -la /Users/cobalt/cobalt/data/backups/cards-live-2026-09-19/p2-live-settings.yaml /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml` → both present, non-empty, record sizes and mtimes.
P4 `git -C /Users/cobalt/cobalt status` (long form, not `--porcelain`) → record verbatim. Expected: only report/machine-written files dirty (the desk's live `cto-2026-09-19.md`, `seat-usage.md`); no merge markers ("You have unmerged paths", "still merging", "rebase in progress"). Anything suggesting another hub is mid-operation on `~/cobalt` → `FAILED: main not clean — <what>`.
P5 `git -C /Users/cobalt/cobalt log --oneline -5` → the tip is a desk/report commit, nothing mid-merge. `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` → R20's row (11:57 ET "Approve cards") is committed on main. Missing → `FAILED: authorization mismatch — R20 not on main`.
P6 THE HOUSE-CHECK GATE (L67 floor, not re-asked of Dejan): Read `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-cards-apply-0919/REVIEW.md`'s last line. It must begin `CARDS APPLY CHECK` (not `FAILED`) and its `hub-verified REAL (blocks the launch: <d>)` must read `<d> = 0`. File missing, last line not that shape, or `<d> > 0` → `FAILED: house check gate — <what the line actually says>`. Quote the line verbatim in the report either way.
P7 `COBALT_ENV=production uv run cobalt heartbeat show` → record verbatim; baseline for STEP 3.
GATE: all seven pass → `CONTINUE: step 1`. Any FAILED above stops here; nothing was touched.

## 1. Live `--dry-run` (R20 string 1 — exact command, no variation)
`COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/cards-live-2026-09-19/p2-live-settings.yaml --sha256 f3663399b1cbb5ba94edadc7b929900a09be02439c84ffc24f89c12ea81c65f0 --dry-run`
Paste the ENTIRE output verbatim in the report. PASS only if every one of these holds, read from that output — nothing assumed from the file, only from what the loader itself prints:
PRINT SHAPE (desk fold 12:3x from the house check, `review-cards-apply-0919/REVIEW.md` row 3 — read from `card.py cmd_load_card` on main): the loader prints one marker line per key (`~` = changed, `+` = added, `-` = deleted), then for each a `db  :` line and a `file:` line whose value is `json.dumps(..., sort_keys=True)` — Decimals appear as QUOTED JSON strings (`"0.9"`, not `0.90`), curve anchors as lists of quoted strings (`[["1","1"],["3","10"]]`), keys SORTED (so `Extension.leg_count` prints first inside `card.curves`). Compare VALUES NUMERICALLY (`"0.9"` = 0.90, `"10"` = 10); never fail on quoting, key order, or a trailing zero.
- `radar.cards_enabled`: a `~` (changed) line, false → true. If the loader shows it already true, something applied it out of band: `FAILED: dry-run mismatch — cards_enabled already true`.
- `card.proposed_key`: a `+` (added) line; its `file:` value holds exactly four bands, numerically `a_plus_min` 0.9, `a_min` 0.7, `b_min` 0.5, `c_min` 0.4 — decimal-clean. Trailing binary-float noise (e.g. `0.9000000000000000222`) → `FAILED: dry-run mismatch — float noise in proposed_key`.
- `card.curves`: a `+` line; its `file:` value holds EXACTLY three curves, numerically `atrs_from_open` [[1,1],[3,10]], `rvol` [[1.2,1],[5,10]], `Extension.leg_count` [[0,10],[1,10],[2,6],[3,3],[5,1]] — and NO `htf_level_proximity` anywhere in the output. If it appears → `FAILED: dry-run mismatch — htf_level_proximity present, not approved` (R20 explicitly excludes it). Float noise in `1.2` → the same float-noise FAILED.
- `card.shadow_promotion_bar`: a `+` line; its `file:` value holds exactly four numbers, numerically 10, 30, 1 and 0.9, under the four field names the FILE itself carries (quote them; the file is sha256-pinned, so the names are his approved ones).
- NOTHING ELSE: exactly one `~` line and three `+` lines; NO `-` (deleted) line; no `card.alignment_default` line; no other key. Anything else → `FAILED: dry-run mismatch — unexpected key <name>`.
Any single deviation → the exact `FAILED: dry-run mismatch — <what>` line, commit nothing, apply nothing, stop (do not proceed to STEP 2, do not roll back — nothing was applied).

## 2. Live `--apply` (R20 string 2 — only after STEP 1 PASSES)
`date` AGAIN, immediately before this apply (desk fold, house check row 1; `02-deploy-stack-3.md` did the same before each write): inside 20:00–21:00 ET, or later than 19:45 ET → do NOT apply: `FAILED: step 2 — window — nothing applied`, stop.
`COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/cards-live-2026-09-19/p2-live-settings.yaml --sha256 f3663399b1cbb5ba94edadc7b929900a09be02439c84ffc24f89c12ea81c65f0 --apply`
Paste the ENTIRE output verbatim (the `applied: {...}` line, any `round trip: ... EQUAL` line the loader prints). Non-zero exit or an error → `FAILED: apply error — <verbatim>`, go to STEP 4 ROLLBACK (nothing durable should exist, but verify — do not assume). L28 trace line in the report: "approved by Dejan via the desk — `cto-2026-09-19.md` row R20 (11:57 ET) · command (above) · sha256 `f3663399…65f0` · applied `<date/time ET>`."

## 3. VERIFY (read-only; every command below is verbatim-from-02 or its wildcard)
3.1 `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key IN ('radar.cards_enabled','card.proposed_key','card.curves','card.shadow_promotion_bar')"` → four rows, quote verbatim; `radar.cards_enabled` = `True`/`true`; the other three JSON values present (do not hand-parse for correctness beyond "present, non-null" — STEP 1's dry-run already proved shape and values; this is a stored-values existence/identity check).
3.2 `COBALT_ENV=production uv run cobalt validate` → exit 0; quote whatever it prints about cards/card states. A non-zero exit or a traceback → treat as a failed verification → STEP 4.
3.3 `COBALT_ENV=production uv run cobalt heartbeat show` → compare against P7's baseline: no NEW red. A new RED not present at P7 → STEP 4.
3.4 `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` and `tail -n 60 /Users/cobalt/cobalt/logs/aset.err` → RED only if a line timestamped AFTER the STEP 2 apply time is a traceback or a refusal that NAMES cards / card settings / `CardSettingsError` / `trader_settings` (desk fold, house check row 2). Older lines, and later noise unrelated to cards or settings, are recorded verbatim under ESCALATE and are NOT a rollback trigger. A qualifying line → STEP 4.
3.5 `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` and `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` → both 200 (liveness only — this does NOT verify the panel shows correct card values; that is checked by Dejan directly, or by Monday's 04:00 ET premarket day-open, since no scan runs today).
GATE: 3.1–3.4 all clean → verification PASSED, rollback NOT USED. Any one red → STEP 4.

## 4. ROLLBACK (only entered from a STEP 2 apply error or a STEP 3 red)
4.1 Dark `--dry-run` (R20 string 3): `COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --dry-run` → quote verbatim. EXPECTED (desk fold, house check row 4 — `card.py`: the file is the WHOLE card-settings set, a key it omits is DELETED in the same transaction): one `~ radar.cards_enabled` true → false and exactly three `-` lines (`card.proposed_key`, `card.curves`, `card.shadow_promotion_bar`), nothing else. If STEP 2 errored before writing, the dry-run may instead show NO change at all — that is also a pass (state is already dark): skip 4.2, go to 4.3. Any other shape → `FAILED: step 4.1 — unexpected rollback diff — <what> — NOT rolled back`, stop.
4.2 `date` first: inside 20:00–21:00 ET → do not apply, `FAILED: <step> — <reason> — rollback refused by window — live rows in place`, stop. Else dark `--apply` (R20 string 4): same command with `--apply` → quote verbatim. L28 trace line as in STEP 2.
4.3 Re-run 3.1–3.4 against the dark state → quote verbatim. DARK PASS for 3.1 = exactly ONE row, `radar.cards_enabled` false, the three `card.*` keys absent (NOT 3.1's live criteria); 3.2–3.4 as written.
4.4 Last line: `FAILED: <step> — <reason> — rolled back — <clean/still red>`. Commit nothing (the desk commits the report).

## 5. Close
Write `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cards-golive-2026-09-19.md` in `~/cobalt` — a NEW file (if it already exists you are a rerun: append `# SECOND RUN` under everything there, rewrite nothing above it). Structure: title `# CARDS GOLIVE APPLY 2026-09-19` → §0 Headline ≤5 lines → PREFLIGHT table → STEP 1 (verbatim dry-run + PASS/FAIL by criterion) → STEP 2 (verbatim apply + L28 trace) → STEP 3 (verify table) → STEP 4 if entered → ESCALATE → last line. Leave the report UNCOMMITTED — this hub does not `git add`/`git commit` anything (no rule in its allowlist permits it, and none should be added: the desk commits the report, per L51-2 pattern, on its own next commit). Last line, exactly one of:
`CARDS LIVE <time ET> · file sha256 f3663399… · stored values: match · resident logs clean: yes · rollback: not used · ESCALATE: <n>`
or
`FAILED: <step> — <reason>`
Then stop.
