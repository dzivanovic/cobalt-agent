STATUS: DEPLOY 3's BUILD LANE, chunk 3 — the LAST build step before the house check (R32: the archiver + ops ship tonight if this lane is green before 21:00 ET). Chunk 2 (`49-rebase-fix.md`) fixed six of the seven reds and deliberately left the seventh red, because it needed a ruling. **The ruling came: `cto-2026-09-19.md` §4 R34, 15:4x ET, "B".** You implement exactly that ruling and take both suites to ZERO failures.

MODEL: Opus 5 (`claude-opus-5`) — you change a scheduling bound that decides when a production job may contact a paid data source (L29, L53) · SEAT: build hub `backfill-window-0919`, launched by the CTO desk in the background, two bare commands: `cd /Users/cobalt/cobalt-wt/archiver-append` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/50-backfill-window.md' and follow it exactly." --model claude-opus-5 --remote-control backfill-window-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rebase main)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(git rebase --abort)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · **NO NEW RULE: this list is `48`'s and `49`'s, byte for byte** (approved as `cto-2026-09-19.md` §4 **R33**), only the file name and the `--remote-control` name differ · SESSION: fresh · auto mode on; never `bypassPermissions`; **NO production command, NO `--allow-prod`, NO push, NO merge into `~/cobalt`, NO `launchctl`, no vault write** · METER: Anthropic small · nobody sits at this terminal.

# IMPLEMENT R34 "B": THE BACKFILL IS BOUNDED OUT OF 04:00–09:30 ET; THE CEILING STAYS AT 50
**HIS RULING, verbatim in substance (`cto-2026-09-19.md` §4 R34):** `radar.finviz_max_rpm` **STAYS 50** — you do not change it, in any file, in any spelling. The archiver's **backfill** is bounded out of the **04:00–09:30 ET** premarket window. The nightly 20:30 `full` run is **untouched**. He also queued, NOT for tonight and NOT for you, the real fix (C): staggering the screens' and lists' cadences.
**WHAT IS ACTUALLY WRONG, read from the code, not guessed:** `src/cobalt/archiver/runner.py:42-46` declares the backfill consumer as `DemandConsumer(name="backfill", rpm=min(len(targets), 60 / GENTLE_SLEEP_SECONDS), window=None, …)` and its own comment says why — "manual backfill is unscheduled, so it counts against every window". `src/cobalt/radar/notes.py:206` defines `window=None` as UNBOUNDED: "it has not been proved disjoint from" the other consumers. So the backfill is added to the 04:00 peak even though nothing makes it run then, and the total reaches 51.00 rpm against a ceiling of 50. **B makes the backfill genuinely disjoint from the premarket window, so its demand stops landing in that peak.**

AUTHORIZATION — VERIFY IT YOURSELF: `cto-2026-09-19.md` `## §4` **R34** (the ruling you implement), **R33** (this rule list), **R32** (the lane), committed — `git -C /Users/cobalt/cobalt log --oneline -14 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. R34 absent or worded differently from the paragraph above → `FAILED: authorization mismatch — R34 <what it actually says>`, stop and change nothing. Laws: L1 fail-loud, L3, L4, L9, L10 (config-as-code), L29, L35, L42, L45, L48, **L53 (the ceiling and the cadence are HIS — you are applying a ruling, not choosing a number)**, L54, L57, L60, L62, L68, L69, L70.
INDEX CARD: `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md` — §4.1's seven-row table and the `# CHUNK 2` section (what chunk 2 changed and what it measured about `full` mode) → `src/cobalt/radar/notes.py` **in full** (the demand model: `DemandWindow`, `DemandConsumer`, `check_scheduled_demand`, `TotalDemandExceeded`) → `src/cobalt/archiver/runner.py:1-140` → `UNATTENDED-LAUNCH.md` §2 + §6 → `LAWS.md` in full.
REPORT (L48): append to `rebase-d3-2026-09-19.md` under `# CHUNK 3 — the backfill window (R34 B)`; rewrite nothing above it.
UNATTENDED RULES: ONE command per Bash call, exact prefixes, no pipe, no redirect, no `&&`, no `$(…)`. `cd` is its own call. **NEVER `cd /Users/cobalt/cobalt`.** Before every pytest call: `cd <worktree>` then `ls -la .env`. A mid-run denial = the run FAILED (L62). You work on `archiver/append-0919` ONLY; `ops/2026-09-19` is green at `bb996a5` and you do not touch it.

## 1. THE CHANGE — tests first
**1.0 FIRST, THE SIXTH PIN chunk 2 uncovered and was not allowed to touch.** Chunk 2's own stop line: `FAILED: 2.2 (d) — the with-DB suite is red beyond item 6 — 2 failed, 2477 passed — the unexpected red is `tests/cobalt/test_archiver_migrations.py::test_rollback_down_to_0007_drops_exactly_those_two_and_nothing_else` (`AssertionError: picks was dropped by a rollback that does not own it`)`. It is a `requires_db` SIXTH member of the same enumeration-pin class as chunk 2's five: written when the archiver was the only unmerged branch, it asserts that `--rollback --down-to 0007` drops exactly the archiver's two tables. After the union that is false in the same way as its offline siblings — 0008/0009 sit below 0010/0011, so a rollback to 0007 correctly also drops P4's `picks` and `movers_daily`. **It could not be seen at `48` §4.1 because that was an OFFLINE run, which skips every `requires_db` test** — record that as the lesson, in one line, in your report.
Rewrite it exactly as chunk 2 rewrote the other five (`49-rebase-fix.md` §1.2, and read what chunk 2 actually did to them before you copy the shape): assert what is now TRUE — `--down-to 0007` drops all four tables — and KEEP the invariant the pin was protecting by asserting the archiver's own operator-undo bound beside it: `--down-to 0009` drops **exactly** `archive_incidents` and `archive_progress` and leaves `picks` and `movers_daily` standing. Rename the test to what it now pins. **Never weaken it and never delete it** (L45, L3). It is a `requires_db` test, so it only runs in §2.2 (d) — you will not see it go green before then; say so plainly rather than claiming a green you have not seen (L35, L70).
1.1 `date`; `git status --porcelain` empty; `git log --oneline -3`; `ls -la .env` → "No such file". Confirm chunk 2 landed (its commits are in the log) and quote its stop line from the report. A dirty tree or a missing chunk 2 → `FAILED: gate — <what>`.
1.2 **grep-prove the ceiling is untouched, before and after your change:** `grep -rn "finviz_max_rpm" src configs` → record every hit and its value. `radar.finviz_max_rpm` must read **50** at 1.2 and again at 2.1. If your change alters any of those lines you have done the wrong thing → revert it and `FAILED: 1.2 — the ceiling moved`.
1.3 **WRITE THE TESTS FIRST** (L45 — real shapes, not invented ones), in `tests/cobalt/test_archiver_runner.py` beside the existing demand tests: (a) a backfill started INSIDE 04:00–09:30 ET is REFUSED, loudly, with a message naming the window and the ruling — and it is refused *before* any Finviz contact; (b) a backfill started OUTSIDE that window is ALLOWED and its combined demand is **≤ 50 rpm**; (c) the nightly `full` run is UNAFFECTED at its own time — the existing `test_run_targets_mode_still_means_the_report_scope` must go GREEN without its assertions being weakened; (d) the ceiling is still 50. Run them, watch them fail for the right reason, quote the failures.
1.4 **THEN THE CODE.** In `src/cobalt/archiver/runner.py`, give the backfill consumer a real `DemandWindow` instead of `window=None`, so `check_scheduled_demand` stops adding it to the 04:00 peak, AND refuse an actual backfill invocation inside 04:00–09:30 ET with a fail-loud error naming the window (a declared window that the runner does not enforce would be a lie to the demand model — both halves or neither). Keep the comment honest: replace "manual backfill is unscheduled, so it counts against every window" with what is now true and cite `cto-2026-09-19.md` R34. **Do not touch** `GENTLE_SLEEP_SECONDS`, the radar's or replay's consumers, any cadence, or any ceiling. If the demand model cannot express "every hour except 04:00–09:30" without a change to `notes.py`'s `DemandWindow`, make the SMALLEST such change and say so in the report in its own paragraph — that file is shared with the radar and P4's replay.
1.5 `uv run pytest -q tests/cobalt/test_archiver_runner.py tests/cobalt/test_archiver_append_store.py` → quote the summary; all green. `git add` BY NAME, commit `feat(archiver): bound the backfill out of the 04:00-09:30 premarket window (cto-2026-09-19 R34 "B"; ceiling stays 50)`.
1.6 **DEVDOCS (CLAUDE.md: agent-authored prose):** update `docs/40 - DevDocs/cobalt/archiver/runner.md` — the "Open deployment gate" entry that has carried this since 09-17 is now CLOSED by ruling; say what was ruled, what changed, and that the ceiling did not move. Commit it with the code or in its own commit, your call, by name.

## 2. BOTH SUITES — zero failures now
2.1 `grep -rn "finviz_max_rpm" src configs` again → `radar.finviz_max_rpm` still **50**. Then OFFLINE: `cd /Users/cobalt/cobalt-wt/archiver-append` → `ls -la .env` ("No such file") → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`) → quote the summary. **GATE: `0 failed`.** Any failure → name each test and assertion, `FAILED: 2.1 — offline suite red — <tests>`. **Never label a red "known" and continue** (L68, L70).
2.2 WITH-DB (`cobalt_dev` ONLY — never production, never `--allow-prod`), exactly as `48` §4.2 specifies: (a) `cp` the `.env` by name, `ls -la` prints it · (b) BASELINE `--proof-only`, expect 0001–0009, the archiver's two tables ABSENT · (c) FORWARD `COBALT_ENV=dev uv run cobalt db migrate`, expect `archive_progress` + `archive_incidents` **CREATED**, every other table `OK`, no `CHANGED` · (d) SUITE `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`, **GATE `0 failed`** · (e) **POST-SUITE `--proof-only`** — the suite writes to `cobalt_dev` by design, so (b)→(e) differences are RECORDED, never a stop; a table that VANISHED is a stop · (f) RESTORE `--rollback --down-to 0009`, expect exactly `0011` then `0010` reversed and nothing else, then `--proof-only` compared **against (e), not (b)**; a digest moved ACROSS the restore is a stop · (g) `rm` the `.env`, `ls -la` says "No such file" — **before ANY stop line**.
2.3 `uv run cobalt jobs restarts <the main tip from 1.1>..HEAD` → VERBATIM, 0 UNCLASSIFIED (L42). Expect the archiver and whatever else reads the changed modules; an UNCLASSIFIED path is named in ESCALATE, never dropped.
2.4 Report committed by name. Last line, exactly this shape:
`BACKFILL WINDOW <archiver tip> | R34 B applied: backfill refused 04:00-09:30 ET, ceiling 50 UNCHANGED (grep-proved) | sixth pin fixed: <name> | peak demand now <n> rpm | offline <p>/<f> | db <p>/<f> | registry 0001-0011 | cobalt_dev: <state you proved> | .env: removed, proven gone | RESTARTS: <line> | ESCALATE: <n>`
or `FAILED: <step> — <reason> — cobalt_dev: <state> — .env: <state>`.
