MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify their findings; no write path · SEAT: review hub `review-deploy-p4-r2-0919`, launched by the CTO desk in the background. NO NEW RULE: `41-review-deploy-p4.md`'s `--allowedTools` list — itself `18-review-ops-0919.md`'s, byte for byte — copied unchanged, with exactly THREE strings different and nothing else: the prompt file name, the `--remote-control` name (`review-deploy-p4-0919` → `review-deploy-p4-r2-0919`) and the `mkdir` path (`scratch/review-deploy-p4-0919` → `scratch/review-deploy-p4-r2-0919`). Round 1 needed nothing outside that list and round 2 is strictly smaller. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/45-review-deploy-p4-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-deploy-p4-r2-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-deploy-p4-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47). **ASTRA:** its Codex meter returned a usage limit at 13:06 ET with "try again at 4:54 PM". Probe it ONCE (`codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."`, `run_in_background`): if the probe comes back UP, Astra reads the fold too and becomes the third house; if it returns a usage limit, record `astra: METER` verbatim and end with two houses — a normal ending, L67's floor met twice over. Do NOT retry, do NOT wait for the reset.

# Tribunal check of a DESK DEPLOY PROMPT, **ROUND 2** (LAWS L67, ≤3 rounds per house while meter allows; 09-18 R8) — ONLY the fold of round 1's **11 hub-verified REAL findings** into `38-deploy-p4.md`, the unattended deploy prompt for DEPLOY 2 (merge `sprint-2/p4` into production, apply migrations 0008/0009 to the live database, residents down and up, a new scheduled job, the first L68 integrated gate). Round 1's stop line: `grok: DO NOT RUN · gemini: RUN AFTER FIXES … · astra: METER · hub-verified REAL: 11 (blocks the launch: 2) · NOT REAL: 10 · UNVERIFIABLE: 2`. **The question for this round is narrow: did each fold fix what it names, and did any fold break something?** Small packet on purpose. DO NOT STOP until the report ends `DEPLOY P4 CHECK R2 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan; `38-deploy-p4.md` is still NOT approved and this round is a precondition of asking him. Standing law, not a one-off approval: LAWS.md **L67** (ruled 2026-09-18 17:30 ET — `cto-2026-09-18.md` §4 R18, with R19–R21's emergency / override / rounds clauses) and **09-18 R8** (14:29 ET, the desk's own write-path prompts are read by another house); `cto-2026-09-18.md` §4 **R9** (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20); `cto-2026-09-19.md` §4 **R1** (06:46 ET, the Codex read-only rule). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line; `ASK DESK: <question> [<time from date>]` for a question that does not stop you — take the safe default and continue, never wait on an answer.

Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim and never the desk's own `approval-list.md` — verify against the real prompt and the real code · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds, floor of one house other than the author.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT, §2 launch spellings and its LESSON bind you here UNCHANGED, with `scratch/review-deploy-p4-r2-0919/` in every path. `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` §3 = the verification rules and table shape. `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-p4-0919/REVIEW.md` = round 1 in full — your predecessor's verdict table is what this round measures against; read its `## Packet` note too (it names one packet defect of its own: `archiver-db-reds.md` was elided mid-sentence — the desk has since re-cut that file, and it is not in this round's packet). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-p4-r2-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line. You commit nothing.

**GROK INVOCATION — bind this spelling and do not improvise it.** `01-review-harness.md` §2's folder-glob form is the one that works on this host: `grok --sandbox cobalt-job --allow "Write(<absolute scratch dir>/**)" -p "<prompt>"`. **A single-file `Write(...)` pattern HARNESSes on this host — proven twice today** (the `--allow` argument must be the FOLDER GLOB ending in `/**`, never one file path). Round 1 used exactly this form for both of its Grok calls and both wrote their file, exit 0, no HARNESS. Reuse it verbatim with `scratch/review-deploy-p4-r2-0919/**`.

## 1. Packet in `scratch/review-deploy-p4-r2-0919/` (`mkdir -p` first; Read → Write, byte-identical) — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/45-packet/`
(1) `38-deploy-p4-after.md` (61,168 B) — **THE FILE UNDER REVIEW, as it stands now, after the fold** · (2) `fold-r2.worddiff.txt` (50,568 B) — the desk's pre-computed word-level diff, round-1 version → now, `git diff --no-index --word-diff=plain`: `[-removed-]`, `{+added+}`; the "before" side is round 1's own staged copy (`41-packet/38-deploy-p4.md`), so the diff is exactly what the houses did not yet see · (3) `round1-verdicts.md` (15,491 B) — round 1's §0 Headline and its complete `## Verdict table`, verbatim, including the NOT REAL and UNVERIFIABLE rows · (4) `approval-list.md` (5,199 B) — the REGENERATED approval list: 23 rule strings that differ from the approved precedent, verbatim, with the class and reason for each, what was removed, and the one question left open · (5) `cli-allow-prod.md` (3,951 B) — the code evidence behind round-1 BLOCKER 1, quoted from `src/cobalt/db_migrations/cli.py` and `src/cobalt/db.py` with line numbers for both trees · (6) `QUESTIONS.md` (5,012 B), the same text every house receives (L44).

**The six files total 141,389 B, so send TWO calls per house rather than one** (round 1's single 121,392 B call worked on both houses, but this packet is larger): **A** = `38-deploy-p4-after.md` + `round1-verdicts.md` + `approval-list.md` + `cli-allow-prod.md` + `QUESTIONS.md` (**90,821 B**, covers Q1, Q2, Q3, Q5, Q6) and **B** = `38-deploy-p4-after.md` + `fold-r2.worddiff.txt` + `QUESTIONS.md` (**116,748 B**, covers Q4 and the diff half of Q1); say in `## Packet` which bucketing each house actually got. **Never drop a file to make a bucket fit, and never let a call truncate silently** — a house that received a truncated packet did not read the packet (L44). Record every file's byte count and confirm each staged copy equals its source.

QUESTIONS.md (verbatim): see `45-packet/QUESTIONS.md` — copy it byte-for-byte into `scratch/review-deploy-p4-r2-0919/QUESTIONS.md` as part of staging step (6); it is the same text every house receives. Six questions: (Q1) is each of the 11 FIXED, quoting the new sentence · (Q2) does any rule still admit a production write outside its named step, and is the narrowing complete or does a step now type a spelling its narrowed rule no longer covers · (Q3) is the point of no return sound, stated consistently, and placed unambiguously · (Q4) what did the fold break · (Q5) the gate after the fold, incl. the red-branch restore and the rerun branch · (Q6) anything else the fold made worse.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-deploy-p4-r2-0919/` in place of their paths. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.

For every allowlist claim (Q2) quote the rule AND the step's command side by side — **and this round that cuts both ways: the fold NARROWED four wildcards, so the new failure mode is a step that types a spelling its rule no longer accepts.** After §4.2 a denied command means denied with production's services DOWN. You verify each BLOCKER and MAJOR against the REAL files: `38-deploy-p4.md` under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/` with Read and `grep` (no Bash rule needed for a plain read); `src/cobalt/db_migrations/cli.py`, `src/cobalt/db.py`, `src/cobalt/db_query.py`, `src/cobalt/jobs/cli.py` on main and in `/Users/cobalt/cobalt-wt/s2-p4/` — **re-derive `cli-allow-prod.md`'s line numbers yourself rather than trusting the excerpt** (L35); `ops/com.cobalt.archiver.plist`, `ops/com.cobalt.backup.plist` and `configs/cobalt/jobs.yaml` for Q3's calendar claims; LAWS.md at `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md`.

A claim only a RUNNING command could settle — whether an unstarred allowlist rule matches by prefix on this host, whether the stacked merge conflicts, whether the stacked suites go green, whether `launchctl bootstrap` is idempotent — is `UNVERIFIABLE FROM READS`: name the exact missing command, do not invent the answer and do not ask a reviewer to run it. Carry round 1's two UNVERIFIABLE rows forward as they stand unless the fold settled them. One extra column per REAL finding: `blocks the launch? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-deploy-p4-r2-0919/` — in particular you never edit `38-deploy-p4.md`; the desk folds.

**NOT this round's business, and not a finding:** the prompt's own P15 archiver precondition still cannot pass (`archiver/append-0919` has no `db <p>/0` line yet) and the wide-vs-narrow L68 scope question is Dejan's. Both are the desk's OPEN items, recorded in round 1 row 23 and in `approval-list.md`; note them in §0 if you like, but do not count them as findings.

Last line, exactly this shape:
`DEPLOY P4 CHECK R2 · grok: <RUN AS IS|RUN AFTER FIXES <n>|DO NOT RUN|HARNESS|METER|TIMEOUT> · gemini: <same shape> · astra: <same shape> · round-1 fixes FIXED: <n> of 11 · hub-verified REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
