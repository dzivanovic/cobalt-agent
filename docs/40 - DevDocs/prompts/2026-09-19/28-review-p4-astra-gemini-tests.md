MODEL: Sonnet 5 (`claude-sonnet-5`) — read, reuse the already-staged packet, launch two remaining reads, verify their findings; no write path · SEAT: review hub `review-p4-0919c`, launched by the CTO desk in the background. NO NEW RULE: this launch line is `21`'s (and `27`'s) launch line with exactly TWO strings changed from `21`'s — the prompt filename (`21-review-p4-build.md` → `28-review-p4-astra-gemini-tests.md`) and the `--remote-control` name (`review-p4-0919` → `review-p4-0919c`). Every `--allowedTools` string, every flag, `--disallowedTools`, and every `--add-dir` are BYTE-IDENTICAL to `21`'s (and therefore to `27`'s) — including the still-unused `Bash(grok *)` rule. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/28-review-p4-astra-gemini-tests.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-0919c --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless read per house, no retries beyond what §3 names (L47 — probe before counting on a house).

# Continuation of the three-house check of `sprint-2/p4` (LAWS L67) — round 1C: ASTRA (full packet, fresh probe — its stated reset has passed) and GEMINI'S UNREAD TEST HALF ONLY (Q3, Q8, Q9), in three balanced calls. Prior rounds: Grok round 1 = `VERDICT: BUILD CLEAN` (0/0/0, full packet, see `REVIEW.md`). Gemini round 1B = PARTIAL: source half (`code-01`..`07` + specs/reports/facts/QUESTIONS) printed `VERDICT: BUILD CLEAN`, whole-packet attempt and test-half attempt (`code-08`..`13`) both HARNESS'd (verbatim in `REVIEW-B.md`'s `## Gemini`) — Q3, Q8, Q9 unanswered. Astra round 1 and round 1B: METER both times (probed at 11:10, 11:41, and ~11:46:22 — the last one minute before its own stated 11:47 ET reset; the order deviation is noted in `REVIEW-B.md`'s PREFLIGHT). It is now past 11:50 ET — Astra's meter should have reset. Same branch, same tip (`a59ee8c`), same packet — nothing about the code under review has changed. DO NOT STOP until the report ends `P4 CHECK C …` or `FAILED …`.

**SCOPE NOTE:** the fund rule (R16, `Asset Type`/`not_equity` rule C) stopped at `FAILED: step 3` and is being redesigned separately — OUT OF SCOPE here, same as rounds 1 and 1B.

AUTHORIZATION — VERIFY IT YOURSELF, same two proofs as `21`/`27`: `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` (L67 / R18–R21) and `git -C /Users/cobalt/cobalt log --oneline -4 sprint-2/p4 -- "docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md"` (branch ref named; `OWED:` line — note `REVIEW-B.md` found a newer commit `e79d55d` on this path, the R16 FAILED-at-step-3 report, out of scope per the SCOPE NOTE above). A mismatch → `FAILED: authorization mismatch — <what>`, stop. Laws, one line each, same as `21`/`27`: L33 · L35 · L36 · L37 · L44 · L47 · L52 · L67.

REPORT: a NEW file, `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/REVIEW-C.md` — never edit `REVIEW.md` or `REVIEW-B.md` (both stay exactly as their own rounds left them) and never edit anything else in the folder outside what §1 permits. Shape: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet (reused)` → `## Astra` → `## Gemini (test half, 3 parts)` → `## Verdict table` → last line.

## 1. Packet — REUSE, do not restage. `mkdir -p scratch/review-p4-0919` first (idempotent; rule string byte-identical to `21`'s). Verify: `ls scratch/review-p4-0919/` + `wc -c scratch/review-p4-0919/*` against `21`'s SIZE TABLE (19 files, 863,573 bytes — copy the table verbatim from `21-review-p4-build.md` into `## Packet (reused)` rather than retyping from memory). Only a MISSING file or a byte-count DIFFERENCE is re-staged, Read → Write, byte-identical (source `docs/40 - DevDocs/prompts/2026-09-19/21-packet/`). Files already in the folder from earlier rounds (`grok-findings.md`, `REVIEW.md`, `astra-findings-b.md` if it exists, `gemini-findings-b*.md`, `REVIEW-B.md`) are read-only to you — leave them.

## 2. RUN ORDER — ASTRA FIRST
2.1 ONE probe, `run_in_background`, give it 3 minutes: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."` (rule: identical string to `21`'s). Record verbatim, with the wall-clock time.
2.2 If the probe is METER (same usage-limit text) or any other denial: record `astra: METER` (or the exact denial), do NOT retry, proceed straight to §3 (Gemini). This is a correct, non-failing outcome — the desk reruns Astra later.
2.3 If the probe is UP (`OK`, exit 0, no usage-limit text): launch the FULL-PACKET read at once, ONE attempt, `run_in_background`, no artificial timeout shorter than the tool's own: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "You are GPT-6 Astra, the cross-house reviewer. Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/ . Start with QUESTIONS.md and follow it exactly. Do NOT run any command other than reading those files and do NOT write any file: print your complete findings as your answer."` → write its printed answer, byte for byte, to `astra-findings-c.md`. This packet is roughly 215,000 tokens (the first Astra run today metered out at 136,000 tokens of a smaller packet, per the archiver check's record) — if Astra's own output states a token or context count at any point (start, mid-stream, or in a usage-limit message), quote it verbatim in `## Astra`. A usage-limit message mid-read → record `astra: METER (mid-read, <tokens if stated, else "not stated">)`, keep whatever partial text printed before it (marked partial), and do NOT retry — one attempt only, per the standing rule.

## 3. GEMINI — test half only, in THREE separate balanced calls, wording that WORKED for call A
`REVIEW-B.md`'s `## Gemini` section: the whole-packet attempt and the whole-test-half attempt (`code-08` through `code-13` together, ~345,000 bytes with `QUESTIONS.md`) both HARNESS'd; only the SOURCE half (`code-01`–`07` + both specs + both build reports + `facts.md` + `QUESTIONS.md`, ~527,000 bytes — LARGER than the failing test half) printed cleanly. Byte size alone does not explain the pattern (the source half was bigger and worked); `REVIEW-B.md` itself flags this as unexplained ("test diffs embed fixture-style content and long real tickers — cause unknown, medium-low confidence either way"). The untried lever is a finer split of the test half — this round does that, in three parts, using CALL A's wording (not call B's — round 1B's own note: "if they differed in anything but the file list, use call A's").

Byte sizes (from `21`'s SIZE TABLE): `code-08-tests-radar.diff` 32,570 · `code-09-tests-replay-a.diff` 65,033 · `code-10-tests-replay-b.diff` 56,812 · `code-11-tests-smoke-tenancy-settings-finviz-jobs.diff` 93,448 · `code-12-tests-cards-p4.diff` 62,607 · `code-13-fixtures-cutter-script.diff` 25,996 (sum 336,466). Balanced groups (each also carries `QUESTIONS.md` 9,158 + `facts.md` 18,867 = +28,025 overhead):
- **Part 1**: `code-08-tests-radar.diff` + `code-09-tests-replay-a.diff` = 97,603 (+ overhead = 125,628)
- **Part 2**: `code-10-tests-replay-b.diff` + `code-12-tests-cards-p4.diff` = 119,419 (+ overhead = 147,444)
- **Part 3**: `code-11-tests-smoke-tenancy-settings-finviz-jobs.diff` + `code-13-fixtures-cutter-script.diff` = 119,444 (+ overhead = 147,469)

Question routing, checked against the real files (`grep -l "test_smoke_k3_sql\|test_k3_hold_row_reads_red" code-*.diff` → the test lives in `code-11`, NOT `code-12` as `QUESTIONS.md`'s Q3 guessed with "or wherever it lands"): Q3 needs `code-11` → **Part 3 only**. Q9 needs BOTH `code-10` AND `code-12` (the two fixtures' test coherence) → **Part 2 only**, which is why `code-10`+`code-12` are grouped together rather than split. Q8 ("spot-check at least three tests across `code-08` through `code-12`") is attempted by ALL THREE parts against whatever tests they hold — three parts spot-checking ≥1 each satisfies "at least three" in total. Every part marks any question its own file list cannot answer as `NOT IN THIS PART` rather than guessing.

3.1 Each part, `run_in_background`, rule `Bash(agy *)`, same flags as every prior Gemini call (`--model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial`), ONE attempt each, NO retries:

`agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You have NO tools for this task except reading local files already present in one folder. Never run a command, never list a directory, never fetch a URL, never search the web — if answering would require any of those, write UNVERIFIABLE FROM PACKET for that item and continue; do not attempt the action first. Read ONLY these <N> files, one at a time, all in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/: QUESTIONS.md, facts.md, <part's diff files>. This is PART <k> of a 3-way split of the test-code half of a larger packet; you are answering ONLY the questions your files can settle. For every other question in QUESTIONS.md, write NOT IN THIS PART. <part-specific instruction below>. Start with QUESTIONS.md, read the part-specific instruction, then follow QUESTIONS.md's severity/evidence format exactly for whatever you can answer. Do NOT write any file: print your complete findings as your answer."`

Part-specific instruction to append to each of the three prints (else identical to the template above and to call A's wording):
- **Part 1** (`code-08`, `code-09`): "Attempt Q8 (spot-check at least one test in these two files against the build report's claim for it) fully; mark Q3 and Q9 NOT IN THIS PART (their files are not here)."
- **Part 2** (`code-10`, `code-12`): "Attempt Q9 fully — both files it needs are here. Attempt Q8 (spot-check at least one test). Mark Q3 NOT IN THIS PART (its file, `code-11`, is not here)."
- **Part 3** (`code-11`, `code-13`): "Attempt Q3 fully — `code-11` contains `test_k3_hold_row_reads_red_documented_ambiguity` (confirmed by grep before this run; QUESTIONS.md's own guess of `code-12` is wrong, ignore it). Attempt Q8 (spot-check at least one test). Mark Q9 NOT IN THIS PART (it needs `code-10`, not here)."

Write each printed answer, byte for byte, to `gemini-findings-c-part1.md` / `-part2.md` / `-part3.md`. A part that HARNESSes is reported as such with its verbatim output in `## Gemini (test half, 3 parts)` — no retry of that part, no fallback wording, no further split. If ALL THREE HARNESS, `gemini-tests: HARNESS (3/3 parts)` in the close line.

## 4. Verify — `21`'s rules only, nothing new
Every BLOCKER/MAJOR from Astra and/or any Gemini part against the REAL files (`git -C /Users/cobalt/cobalt show sprint-2/p4:<path>`, `/Users/cobalt/cobalt-wt/s2-p4/` with Read/`grep`), identically to `21`'s and `27`'s §4. A finding already carried in `REVIEW.md` or `REVIEW-B.md` (Grok's or Gemini source-half's) is cited by reference, not re-verified. A claim needing an uncovered command → `UNVERIFIABLE FROM READS`, name the exact missing rule, do not invent one (same as before: `Bash(uv run pytest *)`, `Bash(uv run cobalt jobs restarts *)`). One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-p4-0919/`; within it you only WRITE `REVIEW-C.md`, `astra-findings-c.md`, and `gemini-findings-c-part{1,2,3}.md`.

## 5. Close
`## Verdict table` combines: Grok round 1 (carried by reference) + Gemini source-half round 1B (carried by reference) + this round's Astra and/or Gemini-parts findings. Last line, beginning EXACTLY with `P4 CHECK C`:
`P4 CHECK C · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> (tokens: <n or "not stated">) · gemini-tests: <part1 status>/<part2 status>/<part3 status> · houses that have now read the WHOLE build: <n> of 3 (count Gemini as a whole read only if all three test parts answered, i.e. no part HARNESSed) · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u> · Q3: <answered|unanswered> · Q8: <answered|unanswered> · Q9: <answered|unanswered>`. Then stop.
