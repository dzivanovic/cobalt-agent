MODEL: Opus 5 (`claude-opus-5`) — same tier as the round-1 build (item c/d touch the migration harness, L29 floor) · SEAT: ops hub `ops-0919-r2`, launched by the CTO desk in the background, SAME worktree as round 1 (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: every rule below is copied byte for byte from `docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md`'s launch line (the round-1 build this session is folding findings into) — zero added, zero altered; dropping was considered and none were dropped, because R10's re-run (below) needs the same `Bash(uv run pytest *)` and `Bash(uv run cobalt jobs restarts *)` rules round 1 already had. THIS RUN IS OFFLINE BY DESIGN, same as round 1: `cobalt_dev` is not this session's to touch. Two bare commands, run by the desk: `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/19-ops-round2.md' and follow it exactly." --model claude-opus-5 --remote-control ops-0919-r2 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `cobalt_dev`, no `.env`, no vault write, no other seat's config · METER: Anthropic medium; a usage-limit message is not a failure — record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

# Ops 2026-09-19, ROUND 2 — fold the three-house check of the round-1 build. Four small fixes, tests first where a code path can run offline, OFFLINE: (R2) test (g) `cancel`/rejoin/close must run in the `finally` on EVERY path, not only the hung one · (R3) the "20 s join fires first" claim — make the ordering true or make the docstring say what is actually guaranteed, smallest honest fix, your call, name which you chose · (R5) the lock-timeout message's diagnostic query must find the HOLDER, not ungranted waiters, and must still make sense after this transaction's own rollback · (R6)–(R9) four report-text corrections in `docs/40 - DevDocs/reports/ops-2026-09-19.md`, no code impact. CARRIED, NOT YOURS: R1 and R11 (the `SET LOCAL` placement — rides with the owed `cobalt_dev` experiment another session holds) and R4 (NOT REAL, no change). DO NOT STOP until the report ends `OPS 0919 R2 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval. This launch line adds NO rule beyond round 1's: `grep -c` each of the 16 `Bash(...)` rule strings above, plus `--disallowedTools "AskUserQuestion" "EnterWorktree"` and the three `--add-dir` values, against `docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md` — every one must count 1 (round 1's own AUTHORIZATION section did the identical check against `02-ops-2026-09-18.md` — same shape, one file back). The WORK ITEM is authorized by: `docs/40 - DevDocs/reports/cto-2026-09-19.md` §4 **R2** (findings are folded before the deploy when the houses say FIX FIRST); LAWS L67 (the three-house tribunal check whose findings you are folding — `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-0919/REVIEW.md`, all three houses FIX FIRST, hub-verified 8 REAL / 0 block shipping / 1 NOT REAL / 2 UNVERIFIABLE); round 1's own report `docs/40 - DevDocs/reports/ops-2026-09-19.md` (the file you append to, committed on this branch at `319eaaa`). A rule here not in round 1's committed line, or a row that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded scope: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(ops-0919-r2): <item> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit file paths under `src/`, `tests/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>`.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL → **`/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/19-packet/verdicts.md`** (THE SPEC for what is REAL and what fix is owed — the `## Verdict table`, rows R2/R3/R5/R6/R7/R8/R9 bind you; where your reading of the code differs from a row, the CODE wins and the difference is an ESCALATE line) → the full review, if you want more context than the table gives, at `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-0919/REVIEW.md` (reachable under your `--add-dir /Users/cobalt/cobalt-wt`) → `docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md` (round 1's own prompt, for the conventions this one continues) → `docs/40 - DevDocs/reports/ops-2026-09-19.md` (round 1's report — the file you APPEND to: add your new `## ROUND 2` section and a new stop line AFTER round 1's existing final line, `OPS 0919 fc1d967 …`; do not delete or restructure round 1's sections except the four exact text spots R6–R9 name) → `src/cobalt/db_migrations/cli.py` IN FULL → `tests/cobalt/test_migrate_proof.py` (test (g) `test_the_alter_waits_for_an_open_transaction_and_then_completes`, lines ≈1226–1335, and `test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied`, ≈1489–1510). Laws, one line each: L1 fail loud · L3 one path — no second validator, no third helper · L35 artifact, not claim · L42 RESTARTS derived by the command, never by judgement · L45 real-shape fixtures only, no invented Postgres behaviour · L46 one commit per item, tree clean before you stop · L48 evidence in the report file in the same turn · L52 nothing here touches scoring, ranking or what reaches a card · L54 you work in this worktree only · L57 every printed value replayable. Report (L48): APPEND to `docs/40 - DevDocs/reports/ops-2026-09-19.md` in THIS worktree — a new `## ROUND 2` top-level section: §0 Headline ≤5 lines → `## ROUND 2 PREFLIGHT` → `## ROUND 2 AUTHORIZATION` (the grep-count table) → one subsection per item (R2, R3, R5, R6–R9) → `## ROUND 2 CLOSE` (the R10 re-run) → `## ROUND 2 ESCALATE` → last line. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `uv run cobalt` anything except `jobs restarts`, any `COBALT_ENV=…` command, `launchctl`, `claude`. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim): `date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; branch must be `ops/2026-09-19`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: `319eaaa`, else the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file"; if it exists → `FAILED PREFLIGHT: .env present — this run must not see the dev database` · `uv run pytest --co -q tests/cobalt/test_migrate_proof.py` (collects cleanly — no code change has landed yet). Any denial → `FAILED PREFLIGHT: <rules>`.

## 1. R2 — test (g)'s cleanup must run on every path, not only the hung one
Read `tests/cobalt/test_migrate_proof.py:1226-1335` in full before touching it. Today the `try` body runs `conn.cancel()` + a second `thread.join` ONLY inside `if hung:` (≈1306-1307), and the `finally` (≈1310-1322) closes `conn` only `if not thread.is_alive()` — so an exception raised between `thread.start()` and that `if hung:` check (e.g. from `_wait_for_a_blocked_lock` or `other.commit()`) skips the cancel entirely and can leave the worker's connection alive, holding its ACCESS EXCLUSIVE request, past the end of the test.
FIX: restructure so the `finally` block ALWAYS does — if `thread.is_alive()`: `conn.cancel()` then `thread.join(timeout=WORKER_STATEMENT_TIMEOUT_S)` — before the existing rollback+close, regardless of which branch of the `try` ran or raised. The `try` body's own `if hung: conn.cancel(); thread.join(...)` becomes redundant once `finally` does it unconditionally; remove the duplicate rather than keep two copies (L3). The test's own assertions (`assert not hung`, `assert blocked`, `assert outcome[0][0] == "completed"`, the `_verdict(...) == "OK"` check) are UNCHANGED — this is cleanup-path hygiene, not a behavior change to what the test proves.
THIS IS A `requires_db` TEST: there is no offline red/green for the fix itself (round 1's item d hit the same wall — say so plainly, as it did). Your proof today: `uv run pytest -q tests/cobalt/test_migrate_proof.py` collects cleanly, count unchanged from round 1's close (34 passed, 17 skipped becomes a starting point you re-verify at ROUND 2 CLOSE, not here) — 0 errors, 0 failures. State in the report, verbatim style of round 1's item d: "THE REAL RUN IS OWED" for this fix too — it joins the dev-DB list already carried in ESCALATE 8. Commit: `fix(db-migrate): test (g) closes its worker connection on every path, not only the hung one (tribunal round 1, R2)`. Last line `CONTINUE: step 2`.

## 2. R3 — the "20 s join fires first" ordering
Read the docstring at `tests/cobalt/test_migrate_proof.py:1236-1257` (the "HOW THIS TEST'S CEILINGS RELATE" block) against `verdicts.md` row R3: the docstring states the 20 s `LOCK_CEILING_S` join "fires first" as if unconditional, but a probe that itself takes long enough (the review's example: a third session also contending, and `_wait_for_a_blocked_lock`'s own polling taking >5 s) can let the clock relationship differ from what is claimed. Two honest fixes, your choice — take the smaller one unless the larger is trivial and safe:
(a) DOCSTRING FIX (preferred if the timing change below is not obviously safe): reword the "fires first" sentence to state the guarantee conditionally — it holds when the blocked-lock probe itself returns quickly relative to `LOCK_CEILING_S`, which is the case this test drives (the probe polls at a short interval and returns as soon as the wait is visible in `pg_locks`); it does not hold in general. Say so in one or two sentences, do not invent a bound you have not verified.
(b) CODE FIX (only if you are confident it changes nothing else): start the `LOCK_CEILING_S` deadline from `thread.start()` instead of after `_wait_for_a_blocked_lock` returns and `other.commit()` runs, e.g. record `started = time.monotonic()` immediately before `thread.start()` and join with `timeout=max(0.0, LOCK_CEILING_S - (time.monotonic() - started))`.
Whichever you take, name it explicitly in the report ("R3: took fix (a)/(b), because …") — this is a desk-left design call, not yours to leave silent. This is the SAME `requires_db` test as R2 — no offline red/green here either. Commit (can combine with step 1's commit if the diff is in the same hunk region, otherwise its own): `fix(db-migrate): test (g)'s docstring states the join-vs-probe ordering as what is actually guaranteed (tribunal round 1, R3)` — or `feat(...)` if you took the timing fix. Last line `CONTINUE: step 3`.

## 3. R5 — the lock-timeout message must point at the HOLDER
Read `src/cobalt/db_migrations/cli.py:687-706` (the `except psycopg.errors.LockNotAvailable` block) and the existing test `tests/cobalt/test_migrate_proof.py:1489-1510`. Today's hint text (`:699`) reads `"pg_locks WHERE NOT granted"` — that finds UNGRANTED (waiting) lock requests, not the session holding the lock; and because this transaction has already rolled back by the time an operator would run it, even a request of THIS transaction's own is gone from the table, so the query can return nothing while a real holder exists elsewhere.
TESTS FIRST: add an assertion to `test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied` that is FALSE today and TRUE after the fix — e.g. `assert "NOT granted" not in message` (quote the RED: today's message contains it) — plus a positive assertion naming whatever query text you land on (e.g. `assert "granted" in message.lower()` if you keep `pg_locks`, or `assert "pg_blocking_pids" in message` if you switch to it). Do not remove the existing `assert "pg_locks" in message` unless your fix genuinely drops `pg_locks` entirely (if you switch fully to `pg_blocking_pids`/`pg_stat_activity`, update that assertion too and say so — it is a existing-test edit, name it).
THE FIX (`src/cobalt/db_migrations/cli.py` only, this one except-block's message text): change the diagnostic hint so it finds a lock HOLDER — either flip the filter to `granted` locks on `AccessExclusiveLock` (or all granted modes) instead of `NOT granted`, or replace it with `pg_blocking_pids(pg_backend_pid())` / a `pg_stat_activity` join, whichever reads more honestly given the message is generic (it does not know the specific table name at this point in the code — do not invent a table-scoped query you cannot back). Keep the rest of the message (NOTHING WAS APPLIED, the timeout value, the retry guidance) untouched. This path IS testable offline (`_StatementRecorder`/`_offline_migrate`, no real Postgres) — run RED before the fix, GREEN after, quote both. Commit: `fix(db-migrate): the lock-timeout message points an operator at the lock HOLDER, not ungranted waiters (tribunal round 1, R5)`. Last line `CONTINUE: step 4`.

## 4. R6–R9 — four report-text corrections, `docs/40 - DevDocs/reports/ops-2026-09-19.md`, no code
Each is a wording/count fix only, in round 1's OWN report on this branch. Quote before/after for each in your new `## ROUND 2` section (do not silently edit — show the correction).
- **R6** (line 157): `"13 failed on \`ImportError\`"` → `"12 failed on \`ImportError\`"` (recount: 7 refusal-class + 5 `test_the_validator_accepts_it` = 12; 12+5+1 = 18, matching the summary line at `:154`).
- **R7** (line 8, the §0 Headline OWED bullet, AND the stop line at `:569`): `"1 new + 2 edited \`requires_db\` tests"` → `"1 new + 1 edited"`; the stop line's `"(+2 edited, never re-run)"` → `"(+1 edited, never re-run)"`. ESCALATE 8's own text already says the second test "shares `JOBS_TABLE` but not the helper" — it was never edited; only `test_the_alter_waits_for_an_open_transaction_and_then_completes` was.
- **R8** (line ≈488): `"three ADDITIONAL paths, all as DELETIONS"` → correct to say two deletions and one modification (`docs/40 - DevDocs/reports/cto-2026-09-19.md` is `M` −60 in the RESTARTS table, not a deletion; the other two ARE deletions). Reword the sentence so it stops calling all three deletions.
- **R9** (line ≈130): item a's `"…premarket open… see \`## ESCALATE\` 1"` points at ESCALATE 1, which is the unrelated DevDocs symbol check. Pick ONE: (i) add a new, genuinely new `## ESCALATE` line in round 1's ESCALATE section stating the premarket-before-open case plainly (a trading day before premarket open is NOT in item a's bound; C3 exempts it via `past_open`, C2 does not) and point line ≈130 at that new number instead of `1`; or (ii) drop the dangling "see ESCALATE 1" clause from line ≈130 entirely and simply state the NOT-in-bound fact with no pointer. Say in your ROUND 2 section which you took and why.
Commit all four together (they are the same file, one concern — "report corrections"): `docs(report): ops-2026-09-19 round-1 report — four wording/count corrections (tribunal round 1, R6-R9)`. Last line `CONTINUE: step 5`.

## 5. ROUND 2 CLOSE — settle R10 (this launch line CAN re-run it; do so)
R10 was UNVERIFIABLE FROM READS only because the REVIEW session's own sandbox lacked `Bash(uv run pytest *)` and `Bash(uv run cobalt jobs restarts *)` — round 1's build line HAD both, and yours is a byte-copy of it, so run them now:
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 1's close: `1608 passed, 298 skipped, 0 failed`. Passed should equal 1608 (no new offline tests were added this round — R2/R3 touch a `requires_db` test, R5 adds offline assertions to an EXISTING test rather than a new one, so the passed count should be unchanged unless you added a new test function, in which case name it and the new total); failed must stay 0; skipped must stay 298. Any drift from those three numbers → name it, do not wave it away.
`uv run cobalt jobs restarts fc1d967..HEAD` → the table VERBATIM (branch point for this round is round 1's build tip `fc1d967`); expect only `db_migrations/cli.py`, `test_migrate_proof.py`, and the report among the changed paths — RESTARTS should read the same as round 1's (`com.cobalt.aset com.cobalt.radar`) unless a path's classification differs; 0 UNCLASSIFIED or ESCALATE why.
`git status --porcelain` empty · `git diff --stat fc1d967 HEAD -- src tests docs` listed, every path one this prompt named · report committed by explicit path: `docs(report): ops 0919 round 2 — <one line>`.
`## ROUND 2 ESCALATE` carries forward, untouched, everything round 1's `## ESCALATE` already carried (b2 design call, item f archiver disarm, the `requires_db` tests never run — now three of them: R2's and R3's fix plus round 1's original two — and the cross-branch note), PLUS whatever this round found new. Then the last line, exactly this shape:
`OPS 0919 R2 <tip> | on ops/2026-09-19 (round 1 <fc1d967>) | offline <passed>/<failed> (<skipped> skipped; round-1 close 1608/0/298) | R2 finally-block <fixed|FAILED> | R3 ordering <fix (a) docstring|fix (b) timing|FAILED> | R5 holder query <fixed|FAILED> | R6 <fixed> | R7 <fixed> | R8 <fixed> | R9 <ESCALATE line added|pointer dropped|FAILED> | RESTARTS: <line> | CARRIED: R1 SET LOCAL placement (owed cobalt_dev experiment), R11 (same experiment), R4 no change | OWED: cobalt_dev run of R1/R2/R3's requires_db tests + round 1's two, next three-house check on this diff, then the deploy prompt | ESCALATE: <n>`
Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or `cobalt_dev`.
