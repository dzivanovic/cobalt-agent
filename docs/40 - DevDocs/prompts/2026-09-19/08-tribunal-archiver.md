MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, put one packet before three houses, collate their rulings; no write path, no verdict of your own (L37) · SEAT: tribunal hub `tribunal-0919a`, launched by the CTO desk in the background. NO NEW RULE: `01-review-harness.md`'s allowlist with one `mkdir` path changed (`Bash(grok *)`, `Bash(agy *)` = R9 of 2026-09-18, standing through 09-20; the Codex read-only rule = R1 of 2026-09-19). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/08-tribunal-archiver.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control tribunal-0919a --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/tribunal-archiver-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless ruling per house · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# FOUR-HOUSE TRIBUNAL, round 1 — DESIGN: the Bar Archiver becomes append-only (LAWS L67: one house designs and proposes; Astra, Grok, Gemini and Fable rule on it and derive the final version; NOTHING is built before that). Proposer = Fable (the CTO desk): `08-packet/PROPOSAL.md`. You put the SAME packet before Astra, Grok and Gemini, independently, and collate what they rule — point by point, agreement and dissent verbatim. Fable's own ruling and the derived final version are the desk's next step, not yours. DO NOT STOP until the report ends `TRIBUNAL ROUND 1 DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words, committed on main: `cto-2026-09-18.md` §4 **R14 + R15** (the archiver appends only what is new; no nightly rewrite), **R9**, **R18–R21** (L67); `cto-2026-09-19.md` §4 **R1** (the Codex read-only rule), **R5** ("The design needs to be reviewed by a tribunal and then built and then deployed."). Prove with `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws (L33, L35, L36, L37, L44, L47, L67), UNATTENDED RULES, PREFLIGHT (Codex: `scratch/review-harness-0919/REVIEW.md` shows Astra answered at 06:55 today → skip the probe, record `astra: UP (carried)`; a usage-limit message on the real run → `astra: METER`, continue with two houses and SAY SO in the last line — the desk then decides under L67) , LESSON (a reviewer cannot run anything; everything it needs is a staged file) and §2 launch spellings bind you UNCHANGED, with `scratch/tribunal-archiver-0919/` in every path and the output files named `grok-ruling.md`, `gemini-ruling.md`, `astra-ruling.md`. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-archiver-0919/TRIBUNAL.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Rulings table` → `## Amendments, verbatim` → `## Checked against the files` → last line. You commit nothing.

## 1. Stage the packet in `scratch/tribunal-archiver-0919/` (`mkdir -p` first; Read → Write, byte-identical)
From `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/08-packet/`: (1) `PROPOSAL.md` · (2) `facts.md`. From `/Users/cobalt/cobalt/` (the code on main): (3) `src/cobalt/archiver/store.py` → `store.py` · (4) `src/cobalt/archiver/runner.py` → `runner.py` · (5) `src/cobalt/radar/poller.py` → `poller.py` · (6) `src/cobalt/archiver/migrations/0001_bars.sql` → `0001_bars.sql` · (7) `tests/cobalt/test_archiver_store.py` → `test_archiver_store.py` · (8) `finviz-windows.md` = `docs/30 - Design/DATA-SOURCE-MEMO.md` sections 2a and 2b (the `p=` table and the date-parameter result), verbatim · (9) `owner-rulings.md` = rows R13, R14, R15 of `docs/40 - DevDocs/reports/cto-2026-09-18.md` §4, verbatim · (10) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): "You are one of four houses sitting as a design tribunal for a trading-support system that runs on one always-on machine with a Postgres 16 database. Read ONLY the files in this folder; you cannot run commands. `PROPOSAL.md` is a design proposed by one house. Your job is to RULE on it, not to admire or to rewrite it: for EACH decision point D1 to D9 in its section 4 give exactly one of ACCEPT / AMEND / REJECT, then your reasoning in at most six lines, then — for AMEND — the exact replacement wording of that design point, and for REJECT the alternative you would build instead. `owner-rulings.md` holds the owner's own words; a design that does not satisfy them is wrong however elegant — say so if the proposal drifts from them, and say so equally if one of his assumptions is unsafe (name the concrete failure). `facts.md` and the code files are the ground truth about today's system; if the proposal misstates them, that is a finding. Be concrete: name the failing scenario (a ticker, a time, a sequence of events) for every objection. Pay particular attention to: (a) the interaction between the nightly archiver and the intraday poller writing the same (ticker, i1) keys on the same day; (b) what happens the night after a failed or partial run; (c) a stock split or a vendor restatement of past bars; (d) daylight-saving changes and half-days; (e) bars for intervals other than i1, whose last bar of the day closes well after it opens; (f) whether anything here makes a later move to a partitioned table, or to storing only i1 and deriving the rest, harder. After D1–D9 add `MISSING:` — anything a sound design of this change needs that the proposal does not contain (tests that must exist, a migration, an operator command, a metric). End with exactly one line: `RULING: BUILD AS PROPOSED / BUILD WITH MY AMENDMENTS <D-numbers> / DO NOT BUILD — REDESIGN <why in ten words>`."

## 2. Launch the three houses — `01-review-harness.md` §2.1, 2.2, 2.3 EXACTLY, with this folder and the three output names above; the instruction sentence inside each launch stays "Start with QUESTIONS.md and follow it exactly." ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped. Do not pre-read the proposal for defects yourself (2.4).

## 3. Collate — you judge NOTHING (L37), you check facts (L35)
`## Rulings table`: one row per decision point D1–D9 + MISSING: `point · grok · gemini · astra · agreement (3-0 accept / split / 3-0 amend …) · the amendment(s) in ≤25 words each`. `## Amendments, verbatim`: every AMEND / REJECT text, house by house, unedited. `## Checked against the files`: every claim a house makes about TODAY'S code or data that you can check against the staged files or the real files on main — `claim · house · file:line · TRUE / FALSE / NOT CHECKABLE FROM READS`. A house's claim about Postgres or Finviz behaviour that files cannot settle = NOT CHECKABLE, with the experiment that would settle it.

## 4. Close
Last line: `TRIBUNAL ROUND 1 DONE · grok: <RULING line|HARNESS|METER|TIMEOUT> · gemini: <…> · astra: <…> · houses that ruled: <n> of 3 · points 3-0 ACCEPT: <list> · points with an AMEND or REJECT: <list> · false claims about the code: <n>`. Then stop.
