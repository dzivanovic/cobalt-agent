MODEL: Opus 5 (`claude-opus-5`) — the session builds itself, no headless builders. Same tier and shape as round 1 (`16-ops-2026-09-19.md`) and round 2 (`19-ops-round2.md`): L29 floor on the migration harness, and the DB step itself is ALWAYS hub-direct, never delegated to a builder — this is what `13-p4-verify.md` step 4 also did ("no builder is launched inside step 4"). NO SONNET-HUB/OPUS-BUILDER SPLIT here: unlike 13, this run has no rebase and no parallel chunks (Asset Type / triage rows) that needed isolating into separate headless launches — it is one coherent fold on two files (`src/cobalt/db_migrations/cli.py`, `tests/cobalt/test_migrate_proof.py`) plus a report append, exactly the shape rounds 1 and 2 already used direct-build for. SEAT: ops hub `ops-0919-db`, launched by the CTO desk in the background, SAME worktree as rounds 1 and 2 (it already exists — do NOT create it, do NOT `git worktree add`). THIS RUN TOUCHES `cobalt_dev`: it is a `.env`-present, `COBALT_ENV=dev` run, unlike rounds 1 and 2 which were offline by design; `cobalt_dev` is FREE (`p4-verify-0919`'s stop line: `cobalt_dev: 0001–0009`, READY, `.env` removed there — verify yourself, PREFLIGHT). Two bare commands, run by the desk: `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/25-ops-db-run.md' and follow it exactly." --model claude-opus-5 --remote-control ops-0919-db --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `--allow-prod`, no vault write, no other seat's config; `cobalt_dev` and `.env` ARE this session's to touch, scoped exactly to the two new rules above · METER: Anthropic medium; a usage-limit message is not a failure — record the reset time, and BEFORE stopping remove `.env` if present and prove it gone (RECOVERY RULE applies to the meter stop too — a `.env` left behind on a meter stop is the one failure mode worse than the wait), commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

# Ops 2026-09-19, DB RUN — close the branch's three carried debts on real `cobalt_dev`: (R1/R11) does `SET LOCAL lock_timeout` take/move the REPEATABLE READ snapshot — settle it by experiment, then fix or record accordingly · (F1) test (g)'s `finally` nests the worker cleanup under EVERY exception, not just `other`/`watcher`'s own close path · (F3/F4/F5) three report-text folds from the round-2 review · then the branch's owed `requires_db` tests get their FIRST REAL RUN. LAW STEP of this work item (L67): BUILD + the DB proof this branch has owed since round 1. After it, none of it yours: at least three houses check this diff · Dejan's approval · the branch rides the next deploy prompt. You never merge, never deploy, never push, never touch production, never `--allow-prod`. DO NOT STOP until the report ends `OPS 0919 DB …`, `CONTINUE: … — meter …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable), not by Dejan; a prompt file is not an approval. Two rule families here:
(a) The 16 `Bash(...)` rules carried from round 1/round 2 unchanged, plus `--disallowedTools "AskUserQuestion" "EnterWorktree"` and the three `--add-dir` values: `grep -c -F '<rule>' docs/40\ -\ DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md` (from THIS worktree, path relative to `/Users/cobalt/cobalt`, so `git -C /Users/cobalt/cobalt` reads it — or read it directly, it is reachable under `--add-dir /Users/cobalt/cobalt`) for each of the 16 + 2 + 3 strings — every one must count 1. These need no new approval: they are round 1's own line, already run twice on this branch.
(b) FOUR rules that are NEW to this branch — `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)`, `Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)`, `Bash(COBALT_ENV=dev uv run cobalt db migrate*)`, `Bash(COBALT_ENV=dev uv run pytest *)`. The LAST TWO are not new to Cobalt: `grep -c -F 'Bash(COBALT_ENV=dev uv run cobalt db migrate' docs/40\ -\ DevDocs/prompts/2026-09-19/13-p4-verify.md` and the same for `Bash(COBALT_ENV=dev uv run pytest` must each count 1 — these are 13's own approved strings, path-agnostic (no worktree path inside the string), reused verbatim on this worktree. The FIRST TWO (`cp`/`rm` of `.env`) are the same SHAPE as 13's `.env` rules but a DIFFERENT PATH (this worktree, not `s2-p4`) — that is why they are typed as NEW rather than counted against 13's file; they are the minimum L4/L41 credential-bootstrap shape (named-file copy/remove, never `cat`/print) applied to this worktree. If Dejan has not separately approved this exact prompt's launch line (his "approve." on the CTO desk's authorization row, named with a time, in `docs/40 - DevDocs/reports/cto-2026-09-19.md` or a later dated report) → `FAILED: authorization mismatch — no approval row for 25-ops-db-run.md`, commit nothing, stop before PREFLIGHT.
YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line — but FIRST run `.env` cleanup (see RECOVERY RULE) — then wip-commit, stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(ops-0919-db): <item> partial — <why>"`, last line `CONTINUE: step <n>`. EVERY `git add` names explicit paths under `src/`, `tests/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named. `.env` DISCIPLINE (L4, L41 — the one rule that overrides every other stop order): on ANY stop — `FAILED`, `CONTINUE`, meter, or the normal end — `ls -la .env` FIRST; if it exists, `rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env` THEN `ls -la .env` again to prove it gone, THEN write whatever stop line you were going to write. A stop line written while `.env` still exists on disk is itself a `FAILED` condition — go back and remove it, then re-open the report and fix the line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you touch `cobalt_dev` directly, L4/L41/L67) → **`/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/25-packet/db-run-spec.md`** (THE SPEC for the R1/R11 experiment and the F1/F3/F4/F5 folds — binding; where your reading of the code differs from it, the CODE wins and the difference is an ESCALATE line) → `docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md` and `19-ops-round2.md` (the conventions this run continues; round 2's own review is quoted in the spec) → `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-r2-0919/REVIEW.md` (the full round-2 tribunal check the spec's F1/F3/F4/F5 are drawn from — reachable under `--add-dir /Users/cobalt/cobalt-wt`) → `docs/40 - DevDocs/reports/ops-2026-09-19.md` in THIS worktree (the file you APPEND to — a new `## DB RUN` top-level section, AFTER `## ROUND 2`'s existing final line `OPS 0919 R2 …`; never delete or restructure existing sections except the exact spots F4/F5 name) → `src/cobalt/db_migrations/cli.py` IN FULL, esp. `_connect` (≈:499-548) and `cmd_migrate` (≈:556-660) → `tests/cobalt/test_migrate_proof.py` §§8-10 (≈:761-1372, plus the new §10 you add) → `docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md` §4 (the `.env`/`COBALT_ENV=dev` command shape this run's DB commands copy) → `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md:166` (the `READY … cobalt_dev: 0001–0009` line — read-only evidence that the lane is free and what it holds; you do not re-derive 0008/0009's presence by any other means — there is NO migration ledger table in this codebase, `cli.py:641` uses the hardcoded `FORWARD` tuple unconditionally, `__init__.py:41-49`, so this branch's own harness cannot see 0008/0009 either way; this report line is the only evidence available without inventing a new SQL-introspection rule, and none is granted here). Laws, one line each: L1 fail loud · L3 one path — the nesting in F1 removes the old duplicate copy, never keeps two · L4 secrets discipline — `.env` by name only, never `cat`/printed, the three-tier bootstrap scope (Postgres/app DB creds) is what this touches and nothing else · L29 Opus floor on the migration harness · L35 artifact, not claim — the desk verifies via `git show`/`git diff`, never your report's prose alone · L41 cross-house credential law — INTERIM: `.env` copied by name only; this session runs the DB-backed proof directly (hub-shaped, no worker), never hands a worker DB access · L45 real-shape evidence — the R1/R11 answer is proved on real Postgres, never asserted from the manual you could not read offline · L46 one run, one commit boundary; small commits within it (CLAUDE.md) · L48 evidence in the report file, same turn · L52 nothing here touches scoring, ranking, or what reaches a card · L54 you work in this worktree only · L57 every printed value replayable · L60 you hold this session until the stop line · L67 this build still needs its three-house check after you stop. Report (L48): APPEND to `docs/40 - DevDocs/reports/ops-2026-09-19.md` in THIS worktree — a new `## DB RUN` section: §0 Headline ≤5 lines → `## DB RUN PREFLIGHT` → `## DB RUN AUTHORIZATION` (the two-family check above) → one subsection per item (R1/R11, F1, F3, F4, F5) → `## DB RUN — WITH-DB SUITE` → `## DB RUN — OFFLINE SUITE + RESTARTS` → `## DB RUN ESCALATE` → last line. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `uv run cobalt` anything except `jobs restarts` and `db migrate` (and `db migrate` ONLY with `COBALT_ENV=dev` in front, never bare, never `--allow-prod`), any `COBALT_ENV=production` command, `launchctl`, `claude`, `cat`/`less`/`more`/anything that could print `.env`'s contents. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, `.env` cleanup, wip-commit, stop. Never retry a denied command in another shape.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim):
`date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; branch must be `ops/2026-09-19`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: `d860bf7`, else the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file" (if it exists → `FAILED PREFLIGHT: .env present before this run started — remove it and say who left it, this is not yours to inherit silently`) · `ls -la /Users/cobalt/cobalt-wt/s2-p4/.env` → MUST say "No such file" (this is the signal that `p4-verify-0919` finished cleanly and is not mid-DB-work; if it EXISTS → `FAILED PREFLIGHT: s2-p4's .env is present — another session may hold cobalt_dev, do not proceed`) · Read (not Bash) `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md`'s last `READY`/`FAILED` line and quote it verbatim (expected: `READY 4cc6859 … cobalt_dev: 0001–0009 …`) · `grep -rn "cobalt_dev" "/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/"` dated after that READY line (by filename date) and read (Read tool) any hit to confirm no LATER session claimed the lane (the known one, `s2-p4-fund-rule-r16` / `e79d55d`, states in its own text "`.env` never copied this run" — quote it; a hit that is NOT that report and DOES claim `.env`/`cobalt_dev` → `FAILED PREFLIGHT: a later session may have touched cobalt_dev — <what you found>`) · `uv run pytest --co -q tests/cobalt/test_migrate_proof.py` (collects cleanly — no code change has landed yet). Any denial → `FAILED PREFLIGHT: <rules>`.

## 0. STATE PROBE — allowlist proof + the "what does `cobalt_dev` hold" line (L62: probe the rules with no harmless variant, once, before real use)
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env` → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (applies NOTHING; quote the FULL output — every table THIS branch's harness knows, its placement, row COUNT, digest, and the trailing `code:` line) → `rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env` → `ls -la .env` proves it gone. Read the `--proof-only` output against `placement.py`'s tables (0001-0007's set only — this branch's `FORWARD` does not know 0008/0009, `__init__.py:41-49`): every table `OK`/present as expected, none `CHANGED` (nothing was applied), no error. State the terminal line for this step: `cobalt_dev holds (this branch's view): 0001–0007 objects OK, <n> rows in <largest table>; 0008/0009 objects not in this branch's proof (expected — see INDEX CARD)`. If ANYTHING here reads `CHANGED`, errors, or the `--proof-only` output does not parse → `.env` cleanup, `FAILED: step 0 — <line>`, stop. Last line `CONTINUE: step 1`.

## 1. R1/R11 — the `SET LOCAL lock_timeout` / REPEATABLE READ snapshot experiment
Follow `25-packet/db-run-spec.md` §A exactly: `cp .env` first, build and run the new `@requires_db` test in `tests/cobalt/test_migrate_proof.py` proving which way the snapshot behaves (Session A opens `cli._connect`'s real REPEATABLE READ transaction, issues ONLY `SET LOCAL lock_timeout`, Session B commits an observable change via `xmin`, Session A's first real query shows which snapshot it got). Branch on the result exactly as the spec says: SAFE TO MOVE → move `SET LOCAL` ahead of the BEFORE probe in `cmd_migrate` (both forward and rollback), add the probe-coverage test (another session holding `ACCESS EXCLUSIVE` makes a short `--lock-timeout-s` run fail in about that many seconds INCLUDING the probe, not stubbed), update the docstring with the proof; CANNOT BE COVERED → leave placement, update the docstring with the proof, no new probe-coverage test. State which branch you took and why in the report, verbatim style: "R1/R11: SET LOCAL <does/does not> take the snapshot — proved by `<test name>`". `rm .env` is NOT run yet — steps 2-3 are offline (no DB needed for F1/F3/F4/F5's own work), but you will re-need `.env` for step 4, so either remove-and-reprove now (cleanest — matches PREFLIGHT's pattern) or leave it and say so explicitly in the report if you keep it open across steps 2-3 (never leave the report silent about `.env`'s presence at ANY step boundary). Commit: `fix(db-migrate): SET LOCAL lock_timeout <moved ahead of the BEFORE probe|placement proved correct> — R1/R11 settled on cobalt_dev (ops DB run)`. Last line `CONTINUE: step 2`.

## 2. F1 — nest test (g)'s `finally` block
Follow `25-packet/db-run-spec.md` §B exactly: nest `other.commit()/close()`, `watcher.close()` under their own `try`, with the existing worker cleanup (`cancel`/`join`/`rollback`/`close`) moved to the outer `finally` so it runs regardless of what the inner block raises. Remove the old duplicate `if hung:` copy if any trace of it remains (L3 — round 1's R2 should already have removed it; if it did not, that is itself an ESCALATE line, fix it here too). This is the SAME `requires_db` test as step 1 — no offline red/green for the nesting itself; say so plainly. Commit: `fix(db-migrate): test (g)'s finally nests other/watcher cleanup so the worker cleanup always runs (tribunal round 2, F1)`. Last line `CONTINUE: step 3`.

## 3. F3 / F4 / F5 — report-text folds, no code
Follow `25-packet/db-run-spec.md` §§C-E exactly, in round 2's own OWN report file (`docs/40 - DevDocs/reports/ops-2026-09-19.md`), inside your NEW `## DB RUN` section (never edit round 1's or round 2's existing text in place beyond the two exact F4/F5 appends the spec names — quote before/after for every change, same convention R6-R9 used). F3 gets NO edit to round 1's `## ESCALATE` list — record its disposition only ("ACCEPTED AS DISCLOSED, no edit"). F4: append `[R7 corrected in round 2]` to round 1's stop line (`:577`), nothing else on that line changes. F5: append the conditional note to BOTH `:311` and `:528`, worded to fit each sentence. Commit all three together (one concern, report text): `docs(report): ops-2026-09-19 — F3/F4/F5 report-text folds (tribunal round 2)`. Last line `CONTINUE: step 4`.

## 4. THE WITH-DB SUITE — this branch's owed `requires_db` tests, real Postgres, `cobalt_dev`
SCOPE, deliberately narrow (Task-1 finding: this file's `requires_db` tests are hardwired to `env.DEV_DB_NAME` regardless of `COBALT_ENV`, never create/drop their own tables/schemas — they read and write real pre-existing rows of `cobalt_jobs` and `cobalt_redactions` and clean up after themselves — and this branch's own migrate harness only knows 0001-0007, so it neither reads nor writes anything 0008/0009 added; running it does not require the `sprint-2/p4` rebase). Repo-wide `requires_db` tests OUTSIDE this file (e.g. `test_jobs.py`, `test_cards.py`, `test_tenancy.py`) are OUT OF SCOPE here — not verified by this run, not authorized by this prompt; leave them skipped (do not run the whole `tests/cobalt` tree with `.env` present in this step).
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env` → `COBALT_ENV=dev uv run pytest -q tests/cobalt/test_migrate_proof.py` → quote the FULL summary line (`<p> passed, <s> skipped, 0 failed` or the reds, named). Every test that was previously `@requires_db`-skipped in this file now runs for real, INCLUDING: test (g) as fixed by F1, its R2/R3-round-1 fixes, the `(j)` test `test_a_migration_that_cannot_get_its_lock_fails_in_about_a_second` (never run before today), the R1/R11 experiment test and (if built) its probe-coverage test from step 1, and every other `@requires_db` test already in this file (§§2, 3, 8, 9). A red: name the test, quote the assertion, state whether it is a TEST-SIDE defect (fix it here, tests-first, its own commit) or a genuine finding about the harness/`cobalt_dev`'s real state (ESCALATE, do not "fix" real data — same rule 13's step 4.5 followed). `cobalt_dev` must be UNCHANGED in content after this step (every test that writes cleans up, and the migrate-harness tests that actually run `_apply` only re-apply already-applied idempotent SQL or roll back on failure by design) — if you have reason to believe otherwise, say so explicitly, do not assume. `rm .env` → `ls -la .env` proves it gone (do NOT skip this because step 5 is next — step 5 is offline). State the terminal line: `cobalt_dev holds (after this step): 0001–0009, unchanged`. Last line `CONTINUE: step 5`.

## 5. OFFLINE SUITE + RESTARTS
With `.env` confirmed absent (re-check `ls -la .env` if any doubt): `uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 2's close `1608 passed, 298 skipped, 0 failed` — passed MUST still be 1608 (F1/F3/F4/F5 touch existing tests/text, no new test function; the R1/R11 work adds ONLY `@requires_db` tests, which skip offline) — skipped = 298 + (1 if you took the CANNOT-BE-COVERED branch, +1 new test only; 2 if SAFE-TO-MOVE, +1 experiment test +1 probe-coverage test) — name the new tests by function name and step; failed MUST stay 0. Any drift from those rules → name it, do not wave it away. `uv run cobalt jobs restarts d860bf7..HEAD` → the table VERBATIM; expect no NEW resident beyond round 1/2's `com.cobalt.aset com.cobalt.radar` (the migration harness is a one-shot CLI, not a resident — round 1 already established this classification); 0 UNCLASSIFIED or ESCALATE why. `git status --porcelain` empty · `git diff --stat d860bf7 HEAD -- src tests docs` listed, every path one this prompt or the packet named · report committed by explicit path: `docs(report): ops 0919 db run — <one line>`.

## 6. DB RUN CLOSE
`## DB RUN ESCALATE` carries forward, untouched, everything round 1's and round 2's ESCALATE sections already carried (b2 design call, item f archiver disarm, the cross-branch note, F3's disposition from step 3, R4 no change), PLUS whatever this run found new (a real red from step 4, any drift from step 5's expected counts, an authorization mismatch). State plainly whether the R1/R11, F1, F3, F4, F5 debts are now CLOSED (they should all be, one way or another, after steps 1-3) and whether the with-DB suite is now GREEN (step 4). `MEMORY:`/`RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, exactly this shape:
`OPS 0919 DB <tip> | code d860bf7 | offline <passed>/<failed> (<skipped> skipped vs round-2 298) | db <passed>/<failed> (tests/cobalt/test_migrate_proof.py only) | R1/R11 <SET LOCAL does/does not take the snapshot — moved|placement kept> | F1 <nested|FAILED> | F3 <disposition recorded> | F4 <marked|FAILED> | F5 <marked|FAILED> | cobalt_dev: 0001–0009, unchanged | .env: removed, proven gone | RESTARTS: <line> | OWED: three-house check on this diff, next deploy prompt | ESCALATE: <n>`
or `FAILED: <step> — <reason>` (with `.env` cleanup proven first, always). Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or with `--allow-prod`.
