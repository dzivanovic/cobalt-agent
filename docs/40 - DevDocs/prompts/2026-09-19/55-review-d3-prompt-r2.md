MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify their findings; no write path · SEAT: review hub `review-d3-prompt-r2-0919`, launched by the CTO desk in the background. NO NEW RULE: `51-review-deploy3-build.md`'s `--allowedTools` list — itself `41-review-deploy-p4.md`'s, itself `18-review-ops-0919.md`'s, byte for byte — copied unchanged, with exactly THREE strings different and nothing else: the prompt file name, the `--remote-control` name (`review-deploy-p4-r2-0919` → `review-d3-prompt-r2-0919`) and the `mkdir` path (`scratch/review-deploy-p4-r2-0919` → `scratch/review-d3-prompt-r2-0919`). Round 2 needed nothing outside that list and round 3 is strictly smaller. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/55-review-d3-prompt-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-d3-prompt-r2-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-d3-prompt-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47). **ASTRA:** its Codex meter returned a usage limit at 13:06 ET and again at 13:5x with "try again at 4:54 PM". Probe it ONCE (`codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."`, `run_in_background`): if the probe comes back UP, Astra reads the fold too and becomes the third house; if it returns a usage limit, record `astra: METER` verbatim and end with two houses — a normal ending, L67's floor met twice over. Do NOT retry, do NOT wait for the reset.

# Tribunal check of a DESK DEPLOY PROMPT, **ROUND 2** — `53-deploy-d3.md`, DEPLOY 3 (LAWS L67, ≤3 rounds per house; 09-18 R8)
Round 1's stop line: `D3 PROMPT CHECK R2 · grok: DO NOT RUN 2 (call B; call A HARNESS) · gemini: no verdict line (A: 1 BLOCKER; B: 1 BLOCKER + 3 Q7 breaks) · astra: METER · hub-verified REAL: 11 (blocks the launch: 2) · NOT REAL: 5 · UNVERIFIABLE: 3`. The desk folded ELEVEN findings, both blockers included, **without changing a single rule string** — so R36's approval still binds the same 51 rules and deploy 3 has not moved to Sunday. **This round is narrow: did each fold fix what it names, and did any fold break something?**
It merges `ops/2026-09-19` AND `archiver/append-0919` into production as ONE fast-forward of the gate branch its own L68 step builds, applies production migrations **0010/0011**, takes both residents down and up, and tags `deploy-2026-09-19c`. **Dejan has ALREADY APPROVED it** (`cto-2026-09-19.md` §4 **R36**, 16:4x ET, given in advance because he is offline) on three conditions, and **the first condition is this check returning ZERO launch blockers**; the second is `D3 BUILD CHECK`'s `blocks deploy 3` staying 0; the third is a start by **20:15 ET**. He is away, so **a finding that forces a change to a RULE STRING does not get folded — it moves deploy 3 to Sunday.** Say so plainly if you find one. Text-only fixes the desk folds and records. DO NOT STOP until the report ends `D3 PROMPT CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. Standing law: LAWS.md **L67** (2026-09-18 17:30, `cto-2026-09-18.md` §4 R18 + R19–R21) and **09-18 R8**; `cto-2026-09-18.md` §4 **R9** (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20); `cto-2026-09-19.md` §4 **R1** (the Codex read-only rule), **R32** (L43 overruled tonight), **R34**, **R35**, **R36** (the approval this check conditions). Prove with `git -C /Users/cobalt/cobalt log --oneline -20 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`, stop. `ASK DESK: <question> [<time>]` for a question that does not stop you — safe default, continue, never wait.

Laws, one line each: L33 reviewers are read-only · L35 trust the file, never a claim and never the desk's own `approval-list.md` — verify against the real prompt and the real code · L36 reviewers spawn nothing · L37 no model-judged approval · L43 (its one-deploy-per-evening clause is overruled for tonight by R32; its restart-window clause is NOT and is met because Saturday is not a trading day) · L44 same packet to every house · L47 meter before counting on a house · L51 the three deploy pre-approvals · L54 worktree rule · L62 unattended launch · L66 residents down before the merge · L67 · L68 integrated gate · L70 unproven escalates.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT and §2 launch spellings bind you UNCHANGED with `scratch/review-d3-prompt-r2-0919/` in every path. `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` §3 = the verification rules and table shape. `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-p4-2026-09-19.md` **IN FULL** — deploy 2 ran this prompt's parent THREE times today: run 1 stopped at the L68 gate on a real conflict, run 2 false-stopped on its own suite's row, run 3 shipped. Its three stop lines and 9 ESCALATE are the best evidence you have for what this shape does under load. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-prompt-r2-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line. You commit nothing.

**GROK INVOCATION — bind this spelling and do not improvise it.** `grok --sandbox cobalt-job --allow "Write(<absolute scratch dir>/**)" -p "<prompt>"` — the `--allow` argument must be the FOLDER GLOB ending in `/**`, never one file path (a single-file pattern HARNESSes on this host). Use `scratch/review-d3-prompt-r2-0919/**`.

## 1. Packet in `scratch/review-d3-prompt-r2-0919/` (`mkdir -p` first; Read → Write, byte-identical) — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/55-packet/`
(1) `53-deploy-d3-after.md` (48,744 B) — **THE FILE UNDER REVIEW, after the fold** · (2) `fold-r2.worddiff.txt` (30,075 B) — the desk's pre-computed word-level diff, round-1 version → now · (3) `round1-verdicts.md` (9,921 B) — round 1's headline and its COMPLETE verdict table, verbatim, NOT REAL and UNVERIFIABLE rows included · (4) `approval-list.md` (2,405 B) — unchanged; the 10 new + 41 identical rule strings Dejan approved as R36 · (5) `QUESTIONS.md` (3,321 B), the same text every house receives (L44).
**Total ≈94 KB. Send TWO calls per house:** **A** = `53-deploy-d3-after.md` + `round1-verdicts.md` + `approval-list.md` + `QUESTIONS.md` (≈64 KB, covers Q1–Q3, Q5, Q6) and **B** = `53-deploy-d3-after.md` + `fold-r2.worddiff.txt` + `QUESTIONS.md` (≈82 KB, covers Q4 and the diff half of Q1–Q3). Say in `## Packet` which bucketing each house got. **Never drop a file to make a bucket fit and never let a call truncate silently.** Record every byte count and confirm each staged copy equals its source; state a whitespace-only difference once and do not chase it.

QUESTIONS.md (verbatim): see `55-packet/QUESTIONS.md` — copy it byte-for-byte into the scratch dir as staging step (5). Six questions: (Q1) **F1**, the first blocker — 4.1 now merges `main` into the stack, proves docs-only, and tests `merge-base --is-ancestor` · (Q2) **F2**, the second — newest-first reverts with `-m 1` per merge commit, and the named non-conflict error branch · (Q3) the other nine folds (F3–F7, F9–F11) · (Q4) **what did the fold break** · (Q5) the rule strings, which must still be exactly 51 · (Q6) anything else that would damage production.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-d3-prompt-r2-0919/` in place of their paths. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.

For every allowlist claim (Q5) quote the rule AND the step's command side by side. **The N1 fold types `launchctl bootstrap` inside §4.2, a step that never typed it before** — that is exactly the class round 2 warned about (a step typing a spelling its rule does not cover), so verify those two bootstrap rules exist in the launch line character for character, including the two different plist paths (`/Users/cobalt/cobalt/ops/com.cobalt.aset.plist` for aset, `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for radar). After §4.2 a denied command means denied with production's services DOWN. You verify each BLOCKER and MAJOR against the REAL files: `38-deploy-p4.md` under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/` with Read and `grep` (no Bash rule needed for a plain read) — and confirm it is byte-identical to the staged `38-deploy-p4-after.md`; `src/cobalt/db_migrations/cli.py` (Q2's transaction claim — re-derive the line numbers yourself, L35), `src/cobalt/db.py`, `src/cobalt/jobs/cli.py` on main and in `/Users/cobalt/cobalt-wt/s2-p4/`; `ops/com.cobalt.aset.plist`, `ops/com.cobalt.replay.plist` and `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for Q1's bootstrap paths; LAWS.md at `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md`.

A claim only a RUNNING command could settle — whether an unstarred allowlist rule matches by prefix on this host, whether `launchctl bootstrap` of a service booted out seconds earlier succeeds, whether the stacked merge conflicts, whether the stacked suites go green — is `UNVERIFIABLE FROM READS`: name the exact missing command, do not invent the answer and do not ask a reviewer to run it. Carry round 2's two UNVERIFIABLE rows forward as they stand unless the fold settled them. One extra column per REAL finding: `blocks the launch? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-d3-prompt-r2-0919/` — in particular you never edit `38-deploy-p4.md`; the desk folds.

**NOT this round's business, and not a finding:** P15, the archiver precondition, is now **MET** — `archiver-append-build-2026-09-19.md` ends `ARCHIVER DB2 f5df6c1 … db 161/0`, verified by the desk at 13:5x ET; the wide-vs-narrow L68 scope question is Dejan's; the prefix-match question is the desk's experiment. Note them in §0 if you like, do not count them.

Last line, exactly this shape:
`D3 PROMPT CHECK R2 · grok: <RUN AS IS|RUN AFTER FIXES <n>|DO NOT RUN|HARNESS|METER|TIMEOUT> · gemini: <same shape> · astra: <same shape> · hub-verified REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
