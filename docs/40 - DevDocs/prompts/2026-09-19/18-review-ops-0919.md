MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-ops-0919`, launched by the CTO desk in the background. NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `14-review-p4-verify.md`'s launch line, with exactly ONE string changed — the `mkdir` path, from `scratch/review-p4-verify-0919` to `scratch/review-ops-0919` — because this check needs no rule `14` did not already carry (the target branch's commits are reachable with the SAME `git -C /Users/cobalt/cobalt show*` / `log*` rules: `ops/2026-09-19` is a branch in the same repository `14` already had `show`/`log` access to, so no `git -C .../ops-2026-09-19 *` rule is added; the unused `git -C .../s2-p2-cards *` rules stay, unused, because they are not the string that changed). If this check turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/18-review-ops-0919.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-ops-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-ops-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of a BUILD (LAWS L67: every build is checked by at least three tribunal members before it ships; Fable is NOT one of the three checkers today — the checkers are Astra (OpenAI), Grok (xAI), Gemini (Google)) — branch `ops/2026-09-19` (build tip `fc1d967`; the report commit `319eaaa` sits above it), five small defect fixes: (a) day-open C2 trading-day guard · (b1) trade-count band refuses an inverted/non-integer band · (c) `lock_timeout` on the migrate transaction · (d) test hygiene A1/A2 · (e) the migrate output names the code it ran from. This code has not merged and does not run against production today, but it is queued for the next deploy and its `requires_db` tests are queued for a real run on `cobalt_dev` — the check is due now per the build's own stop line ("OWED: three-house check"). Round 1 of at most 3 (L67: ≤3 rounds per house while meter allows, never fewer than 2 houses total). DO NOT STOP until the report ends `OPS CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. This check is standing law, not a one-off approval: LAWS.md L67 ("every DEVELOPMENT (build) is checked by at least THREE members of the tribunal"), ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, emergency/override/rounds clauses R19–R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The build's own debt: `docs/40 - DevDocs/reports/ops-2026-09-19.md` §0 and its last line name "three-house check" as OWED — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/ops-2026-09-19.md"` (the report is committed on branch `ops/2026-09-19`, reachable from `/Users/cobalt/cobalt` by the same `show*`/`log*` rules). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the build's own report's claims (`build-report.md` in the packet is the builder's account — treat it as a claim) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-ops-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-ops-0919/REVIEW.md` — the same gitignored-scratch pattern `01` and `14` use, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/18-packet/`; copy each into `scratch/review-ops-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `code.diff` = `18-packet/code.diff` — `git -C /Users/cobalt/cobalt diff main..ops/2026-09-19 -- src tests configs ops` computed ahead of time by the desk (917 insertions / 13 deletions across `src/cobalt/cli.py`, `src/cobalt/daymode/cli.py`, `src/cobalt/daymode/propose.py`, `src/cobalt/dayopen/checks.py`, `src/cobalt/db_migrations/cli.py`, `tests/cobalt/test_daymode.py`, `tests/cobalt/test_dayopen_checks.py`, `tests/cobalt/test_migrate_proof.py`; `configs` and `ops` are UNCHANGED — the diff against those two paths is empty, which is itself worth confirming, not assuming) · (2) `facts.md` = `18-packet/facts.md` — the desk's pre-computed measurements: the commit list, the tip, the merge-base against main, the diff-stat, and a file:line confirmation for each of the five items · (3) `spec-16-ops-2026-09-19.md` = `18-packet/spec-16-ops-2026-09-19.md`, verbatim copy of the build's own prompt (`16-ops-2026-09-19.md`) — what the builder was TOLD to build · (4) `spec-16-packet-facts.md` = `18-packet/spec-16-packet-facts.md`, verbatim copy of `16-packet/facts.md` — the desk's pre-build measurements the builder was bound to · (5) `build-report.md` = `18-packet/build-report.md`, verbatim copy of `docs/40 - DevDocs/reports/ops-2026-09-19.md` — the builder's own account of what it did; QUESTIONS.md calls this a CLAIM, not evidence · (6) `QUESTIONS.md`, verbatim below. If a file is too large for one Write call, split `name.part1`, `name.part2` and say so in `## Packet`.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — everything you need is already here as a file. `spec-16-ops-2026-09-19.md` and `spec-16-packet-facts.md` are what the builder was told to build and the facts it was bound to. `code.diff` is what actually landed on branch `ops/2026-09-19`, five commits, tests-first, offline (no `cobalt_dev`, no production). `build-report.md` is the builder's own account of its work — treat every claim in it as a CLAIM to verify against `code.diff`, never as settled fact. This code is queued for the next deploy against a live production Postgres 16 database for a trading-support system; its `requires_db` tests have never run. For every finding give SEVERITY (BLOCKER = the code does not do what the spec required, breaks an existing invariant, contradicts a LAW (L1 fail-loud, L3 one-path, L10 config-validated-on-load, L45 real-shape fixtures, L52 nothing here may touch scoring/ranking/cards, L57 replayable values), or the report's claim does not match the code / MAJOR / MINOR), FILE + LINE (from `code.diff`), the EVIDENCE you read (quote it), and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) ITEM a, `dayopen/checks.py`: does the diff move the `windows_for` calendar read AHEAD of the `count == 0` FAIL, so a non-trading day with zero rows now PASSES with detail `PASS — no session today (...)` and the raw block is still built? Does a TRADING day with zero rows still FAIL (the existing MONDAY test must be unedited)? Is `dayopen/models.py` touched anywhere in `code.diff` (the spec forbids it — the unmerged P4 branch owns that file)? (Q2) ITEM b1, `daymode/propose.py` + `daymode/cli.py` + `cli.py`: does `validate_band` refuse an inverted band (`min > max`), a non-integer, a negative value, and a `bool` (Python's `bool` is an `int` — confirm it is explicitly excluded, not accidentally accepted as `0`/`1`)? For the five ACCEPTED bands (`(2,6)`, `(3,3)`, `(None,None)`, `(2,None)`, `(None,6)`), does the diff show any change to what `day_facts` computes, or only to what is refused before it — i.e., is the 'no fact changed' claim actually true from the diff, not merely asserted? Is `validate_band` called from exactly TWO places (`daymode/cli.py` `_band()` and `cli.py`'s validate command) with no third copy of the rule (L3)? Is the half-set (exactly one null) case really left unchanged, matching the spec's b2 scope-out? (Q3) ITEM c, `db_migrations/cli.py`: does `SET LOCAL lock_timeout` fire inside the read-write transaction, before `_apply`'s first statement, on both the forward AND rollback paths? Is `--proof-only` provably untouched by this statement (does the diff show it excluded)? Is `LockNotAvailable` wrapped into a `MigrationError` with a rollback, a message naming NOTHING WAS APPLIED and the timeout, matching the existing `SerializationFailure` shape? Is `0`/negative/non-int/bool refused BEFORE any connection is opened? Is `DIGEST_EXCLUDED_COLUMNS` (the P4 branch's tuple) left untouched in the diff? SEPARATELY: the build's own report names an acknowledged gap — the BEFORE probe is not itself under the lock-timeout ceiling because the `SET LOCAL` was placed conservatively after it, and the Postgres manual's wording on whether `SET LOCAL` moves a REPEATABLE READ snapshot was not settled (no network in that build's sandbox). Is that gap named honestly and scoped correctly, and would you call it a BLOCKER for a build that has not yet deployed, or a MAJOR to fix before the deploy prompt is written? (Q4) ITEM d, `test_migrate_proof.py` ONLY: does `code.diff` show ANY change to a file under `src/` in this item, or only to the test file (the spec requires test-file-only)? Does the worker connection get a `SET LOCAL statement_timeout` ABOVE `LOCK_CEILING_S`, and does the hung path call `conn.cancel()`, rejoin, and close — on every path, including if an assertion inside the `try` raises before reaching that cleanup? Does the blocked-lock probe's new `pid` parameter get threaded through to every call site, and is test (f) genuinely left unmodified because it does not share the helper (verify from the diff, not the report's claim)? (Q5) ITEM e, `db_migrations/cli.py`: does `CODE_ROOT` resolve from `Path(__file__).resolve().parents[3]` (the installed package, not `Path.cwd()`)? Is the git call routed through an EXISTING helper (`cobalt.generated.committer._git`) with no new git-shelling code (L3)? Is `_code_line()` truly the LAST line printed on BOTH `--proof-only` and the forward path, including on a path that rolls back with `CHANGED`? Does an exception from the git helper degrade to `code: UNKNOWN — <reason>` without raising out of `cmd_migrate` (L1 — never a plausible fake value)? (Q6) CROSS-CUTTING: does `code.diff` touch anything outside the five items' declared bound, or anything under `configs/` or `ops/`? Does anything in the diff reach scoring, ranking, or a value that lands on a trading card (L52 — if so, name exactly what and why it should have stopped the build)? Any secret, credential, path, or connection string visible in the diff (L4)? Does the diff show a second implementation of a rule that already exists elsewhere (L3), beyond what items b1 and e already name as deliberate reuse? (Q7) Does `build-report.md`'s own narrative (BASELINE numbers, the +46/+1 delta, the `RESTARTS:` line, the nine `## ESCALATE` items) hold up against `code.diff` and `facts.md` — name any claim in the report you cannot confirm from the files given, marking it UNVERIFIABLE FROM READS rather than assuming it is true. End with exactly one line: `VERDICT: BUILD CLEAN / FIX FIRST <finding numbers> / BUILD UNSAFE`."

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-ops-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: `code.diff` against `git -C /Users/cobalt/cobalt show ops/2026-09-19:<path>` (the branch's tip content — reachable from `/Users/cobalt/cobalt` under the SAME `show*` rule, no worktree `cd` needed) and against the real files under `/Users/cobalt/cobalt-wt/ops-2026-09-19/` with Read and `grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (for example, an independent re-derivation of `code.diff` itself with a fresh `git diff` against `/Users/cobalt/cobalt-wt/ops-2026-09-19` rather than reading the pre-staged file, or running `uv run cobalt jobs restarts` to re-check the RESTARTS claim) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-ops-0919/`. Last line: `OPS CHECK · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
