MODEL: Sonnet 5 (`claude-sonnet-5`) — probe one house, run it, verify its findings; no write path · SEAT: review hub `astra-d3-0919`, launched by the CTO desk in the background. NO NEW RULE: `51-review-deploy3-build.md`'s `--allowedTools` list, byte for byte, with exactly ONE string changed — the `mkdir` path. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/52-astra-d3-build.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control astra-d3-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/astra-d3-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small.

# THE THIRD HOUSE on deploy 3's build (L67: every development is checked by at least THREE tribunal members)
`51-review-deploy3-build.md` got Grok and Gemini — `D3 BUILD CHECK · grok: FIX FIRST 1 · gemini: SAFE TO DEPLOY · astra: METER · houses that read it: 2 of 3 · hub-verified REAL: 4 (blocks deploy 3: 0) · NOT REAL: 1 · UNVERIFIABLE: 5`. Astra's Codex meter said "try again at 4:54 PM" and the probe at 16:1x was still refused. **This run exists only to get Astra's read once that reset has passed**, so L67's three-checker bar is met before deploy 3's prompt goes to Dejan. Deploy 3 runs TONIGHT if clean (`cto-2026-09-19.md` §4 R32, L43 overruled for tonight only; hard edge = the 21:40 nightly backup). DO NOT STOP until the report ends `ASTRA D3 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF: LAWS.md **L67**; `cto-2026-09-18.md` §4 **R9**; `cto-2026-09-19.md` §4 **R1** (the Codex read-only rule), **R32**, **R33**, **R34**. `git -C /Users/cobalt/cobalt log --oneline -16 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch`, stop. Laws: L33, L35, L36, L37, L44, L47, L67.

## 1. PROBE FIRST, and do not fight the meter
`date`. Then ONE probe: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."` (`run_in_background`). **Still METER → write the report with `astra: METER`, its verbatim message and the time, and STOP.** Do not retry, do not wait, do not substitute another house — Grok and Gemini have already read this build and re-running them buys nothing (L47: the meter is a precondition, not a router).

## 2. If Astra is UP: the same packet the other two got (L44 — identical bytes, identical questions)
The packet is ALREADY STAGED at `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/51-packet/`: `fixes.diff` (43,622 B), `commits.txt` (463 B), `registry-after.py` (5,122 B), `rebase-report.md` (59,396 B), `QUESTIONS.md` (3,744 B). `mkdir -p scratch/astra-d3-0919`, copy all five in (Read → Write), record each byte count, and note any difference exactly as `51` did (its staged `fixes.diff` came out 60 bytes short — blank context lines losing a space — and that was stated once, not chased).
Two calls, the SAME bucketing `51` used: **A** = `QUESTIONS.md` + `fixes.diff` + `commits.txt` + `registry-after.py`; **B** = `QUESTIONS.md` + `rebase-report.md`. One attempt each, `run_in_background`, no retry. Astra answers the SAME seven questions — do not rewrite them, do not add to them.

## 3. Verify, then close
Every BLOCKER or MAJOR Astra raises is verified by YOU against the real tree in `/Users/cobalt/cobalt-wt/archiver-append/` with Read and `grep`, line numbers re-derived (L35). Then place each finding against `51`'s table (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md`, `## Verdict table`) and say for each: **NEW**, or **already found** (name the row), or **NOT REAL** with the evidence. `51`'s four REAL MINORs are G-A1 (the backfill window is built from `hour:minute`, seconds dropped), G-A2 (a stale `--rollback` sentence in the registry docstring, pre-existing), H1 (the declared window counts the sleeps but not the fetch latency, so a long manual backfill can overrun it), H2 (the docstring says "nothing asserts contiguity" while the new pin does, and the new pins are tail-anchored again). A house agreeing with one of those is not a new finding.
A claim only a RUNNING command could settle is `UNVERIFIABLE FROM READS` — name the missing command; the build reports `offline 2146/0, db 2486/0` and that is evidence, not your verification. One extra column per REAL finding: `blocks deploy 3? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/astra-d3-0919/`. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/astra-d3-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Astra` → `## Placement against round 1` → last line. You commit nothing.

Last line, exactly this shape:
`ASTRA D3 · astra: <SAFE TO DEPLOY|FIX FIRST <n>|DO NOT DEPLOY <n>|HARNESS|METER|TIMEOUT> · NEW REAL: <k> (blocks deploy 3: <d>) · already found: <n> · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
