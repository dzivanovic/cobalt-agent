# Excerpt of 02-deploy-stack-3.md — the approved launch line (line 3, MODEL/SEAT) and the settings-load steps (3.4, 3.6, 3.7, 3.8) and the safe-state rule's card-settings clause (§6 (3))

## Launch line (line 3, verbatim) — the two card-settings strings already in it are strings (3) and (4) of R20
MODEL: Opus 5 (`claude-opus-5`) — Opus floor: production migrations 0006/0007 and two production `trader_settings` writes ship (L29 write path; L7) · SEAT: deploy hub `deploy-stack-0919`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md' and follow it exactly." --model claude-opus-5 --remote-control deploy-stack-0919 --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C /Users/cobalt/cobalt reset --soft HEAD~1)" "Bash(git -C /Users/cobalt/cobalt tag *)" "Bash(git -C /Users/cobalt/cobalt merge --ff-only sprint-2/stack)" "Bash(git -C /Users/cobalt/cobalt revert --no-edit *)" "Bash(git -C /Users/cobalt/cobalt revert --abort)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase main)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase --abort)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * rev-parse*)" "Bash(git -C * rev-list*)" "Bash(git -C * merge-base*)" "Bash(git -C * show*)" "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt jobs *)" "Bash(COBALT_ENV=production uv run cobalt backup run*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt-wt/ops-2026-09-18/scratch/daymode-settings-0918 --apply)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt/data/backups/pre-stack-0919/daymode-settings-rollback --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --from /Users/cobalt/cobalt/data/backups/pre-stack-0919/daymode-settings-rollback --apply)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --card /Users/cobalt/cobalt/data/backups/pre-s2-p2-2026-09-17/p2-dark-settings.yaml --sha256 945e42f86997559267b2e7c20783f2d023b9135ae4bb437ca25a4999ca7cd3ca --apply)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(COBALT_ENV=production uv run cobalt radar evaluate --replay *)" "Bash(launchctl bootout gui/501/com.cobalt.aset)" "Bash(launchctl bootout gui/501/com.cobalt.radar)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist)" "Bash(launchctl kickstart -k gui/501/com.cobalt.aset)" "Bash(launchctl kickstart -k gui/501/com.cobalt.radar)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(mkdir -p /Users/cobalt/cobalt/data/backups/pre-stack-0919/daymode-settings-rollback)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61), never `bypassPermissions`, push never in it, NO `pytest` in it (see WHY) · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely and gets an answer; a dialog gets none).

## Step 3.4 — the OTHER (non-card) forward dry run, for PASS-test comparison
3.4 The over-band FORWARD dry run (exact line) → GATE: its output CONTAINS `1 setting(s) would change` (the CLI's full line is `DRY RUN — 1 setting(s) would change. Nothing written.`) AND the only key marked `~` is `daymode.stepdowns`, the other six `=`; else do NOT apply → §6 (settings untouched — say so) (review finding 3).

## Step 3.6 — the over-band apply + its L28 trace shape
3.6 The over-band `--apply` (exact line) → quote `applied: {…}` and the `field-by-field diff EMPTY` line + L28's trace line (`approved by Dejan via the desk — cto-2026-09-19.md row <n>`, command, hashes, time).

## Step 3.7 — the dark card dry-run + apply (the ONLY prior card-settings load ever run against production)
3.7 The dark card `--dry-run` (exact line) → GATE: ONE add `radar.cards_enabled` = false, ZERO deletions (any existing `card.*` row listed for deletion → do not apply; §6) → the `--apply` line → quote it + the round trip `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → one row, false + the L28 trace line.

## Step 3.8 — validate as post-write verification
3.8 `COBALT_ENV=production uv run cobalt validate` → exit 0; `Step-downs:` carries `trade_count_over_band=down(1)`; ceiling / band lines unchanged. Red anywhere in 3.4–3.8 → §6.

## Section 6 (3) — what 02 says about the dark row on ROLLBACK: it never rolls one back, only ever applied it forward
Goal: code and settings agree, residents UP, before you stop. THE TWO CONSISTENT PAIRS are {old code + six step-down rows} and {new code + seven rows}; either is a safe place to stop, a MIXED pair never is. CLOCK RULE (review finding 4): `date` first — inside 19:40–21:00 ET no settings `--apply` can run, so you do NOT change the row: if the over-band row IS applied, KEEP the new code (skip (1)–(2), go to (3)); if it is NOT applied, revert the code (1) and skip (2). (1) If 3.3 merged: `git -C /Users/cobalt/cobalt revert --no-edit <pre-merge>..HEAD` — the range starts at the commit IMMEDIATELY BEFORE the ff-merge, so it holds the stack's commits and nothing of yours (L54 — never `reset`). A CONFLICT during the revert (not expected on a linear range nothing was committed above): `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → make THAT pair consistent instead: if the over-band row is not applied yet and the clock allows, apply it (3.6) and the dark row (3.7), `validate`; then (3)–(6) with `safe state: NEW code kept — revert conflicted` (review finding 1). IF THE CLOCK DOES NOT ALLOW that apply (V6 — unreachable on a run that started by 18:30, written down anyway): leave BOTH residents DOWN (new code without the row refuses to load, so bringing them up would only crash-loop them), skip (4), and end `FAILED: <step> — revert conflicted inside the refused hour — safe state: red — residents: DOWN`; the desk escalates by push at once and completes the pair after 21:00. (2) If 3.6 applied, or you cannot prove it did not: `date` AGAIN, immediately before this apply — at or after 19:40 ET and before 21:00, or if the apply below is REFUSED by the `market_reset` guard, the pair {old code + six rows} cannot be restored now: leave BOTH residents DOWN (old code refuses the seventh row and would not load), skip (4), end `FAILED: <step> — settings rollback refused — safe state: red — residents: DOWN`; the desk pushes at once and completes the pair after 21:00 (round-2 finding R2). Otherwise: the over-band ROLLBACK `--apply` (exact line) → then `COBALT_ENV=production uv run cobalt validate` → exit 0 on the reverted code. Code reverted + over-band row present, or row removed + new code merged, are BOTH broken states — never leave either. (3) The dark row and migrations 0006/0007 STAY (inert for old code / additive); the schema is never rolled back unattended — PROVE it with `validate` exit 0 and the sheet 200 after (4). (4) Bring the residents up with 4.2's commands and 4.2's single retry / single `kickstart` rule ONLY; a failure here is terminal (`residents: DOWN`), never a second pass through §6. (5) Re-run 5.1's curl / heartbeat / validate / radar lines → quote them. (6) `ESCALATE` with the snapshot id, tag `pre-stack-0919`, the reverted range, which settings are live, what stays applied; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. Residents DOWN at the end is the one outcome the desk escalates by push at once.
