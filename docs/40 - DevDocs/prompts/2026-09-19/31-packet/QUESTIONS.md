You are an independent reviewer from another house, one of at most three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — every measurement you need is already in these files (`facts.md`, `four-strings.md`, `02-settings-load-excerpt.md`, `deploy-report-excerpt.md`, `r20-row.md`). `30-cards-golive-apply.md` is an UNATTENDED Opus prompt that will run with nobody watching, against a LIVE production Postgres database for a trading-support system, and will write to `"user".trader_settings` (a table that gates whether trading-support cards show real grades or nothing). It runs from `/Users/cobalt/cobalt` directly against production — there is no worktree, no rebase, no code merge, no resident restart in this prompt; it is a settings-file load only. `r20-row.md` is Dejan's own HITL approval, verbatim, naming the exact file, sha256, and the four allowed commands. `four-strings.md` restates those four commands and notes that two of them (the dark-file dry-run/apply, used here as ROLLBACK) are byte-identical to two strings already executed successfully in production earlier today (see `02-settings-load-excerpt.md` and `deploy-report-excerpt.md`) — the live-file dry-run/apply strings are brand new, never run before. For every finding give SEVERITY (BLOCKER = the run will fail, can leave production wrong, or can write something Dejan did not approve / MAJOR / MINOR), the STEP in `30-cards-golive-apply.md` (quote the line), the EVIDENCE, and the FAILING SCENARIO; say "none found" rather than inventing.

(Q1) COMMAND-TO-RULE MATCH: go through every command `30-cards-golive-apply.md` tells the hub to type — PREFLIGHT (P1-P7), STEP 1, STEP 2, STEP 3 (3.1-3.5), STEP 4 (4.1-4.3) — and for each, quote the exact `--allowedTools` rule string (from the prompt's own SEAT line) that covers it, character for character, or say NONE. Pay special attention to: the `git -C * status*` / `git -C * log*` wildcard rules (do the specific commands P4/P5 actually match, including flags and paths?); the `db query *` wildcard (does the SQL string in 3.1 need any character the wildcard would not pass through a shell safely — quoting, parentheses, commas?); the two `curl` commands in 3.5 (does the single allowlisted `curl ... :5010/*` prefix actually cover BOTH the bare `/` and `/radar` paths, the way `02-deploy-stack-3.md`'s identical string did?); whether ANY command in the prompt requires `uv run pytest`, a bare `cobalt` command, or a git write (add/commit/tag/merge) that has no rule — if so, name it as a BLOCKER (a mid-run denial on a write-path hub is a failed run, not a safe stop, per L62).

(Q2) NO WRITE OUTSIDE THE TWO APPLIES: can any step in `30-cards-golive-apply.md` write to production other than STEP 2's live `--apply` and STEP 4's dark `--apply`? Check specifically: does `cobalt validate` (3.2) ever write anything (a cache row, a job row)? Does `cobalt db query` ever need write access for a plain `SELECT`? Does `heartbeat show` write a heartbeat row as a side effect? Is the settings file itself ever `git add`ed or committed (L32 forbids it)? Is the produced report file ever committed by this hub (it should not be — say if the prompt is ambiguous about who commits it).

(Q3) THE DRY-RUN PASS TEST'S STRICTNESS (STEP 1): `facts.md` §6 says `htf_level_proximity` was DELIBERATELY OMITTED after the go-live check's Grok BLOCKER (R17 ruled direction only, not a magnitude) — read STEP 1's PASS criteria in `30-cards-golive-apply.md` and confirm they would actually catch: (a) unquoted-YAML-float noise in the bands (`0.9000000000000000222` instead of `0.9`) — is the wording "decimal-clean" and the named bad example enough for an Opus hub with nobody watching to correctly FAIL on real noise, or could it rationalize noise as acceptable? (b) `htf_level_proximity` re-appearing (e.g. a loader default, a stale row from an earlier partial write) — does the prompt's check for "NO htf_level_proximity anywhere in the output" actually work against every plausible print format, or only the one shape the author assumed? (c) an extra, unapproved key changing (the prompt says "the diff must show only these keys" — is there a concrete failure mode where the loader's dry-run output format doesn't cleanly separate "changed keys" from "unchanged keys shown for context," making this check ambiguous rather than a clean pass/fail)? (d) a missing curve (e.g. `Extension.leg_count` silently dropped) — is the "EXACTLY three curves" check specific enough to catch a two-curve or four-curve result? Name any of these that could slip past the written checks as written.

(Q4) ROLLBACK REACHABILITY AND CORRECTNESS (STEP 4): `02-settings-load-excerpt.md`'s §6(3) shows that `02-deploy-stack-3.md` NEVER rolled a card settings row back — it only ever applied the dark row forward, once, and states "the schema is never rolled back unattended." `30-cards-golive-apply.md`'s STEP 4 reuses the SAME dark-file dry-run/apply strings, but now in the OPPOSITE direction: live (four keys, non-empty bands/curves/promotion-bar) back down to dark (one key, `radar.cards_enabled: false`). Is that a safe, well-defined rollback given `card.py`'s schema (does loading a file that OMITS `proposed_key`/`curves`/`shadow_promotion_bar` actually clear those rows, or does the loader only ever ADD/CHANGE keys present in the file and leave the three now-orphaned keys in place — check `facts.md` for anything the schema/loader says about deletion semantics, and mark UNVERIFIABLE FROM READS if the packet does not settle it)? If the three orphaned keys DO stay in the database after the "rollback," is `radar.cards_enabled: false` alone sufficient to make the panel dark again (per `card.py:157-169`'s enable-gate quoted in `facts.md` §10), or does stale `proposed_key`/`curves` data left behind create any risk the next time cards are turned on?

(Q5) FAILURE-POINT COVERAGE: walk each of these and say what `30-cards-golive-apply.md` actually does, quoting the step: (a) a hash mismatch at P2; (b) a permission denial on any allowlisted command mid-run (L62/L63: does the prompt correctly treat this as FAILED rather than retrying in another shape or waiting on a dialog?); (c) the clock reaching 20:00-21:00 ET mid-run (is the check only at P1, or could STEP 2/STEP 4's `--apply` calls straddle into the refused hour on a slow run — is there a second clock check anywhere before either `--apply`, the way `02-deploy-stack-3.md` re-checks its clock before 3.2/3.6/3.7)? (d) a resident (radar or aset) mid-cycle logging an unrelated error at the same moment as the apply (does STEP 3.4's log-tail window and PASS criteria distinguish "an error caused by this settings load" from "an unrelated pre-existing error," or would any red line in the tail wrongly trigger a rollback)?

(Q6) MONDAY 04:00 ET PREMARKET: is anything about this run different for the first live premarket cycle versus what STEP 3's checks can observe today (Saturday, overnight idle, no scan running)? Specifically: does the prompt's STEP 3 verification actually prove the cards will grade correctly once a real scan runs Monday, or only that the settings ROW is stored correctly — and does the prompt say so honestly (compare against `deploy-report-excerpt.md`'s own "no scan can complete today" language from the prior deploy) rather than overclaiming a green Monday morning from today's checks alone?

End with exactly one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <finding numbers> / DO NOT RUN`.
