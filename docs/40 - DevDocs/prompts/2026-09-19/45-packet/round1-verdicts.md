## §0 Headline

- **Two of three houses read it (Grok, Gemini); Astra = `METER`** (Codex usage limit, retry after 4:54 PM — a normal ending; L67's floor of one house other than the author is met twice). Bucket B went as ONE call to both houses, no truncation.
- **Hub-verified REAL: 11, of which 2 block the launch:** (1) the launch line's dev-lane rule `Bash(COBALT_ENV=dev uv run cobalt db migrate*)` also matches `… migrate --allow-prod --rollback --down-to 0007`, and `cmd_migrate` sends `--allow-prod` to `cobalt_brain` whatever `COBALT_ENV` says — so the line's headline claim "a production schema rollback cannot be typed" is FALSE (found by the hub, no house saw the `--allow-prod` route); (2) §2.1's "re-checked … again at 5.2 … after 4.2 go to §8" would send a green merge+migrate to a REVERT if 5.2 lands at ≥19:40.
- Not launchable today for a reason outside the text: P15's archiver precondition — the archiver report's latest DB line is still `db 154/3` (branch tip `193a2ca` adds the DB-1 REVOKE fix, no later `db <p>/0` line exists). The prompt's own P15 stop would fire. That is the desk's item, not a defect.
- Verdicts: Grok `DO NOT RUN` (A) / `RUN AFTER FIXES B1–B7` (B) · Gemini `RUN AFTER FIXES 1,2,3,4` (A) / `1,3,8,9` (B). Hub read: **RUN AFTER FIXES** — two rule/text fixes, then the desk's P15 item, then Dejan.
## Verdict table

Real files read: `38`/`02` under `/Users/cobalt/cobalt/docs/…/prompts/2026-09-19/`; `src/cobalt/db_migrations/cli.py`, `src/cobalt/db.py`, `src/cobalt/db_query.py`, `src/cobalt/radar/store.py`, `src/cobalt/dayopen/checks.py`, `ops/com.cobalt.archiver.plist` on main (production tree) and `db_migrations/cli.py`, `db_query.py`, `ops/com.cobalt.replay.plist` and `reports/s2-p4-verify-2026-09-19.md` in `/Users/cobalt/cobalt-wt/s2-p4/`; `reports/cards-golive-2026-09-19.md`; the archiver report in `/Users/cobalt/cobalt-wt/archiver-append/`; `git log` on main / `sprint-2/p4` / `archiver/append-0919`. Staged copies equal the sources (byte counts above) except where stated.

| # | house(s) · sev (theirs) | step | claim (≤20 words) | what I read | VERDICT | fix in one line | blocks the launch? |
|---|---|---|---|---|---|---|---|
| 1 | **hub-found; nobody named the route** (Grok B's Q1(b) row lists `--allow-prod` under the dev rule but calls it a `cobalt_dev` risk) · MAJOR | launch line; §8 (3); §1.4 | `Bash(COBALT_ENV=dev uv run cobalt db migrate*)` matches `… migrate --allow-prod --rollback --down-to 0007`; that reaches production | `db_migrations/cli.py:537` (P4: `:542`) `dbname = db.PROD_DB_NAME if args.allow_prod else env.resolve_db_name()`; `db.py:144-157` `_prod_gate` only REFUSES when `allow_prod` is false; no `COBALT_ENV` check in `cmd_migrate`. So the SEAT line's "a production schema rollback cannot be typed", §8 (3), and `approval-list.md` "Not added, deliberately" are false for this rule | **REAL** | replace `migrate*` with three exact rules (`… db migrate --proof-only`, `… db migrate`, `… db migrate --rollback --down-to 0009`) | **yes — Dejan approves the launch line on exactly that claim; a launch-line change goes back to him, so fix before the approval row** |
| 2 | Grok B1 (BLOCKER) | §2.1 vs §5.2 / §8 | "again at 5.2 … at or after 19:40 … after 4.2 go to §8" reverts a green merge+migrate | `38` §2.1 last sentences; §5.2 starts `date`; §8 CLOCK NOTE says nothing here is refused by 19:40. Reachable: start 18:29 + a 60–70 min gate → 4.2 at 19:37–19:39 (allowed), 5.2 ≥ 19:40 | **REAL** | say 5.2's clock is a record, never a §8 trigger (delete "and again at 5.2") | **yes — the sentence would mislead the unattended session into reverting a good deploy (0008/0009 stay, replay unarmed, extra outage)** |
| 3 | Grok F9 + B3 (BLOCKER/MAJOR); Gemini A#1 (BLOCKER), Q3(d) (MAJOR) | §5.3, §8 (0) | replay job stays loaded against a reverted tree and cannot be unloaded | `38` §8 (0): "leave it loaded … name it in the ESCALATE line" — the prompt SEES it. `ls /Users/cobalt/cobalt/src/cobalt/replay` → absent on main (whole package arrives with P4); `ops/com.cobalt.replay.plist` exists only on `sprint-2/p4`; allowlist has `bootout` for aset/radar only. So after a §8 revert Monday 21:10 fires `uv run cobalt replay nightly` at a tree without it | **REAL (residual; "BLOCKER" overstated — deliberate, named, nothing runs before Monday)** | add `Bash(launchctl bootout gui/501/com.cobalt.replay)` (a NEW rule, needs Dejan's word) and let §8 (0) unload it if 5.3 ran — or keep the ESCALATE and say what fails Monday | no — only on the §8-after-5.3 path; residents up, first fire is Monday |
| 4 | Grok F10 + B4 (BLOCKER/MAJOR) | §1.4 (c)(d) | a red with-DB suite skips (f)'s `--down-to 0009` restore | `38` §1.4 (c) "Anything else → `.env` cleanup, `FAILED`", (d) "A red → `.env` cleanup, then `FAILED`"; (f) only runs on the green path | **REAL** | run (f) + prove the proof-only baseline before every red-branch stop, or write `cobalt_dev holds 0001–0011` into the FAILED line | no — production untouched; today P15 stops the run before §1.4 |
| 5 | Grok F2–F6, B(b); Gemini A#3, B Q1(b)/Q9(a) (MAJOR/MINOR) | launch line | `cd *`, `uv run pytest *`, `worktree add *`, `tag *`, `add *`, `commit *`, `grep/tail/ls *`, `jobs *` are broader than their steps | Line 3 read. `tag *`, `add *`, `commit *`, `jobs *`, `grep/tail/ls/wc/date` are byte-identical in `02` (approved R7, ran clean); `cd`, `pytest`, `worktree add`, `cp`/`rm .env` are disclosed as NEW in `approval-list.md` #4–12. `cobalt jobs run LABEL -- CMD` (`jobs/cli.py:6`) means `jobs *` can wrap any command — also in `02`. Gemini's "explicitly forbidden in deploy 1" = deploy 1 omitted them (`02` "NO pytest"), 38 adds them deliberately with WHY 2's cwd rule | **REAL (an observation, not a hole)** — safety rests on prose + the classifier, and the desk's approval list already discloses the new ones | narrow only what is cheap (`git tag pre-p4-0919`/`deploy-2026-09-19b`/`tag -d scratch-allow-probe-p4-0919` as exact rules) | no |
| 6 | Grok F1 (BLOCKER), B8 (MINOR) | launch line | unstarred `Bash(… migrate --allow-prod)` may prefix-match `--rollback` | `grep -c -F` = 1 in both `02` and `38` — the rule is byte-identical to the one that ran this morning. Host matcher semantics are not readable from files (documented Claude Code behaviour: a rule without `*` matches exactly; not verified here). Row 1 makes the question moot for the dev rule | **UNVERIFIABLE FROM READS** — experiment: fresh scratch session with `--allowedTools "Bash(echo hi)"`, type `echo hi there`; expect DENIED | — | no |
| 7 | Grok F13, Gemini A#2 (MAJOR) | §6.1 | a NEW heartbeat RED has no explicit §8 route | §6.1 has none; §8's own header lists "red smoke"; 6.2 and 6.4 name their §8 arrows | **REAL (wording only)** | append "→ §8" to 6.1's NEW-RED clause and name the outage-window RED as the exception | no |
| 8 | Grok F7 (MAJOR) | §6.4 / allowlist | `db query *` might accept `UPDATE trader_settings …` and turn cards dark | `db_query.py` (main and P4): `guard_select` accepts exactly one `SELECT`/`WITH`, refuses `INSERT/UPDATE/DELETE/…` words, `BEGIN READ ONLY` at `:149`/`:163`. The Q2 claim (no card write from this session) holds from allowlist AND code | **NOT REAL** | — | — |
| 9 | Grok F14 (MAJOR) | §4.5 (ii) | read-back probes only `movers_daily`; 0009 could be unapplied | `cmd_migrate`: one connection, one `_apply(conn, paths)` over all paths, one `conn.commit()` (`:563`; P4 `:568`) — all-or-nothing; `movers_daily` decides both | **NOT REAL** | — | — |
| 10 | Grok F8 (MAJOR), m1 (MINOR) | §8 (1) | a 56-commit range revert may conflict; `--short` hash ambiguous | The branch is rebased linear (4.1); a conflict is already handled by `revert --abort` → NEW-code-kept state. m1 not verified (a 7-char hash in a 1–2 k-object repo) | **NOT REAL** as a defect (m1: MINOR, unverified) | — | — |
| 11 | Gemini B Q3(b) (BLOCKER); Grok A Q3(b) (UNVERIFIABLE) | §8 (3) | pre-P4 code may `SELECT *` from `radar_membership` and crash on the two new columns | `grep radar_membership` over `src/`: only `radar/store.py`, `dayopen/`, migrations, `placement.py`; every `radar_membership` statement names its columns (`store.py:33,57,281,293`, INSERT `:126-130` explicit list; `dayopen/checks.py:125,131`). The `SELECT *` hits are `radar_pool`, `radar_board_v`, `radar_score(_run)` — untouched by 0008 | **NOT REAL** (static read; "old code actually runs on the new schema" stays under row 22) | — | — |
| 12 | Gemini B Q8 (BLOCKER) | §4.2 / §8 (0) | if `bootout` does not take, §8 loops and the deploy hangs | At 4.2 nothing is merged; the session can always write `FAILED`. Real remainder: §8 (0) has no terminal line for "bootout won't take" | **NOT REAL** as a BLOCKER — MINOR wording gap | add "still loaded after one retry → `FAILED: residents not stopped`, nothing merged" | no |
| 13 | Grok B6 (MAJOR), Gemini B Q9(d) (MAJOR) | §4.2, ESCALATE 2 | archiver is not disarmed and may fire into the outage | `ops/com.cobalt.archiver.plist:54-61`: `StartCalendarInterval` Weekday **1–5** at 20:30 only, `RunAtLoad` false — it does **not** run on a Saturday; the start ≤18:30 / bootout <19:40 rule already keeps clear of a weekday 20:30 | **NOT REAL** as a danger | — | — |
| 13b | hub, from row 13 | §2.1 text; `facts.md` §8 | the prompt's stated reason for the 18:30 cap — "tonight's one-shots: `com.cobalt.archiver` at 20:30" — is false for a Saturday (archiver and replay are Mon–Fri) | plist read above | **REAL (text)** | reword the margin reason (the 21:40 backup only; check its calendar) | no |
| 14 | Grok B2 (MAJOR) | §0 P13 | `--proof-only` from `~/cobalt` (main's registry) cannot list 0008/0009's tables; "expected `movers_daily`, `picks`, `missed` all `-`" is unsatisfiable; ~95 s of `bars` reading | `_probe_all` (`cli.py:280-282`) enumerates the static `MOVED/SEEDED/CREATED_TABLES` of the code in cwd: main proves 23 tables (deploy-1 table), P4 proves 26 (`s2-p4-verify…:868`). P13's gate is only CHANGED/non-zero/timeout, so it cannot false-fail; the "ABSENT" sentence just cannot be met | **REAL (MINOR)** | delete the "record which tables it reports ABSENT" clause; keep P12 (cheap catalog check) | no |
| 15 | Grok B5 (MAJOR) | §1.3/§1.4 | `cd` is its own Bash call; if cwd does not persist the gate tests `~/cobalt` | The harness states cwd persists between Bash calls; `cd *` is already approved in `13`/`24b`/`25`/`35` (approval-list #7, not re-checked). Real wrinkle: §1.4 (d) says "the immediately preceding call is `cd` + `ls .env`", but (c) precedes it — a literal session would re-`cd` | **NOT REAL** (wording MINOR) | say the cd in 1.3 holds through 1.4 | no |
| 16 | Grok B7 (MAJOR) | §6.4 | expects `True`, deploy 1 printed `False` | `cards-golive-2026-09-19.md:137` prints `radar.cards_enabled	True` from the same `db query`; deploy 1's `False` was the then-dark value | **NOT REAL** | — | — |
| 17 | Grok B9 (MINOR) | §1.2 | a rerun cannot `worktree add -b stack/deploy2-0919` over the existing branch/path | No `worktree remove`/`branch -D` rule (deliberate, `approval-list.md`). Likely path today: gate FAILED → desk re-launch → second run stops at 1.2 unless the desk cleans first | **REAL (MINOR)** | one line: rerun → "worktree exists → `FAILED: gate — stack artifact from the first run, desk removes it`" | no |
| 18 | Grok F11 (MAJOR) | §5.3 vs §7 | the fixed DONE line says "bootstrapped" even after a failed replay bootstrap | §7 shape line vs §5.3 "record `NOT bootstrapped` and carry it into ESCALATE" | **REAL (MINOR)** | give the DONE line a `com.cobalt.replay <bootstrapped|NOT bootstrapped>` slot | no |
| 19 | Grok F12 (MAJOR) | L43 first sentence | second production deploy on one date | `38` cites 09-18 R3/R5 ("Deploy and restart … every time we have something to deploy", per `deploy-1-report.md`); deploy 1 ran under the same basis and Dejan approved it. The Saturday-daytime reading of L43/L66's "on a trading day" is the desk's argument for the approval row (L37: not mine to settle) | **NOT REAL** as a text defect (note for Dejan's row) | — | — |
| 20 | Grok F15 (MAJOR) | §1.4 (f) | a moved digest is "NAMED, never waved off" but no stop rule | `38` §1.4 (f) last sentences | **REAL (MINOR)** | make a moved digest on a table other than the two new ones `FAILED: gate` | no |
| 21 | Gemini A#4 (MINOR) | §7 | tag date wrong if launched Sunday | §2.1 turns a Sunday `date` into `FAILED: window`; §7 forbids renaming | **NOT REAL** (designed) | — | — |
| 22 | Grok B(Q3b), F-class; Grok A Q5(a) | §8 (3); §1 | (a) "additive ⇒ inert for reverted code" not proven at runtime; (b) a green three-branch stack can mask a P4-only red; §4.1's byte-identity closes rebase mutation only | (a) only the static `SELECT *` hazard is settled (row 11); no read runs old code on the new schema. (b) inherent to the WIDE L68 reading; `laws.md` states wide-vs-narrow as OPEN for Dejan | **UNVERIFIABLE FROM READS** — (a) `COBALT_ENV=dev`: after `db migrate` with P4, start pre-P4 `radar.store` reads against `cobalt_dev` (a build-lane run, never here); (b) Dejan's L68 scope ruling | — | no |
| 23 | Grok A Q10 / hub | §0 P15 | the run cannot pass P15 today | archiver report tail: latest DB line `ARCHIVER DB d2099e1 … db 154/3`; branch tip is now `193a2ca` (`fix(db-migrate): REVOKE cobalt_user … R25 "B", DB-1`) with no later `db <p>/0` line; DB-2 has no fix commit. `38`'s own stop would write `FAILED PREFLIGHT: archiver known reds unresolved` | **LAUNCH STATE — not a finding, not counted** (the prompt is right to stop) | the desk's DB-1/DB-2 work or Dejan's narrower L68 reading | yes as a precondition — already the desk's OPEN item (`facts.md` §11.3) |

MINORs listed unverified: Gemini A#3 (allowlist breadth — folded into row 5), Gemini B Q2 ("reads no card yaml … writes no row" is a read-back, not a write — wording only), Gemini B Q6 (replay job's function unproven — Grok F11/row 18 and `38` §6.6 both concede this), Grok m1.

**Facts check (`facts.md`, "the tree wins"):** `git log 919d562..main -- . ":(exclude)docs"` prints nothing → main's drift is docs-only **CONFIRMED**. `sprint-2/p4` is now `d9c51b4` (packet said `21bcd5f`): `git log 21bcd5f..sprint-2/p4 -- . ":(exclude)docs"` prints nothing → **code unchanged**; the P4 DB stop line now exists (`P4 DB 9045260 … db 2147/0 … both owed requires_db PASSED`, `s2-p4-verify…:1124`). `ops/2026-09-19` = `a868a69` **CONFIRMED**. `archiver/append-0919` moved `cdd71fe` → `193a2ca` (stale in the packet; see row 23). `com.cobalt.replay` plist exists at `s2-p4/ops/`. **UNVERIFIABLE FROM READS** by this hub's allowlist: the "56 commits" count (`git rev-list --count main..sprint-2/p4`, no `rev-list` rule) and the migration-SQL claims of `facts.md` §2 (files not read here; the harness/proof behaviour they rest on was).

**Q5 of the desk's list (L68 reading):** Grok — §1 implements the WIDE reading, sound as a stop, "not launchable on this packet"; Gemini — sound, P15 the right call, wide reading leaves an open ruling. Neither settles it; that is Dejan's (L37).

DEPLOY P4 CHECK · grok: DO NOT RUN · gemini: RUN AFTER FIXES 1,2,3,4|1,3,8,9 · astra: METER · houses that read it: 2 of 3 · hub-verified REAL: 11 (blocks the launch: 2) · NOT REAL: 10 · UNVERIFIABLE: 2
