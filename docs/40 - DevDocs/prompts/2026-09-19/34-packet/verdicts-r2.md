## Verdict table
Verified against the real files: `/Users/cobalt/cobalt-wt/ops-2026-09-19/tests/cobalt/test_migrate_proof.py`, `…/src/cobalt/db_migrations/cli.py`, the report there, and `git -C /Users/cobalt/cobalt show` of the branch commits. Packet copies match the real files where compared (say so if they differ: they do not, apart from the fidelity notes above). "blocks" is my read; the desk and Dejan decide (L37).

| # | houses | claim (≤20 words) | what I read (file:line) | VERDICT | fix in one line | blocks the branch shipping? |
|---|---|---|---|---|---|---|
| F1 | astra (B, "R2 NOT FIXED") | test (g)'s `finally` runs `other.commit()`/`close()`/`watcher.close()` before the worker cleanup, unnested; an exception there skips it | `test_migrate_proof.py:1323-1327` `if not committed: other.commit()`, `other.close()`, `watcher.close()`, then `:1343-1345` `if thread.is_alive(): conn.cancel(); thread.join(…)`, `:1351-1353` rollback/close; docstring `:1276-1283` "Whatever the `try` did or raised". The same order stood in round 1 (`git show 543810f`: the fold only moved cancel/join below it) | **REAL — MINOR (hub severity)**. The fold does exactly what round 1's R2 row asked ("move `cancel`/`join`/`rollback`/`close` into the `finally` unconditionally (join with a timeout)": one copy, old `if hung:` copy gone, assertions at `:1355-1372` untouched). What remains is a robustness gap that pre-dates the fold: it needs the `other` session's commit/close to raise; a dead `other` connection makes the server drop its transaction and release the lock anyway, so the worker's ALTER ends by itself. The docstring sentence is a slight overstatement (same class as R3) | nest the `finally` (`try: other… finally: try: watcher… finally: worker cleanup`) or reword the docstring to "whatever the verdict was" — and run the owed dev-DB test | **no** — test file only, `requires_db`, needs a failing cleanup call; R2 stays in the owed dev-DB run either way |
| F2 | gemini (M, Q6) | decorators for the R5 and R2/R3 tests are not in `fold.diff`, so "offline" and "skipped" claims are unverified | Real file: `:1225` `@requires_db` directly above `def test_the_alter_waits_for_an_open_transaction_and_then_completes` (`:1226`); `:1519` blank, `:1520` `def test_a_lock_it_cannot_get_rolls_back_and_says_nothing_was_applied(monkeypatch):` with **no** decorator (grep `-B3` shows `:1517` an `assert`, `:1518-1519` blank); the R5 assertions at `:1548-1558`; no `+def test_` in `fold.diff` (all three houses) | **NOT REAL** as a defect of the fold — a limit of the diff's hunk context, not of the code. Q6 holds as `facts.md` says: no new test function, R5's test runs offline, R2/R3's is `requires_db`. (Their offline pass/skip totals themselves were run by the round-2 build, not re-run here — see Other) | n/a — (widen hunk context, `-U10`, for the next packet) | no |
| F3 | gemini (m) · grok (Q5 note) · astra (Q5 note) | R9's option (ii) leaves a reader of round 1's `## ESCALATE` alone without the premarket case | `git show d860bf7 --stat`: **424 insertions, 0 deletions** (pure append); `git show 0ec31f8 --format= -U0` touches only report `:8`, `:130`, `:157`, `:488`, `:569` — none inside `## ESCALATE` (which starts after `:488`'s "Paths" note); the case is at `:130` (in place) and `## ROUND 2 ESCALATE` 1, and ROUND 2 ESCALATE 6 says round 1's 1–5 stand unrenumbered. `facts.md` states the same trade-off itself | **REAL — MINOR, disclosed trade-off, not a dodge.** The account in `facts.md` is supported by the real commits: round 1's list byte-untouched, headline `ESCALATE: 9` and stop line still true, a genuinely new numbered item elsewhere. Grok and Astra call it honest, Gemini "inadequate"; the residual is only that a round-1-list-only reader misses it — round 1's fix line allowed "add the one ESCALATE line **or** drop the pointer" and it took the second | if the desk wants it inside round 1's list, add a tenth entry AND fix `ESCALATE: 9` in headline and stop line — a ruling, not a bug | no |
| F4 | grok (m) · astra (note) | R7's stop-line change `+2`→`+1` carries no inline "[corrected …]" marker | `git show 0ec31f8 -U0` hunk at `:569`: `… (+2 edited, never re-run)` → `… (+1 edited, never re-run)`, no note on that line; the headline hunk at `:8` does carry `[corrected in round 2, R7: …]` and the round-2 section `## Items R6–R9` records both places | **REAL — MINOR.** The number is corrected and disclosed at the headline and in the R7 write-up; only the stop line itself is a silent rewrite | add a trailing `[R7 corrected in round 2]` to the stop line — or leave it (the desk verified the stop line already, and changing it risks the "committed stop line" the R9 reasoning protected) | no |
| F5 | astra (m) · grok (side note) | round 1's report text still states the 20 s ordering unconditionally after R3 fixed it in the docstring | `grep -n` on the real report: `:311` "the main thread's join gives up **first**, so the message a reader sees is this test's own assertion, never a driver error" and `:528` "the 20 s join fires first (so the reader still sees the test's own message)" — in round 1's "A1's required comment" and `## ESCALATE` 2(b), unmarked; the R3 section (`:697-723`) is the corrected statement | **REAL — MINOR** (I found `:311` as well as `:528`). Historical round-1 wording, not the test docstring R3 named; a reader of those two places alone still meets the unconditional claim | add "[see round 2, R3: conditional on the 5 s headroom]" to both, or leave as history | no |

**R6 / R7 / R8 / R9 — verified against the real before/after** (`git -C /Users/cobalt/cobalt show 0ec31f8 --format= -U0`; the packet's diff cannot show it): R6 `:157` `- 13 failed on ImportError…` → `+ … 12 failed on …` — `7 of the refusal class + the 5 test_the_validator_accepts_it cases [corrected in round 2, R6: this read "13", …]` ✓ marked. R7 `:8` `1 new + 2 edited` → `1 new + 1 edited` + note ✓ marked; stop line `:569` `+2 edited`→`+1 edited` ✗ not marked (F4). R8 `:488` `three ADDITIONAL paths, all as DELETIONS` → `two DELETIONS and one MODIFICATION [corrected in round 2, R8: …]`, `cto-2026-09-19.md` named `M, −60` ✓ marked. R9 `:130` `— see ## ESCALATE 1.` → `so on a trading day at 03:00 ET C2 still FAILs on zero rows … [corrected in round 2, R9: …]` ✓ marked, pointer gone.

**R2 / R3 / R5 real code** — R2: see F1. R3: docstring `:1246-1247` item 1 now ends "the test has its verdict."; `:1254-1274` conditional paragraph naming the 5 s headroom, the 50 ms probe and the third-session/slow-probe failure; `thread.join(timeout=LOCK_CEILING_S)` at `:1319` unchanged; no timing-code hunk (option (a), named in the round-2 report as "took fix (a), not (b)"). R5: `cli.py:687-719` — comment block `:692-699`, message `:706-715` now `…so this migration's own waiting request is already gone from pg_locks; the HOLDER is still there. Find it: SELECT l.pid, l.mode, l.relation::regclass, a.state, a.query FROM pg_locks l JOIN pg_stat_activity a USING (pid) WHERE l.granted AND l.relation IS NOT NULL; …`; `NOTHING WAS APPLIED`, `{lock_timeout_s}`, the raise-the-flag guidance and `Postgres said: {e}` at `:705`, `:716-718` intact; test `:1538` `assert "pg_locks" in message` kept and still true (message contains `pg_locks` twice); new asserts `:1548-1558` are false on the old text (`WHERE NOT granted` present; `pg_stat_activity`, `l.granted` absent) and true on the new. Commits `543810f`/`8ed2beb`/`be79b74` touch only `test_migrate_proof.py` and `db_migrations/cli.py` (49+26+17 = 92 test lines, 23 `cli.py` lines — matches the range's diff-stat).

**Confirmed CLEAN, all three houses and by me:** `fold.diff` touches only the two named files; nothing under `configs/` or `ops/`; no scoring/ranking/card reach (L52: the only production hunk is diagnostic text); no secret or connection string (repo paths and `env.DEV_DB_NAME` only); no second copy of a rule (L3); no hunk touches `_probe_all`/`SET LOCAL lock_timeout`/`code:`/`FAILED:` — R1, R4, R11 untouched.

**Other, unverified / hub notes:**
- The R5 query has no table filter (`WHERE l.granted AND l.relation IS NOT NULL` lists every granted relation lock in the database, including the operator's own session); the message says it in words ("any granted lock on the table it touches"). Noisy, not wrong — my own observation, no house raised it.
- Not re-run here: the offline suite (`1608 passed, 298 skipped, 0 failed`, `restarts fc1d967..HEAD = com.cobalt.radar`). Round 2's own build ran both; this launch line carries neither `uv run pytest *` nor `uv run cobalt jobs restarts *`, so the round-2 numbers are the build's claim, confirmed only by the arithmetic that no `+def test_` exists. Missing rules: `Bash(uv run pytest *)`, `Bash(uv run cobalt jobs restarts *)`.
- The `fold.diff` was not independently re-derived with a fresh `git diff` (no `git -C … diff` rule for this repository); instead `git show --stat` of the three code commits sums to the same 92 + 23 lines.
- Gemini's `VERDICT: FOLD UNSAFE` is inconsistent with its own findings (R2–R9 FIXED, worst finding one MAJOR that I found NOT REAL). I record it verbatim as its verdict and do not treat it as a finding.
- Owed regardless of this round: the `cobalt_dev` run of the three never-run `requires_db` tests (R2's fix most of all), and the R1/R11 experiment.

**Round-1 finding tags (mine):** R2 FIXED (residual F1, MINOR) · R3 FIXED · R5 FIXED · R6 FIXED · R7 FIXED (stop line unmarked, F4) · R8 FIXED · R9 FIXED (F3).

OPS CHECK R2 · grok: 0/0/1 FOLD CLEAN · gemini: 0/1/1 FOLD UNSAFE · astra: 1/0/1 FIX FIRST R2 · houses that read it: 3 of 3 · hub-verified REAL: 4 (blocks shipping: 0) · NOT REAL: 1 · UNVERIFIABLE: 0 · R2: FIXED · R3: FIXED · R5: FIXED · R6: FIXED · R7: FIXED · R8: FIXED · R9: FIXED
