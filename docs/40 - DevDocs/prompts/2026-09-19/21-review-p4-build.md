MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-p4-0919`, launched by the CTO desk in the background. NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `20-review-archiver-build.md`'s launch line (itself copied from `18-review-ops-0919.md`'s, itself from `14-review-p4-verify.md`'s), with exactly ONE string changed — the `mkdir` path, from `scratch/review-archiver-0919` to `scratch/review-p4-0919` — because this check needs no rule `20` did not already carry (the target branch's commits are reachable with the SAME `git -C /Users/cobalt/cobalt show*` / `log*` rules: `sprint-2/p4` is a branch in the same repository `20` already had `show`/`log` access to, so no `git -C .../s2-p4 *` rule is added; the unused `git -C .../s2-p2-cards *` rules stay, unused, because they are not the string that changed). If this check turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/21-review-p4-build.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of a BUILD (LAWS L67: every build is checked by at least three tribunal members before it ships; Fable is NOT one of the three checkers today — the checkers are Astra (OpenAI), Grok (xAI), Gemini (Google)) — branch `sprint-2/p4` (code tip `a59ee8c`; the two report-only commits `4cc6859` — the stop line's own `<close commit>` — and `d5947e7` sit above it; merge-base with `main` is `919d562`, `main` now `a89105f`, docs-only since). This is the S2-P4 missed-movers-benchmark + formation-replay rebuild: rebased onto main's P2/harness deploy, DB migrations 0008 (`system.radar_membership` rank columns, `system.movers_daily`) and 0009 (`"user".picks`, `"user".missed`), a `kind: compare` smoke-check primitive restoring K8's machine assertion, K9's short-side fix (`movers_by_side`), a plan-text correction, one shared column-set source for the radar and movers Finviz requests — and the `Asset Type` not-equity rule EVIDENCE-TESTED AND ESCALATED, NOT BUILT (real data disagreed with the candidate rule on 16 of 5,718 fund rows; `not_equity`/`REQUIRED_HEADERS`/`radar.yaml` are byte-identical to `main`). This code has not merged and does not run against production today. Its migrations and `requires_db` tests have run ONCE against `cobalt_dev` (forward, rollback, re-forward — round trip ok) and never against production. The check is due now per the build's own stop line, which names "three-house check" first in its `OWED:` list. **This is the FIRST code-level check this branch has ever had** — a 2026-09-16 review (`02-p4-review.md`) read only the PLAN TEXT (commit `f3cc006`, now `1fa0082` after rebases), by Astra alone, before any P4 code existed and two days before L67's three-house rule was ruled; no chunk of code (`de5d420` chunk A through `a59ee8c` this run's own DB-red fixes — all 45 commits `main..sprint-2/p4`) has ever been read by another house. Round 1 of at most 3 (L67: ≤3 rounds per house while meter allows, never fewer than 2 houses total). Dejan ruled R16 today, 11:05 ET, AFTER this build's own stop line: fund rule C (not-equity when `Asset Type` is non-blank OR `Industry` = `Exchange Traded Fund`) — it is being built as a SEPARATE tests-first chunk on this same branch and will come back to you as a FOLD round on this check once it lands; today's `Asset Type: ESCALATED, not built` is the correct outcome of the code you are reading, not a defect to flag. Two of the kept test-code diffs (`code-08`, `code-10`) embed real ticker strings already committed on the branch, drawn from the public whole-market Finviz movers/radar scan — not from Dejan's personal trading screens (`facts.md`'s L32 note has the detail). DO NOT STOP until the report ends `P4 CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. This check is standing law, not a one-off approval: LAWS.md L67 ("every DEVELOPMENT (build) is checked by at least THREE members of the tribunal"), ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, emergency/override/rounds clauses R19–R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The build's own debt: the report's last line names `OWED: three-house check, receipt-reference tribunal, deploy prompt` — prove with `git -C /Users/cobalt/cobalt log --oneline -4 sprint-2/p4 -- "docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md"` (**name the branch ref**: unlike a report committed on `main`, this report lives ONLY on `sprint-2/p4` — a bare `git log -- <path>` from `/Users/cobalt/cobalt`'s current branch will NOT find it; the branch ref must be given explicitly, the same lesson `18`'s and `20`'s runs recorded). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the build's own report's claims (`build-report-1-*.md` and `build-report-2-*.md` in the packet are the builder/hub's account — treat them as claims) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L52 nothing in this build may touch scoring, ranking or a value that reaches a card — confirm it does not, do not assume it · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-p4-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. TWO LESSONS FROM `18`'S AND `20`'S RUNS, carried here: (a) the hub's Read→Write copy strips trailing whitespace, so a copied file can be shorter than its source by its blank-context lines — this is HARMLESS, state it once in `## Packet`'s fidelity table if you observe it, do not chase it as a defect; (b) `git log -- <path>` for anything committed only on a feature branch (this build's reports, or the plan, or any file that exists only on `sprint-2/p4`) MUST name the branch ref explicitly — a bare `git -C /Users/cobalt/cobalt log -- <path>` run from `main` will silently return nothing and look like a missing commit rather than a wrong command. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/REVIEW.md` — the same gitignored-scratch pattern `01`, `14`, `18` and `20` use, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — ALREADY pre-staged BYTE-IDENTICALLY by the desk (`cp`, not Read→Write) into `scratch/review-p4-0919/` before you were launched. `mkdir -p scratch/review-p4-0919` first (idempotent — the directory already exists; this only guards a relaunch where it does not). Then VERIFY, never blind-trust: `ls scratch/review-p4-0919/` and `wc -c scratch/review-p4-0919/*` against the SIZE TABLE below (file → bytes, source = `docs/40 - DevDocs/prompts/2026-09-19/21-packet/`). Only a file that is MISSING or whose byte count DIFFERS from the table is re-staged by you, Read → Write, byte-identical (the trailing-whitespace-strip lesson below applies to THAT file only). State the verify result as a table in `## Packet`: file · table bytes · `wc -c` bytes · match/RESTAGED.

SIZE TABLE (19 files, 863,573 bytes total):
| file | bytes |
|---|---|
| `code-01-radar.diff` | 42,135 |
| `code-02-replay-core.diff` | 47,524 |
| `code-03-replay-movers-runner-cards.diff` | 82,494 |
| `code-04-smoke.diff` | 59,902 |
| `code-05-cards-aset-settings-jobs-archiver-dayopen-misc.diff` | 65,314 |
| `code-06-migrations.diff` | 14,663 |
| `code-07-configs-ops.diff` | 29,510 |
| `code-08-tests-radar.diff` | 32,570 |
| `code-09-tests-replay-a.diff` | 65,033 |
| `code-10-tests-replay-b.diff` | 56,812 |
| `code-11-tests-smoke-tenancy-settings-finviz-jobs.diff` | 93,448 |
| `code-12-tests-cards-p4.diff` | 62,607 |
| `code-13-fixtures-cutter-script.diff` | 25,996 |
| `facts.md` | 18,867 |
| `QUESTIONS.md` | 9,158 |
| `spec-01-plan-2026-09-15.md` | 63,011 |
| `spec-06-verify-2026-09-19.md` | 37,896 |
| `build-report-1-build2-2026-09-18.md` | 18,506 |
| `build-report-2-verify-2026-09-19.md` | 38,127 |

WHAT EACH FILE IS: `code-01` through `code-13` together ARE the CODE portion of the three-dot diff `git -C /Users/cobalt/cobalt diff main...sprint-2/p4 -- src configs ops tests` — ALL `src/`, `configs/`, `ops/`, both migrations, and every test-CODE file (`tests/**/*.py`, including the fixture-cutter script), 678,008 bytes, split by area only because that exceeds a comfortable single read, individually ≤ ~100 KB (largest 93,448), none needs a `.partN` split — the 13 sum to exactly that byte count. SIX real-shape fixture DATA files (JSON/CSV recordings of a trading day or a Finviz export — no logic) are deliberately NOT staged: `facts.md`'s "EXCLUDED FROM THE PACKET" table names each one, its byte count, what it is, and which kept test reads it, and proves kept (678,008) + excluded (902,624) = the original full diff (1,580,632), exact · `facts.md` — the desk's pre-computed measurements: commit list, tip, merge-base, diff-stat by area, the excluded-fixtures table, a file:line confirmation for each of rows 2/4/6/8, the two migrations' one-line descriptions, confirmation that `Asset Type`/`not_equity`/`REQUIRED_HEADERS` are untouched, the RESTARTS claim checked against the real diff, the L32 ticker note, the R16 fold-round note, the full stop line, all 16 ESCALATE items, the (g) prior-check history, AND the "Specs NOT in the packet" table (three intermediate build prompts, all on `main`, each with its exact `git show main:"<path>"` pull command) · `spec-01-plan-2026-09-15.md` (the Astra-reviewed plan) and `spec-06-verify-2026-09-19.md` (`13-p4-verify.md`, today's verify prompt) — ONLY these two specs are staged (coordinator decision: one review run, no A/B, keep the packet to what a verdict needs; the other three build prompts are a `git show` away per `facts.md`'s table, not duplicated here) · `build-report-1-build2-2026-09-18.md` and `build-report-2-verify-2026-09-19.md` — verbatim copies of the two reports as committed on `sprint-2/p4` — the predecessor's and this run's own accounts of what was done; `QUESTIONS.md` calls both CLAIMS, not evidence · `QUESTIONS.md`, verbatim, already staged as its own file — tells the houses the six fixtures and the three unstaged specs exist but are not in the packet, and that a finding needing them is `UNVERIFIABLE FROM PACKET` (the hub, not the reviewer, pulls them with the launch line's own `show*` rule). If a RESTAGED file is too large for one Write call, split `name.part1`, `name.part2` and say so in `## Packet`.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-p4-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: a `code-*.diff` claim against `git -C /Users/cobalt/cobalt show sprint-2/p4:<path>` (the branch's tip content — reachable from `/Users/cobalt/cobalt` under the SAME `show*` rule, no worktree `cd` needed) and against the real files under `/Users/cobalt/cobalt-wt/s2-p4/` with Read and `grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (for example, an independent re-derivation of the diff itself with a fresh `git diff` against `/Users/cobalt/cobalt-wt/s2-p4` rather than reading the pre-staged files, or running `uv run pytest`/`uv run cobalt jobs restarts` to re-check a suite or RESTARTS claim) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-p4-0919/`. Last line: `P4 CHECK · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
