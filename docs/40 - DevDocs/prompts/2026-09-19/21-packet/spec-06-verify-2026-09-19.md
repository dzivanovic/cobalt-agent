MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50; migrations ride this branch, L29) · SEAT: P4 verify hub `p4-verify-0919`, launched by the CTO desk in the background. NO NEW RULE: every rule below is copied byte for byte from a launch line Dejan already approved for THIS worktree and THIS job shape — (a) 2026-09-18 06:46 ET, `cto-2026-09-18.md` §4 R1 list (3), as committed in `prompts/2026-09-18/01-p4-followup.md` (all of it except `Bash(grok *)`, DROPPED: the house check is a separate hub); (b) 2026-09-17 06:11 ET, `cto-2026-09-17.md` §4 R6, as committed in `prompts/2026-09-17/04-p4-relaunch.md` — ONLY the three rules the movers fixture re-cut needs (the `_fetch_movers.py` run, its `rm`, the fixture cutter). Not brought to him again (`prompts/UNATTENDED-LAUNCH.md` §1.3). Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p4` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-verify-0919 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly *)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(COBALT_ENV=production uv run python /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py*)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py)" "Bash(uv run python tests/fixtures/replay/_cut_p4_fixtures.py*)" "Bash(claude -p *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git rebase main)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(touch scratch/*)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" "CronCreate" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every launch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it · METER: Anthropic medium (two Opus headless chunks + conflict resolves; probe first, L47); Codex = NONE; Grok = NONE; Finviz = TWO export requests, Saturday, radar `idle:overnight` · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# S2-P4 verify — rebase P4 onto main (deploy 1 put P2 + the migration harness there at 08:25 ET today), build the two `Asset Type` fixes he confirmed (09-18 R11) on EVIDENCE FIRST, settle triage rows 2 · 4 · 8, then the DB verification P4 has owed since 09-17 (0008/0009 on top of 0007 on `cobalt_dev`, row 6's K3 SQL actually run, rollback round trip) → `READY`. LAW STEP of this work item (L67): this run COMPLETES THE BUILD and verifies it. After it, in this order and none of it yours: at least three houses check the build · the tribunal rules on the receipt-reference design question (09-18 §21 row 9) · the desk writes the deploy prompt · his approval · deploy. You never merge, never deploy, never push, never write production (one production-token READ of Finviz, step 2.1, is the only command that leaves dev). DO NOT STOP until the report ends `READY …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable `318239db`, 2026-09-19), not by Dejan; a prompt file is not an approval. His words, with times, are committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-17.md` §4 **R6** (06:11 "approve." — the P4 hub list incl. the `_fetch_movers.py` vault-token fetch, `.env` cp/rm by exact path, `COBALT_ENV=dev` migrate/pytest/validate/replay dry-run, read-only real-vault pytest, `claude -p`, git add/commit/rebase main, plain reads, `CronCreate`) and **R5** (unattended standard); `cto-2026-09-18.md` §4 **R1** (06:46 "Approved", list (3) for this worktree) and **R11** (16:02 "Keep as designed." — funds stay out of the missed-movers benchmark; the two `Asset Type` defects are BUILD fixes); `cto-2026-09-19.md` §4 **R3** + **R6** (no idle lane, never a skipped law step) and **R5** (the law is followed, not re-asked). This launch line adds NO rule, so there is no new approval row: compare each rule string above with the launch lines committed in `prompts/2026-09-18/01-p4-followup.md` and `prompts/2026-09-17/04-p4-relaunch.md` (`grep -c` the string in the file). Prove the rows are committed: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"`. This prompt was read by a second house before launch (`prompts/2026-09-19/14-review-p4-verify.md`, 09-18 R8 / L67 floor) — that is a check, never an approval. Laws behind each sensitive step: L61, L62, L41 interim (`.env` and the Finviz token by NAME only, never printed, removed after), L50, L29, L55, L36 (you launch builders; builders launch nothing). A rule here that is in neither committed launch line, or a row that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP. Only the dialog is removed, because nobody sees a `--bg` dialog. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. The desk watches that line. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step (or, where none is named, do not build that item), continue; never block on an answer, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(s2-p4): <chunk> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit paths under `src/`, `configs/`, `tests/`, `ops/`, `docs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory that contains `scratch/` (review round 1, R11: `scratch/` is NOT gitignored on this branch, and it holds the raw exports, the fetch script and the evidence file with tickers from his screens — L32). After every commit: `git show --stat HEAD` lists no `scratch/` path, else `FAILED: <step> — scratch path committed <path>` (the desk repairs it; you have no reset rule). BEFORE ANY STOP LINE, on every path: if `/Users/cobalt/cobalt-wt/s2-p4/.env` may exist, `rm` it and prove it with `ls -la .env` (the file is gitignored, so `git status` can never prove its absence — only `ls` does); same for `scratch/_fetch_movers.py`. BUILDERS (review round 1, R2): every BUILDER `claude -p` launch (NOT PREFLIGHT's one-word meter probe, which is typed exactly as written there, N6) is the shape of `01-p4-build.md` step 2 with its `--permission-mode acceptEdits --allowedTools …` list byte for byte, as ONE bare command WITHOUT the leading `cd … &&` (you are already in the worktree); a builder runs offline tests only — YOU run every `COBALT_ENV=dev` command; NO builder runs while `.env` is in the worktree (its list carries `cat *`): builders finish before 4.1 or start after 4.8. A killed builder's uncommitted edits are wip-committed by you before any relaunch. ONE exception: a rebase stopped on a conflict you could not resolve is left EXACTLY as it stands (no commit is possible, `git rebase --abort` is not in your list): write `git status` verbatim into the report, then the `FAILED` line; the desk aborts it.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (hub, L59) → this worktree's `docs/40 - DevDocs/reports/s2-p4-build2-2026-09-18.md` (`## ESCALATE` 1–12: rows 2, 3, 4, 6, 7, 8 are this run's inputs; §4 row 3 = the K9 proposal) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §21 (the desk's triage of those twelve) and §22 (the `Asset Type` facts) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/01-p4-followup.md` §1.2 (THE CONFLICT PROCEDURE — binding here verbatim) and §4 (the verification shape) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md` step 2 (builder launch shape) and step 3 (hub verification after every chunk) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/04-p4-relaunch.md` B3 (the `_fetch_movers.py` shape) → `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` lines ≈184–191 → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-19.md` last line (`STACK DEPLOY DONE 2893a7f … migrations 0006/0007 applied`). Laws, one line each: L46 one run one commit per chunk, clean before any wait · L54 you rebase, the desk's deploy merges · L29 Opus floor for every code chunk (you write briefs, reports, the fetch script; never `src/`, `configs/`, `tests/`) · L35 artifact, not claim · L45 real-shape fixtures, never edited to fit a parser; attribution, dates, preset ids stripped · L3 one path, one source for the column set and for the not-equity rule · L1 + L10 a config-shape change fails loud on the old shape · L42 every config-shape change is named for RESTARTS · L52 nothing that touches scoring, ranking or what reaches the card is built here · L53 the Finviz ceiling is his; two manual requests on an idle Saturday are recorded, not assumed free · L57 every derived number replayable · L32 no user data in the repo or in your committed report (tickers from HIS screens stay in gitignored `scratch/`) · L41 credentials by name only. Report (L48): `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` in THIS worktree — §0 Headline ≤5 lines (line 1 `DO NOT STOP until READY`) → `## AUTHORIZATION` → `## PREFLIGHT` → one section per step, tables → `## ESCALATE` → last line = `CONTINUE:` / stop line / `FAILED`. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; long runs (`claude -p`, the suites, `migrate`) go `run_in_background`. Counting is done by you from the tool result, never with a pipe. NOT in your list, so never typed: `git rev-parse`, `git cherry`, `git rev-list`, `git rebase --abort`, `git reset`, `git checkout`, `git stash`, `git push`, `python` other than the two listed scripts, any `COBALT_ENV=production` command other than the one fetch, `--allow-prod`, `launchctl`. MID-RUN DENIAL = `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry a denied command in another shape, never ask another session to run it.
PREFLIGHT (first two minutes; one row each: rule · command · exit · allowed/DENIED + reason verbatim; every probe sits inside its own rule's pattern): `date` · `git status --porcelain` (empty or `?? scratch/` — ANYTHING else, → `FAILED PREFLIGHT: worktree not clean — <lines>`, R14) · `git status` (LONG form — `--porcelain` shows a paused clean rebase as empty; quote its first two lines; "rebase in progress" → `FAILED PREFLIGHT: rebase in progress`, N7) · `git log --oneline -1` (first launch: `2a8784c`; a resume after a `CONTINUE:` line: the tip that line's commit left — say which) · `git -C /Users/cobalt/cobalt status --porcelain` · `git -C /Users/cobalt/cobalt log --oneline -1` (record it: this is the main you rebase onto) · `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` → `COBALT_ENV=dev uv run cobalt db migrate --help` → `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` shows it gone (if the `migrate --help` probe is DENIED, the `rm` + `ls` still run before the `FAILED PREFLIGHT` line, R3) · `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (also the L47 meter probe; a usage-limit message → record the reset time, `CronCreate` a wake-up at reset + 5 min, commit the report, wait) · `uv run pytest --co -q tests/cobalt/test_tenancy.py`. The two script rules have no harmless variant and are probed by their first real use (`UNATTENDED-LAUNCH.md` §6). Any denial → `FAILED PREFLIGHT: <rules>`.

## 1. Rebase onto main
1.1 FACTS THE DESK MEASURED 08:36 ET (read-only; you re-measure with your own commands and the two must agree): tip `2a8784c`, tree clean, `main..HEAD` = 65 commits = 34 of the OLD P2 lineage (everything up to `976528f`; each one patch-identical to a commit already on main through the stack's re-land) + 31 of P4's own (`976528f..HEAD`). Re-measure: `git log --oneline main..HEAD` (count the rows: 65; main may have gained docs-only desk commits since — they do not change this count) · `git log --oneline 976528f..HEAD` (31) · `git log --oneline --cherry-mark --right-only main...HEAD` (34 rows marked `=`, 31 marked `+`; every `=` row is at or below `976528f`). A different split → `FAILED: step 1.1 — cherry split <x>/<y>, expected 34/31`, nothing run.
1.2 `git rebase main`. EXPECTED: git drops the 34 patch-identical commits by itself and replays exactly the 31. Conflicts: `01-p4-followup.md` §1.2's procedure exactly — brief in `scratch/rebase-brief-v<n>.md`, ONE Opus headless run per conflict stop, keep BOTH intents, you verify (`git diff --check`, no conflict markers by `grep -n "^<<<<<<<\|^>>>>>>>" <file>`, the touched tests pass), `git add <files>`, `GIT_EDITOR=true git rebase --continue`; three failed resolves on one conflict = `FAILED` (rebase left as it stands, see RECOVERY RULE). Expected zones: `src/cobalt/db_migrations/placement.py`, `__init__.py` and `cli.py` (main now carries the migration harness — streamed proof, `--proof-only`, REPEATABLE READ, ambiguous-name refusal — next to P4's 0008/0009 entries: BOTH stay, numeric order, the harness code is main's byte for byte), `tests/cobalt/test_radar_*.py`, `tests/cobalt/test_tenancy.py`, `configs/cobalt/jobs.yaml`. A conflict stop whose commit subject is one of the 34 P2 commits (the rebase is REPLAYING P2) → `FAILED: step 1.2 — rebase replays P2 commit <sha> <subject>`, never skipped by hand, never improvised.
1.3 After: `git log --oneline main..HEAD` = 31 rows, each subject one of step 1.1's 31 (list the two columns side by side; a subject that is not in step 1.1's list, or one missing → `FAILED: step 1.3 — subject mismatch <which>`, R13) · `git diff main HEAD -- src/cobalt/db_migrations/cli.py src/cobalt/db_migrations/placement.py src/cobalt/db_migrations/__init__.py` (the FULL diff, not `--stat`) — read it: every changed line is P4's (0008/0009 entries and what they need); a line of main's harness altered or missing → `FAILED: step 1.3 — harness changed <file:line>` · `uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote the line; 09-18's offline line was `1714 passed`; main added tests, so more is expected, fewer is a finding). Any count other than 31 → `FAILED: step 1.3 — <n> commits`. Last line `CONTINUE: step 2`.

## 2. Chunk AT — `Asset Type` (09-18 R11, `cto-2026-09-18.md` §22). EVIDENCE BEFORE CODE.
FACTS (desk, read from the files 08:40 ET — verify each before you brief): the radar requires the header and asks Finviz for columns 0–150 (`configs/cobalt/radar.yaml` `export:` block; `src/cobalt/radar/collector.py:161` builds `c` from `range(151)`); P4's movers collector ALREADY sends the same column set, as a SECOND hard-coded literal (`src/cobalt/replay/movers.py:210`, `range(151)` again — an L3 duplicate); what lacks the column is the FIXTURE: `scratch/movers-gainers-raw.csv` has the 21-column default view, so the 09-17 fetch script evidently sent no `c=` (NOT verified — that script was deleted as ordered) and the fixture never had the shape the collector really receives (L45). A live radar export has 151 columns, `Industry` = column 5, `Asset Type` = column 101. `not_equity.values: [Exchange Traded Fund]` is compared against `Asset Type` (`radar/runner.py:144`, `replay/movers.py:313`) and matches nothing: Finviz fills `Asset Type` with a fund's asset class (seen 09-10: Bonds, Commodities & Metals, CryptoCurrency, Equities (Stocks), Multi-Asset, Preferred Stock) and leaves it blank for an ordinary stock (09-18: 16 of 16 stocks blank).
2.1 THE EXPORT (you, not a builder — `04-p4-relaunch.md` B3's shape): Write `scratch/_fetch_movers.py`: `FinvizApiClient._resolve_vault_credentials`, the token never printed, never logged, never written; TWO requests to `/export/screener` with `v` read from `/Users/cobalt/cobalt/configs/cobalt/radar.yaml` `export.v`, `c` = `0,1,…,150`, `o` = `-change` (gainers) and `change` (losers) — the exact parameters `replay/movers.py` `_params` builds (quote that function in the report); no `f=` string; one second apart; the script itself writes `/Users/cobalt/cobalt-wt/s2-p4/scratch/movers-gainers-raw.csv` and `…/movers-losers-raw.csv` (overwriting the 21-column files — first `ls -la scratch/movers-*-raw.csv` and record sizes) and prints ONLY: status code, row count, column count, per file. TOKEN SAFETY (review round 1, R1 — three houses; the repo's own Finviz client builds `…&auth=<token>` into the URL and an httpx error's text CONTAINS that URL): the script goes through the transport the movers collector already uses — `from cobalt.archiver.collector import resolve_token, finviz_get, scrub` (`src/cobalt/archiver/collector.py:74, :78, :175`; see `src/cobalt/replay/movers.py:48, :222`): `resolve_token()` fetches the credential, `finviz_get(path, params, token, on_metrics=…)` is the sole transport — both are `async` (`asyncio.run`), the transport adds `auth` to the request ITSELF and raises a `CollectorError` whose text is already scrubbed, and the status code comes from its `on_metrics` callback (`FetchMetrics.status`) — never the script's own `httpx`/`requests` call, never the token placed in `params`, a URL or an f-string by the script; it NEVER calls `raise_for_status` itself and never touches `e.__cause__` (the chained httpx error is NOT scrubbed); the whole body sits in ONE `try/except Exception as e` whose handler prints exactly `ERROR <type(e).__name__>` and nothing of `str(e)`, no traceback (`sys.exit(2)`, never a bare re-raise); it never prints, logs or writes a URL, a header, a params dict or the token; it configures no logger; BEFORE `resolve_token()` is called it silences BOTH logging systems (review round 2, N1 — three houses): `logging.disable(logging.CRITICAL)` for the standard library (that is what silences `httpx`, which logs the request line at INFO) AND loguru's `logger.remove()` (which silences loguru only). THE RETRY SHAPE (N5): `finviz_get` raises `CollectorError` on a non-200, so the retry is an INNER `try` that catches, prints NOTHING, waits and retries once; the ONE printing handler described above is the OUTER one and wraps everything, `asyncio.run(...)` included. THE RETRY IS INSIDE THE SCRIPT (you have no `sleep` rule): a non-200, a token error or a column count other than 151 → the script waits 60 s itself and tries ONCE more, then exits 2. BOTH responses are validated in memory first; the two raw files are written only after BOTH are good (a failed run leaves the old 21-column files untouched). Before the run, YOU read your own script once against this paragraph, line by line, and write `token-safety: checked` with the line numbers into the report. Run: `cd /Users/cobalt/cobalt` → `COBALT_ENV=production uv run python /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py` → `cd /Users/cobalt/cobalt-wt/s2-p4` → `rm /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py` → `ls scratch/_fetch_movers.py` shows it gone — the `rm` + `ls` run on EVERY path, the failed one included, BEFORE any `FAILED` line is written. Expected: 151 columns, header contains `Asset Type`. Exit 2 → `FAILED: step 2.1 — exit 2, <the status code / row count / column count lines only>`; you NEVER copy anything else from that tool result into the report, and if the tool result shows anything that looks like a URL or a key, the report says only `FAILED: step 2.1 — output withheld, possible credential in tool result` (the desk reads it and rotates). Known and accepted (R19, unverifiable from reads): `uv run` in the production checkout may touch its `.venv`; the same shape ran on 09-17 and the checkout was synced by this morning's deploy. Record under `## ESCALATE` for L53: two Finviz requests at <time>, radar `idle:overnight`, archiver not running.
2.2 THE EVIDENCE (builder run AT-0, Opus headless; brief `scratch/at0-brief.md`): `tests/fixtures/replay/_cut_p4_fixtures.py` gains a read-only `evidence` mode (`uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence`) that reads every `*.csv` under `/Users/cobalt/cobalt/data/radar-cache/` (absolute path, read only) plus the two new raw movers exports, and writes `scratch/asset-type-evidence.md`: files read, rows read; table A — every distinct (`Asset Type`, `Industry`) pair among rows whose `Asset Type` is non-blank after `strip()`, with its row count; table B — every distinct `Asset Type` value (blank shown as `<blank>`) among rows whose `Industry` is `Exchange Traded Fund`, with its row count; table C — the DISAGREEMENT rows: non-blank `Asset Type` with an `Industry` that is not `Exchange Traded Fund`, and `Industry` = `Exchange Traded Fund` with a blank `Asset Type` (file, ticker, both values — tickers stay in `scratch/`, your committed report carries counts and value pairs only, L32). You run it, read the file, copy tables A and B and C's COUNTS into the report.
   DECISION RULE, yours, mechanical, in THIS order (review round 1, R7 — a sample of one or two funds proves nothing): (0) ZERO FUNDS — tables A and B both empty → `Asset Type: ESCALATED — no fund row in the sample`: the rule cannot be proven from real data (L45); build 2.3, and 2.5 WITHOUT the fund row (see 2.5), nothing of 2.4; continue with step 3. (i) SAMPLE FLOOR — the evidence file also counts DISTINCT fund tickers (rows of table A or B) and DISTINCT non-blank `Asset Type` values; fewer than 10 distinct fund tickers, or fewer than 2 distinct `Asset Type` values → `Asset Type: ESCALATED — sample too thin (<n> funds, <v> values)`, build 2.3 and 2.5 only, nothing of 2.4's rule, continue with step 3. (ii) floor met AND table C has 0 rows → "non-blank `Asset Type`" and "is a fund" agree on every real row → build 2.3–2.5. (iii) Table C has ≥1 row → `Asset Type: ESCALATED` — commit the evidence mode, write the C value pairs + counts under `## ESCALATE` with `ASK DESK: which rule — non-blank Asset Type, Industry = Exchange Traded Fund, or either? [<time>]`, build NOTHING of 2.4's rule (2.3's column source and 2.5's fixture are still built — they do not depend on the rule), continue with step 3. (The zero-fund case is branch (0) above and is decided there, once — review round 2, N2.)
2.3–2.5 THE CHANGE (builder run AT-1, Opus headless; brief `scratch/at1-brief.md` = this block + the evidence file), TESTS FIRST — each new or changed test fails before the edit and passes after, evidence in the builder's report `docs/40 - DevDocs/reports/s2-p4-verify-opus-AT-2026-09-19.md`:
   2.3 ONE SOURCE FOR THE COLUMN SET (L3): `replay/movers.py` `_params` takes `c` from the same place the radar collector does (one function or one constant owned by `cobalt.radar`, derived from `config.export.columns`); the second `range(151)` literal dies; a test proves both request builders produce the identical `c` for the shipped config.
   2.4 ONE NOT-EQUITY RULE (L3, R11): not-equity = `Asset Type` non-blank after `strip()`. ONE function (on the `not_equity` config model or beside it in `cobalt.radar`) called by BOTH `radar/runner.py` (≈144) and `replay/movers.py` (≈313). `configs/cobalt/radar.yaml` `not_equity:` changes shape — the `values:` list goes (the 09-10 comment above it is rewritten to say what Finviz really does, citing the evidence counts); the Pydantic model REFUSES the old shape loudly (a config still carrying `values:` fails validation with the key named — L1, L10; test it). Tests with a REAL fund row from the new export (first) and a real blank-`Asset Type` stock row: fund → dropped with reason `not_equity` in the radar AND out of the benchmark in movers; stock → kept in both; whitespace-only → kept. `REQUIRED_HEADERS` in `replay/movers.py` gains `Asset Type` (plan line 191). The `unreported` branch: `ls /Users/cobalt/cobalt/data/radar-cache/*/movers-*` — no retained production movers export exists (P4 never deployed) → no retained file can lack the column → the branch and its tests go (one path, L1); if that `ls` finds files, KEEP the branch and ESCALATE the file list. `not_equity_values` leaves the stored inputs / `gate_detail` (it no longer exists); whatever replaces it (`not_equity_rule: non_blank_asset_type`) is recorded so the row replays (L57) — this changes `inputs_sha256` for rows computed from now on, safe ONLY while 0009 is undeployed: add it to build2's ESCALATE 7 note for the deploy prompt.
   2.5 THE FIXTURE (L45 + companion): `uv run python tests/fixtures/replay/_cut_p4_fixtures.py` re-cuts `movers-{gainers,losers}.real-shape.csv` from the new 151-column raw files — real header row byte for byte, the rows the cutter already takes, PLUS at least one real row whose `Asset Type` is non-blank (if the top rows hold none, the cutter takes the first such row further down and says so in its header comment; if the raw exports hold NO non-blank `Asset Type` row at all, the cutter cuts what exists and says so — a fund row is never fabricated, N2); attribution, dates and preset ids stripped as the cutter already does; the fixture is never edited by hand. A test asserts the movers fixture's header row equals the radar real-shape export fixture's header row (the collector receives ONE shape). The leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures` still passes.
2.6 You verify as chunk B was verified (`01-p4-build.md` step 3): tests-first evidence, `uv run pytest -q tests/cobalt tests/taxonomy` 0 failed, `git diff --stat`, `grep -rn "range(151)" src/cobalt` (AT MOST one hit), `grep -rn "not_equity" src/cobalt` (both callers reach the one function), `uv run cobalt jobs restarts main..HEAD` names `com.cobalt.radar` for `configs/cobalt/radar.yaml`. Commits: `test(fixtures): Asset Type evidence mode for the P4 cutter` · `fix(s2-p4): movers take the radar's column set from one source; not-equity = non-blank Asset Type, one rule for radar and movers (09-18 R11)`. If step 2.2 ended `Asset Type: ESCALATED`, the second commit's message and every note say ONLY what was built (`fix(s2-p4): movers take the radar's column set from one source; fixture re-cut at 151 columns — the not-equity rule is ESCALATED, not built`) and the note below is replaced by "`not_equity` unchanged — no RESTARTS from this chunk" (R20). Under `## ESCALATE`, for the deploy prompt, when the rule WAS built: "`configs/cobalt/radar.yaml` `not_equity` changes shape and changes what the LIVE radar drops (any fund, where today nothing is dropped) — RESTARTS `com.cobalt.radar` (L42); the desk names it in the deploy approval list". Last line `CONTINUE: step 3`.

## 3. Chunk TR — triage rows 2, 4, 8 of `cto-2026-09-18.md` §21 (builder run TR, Opus headless; brief `scratch/tr-brief.md`; tests first — the brief carries the two STOP sentences of rows 4 and 8 below VERBATIM, so the builder meets the L52 guard where it works, R15; the SAME builder run also writes step 4.6's K3 test file, test only, so that no builder runs while `.env` is in the worktree; that ONE file is exempt from the red-first rule offline — it is marked `requires_db`, must SKIP cleanly offline, and its red/green is 4.6's run, N4). Read build2 `## ESCALATE` 2, 4, 8 and its §4 row 3 before briefing.
(row 2) PLAN TEXT: `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` lines ≈184/189 name `cobalt.radar.evaluate`; the shipped entrypoint is `cobalt.radar.evaluate_cli.replay_formations` + `cobalt.radar.evaluate.EVALUATOR_VERSION`. The plan gains a dated correction line `[corrected 2026-09-19 — cto-2026-09-18.md §21 row 2: the shipped entrypoint is …]` directly under the old wording, which STAYS (a plan is a record; no silent rewrite). Docs only; you may write this one yourself.
(row 4) K8 `counts_agree`: DESK RULING (architect): your predecessor's reading stands — a `side: user` smoke query may name a `system.` relation exactly where a migration GRANTs it to `cobalt_user` (K9 → `system.movers_daily`, granted by 0008); the test says that, by reading the GRANTs from the migration files, not from a hand list. The lost machine assertion comes back as ONE new smoke check kind that compares the numeric results of two named checks (`kind: compare`, `left: K8.1`, `right: K8.2`, `op: eq`), Pydantic-validated (unknown check id, non-numeric result, self-reference → loud, L1/L10), with `counts_agree` expressed through it and the hand comparison removed from `expect_text`. Bound: the smoke framework + `configs/cobalt/smoke/s2.yaml` + tests; if it cannot be done without touching how OTHER smoke files are loaded or run → STOP the item, ESCALATE the exact seam.
(row 8) K9 SHORT SIDE: build2 §4 row 3's proposal as written — the replay `job.result` records per-side exported row counts; K9 compares `count = min(top_n, exported)` per side. This is bookkeeping of what Finviz returned, not scoring; if the builder finds it changes WHICH movers are scored, ranked or carded → STOP the item, ESCALATE (L52), build nothing of it.
You verify each item (tests-first evidence, suite 0 failed, `git diff --stat` inside the bound). Commits: `docs(plan): s2-p4 — the shipped replay entrypoint, corrected in place` · `feat(smoke): compare check kind; K8 counts_agree is a machine assertion again` · `fix(s2-p4): K9 compares against the rows the export really had (short side)`. An item stopped = its ESCALATE row + `rows 2 4 6 8` in the stop line says `ESCALATED` for it; the run continues. Last line `CONTINUE: step 4`.

## 4. DB verification — `cobalt_dev` ONLY. You hold the dev-DB lane for this step; no other session uses it today until your stop line. Never `--allow-prod`, never `COBALT_ENV=production` here.
WHAT `cobalt_dev` PROBABLY HOLDS (from the reports, NOT verified by the desk): `s2-p4-followup-2026-09-18.md` left 0008/0009 APPLIED there with 0006/0007 absent; the 09-18 stack gates then applied, rolled back and re-applied 0006/0007 from another worktree. So expect 0001–0009 present, 0008/0009 created by a 09-17 copy of the files (since then ONE comment-only change, `402bfc0`). Whatever you find is recorded verbatim before you change it.
4.1 `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` (by name; never `cat`, `grep`, Read or print it). 4.2 STATE: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (main's harness: applies NOTHING) — output verbatim, its duration in seconds. 4.3 If 0008/0009 are already applied: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` FIRST (verbatim; it must reverse 0009 then 0008 and nothing else), so that the forward run below is the honest one. If 0006 or 0007 is ABSENT on `cobalt_dev` → do not invent an order. The harness keeps NO ledger of applied migrations (review round 1, R5b): a forward run re-runs the whole registered list 0001–0009 in order, every file idempotent, and `--rollback --down-to 0007` reverses the registered files numbered above 0007 (0009, then 0008) whatever was applied. So in that state 4.4 is a FULL forward over 0006–0009 and its duration is not P4's alone — say so; the number the deploy prompt budgets from is then 4.7's second forward run (0008 + 0009 on a database that already holds 0007). 4.4 FORWARD: `COBALT_ENV=dev uv run cobalt db migrate` → 0008 then 0009 applied ON TOP OF 0007; every pre-existing table `OK` unchanged; the three new relations (`system.movers_daily`, `"user".picks`, `"user".missed` — names as the migration files say) created; RECORD THE PROOF'S DURATION (BEFORE + AFTER seconds, and `system.bars` row count on dev) — production's was 104 s for 8.83 M rows and the deploy prompt budgets from your number. A `CHANGED` on a table 0008/0009 do not touch, a serialization failure, or a refusal → verbatim, `FAILED: step 4.4 — <line>` after 4.8's cleanup. 4.5 DB SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote). The four reds of 09-17 ESCALATE 4 / followup ESCALATE 2 must be GONE now that 0006/0007 sit under P4 — any that remains: name the test, quote the assertion, ESCALATE, continue. A red that reads a live `"user".trader_settings` VALUE on a production-mirroring dev DB is the known 09-18 class (tests prove invariants, never assert live values): name it, do not "fix" the data. 4.6 ROW 6 — K3 ON REAL POSTGRES: run K3's SQL through the smoke framework's own test path against `cobalt_dev` (a test that loads `configs/cobalt/smoke/s2.yaml`, executes K3's statement, and asserts it PARSES and returns the declared shape — WRITTEN in step 3 by the TR builder, test only, its own commit `test(smoke): K3's SQL runs on Postgres`; here YOU run it with `COBALT_ENV=dev uv run pytest -q <that file>` — no builder is launched inside step 4); then report, for the desk's decision and building NOTHING more: did it execute · what it returned on dev · whether a HOLD row with a pre-deploy NULL `last_scan_id` makes it read red (build2 ESCALATE 6's scenario, reproduced with a constructed row inside the test's own transaction, rolled back). 4.7 ROLLBACK ROUND TRIP: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` → reverses 0009 then 0008, drops only its own three relations, everything else `OK` → `COBALT_ENV=dev uv run cobalt db migrate` → re-applied; compare 4.4's forward output with this second forward run LINE BY LINE (side by side in the report): the lines naming schema objects — every table's placement/`OK` line and the three new relations — must be EQUAL; a line that differs only in a duration, or in a row count of a table the DB suite of 4.5 writes, is listed and explained, not a mismatch (R18); all three outputs verbatim. A MISMATCH = ESCALATE row + `rollback round-trip ESCALATED` in the stop line; the run continues. A DENIAL of any step-4 command is NOT an escalate — it is `FAILED: step 4.<n> — <command> — <reason>` like every mid-run denial, written AFTER 4.8's cleanup (R12). If the rollback ran and the forward after it FAILED: run the forward ONCE more, record both outputs, and state in the stop line exactly what `cobalt_dev` holds (`cobalt_dev: 0001–0009` or `cobalt_dev: 0001–0007, forward failed`) — the next run must be able to start from your line. Normal ending: `cobalt_dev` is LEFT with 0001–0009 applied. EVERY terminal line written after step 4 has begun — `FAILED` included — ENDS with ` | cobalt_dev: <what it holds | unknown>` (review round 2, N3). The stop line's `db <passed>/<failed>` reports what actually ran, reds included (4.5 lets a named red ESCALATE and continue — then the line says so, e.g. `db 1790/2`); a suite that did not run is `db NOT RUN`, never a number. 4.8 ALWAYS, even on a failure above: `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` shows it gone (quote) → `git status --porcelain` shows no `.env`. 4.9 Without the `.env`: `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest -q tests/cobalt -k "requires_vault or vault" -m ""` (read-only real vault) — quote the line. Last line `CONTINUE: step 5`.

## 5. Close
`uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote) · the leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures` passes (quote) · `uv run cobalt jobs restarts main..HEAD` → the table VERBATIM (expected: `com.cobalt.aset`, `com.cobalt.radar`, the new one-shot `com.cobalt.replay`'s bootstrap; UNCLASSIFIED → ESCALATE) · `git log --oneline main..HEAD` (31 + this run's commits, listed) · `git -C /Users/cobalt/cobalt log --oneline -1` (the main you sit on; if main moved by docs-only desk commits since step 1, say so — that is still READY, the deploy hub rebases again) · worktree clean (only `?? scratch/`), report committed (`docs(report): s2-p4 verify — <one line>`). `## ESCALATE` carries, besides what you found: build2's 7 (FY-3 + step 2.4's `inputs_sha256` note, for the deploy prompt), 9 (receipt reference → tribunal), 5, 10, 11, 12 as CARRIED, one line each, untouched. `MEMORY:` / `RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, exactly this shape:
`READY <tip> | rebased onto <main sha> | offline <n>/0 | db <passed>/<failed> | cobalt_dev: <what it holds> | rollback round-trip <ok/ESCALATED> | Asset Type <built/ESCALATED> | rows 2 4 6 8 <built|ESCALATED each> | RESTARTS: <line> | OWED: three-house check, receipt-reference tribunal, deploy prompt | ESCALATE: <n>`
Then stop. The desk verifies the artifact (L35), stops this session, and launches the three-house check of the build.
