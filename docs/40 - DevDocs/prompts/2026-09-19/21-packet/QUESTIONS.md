You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — everything you need is already here as a file. `spec-01-plan-2026-09-15.md` is the Astra-reviewed S2-P4 plan (design intent). `spec-06-verify-2026-09-19.md` is `13-p4-verify.md`, today's verify prompt — rebase onto main, the `Asset Type` evidence-first rule, triage rows 2/4/6/8, DB verification — the prompt that produced the current tip. Three intermediate build prompts (the 2026-09-17 chunk-build and movers-fetch shapes, the 2026-09-18 followup/E2 fixes) are NOT staged — `facts.md`'s "Specs NOT in the packet" table gives their path and the exact `git show` command the hub uses if a finding needs one; treat any claim resting only on one of those three as `UNVERIFIABLE FROM PACKET` unless `build-report-1`/`build-report-2` or the code itself already settles it. `code-01` through `code-13` together ARE the CODE portion of the non-docs diff `main...sprint-2/p4 -- src tests configs ops` (678,008 bytes, none over 100 KB) — ALL `src/`, `configs/`, `ops/`, migrations, and test CODE (every `tests/**/*.py` file, including the fixture-cutter script `code-13`). SIX fixture DATA files (real-shape JSON/CSV recordings of a trading day or a Finviz export — no logic, nothing to review) are deliberately NOT in this packet; `facts.md`'s "EXCLUDED FROM THE PACKET" table names each one, its byte count, what it is, and which kept test file reads it. If a finding genuinely needs to see inside one of those six files' row content, say `UNVERIFIABLE FROM PACKET — <file> — <what you'd need to see>`; the hub (not you) can pull it with its own `git show` access. `facts.md` is the desk's own pre-computed measurements (commits, tip, merge-base, diff-stat, file:line per claimed row, migrations, RESTARTS, the excluded-files table) — read it, but VERIFY its claims against the `code-*.diff` files yourself where you can; it is the desk's own account, not settled fact. `build-report-1-build2-2026-09-18.md` and `build-report-2-verify-2026-09-19.md` are the builder/hub's own accounts of the work (the second is the final one, ending in the `READY` stop line) — treat every claim in them as a CLAIM to verify against the diffs, never as settled fact.

This is the FIRST code-level check this branch has ever had (facts.md's last section explains why: only the plan TEXT was reviewed on 2026-09-16, by one house, before any code existed). It has not merged, does not run against production today, and is queued for a deploy that will run two new migrations (0008, 0009) against a live production Postgres 16 database. Its `requires_db` tests (K3's SQL, the migration round trip) have run once against `cobalt_dev` only, never against production.

For every finding give SEVERITY (BLOCKER = the code does not do what its own spec/prompt required, breaks an existing invariant, or contradicts a LAW (L1 fail-loud, L3 one-path, L10 config-validated-on-load, L32 no user-screen data in a committed file, L45 real-shape fixtures never hand-edited, L52 nothing here may touch scoring/ranking/what reaches a card, L57 every derived number replayable) / MAJOR / MINOR), FILE + LINE (from the `code-*.diff` files), the EVIDENCE you read (quote it), and the FAILING SCENARIO; say "none found" rather than inventing.

(Q1) ROW 2 (plan correction): does `spec-01-plan-2026-09-15.md`'s original wording actually claim `cobalt.radar.evaluate` as the entrypoint, and does the shipped code (`code-02-replay-core.diff` / `code-01-radar.diff`) really expose `cobalt.radar.evaluate_cli.replay_formations` + `cobalt.radar.evaluate.EVALUATOR_VERSION` as `facts.md` claims — quote the actual function/constant definitions.

(Q2) ROW 4 (`kind: compare`, K8.3): read `CompareOp`/`CompareCheck` in `code-04-smoke.diff` and the grader in `checks.py` — is an unknown check id, a self-reference, a non-numeric operand, or any `op` other than `eq` actually rejected LOUDLY at load (L1/L10), or does one of those cases silently pass? Does `configs/cobalt/smoke/s2.yaml`'s K8.3 (`code-07-configs-ops.diff`) truly replace a hand-written text comparison, with no comparison logic left duplicated elsewhere?

(Q3) ROW 6 (K3 on Postgres): read `tests/cobalt/test_smoke_k3_sql.py` in `code-12-tests-cards-p4.diff` (or wherever it lands) — does `test_k3_hold_row_reads_red_documented_ambiguity` actually construct a HOLD-shaped row (pre-deploy `first_seen_at`, `rank_metric` NULL, a post-cutoff `last_scan_id`) inside its own transaction and roll it back, or does it leave a footprint? Does it prove the FAIL is a genuine false positive (a real RETAIN case), not a bug the test papers over?

(Q4) ROW 8 (K9 short side): in `code-03-replay-movers-runner-cards.diff`, confirm `expected = min(top_n, exported)` is computed from the row counts the export REALLY returned (not assumed), and that this is bookkeeping only — walk `runner.py`'s call site and confirm no changed line touches `benchmark_misses`, `archive_movers`, `MoversStore`, `MoversCollector.exports`, `retained_exports`, or anything that reaches a scored/ranked/carded value (L52). `facts.md` and the build report both claim this was checked line-by-line; verify it yourself against the diff.

(Q5) MIGRATIONS 0008/0009 (`code-06-migrations.diff`): are both `.sql` files strictly additive (`CREATE TABLE IF NOT EXISTS`, `ADD COLUMN IF NOT EXISTS`) with no `ALTER`/index/trigger touching any PRE-EXISTING table other than the two declared `ADD COLUMN`s on `system.radar_membership`? Do the `.rollback.sql` files reverse exactly what the forward files create/add, and nothing else? Is either migration missing an idempotency guard (a second run would fail or duplicate data)?

(Q6) `ASSET TYPE` NOT BUILT: confirm from `code-01-radar.diff`, `code-03-replay-movers-runner-cards.diff`, and `code-07-configs-ops.diff` (the `configs/cobalt/radar.yaml` slice, if present — `facts.md` says the three-dot diff of that file is EMPTY) that `not_equity`, `REQUIRED_HEADERS`, and the `unreported` branch are genuinely untouched by this branch. Is there any place where the ESCALATED-but-not-built rule half-leaked in (a partial code path, a config key added but unused, a test asserting the new rule)?

(Q7) THE ONE THING THAT WAS BUILT TOWARD `Asset Type`: `code-03`'s column-source refactor (`cobalt.radar.config.screener_columns()`/`screener_columns_param()`) — does `grep`-level evidence in the diff support `facts.md`'s claim of "zero remaining `range(151)` literals" in the files this diff touches? Note `facts.md`'s ESCALATE 4: `radar/throttle.py` and `radar/propose.py` are said to STILL hard-code `"v": 152` (a different field, not the column range) — is that consistent with what you read in `code-01-radar.diff`?

(Q8) TEST QUALITY: spot-check at least three tests across `code-08` through `code-12` against the build reports' per-chunk claims — read the actual test body, not just its name, and say whether it proves the claimed property (a real failing-input case) or merely exercises the code path without a meaningful assertion.

(Q9) FIXTURE COHERENCE (ESCALATE 3 of the build report): the movers-gainers/losers and membership-day fixtures themselves are EXCLUDED from this packet (`facts.md`'s table — they are recorded data, not code) — this question is answered from the TEST CODE alone. In `code-10-tests-replay-b.diff` and `code-12-tests-cards-p4.diff`, do the tests' own comments/assertions (e.g. the `QNME`/`IMCC` ticker handling quoted in `facts.md`'s L32 note) support the claim that the movers fixture (09-19) and the membership fixture (09-14) now represent DIFFERENT sessions? Is there a test that silently ASSUMES same-session coherence between the two fixtures and would give a false pass? If settling this needs the fixtures' actual row content rather than what the test code asserts about them, say `UNVERIFIABLE FROM PACKET`.

(Q10) CROSS-CUTTING: does anything in `code-01` through `code-15` (the WHOLE non-fixture-data diff) touch scoring, ranking, or a value that reaches a card, beyond what rows 2/4/6/8 declare (L52)? Any secret, credential, path, or connection string visible anywhere, including in the two large fixture files? Is `configs/cobalt/smoke/s2.yaml` really the only NEW config file, and is it under an approved path (`configs/cobalt/`)?

(Q11) RESTARTS: does `facts.md`'s RESTARTS section hold up — is `com.cobalt.replay` genuinely a brand-new one-shot job (no prior job of that name to "restart"), and do `com.cobalt.aset`/`com.cobalt.radar` actually consume the changed `src/cobalt/radar/*`, `src/cobalt/replay/*`, and `configs/cobalt/taxonomy/tunables.yaml` files as claimed?

(Q12) Does `build-report-2-verify-2026-09-19.md`'s own narrative (the stop line's numbers, the 16 numbered ESCALATE items, the row-by-row BUILT/ESCALATED table) hold up against the `code-*.diff` files and `facts.md` — name any claim you cannot confirm, marking it UNVERIFIABLE FROM READS rather than assuming it is true.

End with exactly one line: `VERDICT: BUILD CLEAN / FIX FIRST <finding numbers> / BUILD UNSAFE`.
