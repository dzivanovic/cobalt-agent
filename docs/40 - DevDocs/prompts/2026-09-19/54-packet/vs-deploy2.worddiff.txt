diff --git a/docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md b/docs/40 - DevDocs/prompts/2026-09-19/54-packet/53-deploy-d3.md
index c8cf336..bd59380 100644
--- a/docs/40 - DevDocs/prompts/2026-09-19/38-deploy-p4.md	
+++ b/docs/40 - DevDocs/prompts/2026-09-19/54-packet/53-deploy-d3.md	
@@ -1,110 +1,108 @@
STATUS: DEPLOY [-2-]{+3+} of the [-weekend (R3/R5 of 09-18: three-]{+weekend, and the SECOND+} production [-deploys by Monday). Deploy 1 ran this morning-]{+deploy of tonight — `cto-2026-09-19.md` §4 **R32**, in which Dejan OVERRULED **L43**'s one-production-deploy-per-evening for tonight only, on his own question ("why are we not redeploying Archiver right after merge and right after deploy+} and {+continuing with build and do another deploy tonight?"), with one condition: **the lane+} is [-on record: `02-deploy-stack-3.md` → `reports/deploy-2026-09-19.md`,-]{+green before 21:00 ET**, else it goes to Sunday. Deploy 1 (`02-deploy-stack-3.md`, 08:25 ET,+} `STACK DEPLOY DONE [-2893a7f`, outage-]{+2893a7f`) and deploy+} 2 [-min 40 s, 5 ESCALATE, §6 never entered.-]{+(`38-deploy-p4.md`, 15:17 ET, `P4 DEPLOY DONE 5b58b7b`, outage 02:33) are both on record and LIVE.+} THIS file lands[-`sprint-2/p4` ONLY (S2-P4 · F3/F12/F13).-] `ops/2026-09-19` [-and-]{+AND+} `archiver/append-0919` [-are DEPLOY 3 (Sunday) and are NOT merged here — they enter this prompt only as the L68 gate's other legs.-]{+together.+} LAUNCHABLE ONLY with Dejan's approval row in `docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4` [-that names-]{+naming+} THIS file, committed on [-main, and only after `37-p4-db-run.md` has ended `P4 DB …` and `36-review-p4-folds.md` has ended with no shipping blocker (both verified in §2, both `FAILED: gate` if absent).-]{+main.+}

MODEL: Opus 5 (`claude-opus-5`) — Opus floor: production migrations [-0008/0009-]{+0010/0011+} ship and this session runs a production write path (L29; [-02-]{+deploys 1 and 2+} ran the same seat on the same floor) · SEAT: deploy hub [-`deploy-p4-0919`,-]{+`deploy-d3-0919`,+} launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - [-DevDocs/prompts/2026-09-19/38-deploy-p4.md'-]{+DevDocs/prompts/2026-09-19/53-deploy-d3.md'+} and follow it exactly." --model claude-opus-5 --remote-control [-deploy-p4-0919-]{+deploy-d3-0919+} --allowedTools "Bash(git -C /Users/cobalt/cobalt add *)" "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git -C /Users/cobalt/cobalt reset --soft HEAD~1)" "Bash(git -C /Users/cobalt/cobalt tag [-scratch-allow-probe-p4-0919)"-]{+scratch-allow-probe-d3-0919)"+} "Bash(git -C /Users/cobalt/cobalt tag -d [-scratch-allow-probe-p4-0919)"-]{+scratch-allow-probe-d3-0919)"+} "Bash(git -C /Users/cobalt/cobalt tag [-pre-p4-0919)"-]{+pre-d3-0919)"+} "Bash(git -C /Users/cobalt/cobalt tag [-deploy-2026-09-19b)"-]{+deploy-2026-09-19c)"+} "Bash(git -C /Users/cobalt/cobalt merge --ff-only [-sprint-2/p4)"-]{+stack/deploy3-0919)"+} "Bash(git -C /Users/cobalt/cobalt revert --no-edit *)" "Bash(git -C /Users/cobalt/cobalt revert --abort)" "Bash(git -C[-/Users/cobalt/cobalt-wt/s2-p4 rebase main)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p4 rebase --abort)" "Bash(git -C-] /Users/cobalt/cobalt worktree add -b [-stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+stack/deploy3-0919 /Users/cobalt/cobalt-wt/stack-deploy3-0919+} main)" "Bash(git -C [-/Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919+} merge --no-edit *)" "Bash(git -C [-/Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919+} merge --abort)" "Bash(git -C * status*)" "Bash(git -C * log*)" "Bash(git -C * diff*)" "Bash(git -C * rev-parse*)" "Bash(git -C * rev-list*)" "Bash(git -C * merge-base*)" "Bash(git -C * show*)" "Bash(cd *)" "Bash(uv run pytest *)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009)" "Bash(cp /Users/cobalt/cobalt/.env [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env)"-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/.env)"+} "Bash(rm [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env)"-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/.env)"+} "Bash(COBALT_ENV=production uv run cobalt validate*)" "Bash(COBALT_ENV=production uv run cobalt jobs register)" "Bash(COBALT_ENV=production uv run cobalt jobs restarts *)" "Bash(COBALT_ENV=production uv run cobalt backup run*)" "Bash(COBALT_ENV=production uv run cobalt backup status*)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod --proof-only)" "Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)" "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run cobalt heartbeat show*)" "Bash(launchctl bootout gui/501/com.cobalt.aset)" "Bash(launchctl bootout gui/501/com.cobalt.radar)" "Bash(launchctl[-bootout gui/501/com.cobalt.replay)" "Bash(launchctl-] bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist)" "Bash(launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist)" "Bash(launchctl[-bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.replay.plist)" "Bash(launchctl-] kickstart -k gui/501/com.cobalt.aset)" "Bash(launchctl kickstart -k gui/501/com.cobalt.radar)" "Bash(launchctl print gui/501/*)" "Bash(curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/*)" "Bash(shasum -a 256 *)" "Bash(grep *)" "Bash(tail *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes from the report's last `CONTINUE:` line · auto mode [-on;-]{+NOT set (no `--permission-mode` flag, exactly as `02` and `38` ran);+} allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it, NO production [-`pytest` (see WHY 2), NO-]{+`pytest`, **NO+} `settings load` in any [-spelling (see WHY 6), NO-]{+spelling**, **NO+} PRODUCTION [-`--rollback`/`--down-to` — **and round 1 of the house check (finding 1, a BLOCKER) is why this sentence is now backed by the allowlist instead of by hope: `COBALT_ENV` does NOT decide the database when `--allow-prod` is present.** `src/cobalt/db_migrations/cli.py:542` (main `:537`) reads `dbname = db.PROD_DB_NAME if args.allow_prod else env.resolve_db_name()`, and `src/cobalt/db.py:146`'s `_prod_gate` only refuses when `allow_prod` is FALSE-]{+`--rollback`/`--down-to`**+} — [-so a rule like `Bash(COBALT_ENV=dev uv run cobalt db migrate*)` would have admitted `COBALT_ENV=dev uv run cobalt db migrate --allow-prod --rollback --down-to 0007`, i.e. a production schema rollback wearing a dev prefix. That wildcard is GONE: the three dev commands §1.4 types are allowlisted as exact strings with no `*`, and the only `--allow-prod` rules are the bare one-]{+never instructed here+} and [-`--proof-only`. **SAID HONESTLY (desk probe,-]{+never typed, in any branch (the desk's+} 2026-09-19 [-14:0x ET,-]{+probe,+} `cto-2026-09-19.md` [-§61):**-]{+§61:+} under [-`--permission-mode auto` the-]{+auto mode an+} allowlist is a [-PRE-APPROVAL list, not a whitelist — a control session whose only rule was `Bash(touch /private/tmp/cobalt-probe-c)` still ran `touch /private/tmp/cobalt-probe-d`, a command no rule matched. So no rule-]{+pre-approval+} list [-can make-]{+rather than+} a [-command impossible to type;-]{+whitelist, so+} what keeps a production rollback out of[-this run is that this prompt never instructs one, forbids it in four places, and L62 makes any mid-run denial-] a [-FAILED run. Type only what a step tells you to type. The ONE rollback this session may type-]{+run+} is the [-DEV-lane `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` of §1.4, on `cobalt_dev`, which-]{+instruction, not+} the [-gate is required to run-]{+rule list; this session carries no auto mode and no such instruction)+} · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely and gets an answer; a dialog gets none).

#[-P4-] DEPLOY 2026-09-19 [-(Saturday, daytime, weekend push R3/R5 — no scanning session today, no trading day)-]{+(3)+} — {+`ops/2026-09-19` + `archiver/append-0919`,+} ONE ff-merge of [-`sprint-2/p4`: S2-P4 (F3 movers/value · F12 picks/missed · F13 benchmark),-]{+the gated stack+}
{+WHAT SHIPS: (1) **ops 0919** — the `cobalt day-open` C2 trading-day guard, `_band()` min ≤ max validation, a 30 s `lock_timeout` on the read-write migrate transaction, test hygiene A1/A2, and the migrate output's `code: <sha> (clean|DIRTY)` line. (2) **the append-only archiver** —+} production migrations [-**0008** (`system.radar_membership.rank_metric/rank_value` + `system.movers_daily`)-]{+**0010** (`system.archive_progress`)+} and [-**0009** (`"user".picks`, `"user".missed`),-]{+**0011** (`system.archive_incidents`), both carrying an EXPLICIT `REVOKE` so `cobalt_user` cannot read them (Dejan's R25 "B");+} the [-R16 fund rule as-]{+archiver's own per-(ticker, interval) progress, a persistent incidents table,+} a [-`configs/cobalt/radar.yaml` shape change,-]{+read-only SHADOW COMPARE,+} and {+**`upsert` mode — today's behaviour byte for byte** (the switch to `append` is HIS later ruling on+} the [-NEW one-shot job `com.cobalt.replay` (`ops/com.cobalt.replay.plist`, Mon–Fri 21:10 ET, `RunAtLoad false`).-]{+shadow numbers, R35: **ONE shadow night**, never automatic). Plus R34 "B": the archiver's backfill is bounded out of **04:00–09:30 ET** and `radar.finviz_max_rpm` **stays 50**.+}
Shape A (L66): both residents DOWN before the merge, UP [-after, plus a first bootstrap of `com.cobalt.replay`. STEP 1-]{+after. NO new scheduled job+} is [-the L68 integrated gate-]{+installed+} — [-offline AND with-DB, on a branch stacking every unmerged branch, GREEN BEFORE-]{+`com.cobalt.archiver` already exists (Mon–Fri 20:30) and `com.cobalt.replay` was armed by deploy 2. NO settings write of any kind;+} the [-merge, never after it in `~/cobalt`.-]{+cards stay LIVE and are only READ.+} Expected outage ≈3–4 minutes (≈95 s of it is the migration's BEFORE+AFTER read of `system.bars`,[-deploy 1's-] measured [-number). NO settings write of any kind. DO-]{+twice today). **DO+} NOT STOP this session until its report ends [-`P4-]{+`D3+} DEPLOY DONE …` or `FAILED [-…`.-]{+…`.**+}

WHY THIS [-SHAPE (each-]{+SHAPE, each+} line[-is-] a failure already paid [-for, or a law that already cost one):-]{+for:+} (1) [-09-17 and 09-18:-]{+**ONE ff-merge of the GATE BRANCH, not+} two {+sequential merges.** Both+} branches [-green alone, red together, twice — hence **L68**,-]{+are rebased onto the same `main`, so they are siblings: ff-merging one makes the other non-ff. §1 builds `stack/deploy3-0919` from `main`, merges both into it,+} and [-hence §1's gate runs on-]{+proves+} the [-STACKED-]{+SUITES ON THAT EXACT TREE; §4.3 then fast-forwards `main` to it. The+} tree [-BEFORE-]{+that lands is byte-identical to+} the [-merge, never after it-]{+tree the gate proved, which is stronger than merging two branches+} in [-`~/cobalt`.-]{+sequence where only their combination was ever tested (L68). Deploy 2's own gate branch was a throwaway because only one branch was landing; here it is the deliverable, and that difference is deliberate.+} (2) 09-18 11:34: `uv run pytest` in `~/cobalt` is NOT offline [-— `~/cobalt/.env`-]{+(`~/cobalt/.env`+} points it at [-`cobalt_dev` —-]{+`cobalt_dev`),+} so[-this prompt runs-] pytest {+runs+} ONLY inside a [-worktree, never with `/Users/cobalt/cobalt` as cwd, and after the merge it runs production `validate` + smoke only.-]{+worktree.+} (3) 09-17 19:18: launchd respawned the radar into merged code → residents are `bootout`ed BEFORE the merge (L66). (4) 09-18: a [-rebase-then-report-commit order broke-]{+commit on main between a rebase and an+} `--ff-only` [-with-]{+broke+} the [-residents down-]{+merge+} →[-the rebase is the LAST thing before `bootout`, and-] NOTHING commits on main between [-the rebase-]{+§1+} and [-the merge-]{+§4.3+} (you hold your report writes; the desk holds its commits). (5) [-THE HARD PAIR IS AUTOMATIC HERE: P4's config-shape change (`not_equity: {header, values}` → `{asset_type_header, industry_header, industry_values}`, `extra="forbid"` on the old shape) lives-]{+Migrations 0010/0011 are ADDITIVE (two new tables) and nothing+} in[-`configs/cobalt/radar.yaml`, a REPO file inside-] the [-merge —-]{+pre-archiver code reads them,+} so a[-`git revert` of the merge range takes-] code [-and config back together-]{+revert leaves the schema+} in [-one action, and there is no settings row to travel with it (L42's "one action"-]{+place; **the schema+} is [-satisfied by the revert itself).-]{+NEVER rolled back unattended** (§8 (3)).+} (6) **CARDS ARE LIVE** since 12:30:01 ET[-today-] (`reports/cards-golive-2026-09-19.md`, R20): [-`radar.cards_enabled = true` in `"user".trader_settings`. This-]{+this+} deploy [-**loads NO-]{+loads no+} settings file,[-reads no card yaml,-] writes no `trader_settings` row, and its rollback [-NEVER-]{+never+} turns the cards[-dark** — deploy 1's six `settings load` strings are DROPPED from the launch line above for exactly that reason, and the-] dark [-file is not reachable from this session. Cards are-]{+—+} proven STILL ENABLED at 6.4 as a smoke check, never touched as [-a deploy-]{+an+} action.[-(7) Migrations 0008/0009 are ADDITIVE (two nullable columns + three new tables) and P4's code is inert for the P2 code already in production, so a code revert leaves the schema in place; the schema is NEVER rolled back unattended (§8 (3)).-]

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval.[-His words, with times, are in the report files, never in this one.-]
- THE ROW: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-19.md` `## §4 Rulings`,[-rows **R30** (his "Approve", 2026-09-19 14:23 ET — the 23 rule strings, the window, the tag) **and R31** (his "A", 14:4x ET — the narrowed L68 scope, and-] the row that [-carries THIS FILE'S FINAL sha256 for run 2), which together must name-]{+names+} THIS file [-(`38-deploy-p4.md`) and approve-]{+(`53-deploy-d3.md`), approves+} THIS launch [-line-]{+line,+} and [-the-]{+carries this+} file's [-sha256 as recorded there.-]{+sha256.+} Verify: `shasum -a 256 "/Users/cobalt/cobalt/docs/40 - [-DevDocs/prompts/2026-09-19/38-deploy-p4.md"`-]{+DevDocs/prompts/2026-09-19/53-deploy-d3.md"`+} equals the hash in [-**R31** (R30's hash is run 1's and is superseded there in writing),-]{+that row,+} and `git -C /Users/cobalt/cobalt log --oneline -10 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` shows the commit that carries it. **No committed row naming THIS file,[-a different hash, a rule string this prompt types that R30 does not carry,-] or a [-missing R30 or R31-]{+different hash+} → `FAILED: authorization mismatch — <what>`, commit the report, stop before PREFLIGHT.** R7 [-of today-]{+and R30+} approved[-a-] DIFFERENT [-file (`02-deploy-stack-3.md`)-]{+files+} and [-is NOT an approval-]{+are not approvals+} of this one.
- STANDING [-ROWS that this deploy also rests on,-]{+ROWS,+} each to be found [-committed (`git log --oneline -10 -- …`): `cto-2026-09-18.md` §4 **R3/R5** (the weekend push through 09-20), **R6** (the accepted baseline-RED class; aset bootstraps from the REPO plist), **R18–R21** (L67: a build checked by ≥3 tribunal members, a deployment by ≥1 other house).-]{+committed:+} `cto-2026-09-19.md` §4 [-**R16**-]{+**R32** (L43 overruled tonight, green-before-21:00), **R25**+} (the [-fund rule "C" — a radar behaviour change: non-blank `Asset Type` OR `Industry = Exchange Traded Fund`),-]{+REVOKE), **R26** (the dropped twin), **R34** (ceiling 50, backfill bounded), **R35** (ONE shadow night),+} **R19** [-(L68/L69/L70 folded;-]{+(push is+} the [-desk pushes only-]{+desk's+} on his word {+only+} — push is not in your list at [-all), **R23** (the `pool.py:189` None-filter STAYS, disclosed — as corrected in §51: ONE filter, element 1, `pool.py:189`; `:198` was already on main).-]{+all); `cto-2026-09-18.md` §4 **R18–R21** (L67).+}
- Laws that bind you: L1, {+L3,+} L4, {+L9,+} L28, L29, [-L32 (P4's fixtures-]{+L32, L35, L42, L43 **as overruled for tonight by R32,+} and [-any Finviz value are user data-]{+only that clause**+} — [-never quoted, never `git add`ed beyond-]{+its restart-window clause still holds and is met because Saturday is not a trading day and+} the [-paths §3.1 names), L35, L42, L43,-]{+radar reads `idle:overnight` at every hour —+} L46, L48, [-L51 (1–3 pre-approved: `COBALT_ENV=production`, committing a machine-written file, extracting a stage-2-retired blob),-]{+L51, L53 (**you change no ceiling and no cadence**),+} L54, L55, L57, L61, L62, L63, L66, [-**L67**, **L68**,-]{+L67, L68,+} L70.
YOU CAN ALWAYS STOP. Doubt → `FAILED: <step> — <concern>` as the last line, commit the report, stop. TWO ASYMMETRIES: (a) from the first `bootout` on, production's residents are DOWN — "stop" then means finishing §8's safe-state rule first [-(NN#16) — with ONE named exception, §4.2's terminal branch, which repairs the resident whose bootout took and stops WITHOUT entering §8 (entering it there would loop on the bootout that just failed); that exception is written out at the step and nowhere else;-]{+(NN#16);+} (b) while `.env` sits in the gate worktree, "stop" means removing it and proving it gone FIRST (L4), then writing the stop line.

INDEX CARD (read in this order, before PREFLIGHT): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory [-`/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md`-]{+`INDEX.md`+} → `## NOW` in `areas/cobalt.md` → `LAWS.md` **IN FULL**[-(you run a production write path; L68 is new today) → **this prompt's facts packet** `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/38-packet/facts.md` (the desk's own pre-computed measurements — a CLAIM to check, never settled fact, L35)-] → `/Users/cobalt/cobalt/docs/40 - [-DevDocs/reports/deploy-2026-09-19.md`-]{+DevDocs/reports/deploy-p4-2026-09-19.md`+} **IN FULL** [-(deploy 1, this morning:-]{+(tonight's deploy 2: its three runs,+} what [-worked — bootout/bootstrap, the tag as the durable rollback point, the 94.1 s migration proof,-]{+its L68 gate caught,+} the [-§6 safe-state rule never needed —-]{+false stop its (d2) fold fixed,+} and all [-5-]{+9+} ESCALATE) → [-`/Users/cobalt/cobalt-wt/s2-p4/docs/40-]{+`/Users/cobalt/cobalt-wt/archiver-append/docs/40+} - [-DevDocs/reports/s2-p4-verify-2026-09-19.md`: §0, `## ESCALATE` item **5 (a)–(e)** (the five deploy-prompt consequences), item **2** (K3/HOLD), the `## FUND RULE (R16)` section's ESCALATE **21 (a)–(d)**, `## ROUND 2`'s-]{+DevDocs/reports/rebase-d3-2026-09-19.md`+} §0 + [-`## ROUND-]{+the CHUNK+} 2 [-ESCALATE`-]{+and CHUNK 3 sections+} + its last line[-`P4 ROUND2 9045260 …`-] → [-`/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/`-]{+`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md` §0 + `## Verdict table` (the houses' read of this exact code: 0 blockers, 4 MINOR — **G-A1** window built from `hour:minute`, **G-A2** a stale pre-existing docstring, **H1**+} the [-P4 DB run's own report section (last line `P4 DB …`, written by `37-p4-db-run.md`) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cards-golive-2026-09-19.md` (last line only: `CARDS LIVE 12:30:01 ET …`)-]{+declared backfill window counts sleeps but not fetch latency, **H2** a docstring/pin contradiction; none is yours to fix)+} → [-`/Users/cobalt/cobalt-wt/s2-p4/ops/com.cobalt.replay.plist`-]{+`/Users/cobalt/cobalt-wt/archiver-append/src/cobalt/db_migrations/0010_archive_progress.sql` and `0011_archive_incidents.sql`+} IN FULL[-(the job you bootstrap) → `/Users/cobalt/cobalt-wt/s2-p4/src/cobalt/db_migrations/0008_radar_value_movers.sql` and `0009_picks_missed.sql`-] (what CREATED is allowed to mean at [-4.5).-]{+4.5) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cards-golive-2026-09-19.md` (last line only).+}
REPORT (L48): `/Users/cobalt/cobalt/docs/40 - [-DevDocs/reports/deploy-p4-2026-09-19.md`-]{+DevDocs/reports/deploy-d3-2026-09-19.md`+} — [-**it ALREADY EXISTS: run 1 (14:26–14:33 ET) stopped at the L68 gate with `FAILED: gate-]{+a NEW file, title `# DEPLOY 2026-09-19 (3)+} — [-conflict merging archiver/append-0919`, nothing deployed, production untouched. You ARE the-]{+ops + archiver`; if it already exists you are a+} rerun: append `# SECOND RUN` under everything there and rewrite [-NOTHING-]{+nothing+} above [-it.** Read run 1's PREFLIGHT and §1.2 sections first — its 16 preflight rows and its `feca2cf` stack tip are evidence you may cite, but every command is RE-RUN, never assumed. Inside it:-]{+it. Inside:+} §0 Headline ≤5 lines → PREFLIGHT → L68 GATE → Preconditions → Deploy table → Smoke → Close → ESCALATE → last line.

[-WILDCARD AUDIT (round-1 finding 5, done after finding 1 showed the class is real — every `*` rule in the launch line, asked one question: does it admit a spelling that reaches PRODUCTION outside the step that needs it?). NARROWED, wildcard removed: `db migrate*` on the dev lane → three exact commands (finding 1: `--allow-prod` beats `COBALT_ENV`) · `tag *` → the four exact tag names this run types · `worktree add *` → the one exact command · `jobs *` → `jobs register` + `jobs restarts *`, because `cobalt jobs run <label> -- <cmd>` exists (`src/cobalt/jobs/cli.py:189`, "Run a command wrapped as a registered job") and would have wrapped ANY command under a `COBALT_ENV=production` prefix. KEPT, with the reason: the seven `git -C * <read>` rules and `heartbeat show*`, `validate*`, `backup status*` are read-only · `db query *` is read-only in code, not merely by convention (`src/cobalt/db_query.py` `guard_select` accepts exactly one `SELECT`/`WITH` and opens `BEGIN READ ONLY`) · `backup run*` writes only a snapshot, which is the point · `revert --no-edit *` and the stack `merge --no-edit *` are git-only and path-scoped · `cd *`, `uv run pytest *`, `COBALT_ENV=dev uv run pytest *` cannot reach `cobalt_brain` (pytest has no `--allow-prod` path and `requires_db` skips without `POSTGRES_*`) · `grep/tail/ls/wc/date/shasum` are reads · `launchctl print gui/501/*` is a read · the `curl` rule is a localhost GET. STILL OPEN, and the desk owns it, not you (round-1 finding 6, UNVERIFIABLE FROM READS): whether an UNSTARRED rule such as `Bash(COBALT_ENV=production uv run cobalt db migrate --allow-prod)` matches by prefix on this host — if it did, it would admit the production rollback by itself. It is byte-identical to the rule Dejan approved for deploy 1 (R7) which ran clean this morning; you never type a longer spelling of it, and the desk settles the matcher question by experiment outside a deploy.-]UNATTENDED RULES: ONE command per Bash call, exactly the listed prefix, no pipe, no redirect, no `; echo`, no `$(…)`, no `&&`. `cd <path>` is its OWN call and is used ONLY to enter a worktree for[-a-] pytest[-run-] and to [-return; every-]{+return. Every+} git command is `git -C <absolute path> …` so cwd never decides a git answer. **CWD [-DISCIPLINE (the one thing that can silently ruin this run):**-]{+DISCIPLINE:**+} the session starts in `/Users/cobalt/cobalt`; the only place pytest may run is [-`/Users/cobalt/cobalt-wt/stack-deploy2-0919` or `/Users/cobalt/cobalt-wt/s2-p4`;-]{+`/Users/cobalt/cobalt-wt/stack-deploy3-0919`;+} before every pytest [-call,-]{+call+} the immediately preceding call is `cd <that worktree>`[-and-] then `ls -la .env` [-(in the stack worktree during-]{+(during+} the with-DB step it must PRINT the file; everywhere else it must say "No such file"); after the last pytest call, `cd /Users/cobalt/cobalt` then `ls -la .env` — in `~/cobalt` that file [-EXISTS,-]{+EXISTS+} and seeing it listed is your proof that cwd is back (never print its contents, never `cat`, never `grep` it). Outputs are read from the tool result; files are written with Write/Edit. MID-RUN DENIAL = THE RUN FAILED (L62): `.env` cleanup if present, then §8 if anything is merged or any resident is [-down — except inside §4.2's terminal branch, which is the one place that repairs and stops without §8 —-]{+down,+} then `FAILED: <step> — <command> — <reason string>`, commit, stop. Never another shape, never another session, never a retry of a denied command in a different spelling.

## 0. PREFLIGHT — nothing here changes production (residents stay UP)
0.1 One row each (rule · command · exit · allowed/DENIED + reason verbatim):
· **P1** `git -C /Users/cobalt/cobalt status --porcelain` AND `git -C /Users/cobalt/cobalt status` (LONG form, quoted in full — no "rebase in progress", no "unmerged paths", branch `main`).
· **P2** `git -C /Users/cobalt/cobalt tag [-scratch-allow-probe-p4-0919`-]{+scratch-allow-probe-d3-0919`+} then `git -C /Users/cobalt/cobalt tag -d [-scratch-allow-probe-p4-0919`.-]{+scratch-allow-probe-d3-0919`.+}
· **P3** `git -C /Users/cobalt/cobalt commit --allow-empty -m "scratch: allowlist probe (reverted next line)"` then `git -C /Users/cobalt/cobalt reset --soft HEAD~1`; `git -C /Users/cobalt/cobalt log --oneline -1` proves HEAD is back.
· **P4** `date` → the WINDOW rule of §2.1 is read here for the record, not enforced yet.
· **P5** `COBALT_ENV=production uv run cobalt validate` → exit 0. Record the `Jobs (F17):` line verbatim (expected [-**15-]{+**16+} registered — 6 resident, [-9-]{+10+} one-shot**: [-the pre-merge state;-]{+deploy 2's state, with+} `com.cobalt.replay` [-is not there yet).-]{+present).+}
· **P6** `COBALT_ENV=production uv run cobalt backup status` and `COBALT_ENV=production uv run cobalt backup run --dry-run`.
· **P7** `COBALT_ENV=production uv run cobalt jobs restarts [-main..sprint-2/p4`-]{+main..archiver/append-0919`+} — record VERBATIM. Expected `RESTARTS: com.cobalt.aset [-com.cobalt.radar` with `ops/com.cobalt.replay.plist A → new one-shot, bootstrap once: com.cobalt.replay` and-]{+com.cobalt.radar`,+} 0 [-UNCLASSIFIED.-]{+UNCLASSIFIED (chunk 3 derived 52 paths, 0 UNCLASSIFIED).+} A non-zero exit or any UNCLASSIFIED path → record it; it is a §2 gate, not a stop here.
· **P8** `COBALT_ENV=production uv run cobalt heartbeat show` —[-deploy 1 found GREEN at 08:16 with only `AMB com.cobalt.herdr unmanaged` (declared interim).-] EXPECT [-GREEN. R6's gate still stands if a RED is there: a-]{+GREEN, 16 jobs (deploy 2 added `com.cobalt.replay`). A+} PRE-EXISTING RED is accepted ONLY if it is the `radar` probe reading `failed_stage bars: poll failures: <n>` AND `tail -n 12 /Users/cobalt/cobalt/logs/radar.err` shows `radar cycle: idle:<session>` lines ≤4 minutes apart with no traceback (Saturday = outside every scanning session, so `idle:overnight` is the correct shape — say so); record it verbatim as the BASELINE. Any other RED → `FAILED PREFLIGHT: production not green`.
· **P9** `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → both running; record both pids and both `path =` lines (aset = `/Users/cobalt/cobalt/ops/com.cobalt.aset.plist`, radar = `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist`; a different path → `FAILED PREFLIGHT: plist path moved`). [-Also `launchctl print gui/501/com.cobalt.replay` → MUST fail "Could not find service" (it does not exist yet; if it IS loaded, someone bootstrapped it already → `FAILED PREFLIGHT: com.cobalt.replay already loaded-]{+Deploy 2 left aset at pid 80628 and radar at 80641+} — [-<output>`).-]{+a DIFFERENT pid is recorded, not a stop (launchd may have respawned), but say so.+}
· **P10** `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` → 200 and `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` → 200.
· **P11** `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT tablename, array_agg(schemaname ORDER BY schemaname) FROM pg_tables WHERE schemaname IN ('public','user','system') GROUP BY tablename HAVING count(*) > 1"` → ZERO [-rows (the harness REFUSES a table name that exists in more than one of those schemas; a duplicate would fail 4.5 with the residents down).-]{+rows.+} Any row → `FAILED PREFLIGHT: duplicate table name — <name> in [-<schemas>` (a human decision, never a retry).-]{+<schemas>`.+}
· **P12**[-(new for P4, same class as P11)-] `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT schemaname, tablename FROM pg_tables WHERE tablename IN [-('movers_daily','picks','missed')"`-]{+('archive_progress','archive_incidents')"`+} → ZERO rows. A row means one of [-0008/0009's three new-]{+0010/0011's+} names already exists[-somewhere-] in production[-and the migration would either collide or create the ambiguity P11 tests for-] → `FAILED PREFLIGHT: table name already present — <schema>.<table>`.
· **P13** `COBALT_ENV=production uv run cobalt db migrate --allow-prod [---proof-only` (the exact line R7 approved this morning for `03-prod-proof-only-3.md`, run here as a preflight read).-]{+--proof-only`.+} **THIS CALL: set the Bash tool's `timeout` parameter to 600000** —[-the default 120 000 ms is LESS than it needs;-] it sits silent ≈50 s per probe reading [-`system.bars` (8.8 M rows).-]{+`system.bars`.+} It applies NOTHING and ends `NOTHING WAS APPLIED: --proof-only ran in a READ ONLY transaction.` Quote the whole proof table and the `proof cost:` line — that number is this deploy's outage budget.[-**(Round-1 finding 14, corrected:) do NOT expect this probe to mention 0008/0009's tables at all.**-] It runs from `/Users/cobalt/cobalt`, i.e. main's code, {+which now includes P4: expect **26** tables+} and [-`_probe_all` (`src/cobalt/db_migrations/cli.py:280-282`) enumerates the STATIC table list of the code-]{+do NOT expect+} it [-runs from — main proves 23 tables (deploy 1's table), P4's tree proves 26. So `movers_daily`/`picks`/`missed` are simply not in this probe's world; their-]{+to mention `archive_progress`/`archive_incidents` (their+} absence is proven by [-P12's catalog query,-]{+P12,+} not [-here. This step's gate is-]{+here). Gate =+} exactly three things: exit 0, no table marked `CHANGED`, no tool timeout. [-A `CHANGED` verdict on ANY table, a non-zero exit, or a tool timeout-]{+Otherwise+} → `FAILED PREFLIGHT: proof-only <what>` (nothing was applied;[-production is untouched;-] do NOT rerun).
· **P14** `.env` LANE CHECK — `ls -la [-/Users/cobalt/cobalt-wt/s2-p4/.env`,-]{+/Users/cobalt/cobalt-wt/archiver-append/.env`,+} `ls -la /Users/cobalt/cobalt-wt/ops-2026-09-19/.env`, `ls -la [-/Users/cobalt/cobalt-wt/archiver-append/.env`,-]{+/Users/cobalt/cobalt-wt/s2-p4/.env`,+} `ls -la [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/.env`+} → every one MUST say "No such file". Any hit[-means another session may hold `cobalt_dev`-] → `FAILED PREFLIGHT: .env present in <path> — another session may hold cobalt_dev`.
· **P15 — THE [-ARCHIVER PRECONDITION: RECORDED, NO LONGER A GATE (R31).** §1's gate no longer stacks `archiver/append-0919`, so its `requires_db` state cannot make this gate red. Two things are still recorded, because the desk wants them in this report: (1) read `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` and quote its LAST line — it should read `ARCHIVER DB2 f5df6c1 … db 161/0 …`, i.e. the three reds that once blocked this gate are GONE; (2) `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` for the branch tip. Neither is a stop: a different line, or none, is RECORDED and the run continues. Run 1 of this prompt found this precondition MET and then failed on the merge instead (`deploy-p4-2026-09-19.md`).-]
[-· **P16 — the other two branches' own state, recorded not judged:**-]{+TWO BRANCH GATES.**+} Read the last line of `/Users/cobalt/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md` (expected `OPS 0919 DB … | db 61/0 [-| cobalt_dev: 0001–0009, unchanged`)-]{+…`)+} and of [-`/Users/cobalt/cobalt-wt/s2-p4/docs/40-]{+`/Users/cobalt/cobalt-wt/archiver-append/docs/40+} - [-DevDocs/reports/s2-p4-verify-2026-09-19.md` (`P4 DB …`, the §2.2 gate).-]{+DevDocs/reports/rebase-d3-2026-09-19.md` (expected `BACKFILL WINDOW … | offline 2146/0 | db 2486/0 | registry 0001-0011 | … | .env: removed, proven gone`).+} A non-zero `failed` in {+either, or+} either {+line absent+} → `FAILED PREFLIGHT: <branch> carries red[-requires_db-] tests — L68 gate cannot be green`. {+Quote both lines and their position in the file.+}
{+· **P16 — THE L67 GATE.** Read `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md`'s LAST line (`D3 BUILD CHECK · … blocks deploy 3: <d> …`) and, if it exists, `/Users/cobalt/cobalt-wt/agy-trial/scratch/astra-d3-0919/REVIEW.md`'s last line (`ASTRA D3 · …`). **`blocks deploy 3` must be 0 in each line that exists.** Non-zero → `FAILED PREFLIGHT: house check blocks deploy 3 — <line>`. The desk owns how many houses read it; you only read the numbers.+}
0.2 `GATE: proven — commit + tag allowed by the session allowlist` (L62's launch proof), or the FAILED line.
0.3 NOT preflighted, because no harmless variant exists inside their own [-pattern (UNATTENDED-LAUNCH §6) — each is probed by its first real use:-]{+pattern:+} `worktree add`, the stack `merge --no-edit`, [-`rebase`,-]{+`merge --ff-only`,+} `bootout`, `bootstrap`, `kickstart`,[-`merge --ff-only`,-] `revert`, `db migrate --allow-prod`, the `cp`/`rm` of `.env`, `COBALT_ENV=dev` anything, `uv run pytest`.
0.4 Report written and committed by explicit name. Last line `CONTINUE: step 1`.

## 1. THE L68 INTEGRATED GATE — [-green BEFORE anything is merged-]{+and it builds the branch that ships+}
**The law, verbatim (LAWS.md [-L68, ruled today):**-]{+L68):**+} *"No branch merges while a second unmerged branch exists, unless an integrated pre-merge gate on the stacked tree is green — the offline suite and the with-DB suite run on the branch that combines them, before the merge, never after it in `~/cobalt`."* [-**SCOPE RULED BY DEJAN 2026-09-19 14:4x ET (`cto-2026-09-19.md` §4 R31 "A"), after run 1 of-]{+Both remaining unmerged branches are in+} this [-prompt stopped here:**-]{+deploy, so+} the stacked tree combines [-`sprint-2/p4` and `ops/2026-09-19` — **`archiver/append-0919` is NOT stacked in this gate**. Why: run 1 merged P4 clean (163 files)-]{+exactly what lands+} and [-ops clean (13 files) to tip `feca2cf`, then hit a 9-file CONFLICT on the archiver branch, five files of which this prompt had named in advance; that branch-]{+nothing else. R31's narrowing+} was[-cut from a pre-P4 `main`, its migration registry deliberately leaves the 0008/0009 gap-] for[-P4, and it must be REBASED before deploy 3 ships it whatever happens tonight. It is not landing tonight, so its seam is proven at-] deploy [-3's own gate, on the tree that actually lands then. This gate proves the seam between what is merging now-]{+2+} and [-what merges next; the merge at 4.3 still ships `sprint-2/p4` ALONE.-]{+does not apply here.+}
1.1 `git -C /Users/cobalt/cobalt log --oneline -1` [-(main's tip, record-]{+(record+} as `<main-at-gate>`) · `git -C[-/Users/cobalt/cobalt-wt/s2-p4 log --oneline -1` · `git -C-] /Users/cobalt/cobalt-wt/ops-2026-09-19 log --oneline -1` {+(expected `bb996a5`)+} · `git -C /Users/cobalt/cobalt-wt/archiver-append log --oneline -1` [-(RECORDED ONLY — that branch is not stacked, R31)-]{+(expected `7117fc3`, code tip `85b6d7c`)+} · `git -C [-/Users/cobalt/cobalt-wt/s2-p4-]{+/Users/cobalt/cobalt-wt/ops-2026-09-19+} status --porcelain`[-empty. Record all four tips in the report; the facts packet names the tips the desk saw,-] and [-a DIFFERENT-]{+the same for `archiver-append` — both EMPTY. A different+} tip is [-recorded,-]{+RECORDED,+} not a stop (a branch may have [-advanced) — but-]{+advanced);+} a dirty[-`s2-p4`-] worktree IS a stop: `FAILED: gate — [-s2-p4-]{+<worktree>+} not clean`.
1.2 BUILD THE STACK: `git -C /Users/cobalt/cobalt worktree add -b [-stack/deploy2-0919 /Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+stack/deploy3-0919 /Users/cobalt/cobalt-wt/stack-deploy3-0919+} main`. **RERUN [-CASE (round-1 finding 17; run 1 of this prompt WAS such a case — the desk removed `stack/deploy2-0919` and its worktree at 14:3x ET before relaunching, verified by `git worktree list`):**-]{+CASE:**+} this[-command-] FAILS if the branch or the path already exists, which is[-exactly-] what a second launch after a failed gate looks [-like —-]{+like;+} no `worktree remove` / `branch -D` rule [-exists-]{+is+} in your list, deliberately. [-So on-]{+On+} that failure: `ls -la [-/Users/cobalt/cobalt-wt/stack-deploy2-0919`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919`+} to show what is there, then `FAILED: gate — stack artifact from an earlier run is still on disk (branch [-`stack/deploy2-0919`-]{+`stack/deploy3-0919`+} and/or its worktree); the desk removes it before the next launch`. Do not reuse [-the existing worktree (its content is from the earlier branch tips),-]{+it,+} do not pick another name.
If `worktree add` SUCCEEDED → then, in this order, TWO separate [-calls (R31 — the archiver merge that run 1 typed here is REMOVED, not skipped silently):-]{+calls:+} `git -C [-/Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919+} merge --no-edit [-sprint-2/p4`-]{+ops/2026-09-19`+} · `git -C [-/Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919+} merge --no-edit [-ops/2026-09-19`. Run 1 got both of these CLEAN (163 files, then 13 files, tip `feca2cf`); a conflict on either one now is NEW and is reported, never resolved here.-]{+archiver/append-0919`.+} A CONFLICT on [-any of them-]{+either+} → `git -C [-/Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919+} merge --abort`, then `FAILED: gate — conflict merging <branch> — <paths>` (NOTHING is down, production is [-untouched,-]{+untouched;+} the desk resolves it in a build run, never here). [-**RUN 1 ALREADY PROVED THIS SEAM, which is why the archiver leg is gone:** P4 and the archiver branch both edit `src/cobalt/db_migrations/__init__.py` (the `FORWARD`/`REVERSE` tuples — P4 adds 0008/0009,-]{+**EXPECT+} the [-archiver adds 0010/0011) and-]{+overlap here if anywhere:**+} both {+branches+} touch [-`src/cobalt/db_migrations/placement.py`, `src/cobalt/cli.py`, `src/cobalt/archiver/runner.py` and `store.py`; run 1's third merge conflicted in exactly those five plus three test files and one DevDoc page (`deploy-p4-2026-09-19.md` §1.2, verbatim output). That is a REAL integration seam, it is recorded, and it is deploy 3's to resolve by rebasing-]{+`src/cobalt/cli.py` (ops +18 lines,+} the archiver [-branch. Between P4 and-]{++28) — different registrations, so it should auto-merge;+} ops [-there is no such overlap and run 1 proved it. Any conflict you hit now is reported, never resolved here.-]{+alone touches `src/cobalt/db_migrations/cli.py`.+} Record the stack tip as `<stack>` and `git -C [-/Users/cobalt/cobalt-wt/stack-deploy2-0919-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919+} log --oneline [--6`;-]{+-8`;+} also `grep -n "0008\|0009\|0010\|0011" [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/src/cobalt/db_migrations/__init__.py`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/src/cobalt/db_migrations/__init__.py`+} →[-with the archiver leg gone (R31)-] the merged `FORWARD` must list [-**0001…0009-]{+**0001…0011+} in numeric order** with `REVERSE` its [-mirror, and **0010/0011 must be ABSENT** (they live only on the unstacked archiver branch);-]{+mirror;+} anything else → `FAILED: gate — merged migration registry is wrong — <what you read>`.
1.3 OFFLINE SUITE ON THE STACK: `ls -la [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/.env`+} → "No such file"[-(a pytest run with `.env` present is NOT offline)-] → `cd [-/Users/cobalt/cobalt-wt/stack-deploy2-0919`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919`+} → `uv run pytest -q tests/cobalt tests/taxonomy` [-(`run_in_background`; read the summary from the tool result).-]{+(`run_in_background`).+} GATE: **`0 failed`**. Quote the whole summary line. [-For context-]{+Context+} only, never [-as a pass/fail rule: P4 alone closed at `1829 passed, 324 skipped`; ops-0919 at `1608/0`;-]{+the bar:+} the archiver [-at `1866 passed, 320 skipped` — the stack's own number is its own, and only `failed == 0` is the bar.-]{+branch alone closed `2146/0`, ops alone `1875/0`.+} Any failure → name each test, quote each assertion, `FAILED: gate — offline suite red on the stack — <tests>`.
1.4 WITH-DB SUITE ON THE STACK (`cobalt_dev` only — never production, never `--allow-prod`). [-**THE STEP ORDER IS PART OF THE GATE.** With the archiver leg gone (R31) the-]{+The+} stacked registry is [-**0001–0009**-]{+**0001–0011**+} and `cobalt_dev` [-is ALREADY-]{+sits+} at 0001–0009, so the forward step [-creates NOTHING and the restore step reverses NOTHING — both are run anyway, as proofs that the lane is what it claims to be, and their EXPECTATIONS below are rewritten accordingly. The order is kept because it is the order every other lane uses and because a surprise in it is exactly what this gate is for. Historical reason for the order, from the archiver's own DB run: with `cobalt_dev` at 0001–0009 and-]{+CREATES+} the archiver's two tables [-dropped, **20 of its 23 `requires_db` tests could not run at all** — its run 1 went `12 failed, 145 passed` purely on `relation … does not exist`, because only 3 of them are self-contained round trips; the rest need the tables to EXIST (seven via the `real_connect` fixture, thirteen via `BarStore().ensure_schema()`, which creates neither). That is-]{+and+} the [-shape this order was built for.-]{+restore drops them again.+} In this exact order:
  (a) `cp /Users/cobalt/cobalt/.env [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/.env`+} (by name, never read, never printed, L4) → `ls -la [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env`-]{+…/.env`+} prints it.
  (b) BASELINE: `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote the table. EXPECT `cobalt_dev` at **0001–0009** with [-`archive_progress` and `archive_incidents`-]{+`archive_progress`/`archive_incidents`+} ABSENT[-(`-`)-] and P4's `movers_daily`/`picks`/`missed` [-PRESENT — that is where the archiver and P4 DB runs each left it, and run 1 of this prompt did not touch it (it never reached §1.4). Record every row count and digest: this table is what (f) is compared against.-]{+PRESENT.+} A `CHANGED` verdict → [-`.env` cleanup, then-]{+go to (h), ending+} `FAILED: gate — cobalt_dev proof CHANGED before the gate — <line>`.
  (c) FORWARD: `COBALT_ENV=dev uv run cobalt db migrate`[-(the stacked registry is **0001–0009**, R31; all nine re-run idempotently by design)-] → quote the full table. EXPECT[-**NOTHING CREATED** —-] `archive_progress` and `archive_incidents` [-stay ABSENT, because 0010/0011 are not in this stack —-]{+**CREATED**,+} every {+pre-existing+} table `OK` with its digest unchanged,[-`content UNCHANGED on every table`,-] no `CHANGED`.[-A `CREATED` line here would mean the stack carries a migration it must not.-] Anything else → [-**go to (h) and follow it in order**,-]{+(h),+} ending `FAILED: gate — dev forward migrate <what>`.
  (d) SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 [-failed`** —-]{+failed`**;+} quote the [-summary line. For context only, never as-]{+summary. Context only:+} the [-bar: P4's-]{+archiver branch's+} own [-DB-]{+with-DB+} close was [-`2136/0` plus the two owed tests `37` ran; the archiver's scoped run was `154/3`, and **those three reds must already be gone by P15 or you never reached this step**.-]{+`2486/0`.+} A red → [-**go to (h) and follow it in order**,-]{+(h),+} ending `FAILED: gate — with-DB suite red on the stack — <test names + assertions verbatim>`. **Never label a red "known" and continue** [-(L68 admits no exception; L70: a red someone actually ran is a fact, not an escalate to carry).-]
[-  (d2)-]{+(L68, L70).+}
{+  (e)+} **POST-SUITE [-PROOF — new in run 3, and it is what (f) compares against (run 2's FALSE STOP, `deploy-p4-2026-09-19.md` SECOND RUN):**-]{+PROOF:**+} `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote [-the table. This is the state of `cobalt_dev` AFTER the suite and BEFORE the restore.-]{+it.+} **The suite WRITES to `cobalt_dev` [-— that is what a with-DB suite does** — so rows and digests legitimately move between (b) and here:-]{+by design** (deploy 2+} run 2 [-stopped because-]{+false-stopped on exactly this —+} `cobalt_redactions`[-went-] 127 [-rows / `0f80638d…` to 128 rows / `a93ce24f…`, which was its OWN suite's round-trip row, not another lane. Differences-]{+→ 128), so differences+} between (b) and [-(d2)-]{+here+} are [-RECORDED, named-]{+RECORDED+} table by [-table,-]{+table+} and are NOT a stop. [-What IS a stop here: a-]{+A+} table PRESENT at (b) and missing now, or [-a table appearing that-]{+one+} no stacked migration creates →[-`.env` cleanup path per-] (h), [-then-]{+ending+} `FAILED: gate — cobalt_dev table set changed during the suite — <what>`.[-(e) STATE THE END STATE PLAINLY, in the report, before you run the restore: `cobalt_dev holds after the suite: 0001–0009, archiver tables ABSENT` — the gate added nothing this time (R31), which is itself the thing to say plainly.-]
  (f) [-RESTORE — run it even though it has nothing to reverse, as the proof that the lane behaves:-]{+RESTORE:+} `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → EXPECT [-**NOTHING applied**, and **EXPECT A NON-ZERO EXIT saying so**: run 2 measured it — `FAILED: MigrationError: no registered migrations are newer than 0009`, exit 1. That is the harness being fail-loud about an empty rollback, it is the CORRECT outcome here, and it is **not** a gate failure: record the exact message and exit code-]{+exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` applied+} and [-go on. (A rollback that DOES apply something is the surprise — see the next sentence.) P4's 0008/0009 are NOT touched — if the output reverses-]{+**nothing else** (if+} anything numbered ≤ [-0009, that-]{+0009+} is [-a serious finding:-]{+reversed:+} quote it, [-`.env` cleanup,-]{+(h),+} `FAILED: gate — dev rollback went below 0009 — [-<what>`-]{+<what>`)+} → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → compare [-row for row against **(d2)'s post-suite proof, NOT against (b)** — (d2) is the state the restore acted on, so any difference between (d2) and here was caused by the restore alone. EXPECT them IDENTICAL (the restore applied nothing) and-]{+**against (e), not (b)**:+} the two archiver tables [-still ABSENT. Then, for the record only, also state how (d2) differed from (b) — that difference is the suite's own writes-]{+ABSENT again+} and [-is never a stop. A-]{+every other+} table [-whose-]{+identical to (e). A+} digest {+that+} moved [-**between (d2) and here** is NAMED with both values and-]{+ACROSS the restore+} is a [-STOP — `.env` cleanup per-]{+stop →+} (h),[-then-] `FAILED: gate — cobalt_dev digest moved across the restore on <table>: <before> → [-<after>` — because the restore applied nothing and therefore must have changed nothing. **A digest that moved between (b) and (d2) is NOT a stop** and must not be reported as one: that window contains the suite, which writes to `cobalt_dev` by design (run 2's `cobalt_redactions` 127 → 128 row was exactly this, and stopping on it was wrong). Another lane holding `cobalt_dev` is excluded at preflight by P14, not by this comparison.-]{+<after>`.+}
  (g) `rm [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env`-]{+/Users/cobalt/cobalt-wt/stack-deploy3-0919/.env`+} → `ls -la [-/Users/cobalt/cobalt-wt/stack-deploy2-0919/.env`-]{+…/.env`+} says "No such file". **The `.env` cleanup happens before ANY stop [-line, including every red branch above**-]{+line**+} — a stop line written while `.env` is on disk is itself a failure [-condition: go back, remove it, prove it, then fix the line.-]
[-  **(h)-]{+condition.+}
{+  (h)+} RED-BRANCH [-RESTORE (round-1 finding 4; premise corrected round 2, finding N2).** With the archiver leg gone (R31) neither (c) nor (d) can leave `cobalt_dev` above 0009 at all — the stack has no migration above it — so this branch is now a belt-and-braces proof rather than a cleanup; run it anyway and say what you proved. A red (c) usually leaves NOTHING applied — `cmd_migrate` applies, probes and commits in ONE transaction and rolls it back on `CHANGED` or error — so there (f) is a no-op restore: its `--rollback --down-to 0009` finds 0010/0011 absent and its two rollback files are `DROP TABLE IF EXISTS`. Run it anyway, because you cannot tell from the output alone how far a partial (c) got, and a no-op restore is free. So-]{+RESTORE:+} on a red at (c) or [-(d):-]{+(d),+} FIRST run (f) in [-full — the `--rollback --down-to 0009` and its closing `--proof-only` —-]{+full,+} THEN [-(g)'s `.env` cleanup,-]{+(g),+} THEN the FAILED line, [-and end that line-]{+ending it+} with `cobalt_dev: [-<state-]{+<the last state+} you [-proved>`. This is a `cobalt_dev` command on a dev database with production untouched; it is always safe to run and it is never skipped because the gate went red.-]{+PROVED>`.+} If the restore ITSELF errors, do not retry it: quote the error and write `FAILED: gate — <original reason> — cobalt_dev: [-<the last state you PROVED — (b)'s baseline 0001–0009 when (c) printed a rolled-back run and (f) printed nothing applied; 0001–0011 when (c)-]{+<last proven state,+} or [-(d) got that far; UNKNOWN when you cannot tell from what was printed>,-]{+UNKNOWN>,+} restore failed: [-<error>` so the desk knows exactly what to clean (round-3 finding R1: this line used to assert 0001–0011 unconditionally, which the corrected premise above makes false for a red (c)).-]{+<error>`.+}
1.5 `cd /Users/cobalt/cobalt` → `ls -la .env` (the file IS listed here — cwd is back; contents never read) → `git -C /Users/cobalt/cobalt log --oneline -1` = `<main-at-gate>` [-(nobody committed while you ran; if-]{+(if+} main moved, record it and go on — [-§4.2-]{+§4.3+} re-checks it where it matters).
1.6 Write the gate into the report as its own section: the [-four-]{+three+} tips, `<stack>`, the merged `FORWARD` line, both suite lines VERBATIM, all THREE `--proof-only` [-verdicts (baseline / post-forward table / post-rollback),-]{+verdicts,+} the forward and rollback tables, the `.env` proof, and [-P15's quoted archiver line with its date. If any part of the gate was not run, say WHICH and WHY in that section — a-]{+both P15 lines quoted. A+} gate section with a missing step is[-a-] `FAILED: gate — incomplete`, never a green one. [-**The gate branch `stack/deploy2-0919` and its worktree are-]{+**`stack/deploy3-0919` is NOT+} a [-GATE ARTIFACT, not work to land: they are never merged, never pushed, never rebased, and they stay on disk for the desk to remove at the close-]{+throwaway this time+} — [-record them in ESCALATE as cleanup owed.**-]{+it is the branch §4.3 fast-forwards `main` to.**+} Report committed by name. Last line `CONTINUE: step 2`.

## 2. Preconditions
2.1 **WINDOW.** `date` → the run STARTS only when now is **≤ [-18:30-]{+20:15+} ET on Saturday 2026-09-19**. [-WHY THIS IS LAWFUL AND WHY THE HOUR MATTERS: L43 binds `com.cobalt.radar` restarts to the 20:00–21:00 ET `market_reset` pause "or during overnight idle … **on a trading day**; never while a scanning session is open" — today is Saturday, not a trading day, and the radar reads `idle:overnight` at every hour, so no scanning session can be open and-]{+WHY: R32 overruled+} L43's [-trading-day window does not bind; L66's-]{+one-deploy-per-evening for tonight and set its+} own [-requirement (residents down before-]{+condition —+} the [-merge-]{+lane green before 21:00 —+} and[-up after) is met by §4 regardless of hour. This is-] the [-same basis deploy 1 ran on at 08:19 ET this morning under 09-18 R3/R5. The 18:30 cap-]{+hard edge+} is[-not L43 — it is margin against **the nightly backup,-] `com.cobalt.backup`, which runs at [-21:40-]{+**21:40+} ET on ALL SEVEN days** [-(`configs/cobalt/jobs.yaml:224` `weekdays: [0,1,2,3,4,5,6]`, `ops/com.cobalt.backup.plist:72-79`)-]{+(`configs/cobalt/jobs.yaml:224`, `ops/com.cobalt.backup.plist:73-79`)+} and must never fire into a half-deployed tree. [-**Round-1 finding 13b, corrected here:**-]{+20:15 leaves the measured ≈3–4 minute outage plus smoke plus margin.+} `com.cobalt.archiver` (20:30) and `com.cobalt.replay` (21:10) are **Mon–Fri only**[-(`ops/com.cobalt.archiver.plist:56-57`, Weekday 1–5)-] and[-therefore-] cannot fire on a {+Saturday. L43's restart-window clause is met independently:+} Saturday [-at all — the earlier wording named the archiver as tonight's risk-]{+is not a trading day+} and[-was wrong. Besides the backup,-] the [-only thing running today is `com.cobalt.heartbeat` (every interval, all week).-]{+radar reads `idle:overnight` at every hour, so no scanning session can be open.+} Now > [-18:30-]{+20:15+} → `FAILED: window — wait for the desk` (the desk re-approves a new window; you never pick one).
**THE CLOCK IS RE-CHECKED with `date` ONCE, immediately before 4.2 (bootout), and nowhere else. At or after [-19:40-]{+21:00+} ET at that check → `FAILED: window`, nothing is down, nothing is merged, stop.**
**THE POINT OF NO [-RETURN (round-1 finding 2, a BLOCKER in the previous wording — read this twice).** The earlier text re-checked the clock "again at 5.2" and sent a late run to §8; that would have taken a GREEN merge and a GREEN migration and REVERTED them for no reason but the hour, leaving 0008/0009 applied over reverted code, the replay job unarmed and a second outage. It is deleted. The rule is now: **once-]{+RETURN: once+} 4.3's `merge --ff-only` has returned success, this run FINISHES FORWARD.** From that instant the clock is a MEASUREMENT, never a [-trigger —-]{+trigger;+} the `date` calls at 4.2 and 5.2[-exist to-] compute the outage length and[-to-] stamp the report, and no [-hour, however late,-]{+hour+} sends a successful merge+migration to §8. §8 is entered ONLY for a RED result (a failed step, a red smoke, a mid-run denial), never for a late clock. If the run is still going at 21:40 and the backup fires beside it, that is an ESCALATE [-line in the report,-]{+line,+} not a revert. Conversely nothing after 4.2 may be ABANDONED for the hour either: finish §5's residents-up, finish §6's smoke, write the stop line.[-Said once more without the shorthand (round-2 finding N4): once 4.3 returns success the CLOCK never sends this run to §8 — a RED result at 4.3–4.6 or in §6's smoke still does, and §8 is then run as the step that sent you there specifies (4.3's foreign-commit branch says steps 3–5 only; 5.2's says (0) through (3); everything else enters at (0) and each of §8's steps is conditional on its own trigger).-]
2.2 **THE [-TWO P4 GATES** (L67 + L68's companions): (a) the P4 DB run's report section, written by `37-p4-db-run.md`-]{+GATES ALREADY READ AT P15/P16** are quoted again+} in [-`/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md`, ends `P4 DB <sha> | … | owed requires_db: <names → PASSED> | …` with-]{+this section as the record:+} both [-owed tests PASSED and `0` failed — missing, or ending `FAILED`, or either owed test not passed → `FAILED: gate — P4 DB run <what>`; (b)-]{+branch stop lines,+} the [-houses' fold read `36-review-p4-folds.md` has landed-]{+`D3 BUILD CHECK` line,+} and the [-desk's §4/§51-class section in `cto-2026-09-19.md` records-]{+`ASTRA D3` line if+} it [-with NO blocker for shipping — absent → `FAILED: gate — L67 house check not recorded`.-]{+exists.+} Neither is re-derived by [-you; you read the stop line and the desk's recorded verdict, and quote both.-]{+you.+}
2.3 **THE GATED CODE.** `<gated>` = the [-`sprint-2/p4` tip-]{+two branch tips+} named in the approval row. `git -C [-/Users/cobalt/cobalt-wt/s2-p4 log -1 --format=%D` shows `HEAD -> sprint-2/p4`; `git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` empty; `git -C /Users/cobalt/cobalt-wt/s2-p4-]{+/Users/cobalt/cobalt-wt/archiver-append+} diff --stat [-<gated>-]{+<the archiver tip in the row>+} HEAD -- . ':(exclude)docs'` prints [-NOTHING (only-]{+NOTHING, and the same for ops. Only+} report commits may sit above the gated [-code-]{+code; anything else → `FAILED: gate+} — [-the whole tree outside `docs/` is compared, never a hand-listed set of folders); record `<n>` = `git -C /Users/cobalt/cobalt-wt/s2-p4 rev-list --count main..HEAD`.-]{+<branch> moved since approval — <paths>`.+}
2.4 `git -C /Users/cobalt/cobalt log --oneline -3`: main's tip is a desk or deploy docs commit; no [-build or deploy-]{+other+} session is merging (the desk launches you alone).[-`git -C /Users/cobalt/cobalt merge-base main sprint-2/p4` → record it (the desk saw `919d562`, and `git -C /Users/cobalt/cobalt diff --stat 919d562 main -- . ':(exclude)docs'` printed NOTHING — main has moved by DOCS ONLY since P4 branched, which is why 4.1's rebase cannot change code; re-prove that with the merge-base you actually find, and a NON-EMPTY diff there → record it and treat 4.1's proof as the real gate, not this one).-]

## 3. Machine-written files, snapshot, tag
3.1 `git -C /Users/cobalt/cobalt status --porcelain` → expected only `docs/40 - DevDocs/reports/seat-usage.md`, the desk's live `cto-2026-09-19.md`, your own report, and the two untracked `docs/40 - DevDocs/prompts/2026-09-19/31-packet/*.yaml` copies [-the desk left (those are copies-]{+(copies+} of files under gitignored `data/backups/` — **do NOT `git add` them**, name them in ESCALATE and leave them untracked). `git add` BY NAME (never `-A`, never `.`, never[-`data/`, never-] a directory), commit `chore: machine-written + report files, committed as-is (L51-2)`. Anything else dirty → `FAILED: main dirty — <path>`.
3.2 `COBALT_ENV=production uv run cobalt backup run` (**THIS CALL: `timeout` 600000**) → quote the `cobalt_brain` dump size line and the restic snapshot id; `COBALT_ENV=production uv run cobalt backup status` shows it newest. No snapshot id → `FAILED: snapshot`. `git -C /Users/cobalt/cobalt tag [-pre-p4-0919`. (Known, ops ESCALATE 4: the-]{+pre-d3-0919`. (The+} 21:40 nightly prunes this snapshot — record id + time; **the durable rollback point is the tag [-`pre-p4-0919`**.)-]{+`pre-d3-0919`**.)+} There is NO rollback [-DIRECTORY in this deploy:-]{+DIRECTORY:+} no settings file is read or [-written, so there is nothing to stage (deploy 1's `mkdir -p …/daymode-settings-rollback` and its four `settings load --from` strings are absent from your allowlist by design).-]{+written.+}
3.3 Report committed by name NOW — **this is your LAST commit before the merge**. Last line `CONTINUE: step 4`.

## 4. [-Rebase → residents-]{+Residents+} DOWN → merge → migrate → validate (no report prose between these commands; record outputs, write them up after 5.2)
4.1 [-`git -C /Users/cobalt/cobalt-wt/s2-p4 rebase main` (brings P4 above your own commits; a conflict → `git -C /Users/cobalt/cobalt-wt/s2-p4 rebase --abort` then `FAILED:-]{+(no+} rebase [-conflict — <paths>`; nothing is down yet). **PROOF before going on:** `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat <gated> HEAD -- . ':(exclude)docs'` prints NOTHING (what you merge is byte-identical to what-]{+step:+} §1 [-gated and §2.2 proved). ONE NAMED EXCEPTION (it happened on 09-18): if your own 3.1 committed a machine-written `configs/cobalt/rules.yaml`, the rebase carries that file onto-]{+built+} the branch[-and this diff lists exactly-] that [-ONE path — accept it ONLY when `git -C /Users/cobalt/cobalt-wt/s2-p4 diff --stat main HEAD -- configs/cobalt/rules.yaml` prints nothing. ANY other path → `FAILED: rebase changed code — <paths>` (nothing is down; do-]{+ships, and `main` has+} not [-go on). Then-]{+moved since `<main-at-gate>` — re-prove that here:+} `git -C [-/Users/cobalt/cobalt-wt/s2-p4 rev-list --count main..HEAD`-]{+/Users/cobalt/cobalt rev-parse --short HEAD`+} =[-`<n>` of 2.3 (the rebase dropped nothing). Record-] `<pre-merge>` = {+`<main-at-gate>`; if main moved,+} `git -C /Users/cobalt/cobalt [-rev-parse --short HEAD`.-]{+merge-base --is-ancestor <main-at-gate> HEAD` and record — a moved main means the gate proved a different base, so `FAILED: gate — main moved after the gate — <hash>`, nothing is down.)+}
4.2 `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` → baseline. `date` (the 2.1 re-check). Then **residents DOWN (L66)**: `launchctl bootout gui/501/com.cobalt.aset` · `launchctl bootout gui/501/com.cobalt.radar` · `launchctl print gui/501/com.cobalt.aset` and `launchctl print gui/501/com.cobalt.radar` → each must FAIL with "Could not find service" (still printed → one `date`, print once more; **still loaded after that one retry → the run [-STOPS —-]{+STOPS,+} but not before you put the OTHER resident [-back. REPAIR FIRST (round-2 finding N1, all three reading calls): `bootout` is two separate commands, so one of them may have SUCCEEDED;-]{+back:+} for EACH label whose `launchctl print` now says "Could not find service" (i.e. the bootout that DID take), run 5.2's bootstrap for that label [-ONCE — `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` for aset, `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for radar — then `launchctl-]{+ONCE,+} print [-gui/501/<label>`-]{+`state`+} once [-more-]{+more,+} and record [-`state`.-]{+it.+} ONE attempt per label, no `kickstart`, no second pass. THEN write `FAILED: residents not stopped — <label> — <launchctl output verbatim>`,[-and-] say plainly that NOTHING was merged and nothing was migrated, [-so production's CODE is untouched;-]{+and+} state the services[-from what you just printed,-] as `residents: aset <up/DOWN>, radar [-<up/DOWN>`,-]{+<up/DOWN>` from what you just printed,+} NEVER as "as they were". Do NOT enter §8** — §8's step (0) is a bootout, which is the thing that just [-failed; entering it would loop).-]{+failed).+} A heartbeat beat landing in this window goes RED on the sheet/radar probes and DMs him — expected; note the beat time afterwards.
4.3 `git -C /Users/cobalt/cobalt rev-parse --short HEAD` = `<pre-merge>` (else `FAILED: foreign commit on main — <hash>` → §8 steps 3–5 [-only: nothing merged, bring the residents up)-]{+only)+} → `git -C /Users/cobalt/cobalt merge --ff-only [-sprint-2/p4`-]{+stack/deploy3-0919`+} → record the tip and the file/insertion counts. {+A non-fast-forward error here means main moved: `FAILED: merge not a fast-forward — <output>` → §8 steps 3–5 only (nothing merged, bring the residents up).+}
4.4 (nothing here —[-there is-] no settings [-dry-run-]{+step+} in this [-deploy; the config-shape change rode in with 4.3.)-]{+deploy.)+}
4.5 **THE MIGRATION.** `COBALT_ENV=production uv run cobalt db migrate --allow-prod` — **THIS CALL:[-set the Bash tool's-] `timeout`[-parameter to-] 600000.** EXPECT it to sit SILENT ≈50 s in its BEFORE probe and again in its AFTER [-probe (that is the proof reading `system.bars`, not a hang):-]{+probe:+} run it in the FOREGROUND, do not interrupt it, do not background it.[-Budget ≈2–3 minutes.-] The runner walks ALL of `FORWARD` = [-0001…0009-]{+0001…0011+} and prints `-- applying` for each; [-0001–0007-]{+0001–0009+} re-run idempotently by design. **GATE = its proof table:** NO table marked `CHANGED`, and the only `CREATED` objects are the ones [-`0008_radar_value_movers.sql`-]{+`0010_archive_progress.sql`+} and [-`0009_picks_missed.sql`-]{+`0011_archive_incidents.sql`+} define — read those two files (index card) and hold the output to them: [-`system.movers_daily`, `"user".picks`, `"user".missed`, plus `system.radar_membership` gaining `rank_metric`/`rank_value` as ADDED COLUMNS on an `OK` table, never as a `CHANGED` verdict.-]{+`system.archive_progress`, `system.archive_incidents`, and the sequence `archive_incidents_id_seq`.+} Quote the whole table and the `proof cost:` line.[-Take each new table's SIDE from the printed table, never from memory — 6.3 queries them with that side.-] STATED FINDINGS, not to be re-derived: (i) the whole migrate transaction runs at REPEATABLE READ, so a concurrent commit cannot read as `CHANGED`;[-only a concurrent UPDATE/DELETE of-] a[-row this migration also updates can raise-] `could not serialize access` [-— if it happens-]{+error means+} the harness [-names it, exits non-zero and applies NOTHING:-]{+applied NOTHING —+} that is a failed 4.5 → §8, say `serialization failure`, and do NOT [-rerun the command.-]{+rerun.+} (ii) The harness COMMITS BEFORE it prints its proof table, so a timeout[-or dropped connection-] in the instant after the commit leaves [-0008/0009-]{+0010/0011+} APPLIED with no table printed. **Whenever 4.5 ends without a complete proof table, do NOT write "nothing applied" from the absence of output: read it back first** — `COBALT_ENV=production uv run cobalt db query --side system --prod "SELECT [-to_regclass('system.movers_daily')"`-]{+to_regclass('system.archive_progress')"`+} → empty/NULL = not applied, a name = applied and committed — then go to §8[-(the same path either way: the code is reverted, the additive schema stays)-] and label the FAILED line `migrations applied: yes/no` from that read. A tool timeout is labelled `tool timeout`, never `migration error`. Non-zero exit → §8; output showing a migration PARTIALLY applied → run NO rollback, quote it, `FAILED: migration partial` after §8's code revert + residents up.
4.6 `COBALT_ENV=production uv run cobalt validate` → exit 0. Record the `Jobs (F17):` [-line (expected **16 registered — 6 resident, 10 one-shot** once 5.1 registers; at this point it may still read 15 — say which),-]{+line,+} `registry <-> ops/` and `registry <-> plists` lines, and `Placement: tree clean`. {+**`Placement: tree clean` matters more than usual tonight** — 0010/0011 add two `system` tables and the placement map must already name them (the suite asserts it; this is production's own check).+} Red anywhere in 4.3–4.6 → §8.

## 5. Residents UP[-+ the `com.cobalt.replay` first bootstrap-]
5.1 `COBALT_ENV=production uv run cobalt jobs register` → quote the count (expected `registered 16 [-job(s)`; 15 before the merge-]{+job(s)`+} — [-the-]{+this deploy adds NO+} new [-one-]{+job; a different number+} is [-`com.cobalt.replay`).-]{+recorded and named).+} Then `COBALT_ENV=production uv run cobalt jobs restarts <pre-merge>..HEAD` VERBATIM; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`, 0 [-UNCLASSIFIED, with `ops/com.cobalt.replay.plist A → new one-shot, bootstrap once: com.cobalt.replay` on its own row.-]{+UNCLASSIFIED.+} A THIRD resident derived → ESCALATE, not restarted today. Any UNCLASSIFIED path → ESCALATE by name, never dropped (L42).
5.2 `date` (**MEASUREMENT [-ONLY** — it stamps the outage length; it is never a gate and never sends this run to §8, §2.1's point of no return)-]{+ONLY**)+} → `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.aset.plist` (the REPO plist, R6) · `launchctl bootstrap gui/501 /Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` · `launchctl print gui/501/com.cobalt.aset` and [-`launchctl print gui/501/com.cobalt.radar`-]{+`…/com.cobalt.radar`+} → `state = running`, NEW pids, `path =` lines unchanged. `Bootstrap failed: 5` → one `date`, retry ONCE. Loaded but not running → `launchctl kickstart -k gui/501/<label>` ONCE. Still not running → `FAILED: residents DOWN — <label> — <launchctl output verbatim>` after running §8 from[-its step-] (0) through [-(3) — step (0) boots out the resident that DID start, so nothing runs while the code is reverted — and then, unless §8 itself ended in one of its terminal residents-DOWN branches (those stand),-]{+(3), then+} ONE bootstrap attempt of both on the reverted, validated [-tree (this step's commands, the same single retry / single `kickstart`).-]{+tree.+} Whatever is not running after that stays down:[-that is the-] terminal state; §8 is never entered [-a second time.-]{+twice.+} `date` → outage length (4.2 → 5.2).[-5.3 **`com.cobalt.replay` FIRST BOOTSTRAP (L55: Code installs and proves every scheduled job it ships).** `launchctl bootstrap gui/501 /Users/cobalt/cobalt/ops/com.cobalt.replay.plist` → `launchctl print gui/501/com.cobalt.replay` → quote `state` and the calendar entries. The plist declares `RunAtLoad false` and `StartCalendarInterval` **Mon–Fri 21:10 local (ET)**, so bootstrapping it on a SATURDAY arms it and runs NOTHING tonight — its first run is **Monday 2026-09-21 21:10 ET**, after the 20:30 archiver it depends on and after the `market_reset` pause. Say that sentence in the report. `Bootstrap failed: 5` → one `date`, retry ONCE; still failing → this is an ESCALATE, **not** a §8 trigger and **not** a deploy failure: the two residents are up, the merge stands, the job is simply unarmed — record `com.cobalt.replay: NOT bootstrapped — <output verbatim>` and carry it into the stop line's ESCALATE count. A failed bootstrap of a job that runs Monday never justifies reverting a green deploy.-]

## 6. Smoke — ALL delivered functionality (NN#16), each quoted in `## Smoke`
6.1 (every `curl`[-is-] typed in its FULL allowlisted [-spelling, never the abbreviation printed here)-]{+spelling)+} `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/` = 200 · `curl -s -o /dev/null -w %{http_code} http://127.0.0.1:5010/radar` = 200 · `COBALT_ENV=production uv run cobalt heartbeat show` → **no NEW RED, and a NEW RED sends this run to [-§8** (round-1 finding 7 — §8's own header lists "red smoke" but this line did not say so).-]{+§8**.+} TWO named exceptions, neither[-of which is-] a new RED: the P8 BASELINE [-class recorded at preflight,-]{+class,+} and a RED whose timestamp falls INSIDE the outage window [-4.2→5.2 (the 15-minute beat landing while the residents were down) — name either verbatim and go on.-]{+4.2→5.2.+} Anything else red → §8 · `COBALT_ENV=production uv run cobalt validate` exit [-0,-]{+0+} with [-the-]{+its+} `Jobs (F17)` / `registry <-> ops/` / `registry <-> plists` {+/ `Placement`+} lines quoted[-(this is `com.cobalt.replay`'s registry proof)-] · `grep -c "ENTERED RED" /Users/cobalt/cobalt/logs/heartbeat.log` against 4.2's baseline.
6.2 LOGS: `tail -n 60 /Users/cobalt/cobalt/logs/radar.err` → started on the new code, no traceback, **no `CardSettingsError`, no `trade_def_slug`, no [-`radar_pool_failed_stage_check`** (the 09-17 incident signatures),-]{+`radar_pool_failed_stage_check`**,+} and **no `not_equity` / config-shape [-error** — the R16 rule renamed `radar.yaml`'s `not_equity` keys and the loader refuses the OLD shape by name, so a parse error here means the merged config and the merged code disagree → §8.-]{+error**.+} Outside a scanning session the correct cycle line is `radar cycle: idle:overnight scan_id=None` ≈100 s apart — say so. · `tail -n 30 /Users/cobalt/cobalt/logs/aset.log` → no traceback, no degraded day-mode banner.
6.3 [-P4's-]{+**THE ARCHIVER'S+} OWN [-surfaces, read-only: for each of `movers_daily`, `picks`, `missed`, ONE-]{+SURFACES, read-only.** (a) the two new tables exist and are readable on the system side:+} `COBALT_ENV=production uv run cobalt db query --side [-<the side 4.5's proof table printed for that table>-]{+system+} --prod "SELECT count(*) FROM [-<table>"`-]{+archive_progress"` and the same for `archive_incidents`+} → a number, no error [-(0-]{+(**0+} rows is the expected and correct [-case: nothing-]{+case** — no nightly run+} has [-written them yet) ·-]{+happened yet; the first is Monday 20:30, Mon–Fri). (b) **THE REVOKE (R25), the one thing this deploy adds that is a PERMISSION and not a table:**+} `COBALT_ENV=production uv run cobalt db query --side [-system-]{+user+} --prod "SELECT [-count(*) FROM radar_membership WHERE rank_metric IS NOT NULL"`-]{+has_table_privilege('cobalt_user','system.archive_progress','SELECT')"` and the same for `archive_incidents`+} → [-a number, no error (0008's two columns exist-]{+**both must return FALSE**. A TRUE means 0001's blanket grant won+} and [-are readable; 0-]{+R25 did not take → that+} is [-expected — no scan has run since they were added). Never guess-]{+not+} a [-side; take-]{+crash but it is a defect against an explicit ruling: record+} it [-from-]{+verbatim, finish+} the [-table you quoted.-]{+smoke, and put it in ESCALATE as `R25 NOT IN FORCE` — **do not revert for it and do not try to fix it** (a `REVOKE` typed by hand is a production DDL nobody approved). If the query itself errors, quote the error and say the check could not run (L70) rather than reporting a pass.+}
6.4 **CARDS ARE STILL LIVE** [-(the one card check in this deploy, and it is a-]{+(a+} READ): `COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.cards_enabled'"` → **one row, True**. Anything else[-— a missing row, `False`, more than one row — means something in this deploy touched a settings row it must never touch-] → `FAILED: cards state changed — <what>` and §8 (whose (3) leaves every settings row exactly as it is). This is a verification, never a repair: you have no `settings load` command [-of-]{+in+} any [-kind.-]{+spelling.+}
6.5 **`cobalt smoke s2` IS NOT [-RUN,-]{+RUN**,+} and its redness is not a deploy [-verdict.** Two of its checks are RED BY DESIGN tonight and neither is a defect: (a) **K9**-]{+verdict+} — [-the replay `job.result` shape gained `movers_by_side`,-]{+K9+} and {+K3 are red by design+} until [-the-]{+Monday's+} first [-`com.cobalt.replay` run writes a job-]{+replay+} row[-carrying it, K9.2/K9.5 read FAIL-] and [-K9.3/K9.6 ERROR;-]{+until+} the [-first such row is **Monday 2026-09-21 21:10 ET**-]{+desk's A7/K3 ruling+} (`s2-p4-verify-2026-09-19.md` ESCALATE[-15 / 5(a)); (b) **K3** — a legitimate frozen HOLD grades red: a pre-deploy episode held with `rank_metric` NULL and a post-cutoff `last_scan_id` makes `rescanned_admitted ≥ 1` and `rescanned_metric_missing ≥ 1`, and the framework's grader returns FAIL naming `rescanned_metric_missing`, indistinguishable by column from a RETAIN defect — confirmed on real Postgres (`s2-p4-verify-2026-09-19.md` §4.6), carried as **ESCALATE-] 2 / [-tribunal A7**, and the desk's ruling on it is OWED and is NOT this hub's to take. **Therefore: do-]{+15). Do+} not run [-`cobalt smoke s2`,-]{+it,+} do not add it to any check list,[-and-] never write "smoke red" as this deploy's [-verdict.** NN#16's "full smoke of ALL delivered functionality"-]{+verdict. NN#16+} is met by [-6.1–6.4, which are deploy 1's checks plus P4's own three.-]{+6.1–6.4.+} If some other command's output [-happens to print-]{+prints+} a K3 or K9 line, record it verbatim[-under `## Smoke`-] labelled `RED BY DESIGN —[-ESCALATE 2 / ESCALATE 15,-] not a deploy failure`, and continue.
6.6 NOT DONE HERE, deliberately: no [-`radar evaluate --replay` (deploy 1 ran it as P2's DARK proof; cards are LIVE now-]{+`cobalt archiver` run of any kind (the nightly is Mon–Fri 20:30+} and [-this hub has not established-]{+the first live run is **Monday 2026-09-21**; R35 makes+} that [-a replay stays read-only with them enabled —-]{+ONE shadow night before his ruling on the numbers) · no backfill (R34 bounds+} it [-is not in your allowlist, so-]{+out of 04:00–09:30 and+} it [-cannot be typed)-]{+is operator-initiated anyway)+} · no scan proof: Saturday is overnight idle throughout, so the first live proof of the [-R16 fund rule-]{+ops day-open guard is **Sunday 2026-09-20's `cobalt day-open`**+} and of the [-new columns is-]{+archiver+} **Monday [-2026-09-21 04:00 ET premarket**, proven by that day's day-open; write that sentence.-]{+20:30**. Write those two sentences.+}

## 7. Close
Green → `git -C /Users/cobalt/cobalt tag [-deploy-2026-09-19b`.-]{+deploy-2026-09-19c`.+} **The tag name is fixed at approval and is never re-derived by you**: `deploy-2026-09-19` [-is already-]{+and `deploy-2026-09-19b` are+} taken by [-this morning's deploy,-]{+tonight's first two deploys,+} so this one is [-`deploy-2026-09-19b` — the second deploy of the same date.-]{+`deploy-2026-09-19c`.+} If `date` says the day is no longer 2026-09-19 when you reach §2.1, that is `FAILED: window` at 2.1 and you never reach here; you do NOT silently rename the [-tag to another day.-]{+tag.+} Then: the Deploy table, `RESTARTS done`, the outage length,[-`com.cobalt.replay` armed with its first run time, the membership gap (4.2 → first completed scan = Monday 04:00 ET), and-] the two "first live proof" sentences of [-6.6.-]{+6.6, and the R25 REVOKE result from 6.3 (b) stated explicitly either way.+} ESCALATE section: the gate artifacts [-(`stack/deploy2-0919`-]{+(`stack/deploy3-0919`+} + its [-worktree) owed for cleanup,-]{+worktree — **this time the branch IS merged, so only the WORKTREE is cleanup owed**),+} the two untracked `31-packet/*.yaml`, the [-carried-]{+four MINOR findings the houses left open (G-A1, G-A2, H1, H2 — carried, not fixed), and the+} P4 escalates the desk still owes a ruling on [-(A2 deadline scope,-]{+(A2,+} A7/K3, ESCALATE [-19 ticker pinning),-]{+19),+} plus anything you found. Report committed by name. Last line, exactly this shape:
[-`P4-]{+`D3+} DEPLOY DONE <hash> · LIVE: [-sprint-2/p4-]{+ops/2026-09-19 + archiver/append-0919+} · migrations [-0008/0009-]{+0010/0011+} applied · gate: stack <stack> offline <p>/<f>, db <p>/<f> · RESTARTS done: com.cobalt.aset com.cobalt.radar · [-com.cobalt.replay <bootstrapped,-]{+R25 REVOKE: <in force | NOT IN FORCE> · archiver mode: upsert,+} first [-run-]{+nightly+} Mon [-21:10-]{+20:30+} ET[-| NOT bootstrapped: <reason>>-] · cards: LIVE, untouched · outage <mm:ss> · tag [-deploy-2026-09-19b-]{+deploy-2026-09-19c+} · PUSH: Dejan · ESCALATE: <n>`[-(Round-1 finding 18: that slot is filled from what 5.3 actually printed. The line never says "bootstrapped" when 5.3 recorded `NOT bootstrapped` — a stop line that contradicts its own report is worse than a failed bootstrap.)-]
or `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`.

## 8. SAFE-STATE RULE — red at 4.3–4.6, red smoke, a failed resident bootstrap, or a mid-run denial after 4.2
STEP (0), FIRST, ALWAYS: `launchctl print gui/501/com.cobalt.aset` and [-`launchctl print gui/501/com.cobalt.radar`;-]{+`…/com.cobalt.radar`;+} if EITHER is loaded[-(you came from 5.2 or from a red smoke in §6)-] → `launchctl bootout` both and require "Could not find service" for both BEFORE you touch code. A resident must never be running while its code is reverted under it; `bootstrap` in (4) is only ever run on a service that is not loaded. [-**IF `com.cobalt.replay` WAS BOOTSTRAPPED AT 5.3, BOOT IT OUT TOO** (round-1 finding 3): `launchctl bootout gui/501/com.cobalt.replay` → `launchctl print gui/501/com.cobalt.replay` must fail "Could not find service". WHY this changed: the whole `src/cobalt/replay` package arrives WITH P4 and is absent from main, so after a §8 revert a job left armed would fire `uv run cobalt replay nightly` on Monday 21:10 ET against a tree that has no `replay` module — a loud Monday-night failure nobody-]{+**`com.cobalt.replay`+} is [-watching for. Record it in the ESCALATE line either way (`com.cobalt.replay:-]{+NOT+} booted [-out` / `never bootstrapped`). If the bootout does not take after one retry, say so plainly in the ESCALATE and in the FAILED line-]{+out here**+} — it {+arrived with deploy 2, it+} is not [-worth a second pass through-]{+in+} this [-section, but the desk must know-]{+deploy's range, and reverting this range leaves its module in place; `com.cobalt.archiver` likewise already existed. Neither can fire on+} a [-dead job is armed for Monday.-]{+Saturday.+}
GOAL: code and config agree, residents UP, before you stop. **THIS DEPLOY HAS NO SETTINGS LEG** — no `trader_settings` row was written, so there is no second half to [-restore and no `market_reset` clock to beat.-]{+restore.+} CLOCK NOTE:[-the 19:40–21:00 ET refusal that bound deploy 1 applied to `settings load --apply`; nothing here is refused by it, and-] **no hour ever sends a green result into this section** (§2.1's point of no return). [-`com.cobalt.archiver` and `com.cobalt.replay` are Mon–Fri and cannot fire on a Saturday; the-]{+The+} only job that fires tonight is the 21:40 [-backup (all seven days). If-]{+backup; if+} §8 is still running as [-21:40-]{+it+} approaches, finish (0)–(5) anyway[-— a half-finished safe state is worse than a snapshot taken beside one —-] and let the ESCALATE say so.
(1) **If 4.3 merged:** `git -C /Users/cobalt/cobalt revert --no-edit <pre-merge>..HEAD` — the range starts at the commit IMMEDIATELY BEFORE the ff-merge, so it holds [-P4's-]{+both branches'+} commits and nothing of yours (L54 — **never `reset --hard`**, which is not in your allowlist in any spelling).[-This reverts P4's code AND `configs/cobalt/radar.yaml` together, which is the whole hard pair (WHY 5).-] A CONFLICT during the [-revert (not expected on a linear range nothing was committed above):-]{+revert:+} `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → that pair is ALREADY [-consistent (code + its own config, both from the merge),-]{+consistent,+} so make THAT your safe state: `COBALT_ENV=production uv run cobalt validate` exit 0, then (3)–(6) with `safe state: NEW code kept — revert conflicted`.
(2) (no settings [-step — see the GOAL note above.)-]{+step.)+}
(3) **Migrations [-0008/0009-]{+0010/0011+} STAY.** They are additive (two[-nullable columns on `system.radar_membership`; three-] new [-empty tables) and the pre-P4 code neither-]{+tables, nothing else+} reads [-nor writes them,-]{+them)+} so reverted code runs unchanged over them. **The schema is NEVER rolled back unattended** — no [-PRODUCTION-]{+production+} rollback is instructed anywhere in this prompt and none is [-allowlisted as its own rule: `--allow-prod` is allowlisted only bare and with `--proof-only`, and you never type `--allow-prod --rollback --down-to …`-]{+typed+} in any [-branch (the desk's probe above is why this sentence says "never typed" rather than "cannot be typed") (the dev-lane rollback of §1.4 is a different command against `cobalt_dev` and has nothing to do with production);-]{+branch;+} the rollback files[-(`0009_picks_missed.rollback.sql`, `0008_radar_value_movers.rollback.sql`, reversed by `COBALT_ENV=production uv run cobalt db migrate --allow-prod --rollback --down-to 0007`)-] exist and are the DESK's documented path with Dejan, run as its own approved job, never from inside a failing deploy. PROVE the schema is harmless where it stands: `validate` exit 0 and the sheet 200 after (4). Likewise **no settings row is touched** — cards stay LIVE through every branch of this section.
(4) Bring the residents up with 5.2's commands and 5.2's single retry / single `kickstart` rule ONLY; a failure here is terminal (`residents: DOWN`), never a second pass through §8.
(5) Re-run 6.1's curl / heartbeat / validate lines and 6.4's cards read → quote them.
(6) `ESCALATE` with the snapshot id, the tag [-`pre-p4-0919`,-]{+`pre-d3-0919`,+} the reverted range, what stays applied [-(0008/0009), the state of `com.cobalt.replay`,-]{+(0010/0011),+} and the gate artifacts; last line `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/DOWN>`; commit the report. **Residents DOWN at the end is the one outcome the desk escalates by push at once.**
