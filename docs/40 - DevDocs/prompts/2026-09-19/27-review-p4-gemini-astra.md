MODEL: Sonnet 5 (`claude-sonnet-5`) — read, reuse the already-staged packet, launch two reviewers, verify their findings; no write path · SEAT: review hub `review-p4-0919b`, launched by the CTO desk in the background. NO NEW RULE: this launch line is `21-review-p4-build.md`'s launch line with exactly TWO strings changed — the prompt filename (`21-review-p4-build.md` → `27-review-p4-gemini-astra.md`) and the `--remote-control` name (`review-p4-0919` → `review-p4-0919b`). Every `--allowedTools` string, every flag, `--disallowedTools`, and every `--add-dir` are BYTE-IDENTICAL to `21`'s — including the now-unused `Bash(grok *)` rule (Grok already read this build round 1; the rule stays, unused, for the same reason `21` kept unused `s2-p2-cards` rules: it is not the string that changed). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/27-review-p4-gemini-astra.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-0919b --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per remaining house (L47 — probe before counting on a house). **No scheduling tool** (`CronCreate`/`ScheduleWakeup`) is named anywhere in this allowlist — confirmed by reading it above; this governs the Astra wait rule in §3 below.

# Continuation of the three-house check of `sprint-2/p4` (LAWS L67) — round 1B, GEMINI AND ASTRA ONLY. Grok already read this build round 1 and returned `VERDICT: BUILD CLEAN` (0 BLOCKER / 0 MAJOR / 0 MINOR) — see `scratch/review-p4-0919/REVIEW.md`'s `## Grok` section; do not re-read Grok, do not re-launch Grok, carry its verdict forward into your own close line only. This run owes the two checks that did not complete round 1: Gemini went HARNESS (`agy … --print` produced nothing: a tool requiring the "command" permission was auto-denied, headless mode cannot prompt for it), Astra went METER (Codex usage limit, reset per its own message 11:47 AM ET). Same branch, same tip (`a59ee8c`), same packet, same rules as `21` — nothing about the code under review has changed. DO NOT STOP until the report ends `P4 CHECK B …` or `FAILED …`.

**SCOPE NOTE:** the fund rule (R16, `Asset Type`/`not_equity` rule C) stopped at `FAILED: step 3` today and is being redesigned as a separate chunk — it is OUT OF SCOPE for this run, exactly as it was for round 1. Do not check it, do not wait for it.

AUTHORIZATION — VERIFY IT YOURSELF, same two proofs as `21` (unchanged since round 1 ran minutes ago): `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"` (L67 / R18–R21) and `git -C /Users/cobalt/cobalt log --oneline -4 sprint-2/p4 -- "docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md"` (branch ref named; `OWED:` line). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. Laws, one line each, same as `21`: L33 · L35 · L36 · L37 · L44 · L47 · L52 · L67 (≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out — Grok + one of {Gemini, Astra} already clears that floor; this run's job is to get as close to 3 as the meter allows).

REPORT: a NEW file, `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/REVIEW-B.md` — never edit `REVIEW.md` (round 1's report stays exactly as Grok/Gemini/Astra round 1 left it) and never edit anything else in the folder outside what §1 below permits. Shape: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet (reused)` → `## Astra` → `## Gemini` → `## Verdict table` → last line.

## 1. Packet — REUSE, do not restage. `mkdir -p scratch/review-p4-0919` first (idempotent; the directory already exists from round 1 — this only guards a relaunch where it does not; the rule string is byte-identical to `21`'s for exactly this reason). Then verify, never blind-trust: `ls scratch/review-p4-0919/` and `wc -c scratch/review-p4-0919/*` against `21`'s own SIZE TABLE (19 files, 863,573 bytes total — copy the table from `21-review-p4-build.md` verbatim into your own `## Packet (reused)` section rather than retyping it from memory). Only a file that is MISSING or whose byte count DIFFERS from that table is re-staged, Read → Write, byte-identical (source = `docs/40 - DevDocs/prompts/2026-09-19/21-packet/`). You are NOT staging fresh — you are confirming round 1's packet is still intact, since Gemini and Astra must read the SAME packet Grok read (L44). `grok-findings.md` (round 1's Grok output) may already be present in the folder from round 1 — leave it, it is not part of the 19-file table and not yours to touch.

## 2. Astra — PREFLIGHT probe, then the wait/skip rule
2.1 Probe, `run_in_background`, give it 3 minutes: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."` (rule: identical string to `21`'s). Record verbatim.
2.2 If the probe says UP (prints `OK`, exit 0, no usage-limit text): launch Astra now, ONE attempt, `run_in_background`: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "You are GPT-6 Astra, the cross-house reviewer. Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/ . Start with QUESTIONS.md and follow it exactly. Do NOT run any command other than reading those files and do NOT write any file: print your complete findings as your answer."` → write its printed answer, byte for byte, to `astra-findings-b.md`. A usage-limit message mid-answer → `astra: METER`, keep what it printed, marked partial. Then proceed to §3 (Gemini).
2.3 If the probe still says METER (usage limit, verbatim text same shape as round 1's): **this launch line carries no scheduling tool** (confirmed in the header above) — so do NOT wait-and-reprobe on a timer yourself. Instead: run Gemini first (§3), and only AFTER Gemini's attempt(s) finish, probe Astra ONE more time with the same bare command. If that second probe is UP, launch Astra as in 2.2. If it is STILL METER, close with `astra: METER` in the last line (the desk reruns Astra later — this is a correct, non-failing ending, never a third probe, never a loop, never a self-scheduled wait).

## 3. Gemini — reworded prompt, at most TWO attempts
Diagnosis carried into this run (from the hub's read of round 1's `REVIEW.md`, the archiver check's `REVIEW.md`, and the two working small-packet runs `scratch/review-ops-0919/` and `scratch/review-ops-r2-0919/`): the packets that HARNESSed today (`review-p4-0919`, 19 files / 863,573 bytes; `review-archiver-0919`, ~473,000 bytes across multiple `.partN` splits) were both far larger and more cross-referential (naming fixtures and specs deliberately EXCLUDED from the folder, to be pulled separately by the hub) than the packets that printed clean answers (`review-ops-0919` ≈148,000 bytes total, `review-ops-r2-0919` ≈148,000 bytes, `review-harness-0919` ≈75,000 bytes) — none of which named anything outside the folder. The outer `--print` wrapper already said "Do NOT run any command and do NOT write any file" in every one of today's runs (round 1 inherited it unchanged from `01-review-harness.md` §2.2); that instruction held on small packets and did not hold on the two large, cross-referential ones. Most likely cause: past some packet-size/complexity threshold, and specifically when the material tells the reader that other files exist but are NOT here, the model's own headless tool-orchestration layer reaches for an active tool (a shell command in this build's case, `read_url` in the archiver's case that same minute) instead of a plain sequential read — and that tool category is auto-denied in headless mode. This is a correlation from today's four runs, not a confirmed root cause (agy prints no reasoning trace) — **confidence: medium**. Smallest fix attempted here: strengthen and relocate the no-tools instruction so it sits directly beside every place the packet names something not present, and remove any open-ended "the files in this folder" phrasing in favor of an explicit list.

3.1 FIRST ATTEMPT, `run_in_background`: `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You have NO tools for this task except reading local files already present in one folder. Never run a command, never list a directory, never fetch a URL, never search the web — if answering a question would require any of those, write UNVERIFIABLE FROM PACKET for that item and continue; do not attempt the action first. Read ONLY these 19 files, one at a time, all in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-0919/: QUESTIONS.md, facts.md, spec-01-plan-2026-09-15.md, spec-06-verify-2026-09-19.md, build-report-1-build2-2026-09-18.md, build-report-2-verify-2026-09-19.md, code-01-radar.diff, code-02-replay-core.diff, code-03-replay-movers-runner-cards.diff, code-04-smoke.diff, code-05-cards-aset-settings-jobs-archiver-dayopen-misc.diff, code-06-migrations.diff, code-07-configs-ops.diff, code-08-tests-radar.diff, code-09-tests-replay-a.diff, code-10-tests-replay-b.diff, code-11-tests-smoke-tenancy-settings-finviz-jobs.diff, code-12-tests-cards-p4.diff, code-13-fixtures-cutter-script.diff. QUESTIONS.md and facts.md both mention six fixture files and three build prompts that are deliberately NOT in this folder: do not try to find, fetch, or reconstruct them by any means — treat every question that depends on them as UNVERIFIABLE FROM PACKET, exactly as QUESTIONS.md itself says. Start with QUESTIONS.md and follow it exactly. Do NOT write any file: print your complete findings as your answer."` → write its printed answer, byte for byte, to `gemini-findings-b.md`.
3.2 If 3.1 is HARNESS (any tool-permission denial, any "no output produced"): ONE more attempt, same rule (`Bash(agy *)`), SPLIT the read into two calls by area so each call's packet is roughly half the size: call A = QUESTIONS.md + facts.md + the two specs + the two build reports + code-01 through code-07 (radar/replay/smoke/cards/migrations/configs); call B = code-08 through code-13 (all the test-code diffs) plus QUESTIONS.md again so it can answer the test-focused questions (Q8, Q9) standing alone. Same "you have NO tools" opening sentence, same explicit file list (only the relevant half), same "print your complete findings, do NOT write any file." Write each printed answer to `gemini-findings-b-partA.md` / `gemini-findings-b-partB.md`, and say in `## Gemini` that you combined two prints into one house's findings for this round.
3.3 If 3.2 is ALSO HARNESS: record `gemini: HARNESS` (both attempts' verbatim text) and STOP — a third attempt is never made, on either wording or split.
3.4 If Astra was deferred to after Gemini (§2.3), re-probe Astra now per that rule.

## 4. Verify — using ONLY `21`'s rules, nothing new
Every BLOCKER and MAJOR from Astra and/or Gemini against the REAL files: a `code-*.diff` claim against `git -C /Users/cobalt/cobalt show sprint-2/p4:<path>` and against `/Users/cobalt/cobalt-wt/s2-p4/` with Read/`grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`) — identically to `21`'s §2–4. If a finding duplicates one Grok already raised in round 1 (check `REVIEW.md`'s `## Verdict table`), say so and do not re-litigate it — carry Grok's row forward by reference into your own table rather than re-verifying it. A claim needing a command not in this launch line → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it (same missing rules round 1 already named: `Bash(uv run pytest *)`, `Bash(uv run cobalt jobs restarts *)`). One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-p4-0919/`, and within it you only WRITE `REVIEW-B.md` plus your own new findings files (`astra-findings-b.md`, `gemini-findings-b.md` or the two `-partA`/`-partB` files) — `REVIEW.md`, `grok-findings.md`, and every `21-packet` source file are read-only to you.

## 5. Close
`## Verdict table` combines: Grok's round-1 verdict (carried forward, cited, not re-verified) + any REAL/NOT REAL/UNVERIFIABLE findings from this round's Astra and/or Gemini. Last line, beginning EXACTLY with `P4 CHECK B`:
`P4 CHECK B · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that have now read it: <n> of 3 (incl. Grok round 1: BUILD CLEAN) · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
