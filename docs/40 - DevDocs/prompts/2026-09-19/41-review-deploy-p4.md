MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify their findings; no write path · SEAT: review hub `review-deploy-p4-0919`, launched by the CTO desk in the background. NO NEW RULE: `18-review-ops-0919.md`'s `--allowedTools` list, copied byte for byte, with exactly THREE strings changed and nothing else — the prompt file name, the `--remote-control` name (`review-ops-0919` → `review-deploy-p4-0919`) and the `mkdir` path (`scratch/review-ops-0919` → `scratch/review-deploy-p4-0919`). This check needs no rule `18` did not already carry: the packet is files-only, read with `Read`/`grep`/`ls`/`wc`/`tail` inside the same `agy-trial` worktree, and every verification target (`38-deploy-p4.md`, `02-deploy-stack-3.md`, `deploy-2026-09-19.md`, the branch reports) sits under `--add-dir /Users/cobalt/cobalt` or `--add-dir /Users/cobalt/cobalt-wt`, which need no Bash rule for a plain read. The unused `git -C .../s2-p2-cards *` rules stay, unused, because they are not among the three strings that changed. If this check turns out to need something those rules cannot do, the missing rule is NAMED in the report, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/41-review-deploy-p4.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-deploy-p4-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-deploy-p4-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house). **ASTRA IS OUT: its Codex meter returned a usage limit at 12:26 ET, "retry after 4:54 PM".** Probe it ONCE as usual; a usage-limit message → record `astra: METER` and end with two houses — that is a normal ending, not a failure, and L67's floor (≥1 house other than the author) is met by either of the other two. Do NOT retry, do NOT wait for the reset.

# Three-house check of a DESK DEPLOY PROMPT before it runs and before Dejan approves it (09-18 R8; LAWS L67: "This includes the CTO desk's own deploy, rebase and re-land prompts" — R8's origin: three unreviewed desk prompts failed on 2026-09-18; the first reviewed one had five real defects found before it ran) — `38-deploy-p4.md`, the UNATTENDED Opus deploy prompt for **DEPLOY 2 of the weekend**: it merges `sprint-2/p4` into `/Users/cobalt/cobalt` (production), applies production migrations 0008/0009 to the live database, takes both resident services down and back up, bootstraps a new scheduled job, and carries the first-ever **L68 integrated pre-merge gate**. Nothing in it has run. **Would `38-deploy-p4.md` FAIL, or DO HARM, as written?** Round 1 of at most 3 (L67: ≤3 rounds per house while meter allows, never fewer than 2 houses total). DO NOT STOP until the report ends `DEPLOY P4 CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan; `38-deploy-p4.md` is NOT yet approved and this check is a precondition of asking him. This check is standing law, not a one-off approval: LAWS.md **L67**, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, with R19–R21's emergency / override / rounds clauses) and **09-18 R8** (14:29 ET, the second-opinion rule, the desk's own write-path prompts included); `cto-2026-09-18.md` §4 **R9** (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20); `cto-2026-09-19.md` §4 **R1** (06:46 ET, the Codex read-only rule). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. `ASK DESK: <question> [<time from date>]` in the report for a question that does not stop you — then continue with the safe default, never wait on an answer, never message anyone.

Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the desk's own `facts.md` claims — verify against the real files · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds, floor of one house other than the author.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT (incl. the Codex probe and "Astra down = continue with two houses, not a failed run"), its §2 launch spellings and its LESSON bind you here UNCHANGED, with `scratch/review-deploy-p4-0919/` in every path — **including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every file it needs is pre-staged, never asked of it by command.** `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` §3 = the verification rules and table shape. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-p4-0919/REVIEW.md` — the same gitignored-scratch pattern `01`, `14` and `18` use, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line. You commit nothing.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/41-packet/`; copy each into `scratch/review-deploy-p4-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `38-deploy-p4.md` — **THE FILE UNDER REVIEW** · (2) `facts.md` — the desk's own pre-computed measurements for `38` (its `38-packet/facts.md`, copied unchanged); QUESTIONS.md calls this a CLAIM, not evidence · (3) `approval-list.md` — the 14 rule strings that differ from the approved precedent, each with its class, its reason and the step that uses it, plus the strings DROPPED and the ones deliberately not added · (4) `laws.md` — L43, L54, L66, L68 verbatim from `LAWS.md`, with a closing note stating the open L68-scope question as open · (5) `archiver-db-reds.md` — the archiver branch's own with-DB evidence (its run-1 step-order proof, the four reds, its `## DB RUN ESCALATE`), the basis of `38`'s P15 precondition · (6) `02-deploy-stack-3.md` — the approved PRECEDENT prompt, whole · (7) `deploy-1-report.md` — `reports/deploy-2026-09-19.md`, what that precedent actually printed this morning, whole · (8) `QUESTIONS.md`, the same text every house receives (L44).

**BUCKETING — the eight files total 157,691 B, so no house gets them in one call.** Two calls per house; `38-deploy-p4.md` (52,318 B) and `QUESTIONS.md` (9,863 B) go in BOTH, because the subject and the question set must travel together:
- **Bucket A — the prompt and its own evidence, 98,480 B:** `38-deploy-p4.md` + `facts.md` (15,456) + `approval-list.md` (4,999) + `laws.md` (4,307) + `archiver-db-reds.md` (11,537) + `QUESTIONS.md`. Covers Q1–Q8 and Q10.
- **Bucket B — the prompt against its precedent, 121,392 B:** `38-deploy-p4.md` + `02-deploy-stack-3.md` (33,229) + `deploy-1-report.md` (25,982) + `QUESTIONS.md`. Covers Q9 and any Q1/Q3 point needing the precedent's own wording.
**Bucket B is 121,392 B — over 120,000 by 1,392.** Send it as one call first; if a house rejects it on size, or truncates it, split it into **B1** = `38-deploy-p4.md` + `02-deploy-stack-3.md` + `QUESTIONS.md` (95,410 B) and **B2** = `38-deploy-p4.md` + `deploy-1-report.md` + `QUESTIONS.md` (88,163 B), and say so in `## Packet`. **Never drop a file to make a bucket fit, and never silently let a call truncate** — a house that received a truncated packet did not read the packet (L44). Record every file's byte count in `## Packet`, confirm each staged copy's size equals its source's, and state which bucketing each house actually got.

QUESTIONS.md (verbatim): see `41-packet/QUESTIONS.md` — copy it byte-for-byte into `scratch/review-deploy-p4-0919/QUESTIONS.md` as part of staging step (8) above; it is the same text every house receives, in both buckets.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-deploy-p4-0919/` in place of their paths. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.

For every allowlist claim (Q1) quote the rule AND the step's command side by side — that is the finding class that cost the most on 09-18, and `38`'s §4.2 onwards runs with production's services DOWN, so a denied command there is not a retry, it is an outage. You verify each BLOCKER and MAJOR against the REAL files, not against the staged copies alone: `38-deploy-p4.md`, `02-deploy-stack-3.md` and `reports/deploy-2026-09-19.md` directly under `/Users/cobalt/cobalt` with Read and `grep` (no Bash rule needed for a plain read; `--add-dir /Users/cobalt/cobalt` covers it); `sprint-2/p4`'s content and its report under `/Users/cobalt/cobalt-wt/s2-p4/`; the archiver branch's report under `/Users/cobalt/cobalt-wt/archiver-append/`; anything on main with `git -C /Users/cobalt/cobalt show main:<path>` / `log*`. LAWS.md itself lives at `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` (`--add-dir /Users/cobalt/Vault`) — check `laws.md`'s four excerpts against it rather than trusting the copy.

A claim only a RUNNING command could settle — whether the stacked merge conflicts, whether the stacked suites go green, whether `launchctl bootstrap` is idempotent on this host, whether `radar evaluate --replay` writes with cards enabled, what `cobalt_dev` actually holds right now — is `UNVERIFIABLE FROM READS`: name the exact missing command, do not invent the answer and do not ask a reviewer to run it (none of you may). One extra column per REAL finding: `blocks the launch? yes/no — why`. A finding that would only change the prompt's TEXT (a wording fix, a clearer gate) is `blocks: no` unless the text is what would mislead the unattended session into a wrong action. You fix NOTHING and edit NOTHING outside `scratch/review-deploy-p4-0919/` — in particular you never edit `38-deploy-p4.md`; the desk folds the findings.

Last line, exactly this shape:
`DEPLOY P4 CHECK · grok: <RUN AS IS|RUN AFTER FIXES <n>|DO NOT RUN|HARNESS|METER|TIMEOUT> · gemini: <same shape> · astra: <same shape> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
