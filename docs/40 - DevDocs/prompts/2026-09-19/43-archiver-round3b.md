MODEL: Opus 5 (`claude-opus-5`) — same tier as rounds 1/2 and the DB run (this chunk edits the migrations that carve `cobalt_user`'s read boundary on two production system tables, L29 floor) · SEAT: archiver build hub `archiver-round3b-0919`, launched by the CTO desk in the background, SAME worktree as rounds 1/2/the DB run (it already exists — do NOT create it, do NOT `git worktree add`, do NOT rebase). NO NEW RULE: every rule below is copied byte for byte from `docs/40 - DevDocs/prompts/2026-09-19/26-archiver-round2.md`'s launch line — zero added, zero altered; dropping was considered and none were dropped, because this chunk's steps need exactly the same `Bash(uv run pytest *)`, `Bash(git add *)`/`commit`, `Bash(uv run cobalt jobs restarts *)` and read-only shell rules 26 already had, and nothing this chunk does needs a tool outside that set. THIS RUN IS OFFLINE BY DESIGN, same as rounds 1/2: `cobalt_dev` is not this session's to touch. The with-DB re-run that actually flips the two red tests green is a SEPARATE later step — the desk re-launches `35-archiver-db-run.md`'s shape, with the corrected apply → suite → roll back order the DB run's own report named (DB-3). Two bare commands, run by the desk:

```
cd /Users/cobalt/cobalt-wt/archiver-append
claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/43-archiver-round3b.md' and follow it exactly." --model claude-opus-5 --remote-control archiver-round3b-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt
```

SESSION / REMOTE-CONTROL: `archiver-round3b-0919` — fresh session, every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `cobalt_dev`, no `.env`, no vault write, no other seat's config · METER: Anthropic LARGE, shared with any other Opus seat running today; a usage-limit message is not a failure — record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

LAUNCH-LINE RULE PROOF (this session's rule strings vs 26's, sorted, `comm -3`): both sides are the 16 `Bash(...)` allow strings + `AskUserQuestion` + `EnterWorktree` + the three `--add-dir` values (21 strings each). `comm -3 <(sort 26-rules.txt) <(sort 40-rules.txt)` → **empty output** — the sets are identical, nothing added, nothing altered, nothing dropped. (Every rule this chunk's steps need — `git add` by explicit path, `git commit`, `uv run pytest`, `uv run cobalt jobs restarts`, `grep`/`Read` for the tests-first RED quotes — is already in 26's set; no command this prompt asks for falls outside it. If a step below needs a tool not in this list, name it in the report as an `## ESCALATE` line — do not invent the rule and do not run the command anyway.)

# Archiver append-only — ROUND 3b, drop the unobservable `requires_db` twin (Dejan's ruling, `cto-2026-09-19.md` §4 R26, "A", 13:21 ET). OFFLINE, one commit for the test deletion, one for the report. DO NOT STOP until the report ends `ARCHIVER R3B …` or `FAILED …`.

CONTEXT. Round 3 (`docs/40 - DevDocs/prompts/2026-09-19/40-archiver-round3.md`) closed with last line `ARCHIVER R3 193a2ca | offline 1871/0 (320 skipped...) | DB-1: REVOKE in 0010 + 0011 (offline pin green; 2 requires_db OWED to the DB re-run) | DB-2: not touched — ruling owed | ...`. DB-2 was `tests/cobalt/test_archiver_append_store.py::test_the_own_connection_upsert_survives_another_transactions_rollback` (currently at `:711-738`, decorated `@requires_db` at `:710`) — RED on the DB run because it asserts a COMMIT made by `upsert_bars`'s own connection survives a *different* transaction's rollback, and `tests/cobalt/conftest.py:133`'s autouse `dev_db_tx` fixture wraps every new-core test, including this one's "own connection", inside ONE savepoint-backed `cobalt_dev` transaction that the suite always rolls back — so the commit this test exists to prove can never actually land during a test run. Dejan ruled (R26, quoted verbatim): *"the `requires_db` test `test_archiver_append_store.py::test_the_own_connection_upsert_survives_another_transactions_rollback` is DROPPED (the dev harness's single-transaction fixture makes a real commit unobservable); the three offline pins of `upsert_bars`'s own connection + the only `DO UPDATE` stay. His ruling, so L45's companion ('never narrow the test') is not the desk's to weigh here; no committing lane is added to the harness."* This chunk executes that ruling and nothing else. DB-1 (the REVOKE, closed in round 3), R2-1 (shadow timezone), R3-1 (§11 "follows 0006's pattern" sentence), R3-2/R3-3 (sequence REVOKE, idempotency-owed) are CARRIED, NOT YOURS — do not re-litigate, carry them forward in `## ROUND 3B ESCALATE` by name, untouched.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval. `grep -c` each of the 16 `Bash(...)` rule strings above, plus `--disallowedTools "AskUserQuestion" "EnterWorktree"` and the three `--add-dir` values, against `docs/40 - DevDocs/prompts/2026-09-19/40-archiver-round3.md` — every one must count 1 (40's own launch-line-rule-proof did the identical check against 26; same shape, one file back). The WORK ITEM is authorized by `docs/40 - DevDocs/reports/cto-2026-09-19.md`, `## §4 Rulings`, row **R26** (13:21 ET, "A", quoted above in full — the ONLY ruling this chunk executes; it does not extend to DB-1's remaining owed items, R2-1, R3-1, R3-2, or R3-3) and its APPLIED note ("chunk `43` in draft, runs after `ARCHIVER R3`"). If that row is missing, reworded, or its ruling text does not match what is quoted above → `FAILED: authorization mismatch — <what>`, commit nothing, stop before PREFLIGHT. A rule here not in 40's committed line, or a citation that does not say what is quoted → same `FAILED` shape.

YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded scope: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Anything this prompt expected to exist and does not, or any number that does not match what a cited row or file says → `ESCALATE`, never silently reconciled, never asked about mid-run.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(archiver-r3b): <item> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit file paths under `tests/cobalt/test_archiver_append_store.py`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `data/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>`.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL → `cto-2026-09-19.md` §4 row R26 (THE ruling this chunk executes) → round 3's report section, `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`, headed `# ROUND 3 — REVOKE cobalt_user off the two archiver tables (cto-2026-09-19 R25 "B", DB-1)`, IN FULL, and its stop line (quoted above) — this is what PREFLIGHT checks is really closed → `tests/cobalt/conftest.py:133-175` (the autouse `dev_db_tx` fixture — the mechanism R26's ruling names) → `tests/cobalt/test_archiver_append_store.py:679-738` IN FULL (the F1 pair: `test_a_rollback_unwrites_an_upsert_made_on_the_targets_connection` at `:680`, which STAYS — it proves the connection-taking sibling `upsert_bars_on` unwrites on the CALLER's rollback, which the savepoint fixture CAN observe since it IS the caller's transaction; and the DROPPED test at `:711`) → `tests/cobalt/test_archiver_append_store.py:184-230` (candidate offline pins — read and confirm, do not take this prompt's naming on faith, per step 1: `test_upsert_bars_still_does_update_and_never_do_nothing` at `:189` asserts `upsert_bars` reaches the statement via `self._connect()`, i.e. its OWN connection; `test_only_one_copy_of_the_upsert_statement_exists` at `:213` pins there is exactly one `DO UPDATE` in the whole `BarStore`; `test_upsert_bars_on_takes_the_callers_connection_and_never_opens_one` at `:219` pins the sibling takes the CALLER's connection and never opens its own — together these three are the offline shape of the property the dropped test asserted on real Postgres). Laws, one line each: L1 fail loud — deleting a test is not a silent narrowing when a human ruled it, but the report still states the coverage that remains, honestly · L3 one path — no second copy of the deleted assertion re-appears elsewhere · L35 your claims are checked against your artifacts (quote the three pins' current PASS, quote the diff of what you deleted) · L45's companion ("never narrow the test") is Dejan's to weigh, and he weighed it (R26) — not yours to re-litigate · L46 one commit per item, tree clean before you stop · L48 evidence in the report file in the same turn · L54 you work in this worktree only · L60 recovery: partial work is wip-committed, never discarded · L62/L63 no questions mid-run, no dialogs; the report is your stop channel.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `psql`, any `uv run cobalt` other than `jobs restarts`, any `COBALT_ENV=…` command, `launchctl`, `claude`.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim): `date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form, quote it in full — branch must be `archiver/append-0919`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (record the tip) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file"; if it exists → `FAILED PREFLIGHT: .env present — this run must not see the dev database` · Read (not Bash) `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`'s LAST LINE and quote it verbatim — it MUST start `ARCHIVER R3 ` (round 3 closed clean); anything else, including a `CONTINUE:` or `FAILED:` line, → `FAILED PREFLIGHT: round 3 not closed` · `uv run pytest --co -q tests/cobalt/test_archiver_append_store.py` (collects cleanly — confirms the file is in the state round 3 left it, before you touch anything). Any denial → `FAILED PREFLIGHT: <rules>`.

## 1. Name and confirm the three offline pins — by node id, quoted, passing
Read `tests/cobalt/test_archiver_append_store.py:184-230` yourself (do not take the INDEX CARD's naming on faith). Name, by full pytest node id (`tests/cobalt/test_archiver_append_store.py::<name>`), the three offline (no `@requires_db`) tests that together pin the property R26's ruling says "stay": `upsert_bars`'s own-connection commit, and there being only one `DO UPDATE` statement in `BarStore`. Confirm each exists (quote the `def` line and, for the own-connection ones, the assertion line that names `self._connect()` / the single-copy count / the caller's-connection-never-its-own claim). Run `uv run pytest -q tests/cobalt/test_archiver_append_store.py -k "<the three names, OR'd>"` and quote the summary line — all three must be green. **If fewer than three such offline tests exist, or any of the three is red: `ESCALATE`, name what you found, and STOP — do not delete `test_the_own_connection_upsert_survives_another_transactions_rollback` without this proof in hand.** Last line `CONTINUE: step 2`.

## 2. Delete the one test, leave the citation
In `tests/cobalt/test_archiver_append_store.py`, delete `test_the_own_connection_upsert_survives_another_transactions_rollback` (`:710-738` as PREFLIGHT found it — confirm the exact span from the file, not from this prompt, before you cut). Check whether any helper, fixture, or constant in this file exists ONLY to serve this one test (read what it calls: `BarStore`, `bar()`, `datetime`/`UTC` — all shared elsewhere in the file per your own read; if you find something used nowhere else, delete it too and name it in the report; if nothing is exclusive to it, say so plainly rather than deleting something shared). In its place, leave a 2-line comment:
```
# Dropped 2026-09-19 (cto-2026-09-19.md R26): unobservable under
# conftest.py:133's autouse single-transaction fixture. Offline pins stay: <the three node ids from step 1>.
```
Commit: `test(archiver): drop the unobservable own-connection-commit twin (cto-2026-09-19 R26)`. Last line `CONTINUE: step 3`.

## 3. Offline suite — state the arithmetic
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 3's own close (`offline 1871/0 (320 skipped...)`, quoted in CONTEXT above): `passed` MUST be unchanged at **1871** (you deleted a test that only ever SKIPPED with `.env` absent — offline `passed` never counted it), `skipped` MUST drop by exactly **1**, to **319** (one fewer `requires_db` test exists to skip), `failed` MUST stay **0**. State this arithmetic explicitly — do not just paste the line, show the subtraction. A mismatch of any of the three numbers → `ESCALATE`, do not reconcile silently. Last line `CONTINUE: step 4`.

## 4. Close — append the report, stop
`uv run cobalt jobs restarts <round-3-tip>..HEAD` (the branch point for this chunk is round 3's own tip, `193a2ca` — confirm this is still HEAD's actual parent chain, not assumed) → the table VERBATIM; expect only `tests/cobalt/test_archiver_append_store.py` and `docs/40 - DevDocs/` among the changed paths; 0 UNCLASSIFIED · `git status --porcelain` empty · `git diff --stat 193a2ca HEAD -- src tests docs` listed, every path one this prompt named · append a new top-level section to `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md`, AFTER `# ROUND 3`'s existing last line, headed `# ROUND 3b — drop the unobservable requires_db twin (cto-2026-09-19 R26)`, containing: a `## §0 Headline` (≤5 lines), step 1's three node ids + PASS quote, step 2's diff (the deleted span + the 2-line comment, quoted), step 3's arithmetic, and a `## ROUND 3B ESCALATE` carrying forward EVERYTHING round 3's `## ROUND 3 ESCALATE` already had (all 6 items, untouched, by name) — DB-2 itself is now CLOSED by R26, say so in one line, do not carry it forward as "owed". Commit the report by explicit path: `docs(report): archiver append round 3b — <one line>`.

LAST LINE, exactly this shape:
`ARCHIVER R3B <sha> | offline <p>/<f> (<s> skipped) | DB-2: twin dropped per R26, 3 offline pins green: <ids> | ESCALATE: <n> | cobalt_dev: untouched`
or `FAILED: … | cobalt_dev: untouched`. Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or `cobalt_dev`.
