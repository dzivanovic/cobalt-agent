MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-0919b`, launched by the CTO desk in the background AFTER `review-0919a` has written its stop line (one review run at a time: the same three houses, the same meters). NO NEW RULE: the launch line is `01-review-harness.md`'s with one `mkdir` path changed — `Bash(grok *)` + `Bash(agy *)` = Dejan's R9 of 2026-09-18 (standing through 09-20); the Codex read-only rule = his R1 of 2026-09-19 06:46 ET; the rest are reads. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/04-review-deploy-delta.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-0919b --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-deploy-delta-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61, L62); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Tribunal check of the DEPLOYMENT (LAWS L67: a deployment is checked by at least one house other than its author, more when the meters allow) — the re-issued stacked-deploy prompt `02-deploy-stack-3.md` against `10-deploy-stack.md`, the version Grok and Gemini reviewed on 2026-09-18 (5 real findings, all folded). Yesterday's delta review failed on BOTH houses because the desk told them to diff the files themselves; this time the diff is PRE-COMPUTED and staged. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words, committed on main: `cto-2026-09-18.md` §4 **R9** and **R18–R21** (L67); `cto-2026-09-19.md` §4 **R1** (the Codex read-only rule, "prior list is approved now.", 06:46 ET). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP: `FAILED: <step> — <concern>` is always a correct ending. Laws: L33, L35, L36, L37, L44, L47, L67 — one line each as in `01-review-harness.md`, which you read first.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its UNATTENDED RULES, its PREFLIGHT (incl. the Codex probe and the rule "Astra down = continue with two houses, not a failed run"), its §2 launch spellings and its LESSON bind you here UNCHANGED, with `scratch/review-deploy-delta-0919/` in every path; `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` §3 = the verification rules and table shape; `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/REVIEW.md` = this morning's run (which houses answered, how long each took — reuse what worked, and carry its PREFLIGHT verdict on Astra: if the probe said METER less than two hours ago, do not spend a second probe, record `astra: METER (carried from review-0919a)`). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-delta-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → one section per house → `## Verdict table` → last line. You commit nothing.

## 1. Stage the packet in `scratch/review-deploy-delta-0919/` (`mkdir -p` first; Read → Write, byte-identical)
From `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/`: (1) `2026-09-18/10-deploy-stack.md` → `10-deploy-stack.md` (the REVIEWED version) · (2) `2026-09-19/02-deploy-stack-3.md` → `02-deploy-stack-3.md` (the version that will run) · (3) `2026-09-19/04-packet/deploy-10-vs-02.worddiff.txt` → same name — the desk's pre-computed word-level diff, `git diff --no-index --word-diff=plain 10 02`: `[-removed-]`, `{+added+}` · (4) `2026-09-19/04-packet/state.md` → same name — what already exists on the host (tags, backup directories, report files, branch position) · (5) `incident.md` — YOU write it, verbatim excerpts from `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-18.md` `# THIRD RUN`: its §0 Headline, the step 3.5 rows, its safe-state section and its ESCALATE list · (6) `snapshot-escalate.md` — the `## ESCALATE` section of `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/migrate-snapshot-fix-2026-09-18.md`, verbatim · (7) `QUESTIONS.md`, verbatim from the block below. CHECK before launching anyone: `grep -c "0918b" 02-deploy-stack-3.md` in the staged copy — record the number (the desk expects 0).

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — the diff you need is already here as a file. `10-deploy-stack.md` is an UNATTENDED production deploy prompt for a live trading-support system; two other houses reviewed it on 2026-09-18 and five real defects were fixed in it. It then ran and failed at its step 3.5 on a defect in the migration tool, rolling back cleanly (`incident.md`). The tool has since been fixed twice (streamed proof; REPEATABLE READ — `snapshot-escalate.md` is the builder's own list of what that change implies). `02-deploy-stack-3.md` is the RE-ISSUED prompt that will run today, a Saturday with no market session: new tag / rollback-directory / session / report-file names (`pre-stack-0919`, `deploy-stack-0919`, `deploy-2026-09-19.md`, tag `deploy-2026-09-19`), the branch `sprint-2/stack` instead of `sprint-2/cards`, a rewritten gate (step 1.2 and the proof in 3.0), a rewritten step 3.5 (the command now takes ≈2–3 minutes; a tool-timeout instruction; a stated finding about serialization failures), new authorization rows. `deploy-10-vs-02.worddiff.txt` shows EVERY difference between the two: `[-text-]` was removed, `{+text+}` was added. `state.md` lists what already exists on the host. For every finding give SEVERITY (BLOCKER = will fail mid-deploy or can leave production broken / MAJOR / MINOR), the STEP, the EVIDENCE (quote the line), and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) CONSISTENCY OF EVERY RENAME: the launch line's allowlist must contain, character for character, every command a step tells the session to run. For each renamed thing — the `merge --ff-only` branch, the two worktree `rebase` lines, the `mkdir -p` directory versus the directory step 0.3 writes and the two `settings load --from …rollback` lines, the `tag` names, the probe tag — is the spelling in the allowlist identical to the spelling in the step? One mismatch = a denied command with the services down. Is any command in a step missing from the allowlist altogether (look hard at step 1.2's and 3.0's new `diff --stat` and `rev-list --count` lines against the `git -C * …` rules)? (Q2) STALE STATE: using `state.md`, does the prompt ever try to create something that already exists (a tag, a directory, a report file) or assume something is absent that is present, or present that is absent (it names `prod-proof-only-3-2026-09-19.md` as a precondition — what happens if that file does not exist)? (Q3) THE GATE: step 1.2 and the proof in 3.0 now anchor on the code commit `a70e286` with report-only commits above it, and on a commit count recorded before the rebase. Can the rebase onto a main that has moved (many docs commits since the branch point) silently change or drop code and still pass these two checks? Is `diff --stat a70e286 HEAD -- src tests configs ops .gitignore` a sound byte-identity proof across a rebase? (Q4) STEP 3.5 as rewritten: is the tool-timeout instruction sufficient; is the described behaviour on a client-side timeout right (transaction rolled back, nothing applied); what if the timeout or a dropped connection hits AFTER the server committed but before the client prints — does §6 then do the wrong thing? Is 'do NOT rerun the command' after a serialization failure the right instruction? (Q5) SATURDAY: anything in the prompt that assumes a trading day or an open market session and breaks or gives a false red today (the heartbeat gate P8, the smoke's scan checks, the clock rule around 20:00–21:00, the replay date)? (Q6) THE SAFE-STATE RULE §6: with the renames applied, does every path in it still end with code and settings agreeing and the services UP? (Q7) Anything else that changed for the worse between the two files. End with exactly one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <finding numbers> / DO NOT RUN`."

## 2. Launch the reviewers — `01-review-harness.md` §2 EXACTLY (2.1 Grok, 2.2 Gemini, 2.3 Astra only if UP, 2.4), with `scratch/review-deploy-delta-0919/` in every path. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.
## 3. Verify — `11-review-deploy-stack.md` §3 EXACTLY, against the REAL files (the two prompts on main; `sprint-2/stack` in `/Users/cobalt/cobalt-wt/s2-p2-cards`; for an allowlist claim, quote the rule AND the step's command side by side). One extra column per REAL finding: `blocks today's deploy? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-deploy-delta-0919/`.
## 4. Close
Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks the deploy: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
