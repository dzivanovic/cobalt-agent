MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify; no write path · SEAT: review hub `review-0919c`, launched by the CTO desk in the background, in parallel with `review-0919b` (different packet folder). NO NEW RULE: `01-review-harness.md`'s allowlist with one `mkdir` path changed (R9 of 2026-09-18; R1 of 2026-09-19). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/06-review-harness-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-0919c --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-harness-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house.

# Tribunal check of the BUILD, ROUND 2 (LAWS L67: changed code is checked before it ships; ≤3 rounds) — ONLY the fold of round 1: commits `fb7911b` (red tests) + `ad9b07c` (fix) on `sprint-2/stack`. Small on purpose. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words on main: `cto-2026-09-18.md` §4 R9, R18–R21; `cto-2026-09-19.md` §4 R1 (the Codex read-only rule) and R2 ("your call was right." — fold before the deploy). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED:` line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT (Codex probe: if `scratch/review-harness-0919/REVIEW.md` shows Astra answered today, skip the probe and record `astra: UP (carried)`), LESSON, §2 launch spellings, §3 verification and §4 last-line shape bind you UNCHANGED, with `scratch/review-harness-r2-0919/` in every path. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-r2-0919/REVIEW.md`.

## 1. Packet in `scratch/review-harness-r2-0919/` (`mkdir -p` first; output → Write, byte-identical)
(1) `r2-tests.diff` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show fb7911b` · (2) `r2-fix.diff` = `… show ad9b07c` · (3) `cli.py` = `… show sprint-2/stack:src/cobalt/db_migrations/cli.py` · (4) `round1-verdicts.md` = the `## Verdict table` section and the experiments of `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/REVIEW.md`, verbatim · (5) `build-report.md` = `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/harness-round2-2026-09-19.md`, whole · (6) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three. Read ONLY the files in this folder; you cannot run commands. This is ROUND 2 of a review of a database-migration tool that runs TODAY against a live production Postgres 16 database with two services stopped. `round1-verdicts.md` is what round 1 found (R1, R2, R3, U1). `r2-tests.diff` and `r2-fix.diff` are the ONLY code changes since; `cli.py` is the module as it ships; `build-report.md` is the builder's account — treat its claims as claims. Give SEVERITY (BLOCKER / MAJOR / MINOR), FILE + LINE, EVIDENCE, FAILING SCENARIO; say 'none found' rather than inventing; do not re-file round-1 findings that are fixed. (Q1) R1: does `_schema_of` now raise on more than one match, name the table and every schema, and behave exactly as before for one match and for none? Can the new raise fire wrongly on a healthy database (views, partitions, temp tables, case)? It raises INSIDE the transaction — is everything rolled back and the exit non-zero? (Q2) R3: the builder says no database-state assertion can prove the explicit rollback is load-bearing, because Postgres aborts the transaction and `finally: conn.close()` discards it anyway (its ESCALATE 1). Is that right? Is the strengthened test (e) still worth having, and is anything UNSAFE left by accepting it as shipped? (Q3) U1 tests (f) and (g): do they exercise the real statement from `0003`, with a genuinely separate committed session in (f) and a genuinely open transaction in (g)? Can (g) hang the suite or pass by luck of timing? Do they leave the dev database changed? (Q4) the two MINOR folds (explicit `read_only = False`; docstring wording): any behaviour change hiding in them? (Q5) did this diff change ANYTHING outside what round 1 asked for — the digest, the probe order, the isolation setup, the apply path? (Q6) anything in `r2-fix.diff` you would not run against production today. End with exactly one line: `VERDICT: SAFE TO DEPLOY / FIX FIRST <finding numbers> / DO NOT DEPLOY`."

## 2–4. Launch, verify, close — `01-review-harness.md` §2, §3, §4 EXACTLY, with this folder's path. One attempt per house. You fix NOTHING.
