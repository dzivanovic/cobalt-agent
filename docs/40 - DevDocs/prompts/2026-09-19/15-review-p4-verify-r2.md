MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify; no write path · SEAT: review hub `review-p4-verify-r2-0919`, launched by the CTO desk in the background. NO NEW RULE: `01-review-harness.md`'s allowlist with one `mkdir` path changed (R9 of 2026-09-18; R1 of 2026-09-19). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/15-review-p4-verify-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-verify-r2-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-verify-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house, the packet is the FOLD only.

# Second-house check of a DESK PROMPT, ROUND 2 of at most 3 — `13-p4-verify.md` after the desk folded round 1's fifteen REAL findings (text only; the launch line is byte-identical, proven by the desk with `diff` on line 1 and on the sorted rule strings). Read ONLY the fold. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `318239db`, 2026-09-19), not by Dejan. His words on main: `cto-2026-09-18.md` §4 R8 (14:29 — the second-opinion rule, the desk's own deploy / rebase / re-land prompts included), R9 (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20), R18–R21 (L67); `cto-2026-09-19.md` §4 R1 (the Codex read-only rule). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED:` line; `ASK DESK: <question> [<time>]` in the report for a question that does not stop you — then continue with the safe default, never wait on an answer.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — laws, UNATTENDED RULES, PREFLIGHT (Astra answered round 1 at 09:0x ET today → `astra: UP (carried)`, no probe; a usage-limit message at launch → `astra: METER`, continue with two houses), LESSON, §2 launch spellings, and `2026-09-18/11-review-deploy-stack.md` §3 bind you UNCHANGED, with `scratch/review-p4-verify-r2-0919/` in every path. `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-verify-0919/REVIEW.md` = round 1 (your predecessor's Verdict table). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-verify-r2-0919/REVIEW.md`.

## 1. Packet in `scratch/review-p4-verify-r2-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `13-p4-verify.md` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md` (as it is NOW, after the fold) · (2) `fold-r1.worddiff.txt` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/14-packet/fold-r1.worddiff.txt` — the desk's pre-computed word-level diff, before the fold → after: `[-removed-]`, `{+added+}` · (3) `round1-verdicts.md` = the `## Verdict table` section of `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-verify-0919/REVIEW.md` (round 1) through its `Q-by-Q answers` paragraph, verbatim · (4) `builder-shape.md` = step 2 (`## 2. Builder launches …` up to `## 3.`) of `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md`, verbatim — the builder launch shape the fold now binds · (5) `transport.md` = from `/Users/cobalt/cobalt/src/cobalt/archiver/collector.py` (main's copy — the fetch script runs from the production checkout, so THIS is the code it imports): the `_AUTH_RE` line, and the functions `scrub`, `resolve_token` and `finviz_get` whole, with their line numbers, and the file's import block — the transport the fold tells the fetch script to use · (6) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands. This is ROUND 2 of at most 3. `13-p4-verify.md` is an UNATTENDED prompt a hub will follow with nobody watching; it must never harm production and never let a credential reach a log, a report, a tool result or a commit. `round1-verdicts.md` is what round 1 found and a verifier confirmed against the real code: fifteen REAL findings (one blocking: R1, the token), six NOT REAL (R4, R5a, R6, R9, R17, R15b — do not re-file them unless you can quote a line the verifier misread), two UNVERIFIABLE. `fold-r1.worddiff.txt` shows EVERY change made since: `[-text-]` removed, `{+text+}` added; nothing else changed and the launch line is byte-identical. For every finding give SEVERITY (BLOCKER = the run will fail, or can damage the branch, the dev database, production, or leak a credential / MAJOR / MINOR), the STEP, the EVIDENCE (quote the line), the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) R1, THE TOKEN: read `transport.md`. The script obtains the token with `resolve_token()` and hands it to `finviz_get(path, params, token, on_metrics=…)` — so the script HOLDS the token in a variable: which sentences of the fold protect that variable, and is any missing? Does `finviz_get` as quoted take the parameters step 2.1 needs (`v`, `c`, `o`, no `f=`)? The transport raises `CollectorError(... scrub(str(e))) from e` — the chained httpx error is unscrubbed: does the fold's handler (type name only, no traceback, never `e.__cause__`) close that? With the fold's rules (no `raise_for_status`, one `try/except` printing only the exception's type name, no logger, handlers removed, the retry inside the script, files written only after both responses validate, the hub's line-by-line check, the `output withheld` ending), name ANY path left by which the token reaches the tool result, the report, a log file or a commit. Is `logger.remove()` enough if the transport logs through the standard library instead of loguru? Does `scrub` as quoted cover the `auth=` parameter? (Q2) For each of R2, R3, R5b, R7, R10, R11, R12, R13, R14, R15, R16, R18, R20: does the fold fix exactly what the finding names? Quote the new sentence; say FIXED / PARTLY / NOT. (Q3) Did the fold BREAK anything: every command the new text tells the hub to type (`git show --stat HEAD`, `git diff main HEAD -- <three files>`, `COBALT_ENV=dev uv run pytest -q <that file>`, the `rm` + `ls` pairs on the failed paths) against the launch line's rules, character for character; the builder launched WITHOUT `cd … &&` — does `builder-shape.md`'s command still work as one bare command from the worktree; 'no builder runs while `.env` is in the worktree' against the step order (is there any step between 4.1 and 4.8 that still launches a builder; PREFLIGHT's `claude -p` probe comes after the `.env` probe's `rm` — confirm); the K3 test now written in step 3 but needing a database — can its builder prove 'fails before, passes after' offline, and if not, does the prompt's tests-first rule make that builder stop; the sample floor (10 distinct fund tickers, 2 distinct values) — too high for two whole-market exports plus a week of stock screens, too low, or about right, and does the mechanical order (i)(ii)(iii) leave any case undecided. (Q4) Any contradiction the fold introduced between two sentences of the prompt (RECOVERY RULE vs the rebase exception; 'a denial is FAILED' vs 4.5's and 4.7's 'ESCALATE, continue'; the stop line's new `cobalt_dev:` field vs the FAILED endings, which have no such field). End with exactly one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <finding numbers> / DO NOT RUN`."

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4 and `11-review-deploy-stack.md` §3 EXACTLY, with this folder's path; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: `13-p4-verify.md` on main, the code under `/Users/cobalt/cobalt-wt/s2-p4/` and `/Users/cobalt/cobalt/src/` with Read and `grep` (you have no git rule for the `s2-p4` worktree; a claim that only a git command there could settle = `UNVERIFIABLE FROM READS` with the exact command). One extra column per REAL finding: `blocks the launch? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-p4-verify-r2-0919/`. Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · round-1 findings FIXED: <f> of 13 · new REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
