MODEL: Opus 5 (`claude-opus-5`) — the session builds itself, no headless builders (item b is on the day-mode path, item c on the migration harness: Opus floor, L29) · SEAT: ops hub `ops-0919`, launched by the CTO desk in the background. NO NEW RULE: every rule below is copied byte for byte from the launch line Dejan approved 2026-09-18 06:46 ET (`cto-2026-09-18.md` §4 R1 list (2)) as committed in `prompts/2026-09-18/02-ops-2026-09-18.md` — same job shape (dated ops worktree off main, Opus), and this line DROPS every rule that run had beyond editing, testing and committing inside its own worktree: the `.env` copy and removal, every `COBALT_ENV=dev` command, every `COBALT_ENV=production` read, `launchctl`, `curl`, `chmod`, `ops/cto-desk.sh`, `shasum`, `git -C … tag --list`. THIS RUN IS OFFLINE BY DESIGN: `cobalt_dev` belongs to `p4-verify-0919` today. Three bare commands, run by the desk: `git -C /Users/cobalt/cobalt worktree add -b ops/2026-09-19 /Users/cobalt/cobalt-wt/ops-2026-09-19 main` then `cd /Users/cobalt/cobalt-wt/ops-2026-09-19` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/16-ops-2026-09-19.md' and follow it exactly." --model claude-opus-5 --remote-control ops-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — every launch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `cobalt_dev`, no `.env`, no vault write, no other seat's config · METER: Anthropic medium, and the Opus meter is SHARED today with `p4-verify-0919`'s headless builders — a usage-limit message is not a failure: record the reset time, commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches; you have no `CronCreate` rule); Codex = NONE; Grok = NONE · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# Ops 2026-09-19 — five small defect fixes, each already named in a desk report, tests FIRST, OFFLINE: (a) day-open C2 trading-day guard · (b1) the trade-count band refuses an inverted or non-integer band · (c) `lock_timeout` on the migrate transaction · (d) test hygiene A1/A2 · (e) the migrate output names the code it ran from. LAW STEP of this work item (L67): BUILD. After it, none of it yours: at least three houses check the build · the `requires_db` tests you write get their first real run on `cobalt_dev` once `p4-verify-0919` has released it · the branch rides the next deploy prompt, with his approval. You never merge, never deploy, never push, never touch production, `cobalt_dev` or a `.env`. DO NOT STOP until the report ends `OPS 0919 …`, `CONTINUE: … — meter …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable `318239db`, 2026-09-19), not by Dejan; a prompt file is not an approval. His words, with times, are committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4 **R1** (06:46 "Approved" — list (2), the ops hub's launch line as committed in `prompts/2026-09-18/02-ops-2026-09-18.md`); `cto-2026-09-17.md` §4 **R5** (the unattended standard); `cto-2026-09-19.md` §4 **R3** + **R6** (no idle lane, never a skipped law step) and **R5** (the law is followed, not re-asked). This launch line adds NO rule: `grep -c` each rule string above in `prompts/2026-09-18/02-ops-2026-09-18.md` — every one must count 1. Prove the rows are committed: `git -C /Users/cobalt/cobalt log --oneline -8 -- "docs/40 - DevDocs/reports/"`. Laws behind it: L61, L62, L29, L55, L36 (you launch nothing). A rule here that is not in that committed line, or a row that does not say what is quoted → `FAILED: authorization mismatch — <what>`, commit the report, stop.
YOU CAN ALWAYS STOP. Only the dialog is removed, because nobody sees a `--bg` dialog. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. The desk watches that line. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step (where none is named: do not build that item), continue; never block on an answer, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(ops-0919): <item> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` in this run names explicit file paths under `src/`, `tests/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory (`scratch/` is not gitignored; today's lesson from the P4 prompt check, R11). After every commit: `git show --stat HEAD` lists only paths you named, else `FAILED: <step> — unexpected path committed <path>` (you have no reset rule; the desk repairs it).

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you build on a write path and on the day-mode path, L59) → **`/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/16-packet/facts.md`** (THE SPEC: per item its origin, the code lines the desk read, the BOUND of the change, the tests that must fail first — binding; where your reading of the code differs from it, the CODE wins and the difference is an ESCALATE line) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-19.md` §0, §5a, §8, §14, §15 and `cto-2026-09-18.md` §18 row F2 (the origins, in the desk's words) → `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/prod-proof-only-3-2026-09-19.md` `## ESCALATE` 2–3. Laws, one line each: L1 fail loud, never a plausible value · L3 one path — no second validator, no third git helper · L10 config validated on load, caught by `validate`, not at 09:00 · L7 + CLAUDE.md HITL: item b sits on the day-mode path — you change NO fact, NO step-down, NO settings row; a change that would alter an outcome for a VALID band is not yours to build · L29 Opus floor on the migration harness · L35 artifact, not claim · L42 RESTARTS derived by the command, never by judgement · L45 real-shape fixtures only · L46 one commit per item, tree clean before you stop · L48 evidence in the report file in the same turn · L52 nothing here touches scoring, ranking or what reaches a card — if an item turns out to, STOP it and ESCALATE · L54 you work in this worktree only; the desk's deploy merges · L57 every printed value replayable. Report (L48): `docs/40 - DevDocs/reports/ops-2026-09-19.md` in THIS worktree — §0 Headline ≤5 lines (line 1 `DO NOT STOP until OPS 0919`) → `## AUTHORIZATION` → `## PREFLIGHT` → `## BASELINE` → one section per item (red-first evidence, diff stat, suite line) → `## ESCALATE` → last line = `CONTINUE:` / stop line / `FAILED`. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. Counting is done by you from the tool result, never with a pipe. NOT in your list, so never typed: `cp`, `rm`, `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `uv run cobalt` anything except `jobs restarts`, any `COBALT_ENV=…` command, `launchctl`, `claude`. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry a denied command in another shape, never ask another session to run it.
PREFLIGHT (first two minutes; one row each: rule · command · exit · allowed/DENIED + reason verbatim; every probe sits inside its own rule's pattern): `date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; the branch must be `ops/2026-09-19`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: main's tip at the moment the desk created the worktree; a resume: the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it) · `ls -la .env` → MUST say "No such file" — this run is offline BECAUSE no `.env` exists in this worktree (09-18's lesson: a suite that finds a `.env` reads `cobalt_dev`); if it exists → `FAILED PREFLIGHT: .env present — this run must not see the dev database` (you have no `rm` rule, and must not want one) · `uv run pytest --co -q tests/cobalt/test_dayopen_checks.py` · `uv run cobalt jobs restarts main..HEAD` (empty range on a first launch — the probe of the rule). Any denial → `FAILED PREFLIGHT: <rules>`.

## 0. BASELINE (before any edit)
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line: `<p> passed, <s> skipped, 0 failed`. This is the number every later suite line is compared with: passed only grows by the tests you add, skipped only grows by the `requires_db` tests you add, failed stays 0. A red BASELINE is not yours to fix: name the tests, `FAILED: step 0 — baseline red <n>`, stop. Also `uv run pytest -q tests/cobalt/test_migrate_proof.py` alone → its own `<p> passed, <s> skipped` (item d's comparison). Last line `CONTINUE: step 1`.

## 1. Item a — day-open C2 gets the trading-day guard (`facts.md` §a)
Tests FIRST in `tests/cobalt/test_dayopen_checks.py` (run them, quote the RED): a non-trading day with zero `radar_membership` rows is `Verdict.PASS` with detail `PASS — no session today (<date> is not a trading day)` — for `SUNDAY` (the file's constant) and for Saturday `date(2026, 9, 19)`, the real false alarm; the raw block is still produced. `test_c2_fail_zero_rows` (a MONDAY) is NOT edited and stays green: a trading day with zero rows is still FAIL. Then the change, in `src/cobalt/dayopen/checks.py` `check_c2_radar_membership` ONLY: the calendar read C3 already uses (`clock.windows_for(report_date)` empty = not a trading day) moves ahead of the `count == 0` FAIL. Do NOT touch `src/cobalt/dayopen/models.py` (the unmerged P4 branch changes it). NOT in the bound: a trading day BEFORE premarket open — if you think it matters, one ESCALATE line, nothing built. DevDoc `docs/40 - DevDocs/cobalt/dayopen/checks.md`: C2's paragraph says what a non-trading day returns. Verify: the two new tests green, the file's other tests unchanged, full offline suite vs BASELINE. Commit `fix(dayopen): C2 passes on a non-trading day — no session, no rows, no false AMBER (09-19 Saturday day-open)`. Last line `CONTINUE: step 2`.

## 2. Item b1 — the trade-count band refuses what cannot be a band (`facts.md` §b). THE DAY-MODE PATH: no fact changes.
Tests FIRST in `tests/cobalt/test_daymode.py`: REFUSED loudly, naming both keys and both values — `(6, 2)`, `(2, "6")`, `(2.5, 6)`, `(-1, 6)`, `(True, 6)`; ACCEPTED with `day_facts` output BYTE-IDENTICAL to today's for the same inputs — `(2, 6)`, `(3, 3)`, `(None, None)`, `(2, None)`, `(None, 6)` (one parametrized test that computes the facts dict on the untouched code path first and pins it; the last three still produce `trade_count_band_placeholder: True`). `test_a_day_with_7_trades_is_adverse` is not edited. Then ONE validation function in `cobalt.daymode` beside `BAND_MIN_KEY`/`BAND_MAX_KEY` (`propose.py:85-86`), called from BOTH readers of those two rows — `daymode/cli.py` `_band()` (`:52-66`) and `src/cobalt/cli.py` `:214-221` (`cobalt validate`, which today prints the band and checks nothing; on a refusal it prints `FAILED: …` and exits 1 like its neighbours) — never two copies (L3). SCOPED OUT, DO NOT BUILD (b2): "a HALF-SET band fails loud". Today a null is deliberately an adverse SIGNAL, not a crash; making it a crash changes what `com.cobalt.daymode-propose` does at 09:00. That is a design call for the desk — your function's docstring states that exactly one null is still "not ruled yet", and a test pins it. If you find you cannot validate without changing a fact for some VALID band → STOP this item, ESCALATE, continue with step 3. You do NOT run `cobalt validate` (no rule; whether it needs a database is not verified) — its new branch is proven by a unit test that calls the function the command calls. DevDocs `docs/40 - DevDocs/cobalt/daymode/cli.md` + `propose.md`. Commit `fix(daymode): the trade-count band refuses an inverted or non-integer band — one validator for propose and validate (09-18 review F2)`. Last line `CONTINUE: step 3`.

## 3. Item c — `lock_timeout` on the migrate transaction (`facts.md` §c)
Tests FIRST in `tests/cobalt/test_migrate_proof.py`, OFFLINE with the file's own fakes (`_RecordingConn`, `_RecordingConnection`): a forward run's recorded statements contain `SET LOCAL lock_timeout` BEFORE the first migration file's text; a rollback run the same; `--proof-only` sends NO `lock_timeout`; a fake `_apply` raising `psycopg.errors.LockNotAvailable` → `rollback()` called, `MigrationError` whose text says NOTHING WAS APPLIED and names the timeout, exit 1 at the CLI boundary (the shape of `test_the_cli_turns_a_migration_error_into_failed_and_exit_1`); `--lock-timeout-s 0` and a negative value are refused BEFORE any connection is opened. PLUS one `requires_db` test, WRITTEN and NOT RUN today (it must SKIP cleanly offline, never error): another session holds `ACCESS EXCLUSIVE` on `system.cobalt_jobs`, migrate with `--lock-timeout-s 1` → FAILED in about a second, the table's proof unchanged. Then the change in `src/cobalt/db_migrations/cli.py`: module constant `DEFAULT_LOCK_TIMEOUT_S = 30`, flag `--lock-timeout-s <int>`, `SET LOCAL lock_timeout` issued inside the READ-WRITE migrate transaction (forward and rollback) before `_apply`'s first statement, `LockNotAvailable` wrapped exactly as `SerializationFailure` already is (`:563-577`). THE SNAPSHOT: the BEFORE probe and the migration must still see ONE REPEATABLE READ snapshot — state in the docstring, citing the Postgres manual's wording, whether `SET LOCAL` takes the snapshot; if you cannot cite it, the `SET LOCAL` goes AFTER `_probe_all`'s BEFORE pass and before `_apply`, where it cannot matter. `--proof-only` is untouched. Do NOT touch the `DIGEST_EXCLUDED_COLUMNS` tuple (`≈:52-57`) — the unmerged P4 branch adds two entries there and nothing else in this file. DevDoc `docs/40 - DevDocs/cobalt/db_migrations/cli.md`: the flag, the default and why 30 s (residents are down during a deploy, an honest lock wait is sub-second; the test suite's own ceiling is 20 s), and the operator's next step on a timeout. Under `## ESCALATE`, for the deploy prompt, ALWAYS: "a lock timeout at deploy = `FAILED` at the migrate step with nothing applied → the deploy prompt's rollback section; the desk names it there". Commit `feat(db-migrate): lock_timeout on the migrate transaction — a DDL that cannot get its lock fails in 30 s instead of hanging the deploy (proof-only-3 ESCALATE 3)`. Last line `CONTINUE: step 4`.

## 4. Item d — test hygiene A1 / A2 (`facts.md` §d) — `tests/cobalt/test_migrate_proof.py` ONLY, no production code
A1, test (g) `test_the_alter_waits_for_an_open_transaction_and_then_completes`: the worker connection gets a server-side ceiling (`SET statement_timeout`, a little above `LOCK_CEILING_S`) so a hung ALTER ends itself; on the hung path the test cancels (`conn.cancel()` — psycopg's documented cross-thread call), joins again, closes, and STILL fails with the "did not finish" message — no connection is left waiting for ACCESS EXCLUSIVE on `cobalt_dev` after the test. With step 3 in place, say in a comment how the 30 s `lock_timeout` of `cli._connect`'s transaction relates to this test's 20 s ceiling (which fires first, and that the test's own message stays the one a reader sees). A2, `_wait_for_a_blocked_lock`: it takes the worker's backend pid (`SELECT pg_backend_pid()` on the worker connection, read BEFORE the thread starts) and counts only `l.pid = <that pid> AND l.mode = 'AccessExclusiveLock' AND NOT l.granted`; every caller passes the pid. Read test (f) (`:1171`) first: change it only if it shares the helper. These tests are `requires_db`: there is no red-first offline — your proof today is that `uv run pytest -q tests/cobalt/test_migrate_proof.py` shows BASELINE's passed count plus step 3's new offline tests, the skips plus the ones you added, 0 errors, 0 failures (a collection error is a failure). Their real run is OWED — say so in the stop line. Commit `test(db-migrate): the lock-wait test cannot leave a waiting backend behind; the blocked-lock probe looks at the worker's own ACCESS EXCLUSIVE request (Astra A1, A2)`. Last line `CONTINUE: step 5`.

## 5. Item e — the migrate output names the code it ran from (`facts.md` §e)
Tests FIRST (offline, `monkeypatch` the helper): `--proof-only` output's LAST line is `code: <short sha> (clean) · <repo root>`; a dirty tree reads `(DIRTY: 2 path(s))`; a helper that raises → `code: UNKNOWN — <reason>` and `--proof-only` still exits 0; the forward path prints the same line after its proof table. Then the change in `src/cobalt/db_migrations/cli.py`: the repo root is the checkout the running `cobalt` package was imported from (never the current directory — a deploy hub's `cd` must not change the answer); the git call goes through an EXISTING helper by import — `src/cobalt/jobs/restarts.py:58` `_git` or `src/cobalt/generated/committer.py:60` `_git` — NEVER a third copy (L3); if neither imports without a cycle or a side effect → STOP this item, ESCALATE the exact import error, build nothing of it. A git failure NEVER fails a migration or a proof: `code: UNKNOWN — …` is explicit, not a plausible value (L1); refusing an UNKNOWN or DIRTY tip is the DEPLOY GATE's job, in the desk's prompt. DevDoc `db_migrations/cli.md`: the line and who reads it. Under `## ESCALATE`, for the desk, ALWAYS: "proof and deploy prompts: the stop line quotes the `code:` line, and the deploy gate compares it with the branch tip it was approved for". Commit `feat(db-migrate): the proof names the code it ran from — code: <sha> (clean|DIRTY) as the last output line (09-19 deploy check, known limit)`. Last line `CONTINUE: step 6`.

## 6. Close (offline)
`uv run pytest -q tests/cobalt tests/taxonomy` → quote the line; against BASELINE: failed 0, passed = BASELINE + the tests you added (list them by name with their step), skipped = BASELINE + the `requires_db` tests you added (list them) · the DevDocs symbol check and the leak test `test_screen_filter_values_live_only_in_approved_radar_fixtures` are in that suite — name their lines · `uv run cobalt jobs restarts main..HEAD` → the table VERBATIM; UNCLASSIFIED → ESCALATE; the `RESTARTS:` line is what the command derives, never your judgement (L42) · `git log --oneline main..HEAD` (one commit per built item + the report; list them) · `git diff --stat main HEAD` → every path is one this prompt names (`src/cobalt/dayopen/checks.py`, `src/cobalt/daymode/*`, `src/cobalt/cli.py`, `src/cobalt/db_migrations/cli.py`, the three test files, the DevDocs, your report) — any other path → ESCALATE with why · `git status --porcelain` empty · report committed by explicit path (`docs(report): ops 0919 — <one line>`). `## ESCALATE` carries, besides what you found, ALWAYS and untouched: (i) b2 half-set band = DESIGN, the desk's call; (ii) item f, disarming `com.cobalt.archiver`, is NOT a build — it is two new `launchctl` rules in the next WEEKDAY deploy's approval list (`facts.md` §f); (iii) the `requires_db` tests of steps 3 and 4 have NEVER run — their first run on `cobalt_dev` is owed before this branch may deploy; (iv) cross-branch: `sprint-2/p4` also changes `db_migrations/cli.py` (the excluded-columns tuple) and `dayopen/models.py` — the branch that lands second rebases and the integrated suite on the combined tree is the gate. `MEMORY:` / `RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, exactly this shape:
`OPS 0919 <tip> | on main <sha> | offline <passed>/<failed> (<skipped> skipped; baseline <p>/<s>) | C2 guard <built|ESCALATED> · band <built|ESCALATED> · lock_timeout <built|ESCALATED> · A1/A2 <built|ESCALATED> · code line <built|ESCALATED> | RESTARTS: <line> | db: OWED — <n> requires_db tests written, never run | OWED: three-house check, dev-DB run after p4-verify's stop line, next deploy prompt | ESCALATE: <n>`
Then stop. The desk verifies the artifact (L35), stops this session, and launches the three-house check of the build.
