MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify; no write path · SEAT: review hub `review-p4-verify-0919`, launched by the CTO desk in the background. NO NEW RULE: `01-review-harness.md`'s allowlist with one `mkdir` path changed (R9 of 2026-09-18; R1 of 2026-09-19). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/14-review-p4-verify.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-verify-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-verify-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house.

# Second-house check of a DESK PROMPT before it runs (09-18 R8; LAWS L67 floor: the desk's own rebase prompts are checked by another house) — `13-p4-verify.md`, an UNATTENDED build-and-verify prompt for the P4 branch: a rebase onto main, one production-token read of Finviz, two code chunks, migrations and a rollback on the DEV database. Would it FAIL or DO HARM as written? Round 1 of at most 3; small on purpose. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `318239db`, 2026-09-19), not by Dejan. His words on main: `cto-2026-09-18.md` §4 R8 (14:29 — the second-opinion rule, the desk's own deploy / rebase / re-land prompts included), R9 (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20), R18–R21 (the tribunal, L67); `cto-2026-09-19.md` §4 R1 (the Codex read-only rule). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED:` line; `ASK DESK: <question> [<time>]` in the report for a question that does not stop you — then continue with the safe default, never wait on an answer.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — laws, UNATTENDED RULES, PREFLIGHT (Astra answered the archiver tribunal's round 3 at 08:31 ET today → `astra: UP (carried)`, no probe; a usage-limit message at launch → `astra: METER`, continue with two houses), LESSON, §2 launch spellings, and `2026-09-18/11-review-deploy-stack.md` §3 bind you UNCHANGED, with `scratch/review-p4-verify-0919/` in every path. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-verify-0919/REVIEW.md`.

## 1. Packet in `scratch/review-p4-verify-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `13-p4-verify.md` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md` (as it is NOW) · (2) `facts.md` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/14-packet/facts.md` — the desk's pre-computed measurements (the branch's commit split, what `cobalt_dev` was left holding, the `Asset Type` code lines, the rule-by-rule comparison of the launch line with what Dejan approved) · (3) `followup-0918-conflict-procedure.md` = §1 (`## 1. Rebase onto main`) and §4 (`## 4. Full verification`) of `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/01-p4-followup.md`, verbatim — the procedure `13` binds by reference · (4) `relaunch-0917-B3.md` = the one paragraph `B3 Movers export: …` of `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/04-p4-relaunch.md`, verbatim · (5) `build2-escalate.md` = the `## ESCALATE (12)` section of `/Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/s2-p4-build2-2026-09-18.md`, verbatim · (6) `desk-triage.md` = §21 and §22 of `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md`, verbatim · (7) `unattended-standard.md` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`, whole · (8) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — every measurement you need is already in `facts.md`. `13-p4-verify.md` is an UNATTENDED prompt: a Sonnet hub will follow it with nobody watching, launching Opus builders for the code. It runs in a git worktree on a branch called P4. It must never harm production: a trading-support system whose live code is the `main` checkout and whose live database it must never touch. `unattended-standard.md` is how such runs work: every command that is not a plain read must match a rule in the launch line's `--allowedTools` list CHARACTER FOR CHARACTER as one bare command; a command that matches no rule may be denied mid-run, and a mid-run denial means the whole run FAILED. For every finding give SEVERITY (BLOCKER = the run will fail, or can damage the branch, the dev database, production, or leak a credential / MAJOR / MINOR), the STEP, the EVIDENCE (quote the line of `13-p4-verify.md`), and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) THE REBASE, step 1: `facts.md` §A gives the measured split — 34 commits patch-identical to commits already on main, 31 of the branch's own. Is the prompt's expectation right that a plain `git rebase main` drops the 34 by itself and replays exactly the 31? Name any case where git would NOT drop a patch-identical commit (a commit that became empty versus one skipped as already-applied, merge commits in the range, a commit whose patch-id differs because main's copy was amended by a conflict resolution during the re-land) and say what the hub would see and whether the prompt's FAILED conditions catch it. Are the three measuring commands in 1.1 and the checks in 1.3 enough to PROVE the result, using only commands the launch line allows? Is leaving a stuck rebase in place (no `git rebase --abort` rule) safe, and is it clearly better or worse than asking the owner for that one rule? (Q2) COMMANDS VERSUS RULES: go through every command the prompt tells the hub to type, step by step including PREFLIGHT, and for each quote the rule that covers it side by side, or say NONE. Look hard at: `git log --oneline --cherry-mark --right-only main...HEAD`; `git diff --check`; `git diff --stat main HEAD -- …`; `grep -n \"^<<<<<<<\\|^>>>>>>>\" <file>`; `ls -la .env`; `cd /Users/cobalt/cobalt` followed by the production-token fetch and the `cd` back; `uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence`; `COBALT_ENV=dev uv run cobalt db migrate --proof-only`; `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007`; `uv run pytest --co -q …`; `uv run cobalt jobs restarts main..HEAD`; anything a BUILDER (`claude -p`) would need that the prompt does not say. (Q3) THE PRODUCTION-TOKEN FETCH, step 2.1: a hub-written script reads the Finviz credential through the application's own resolver and makes two requests. Can the token reach a log, the report, the tool result or a committed file by any path the prompt leaves open (an exception traceback, a printed URL, the retry)? Is running it from the production checkout with `COBALT_ENV=production` able to WRITE anything in production (a cache file, a job row, a rate-limit counter)? Is overwriting the two old raw files a loss? (Q4) EVIDENCE BEFORE CODE, step 2.2: the rule 'not-equity = any non-blank Asset Type' is about to change what a LIVE service drops. Is the evidence test the right one — does 'table C has 0 rows' actually prove that non-blank Asset Type and being a fund are the same thing on real data, given that the radar-cache files come from the owner's stock screens (which rarely surface funds) and the two movers exports are one day's whole market? What sample would be too thin to conclude from, and does the decision rule notice that? Is the mechanical decision rule safe in both directions (builds when it should not / refuses when it should build)? Is extending a committed fixture-cutter script with an `evidence` mode, in order to stay inside an existing rule, a reasonable use of that rule or a stretch the owner should be told about? (Q5) THE CODE CHUNKS, steps 2.3–2.5 and 3: does any instruction contradict the quoted code in `facts.md` §C or the escalate text in `build2-escalate.md`? Is removing the `unreported` branch safe on the stated proof? Does the new smoke `compare` kind or the K9 change risk touching scoring, ranking or what reaches a trading card (the prompt says stop and escalate if so — is that guard placed where a builder would actually hit it)? (Q6) THE DEV DATABASE, step 4: `facts.md` §B says what `cobalt_dev` was probably left holding (0008/0009 applied before 0006/0007 existed there). Walk the sequence 4.2 → 4.3 → 4.4 → 4.7 for each plausible starting state (0001–0009 all applied; 0008/0009 applied with 0006/0007 absent; 0008/0009 absent) and say where it fails or lies. Is `--down-to 0007` the right rollback target for a branch whose own migrations are 0008 and 0009? Can any command in step 4 reach the production database (the `.env` it copies is the production checkout's file; the prompt relies on `COBALT_ENV=dev`)? Is the `.env` removed on EVERY path, including a FAILED one? (Q7) Anything else that would make this run fail late, leave the worktree or `cobalt_dev` in a state the next run cannot start from, or report READY when it is not. End with exactly one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <finding numbers> / DO NOT RUN`."

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4 and `11-review-deploy-stack.md` §3 EXACTLY, with this folder's path; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: `13-p4-verify.md` on main, the code under `/Users/cobalt/cobalt-wt/s2-p4/` and `/Users/cobalt/cobalt/src/` with Read and `grep` (you have no git rule for the `s2-p4` worktree — `facts.md` §A is the desk's measurement; a claim that only a git command there could settle = `UNVERIFIABLE FROM READS` with the exact command). One extra column per REAL finding: `blocks the launch? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-p4-verify-0919/`. Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
