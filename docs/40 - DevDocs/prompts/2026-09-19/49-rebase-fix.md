STATUS: DEPLOY 3's BUILD LANE, chunk 2 (R32 — the archiver + ops ship tonight IF this lane is green before 21:00 ET). `48-rebase-deploy3.md` REBASED both branches successfully and stopped on a red offline suite: **7 failed / 2131 passed**. Six of the seven are yours to fix. The seventh (`test_run_targets_mode_still_means_the_report_scope`, `TotalDemandExceeded 51.00 rpm vs finviz_max_rpm=50`) is a RULING Dejan owes under L53 and is **NOT yours to touch in any way** — not the ceiling, not the pacing, not the test.

MODEL: Opus 5 (`claude-opus-5`) — you change a migration-registry test set and fold two DB read methods into one (L29) · SEAT: build hub `rebase-fix-0919`, launched by the CTO desk in the background, two bare commands: `cd /Users/cobalt/cobalt-wt/archiver-append` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/49-rebase-fix.md' and follow it exactly." --model claude-opus-5 --remote-control rebase-fix-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git rebase main)" "Bash(GIT_EDITOR=true git rebase --continue)" "Bash(git rebase --abort)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --proof-only)" "Bash(COBALT_ENV=dev uv run cobalt db migrate)" "Bash(COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · **NO NEW RULE: this list is `48`'s, byte for byte** (approved as `cto-2026-09-19.md` §4 **R33**), with only the prompt file name and the `--remote-control` name different · SESSION: fresh · auto mode on; never `bypassPermissions`; **NO production command, NO `--allow-prod`, NO push, NO merge into `~/cobalt`, NO `launchctl`, no vault write** · METER: Anthropic small · nobody sits at this terminal.

# FIX THE SIX, PROVE BOTH SUITES, LEAVE THE SEVENTH ALONE
AUTHORIZATION — VERIFY IT YOURSELF: `cto-2026-09-19.md` `## §4` **R33** (the rule list, 15:0x ET) and **R32** (the lane exists), committed — `git -C /Users/cobalt/cobalt log --oneline -12 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Also standing: **R25** (the REVOKE), **R26** (the dropped twin), **R31**. Missing → `FAILED: authorization mismatch`, stop. Laws: L1, L3 (one path — item 1 IS an L3 fold), L4, L29, L35, L45 (never edit the trader's artifact to fit a parser; never delete a test to make a suite green), L48, L53 (**the ceiling and the cadence are Dejan's and are never settled in a config file — item 7**), L54, L60, L62, L68, L69, L70.
INDEX CARD: `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md` **§4.1's seven-row table IN FULL** (each row quotes the failing assertion and why it is red — that table is your specification) and its §3 resolution sections → `UNATTENDED-LAUNCH.md` §2 + §6 → `LAWS.md` in full → `src/cobalt/db_migrations/__init__.py` (the rebased registry, 0001…0011).
REPORT (L48): append to `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/rebase-d3-2026-09-19.md` under a new heading `# CHUNK 2 — the six fixes`; rewrite nothing above it. Commit in the archiver worktree by name.
UNATTENDED RULES: ONE command per Bash call, exact prefixes, no pipe, no redirect, no `&&`, no `$(…)`. `cd` is its own call. **NEVER `cd /Users/cobalt/cobalt`.** Before every pytest call: `cd <worktree>` then `ls -la .env`. A mid-run denial = the run FAILED (L62): `.env` cleanup if present, then `FAILED: <step> — <command> — <reason>`, commit, stop. You start in `/Users/cobalt/cobalt-wt/archiver-append` and you work on `archiver/append-0919` ONLY — `ops/2026-09-19` is already green (`bb996a5`, offline 1875/0) and you do not touch it.

## 1. TESTS FIRST, then the fold
1.1 `date`; `git status --porcelain` empty; `git log --oneline -1` = `12ef535`; `ls -la .env` → "No such file". Any surprise → `FAILED: gate — <what>`.
1.2 **ITEMS 2, 3, 4, 5 and 7 are ONE defect class with ONE shape** (`rebase-d3` §4.1 rows 2–5 and 7): five enumeration pins, each written while its own branch was the only unmerged one, each asserting "the registry ends with exactly MY two migrations". After the union both sides' claims are false in the same way. Rewrite each to assert what is now TRUE, and **keep every invariant the pin was protecting** — do not weaken a test to green, and do not delete one (L45, L3):
  · `test_archiver_migrations.py::test_forward_ends_0007_0010_0011` → `FORWARD`'s tail is `0008 → 0009 → 0010 → 0011`; rename the test to what it now pins.
  · `::test_reverse_begins_0011_0010_0007` → `REVERSE` begins `0011 → 0010 → 0009 → 0008`, the exact mirror.
  · `::test_rollback_down_to_0007_selects_exactly_the_two_new_files` → `_rollback_paths("0007")` selects **four** files, `0011, 0010, 0009, 0008` in that order. The archiver's own operator-undo bound is `--down-to 0009` — assert that too: `_rollback_paths("0009")` = exactly `0011, 0010`.
  · `::test_the_registry_is_an_explicit_list_so_the_0008_0009_gap_is_legal` → the gap is CLOSED by design. The invariant worth keeping is the one underneath it: the registry is an explicit ordered list whose numbers are **contiguous 1…11 with no duplicates**, and `REVERSE` is exactly `FORWARD` reversed. Rewrite it to assert that, and rename it.
  · `test_p4_migrations.py::test_rollback_down_to_0007_reverses_0009_then_0008_only` → the mirror image: `--down-to 0007` now reverses all four. Keep P4's own bound as a second assertion: `_rollback_paths("0009")` does NOT contain 0008 or 0009.
  Then `cd /Users/cobalt/cobalt-wt/archiver-append` → `ls -la .env` → `uv run pytest -q tests/cobalt/test_archiver_migrations.py tests/cobalt/test_p4_migrations.py` → quote the summary. `git add` BY NAME, commit `test(db-migrate): the five enumeration pins assert the united 0001-0011 registry (rebase-d3 chunk 2)`.
1.3 **ITEM 1 — the owed L3 FOLD** (`rebase-d3` §4.1 row 1): `test_archiver_append_store.py::test_there_is_exactly_one_range_read_on_the_store` is the archiver's OWN pin and its docstring names this moment — "`sprint-2/p4` adds `bars_between()` with its own connection; the two fold into one method at integration." Read BOTH methods in full first (`src/cobalt/archiver/store.py`), then fold them into ONE range-read method on the store (L3, one path). RULES: the surviving method keeps BOTH call sites working, keeps the archiver's own-connection behaviour the three offline pins assert (R26's kept pins), and keeps P4's semantics for its own callers; if the two cannot be folded without changing what either caller receives, **STOP** — `FAILED: 1.3 — bars_between and _bars_in_range cannot fold without a behaviour change — <the two signatures and what differs>` — that is a design call, not yours. Update the pin to assert the ONE surviving name. `uv run pytest -q tests/cobalt/test_archiver_append_store.py tests/cobalt/test_archiver_runner.py` → quote. `git add` by name, commit `refactor(archiver): fold bars_between and _bars_in_range into one range read (L3, owed at P4 integration)`.
1.4 **ITEM 6 — DO NOT TOUCH IT.** `test_archiver_runner.py::test_run_targets_mode_still_means_the_report_scope` fails with `TotalDemandExceeded: peak 51.00 rpm at 04:00 ET over [radar, archiver, replay, backfill], finviz_max_rpm=50`. That is the L53 total-demand gate working exactly as designed. **You do not change `finviz_max_rpm`, any consumer's cadence, any window, the gate, or this test.** It stays RED and you say so in the stop line. Measure ONE thing for the desk, read-only, and report it: run `uv run pytest -q tests/cobalt/test_archiver_runner.py -k "full"` (or read the code if no such test exists) and state whether the `full` nightly mode ALSO exceeds the ceiling or only `backfill` does — `rebase-d3` §ESCALATE 3 says this was never measured (L70). If you cannot measure it without running something not in your allowlist, say `NOT MEASURED — <why>`; never guess.

## 2. BOTH SUITES
2.1 OFFLINE: `cd /Users/cobalt/cobalt-wt/archiver-append` → `ls -la .env` ("No such file") → `uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`) → quote the summary. **GATE: exactly ONE failure, and it is item 6 by name.** Any other failure → `FAILED: 2.1 — offline suite red beyond item 6 — <tests + assertions>`. Zero failures is also a stop if item 6 vanished: say so plainly (`FAILED: 2.1 — item 6 no longer fires; the L53 gate changed and nobody ruled it`).
2.2 WITH-DB (`cobalt_dev` ONLY — never production, never `--allow-prod`), in this exact order, exactly as `48` §4.2 specifies it: (a) `cp` the `.env` by name, `ls -la` prints it · (b) BASELINE `--proof-only`, expect 0001–0009 with the archiver's two tables ABSENT · (c) FORWARD `COBALT_ENV=dev uv run cobalt db migrate`, expect `archive_progress` + `archive_incidents` **CREATED**, no `CHANGED` · (d) SUITE `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy`, **GATE: exactly ONE failure, item 6** · (e) **POST-SUITE `--proof-only`** — the suite writes to `cobalt_dev` by design, so (b)→(e) differences are RECORDED, never a stop; a table that VANISHED is a stop · (f) RESTORE `--rollback --down-to 0009`, expect exactly `0011` then `0010` reversed and nothing else, then `--proof-only` compared **against (e), not (b)**; a digest moved ACROSS the restore is a stop · (g) `rm` the `.env`, `ls -la` says "No such file" — **before ANY stop line**.
2.3 `uv run cobalt jobs restarts <the main tip you read at 1.1>..HEAD` → VERBATIM, 0 UNCLASSIFIED (L42).
2.4 Report committed. Last line, exactly this shape:
`REBASE FIX <archiver tip> | six fixed: <names> | item 6 STILL RED by ruling (L53, <full mode also exceeds | only backfill | NOT MEASURED>) | offline <p>/<f> | db <p>/<f> | registry 0001-0011 | cobalt_dev: <state you proved> | .env: removed, proven gone | RESTARTS: <line> | ESCALATE: <n>`
or `FAILED: <step> — <reason> — cobalt_dev: <state> — .env: <state>`.
