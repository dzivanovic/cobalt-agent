MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch Grok across TWO bucketed calls, verify its findings, restate Gemini's already-read result from `36`; no write path · SEAT: review hub `review-p4-folds-grok-0919`, launched by the CTO desk in the background, SAME worktree as round 1 and `36` (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `36-review-p4-folds.md`'s launch line, with exactly THREE strings changed — the prompt filename (`36-review-p4-folds.md` → `42-review-p4-folds-grok.md`), the `--remote-control` name (`review-p4-folds-0919` → `review-p4-folds-grok-0919`), and the `mkdir` path inside `--allowedTools` (`scratch/review-p4-folds-0919` → `scratch/review-p4-folds-grok-0919`) — because this run needs no rule `36` did not already carry (same repository, same branch, same `git -C /Users/cobalt/cobalt show*`/`log*` reach; the now-unused `Bash(agy *)` and `Bash(codex exec …)` rules stay, unused, for the same reason unused rules stayed in `33` — this round makes no Gemini or Astra call; nothing is removed, only the three named strings change). Proof: sorted `comm -3` between `36`'s `--allowedTools` rule-string list and this prompt's rule-string list yields exactly one line on each side, both the `mkdir` string (the bare `--bg "Read '<file>' …"` string and the `--remote-control` value also differ, as they always do between rounds — those are the filename/name changes already named above, not a second and third rule):
```
"Bash(mkdir -p scratch/review-p4-folds-0919)"
	"Bash(mkdir -p scratch/review-p4-folds-grok-0919)"
```
If this round turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/42-review-p4-folds-grok.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-p4-folds-grok-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-p4-folds-grok-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; ONE headless Grok review per bucket, one attempt each, never retried (L47 — probe before counting on a house; `grok --version` is the probe here).

# P4 FOLDS GROK RE-RUN — a Grok-only re-run of `36-review-p4-folds.md`'s two buckets (fund-rule, round2) on `sprint-2/p4` (worktree `/Users/cobalt/cobalt-wt/s2-p4`). Cause of `36`'s Grok HARNESS (the hub's own note there, and proven today by a run that worked): the hub scoped Grok's permission to a single file, `--allow "Write(<one file>)"`, which Grok's sandbox blocks; the spelling PROVEN on this host is the folder glob — `grok --sandbox cobalt-job --allow "Write(<scratch-dir>/**)" -p "…"` (see `33-review-archiver-grok-whole.md` §2 in this same prompts folder — that run answered 4 of 4 buckets — and `01-review-harness.md` §2). `36` closed with gemini having read both buckets for real (`gemini: … FOLD CLEAN / FIX FIRST 2 (non-blocking)`) and grok HARNESS on both — so only ONE house has actually read P4's two folds. LAWS L67 wants ≥3 checkers with a floor of 2 when a meter runs out; Astra is METER until ~16:54 ET (per `36`'s own §4 probe), so the floor here is Grok + Gemini, and Grok is the one owed a re-run. This round makes NO Gemini call (it already read both buckets in `36`'s run — reused by reference, not re-run) and NO Astra probe (still METER per `36`'s report; re-probing before ~16:54 ET would waste an attempt against a known-out meter). DO NOT STOP until the report ends `P4 FOLDS GROK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Standing law: LAWS.md L67, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, R19-R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. This round's own debt: Dejan's R16 (`cto-2026-09-19.md` §4) named the two-fold read as NEXT; his R23 named the round-2 re-confirmation that led to `36`'s launch — prove both are committed with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. `36`'s own close line is this round's specific trigger: read `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-folds-0919/REVIEW.md`'s last line and `## Grok` section — confirm it shows grok HARNESS on both buckets and gemini FOLD CLEAN / FIX FIRST 2 before proceeding; a mismatch (e.g. grok did NOT harness, or gemini did not complete both buckets) → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the packet's own `facts.md` (verify it against the diffs, don't just relay it) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet for a given bucket — Grok gets exactly the file list `36` gave it, nothing narrower · L47 a meter is checked BEFORE a house is counted on · L52 nothing in either fold may touch scoring, ranking or a card value without disclosure — fold 2's A3 is exactly this, already ruled (R23) · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out — Astra's meter is still known OUT this round, so the floor is Grok + Gemini, and this run is what closes that floor.

INDEX CARD (you; Grok gets its assigned bucket only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close), `2026-09-18/11-review-deploy-stack.md` §3 (verify), and `33-review-archiver-grok-whole.md` §2 (the proven folder-glob Grok invocation pattern, copied EXACTLY below) BIND you here UNCHANGED, with `scratch/review-p4-folds-grok-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. SECOND LESSON (carried from `18`/`22`/`36`): the Write tool strips trailing whitespace from copied diffs — prefer `cp` for the packet copy, byte-identical, as `33` did; if Read→Write is used instead, state the byte delta once in `## Packet`, never chase it. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-folds-grok-0919/REVIEW.md` — same gitignored-scratch pattern, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini (restated from 36)` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — REUSE `docs/40 - DevDocs/prompts/2026-09-19/36-packet/` UNCHANGED, no new files, no edits; copy it into `scratch/review-p4-folds-grok-0919/` (`mkdir -p` first; `cp`, or Read → Write byte-identical where the tool allows — same method `36` used)

Files, with byte counts to verify with `wc -c` after copying (re-stage ONLY a file whose count differs) — the SAME table `36` used, nothing added, nothing removed:

| file | bytes | for |
|---|---|---|
| `fund-rule.diff` | 86,951 | fund-rule bucket |
| `round2.diff` | 30,600 | round-2 bucket |
| `round2-report-section.md` | 45,593 | round-2 bucket |
| `verdicts.md` | 8,430 | both buckets |
| `rulings.md` | 2,377 | both buckets |
| `facts.md` | 6,367 | both buckets |
| `QUESTIONS.md` | 7,326 | both buckets |

**BUCKETS** — same split `36` used, each read as a separate call, never combined:
- **fund-rule bucket** (~111.5 KB, 5 files): `fund-rule.diff`, `verdicts.md`, `rulings.md`, `facts.md`, `QUESTIONS.md`.
- **round-2 bucket** (~100.7 KB, 6 files): `round2.diff`, `round2-report-section.md`, `verdicts.md`, `rulings.md`, `facts.md`, `QUESTIONS.md`.

`fund-rule.diff` = `git diff d5947e7..2738cdc -- src tests` from `/Users/cobalt/cobalt-wt/s2-p4`, with the two raw Finviz-export fixture files excluded under L32 (`facts.md` names the exclusion). `round2.diff` = `git diff 2738cdc..9045260 -- src tests`, complete. Verify both, if needed, with Read/`grep` under `/Users/cobalt/cobalt-wt/s2-p4/` (covered by `--add-dir /Users/cobalt/cobalt-wt`) or `git -C /Users/cobalt/cobalt show sprint-2/p4:<path>` if that ref is fetched.

## 2. Grok — TWO calls, one per bucket, no shell, headless — `33-review-archiver-grok-whole.md` §2's invocation pattern EXACTLY, folder-glob `--allow`, adapted only for this run's files and buckets
For EACH bucket (fund-rule, round2), ONE `grok` call, `run_in_background`, no `--always-approve`, sandboxed with write access scoped to ONLY this run's scratch directory (the proven folder-glob spelling):

`grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-folds-grok-0919/**)" -p "You have NO tools for this task except reading the local files named below and writing ONE output file. Never run a command, never list a directory, never fetch a URL, never search the web — if answering a question would require any of those, write UNVERIFIABLE FROM PACKET for that item and continue. Read ONLY these <N> files, all in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-folds-grok-0919/: <explicit file list for this bucket, six or fewer, from the table above>. Follow QUESTIONS.md exactly, answering only the questions your files can address (mark the rest N/A THIS CALL). Write your complete findings to grok-findings-<fund-rule|round2>.md and reply with only that path."`

fund-rule call's `<N>`/file list = 5 files: `fund-rule.diff`, `verdicts.md`, `rulings.md`, `facts.md`, `QUESTIONS.md`. round2 call's `<N>`/file list = 6 files: `round2.diff`, `round2-report-section.md`, `verdicts.md`, `rulings.md`, `facts.md`, `QUESTIONS.md`. One attempt per bucket, NEVER a retry — a call that HARNESSes (any tool-permission denial, any empty/no output) is recorded as `grok bucket <fund-rule|round2>: HARNESS` and you move to the next bucket. Record each call's verbatim output (or the HARNESS text) as `grok-findings-fund-rule.md` / `grok-findings-round2.md`. Combine both calls into one `## Grok` section: file, bytes, F-tags verbatim, verdict-this-call verbatim, per bucket.

## 3. Gemini — NOT re-run; restate `36`'s result by reference
Read `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-p4-folds-0919/REVIEW.md`'s `## Gemini` section and its close line's `gemini:` field. Copy its per-bucket verdicts (fund-rule / round2) and F-tags verbatim into `## Gemini (restated from 36)`, citing that file path. Say plainly why no new Gemini call is made: it already read both buckets for real in `36`'s run, and L44 (same packet for a given bucket) is satisfied by the packet being byte-identical (`36-packet/`, reused unchanged in §1 above) — a re-run would be the same house reading the same bytes twice, not a second house.

## 4. Astra — NOT probed; say why
Do not run the Codex probe this round. `36`'s own §4 recorded Astra METER (expected up ~16:54 ET) — re-probing now would be a second attempt against a meter already known out, and this round's only job is to fill the Grok gap `36` left, not to re-check Astra. Record `astra: METER (not re-probed — 36's probe already recorded it, expected up ~16:54 ET)` in `## Astra` and move on.

## 5. Verify — using ONLY this launch line's rules, nothing new
Every REAL finding from Grok against the REAL files: a diff claim against `git -C /Users/cobalt/cobalt-wt/s2-p4` via Read/`grep` under `--add-dir /Users/cobalt/cobalt-wt` (no Bash rule needed for a plain read), and against `rulings.md`/`verdicts.md`'s own quoted text for anything about what R16/R23/REVIEW-C actually say. If a verification genuinely needs a command this launch line does not cover (an independent re-derivation of either diff with a fresh `git diff` against `/Users/cobalt/cobalt-wt/s2-p4`, or `uv run pytest`/`uv run cobalt jobs restarts` to re-check the offline counts or RESTARTS claims in `facts.md`) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks shipping? yes/no — why`. If a Grok finding duplicates one Gemini already raised in `36`, say so and cite it rather than re-litigating. You fix NOTHING and edit NOTHING outside `scratch/review-p4-folds-grok-0919/`.

## 6. Close
`## Verdict table` combines Grok's (this run) and Gemini's (restated from `36`) Q1–Q9 answers against the real files, one row per question, naming which house(s) answered it and from which bucket, plus any finding that duplicates something `verdicts.md` (REVIEW-C) or `rulings.md` (R16/R23) already settled — cite it, don't re-litigate it. Last line, beginning EXACTLY `P4 FOLDS GROK`:
`P4 FOLDS GROK · grok: <b>/<m>/<n> <FOLD CLEAN|FIX FIRST n|HARNESS|METER|TIMEOUT> (fund-rule: …, round2: …) · houses that have read both folds (with 36's Gemini): <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>` or, on any authorization/harness failure severe enough to stop entirely, `FAILED: <step> — <concern>`. Then stop.
