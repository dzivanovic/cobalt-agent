MODEL: Opus 5 (`claude-opus-5`) — same seat and shape as `25-ops-db-run.md` (the ops with-DB run): L29 floor on the migration harness, and the DB step itself is ALWAYS hub-direct, never delegated to a builder. SEAT: `archiver-db-0919`, worktree `/Users/cobalt/cobalt-wt/archiver-append` (it already exists, round-2 code tip `9056e73`, report commit `fb825df` on top — do NOT create it, do NOT `git worktree add`). THIS RUN TOUCHES `cobalt_dev`: it is a `.env`-present, `COBALT_ENV=dev` run. `cobalt_dev` is FREE now — `ops-0919-db`'s stop line: `cobalt_dev: 0001–0009, unchanged`, `.env` removed there, verified (PREFLIGHT proves it yourself). Two bare commands, run by the desk: `cd /Users/cobalt/cobalt-wt/archiver-append` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/35-archiver-db-run.md' and follow it exactly." --model claude-opus-5 --remote-control archiver-db-0919 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run pytest *)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh — every relaunch resumes from the report's last `CONTINUE:` line · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it; no production command, no `--allow-prod`, no vault write, no other seat's config; `cobalt_dev` and `.env` ARE this session's to touch, scoped exactly to the two rules above · METER: Anthropic medium; a usage-limit message is not a failure — record the reset time, and BEFORE stopping remove `.env` if present and prove it gone (RECOVERY RULE applies to the meter stop too), commit, last line `CONTINUE: step <n> — meter, reset <time>`, stop (the desk relaunches) · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).

# Archiver 2026-09-19, DB RUN — the append-only archiver's owed with-DB proof: migrations 0010/0011 applied AND reversed on real `cobalt_dev`, then this branch's owed `requires_db` tests get their FIRST REAL RUN. LAW STEP of this work item (L67, 09-18 R13–R17 / 09-19 R8): BUILD + the DB proof this branch has owed since round 1 (`archiver-append-build-2026-09-19.md`'s own ESCALATE (ii)/R2-3). After it, none of it yours: the branch still needs its whole-build house check closed (Astra's read is owed after its meter resets; Grok's whole-build read is queued separately) · Dejan's approval · deploy 3, Sunday, after P4. You never merge, never deploy, never push, never touch production, never `--allow-prod`. DO NOT STOP until the report ends `ARCHIVER DB …`, `CONTINUE: … — meter …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (Fable), not by Dejan; a prompt file is not an approval.
(a) The 16 base `Bash(...)` rules + `Bash(uv run cobalt jobs restarts *)` + the two `COBALT_ENV=dev` rules + `--disallowedTools`/`--add-dir` above are `25-ops-db-run.md`'s own launch line, already run once on `cobalt_dev` (`ops-0919-db`, stop line `OPS 0919 DB be00506 …`) — same strings, this worktree's path only where the `.env` rules name it. These need no new approval beyond what covered `25`.
(b) The TWO rules new to THIS worktree — `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)`, `Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)` — were approved together with `25`'s own pair in ONE message. Quote it exactly, verbatim from `docs/40 - DevDocs/reports/cto-2026-09-19.md`, `## §4 Rulings`, row **R18** (11:33 ET, "approve env"): *"FOUR new rule strings, exactly: `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)` · `Bash(rm /Users/cobalt/cobalt-wt/ops-2026-09-19/.env)` · `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env)` · `Bash(rm /Users/cobalt/cobalt-wt/archiver-append/.env)` — a by-name copy of `.env` into that worktree for the dev-DB test run, never printed, removed at the end with its absence proven in the terminal line; disclosed with it as REUSED from 13's approved line: `Bash(COBALT_ENV=dev uv run cobalt db migrate*)`, `Bash(COBALT_ENV=dev uv run pytest *)`. NEVER: production, push, vault write, `bypassPermissions`. Covers: `25-ops-db-run.md` (launch when `cobalt_dev` is free) and the archiver's DB run after its round 2."* If that row is missing, reworded, or its APPLIED marker absent → `FAILED: authorization mismatch — no R18 row for the archiver .env pair`, commit nothing, stop before PREFLIGHT.
YOU CAN ALWAYS STOP. If a step looks wrong, unsafe, or outside the recorded approval: `FAILED: <step> — <your concern>` as the report's last line — but FIRST run `.env` cleanup (see below) — then wip-commit, stop. ASK DESK: a question that does not stop you → `ASK DESK: <question> [<time from date>]` in the report, take the safe default named in the step, continue; never block on an answer, never message anyone.

RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(archiver-db-0919): <item> partial — <why>"`, last line `CONTINUE: step <n>`. EVERY `git add` names explicit paths under `src/`, `tests/`, `docs/40 - DevDocs/` — NEVER `scratch/`, never `-A`, never `.`, never a directory. After every commit: `git show --stat HEAD` lists only paths you named. `.env` DISCIPLINE (L4, L41 — the one rule that overrides every other stop order): on ANY stop — `FAILED`, `CONTINUE`, meter, or the normal end — `ls -la .env` FIRST; if it exists, `rm /Users/cobalt/cobalt-wt/archiver-append/.env` THEN `ls -la .env` again to prove it gone, THEN write whatever stop line you were going to write. A stop line written while `.env` still exists on disk is itself a `FAILED` condition — go back and remove it, then re-open the report and fix the line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 (binds you) → Memory `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL (you touch `cobalt_dev` directly, L4/L41/L67) → `docs/40 - DevDocs/reports/cto-2026-09-19.md` row **R18** (quoted above — the authorization) → `docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md` §4/§11 (the migrations' own spec — binding; where your reading of the code differs from it, the CODE wins and the difference is an ESCALATE line) → `src/cobalt/db_migrations/__init__.py` IN FULL (the `FORWARD`/`REVERSE` tuples and the module docstring's own account of the 0008/0009 gap — read it before you touch anything) → `src/cobalt/db_migrations/cli.py`, esp. `cmd_migrate` (≈:497-660) and its `--rollback`/`--down-to`/`--proof-only` flags → `tests/cobalt/test_archiver_migrations.py` IN FULL (the 0010/0011 `requires_db` half, ≈17 tests incl. `test_forward_creates_both_tables_on_the_system_side`, `test_migrate_twice_is_idempotent_for_the_two_new_tables`, `test_rollback_down_to_0007_drops_exactly_those_two_and_nothing_else`) → `tests/cobalt/test_archiver_append_store.py` §§ marked `requires_db` → `tests/cobalt/test_archiver_quiet.py`'s one `requires_db` test (the F3/Q10 race twin) → `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` in THIS worktree (the file you APPEND to — a new `## DB RUN` top-level section, AFTER `## ROUND 2`'s existing final line `ARCHIVER APPEND R2 9056e73 …`; never delete or restructure existing sections) → `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-r2-0919/REVIEW.md` (the three-house check this run's fold rests on; its `UNVERIFIABLE FROM READS` line names three things a DB run can settle — settle all three: the offline `1866/0/320` count + the 23 `requires_db` tests, the `RESTARTS: com.cobalt.aset com.cobalt.radar` line, and a fresh `git diff --stat` of `c974fbf..HEAD`) → `25-ops-db-run.md` (the shape this run copies almost unchanged — same seat, same `.env`/`COBALT_ENV=dev` command shape, same PREFLIGHT pattern) and its close report `~/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md`'s last line (read-only evidence of what `cobalt_dev` holds now — you do not re-derive it by any other means). Laws, one line each: L1 fail loud · L3 one path — the FORWARD/REVERSE tuples are the only registry, never a second one · L4 secrets discipline — `.env` by name only, never `cat`/printed · L29 Opus floor on the migration harness · L35 artifact, not claim — the desk verifies via `git show`/`git diff`, never your report's prose alone · L41 cross-house credential law — this session runs the DB-backed proof directly (hub-shaped, no worker) · L45 real-shape evidence — 0010/0011's forward-and-reversed proof is on real Postgres, never asserted from the SQL text alone · L46 one run, one commit boundary; small commits within it (CLAUDE.md) · L48 evidence in the report file, same turn · L54 you work in this worktree only · L57 every printed value replayable · L60 you hold this session until the stop line · L67 this build still needs its house check closed after you stop (Astra's whole-build read is owed separately — not yours to chase). Report (L48): APPEND to `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` in THIS worktree — a new `## DB RUN` section: §0 Headline ≤5 lines → `## DB RUN PREFLIGHT` → `## DB RUN AUTHORIZATION` → `## DB RUN — MIGRATIONS 0010/0011` → `## DB RUN — WITH-DB SUITE` → `## DB RUN — OFFLINE SUITE + RESTARTS + UNVERIFIABLE SETTLED` → `## DB RUN ESCALATE` → last line. Run `date` before you write any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` is its own call; outputs are read from the tool result, files written with Write/Edit; a long suite goes `run_in_background`. NOT in your list, so never typed: `git rebase`, `git reset`, `git checkout`, `git stash`, `git push`, `git rev-parse`, `python`, `uv run cobalt` anything except `jobs restarts` and `db migrate` (and `db migrate` ONLY with `COBALT_ENV=dev` in front, never bare, never `--allow-prod`), any `COBALT_ENV=production` command, `launchctl`, `claude`, `cat`/`less`/`more`/anything that could print `.env`'s contents. A DENIAL IS A FAILURE, never an escalate-and-continue: `FAILED: <step> — <command> — <reason string>`, `.env` cleanup, wip-commit, stop. Never retry a denied command in another shape.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED + reason verbatim):
`date` · `git status --porcelain` (EMPTY — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (LONG form; quote its first two lines; branch must be `archiver/append-0919`; "rebase in progress" → `FAILED PREFLIGHT`) · `git log --oneline -1` (first launch: `fb825df`, else the tip your last `CONTINUE:` commit left — say which) · `git -C /Users/cobalt/cobalt log --oneline -1` (record it, no equality required) · `ls -la .env` → MUST say "No such file" (if it exists → `FAILED PREFLIGHT: .env present before this run started — remove it and say who left it, this is not yours to inherit silently`) · `ls -la /Users/cobalt/cobalt-wt/ops-2026-09-19/.env` → MUST say "No such file" (the signal that `ops-0919-db` finished cleanly and does not still hold the lane; if it EXISTS → `FAILED PREFLIGHT: ops-0919-db's .env is present — another session may hold cobalt_dev, do not proceed`) · Read (not Bash) `/Users/cobalt/cobalt-wt/ops-2026-09-19/docs/40 - DevDocs/reports/ops-2026-09-19.md`'s last `OPS 0919 DB`/`FAILED` line and quote it verbatim (expected: `OPS 0919 DB be00506 … cobalt_dev: 0001–0009, unchanged … .env: removed, proven gone`) · `grep -rn "cobalt_dev" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/"` dated after that line's timestamp (by filename/section date) and read (Read tool) any later hit to confirm no session claimed the lane after `ops-0919-db` closed it — a hit that claims `.env`/`cobalt_dev` and is NOT `ops-0919-db`'s own report → `FAILED PREFLIGHT: a later session may have touched cobalt_dev — <what you found>` · `git -C /Users/cobalt/cobalt-wt/archiver-append diff --stat c974fbf..HEAD -- src tests docs` (quote it — this is the first of the three UNVERIFIABLE-settle items; it must show only what round 2's own commits touched, nothing else; a surprise path → ESCALATE, not silently accepted) · `uv run pytest --co -q tests/cobalt/test_archiver_migrations.py tests/cobalt/test_archiver_append_store.py tests/cobalt/test_archiver_quiet.py` (collects cleanly — no code change has landed yet; note the collected `requires_db`-marked count against the report's own figure of 23, name any drift). Any denial → `FAILED PREFLIGHT: <rules>`.

## 0. STATE PROBE — allowlist proof + the "what does `cobalt_dev` hold" line (L62: probe the rules with no harmless variant, once, before real use)
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env` → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (applies NOTHING; quote the FULL output — every table THIS branch's harness knows, its placement, row COUNT, digest, and the trailing `code:` line) → `rm /Users/cobalt/cobalt-wt/archiver-append/.env` → `ls -la .env` proves it gone. Read the `--proof-only` output against `placement.py`'s tables (0001-0007's set, present and `OK`; 0010/0011's two tables — `archive_progress`, `archive_incidents` — expected ABSENT/MISSING, since this branch has never applied them against real `cobalt_dev`; P4's 0008/0009 are not in this branch's `FORWARD` at all, so its harness has no opinion on them one way or the other). State the terminal line for this step: `cobalt_dev holds (this branch's view, before this run): 0001–0007 objects OK, <n> rows in <largest table>; 0010/0011 objects: absent (expected — never applied on this cobalt_dev)`. If ANYTHING here reads `CHANGED`, errors, or the two new tables already exist → STOP and ESCALATE (do not assume a prior partial run cleaned up; name what you found) — `.env` cleanup, `FAILED: step 0 — <line>`, stop. Last line `CONTINUE: step 1`.

## 1. MIGRATIONS 0010/0011 — applied AND reversed, proved on real `cobalt_dev`
`cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/archiver-append/.env` (re-copy; step 0 removed it) → `COBALT_ENV=dev uv run cobalt db migrate` (forward: 0001-0007 idempotent no-ops against what is already there, 0010 and 0011 apply fresh; quote the FULL output) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (prove every table 0001-0011 now reads `OK`, none `CHANGED`; quote it) → `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` (per `_rollback_paths`' numeric-prefix selection this reverses exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` and stops — 0001-0009 are NOT touched; quote the FULL output) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` (prove `cobalt_dev` is back at 0001-0009: 0010/0011's two tables absent again, every 0001-0009 object unchanged from step 0's baseline; quote it). `.env` stays in place — you need it again for step 2 (the with-DB suite), same convention `25`'s own step 1 used; say so explicitly rather than leaving the report silent about `.env`'s presence at this boundary. If EITHER proof-only reads `CHANGED` where it should not, or the rollback leaves either new table behind, or an error surfaces → `.env` cleanup, `FAILED: step 1 — <line>`, stop; this is the one step where "roll back and prove it" is the safety net, so a failed rollback is escalated, never silently left half-applied. State the terminal line for this step, feeding the stop line's own field: `migrations 0010/0011: applied (both tables created, `--proof-only` OK) and reversed (`--rollback --down-to 0009` dropped both, `--proof-only` shows cobalt_dev back at 0001–0009 unchanged)`. Commit: `test(db-migrate): 0010/0011 applied and reversed on real cobalt_dev — first live proof (archiver DB run)` (report-only commit if no code changed; if the proof surfaces a code defect, fix it tests-first, its own commit, named plainly). Last line `CONTINUE: step 2`.

## 2. THE WITH-DB SUITE — this branch's owed `requires_db` tests, real Postgres, `cobalt_dev`
SCOPE, deliberately narrow, same discipline `25`'s step 4 used: this branch's OWN `requires_db` tests are `tests/cobalt/test_archiver_migrations.py`, `tests/cobalt/test_archiver_append_store.py`, and `tests/cobalt/test_archiver_quiet.py`'s one race twin (the report's count of 23, confirmed or corrected against PREFLIGHT's collection). Repo-wide `requires_db` tests this branch merely touched in passing (`test_radar_score_migration.py`, `test_tenancy.py` — both explicitly OUT OF SCOPE in `25` too) are OUT OF SCOPE here — not verified by this run, not authorized by this prompt; leave them skipped, do not run the whole `tests/cobalt` tree with `.env` present in this step.
`COBALT_ENV=dev uv run pytest -q tests/cobalt/test_archiver_migrations.py tests/cobalt/test_archiver_append_store.py tests/cobalt/test_archiver_quiet.py` → quote the FULL summary line (`<p> passed, <s> skipped, 0 failed` or the reds, named). Every test that was previously `requires_db`-skipped in these three files now runs for real. A red: name the test, quote the assertion, state whether it is a TEST-SIDE defect (fix it here, tests-first, its own commit) or a genuine finding about the harness/`cobalt_dev`'s real state (ESCALATE, do not "fix" real data). `cobalt_dev` must be UNCHANGED in content after this step beyond what step 1 already proved reversed (every test that writes cleans up; the migration-harness tests in `test_archiver_migrations.py` that call `_apply`/`_rollback_paths` directly are self-contained round trips) — if you have reason to believe otherwise, say so explicitly, do not assume. `rm /Users/cobalt/cobalt-wt/archiver-append/.env` → `ls -la .env` proves it gone. State the terminal line, feeding the stop line's `db` field and settling the first UNVERIFIABLE item: `cobalt_dev holds (after this step): 0001–0009, unchanged | db <p>/<f> (the 23 archiver requires_db tests, scoped as above)`. Last line `CONTINUE: step 3`.

## 3. OFFLINE SUITE + RESTARTS + the other two UNVERIFIABLE-SETTLE items
With `.env` confirmed absent (re-check `ls -la .env` if any doubt): `uv run pytest -q tests/cobalt tests/taxonomy` → quote the summary line; compare against round 2's own close `1866 passed, 320 skipped, 0 failed` (`archiver-append-build-2026-09-19.md`'s ROUND 2 stop line) — `passed` MUST still be 1866 and `failed` MUST stay 0 (this step touches no offline test); `skipped` should drop by however many of the 23 `requires_db` tests ran for real in step 2 — name the arithmetic, do not wave off a mismatch. This settles the REVIEW.md UNVERIFIABLE item on the 1866/0/320 count. `uv run cobalt jobs restarts 8838dda..HEAD` (the branch's merge-base with `main`) → the table VERBATIM — this settles the REVIEW.md UNVERIFIABLE item on the `RESTARTS: com.cobalt.aset com.cobalt.radar` line; name any UNCLASSIFIED entry, do not assume it matches. The third UNVERIFIABLE item (a fresh `git diff` of `c974fbf..archiver/append-0919`) was already run in PREFLIGHT — restate its result here rather than rerunning it. `git status --porcelain` empty · `git diff --stat 8838dda HEAD -- src tests docs` listed, every path one this prompt or the branch's own prior commits named · report committed by explicit path: `docs(report): archiver db run — <one line>`.

## 4. DB RUN CLOSE
`## DB RUN ESCALATE` carries forward, untouched, everything round 1's and round 2's ESCALATE sections already carried (the report's own R2-1 shadow-night timezone ruling still owed, R2-3 now CLOSED by this run), PLUS whatever this run found new (a real red from step 2, any drift from step 3's expected counts, an authorization mismatch). State plainly whether the branch's DB debt is now CLOSED (it should be, one way or another, after steps 1-2) and list the three REVIEW.md UNVERIFIABLE items by name with how each was settled. `MEMORY:`/`RULING:` lines if you have any (L48, L58 — you never write the memory folder). Last line, exactly this shape:
`ARCHIVER DB <sha> | offline <passed>/<failed> (<skipped> skipped vs round-2 320) | db <passed>/<failed> (the 23 archiver requires_db tests) | migrations 0010/0011: <applied+reversed proof, one clause> | UNVERIFIABLE settled: <the three items, one clause each> | cobalt_dev: <state> | .env: removed, proven gone | RESTARTS: <line> | OWED: whole-build house check close (Astra), next deploy prompt (deploy 3, after P4) | ESCALATE: <n>`
or `FAILED: <step> — <reason> | cobalt_dev: <state>` (with `.env` cleanup proven first, always). Never merge, never touch `~/cobalt`'s tree, never push, never run anything against production or with `--allow-prod`.
