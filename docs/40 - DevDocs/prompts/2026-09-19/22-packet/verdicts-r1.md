## Verdict table
Verified against the real files on branch `ops/2026-09-19` (worktree `/Users/cobalt/cobalt-wt/ops-2026-09-19/`, tip `319eaaa`, read with Read/`grep`; packet `code.diff` line numbers match the real files, e.g. `db_migrations/cli.py:664` `SET LOCAL`, `test_migrate_proof.py:1289/1306/1320`). Houses' own severity in parentheses; "blocks" is my read, the desk and Dejan decide (L37).

| # | houses | claim (≤20 words) | what I read (file:line) | VERDICT | fix in one line | blocks the branch shipping? |
|---|---|---|---|---|---|---|
| R1 | grok F1 (M) · gemini F1 (M) · astra #5 (M) | BEFORE probe runs before `SET LOCAL lock_timeout`, so an ACCESS EXCLUSIVE holder can still hang it with no ceiling | `db_migrations/cli.py:659` `before = _probe_all(conn)`, `:664` `SET LOCAL`, `:665` `_apply`; docstring names it (`:600-606`); test stubs `_probe_all` (`test_migrate_proof.py`, `(j)` docstring) | **REAL** (the gap; disclosed, and the spec's own named safe default) | once R11 is settled, move the `SET LOCAL` into `_connect` before `_assert_utf8`/probe | no — spec-mandated placement, disclosed as ESCALATE 2; all three houses call it MAJOR-before-the-deploy-prompt, not BLOCKER; desk decides |
| R2 | grok F2 (M) · gemini F2 (B) · astra #1 (B) | test (g) `cancel`+rejoin+close sit in the `try`'s `if hung:`; an exception before that leaves a live worker unclosed | `test_migrate_proof.py:1288-1308` (`thread.start()` … `if hung:` `conn.cancel()` `thread.join`), `:1310-1322` `finally`: `other.commit()` then `if not thread.is_alive():` close. A raise from `_wait_for_a_blocked_lock` → `finally` releases the ALTER but checks `is_alive()` immediately (race), so the worker's uncommitted ALTER can be left holding ACCESS EXCLUSIVE until the interpreter exits | **REAL** — the specified hung path works; the exceptional path is not covered. Hub severity: MINOR/MAJOR test hygiene, not BLOCKER (test-file only; `requires_db`; the spec literally asks for cancel/join/close on the *hung* path) | move `cancel`/`join`/`rollback`/`close` into the `finally` unconditionally (join with a timeout) | no — test file only, exceptional path only; must be fixed before the owed dev-DB run |
| R3 | astra #2 (B) | the "20 s join fires first" ordering is not guaranteed; a >5 s probe lets the 25 s server timeout fire first | `:1288-1295` probe then `join(LOCK_CEILING_S)`; `statement_timeout` (`:1281`) starts at the ALTER, the join clock after the probe; docstring claims 20 s "fires first" unconditionally | **REAL but MINOR** — needs the ALTER still blocked by a *third* session after `other.commit()` AND a probe >5 s (the probe returns in ms once the wait is visible); then `hung` is False and `_probe(conn)` hits an aborted transaction (a driver error) | state the ordering as conditional, or start the join deadline from `thread.start()` | no — test-file wording/edge case |
| R4 | astra #3 (B) | `code:` is not the final CLI output on a `CHANGED` refusal (a later `FAILED:` follows) | `cli.py:712-720`: `print(_code_line())` at `:715`, then `raise MigrationError` at `:717`; `src/cobalt/cli.py:503` prints `FAILED: …` to **stderr**; comment at `:713-714` says the ordering is deliberate. Grok read the same and found none | **NOT REAL** as a defect: `code:` IS the last line `cmd_migrate` prints on stdout on every path; the stderr `FAILED:` is the CLI boundary's, and a hub capturing stdout sees `code:` last. (Wording nuance only: a `2>&1` capture would end on `FAILED:`) | none needed; quote the last *stdout* line in the deploy gate | no |
| R5 | astra #4 (B) | the timeout message's `pg_locks WHERE NOT granted` lists waiters, not the holder, and runs after rollback | `db_migrations/cli.py:697-699` text: "Find the session holding it: SELECT pid, mode, granted, relation::regclass FROM pg_locks WHERE NOT granted;" — that filter selects ungranted requests; after this transaction rolled back its own waiting row is gone, so the query can return no rows while a holder exists | **REAL** (operator guidance is inexact; it does name `pg_locks` as the spec asked). Hub severity MINOR | change the hint to list *granted* locks on the relation, or `pg_blocking_pids`/`pg_stat_activity` | no — diagnostic text; one line, fix before the deploy prompt quotes it |
| R6 | astra #6a (B) | report's b1 red-first breakdown "13 failed" import + 5 + 1 = 19 ≠ "18 failed" | `ops-2026-09-19.md` (branch) `:154` "18 failed, 10 passed", `:157` "13 failed on `ImportError`". Recount from the diff: 7 (refusal class) + 5 (`test_the_validator_accepts_it`) = **12** import failures; 12+5+1 = 18 | **REAL** (the total 18 is right, the "13" is off by one) | correct "13" to "12" | no — report text |
| R7 | astra #6b (B) | headline "1 new + 2 edited `requires_db` tests" conflicts with the diff: only test (g) is edited | `ops-2026-09-19.md:8` and stop line; `test_migrate_proof.py`: `_wait_for_a_blocked_lock(` has ONE call (`:1289`, in (g)); ESCALATE 8 itself says the second "shares `JOBS_TABLE` but not the helper" (no hunk in `code.diff`) — so the second is not edited (this also confirms test (f) is unmodified and does not share the helper) | **REAL** (over-count; conservative direction) | say "1 new + 1 edited" | no — report text |
| R8 | astra #6c (B) | ESCALATE 5 calls three main-only files "all as DELETIONS", but `cto-2026-09-19.md` is `M` in the RESTARTS table | packet `build-report.md` RESTARTS rows: two `D`, `docs/40 - DevDocs/reports/cto-2026-09-19.md M`; text says "three ADDITIONAL paths, all as DELETIONS" | **REAL** (wording; one path is a modification with −60 lines) | say "two deletions and one modification" | no — report text |
| R9 | grok F3 (m) · astra #6d (B) | item a says the premarket-trading-day case is "see ESCALATE 1"; ESCALATE 1 is the DevDocs symbol check | `ops-2026-09-19.md:129` "…premarket open… see `## ESCALATE` 1"; ESCALATE 1 (`:496`) = DevDocs symbol check; `grep -i premarket` finds no ESCALATE entry (the spec asked for "one ESCALATE line" if the builder thought it mattered) | **REAL** (dangling pointer; the case itself is unchanged behaviour — a trading day with zero rows still FAILs) | add the one ESCALATE line or drop the pointer | no — report text |
| R10 | gemini F3 (m) · astra Q7 · grok Q7 | suite counts, BASELINE, `RESTARTS:`, red-first transcripts cannot be confirmed from reads | packet has no pytest log or command output; only the inventory (+46 named tests, +1 `requires_db`) reconciles by count from `code.diff` (grok and astra both did) | **UNVERIFIABLE FROM READS** — re-running `uv run pytest -q tests/cobalt tests/taxonomy` and `uv run cobalt jobs restarts main..HEAD` needs rules this launch line does not carry: `Bash(uv run pytest *)` and `Bash(uv run cobalt jobs restarts *)` (both were in the *build's* line). The desk's own `git diff`/`git -C … diff` also not covered for `ops-2026-09-19` (only `s2-p2-cards diff*`) | rerun by the desk or a hub with those two rules | no — evidence gap, not a defect |
| R11 | all three (context) | whether `SET LOCAL` takes/moves the REPEATABLE READ snapshot (decides where `lock_timeout` may go) | build docstring `:582-598`: "NOT SETTLED from a citable source"; no file in the packet or repo settles it. (My recollection is that a plain `SET`/`SHOW` takes no snapshot — unverified, not to be relied on.) | **UNVERIFIABLE FROM READS** | experiment on `cobalt_dev` (the desk runs it): session A `BEGIN ISOLATION LEVEL REPEATABLE READ; SET LOCAL lock_timeout='1s';` (no query yet); session B inserts+commits a row into a scratch table; session A `SELECT count(*)` — if A sees the row the `SET` took no snapshot (safe to move `SET LOCAL` first); if not, it did | no — informs R1's fix |

Confirmed CLEAN by all three houses and, where cheap, by me: item a guard and MONDAY/`models.py` untouched · b1 `validate_band` refuses inverted/non-int/negative/`bool` and accepts the five bands, exactly two call sites, half-set unchanged, no fact computation touched · c `SET LOCAL` inside the read-write branch before `_apply` (forward and rollback share it), `--proof-only` returns earlier, `LockNotAvailable` wrapped with rollback and NOTHING WAS APPLIED, `0`/negative/non-int/bool refused before any connection (`cli.py:616-626` vs `_connect` at `:645/:657`), `DIGEST_EXCLUDED_COLUMNS` untouched · d only test-file hunks, `statement_timeout` 25 s > 20 s, pid threaded through the one call site · e `CODE_ROOT` from `__file__`, existing `committer._git`, `UNKNOWN` fallback · nothing under `configs/` or `ops/`, no L52 reach, no secret.

Other, unverified: grok's stale-sha note (report says main `3723d86`, `facts.md` says `fc4acd6`) is explained by the desk's `facts.md` (main moved again); not raised as a finding.

OPS CHECK · grok: 0/2/1 FIX FIRST F2 · gemini: 1/1/1 FIX FIRST 1, 2, 3 · astra: 5/1/0 FIX FIRST 1, 2, 3, 4, 5, 6 · houses that read it: 3 of 3 · hub-verified REAL: 8 (blocks shipping: 0) · NOT REAL: 1 · UNVERIFIABLE: 2
