MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-0919a`, launched by the CTO desk in the background. RULES: `Bash(grok *)` + `Bash(agy *)` = Dejan's R9 of 2026-09-18 14:33 ET (standing for the review runs of the weekend push, 09-18 → 09-20); ONE NEW RULE, approved by him on 2026-09-19 (row R1 of `cto-2026-09-19.md` §4): `Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)`; every other rule is a read that ran in yesterday's four review hubs. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-0919a --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-harness-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; allowlist per session (L55, L61, L62); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; Grok, Gemini (agy), Astra (Codex) = one headless review each · nobody sits at this terminal — the report file is your channel (`FAILED:` stops the run safely).

# Tribunal check of the BUILD (LAWS L67: every build is checked by at least three tribunal members before it ships) — the two migration-harness changes made after yesterday's reviews: F1 (`0e1f768`, row count taken inside the digest fold) and the SNAPSHOT fix (`ee9bbcb` red tests + `a70e286`: `cobalt db migrate` and `--proof-only` run at REPEATABLE READ). This code runs against PRODUCTION in today's deploy. Three independent readers — Grok, Gemini, Astra — then you verify every BLOCKER and MAJOR against the real files (L35). DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words, committed on main: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §4 **R9** (14:33 ET "Approved" — `Bash(grok *)`, `Bash(agy *)`, standing through 09-20) and **R18–R21** (the tribunal, LAWS L67); `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-19.md` §4 **R1** (the Codex rule above, his word and the time). Prove both with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. The record does not match this file → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds (this is round 1; the desk decides on a round 2).

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` — its §1 staging rules and §3 verification rules and table shape BIND you here unchanged; `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-migrate-fix/REVIEW.md` = the last run where both Grok and Gemini worked (small packet, printed answer). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/REVIEW.md` (gitignored scratch; you commit nothing) — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.
UNATTENDED RULES: one bare command per Bash call, exactly the listed prefix, no pipe, no redirect, no `cd`; long runs go `run_in_background`; files via Write. PREFLIGHT (METER, L47), one row each (rule · command · exit · allowed/DENIED + reason verbatim): `grok --version` · `agy --version` · `date` · THE CODEX PROBE, `run_in_background`, give it 3 minutes: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."`. Probe = `OK` + exit 0 + no usage-limit text → Astra is UP. A denial of that command, a usage-limit message, no answer in 3 minutes, or a hang on input → record `astra: METER` / `astra: HARNESS` with the text verbatim and CONTINUE WITH TWO HOUSES — this one outcome is NOT a failed run (the desk planned for it; the Codex meter was spent yesterday). A denial of `grok` or `agy` → `FAILED PREFLIGHT: <rule>`, stop.
LESSON THAT BINDS THE PACKET (yesterday's delta review, both houses HARNESS): a reviewer in a reads-only sandbox cannot RUN anything — not `diff`, not `git`, not `python`. Every diff is PRE-COMPUTED by you into a file; QUESTIONS.md never asks a reviewer to compute, compare files itself by command, or run a test.

## 1. Stage the packet in `scratch/review-harness-0919/` (`mkdir -p` first; command output → Write, byte-identical)
(1) `f1.diff` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show 0e1f768` · (2) `snapshot-tests.diff` = `… show ee9bbcb` · (3) `snapshot-fix.diff` = `… show a70e286` · (4) `cli.py` = `git -C /Users/cobalt/cobalt-wt/s2-p2-cards show sprint-2/stack:src/cobalt/db_migrations/cli.py` (the module as it ships, 604 lines) · (5) `build-report.md` = `/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/migrate-snapshot-fix-2026-09-18.md`, whole · (6) `context.md` — YOU write it, facts only, each quoted from `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-18.md` §20, §24, §26, §31 with the section named: what the harness does (one transaction: BEFORE probe of every table → apply → AFTER probe → any unexpected `CHANGED` → rollback); the 1 GB `string_agg` failure and the streamed fix; the two production `--proof-only` runs (rows, seconds); why the snapshot fix exists (≈100 s between the probes; `com.cobalt.heartbeat` every 15 min and `com.cobalt.seat-usage` hourly write `system.cobalt_jobs`, `cobalt_redactions` appends; the deploy stops only the two residents); Postgres 16; the deploy migrates 0006/0007 (DDL: new tables and columns for radar cards) with both residents DOWN · (7) `QUESTIONS.md`, verbatim from the block below. If a `show` prints more than the Write tool takes in one call, write the file in ordered parts `name.part1`, `name.part2` and say so in `## Packet`.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — every diff you need is already here as a file. `context.md` says what the tool is and why it changed. The code in `cli.py` will run TODAY against a live production Postgres 16 database for a trading-support system, inside a deploy that has two services stopped while it runs. Two changes are under review: `f1.diff` (the row count is now taken inside the same streamed pass that computes the content digest) and `snapshot-tests.diff` + `snapshot-fix.diff` (the migrate transaction and the read-only `--proof-only` mode now run at REPEATABLE READ so that a commit by another session between the BEFORE and AFTER probes can no longer make a good migration read CHANGED and roll back). `build-report.md` is the builder's own account — treat its claims as claims. For every finding give SEVERITY (BLOCKER = can fail mid-deploy, leave production wrong, or make the proof lie / MAJOR / MINOR), FILE + LINE, the EVIDENCE you read, and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) Where exactly is the isolation level set, and is it in force BEFORE the first statement whose snapshot matters? In Postgres the REPEATABLE READ snapshot is taken at the first query after BEGIN — is there any statement between BEGIN and the BEFORE probe (advisory lock, `SET`, catalog read) that changes what the snapshot means, or any path where the level silently stays READ COMMITTED (connection reuse, autocommit, a nested transaction, a driver default)? (Q2) The migrations are DDL (CREATE TABLE, ALTER TABLE ADD COLUMN, CREATE INDEX, GRANT) plus a few row statements. Does anything about DDL inside a REPEATABLE READ transaction behave differently from READ COMMITTED in a way that matters here — lock waits against the still-running scheduled jobs, a serialization failure on a catalog row, `ALTER TABLE` forms that rewrite a table? (Q3) The serialization-failure path (`could not serialize access`): is it caught, named in plain words for an operator, rolled back, and returned as a NON-ZERO exit — and never retried silently? Is there any path where it is swallowed and the run reports OK? (Q4) `--proof-only`: are READ ONLY and REPEATABLE READ both in force together, in an order Postgres accepts, and can this mode still never reach the apply code? (Q5) THE PROOF'S MEANING: before the change, a concurrent commit made the proof say CHANGED. Now the proof cannot see it. Name any case where that hides something the operator needed to know — a concurrent writer touching a table the migration alters, a row the migration's own statement would have matched had it been visible — and say whether the transaction would then fail loudly at commit or succeed silently. Is 'OK' still an honest statement, and of exactly what? (Q6) The five new tests: can each one actually fail? Does test (a) really commit from a SECOND session between the two probes? Does any test depend on timing, sleep or ordering luck? The builder says test (b) passes before the fix by design — is that acceptable or a test that proves nothing? (Q7) `f1.diff`: is the count taken from the same single pass as the digest, with no off-by-one at the first row, at batch boundaries or for an empty table, and is the digest byte-identical to before (separator placement unchanged)? Does any separate `count(*)` remain in the content proof? (Q8) Cost on an 8.6-million-row table: locks held for the whole transaction, the pinned vacuum horizon (≈100 s), memory of the streamed cursor under REPEATABLE READ, interaction with a `pg_dump` taken minutes before. Anything that gets worse than linear? (Q9) Anything else in `cli.py` as it ships that you would not run against production today. End with exactly one line: `VERDICT: SAFE TO DEPLOY / FIX FIRST <finding numbers> / DO NOT DEPLOY`."

## 2. Launch the reviewers — independent, neither sees another's findings, ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped
2.1 Grok, `run_in_background`: `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/**)" -p "Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/ . Start with QUESTIONS.md and follow it exactly. Write your findings to /Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/grok-findings.md and reply with only that path."` — never `--always-approve`.
2.2 Gemini, `run_in_background`: `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/ . Start with QUESTIONS.md and follow it exactly. Do NOT run any command and do NOT write any file: print your complete findings as your answer."` → YOU write its printed answer, byte for byte, to `gemini-findings.md`.
2.3 Astra, only if the probe said UP, `run_in_background`: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "You are GPT-6 Astra, the cross-house reviewer. Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-harness-0919/ . Start with QUESTIONS.md and follow it exactly. Do NOT run any command other than reading those files and do NOT write any file: print your complete findings as your answer."` → YOU write its printed answer, byte for byte, to `astra-findings.md`. A usage-limit message mid-answer → `astra: METER`, keep what it printed, marked partial.
2.4 While they run, do NOT pre-read the diffs for defects yourself — your job is verification, and an anchored verifier confirms what it expects.

## 3. Verify — `11-review-deploy-stack.md` §3 EXACTLY, against the REAL files on `sprint-2/stack` in `/Users/cobalt/cobalt-wt/s2-p2-cards` (say if a packet copy differs). Two or three houses naming the same defect = ONE row, all houses listed (agreement is evidence). One extra column per REAL finding: `blocks today's deploy? yes/no — why`. A claim about Postgres behaviour that the files cannot settle = `UNVERIFIABLE FROM READS`, with the exact experiment on `cobalt_dev` that would settle it (you do NOT run it). You fix NOTHING and edit NOTHING outside `scratch/review-harness-0919/`.

## 4. Close
Last line: `REVIEW DONE · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks the deploy: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
