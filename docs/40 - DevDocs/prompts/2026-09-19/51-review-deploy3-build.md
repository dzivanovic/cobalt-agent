MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify their findings; no write path · SEAT: review hub `review-d3-build-0919`, launched by the CTO desk in the background. NO NEW RULE: the `--allowedTools` list is `47-review-archiver-folds.md`'s — itself `29`'s, itself `20`'s — byte for byte, with exactly ONE string changed, the `mkdir` path. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/51-review-deploy3-build.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-d3-build-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-d3-build-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47). **ASTRA:** its Codex meter said "try again at 4:54 PM" and it is now past that — probe it ONCE (`codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."`, `run_in_background`). UP → Astra is the third house and L67's "three checkers on every development" is met in one round. Still METER → record it verbatim and end with two; the desk runs Astra separately.

# L67 BUILD CHECK of DEPLOY 3's code: the rebase resolutions + three fix chunks on `archiver/append-0919` (and `ops/2026-09-19`, green and unchanged since its own check)
This is the LAST gate before the deploy-3 prompt is written, and deploy 3 runs TONIGHT if this is clean (`cto-2026-09-19.md` §4 **R32** — Dejan overruled L43's one-deploy-per-evening for tonight only, with a hard edge: the 21:40 nightly backup). What is new since any house last read this branch: the rebase onto the post-P4 `main` with **nine conflicted files resolved**, five enumeration pins rewritten, a sixth (`requires_db`) pin rewritten, an L3 fold of two DB read methods into one, and **R34 "B"** — the backfill bounded out of 04:00–09:30 ET with the Finviz ceiling held at 50. Suites after it all: **offline 2146/0, db 2486/0**. DO NOT STOP until the report ends `D3 BUILD CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. Standing: LAWS.md **L67** (2026-09-18 17:30, `cto-2026-09-18.md` §4 R18 + R19–R21); `cto-2026-09-18.md` §4 **R9** (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20); `cto-2026-09-19.md` §4 **R1** (the Codex read-only rule), **R25**, **R26**, **R32**, **R33**, **R34**. Prove with `git -C /Users/cobalt/cobalt log --oneline -16 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`, stop. `ASK DESK: <question> [<time>]` for a question that does not stop you — safe default, continue, never wait.
Laws, one line each: L1 fail-loud · L3 one path (the fold IS an L3 job) · L9 loud degradation · L10 config-as-code · L33 reviewers are read-only · L35 trust the artifact, never a claim — verify against the real tree · L36 reviewers spawn nothing · L37 no model-judged approval · L42 restarts by rule · L44 same packet to every house · L45 real-artifact fixtures, never delete a test to green a suite · L47 meter before counting on a house · **L53 the ceiling and the cadence are Dejan's — R34 is the ruling being implemented, not a number this build chose** · L57 explainability · L67 ≥3 checkers, floor of one other house · L68 integrated gate · L69 settings in tests · L70 unproven escalates.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT and §2 launch spellings bind you UNCHANGED with `scratch/review-d3-build-0919/` in every path. `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/29-review-archiver-r2.md` §3 = the verification rules and table shape. `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-p4-2026-09-19.md` **FIRST RUN §1.2** (the verbatim conflict output — the map of what had to be resolved). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-d3-build-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line. You commit nothing.

**GROK INVOCATION — bind this spelling.** `grok --sandbox cobalt-job --allow "Write(<absolute scratch dir>/**)" -p "<prompt>"` — the `--allow` argument is the FOLDER GLOB ending `/**`, never one file path. Use `scratch/review-d3-build-0919/**`.

## 1. Packet in `scratch/review-d3-build-0919/` (`mkdir -p` first; Read → Write, byte-identical) — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/51-packet/`
(1) `fixes.diff` (43,622 B) — the four fix commits in full, docs excluded · (2) `commits.txt` (463 B) · (3) `registry-after.py` (5,122 B) — the merged migration registry as it now stands · (4) `rebase-report.md` (59,396 B) — the build's own account: §3's nine conflict resolutions file by file, then the CHUNK 2 and CHUNK 3 sections · (5) `QUESTIONS.md` (3,744 B), the same text every house receives (L44).
**Total ≈112 KB. Send TWO calls per house:** **A** = `fixes.diff` + `commits.txt` + `registry-after.py` + `QUESTIONS.md` (≈53 KB, covers Q2–Q7) and **B** = `rebase-report.md` + `QUESTIONS.md` (≈63 KB, covers Q1 and the context for the rest). Say in `## Packet` which bucketing each house got. **Never drop a file to make a bucket fit, and never let a call truncate silently.** Record every byte count and confirm each staged copy equals its source.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `29-review-archiver-r2.md` §3, EXACTLY, with `scratch/review-d3-build-0919/` in place of their paths. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.

You verify every BLOCKER and MAJOR against the REAL tree with Read and `grep` (no Bash rule needed for a plain read), in `/Users/cobalt/cobalt-wt/archiver-append/`: `src/cobalt/db_migrations/__init__.py` and `placement.py`, `src/cobalt/cli.py`, `src/cobalt/archiver/runner.py` and `store.py`, `src/cobalt/radar/notes.py` (the demand model), `tests/cobalt/test_archiver_migrations.py`, `test_archiver_append_store.py`, `test_archiver_runner.py`, `test_p4_migrations.py`, `test_radar_migration.py`, `test_radar_score_migration.py`, `test_tenancy.py`, every `db_migrations/00*.sql` and `*.rollback.sql`, and `configs/cobalt/taxonomy/tunables.yaml` (**`radar.finviz_max_rpm` must read 50** — check it yourself, the build's grep proof is a claim). Compare against `main` with `git -C /Users/cobalt/cobalt show*`. Re-derive every line number (L35).
A claim only a RUNNING command could settle — whether the suites are actually green, whether the REVOKE denies on a live database, whether a real 04:00 backfill is refused — is `UNVERIFIABLE FROM READS`: name the exact missing command; the build reports `offline 2146/0, db 2486/0` and that is EVIDENCE, not your verification. One extra column per REAL finding: `blocks deploy 3? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-d3-build-0919/`.

Last line, exactly this shape:
`D3 BUILD CHECK · grok: <SAFE TO DEPLOY|FIX FIRST <n>|DO NOT DEPLOY <n>|HARNESS|METER|TIMEOUT> · gemini: <same> · astra: <same> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks deploy 3: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
