MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify their findings; no write path · SEAT: review hub `review-archiver-folds-0919`, launched by the CTO desk in the background. NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `29-review-archiver-r2.md`'s launch line — itself `20-review-archiver-build.md`'s — with exactly ONE string changed, the `mkdir` path (`scratch/review-archiver-r2-0919` → `scratch/review-archiver-folds-0919`). This round reads a SMALLER diff than `29` did and needs nothing `29` did not carry. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/47-review-archiver-folds.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-archiver-folds-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-archiver-folds-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47). **ASTRA:** its Codex meter returned a usage limit today with "try again at 4:54 PM". Probe it ONCE (`codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."`, `run_in_background`): UP → Astra is the third house; a usage limit → record `astra: METER` verbatim and end with two houses, a normal ending. Do NOT retry, do NOT wait for the reset.

# House check of the APPEND-ONLY ARCHIVER's LAST UNREAD CODE (deploy 3, Sunday — OFF-LADDER, ruled `cto-2026-09-19.md` R4/R5/R8) — three commits, `9056e73..186c9a9` in `/Users/cobalt/cobalt-wt/archiver-append` (branch `archiver/append-0919`)
Every earlier round of this build went through the four-house tribunal (three rounds, `cto-2026-09-19.md` §11/§13/§16) and through three house checks of the build (`review-archiver-0919`, `review-archiver-r2-0919`, `review-archiver-grokwhole-0919`). These three commits landed AFTER all of that, because Dejan ruled on two red `requires_db` tests (R25 13:10 ET, R26 13:21 ET). **No house has read them.** L67 requires ≥3 checkers on a development and a floor of at least one house other than the author — this round closes that gap before deploy 3's prompt is written. DO NOT STOP until the report ends `ARCHIVER FOLDS CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. Standing law, not a one-off approval: LAWS.md **L67** (2026-09-18 17:30 ET, `cto-2026-09-18.md` §4 R18 with R19–R21); `cto-2026-09-18.md` §4 **R9** (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20); `cto-2026-09-19.md` §4 **R1** (the Codex read-only rule), **R25** and **R26** (the two rulings these commits implement). Prove with `git -C /Users/cobalt/cobalt log --oneline -6 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. `ASK DESK: <question> [<time from date>]` for a question that does not stop you — take the safe default and continue, never wait.

Laws, one line each: L1 fail-loud · L28 vault-write law (the archiver's append-only carve-out is in it) · L33 reviewers are read-only verification runs · L35 you trust the artifact, never a reviewer's claim and never this prompt's summary — verify against the real tree · L36 reviewers spawn nothing · L37 no model-judged approval · L44 every house gets the same packet · L45 test against the real artifact · L47 meter checked before a house is counted on · L57 explainability · L67 ≥3 checkers, floor of one other house · L69 a test may read `"user".trader_settings` to prove an invariant, never to assert a value · L70 an escalate whose evidence is "not run" is unproven.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT and §2 launch spellings bind you here UNCHANGED, with `scratch/review-archiver-folds-0919/` in every path. `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/29-review-archiver-r2.md` §3 = the verification rules and table shape. `/Users/cobalt/cobalt-wt/archiver-append/docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` — §0 Headline, the round-3 and round-3b sections, the DB-re-run section, and the LAST line (`ARCHIVER DB2 f5df6c1 … db 161/0 …`). `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/12-packet/` — the FINAL archiver design, §11 in particular (its "nothing granted" clause is what R25 makes explicit, and its "follows 0006's pattern" sentence is an OPEN wording item, R3-1 — NOT yours to fix). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-folds-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line. You commit nothing.

**GROK INVOCATION — bind this spelling and do not improvise it.** `grok --sandbox cobalt-job --allow "Write(<absolute scratch dir>/**)" -p "<prompt>"` — the `--allow` argument must be the FOLDER GLOB ending in `/**`, never one file path (a single-file pattern HARNESSes on this host, proven repeatedly today). Use `scratch/review-archiver-folds-0919/**`.

## 1. Packet in `scratch/review-archiver-folds-0919/` (`mkdir -p` first; Read → Write, byte-identical) — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/47-packet/`
(1) `folds.diff` (11,651 B) — the COMPLETE code diff `9056e73..186c9a9`, docs excluded · (2) `commits.txt` (344 B) — the three commits with their subjects · (3) `rulings.md` (1,270 B) — R25 and R26 verbatim from `cto-2026-09-19.md` §4 · (4) `QUESTIONS.md` (2,975 B), the same text every house receives (L44).
**Total ≈16 KB — ONE call per house, no bucketing.** Record every file's byte count and confirm each staged copy equals its source; a copy that differs by so much as a trailing tab is named in `## Packet` with both byte counts.

QUESTIONS.md (verbatim): see `47-packet/QUESTIONS.md` — copy it byte-for-byte into the scratch dir as staging step (4). Five questions: (Q1) R25's REVOKE — exactly that and nothing more, complete incl. the sequence and the default-privilege path, idempotent, rollback clean, no other role broken · (Q2) R26's dropped test — the three offline pins kept and actually pinning, coverage lost, leftovers · (Q3) `d2099e1`'s two-session advisory-lock test — does it prove mutual exclusion, can it hang or leak · (Q4) **does anything in this diff change PRODUCTION behaviour** rather than test behaviour (the build ships in `upsert` mode = today's behaviour byte for byte; anything else is a BLOCKER) · (Q5) anything else you would not ship.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `29-review-archiver-r2.md` §3, EXACTLY, with `scratch/review-archiver-folds-0919/` in place of their paths. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.

You verify each BLOCKER and MAJOR against the REAL tree with Read and `grep` (no Bash rule needed for a plain read): `/Users/cobalt/cobalt-wt/archiver-append/src/cobalt/db_migrations/0010_archive_progress.sql`, `0011_archive_incidents.sql` and both `.rollback.sql` files, `0001_schemas.sql` (the blanket GRANT and the `ALTER DEFAULT PRIVILEGES` lines R25 overrides), `0006`'s grant pattern (the design's §11 cites it), `tests/cobalt/test_archiver_migrations.py`, `tests/cobalt/test_archiver_append_store.py`, `src/cobalt/archiver/store.py` and `runner.py`. Re-derive every line number yourself (L35). A claim only a RUNNING command could settle — whether the REVOKE actually denies `cobalt_user` on a live database, whether the suite is green — is `UNVERIFIABLE FROM READS`: name the exact missing command and note that the DB re-run already reports `db 161/0` with `DB-1 tests PASSED/PASSED` and the REVOKE apply-twice idempotent, which is EVIDENCE, not your own verification. One extra column per REAL finding: `blocks deploy 3? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-archiver-folds-0919/`.

**NOT this round's business, and not findings:** the design's §11 "follows 0006's pattern" wording (open ruling R3-1, the desk's), the `append`-mode switch (his ruling on the shadow numbers, never this build's), the shadow-file date question (R2-1), `poller_writable` (C1), the shadow-night count (O-1) and O-6. Note them in §0 if you like; do not count them.

Last line, exactly this shape:
`ARCHIVER FOLDS CHECK · grok: <SAFE TO DEPLOY|FIX FIRST <n>|DO NOT DEPLOY <n>|HARNESS|METER|TIMEOUT> · gemini: <same shape> · astra: <same shape> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks deploy 3: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
