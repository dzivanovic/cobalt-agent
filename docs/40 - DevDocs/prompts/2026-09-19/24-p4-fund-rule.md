MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50; L29 floor — this chunk is all `src/`+`configs/`+`tests/`, never built by the hub itself) · SEAT: same hub/builder shape as `13-p4-verify.md` (Sonnet hub, no new rule) · SESSION: fresh, remote-control `p4-fund-rule-0919` · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it · METER: Anthropic small (ONE Opus chunk, small diff; probe first, L47); Codex = NONE; Grok = NONE; Finviz = NONE this run — no export request; the two 09-19 raw exports already in gitignored `scratch/` and the committed 151-column fixtures are read-only reused · nobody sits at this session's terminal — your channel is the report file (a written `FAILED:` line stops the run safely; a dialog gets none).
Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p4` then
`claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/24-p4-fund-rule.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-fund-rule-0919 --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(uv run python tests/fixtures/replay/_cut_p4_fixtures.py*)" "Bash(claude -p *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" "CronCreate" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`

# S2-P4 fund rule (R16 "C") — one config-shape chunk, tests first, no rebase, no worktree creation (both already done). Builds ONLY what `13-p4-verify.md` step 2.4 left ESCALATED. DO NOT STOP until the report ends `P4 FUND RULE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk, not by Dejan; a prompt file is not an approval. The RULE this chunk builds is his: `docs/40 - DevDocs/reports/cto-2026-09-19.md` §4 **R16** (11:05 ET, "C" — "a row is dropped as not-equity when `Asset Type` is non-blank OR `Industry` = `Exchange Traded Fund`"), drafted in §32's draft (c). The PERMISSIONS this chunk runs under add NO new rule: every `--allowedTools` string above is copied byte for byte from `13-p4-verify.md`'s already-approved launch line (itself sourced from `cto-2026-09-17.md` §4 R6/R5 and `cto-2026-09-18.md` §4 R1) — prove it yourself before step 1: sort both files' rule strings and `comm -23` this file's list against `13-p4-verify.md`'s list must print NOTHING (every line here already exists there); the reverse `comm -13` (this file vs 13) is expected non-empty — those are the rules 13 needed that this chunk does not (no migrate/validate/replay-nightly, no real-vault pytest, no Finviz fetch + its `rm`, no `git rebase main` + `--continue`, no `touch scratch/*`). A string here that `comm` cannot find on 13's side → `FAILED: authorization mismatch — <string>`, commit the report, stop. Laws behind the sensitive steps: L61, L62, L63, L50, L29, L55, L41 interim (`.env` by name only, used only in step 6, removed after), L47 (meter is a precondition).
YOU CAN ALWAYS STOP. `FAILED: <step> — <concern>` as the report's last line, wip-commit (RECOVERY RULE), stop. `ASK DESK: <question> [<time>]` for a question that doesn't stop you — take the safe default named in the step, continue; never block, never message anyone.
RECOVERY RULE (L60): partial work is never discarded — `git add <each changed path, by name>` + `git commit -m "wip(s2-p4): fund-rule <chunk> partial — <why>"`, last line `CONTINUE: step <n>`; the same launch line resumes from it. EVERY `git add` names explicit paths under `src/`, `configs/`, `tests/`, `docs/` — NEVER `scratch/`, never `-A`, never `.` (review finding on 13, R11: `scratch/` is NOT gitignored on this branch and holds tickers from real screens, L32). After every commit: `git show --stat HEAD` lists no `scratch/` path, else `FAILED: <step> — scratch path committed <path>`. BEFORE ANY STOP LINE: if `.env` may exist, `rm` it and prove with `ls -la .env` (gitignored — `git status` cannot prove its absence).
BUILDER LAUNCH (unchanged, byte for byte — `13-p4-verify.md`'s own RECOVERY RULE paragraph): every `claude -p` you launch is the shape of `docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md` step 2's `--permission-mode acceptEdits --allowedTools "Bash(uv run pytest *)" "Bash(uv run python *)" "Bash(uv run cobalt validate*)" "Bash(uv run cobalt replay *--dry-run*)" "Bash(uv run cobalt smoke *)" "Bash(uv run cobalt --help*)" "Bash(git status*)" "Bash(git diff*)" "Bash(git log*)" "Bash(git show*)" "Bash(ls *)" "Bash(grep *)" "Bash(rg *)" "Bash(cat *)" "Bash(sed -n *)" "Bash(wc *)" "Bash(mkdir -p *)"` list byte for byte, ONE bare command without a leading `cd … &&` (you are already in the worktree). That list already covers everything this chunk's builder needs (`uv run python *` covers the fixture cutter; no DB, no vault, no Finviz — none of that list needs touching). A killed builder's uncommitted edits are wip-committed by you before any relaunch.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 + §6 → Memory `INDEX.md` → `## NOW` in `areas/cobalt.md` → `LAWS.md` IN FULL → `docs/40 - DevDocs/reports/cto-2026-09-19.md` §4 R16 (the ruling) → this worktree's `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` §2.2–§2.5 and `## ESCALATE` 1 (the evidence: 16,307 files / 643,998 rows / 5,702 distinct fund tickers / 28 distinct `Asset Type` values / table C = 16 rows, 8 distinct tickers) → `docs/40 - DevDocs/prompts/2026-09-19/13-p4-verify.md` step 2.4 (the rule description that was NOT built) and steps 2.2/2.3/2.5 (the evidence mode and fixture mechanics that WERE built and stay as they are) → `configs/cobalt/radar.yaml` → `src/cobalt/radar/config.py` → `src/cobalt/radar/runner.py:144` → `src/cobalt/replay/movers.py` → `src/cobalt/replay/models.py:305-314` → `tests/fixtures/replay/_cut_p4_fixtures.py`. Laws, one line each: L1 a bad config shape crashes with a line number, never a silent empty rule · L3 one function decides not-equity, called by radar AND movers, no second copy · L10 Pydantic schema, old shape refused by name · L32 counts only, never tickers, anywhere you write · L42 config-shape change → RESTARTS named · L45 tests specified against real-shape rows, never invented, never hand-edited to fit · L46/L60 one run, one commit, wip on interrupt · L47 meter is a precondition, checked before the builder launches · L53 no Finviz request this run — say so · L57 the new rule marker replaces `not_equity_values` in what gets stored, so replay stays replayable. Report (L48): append to `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` in THIS worktree — new `## FUND RULE (R16)` section at the end, existing content untouched. Run `date` before writing any time.

UNATTENDED RULES: one command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`; `cd <path>` its own call. Long runs (`claude -p`, the suites) `run_in_background`. NOT in your list: `git rebase*`, `git reset*`, `git checkout*`, `git stash*`, `git push*`, any `COBALT_ENV=production` command, `python` other than the one listed script, `--allow-prod`, `launchctl`. MID-RUN DENIAL = `FAILED: <step> — <command> — <reason string>`, wip-commit, stop. Never retry denied in another shape.

PREFLIGHT (one row each: rule · command · exit · allowed/DENIED): `date` · `git status --porcelain` (expect empty; tip `d5947e7` per the last READY line — anything else → `FAILED PREFLIGHT: worktree not clean — <lines>`) · `git status` (long form; "rebase in progress" → `FAILED PREFLIGHT: rebase in progress` — none is expected, you never rebase here) · `git log --oneline -1` (first launch: `d5947e7`; a resume: the tip the last `CONTINUE:` left) · `git -C /Users/cobalt/cobalt status --porcelain` · `git -C /Users/cobalt/cobalt log --oneline -1` (record it) · `ls -la scratch/movers-gainers-raw.csv scratch/movers-losers-raw.csv` (expect present, ~11,481,555 bytes each, from the 09-19 09:2x ET fetch — confirms no new Finviz call is needed) · `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (meter probe, L47 — a usage-limit message: record reset time, `CronCreate` a wake-up at reset+5min, commit report, wait) · `uv run pytest --co -q tests/cobalt/test_replay_movers.py`. Any denial → `FAILED PREFLIGHT: <rules>`.

## 1. FACTS (verify each yourself; a mismatch is a finding, not silently corrected)
Current shape — `configs/cobalt/radar.yaml:22-24`:
```
not_equity:
  header: Asset Type
  values: [Exchange Traded Fund]
```
`src/cobalt/radar/config.py:81-84` — `class NotEquityConfig(BaseModel)`: `model_config = ConfigDict(extra="forbid")`, `header: str = Field(min_length=1)`, `values: list[str] = Field(min_length=1)`. Consumed at `radar/runner.py:144`: `if row.get(self.config.not_equity.header) in self.config.not_equity.values: excluded.add(ticker)`. In `replay/movers.py`, `config.not_equity.header` (line 159) only picks which raw column feeds `MoverRow.asset_type` at parse time (`movers.py:162-171`); the actual not-equity TEST at benchmark time is `movers.py:347`, `if lead.asset_type is not None and lead.asset_type in not_equity_values`, where `not_equity_values: Sequence[str]` is a bare param threaded through `benchmark_misses` (`movers.py:328`) — NOT read from the shared config object. This is the L3 duplicate the 09-19 verify hub found but did not fix (it built nothing of the rule).
`export.required_headers` (`radar.yaml:17`) = `[Ticker, Volume, Relative Volume, Asset Type]` — no `Industry`. `REQUIRED_HEADERS` (`replay/movers.py:83`) = `["Ticker", "Change", "Volume"]`, comment: "`Asset Type` is read when present" (line 82) — `Asset Type` is optional there today.
R16 needs an OR across TWO different headers (`Asset Type` non-blank, `Industry` = `Exchange Traded Fund`) — the current one-header/one-value-list shape cannot express it structurally, not just by content.

## 2. THE RULE SHAPE (config + Pydantic) — tests first (L1, L10)
Failing test first: a config fixture carrying the OLD shape (`header:`/`values:`) must be REJECTED — `extra="forbid"` plus the new required fields means it fails with those exact keys named (assert the `ValidationError`/`RadarConfigError` text names `header`/`values` as unexpected or `asset_type_header`/`industry_header`/`industry_values` as missing — your call which; either satisfies L1 as long as it is loud and names a key, never a silent pass).
New shape (name it exactly this way unless a real blocker forces a different name — if so, name the blocker in the report):
```
not_equity:
  asset_type_header: Asset Type
  industry_header: Industry
  industry_values: [Exchange Traded Fund]
```
```python
class NotEquityConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    asset_type_header: str = Field(min_length=1)
    industry_header: str = Field(min_length=1)
    industry_values: list[str] = Field(min_length=1)
```
`load_config`'s `needed` set (`config.py:135-140`) must require BOTH `config.not_equity.asset_type_header` and `config.not_equity.industry_header` to sit in `export.required_headers` (today it only checks the single `.header`) — a config where either is missing from `required_headers` still fails loud with the same message shape as today, key named.
Rewrite the comment block above `not_equity:` (`radar.yaml` lines 1-9): replace the "unresolved, watch the first live run" sentence with what 2.2's evidence actually found — counts only (L32): distinct fund tickers, distinct `Asset Type` values, table C row count, citing `s2-p4-verify-2026-09-19.md` §2.2. Never a ticker in this comment.

## 3. THE EVALUATOR — one function, one path (L3)
ONE function (e.g. `is_not_equity(row: Mapping[str, str], config: NotEquityConfig) -> bool`, on or beside `NotEquityConfig` in `cobalt.radar.config`) implementing exactly: `(row.get(config.asset_type_header) or "").strip() != "" or (row.get(config.industry_header) or "").strip() in config.industry_values`. Called by BOTH:
- `radar/runner.py:144` — replaces the inline comparison.
- `replay/movers.py`'s benchmark-time not-equity test (today `movers.py:347`). This needs `MoverRow` (`replay/models.py:305-314`) to also carry the row's `Industry` (add `industry: Optional[str] = None`, read at parse time in `parse_movers` the same way `asset_type` is read, `movers.py:162-171`), and `benchmark_misses`'s `not_equity_values: Sequence[str]` parameter (`movers.py:328`) replaced by the `NotEquityConfig` (or the bound evaluator) so ONE function decides for both callers — never a second copy of the OR.
Proof after: `grep -rn "not_equity" src/cobalt` shows both call sites reaching the one function; `grep -rn "\.values\b" src/cobalt/radar src/cobalt/replay` and `grep -rn "not_equity_values" src/cobalt/replay` show no inline re-implementation of the OR left anywhere (quote the grep output in the report).

## 4. HEADERS — both required, missing = loud FAILED (L1) — never "no funds"
`radar.yaml` `export.required_headers` gains `Industry` (now `[Ticker, Volume, Relative Volume, Asset Type, Industry]`).
`replay/movers.py` `REQUIRED_HEADERS` (line 83) gains `"Asset Type"` and `"Industry"`; delete the now-false comment at line 82 ("`Asset Type` is read when present"). `parse_screener_csv`'s existing required-headers check (already the mechanism that fails loud on any missing declared header) now covers both — test it: an export missing either header raises, naming the header, through the SAME path as a missing `Ticker`/`Change`/`Volume` today; never a silent `None`/"unreported" for a MISSING COLUMN.
`unreported` (`movers.py:21-23` docstring, `:372`): re-run 2.4's own check — `ls /Users/cobalt/cobalt/data/radar-cache/*/movers-*` (read-only). Expected empty (P4 never deployed; no retained production movers export exists). If empty: the `unreported` label now means only a ROW-LEVEL blank value on a header that IS present (an ordinary stock legitimately has no `Asset Type`/`Industry` payload) — keep the label for that case, rewrite the docstring sentence to say a column is never missing once required, only a value may be blank. Do not delete the branch (this differs from step 2.4's original wording, which expected the whole branch to go — that assumed `Asset Type` alone became required; `Industry` becoming required too does not change this outcome, but the reasoning is restated so a future reader is not confused). If that `ls` finds files: STOP this item, `ESCALATE` the file list exactly as 2.4 said, change nothing about the branch.

## 5. TESTS AGAINST REAL-SHAPE ROWS (L45) — failing before the fix, passing after
Sources, in this order of preference: (a) the ALREADY-COMMITTED `tests/fixtures/replay/movers-{gainers,losers}.real-shape.csv` (151 columns, both `Asset Type` and `Industry` present; verified now: gainers has 29 non-blank-`Asset Type` rows / losers 19, ZERO rows of the blank-`Asset Type`+fund-`Industry` class in either — checked directly). (b) for the blank-type/fund-industry class specifically (the 8-ticker class table C found), a real row is not in the committed 60-row cuts — extend `_cut_p4_fixtures.py`'s `cut_movers()` (the existing `_first_fund_row()` helper at line 260 already guarantees ≥1 non-blank-`Asset Type` row survives the cut; add the same guarantee for ≥1 row where `Industry == Exchange Traded Fund` and `Asset Type` is blank, scanning `scratch/movers-{side}-raw.csv` — the same two files already read for evidence, no new fetch) OR, if neither raw export contains one in a bounded scan, cut it from wherever `data/radar-cache/*.csv` evidence located the 8 tickers (`uv run python tests/fixtures/replay/_cut_p4_fixtures.py*` — already an unchanged rule). Never fabricate a row (L45, N2 from 13's own step 2.5): if truly none exists after both scans, say so and test only the three provable cases below, `ASK DESK` for the fourth.
Cases (counts only in anything committed, L32):
- non-blank `Asset Type` + fund `Industry` → dropped (the 5,702-ticker class).
- BLANK `Asset Type` + fund `Industry` → dropped (the 8-ticker class, table C(ii)).
- blank `Asset Type` + non-fund `Industry` → kept (an ordinary stock).
- non-blank `Asset Type` + non-fund `Industry` → dropped, AND the count of rows hitting this arm is asserted/emitted (table C(i) was 0 on the full sample — a future non-zero must be visible, never silently absorbed into a boolean).
- either required header missing from an export → `parse_screener_csv` raises, naming the header — FAILED, not "no funds".
Existing tests on the OLD field names that must now fail before your fix (grep for `not_equity_values=` — at least `tests/cobalt/test_replay_movers.py` lines ≈108, 124, 135, 337, 349, 358, plus `test_not_equity_movers_leave_the_benchmark_and_unreported_asset_type_stays_in` at ≈354-360 whose scenario changes meaning under R16 — a blank-`Asset Type` fund is now DROPPED, not kept-and-marked-`unreported`; rewrite its assertion, don't delete the test) and `tests/cobalt/test_radar_replay.py` lines ≈228-238 (builds a modified `not_equity` config via `.values`/`.header` — rewrite for the new field names) and `tests/cobalt/test_radar_pool.py:110` `test_inactive_degraded_and_not_equity_precedence` (check its fixture row still exercises the intended precedence under the new shape). List every renamed/rewritten test in the report, red-before/green-after.

## 6. COUNTS-ONLY RE-RUN OF TABLES A/B/C UNDER THE NEW RULE (over the SAME scratch evidence, no new fetch)
`_cut_p4_fixtures.py`'s `collect_evidence()` (lines ≈423-500) already computes, per row, `asset_type` (stripped `Asset Type`), `is_fund_industry = industry == FUND_INDUSTRY` (stripped `Industry` == `Exchange Traded Fund`) — the condition `asset_type or is_fund_industry` IS R16's rule C, already evaluated in that loop today (it currently only feeds table membership, not a total). Add one field to `EvidenceReport` (line ≈387), e.g. `dropped_under_new_rule: int`, incremented in that same loop whenever `asset_type or is_fund_industry` is true, and surface it in `render_evidence`/`summarize_evidence`. "Stock rows hit" = `len(table_c_non_blank_not_fund)` (table C(i)), already computed — surface it explicitly in the summary line too if it is not already obvious there. Re-run: `uv run python tests/fixtures/replay/_cut_p4_fixtures.py evidence` → read `scratch/asset-type-evidence.md` → copy INTO THE REPORT: `dropped_under_new_rule` (expect = every row counted across tables A + B, i.e. every fund row) and table C(i)'s count (expect 0 — "stock rows hit"). A non-zero stock-rows-hit is `ESCALATE`, not a fix — the run continues, nothing further of this chunk changes because of it.

## 7. SUITES
Offline: `uv run pytest -q tests/cobalt tests/taxonomy` — quote the line verbatim; the 09-19 verify report's last READY line said `offline 1796/0` — 0 failed is required, the total may differ (new/renamed tests) and that is expected, say so, do not force the old number.
DB (item 6 of the brief): `cobalt_dev` is FREE — the 09-19 verify hub left it at `0001–0009` and released the lane at its `READY` line; no other session holds it today. This chunk's changes touch no migration and no DB-facing code, so the DB suite is a confidence re-run only, not required to build the rule — run it: `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` → quote the line (09-19's was `2114/0`) → `rm /Users/cobalt/cobalt-wt/s2-p4/.env` → `ls -la .env` shows it gone → `git status --porcelain` shows no `.env`. State explicitly in the report what `cobalt_dev` holds after (expect unchanged: `0001–0009`, no migration ran). No builder runs while `.env` is in the worktree — finish all of steps 2-6 (code + tests) BEFORE this DB re-run.

## 8. RESTARTS
`uv run cobalt jobs restarts main..HEAD` (this branch's own commits) — expect `com.cobalt.radar` at minimum (the `radar.yaml` `not_equity` shape change, L42); `replay/movers.py`/`models.py` changed too, so the one-shot `com.cobalt.replay` bootstrap may also classify — record whichever the tool actually names. UNCLASSIFIED → `ESCALATE`, never dropped.

## 9. CLOSE
`uv run pytest -q tests/cobalt tests/taxonomy` → 0 failed (quote) · `git diff --stat` (bound: `configs/cobalt/radar.yaml`, `src/cobalt/radar/config.py`, `src/cobalt/radar/runner.py`, `src/cobalt/replay/movers.py`, `src/cobalt/replay/models.py`, `tests/fixtures/replay/_cut_p4_fixtures.py`, the named test files — a path outside this list is a finding, not silently kept) · commits: `test(fixtures): dropped-under-new-rule count in the evidence mode` · `fix(s2-p4): not_equity = non-blank Asset Type OR Industry = Exchange Traded Fund, one rule for radar and movers (R16 \"C\")` — bodies name the evidence counts and R16. Append `## FUND RULE (R16)` to `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` (existing content untouched, this section and the new last line only): what was built (rule shape old→new, one line each), the grep proofs from step 3, the test list from step 5, the counts from step 6, the suite lines from step 7, RESTARTS from step 8, any ESCALATE. Commit: `docs(report): s2-p4 verify — fund rule R16 built, folded onto the verify report`.
Last line, exactly this shape:
`P4 FUND RULE <tip> | offline <p>/<f> | db <p>/<f-or-NOT RUN> | dropped <n> kept n/a | stock rows hit <n> | RESTARTS: <line>`
or `FAILED: <step> — <reason>`.
Then stop. The desk verifies the artifact (L35) and routes it into P4's existing three-house build check (OWED, `cto-2026-09-19.md` §32).
