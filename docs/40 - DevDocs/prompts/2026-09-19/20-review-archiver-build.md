MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify their findings; no write path · SEAT: review hub `review-archiver-0919`, launched by the CTO desk in the background. NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `18-review-ops-0919.md`'s launch line (itself copied from `14-review-p4-verify.md`'s), with exactly ONE string changed — the `mkdir` path, from `scratch/review-ops-0919` to `scratch/review-archiver-0919` — because this check needs no rule `18` did not already carry (the target branch's commits are reachable with the SAME `git -C /Users/cobalt/cobalt show*` / `log*` rules: `archiver/append-0919` is a branch in the same repository `18` already had `show`/`log` access to, so no `git -C .../archiver-append *` rule is added; the unused `git -C .../s2-p2-cards *` rules stay, unused, because they are not the string that changed). If this check turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/20-review-archiver-build.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-archiver-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-archiver-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of a BUILD (LAWS L67: every build is checked by at least three tribunal members before it ships; Fable is NOT one of the three checkers today — the checkers are Astra (OpenAI), Grok (xAI), Gemini (Google)) — branch `archiver/append-0919` (code tip `c974fbf`; the report commit `7c972ee` sits above it; merge-base with `main` is `8838dda`, `main` now `0569727`), the append-only Bar Archiver rebuild per the tribunal's FINAL design: settings + write_mode flag, migrations 0010/0011 (`system.archive_progress`, `system.archive_incidents`), the pure `reconcile.py` core, append-path store methods on one transaction, the runner's mode-isolated dispatch + pre-write shadow compare, the quiet-window-gated repair commands, and the heartbeat's new incident probe. This code has not merged and does not run against production today; it deploys in `upsert` mode only (today's write path, unchanged, plus one new advisory lock and a read-only shadow compare) — nothing switches to `append` without the owner's own ruling on shadow-night numbers. Its 18 `requires_db` tests have never run (offline build). The check is due now per the build's own stop line, which names "three-house check" first in its `OWED:` list. Round 1 of at most 3 (L67: ≤3 rounds per house while meter allows, never fewer than 2 houses total). DO NOT STOP until the report ends `ARCHIVER CHECK …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. This check is standing law, not a one-off approval: LAWS.md L67 ("every DEVELOPMENT (build) is checked by at least THREE members of the tribunal"), ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, emergency/override/rounds clauses R19–R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The build's own debt: the report's last line names `OWED: three-house check, dev-DB run after p4-verify's stop line, deploy prompt (upsert), his approval, shadow nights, his switch ruling` — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- archiver/append-0919 -- "docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md"` (**name the branch ref**: unlike a report committed on `main`, this report lives ONLY on `archiver/append-0919` — a bare `git log -- <path>` from `/Users/cobalt/cobalt` on the current branch will NOT find it; the branch ref must be given explicitly, same lesson `18`'s run recorded). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the build's own report's claims (`build-report.md` in the packet is the builder's account — treat it as a claim) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L52 nothing in this build may touch scoring, ranking or a value that reaches a card — confirm it does not, do not assume it · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close) and `2026-09-18/11-review-deploy-stack.md` §3 (verify) BIND you here UNCHANGED, with `scratch/review-archiver-0919/` in every path — including the LESSON: a reviewer in a read-only sandbox cannot RUN anything, so every diff is PRE-COMPUTED into a file, never asked of a reviewer by command. TWO LESSONS FROM `18`'S RUN, carried here: (a) the hub's Read→Write copy strips trailing whitespace, so a copied file can be shorter than its source by its blank-context lines — this is HARMLESS, state it once in `## Packet`'s fidelity table if you observe it, do not chase it as a defect; (b) `git log -- <path>` for anything committed only on a feature branch (this build's report, or any file that exists only on `archiver/append-0919`) MUST name the branch ref explicitly — a bare `git -C /Users/cobalt/cobalt log -- <path>` run from `main` will silently return nothing and look like a missing commit rather than a wrong command. REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-0919/REVIEW.md` — the same gitignored-scratch pattern `01`, `14` and `18` use, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/20-packet/`; copy each into `scratch/review-archiver-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `code-01-settings-migrations-config.diff`, `code-02-reconcile-store.diff`, `code-03-runner-shadow-quiet-cli-heartbeat.diff`, `code-04-tests-store-migrations-settings-heartbeat.diff`, `code-05-tests-quiet-reconcile-runner.diff` = the five `20-packet/code-*.diff` files — together they ARE `git -C /Users/cobalt/cobalt diff main...archiver/append-0919 -- src tests configs ops` (three-dot, so main's later docs-only commits never show as deletions), computed ahead of time by the desk: 31 files, 7,708 insertions / 38 deletions, split into five files by area ONLY because the whole diff is 328,366 bytes, over a comfortable single read — the five sum to exactly that byte count, verified by the desk before staging (no path double-counted, none dropped). `configs` and `ops` are UNCHANGED against this branch — the diff against those two paths (beyond the one `tunables.yaml` edit already in `code-01`) is empty, which is itself worth confirming, not assuming · (2) `facts.md` = `20-packet/facts.md` — the desk's pre-computed measurements: commit list, tip, merge-base, three-dot diff-stat by area, a file:line confirmation for each claimed design item, the design-§15-test-list-vs-actual-tests gap this hub already found (see Q10), the full stop line, and the numbered ESCALATE items · (3) `spec-final-design.md` = `20-packet/spec-final-design.md`, verbatim copy of the tribunal's FINAL design (`docs/30 - Design/ARCHIVER-APPEND-ONLY-FINAL-2026-09-19.md`) — what the design REQUIRES · (4) `spec-17-build-prompt.md` = `20-packet/spec-17-build-prompt.md`, verbatim copy of the build's own prompt (`17-archiver-append-build.md`) — what the builder was TOLD to build · (5) `build-report.md` = `20-packet/build-report.md`, verbatim copy of `docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md` as committed on `archiver/append-0919` (`git -C /Users/cobalt/cobalt show archiver/append-0919:"docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md"`) — the builder's own account of what it did; `QUESTIONS.md` calls this a CLAIM, not evidence · (6) `QUESTIONS.md`, verbatim below. If a file is too large for one Write call, split `name.part1`, `name.part2` and say so in `## Packet`.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three reading this packet separately. Read ONLY the files in this folder; you cannot run commands — everything you need is already here as a file. `spec-final-design.md` is the tribunal-derived FINAL design this build was told to build; `spec-17-build-prompt.md` is the literal build prompt the builder read. `code-01` through `code-05` are five files, split by area, that together ARE the whole non-docs diff `main...archiver/append-0919 -- src tests configs ops` (328,366 bytes total, split only because that exceeds a comfortable single read). `facts.md` is the desk's own pre-computed measurements (commits, tip, merge-base, diff-stat, file:line per claimed item) — read it, but VERIFY its claims against the `code-*.diff` files yourself where you can; it is the desk's own account, not settled fact. `build-report.md` is the builder's own account of its work — treat every claim in it as a CLAIM to verify against the diffs, never as settled fact. This code has not merged, does not run against production today, and its 18 `requires_db` tests have NEVER run (offline build, no `.env`, no `cobalt_dev`). It is queued for a deploy that will run two new migrations against a live production Postgres 16 database with an 8.4M-row `bars` table, for a trading-support system, and then run in `upsert` mode (today's behaviour, unchanged except for one new advisory lock) for N shadow nights before anyone considers switching it to `append`. For every finding give SEVERITY (BLOCKER = the code does not do what the design required, breaks an existing invariant, or contradicts a LAW (L1 fail-loud, L3 one-path, L10 config-validated-on-load, L45 real-shape fixtures, L52 nothing here may touch scoring/ranking/cards, L57 replayable/reconciling values) / MAJOR / MINOR), FILE + LINE (from the `code-*.diff` files), the EVIDENCE you read (quote it), and the FAILING SCENARIO; say 'none found' rather than inventing. (Q1) MODE ISOLATION (design §5): does `code-03` show the `write_mode` dispatch happening BEFORE any append-specific step, with the `upsert` branch sending the WHOLE export through `upsert_bars` exactly as today — no completeness filter, no range cut, no comparison gate, no withholding, no `archive_progress` write, no incident write? Is the 'complete list' of what an `upsert` night now does beyond today (the advisory lock, the pre-write shadow compare, one `shadow` key in `job.result` plus one artifact file) actually complete? (Q2) THE SHADOW COMPARE (design §5): does it run strictly after the fetch and before that target's write? Is it read-only? Does an exception or timeout inside it leave the target's `upsert_bars` call and the run summary IDENTICAL to a shadow-off night? (Q3) THE RE-CHECK INSIDE THE REPAIR TRANSACTION BEFORE COMMIT (design §8): confirm from `code-01`/`code-03` (quiet.py is in `code-03`) that Q1–Q3 of the quiet window are genuinely re-evaluated a second time immediately before COMMIT, not merely at command start, and that a failed re-check rolls the transaction back — no bar row surviving the write. Do the two named tests in `code-05` (`test_gemini_close_boundary_poller_lag`, `test_astra_open_boundary_repair_crosses_open`) actually exercise this, or only the start-time check? (Q4) `late` = key ≤ PREVIOUS `archived_through` (design §7): find the exact comparator in `reconcile.py` (`code-02`) — is it `<=`, matching the design's wording, or something else? Quote the line. (Q5) THE FIVE INCIDENT KINDS (design §11): does `0011_archive_incidents.sql`'s CHECK constraint (`code-01`) enumerate EXACTLY `gap, restated, stored_only, empty_export, regression`? Is the partial unique index `WHERE resolved_at IS NULL` with `NULLS NOT DISTINCT` actually present and syntactically valid for Postgres 16? (Q6) MODE ISOLATION + THE ADVISORY LOCK IN `upsert` MODE (design §9): confirm from `code-03` that the lock wraps BOTH the `upsert` and `append` dispatch paths, is SESSION-level, ONE constant key, and that a second holder refuses loudly. (Q7) Is production behaviour in `upsert` mode unchanged apart from that lock plus the shadow read and its one `job.result` key — walk the `upsert` branch in `code-03` end to end and name anything else you find. (Q8) Are the migrations in `code-01` safe on a live Postgres 16 with a large `bars` table — confirm both `CREATE TABLE` statements and their rollbacks are additive and touch nothing on `bars` (no ALTER, no index, no trigger on it). (Q9) Do the tests prove what they claim — spot-check `code-04`/`code-05` against at least three of `build-report.md`'s per-chunk claims, read the actual test body, and say whether it proves the claimed property or merely runs. (Q10) THE ONE NAMED GAP THIS HUB ALREADY FOUND: `facts.md` says the design's quiet-window bullet asks for 'differing-value and equal-value races' and this hub could not find a test with that shape in `code-05` — the two named boundary tests and the pre-commit-recheck test all assert only 'no bar row changed,' never a scenario where the poller and a repair race to write DIFFERENT values vs the SAME value with the outcome checked. CONFIRM OR REFUTE this from the real diff; if you find such a test, name it file:line; if you agree none exists, say so plainly — this is the single most load-bearing verification of this review. (Q11) CROSS-CUTTING: does anything in `code-01` through `code-05` (together the WHOLE non-docs diff) touch `src/cobalt/radar/`? Does anything reach scoring, ranking, or a card value (L52)? Any secret, credential, path or connection string visible anywhere? Is `configs/cobalt/taxonomy/tunables.yaml` the only config file touched, and is it an edit to an EXISTING file? (Q12) Does `build-report.md`'s own narrative (BASELINE/CLOSE numbers, the 8 numbered ESCALATE items, the §14 OPEN-item table, the RESTARTS line) hold up against the `code-*.diff` files and `facts.md` — name any claim you cannot confirm, marking it UNVERIFIABLE FROM READS rather than assuming it is true. End with exactly one line: `VERDICT: BUILD CLEAN / FIX FIRST <finding numbers> / BUILD UNSAFE`."

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-archiver-0919/` in place of their paths; for every allowlist claim quote the rule AND the step's command side by side. You verify each BLOCKER and MAJOR against the REAL files: a `code-*.diff` claim against `git -C /Users/cobalt/cobalt show archiver/append-0919:<path>` (the branch's tip content — reachable from `/Users/cobalt/cobalt` under the SAME `show*` rule, no worktree `cd` needed) and against the real files under `/Users/cobalt/cobalt-wt/archiver-append/` with Read and `grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (for example, an independent re-derivation of the diff itself with a fresh `git diff` against `/Users/cobalt/cobalt-wt/archiver-append` rather than reading the pre-staged files, or running `uv run pytest`/`uv run cobalt jobs restarts` to re-check a suite or RESTARTS claim) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. One attempt per house. You fix NOTHING and edit NOTHING outside `scratch/review-archiver-0919/`. Last line: `ARCHIVER CHECK · grok: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · astra: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that read it: <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`. Then stop.
