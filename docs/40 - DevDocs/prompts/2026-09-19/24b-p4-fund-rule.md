MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50; L29 floor — this chunk is all `src/`+`configs/`+`tests/`, never built by the hub itself) · SEAT: same hub/builder shape as `13-p4-verify.md` and `24-p4-fund-rule.md` (Sonnet hub, no new rule) · SESSION: fresh, remote-control `p4-fund-rule-0919b` · auto mode on; allowlist per session (L55, L61, L62), never `bypassPermissions`, push never in it · METER: Anthropic small (ONE Opus chunk, small diff; probe first, L47); Codex = NONE; Grok = NONE; Finviz = NONE this run · nobody sits at this session's terminal — your channel is the report file.
Two bare commands: `cd /Users/cobalt/cobalt-wt/s2-p4` then
`claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/24b-p4-fund-rule.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-fund-rule-0919b --allowedTools "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(uv run python tests/fixtures/replay/_cut_p4_fixtures.py*)" "Bash(claude -p *)" "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt *log*)" "Bash(git -C /Users/cobalt/cobalt status*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" "CronCreate" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`

LAUNCH-LINE PROOF: this list is IDENTICAL to `24-p4-fund-rule.md`'s — option B needs no schema change and no new permission. `comm -23 <(sort 24b's strings) <(sort 13's strings)` must print NOTHING (verified by the hub before this file was written: it does). If step 3 below turns out to need anything more than these strings, STOP and ESCALATE the exact string — never improvise one.

# S2-P4 fund rule (R16 "C") — retry after `p4-fund-rule-0919`'s FAILED (ESCALATE 17, `s2-p4-verify-2026-09-19.md` §"FUND RULE (R16)"). Starts from tip `e79d55d`. Same chunk, step 3 REDESIGNED (option B: decide at ingest, in memory — no migration). DO NOT STOP until the report ends `P4 FUND RULE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. The RULE is his: `cto-2026-09-19.md` §4 R16 (11:05 ET, "C"). The prior attempt's ESCALATE 17 recorded no ruling on the fork ("RULING (proposed, for the desk): none") — this file is the desk choosing option B on engineering grounds (no schema change, stays inside the already-approved permissions, keeps ONE evaluator) and re-launching; it is NOT a new HITL approval on the trading-logic content of the rule (R16 already covers that) and NOT a migration approval (none is used). Permissions add NO new rule — see LAUNCH-LINE PROOF above. A string here `comm` cannot find on 13's side → `FAILED: authorization mismatch — <string>`, commit, stop. Laws: L61, L62, L63, L50, L29, L55, L41 interim, L47.
YOU CAN ALWAYS STOP. `FAILED: <step> — <concern>` as the last line, wip-commit (RECOVERY RULE), stop. `ASK DESK: <question> [<time>]` for a non-blocking question — take the safe default, continue.
RECOVERY RULE (L60): `git add <each changed path, by name>` + `git commit -m "wip(s2-p4): fund-rule <chunk> partial — <why>"`, last line `CONTINUE: step <n>`. NEVER `scratch/`, `-A`, or `.` in `git add`. `git show --stat HEAD` after every commit lists no `scratch/` path. `.env` removed and proven with `ls -la .env` before any stop line.
BUILDER LAUNCH: unchanged from `24-p4-fund-rule.md` — the byte-for-byte `01-p4-build.md` step 2 `--permission-mode acceptEdits --allowedTools` list (`uv run pytest *`, `uv run python *`, `uv run cobalt validate*`, `uv run cobalt replay *--dry-run*`, `uv run cobalt smoke *`, `uv run cobalt --help*`, `git status*`, `git diff*`, `git log*`, `git show*`, `ls *`, `grep *`, `rg *`, `cat *`, `sed -n *`, `wc *`, `mkdir -p *`), ONE bare command, no leading `cd … &&`. Covers everything step 3 below needs — no DB, no vault, no Finviz, no migration tool.

INDEX CARD: `UNATTENDED-LAUNCH.md` §2+§6 → Memory `INDEX.md` → `## NOW` → `LAWS.md` IN FULL → `cto-2026-09-19.md` §4 R16 → `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` §2.2-§2.5, its `## FUND RULE (R16)` section and **ESCALATE 17** (the prior attempt's blocker and its own proposed option B — read it, this file builds exactly that) → `24-p4-fund-rule.md` steps 1, 2, 4-9 (UNCHANGED — this file only replaces step 3; read them there rather than duplicated here) → `src/cobalt/replay/runner.py:311-353` (`movers_step`) → `src/cobalt/replay/movers.py` (`parse_movers` 136-171, `MoversStore.reconcile` 500-542, `benchmark_misses` ≈310-390) → `src/cobalt/replay/models.py:305-387` (`MoverRow`, `StoredMover`) → `src/cobalt/db_migrations/0008_radar_value_movers.sql` (confirms `movers_daily` columns, no `industry`). Laws: L1, L3, L10, L32, L42, L45, L46/L60, L47, L53, L57 (the miss row's own `inputs`/`gate_detail` is where the deciding `asset_type`/`industry`/rule-marker are stored — NOT `movers_daily`, which never stored not-equity inputs either, only the raw `asset_type` column for display).

## 1-2. THE RULE SHAPE + EVALUATOR — UNCHANGED from `24-p4-fund-rule.md` steps 1-2
Same `NotEquityConfig` redesign (`asset_type_header`/`industry_header`/`industry_values`), same `is_not_equity(row, config)` function, same `radar/runner.py:144` call site. Build exactly as 24 step 2 specifies. Nothing here differs.

## 3. THE MOVERS SIDE — decide AT INGEST, never through the DB round trip (option B, ESCALATE 17)
FACT, confirmed by this file's own investigation before writing it: `movers_step()` (`replay/runner.py:311-353`) builds `exports: list[MoversExport]` (each `.rows: list[MoverRow]`, straight from the parsed CSV — has `Asset Type` AND, once step 4 adds it, `Industry`). It then calls `deps.movers_store.reconcile(...)` (live) — which INSERTs/UPDATEs `movers_daily` using ONLY the columns in `MoversStore._COLUMNS` (`movers.py:484`: no `industry`) and returns `self.active(trade_date)` — a FRESH `SELECT` (`movers.py:493-498`) that reads back ONLY those same columns. So `stored: list[StoredMover]`, whatever `benchmark_misses` receives, has NEVER carried `Industry` on the live path, and adding an `industry` field to `MoverRow` alone (24's original step-3 plan) does not fix it — `StoredMover` is a different, DB-shaped model, rebuilt from a `SELECT` that cannot select a column that is not there. (On the `dry_run` path only, `stored` is built in memory from `exports` directly — `runner.py:322-329` — so a `MoverRow.industry` field WOULD have reached `benchmark_misses` there, which is exactly the asymmetry that would have shipped a rule that works dry-run and not live.)
FIX — one lookup, keyed by what identifies a mover, built ONCE from `exports` (the export-time truth), used on BOTH the dry-run and live paths so there is still ONE decision path (L3):
1. `MoverRow` (`models.py:305-314`) gains `industry: Optional[str] = None`, read in `parse_movers` (`movers.py:162-171`) the same way `asset_type` is — now unconditional, since step 4 makes both headers required.
2. In `movers_step()`, immediately after `exports` is computed and before `export_counts`/`reconcile`/`archive_movers` run, build `verdicts: dict[tuple[str, str], MoverRow] = {(row.side, row.ticker): row for export in exports for row in export.rows}` (side+ticker is already how `reconcile` and `benchmark_misses` key a mover — confirm no side+ticker collision inside one day's two exports; if one exists, name it, do not silently pick one).
3. `benchmark_misses`'s signature (`movers.py`, ≈310-330) drops its `not_equity_values: Sequence[str]` parameter and instead takes the `verdicts` lookup (or the `NotEquityConfig` plus the lookup — your call, tests decide) — for each `lead: StoredMover` it looks up `verdicts.get((lead.side, lead.ticker))` to get that mover's real `asset_type`/`industry` (never `lead`'s own fields, which the DB round trip may have staled or, for `industry`, never had) and applies the ONE evaluator from step 2. A `lead` with no lookup match (should not happen — every `StoredMover` came from an `export.rows` this same run) is a loud finding, not a silent keep: assert it, name the ticker/side, ESCALATE if it ever fires — never build a fallback that guesses.
4. The ONE other call site the prior run found and this prompt's step 1-2 must also fix: `replay/runner.py:353`'s `benchmark_misses(stored, episodes, ..., not_equity_values=deps.radar_config.not_equity.values)` becomes `benchmark_misses(stored, episodes, ..., not_equity=verdicts)` (or whatever the new parameter is named — keep it consistent with the signature chosen in 3).
5. The miss row's stored inputs (`movers.py` ≈372, ≈385 — `"asset_type": lead.asset_type if lead.asset_type is not None else "unreported"`, `"not_equity_values": list(not_equity_values)`) become: the LOOKED-UP row's `asset_type` and `industry` (both, from `verdicts`, never from `lead`) plus a rule marker (e.g. `"not_equity_rule": "asset_type_or_etf_industry"`) replacing `not_equity_values` — this is the L57 record: the miss line's own receipt carries the deciding inputs, so a dropped-mover decision replays without needing `movers_daily` to carry `Industry` at all. `movers_daily` itself is UNCHANGED — no migration, no new column — its `asset_type` column keeps meaning "what Finviz called it", same as today; it was never the record of a not-equity DECISION, only of the raw value.
TESTS FIRST: a fake/offline `MoversStore`-shaped `StoredMover` list built to LACK the real `Industry` (simulating the DB round trip) plus a real `verdicts` lookup that DOES carry it — `benchmark_misses` must still drop the 8-ticker-class row correctly (proves the fix works on the path the prior attempt's `MoverRow`-only plan would have missed); a dry-run path test proves the SAME function/lookup shape is used (no second branch); a lookup-miss case is asserted loud, not swallowed.

## 4-9. UNCHANGED from `24-p4-fund-rule.md`
Steps 4 (headers required, `unreported` branch), 5 (real-shape tests — adjust only for the new `benchmark_misses` signature; the four AT/BLANK/kept/dropped cases and the header-missing case are unchanged in substance), 6 (counts-only evidence re-run), 7 (offline + DB suites — DB suite is STILL a confidence re-run only: this file adds no migration, so `cobalt_dev` needs nothing beyond what it already holds, `0001–0009`, unchanged), 8 (RESTARTS — `com.cobalt.radar` at minimum; `replay/movers.py`/`models.py`/`runner.py` changed, so `com.cobalt.replay`'s bootstrap likely classifies too), 9 (close) apply exactly as written there, with `git diff --stat`'s bound gaining `src/cobalt/replay/runner.py` (the `movers_step` edit) alongside the files 24 already named. Commit message for the movers half: `fix(s2-p4): not-equity for movers decided at ingest from the parsed export, never through the movers_daily round trip (R16 "C", ESCALATE 17 option B)`.
Append to the SAME `## FUND RULE (R16)` section family in `docs/40 - DevDocs/reports/s2-p4-verify-2026-09-19.md` — a new `### Retry (0919b)` subsection under it, not a new top-level section; existing ESCALATE 17 text stays, mark it `RESOLVED — option B built` with one line saying so.
Last line, exactly: `P4 FUND RULE <tip> | offline <p>/<f> | db <p>/<f-or-NOT RUN> | dropped <n> kept n/a | stock rows hit <n> | RESTARTS: <line>` or `FAILED: <step> — <reason>`.
Then stop.
