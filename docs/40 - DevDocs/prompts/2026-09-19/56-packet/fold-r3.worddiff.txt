diff --git a/docs/40 - DevDocs/prompts/2026-09-19/55-packet/53-deploy-d3-after.md b/docs/40 - DevDocs/prompts/2026-09-19/56-packet/53-deploy-d3-after.md
index ae7bf3d..3af1c3b 100644
--- a/docs/40 - DevDocs/prompts/2026-09-19/55-packet/53-deploy-d3-after.md	
+++ b/docs/40 - DevDocs/prompts/2026-09-19/56-packet/53-deploy-d3-after.md	
@@ -53,9 +53,9 @@ If `worktree add` SUCCEEDED → then, in this order, TWO separate calls: `git -C
  (c) FORWARD: `COBALT_ENV=dev uv run cobalt db migrate` → quote the full table. EXPECT `archive_progress` and `archive_incidents` **CREATED**, every pre-existing table `OK` with its digest unchanged, no `CHANGED`. Anything else → (h), ending `FAILED: gate — dev forward migrate <what>`.
  (d) SUITE: `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (`run_in_background`). GATE: **`0 failed`**; quote the summary. Context only: the archiver branch's own with-DB close was `2486/0`. A red → (h), ending `FAILED: gate — with-DB suite red on the stack — <test names + assertions verbatim>`. **Never label a red "known" and continue** (L68, L70).
  (e) **POST-SUITE PROOF:** `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → quote it. **The suite WRITES to `cobalt_dev` by design** (deploy 2 run 2 false-stopped on exactly this — `cobalt_redactions` 127 → 128), so differences between (b) and here are RECORDED table by table and are NOT a stop. A table PRESENT at (b) and missing now, or one no stacked migration creates → (h), ending `FAILED: gate — cobalt_dev table set changed during the suite — <what>`.
  (f) RESTORE: `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` → EXPECT exactly `0011_archive_incidents.rollback.sql` then `0010_archive_progress.rollback.sql` applied and **nothing else** (if anything numbered ≤ 0009 is reversed: quote it, (h), `FAILED: gate — dev rollback went below 0009 — <what>`) → `COBALT_ENV=dev uv run cobalt db migrate --proof-only` → compare **against [-(e), not (b)**:-]{+(e) if (e) ran, else against (b)** (round-2 finding R2-5, aligning this sentence with (h)):+} the two archiver tables ABSENT again and every other table identical to [-(e).-]{+that proof.+} A digest that moved ACROSS the restore is a stop → (h), `FAILED: gate — cobalt_dev digest moved across the restore on <table>: <before> → <after>`.
  (g) `rm /Users/cobalt/cobalt-wt/stack-deploy3-0919/.env` → `ls -la …/.env` says "No such file". **The `.env` cleanup happens before ANY stop line** — a stop line written while `.env` is on disk is itself a failure condition.
  (h) RED-BRANCH RESTORE (trigger widened, round-1 finding [-F6):-]{+F6; narrowed again by round-2 finding R2-5):+} on a red at [-ANY-]{+any+} of [-(b)-]{+**(b)+} through [-(f),-]{+(e)**,+} FIRST run (f) in [-full-]{+full. **A red inside (f) itself does NOT re-run (f)** — go straight to (g) and the FAILED line, ending it with the last state you proved+} — it is always safe, it is a dev-lane command, and after a red (b) or (c) it usually has nothing to reverse — THEN (g), THEN the FAILED line, ending it with `cobalt_dev: <the last state you PROVED>`. Compare (f)'s closing proof **against (e) if (e) ran, else against (b)**. If the restore ITSELF errors, do not retry it: quote the error and write `FAILED: gate — <original reason> — cobalt_dev: <last proven state, or UNKNOWN>, restore failed: <error>`.
1.5 `cd /Users/cobalt/cobalt` → `ls -la .env` (the file IS listed here — cwd is back; contents never read) → `git -C /Users/cobalt/cobalt log --oneline -1`. Your OWN report commits are expected here and are fine (4.1 absorbs them). **What is not fine is a commit that is not yours or the desk's** — `git -C /Users/cobalt/cobalt log --oneline <main-at-gate>..HEAD` and read every subject: anything that is not this run's report, the desk's docs, or a machine-written file commit → `FAILED: gate — foreign commit on main during the gate — <hash> <subject>` (round-1 finding F5: stop HERE, before §3's production snapshot and before the `pre-d3-0919` tag, which has no delete rule on a rerun).
1.6 Write the gate into the report as its own section: the three tips, `<stack>`, the merged `FORWARD` line, both suite lines VERBATIM, all THREE `--proof-only` verdicts, the forward and rollback tables, the `.env` proof, and both P15 lines quoted. A gate section with a missing step is `FAILED: gate — incomplete`, never a green one. **`stack/deploy3-0919` is NOT a throwaway this time — it is the branch §4.3 fast-forwards `main` to.** Report committed by name. Last line `CONTINUE: step 2`.

@@ -100,7 +100,7 @@ or `FAILED: <step> — <reason> — safe state: <green/red> — residents: <up/D
## 8. SAFE-STATE RULE — red at 4.3–4.6, red smoke, a failed resident bootstrap, or a mid-run denial after 4.2
STEP (0), FIRST, ALWAYS: `launchctl print gui/501/com.cobalt.aset` and `…/com.cobalt.radar`; if EITHER is loaded → `launchctl bootout` both and require "Could not find service" for both BEFORE you touch code. A resident must never be running while its code is reverted under it; `bootstrap` in (4) is only ever run on a service that is not loaded. **`com.cobalt.replay` is NOT booted out here** — it arrived with deploy 2, it is not in this deploy's range, and reverting this range leaves its module in place; `com.cobalt.archiver` likewise already existed. Neither can fire on a Saturday.
GOAL: code and config agree, residents UP, before you stop. **THIS DEPLOY HAS NO SETTINGS LEG** — no `trader_settings` row was written, so there is no second half to restore. CLOCK NOTE: **no hour ever sends a green result into this section** (§2.1's point of no return). The only job that fires tonight is the 21:40 backup; if §8 is still running as it approaches, finish (0)–(5) anyway and let the ESCALATE say so.
(1) **If 4.3 merged —[-and-] read this [-twice,-]{+twice;+} it is [-round-1-]{+round-2+} finding [-F2, a launch BLOCKER-]{+R2-1, named by all three houses+} in {+all six calls, and+} the [-previous wording.** The range `<pre-merge>..HEAD` **CONTAINS MERGE COMMITS**-]{+round-1 fold got it WRONG.** After 4.3, `HEAD` is the stack branch's tip, whose LAST commit is 4.1's `merge --no-edit main`+} —[-§1.2-] made [-two (`ops` and `archiver` into-]{+INSIDE+} the [-stack) and 4.1 made a third (`main` into-]{+stack worktree, so its **parent 1 is+} the [-stack) —-]{+stack (the NEW code)+} and [-`git revert` REFUSES a merge commit without `-m`. Deploy 2's range-]{+its parent 2 is `main` as it stood at `<pre-merge>` (the code production+} was [-linear; this-]{+actually running).** Reverting that+} one {+commit with `-m 2` therefore restores production's side in a single step, and it undoes EVERYTHING this deploy brought — both branches' commits are inside it. So the whole code revert+} is [-not. So:-]{+ONE command, not a loop:+} `git -C /Users/cobalt/cobalt log --oneline [---merges <pre-merge>..HEAD`-]{+-1` (confirm HEAD is that merge)+} →[-list them, then revert **newest first, one command per commit**:-] `git -C /Users/cobalt/cobalt revert --no-edit -m [-1 <sha>` for each-]{+2 <that sha>` (covered by the approved `revert --no-edit *` rule). **Do NOT then revert the individual commits** — the round-1 wording said to walk the range newest-first and that would RE-APPLY what this revert just undid; both branches are linear inside the+} merge[-commit,-] and {+are already covered. **PROVE IT, do not assume it:**+} `git -C /Users/cobalt/cobalt [-revert --no-edit <sha>` for each non-merge commit in the same descending order (`-m 1` = keep the first parent, which is `main`'s side-]{+diff --stat <pre-merge> HEAD -- . ':(exclude)docs'` prints **NOTHING**+} — the [-side-]{+tree's code is byte-identical to what+} production [-was on). Both spellings are covered by the approved `revert --no-edit *` rule. Quote every command-]{+ran before this deploy. If it prints anything, say so verbatim+} and [-its exit.-]{+treat the safe state as NOT reached: `FAILED: §8 (1) — revert did not restore <pre-merge>'s code — <paths>`, then continue with (3)–(6) so the residents still come up. (L54 — **never `reset --hard`**, which is not in your allowlist in any spelling.)+} **If [-a-]{+the+} revert errors for any reason other than a conflict** (a [-bad-]{+wrong+} `-m`, an empty revert, anything): [-stop reverting,-]{+stop,+} do NOT improvise another spelling, and go[-straight-] to (3)–(6) with `safe state: NEW code kept — revert failed: <error>` — a live tree on new code that passed [-its own-]{+both+} suites is safer than a half-reverted one. A CONFLICT during the revert: `git -C /Users/cobalt/cobalt revert --abort` → the tree is back on the merged new code → that pair is ALREADY consistent, so make THAT your safe state: `COBALT_ENV=production uv run cobalt validate` exit 0, then (3)–(6) with `safe state: NEW code kept — revert conflicted`.
(2) (no settings step.)
(3) **Migrations 0010/0011 STAY — IF 4.5 RAN** (round-1 finding F9: on the 4.3 non-ff or foreign-commit branch nothing was ever applied, and the report must say `migrations applied: no` rather than that they stay). When they were applied: they are additive (two new tables, nothing else reads them) so reverted code runs unchanged over them. **The schema is NEVER rolled back unattended** — no production rollback is instructed anywhere in this prompt and none is typed in any branch; the rollback files exist and are the DESK's documented path with Dejan, run as its own approved job, never from inside a failing deploy. PROVE the schema is harmless where it stands: `validate` exit 0 and the sheet 200 after (4). Likewise **no settings row is touched** — cards stay LIVE through every branch of this section.
(4) Bring the residents up with 5.2's commands and 5.2's single retry / single `kickstart` rule ONLY; a failure here is terminal (`residents: DOWN`), never a second pass through §8.
