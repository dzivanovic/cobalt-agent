# ROUND 2 — headline and the complete verdict table (verbatim)

## §0 Headline
- **NOT ZERO — one launch blocker remains, and it is the fold of F2 itself.** F1's fold is good (4.1 merge + docs-only proof + `merge-base --is-ancestor`; ff at 4.3 works, conflict at 4.1 leaves nothing down). **§8 (1) as folded cannot reach its stated safe state:** the new 4.1 merge is `main` merged INTO the stack, so its parents are (stack, main) and `-m 1` keeps the NEW code; then the "non-merge commits in the same descending order" re-revert what the merge reverts already undid (both branches are linear: ~15 ops + N archiver commits sit in the range). All 3 houses / all 6 calls say so.
- **The fix is text-only, NO rule string changes** — `Bash(git -C /Users/cobalt/cobalt revert --no-edit *)` already covers `revert --no-edit -m 2 <sha>`; R36's "a rule-string change waits for him" is NOT triggered. Three houses converge on the same one-liner: revert the 4.1 merge ONCE with `-m 2` (parent 2 = `<pre-merge>`), then prove `diff --stat <pre-merge> HEAD -- . ':(exclude)docs'` prints nothing; drop the non-merge loop. The desk folds it, records the new sha256 (R36 carries the pre-fold hash), and L67 allows a round 3 for the changed text.
- Houses that read it: **Grok** (A+B) `DO NOT RUN 1`/`1`, **Gemini** (A+B) no verdict line (both name the same defect), **Astra UP this time** (A+B) `DO NOT RUN 2`/`5` — three houses, L67's floor met with one to spare. Other nine folds: 6 fixed cleanly, F5/F6/F9 leave MINOR text residue; F3, F4, F7, F10, F11 verified.
- Q5 by hub: 51 rules = 41 identical + 10 new (compared string by string against `38-deploy-p4.md`), 14 dropped, both `launchctl bootstrap` strings and both plists exist, no step types an uncovered command. Not counted: P15 (met), wide-vs-narrow L68 scope, the prefix-match question.


## Verdict table
Same defect named by several houses = one row. `blocks the launch?` for REAL rows only.

| # | house(s) | sev (theirs) | where | claim (≤20 words) | what I read | VERDICT | blocks the launch? | fix |
|---|---|---|---|---|---|---|---|---|
| **R2-1** | Grok A B1, Grok B B1, Gemini A Q2/Q4/Q6, Gemini B Q2d, Astra A #1, Astra B #1 | BLOCKER | §8 (1) `:103` vs 4.1 `:76` | `-m 1` on the 4.1 merge keeps NEW code; "non-merge commits in the same order" re-revert already-undone commits | `:76` merge runs `git -C …/stack-deploy3-0919 merge --no-edit main` → HEAD=stack is parent 1, main parent 2; `:103` says "`-m 1` = keep the first parent, which is `main`'s side" (false for 4.1); `git log --merges main..ops/2026-09-19` and `main..archiver/append-0919` both EMPTY (linear) → range holds 3 merges + 15 ops commits (`git log --oneline main..ops/…`) + the archiver's; reverting a merge already undoes those commits | **REAL** (structure by reads + git's parent definition; git's error string not run — U4) | **YES** — on any red at 4.3–4.6/smoke §8 (1) either reverts main's docs instead of code, or ends half-reverted and labelled "NEW code kept"; residents come up on an unknown tree (L70) | Text-only, no rule: revert the 4.1 merge once, `revert --no-edit -m 2 <newest merge from log --merges>` (parent 2 = `<pre-merge>`), then `diff --stat <pre-merge> HEAD -- . ':(exclude)docs'` must print NOTHING; delete the non-merge loop; if no 4.1 merge exists, `-m 1` on the two §1.2 merges newest-first and nothing else. State the observed tree, never "NEW code kept" unverified |
| R2-2 | Astra A #2, Astra B #2, Grok B M2 (Grok A Q2c) | BLOCKER / MAJOR | §8 (1) conflict + non-conflict sentences | Separate reverts each commit; `revert --abort` cancels only the current one, so "back on the merged new code" is false after a first success | git semantics; the untouched sentence was written for ONE range revert (deploy 2) | **REAL** | no on its own — it disappears with R2-1's single-revert fix (one command → abort really returns to `<stack-final>`) | Fixed by R2-1 (or say "abort cancels this command only") |
| R2-3 | Astra A Q1d, Astra B #4, Gemini B Q1d/Q4-1, Grok A/B Q1d/M1 | MINOR–MAJOR | WHY (1) `:9`; §1 `:45` | "The tree that lands is byte-identical to the tree the gate proved" / "combines exactly what lands" — false after 4.1 adds docs | `:9` verbatim quoted; 4.1's proof is exclude-`docs` | **REAL** (all four houses; Grok B rates MAJOR, hub MINOR — the exclude-docs proof, not this sentence, is the gate) | no | "identical outside `docs/`, plus the docs commits main gained" (WHY (1) and §1) |
| R2-4 | Astra A, Astra B #5, Grok A, Grok B m1 | MINOR | §8 (6) `:108` | (3) now says 0010/0011 stay "IF 4.5 RAN"; (6) still "what stays applied (0010/0011)" | `:105` vs `:108` | **REAL** | no | (6): "…(0010/0011 if 4.5 ran, else `migrations applied: no`)" |
| R2-5 | Astra A F6, Astra B #3 (MAJOR), Gemini B Q3/Q4-4, Grok A F6, Grok B m2 | MINOR / MAJOR | 1.4 (f) `:56` vs (h) `:58` | (f) still says compare "against (e), not (b)"; (h) says (e) if ran else (b); a red at (f) re-runs (f) | both sentences read; (g) removes `.env` before any stop line; dev DB only | **REAL** (hub: MINOR — `cobalt_dev` only, nothing down, FAILED line carries the last proven state; not a MAJOR) | no | (h) trigger (b)–(e) only, or "a red at (f) skips the second (f)"; align (f)'s compare sentence to (h) |
| R2-6 | Grok A nit, Grok B m3 | MINOR | TWO ASYMMETRIES `:15` | Says the 4.2 exception is "written out at the step and nowhere else" — it is now also in UNATTENDED RULES `:20` | `:15` and `:20` read | **REAL** | no | Drop "and nowhere else" |
| R2-7 | Astra B #2 tail | MINOR | §8 (1) fallback | "NEW code kept — revert failed" after a failed 4.5 means new code over a missing 0010/0011 | archiver worktree `src/cobalt/heartbeat/probes.py:396-397`: a missing `archive_incidents` reads as RED "run `cobalt db migrate`" — loud, not silent; archiver/replay cannot fire Saturday | **REAL but loud** (a two-failure branch) | no | Moot after R2-1; if kept, name it in the label (`schema absent`) |
| — | Gemini B | (Q3/Q4-3) | 1.5 vs 1.6 | "OWN report commits are expected" at 1.5 but first report commit is at 1.6 | `:87` `0.4 Report written and committed by explicit name`; P3 also commits and soft-resets | **NOT REAL** | n/a | — |
| — | Astra A, Astra B | (Q3 F5) | 1.5 | Screening by commit subject proves neither authorship nor paths | 4.1's exclude-docs diff is the content gate; the fold says exactly what it claims | **NOT REAL** (a limitation, backstopped) | n/a | — |
| — | Grok B m4 | MINOR | 4.1 | 4.1 can still conflict on a live docs file | branches touch no report file; conflict → `merge --abort`, nothing down — fail-closed by design | **NOT REAL** (as a defect) | n/a | — |
| — | Grok A Q6 | (aside) | 4.1 non-docs fail | No `merge --abort` after the non-docs `FAILED`, stack left with the merge | the merge completed (nothing to abort); the RERUN CASE at 1.2 already covers a leftover stack for every failure | **NOT REAL** | n/a | — |
| — | Astra A Q1b | MINOR | 4.1 | Also require the diff's exit 0 | `git diff --stat` exits 0 unless it errors; the gate is "prints NOTHING" | **NOT REAL** (style) | n/a | — |

**Q3 folds, one line each (hub):** F3 ✓ both globals carry the 4.2 exception (nit → R2-6) · F4 ✓ `954cabb` (ops code tip, `bb996a5` docs above) and `85b6d7c` (archiver code, `7117fc3` docs above) read from `git log` · F5 ✓ (see NOT REAL) · F6 ✓ with residue R2-5 · F7 ✓ · F9 ✓ in (3), residue R2-4 · F10 ✓ · F11 ✓ `configs/cobalt/jobs.yaml:242` = `schedule: {at: "21:40", weekdays: [0, 1, 2, 3, 4, 5, 6]}`, `:223` = `daymode-propose`.
**F1 answers (hub):** (a) yes — after a successful 4.1, `main` is an ancestor and 4.3 can ff; a later commit is caught by 4.3's `<pre-merge>` check. (b) yes for code; a runtime file under `docs/` could enter untested, `validate` at 4.6 is the backstop. (c) conflict → `merge --abort`, nothing down. (d) only WHY (1)/§1 (R2-3).

**UNVERIFIABLE FROM READS (5)** — missing command named, none invented:
1. **U1 (carried):** whether an UNSTARRED rule matches by prefix here — scratch session `--allowedTools "Bash(echo hi)"`, type `echo hi there`, expect DENIED. (This deploy has no auto mode.)
2. **U2 (carried):** reverted code runs unchanged over additive 0010/0011 — on `cobalt_dev`, run pre-archiver code after `COBALT_ENV=dev uv run cobalt db migrate` (build-lane).
3. **U3 (carried):** merges conflict-free and both suites green on the stack — the gate's own 1.2–1.4.
4. **U4 (new):** that `git revert --no-edit -m 2 <4.1 merge>` returns the tree to `<pre-merge>` — scratch repo: commit A on `main`; branch with two commits; add commit B on `main`; `merge --no-edit main` on the branch → M; `git merge --ff-only branch` on `main`; `git revert --no-edit -m 2 M`; `git diff --stat A…` must be empty outside the file B touched. (Also the expected error for the old spelling: `commit … is a merge but no -m option was given`.)
5. **U5 (new):** what `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0009` does on a `cobalt_dev` where 0010/0011 were never applied (after a red (b)/(c) per R2-5) — run on `cobalt_dev` from the stack tree and read the exit. (Deploy 2's empty rollback exited 1; here 0010/0011 ARE registered so the outcome depends on whether their rollback files are `IF EXISTS`.)
6. **U6 (desk, not a finding):** whether the fold's file still carries R36's `3d18b352…9339` — it cannot, the text changed since; run `shasum -a 256 "…/53-deploy-d3.md"` after the desk's fold and record the new hash in R36 (R7's text-only-fold precedent), or the deploy hub stops at its own authorization check.

(That is 5 counted UNVERIFIABLE rows: U1–U5; U6 is a desk action, not a claim.)

D3 PROMPT CHECK R2 · grok: DO NOT RUN 1 · gemini: no verdict line (A and B both name R2-1) · astra: DO NOT RUN 2 (A) / 5 (B) · hub-verified REAL: 7 (blocks the launch: 1) · NOT REAL: 5 · UNVERIFIABLE: 5
