MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch two-or-three reviewers, verify their findings; no write path · SEAT: review hub `review-deploy-p4-r3-0919`, launched by the CTO desk in the background. NO NEW RULE: `45-review-deploy-p4-r2.md`'s `--allowedTools` list — itself `41-review-deploy-p4.md`'s, itself `18-review-ops-0919.md`'s, byte for byte — copied unchanged, with exactly THREE strings different and nothing else: the prompt file name, the `--remote-control` name (`review-deploy-p4-r2-0919` → `review-deploy-p4-r3-0919`) and the `mkdir` path (`scratch/review-deploy-p4-r2-0919` → `scratch/review-deploy-p4-r3-0919`). Round 2 needed nothing outside that list and round 3 is strictly smaller. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/46-review-deploy-p4-r3.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-deploy-p4-r3-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-deploy-p4-r3-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47). **ASTRA:** its Codex meter returned a usage limit at 13:06 ET and again at 13:5x with "try again at 4:54 PM". Probe it ONCE (`codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."`, `run_in_background`): if the probe comes back UP, Astra reads the fold too and becomes the third house; if it returns a usage limit, record `astra: METER` verbatim and end with two houses — a normal ending, L67's floor met twice over. Do NOT retry, do NOT wait for the reset.

# Tribunal check of a DESK DEPLOY PROMPT, **ROUND 3 — THE LAST ROUND** (LAWS L67, ≤3 rounds per house while meter allows; 09-18 R8/R20) — ONLY the fold of round 2's **four hub-verified REAL findings (N1–N4)** into `38-deploy-p4.md`, the unattended deploy prompt for DEPLOY 2 (merge `sprint-2/p4` into production, apply migrations 0008/0009 to the live database, residents down and up, a new scheduled job, the first L68 integrated gate). Round 2's stop line: `grok: RUN AFTER FIXES 1,3|B1,B2,B3 · gemini: RUN AS IS|no verdict line (B: 1 BLOCKER) · astra: METER · round-1 fixes FIXED: 10 of 11 · hub-verified REAL: 4 (blocks the launch: 1) · NOT REAL: 3 · UNVERIFIABLE: 2`. **The question for this round is narrow: did each of the four folds fix what it names, and did any fold break something?** The fold changed NO rule string — Q5 is where you prove or disprove that. Small packet on purpose. DO NOT STOP until the report ends `DEPLOY P4 CHECK R3 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan; `38-deploy-p4.md` is still NOT approved and this round is the last precondition of asking him. Standing law, not a one-off approval: LAWS.md **L67** (ruled 2026-09-18 17:30 ET — `cto-2026-09-18.md` §4 R18, with R19–R21's emergency / override / rounds clauses) and **09-18 R8** (14:29 ET, the desk's own write-path prompts are read by another house); `cto-2026-09-18.md` §4 **R9** (`Bash(grok *)`, `Bash(agy *)`, standing through 09-20); `cto-2026-09-19.md` §4 **R1** (06:46 ET, the Codex read-only rule). Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"` and the same for `cto-2026-09-18.md`. A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line; `ASK DESK: <question> [<time from date>]` for a question that does not stop you — take the safe default and continue, never wait on an answer.

Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim and never the desk's own `approval-list.md` — verify against the real prompt and the real code · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets the same packet · L47 a meter is checked BEFORE a house is counted on · L67 ≥3 checkers, ≤3 rounds, floor of one house other than the author.

INDEX CARD (you; the reviewers get the packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — its laws, UNATTENDED RULES, PREFLIGHT, §2 launch spellings and its LESSON bind you here UNCHANGED, with `scratch/review-deploy-p4-r3-0919/` in every path. `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-18/11-review-deploy-stack.md` §3 = the verification rules and table shape. `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-p4-r2-0919/REVIEW.md` = round 2 in full — its `### New findings this round` table is what this round measures against, and its `## Packet` note records one whitespace defect of its own (a trailing TAB dropped from two diff headers; the same class can bite you — check it). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-p4-r3-0919/REVIEW.md` — §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok` → `## Gemini` → `## Astra` → `## Verdict table` → last line. You commit nothing.

**GROK INVOCATION — bind this spelling and do not improvise it.** `01-review-harness.md` §2's folder-glob form is the one that works on this host: `grok --sandbox cobalt-job --allow "Write(<absolute scratch dir>/**)" -p "<prompt>"`. **A single-file `Write(...)` pattern HARNESSes on this host — proven twice today** (the `--allow` argument must be the FOLDER GLOB ending in `/**`, never one file path). Rounds 1 and 2 both used exactly this form for all four of their Grok calls and every one wrote its file, exit 0, no HARNESS. Reuse it verbatim with `scratch/review-deploy-p4-r3-0919/**`.

## 1. Packet in `scratch/review-deploy-p4-r3-0919/` (`mkdir -p` first; Read → Write, byte-identical) — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/46-packet/`
(1) `38-deploy-p4-after.md` (62,652 B) — **THE FILE UNDER REVIEW, as it stands now, after the round-2 fold** · (2) `fold-r3.worddiff.txt` (22,210 B) — the desk's pre-computed word-level diff, round-2 version → now, `git diff --no-index --word-diff=plain`: `[-removed-]`, `{+added+}`; the "before" side is round 2's own staged copy (`45-packet/38-deploy-p4-after.md`), so the diff is exactly what the houses did not yet see · (3) `round2-verdicts.md` (8,809 B) — round 2's §0 Headline, its 11-row Q1 table and its complete `### New findings this round` table, verbatim, including the NOT REAL rows · (4) `approval-list.md` (5,199 B) — the approval list as it stood at round 2: 23 rule strings that differ from the approved precedent, verbatim, with the class and reason for each. **This fold was supposed to leave it untouched; Q5 is where you check that against the real launch line** · (5) `QUESTIONS.md` (4,775 B), the same text every house receives (L44).

**The five files total 103,645 B. Send TWO calls per house** (round 1's single 121,392 B call worked on both houses, so either bucket is comfortably inside what is proven): **A** = `38-deploy-p4-after.md` + `round2-verdicts.md` + `approval-list.md` + `QUESTIONS.md` (**81,435 B**, covers Q1, Q2, Q3, Q5, Q6) and **B** = `38-deploy-p4-after.md` + `fold-r3.worddiff.txt` + `QUESTIONS.md` (**89,637 B**, covers Q4 and the diff half of Q1–Q3); say in `## Packet` which bucketing each house actually got. **Never drop a file to make a bucket fit, and never let a call truncate silently** — a house that received a truncated packet did not read the packet (L44). Record every file's byte count and confirm each staged copy equals its source; if a staged copy differs by so much as a trailing tab, say so in `## Packet` with the byte counts, exactly as round 2 did.

QUESTIONS.md (verbatim): see `46-packet/QUESTIONS.md` — copy it byte-for-byte into `scratch/review-deploy-p4-r3-0919/QUESTIONS.md` as part of staging step (5); it is the same text every house receives. Six questions: (Q1) N1, the launch blocker — does §4.2's terminal branch now restore a resident whose bootout took, does it still forbid §8, does its FAILED line state what was printed, are its commands in the allowlist verbatim, can it loop · (Q2) N2 — do §1.4 (c)/(d) route to (h), is (h)'s corrected premise true against `cli.py`, is the order unambiguous · (Q3) N3/N4 wording · (Q4) **what did the fold break** — every `{+added+}` region against the sentences the diff did not touch · (Q5) the approval list: is it still exactly those 23 strings, does any step type a command no rule covers, is any rule now unused · (Q6) anything else the fold made worse.

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4, and `2026-09-18/11-review-deploy-stack.md` §3, EXACTLY, with `scratch/review-deploy-p4-r3-0919/` in place of their paths. ONE attempt per house; HARNESS / METER / TIMEOUT (15 min) recorded verbatim, never looped.

For every allowlist claim (Q5) quote the rule AND the step's command side by side. **The N1 fold types `launchctl bootstrap` inside §4.2, a step that never typed it before** — that is exactly the class round 2 warned about (a step typing a spelling its rule does not cover), so verify those two bootstrap rules exist in the launch line character for character, including the two different plist paths (`/Users/cobalt/cobalt/ops/com.cobalt.aset.plist` for aset, `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for radar). After §4.2 a denied command means denied with production's services DOWN. You verify each BLOCKER and MAJOR against the REAL files: `38-deploy-p4.md` under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/` with Read and `grep` (no Bash rule needed for a plain read) — and confirm it is byte-identical to the staged `38-deploy-p4-after.md`; `src/cobalt/db_migrations/cli.py` (Q2's transaction claim — re-derive the line numbers yourself, L35), `src/cobalt/db.py`, `src/cobalt/jobs/cli.py` on main and in `/Users/cobalt/cobalt-wt/s2-p4/`; `ops/com.cobalt.aset.plist`, `ops/com.cobalt.replay.plist` and `/Users/cobalt/Library/LaunchAgents/com.cobalt.radar.plist` for Q1's bootstrap paths; LAWS.md at `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md`.

A claim only a RUNNING command could settle — whether an unstarred allowlist rule matches by prefix on this host, whether `launchctl bootstrap` of a service booted out seconds earlier succeeds, whether the stacked merge conflicts, whether the stacked suites go green — is `UNVERIFIABLE FROM READS`: name the exact missing command, do not invent the answer and do not ask a reviewer to run it. Carry round 2's two UNVERIFIABLE rows forward as they stand unless the fold settled them. One extra column per REAL finding: `blocks the launch? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-deploy-p4-r3-0919/` — in particular you never edit `38-deploy-p4.md`; the desk folds.

**NOT this round's business, and not a finding:** P15, the archiver precondition, is now **MET** — `archiver-append-build-2026-09-19.md` ends `ARCHIVER DB2 f5df6c1 … db 161/0`, verified by the desk at 13:5x ET; the wide-vs-narrow L68 scope question is Dejan's; the prefix-match question is the desk's experiment. Note them in §0 if you like, do not count them.

Last line, exactly this shape:
`DEPLOY P4 CHECK R3 · grok: <RUN AS IS|RUN AFTER FIXES <n>|DO NOT RUN|HARNESS|METER|TIMEOUT> · gemini: <same shape> · astra: <same shape> · round-2 fixes FIXED: <n> of 4 · hub-verified REAL: <k> (blocks the launch: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u>`
Then stop.
