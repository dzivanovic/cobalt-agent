MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers, verify; no write path · SEAT: review hub `review-0919d`, launched by the CTO desk in the background. NO NEW RULE: `01-review-harness.md`'s allowlist with one `mkdir` path changed (R9 of 2026-09-18; R1 of 2026-09-19). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/09-review-deploy-fold.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-0919d --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-deploy-fold-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house.

# Tribunal check of the DEPLOYMENT, ROUND 2 (LAWS L67, ≤3 rounds) — ONLY the fold of round 1's six verified findings (V1, V2, V4, V5, V6, V7 + the optional V3, V8) into `02-deploy-stack-3.md`. Did each fold fix what it names, and did any fold break something? Small on purpose. DO NOT STOP until the report ends `REVIEW DONE …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (Fable `c566654e`, 2026-09-19), not by Dejan. His words on main: `cto-2026-09-18.md` §4 R9, R18–R21; `cto-2026-09-19.md` §4 R1. Prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-19.md"`. Mismatch → `FAILED: authorization mismatch — <what>`. YOU CAN ALWAYS STOP with a `FAILED:` line.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` — laws, UNATTENDED RULES, PREFLIGHT (Astra answered three times today → `astra: UP (carried)`, no probe), LESSON, §2 launch spellings, and `2026-09-18/11-review-deploy-stack.md` §3 bind you UNCHANGED, with `scratch/review-deploy-fold-0919/` in every path. `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-delta-0919/REVIEW.md` = round 1 (your predecessor's Verdict table). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-deploy-fold-0919/REVIEW.md`.

## 1. Packet in `scratch/review-deploy-fold-0919/` (`mkdir -p` first; Read → Write, byte-identical)
(1) `02-deploy-stack-3.md` = `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md` (as it is NOW, after the fold) · (2) `fold-r2.worddiff.txt` = `…/2026-09-19/04-packet/fold-r2.worddiff.txt` — the desk's pre-computed word-level diff, before the fold → after: `[-removed-]`, `{+added+}` · (3) `round1-verdicts.md` = the `## Verdict table` section + the two UNVERIFIABLE items of round 1's REVIEW.md, verbatim · (4) `state.md` = `…/2026-09-19/04-packet/state.md` · (5) `QUESTIONS.md`, verbatim below.

QUESTIONS.md (verbatim): "You are an independent reviewer from another house, one of three. Read ONLY the files in this folder; you cannot run commands. This is ROUND 2. `02-deploy-stack-3.md` is an UNATTENDED production deploy prompt that will run today. `round1-verdicts.md` is what three houses found in round 1 and what a verifier confirmed (V1–V8). `fold-r2.worddiff.txt` shows EVERY change made since: `[-text-]` removed, `{+text+}` added. Give SEVERITY (BLOCKER = will fail mid-deploy or can leave production broken / MAJOR / MINOR), the STEP, the EVIDENCE (quote the line), the FAILING SCENARIO; say 'none found' rather than inventing; do not re-file a round-1 finding that the fold fixes. (Q1) For each of V1, V2, V4, V5, V6, V7: does the fold fix exactly what the finding names? Quote the new sentence that does it. (Q2) Did any fold BREAK something: a new command that is not covered by the launch line's allowlist, character for character (look at the new `diff --stat … -- . ':(exclude)docs'`, the `diff --stat main HEAD -- configs/cobalt/rules.yaml`, the `log -1 --format=%D`, the `grep -c`, the `db query … to_regclass …`, the new `launchctl print` / `bootout` in §6 step (0)) — or a new instruction that contradicts an old one elsewhere in the file (the 18:30 start versus any remaining '19:00'; §6 step (0) versus §6 (4); the V6 'leave both residents DOWN' branch versus the file's rule that residents DOWN is the one outcome to avoid)? (Q3) §6 step (0): is `bootout` before a code revert right in EVERY entry path, including entry from 3.4–3.8 where the residents are already down (does `launchctl print` failing there derail the step)? (Q4) the V1 exception: can it be abused to wave through a real code change? (Q5) anything else the fold made worse. End with exactly one line: `VERDICT: RUN AS IS / RUN AFTER FIXES <finding numbers> / DO NOT RUN`."

## 2–4. Launch, verify, close — `01-review-harness.md` §2 and §4 and `11-review-deploy-stack.md` §3 EXACTLY, with this folder's path; for every allowlist claim quote the rule AND the step's command side by side. One attempt per house. You fix NOTHING.
