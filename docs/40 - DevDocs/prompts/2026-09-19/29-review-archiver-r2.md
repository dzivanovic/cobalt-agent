MODEL: Sonnet 5 (`claude-sonnet-5`) — read, stage, launch three reviewers with THREE DIFFERENT reads, verify their findings; no write path · SEAT: review hub `review-archiver-r2-0919`, launched by the CTO desk in the background, SAME worktree as round 1 (it already exists — do NOT create it, do NOT `git worktree add`). NO NEW RULE: the `--allowedTools` list below is copied byte for byte from `20-review-archiver-build.md`'s launch line, with exactly ONE string changed — the `mkdir` path, from `scratch/review-archiver-0919` to `scratch/review-archiver-r2-0919` — because this round needs no rule `20` did not already carry (same repository, same branch, same `git -C /Users/cobalt/cobalt show*`/`log*` reach; the unused `git -C .../s2-p2-cards *` rules stay, unused, for the same reason they stayed in `20`). Proof: sorted `comm -3` between `20`'s rule-string list and this prompt's rule-string list yields exactly one line on each side, both the `mkdir` string:
```
"Bash(mkdir -p scratch/review-archiver-0919)"
	"Bash(mkdir -p scratch/review-archiver-r2-0919)"
```
If this round turns out to need something those rules cannot do, the missing rule is NAMED below, never invented. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/29-review-archiver-r2.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control review-archiver-r2-0919 --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(codex exec --skip-git-repo-check -m gpt-6-astra -s read-only *)" "Bash(mkdir -p scratch/review-archiver-r2-0919)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on; never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO git write, no vault write · METER: Anthropic small; one headless review per house (L47 — probe before counting on a house).

# Three-house check of `archiver/append-0919`, ROUND 2 of ≤3 (LAWS L67) — THREE DIFFERENT READS, not one packet read three ways. Round 1 (`/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-0919/REVIEW.md`) ended with ONE house reading (Grok: `FIX FIRST F1 F3`, 1 BLOCKER/4 MAJOR/2 minor, all 7 hub-verified REAL; Gemini HARNESS; Astra METER). A fix chunk (`26-archiver-round2.md` + `26-packet/verdicts.md`) then folded all seven findings into the branch — round-2 code tip `9056e73`, report tip `fb825df`, stop line: `ARCHIVER APPEND R2 9056e73 | … | F1 restate fixed · backfill-missing shape already-safe | F2 command-path tests added | F3/Q10 race tests offline+requires_db | F4 four tests rewritten fixed | F5 exit code 2 on cobalt fixed | F6 shadow night = ET date fixed | F7 NULLS NOT DISTINCT pinned fixed | radar diff: empty yes | write_mode ships: upsert (unchanged) | RESTARTS: com.cobalt.aset com.cobalt.radar | CARRIED: 23 requires_db tests never run | ESCALATE: 4`. **GROK reads ONLY THE FOLD** (what changed since round 1's code tip — it already knows the whole build from round 1). **GEMINI AND ASTRA never read round 1 — they read the WHOLE updated build** (three-dot, non-docs, logic only), the FINAL design, and round 1's verdict table for context, so they can judge the fold in place rather than blind. DO NOT STOP until the report ends `ARCHIVER CHECK R2 …` or `FAILED …`.

AUTHORIZATION — VERIFY IT YOURSELF. Same standing law as round 1: LAWS.md L67, ruled 2026-09-18 17:30 ET (`cto-2026-09-18.md` §4 R18, R19–R21) — prove with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-18.md"`. The fold's own debt: round 1's report named "three-house check" OWED; the round-2 report's own `## ROUND 2 ESCALATE` and stop line show the fixes are in and awaiting THIS check — prove with `git -C /Users/cobalt/cobalt log --oneline -4 archiver/append-0919 -- "docs/40 - DevDocs/reports/archiver-append-build-2026-09-19.md"` (**name the branch ref** — this report lives ONLY on `archiver/append-0919`, same lesson round 1's own check recorded: a bare `log -- <path>` from `main` returns nothing and looks like a missing commit rather than a wrong command). A mismatch → last line `FAILED: authorization mismatch — <what>`, stop. YOU CAN ALWAYS STOP: a written `FAILED: <step> — <concern>` line is always a correct ending. Laws, one line each: L33 reviewers are read-only verification runs · L35 you trust the files, never a reviewer's claim, and never the report's own claims (`facts.md` and `build-report.md` in the packet are claims — verify them against the diffs) · L36 reviewers spawn nothing · L37 no model-judged approval: you verify, the desk and Dejan decide · L44 every house gets a complete, sufficient packet for its assigned read (Grok's fold packet and Gemini/Astra's whole-build packet are DIFFERENT by design this round — that is not an L44 violation, since round 1 already gave Grok the whole build and this fold-only read is a deliberate efficiency for a house that already has the context; Gemini and Astra get the WHOLE build because they have none of that context) · L47 a meter is checked BEFORE a house is counted on · L52 nothing in this build may touch scoring, ranking or a card value · L67 ≥3 checkers, ≤3 rounds, floor of 2 houses if a meter runs out.

INDEX CARD (you; the reviewers get their assigned packet only): `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/01-review-harness.md` §2 (launch), §4 (close), `2026-09-18/11-review-deploy-stack.md` §3 (verify), and `27-review-p4-gemini-astra.md` (the split-call Gemini pattern and the Astra wait/probe rule) BIND you here UNCHANGED, with `scratch/review-archiver-r2-0919/` in every path. THREE LESSONS carried into this run: (a) Read→Write strips trailing whitespace — a copied diff is shorter than its source by its blank-context lines; state it once in `## Packet`, never chase it; (b) `git log -- <path>` for a branch-only file needs the branch ref named explicitly; (c) **the Gemini harness lesson of today** (`scratch/review-p4-0919/REVIEW-B.md` and `27-review-p4-gemini-astra.md`): agy/Gemini printed NOTHING on every packet above ~150 KB or that named files NOT present in the folder ("the files in this folder" phrasing, or naming excluded fixtures) — packets under ~150 KB with an EXPLICIT file list and a "you have NO tools, never run a command" opening sentence printed clean answers. This round's whole-build packet is pre-split into four ~76–112 KB area buckets for exactly this reason — give Gemini ONE bucket per call, one attempt per call, NEVER a retry on the same bucket (if a bucket HARNESSes, record it and move to the next bucket; do not re-attempt). REPORT: `/Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-r2-0919/REVIEW.md` — same gitignored-scratch pattern, nothing outside it, no commit question: §0 Headline ≤5 lines → `## PREFLIGHT` → `## Packet` → `## Grok (fold)` → `## Gemini (whole build, 4 calls)` → `## Astra (whole build)` → `## Verdict table` → last line.

## 1. Packet — pre-staged by the desk in `docs/40 - DevDocs/prompts/2026-09-19/29-packet/`; copy each into `scratch/review-archiver-r2-0919/` (`mkdir -p` first; Read → Write). The desk's SIZE TABLE (verify with `ls`/`wc -c`; re-stage ONLY a file that is missing or whose byte count differs):

| file | bytes | for |
|---|---|---|
| `fold.diff` | 56,795 | Grok only |
| `verdicts-r1.md` | 7,212 | Grok, Gemini, Astra (context) |
| `facts.md` | 14,992 | Grok, Gemini, Astra |
| `build-report.md` | 81,852 | Grok, Gemini, Astra (the branch's current report, round 1 + round 2) |
| `QUESTIONS-fold.md` | 6,343 | Grok only |
| `QUESTIONS-whole.md` | 6,760 | Gemini, Astra |
| `spec-final-design.md` | 38,813 | Astra (whole design) |
| `spec-excerpt-A-settings-heartbeat-store.md` | 9,970 | Gemini call A |
| `spec-excerpt-B-migrations-reconcile.md` | 15,453 | Gemini call B |
| `spec-excerpt-C-runner-shadow-report.md` | 3,666 | Gemini call C |
| `spec-excerpt-D-quiet-cli.md` | 9,881 | Gemini call D |
| `build-A-settings-heartbeat-store.diff` | 97,907 | Gemini call A, Astra |
| `build-B-migrations-reconcile.diff` | 111,567 | Gemini call B, Astra |
| `build-C-runner-shadow-report.diff` | 76,100 | Gemini call C, Astra |
| `build-D-quiet-cli.diff` | 88,636 | Gemini call D, Astra |

The four `build-*.diff` files ARE, together, the WHOLE non-docs three-dot diff `main...archiver/append-0919 -- src tests configs` (374,210 bytes total, split ONLY because that exceeds a comfortable single read and because of the Gemini harness lesson above) — `facts.md` states the byte sum matches exactly. `ops` and the rest of `configs` are unchanged beyond the one `tunables.yaml` block already in bucket A — worth confirming with a listing, not assuming.

## 2. Grok — reads ONLY the fold
Launch once, background, one attempt: read `fold.diff`, `verdicts-r1.md`, `facts.md`, `build-report.md`, `QUESTIONS-fold.md` — follow `QUESTIONS-fold.md` exactly, verbatim, as staged. Grok already has round 1's whole-build context; it does not need `build-*.diff` or the design excerpts. Record its verdict and its per-finding `F1…F7` tags verbatim.

## 3. Gemini — the WHOLE updated build, as FOUR SEPARATE calls, one attempt each, never a retry
For each bucket (A, B, C, D), ONE `agy` call, `run_in_background`, using the P4-call-A wording that worked (from `27-review-p4-gemini-astra.md` §3.1): open with "You have NO tools for this task except reading local files already present in one folder. Never run a command, never list a directory, never fetch a URL, never search the web — if answering a question would require any of those, write UNVERIFIABLE FROM PACKET for that item and continue." then name the EXPLICIT file list for that call only (`QUESTIONS-whole.md`, `verdicts-r1.md`, `facts.md`, `build-report.md`, the ONE `spec-excerpt-*.md` for that bucket, the ONE `build-*.diff` for that bucket — six files, none named that is absent), then "Follow QUESTIONS-whole.md exactly, answering only the questions your files can address (mark the rest N/A THIS CALL). Do NOT write any file: print your complete findings as your answer." Write each call's printed answer, byte for byte, to `gemini-findings-A.md` / `-B.md` / `-C.md` / `-D.md`. A call that HARNESSes (any tool-permission denial, any "no output produced") is recorded as `gemini bucket <X>: HARNESS` and you move to the next bucket — NEVER retried, NEVER split further (the bucket is already sized for this). Combine all four calls' answers into one `## Gemini` section; your close-line `gemini:` field aggregates across whichever buckets answered.

## 4. Astra — runs LAST, after a fresh probe, one attempt, record its token count
Astra's Codex meter reset at 11:47 ET today; another hub (`p4-verify`'s) reads roughly 215k tokens on it before this run — probe fresh, do not assume UP from an earlier probe elsewhere. 4.1 PROBE first, `run_in_background`: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Reply with only the word OK."` — record verbatim, and record that this is a FRESH probe taken after Gemini's calls (so its result reflects the meter state at the time Astra actually runs, not an earlier moment). 4.2 If UP: launch ONCE, `run_in_background`: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "You are GPT-6 Astra, the cross-house reviewer. Read ONLY the files in /Users/cobalt/cobalt-wt/agy-trial/scratch/review-archiver-r2-0919/ : QUESTIONS-whole.md, verdicts-r1.md, facts.md, build-report.md, spec-final-design.md, build-A-settings-heartbeat-store.diff, build-B-migrations-reconcile.diff, build-C-runner-shadow-report.diff, build-D-quiet-cli.diff. Start with QUESTIONS-whole.md and follow it exactly, answering every question (you have every bucket, so nothing is N/A). Do NOT run any command other than reading those files and do NOT write any file: print your complete findings as your answer."` → write its printed answer, byte for byte, to `astra-findings.md`. **Record its token usage if the output states one** (round 1's probe used 2,277 tokens and the real run used 136,399 on a much smaller packet; this packet is roughly 3x larger — a METER partway through is expected, not a surprise; keep whatever it printed before metering, marked partial). 4.3 If METER (either probe): record `astra: METER` with the verbatim message and its token count if given, and END — the desk reruns Astra later; this is a correct, non-failing ending, never a second probe here, never a loop, never a self-scheduled wait (no `CronCreate`/`ScheduleWakeup` in this allowlist).

## 5. Verify — using ONLY this launch line's rules, nothing new
Every BLOCKER and MAJOR from Grok, Gemini, and/or Astra against the REAL files: a diff claim against `git -C /Users/cobalt/cobalt show archiver/append-0919:<path>` (reachable under the SAME `show*` rule, no worktree `cd` needed) and against `/Users/cobalt/cobalt-wt/archiver-append/` with Read/`grep` (covered by `--add-dir /Users/cobalt/cobalt-wt`, no Bash rule needed for a plain read). If a verification genuinely needs a command this launch line does not cover (an independent re-derivation of a diff with a fresh `git diff`, or `uv run pytest`/`uv run cobalt jobs restarts` to re-check a suite or RESTARTS claim) → `UNVERIFIABLE FROM READS`, name the exact missing rule string, do not invent it. If a finding from Gemini or Astra duplicates one Grok already raised (or one round 1 already raised), say so and cite it rather than re-litigating — carry it forward by reference. One extra column per REAL finding: `blocks the branch shipping? yes/no — why`. You fix NOTHING and edit NOTHING outside `scratch/review-archiver-r2-0919/`.

## 6. Close
`## Verdict table` combines all three houses' findings against the real files, with a row per round-1 finding (F1–F7) showing FIXED / NOT FIXED / REGRESSED per house that actually checked it, plus any NEW finding this round surfaced. Last line, beginning EXACTLY `ARCHIVER CHECK R2`:
`ARCHIVER CHECK R2 · grok-fold: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · gemini-whole: <B>/<M>/<m> <verdict|HARNESS(n of 4 buckets)|METER|TIMEOUT> · astra-whole: <B>/<M>/<m> <verdict|HARNESS|METER|TIMEOUT> · houses that have read the WHOLE build: <n> of 3 · hub-verified REAL: <k> (blocks shipping: <d>) · NOT REAL: <n> · UNVERIFIABLE: <u> · F1: <FIXED|NOT FIXED|REGRESSED> · F2: <…> · F3/Q10: <…> · F4: <…> · F5: <…> · F6: <…> · F7: <…>`. Then stop.
