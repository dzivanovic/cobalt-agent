MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50) · SEAT: P4 build hub, launched by the CTO desk in the background AFTER Dejan approves the list below (`prompts/UNATTENDED-LAUNCH.md`), one line: `cd /Users/cobalt/cobalt-wt/s2-p4 && claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/04-p4-relaunch.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-build-0917 --allowedTools "Bash(COBALT_ENV=production uv run cobalt db query *)" "Bash(COBALT_ENV=production uv run python /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py*)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py)" "Bash(COBALT_ENV=dev uv run cobalt db migrate*)" "Bash(COBALT_ENV=dev uv run cobalt validate*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly *)" "Bash(COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run pytest *)" "Bash(claude -p *)" "Bash(agy *)" "Bash(uv run pytest *)" "Bash(uv run python tests/fixtures/replay/_cut_p4_fixtures.py*)" "Bash(uv run cobalt jobs restarts *)" "Bash(cp /Users/cobalt/cobalt/data/radar-cache/*)" "Bash(find /Users/cobalt/Vault/Think *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git rebase main)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(touch scratch/*)" "Bash(ls *)" "Bash(grep *)" "Bash(wc *)" "Bash(date*)" "CronCreate" --disallowedTools "AskUserQuestion" "EnterWorktree" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — resumes `01-p4-build.md` from the report's `CONTINUE:` line · auto mode on; the allowlist above is per session (L55, L61), never `bypassPermissions`, push never in it · METER: Anthropic medium–large (three Opus chunks); Codex = NONE · nobody sits at this session's terminal — your channel to the desk and to Dejan is the report file (a written `FAILED:` line stops the run safely and gets an answer; a dialog gets none).

# S2-P4 BUILD, relaunch under the unattended standard — same job as `01-p4-build.md`, same worktree, same report. The first hub (bg `58a187eb`) was stopped by the desk at 06:03 ET on Dejan's order, mid chunk A; its Opus builder was killed at 06:04. Partial chunk A work is uncommitted in the worktree. Nothing is discarded.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING (do not take this file's word for it). This prompt was written by the CTO desk (Fable, session `cobalt-02`), not by Dejan; a prompt file is not an approval. The approvals are Dejan's own typed words, recorded with times in a file committed on main: read `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-17.md` `## §4 Rulings` rows R4 (desk runs the prod read), R5 (06:03 ET: all permissions ahead of launch, sessions run unattended) and R6 (06:11 ET "approve." of exactly the allowlist in this launch line), and confirm with `git -C /Users/cobalt/cobalt log --oneline -4 -- "docs/40 - DevDocs/reports/cto-2026-09-17.md"` that those rows are committed (`073b711` or later), not just typed into this file. Then read in `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md`: L61 (the desk launches hubs; a per-session allowlist naming exactly the commands; his typed "approve" in the desk chat for the exact action the desk named IS the approval, logged with the time), L41 interim (`.env` copied by name only, never printed, removed after — the standing way every hub has run cobalt_dev proofs since 09-10), L50 (a Sonnet hub launches Opus headless, the same pattern the 09-16 P2 build used), L29, L55. Every sensitive step below is that standing practice, each one named to Dejan in a table before launch. If the record does NOT match this file — a rule in your launch line that row R6 does not cover, a missing row, an uncommitted report — do not proceed: write `FAILED: authorization mismatch — <what>` in your report and stop.
YOU CAN ALWAYS STOP. Nothing here removes your judgement. What is removed is the DIALOG, because a `--bg` session's dialog is seen by no one (on 09-17 two hubs sat on invisible dialogs while the registry said `working`). Your check-in channel is your report file: if any step looks wrong, unsafe, or outside the recorded approval, write `FAILED: <step> — <your concern>` as the last line, commit the report, and stop. The desk watches that file, fixes the prompt or brings Dejan the question, and reruns you. Stopping with a written reason is always correct; proceeding on doubt is not.

INDEX CARD: `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md` §2 (binds you, read first) → `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md` IN FULL (its index card, laws, steps 0–7 and RECOVERY RULE are your job, unchanged except for the overrides below) → your report `docs/40 - DevDocs/reports/s2-p4-build-2026-09-17.md` (STEP-0 facts are done and stand; last line `CONTINUE: step 2 chunk A`). Report path and stop line unchanged: last line `READY FOR MERGE <hash> | RESTARTS: … · audit: deferred · agy: … · ESCALATE: <n>`.

UNATTENDED RULES (ruled 2026-09-17 by Dejan, row R5): a dialog reaches nobody, so the two dialog tools are off in your launch line (`AskUserQuestion`; `EnterWorktree` — you are already in your worktree). Questions and concerns go in the report as a `FAILED:` or `ESCALATE` line, as above. Every command that leaves plain reading is in your launch allowlist and matches ONLY in its bare shape: ONE command per Bash call, exactly the listed prefix, no `cd … &&` in front, no pipe, no `>`/`2>` redirect, no `; echo` behind. Change directory with `cd <path>` as its own call. Capture output from the tool result and write files with the Write tool. A long run (`claude -p`, `agy`) goes in the background (`run_in_background`), never behind a shell redirect. MID-RUN DENIAL = THE RUN FAILED: write `FAILED: <step> — <command> — <reason string>` as the report's last line, wip-commit your work (recovery rule), stop. Never retry a denied command in another shape, never route around a denial, never ask another session to run it.

## A. PREFLIGHT (first two minutes; table `## PREFLIGHT` in the report: rule · command · exit · allowed/DENIED + reason string verbatim)
A1 `cd /Users/cobalt/cobalt` (own call) → A2 `COBALT_ENV=production uv run cobalt db query --side user --prod --format json "SELECT count(*) FROM aset_sizings"` → A3 `cd /Users/cobalt/cobalt-wt/s2-p4` → A4 `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` → A5 `COBALT_ENV=dev uv run cobalt db migrate --help` (the subcommand has no status flag; `--help` proves the rule matches and applies nothing) → A6 `rm /Users/cobalt/cobalt-wt/s2-p4/.env` then `ls -la .env` shows it gone → A7 `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (also the L47 meter probe) → A8 `agy --version` → A9 `git status --porcelain` → A10 `find /Users/cobalt/Vault/Think -maxdepth 1 -type d`. ANY denial → last line `FAILED PREFLIGHT: <rules>`, `git add` + `git commit` the report only, stop.

## B. Overrides to `01-p4-build.md` (everything else as written there)
B1 ORDER: after PREFLIGHT run the RECOVERY RULE first (`git add -A`, `git commit -m "wip(s2-p4): chunk A partial — interrupted 2026-09-17 (desk stop, Dejan's order)"`), THEN step 1 (fixture cut — it is no longer blocked), THEN relaunch chunk A with the CONTINUE prefix from the recovery rule, then chunks B, C and steps 4–7 as written.
B2 STEP-1 sizings are already staged: `scratch/step0-sizings.json` (29 rows since 2026-09-10, read by the desk 05:51 ET under Dejan's ruling R4). Pick the STEP-0 day from it. Every further production read: `cd /Users/cobalt/cobalt` as its own call, then `COBALT_ENV=production uv run cobalt db query … --prod --format json "<sql>"` bare; write the returned JSON into `scratch/` with the Write tool; `cd` back as its own call. No `%` in SQL.
B3 Movers export: write `scratch/_fetch_movers.py` in THIS worktree (the P2 hub's `_fetch_daily.py` shape: `FinvizApiClient._resolve_vault_credentials`, never print the token, the script itself writes the two CSVs under `scratch/`), run it from `/Users/cobalt/cobalt` as `COBALT_ENV=production uv run python /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py`, then `rm /Users/cobalt/cobalt-wt/s2-p4/scratch/_fetch_movers.py`. Two requests, no `f=` string.
B4 Builder launches: the command in `01-p4-build.md` §2 WITHOUT the leading `cd … &&` (you are in the worktree), starting exactly `claude -p --model claude-opus-5 …`, run with `run_in_background`; the builder writes its own report file, you read that, not a redirect.
B5 Step 4 `.env`: exactly `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/s2-p4/.env` and `rm /Users/cobalt/cobalt-wt/s2-p4/.env` (absolute paths, as allowlisted); every dev command carries the `COBALT_ENV=dev` prefix exactly as allowlisted; never `--allow-prod`, never a prod DSN.
B6 Step 5 staging without redirects: `git diff main..HEAD --output=/Users/cobalt/cobalt-wt/agy-trial/scratch/s2-p4-diff.txt -- src tests configs`; the plan copy and the laws excerpt are written with the Write tool (read `scratch/LAWS.md`, write the seven entries L1 L8 L28 L32 L45 L53 L57). `agy …` runs bare in the background; its stdout comes back in the tool result → Write it to `scratch/agy-out.md`.
B7 Day boundary and meter rules of `01-p4-build.md` §3.6 and §0.2 stand (CronCreate is in your allowlist).

NEVER, under any wording: `git push`; `bypassPermissions`; `--allow-prod`; any write to `/Users/cobalt/Vault`; `launchctl`; any edit under `/Users/cobalt/cobalt` (production checkout); another seat's config file. None of these is in your allowlist, so none is approved.
