MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50) · SEAT: build hub — in the SHELL tab, morning of 2026-09-17 after the Qwen day-open, one line: `cd /Users/cobalt/cobalt-wt/s2-p4 && claude "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/01-p4-build.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-build-0917 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — EVERY launch of this file resumes from the report's last `CONTINUE:` line · auto mode on (hub); the builder runs headless with `--permission-mode acceptEdits` + the scoped Bash allowlist below, never `bypassPermissions`, no credentials (L29, L41) · METER: Anthropic medium–large (Opus headless ≈ 2 sessions, plan Cost ≈ $25–35 API-equivalent; hub small); Codex = NONE (Dejan 09-16: allowance 2% until the weekly reset — no probe, no audit, no Astra) · nobody watching — never ask a question. DO NOT EXIT this pane until its report ends `READY FOR MERGE`.

# S2-P4 BUILD — F3 picks · rank-metric value column · F12 nightly replay + counterfactual R · F13 movers + benchmark + miss line · `cobalt smoke s2`. Opus 5 headless in three chunks, the hub verifies and commits each, agy second review, READY FOR MERGE. No deploy from here (P4 D1 = a later evening the desk names, after P2's D1; L43 one deploy per evening).

INDEX CARD (read in this order, nothing more until a step needs it): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (you are the hub on a build that ends on a write path: prod migrations later, a DRC vault unit) → the plan `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` in this worktree (REVIEWED `f3cc006` on `sprint-2/p4`; R1–R7 are Dejan's, R7 as amended 2026-09-16; every `[amended 2026-09-16 per Astra …]` block is part of its step; 0 dissent) → `docs/40 - DevDocs/reports/s2-p4-review-2026-09-16.md` §0 → `docs/40 - DevDocs/prompts/2026-09-16/01-p2-build.md` §1–§5 (yesterday's identical hub pattern on P2: fixtures from prod reads, headless launch with allowlist, per-chunk verification, RECOVERY RULE, agy staging) → `docs/40 - DevDocs/reports/s2-p2-build-2026-09-16.md` (what P2 shipped: the formation callable P4's STEP-5 adapter binds to — R1-21/R2-7; migrations 0006/0007 numbers; where `CardStore.fill` ended up) → your report `docs/40 - DevDocs/reports/s2-p4-build-2026-09-17.md` (create at step 0; if it exists, obey its `CONTINUE:` line). Laws that bind, one line each: L35 trust the artifact, never the builder's claim · L36 you are the only spawner · L41 interim: no credential in this worktree; DB proofs copy `~/cobalt/.env` by name and delete it · L45 fixtures cut from production reads, real-shape, attribution stripped; the DRC note never enters the repo · L46 branch committed clean after every chunk; main untouched · L47 meter is a precondition; Opus outage = self-scheduled wait, never Sol this week · L29 Opus floor on the write path; never bypassPermissions · L42 RESTARTS derived, UNCLASSIFIED → ESCALATE (`configs/cobalt/smoke/s2.yaml` is the known candidate) · L53 ONE total-demand computation across radar + archiver + replay with a refusal test · L57 every cf-R input stored · L28 the miss line is a marker-bounded unit, create-if-absent never, human wins · L8 n<30 renders insufficient · L48 evidence in the report before chat.

## 0. Preconditions (read-only) and report
1. `git status --porcelain` empty here; `git log --oneline -1` = `14c30f4` (or later docs-only); `git rebase main` — P2 is NOT expected on main yet (its D1 is a separate evening); if P2 IS on main, the rebase must apply clean (docs-only branch) and STEP-5's adapter binds to the shipped callable; either way record `main = <hash>, P2 merged: yes/no` in the report. A conflict → stop and report.
2. Anthropic meter probe: `claude -p --model claude-opus-5 "Reply with exactly the word OK"` → `OK`. A usage-limit message → record its reset time, schedule your own wake-up (CronCreate) for that time + 5 min with the prompt `Re-run 01-p4-build.md from step 0`, reply in one line, wait.
3. `mkdir -p scratch && echo 'scratch/' >> "$(git rev-parse --git-path info/exclude)"`; `cp "/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md" scratch/LAWS.md`.
4. Create the report with §0 Headline (≤5 lines, updated every step, line 1 = `DO NOT EXIT until READY FOR MERGE`), `## STEP-0 facts`, `## Launches` (chunk · start · end · exit · report path · suite result · commit), `## Verification`, `## Second review (agy)`, `## Audit (L52-d)` = `AUDIT: deferred — Codex out until the weekly reset (Dejan 2026-09-16); the desk schedules the recompute of the cf-R bundle before the first Friday review uses it`, `## ESCALATE`, last line `CONTINUE: step 1`.

## 1. STEP-0 of the plan — the hub cuts the L45 fixtures from PRODUCTION READS and records the facts
Run from `~/cobalt` (credentials live there). Reads only; `--prod`; no `%` in SQL (use `~`). Raw output under `scratch/` only. Transform script `tests/fixtures/replay/_cut_p4_fixtures.py` (deterministic, re-runnable, reads from `scratch/`), same rules as P2's `_cut_p2_fixtures.py`: one synthetic anchor day, `@<12-hex>` suffixes zeroed, sha fields zeroed, `user_id` → 1, free-text reasons → `"<reason>"`, no personal attribution.
1. The STEP-0 day: `cobalt db query --side user --prod --format json "SELECT s.id, s.ticker, s.state, s.direction, s.entry, s.stop, s.created_at FROM aset_sizings s WHERE s.created_at::date >= DATE '2026-09-10' ORDER BY s.created_at"` → pick one trading day with ≥1 unfilled card whose entry was crossed (compare against `system.bars` i1 for that ticker/day), plus a filled card and a passed one if that day has them; else the nearest day that does, and say so. Record the day and the card ids in the report.
2. `tests/fixtures/replay/cards-day.real-shape.json` ← `aset_sizings` + `card_transitions` rows of that day (plan §3 row 1). `tests/fixtures/replay/bars-day.real-shape.json` ← `system.bars` i1 for those tickers that day. `tests/fixtures/replay/membership-day.real-shape.json` ← P3's `tests/fixtures/radar/panel-pool.real-shape.json` reused + that day's `radar_membership` episodes. `tests/fixtures/radar/pool-metrics.real-shape.csv` ← one cached screen CSV from `~/cobalt/data/radar-cache/<day>/`, trimmed to 20 rows, generic filename.
3. Movers: one hub-side UNFILTERED export per side via the existing vault-token path (the P2 hub's `scratch/_fetch_daily.py` shape: `FinvizApiClient._resolve_vault_credentials`, never print the token, delete the script after) hitting `/export/screener?v=152&o=-change` and `o=change` with NO `f=` string → `tests/fixtures/replay/movers-gainers.real-shape.csv`, `movers-losers.real-shape.csv`, trimmed to 60 rows, header byte-exact. Record the header line (only) in the report. Record that day's top gainer and whether it was in the pool (`radar_membership` for the day) — the "Tesla-class" case.
4. Proofs: `grep -rE '@[0-9a-f]{12}' tests/fixtures/ | grep -v '@000000000000' | wc -l` = 0; `grep -rlE '2026-09-' tests/fixtures/replay/ tests/fixtures/radar/*.csv tests/fixtures/radar/*.json | wc -l` = 0; `grep -rlE 'f=|expr:|trade_def:' tests/fixtures/ | wc -l` = 0; `git check-ignore` none. The DRC note is never copied: the `requires_vault` test reads the live DRC template shape, run by you in step 4.
Update the report; `CONTINUE: step 2 chunk A`.

## 2. Builder launches — Opus 5 headless, three chunks; the hub verifies and commits after each (step 3)
RECOVERY RULE (from `01-p2-build.md` §2, verbatim in force): on any launch where `git status --porcelain | grep -v '^?? scratch/'` is non-empty AND the chunk named by `CONTINUE:` has no builder report → vault check against the existing marker, `git add -A && git commit -m "wip(s2-p4): chunk <X> partial — interrupted <date>"`, relaunch that chunk with `CONTINUE: a previous run of this chunk was killed mid-way; its partial work is the last commit — run 'git show --stat HEAD' and 'git diff main..HEAD --stat', keep what is correct, finish the chunk exactly as specified, write your report as one run.` prepended; never discard; no squash.
Before EVERY launch: `touch scratch/.launch-marker`. The three commands are identical except the CHUNK sentence and the report name.

Chunk A:
```
cd /Users/cobalt/cobalt-wt/s2-p4 && claude -p --model claude-opus-5 --permission-mode acceptEdits --allowedTools "Bash(uv run pytest *)" "Bash(uv run python *)" "Bash(uv run cobalt validate*)" "Bash(uv run cobalt replay *--dry-run*)" "Bash(uv run cobalt smoke *)" "Bash(uv run cobalt --help*)" "Bash(git status*)" "Bash(git diff*)" "Bash(git log*)" "Bash(git show*)" "Bash(ls *)" "Bash(grep *)" "Bash(rg *)" "Bash(cat *)" "Bash(sed -n *)" "Bash(wc *)" "Bash(mkdir -p *)" "You are Opus 5, the builder (LAWS L29 floor), launched headless by the Sonnet hub (L50). Working directory = the git worktree of branch sprint-2/p4 at /Users/cobalt/cobalt-wt/s2-p4. You change files ONLY inside this worktree: never ~/cobalt, never /Users/cobalt/Vault, never another worktree; you have no database and no credentials (L41 interim) and you never look for them. Read in this order: scratch/LAWS.md in full (current law); 'docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md' in full — rulings R1-R7 are Dejan's and bind (R7 as amended 2026-09-16), every '[amended 2026-09-16 per Astra …]' block is part of its step, §2, §3, §4, §5, §6 bind; 'docs/40 - DevDocs/PLAN-TEMPLATE.md'; 'docs/10 - Decisions/' ADR-0008 (tenancy); '/Users/cobalt/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md' STEP-1 and STEP-4 read-only (P2's migrations 0006/0007 and its CardStore.fill / formation callable — P4 never duplicates a table or a seam, and binds to P2's shipped names as recorded in the hub's report §STEP-0 facts); the DevDocs page under 'docs/40 - DevDocs/cobalt/' of every .py you will touch. CHUNK A = build STEP-1 (migrations 0008 system + 0009 user with FORWARD/REVERSE registry entries, placement map, tenancy tests, the immutable receipt/is_current versioning per R1-8/R2-1/R3-1, OWNER/GRANT per R1-2, both merge orders per R1-1/R1-3), STEP-2 (rank_metric + rank_value on radar_membership written by S1 on every scan per R1; MembershipRecord/PoolRow/OpenMember per R1-19/R2-2; /radar value column, pre-deploy NULL renders a dash) and STEP-3 (F3 picks: src/cobalt/cards/picks.py + the pick row inside CardStore.fill's transaction under a SAVEPOINT per R2 as amended R1-4/R1-5/R1-6/R1-7: fill always commits, banner + log on pick failure, 'cobalt cards picks' with MISSING semantics, USER-connection reads, K6 deploy cutoff) exactly as written, failing test first for every §4 sentence those steps own. Rules: Pydantic for every structured value; fail-loud (L1), no plausible-empty artifact, no silent fallback; no new dependency; no person or vendor name in any identifier (L31); every new table on exactly one side, user tables user_id NOT NULL + FK + GUC default (L32, ADR-0008); no derived number without its stored inputs (L57); fixtures under tests/fixtures/replay/ and tests/fixtures/radar/ are hub-cut — never edit or invent one; the DRC note is never copied into the repo; requires_db and requires_vault tests are written by you and run by the hub and must SKIP cleanly offline, never error. Run 'uv run pytest -q tests/cobalt tests/taxonomy' and make it green. One DevDoc page per new .py and an update of every touched page (agent-authored prose). Do not commit. When done, write 'docs/40 - DevDocs/reports/s2-p4-build-opus-A-2026-09-17.md': §0 headline, files changed, tests added (names), suite result verbatim, anything you could not do and why. Reply in chat with only that path."
```
Chunk B: the same command with the CHUNK sentence replaced by: `CHUNK B = build STEP-4 (the one-shot com.cobalt.replay job: src/cobalt/replay/{cli,runner,movers,cards,line,models}.py per R3 with the archiver-done-today precondition as amended R1-15/R1-16/R1-17 — tonight's-occurrence check, enforced deadline before the 21:40 backup, as_job(skip=True) dry-run writes nothing; per-side commit phases per R2-3, receipt-keyed recovery, step failure names the step and earlier steps stand), STEP-5 (F12 replay + counterfactual R per R4 as amended R1-9/R1-10/R1-11/R1-12/R1-21/R2-4/R2-7/R3-2: planned_entry basis, exhaustive gate mapping rule_10 → window → state with the (at,id) tiebreak, horizon H = min(resolved window end, actual session close) with bar_start + 1 minute ≤ H eligibility, gap-through fills at bar open, same-bar stop wins, mfe_r stored as observation, input_stale writes no row, formation adapter bound to P2's named callable or the exact 'formation replay unavailable' line with no rows, every number replays from stored inputs), STEP-6 (F13 movers + benchmark per R5 as amended R1-13/R1-14/R1-20/R2-5: unfiltered top movers both sides archived regardless of watchlist, movers_daily active-flag reconciliation, radar.benchmark read through the real settings-loader seam and never in required keys, and the ONE total-demand gate — a named shared function called at every pre-request site in radar, archiver and replay, with the test that refuses when the sum exceeds radar.finviz_max_rpm while replay alone would pass) and STEP-7 (the miss line per R5: unit drc-misses/miss_line upserted through VaultWriter.upsert_unit, zero-width placement after close_line per R1-18, idempotent, human edit wins + override logged, note absent = loud + creates nothing, n and average with insufficient below 30, --dry-run prints the unified diff)` and the report name `s2-p4-build-opus-B-2026-09-17.md`.
Chunk C: the same command with: `CHUNK C = build STEP-9 ('cobalt smoke s2' per R6: checks loaded through a Pydantic schema from configs/cobalt/smoke/s2.yaml, bad file crashes with a line number; verdict RED on error / AMBER on fail / KNOWN never lowers; the sentinel test that the smoke module imports no writer and calls no write; report file with the numbered table; one test per K-check kind on fakes; each K row carries its exact command and expected output per R1-24) and STEP-8 (DevDocs for every new/changed .py, BACKLOG.md kanban row, ADR-0010 'missed corpus + counterfactual R + picks' in docs/10 - Decisions/ with the formula, gate order and horizon rule; the jobs.yaml row + plist for com.cobalt.replay 21:05 weekdays with its explicit bootstrap action per R1-22/R2-6)` and the report name `s2-p4-build-opus-C-2026-09-17.md`.

## 3. After EVERY chunk — hub verification and commit (L35, L46), exactly as `01-p2-build.md` §3
1. Vault untouched: `find "/Users/cobalt/Vault/Think" -type f -newer scratch/.launch-marker -not -path '*/.obsidian/*' | head -20` → empty or only files Dejan edits himself; anything under `6 - Permanent/`, `1 - Trading/`, `0 - Projects/Cobalt/` → ESCALATE with paths.
2. `git status --porcelain | grep -v '^?? scratch/'`; `git diff --stat`; read the builder's report and the diff of every new module (no new dependency in `pyproject.toml`, no `alert(`/`prompt(`/`confirm(`/`.focus(`/`autofocus`/`http-equiv` in templates, no credentials, no note text, no `f=` string anywhere).
3. `uv run pytest -q tests/cobalt tests/taxonomy` → green (skips allowed for `requires_db`/`requires_vault`); `uv run pytest -q tests/cobalt/test_radar_notes.py -k leak` → green, unmodified.
4. Red → ONE relaunch of the same chunk with `CONTINUE: read your report 's2-p4-build-opus-<X>-2026-09-17.md' and fix only what is red below, nothing else: <last 40 lines of pytest>` prepended. Still red → `git add -A && git commit -m "wip(s2-p4): chunk <X> RED — see report"`, `ESCALATE: chunk <X> red after one relaunch`, `CONTINUE: step 2 chunk <X> (red)`, stop.
5. Green → `git add -A && git commit -m "feat(s2-p4): chunk <X> — <STEP list> (plan f3cc006, Opus 5 headless)"` with the builder's report path and suite line in the body. Update `## Launches`; `CONTINUE: step 2 chunk <next>` (or `step 4` after C).
6. Day boundary: past 19:30 ET, or the meter binds and the relaunch is refused → finish the current chunk's verification only if already green (commit), write `CONTINUE:`, reply ≤6 lines, stop; the same launch line resumes tomorrow.

## 4. Full verification after chunk C
1. Offline suite + leak scan as in 3.3; `COBALT_ENV=dev uv run cobalt validate` → exit 0 (the new jobs row + plist + smoke config load).
2. DB proofs on `cobalt_dev` (L41 interim): `cp ~/cobalt/.env .env` → `COBALT_ENV=dev uv run cobalt db migrate` (0008, 0009 up; if P2's 0006/0007 are absent on dev the numbering gap is harmless — record it) → `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0007` → up again → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (requires_db: fill → pick end-to-end, tenancy on the three tables, rollback order 0009 → 0008, replay end-to-end) → the dev replay: `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly --date <fixture day> --dry-run` (dev vault ONLY — never the real vault for a write-capable command; the dry-run prints the miss-line unified diff: paste it into the report) → `rm .env && ls -la .env 2>&1 | tail -1` shows it gone. Never `--allow-prod`; never a prod DSN.
3. `requires_vault` tests (the live DRC template shape accepts the `drc-misses/miss_line` placement): run with `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think` set; they only read; before/after `find "/Users/cobalt/Vault/Think/1 - Trading" -type f -newer scratch/.launch-marker | head` unchanged.
4. `uv run cobalt jobs restarts main..HEAD` → expected `com.cobalt.aset com.cobalt.radar` + `com.cobalt.replay` as a bootstrap action; `configs/cobalt/smoke/s2.yaml` UNCLASSIFIED → ESCALATE (never dropped, L42) with the classifier rule you would propose.
5. L53 evidence: quote the total-demand test's name and its refusal assertion, and the three call sites (file:line) where the shared gate is invoked.
6. Tests-first evidence: for each §4 area, failing-then-passing test names from the builders' reports, spot-checked by you.
`CONTINUE: step 5`.

## 5. Second reviewer — Gemini 3.1 Pro under `agy` (ruled 09-15 A), exactly as `01-p2-build.md` §5
Stage inside the allow-listed worktree: `mkdir -p /Users/cobalt/cobalt-wt/agy-trial/scratch && git diff main..HEAD -- src tests configs > /Users/cobalt/cobalt-wt/agy-trial/scratch/s2-p4-diff.txt && cp "docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md" /Users/cobalt/cobalt-wt/agy-trial/scratch/plan-s2-p4.md && for L in L1 L8 L28 L32 L45 L53 L57; do n=$((${L#L}+1)); awk "/^### $L /,/^### L$n /" scratch/LAWS.md; done > /Users/cobalt/cobalt-wt/agy-trial/scratch/laws-excerpt.md && wc -l /Users/cobalt/cobalt-wt/agy-trial/scratch/*`. Launch:
```
agy --model gemini-3.1-pro-high --mode plan --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You are a cross-house code reviewer. Everything you need is under /Users/cobalt/cobalt-wt/agy-trial/scratch/: read plan-s2-p4.md (the plan, as amended and reviewed), then s2-p4-diff.txt (git diff main..HEAD of src/, tests/ and configs/ on branch sprint-2/p4), then laws-excerpt.md (laws L1, L8, L28, L32, L45, L53, L57). Task: does the build implement the plan, step by step, and does any change breach a quoted law? Produce ONLY a markdown table: id | severity BLOCKER/AMEND/NOTE | plan step | finding | evidence (file path and line as they appear in the diff). Rules: every finding cites a line you actually read in the diff; no finding without evidence; no rewrite suggestions longer than one sentence; if the build matches the plan, say so in one line above an empty table. Do not modify anything." > scratch/agy-out.md 2> scratch/agy-err.log; echo "exit=$?"
```
Judge every finding (VERIFIED / WRONG / INVENTED); a denial in `agy-err.log` = `HARNESS — not measured` (quote the denied tool and the rule that would cover it; no rerun loop). VERIFIED BLOCKER/AMEND → one bounded builder fix launch (chunk C command with `CONTINUE: fix exactly these verified findings: …` prepended) → re-verify per step 3 → commit `fix(s2-p4): agy-verified findings <ids>`. `CONTINUE: step 6`.

## 6. Audit bundle (L52-d) — produce, do not audit
`cp ~/cobalt/.env .env && COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/dev-vault-cobalt uv run cobalt replay nightly --date <fixture day> --dry-run --audit-out scratch/audit-bundle/ ; rm .env` (if STEP-5 shipped the bundle flag under another name, use that name and say so; if no bundle flag shipped, ESCALATE — L52-d needs the frozen inputs: bars window, cards, gate decisions, cf-R inputs and outputs, hashes). Record `wc -c scratch/audit-bundle/*` and the bundle's hash in the report. `AUDIT: deferred` stands — Codex recomputes after the weekly reset (the desk's prompt), never from here. `CONTINUE: step 7`.

## 7. Close
1. `git status --porcelain | grep -v '^?? scratch/'` empty; `git log --oneline main..HEAD` lists the chunk commits (and fixes); no squash.
2. Report's last line: `READY FOR MERGE <hash> | RESTARTS: <line> · audit: deferred · agy: <verified n / total m | HARNESS> · ESCALATE: <n>`; ≤10 lines to Dejan. Deploy = plan §6 STEP-D1 (20:05 inside the pause, snapshot, `pre-s2-p4` tag, migrations 0008/0009 with `--allow-prod`, kickstart radar then aset, bootstrap `com.cobalt.replay`, Dejan applies `radar.benchmark`, dry-run, 21:05 first live run) on an evening the desk names, after P2's D1 and after the archiver-ceiling ruling (plan ESC 4 = deployment gate) — never from this session.
