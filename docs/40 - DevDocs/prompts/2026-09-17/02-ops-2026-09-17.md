MODEL: Opus 5 (`claude-opus-5`) · SEAT: ops builder/hub — launched by the CTO desk as a background session (ruling 2B) after the 09-17 day-open, one line: `cd ~/cobalt && git worktree add -b ops/2026-09-17 /Users/cobalt/cobalt-wt/ops-2026-09-17 main && cd /Users/cobalt/cobalt-wt/ops-2026-09-17 && claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/02-ops-2026-09-17.md' and follow it exactly." --model claude-opus-5 --remote-control ops-0917 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt --add-dir /Users/cobalt/.grok` · SESSION: fresh · auto mode on (never bypassPermissions; no prod restart, no vault/DB write; another seat's config file is never edited by you — draft the rule in the report, the desk applies) · METER: Anthropic medium; Grok = three read probes; Codex = NONE (until the weekly reset) · nobody watching — never ask a question

# Ops 2026-09-17 — Finviz ceiling 45 (ruled), grok-deny finished (HARNESS classifier rule + probes), agy-trial rebuilt, GOOGLE_OAUTH check, context-ETF i1 proposal, morning proof of `deploy-2026-09-16`

INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `.../areas/cobalt-product-definition.md` (the 09-16 ceiling ruling, last lines) → `.../topics/cto-desk.md` (seat-busy guard: waived for headless read-only probes; another seat's config = desk-applied) → LAWS.md L3, L42, L45, L46, L53, L54 only → `docs/40 - DevDocs/reports/ops-2026-09-16.md` (§ESCALATE 1–5 = your carry, with the hub's own recommendations) → `docs/40 - DevDocs/reports/deploy-2026-09-16.md` (what went LIVE last night, RESTARTS done, P2 yes/no). Report from the first step (L48): `docs/40 - DevDocs/reports/ops-2026-09-17.md` in this worktree — §0 Headline ≤5 lines → one table per item → ESCALATE. No production process is touched from any worktree.

## 0. Morning proof of last night's deploy (read-only, from `~/cobalt`)
`git -C ~/cobalt log --oneline -8` shows `deploy-2026-09-16` (`git -C ~/cobalt tag --list 'deploy-2026-09-16'`); `grep "2026-09-17" ~/cobalt/logs/heartbeat.log | grep -c "ENTERED RED: radar"` = 0 (the flap fix, now live); `grep -E "Email sent" ~/cobalt/logs/heartbeat.err | tail -1` is dated before the 09-16 deploy; `COBALT_ENV=production uv run cobalt heartbeat show` current beat; if P2 merged: `grep -ciE "cards.*(disabled|off)" ~/cobalt/logs/radar.err | tail -1` shows the dark line and `/radar` shows an empty ladder. Anything RED the deploy report did not carry → ESCALATE.

## 1. Finviz ceiling 45 — RULED A by Dejan 2026-09-16 07:18 ET (L53: his number)
`configs/cobalt/taxonomy/tunables.yaml` `radar.finviz_max_rpm`: `value: 40` → `45`, `source: ruling` stays, add to `consumers` the line `"ruled 45 on 2026-09-16 (Dejan; 09-10 throttle report: 4 grids clean to 60 rpm)"`. Failing test first if any test pins 40 (`grep -rn "40" tests/cobalt/test_radar_sources.py tests/cobalt/test_radar_*.py | grep -i rpm`); the L53 total-demand test must now refuse only above 45 (adjust its fixture numbers, keep its shape). `uv run pytest -q tests/cobalt tests/taxonomy` green; `COBALT_ENV=dev uv run cobalt validate` exit 0. `uv run cobalt jobs restarts main..HEAD` → expected `com.cobalt.radar` (tunables reader) — quote the table. Placement note for the report: L53 says caps live in the vault pool block; this cap sits in tunables — say so in one line, do not move it.

## 1b. `daymode.trade_count_band` 2–6 — RULED by Dejan 2026-09-16 08:5x ET ("2 - 6 and make it tunable")
`configs/cobalt/taxonomy/tunables.yaml`: `daymode.trade_count_band.min` `value: null` → `2`, `.max` → `6`, `status: proposed` → `solidified`, `source: ruling`, add `"ruled 2026-09-16 (Dejan)"` to the comment above the block; `dynamic: true` stays (it is his to tune). Failing test first for the proposer: a day with 7 trades is an adverse signal, a day with 4 is not, an unset band still pins to the floor (the existing behaviour, now a regression test). Suite green; `uv run cobalt jobs restarts main..HEAD` → the consumers are the F6 day-mode one-shot (no resident) — expect none beyond item 1's radar; quote the table.

## 2. `ops/grok-deny-0915` — finish (carry: ops-2026-09-16 ESCALATE 2, 3)
In `/Users/cobalt/cobalt-wt/grok-deny` (tip `95cca23`): `git rebase main` (clean or stop). (a) Classifier: `ops/grok-sandbox.toml` derives UNCLASSIFIED — add the `HARNESS` rule beside the existing `.claude/` rule in `src/cobalt/jobs/restarts.py` (or wherever the ops-2026-09-16 report §ESCALATE 3 points) with a failing-then-passing test; `uv run cobalt jobs restarts main..HEAD` on that branch → no UNCLASSIFIED, `RESTARTS: none`. (b) Probes, busy guard WAIVED (desk ruling 09-16: an idle interactive Grok pane never blocks a headless run; record the pane's CPU once for the file, then proceed): from `/tmp`, the three probes exactly as `03-ops-2026-09-16.md` item 1 lists them (`.env` DENIED / branch toml readable / `~/.claude/` DENIED); append `## 6. Re-proof 2026-09-17` to `docs/40 - DevDocs/reports/grok-deny-2026-09-15.md` there; commit there: `ops(grok): cobalt-job profile — classifier HARNESS rule, directory deny proven`. READY line.

## 3. `ops/agy-trial-0915` — rebuild as one commit (carry: ESCALATE 1; desk ruling 09-16: history rewrite approved for this reports-only branch)
First: `ls /Users/cobalt/cobalt-wt/agy-trial/scratch/` and `claude agents --json | grep -c agy-trial` — if the audit hub of 09-16 is still running there (a session with cwd `agy-trial`), STOP this item, write `agy-trial: deferred — audit hub live`, and go on. Else in that worktree: `git checkout -B ops/agy-trial-0915 main && git checkout 6b7c5a4 -- "docs/40 - DevDocs/reports/agy-*.md" && git add -A && git commit -m "docs(agy): 09-15 trial reports (rebuilt on main, one commit)"`; `git diff --stat main..HEAD` touches only `docs/40 - DevDocs/reports/agy-*.md` and `audit-house-2026-09-16.md` if the audit hub left it there (include it); `scratch/` stays excluded via `info/exclude`. READY line.

## 4. `GOOGLE_OAUTH_*` VaultManager entries (carry: ESCALATE 4)
The 09-16 prompt's check command did not exist. Find the real one: `uv run cobalt vault --help` and `uv run cobalt --help | grep -i vault`; quote the listing command that shows key NAMES only (never values). If `GOOGLE_OAUTH_*` entries exist: write the exact removal command into the report as `DEJAN RUNS:` (VaultManager writes are his under L4/L41) — do not run it. If none: `GOOGLE_OAUTH: already removed`, ESCALATE closed.

## 5. Context-ETF i1 bars — PROPOSAL with the diff, NOT applied (carry: ESCALATE 5, now inside the 45 ceiling)
The Lists note (`Think/1 - Trading/Radar Lists.md`, `list: tier_b`, `archive: [i5, i30]`) is a vault unit: propose the exact edit (`archive: [i1, i5, i30]` for tier_b, or a new tier holding only SPY + the 11 sector ETFs at i1) and compute the demand: current 975 req / 23m20s → new request count and rpm; must stay under 45 with the L53 total-demand sum (radar idle in the archiver window; P4's 21:05 replay runs after). Write the numbers and the one-line Lists edit into the report as `DEJAN DECIDES` (a vault note edit is his hand or a Cobalt write under L28); do not touch the vault.

## 5b. Finviz consumers outside the L53 total-demand plan — trace + failing test (P2 build ESCALATE 1, 2026-09-16)
`com.cobalt.prefill-daily` (05:15 ET) and `com.cobalt.prefill-drc` (15:40 ET) call the Finviz transport with `p=d` daily exports outside the radar resident's TokenBucket (P2 report `s2-p2-build-2026-09-16.md` §ESCALATE 1). Establish with file:line: every call site that reaches `finviz_get`/`FinvizApiClient` outside `radar/collector.py` and `archiver/`; the request count each one-shot makes per run (quote from `logs/prefill-*.err` or the code); whether any overlaps a radar scanning session (15:40 is RTH → it does). Verdict + the shape of the fix (one shared gate function called at every pre-request site — the same design P4 STEP-6 builds; if P4 lands first, this is a P4 acceptance check, else a one-line ops fix with a failing test). Report-only unless the fix is one line; RESTARTS derived if code changes.

## 6. Carried, report-only (one row each): `*_scan_id` phantom columns (proposal A opaque ids — ruling owed); `trade_count_band` unset (P4 ESC 2, Dejan's value); RESTIC_PASSWORD rotation (Dejan's procedure); P2 ESC 1 curve-tribunal owner/date; alignment default-map authorization.

## 7. Close
One commit per branch touched (`ops/2026-09-17` = ceiling + tests: `ops(2026-09-17): finviz ceiling 45 (ruled), GOOGLE_OAUTH check, ETF i1 proposal, deploy morning proof` with the derived `RESTARTS:` line in the body; `ops/grok-deny-0915`; `ops/agy-trial-0915`). Report's last line: `OPS 0917 <hash> (RESTARTS radar) · READY: grok-deny <hash> · agy-trial <hash> · ESCALATE: <n>`; ≤10 lines to Dejan. Merges happen in the 20:05 pause by the evening hub (the desk writes it), never from here.
