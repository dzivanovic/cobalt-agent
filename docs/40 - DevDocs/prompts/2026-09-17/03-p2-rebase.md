MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless resolver (`claude -p --model claude-opus-5`, L50) · SEAT: the LIVE P2 hub `s2-p2-cards-12` (interactive, idle, already in the worktree — the desk messages it `Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/03-p2-rebase.md' and follow it exactly.`); standalone only if that session is gone (never two sessions in one worktree, L54): `cd /Users/cobalt/cobalt-wt/s2-p2-cards && claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-17/03-p2-rebase.md' and follow it exactly." --model claude-sonnet-5 --remote-control p2-rebase-0917 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: continued or fresh — every launch resumes from the report's last `CONTINUE:` line · auto mode on (hub); the resolver runs headless with `--permission-mode acceptEdits`, Bash limited to `uv run pytest`, `git add`, `git diff`, `git log`, `git status`; never `bypassPermissions`, no credentials (L29, L41) · METER: Anthropic small (hub) + one short Opus headless run per conflict; Codex = NONE · nobody watching — never ask a question; `ASK DESK: <question> [<time>]` in the report and take the safe default. DO NOT EXIT until the report ends `READY FOR MERGE`.

# S2-P2 rebase — bring `sprint-2/cards` (tip `1806c12`, 19 commits) onto main (`e87b26b` or later, LIVE `deploy-2026-09-16`), resolve the two test-file conflicts with Opus, prove the suite and the migrations unchanged, READY FOR MERGE. No deploy from here: P2 D1 is its own evening the desk names (dark load re-approved by Dejan that day — the 07:07 09-16 approval was for 09-16 only).

INDEX CARD (read in this order, nothing more until a step needs it): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `.../LAWS.md` in full (hub, L59) → `docs/40 - DevDocs/reports/deploy-2026-09-16.md` §3 step 3b (the conflict as it happened: `CONFLICT` at 4/19 replaying `3724dd7 wip(s2-p2): chunk A partial`, files `tests/cobalt/test_sheet_daymode_probe.py`, `tests/cobalt/test_tenancy.py`, aborted) → this worktree's `docs/40 - DevDocs/reports/s2-p2-build-2026-09-16.md` §Close (READY FOR MERGE `2b7c08f`, RESTARTS aset radar, 11 open items) → `git -C ~/cobalt log --oneline 3575174..HEAD` (what landed on main tonight: ops-0915 rebased `8765d62`, ops-0916 `8c3f4a3`, close `e87b26b`). Binding laws, one line each: L46 one run one commit (main clean before you start; you commit clean before you stop); L54 worktree rule (rebase-then-ff on every merge; you rebase, the desk's evening merges); L29 (Opus 5 floor for the resolve — migrations ride on this branch); L35 trust the artifact (suite output, diff, not the resolver's claim); L50 (Opus headless from a Sonnet hub); L60 (recovery rule below; commit clean before any wait); L45 (a real fixture is never edited to fit; if a conflict is inside a fixture, keep the real shape). Report (L48): `docs/40 - DevDocs/reports/s2-p2-rebase-2026-09-17.md` in this worktree — §0 Headline ≤5 lines → tables → ESCALATE → last line `READY FOR MERGE <sha> | rebased onto <main sha> | RESTARTS: <list> | ESCALATE: <n>` or `CONTINUE: <step>`. Stop line: READY FOR MERGE written and the worktree clean.

RECOVERY RULE (L60): a rebase is never left half-done across a stop — if this session stops mid-rebase, the next launch runs `git status`; a `rebase in progress` state is finished by re-running step 2 from the current conflict, never `--abort` unless the report says so. Partial resolver output is never discarded; the same launch line resumes.

## 1. Preconditions (each is one bare command; record the verbatim output)
1.1 `git status --porcelain` → empty except `?? scratch/` (else stop: `CONTINUE: step 1 — dirty worktree` and ESCALATE the lines).
1.2 `git rev-parse --short HEAD` = `1806c12`; `git -C /Users/cobalt/cobalt rev-parse --short main` (record it — this is `<main sha>`); `git -C /Users/cobalt/cobalt status --porcelain` empty (main clean, L46 — if dirty, do NOT touch main; record and continue, the desk's evening handles it).
1.3 `git log --oneline main..HEAD | wc -l` = 19 before the rebase.
1.4 `git diff --stat main...HEAD -- migrations/` → record the two migration files (0006, 0007) — they must be byte-identical after the rebase (step 4).

## 2. Rebase with Opus resolving each conflict
2.1 `git rebase main`. On `CONFLICT`: record `git status --porcelain` and `git diff --name-only --diff-filter=U`.
2.2 For each conflict, launch the resolver headless from this worktree, ONE run per conflict stop, brief written to `scratch/rebase-brief-<n>.md` first (the resolver reads it; nothing pasted): the conflicting file paths; the two intents — `main` side = what `ops/2026-09-15` + `ops/2026-09-16` changed in those files (`git -C /Users/cobalt/cobalt log -p 3575174..8c3f4a3 -- <file>`), P2 side = what the replaying commit changed (`git show <commit> -- <file>`); the rule: keep BOTH intents (every assertion from both sides survives; a test renamed on one side and edited on the other is renamed AND edited; a fixture keeps its real shape, L45); no other file is touched; when done, `uv run pytest <the two files>` must pass; write `scratch/rebase-resolve-<n>.md` with what was kept from each side, then stop. Launch: `claude -p "Read 'scratch/rebase-brief-<n>.md' and follow it exactly." --model claude-opus-5 --permission-mode acceptEdits --allowedTools "Read,Edit,Write,Bash(uv run pytest *),Bash(git diff *),Bash(git show *),Bash(git log *),Bash(git status *)" > scratch/rebase-resolve-<n>.log 2>&1`.
2.3 Hub verifies the artifact, not the claim (L35): `git diff --check` clean, no `<<<<<<<`/`>>>>>>>` left (`grep -rn "^<<<<<<<\|^>>>>>>>" tests/ src/ migrations/` empty), `uv run pytest tests/cobalt/test_sheet_daymode_probe.py tests/cobalt/test_tenancy.py -q` passes. Then `git add <files>` and `GIT_EDITOR=true git rebase --continue`. Repeat 2.2–2.3 at every further conflict (a later chunk commit may re-touch the same files). Three failed resolver runs on one conflict = stop, `CONTINUE: step 2 conflict <n>` + ESCALATE with both diffs (3-tries rule); never hand-resolve as Sonnet.
2.4 After the rebase: `git log --oneline main..HEAD | wc -l` = 19 (no commit dropped or squashed); `git rev-parse --short HEAD` = the new tip.

## 3. Prove the whole
3.1 `uv run pytest -q` — full suite; record the pass/skip counts; expected ≥ the P2 close's 1528 passed plus whatever ops added on main (record main's own count from `git -C /Users/cobalt/cobalt log -1 --format=%B 8c3f4a3` if stated, else note "main count not stated"). Any failure = fix only if it is a rebase artifact (an assertion both sides changed); anything else = ESCALATE, not a silent fix.
3.2 `COBALT_ENV=production uv run cobalt validate` (bare, from this worktree) exit 0.
3.3 Leak scan as in the P2 close (`uv run pytest tests/cobalt -k leak -q` or the exact command the build report names) green.

## 4. Migrations unchanged + restart derivation
4.1 `git diff 1806c12 HEAD -- migrations/` → EMPTY (the desk's 09-16 08:53 dev round-trip proof — rollback to 0005, reapply — stands only if 0006/0007 are byte-identical). Not empty = ESCALATE, the proof must be rerun by the desk (the classifier denied this hub `db migrate --rollback` on cobalt_dev on 09-16; do not retry it here).
4.2 `COBALT_ENV=production uv run cobalt jobs restarts main..HEAD` (bare) — paste the table verbatim; expected `RESTARTS: com.cobalt.aset com.cobalt.radar`; any UNCLASSIFIED path listed in ESCALATE, never dropped (L42).

## 5. Close
5.1 `git status --porcelain | grep -v '^?? scratch/'` empty (report committed on this branch as its own docs commit; scratch/ stays untracked, it holds no user data beyond the briefs — delete `scratch/rebase-*.log` if any contains a vault path).
5.2 Last line of the report: `READY FOR MERGE <sha> | rebased onto <main sha> | RESTARTS: com.cobalt.aset com.cobalt.radar | ESCALATE: <n>` — the desk's evening prompt for P2 D1 reads this line; deploy never from here.
5.3 Reply in this session ≤6 lines: new tip, counts, ESCALATE items. Then stop; the desk exits this pane after reading the report.
