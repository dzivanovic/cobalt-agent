MODEL: Sonnet 5 (`claude-sonnet-5`) · SEAT: review hub — in the SHELL tab, FIRST THING on 2026-09-16 (this is the morning's ONE Codex unit; the P2 audit waits ≥5 h after this launch): `cd /Users/cobalt/cobalt-wt/s2-p4 && claude "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-16/02-p4-review.md' and follow it exactly." --model claude-sonnet-5 --remote-control p4-review-0916 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt` · SESSION: fresh · auto mode on · METER: Anthropic small (hub); Codex probed in step 1 — if the window is spent the hub waits by itself (self-scheduled wake-up), no human needed; nobody watching — never ask a question

# S2-P4 plan — Astra review (≤3 rounds), amendments in place, REVIEWED — ready for the builder. No build.

INDEX CARD: you are the HUB (L36 only spawner, L35 trust the artifact; you are the clerk, not the architect). Read only: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full → the plan `docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md` (committed `5312d3d` on `sprint-2/p4`; if it is not there, stop and report) → `docs/40 - DevDocs/reports/s2-p4-plan-2026-09-15.md` → `docs/40 - DevDocs/prompts/2026-09-15/04-p2-review.md` and `docs/40 - DevDocs/reports/s2-p2-review-2026-09-15.md` (yesterday's identical job on P2: the disposition rules and report shape you copy). Rulings R1–R7 in the plan are Dejan's and are not reopened. Report from the first step (L48): `docs/40 - DevDocs/reports/s2-p4-review-2026-09-16.md` — §0 Headline ≤5 lines → probe → per-round disposition tables → ESCALATE.

## 0. Preconditions and the one clerk's update before Astra reads
1. `git -C /Users/cobalt/cobalt-wt/s2-p4 status --porcelain` empty; `git log --oneline -1` = `5312d3d`; `git rebase main` (docs-only branch: clean or stop). `codex exec --help` confirms the flags below.
2. Record Dejan's later ruling in the plan so Astra reviews the current truth (a dated ruling, not your judgment): in §R, under R7, append `**[amended 2026-09-16 per Dejan's 2026-09-15 Codex meter ruling — `areas/cobalt-houses.md`]** Builder = Opus 5 headless (`claude -p --model claude-opus-5`, L50), launched by the Sonnet hub; Sol = none this week (weekly allowance at 26% until 09-19; Codex serves reviews, tribunals and the L52-d read-only recompute only). Sol high returns as the L47 alternative only after the weekly reset, by ruling.` Mirror the same sentence in the plan header's `Builder:` line, §6 Build and Cost (`Build: Opus 5 headless, one ~3 h session, ~$25–35 API-equivalent`). Tag each `[amended 2026-09-16 …]`. Nothing else changes before round 1.

## 1. Codex meter probe (L47)
```
cd /Users/cobalt/cobalt-wt/s2-p4 && wc -l "docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md"
cd /Users/cobalt/cobalt-wt/s2-p4 && codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Run: wc -l 'docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md' -- reply with only the number." < /dev/null
```
Match + exit 0 + no usage-limit text = UP → continue. Otherwise (window spent — the probe proves reachability, not budget: if round 1 itself dies on the limit, treat it the same): write `WAITING FOR CODEX RESET — at <time>, reset cited <time>` in the report, schedule your own wake-up (CronCreate) for the cited reset + 5 min (or 5 h 10 min if none is cited) with the prompt `Re-run 02-p4-review.md from step 1`, reply in one line, and wait. At most three probes; after the third failure stop with `ESCALATE: Codex unavailable`. Never substitute another house for the reviewer (L29: plan review = architect + Astra); never ask a question.

## 2. Astra rounds (L29 ≤3, L39 no fourth)
Round 1:
```
cd /Users/cobalt/cobalt-wt/s2-p4 && codex exec --skip-git-repo-check -m gpt-6-astra -s workspace-write "You are GPT-6 Astra, the cross-house reviewer (LAWS L29). Working directory = git worktree of branch sprint-2/p4. Read in order: /Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md in full; 'docs/40 - DevDocs/plans/plan-s2-p4-2026-09-15.md'; 'docs/10 - Decisions/' ADR-0008 (tenancy); 'docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md' STEP-1 and §7 (the seam P4 must not duplicate; migration numbers 0006/0007 are P2's, 0008/0009 are P4's); every file:line the plan's §1 Facts cite (radar/pool.py, radar/store.py, radar/models.py, radar/collector.py, aset/store.py, aset/models.py, cards/store.py, cards/models.py, cards/expire.py, archiver/collector.py, archiver/config.py, archiver/models.py, archiver/runner.py, session/guard.py, vaultwrite/writer.py, prefill/drc.py, db_migrations/__init__.py, db_migrations/cli.py, db_migrations/placement.py, db_migrations/0004_radar_pool.sql, settings/models.py, heartbeat/probes.py, configs/cobalt/jobs.yaml, configs/cobalt/radar.yaml, configs/cobalt/taxonomy/tunables.yaml); 'docs/00 - Project/MVP-CHARTER-v0_2.md' F3, F12, F13; 'docs/40 - DevDocs/reports/day-open-2026-09-15.md' (the day-open check pattern R6 copies). Rulings R1-R7 (R7 as amended 2026-09-16) are Dejan's and are not under review. Review for: (1) factual errors against the code (file:line); (2) a step that would break a law (cite the L-number) — in particular L28 (the 4th DRC unit: create-if-absent never, marker-bounded, versioned, human wins), L42 (RESTARTS derivation for a new plist + two residents), L43 (21:05 sits outside the pause: any radar restart implied?), L53 (the movers i1 archive and the archiver inside ONE total-demand computation with a refusal test), L57 (every counterfactual-R input stored); (3) the DDL of migrations 0008/0009 against ADR-0008 tenancy (side per table for picks, movers_daily, missed columns; user_id NOT NULL + FK + GUC default; grants; registry; rollback order) and against P2's 0006/0007 (no duplicate table, no ordering assumption that breaks if P4 merges first); (4) R2's SAVEPOINT inside CardStore.fill — first-run failure risk, the banner path, 'cobalt cards picks' MISSING semantics, and the two-side-role connection constraint; (5) R4's one formula — every input fetchable and stored, the gap-through fill rule, the stop-wins tie, horizon end definition, mfe_r as observation; (6) R3's precondition on the archiver job row and the 21:40 backup window; (7) a missing test for a stated behaviour or a first-run failure; (8) the four open ESCALATE items (2-5) — say for each whether the plan's proposed handling is sound. Write ONE file 'docs/40 - DevDocs/reports/p4-review-astra-r1-2026-09-16.md': 3-line summary, then a numbered table id | severity BLOCKER/AMEND/NOTE | plan section | finding | evidence file:line | proposed wording. No rewrite of the plan, no alternatives that contradict R1-R7. Reply in chat with only the path." < /dev/null
```
Disposition (the clerk's three rules, as in `04-p2-review.md`): (a) factual correction of a name, path, column, line or DDL detail → amend the plan in place, tag `[amended 2026-09-16 per Astra R1-<id>]`; (b) a change contradicting R1–R7 → not applied, recorded verbatim under `## Dissent (Astra)` in the plan, the plan stands; (c) a missing test / first-run failure / tenancy or law defect → amend §2/§4/§6 accordingly. Write the disposition table into your report. Round 2 = the same launch, `r2` file name, prepend: "Round 2. Read your R1 file and the hub's dispositions in 'docs/40 - DevDocs/reports/s2-p4-review-2026-09-16.md'; only findings you still hold, or new ones caused by the amendments." Round 3 only if R2 still holds a BLOCKER; after it the plan stands and every remaining dissent is recorded verbatim.

## 3. Close
1. `cd /Users/cobalt/cobalt-wt/s2-p4 && git add -A && git commit -m "docs(plan): S2-P4 plan Astra-reviewed (<n> rounds, <b> blockers/amends folded, <d> dissents recorded; R7 amended per the 09-15 Codex ruling)"` — one commit (L46). No source files touched; if any were, revert them and say so.
2. Report's last line: `REVIEWED <hash> | rounds <n> · dissent <d> · ESCALATE <k>` — then tell Dejan in ≤10 lines. The desk writes the P4 build prompt from this report (Opus 5 headless, after P2's build day or alongside it on a separate worktree — the desk sequences); you do not build.
