MODEL: Sonnet 5 (`claude-sonnet-5`) hub → Opus 5 headless builder (`claude -p --model claude-opus-5`, L50) · SEAT: build hub — in the SHELL tab, morning of 2026-09-16 AFTER `docs/40 - DevDocs/reports/close-2026-09-15.md` shows `LIVE: yes` for `sprint-2/radar-panel` (P2 builds on P3's `/radar`), one line: `cd /Users/cobalt/cobalt-wt/s2-p2-cards && claude "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-16/01-p2-build.md' and follow it exactly." --model claude-sonnet-5 --remote-control p2-build-0916 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh — EVERY launch of this file resumes from the report's last `CONTINUE:` line (day 2 uses the same launch line) · auto mode on (hub); the builder runs headless with `--permission-mode acceptEdits` + a scoped Bash allowlist, never `bypassPermissions`, no credentials (L29, L41) · METER: Anthropic medium–large (Opus headless ≈ 2 sessions, plan Cost ≈ $30–45 API-equivalent; hub small) + Codex ONE read-only unit for the L52-d recompute, launched only ≥5 h after the morning's P4 Astra review started (one Codex unit per 5-hour window; houses rule 09-15) · nobody watching — never ask a question

# S2-P2 BUILD — F8 precondition evaluator + F10 dots / ladder / accept-up-down. Opus 5 headless in three chunks, the hub verifies and commits each, agy second review, OpenAI-house audit, READY FOR MERGE (ships dark). No deploy from here.

INDEX CARD (read in this order, nothing more until a step needs it): `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (you are the hub on a build that ends on a write path) → the plan `docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md` in this worktree (REVIEWED `b32dae5`/`3142a1b` on `sprint-2/cards`; R1–R11 are Dejan's; every `[amended 2026-09-15 per Astra …]` block is part of its step; 0 dissent) → `docs/40 - DevDocs/reports/s2-p2-review-2026-09-15.md` §0 → `docs/40 - DevDocs/prompts/2026-09-15/01-p3-review-build.md` §3–§6 (the proven hub pattern: fixtures from prod reads, headless launch, verification, one commit) → your report `docs/40 - DevDocs/reports/s2-p2-build-2026-09-16.md` (create it at step 0; if it exists, obey its `CONTINUE:` line). Laws that bind, one line each: L35 trust the artifact (tests, diff, log), never the builder's claim · L36 you are the only spawner · L41 interim: no credential ever enters this worktree; DB proofs copy `~/cobalt/.env` by name and delete it · L45 fixtures are cut from production reads, real-shape, personal attribution stripped; the Rubberband note is NEVER copied into the repo · L46 the branch is committed clean after every chunk; main is untouched · L47 the meter is a precondition; Codex builds are ruled OUT this week (Dejan 09-15) — the builder is Opus 5 headless, the alternative to an Opus outage is a self-scheduled wait, never Sol · L29 Opus floor on the write path; never bypassPermissions · L42 RESTARTS derived, UNCLASSIFIED → ESCALATE · L52-d another house recomputes before any card can land · L57 every number replays from stored inputs · L48 evidence in the report before chat.

## 0. Preconditions (read-only) and report
1. `git -C ~/cobalt log --oneline -6` shows `deploy-2026-09-15` (`git -C ~/cobalt tag --list 'deploy-2026-09-15'`) and `close-2026-09-15.md`'s `EVENING DONE` line says `LIVE: yes` for `sprint-2/radar-panel`. If P3 is not on main: write `ESCALATE: P3 not merged — P2 builds on /radar` and stop.
2. `git status --porcelain` empty here; `git log --oneline -1` = `3142a1b`; `git rebase main` (docs-only branch: must apply clean; a conflict → stop and report).
3. Anthropic meter probe: `claude -p --model claude-opus-5 "Reply with exactly the word OK"` → `OK`. A usage-limit message → record its reset time, schedule your own wake-up (CronCreate) for that time + 5 min with the prompt `Re-run 01-p2-build.md from step 0`, reply in one line, wait (L47 wait-exception, justified by the 09-15 ruling: no Sol builds this week).
4. `scratch/` is your staging area, never committed: `mkdir -p scratch && echo 'scratch/' >> "$(git rev-parse --git-path info/exclude)"`; `cp "/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md" scratch/LAWS.md` (the builder reads law from here and never needs the vault).
5. Create the report with §0 Headline (≤5 lines, updated every step), a `## Launches` table (chunk · start · end · exit · report path · suite result · commit), `## Verification`, `## Second review (agy)`, `## Audit (L52-d)`, `## ESCALATE`, and the last line `CONTINUE: step 1`.

## 1. STEP-0 of the plan — the hub cuts the L45 fixtures from PRODUCTION READS and records the STEP-0 facts
Run from `~/cobalt` (credentials live there; this worktree never has them). Reads only; `--prod`; no `%` in SQL (use `~`). Raw output goes under `scratch/` only.
1. Find the fixture day and names: `grep -rl "trade_def: rubberband" "/Users/cobalt/Vault/Think/1 - Trading/"` (read-only; take the most recent 1–2 trade notes → their tickers and dates). If none exists: use the two most-traded pool names of 2026-09-14 (`radar_membership` by `last_rank`) and say so in the report — the Rubberband replay proof then comes from the dev evaluator's own formation on those bars, never from an invented row.
2. Bars: `cd ~/cobalt && COBALT_ENV=production uv run cobalt db query --side system --prod --format json --limit 20000 "SELECT * FROM bars WHERE interval = 'i1' AND ticker = ANY(ARRAY['<T1>','<T2>']) AND ts::date = DATE '<day>' ORDER BY ticker, ts"` → `tests/fixtures/radar/bars-rubberband.real-shape.json` (dates shifted to one fixed synthetic day, intraday offsets kept).
3. Daily bars: one hub-side Finviz daily export per fixture name via the existing collector path (`uv run cobalt --help` → the archiver/radar fetch command that hits `/export/stock?t=<T>&p=d`; if no CLI exposes a daily export, save the export the collector interface can produce and note the gap for STEP-3's builder) → `tests/fixtures/radar/daily-bars.real-shape.csv`, dates shifted.
4. Settings: `cd ~/cobalt && COBALT_ENV=production uv run cobalt db query --side user --prod --format json "SELECT key, value FROM trader_settings WHERE key ~ '^(aset\.sheet_modes|aset\.enabled_grades|daymode\.)'"` → `tests/fixtures/radar/card-settings.real-shape.json` (hashes zeroed, values kept).
5. STEP-0 facts, recorded verbatim in the report: `system.bars` i1 coverage for `SPY, QQQ, IWM, XLK, XLF, XLE, XLV, XLY, XLP, XLI, XLB, XLU, XLRE, XLC` (`SELECT ticker, count(*), min(ts), max(ts) FROM bars WHERE interval='i1' AND ticker = ANY(ARRAY[…]) GROUP BY ticker`); whether the pool screener CSVs carry a Sector column (`head -1 "$(ls -t ~/cobalt/data/radar-cache/*/*.csv | head -1)"`); the live `trader_settings` keys above. These decide plan ESCALATE 4: no sector map and no ETF bars → STEP-5 ships `sector_alignment`/`market_alignment` defaults as explicit N/A through S2 (already the plan's text; do not invent a map).
6. Transform script `tests/fixtures/radar/_cut_p2_fixtures.py` (deterministic, re-runnable, reads its input from `scratch/`), same rules as P3's `_cut_panel_fixtures.py`: one synthetic anchor day, `@<12-hex>` source suffixes zeroed, sha fields zeroed, `user_id` → 1, no personal attribution. Proofs before moving on: `grep -rE '@[0-9a-f]{12}' tests/fixtures/radar/ | grep -v '@000000000000' | wc -l` = 0; `grep -rl '2026-09-' tests/fixtures/radar/ | wc -l` = 0; `grep -rlE 'expr:|trade_def:' tests/fixtures/ | wc -l` = 0 (no note content); `git check-ignore` reports none of the new fixture files ignored.
7. The Rubberband definition unit and its tunables unit are NOT copied anywhere in the repo (plan §3, ruling B + Astra R1-21): the `requires_vault` test reads the live unit path and is run by you in step 4.
Update the report; `CONTINUE: step 2 chunk A`.

## 2. Builder launches — Opus 5 headless, three chunks; the hub verifies and commits after each (step 3)
Before EVERY launch: `touch scratch/.launch-marker` (vault-untouched proof, step 3.1). Every launch is one command in this worktree. The three commands are identical except the CHUNK sentence and the report name; copy them exactly.

Chunk A:
```
cd /Users/cobalt/cobalt-wt/s2-p2-cards && claude -p --model claude-opus-5 --permission-mode acceptEdits --allowedTools "Bash(uv run pytest *)" "Bash(uv run python *)" "Bash(uv run cobalt validate*)" "Bash(uv run cobalt radar evaluate *)" "Bash(uv run cobalt --help*)" "Bash(git status*)" "Bash(git diff*)" "Bash(git log*)" "Bash(ls *)" "Bash(grep *)" "Bash(rg *)" "Bash(cat *)" "Bash(sed -n *)" "Bash(wc *)" "Bash(mkdir -p *)" "You are Opus 5, the builder (LAWS L29 floor), launched headless by the Sonnet hub (L50). Working directory = the git worktree of branch sprint-2/cards at /Users/cobalt/cobalt-wt/s2-p2-cards. You change files ONLY inside this worktree: never ~/cobalt, never /Users/cobalt/Vault, never another worktree; you have no database and no credentials (L41 interim) and you never look for them. Read in this order: scratch/LAWS.md in full (current law); 'docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md' in full — rulings R1-R11 are Dejan's and bind, every '[amended 2026-09-15 per Astra …]' block is part of its step, §L52, §3, §4, §5 bind; 'docs/40 - DevDocs/PLAN-TEMPLATE.md'; 'docs/10 - Decisions/' ADR-0008 (tenancy); the DevDocs page under 'docs/40 - DevDocs/cobalt/' of every .py you will touch. CHUNK A = build STEP-1 (migrations 0006 system + 0007 user with FORWARD/REVERSE registry entries, placement map, tenancy tests, the receipt table and every STEP-1 amendment), STEP-2 (predicate grammar src/cobalt/taxonomy/predicate.py per R3, loud parse errors with note + line; the dry-run CLI may land in chunk B if it needs the stage) and STEP-3 (detectors src/cobalt/radar/anatomy/ per R2/R4/R5, including the daily-bars collector behind the collector interface, once-per-name-per-day cache, and the L53 total-demand test across every Finviz consumer) exactly as written, failing test first for every §4 sentence those steps own. Rules: Pydantic for every structured value; fail-loud (L1), no plausible-empty artifact, no silent fallback; no new dependency; no person or vendor name in any identifier (L31); every new table declared on exactly one side with user_id NOT NULL + FK on user tables (L32, ADR-0008); no derived number without its stored inputs (L57); fixtures under tests/fixtures/radar/ are hub-cut — never edit or invent one; the Rubberband note is never copied into the repo — the requires_vault test reads its live path and is run by the hub; requires_db tests are written by you and run by the hub; tests that need DB or vault must SKIP cleanly offline, never error. Run 'uv run pytest -q tests/cobalt tests/taxonomy' and make it green. One DevDoc page per new .py and an update of every touched page (agent-authored prose). Do not commit. When done, write 'docs/40 - DevDocs/reports/s2-p2-build-opus-A-2026-09-16.md': §0 headline, files changed, tests added (names), suite result verbatim, anything you could not do and why. Reply in chat with only that path."
```
Chunk B: the same command with the CHUNK sentence replaced by: `CHUNK B = build STEP-4 (the S5 evaluate stage in radar/runner.py + radar/evaluate.py per R1/R9 with 'cobalt radar evaluate --replay <date> [--trade-def <slug>]' writes-nothing, the dev-persistence harness the R1-11 amendment names, and every STEP-4 amendment: sized-card ARM invariant inside the locked CardStore transition, one creation path for unsized radar cards, publish ordering across the two side-role connections with DB-level open-card uniqueness, departed-member lifecycle, persisted expiry deadline read from preferred_windows_ref), STEP-5 (dots + scoring in src/cobalt/cards/scoring.py per R6 shadow / R7 proposed-key bands and curve anchors in trader_settings, card_score = round(conviction × proximity × 100)), STEP-6 (ladder sizing + tap through the one aset/engine.py path per R8, snap DOWN only) and STEP-7 (cards/health.py with the P3 R2 threshold table copied, not re-ruled)` and the report name `s2-p2-build-opus-B-2026-09-16.md`.
Chunk C: the same command with: `CHUNK C = build STEP-8 (panel wiring in aset/radar_panel.py + web.py on '"user".radar_cards_v', badges, hollow/shadow dots, the 1-10 tap strip, the explicit POST-route allowlist test, and the carried sentinel test for GET /api/radar/pool), STEP-9 (taxonomy v0.8 per R10: the one catalyst review file, batch apply by sha256, loader gate stays 0.4), STEP-10 ('cobalt cards shadow-report'), STEP-11 ('cobalt radar audit-export --run <id>|--replay <date>' writing the frozen bundle: bars window, settings snapshot, tunables rows, AST, published card rows, hashes) and STEP-12 (DevDocs for every new/changed .py, BACKLOG.md kanban row, ADR-0009 'radar cards seam + precondition AST' in docs/10 - Decisions/)` and the report name `s2-p2-build-opus-C-2026-09-16.md`.

## 3. After EVERY chunk — hub verification and commit (L35, L46)
1. Vault untouched: `find "/Users/cobalt/Vault/Think" -type f -newer scratch/.launch-marker -not -path '*/.obsidian/*' | head -20` → empty, or only files Dejan edits himself (name them). Anything under `6 - Permanent/`, `1 - Trading/`, `0 - Projects/Cobalt/` written during the launch → ESCALATE (the builder had no business there) and report the paths.
2. `git status --porcelain | grep -v '^?? scratch/'`; `git diff --stat`; read the builder's report; read the diff of every new module yourself (no POST without the allowlist test, no new dependency in `pyproject.toml`, no `alert(`/`prompt(`/`confirm(`/`.focus(`/`autofocus`/`http-equiv` in templates, no credentials, no strategy-note text).
3. `uv run pytest -q tests/cobalt tests/taxonomy` → green (skips allowed for `requires_db`/`requires_vault`); `uv run pytest -q tests/cobalt/test_radar_notes.py -k leak` → green, unmodified.
4. Red → ONE relaunch of the same chunk command with this prepended to the prompt: `CONTINUE: read your report 's2-p2-build-opus-<X>-2026-09-16.md' and fix only what is red below, nothing else: <the last 40 lines of the pytest output>` (L47's bounded form). Still red → commit the state as `wip(s2-p2): chunk <X> RED — see report` so nothing is lost, write `ESCALATE: chunk <X> red after one relaunch` + `CONTINUE: step 2 chunk <X> (red)` and stop (3-tries rule: the desk decides the next step, not you).
5. Green → commit: `git add -A && git commit -m "feat(s2-p2): chunk <X> — <STEP list> (plan b32dae5, Opus 5 headless)"` with the builder's report path and suite line in the body. Update the `## Launches` row and the headline; `CONTINUE: step 2 chunk <next>` (or `step 4` after C).
6. Day boundary: if it is past 19:30 ET, or the Anthropic meter binds mid-chunk and the relaunch is refused, finish this chunk's verification only if it is already green (then commit), write the `CONTINUE:` line, reply in ≤6 lines and stop. Tomorrow Dejan runs the same launch line in the shell tab (after `/exit` of this pane); that session reads the report and resumes exactly at `CONTINUE:`.

## 4. Full verification after chunk C
1. Offline suite + leak scan as in 3.3; `uv run cobalt validate` reads its config from this worktree: `COBALT_ENV=dev uv run cobalt validate` → exit 0.
2. DB proofs on `cobalt_dev` (L41 interim; `.env` copied by name, never printed, deleted after): `cp ~/cobalt/.env .env` → `uv run cobalt db migrate --help` (read the flags first) → `COBALT_ENV=dev uv run cobalt db migrate` (0006, 0007 up on cobalt_dev) → `COBALT_ENV=dev uv run cobalt db migrate --rollback --down-to 0005` → `COBALT_ENV=dev uv run cobalt db migrate` again (idempotent up) → `COBALT_ENV=dev uv run pytest -q tests/cobalt tests/taxonomy` (with `.env` present the `requires_db` tests run: S5 end-to-end, views, publish-ordering crash cases, tenancy assertions on the new tables) → the dev replay: load the §3 bars fixture into `cobalt_dev` with the dev-persistence harness STEP-4 shipped (its command is named in the chunk B report), then `COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar evaluate --replay <synthetic day> --trade-def rubberband` (writes nothing; paste the formations list and the "not evaluable: missing atoms […]" lines for the other five into the report) → `rm .env && ls -la .env 2>&1 | tail -1` shows it is gone. Never `--allow-prod`; never a prod DSN.
3. `requires_vault` tests (grammar: all 13 live defined notes parse; the five report their exact missing atoms; the trail_fit draft is produced and never applied): run them with `COBALT_VAULT_PATH=/Users/cobalt/Vault/Think` set; they only read. Before/after: `find "/Users/cobalt/Vault/Think/1 - Trading" -type f -newer scratch/.launch-marker | head` unchanged.
4. `uv run cobalt jobs restarts main..HEAD` → expected `com.cobalt.aset com.cobalt.radar`; any other label is restarted in the same action later and named now; UNCLASSIFIED → ESCALATE, never dropped (L42).
5. Tests-first evidence: for each §4 area, the failing-then-passing test names from the builders' reports, spot-checked by you (`git log -p` is not available per chunk — read the test files and confirm each acceptance sentence has a test).
`CONTINUE: step 5`.

## 5. Second reviewer — Gemini 3.1 Pro under `agy` (ruled 2026-09-15 A: bounded second reviewer on every build from S2-P2 on)
Stage everything INSIDE the allow-listed worktree (`read_file(/Users/cobalt/cobalt-wt/agy-trial/)` is in the 17-rule allowlist; this worktree is not): `mkdir -p /Users/cobalt/cobalt-wt/agy-trial/scratch && git diff main..HEAD -- src tests > /Users/cobalt/cobalt-wt/agy-trial/scratch/s2-p2-diff.txt && cp "docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md" /Users/cobalt/cobalt-wt/agy-trial/scratch/plan-s2-p2.md && for L in L1 L10 L32 L45 L52 L57; do n=$((${L#L}+1)); awk "/^### $L /,/^### L$n /" scratch/LAWS.md; done > /Users/cobalt/cobalt-wt/agy-trial/scratch/laws-excerpt.md && wc -l /Users/cobalt/cobalt-wt/agy-trial/scratch/*`. Launch (read-only, one shot, 15 min):
```
agy --model gemini-3.1-pro-high --mode plan --sandbox --print-timeout 15m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You are a cross-house code reviewer. Everything you need is under /Users/cobalt/cobalt-wt/agy-trial/scratch/: read plan-s2-p2.md (the plan, as amended and reviewed), then s2-p2-diff.txt (git diff main..HEAD of src/ and tests/ on branch sprint-2/cards), then laws-excerpt.md (laws L1, L10, L32, L45, L52, L57). Task: does the build implement the plan, step by step, and does any change breach a quoted law? Produce ONLY a markdown table: id | severity BLOCKER/AMEND/NOTE | plan step | finding | evidence (file path and line as they appear in the diff). Rules: every finding cites a line you actually read in the diff; no finding without evidence; no rewrite suggestions longer than one sentence; if the build matches the plan, say so in one line above an empty table. Do not modify anything." > scratch/agy-out.md 2> scratch/agy-err.log; echo "exit=$?"
```
Judge every finding yourself (VERIFIED / WRONG / INVENTED) against the worktree; a denial in `agy-err.log` = `HARNESS — not measured` (quote the denied tool and the `permissions.allow` rule that would cover it; no rerun loop — the three tries were spent 09-15). VERIFIED BLOCKER/AMEND → one bounded builder fix launch (chunk C command with `CONTINUE: fix exactly these verified findings: …` prepended) → re-verify per step 3 → commit `fix(s2-p2): agy-verified findings <ids>`. Table in the report: findings, verdicts, overlap with the plan's own tests. `CONTINUE: step 6`.

## 6. L52-d audit — the OpenAI house recomputes from the frozen bundle (plan STEP-11, R11; Codex read-only recompute is inside the 09-15 meter ruling)
Precondition: ≥5 h since the morning's P4 Astra review launched (`ls -la --time-style=full-iso /Users/cobalt/cobalt-wt/s2-p4/docs/40 - DevDocs/reports/p4-review-astra-r1-2026-09-16.md` gives its time; if the review has not started today, go ahead). Probe first: `codex exec --skip-git-repo-check -m gpt-6-astra -s read-only "Run: wc -l 'docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md' -- reply with only the number." < /dev/null` (match + exit 0 + no usage-limit text). Window spent → `AUDIT: deferred — Codex window spent at <time>`; the desk schedules it; do not wait.
Bundle: `cp ~/cobalt/.env .env && COBALT_ENV=dev COBALT_VAULT_PATH=/Users/cobalt/Vault/Think uv run cobalt radar audit-export --replay <synthetic day> --out scratch/audit-bundle/ ; rm .env` (exact flags as chunk C built them; the bundle holds bars window, settings snapshot, tunables rows, AST, candidate card rows, hashes — no note content). Then:
```
codex exec --skip-git-repo-check -m gpt-6-astra -s workspace-write "You are the OpenAI-house auditor (LAWS L52-d). Network is off. Read ONLY scratch/audit-bundle/ (the frozen bundle) and 'docs/40 - DevDocs/plans/plan-s2-p2-2026-09-15.md' STEP-5 (the formulas: dot curves from the settings snapshot, conviction = taps only, proximity, card_score = round(conviction × proximity × 100)). Do NOT read src/cobalt/cards/scoring.py or any Cobalt source. Write your own independent checker under scratch/audit-check/ (Python, standard library only), recompute every dot value, conviction, proximity and card_score for every card in the bundle from the bundle's inputs alone, and write scratch/audit-verdict.md: a table card_id | field | bundle value | recomputed | |Δ| | PASS/FAIL (exact match for integers and labels, |Δ| ≤ 1e-6 for intermediates), then one line PASS or FAIL with the count. Reply in chat with only that path." < /dev/null
```
Copy `scratch/audit-verdict.md`'s table into the report (the file itself stays in scratch). FAIL → ESCALATE (a mismatch blocks enable; no fix launch tonight — the desk rules on it). Note in the report that this is the dark-bundle audit (plumbing + dot arithmetic); the D2-gating audit against the frozen D2 settings (Astra R1-11) is a later prompt.

## 7. Close
1. `git status --porcelain | grep -v '^?? scratch/'` empty; `git log --oneline main..HEAD` lists your chunk commits (and fixes); no squash.
2. Report's last line: `READY FOR MERGE <hash> | RESTARTS: <line> · audit: <PASS|FAIL|deferred> · agy: <verified n / total m | HARNESS> · ESCALATE: <n>`; ≤10 lines to Dejan: hash, RESTARTS, audit, ESCALATE count, report path. Deploy = plan STEP-D1 (≥16:05 ET, dark: `radar.cards_enabled=false`, migrations 0006/0007 with `--allow-prod`, radar restart only inside 20:00–21:00) on an evening the desk names in its own prompt — never from this session.
