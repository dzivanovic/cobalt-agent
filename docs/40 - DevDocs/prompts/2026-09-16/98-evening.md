MODEL: Opus 5 (`claude-opus-5`) — the Opus floor because P2's prod migrations 0006/0007 MAY ship tonight (L29 write path) · SEAT: evening hub — deploy `deploy-2026-09-16` at 20:05 ET inside the pause, then the close — in the SHELL tab, any time after the ops hub (`ops-0916`) reports READY and before 19:30 ET, one line (RULED B, Dejan 2026-09-16: the per-session allowlist below IS the permission grant for exactly these deploy actions, explicit rules, not auto-mode judgment; push is not in it, L55): `cd ~/cobalt && claude "Read 'docs/40 - DevDocs/prompts/2026-09-16/98-evening.md' and follow it exactly." --model claude-opus-5 --remote-control evening-0916 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt --allowedTools "Bash(git -C /Users/cobalt/cobalt commit *)" "Bash(git commit *)" "Bash(git merge --ff-only *)" "Bash(git -C /Users/cobalt/cobalt-wt/* rebase main)" "Bash(git rebase main)" "Bash(git rebase --abort)" "Bash(git tag *)" "Bash(git reset --soft HEAD~1)" "Bash(launchctl kickstart *)" "Bash(launchctl print *)" "Bash(uv run cobalt *)" "Bash(COBALT_ENV=production uv run cobalt *)" "Bash(pg_dump *)" "Bash(mkdir -p *)" "Bash(curl *)" "Bash(git status*)" "Bash(git log*)" "Bash(git diff*)" "Bash(git rev-parse*)" "Bash(grep *)" "Bash(tail *)" "Bash(cat *)" "Bash(ls *)" "Bash(wc *)" "Bash(date*)"` — then Dejan runs `/login` in that pane once, so the scheduled wake-up survives · SESSION: fresh · auto mode on for everything outside the allowlist (never bypassPermissions) · METER: Anthropic small · nobody watching after launch — never ask a question; a step that cannot proceed is an ESCALATE line, and the deploy stops at that branch with main as it was before it

# Evening 2026-09-16 — prove the permission gate, wait for 20:05 ET, deploy four ops branches (+ P2 dark if READY), close. DO NOT EXIT this pane until its report ends `EVENING DONE`.

INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (you end on a production deploy) → `docs/40 - DevDocs/reports/ops-2026-09-16.md` (today's ops hub: the READY lines, the rebased `ops/2026-09-15` hash, RESTARTS) → `docs/40 - DevDocs/reports/close-2026-09-15.md` §Deploy table (last night's shape and what is already LIVE) → `docs/40 - DevDocs/prompts/2026-09-15/01-p3-review-build.md` §7 (the proven deploy sequence you copy). Laws, one line each: L43 one deploy per evening, radar restart only inside 20:00–21:00 · L54 rebase-then-ff from `~/cobalt`, merge = deploy · L42 RESTARTS derived, UNCLASSIFIED → ESCALATE · L51 pre-approvals: `COBALT_ENV=production`, committing machine-written files, blob extraction · L46 one run one commit, main clean after · L55 push is Dejan's · L48 evidence in the report before chat. Report from the first step: `docs/40 - DevDocs/reports/deploy-2026-09-16.md` — §0 Headline ≤5 lines → gate proof → deploy table → smoke → ESCALATE.

## 1. Permission-gate proof (L29: a gate is proven in a scratch test, never assumed) — at launch, before anything else
`git -C ~/cobalt tag scratch-allow-probe-0916 && git -C ~/cobalt tag -d scratch-allow-probe-0916` and `git -C ~/cobalt commit --allow-empty -m "scratch: allowlist probe (reverted next line)" && git -C ~/cobalt reset --soft HEAD~1` — both must run WITHOUT a permission denial (in auto mode a denial arrives as a denied tool result naming a classifier category; quote it if it appears). Both pass → write `GATE: proven — commit + tag allowed by the session allowlist` in the report and continue. Either denied → write `GATE: NOT proven — <category quoted>` + `ESCALATE: ruling B gate failed; Dejan types the deploy lines at 20:05 (ruling A fallback)`, reply in one line, and STOP — do not schedule, do not deploy.

## 2. Schedule, then (at 20:05) preconditions — this session is launched by the desk in the MORNING as a background session (`claude --bg`, ruling 2B 2026-09-16), so steps 2.1–2.3 are read at 20:05, not at launch
0. **Schedule yourself now** for 20:05 ET today (ScheduleWakeup/CronCreate) with the prompt `Run 98-evening.md step 2.1 onward`. Confirm in one line what you scheduled and the cron id; write `DO NOT EXIT until EVENING DONE` as the report's headline line 1. Then stop and wait; do not poll. (If the wake-up never fires, the desk relaunches this file at 20:10 with `Run 98-evening.md step 2.1 onward` as the instruction.)
1. `docs/40 - DevDocs/reports/ops-2026-09-16.md` last line (written 07:05 ET) names READY `ops-0915 a502c23` (RESTARTS aset radar) and `ops-0916` (tip `ee1780e`, RESTARTS radar). Desk ruling 07:10 ET on that report's ESCALATE 1–3: `ops/grok-deny-0915` is NOT deployed tonight (its `ops/grok-sandbox.toml` derives UNCLASSIFIED and the probes are unrun — a HARNESS classifier rule + probes land on tomorrow's ops branch), and `ops/agy-trial-0915` is NOT deployed tonight (rebase not clean; its worktree's `scratch/` is in use by the audit hub — rebuilt as one commit tomorrow). Tonight's branches: `ops/2026-09-15` then `ops/2026-09-16`, then P2 if 2.3 says yes. If `ops/2026-09-15` is not at `a502c23` or its worktree is dirty, write `ESCALATE: ops-0915 changed since READY — skipped` and deploy the rest.
2. Every worktree you will rebase is clean: `for w in ops-2026-09-15 grok-deny agy-trial ops-2026-09-16; do echo "$w: $(git -C /Users/cobalt/cobalt-wt/$w status --porcelain | wc -l)"; done` → all 0. A dirty one: skip that branch, ESCALATE, never `stash`.
3. P2 rides tonight ONLY if ALL of: `docs/40 - DevDocs/reports/s2-p2-build-2026-09-16.md` (worktree `s2-p2-cards`) last line starts `READY FOR MERGE` and carries `RESTARTS: com.cobalt.aset com.cobalt.radar` (nothing else) · its agy line is not `HARNESS` with unverified BLOCKERs · the absence semantics of `radar.cards_enabled` are as chunk B built them (`test_missing_cards_enabled_setting_fails_loud_before_any_write`, `test_the_runtime_reader_refuses_missing_cards_enabled_rather_than_defaulting`, `test_evaluate_failure_stamps_failed_stage_and_s1_to_s4_stand`): an ABSENT setting makes the S5 evaluate stage refuse loudly before any write while S1–S4 keep scanning — that is acceptable ONLY because Dejan approved the dark load (07:07 ET), which step 3b runs after the migration and BEFORE the radar restart, so radar never runs a cycle without the setting; if the build report says absence crashes the radar PROCESS (not just the stage) or enables cards, P2 waits for its own evening · that worktree is clean. Record the four answers in the report as `P2 tonight: yes/no — <reason>`. If yes, step 3 gains 3b below; the dark settings load stays Dejan's hand under L28 (see 3b).
4. Preconditions recorded in the report → continue straight into step 3 (same wake-up).

## 3. 20:05 ET — deploy (`~/cobalt`; L54; L51 pre-approvals)
1. `git -C ~/cobalt status --porcelain` → only machine-written files (`configs/cobalt/rules.yaml`, `docs/40 - DevDocs/reports/seat-usage.md`, `day-open-*.md`, desk reports the desk did not commit) → `git -C ~/cobalt add -A && git -C ~/cobalt commit -m "chore: machine-written + desk files, committed as-is (L51-2)"`. Record `<main-before>` = `git -C ~/cobalt rev-parse --short HEAD`.
2. In this order, each: `git -C /Users/cobalt/cobalt-wt/<wt> rebase main` (conflict → `git rebase --abort`, skip the branch, ESCALATE with the conflicting path) → `cd ~/cobalt && git merge --ff-only <branch>`: (a) `ops/2026-09-15` from `ops-2026-09-15`; (b) `ops/2026-09-16` from `ops-2026-09-16`. (grok-deny and agy-trial: not tonight — see 2.1.)
3. `cd ~/cobalt && COBALT_ENV=production uv run cobalt validate` → exit 0; `uv run cobalt jobs register` (no plist change expected; if the derivation names one, register and say so).
3b. **P2 dark ship (only if step 2.3 said yes; plan `plan-s2-p2-2026-09-15.md` §6 STEP-D1, radar part inside the pause):** snapshot `mkdir -p ~/cobalt/data/backups/pre-s2-p2-2026-09-16 && pg_dump` of `system.radar_pool`, `system.radar_membership`, `"user".aset_sizings`, `"user".card_transitions`, `"user".trader_settings` (DSN parts via `COBALT_ENV=production uv run cobalt db query --help` → use the documented dump path, never a literal password in the command; if no dump command exists without a literal DSN, write `ESCALATE: snapshot needs a DSN — P2 skipped` and skip P2) → `git -C ~/cobalt tag pre-s2-p2` → `git -C /Users/cobalt/cobalt-wt/s2-p2-cards rebase main` (conflict → abort, skip P2, ESCALATE the path) → `git merge --ff-only sprint-2/cards` → `COBALT_ENV=production uv run cobalt db migrate --allow-prod` (0006, 0007; quote the output) → validate again. Dark settings — APPROVED by Dejan 07:07 ET via the desk (chat approval = the HITL token, his ruling of 09-16): the hub writes a settings file containing ONLY `radar.cards_enabled: false` in the exact input format chunk B's loader accepts (read `tests/cobalt/test_card_settings.py` and `src/cobalt/settings/` in the merged tree for the format; the five-key schema refuses anything else) under `docs/40 - DevDocs/reports/p2-dark-settings-2026-09-16.yaml`, prints its sha256, runs `COBALT_ENV=production uv run cobalt settings load --apply --sha256 <sha> <file>` itself BEFORE any restart, quotes the output and the round-trip proof, and writes the line `approved by Dejan 07:07 ET via the desk` beside it. No other key is loaded tonight. Restarts for P2 are the same aset + radar as step 4; no extra restart.
4. Restarts, derived: `uv run cobalt jobs restarts <main-before>..HEAD` → the table goes into the report verbatim. Expected `com.cobalt.aset com.cobalt.radar` (the ops-0915 code change). UNCLASSIFIED rows: the ops branch ships the DOCS classifier rule and a `rules.yaml` rule — if anything is still UNCLASSIFIED, restart aset + radar only (Dejan's 09-15 ruling for exactly these two paths, `QWEN.md` and `configs/cobalt/rules.yaml`, both docs/generated without a runtime reader) and ESCALATE the path. `launchctl kickstart -k gui/$(id -u)/com.cobalt.aset`, then `launchctl kickstart -k gui/$(id -u)/com.cobalt.radar` — radar inside the pause only; if the clock is past 20:55, skip the radar restart, write `RESTARTS: radar DEFERRED to overnight idle` and ESCALATE.
5. Smoke: `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:5010/` = 200; same for `/radar` = 200; `COBALT_ENV=production uv run cobalt notify --help | grep -c email` = 0 (email retired); `COBALT_ENV=production uv run cobalt heartbeat show` no new RED; `launchctl print gui/$(id -u)/com.cobalt.radar | grep -E "state =|pid ="` running with a new pid; `COBALT_ENV=production uv run cobalt day-open --help | grep -ciE "force|json"` ≥ 1 (item 6b landed: `--json` writes nothing, dated report never overwritten — do NOT run `day-open` without `--json` tonight).
5b. P2 smoke (if merged): `/radar` 200 shows the pool and an EMPTY ladder; `COBALT_ENV=production uv run cobalt radar evaluate --replay 2026-09-16` (writes nothing) runs and its formation lines go into the report; `grep -ciE "cards.*(disabled|off)" logs/radar.err | tail -1` shows the loud dark line after the restart; heartbeat green at the next beat.
6. Tag `git -C ~/cobalt tag deploy-2026-09-16`. Rollback = `git revert` of the merge range + restart aset at once, radar in the next pause (L54); P2 schema rollback = `cobalt db migrate --rollback --down-to 0005 --allow-prod` after the revert, never before.
7. Deploy table in the report (branch | tip | status | notes), `LIVE <hash> · RESTARTS done: <labels> · ESCALATE: <n>`.

## 4. 21:05 ET check (schedule one more wake-up, same rules) — the flap proof
`grep -c "ENTERED RED: radar" ~/cobalt/logs/heartbeat.log` for lines after the restart = 0 and `cobalt db query --side system --prod "SELECT last_scan_at, members FROM radar_pool"` shows a post-reset scan (or a documented idle: no scan until 04:00 is fine, quote the probe line). Append to the report.

## 5. Close — run `docs/40 - DevDocs/prompts/2026-09-16/99-close.md` steps 1–4 yourself (ledger appendix, close report with the deploy table, validate, one commit). Last line of the close report: `EVENING DONE <hash> · LIVE: <yes/no per branch> · PUSH: Dejan · ESCALATE: <n>`. Reply in this pane in ≤6 lines and stop. Dejan reads it in the morning; the desk rewrites NOW and applies the LAWS L42 O9 marker itself.
