MODEL: Sonnet 5 (`claude-sonnet-5`) · SEAT: audit hub — launched by the CTO desk as a background session (ruling 2B 2026-09-16): `cd /Users/cobalt/cobalt-wt/agy-trial && claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-16/04-audit-house.md' and follow it exactly." --model claude-sonnet-5 --remote-control audit-house-0916 --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode on · METER: Anthropic small; Gemini (agy, consumer login) and Grok (SuperGrok) bounded runs; Codex = NONE (Dejan 09-16) · nobody watching — never ask a question; DO NOT EXIT until the report ends `AUDIT HOUSE DONE`

# Audit house 2026-09-16 — (1) capability trial: can Gemini under `agy` and Grok under `grok --sandbox cobalt-job` each WRITE and RUN an independent Python checker inside a scratch directory, network off? (2) whichever passes recomputes P2's frozen L52-d bundle. Ruled by Dejan 2026-09-16: "Let Gemini audit and test Grok for that capability as well."

INDEX CARD: `/Users/cobalt/Vault/Think/6 - Permanent/Memory/INDEX.md` → `## NOW` at the top of `.../areas/cobalt.md` → `.../areas/cobalt-houses.md` (every 09-15 line on agy and Grok: the 17-rule allowlist, "one denial = zero output", the `cobalt-job` sandbox profile, the seat-busy CPU guard) → `.../topics/cto-desk.md` (headless seats: the hub STAGES every input inside the allowed workspace) → LAWS.md L29, L33, L35, L44, L52, L55 only → `docs/40 - DevDocs/reports/agy-trial-2026-09-15.md` (Attempt D recipe) → `docs/40 - DevDocs/reports/grok-deny-2026-09-15.md` (the profile and its probes) → `docs/40 - DevDocs/prompts/2026-09-16/01-p2-build.md` §6 (the audit task as it was written for Codex — the recompute contract you now give to another house). Report from the first step (L48): `docs/40 - DevDocs/reports/audit-house-2026-09-16.md` — §0 Headline ≤5 lines → capability table → audit verdict table → ESCALATE. This worktree (`ops/agy-trial-0915`) is reports-only; nothing under `scratch/` is ever committed (`git check-ignore scratch/` must match; if not, add `scratch/` to `$(git rev-parse --git-path info/exclude)`).

## 1. The toy bundle (hub-made, deterministic, no Cobalt source)
`mkdir -p scratch/toy-bundle scratch/toy-check-agy scratch/toy-check-grok` and write `scratch/toy-bundle/cards.json`: 6 cards, each with `dots` (3–4 factors, `trader_grade` 1–10 or null), `last`, `trigger`, `stop`, and the bundle's own claimed `conviction`, `proximity`, `card_score` — make exactly ONE card's claimed `card_score` wrong by 1. Write `scratch/toy-bundle/FORMULA.md` with the P2 STEP-5 formulas verbatim: `conviction = mean(trader_grade of tapped dots)/10` (no taps → null), `proximity = clamp(1 − |last − trigger| ÷ (3 × |trigger − stop|), 0, 1)`, `card_score = round(conviction × proximity × 100)` (round half up). Record in the report which card is wrong and by how much (the ground truth the houses must find).

## 2. Capability trial — Gemini under `agy` (3 tries max, then stop)
Read `~/.gemini/settings.json` (or wherever the 09-15 17-rule allowlist lives — `agy-trial-2026-09-15.md` names it) and quote the rules. The task needs THREE capabilities the read-only allowlist lacks: write a file under `scratch/toy-check-agy/`, run `python3` on it, read the bundle. Add the narrowest rules that grant exactly those inside this worktree (write_file scoped to `/Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-agy/`, `run_shell_command(python3 /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-agy/*)`, read_file of `scratch/toy-bundle/`) — record the exact JSON you added; never `--dangerously-skip-permissions` (L55's bypass ban applies to every house on this host). Launch, `--mode accept-edits --sandbox`, 10 min:
```
agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 10m --add-dir /Users/cobalt/cobalt-wt/agy-trial --print="You are an independent auditor. Network is off. Read ONLY /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-bundle/cards.json and FORMULA.md. Write your own checker as /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-agy/check.py (Python 3, standard library only), run it with python3, and write /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-agy/verdict.md: a table card_id | field | bundle value | recomputed | |Δ| | PASS/FAIL (exact match for integers, |Δ| ≤ 1e-6 for intermediates), then one line PASS or FAIL with the count. Reply in chat with only the verdict file path." > scratch/agy-toy-out.md 2> scratch/agy-toy-err.log; echo "exit=$?"
```
PASS = `verdict.md` exists, was produced by running `check.py` (the file exists and `python3 scratch/toy-check-agy/check.py` run by YOU reproduces the table), and it flags exactly the wrong card. A denial in the err log → add the one rule the denial names, retry (≤3 total), record each. Verdict row: `agy: CAPABLE / INCAPABLE (<reason>) / HARNESS (<denied tool>)`.

## 3. Capability trial — Grok under `grok --sandbox cobalt-job` (3 tries max)
Busy guard by CPU, never `pgrep` alone: `pid=$(pgrep -x grok | head -1); [ -z "$pid" ] || { a=$(ps -o %cpu= -p $pid); sleep 10; b=$(ps -o %cpu= -p $pid); echo "cpu $a → $b"; }` → below 1.0 both = idle. Read `~/.grok/sandbox.toml` `[profiles.cobalt-job]` (quote it; do not edit the live file — if the profile denies writes under this worktree's `scratch/`, say so and use `--allow` rules instead of touching the profile). Launch from this worktree:
```
grok --sandbox cobalt-job --allow "write_file(/Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-grok/*)" --allow "bash(python3 /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-grok/*)" -p "You are an independent auditor. Network is off. Read ONLY /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-bundle/cards.json and FORMULA.md. Write your own checker as /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-grok/check.py (Python 3, standard library only), run it with python3, and write /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-grok/verdict.md: a table card_id | field | bundle value | recomputed | |Δ| | PASS/FAIL, then one line PASS or FAIL with the count. Reply with only the verdict file path." 2>&1 | tail -20
```
(`grok --help` names the exact tool-rule syntax for `--allow`; read it first and use its spelling — the two rules above are the intent, not the proven spelling.) Same PASS test as step 2, run by you. Verdict row: `grok: CAPABLE / INCAPABLE / HARNESS`. Never `--always-approve`.

## 4. Wait for P2's bundle, then the real L52-d recompute
The P2 hub stages the frozen bundle at `/Users/cobalt/cobalt-wt/agy-trial/scratch/p2-audit-bundle/` with `scratch/p2-step5.md` beside it (its report `~/cobalt-wt/s2-p2-cards/docs/40 - DevDocs/reports/s2-p2-build-2026-09-16.md` says `AUDIT: bundle staged` when done). If it is not there: schedule your own wake-up every 60 min (CronCreate) with the prompt `Re-run 04-audit-house.md from step 4`, reply in one line, wait; give up at 22:00 ET with `AUDIT: bundle never arrived` and finish the report. When it is there: run the CAPABLE house(s) — Gemini first (Dejan's ruling), Grok too if CAPABLE (two houses agreeing beats one) — with the step-2/3 command reshaped: read ONLY `scratch/p2-audit-bundle/` and `scratch/p2-step5.md`, never any Cobalt source; checker under `scratch/p2-check-<house>/`; verdict table for every card in the bundle: dot values, conviction, proximity, card_score; then PASS/FAIL with the count. Reproduce each verdict yourself by running the house's `check.py`; compare the two houses' tables if both ran. Copy the verdict table(s) into the report. FAIL → ESCALATE (a mismatch blocks P2's ENABLE, not tonight's dark merge). Note in the report: this is the dark-bundle audit (plumbing + dot arithmetic); the D2-gating audit against the frozen D2 settings (P2 plan Astra R1-11) is a later prompt, run by the same capable house.

## 5. Close
No commits (reports-only worktree; the audit report is a docs file the desk commits on main — copy it to `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/audit-house-2026-09-16.md` at the end and say so). Report's last line: `AUDIT HOUSE DONE · agy: <CAPABLE|INCAPABLE|HARNESS> · grok: <…> · P2 audit: <PASS n/n | FAIL k | not run> · ESCALATE: <n>`; ≤10 lines to Dejan.
