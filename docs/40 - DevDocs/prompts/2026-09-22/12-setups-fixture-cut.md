MODEL: Sonnet 5 (`claude-sonnet-5`) — WHY THIS SEAT (L29): no write path. Production is READ only (two read-only Cobalt commands, below); the only files written are a fixture cutter, committed real-shape fixture files, one test module, a shrunk test constant and this job's report, all on the branch; no `src/` change, no migration, no user table, no vault. `auto` mode is allowed because there is no write path (the `80-ops-fix-r3.md` precedent, 09-21 R56); the allowlist below still binds each Bash call (L62 / L63) · SEAT: fixture-cut hub `setups-fixture-cut-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK after `66-setups-one-check.md` ended `SETUPS ONE CHECK DONE …`, after the launch row **R__** and this file are committed on main, and after Dejan approved the NEW strings below in chat (L61 / L62): `cd /Users/cobalt/cobalt-wt/setups-c1` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/12-setups-fixture-cut.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control setups-fixture-cut-0922 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/setups-c1/.env)" "Bash(rm /Users/cobalt/cobalt-wt/setups-c1/.env)" "Bash(COBALT_ENV=production uv run cobalt db query --side system --prod *)" "Bash(COBALT_ENV=production uv run cobalt radar evaluate --replay *)" "Bash(ln -s /Users/cobalt/cobalt/data/radar-cache /Users/cobalt/cobalt-wt/setups-c1/data/radar-cache)" "Bash(rm /Users/cobalt/cobalt-wt/setups-c1/data/radar-cache)" "Bash(uv run python tests/fixtures/radar/_cut_setups_fixtures.py *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS, each marked: the first NINETEEN allow strings = `prompts/2026-09-21/65-setups-one-build.md`'s launch line byte for byte (PRECEDENTED: his R25 of 09-20, R18 of 09-19, R41 of 09-21 — see `65`'s MODEL line); `"Bash(COBALT_ENV=production uv run cobalt db query --side system --prod *)"` = NEW STRING, the precedented `"Bash(COBALT_ENV=production uv run cobalt db query *)"` (`prompts/2026-09-19/02-deploy-stack-3.md`) narrowed to the system side and `--prod`, the command shape of `prompts/2026-09-16/01-p2-build.md:17` (the P2 fixture cut) and `02-deploy-stack-3.md:45`; `"Bash(COBALT_ENV=production uv run cobalt radar evaluate --replay *)"` = PRECEDENTED STRING (`prompts/2026-09-18/10-deploy-stack.md`, `19-deploy-stack-2.md`, `2026-09-19/02-deploy-stack-3.md`) in a NEW USE: run from THIS worktree on the branch's code with the R41 `.env` copy, never before from a worktree; the two `ln -s` / `rm` cache-link strings = NEW; `"Bash(uv run python tests/fixtures/radar/_cut_setups_fixtures.py *)"` = NEW STRING, `01-p2-build.md:32`'s `"Bash(uv run python *)"` narrowed to the one cutter file · the three deny strings and the `--add-dir` triplet = `65`'s, byte for byte · SESSION: fresh on the first launch; every relaunch uses the SAME line and resumes from `## CONTINUE` · never `bypassPermissions`; no `COBALT_ENV=production` command other than the two read strings; no `--allow-prod`; no `cobalt db migrate`; no vault write; no memory-folder write (L58); no `launchctl`; no `claude`; you launch nothing (L36) · METER: Anthropic small–medium (≈ 1–2 h of Sonnet); Codex / Grok / Gemini NONE · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the named safe default, continue.

# FIXTURE CUT — close `AWAITING_A_DAY` (rubberband, hitchhiker) on a STORED pool day the DEFINITION forms on (2026-09-22)

Worktree `/Users/cobalt/cobalt-wt/setups-c1`, branch `setups/seven-0921`, from tip `60ddac4` (`docs(report): setups one build — BUILT dd4a9b9 …`). WHY: the one build left two pins (`tests/cobalt/test_setups_lego.py:36`, `AWAITING_A_DAY = frozenset({"rubberband", "hitchhiker"})`) because the only committed real-shape day (one trade date, FTFT + BGFI — `tests/fixtures/radar/_cut_p2_fixtures.py:36-38`) forms neither. The FINAL closes each pin BEFORE the deploy (`docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` §9 point 1 and point 4, [F-16] (1), [F-21]): a stored pool day on which the DEFINITION, evaluated on the bars, forms — **never a day because he traded or tagged it** (his R24, 09-21 11:13 ET: *"Do not assume days and look alikes. This is not known at the time"*). You SELECT the day by the engine's exit code, CUT it into a committed real-shape fixture, and PIN the formation with values the ENGINE printed. A BLIND house re-derives those values later (`13-setups-blind-values.md`, not yours). LAW STEP: build DONE (`60ddac4`) → check `66` → **THIS** → `13` (blind values) → deploy prompt → his "approve" (L7 / L61) → deploy.

**WHAT YOU DO NOT DO:** you choose NO day from his trades, tags, notes, the DRC, `1 - Trading/2 - Trades`, or any `trade_def:` frontmatter — you do not open them. You never invent, edit, re-order or re-price a bar; the cutter only re-dates (the precedent's rule). You change nothing under `src/`. You write no `DEF_WRITTEN_*` value by judgement — every one is copied from a test's printed engine output (L35).

## INDEX CARD — read these, in this order, nothing more until a step needs it
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59). Binding here, one line each: **L1** fail-loud — a raw read that does not parse, an empty day, a missing daily file → `FAILED`, never a plausible fixture. **L4** no secret printed; `.env` copied by name only, never read, never `cat`-ed. **L19** this prompt is whole; a change is a re-issue. **L28** no vault write, not even the dev vault. **L32** his trades and notes never leave the repo; a fixture is ANATOMY ONLY (bars, a membership window, daily bars — system data); a formation line printed under his definition carries prices of the bars, never a value of his note, and you copy no note text anywhere. **L35** every number in your report and every constant you write is tool output, verbatim. **L36** you launch nothing. **L41** production credentials never reach a worker; the ONLY `.env` copy is `/Users/cobalt/cobalt-wt/setups-c1/.env` (his R41); production is READ only, through Cobalt's own read commands. **L45** real shape, not verbatim — dates stripped (re-dated to a synthetic day), personal attribution and preset ids stripped; the ticker STAYS (the precedent keeps it). **L46 / L54 / L60** you work only in this worktree on this branch; `~/cobalt` is production and you only READ it; recovery = wip-commit + continue. **L48** evidence lands in the report in the same turn. **L57** every pinned value is replayable from the committed fixture. **L62 / L63** unattended; a denial is a FAILED, never a dialog. **L67** the pins you write are checked blind by another house (`13`). **L70** a "not run" is UNPROVEN, never a defect. **L71** your stop line is the LAST NON-BLANK LINE of the report.
2. THE FINAL: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` §9 (points 1–5), the `[F-16]` block, `[F-21]`; §1 `### The mechanism` (rubberband: `A-01`, the day-1 HTF avoid) and the §3 table rows D0–D3 (hitchhiker: D1 + D2 + D3; trigger `range_break`, stop `consolidation_low`, §2.2 / §2.3 rows).
3. THE BUILD REPORT `docs/40 - DevDocs/reports/setups-one-build-2026-09-21.md` (in this worktree): `## AWAITING_A_DAY`, `## ESCALATE` (i), (ii), (iii) and (ix) — (ix) says every numeric assumed hole is null in production until the assumed note is written, "so only rubberband (convention `A-01` alone) can form in production"; read it BEFORE STEP-2 (it predicts hitchhiker's outcome — you still run it; the exit codes decide, not the prediction).
4. THE REPLAY: `src/cobalt/radar/evaluate_cli.py` — `replay_formations` (it reads `"user".trade_defs` through `TradeDefStore().loaded_for_evaluation` = the PRODUCTION-synced definitions, `system.radar_membership` via `RadarStore.members_for_day`, `system.bars` i1 via `RadarStore.i1_bars`, daily bars ONLY from `data/radar-cache/<day>/daily/<TICKER>.csv` RELATIVE TO THE CWD — `CachedDailyBars`, `configs/cobalt/radar.yaml:30-31`), `scan_instants`, `admitted_at`, the FORMED print line, `expect_formed_gate` (`--expect-formed` → `SystemExit` "formed 0 times … RED"), `_select` (`SystemExit` "no loaded trade_def …; loaded: […]"). `src/cobalt/radar/evaluate.py` the avoid block (an avoid that is unknown → `input_stale` when `no_daily_bars`, else `not_formed` — no formation without daily bars for a def whose avoid reads them).
5. THE FIXTURE PRECEDENT: `tests/fixtures/radar/_cut_p2_fixtures.py` (whole: inputs by path, `_shift_datetime_str`, `cut_bars`, `cut_daily` with `_DAILY_TRIM_ROWS = 40`, output names, the "never reads the live DB" rule) and how tests load it: `tests/cobalt/radar_p2_support.py` `fixture_bars` / `fixture_daily`, `tests/cobalt/setups_shapes.py` (`Shape`, `SHAPES`, `VARIANTS`, `load_shape`, `every_scan`, `rubberband_mapping`, `drive_then_range_mapping`, `D2_CONSTRUCTED`, `drive_then_range_rows`), `tests/cobalt/test_setups_lego.py:1-80` (`AWAITING_A_DAY`, `_forms_on_a_committed_day`, `test_registry_evaluable_implies_forms_or_awaits_a_day`), `tests/cobalt/test_setups_hitchhiker.py` and `tests/cobalt/test_rubberband_forms.py` (how a formation is asserted; `DEF_WRITTEN_*` style).
6. The P2 cut's leak proofs: `prompts/2026-09-16/01-p2-build.md` line 21 (the three greps).

REPORT (L48): `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md` (the desk watches it, keyed on the LAST line changing to `^(SETUPS FIXTURE CUT BUILT|FAILED)`). Sections, in order: `## §0 Headline` (≤5 lines) → `## AUTHORIZATION` → `## PREFLIGHT` → `## CANDIDATE DAYS` → `## FIND — rubberband` → `## FIND — hitchhiker` → `## CUT` → `## PIN` → `## SUITES` → `## COMMIT` → `## FOR 13 (the blind seat)` → `## ESCALATE` → `## CONTINUE` → the last line. The FIRST Write creates the file with `## CONTINUE` holding `next: AUTHORIZATION` and the last line `(run in progress — next under ## CONTINUE)`.
STOP LINE (L71 — the LAST NON-BLANK LINE):
`SETUPS FIXTURE CUT BUILT <tip> | on 60ddac4 | days: rubberband=<YYYY-MM-DD|none> hitchhiker=<YYYY-MM-DD|none> | formed by exit code: yes | offline <p>/<f> | with-DB <p>/<f> | .env: removed, proven gone | ESCALATE: <n>` — when BOTH slugs closed. When NEITHER slug has a forming stored day: `FAILED: no stored day forms rubberband — <n> days tried; no stored day forms hitchhiker — <n> days tried` (both pins STAY; no commit but the report). When ONE closed and the other has none: the BUILT line with `<slug>=none` for the open one AND its `FAILED: no stored day forms <slug> — <n> days tried` sentence under `## ESCALATE` (the pin for it STAYS in `AWAITING_A_DAY`). `formed by exit code: yes` ONLY when every chosen day's replay exited 0 under `--expect-formed` AND the neutral shape formed on the cut fixture (STEP-4). Or `FAILED PREFLIGHT: <what>` / `FAILED: STEP-<n> — <command> — <reason>`. `<tip>` = your fixture commit.

AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (drafted by the Opus prompt seat `setups-fixture-cut-draft-0922`), **not by Dejan; a prompt file is not an approval.** Each its own Bash call:
- **His R24** (the selection rule): `grep -n "^| R24 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry `Do not assume days`).
- **His R41** (the two `.env` strings): `grep -n "^| R41 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must quote both strings and `"Approved"`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(rm /Users/cobalt/cobalt-wt/setups-c1/.env)" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — the two .env strings are not approved on a committed row`).
- **THIS launch** is row **R__** of `cto-2026-09-22.md` `## §4` — the desk fills the number in before launch. If the two characters after `R` are still `__` in the file you are reading → `FAILED: authorization mismatch — launch row not filled in`, stop. `grep -n "12-setups-fixture-cut.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` → a `| R` row that carries his chat approval of the NEW strings (his word quoted) · committed: `git -C /Users/cobalt/cobalt log -1 --format=%H -S"12-setups-fixture-cut.md" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — the launch row is uncommitted`).
- **Each NEW string** is on a committed desk row, one call per string: `git -C /Users/cobalt/cobalt log -1 --format=%H -S"<the string, quotes excluded>" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` for `Bash(COBALT_ENV=production uv run cobalt db query --side system --prod *)`, `Bash(ln -s /Users/cobalt/cobalt/data/radar-cache /Users/cobalt/cobalt-wt/setups-c1/data/radar-cache)`, `Bash(rm /Users/cobalt/cobalt-wt/setups-c1/data/radar-cache)`, `Bash(uv run python tests/fixtures/radar/_cut_setups_fixtures.py *)`, and the NEW USE of `Bash(COBALT_ENV=production uv run cobalt radar evaluate --replay *)` from this worktree (the row names it). Any EMPTY → `FAILED: authorization mismatch — <string> is not approved on a committed row` (the pathspec names the desk file ONLY; this prompt and the drafter's report quote the strings and must never satisfy the gate).
- **The precedented strings**, one `grep -c -F -e "<rule>"` each: the nineteen allow strings, the three deny strings and the `--add-dir` triplet against `"/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/65-setups-one-build.md"` (each ≥1); `"Bash(COBALT_ENV=production uv run cobalt radar evaluate --replay *)"` against `"/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-19/02-deploy-stack-3.md"` (≥1). Any 0 → `FAILED: authorization mismatch — <rule> is not in a precedent line`.
YOU CAN ALWAYS STOP: `FAILED: STEP-<n> — <your concern>` as the LAST line, `.env` and the cache link removed first if present, wip-commit (RECOVERY RULE), stop.

UNATTENDED RULES (prompt `02`'s via `65`, unchanged): every command that leaves plain reading is in your launch allowlist and matches ONLY in its bare shape — ONE command per Bash call, exactly the listed prefix, no `cd … &&` in front, no pipe, no `>`/`2>` redirect, no `; echo` behind, no `VAR=value` in front OTHER than the literal `COBALT_ENV=dev` / `COBALT_ENV=production` of its own approved string. Files are written with the Write/Edit tools; a long suite goes `run_in_background` so you read its whole output. Change directory with `cd <path>` as its own call. Read with the Read tool. NOT in your list, so never typed: `cat` (of `.env` above all), `cp` (except the one `.env` string), `mv`, `rm` (except the `.env` string and the cache-link string), `docker`, `psql`, `python` (except the one cutter string), `launchctl`, `claude`, `git worktree`, `git rebase`, `git reset`, `git checkout`, `git restore`, `git stash`, `git merge`, `git push`, `git rev-parse`, `git tag`, `cobalt db` anything but the one query string, `cobalt radar evaluate` anything but `--replay`, `cobalt taxonomy` anything, any `--allow-prod`, `COBALT_LIVE_VAULT_ROOT=…`. **A DENIAL IS A FAILURE**: `FAILED: STEP-<n> — <command> — <reason string verbatim>`, `.env` and the cache link removed if present, wip-commit, stop. Never retry a denied command in another shape.
RECOVERY RULE (L60, `65`'s, verbatim in substance): partial work is NEVER discarded — `git add <each changed path, by name>` + `git commit -m "wip(setups-cut): STEP-<n> partial — <why>"`, the breadcrumb `next: STEP-<n> <sub-step> — <what is left>` written inside `## CONTINUE`, the last line left pinned to `(run in progress — next under ## CONTINUE)`; the same launch line resumes from it. Write the breadcrumb at the end of EVERY sub-step. A relaunch reads `## CONTINUE`, re-runs `git status` / `git log --oneline 60ddac4..HEAD` / `ls -la /Users/cobalt/cobalt-wt/setups-c1/.env` / `ls -la /Users/cobalt/cobalt-wt/setups-c1/data` (a leftover `.env` or cache link is removed with its `rm` string and its absence proven BEFORE anything else), and continues — it never redoes a committed step and never re-runs a replay whose exit code and output are already in the report. On a USAGE-LIMIT message: remove `.env` and the cache link, record the reset time VERBATIM under `## CONTINUE`, wip-commit, stop with the last line pinned (a meter stop is NOT a failure). EVERY `git add` names explicit file paths under `tests/fixtures/radar/`, `tests/cobalt/`, `docs/40 - DevDocs/` — NEVER `-A`, never `.`, never a directory, never `.env`, never `data/`, never `scratch/`. Long-form `git status` before every commit (`.env` and `data/` must show as ignored or absent, NEVER staged); `git show --stat HEAD` after — only the paths you named, else `FAILED: STEP-<n> — unexpected path committed <path>`.

## STEP-0 — PREFLIGHT (one row each: command · exit · result verbatim)
`date` · `cd /Users/cobalt/cobalt-wt/setups-c1` · `git status` (long form — clean except untracked files you record; anything MODIFIED → `FAILED PREFLIGHT: worktree not clean — <paths>`) · `git log -1 --format=%h` = `60ddac4` (else `FAILED PREFLIGHT: branch tip is <sha>, not 60ddac4 — the desk re-issues on the new tip`) · `git log -1 --format=%D` names `setups/seven-0921` · `ls -la /Users/cobalt/cobalt-wt/setups-c1/.env` → absent (present → `rm` string, then prove absent) · `ls -la /Users/cobalt/cobalt-wt/setups-c1/data` → absent (a `data` directory that exists is recorded with its listing; a `radar-cache` link in it → `rm` string, prove absent) · `grep -n "^data/\|^\.env" /Users/cobalt/cobalt-wt/setups-c1/.gitignore` → both ignored (else `FAILED PREFLIGHT: data/ or .env not ignored`) · `ls /Users/cobalt/cobalt/data/radar-cache` (record the day folders) and, per day folder of the last 30 days, `ls /Users/cobalt/cobalt/data/radar-cache/<day>/daily` (record the count; `No such file` = 0) — THE DAILY-CACHE TABLE (day · daily files). Then `## BASELINE`: the two suite commands EXACTLY as `setups-one-build-2026-09-21.md` `## CLOSE` spells them (quote them from that section; the with-DB one inside the `.env` pair: `cp` string → suite → `rm` string → `ls -la …/.env` proves absent) — record `<p>/<f>` each; any failure → `FAILED PREFLIGHT: baseline red — <test ids>`.

## STEP-1 — CANDIDATE DAYS (production READ, from `~/cobalt`, precedent `01-p2-build.md:15-17`: main's code, main's own credentials, no copy)
`cd /Users/cobalt/cobalt` then ONE call: `COBALT_ENV=production uv run cobalt db query --side system --prod --format json "SELECT trade_date, count(*) AS admitted FROM radar_membership WHERE pool_key = 'primary' AND entered_at IS NOT NULL AND trade_date < DATE '2026-09-22' GROUP BY trade_date ORDER BY trade_date DESC"` — the output quoted verbatim under `## CANDIDATE DAYS` (read-only: one SELECT through the guarded read path; no `%` in SQL). `cd /Users/cobalt/cobalt-wt/setups-c1`. The candidate list = those days, MOST RECENT FIRST, today excluded (its session is not over). An empty result → `FAILED: STEP-1 — no stored pool day`. Record beside each day its daily-cache count from PREFLIGHT. Nothing else selects or orders a day: not a trade, not a tag, not a note, not a chart.

## STEP-2 — FIND THE DAY, per slug, by EXIT CODE (production READ, from THIS worktree — the seven-setups evaluator exists only on this branch; that is the NEW USE)
2.0 `mkdir -p /Users/cobalt/cobalt-wt/setups-c1/data` · `ln -s /Users/cobalt/cobalt/data/radar-cache /Users/cobalt/cobalt-wt/setups-c1/data/radar-cache` (WHY: the replay reads daily bars only from `data/radar-cache` relative to its cwd; without the link every def whose avoid reads daily bars is `input_stale` on every day — rubberband's day-1 HTF avoid is one — and the exit codes would say nothing about the definition; the link is read-only use of production's cache, the replay never fetches) · `ls -la /Users/cobalt/cobalt-wt/setups-c1/data` (the link shown) · `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/setups-c1/.env` (by name, never read, never printed).
2.1 For **rubberband**, then **hitchhiker**, walk the candidate list most recent first. Per day ONE call, `run_in_background: true`, read its whole output: `COBALT_ENV=production uv run cobalt radar evaluate --replay <YYYY-MM-DD> --trade-def <slug> --expect-formed` (it WRITES NOTHING — `evaluate_cli.py` module docstring). Record, per day: exit code · every `FORMED` line VERBATIM · the `replay <day>: scans=… formations=… counts=…` line VERBATIM · any `not evaluable: missing atoms […]` line VERBATIM · the daily-cache count of that day. Exit 0 with ≥1 `FORMED` line → that day is the slug's CANDIDATE → STEP-3/4 for it. Exit non-zero → next day.
2.2 STOP RULES per slug (each recorded verbatim, never argued): (a) `no loaded trade_def '<slug>'; loaded: […]` → the production `"user".trade_defs` does not hold the slug — `FAILED: no stored day forms <slug> — 1 days tried (not loaded in production trade_defs: <the loaded list verbatim>)`, no further day for that slug. (b) a `not evaluable: missing atoms […]` line for the slug → the same, `(not evaluable in production: <atoms verbatim>)`. (c) a `TradeDef` validation error or any traceback → `FAILED: STEP-2 — <slug> — <last line verbatim>`, `.env` and link removed, stop. (d) the list exhausted → `FAILED: no stored day forms <slug> — <n> days tried`; its pin STAYS.
2.3 WHAT THE EXIT CODE PROVES, stated in the report once: the replay evaluates the PRODUCTION-synced definition of the slug (`"user".trade_defs`, his note as main last synced it) with production's tunables (every numeric assumed hole null — build report ESCALATE (ix)) on the stored bars — i.e. FINAL §9 point 4's own gate. It does NOT prove the build's NEUTRAL test shape forms; STEP-4 proves that on the cut, and a day where it does not is NOT taken (back to 2.1, next day).

## STEP-3 — CUT (the candidate day, the forming ticker(s) only — the tickers named on that day's `FORMED` lines, first one if several, at most two)
3.1 Raw reads, from `~/cobalt` (the P2 precedent), `run_in_background: true` — the harness writes each call's whole stdout to an output file whose path the tool result names; record that path and `wc -c` it; you never copy or retype a raw row:
 - bars: `COBALT_ENV=production uv run cobalt db query --side system --prod --format json --limit 20000 "SELECT * FROM bars WHERE interval = 'i1' AND ticker = ANY(ARRAY['<T>']) AND ts >= TIMESTAMPTZ '<day> 00:00:00 America/New_York' AND ts < TIMESTAMPTZ '<day+1> 00:00:00 America/New_York' ORDER BY ticker, ts"` (the replay's own window, `evaluate_cli.py` `day_start` … `+ timedelta(days=1)`).
 - membership: `COBALT_ENV=production uv run cobalt db query --side system --prod --format json "SELECT id, ticker, trade_date, entered_at, left_at, session, rank_at_entry, last_rank FROM radar_membership WHERE pool_key = 'primary' AND trade_date = DATE '<day>' AND ticker = ANY(ARRAY['<T>']) ORDER BY id"`.
 - daily: the cache file `/Users/cobalt/cobalt/data/radar-cache/<day>/daily/<T>.csv` (read by path; absent → the candidate is REJECTED for a slug whose avoid reads daily bars, recorded, back to 2.1).
 Then `cd /Users/cobalt/cobalt-wt/setups-c1`.
3.2 The cutter — `tests/fixtures/radar/_cut_setups_fixtures.py`, written with the Write tool on `_cut_p2_fixtures.py`'s rules and layout (deterministic, re-runnable, inputs by PATH — as argv here, never a raw row in the repo): argv = `<slug> <real day> <synthetic day> <raw bars json> <raw membership json> <daily csv>`; asserts every input parses (a raw output file with any non-JSON text → `SystemExit`, L1 — no hand repair), that every bar's ticker is in the membership rows, and that the bar count > 0; re-dates EVERY datetime by the one day delta (the precedent's `_shift_datetime_str`); trims daily to the precedent's 40 rows, re-dated so the last row lands on the synthetic day (`cut_daily`'s rule); membership: ids → 1, 2, …; scan ids and `rank_*` kept only if the shape needs them, else dropped (name what you keep); no `user_id`, no source suffix, no sha. Synthetic days: rubberband `2026-01-07`, hitchhiker `2026-01-08` (trading days; never the committed `2026-01-06`). Outputs next to it: `bars-setups-<slug>.real-shape.json`, `daily-bars-setups-<slug>.real-shape.csv`, `membership-setups-<slug>.real-shape.json`. The real day is NEVER a literal in the cutter or in any output (it comes in by argv).
3.3 Run: `uv run python tests/fixtures/radar/_cut_setups_fixtures.py <argv>` — its printed counts quoted.
3.4 Leak proofs (the P2 cut's, `01-p2-build.md:21`), each its own call, output quoted: `grep -rl "2026-09-" tests/fixtures/radar/` → NO output · `grep -rlE "@[0-9a-f]{12}" tests/fixtures/radar/` → none beyond the pre-existing zeroed files (list them) · `grep -rlE "expr:|trade_def:" tests/fixtures/` → NO output · `grep -c "user_id" tests/fixtures/radar/bars-setups-<slug>.real-shape.json` → 0. Any hit → fix the CUTTER (never the output by hand), re-run 3.3.

## STEP-4 — PIN (tests only)
4.1 `tests/cobalt/test_setups_fixture_cut.py` (new): loads the cut through the real loaders' shapes (`radar_p2_support`'s `Bar` / `DailySeries` parsing, extended — if needed — with a file-name parameter that keeps every existing caller byte-identical), loads the slug's NEUTRAL shape exactly as the lego corpus does (`setups_shapes.load_shape` / `SHAPES` / `VARIANTS` — rubberband's FULL shape with its HTF avoid, the key the lego corpus pins), and evaluates it at the replay's own grid (`evaluate_cli.scan_instants` + `admitted_at` over the cut membership window, `radar.scan_interval` from the engine tunables) through `evaluate_member`. FIRST a printing test, `test_<slug>_cut_day_engine_output` (`uv run pytest tests/cobalt/test_setups_fixture_cut.py -s -k engine_output`), that prints, for the FIRST formation: `side`, `formed_bar_ts`, `trigger.price`, `stop.price`, `anchor` (the `Formation.anchor` repr, FINAL §2.4) and the scan instant. NO formation on the cut → the candidate is REJECTED for this slug (recorded, verbatim test output), the cut files are never committed: return to 2.1 with the next day and cut the next candidate under the same names (the cutter overwrites); a slug that ends with NO accepted day has its three outputs removed by the cutter's own `--remove <slug>` mode (`uv run python tests/fixtures/radar/_cut_setups_fixtures.py --remove <slug>`, which deletes ONLY those three named files and prints them) and `git status` proves them gone.
4.2 Constants, copied VERBATIM from 4.1's output (L35): `DEF_WRITTEN_<SLUG>_CUT_SIDE`, `_FORMED_BAR`, `_TRIGGER`, `_STOP`, `_ANCHOR`, each with the comment `# engine on the cut day at 60ddac4; a blind house re-derives it (13, [F-16] (1))`. `test_<slug>_forms_on_the_cut_day` asserts `formed` + the five, and the geometry guard ([F-16] (5)). Keep the printing test (it is the blind seat's comparison source).
4.3 Close the pin: `tests/cobalt/test_setups_lego.py` — `AWAITING_A_DAY` loses EXACTLY the closed slug(s), and `_forms_on_a_committed_day` (or the `Shape` it reads) also evaluates the cut day(s) through the same `every_scan`-style path, so gate 2's property still fails when a pinned set grows or shrinks unannounced; the docstring comment for a closed slug is replaced by one line naming its fixture file. `test_radar_evaluate.py:705-731` reads the constant — untouched. An open slug keeps its pin and its comment.
4.4 `uv run pytest tests/cobalt/test_setups_fixture_cut.py tests/cobalt/test_setups_lego.py -q` → green, quoted.

## STEP-5 — SUITES
Offline: the build's offline command (BASELINE's spelling), `run_in_background: true` → `<p>/<f>`. With-DB on `cobalt_dev`: `.env` is still in place from 2.0 → the build's with-DB command → `<p>/<f>` → `rm /Users/cobalt/cobalt-wt/setups-c1/.env` → `ls -la /Users/cobalt/cobalt-wt/setups-c1/.env` quoted (`No such file`) · `rm /Users/cobalt/cobalt-wt/setups-c1/data/radar-cache` → `ls -la /Users/cobalt/cobalt-wt/setups-c1/data` quoted (no link). Any red → `FAILED: STEP-5 — <test ids>` (after the removals). `uv run cobalt jobs restarts 60ddac4..HEAD` → quoted (expected: none — no `src/` change).

## STEP-6 — COMMIT
Long-form `git status` (`.env` absent, `data/` ignored). `git add` by explicit path: the cutter, each committed fixture file, `tests/cobalt/test_setups_fixture_cut.py`, `tests/cobalt/test_setups_lego.py`, any support file you extended. `git commit -m "fix(setups): AWAITING_A_DAY closed — <slugs> on stored day <YYYY-MM-DD>"` (the body names the replay command and its exit code per slug; trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`) · `git show --stat HEAD` quoted. Then `## FOR 13`: per closed slug — the fixture paths, the neutral shape's key and its `setups_shapes.py` line range, the pinned test file and the constants' LINE NUMBERS (not their values) — the desk keeps these lines out of the blind seat's packet. Then the report is committed by explicit path (`docs(report): setups fixture cut — <one line>`), `git show --stat HEAD` (the report only), the stop line LAST.

`## ESCALATE` carries ALWAYS: (i) what STEP-2's exit code proves and does not (2.3); (ii) every rejected candidate with its reason; (iii) an open slug's `FAILED: no stored day forms <slug> — <n> days tried` sentence (the desk brings it to him); (iv) the raw read output files' paths (outside the repo, harness-written — the desk decides their cleanup); (v) every `ASK DESK`. **NEXT STEP, not yours:** `13-setups-blind-values.md` compares a blind house's values with your constants; a MISMATCH goes to the desk as FIX-or-OWNER (L37).
