MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `r3-check-widen-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/69-draft-r3-check-widen.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control r3-check-widen-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `66`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool, the REPORT LAST · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps.

# RE-ISSUE `67-setups-check-r3.md` WIDENED (L19: a full re-issue). LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults` (S2 stops 2026-09-23). LAW STEP (L67: every build checked by ≥3): the desk launched `67` at 20:08 and STOPPED it at 20:1x before any house ran (no report, no scratch folder) because `reports/r3-check-devdb-draft-2026-09-22.md` ESCALATE (b) holds: `16` (the round-2 check) NEVER RAN, so round 2's fix (`60ddac4..826d284`, `15-setups-fix.md`, report `setups-fix-r2-2026-09-22.md`) and `12`'s fixture cut (`74eefd8..65c08a0`, report `setups-fixture-cut-2026-09-22.md`) have had NO house check. Desk ruling (answers that ASK DESK): ONE check covers the whole unchecked range.

INDEX CARD: read `6 - Permanent/Memory/LAWS.md` in full (L59); binding: L32 · L35 · L37 · L39 · L45 · L67 (R46 / R49 code-check seats) · L68 · L70 · L71 · L74 · L75. Files: `prompts/2026-09-22/67-setups-check-r3.md` (your base — its launch line is byte-identical to `16`'s and STAYS so); `16-setups-check-r2.md` (r2's F1–F5 + P1 question set); `12-setups-fixture-cut.md` and its report; `15-setups-fix.md`; `33-setups-fix-r3.md`; the three build reports in `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/` (`setups-fix-r2-…`, `setups-fixture-cut-…`, `setups-fix-r3-…`); `reports/setups-one-check-2026-09-22.md` (round 1's defects that HELD); `reports/r3-check-devdb-draft-2026-09-22.md`.

WRITE (Write tool) `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/70-setups-check-r2r3.md` = `67` with these changes ONLY:
- remote-control name `setups-check-r2r3-0922`; prompt path `70-…`; report `reports/setups-check-r2r3-2026-09-22.md`; scratch subfolder `…/setups-check-r2r3/`.
- RANGE `60ddac4..be44eb4` (the whole fix path since round 1's check: r2 fix → fixture cut → r3 fix), with the three sub-ranges and their reports named, so a checker can attribute every finding to its round. The boundary path list = the union of `15`'s, `12`'s and `33`'s CLOSE lists PLUS r3 ESC (ix)/(x)'s files.
- QUESTION SET = r2's F1–F5 + P1 (from `16`), `12`'s fixture-cut rows (the fixture is real-shape per L45, no value of his, the cut reproduces the committed day's formations), r3's F1–F6, the proposal KEYS + grades, Q1 (r2 ESC (viii) stop resolvers) and Q2 (r3 ESC (v) engine dial not per-setup) — each question tagged with its round.
- ROUND COUNT stated honestly: this is the SECOND house check of this build (round 1 = `66`); `16` never ran and is not counted (L67 P-c: a round that produced no ruling is not spent). A defect that HOLDS → a fix round is still lawful (one more round at most), classified first (L75).
- Launch row `R__` placeholder (the desk fills it), stagger literals as `67`'s (`59` / `63`). Everything else — the launch line strings, the date gate (R30, through 2026-09-23 23:59 ET), OFFLINE ONLY, L32 staging, the Opus / Sol / Grok / Gemini seats — stays byte for byte. Packet ≤150 KB; state size and token estimate per house.
- Stop line `SETUPS CHECK R2R3 DONE · grok: … · gemini: … · opus: … · sol: … · defects that HOLD: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.

LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/r3-check-widen-draft-2026-09-22.md`: §0 ≤5 lines → `## DIGEST FOR THE DESK` ≤20 lines → RULE PROOF (`70` vs `67` launch line: expected differences = path + remote-control name only) → `NEW strings:` (expected none) → `## ESCALATE` → `## CONTINUE` → last line; while you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE: `R2R3 CHECK DRAFTED · range: 60ddac4..be44eb4 · questions: <n> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape. `date` before any time you write. Stop within 25 minutes.
