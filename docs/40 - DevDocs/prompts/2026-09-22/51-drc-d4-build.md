MODEL: Opus 5.5 (`claude-opus-5-5`, R32 / R36) — D4 changes the settings loader and adds a write of `"user".trader_settings` from the ASET app (a DB write path, L29), so the floor is Opus. `--permission-mode auto`: the DEV LANE as `33` (L62 as amended R80, stated here and on the line) — offline build + the with-DB suite on `cobalt_dev` inside its rollback transaction; no production command, no vault write, no settings row written outside the suite. NOT `acceptEdits` (L63). An unlisted command = do not call it; files via Write / Edit · SEAT: DRC D4 builder `drc-d4-build`, launched by the CTO desk in the background. The desk FIRST runs `git -C /Users/cobalt/cobalt worktree add -b drc/d4-settings /Users/cobalt/cobalt-wt/drc-d4 main`, fills the BASE TIP (= `main` at that moment) and launch row **R__**, commits both, then: `cd /Users/cobalt/cobalt-wt/drc-d4` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/51-drc-d4-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-d4-build --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/drc-d4/.env)" "Bash(rm /Users/cobalt/cobalt-wt/drc-d4/.env)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: `33`'s seventeen shared strings + denies + triplet BYTE FOR BYTE; the TWO `.env` strings are NEW (re-pointed to `drc-d4`) · SESSION: fresh; relaunch = same line, resume from `## CONTINUE` · never `bypassPermissions`; push DENIED; no `COBALT_ENV=production`, no `cobalt settings load` typed (the loader is exercised by tests only), no vault write, no memory-folder write, no `launchctl`, no `claude` · `.env` by name, removed at every stop (L41) · METER: Anthropic MEDIUM (≈5 seat hours: v2's 4 + R102's change line) · nobody sits at this terminal: `ASK DESK: <q> [<time>]` under `## ESCALATE`, safe default, continue.

# DRC D4 — THE DRC SETTINGS FAMILY, ONE SOURCE FOR THE DAILY STOP AND THE DOLLARS PER GRADE, HIS CHANGE LINE IN ASET, THE DAILY NOTE READS THE KEY. Branch `drc/d4-settings`, worktree `/Users/cobalt/cobalt-wt/drc-d4`, from BASE TIP **`__`** (main). D4 is FIRST in the DRC stack (D4 → D1 → D2 → D3; D5 later) because it is the ONE chunk that parses none of his files — it does not wait on E1 (L72, L73: minimum idle). DO NOT STOP until the last line is `DRC D4 BUILT …` or `FAILED …`. NEVER END A TURN BETWEEN STEPS.

WHY: v2 §5 T9 `[F-18]` + §9 D4, as HIS rulings reshaped them: **R95** (O5, "A"): ONE place for the daily stop and the dollars per grade; the committed daily template stops carrying the number — removed INSIDE this build. **R96 + R102** (O6, "1.A"): that place is the CENTRAL SETTINGS (`"user".trader_settings`), "changed by me clicking on a change line we do in ASET now" (his words carry "lime"; he means the ASET line), and SHOWN in the daily note. **R102** (O11): only `Sleep:` / `Readiness:` / `RHR:` / `1% goal:` are read from the daily note (D3's reader; nothing here). **R101** (O7): NO rule→checker map, no checker ids, no binding hash in this build — v2 `[F-16]`'s binding rows are STRUCK; its `OPTIONAL_SETTING_KEYS` placement STANDS. LADDER `S3-P3 · F14`.

## THE ROWS — in this order, nothing else
| row | what | files |
|---|---|---|
| **D4-1** KEYS + SCHEMA | The DRC key families of SPEC §7 that this slice READS, each a Pydantic model registered in `OPTIONAL_SETTING_KEYS` (NEVER `SETTING_KEYS` — a missing DRC key never touches a sheet reader, v2 `[F-16]`): `account.*` (the daily stop — take the key NAMES from SPEC §7 of `/Users/cobalt/cobalt/docs/_inflight/DRC-automation-spec-2026-09-22.md`, NAMES ONLY, never a value — L32), `limits.card_match_window_minutes`, `windows.*`, `goal.*` (the names SPEC §7 carries). NOT `grades.*` (the dollars per grade ARE the existing `aset.sheet_modes` row — a second copy is an L3 defect, v2 `[F-18]`), NOT `sleep_trigger.*` (later slice), NOT any checker / binding key (R101). One reader: `load_drc_settings(conn) -> DrcSettings` — every field `Optional`; absent → `None`, rendered `not given` by callers; NO default, NO fallback (L1). | `src/cobalt/settings/models.py`, `src/cobalt/settings/drc.py` (new) |
| **D4-2** ONE READER OF THE DAILY STOP + GRADE DOLLARS | `daily_risk_values(conn) -> DailyRisk(daily_stop…, grade_dollars)` — the ONLY function on any DRC or daily-note path that returns those numbers: daily stop from D4-1's `account.*` row, grade dollars from `load_sheet_modes_config()` (`aset/config.py` — re-locate by symbol). Nothing reads `daily.md.j2`, a `Rules.md` sentence, `aset.yaml`, or a second config for them. | `src/cobalt/settings/drc.py` |
| **D4-3** THE DAILY NOTE READS THE KEY | `configs/cobalt/templates/daily.md.j2` — the daily-stop line (`:18`, re-locate by its heading text) renders the value from `daily_risk_values` passed in by `prefill/daily.py`; absent key → the line renders `not given` (never the old literal, never blank-as-number). AFTER this row `grep -n` of the old literal in `configs/` and `src/` returns 0 (the literal is his value leaving git — R95; you READ it once to grep for it, and never write it anywhere, the report included). The REST of the template's lines are untouched (his words on those lines are not this ruling). | `configs/cobalt/templates/daily.md.j2`, `src/cobalt/prefill/daily.py` |
| **D4-4** THE CHANGE LINE IN ASET (R96 / R102) | ONE form on the ASET page, placed with the existing attestation form (`/attest`'s section — find its template by symbol): shows the CURRENT daily stop and dollars per grade read through `daily_risk_values`; he types new values → `POST /settings/daily` renders the PER-KEY DIFF (old → new) with a sha256 of the proposed payload and an Apply button; Apply (`POST /settings/daily/apply` with that sha256) writes through the SAME function `cobalt settings load --apply` uses for its `put` (factor it out if it is CLI-bound — ONE write path, L3; the CLI and the page are callers), then a round-trip read; the page shows `saved` only after the read-back equals the payload. Refused inside `market_reset` (20:00–21:00 ET) with the reason (L66 — a `trader_settings` write is refused in the pause; if the store does not already refuse, the route does, and says so). Its trace: the settings history row the store keeps (versioned) + one log line naming keys, hash and time — never the values in the log (L4 is for secrets; values are his user data, L32: log keys only). Validation fails loud per field (a non-number, a negative, a grade with no dollar) naming the field; nothing is written on any failure. | `src/cobalt/aset/web.py` (a route block placed directly AFTER the `/attest` route — D2 adds its `/drc` routes at the END of the file; neither touches the other's block), the ASET page template the attestation form lives in, `src/cobalt/settings/cli.py` (only to factor the one apply function), `src/cobalt/settings/store.py` (only if the refusal is added there) |
| **D4-5** DRY-RUN | `cobalt settings show` (existing) prints the DRC keys with `not given` for absent ones; the D3 `cobalt drc build --dry-run` is D3's (v2 `[F-27]`: `cli.py` is D3's — you do NOT edit `src/cobalt/cli.py`). | `src/cobalt/settings/cli.py` |
| **D4-6** E10 + X12 (v2 §10, gates of D4) | With-DB tests in the suite's rollback: **E10** — insert one DRC key row on `cobalt_dev` with NO migration → read back through `load_drc_settings` (pass → D4 stays migration-free; fail → `FAILED: D4-6 — E10: trader_settings needs a migration — <output>`, the desk numbers one). **X12** — no DRC key rows → the `/size` route still sizes (TestClient) and `load_drc_settings` returns every field `None`. | `tests/cobalt/test_drc_settings_db.py` |
| **D4-7** DEVDOCS | one paragraph per touched `.py`; new DevDoc for `settings/drc.py`. | `docs/40 - DevDocs/cobalt/**` |

NOT IN D4: any DRC parser, page, note or build (D1–D3); any rule checker or rule→checker map (R101); `sleep_trigger.*`; the Charter / ladder wording (desk). YOUR LITERALS in tests only (L69: a test never asserts a value read from `trader_settings`; values live on a constructed config).

## INDEX CARD
1. LAWS.md IN FULL (L59). Binding here: **L1** absent = `not given`, never a default. **L3** one reader of the daily stop / grade dollars; one settings write path shared by CLI and page. **L7** no human→engine flip here (he types his own values; nothing computes them). **L10** each family a Pydantic schema, dry-run = `settings show`. **L28 (amended 09-15)** his hand is exempt from the HITL token; the trace is the history row + log line. **L31** no name in keys. **L32 / L53** no value of his in anything committed — the daily-stop literal LEAVES git. **L40** `trader_settings` writes only through the settings store. **L42** RESTARTS derived. **L43 / L66** a resident that reads `trader_settings` restarts inside the pause. **L69** tests never assert a settings value. **L19, L29, L35, L41, L46, L48, L54, L60, L62, L63, L67, L68, L70, L71, L73, L74** as `48`'s INDEX CARD states them.
2. v2 §5 (T9 `[F-18]`, `[F-16]`), §9 D4, §10 E10 / X12, §12.
3. `cto-2026-09-22.md` §4 R95, R96, R101, R102.
4. Code: `src/cobalt/settings/{models,store,cli,card}.py`, `src/cobalt/aset/config.py` (`SheetModesConfig`, `load_sheet_modes_config`), `src/cobalt/aset/web.py` (`/attest`, `/size`, `_render`), `src/cobalt/prefill/daily.py`, `configs/cobalt/templates/daily.md.j2`, `src/cobalt/vaultwrite/writer.py` (`assert_writable` — the `market_reset` clock), `configs/cobalt/jobs.yaml` (`reads:` of `trader_settings`).

## AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (drafted by `drc-build-draft-0922`, 2026-09-22), not by Dejan. Each its own Bash call on the desk file `"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"`: `grep -n "^| R95 " <file>` (`the committed daily template stops carrying the number`) · `grep -n "^| R96 " <file>` (`clicking on a change lime`) · `grep -n "^| R101 " <file>` (`psychology is disclosed`) · `grep -n "^| R102 " <file>` (`1.A 2. A 3.A 4. B`) · `grep -n "^| R32 " <file>` (`claude-opus-5-5`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"1.A 2. A 3.A 4. B" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY. THIS launch = row **R__** (`grep -n "51-drc-d4-build.md"` on the launch day's desk file → a `| R` row; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"51-drc-d4-build.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` NON-EMPTY). The two `.env` strings: `grep -c -F -e "Bash(rm /Users/cobalt/cobalt-wt/drc-d4/.env)"` ≥1 on the launch day's desk file AND committed (`git log -1 -S…` on the desk files ONLY). Every other string ≥1 in `33-setups-fix-r3.md` (`grep -c -F -e`). Any miss → `FAILED: authorization mismatch — <what>`, stop.

UNATTENDED RULES and RECOVERY RULE: `48`'s, with `drc-d4` / `D4-<n>` / `wip(drc-d4)` in place of `drc-d1` / `D1-<n>` / `wip(drc-d1)`. NEVER `git add` the spec file or anything under `docs/_inflight/`.

REPORT (L48): `/Users/cobalt/cobalt-wt/drc-d4/docs/40 - DevDocs/reports/drc-d4-build-2026-09-2<n>.md`, committed on the branch; sections as `48`'s with `## D4-1` … `## D4-7` and no `## E1`. First Write: `## CONTINUE` = `next: AUTHORIZATION`, pinned last line `(run in progress — next step under ## CONTINUE)`.
STOP LINE (L71): `DRC D4 BUILT <tip> | on <base> | offline <p>/<f> | with-DB <p>/<f> | E10: <PASS|FAIL> | X12: <PASS|FAIL> | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` — or `FAILED PREFLIGHT: <rule>` / `FAILED: D4-<n> — <command> — <reason>`.

## STEP-0 — PREFLIGHT, BASELINE
PREFLIGHT rows: `date` · BASE TIP filled · `git status --porcelain` EMPTY · `git status` (branch `drc/d4-settings`) · `git log --oneline -1` = BASE TIP · `ls -la /Users/cobalt/cobalt-wt/drc-d4/.env` "No such file" · DEV-DB STAGGER (`ls -la /Users/cobalt/cobalt-wt/*/.env` → no file) · `ls /Users/cobalt/cobalt/docs/_inflight/DRC-automation-spec-2026-09-22.md` (absent → `ASK DESK: the spec is gone — key names? [<time>]`, safe default: `FAILED PREFLIGHT: SPEC §7 unreadable — key names are his spec's`) · `uv run cobalt jobs restarts <base>..HEAD`.
BASELINE: OFFLINE `uv run pytest -q tests/cobalt -p no:cacheprovider` (`run_in_background`) → `failed` 0; WITH-DB inside the `.env` pair: `COBALT_ENV=dev uv run pytest -q -p no:cacheprovider -rs tests/cobalt/test_tenancy.py` + every with-DB settings / aset test on the base (`grep -l "trader_settings" tests/cobalt/*.py`, each by path, say so) → `rm` → "No such file".

## ROW SHAPE — `48`'s, with tests in `tests/cobalt/test_drc_settings.py` (offline) and `tests/cobalt/test_drc_settings_db.py` (with-DB). Required, at least: each family validates / fails naming the field; absent key → `None`; `daily_risk_values` is the only reader (a test greps `src/cobalt` for the key names outside `settings/` and the one caller — list the hits); the daily template renders the key and `not given` when absent, the old literal gone (`grep` proof, value not printed); the change line: diff shown, apply with a WRONG sha256 refused and nothing written, apply inside a patched `market_reset` clock refused, a bad field refused naming it, a good apply round-trips; `/attest` and `/size` behaviour unchanged (their existing tests green, untouched).

## CLOSE — quoted VERBATIM, each its own call
OFFLINE + WITH-DB (baseline set + `test_drc_settings_db.py`) → `failed` 0 → `rm` → "No such file" · `git diff --stat <base>` → only the rows' files + tests + DevDocs + report · `git diff <base> -- src/cobalt/cli.py src/cobalt/drc src/cobalt/db_migrations src/cobalt/aset/engine.py src/cobalt/cards src/cobalt/radar` EMPTY (`aset/engine.py` — sizing math — is never touched; L52 boundary) · L32: `grep -rn -F "<the old daily-stop literal>" configs src tests` → 0 hits, reported as `old literal: 0 hits` WITHOUT the value · `uv run cobalt jobs restarts <base>..HEAD` VERBATIM (expect `com.cobalt.aset`, and every resident reading `trader_settings`; a radar restart → name it for the pause, L43 / L66; UNCLASSIFIED → ESCALATE) · L68: every unmerged branch touching `src/cobalt/aset/web.py` or `src/cobalt/settings/` (`git -C /Users/cobalt/cobalt log --oneline --all --since=2026-09-15 -- <path>` → branch names) → table.
`## ESCALATE` ALWAYS: (i) the key NAMES added, per family; (ii) E10 / X12 results; (iii) the RESTARTS table and whether a radar restart lands in the pause; (iv) that his daily-stop VALUE must be loaded by HIM before the first deploy (his change line or `cobalt settings load --optional`) — until then the daily note says `not given` (a production-visible change the deploy prompt names); (v) every `ASK DESK`; (vi) `MEMORY:` / `RULING:` lines.
Stop line LAST, report committed by path (`git commit -m "feat(drc): D4 — report"`), `git status --porcelain` EMPTY, stop. NEXT, not yours: `56-drc-d4-check.md`; D1 (`48`) stacks on this tip once E1 is dropped.
