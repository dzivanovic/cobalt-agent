MODEL: Opus 5 (`claude-opus-5`) — a read-and-write drafting job of a PRODUCTION DEPLOY prompt (two branches, one stacked gate, residents down and up); no code, no DB, no launch · SEAT: drafter `stacked-deploy-draft-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK from `~/cobalt`: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/04-draft-stacked-deploy.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control stacked-deploy-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as 09-21's `13` / `50` / `64` / `79`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# Draft TODAY's stacked deploy — `s2/stale-marker-0921` + `ops/2026-09-21`, from 11:00 ET (2026-09-22)

HIS RULING THIS MORNING (`cto-2026-09-22.md` §4 **R3**, 06:43 ET, verbatim): "we will deploy today after trading day ends at 11 am and continure building and deploying through the day if anything build and ready" → an OVERRIDE for today of L43 (one deploy per evening) and of the radar-restart pause window (L43's 2026-09-15 clause / L66's "inside the 20:00–21:00 pause"). NOT set aside: L66's SHAPE (residents that restart go DOWN before the merge, UP after), L67 (this prompt is read by another house before it runs), L68 (the integrated gate on the stacked tree BEFORE the merge, offline AND with-DB), L42 (restarts derived by rule, `cobalt jobs restarts`), L54 (rebase-then-ff; rollback = revert), L35, L62 / L63 (every permission ahead; a mid-run question or denial = FAILED), L71.

## INDEX CARD — read these, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L42, L43 (as amended 09-21), L51, L54, L62, L63, L66, L67, L68 (both amendments — the SCOPE clause and "THE GATE BRANCH MAY BE WHAT SHIPS"), L70, L71. One line each in the prompt you write.
2. THE TWO PRECEDENTS, end to end: `docs/40 - DevDocs/prompts/2026-09-21/53-stale-marker-deploy.md` (SUPERSEDED by R47 — never launched — but its STEP-0…STEP-6 shape, its rule strings, its smoke rows and its rollback are the base for branch 1) and `docs/40 - DevDocs/prompts/2026-09-19/53-deploy-d3.md` + its report `docs/40 - DevDocs/reports/deploy-2026-09-19.md` (the D3 section — tag `deploy-2026-09-19c`; the FIRST stacked deploy under L68's gate-branch clause: how the gate branch was cut, how `main` was merged INTO it and proved docs-only, the `--ff-only` of the gate branch, the ONE `git revert -m 2` rollback). Also `docs/40 - DevDocs/prompts/2026-09-21/54-review-stale-marker-deploy.md` (the house read of a deploy prompt — the shape of your file (b)) and `docs/40 - DevDocs/prompts/2026-09-21/16-degraded-line-deploy.md` STEP-4 (residents down → merge → up, the exact `launchctl` calls that ran 20:06 last night — `reports/deploy-2026-09-21b.md`).
3. THE TWO BRANCHES: (a) `s2/stale-marker-0921`, worktree `/Users/cobalt/cobalt-wt/stale-marker`, tip `ca9566f`, build report `/Users/cobalt/cobalt-wt/stale-marker/docs/40 - DevDocs/reports/stale-marker-build-2026-09-21.md` (`STALE MARKER BUILT ead43a0 | on 5b208a0 | offline 2230/0 … RESTARTS: com.cobalt.aset`), its check `reports/stale-marker-check-2026-09-22.md` (RUNNING or not yet run when you draft — your prompt's PREFLIGHT reads its LAST line: `STALE MARKER CHECK DONE …` with `ready: yes` or the shape `68` defines, else FAILED); (b) `ops/2026-09-21`, worktree `/Users/cobalt/cobalt-wt/ops-0921`, tip `8f3db83`, reports `ops-0921-…`, `ops-classifier-fix-2026-09-22.md`, `ops-fix-r3-2026-09-22.md` on that worktree (`OPS FIX R3 BUILT af77d6b | on 3ad064f | offline 2213/0 … src diff: empty`), its round-3 check `reports/ops-6a-check-r3-2026-09-22.md` (same rule: PREFLIGHT reads its last line, `OPS 6A CHECK R3 DONE …`, ready per `81`'s shape). `git -C /Users/cobalt/cobalt log --oneline main..s2/stale-marker-0921` and `main..ops/2026-09-21` = what lands; `git -C /Users/cobalt/cobalt log --oneline --stat s2/stale-marker-0921..main | head` = how far main moved (docs only since `5b208a0`? — PROVE it with `--stat`, name every non-docs path if any).
4. WHAT EACH BRANCH TOUCHES (for restarts, L42): `git -C /Users/cobalt/cobalt show --stat` of each commit on the two branches. Known: stale marker = `src/cobalt/aset/radar_panel.py` + tests + DevDoc (aset); ops = `configs/cobalt/backup.yaml` (restic include set — read by the `com.cobalt.backup` one-shot at 21:40, not a resident), `configs/cobalt/jobs.yaml` (a `no_resident_reads` row — read by `cobalt jobs restarts`), tests, `.gitignore`, DevDocs, BACKLOG, `UNATTENDED-LAUNCH.md`. The hub's STEP-3 runs `COBALT_ENV=production uv run cobalt jobs restarts <base>..<gate tip>` on the combined tree and the prompt PREDICTS its table: expected `RESTARTS: com.cobalt.aset` ONLY — the radar is NOT restarted by this set unless the table says so. If the table names the radar, the hub restarts it too (R3 allows it today) but records why.
5. THE GATE (L68): both branches are siblings off `main`; main has moved (docs commits). Shape from precedent 2: cut a gate branch `deploy/stacked-0922` FROM `main` in a NEW worktree `/Users/cobalt/cobalt-wt/stacked-0922` (`git -C /Users/cobalt/cobalt worktree add -b deploy/stacked-0922 /Users/cobalt/cobalt-wt/stacked-0922 main` — run by THE DESK before launch, like `65`'s worktree; the hub never runs `worktree add`), merge both branches into it (`git -C … merge --no-ff` or rebase-then-merge — choose the one precedent 2 used and say why), run the OFFLINE suite and the WITH-DB suite there (the with-DB run needs `.env` in that worktree: the R41-shaped pair `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/stacked-0922/.env)` / `Bash(rm /Users/cobalt/cobalt-wt/stacked-0922/.env)` are NEW strings for this path — name them as NEW; `COBALT_ENV=dev uv run pytest *` = 09-19 R18; the hub proves `.env` is gone before any commit), `cobalt validate`, `cobalt jobs restarts`, then `main` merged INTO the gate branch and the diff proved docs-only, then `git -C /Users/cobalt/cobalt merge --ff-only deploy/stacked-0922` inside the residents-down window. Rollback = ONE `git revert -m 2` of that merge (precedent 2), never a per-commit walk; name the exact revert string.
6. THE WINDOW (R3): the hub's STEP-0 `date` gate = 2026-09-22, at or after 11:00 ET, before 19:55 ET (the 20:00 pause and the 20:30 archiver / 21:10 replay / 21:40 backup are NOT to be crossed by a residents-down window); no other clock rule. Residents-down window = aset only (bootout → merge → bootstrap/kickstart → `/radar` 200 → heartbeat row), the calls of `16` STEP-4 minus the radar pair unless item 4's table names the radar.
7. SMOKE (NN#16) after the merge, copied from `53` STEP-4.5 and widened for ops: `/radar` 200 · the pool renders (the stale badge absent when healthy — `53`'s rows) · `cobalt heartbeat show` green or the known-red replay row only (last night's replay failure is KNOWN; it clears at 21:10 tonight — not this deploy's red) · `cobalt jobs restarts` on the landed range exits 0 · `cobalt validate` (NOTE: it exits 1 TODAY on three untracked `docs/_inflight/*.md` files — `close-2026-09-21.md` ESCALATE 1 — the hub quotes it and treats THAT one violation as known, any other line = red).
8. RULE STRINGS: start from `53`'s list (item 2) — every string of your hub prompt is either in `53` byte for byte, in `2026-09-19/53-deploy-d3.md` byte for byte, or NEW; list every NEW one in your report's `## RULE PROOF` with the step that needs it (expected NEW: the ops worktree rebase pair `git -C /Users/cobalt/cobalt-wt/ops-0921 rebase main` / `rebase --abort`, the gate worktree's merge / rebase / `.env` strings, the `--ff-only deploy/stacked-0922` string, the `revert -m 2` shape if `53`'s `revert --no-edit *` does not cover it). `Bash(git push*)` stays DENIED (L55).

## THE JOB — write three files with the Write tool, nothing else
(a) `docs/40 - DevDocs/prompts/2026-09-22/05-stacked-deploy.md` — the DEPLOY HUB, ONE complete block (MODEL Sonnet 5 with the WHY-NOT-OPUS finding restated from `53` and re-verified for ops: no migration, no `trader_settings` write, no DB write, no vault write → no L29 floor; if ANY commit on either branch touches a write path, say so and make it Opus 5 + `--permission-mode acceptEdits` · SEAT `stacked-deploy-0922` · SESSION fresh · auto mode on (or acceptEdits if Opus) · METER Anthropic small; launch line FIRST = the desk's bare commands incl. the worktree add the DESK runs, then `cd /Users/cobalt/cobalt`, then the `claude --bg` line with the full `--allowedTools` list and `--disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)"`; index card SECOND; AUTHORIZATION paragraph citing R3 + the `R__` launch row the desk fills; PLACEHOLDER GATE as `2026-09-22/01-radar-benchmark-load.md` has it; STEP-0 PREFLIGHT (date window, both check reports' last lines, both worktrees clean, main clean-enough, the desk's house-read report `reports/stacked-deploy-review-2026-09-22.md` last line `… DONE … blockers: 0`) → STEP-1 gate branch + rebases → STEP-2 the two suites + validate + restarts on the gate tip (with `.env` in and OUT) → STEP-3 main into gate, docs-only proof → STEP-4 residents down → `--ff-only` → up → `/radar` 200 → STEP-4.5 smoke → STEP-5 rollback (the single revert, then residents up again, then `FAILED` with `rollback: used`) → STEP-6 tag `deploy-2026-09-22` (a second deploy today tags `deploy-2026-09-22b`, and so on), report commit, worktree note. Report `docs/40 - DevDocs/reports/deploy-2026-09-22.md`, LAST LINE `STACKED DEPLOY DONE <tip> · tag deploy-2026-09-22 · branches: 2 · residents down <n> s · /radar 200 · rollback: not used · ESCALATE: <n>` or `FAILED: <step> — <reason> · rollback: <used|not used>`. NO desk commit on main between launch and stop line — say it in the file.
(b) `docs/40 - DevDocs/prompts/2026-09-22/06-review-stacked-deploy.md` — the ONE-ROUND HOUSE READ of (a) on `54`'s shape: Grok + Gemini (`Bash(grok *)` / `Bash(agy *)` through 2026-09-22 23:59 ET, 09-21 R39), staged under the approved root `scratch/tribunal-bars-0920/stacked-deploy-0922/` (NO `mkdir` — the first Write creates the folder). Questions: (Q1) every command the steps run is in the allowlist and nothing else is; (Q2) the L68 gate runs BEFORE the merge on the tree that ships, with `.env` proven gone; (Q3) the residents-down window matches item 4's restart table and crosses no job; (Q4) the rollback is one revert and restores the residents; (Q5) any path to a mid-run question. Report `docs/40 - DevDocs/reports/stacked-deploy-review-2026-09-22.md`, last line `STACKED DEPLOY REVIEW DONE · houses: <n> of 2 · blockers: <n> · folds: <n>` or `FAILED: …`.
(c) your report `docs/40 - DevDocs/reports/stacked-deploy-draft-2026-09-22.md`: §0 DIGEST ≤12 lines (files written; builder seat and why; the predicted RESTARTS line; how far main moved and whether anything non-docs is in it; the window; every NEW rule string by exact text; ESCALATE count) → `## RULE PROOF` (the hub's full allowlist, each string marked `53` / `d3` / NEW) → `## ESCALATE` → LAST LINE `STACKED DEPLOY PROMPTS DRAFTED · prompts: 2 · new rule strings: <n> · restarts: <list> · window: 11:00–19:55 ET · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. Never invent a flag or a subcommand: every `cobalt` / `git` / `launchctl` shape you write appears in a precedent file or in `src/cobalt/*/cli.py`. The hub never runs `worktree add`, never pushes, never touches `cobalt_dev` from `~/cobalt`, never `reset --hard`. `date` for every time you write. Stop within 35 minutes; your stop line is your report's last non-blank line.
