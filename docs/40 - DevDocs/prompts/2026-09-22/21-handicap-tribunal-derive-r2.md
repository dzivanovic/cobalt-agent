DERIVE ROW: R30

MODEL: Fable 5.1 (`claude-fable-5-1`) — the FABLE SEAT's CLOSING JOB on the float-handicap tribunal (`cto-2026-09-21.md` §4 R30: `Fable seat: yes` · `derive seat: claude-fable-5-1` — "the blind round-1 ruling + the derive step", as on the bars and setups tribunals; the setups precedent: R20 covered its second derive `46`); you derive v3 from v2 and the round-2 rulings; you build, launch and commit nothing · SEAT: `float-handicap-tribunal-derive-r2-0922`, launched by the CTO desk in the background ONLY after BOTH round-2 stop lines are committed (hub `19`, the Fable seat `20`). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/21-handicap-tribunal-derive-r2.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control float-handicap-tribunal-derive-r2-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the `--model` value is the one R30 names after `derive seat:`, already filled; the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `39`, `40`, `46`, 09-20 R13 — nothing new) · SESSION: fresh (the round-2 seat's context is NOT carried; its report is on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic FABLE — honest estimate: the round-2 hub report ≈ 8–15k tokens, the raw round-2 rulings of the houses that ruled ≈ 6–12k, your round-2 report ≈ 6k, v2 ≈ 16k (read whole — v3 is v2 amended), the derive report ≈ 5k, LAWS.md ≈ 25k, code re-opened only where a fold turns on it ≈ 5–10k; v3 ≈ 16–18k tokens of output (v2 whole + the folds); peak context ≈ 110–140k. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v3 of the FLOAT HANDICAP DESIGN from v2 and the round-2 rulings. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`. LAW STEP (L67 / L52): proposal → round 1 → derive v2 (`40`) → round 2 (hub `19`: Grok, Gemini; Astra METER, recorded under `cto-2026-09-22.md` R13; the Fable seat `20`) → **THIS = the second derive; it CLOSES the tribunal** (or L39 → Dejan for what did not converge) → the H1 build prompt (≥3 checkers) → deploy. L52 (a)–(d) is the bar v3 must meet; **R2-1 gates H1, R2-2 gates H2**. DO NOT STOP until the report's last line is `FLOAT HANDICAP DERIVED v3 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). Each its own Bash call: `grep -n "^| R30 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry `Fable seat: yes`, `derive seat: claude-fable-5-1` and `FLOAT-HANDICAP-PROPOSAL-2026-09-21.md`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R30 | " -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — R30 is not committed`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — row R30 names <id>; this session runs <id>`, stop · **BOTH ROUND-2 STOP LINES, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-r2-2026-09-22.md"` (the LAST NON-BLANK line must start `FLOAT HANDICAP TRIBUNAL R2 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 2 not done — last line: <line verbatim>`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r2-2026-09-22.md"` (must start `FLOAT HANDICAP TRIBUNAL FABLE R2 DONE ` — else `FAILED: refused — Fable round 2 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"FLOAT HANDICAP TRIBUNAL R2 DONE" -- "docs/40 - DevDocs/reports/float-handicap-tribunal-r2-2026-09-22.md"` and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"FLOAT HANDICAP TRIBUNAL FABLE R2 DONE" -- "docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r2-2026-09-22.md"` (either EMPTY → `FAILED: refused — <report> stop line is not committed on main`) · **R2-1 NEEDS A HOUSE:** the hub's stop line shows `houses that ruled: 0 of 3`, or its `## Rulings table` shows no house (Grok or Gemini) answering R2-1.1 with `ADOPT A` / `ADOPT B` / `ADOPT WITH` / `OWNER` → `FAILED: R2-1 has no house ruling — the desk brings it to him` (the Fable seat alone never settles the item that gates H1 — it holds one side of it) · **ASTRA (R13):** the hub's `astra:` field is `SKIPPED — …` — RECORDED, not a refusal: derive from the seats that ruled, mark v3 `ASTRA PENDING (R13)` in its header and at each `[R2F-nn]`, and name in §0 that Astra reads the FINAL on Sat 09-26. **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L3 · L7 · L19 · L32** (v3 and your report are COMMITTED: no value of his — thresholds, factor, combinator value by KEY only) **· L35 · L36 · L37** (you derive; a SPLIT is carried to him, never settled by count) **· L39** (≤3 rounds; an item unresolved after round 2 goes to Dejan — NO round 3 is drafted unless a house said `DO NOT BUILD`; a law-file / design split that does not converge → Dejan, never a vote) **· L48 · L52** (a)–(d) **· L53** (his `priority`, `first_from`, cap and stickiness count are HIS: an `OWNER` answer on R2-1 is his to rule, never yours) **· L57 · L62 · L63 · L67 · L70 · L71 · L72 · L74**.
2. THE ROUND-2 RULINGS: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-r2-2026-09-22.md` whole — `## Rulings table`, `## Wording offered, verbatim`, `## Fable round-1 claims, file-checked` (C1–C9), `## Checked against the files`, `## Experiments named (L70)`, `## OWNER answers`, `## Independence`, `## ESCALATE` — its HOLDS / DOES NOT HOLD tables are your filter · the raw round-2 rulings of every house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/float-handicap-tribunal/r2/` (`grok-ruling-r2.md`, `gemini-ruling-r2.md` — the raw file is what "verbatim" means, but you never copy a value span out of it) · your own round-2 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-fable-r2-2026-09-22.md` (its SELF-ATTACK and WITHDRAWN lines bind you: a withdrawn sentence of yours is never folded).
3. v2 `/Users/cobalt/cobalt/docs/30 - Design/FLOAT-HANDICAP-v2-2026-09-21.md` whole, and the derive report `reports/float-handicap-tribunal-derive-2026-09-21.md` (`## NEEDS ROUND 2`, `## OWNER ITEMS`, `## ESCALATE`).
4. Only where a fold turns on it: the real file a file-check cites — read to check, never to copy.

OWNER RULINGS THAT BIND: R28 / R29 (a SOFT penalty, never an exclusion; the size and shape his, read AFTER the tribunal; nothing of his is a precondition to build — a round-2 wording that makes one a precondition is NOT taken, and is named in `## ESCALATE`).

THE RULES OF THIS DERIVE — `40`'s and `46`'s rules, UNCHANGED, plus the closing rule:
- **Take a wording VERBATIM** — a house's `ADOPT WITH` replacement, or the round-1 text (grok's or yours) an `ADOPT A` / `ADOPT B` names, word for word, or not at all; you choose, you never blend, edit or improve. A wording whose supporting claim DOES NOT HOLD in the round-2 hub's file-check (or is WITHDRAWN in your round-2 report) is NOT taken.
- **When is a question CONVERGED.** Count the SEATS that ruled it: the houses (Grok, Gemini) plus the Fable seat. CONVERGED = (i) every seat that ruled it gave the same answer (`ADOPT A`, `ADOPT B`, or `ADOPT WITH` texts the hub's file-check shows are the same mechanism — otherwise not the same), **with at least one HOUSE among them**; or (ii) every HOUSE that ruled it gave the same answer and the Fable seat's contrary answer rests on a claim that DOES NOT HOLD in the hub's C1–C9 or file-check table, or that the Fable seat itself WITHDREW. The Fable seat's answer alone never settles a question (it is one of the two sides). **An `OWNER` answer is never converged into a design choice by you:** a question on which every seat answered `OWNER`, or on which any HOUSE answered `OWNER`, is OPEN FOR DEJAN (L53: ceilings and tiers are his). Everything else is STILL SPLIT. An ITEM is converged when each of its questions is.
- **NOT CONVERGED → an `OPEN FOR DEJAN` block, never settled by you (L37, L39).** ONE block per item (not per question): the two positions VERBATIM (`A — <text>, whose` / `B — <text>, whose`), each seat's reason in ONE sentence with its `file:line`, the desk's A/B to bring him in ONE line each with the COST of each in HIS terms (a handicapped name on the `first_from` screen that can never lose its seat · a list name that can overtake a screen name, weakening his `priority` · a migration and two CHECK changes · a night's miss line failing after a revert · a mislabelled exclusion on the `/radar` excluded list · an extra deploy evening), plus which chunk waits on his answer and which of A / B that chunk builds if the houses named a default. Never a vote, never your own preference. No round 3 is drafted for a split — the ONLY path to a round 3 is a house's `TRIBUNAL R2: DO NOT BUILD`, which you list under `## NEEDS ROUND 3` with its text verbatim (L39: round 3 is the last).
- **Prefer the smaller mechanism** only BETWEEN texts the seats agree are correct; a disagreement about correctness is a split.
- **Every "not checkable from reads" claim → a first-gate experiment** in v3's list, placed before the chunk it gates (L70); round-2 experiments that repeat v2's X2 / X12 / X13 / X15 / X16 are merged into that row, attributed, never duplicated.
- **Invent nothing. Nothing of his before build. Dissents carried VERBATIM** (`## Dissents, verbatim` gains every round-2 answer v3 does not follow, with seat and question). **NO VALUE OF HIS** (L32): spans replaced by `[user data: <KEY>]`, counted.

WRITE (Write tool, TWO new files):
- `/Users/cobalt/cobalt/docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md` — v2, whole, with each round-2 block (`[F-09 · F-04 · F-18] … NEEDS ROUND 2 (R2-1)` in §1; `[F-06 · F-13] … NEEDS ROUND 2 (R2-2)` in §6; the `[F-06]` marker in §4; the R2 cells of §9's H2 row, `## L52`, `## What this does NOT do`, `## Open to the tribunal`) REPLACED in place by the converged wording, verbatim, tagged `[R2F-nn]` (a row of your fold table), or by an `OPEN FOR DEJAN — R2-n` block as above; the header names v2, the round-2 hub report, the raw rulings of every house that ruled round 2, your round-2 report, the fold table's path, and `ASTRA PENDING (R13)`; a `## Status after round 2` section (per item: converged / OPEN FOR DEJAN, and the chunk each gates); `## First-gate experiments (L70)` = v2's X1–X16 plus every round-2 experiment the fold takes; `## Dissents, verbatim` extended; `## L52 (a)–(d)` re-answered for v3 — (c) is MET only if both R2-1 (the JSONB field set) and R2-2 (the CHECKs, if any) are converged. Committable: no value of his.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-derive-r2-2026-09-22.md` — §0 Headline ≤5 lines (converged / open for Dejan, whose text, H1 unblocked or not, Astra pending) → `## DIGEST FOR THE DESK` (≤30 lines: per item, converged or OPEN FOR DEJAN and whose text; H1 unblocked or not and why; H2 unblocked or not and why; seats that ruled round 2; owner items; evenings to a marked name on `/radar` and to full effect UNDER v3) → `## Fold table` (`R2F-nn · question (R2-1.1, R2-1.2, R2-2.1, R2-2.2) · seats that ruled it (<n>, named) · answers (per seat) · whose wording · adopted verbatim? yes / no / not taken · why (≤25 words, citing the round-2 hub's file-check row or C<n>)`) → `## OPEN FOR DEJAN` (the blocks, one per item; empty = none) → `## NEEDS ROUND 3` (only a `DO NOT BUILD`; else `none`) → `## OWNER ITEMS (after the tribunal)` (v2's thirteen, updated, deduplicated, each with who raised it; none a precondition) → `## Redactions` → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `FLOAT HANDICAP DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch reads its own report first and continues from `## CONTINUE`; v3, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the report, L71): `FLOAT HANDICAP DERIVED v3 · converged: <n> of 2 · open for Dejan: <n> · owner items: <n> · ESCALATE: <n>` — where `converged` counts ITEMS whose every question converged and `open for Dejan` counts `OPEN FOR DEJAN` blocks — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `converged: 2 of 2` → the tribunal CLOSES, the desk commits v3 and an Opus seat drafts the H1 build prompt from it (v2's first-gate experiments for H1 run first); `open for Dejan: n` → the desk brings him ONE A/B per item, one per message, and the chunk each gates waits on his answer; either way Astra reads the FINAL on Sat 09-26 (R13).
