MODEL: Opus 5.5 (`claude-opus-5-5`, R36) — DESIGN-PROPOSAL seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: proposer `drc-design-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/38-propose-drc-automation.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-design-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `2026-09-21/70-propose-s3-exits.md`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool, the REPORT LAST · METER: Anthropic medium (the week's meter is HIGH, his R58 — read by section) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# PROPOSE (do NOT build, do NOT draft build prompts) the DRC AUTOMATION design. LADDER: `S3-P3 · F14` (+ the F15 seams it needs) — `docs/00 - Project/SPRINT-LADDER-v0_1.md` `## S3`. LAW STEP (L67): ONE house proposes → the houses rule and derive → build → ≥3 checkers → deploy. **THIS = the proposal.** DO NOT STOP until the report's last line is `DRC AUTOMATION PROPOSED …` or `FAILED …`.

WHAT HE RULED IN TODAY'S DRC SITTING, AND NOTHING MORE (you widen NOTHING) — `docs/40 - DevDocs/reports/cto-2026-09-22.md` `## §4 Rulings`:
- **R65 (15:39 ET)**: the CONTENT and PROCESS start from his trading coach's spec — `/Users/cobalt/cobalt/docs/_inflight/DRC-automation-spec-2026-09-22.md` (gitignored: it carries his dollar values and rule wording; L32 — you cite it by SECTION and KEY, never copy a value or rule sentence of his into a committed file). The current note's shape: `/Users/cobalt/Vault/Think/1 - Trading/5 - Review/DRC-2026-09-21.md`. The trade-reporter Flask app (`docs/90 - References/trade-reporter/`: `app.py`, `templates/index.html`, `utils/drc_builder.py`) is the AUTOMATION PATTERN ONLY (inputs dropped into a form → a builder → one artifact) — NOT its SMB field set. **NO PDF.** The output stays the markdown note `1 - Trading/5 - Review/DRC-<YYYY-MM-DD>.md`, from the DRC template in `/Users/cobalt/Vault/Think/5 - Templates/`.
- **R66 (15:42 ET)**: no new collectors. He drops his TWO inputs by hand, as he makes them today: the **TradeZella trade screenshots** and the **DAS trading-log export** (his own exported file). The design gives him **ONE IMPORT PLACE** for both (trade-reporter's drag-drop form is the pattern — say where it lives: the existing ASET / radar Flask app on Tailscale is the one web surface, Charter §5, L3 — a second app is a one-path violation unless you prove the need). **The DRC is CREATED ONLY WHEN BOTH INPUTS ARE PLACED** — no inputs, no DRC; today's 15:40 create-if-absent trigger (`ops/com.cobalt.prefill-drc.plist`, `src/cobalt/prefill/drc.py`) gives way to the input-driven one — say what happens to that job, the three Cobalt units it writes today, the 21:10 miss line (`src/cobalt/replay/line.py`, which never creates the note) and the 09:00 morning reader (`src/cobalt/daymode/drc.py`).
- **R60 / R58**: this is IN this week's build lane; nothing beyond the spec's MVP gets proposed as this week's build — the rest is named as later slices.
- CLAUDE.md absolute boundaries: Cobalt NEVER touches a trading platform and NEVER executes or automates a trade. Reading an export file he placed is read-only awareness; no process of Cobalt's reaches DAS or TradeZella (R66 sets aside S3-exits v3's "a fill … never read from DAS" wording for the DRC reconcile ONLY — name the seam with v3's legs, do not re-decide v3).

INDEX CARD — read in this order, nothing more until a step needs it:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding here: **L1** fail-loud (an unparseable export or screenshot = a loud failure naming the file, never a partial DRC) · **L2** watcher standard (the "inputs placed" trigger is a deterministic watcher emitting a typed event) · **L3** one path (the 15:40 writer, the morning reader, the miss line: reused or named as the copy that dies) · **L7 / L8** (nothing flips human-fed → engine-fed without shadow; every rate / tally carries its n) · **L9** (each input behind a parser interface; a changed export format = loud degraded flag) · **L10** (rules, grades, windows, limits = config with a Pydantic schema; the spec §7 is HIS config — its home is his note or `"user".trader_settings`, never committed code) · **L11** · **L28** (the DRC note is a vault write: create-if-absent, marker-bounded units, human wins, versioned) · **L29** (say which chunks are write paths) · **L32** (trades, fills, screenshots, narrative = USER data, `"user"` schema) · **L35** · **L40** · **L42** · **L45** (parsers tested against the REAL export shape — say where a real-shape fixture comes from, redacted) · **L52** (say whether anything reaches scoring / ranking) · **L53** · **L57** (every derived number stored with its inputs) · **L67** · **L70** (every "not checkable from reads" = a first-gate experiment) · **L71** · **L74**.
2. His coach's spec (above) whole — §1 inputs, §2 derived fields, §3 rule engine, §4 ledgers, §5 outputs, §8 processing order, §9 non-negotiables. The current note `DRC-2026-09-21.md` whole. `docs/30 - Design/DRC-SITTING-PACKET-2026-09-21.md` §2–§4 (what exists today; the packet's 15 questions — say which the spec answers and which stay HIS, as owner items).
3. `docs/90 - References/trade-reporter/` — `app.py` (routes, session dirs, the upload → build → download flow), `templates/index.html` (the drop form), `utils/drc_builder.py` (only how it consumes a data dict — not its fields), `README.md`.
4. THE CODE, on MAIN (Read tool under `/Users/cobalt/cobalt`), every claim with `file:line`: `src/cobalt/prefill/` (`drc.py` — the units, create-if-absent, the template copy `configs/cobalt/templates/drc.md.j2`), `src/cobalt/daymode/drc.py`, `src/cobalt/replay/line.py`, `src/cobalt/vaultwrite/` (`create_if_absent`, `upsert_unit`), `src/cobalt/aset/web.py` + `aset/store.py` (the Flask app, the ASET cards and fill updates the spec's §1.1 row 3 reads), `src/cobalt/cards/` (picks, shadow taps), the Rules source (`/Users/cobalt/Vault/Think/1 - Trading/5 - Review/Rules.md` and its parser in `src/cobalt/`), `src/cobalt/db_migrations/` (next free number; the unmerged branches' numbers: `0012` bars, `0013` setups, `0014` H1, `0015` possibly stale-score — L68 seam).
5. `docs/30 - Design/S3-EXITS-v3-2026-09-22.md` §1 (F-rows on fills / legs) and its seams to F14 — the DRC reads legs / fills from there; say where the DAS export's executions reconcile against v3's legs (estimated vs confirmed) and what wins (HE wins; a mismatch is shown, never silently fixed).
6. `docs/00 - Project/MVP-CHARTER-v0_2.md` F14 (`:160-171`), F15; the ladder's S3 rows + S3 smoke.

WRITE (Write tool, exactly three files, the report LAST):
- `/Users/cobalt/cobalt/docs/30 - Design/DRC-AUTOMATION-PROPOSAL-2026-09-22.md`, ≤350 lines, KEYS ONLY (no value, rule sentence, ticker or price of his): (1) FACT BASE `claim · file:line · PROVEN | UNPROVEN`; (2) THE IMPORT PLACE — where he drops the two inputs, what it accepts, how a day is bound to its files, what it shows back (parsed count, failures by file); (3) THE TRIGGER — both inputs placed → a typed event → the DRC build; what happens when only one is placed; re-import / correction; (4) PARSERS — DAS export (executions → trades → legs FIFO, stop-order history if the export carries it — say UNPROVEN until a real-shape sample is read), TradeZella screenshot (what is read from the image and by what — OCR in Cobalt is a NEW dependency: name it, or propose his typed / voice confirmation of the few fields instead; mark the choice an OWNER item), ASET cards, daily-note premarket block, Rules; each with its fail-loud rule; (5) DERIVED FIELDS and the RULE ENGINE — the spec §2 / §3 mapped to what Cobalt already computes vs what is new; rules read as data from `Rules.md`; (6) THE NOTE — which sections Cobalt writes (as marker-bounded units) vs his (never touched), mapped onto `DRC-2026-09-21.md`'s headings; the voice / narrative part (the spec §1.2) and where he gives it (today: his DRC chat) — propose, mark OWNER; (7) LEDGERS (§4) — which this slice builds, which later; (8) what dies (the 15:40 job's create step) and what is reused; (9) CHUNKS for THIS WEEK's MVP + later slices, each chunk: files, write path yes/no, migration yes/no, RESTARTS, hours; (10) first-gate EXPERIMENTS (L70) — a real DAS export's shape, a real screenshot's legibility, the template read; (11) OWNER ITEMS — every choice that is his; (12) L52 answer.
- `/Users/cobalt/cobalt/docs/_inflight/drc-automation-values-2026-09-22.md` (gitignored — prove it with `ls` of the `.gitignore` rule via `grep -n "_inflight" /Users/cobalt/cobalt/.gitignore`): ONLY IF your proposal needs to show his config values mapped to keys; otherwise write the file with one line `no values needed`.
- Your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-design-2026-09-22.md`: §0 Headline ≤5 lines → the UNPROVEN rows → `READING:` list → `## ESCALATE` → `## CONTINUE` → **`## DIGEST FOR THE DESK` ≤40 lines** (plain words: what he gets, what he drops where, when the DRC appears, what exists already, chunks + hours for this week, what is HIS, what the tribunal must settle). While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line of the report): `DRC AUTOMATION PROPOSED · chunks this week: <n> · later slices: <n> · write-path chunks: <n> · migrations: <n> · import place: <where> · new dependency: <none|name> · reaches scoring: <yes|no> · build estimate: <n> h · open to the tribunal: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write. Stop within 45 minutes.
