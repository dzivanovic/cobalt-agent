DERIVE ROW: R109

MODEL: `claude-opus-5-5` — the DERIVE SEAT his row names (`cto-2026-09-22.md` §4 R109, 19:34 ET, his words "Make all Opus 5.5 for now" — standing until his next word; for the tribunal of `ROUTING-PROPOSAL-2026-09-22.md`: `derive seat: claude-opus-5-5`; R109 answers R76's per-seat ask for every Fable-type seat, so this file was drafted FILLED — no placeholder, no desk fill). This is the Anthropic house's SECOND JOB on this tribunal, as on the bars, setups, float-handicap, stale-score, S3-exits, DRC and voice tribunals; you derive the version the round-1 rulings produce; you build, launch and commit nothing, and you write NO law: the text you derive is a proposed FINAL that HE rules (R4: "Laws are all mine"), and the desk folds LAWS.md only after his ruling (L58) · SEAT: `routing-tribunal-derive-0922`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `63`; the Anthropic seat `64`). YOUR POSITION, STATED: the Anthropic house proposed this design (Opus 5.5, `routing-propose-0922`) AND ruled round 1 through the Anthropic seat (`64`, on `claude-opus-5-5` by R109); you are that house in a fresh session, deriving a law that seats your own house — you HOLD A SIDE twice over, so you recommend nothing to him (L37), the Anthropic seat's round-1 wordings pass the same filter as every house's, and an UNMEASURED self-assignment of ANY house, yours first, is never folded as a rule (L26). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/65-routing-tribunal-derive.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control routing-tribunal-derive-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `74`, `75`, `37`, `42`, `61`, 09-20 R13 — nothing new) · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write, NO edit of LAWS.md or LAWS-HISTORY.md (L58) — you write exactly TWO files with the Write tool · METER: Anthropic, on `claude-opus-5-5` (his R74: the week refreshed full 09-22) — honest estimate: the hub report `63` ≈ 25–35k tokens (20 items, a self-assignment table), the raw rulings of the houses that ruled ≈ 25–35k (20 items each, SELF lines), the Anthropic seat's round-1 report ≈ 12–16k, the proposal ≈ 6.2k (read whole — v2 is the proposal amended), its author's report ≈ 1k, the rulings R4 / R13 / R36 / R46 / R76 / R81 / R109 + R30 / R32 / R49 / R73 / R74 ≈ 3.5k, LAWS.md ≈ 20.5k (82,174 B measured at drafting), LAWS-HISTORY H-L27 / H-L29a–e ≈ 1k, files re-opened only where a fold turns on it ≈ 5–10k; v2 ≈ 10–14k tokens of output (the frozen entries old beside new, the seat table, the trace, the folds) plus the report ≈ 6–9k; peak context ≈ 120–150k — read the raw rulings item by item, not all at once, if the peak nears your window. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v2 of THE ROUTING LAWS from the proposal and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: `OFF-LADDER — ruled cto-2026-09-22.md R81`. LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini via hub `63`; Astra `METER — proceed on three`, R13; the Anthropic seat via `64`) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (≤3 rounds, L17 / L39; after round 3 an unresolved item is an OPEN item for him — a law file is never voted) → Astra reads v2 on Sat 09-26 (R13) → HIS ruling of the law text, entry by entry (R4) → the desk folds LAWS.md and LAWS-HISTORY.md (L58). L1, L3, L10, L17 / L39, L23, L25, L26, L27's ceiling, L29's write-path floor, L31, L47, L49 and L67 are the bar v2 must meet. DO NOT STOP until the report's last line is `ROUTING DERIVED v2 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The first line of this file names the row of `cto-2026-09-22.md` `## §4 Rulings 2026-09-22` that carries his answer on the derive seat for THIS tribunal — R109, filled at drafting from his row (L19: any other change is a re-issue). The row must carry, as literal text: `ROUTING-PROPOSAL-2026-09-22.md` and `derive seat: claude-opus-5-5`. Each its own Bash call: `grep -c -E "__DERIVE_(ROW|MODEL)__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/65-routing-tribunal-derive.md"` (no fill span was ever written into this file; this sentence's own text does not match the pattern) — **anything but 0** → `FAILED: authorization — a placeholder is in this file; it was drafted filled (R109)`, stop · `grep -c -x -E "DERIVE ROW: R[0-9]+" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/65-routing-tribunal-derive.md"` — must print **1**; read `<nn>` from line 1 (`109`) · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (exactly ONE `R<nn>` row carrying BOTH literals above, with his words in quotes — a DESK row with "NO WORDS OF HIS" does not count; any literal missing, or two such rows → `FAILED: authorization mismatch — row R<nn> does not name the derive seat for this tribunal`; a missing `cto-2026-09-23.md` is recorded, not fatal) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md" "docs/40 - DevDocs/reports/cto-2026-09-23.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **HIS LATER WORD WINS:** `grep -n -e "derive seat:" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` — a row NEWER than R<nn> (above it in the table, or in the later file) that names a different derive seat for `ROUTING-PROPOSAL-2026-09-22.md`, or that ends R109's "for now" → `FAILED: authorization — a later row of his (<row>) changes the derive seat; the desk re-issues this file`, stop · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` in that row, the `--model` value on this file's launch line, and the model id your own system prompt states you run as must be the SAME id — else `FAILED: seat mismatch — row R<nn> names <id>; this file / this session runs <id>`, stop. **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/routing-tribunal-2026-09-22.md"` (the LAST NON-BLANK line must start `ROUTING TRIBUNAL R1 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"ROUTING TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/routing-tribunal-2026-09-22.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/routing-tribunal-fable-r1-2026-09-22.md"` (must start `ROUTING TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Fable round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"ROUTING TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/routing-tribunal-fable-r1-2026-09-22.md"` (EMPTY → `FAILED: refused — the Fable round-1 stop line is not committed on main`) · **A HOUSE MUST HAVE RULED:** the hub's stop line shows `houses that ruled: 0 of 3`, or neither `grok:` nor `gemini:` in it carries a `TRIBUNAL R1:` line → `FAILED: no house ruled round 1 — the desk relaunches 63 or brings it to him` (the Anthropic seat alone never settles an item: you would be deriving a law that seats your house from your own house's ruling, L37 / L26) · **ASTRA (R13):** the hub's `astra:` field is `METER — proceed on three` or `SKIPPED — R13 (probe UP, recorded)` — RECORDED, not a refusal: derive from the seats that ruled, mark every question only Astra would have answered `ASTRA PENDING (R13)`, mark v2's header `ASTRA PENDING (R13)`, and name in §0 that Astra reads this v2 when its meter returns (Sat 09-26) — and that the OpenAI house has NOT ruled on a routing design that assigns its own seats (seat-map rows 9–12), so the desk brings Astra's read to him BEFORE his ruling. Gemini or Grok alone not having ruled does NOT refuse the derive: the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). THE SUBJECT: **L5 · L21–L27 · L29 · L49** (FROZEN, R81 line of `How to read this file`). Binding: **L1 · L3** (ONE seat table; ONE model-access module) **· L10** (config-as-code, dry-run) **· L17 / L39** (≤3 rounds, no fourth; A LAW FILE IS NEVER VOTED — unresolved → an OPEN item for Dejan) **· L23 · L25** (cloud fallback is an exception handler, never a route) **· L26** (evidence-cited routing; the conflict-of-interest clause) **· L27** (the budget ceiling — NOT OPEN) **· L29** (the write-path floor; never auto mode on a write path; the scratch-tested gate) **· L31** (no vendor or model name in law text) **· L32** (v2 and your report are COMMITTED: no user data) **· L35 · L36 · L37** (you derive; a SPLIT between houses is carried, never settled by vote; a seat that holds a side recommends nothing to him — you hold one) **· L44 · L47** (the meter is a precondition, not the router) **· L49 · L53** (every n, threshold and count is his) **· L58** (you write no law file; the desk folds after his ruling) **· L62 / L63** (and L63's state note) **· L67 · L70 · L71 · L72 · L73 · L74**. CLAUDE.md's absolute boundary: nothing in v2 reads, writes or infers from his trading platform.
2. THE ROUND-1 RULINGS, whole: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/routing-tribunal-2026-09-22.md` — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Self-assignments (L26)` (your filter for every SELF row), `## Fable round-1 claims, file-checked` (the FC rows, or its one line saying the Anthropic seat's report was not done at collate), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/routing-tribunal/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line (the raw file is what "verbatim" means, but you never copy a user-data span out of it) · the Anthropic seat's round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/routing-tribunal-fable-r1-2026-09-22.md` — including its `## Self-attack` (its `WITHDRAWN:` lines bind you: a withdrawn sentence is never folded) and its `## SELF-ASSIGNMENTS`.
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/ROUTING-PROPOSAL-2026-09-22.md` in full; its author's report `reports/routing-propose-2026-09-22.md` (whole); `cto-2026-09-22.md` §4 rows **R4, R13, R36, R76, R81, R109** and `cto-2026-09-21.md` **R46** (`grep -n "^| R4 "` etc.) — R81 (the tribunal runs this week; the cluster FROZEN until it rules), R4 ("Laws are all mine"), R13 (Astra reads the FINAL Sat 09-26), R36 / R76 / R109 (today's Anthropic seats, his per-case rulings), R46 (seats by kind of work) — and, as the proposal cites them, **R30, R32, R73, R74** and 09-21 **R49**; `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS-HISTORY.md` :27–32 (H-L27) and :48–72 (H-L29a–e).
4. Only where a fold turns on it: the real file the hub's file-check cites (the ledger, `MODELS.md`, `areas/cobalt-houses.md` seat lines, `seat_usage.yaml`, `seat-usage.md`, the prompts' launch lines, the laws audit) — read to check, never to copy.

THE RULES OF THE DERIVE — `61`'s rules (the bars tribunal's lesson, `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding, with this tribunal's substitutions:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into v2 word for word, or it is not taken. You may choose BETWEEN seats' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check, or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **An UNMEASURED self-assignment is NEVER folded as a rule (L26).** A wording that assigns a seat, a role, a task class or a share of work to the house that wrote it, and whose row in the hub's `## Self-assignments (L26)` ends `UNMEASURED`, `DOES NOT HOLD` or `UNMARKED SELF`, does not enter v2's law text or its seat table as a standing assignment: it enters v2's `## Re-test rows` (the proposal's §5 shape — candidate, incumbent, class, the trial that would measure it, the n the law leaves to him) and the fold table says `SELF UNMEASURED — re-test row R-nn`. A ruling of his cited as evidence is `RULED`, not measured: the row stands in the seat table as RULED (the proposal's own MEASURED / RULED split), never as a law sentence. This binds the Anthropic house's rows FIRST: the proposal's §2 rows 1–8 are your house's.
- **A Fable-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's `## Fable round-1 claims, file-checked` has its FC rows, they are your filter for the Anthropic seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the Anthropic seat's report as not done at collate, every one of its claims is UNCHECKED: you may take its wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure, and where you cannot settle it by reading it is an experiment, not a fold. Where the Anthropic seat's wording competes with a house's wording that is equally correct, the house's is taken — your own house's text is never preferred.
- **His rulings are NEVER re-opened.** R81 (this lane, the freeze), R4 (the FINAL is his to rule), R13 (Astra reads it Sat), R46 / R49 (checker seats by kind of work — L67 as amended), today's per-case seats (R13, R32, R36, R73, R76, R109) and L27's budget ceiling (no top-ups, ever): a wording that would change one of them is NOT folded — it is carried in `## Dissents, verbatim` and named in `## ESCALATE` as `RE-OPENS A RULING (<R-row>)`, his to rule again if he chooses, never yours. A wording that changes HOW a clause is written, or names that a clause's SOURCE does not hold (a table that does not exist, a measurement never taken), IS a fold; v2 states the source the rulings leave it, or `source pending <X n>`.
- **D1–D3 are his.** The proposal's §7 items and every item a seat names as his stay OWNER ITEMS: v2 keeps §7 as the proposal wrote it (the proposing house's "Recommend" lines stay attributed to that house — the Anthropic house, L26 — and are never restated as the tribunal's), and a seat that RULED one of them is carried in `## ESCALATE` as `RULES AN OWNER ITEM (<D-n>)`, never folded.
- **A law file is never voted (L39).** Where the seats that ruled give DIFFERENT wordings for one clause and each wording's claims HOLD, v2 carries the wording that changes the frozen text LEAST (the proposal's, when a seat ADOPTED it) and the other goes to `## FOR DEJAN` as that clause's A/B — never settled by a count of seats. Where two seats disagree on whether a clause is CORRECT (one REJECTs with a claim that HOLDS, one ADOPTs), the clause goes to `## NEEDS ROUND 2` — and after round 3, to him as OPEN.
- **Nothing dropped silently.** Every clause of the frozen text (L5, L21–L27, L29, L49 — L29 by its clause list in the proposal's §1.5 trace, .6 "never auto mode on a write path" BY NAME) appears in v2's `## Clause trace` with where it goes — kept, reworded (which v2 entry), moved (which law carries it, cited), superseded (by which law), or DROPPED (named, with the seat that dropped it and the hub's check). A rewrite a seat ADOPTED that the hub's check found drops an obligation is not folded until a seat's wording restores it or it is carried to him as OPEN.
- **Push every "not checkable from reads" claim into an experiment or a trial** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in v2's experiment list beside the proposal's §5 shadow build and T11's trials, with the result that would change the law — never an argument in v2's text, never a defect.
- **Invent nothing.** v2 adds no clause, number, n, threshold, key, table column, task class, seat row, model id or trial that neither the proposal nor a seat's ruling contains. A gap no ruling fills stays a named gap (an experiment, an owner item, or a round-2 item) — never your filler.
- **Nothing of his decided here.** Every n, threshold and count the law needs, D1–D3, and every choice the seats name as his is HIS, read by him AFTER the tribunal (R4); a seat's proposed VALUE is recorded as `<what>: value proposed by <seat> — <file>:<line>` (a pointer, never adopted) and listed under OWNER ITEMS; a seat that wrote one as a precondition is carried as an OWNER ITEM and named in `## ESCALATE`.
- **Model ids out of law text (L31).** v2's law text names houses and seats by ROLE; model ids live in the seat table only — exactly where the proposal puts them, unless a seat's verbatim wording that the rulings leave standing does otherwise (then it is folded as written and named in `## ESCALATE` against L31).
- **The proposer's ESCALATE 1** (L25 points at a bake-off table that `docs/10 - Decisions/` does not contain): v2 places it exactly as the rulings decide it — a fold of L25's pointer, an owner item, or a desk record item; never an edit of an ADR.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT ADOPT` that v2 does not follow goes into v2's `## Dissents, verbatim` word for word, with its house and item.
- **No user data in either file** (L32): the packet had none; if a seat's text quotes a value of his anyway, it is taken with that span replaced by `[user data: <file:line>]`; count every redaction.

WRITE (Write tool, TWO new files, these names WHATEVER the date of the run):
- `/Users/cobalt/cobalt/docs/30 - Design/ROUTING-v2-2026-09-22.md` — a header naming the proposal, R81 / R4 / R13 / R109 and the derive row, the rulings of every seat that ruled (paths; Astra `METER — ASTRA PENDING (R13)`; the Anthropic seat's status from row R109), the fold table's path, and the line `PROPOSED LAW TEXT — NOT LAW until Dejan rules it (R4); the desk folds LAWS.md after his ruling (L58).` → `## Proposed law text, entry by entry` — for EACH frozen entry in LAWS.md order (L5, L21, L22, L23, L24, L25, L26, L27, L29, L49): the CURRENT text verbatim from LAWS.md (headed `Current (LAWS.md:<lines>)`) and, beside it, the PROPOSED text (headed `Proposed v2`) with each amended sentence tagged `[F-nn]`, or `Proposed v2: unchanged` — L27's budget ceiling sentences unchanged by rule; L27's routing sentence and D1 / D3's clauses shown as `OPEN FOR DEJAN (D-n)` with the proposal's options, never resolved by you → `## The seat table` (its form and home as T4's rulings leave it; its columns as the rulings leave them; its first rows = the proposal's §2 seat map as the hub's fact-check leaves it, each row MEASURED / RULED / UNMEASURED and citing its evidence) → `## Assignment key and re-test` (§4 and §5 as the rulings leave them; every n `his — OWNER ITEM`) → `## Re-test rows` (every UNMEASURED self-assignment, by house) → `## First trials and experiments (L70)` (T11's trials, T6's retrospective validation, the §5 shadow build and every X a seat named that the fold takes, each with who runs it, on what, and the result that would change the law) → `## Clause trace` (every clause of the frozen text → where it goes; nothing dropped silently) → `## Dissents, verbatim` → `## OPEN for Dejan (L39)` (every split on law text, D1–D3, every owner item the law turns on). Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/routing-tribunal-derive-2026-09-22.md` — §0 Headline ≤5 lines (folds, round 2 or not, owner items, self-assignments turned into re-test rows, Astra pending) → `## DIGEST FOR THE DESK` (≤40 lines: what v2 changed from the proposal, entry by entry in one line each; which seats ruled round 1 (in full / not / Astra METER); how many Anthropic-seat claims were hub-checked, how many UNCHECKED; how many self-assignments per house were folded as MEASURED, kept as RULED, or turned into re-test rows; what goes to round 2 and why; the owner items in one line each; the first trials under v2 and who runs them) → `## Fold table` (`F-nn · item (T1–T12, (a)–(h), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (seat, or "proposal kept") · adopted verbatim? yes / no / not taken · SELF (yes / no; MEASURED / RULED / UNMEASURED) · why (≤25 words, citing the hub's file-check row — `Checked` row, `Self-assignments` row or `FC<n>` — where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`, or `SELF UNMEASURED — re-test row R-nn`, or `RE-OPENS A RULING (<R-row>)`, or `RULES AN OWNER ITEM (<D-n>)`)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT ADOPT`, or two seats disagree on whether a clause is CORRECT; each with both positions quoted and the question round 2 must answer; EMPTY = the tribunal closes and v2 goes to him after Astra's read) → `## OWNER ITEMS (after the tribunal)` (deduplicated, each with who raised it — the proposal's D1–D3 first; none decided here) → `## FOR DEJAN` (each owner item and each split on law text in HIS terms — which model would sit which seat he watches (the desk, a build, a deploy, a check, the day-open), what a spent meter would do on a given morning, what a production write would run on — as ONE A/B question per item: A = what v2 carries by default, B = the alternative a seat named, each with the seats that hold it; NO recommendation — the Anthropic house proposed and ruled round 1 and holds a side (L37); ≤4 lines per item) → `## ASTRA READ OWED` (what the OpenAI house must read on Sat 09-26 before his ruling: every item marked `ASTRA PENDING (R13)`, every seat-table row naming an OpenAI seat (the proposal's §2 rows 9–12 and any a fold added), every re-test row that would move a seat into or out of the OpenAI house, and D2 — L27's routing sentence, which names that house's role) → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` (every `RE-OPENS A RULING`, every `RULES AN OWNER ITEM`, every UNMEASURED self-assignment a seat still presses as law, every DOES-NOT-HOLD wording a seat still presses, every clause the trace finds dropped, every L31 breach in a folded wording, the ASTRA line above, every `ASK DESK`) → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `ROUTING DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both file paths, reads its own report first and continues from `## CONTINUE`; v2, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the derive report, L71): `ROUTING DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits v2 as the FINAL's input, Astra reads it on Sat 09-26 (R13; its read goes to him with v2), and the desk brings him `## FOR DEJAN` one item per message and the law text entry by entry (R4) — the desk folds LAWS.md and LAWS-HISTORY.md only from his ruling (L58); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a run after 2026-09-23 23:59 ET needs his word for `grok` / `agy` for this tribunal — R30 ends then, and R105's extension is scoped to the DRC check hubs `53`–`57`).
