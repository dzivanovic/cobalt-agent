MODEL: Opus 5.5 (`claude-opus-5-5`, R32 / R36) — D3 is the DRC note writer (vault units) and the `drc_rows` writer, and retires a scheduled job (L29's list), so the floor is Opus. `--permission-mode auto`: the DEV LANE as `33` (L62 as amended R80, stated here and on the line) — offline build; every note written in a `tmp_path` vault; DB only inside the with-DB suite's rollback on `cobalt_dev`; no production command, no write to the real or the dev vault, no `launchctl` (the job's bootout is the DEPLOY's step, not yours). NOT `acceptEdits` (L63). Unlisted command = do not call it; files via Write / Edit · SEAT: DRC D3 builder `drc-d3-build`, launched by the CTO desk in the background. The desk FIRST runs `git -C /Users/cobalt/cobalt worktree add -b drc/d3-build /Users/cobalt/cobalt-wt/drc-d3 <BASE TIP>`, fills the BASE TIP and launch row **R__**, commits both, then: `cd /Users/cobalt/cobalt-wt/drc-d3` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/50-drc-d3-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-d3-build --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/drc-d3/.env)" "Bash(rm /Users/cobalt/cobalt-wt/drc-d3/.env)" "Bash(git rm configs/cobalt/templates/drc.md.j2)" "Bash(git rm ops/com.cobalt.prefill-drc.plist)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: `33`'s seventeen shared + denies + triplet BYTE FOR BYTE; FOUR NEW: the two `.env` strings (re-pointed to `drc-d3`) and the two exact-path `git rm` strings (v2 §8: the repo template dies; the plist leaves `ops/`) · SESSION: fresh; relaunch = same line, resume from `## CONTINUE` · never `bypassPermissions`; push DENIED; no `COBALT_ENV=production`, no `launchctl`, no `cobalt drc build` typed (the CLI is proven inside pytest), no vault write outside `tmp_path`, no memory-folder write, no `claude` · `.env` by name, removed at every stop · METER: Anthropic HIGH (≈10 seat hours: v2's 9 + R93's no-trade path + R102's PnL unit − R101's engine) · nobody sits at this terminal: `ASK DESK: <q> [<time>]` under `## ESCALATE`, safe default, continue.

# DRC D3 — THE BUILD AND THE NOTE, FROM HIS TEMPLATE; THE NO-TRADE DRC; THE 21:10 MISS LINE; THE 15:40 JOB RETIRED. Branch `drc/d3-build`, worktree `/Users/cobalt/cobalt-wt/drc-d3`, from BASE TIP **`__`** (the D2 build's stop-line tip — or its fix round's — `49-drc-d2-build.md`; stack D4 → D1 → D2 → D3). D2 + D3 (with D4 and D1 below them) land in ONE deploy. DO NOT STOP until the last line is `DRC D3 BUILT …` or `FAILED …`. NEVER END A TURN BETWEEN STEPS.

RE-ISSUED 2026-09-22 (L19, whole file) by the Opus 5.5 prompt seat `drc-reissue-draft-0922` for his R114 (E1 gate by HEADER; several playbooks per trade, all rendered) and R116 / R117 (playbook → setup = one-to-one names, D3-2b). The launch line above is unchanged byte for byte.

WHY: v2 §6 (the note), §5 `[F-19]` (stored inputs), §8 (what dies), T3 `[F-24]`, T6 `[F-21]`, T8 `[F-20]`, T10 `[F-25]`, (b) `[F-22]`, `[F-23]`, `[F-28]`, §13 as ruled (R69–R72), as HIS rulings reshaped them:
- **R93** — a DRC on EVERY market trading day; a `no_trade` event (D2) builds a DRC from his template with a Cobalt `drc-day/no_trade` unit (date, "no trades", cards written that day, from `aset_sizings`) and HIS blank unit `drc-day/voice-no-trades` under a Cobalt heading `### Why no trades today:` placed directly under the summary — created ONCE, never re-upserted (T6's create-once).
- **R102** (O16) — Cobalt FILLS `### PnL on the day:` from the trading log: a unit `drc-risk/pnl` under that heading. (O11) — the premarket block reads ONLY `Sleep:` / `Readiness:` / `RHR:` / `1% goal:` from the daily note; meditated / tone / hotkey render `not given`.
- **R103** (O21) — the summary line shows BOTH gross and net P&L. (O17) Postgres only. (O18) the S2 smoke no longer requires "exists at 15:41".
- **R99** (O9) — his per-trade answer lines sit in their OWN unit `drc-trades/voice-<trade_id>` NEXT TO that trade's Cobalt block; a re-drop never rewrites them (create-once via `upsert_unit(..., skip_if=…)`, `writer.py:652` — re-locate).
- **R98** (O8) — the example trade blocks and example If/Then in his template copy into every DRC; nothing strips them.
- **R101** (O7) — NO rule engine, NO `drc-rules/rule_engine` unit, NO checker, NO rules line in the summary: rule adherence stays with his DRC coach session; the DRC hands that session complete data. `drc-rules/rules_check` (his checkbox scaffold, existing) STAYS. Every §13 row whose source is "engine" renders nothing this slice (A21's engine half, A24's rules half, B33, C11) — named in the report, never faked.
- **R91** — planned R, target, assumed / realized R:R, MAE / MFE / best exit come from the stats log (D1) or render `not given`; Cobalt's realized R stays `not computed` until D5 (`[F-19]`: never `confirmed` before the reconcile writes legs).
- **R94** — one bucket; no account line.
- **R114** — a trade may carry SEVERAL playbook names (D1 keeps them all, in order); the DRC renders every one, in that order; nothing picks one.
- **R116 / R117** — ONE-TO-ONE NAMES: "we can just have one to one naming convention" / "Assume all wilde named to match" → a stats-log playbook name, after stripping ONE trailing ` Long` / ` Short` (case-insensitive, whole word), that EQUALS a note title in `1 - Trading/4 - Strategies/` IS that note's setup; anything else renders `unmapped: <name>` and is counted — never guessed, never a failure. No mapping table, no fuzzy match, no LLM (L2 / L57). The titles are read at BUILD TIME from the vault folder, never a committed list (L32 / L45). D3-2b.
- **R90 / R67** — the reconcile unit shows the DIFF only; `adjustment pending (legs writer not built)` until D5; D5 adds the `unresolved: card <id>` lines and the RESOLVE action.

## THE ROWS — in this order, nothing else
| row | what | files |
|---|---|---|
| **D3-1** HIS TEMPLATE (`[F-20]`, E3) | The build reads HIS `5 - Templates/DRC.md` (path relative to the vault root, from the prefill paths config — add ONE key beside the existing DRC path in the file `load_prefill_paths()` reads; config boundary: `configs/dev/` / `configs/cobalt/` only) and replaces ONLY the literal `{{date:YYYY-MM-DD}}`; any other `{{` FAILs naming the line; no Jinja, no Templater on his file. FIXTURE (L45 / L32): `tests/fixtures/drc/template_shape.md` — his template's HEADING lines (`#`-prefixed) and token lines byte-identical, IN ORDER, every other line replaced by a neutral placeholder of your words, the same line count per section (a stripped real-shape copy; his prose and example trade values never enter git). Prove the shape once: Read his real template and list (in the report) the count of headings and `{{` lines in both — equal, or `FAILED: D3-1 — fixture shape ≠ his template`. | `src/cobalt/drc/template.py` (new), the prefill paths config file, `tests/fixtures/drc/template_shape.md` |
| **D3-2** THE BUILD (`run_drc_build(event)`) | First check: the event is `trades` with both files `parsed`, or `no_trade` (D2) — anything else FAILs and writes nothing. `create_if_absent` from D3-1's text at the path `load_prefill_paths()` names (same as `drc_note_path`), then ALWAYS upsert every unit (never the `action == "created"` early return, `prefill/drc.py` — re-locate). Units, each its placement under HIS heading (v2 §6 table): `drc-summary/summary` directly under the date line (gross P&L AND net P&L, W/L, trades, open positions, cards written / taken, screenshots bound / trades, `unmapped playbooks: <n>` from D3-2b — no rules line, R101) · `drc-day/premarket` under the summary (the four keys, R102; `not given` for the rest; A25) · `drc-day/no_trade` + `drc-day/voice-no-trades` (R93, no-trade days only) · `drc-risk/pnl` under `### PnL on the day:` (gross + net, R102 O16) · `drc-risk/facts` above his risk paragraph (planned vs actual risk per trade, overrun flags, distance to the daily stop through D4's `daily_risk_values` — `not given` when unset; A9 without the engine half) · `drc-risk/risk_parameters` (existing) · `drc-trades/tickers` REBUILT: one block per trade (B-rows the data can fill: B1, B2, B4, B5, B6, B13 embed `![[1 - Trading/5 - Review/_imports/drc/<date>/<file>]]` when a screenshot is bound, B19–B24, B25 planned R from the stats log + realized R `not computed`, B26 from the stats log or `not given`, B27 from D4's `windows.*` or `not given`, B28, B29, B34's flags that need no engine, and ONE `playbooks:` line — every name of the trade in D1's order, each rendered `<name> → <setup id>` or `unmapped: <name>` by D3-2b, `playbooks: not given` when the trade has none) and `drc-trades/voice-<trade_id>` NEXT TO it, create-once (R99) · `drc-trades/reconcile` (the diff of each trade vs its card's legs when v3's `legs_current_v` exists, else `legs: not built`; completion text `adjustment pending (legs writer not built)`) · `drc-trades/open_positions` (R67: symbol, side, held shares, trade_id, day opened; a carried trade reads `continuing open position`) · `drc-rules/rules_check` (existing scaffold; its card reconcile now "cards with no trade") · `drc-misses/miss_line` via D3-3. Card match: `AsetStore.for_date`, nearest prior card, same ticker + direction, within `limits.card_match_window_minutes` (D4) — unset → every trade `card: not matched (window not given)`, loud on the page, never a guessed window. `[F-23]`: no unit ever writes a line beginning `Grade:` or `Goal:`. Every derived number stored in `drc_rows` with `inputs` (the card snapshot `[F-19]` lists, the stats row, the window bounds) + `derived` + `fn_version` (L57); the note renders ONLY what the row holds. A vanished trade on a re-build: its derived block goes; its voice unit stays with ONE Cobalt line `orphaned` above it (E8). | `src/cobalt/drc/build.py` (new), `src/cobalt/drc/units.py` (new), `src/cobalt/prefill/drc.py` (reused helpers kept; `format_tickers_block`, `parse_fill_updates`, `find_trade_note_for_card` leave the DRC path; the create step removed; `run_drc_prefill` → the build) |
| **D3-2b** PLAYBOOK → SETUP, ONE-TO-ONE (R116 / R117) | `resolve_playbooks(names: list[str], vault_root) -> list[PlaybookResolution]`, called by the build, deterministic, no LLM, no table (L2): (1) READ AT BUILD TIME the note titles of the vault's strategies folder — the folder constant REUSED from `src/cobalt/taxonomy/vault_loader.py` (`STRATEGIES_DIR`, never a second path literal, L3), top-level `*.md` files only, title = the file name without `.md`; (2) per name, in D1's order: strip ONE trailing ` Long` or ` Short` — case-insensitive, a WHOLE word (preceded by one space, ending the name; `X Long Long` strips once; `Longhorn` never) — never anything else (no trimming of other words, no plural, no punctuation folding); (3) the stripped name EQUALS a title (exact string equality — see ESCALATE) → that note's setup id = its frontmatter `trade_def:` read with the ONE frontmatter parser (`split_frontmatter`, `src/cobalt/vaultwrite/frontmatter.py`) and validated by `validate_slug` (`src/cobalt/taxonomy/slug.py` — the slug IS the id, ADR-0008 D3); (4) no equal title → `unmapped: <name>`; a matched note whose slug is missing or fails `validate_slug` → `unmapped: <name> — note <title>: <SlugError text>`, loud; both counted, NEITHER a build failure. An unreadable strategies folder (absent, not a directory) → the build does NOT fail: every name renders `unmapped: <name> — strategies folder not readable` and the summary says so (L1: loud, never plausible). Stored (L57): the per-trade `drc_rows` row's `inputs` carries the names in order and the title → slug list the build read; `derived` the resolutions; `fn_version`. The summary's `unmapped playbooks: <n>` counts the unmapped NAMES of the day. | `src/cobalt/drc/playbooks.py` (new), `src/cobalt/drc/units.py`, `src/cobalt/drc/build.py` |
| **D3-3** 21:10 MISS LINE (`[F-24]`, E6) | Note absent at 21:10 → `line_step` does not raise and creates nothing: it stores the exact `render_line` arguments on the replay run (`card_rows`, `mover_rows`, `settings`, `formation_replay`, `input_stale`, `formation_rows`, `formation_suppressed`, `formation_input_stale` — re-read the runner), records `line: pending (no DRC)`, ends green. The build, after `drc-rules` exists, is the ONLY later caller of `render_line` + `write_miss_line` for that stored blob; blob missing → `miss line: pending (replay inputs not stored)`. Note present at 21:10 → as today; the build writes no second line. **E6**: re-render from the stored blob == the in-memory render, byte for byte (fail → `FAILED: D3-3 — E6 — <diff>`: T3 does not hold as written). | `src/cobalt/replay/runner.py`, `src/cobalt/replay/line.py` (only the absent-note branch), the replay run's storage (its existing columns / JSON — no migration; a needed column → `FAILED: D3-3 — the blob needs a migration`, the desk numbers one) |
| **D3-4** ONE CLI | `cobalt drc build --date D [--dry-run] [--no-trades]` in `src/cobalt/cli.py` calls the SAME function; `--dry-run` writes nothing and prints the units + the `drc_rows` it would write; `--no-trades` goes through D2's no-trade function (its refusals). `prefill drc` retired: `prefill/cli.py`'s `drc` subparser + `_run_drc` + `DRC_JOB` (F39) removed. | `src/cobalt/cli.py`, `src/cobalt/drc/cli.py` (new), `src/cobalt/prefill/cli.py` |
| **D3-5** RETIREMENT (`[F-22]`, `[F-28]`) | `git rm ops/com.cobalt.prefill-drc.plist` · `git rm configs/cobalt/templates/drc.md.j2` · `configs/cobalt/jobs.yaml`: the prefill-drc rows (`:181-186` — re-locate) removed, the `:310` reader label removed, and `configs/cobalt/rules.yaml` LEAVES `no_resident_reads` for `com.cobalt.aset`'s `reads:` (the build calls `regenerate_rules_config()` inside the resident, F41) · `ops/README.md` (F40: the 15:40 job lines) · every test that pinned the 15:40 path or the repo template is re-pointed to the build with the SAME strength or removed WITH the code it tested (each listed `test · why`), never skipped. | as named |
| **D3-6** S2 SMOKE (`[F-25]`, R103 O18) | `smoke/s2.yaml`: the `com.cobalt.prefill-drc` label rows (`:436-438`) REMOVED; the DRC unit rows (`:383-398`) → `pending` when no DRC event exists for the date, FAIL when the event is `failed` or `done` without the note and its `drc-misses` markers, PASS when `done` and both present; nothing requires 15:41. A new check kind in `smoke/checks.py` ONLY if the conditional needs one (say which). | `configs/cobalt/smoke/s2.yaml`, `src/cobalt/smoke/checks.py` (if needed) |
| **D3-7** EXPERIMENTS AS TESTS | **E3** on `template_shape.md` in `tmp_path`: every unit under its heading, no other `{{`, every non-unit line byte-identical to the fixture after the build (twice — idempotent). **E8**: two builds, a trade removed, text in its voice unit → the unit still there with `orphaned`, the derived block gone. **E11** (real build): an exception injected after `pending` → `failed`, page not `DRC built`. **X10** (display): the dataview line is his template's; record it as NOT CHECKABLE without Obsidian (an owner item if it lists D+1's notes). **E9** (embed renders) is display-only and not run (Obsidian). | tests |
| **D3-8** DEVDOCS | new `drc/{build,units,template,cli,playbooks}.md`; one paragraph each for `prefill/drc.py`, `prefill/cli.py`, `replay/runner.py`, `replay/line.py`, `cli.py`, `smoke/checks.py`; the retired files' DevDocs moved to `docs/_archive/` by a note line, never deleted (D6). | `docs/40 - DevDocs/cobalt/**` |

NOT IN D3: leg writes, `unresolved` lines, RESOLVE (D5); settings keys (D4); the 3-line daily stub, C13 (`1% goal:` fill — through the daily-note writer, a later slice, `[F-19]`); A26–A39 sections other than the ones listed; weekly pack; OCR; voice (R100); the Charter / ladder "15:41" wording (the desk edits its own documents, R103 O18); any `launchctl`.

## INDEX CARD
1. LAWS.md IN FULL. Binding here: **L1** no plausible-empty note — never the create-then-return path; a missing source renders `not given`; an unknown `{{` FAILs. **L3** one creator of the note, one template (his), one miss-line function, one CLI, the 15:40 job and its CLI entry killed in the SAME deploy (`[F-22]`). **L7 / R101** no engine result anywhere. **L8** any rate with its n; n<30 `insufficient data` (the one constant, `replay/line.py:59`). **L11** his voice units are his. **L28** create-if-absent, units by id, human wins + override rows, voice units created once, versioned, refused in `market_reset`. **L32 / L45** the template fixture is shape only. **L42** RESTARTS derived — the job removed + `com.cobalt.replay` + `com.cobalt.aset`. **L57** every derived value in `drc_rows` with inputs + `fn_version`. + `48`'s INDEX CARD list.
2. v2 §3, §5, §6, §8, §9 D3, §10 E3 / E6 / E8 / E9 / E11 / X10, §12, §13 (A, B, C) as ruled.
3. `cto-2026-09-22.md` §4 R65–R73, R90–R103, R114, R116, R117.
4. D1's and D2's reports on your base; `docs/40 - DevDocs/cobalt/drc/*.md`.
4b. The strategies folder `/Users/cobalt/Vault/Think/1 - Trading/4 - Strategies/` — `ls` ONLY (titles), never a note's body; `src/cobalt/taxonomy/slug.py`, `src/cobalt/taxonomy/vault_loader.py` (`STRATEGIES_DIR`), `src/cobalt/vaultwrite/frontmatter.py` (`split_frontmatter`). Tests build their OWN strategies folder in `tmp_path` with constructed titles and slugs (L32 / L45: never his titles in a test or fixture).
5. His template `/Users/cobalt/Vault/Think/5 - Templates/DRC.md` and `1 - Trading/5 - Review/DRC-2026-09-21.md` (Read tool, READ ONLY — headings and token lines only enter the fixture).
6. Code: `src/cobalt/prefill/{drc,cli,config,daily}.py`, `src/cobalt/vaultwrite/writer.py` (`create_if_absent`, `upsert_unit`, `skip_if`), `src/cobalt/replay/{runner,line,cli}.py`, `src/cobalt/daymode/drc.py` (the 09:00 reader — UNCHANGED, `[F-23]` guards it), `src/cobalt/smoke/{checks.py}`, `configs/cobalt/smoke/s2.yaml`, `configs/cobalt/jobs.yaml`, `ops/README.md`, `pyproject.toml:64`.

## AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (drafted by `drc-build-draft-0922`, 2026-09-22), not by Dejan. Each its own Bash call on `"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"`: `grep -n "^| R114 "` (`obsidian is blind to that extention`) · `grep -n "^| R116 "` (`one to one naming convention`) · `grep -n "^| R117 "` (`Assume all wilde named to match`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"one to one naming convention" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY, and the same for `Assume all wilde named to match` and `obsidian is blind to that extention` · `grep -n "^| R93 "` (`DRC every market trading day`) · `grep -n "^| R98 "` (`copy into every DRC`) · `grep -n "^| R99 "` (`a re-drop never rewrites them`) · `grep -n "^| R101 "` (`psychology is disclosed`) · `grep -n "^| R102 "` (`1.A 2. A 3.A 4. B`) · `grep -n "^| R103 "` (`It's a small .csv`) · `grep -n "^| R91 "` (`Tradezella log for the day`) · `grep -n "^| R32 "` (`claude-opus-5-5`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"It's a small .csv" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY. THIS launch = row **R__** (`grep -n "50-drc-d3-build.md"` on the launch day's desk file; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"50-drc-d3-build.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` NON-EMPTY). The FOUR NEW strings (`Bash(rm /Users/cobalt/cobalt-wt/drc-d3/.env)`, `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/drc-d3/.env)`, `Bash(git rm configs/cobalt/templates/drc.md.j2)`, `Bash(git rm ops/com.cobalt.prefill-drc.plist)`) each ≥1 on the launch day's desk file AND committed (`git log -1 -S…` on the desk files ONLY). Every other string ≥1 in `33-setups-fix-r3.md`. Any miss → `FAILED: authorization mismatch — <what>`, stop.

UNATTENDED RULES and RECOVERY RULE: `48`'s, with `drc-d3` / `D3-<n>` / `wip(drc-d3)`. `git rm` only in its two exact strings.

REPORT (L48): `/Users/cobalt/cobalt-wt/drc-d3/docs/40 - DevDocs/reports/drc-d3-build-2026-09-2<n>.md`, committed on the branch; sections as `48`'s (`## D3-1` … `## D3-8`, with `## D3-2b` after `## D3-2`, plus `## EXPERIMENTS` (E3, E6, E8, E11, X10 results) and `## NOT RENDERED` (every §13 row this slice leaves empty and why — R101 engine rows, order-log rows, later slices)).
STOP LINE (L71): `DRC D3 BUILT <tip> | on <base> | offline <p>/<f> | with-DB <p>/<f> | E3: <PASS|FAIL> | E6: <PASS|FAIL> | E8: <PASS|FAIL> | E11: <PASS|FAIL> | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` — or `FAILED PREFLIGHT: <rule>` / `FAILED: D3-<n> — <command> — <reason>`.

## STEP-0 — PREFLIGHT, BASELINE
PREFLIGHT rows: `date` · BASE TIP filled · `tail -n 3` of D2's report (`/Users/cobalt/cobalt-wt/drc-d2/docs/40 - DevDocs/reports/drc-d2-build-2026-09-2<n>.md`) → last non-blank line starts `DRC D2 BUILT ` (or D2's fix round's `… BUILT `) with tip = BASE TIP · **E1 GATE (R92 / R114 — BY HEADER)**: `ls "/Users/cobalt/Vault/Think/1 - Trading/5 - Review/_imports/drc"` → a `YYYY-MM-DD` folder (`_reference/` skipped, never read); `ls -la` of the newest one; the Read tool with `limit: 1` on each file there, classified by hand against the REQUIRED-NAME constants of `src/cobalt/drc/trading_log.py` / `stats_log.py` on your base (`48` D1-2a's rule) → exactly one trading-log header and one stats-log header, whatever the names and extensions; none of a kind → `FAILED PREFLIGHT: E1 not dropped — <what the headers show>`; two of one kind → `FAILED PREFLIGHT: E1 ambiguous — <kind> — <name 1>, <name 2>` · `ls "/Users/cobalt/Vault/Think/5 - Templates/DRC.md"` (absent → `FAILED PREFLIGHT: his template is missing`) · clean tree, branch `drc/d3-build`, tip = BASE TIP · `.env` absent · DEV-DB STAGGER · `uv run cobalt jobs restarts <base>..HEAD`.
BASELINE: OFFLINE `uv run pytest -q tests/cobalt -p no:cacheprovider` → `failed` 0; WITH-DB inside the `.env` pair: `COBALT_ENV=dev uv run pytest -q -p no:cacheprovider -rs tests/cobalt/test_tenancy.py tests/cobalt/test_drc_store.py tests/cobalt/test_drc_imports_db.py tests/cobalt/test_replay_runner.py` + every with-DB prefill / smoke test (`grep -l "cobalt_dev\|COBALT_ENV" tests/cobalt/test_prefill*.py tests/cobalt/test_smoke*.py`, each by path) → `rm` → "No such file".

## ROW SHAPE — `48`'s; tests in `tests/cobalt/test_drc_build.py` (offline, `tmp_path` vault, `template_shape.md`, D1's fixtures) and `tests/cobalt/test_drc_build_db.py` (with-DB). Required, at least: the build on a `trades` event (D1 fixture day) — every unit present under its heading, gross AND net in the summary and the PnL unit; a re-build (superseding file) — units upsert, voice units untouched, a Cobalt line he changed → human wins + override row; the no-trade event → `drc-day/no_trade` + the blank voice unit, no trade blocks; a `{{time}}` line in a mutated template → FAILED naming it; `Grade:` / `Goal:` never written by any unit (`[F-23]`, and `daymode/drc.py`'s tests untouched and green); the open position unit and a carried position on day 2; `drc_rows` inputs snapshot survives a later `aset_sizings` update (`[F-19]`); miss line absent-note path + E6; the CLI dry-run writes nothing; the smoke conditional's three outcomes; **D3-2b** (a `tmp_path` strategies folder with constructed titles + slugs, and D1's multi-playbook fixture trade) — `<Title> Long` / `<Title> short` / `<Title> LONG` → the setup; `<Title>` alone → the setup; `<Title> Long Long` → stripped once → `unmapped` unless a title `<Title> Long` exists; `<Title>Long` (no space) and `<Title> Longer` → `unmapped`; a near-miss spelling (one letter, a space, a case change inside the title) → `unmapped` (no fuzzy match); two names on one trade → both rendered in order; a title whose note has no / a bad `trade_def:` → `unmapped … <SlugError text>`; a missing strategies folder → every name `unmapped … not readable`, the build `done`; a title added to the folder between two builds → mapped on the second build with no code change (titles read at build time); `grep -rn` of the constructed titles in `src/cobalt/drc` → 0 (no committed list); `rules.yaml`'s `reads:` move (`cobalt jobs restarts` output in the report).

## CLOSE — quoted VERBATIM
OFFLINE + WITH-DB (baseline set + `test_drc_build_db.py`) → `failed` 0 → `rm` → "No such file" · `git diff --stat <base>` → only the rows' files + tests + fixtures + DevDocs + report · `git diff <base> -- src/cobalt/daymode src/cobalt/cards src/cobalt/radar src/cobalt/aset/engine.py src/cobalt/settings src/cobalt/db_migrations` EMPTY (L52 boundary: nothing reaches a score, grade or size) · `git ls-files ops/com.cobalt.prefill-drc.plist configs/cobalt/templates/drc.md.j2` → no output · `grep -rn "prefill-drc\|drc.md.j2\|prefill drc" src configs ops tests` → only removal-history comments, each named · L32: for 3 heading-free lines of his template (never written), `grep -rn -F "<line>" tests/fixtures/drc` → 0, reported without the text · `uv run cobalt jobs restarts <base>..HEAD` VERBATIM (expect the removed job, `com.cobalt.replay`, `com.cobalt.aset`; UNCLASSIFIED → ESCALATE) · L68 table for `replay/runner.py`, `prefill/*`, `cli.py`, `jobs.yaml`, `s2.yaml` across unmerged branches (the S3 C1 / C2 branches, if cut, are named).
`## ESCALATE` ALWAYS: (i) the deploy's steps this build needs and does not run (bootout of `com.cobalt.prefill-drc` + the plist removed from `~/Library/LaunchAgents`, RESTARTS, the E7 ops read, his daily-stop value loaded before the first build — `48`/`51` items); (ii) the Charter / ladder "15:41" re-wording is the desk's (R103 O18: "within the build time after the second file", "every market trading day"); (iii) `## NOT RENDERED` summarised; (iv) X10's display item; (v) every `ASK DESK`, and ALWAYS `ASK DESK: R116 — the stripped name must EQUAL a note title; built as exact, case-sensitive equality (a case difference such as "reclaim" vs "Reclaim" renders unmapped) — confirm exact, or should case be ignored? [<time>]` (default built: exact — the fewest guesses); (vi) D3-2b on D1's fixtures: names read / mapped / unmapped (counts); (vii) `MEMORY:` / `RULING:` lines.
Stop line LAST, report committed by path (`git commit -m "feat(drc): D3 — report"`), `git status --porcelain` EMPTY, stop. NEXT, not yours: `55-drc-d3-check.md`; then the deploy prompt for D4 + D1 + D2 + D3 (one stacked set, L43 / L68, his "approve").
