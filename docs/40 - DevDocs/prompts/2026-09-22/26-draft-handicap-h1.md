MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `handicap-h1-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/26-draft-handicap-h1.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control handicap-h1-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `08` / `11` / `14` / `18` / `22` / `24`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the FOUR files named below with the Write tool, the REPORT LAST (09-22 lesson 3) · METER: Anthropic medium (v3 is 79 KB) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the H1 BUILD of the float handicap — the FINAL from v3 + his R26 "B" — with its first-gate experiments and its three-checker check, 2026-09-22

WHY: the four-house tribunal on the soft float / market-cap handicap (his 09-21 R28 / R29) is CLOSED: proposal → round 1 (Grok, Gemini, Fable; Astra METER, `cto-2026-09-22.md` R13) → v2 → round 2 (`19` Grok + Gemini, `20` the Fable seat) → v3 (`docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md`, `reports/float-handicap-tribunal-derive-r2-2026-09-22.md`): R2-2 CONVERGED (JSONB `handicap.decisive`, no enum / CHECK / migration for the cap exclusion; the counterfactual = a second full `decide()`), and R2-1's one open question was RULED BY HIM at 12:28 ET — `cto-2026-09-22.md` §4 **R26 "B" = POOL-WIDE division** (the Fable seat's round-1 (c) replacement wording, quoted verbatim in v3's `OPEN FOR DEJAN — R2-1` block as position B; R2-1.2 stays converged: `_metric_position` untouched). v3 + R26 IS THE FINAL (Astra reads it Sat 09-26, R13 — `ASTRA PENDING` marks stay). LAW STEP (L67 / L52): design DONE → **THIS = drafting H1's build (+ its first-gate experiments) and its check** → build → ≥3 checkers → its deploy (the L68 stacked gate; L43 as amended). LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29` (blocks nothing, L72). H1 = v3 §9's first chunk: the `handicap` block read from his pool note (keys only in code; no default — a missing key is a loud refusal, L1), the per-name factor computed and STORED on the membership row (`raw_rank`, the would-be `rank`, `h`, `decisive` — v3 §6's JSONB field set as B defines it), `mode: shadow` computing and storing, NEVER sorting (L7: shadow first), the dry-run over every retained day, the `/radar` H chip; NO `live` sort, no score change, no card change in H1 (those are H2 / H3 per v3 §9). The DESIGN FILE itself is not edited by you: your build prompt tells the builder to write the R26 resolution into v3 as its FIRST step (the `OPEN FOR DEJAN — R2-1` block replaced by position B's text, tagged `[R26]`, and `## Status after round 2` updated) so the file changes once, with the build, on the branch.

## INDEX CARD — read these, in this order, nothing more until a step needs it
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L1, L7 (shadow first; nothing flips without a shadow run and his token), L9, L19, L28 (his pool note is read through the existing vault reader; the `handicap:` block is HIS text — the desk writes it on his ruling, L65; the code never writes it), L29 (Opus floor: a migration ships), L32 (his threshold VALUES never in code, tests or fixtures — keys only; tests use literals of the builder's own), L35, L42 (RESTARTS by `cobalt jobs restarts`), L45, L48, L52 (a)–(d), L53, L57 (every stored number replayable), L60, L62 / L63, L66, L67 (three checkers: Grok · Gemini · Opus 5 — Sol METER), L68, L69 (settings in tests), L70 (every "not checkable from reads" claim = a first-gate experiment, run FIRST), L71, L73. One line each in the prompts you write.
2. THE FINAL: v3 whole (`docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md`) — §1 (the mechanism; the `OPEN FOR DEJAN — R2-1` block: position B is the ruling), §2 (the block's keys — by KEY), §4 (the excluded list), §6 (the columns and the JSONB field set — B's meaning of `raw_rank` / `rank` / `effective`), §7–§8 (his values live in the note; no code default), §9 (chunks H1 → H2 → H3, `[F-16]` the `h = 1` dry-run must reproduce stored membership), `## First-gate experiments (L70)` (X1–X16: which gate H1 — the derive says X2 first; X4 the rollback freeze; X12 / X15 the position basis), `## Dissents, verbatim`, `## L52`; the derive-r2 report's `## OPEN FOR DEJAN` (B's text verbatim — the builder pastes THAT into v3), `## OWNER ITEMS` (13 — none a precondition; the block's VALUES are his, read AFTER: the build ships with the block ABSENT and proves the loud refusal + the `h = 1` identity), `## ESCALATE` 4–6 (the FORMULA_FILES rider — settle it from the code: does the module that computes `h` join `evaluate.py:144-152`'s hash? the Fable seat says no because `h` is stored on the row and the pool has never been in the hash — read `evaluate.py` and rule it in the prompt with the file:line; the H2 edit-site count is H2's, not yours; the rollback hazard: every H1 deploy / rollback prompt carries "desk removes the `handicap:` block first" — write it into the build report's `## FOR THE DEPLOY` section; the fail-soft guard: `decide()` runs before S1 (`runner.py:186`) — decide in the prompt, with file:line, whether the handicap step is caught in ONE place (raw ranks, factor 1, degraded banner, loud log) or guarded by tests only, and say why; `last_scan_ms` → ops item, not yours).
3. THE PROPOSAL and round 1 (for the chunk plan only): `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` §9 chunks, `reports/float-handicap-design-2026-09-21.md` (the author's `enters at: pool position and card_score · chunks: 3 · migration: yes`).
4. THE CODE the build touches (main at HEAD, `~/cobalt`): `src/cobalt/radar/pool.py` (`_ranked` `:109-212`, the `:326` re-sort point, `decide()` `:280-400`), `src/cobalt/radar/models.py` (`:75-116` the pool block model; `:160-195` `ExcludedBy`, the membership row), `src/cobalt/radar/store.py` (`:108-160` membership writes; the DDL it expects), `src/cobalt/radar/runner.py:178-192` (S1 / `decide()`), `src/cobalt/radar/evaluate.py:140-170` (`FORMULA_FILES`), `src/cobalt/db_migrations/` (the newest number → yours is next; the rollback file convention: `ls`), the vault reader of the pool block (`grep -rn "pool_block\|def load_sources\|priority" src/cobalt/radar/sources.py src/cobalt/vault*` — find it), `src/cobalt/aset/radar_panel.py` (the pool section, for the H chip), `tests/cobalt/test_radar_pool*.py`, `tests/cobalt/test_radar_panel*.py`, `tests/fixtures/radar/` (the committed real-shape day — the dry-run's identity test runs on it, L45).
5. THE SHAPES YOU COPY: `prompts/2026-09-21/65-setups-one-build.md` (a resumable multi-step Opus build in its own worktree: launch line, UNATTENDED RULES, RECOVERY RULE, the `.env` pair for the with-DB suite — R41's two strings are `setups-c1`-specific: YOUR worktree needs its own pair → TWO NEW strings `Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/handicap-h1/.env)` / `Bash(rm /Users/cobalt/cobalt-wt/handicap-h1/.env)`, list them under `## NEW STRINGS`; the migration on `cobalt_dev` = `COBALT_ENV=dev uv run cobalt db migrate` (09-19 R18 / d3 — precedented) — a migration is a WRITE PATH: MODEL Opus 5; `--permission-mode auto` per today's dev-lane reading (`05`, `12`, `15`: the with-DB suite and a `cobalt_dev` migrate are the dev lane, no production write; NOT `acceptEdits`), state it in the MODEL line); the bars build's first-gate experiment chunk `prompts/2026-09-20/09-draft-bars-build-prompts.md` and its chunk E (experiments run FIRST, each with `AS EXPECTED / NOT AS EXPECTED`, a NOT AS EXPECTED that changes the design = STOP + ESCALATE, never a silent redesign); the check shape `prompts/2026-09-21/66-setups-one-check.md` (four seats, Sol METER → three; the packet rules; the question set; the file-check tables; the stop line).
6. `cto-2026-09-22.md` §4 R13 (no Astra this week; checks on Grok · Gemini · Opus), R26 (his "B"); `cto-2026-09-21.md` R28 / R29 (the handicap rulings — KEYS), R41 (the `.env` pair shape), R46 / R49 (check seats, the two strings verbatim), R39 (`grok` / `agy` through 2026-09-22 23:59 ET — a check launched TOMORROW needs his extension: say so in the check prompt's DATE GATE, `66`'s wording).

## THE JOB — four files, Write tool, the report LAST
(a) `docs/40 - DevDocs/prompts/2026-09-22/27-handicap-h1-build.md` — ONE complete block on `65`'s shape: MODEL Opus 5 (why: a migration + `src/cobalt/radar/pool.py`), `--permission-mode auto` (the dev-lane reading, stated), NEW worktree `/Users/cobalt/cobalt-wt/handicap-h1`, branch `radar/handicap-h1-0922` off main (the DESK runs `git -C /Users/cobalt/cobalt worktree add -b radar/handicap-h1-0922 /Users/cobalt/cobalt-wt/handicap-h1 main` before launch — say so); launch line FIRST = `cd /Users/cobalt/cobalt-wt/handicap-h1` then the `claude --bg` line whose `--allowedTools` = `65`'s nineteen strings with the two `.env` strings RE-POINTED to this worktree (NEW, listed) and nothing else added, `--remote-control handicap-h1-0922`, the three denies, the `--add-dir` triplet; index card SECOND; AUTHORIZATION greps R28 / R29 / R26 / the `R__` launch row the desk fills / the `.env` pair on a committed row of HIS; PREFLIGHT (worktree clean on the branch at main's tip, `.env` absent, `date`, the two BASELINE suites as `05` spells them today — offline `uv run pytest -q tests/cobalt tests/taxonomy -p no:cacheprovider`, with-DB inside the `.env` pair after `COBALT_ENV=dev uv run cobalt db migrate` — 09-22 lesson 8: `cobalt_dev` drifts; a STAGGER gate on the dev-DB lane: no other worktree holds a `.env` — `ls -la /Users/cobalt/cobalt-wt/*/.env` must list none — else `FAILED PREFLIGHT: the dev-DB lane is held — the desk staggers`); STEP-0 = write R26 into v3 (the `OPEN FOR DEJAN — R2-1` block → position B's text verbatim tagged `[R26]`; `## Status after round 2` → R2-1 RULED B; committed on the branch as `docs(design): float handicap — R26 pool-wide`); STEP-1 = the FIRST-GATE EXPERIMENTS H1 needs (from v3's list — X2 first; X4; X12 / X15 if the pool-wide reading still needs the position basis; every "not checkable from reads" the derive named), each run on the committed real-shape day or `cobalt_dev`, each closed `AS EXPECTED` / `NOT AS EXPECTED` with the output verbatim — a NOT AS EXPECTED that changes the design = `FAILED: STEP-1 — X<n> — <what>` + the design question for the desk, never a silent redesign; STEP-2 … n = H1 in `65`'s T → C → A1 → D → SUITE → COMMIT shape per row: the pool-block schema (keys by KEY, `combinator`, `mode`, `missing`; NO default — absent block = the feature is OFF and every row stores `h = 1`, `raw_rank = rank`; a PRESENT block missing a key = loud refusal at load, L1), the vault reader, the factor per name (`yes` / `no` / `unknown` per `any` / `all`, `missing:` handling as v3 §… words it), the STORAGE (migration: the `handicap` JSONB column + `raw_rank`; rollback file; `cobalt db migrate` on `cobalt_dev`), the shadow computation (computed and stored, NEVER used to sort in `mode: shadow`; `raw_rank` = the `_ranked()` index without factor — always stored), the `h = 1` IDENTITY test ([F-16]: with the block absent or every factor 1 the stored membership and order are byte-identical to today's — the dry-run over every retained day proves it), the dry-run CLI (`cobalt radar handicap dry-run <day>`? — the name and flags come from v3; if v3 names none, the smallest addition to the existing `radar` CLI, named), the `/radar` H chip (render only when a row's `h < 1`; the pool section; no card change), the DevDocs (one wiki file per touched `.py`), `uv run cobalt jobs restarts main..HEAD` (expected: `com.cobalt.radar` — the pool code — AND `com.cobalt.aset` — the panel; the migration at deploy is the deploy hub's), the L68 note (other unmerged branches touching `pool.py` / `models.py` / `store.py` / `radar_panel.py`: `git -C /Users/cobalt/cobalt log --oneline main..<branch> -- <paths>` for `setups/seven-0921`, `s2/stale-marker-0921`, `ops/2026-09-21`, `bars/chunk-1a-0920`, `bars/chunk-2-0920`, `bars/chunk-e-0920` — a shared path → named for the stacked gate); report `/Users/cobalt/cobalt-wt/handicap-h1/docs/40 - DevDocs/reports/handicap-h1-build-2026-09-22.md` with `## FOR THE DEPLOY` (RESTARTS, the migration, "the desk removes the `handicap:` block from his note before any rollback" — L65, X4's proof, the `mode` the note must carry = `shadow` — his value, read after), `## FOR 28` (what the checkers file-check), `## ESCALATE`, `## CONTINUE`, the pinned in-progress last line; STOP LINE `HANDICAP H1 BUILT <tip> | on <main tip> | experiments <n>/<as expected>/<not> | offline <p>/<f> | with-DB <p>/<f> | migration: <nnnn> | h=1 identity: proven | .env: removed, proven gone | ESCALATE: <n>` or `FAILED: …`. "Never end a turn between steps" (09-22 lesson 2). RECOVERY RULE from `65` verbatim in substance. No `DEF_WRITTEN_`-style pin of an engine output by judgement (L35): every asserted number is a test's own construction or a printed engine output copied verbatim and said so.
(b) `docs/40 - DevDocs/prompts/2026-09-22/28-handicap-h1-check.md` — the three-checker CODE CHECK on `66`'s launch line BYTE FOR BYTE (only the path and the remote-control name `handicap-h1-check-0922`; the same seats: Grok · Gemini · Sol (METER → recorded, skipped) · Opus 5; the packet = the build diff `main..<tip>` in parts, the build report, v3 (its §1 / §6 / §9 and the R26 block), the experiment outputs, the migration + rollback files; the Q-set on `66`'s shape: L52 (a)–(d) for H1, the `h = 1` identity, shadow never sorts, the loud refusal, the FORMULA_FILES rider as the build ruled it, the rollback order, no value of his in code or tests (L32 grep), every experiment's `AS EXPECTED` re-read against its output); stop line `HANDICAP H1 CHECK DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for a deploy prompt: <r> of <n> · ESCALATE: <n>` (gate on `houses that checked`); DATE GATE `66`'s (a launch after 2026-09-22 23:59 ET needs his committed extension of `grok` / `agy`); STAGGER on the house lane (`tail -n 1` of the newest running Grok/Gemini hub's report — name the pattern: any `reports/*-2026-09-2*.md` whose last line is the in-progress line — or simpler: the desk staggers by hand; write the gate the desk can fill: `<STAGGER REPORT>` placeholder the desk replaces before launch, L19 mechanics).
(c) `docs/40 - DevDocs/prompts/2026-09-22/29-handicap-h1-facts.md` — NOT a prompt: the FACTS PACKET the builder reads at STEP-0 (the 09-19 practice): v3's B text verbatim (from the derive-r2 report's OPEN block), the JSONB field set with each field's meaning under B, the 13 owner items as "his, read after — the build ships without them", the experiments list with what each proves and what result stops the build, the FORMULA_FILES ruling with file:line, the fail-soft decision with file:line, the rollback order. Keys only, never a value of his.
(d) LAST, YOUR REPORT `docs/40 - DevDocs/reports/handicap-h1-draft-2026-09-22.md`: `## DIGEST` (≤12 lines: the chunk's scope in one sentence; steps count; experiments count and which run first; MODEL / mode / worktree; RESTARTS expected; the migration number; new rule strings = 2 expected — the `.env` pair for the new worktree), `## NEW STRINGS` (each verbatim with the precedent it re-points — R41's pair), `## RULE PROOF` (`27`'s strings vs `65`'s line, `28`'s vs `66`'s — every count, the two new ones marked), `## RULINGS TAKEN` (FORMULA_FILES, fail-soft, each with file:line), `## ESCALATE`, and your LAST LINE: `HANDICAP H1 PROMPTS DRAFTED · steps: <n> · experiments first: <n> · new rule strings: <n> · migration: yes · restarts: <list> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. L32: no value of his anywhere in the four files — KEYS only. Never invent a flag: every command shape appears in `65` / `66` / `05` / the code's CLI (`uv run cobalt radar --help` is NOT available to you — read `src/cobalt/radar/*cli*.py` and `src/cobalt/cli.py` for the real subcommands). Every OWNER item stays his. `date` for every time you write. Stop within 35 minutes; your stop line is your report's last non-blank line, written LAST.
