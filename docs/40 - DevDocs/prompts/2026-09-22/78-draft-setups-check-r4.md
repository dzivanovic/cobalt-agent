MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat · SEAT: drafter `setups-check-r4-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/78-draft-setups-check-r4.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control setups-check-r4-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `66` / `69` / `71`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO git write, NO launch (L36), no vault / memory write (L58) — you write exactly the files named below with the Write tool, the REPORT LAST · METER: Anthropic medium · never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps.

# DRAFT the setups CHECK ROUND 3 of 3 (THE LAST, L67 / L39) on fix round 4, and re-point `13` (blind values). LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults` (S2 stops 2026-09-23). LAW STEP: r2r3 check `70` (`defects that HOLD: 5`) → classified + fix r4 (`71` → `72`) BUILT: `SETUPS FIX R4 BUILT f5aaeb4 | on 8da261a | offline 2464/0 | with-DB: OWED (68) | tests added: 11 … | ESCALATE: 12` (report `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r4-2026-09-22.md`, report commit `f78bf4d` on `setups/seven-0921`) → **THIS = drafting check round 3 + `13`'s re-point** → deploy prompt (owed; carries R119's three Assumed-Defaults rows).

INDEX CARD: LAWS.md in full (L59); binding L32 · L35 · L37 · L39 · L45 · L67 (R46 / R49 code-check seats) · L70 · L71 · L74 · L75. Files: `prompts/2026-09-22/70-setups-check-r2r3.md` (your base, launch line byte for byte), `reports/setups-check-r2r3-2026-09-22.md`, `reports/setups-fix-r4-draft-2026-09-22.md` (classification + the re-point lines under its (A)3), `72-setups-fix-r4.md`, the r4 build report, `13-setups-blind-values.md` (+ `reports/setups-blind-*` if any), `cto-2026-09-22.md` §4 R118–R121, R125.

WRITE:
1. `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/79-setups-check-r4.md` — `70` re-pointed per the r4 drafter's list: seat / remote-control `setups-check-r4-0922`, range `8da261a..f5aaeb4` (report commit `f78bf4d`), FIX rows [R4] F1–F5 only (+ the r4 build's own ESCALATE rows as questions where they touch a FIX row), prompt path; the launch line's strings BYTE FOR BYTE; OFFLINE ONLY (with-DB OWED until `68`); THIS IS THE LAST ROUND — a defect that HOLDS goes to `## FOR DEJAN` with the override question, never to a round 4 (L39); DATE GATE R30 (through 2026-09-23 23:59 ET); STAGGER: refuses while `63` (routing tribunal) or `13` is in progress without a stop line; launch-row `R__` placeholder. Report `reports/setups-check-r4-2026-09-22.md`; stop line `SETUPS CHECK R4 DONE · grok: … · gemini: … · opus: … · sol: … · defects that HOLD: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
2. `13`'s re-point (if `13` needs one after r4's re-cut — the five `DEF_WRITTEN_RUBBERBAND_CUT_*` pins were re-copied in `f5aaeb4`): re-issue `13-setups-blind-values.md` in place (read whole, then Write) pointed at the r4 tip; the blind seat is NEVER pointed at any check packet folder holding those five values (`scratch/tribunal-bars-0920/setups-check/**`); launch line byte for byte; `R__` placeholder. If `13` needs no change, say so and write nothing for it.
LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-check-r4-draft-2026-09-22.md`: §0 ≤5 lines → `## DIGEST FOR THE DESK` ≤20 lines (launch order: `13` and `79` — can they run side by side? `13` is a blind code-capable seat; name its house lane use) → RULE PROOF (`79` vs `70`; `13` vs `HEAD`) → `NEW strings:` (expected none) → `## ESCALATE` → `## CONTINUE` → last line; while you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE: `SETUPS CHECK R4 DRAFTED · range: 8da261a..f5aaeb4 · questions: <n> · 13 re-issued: <yes|no> · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape. `date` before any time you write. L32: no value, ticker or date of his in any file you write. Stop within 30 minutes.
