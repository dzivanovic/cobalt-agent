MODEL: Sonnet 5 (`claude-sonnet-5`) — stage a packet, put it before ONE house, copy its answer, then compare; no verdict of your own (L37) · SEAT: blind-values hub `setups-blind-committed-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK when the house lane is free (the STAGGER gate below) and the launch row **R15** and this file are committed on main: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/17-setups-blind-committed-day.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control setups-blind-committed-0922 --allowedTools "Bash(agy *)" "Bash(claude -p --model claude-opus-5 *)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: `13-setups-blind-values.md`'s launch line BYTE FOR BYTE — the nine allow strings, the three deny strings and the `--add-dir` triplet, each in `prompts/2026-09-21/66-setups-one-check.md`'s launch line byte for byte (`"Bash(agy *)"` his R39 of 09-21; `"Bash(claude -p --model claude-opus-5 *)"` his R49 of 09-21; the read strings); NOTHING NEW. Only the prompt path and the remote-control name differ from `13`'s line. `auto` mode: no write path, reads only (the `66` precedent) · SESSION: fresh · no DB, no `.env`, no docker, no pytest, no git write, no vault write, no memory-folder write (L58); the one house you launch is the blind seat this prompt names (the `66` / `13` pattern — a check hub puts a packet before a house; you launch no hub and no worker of your own, L36) · METER: Anthropic small (you) + ONE house call (Gemini, or Opus 5 as the fallback) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default (fewer files staged, never more access), continue.

# BLIND EXPECTED VALUES ON THE COMMITTED DAY — a house that has NOT seen the pins re-derives the 17 existing `DEF_WRITTEN_*` values from the bars and the definitions alone (2026-09-22)

WHY: round 1's HOLD 1 (`docs/40 - DevDocs/reports/setups-one-check-2026-09-22.md` `## ESCALATE` 1): FINAL [F-16] (1) — *"For each per-setup fixture day, the expected side, formed bar, trigger price and stop price are written by a CHECKER house from the bars alone, before it sees the builder's output; the test asserts all four, not only `formed`."* — was not met: the 17 `DEF_WRITTEN_*` constants on branch `setups/seven-0921` are the engine's own output (the build report's `## ESCALATE` (ii), in its own words). `13` covers the CUT days of `12`; THIS covers the pins that ALREADY EXIST, on the COMMITTED synthetic day `2026-01-06` (the P2 real-shape fixture, tickers FTFT and BGFI — `tests/fixtures/radar/_cut_p2_fixtures.py:36-38`). It runs on tip `60ddac4`: the fix round (`15`) is forbidden to change any `DEF_WRITTEN_*` value or the four files that hold them, so this may run before, beside or after `15`. LAW STEP: build → check r1 (HOLD 1) → **THIS** (+ `15` the fix) → check r2 (`16`, reads your `## Comparison`) → `12` → `13` → deploy prompt → his "approve" (L7 / L61).

## INDEX CARD (you; the house gets the packet only)
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — one line each: **L32** no value of his note enters the packet — the definitions staged are the build's NEUTRAL shapes (value-free literals of the build's choosing), never `4 - Strategies/*.md`. **L35** every value in your table is copied from a file, verbatim. **L37** you judge nothing: MATCH / MISMATCH / UNDERIVABLE is string equality after the normalisation named below and nothing else; a MISMATCH is not a verdict. **L45** the packet's bars are the committed real-shape fixture, byte-identical. **L48** evidence in the report the same turn. **L62 / L63** unattended; a denial is a FAILED. **L67** the blind seat is a house OTHER than the build's author where one is up. **L70** an UNDERIVABLE is not a defect. **L71** the stop line is the last non-blank line.
2. `prompts/2026-09-22/13-setups-blind-values.md` — the SHAPE this file copies: its staging rules, KEEP-OUT discipline, house invocations, ORDER proof and comparison. Where this file is silent, `13` binds.
3. `docs/40 - DevDocs/reports/setups-fixture-cut-draft-2026-09-22.md` `## KEEP OUT OF 13` ONLY — the four files and the line numbers of the 17 constants (read the TABLE; do not open the files it names).
4. `prompts/2026-09-21/66-setups-one-check.md` — its packet rules (Read → Write byte-identical, parts ≤ 38,000 B, `wc -c` against the original, the whitespace-gap check), its Opus probe, its `agy` / `claude -p` invocations.

**THE 17 PINS** (from `## KEEP OUT OF 13`, at `60ddac4`, in `/Users/cobalt/cobalt-wt/setups-c1/`):
| slug | file | lines | constants | how the test reads the formation |
|---|---|---|---|---|
| rubberband | `tests/cobalt/test_rubberband_forms.py` | :56 :58 :60 :62 :64 | `DEF_WRITTEN_RUBBERBAND_FORMED_BAR` · `_SIDE` · `_TRIGGER` · `_STOP` · `_LAST` | the evaluation AT ONE scan, `2026-01-06T16:30:00+00:00`, ticker FTFT, on the shape `rubberband_mapping(MIXED, htf_avoid=False)` (4 countertrend + 1 with_trend setup refs, no HTF avoid) — five fields: formed bar, side, trigger, stop, last close |
| nine-ema-scalp | `tests/cobalt/test_setups_nine_ema.py` | :221 :223 :225 :227 | `DEF_WRITTEN_NINE_EMA_SCALP_SIDE` · `_FORMED_BAR` · `_TRIGGER` · `_STOP` | the FIRST formed scan walking FTFT's scans then BGFI's, on the corpus shape `SHAPES["nine-ema-scalp"]` |
| vwap-continuation | `tests/cobalt/test_setups_vwap_cont.py` | :203 :205 :207 :209 | `DEF_WRITTEN_VWAP_CONTINUATION_SIDE` · `_FORMED_BAR` · `_TRIGGER` · `_STOP` | the same walk, `SHAPES["vwap-continuation"]` |
| second-chance | `tests/cobalt/test_setups_second_chance.py` | :198 :200 :202 :204 | `DEF_WRITTEN_SECOND_CHANCE_SIDE` · `_FORMED_BAR` · `_TRIGGER` · `_STOP` | the same walk, `SHAPES["second-chance"]` |
Total fields: 5 + 4 + 4 + 4 = **17**. Hitchhiker has no `DEF_WRITTEN_*` (it is `AWAITING_A_DAY`); backside and fashionably-late are `AWAITING_A_RULING` — none of the three is asked.

**NEVER STAGED, NEVER OPENED BY YOU BEFORE THE HOUSE HAS ANSWERED:** the four files above, `tests/cobalt/test_setups_lego.py`, `tests/cobalt/test_setups_hitchhiker.py`, every other `tests/cobalt/test_setups_*.py`, the build report `setups-one-build-2026-09-21.md`, `prompts/2026-09-21/65-setups-one-build.md`, `56-setups-c1-build.md`, `tests/cobalt/setups_shapes.py:1-44` (`:40-42` names the committed day's culmination bar — an engine result, not a rule), round 1's check reports AND the whole folder `scratch/tribunal-bars-0920/setups-one-check/` (its `30-diff-*-tests` parts carry the pinned values), `12`'s cut report sections other than `## FOR 13`, and ANY line containing `DEF_WRITTEN_`. Before each staged file is written: `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED " <original>` → 0 (for an excerpt: on the excerpt's lines, counted by you from a `grep -n` of the original), else that file is NOT staged (ESCALATE).

REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-blind-committed-day-2026-09-22.md` (Write tool; you commit nothing — the desk commits it). Sections: `## §0 Headline` (≤5 lines) → `## AUTHORIZATION` → `## PREFLIGHT` → `## Packet` (file · original path · lines · bytes original · bytes staged · parts) → `## House` → `## ORDER` (the timestamp proof) → `## Comparison` → `## ESCALATE` → `## CONTINUE` → the last line. FIRST Write creates it with `## CONTINUE` = `next: AUTHORIZATION` and the last line `(run in progress — next under ## CONTINUE)`.
STOP LINE (L71 — LAST NON-BLANK LINE): `SETUPS BLIND COMMITTED DAY DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: <why>` / `FAILED PREFLIGHT: <rule>`.

AUTHORIZATION — each its own Bash call; a prompt file is not an approval:
- THIS launch is row **R15** of `cto-2026-09-22.md` (or the launch day's `cto-2026-09-2<n>.md`) — still `__` in the file you read → `FAILED: authorization mismatch — launch row not filled in`, stop. `grep -n "17-setups-blind-committed-day.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` → a `| R` row (a missing desk file is recorded, not fatal) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"17-setups-blind-committed-day.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` non-empty (else `FAILED: authorization mismatch — the launch row is uncommitted`; the desk files ONLY).
- The seats: `grep -n "^| R49 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry `claude -p --model claude-opus-5`) · `grep -n "^| R39 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (record what it says of `agy` and until when).
- Each allow string: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/66-setups-one-check.md"` ≥1 each (the nine, the three deny strings, the triplet). Any 0 → `FAILED: authorization mismatch — <rule>`.

UNATTENDED RULES (`66`'s, unchanged): ONE command per Bash call, the bare listed prefix, no pipe, no redirect, no `cd … &&`, no `VAR=value` in front; files by the Write tool; reading by the Read tool (large files with offset / limit). **A DENIAL IS A FAILURE:** `FAILED: <step> — <command> — <reason verbatim>`, stop. RECOVERY (L60): a relaunch first runs `ls /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-committed`; a staged file is not re-staged (its `wc -c` is re-proven); an existing `blind-values.md` is NEVER re-asked — you go to `## ORDER`.

## STEP-0 — PREFLIGHT (one row each: command · exit · result verbatim)
- `date` (the DATE GATE for Gemini: record it).
- `agy --version` · `claude -p --model claude-opus-5 "Reply with exactly the word OK"` (`66`'s probe, `run_in_background`, 3 minutes).
- **STAGGER — the house lane (one Grok / Gemini hub at a time):** `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-2026-09-21.md"` — its line must START `FLOAT HANDICAP TRIBUNAL R1 DONE` or `FAILED`; anything else, the file missing included → `FAILED PREFLIGHT: 38 is running — the desk staggers`, launch nothing.
- `git -C /Users/cobalt/cobalt log --oneline -1 setups/seven-0921` (record the branch tip).
- **THE PINS AND THE STAGED SOURCES ARE STILL `60ddac4`'s:** `git -C /Users/cobalt/cobalt log --oneline 60ddac4..setups/seven-0921 -- tests/fixtures/radar tests/cobalt/setups_shapes.py tests/cobalt/radar_p2_support.py configs/cobalt/taxonomy/tunables.yaml tests/cobalt/test_rubberband_forms.py tests/cobalt/test_setups_nine_ema.py tests/cobalt/test_setups_vwap_cont.py tests/cobalt/test_setups_second_chance.py` → EMPTY (a fix commit that touched any of these → `FAILED PREFLIGHT: a staged source or a pinned file moved after 60ddac4 — <line>`; `15` forbids it, so this is a finding for the desk).
- `ls /Users/cobalt/cobalt-wt/setups-c1/tests/fixtures/radar` (quote; the committed day's files below must be present).
THE HOUSE (L67: a house other than the build's author — the build and its pins are Anthropic-house work): **PRIMARY = Gemini** (`agy`, per R39 — valid only while R39's window holds, through 2026-09-22 23:59 ET; `date` past it, or `agy --version` failing, → fallback). **FALLBACK = Opus 5** (`claude -p`, R49 — the code-check seat, no calendar limit; the SAME house as the builder, so its use is recorded under `## ESCALATE` as "blind but same-house"). Neither up → `FAILED PREFLIGHT: no blind house — <each line verbatim>`.

## STEP-1 — STAGE (`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-committed/` — the first Write creates it; NO `mkdir`)
By Read → Write byte-identical from `/Users/cobalt/cobalt-wt/setups-c1/`, each proven by `wc -c` (original vs staged; parts sum to the original), `66`'s whitespace-gap check first:
 (a) **THE COMMITTED DAY** — `tests/fixtures/radar/bars-rubberband.real-shape.json` (both tickers' working bars; 183,837 B at `60ddac4` → parts ≤ 38,000 B, cut only between two JSON objects, after a `},` line), `tests/fixtures/radar/daily-bars.real-shape.csv` (whole), `tests/fixtures/radar/card-settings.real-shape.json` (whole — the settings the stage reads). Nothing else from `tests/fixtures/radar/`.
 (b) **THE LOADERS** — an EXCERPT of `tests/cobalt/radar_p2_support.py`: the fixture readers (`fixture_bars`, `fixture_daily`, the settings reader, `defaults`, `TRADE_DATE`, `engine_tunables`), found with `grep -n "^def \|^TRADE_DATE\|^FIXTURES"`, each excerpt headed by one line naming its real path and line range.
 (c) **THE DEFINITIONS, as the build's neutral shapes render them** — EXCERPTS of `tests/cobalt/setups_shapes.py`, each headed by its real path and line range, the body those lines byte for byte (ranges read at `60ddac4`; re-find each with `grep -n` and use what the file says): the note templates and constructed lists `NOTE`, `TUNABLES_UNIT`, `D2_CONSTRUCTED`, `MIXED`, `COUNTERTREND_ONLY`, `WITH_TREND_ONLY`, `HTF_AVOID` (`:45-93`); `example_mapping`, `rubberband_mapping` (`:94-128`); the loaders and THE SCAN GRID `load_note`, `user_rows`, `tunables_for`, `bars`, `member`, `evaluate`, `every_scan` (`:129-203` — `every_scan` is the grid: `DAY_START + m minutes` for `m` in `range(0, 391, 2)`, `scan_interval=100`); `D4_CONSTRUCTED` (`:258-266`); `pullback_to_ema_mapping` (`:316-345`); `pullback_to_vwap_mapping` (`:346-377`); `break_retest_turn_mapping` (`:378-406`); `D5_CONSTRUCTED`, `D6_CONSTRUCTED` (`:407-412`); the `Shape` dataclass (`:205-218`) and `SHAPES` (`:423-475`). NEVER `:1-44`. `DAY_START` is written into `TASK.md` by you as `2026-01-06T14:30:00+00:00` (its value at `:43`, a grid input — not the forbidden `:40-42` comment).
 (d) **THE ENGINE TUNABLES** the shapes' `cfg(...)` tokens and resolvers read — an excerpt of `configs/cobalt/taxonomy/tunables.yaml`: every row whose key a staged shape names (find each with `grep -n "key: <key>"`), every `A-nn` convention row the shapes consult, and `radar.scan_interval` if present. A key you cannot find → listed in `TASK.md` as "not staged", never guessed.
 (e) **THE PUBLIC RULES** — excerpts of `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`: §1 `### The mechanism` (orientation `A-01`), §2.1, the §2.2 and §2.3 tables with the geometry guard and `[F-16]` (5), §2.4, the §3 table rows D0–D6, and the §4 interpreter table.
 (f) `TASK.md`, written by you: the task sentence below, the file list with parts, the scan instants per slug (rubberband: the ONE scan `2026-01-06T16:30:00+00:00`, FTFT; the other three: every scan of the grid, FTFT first then BGFI, the FIRST that forms), and the fields owed per slug (rubberband five: formed bar, side, trigger, stop, last close at that scan; the others four: side, formed bar, trigger, stop).
`ls -la` of the folder quoted; every staged file re-checked with `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED "` → 0.

## STEP-2 — THE HOUSE (ONE call, its whole output read; `run_in_background: true`)
THE TASK SENTENCE (identical for either house): *"You are a blind checker (FINAL [F-16] (1)). Everything you may use is in this folder. For each setup in TASK.md, from the bars, the daily bars, the settings, the definition excerpts, the tunables excerpt and the public rules ONLY, derive what the rules produce at the scan instants TASK.md names on the two-minute scan grid: for rubberband, the evaluation at its one named scan; for the other three, the FIRST scan on which the setup forms, walking FTFT's scans then BGFI's. Give the side (long|short), the formed bar's timestamp exactly as the bars file spells it, the trigger price, the stop price (and, for rubberband, the last close at its scan). Show your work: cite every bar timestamp you use. Where the staged rules do not determine a value — an EMA, VWAP or ATR you cannot compute by hand, a rule the excerpts do not state — write UNDERIVABLE and the missing rule or computation, never a guess. Do not open any file outside this folder. Write one table per setup: field · value · the bars it rests on."*
 - **Gemini:** `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-committed --print="<the task sentence> Write your answer to blind-values.md in this folder and nothing else."` (`13`'s invocation with the folder narrowed to this packet).
 - **Opus 5 (fallback):** `claude -p --model claude-opus-5 "<the task sentence> Read files with the Read, Grep and Glob tools only. Write no file. Print your complete answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-committed --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"` (`13`'s invocation) — YOU write its printed answer to `blind-values.md` VERBATIM.
 A METER / HARNESS line → the other house once; both down → `FAILED: STEP-2 — no house answered — <lines verbatim>`. `wc -c` of `blind-values.md` quoted. The 45-minute clock of `66` §2 applies (TaskStop, `TIMEOUT`, never relaunched).

## STEP-3 — ORDER, then COMPARE
3.1 THE ORDER PROOF (`## ORDER`, each its own call, output verbatim): `ls -lT /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-committed/blind-values.md` (T1 = its mtime) → `date` (T2) → ONLY NOW the first read of the pins, one call per file: `grep -n "^DEF_WRITTEN_" /Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/test_rubberband_forms.py` · the same for `test_setups_nine_ema.py`, `test_setups_vwap_cont.py`, `test_setups_second_chance.py` (the constants, verbatim; the line numbers must equal THE 17 PINS table — any other line → ESCALATE) → `date` (T3). T1 < T2 ≤ T3 is the proof; state also that no earlier tool call of this session named any of the four files except inside this prompt's text (your transcript is the record; the desk reads it).
3.2 `## Comparison`: table `slug · field · pinned · blind · MATCH|MISMATCH|UNDERIVABLE`, one row per pinned constant — 17 rows. Normalisation, `13`'s and nothing else: prices compared as decimals (trailing zeros ignored, `5.5000` = `5.5`); timestamps compared as instants (`2026-01-06 16:22:00+00:00` = `2026-01-06T16:22:00+00:00`); a `datetime(...)` constructor in a pin is read as the instant it names; the side compared as the literal the pin holds against the house's `long|short` — a pin that spells the side any other way is recorded verbatim and the row is `MISMATCH` with the note "spelling" (you do not translate, L37). `fields` in the stop line = the rows of this table.
3.3 `## ESCALATE`: every MISMATCH as `FIX-or-OWNER: <slug> <field> pinned <x> blind <y> — the house's cited bars <…>` (the desk takes it to a fix round or to him; you do not pick a side, L37); every UNDERIVABLE with the missing rule or computation the house named — L70: NOT a defect, and expected for an EMA / VWAP / ATR-resting value (the fixture-cut drafter's ESCALATE 7); write for each what a CODE-CAPABLE independent seat (Sol, read/write, back Sat 2026-09-26 06:47 ET per `cto-2026-09-22.md` R13 — a NEW string would be needed) would add: it would compute the indicator series on the staged bars and close the row, still blind; the house used and, for Opus, "blind but same-house as the builder"; every file NOT staged and why; every `ASK DESK`. Then the stop line LAST.
NEXT STEP, not yours: the desk reads the comparison; `16` stages it as HOLD 1's closure for the existing pins.
