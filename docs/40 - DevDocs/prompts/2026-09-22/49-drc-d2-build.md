MODEL: Opus 5.5 (`claude-opus-5-5`, R32 / R36) — D2 adds a vault BYTES write method and DB event rows (L29's list), so the floor is Opus. `--permission-mode auto`: the DEV LANE as `33` (L62 as amended R80, stated here and on the line) — offline build; every vault write in a `tmp_path` vault; DB only inside the with-DB suite's rollback on `cobalt_dev`; no production command, no write to the real or the dev vault. NOT `acceptEdits` (L63). Unlisted command = do not call it; files via Write / Edit · SEAT: DRC D2 builder `drc-d2-build`, launched by the CTO desk in the background. The desk FIRST runs `git -C /Users/cobalt/cobalt worktree add -b drc/d2-import /Users/cobalt/cobalt-wt/drc-d2 <BASE TIP>`, fills the BASE TIP and launch row **R__**, commits both, then: `cd /Users/cobalt/cobalt-wt/drc-d2` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/49-drc-d2-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-d2-build --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/drc-d2/.env)" "Bash(rm /Users/cobalt/cobalt-wt/drc-d2/.env)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: `33`'s seventeen shared + denies + triplet BYTE FOR BYTE; TWO NEW `.env` strings (re-pointed to `drc-d2`) · SESSION: fresh; relaunch = same line, resume from `## CONTINUE` · never `bypassPermissions`; push DENIED; no `COBALT_ENV=production`, no `launchctl`, no running ASET server (the page is proven with FastAPI's TestClient inside pytest), no vault write outside `tmp_path`, no memory-folder write, no `claude` · `.env` by name, removed at every stop · METER: Anthropic MEDIUM (≈7 seat hours, v2) · nobody sits at this terminal: `ASK DESK: <q> [<time>]` under `## ESCALATE`, safe default, continue.

# DRC D2 — THE ONE IMPORT PLACE (`/drc` on the ASET app), THE BYTES WRITER, BOTH-PLACED + THE NO-TRADE DAY, THE EVENT ROW. Branch `drc/d2-import`, worktree `/Users/cobalt/cobalt-wt/drc-d2`, from BASE TIP **`__`** (the D1 build's stop-line tip — or its fix round's, whichever the desk names — `48-drc-d1-build.md`; stack D4 → D1 → D2 → D3). D2 and D3 land in ONE deploy (v2 §9, grok (b)); D2 alone never deploys. DO NOT STOP until the last line is `DRC D2 BUILT …` or `FAILED …`. NEVER END A TURN BETWEEN STEPS.

RE-ISSUED 2026-09-22 (L19, whole file) by the Opus 5.5 prompt seat `drc-reissue-draft-0922` for his R114 (every daily file identified by its HEADER, any name, any extension). The launch line above is unchanged byte for byte.

RE-ISSUED 2026-09-23 (L19, whole file) by the Opus 5.5 prompt seat `drc-r17-draft-0923` for his 09-23 R17 (`cto-2026-09-23.md` §4): files he drops BY HAND into `_imports/drc/<date>/` ARE imported through the same classifier and the same `place()` path as the upload (NEW row D2-3b); the folder / upload date is the trading log's date; a `partial` file is stored, parsed and shown `PARTIAL — missing: <columns>`. The launch line above is unchanged byte for byte.

WHY: v2 §2 (the import place), §3 (the trigger), T1 `[F-06]`, T2 `[F-08]`, (c) `[F-07]`, (d) `[F-09]`, as HIS rulings reshaped them:
- **R91** — his two daily files are the DAS trading-log CSV and the TradeZella LOG (parsed by D1). **BOTH-PLACED = a `parsed` (or `partial`, R17 (5)) trading log AND a `parsed` (or `partial`) stats log for the date.** Screenshots stay OPTIONAL: a per-trade drop zone binds one to a trade key (embedded by D3), never a gate. (v2's "a screenshot bound to every trade" condition is SUPERSEDED by R91; `ASK DESK` carried below, safe default = this.)
- **R92** — files are saved at `1 - Trading/5 - Review/_imports/drc/<date>/` (vault, synced).
- **R114** — his files carry CSV content under a `.md` extension ("obsidian is blind to that extention") and names he chooses every day → the import path identifies each daily file by its HEADER LINE through D1's ONE classifier (`src/cobalt/drc/detect.py`, `48` D1-2a), NEVER by its name, its extension or a kind picked on the page. Any name, any extension is accepted and saved under his own name. A header matching neither kind → `ignored`, listed, nothing stored; two files of ONE kind in one drop → `FAILED: <kind> ambiguous — <name 1>, <name 2>`, the WHOLE drop refused, nothing stored, neither picked. (The 09-22 "half-matching header → degraded, nothing stored" is SUPERSEDED by R17 (5), next bullet.) `_imports/drc/_reference/` is never an input (the bytes path only ever writes a `YYYY-MM-DD` folder, D2-2; the folder scan reads only `YYYY-MM-DD` folders, D2-3b).
- **R17 of 09-23** (`cto-2026-09-23.md` §4, his words "1)A 2)A 3)folder is a date 4)use stops from tradezella. This is mostly why I'm giving you both. 5) B but needed to say its partial"), the parts D2 builds: **(2)** files he drops BY HAND into `1 - Trading/5 - Review/_imports/drc/<date>/` ARE imported — through D1's ONE classifier and the SAME `place()` pipeline as the `/drc` upload (one parse path, L3; D2-3b); **(3)** the date of the trading log's rows = the `<date>` folder the file sits in, or the upload's `date` field — `place()` passes it to D1's parser; no date column exists; **(5)** a file D1-2a returns as `partial` IS stored and parsed, counts as placed, and the page shows `PARTIAL — missing: <columns>` on its line, loud; the event carries each partial file's `missing` list to the build (D3 renders it). (1) and (4) are D3's / D1's.
- **R93** — "DRC every market trading day. I explain why no trades in drc" → SUPERSEDES R66 for no-trade days: the page carries a **"No trades today"** action for a date that is a market trading day (`daymode.propose.prior_trading_day`'s calendar — re-locate the one trading-day rule by symbol; never a second calendar, L3); it writes a `no_trade` event → D3 builds the no-trade DRC. A parsed trading log with ZERO executions for the date is the SAME no-trade path (no stats log needed that day). A date with a parsed trading log carrying executions REFUSES the no-trade action, naming the count. A non-trading date refuses both.
- **R102** (O14) — an upload or action between 20:00 and 21:00 ET is REFUSED with the reason ("market reset 20:00–21:00 — drop again after 21:00"); nothing stored, nothing queued.
- **R94** — accounts are one bucket (D1); the page never asks which account.
- **R103** (O22) — no upload size ceiling this slice.
- **L31**: event field `import_id` (never `das_import_id`); kinds `trading_log`, `stats_log`, `screenshot`.
- **R101**: no rule checker anywhere on this page.
- **R90**'s RESOLVE action is D5's (it needs C2's leg writer); D2 builds none of it.

## THE ROWS — in this order, nothing else
| row | what | files |
|---|---|---|
| **D2-1** E5 + DEPENDENCY | T: a TestClient multipart `UploadFile` POST against a minimal route using the transitive `python-multipart` (E5). Pass → `pyproject.toml` pins `python-multipart` as a DIRECT dependency at the version `uv.lock` already carries (no new package; `uv run` re-locks — the `uv.lock` diff must show ONLY that package moving from transitive to direct, quoted). Fail → `FAILED: D2-1 — E5 multipart does not parse — <output>` (a new dependency is the desk's to bring). | `pyproject.toml`, `uv.lock` |
| **D2-2** THE BYTES WRITER (L40, `[F-06]`) | A NEW method on `VaultWriter` (the vault-write expert), e.g. `write_import_bytes(rel_path, data: bytes) -> ImportWrite`: path confined to `1 - Trading/5 - Review/_imports/drc/<YYYY-MM-DD>/` (any other path FAILs); create-if-absent ONLY (an existing name → the next free `<stem>.<n><ext>`, old bytes NEVER overwritten or deleted); atomic write + mtime guard as the text path; sha256 computed and recorded on the versioned write row; refused inside `market_reset` by the SAME `_session_gate` as notes. The text `_commit` stays text-only (FC5): this is a separate bytes path INSIDE `VaultWriter`, not a second writer. `vault.py`'s Cobalt-owned `_imports/` location (`vault.py:99-101` — re-locate) gains the `1 - Trading/5 - Review/_imports/` location (R92). | `src/cobalt/vaultwrite/writer.py`, `src/cobalt/vault.py` |
| **D2-3** THE IMPORT HANDLER | `src/cobalt/drc/imports.py`: `place(date, files: list[(filename, data)], trade_key=None)` → market-reset refusal → KIND (R114): with a `trade_key` the file is a `screenshot` (its image header decides, below); without one, `detect_set` of D1's classifier over the drop decides each file's kind from its HEADER alone — `ignored` files are listed and NOT stored; a `partial` file (R17 (5)) is stored and parsed like a complete one, its `missing` list kept; an ambiguous set refuses the whole drop and stores nothing; no kind is ever taken from the name, the extension or the request → bytes via D2-2 (his own file name, any extension) — for an UPLOAD only; a hand-dropped file is already in the folder and is never written again (D2-3b) → `drc_imports` row (kind, name, sha256, trade key, parse status, `supersedes` the prior row of that kind + date + trade key) → parse via D1's sources, the trading log with `import_date = date` (R17 (3): the upload's `date` / the folder's date — nothing read from the file) (screenshot: zero bytes or not a PNG / JPEG header FAILs that file; its trade key must be a trade of the current parsed trading log, else FAILs and binds nothing — `[F-07]`) → per-file `parsed` / `partial` (`PARTIAL — missing: <columns>`) / `failed` + reason + line → recompute the date's STATE, deterministic, no LLM (L2): `waiting for: trading log` · `waiting for: stats log` · `READY` · `no-trade` → on READY or no-trade write ONE event on the `drc_imports` event columns `DrcInputsPlaced{date, import_id, stats_import_id, screenshot_import_ids[], sha256s, partial: {<import id>: [<missing columns>]}, kind ∈ {trades, no_trade}}` (`partial` empty when every file is complete, R17 (5)), state `pending` → call the build (`[F-08]`: in the request; the event row is the state record; `done` only after the build returns; any exception → `failed` with the reason; never `done` on an exception). THE BUILD IS D3's: D2 calls ONE registered entry `cobalt.drc.build.run_drc_build(event)`; until D3 exists that name raises, so the event lands `failed: build not built (D3)` — loud, and never deployed (D2 + D3 ship together). A superseding upload of a kind re-fires the event for the date (v2 §3 re-import). **X13** (v2 §10, before D2): a superseding trading log that CHANGES or DROPS a trade key a screenshot was bound to → the binding is listed `orphaned: <file> — trade <key> not in the current trading log` on the page and on the event (never silently re-bound, never deleted) — the test is the proof; the result is written into the report as X13's. | `src/cobalt/drc/imports.py` (new), `src/cobalt/drc/store.py` (event + supersede methods only) |
| **D2-3b** HAND-DROPPED FILES (R17 (2), L3) | `scan_folder(date)` in `imports.py`: `date` must be a real `YYYY-MM-DD` (else FAILs, reads nothing); lists the TOP-LEVEL files of `1 - Trading/5 - Review/_imports/drc/<date>/` only (sub-folders, `_reference/` and any non-date folder are never read); a file whose name AND sha256 already sit on a `drc_imports` row for the date (an upload D2-2 wrote, or an earlier scan) is skipped and listed `already imported`; every OTHER file goes, as ONE set, into the SAME `place()` pipeline as an upload (D2-3 — the same `detect_set`, the same rows, the same D1 parse with `import_date = date`, the same `supersedes`, the same state recompute and event) with exactly ONE difference: D2-2's bytes writer is NOT called — nothing in the folder is written, renamed, moved or deleted; the row records the file's own name and the sha256 of its bytes as read. A new file of a kind that already has a current row SUPERSEDES it (as an upload re-drop does) and re-fires the event; two new files of one kind → the set refused `ambiguous`, both named, nothing stored; notes, images and other files → `ignored`, listed (an image binds to a trade ONLY through the page's per-trade drop zone — `ASK DESK` below). Refused inside `market_reset` like every action (R102). TRIGGER: the page's action (D2-4); no poller, no scheduled job, no watcher in this build — `ASK DESK` below (L2 would allow a deterministic watcher; its job, plist and RESTARTS are a design step, never invented here). There is NO second parser, classifier or row writer: `scan_folder` calls `place()`, and a test proves the same bytes give the same rows and parse result through either door. | `src/cobalt/drc/imports.py` |
| **D2-4** THE PAGE | `GET /drc?date=YYYY-MM-DD` (default: today ET) and `POST /drc/import` (multipart: `date`, one or more files, optional `trade_key` — NO `kind` field: the day's files go into ONE drop zone and are told apart by header, R114) and `POST /drc/no-trade` (`date`) and `POST /drc/scan` (`date` — "Import the files in the folder", R17 (2), D2-3b) on the existing ASET FastAPI app, routes placed at the END of `aset/web.py` (D4's route block sits after `/attest`; neither touches the other). The route owns NO side effect (L40): it calls `imports.place` / `imports.scan_folder` / the no-trade function only. `GET /drc` writes nothing: it LISTS the date folder's files not yet on a `drc_imports` row (`in the folder, not imported: <n> — <names>`, a read of names and sha256s only) beside the scan action. Shows, per file: its name, the kind its header was detected as, ✓ parsed / `PARTIAL — missing: <columns>` (stored, parsed, loud — R17 (5)) / FAILED with reason + line / `ignored: … header matches neither kind` / `already imported` / `FAILED: <kind> ambiguous — <names>`; counts: executions, trades, open positions, stats rows matched / unmatched, screenshots bound / trades, trades without a screenshot, cards with no trade (from `AsetStore.for_date`, read only); the status line exactly one of `waiting for: trading log` · `waiting for: stats log` · `READY → DRC built: <note path>` (only when the event is `done`) · `DRC build FAILED: <step> — <reason>` · `refused: market reset 20:00–21:00 — drop again after 21:00` · `no-trade day recorded → DRC built: <note path>`. The trades list carries one drop zone per trade (drop, click or paste — trade-reporter's per-ticker zones, `90 - References/trade-reporter/` PATTERN only, no SMB field set). No PDF. | `src/cobalt/aset/web.py`, the ASET page template (a new `drc` template beside the existing one) |
| **D2-5** E11 | T: a stub build that raises after `pending` → the row is `failed`, the page is not `DRC built`; a stub that returns → `done`. (The dead-REQUEST kill of E11 is re-proven by D3 on the real build.) | tests |
| **D2-6** DEVDOCS | new `drc/imports.md`; one paragraph each for `writer.py`, `vault.py`, `web.py`, `store.py`. | `docs/40 - DevDocs/cobalt/**` |

NOT IN D2: the note, the template reader, units, derived rows, the 15:40 retirement (D3); settings (D4); leg corrections + RESOLVE (D5); a heartbeat item (not built — v2 "may"); LAN reach from the trading PC (E7: an OPS read after the deploy — `aset.local.yaml` bind; never a precondition).

## INDEX CARD
1. LAWS.md IN FULL. Binding here: **L1** a dead request is `failed`, never `done`; one input writes no note; a bad file names its line; a `partial` file is loud on the page (R17 (5)). **L2** both-placed is deterministic code in the request, no poller, no LLM. **L3** one import place, one bytes writer inside `VaultWriter`, one trading-day calendar, ONE `place()` pipeline behind both the upload and the folder scan (R17 (2)). **L9** each input through D1's source interfaces. **L18** the event is a state machine. **L28** the bytes are create-if-absent, versioned, atomic, sha256'd, refused in `market_reset`; `_imports/` is Cobalt-owned (R3 of 09-06). **L32** fixtures only D1's stripped ones or your constructed bytes. **L40** the route owns no side effect. **L45** tests run on D1's real-shape fixtures. **L57** the event carries the import ids + sha256s it was built from. + `48`'s INDEX CARD list.
2. v2 §2, §3, §9 D2, §10 E5 / E7 / E11 / X13, §12.
3. `cto-2026-09-22.md` §4 R90–R103, R114; `cto-2026-09-23.md` §4 R17 (`reports/open-items-2026-09-23.md` items 1–5).
4. D1's report on your base (its `## E1` and `## D1-2a` — the classifier you call) (the models, `DrcStore`, the event columns) and `docs/40 - DevDocs/cobalt/drc/*.md`.
5. Code: `src/cobalt/vaultwrite/writer.py` (`_session_gate`, `assert_writable`, `_commit`, `create_if_absent`), `src/cobalt/vault.py`, `src/cobalt/aset/web.py` (`request.form()` sites, `_render`, `/attest`), `src/cobalt/aset/store.py` (`for_date`), `src/cobalt/daymode/propose.py` (`prior_trading_day`), `docs/90 - References/trade-reporter/` (`app.js`, `index.html` — pattern only).

## AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk (drafted by `drc-build-draft-0922`, 2026-09-22), not by Dejan. Each its own Bash call on `"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"`: `grep -n "^| R114 "` (`obsidian is blind to that extention`; committed: `git -C /Users/cobalt/cobalt log -1 --format=%H -S"obsidian is blind to that extention" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY) · `grep -n "^| R91 "` (`Tradezella log for the day`) · `grep -n "^| R92 "` (`_imports should be a subdirectory of 1 - Trading/5 - Review`) · `grep -n "^| R93 "` (`DRC every market trading day`) · `grep -n "^| R94 "` (`DRC is account agnostic`) · `grep -n "^| R102 "` (`1.A 2. A 3.A 4. B`) · `grep -n "^| R103 "` (`It's a small .csv`) · `grep -n "^| R32 "` (`claude-opus-5-5`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"DRC every market trading day" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY · on `"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"`: `grep -n "^| R17 "` (`3)folder is a date`) and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"3)folder is a date" -- "docs/40 - DevDocs/reports/cto-2026-09-23.md"` NON-EMPTY. THIS launch = row **R__** (`grep -n "49-drc-d2-build.md"` on the launch day's desk file → `| R` row; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"49-drc-d2-build.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` NON-EMPTY). `.env` strings: `Bash(rm /Users/cobalt/cobalt-wt/drc-d2/.env)` ≥1 on the launch day's desk file AND committed. Every other string ≥1 in `33-setups-fix-r3.md`. Any miss → `FAILED: authorization mismatch — <what>`, stop.

UNATTENDED RULES and RECOVERY RULE: `48`'s, with `drc-d2` / `D2-<n>` / `wip(drc-d2)`.

REPORT (L48): `/Users/cobalt/cobalt-wt/drc-d2/docs/40 - DevDocs/reports/drc-d2-build-2026-09-2<n>.md`, committed on the branch; sections as `48`'s (`## D2-1` … `## D2-6`, with `## D2-3b` after `## D2-3`, plus `## X13` with its result, no `## E1`).
STOP LINE (L71): `DRC D2 BUILT <tip> | on <base> | offline <p>/<f> | with-DB <p>/<f> | E5: <PASS|FAIL> | X13: <orphaned|other: …> | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` — or `FAILED PREFLIGHT: <rule>` / `FAILED: D2-<n> — <command> — <reason>`.

## STEP-0 — PREFLIGHT, BASELINE
PREFLIGHT rows: `date` · BASE TIP filled · `tail -n 3 "/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-d1-build-2026-09-2<n>.md"` → last non-blank line starts `DRC D1 BUILT ` (or D1's fix round's `… BUILT `) with tip = BASE TIP, else `FAILED PREFLIGHT: D1 has not built on this tip — <line>` · **E1 GATE (R92 / R114 — D2 parses his files through D1's sources; BY HEADER):** `ls "/Users/cobalt/Vault/Think/1 - Trading/5 - Review/_imports/drc"` → a `YYYY-MM-DD` folder (`_reference/` skipped, never read); `ls -la` of the newest one; the Read tool with `limit: 1` on each file there, classified by hand against the REQUIRED-NAME constants of `src/cobalt/drc/trading_log.py` / `stats_log.py` on your base (D1-2a's rule) → exactly one trading-log header and one stats-log header, whatever the names and extensions; none of a kind → `FAILED PREFLIGHT: E1 not dropped — <what the headers show>`; two of one kind → `FAILED PREFLIGHT: E1 ambiguous — <kind> — <name 1>, <name 2>` · `ls /Users/cobalt/cobalt-wt/drc-d2/tests/fixtures/drc` → D1's fixtures present · clean tree, branch `drc/d2-import`, tip = BASE TIP · `.env` absent · DEV-DB STAGGER · `uv run cobalt jobs restarts <base>..HEAD`.
BASELINE: OFFLINE `uv run pytest -q tests/cobalt -p no:cacheprovider` → `failed` 0; WITH-DB inside the `.env` pair: `COBALT_ENV=dev uv run pytest -q -p no:cacheprovider -rs tests/cobalt/test_tenancy.py tests/cobalt/test_drc_store.py` + every with-DB vaultwrite / aset test (`grep -l "cobalt_dev\|COBALT_ENV" tests/cobalt/test_vault*.py tests/cobalt/test_aset*.py`, each by path) → `rm` → "No such file".

## ROW SHAPE — `48`'s; tests in `tests/cobalt/test_drc_imports.py` (offline, `tmp_path` vault, TestClient) and `tests/cobalt/test_drc_imports_db.py` (with-DB). Required, at least: bytes path confinement (a path outside `_imports/drc/<date>/` FAILs); create-if-absent + suffix on a second drop, old bytes byte-identical after; sha256 on the write row equals the bytes'; `market_reset` refusal of upload, of no-trade, and of the bytes method directly (patched clock); both-placed state for every order of arrival (log first, stats first, re-drop); a superseding upload re-fires the event and keeps the old row; one input → no event, page says what is missing; no-trade on a trading day with 0 executions → `no_trade` event; no-trade refused on a date with executions (count named) and on a non-trading date; screenshot bad header / unknown trade key FAILs and binds nothing; **R114** — D1's fixtures dropped under constructed names ending `.md`, `.csv` and no extension, together in one drop and one at a time → each stored under its own name with the kind its HEADER shows (the request carries no kind); a markdown note in the drop → `ignored`, listed, not stored, the other files proceed; two trading logs in one drop → the whole drop refused `ambiguous`, both named, nothing stored; **R17 (5)** a fixture with one required column removed → `partial`, STORED and parsed, the page line reads `PARTIAL — missing: <that column>`, it counts as placed, and the event's `partial` map names it; **R17 (3)** the same trading-log bytes dropped for two dates → each row set carries its drop's date; **R17 (2) / D2-3b** (files written into the `tmp_path` folder by the TEST, never through `VaultWriter`) — D1's two fixtures hand-placed under `.md` names → `scan_folder` stores and parses both, the state READY, one event; the folder's file list and every file's bytes byte-identical before and after the scan (no write, no rename, no suffix); a second scan → nothing new, both `already imported`; an UPLOAD then a scan → the uploaded file is not re-imported; a hand-placed file replaced by new bytes under the same name → a superseding row + the event re-fired; two new trading logs in the folder → `ambiguous`, both named, nothing stored; a markdown note and a PNG in the folder → `ignored`, listed; a `_reference/` folder and a sub-folder with a fixture inside → never read; a non-date `date` → FAILs, reads nothing; the scan inside `market_reset` → refused with the reason; ONE-PATH PROOF — the same fixture bytes through the upload and through the scan give equal `drc_imports` kind / status / sha256 and equal parse results, and `scan_folder` reaches the parse only through `place()` (a spy or a patched `place` shows the call); `GET /drc` stores nothing (row count before = after) and lists the not-yet-imported files; X13; E11 stubs; `/attest`, `/size`, `/fill` tests untouched and green.

## CLOSE — quoted VERBATIM
OFFLINE + WITH-DB (baseline set + `test_drc_imports_db.py`) → `failed` 0 → `rm` → "No such file" · `git diff --stat <base>` → only the rows' files + `pyproject.toml` + `uv.lock` + tests + DevDocs + report · `git diff <base> -- uv.lock` → quote whole: only `python-multipart`'s direct marker · `git diff <base> -- src/cobalt/prefill src/cobalt/replay src/cobalt/cards src/cobalt/radar src/cobalt/aset/engine.py src/cobalt/settings configs src/cobalt/db_migrations` EMPTY · `git diff <base> -- src/cobalt/vaultwrite/writer.py` quoted whole: the text `_commit` path byte-unchanged · `uv run cobalt jobs restarts <base>..HEAD` VERBATIM (expect `com.cobalt.aset`) · L68 table for `aset/web.py`, `vaultwrite/writer.py`, `vault.py` across unmerged branches.
`## ESCALATE` ALWAYS: (i) `ASK DESK: R91 made both-placed = trading log + stats log; screenshots are optional per-trade embeds, not a gate — confirm, or must every trade carry a screenshot before the DRC builds? [<time>]` (default taken: optional); (ii) `ASK DESK: R93 — the no-trade DRC is created by his "No trades today" action (or a zero-execution trading log); nothing creates it unprompted — confirm? [<time>]` (default taken: his action); (iii) E5 and X13 results; (iv) E7 is an ops read after the deploy (which aset config production loads; a >1 MB file POSTed from the trading PC) — not run here; (v) RESTARTS; (vi) R17 (2), ALWAYS: `ASK DESK: R17 (2) — hand-dropped files import when he presses the page's scan action; should a deterministic watcher (L2) import them with no click, as its own design step (a job, its plist, RESTARTS)? [<time>]` (default built: the page action only) and `ASK DESK: R17 (2) — an image he drops by hand is listed ignored; it binds to a trade only through the per-trade drop zone (which writes a second copy) — confirm? [<time>]` (default built: listed, not bound); (vii) every other `ASK DESK`; (viii) `MEMORY:` / `RULING:` lines.
Stop line LAST, report committed by path (`git commit -m "feat(drc): D2 — report"`), `git status --porcelain` EMPTY, stop. NEXT, not yours: `54-drc-d2-check.md`; D3 (`50`) stacks on this tip.
