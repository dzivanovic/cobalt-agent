MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `deploy-fold-read-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/24-draft-deploy-fold-read.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control deploy-fold-read-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `04` / `08` / `11` / `14` / `18` / `22`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool, the REPORT LAST · METER: Anthropic small · SMALL AND FAST: stop within 12 minutes — a production deploy waits on this read · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-2 HOUSE READ of the stacked deploy prompt — THE FOLD ONLY (two P12 / smoke sentences), 2026-09-22

WHY: `05-stacked-deploy.md` (read by Grok + Gemini this morning, `reports/stacked-deploy-review-2026-09-22.md`, 6 folds applied 09:0x) launched at 11:00 under his R3 and ended `FAILED PREFLIGHT: radar probe already red — the deploy could not be judged · rollback: not used` at 11:06 (`reports/deploy-2026-09-22.md`): the heartbeat's radar probe read `RED radar failed_stage bars: poll failures: 1` — ONE carried per-ticker bars-poll failure record (ticker ILAG, `reason: stale`, since 11:02 ET; the pool row `degraded: false`; `com.cobalt.radar` running, heartbeat fresh, cycles every ≈3 min). That RED is the 09-21 R21 / R25 per-ticker degraded design (`src/cobalt/radar/runner.py:376` stamps `failed_stage='bars'` while ≥1 record is carried; `src/cobalt/radar/poller.py:67` pops a record only on a successful fetch of its own ticker or when it leaves membership; `src/cobalt/heartbeat/probes.py:106` reports any `failed_stage` as RED). It will recur on any RTH deploy day and says nothing about whether the deploy can be judged. THE DESK FOLDED TWO SENTENCES into `05` (L19, tagged `[fold, desk 11:2x …]`): P12's radar-RED stop now EXCEPTS a radar line whose ONLY finding is `failed_stage bars: poll failures: <n>` (recorded as `<pf0>`, baseline, named), and smoke (e) keeps that line baseline whatever `<n>` reads after the deploy, RED only on a finding of another kind or a radar not `running … heartbeat fresh`. L67: the desk's own deploy prompt is read by other houses before it runs — THIS round reads ONLY the fold. LAW STEP: deploy prompt read (round 1 DONE) → FAILED PREFLIGHT → fold → **THIS = round-2 read of the fold** → relaunch `05` (`# SECOND RUN`).

## INDEX CARD — read these, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L1 (fail-loud: the fold must not hide a real radar failure), L9 (loud degradation — the per-ticker banner stays), L19, L35, L48, L62, L63, L66, L67 (round 2 reads the fold; ≤3 rounds), L70, L71. One line each in the prompt you write.
2. THE ROUND-1 READ, the shape you copy: `prompts/2026-09-22/06-review-stacked-deploy.md` (Grok + Gemini one-round read of `05`; its launch line = yours BYTE FOR BYTE except the path and the remote-control name `stacked-deploy-review-r2-0922`; its staging root, its house spellings, its collation and stop line) and its report `reports/stacked-deploy-review-2026-09-22.md` (`## Blockers`, the hub's file-check — the 6 folds already applied are NOT re-read).
3. THE FOLD: `prompts/2026-09-22/05-stacked-deploy.md` — `grep -n "fold, desk 11:2x" <file>` gives the two lines; read each with ±6 lines of context (P12's baseline bullets; smoke (e) with (d) and (f) around it) — the packet is EXACTLY those two excerpts plus the P12 paragraph whole and the STEP-4.5 smoke section whole, byte-identical (`wc -c` proven), nothing else of `05`.
4. THE EVIDENCE the houses judge against (excerpts, byte-identical, headed by path + line range): `src/cobalt/radar/runner.py:355-380` (the pool row the runner writes: `failed_stage`, `failed_detail`, `degraded`, `poll_failures`), `src/cobalt/radar/store.py:245-290` (`stamp_poll`: clearing only a recovered bars failure), `src/cobalt/radar/poller.py:55-90` (the carry / drop rule), `src/cobalt/heartbeat/probes.py:85-125` (the radar probe: every finding it can emit), `reports/deploy-2026-09-22.md` `## PREFLIGHT` (the 11:06 run's P12 rows verbatim), the pool row as the desk read it at 11:1x (quote from `cto-2026-09-22.md`'s 11:00 bullet: `poll_failures: [{ticker: ILAG, reason: stale, since 2026-09-22T15:02:06Z}]`, `degraded: false`).
5. `cto-2026-09-22.md` §4 R3 (his daytime-deploy ruling), R10 / R11 (the strings and the launch row — unchanged by the fold: NO new string), R9 (round 1's launch row); `cto-2026-09-21.md` R21 / R25 (the per-ticker degraded design he ruled), R39 (`grok` / `agy` through 2026-09-22 23:59 ET).

## THE JOB — two files, Write tool, the report LAST
(a) `docs/40 - DevDocs/prompts/2026-09-22/25-review-stacked-deploy-r2.md` — ONE complete block on `06`'s shape, launch line FIRST (`06`'s BYTE FOR BYTE but the path and `--remote-control stacked-deploy-review-r2-0922`; cwd `~/cobalt-wt/agy-trial`; Sonnet 5; `auto`; NO new string — state it), index card SECOND; staging in a NEW subfolder under `06`'s approved root (`…/stacked-deploy-review/r2/`, first Write creates it, no `mkdir`); PREFLIGHT = `date` (R39's date gate) · `grok --version` · `agy --version` · the STAGGER on the house lane: `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-r2-2026-09-22.md"` must start `FLOAT HANDICAP TRIBUNAL R2 DONE` or `FAILED`, OR the file must not exist (then `19` has not launched — record it, continue); anything else → `FAILED PREFLIGHT: 19 is running — the desk staggers`. THE QUESTIONS, and only these, each answered `RUN IT` / `DO NOT RUN — <the concrete failing scenario, file:line>` / `RUN IT AFTER <one fold, verbatim-ready>`: (Q1) Is the P12 exception SAFE — can a radar that cannot be judged (scans stopped, a stage failure, a degraded source, a mirror failure, a lifecycle refusal that ALSO stamps `failed_stage='bars'` with a different `failed_detail`) still pass it? Walk `probes.py` and `runner.py:300-315` (the lifecycle refusal stamps `failed_stage='bars'` with the refusal text — does the exception's "ONLY finding is `poll failures: <n>`" wording exclude it?). (Q2) Is smoke (e) still a real smoke for the radar — what radar failure caused BY this deploy (the radar is not restarted by this set; `05` 2.7 proves its paths have an empty diff) could hide behind the baseline line? (Q3) Does the fold change any string, any step order, or the residents-down window (L66) — expected: no. Each house also lists every OTHER sentence of `05` that mentions the radar probe or the heartbeat and says whether the fold leaves it consistent (grep the packet). REPORT `docs/40 - DevDocs/reports/stacked-deploy-review-r2-2026-09-22.md` on `06`'s layout; the hub FILE-CHECKS every DO NOT RUN / RUN IT AFTER claim against the four source excerpts (`HOLDS / DOES NOT HOLD`); stop line on `06`'s shape: `STACKED DEPLOY REVIEW R2 DONE · houses: <n> of 2 · blockers: <n> · folds: <n>` (`blockers` = the DO NOT RUN claims that HOLD in the file-check). The desk relaunches `05` on `blockers: 0` after folding any `RUN IT AFTER` wording.
(b) LAST, YOUR REPORT `docs/40 - DevDocs/reports/deploy-fold-read-draft-2026-09-22.md`: `## DIGEST` (≤8 lines), `## RULE PROOF` (`25`'s strings `grep -c -F` against `06`'s line — every count ≥1; new: 0 expected), `## ESCALATE` (always: your OWN reading of Q1 — does the lifecycle-refusal path (`runner.py:305-311`) share the `failed_stage='bars'` stamp, and does the fold's "ONLY finding" wording exclude it? state it with file:line; the desk reads it before the houses do), and your LAST LINE: `DEPLOY FOLD READ PROMPT DRAFTED · questions: 3 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. Never invent a flag: every command shape appears in `06`. You change NOTHING in `05` (the desk owns its fold). `date` for every time you write. Stop within 12 minutes; your stop line is your report's last non-blank line, written LAST.
