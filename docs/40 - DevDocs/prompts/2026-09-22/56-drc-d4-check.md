MODEL: Sonnet 5 (`claude-sonnet-5`) — stage ONE packet before FOUR checkers, file-check, tabulate; no verdict of your own (L37) · SEAT: build-check hub `drc-d4-check`, launched by the CTO desk in the background · LADDER `S3-P3 · F14` · LAW STEP (L67): build `51-drc-d4-build.md` → **THIS = CHECK ROUND 1 of at most 3, the WHOLE D4 build** → fix round (L75) if a defect HOLDS. D4 is BUILT + CHECKED before D1 stacks on it; D4 may ride an earlier evening's deploy on its own if the desk finds it ready (L43: a ready branch is never held) — the daily note then reads `not given` until his value is loaded. RULE STRINGS: `53-drc-d1-check.md`'s line BYTE FOR BYTE; only the prompt path and remote-control name differ; NO NEW STRING. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/56-drc-d4-check.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control drc-d4-check --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/tribunal-bars-0920)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" "Bash(claude -p --model claude-opus-5-5 *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode (read-only seat; stated here and on the line); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO database, NO `pytest`, NO git write, no vault write, no memory-folder write · METER: as `53` · nobody sits at this terminal. DESK: watch the LAST NON-BLANK LINE, `^(DRC D4 CHECK DONE|FAILED)`.

# BUILD CHECK, ROUND 1 — DRC D4: THE DRC SETTINGS FAMILY, ONE SOURCE FOR THE DAILY STOP AND THE DOLLARS PER GRADE, HIS CHANGE LINE IN ASET, THE DAILY NOTE READS THE KEY. Everything in `53-drc-d1-check.md` binds you UNCHANGED, with ONLY these substitutions: `d1` → `d4` in every path, name and line (`drc-check/d4/`, `/Users/cobalt/cobalt-wt/drc-d4`, `drc-d4-build-2026-09-2<n>.md`, `drc-d4-check-2026-09-2<n>.md`, `DRC D4 BUILT `, `CHECK D4:`, `DRC D4 CHECK DONE`, rows `D4-1 … D4-7`); launch-row filename `56-drc-d4-check.md`, row **R__**; the build prompt is `51-drc-d4-build.md`; the stagger path filled by the desk here: **`__`**; `rulings.md` = §4 rows R95, R96, R101, R102; `design.md` = v2 §5 (T9 `[F-18]`, `[F-16]`), §9 D4, §10 E10 / X12, §12; the (ii) boundary check = `git -C /Users/cobalt/cobalt log --oneline <base>..<tip> -- src/cobalt/cli.py src/cobalt/drc src/cobalt/db_migrations src/cobalt/aset/engine.py src/cobalt/cards src/cobalt/radar` EMPTY; (iii) = the old daily-stop literal: read `git -C /Users/cobalt/cobalt show <base>:configs/cobalt/templates/daily.md.j2` (the line with the daily-stop heading text), then `grep -rn -F "<that literal>" /Users/cobalt/cobalt-wt/drc-d4/configs /Users/cobalt/cobalt-wt/drc-d4/src /Users/cobalt/cobalt-wt/drc-d4/tests` → 0 hits — report `old literal: <hits>` WITHOUT the value (L32), and never stage the base template's line; (iv) = `grep -rn "sheet_modes\|daily_stop" /Users/cobalt/cobalt-wt/drc-d4/src/cobalt` → every reader placed (ONE reader on the DRC / daily path); the `ready` field reads `ready for the next chunk`.

QUESTIONS.md (verbatim): "You are one of four checkers of ONE BUILD CHUNK (round 1 of at most 3). The sentence that launched you says which house you are. Read ONLY the files in this folder. You cannot run commands. The chunk adds the Daily Report Card's settings keys (optional keys of a per-trader settings table: the daily stop, a card-match window, time windows, a goal metric) with a validated schema per family; ONE reader that returns the daily stop and the dollars per grade (the dollars per grade are an EXISTING settings row that also drives position sizing — no second copy); the committed daily-note template now shows the daily stop from that reader instead of a literal number; and a 'change line' on the existing trading web app where the trader edits his daily stop and dollars per grade himself: the page shows a per-key diff and a hash, Apply writes through the SAME function the command-line settings loader uses, then reads it back; refused during the 20:00–21:00 ET market reset. The owner ruled: no rule-to-checker map in this build (R101). The design is `design.md`, the rulings `rulings.md`, the prompt `prompt.md`, the build `build-diff.md` + `devdocs.md`, the builder's claims `build-report.md` (a CLAIM). The contract: *an absent key is `not given`, never a default or fallback; one reader, one write path; tests never assert a value read from the live settings table; the sizing math is never touched; no value of the owner's in the repository*. For EACH row D4-1 … D4-7 of `prompt.md`, exactly one of, each with `file:line`: `MEETS THE ROW` / `DOES NOT MEET — <file:line, the concrete input that shows it>` / `NEW DEFECT — <file:line>`. Look hardest at: **optional, never required** — a missing DRC key can never break sizing (`/size` still serves, X12), and the new keys are not in the required tuple; **one source** — is there any second place (a template literal, a config file, a rules sentence, a copy under a new key) the daily stop or the grade dollars are read from? **the change line** — can Apply write without the diff's hash matching, inside the market reset, with a bad field, or partially (one key written, one not)? does it share the CLI's write function or duplicate it? is the read-back real? are values ever logged? **the daily template** — absent key renders `not given`, the literal is gone; **E10** — did the new key land without a migration, proven on the dev database? THEN over the whole diff: (a) weak assertions, (b) changes outside the rows' files (the attestation and sizing routes untouched), (c) L52 — can anything here change a size or a grade other than his own typed value through the existing sizing row. `NOT CHECKABLE FROM READS — <what would have to be run>` is not a defect. End with EXACTLY one line: `CHECK D4: BUILD STANDS` / `CHECK D4: BUILD STANDS EXCEPT <items>` / `CHECK D4: FIX <items>` — followed on the SAME line by ` · ready for the next chunk: YES|NO · <ten words of reason if NO>`."

STOP LINE (L71): `DRC D4 CHECK DONE · grok: <…> · gemini: <…> · sol: <…> · opus: <…> · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the next chunk: <r> of <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`. NEXT, not yours: ≥3 checked, 0 HOLD → D1 (`48`) stacks on D4's tip once E1 is dropped; else a fix round (L75).
