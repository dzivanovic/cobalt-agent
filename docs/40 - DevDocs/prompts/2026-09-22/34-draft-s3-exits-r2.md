MODEL: Opus 5.5 (`claude-opus-5-5`, R36) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `s3-exits-r2-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/34-draft-s3-exits-r2.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control s3-exits-r2-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `08` / `11` / `14` / `18` / `22` / `24` / `26` / `30`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the FOUR files named below with the Write tool, the REPORT LAST (09-22 lesson 3) · METER: Anthropic small–medium (the week's meter is HIGH, his R58 — read by section) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) ROUND 2 of the S3 exits tribunal — ONLY the three items of `## NEEDS ROUND 2`. LADDER: `S3-P1 · F11` + `S3-P2 · F22` (S3 opens 09-24). LAW STEP (L67): proposal DONE → round 1 DONE (Grok + Gemini via hub `73`, the Fable seat `74`; Astra METER — `cto-2026-09-22.md` R13: proceed on three, Astra reads v2 Sat 09-26) → derive v2 DONE (`S3 EXITS DERIVED v2 · folds: 42 · verbatim: 26 · needs round 2: 3 · owner items: 20 · ESCALATE: 8`; `docs/30 - Design/S3-EXITS-v2-2026-09-22.md`) → **THIS = drafting round 2** (his R62, 14:4x ET: "A" — run it this week on Grok, Gemini and Opus) → a second derive closes the tribunal (or L39 → Dejan for what does not converge) → the S3 build prompts.

## INDEX CARD — read these, in this order, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L19, L32 (no traded ticker or price of his in a packet — `[TICKER]` / `[PRICE]`, as round 1 did), L35, L37, L39 (a design split that does not converge → Dejan, never a vote), L48, L52, L53, L57, L62, L63, L67 (four houses; ≤3 rounds; a non-ruling stop spends no round), L70, L71, L72. One line each in the prompts you write.
2. THE ROUND-2 PRECEDENT, the shape you copy: `prompts/2026-09-22/18-draft-handicap-r2.md` (this file's own shape) and the three it produced — `19-handicap-tribunal-r2.md` (three-house hub, round 2 reads the FOLD only), `20-handicap-tribunal-fable-seat-r2.md` (the blind Fable seat, round 2), `21-handicap-tribunal-derive-r2.md` (the second derive); their reports `reports/handicap-r2-draft-2026-09-22.md`, `float-handicap-tribunal-r2-2026-09-22.md`.
3. THIS TRIBUNAL'S ROUND 1: `prompts/2026-09-21/73-s3-exits-tribunal.md` (the hub's launch line, packet layout, `r1/` staging, the astra probe, its date gate), `74-s3-exits-tribunal-fable-seat.md` (the Fable seat, R57 "Fable seat: yes"), `75-s3-exits-tribunal-derive.md` (the derive; `DERIVE ROW: R36`); the reports `reports/s3-exits-tribunal-2026-09-22.md` (hub round 1: `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files`, `## Experiments named (L70)`), `s3-exits-tribunal-fable-r1-2026-09-22.md`, `s3-exits-tribunal-derive-2026-09-22.md` (`## DIGEST FOR THE DESK`, **`## NEEDS ROUND 2`** — R2-1, R2-2 (a)–(g), R2-3 — verbatim).
4. THE DESIGN: `docs/30 - Design/S3-EXITS-v2-2026-09-22.md` (the round-2 packet's base: the `[F-nn]` folds and the `ROUND 2` markers). The code the three items turn on — read ONLY what the derive's `## NEEDS ROUND 2` cites by `file:line` (the fill path `mark_filled` / `store.fill`, `refuse_row_update()`, the stop-edit share count, the migration sets `src/cobalt/db_migrations/`, `cards/migrations/`, `aset/migrations/`).
5. `cto-2026-09-22.md` §4 R13 (no Astra this week — the astra string carried, probe recorded, SKIPPED), R30 (`grok` / `agy` extended through 2026-09-23 23:59 ET — the round-2 hub's DATE GATE uses R30's literal), R34 / R35 / R36 (the round-1 and derive launch rows; R36 = every Fable-type seat on `claude-opus-5-5` until his evaluation), R58 (build lane only this week — this round is IN lane by R62), R62 (this round).

## THE JOB — four files, Write tool, the report LAST
(a) `docs/40 - DevDocs/prompts/2026-09-22/35-s3-exits-tribunal-r2.md` — the three-house ROUND-2 hub on `19`'s shape and `73`'s launch line BYTE FOR BYTE (only the prompt path and the remote-control name `s3-exits-tribunal-r2-0922` change; the astra string CARRIED: probe → `METER` → recorded, skipped, proceed on Grok + Gemini, R13; staging in `scratch/tribunal-bars-0920/s3-exits-tribunal/r2/`, first Write creates it, no `mkdir`). ROUND 2 READS THE FOLD ONLY: the packet = v2's sections that carry R2-1 / R2-2 / R2-3 (the `ROUND 2` markers and the folds they touch), the derive report's `## NEEDS ROUND 2` verbatim, each house's OWN round-1 ruling on those items, and the `file:line` excerpts the derive cites. Each house answers per item `ADOPT <position>` / `NEITHER — <wording>` / `OWNER — <why>`, R2-1 additionally `DO NOT BUILD stands: <defect in v2's text with file:line>` or `withdrawn`. Report `reports/s3-exits-tribunal-r2-2026-09-22.md`; stop line on `19`'s shape: `S3 EXITS TRIBUNAL R2 DONE · grok: … · gemini: … · astra: METER — proceed on three · houses that ruled: <n> of 3 · converged: <n> of 3 · ESCALATE: <n>` or `FAILED …`.
(b) `docs/40 - DevDocs/prompts/2026-09-22/36-s3-exits-tribunal-fable-seat-r2.md` — the blind Fable seat, round 2, on `20`'s shape and `74`'s launch line BYTE FOR BYTE except the path, the name `s3-exits-tribunal-fable-r2-0922` and `--model claude-opus-5-5` (R36 — say so in its MODEL line). `DERIVE ROW: R36` covers this seat. It rules on R2-1, R2-2, R2-3 ONLY, from the same fold (its OWN round-1 report for reference, never the houses' folder — blind), same answer shape. Report `reports/s3-exits-tribunal-fable-r2-2026-09-22.md`; stop line `S3 EXITS TRIBUNAL FABLE R2 DONE · R2-1: <answer> · R2-2: <answer> · R2-3: <answer> · ESCALATE: <n>` or `FAILED …`.
(c) `docs/40 - DevDocs/prompts/2026-09-22/37-s3-exits-tribunal-derive-r2.md` — the second derive on `21`'s shape and `75`'s launch line BYTE FOR BYTE (path, name `s3-exits-tribunal-derive-r2-0922`; `--model claude-opus-5-5`; `DERIVE ROW: R36`). PRECONDITIONS: (a)'s and (b)'s stop lines present and committed. It writes v3 = `docs/30 - Design/S3-EXITS-v3-2026-09-22.md` (v2 with the three items settled where the houses converged — the taken wording verbatim, tagged — and, for an item that did NOT converge, an `OPEN FOR DEJAN` block: the positions verbatim, each house's reason, the A/B the desk brings him, L39 — never a vote, never the seat's own preference), plus its report `reports/s3-exits-tribunal-derive-r2-2026-09-22.md`; stop line `S3 EXITS DERIVED v3 · converged: <n> of 3 · open for Dejan: <n> · owner items: <n> · ESCALATE: <n>` or `FAILED …`.
(d) LAST, YOUR REPORT `docs/40 - DevDocs/reports/s3-exits-r2-draft-2026-09-22.md`: `## DIGEST` (≤12 lines: the three items in one sentence each, new rule strings = 0 expected, the launch order `36` beside `35` → both stop → `37`), `## RULE PROOF` (each launch line's strings `grep -c -F` against its precedent line — `73` for `35`, `74` for `36`, `75` for `37` — every count ≥1; any string not in its precedent = NEW, listed; the ONE expected difference: `--model` on `36`), `## EXPERIMENTS` (the derive's X4 / X6 / X7: which a house could run from reads alone — none expected — and which the S3 build prompt must run first; you launch none), `## ESCALATE`, and your LAST LINE: `S3 EXITS R2 PROMPTS DRAFTED · prompts: 3 · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. L32: no traded ticker or price of his in any file you write — `[TICKER]` / `[PRICE]`. Never invent a flag: every command shape appears in `73` / `74` / `75` / `19` / `20` / `21`. Every OWNER item stays an owner item, never resolved by you. `date` for every time you write. Stop within 25 minutes; your stop line is your report's last non-blank line, written LAST.
