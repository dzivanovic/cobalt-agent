MODEL: Opus 5.5 (`claude-opus-5-5`, R32) — PROMPT DRAFTER: you write the DRC build prompts; you build nothing · SEAT: `drc-build-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/45-draft-drc-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-build-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the seven read-only strings of `39` / `43` — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO DB, docker, pytest, git write, agent launch (L36), vault write, memory-folder write (L58) — you write only the files named below, Write tool · METER: Anthropic (full, R74) · nobody sits at this terminal: `ASK DESK: <question> [<time>]` in your report, safe default, continue.

# DRC AUTOMATION — DRAFT THE BUILD PROMPTS (S3-P3 · F14)

WHY: the DRC design is derived (`docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md`, Fable derive, `reports/drc-tribunal-derive-2026-09-22.md`) and he ruled every open item the same evening: `reports/cto-2026-09-22.md` §4 **R90–R103** (R2-1 settled by R90 — no round 2; all 21 owner items). The build is chunked per v2 (D1–D5, 5 write-path chunks, 1 migration) and lands in S3 (opens Thu 09-24; `docs/00 - Project/SPRINT-LADDER-v0_1.md`). E1 = ONE REAL DAY of his two files (DAS CSV + TradeZella log, R91) — he is dropping them; no chunk that parses them runs before they exist (L45).

## INDEX CARD
Read: (1) `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (applied today — 75 entries; note L31, L45, L62 as amended R80, L67 checker seats, L68, L75) · (2) v2 in full + the derive report's `## FOR DEJAN` and `## ESCALATE` · (3) `cto-2026-09-22.md` §4 R65–R73 and R90–R103 (his words govern where v2 differs; R93 SUPERSEDES R66 for no-trade days; R91 replaces typed MAE/MFE cells with parsing; R94 account-agnostic; R96/R102 the central settings + ASET change line; R101 no rule→checker map) · (4) the most recent build prompt shape to copy: `prompts/2026-09-22/33-setups-fix-r3.md` (launch line, AUTHORIZATION, PREFLIGHT, UNATTENDED RULES, RECOVERY RULE, stop line) and a check prompt `prompts/2026-09-22/16-setups-check-r2.md` · (5) `docs/40 - DevDocs/prompts/UNATTENDED-LAUNCH.md`.
Binding: L1 fail loud · L19 each prompt one complete block · L28 vault writes (marker-bounded units) · L31 NAMES: every `das*` identifier in v2 is renamed (`trading_log.py`, `import_id`, source `trading_log`, flag `trading_log_shape`) — law in force, no question (L73) · L32 no user data in git; the stripped fixture commit is consented with the export (R103 O15) · L45 real-shape fixtures · L54 worktree · L62 as amended (mode stated on the line; interim `auto` dev lane as `33`) · L63 no dialogs · L67 ≥3 checkers per build (Sol + Opus 5.5 + Grok/Gemini, R46) · L68 stacked gate · L71 · L75 fix rounds classify first.

## WRITE
(a) one BUILD prompt per chunk v2 names, numbered from `48-` upward in `prompts/2026-09-22/` (e.g. `48-drc-d1-build.md`), each: launch line first (Opus 5.5, worktree `~/cobalt-wt/drc-<chunk>`, `auto`, the smallest allowlist — reuse `33`'s strings where they fit, and LIST every NEW string separately so the desk can bring ONE approval list to him), AUTHORIZATION grepping R90–R103 + a `R__` launch row, PREFLIGHT (a chunk that parses his files FAILS PREFLIGHT until `1 - Trading/5 - Review/_imports/drc/<date>/` holds both files — R92), rows with their test first, RESTARTS line, stop line `DRC <CHUNK> BUILT <tip> … | ESCALATE: <n>` or `FAILED …`. (b) ONE CHECK prompt per chunk (three checkers, L67). (c) your report `reports/drc-build-draft-2026-09-22.md`: §0 digest (chunks, order, which wait on E1, NEW rule strings list verbatim, hours) → `## ESCALATE` → LAST LINE `DRC BUILD PROMPTS DRAFTED · build prompts: <n> · check prompts: <n> · waiting on E1: <n> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <why>`.
Also fold in, where each belongs: R90's RESOLVE action on `/drc` for an `unresolved: card <id>` line; R93's no-trade-day DRC with his "why no trades" section; R102's settings keys edited through the ASET change line; R103's gross + net summary. `date` for every time. Stop within 100 minutes. You commit nothing.
