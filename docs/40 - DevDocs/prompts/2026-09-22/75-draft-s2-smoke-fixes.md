MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — forensics by READS + DRAFTING seat; no code, no DB write, no launch · SEAT: drafter `s2-smoke-fixes-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/75-draft-s2-smoke-fixes.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control s2-smoke-fixes-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `66` / `71` / `74`, 09-20 R13 — nothing new; NO database query: every DB fact you need is already quoted in the smoke report, or it is named as a hub proof step in the prompt you draft) · SESSION: fresh · auto mode on; never `bypassPermissions`; no vault write, no memory-folder write (L58), no git write — you write exactly the files named below with the Write tool, the REPORT LAST · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps.

# S2 SMOKE REDS → ONE FIX BUILD PROMPT. LADDER: `S2 smoke (cobalt smoke s2)` + `F12 / F13 nightly replay (S2-P4)`. S2 stops 2026-09-23; its close needs ONE green smoke night (09-21 R46), and tonight's replay FAILED for the second night on a NEW bug. LAW STEP: smoke look DONE (`reports/s2-smoke-look-2026-09-22.md`: `replay ran: yes 21:10 FAILED · checks: 23 green / 11 red · reds expected tonight: 4 · real or UNPLACED reds: 7 · … · ESCALATE: 5`) → **THIS = forensics + drafting the fix build** → build (offline worktree off main) → ≥3 checkers (L67; code check seats R46/R49: Opus + Grok + Gemini, Sol METER) → tomorrow evening's ONE deploy (L43: it joins the stacked set with the setups build if both are checked; residents down inside the 20:00–21:00 pause, L66; the fix must be live BEFORE the 21:10 replay).

INDEX CARD: read `6 - Permanent/Memory/LAWS.md` in full (L59). Binding: L1 · L9 · L35 · L42 · L45 (the movers fix is specified against the REAL source row shape that failed) · L46 · L54 · L57 · L62 / L63 · L67 · L68 · L70 · L71 · L73 · L74 · L75 (classify first). Files: `reports/s2-smoke-look-2026-09-22.md` whole; `logs/replay.err` / `logs/replay.out` tails in `/Users/cobalt/cobalt/logs/` (read with `tail`); `src/cobalt/replay/`, the movers source (`grep -rn "is not a percentage" /Users/cobalt/cobalt/src/cobalt`), `src/cobalt/radar/store.py` (the INSERT path of `radar_membership` and `rank_metric`), `src/cobalt/aset/radar_panel.py:373` (the `since` param) and `configs/cobalt/smoke/s2.yaml` K4.4; `docs/PLACEMENT.md` `_inflight` rule and `ls /Users/cobalt/cobalt/docs/_inflight/`; the last deploy prompts of S2-P4 (`prompts/2026-09-21/`, `2026-09-22/05-stacked-deploy.md`) for the build-prompt shape and its approved strings; `topics/cto-desk.md` 2026-09-21/22 lessons.

1. CLASSIFY every smoke ESCALATE 1–5 and every red K-row (L75 classes: FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM), each from the smoke report's quoted output and the code at `file:line`. For ESC 1 (movers): what value the source sends (`Change ''` — an empty percent cell), which rows, whether an empty cell is a legitimate "no change" / missing value that must degrade LOUDLY per record (L9) rather than kill the whole replay (L1 is not "one bad cell fails the night" when the design says per-record degradation — cite the design line either way). For ESC 2 (K3, 66 of 301 INSERT-path rows missing `rank_metric`): defect or a new class needing his ruling. For ESC 3 (K4.4): the check is stale or the endpoint is wrong — which commit changed it and whether the change was ruled. ESC 4 (09-21 miss-line never written) and ESC 5 (`_inflight` strays) → OWNER ITEM / ops item with an A/B in his terms.
2. Write `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/76-s2-smoke-fixes-build.md` — FIX rows ONLY; Opus 5.5 builder (L29), `--permission-mode auto`, NEW worktree `~/cobalt-wt/s2-smoke-fix` on a new branch `s2/smoke-fix-0922` off `main` (worktree creation in the launch step, the shape the setups / stale-marker builds used — copy their approved strings; list every new one); OFFLINE ONLY (the dev DB is BROKEN until `68` lands — every with-DB proof is `OWED (68)` in the stop line); a real-shape fixture of the failing movers row (L45: shape, not his values); RESTARTS derived (L42); stop line `S2 SMOKE FIX BUILT <tip> | on <base> | offline <p>/<f> | with-DB: OWED (68) | RESTARTS: … | ESCALATE: <n>` — or `FAILED …`. Also name the check prompt shape it needs (a `70`-shape re-point) — do not draft it.
3. LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s2-smoke-fixes-draft-2026-09-22.md`: §0 ≤5 lines (can S2 still close 09-23? say plainly: fix built + checked by afternoon, deployed in the 20:00 pause, a green 21:10 replay + smoke look after 21:20) → `## CLASSIFICATION` → `## DIGEST FOR THE DESK` ≤25 lines → RULE PROOF (`76`'s launch line vs the precedent it copies) → `NEW strings:` verbatim (these go to Dejan as ONE list) → `## FOR DEJAN` (owner items, one A/B each, his terms) → `## ESCALATE` → `## CONTINUE` → last line; while you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE: `S2 SMOKE FIXES DRAFTED · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · new rule strings: <n> · S2 by 09-23: <possible|not possible> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape. `date` before any time you write. L32 / L4: no secret, no ticker or value of his in any file you write. Stop within 40 minutes.
