MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `drc-reissue-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/74-draft-drc-reissue.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-reissue-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `66` / `69` / `71`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write only the re-issued prompt files and your report with the Write tool, the REPORT LAST · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps.

# RE-ISSUE the DRC build + check prompts `48`–`57` (L19: full re-issue of each file that changes) for his rulings R114, R116, R117. LADDER `S3-P3 · F14`. LAW STEP: v2 derived → his rulings R90–R103, R105 (strings + grok/agy through 2026-10-07 for `53`–`57`) → prompts `48`–`57` drafted (`reports/drc-build-draft-2026-09-22.md`) → **THIS = their re-issue for R114 / R116 / R117** → D4 launches when S3 opens 09-24; D1–D3, D5 after it.

INDEX CARD: read `6 - Permanent/Memory/LAWS.md` in full (L59). Binding: L1 · L9 · L19 · L28 · L32 (no value, ticker, price or P&L of his in any file you write — you READ his E1 files only to learn their SHAPE: header line, delimiter, column names, time format, side codes; you never copy a value) · L45 · L53 · L62 / L63 (no new rule string unless unavoidable; any new one listed verbatim for his approval) · L71 · L74. Files: `reports/cto-2026-09-22.md` §4 R90–R105 and R114, R116, R117 (VERBATIM — read them first); `reports/drc-build-draft-2026-09-22.md` (the drafter's map + ESCALATE 1–14); `prompts/2026-09-22/48`–`57`; `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md`; his E1 folder `/Users/cobalt/Vault/Think/1 - Trading/5 - Review/_imports/drc/2026-09-18/` (two files, `.md` extension, CSV content); the note titles in `/Users/cobalt/Vault/Think/1 - Trading/4 - Strategies/` (`ls` only); `src/cobalt/taxonomy/slug.py` on `setups/seven-0921` (`git -C /Users/cobalt/cobalt show setups/seven-0921:src/cobalt/taxonomy/slug.py`) — the note-title → slug rule.

WHAT CHANGES (and nothing else):
1. (R114) FILE DETECTION BY HEADER: every E1 gate and every import path identifies the trading-log export and the stats log by their HEADER LINE (the column names E1 shows — record both headers' column NAMES in your report, never a data row), never by file name or extension. Any name, any extension (his files end `.md` because Obsidian hides `.csv`; names change every day). A file whose header matches neither = ignored and listed; two files matching one kind = `FAILED … ambiguous` naming both; a header that half-matches = loud degraded (L9), never a guess. The `_reference/` folder beside the date folders is never read as an input. The fixtures cut from E1 keep the real header BYTE-IDENTICAL (L45) — their committed name may be `.csv`.
2. (R114) SEVERAL PLAYBOOKS ON ONE TRADE: a trade (FIFO position from open to flat, with several entries / exits / stops / sizes) may carry MORE THAN ONE playbook name in the stats log; all are kept, in order, on the trade; nothing picks one. E1's SPCX-shaped case (two setups adding onto each other without closing the first) is the fixture's reference structure — constructed values only.
3. (R114) NO OVERNIGHT CARRY IN E1: the carry fixture is CONSTRUCTED in E1's shape and says so (`48` D1-0 already allows it).
4. (R116 / R117) PLAYBOOK → SETUP = ONE-TO-ONE NAMES: a stats-log playbook name, after stripping ONE trailing ` Long` / ` Short` (case-insensitive, whole word), equals a note title in `1 - Trading/4 - Strategies/` → that setup (by `slug.py`'s rule). Anything else renders `unmapped: <name>` in the DRC and is counted, never guessed, never a failure. No mapping table, no fuzzy match, no LLM (L2 / L57). The note titles are read at run time from the vault folder, never a committed list (L32 / L45).
5. Each changed file keeps its launch line BYTE FOR BYTE (strings R105 approved); if a change truly needs a new string, list it under `NEW strings:` verbatim with the reason.
6. The DRC check prompts (`53`–`57`) get the matching new QUESTIONS (header detection, multi-playbook, the 1:1 name rule) — nothing else.

WRITE: each re-issued file under its SAME name in `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/` (overwrite with Write after reading it whole). LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-reissue-draft-2026-09-22.md`: §0 ≤5 lines → `## E1 SHAPE` (per file: detected kind, delimiter, column NAMES, time format, side codes, row count — no values) → `## CHANGES` (file · what changed · which ruling) → RULE PROOF (every re-issued launch line vs its committed version at `HEAD`: `git -C /Users/cobalt/cobalt show HEAD:<path>` — expected: identical) → `NEW strings:` (expected none) → `## ESCALATE` → `## CONTINUE` → last line; while you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE: `DRC PROMPTS REISSUED · files changed: <n> · header kinds: 2 · playbooks on the E1 multi-playbook trade: <n> · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape. `date` before any time you write. Stop within 40 minutes.
