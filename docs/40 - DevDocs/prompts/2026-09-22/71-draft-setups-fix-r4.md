MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — a classify-then-draft job (L75); no code, no DB, no launch · SEAT: drafter `setups-fix-r4-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/71-draft-setups-fix-r4.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control setups-fix-r4-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `66` / `69`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the FILES named below with the Write tool, the REPORT LAST · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps.

# TWO JOBS, one report. LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults` (S2 stops 2026-09-23; the setups build must deploy before a formed card can exist).

INDEX CARD: read `6 - Permanent/Memory/LAWS.md` in full (L59). Binding: L29 · L32 · L35 · L37 · L39 · L45 · L62 / L63 · L67 · L68 · L70 · L71 · L73 · L74 · L75. Files: `reports/setups-check-r2r3-2026-09-22.md` (the check: `defects that HOLD: 5 · owner items: 2 · ESCALATE: 9`, its `## FOR THE CLASSIFIER`, `## FOR DEJAN`, file-check rows); `prompts/2026-09-22/14-draft-setups-fix.md` + its outputs `15-setups-fix.md` / `reports/setups-fix-draft-2026-09-22.md` (the classify-and-draft shape you copy); `33-setups-fix-r3.md` (the newest fix-build shape, its launch line and strings — `acceptEdits`? read its MODEL / SEAT line and copy the mode it states); `12-setups-fixture-cut.md` + `13-setups-blind-values.md`; the three build reports in `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/`; `68-devdb-repair.md` + `reports/r3-check-devdb-draft-2026-09-22.md`; `25-review-stacked-deploy-r2.md` (the shape of a house READ of a desk prompt); `topics/cto-desk.md` 2026-09-21/22 lessons.

## (A) CLASSIFY (L75) and DRAFT fix round 4
1. Classify EVERY finding of the r2r3 check (its HOLDS rows, `## FOR THE CLASSIFIER` items 1–6, ESCALATE 1–14) as FIX · NOT REAL · UNPROVEN (L70) · OUT OF SCOPE · OWNER ITEM, each from the hub's own file-check row (`file:line`). Only FIX rows are built; each backed by a row that HOLDS; the fix widens nothing. Owner items (Q1, Q2 and any new one) go to `## FOR DEJAN` verbatim, one per item, A/B in his terms.
2. Write `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/72-setups-fix-r4.md` — `33`'s shape and launch line byte for byte (Opus 5.5, the mode `33` states, worktree `setups-c1`, branch `setups/seven-0921`, base tip = `8da261a` or the branch tip you read), FIX rows only. The dev DB is BROKEN until `68` lands: the build is OFFLINE — no `.env` copy, no with-DB run; its with-DB proof is owed after `68` and named as such in its stop line (`with-DB: OWED (68)`). If a FIX row CANNOT be proven offline, say so and mark it for the with-DB re-run. Stop line `SETUPS FIX R4 BUILT <tip> | on <base> | offline <p>/<f> | with-DB: OWED (68) | tests added: <n> | ESCALATE: <n>` — or `FAILED …`.
3. The NEXT check is round 3 of 3, the LAST (L67 / L39): name in your report that `70`'s shape re-pointed to r4's range is the check, and that `13` (the blind re-derivation of the cut's five new pins) runs AFTER the cutter's hour-shift fix (check ESCALATE 9) — draft neither; list the re-point lines.

## (B) DRAFT the house READ of `68-devdb-repair.md` (L67 floor, 09-18 R8: a desk prompt with a DB-recovery step is read by ≥1 other house before it runs)
Write `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/73-review-devdb-repair.md` — `25-review-stacked-deploy-r2.md`'s shape and launch line byte for byte (Sonnet 5 hub in `~/cobalt-wt/agy-trial`; Grok + Gemini read; Opus is the author's house and does NOT count as the other house), only the prompt path and remote-control `devdb-repair-review-0922` differ. Packet: `68` whole, `src/cobalt/devdb.py`, `src/cobalt/env.py` (`assert_destructive_target`), the tenancy / migration test that grows the dropped columns (`tests/cobalt/test_tenancy.py` `TestMigrationRoundTrip`), and the drafter's ESCALATE (a)–(f). Questions: (1) can any string of `68` reach `cobalt_brain` or production (container, DSN, database name, the deny strings' coverage); (2) is pg_dump + restore the smallest repair that frees the dropped-column slots (vs. a table-level recreate); (3) is the rollback (rename back) sound, and is `cobalt_dev_bloated_0922` kept safely; (4) does `db migrate` after restore leave `cobalt_dev` at main's head with grants / schemas / search_path as production's shape; (5) is every command the hub runs in the allowlist verbatim (L63 — `acceptEdits` ASKS on an unlisted Bash). DATE GATE R30 (through 2026-09-23 23:59 ET). STAGGER: refuses while `59`, `63` or the r4 check is in progress without a stop line (launch-row `<nn> is not running` rule). Report `reports/devdb-repair-review-2026-09-22.md`; stop line `DEVDB REPAIR REVIEWED · grok: … · gemini: … · blockers: <n> · string changes: <n> · ESCALATE: <n>` — or `FAILED …`. Launch row `R__` placeholder for the desk.

LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-fix-r4-draft-2026-09-22.md`: §0 ≤5 lines → `## CLASSIFICATION` (every finding, its class, the backing row) → `## DIGEST FOR THE DESK` ≤25 lines (launch order: `72` offline now — it holds no dev-DB lane; `73` in the house lane; `68` after `73` + his approval) → RULE PROOF (`72` vs `33`, `73` vs `25`: expected differences path + remote-control name only) → `NEW strings:` (expected none) → `## FOR DEJAN` → `## ESCALATE` → `## CONTINUE` → last line; while you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE: `SETUPS FIX R4 DRAFTED · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · prompts: 2 · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape. `date` before any time you write. L32: no value, ticker or date of his in any file you write. Stop within 35 minutes.
