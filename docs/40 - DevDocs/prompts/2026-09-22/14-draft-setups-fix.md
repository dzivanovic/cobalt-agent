MODEL: Opus 5 (`claude-opus-5`) — a classify-then-draft job (09-20 lesson 3: a fix round starts with a drafter that CLASSIFIES every finding from the check hub's file-check rows — FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM — before anything is built); no code, no DB, no launch · SEAT: drafter `setups-fix-draft-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK from `~/cobalt`: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/14-draft-setups-fix.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-fix-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `08` / `11` and 09-21's `50` / `76` / `79`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no memory-folder write (L58) — you write exactly the FOUR files named below with the Write tool, the REPORT LAST (09-22 lesson 3: a drafter that writes its stop line before its prompt files fires the desk's watch early) · METER: Anthropic small–medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# Seven setups — FIX ROUND (round 2) drafter, 2026-09-22

WHY: `reports/setups-one-check-2026-09-22.md` closed `houses that checked: 3 of 4 (floor 3) · defects that HOLD: 3 · ready for a deploy prompt: 0 of 3` (Grok: DO NOT DEPLOY until expected sides are independently derived; Opus 5: FIX anchorless defs evaluable-never-form, document anchors, rollback runbook for assumed rows; Gemini: BUILD STANDS on a premise the hub's file-check found false; Sol: METER until Sat 09-26 06:47 ET). The three HOLDs, hub-confirmed: (1) [F-16] (1)'s blind expected-value derivation of the `DEF_WRITTEN_*` pins was never done; (2) an undocumented `ANCHORS` registry (`src/cobalt/radar/formation/anchors.py:73-90`, `evaluate.py:1080-1081`) makes any definition outside its four anchor families permanently `not_formed: no formation anchor` while `evaluability()` and `ADDING-A-SETUP.md` say nothing of it; (3) the `sequence` trigger is hardcoded to one exact three-step tuple (`formation/triggers.py:219-236`) against FINAL §2.2's "each step is a predicate evaluated by the same interpreter". The branch takes a FIX round and a round-2 check before its deploy prompt is written (L67). LAW STEP: build DONE (`dd4a9b9`, tip `60ddac4`) → check r1 DONE → **FIX** → check r2 → blind values (`13`, `17`) → fixture cut (`12`) → deploy prompt → his "approve" (L7 / L61) → deploy. LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults (09-21 R15/R17/R18/R44)`.

## INDEX CARD — read these, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L29 (model rule: Opus floor on write paths; never auto mode on a write path — an offline build in a worktree plus the with-DB suite on `cobalt_dev` is the DEV LANE, not a write path: today's `05` and `12` and 09-21's `80` run it under `auto`), L32 (no trade name in `src/`; his note values never in a fixture or a test), L35, L45, L48, L60, L62, L63, L67 (three checkers; ≤3 rounds; a non-ruling stop spends no round — 09-21 close P-c, proposed), L70 (unproven ≠ defect), L71, L73. One line each in the prompts you write.
2. THE CHECK REPORT, end to end: `docs/40 - DevDocs/reports/setups-one-check-2026-09-22.md` — `## Per question`, `## Checked against the branch` (the file-check rows are your evidence: a finding HOLDS only where the hub's row says so), `## Ready for a deploy`, `## ESCALATE` 1–11. The checker files under the folder the report's `## Packet` names (`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/…`) — read Opus's and Grok's in full, Gemini's `CHECK:` section and its Q5 answer (the false premise).
3. THE DESIGN: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` — §2.2 (triggers, the `sequence` sentence at `:148`), §2.4 (anchors), §9 points 1–5, `[F-16]`, `[F-21]`; `docs/40 - DevDocs/ADDING-A-SETUP.md` (in the worktree — no "anchor" hit today; the doc the fix must extend).
4. THE BUILD: branch `setups/seven-0921`, worktree `/Users/cobalt/cobalt-wt/setups-c1`, tip `60ddac4` (build `dd4a9b9` + the report commit), build report `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-one-build-2026-09-21.md` (`## AWAITING_A_DAY`, `## ESCALATE` (i)–(ix), `## CLOSE` — the two suite commands EXACTLY as spelled there), the build prompt `prompts/2026-09-21/65-setups-one-build.md` (the contract; its launch line = the fix prompt's allowlist base; its RECOVERY RULE), `git -C /Users/cobalt/cobalt log --oneline main..setups/seven-0921`. The code: `src/cobalt/radar/formation/anchors.py`, `src/cobalt/radar/formation/triggers.py`, `src/cobalt/radar/evaluate.py` (the anchor block `:1080-1081` and `evaluability()`), `src/cobalt/taxonomy/registry.py` or wherever `evaluability()` lives (find it: `grep -rn "def evaluability" src/cobalt`), `tests/cobalt/setups_shapes.py`, `tests/cobalt/test_setups_lego.py`, `tests/cobalt/test_setups_second_chance.py`, `tests/cobalt/test_rubberband_forms.py`, `tests/cobalt/test_setups_nine_ema.py`, `tests/cobalt/test_setups_vwap_cont.py`.
5. THE ROUND-1 CHECK PROMPT `prompts/2026-09-21/66-setups-one-check.md` — its launch line, packet layout, question set and stop-line shape are the base for the round-2 check (`16`); the round-2 shape `prompts/2026-09-22/10-stale-marker-check-r2.md` (round 2 reads ONLY the fold: the fix diff + round 1's verdict table + the FIX rows' status). THE BLIND-SEAT PROMPT `prompts/2026-09-22/13-setups-blind-values.md` (the shape `17` copies: staging, the KEEP-OUT list, the house invocations, the ORDER proof, the comparison table) and the drafter report that wrote it, `reports/setups-fixture-cut-draft-2026-09-22.md` `## KEEP OUT OF 13` (the 17 `DEF_WRITTEN_*` constants across 4 files, with line numbers).
6. `cto-2026-09-22.md` §4 R3 (today's deploy ruling), R12 (the round-1 launch row), R13 (no Astra this week; Sol METER — checks run on Grok · Gemini · Opus 5, L67's floor of three); `cto-2026-09-21.md` R44 / R45 (ONE build, ONE check, ONE deploy; requirement (c) the per-setup ON/OFF dial), R46 / R49 (check seats and the two strings verbatim), R41 (the two `.env` strings), R39 (`grok`/`agy` through 2026-09-22 23:59 ET), R24 (never a day because he traded it).

## THE JOB — four files, Write tool, the report LAST
(a) `docs/40 - DevDocs/prompts/2026-09-22/15-setups-fix.md` — the FIX build, ONE complete block on `65`'s shape: MODEL = **Opus 5** if any FIX row changes `src/` (expected: yes — HOLDs 2 and 3 are code), else Sonnet 5; `--permission-mode auto` (dev lane, no write path: the offline suite + the with-DB suite on `cobalt_dev` through R41's `.env` pair; NOT `acceptEdits` — that mode raised three permission prompts on `65` and `01`; say so in the MODEL line); launch line FIRST = `cd /Users/cobalt/cobalt-wt/setups-c1` then the `claude --bg` line whose `--allowedTools` are `65`'s nineteen strings BYTE FOR BYTE (drop nothing — the with-DB suite needs the `.env` pair and `COBALT_ENV=dev uv run pytest`; state "no new string"), `--disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)"`, `--add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`; remote-control `setups-fix-0922`; index card SECOND; AUTHORIZATION greps R44 / R41 of 09-21 and the `R__` launch row of `cto-2026-09-22.md` the desk fills; PREFLIGHT = `date` · worktree clean on `setups/seven-0921` at `60ddac4` (else `FAILED PREFLIGHT: branch tip is <sha> — the desk re-issues`) · `ls -la /Users/cobalt/cobalt-wt/setups-c1/.env` absent · **STAGGER GATE on the dev-DB lane**: `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-22.md"` must start `STACKED DEPLOY DONE` or `FAILED` (today's stacked deploy `05` holds `cobalt_dev` and refuses while this worktree's `.env` exists — if that file does not exist or its last line is neither, `FAILED PREFLIGHT: 05 has not stopped — the desk relaunches after its stop line`) · the two BASELINE suites exactly as the build report's `## CLOSE` spells them (offline, then with-DB inside the `.env` pair: `cp` string → suite → `rm` string → `ls -la` proves absent) — record `<p>/<f>`; STEPS = the FIX rows only, each as T (a test that is RED on `60ddac4` for a real defect, or GREEN-as-pin for coverage — say which) → C (the smallest code change that turns it green; `ADDING-A-SETUP.md` extended in the same step for HOLD 2: the anchor families, what `evaluability()` now reports for a def outside them — a def with no anchor must be reported NOT EVALUABLE with the reason, never "evaluable" and never forming; for HOLD 3: the `sequence` trigger evaluates its steps as predicates through the one interpreter, second-chance's own tuple becoming one instance of it, its existing tests unchanged in outcome) → NEVER a `DEF_WRITTEN_*` value changed or added by judgement (L35; HOLD 1 is NOT built here — it is `17`'s blind seat) → the OFFLINE suite (baseline `2419/0` on the branch) and the with-DB suite (`540/0`, 1 skip) green → `uv run cobalt jobs restarts 60ddac4..HEAD` quoted → DevDoc(s) of every changed module updated (one wiki file per `.py`, `docs/40 - DevDocs/`) → `git add` by explicit path (never `-A`, `.`, `.env`, `data/`, `scratch/`), `git commit -m "fix(setups): round 2 — <what>"`, report `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r2-2026-09-22.md` committed on the branch, LAST LINE `SETUPS FIX R2 BUILT <tip> | on 60ddac4 | offline <p>/<f> | with-DB <p>/<f> | src changed: <yes|no> | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` or `FAILED: …`. "Never end a turn between steps" (09-22 lesson 2) written in. RECOVERY RULE (L60) verbatim from `65`. A denied Bash = FAILED, never a re-shape; file content through Write / Edit only, never a shell heredoc. NOTE FOR THE DESK in your report: `12-setups-fixture-cut.md` PREFLIGHTs on tip `60ddac4` and must be re-pointed to the fix tip (launch mechanics only, strings unchanged, L19) before it runs.
(b) `docs/40 - DevDocs/prompts/2026-09-22/16-setups-check-r2.md` — ROUND 2 of the check on `66`'s launch line BYTE FOR BYTE (only the prompt path and the remote-control name `setups-check-r2-0922` change; the same seats and strings; Sol's `codex exec` string carried, Sol on METER → recorded, skipped, three houses — R13's shape; staged in a NEW subfolder `setups-check/r2/` under the approved root, first Write creates it). PREFLIGHT: the fix report's last line starts `SETUPS FIX R2 BUILT `; STAGGER gates — `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-2026-09-21.md"` starts `FLOAT HANDICAP TRIBUNAL R1 DONE` or `FAILED` (the house lane: one Grok/Gemini hub at a time) and `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-blind-committed-day-2026-09-22.md"` starts `SETUPS BLIND COMMITTED DAY DONE` or `FAILED` (HOLD 1's evidence is in the fold), else `FAILED PREFLIGHT: <which> is running — the desk staggers`. PACKET = the FOLD ONLY: the fix diff (`git -C /Users/cobalt/cobalt log -p 60ddac4..<tip> -- . ":(exclude)docs"` in parts ≤ 38,000 B), the fix report, `17`'s comparison table (HOLD 1's closure), round 1's `## Ready` + `## Checked against the branch` tables, your classification table, the three HOLD findings verbatim. QUESTIONS: for each FIX row — is it closed by the fold (the test exists, asserts the right thing, is green for the right reason)? For HOLD 1 — does `17`'s table close it (every field MATCH, or a MISMATCH the desk has already routed)? Any NEW defect the fix introduced? The OWNER ITEMS are NOT re-asked. Report `docs/40 - DevDocs/reports/setups-check-r2-2026-09-22.md`; LAST LINE on `66`'s shape: `SETUPS CHECK R2 DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for a deploy prompt: <r> of <n> · ESCALATE: <n>` (gate on `houses that checked`, never on seats — 09-22 lesson 6).
(c) `docs/40 - DevDocs/prompts/2026-09-22/17-setups-blind-committed-day.md` — HOLD 1's closure for the pins that ALREADY EXIST: a blind seat re-derives, on the COMMITTED synthetic day (`2026-01-06`, the P2 real-shape fixture `tests/fixtures/radar/*.real-shape.*` — name the exact files), the 17 `DEF_WRITTEN_*` values of `test_rubberband_forms.py`, `test_setups_nine_ema.py`, `test_setups_vwap_cont.py`, `test_setups_second_chance.py` (the KEEP-OUT table's line numbers), on `13`'s shape BYTE FOR BYTE for the launch line (`66`'s nine strings, cwd `~/cobalt-wt/agy-trial`, remote-control `setups-blind-committed-0922`, nothing new), staging under `scratch/tribunal-bars-0920/setups-blind-committed/` (first Write creates it), house PRIMARY Gemini (`agy`, R39 through today 23:59 ET) / FALLBACK Opus 5 (same-house, recorded), the KEEP-OUT list + `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED "` = 0 on every staged file, the ORDER proof T1 < T2 ≤ T3, the comparison table `slug · field · pinned · blind · MATCH|MISMATCH|UNDERIVABLE`, `13`'s normalisation and nothing else (L37). It reads the pins ONLY after the house has answered. It runs on the CURRENT tip `60ddac4` (the pins do not move in the fix — the fix prompt forbids it) so it may launch as soon as the house lane is free; STAGGER gate on `float-handicap-tribunal-2026-09-21.md`'s last line as in (b). Report `docs/40 - DevDocs/reports/setups-blind-committed-day-2026-09-22.md`; LAST LINE `SETUPS BLIND COMMITTED DAY DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: <why>`. An UNDERIVABLE for an EMA/VWAP/ATR-resting value is expected and is not a defect (L70; the fixture-cut drafter's ESCALATE 7) — say what a code-capable seat (Sol, back Sat 09-26) would add.
(d) LAST, YOUR REPORT `docs/40 - DevDocs/reports/setups-fix-draft-2026-09-22.md`: the CLASSIFICATION TABLE — every finding of every checker (`## Per question` rows + `## Checked against the branch` rows + ESCALATE 1–11), one row each: `id · checker(s) · finding (≤2 lines) · file-check row (HOLDS / DOES NOT HOLD / NOT CHECKABLE / not checked) · CLASS: FIX | NOT REAL | UNPROVEN (L70) | OUT OF SCOPE | OWNER ITEM · what the fix is (FIX rows only) · which prompt closes it (15 / 16 / 17 / none)`. Expected from the stop line: FIX = HOLD 2 (anchors: evaluability + doc) and HOLD 3 (sequence interpreter); HOLD 1 = closed by `17` (+ `13` for the cut days), not a code fix; ESCALATE 4 (one-def relations), 5 (rollback runbook after the assumed note), 6 (closure completeness — latent), 7 (nine-ema wrong side — not run) = classify each honestly, UNPROVEN or FIX with the test that would prove it; ESCALATE 8 (`AWAITING_A_RULING` shapes escape gate 2) = FIX if a pin can be written without a value of his, else OWNER ITEM; ESCALATE 9 (the per-setup ON/OFF dial, R44 (c)) = OWNER ITEM, quoted verbatim — the desk brings it to him; ESCALATE 10 (Grok did not self-write) = OUT OF SCOPE (harness). Then `## DIGEST` (≤12 lines: what is built, what each prompt needs, new rule strings = 0 expected, the order the desk launches them in: `17` when the house lane frees → `15` after `05`'s stop → `16` after `15` BUILT and `17` DONE → the desk re-points `12` → `12` → `13` → deploy prompt drafter), `## ESCALATE`, and your LAST LINE: `SETUPS FIX PROMPTS DRAFTED · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · code change expected: <yes|no> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. Never invent a flag: every command shape appears in `65` / `66` / `13` / the code's CLI. Every OWNER ITEM is quoted, never resolved by you. No `DEF_WRITTEN_*` value is ever typed by you or by the fix. `date` for every time you write. Stop within 30 minutes; your stop line is your report's last non-blank line, written LAST.
