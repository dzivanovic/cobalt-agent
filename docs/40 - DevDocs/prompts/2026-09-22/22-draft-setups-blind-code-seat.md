MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `setups-blind-code-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/22-draft-setups-blind-code-seat.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-blind-code-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `08` / `11` / `14` / `18`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the TWO files named below with the Write tool, the REPORT LAST (09-22 lesson 3) · METER: Anthropic small–medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the BLIND CODE SEAT that closes HOLD 1 of the seven-setups check — a house that has NOT seen the pins COMPUTES the 17 `DEF_WRITTEN_*` values on the committed day with code of its own, 2026-09-22

WHY: `reports/setups-blind-committed-day-2026-09-22.md` closed `MATCH: 0 · MISMATCH: 0 · UNDERIVABLE: 17` — a hand derivation cannot produce values that rest on EMA / VWAP / ATR seeding, the extension lifecycle, leg roles, the level set and the RangeBreak lifecycle (its `## ESCALATE` table names the missing detector per field). FINAL [F-16] (1) (`docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`) still binds: *"the expected side, formed bar, trigger price and stop price are written by a CHECKER house from the bars alone, before it sees the builder's output"*. Round 1's Grok verdict is `DO NOT DEPLOY until expected sides are independently derived`. So the checker house must COMPUTE, not read: a seat that can run its own code in a sandbox, given the bars, the definitions as neutral shapes, the public rules and the engine's DETECTOR SOURCE as the rule text (source is rules; the pinned VALUES and the tests that hold them are the builder's output and stay out). LAW STEP: check r1 (HOLD 1) → `17` (hand: UNDERIVABLE) → **THIS = the code seat** → check r2 `16` reads its comparison → `12` / `13` → deploy prompt. LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults (09-21 R15/R17/R18/R44)`.

## INDEX CARD — read these, in this order, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L32 (no value of his note in a packet; the neutral shapes only), L33 (roles: a seat that must produce an artifact gets WRITE access to its own workspace — the scratch folder — and no network), L35, L37 (the hub judges nothing: MATCH / MISMATCH / UNDERIVABLE by string equality after `13`'s normalisation), L41 (no credential reaches the seat; no DB; the fixture files only), L44 / L59, L45, L48, L62 / L63 (every string approved before launch; a denial = FAILED), L67 (a house OTHER than the build's author — Anthropic built it: Grok or Sol, never Opus/Fable here), L70, L71. One line each in the prompt you write.
2. THE HAND SEAT AND WHY IT FAILED: `prompts/2026-09-22/17-setups-blind-committed-day.md` (the packet shape, THE 17 PINS table, the KEEP-OUT list, the ORDER proof, the comparison — you copy all of it) and `reports/setups-blind-committed-day-2026-09-22.md` (`## Packet`, `## House` — Gemini's harness denial verbatim, `## ESCALATE` — the per-field missing detector, the two packet-quality items: the daily/intraday contradiction and the `stop.buffer` unit).
3. THE HOUSE MECHANICS: **Grok** — `prompts/2026-09-16/04-audit-house.md` and its report (find it: `ls "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/" | grep -i audit`) — the 09-16 trial of Grok "writing + running a checker in a sandboxed scratch dir, network off": read what it proved and what its launch spelling was; `prompts/2026-09-21/38-float-handicap-tribunal.md` §2 and `66-setups-one-check.md` §2 — Grok's APPROVED launch spelling today (`grok --sandbox …` with `--allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)"`): whether that spelling lets Grok RUN a script it wrote (a `--allow "Bash(…)"` / `Shell` form) is the FIRST thing your report settles from those files — if it cannot, say so and the seat is Sol. **Sol** — `prompts/2026-09-21/66-setups-one-check.md`'s `codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only` string (READ-ONLY today; a code seat needs `-s workspace-write`, LAWS L29 / L33: "a Codex run that must write a report or plan launches `-s workspace-write`" — a NEW string, listed for his approval; Sol's meter returns Sat 2026-09-26 06:47 ET, `cto-2026-09-22.md` R13).
4. THE RULES THE SEAT MAY SEE AS SOURCE (the engine's detector modules on branch `setups/seven-0921` at `60ddac4`, in `/Users/cobalt/cobalt-wt/setups-c1/`): `src/cobalt/radar/anatomy/*.py` (extension lifecycle, legs, ranges, levels, EMA/VWAP/ATR seeding — find the modules the hand seat's table names: `extension.py`, `daily.py`, `structure.py`, the leg-role and level-set modules), `src/cobalt/radar/formation/*.py` (atoms, triggers, stops, anchors), `src/cobalt/radar/evaluate.py` (`evaluate_member`, `scan_instants`, `admitted_at`), `configs/cobalt/taxonomy/tunables.yaml`. NEVER: the four pinned test files, `tests/cobalt/test_setups_lego.py`, `test_setups_hitchhiker.py`, any `test_setups_*.py`, `tests/cobalt/setups_shapes.py:1-44`, the build report, `65`, round 1's check folder, any line containing `DEF_WRITTEN_` — the KEEP-OUT list of `17`, unchanged, enforced by the same `grep -c` = 0 on every staged file.
5. `cto-2026-09-22.md` §4 R12 / R13 / R15 (the check, the meter, the hand seat's row); `cto-2026-09-21.md` R39 (`grok` / `agy` through 2026-09-22 23:59 ET), R49 (the Sol read-only string).

## THE JOB — two files, Write tool, the report LAST
(a) `docs/40 - DevDocs/prompts/2026-09-22/23-setups-blind-code-seat.md` — a Sonnet 5 hub (`setups-blind-code-0922`, cwd `~/cobalt-wt/agy-trial`, `auto`, reads only for the hub itself) on `17`'s shape: it STAGES the packet into `scratch/tribunal-bars-0920/setups-blind-code/` (first Write creates it) — `17`'s packet (the committed-day fixtures, the loaders, the neutral shapes excerpts, the tunables excerpt, the public rules, `TASK.md` with the scan instants per slug and the 17 fields owed) PLUS the detector SOURCE files whole (byte-identical copies, `wc -c` proven, each headed by its real path in `TASK.md`'s file list, parts ≤ 38,000 B where a file is larger) — every staged file re-checked `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED "` = 0; then puts ONE house before it with a task sentence built on `17`'s, extended: *"… You MAY and SHOULD write your own program in this folder and run it on the staged bars: implement what the staged source and public rules specify, compute the seeded indicators and the detectors yourself, and print, per setup, the side, the formed bar's timestamp exactly as the bars file spells it, the trigger price, the stop price (and rubberband's last close) at the scan instants TASK.md names. Cite the bars each value rests on. Where the staged rules do not determine a value, write UNDERIVABLE and the missing rule — never a guess. Do not open any file outside this folder; no network."* — HOUSE PRIMARY: **Grok** in its sandbox, the approved spelling of `38` / `66` § 2 extended ONLY if your INDEX CARD (3) reading shows the run permission is already inside the approved string; if a NEW allow form is needed for Grok to execute its script, write the exact string under `## NEW STRINGS` for his approval and say which precedent it narrows. HOUSE FALLBACK: **Sol** `codex exec --skip-git-repo-check -m gpt-5.6-sol -s workspace-write --cd <the packet folder> …` (NEW string — list it; runs Sat 09-26 or later; the prompt's DATE GATE names both windows: Grok through today 23:59 under R39, Sol from Sat 06:47). NEVER Opus, never Fable (the builder's house). THE ORDER PROOF (T1 < T2 ≤ T3), the comparison table (17 rows, `13`'s normalisation), `FIX-or-OWNER` per MISMATCH, UNDERIVABLE with the rule named — all as `17`. Report `docs/40 - DevDocs/reports/setups-blind-code-2026-09-22.md`; LAST LINE `SETUPS BLIND CODE SEAT DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: <why>` / `FAILED PREFLIGHT: <rule>`. Its own STAGGER gate on the house lane: `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-r2-2026-09-22.md"` must start `FLOAT HANDICAP TRIBUNAL R2 DONE` or `FAILED` — or the file must NOT EXIST AND `ls "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/19-handicap-tribunal-r2.md"` must fail (never drafted) — else `FAILED PREFLIGHT: 19 is running — the desk staggers`. The seat's clock: 60 minutes (it writes and runs code), TaskStop at the ceiling, `TIMEOUT` recorded. AUTHORIZATION greps: R12 / R15 of `cto-2026-09-22.md`, R39 / R49 of `cto-2026-09-21.md`, the `R__` launch row the desk fills, every allow string against its precedent file (`66` for the read strings and `grok`; the 09-16 audit-house prompt for any Grok run form), and each NEW string against the desk row that carries his word.
(b) LAST, YOUR REPORT `docs/40 - DevDocs/reports/setups-blind-code-draft-2026-09-22.md`: `## GROK CAN RUN CODE?` (the 09-16 evidence verbatim: what spelling, what it did, what the sandbox denied; your answer YES / NO / UNPROVEN with file:line), `## NEW STRINGS` (each, verbatim, with the precedent it narrows — expected: 0 for Grok if the run form is approved, 1 for Sol), `## DIGEST` (≤10 lines), `## ESCALATE` (always: the daily/intraday contradiction of the committed fixture — is it the cutter's construction or `daily.py`'s trade-date exclusion? state what you read in `src/cobalt/radar/anatomy/daily.py` and the P2 cutter, UNPROVEN if reads do not settle it; the `stop.buffer` unit; anything the seat cannot get from the packet), and your LAST LINE: `SETUPS BLIND CODE PROMPT DRAFTED · house: <grok|sol> · grok runs code: <yes|no|unproven> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. Never invent a flag: every command shape appears in `17` / `66` / `38` / the 09-16 audit-house prompt / `codex --help` as quoted in an approved prompt. No `DEF_WRITTEN_*` value is ever read by you (the four pinned files and the KEEP-OUT list bind you too). `date` for every time you write. Stop within 25 minutes; your stop line is your report's last non-blank line, written LAST.
