MODEL: Sonnet 5 (`claude-sonnet-5`) — stage ONE packet before FOUR checkers, file-check, tabulate; no verdict of your own (L37) · SEAT: build-check hub `drc-d5-check`, launched by the CTO desk in the background · LADDER `S3-P3 · F14` · LAW STEP (L67): build `52-drc-d5-build.md` (after the DRC set and S3 C2 are on main) → **THIS = CHECK ROUND 1 of at most 3, the WHOLE D5 build** → fix round (L75) if a defect HOLDS → its own deploy. RULE STRINGS: `53-drc-d1-check.md`'s line BYTE FOR BYTE; only the prompt path and remote-control name differ; NO NEW STRING. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/57-drc-d5-check.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control drc-d5-check --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/tribunal-bars-0920)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" "Bash(claude -p --model claude-opus-5-5 *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode (read-only seat; stated here and on the line); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO database, NO `pytest`, NO git write, no vault write, no memory-folder write · METER: as `53` — Sol may be BACK after Sat 2026-09-26 06:47 ET: probe it and launch it if UP (four checkers) · nobody sits at this terminal. DESK: watch the LAST NON-BLANK LINE, `^(DRC D5 CHECK DONE|FAILED)`.

# BUILD CHECK, ROUND 1 — DRC D5: THE RECONCILE WRITES LEGS, THE UNRESOLVED LINE, HIS RESOLVE ACTION. Everything in `53-drc-d1-check.md` binds you UNCHANGED, with ONLY these substitutions: `d1` → `d5` in every path, name and line (`drc-check/d5/`, `/Users/cobalt/cobalt-wt/drc-d5`, `drc-d5-build-<date>.md`, `drc-d5-check-<date>.md`, `DRC D5 BUILT `, `CHECK D5:`, `DRC D5 CHECK DONE`, rows `D5-0 … D5-6`); launch-row filename `57-drc-d5-check.md`, row **R__**; the build prompt is `52-drc-d5-build.md`; the stagger path filled by the desk here: **`__`**; `rulings.md` = §4 rows R67, R90; `design.md` = v2 §3 (reconcile, carry), §4 (S-C1 / S-C2 / S-C2b), §6, §10 X11, `## OPEN — ROUND 2` VERBATIM; PLUS (8) `c2-writer.md` = the S3 C2 correction writer's source file at the BASE TIP (`git -C /Users/cobalt/cobalt show <base>:<path>` — the path from the build report's `## D5-0`), whole; the (ii) boundary check = `git -C /Users/cobalt/cobalt log --oneline <base>..<tip> -- src/cobalt/db_migrations src/cobalt/cards src/cobalt/radar src/cobalt/aset/engine.py src/cobalt/aset/store.py src/cobalt/settings <C2's writer path>` EMPTY; (iii) = `grep -rn "UPDATE\|DELETE" /Users/cobalt/cobalt-wt/drc-d5/src/cobalt/drc` → each hit placed (the resolution and correction paths are append-only); (iv) = `grep -rn "CLOSED\|FILLED\|state" /Users/cobalt/cobalt-wt/drc-d5/src/cobalt/drc/resolve.py /Users/cobalt/cobalt-wt/drc-d5/src/cobalt/drc/reconcile.py` → no write of a card state; the `ready` field reads `ready for a deploy prompt`.

QUESTIONS.md (verbatim): "You are one of four checkers of ONE BUILD CHUNK (round 1 of at most 3). The sentence that launched you says which house you are. Read ONLY the files in this folder. You cannot run commands. The chunk makes the Daily Report Card's reconcile WRITE: the broker trading log is the truth, so each trade's recorded legs are corrected to it as APPEND-ONLY correction rows (or new exit legs) through the ONE existing leg-correction writer (`c2-writer.md`), each naming its source and import row; the trader's own taps and held-count statements stay as history. When that writer REFUSES a correction (e.g. a card he closed by hand while the export still shows shares open), the owner ruled (R90): the report is still built, the position stays OPEN as the export shows it, ONE line `unresolved: card <id> — <refusal>` is carried on every later report until he resolves it, and the page gives him a RESOLVE action (his choice + his text, recorded append-only, versioned, never a silent close). The card's own state machine is NOT changed. The design is `design.md`, the rulings `rulings.md`, the prompt `prompt.md`, the build `build-diff.md` + `devdocs.md`, the builder's claims `build-report.md` (a CLAIM). The contract: *one leg writer; append-only; a refusal is loud and carried, never forced and never a failed build; nothing reaches a score, grade or size*. For EACH row D5-0 … D5-6 of `prompt.md`, exactly one of, each with `file:line`: `MEETS THE ROW` / `DOES NOT MEET — <file:line, the concrete legs + export that show it>` / `NEW DEFECT — <file:line>`. Look hardest at: **one writer** — does any code write `legs` other than through `c2-writer.md`'s function? **idempotence** — a re-build on the same import writes zero new rows; a superseding import corrects relative to the current view; **the walks** — held 50 vs export 60; an export exit never recorded; a recorded leg with no export execution; no legs yet; **the refusal** — caught per card, recorded with the refusal text verbatim, the DRC still `done`, the line carried to the next day (X11); **RESOLVE** — both fields required; refused in the market reset; a second resolution supersedes by reference, never updates or deletes; after `stop carrying` the carried position stops rendering and the card row is untouched; **realized R** — `confirmed` only after a reconcile, `not computed (unresolved)` otherwise, one formula. THEN over the whole diff: (a) weak assertions, (b) changes outside the rows' files (the C2 writer itself unchanged), (c) L52. `NOT CHECKABLE FROM READS — <what would have to be run>` is not a defect. End with EXACTLY one line: `CHECK D5: BUILD STANDS` / `CHECK D5: BUILD STANDS EXCEPT <items>` / `CHECK D5: FIX <items>` — followed on the SAME line by ` · ready for a deploy prompt: YES|NO · <ten words of reason if NO>`."

STOP LINE (L71): `DRC D5 CHECK DONE · grok: <…> · gemini: <…> · sol: <…> · opus: <…> · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for a deploy prompt: <r> of <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`. NEXT, not yours: ≥3 checked, 0 HOLD → the desk drafts D5's deploy prompt; else a fix round (L75).
