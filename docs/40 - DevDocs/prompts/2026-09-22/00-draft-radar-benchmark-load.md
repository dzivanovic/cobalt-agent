MODEL: Sonnet 5 (`claude-sonnet-5`) — a read-and-write drafting job: two prompt files from a proven precedent, no code, no DB · SEAT: drafter `radar-benchmark-draft-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK from `~/cobalt`: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/00-draft-radar-benchmark-load.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control radar-benchmark-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as 09-21's `13` / `50` / `64` / `71`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# Draft the radar-benchmark load chain (2026-09-22)

## INDEX CARD — read these, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — sections L28 (vault-write law; its 2026-09-15 clause: a trader-run `settings load --apply` is exempt from the token), L35 (trust the artifact), L48 (evidence in the report file), L61 (chat "approve" = the HITL for the exact action the desk named; the deploy allowlist shape), L62 (unattended launch: every permission ahead, a mid-run question = FAILED), L63 (no dialogs), L67 (the desk's own prompts are read by at least one other house before they run), L71 (the stop line = the report's last non-blank line). One line each, then move on.
2. `docs/40 - DevDocs/reports/cto-2026-09-22.md` — §4 row R1 (his ruling: `top_n: 20, min_move_pct: 10`, "approved. we will tune if needed later") and §1 (the chain you are drafting).
3. THE PRECEDENT, read end to end — `docs/40 - DevDocs/prompts/2026-09-20/17-cards-htf-apply.md` (the apply hub: PREFLIGHT, dry-run, apply, proof, stop line, its allowlist shape) and `docs/40 - DevDocs/prompts/2026-09-20/18-review-cards-htf.md` (the one-round house read of that hub prompt). Also `docs/40 - DevDocs/reports/cards-htf-apply-2026-09-20.md` (what the hub's report looked like when it worked; last line `CARDS HTF LIVE …`).
4. THE FILE TO LOAD — `docs/40 - DevDocs/prompts/2026-09-22/01-packet/radar-benchmark.yaml` and its byte-identical NO-SPACE copy `/Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml` (gitignored under `data/`; the load string must use THIS path — the allowlist cannot carry a path with spaces). sha256 of both, computed by the desk 06:2x: `10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880`. Recompute nothing; the hub prompt you write makes the HUB verify the hash at PREFLIGHT with a string already in its list (the `--dry-run` load verifies the bytes before it parses — `settings/cli.py:84-100`).
5. THE COMMAND — `src/cobalt/settings/cli.py` lines 1-20, 81-160 and 300-330 (`cobalt settings load --optional <file> --sha256 <hash> --dry-run | --apply`; `OPTIONAL_FILE_ROOT = "optional_settings"`; what `--dry-run` prints; what `--apply` writes and whether it refuses inside the 20:00–21:00 pause — read, do not assume). The reader of the row: `src/cobalt/replay/runner.py` around line 318 (`BenchmarkSettings.from_rows(deps.settings_values())`) and `src/cobalt/settings/models.py` lines 53-80. `logs/replay.err` (3 lines — last night's failure text).
6. Rule-string precedent for the PROOF read: `grep -rn "cobalt db query --side user --prod" "docs/40 - DevDocs/prompts/2026-09-21/" | head` — copy an existing approved shape for reading `"user".trader_settings` if one exists; if none, the proof string is NEW and you name it as such.

## THE JOB — write three files with the Write tool, nothing else
(a) `docs/40 - DevDocs/prompts/2026-09-22/01-radar-benchmark-load.md` — the APPLY HUB, ONE complete block on the shape of `17` (MODEL Sonnet 5 · SEAT · SESSION fresh · auto mode on · METER Anthropic small; launch line FIRST = two bare commands `cd /Users/cobalt/cobalt` then `claude --bg "Read '<this file>' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control radar-benchmark-load-0922 --allowedTools <strings> --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt`; index card SECOND). Its allowlist = EXACTLY: `"Bash(COBALT_ENV=production uv run cobalt settings load --optional /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml --sha256 10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880 --dry-run)"`, the same with `--apply`, ONE read-only proof string for the row (item 6), `"Bash(date*)"`, `"Bash(git add *)"`, `"Bash(git commit *)"`, `"Bash(git status*)"`, `"Bash(tail *)"`, `"Bash(grep *)"`, `"Bash(ls *)"` — and the `R__` authorization line the desk fills with today's launch row. STEPS: PREFLIGHT (mode check, `git status` clean-enough per `17`, the file exists, `date` outside 20:00–21:00 ET if `--apply` refuses in the pause — read item 5 and say which) → `--dry-run` (the hash gate + what it prints, quoted verbatim in the report) → `--apply` → PROOF: the row read back (`key = 'radar.benchmark'`, `value ->> 'top_n'` = 20, `min_move_pct` = 10), quoted verbatim → `git add` its report by name + `git commit` (docs only, `~/cobalt` main — the desk makes no commit between launch and the stop line) → stop line. Report `docs/40 - DevDocs/reports/radar-benchmark-load-2026-09-22.md`; LAST LINE `RADAR BENCHMARK LOADED <time> · row: top_n 20 · min_move_pct 10 · sha256 verified · replay next run: 21:10 ET · ESCALATE: <n>` or `FAILED: <step> — <reason>`. Recovery: a denial = FAILED, never a re-shape; no dialog ever. Bind the laws of item 1 in its own index card, one line each.
(b) `docs/40 - DevDocs/prompts/2026-09-22/02-review-radar-benchmark-load.md` — the ONE-ROUND HOUSE READ of (a) on the shape of `18`: Grok + Gemini (`Bash(grok *)` / `Bash(agy *)` are approved through 2026-09-22 23:59 ET, 09-21 R39 — copy `18`'s launcher strings and packet-staging shape byte for byte; if `18` used a different house set, say so and use R39's two). Question set: (Q1) does the allowlist carry every command the steps run, and nothing the steps do not run; (Q2) does `--apply` refuse inside the market_reset pause, and does the prompt's clock rule match; (Q3) is the hash gate real (bytes before parse) and the proof read the right table/side; (Q4) any way the run could stop on a question. Report `docs/40 - DevDocs/reports/radar-benchmark-load-review-2026-09-22.md`, last line `RADAR BENCHMARK LOAD REVIEW DONE · houses: <n> of 2 · blockers: <n> · folds: <n>` or `FAILED: …`.
(c) your own report `docs/40 - DevDocs/reports/radar-benchmark-draft-2026-09-22.md`: §0 DIGEST ≤12 lines (files written; the hub's NEW strings vs `17`'s — list every string not seen in an approved prompt, by exact text; whether `--apply` is pause-bound; ESCALATE count), a `## RULE PROOF` section listing (a)'s full allowlist, `## ESCALATE`, then the LAST LINE `RADAR BENCHMARK PROMPTS DRAFTED · prompts: 2 · new rule strings: <n> · apply pause-bound: yes|no · ESCALATE: <n>` or `FAILED: <why>`. Run `date` for every time you write.

## RULES THAT BIND YOU
- L19 whole-prompt: each file is ONE complete block. L36: you launch nothing. L58: no memory-folder write. L62/L63: the hub you draft never asks — a denial is a FAILED line. Never invent a command flag: every flag you write must appear in `settings/cli.py`'s parser (item 5). Never widen: the hub's list is the two load strings, one proof read, and the docs-commit strings — nothing else.
- Stop within 25 minutes of your start. Your stop line is the LAST non-blank line of your report.
