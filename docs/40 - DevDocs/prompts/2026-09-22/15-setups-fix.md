MODEL: Opus 5 (`claude-opus-5`) — FIX rows F1, F2 and F3 change `src/` (the registry, the anchor table, the trigger table, the interpreter's step shapes, the closure), so the floor is Opus 5 (L29: the build this fixes was an Opus 5 seat; Sonnet 5 only when no FIX row changes `src/`). `--permission-mode auto`: this is the DEV LANE, not a write path — an offline build in a worktree plus the with-DB suite on `cobalt_dev` through R41's `.env` pair, inside the suite's never-committed rollback transaction; no production command, no vault write, no migration applied outside the suite (today's `05` and `12`, and 09-21's `80`, run the same lane under `auto`). NOT `acceptEdits`: that mode raised three permission prompts on `65` and `01`, and a dialog in an unattended run is a stop nobody answers (L63) · SEAT: setups fix hub `setups-fix-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK after `05`'s stop line (the STAGGER GATE below) and after the launch row **R16** and this file are committed on main: `cd /Users/cobalt/cobalt-wt/setups-c1` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/15-setups-fix.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-fix-0922 --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/setups-c1/.env)" "Bash(rm /Users/cobalt/cobalt-wt/setups-c1/.env)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: the nineteen allow strings, the three deny strings and the `--add-dir` triplet are `prompts/2026-09-21/65-setups-one-build.md`'s launch line BYTE FOR BYTE — NO NEW STRING, none dropped (the with-DB suite needs the `.env` pair and `COBALT_ENV=dev uv run pytest *`; `mkdir -p *` is carried unused so the list stays the approved one). Only the prompt path, the remote-control name and the permission mode differ from `65`'s line · SESSION: fresh; EVERY relaunch uses the SAME line and resumes from `## CONTINUE` · never `bypassPermissions`; push DENIED in the line; no production command, no `--allow-prod`, no `COBALT_ENV=production`, no `cobalt radar evaluate`, no `cobalt db` anything, no `cobalt taxonomy` anything typed, no vault write (the dev vault included), no memory-folder write (L58), no `launchctl`, no `claude` (L36) · `cobalt_dev` ONLY through `COBALT_ENV=dev uv run pytest …`, `.env` copied BY NAME, never printed, never read, removed at EVERY stop (L41) · METER: Anthropic MEDIUM (three code rows, two test-only rows, two full suites twice — ≈36 min of suite wall time per pass) · nobody sits at this terminal — your channel is the report file; never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default named in the row, continue.

# SETUPS AT DEFAULTS — FIX ROUND (round 2 of at most 3): the rows round 1 left HOLDING, and nothing else. Branch `setups/seven-0921`, worktree `/Users/cobalt/cobalt-wt/setups-c1`, from tip `60ddac4` (build `dd4a9b9` + its report commit). DO NOT STOP until the report's last line is `SETUPS FIX R2 BUILT …` or `FAILED …`. NEVER END A TURN BETWEEN STEPS (09-22 lesson 2): a turn that ends with work left and no stop line is a stall the desk cannot see — the next row starts in the same turn the last one committed.

WHY: `docs/40 - DevDocs/reports/setups-one-check-2026-09-22.md` closed `houses that checked: 3 of 4 (floor 3) · defects that HOLD: 3 · ready for a deploy prompt: 0 of 3`. The drafter CLASSIFIED every finding (`docs/40 - DevDocs/reports/setups-fix-draft-2026-09-22.md` `## CLASSIFICATION TABLE`). THIS builds ONLY its FIX rows F1–F5 and the one coverage pin P1. HOLD 1 (the blind `DEF_WRITTEN_*` values, [F-16] (1)) is NOT built here: it is `17`'s blind seat on the committed day and `13`'s on the cut days. LAW STEP (L67): build DONE → check r1 DONE → **THIS = FIX** → check r2 (`16`, the fold only) → blind values (`17`, `13`) → fixture cut (`12`, re-pointed to your tip) → deploy prompt → his "approve" (L7 / L61) → deploy. LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults (09-21 R15/R17/R18/R44)`.

## THE FIX ROWS — build these, in this order, nothing else
| row | round-1 source | what is wrong (file:line at `60ddac4`) | the fix |
|---|---|---|---|
| **F1** | HOLD 2 · check ESCALATE 2 · Opus Q3 item 5, Q4 | `ANCHORS` (`src/cobalt/radar/formation/anchors.py:73-90`) decides whether a def can form at all; `anchor_for` returns None for a def whose preconditions name none of `Extension.` · `Range(micro).` · `Leg(pullback)` · `RangeBreak(level)`, and `evaluate.py:1080-1081` then returns `not_formed: no formation anchor` on every scan. `registry.evaluability` (`src/cobalt/radar/anatomy/registry.py:44-70`) never reads `ANCHORS`, so such a def is called **evaluable** and never forms. `ADDING-A-SETUP.md` has no "anchor" hit. The drift test (`tests/cobalt/test_setups_lego.py:270`) does not cover `ANCHORS`. | `evaluability` reads `anchor_for` (the SAME table formation dispatches through, L3 / FINAL §2.5) and a def with no anchor row is NOT EVALUABLE with the missing entry `anchor:none` — never "evaluable", never forming. `evaluate_member`'s evaluability gate then reports it `not_evaluable` naming `anchor:none`; the `:1080-1081` branch stays as the fail-loud backstop. `ADDING-A-SETUP.md` gains an "Anchors" section: the four anchor objects and their atom prefixes, the rule that the FIRST row whose prefix a precondition names wins (tuple order), what the dry-run says for a def with none (`anchor:none`), and FINAL §1's `A-01`: a def whose preconditions name an `Extension.` atom trades AGAINST the Extension (a with-trend def naming `Extension.state` is forced to the other side — Opus Q4's second trap), in the FINAL's words, no invented rule. The drift test fails when an `ANCHORS` object is missing from the doc. |
| **F2** | HOLD 3 · check ESCALATE 3 · Opus Q3 item 2 | `Sequence` (`src/cobalt/radar/formation/triggers.py:219-236`) serves exactly one step tuple, `STEPS = ("price close_through Level_ref", "event(retest)", "close_above(prior_bar)")`, compared as strings; "any other step list is not served (named `trigger:sequence`)". FINAL §2.2 `:148`: "the steps in order; each step is a predicate evaluated by the same interpreter (§4); the last step's bar is the trigger bar". FINAL §4 row: "`close_through`, `close_above(prior_bar)` inside `sequence` steps → relation resolvers over working-TF closes". | The `sequence` trigger evaluates its steps as predicates through the ONE interpreter: each step's predicate is checked by the same `predicate_gaps` dispatch `evaluability` uses (an unserved step names ITS gap — the atom, `relation:<word>` or `Unsupported(…)` — not `trigger:sequence`), and is resolved on the working-TF bars in order: step k is the FIRST bar after step k−1's bar on which its predicate is true; the last step's bar is the trigger bar and its close the trigger price (as today). `close_through` and `close_above(prior_bar)` become served step shapes per FINAL §4 ("relation resolvers over working-TF closes"). `Level_ref` inside a step keeps TODAY's binding — the level of the def's `RangeBreak(level)` anchor (the tuple's own reading, `triggers.py:222-224`) — named in the DevDoc as the existing binding, not a new rule; any other `Level_ref` form is `Unsupported(…)`, named. Second-chance's own tuple becomes ONE INSTANCE of the general walk: its tests (`tests/cobalt/test_setups_second_chance.py`, the Lego tests, X5) are UNCHANGED IN OUTCOME and untouched in text. |
| **F3** | check ESCALATE 6 · Grok Q6 · Opus Q6 / Q10 (a) · build ESCALATE (xi) ASK DESK 18 | The declared-key closure misses the TRIGGER resolver's reads. The build's own guard pins it: `tests/cobalt/test_setups_lego.py:305-311` `TRIGGER_READ_UNDECLARED = {"pivot.n", "range.micro.bound_flat_slope_atr", "range.micro.touch_tolerance_atr", "range.micro.touches_per_side"}`, undeclared for `vwap-continuation`. Two of them are NULL engine rows (`configs/cobalt/taxonomy/tunables.yaml:215-217`, `:226-228`) — holes the assumed note can fill with `source: assumed` (FINAL §7-§8), after which a vwap-continuation card would form on an assumed value with NO `assumed_formation` mark (L52 (a); latent today — no key is `source: assumed` in committed config). | Every key a def's trigger (and stop) resolver reads joins the def's declared closure — the resolver declares its keys the way atom resolvers already do (`tunable_keys=` on `AtomResolver`, `formation/atoms.py`), and the closure unions them. The X22 guard's `TRIGGER_READ_UNDECLARED` becomes EMPTY (an A1 re-point of the build's own guard, BY DESIGN of this row — listed). `ALWAYS_READ_UNDECLARED` (the three `extension.path_*` keys) is NOT touched: they are non-null engine rows, hole-fill can never make them assumed (`test_setups_lego.py:333` asserts it) — that half of the finding is NOT REAL (classification row). |
| **F4** | check ESCALATE 5 · Opus Q11 · Opus CHECK line | Rolling code back after `1 - Trading/Assumed Defaults.md` has been written and loaded breaks old code: main's `TunableSource` (`src/cobalt/taxonomy/tunables.py:63-66` in `~/cobalt`) has no `assumed`, so every scan's tunables read fails; 0013's rollback refuses while a NULL-slug row exists; no runbook says so. The drafter read main's enum (`git show main:src/cobalt/taxonomy/tunables.py` → `RULING`, `SHEET`, `DWV` only). | DOC ONLY: `ADDING-A-SETUP.md` gains a "Rolling back after the assumed note" section — the order (remove the assumed rows first: the note's `tunables:assumed` unit emptied and `taxonomy load` re-run by the owner's deploy hub, THEN code rollback, THEN 0013's rollback), and why (the enum, the refusal). No code, no test: prove the premise by quoting `git show main:src/cobalt/taxonomy/tunables.py` lines for `TunableSource` in the report. The deploy prompt's drafter reads this section (named in your report's ESCALATE). |
| **F5** | check ESCALATE 8 · Opus Q5 / Q9 | `AWAITING_A_RULING` shapes are skipped by gate 2 with no pin of their own (`tests/cobalt/test_setups_lego.py:45`, `:72`): if backside or fashionably-late began to form (or the set grew), nothing fails. vwap-continuation is `AWAITING_A_RULING: F1` in the report only; its test fills `dist.k.vwap` (`setups_shapes.py:407`, L69). | TEST ONLY, GREEN-as-pin: for every key of `AWAITING_A_RULING`, assert it is evaluable AND forms on NO scan of the committed day at its shape's engine rows — so a member that starts forming fails the suite and must leave the set, announced. For vwap-continuation, assert its shape with its `engine=` fill REMOVED (committed engine rows only, `dist.k.vwap` null) forms on NO scan — the F1 status pinned in a test, not only in a report. No value of his (L32 / L69): only the committed config and the test's own constructed rows. |
| **P1** | check ESCALATE 4 · Opus Q3 items 3–4 (class NOT REAL — coverage only) | The single-sentence relations (`after`, `inside`, `on`, `between`) are narrow by design (FINAL §4 "only what the seven need") and a sentence outside them is ALREADY reported not evaluable, named (`formation/atoms.py` `_on_gaps`, `_after_gaps`, `_inside_gaps` → `Unsupported(<word>:…)`). No test pins that. | TEST ONLY, GREEN-as-pin: one neutral def per relation using it in a sentence outside the served one → `evaluability(...).evaluable is False` and its `missing_atoms` names `Unsupported(<word>:…)` (the literal the code returns, read from the run, never guessed). No code. |
NOT BUILT HERE, and why (the classification's other rows — read them): HOLD 1 (`17` / `13`); every UNPROVEN row (L70 — nobody ran it; a test you write for one is NOT in scope); every OWNER ITEM (his: the per-setup ON/OFF dial R44 (c), X10's fix, F1's holes, the impulse size rule, the health pill); every OUT OF SCOPE row. **NO `DEF_WRITTEN_*` value is changed, added or re-pinned by you, for any reason (L35; [F-16] (1)).** A `DEF_WRITTEN_*` assertion that goes RED under your change is a DEFECT OF YOUR CHANGE: fix the code; if you cannot, `FAILED: F<n> — <test> red — <assertion line>`.

## INDEX CARD — read these, in this order, nothing more until a row needs it
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59). Binding here, one line each: **L1** fail-loud — `anchor:none` and an unserved step are named, never silent. **L3** one path — `evaluability` reads the tables formation dispatches through, `ANCHORS` included; the sequence walk uses the one interpreter's dispatch. **L29** Opus floor; this seat is the dev lane, not a write path. **L32** no value, quote or wording of his in anything you write; constructed literals are yours (L69). **L35** every number in your report is tool output, verbatim; no `DEF_WRITTEN_*` value typed by you. **L41** `cobalt_dev` only through the pytest string; `.env` never printed. **L42** RESTARTS by `cobalt jobs restarts`, never by judgement. **L45** formation acceptance stays on the committed real-shape day; a resolver's unit test may run on a constructed series. **L48** evidence in the report the same turn. **L60** recovery = wip-commit + CONTINUE, never discard. **L62 / L63** every permission approved before launch; a denial = FAILED; no dialog. **L67** three checkers read this fix (`16`), ≤3 rounds (this is round 2's fix; a round 3 would be the last). **L70** unproven ≠ defect — you build no UNPROVEN row. **L71** your stop line is the LAST NON-BLANK LINE. **L73** no step dropped.
2. The classification: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-fix-draft-2026-09-22.md` `## CLASSIFICATION TABLE` — the F-rows and P1 are the ONLY work; the other rows say why they are not.
3. Round 1's result: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-one-check-2026-09-22.md` (`## Checked against the branch`, `## ESCALATE` 2–8) and Opus's check `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-one-check/opus-check.md` Q3, Q4, Q6, Q11.
4. THE DESIGN: `/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` §1 `### The mechanism` (`A-01`), §2.2 (`:148`), §2.4, §2.5, §4 (the interpreter table, the `close_through` / `close_above(prior_bar)` row), §7, §8.
5. The build: `docs/40 - DevDocs/reports/setups-one-build-2026-09-21.md` in this worktree — `## CLOSE` (the two suite commands, spelled below), `## ESCALATE` (ii), (ix), (xi) 18, Found 15; the build prompt `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/65-setups-one-build.md` (THE SHAPE OF EVERY STEP, UNATTENDED RULES, RECOVERY RULE — they bind you as written there).
6. THE CODE (RE-READ before every edit): `src/cobalt/radar/anatomy/registry.py` (whole), `src/cobalt/radar/formation/anchors.py` (whole), `src/cobalt/radar/formation/triggers.py` (`Sequence` `:219-248`, `trigger_resolver` `:261-274`), `src/cobalt/radar/formation/atoms.py` (`ATOMS`, `RELATIONS` `:326-335`, `predicate_gaps` `:416-457`, `relation_operand_names`), `src/cobalt/radar/evaluate.py` (the evaluability gate, the anchor block `:1078-1090`, the closure / `assumed_keys` computation), `src/cobalt/radar/evaluate_cli.py` (the dry-run line), `tests/cobalt/setups_shapes.py`, `tests/cobalt/test_setups_lego.py`, `tests/cobalt/test_setups_second_chance.py`, `tests/cobalt/test_setups_vwap_cont.py`, `tests/cobalt/test_setups_x5.py`, `docs/40 - DevDocs/cobalt/radar/ADDING-A-SETUP.md`, the DevDocs of every module you touch under `docs/40 - DevDocs/cobalt/radar/`.

## AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (drafted by the Opus prompt seat `setups-fix-draft-0922`, 2026-09-22), not by Dejan; a prompt file is not an approval. Each its own Bash call:
- **The packaging** is his R44: `grep -n "^| R44 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must carry `ONE BUILD of the whole FINAL`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"ONE BUILD of the whole FINAL" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — R44 is uncommitted`).
- **The two `.env` strings** are his R41: `grep -n "^| R41 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (must quote BOTH strings and `"Approved"`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(rm /Users/cobalt/cobalt-wt/setups-c1/.env)" -- "docs/40 - DevDocs/reports/cto-2026-09-21.md"` (EMPTY → `FAILED: authorization mismatch — the two .env strings are not approved on a committed row`; the pathspec names the desk file ONLY).
- **The desk's fix-round call**, committed: `git -C /Users/cobalt/cobalt log -1 --format=%H -S"14-draft-setups-fix.md" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — the fix-round call is uncommitted`).
- **THIS launch** is row **R16** of `cto-2026-09-22.md` (the desk fills the number in before launch; still `__` in the file you are reading → `FAILED: authorization mismatch — launch row not filled in`, stop): `grep -n "15-setups-fix.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` → a `| R` row · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"15-setups-fix.md" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — the launch row is uncommitted`; the desk file ONLY — the drafter's report quotes this filename and must never satisfy the gate).
- **Each string of your line**, quotes included, one `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/65-setups-one-build.md"` per string: the nineteen allow strings, the three deny strings, the `--add-dir` triplet — each **≥1** (any 0 → `FAILED: authorization mismatch — <rule> is not in 65's approved line`).
YOU CAN ALWAYS STOP: `FAILED: F<n> — <your concern>` as the LAST line, `.env` removed first if it exists, wip-commit (RECOVERY RULE), stop.

UNATTENDED RULES: `65`'s, UNCHANGED (its paragraph "UNATTENDED RULES (prompt `02`'s, unchanged)") — ONE command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no `>`/`2>` redirect, no `; echo`, no `VAR=value` in front other than the literal `COBALT_ENV=dev` of its approved string; long suites `run_in_background` and read whole; `cd <path>` as its own call; counting by YOU from the tool result; reading with the Read tool. **File content through the Write / Edit tools ONLY, never a shell heredoc or redirect** (the build's STEP-3 process slip). **A DENIED BASH CALL = FAILED** (`FAILED: F<n> — <command> — <reason verbatim>`, `.env` removed if present, wip-commit, stop) — never re-shaped, never routed around.

**RECOVERY RULE (L60) — `65`'s, VERBATIM:**
> RECOVERY RULE (L60): partial work is NEVER discarded — `git add <each changed path, by name>` + `git commit -m "wip(setups-one): STEP-<n> partial — <why>"`, the breadcrumb `next: STEP-<n> <sub-step> — <what is left>` written inside `## CONTINUE`, the last line left pinned to `(run in progress — step <n> of 9, next under ## CONTINUE)`; the same launch line resumes from it. **THE RUN WILL OUTLIVE ONE CONTEXT:** write the breadcrumb at the end of EVERY sub-step, not only at a stop; a relaunch reads `## CONTINUE`, re-runs `git status` / `git log --oneline main..HEAD` / `ls -la .env` (a leftover `.env` is removed with the `rm` string and its absence proven BEFORE anything else), re-reads the files the next sub-step touches, and continues — it never redoes a committed step. On a USAGE-LIMIT message: `rm /Users/cobalt/cobalt-wt/setups-c1/.env` if it exists, record the reset time VERBATIM under `## CONTINUE`, wip-commit, stop with the last line pinned (the desk relaunches; a meter stop is NOT a failure). EVERY `git add` names explicit file paths under `src/cobalt/`, `tests/cobalt/`, `tests/experiments/setups_one/`, `configs/cobalt/taxonomy/tunables.yaml`, `docs/40 - DevDocs/` — NEVER `-A`, never `.`, never a directory, never `.env`, never `scratch/`, never `data/`, never `docs/_inflight/`. Long-form `git status` before every commit (`.env` must show as ignored or absent, NEVER staged); `git show --stat HEAD` after — it lists only paths you named, else `FAILED: STEP-<n> — unexpected path committed <path>` (you have no reset rule; the desk repairs it).

Applied here with these names, and nothing else changed: `wip(setups-one): STEP-<n>` reads `wip(setups-fix-r2): F<n>`; `step <n> of 9` reads `row <n> of 6` (F1–F5, P1); `main..HEAD` reads `60ddac4..HEAD`; `STEP-<n>` in a FAILED line reads `F<n>` / `P1`. You add nothing under `configs/` (no FIX row touches config).

REPORT (L48): `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r2-2026-09-22.md`, committed on the branch (the desk watches that path's LAST NON-BLANK line for `^(SETUPS FIX R2 BUILT|FAILED)`). Sections, in order: `## §0 Headline` (≤5 lines) → `## AUTHORIZATION` → `## PREFLIGHT` (table: rule · command · exit · result verbatim) → `## BASELINE` → `## F1` … `## F5`, `## P1`, each with `### T` · `### C` · `### A1` · `### D` · `### SUITE` · `### COMMIT` → `## CLOSE` → `## NOTE FOR THE DESK` → `## ESCALATE` → `## CONTINUE` → the last line. The FIRST Write creates the file with `## CONTINUE` holding `next: AUTHORIZATION` and the pinned last line `(run in progress — row 0 of 6, next under ## CONTINUE)`.
STOP LINE (L71 — the LAST NON-BLANK LINE): `SETUPS FIX R2 BUILT <tip> | on 60ddac4 | offline <p>/<f> | with-DB <p>/<f> | src changed: <yes|no> | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` — `<tip>` = your LAST code commit (the report commit sits above it); `<p>/<f>` = passed / failed from the CLOSE suite lines verbatim; `tests added` = the count of new `def test_` functions, counted from `git diff 60ddac4 --stat` + the files. Or `FAILED PREFLIGHT: <rule>` / `FAILED: F<n> — <command> — <reason>`.

## STEP-0 — PREFLIGHT, then BASELINE (before any edit)
PREFLIGHT, one row each, in this order:
- `date` (every time you write comes from this).
- `git status --porcelain` → EMPTY on a first launch (a resume shows only your own report) — else `FAILED PREFLIGHT: worktree not clean — <lines>`.
- `git status` (LONG form; branch `setups/seven-0921`; "rebase in progress" → `FAILED PREFLIGHT`).
- `git log --oneline -1` → `60ddac4` on a first launch (a resume: your last commit, say which). Anything else on a first launch → `FAILED PREFLIGHT: branch tip is <sha> — the desk re-issues`.
- `git log --oneline 60ddac4..HEAD` → EMPTY on a first launch; a resume lists only your commits.
- `ls -la /Users/cobalt/cobalt-wt/setups-c1/.env` → "No such file" (present on a first launch → `FAILED PREFLIGHT: .env present before the DB step`; on a resume, a leftover is removed with the `rm` string FIRST and its absence proven).
- **STAGGER GATE on the dev-DB lane:** `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/deploy-2026-09-22.md"` — its line must START `STACKED DEPLOY DONE` or `FAILED` (today's stacked deploy `05` holds `cobalt_dev` and refuses while this worktree's `.env` exists). The file missing, or its last line anything else → `FAILED PREFLIGHT: 05 has not stopped — the desk relaunches after its stop line`, launch nothing more.
- **THE SAME WORKTREE'S OTHER JOB:** `ls -la "/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md"` → "No such file" (`12` runs in THIS worktree after you, re-pointed to your tip; present → `FAILED PREFLIGHT: 12 has started in setups-c1 — the desk sequences 15 and 12`).
- `uv run cobalt jobs restarts 60ddac4..HEAD` (empty range on a first launch — the probe of that rule).
BASELINE (on `60ddac4`, before any edit), each quoted VERBATIM — the build report's `## CLOSE` spells them:
- OFFLINE: `uv run pytest -q tests/cobalt tests/taxonomy -p no:cacheprovider` (`run_in_background`) → expected `2419 passed, 361 skipped, 1 xfailed` (the build's CLOSE line); `failed` must be 0 (else `FAILED: STEP-0 — baseline red on 60ddac4: <line>`). Record `<p>/<f>`.
- WITH-DB, inside the `.env` pair: `cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/setups-c1/.env` → `COBALT_ENV=dev uv run pytest -q -p no:cacheprovider -rs tests/cobalt/test_radar_cards_db.py tests/cobalt/test_taxonomy_store.py tests/cobalt/test_radar_score_migration.py tests/experiments/setups_one tests/cobalt/test_rubberband_forms.py tests/cobalt/test_setups_registries.py tests/cobalt/test_setups_d1.py tests/cobalt/test_setups_hitchhiker.py tests/cobalt/test_setups_d4.py tests/cobalt/test_setups_nine_ema.py tests/cobalt/test_setups_vwap_cont.py tests/cobalt/test_setups_second_chance.py tests/cobalt/test_setups_lego.py tests/cobalt/test_setups_x5.py tests/cobalt/test_assumed_store.py tests/cobalt/test_replay_formations.py tests/cobalt/test_replay_runner.py tests/cobalt/test_radar_evaluate.py tests/cobalt/test_radar_evaluate_cli.py tests/cobalt/test_radar_audit_export.py tests/cobalt/test_radar_anatomy.py tests/cobalt/test_archiver_migrations.py tests/cobalt/test_p4_migrations.py tests/cobalt/test_radar_migration.py tests/cobalt/test_tenancy.py` (`run_in_background`; the build's CLOSE set — its four prompt files + every with-DB file it touched, named there without paths) → expected `540 passed, 1 skipped` (the one skip `test_radar_evaluate.py:695`, `COBALT_LIVE_VAULT_ROOT not set` — by design) → `rm /Users/cobalt/cobalt-wt/setups-c1/.env` → `ls -la /Users/cobalt/cobalt-wt/setups-c1/.env` → "No such file". Record `<p>/<f>`.
Every later suite line is compared with these: `failed` stays 0; `passed` grows only by your new tests; `skipped` does not change except by your own with-DB tests skipped offline (named). `## CONTINUE`: `next: F1`.

## THE SHAPE OF EVERY ROW (F1 … F5, P1) — `65`'s THE SHAPE OF EVERY STEP, narrowed
**(T) the test FIRST, run on the code as it stands**, and SAID which kind it is: **RED-on-`60ddac4`** (the row is a real defect — quote the failing assertion line VERBATIM; a test GREEN when it must be RED does not test the change: fix the TEST until it is red, never the code first) or **GREEN-as-pin** (coverage of behaviour that is already right — say so, and say what regression would turn it red). New tests go in ONE new module `tests/cobalt/test_setups_fix_r2.py` (offline, the `setups_shapes` / `radar_p2_support` helpers, neutral notes of your own words and literals — L32 / L69), except the drift-test and X22-guard changes, which stay in `tests/cobalt/test_setups_lego.py` → **(C) the smallest change** that turns it green, in the row's named files only (a needed edit elsewhere → ESCALATE with why; a file another unmerged branch touches → name it, L68) → **(A1)** every existing assertion that went red because it pinned what the row changes BY DESIGN is re-pointed with the SAME strength, listed `test · old · new · row`; a red that is not by design is a DEFECT of your change — fix the CODE, never the test; a removed or loosened assertion, a new skip / xfail, a changed `DEF_WRITTEN_*` is FORBIDDEN → **(D)** the DevDoc of every touched `.py` gets ONE paragraph for this row (`docs/40 - DevDocs/cobalt/radar/<module path>.md`, one wiki file per `.py`) — nothing else in it changes → **(SUITE)** the OFFLINE command above → `failed` 0, quoted (the with-DB suite runs once at CLOSE, not per row, unless the row touches a with-DB file — then per row, inside the `.env` pair) → **(COMMIT)** by explicit paths, `git commit -m "fix(setups): round 2 — F<n> <what>"`, `git show --stat HEAD` quoted → breadcrumb `next: <row>` under `## CONTINUE`, and the next row starts IN THE SAME TURN.

## F1 — anchors: evaluability names `anchor:none`; ADDING-A-SETUP documents the anchors
T (RED-on-`60ddac4`), in `test_setups_fix_r2.py`: a neutral def from EXISTING bricks and no anchor object — preconditions `InPlay.state == active`, `price > VWAP`, `price > EMA21`; trigger `bar_break {bars_cleared: <your literal>}`; stop `indicator VWAP, at_entry` (Opus Q4's worked def, in your own note) — loaded through `load_vault_trade_defs`: `evaluability(td).evaluable is False` and `"anchor:none" in evaluability(td).missing_atoms` (RED today: evaluable True); `evaluate_member` on a committed-day scan → `evaluation == "not_evaluable"` with `anchor:none` named (RED today: `not_formed`, note `no formation anchor`). Plus, in `test_setups_lego.py`, the drift test gains `ANCHORS`: every `row.object` of `formation.anchors.ANCHORS` appears in `ADDING-A-SETUP.md` (RED today: the doc has no anchor object as an anchor). GREEN guards to quote after C: all eight corpus shapes (`SHAPES` + the eighth) still `evaluable` (`test_every_unlocked_setup_shape_is_evaluable`, `test_lego_iv_evaluability_for_all_eight` unchanged in text and outcome).
C: `src/cobalt/radar/anatomy/registry.py` (`evaluability` reads `formation.anchors.anchor_for`; the module docstring names the fifth table it now reads), `src/cobalt/radar/formation/anchors.py` (only if an export is needed), `src/cobalt/radar/evaluate.py` only if the not-evaluable note does not already carry the missing list verbatim (then the smallest change; the `:1080-1081` branch STAYS), `docs/40 - DevDocs/cobalt/radar/ADDING-A-SETUP.md` (the "Anchors" section above, and the `anchor:none` line in "what the system says when a definition asks for a brick that does not exist"; ≤120 lines if it can hold it — `65`'s cap; if it cannot, state the line count in the report, never drop a section).

## F2 — `sequence` evaluates its steps through the one interpreter
T (RED-on-`60ddac4`), in `test_setups_fix_r2.py`, neutral defs of your own:
- (a) a `sequence` whose step list is NOT second-chance's — e.g. `price close_through Level_ref` then `close_above(prior_bar)` (two steps), on the same RangeBreak-anchored preconditions as the second-chance shape — `evaluability` → evaluable (RED today: `trigger:sequence` missing), and on a CONSTRUCTED bar series written from its own words it FORMS with the trigger bar = the first bar after the break on which `close_above(prior_bar)` holds, trigger price = that bar's close; a second series on which the last step never holds does not form.
- (b) a `sequence` with a step whose predicate uses an atom no registry serves → NOT evaluable, and `missing_atoms` names THAT atom (never `trigger:sequence`) (RED today).
- GREEN guards, quoted before AND after C, UNTOUCHED in text: `tests/cobalt/test_setups_second_chance.py` whole (its `DEF_WRITTEN_SECOND_CHANCE_*` assertion included), the Lego tests, `tests/cobalt/test_setups_x5.py` (quote X5's number before and after — FINAL §3's budget is 100 s).
C: `src/cobalt/radar/formation/triggers.py` (`Sequence`: `serves_def` asks the interpreter's gap dispatch about each step; `resolve` walks the steps in bar order), `src/cobalt/radar/formation/atoms.py` (the served step shapes `close_through`, `close_above(prior_bar)` per FINAL §4, and a bar-indexed resolution for each served step shape — `event(retest)` reads the RangeBreak observation's retest bar as today), `src/cobalt/radar/anatomy/registry.py` only if the step gaps need it. The anaphora and `Level_ref` binding stay as built (the def's `RangeBreak(level)` anchor); any other form is `Unsupported(…)`, named.
SAFE DEFAULT (stop F2, not the run): if walking the steps in bar order cannot be built without (i) a rule the FINAL does not state (how a step's bar is found for a predicate shape the seven do not use), or (ii) X5 above 100 s — STOP F2, keep `Sequence` as it is (revert your F2 edits by Edit, the wip commit keeps the tests), `ASK DESK: F2 needs <the missing rule | X5 = <n> s> [<time>]` under `## ESCALATE`, and continue with F3. Second-chance's pins and outcomes are never the price of F2.

## F3 — the trigger resolver's reads join the declared closure
T (RED-on-`60ddac4`), in `test_setups_fix_r2.py`: a `tmp_path` vault whose `1 - Trading/Assumed Defaults.md` `tunables:assumed` unit fills `range.micro.touch_tolerance_atr` and `range.micro.bound_flat_slope_atr` with literals of YOUR choosing (L69, never the companion's — the build's `Found 11` hygiene rule: grep the companion's key NAMES only, never its values), loaded through the real reader; the vwap-continuation shape evaluated on the committed day with those rows merged → on every formed scan, `formation.assumed_keys` contains the filled key(s) the trigger read, and the card's `assumed_formation` dot names them (RED today: the keys are not in the closure). No DB: the merge is offline (`merge_tunables`), as the build's T5 pattern does.
C: `src/cobalt/radar/formation/triggers.py` (each resolver declares the keys it reads — `TrendlineBreak` at least `pivot.n` and the `range.micro.*` it reads through the micro-Range observation), `src/cobalt/radar/formation/stops.py` only if a stop resolver reads an undeclared key (X22 names none today), the closure union in `src/cobalt/radar/evaluate.py` (or wherever the build computes `assumed_keys` — find it: `grep -n "assumed_keys" src/cobalt/radar/evaluate.py`). A1 (BY DESIGN, listed): `test_setups_lego.py` `TRIGGER_READ_UNDECLARED` → `frozenset()`; `ALWAYS_READ_UNDECLARED` and `:333` untouched. Any card pin that moves because a closure grew is NOT by design at committed config (no filled row is assumed there) → a defect of your change.

## F4 — the rollback order after the assumed note (DOC ONLY)
T: none (a doc row; say so). Evidence first: `git show main:src/cobalt/taxonomy/tunables.py` → quote the `class TunableSource` lines (no `ASSUMED`) and `git show 60ddac4:src/cobalt/db_migrations/0013_tunables_slug_nullable.rollback.sql` → quote its refusal. C: `ADDING-A-SETUP.md` § "Rolling back after the assumed note" — three ordered lines and the why, in plain words, no command invented beyond the existing `taxonomy load` and `db migrate` names the doc already uses. COMMIT with F1's doc if you prefer; say which.

## F5 — `AWAITING_A_RULING` gets its own pin (TEST ONLY, GREEN-as-pin)
In `test_setups_lego.py` (beside `AWAITING_A_RULING`, `:45`): for every key, `evaluability(...).evaluable is True` and the shape forms on NO scan of its committed tickers; and one test that the vwap-continuation shape, loaded with its `engine=` fill REMOVED (committed engine rows only), forms on NO scan — its `AWAITING_A_RULING: F1` status as an assertion. Say which regression turns each red. If ANY of them is RED on `60ddac4` (a member that forms, or vwap forming without the fill), that is a FINDING, not a pin to bend: record the run's output under `## ESCALATE` with `ASK DESK: <shape> forms at <scan> — the AWAITING_A_RULING list is wrong [<time>]`, do NOT commit that one assertion (never a red test, never an xfail), and continue.

## P1 — the single-sentence relations are reported, not silent (TEST ONLY, GREEN-as-pin)
In `test_setups_fix_r2.py`: one neutral def per relation word `after`, `inside`, `on`, `between`, each used in a sentence OUTSIDE its served one → `evaluable is False`, and `missing_atoms` carries the `Unsupported(<word>:…)` entry — the literal read from the run's own output and pasted (never guessed). No code. Say what regression turns each red.

## CLOSE — the proofs, then the report
Each its own call, each result quoted VERBATIM:
- OFFLINE (the BASELINE command) → `failed` 0; `passed` = baseline + your new tests (say how counted).
- WITH-DB inside the `.env` pair (the BASELINE command, plus `tests/cobalt/test_setups_fix_r2.py` if it holds a with-DB test) → `failed` 0, the one by-design skip → `rm` → `ls -la` → "No such file" (`.env` present at the report commit → `FAILED: CLOSE — .env left in the worktree`).
- `git diff --stat 60ddac4` → ONLY `src/cobalt/radar/anatomy/registry.py`, `src/cobalt/radar/formation/*.py`, `src/cobalt/radar/evaluate.py` (if touched), `tests/cobalt/test_setups_fix_r2.py`, `tests/cobalt/test_setups_lego.py`, `docs/40 - DevDocs/cobalt/radar/**`, the report. Any other path → ESCALATE with why.
- EMPTY, each its own call, each quoted "no output": `git diff 60ddac4 -- configs` · `git diff 60ddac4 -- src/cobalt/db_migrations` · `git diff 60ddac4 -- src/cobalt/cards` · `git diff 60ddac4 -- src/cobalt/taxonomy` · `git diff 60ddac4 -- tests/fixtures` · `git diff 60ddac4 -- tests/cobalt/test_rubberband_forms.py tests/cobalt/test_setups_nine_ema.py tests/cobalt/test_setups_vwap_cont.py tests/cobalt/test_setups_second_chance.py` (the four files that hold the `DEF_WRITTEN_*` pins — `17` re-derives them blind on `60ddac4`; any hunk here → `FAILED: CLOSE — a pinned test file changed`).
- `grep -rn "DEF_WRITTEN_" tests/cobalt/test_setups_fix_r2.py` → no output (you pinned nothing new of that name).
- `uv run cobalt jobs restarts 60ddac4..HEAD` → the table VERBATIM (L42); any `UNCLASSIFIED` → ESCALATE, never dropped.
- `git log --oneline 60ddac4..HEAD` → your commits, listed.
- L68: `git -C /Users/cobalt/cobalt log --oneline main..bars/chunk-2-0920 -- <each path you changed>` and the same for `bars/chunk-1a-0920`, `bars/chunk-e-0920` → a table `branch · shared path · commits` (the drafter expects none: no FIX row touches their paths).
`## NOTE FOR THE DESK` (write it verbatim, then your tip): "`prompts/2026-09-22/12-setups-fixture-cut.md` PREFLIGHTs on tip `60ddac4` (`git log -1 --format=%h` = `60ddac4`, its RECOVERY `git log --oneline 60ddac4..HEAD`, its pin comment `# engine on the cut day at 60ddac4`) and must be re-pointed to this fix's report commit before it runs — launch mechanics only, its strings unchanged (L19: a full re-issue of the file). `13` reads `12`'s output and needs no re-point. `16` reads this report's last line."
`## ESCALATE` carries, besides what you found, ALWAYS: (i) HOLD 1 is NOT closed here — `17` (committed day, the 17 existing pins) and `13` (the cut days); (ii) F4's section is the deploy prompt's rollback source — its drafter must read it; (iii) every `ASK DESK` with the safe default taken; (iv) every A1 re-point, one line each; (v) the with-DB suite's 11 out-of-set reds the build named (`Found 6`) are NOT re-run and NOT yours (L70); (vi) `MEMORY:` / `RULING:` lines if any (L48, L58).
Then the stop line as the LAST NON-BLANK line, commit the report by explicit path (`git commit -m "fix(setups): round 2 — report"`), `git show --stat HEAD` (the report only), `git status --porcelain` → EMPTY, and stop.
**NEXT STEP, not yours (L67):** the desk verifies the artifact (L35 — clean worktree, `.env` absent, both suite lines, the four pinned files untouched) and launches `16-setups-check-r2.md` once `17` is DONE; it re-points `12` to your tip.
