MODEL: Opus 5.5 (`claude-opus-5-5`, R36 / R109) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `r3-check-devdb-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/66-draft-r3-check-devdb-repair.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control r3-check-devdb-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `39` / `58` / `62`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool, the REPORT LAST · METER: Anthropic medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. Never end a turn between steps. (`git -C /Users/cobalt/cobalt-wt/setups-c1 …` reads: use `ls` / `grep` / `tail` on that tree's files, and `git -C /Users/cobalt/cobalt show <sha>:<path>` / `log` — the branch's commits are in the shared object store.)

# TWO DRAFTS, one report. LADDER: (A) `S2 · F8/F10 LADDER CHANGE` (the setups ONE build — S2 stops 2026-09-23 and its smoke checks that need a formed card stay red until this deploys); (B) the dev-DB lane that `31` (stale-score, S2 health) and `27` (H1) wait on — ops repair, no ladder line of its own, it unblocks the lane.

INDEX CARD: read `6 - Permanent/Memory/LAWS.md` in full (L59). Binding: L1 · L4 / L41 (`.env` copied by name only, never printed; production credentials never issued) · L28 (live DB never a test target) · L29 (a DB write / delete / recovery = Opus floor, never auto mode) · L35 · L37 · L45 · L62 / L63 (every permission listed before launch; `acceptEdits` + `--allowedTools` ASKS on an unlisted Bash — list every command the repair runs, verbatim) · L67 (code check seats: Opus + Sol, Grok, Gemini — R49; Sol METER until Sat 09-26 06:47 ET, probed then skipped) · L68 · L70 · L71 · L74. Files: `reports/setups-fix-r3-2026-09-22.md` (in `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/`, committed `8da261a` on `setups/seven-0921`); `reports/build-lane-draft-2026-09-22.md` `## ORDER` (the `16` re-point list); `prompts/2026-09-22/16-setups-check-r2.md`; `prompts/2026-09-22/33-setups-fix-r3.md`; `prompts/2026-09-22/31-stale-score-build.md`; `src/cobalt/devdb.py`, `src/cobalt/env.py` (`assert_destructive_target`), `src/cobalt/db.py`, `src/cobalt/db_migrations/`, `tests/cobalt/test_*migrat*.py`; `topics/cto-desk.md` lessons 2026-09-21/22.

FACTS the desk read at 19:4x ET (read-only `COBALT_ENV=dev uv run cobalt db query --side system --format json`, pg_catalog — L35 unscoped): in `cobalt_dev`, `public.aset_sizings` carries **1,560 dropped columns** (`attisdropped`), `public.day_modes` 194, `public.vault_writes` / `card_stop_edits` / `card_transitions` / `vault_overrides` / `cobalt_jobs` 130 each; `"user".aset_sizings` does NOT exist; the new-core tables sit in `public` — so the r3 hub's reading is CONFIRMED: `cobalt_dev` is left BELOW the schema move (a round-trip test's `--rollback --down-to 0001` then `migrate` failed at `0007` with `TooManyColumns: tables can have at most 1600 columns`). Dropped columns are NOT reclaimed by `VACUUM FULL` in PostgreSQL (the attribute slots stay); only a NEW table (recreate / dump-restore of the table, or a new database) frees them — prove or refute that from the PostgreSQL docs text you can quote, or mark it UNPROVEN and design the repair so it works either way (L70).

## (A) `67-setups-check-r3.md` — `16` RE-POINTED to round 3
Copy `16-setups-check-r2.md` BYTE FOR BYTE (its launch line's 15 allow strings, 3 denies, `--add-dir` triplet unchanged; only the prompt path and remote-control `setups-check-r3-0922` differ) and apply exactly the re-point list in `build-lane-draft-2026-09-22.md` `## ORDER` (`:1`, `:15`, `:16`, `:18`, `:33–:35`, `:45`): the r3 report and its stop line `SETUPS FIX R3 BUILT be44eb4 | on 65c08a0 | …`; ranges `65c08a0..be44eb4` (report commit `8da261a`); `33`'s CLOSE path list PLUS the files r3's ESCALATE (ix)/(x) names outside it (`src/cobalt/radar/evaluate.py`, `anatomy/extension.py`, `anatomy/frame.py`, `formation/atoms.py`, the four test files) — a checker reads them, the hub does not treat them as out of scope; the FIX-row question set → r3's F1–F6; add r2 ESCALATE (viii) (stop resolvers not given `tunable_keys`) and r3's (v) finding (per-setup tuning of an ENGINE dial not possible today) as QUESTIONS; the gitignored proposal `docs/_inflight/setups-assumed-values-r3-2026-09-22.md` is READ by the check (R48) — stage its KEYS and grades only, never a value into a committed file (L32). THIS = round 3 of at most 3 (L67): the prompt says so, and a defect that HOLDS goes to `## FOR DEJAN` with the override question, never to a round 4 (L39). OFFLINE ONLY: the check's with-DB reads are UNPROVEN until (B) lands — state it; never touch `cobalt_dev`. DATE GATE: `Bash(grok *)` / `Bash(agy *)` through 2026-09-23 23:59 ET on R30 (copy `16`'s gate). STAGGER: refuses while `59` (voice tribunal) or `63` (routing tribunal) is in progress without a stop line (the `<nn> is not running` launch-row rule). Stop line `SETUPS CHECK R3 DONE · grok: … · gemini: … · opus: … · sol: … · defects that HOLD: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`; report `reports/setups-check-r3-2026-09-22.md`.

## (B) `68-devdb-repair.md` — rebuild `cobalt_dev`'s schema, dev only
MODEL Opus 5.5 (L29 floor), `--permission-mode acceptEdits` + an explicit `--allowedTools` list naming EVERY command verbatim (L62 / L63 — nothing the hub runs may be unlisted), cwd `~/cobalt` (read-only there: no file written in `~/cobalt`; its report is written with the Write tool under `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/devdb-repair-2026-09-22.md` and committed by the DESK, not the hub — the hub makes NO git write). Design the SMALLEST repair that leaves `cobalt_dev` at main's migration head with no table near the 1,600 limit, preferring an EXISTING guarded path (`src/cobalt/devdb.py` and `cobalt.env.assert_destructive_target()`, which refuse any database but `cobalt_dev`) over raw `psql` / `docker exec`; if no guarded path can drop or recreate a table or the database, say so and name the smallest raw command, with the refusal it lacks, as an ESCALATE the desk brings to Dejan — never a production-reachable command (L28; the DSN never names `cobalt_brain`). Steps: PREFLIGHT (`date`; the lane is free — `ls -la /Users/cobalt/cobalt-wt/*/.env` shows no file; read-only state: the dropped-column counts and the tables per schema, as the desk read them) → the repair → `COBALT_ENV=dev uv run cobalt db migrate` → PROOF (read-only: every new-core table on exactly one side, max dropped-column count, the migration head equal to main's highest `db_migrations/NNNN`) → with-DB proof on main: `COBALT_ENV=dev uv run pytest -q -p no:cacheprovider tests/cobalt/test_tenancy.py` + the three r3 CLOSE reds by path. ROOT CAUSE, named for an ops item (not built here): which test's rollback / re-apply adds dropped columns every run, and the fix shape (e.g. run the round trip in a scratch schema, or recreate instead of `ALTER … DROP COLUMN`). Stop line `DEVDB REPAIRED · head: <NNNN> · max dropped: <n> · tenancy: <p>/<f> · r3 reds: <p>/<f> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`.

LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/r3-check-devdb-draft-2026-09-22.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (launch order: `68` first if `67` needs nothing from it — it does not, it is offline; `67` in the house lane NOW, ahead of `59` / `63`; `31` after `68`'s stop) → RULE PROOF (`67` vs `16` byte for byte; expected differences: path, remote-control name) → **`NEW strings:`** `68`'s full list verbatim, one per line, each marked NEW or `= <file>` where an approved prompt already carries it (`grep -c -F`) — these go to Dejan as ONE approval list → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `R3 CHECK + DEVDB REPAIR DRAFTED · prompts: 2 · 67 new strings: <n> · 68 new strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write. L32 / L4: no secret, no value of his in any file you write. Stop within 35 minutes.
