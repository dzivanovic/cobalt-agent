MODEL: Sonnet 5 (`claude-sonnet-5`) — stage a packet, put it before ONE code-capable house, copy its answer, then compare; no verdict of your own (L37) · SEAT: blind-code hub `setups-blind-code-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK when the house lane is free (the STAGGER gate below), the launch row **R25** (the desk fills the number) and this file are committed on main, and HIS row approving the house's NEW string exists (the `## NEW STRINGS` of `reports/setups-blind-code-draft-2026-09-22.md`). ONE of two lines, by date:
- **LINE-G (Grok, through 2026-09-22 23:59 ET under 09-21 R39):** `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/23-setups-blind-code-seat.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control setups-blind-code-0922 --allowedTools "Bash(grok *)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt`
- **LINE-S (Sol, from Sat 2026-09-26 06:47 ET, Codex meter back per `cto-2026-09-22.md` R13):** the same line with `"Bash(grok *)"` REPLACED by `"Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s workspace-write *)"` (NEW — his row required) and `-0922` in the remote-control name replaced by the launch day's `-MMDD`.
RULE STRINGS: the seven read strings, the three deny strings and the `--add-dir` triplet are `prompts/2026-09-21/66-setups-one-check.md`'s launch line byte for byte (the `17` / `13` set minus `agy` and `claude -p`); `"Bash(grok *)"` is `66`'s (his R39 of 09-21, through 2026-09-22 23:59 ET). INSIDE the house call: Grok's approved `--allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)"` (`66` §2) PLUS ONE NEW run rule `--allow "Bash(python3 /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code/*)"` (the 09-16 audit-house run form, `reports/audit-house-2026-09-16.md:238`, narrowed to this packet folder); Sol's `-s workspace-write` line is NEW. `auto` mode: reads only for you, the hub — the house writes, you do not run code (L35 is served by the comparison, which is string equality on the house's file) · SESSION: fresh · no DB, no `.env`, no docker, no pytest, no git write, no vault write, no memory-folder write (L58); the one house you launch is the seat this prompt names (a check hub puts a packet before a house; you launch no hub and no worker of your own, L36) · METER: Anthropic small (you) + ONE house call (Grok, or Sol on LINE-S) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default (fewer files staged, never more access), continue.

# BLIND CODE SEAT ON THE COMMITTED DAY — a house that has NOT seen the pins COMPUTES the 17 existing `DEF_WRITTEN_*` values with code of its own, from the bars, the definitions and the engine's detector SOURCE as rule text (2026-09-22)

WHY: `reports/setups-blind-committed-day-2026-09-22.md` (`17`'s hand seat) stopped `MATCH: 0 · MISMATCH: 0 · UNDERIVABLE: 17` — a hand derivation cannot produce values resting on EMA / VWAP / ATR seeding, the extension lifecycle, leg roles, the level set and the RangeBreak lifecycle. FINAL [F-16] (1) (`docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`) still binds: *"the expected side, formed bar, trigger price and stop price are written by a CHECKER house from the bars alone, before it sees the builder's output"*; round 1's Grok verdict is `DO NOT DEPLOY until expected sides are independently derived`. So this house COMPUTES: it gets the bars, the neutral shapes, the public rules and the engine's detector SOURCE — source is RULES; the pinned VALUES and the tests that hold them are the builder's OUTPUT and stay out. LAW STEP: check r1 (HOLD 1) → `17` (hand: UNDERIVABLE) → **THIS = the code seat** → check r2 `16` reads your `## Comparison` → `12` → `13` → deploy prompt → his "approve". LADDER: `S2 · F8/F10 LADDER CHANGE — all 13 setup definitions at defaults (09-21 R15/R17/R18/R44)`.

## INDEX CARD (you; the house gets the packet only)
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — one line each: **L32** no value of his note enters the packet; the definitions staged are the build's NEUTRAL shapes, never `4 - Strategies/*.md`. **L33** a seat that must produce an artifact gets WRITE access to its own workspace — here the packet folder only — and no network; a Codex run that writes launches `-s workspace-write`. **L35** every value in your table is copied from a file, verbatim. **L37** you judge nothing: MATCH / MISMATCH / UNDERIVABLE is string equality after `13`'s normalisation and nothing else. **L41** no credential, no DB, no `.env` reaches the seat — the fixture files only. **L44 / L59** equal access is the entitlement; the packet is the working set for a BLIND seat, never a withholding of rules — the source is staged whole. **L45** the packet's bars are the committed real-shape fixture, byte-identical. **L48** evidence in the report the same turn. **L62 / L63** every string approved before launch; unattended; a denial is a FAILED. **L67** the seat is a house OTHER than the build's author: Anthropic built it — Grok or Sol, NEVER Opus, never Fable. **L70** an UNDERIVABLE is not a defect. **L71** the stop line is the last non-blank line.
2. `prompts/2026-09-22/17-setups-blind-committed-day.md` — the SHAPE this file copies: its packet (a)–(f), THE 17 PINS table (reproduced below), KEEP-OUT, ORDER proof and comparison. Where this file is silent, `17` binds (and through it `13`, then `66`).
3. `prompts/2026-09-21/66-setups-one-check.md` — its packet rules (Read → Write byte-identical, parts ≤ 38,000 B, `wc -c` against the original, the whitespace-gap check) and its §2 Grok spelling.

**THE 17 PINS** (`17`'s table, at `60ddac4`, in `/Users/cobalt/cobalt-wt/setups-c1/`):
| slug | file | lines | constants | how the test reads the formation |
|---|---|---|---|---|
| rubberband | `tests/cobalt/test_rubberband_forms.py` | :56 :58 :60 :62 :64 | `DEF_WRITTEN_RUBBERBAND_FORMED_BAR` · `_SIDE` · `_TRIGGER` · `_STOP` · `_LAST` | the evaluation AT ONE scan, `2026-01-06T16:30:00+00:00`, ticker FTFT, on the shape `rubberband_mapping(MIXED, htf_avoid=False)` (4 countertrend + 1 with_trend setup refs, no HTF avoid) — five fields: formed bar, side, trigger, stop, last close |
| nine-ema-scalp | `tests/cobalt/test_setups_nine_ema.py` | :221 :223 :225 :227 | `DEF_WRITTEN_NINE_EMA_SCALP_SIDE` · `_FORMED_BAR` · `_TRIGGER` · `_STOP` | the FIRST formed scan walking FTFT's scans then BGFI's, on the corpus shape `SHAPES["nine-ema-scalp"]` |
| vwap-continuation | `tests/cobalt/test_setups_vwap_cont.py` | :203 :205 :207 :209 | `DEF_WRITTEN_VWAP_CONTINUATION_SIDE` · `_FORMED_BAR` · `_TRIGGER` · `_STOP` | the same walk, `SHAPES["vwap-continuation"]` |
| second-chance | `tests/cobalt/test_setups_second_chance.py` | :198 :200 :202 :204 | `DEF_WRITTEN_SECOND_CHANCE_SIDE` · `_FORMED_BAR` · `_TRIGGER` · `_STOP` | the same walk, `SHAPES["second-chance"]` |
Total fields: **17**. Hitchhiker, backside and fashionably-late are not asked.

**NEVER STAGED, NEVER OPENED BY YOU BEFORE THE HOUSE HAS ANSWERED:** `17`'s list unchanged — the four files above, `tests/cobalt/test_setups_lego.py`, `tests/cobalt/test_setups_hitchhiker.py`, every other `tests/cobalt/test_*.py`, the build report `setups-one-build-2026-09-21.md`, `prompts/2026-09-21/65-setups-one-build.md`, `56-setups-c1-build.md`, `tests/cobalt/setups_shapes.py:1-44`, `tests/cobalt/radar_p2_support.py:38` (`FORMED_AT`, `17`'s exclusion), round 1's check reports AND the folder `scratch/tribunal-bars-0920/setups-one-check/`, `12`'s cut report outside `## FOR 13`, and ANY line containing `DEF_WRITTEN_` — PLUS, new here: **`reports/setups-blind-committed-day-2026-09-22.md` WHOLE** (its `## Comparison` carries the pinned values), `reports/setups-check-r2-2026-09-22.md`, the folders `scratch/tribunal-bars-0920/setups-blind-committed/` (another house's answer) and `scratch/tribunal-bars-0920/setups-check-r2/`, and `src/cobalt/radar/evaluate_cli.py:218` (a `FORMED ` print line; the file is excerpted, below). Before each staged file is written: `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED " <original>` → 0 (for an excerpt: on the excerpt's lines, counted from a `grep -n` of the original), else that file is NOT staged (ESCALATE).

REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-blind-code-2026-09-22.md` (Write tool; you commit nothing — the desk commits it). Sections: `## §0 Headline` (≤5 lines) → `## AUTHORIZATION` → `## PREFLIGHT` → `## Packet` (file · original path · lines · bytes original · bytes staged · parts) → `## House` → `## Program` → `## ORDER` → `## Comparison` → `## ESCALATE` → `## CONTINUE` → the last line. FIRST Write creates it with `## CONTINUE` = `next: AUTHORIZATION` and the last line `(run in progress — next under ## CONTINUE)`.
STOP LINE (L71 — LAST NON-BLANK LINE): `SETUPS BLIND CODE SEAT DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: <why>` / `FAILED PREFLIGHT: <rule>`.

AUTHORIZATION — each its own Bash call; a prompt file is not an approval:
- THIS launch: `grep -n "23-setups-blind-code-seat.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (and the launch day's `cto-2026-09-2<n>.md` if later; a missing desk file is recorded, not fatal) → a `| R` row with its number filled (a row still `R__` → `FAILED: authorization mismatch — launch row not filled in`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"23-setups-blind-code-seat.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` non-empty (else `FAILED: authorization mismatch — the launch row is uncommitted`).
- The context rows: `grep -n "^| R12 " …/cto-2026-09-22.md` (the check) · `grep -n "^| R15 " …/cto-2026-09-22.md` (the hand seat `17`) · `grep -n "^| R39 " …/cto-2026-09-21.md` (record what it says of `grok` and until when) · `grep -n "^| R49 " …/cto-2026-09-21.md` (Sol's READ-ONLY string — it does NOT cover `-s workspace-write`; recorded as context only).
- Each read / deny string and `"Bash(grok *)"`: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/66-setups-one-check.md"` ≥1 each (the seven, the three deny strings, the triplet, `Bash(grok *)`, and `Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)`). Any 0 → `FAILED: authorization mismatch — <rule>`.
- The Grok run form's precedent: `grep -c -F -e "Bash(python3 /Users/cobalt/cobalt-wt/agy-trial/scratch/toy-check-grok/*)" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/audit-house-2026-09-16.md"` ≥1 (the proven shape; not an approval).
- EACH NEW STRING needs HIS word: for the house you will use, `grep -n -F -e "<the new string>" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-2<n>.md"` (every desk file from `-22` to the launch day) → a `| R` row carrying that literal AND his words in quotes (a DESK LAUNCH ROW with "NO WORDS OF HIS" does NOT count) + `git -C /Users/cobalt/cobalt log -1 --format=%H -S"<the new string>" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` non-empty. Grok: the string is `Bash(python3 /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code/*)`. Sol: `Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s workspace-write *)`. Missing → `FAILED: authorization mismatch — no row of his approves <string>`, launch nothing.

UNATTENDED RULES (`66`'s, unchanged): ONE command per Bash call, the bare listed prefix, no pipe, no redirect, no `cd … &&`, no `VAR=value` in front; files by the Write tool; reading by the Read tool (large files with offset / limit). **A DENIAL IS A FAILURE:** `FAILED: <step> — <command> — <reason verbatim>`, stop. RECOVERY (L60): a relaunch first runs `ls /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code`; a staged file is not re-staged (its `wc -c` is re-proven); an existing `blind-values.md` is NEVER re-asked — you go to `## ORDER`.

## STEP-0 — PREFLIGHT (one row each: command · exit · result verbatim)
- `date` — **THE DATE GATE:** `<D>` = 2026-09-22 and before 23:59 ET → HOUSE = Grok (LINE-G). `<D>` ≥ 2026-09-26 and at/after 06:47 ET → HOUSE = Sol (LINE-S). Any other time → `FAILED PREFLIGHT: no house window — Grok's R39 closed, Sol's meter not back`. The line you were launched with must match the house (Grok needs `Bash(grok *)` in your allow list, Sol the workspace-write string): a mismatch shows as a denial → FAILED.
- The house probe: Grok — `grok --version`; Sol — `codex exec --skip-git-repo-check -m gpt-5.6-sol -s workspace-write "Reply with exactly the word OK"` (`run_in_background`, 3 minutes). A METER / HARNESS line → `FAILED PREFLIGHT: <house> down — <line verbatim>`. There is NO fallback house: never Opus, never Fable (L67 — the builder's house).
- **STAGGER — the house lane (one Grok / Gemini hub at a time):** `tail -n 1 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/float-handicap-tribunal-r2-2026-09-22.md"` — must START `FLOAT HANDICAP TRIBUNAL R2 DONE` or `FAILED`; if the file does not exist, `ls "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/19-handicap-tribunal-r2.md"` must FAIL (never drafted); anything else → `FAILED PREFLIGHT: 19 is running — the desk staggers`, launch nothing. (On LINE-S the gate is recorded and still applies.)
- **THE FIX ROUND IS NOT EDITING THE WORKTREE:** `ls "/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r2-2026-09-22.md"` — absent = `15` not started (continue); present → `tail -n 1` of it must START `SETUPS FIX R2 BUILT` or `FAILED`, else `FAILED PREFLIGHT: 15 is editing setups-c1 — the source you would stage is in flight`.
- `git -C /Users/cobalt/cobalt log --oneline -1 setups/seven-0921` (record the tip `<tip>`).
- **THE PINS ARE STILL `60ddac4`'s:** `git -C /Users/cobalt/cobalt log --oneline 60ddac4..setups/seven-0921 -- tests/fixtures/radar tests/cobalt/setups_shapes.py tests/cobalt/radar_p2_support.py tests/cobalt/test_rubberband_forms.py tests/cobalt/test_setups_nine_ema.py tests/cobalt/test_setups_vwap_cont.py tests/cobalt/test_setups_second_chance.py` → EMPTY (else `FAILED PREFLIGHT: a staged fixture or a pinned file moved after 60ddac4 — <line>`).
- **THE SOURCE YOU STAGE:** `git -C /Users/cobalt/cobalt log --oneline 60ddac4..setups/seven-0921 -- src/cobalt/radar src/cobalt/taxonomy src/cobalt/archiver/aggregate.py configs/cobalt/taxonomy/tunables.yaml` — EMPTY → the source is `60ddac4`'s; NON-EMPTY (only possible after `15` BUILT) → quote every line; you stage what the worktree holds at `<tip>` and say so in `## Packet` (the pins are unchanged at `<tip>` by the gate above and `15`'s suite keeps them green there).
- `ls /Users/cobalt/cobalt-wt/setups-c1/tests/fixtures/radar` · `ls /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920` (the base folder Grok's Write rule covers must exist; absent → `FAILED PREFLIGHT: scratch/tribunal-bars-0920 missing`; you do NOT create it).

## STEP-1 — STAGE (`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code/` — the first Write creates it; NO `mkdir`)
By Read → Write byte-identical from `/Users/cobalt/cobalt-wt/setups-c1/`, each proven by `wc -c` (original vs staged; parts sum to the original), `66`'s whitespace-gap check first (`grep -c -E "[[:space:]]$" <original>` — the drafter counted 0 for every source file below at `60ddac4`; re-count, disclose any gap):
 (a)–(e) **`17`'s packet, unchanged:** the committed day (`tests/fixtures/radar/bars-rubberband.real-shape.json` in parts ≤ 38,000 B cut only after a `},` line; `daily-bars.real-shape.csv` whole — its CRLF line endings do not survive the Write tool: disclose the gap with the `grep -c $'\r'`-style count `17` recorded (76), never silently; `card-settings.real-shape.json` whole) · the loaders excerpt of `tests/cobalt/radar_p2_support.py` (`FIXTURES`, `TRADE_DATE`, `fixture_bars`, `fixture_daily`, `fixture_settings_rows`, `engine_tunables`, `defaults` — found with `grep -n "^def \|^TRADE_DATE\|^FIXTURES"`; NEVER line 38) · the `tests/cobalt/setups_shapes.py` excerpts `17` (c) names (never `:1-44`; `DAY_START` written into `TASK.md` as `2026-01-06T14:30:00+00:00`) · the public rules excerpt of `SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` `17` (e) names. Each excerpt headed by one line naming its real path and line range.
 (d′) **THE TUNABLES WHOLE** (not `17`'s excerpt): `configs/cobalt/taxonomy/tunables.yaml` (34,366 B at `60ddac4` — one file, under the part limit).
 (g) **THE DETECTOR SOURCE, WHOLE** — every file byte-identical, staged under a FLAT name with a `.txt` suffix so it cannot be imported (`src/cobalt/radar/anatomy/extension.py` → `SRC__cobalt__radar__anatomy__extension.py.txt`; parts `….py.part1.txt` …), each listed in `TASK.md` with its REAL path:
   - `src/cobalt/radar/anatomy/`: `__init__.py`, `bars.py`, `daily.py`, `extension.py`, `frame.py`, `freshness.py`, `in_play.py`, `indicators.py`, `leg_roles.py`, `leg.py`, `micro_range.py`, `pivots.py`, `range_break.py`, `registry.py`, `session_levels.py`, `slope.py`, `structure.py` (17 files; `frame.py` 25,919 B is the largest — no parts);
   - `src/cobalt/radar/formation/`: `__init__.py`, `anchors.py`, `atoms.py`, `stops.py`, `triggers.py`;
   - `src/cobalt/radar/evaluate.py` (97,981 B at `60ddac4`) in THREE parts cut at a blank line before a top-level `def` / `class` — the drafter's cut: lines 1–778, 779–1516, 1517–end; re-find with `grep -n "^def \|^class "` and use what the file says;
   - `src/cobalt/taxonomy/predicate.py` (the interpreter), `src/cobalt/taxonomy/loader.py` (`cfg(...)` resolution), `src/cobalt/taxonomy/trade_def.py` (the shape model), `src/cobalt/archiver/aggregate.py` (bar aggregation);
   - an EXCERPT of `src/cobalt/radar/evaluate_cli.py`: `_aware`, `admitted_at`, `scan_instants` (the drafter read `:109-138`; re-find with `grep -n "^def "` and stop before `_select`) — the whole file is NOT staged (line 218 holds a `FORMED ` string).
   Every original gets its forbidden-string `grep -c` BEFORE its Write (the drafter counted 0 for every file above at `60ddac4`; a non-zero count now → that file is not staged, ESCALATE).
 (f) `TASK.md`, written by you: the task sentence below; the file list with real paths, lines and parts; the scan instants per slug (rubberband: the ONE scan `2026-01-06T16:30:00+00:00`, FTFT, on `rubberband_mapping(MIXED, htf_avoid=False)`; the other three: every scan of the `every_scan` grid — `DAY_START + m` minutes for `m` in `range(0, 391, 2)` — FTFT first then BGFI, the FIRST that forms, on `SHAPES["<slug>"]`); the fields owed per slug (rubberband five: formed bar, side, trigger, stop, last close at that scan; the others four: side, formed bar, trigger, stop); the trade date `2026-01-06`; and TWO neutral pointers, no value: "the daily bars file carries a row dated the trade date — which daily rows a detector reads is decided by the staged source (`daily.py`), not by this note" and "where a tunables row's `unit` label and the staged source disagree, the source is the rule".
`ls -la` of the folder quoted; every staged file re-checked with `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED "` → 0.

## STEP-2 — THE HOUSE (ONE call, `run_in_background: true`, its whole output read; clock 60 minutes)
THE TASK SENTENCE (identical for either house): *"You are a blind checker (FINAL [F-16] (1)). Everything you may use is in the folder /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code/. For each setup in TASK.md, from the bars, the daily bars, the settings, the definition excerpts, the tunables, the public rules and the staged engine SOURCE (files named SRC__*.txt — they are the rules as written, not results), derive what the rules produce at the scan instants TASK.md names on the two-minute scan grid: for rubberband, the evaluation at its one named scan; for the other three, the FIRST scan on which the setup forms, walking FTFT's scans then BGFI's. You MAY and SHOULD write your own program in this folder and run it on the staged bars: implement what the staged source and public rules specify, compute the seeded indicators and the detectors yourself, and print, per setup, the side (long|short), the formed bar's timestamp exactly as the bars file spells it, the trigger price, the stop price (and rubberband's last close) at the scan instants TASK.md names. Write your own implementation in Python 3 with the standard library only; do not import, exec or copy-run the SRC__*.txt files. Run your program only as python3 followed by its full path inside this folder. Cite the bars each value rests on. Where the staged rules do not determine a value, write UNDERIVABLE and the missing rule — never a guess. Do not open any file outside this folder; no network. Write your final answer to blind-values.md in this folder: one table per setup, field · value · the bars it rests on, then the path of every program you ran."*
 - **Grok (LINE-G):** `grok --sandbox cobalt-job --allow "Write(/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/**)" --allow "Bash(python3 /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code/*)" -p "<the task sentence>"` — `66` §2's spelling plus the ONE approved run rule; never `--always-approve`; it writes `blind-values.md` and its program(s) itself.
 - **Sol (LINE-S):** `codex exec --skip-git-repo-check -m gpt-5.6-sol -s workspace-write "<the task sentence>"` — from your cwd `/Users/cobalt/cobalt-wt/agy-trial` (its writable root; network off by the workspace-write default); it writes `blind-values.md` and its program(s) itself.
 A METER / HARNESS / denial line → `FAILED: STEP-2 — <house> — <line verbatim>` (no second house). At 60 minutes with no stop: TaskStop, record `TIMEOUT`, never relaunched; a `blind-values.md` already written is still compared.
 `## House`: the launch line as run, the whole output (or its last 60 lines plus `wc -c`, if longer), `wc -c` of `blind-values.md`, and `ls -la` of the folder before and after the call — the only new entries allowed are `blind-values.md` and the house's program / output files (name each); a changed STAGED file → ESCALATE `STAGED FILE CHANGED: <name>`.
 `## Program` (INDEPENDENCE, deterministic, no judgement): for each program file the house wrote, `grep -c -E "SRC__|exec\(|importlib|runpy|__import__" <file>` and `grep -c -E "test_setups|test_rubberband|DEF_WRITTEN|setups-blind-committed|setups-one-check" <file>`, and the same two greps on `blind-values.md` and on the house's output — a count above 0 → read the matching lines and quote them; an import / exec of staged source → the answer is KEPT and marked `RAN STAGED SOURCE` (a circular check), named under `## ESCALATE`; a KEEP-OUT name in a read or open → `OPENED KEEP-OUT`, kept and marked, ESCALATE. A program merely NAMING a SRC file in a comment is not a breach — quote it and say so.

## STEP-3 — ORDER, then COMPARE (`17` §3, unchanged)
3.1 `## ORDER` (each its own call, output verbatim): `ls -lT /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind-code/blind-values.md` (T1 = its mtime) → `date` (T2) → ONLY NOW the first read of the pins, one call per file: `grep -n "^DEF_WRITTEN_" /Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/test_rubberband_forms.py` · the same for `test_setups_nine_ema.py`, `test_setups_vwap_cont.py`, `test_setups_second_chance.py` (the line numbers must equal THE 17 PINS table — any other line → ESCALATE) → `date` (T3). T1 < T2 ≤ T3 is the proof; state also that no earlier tool call of this session named any of the four files or any KEEP-OUT report except inside this prompt's text.
3.2 `## Comparison`: `slug · field · pinned · blind · MATCH|MISMATCH|UNDERIVABLE`, 17 rows. `13`'s normalisation and nothing else: prices as decimals (trailing zeros ignored); timestamps as instants (`2026-01-06 16:22:00+00:00` = `2026-01-06T16:22:00+00:00`); a `datetime(...)` constructor in a pin read as the instant it names; the side literal against the house's `long|short` — any other spelling recorded verbatim, `MISMATCH` with the note "spelling". A house that gives TWO candidates for one field has not answered it: UNDERIVABLE, its candidates quoted (`17`'s rule). `fields` in the stop line = the rows of this table.
3.3 `## ESCALATE`: every MISMATCH as `FIX-or-OWNER: <slug> <field> pinned <x> blind <y> — the house's cited bars <…> — its program <path>` (you do not pick a side, L37); every UNDERIVABLE with the missing rule the house named (L70: not a defect); every `RAN STAGED SOURCE` / `OPENED KEEP-OUT` / `STAGED FILE CHANGED` mark; the CRLF gap; every file NOT staged and why; every `ASK DESK`. Then the stop line LAST.
NEXT STEP, not yours: the desk reads the comparison; `16` stages it as HOLD 1's closure for the existing pins.
