DERIVE ROW: R36

MODEL: Opus 5.5 (`claude-opus-5-5`) — the DERIVE SEAT that row names (`cto-2026-09-22.md` §4 R36, 13:45 ET: for the tribunal of `S3-EXITS-PROPOSAL-2026-09-21.md`, `Fable seat: yes` · `derive seat: claude-opus-5-5`; his R62, 14:48 ET, "A": round 2 this week, then this second derive); the SECOND derive of the S3 exits tribunal — the first (`75`) ran on the same row; you derive v3 from v2 and the round-2 rulings; you build, launch and commit nothing · SEAT: `s3-exits-tribunal-derive-r2-0922`, launched by the CTO desk in the background ONLY after BOTH round-2 stop lines are committed (hub `35`, the Fable seat `36`). YOUR POSITION, STATED: the round-2 Fable seat (`36`) also runs on `claude-opus-5-5`, in ANOTHER session; you are a fresh session that ruled nothing, held no side in either round, and recommend nothing (L37). Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/37-s3-exits-tribunal-derive-r2.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control s3-exits-tribunal-derive-r2-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the `--model` value is the one R36 names after `derive seat:`, already filled — `75`'s "THE DESK replaces `--model`" clause is spent; the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `74`, `75`, `21`, 09-20 R13 — nothing new) · SESSION: fresh (the round-2 seats' context is NOT carried; their reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic (Opus 5.5 — the week's meter is HIGH, his R58) — honest estimate: the round-2 hub report ≈ 10–18k tokens, the raw round-2 rulings of the houses that ruled ≈ 6–12k, the Fable seat's round-2 report ≈ 6–9k, v2 ≈ 16k (read whole — v3 is v2 amended), the derive report ≈ 7k (`## NEEDS ROUND 2`, `## Fold table`, `## OWNER ITEMS`, `## FOR DEJAN`, `## ESCALATE`), LAWS.md ≈ 25k, code re-opened only where a fold turns on it ≈ 5–10k; v3 ≈ 17–20k tokens of output (v2 whole + the folds); peak context ≈ 110–140k. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v3 of the S3 EXITS DESIGN from v2 and the round-2 rulings. LADDER: `S3-P1 · F11` + `S3-P2 · F22` (S3 opens 09-24). LAW STEP (L67): proposal → round 1 → derive v2 (`75`) → round 2 (hub `35`: Grok, Gemini; Astra METER, recorded under `cto-2026-09-22.md` R13; the Fable seat `36` on `claude-opus-5-5`, R36) → **THIS = the second derive; it CLOSES the tribunal** (or L39 → Dejan for what did not converge; a round 3 ONLY on a house's `DO NOT BUILD`) → the S3 build prompts (C1 first) → ≥3 checkers → deploy. L1, L3, L28, L32, L53 and L57 are the bar v3 must meet, and v2's `## L52 and the bar` is re-answered for v3. **R2-2 gates C1's DDL and C2; R2-3 gates C1's migration; R2-1 decides whether a round 3 is owed.** DO NOT STOP until the report's last line is `S3 EXITS DERIVED v3 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). Each its own Bash call: `grep -n "^| R36 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (the row carrying `S3-EXITS-PROPOSAL-2026-09-21.md` must carry `Fable seat: yes` AND `derive seat: claude-opus-5-5`; exactly ONE such row) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R36 | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — R36 is not committed`) · `grep -n "^| R62 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (must carry `S3 exits tribunal ROUND 2`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — row R36 names <id>; this session runs <id>`, stop · **BOTH ROUND-2 STOP LINES, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-r2-2026-09-22.md"` (the LAST NON-BLANK line must start `S3 EXITS TRIBUNAL R2 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 2 not done — last line: <line verbatim>`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-fable-r2-2026-09-22.md"` (must start `S3 EXITS TRIBUNAL FABLE R2 DONE ` — else `FAILED: refused — Fable round 2 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"S3 EXITS TRIBUNAL R2 DONE" -- "docs/40 - DevDocs/reports/s3-exits-tribunal-r2-2026-09-22.md"` and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"S3 EXITS TRIBUNAL FABLE R2 DONE" -- "docs/40 - DevDocs/reports/s3-exits-tribunal-fable-r2-2026-09-22.md"` (either EMPTY → `FAILED: refused — <report> stop line is not committed on main`) · **A HOUSE MUST HAVE RULED:** the hub's stop line shows `houses that ruled: 0 of 3`, or its `## Rulings table` shows no house (Grok or Gemini) answering ANY of R2-1, R2-2, R2-3 with `ADOPT` / `NEITHER` / `OWNER` → `FAILED: no house ruled round 2 — the desk brings it to him` (the Fable seat alone never settles an item — it holds one side of two of them) · **ASTRA (R13):** the hub's `astra:` field is `METER — proceed on three` or `SKIPPED — R13 (probe UP, recorded)` — RECORDED, not a refusal: derive from the seats that ruled, mark v3 `ASTRA PENDING (R13)` in its header and at each `[R2F-nn]`, and name in §0 that Astra reads the FINAL on Sat 09-26. **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L3 · L19 · L28 · L32** (v3 and your report are COMMITTED: no traded ticker or price of his — `[TICKER]` / `[PRICE]`, as v2 did) **· L35 · L36 · L37** (you derive; a SPLIT is carried to him, never settled by count) **· L39** (≤3 rounds; an item unresolved after round 2 goes to Dejan — NO round 3 is drafted unless a house said `DO NOT BUILD`; a split that does not converge → Dejan, never a vote) **· L48 · L52 · L53** (every threshold, rounding rule, preset and interval is his: an `OWNER` answer is his to rule, never yours) **· L57 · L62 · L63 · L67 · L68** (migration NUMBERS are the gate's) **· L70 · L71 · L72 · L74**. CLAUDE.md's absolute boundary: nothing in v3 reads, writes or infers from his trading platform.
2. THE ROUND-2 RULINGS: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-r2-2026-09-22.md` whole — `## Rulings table`, `## Wording offered, verbatim`, `## Fable round-1 claims, file-checked` (C1–C9), `## Checked against the files`, `## Experiments named (L70)`, `## OWNER answers`, `## Independence`, `## ESCALATE` — its HOLDS / DOES NOT HOLD tables are your filter · the raw round-2 rulings of every house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/s3-exits-tribunal/r2/` (`grok-ruling-r2.md`, `gemini-ruling-r2.md` — the raw file is what "verbatim" means, but you never copy a user-data span out of it) · the Fable seat's round-2 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-fable-r2-2026-09-22.md` (its SELF-ATTACK and WITHDRAWN lines bind you: a withdrawn sentence of that seat is never folded; its claims were NOT file-checked by a hub in round 2 — the hub checked the seat's ROUND-1 claims (C1–C9) only — so a round-2 Fable claim the hub did not check is UNCHECKED: you may rest on it only after opening the file it cites yourself, and the fold row says `UNCHECKED by a hub — read by the derive at <file:line>`, as `75` did).
3. v2 `/Users/cobalt/cobalt/docs/30 - Design/S3-EXITS-v2-2026-09-22.md` whole, and the derive report `reports/s3-exits-tribunal-derive-2026-09-22.md` (`## NEEDS ROUND 2`, `## Fold table`, `## OWNER ITEMS`, `## FOR DEJAN`, `## ESCALATE`).
4. Only where a fold turns on it: the real file a file-check cites — read to check, never to copy.

OWNER RULINGS THAT BIND: R38 (his stop becomes the plan until he resets it; Cobalt's structural stop stays visible; the gap is recorded on the trade) · v2's O1–O20 are his, read AFTER the tribunal; nothing of his is a precondition to build — a round-2 wording that makes one a precondition is NOT taken, and is named in `## ESCALATE`.

THE RULES OF THIS DERIVE — `75`'s and `21`'s rules, UNCHANGED, plus the closing rule:
- **Take a wording VERBATIM** — a house's `NEITHER` replacement, or the round-1 / v2 text an `ADOPT <letter>` names (v2 §3 Option G or Option F, grok's / gemini's / the Fable seat's R2-3 text as `NEEDS-ROUND-2` quotes it), word for word, or not at all; you choose, you never blend, edit or improve. A wording whose supporting claim DOES NOT HOLD in the round-2 hub's file-check (or is WITHDRAWN by the Fable seat in round 2) is NOT taken.
- **When is an item CONVERGED.** Count the SEATS that ruled it: the houses (Grok, Gemini) plus the Fable seat. CONVERGED = (i) every seat that ruled it gave the same answer (`ADOPT` the same letter, or `NEITHER` texts the hub's file-check shows are the same mechanism — otherwise not the same), **with at least one HOUSE among them**; or (ii) every HOUSE that ruled it gave the same answer and the Fable seat's contrary answer rests on a claim that DOES NOT HOLD in the hub's C1–C9 or file-check table, or that the Fable seat itself WITHDREW. The Fable seat's answer alone never settles an item (it is one of the sides of R2-2 and R2-3). **An `OWNER` answer is never converged into a design choice by you:** an item on which every seat answered `OWNER`, or on which any HOUSE answered `OWNER`, is OPEN FOR DEJAN (L53). For R2-2, the item answer decides; the (a)–(g) lines are the support and the material of an OPEN FOR DEJAN block, never a basis to converge an item the answers split. Everything else is STILL SPLIT.
- **R2-1 closes as follows.** Every seat that ruled it wrote `DO NOT BUILD withdrawn`, or every HOUSE did and no `DO NOT BUILD stands` line rests on a defect that HOLDS → R2-1 CONVERGED (the tribunal's dissent is spent; gemini's round-1 text stays in `## Dissents, verbatim`). A house's `DO NOT BUILD stands: <defect>` whose defect HOLDS in the hub's file-check, or a house's closing `TRIBUNAL R2: DO NOT BUILD …` → `## NEEDS ROUND 3` with its text verbatim (L39: round 3 is the last); a `stands` line whose defect DOES NOT HOLD is carried in `## Dissents, verbatim` and does not reopen the item.
- **NOT CONVERGED → an `OPEN FOR DEJAN` block, never settled by you (L37, L39).** ONE block per item: the positions VERBATIM (`G — <text>, grok` / `F — <text>, Fable` for R2-2; `A` / `B` / `C` for R2-3), each seat's reason in ONE sentence with its `file:line`, the desk's A/B to bring him in ONE line each with the COST of each in HIS terms (a correction to his entry that leaves the exit rows stating a stale running count · a stop edit that shows open risk on shares he no longer holds · a second ½ tap refused vs recorded as 25 shares · a migration that re-runs on every page load vs one that runs once with a rollback · an extra deploy evening), plus which chunk waits on his answer and which position that chunk builds if the houses named a default. Never a vote, never your own preference.
- **Prefer the smaller mechanism** only BETWEEN texts the seats agree are correct; a disagreement about correctness is a split.
- **Every "not checkable from reads" claim → a first-gate experiment** in v3's list, placed before the chunk it gates (L70); round-2 experiments that repeat v2's X1 / X4 / X6 / X7 / X9 / X19 are merged into that row, attributed, never duplicated. X4 and X7 run on `cobalt_dev` before C1 / C2 whichever option v3 keeps (v2 §3).
- **Invent nothing. Nothing of his before build. Dissents carried VERBATIM** (`## Dissents, verbatim` gains every round-2 answer v3 does not follow, with seat and item). **No user data** (L32): spans replaced by `[TICKER]` / `[PRICE]` / `[user data]`, counted. The migration NUMBER stays the desk's at the L68 gate — v3 states the SET the rulings decide, never a number.

WRITE (Write tool, TWO new files):
- `/Users/cobalt/cobalt/docs/30 - Design/S3-EXITS-v3-2026-09-22.md` — v2, whole, with each round-2 point REPLACED in place by the converged wording, verbatim, tagged `[R2F-nn]` (a row of your fold table), or by an `OPEN FOR DEJAN — R2-n` block as above: the `[R2-2]` cells of §2's transition table and §3's `legs` table (`seq`, `running_before, running_after`, `direction`), §3's "Corrections and running shares" Options G / F, the `(The source of direction is [R2-2])` sentence, §3's migrations sentence and §5's `kind` bullet (`[R2-3]`), §5's `[F-29]` form of the FILLED share read, §10's C1 `M1 (set: [R2-3])` and C2 cells, §11's `[F-40]` note, the X4 / X7 rows, §12's Q3 / Q4 dispositions, `## Dissents, verbatim` (gemini's R2-1), `## L52 and the bar`'s L57 sentence. The header names v2, the round-2 hub report, the raw rulings of every house that ruled round 2, the Fable seat's round-2 report, the fold table's path, and `ASTRA PENDING (R13)`; a `## Status after round 2` section (per item: converged / OPEN FOR DEJAN / NEEDS ROUND 3, and the chunk each gates); `## First-gate experiments (L70)` = v2's list plus every round-2 experiment the fold takes; `## Dissents, verbatim` extended; `## L52 and the bar` re-answered for v3 — L57 is MET only if R2-2 converged; (c) seams as real artifacts only if R2-2 and R2-3 both converged. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/s3-exits-tribunal-derive-r2-2026-09-22.md` — §0 Headline ≤5 lines (converged / open for Dejan / round 3, whose text, C1 and C2 unblocked or not, Astra pending) → `## DIGEST FOR THE DESK` (≤30 lines: per item, converged or OPEN FOR DEJAN and whose text; R2-1's outcome and whether a round 3 is owed; C1 unblocked or not and why; C2 unblocked or not and why; seats that ruled round 2; owner items; the chunks and hours under v3 against S3's 09-24 → 10-07, a GUESS marked as one) → `## Fold table` (`R2F-nn · item (R2-1, R2-2, R2-3; R2-2 sub-points where a fold turns on one) · seats that ruled it (<n>, named) · answers (per seat) · whose wording · adopted verbatim? yes / no / not taken · why (≤25 words, citing the round-2 hub's file-check row or C<n>, or `UNCHECKED by a hub — read by the derive at <file:line>`)`) → `## OPEN FOR DEJAN` (the blocks, one per item; empty = none) → `## NEEDS ROUND 3` (only a `DO NOT BUILD` as the R2-1 rule says; else `none`) → `## OWNER ITEMS (after the tribunal)` (v2's twenty, updated, deduplicated, each with who raised it; none a precondition) → `## FOR DEJAN` (v2's `## FOR DEJAN` carried, plus one A/B per OPEN FOR DEJAN item in HIS terms; no recommendation) → `## Redactions` → `## READING` → `## ESCALATE` → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `S3 EXITS DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch reads its own report first and continues from `## CONTINUE`; v3, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the report, L71): `S3 EXITS DERIVED v3 · converged: <n> of 3 · open for Dejan: <n> · owner items: <n> · ESCALATE: <n>` — where `converged` counts ITEMS marked converged and `open for Dejan` counts `OPEN FOR DEJAN` blocks (an item sent to `## NEEDS ROUND 3` is counted in neither, and named in §0 and `## ESCALATE`) — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `converged: 3 of 3` → the tribunal CLOSES, the desk commits v3 as the FINAL's input, brings him `## FOR DEJAN`, and an Opus seat drafts the C1 build prompt from it (v3's first-gate experiments for C1 run first, ordered against the setups one-build and bars `0012` at the L68 gate); `open for Dejan: n` → the desk brings him ONE A/B per item, one per message, and the chunk each gates waits on his answer; a `## NEEDS ROUND 3` entry → a round-3 hub for that item only; in every case Astra reads the FINAL on Sat 09-26 (R13).
