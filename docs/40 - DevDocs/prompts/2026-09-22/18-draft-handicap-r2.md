MODEL: Opus 5 (`claude-opus-5`) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `handicap-r2-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/18-draft-handicap-r2.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control handicap-r2-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as `43` of 09-21 and today's `08` / `11` / `14`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the FOUR files named below with the Write tool, the REPORT LAST (09-22 lesson 3) · METER: Anthropic small–medium · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default. **THE HOUSES' STRINGS (`grok` / `agy`) END TODAY 2026-09-22 23:59 ET (his R39 of 09-21); round 2 must be drafted now and SMALL — two items.**

# DRAFT (do NOT launch) ROUND 2 of the float-handicap tribunal — ONLY the two items of `## NEEDS ROUND 2`. LADDER: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`. LAW STEP (L67 / L52): proposal DONE → round 1 DONE (Grok, Gemini, Fable ruled; Astra METER — `cto-2026-09-22.md` R13: proceed on three, Astra reads v2 on Sat 09-26) → derive v2 DONE (`FLOAT HANDICAP DERIVED v2 · folds: 26 · verbatim: 18 · needs round 2: 2 · owner items: 13 · ESCALATE: 6`; `docs/30 - Design/FLOAT-HANDICAP-v2-2026-09-21.md`) → **THIS = drafting round 2** → a second derive closes the tribunal (or L39 → Dejan for what does not converge) → the H1 build prompt. **R2-1 gates H1, R2-2 gates H2** (derive ESCALATE 2 / 3). DO NOT STOP until the report's last line is `HANDICAP R2 PROMPTS DRAFTED …` or `FAILED …`.

## INDEX CARD — read these, in this order, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L19, L32 (his threshold VALUES never in a packet — the round-1 packet redacted by KEY; keep that rule), L35, L37, L39 (a law-file / design split that does not converge → Dejan, never a vote), L48, L52 (the bar v2 must meet), L53 (ceilings and tiers are HIS — both houses name the R2-1 crossing as his), L62, L63, L67 (four houses; ≤3 rounds; a non-ruling stop spends no round — 09-21 close P-c proposed), L70, L71, L72. One line each in the prompts you write.
2. THE ROUND-2 PRECEDENT, the shape you copy: `prompts/2026-09-21/43-draft-setups-tribunal-r2.md` (this file's own shape) and the three it produced — `44-setups-tribunal-r2.md` (three-house hub, round 2 reads the FOLD only), `45-setups-tribunal-fable-seat-r2.md` (the blind Fable seat, round 2), `46-setups-tribunal-derive-r2.md` (the second derive that closes the tribunal); their reports `reports/setups-tribunal-r2-draft-2026-09-21.md`, `setups-tribunal-r2-2026-09-21.md`.
3. THIS TRIBUNAL'S ROUND 1: `prompts/2026-09-21/38-float-handicap-tribunal.md` (the hub's launch line, packet layout, `r1/` staging, the astra probe; its date gate re-issued to R39's literal), `39-float-handicap-tribunal-fable-seat.md` (the Fable seat, R30 "Fable seat: yes"), `40-float-handicap-tribunal-derive.md` (the derive; `DERIVE ROW: R30`); the reports `reports/float-handicap-tribunal-2026-09-21.md` (hub round 1: `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files`, `## Experiments named (L70)`), `float-handicap-tribunal-fable-r1-2026-09-21.md`, `float-handicap-tribunal-derive-2026-09-21.md` (`## DIGEST FOR THE DESK`, **`## NEEDS ROUND 2`** — R2-1 and R2-2 with the questions round 2 must answer and their gates, `## OWNER ITEMS`, `## ESCALATE`).
4. THE DESIGN: `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` (the proposal) and `docs/30 - Design/FLOAT-HANDICAP-v2-2026-09-21.md` (v2 — the round-2 packet's base: the `[F-nn]` folds and the two `ROUND 2` markers). The code the two items turn on: `src/cobalt/radar/pool.py` (`_ranked`, the `min` at `:165`, the key at `:209`, `:326` — read what the derive cites), `src/cobalt/radar/store.py` / the `radar_membership` DDL (`excluded_by` CHECK values), `src/cobalt/replay/movers.py:430-450` (`EPISODE_EXCLUSIONS`, the raise the Fable seat cites), `src/cobalt/radar/runner.py:180-190` (`decide()` before S1).
5. `cto-2026-09-22.md` §4 R13 (no Astra this week — the astra string is carried, its probe recorded, SKIPPED; three houses), R14 / R18 (the round-1 and derive launch rows); `cto-2026-09-21.md` R28 / R29 (his handicap rulings — the packet cites them by KEY, never their values), R30 (Fable seat yes), R39 (`grok` / `agy` through 2026-09-22 23:59 ET).

## THE JOB — four files, Write tool, the report LAST
(a) `docs/40 - DevDocs/prompts/2026-09-22/19-handicap-tribunal-r2.md` — the three-house ROUND-2 hub on `44`'s shape and `38`'s launch line BYTE FOR BYTE (only the prompt path and the remote-control name `float-handicap-tribunal-r2-0922` change; the astra string CARRIED: probe → `METER` → recorded, skipped, proceed on Grok + Gemini, R13; staging in `scratch/tribunal-bars-0920/float-handicap-tribunal/r2/`, first Write creates it, no `mkdir`). ROUND 2 READS THE FOLD ONLY: the packet = v2's sections that carry R2-1 and R2-2 (the `ROUND 2` markers and the folds they touch), the derive report's `## NEEDS ROUND 2` verbatim, each house's OWN round-1 ruling on O4 / (c) / (k) and O6 / (g) (its own file only, for reference), the Fable seat's round-1 wording on those items verbatim (the hub file-checks it too — 09-21 lesson 3), and the code excerpts the two items turn on (headed by path + line range, byte for byte). QUESTIONS-R2, exactly the derive's two questions: R2-1 — does R28 (b) ("competes on its LOWERED score for the pool's 50") require a handicapped name to compete across `priority` / `first_from` (B, pool-wide) or within its tier (A, tier-bound), and under A does H2 change the position basis? R2-2 — (i) storage: additive `ExcludedBy` enum + two CHECK changes + a migration, or a JSONB `handicap.decisive` flag with none, given `movers.py`'s raise on reverted code; (ii) predicate: a full second `decide()` with stickiness and held seats, or the O(1) `winners[-1]` comparison? Each house answers per item `ADOPT A | ADOPT B | ADOPT WITH <wording> | OWNER (his — why)` with file:line evidence; a house that says OWNER names what only he can decide (L53). Report `reports/float-handicap-tribunal-r2-2026-09-22.md`; stop line on `38`'s shape: `FLOAT HANDICAP TRIBUNAL R2 DONE · grok: … · gemini: … · astra: … · houses that ruled: <n> of 3 · items converged: <n> of 2 · owner: <n> · ESCALATE: <n>`. Its own STAGGER gate on the house lane: `tail -n 1` of `reports/setups-blind-committed-day-2026-09-22.md` must start `SETUPS BLIND COMMITTED DAY DONE` or `FAILED` (one Grok/Gemini hub at a time), else `FAILED PREFLIGHT: 17 is running — the desk staggers`. DATE GATE: R39's literal through 2026-09-22 23:59 ET; a later launch → `FAILED: authorization expired …` unless a committed row of HIS extends it.
(b) `docs/40 - DevDocs/prompts/2026-09-22/20-handicap-tribunal-fable-seat-r2.md` — the blind Fable seat, round 2, on `45`'s shape and `39`'s launch line BYTE FOR BYTE (only the path and the name `float-handicap-tribunal-fable-r2-0922`; `--model claude-fable-5-1`; the seven read-only strings). `DERIVE ROW: R30` covers this seat (the setups precedent: R20 covered `45`). It rules on R2-1 and R2-2 ONLY, from the same fold (its OWN round-1 report for reference, never the houses' folder — blind), same answer shape, `OWNER` allowed with the why. Report `reports/float-handicap-tribunal-fable-r2-2026-09-22.md`; stop line `FLOAT HANDICAP TRIBUNAL FABLE R2 DONE · R2-1: <answer> · R2-2: <answer> · owner: <n> · ESCALATE: <n>`. It runs BESIDE (a), no house call, no stagger.
(c) `docs/40 - DevDocs/prompts/2026-09-22/21-handicap-tribunal-derive-r2.md` — the second derive on `46`'s shape and `40`'s launch line BYTE FOR BYTE (path, name `float-handicap-tribunal-derive-r2-0922`; `--model claude-fable-5-1`; `DERIVE ROW: R30`). PRECONDITIONS: (a)'s and (b)'s stop lines present and committed. It writes v3 = `docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md` (v2 with the two items settled where the houses converged — the taken wording verbatim, tagged — and, for an item that did NOT converge, an `OPEN FOR DEJAN` block: the two positions verbatim, each house's reason, the desk's A/B to bring him, L39 — never a vote, never the seat's own preference), plus its report; stop line `FLOAT HANDICAP DERIVED v3 · converged: <n> of 2 · open for Dejan: <n> · owner items: <n> · ESCALATE: <n>`. `converged: 2 of 2` = the tribunal CLOSES and v3 is the H1 prompt's input; `open for Dejan: n` → the desk brings ONE A/B per item; either way Astra reads the FINAL on Sat 09-26 (R13, `ASTRA PENDING (R13)` marks).
(d) LAST, YOUR REPORT `docs/40 - DevDocs/reports/handicap-r2-draft-2026-09-22.md`: `## DIGEST` (≤12 lines: the two items in one sentence each, what each prompt needs, new rule strings = 0 expected, the launch order `20` beside `19` → both stop → `21`), `## RULE PROOF` (each launch line's strings `grep -c -F` against its precedent line — `38` for `19`, `39` for `20`, `40` for `21` — every count ≥1; any string not in its precedent = NEW, listed), `## EXPERIMENTS` (the derive's `## Experiments named (L70)` X2 / X13 / X15 / X16: which of them a house could run from reads alone — none, expected — and which the H1 build prompt must run first; you launch none), `## ESCALATE`, and your LAST LINE: `HANDICAP R2 PROMPTS DRAFTED · prompts: 3 · items: 2 · new rule strings: <n> · experiments for H1: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. L32: no value of his (the two thresholds, the factor, the combinator's value) appears in any file you write — keys only, as round 1 did. Never invent a flag: every command shape appears in `38` / `39` / `40` / `44` / `45` / `46`. Every OWNER item stays an owner item, never resolved by you. `date` for every time you write. Stop within 25 minutes; your stop line is your report's last non-blank line, written LAST.
