MODEL: Opus 5 (`claude-opus-5`) — a read-and-write drafting job of a DB-reading job prompt and a blind-seat prompt; no code, no DB, no launch · SEAT: drafter `setups-fixture-cut-draft-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK from `~/cobalt`: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/11-draft-setups-fixture-cut.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control setups-fixture-cut-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as 09-21's `64` / `79` and today's `04` / `08`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# Draft the `AWAITING_A_DAY` closer — the FIXTURE-CUT job + the BLIND expected-values seat (2026-09-22)

WHY: `SETUPS ONE BUILD BUILT dd4a9b9` (`/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-one-build-2026-09-21.md`) left two pins `AWAITING_A_DAY` — rubberband and hitchhiker — because no COMMITTED real-shape day forms them (the committed days are one trade date, tickers FTFT and BGFI, `tests/fixtures/radar/_cut_p2_fixtures.py:36-38`). The FINAL (`docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` §9 gates, [F-16] (1), [F-21]) closes each pin BEFORE the deploy by (a) a DB-backed FIXTURE-CUT job: a stored pool day chosen BECAUSE THE DEFINITION FORMS ON IT — never because he traded or tagged it (his R24, 09-21 11:13 ET: "Do not assume days and look alikes. This is not known at the time") — cut into a committed real-shape fixture (L45: real shape, attribution / dates / preset ids stripped as the `_cut_p2_fixtures.py` precedent does), and (b) a BLIND expected-values seat: a house that has NOT seen the build's `DEF_WRITTEN_*` pins re-derives the expected values from the fixture's bars and the definition alone, and the two are compared. LAW STEP: build DONE → check (`66`, running) → THIS (the FINAL's own acceptance) → deploy prompt → his approve (L7 / L61) → deploy.

## INDEX CARD — read these, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L1, L4, L28, L32 (user data: his trades never leave the repo; a fixture is anatomy-only), L35, L41 (production credentials never to a worker: production READS run through Cobalt commands from `~/cobalt`; a hub never copies production `.env` anywhere but the R41-approved path), L45, L48, L57, L62, L63, L67, L70, L71. One line each in the prompts you write.
2. THE FINAL: `docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` §9 (gates 1–5), [F-16], [F-21], and each setup's section for rubberband and hitchhiker (what "forms" means for each: trigger, stop, anchor).
3. THE BUILD REPORT, the sections `## AWAITING_A_DAY`, `## ESCALATE` (i)–(iii), and every `DEF_WRITTEN_*` constant it names (which file, which value) — the blind seat must NOT be shown these values; you list them in your report so the DESK can keep them out of the seat's packet. The build prompt `prompts/2026-09-21/65-setups-one-build.md` §"THE PER-SETUP ACCEPTANCE" and its `## AWAITING_A_DAY` instructions (line ≈107, ≈120).
4. THE ENGINE'S REPLAY PATH: `src/cobalt/radar/evaluate_cli.py` (`cobalt radar evaluate --replay <day> --trade-def <slug> --expect-formed`, exit non-zero on zero formations — FINAL gate 4), `src/cobalt/replay/` (which tables a replay reads: `radar_membership`, `bars`, sessions — production `system` side, READ only), the fixture precedent `tests/fixtures/radar/_cut_p2_fixtures.py` (how a day was cut, what was stripped, how the committed fixture is loaded by tests) and the tests that consume it. `configs/cobalt/smoke/s2.yaml` for how a production read-only command is spelled with `--prod`.
5. WHAT IS APPROVED for production READS: `grep -rn "cobalt db query --side system --prod\|cobalt radar evaluate --replay" "docs/40 - DevDocs/prompts/2026-09-2*/"` — copy an approved string shape where one exists; a production replay that reads prod rows from a WORKTREE needs the R41-shaped `.env` copy (approved ONLY for `/Users/cobalt/cobalt-wt/setups-c1/.env`) — so the cut job runs IN the setups worktree with that pair, or from `~/cobalt` on main's code if the replay is on main (it is not — the seven-setups evaluator is on the branch). Say which and why; every NEW string is named as NEW.
6. Seats: 09-21 R46 / R49 (code checks: Grok · Gemini · Sol · Opus 5 — Sol's Codex meter is OUT until Sat 09-26, `cto-2026-09-22.md` R13); the blind seat is a CHECK of computed values → Opus 5 headless (`claude -p --model claude-opus-5 *`, the R49 string) or Gemini (`agy`, R39 through today 23:59) — pick one primary and one fallback and say why; a FABLE seat is not asked for.

## THE JOB — three files, Write tool
(a) `docs/40 - DevDocs/prompts/2026-09-22/12-setups-fixture-cut.md` — THE CUT JOB, ONE complete block (MODEL: Sonnet 5 unless a step writes a user table or production — none expected: production is READ, the fixture is a repo file on the branch; state the seat and why; `--permission-mode auto`; worktree `/Users/cobalt/cobalt-wt/setups-c1`, branch `setups/seven-0921` from `60ddac4`; launch line FIRST with the full `--allowedTools` (from `65`'s list + whatever production read shape item 5 finds, each marked precedented / NEW), `--disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)"`; index card SECOND; AUTHORIZATION greps the `R__` launch row). STEPS: PREFLIGHT (worktree clean on the branch, `.env` absent, `date`) → FIND THE DAY per pin: enumerate stored pool days (a production READ of the distinct `radar_membership` days — read-only query, quoted), run the engine's replay per candidate day with `--trade-def <slug> --expect-formed`, take the FIRST day (most recent first) on which the definition FORMS with ≥1 formation, record the exit code and the formation output verbatim; NO day is chosen from his trades, tags, notes or the DRC (R24) — the prompt says so and the report proves the selection was by exit code → CUT: the day's bars + membership for the forming ticker(s) into a committed real-shape fixture on the `_cut_p2_fixtures.py` shape (anonymised as that precedent anonymises; the ticker stays — it is anatomy, not his data, unless the precedent strips it: follow the precedent) → PIN: the test that was `AWAITING_A_DAY` now loads the fixture and asserts formation; its expected values (trigger, stop, anchor, formed bar) are taken FROM THE ENGINE'S OUTPUT on the fixture and written as `DEF_WRITTEN_*`-style constants → suites (offline, and with-DB on `cobalt_dev` with the R41 `.env` pair, `.env` removed and proven gone) → `git add` by explicit path, commit `fix(setups): AWAITING_A_DAY closed — <slugs> on stored day <YYYY-MM-DD>` → report `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md`, LAST LINE `SETUPS FIXTURE CUT BUILT <tip> | on 60ddac4 | days: <slug>=<day> … | formed by exit code: yes | offline <p>/0 | with-DB <p>/0 | .env: removed, proven gone | ESCALATE: <n>` or `FAILED: …` (a slug with NO forming day in the store → `FAILED: no stored day forms <slug> — <n> days tried` and the pin STAYS; the desk brings that to him). RECOVERY RULE verbatim from `65`; file content via Write / Edit only; a denied Bash = FAILED.
(b) `docs/40 - DevDocs/prompts/2026-09-22/13-setups-blind-values.md` — THE BLIND SEAT: a Sonnet hub stages ONLY the fixture file(s), the two definitions' vault notes as the build's neutral shape notes render them (`tests/cobalt/setups_shapes.py` and the FINAL's per-setup section), and the engine's PUBLIC rules (the FINAL §3 text for each setup) — NEVER the test file with the pins, never the build report, never `DEF_WRITTEN_*` values — into `scratch/tribunal-bars-0920/setups-blind/` (first Write creates it), then launches ONE house (item 6) to derive, from the bars and the definition alone, the formed bar, trigger, stop and anchor per slug, writing `blind-values.md`; the hub then compares house values vs the pinned constants (it reads the pins ONLY after the house has answered — say so, and prove the order by the timestamps of the two reads), table `slug · field · pinned · blind · MATCH|MISMATCH|UNDERIVABLE`. Report `docs/40 - DevDocs/reports/setups-blind-values-2026-09-22.md`, LAST LINE `SETUPS BLIND VALUES DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: …`. A MISMATCH is not a verdict (L37): it goes to the desk as a FIX-or-OWNER item.
(c) your report `docs/40 - DevDocs/reports/setups-fixture-cut-draft-2026-09-22.md`: §0 DIGEST ≤12 lines (files written; where the replay reads production and the exact read strings, precedented or NEW; the `.env` path used; the blind house chosen and its fallback; the list of `DEF_WRITTEN_*` values the seat must not see (file:line); ESCALATE count) → `## RULE PROOF` (both prompts' full allowlists, each string precedented / NEW) → `## ESCALATE` → LAST LINE `SETUPS FIXTURE CUT PROMPTS DRAFTED · prompts: 2 · production reads: <n> strings (<new> NEW) · blind house: <name> · ESCALATE: <n>` or `FAILED: <why>`. Write the report LAST, after both prompt files exist.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. L41: no prompt you write ever copies production `.env` anywhere but `/Users/cobalt/cobalt-wt/setups-c1/.env` (R41), and production is READ only. R24: no day selection rests on his trades. Never invent a flag: every `cobalt` / `git` shape appears in a precedent prompt or in the code's CLI (item 4). `date` for every time you write. Stop within 35 minutes; your stop line is your report's last non-blank line.
