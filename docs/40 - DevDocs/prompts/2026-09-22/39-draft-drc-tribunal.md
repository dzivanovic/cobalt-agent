MODEL: Opus 5.5 (`claude-opus-5-5`, R36) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `drc-tribunal-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/39-draft-drc-tribunal.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-tribunal-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `30` / `34` / `38`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the FOUR files named below with the Write tool, the REPORT LAST · METER: Anthropic medium (the week's meter is HIGH, his R58) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) the ROUND-1 prompts of the TRIBUNAL (L67) on `docs/30 - Design/DRC-AUTOMATION-PROPOSAL-2026-09-22.md` (with its `## 13. DIFF MODEL` AS RULED). LADDER: `S3-P3 · F14`. LAW STEP: proposal DONE (Opus 5.5, `reports/drc-design-2026-09-22.md`: `DRC AUTOMATION PROPOSED · chunks this week: 5 · later slices: 8 · write-path chunks: 5 · migrations: 1 · import place: /drc page on the ASET app (aset/web.py) · new dependency: none · reaches scoring: no · build estimate: 32 h · open to the tribunal: 11 · owner items: 19 · diff rows: 74 · additions: 13 · ESCALATE: 6`) → his DRC sitting ruled the inputs and the diff (`cto-2026-09-22.md` §4 R65–R73) → **THIS = drafting its tribunal** → the tribunal rules and derives the FINAL → build per chunk → ≥3 checkers → deploy. DO NOT STOP until the report's last line is `DRC TRIBUNAL PROMPTS DRAFTED …` or `FAILED …`.

THIS IS 09-21's S3-EXITS TRIBUNAL AGAIN, ON ANOTHER DESIGN. Your template is `prompts/2026-09-21/72-draft-s3-exits-tribunal.md` and what it produced — `73-s3-exits-tribunal.md` · `74-s3-exits-tribunal-fable-seat.md` · `75-s3-exits-tribunal-derive.md` (and today's round-2 copies `35` / `36` / `37` for the newest gate wording). Copy those shapes byte for byte wherever the same sentence serves; every rule of `72` binds here unchanged, with these substitutions:
- (1') WHAT HE RULED, and the tribunal does NOT re-open (they are the design's inputs): R65 (the coach spec is the content / process start; trade-reporter = automation pattern only; NO PDF; output = `1 - Trading/5 - Review/DRC-<date>.md` from his template), R66 (one import place; his two hand-dropped inputs — TradeZella screenshots + DAS trading-log export; the DRC is created ONLY when both are placed), R67 (S3-exits legs: his held-count statement wins mid-trade; DAS reconcile is the truth; open positions carry to the next day), R68–R72 (the diff model: every row as ruled — KEEP / MERGE / DROP and the AUTO / PRE / HIS writer are HIS decisions; houses rule HOW each is built and whether its SOURCE / mechanism holds, never whether he gets it). R58 / R60: in lane this week.
- (2') THE FABLE SEAT IS APPROVED — R73 (16:05 ET): `Fable seat: yes`, on **`claude-fable-5-1`** (a per-case override of R36 for THIS tribunal), `derive seat: claude-fable-5-1`. First lines `FABLE ROW: R73` / `DERIVE ROW: R73`; the row literals the gates grep: `DRC-AUTOMATION-PROPOSAL-2026-09-22.md` · `Fable seat: yes` · `derive seat: claude-fable-5-1`.
- (3') ASTRA: METER until Sat 09-26 06:47 ET — R13 (`cto-2026-09-22.md`): proceed on three (Grok · Gemini · Fable), the astra string CARRIED, its probe recorded, SKIPPED; Astra reads the FINAL Sat.
- (5') `Bash(grok *)` / `Bash(agy *)` through **2026-09-23 23:59 ET** (R30 — copy `35`'s DATE + EXTENSION gate). Later → FAILED, his extension row needed.
- (7') STAGGER (one Grok/Gemini hub at a time): the house hub refuses while the report of `16` (setups check r2), `28` (H1 check) or `32` (stale-score check) is in progress without a stop line (`35`'s rule, incl. "a report that does not exist = not running only if the desk's launch row says `<nn> is not running`"), and refuses 19:25–20:45 ET (the deploy window).
- (8') PACKET ≤140 KB, MANDATORY + OPEN-AS-NEEDED in reading order (`73`'s layout): the proposal whole (incl. §13); the proposer's report DIGEST, UNPROVEN rows, ESCALATE; his rulings R65–R73 VERBATIM from `cto-2026-09-22.md` §4; the coach spec by SECTION HEADINGS and KEYS ONLY (it is gitignored user data — `docs/_inflight/DRC-automation-spec-2026-09-22.md`; its values and rule sentences NEVER enter the packet, L32); his current DRC note's HEADINGS and PROMPT LINES only (`1 - Trading/5 - Review/DRC-2026-09-21.md` — no trade content, no ticker, no price, no P&L); trade-reporter's `app.py` + `templates/index.html` (the pattern); the code the proposal cites as pre-cut excerpts with `file:line` headers (`prefill/drc.py`, `daymode/drc.py`, `replay/line.py`, `vaultwrite/writer.py`, `aset/web.py` routes, `aset/store.py`, `db_migrations` numbering); S3-exits v3 §2 (legs) and the R67 row. State the total size and an honest token estimate per house.
QUESTION SET for each house — per item `ADOPT · ADOPT WITH <replacement wording, verbatim-ready> · REJECT <why, file:line>`: the proposal's 11 "open to the tribunal" items in ITS order, plus: (a) does the FACT BASE hold — any writer of the DRC note or of its units it missed (`file:line`); (b) L3: the 15:40 job, the morning reader, the miss line — reused or killed, and is any second DRC writer left; (c) the import place on the ASET app — L3 / L40 (one expert per side effect), upload handling, a day bound to its files, fail-loud on a bad file (L1), what he sees back; (d) the trigger — "both inputs placed" as a deterministic watcher (L2), one-input and re-import behaviour, idempotency of the note write (L28 create-if-absent + units); (e) the DAS export parser — FIFO pairing, partials, open positions across days (R67), a changed format = loud degraded (L9), the real-shape fixture's source and redaction (L45); (f) the reconcile against S3-exits legs — correction rows through v3's ONE writer, DAS wins, the diff shown first (R67); (g) the TradeZella screenshot — what is read, by what, and the proposal's no-new-dependency claim; (h) the rule engine in SHADOW (L7) — rules read as data from `Rules.md`, per-trade / per-day results, nothing flips his checkboxes; (i) every ledger / addition with its n (L8) and its stored inputs (L57); (j) the chunks for THIS week: write paths, migration number against `0012`–`0015` (L68), RESTARTS (L42), hours.

WRITE (Write tool, new files only, under `/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/`):
- `40-drc-tribunal.md` — Sonnet 5 hub `drc-tribunal-0922`, cwd `~/cobalt-wt/agy-trial`, `73`'s shape, its 14 + 3 strings byte for byte, folder `scratch/tribunal-bars-0920/drc-tribunal/r1/` (Write-created), DATE GATE (5'), STAGGER (7'), Codex PREFLIGHT (3'), `date` at every completion notice, the 20-minute task-stop, the hub file-checks the Fable seat's claims too. Report `reports/drc-tribunal-2026-09-22.md`; stop line `DRC TRIBUNAL R1 DONE · grok: … · gemini: … · astra: … · houses that ruled: <n> of 3 · claims that HOLD: <n> · blockers to build: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- `41-drc-tribunal-fable-seat.md` — MODEL Fable 5.1 (`claude-fable-5-1`, R73 — say so in the MODEL line), seat `drc-tribunal-fable-0922`, `74`'s shape and strings (with `--model claude-fable-5-1`); AUTHORIZATION per (2'); BLIND to the houses' folder and to `40`'s report; ONE file; stop line `DRC TRIBUNAL FABLE R1 DONE · verdict: … · adopt: <n> · adopt with wording: <n> · reject: <n> · experiments named: <n> · ESCALATE: <n>` — or `FAILED …`. METER line: an honest token estimate.
- `42-drc-tribunal-derive.md` — `75`'s shape with `--model claude-fable-5-1` (R73) and `DERIVE ROW: R73`; refuses unless both round-1 stop lines are present and committed; a wording whose supporting claim DOES NOT HOLD or is WITHDRAWN is never folded; his R65–R73 are never re-opened; writes `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` and its report `reports/drc-tribunal-derive-2026-09-22.md` (fold table, `## NEEDS ROUND 2`, `## OWNER ITEMS`, `## FOR DEJAN` in his terms, one A/B per item, no recommendation from a seat that holds a side — L37). Stop line `DRC DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — or `FAILED …`.
- LAST, your report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-tribunal-draft-2026-09-22.md`: §0 Headline ≤5 lines → `## DIGEST FOR THE DESK` ≤30 lines (what each prompt does, the launch order) → PACKET (source → staged path, sizes, token estimate per house) → RULE PROOF (`grep -c -F -e` per string: `40` vs `73`, `41` vs `74`, `42` vs `75`; the expected differences: path, remote-control name, `--model claude-fable-5-1` on `41` / `42`) → **`NEW strings:`** (expected: none) → `READING:` → `## ESCALATE` → `## CONTINUE` → last line. While you work the last line is `(run in progress — next step under ## CONTINUE)`.
STOP LINE (last non-blank line): `DRC TRIBUNAL PROMPTS DRAFTED · prompts: 3 · astra: METER — proceed on three · questions: <n> · packet: <n> KB · new rule strings: <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>`. One command per Bash call, bare shape, no pipe, no redirect. Run `date` before any time you write. L32: no value, rule sentence, ticker, price or P&L of his in any file you write. Stop within 35 minutes.
