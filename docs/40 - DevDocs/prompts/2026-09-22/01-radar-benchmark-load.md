MODEL: Opus 5 (`claude-opus-5`) — L29 write-path floor: a production `"user".trader_settings` write (ONE new key, `radar.benchmark`) · SEAT: apply hub `radar-benchmark-load-0922`, launched by the CTO desk in the background, two bare commands from the desk's shell: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/01-radar-benchmark-load.md' and follow it exactly." --model claude-opus-5 --permission-mode acceptEdits --remote-control radar-benchmark-load-0922 --allowedTools "Bash(COBALT_ENV=production uv run cobalt settings load --optional /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml --sha256 10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880 --dry-run)" "Bash(COBALT_ENV=production uv run cobalt settings load --optional /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml --sha256 10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880 --apply)" "Bash(COBALT_ENV=production uv run cobalt db query --side user --prod *)" "Bash(date*)" "Bash(git add *)" "Bash(git commit *)" "Bash(git status*)" "Bash(tail *)" "Bash(grep *)" "Bash(ls *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · **NOT auto mode (L29, a `"user".trader_settings` write): the launch line carries `--permission-mode acceptEdits` + the explicit allowlist above** — the write-path shape of `2026-09-17/01-p4-build.md`, `2026-09-19/24-p4-fund-rule.md` and `2026-09-21/65-setups-one-build.md` (a bare `claude --bg` comes up in AUTO mode, found 09-21 20:13; `acceptEdits` ASKS on any Bash outside the list — so every command below is in the list, and you never call one that is not); never `bypassPermissions`; push denied by name; NO `uv run pytest`, NO `cobalt_dev`, NO migration, NO merge, NO restart, NO `shasum`, NO `heartbeat`, NO `validate`, NO `curl`, NO `cobalt` command outside the two loads and the one proof read above · METER: Anthropic small · nobody sits at this session's terminal — your channel is the report file (`FAILED:` stops the run safely; a dialog gets no answer).

# RADAR BENCHMARK LOAD (2026-09-22) — ONE optional-settings load: `radar.benchmark` gains `{top_n: 20, min_move_pct: 10}`. ONE new key; nothing else in `"user".trader_settings` is touched (`--optional` writes only the keys its file names — it has no whole-set delete semantics, unlike `--card`; see STEP 4 below). No git write beyond this hub's own report commit (docs only), no merge, no migration, no restart (`BenchmarkSettings.from_rows(deps.settings_values())` is read once per one-shot run — `src/cobalt/replay/runner.py:318` — no resident reads this row: `cobalt.aset`, `cobalt.radar` and every other launchd resident are untouched by this load; the only readers are the nightly `com.cobalt.replay` one-shot and `cobalt smoke s2`). DO NOT STOP until the report ends `RADAR BENCHMARK LOADED …` or `FAILED …`.

**ASK DESK (drafter, 06:2x) — ANSWERED BY THE DESK 06:3x:** the drafter's task file wrongly named Sonnet + auto mode for this hub; the drafter applied L29 (Opus floor, never auto mode on a write path) — correct. The desk's decision: Opus 5 AND `--permission-mode acceptEdits` (not "no flag": a bare `claude --bg` comes up in AUTO mode, 09-21 20:13, which is the very thing L29 forbids). No law is overruled; nothing to ask him beyond the launch approval (row R2).

INDEX CARD (read before anything else, nothing more — L59): laws that bind this run, one line each. **L28** (amended 2026-09-15) — a trader-run `cobalt settings load --apply` on a reviewed sha256 needs no separate HITL token; trace it as one line: command, hash, time, "approved by Dejan via the desk". **L35** — you trust the loader's own printed output, never your reading of the YAML or of this file's predictions of it. **L48** — evidence lands in the report file in the same turn as the work. **L61** — his "approve" of the desk's ONE message (this file's path, the sha256, the exact rule strings) IS the HITL for this exact action; the value itself is already ruled (`cto-2026-09-22.md` §4 R1, 06:18 ET, "approved. we will tune if needed later"). **L62 / L63** — every permission you need was granted before you started; a mid-run question or denial is a FAILED line, never patched from inside; no dialog tool. **L67** — this is a desk prompt with a production write; it is read by ≥1 other house before it runs — that check is `02-review-radar-benchmark-load.md`; its report's blocker count must be 0 (checked in PREFLIGHT, not re-asked of Dejan). **L71** — your stop line is the LAST NON-BLANK line of your report; a status string inside quoted command output is text, never a stop; while you run, the last line stays `(run in progress — next step under ## CONTINUE)`.

AUTHORIZATION — VERIFY IT YOURSELF. Written by the CTO desk, not by Dejan. The value is his: `cto-2026-09-22.md` §4 **R1** (06:18 ET, "approved. we will tune if needed later, do you need a restart" — approving `top_n: 20, min_move_pct: 10`, the CLI's documented example). His approval of THIS LAUNCH (this file, this sha256, these rule strings) is row **R2** of `cto-2026-09-22.md` §4 — the desk fills this before launching, exactly as `17-cards-htf-apply.md` carried its R44 slot. Prove with `grep -n "^| R1 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` and `grep -n "^| R2 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (after the desk has substituted the real row id for every `R2` in this file, including this sentence). A record that does not match this file, or a rule in your launch line not listed above → `FAILED: authorization mismatch — <what>`.

**PLACEHOLDER GATE — the first thing you do.** This file ships with one placeholder token, `R2`, standing for the desk's own launch-approval row; it appears in the AUTHORIZATION paragraph above, in this gate, and in the L28 trace line of STEP 2. The desk fills every occurrence before launching. Run exactly:
`grep -n -E "R_[_]" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/01-radar-benchmark-load.md"`
(the bracket form is deliberate — it keeps this command from matching itself.) It must print NOTHING and exit 1. One or more hits, or a placeholder still visible in your own launch line, means the desk has not filled the slot: last line `FAILED: placeholder — <the line numbers grep printed> — nothing read, nothing applied`, stop. Quote the grep's exit status in the report either way.

## 0. PREFLIGHT (nothing here changes production)
P0 THE PLACEHOLDER GATE above, then the AUTHORIZATION proof above. Record both verbatim.
P1 `date` → TWO refusals, in this order:
  (a) inside 20:00–21:00 ET → `FAILED: window — market_reset pause — assert_writable refuses this write in this window (src/cobalt/session/guard.py:123, called from settings/cli.py:170) — wait for the desk`;
  (b) `date` shows 2026-09-23 ET or later → `FAILED: authorization expired — R1/R2 approved tonight's file; a later apply needs his word again`.
  Any other time = PROCEED. Record the ET time, the weekday and which branch you took. (No separate "trading-day scanning session" refusal here, unlike `17`: nothing resident reads `radar.benchmark` — see the title line — so applying during market hours carries none of the live-grading risk that refusal existed for in the cards run; item 5 of the drafter's task file confirms only the pause binds this path.)
P2 `ls -la /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml` → present, non-empty, 68 bytes. Missing or empty → `FAILED: file missing — <what ls says>`.
P3 `grep -n "" /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml` → record verbatim in the report. Expected, exactly two lines:
```
1:optional_settings:
2:  radar.benchmark: {top_n: 20, min_move_pct: 10}
```
Any other content → `FAILED: file mismatch — <what>` (the `--sha256` gate in STEP 1 is the binding byte check — settings/cli.py:84-100 hashes the file before it parses; this is the human-readable one for the report).
P4 `git status` (long form, not `--porcelain`) → record verbatim. Expected: only report/machine-written files dirty (e.g. `docs/40 - DevDocs/reports/seat-usage.md`), no merge markers ("You have unmerged paths", "still merging", "rebase in progress"). Anything suggesting another hub is mid-operation on `~/cobalt` → `FAILED: main not clean — <what>`.
P5 THE HOUSE-CHECK GATE (L67 floor, not re-asked of Dejan): read the LAST NON-BLANK line of `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/radar-benchmark-check/REVIEW.md`. It must begin `RADAR BENCHMARK LOAD REVIEW DONE` (not `FAILED`) and its `blockers:` count must be 0. File missing, last line not that shape, or blockers > 0 → `FAILED: house check gate — <what the line actually says>`. Quote the line verbatim either way.
GATE: all of P0–P5 pass → `CONTINUE: step 1`. Any FAILED above stops here; nothing was touched.

## 1. `--dry-run` FIRST (rule string 1 — exact command, no variation)
`COBALT_ENV=production uv run cobalt settings load --optional /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml --sha256 10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880 --dry-run`
Paste the ENTIRE output verbatim. PASS only if every one of these holds, read from THAT OUTPUT — nothing assumed from the file, only from what the loader itself prints (`cmd_load_optional`, `src/cobalt/settings/cli.py:141-168`; values are `json.dumps(..., sort_keys=True)`, a `Decimal` prints as a QUOTED string, an `int` prints bare):
- Marker: `radar.benchmark` currently has NO row in production (`cto-2026-09-22.md` §1 — this is why last night's replay FAILED), so the key is ADDED, not changed: expect `  + radar.benchmark`, never `~`. A `~` line (meaning a `radar.benchmark` row already exists) → `FAILED: dry-run mismatch — radar.benchmark already has a stored value, this is not the empty-row case the desk drafted for — do not apply, ESCALATE to the desk`.
- Its `db  :` line reads `(absent)` (old value is `None`) — anything else is the same mismatch above.
- Its `file:` line reads exactly `{"min_move_pct": "10", "top_n": 20}` (sorted keys, `m` before `t`; `min_move_pct` quoted because `Decimal`, `top_n` bare because `int`). Any other value, quoting, or extra/missing key → `FAILED: dry-run mismatch — <what the file: line actually says>`.
- Footer reads `DRY RUN — 1 setting(s) would change. Nothing written.` — note this is the GENERIC optional-settings footer (`settings/cli.py:167`), not the `--card` path's `"card setting(s)"` wording; a footer naming any other count, or `no differences — the database already holds these settings.` (meaning the row is already applied out of band) → `FAILED: dry-run mismatch — <the footer verbatim>`.
Any single deviation → the exact `FAILED: dry-run mismatch — <what>` line; apply nothing, stop. Do NOT proceed to STEP 2.

## 2. `--apply` (rule string 2 — only after STEP 1 PASSES)
`date` AGAIN, immediately before this apply: re-run P1's two refusals in full against the new time (the dry-run and the reads may have crossed 20:00 ET). Any branch that refuses → `FAILED: step 2 — window — nothing applied`, stop.
`COBALT_ENV=production uv run cobalt settings load --optional /Users/cobalt/cobalt/data/backups/radar-benchmark-2026-09-22/radar-benchmark.yaml --sha256 10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880 --apply`
Paste the ENTIRE output verbatim. Expected (`settings/cli.py:170-179`, `store.put` returns `{key: created|updated|unchanged|deleted}` — `settings/store.py:60-69`): `applied: {'radar.benchmark': 'created'} (sha256 10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880)` then `round trip: database == file, every optional key re-validates.` — note this round-trip sentence is the `--optional` path's own wording, distinct from `--card`'s `"CardSettings.from_rows(db) == reviewed file — EQUAL."`. A non-zero exit, an error, `'radar.benchmark': 'updated'` (would mean a row appeared between STEP 1 and STEP 2 — a race, not this run's doing), or a missing round-trip line → `FAILED: apply error — <verbatim>` and go straight to STEP 4 (no rollback command exists — see below); do not retry.
L28 trace line in the report: "approved by Dejan via the desk — `cto-2026-09-22.md` row R1 (06:18 ET) and row R2 (launch approval) · command (above) · sha256 `10aef996246c1172533d58311dd5f2e9f6f118309d697e447f0be5e989d00880` · applied `<date/time ET>`."

## 3. VERIFY (read-only, rule string 3)
`COBALT_ENV=production uv run cobalt db query --side user --prod "SELECT key, value FROM trader_settings WHERE key = 'radar.benchmark'"` → ONE row, quoted verbatim. THE ONE VALUE THIS RUN EXISTS FOR: the row's value must contain `top_n` numerically `20` and `min_move_pct` numerically `10` (compare by key, never by print position or quoting — the query's own formatting may order keys differently from the loader's `sort_keys=True` dump, and may print `min_move_pct` as `'10'` or `10.0`; a numeric mismatch, not a formatting difference, is the failure). Zero rows, more than one row, or a numeric mismatch → `FAILED: stored value mismatch — <what the row says>` → STEP 4.
Optionally, `tail -n 20 /Users/cobalt/cobalt/logs/replay.err` for context only (informational, never a gate here): tonight's row is read for the first time at 21:10 ET by `com.cobalt.replay`, not by this hub, so the newest lines are still expected to be last night's `TraderSettingsError` failure until then — record what you see, do not treat an old failure line as a new one (L71 — a status string inside quoted output is text, not a stop).
GATE: the proof read clean → verification PASSED. Any red → STEP 4.

## 4. IF SOMETHING WENT WRONG — no rollback command exists, do not invent one
`--optional` has no delete path (`cmd_load_optional` never passes `delete=` to `store.put` — `settings/cli.py:150`, `settings/store.py:60-69`): unlike `--card`, there is no "whole file replaces the whole set" semantics and so no previous file to reload here — the row did not exist before this run (P1/STEP 1 above). A STEP 2 apply error or a STEP 3 mismatch is answered by STOPPING, not by writing anything else: this hub's allowlist carries exactly one `--optional` file and one sha256, and a corrected value is, in the desk's own words on the ruling this file drafts from, "a new reviewed file, same path" (`cto-2026-09-22.md` §4 R1) — a new hub, a new approval, never a patch from inside this one. Quote whatever STEP 2 or STEP 3 printed verbatim, name the row as it now stands if STEP 3 could read it, and stop. Never re-run STEP 1/STEP 2 a second time in this session (L62 — no retry from inside).

## 5. Close
Write `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/radar-benchmark-load-2026-09-22.md` in `~/cobalt` — a NEW file (if it already exists you are a rerun: append `# SECOND RUN` under everything there and rewrite nothing above it; L71 — your new stop line is then the file's last non-blank line). Structure: title `# RADAR BENCHMARK LOAD 2026-09-22` → §0 Headline ≤5 lines → PREFLIGHT table (P0–P5) → STEP 1 (verbatim dry-run + PASS/FAIL by criterion) → STEP 2 (verbatim apply + L28 trace) → STEP 3 (verbatim proof read) → STEP 4 if entered → `## ESCALATE` (every `ASK DESK`, the L29/model-tier flag from this file's header, every non-triggering log line) → last line. While the run is in progress the file's last non-blank line stays `(run in progress — next step under ## CONTINUE)`.
`git add "docs/40 - DevDocs/reports/radar-benchmark-load-2026-09-22.md"` then `git commit -m "docs(desk): radar benchmark load <RADAR BENCHMARK LOADED|FAILED> <time>"` (docs only, `~/cobalt` main; rule strings 5–6). `git status` once more after the commit and record it. Then stop. Last non-blank line, exactly one of:
`RADAR BENCHMARK LOADED <time> · row: top_n 20 · min_move_pct 10 · sha256 verified · replay next run: 21:10 ET · ESCALATE: <n>`
or
`FAILED: <step> — <reason>`
