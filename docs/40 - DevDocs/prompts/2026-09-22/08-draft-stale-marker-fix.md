MODEL: Opus 5 (`claude-opus-5`) — a classify-then-draft job (09-20 lesson 3: a fix round starts with a drafter that CLASSIFIES every finding from the check hub's file-check rows — FIX · NOT REAL · UNPROVEN · OUT OF SCOPE · OWNER ITEM — before anything is built); no code, no DB, no launch · SEAT: drafter `stale-marker-fix-draft-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK from `~/cobalt`: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/08-draft-stale-marker-fix.md' and follow it exactly." --model claude-opus-5 --permission-mode auto --remote-control stale-marker-fix-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as 09-21's `50` / `76` / `79`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no memory-folder write (L58) — you write exactly the THREE files named below with the Write tool · METER: Anthropic small · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# Stale marker — FIX ROUND (round 2) drafter, 2026-09-22

WHY: `reports/stale-marker-check-2026-09-22.md` closed `houses that checked: 4 of 4 · defects that HOLD: 3 · ready for the stacked deploy: 3 of 4` (Sol: FIX FIRST — "mixed-ticker exactness and stale-card route tests"; Opus: BUILD STANDS EXCEPT departed/excluded no-badge untested, report §0 overclaims). Today's stacked deploy prompt (`prompts/2026-09-22/05-stacked-deploy.md` P6) needs `defects that HOLD: 0` and `ready: n of n` for THIS branch, so the branch takes a fix round and a round-2 check before it ships (L67; his R3 today: deploys run from 11:00 ET on, as many as are built and checked). LAW STEP: check → FIX (tests only, expected) → check r2 → deploy.

## INDEX CARD — read these, nothing more
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — L35, L45 (real-artifact fixtures), L48, L62, L63, L67 (three checkers; ≤3 rounds), L70 (unproven ≠ defect), L71, L73. One line each in the prompts you write.
2. THE CHECK REPORT, end to end: `docs/40 - DevDocs/reports/stale-marker-check-2026-09-22.md` — `## Per question`, `## Checked against the branch` (the file-check rows are your evidence: a finding HOLDS only where the hub's row says so), `## Ready for the stacked deploy`, `## ESCALATE` 1–6. The four checker files under `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/` (the folder the report names) — read Sol's and Opus's in full, Grok's and Gemini's `CHECK:` sections.
3. THE BUILD: branch `s2/stale-marker-0921`, worktree `/Users/cobalt/cobalt-wt/stale-marker`, tip `ca9566f`, build report `/Users/cobalt/cobalt-wt/stale-marker/docs/40 - DevDocs/reports/stale-marker-build-2026-09-21.md` (its §0 headline "8 new tests RED on main's code" vs its T1 section: 5 red, 3 pins — Opus's HOLD), the build prompt `prompts/2026-09-21/51-stale-marker-build.md` (the contract; its allowlist = the fix prompt's base), `git -C /Users/cobalt/cobalt log --oneline main..s2/stale-marker-0921` and `git -C /Users/cobalt/cobalt show --stat` of each commit. The code: `src/cobalt/aset/radar_panel.py` (the branch's diff), `tests/cobalt/test_radar_panel.py`, `tests/cobalt/test_radar_panel_cards.py`.
4. THE ROUND-1 CHECK PROMPT `prompts/2026-09-21/68-stale-marker-check.md` — its launch line, packet layout, question set and stop-line shape are the base for the round-2 check (`10`); the 09-20 round-2 shape `prompts/2026-09-20/21-bars-chunk-1a-check-r2.md` (round 2 reads ONLY the fold: the fix diff + round 1's verdict table + the FIX rows' status).
5. `cto-2026-09-22.md` §4 R3 (today's deploy ruling) and R5 (the round-1 launch row); `cto-2026-09-21.md` R36 (his stale-marker ruling), R46 / R49 (check seats: Grok · Gemini · Sol · Opus 5, the two strings verbatim), R39 (`grok`/`agy` through 2026-09-22 23:59 ET).

## THE JOB — three files, Write tool
(a) FIRST, IN YOUR REPORT `docs/40 - DevDocs/reports/stale-marker-fix-draft-2026-09-22.md`: the CLASSIFICATION TABLE — every finding of every checker (the report's `## Per question` rows + ESCALATE 1–6), one row each: `id · checker(s) · finding (≤2 lines) · file-check row (HOLDS / DOES NOT HOLD / not checked) · CLASS: FIX | NOT REAL | UNPROVEN (L70) | OUT OF SCOPE | OWNER ITEM · what the fix is (FIX rows only)`. Expected from the stop line: FIX = the mixed-ticker exactness tests (a stale ticker beside a healthy one in the same pool category; a badge on exactly the stale row and card, none on the healthy), the stale-card route test (the real `/radar` card route renders the badge — a route test, not only the renderer), the departed/excluded no-badge tests (`tc:400-401, 436-439`), and the build report's §0 headline corrected to "5 red, 3 pins"; OWNER ITEM = ESCALATE 3 (the two clocks — poller 180 s RTH-only vs evaluator 200 s all-sessions — L53: his) and ESCALATE 4 (in-card badge lag, ASSUMED by the proposal); UNPROVEN = ESCALATE 5 (no browser). Only FIX rows are built; every OWNER ITEM reaches him verbatim through the desk.
(b) `docs/40 - DevDocs/prompts/2026-09-22/09-stale-marker-fix.md` — the FIX build, ONE complete block on `51`'s shape (MODEL Sonnet 5 unless a FIX row touches a write path — none expected: tests + one report line; OFFLINE; EXISTING worktree `/Users/cobalt/cobalt-wt/stale-marker`, branch `s2/stale-marker-0921` from `ca9566f`; launch line FIRST = `cd /Users/cobalt/cobalt-wt/stale-marker` then the `claude --bg` line whose `--allowedTools` are `51`'s BYTE FOR BYTE minus anything a tests-only fix never needs (say which you dropped), `--permission-mode auto` (a tests-only build in the dev lane; NOT `acceptEdits` — that mode raised three permission prompts today; no `.env`, no `cobalt_dev`: the offline suite only), `--disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)"`, `--add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt`; index card SECOND; AUTHORIZATION greps R3 + the `R__` launch row the desk fills; PREFLIGHT (worktree clean on the branch at `ca9566f`, `git status` long form, `date`); STEPS = the FIX rows only, each as T (test written, RED shown where a real defect, or GREEN-as-pin where coverage only — say which) → C (only if a test exposes a code defect; expected NONE — a coverage gap fixed by tests that pass on the current code is recorded as "coverage added, code unchanged") → the OFFLINE suite `uv run pytest -q tests/cobalt tests/taxonomy` green (baseline `2230/0` on the branch) → the build report's §0 headline corrected in place (its T1 numbers) with a dated line naming this round → DevDoc untouched unless a public behaviour changed → `git add` by explicit path, `git commit -m "fix(stale-marker): round 2 — <what>"`, report `/Users/cobalt/cobalt-wt/stale-marker/docs/40 - DevDocs/reports/stale-marker-fix-r2-2026-09-22.md` committed on the branch, LAST LINE `STALE MARKER FIX R2 BUILT <tip> | on ca9566f | offline <p>/0 | tests added: <n> | code changed: <yes|no> | report headline corrected: yes | ESCALATE: <n>` or `FAILED: …`. RECOVERY RULE (L60) verbatim from `51`. A denied Bash = FAILED, never a re-shape; file content through Write / Edit only, never a shell heredoc.
(c) `docs/40 - DevDocs/prompts/2026-09-22/10-stale-marker-check-r2.md` — ROUND 2 of the four-seat check on `68`'s launch line BYTE FOR BYTE (only the prompt path and the remote-control name `stale-marker-check-r2-0922` change; same Grok · Gemini · Sol · Opus 5 strings, same `scratch/tribunal-bars-0920` root, staged in a NEW subfolder `stale-marker-check/r2/` — first Write creates it, no `mkdir` beyond the approved root string). PREFLIGHT: the fix report's last line starts `STALE MARKER FIX R2 BUILT `; STAGGER gates on `reports/ops-6a-check-r3-2026-09-22.md` (`OPS 6A CHECK R3 DONE|FAILED` as its last line, else `FAILED PREFLIGHT: 81 is running — the desk staggers`) — round 2 runs AFTER `81`. PACKET = the FOLD ONLY: the fix diff (`git -C /Users/cobalt/cobalt log -p ca9566f..<tip> -- . ":(exclude)docs/40 - DevDocs/reports/stale-marker-fix-r2-2026-09-22.md"`), the fix report, round 1's `## Ready` + `## Checked against the branch` tables, your classification table, and the three HOLD findings verbatim. QUESTIONS: for each FIX row — is it closed by the fold (the test exists, asserts the right thing, is green for the right reason)? Any NEW defect the fix introduced? The OWNER ITEMS are NOT re-asked. Report `docs/40 - DevDocs/reports/stale-marker-check-r2-2026-09-22.md`; LAST LINE on `68`'s shape: `STALE MARKER CHECK R2 DONE · grok: … · gemini: … · sol: … · opus: … · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the stacked deploy: <r> of <n> · ESCALATE: <n>` — `05` P6 reads THIS file's last line when the desk re-points it (note that for the desk in your report).
Your report's LAST LINE: `STALE MARKER FIX PROMPTS DRAFTED · FIX: <n> · NOT REAL: <n> · UNPROVEN: <n> · OUT OF SCOPE: <n> · OWNER ITEM: <n> · code change expected: <yes|no> · new rule strings: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
- L19 whole-prompt. L36 you launch nothing. L58 no memory write. Never invent a flag: every command shape appears in `51` / `68` / the code's CLI. Every OWNER ITEM is quoted, never resolved by you. `date` for every time you write. Stop within 25 minutes; your stop line is your report's last non-blank line.
