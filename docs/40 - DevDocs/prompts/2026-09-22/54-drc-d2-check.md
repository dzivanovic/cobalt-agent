MODEL: Sonnet 5 (`claude-sonnet-5`) — stage ONE packet before FOUR checkers, file-check, tabulate; no verdict of your own (L37) · SEAT: build-check hub `drc-d2-check`, launched by the CTO desk in the background · LADDER `S3-P3 · F14` · LAW STEP (L67): build `49-drc-d2-build.md` → **THIS = CHECK ROUND 1 of at most 3, the WHOLE D2 build** → fix round (L75) if a defect HOLDS → D3 stacks. RULE STRINGS: `53-drc-d1-check.md`'s line BYTE FOR BYTE (= `16`'s fifteen allow strings, three denies, triplet); only the prompt path and remote-control name differ; NO NEW STRING. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/54-drc-d2-check.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control drc-d2-check --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/tribunal-bars-0920)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" "Bash(claude -p --model claude-opus-5-5 *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode (read-only seat; stated here and on the line, L62 as amended R80); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO database, NO `pytest`, NO git write, no vault write, no memory-folder write · METER: as `53` (Sol probed → METER until Sat 2026-09-26 06:47 ET, skipped; Opus 5.5, Grok, Gemini; ≥3) · nobody sits at this terminal. DESK: watch the LAST NON-BLANK LINE, `^(DRC D2 CHECK DONE|FAILED)`.

# BUILD CHECK, ROUND 1 — DRC D2: THE IMPORT PAGE, THE BYTES WRITER, BOTH-PLACED + THE NO-TRADE DAY, THE EVENT ROW. Everything in `53-drc-d1-check.md` binds you UNCHANGED — AUTHORIZATION (with `54-drc-d2-check.md` as the launch-row filename and row **R__**), the DATE + EXTENSION GATE, INDEX CARD, PREFLIGHT, REPORT, §1 staging rules, §2 launch, §3 collation, §4 close — with ONLY these substitutions: `d1` → `d2` in every path, name and line (`drc-check/d2/`, `/Users/cobalt/cobalt-wt/drc-d2`, `drc-d2-build-2026-09-2<n>.md`, `drc-d2-check-2026-09-2<n>.md`, `DRC D2 BUILT `, `CHECK D2:`, `DRC D2 CHECK DONE`, rows `D2-1 … D2-6`); the build prompt is `49-drc-d2-build.md` and its CLOSE list is the boundary; the stagger path is filled by the desk here: **`__`**; the packet's `rulings.md` = §4 rows R90, R91, R92, R93, R94, R101, R102, R103; `design.md` = v2 §2, §3, §9, §10 (E5, E7, E11, X13), §12; the (ii) boundary check = `git -C /Users/cobalt/cobalt log --oneline <base>..<tip> -- src/cobalt/prefill src/cobalt/replay src/cobalt/cards src/cobalt/radar src/cobalt/aset/engine.py src/cobalt/settings configs src/cobalt/db_migrations` EMPTY; (iii) = `git -C /Users/cobalt/cobalt show <tip> --stat -- uv.lock` + the `uv.lock` hunk quoted (only `python-multipart` direct); (iv) = the text `_commit` of `vaultwrite/writer.py` unchanged (`git -C /Users/cobalt/cobalt log -p <base>..<tip> -- src/cobalt/vaultwrite/writer.py`, every hunk quoted and placed); the `ready` field reads `ready for the next chunk`.

QUESTIONS.md (verbatim): "You are one of four checkers of ONE BUILD CHUNK (round 1 of at most 3). The sentence that launched you says which house you are. Read ONLY the files in this folder. You cannot run commands. The chunk adds ONE import page to an existing web app (FastAPI): the trader drops his two daily files (a broker trading-log CSV and a trade-journal log; optional chart screenshots per trade); each file is saved into his notes vault through the ONE vault-write component by a NEW bytes method (create-if-absent, versioned, sha256'd, refused during the 20:00–21:00 ET market reset), recorded as a row, parsed by the previous chunk's parsers; when both files for a date are parsed — or the trader marks a market trading day as a no-trade day — the page writes ONE event row (pending → running → done | failed) and calls the build (the NEXT chunk; until it exists the event lands `failed`, loudly). The design is `design.md`, the owner's rulings `rulings.md`, the prompt `prompt.md`, the build `build-diff.md` + `devdocs.md`, the builder's claims `build-report.md` (a CLAIM). The contract: *a red test is a RESULT; nothing plausible-empty; one input creates nothing; the route itself owns no side effect; no value of the owner's in the repository*. For EACH row D2-1 … D2-6 of `prompt.md`, exactly one of, each with `file:line`: `MEETS THE ROW` / `DOES NOT MEET — <file:line, the concrete request or file that shows it>` / `NEW DEFECT — <file:line>`. Look hardest at: **the bytes method** — can any request write outside `1 - Trading/5 - Review/_imports/drc/<date>/` (a `..`, an absolute name, a date that is not a date)? can a second drop overwrite or delete old bytes? is the sha256 recorded the bytes' own? is it refused in the market reset by the same gate as notes, and is the text-note write path unchanged? **both-placed** — every arrival order, a re-drop, one file only (no event, the page says what is missing); an exception after `pending` leaves `failed`, never `done`; nothing polls, nothing queues; **the no-trade day** (owner's R93) — refused on a non-trading date and on a date whose trading log has executions (count named); a zero-execution trading log takes the same path; one trading-day calendar, not a second; **the market reset** (R102) — upload, no-trade and the bytes method each refused with the reason, nothing stored; **screenshots** — a bad header or an unknown trade key fails that file and binds nothing; a superseding trading log that drops a trade key leaves the binding listed `orphaned`, never re-bound or deleted (X13); **the dependency** — `python-multipart` pinned direct at the already-locked version, no new package; **names** — no vendor or person name in an identifier (L31). THEN over the whole diff: (a) weak assertions, (b) changes outside the rows' files, (c) L52 — any path to a score, rank, grade or size. `NOT CHECKABLE FROM READS — <what would have to be run>` is not a defect. End with EXACTLY one line: `CHECK D2: BUILD STANDS` / `CHECK D2: BUILD STANDS EXCEPT <items>` / `CHECK D2: FIX <items>` — followed on the SAME line by ` · ready for the next chunk: YES|NO · <ten words of reason if NO>`."

STOP LINE (L71): `DRC D2 CHECK DONE · grok: <…> · gemini: <…> · sol: <…> · opus: <…> · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the next chunk: <r> of <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`. NEXT, not yours: ≥3 checked, 0 HOLD → the desk launches D3 (`50`); else a fix round (L75).
