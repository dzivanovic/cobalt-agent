MODEL: Sonnet 5 (`claude-sonnet-5`) — stage ONE packet before FOUR checkers, file-check, tabulate; no verdict of your own (L37) · SEAT: build-check hub `drc-d3-check`, launched by the CTO desk in the background · LADDER `S3-P3 · F14` · LAW STEP (L67): build `50-drc-d3-build.md` → **THIS = CHECK ROUND 1 of at most 3, the WHOLE D3 build** → fix round (L75) if a defect HOLDS → the deploy prompt for the stacked set D4 + D1 + D2 + D3 (L43 / L68; read by houses other than its author, L67) → his "approve". RULE STRINGS: `53-drc-d1-check.md`'s line BYTE FOR BYTE; only the prompt path and remote-control name differ; NO NEW STRING. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/55-drc-d3-check.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control drc-d3-check --allowedTools "Bash(grok *)" "Bash(agy *)" "Bash(mkdir -p scratch/tribunal-bars-0920)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards show*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards log*)" "Bash(git -C /Users/cobalt/cobalt-wt/s2-p2-cards diff*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(codex exec --skip-git-repo-check -m gpt-5.6-sol -s read-only *)" "Bash(claude -p --model claude-opus-5-5 *)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · SESSION: fresh · auto mode (read-only seat; stated here and on the line); never `bypassPermissions`; NO production command, NO `cobalt_dev`, NO database, NO `pytest`, NO git write, no vault write, no memory-folder write · METER: as `53` · nobody sits at this terminal. DESK: watch the LAST NON-BLANK LINE, `^(DRC D3 CHECK DONE|FAILED)`.

# BUILD CHECK, ROUND 1 — DRC D3: THE BUILD AND THE NOTE FROM HIS TEMPLATE, THE NO-TRADE DRC, THE 21:10 MISS LINE, THE 15:40 JOB RETIRED. Everything in `53-drc-d1-check.md` binds you UNCHANGED, with ONLY these substitutions: `d1` → `d3` in every path, name and line (`drc-check/d3/`, `/Users/cobalt/cobalt-wt/drc-d3`, `drc-d3-build-2026-09-2<n>.md`, `drc-d3-check-2026-09-2<n>.md`, `DRC D3 BUILT `, `CHECK D3:`, `DRC D3 CHECK DONE`, rows `D3-1 … D3-8`); launch-row filename `55-drc-d3-check.md`, row **R__**; the build prompt is `50-drc-d3-build.md`; the stagger path is filled by the desk here: **`__`**; `rulings.md` = §4 rows R65–R73, R90–R103, R114, R116 and R117; `design.md` = v2 §3, §5, §6, §8, §9, §10, §12 and §13 VERBATIM (§13 is long — parts at `### ` boundaries); PLUS (8) `template-shape.md` = the committed `tests/fixtures/drc/template_shape.md` at the tip (for the E3 claim) — his real template is NOT staged (his user data); the (ii) boundary check = `git -C /Users/cobalt/cobalt log --oneline <base>..<tip> -- src/cobalt/daymode src/cobalt/cards src/cobalt/radar src/cobalt/aset/engine.py src/cobalt/settings src/cobalt/db_migrations` EMPTY; (iii) = `git -C /Users/cobalt/cobalt show <tip>:ops/com.cobalt.prefill-drc.plist` → "does not exist" AND `git -C /Users/cobalt/cobalt show <tip>:configs/cobalt/templates/drc.md.j2` → "does not exist" (both quoted); (iv) = `grep -rn "prefill-drc\|drc.md.j2" /Users/cobalt/cobalt-wt/drc-d3/src /Users/cobalt/cobalt-wt/drc-d3/configs /Users/cobalt/cobalt-wt/drc-d3/ops` → each hit quoted and placed; the `ready` field reads `ready for the deploy prompt`.

RE-ISSUED 2026-09-22 (L19, whole file) by the Opus 5.5 prompt seat `drc-reissue-draft-0922`: the QUESTIONS gain R114's several-playbooks check and R116 / R117's one-to-one name rule (row D3-2b); `rulings.md` carries R114, R116, R117. The launch line above is unchanged byte for byte.

QUESTIONS.md (verbatim): "You are one of four checkers of ONE BUILD CHUNK (round 1 of at most 3). The sentence that launched you says which house you are. Read ONLY the files in this folder. You cannot run commands. The chunk builds the trader's Daily Report Card note: on an event from the import page it creates the note ONCE from HIS OWN Obsidian template (only the date token substituted; any other `{{` fails), then upserts Cobalt-owned marked units under his headings — a summary (gross AND net P&L), a premarket block (four keyed lines only), the P&L line, risk facts, one block per trade with his OWN answer unit beside it (created once, never rewritten), a reconcile diff (writes to legs come in a later chunk), open positions carried, his existing checkbox scaffold, the nightly miss line — and stores every derived number with its inputs. On a no-trade day it writes a no-trade unit and a blank unit for his 'why no trades'. It retires the old 15:40 scheduled job, its CLI entry and the repo copy of the template in the same change, moves the 21:10 miss line to a stored-arguments path when the note does not exist yet, and re-keys a smoke check on the event. There is NO rule engine (the owner's R101: rule adherence stays with his coach session). The design is `design.md`, the rulings `rulings.md`, the prompt `prompt.md`, the build `build-diff.md` + `devdocs.md`, the builder's claims `build-report.md` (a CLAIM), the template-shape fixture `template-shape.md`. The contract: *nothing plausible-empty — never create-then-return; a missing source renders `not given`; his text is never rewritten (a Cobalt line he changed → his wins, logged); no unit ever writes a line beginning `Grade:` or `Goal:` (the 09:00 reader's regexes); nothing reaches a score, grade or size; no value of the owner's in the repository*. For EACH row D3-1 … D3-8 of `prompt.md` (D3-2b included), exactly one of, each with `file:line`: `MEETS THE ROW` / `DOES NOT MEET — <file:line, the concrete event, template or note that shows it>` / `NEW DEFECT — <file:line>`. Look hardest at: **the template** — only the date token replaced; the fixture keeps his headings in order and carries none of his prose or example values; **idempotence** — a second build with the same inputs changes nothing; a superseding file upserts derived units and leaves every voice unit byte-identical; a vanished trade leaves its voice unit with ONE `orphaned` line (E8); **the no-trade DRC** (R93) and **gross + net** (R103) and **the P&L line filled** (R102) — present and sourced from the stored rows; **`not given`** — for every field the stats log or settings do not carry (never 0, never a guess); the card match window unset → `card: not matched (window not given)`; **the miss line** — absent note at 21:10: no raise, no note, the exact render arguments stored, the run green; the build later renders the SAME bytes (E6) and writes exactly one line; **retirement** — the job, its jobs.yaml rows, reader label, CLI subcommand, README lines and the repo template all gone in this ONE change, no dangling reference; the rules config moved to the ASET resident's reads; **smoke** — pending / fail / pass on the event, nothing at 15:41; **engine rows** — nothing renders a rule result (R101); **playbook → setup** (the owner's R116 / R117: one-to-one names) — is a journal playbook name mapped to a setup ONLY when, after stripping ONE trailing ` Long` / ` Short` (case-insensitive, a whole word — never `Longer`, never a name with no space before it, stripped once), it EQUALS a strategy-note title read from the vault folder AT BUILD TIME, the setup id being that note's own slug (validated, never normalised)? does everything else render `unmapped: <name>`, counted in the summary, never guessed, never a build failure — including a missing folder or a note with a bad slug? is there ANY mapping table, alias list, fuzzy / similarity match, committed list of titles, or model call anywhere on this path? **several playbooks on one trade** (R114) — does every trade render ALL its playbook names, in the journal's order, with nothing picking one? is each resolution stored with its inputs (the names and the titles the build read) and a function version? THEN over the whole diff: (a) weak assertions — and every removed test named with the code it tested, (b) changes outside the rows' files, (c) L52. `NOT CHECKABLE FROM READS — <what would have to be run>` is not a defect. End with EXACTLY one line: `CHECK D3: BUILD STANDS` / `CHECK D3: BUILD STANDS EXCEPT <items>` / `CHECK D3: FIX <items>` — followed on the SAME line by ` · ready for the deploy prompt: YES|NO · <ten words of reason if NO>`."

STOP LINE (L71): `DRC D3 CHECK DONE · grok: <…> · gemini: <…> · sol: <…> · opus: <…> · houses that checked: <n> of 4 · defects that HOLD: <n> · ready for the deploy prompt: <r> of <n> · ESCALATE: <n>` — or `FAILED: <step> — <reason>` / `FAILED PREFLIGHT: <rule>`. NEXT, not yours: ≥3 checked, 0 HOLD across D4 / D1 / D2 / D3 → the desk drafts the stacked deploy prompt (residents down before the merge, inside the pause, L66; bootout of `com.cobalt.prefill-drc`; migration number fixed at the L68 gate; his daily-stop value loaded first); else a fix round (L75).
