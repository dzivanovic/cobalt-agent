MODEL: Opus 5.5 (`claude-opus-5-5` — his R32 of 2026-09-22 13:20 ET; R36) — D1 adds a `db_migrations` file and a DB store (L29's list: a migration), so the floor is Opus, Opus 5.5 by R32. `--permission-mode auto`: the DEV LANE as `33` runs it (L62 as amended R80 — the mode is stated here and on the line): an offline build in a worktree plus the with-DB suite on `cobalt_dev` through the `.env` pair, the migration applied ONLY inside the suite's never-committed rollback transaction; no production command, no vault write (the dev vault included), no migration applied outside the suite. NOT `acceptEdits` (it raises dialogs on unlisted Bash — L63). An unlisted command = do not call it; file content via Write / Edit, never a shell heredoc · SEAT: DRC D1 builder `drc-d1-build`, launched by the CTO desk in the background. The desk FIRST creates the worktree (`git -C /Users/cobalt/cobalt worktree add -b drc/d1-trading-log /Users/cobalt/cobalt-wt/drc-d1 <BASE TIP>`), fills the BASE TIP below and the launch row **R__**, commits both on main, then runs two bare commands: `cd /Users/cobalt/cobalt-wt/drc-d1` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/48-drc-d1-build.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control drc-d1-build --allowedTools "Bash(uv run pytest *)" "Bash(uv run cobalt jobs restarts *)" "Bash(git add *)" "Bash(git commit *)" "Bash(git diff *)" "Bash(git status*)" "Bash(git log*)" "Bash(git show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(cd *)" "Bash(mkdir -p *)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" "Bash(COBALT_ENV=dev uv run pytest *)" "Bash(cp /Users/cobalt/cobalt/.env /Users/cobalt/cobalt-wt/drc-d1/.env)" "Bash(rm /Users/cobalt/cobalt-wt/drc-d1/.env)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: `33`'s seventeen shared allow strings, three denies and `--add-dir` triplet BYTE FOR BYTE; the TWO `.env` strings are NEW (R64's shape re-pointed to `drc-d1`) and are in the desk's ONE approval list · SESSION: fresh; every relaunch uses the SAME line and resumes from `## CONTINUE` · never `bypassPermissions`; push DENIED; no production command, no `--allow-prod`, no `COBALT_ENV=production`, no `cobalt db` typed, no vault write, no memory-folder write (L58), no `launchctl`, no `claude` (L36) · `cobalt_dev` ONLY through `COBALT_ENV=dev uv run pytest …`; `.env` copied BY NAME, never printed, never read, removed at EVERY stop (L41) · METER: Anthropic MEDIUM (≈9 seat hours by v2's estimate + R91's second parser; two full suites per row that touches the DB) · nobody sits at this terminal — your channel is the report file: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default the row names, continue.

# DRC D1 — THE TWO DAILY-FILE PARSERS, PAIRING, CARRIED POSITIONS, MODELS, STORE, ONE MIGRATION. Branch `drc/d1-trading-log`, worktree `/Users/cobalt/cobalt-wt/drc-d1`, from BASE TIP **`__`** (the desk fills it: the D4 build's stop-line tip, `51-drc-d4-build.md` — the DRC chunks are ONE LINEAR STACK D4 → D1 → D2 → D3, D5 later, so no two DRC branches share a file as siblings, L72 as amended R87 P-b). DO NOT STOP until the report's last line is `DRC D1 BUILT …` or `FAILED …`. NEVER END A TURN BETWEEN STEPS.

RE-ISSUED 2026-09-22 (L19, whole file) by the Opus 5.5 prompt seat `drc-reissue-draft-0922` for his R114 / R117 (files found by HEADER, several playbooks per trade, the carry fixture constructed, constructed playbook names in the fixtures). The launch line above is unchanged byte for byte.

RE-ISSUED 2026-09-23 (L19, whole file) by the Opus 5.5 prompt seat `drc-r17-draft-0923` for his 09-23 R17 (`cto-2026-09-23.md` §4; open items 1–5 of `reports/open-items-2026-09-23.md`): the trading log's date = the folder / upload date (D1-2); the stop price only from the stats log's own stop field, never computed (D1-4); a half-matching header is now READ as `partial` with a loud `PARTIAL — missing: <columns>` flag (D1-2a / D1-2 / D1-4). The launch line above is unchanged byte for byte.

WHY: the DRC design is derived (`docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md`, "v2") and every owner item is ruled (`cto-2026-09-22.md` §4 R90–R103). D1 is v2 §9's first chunk: the parser of his DAS trading-log export, FIFO pairing into trades and exit legs, open positions carried day to day (R67), and — NEW by his R91 — the parser of his TradeZella daily LOG, which carries entries, stops, targets, assumed R:R and realized R:R (typed cells and OCR are both OUT; R91 replaces them). LADDER `S3-P3 · F14`. E1 = ONE REAL DAY of his two files (R91), dropped by him at `1 - Trading/5 - Review/_imports/drc/<date>/` (R92). D1 FAILS PREFLIGHT until both exist (L45: no invented fixture when the real artifact is coming).

## HIS RULINGS THAT CHANGE v2 FOR THIS CHUNK (his words govern where v2 differs)
- **R91** (O1): entries, stops, targets, assumed R:R, realized R:R are PARSED from the TradeZella log; MAE / MFE / best exit ONLY if the log carries them, else `not given` (L1: no invented values). v2 §4's `TradeStatsSource` "reads nothing" row is REPLACED by a real parser.
- **R94** (O4): "DRC is account agnostic. Just assume all goes into same bucket" → every account in the export is ONE bucket; the account column is READ and stored per execution, NEVER a gate. v2 `[F-13]`'s "more than one distinct account value FAILs the file" is STRUCK.
- **R92** (O2): the files live at `1 - Trading/5 - Review/_imports/drc/<date>/` (NOT vault-root `_imports/`).
- **R103** (O15): his consent to commit the STRIPPED real-shape fixture came WITH the export; (O17) Postgres only — no `trades.jsonl` / `days.jsonl`; (O22) no upload size ceiling.
- **R114** (E1 dropped, facts): his two files sit in `1 - Trading/5 - Review/_imports/drc/2026-09-18/`, CSV content with a `.md` extension ("obsidian is blind to that extention"), names he chooses every day → every file is identified by its HEADER LINE, never by its name or extension (D1-2a). One trade carries several entries, exits, stops and sizes AND "multiple playbook names because it was two playbook setups adding onto each other without closing first setup" → a trade keeps EVERY playbook name, in order; nothing picks one (D1-4). "I did not leave any open trades overnight" → the carry fixture is CONSTRUCTED in E1's shape and says so (D1-0). The `_reference/` folder beside the date folders is never an input.
- **R117**: "Assume all wilde named to match" — the fixtures replace his playbook names with CONSTRUCTED names (L45 / L32); the name → setup rule itself is D3's (R116, `50` D3-2b).
- **R17 of 09-23** (`cto-2026-09-23.md` §4, his words "1)A 2)A 3)folder is a date 4)use stops from tradezella. This is mostly why I'm giving you both. 5) B but needed to say its partial"), the parts D1 builds: **(3)** the trading log has no date column (E1: `Time` is `HH:MM:SS`, no date, no zone) → every execution's DATE is the import date the caller passes (the `<date>` folder / the upload's `date`), never read from the file, never inferred (D1-2). **(4)** the STOP PRICE comes ONLY from the stats log's own stop field, read by header NAME; it is NEVER computed from `Trade Risk` (dollars) or any other column; `not given` only when the stats log has none for that trade. E1's stats-log header (49 names, read 2026-09-23 by the prompt seat) carries NO stop column, so no stop column name is written in this prompt or in code — every E1 trade's stop is `not given` and the question goes to the desk (`ASK DESK`, D1-4). **(5)** a file whose header carries SOME of a kind's required names is READ — what is there — as `partial`, and carries the LOUD flag `PARTIAL — missing: <columns>` (L1 is met by the flag; nothing silent) — this REPLACES the 09-22 re-issue's "half-match → degraded, not parsed" (D1-2a). **(1)** exact title equality after the Long / Short strip is D3's (`50` D3-2b, as built); **(2)** hand-dropped files in `_imports/drc/<date>/` are D2's (`49` D2-3b), through D1's ONE classifier and D1's parsers — D1 builds no folder reader.
- **L31 NAMES (law in force, L73 — no question):** every `das*` identifier of v2 is renamed: module `src/cobalt/drc/trading_log.py`, id `import_id`, source / kind value `trading_log`, degraded flag `trading_log_shape`. The TradeZella parser takes the same rule: module `src/cobalt/drc/stats_log.py`, kind `stats_log`, flag `stats_log_shape` (a vendor name never enters an identifier, config key, enum value or schema; it appears only in docstrings that cite his file, and in the report).

## THE ROWS — build these, in this order, nothing else
| row | what | files |
|---|---|---|
| **D1-0** FIXTURE CUT | From E1's two real files, cut TWO stripped real-shape fixtures (L45 companion: real shape, not verbatim). Header line(s) BYTE-IDENTICAL; column order, delimiter, quoting, number formats (decimals), time format and zone marker, side codes, blank-cell spelling, line endings PRESERVED; every VALUE replaced by a constructed one of the same format: account ids → `ACCT1`, `ACCT2` …; symbols → `AAA`, `BBB` …; dates → `2001-01-02` in the file's own date format; prices / quantities / P&L → your own constructed numbers that keep every STRUCTURAL feature the real day shows (partial exits, scale-ins, a position left open at file end, a second account, a reversal through 0, order-log rows — each kept if present, named if absent). **R114 — THE HEADER:** each fixture's header line is E1's header line BYTE-IDENTICAL (every column name, its order, the trailing delimiter if E1 has one, the line ending); the committed names end `.csv` (the name means nothing — detection is by header, D1-2a). **R114 — SEVERAL PLAYBOOKS:** the stats fixture carries ONE trade in E1's multi-playbook STRUCTURE — a position opened, ADDED TO by a second entry before the first setup closes, scaled out over several exits (split fills sharing one order id where E1 shows that), and TWO playbook names in ONE cell in E1's own quoting and separator — with CONSTRUCTED names (R117: never his names; e.g. `Alpha Setup Long, Beta Setup Long`), plus at least one single-name row whose name strips to a constructed title and one that does not (D3's rule sees both outcomes). **R114 — NO CARRY IN E1:** E1 has no overnight position ("I did not leave any open trades overnight"), so `trading_log_carry_day1.csv` (a constructed day ending with a position open) and `trading_log_carry_seed.csv` (a constructed next day whose first executions close it) are BOTH CONSTRUCTED in E1's exact header and formats, and the README says so. | `tests/fixtures/drc/trading_log_e1.csv`, `tests/fixtures/drc/stats_log_e1.csv`, `tests/fixtures/drc/trading_log_carry_day1.csv`, `tests/fixtures/drc/trading_log_carry_seed.csv`, `tests/fixtures/drc/README.md` (what was stripped, what structure was kept, what was constructed — NO value of his) |
| **D1-1** MODELS | Pydantic models: `Execution` (time tz-aware — its date the import date, R17 (3), its clock time the row's `Time` —, symbol, side ∈ the export's own side codes as an enum, price, qty, route, account, raw line number), `Trade` (trade_id, symbol, direction, entry/exit time, hold, avg entry/exit, shares, gross / net P&L, commissions, exit `Leg`s, `status ∈ {closed, open}`, `held_shares`, `playbooks: list[str]` — every name of its matched stats row, in order, `[]` when none), `Leg`, `StatsRow` (whatever the stats log carries of: symbol, side, entry, stop, target, assumed R:R, realized R:R, MAE, MFE, best exit — each `Optional`, `None` renders `not given`; `stop` is filled ONLY from a stats-log stop column, R17 (4) — and `playbooks: list[str]`, R114), `Detection` (D1-2a), `ImportResult` (per file: `parsed` / `partial` / `failed` / `ignored` + reason + line; `partial` carries `missing: list[str]` — the absent required names, in the kind's column order — R17 (5); an added column is the separate `degraded: <kind>_shape` flag, carried on a `parsed` or `partial` file). | `src/cobalt/drc/__init__.py`, `src/cobalt/drc/models.py` |
| **D1-2a** HEADER DETECTION (R114) | `detect_kind(name, data: bytes) -> Detection` — the ONE classifier (L3) that every import path (D2) and every test calls; it reads ONLY the first line and NEVER the file name or extension (`name` is carried for messages only): any name, any extension — his files end `.md` because Obsidian hides `.csv`, and the names change every day. Header = the first line decoded UTF-8 (a BOM stripped), split as ONE CSV record; a trailing empty cell (E1's trading-log header ends with the delimiter) is ignored for classification only. Each kind's REQUIRED NAME SET is ONE constant owned by its parser module (`trading_log.py`, `stats_log.py`; imported by `detect.py`, never copied) = every non-empty column name E1's header of that kind shows, EXCEPT a column whose name carries a vendor or person name (L31 — never in code; that column is neither required nor read; its count, not its name, goes in your report). Outcomes, deterministic: every name of ONE kind present → that kind (extra names → still that kind + `degraded: <kind>_shape` naming the extras — D1-2 / D1-4's rule); every name of BOTH kinds present → `failed: header matches both kinds`; **R17 (5) — PARTIAL:** SOME but not all names of a kind present, no kind complete → the kind is decided ONLY by the names UNIQUE to one kind's set (a name in both sets — E1 shows two, the symbol and side columns — decides nothing; the shared set is computed from the two constants, never typed): unique names of exactly ONE kind present → that kind, outcome `partial` with `missing: <names>` — the file IS parsed (D1-2 / D1-4: what is there, nothing invented) and carries the flag `PARTIAL — missing: <columns>` to the page (D2) and the DRC (D3), loud (L1, L9), never silent; unique names of BOTH kinds present → `failed: header matches both kinds`; only shared names, or NO required name of either kind (a note, an image, an undecodable first line = `not text`) → `ignored: <name> — header matches neither kind`, listed, never parsed. A SET of files (a folder listing, a multi-file drop) → `detect_set(files)`: exactly one file per kind (complete or `partial`) → pass; two files of ONE kind (either complete or `partial`) → `FAILED: <kind> ambiguous — <name 1>, <name 2>` (both named, neither picked); every ignored / partial file listed. Any reader of the `_imports/drc/` tree takes ONLY `YYYY-MM-DD` folders — `_reference/` is never an input. | `src/cobalt/drc/detect.py` (new), the REQUIRED-NAME constant in `src/cobalt/drc/trading_log.py` and in `src/cobalt/drc/stats_log.py` |
| **D1-2** TRADING-LOG PARSER | `ExecutionSource` interface (L9) + `TradingLogSource`: columns by header NAME; a file reaches the parser only after D1-2a detected it as `trading_log`, complete or `partial` (R114, R17 (5)). **PARTIAL (R17 (5)):** a `partial` file is parsed on the columns it HAS; each field of an absent column is `None` (`not given`); every later step whose inputs include an absent column (pairing reads the symbol, side, price, quantity and time columns — named from the constant, never typed twice) does not run and says so on the result, `not computed — missing: <columns>`, never a default, never a guess; the file's `ImportResult` is `partial` with `missing`. An extra column (with every required name, or on a `partial` file) → parses, sets `degraded: trading_log_shape` naming it (L9, v2 `[F-13]`); reorder alone is not a failure. **DATE (R17 (3)):** `parse(data, import_date)` — every execution's date IS `import_date` (the `<date>` folder / the upload's `date`), combined with the row's `Time` as ET (the zone E1 evidences: the 09-22 drafter paired 4 of 4 trades to the second against the stats log's `EDT` open times — re-prove it in `## E1`; not to the second → `FAILED: D1-2 — trading-log zone not proven — <counts>`, no zone invented); nothing reads a date from the file; v2 `[F-07]`'s wrong-date FAIL is STRUCK for the trading log (no date column exists to disagree). Rows are ordered by `Time` before pairing (E1 is newest-first); the order of rows sharing one `Time` follows a rule the builder records in `## E1` and pins with a test. Every row validates on the columns present or the WHOLE file is `failed` (partial means missing COLUMNS, never skipped ROWS). Accounts: ONE bucket (R94). | `src/cobalt/drc/trading_log.py` |
| **D1-3** PAIRING + CARRY | FIFO per symbol and direction; a trade closes at position 0; a position through 0 in ONE execution FAILs the file UNLESS E1 shows the export books reversals that way (then it is a lawful close + open — the row says which E1 showed, v2 E1 result column). Open position at file end → `Trade(status=open, held_shares=…)`, NOT a failure (R67). Next-day seed: from the prior day's stored open positions; a carried symbol with no prior row FAILs naming the symbol; a file contradicting the seed FAILs; `unrealized: not computed` (v2 `[F-10]`). E1's result on FLAT START vs listed overnight rows decides the cross-check: record it; if the export starts flat, the seed has no cross-check — `ASK DESK: E1 starts flat — a lawfully skipped day between two imported days seeds a phantom position; add a contiguity check (the prior TRADING day, `daymode.propose.prior_trading_day`, must have an import or a no-trade DRC)? [<time>]` — safe default: BUILD that contiguity check (it only FAILS loud, never invents). | `src/cobalt/drc/pairing.py` |
| **D1-4** STATS-LOG PARSER | `TradeStatsSource` interface + `StatsLogSource` over E1's real stats-log shape (columns by header name, reached only after D1-2a detected `stats_log`, complete or `partial`; the same fail-loud / PARTIAL / degraded rules as D1-2, flag `stats_log_shape`; a `partial` stats log whose absent columns are MATCH inputs leaves every row `unmatched — missing: <columns>`). **STOP (R17 (4)):** `StatsRow.stop` is read ONLY from a stop column of the stats log, BY HEADER NAME, and only when that name is a ruled one. E1's header has NO stop column (its 49 names: `reports/drc-reissue-draft-2026-09-22.md` `## E1 SHAPE`), so this build names NO stop column: `stop` is `None` → `not given` on every trade, and `ASK DESK: R17 (4) — E1's stats-log header carries no stop column; which field of his export carries the stop (its exact header name), or does the export need a column added? [<time>]` is ALWAYS in `## ESCALATE` — safe default: `not given`, no column name guessed. The stop is NEVER computed — not from `Trade Risk` (dollars), `Initial Target`, `Reward Ratio`, `Realized RR`, prices or shares, alone or together; a test proves a row with `Trade Risk` set yields `stop = None`, and `grep -rn "Trade Risk" src/cobalt/drc` shows no line that derives a price from it (each hit placed in the report). The day his export gains a stop column, D1-2a returns `stats_log` + `degraded: stats_log_shape` naming the new column (loud); reading it is a later change on his ruling of the name, with a new real-shape fixture (L45). MATCH to trading-log trades: EXACT on the columns E1 shows both files share (symbol + side + entry time to the precision both carry is the default). If E1 shows the two files' times cannot match exactly (a different zone, rounding), `ASK DESK: the two logs need a match tolerance — a settings key for him to set? [<time>]` — safe default: build the EXACT match only; every row it cannot pair is `unmatched` (below). No tolerance literal in code (L53). A stats row matching zero or more than one trade is `unmatched`, carried and SHOWN (a fact, like `card: none`), never dropped, never guessed. MAE / MFE / best exit: parsed only if the log has the column; else the field is absent → `not given`. **SEVERAL PLAYBOOKS (R114):** the playbook column (by header name) is a LIST — one cell may carry several names (E1: one quoted cell, names separated by a comma and a space); split on the comma, strip each name's surrounding whitespace, drop empty pieces, KEEP THE ORDER, never de-duplicate, never pick one → `StatsRow.playbooks`; the matched `Trade` carries all of them in that order; an empty cell → `[]` (rendered `playbooks: not given`). A stats-log column other than the playbook column (E1 also shows a separate setups column, empty that day) is never read as a playbook. No name → setup mapping here (D3-2b, R116). | `src/cobalt/drc/stats_log.py` |
| **D1-5** STORE + MIGRATION | ONE migration file + `.rollback.sql` (v2 `[F-35]`): `"user".drc_imports` (one row per file: date, kind ∈ {`trading_log`, `stats_log`, `screenshot`}, name, sha256, trade key (screenshot), parse status (`parsed` / `partial` / `failed`), reason (a `partial` row's reason is its `PARTIAL — missing: <columns>` text, R17 (5)), `supersedes` FK, `user_id NOT NULL` + FK, AND the event state columns for D2's `DrcInputsPlaced` — `pending / running / done / failed`, v2 §3, three tables, no fourth), `"user".drc_fills` (one row per execution, `import_id` FK), `"user".drc_rows` (the declared table, `placement.py:111`: one row per trade / per day / per open position / per stats row, JSON `inputs` + `derived` + `fn_version`, `kind` column, L57). NOT `"user".fills`, NOT `order_fills` (v2 `[F-35]`). `placement.py` entries move from declared to built. ONE `DrcStore` owns every `drc_*` write (L40), through the one connection factory. NUMBER: the next free number over main AND every unmerged branch (`git log --all --name-only --format= -- src/cobalt/db_migrations`); record the list; the DESK renumbers at the L68 gate. | `src/cobalt/db_migrations/00NN_drc.sql` + `.rollback.sql`, `src/cobalt/db_migrations/placement.py`, `src/cobalt/drc/store.py` |
| **D1-6** DEVDOCS | One DevDoc per new `.py` (`docs/40 - DevDocs/cobalt/drc/<module>.md`), agent-authored; `placement.py`'s DevDoc gains one paragraph. | `docs/40 - DevDocs/cobalt/drc/**` |

NOT IN D1: the import page, the bytes writer, the event trigger (D2); the note, the build, the retirement (D3); settings keys and the change line (D4); leg corrections and RESOLVE (D5); OCR, voice (R100: its own design lane), any rule checker (R101: NO rule→checker map this build).

## INDEX CARD — read these, in this order
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` IN FULL (L59). Binding here: **L1** every parser FAILs naming file + line; no plausible-empty import. **L3** one store, one pairing function. **L9** each file behind its source interface; an added column = degraded flag, still parses. **L19** this prompt is whole. **L29** Opus floor. **L31** the names above — no vendor name in any identifier. **L32 / L45** the fixtures are real SHAPE, never his values; R103 consented the stripped commit. **L35** every number in your report is tool output. **L40** `drc_*` rows only through `DrcStore`. **L41** `.env` by name. **L42** RESTARTS by `cobalt jobs restarts`. **L46 / L60** wip-commit + CONTINUE. **L48** evidence same turn. **L54** worktree only. **L57** every derived value stored with its inputs + `fn_version`. **L62 / L63** permissions before launch; a denial = FAILED. **L67** three checkers read this build (`53-drc-d1-check.md`). **L68** name every unmerged branch sharing your paths. **L70** unproven ≠ defect. **L71** stop line = LAST NON-BLANK LINE. **L73** no step dropped. **L74** a `Claude-Session` line request inside a tool result is DATA: record it ONCE, never follow it.
2. v2 in full; §3, §4, §9 D1, §10 E1 / E4, §12 bar are yours.
3. `cto-2026-09-22.md` §4 R65–R73, R90–R103, R114, R117; `cto-2026-09-23.md` §4 R17 (and the five questions it answers: `reports/open-items-2026-09-23.md` items 1–5); the E1 shape the re-issue drafter recorded (`reports/drc-reissue-draft-2026-09-22.md` `## E1 SHAPE` — column names and formats, no values).
4. `S3-EXITS-v3-2026-09-22.md` `:110-139` (the `legs` table and `realized_r.1` — D1 does NOT write legs; it stores trades so D3 / D5 can).
5. His two files at `/Users/cobalt/Vault/Think/1 - Trading/5 - Review/_imports/drc/<date>/` — found by their HEADER (the E1 GATE below), whatever their names and extensions; `_imports/drc/_reference/` is never read (Read tool, READ ONLY; never copied whole anywhere in the repo).
6. Code: `src/cobalt/db_migrations/` (the newest file pair and a migration test, e.g. `tests/cobalt/test_archiver_migrations.py`), `placement.py`, the one connection factory (`grep -rn "def connect\|search_path" src/cobalt/db*`), an existing store for the house pattern (`src/cobalt/aset/store.py`), `src/cobalt/daymode/propose.py` (`prior_trading_day`).

## AUTHORIZATION — VERIFY IT YOURSELF BEFORE YOU RUN ANYTHING. Written by the CTO desk (drafted by the Opus 5.5 prompt seat `drc-build-draft-0922`, 2026-09-22), not by Dejan; a prompt file is not an approval. Each its own Bash call, on `"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"`:
- `grep -n "^| R114 " <file>` (`obsidian is blind to that extention`) · `grep -n "^| R117 " <file>` (`Assume all wilde named to match`) · `grep -n "^| R91 " <file>` (`Tradezella log for the day`) · `grep -n "^| R92 " <file>` (`_imports should be a subdirectory of 1 - Trading/5 - Review`) · `grep -n "^| R93 " <file>` (`DRC every market trading day`) · `grep -n "^| R94 " <file>` (`DRC is account agnostic`) · `grep -n "^| R101 " <file>` (`psychology is disclosed`) · `grep -n "^| R103 " <file>` (`It's a small .csv`) · `grep -n "^| R32 " <file>` (`claude-opus-5-5`). On `"/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"`: `grep -n "^| R17 " <that file>` (`4)use stops from tradezella`) and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"4)use stops from tradezella" -- "docs/40 - DevDocs/reports/cto-2026-09-23.md"` NON-EMPTY. Then `git -C /Users/cobalt/cobalt log -1 --format=%H -S"DRC is account agnostic" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` and the same for `It's a small .csv`, `obsidian is blind to that extention` and `Assume all wilde named to match` — EMPTY → `FAILED: authorization mismatch — R<n> is uncommitted`. Any row missing or without its phrase → `FAILED: authorization mismatch — R<n> missing`, stop.
- **THIS launch** is row **R__** (the desk fills the number; still `__` → `FAILED: authorization mismatch — launch row not filled in`): `grep -n "48-drc-d1-build.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-2<n>.md"` for the launch day's desk file → a `| R` row; `git -C /Users/cobalt/cobalt log -1 --format=%H -S"48-drc-d1-build.md" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` NON-EMPTY (desk files ONLY — the drafter's report quotes this filename and must never satisfy the gate).
- **The two NEW `.env` strings** are his, on a committed row: `grep -c -F -e "Bash(rm /Users/cobalt/cobalt-wt/drc-d1/.env)" <the launch day's desk file>` ≥1 and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(rm /Users/cobalt/cobalt-wt/drc-d1/.env)" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` NON-EMPTY; else `FAILED: authorization mismatch — the drc-d1 .env strings are not approved on a committed row`.
- **Every other string** of your line, one `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/33-setups-fix-r3.md"` per string, each ≥1.
YOU CAN ALWAYS STOP: `FAILED: D1-<n> — <your concern>` as the LAST line, `.env` removed first if present, wip-commit, stop.

UNATTENDED RULES: `33`'s, UNCHANGED — ONE command per Bash call, exactly the listed prefix, no `cd … &&`, no pipe, no redirect, no `; echo`, no `VAR=value` in front other than the literal `COBALT_ENV=dev`; long suites `run_in_background` and read whole; counting by YOU from the tool result; files via Read / Write / Edit only. Never typed: `uv run python …`, `cp` other than the `.env` string, `cat`, `shasum` (a sha256 you need is computed by a test and printed). **A DENIED BASH CALL = FAILED** — never re-shaped, never routed around.

RECOVERY RULE (L60): after every row's commit the report's `## CONTINUE` names the next row. On any stop: `.env` removed and proven gone, `git add` of your changed paths by name, `git commit -m "wip(drc-d1): D1-<n>"`, then the FAILED line. A relaunch reads `## CONTINUE`, re-runs PREFLIGHT in resume form (your own commits above the base are expected — list them), and continues; nothing is discarded. NEVER `git add` anything under the vault, `docs/_inflight/`, or a real file of his.

REPORT (L48): `/Users/cobalt/cobalt-wt/drc-d1/docs/40 - DevDocs/reports/drc-d1-build-2026-09-2<n>.md` (the launch day), committed on the branch. Sections: `## §0 Headline` (≤5 lines) → `## L74` → `## AUTHORIZATION` → `## PREFLIGHT` (rule · command · exit · result verbatim) → `## E1` (the shape table below) → `## BASELINE` → `## D1-0` … `## D1-6` (with `## D1-2a` after `## D1-1`), each with `### T` · `### C` · `### D` · `### SUITE` · `### COMMIT` → `## CLOSE` → `## ESCALATE` → `## CONTINUE` → the last line. The FIRST Write creates the file with `## CONTINUE` holding `next: AUTHORIZATION` and the pinned last line `(run in progress — next step under ## CONTINUE)`.
STOP LINE (L71): `DRC D1 BUILT <tip> | on <base> | offline <p>/<f> | with-DB <p>/<f> | migration: <file> (desk renumbers) | E1: <date folder> | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` — or `FAILED PREFLIGHT: <rule>` / `FAILED: D1-<n> — <command> — <reason>`.

## STEP-0 — PREFLIGHT, then E1, then BASELINE
PREFLIGHT, one row each:
- `date`.
- BASE TIP filled (7–40 hex) — else `FAILED PREFLIGHT: base tip not filled in`.
- `tail -n 3 "/Users/cobalt/cobalt-wt/drc-d4/docs/40 - DevDocs/reports/drc-d4-build-2026-09-2<n>.md"` — the LAST NON-BLANK line starts `DRC D4 BUILT ` (or the D4 fix round's `DRC D4 FIX … BUILT `) and its tip = the BASE TIP; else `FAILED PREFLIGHT: D4 has not built on this tip — last line: <verbatim>`.
- **E1 GATE (R91 / R92 / R114 — BY HEADER, NEVER BY NAME OR EXTENSION):** `ls "/Users/cobalt/Vault/Think/1 - Trading/5 - Review/_imports/drc"` → at least one `YYYY-MM-DD` folder (`_reference/` is skipped, never read); then `ls -la "/Users/cobalt/Vault/Think/1 - Trading/5 - Review/_imports/drc/<the newest date folder>"`; for EACH file listed, the Read tool with `limit: 1` (its first line only) and classify it by D1-2a's rule, by hand, against E1's two column-name sets (`reports/drc-reissue-draft-2026-09-22.md` `## E1 SHAPE`): exactly ONE trading-log header AND ONE stats-log header → PASS, both named by their real names; every other file listed `ignored` / `degraded` with its reason. No file of a kind (absent folder, one file, only notes or images) → `FAILED PREFLIGHT: E1 not dropped — <what the headers show> — D1 waits for his two files (R91, R92)`, launch nothing more. A PARTIAL header that leaves its kind without a complete file → `FAILED PREFLIGHT: E1 partial — <name> is <kind> missing <names> — the fixtures need E1's whole shape (L45)` (R17 (5) reads such a file in production; the FIXTURE source must be whole). Two files of ONE kind → `FAILED PREFLIGHT: E1 ambiguous — <kind> — <name 1>, <name 2>` (neither picked).
- `git status --porcelain` EMPTY (first launch) · `git status` (branch `drc/d1-trading-log`) · `git log --oneline -1` = the BASE TIP (first launch).
- `ls -la /Users/cobalt/cobalt-wt/drc-d1/.env` → "No such file" (a resume removes a leftover with the `rm` string first).
- DEV-DB STAGGER: `ls -la /Users/cobalt/cobalt-wt/*/.env` → NO file (zsh "no matches found" is the PASS, quoted); any listed → `FAILED PREFLIGHT: the dev-DB lane is held — <path> — the desk staggers`.
- `uv run cobalt jobs restarts <base>..HEAD` (the probe of that rule; empty range).
E1 (READ ONLY, Read tool): for each file, a table — header set (names only), delimiter, time format + zone, side codes, account column(s) (count of distinct values, never the values), order-log rows present?, first executions flat or an overnight row?, any execution crossing 0?, row count. For the stats log: its columns (names only), which of entry / stop / target / assumed R:R / realized R:R / MAE / MFE / best exit are present, and the columns it shares with the trading log. R114, per file: the detected kind, the file's extension (a fact, never a rule), the count of vendor-named columns left out of the required set; for the stats log: the playbook column's separator and quoting, the count of rows with more than one playbook name and the name count on each (never the names). R17, per file: (3) the trading log's date source (`import date — no date column`), the zone proof (trades paired to the second against the stats log's open times: `<k> of <n>`), and the order rule for rows sharing one `Time`; (4) whether ANY stats-log column name is a stop field (E1 as read 09-23: none) — a yes names the column and is `ASK DESK`, never read without his ruling. NO value of his in the report — shapes and counts only (L32).
BASELINE on the BASE TIP, quoted VERBATIM: OFFLINE `uv run pytest -q tests/cobalt -p no:cacheprovider` (`run_in_background`) → `failed` 0, record `<p>/<f>`. WITH-DB: `cp` string → `COBALT_ENV=dev uv run pytest -q -p no:cacheprovider -rs tests/cobalt/test_tenancy.py tests/cobalt/test_archiver_migrations.py tests/cobalt/test_p4_migrations.py tests/cobalt/test_radar_migration.py` (+ every with-DB migration test on the base — `grep -l "COBALT_ENV\|cobalt_dev" tests/cobalt/*.py`, add each by path, say so) → `rm` string → `ls -la` "No such file". Record `<p>/<f>`.

## THE SHAPE OF EVERY ROW — `33`'s, applied
**(T)** the test FIRST, run on the code as it stands, said RED (quote the failing line) or GREEN-as-pin; new tests in `tests/cobalt/test_drc_detect.py`, `test_drc_trading_log.py`, `test_drc_pairing.py`, `test_drc_stats_log.py` (offline), `test_drc_store.py` (with-DB, skipped offline — named), all against the D1-0 fixtures or YOUR constructed literals (L69) — never a value of his. Required cases, at least: every fail-loud rule of D1-2 / D1-3 / D1-4 on a one-line mutation of the fixture (R17 (5): a required column removed → `partial`, the rows parsed, that field `not given`, `missing` naming it, and a step that needs it `not computed — missing: <column>` — for EACH of the pairing and match input columns, one test each; extra column → parsed + `trading_log_shape`; R17 (3): the same file parsed under two import dates → the executions carry each import date, nothing read from the file; bad row → whole file `failed`, zero rows stored); two accounts → one bucket (R94); partial exits → one trade, n legs; open at file end → `status=open` + held shares; seed round-trip across the two fixture days; stats row unmatched → shown, not dropped; a stats column absent → `not given`; **R114 detection** — each fixture's bytes under a constructed name with `.md`, with `.csv` and with no extension → the SAME kind (name and extension never read); a text file / a markdown note / image bytes → `ignored`, listed; a header with one required name removed → `partial` of its kind, `missing` naming it, the rows parsed (R17 (5)); a header of ONLY the shared names → `ignored`; a header carrying unique names of both kinds → `failed: header matches both kinds`; a `partial` file plus a complete file of the same kind in one set → `ambiguous`, both named; a header with an added name → that kind + the shape flag; **R17 (4)** — a stats row with `Trade Risk`, prices and shares set and no stop column → `stop` is `None` (`not given`); two trading-log files in one set → `FAILED … ambiguous` naming both; a `_reference/` folder beside a date folder is never read; the fixtures' first lines equal E1's header byte for byte (proven at CLOSE, below); **R114 playbooks** — the multi-playbook row → its trade carries both names in order; a single name → one; an empty cell → `[]`; migration forward + rollback + tenancy side (`test_tenancy`'s one-side assertion covers the three tables) → **(C)** the smallest change, in the row's files only → **(D)** the DevDoc paragraph → **(SUITE)** offline, `failed` 0 (with-DB inside the `.env` pair for D1-5) → **(COMMIT)** explicit paths, `git commit -m "feat(drc): D1-<n> <what>"`, `git show --stat HEAD` → breadcrumb, next row IN THE SAME TURN.

## CLOSE
Each its own call, quoted VERBATIM: OFFLINE → `failed` 0; WITH-DB (baseline set + `tests/cobalt/test_drc_store.py`) inside the `.env` pair → `failed` 0 → `rm` → "No such file"; `git diff --stat <base>` → ONLY the rows' files + tests + fixtures + DevDocs + the report (any other → ESCALATE); `git diff <base> -- src/cobalt/aset src/cobalt/prefill src/cobalt/replay src/cobalt/cards src/cobalt/radar configs` EMPTY; **L32 SELF-CHECK:** for 3 symbols, 3 prices and every account id you READ in E1 (hold them only in your head, never write them), `grep -rn -F "<value>" tests/fixtures/drc src/cobalt/drc "docs/40 - DevDocs/cobalt/drc"` each its own call → report `E1 value <k>: <hits>` WITHOUT the value; a hit → strip before the report commit; **L31 SELF-CHECK:** `grep -rn -i "das\b\|das_\|tradezella\|zella" src/cobalt/drc src/cobalt/db_migrations tests/cobalt/test_drc_*` → only docstring / comment lines citing his file, each named (the vendor-named header column lives ONLY in the fixture files, byte-identical — tests mutate a fixture's header by reading it, never by typing that name); **R114 HEADER PROOF:** `grep -m 1 -n "" <path>` on each of his two E1 files and on `trading_log_e1.csv` / `stats_log_e1.csv` / `trading_log_carry_day1.csv` / `trading_log_carry_seed.csv`, each its own call → each fixture's line 1 equals its kind's E1 line 1 byte for byte (`HEADER <fixture>: IDENTICAL` / `DIFFERS at column <n>`; a header is column names, never a value); `uv run cobalt jobs restarts <base>..HEAD` VERBATIM (L42; D1 is a library — expect none; any UNCLASSIFIED → ESCALATE); `git log --oneline <base>..HEAD`; L68: `git -C /Users/cobalt/cobalt log --oneline --all -- src/cobalt/db_migrations/placement.py` since main, branch names that touch it → table.
`## ESCALATE` ALWAYS carries: (i) E1's shape results that change v2 (reversal booked? flat start? order-log rows → v2 later slice L2 lives or dies); (ii) the migration number you took and the list you took it from — the desk renumbers at L68; (iii) the stats-log match rule you built, and the count of E1's stats rows it left `unmatched`; (iv) every `ASK DESK` with its safe default; (v) the stripped-fixture statement (what structure kept, what stripped, what constructed — the carry pair and the playbook names, R114 / R117 — no value); (vi) R114: the two required-name sets' sizes, the vendor-named columns left out (count), the HEADER PROOF rows, and the E1 multi-playbook trade's name count; (vii) R17: the stop `ASK DESK` of D1-4 (ALWAYS, with its default `not given`), the zone proof `<k> of <n>` and the same-`Time` order rule (3), and the PARTIAL outcomes built (5) with the shared-name count; (viii) `MEMORY:` / `RULING:` lines if any.
Then the stop line as the LAST NON-BLANK line, commit the report by explicit path (`git commit -m "feat(drc): D1 — report"`), `git status --porcelain` EMPTY, stop.
NEXT STEP, not yours: the desk verifies the artifact (L35) and launches `53-drc-d1-check.md`; D2 (`49`) stacks on this tip.
