DERIVE ROW: __DERIVE_ROW__

MODEL: `__DERIVE_MODEL__` — the DERIVE SEAT his row names (`cto-2026-09-2<n>.md` §4, the row on line 1 of this file: for the tribunal of `DRC-VOICE-PROPOSAL-2026-09-22.md`, `derive seat: <model id>` — his per-seat answer under his R76 ("I tell you for each Fable seat if Opus 5.5 runs it or Fable runs it. You ask me."); NOT YET RULED at drafting: R106 (19:13 ET) answered the Fable seat only — "Derive seat asked separately"). THE DESK FILLS, before launch, EXACTLY THREE SPANS from that row and nothing else (L19: that fill is this drafted re-issue): line 1's row placeholder with `R<nn>`, and this line's two model placeholders — the one opening this MODEL line and the one after `--model` in the launch line — with the model id after `derive seat:`. This is the Anthropic house's SECOND JOB on this tribunal, as on the bars, setups, float-handicap, stale-score, S3-exits and DRC tribunals; you derive the version the round-1 rulings produce; you build, launch and commit nothing · SEAT: `voice-tribunal-derive-0922`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `59`; the Fable seat `60`). YOUR POSITION, STATED: the Anthropic house ruled round 1 through the Fable seat (`60`, on `claude-opus-5-5` by R106); whichever model the row names, you are that house in a fresh session — you HOLD A SIDE, so you recommend nothing to him (L37) and the Fable seat's round-1 wordings pass the same filter as every house's. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/61-voice-tribunal-derive.md' and follow it exactly." --model __DERIVE_MODEL__ --permission-mode auto --remote-control voice-tribunal-derive-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `74`, `75`, `37`, `42`, 09-20 R13 — nothing new) · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic, on the model the row names (the week's meter is HIGH, his R58) — honest estimate: the hub report `59` ≈ 20–30k tokens, the raw rulings of the houses that ruled ≈ 20–30k (17 items each), the Fable seat's round-1 report ≈ 10–14k, the proposal ≈ 5k (read whole — v2 is the proposal amended), its author's report ≈ 1k, the rulings R66 / R90 / R92 / R93 / R99–R101 / R106 + the derive row ≈ 3k, LAWS.md ≈ 20.5k (82,174 B measured at drafting), code and DRC build prompts re-opened only where a fold turns on it ≈ 5–10k; v2 ≈ 7–10k tokens of output (the proposal whole + the folds) plus the report ≈ 5–8k; peak context ≈ 100–130k — read the raw rulings item by item, not all at once, if the peak nears your window. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v2 of the DRC PER-TRADE VOICE CAPTURE DESIGN from the proposal and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: `OFF-LADDER — ruled cto-2026-09-22.md R100`. LAW STEP (L67): proposal DONE → round 1 DONE (Grok, Gemini via hub `59`; Astra `METER — proceed on three`, R13; the Fable seat via `60`) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (empty → the tribunal closes and v2 is the FINAL's input) → build → ≥3 checkers → deploy. L1, L3, L9, L15, L23, L25, L28, L32, L40, L45 and L57 are the bar v2 must meet, and the proposal's §9 L52 finding is re-answered for v2. DO NOT STOP until the report's last line is `VOICE DERIVED v2 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The first line of this file names the row of `cto-2026-09-22.md` (or, if he rules on the next day, `cto-2026-09-23.md`) `## §4 Rulings` that carries his answer on the derive seat for THIS tribunal. The row must carry, as literal text: `DRC-VOICE-PROPOSAL-2026-09-22.md` and `derive seat: <model id>`. Each its own Bash call: `grep -c -E "__DERIVE_(ROW|MODEL)__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/61-voice-tribunal-derive.md"` (the three fill spans; this sentence's own text does not match the pattern) — **anything but 0** → `FAILED: authorization — the derive seat is not filled (a placeholder is still in this file)`, stop · `grep -c -x -E "DERIVE ROW: R[0-9]+" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/61-voice-tribunal-derive.md"` — must print **1**; read `<nn>` from line 1 · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` (exactly ONE `R<nn>` row carrying BOTH literals above, with his words in quotes — a DESK row with "NO WORDS OF HIS" does not count; any literal missing, or two such rows → `FAILED: authorization mismatch — row R<nn> does not name the derive seat for this tribunal`; a missing `cto-2026-09-23.md` is recorded, not fatal) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md" "docs/40 - DevDocs/reports/cto-2026-09-23.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` in that row, the `--model` value on this file's launch line, and the model id your own system prompt states you run as must be the SAME id — else `FAILED: seat mismatch — row R<nn> names <id>; this file / this session runs <id>`, stop. **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tribunal-2026-09-22.md"` (the LAST NON-BLANK line must start `VOICE TRIBUNAL R1 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/voice-tribunal-2026-09-22.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tribunal-fable-r1-2026-09-22.md"` (must start `VOICE TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Fable round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"VOICE TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/voice-tribunal-fable-r1-2026-09-22.md"` (EMPTY → `FAILED: refused — the Fable round-1 stop line is not committed on main`) · **A HOUSE MUST HAVE RULED:** the hub's stop line shows `houses that ruled: 0 of 3`, or neither `grok:` nor `gemini:` in it carries a `TRIBUNAL R1:` line → `FAILED: no house ruled round 1 — the desk relaunches 59 or brings it to him` (the Anthropic seat alone never settles an item: you would be deriving from your own house's ruling, L37) · **ASTRA (R13):** the hub's `astra:` field is `METER — proceed on three` or `SKIPPED — R13 (probe UP, recorded)` — RECORDED, not a refusal: derive from the seats that ruled, mark every question only Astra would have answered `ASTRA PENDING (R13)`, mark v2's header `ASTRA PENDING (R13)`, and name in §0 that Astra reads this v2 when its meter returns (Sat 09-26). Gemini or Grok alone not having ruled does NOT refuse the derive: the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L2 · L3 · L9 · L10 · L15** (a new dependency passes four gates) **· L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L18 · L23 · L25** (cloud fallback is an exception handler, never a route) **· L28 · L29 · L31 · L32** (v2 and your report are COMMITTED: no user data — no audio, no transcript, no line of his notes, no value of the coach spec, no ticker, price or P&L of his) **· L35 · L36 · L37** (you derive; a SPLIT between houses is carried, never settled by vote; a seat that holds a side recommends nothing to him — you hold one) **· L40 · L42 · L43 / L66 · L45 · L52 · L53** (every threshold, window, limit, clip length, retention period, key value and N is his) **· L57 · L67 · L68** (migration NUMBERS are the gate's) **· L70 · L71 · L72** (P-b: the seam with the DRC build is ONE document both build prompts cite) **· L73 · L74**. CLAUDE.md's absolute boundary: nothing in v2 reads, writes or infers from his trading platform; the trading PC is a browser tab at most — no install, no agent.
2. THE ROUND-1 RULINGS, whole: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tribunal-2026-09-22.md` — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Fable round-1 claims, file-checked` (the FC rows, or its one line saying the Fable report was not done at collate), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/voice-tribunal/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line (the raw file is what "verbatim" means, but you never copy a user-data span out of it) · the Fable seat's round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tribunal-fable-r1-2026-09-22.md` — including its `## Self-attack` (its `WITHDRAWN:` lines bind you: a withdrawn sentence is never folded).
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/DRC-VOICE-PROPOSAL-2026-09-22.md` in full; its author's report `reports/drc-voice-propose-2026-09-22.md` (§0, Findings, Chunks, ESCALATE 1–2); `cto-2026-09-22.md` §4 rows **R92, R93, R99, R100** (`grep -n "^| R92 "` etc.) — R92 (imports in the vault at `1 - Trading/5 - Review/_imports/drc/<date>/`), R93 (a DRC every market trading day, a no-trade section), R99 (his per-trade answer lines in their own unit next to that trade's Cobalt block; a re-drop never rewrites them), R100 ("A, B as soon as possible": no voice in the DRC slice; per-trade voice capture its own design item) — and, as the proposal cites them, **R66, R90, R101, R106**; the DRC FINAL's input this design attaches to, `docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` (:62, :70, :79–82, :139, :148, :176, :224–230) and the DRC build prompts as drafted, `prompts/2026-09-22/49-drc-d2-build.md` (:7, :20–21) and `50-drc-d3-build.md` (:6, :9, :20, :25, :28).
4. Only where a fold turns on it: the real file the hub's file-check cites (code on main, migrations, configs, the lock file, the heartbeat, the device facts in `topics/devices.md` :8, :11, :12, :17, :31) — read to check, never to copy.

THE RULES OF THE DERIVE — `42`'s rules (the bars tribunal's lesson, `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding, with this tribunal's substitutions:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into v2 word for word, or it is not taken. You may choose BETWEEN seats' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check, or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **A Fable-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's `## Fable round-1 claims, file-checked` has its FC rows, they are your filter for the Fable seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the Fable report as not done at collate, every Fable claim is UNCHECKED: you may take a Fable wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure, and where you cannot settle it by reading it is an experiment, not a fold. Where the Fable seat's wording competes with a house's wording that is equally correct, the house's is taken — your own house's text is never preferred.
- **His R92, R93, R99, R100 are NEVER re-opened.** A wording that would put voice back into the DRC build slice, let a re-drop or re-build rewrite, reorder or remove a byte of his unit, store the audio or imports anywhere but R92's dir without his word (W3 is his), or drop the no-trade DRC, is NOT folded: it is carried in `## Dissents, verbatim` and named in `## ESCALATE` as `RE-OPENS A RULING (<R-row>)` — his to rule again if he chooses, never yours. A wording that changes HOW a part is built or names that its SOURCE does not hold (a source that does not exist, an experiment that gates it) IS a fold; v2 states the source the rulings leave it, or `source pending <V-E/X n>`.
- **The DRC FINAL is not rewritten here.** A seat's ruling that the DRC design (v2 + his R90–R103) or its build prompts `48`–`57` must change — a new `VaultWriter` op inside D2 / D3, a unit placement, the bytes method's shape — is stated in v2 as a SEAM (what this design needs from which DRC chunk, and when) and carried in `## ESCALATE` for the desk (the DRC build prompts are drafted from v2 + R90–R103), never folded as a change to them. v2 names the ONE seam document both builds cite (L72 P-b) as the rulings leave it.
- **Prefer the simpler mechanism.** Where two seats offer different mechanisms for one item, take the smaller one when both meet the laws above, and write the other's cost and trade-off as an OWNER ITEM (after the tribunal) — unless the two disagree on whether a mechanism is CORRECT, which is `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in v2's experiment list beside the proposal's V-E1…V-E5, placed before the chunk it gates, with the result that would change the design — never an argument in v2's text, never a defect.
- **Invent nothing.** v2 adds no mechanism, number, rule, key, column, table, route, engine, model size or chunk that neither the proposal nor a seat's ruling contains. A gap no ruling fills stays a named gap (an experiment, an owner item, or a round-2 item) — never your filler.
- **Nothing of his before build.** Every value and choice that is his — the proposal's W1–W6, every threshold, window, limit, clip length, retention period, key value and N, and every choice the seats name as his — is HIS, read by him AFTER the tribunal; nothing in v2 may make a chunk's BUILD depend on one of them (tests use constructed settings, L69); a seat that wrote one as a precondition to build is carried as an OWNER ITEM (after the tribunal) and named in `## ESCALATE`. His real clips (V-E2, L45) are INPUTS a chunk waits on, not owner values — v2 states which chunk waits on them, as the rulings leave it.
- **The author's ESCALATE 1–2** (CLAUDE.md states a voice stack as an environment fact while none is built; DRC v2 F17 says the trading PC is not on the tailnet while his 09-20 R27 says it is): v2 places each exactly as the rulings decide it — a desk record item, an owner item, or inside this design; never a v2 edit of CLAUDE.md or of the DRC v2.
- **The migration (one set, its number) and the chunk order** (V-E1…V-E5 → V1 → V2 after DRC D2 → V3 after DRC D3 → V4): v2 states what the rulings decide; the deploy order and the numbering at build are the desk's lane (L68), stated as a fact, never as your ruling.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT BUILD` that v2 does not follow goes into v2's `## Dissents, verbatim` word for word, with its house and item.
- **No user data in either file** (L32): the packet had none after the hub's REDACTION rule; if a seat's text quotes a value of his anyway, it is taken with that span replaced by `[user data: <file:line>]`, and a seat's proposed VALUE for one of his keys is recorded as `<key>: value proposed by <seat> — <file>:<line>` (a pointer, never the value) and listed under OWNER ITEMS; count every redaction.

WRITE (Write tool, TWO new files, these names WHATEVER the date of the run):
- `/Users/cobalt/cobalt/docs/30 - Design/DRC-VOICE-v2-2026-09-22.md` — the proposal, whole (§1–§10), with each amended section REPLACED in place and tagged `[F-nn]` at the amended paragraph (so a reader can diff v2 against the proposal section by section); a header naming the proposal, R92 / R93 / R99 / R100 / R106 and the derive row, the rulings of every seat that ruled (paths; Astra `METER — ASTRA PENDING (R13)`; the Fable seat's status from row R106) and the fold table's path; `## First-gate experiments (L70)` = the proposal's V-E1…V-E5 and UNPROVEN rows V19–V21 as the rulings leave them, plus every experiment the seats named that the fold takes, each before the chunk it gates, each runnable on `cobalt_dev`, in the dev vault, on the phone and desk browser, or on the clips he hands over (a production read stays a hub-run read, L41); `## Seam with the DRC build` (per the DRC-FINAL rule above); `## Dissents, verbatim`; `## L52 and the bar` — the proposal's §9 finding re-answered for v2, with L1, L3, L23, L25, L28, L57 each stated for v2. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/voice-tribunal-derive-2026-09-22.md` — §0 Headline ≤5 lines (folds, round 2 or not, owner items, Astra pending) → `## DIGEST FOR THE DESK` (≤40 lines: what v2 changed from the proposal, item by item in one line each; which seats ruled round 1 (in full / not / Astra METER); how many Fable claims were hub-checked, how many UNCHECKED; what goes to round 2 and why; the owner items in one line each; the chunks, seats and RESTARTS under v2; what waits on his clips and on DRC D2 / D3; the hours under v2 beside the DRC build and S3 C1–C4 in the 09-24 → 10-07 window, a GUESS marked as one) → `## Fold table` (`F-nn · item (T-V1–T-V10, (a)–(g), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (seat, or "proposal kept") · adopted verbatim? yes / no / not taken · why (≤25 words, citing the hub's file-check row — `Checked` row or `FC<n>` — where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`, or `RE-OPENS A RULING (<R-row>)`, or `DRC FINAL — carried as a seam`)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT BUILD`, or two seats disagree on whether a mechanism is CORRECT; each with both positions quoted and the question round 2 must answer; EMPTY = the tribunal closes) → `## OWNER ITEMS (after the tribunal)` (deduplicated, each with who raised it — the proposal's W1–W6 first; none a precondition to build) → `## FOR DEJAN` (each owner item in HIS terms — what he would see on his phone, on the card, on the `/drc` trade row, in his DRC note, when the local engine is down, at which moment — as ONE A/B question per item: A = what v2 builds by default, B = the alternative a seat named, each with the seats that hold it; NO recommendation — the Anthropic house ruled round 1 and holds a side (L37); ≤4 lines per item) → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` (every `RE-OPENS A RULING`, every DRC-FINAL change a seat asked for, every precondition-to-build, every DOES-NOT-HOLD wording a seat still presses, every `ASK DESK`) → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `VOICE DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both file paths, reads its own report first and continues from `## CONTINUE`; v2, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the derive report, L71): `VOICE DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits v2 as the FINAL's input, brings him `## FOR DEJAN` one item per message, and the build prompts are drafted (the experiments first — his real clips for V-E2 — then V1, per v2, ordered against the DRC build and the stacked branches at the desk's L68 gate); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a run after 2026-09-23 23:59 ET needs his word for `grok` / `agy` for this tribunal — R30 ends then, and R105's extension is scoped to the DRC check hubs `53`–`57`); in every case Astra reads the FINAL on Sat 09-26 (R13).
