MODEL: Sonnet 5 (`claude-sonnet-5`) — stage a packet, put it before ONE house, copy its answer, then compare; no verdict of your own (L37) · SEAT: blind-values hub `setups-blind-values-0922`, launched by the CTO desk in the background. **RE-ISSUED 2026-09-22 (L19, a full re-issue, by the drafting seat `setups-check-r4-draft-0922`) — pointed at fix round 4's tip: `72` RE-CUT the fixture on the New York wall clock and RE-COPIED the five `DEF_WRITTEN_RUBBERBAND_CUT_*` pins in `f5aaeb4`; the launch row is a NEW row `R__` (R24 was the first issue's row, never launched on, and never satisfies this file's gate).** Two bare commands, run by THE DESK after `72-setups-fix-r4.md` ended `SETUPS FIX R4 BUILT …` and the launch row **R__** and this file are committed on main: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/13-setups-blind-values.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control setups-blind-values-0922 --allowedTools "Bash(agy *)" "Bash(claude -p --model claude-opus-5-5 *)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: the launch line is the first issue's BYTE FOR BYTE (only the launch row changes). All nine allow strings, the three deny strings and the `--add-dir` triplet are PRECEDENTED — each is in `prompts/2026-09-21/66-setups-one-check.md`'s launch line byte for byte (`"Bash(agy *)"` his R39 of 09-21, extended by his R30 of 09-22 through 2026-09-23 23:59 ET; `"Bash(claude -p --model claude-opus-5-5 *)"` his R49 of 09-21 with R32's id; the read strings 09-21 `64` / `79`, 09-22 `04` / `08`); NOTHING NEW. `auto` mode: no write path, reads only (the `66` precedent) · SESSION: fresh · no DB, no `.env`, no docker, no pytest, no git write, no vault write, no memory-folder write (L58); the one house you launch is the check seat this prompt names (the `66` pattern — a check hub puts a packet before a house; you launch no hub, no worker of your own, L36) · METER: Anthropic small (you) + ONE house call (Gemini, or Opus 5.5 as the fallback) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default (fewer files staged, never more access), continue.

# BLIND EXPECTED VALUES — a house that has NOT seen the pins re-derives the RE-CUT day's formation from the bars and the definition alone (2026-09-22, re-issued at fix round 4's tip)

WHY: FINAL [F-16] (1) — *"For each per-setup fixture day, the expected side, formed bar, trigger price and stop price are written by a CHECKER house from the bars alone, before it sees the builder's output; the test asserts all four, not only `formed`."* — and [F-21] (`docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`). `12-setups-fixture-cut.md` cut one stored pool day (rubberband; hitchhiker did not close) into committed real-shape fixtures on branch `setups/seven-0921` (`/Users/cobalt/cobalt-wt/setups-c1`); fix round 4 (`72`, F4) found the cutter had kept the UTC clock across a DST boundary, RE-CUT the fixture on the New York wall clock and RE-COPIED the ENGINE's values as the five `DEF_WRITTEN_RUBBERBAND_CUT_*` constants (report `setups-fix-r4-2026-09-22.md`, stop line `SETUPS FIX R4 BUILT f5aaeb4 | on 8da261a | …`). You get a second house's values WITHOUT showing it the pins, then compare. LAW STEP: `12` → fix r4 `72` (re-cut, `f5aaeb4`) → **THIS** (and check round 3 of 3, `79`, in the same house lane — one at a time, see STAGGER) → with-DB after `68` → deploy prompt → his "approve" (L7 / L61).

## INDEX CARD (you; the house gets the packet only)
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — one line each: **L32** no value of his note enters the packet — the definition staged is the build's NEUTRAL shape (value-free literals of the build's choosing), never `4 - Strategies/*.md`; **the real stored day is never written** — in the packet, `TASK.md` and your report it is `<real day>` (the cut report's last line and its `## FOR 13` carry it: you read them, you never copy the day). **L35** every value in your table is copied from a file, verbatim. **L37** you judge nothing: MATCH / MISMATCH / UNDERIVABLE is string equality after the normalisation named below; a MISMATCH is not a verdict. **L45** the packet's bars are the committed real-shape fixture at the r4 tip, byte-identical. **L48** evidence in the report the same turn. **L62 / L63** unattended; a denial is a FAILED. **L67** the blind seat is a house OTHER than the build's author where one is up. **L70** an UNDERIVABLE is not a defect. **L71** the stop line is the last non-blank line.
2. `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md` — its LAST line and its FIRST `## FOR 13` section ONLY (the heading `## FOR 13` exactly — the later `## FOR 13 (the blind seat)` heading holds `(pending)` and is not read): the fixture paths, the neutral-shape key and `setups_shapes.py` line range, the pinned test file. **ITS CONSTANTS' LINE NUMBERS ARE STALE** (r4 moved them by +2): you never use them — STEP-3 re-locates the constants by symbol. You do NOT read its `## FIND`, `## CUT` or `## PIN` sections (they quote the engine's FORMED lines — the values the house must not see, nor you before it answers).
3. `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r4-2026-09-22.md` — its LAST line and its `## NOTE FOR THE DESK` section ONLY (it names the moved pins and their new line numbers, no value). Every other section of it (`## §0 Headline`, `## F4`, `## C3`, `## PINS MOVED`, `## ESCALATE` …) quotes the new pins: NEVER opened before the house has answered.
4. `prompts/2026-09-21/66-setups-one-check.md` — its packet rules (staging by Read → Write byte-identical, parts ≤ 38,000 B, `wc -c` against the original, the whitespace gap check), its Opus probe, and its `agy` / `claude -p` invocations: THIS file copies them.

**NEVER STAGED, NEVER OPENED BEFORE THE HOUSE HAS ANSWERED:** the pinned test file `tests/cobalt/test_setups_fixture_cut.py`, `tests/cobalt/test_rubberband_forms.py`, `tests/cobalt/test_setups_hitchhiker.py`, `tests/cobalt/test_setups_lego.py`, every other `tests/cobalt/test_setups_*.py` (`test_setups_fix_r4.py` included), the build report `setups-one-build-2026-09-21.md`, the cut report's sections other than the first `## FOR 13` and the last line, the r4 report's sections other than `## NOTE FOR THE DESK` and the last line, `66`'s and `70`'s check reports, **every CHECK PACKET folder `scratch/tribunal-bars-0920/setups-check/**` (it holds the pins in its diffs — this seat and its house are NEVER pointed at it)**, `git log -p` of any range touching `tests/cobalt/test_setups_fixture_cut.py`, and ANY line containing `DEF_WRITTEN_`. Before each staged file is written: `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED \|CUT ENGINE OUTPUT" <original>` → 0, else that file is NOT staged (ESCALATE).

REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-blind-values-2026-09-22.md` (Write tool; you commit nothing — the desk commits it). Sections: `## §0 Headline` (≤5 lines) → `## AUTHORIZATION` → `## PREFLIGHT` → `## Packet` (file · original path · bytes original · bytes staged · parts) → `## House` → `## ORDER` (the timestamp proof) → `## Comparison` → `## ESCALATE` → `## CONTINUE` → the last line. FIRST Write creates it with `## CONTINUE` = `next: AUTHORIZATION` and the last line `(run in progress — next under ## CONTINUE)`.
STOP LINE (L71 — LAST NON-BLANK LINE): `SETUPS BLIND VALUES DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: <why>`.

AUTHORIZATION — each its own Bash call; a prompt file is not an approval:
- THIS launch is row **R__** of the launch day's desk report (the desk fills the number in before launch; still `__` in the file you read → `FAILED: authorization mismatch — launch row not filled in`, stop): `grep -n "^| R__ " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` → ONE row that names `13-setups-blind-values.md` AND `f5aaeb4` (a missing desk file is recorded, not fatal, when the row is in the other; **R24 names this file too — it is the FIRST issue's row and never satisfies this gate**) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R__ |" -- "docs/40 - DevDocs/reports/cto-2026-09-2*.md"` non-empty (the desk files ONLY; else `FAILED: authorization mismatch — the launch row is uncommitted`).
- The seats: `grep -n "^| R32 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (must carry `claude -p --model claude-opus-5-5` — his 13:20 ET ruling: Opus tasks run on Opus 5.5; the Opus seat itself is 09-21 R49's, the id is R32's; the `Bash(claude -p --model claude-opus-5-5 *)` string in this launch line stands on R32, not on `66`'s line — the `66` grep below is satisfied for every OTHER string) · **THE DATE GATE for Gemini, his R30:** `grep -n -F "Bash(grok *) and Bash(agy *) through 2026-09-23" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` must print the `| R30 |` row, and `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(grok *) and Bash(agy *) through 2026-09-23" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY (either missing → Gemini is NOT a lawful house for this run: record it, the house is the Opus fallback).
- Each allow string: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/66-setups-one-check.md"` ≥1 each (the eight OTHER than the Opus string, the three deny strings, the triplet; the Opus string `"Bash(claude -p --model claude-opus-5-5 *)"` stands on R32 above — `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(claude -p --model claude-opus-5-5 *)" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY). Any 0 → `FAILED: authorization mismatch — <rule>`.

UNATTENDED RULES (`66`'s, unchanged): ONE command per Bash call, the bare listed prefix, no pipe, no redirect, no `cd … &&`, no `VAR=value` in front; files by the Write tool; reading by the Read tool. **A DENIAL IS A FAILURE:** `FAILED: <step> — <command> — <reason verbatim>`, stop. RECOVERY (L60): a relaunch first runs `ls /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind`; a staged file is not re-staged (its `wc -c` is re-proven); an existing `blind-values.md` is NEVER re-asked — you go to `## ORDER`.

## STEP-0 — PREFLIGHT (one row each: command · exit · result verbatim — the real stored day, wherever a result carries it, is written `<real day>`)
- `date` (**THE DATE GATE**: `<D>` ≤ 2026-09-23 and R30's rows above present → Gemini lawful; `<D>` later, or R30 missing → Gemini NOT lawful, the Opus fallback is the house).
- `agy --version` · `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (`66`'s probe, `run_in_background`).
- **THE R4 TIP:** `tail -n 3 "/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r4-2026-09-22.md"` → its LAST NON-BLANK line must start `SETUPS FIX R4 BUILT ` and carry `| on 8da261a |`; `<tip>` = the field after `FIX R4 BUILT` (expected `f5aaeb4`), taken FROM THAT LINE (anything else → `FAILED PREFLIGHT: fix round 4 is not built — <line verbatim>`). Then `grep -n "^## NOTE FOR THE DESK" "<same report>"` and Read that section only (offset / limit to the next `## `).
- **THE FIXTURE IS THE TIP'S:** `git -C /Users/cobalt/cobalt log --oneline -1 setups/seven-0921 -- tests/fixtures/radar/bars-setups-rubberband.real-shape.json` → must print `<tip>` (the re-cut is the fixture's latest commit) · `git -C /Users/cobalt/cobalt log --oneline <tip>..setups/seven-0921 -- tests src configs` → EMPTY (the worktree's staged sources ARE `<tip>`). Either wrong → `FAILED PREFLIGHT: the fixture is not the r4 re-cut — <output verbatim>`.
- `tail -n 1 "/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md"` → must start `SETUPS FIXTURE CUT BUILT` (else `FAILED PREFLIGHT: the cut is not built — <line>`; the real day written `<real day>`); the closed slugs = those with a day, not `none` (expected: rubberband only) · `grep -n "^## FOR 13" "<same report>"` then Read the FIRST section only (offset / limit to the next `## `).
- **THE STAGGER — the house lane (one Grok / Gemini hub at a time).** Each its own `tail -n 3` call; a file that EXISTS → its LAST NON-BLANK line must start with its done prefix or `FAILED`; anything else → `FAILED PREFLIGHT: another house hub is running (<63|79>) — the desk staggers`, launch nothing: (s1) `63` routing tribunal, `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/routing-tribunal-2026-09-22.md`, done prefix `ROUTING TRIBUNAL R1 DONE ` · (s2) `79` setups check r4, `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-check-r4-2026-09-22.md`, done prefix `SETUPS CHECK R4 DONE `. A file that DOES NOT EXIST → not running ONLY if the desk's launch row says so: `grep -n -F "<nn> is not running" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-23.md"` — a printed line that ALSO names `13-setups-blind-values.md` → continue; none → `FAILED PREFLIGHT: <nn> has no report and the desk's launch row does not say "<nn> is not running"`, launch nothing.
THE HOUSE (L67: a house other than the build's author — the build, the cut and the r4 re-cut are Anthropic-house work): **PRIMARY = Gemini** (`agy`, lawful per THE DATE GATE above; `agy --version` failing → fallback). **FALLBACK = Opus 5.5** (`claude -p`, R49 with R32's id — the code-check seat, no calendar limit; the SAME house as the builder, so its use is recorded under `## ESCALATE` as "blind but same-house"). Neither up → `FAILED PREFLIGHT: no blind house — <each line verbatim>`.

## STEP-1 — STAGE (`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind/` — the first Write creates it; NO `mkdir`)
Per closed slug, by Read → Write byte-identical, each proven by `wc -c` (original vs staged; parts sum to the original), `66`'s whitespace-gap check first:
 (a) the committed fixture files named in `## FOR 13` (`tests/fixtures/radar/bars-setups-<slug>.real-shape.json`, `daily-bars-setups-<slug>.real-shape.csv`, `membership-setups-<slug>.real-shape.json`, under `/Users/cobalt/cobalt-wt/setups-c1/` — the r4 re-cut, proven above) — parts ≤ 38,000 B, cut only between two JSON objects (after a `},` line) for the bars, at a line for the CSV;
 (b) the DEFINITION as the build's neutral shape renders it: an EXCERPT of `/Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/setups_shapes.py`, the line range `## FOR 13` names for the slug (unchanged since the cut — re-prove it: `grep -n "\"<slug>\": Shape(" /Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/setups_shapes.py` must print the range's first line; else re-locate by that symbol and say so) PLUS the functions and constants that shape calls (`example_mapping`, the relation lists, the slug's `*_rows`, the constructed fills it names such as `D2_CONSTRUCTED`), each excerpt headed by one line naming its real path and line range, its body those lines byte for byte; NEVER the module head `setups_shapes.py:1-45` (`:40-42` names the committed day's formation bar — an engine result, not a rule);
 (c) the engine tunables the shape's `cfg(...)` tokens and the scan grid read: an excerpt of `/Users/cobalt/cobalt-wt/setups-c1/configs/cobalt/taxonomy/tunables.yaml` — the rows whose keys the shape names plus `radar.scan_interval` (find each with `grep -n "<key>"`);
 (d) the PUBLIC RULES, excerpts of the FINAL (`/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`): §1 `### The mechanism` (orientation `A-01`), §2.1, the §2.2 and §2.3 tables with the geometry guard and `[F-16]` (5), §2.4, and the §3 table rows D0–D3 (and D4 for rubberband's Extension);
 (e) the replay grid, an excerpt of `/Users/cobalt/cobalt-wt/setups-c1/src/cobalt/radar/evaluate_cli.py` — `admitted_at` and `scan_instants` only;
 (f) `TASK.md`, written by you: the task sentence below, the file list with parts, and the fields owed — and no day other than what the staged files themselves carry.
`ls -la` of the folder quoted; every staged file re-checked with `grep -c "DEF_WRITTEN_"` → 0.

## STEP-2 — THE HOUSE (ONE call, its whole output read; `run_in_background: true`)
THE TASK SENTENCE (identical for either house): *"You are a blind checker (FINAL [F-16] (1)). Everything you may use is in this folder. For each setup in TASK.md, from the bars, the daily bars, the membership window, the definition excerpt, the tunables excerpt and the public rules ONLY, derive the FIRST formation the rules produce on the replay grid (admitted window, RTH, `radar.scan_interval`): the side (long|short), the formed bar's timestamp exactly as the bars file spells it, the trigger price, the stop price, and the anchor (the object the formation hangs on per FINAL §2.4, with its direction, its bar timestamp and price). Show your work: cite every bar timestamp you use. Where the staged rules do not determine a value, write UNDERIVABLE and the missing rule — never a guess. Do not open any file outside this folder. Write one table per setup: field · value · the bars it rests on."*
 - **Gemini:** `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind --print="<the task sentence> Write your answer to blind-values.md in this folder and nothing else."` (`66`'s invocation with the folder narrowed to the packet).
 - **Opus 5.5 (fallback):** `claude -p --model claude-opus-5-5 "<the task sentence> Read files with the Read, Grep and Glob tools only. Write no file. Print your complete answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"` (`66`'s invocation) — YOU write its printed answer to `blind-values.md` VERBATIM.
 A METER / HARNESS line (e.g. `17`'s `jetski: no output produced — a tool required the "command" permission …`) → the other house once; both down → `FAILED: STEP-2 — no house answered — <lines verbatim>`. `wc -c` of `blind-values.md` quoted.

## STEP-3 — ORDER, then COMPARE
3.1 THE ORDER PROOF (`## ORDER`, each its own call, output verbatim): `ls -lT /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind/blind-values.md` (T1 = its mtime) → `date` (T2) → ONLY NOW the first read of the pins: `grep -n "^DEF_WRITTEN_.*_CUT_" /Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/test_setups_fixture_cut.py` (the constants' first lines, verbatim) → for the `_ANCHOR` constant, whose value spans its continuation lines, Read the file from its printed line with `limit: 4` and quote those lines verbatim → `date` (T3). T1 < T2 ≤ T3 is the proof; state also that no earlier tool call of this session named that file (your transcript is the record; the desk reads it). The constants' comments must read `… at R4-F4 on 8da261a …` (the re-copied pins); any other comment → a row under `## ESCALATE` (the pins you compared are not r4's).
3.2 `## Comparison`: table `slug · field · pinned · blind · MATCH|MISMATCH|UNDERIVABLE`, five fields per slug (side, formed bar, trigger, stop, anchor). Normalisation, and nothing else: prices compared as decimals (trailing zeros ignored, `5.5000` = `5.5`); timestamps compared as instants (`2026-01-07 16:22:00+00:00` = `2026-01-07T16:22:00+00:00`); anchor compared on its object, its direction and its bar timestamp (each a sub-row; the pin carries no anchor price — the house's anchor price is RECORDED, not compared). `fields` in the stop line = the rows of this table.
3.3 `## ESCALATE`: every MISMATCH as `FIX-or-OWNER: <slug> <field> pinned <x> blind <y> — the house's cited bars <…>` (the desk takes it to him — the setups fix rounds are spent, L39; you do not pick a side, L37); every UNDERIVABLE with the missing rule the house named (L70: not a defect); the house used and, for Opus, "blind but same-house as the builder"; every file NOT staged and why; every `ASK DESK`. Then the stop line LAST.
