MODEL: Sonnet 5 (`claude-sonnet-5`) — stage a packet, put it before ONE house, copy its answer, then compare; no verdict of your own (L37) · SEAT: blind-values hub `setups-blind-values-0922`, launched by the CTO desk in the background. Two bare commands, run by THE DESK after `12-setups-fixture-cut.md` ended `SETUPS FIXTURE CUT BUILT …` (at least one slug closed) and the launch row **R24** and this file are committed on main: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/13-setups-blind-values.md' and follow it exactly." --model claude-sonnet-5 --permission-mode auto --remote-control setups-blind-values-0922 --allowedTools "Bash(agy *)" "Bash(claude -p --model claude-opus-5-5 *)" "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` · RULE STRINGS: all nine allow strings, the three deny strings and the `--add-dir` triplet are PRECEDENTED — each is in `prompts/2026-09-21/66-setups-one-check.md`'s launch line byte for byte (`"Bash(agy *)"` his R39 of 09-21; `"Bash(claude -p --model claude-opus-5-5 *)"` his R49 of 09-21; the read strings 09-21 `64` / `79`, 09-22 `04` / `08`); NOTHING NEW. `auto` mode: no write path, reads only (the `66` precedent) · SESSION: fresh · no DB, no `.env`, no docker, no pytest, no git write, no vault write, no memory-folder write (L58); the one house you launch is the check seat this prompt names (the `66` pattern — a check hub puts a packet before a house; you launch no hub, no worker of your own, L36) · METER: Anthropic small (you) + ONE house call (Gemini, or Opus 5 as the fallback) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time from date>]` under `## ESCALATE`, take the safe default (fewer files staged, never more access), continue.

# BLIND EXPECTED VALUES — a house that has NOT seen the pins re-derives the cut day's formation from the bars and the definition alone (2026-09-22)

WHY: FINAL [F-16] (1) — *"For each per-setup fixture day, the expected side, formed bar, trigger price and stop price are written by a CHECKER house from the bars alone, before it sees the builder's output; the test asserts all four, not only `formed`."* — and [F-21] (`docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`). `12-setups-fixture-cut.md` cut one stored pool day per closed slug (rubberband, hitchhiker — whichever closed) into committed real-shape fixtures on branch `setups/seven-0921` (`/Users/cobalt/cobalt-wt/setups-c1`) and pinned the ENGINE's values as `DEF_WRITTEN_<SLUG>_CUT_*`. You get a second house's values WITHOUT showing it the pins, then compare. LAW STEP: `12` → **THIS** → deploy prompt → his "approve" (L7 / L61).

## INDEX CARD (you; the house gets the packet only)
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — one line each: **L32** no value of his note enters the packet — the definition staged is the build's NEUTRAL shape (value-free literals of the build's choosing), never `4 - Strategies/*.md`. **L35** every value in your table is copied from a file, verbatim. **L37** you judge nothing: MATCH / MISMATCH / UNDERIVABLE is string equality after the normalisation named below; a MISMATCH is not a verdict. **L45** the packet's bars are the committed real-shape fixture, byte-identical. **L48** evidence in the report the same turn. **L62 / L63** unattended; a denial is a FAILED. **L67** the blind seat is a house OTHER than the build's author where one is up. **L70** an UNDERIVABLE is not a defect. **L71** the stop line is the last non-blank line.
2. `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md` — its LAST line (the closed slugs and days) and its `## FOR 13` section ONLY: the fixture paths, each slug's neutral-shape key and `setups_shapes.py` line range, the pinned test file and the constants' LINE NUMBERS. You do NOT read its `## FIND`, `## CUT` or `## PIN` sections (they quote the engine's FORMED lines — the values the house must not see, nor you before it answers).
3. `prompts/2026-09-21/66-setups-one-check.md` — its packet rules (staging by Read → Write byte-identical, parts ≤ 38,000 B, `wc -c` against the original, the whitespace gap check), its Opus probe, and its `agy` / `claude -p` invocations: THIS file copies them.

**NEVER STAGED, NEVER OPENED BEFORE THE HOUSE HAS ANSWERED:** the pinned test file named in `## FOR 13` (`tests/cobalt/test_setups_fixture_cut.py`), `tests/cobalt/test_rubberband_forms.py`, `tests/cobalt/test_setups_hitchhiker.py`, `tests/cobalt/test_setups_lego.py`, every other `tests/cobalt/test_setups_*.py`, the build report `setups-one-build-2026-09-21.md`, the cut report's sections other than `## FOR 13` and the last line, `66`'s check reports, and ANY line containing `DEF_WRITTEN_`. Before each staged file is written: `grep -c "DEF_WRITTEN_\|formed_bar_ts =\|FORMED " <original>` → 0, else that file is NOT staged (ESCALATE).

REPORT: `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/setups-blind-values-2026-09-22.md` (Write tool; you commit nothing — the desk commits it). Sections: `## §0 Headline` (≤5 lines) → `## AUTHORIZATION` → `## PREFLIGHT` → `## Packet` (file · original path · bytes original · bytes staged · parts) → `## House` → `## ORDER` (the timestamp proof) → `## Comparison` → `## ESCALATE` → `## CONTINUE` → the last line. FIRST Write creates it with `## CONTINUE` = `next: AUTHORIZATION` and the last line `(run in progress — next under ## CONTINUE)`.
STOP LINE (L71 — LAST NON-BLANK LINE): `SETUPS BLIND VALUES DONE · house: <name> · slugs: <n> · fields: <n> · MATCH: <n> · MISMATCH: <n> · UNDERIVABLE: <n> · ESCALATE: <n>` or `FAILED: <why>`.

AUTHORIZATION — each its own Bash call; a prompt file is not an approval:
- THIS launch is row **R24** of `cto-2026-09-22.md` — still `__` in the file you read → `FAILED: authorization mismatch — launch row not filled in`, stop. `grep -n "13-setups-blind-values.md" "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` → a `| R` row · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"13-setups-blind-values.md" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` non-empty (else `FAILED: authorization mismatch — the launch row is uncommitted`).
- The seats: `grep -n "^| R32 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (must carry `claude -p --model claude-opus-5-5` — his 13:20 ET ruling: Opus tasks run on Opus 5.5; the Opus seat itself is 09-21 R49's, the id is R32's; the `Bash(claude -p --model claude-opus-5-5 *)` string in this launch line stands on R32, not on `66`'s line — the `66` grep below is satisfied for every OTHER string) · `grep -n "^| R39 " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-21.md"` (record what it says of `agy` and until when).
- Each allow string: `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/66-setups-one-check.md"` ≥1 each (the eight OTHER than the Opus string, the three deny strings, the triplet; the Opus string `"Bash(claude -p --model claude-opus-5-5 *)"` stands on R32 above — `git -C /Users/cobalt/cobalt log -1 --format=%H -S"Bash(claude -p --model claude-opus-5-5 *)" -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` NON-EMPTY). Any 0 → `FAILED: authorization mismatch — <rule>`.

UNATTENDED RULES (`66`'s, unchanged): ONE command per Bash call, the bare listed prefix, no pipe, no redirect, no `cd … &&`, no `VAR=value` in front; files by the Write tool; reading by the Read tool. **A DENIAL IS A FAILURE:** `FAILED: <step> — <command> — <reason verbatim>`, stop. RECOVERY (L60): a relaunch first runs `ls /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind`; a staged file is not re-staged (its `wc -c` is re-proven); an existing `blind-values.md` is NEVER re-asked — you go to `## ORDER`.

## STEP-0 — PREFLIGHT (one row each: command · exit · result verbatim)
`date` (the DATE GATE for Gemini: record it) · `agy --version` · `claude -p --model claude-opus-5-5 "Reply with exactly the word OK"` (`66`'s probe) · `tail -n 1 "/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fixture-cut-2026-09-22.md"` → must start `SETUPS FIXTURE CUT BUILT` (else `FAILED PREFLIGHT: the cut is not built — <line>`); the closed slugs = those with a day, not `none` · `grep -n "^## FOR 13" "<same report>"` then Read that section only (Read with offset/limit to its lines).
THE HOUSE (L67: a house other than the build's author — the build and the cut constants are Anthropic-house work): **PRIMARY = Gemini** (`agy`, per R39 — valid only while R39's window holds; `date` past it, or `agy --version` failing, → fallback). **FALLBACK = Opus 5** (`claude -p`, R49 — the code-check seat, no calendar limit; the SAME house as the builder, so its use is recorded under `## ESCALATE` as "blind but same-house"). Neither up → `FAILED PREFLIGHT: no blind house — <each line verbatim>`.

## STEP-1 — STAGE (`/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind/` — the first Write creates it; NO `mkdir`)
Per closed slug, by Read → Write byte-identical, each proven by `wc -c` (original vs staged; parts sum to the original), `66`'s whitespace-gap check first:
 (a) the committed fixture files named in `## FOR 13` (`tests/fixtures/radar/bars-setups-<slug>.real-shape.json`, `daily-bars-setups-<slug>.real-shape.csv`, `membership-setups-<slug>.real-shape.json`, under `/Users/cobalt/cobalt-wt/setups-c1/`) — parts ≤ 38,000 B, cut only between two JSON objects (after a `},` line) for the bars, at a line for the CSV;
 (b) the DEFINITION as the build's neutral shape renders it: an EXCERPT of `/Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/setups_shapes.py`, the line range `## FOR 13` names for the slug PLUS the functions and constants that shape calls (`example_mapping`, the relation lists, the slug's `*_rows`, the constructed fills it names such as `D2_CONSTRUCTED`), each excerpt headed by one line naming its real path and line range, its body those lines byte for byte; NEVER the module head `setups_shapes.py:1-45` (`:40-42` names the committed day's formation bar — an engine result, not a rule);
 (c) the engine tunables the shape's `cfg(...)` tokens and the scan grid read: an excerpt of `/Users/cobalt/cobalt-wt/setups-c1/configs/cobalt/taxonomy/tunables.yaml` — the rows whose keys the shape names plus `radar.scan_interval` (find each with `grep -n "<key>"`);
 (d) the PUBLIC RULES, excerpts of the FINAL (`/Users/cobalt/cobalt/docs/30 - Design/SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md`): §1 `### The mechanism` (orientation `A-01`), §2.1, the §2.2 and §2.3 tables with the geometry guard and `[F-16]` (5), §2.4, and the §3 table rows D0–D3 (and D4 for rubberband's Extension);
 (e) the replay grid, an excerpt of `/Users/cobalt/cobalt-wt/setups-c1/src/cobalt/radar/evaluate_cli.py` — `admitted_at` and `scan_instants` only;
 (f) `TASK.md`, written by you: the task sentence below, the file list with parts, and the fields owed.
`ls -la` of the folder quoted; every staged file re-checked with `grep -c "DEF_WRITTEN_"` → 0.

## STEP-2 — THE HOUSE (ONE call, its whole output read; `run_in_background: true`)
THE TASK SENTENCE (identical for either house): *"You are a blind checker (FINAL [F-16] (1)). Everything you may use is in this folder. For each setup in TASK.md, from the bars, the daily bars, the membership window, the definition excerpt, the tunables excerpt and the public rules ONLY, derive the FIRST formation the rules produce on the replay grid (admitted window, RTH, `radar.scan_interval`): the side (long|short), the formed bar's timestamp exactly as the bars file spells it, the trigger price, the stop price, and the anchor (the object the formation hangs on per FINAL §2.4, with its bar timestamp and price). Show your work: cite every bar timestamp you use. Where the staged rules do not determine a value, write UNDERIVABLE and the missing rule — never a guess. Do not open any file outside this folder. Write one table per setup: field · value · the bars it rests on."*
 - **Gemini:** `agy --model gemini-3.1-pro-high --mode accept-edits --sandbox --print-timeout 45m --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind --print="<the task sentence> Write your answer to blind-values.md in this folder and nothing else."` (`66`'s invocation with the folder narrowed to the packet).
 - **Opus 5 (fallback):** `claude -p --model claude-opus-5-5 "<the task sentence> Read files with the Read, Grep and Glob tools only. Write no file. Print your complete answer." --permission-mode plan --add-dir /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind --allowedTools "Read" "Grep" "Glob" --disallowedTools "Bash" "Write" "Edit" "NotebookEdit" "Agent" "WebFetch" "WebSearch" "AskUserQuestion" "EnterWorktree"` (`66`'s invocation) — YOU write its printed answer to `blind-values.md` VERBATIM.
 A METER / HARNESS line → the other house once; both down → `FAILED: STEP-2 — no house answered — <lines verbatim>`. `wc -c` of `blind-values.md` quoted.

## STEP-3 — ORDER, then COMPARE
3.1 THE ORDER PROOF (`## ORDER`, each its own call, output verbatim): `ls -lT /Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/setups-blind/blind-values.md` (T1 = its mtime) → `date` (T2) → ONLY NOW the first read of the pins: `grep -n "DEF_WRITTEN_.*_CUT_" /Users/cobalt/cobalt-wt/setups-c1/tests/cobalt/test_setups_fixture_cut.py` (the constants, verbatim) → `date` (T3). T1 < T2 ≤ T3 is the proof; state also that no earlier tool call of this session named that file (your transcript is the record; the desk reads it).
3.2 `## Comparison`: table `slug · field · pinned · blind · MATCH|MISMATCH|UNDERIVABLE`, five fields per slug (side, formed bar, trigger, stop, anchor). Normalisation, and nothing else: prices compared as decimals (trailing zeros ignored, `5.5000` = `5.5`); timestamps compared as instants (`2026-01-07 16:22:00+00:00` = `2026-01-07T16:22:00+00:00`); anchor compared on its object and its bar timestamp and price (each a sub-row). `fields` in the stop line = the rows of this table.
3.3 `## ESCALATE`: every MISMATCH as `FIX-or-OWNER: <slug> <field> pinned <x> blind <y> — the house's cited bars <…>` (the desk takes it to a fix round or to him; you do not pick a side, L37); every UNDERIVABLE with the missing rule the house named (L70: not a defect); the house used and, for Opus, "blind but same-house as the builder"; every file NOT staged and why; every `ASK DESK`. Then the stop line LAST.
