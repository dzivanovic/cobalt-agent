DERIVE ROW: R73

MODEL: Fable 5.1 (`claude-fable-5-1`) — the DERIVE SEAT that row names (`cto-2026-09-22.md` §4 R73, 16:05 ET: for the tribunal of `DRC-AUTOMATION-PROPOSAL-2026-09-22.md`, `Fable seat: yes` on `claude-fable-5-1` — a PER-CASE OVERRIDE of his R36 for this tribunal, L73 — and `derive seat: claude-fable-5-1`); this is the Fable seat's SECOND JOB, as on the bars, setups, float-handicap and stale-score tribunals; you derive the version the round-1 rulings produce; you build, launch and commit nothing · SEAT: `drc-tribunal-derive-0922`, launched by the CTO desk in the background ONLY after every round-1 stop line this file needs is committed (hub `40`; the Fable seat `41`). YOUR POSITION, STATED: you are the same SEAT that ruled round 1 blind (`41`), in a fresh session — you HOLD A SIDE, so you recommend nothing to him (L37) and your own round-1 wordings pass the same filter as every house's. Two bare commands: `cd /Users/cobalt/cobalt-wt/agy-trial` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/42-drc-tribunal-derive.md' and follow it exactly." --model claude-fable-5-1 --permission-mode auto --remote-control drc-tribunal-derive-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the `--model` value is the one R73 names after `derive seat:`, already filled — `75`'s "THE DESK replaces `--model`" clause is spent; the SAME seven READ-ONLY strings and three denies as `22-draft-setups-tribunal.md`, `74`, `75`, `37`, 09-20 R13 — nothing new) · SESSION: fresh (a round-1 seat's context is NOT carried; the round-1 reports are on disk) · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly TWO files with the Write tool · METER: Anthropic FABLE (Fable at 76 % for the week on Tuesday 13:45, his R36 figure; the week's meter is HIGH, his R58) — honest estimate: the hub report `40` ≈ 25–35k tokens, the raw rulings of the houses that ruled ≈ 25–40k (21 items each), your seat's round-1 report ≈ 12–16k, the proposal ≈ 11k (read whole — v2 is the proposal amended), its author's report ≈ 3k, the rulings R60 / R65–R73 ≈ 3.5k, LAWS.md ≈ 17k (67,812 B measured at drafting), code re-opened only where a fold turns on it ≈ 5–15k; v2 ≈ 14–18k tokens of output (the proposal whole + the folds) plus the report ≈ 6–9k; peak context ≈ 120–150k — read the raw rulings item by item, not all at once, if the peak nears your window. It does NOT depend on the grok/agy window (no house runs) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DERIVE v2 of the DRC AUTOMATION DESIGN from the proposal (with its `## 13. DIFF MODEL` AS RULED) and the round-1 rulings of EVERY SEAT THAT RULED. LADDER: `S3-P3 · F14`. LAW STEP (L67): proposal DONE → his DRC sitting ruled the inputs and the diff (R65–R73) → round 1 DONE (Grok, Gemini via hub `40`; Astra `METER — proceed on three`, R13; Fable via `41`) → **THIS = the derive** → round 2 ONLY for what `## NEEDS ROUND 2` lists (empty → the tribunal closes and v2 is the FINAL's input) → build per chunk → ≥3 checkers → deploy. L1, L2, L3, L7, L8, L9, L28, L32, L40, L45, L53 and L57 are the bar v2 must meet, and the proposal's §12 L52 finding is re-answered for v2. DO NOT STOP until the report's last line is `DRC DERIVED v2 …` or `FAILED …`.

AUTHORIZATION AND PRECONDITIONS — VERIFY THEM YOURSELF (a prompt file is not an approval). **THE DERIVE ROW.** The first line of this file names the row of `cto-2026-09-22.md` `## §4 Rulings 2026-09-22` that carries his answer on the Fable seat and the derive seat for THIS tribunal — R73, filled at drafting (L19: any other change is a re-issue). The row must carry, as literal text: `DRC-AUTOMATION-PROPOSAL-2026-09-22.md`, `Fable seat: yes` and `derive seat: claude-fable-5-1`. Each its own Bash call: `grep -c -x -F "DERIVE ROW: R__" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/42-drc-tribunal-derive.md"` (`-x` = whole line: only the first line can match) — **1** → `FAILED: authorization — the DERIVE ROW is unfilled`, stop; **0** → read `<nn>` from the first line (`73`) · `grep -n "^| R<nn> " "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/cto-2026-09-22.md"` (exactly ONE `R<nn>` row carrying `DRC-AUTOMATION-PROPOSAL-2026-09-22.md`, with the three literals above all present — any missing, or two such rows → `FAILED: authorization mismatch — row R<nn> does not name the Fable seat's status and the derive seat for this tribunal`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"| R<nn> | " -- "docs/40 - DevDocs/reports/cto-2026-09-22.md"` (EMPTY → `FAILED: authorization mismatch — row R<nn> is not committed`) · **YOU ARE THE SEAT THE ROW NAMES:** the model id after `derive seat:` must equal the model id your own system prompt states you run as — else `FAILED: seat mismatch — row R<nn> names <id>; this session runs <id>`, stop. **EVERY ROUND-1 STOP LINE THIS DERIVE NEEDS, PRESENT AND COMMITTED — else you REFUSE:** `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-tribunal-2026-09-22.md"` (the LAST NON-BLANK line must start `DRC TRIBUNAL R1 DONE ` — anything else, the in-progress line, `FAILED` and a missing file included → `FAILED: refused — hub round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"DRC TRIBUNAL R1 DONE" -- "docs/40 - DevDocs/reports/drc-tribunal-2026-09-22.md"` (EMPTY → `FAILED: refused — the hub report's stop line is not committed on main`) · `tail -n 3 "/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-tribunal-fable-r1-2026-09-22.md"` (must start `DRC TRIBUNAL FABLE R1 DONE ` — else `FAILED: refused — Fable round 1 not done — last line: <line verbatim>`) · `git -C /Users/cobalt/cobalt log -1 --format=%H -S"DRC TRIBUNAL FABLE R1 DONE" -- "docs/40 - DevDocs/reports/drc-tribunal-fable-r1-2026-09-22.md"` (EMPTY → `FAILED: refused — the Fable round-1 stop line is not committed on main`) · **A HOUSE MUST HAVE RULED:** the hub's stop line shows `houses that ruled: 0 of 3`, or neither `grok:` nor `gemini:` in it carries a `TRIBUNAL R1:` line → `FAILED: no house ruled round 1 — the desk relaunches 40 or brings it to him` (your seat alone never settles an item: you would be deriving from your own ruling, L37) · **ASTRA (R13):** the hub's `astra:` field is `METER — proceed on three` or `SKIPPED — R13 (probe UP, recorded)` — RECORDED, not a refusal: derive from the seats that ruled, mark every question only Astra would have answered `ASTRA PENDING (R13)`, mark v2's header `ASTRA PENDING (R13)`, and name in §0 that Astra reads this v2 when its meter returns (Sat 09-26). Gemini or Grok alone not having ruled does NOT refuse the derive: the seats that ruled are counted per item (below). **THIS LAUNCH LINE ADDS NO RULE** — `grep -c -F -e "<rule>" "/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-21/22-draft-setups-tribunal.md"` for each of the seven allow strings and the three deny strings, quotes included — each must count **≥1**. YOU CAN ALWAYS STOP with a `FAILED: <step> — <concern>` line.

INDEX CARD — read in this order:
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` in full (L59). Binding: **L1 · L2 · L3 · L7 · L8 · L9 · L10 · L17 / L39** (≤3 rounds, no fourth; unresolved → Dejan) **· L28 · L29 · L32** (v2 and your report are COMMITTED: no user data — the coach spec and his DRC note by headings, key names and template prompt lines only; no value, rule sentence, ticker, price or P&L of his) **· L35 · L36 · L37** (you derive; a SPLIT between houses is carried, never settled by vote; a seat that holds a side recommends nothing to him — you hold one) **· L40 · L42 · L43 / L66 · L45 · L52 · L53** (every threshold, window, limit, key value and N is his) **· L57 · L67 · L68** (migration NUMBERS are the gate's) **· L70 · L71 · L72 · L73 · L74**. CLAUDE.md's absolute boundary: nothing in v2 reads, writes or infers from his trading platform — the DAS export he drops by hand is his own exported file, read-only (R66 a).
2. THE ROUND-1 RULINGS, whole: the hub report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-tribunal-2026-09-22.md` — its `## Rulings table`, `## Wording offered, verbatim`, `## Checked against the files` (the HOLDS / DOES NOT HOLD / UNVERIFIABLE table is your filter), `## Fable round-1 claims, file-checked` (the FC rows, or its one line saying the Fable report was not done at collate), `## Experiments named (L70)`, `## OWNER ITEMS (after the tribunal)`, `## Independence`, `## ESCALATE` · the raw rulings of EVERY house that ruled, in `/Users/cobalt/cobalt-wt/agy-trial/scratch/tribunal-bars-0920/drc-tribunal/r1/`: `grok-ruling.md` and `gemini-ruling.md` only if the hub's stop line shows that house's `TRIBUNAL R1:` line (the raw file is what "verbatim" means, but you never copy a user-data span out of it) · your seat's round-1 report `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-tribunal-fable-r1-2026-09-22.md` — including its `## Self-attack` (its `WITHDRAWN:` lines bind you: a withdrawn sentence is never folded).
3. The proposal `/Users/cobalt/cobalt/docs/30 - Design/DRC-AUTOMATION-PROPOSAL-2026-09-22.md` in full; its author's report `reports/drc-design-2026-09-22.md` (the UNPROVEN rows, ESCALATE 1–6, DIGEST); `cto-2026-09-22.md` §4 rows **R60, R65–R73** (`grep -n "^| R65 "` etc.) — R65 (the coach spec is the content / process start; trade-reporter = automation pattern only; NO PDF; output = `1 - Trading/5 - Review/DRC-<date>.md` from his template), R66 (one import place; his two hand-dropped inputs; the DRC is created ONLY when both are placed), R67 (his held-count statement wins mid-trade; the DAS reconcile is the truth; open positions carry), R68–R72 (the diff model as ruled); the S3 exits FINAL on legs, `docs/30 - Design/S3-EXITS-v3-2026-09-22.md` :17–34, :110–170 (v3 + R67).
4. Only where a fold turns on it: the real file the hub's file-check cites (code on main, migrations, configs, jobs and smoke rows, the lock file, the trade-reporter pattern; the coach spec by headings and key names ONLY; his DRC note and template by headings and template prompt lines ONLY) — read to check, never to copy.

THE RULES OF THE DERIVE — `75`'s rules (the bars tribunal's lesson, `topics/cto-desk.md` 2026-09-20, "THE FABLE SEAT ON A TRIBUNAL": everything the derive INVENTED was broken in round 2; everything ADOPTED from a house held), made binding, with this tribunal's substitutions:
- **Take a house's replacement wording VERBATIM.** An `ADOPT WITH` text goes into v2 word for word, or it is not taken. You may choose BETWEEN seats' wordings; you do not blend them, edit them or improve them. **A wording whose supporting claim DOES NOT HOLD in the hub's file-check, or is WITHDRAWN by its own seat, is NEVER folded** — say so in the fold table.
- **A Fable-seat claim the hub did not check is UNCHECKED, never HOLDS.** When the hub's `## Fable round-1 claims, file-checked` has its FC rows, they are your filter for your seat's wordings exactly as `## Checked against the files` is for the houses'. When the hub recorded the Fable report as not done at collate, every Fable claim is UNCHECKED: you may take a Fable wording only after opening the file it cites yourself and finding the claim borne out, and its fold row says `UNCHECKED by a hub — read by the derive at <file:line>`; such a row never counts toward a "claims that HOLD" figure, and where you cannot settle it by reading it is an experiment, not a fold. Where your seat's wording competes with a house's wording that is equally correct, the house's is taken — you never prefer your own text.
- **His R65–R73 are NEVER re-opened.** A wording that would change a §13 row's ACTION (KEEP / MERGE / DROP) or WRITER (AUTO / PRE / HIS), drop or narrow an addition C1–C13, create a DRC from one input, add a PDF, reach his platform, or set aside R67's held-count / DAS-truth / carry rules is NOT folded: it is carried in `## Dissents, verbatim` and named in `## ESCALATE` as `RE-OPENS A RULING (<R-row>)` — his to rule again if he chooses, never yours. A wording that changes HOW a row is built or names that its SOURCE does not hold (a source that does not exist, an experiment that gates it) IS a fold: the row keeps his ACTION and WRITER, and v2 states the source the rulings leave it, or `source pending <E/X n>`.
- **Prefer the simpler mechanism.** Where two seats offer different mechanisms for one item, take the smaller one when both meet the laws above, and write the other's cost and trade-off as an OWNER ITEM (after the tribunal) — unless the two disagree on whether a mechanism is CORRECT, which is `## NEEDS ROUND 2`.
- **Push every "not checkable from reads" claim into a first-gate experiment** (L70): an `UNVERIFIABLE FROM READS` row becomes an `X<n>` in v2's experiment list beside the proposal's E1–E8, placed before the chunk it gates, with the result that would change the design — never an argument in v2's text, never a defect.
- **Invent nothing.** v2 adds no mechanism, number, rule, key, column, table, clock, route or chunk that neither the proposal nor a seat's ruling contains. A gap no ruling fills stays a named gap (an experiment, an owner item, or a round-2 item) — never your filler.
- **Nothing of his before build.** Every value and choice that is his — the proposal's O1–O18 (O19 is spent: R69–R72 ruled the diff), every threshold, window, limit, key value, N and agreement definition, and every choice the seats name as his — is HIS, read by him AFTER the tribunal; nothing in v2 may make a chunk's BUILD depend on one of them (tests use constructed settings, L69); a seat that wrote one as a precondition to build is carried as an OWNER ITEM (after the tribunal) and named in `## ESCALATE`. Where the proposal makes a missing setting REFUSE, v2 states what the rulings decide about that refusal; it never fills a value. The ONE real DAS export (E1) and one real TradeZella screenshot (E2) he hands over are INPUTS a chunk waits on (L45), not owner values — v2 states which chunk waits on which, as the rulings leave it.
- **The author's ESCALATE 1–6** (a committed file carrying one of his values, `daily.md.j2:18`, and the four sources of one number — T9 / O5; the spec's no-trade DRC vs R66 — O3; the Charter's and the S3 smoke's "exists at 15:41" vs R66 — O18; lane load; D1 blocked on a real export — E1 / O15; the record correction on `DRC-2026-09-21.md`'s template): v2 places each exactly as the rulings decide it — inside this design, an owner item, a desk item, or its own item; a split on whether a mechanism is correct is `## NEEDS ROUND 2`, never your call.
- **The S3 seams (T7: S-C1, S-C2, S-C2b; D5 after S3 C2)**: v2 states what the rulings decide about each seam as a SEAM — what this design needs from S3's C1 / C2 and when — and never rewrites the S3 exits FINAL (v3 + R67); a ruling that S3's FINAL must change is carried in `## ESCALATE` for the desk (the S3 build prompts are drafted from v3 + R67).
- **The migration (one set, its number) and the chunk order** (D1 → D4 ∥ D2 → D3; D5 after S3 C2; E1 before D1): v2 states what the rulings decide; the deploy order and the numbering at build are the desk's lane (L68), stated as a fact, never as your ruling.
- **Dissents carried VERBATIM**: a house's REJECT or `DO NOT BUILD` that v2 does not follow goes into v2's `## Dissents, verbatim` word for word, with its house and item.
- **No user data in either file** (L32): the packet had none after the hub's REDACTION and SHAPE-ONLY rules; if a seat's text quotes a value of his anyway, it is taken with that span replaced by `[user data: <file:line>]`, and a seat's proposed VALUE for one of his keys is recorded as `<key>: value proposed by <seat> — <file>:<line>` (a pointer, never the value) and listed under OWNER ITEMS; count every redaction.

WRITE (Write tool, TWO new files, these names WHATEVER the date of the run):
- `/Users/cobalt/cobalt/docs/30 - Design/DRC-AUTOMATION-v2-2026-09-22.md` — the proposal, whole (§1–§13 and the T list), with each amended section REPLACED in place and tagged `[F-nn]` at the amended paragraph (so a reader can diff v2 against the proposal section by section); §13 carried as RULED (R69–R72) — only its SOURCE cells may carry an `[F-nn]` tag, never an ACTION or WRITER cell; a header naming the proposal, R65–R73, the rulings of every seat that ruled (paths; Astra `METER — ASTRA PENDING (R13)`; the Fable seat's status from row R73) and the fold table's path; `## First-gate experiments (L70)` = the proposal's E1–E8 and UNPROVEN rows as the rulings leave them, plus every experiment the seats named that the fold takes, each before the chunk it gates, each runnable on `cobalt_dev`, in the dev vault or on the one real export he hands over (a production read stays a hub-run read, L41); `## Dissents, verbatim`; `## L52 and the bar` — the proposal's §12 L52 finding re-answered for v2, with L1, L2, L3, L7, L28, L57 each stated for v2. Committable: no user data.
- `/Users/cobalt/cobalt/docs/40 - DevDocs/reports/drc-tribunal-derive-2026-09-22.md` — §0 Headline ≤5 lines (folds, round 2 or not, owner items, Astra pending) → `## DIGEST FOR THE DESK` (≤40 lines: what v2 changed from the proposal, item by item in one line each; which seats ruled round 1 (in full / not / Astra METER); how many Fable claims were hub-checked, how many UNCHECKED; what goes to round 2 and why; the owner items in one line each; the chunks, seats and RESTARTS under v2; what waits on E1 / E2 and on S3 C2; the hours under v2 against the S3 window 09-24 → 10-07 beside S3 C1–C4, a GUESS marked as one) → `## Fold table` (`F-nn · item (T1–T11, (a)–(j), WRONG FACTS) · seats that ruled it (<n> of 4, named) · whose wording (seat, or "proposal kept") · adopted verbatim? yes / no / not taken · why (≤25 words, citing the hub's file-check row — `Checked` row or `FC<n>` — where it decides, or `UNCHECKED by a hub — read by the derive at <file:line>`, or `RE-OPENS A RULING (<R-row>)`)`) → `## NEEDS ROUND 2` (ONLY items where a house said `DO NOT BUILD`, or two seats disagree on whether a mechanism is CORRECT; each with both positions quoted and the question round 2 must answer; EMPTY = the tribunal closes) → `## OWNER ITEMS (after the tribunal)` (deduplicated, each with who raised it — the proposal's O1–O18 first; none a precondition to build) → `## FOR DEJAN` (each owner item in HIS terms — what he would see on the `/drc` page, in his DRC note, on the day he drops only one file, on the next morning, at which moment — as ONE A/B question per item: A = what v2 builds by default, B = the alternative a seat named, each with the seats that hold it; NO recommendation — you ruled round 1 and hold a side (L37); ≤4 lines per item) → `## Redactions` (count, and where) → `## READING` → `## ESCALATE` (every `RE-OPENS A RULING`, every S3-FINAL change a seat asked for, every precondition-to-build, every DOES-NOT-HOLD wording a seat still presses, every `ASK DESK`) → `## CONTINUE` → last line. Written in the same turn as the work (L48); while you work the last non-blank line is EXACTLY `(run in progress — next step under ## CONTINUE)`; the breadcrumb lives inside `## CONTINUE` only; no other line may START with `DRC DERIVED`, `FAILED` or `CONTINUE`. RECOVERY (L60): a relaunch first runs `ls` on both file paths, reads its own report first and continues from `## CONTINUE`; v2, if already written, is re-read, never re-derived from scratch. One command per Bash call, bare shape, no pipe, no redirect. `date` for every time you write.

STOP LINE (last non-blank line of the derive report, L71): `DRC DERIVED v2 · folds: <n> · verbatim: <n> · needs round 2: <n> · owner items: <n> · ESCALATE: <n>` — where `folds` = rows of the fold table, `verbatim` = rows marked `yes` — or `FAILED: <step> — <reason>`. Then stop. NEXT STEP, not yours: `needs round 2: 0` → the desk commits v2 as the FINAL's input, brings him `## FOR DEJAN` one item per message, and the build prompts are drafted (E1 first — his one real DAS export — then D1, per v2, ordered against the S3 build and the stacked branches at the desk's L68 gate); otherwise a round-2 hub is drafted for the listed items only, with the same approved strings (a run after 2026-09-23 23:59 ET needs his word for `grok` / `agy` — R30 ends then); in every case Astra reads the FINAL on Sat 09-26 (R13).
