MODEL: Opus 5.5 (`claude-opus-5-5`, R36) — DRAFTING seat (`cto-2026-09-20.md` R16–R18: the desk drafts nothing) · SEAT: prompt drafter `build-lane-draft-0922`, launched by the CTO desk in the background. Two bare commands: `cd /Users/cobalt/cobalt` then `claude --bg "Read '/Users/cobalt/cobalt/docs/40 - DevDocs/prompts/2026-09-22/30-draft-build-lane.md' and follow it exactly." --model claude-opus-5-5 --permission-mode auto --remote-control build-lane-draft-0922 --allowedTools "Bash(git -C /Users/cobalt/cobalt show*)" "Bash(git -C /Users/cobalt/cobalt log*)" "Bash(ls *)" "Bash(grep *)" "Bash(tail *)" "Bash(wc *)" "Bash(date*)" --disallowedTools "AskUserQuestion" "EnterWorktree" "Bash(git push*)" --add-dir /Users/cobalt/Vault --add-dir /Users/cobalt/cobalt --add-dir /Users/cobalt/cobalt-wt` (the SAME seven READ-ONLY strings as today's `08` / `11` / `14` / `18` / `22` / `24` / `26`, 09-20 R13 — nothing new) · SESSION: fresh · auto mode on; never `bypassPermissions`; NO database, NO docker, NO pytest, NO git write, NO launch of any agent or house (L36), no vault write, no memory-folder write (L58) — you write exactly the files named below with the Write tool, the REPORT LAST (09-22 lesson 3) · METER: Anthropic medium (the week's meter is HIGH, his R58 — read by section, never whole corpora you do not need) · nobody sits at this terminal — the report file is your channel; never ask: `ASK DESK: <question> [<time>]` in the report and continue with the safe default.

# DRAFT (do NOT launch) THREE build-lane changes his rulings of 14:05–14:23 ET require, 2026-09-22

WHY: his R58 (14:28 ET) — "concentrate on saving work for what we are trying to build and deploy this week without any additional work" — keeps this week to the build lane. Three items in that lane need their prompts brought in line with rulings he gave this afternoon (`cto-2026-09-22.md` §4):
1. **H1 handicap build (`27`) + its check (`28`)** — drafted this morning, NOT launched. R54 changes the design: a DEAD column (a source's whole float / market-cap column blank) makes the handicap INOPERATIVE at factor 1 for that scan, with the degraded banner and its reason (Fable's reading of v3 [F-10]); per-name unknowns still `apply` (R52). R53 `combinator: any`, R55 (items 9 / 10 as designed), R56 (excluded list: suffix only), R57 (exact tie → unhandicapped first) confirm v3 as written — no design change, but the build prompt names them as ruled. The keys' VALUES stay his (L32 / L53).
2. **The stale-score BUILD + its three-checker CHECK** — NEW prompts. The design tribunal is CLOSED (`docs/30 - Design/STALE-SCORE-v2-2026-09-22.md`, derive report `reports/stale-score-tribunal-derive-2026-09-22.md`, `needs round 2: 0`); his owner items R37–R45 are ALL RULED: 1 A (a stale WATCH card's score chip is `—` and it drops below every scored card at his next tap / reload) · 2 A (premarket `—`, returns on the next bar) · 3 A (two clocks, as designed) · 4 **B** (stale-price `htf_level_proximity` shadow grades EXCLUDED from the agreement numbers; X25 counts them) · 5 A (`—` ties by pool position) · 6 A (FILLED pills unchanged, own item later) · 7 A (chip / slot at next tap, badge within a scan) · 8 A (stamp look as deployed) · 9 **B** (the same lines ALSO recompute the FRESH-price tap race — the row never shows an old-price score beside a new proximity; this GROWS the build's scope). The build STACKS on `setups/seven-0921` (v2 §6 and derive ESCALATE 5, desk call: L68 — it joins the setups deploy set or the next stacked one).
3. **Setups FIX round 3** — NEW prompt, on `setups/seven-0921` in `/Users/cobalt/cobalt-wt/setups-c1`, AFTER the running fixture cut `12` (its tip is the base — the desk fills it). His rulings: R47 O2 / X10 = **Grok's fix** ("A definition wins": `backside` and `fashionably-late` bind side through the mirrored frame on their OWN long-side definition text, never a direction recomputed from `last_close − session_open`; `SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md:418` quotes both fixes) · R48 O3 / F1 **WIDENED** (`per_indicator` tunables may carry ASSUMED defaults, marked as such, like every other hole under 09-21 R44 — fill the three holes: fashionably-late's two flat thresholds `A-09` / `A-10`, vwap-continuation's `dist.k.vwap` `A-16`, each value derived from a cited cheat-sheet / FINAL source and marked `source: assumed`; a hole no source supports stays null and is an ESCALATE, never invented) · R49 O4 (a MINIMUM-SIZE rule for `impulse` / `pullback` legs as a NEW `A-nn` key with an ASSUMED default, in place before cards go live; "we tune in live" — the value is his config value afterwards, L53) · R50 O5 (a FILLED card's health pills SKIP `assumed_formation`; `cards/health.py`) · R51 (`stop.buffer`: value stays `0.02`, `tunables.yaml` unit label `cents` → `dollars`; no conversion code, no behaviour change). The fix r2 report's ESCALATE (viii) (stop resolvers not given `tunable_keys`) is NOT his ruling — carry it as a question for the check, not a fix. NOT IN SCOPE (R58 defers it): R46's per-setup ON/OFF dial.

## INDEX CARD — read these, in this order, nothing more until a step needs it
1. `/Users/cobalt/Vault/Think/6 - Permanent/Memory/LAWS.md` — you are a drafter: read in full (L59). Binding, one line each in every prompt you write: L1, L19, L28, L29 (Opus floor on a write path; a migration or `src/` change = Opus 5.5 builder), L32 (no value of his in code / tests / fixtures — keys only; an ASSUMED default is a cited system default, not his), L35, L36, L42, L45, L46 / L54 / L60, L48, L52, L53, L57, L62 / L63, L66, L67 (checks seat Grok · Gemini · Opus 5.5 · Sol — Sol / Astra on METER until Sat 09-26 06:47 ET, recorded, skipped, R13), L68, L69, L70 (every "not checkable from reads" = a first-gate experiment run FIRST), L71, L73.
2. `docs/40 - DevDocs/reports/cto-2026-09-22.md` `## §4 Rulings` rows R13, R23, R26, R30, R31, R32, R36, R37–R45, R46–R51, R52–R57, R58 — his words; every prompt's AUTHORIZATION greps the rows it rests on, each its own Bash call, and FAILS on a missing row.
3. ITEM 1: `prompts/2026-09-22/26-draft-handicap-h1.md` (how `27` / `28` were drafted), `27-handicap-h1-build.md`, `28-handicap-h1-check.md`, `29-handicap-h1-facts.md` whole; `reports/handicap-h1-draft-2026-09-22.md`; v3 `docs/30 - Design/FLOAT-HANDICAP-v3-2026-09-21.md` [F-10] and §9; `reports/float-handicap-tribunal-fable-r1-2026-09-21.md:123` region for Fable's (d) wording.
4. ITEM 2: `docs/30 - Design/STALE-SCORE-v2-2026-09-22.md` whole (§6 chunks / seats / migration / restarts, §7 first-gate experiments, `## L52`); the derive report's `## FOR DEJAN` (the nine items as he answered them above) and `## ESCALATE`; the code v2 cites, by its `file:line` references only.
5. ITEM 3: `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-fix-r2-2026-09-22.md` (`## ESCALATE`, the F-rows' shape), `prompts/2026-09-22/15-setups-fix.md` (the fix-round prompt shape and its launch line — YOUR r3 launch line is `15`'s BYTE FOR BYTE except the prompt path and `--remote-control setups-fix-r3-0922`), `reports/setups-fix-draft-2026-09-22.md` lines 66–76 (O1–O5 as the build left them), `reports/setups-blind-code-draft-2026-09-22.md` :33–36 (`stop.buffer`), the FINAL `SETUPS-AT-DEFAULTS-FINAL-2026-09-21.md` :64, :189, :368, :418, and the build report `/Users/cobalt/cobalt-wt/setups-c1/docs/40 - DevDocs/reports/setups-one-build-2026-09-21.md` :1067, :1117–1140.
6. THE SHAPES YOU COPY: `prompts/2026-09-21/65-setups-one-build.md` (resumable multi-step Opus build in its own worktree: UNATTENDED RULES, RECOVERY RULE, "never end a turn between steps", "an unlisted command = do not call it; file content via Write / Edit, never a shell heredoc", the `.env` pair), `prompts/2026-09-21/66-setups-one-check.md` (the three-checker code check's launch line — copy BYTE FOR BYTE, only path + remote-control name change), `topics/cto-desk.md` lines 95, 99–102 (the lessons every build / check prompt now carries).

## THE JOB — Write tool, one file per call, the REPORT LAST
(a) RE-ISSUE `docs/40 - DevDocs/prompts/2026-09-22/27-handicap-h1-build.md` WHOLE (L19): every line of the current file kept except what R54 changes and the rows it now also greps (R52–R57 beside R26 / R31); the dead-column rule becomes a step with a RED-first test (a fixture source whose float AND cap columns are blank → factor 1 for every name of that source, the banner with its reason, `handicap JSONB.reason` names it; a single blank cell on a live column still `apply`s); launch line and allow / deny strings UNCHANGED byte for byte (prove it in the report). Then RE-ISSUE `28-handicap-h1-check.md` WHOLE with one added question on R54's step; its date gate follows R30 (Grok / agy through 2026-09-23 23:59 ET) — unchanged unless R30 says otherwise. Re-issue `29-handicap-h1-facts.md` with R52–R57 in one line each.
(b) NEW `docs/40 - DevDocs/prompts/2026-09-22/31-stale-score-build.md` — ONE complete block on `65`'s shape, v2 §6 as the chunk plan (S1 first), R37–R45 folded as the spec (item 9's fresh-tap race as its own RED-first test), v2 §7's experiments FIRST (L70; X9 / X25 are production READS — they are the HUB's, run inside the check, never the builder's, L41). MODEL / mode / worktree / branch per v2 §6 and L29 (a `src/` change = Opus 5.5; auto mode on the dev lane, stated as `27` states it); its worktree is NEW, cut by the DESK from the setups branch tip it stacks on — write the `git -C /Users/cobalt/cobalt worktree add -b <branch> <path> <tip>` line with `<tip>` as `__TIP__` for the desk to fill, and a PREFLIGHT that fails on an unfilled tip; its `.env` pair = TWO NEW strings re-pointed to its worktree (R41's shape) — list them. Then NEW `32-stale-score-check.md` on `66`'s launch line byte for byte (path + `--remote-control stale-score-check-0922`), Q-set on `66`'s shape: L52 (a)–(d), each of R37–R45 as built, v2's experiments' `AS EXPECTED` re-read, X9 / X25 run by the HUB read-only on production under the precedented read string (name its precedent), no value of his (L32 grep).
(c) NEW `docs/40 - DevDocs/prompts/2026-09-22/33-setups-fix-r3.md` — ONE complete block on `15`'s shape and launch line (byte for byte but path + remote-control name), base tip `__TIP__` (the desk fills it from `12`'s stop line; PREFLIGHT fails unfilled or on a different tip), rows F1–F5 = R47, R48, R49, R50, R51 in that order, each RED-first where behaviour changes (R51 is a label change: prove `structure.py:104`'s arithmetic unchanged by the existing pins), the three ASSUMED values and the new `A-nn` default each with its cited source quoted verbatim (L35), every pinned `DEF_WRITTEN_*` untouched unless a row changes a formation — then the pin moves ONLY with the engine's printed output and the row says which blind seat must re-derive it (`13` / `23`). Stop line on `15`'s shape: `SETUPS FIX R3 BUILT <tip> | on <base> | offline <p>/<f> | with-DB <p>/<f> | src changed: yes | tests added: <n> | .env: removed, proven gone | ESCALATE: <n>` or `FAILED …`.
(d) LAST, YOUR REPORT `docs/40 - DevDocs/reports/build-lane-draft-2026-09-22.md`: `## DIGEST` (≤15 lines: per prompt — scope in one sentence, steps, experiments first, MODEL / mode / worktree, RESTARTS expected, migration yes/no, new rule strings count), `## NEW STRINGS` (each verbatim with the precedent it re-points), `## RULE PROOF` (every launch line vs its precedent line — every string's `grep -c -F` count; `27`'s old vs new launch line identical), `## ORDER` (the dev-DB lane order you read from the prompts' gates: `12` running → which next, and what `16` (setups check r2) must be re-pointed to after r3 — name the lines, do not edit `16`), `## ESCALATE`, and your LAST LINE: `BUILD LANE PROMPTS DRAFTED · prompts: <n> · re-issued: <n> · new rule strings: <n> · migrations: <n> · ESCALATE: <n>` or `FAILED: <why>`.

## RULES THAT BIND YOU
L19 whole-prompt (a re-issue is the whole file). L36 you launch nothing. L58 no memory write. L32: no value of his anywhere — KEYS only; an ASSUMED default is a cited system default. Never invent a flag or a subcommand: every command shape appears in `65` / `66` / `15` / `27` / the code's CLI (read `src/cobalt/cli.py` and `src/cobalt/**/*cli*.py`). Every owner item not ruled stays his. `date` for every time you write. Stop within 45 minutes; your stop line is your report's last non-blank line, written LAST.
