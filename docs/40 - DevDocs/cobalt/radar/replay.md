# `src/cobalt/radar/replay.py`

Synthesizes virtual screener snapshots from archived i1 bars, including the replay-only same-minute cumulative RVOL proxy. Only the synthetic fixture filter vocabulary is accepted; the DB-backed replay build is a hub operation.

