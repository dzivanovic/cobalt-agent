# `src/cobalt/radar/sources.py`

Derives archiver and backfill targets from validated Lists-note blocks. The CLI can report sources or compare note-derived targets with a historical watchlists revision without adding a second derivation path. The retired YAML schema lives here only for proposal/equality proofs; the archiver never loads it.
