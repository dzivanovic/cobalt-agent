# `src/cobalt/radar/sources.py`

Derives archiver and backfill targets from validated Lists-note blocks. The CLI can report sources or compare note-derived targets with the committed watchlists revision without adding a second derivation path.

