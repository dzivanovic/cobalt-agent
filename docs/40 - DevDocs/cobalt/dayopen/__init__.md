# `src/cobalt/dayopen/__init__.py`

Module docstring only: why the morning sweep runs as a Cobalt command
(RULED 2026-09-14, A) rather than the local seat composing it — the
Qwen allowlist denies every write, and L49 says the local seat reads and
judges, never composes.
