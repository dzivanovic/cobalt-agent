# S2-P3 PLAN — Radar panel (pool view live + card-ladder shell) (2026-09-14)

Architect: Opus 5 (plan only, no build) · Reviewer: Codex `gpt-6-astra`, 09-15, ≤3 rounds (L29/L39) · Branch `sprint-2/radar-panel` off `main` `9c47d6f` · Worktree `~/cobalt-wt/s2-p3-radar-panel` (L54) · Builder: Sol `gpt-5.6-sol` high, `codex exec -s workspace-write`, launched by the Sonnet hub after Astra (2026-09-14 R4) · Hub: Sonnet, which cuts the fixture, runs the DB test and commits · S2 ends 09-23.

## DEPLOY WINDOW

No production deploy 09:00–16:00 ET on a trading day unless waived in writing for that day (ruled 09-13, O29).

## §0 Headline

- Adds a read-only `GET /radar` page to the ASET sheet on :5010, in the same process (Charter §5 #10). **Layer A, the POOL VIEW, is live now:** it shows the running S2-P1 pool (50 names, position, session metric, sources, entered/left, churn). **Layer B, the CARD LADDER shell,** is proven against fixtures and shows an honest empty state in production (0 radar cards).
- No write path: no POST, no taps persisted, no config rows, no migration. Rollback = `git revert`, no data.
- Rulings taken 2026-09-14: R1 two layers · R2 per-class health thresholds, rows land at P2 · R3 detail order levels→rank+WHY→news→notes→chart (empty) · R4 Sol high, Opus headless fallback.
- ESCALATE: none. Both raised items were closed by Dejan's approval (§8).

## Context — why

S2-P1 is live. `system.radar_pool` `primary` is `scanning`, cap 50, members 50, ranked by session metric (`rth: rvol`, `premarket/aftermarket: volume`), `stickiness_scans: 3`, `radar.scan_interval` 100 s. Today it holds 676 membership rows, 172 of them left. Only the heartbeat line shows it to Dejan. S2-P2 (F8 cards, F10 dots) is gated on the tribunal. The panel's acceptance needs 2–3 live mornings beside DAS, so it has to start before P2 lands or S2 closes (09-23) without it.

## §1 Facts (verified read-only 2026-09-14)

| # | Fact | Evidence |
|---|---|---|
| F1 | `radar_pool` columns: pool_key, state, degraded, degraded_sources, failed_stage, failed_detail, sources, session, cap, members, last_scan_id/at/ms, last_poll_at, poll_failures, budget, updated_at. | information_schema query |
| F2 | `radar_membership` columns: id, pool_key, ticker, trade_date, first_seen_at, entered_at, left_at, source, sources (jsonb), rank_at_entry, last_rank, below_cap_streak, excluded_by, session, opened/last/closed_scan_id. **No rank-metric value column.** | same |
| F3 | Metric values exist only in the gitignored CSV cache `data/radar-cache/<date>/` (7-day retention). | `configs/cobalt/radar.yaml` |
| F4 | Pool rule mirror: `"user".trader_settings` key `radar.pool` → `{block:{cap, rank_metric{rth,premarket,aftermarket}, overrides{day_scan:first_from 10:00, morning_low_float: volume}, stickiness_scans}, status, note_sha256, block_sha256}`. | query |
| F5 | The card table already exists: `"user".aset_sizings` (origin, state, pool_member_id, account_mode…). Production has 119 manual cards and 0 radar cards. No dot, health or WHY columns. | query |
| F6 | The sheet is **FastAPI** (not Flask), f-string HTML, no templates, :5010. `RadarStore.open_members` / `pool_row` exist. | `aset/web.py:73`, `radar/store.py:29,39` |
| F7 | `com.cobalt.aset` reads `configs/dev/aset.yaml`, `tunables.yaml`, imports `cobalt.aset.__main__`, `cobalt.aset.web`. `com.cobalt.radar` imports `cobalt.cli`, which reaches `cobalt.radar.*`. Docs and tests derive no restart. | `jobs.yaml:39-67,156-164`; `cobalt jobs restarts` run on a docs-only range |
| F8 | `radar.board_refresh` does not exist. It is a tribunal value still to rule. | `tribunal-grok-2026-09-14/fable/OPEN.md:17` |
| F9 | The mock package has no `prototype.html` and `source/` is empty. Only card-spec, decisions, open-questions and 6 PNGs are present. The PNGs are the design of record (Dejan, 09-14). | `ls` |
| F10 | The L45 leak scan `test_screen_filter_values_live_only_in_approved_radar_fixtures` scans src, configs, tests, ops and `docs/40 - DevDocs` for filter strings. `tests/fixtures/radar/` is exempt. | `test_radar_notes.py:26` |

## §R Rulings (2026-09-14, Dejan)

| Ruling | Decision | Where it binds |
|---|---|---|
| R1 | Two layers on one new route, read-only. Pool view shows **rank position + metric NAME** (no value until a column exists). Refresh = `radar.scan_interval` until `board_refresh` is ruled. Ladder uses a Pydantic view model: real-shape where real rows exist, card-spec §6 contract-only for dots/health/WHY, empty state in prod. | STEP-2..5 |
| R2 | Health thresholds are per class. They are recorded here, and the rows land in `tunables.yaml` **at S2-P2** with the computer. P3 displays ok/warn/bad only. | §7 carry |
| R3 | Detail column: 1 levels (trigger · stop · 1R · 2R) → 2 rank breakdown (card_score, conviction, proximity, pool position + source) + WHY → 3 news → 4 notes → 5 chart = reserved EMPTY slot (no embed). News and notes are labelled empty slots in P3. | STEP-4 |
| R4 | Builder: Sol high, launched by the Sonnet hub 09-15 after Astra; Codex meter probed before launch (L47); Opus 5 headless fallback. Reason (Dejan): L27's overflow sentence is under routing-tribunal review; use the full Codex week; Opus reserved for P2/P4 write paths. Hub cuts fixture + commits. | §6 |

R2 values, for the P2 prompt (`card.health.*`, 8 tunables):

| Class | deteriorating (amber ↓) | broken (red ↓↓) |
|---|---|---|
| Participation ratio (RVOL, volume pace) vs entry | < 70 % | < 45 % |
| Cost (spread) vs entry | ≥ 1.5× | ≥ 2.5× |
| Graded dot (1–10) | down ≥ 2 points | ≤ DOT_RED_MAX (3) |
| Structural (level, MA side, market/sector alignment) | touched / tested | lost on a closed bar at working TF |

Judgment dots are never health-scored (L11).

## §2 Steps

- **STEP-1 (hub, before Sol; prod reads only):** cut the fixtures, see §3.
- **STEP-2 read layer:** add `RadarStore.members_for_day(pool_key, trade_date) -> list[dict]` (open + left, every F2 column) in `src/cobalt/radar/store.py`. It is the one store for these tables (L3). The pool block comes from `TraderSettingsStore.values()["radar.pool"]`. The session comes from `session_clock().session(now_utc())`. The interval comes from `load_tunables()` `radar.scan_interval`.
- **STEP-3 view models + builder:** new `src/cobalt/aset/radar_panel.py`.
  - `PoolView` / `PoolRow` / `ChurnDelta`: rows ordered by `last_rank`, left rows with `left_at` + `excluded_by`, churn = `entered_at > since` / `left_at > since`. `since` is the client's last server timestamp, so the server keeps no state.
  - Fail-loud (L1/L9): missing pool row → FAILED; `degraded` → red banner naming `degraded_sources`; `failed_stage` → FAILED with detail; `last_scan_at` older than 2 × scan_interval → STALE banner; pool-block `status != ok` → FAILED.
  - `CardView` per card-spec §6 (dots, health, trails, etc.) + `LadderView`. Order: ARMED/TRIGGERED/IN-TRADE pinned above WATCH regardless of score; WATCH by `card_score` desc; terminal muted below.
  - Card source adapter: open radar-origin rows in `aset_sizings`. 0 → empty state ("No radar cards — F8 lands in S2-P2"). >0 → FAILED ("radar cards exist but the card contract is not wired — S2-P2"), never a half render.
- **STEP-4 render:** pure `render_pool(view)` / `render_ladder(view)` in the f-string + `html.escape` idiom of `web.py`, laid out per the state PNGs (design of record).
  - Strips (52 px), #1 + #2 open by default, client-only toggle + `collapse all` / `top 2`.
  - Owner badges COBALT / YOU / N/A MANUAL. Rank chip = opaque `card_score` plus one F3 WHY line.
  - Health line: pills + `all holding` / `n deteriorating`.
  - Detail column in R3 order; `@media (max-width:1149px)` drops it below the card.
  - Phone frame: `@media (max-width:430px)`, 366 px inner, plus `?frame=phone` preview.
  - Promote ↑ and the judgment tap strip render **disabled** with title "S2-P2". Visual tokens per card-spec §7, system font fallbacks, no external font fetch.
  - Terminal section below the ladder, as in the mock (#12 still open).
- **STEP-5 routes** in `web.py`: `GET /radar` (HTML, both layers, pool first) and `GET /api/radar/pool?since=` (JSON from the same builder, one path). A link to it from the sheet header. No POST route added.
  - **Focus fix (09-02 fumble):** the panel never calls `alert(`, `prompt(`, `confirm(`, `.focus(` or `autofocus`, has no inputs, and no `http-equiv="refresh"`. The refresh is a background `fetch` every scan_interval that swaps table content only.
- **STEP-6 tests + DevDocs:** §4; a DevDoc for `aset/radar_panel.py`, plus updates to `aset/web.py` and `radar/store.py` pages; BACKLOG/kanban row.

## §3 Fixture (L45, real-shape, hub-cut)

| File | Source | Transform |
|---|---|---|
| `tests/fixtures/radar/panel-pool.real-shape.json` | One live `radar_pool` row + the full membership set for one trading day, spanning premarket and rth, open and left (`cobalt db query --side system --prod`, no `%` in SQL) | Dates shifted to a fixed synthetic day with intra-day offsets kept. Scan ids rebased. Source hash suffixes (12 hex) → all zeros. Tickers, screen keys, ranks, streaks and `excluded_by` kept. |
| `tests/fixtures/radar/panel-pool-block.real-shape.json` | `trader_settings` `radar.pool` value | `note_sha256` and `block_sha256` zeroed |
| `tests/fixtures/radar/panel-cards.contract.json` | Column shape from one real `aset_sizings` row; `user_id` → 1, date shifted | Dot, health and WHY fields per card-spec §6, marked `"_contract": "card-spec §6 — no real artifact until S2-P2; P2 replaces"`. One card per state: WATCH, ARMED, TRIGGERED, IN-TRADE (FILLED), CLOSED, PASSED, EXPIRED, MISSED, plus N/A MANUAL stop and a degraded case. |

## §4 Tests (`tests/cobalt/test_radar_panel.py`, offline TestClient, stores monkeypatched as in `test_aset_web.py`)

| Area | Assertions |
|---|---|
| Pool | Rows ordered by `last_rank`, count = members. Left rows carry `left_at` + `excluded_by`. Churn for a given `since`. Metric name per session (premarket volume, rth rvol, `day_scan` first_from 10:00 override shown). Cap shown. Banners: degraded, FAILED stage, STALE, missing pool row, block status ≠ ok. Refresh interval = tunable value, not a literal. JSON == HTML builder output. |
| Ladder | Render test for every state in the fixture. Pinning beats a higher WATCH score. #1/#2 open, others strips. Each owner badge appears, N/A MANUAL included. Health ok/warn/bad pills + summary. R3 detail order by DOM index. The 1150 px media query is present. Phone frame renders. Prod empty state at 0 radar cards. FAILED at >0 un-wired radar cards. |
| Read-only + focus | `/radar*` routes are GET only. Panel HTML has no `<form`, `method="post"`, `alert(`, `prompt(`, `confirm(`, `.focus(`, `autofocus` or `http-equiv="refresh"`. |
| L45 leak | The existing scan stays green unmodified. New: panel fixtures carry no non-zero 12-hex source suffix and no real date. Rendered HTML never contains a filter token. |
| DB (hub, cobalt_dev, `requires_db`) | `members_for_day` returns open + left for the day and nothing from other days or pools. |
| Suite | Full suite green; `cobalt validate` green. |

## §5 RESTARTS

- Plan commit (this run): docs only → `RESTARTS: none`.
- Build range: `cobalt jobs restarts main..HEAD`. Expected: `com.cobalt.aset` (reach from `cobalt.aset.web` to `web.py` + `radar_panel.py`) and `com.cobalt.radar` (reach from `cobalt.cli` to `radar/store.py`). No config file changes. Any other label or UNCLASSIFIED → ESCALATE before merge (L42).

## §6 Build, deploy, acceptance, rollback

- **Build (09-15, after Astra):** the hub probes the Codex meter, then runs STEP-1. Sol runs STEP-2..6 in the worktree without DB credentials (L41 interim). The hub runs the DB test, the leak scan and the full suite, verifies the artifact (L35), and makes one commit (L46).
- **Deploy (L43 one per evening; L54 rebase, then `--ff-only` merge from `~/cobalt`):**
  - **STEP-D1, 16:05 ET or later:** merge, then `launchctl kickstart -k gui/$(id -u)/com.cobalt.aset`. Smoke: `/api/health` ok, `GET /` 200 with the banner unchanged, `GET /radar` 200, ladder in its empty state.
  - **STEP-D2, inside the 20:00–21:00 ET market_reset pause only (radar idle):** `launchctl kickstart -k gui/$(id -u)/com.cobalt.radar`. It is never restarted right after 16:00 during the aftermarket session (same as 09-14 ruling B for the alerts deploy). Until STEP-D2 the running radar resident still has the old `radar/store.py`. That is harmless: the new read method is used only by the aset process.
  - Smoke after STEP-D2: the first post-reset scan lands, `radar_pool.members` matches the `/radar` table, radar heartbeat green, full suite green.
- **Acceptance:** Dejan keeps `/radar` open beside DAS on 2–3 live mornings. He confirms (a) the names and churn match what he sees, (b) DAS hotkeys never lose focus to the panel, (c) it is readable at one-card width. Ladder shell acceptance = fixture renders, with screenshots in the report. Live ladder acceptance moves to P2.
- **Rollback:** `git revert` of the merge range. Restart `com.cobalt.aset` at once, and `com.cobalt.radar` in the next 20:00–21:00 pause. No data, no migration, no vault.

## §7 Carried to other prompts

- **S2-P2:** add the 8 `card.health.*` rows (R2 table) with the health computer. Replace `panel-cards.contract.json` with real rows. Wire the card adapter. Enable promote and the tap strip.
- **S2-P4:** persist the rank-metric value per membership (a new column, write path, Opus floor per L29) so the pool view can show it. If P2 ships first, it may carry this instead.
- **Open, unruled:** `radar.board_refresh` value (tribunal); terminal placement (mock #12).

## §8 ESCALATE — none (both closed 2026-09-14 by Dejan)

1. ~~Mock package missing `prototype.html` / `source/`~~ → CLOSED: the state PNGs are the design of record.
2. ~~Rank-metric value has no persisted home~~ → CLOSED: S2-P4 carries the column (write path, Opus floor). If P2 ships first, it may carry it instead.

## Cost

Builder: Sol high, one mid-size build session (~2 h) on the Codex weekly meter, % unknown until the pre-launch probe. Review: Astra ≤3 rounds (Codex). Hub (Sonnet): fixture cut, DB test, commit, deploy, about $3 API-equivalent. This plan: Opus 5, about $4–6 API-equivalent.
