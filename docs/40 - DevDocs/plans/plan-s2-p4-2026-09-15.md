# S2-P4 PLAN — F3 pick-vs-rank rows · rank-metric value column · F12 nightly replay + counterfactual R · F13 top-N archive + benchmark + miss line · S2 smoke (2026-09-15)

Architect: Opus 5 (plan only) · Reviewer: Codex `gpt-6-astra`, ≤3 rounds (L29/L39), later day · Branch `sprint-2/p4` off `main` `1859803` · Worktree `~/cobalt-wt/s2-p4` (L54) · Builder: **[amended 2026-09-16 per Dejan's 2026-09-15 Codex meter ruling — `areas/cobalt-houses.md`]** Opus 5 headless (`claude -p --model claude-opus-5`, L50), launched by the Sonnet hub; Sol = none this week (weekly allowance at 26% until 09-19; Codex serves reviews, tribunals and the L52-d read-only recompute only) — Sol high returns as the L47 alternative only after the weekly reset, by ruling · Hub: Sonnet (fixtures, DB proofs, commit, deploy) · S2 ends 09-23; dates never move, scope moves out.

**Sequencing fact.** P3 merges 09-15 20:05. P4 rebases onto main after that, before the build, because it touches P3's `radar/store.py` and `aset/radar_panel.py`. If P2 lands first, P4 rebases after P2 merges. If P4 is smaller, it may ship first and carry the value column (P3 plan §7). Migration numbers are fixed either way: P2 = 0006/0007, P4 = 0008/0009. The FORWARD tuple is ordered and idempotent, and a numbering gap is harmless (`db_migrations/__init__.py:32-46`; `--down-to` is by version, `db_migrations/cli.py:256`).

## DEPLOY WINDOW

No production deploy 09:00–16:00 ET on a trading day unless waived in writing for that day (ruled 09-13, O29).

## §0 Headline

- **Value column (R1):** `system.radar_membership` gains `rank_metric` + `rank_value`, written by S1 on every scan. `/radar` shows the value next to the metric name. No backfill: rows from before the deploy stay NULL and render `—`.
- **F3 picks (R2):** a pick is a card reaching FILLED. One `"user".picks` row is written inside the `CardStore.fill` transaction and snapshots pool rank, metric, value, `card_score` and card-score rank at pick time. The row exists now; DRC rendering lands in S3.
- **F12 + F13 (R3/R4/R5):** new one-shot `com.cobalt.replay` at 21:05 ET weekdays, after the reset and after the archiver.
  - It fetches unfiltered top movers, archives their i1 bars, and replays today's unfilled cards over i1 bars.
  - Each miss becomes a `"user".missed` row with `excluded_by` and one counterfactual R (hard stop → window end) plus all its inputs (L57).
  - It diffs the benchmark movers against the in-play set and upserts ONE miss line as unit `drc-misses/miss_line` into that day's DRC note.
- **S2 smoke (R6):** `cobalt smoke s2`, a read-only command in the day-open pattern: a numbered check table, PASS/FAIL/KNOWN/ERROR, and a report file. The checklist in STEP-9 is its spec. If the build slips past 09-22, the hub runs the checklist by hand.
- ESCALATE: 4 (§8). Engine trade_def formations are replayed only once P2's evaluator exists. Until then there is one loud line and no fake rows.

## Context — why

S2-P1 is live (pool of 50). P3 (read-only `/radar`) merges tonight. P2 (cards, shadow dots, dark) has its plan written. The ladder's S2 block still owes four things: F3's pick-vs-rank record, F12's MISSED corpus with counterfactual R, F13's "what moved that we never saw" line, and the smoke that closes S2. P3 plan §7 also handed P4 the rank-metric value column. The miss line and the cost-of-discipline rows are the Friday-review input for loosening rule 10 (`SPRINT-LADDER-v0_1.md:44-49`).

## §1 Facts (verified read-only 2026-09-15)

| # | Fact | Evidence |
|---|---|---|
| F1 | `radar_membership` stores `rank_at_entry`, `last_rank`, `excluded_by ∈ {config_cap, not_equity, screen_inactive, manual}`. It has no metric value. | `db_migrations/0004_radar_pool.sql:29-51` |
| F2 | The value is computed and then discarded. `_ranked` reads `SourceSet.metrics[ticker][metric]`, chooses the session metric or a per-screen override metric, and takes the max across lists for list-sourced names. It returns only `ranks` and `source_for`. `Transition` has `rank`, not a value. | `radar/pool.py:37-47,80-92,95-176`; `radar/models.py:171-180` |
| F3 | `apply_membership` writes `last_rank` for RETAIN/HOLD and EXCLUDE, and `rank_at_entry`/`last_rank` on INSERT. The HOLD path (frozen pool) carries `member.last_rank`. | `radar/store.py:69-126`; `pool.py:340` |
| F4 | The CSV cache has a 7-day retention, but only `2026-09-14` and `2026-09-15` exist on prod (1634 files on 09-15). Directory dates come from `now.date()` of the scan instant. | `configs/cobalt/radar.yaml`; `radar/collector.py:133-150`; `ls data/radar-cache` |
| F5 | FILLED is reached through one path: `AsetStore.mark_filled` → `CardStore.fill` (actor YOU, evidence) and then the fill-figures UPDATE. Cards carry `direction`, `entry`, `stop`. | `aset/store.py:185-252`; `cards/store.py:374`; `aset/migrations/0001_aset_sizings.sql:11`; `aset/models.py:56,87-92` |
| F6 | States are WATCH/ARMED/TRIGGERED/FILLED/CLOSED/PASSED/EXPIRED/MISSED. WATCH→MISSED needs a reason. EXPIRED is terminal. `cards-expire` runs at 16:05, so open WATCH cards are already EXPIRED by night. | `cards/models.py:35-47,90-134`; `jobs.yaml` `com.cobalt.cards-expire` |
| F7 | `assert_writable` refuses in `market_reset`. `VaultWriter._session_gate` refuses vault paths in `market_reset` but allows repo-owned paths and dry-runs. | `session/guard.py:123-150`; `vaultwrite/writer.py:387-410` |
| F8 | `com.cobalt.archiver` runs at 20:30, inside `market_reset` (20:00–21:00). Recent full runs: 975 requests, 23m13s–23m21s, finishing ~20:53. `job.result` = rows/tickers/requests/failures/duration. | `jobs.yaml` archiver row; `docs/30 - Design/archiver-runs.md` tail; `archiver/runner.py:91-118` |
| F9 | The archiver's targets are the Lists note archive targets only (210 tickers). Bars come from `/export/stock?t=&p=` via `finviz_get`, the sole transport. Intervals are i1/i2/i5/i15/i30, with no daily. | `archiver/config.py:34-60`; `archiver/collector.py:175-237`; `archiver/models.py:12-26` |
| F10 | Screener export = `/export/screener` with `v=152` and `c=0-150`; `o=` is the sort. `parse_screener_csv` validates the required headers. The CSV carries Change, Gap, Change from Open, Asset Type, Volume, Relative Volume. The TokenBucket is per process at `radar.finviz_max_rpm` = 40. | `radar/collector.py:81-104,120-128,152-177`; `DATA-SOURCE-MEMO.md:72,88-89`; `tunables.yaml:431` |
| F11 | The DRC prefill (15:40) writes three L28 units: `drc-risk/risk_parameters`, `drc-trades/tickers`, `drc-rules/rules_check` via `VaultWriter.upsert_unit`. | `prefill/drc.py:1-24,441-448`; `vaultwrite/writer.py:644-661` |
| F12 | The placement map already DECLARES `missed`, `legs`, `fills`, `drc_rows`, `prediction_records` USER. `picks` and a movers table are not declared. ADR-0008 names J3 `"user".missed.pool_member_id → system.radar_membership(id)` (counterfactual R from `system.bars`) and J4 (F13 movers vs in-play, both system-side). | `db_migrations/placement.py:73-83`; ADR-0008:131,347-348 |
| F13 | Migrations: FORWARD 0001–0005 on main. P2 reserves 0006 (system seam) + 0007 (user cards: `card_score`, `radar_score_id`, …). | `db_migrations/__init__.py`; P2 plan STEP-1 |
| F14 | `trader_settings` `SETTING_KEYS` are REQUIRED by `TraderSettings`: a missing key crashes the sheet. A new optional key must not join that tuple. | `settings/models.py:29-75` |
| F15 | Rule 10 = max two open positions (circuit breaker). `daymode.trade_count_band.{min,max}` = `null` PLACEHOLDER. | `configs/cobalt/rules.yaml:42-45`; `tunables.yaml:316-333` |
| F16 | The expiry window resolver is conservative: prose windows resolve to a time only when unambiguous, otherwise the session close. | `cards/expire.py:1-40` (`_resolve_window_end`) |
| F17 | The day-open check pattern is a numbered S-checks list: a verbatim read-only command + EXPECT, output pasted, a PASS/FAIL/KNOWN/ERROR verdict table, and OVERALL RED if any ERROR, else AMBER if any FAIL, else GREEN (KNOWN never lowers it). | `prompts/2026-09-15/00-day-open-qwen.md`; `reports/day-open-2026-09-15.md` |
| F18 | The heartbeat MISSED/freshness probes are driven by the job registry. `cobalt validate` fails on a jobs.yaml row with no plist. | `heartbeat/probes.py:261-296`; `jobs.yaml` header |
| F19 | The P3 branch adds `RadarStore.members_for_day` and `aset/radar_panel.py` (PoolRow shows metric NAME only, per P3 R1). | `git diff main...sprint-2/radar-panel -- src/cobalt/radar/store.py`; P3 plan §2 |

## §R Rulings (2026-09-15, Dejan)

| Ruling | Decision | Binds |
|---|---|---|
| R1 | **A.** `rank_metric TEXT` + `rank_value NUMERIC` on `system.radar_membership`, latest only, written by S1. The F3 pick row snapshots rank + metric + value at pick time. **No backfill** (L57): pre-deploy rows stay NULL and render `—`. | STEP-1,2 |
| R2 | **A, amended at approval.** Pick = any card reaching FILLED (manual now, radar later). One `"user".picks` row written by `record_pick` in a **SAVEPOINT inside the `CardStore.fill` transaction**. A pick-insert failure rolls back the savepoint only: the fill commits, the sheet shows a red "pick not recorded" banner, and a job-log line is written. `cobalt cards picks` reports the gap as MISSING. A ticker not in the pool gets `pool_rank` null and `not_in_pool=true`. CLI `cobalt cards picks --date`. DRC rendering stays in S3. | STEP-3 |
| R3 | **B.** New one-shot `com.cobalt.replay` at 21:05 ET weekdays (jobs.yaml row + plist). It refuses loud unless tonight's `com.cobalt.archiver` job row is `done`. Its vault write falls outside `market_reset`; the 21:40 backup captures it. The archiver and its L28 carve-out stay untouched. | STEP-4 |
| R4 | **A.** Replay scope = every card created that ET day that never reached FILLED, plus P2 formations once `cobalt radar evaluate --replay` exists (until then one loud line: `trade_def replay: not available until S2-P2`, no rows). One formula: trigger = first i1 bar trading through `entry`, filled at `entry` (or at the bar open if it gapped through). R = (exit − entry) / \|entry − stop\| × sign(direction). Exit = the stop if touched first (stop wins a same-bar tie), else the close of the last i1 bar at the horizon end. `mfe_r` is stored as an observation, not a second R. Every input is stored. | STEP-5 |
| R5 | **A.** Settings `radar.benchmark {top_n, min_move_pct}` in `"user".trader_settings` (his values, applied by him, L53). 21:05: two unfiltered exports (gainers `o=-change`, losers `o=change`) → `system.movers_daily` → i1 archive for the top-N not already in `system.bars` for that day (counted in the total-demand test). Benchmark (\|change\| ≥ `min_move_pct`) is diffed against the day's admitted episodes → `missed` rows. ONE line as a 4th DRC unit `drc-misses/miss_line`. DRC note absent → no create; loud FAILED and the job row red. | STEP-6,7 |
| R6 | **B, amended.** `cobalt smoke s2` is built on the day-open check pattern: read-only, prints the same verdict table, writes `reports/s2-smoke-<date>.md`. The plan still carries the checklist as its spec. If the build slips past 09-22, the hub runs the checklist by hand (A) and the command carries to S3. | STEP-9 |
| R7 | **A.** Builder Sol high, launched by the Sonnet hub after Astra. The hub probes the Codex meter at launch (L47); Opus 5 headless is the fallback. The hub runs DB proofs and commits (L41 interim). This splits P2 (Opus, Anthropic meter) and P4 across meters (L27). | §6 |
| — | **[amended 2026-09-16 per Dejan's 2026-09-15 Codex meter ruling — `areas/cobalt-houses.md`]** Builder = Opus 5 headless (`claude -p --model claude-opus-5`, L50), launched by the Sonnet hub; Sol = none this week (weekly allowance at 26% until 09-19; Codex serves reviews, tribunals and the L52-d read-only recompute only). Sol high returns as the L47 alternative only after the weekly reset, by ruling. | §6 |

## §2 Steps

- **STEP-0 (hub, before build, prod reads only):**
  - Rebase `sprint-2/p4` onto main after P3 (and P2, if merged).
  - Cut the fixtures in §3.
  - Record: one trading day with ≥1 unfilled manual card whose entry was crossed; that day's top gainer absent from the pool (the "Tesla-class" case); the column set of one live unfiltered `o=-change` export (headers only in the report, no filter strings).
  - Probe the Codex meter.
- **STEP-1 migrations** (central `src/cobalt/db_migrations/`, appended to `FORWARD`/`REVERSE`, placement map + tests):
  - `0008_radar_value_movers.sql` (+ rollback), system side:
    ```sql
    ALTER TABLE system.radar_membership
      ADD COLUMN IF NOT EXISTS rank_metric TEXT CHECK (rank_metric IN ('volume','rvol')),
      ADD COLUMN IF NOT EXISTS rank_value  NUMERIC(20,6);
    CREATE TABLE IF NOT EXISTS system.movers_daily (
      id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
      trade_date DATE NOT NULL,
      side TEXT NOT NULL CHECK (side IN ('gainers','losers')),
      rank INTEGER NOT NULL CHECK (rank >= 1),
      ticker TEXT NOT NULL,
      change_pct NUMERIC(10,4) NOT NULL,
      asset_type TEXT, volume BIGINT, rvol NUMERIC(12,4),
      export_sha256 TEXT NOT NULL,          -- raw CSV bytes (L57)
      fetched_at TIMESTAMPTZ NOT NULL,
      bars_archived BOOLEAN NOT NULL DEFAULT false,
      UNIQUE (trade_date, side, ticker));
    ALTER TABLE system.movers_daily OWNER TO cobalt_system;
    GRANT SELECT ON system.movers_daily TO cobalt_user;
    ```
  - `0009_picks_missed.sql` (+ rollback), user side. Both tables carry `user_id INTEGER NOT NULL DEFAULT current_setting('cobalt.trader_id')::int REFERENCES "user".traders(id)`, the same as P2's 0007:
    ```sql
    CREATE TABLE IF NOT EXISTS "user".picks (
      id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, user_id …,
      card_id BIGINT NOT NULL UNIQUE REFERENCES "user".aset_sizings(id),
      transition_id BIGINT NOT NULL,        -- the FILLED card_transitions row
      ticker TEXT NOT NULL, picked_at TIMESTAMPTZ NOT NULL, session TEXT NOT NULL,
      origin TEXT NOT NULL, account_mode TEXT,
      pool_member_id BIGINT REFERENCES system.radar_membership(id),
      not_in_pool BOOLEAN NOT NULL,
      pool_rank INTEGER, pool_size INTEGER, rank_metric TEXT, rank_value NUMERIC(20,6),
      pool_scan_id BIGINT, pool_scan_at TIMESTAMPTZ,
      card_score INTEGER, card_score_rank INTEGER, focus_top4 BOOLEAN,
      score_basis TEXT NOT NULL,            -- 'card_score' | 'unavailable: <reason>'
      CHECK (not_in_pool = (pool_member_id IS NULL)));
    CREATE TABLE IF NOT EXISTS "user".missed (
      id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY, user_id …,
      trade_date DATE NOT NULL,
      kind TEXT NOT NULL CHECK (kind IN ('card','formation','mover')),
      ticker TEXT NOT NULL, direction TEXT CHECK (direction IN ('long','short')),
      card_id BIGINT REFERENCES "user".aset_sizings(id),
      trade_def_md5 TEXT,
      pool_member_id BIGINT REFERENCES system.radar_membership(id),   -- J3
      mover_id BIGINT REFERENCES system.movers_daily(id),
      excluded_by TEXT NOT NULL CHECK (excluded_by IN (
        'unarmed','passed','not_filled','no_card','window','rule_10',
        'not_in_any_source','config_cap','not_equity','screen_inactive','manual')),
      gate_detail JSONB NOT NULL,           -- every gate evaluated, incl. trade_count_band: unset
      entry NUMERIC(14,4), stop NUMERIC(14,4),
      trigger_ts TIMESTAMPTZ, fill_price NUMERIC(14,4), horizon_end TIMESTAMPTZ,
      exit_ts TIMESTAMPTZ, exit_price NUMERIC(14,4), exit_reason TEXT CHECK (exit_reason IN ('stop','horizon_end')),
      cf_r NUMERIC(10,4), mfe_r NUMERIC(10,4),
      bars_watermark TIMESTAMPTZ, formula_version TEXT, inputs_sha256 TEXT NOT NULL,
      replay_job_id BIGINT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
      CHECK (kind <> 'card' OR (card_id IS NOT NULL AND entry IS NOT NULL AND stop IS NOT NULL)),
      CHECK (kind <> 'mover' OR mover_id IS NOT NULL));
    CREATE UNIQUE INDEX IF NOT EXISTS missed_one_per_subject ON "user".missed
      (user_id, trade_date, kind, ticker, COALESCE(card_id,0), COALESCE(trade_def_md5,''));
    ```
  - `placement.py`: `movers_daily` → `CREATED_TABLES` SYSTEM; `picks`, `missed` → `CREATED_TABLES` USER (`missed` leaves `DECLARED_TABLES`). The rollback deletes nothing beyond its own tables and columns.
  - **[amended 2026-09-16 per Astra R1-1]** The migration proof excludes `rank_metric`/`rank_value` from populated-membership digest checks (adding nullable columns to an existing table is not content corruption); prove first-apply, second-apply and reverse/reapply on populated `radar_membership` with non-null post-deploy values, in both P2-before-P4 and P4-before-P2 merge orders.
  - **[amended 2026-09-16 per Astra R1-2]** `picks`/`missed`: `OWNER TO cobalt_user`. `movers_daily` keeps `OWNER TO cobalt_system`; add `GRANT SELECT, REFERENCES ON system.movers_daily TO cobalt_user` (its FK from `"user".missed.mover_id` needs REFERENCES) plus matching sequence grants. Test ownership, insert/identity use, USER reads of `movers_daily`, missing-GUC rejection and wrong-side rejection through real side-role connections (L32).
  - **[amended 2026-09-16 per Astra R1-3]** No duplicate table: P4's `picks`/`missed`/`movers_daily` are distinct from P2's score/receipt/dot seam. `missed` moves out of `placement.py`'s `DECLARED_TABLES` into `CREATED_TABLES`; all three P4 tables register there. Test 0005→0008/0009→(later)0006/0007 and the reverse merge order, plus rerunning 0008/0009 idempotently; P4-before-P2 has no SQL/import dependency on any P2 column or table.
  - **[amended 2026-09-16 per Astra R1-8]** A digest + bar range + watermark cannot replay `cf_r`/`mfe_r` (L57): `system.bars` is mutable and a hash alone can't recover a since-changed source. `"user".missed` gains an immutable receipt payload (JSONB) retaining the actual consumed OHLC bars, `entry`/`stop` as evaluated (with any historical stop-edit version), `created_at`, window text/resolution, horizon, the state/position history behind each gate, and the benchmark/settings inputs — hashed by `inputs_sha256`, never hash-only. `replay_job_id` is dropped in favor of a receipt-owned replay-run identifier (not `cobalt_jobs`'s label-keyed, overwritten-per-run id). Recompute is required after source bars, settings, or a card's stop change or disappear. Formations reuse P2's receipt/evaluator seam — never a second scoring implementation.
  - **[amended 2026-09-16 per Astra R2-1; corrected by R3-1]** A rerun with changed bars/settings/movers cannot overwrite an immutable receipt row nor collide with `missed_one_per_subject`'s uniqueness, and an INSERT-then-link ordering can't satisfy an immediate partial-unique index (the replacement row and the not-yet-updated predecessor briefly co-exist as "current"). `"user".missed` gains `run_seq INTEGER NOT NULL DEFAULT 1`, `is_current BOOLEAN NOT NULL DEFAULT true`, and `superseded_by BIGINT REFERENCES "user".missed(id)`; the unique index is `missed_one_current_per_subject ON "user".missed (user_id, trade_date, kind, ticker, COALESCE(card_id,0), COALESCE(trade_def_md5,'')) WHERE is_current`. One serialized USER reconciliation transaction per rerun, in this order: (1) `UPDATE ... SET is_current = false` on every subject the new run recomputes (frees the unique slot first); (2) INSERT the replacement row(s) (`is_current = true` by default); (3) `UPDATE` the now-retired predecessor(s) to set `superseded_by` to the new row's id (the id exists only after step 2). A subject whose miss disappears entirely on rerun (a bar correction removes a trigger, or a mover rises out of the benchmark's exclusion set) is retired the same way — step (1) only, no step (2)/(3): `is_current = false`, `superseded_by` stays NULL, and the retiring run identity is recorded on the row. `system.movers_daily` rows dropping out of the active set are marked `active = false` (STEP-6), never deleted, so `missed.mover_id`'s FK never breaks. Every prior versioned row keeps its own immutable receipt, queryable by id regardless of `is_current`. The miss line and `job.result` counts are published only from the run's own successfully reconciled current set. Test the real replacement INSERT against live uniqueness/FKs in this exact order, a trigger disappearing after a bar correction, a mover rising out of the benchmark set while remaining top-N, identical-rerun idempotency, and a retry that resumes between step (1)/(2) and the SYSTEM-side `movers_daily` commit.
- **STEP-2 value column (R1)** — `radar/pool.py`, `radar/store.py`, `aset/radar_panel.py`:
  - `_ranked` also returns `values: dict[ticker, (metric, value)]`, using the metric that actually ranked the name: the screen override metric, else the session metric. For list-sourced names it is the max across lists (the same expression as `pool.py:143-144`). A value of None stays None.
  - `Transition` gains `rank_metric`, `rank_value`.
  - `apply_membership` writes both on RETAIN, EXCLUDE and INSERT. HOLD (frozen) keeps the prior values (`COALESCE`). LEAVE keeps the last values.
  - `open_members`/`members_for_day` select both.
  - P3's `PoolRow` gains `rank_value`; `render_pool` prints the value beside the metric name (`—` when NULL).
  - `replay.py` snapshots stay untouched.
  - **[amended 2026-09-16 per Astra R1-19]** P3's `PoolRow` carries no per-row metric field today — the metric name is a view-level session value — so `MembershipRecord` (which forbids extra fields) also gains the selected `rank_metric`/`rank_value` columns, threaded through row conversion into `PoolRow`, so the rendered metric name reflects the actual per-row value (including screen-override names), not just the session default. Test the whole query→model→HTML path, not rendering fakes alone. Correction to F2: `Transition` is defined at `pool.py:37`; `_ranked` returns ordered tickers as well as ranks/`source_for`.
  - **[amended 2026-09-16 per Astra R2-2]** `open_members()`'s other consumer constructs `OpenMember(**row)`, which forbids extra fields the same way `MembershipRecord` does; `OpenMember` also gains nullable `rank_metric`/`rank_value`, carried through the frozen HOLD path (`COALESCE` to the prior pair). Test a real-shaped `open_members` row through the actual resident scan (`radar/runner.py`), for pre-deploy NULLs, populated values, and HOLD preserving the prior pair — not only the query→HTML path.
- **STEP-3 F3 picks (R2)** — `src/cobalt/cards/picks.py` + `cards/store.py`:
  - `CardStore.fill` calls `record_pick(conn, card_id, transition_id, now)` on the FILLED hop inside its open transaction (one path, `cards/store.py:374`), wrapped in `SAVEPOINT pick` (R2 amended). Any exception → `ROLLBACK TO SAVEPOINT pick`, the fill continues and commits, `logger.error` names the card and the error class, and `fill()` returns a `pick_recorded=False` flag. The sheet renders a red "pick not recorded" banner from that flag, the same banner idiom as `aset/web.py`'s FAILED banner.
  - The pool lookup reads the admitted episode open at `now` for the ticker in `radar.yaml` `pool_key` (`entered_at IS NOT NULL AND entered_at <= now AND (left_at IS NULL OR left_at > now)`) plus `radar_pool.last_scan_id/at` and `members`.
  - `card_score` / `card_score_rank` (rank among open radar cards by `card_score` desc, non-null only) are read only if `aset_sizings.card_score` exists. Otherwise `score_basis='unavailable: S2-P2 not merged'`. Null conviction → `'unavailable: no taps'`. `focus_top4 = card_score_rank <= 4`.
  - A missing pool row or a degraded pool writes a row with nulls and a named `score_basis`/`not_in_pool`. A pick-insert failure never refuses the fill (savepoint above).
  - CLI `cobalt cards picks [--date]`: a table of pick, pool rank/value and card-score rank for every FILLED card that day. A FILLED card with no pick row prints `MISSING` and exits 1.
  - **[amended 2026-09-16 per Astra R1-4]** `fill()` owns one USER transaction on every legal route (including the radar/already-TRIGGERED shortcut that today calls `transition()` unwrapped and commits before returning), passing that transaction into every `transition()` call. `SAVEPOINT pick` wraps only the pick read/insert, after the FILLED hop, inside that one fill transaction, before its commit. Test the WATCH/ARMED shortcut and the strict TRIGGERED manual/radar route with an injected SQL error on the pick insert; prove a fill-transaction failure never reports success.
  - **[amended 2026-09-16 per Astra R1-5]** `fill()` returns a typed result (transition ids + `pick_recorded`), propagated through `mark_filled()` (which today discards the return), the direct sheet route, and the CLI (which today expects a bare transition-id list). Existing successful-fill reporting is preserved; the red "pick not recorded" banner is appended, not substituted. Name the concrete resident log destination for the required `logger.error` line (a log line is not a persisted job row). Test both HTTP fill paths, including that fill figures still persist after a pick failure.
  - **[amended 2026-09-16 per Astra R1-6]** `record_pick` reads `system.radar_*` on the passed-in USER connection (qualified cross-schema reads are allowed under ADR-0008; SYSTEM cannot read cards/settings or write picks/missed, so no role switch and no separate connection/commit). The pick-time card-score cohort explicitly includes the just-picked card itself (filtering to open WATCH/ARMED/TRIGGERED after the FILLED hop would drop it from its own ranking), with a deterministic tie policy and a named `unavailable` reason; the cohort/score inputs needed to replay the rank are stored (L57). Test permissions (USER read of qualified SYSTEM tables) and tied ranks.
  - **[amended 2026-09-16 per Astra R1-7]** "Every FILLED card that day" means a FILLED **transition** on that ET date (from `card_transitions`), not current `state='FILLED'`, card creation date, or `aset_sizings.filled_at` (unpopulated by the direct-button route; later CLOSED cards still carry a pick). `cobalt cards picks` drives its output from FILLED transitions bounded by the ET day, left-joined to `picks`, printing MISSING/exit 1 for every gap including CLOSED cards. K6 (§9) compares only transitions at/after the P4 deploy cutoff, so pre-deploy historical MISSING rows stay visible without failing that check.
- **STEP-4 job (R3)** — `src/cobalt/replay/` (`cli.py`, `runner.py`, `movers.py`, `cards.py`, `line.py`, `models.py`):
  - `uv run cobalt replay nightly [--date YYYY-MM-DD] [--dry-run]`, wrapped by `as_job("com.cobalt.replay")`.
  - `jobs.yaml` row: one-shot, `supervisor: self`, `timeout_s: 1800`, `schedule {at "21:05", weekdays 1–5}`.
  - `ops/com.cobalt.replay.plist` sets `COBALT_ENV=production` + `COBALT_VAULT_PATH` as the prefill plists do.
  - Order:
    - (0) Precondition: the `cobalt_jobs` row `com.cobalt.archiver` finished `done` on this ET date; otherwise FAILED `archiver not done`.
    - (1) F13 movers (STEP-6).
    - (2) F12 cards (STEP-5), each step in its own transaction.
    - (3) Miss line (STEP-7).
  - A failed step marks the job failed with the step name; earlier steps stand.
  - `--dry-run` writes nothing (DB or vault) and prints the rows plus the line diff (L10).
  - Re-runs are idempotent (unique index / upsert).
  - `job.result = {movers, archived, card_misses, mover_misses, formation_replay, line_action}`.
  - **[amended 2026-09-16 per Astra R1-15]** The archiver precondition identifies **tonight's scheduled 20:30 occurrence** of `com.cobalt.archiver` — label + current ET date + `done` + exit 0 + coherent `started_at`/`finished_at`, never a stale prior-day or earlier manual run. At the 20:05 D1 step tonight's archiver has not yet run, so D1's dry-run proof runs after tonight's archiver completes (or uses the already-proven dev fixture), not at 20:05 itself. Test previous-day-done, same-day-early, running, failed, absent-row and UTC/ET date-crossover cases.
  - **[amended 2026-09-16 per Astra R1-16]** `timeout_s: 1800` bounds a stall (watchdog needs both excess runtime and a stale heartbeat) but does not itself cut a healthy long run off before 21:35. Replay adds an enforced execution deadline with cleanup and no late vault write, leaving a measured margin before the 21:40 backup; work whose bounded demand can't fit inside that margin refuses rather than running long. Test a slow-but-heartbeating collector, a timeout mid-step, and a failed/late archiver.
  - **[amended 2026-09-16 per Astra R1-17]** `--dry-run` runs `as_job(..., skip=True)` (bare `as_job(...)` still writes schema/registration/state) with read-only store calls (no `ensure_schema`), no radar-cache mutation, and `VaultWriter`'s dry-run diff path — a writer-import ban alone doesn't prove no side effect. Test that the full CLI dry-run path performs no DDL/DML/job-stamp/cache/vault write while still printing rows and the line diff.
- **STEP-5 F12 replay + counterfactual R (R4)** — `replay/cards.py`, pure functions over `Bar` lists plus one store:
  - **Candidates:** `aset_sizings` created that ET day whose state history (`card_transitions`) never reached FILLED.
  - **Bars:** `system.bars` i1 for the ticker from `created_at` to 16:00 ET. Missing or stale bars (watermark < 15:59) → the row is not written, the reason is logged as `input_stale`, and the job counts it (loud, never a fake R).
  - **Trigger:** the first bar with `high ≥ entry` (long) / `low ≤ entry` (short) at or after `created_at`. `fill_price = entry`, or `open` if that bar opened beyond entry. No trigger → no row.
  - **`excluded_by`**, the first matching gate in fixed order:
    1. `rule_10`: ≥2 cards FILLED and not CLOSED at `trigger_ts`.
    2. `window`: `trigger_ts` > `_resolve_window_end`.
    3. State at `trigger_ts` from `card_transitions`: WATCH → `unarmed`, PASSED → `passed`, ARMED/TRIGGERED → `not_filled`.
    - `gate_detail` records every gate's evaluation. `trade_count_band` is recorded as `unset` while the tunables are null (§8 item 2) and is never an `excluded_by` value.
  - **Horizon end** = window end if `trigger_ts ≤` window end, else 16:00 ET (decided with veto).
  - **Exit:** walk bars from the trigger bar. The stop is touched if `low ≤ stop` (long) / `high ≥ stop` (short); on the trigger bar itself a stop touch counts (tie → stop). First touch → `exit_price = stop`, `exit_reason='stop'`. Otherwise the close of the last bar ≤ horizon end.
  - `cf_r = (exit_price − fill_price)/|entry − stop| × (+1 long / −1 short)`, rounded half-up to 4 dp once. `mfe_r` = best excursion before exit in the same units.
  - `inputs_sha256` = canonical JSON of {entry, stop, direction, created_at, window_end, horizon_end, bar ts range, bars_watermark, formula_version}.
  - **Formations:** if `cobalt.radar.evaluate` imports (P2 merged), call its replay for the date. Each formation whose trigger fired with no open radar card for (member, def, direction) → `kind='formation'`, `excluded_by='no_card'` (or the card's state per the gates), same R formula. Otherwise log exactly `trade_def replay: not available until S2-P2` and set `formation_replay='unavailable'`.
  - **[corrected 2026-09-19 — `cto-2026-09-18.md` §21 row 2: the shipped entrypoint is `cobalt.radar.evaluate_cli.replay_formations` (called with the day, the pool key and the read-only sources; it returns a `ReplayReport` of `ReplayFormation` rows), and the capability/version marker is `cobalt.radar.evaluate.EVALUATOR_VERSION` (`"s2p2.1"`); `cobalt.radar.evaluate` itself carries the marker and the evaluator, not the replay entrypoint. The build binds to those names (`replay/formations.py`, the E2 chunk); the sentence above is kept as the record of what the plan said.]**
  - **[amended 2026-09-16 per Astra R1-9]** `cf_r`'s numerator uses the actual counterfactual `fill_price` (which may differ from `entry` on a gap-through fill) against the planned `entry`/`stop` risk in the denominator — name the two separately (`planned_entry` vs. `fill_price`) so a gapped fill (e.g. long entry 100/stop 99, gap fill 102, stopped out → −3R, not −1R) isn't mislabeled by the test name. `mfe_r` on the trigger/stop bar itself is documented as a bar-level observation only — within-bar high/low ordering is unknowable from OHLC alone — never a second R formula.
  - **[amended 2026-09-16 per Astra R1-10; corrected by R2-4, R3-2]** The public resolver only reaches its early-close-aware fallback when the card's window text is ambiguous — an explicit window resolving directly to a time (e.g. "16:00") returns that time even on a half-day. STEP-5 resolves the window text through the existing public resolver AND separately fetches that trade date's actual calendar session close: `W = min(resolved window end, actual session close)`. Horizon `H = W` when `trigger_ts ≤ W`; otherwise `H = actual session close` (the ruled after-window-trigger branch — trigger past the window still gets a horizon, bounded by the real close, never left unresolved). `system.bars` i1 timestamps are stamped at bar **start**; a bar is eligible for exit/observation only if its full minute has elapsed by `H` — `bar_start + 1 minute ≤ H` — not merely "started at or before H," which would admit a bar whose prices run past the horizon. A trigger that leaves no eligible completed bar (e.g. triggered inside the final minute before `H`) writes no row, logged `input_stale`, same as missing bars. The selected bar timestamps and this cutoff policy are part of the stored receipt (R1-8). Test: ambiguous prose (fallback path), an explicit-time window on an early-close day (must not run past the actual close), a stop that only occurs in the bar starting at `H` (must be excluded), a trigger at exactly `H`, and an after-window trigger — each asserting the precise eligible-bar set and exit timestamp.
  - **[amended 2026-09-16 per Astra R1-11]** The as-of-trigger state mapping is exhaustive over every `card_transitions` state (including terminal MISSED/EXPIRED with their recorded reason), ordered by `(at, id)` to break same-timestamp ties, and reads `entry`/`stop`/position state as of the trigger time, not current mutable values. `rule_10`'s gate is labeled explicitly as the two-open-position **proxy** for rule 10 (narrower than the full rule text, which also exempts a stop already at the card level) — uncomputed clauses are recorded `unavailable`, never silently treated as passing. Test terminal states, simultaneous transitions, a historically-edited stop, and a position-close boundary.
  - **[amended 2026-09-16 per Astra R1-12]** "Any i1 bars that day" is not evidence of horizon-covering archival: a bar-coverage check verifies the returned bars actually span the required window before a ticker is marked archived/skipped. Manual-card tickers outside both the Lists archive and the top-N movers have no assured bar source; missing/partial coverage is counted and surfaced in `job.result`, the miss line, and smoke — never silently rendered as a zero-miss green run. Test partial-day bars, a wrong-day export, a manual card with no archived bars, and a genuinely complete day with no triggers.
  - **[amended 2026-09-16 per Astra R1-21; corrected by R2-7]** Before implementing the adapter, the build records P2's actual shipped replay contract — the concrete callable/entrypoint under `cobalt.radar.evaluate`, its arguments (date, and any formation/member scope), its capability/version marker, and its typed returned fields (formation timestamp, member id, trade_def id, direction, trigger/stop/window inputs and its retained score-receipt reference) — rather than assuming a formation-returning API sight-unseen (P2's plan specifies a writes-nothing replay CLI over immutable score receipts, not a bare importable function). The `missed` unique-per-subject key (STEP-1) is extended for `kind='formation'` rows to include the formation's trigger timestamp and member id from that recorded contract, so two same-day formations of the same (ticker, trade_def) never collapse into one row. P2 absence logs exactly `trade_def replay: not available until S2-P2`; a present-but-incompatible P2 (capability/version mismatch) fails loud, never silently degrades. An existing open radar card for that (member, def, direction) suppresses the miss. Test P2 absent, P2 present with two distinct formations persisting under the extended key, existing-card suppression, and that the cf-R computation matches STEP-5's one formula from the same retained inputs, using P2's evaluator directly rather than a second implementation.
  - **[corrected 2026-09-19 — `cto-2026-09-18.md` §21 row 2: "under `cobalt.radar.evaluate`" above reads, as shipped, `cobalt.radar.evaluate_cli.replay_formations` (the callable) and `cobalt.radar.evaluate.EVALUATOR_VERSION` (the capability/version marker); the returned type is `ReplayFormation`. Same correction as under STEP-5's Formations bullet.]**
- **STEP-6 F13 movers + benchmark (R5)** — `replay/movers.py`:
  - `MoversCollector` behind the collector interface (L9) reuses `finviz_get` + `parse_screener_csv` (required headers `Ticker, Change, Asset Type, Volume`) and `process_bucket(radar.finviz_max_rpm)`.
  - Two requests (`o=-change`, `o=change`, `v=152`, `c` from `radar.yaml`), cached under `data/radar-cache/<date>/movers-<side>-HHMMSS.csv`.
  - Settings `radar.benchmark` are validated by a Pydantic model in `settings/models.py`, NOT added to `SETTING_KEYS` (F14). An absent or malformed key → STEP-6 FAILED loud.
  - Upsert the top `top_n` per side into `movers_daily` (`export_sha256` = raw bytes).
  - Archive i1 bars through `archiver.collector.fetch_bars` + `BarStore.upsert_bars` for each top-N ticker lacking that day's i1 bars. Set `bars_archived`. A per-ticker failure is logged and counted, the job still fails at end (archiver semantics).
  - **Benchmark:** movers with `|change_pct| ≥ min_move_pct` and `asset_type` equity, versus `radar_membership` episodes for that `trade_date`.
    - Any admitted episode → in play, no row.
    - Else a never-admitted episode → `excluded_by` = its value (`config_cap`/`not_equity`/`screen_inactive`/`manual`).
    - Else → `not_in_any_source`.
    - Write `kind='mover'` rows with `pool_member_id` (when an episode exists), `mover_id` and `gate_detail {change_pct, side, rank, episodes}`. No cf R (no trigger without a trade_def; formations cover that post-P2).
  - **L53 total demand:** the demand computation in `radar/notes.py`/`propose.py` adds the replay consumer (2 + ≤ 2×top_n requests in its window). The archiver window is excluded only by the STEP-4 precondition. The test proves refusal when the sum exceeds the ceiling while replay alone passes.
  - **[amended 2026-09-16 per Astra R1-13; corrected by R2-5]** ESCALATE-4's "archiver excluded by precondition" does not satisfy L53: the total-demand computation is ONE sum across every Finviz consumer with a defined rpm conversion — radar screens/lists/polls, P2's daily/context reads when merged, the nightly archiver's own request count, and replay's movers + mover-archive requests — over their actual overlapping/non-overlapping time windows (non-overlap contributes zero only where the windows are proved disjoint; the archiver is never omitted just because a job precondition serializes it). A single named shared gate (`check_total_demand()` or equivalent, sited in `radar/notes.py`) is called, and refuses before the request, at every pre-request call site: radar's `load_sources`, the archiver's collector/runner path, and replay's `movers.py` (both the screener exports and `fetch_bars`, which is wrapped in the same `TokenBucket`/process bucket the screener uses — today it bypasses it entirely). The refusal test exercises the actual call sites, not just the pure sum function, proving refusal when the total exceeds the ruled ceiling while any single consumer alone would pass. The existing ceiling/cadence values are unchanged pending Dejan's ruling. **ESCALATE 4 stays an open deployment gate:** the archiver's own measured demand (~42 rpm) is already above the unchanged ceiling (40 rpm) — this amendment fixes the proof design, it does not by itself make the plan deployment-ready; deployment is blocked on either a conforming measured-demand proof under the ceiling or Dejan's ruling on the number.
  - **[amended 2026-09-16 per Astra R1-14]** `radar.benchmark` cannot be applied by the settings loader as specified: today's loader reads two fixed legacy YAML shapes, diffs exactly the required `SETTING_KEYS`, takes a source-file argument, and has no `--sha256` flag. STEP-6 either extends the loader with an explicit optional-key round trip (validated by the new Pydantic model, hash-reviewed, never added to `SETTING_KEYS`) or names the exact existing seam it reuses — either way the load path is real, not merely "a Pydantic model exists." Dejan's apply is scheduled outside `market_reset` and before 21:05 (not at 20:05, which sits inside the pause per `session/guard.py`), without a radar restart. Test the optional-key round trip, an absent/malformed `radar.benchmark`, a hash mismatch, and refusal inside the pause.
  - **[amended 2026-09-16 per Astra R1-20; corrected by R2-1]** A same-`trade_date` rerun with changed top-N or settings deactivates (`active = false`, never deletes — see STEP-1's R2-1 versioning) yesterday's no-longer-selected `movers_daily` rows for that reconciliation, not just upserts new ones — one dated run identity per side reconciles the active (`active = true`) benchmark set transactionally while retaining every prior row's replay inputs for audit and never breaking `missed.mover_id`'s FK. A historical `--date` run requires the day's exports/bars already retained, or refuses (it never relabels a fresh live fetch as historical). Multiple never-admitted exclusion episodes for the same mover pick a deterministic one (most-recent) for `gate_detail`; a ticker appearing on both the gainers and losers side in one run resolves to one `missed` row via the unique-index key. Test a changed top-N/settings rerun, a rerun after partial failure, a ticker on both sides, and an unavailable historical date.
  - **[amended 2026-09-16 per Astra R2-3]** Replay's transaction boundary is side-scoped, not a single cross-side transaction: one USER-side commit phase covers `trader_settings` reads, `"user".missed`/`"user".picks`-adjacent writes, receipts and vault audits; one SYSTEM-side commit phase covers `movers_daily`/`bars`/job bookkeeping — no connection or role ever spans both inside one transaction (ADR-0008: SYSTEM cannot read cards/settings or write picks/missed). STEP-4's "each step in its own transaction" means each of STEP-5/6/7 owns its own per-side commit boundary(ies) in that fixed order, with recovery keyed to the receipt-owned replay-run identifier (R1-8) rather than assumed atomicity. Test actual side-role permissions on each phase and a retry/resume after a commit boundary, not just full-success and full-failure.
- **STEP-7 miss line (R5)** — `replay/line.py`:
  - Body, one line: `Misses YYYY-MM-DD: cards N (unarmed a · passed b · not_filled c · window d · rule_10 e) · cf-R Σ ±x.xR, n=N · movers ≥ m%: K not in play — T1 +12.3% not_in_any_source · T2 −9.8% config_cap · T3 … (+J more) · formations: unavailable until S2-P2`.
  - n is attached to the sum. An average appears only when n ≥ 30, else `avg: insufficient data (n<30)` (L8).
  - `VaultWriter.upsert_unit(drc_path, "drc-misses", "miss_line", body, placement=after drc-rules section)`, the one L28 path: versioned, diffed, human-wins.
  - DRC path from `prefill.yaml` `review_dir` + `drc_filename_pattern`. Absent → FAILED `DRC note absent — prefill-drc owns creation`, no create.
  - The unified diff goes to the job log and `job.result`.
  - **[amended 2026-09-16 per Astra R1-18]** Placement is zero-width, inserted after `find_section("drc-rules").close_line` (the section's closing marker, not wrapping or overwriting human content), using the writer's existing safe-append fallback; `upsert_unit` never calls `create_if_absent` from replay (L28: existing-note-only). The hub's `requires_vault` test reads the live DRC shape only — it never becomes a live write test; writes/diffs/versioning/restore are proved on a dev-vault copy. §4 gains tests for first-unit placement, a missing anchor, a malformed marker, a human override, a sync-revert, and an mtime conflict, each asserting every human byte outside the unit is unchanged.
- **STEP-8 docs:** DevDocs for every new or changed .py; `BACKLOG.md`/kanban row; ADR-0009-adjacent note → **ADR-0010 "missed corpus + counterfactual R + picks"** (formula, gate order, horizon rule). `configs/cobalt/generated.yaml` gains nothing (the smoke report is committed by the hub under L51(2)).
- **STEP-9 `cobalt smoke s2` (R6):**
  - `src/cobalt/smoke/` (`cli.py`, `checks.py`, `models.py`) + config `configs/cobalt/smoke/s2.yaml` (Pydantic schema; each check = id, title, kind ∈ {launchctl, sql(side), http, log_grep, job_row, vault_unit, cli}, command/query, expect rule, optional `known_if`).
  - Every probe is read-only: DB reads via `cobalt db query`'s read path, a vault read via `find_section`, no writer imports (sentinel-tested).
  - Output: the verdict table (check | verdict | evidence) + OVERALL (RED any ERROR · AMBER any FAIL · GREEN; KNOWN never lowers) to stdout and `docs/40 - DevDocs/reports/s2-smoke-<date>.md`. S2 done = GREEN only.
  - **Checklist (the spec; the hand fallback runs these verbatim):**

  | # | Check | Expect |
  |---|---|---|
  | K1 | `com.cobalt.radar` launchctl | running |
  | K2 | `radar_pool` primary | state valid for the session clock; `members ≤ cap`; `failed_stage` null or KNOWN-listed |
  | K3 | membership today | admitted rows scanned after the deploy have non-null `rank_metric` + `rank_value` |
  | K4 | sheet | `/api/health` 200, `GET /` 200, `GET /radar` 200 with value column, `/api/radar/pool` 200 |
  | K5 | P2 seam | latest `radar_score_run` `complete`, `failed_stage` null; `radar.cards_enabled` value reported (dark = PASS). P2 not deployed = FAIL |
  | K6 | picks | every FILLED card since the P4 deploy has exactly one `picks` row (`cobalt cards picks --date` exit 0) |
  | K7 | replay job | `com.cobalt.replay` last weekday `done`, exit 0; `job.result` present |
  | K8 | missed | last trading day: every `kind='card'` row has `excluded_by`, `cf_r`, `inputs_sha256`; counts equal `job.result` |
  | K9 | movers | `movers_daily` last trading day rows = 2 × `top_n` (or fewer with export evidence); `bars_archived` true for all |
  | K10 | miss line | DRC note of the last trading day contains unit `drc-misses/miss_line`; `vault_writes` row present |
  | K11 | heartbeat | last beat ≤ 20 min; database, sheet HTTP OK |
  | K12 | prefill | `prefill-daily` + `prefill-drc` last run `done` |
  | K13 | archiver | heartbeat archiver OK, last run ≤ 30 h |
  | K14 | backup | `com.cobalt.backup` last run `done` |
  | K15 | alerts | heartbeat summaries sent at the last 07:00/16:30 slot (notify log) |
  | K16 | cards-expire, daymode-propose | last weekday runs `done` |
  | K17 | `cobalt validate` | exit 0 |
  | K18 | `cobalt jobs` registry vs launchd | no drift |

  - **[amended 2026-09-16 per Astra R1-19, R1-24]** K3 is corrected: non-null `rank_metric`/`rank_value` is required only for admitted rows actually re-scanned after the deploy — a frozen HOLD row keeping its pre-deploy NULL, or a genuinely missing metric, is not a K3 failure; only a write-path defect is. The checklist as written is not yet executable verbatim: each K-check needs its exact read-only command/query, expected shape, date/cutoff, and comparison rule before the hand fallback can run it blind — written out at build time from this corrected spec. K13 uses the archiver's existing cadence-aware freshness probe, not a flat 30h cutoff. K8 additionally requires zero missing/stale-input candidate coverage (STEP-5/R1-12), not just presence of the three named columns. K6 requires at least one post-deploy FILLED transition to be meaningful — an empty candidate set reports `KNOWN: no fill yet`, never a vacuous PASS.

## §3 Fixtures (L45 real-shape, hub-cut from prod reads; personal attribution stripped; no user strategy note leaves the vault)

| File | Source | Transform |
|---|---|---|
| `tests/fixtures/replay/cards-day.real-shape.json` | `aset_sizings` + `card_transitions` for the STEP-0 day (unfilled crossed, unfilled uncrossed, filled, passed, ≥2 filled overlapping for rule_10) | `user_id` → 1; all timestamps shifted to a fixed synthetic day with intraday offsets kept; free-text reasons → `"<reason>"` |
| `tests/fixtures/replay/bars-day.real-shape.json` | `system.bars` i1 for those tickers that day | same shift |
| `tests/fixtures/replay/movers-gainers.real-shape.csv`, `movers-losers.real-shape.csv` | one hub-side unfiltered export per side (market data), trimmed to 60 rows | header kept byte-exact; no filter string anywhere |
| `tests/fixtures/replay/membership-day.real-shape.json` | P3's `panel-pool.real-shape.json` reused, plus the STEP-0 day's episodes for the Tesla-class ticker's absence | P3 transforms |
| `tests/fixtures/radar/pool-metrics.real-shape.csv` | one cached screen CSV from `data/radar-cache/2026-09-15/` | trimmed to 20 rows; filename generic |
| DRC note | **hub-local, never committed** (P2 fixture ruling B): `requires_vault` test against the live DRC template shape; committed tests use `configs/cobalt/templates` DRC render as in `test_prefill_drc.py` | — |

## §4 Tests (acceptance sentences → names)

| Area | Tests |
|---|---|
| F3 (charter: "DRC shows his pick and Cobalt's rank for every card") | `test_fill_writes_exactly_one_pick_row_in_the_fill_transaction` · `test_pick_row_snapshots_pool_rank_metric_and_value_at_pick_time` · `test_pick_for_ticker_not_in_pool_records_not_in_pool_and_fill_succeeds` · `test_degraded_or_missing_pool_row_writes_named_nulls_never_refuses_fill` · `test_pick_insert_failure_rolls_back_savepoint_only_fill_commits_banner_and_log` · `test_picks_cli_reports_savepoint_gap_as_missing` · `test_card_score_rank_unavailable_before_p2_named_in_score_basis` · `test_picks_cli_lists_pick_and_rank_for_every_filled_card_and_exits_1_on_missing` |
| Value (R1) | `test_rank_value_written_on_retain_exclude_insert_with_ranking_metric` · `test_screen_override_metric_value_recorded_under_override_name` · `test_list_sourced_value_is_max_across_lists` · `test_hold_keeps_prior_value` · `test_pre_deploy_null_value_renders_dash` · `test_pool_view_shows_value_beside_metric_name` |
| F12 (charter: "nightly report lists misses with the gate/variable that excluded them") | `test_nightly_report_lists_misses_with_the_gate_or_variable_that_excluded_them` · `test_trigger_on_unarmed_card_is_missed_row_unarmed` · `test_gate_order_rule_10_then_window_then_state` · `test_trade_count_band_unset_recorded_in_gate_detail_never_excluded_by` · `test_uncrossed_card_writes_no_row` · `test_cf_r_stop_touched_first_is_minus_one` · `test_cf_r_same_bar_stop_and_trigger_stop_wins` · `test_cf_r_gap_through_fills_at_bar_open` · `test_cf_r_horizon_end_close_window_or_1600` · `test_mfe_r_stored_as_observation` · `test_every_missed_number_replays_from_stored_inputs` (L57) · `test_stale_bars_write_no_row_and_count_input_stale` · `test_formation_replay_unavailable_logs_exact_line_no_rows` · `test_rerun_is_idempotent` |
| F13 (charter: "a Tesla-class mover absent from the pool appears in the miss line with excluded_by") | `test_tesla_class_mover_absent_from_pool_appears_in_miss_line_with_excluded_by` · `test_admitted_mover_is_not_a_miss` · `test_never_admitted_episode_supplies_its_excluded_by` · `test_movers_archived_regardless_of_watchlist` · `test_benchmark_settings_absent_or_malformed_fails_loud` · `test_benchmark_key_not_in_required_setting_keys` · `test_total_finviz_demand_refuses_when_sum_exceeds_ceiling_while_replay_alone_passes` (L53) |
| Miss line (L28/L8) | `test_miss_line_upserts_drc_misses_unit_idempotent` · `test_human_edit_to_miss_line_wins_and_logs_override` · `test_drc_note_absent_fails_loud_and_creates_nothing` · `test_miss_line_sum_carries_n_and_average_insufficient_below_30` · `test_dry_run_writes_nothing_and_prints_diff` · `requires_vault` (hub): the live DRC shape accepts the unit placement |
| Job | `test_replay_refuses_when_archiver_not_done_today` · `test_step_failure_names_step_and_earlier_steps_stand` · `cobalt validate` green with the new row + plist |
| Smoke (R6) | `test_smoke_checks_load_through_schema_bad_file_crashes_with_line` · `test_verdict_red_on_error_amber_on_fail_known_never_lowers` · `test_smoke_imports_no_writer_and_calls_no_write` (sentinels) · `test_smoke_writes_report_file_with_table` · one test per K-check kind on fakes |
| Tenancy | placement: `movers_daily` SYSTEM, `picks`/`missed` USER; `user_id` NOT NULL + FK + GUC default on both; rollback order 0009 → 0008; side lint covers the new stores |
| L45 | leak scan unchanged and green; fixtures carry no filter token and no real date |
| DB (hub, `requires_db`, cobalt_dev) | 0008/0009 up/down; fill → pick end-to-end; `cobalt replay nightly --date <fixture day>` end-to-end on cobalt_dev + dev vault DRC |
| Suite | full suite green; `cobalt validate` green |

## §5 RESTARTS

- Plan commit (this run): docs only → `RESTARTS: none`.
- Build range: `cobalt jobs restarts main..HEAD`. Expected:
  - `com.cobalt.radar` (`radar/pool.py`, `radar/store.py` via `cobalt.cli`)
  - `com.cobalt.aset` (`cards/store.py`, `cards/picks.py`, `aset/radar_panel.py`)
  - `com.cobalt.replay` plist new → bootstrap, no restart
- One-shots pick up new code on their next run. No resident `reads:` file changes (`radar.benchmark` lives in `trader_settings`, read per run). Any other label, or UNCLASSIFIED → ESCALATE before merge (L42).
- **[amended 2026-09-16 per Astra R1-22; corrected by R2-6]** The restart classifier today drops a brand-new one-shot plist from its restart-label output entirely; STEP-4 makes the plist's derived action explicit — `com.cobalt.replay` bootstraps once. The P4 deploy itself restarts `com.cobalt.radar` and `com.cobalt.aset` as derived above (D1's kickstarts, for the imported changes) — the earlier amendment's point is narrower: only the nightly **21:05 replay invocation itself**, and reading the per-run `radar.benchmark` setting, imply no *additional* radar restart beyond D1's. `configs/cobalt/smoke/s2.yaml` is not a resident `reads:` file and may classify UNCLASSIFIED → its classification is resolved (or explicitly ESCALATEd) before merge, per L42. The actual `cobalt jobs restarts main..HEAD` derivation table for the final build range is attached to the build report.

## §6 Build, deploy, acceptance, rollback

- **Build (after Astra ≤3 rounds, after the STEP-0 rebase):**
  - The hub runs STEP-0 and the Codex meter probe (L47).
  - **[amended 2026-09-16 per Dejan's 2026-09-15 Codex meter ruling — `areas/cobalt-houses.md`]** Opus 5 headless (`claude -p --model claude-opus-5`, L50), launched by the Sonnet hub, runs STEP-1..9 in the worktree without DB credentials (L41 interim); never auto mode on a write path (L29). Sol high returns as the L47 alternative only after the weekly reset (09-19), by ruling.
  - The hub runs DB tests, the dev replay, the leak scan and the full suite, verifies the artifact (L35), and makes one commit (L46).
- **Deploy (L43 one per evening, not the P2 D1 evening; L54 rebase + `--ff-only` from `~/cobalt`):**
  - **STEP-D1, 20:05 ET inside the 20:00–21:00 pause (radar idle):**
    1. Snapshot: `pg_dump` of `system.radar_membership`, `"user".aset_sizings`, `card_transitions`, `trader_settings` → `data/backups/pre-s2-p4-<date>/`.
    2. `git tag pre-s2-p4`; merge; `cobalt db migrate --allow-prod` (0008, 0009).
    3. `launchctl kickstart -k` `com.cobalt.radar`, then `com.cobalt.aset`.
    4. `launchctl bootstrap` `com.cobalt.replay`.
    5. Dejan applies `radar.benchmark` via `cobalt settings load --apply --sha256 <reviewed>`.
    6. `cobalt replay nightly --dry-run` (today).
  - **Smoke D1:** `/radar` 200; sheet `/` 200; `cobalt validate` green; heartbeat green at the next beat; dry-run output in the report.
  - **21:05 same evening:** the first live replay run (archiver done ~20:53). Verify missed/movers rows + the DRC miss-line diff; paste the diff into the report (L28).
  - **Next trading day:** the first fill writes a pick row; the 21:05 run repeats.
  - **S2 smoke (≥09-22 evening, after P2 D1 and one full P4 day):** the hub runs `cobalt smoke s2`, or the checklist by hand if the command did not ship (R6); report `s2-smoke-<date>.md`, committed under L51(2). GREEN = S2 done.
- **Acceptance:**
  - F3: `cobalt cards picks` lists pick + pool rank/value for every FILLED card.
  - F12: the nightly miss line lists misses with `excluded_by` + cf R, n attached.
  - F13: a real absent mover ≥ threshold appears with `excluded_by`.
  - Value column visible on `/radar`.
  - Dejan reads one miss line and confirms it matches his day.
- **Rollback — one domain per write surface, each with its snapshot:**
  1. User tables: `cobalt db migrate --rollback --down-to 0008` (drops `picks`, `missed`); restore from the D1 dump if needed.
  2. System: `--down-to <version before 0008>` (drops `movers_daily`, the two columns); dump restore of `radar_membership`.
  3. Settings: Dejan re-applies the D1 `trader_settings` snapshot (or removes `radar.benchmark`).
  4. Vault: `cobalt vault restore --write-id <id>` per `drc-misses/miss_line` write.
  5. Job: `launchctl bootout gui/$(id -u)/com.cobalt.replay`; `enabled: false` in the registry revert.
  6. Code: `git revert` of the merge range; restart `com.cobalt.aset` at once (outside 09:00–16:00), `com.cobalt.radar` in the next 20:00–21:00 pause.
  7. **[amended 2026-09-16 per Astra R1-23]** Rollback selection is by migration number, not applied state — a repeated `--down-to 0008` on an already-rolled-back tree is a no-op only if 0009's DDL is itself idempotent to re-reverse, and "version before 0008" must never be read as reversing P2's 0006/0007 once P2 has landed; the explicit boundary is `--down-to 0007 --allow-prod` (numerically correct whether or not 0007 exists yet), never an implicit "whatever came before." Before any schema reversal: stop `com.cobalt.replay` and the affected residents at an L43-safe time, snapshot current P4 tables/receipts and affected DRC audit units (P2's 0007 rollback deletes radar cards that picks/missed now reference with NO ACTION FKs — that dependency is resolved or the P2 rollback is blocked, never silent data loss), and note a D1 dump cannot restore picks/missed rows created after D1. Test the full rollback/reapply sequence on populated dev data in both P2-before-P4 and P4-before-P2 merge orders (L54), preserving post-D1 manual data throughout.

## §7 Carried

- **S2-P2 (if it merges after P4):** its fill path keeps `record_pick`; with `card_score` columns present, `score_basis` flips to `card_score` without code change (tested by a column-present fixture). Its `radar evaluate --replay` becomes STEP-5's formation source.
- **S3:** DRC rendering of pick-vs-rank and the miss corpus (F14); `legs`/`fills` tables; formations for the other five trade_defs; the cf-R 1-bar-trail variant as a shadow column only if the curve/exit tribunal asks.
- **Ops:** radar-cache retention (§8 item 3); archiver rate vs ceiling (§8 item 4).
- **Dejan:** values for `radar.benchmark {top_n, min_move_pct}`; `daymode.trade_count_band` values.

## §8 ESCALATE (4)

~~1. Pick row inside the fill transaction~~ → CLOSED at approval (R2 amended: savepoint, the fill always commits, red banner + MISSING).
2. **`trade_count_band` is unset** (`tunables.yaml:316-333`, value null). The gate is recorded as `unset` in `gate_detail` and never excluded by, so the ladder's `trade_count_band` exclusion is empty until he sets the band. **[amended 2026-09-16 per Astra R1-25]** Sound interim handling, confirmed: `unset` is never invented or used as an exclusion. Noted for when he does set it — the band is a day-mode proposal tunable today, not a wired replay gate; loading numbers alone will not activate `rule_10`'s band clause, which needs its own replay-gate semantics/schema/tests specified when ruled.
3. **Radar-cache retention mismatch:** 7-day config, only 2 days on disk (F4). R1 does not depend on it, but it is unexplained data loss. Ops ticket. **[amended 2026-09-16 per Astra R1-26]** Corrected framing: 7 days is a maximum retention window, not proof 7 days of collection existed, and the cache folder's date is the fetch instant's date, not confirmed ET — "unexplained data loss" is not yet established, only a retention/inventory discrepancy pending the Ops ticket's investigation. R1 (§R) does not depend on this either way; replay retains its own inputs durably (L57) rather than relying on this expiring cache.
4. **Archiver vs Finviz ceiling:** 975 requests in ~23 min ≈ 42 rpm against `radar.finviz_max_rpm` 40, and the archiver is not in the total-demand computation (L53: every consumer). P4 keeps replay out of the archiver window by precondition; the archiver's own count needs a ruling. **[amended 2026-09-16 per Astra R1-13]** A job precondition serializing replay after the archiver does not satisfy L53's "every consumer" requirement on its own — see STEP-6's amended total-demand computation, which now sums the archiver, radar, P2 (when merged), and replay/movers together with a refusal test. The ceiling-value ruling itself stays OPEN for Dejan.
5. **Schedule:** Astra ≥09-16, Sol build ~1 day, D1 on an evening P2 does not use (≥09-18), one full P4 day, smoke ≥09-22. Scope-out order if it slips (each a ruling, never a date move): the smoke command → the hand checklist (R6 fallback); then the movers i1 archive (keep the benchmark diff + miss line); then formations (already conditional on P2). **[amended 2026-09-16 per Astra R1-27]** Corrected for amended R7: the build lane is Opus 5 headless, not Sol (the "Sol build ~1 day" estimate above is stale against the 2026-09-16 builder amendment — see §R/header/Cost). Conditional scope handling otherwise sound: each further scope cut past the listed order is its own recorded ruling, never silent; dropping the movers i1 archive leaves F13 incomplete unless that acceptance criterion is explicitly rescoped alongside the cut.

## Cost

- This plan: Opus 5, ~$5 API-equivalent.
- Build: **[amended 2026-09-16 per Dejan's 2026-09-15 Codex meter ruling — `areas/cobalt-houses.md`]** Opus 5 headless, one ~3 h session, ~$25–35 API-equivalent.
- Review: Astra ≤3 rounds (Codex).
- Hub (Sonnet): fixtures, DB proofs, deploy, smoke, ~$4.
- Fallback: Opus 5 headless, ~$25–35 API-equivalent.

