# 10 - Decisions

Architecture Decision Records (ADRs) for the new core (`src/cobalt/`).
One file per decision, per `CLAUDE.md`'s non-negotiable ("ADR per
decision"). Empty as of the 2026-08-26 D6 restructure — the pre-
strangler ADRs live in the gitignored playground vault
(`docs/0 - Projects/Cobalt/00 - Master Plan/ADR/`) and are untouched by
that restructure (live/uncertain prod surface; see `docs/00 - Project/BACKLOG.md`
and `CLAUDE.md`'s strangler rules). The first ADRs to land here are the
ones TRIAGE.md's design-session register calls for (data-model,
embedder, vault structure, playbook schema, HITL rebuild, …).
