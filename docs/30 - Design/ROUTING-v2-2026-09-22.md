# ROUTING v2 — 2026-09-22 (derived from the proposal and the round-1 rulings)

- **Input proposal:** `docs/30 - Design/ROUTING-PROPOSAL-2026-09-22.md` (Anthropic house, Opus 5.5, seat `routing-propose-0922`).
- **Authority:** `cto-2026-09-22.md` R81 (the routing tribunal runs this week, its own lane; the cluster FROZEN until it rules) · R4 ("Laws are all mine" — the FINAL is his to rule) · R13 (Astra reads the derived FINAL Sat 09-26 when the Codex meter returns) · **R109** (derive row: "Make all Opus 5.5 for now" → `derive seat: claude-opus-5-5`; the Anthropic tribunal seat `seat model: claude-opus-5-5`).
- **Round-1 rulings, every seat that ruled:**
  - Grok (xAI): `scratch/tribunal-bars-0920/routing-tribunal/r1/grok-ruling.md` — `TRIBUNAL R1: ADOPT AFTER restore dropped clauses; T5 waits on T6`.
  - Gemini (Google): `…/r1/gemini-ruling.md` — `TRIBUNAL R1: ADOPT WITH AMENDMENTS`.
  - Hub collate + file-check: `docs/40 - DevDocs/reports/routing-tribunal-2026-09-22.md` (`ROUTING TRIBUNAL R1 DONE`).
  - Anthropic seat (R109, `claude-opus-5-5`): `docs/40 - DevDocs/reports/routing-tribunal-fable-r1-2026-09-22.md` — `ADOPT AFTER write-path scope ruled, bare § refs fixed, L33 profile carried, T6 run blind`.
  - Astra (OpenAI): `METER — ASTRA PENDING (R13)`. **This v2 is ASTRA PENDING (R13).** The OpenAI house has not ruled on a design that seats its own houses (seat table rows 9–12).
- **Fold table:** `docs/40 - DevDocs/reports/routing-tribunal-derive-2026-09-22.md` `## Fold table` (every `[F-nn]` tag below).
- Derived by seat `routing-tribunal-derive-0922` (`claude-opus-5-5`). This seat is the Anthropic house, which proposed this design and ruled round 1. It holds a side and recommends nothing (L37, L26).

**PROPOSED LAW TEXT — NOT LAW until Dejan rules it (R4); the desk folds LAWS.md after his ruling (L58).**

## Scope: two subjects share the word "routing" [F-01]

Grok's T1 wording, verbatim (`grok-ruling.md:7`):

> ENGINE routing is every model call Cobalt's code makes (L5; L22's two planes; L24 rung 3). SEAT routing is which house and model sits each agent session that builds, checks or designs Cobalt (L26, L29, L49), with L24's rungs as a cost order only. L25 binds both an engine lane and a seat: its fallback chain, reason class, and "exception handler, not a route" are not parked under ENGINE.

## Proposed law text, entry by entry

### L5

**Current (LAWS.md:47-49)**
> ### L5 Routing law (ruled 08-22) — under review, routing tribunal
> Every LLM call goes through the routing layer; out-of-band calls (embeddings, extractor) are routing bypasses and were ruled accordingly.
> — TRIAGE:16

**Proposed v2** [F-02] (the proposal's §1.1, adopted by Grok, Gemini and the Anthropic seat)
> L5 Engine routing (ruled 08-22; amended <date>). Every model call Cobalt's CODE makes (inference, embedding, extraction, any future call) goes through ONE model-access module. A direct call to a model endpoint from any other module is a defect, killed on sight (L3). Agent sessions that build, check or design Cobalt are not engine calls; their house and model come from the seat table (L26), under L29.

### L21

**Current (LAWS.md:124-126)**
> ### L21 Phase-1 model doctrine (ruled 08-28)
> Appropriate intelligence for appropriate task, local when available. Hot-swap purity = Phase 2; never blocks today's progress.
> — LEDGER:21

**Proposed v2: unchanged** [F-25]

### L22

**Current (LAWS.md:128-130)**
> ### L22 Two-plane model access (ruled 08-29/31)
> FAST WIRE (LiteLLM/direct, single-shot) for pipeline inference, memory machinery, council briefs; CHASSIS (Agent SDK sessions) for agents that act with tools. Coexist by task shape; they meet only at the local lane (LiteLLM proxy-mode adapting mainframe for SDK sessions). Economics: Max subscription and Anthropic API are separate meters — SDK sessions on claude-login auth = zero marginal cost; LiteLLM→Anthropic API = paid. SDK chassis = Claude-on-subscription + local-via-proxy only. Cobalt code is the hub (reaffirmed 09-07, LEDGER:1038).
> — LEDGER:23

**Proposed v2: unchanged** [F-12]. The proposal's one-phrase edit ("Max subscription and Anthropic API" → "a house's subscription and its API") was not taken. Its only stated ground was L31, and the hub's file-check found L31 does not cover law text. That edit is `## OPEN for Dejan` item O-10.

### L23

**Current (LAWS.md:132-135)**
> ### L23 Local-first law (ruled 08-29/31)
> Local trumps cloud even when cloud is free — continuity rationale (Gemini outage 08-28 killed a morning briefing). Every lane local can serve runs local-first with cloud fallback; judgment lanes cloud-first with loud degraded-mode on outage. LOCAL = first candidate for every task → cheapest sufficient step up.
> — LEDGER:24
> Status note (09-13, not law text; O19): mainframe gate — local lane assignments earned via bake-off; bake-off still queued (LEDGER:1143).

**Proposed v2** [F-09] (the proposal's §1.7 L23 sentence, adopted by Gemini and the Anthropic seat; Grok's longer wording is O-18)
> ### L23 Local-first law (ruled 08-29/31; amended <date>)
> Local trumps cloud even when cloud is free — continuity rationale (Gemini outage 08-28 killed a morning briefing). Every lane local can serve runs local-first with cloud fallback; judgment lanes cloud-first with loud degraded-mode on outage. LOCAL is the first candidate ASSESSED for every task class: it is assigned where the seat table records it passing, and every other class records why not. [F-09]
> — LEDGER:24
> (Status note removed and replaced by the seat table's local rows. [F-09])

### L24

**Current (LAWS.md:137-139)**
> ### L24 Three-rung economics (ruled 08-29/31) — routing sentence under review, routing tribunal
> (1) local = free, first · (2) subscription-agent FEDERATION = free — async via dead-drop (Grok Bot etc.) and sync via in-session CLI bridges for code work · (3) metered fast-wire APIs = sync-only, rare, bounded, cost-footered. Pay-per-token = narrowest rung.
> — LEDGER:25

**Proposed v2** [F-03] (Grok's T3 wording, verbatim, `grok-ruling.md:31`)
> L24 Three-rung economics (ruled 08-29/31; amended <date>). The rungs are a COST order, not an assignment: (1) local = zero marginal cost · (2) subscription seats = prepaid and bounded by a weekly meter, each meter a monitored resource (L25); async dead-drop and sync in-session CLI bridges stay lawful shapes on this rung · (3) metered APIs = paid per token, engine calls only, sync-only, bounded, cost-footered, the narrowest rung. Which seat runs a task is the seat table's row for that task class (L26). A rung number never fills a row that has no PASS, and cost never overrides a ruled row. [F-03]

### L25

**Current (LAWS.md:141-145)**
> ### L25 Failover doctrine (ruled 08-29/31; amended 09-09)
> Every intelligence lane carries a config-defined fallback chain — Claude → next-best house (headless, task-shape-routed) → LOCAL as the owned always-up floor (also always-on verifier/triage). Degradation loud at every hop. Local-SPOF question parked. Phase 1: cross-house seats NOT load-bearing — advisors and fallbacks only; load-bearing status earnable later by explicit ruling. Quota extension: token-limit exhaustion = outage class — routed, not a crisis; usage ledger exposes quota as a monitored resource; CoS sheds load down the chain proactively and loudly (compute budget enforced like the trading risk budget). Deterministic pipeline burns zero Claude tokens.
> [amended 09-09] Cloud fallback is an exception handler, not a route: permitted only for an UNFORESEEN local failure (crash, host down, unrecoverable timeout) and bounded: every fallback logs a reason class (crash / timeout / capability / quality), is counted per class in the seat-usage report, and turns the heartbeat AMBER when a class recurs on the same day. A recurring class becomes a fix ticket with an owner before the next sprint; it never stays a route. A KNOWN local defect never gets a cloud bypass — fix local, or move that task class to cloud explicitly in ADR-0008's bake-off table, by ruling, not by fallback. Disaster continuity (host dead) is the one open-ended case, bounded by the rebuild. L23 is the directive; L25 is its exception handler, not its replacement.
> — LEDGER:26, :29
> Status note (not law text): the seat-usage report's reason-class column is a build item, still owed (LEDGER:1177).

**Proposed v2** [F-10]. One phrase only: "ADR-0008's bake-off table" → "the seat table". All three seats adopted this, and the hub's check HOLDS: `docs/10 - Decisions/` has no bake-off table. The rest of the entry, including the status note, is unchanged. The first-hop edit ("Claude → next-best house" → "the seat's house → the next row the seat table names") was not taken [F-11]. Its ground was L31, which does not hold for law text. That edit is O-10.
> … A KNOWN local defect never gets a cloud bypass — fix local, or move that task class to cloud explicitly in the seat table, by ruling, not by fallback. [F-10] …

### L26

**Current (LAWS.md:147-149)**
> ### L26 House-agnostic routing (ruled 08-29/31) — under review, routing tribunal
> Appropriate intelligence for appropriate task applies across houses — task classes assigned by measured evidence (bake-offs, slice reviews, cost-ledger token-per-outcome), recorded in routing config with rationale; Claude's conflict of interest neutralized by evidence-cited routing (Claude recommends against Claude when measurements say so). Assignments re-tested as local models improve.
> — LEDGER:27

**Proposed v2** [F-13]. This is the proposal's §1.3 text. Grok accepted it in (b), where the dropped "as local models improve" trigger is restored by F-08's trigger (e); Gemini did not address L26. **Form defect, named:** the text carries bare "(§4)" and "§5", and LAWS.md:12 says the fold refuses a bare §-reference. Its "(L31)" pointer also over-reaches L31's text (hub check). The Anthropic seat's fold-ready wording is option B of O-11. The desk cannot fold A as written.
> L26 House-agnostic routing (ruled 08-29/31; amended <date>). Right intelligence for the right task, across houses. Every seat assignment is ONE row of the seat table: task class, house, model id, meter, evidence, date, re-test trigger. Evidence is one of two kinds, named on the row: MEASURED (a trial report, a bake-off, a check record, a cost-ledger figure, each with its n) or RULED (his ruling, cited by date and R-number). A ruled row stands (Dejan rules). It is marked ruled, never passed off as measured. The assignment key is the SPEC CLASS (§4). A seat's model is re-tested on the triggers in §5. The house that would lose a seat by a measurement says so (the Claude-against-Claude clause, house-neutral). Model ids live in the table, never in law text (L31). [F-13]

### L27

**Current (LAWS.md:151-154)**
> ### L27 Budget ceiling — hard (ruled 08-29/31; amended 09-10)
> Claude spend caps at the Max plan; no usage top-ups beyond max plan, ever. Grok + Codex enter at MINIMUM tiers. Demand exceeding the envelope → routing discipline + local lane, never spend.
> [amended 09-10, R11] No larger Claude plan — the Max 20x escalation is off the table; no top-ups. A weekly meter hitting its ceiling moves work to another house, never money. Human-facing seats run on consumer plans; API keys are for Cobalt's engine under this law (09-07, LEDGER:879-880). [routing sentence, under review — routing tribunal] Codex is the OVERFLOW valve, never the main line — local first, Claude where it is the floor or best fit, Codex when a Claude meter binds; all of the Codex weekly allowance used each week, most of it on Astra.
> — LEDGER:28, :1142, :879-880; R11 merge confirmed 09-13 (O6). Superseded wording → LAWS-HISTORY H-L27.

**Proposed v2.** Every budget-ceiling sentence stays unchanged; the ceiling is NOT OPEN (no top-ups, ever) [F-25]. The routing sentence is **OPEN FOR DEJAN (D2)** [F-23]:
- **Option A** (the proposal's §1.4, house-neutral): "Each house's weekly allowance is planned in the seat table: none is left idle by design, none is spent past its ceiling, and a bound meter moves the seat to the fallback its row names (L47). Never money."
- **Option B:** keep the sentence as written.
- Seats' notes on A, not rulings:
  - Grok: A's "moves the seat" conflicts with L47, because it makes the meter the router and the move sticky. Its alternative text is at `grok-ruling.md:289`.
  - The Anthropic seat: A adds a new duty, "none is left idle", for every house.
- **ASTRA PENDING (R13):** this sentence sets the OpenAI house's role.

### L29

**Current (LAWS.md:168-174)**
> ### L29 Model rule (ruled 09-03/04; amended 09-07, 09-10, 09-12, 09-13) — UNDER REVIEW, ROUTING TRIBUNAL 09-13; carried as written
> Any Code session touching a vault/DB write path, migration, delete, recovery or forensics runs on the house's top implementation model or higher — Claude: Opus 5 floor, Fable at Dejan's call per prompt. Any house may hold any seat, write paths included, once (1) that floor is met and (2) a permission gate is proven in a scratch test to stop file AND command writes. Sonnet/Haiku and their equivalents: non-write mechanical work only. Auto mode stays, granted per session, never blanket; never auto mode on a write path. Human-facing seats run on consumer plans; API keys are for Cobalt's engine under L27. No house privileged, Claude included.
> [amended 09-10] OpenAI house roles fixed — GPT-5.6-Sol at high effort = the implementation floor, GPT-6-Astra = the cross-check reviewer (Fable's role in that house). Plan review is architect + Astra, two parties, ≤3 rounds; round 3 is final with dissent recorded verbatim and the architect's plan standing (L39). Sol's standing writer profile: `codex exec -s workspace-write` with network on, never `danger-full-access`; Sol cannot commit. Code sessions are cleared every turn — durable state lives in the plan file and the report, never in session context; one scheduled small-context wake-up is permitted.
> [amended 09-12] The architect seat may be assigned to any house by his ruling, with another house reviewing — L29's default split is a default, not a fixture.
> [restored 09-13, O12] "Never auto mode on a write path" (present in the 09-03/04 wording, LEDGER:630-632) was silently absent from the 09-10 fold wording. Not a deliberate drop — restored above.
> — LEDGER:1034, :1149, :1242, :1252. Superseded wordings → LAWS-HISTORY H-L29a–e.
> Status note (not law text, routing tribunal evidence): the 09-07 local-seat verdict read this law as "never a write path (floor is cloud top-tier)" (LEDGER:906-907), a floor not in this text; 09-13 records that such floors "have since hardened into law that Astra cites to declare houses INELIGIBLE without assessing anything" (LEDGER:1325). Routing substance is not rewritten here — FROZEN (R81).

**Proposed v2** [F-07] (Grok's T7 wording, verbatim, `grok-ruling.md:77`: the floor stays operative until he rules D1)
> L29 Write-path seat rule (ruled 09-03/04; amended 09-07, 09-10, 09-12, 09-13, <date>). A WRITE PATH is a vault or DB write, migration, delete, recovery or forensics. Until he rules D1, a session on a write path runs only on the house's top implementation model or higher. A trial does not earn a write path down. A session on a write path also requires (2) the house's permission gate proven in a scratch test to stop file AND command writes outside the session's workspace, and (3) a launch line that states a permission mode other than auto. Never auto mode on a write path. Auto mode stays, granted per session, never blanket. Sonnet/Haiku and their equivalents do non-write mechanical work only, unless a seat-table row cites his ruling or a measured trial for a named class. Any house may hold any seat on these terms; no house is privileged. The OpenAI writer's standing profile stays on its row: `workspace-write`, network on, never `danger-full-access`, cannot commit. The architect seat is assigned to any house by his ruling, another house reviewing. Code sessions are cleared every turn: durable state lives in the plan file and the report, never in session context; one scheduled small-context wake-up is permitted. A session with NO write path takes its model from the seat table by task class. Consumer plans and engine API keys stay in L27. [F-07]

**OPEN FOR DEJAN (D1)** [F-22]. The proposal's §7 options:
- **A:** a PRODUCTION write path (live vault, production DB, `--allow-prod` migration, `trader_settings` write, recovery/forensics on production) always runs on the house's top implementation model. Only DEV write paths (`cobalt_dev`, dev vault, worktree) can be earned down by trial.
- **B:** the same trial rule everywhere.
- The proposing house (Anthropic) wrote "Recommend A". That is its line, not the tribunal's. Gemini's round-1 wording writes A into the text ("OR (for dev write paths only) …"). It was not folded: RULES AN OWNER ITEM (D1).
- The write-path SCOPE for all three conditions is a separate item, O-4.

**Retired from LAWS.md into LAWS-HISTORY at the fold:**
- L29 [amended 09-10]'s plan-review sentence: superseded by L67 [amended 2026-09-18 R20].
- The model names "Opus 5 floor" and "Fable at Dejan's call per prompt": these move to seat-table rows 4 and 7 as RULED.
- "GPT-5.6-Sol … floor / GPT-6-Astra … reviewer": moves to rows 9–11 as RULED.
- The status note: the proposal retired it; no seat objected.
- Full trace: `## Clause trace`.

### L49

**Current (LAWS.md:278-281)**
> ### L49 Local-seat doctrine (ruled 09-11, R3; amended 09-15) — routing tribunal may touch
> The local seat READS AND JUDGES, never COMPOSES. Reading logs, running fixed commands, verifying, diagnosing — cheap. Generating long prose is not. Bounded read-and-judge tasks only: no report authoring, no source code, nothing whose OUTPUT is long. Every local-seat prompt carries a cost estimate up front, the way a METER line does for the paid houses, so a run that is about to be expensive can be refused BEFORE it starts, not discovered at minute 50. A long local run is acceptable when it is work that was chosen; it is not acceptable when it is work nobody authorised.
> [amended 2026-09-15] The local seat's only write path is a Cobalt command (e.g. `cobalt day-open`, which writes its own report), never the shell: no `tee`, redirection or editor writes from the local seat; a local-seat prompt that needs a file written names the Cobalt command that writes it. Approved for the fold 2026-09-15 (Dejan); applied by the CTO desk at the 2026-09-15 close.
> — LEDGER:1185 (reaffirmed LEDGER:1313); amendment LEDGER 2026-09-15 appendix

**Proposed v2** [F-14]. The heading marker "— routing tribunal may touch" is removed. The text of LAWS.md:279-280 is unchanged: read-and-judge, never compose, cost estimate up front, and the only write path is a Cobalt command. All three seats ADOPT. The local seat's wider use goes through trial X4, not a law edit.

## The seat table

**Form and home** [F-04]: Grok's T4 wording, verbatim (`grok-ruling.md:43`):
> The seat table's only home is a committed `configs/cobalt/seats.yaml`. A schema check fails the file when a row lacks either a MEASURED report path and its n, or a RULED date and R-number. `MODELS.md` is a render of that file and is not edited by hand. The yaml is not a law file (R4): committing a row does not rule it.

**Source today: `source pending <T4 build>`.** `configs/cobalt/seats.yaml` does not exist (hub check; `02-greps.txt:14-16`). Until it is built, the rows below are the record, and each row's own ruling or report is its source. How launches read a row before the file exists is O-12, the Anthropic seat's interim sentence.

**The seat table and L67** [F-18]: Grok's (g) wording, verbatim (`grok-ruling.md:273`):
> L67 remains the kind-of-work rule. The seat table may record the model id L67 already seats. A row that seats a different house, or a different kind of work, is `RE-OPENS A RULING (R46)` and is not folded. This house's "blind code seat" (`10-PROPOSAL.md:161`) is not in R46 or L67 and is not added. If the table and L67 disagree, the launch does not go out, and the disagreement is OPEN for him.

**Columns** (from the proposal's §0 and L26): task class / seat · house · model id · meter · evidence (MEASURED report + n / RULED date + R-row) · date · re-test. **First rows**: the proposal's §2 map, corrected by the hub's file-check and the seats [F-17]. A row whose evidence is UNMEASURED is not a standing row (F-04's schema) and appears under `## Re-test rows`.

| # | task class / seat | house | model id | meter | evidence | date | re-test |
|---|---|---|---|---|---|---|---|
| 1 | CTO desk | Anthropic | `claude-opus-5-5` | subscription weekly (refreshed full 09-22, R74) | RULED R36 / R76 / R109 (his trial, "until his evaluation") | 09-22 | X10 (5.5 shadow); his evaluation |
| 2 | hubs (tribunal, check, build-lane) | Anthropic | `claude-sonnet-5` (launch lines, e.g. `prompts/2026-09-20/14-bars-chunk-1a-check.md:1`) | same | UNMEASURED as an assignment (practice; record: six correct STOPs 09-13, `PROJECT-LEDGER.md:1326`, not :1325); build-lane hub has no launch in the sample (Grok) | — | **not standing — R-08** |
| 3 | production deploy hub, no write path | Anthropic | `claude-sonnet-5` | same | MEASURED record, n = 3 no-write-path DONE, rollback not used (`deploy-2026-09-21.md`, `-21b`, `-21h`); the 4th (`deploy-2026-09-22.md:80`) ran a dev `db migrate` under auto = S + W, not "no write path" (hub check HOLDS). Five WHY-NOT-OPUS prompts exist, not four | 09-21/22 | n below any class-level n he sets (O-5) |
| 4 | production deploy hub WITH migration / settings write | Anthropic | `claude-opus-5-5` by R32 / R36; last launch `claude-opus-5` (`prompts/2026-09-19/02-deploy-stack-3.md:3` — the proposal's cited id DOES NOT HOLD) | same | RULED L29 floor (09-03/04) + R32 / R36 id | 09-22 | D1 |
| 5 | builder / fix builder | Anthropic | `claude-opus-5-5` | same | RULED R32 (the L75 FIX 13 / FIX 6 record is Opus 5's, `LAWS.md:425`) | 09-22 | X10 |
| 6 | prompt drafter | Anthropic | `claude-opus-5-5` | same | RULED R32 / R36 | 09-22 | — |
| 7 | design-tribunal seat, derive seat, forensics (Fable-type) | Anthropic | `claude-opus-5-5` for every Fable-type seat "for now"; per-case: DRC `41` / `42` on `claude-fable-5-1` (R73) | same | RULED R109 (19:34, supersedes R76's per-seat ask) + R73 | 09-22 | his next word |
| 8 | code checker | Anthropic | `claude-opus-5-5` (`claude -p`) | same | RULED 09-21 R46 + R32 | 09-22 | R-06; X10 |
| 8a | code-check stand-in when Astra / the Codex meter is out | Anthropic | `claude-opus-5-5` | same | RULED 09-21 R46 (L67 [amended 2026-09-21]; `areas/cobalt-houses.md:65`) — row added from the Anthropic seat's (a) MISSED line | 09-21 | R-07 |
| 9 | design-tribunal seat | OpenAI | `gpt-6-astra` | Codex weekly; OUT until 09-26 06:47 (R13) | RULED L67 / 09-21 R46 — **ASTRA PENDING (R13)** | 09-21 | — |
| 10 | code checker | OpenAI | `gpt-5.6-sol` | same allowance as row 9 | RULED 09-21 R46 — **ASTRA PENDING (R13)** | 09-21 | — |
| 11 | builder | OpenAI | `gpt-5.6-sol` | same | RULED 09-10 (L29 [amended 09-10], moved here); no build since 09-15 by meter rulings (09-15, 09-16); "none held since" UNSETTLED beyond those (Grok) — **ASTRA PENDING (R13)** | 09-10 | — |
| 13 | design-tribunal seat; code checker | xAI | observed `grok-4.7-build` (`seat-usage.md:33`), unpinned (X8); config key stale `grok-4.6-build` (`seat_usage.yaml:82`) | subscription; `grok` rule strings to 09-23 23:59 (R30) | RULED L67 (09-18 R18; 09-21 R46 "unchanged"); MEASURED spot check n = 1 as checker (`audit-house-2026-09-16.md`, toy bundle, one planted defect). Adverse: 09-22 sandbox refusal (`areas/cobalt-houses.md:66`). "Blind code seat": in no ruling, not added (O-15) | 09-21 | R-01, R-02 |
| 14 | design-tribunal seat; code checker (bounded reviewer / verifier) | Google | launch id `gemini-3.1-pro-high` under `agy` | subscription, two weekly buckets | RULED 09-15 "A" + L67; MEASURED spot check n = 1 (`agy-trial-2026-09-15.md` Attempt D: 250 s, 0 denials, 1 VERIFIED new finding + 1 WRONG on substance). Adverse: audit-house HARNESS 3/3 (`areas/cobalt-houses.md:60`); 09-22 blind-seat death (`:66`) | 09-15 | R-03 |
| 15 | day-open verdict (read-and-judge) | local | `mainframe` (Qwen3.8-27B 8-bit) | zero marginal | MEASURED: 684–1,067 output tokens/day 09-16→09-22 (`seat-usage.md`, FC2); GREEN read for 09-20/21/22 (`day-open-2026-09-2{0,1,2}.md` verdict lines; 09-16→19 UNVERIFIED) | 09-22 | X4 |

- **Not seated:**
  - Row 12, OpenAI `gpt-5.6-terra`: 15,605 output tokens ever, no evidence. UNMEASURED; X2 would measure it. **ASTRA PENDING (R13).**
  - Haiku (`claude-haiku-4-5-20251001`): ≤ 8,916 output tokens a day. X3 would measure it.
  - The Cobalt engine: no model calls.
- **Measurement gaps:** see T12 below.

## Assignment key and re-test

### The key: spec class [F-05]

The proposal's §4 is kept as written. Gemini adopted it, and it changes nothing frozen.
- **Class S (specified):** "the spec is defensible against a real-world artifact and testable." There is nothing to infer, and a wrong guess fails visibly. The lowest tier that PASSED this class in the seat table may take it.
- **Class H (horizon):** "a new horizon with undefined outcomes." It needs a model that will notice the spec ITSELF is wrong, so it takes the top seat. This matches 09-21 R46: "For the designs and creations we need the higher level models."
- **W overlay:** a write path adds L29's three conditions to either class.

**Binding:** the key moves no seat until the retrospective validation X5 is on file. The proposal, Grok, Gemini and the Anthropic seat all state this.
- **Found by all three seats (hub check HOLDS):** the proposal's own §4 data table classes rows by their outcome. It reads 09-13 as "S" because the STOPs already happened, and S2-P1 as "H disguised as S". That table is not evidence for the key.
- **Open:** whether S is instead defined by a pre-build citation rule (the prompt names an existing artifact and a test a wrong guess fails). That is O-17. Grok's T5 wording, which carries it, is not folded here: SELF UNMEASURED, re-test row R-01.

### Re-test procedure, PASS bar, n [F-08]

Grok's T8 wording, verbatim (`grok-ruling.md:89`):
> A §5 trigger schedules a re-test. It does not unseat a ruled row and it does not hold a build. Triggers: (a) a house ships a new model id; (b) a meter change makes a cheaper row worth measuring; (c) class-S HOLDs on that seat rise above the table's recorded baseline; (d) he asks; (e) the local model changes, or a measured read-and-judge cost improves. Procedure: the candidate builds the same checked class-S prompt in its own worktree, on the same base tip, beside or after the incumbent. The same three checkers and the same offline and with-DB gate run on it. No shadow build merges. PASS: both gates green; HOLD count no higher than the incumbent's on that prompt; no false completion and no failure claim; wall-clock and meter recorded. The three checkers measure. The candidate house does not grade its own shadow. The seat-table row stores the report path and the n. An n of 1 is a spot check and is not a class-level PASS. He sets the n that is. Promoting a row is his approval, one message.

- **n:** his, an OWNER ITEM (O-5). Value proposed by Gemini, `gemini-ruling.md:88`, as a pointer only.
- **What counts as a defect:** his (O-6). The proposal's "HOLD findings" versus L75's FIX rows (the Anthropic seat).
- **Trigger (c) baseline:** his (O-8).
- **Law steps** (the proposal's §5, kept): a trial is its own lane (L72) and never holds a build.

## Re-test rows

Every UNMEASURED self-assignment, by house. The shape follows the proposal's §5: candidate, incumbent, class, the trial that measures it, and the n, which is his. None of these rows is a standing assignment.

| R | house | what the seat assigned to itself | evidence (hub row) | candidate vs incumbent · class | trial that would measure it | n |
|---|---|---|---|---|---|---|
| R-01 | xAI (Grok) | class H keeps "the top seat": its design-tribunal seat (T5, (e)) | UNMEASURED (Self-assignments T5, (e)) | the L67 design seats as ruled, against any house · H | none named. His L67 row is RULED; a class-H comparison is not in any seat's experiments | his |
| R-02 | xAI (Grok) | code-check share kept via "the three checkers measure" (T8, (a), (f), (g)); model id `grok-4.7-build` | `16-seats.excerpt.md:63`, n = 1 toy bundle: DOES NOT HOLD as a class-level measurement (Self-assignments T8) | Grok checker against the L67 checker set · S (check) | a repeat checker trial at his n. Grok itself declined to add one (X9 "not added") | his |
| R-03 | Google (Gemini) | design + code-check seats kept under L67 "unchanged" ((g)) | UNMARKED SELF (Self-assignments (g)) | Gemini against the L67 set · S (check) / H (design) | none named | his |
| R-04 | Anthropic (the seat) | the top-implementation-model floor holds Opus on write paths (T7) | UNMEASURED (RULED L29 09-03/04) | top model against a PASSED lower tier · W | X1 on a dev write path, only if D1 / O-4 permit | his |
| R-05 | Anthropic (the seat) | Sonnet and Haiku as the first trial candidates (T11) | UNMEASURED (`PROJECT-LEDGER.md:1325`) | Sonnet against Opus (builder, S); Haiku against Sonnet (hub) | X1, X3 | his |
| R-06 | Anthropic (the seat) | the Opus code-checker seat restated in table rows ((g)) | UNMEASURED (RULED R46, R32); no committed check record names `claude-opus-5-5` | `claude-opus-5-5` against `claude-opus-5` · S (check) | X10 | his |
| R-07 | Anthropic (the seat) | the Codex-out fallback lands on Opus ((h)) | UNMEASURED (RULED R46) | Opus stand-in against Sol · S (check) | none named. The row stands as RULED 8a | his |
| R-08 | Anthropic (the proposal, §2 row 2) | Sonnet as hub (tribunal, check, build-lane) | practice, no ruling cited; six STOPs 09-13 (`PROJECT-LEDGER.md:1326`) is a record, not a trial | Sonnet (incumbent) against Haiku (candidate) · S (hub) | X3 | his |

## First trials and experiments (L70)

Base procedure: the re-test procedure above, the §5 shadow build [F-08]. Trials T11, Grok's wording verbatim [F-15] (`grok-ruling.md:127`):
> Schedule, do not seat. (1) One class-S shadow build for Sonnet 5 against the incumbent, on a prompt that names a real artifact and a failing test. (2) One class-S shadow build for Terra, on a prompt Sol already finished green; if no such prompt is in the record, label the result class-S-only and not an answer to whether Terra can build what Sol builds. (3) One Haiku trial as a read-only tribunal hub. Its pass line is a STOP that names a planted proof mismatch. No write path. A smooth summary that misses the mismatch is a fail. None of the three promotes a row without his approval. No fourth trial is added by this house.

| X | what | named by | who runs it, on what | result that would change the law or the table |
|---|---|---|---|---|
| X1 | Sonnet 5 class-S shadow build (T11 (1)) | Grok X1; the Anthropic seat X3 (candidate prompt `prompts/2026-09-20/11-bars-chunk-2-build.md`, Opus 5 FIX 6); Gemini X1 (on a write-path deploy, `68-devdb-repair.md`, which v2's L29 does not allow as an earn-down until D1) | a build lane on the same base tip; L67's three checkers; both gates | PASS → a MEASURED Sonnet class-S builder row (n = 1 = spot check; he promotes) |
| X2 | Terra class-S shadow build (T11 (2)) | Grok X2; the Anthropic seat X4 (Terra's meter UNVERIFIED — the seat WITHDREW its "Codex allowance" claim) | a build lane, when Terra's meter allows (read first) — **ASTRA PENDING (R13)** | green on a prompt Sol finished green answers "can Terra build what Sol builds"; otherwise class-S-only |
| X3 | Haiku read-only hub, planted proof mismatch (T11 (3)) | Grok X3; the Anthropic seat X5 variant: CHECK-hub replay of `bars-chunk-1a-check-2026-09-20` (HOLD 25 · DO NOT HOLD 3 · NOT CHECKABLE 5, `:204`, read by the derive) | a hub on a staged read-only packet | a STOP naming the mismatch → PASS for a hub trial; a smooth summary → fail |
| X4 | local seat as a read-and-judge checker beside a cloud checker (T10) | Grok X4; the Anthropic seat X6; Gemini X2 (hub trial parsing `11-panel-order-deploy.md`, which bears on D3) | `mainframe` on a class-S diff, verdict table only, L49 cost estimate before launch; never counts toward L67's three | inside its estimate and matching the file-check rows → a non-counting local checker row; long prose or a missed HOLD → L49 does not widen |
| X5 | retrospective validation (T6) | proposal T6 (hub job; required before the key binds); Grok T6 + X5; the Anthropic seat T6 / X7 (blind two-pass, run by a house other than the proposer) | who runs it and the match rate that means "predicts" are O-7. Grok's X5 wording, verbatim: "Keep T6, and do it before any of X1–X4 promotes a row. One table, 09-10 through 09-22, class read by the citation rule with the outcome covered. If the citation rule labels S2-P1 as S, the rule is not ready and T5 stays unbound." | the key binds, or stays unbound |
| X6 | agy usage capture (T12) | Grok X6 | an ops build | a number retires the gap; "no log" keeps it named |
| X7 | Codex meter readout (T12) | Grok X7 | the hub's pre-launch probe, logged | a different reset time changes the desk's precondition (L47); no change to L27 by itself |
| X8 | Grok model pin | the Anthropic seat X2 (the hub attributes it to Grok; Grok's raw file carries no such X) | a read of `grok --help` / a launch | no pin → the Grok model column is observed-after, never enforced |
| X9 | write-path launch-shape scratch test | Grok X8; the Anthropic seat X1; owed since the L63 state note (`LAWS.md:350`) | scratch worktree; an allowlist missing one write command, under each non-auto mode; per house for condition (2) | a mode that stops without a dialog and without writing → L29 (2) and (3) are satisfiable unattended; none → write paths are attended-only until one is |
| X10 | `claude-opus-5-5` shadow of a class-S prompt with an Opus 5 record (trigger (a)) | the Anthropic seat X8 | a build lane + L67 check | moves rows 1/5/6/8 from RULED to MEASURED or not; does not re-open R36 (his evaluation stands) |
| X11 | 09-22 meter share, from the hub's UNVERIFIABLE FROM READS row (the Anthropic seat AGAINST (3): ≈ 90.3 % Anthropic output at 19:00) | hub `## Checked against the files` | any read-only hub, on the committed day-final 09-22 section of `seat-usage.md` | informs D2; changes no law by itself |
| X12 | FC11: the Anthropic seat's own `ls` at 19:5x did not show the hub report | hub FC11, UNVERIFIABLE FROM READS | no experiment can reach a past session's listing | recorded, never a defect (L70) |

**T12 measurement gaps** [F-16]. The proposal's three are kept: agy usage capture, a Codex meter readout, and seat-usage role hints regenerated from the seat table. The Anthropic seat's T12 additions, verbatim (`routing-tribunal-fable-r1-2026-09-22.md:110`), each checked:
> "Also owed, same ops build: (1) `seat_usage.yaml:82` key `grok-4.6-build` vs observed `grok-4.7-build` (`seat-usage.md:33`), and `claude-opus-5-5` unmapped (`:37`); (2) `src/cobalt/seatusage/report.py:127` renders '(L29)' as the authority for consumer plans, which moves to L27 in this rewrite; (3) L25's reason-class column (status note, `LAWS.md:145`) — `seat-usage.md:29` has none, so no fallback is counted; (4) Grok's launch model is unpinned; (5) the pricing pin for `claude-opus-5-5`/`claude-fable-5-1` (`seat-usage.md:42`)."

Owners: none named by the proposal (Grok; hub check HOLDS). He names them (O-16).

## Clause trace

Every clause of the frozen text and where it goes. Nothing is dropped silently.

| law · clause | goes to |
|---|---|
| L5 "Every LLM call goes through the routing layer" | reworded: v2 L5 (ONE model-access module, engine calls) [F-02] |
| L5 "out-of-band calls (embeddings, extractor) are routing bypasses" | reworded: v2 L5 names embedding and extraction in the same ban [F-02] |
| L5 "and were ruled accordingly" | DROPPED as provenance, not a duty (Grok T2; the Anthropic seat T2 "nothing dropped"); hub: not checked |
| L21 both sentences | kept, unchanged [F-25] |
| L22 every sentence | kept, unchanged [F-12] |
| L23 "Local trumps cloud … continuity rationale" | kept [F-09] |
| L23 "Every lane local can serve runs local-first with cloud fallback; judgment lanes cloud-first …" | kept [F-09]. Grok reads these as still assigning (O-18) |
| L23 "LOCAL = first candidate for every task" | reworded: "first candidate ASSESSED for every task class" [F-09] |
| L23 "→ cheapest sufficient step up" | DROPPED as an assignment (named by Grok (b) / T9); cost is L24's order, not a selector [F-03]; hub: not checked |
| L23 status note | replaced by the seat table's local rows (not law) [F-09] |
| L24 "(1) local = free, first" | reworded: "zero marginal cost"; "first" → L23 "first ASSESSED" [F-03] |
| L24 "(2) subscription-agent FEDERATION = free" | reworded: "prepaid and bounded by a weekly meter" [F-03] |
| L24 "async via dead-drop … and sync via in-session CLI bridges for code work" | kept as lawful shapes [F-03] (the proposal dropped them; hub WRONG FACTS, Gemini row HOLDS). "(Grok Bot etc.)" removed as a vendor example binding no duty (Grok) |
| L24 "(3) metered fast-wire APIs = sync-only, rare, bounded, cost-footered" | reworded [F-03]: "sync-only, bounded, cost-footered, the narrowest rung". **The word "rare" is not in the folded wording**, though Grok's (b) says it is "kept" — named, ESCALATE |
| L24 "Pay-per-token = narrowest rung" | kept as "the narrowest rung" [F-03] |
| L25 every clause except the bake-off pointer | kept unchanged: the chain, the local floor, loud degradation, SPOF parked, "Phase 1 … NOT load-bearing" (tension with L67 → O-9), quota = outage class, shed load, zero Claude tokens, exception handler, reason class, AMBER, fix ticket, disaster continuity, L23/L25 relation, status note |
| L25 "ADR-0008's bake-off table" | reworded: "the seat table" [F-10] |
| L25 "Claude → next-best house" | kept (edit not taken [F-11]; O-10) |
| L26 "Appropriate intelligence … across houses" | reworded [F-13] |
| L26 "task classes assigned by measured evidence (bake-offs, slice reviews, cost-ledger token-per-outcome)" | reworded [F-13]: MEASURED (trial report, bake-off, check record, cost-ledger figure, with n) or RULED. "slice reviews" is not named; it is read as "a check record". RULED evidence is newly lawful, marked |
| L26 "recorded in routing config with rationale" | reworded: one row of the seat table [F-13]; source pending the T4 build [F-04] |
| L26 "Claude recommends against Claude when measurements say so" | reworded house-neutral [F-13]; the Anthropic seat names its words as dropped (O-11 B restores them) |
| L26 "Assignments re-tested as local models improve" | moved: re-test trigger (e) [F-08]. The proposal alone dropped it (Grok (b); the Anthropic seat (b); read by the derive at LAWS.md:148 against the proposal's :192-196) |
| L27 every budget-ceiling sentence (Max plan cap; no top-ups ever; minimum tiers; never spend; no larger plan; meter → another house, never money; consumer plans / API keys) | kept, unchanged; NOT OPEN [F-25] |
| L27 routing sentence ("Codex is the OVERFLOW valve …") | OPEN FOR DEJAN (D2) [F-23] |
| L29 .1 write-path list + "top implementation model or higher" | kept: v2 L29 "Until he rules D1 … or higher" [F-07]; D1 open |
| L29 .1 "Claude: Opus 5 floor" | moved: seat table row 4 (RULED; id by R32 / R36) |
| L29 .2 "Fable at Dejan's call per prompt" | moved: seat table row 7 (RULED R109, superseding R76) |
| L29 .3 "Any house may hold any seat … once (1) floor and (2) scratch-proven gate" | kept [F-07]. Changed, named: the gate now stops writes "outside the session's workspace" (the Anthropic seat T7) |
| L29 .4 "Sonnet/Haiku and their equivalents: non-write mechanical work only" | kept [F-07], with an exception added by Grok's wording: "unless a seat-table row cites his ruling or a measured trial for a named class" — named |
| L29 .5 "Auto mode stays, granted per session, never blanket" | kept [F-07] |
| **L29 .6 "never auto mode on a write path"** | **kept BY NAME**: "Never auto mode on a write path." + (3) "a permission mode other than auto" [F-07] |
| L29 .7 "Human-facing seats run on consumer plans; API keys … under L27" | moved: L27 [amended 09-10] carries it; v2 L29 points there [F-07] |
| L29 .8 "No house privileged, Claude included" | kept as "no house is privileged" [F-07]. The words "Claude included" are dropped; the substance is house-neutral. Named |
| L29 [09-10] OpenAI roles (Sol floor, Astra reviewer) | moved: seat table rows 9–11 (RULED 09-10 / L67 / R46) — **ASTRA PENDING (R13)** |
| L29 [09-10] plan review "architect + Astra, two parties, ≤3 rounds; round 3 final … architect's plan standing" | superseded by L67 [amended 2026-09-18 R20] (four houses, ≤3 rounds each; L39 unchanged) → LAWS-HISTORY |
| L29 [09-10] Sol writer profile (`workspace-write`, network on, never `danger-full-access`) | kept: v2 L29 "The OpenAI writer's standing profile stays on its row …" [F-07]; the L33 alternative is O-13 |
| L29 [09-10] "Sol cannot commit" | kept: "cannot commit" [F-07] |
| L29 [09-10] "cleared every turn … never in session context; one … wake-up" | kept [F-07] ("never in session context" restored; the proposal had dropped it — hub WRONG FACTS Grok row HOLDS) |
| L29 [09-12] architect seat any house by his ruling | kept [F-07] |
| L29 [09-13 O12] restore note | provenance → LAWS-HISTORY with the superseded L29 wording |
| L29 status note (LEDGER:906-907, :1325) | retired (the proposal; no seat objected) — the rewrite answers it |
| L49 heading marker "— routing tribunal may touch" | removed [F-14] |
| L49 all text | kept, unchanged [F-14] |

## Dissents, verbatim

No house REJECTed an item or said `DO NOT ADOPT` (hub `## Rulings table`; the Anthropic seat `reject: 0`). The wordings v2 does not follow are carried here verbatim:

- **Gemini, T7** (`gemini-ruling.md:32`), not folded (RULES AN OWNER ITEM (D1)):
  > A WRITE PATH is a vault or DB write, migration, delete, recovery or forensics. A session on one runs only when (1) its model is the house's top implementation model, OR (for dev write paths only) the seat table (L26) records that model PASSED for that task class by a measured trial (§5) — [D1] · (2) the house's permission gate is proven in a scratch test to stop file AND command writes outside the session's workspace · (3) its launch line states its permission mode, never auto mode (L62).
- **Grok, T5** (`grok-ruling.md:55`), not folded (SELF UNMEASURED, R-01; O-17):
  > Class S: the prompt cites an artifact that exists at launch and a test a wrong guess fails. The lowest tier with a MEASURED PASS for that class may be proposed; he promotes. Class H: the prompt does not cite both, or the work is a new horizon with undefined outcomes; it takes the top seat his rulings already name for that kind of work. W: a write path overlays either class and adds L29's conditions. No class is read off the outcome. This key moves no current seat until the retrospective table is in the record.
- **Grok, T9 L23 paragraph** (`grok-ruling.md:101`), not folded (the proposal's sentence changes less; O-18):
  > L23, the assignment sentence only: LOCAL is the first candidate ASSESSED for every task class. It is assigned only where the seat table records it passing; every other class records why not. Continuity rationale kept: local-first was ruled so a morning still happens when a vendor is down (Gemini outage 08-28). "Local trumps cloud even when cloud is free" and "every lane local can serve runs local-first" remain the reason local is assessed first. They are not a route. Judgment lanes stay cloud-seated, with a loud degraded mode on outage (L25). The status note (bake-off queued) is replaced by the seat table's local rows.
- **Grok, T9 L25 first hop and L22 phrase** (`grok-ruling.md:103`, `:105`), not folded (the L31 ground does not hold; O-10):
  > "Claude → next-best house (headless, task-shape-routed)" becomes "the seat's house → the next row the seat table names (headless, task-shape-routed)".
  > L22, one phrase: "Max subscription and Anthropic API are separate meters" becomes "a house's subscription and its API are separate meters." The following sentence is not rewritten and still names Claude and the Anthropic API.
- **Grok, (h)** (`grok-ruling.md:289`), carried with D2:
  > A weekly meter at its ceiling: do not launch that row. Check before launch (L47). Hand that build to another house with a CONTINUE brief. Per build, not sticky. If the alternate is a poor fit, say so and do not proceed. Never money. Exhaustion is an outage class and is loud (L25). The seat table does not switch a seat. Which house receives the build is his alternate for that build.
- **The Anthropic seat.** These wordings are not taken; full text is in its report:
  - T3 (`:47`): drops the rung-2 shapes the hub found dropped.
  - T4 (`:53`): O-12.
  - T5 (`:60`): O-17.
  - T6 (`:66`): O-7.
  - T7 (`:73`): keeps the OR operative ahead of D1. SELF UNMEASURED, R-04.
  - T8 (`:82`) and (f) (`:149`): O-6.
  - T11 (`:103`): SELF UNMEASURED, R-05. Its prompts are named in X1 and X3.
  - (b) L26 (`:125`): O-11.
  - (g) (`:155`): Grok's (g) wording was taken instead.
  - (h) (`:161`): SELF UNMEASURED, R-07.

## OPEN for Dejan (L39)

A law file is never voted. Each item is his, read after Astra's Saturday read (R13). The A/B for each is in the derive report's `## FOR DEJAN`.

| O | item | v2 carries (A) | raised by |
|---|---|---|---|
| O-1 | D1: production write-path floor | L29 floor operative "until he rules D1" (Grok's wording); options A/B above | the proposal §7 |
| O-2 | D2: L27's routing sentence | the sentence under review, options A/B above — **ASTRA PENDING (R13)** | the proposal §7 |
| O-3 | D3: the local seat as Cobalt's hub (plan of record, LEDGER:1325) | not decided; proposal options A (keep, gated on a measured hub trial) / B (retire) | the proposal §7 |
| O-4 | write-path SCOPE for all three L29 conditions (does a dev migration / with-DB suite count?) | v2 L29 as written: every migration is a write path | the Anthropic seat (D1 MIS-FRAMED; audit C5); live: `05-stacked-deploy` ran a dev `db migrate` under auto (hub check HOLDS) |
| O-5 | the class-level n for a PASS | unset; every trial row reads "spot check, n = 1". value proposed by Gemini — `gemini-ruling.md:88` | all seats |
| O-6 | PASS bar: what counts against a candidate | "HOLD count" (Grok's T8) | Grok, the Anthropic seat (FIX rows per L75; rows 5, 6 and 25 of `bars-chunk-1a-check-2026-09-20.md` are HOLDs confirming a build is right, read by the derive) |
| O-7 | T6: who runs it, and the match rate that means "predicts" | "a hub job" (the proposal) | the Anthropic seat (a house other than the proposer, blind two-pass) |
| O-8 | the trigger (c) baseline | unset | the Anthropic seat |
| O-9 | L25 "Phase 1: cross-house seats NOT load-bearing" against L67 | kept unchanged | Grok |
| O-10 | L31's reach: does it bar vendor names in LAW TEXT? It decides the L22 phrase, L25's first hop, L26's "(L31)" and L22's chassis sentence | L22 and L25 unchanged | the Anthropic seat ESCALATE 1; Grok (b); hub check (L31 lists no law text) |
| O-11 | L26 wording | the proposal's text (bare §4 / §5, which the fold refuses) | the derive, from the Anthropic seat's (b) |
| O-12 | the seat table's interim source until `seats.yaml` exists | `source pending <T4 build>` | the Anthropic seat T4 |
| O-13 | the OpenAI writer profile: in L29 (on its row) or re-housed in L33 | in L29 (Grok's T7) — **ASTRA PENDING (R13)** | the Anthropic seat |
| O-14 | RE-OPENS A RULING (R46): the model ids inside L67 [amended 2026-09-21] | L67 untouched; the table records ids as RULED | the Anthropic seat; Grok (g) |
| O-15 | does a "blind code seat" exist in any ruling? | not in the table | Grok |
| O-16 | owners of the T12 ops items | none named | Grok |
| O-17 | T5: is class S defined by a pre-build citation rule? | the proposal's §4 | Grok (T5, SELF), the Anthropic seat (T5) |
| O-18 | L23 wording | the proposal's sentence | Grok (T9) |
