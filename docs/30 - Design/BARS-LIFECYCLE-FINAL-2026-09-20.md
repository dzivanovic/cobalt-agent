# BARS LIFECYCLE — FINAL (four-house tribunal CLOSED, LAWS L67 / L39) — 2026-09-20

**Status: FINAL.** Three rounds were held; there is no round 4 (L39). Nothing here was voted; everything still contested is carried verbatim to the owner in §5. **Derived by:** the Fable seat (a fork of the CTO desk `1f4097ee`), 2026-09-20 15:3x ET. **Base text:** `docs/30 - Design/BARS-LIFECYCLE-DERIVED-v3-2026-09-20.md` (v3). **Inputs read:** v3 in full; `docs/40 - DevDocs/reports/bars-tribunal-r3-2026-09-20.md` in full; the raw rulings `cobalt-wt/agy-trial/scratch/tribunal-bars-0920/r3/{astra,grok,gemini}-ruling-r3.md` (L35: the rulings are the source, the report a collation); on main, the lines a fix rests on — `src/cobalt/radar/poller.py:84-128`, `src/cobalt/archiver/store.py:104-120`, `src/cobalt/db_migrations/cli.py:552, 685-690`. **No DB query was run. Nothing is implemented, launched or deleted by this file.**

**HOW TO READ IT — FINAL = v3 + this file; where they differ, THIS FILE WINS.** v3 §3.1 (partitioning), §3.5 (i1-only enforcement), §3.6 (derived i2), §3.8 (growth) and §3.11 (wrong facts) stand as written in v3 and are not repeated. §3.2, §3.3, §3.4, §3.7, §3.9, §3.10, §4, §5, §6 and §7 are amended below; each amendment says exactly what it replaces. A builder reads v3, then this file.

## §0 Headline
- Round 3: grok `BUILD v3 WITH 4 FIXES`, gemini `BUILD v3 WITH 3 FIXES`, astra `BUILD v3 WITH 6 FIXES` — **all three: "builder may fold them without another round: YES"**. With the Fable seat: 4 of 4 BUILD. 22 of 25 round-2 defects RESOLVED by their raiser; the three NOT RESOLVED (G-01, G-12, G-13, all astra) are answered by astra's own F1 and F2, folded verbatim here.
- **This fold invents no mechanism.** 26 fold rows (§2): **FV 17 · FM 5 · NF 4**. Every NF has a file:line reason. One thing NO house specified and this seat does not invent: HOW database admission is closed (F1 states the requirement, not the means) — §6 U21; the chunk-4 builder names it and E/R demonstrate it.
- **One house-vs-house conflict of fixes** (empty vendor export: gemini vs grok + astra) — carried as C1 in §5, default = the conservative label `unknown`. One factual disagreement (grok vs astra on a writer that commits after the activity check) is not decided by anyone: it is experiment E-15, and the design takes the conservative side (astra F1).
- **Chunk E grows from 14 to 19 experiments** (+ one sub-row E-6b); all 9 NOT CHECKABLE items of round 3 are covered; none is uncovered (§6).
- **HIS RULINGS: still twelve** (§4). Round 3 forced one narrowing — item 5 loses 5a (MP4 has no activation path in this build, astra F5) — and one shift he must hear plainly: **item 4 (repair seam) is now 2–2** (gemini moved to "Add B").
- **No fix touches a ruled input.** No `R9 RISK` was raised in round 3. R9 (i1 only) and R9-extended (session segmentation from i1) stand untouched.
- Outage numbers are ILLUSTRATION ONLY until the rehearsal R measures them: end-to-end **≈3.8–6.5 min at today's size, ≈6.3–11.6 min at +20 trading days** (§7).
- ESCALATE: 0. Dissent blocks carried verbatim: 12 (+ C1).

## §1 RULED INPUTS — never questions (v3 §1, unchanged)
09-20 R9 (i1 ONLY stored; i2 derived) · 09-20 R9-extended (i1 is the only source of session segmentation) · nothing is deleted anywhere until he rules after the tribunal — `bars_legacy` and every non-i1 row stay physically intact through every chunk · 09-19 R8 (A) repairs only in a quiet window, the live poller's write path untouched by a repair mechanism · 09-19 R14/R35 one shadow night Mon 09-21 20:30 · any change to `complete` or a detector's refusal reaches the card (L7) and is out of this build.

## §2 FOLD TABLE R3
Status: **FV** folded, the house's wording verbatim · **FM** folded, modified (how, why) · **NF** not folded (why, file:line). `hub #n` = the round-3 report's "Checked against the files" row.

| id | house | the point (≤25 words) | lands | status |
|---|---|---|---|---|
| F1 | astra | Swap and rollback lack an enforced admission fence; the harness's REPEATABLE READ snapshot is taken by the before-probe, before the lock. | §3.4 steps 3, 6 | **FV** — confirmed on main: `before = _probe_all(conn)` `cli.py:689` precedes `_apply` `:690`; isolation `:552`. Answers G-12, G-13. |
| G-12, G-13 | astra | NOT RESOLVED in round 3: "every writer is down" was residents + a point-in-time activity check. | §3.4 | **FV** via F1 (astra: "F1 supplies the sequence and replacement"; "F1 covers both directions"). |
| (a) | gemini | A cron `restate` queued behind ACCESS EXCLUSIVE resumes after the rename and writes into `bars_legacy`. | §3.4 step 3; E-16 | **FM** — gemini gave no replacement wording; astra's F1 sentence covers it verbatim ("An already waiting operation must not be allowed to resume against the retired relation after commit"). The PG behaviour itself → E-16. |
| P-span | grok | Children are created ≥26 weeks AHEAD only; live i1 starts 2026-07-30; one source row with no child rolls the whole swap back. | §3.2, §3.4 step 1 | **FV** (hub #3 HOLDS). |
| grants | grok | "parent + open child" is not enough; older weeks are read by replay. | §3.4 step 3; E-17 | **FM** — the PG claim is NOT CHECKABLE (hub #24) → E-17 decides parent-only vs every child; until then grants are applied and asserted on parent + EVERY child (the conservative side). |
| REPLACED fold | grok | The invariant "source i1 = target i1" is not bound to the row fold; keys + counts alone would pass 141.20 vs 141.00. | §3.4 step 3 | **FM** — grok gave the rule, not wording: `REPLACED` is bound to MP1's own fold (`to_jsonb(t)`, PK-ordered, all columns) with a filter; `_probe` takes no `WHERE` today (hub #26), so a filtered variant is written in chunk 1a. |
| old OID | grok | A leftover prepared statement would still see the old OID. | E-16 | **FM** — NOT CHECKABLE (hub #25); E-14's v3 expectation says the opposite; moved whole to E-16, expectation recorded as CONTESTED. |
| 820 MiB | grok | "Sizing the gate from 820 MiB under-buys WAL/temp." | — | **NF** — DOES NOT HOLD (hub #23): v3 `:238` says "both before WAL/temp" and `:88` sizes the gate from R's measured peak incl. WAL/temp. |
| F2 | astra | A zero-row cycle never re-reads bounds (`upsert_bars([])` returns before connecting); any post-rollback write error is labelled `no_partition`. | §3.2 | **FV** — confirmed on main: `store.py:118` `if not bars` returns before `_connect()` `:120`. Answers G-01. |
| G-01 | astra | NOT RESOLVED in round 3. | §3.2 | **FV** via F2. |
| P-closed | grok | Monday 04:00:30: `closed` is all prior-week; only `now`'s period is tested; W0 missing ⇒ ≈500 per-ticker failures, health not RED. | §3.2 | **FV** (hub #4 HOLDS; `poller.py:98-102`). Composed with F2 — see the note under §3.2: no house has seen the two together. |
| stale/error | grok | Uncovered historical must not take the `except` path; `newest` must not be computed from unwritten minutes; `no_partition` is its own class. | §3.2 | **FV** — confirmed `poller.py:114-124`; agrees with F2's third paragraph. |
| F3 | astra | A generic `PollFailure` / `stopped_at` / incomplete cycle becomes minute-level `ingestion_failure`; `max()` on a null watermark. | §3.7 | **FV** — confirmed: `PollFailure(ticker, "error", now)` carries no range (`poller.py:93, 113`); `stopped_at` is set in both `except` branches and the loop `continue`s (`:94-95, :114-115`). |
| P-range | grok | Grok's own R2 formula starts at the watermark and excludes the overlap window the response did cover. | §3.7 | **FV** — the replacement sentence. Its sub-step "`closed` includes 09:57" DOES NOT HOLD (hub #5: `bar.ts > threshold`, `poller.py:101`); the replacement does not depend on it; the boundary minute is a listed test. |
| empty export | grok | Empty successful export → no covered range, absences `unknown`. | §3.7 | **FV** — agrees with astra F3 ("An empty response supplies no inferred range"). |
| (d) | gemini | Empty export: `max(None, …)` raises; "the covered range for an empty success must use the requested bounds". | §3.7; §5 C1 | defect **fixed** by F3 + grok; gemini's REPLACEMENT is **NF** — there are no requested bounds on main: `self.fetch(ticker, Interval.I1, self.token)` takes no range (`poller.py:88`). It also conflicts with grok and astra → carried verbatim as **C1**. |
| `stopped_at` | grok | "`ingestion_failure` only for a ticker with a covering `PollFailure`, or a ticker at/after `stopped_at`." | — | **NF** — the premise DOES NOT HOLD (hub #7): `stopped_at` is the last ERRORED ticker, not a cut-off (`poller.py:94-95, 114-115`), and a `PollFailure` carries no range. Astra's F3 governs. Grok's "not polled this cycle → `unknown`" IS in F3's wording and stands. |
| F4 | astra | E-5's keyword fallback and F-B leave the substitute undemonstrated; F-B's exposure is understated; a newly found invalidation needs a PRE-migration value. | §3.3, §6 | **FV**. |
| E-6 fallback | grok | "A generation that DETACH breaks" has no writer: bind DETACH to `cobalt bars close` in one transaction with generation + 1, or F-A. | §6 E-6, E-6b | **FM** — automatic fallback = F-A (astra F4: "unnamed membership generations … are not substitutes"); grok's named binding is kept as candidate E-6b, which must itself pass before it is relied on. |
| F-B timing | grok | "F-B does not say whether the bracket runs before residents restart." | — | **NF** — DOES NOT HOLD (hub #22): v3 `:80` "immediately before the drain and immediately after the restart". Grok's REFUSAL of F-B is advice on item 5 and is carried there. |
| F5 | astra | Chunk 6 can DETACH before E/R; MP4 has a path with no E/R dependency; "reported" is not "passed". | §3.9, §8 | **FV**. |
| P-swap | grok | `FROZEN_ARCHIVE` narrows the proof at chunk 4, whose wait-list names item 11, not item 12. "The table must match the prose." | §3.9, §8 | **FV** with F5 ("FROZEN_ARCHIVE activation requires the swap rehearsal and item 12 authorization"). |
| F6 | astra | `lock_timeout` does not stop a running statement; the illustrations omit ≈42.2 s of deploy work; 7.8 s is an estimate, not a maximum wait. | §3.4 step 5, §7 | **FV**. Also strikes v3's "≥5× slack" and "no session opens for >48 h" (astra: "not established"). |
| (h) | gemini | The 3.1 / 5.6 min figures exclude the 42 s the same row names. | §7 | **FV** in substance — same correction as F6; astra's 42.2 s (149 − 106.8) is used; hub: ARITHMETIC OK both ways. |
| illustration | grok | Copy rates assumed; filtered digest at the unfiltered rate; +20 d ignores non-i1 growth; "item 11 must wait on R, not on 3.1–5.8 min". | §7, §4 item 11 | **FV** — every such number is labelled ILLUSTRATION ONLY. |
| chunk-E assessment | astra | E-3…E-14: incomplete fallbacks, three non-universal expectations, three routes with no experiment. | §6 | **FV** — her assessment sentences are the amended rows; the three routes are E-15, E-16, E-19. |

Counts (26 rows): **FV 17** (F1, G-12/G-13, P-span, F2, G-01, P-closed, stale/error, F3, P-range, empty export, F4, F5, P-swap, F6, (h), illustration, chunk-E assessment) · **FM 5** ((a), grants, REPLACED fold, old OID, E-6 fallback — three of them also add an experiment: E-16, E-17, E-16) · **NF 4** (820 MiB · gemini (d)'s replacement wording — its defect IS fixed · grok's `stopped_at` clause · grok's F-B timing). **Not folded because the code or the text says otherwise, one line each:** 820 MiB — v3 already sizes the gate from R's peak · requested bounds — do not exist (`poller.py:88`) · `stopped_at` — last errored ticker, loop continues (`poller.py:94-95, 114-115`) · F-B timing — v3 `:80` says it.

## §3 AMENDED SECTIONS (each replaces the named v3 text; everything else in v3 §3 stands)

### 3.2 The poller with no partition — REPLACES v3 §3.2 bullets "How the rule is met…" steps (1)–(4); the rest of v3 §3.2 stands
- **Children reach BACK as well as ahead (grok P-span, verbatim):** *"before chunk 4, children cover `[min(ts) of source i1, now + horizon]`, contiguous, no `DEFAULT`. Heartbeat after swap goes RED for any hole in the still-attached span, not only the vendor window."* R creates exactly the same span (the text now requires the children R needs).
- **THE RULE, added to astra's round-2 paragraph (astra F2, verbatim):** *"On an unpartitioned parent, execute the existing poller path unchanged. On a partitioned parent, validate current coverage before fetching and refresh it before publishing cycle health, including cycles with zero submitted rows. Serialize partition maintenance with polling or use a tested coverage-generation protocol so a topology change invalidates the cycle's cached inventory.*
  *After a write failure, preserve StageDropped propagation, roll back, and refresh bounds. Unreadable bounds produce coverage_unknown and abort; missing current coverage aborts RED. Classify no_partition only when submitted timestamps are actually uncovered, recording all uncovered ranges. If all submitted timestamps remain covered, retain the existing generic error classification. Record the entire rolled-back submitted batch as unpersisted work, separately from the uncovered subset.*
  *For a successfully persisted covered subset, compute freshness from persisted rows and the prior watermark. Preserve its historical no_partition failure and recovery ranges; do not clear them through the generic error-success branch."*
- **What "current coverage" includes (grok P-closed, verbatim):** *"abort RED if **any period that this cycle's `closed` would write** is missing (period of `now` **and** of `max(closed.ts)` when `closed` is non-empty), not only `now`."*
- **The `stale` / `error` bookkeeping (grok, verbatim):** *"Uncovered historical must **not** take the `except` path (`:112-115` sets `error` + `stopped_at`). Do not compute `newest` (`:117-120`) from unwritten `closed` minutes — use persisted `watermark` / written ts. Empty covered at Monday 04:00 must not look like a successful ticker with Friday `newest`. `no_partition` is its own class; do not also plant `error`."*
- **Serialising partition maintenance with polling:** DETACH and `ensure` run only in a quiet window with residents down (chunk 6, deploy steps), which is F2's first option; the coverage-generation protocol is NOT built. An operator DETACH inside a scan is operator error, fails one ticker one cycle late (grok's walk), and is a listed test.
- **COMPOSITION NOTE (this seat, said plainly):** F2 and P-closed were written by two houses who did not see each other's text. They compose as: the periods containing `now` and the newest closed minute are "current" (missing ⇒ abort RED); any older hole is "historical" (per-ticker `no_partition`, named range). Astra's "historical-only holes should remain local to the affected ticker" is not contradicted — at Monday 04:00:30 the prior week is where EVERY ticker writes. No house has ruled on the composition; chunk 2 is a live-write-path change and gets ≥3 checkers anyway (L67), with grok's Monday sequence and both of astra's F2 sequences as named tests.

### 3.3 The migration proof — AMENDS v3 §3.3 items 2–3; item 1 (MP1 default), item 4 (text scan widens only) and the cost paragraph stand
- **Fallbacks (astra F4, verbatim — replaces every "if not" cell that named a keyword, a generation or "counters alone"):** *"A failed or inconclusive experiment disables every proof optimization that depends on that behavior. The automatic fallback is F-A: full live-table content proof before and after the migration, with CHANGED rejected before commit. A proposed alternative guard must itself be specified and demonstrated before activation; keywords, unnamed membership generations and untested counter combinations are not substitutes.*
  *Any partition selected for post-migration digestion must have a trusted pre-migration comparison value. A newly discovered invalidation cannot be handled by computing only a post-change digest. Seal publication must take its lock before establishing the content snapshot used for publication.*
  *F-B requires explicit owner acceptance of post-commit detection. State that wrong SEALED content may remain until repair or recovery completes, not merely until detection finishes. Define the preflight baseline, post-deploy comparison, failure alert, affected-reader handling and recovery procedure before offering F-B for activation. Text scanning may only widen the proof."*
- Consequences: v3 item 3's "F-B, only on his explicit word" is **not offered** until those five things are written; E-7's and E-8's fallback is F-A (grok: "not a pre-commit guard ⇒ F-A (not F-B)"). Astra, carried: *"Passing fourteen examples alone is not proof that arbitrary privileged code cannot bypass the protocol."* — which is why 5-prod still needs HIS ruling after E (§4 item 5).
- **MP4 (astra F5, verbatim):** *"MP4 has no activation path in this build; any later proposal requires its own demonstrated protocol and owner ruling."* v3's last sentence of §3.3 ("unless he rules §4 item 5a YES") is STRUCK.
- The seal lock's wait (astra F6): *"A seal's approximately 7.8-second digest is likewise an estimate, not a maximum writer wait."* R measures a first-sight writer arriving at 20:35 that touches several closed children.

### 3.4 The swap — AMENDS v3 §3.4 steps 1, 3, 5, 6, 7; steps 2, 4, 8 stand
- **Step 1, added gates:** children cover `[min(ts) of source i1, now + horizon]` (P-span); E-12, E-13, E-14, **E-15, E-16, E-17, E-18 PASSED** (not "reported", F5); item 12 RULED (YES or NO — NO means `bars_legacy` stays under MP1 inside the swap deploy and after).
- **Step 3 — the fence (astra F1, verbatim):** *"Before either cutover or reverse reconciliation, close database admission to every non-migration writer, including operator commands, scheduled jobs and other desks; drain existing sessions and transactions. An activity query is diagnostic, not the fence. Keep admission closed through commit and connection replacement. Acquire locks on every source and destination relation before establishing the content snapshot. Because the existing harness probes before applying migrations under REPEATABLE READ, the swap requires a pre-probe locking phase or a dedicated transaction whose first content snapshot follows successful lock acquisition.*
  *Preserve a trusted source baseline under that fence. Before cutover commit, verify that source i1 has not changed from that baseline and that the complete target i1 count and OHLCV digest equal it. Preserve and verify the complete legacy archive independently. Reverse reconciliation uses the same fence and locks: full i1 upsert, explicit legacy-only-key check, equality, renames, commit, fresh connections, then reopen admission. A nonempty legacy-only-key result aborts without committing reconciliation or renames; resolving deletion or retention differences requires a separate authorized procedure."*
  Astra: *"This preserves the one-transaction design. It does not require live change capture."* And: *"`REPLACED` catches a price changed only in the target. Source-versus-target equality alone does **not** catch the same wrong price written into both. The trusted source baseline above closes that distinction."*
  The order is therefore: close admission → drain → LOCK source + destination → first content snapshot → baseline digest of source i1 → copy → `REPLACED` (baseline = source i1 now = target i1, count AND the MP1 row fold `to_jsonb(t)` PK-ordered with an i1 filter — grok) → renames → grants on parent + every child (until E-17 says parent-only suffices) → commit → connection replacement → reopen admission. v3 `:90` (no before-probe) and v3 `:236` (a before-probe in the sum) contradicted each other (hub #14); this order settles it. **HOW admission is closed is not specified by any house — U21.**
- **Step 5 — deadlines (astra F6, verbatim):** *"Report end-to-end resident outage, including admission closure, lock acquisition, all proof passes, copy, commit, connection replacement and verified restart. Set and rehearse finite statement deadlines plus an overall deployment deadline and cancellation/recovery procedure; lock_timeout bounds lock waits only. Reserve time for abort, crash recovery and restart before the next scanning session. Disk failure and rollback can leave WAL, cleanup work or recovery delay even when production rows remain transactionally unchanged.*
  *The current arithmetic illustrates copy-and-proof time, not the complete outage. Add the reported approximately 42.2 seconds of other deployment work, then replace all illustrations with R's measured end-to-end result."* STRUCK from v3 step 5 and step 2: "inside the 60-minute pause with ≥5× slack" and "no session opens for >48 h" — the scheduled window and R determine both.
- **Step 6 — rollback after restart:** uses the SAME fence and locks (F1 above; grok: "No NEW DEFECT if the lock is the same fence as the swap; say so in the procedure" — said). Astra, carried: after a future retention DETACH, legacy-only i1 keys can be EXPECTED, so *"“rollback exists while legacy exists” means a recovery route remains, not that the automatic reverse-rename procedure will always succeed."*
- **Step 7 — `FROZEN_ARCHIVE`:** activates ONLY with item 12 = YES and the swap rehearsal passed (F5 / P-swap); grok: *"Do **not** default this on in chunk 4; until you say YES, MP1 still covers it."* Astra's conditions if YES: *"an enforced frozen relation, retained trusted digest, recoverable backup, explicit off-outage comparisons and verification before using it for rollback."* Both houses' residual, stated: the archive still holds the preserved non-i1 rows; reverse-upserting live i1 does not repair damage to those, and a delayed alert cannot reconstruct them.

### 3.7 Gap states — REPLACES v3 §3.7's bullet "The evidence, with no new table"; astra's round-2 rule and the rest of §3.7 stand
- **THE RULE (astra F3, verbatim):** *"PollResult records cycle identity, attempted tickers, response coverage, successful persistence coverage, and any evidenced failed ranges. A generic PollFailure, stale record, stopped_at value or incomplete cycle is not minute-level coverage evidence.*
  *For each absent closed minute, use no_print_as_fetched only when a retained successful response covers it and omits it; use ingestion_failure only when retained acquisition or persistence evidence covers that minute. Otherwise retain unknown. Distinguish returned-but-unpersisted rows from rows omitted by the response.*
  *On first contact, use oldest_returned_ts without applying max to a null watermark. An empty response supplies no inferred range. The successful range must remain within the returned closed-time extent; overlap below the conservative watermark boundary may remain unknown. Cards rendered between cycles use explicitly identified retained evidence or unknown, never an implied current-cycle success. Persist the classification and supporting evidence with the card inputs."*
- **The covered range (grok P-range, verbatim — replaces grok's own round-2 formula in v3 `:114`):** *"covered = `[threshold if watermark else oldest_returned_ts, newest_after_fetch]` clipped to the successful response; first contact still starts at `oldest_returned_ts`."* And: *"empty successful export → **no covered range**, absences `unknown`."* And: *"Not polled this cycle → `unknown` (not no-print). Between cycles, last stored card-input range only."*
- "No new table" is, in astra's words, *"a design option, not a demonstrated integration fact"* — where card inputs persist and whether that schema can carry the evidence is U20, read in G0 before chunk 8 is planned.
- `complete` and every detector's refusal stay UNCHANGED (L7).

### 3.9 Build order — AMENDS v3 §3.9's table; the full chunk list with law steps is §8
- **Gates (astra F5, verbatim):** *"Production renames, DETACH operations and proof narrowing require the relevant mechanism experiments to PASS and the relevant production-scale rehearsal to PASS; “reported” is insufficient. Chunk 6 explicitly depends on those gates, the owner's retention rulings and the restore drill. FROZEN_ARCHIVE activation requires the swap rehearsal and item 12 authorization. MP4 has no activation path in this build; any later proposal requires its own demonstrated protocol and owner ruling.*
  *Chunk 0 plus 0b activation is an explicit exception to the E/R sequencing: after the required shadow night and the owner's timing decision, the effective lists and every non-i1 refusal activate together. While OFF, all associated store and CLI refusal branches preserve existing behavior."*
- v3 `:131` "before it has reported" → **"before it has PASSED"**. v3 `:145` "The first DETACH is chunk 6 (gated on his rulings + the restore drill)" → **"…gated on E PASSED, R PASSED, his rulings on items 1–2, and the restore drill"**.

### 3.10 Tests and operator commands — ADDED to v3's list (round-3 items ►►)
►► swap: a non-resident writer that commits between the activity check and the lock (astra F1) is IN the copy and the baseline; a writer queued behind the lock cannot write into `bars_legacy` after commit (gemini (a)); a source row older than the first child fails preflight, not the swap (grok P-span); `REPLACED` fails on a single changed `close` with equal keys and counts (grok); a statement deadline cancels a slow copy and the source is intact (F6) · ►► rollback: a non-resident correction arriving during reverse reconciliation is excluded by the fence, not missed (F1) · ►► poller: Monday 04:00:30 with W1 present and W0 missing aborts RED (grok P-closed); a zero-submitted-row cycle after a mid-cycle detach still refreshes coverage before publishing health (F2-1); a non-routing write error with full coverage keeps the generic `error` class and names no range (F2-2); an uncovered historical subset plants `no_partition` only, never `error`/`stopped_at`, and `newest` comes from persisted rows (grok) · ►► gap states: first contact with a null watermark does not raise; an empty export yields no range and no crash (gemini (d), grok, astra); the overlap minute 09:58 omitted by a response that covered it is `no_print_as_fetched`, and the boundary minute at `threshold` is asserted explicitly (grok P-range, hub #5); a ticker failure today does not label 09-04 10:17 `ingestion_failure` (F3); an unattempted ticker in a cycle with errors stays `unknown` (F3, grok) · ►► gates: chunk 6 refuses to run while E or R is not PASSED (F5); `FROZEN_ARCHIVE` cannot activate without item 12 (P-swap).
Operator commands, added: the admission close / reopen procedure (once U21 is settled) · the deployment-deadline cancel-and-recover procedure (F6).

## §4 HIS RULINGS — twelve, IN THE ORDER HE SHOULD RULE THEM (v3's item numbers kept so every report still matches)
The design decides none of them. **Before any of these: chunk E is reads-and-a-scratch-database only and needs no ruling — it needs his ONE approval of its launch rule list (L62).** "Reversible" = the decision can be undone without losing data.

| order | v3 # | the question, one line | A / B | houses, round 3 | FINAL recommends | reversible? |
|---|---|---|---|---|---|---|
| 1 | 8 | May a production-scale BARS-ONLY copy go into `cobalt_dev` for the rehearsal? | YES / NO | 4 of 4 YES | **YES.** NO ⇒ R cannot run ⇒ the swap stays BLOCKED; no unrehearsed path exists. | REVERSIBLE (drop the dev copy) |
| 2 | 9 | When do the Lists edit → `[i1]` and the writer refusal switch ON land? | Tue 09-22 or later, together / later | 4 of 4 together, after Monday's shadow night, before `append` | **Tuesday 09-22 or later, one deploy, both together.** Time-bound: every night it waits costs 790 Finviz requests. | REVERSIBLE (revert edit + switch; vendor re-serves ≈14 days) |
| 3 | 11 | The swap's shape: (A) everything copied with the system down for minutes, or (B) copy while live with change capture on the poller's write path? | A / B | **4 of 4 A, on a weekend**; B only if R cannot fit the window he names | **A, in a measured weekend window HE names**; the statement and deployment deadlines come from R (F6). B needs its own design + tribunal. | REVERSIBLE until the swap runs |
| 4 | 12 | After the swap, may the frozen `bars_legacy` be proven OFF the outage only? | YES / NO | grok YES · gemini YES · astra CONDITIONAL YES (frozen relation, trusted digest, recoverable backup) · fable YES | **YES with astra's conditions.** NO = ≈107 s on every later deploy. While it waits: NO (MP1 covers it). | REVERSIBLE as a decision; damage to the archive's non-i1 rows would NOT be |
| 5 | 4 | The repair seam: A only / add D (closed weeks) / B (per-target lock + generation on the poller) | A+D / B | **NOW 2–2:** grok A+D, "do not build B" · fable A+D, B held · **gemini CHANGED to "Add B"** · astra B | **A + D; B held.** B is a live-write-path change against 09-19 R8 and would need its own design + tribunal (L67); accepting A means accepting D1/D2 (§5). A only until he rules. | REVERSIBLE |
| 6 | 5 | May the deploy-time proof read less than the whole live table (the sealed ledger, "5b")? — **5a (MP4) is REMOVED: no activation path in this build (astra F5)** | YES after E / NO | grok YES only if E-3/6/7/8/9 pass, else F-A, **never F-B** · gemini yes · astra retain MP1 until the complete protocol passes · fable after E | **Ask him AFTER chunk E, with E's result.** MP1 until then. If E cannot exclude a bypass: F-A automatically; F-B is not offered until its five procedures exist (F4). | REVERSIBLE (`--full` always exists) |
| 7 | 10 | The per-side proof budget that paints AMBER | 30 s / other | his; 30 s is nobody's ruling | **30 s/side until he says otherwise.** | REVERSIBLE |
| 8 | 7 | K and cadence of the vendor-i2 sample cross-check | OFF / K = n | ships OFF 4 of 4 (gemini's K = 50 not restated in round 3) | **OFF; rule K after U10.** | REVERSIBLE |
| 9 | 1 | How long do i1 bars stay in the live table? | ≥365 days after the restore drill / shorter | grok 365 after the drill, and says the harm plainly: under MP1 ≈14 min of deploy outage at 12 months · gemini 365 · astra indefinitely for now · fable ≥365 | **Nothing detaches now; everything hot until E, R and the restore drill pass; then 365 days.** | **IRREVERSIBLE in combination with item 2 = delete** (the vendor re-serves ≈14 days only) |
| 10 | 2 | When bars age out: cold file or delete? | DETACH + verified cold file / DROP | 4 of 4 DETACH, never DROP; astra adds a separate durable copy | **DETACH + verified cold file + restore drill first.** | **DROP is IRREVERSIBLE**; DETACH is not |
| 11 | 3 | Cap how many tickers are stored? | none / cap | 4 of 4 none | **None.** Nothing built. | REVERSIBLE |
| 12 | 6 | May a permanent derived rollup exist? | no / yes | 4 of 4 no | **No.** | REVERSIBLE |

NOT ASKED NOW, named so it is never done by default: the disposal of `bars_legacy` and of the 3,553,219 non-i1 rows — **IRREVERSIBLE**, his word only, after the swap, never part of any chunk here. Items 11 and 12 (order 11–12) can be one word each. No new item was added: F6's deadlines live inside item 11 (the window he names); F4's F-B acceptance lives inside item 5; C1 is not an owner item unless he wants it.

## §5 CARRIED TO THE OWNER — verbatim, never voted (L39). Each: the house · what it asks of him · what FINAL does if he says nothing
### 5.1 The three NOT RESOLVED rows (astra) — FOLDED; nothing is asked unless he wants otherwise
- **G-01 (astra):** *"Reading bounds solves the original mixed-batch ambiguity, in either row order. However, §3.2's exception branch labels every remaining write exception `no_partition`, even when refreshed bounds cover every submitted timestamp. Its once-per-cycle inventory also permits a current partition to disappear without another write exposing it. Concrete sequences and replacement: F2."* — Asks: nothing; F2 is folded verbatim (§3.2). Default: built as F2.
- **G-12 (astra):** *"Copying all timestamps removes T0's age assumption. However, “every writer is down” is implemented as residents down plus a point-in-time activity check. The staged harness establishes its repeatable-read snapshot before `_apply`; a nonresident writer can commit after that snapshot and before the exclusive lock. Both copy and comparison can omit its acknowledged row. F1 supplies the sequence and replacement."* — Asks: nothing; F1 folded (§3.4). Default: built as F1; E-15 demonstrates it.
- **G-13 (astra):** *"Full upsert correctly carries an old-timestamp correction, and stopping on legacy-only keys avoids silently resurrecting deleted data. But “residents down” still does not enforce the quoted requirement to drain every writer during rollback. A concurrent acknowledged correction can miss reconciliation. F1 covers both directions."* — Asks: nothing; F1 folded. Default: the rollback uses the swap's fence.

### 5.2 The twelve dissent blocks
**gemini (2)**
- *"D1: A repair seam lacking a strict per-target lock allows delayed poller threads to overwrite your reviewed corrections with bad data in a seconds-wide race. If this happens, wrong prices return to your cards. It is highly likely to happen during latency spikes. Fixing this now costs a simple generation lock; fixing it later means chasing phantom data regressions in production."* — Asks: item 4 = B. Default: A only (09-19 R8); D if he adds it.
- *"D3: After a vendor restatement, the unchanged poller will continue mixing old and adjusted prices. A red dashboard alone does not stop these mixed, wrong prices from reaching your cards. This is guaranteed to happen on every restatement. Fixing it now by suppressing price-derived outputs on degraded tickers costs little; fixing it later means trading on false signals."* — Asks: a degraded/suppressed state on the card's face after a restatement. Default: **this design does not fix it**; a suppression reaches the card (L7) and is its own design + tribunal.

**grok (4; its N3 line is "NONE")**
- *"**D1 — delayed poller overwrites a reviewed repair (open week).** What: you correct NVDA's 10:17 bar at 20:40; a poller fetch that started at 19:58 lands after and `DO UPDATE`s the old print back (`poller.py:64` hook optional, `:103-105` no generation). Closed weeks: only if you add D. How likely: any repair that overlaps an in-flight fetch — seconds-wide, not theoretical. Cost now: D on closed children (already in the candidate ledger). Cost later: one bad print under a card you already reviewed, found when you replay."* — Asks: item 4 add D. Default: A only.
- *"**D2 — a quiet-window margin is not exclusion.** What: repair starts 03:50, scanning opens 04:00, in-flight write commits 04:00:20 onto the corrected row. Same overwrite as D1. How likely: every pre-open repair that is not finished and committed before the first poll acquires. Cost now: finish repairs inside the pause or take D. Cost later: same mixed print, plus a false sense that "≥5 min after last poll" closed it."* — Asks: item 4; an operating rule that repairs finish inside the pause. Default: A only.
- *"**D3 — restatement mixes old and new prices on the card.** What: vendor restates a morning; append writes new minutes; old minutes stay; the card still mixes both. This design does not fix it (red dashboard does not reconcile). How likely: every restated name until you rule a degraded/suppressed state. Cost now: L9-degrade that ticker (reaches the card face — your word). Cost later: size off a mixed tape."* — Asks: his word on an L9-degraded ticker state. Default: not fixed here.
- *"**N2 — Astra wants B (per-target lock on the poller).** I still refuse B. What: a bug on the 04:00 write path is a dead radar, not a slower night. How likely: first non-trivial lock/generation bug in a scan loop. Cost now: nothing — A+D holds. Cost later if you take B: you debug the poller during premarket instead of during a pause."* — Asks: item 4 ≠ B. Default: B not built.

**astra (6)**
- *"**G-12/G-13 — write fencing:** A separately launched job can acknowledge a bar or correction that the swap or rollback omits. The overlap is probably uncommon, but no staged evidence measures it. Fix admission and snapshot ordering now; after cutover, finding stranded corrections requires reconciliation and another outage. F1 is required."* — Asks: nothing further; F1 folded.
- *"**G-01 — coverage handling:** A disappearing current partition can escape a cycle with no submitted rows; unrelated write errors can be mislabeled as missing partitions. Both become deterministic under the described events, though their production frequency is unknown. F2 is a contained change now; later diagnosis starts from misleading failure records."* — Asks: nothing further; F2 folded.
- *"**D1 — delayed poller write:** A response fetched before a repair can later overwrite the reviewed price. I judge the race uncommon under quiet-window discipline, but possible and unmeasured. Transactional exclusion now costs a separately reviewed live-path change; afterward it costs identifying and repairing the overwritten bars."* — Asks: item 4. Default: A only.
- *"**D2 — quiet-window margin:** Starting a repair before scanning opens does not itself stop it overlapping a later writer. Likelihood depends on delays and repair duration, neither measured here. Enforced exclusion costs design and testing now; after an incident, it also costs an outage and price reconciliation."* — Asks: item 4. Default: A only.
- *"**D3 — mixed adjusted prices:** Old and adjusted prices can coexist despite a red dashboard. This is a credible restatement failure, not a quantified daily probability. An integrity state and authorized suppression require separate card-facing work now; later, affected outputs and stored history also need review."* — Asks: a separate card-facing design. Default: not fixed here.
- *"**N2 — recommendation B:** I still recommend transaction-level repair exclusion and generation validation, subject to your current A ruling. It introduces live-path implementation risk but directly addresses D1/D2. Building it later remains possible; accepting A meanwhile accepts those correction-overwrite risks."* — Asks: item 4 = B. Default: B not built.

### 5.3 House-vs-house, not smoothed
- **C1 — the empty vendor export (a conflict of FIXES).** Gemini: *"The covered range for an empty success must use the requested bounds, not the returned row content."* Grok: *"empty successful export → **no covered range**, absences `unknown`."* Astra: *"An empty response supplies no inferred range."* — Asks: only if he wants an empty export to count as "covered, no print". Default: `unknown` (2 of 3, the conservative label, and the poller sends no requested bounds today — `poller.py:88`).
- **C2 — a writer that commits after the activity check (a disagreement of FACT, hub #30).** Grok: it *"either finishes (its rows are in the copy) or holds a lock … and the swap aborts — production unchanged."* Astra: its row is omitted from copy and digest (F1's sequence). — Asks: nothing. Default: astra's fence is built (the conservative side); E-15 shows which is true.
- **N3 (gemini, round 2: always abort) — CLOSED.** Gemini round 3: G-03 `RESOLVED`, no N3 block. Grok: "NONE". Astra: "N3 is not my dissent."
- Astra, carried from round 1 and not answered by this build: *"The separately staged Grok half-day freshness objection is also not answered by finer bar granularity: a shorter session can still trip an export-freshness rule comparing against a previous full session."*

## §6 CHUNK E — 19 experiments on a SCRATCH database (production PG16 image, synthetic rows; never `cobalt_brain`, never a production row) — the FIRST GATE
Rows E-1, E-2, E-11 stand as v3. **Every "if not" that named a keyword, a generation or "counters alone" is replaced by F-A (astra F4).** Amended and new rows:

| E | experiment (amendment in the house's words) | expected | if not |
|---|---|---|---|
| E-3 | as v3; astra: *"“Counters alone” is acceptable only after a complete counter protocol passes its own tests; otherwise F-A."* | ordinary silent; ALWAYS fires | F-A |
| E-4 | as v3 + astra: *"Demonstrate those triggers under replica role too. A surviving `relfilenode` change can force content proof; it is not itself content equality."* | row trigger silent; `relfilenode` moves; TRUNCATE trigger fires, also under replica role | F-A |
| E-5 | as v3 | INSERT row triggers fire | **F-A** (the `COPY` keyword stays only as a widening rule) |
| E-6 | as v3 + astra: *"Specify whether counters are inspected before commit or after statistics publication."* | OID + `relfilenode` unchanged; `n_tup_upd` moved | **F-A** |
| E-6b | grok's candidate: DETACH bound to `cobalt bars close` in one transaction with `generation + 1`; then repeat E-6 | the generation breaks equality | the candidate is dropped; F-A |
| E-7 | as v3 + astra: *"test the exact comparison algorithm, cache behavior and trusted-baseline selection."* | `_xact_` view shows own DML; cumulative does not | **F-A (not F-B — grok)** |
| E-8 | as v3 + astra: *"transaction-local DML evidence must still force comparison with trusted content."* | trigger silent; hash equal after restore; counters moved | F-A |
| E-9 | as v3, expectation CORRECTED (astra): *"Clean restarts can preserve statistics; zero-valued counters can reset to zero. Test damaging histories that collide with the stored tuple, not merely harmless equality."* | no colliding history passes equality | counters dropped ⇒ F-A |
| E-10 | as v3, expectation CORRECTED (astra): the wait is *"workload-dependent, not guaranteed to equal one digest"*; the residents-down fallback *"must include nonresident writers and the snapshot ordering in F1"*; the seal takes its lock BEFORE its content snapshot (F4) | no invalidation cleared unread; the wait is MEASURED | seal only under the F1 fence; a cleared invalidation ⇒ F-A |
| E-12 | as v3 + astra: `INSERT … SELECT` is one statement, `executemany` is many inside the store's transaction — *"Verify the context manager rolls back earlier executions on error. Bounds prechecking alone cannot replace atomicity if this test fails."* | nothing persists in either case | the swap and chunk 2 are BLOCKED until the store's transaction handling is fixed |
| E-13 | as v3, expectation CORRECTED (astra): *"Correct measurement, not a near-zero guarantee."* Deferred registration only if placement validation stays separate and the swap registers the complete proof set. | a MEASURED cost, small | register the parent only inside the swap deploy |
| E-14 | SPLIT (astra): an existing conflicting reader; an idle transaction; fresh statements on old connections — each separately, under `lock_timeout` | the blocker times the migration out cleanly | restart / terminate that class of session BEFORE the renames are trusted |
| **E-15** | **NEW — the pre-probe snapshot race (astra F1, hub #13):** RR transaction reads → another session commits an i1 row → the first takes ACCESS EXCLUSIVE, copies, digests; then the same with the lock FIRST | probe-first: the late row is ABSENT from copy and both digests (astra's sequence); lock-first: PRESENT | lock-first does not see it ⇒ chunk 4 BLOCKED, the dedicated-transaction shape is redesigned |
| **E-16** | **NEW — sessions across the rename (gemini (a), grok, hub #15, #25):** a writer queued on the lock; a prepared statement on an old session; which relation each hits after commit | **CONTESTED** — v3 said old connections see the new table; grok and gemini expect the old OID (`bars_legacy`) | the design does not depend on the answer (admission is closed through commit and connection replacement); if any excluded session CAN write into `bars_legacy`, closing admission must end such sessions before commit, else chunk 4 BLOCKED |
| **E-17** | **NEW — grants through the parent (grok, hub #24):** a role with SELECT on the parent only reads rows living in a child with no GRANT | **CONTESTED** (grok: a missing-history read) | grants on parent + EVERY child, applied by `ensure` to each new child, asserted after the swap (the default already taken in §3.4) |
| **E-18** | **NEW — deadlines (astra F6, hub #17):** `lock_timeout = 1s` then a long `INSERT … SELECT`; then a statement deadline and an overall deadline; measure recovery, WAL and cleanup | `lock_timeout` does NOT cancel the running copy; the deadline does, source intact | the source is not intact after a cancel ⇒ chunk 4 BLOCKED |
| **E-19** | **NEW — a bypass combined with a counter reset / collision before commit (astra's third uncovered route)** | the protocol detects it | F-A |

**The 9 NOT CHECKABLE items of round 3 → all covered, none uncovered:** gemini (a) → E-16 · astra F1 snapshot → E-15 · grok grants → E-17 · grok prepared statement → E-16 · astra E-12 context manager → E-12 · astra E-9 / E-10 / E-13 expectations → those rows, corrected · `lock_timeout` vs a running statement → E-18. **E must PASS, not report (F5).** R repeats E-14…E-18 at production scale.

UNVERIFIED, added to v3's U-list: **U20** where card inputs persist and whether that schema can carry F3's evidence (read in G0; astra: "no new table" is an option, not a fact) · **U21** the MEANS of closing database admission to every non-migration writer — no house named one and this seat invents none; the chunk-4 plan names it, E-15/E-16 and R demonstrate it, ≥3 checkers read it · U19 still open for `ops/`, `dev_utils/` and the old tree (grok).

## §7 NUMBERS — changed rows only; every other v3 §7 row stands (hub: ARITHMETIC OK on all)
| number | arithmetic / source |
|---|---|
| **END-TO-END swap outage, today's size — ILLUSTRATION ONLY (copy rates ASSUMED at 100,000 and 25,000 rows/s; the filtered digest ASSUMED at the unfiltered 170,000 rows/s; U5):** 187.8 + 42.2 = **230.0 s ≈ 3.8 min** · 346.3 + 42.2 = **388.5 s ≈ 6.5 min** | 149 − 106.8 = 42.2 s of other deploy work (09-19 deploy 3); astra F6, gemini (h) |
| **the same at +20 trading days — ILLUSTRATION ONLY (also ignores non-i1 growth until chunk 0):** 335.2 + 42.2 = **377.4 s ≈ 6.3 min** · 653.6 + 42.2 = **695.8 s ≈ 11.6 min** | hub cross-check row "+20 trading days" |
| NOT included in either, and unmeasured: admission closure, the baseline digest of F1 (one more pass over source i1, ≈31 s at today's size by the same assumed rate), connection replacement, abort / crash-recovery reserve | F1, F6 — R replaces all of it with one measured end-to-end figure |
| STRUCK from v3: "≈3.1–5.8 min" and "≈5.6–10.9 min" AS THE OUTAGE (they are copy-and-proof time) · "inside the 60-minute pause with ≥5× slack" · "no session opens for >48 h" · "a writer waits ≤ one child's one-sided digest (≈7.8 s)" as a maximum (it is an estimate) | F6; astra's arithmetic audit |
| MP1 today **106.8 s is MEASURED**, not 8,834,532 × 2 ÷ 170,000 (= 103.9 s); 170,000 is a round of 165,782–187,769 | grok (h) |
| disk ≈ 820 MiB today / ≈ 1,648 MiB at +20 td — TABLE BYTES ONLY; the gate is sized from R's measured peak incl. WAL/temp, never from this row | v3 `:88`, `:238`; grok |

## §8 THE BUILD, IN ORDER — law step of every chunk (L67: build → ≥3 checkers → deploy; L73: no step skipped). NOTHING IS LAUNCHED BY THIS DOCUMENT.
The design step is CLOSED for everything below (three rounds, L39). It is NOT closed — own design + own tribunal first — for: swap option B · repair seam B · any D3 card-facing suppression · a coverage ledger · MP4 · F-B.
**The build waits for (1) his rulings in §4's order and (2) his ONE approval of chunk E's launch rule list (a scratch-database hub = new rule strings, L62).**

| # | chunk | production effect | law step now | waits for |
|---|---|---|---|---|
| G0 | Gates, reads only: U1, U3, U4, U9, U18, U19 (+ `ops/`, `dev_utils/`, old tree), **U20** | none | run (a hub, reads) → results read by ≥1 other house | nothing |
| **E** | **E-1 … E-19 + E-6b on a scratch database** | none | build the harness → run → results checked by ≥3 (they gate everything) | his approval of its launch list (L62) |
| 7 | Derivation + segmentation siblings, provenance flag, 52-key fixture, DST test | additive | build → ≥3 checkers → deploy | nothing (parallel, L72) |
| 8 | Gap states per §3.7 (F3 + P-range) + table-level probe + growth metrics | additive | build → ≥3 checkers → deploy | U20 |
| 0b-impl | Writer closure behind its switch, OFF; inert while OFF is a test | none while OFF | build → ≥3 checkers → deploy | nothing |
| 0 + 0b-on | Lists `archive:` → `[i1]` (desk edit, L65) + switch ON, one deploy + effective-target proof — **the explicit exception to E/R sequencing (F5)** | non-i1 WRITES stop; no row deleted | deploy (≥1 other house reads the deploy prompt) | **§4 order 2**; after the R35 shadow night; before `append` |
| 1a | Harness: `REPLACED` bound to the MP1 fold with a filter; per-table proof policy; `placement.py` pattern; proof statuses; the lock-before-snapshot phase (F1) | none until used | build → ≥3 checkers → deploy | E-13, E-15 PASSED |
| 2 | EMPTY `bars_p` with children over `[min(ts) of source i1, now + horizon]` + `ensure` (+ grants per E-17) + coverage probe + the poller's bounds check per §3.2 (inert until the swap) | an empty table; an inert path on the live write path | build → **≥3 checkers (live write path)** → deploy | E-12, E-17 PASSED |
| 5-dev | MP2 ledger, ALWAYS triggers, seal lock (lock before snapshot), counters, E-6b if it passed — DEVELOPMENT only | none | build → ≥3 checkers | E-1 … E-10, E-19 PASSED |
| S | Production-scale bars-only copy into `cobalt_dev` | none (dev) | run | **§4 order 1** — withheld ⇒ R cannot run ⇒ chunk 4 BLOCKED |
| R | REHEARSAL at production scale: admission close → the whole swap transaction → restart, timed END TO END; E-14…E-18 at scale; a crash mid-copy; the deadline cancel; the step-6 rollback under the same fence, timed; trigger overhead; seal wait with a multi-child first-sight writer; peak disk / WAL | none (dev) | run → results checked by ≥3 | 1a, 2, 5-dev, S; U21 named |
| 4 | **THE SWAP** — fence + copy + baseline + `REPLACED` + renames + grants, one transaction | the table is replaced; **nothing deleted** | deploy prompt → ≥3 checkers → deploy, residents down (L66), L43 / L68 | **E PASSED; R PASSED;** §3.4 step 1's gates; **§4 order 3 (item 11) AND order 4 (item 12) RULED**; the stated end-to-end outage and deadlines |
| 5-prod | MP2 activated in production | the deploy proof narrows | deploy | 4; E PASSED for every guard MP2 relies on; **§4 order 6 (item 5), asked after E** — else MP1 |
| 6 | Retention: cold-file format, **restore drill on `cobalt_dev` BEFORE the first production DETACH**, cold files inside the restic set (U12) | first DETACH | build → ≥3 checkers → deploy | **E PASSED; R PASSED; §4 order 9–10 (items 1–2); the restore drill** |
| 9 | DevDocs, smoke, docstrings — beside each behaviour change | — | with each chunk's close | — |

**Is anything destructive reachable before E and R have PASSED?** No chunk deletes. 0 + 0b-on stops non-i1 writes before E/R by design, on his timing, reversibly — the one stated exception. The first rename (4), the first proof narrowing (item 12 at chunk 4, or 5-prod) and the first DETACH (6) are each gated on E PASSED + R PASSED + his word.
