# DRC AUTOMATION — v2 (derived 2026-09-22) · ASTRA PENDING (R13)

Derived by the derive seat `drc-tribunal-derive-0922` (`claude-fable-5-1`, R73 `derive seat: claude-fable-5-1`; the same SEAT that ruled round 1 blind — it recommends nothing, L37) from:
- the proposal `docs/30 - Design/DRC-AUTOMATION-PROPOSAL-2026-09-22.md` (Opus 5.5, `c52133c`), with its `## 13. DIFF MODEL` AS RULED — `cto-2026-09-22.md` §4 R69 (A1–A23), R70 (A24–A39), R71 (B1–B35), R72 (C1–C13); the inputs R65, R66, R67; R60 (lane); R68 (the diff-model instruction);
- round 1 of every seat that ruled: Grok `scratch/tribunal-bars-0920/drc-tribunal/r1/grok-ruling.md` (in full, 21 items) · Gemini `…/r1/gemini-ruling.md` (in full; T1–T10 tagged, (a)–(j) untagged prose) · the Fable seat `docs/40 - DevDocs/reports/drc-tribunal-fable-r1-2026-09-22.md` (blind, `Fable seat: yes` on `claude-fable-5-1` per R73 — a PER-CASE OVERRIDE of R36, L73) · the hub `docs/40 - DevDocs/reports/drc-tribunal-2026-09-22.md` (`DRC TRIBUNAL R1 DONE`, houses that ruled 2 of 3) · Astra: `METER — ASTRA PENDING (R13)` — Astra reads this v2 when its meter returns (Sat 2026-09-26);
- the fold table: `docs/40 - DevDocs/reports/drc-tribunal-derive-2026-09-22.md` `## Fold table` (every `[F-nn]` below is a row there).

Reading rule: every seat wording below is VERBATIM from the raw ruling file named in its tag; the derive edited none. The hub's `## Wording offered, verbatim` attributed Fable-seat text to gemini on T3, T4, T6, T7, T9, T10, (e), (g), (h); this v2 takes gemini's wording from the raw file only (report ESCALATE 1). Where the rulings leave a gap, it is a named gap (experiment, owner item, round 2), never filled here. Every value that is his (O1–O18, every threshold, window, limit, key value, N) stays his, read by him AFTER the tribunal; no chunk's BUILD depends on one (L69). `ASTRA PENDING (R13)` marks every question only Astra would have answered.

Vendor names in identifiers (L31): the proposal's `das.py`, `das_import_id`, source value `das`, flag `das_export_shape` are carried AS WRITTEN because the house wordings that name them are taken verbatim; each carries the tag `[L31 — NAME PENDING]`: the Fable seat's ruling (FC1 HOLDS against `LAWS.md:173-175`) renames them `trading_log.py` / `import_id` / `trading_log` / `trading_log_shape`; neither house saw L31 (the packet cut it — hub ESCALATE 1). A rename at build changes no ACTION, WRITER, table, mechanism or chunk; it is the desk's call (report ESCALATE 2), not settled by the seat that raised it.

LADDER `S3-P3 · F14` (+ the F15 seams it needs). L67 step: THIS IS THE DERIVED v2 — round 2 is owed ONLY for `## OPEN — ROUND 2` (one item, R2-1); Astra reads the FINAL on Sat 09-26 (R13). Content/process source: his coach's spec `docs/_inflight/DRC-automation-spec-2026-09-22.md` (gitignored, L32) — cited as `SPEC §n` by section and key only; no value, rule sentence, ticker or price of his appears in this file. Automation pattern: `docs/90 - References/trade-reporter/` (pattern only). NO PDF (R65 c). Output: `1 - Trading/5 - Review/DRC-<date>.md` from HIS template in `5 - Templates` (R65 d). Nothing in this design reads, writes or infers from his trading platform — the export he drops by hand is his own exported file, read-only (R66 a; all three seats: boundary NONE).

## 1. FACT BASE

| # | Claim | file:line | Status |
|---|---|---|---|
| F1 | The 15:40 job runs `uv run prefill drc` Mon–Fri | `ops/com.cobalt.prefill-drc.plist:24-29`, `:50-54` | PROVEN |
| F2 | It creates the note whole only when absent, from the REPO copy `drc.md.j2`, then returns | `prefill/drc.py:270-272`, `:427-432`; `prefill/config.py:30` | PROVEN |
| F3 | On an existing note it upserts three units: `drc-risk/risk_parameters`, `drc-trades/tickers`, `drc-rules/rules_check` | `prefill/drc.py:12-16`, `:441-449` | PROVEN |
| F4 | `DRC-2026-09-21.md` was rendered from the repo copy, not his Obsidian template (date line, no example blocks, `### Cobalt Rules Check` heading that only `drc.md.j2:132` carries) | `DRC-2026-09-21.md:5-17`, `:228`; `drc.md.j2:5-17`, `:132`; `5 - Templates/DRC.md:7`, `:60-124` | PROVEN (shape) — contradicts the "Cobalt uses the Obsidian template" fact recorded at `cto-2026-09-22.md:197`; that note only (author's ESCALATE 6 → a desk record item, §11) |
| F5 | His template uses core-Templates tokens `{{date:YYYY-MM-DD}}` (two places), carries its own risk-parameters line, two filled example trade blocks and a filled example If/Then | `5 - Templates/DRC.md:7`, `:17`, `:51`, `:60-124`, `:177-187` | PROVEN (Fable seat: exactly two `{{` lines — hub Rulings table T8) |
| F6 | Placements for the three units already handle a note Cobalt did not create (below his risk line, under the trades heading, end of note) | `prefill/drc.py:321-347`, `:441-449` | PROVEN (code) · on HIS template's text: UNPROVEN → E3 |
| F7 | `create_if_absent` never rewrites; `upsert_unit` refuses an absent note | `vaultwrite/writer.py:559-619`, `:669-673` | PROVEN |
| F8 | Every vault write is refused inside `market_reset` (repo targets exempt) | `vaultwrite/writer.py:387-416` | PROVEN |
| F9 | Atomic write + mtime guard, versioned rows, human-wins merge | `vaultwrite/writer.py:457-528`, `:644-661` | PROVEN · "orphaned text listed" is NOT in the writer or the merge (`grep -ri orphan src/cobalt/vaultwrite/` → 0; `writer.py:684-806` read by the derive) → E8 `[F-21]` |
| F10 | Replay (21:10) writes `drc-misses/miss_line` into an EXISTING note only; absent → `DrcNoteAbsent` | `replay/line.py:28-34`, `:155-172`; `ops/com.cobalt.replay.plist:38-42` | PROVEN |
| F11 | The line step's failure is recorded and re-raised → the replay run FAILS on an absent note | `replay/runner.py:443-466` | PROVEN |
| F12 | The 09:00 reader reads only the prior note's `Grade:`/`Goal:` lines; absent note = loud `no prior DRC` | `daymode/drc.py:85-139` (a filled grade must be a ladder value, a filled goal must contain a digit — `:116-132`, hub HOLDS); `ops/com.cobalt.daymode-propose.plist:51-55` | PROVEN |
| F13 | The one web surface is the ASET app — **FastAPI**, not Flask ("modeled on the trade-reporter Flask pattern") | `aset/web.py:1-4`, `:83` | PROVEN |
| F14 | Its forms parse with `request.form()` (multipart-capable parser) | `aset/web.py:947`, `:1048`, `:1147`, `:1190`, `:1247`, `:1282` (grok WRONG FACTS #3 — six sites, hub HOLDS) `[F-02]` | PROVEN |
| F15 | `python-multipart` is locked only as a transitive dependency (via `mcp`); `pillow` via `matplotlib`; no OCR library in the lock | `uv.lock:2879`, `:2789`; `pyproject.toml:32-33` | PROVEN |
| F16 | Bind is config: dev = loopback, a local override = `lan` | `configs/dev/aset.yaml:39-41`; `configs/dev/aset.local.yaml:28`; `aset/__main__.py:28-50` | PROVEN · which file production loads: UNPROVEN → E7 (`aset/config.py:41-42` names both paths — read by the derive; which one production has is E7) |
| F17 | The trading PC is not on the tailnet | `SPRINT-LADDER-v0_1.md:610` | PROVEN (text) |
| F18 | Cards live in `aset_sizings` (ticker, direction, grade, entry, stop, shares, risk, state, actual_fill, recomputed shares, created_at) | `aset/store.py:292-322`, `:268` | PROVEN |
| F19 | The daily note carries the cards as ```aset / ```aset-fill fences — a rendered copy of the DB rows | `1- Daily Notes/2026-09-21.md:385-546`; `prefill/drc.py:70` | PROVEN |
| F20 | Rules parser: numbered lines, exactly one trailing tag, fail-loud; no dated/expiring-rule syntax; rules are numbered by ORDER (`rule_number += 1`) | `prefill/rules_gen.py:37-107`, `:88-108`, `:128`; `Rules.md:4-15` | PROVEN (order numbering: FC7 HOLDS) |
| F21 | Daily note premarket keys that exist: `Sleep:`, `Readiness:`, `RHR:`, `1% goal:`; meditated / tone / hotkey are not keyed lines (free text in a segment-score line) | `daily.md.j2:11-16`; `1- Daily Notes/2026-09-21.md:11-17`, `:330` | PROVEN · what `Sleep:` measures (hours vs score): UNPROVEN → O11 |
| F22 | The committed daily template hard-codes a daily-stop dollar line | `configs/cobalt/templates/daily.md.j2:18` | PROVEN (author's ESCALATE 1 → T9 `[F-18]`, O5) |
| F23 | `"user".legs`, `fills`, `drc_rows`, `prediction_records` are DECLARED, not built (`placement.py:109-112`); `order_fills` is a taken old-tree name (`:143`). `[F-02]` Grok WRONG FACTS #1, verbatim: "`10-PROPOSAL.md.part1` F23 says `public.trades` is taken at `placement.py:106-112` and `:143-147`. The staged placement grep (`02-greps.txt`) shows `legs`, `fills`, `drc_rows`, `prediction_records` at :109–112 and `order_fills` at :143. It does not show `public.trades`. The collision is not established." (hub file-check: HOLDS — no `public.trades` anywhere in `placement.py`) | `db_migrations/placement.py:106-112`, `:143` | PROVEN as corrected |
| F24 | Migrations on main end at `0011`; unmerged branches add `0012` (bars, `02d67a6`) and `0013` (setups, `61a283f`); `0014`/`0015` claimed by H1 / stale-score / S3 M1 | `git log --all` on `db_migrations/`; `S3-EXITS-v3-2026-09-22.md:139` | PROVEN (0012/0013) · 0014+ claims: text only — gemini's "`db_migrations/0014_legs.sql`" DOES NOT HOLD (hub) and is not carried `[F-34]` |
| F25 | v3: one `legs` table, estimated vs confirmed flag, `realized_r.1` pure function, `fills` kept declared (strike = v3 O16), "Nothing reads DAS" | `S3-EXITS-v3-2026-09-22.md:110-139`, `:164`, `:168`, `:237` | PROVEN (text) · `[F-03]` Grok WRONG FACTS #2, verbatim: "`10-PROPOSAL.md.part1` F25 status says `v3 R2-2 OPEN FOR DEJAN`. `12-rulings.md` R67 ruled it, and the same proposal's F33 says so. The v3 excerpt still prints OPEN because that file was not edited. The decision is not open." (hub HOLDS) — R2-2 is RULED by R67 (desk reading B = the Fable seat's N + his three additions); v3 + R67 = the S3 exits FINAL |
| F26 | v3 names seven F14 seams (trade_note_path, trade_def, fill data from legs, realized R + provisional, stop path, pick row, cf-R at 21:10) | `S3-EXITS-v3-2026-09-22.md:228` | PROVEN (text) |
| F27 | S2 smoke checks the `drc-misses` unit and the prefill-drc job's last run | `configs/cobalt/smoke/s2.yaml:383-398`, `:436-438`; `configs/cobalt/jobs.yaml:181-186` | PROVEN |
| F28 | Charter F14 and the S3 smoke require "DRC note exists at 15:41" | `MVP-CHARTER-v0_2.md:160-171`; `SPRINT-LADDER-v0_1.md:612`, `:615-617` | PROVEN — contradicted by R66 (b) → O18 |
| F29 | Trade-reporter pattern: drop / click / paste per zone → client reads to data URL → one JSON POST → `build_drc_pdf(data: dict)` → artifact under `tmp/<uuid>` → view/download | `app.js:109`, `:121`, `:152-164`, `:263`; `app.py:10`, `:21-31`, `:47-68`; `drc_builder.py:60`; `index.html:62-63`, `:72-73` | PROVEN |
| F30 | `_imports/` is a Cobalt-owned vault location by the 09-06 R3 ruling; it does not exist in the vault today | `vault.py:99-101`; `find` of `Vault/Think` (none) | PROVEN (FC6 HOLDS) |
| F31 | No real DAS export or TradeZella screenshot exists anywhere readable on this host | `find` over Vault, docs, Downloads, Desktop | PROVEN (absence) → E1, E2 |
| F32 | The DAS export carries an order log (stop placed/modified/cancelled/filled) in the same file | SPEC §1.1 row 1 | UNPROVEN → E1 |
| F33 | R67 (15:48 ET, S3-exits R2-2): the DAS import is the RECONCILE truth — Cobalt's legs are corrected to DAS as append-only correction rows naming the DAS source (L57), his taps / held-count statements kept as history; a mismatch is shown on the DRC, then adjusted; a position open at the DRC carries to the next day as an OPEN POSITION with its held shares until closed | `cto-2026-09-22.md` §4 row R67 `[F-01]` (cited by ROW, not line: the desk inserts rows above it — FC3 HOLDS; the proposal's `:35` was `:43` at the Fable seat's read and `:50` at the hub's) | PROVEN (ruling text; desk reading "B + his additions") |
| F34 | v3's `legs` correction row carries `price_source`; its values today are `last_poll` / `typed` / `dm` — no DAS value, no source-id column | `S3-EXITS-v3-2026-09-22.md:125`, `:132`, `:164` (read by the derive: `price_source` CHECK ∈ {last_poll, typed, dm}, `source` CHECK ∈ {panel, sheet, dm}) | PROVEN (text) → seam S-C1 below |
| F35 | `[F-04]` Gemini (a), verbatim: "Caller of the 15:40 job (`run_drc_prefill`): `src/cobalt/prefill/cli.py:49`" | `prefill/cli.py:49` | PROVEN (hub HOLDS) |
| F36 | `[F-04]` Gemini (a), verbatim: "Caller of the 09:00 reader (`prior_day_inputs`): `src/cobalt/daymode/cli.py:151`" | `daymode/cli.py:151` | PROVEN (hub HOLDS) |
| F37 | `[F-04]` Gemini (a), verbatim: "Caller of the miss line setup (`drc_note_path`): `src/cobalt/replay/cli.py:98`" | `replay/cli.py:98` | PROVEN (hub HOLDS) |
| F38 | `[F-04]` Gemini (a), verbatim: "Reader of the DRC note path for tests: `src/cobalt/smoke/checks.py:151, 566`" | `smoke/checks.py:122-129`, `:151`, `:566` | PROVEN (hub HOLDS; `:122-129` resolves the path from `prefill.yaml`'s two values — read by the derive) |
| F39 | `[F-05]` The hand path `uv run prefill drc` ships as a console script and a subcommand: `pyproject.toml:64` (`prefill = "cobalt.prefill.cli:main"`); `prefill/cli.py:1`, `:22`, `:25` (`DRC_JOB`), `:46-49` (`_run_drc`), `:67-69` (the `drc` subparser), `:79` — D3 removes the `drc` subcommand (§8) | as cited | PROVEN — UNCHECKED by a hub — read by the derive at those lines (Fable seat (a) F36) |
| F40 | `[F-05]` `ops/README.md:30`, `:145`, `:157` document the 15:40 job — D3 edits | as cited | PROVEN — UNCHECKED by a hub — read by the derive at those lines (Fable seat (a) F37) |
| F41 | `[F-28]` `configs/cobalt/jobs.yaml:309-316` lists `configs/cobalt/rules.yaml` under `no_resident_reads` with `readers: [com.cobalt.prefill-daily, com.cobalt.prefill-drc]` and "no resident imports either path" — false once the build calls `regenerate_rules_config()` inside `com.cobalt.aset` (T2) | `jobs.yaml:309-316` | PROVEN — UNCHECKED by a hub — read by the derive (Fable seat (j)); an L42 derivation item → D3 (§9), report ESCALATE |

## 2. THE IMPORT PLACE

**Where.** ONE page, `/drc`, on the existing ASET app (`aset/web.py:83`) — the one web surface (Charter §5, L3). A second app is not needed: the ASET app already serves forms, already writes the vault through `VaultWriter`, already runs as `com.cobalt.aset`. Reached from the trading PC over the LAN bind (F16, F17 — the tailnet does not reach that PC) and from the Mac/laptop over Tailscale. LAN reach is E7, an ops item after the build, never a precondition (all three seats).

**What it accepts.**
- The DAS export: exactly one CSV per day, filename shape per SPEC §1.1 row 1 (date in the name).
- TradeZella screenshots: PNG/JPEG, one per trade. The page lists the trades parsed from the CSV; each trade row has its own drop zone (trade-reporter's per-ticker zones, F29) — drop, click or paste. That binds a screenshot to a trade deterministically, no image reading needed.
- Optional per-trade typed cells for the few figures only TradeZella carries (SPEC §2 keys `MAE`, `MFE`, planned target, best exit) — blank stays blank (`not given`), never guessed. Choice vs OCR = **O1**.
- Upload is multipart (not trade-reporter's base64 JSON — screenshots are heavy; F29 uses a 50 MB cap). `python-multipart` becomes a DIRECT dependency at its already-locked version (F15) — no new package. No import-size ceiling is proposed here: SPEC §7 `limits` has no import-size key → **O22** (grok, MISSING).

**How a day is bound to its files.** A date field, default today (ET). `[F-06]` Grok T1, verbatim: "The one import place is `GET /drc` and `POST /drc/import` on the existing ASET FastAPI app (`aset/web.py:83`). Dropped bytes land under the vault's `_imports/drc/<YYYY-MM-DD>/` only by a new method on `VaultWriter` (the vault-write expert, L40), versioned and sha256'd, refused inside `market_reset` by the same `_session_gate` as notes (`22-vaultwrite.excerpt.py`). The route calls that method and the DRC store; it does not open vault paths or SQL itself. The note stays `create_if_absent` plus `upsert_unit`. O2 may move the home later; this slice does not grow a second byte writer." (`_session_gate` is `writer.py:387`, calling `assert_writable` at `:411` — read by the derive. The writer's existing commit path is text-only — `_commit` opens `"w", encoding="utf-8"`, `writer.py:470`, FC5 HOLDS — so the new method is a BYTES path the D2 builder adds inside `VaultWriter`; the Fable seat's competing import-store wording is not taken, report fold table.) The sha256 is recorded in `"user".drc_imports` (one row per file: date, kind, name, sha256, trade key for a screenshot, parse status, supersedes). `[F-07]` Grok (c), verbatim: "One web surface, the ASET app (L3). Side effects: vault bytes and the note through `VaultWriter`, `drc_*` rows through one DRC store, using the one connection factory. The route does not own a side effect (L40). Upload is multipart; `python-multipart` is pinned direct at the version already locked (`uv.lock:2879`, `:4460` in `02-greps.txt`), not a new package. There is no `UploadFile` in `src/cobalt` today. The day is the form date. An execution whose time is not that ET date FAILs the whole file. The filename is a gate only after E1 shows it carries that date. A screenshot binds only by the drop-zone trade key; a key not in this CSV FAILs that file and binds nothing. A file is `parsed` only after every row validates; a bad line rolls that file's rows back. The page says `DRC built` only when the event is `done`. LAN from the trading PC is E7 (dev bind is loopback, `aset.yaml:40`; production bind is UNPROVEN). It is not a precondition. Loopback on the Mac is the build." What happens to a screenshot binding when a SUPERSEDING export changes or drops its trade key is not settled by any house wording → **X13** (before D2).

**What it shows back.** Per file: parsed ✓ / FAILED with the reason and the line. Counts: executions, trades paired, screenshots bound / trades, trades without a screenshot, cards with no DAS trade. Status line: `waiting for: DAS export` · `waiting for: screenshots (k of n)` · `READY → DRC built: <note path>` · `DRC build FAILED: <step> — <reason>`.

## 3. THE TRIGGER

- **Both placed** = a parsed CSV for the date AND a screenshot bound to every trade it contains (or **O3**'s no-trade form). The import handler computes that state after every upload — deterministic code, no LLM (L2) — and writes one typed event row `DrcInputsPlaced{date, das_import_id [L31 — NAME PENDING], screenshot_import_ids[], sha256s}` with state `pending → running → done | failed` (L18); the event state lives with `"user".drc_imports` (§9 D1 "+ event state") — three tables, no fourth (T4 `[F-35]`).
- **The build** runs on that event. `[F-08]` Grok T2, verbatim: "The build runs in the request, and `cobalt drc build --date D [--dry-run]` calls that same function. The event row is the state record of that run (`pending` before any note write, then `running`, then `done` or `failed`), not a polled watcher and not a one-shot job. `done` is written only after the note write returns. A dead request leaves `failed`, never `done`. No second scheduler." Gemini T2 ADOPT; Fable seat ADOPT. E11 (grok) proves the dead-request path before D2/D3.
- **Only one placed** → nothing is created, no note, no unit. The page says what is missing. No nag timer, no failure: R66 makes "no DRC" his lawful choice. The heartbeat may show `DRC inputs: none / partial for D` as information (not red).
- **Placed inside `market_reset`** (20:00–21:00), re-import, and the CLI: `[F-09]` Grok (d), verbatim: "Both-placed is computed in the request by deterministic code. The event row is state (T2), not a watcher. One input writes no note, no unit, and no `done` row. The build function's first check is both-placed; a CLI run otherwise exits failed and writes nothing. Dry-run writes nothing. The 15:40 job creates notes until the (b) retirement. The miss line never creates a note. The 09:00 reader never creates a note. Re-import supersedes the file row, keeps the old bytes, and re-runs the same function: `create_if_absent` skips, derived units upsert, voice units follow T6. Inside `market_reset` the upload is refused, nothing is stored as parsed, and he drops again after 21:00. Not queued. Not accepted-with-defer. O14 may overrule that." (Gemini (d): refused with a reason, "silent queueing would violate predictability"; the Fable seat (d): refuse, O14 default — 3 of 3.)
- **Reconcile step (R67, F33) — inside the build, after parsing, before the note write.** (1) Match each DAS trade to its card (§4) and to that card's current legs (`legs_current_v`, v3 C1). (2) Compute the diff per leg seq: shares, price, time; DAS exits with no Cobalt leg; Cobalt legs with no DAS execution; held count after each leg. (3) Render the diff (the "before") into the note's reconcile unit (§6). (4) Write the adjustment through v3's ONE correction writer (C2 — never a second leg writer, L3): a correction row per mismatched leg, a new exit leg per DAS exit Cobalt never recorded, each with source `das` `[L31 — NAME PENDING]` + the `drc_imports` row id (L57); his taps and held-count statements stay as history rows (append-only). (5) Running shares after reconcile = the DAS position. (6) Re-render the unit with "adjusted to DAS: k rows (ids)". A card with no legs yet (filled before C1) gets its entry leg from DAS through the same writer. **A refusal from C2's writer (e.g. a CLOSED card moved off 0) = build FAILED naming the card — never forced.** `[F-12 — OPEN — ROUND 2, R2-1]` This sentence is the proposal's, ADOPTED by grok and REPLACED by the Fable seat, with gemini's walk on a third path; the three positions are in `## OPEN — ROUND 2` below, verbatim; nothing here is settled by the derive (L37). Until round 2 answers, D5 is not built for the CLOSED-card case and X11 runs first.
- **Open position carry (R67, F33).** A symbol whose DAS position is not 0 at file end is NOT a parse failure: the trade is `OPEN` with its held shares, its card stays FILLED (v3 expiry never touches FILLED, v3 F15), and it is stored in `drc_rows` as the day's open position. `[F-10]` Grok T11, verbatim: "Next-day pairing seeds each symbol from the prior `drc_rows` open position, keeps one `trade_id`, realizes P&L only on that day's executions, and stores `unrealized: not computed`. A carried symbol with no prior row FAILs the import naming the symbol. A file that contradicts the seed FAILs the import. A position left non-zero is an `OPEN` trade, not a parse failure. E1 still has to show whether a day-file starts flat." Gemini T11 ADOPT. The Fable seat's contiguous-chain check (a prior trading day with no import FAILs naming the day) is not taken as wording; its scenario — a day he lawfully did not import between two imported days seeds a phantom position when the export starts flat — is E1's result column (§10): a flat-start export leaves the seed with no cross-check.
- **Re-import / correction.** Dropping a new file of a kind supersedes the old row (`supersedes` FK; old bytes kept, never deleted). A superseding upload re-fires the event; the build re-runs: the note exists → `create_if_absent` skips (F7), units upsert in place by id, his text inside a unit is preserved, a Cobalt line he changed → human wins + override row (F9). A trade that disappears from the new CSV: `[F-21]` per T6 (§6) — its derived block goes, its voice unit stays with one `orphaned` line (E8 proves).

## 4. PARSERS — each behind an interface (L9), each fail-loud (L1), each against the real shape (L45)

| Input | Interface → model | Reads | Fail-loud rule |
|---|---|---|---|
| DAS export | `ExecutionSource` → `Execution` (Pydantic: time, symbol, side ∈ SPEC §1.1 side codes, price, qty, route, account) | columns by header NAME; FIFO pairing per symbol/direction, trade closes at position 0 (SPEC §2 row 1) → `Trade` + exit `Leg`s | `[F-13]` Grok (e), verbatim: "Columns are read by header name. FIFO is per symbol and direction, seeded by T11. A position through 0 in one execution FAILs the whole file until E1 shows the export books that. More than one distinct account value FAILs the file naming them until O4 is loaded; accounts are not merged. A missing required column FAILs the import (L1). An extra column, required names present, sets `degraded: das_export_shape` (L9) and still parses. Reorder alone is not a failure. The fixture's source is one real export he hands over (E1); personal attribution, dates, and accounts are stripped before any git commit; O15 is that commit. Platform path: NONE. `02-greps.txt` DAS / Lightspeed / TradeStation / CenterPoint hits are existing never-touch comments. This design reads only the file he drops." — replaces the proposal's "a header change = `degraded: das_export_shape` flag (L9)" (gemini WRONG FACTS, verbatim: "a changed CSV header breaks columnar parsing and must FAIL the import loudly, not just flag it as degraded" — consistent with grok on a missing/renamed column; silent on an added column). Flag name `das_export_shape` `[L31 — NAME PENDING]`. Mixed-date file = FAILED (F-07). Order-log rows → `stop_moves` only if E1 proves they are in the file (F32 UNPROVEN). An open position at file end = `OPEN`, carried (§3) |
| TradeZella screenshot | `TradeStatsSource` → `TradeStats` | MVP: nothing read from the image — it is bound to a trade, embedded in the note, and its typed cells (if he types them) are the figures. OCR (Apple Vision via `pyobjc-framework-Vision`, or tesseract) = a NEW dependency, a later slice, in shadow beside his typed cells (CLAUDE.md: extracted figures need their verbatim source) — **O1** | `[F-14]` Grok (g), verbatim: "This slice reads nothing from the image. The drop zone binds it. The note embeds `![[_imports/drc/<date>/<file>]]`. MAE, MFE, planned R, and the C12 flag read typed cells only. Blank stays `not given`. B26, B25, and C12 do not read pixels. No OCR library and no new package (`02-greps.txt`: pillow at `:2789` and `:3525`, already transitive; no `pyobjc`, no `pytesseract`). This slice does not import pillow. A file that is zero bytes or not a PNG/JPEG header FAILs that file. Whether Obsidian renders the embed is E9, not a build failure." A trade without a screenshot keeps the day `waiting` |
| ASET cards | existing `AsetStore.for_date` (F18) — the DB, not the note fences (F19; L3) | nearest prior card, same ticker+direction, within `limits.card_match_window_minutes` (SPEC §2 key; the value is his) | DB unreachable = build FAILED; a trade with no card = `card: none` shown (a rule-1 fact, not an error) |
| Daily-note premarket block | new `PremarketSource` reading keyed lines only (F21) | `Sleep:`, `Readiness:`, `RHR:`, `1% goal:` | a missing key renders `not given`; never inferred (SPEC §9); keys that do not exist (meditated, tone, hotkey, sleep hours) → **O11** |
| Rules | existing `regenerate_rules_config()` (F20) | numbered rules + tags | as today (loud, line-numbered). Dated temporary rules need new syntax → later slice |
| v3 legs (after S3 C1/C2) | `legs_current_v` per v3 (F25) | Cobalt's estimated/confirmed legs + his held-count statements | absent (not built yet) = reconcile section says `legs: not built`, no write |
| Carried positions | `drc_rows` of the prior DRC day | open trades with held shares (R67) | a carried position with no prior row = FAILED naming the symbol (never assumed flat); a file that contradicts the seed = FAILED (`[F-10]`) |

**Seam with v3 C1/C2 (R67) — S-C1, S-C2, S-C2b, stated as SEAMS (what this design needs from S3's C1 / C2 and when); the S3 exits FINAL (v3 + R67) is not rewritten here.** `[F-11]` Grok T7, verbatim: "S-C1 amends S3 C1's set (`25-s3-legs.excerpt.md` migrations paragraph, v3 :139), not the DRC migration. `source` and `price_source` each gain the value `das`. A nullable `source_id` names the `drc_imports` row. The desk numbers the file at the L68 gate. S-C2 / S-C2b amend C2's one writer: append-only correction or new exit leg, his taps kept, running shares may stay above 0 across days, CLOSED check only when the DAS position is 0. D5 calls that writer and no other. Until C2 is merged the reconcile unit's only completion text is `adjustment pending (legs writer not built)`." Gemini T7 ADOPT (its "S-C1 rides C1's migration (`db_migrations/0014_legs.sql`)" DOES NOT HOLD — hub — and is not carried; the number is the desk's at the L68 gate). The value `das` `[L31 — NAME PENDING]`. What S3 must carry: grok (f) and gemini (f) both say S-C1 / S-C2 CHANGE the S3 FINAL's C1 DDL (a new CHECK value on `source` and `price_source`, a nullable source-id column) and C2's writer semantics (a DAS-sourced correction and a DAS-sourced new exit leg under the same lock and refusals; a held-count statement is a history row the DAS correction supersedes, never deletes; running shares may stay > 0 across days; the CLOSED check runs only on 0) — carried in the report's ESCALATE for the desk, whose S3 build prompts are drafted from v3 + R67. D5 (§9) is gated on C2 merged. `ASTRA PENDING (R13)`: whether S-C1's column belongs on C1's DDL or on a later alter is a question only the S3 reviewer would have answered; the seats that ruled place it in C1's set (grok, Fable) — gemini's placement text does not hold.

**Clock seam.** Rule 1 compares a card's `created_at` (Mac clock) with the first DAS fill time (trading-PC clock, timezone per export). Seconds matter (`card_lead_seconds`, SPEC §2). Skew and timezone are UNPROVEN → E4.

## 5. DERIVED FIELDS AND THE RULE ENGINE

**SPEC §2, mapped.**

| SPEC §2 key(s) | Exists today | This slice | Later |
|---|---|---|---|
| trade_id, ticker, side, entry/exit time, hold, avg entry/exit, shares, gross/net P&L, commissions, legs | — | NEW from DAS (confirmed data, his export); gross vs net on the summary line = **O21** | — |
| card_id, grade_at_card, planned stop/entry, risk budget, planned shares | `aset_sizings` (F18) | card match | — |
| card_first, card_lead_seconds, card_regenerations | card timestamps exist | NEW (after E4) | — |
| planned/actual risk, risk_overrun_pct | fill recompute on the card (`aset/store.py:185-257`) | NEW over DAS fills | — |
| planned_R, realized_R | v3 `realized_r.1` over legs (F25) | realized R = the SAME pure function fed DAS legs (one formula, L3); planned R needs a target → typed cell or `untested` | — |
| MAE, MFE, exit_efficiency, best_exit_gap_R | — | typed cells only (O1) | OCR |
| window | — | NEW from `windows.*` (SPEC §7 keys) | — |
| seat_state_at_entry, thesis_key, attempt_number, consecutive_losses_before | — | NEW (pure, from the day's DAS trades) | correlated flag needs the sector map |
| stop_moves, discretion_delta_R | v3 `card_stop_edits` (Cobalt-side only) | — | DAS order log (if E1) |
| known_family, written_exit_exists | — | — | setup-sheet reads |

Every derived value is stored with its inputs and the function id that computed it (L57) in `"user".drc_rows` (declared, F23): one row per trade, one per day, JSON `inputs` + `derived` + `fn_version`. No number is re-derived in the note that is not in the row. `[F-19]` Grok (i), verbatim: "Every rate in §7 and in C1–C13 renders with its stored n, or the literal `insufficient data` when n<30 (`replay/line.py:59`). A per-trade figure is not an average. `drc_rows.inputs` snapshots, at build time, every card field the figure used (`id`, `grade`, `direction`, `entry`, `stop`, `shares`, `risk_budget`, `state`, `actual_fill`, `recomputed_shares`, `created_at`, `distance_change_pct` — the `for_date` columns in `23-aset.excerpt.py`) plus the typed cells plus the window bounds used. A later edit to `aset_sizings` does not change the stored figure. Realized R stores `fn_version = realized_r.1` and the post-reconcile current legs. Before D5 it is `not computed`, never `confirmed`. The miss line's inputs live on the replay run (T3), not in `drc_rows`. C1, C3, C6, and C11 render `not given` until the slice that stores their inputs; they are not computed from a live re-read. C13 fills a blank `1% goal:` only through the existing daily-note writer, clause 2a, and not in D3; the DRC build does not upsert the daily note." (FC13 HOLDS: `mark_filled` UPDATEs the card row after a fill — the snapshot is what keeps a replay honest. The timing of C1/C3/C6/C11/C13 here is HOW and WHEN they are built, not a change to R72's acceptance of C1–C13 — report ESCALATE.)

**Rule engine.** Rules are data: rule text and numbers from `Rules.md` (F20); thresholds from his config (SPEC §7 keys `account.*`, `grades.*` (→ the existing `aset.sheet_modes` row, `[F-18]` below), `windows.*`, `limits.*`, `goal.*`, `sleep_trigger.*`) — his config, home `"user".trader_settings` (`settings/models.py:107`) loaded by his own `cobalt settings load --apply` (L28 as amended 09-15), Pydantic schema + dry-run (L10); never a committed value (L32, L53). Checkers are code keyed by a stable checker id; the binding `rule number → checker id` is his config (renumbering a rule is his edit, not ours) — **O7**. Result per trade/day: `pass | fail | untested | needs_voice` (SPEC §3). `[F-17]` Grok (h), verbatim: "Rules text comes from `regenerate_rules_config()` (`02-greps.txt`). Thresholds come from the loaded settings keys. A checker contains no numeric literal and no rule sentence. The only binding is his `rule number → checker id` map (O7). A number with no map entry renders `untested` and is not bound to another checker. A map entry whose checker id is missing FAILs the build naming the id. No path ticks his checkboxes or feeds `daymode/drc.py`, a grade, or a size (T5, §12)."

`[F-16 — Fable seat only; FC7, FC8 HOLD; X12 gates D4; report ESCALATE 5 lists it as a single-seat fold the desk may strip]` Fable seat T5, verbatim: "The SPEC §7 key families (`account.*`, `grades.*`→see T9, `windows.*`, `limits.*`, `goal.*`, `sleep_trigger.*`) and the `rule → checker` binding register in `OPTIONAL_SETTING_KEYS` (`settings/models.py:44-56`), never in `SETTING_KEYS`: a missing DRC key fails the DRC build naming the key and touches no sheet reader. Each binding row carries `rule_id`, `checker_id` and `text_sha256` of the rule text at binding time (the dry-run prints them for him). At build, `regenerate_rules_config()` re-parses `Rules.md`; a bound rule whose current text hash differs renders `untested: rule <n> text changed since binding <date>` — never re-bound silently."

**One source for the daily stop and grade dollars (T9 — the mechanism; O5 the value, O6 the home, both his).** `[F-18]` Gemini T9, verbatim: "his configuration in trader_settings is the single source of truth; the committed daily template and ASET sheet-mode config must read from it, removing hardcoded values." Facts the derive read for this fold: grade dollars ALREADY live in `"user".trader_settings` as the `aset.sheet_modes` row — `SheetModesConfig.dollars_for` (`aset/config.py:222-237`), loaded from the DB by `load_sheet_modes_config()` (`:251-269`, `TraderSettings.from_db().sheet_modes`), keys `SHEET_KEYS` (`settings/models.py:30`); `daily_stop` exists in code today only as a comment (`aset/config.py:9`). So: the daily stop is a NEW key family (SPEC §7 `account.*`) in `trader_settings`; the grade dollars are the EXISTING `aset.sheet_modes` row — a new `grades.*` family would be a second copy of one number (L3), so grok's T9 text naming `grades.*` keys is not taken (report fold table). One function is the only reader of both on the DRC path; an absent key renders `not given`; no fallback; nothing reads `daily.md.j2`, a Rules.md sentence or a second config. The committed literal at `daily.md.j2:18` (F22) leaves git by reading that key (gemini's wording places this INSIDE the design; grok places its removal OUTSIDE D1–D5 as "his or the desk's on his word" — a split on SCOPE, carried in the report's ESCALATE; which chunk edits `daily.md.j2` / `prefill/daily.py` is unassigned by the rulings — the desk's at L68).

**L7.** `[F-15]` Grok T5, verbatim: "The engine writes only `drc-rules/rule_engine`, labelled shadow. `drc-rules/rules_check` stays the checkbox scaffold `format_rules_checkbox_block` already writes. No checker writes a `[x]`. No result is read by `daymode/drc.py`, a grade, a size, or `daymode/propose.py`. Checkboxes retire only on his later HITL. N and agreement stay O13; a rate renders with n, and n<30 is `insufficient data` (`replay/line.py:59`)." Gemini T5 ADOPT. Agreement per rule shown with its n (L8); his checkboxes retire only on his HITL approval (L61 interim). N and "agreement" = **O13**.

## 6. THE NOTE

**Template.** His `5 - Templates/DRC.md` is THE template (R65 d). `[F-20]` Grok T8, verbatim: "The build reads `5 - Templates/DRC.md` and replaces only the literal token `{{date:YYYY-MM-DD}}`. Any other `{{` FAILs the build naming the line. No Jinja and no Templater on his file. `configs/cobalt/templates/drc.md.j2` is deleted in D3. The only caller today is `prefill/drc.py:272` (`02-greps.txt`). The build uses `load_prefill_paths()` for the note path, the same values as `drc_note_path`, not a hard-coded string. It always upserts units after `create_if_absent`. It does not take the early return at `prefill/drc.py` (excerpt: return when `action == "created"`)." Gemini T8 ADOPT; Fable seat ADOPT (two `{{` lines in his template, both the date token). His example trade blocks and example If/Then (F5) would copy into every DRC → **O8** (keep, or the desk removes them on his word, L65). The `this.file.cday` dataview on a note created the morning after = X10 (display; his template's item if it fails).

**Sections mapped onto `DRC-2026-09-21.md`'s headings.**

| Heading (his) | Writer | Unit |
|---|---|---|
| title, date, `### Tickers:` dataview, `### Theme:` embed | HIS (template) | — |
| NEW: directly under the date line | COBALT | `drc-summary/summary` — rules line FIRST (engine result, shadow-labelled, SPEC §5.1 item 1), net P&L from DAS (gross vs net = O21), W/L, trades, cards written / taken, screenshots bound |
| `### My GOAL for today:` + `Grade:` | HIS — never touched | — |
| `### How I managed risk:` · `### PnL on the day:` | HIS text; the heading's placeholder is his (**O16**) | `drc-risk/risk_parameters` (existing, kept) |
| `### Catalyst + Set Up + Trades` | COBALT scaffold + HIS answers | `[F-21]` `drc-trades/tickers` REBUILT: one block per DAS trade — derived table, matched card, legs, flags (SPEC §5.1 item 3 keys), screenshot embed `![[_imports/drc/<date>/<file>]]`; his answers per R71's HIS rows live in voice units. Grok T6, verbatim: "A rebuild never drops text he typed. Derived figures live in Cobalt units the build upserts. His answers live in units the build creates blank once (`create_if_absent` of that unit's body) and does not upsert again. O9 picks whether those voice units sit inside the trade section or beside it; the id is `drc-trades/voice-<trade_id>` either way. A trade missing from a later CSV leaves its voice unit in place with one Cobalt line `orphaned` above it. The proposal's "merge lists it as orphaned" is not in `VaultWriter.upsert_unit` as staged (`22-vaultwrite.excerpt.py`). E8 is the proof of this wording, not of an unshown merge." (The create-once call is `upsert_unit(..., skip_if=…)` — `writer.py:652`, `:680-682`, read by the derive; `create_if_absent` at `:559` is note-level. Gemini T6's "orphaned text from removed trades is appended at the bottom of the unit" has no code behind it — `grep -ri orphan src/cobalt/vaultwrite/` → 0 — and is not taken; the Fable seat's per-trade-unit wording is not taken, report fold table.) Inside-unit vs his own block = **O9** |
| same | COBALT | `drc-trades/reconcile` — per trade: DAS legs vs Cobalt legs (v3) — the mismatches as found (before), then "adjusted to DAS: k rows (ids)" (R67, §3); his taps / held-count statements listed as history; before C2: `adjustment pending (legs writer not built)` `[F-11]` |
| same | COBALT | `drc-trades/open_positions` — every position open at the DRC (R67): symbol, side, held shares, trade_id, day opened; on the next day's DRC the same trade appears as `continuing open position` until closed |
| `### Technology:` · `### Collaboration:` · both `### What I learned` · `### Selectivity notes:` · `### Tomorrow's one percent better:` | HIS — never touched | — |
| `### Cobalt Rules Check` (heading exists only in the repo copy — his template lacks it, F4) | COBALT | `drc-rules/rules_check` (existing: his checkboxes + card reconcile, now "cards with no DAS trade") · NEW `drc-rules/rule_engine` (shadow, §5 `[F-15]`) |
| after `drc-rules` | COBALT (replay) | `drc-misses/miss_line` (existing, §8 `[F-24]`) |

`[F-23 — Fable seat only; hub Checked row `daymode/drc.py:116-132` HOLDS; report ESCALATE 5]` Fable seat (b), verbatim: "A PRE draft (A5, A7, A16, A18, A24) never writes a line the 09:00 reader's regexes match (`daymode/drc.py:112-113`: a line beginning `Grade:` or `Goal:`); the reader stays unchanged." (A constraint on HOW the PRE writers of §13 write; their ACTION and WRITER are as ruled, R69.)

**Voice / narrative (SPEC §1.2).** Today he gives it in the DRC chat. MVP: no voice pipeline — he types into the blank lines in the note (as today) or dictates in the DRC chat. Later slice: the local voice stack captures per-trade and per-day blocks, stored verbatim beside parsed fields (SPEC §9), corrections to any auto value logged as overrides with reason. Channel and timing = **O10**.

## 7. LEDGERS (SPEC §4)

| Ledger | This slice | Later |
|---|---|---|
| Streak | the day row in `drc_rows` (DRC built or not, by date) | render |
| Rule breaks per day/rule | engine rows (shadow), core vs new split needs rule age → later | ✓ |
| Sniper tally, grade-accuracy, discretion audit, exit efficiency, rule-10 ledger, prevented column, playbook instances, goal progress | — | each a slice; every rate with its n, n<30 = `insufficient data` (L8) |
| `trades.jsonl` / `days.jsonl` (SPEC §5.2) | NOT built — Postgres is the store; a file export is **O17** | — |
| Weekly pack (SPEC §5.3) | — | ✓ |

## 8. WHAT DIES, WHAT IS REUSED

- **15:40 job** (`com.cobalt.prefill-drc`, F1): `[F-22]` Grok (b), verbatim: "Kill `com.cobalt.prefill-drc` in the same deploy as the first input-driven build: plist, `jobs.yaml:181-186`, the `:310` reader label, and the `prefill drc` CLI entry (`prefill/cli.py:49`). Keep the 09:00 reader unchanged (`daymode/drc.py`). Change the 21:10 step as T3. After that, one creator of the note (the build function), one template (his), one miss-line function (`write_miss_line`). Page and CLI are callers, not second implementations. No unit has two writers." (Gemini (b): after the change, NO second DRC writer or second template is left — 3 of 3. The CLI entry: `prefill/cli.py:46-49`, `:67-69`, `:79`, `pyproject.toml:64` — F39; `ops/README.md:30`, `:145`, `:157` — F40.) `launchctl bootout`, plist removed from `ops/`, S2 smoke rows per `[F-25]`. RESTARTS: that job (removed) + `com.cobalt.aset` (L42).
- **Its three units** are REUSED by the build: `format_risk_parameters` (`prefill/drc.py:248`), `format_rules_checkbox_block` (defined in `prefill/daily.py:165`, imported at `drc.py:54`), `format_card_reconcile_block` (`prefill/drc.py:230`), the DRC's own `format_rules_check_block` (`:285`), the placements (`:321-347`) `[F-26]` (FC2 HOLDS — the proposal's single range `:248-347` did not hold as a clean range). `format_tickers_block` (`:220`) is replaced (cards → DAS trades; T6). `parse_fill_updates` (`:70`) and `find_trade_note_for_card` (`:101`) leave the DRC path (DB fill columns; v3 `trade_note_path` seam). `run_drc_prefill` becomes `run_drc_build(event)`; `uv run prefill drc` is retired for `cobalt drc build` (one CLI).
- **21:10 miss line** (F10, F11): unchanged when the note exists. When it does not, today's run FAILS every such night — wrong under R66. `[F-24]` Grok T3, verbatim: "If the note is absent at 21:10, `line_step` does not raise and does not create a note. It stores the exact arguments `render_line` is called with today (`21-drc-writers.excerpt.py` runner excerpt: `card_rows`, `mover_rows`, `settings`, `formation_replay`, `input_stale`, `formation_rows`, `formation_suppressed`, `formation_input_stale`) on the replay run, records `line: pending (no DRC)`, and ends green. The build, after the `drc-rules` unit exists, is the only later caller of `render_line` plus `write_miss_line` for that stored blob. If the blob is missing, the unit is not written and the note says `miss line: pending (replay inputs not stored)`. When the note already exists at 21:10, `line_step` writes as today and the build does not write a second line." (Gemini T3's "re-runs the line_step logic using the stored replay_run_id" names no stored inputs — a re-run reads live settings (L57) — not taken; the Fable seat's stored-body wording not taken, report fold table.) E6 = grok's CHANGE (§10). One writer identity for `drc-misses/miss_line` whatever process writes it.
- **09:00 reader** (F12): unchanged. A day he did not import reads `no prior DRC` (loud, steps down) — consistent with R66. What it quotes stays packet Q10/Q11 (his).
- **Repo template** `drc.md.j2`: dies (§6). **S2 smoke:** `[F-25]` Grok T10, verbatim: "In the same deploy as the prefill retirement, `s2.yaml:436-438` (label `com.cobalt.prefill-drc`) is removed, not pointed at a dead job. `s2.yaml:383-398` passes as `pending` when no DRC event exists for that date, fails when the event is `failed` or `done` without the note and the `drc-misses` markers, and passes when `done` and both markers are present. It does not require the note at 15:41. Charter and S3-smoke wording stay O18." Gemini T10 ADOPT. Whether that conditional needs a new check kind is settled at build: `s2.yaml` carries eight kinds today (`launchctl`, `sql`, `http`, `job_row`, `compare`, `vault_unit`, `log_grep`, `cli` — read by the derive; the Fable seat's "three kinds" list DOES NOT HOLD as exhaustive), none conditional on a DB row.
- **Charter F14 / S3 smoke "exists at 15:41"** (F28): re-worded to "exists within the build timeout of the second input placed" — his ruling (**O18**); smoke rows in D3 do not wait on it (grok).

## 9. CHUNKS

This week (R60) — the MVP only. Seats: Opus 5 floor on every write-path chunk, never auto mode on a write path (L29); every build checked by ≥3 tribunal members (L67: Sol, Opus 5, Grok, Gemini for code checks — R46). Hours are seat hours; a fix round ≈ 0.4× on each (v3's own pattern — UNVERIFIED, grok/Fable: no measurement in the packet).

| Chunk | Files | Write path | Migration | RESTARTS | h |
|---|---|---|---|---|---|
| D1 DAS parser + pairing (incl. carried open positions, R67) + models + stores | new `src/cobalt/drc/{das.py [L31 — NAME PENDING],models.py,store.py}`; real-shape fixture from E1 (redacted, L45/L32; the git commit of it = O15); one `db_migrations` file + `.rollback.sql`: `[F-35]` Grok T4, verbatim: "D1 creates `"user".drc_imports` and `"user".drc_fills` and uses the already-declared `"user".drc_rows` (`placement.py:111`). It does not create `"user".fills` (`placement.py:110`, declared, not built). It does not create or alter `order_fills` (`placement.py:143`)." (Gemini T4: `"user".drc_fills` — 2 houses; the Fable seat's `drc_executions` not taken.) `drc_imports` carries the event state (§3); `placement.py` entries | DB — yes | YES — one set; number the desk's at L68 (≥ the stacked branches'; main ends `0011`, `0012` bars, `0013` setups — text claims of `0014`/`0015` are not files) | none (library) | 7 |
| D2 Import page + trigger | `aset/web.py` (`GET /drc`, `POST /drc/import`), `src/cobalt/drc/imports.py`, the new `VaultWriter` bytes method (`[F-06]`), `pyproject.toml` (direct `python-multipart`) | vault `_imports/` + DB — yes | — | `com.cobalt.aset` | 7 |
| D3 Build + note | `src/cobalt/drc/build.py` (his-template reader, create-if-absent, units §6, derived + engine rows §5), `prefill/drc.py` (reused helpers; create step removed), `replay/runner.py` line-step pending + build hook, `cli.py` (`cobalt drc build`), `prefill/cli.py` (drop the `drc` subcommand, F39), `ops/README.md` (F40), `drc.md.j2` deleted, `ops/com.cobalt.prefill-drc.plist` retired, `jobs.yaml` (rows `:181-186`, the `:310` reader label; `[F-28]` and the `configs/cobalt/rules.yaml` entry at `:309-316` leaves `no_resident_reads` for `com.cobalt.aset`'s `reads:` — the build calls `regenerate_rules_config()` inside the resident, F41), `smoke/s2.yaml` (`[F-25]`; `smoke/checks.py` only if the conditional needs a kind) | vault + DB — yes | — | `com.cobalt.prefill-drc` (retired), `com.cobalt.replay`, `com.cobalt.aset` — derived by `cobalt jobs restarts` at build (L42) | 9 |
| D4 DRC config family | `settings/models.py` keys + Pydantic schema for SPEC §7 key families (in `OPTIONAL_SETTING_KEYS`, `[F-16]`), checker binding, dry-run (`cobalt drc build --dry-run`); the `daily.md.j2:18` variable and `prefill/daily.py` IF the desk places the template read here (`[F-18]`, unassigned) | DB (settings loader) — yes | none if `trader_settings.value` takes a new payload without a migration — E10 (grok) proves; fail → the migration the desk numbers | readers of `trader_settings` per `cobalt jobs restarts` (L42); if `com.cobalt.radar` is among them the restart lands inside the 20:00–21:00 pause (L43/L66) | 4 |
| D5 Reconcile writes to legs (R67) | `src/cobalt/drc/reconcile.py` calling C2's correction writer; reconcile + open-position units; the CLOSED-card path waits on `## OPEN — ROUND 2` R2-1 and X11 | DB (`legs`) + vault — yes | none of its own (S-C1 rides C1's set) | `com.cobalt.aset` | 5 |

This slice: 5 chunks, 32 h seats (7+7+9+4+5 — arithmetic re-derived by grok, gemini and the Fable seat; ≈ 45 h with one fix round each — the 0.4× factor UNVERIFIED). `[F-27]` Grok (j), verbatim: "D1–D5 are write paths. Builder seat is Opus 5, never auto mode (L29). D1 is blocked on E1. D5 is after S3 C2 and adds no migration (S-C1 rides C1). The DRC migration is one `db_migrations` file plus `.rollback.sql`, listed in FORWARD and REVERSE. It takes no number in this design. Main ends at 0011 (`24-migrations.excerpt.py`). Unmerged files exist for 0012 and 0013 only (`02-greps.txt` log). 0014 / 0015 are text claims, not files. The desk numbers the file at the L68 gate. RESTARTS: D2 and D5 `com.cobalt.aset`; D3 that job plus `com.cobalt.replay` plus bootout of `com.cobalt.prefill-drc`; D4 whatever `jobs restarts` names for `trader_settings`. D4 and D2 share no file. `cli.py` is D3's; D4 must not edit it. D1 restarts nothing. Hours: 7+7+9+4+5 = 32, as the sum of the proposal's own estimates. Those estimates and the 0.4× factor behind ≈45 are UNVERIFIED — no measurement in the packet. The arithmetic is not a re-derived schedule. Hold the chunk cut; do not invent a new total." Order: E1–E4 → D1 → D4 ∥ D2 → D3; D5 after v3 C2 is merged (S3 window, 09-24 →). D1 cannot start before E1 (L45: the real shape exists; no invented fixture) — E1's export is an INPUT the chunk waits on, not an owner value. D2 and D3 land in ONE deploy (grok (b): the retirement in the same deploy as the first input-driven build; L43: the one deploy carries everything ready). The deploy order and the migration number are the desk's lane (L68), stated here as facts, not rulings. Until D5 lands, the reconcile unit shows the diff only and says `adjustment pending (legs writer not built)`.

Later slices (named, not proposed as this week's build): **L1** TradeZella OCR (new dependency, shadow vs typed) · **L2** DAS order log → stop_moves + discretion audit (if E1) · **L3** voice capture per trade / per day · **L4** ledgers + weekly pack (SPEC §4, §5.3) · **L5** premarket keys + sleep trigger + opening window + dated temporary rules (Rules.md syntax) · **L6** sector/correlation map (rule 10 correlated half) · **L7** daily-note 3-line stub + wider 09:00 quote (packet Q10, Q12) · **L8** setup-sheet reads (rule 11 written exit, known family).

## 10. First-gate experiments (L70) — every "not checkable from reads", each before the chunk it gates

Each runs on `cobalt_dev`, in the dev vault, or on the one real export / screenshot he hands over; a production read stays a hub-run read (L41). `[F-30]`

| # | Before | What | Pass / result that changes the design | named by |
|---|---|---|---|---|
| E1 | D1 | one real DAS export from him (any recent day): header set, side codes, time format + zone, account column(s), whether order-log rows share the file, whether a day-file starts flat or lists overnight positions, whether one execution can cross 0 (reversal) | columns named → fixture cut (values stripped, L45 companion); order log absent → L2 needs a second export or dies; overnight rows listed → the file is the seed and the carried `drc_rows` row the cross-check; a FLAT start → the seed has no cross-check, so a day he lawfully did not import between two imported days seeds a phantom position (Fable seat T11 scenario) — the seed rule then needs a contiguity check (round 2 or his); a through-0 execution booked by the export → §4's FAIL becomes a lawful reversal | proposal · grok · gemini · Fable (X1) |
| E2 | D2 | one real TradeZella screenshot: which SPEC §1.1 row 2 fields are legible on the stats panel | decides O1's typed-cell list; OCR accuracy only if O1 = OCR | all |
| E3 | D3 | on the dev vault: his template with only the date token substituted, then the existing placements plus summary, reconcile, open-position, and voice units | every unit under its heading, no other `{{` token, his non-unit lines byte-identical; fail → change the placement, not the template, until O8 | proposal · grok (widened) · gemini · Fable (X3) |
| E4 | D1 | one card created in ASET vs the same trade's first DAS fill time | skew + zone known → `card_lead_seconds` trustworthy; unknown → `card_lead_seconds` stays `not computed` and rule 1 does not compare the clocks (a tolerance key is his) | all |
| E5 | D2 | multipart `UploadFile` in the dev ASET app with the transitive `python-multipart` | works → pin direct; fails → the dependency is added knowingly, still not a new package | all |
| E6 (CHANGED) | D3 | grok, verbatim: "After a 21:10 run with no note, re-render `render_line` from the stored argument blob only. Same bytes as that run's in-memory render: T3 holds. Any other byte: the build must not write `miss_line`." | fail → T3's stored-arguments mechanism does not hold as written (the alternatives on record: gemini's re-run by run id; the Fable seat's stored body X6) | grok |
| E7 | D2, ops | which aset config production loads (`aset.local.yaml` bind `lan`?), and a >1 MB PNG POSTed from the trading PC over the LAN | reachable; else the page is used from the Mac and the CSV travels by his own means — not a precondition either way | all |
| E8 (CHANGED) | D3 | grok, verbatim: "Two builds, a trade removed, text in `drc-trades/voice-<trade_id>`. Pass: the voice unit still there with `orphaned`, derived block gone. Fail: D3 does not ship the vanished-trade path." | as stated; also proves gemini's untaken claim (the merge alone keeps removed-block text) false or true | grok · (gemini, Fable X8 on other shapes) |
| E9 | D3 (display) | grok, verbatim: "On the dev vault, a PNG under `_imports/drc/<date>/` and the wikilink in a note. Pass: Obsidian embeds it. Fail: the file row is still the record; the embed is not a gate. Does not change D1–D5." | display only | grok · Fable (X9) |
| E10 | D4 | grok, verbatim: "Insert one new `trader_settings` key on `cobalt_dev` with no migration. Pass: D4 stays migration-free. Fail: D4 gains the migration the desk numbers." | as stated (the proposal's "read at build") | grok |
| E11 | D2 / D3 | grok, verbatim: "Kill the ASET request after `pending` and before `done`. Pass: the row is `failed` and the page is not `DRC built`. Fail: the request path is wrong and T2's wording is not met." | as stated | grok |
| X10 | none (owner item if it fails) | Fable seat, verbatim: "a DRC created on D+1 morning for date D — `this.file.cday` dataview (`drc.md.j2:9-14`, his template :9) lists D+1's trade notes, not D's; if so, the tickers dataview line is an OWNER item (his template), not a build defect." | display | Fable |
| X11 | D5 (R2-1) | Fable seat, verbatim: "cobalt_dev after S3 C2 — a CLOSED card whose export exits total less than Cobalt's → C2 refuses; the build writes the unresolved-mismatch row, renders it, and the DRC is built (T7 (i))." — run under whichever position round 2 adopts; its result is round 2's evidence, not a fold | decides which R2-1 position is livable | Fable |
| X12 | D4 | Fable seat, verbatim: "cobalt_dev without the DRC key rows → `/size` still serves and sizes; `cobalt drc build` fails naming the missing key (T5's `OPTIONAL_SETTING_KEYS` placement)." | fail → `[F-16]` does not hold; the keys' placement returns to the desk | Fable |
| X13 | D2 | build twice on the dev vault with a superseding export that changes or drops a trade's key after a screenshot was bound to it: is the binding listed as orphaned, silently re-bound, or deleted (the Fable seat's (c) sentence, not folded — no house wording covers it) | the page's binding rule for a superseding export is written from the result, never from this text | Fable ((c), as an experiment) |

UNPROVEN rows of the proposal's report as v2 leaves them: the DAS columns/side codes/zone/account → E1; the order log → E1; flat start → E1 (result widened above); TradeZella legibility → E2; placements on his template → E3; clock skew → E4; multipart → E5; `render_line` inputs persisted → E6 (grok's shape); production aset config + LAN → E7; removed-block text → E8 (grok's shape); `Sleep:` → O11; `trader_settings` new keys without a migration → E10; `0014`/`0015` → the desk at L68.

## 11. OWNER ITEMS (his, one per message, AFTER the tribunal; none a precondition to build)

O1 TradeZella figures: typed cells (MVP) vs OCR (new dependency) · O2 file home: vault `_imports/drc/<date>/` (every screenshot then syncs to every device — Fable) vs a non-synced data directory · O3 a no-trade day: what counts as "both placed" (SPEC §9 streak vs R66 "no inputs, no DRC") — default until he rules: no DRC (all seats; author's ESCALATE 2) · O4 which DAS accounts count (until then a multi-account file FAILs, `[F-13]`) · O5 the daily stop and grade dollar VALUES (the one-source mechanism is `[F-18]`; author's ESCALATE 1) · O6 config home: `trader_settings` (default until he rules — grok) vs a keyed unit in his note · O7 rule number → checker binding (the map is the copy he edits; `[F-16]` adds the text hash) · O8 his template's example blocks (keep, or the desk removes on his word, L65) · O9 his per-trade answers inside Cobalt's trade section or beside it (`drc-trades/voice-<trade_id>` either way, `[F-21]`) · O10 voice channel and timing · O11 premarket keys (meditated, tone, hotkey, sleep hours vs the `Sleep:` value) · O12 the packet questions still his: Q2 (confirm "none" from R65 b), Q4, Q5, Q6, Q7, Q8, Q10, Q11, Q12, Q13, Q14 · O13 rule-engine shadow length N + agreement definition · O14 inputs dropped inside `market_reset` (the L1-clean default ruled by the seats: refuse with the reason; his if he wants otherwise) · O15 consent to a redacted real-shape DAS fixture in git — grok, verbatim: "Mis-framed as a free option: L45 requires the committed shape before production. He can hand E1 over without this; the git commit cannot." (a precondition to PRODUCTION, not to E1/D1 on `cobalt_dev` — report ESCALATE) · O16 the PnL heading placeholder: his, or Cobalt fills it (the DAS net figure's unit is separate — grok) · O17 `trades.jsonl` / `days.jsonl` files wanted beside Postgres · O18 Charter F14 / S3-smoke "15:41" wording (`MVP-CHARTER-v0_2.md:171`; ladder `:612`, `:616`; author's ESCALATE 3; smoke rows in D3 do not wait on it) · O19 SPENT (R69–R72) · **O20 (Fable seat, new)** how a CLOSED card the export shows still open is reopened — a CLOSED→FILLED edge does not exist in v3's state machine (v3 :149, N — the mechanism behind `## OPEN — ROUND 2`); adding one is a trading-logic change (L7 + HITL) · **O21 (Fable seat, new)** the summary line's P&L: gross or net (SPEC §2 carries both; the proposal writes net) · **O22 (grok, new — "MISSING")** grok, verbatim: "SPEC §7 `limits` has no import-size key. Not proposed here. Unbounded upload stays unproven (E5 does not settle size)." `[F-31]`

Desk items (not his): the record correction at `cto-2026-09-22.md:197` (F4; author's ESCALATE 6); the lane load — 32 h (≈45) beside v3 C1–C4 inside 09-24 → 10-07 (author's ESCALATE 4; L68, L72); the migration number; the L31 names; the S-C1/S-C2 text for the S3 C1/C2 build prompts.

Packet §4 against the spec + rulings: answered — Q1 (R65 a/d), Q3 (R65 d), Q9 (SPEC §2: P&L from fills → DAS, confirmed), Q15 (R65 c). Partly — Q2 (R65 b reading), Q7 (SPEC §1.2 per-day session scores), Q8 (SPEC §4). His — the rest (O12).

## 12. L52 and the bar — re-answered for v2 `[F-32]`

Nothing this slice builds feeds a score, a rank, an admission, a grade or a size: no chunk touches `cards/scoring.py`, `cards/radar.py`, `radar/evaluate.py`, `aset/engine.py` or `daymode/propose.py` (hub: HOLDS on a negative grep; a full proof needs the built diff — a checker item at each chunk's L67 check). The DRC reads cards and writes a review note, review rows and (D5, R67) leg corrections through v3's own writer — records the card DISPLAYS (realized R, running shares), never an input to its score. **Reaches scoring: no.** Named boundary for later: SPEC §3's sleep trigger (key `sleep_trigger.effect`) and any use of rule results or grade-accuracy in the 09:00 proposal WOULD gate sizing — each is a trading-logic change (L7 shadow + HITL) and, reaching the card, an L52 tribunal first. L52 (a)–(d) therefore do not bind this slice; L67's tribunal path does, and this v2 is its derive.

The bar, stated for v2:
- **L1 fail-loud:** every parser FAILs naming file + line (`[F-13]`, `[F-14]`); a missing DRC key fails the build naming the key (`[F-16]`); a dead request leaves `failed`, never `done` (`[F-08]`); one input creates nothing and the page says what is missing (`[F-09]`); `adjustment pending (legs writer not built)` names the missing write, never `adjusted: 0 rows` (`[F-11]`); a forever-green smoke on a lawful no-DRC day is avoided by keying on the event (`[F-25]`); no plausible-empty note: the build never takes the create-then-return path (`[F-20]`).
- **L2 watcher standard:** "both placed" is deterministic code in the request, typed state on the event row, no LLM anywhere in the path, no poller (`[F-08]`, `[F-09]`).
- **L3 one path:** one web surface, one import place, one DRC creator, one template (his), one miss-line function, one leg writer (C2), one reader of the daily stop / grade dollars (`[F-18]` — no `grades.*` copy of `aset.sheet_modes`), one `MIN_N_FOR_AVERAGE`, no second scheduler, no second byte writer (`[F-06]`), the 15:40 job and its CLI entry killed in the same deploy (`[F-22]`).
- **L7 shadow-mode:** the engine renders in its own unit beside his checkboxes, labelled shadow, N and agreement his (O13); nothing ticks a box or feeds the 09:00 proposal, a grade or a size (`[F-15]`, `[F-17]`); a CLOSED→open edge would be a trading-logic change (O20, R2-1).
- **L8 sample size:** every rate with its stored n; n<30 = `insufficient data` from the one constant (`[F-19]`).
- **L9 source substitution:** every input behind a collector interface (§4); an added column raises the degraded flag and still parses (`[F-13]`); a dead source is a FAILED import, never silent.
- **L28 vault-write:** create-if-absent from HIS template, units by id, human wins + override rows, versioned; the `_imports/` bytes through the vault-write expert, versioned and sha256'd, refused in `market_reset` (`[F-06]`); voice units created once, never re-upserted (`[F-21]`); C13 only through the daily-note writer under clause 2a (`[F-19]`).
- **L32:** no value of his in this file, in any fixture (real-shape, stripped — L45 companion, O15) or in committed config (`[F-18]` removes the `daily.md.j2:18` literal by reading the key).
- **L40:** vault bytes and the note = `VaultWriter`; `drc_*` rows = one DRC store; `legs` = C2's writer; the route owns no side effect (`[F-07]`).
- **L45:** D1 waits on E1's real export; the fixture is its real shape.
- **L53:** every threshold, window, limit, N and agreement definition is his (O5, O7, O13, O22); no cap in a committed file.
- **L57 explainability:** `drc_rows.inputs` snapshots every card field, typed cell and window bound the figure used (`[F-19]`); the miss line's inputs are the stored `render_line` arguments on the replay run (`[F-24]`); a DAS correction names its source and import row (`[F-11]`).

## OPEN — ROUND 2 (R2-1) — a C2 refusal at the reconcile: a CLOSED card the export shows still open `[F-12]`

The question round 2 must answer: when C2's writer REFUSES the reconcile's correction (under v3 + R67 = N: "a shares change is refused if … it moves a CLOSED card's running off 0", v3 :149), is the CORRECT behaviour (A) the build FAILS naming the card, (B) the DRC builds with an unresolved-leg-mismatch row carried forward, or (C) the correction is written and the position carries as OPEN? Two seats disagree on whether the proposal's mechanism is correct; the derive settles nothing (L37). Each position verbatim:

> **A — grok (f), verbatim (adopts the proposal's own step-4 sentence):** "A CLOSED card whose DAS position is still open: the build FAILs naming the card. This writer does not reopen the card." — and grok (f) on the surrounding walks: "He says holding 50, DAS shows 60. His statement stays a history row. A new correction row, not an update (`refuse_row_update` in the excerpt), sets running shares to 60. The unit shows before, then the DAS row ids. A DAS exit Cobalt never recorded becomes a new exit leg through C2 only. Before C2, pending text, no insert. A Cobalt leg with no DAS execution gets a correction the current-view prefers; the old row stays. If C2 cannot express that, the build FAILs naming the card. It does not delete the leg. A card filled before C1, no legs, C2 unmerged: `legs: not built`, no write. After C2: one entry leg from DAS through that same writer."
>
> **B — the Fable seat T7 (i), verbatim:** "A refusal from C2's writer is never forced and never fails the build: it is recorded as an UNRESOLVED LEG MISMATCH (a `drc_rows` row: card id, leg ids, the export rows, the refusal text), rendered in the reconcile unit as `unresolved: card <id> — <refusal>`, and carried forward on every later DRC (A31) until resolved; how a CLOSED card the export shows still open is reopened is his (O20)." — its failing scenario for A, verbatim: "2026-09-24 10:15 he taps flat on card 812 (running 100 → 0, CLOSED) while the export later shows exits totalling 60; 16:05 he drops the file; the correction would put running at 40 on a CLOSED card → refused → `build FAILED: card 812` → no DRC-2026-09-24; re-dropping fails identically; no path in the design can ever build that day."
>
> **C — gemini (f), verbatim:** "CLOSED card DAS says open: Reconcile writes the correction, and the trade carries to the next day as OPEN (R67)." (Gemini T7: ADOPT of the proposal, whose step-4 sentence is A. Read by the derive against the FINAL: a shares correction that moves a CLOSED card's running off 0 is REFUSED under N (v3 :149, ruled by R67 as reading B) — C is buildable only with a CLOSED→open edge, which is O20, his and a trading-logic change.)
>
> What he would see, in his terms (the desk brings it; no seat and not this derive recommends): **A** — the day he flattened a card by hand and the export disagrees, the page says `DRC build FAILED: card <id>` and no DRC exists for that day until the leg question is settled (O20); **B** — the DRC is built that evening with one line `unresolved: card <id> — <refusal>` in the reconcile unit and in `open items carried forward` (A31) until it is settled; **C** — the DRC is built and the card shows as open again — which today's state machine refuses, so C waits on O20 before it can be built at all.
>
> Waits on the answer: D5's CLOSED-card path only (D1–D4 and the rest of D5 do not turn on it); X11 runs on `cobalt_dev` after S3 C2 under the adopted position.

## Dissents, verbatim

No house issued a REJECT or a `DO NOT BUILD` on any item (hub: grok 21 ADOPT/ADOPT WITH, gemini 21 ADOPT/ADOPT WITH/untagged adopt-with, the Fable seat 21 ADOPT/ADOPT WITH). The positions v2 does not follow are carried where they arose: R2-1 above (grok A vs Fable seat B vs gemini C, all verbatim); the report's fold table for every wording not taken (each named with its seat and its reason); the report's ESCALATE for the L31 names and the T9 scope split.

## TRIBUNAL — open items (T), as v2 leaves them

T1 `[F-06]` · T2 `[F-08]` · T3 `[F-24]` · T4 `[F-35]` · T5 `[F-15]`, `[F-16]` · T6 `[F-21]` · T7 `[F-11]` + R2-1 `[F-12]` · T11 `[F-10]` · T8 `[F-20]` · T9 `[F-18]` · T10 `[F-25]` · (a) `[F-02]`–`[F-05]` · (b) `[F-22]`, `[F-23]` · (c) `[F-07]` · (d) `[F-09]` · (e) `[F-13]` · (f) `[F-12]` · (g) `[F-14]` · (h) `[F-17]` · (i) `[F-19]` · (j) `[F-27]`, `[F-28]` · WRONG FACTS `[F-01]`, `[F-02]`, `[F-03]`, `[F-26]`, `[F-34]`.

## 13. DIFF MODEL — current DRC vs coach spec — AS RULED (R69 A1–A23 · R70 A24–A39 · R71 B1–B35 · R72 C1–C13); the derive changed no ACTION and no WRITER cell

Keys and line refs only (L32). `DRC:n` = `DRC-2026-09-21.md` line n; `SPEC §n` = the coach spec. ACTION = KEEP · MERGE → (target) · DROP. WRITER = **AUTO** (Cobalt populates; source named) · **PRE** (Cobalt drafts, he edits) · **HIS** (judgment only he can give). His default applied: AUTO wherever data exists; HIS only where no data can say it. No row is DROPped: every coach item survives as its own row or inside a named MERGE target. A PRE writer never writes a `Grade:`/`Goal:` line (`[F-23]`).

### (A) SECTIONS — RULED R69 (A1–A23), R70 (A24–A39)

| # | Item | Today | ACTION | WRITER · source |
|---|---|---|---|---|
| A1 | title line | DRC:5 | KEEP | AUTO · his template |
| A2 | date line | DRC:7 | KEEP | AUTO · template date token |
| A3 | `### Tickers:` dataview | DRC:9-14 | KEEP | AUTO · Obsidian dataview over trade notes (X10) |
| A4 | `### Theme:` embed (target folder empty, packet §2) | DRC:15-17 | MERGE ← SPEC §5.1-2 market tone | AUTO · embed re-pointed to the day's daily note `### Market Context:` (`daily.md.j2:51`) |
| A5 | `### My GOAL for today:` + one-goal prompt | DRC:22-24 | MERGE ← SPEC §1.1 row 4 goal, §5.1-2 goal | PRE · daily note `1% goal:` key |
| A6 | goal-format prompts (sentences / specific) | DRC:27-29 | MERGE → A5 | template prompt text, unchanged |
| A7 | progress-toward-goal summary + paragraph | DRC:32-35 | MERGE ← SPEC §4 goal progress | PRE · metric line from config `goal.metric` over the day's tags (count AUTO, tags HIS, n shown); paragraph HIS |
| A8 | `Grade:` + why prompt | DRC:38-40 | MERGE ← SPEC §1.2 per-day overall score | HIS (self-grade; the 09:00 reader's source, F12) |
| A9 | `### How I managed risk:` prompt | DRC:45-47 | KEEP | PRE · facts above his paragraph: planned vs actual risk, overrun flags, distance to daily stop, rules 2/10/12 results (DAS + cards + engine) |
| A10 | `### PnL on the day:` | DRC:49 | KEEP | AUTO · DAS net P&L (placement O16) |
| A11 | risk-parameters unit + allocation prompt | DRC:51-57 | KEEP | AUTO · sheet-mode config + cards (existing unit) |
| A12 | `### Catalyst + Set Up + Trades` | DRC:62 | KEEP | per trade → table (B) |
| A13 | cards written · trades taken | DRC:66 | KEEP | AUTO · `aset_sizings` + DAS trades |
| A14 | `### Technology:` (2 prompts) | DRC:167-169 | KEEP | HIS |
| A15 | `### Collaboration:` (3 prompts) | DRC:173-179 | KEEP | HIS |
| A16 | `### What I learned (from the trading day)` (3 prompts) | DRC:183-189 | MERGE ← SPEC §1.2 per-trade mistakes/lesson | PRE · his per-trade lesson lines gathered; paragraph HIS |
| A17 | `### What I learned (from myself)` (3 prompts) | DRC:193-199 | KEEP | HIS (SPEC §9: nothing inferred about state) |
| A18 | `### Selectivity notes:` passed-on ticker + why | DRC:203-205 | MERGE ← SPEC §1.2 passes, §4 prevented column | PRE · list AUTO: cards with no DAS trade, radar cards passed/expired (`card_transitions`), engine-detected stand-downs/cooldowns; reasons HIS |
| A19 | passed-on: mistake not repeated | DRC:206 | KEEP | HIS |
| A20 | `### Tomorrow's one percent better:` If / Then / Because | DRC:211-223 | MERGE ← SPEC §1.2 tomorrow's goal / IF-THEN | HIS |
| A21 | `### Cobalt Rules Check` checkboxes | DRC:228-244 | KEEP | AUTO · Rules.md (existing) + engine shadow unit (§5 `[F-15]`: the scaffold plus the `drc-rules/rule_engine` unit, never a tick) |
| A22 | card reconcile (taken / passed / discarded) | DRC:246-249 | KEEP | PRE · "taken" AUTO from DAS match; passed vs discarded HIS |
| A23 | miss line | unit `drc-misses` | KEEP | AUTO · replay 21:10 (§8 `[F-24]`: written by the build from the stored blob when the note came after 21:10) |
| A24 | header: rules line first, W/L, sniper x/y | SPEC §5.1-1 | MERGE → summary unit (§6) | AUTO (rules, W/L from engine + DAS); sniper x/y count AUTO over HIS tags |
| A25 | premarket block: sleep, meditated, tone, goal, sheet mode / hotkey file, score | SPEC §5.1-2, §1.1 row 4 | NEW section under A5 | PRE · daily note keys (F21), sheet + hotkey from the day-mode attestation row; meditated / tone per O11; score HIS |
| A26 | session scores (premarket + four blocks + overall) | SPEC §1.2 per day, §5.1-4 | NEW section | PRE · copied from the daily note's `Score -` lines (`daily.md.j2:84-99`); the grades are HIS (packet Q7) |
| A27 | playbookable trade | SPEC §1.2, §5.1-8 | NEW section | PRE · candidates AUTO (trades with every auto rule `pass`); choice HIS |
| A28 | disputes / correction lines | SPEC §1.2, §8 step 8 | NEW unit | HIS input; AUTO override log with reason (SPEC §9) |
| A29 | next-day review grade per trade → grade log | SPEC §1.2, §5.1-7, §4 | NEW section on the prior DRC | HIS grade; AUTO slot + miss direction vs grade at card |
| A30 | discretion audit entries | SPEC §5.1-6, §4 | NEW section | PRE · stop moves AUTO (DAS order log if E1; v3 stop edits); structure HIS |
| A31 | open items carried forward | SPEC §5.1-10 | NEW section | AUTO · open positions (R67), unanswered reconcile, `needs_voice` rules, unresolved leg mismatches (R2-1 position B would write here), prior DRC's empty HIS rows |
| A32 | streak | SPEC §4 | MERGE → summary | AUTO · `drc_rows` day rows |
| A33 | rule-break ledger (core vs new) | SPEC §4 | weekly | AUTO · engine rows (shadow) |
| A34 | sniper tally vs target | SPEC §4 | weekly | AUTO count over HIS tags, n shown (L8) |
| A35 | exit-efficiency distribution | SPEC §4 | weekly | AUTO when MFE present (O1), n |
| A36 | rule-10 ledger (cost vs prevented) | SPEC §4 | weekly | PRE · counts AUTO, cost instances HIS (voice) |
| A37 | playbook instances per setup | SPEC §4 | weekly | AUTO · setup key per trade, n |
| A38 | structured trade / day records | SPEC §5.2 | KEEP as DB | AUTO · `drc_rows` (file export O17) |
| A39 | weekly review pack | SPEC §5.3 | separate weekly note (later slice) | AUTO |

### (B) PER-TICKER — RULED R71 — `### Catalyst + Set Up + Trades` block vs SPEC per-trade fields

| # | DRC bullet (DRC:68-162 shape) | SPEC field | ACTION | WRITER · source |
|---|---|---|---|---|
| B1 | `Ticker:` | §2 ticker, trade_id | KEEP | AUTO · DAS |
| B2 | `Entry #n — time` | §2 entry_time, attempt_number, thesis_key | MERGE (n = attempt per thesis) | AUTO · DAS + card time |
| B3 | re-entry #2 what's-new line | §1.2 re-entry what's new, §6 what_new | KEEP | HIS (presence AUTO-checked, rule 7) |
| B4 | Grade · Direction · Sheet mode | §2 grade_at_card, side; §6 card.sheet_mode | KEEP | AUTO · card |
| B5 | Entry · Stop · Shares · Risk budget | §2 planned_entry, planned_stop, planned_shares, risk_budget | KEEP | AUTO · card |
| B6 | Fill update | §2 avg_entry, shares (actual) | MERGE → plan-vs-DAS line | AUTO · DAS + `aset_sizings` fill columns (snapshotted, `[F-19]`) |
| B7 | `Catalyst:` | §1.2 catalyst | KEEP | PRE · daily-note Trade Ideas `Catalyst` column (`daily.md.j2:65`) / radar card WHY |
| B8 | `Set Up:` | §1.2 setup name, §6 setup_family / setup_name | KEEP | PRE · card `trade_def` (v3 seam 2) / trade note / TZ tag |
| B9 | `Trade Notes:` | §1.2 read at entry + flip condition | MERGE → "read / flip" | HIS |
| B10 | `Keys to success here:` | — (no SPEC field) | KEEP | HIS |
| B11 | setup + self observation | §6 narrative (verbatim) | KEEP | HIS (SPEC §9) |
| B12 | ideal vs actual dots on chart | §1.2 entry self-tag (closest) | KEEP + NEW tag line (B15) | HIS |
| B13 | `Insert Chart with Executions:` | §6 attachments | KEEP | AUTO · embed of the dropped screenshot (E9) |
| B14 | excitement question (reversion-tagged only, `prefill/drc.py` scaffold) | — (no SPEC field) | KEEP | HIS (packet Q14) |
| B15 | — | §1.2 entry self-tag | NEW bullet | HIS (config enum) |
| B16 | — | §1.2 exit reasoning, §6 exit_structure | NEW bullet | HIS (config list offered) |
| B17 | — | §1.2 stop-move structure | NEW line per stop move | PRE · time/price AUTO (order log, E1); structure HIS |
| B18 | — | §1.2 mistakes / lesson | NEW bullet | HIS (feeds A16) |
| B19 | — | §2 exit_time, hold_minutes | NEW line | AUTO · DAS |
| B20 | — | §2 avg_exit, gross/net P&L, commissions | NEW line | AUTO · DAS |
| B21 | — | §2 legs / scale events, R at exit | NEW table | AUTO · DAS (+ reconcile R67; source value `[L31 — NAME PENDING]`) |
| B22 | — | §2 card_first, card_lead_seconds | NEW line | AUTO · card + DAS (E4) |
| B23 | — | §2 card_regenerations | NEW line | AUTO · `aset_sizings` |
| B24 | — | §2 planned/actual risk, risk_overrun_pct | NEW line | AUTO |
| B25 | — | §2 planned_R, realized_R | NEW line | AUTO · `realized_r.1` (`not computed` before D5, `[F-19]`); planned R needs a target (typed / TZ) |
| B26 | — | §2 MAE, MFE, exit_efficiency, best_exit_gap_R | NEW line | AUTO when typed/OCR (O1) |
| B27 | — | §2 window | NEW line | AUTO · config windows |
| B28 | — | §2 seat_state_at_entry | NEW line | AUTO · DAS day |
| B29 | — | §2 consecutive_losses_before, minutes since last loss | NEW line | AUTO |
| B30 | — | §2 stop_moves, positive_pnl_stop | NEW table | AUTO · order log (E1) |
| B31 | — | §2 discretion_delta_R | NEW line | AUTO |
| B32 | — | §2 known_family, written_exit_exists | NEW line | AUTO · setup sheets (later slice) |
| B33 | — | §6 rule_results per trade | NEW line | AUTO · engine (shadow) |
| B34 | — | §5.1-3 flags (card after fill, regenerations, overrun, winner→loser, late tag at size) | NEW line | AUTO (late-tag flag needs HIS tag) |
| B35 | — | §1.2 next-day review grade, §6 grade_review | NEW line (next day) | HIS |

### (C) ADDITIONS — RULED R72: ALL ACCEPTED (displays / review rows only, n shown, <30 = insufficient data, none feeds a score or rank)

| # | Addition | Source | n rule (L8) |
|---|---|---|---|
| C1 | pick vs Cobalt's rank per filled card | `picks` rows (`cards/picks.py`) — `not given` until the slice that stores its inputs (`[F-19]`) | per-day list; agreement rate only at n≥30 |
| C2 | realized R confirmed after the DAS reconcile (no longer provisional) | v3 `realized_r.1` over reconciled legs — `not computed`, never `confirmed`, before D5 (`[F-19]`) | per trade; averages n≥30 |
| C3 | "R had Cobalt's stop held" | v3 seam 7, 21:10 replay — `not given` until stored (`[F-19]`) | per trade; Σ with n |
| C4 | stop gap: his stop vs Cobalt's structural stop | v3 `stop_in_force` − `structural_stop` | per trade |
| C5 | plan-vs-fill drift | `aset_sizings.distance_change_pct` vs DAS avg entry (snapshotted) | per trade |
| C6 | the day's radar cards (armed / triggered / expired / missed) linked to trades | `card_transitions`, `missed` — `not given` until stored (`[F-19]`) | counts with n |
| C7 | human-only dot per card, shown as his (L11) | card dots | — |
| C8 | logging accuracy: his taps / held-counts vs DAS mismatches | R67 reconcile rows | weekly rate, n≥30 |
| C9 | P&L and R by window (config windows) | `drc_rows` | weekly, n per window; <30 = insufficient data |
| C10 | expectancy per setup | `drc_rows` × setup key | n≥30 else insufficient data |
| C11 | all-rules-pass streak (shadow) | engine rows — `not given` until stored (`[F-19]`) | count days |
| C12 | TradeZella vs DAS P&L cross-check flag | typed TZ figure vs DAS | per trade |
| C13 | tomorrow's If/Then offered into the next daily note's blank `1% goal:` (L28 clause 2a, blank → value only) | A20 — only through the existing daily-note writer, not in D3 (`[F-19]`) | — |

None of (C) feeds a score or rank; all are displays / review rows (§12 unchanged).
