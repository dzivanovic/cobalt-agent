# Low-float / small-cap handicap: DERIVED v2, 2026-09-21

**Derived from:** `docs/30 - Design/FLOAT-HANDICAP-PROPOSAL-2026-09-21.md` (Opus 5, seat `float-handicap-design-0921`), whole; each amended paragraph is REPLACED in place and tagged `[F-nn]`; unchanged text is the proposal's own. Derive seat: Fable 5.1 (`claude-fable-5-1`), `float-handicap-tribunal-derive-0921`, per `cto-2026-09-21.md` §4 row R30 (`Fable seat: yes` · `derive seat: claude-fable-5-1`).

**Round-1 rulings folded (L67):**

| seat | status | source |
|---|---|---|
| Grok | ruled in full — `TRIBUNAL R1: BUILD AFTER ADR D4 names ladder_order` | `scratch/tribunal-bars-0920/float-handicap-tribunal/r1/grok-ruling.md` (raw); hub `reports/float-handicap-tribunal-2026-09-21.md` |
| Gemini | ruled in full — `TRIBUNAL R1: BUILD AFTER <ADR-0009 D4 text is amended and combinator key is added to HandicapBlock>` | `…/r1/gemini-ruling.md` (raw); hub report as above |
| Astra | **`SKIPPED — METER at probe`** — RECORDED, not a refusal, under `cto-2026-09-22.md` §4 R13 (this week's design tribunals run on Grok · Gemini · Fable; Astra reads this v2 when its meter returns, 2026-09-26 06:47 ET). No item below was answerable by Astra alone, so no item carries `ASTRA PENDING (R13)` on its own; the whole file is Astra-pending as one. | — |
| Fable | ruled in full (blind) — `TRIBUNAL R1: BUILD AFTER the derive settles pool-wide versus tier-bound division and one applied factor for both consumers` | `reports/float-handicap-tribunal-fable-r1-2026-09-21.md` |

**Fold table:** `docs/40 - DevDocs/reports/float-handicap-tribunal-derive-2026-09-21.md` `## Fold table`. **Status:** `## NEEDS ROUND 2` lists TWO items (R2-1 tier-bound vs pool-wide division; R2-2 the `handicap_cap` mechanism and its counterfactual) — this v2 is NOT yet the FINAL's input. Ladder: `OFF-LADDER — ruled cto-2026-09-21 R28 / R29`. Committable: no value of his (L32) — thresholds, factor, missing rule, combinator and mode appear as KEYS only.

---

## 0. The fact base, in brief

| # | Fact | Evidence |
|---|---|---|
| F1 | Pool rank does not come from one score. It is a lexicographic key: (priority group, `first_from` flag, **position of the name inside the source that ranks it best**, note order, ticker). The position is the name's place in one screen, sorted by that screen's metric (volume or rvol). The key compares positions ACROSS screens, never metric values. | `src/cobalt/radar/pool.py:94-106`, `:131-209` (the tuple at `:209`) |
| F2 | The 50 are `ordered[:cap − held]`, adjusted by stickiness (`below_cap_streak` against `stickiness_scans`). A name that loses its seat gets `EXCLUDE` or `LEAVE` with `excluded_by = config_cap`. | `pool.py:332-334`, `:349-378`, `:393-398` |
| F3 | When the pool is ranked, each candidate carries only `{volume, rvol}` per source, plus the `not_equity` verdict. Float and market cap are **not parsed**. | `src/cobalt/radar/runner.py:139-145`; `SourceSet.metrics` at `src/cobalt/radar/models.py:184` |
| F4 | Screens and lists are both fetched with the same full export, columns `0-150` (`c=`). **Every source's CSV therefore carries `Market Cap` and `Shares Float`**, but neither is in `required_headers`. | `src/cobalt/radar/collector.py:158-162`, `:199-208`; `configs/cobalt/radar.yaml:18-24` |
| F5 | Units, verified on the real-shape fixture: `Market Cap` is in **$ millions** and `Shares Float` is in **millions of shares**. Row `AA` has `Market Cap 12659.74`, `Shares Outstanding 263.91`, `Shares Float 262.49`, `Price 47.97`, and 263.91 × 47.97 = 12,659.8. | `tests/fixtures/radar/pool-metrics.real-shape.csv:1` (header), `:2` |
| F6 | Some real equity rows are blank. In the real-shape fixtures, 4 non-fund rows have a blank float (1 in gainers, 3 in losers) and 1 also has a blank cap. In the retained production cache, equity (source, ticker) pairs with a blank float / blank cap were 3/1 of 409 on 2026-09-18 and 4/2 of 383 on 2026-09-21, from both screens and lists. **So the data is present for almost every candidate, but not every one.** | `tests/fixtures/replay/movers-gainers.real-shape.csv`, `movers-losers.real-shape.csv`; `data/radar-cache/2026-09-18/`, `2026-09-21/` (read 2026-09-21) |
| F7 | The raw CSVs are kept for **7 days** only. No DB row stores float or cap. | `configs/cobalt/radar.yaml:30-32`; `src/cobalt/db_migrations/0004_radar_pool.sql:29-51` |
| F8 | The S5 receipt stores `pool_unit`, which holds the pool block and every `SourceSet`, including `metrics`. Once float and cap are in `metrics`, they are stored inputs (L57) at no extra cost. | `runner.py:319-323`; `src/cobalt/radar/evaluate.py:920-921` |
| F9 | `card_score = round(conviction × proximity × 100)` is documented as THE one ranking authority on the card. Conviction is the mean of HIS taps only. An untapped card has no score. | `src/cobalt/cards/scoring.py:1-4`, `:27-28`, `:238-244`, `:264-267`; ADR-0009 D4 (`docs/10 - Decisions/ADR-0009-…md:28`) |
| F10 | A card is born with no taps, so `card_score` is null at creation. It becomes a number only when he taps, through `store.py:1219`, or on refresh through `evaluate.py:777`. | `evaluate.py:1383-1395`; `src/cobalt/cards/store.py:1185-1227` |
| F11 | The ladder sorts WATCH cards by (`card_score` desc NULLS LAST, **`pool_position`**, ticker). It sorts PINNED cards by (**`pool_position`**, score). `pool_position` is `radar_membership.last_rank`. **Before any tap, the ladder order IS the pool rank.** | `src/cobalt/cards/radar.py:23-33`, `:174-183`; `src/cobalt/db_migrations/0007_radar_cards.sql:227`; `evaluate.py:1225` |
| F12 | F3 focus (`FOCUS_TOP_N = 4`) ranks open cards by `card_score` only. | `src/cobalt/cards/picks.py:59`, `:226-233` |
| F13 | No scored dot reads float today. No computer exists for it (`FACTOR_COMPUTERS` = atrs_from_open, rvol, leg_count, htf_level_proximity). One strategy note carries a HUMAN short-interest-of-float dot, which is hollow and counts only when tapped. F10's charter lists "float" among possible screener-field dots. | `evaluate.py:429`; `scoring.py:163-165`; `docs/00 - Project/SPRINT-LADDER-v0_1.md:453` |
| F14 | The pool block is strict (`extra="forbid"`). An unknown key makes the note fail to parse, so `pool_error` is set and the pool FREEZES, loudly. | `models.py:99-107`; `src/cobalt/radar/notes.py:107-109`, `:408-415`, `:64-65` |
| F15 | `excluded_by` is CHECK-constrained in two tables and one Python set. The miss line counts by it. | `0004_radar_pool.sql:42-43`; `0009_picks_missed.sql:76`; `src/cobalt/replay/models.py:30-34`; `src/cobalt/replay/movers.py:102`; `src/cobalt/replay/line.py:97` |
| F16 | The `/radar` pool view orders its rows by `last_rank`. The card face shows score, conviction, proximity and pool under owner badges. | `src/cobalt/aset/radar_panel.py:542-544`, `:998`; `cards/radar.py:51-68` |

**Corrections to the fact base, taken verbatim from the round-1 WRONG FACTS lists (each HOLDS in the hub's file-check or was re-opened by the derive seat):**

- **[F-19] F1 — grok, WRONG FACTS #1 (HOLDS, hub file-check `pool.py:154-165`, `:209`):** "`PROPOSAL.md:13` says the key uses the position inside the source that ranks the name best. `pool.py:162-165` takes `min` of `(first, position, note_order, source)`. A worse position on the `first_from` screen wins."
- **[F-20] F2 — grok, WRONG FACTS #2 (HOLDS, hub file-check `pool.py:351`, `:370`):** "`PROPOSAL.md:14` (F2) says a name that loses its seat gets `excluded_by = config_cap`. `pool.py:351` sets `config_cap` only when the ticker is still a candidate, otherwise `None`. `pool.py:370` sets `config_cap` only when `rank` is not `None`."
- **[F-21] F11 — grok, WRONG FACTS #3 (HOLDS, hub file-check `cards/radar.py:174-185`):** "`PROPOSAL.md:26` (F11) says that before any tap the ladder order is the pool rank. `cards-radar.py:174-177` places every pinned card above WATCH, so a pinned card at a worse `pool_position` ranks above a better untapped WATCH card. The narrower table at `PROPOSAL.md:35` (untapped WATCH only) is not this error."
- **[F-24] F15 / §6 / Open 6 — Fable, WRONG FACTS 1 (re-opened by the derive seat: `replay/line.py:60`, `:95-98`, `:119` read; HOLDS):** "Proposal F15 / §6 / Open 6: 'The miss line counts by it … (`line.py:97` counts by value, and no other change is needed there)'. `replay/line.py:95-98` counts CARD rows over `CARD_GATES` (`line.py:60`: five card gates; `config_cap` is not one). A mover's `excluded_by` is PRINTED per ticker at `line.py:119`, never counted."
- **[F-26] Open 4 — Fable, WRONG FACTS 3 (HOLDS, hub file-check of item (k) `pool.py:127`, `:154-165`, `:209`):** "Proposal Open 4 presents screens-over-lists as THE dominance; `pool.py:161-165` + `:209` hold a second one (`first`), and his pool block sets `first_from` on one screen (`1 - Trading/Radar Screens.md:236-242`, key only)."
- F6's counts and the §2 worked example are **UNVERIFIED** by every house that ruled (grok X1, X2; Fable X1, X2) → experiments X1 and X2 below, never a defect (L70).

What "score" and "rank" mean at each surface he named:

| Surface | Number that orders it today | Where |
|---|---|---|
| Pool membership (the 50) | lexicographic key with within-source **position** (F1) | `pool.py:209` |
| Ladder, untapped WATCH cards (the morning case) | `pool_position` = `last_rank` | `cards/radar.py:181-182` |
| Ladder, tapped WATCH cards | `card_score` | `cards/radar.py:181` |
| Ladder, pinned cards | `pool_position`, then score | `cards/radar.py:176-177` |
| F3 focus (top 4) | `card_score` rank | `picks.py:226-233` |
| Card dots / proposed key | conviction bands. **Not a rank, and not touched here.** | `scoring.py:273-286` |

- **[F-25] two consumers this table omits — Fable, WRONG FACTS 2 (re-opened by the derive seat: `cards/picks.py:184-192`, `:327` and `radar/store.py:419-427`, fed at `evaluate.py:1420-1424`, read; HOLDS):** "Proposal §1 consumer table claims 'Every consumer of an order, checked' — it omits the pick journal, which reads `last_rank` as `pool_rank` (`cards/picks.py:184-192`, rendered `:327`), and the `system.radar_score.card_score` copy (`radar/store.py:419-427`)."

---

## 1. WHERE THE PENALTY ENTERS

**[F-01] O1 — grok's wording, verbatim (gemini ADOPT; Fable ADOPT WITH a storage variant, carried as an owner item):** "Option (c) stands. One factor `h` is computed once per ticker per scan, stored on the membership row, and consumed read-only at the pool key and at `card_score`. `handicap.mode: live` is not written until the H3 deploy is in production. In `shadow`, neither consumer sorts by `h`: `last_rank` stays the raw admission rank, and `card_score` stays `round(conviction × proximity × 100)`. The penalised rank and the penalised score are display-only would-be figures. In `live`, the pool key uses the effective position and `card_score` is `round(conviction × proximity × h × 100)`, rounded once."

| Option | What he gets | Cost | Verdict |
|---|---|---|---|
| (a) pool rank only | The 50 move. Untapped ladder order moves, because it IS the pool rank (F11). Tapped cards and F3 focus do NOT move. | M, one migration | Rejected as the full design. The ONE ranking authority (`card_score`, F9) would not carry the handicap, so from his first tap the card would be ordered by an unhandicapped number while its secondary key is handicapped. That is two authorities disagreeing (L52 b). |
| (b) `card_score` only | Nothing moves until he taps (F10). The pool's 50 are unchanged. | S–M | Rejected. It misses his first requirement ("still within the top 50"). At first render every card is untapped, so the ladder would not change either. |
| **(c) one `h`, both places** | The 50 **and** the whole ladder (untapped through `pool_position`, tapped through `card_score`) **and** F3 focus (it reads `card_score`) | Three chunks, ~M each (§9) | **Proposed.** It is one computation. The pool position and the card score are its two consumers, and `h` is never recomputed at the second (L3). |

**Why `h` must enter the POSITION and not the metric value.** Pool rank compares positions across screens (F1). Multiplying a name's volume or rvol by `h` only reorders that name inside its own screen. If every name in a screen is in the group, which is the case for the morning low-float screen by construction, a metric multiplier changes **nothing**: the relative order is identical, so the positions are identical. The handicap must act on the cross-source comparable, which is the position.

**[F-09 · F-04 · F-18] Mechanism at the pool — NEEDS ROUND 2 (R2-1): the houses SPLIT on what is divided.** The proposal's sentence was: `effective_position = position ÷ h`, replacing `position` in the key tuple at `pool.py:209` for screen-group names, and the union position for list-group names (`pool.py:179-206`); the priority group untouched; `rank_value` stays the raw metric. Two replacement wordings were offered. Neither is taken by this derive; both are carried verbatim (L37: a split is carried, never settled by vote); round 2 settles which one v2 builds.

- **Position A — TIER-BOUND (grok, item (c), ADOPT WITH; gemini item (c) ADOPT and item (k) TIER-BOUND agree; hub file-check of (k) HOLDS):** "`effective_position = position / h`, in `Decimal`, quantized to the `handicap_factor` scale (`NUMERIC(6,4)`), replaces `position` in the key at `pool.py:209`. `position` is the 1-based place among names that reach ranking. On a screen that is `_metric_position` over `source.tickers` minus not-equity and minus the exclude block. On a list group it is the existing union index over candidates (`pool.py:179-206`). Apply `h` once, to the winning position after the min at `pool.py:165`. `rank_value` stays the raw metric. Stickiness and `below_cap_streak` are unchanged. A tie on the quantized position breaks by `note_order`, then `ticker`." — and its tier statement, grok item (k), verbatim: "The handicap is TIER-BOUND. It divides the within-source position. It does not change `priority`, `first_from`, or the held-seat reservation. A handicapped name in an earlier tier still beats every name in a later tier, whatever `h` is. POOL-WIDE is not this build. Tiers, and what `h` does to each: (1) `priority`, key component 0, `pool.py:127` and `:209`, `radar-models.py:104-112` — inert for which group a name is in; active only for order inside the group. (2) `first_from`, key component 1 inside screens, `pool.py:154-165` and `:209`, `radar-models.py:78-88` and `:113-115` — after the clock, the one screen that set it sorts ahead of other screens; `h` does not cross that. Before the clock every screen has `first = 1` and the tier is flat. (3) Held members, `pool.py:283-286`, `:312-321`, `:323-324`, `:380-384` — a name whose only live source is degraded keeps its prior seat and is not re-ranked; `h` is inert. (4) Stickiness, `pool.py:336-378` — not a tier `h` may cross; `h` does change who is evicted, because the victim is the worst newcomer by the handicapped rank. The grace count is his `stickiness_scans`, unchanged. (5) Removals before ranking — manual exclude, not-equity, screen-inactive — `pool.py:269-309`; `h` never sees those names."
- **Position B — POOL-WIDE (Fable, item (c), ADOPT WITH replacement; Fable REJECTS O4 on the same ground — see `## Dissents, verbatim`):** "`_ranked()` is untouched. After `pool.py:326`, when the block is present: `eff[t] = Decimal(raw_rank[t]) ÷ factor[t]` (`factor` = `handicap.factor` for an in-group name, 1 otherwise; a `Decimal` field, never a float); `ordered` is re-sorted by `(eff, in_group, raw_rank)` — on an exact tie the unhandicapped name wins; `ranks` is rebuilt from that order. In `mode: shadow` the re-sort is computed, stored as the would-be rank and NOT used. `raw_rank` and `rank` both ride the `Transition`. The group values come from the source `source_for[ticker]` names."
- **What round 2 must answer:** (i) does R28 (b) — "competes on its LOWERED score for the pool's 50" — require a handicapped name to compete ACROSS `priority` and `first_from` (B), or inside its tier (A), given that both houses also name the crossing as HIS under L53 (grok OWNER item "POOL-WIDE instead of TIER-BOUND"; Fable OWNER 1)? (ii) under A, does H2 change the position basis to "names that reach ranking" (grok: not-equity and exclude-block rows no longer occupy positions — a change to raw order at `h = 1`) or does `_metric_position` stay as it is (Fable: "do NOT 'fix' it — the `h = 1` dry-run must reproduce stored membership")? Unresolved after round 2 → Dejan (L39), never a vote.
- **Common ground, both positions:** `Decimal`, never float; the exclude block stays the only hard tool; stickiness keeps his `stickiness_scans`; `rank_value` stays the raw metric.

Mechanism at the card: `card_score = round(conviction × proximity × h × 100)`, rounded ONCE (`scoring.py:39-41`). Conviction, proximity, dots and `proposed_key` are **unchanged**. His taps stay his grades, and the key he is offered and the size come from conviction alone (`scoring.py:273-286`). Only the ORDER changes.

**Proof that nothing else re-ranks around it (L52 b).** Every consumer of an order, checked:

| Consumer | Reads | After the change |
|---|---|---|
| Pool 50 + stickiness | key with `effective_position`; `ranks` | carries `h` (`pool.py:326`, `:349-378`) |
| `/radar` pool view | `last_rank` | carries `h` (`radar_panel.py:542-544`) |
| Ladder, WATCH + pinned | `card_score`, `pool_position` (= `last_rank`) | both carry the same `h` |
| F3 focus | `card_score` | carries `h` |
| Poller | `rank`, polling ORDER only (every member is polled) | carries `h`. It orders polling and never reaches the card (`src/cobalt/radar/poller.py:84`). |
| Proposed key / sizing | conviction | **untouched by design** |

- **[F-22] correction to the sentence below the table — grok, WRONG FACTS #4 (HOLDS, hub file-check `cards/radar.py:174-183`):** "`PROPOSAL.md:70` says the two consumers cannot re-rank around each other because both carry `h`. `cards-radar.py:179-182` orders tapped WATCH by `card_score` then `pool_position`. `pool.py:209` orders the pool by `(priority, first, position, note_order, ticker)`. `×h` and `÷h` are not the same function, and the staged sorts already diverge."
- The poller row's citation (`poller.py:84`) is **UNVERIFIED** by grok (X8 below); the pick journal and the `radar_score` copy are added to this table by [F-25] above.

**[F-07] (a) ONE FACTOR, TWO PLACES — grok's wording, verbatim (HOLDS in the hub's file-check; gemini's "None exists" DOES NOT HOLD as stated and is not taken; Fable's ADOPT WITH is bound to its O1 storage variant and is carried as an owner item):** "`h` is one computation, stored once on the membership row (L3), and read at two consumers. It is not one sort. The one ranking authority that reaches the card is `ladder_order`. In `live`, both of its numeric inputs carry this same stored `h`: `card_score` is multiplied by `h`, and `pool_position` is the admission rank from the key whose position was divided by `h`. The `/radar` pool view orders by that admission rank. Focus orders by `card_score`. The surfaces may disagree. The card does not re-sort to match the pool, and each surface shows the raw figure beside the penalised one. The poll consumer in the staged search is `PollMember(..., last_rank)` at `runner.py:245`. It does not reach the card. `poller.py:84` is not in that search (item j, X8)."

**[F-08] (b) THE AUTHOR'S ESCALATE 1 (ADR-0009 D4 against `ladder_order`) — resolved as ADR TEXT, placed BEFORE H1; grok's wording, verbatim (gemini's ADOPT WITH is the same amendment in fewer words; Fable's "new ADR shipped in H1's commit" reconciles the same sentence and is not taken because it bundles Fable's other rulings; the three houses do not split on text-not-code):** "Before H1, amend ADR-0009 D4 as a documentation change, not as a behaviour change. The one authority that reaches the card is `ladder_order` (`cards/radar.py:174-183`). Pinned cards order by `pool_position`, then `card_score`. WATCH orders by `card_score` nulls last, then `pool_position`. When every WATCH `card_score` is null, that block's order is `pool_position`. `last_rank` admits and is that `pool_position`. The sentence 'pool `last_rank` only admits' is withdrawn. Do not change `ladder_order`. In `live` both numeric inputs carry the stored `h`."

---

## 2. THE SHAPE

**Flat multiplier `h = handicap.factor` (0 < h ≤ 1) for the whole group. ASSUMED by key. One value serves both consumers.** [F-02: O2 ADOPT by grok, gemini and Fable — proposal kept; Fable's threshold-flapping concern is experiment X11, not an objection.]

- Pool: position ÷ h. Card: score × h. "A handicapped name counts h as much" reads the same at both.
- Flat, not graded. A graded shape (h rising from a floor at tiny float to 1 at the threshold) needs two more values of his and is harder to explain aloud. It is listed as an alternative (Open 2).
- A rank offset (+N positions) was rejected. It has no meaning on `card_score`, so it would need a second mechanism and a second value (L3).
- A point deduction on `card_score` was rejected. It has no meaning on a position.

**Worked example on the real stored SHAPE** (`data/radar-cache/2026-09-18/`, an RTH scan). Four screens were active, carrying 11 / 9 / 15 / 24 equity names. Every name of the low-float morning screen is in the group by construction. A minority of the others are. Screens sort ahead of lists (his priority), and interleave by position, so about four names are admitted per position level until the small screens run out. With his `cap`, the cut-off lands at some position `c` (on this scan, around the low-float screen's last name).

- A handicapped name at within-screen position `p` competes as `p ÷ h`. **It keeps its seat iff `p ≤ h × c`.** A genuinely top name (p = 1, 2, 3) survives any reasonable `h`. With `h` near 1 only the tail of the group slides out. With `h` = ½, a handicapped name needs the position an unhandicapped name reaches at half the depth.
- The low-float screen stops taking its whole column. Its bottom `(1 − h) × c` names give their seats to the next unhandicapped names (further screen positions or lists). **That is the effect he described.**
- Ladder top ten, tapped: a handicapped card stays top ten iff `h × raw_score ≥` the tenth-best unhandicapped score. Untapped: iff its `effective_position` ranks inside the ten.

- **[F-23] correction — grok, WRONG FACTS #5 (HOLDS, hub file-check `pool.py:209`, `:332-378`):** "`PROPOSAL.md:85` says a handicapped name keeps its seat iff `p ≤ h × c`. `pool.py:209` compares `priority` and `first` before position, and `pool.py:332-378` reserves held seats and then runs stickiness. Any of those binds the `iff` false." The screen counts and the cut-off of this example are UNVERIFIED (grok X2; Fable X2) → experiment X2; "in the group by construction" → experiment X8.

The tribunal receives the report's numeric version of this example at his ASSUMED default. This committed file carries only the shape.

---

## 3. THE GROUP TEST

`in_group = (float < handicap.float_below_m) OR (market_cap < handicap.market_cap_below_m)`. Both thresholds are his keys, in the export's own units: millions of shares and $ millions (F5). The test is strict `<`, as he said "below". [F-12: the OR is now the `combinator` KEY of §5 (R29: his to flip without a build); the `any` / `all` semantics are in [F-10] below.]

- **ONE function** (L3), shaped like `is_not_equity` (`src/cobalt/radar/config.py:100-117`): `handicap_group(row, headers, block) -> GroupVerdict{in_group: yes|no|unknown, float_m, market_cap_m, reason}`.
- **Header names and unit are engine config**, not his values. They go in `radar.yaml` under `export.handicap_headers: {float: "Shares Float", market_cap: "Market Cap"}` and are checked by `load_config` (`config.py:168-177`). They are **not** added to `required_headers`: a renamed Finviz column must degrade the handicap, not kill every source (see L9 below).
- **Values**: parsed by the existing `_number` (`runner.py:391-398`). Blank and `-` become None.
- **[F-10] Missing data (L1) and L9, dead column — grok's item (d) wording, verbatim, REPLACES the proposal's two bullets (gemini ADOPT with `X2` on the live format; Fable's ADOPT WITH — an unparseable non-blank cell and a dead column go inoperative at factor 1 whatever `handicap.missing` says — is the larger mechanism and is carried as an owner item on the meaning of his `missing` key):** "The verdict is `yes`, `no`, or `unknown`, from one function. `combinator any`: `yes` when at least one known value meets its threshold; `no` when every value the combinator needs is known and none meets; `unknown` when a blank could still change the result. `combinator all`: `yes` only when both are known and both meet; `no` when both are known and one fails; `unknown` when either is blank. Blank, `-`, and unparseable are `unknown`, never a silent `no`. `handicap.missing` is required whenever the block exists. The pool row shows `unknown → applied` or `unknown → not applied`. Applied shows the `H` chip and the penalised figures; skipped shows the line and no chip. The card shows the same line under the COBALT badge. A missing header, or every equity candidate on the scan `unknown`, appends `handicap` to `degraded_sources` with a reason the panel renders: which header, and, when `missing` is `apply`, that every name carries the same `h` so the order is unchanged. `_number` (`runner.py:391-398`) is the parser: it strips commas and one percent sign, and it returns null for a currency symbol or a suffix. The dry-run prints the header names it read and five raw cells."
- **Source of the values**: the source that ranked the name (`source_for`, `pool.py:208`), recorded with the verdict. Every source of one scan comes from the same Finviz snapshot. (Whether two sources disagree on one ticker's cells is experiment X10.)
- **Order**: not-equity (`runner.py:144-145`) and the `exclude` block (`pool.py:292-297`) act first. The test runs only on names that reach ranking.
- **Replay-from-bars** (`src/cobalt/radar/replay.py:72-81`, header `Ticker, Volume, Relative Volume`) has no float or cap. That tool reports `handicap: not replayable from bars` and does not guess.

---

## 4. VISIBLE

- **Pool row** (`PoolRow`, `radar_panel.py:148-163`): a badge **`HANDICAP`** with a hover or detail line reading `float <v>M / cap $<v>M → group (float|cap|unknown) · pos <p> → <p÷h> · rank <raw> → <effective>`. In shadow mode the badge reads **`HANDICAP (shadow)`** with the would-be rank. The badge style is `badge`, like the others (`radar_panel.py:1059` CSS), and the owner is COBALT.
- **Pool header**: `handicap: live · shadow · not configured · degraded`. "Not configured" means the block is absent. It is shown, and the state is never implied.
- **[F-14] Card face — grok's item (h) wording, verbatim, REPLACES the proposal's card-face bullet (HOLDS in the hub's file-check: `proposed_key`, `snap_down` and sizing do not read `card_score`):** "The card face gains an `H` chip and the score reads raw → penalised, owner COBALT. `FIELD_OWNERS` (`cards-radar.py:51-68`) gains the new columns. In `shadow` the chip says would-be and `card_score` stays unpenalised (O1). `proposed_key`, `snap_down`, the tapped grade, `shares`, and `risk_budget` do not read `card_score` or `h`. A test scores the same dots at `h = 1` and at `handicap.factor` and asserts the proposed key, the tap, the share count, and the risk budget are equal."
- **[F-15] Card path completeness — Fable's item (h) wording, verbatim (no hub file-check; the derive seat opened `radar/store.py:419-427`, `evaluate.py:777`, `:1080`, `:1384`, `:1420-1424` and `cards/store.py:1219`: every cited site exists and calls `score_card` / `card_score` or copies `card_score`):** "H3's list adds: the `system.radar_score` copy (`radar/store.py:419-427`, fed at `evaluate.py:1420-1424`) carries the handicapped `card_score` and must be reachable to its factor through `membership_id`; the four `card_score` call sites are `evaluate.py:777`, `:1080`, `:1384`, `store.py:1219` — all four take the factor or the recompute is not exact."
- **Excluded list**: a name that lost its seat BECAUSE of the handicap shows `excluded: handicap_cap`, separate from `config_cap` (§6). [F-06: the label and its predicate are NEEDS ROUND 2 (R2-2) — see §6.]

Nothing is hidden. The raw rank and the raw score are always one click away.

---

## 5. TUNING

**[F-12] Home and schema — grok's item (f) wording, verbatim, REPLACES the proposal's "Home" paragraph and its "Schema (L10)" bullet (HOLDS: the hub's file-check confirms the proposal had no `combinator` key; gemini's `combinator: Literal["any", "all"] = "any"` is NOT taken — a code default is an assumed value in production, against O5 which gemini itself adopted; Fable's `combine` key is the same key under another name and is not taken — R29's own word is "combinator"; Fable's ROLLBACK clause is carried in the derive report's ESCALATE, owner: desk):** "The home is his vault pool block, sub-block `handicap`, model `HandicapBlock` nested on `PoolBlock`, `extra="forbid"`. It is not `"user".trader_settings` `card.*`. Keys, all required, no code default: `float_below_m` Decimal `> 0`; `market_cap_below_m` Decimal `> 0`; `factor` Decimal `0 < factor ≤ 1`; `missing` Literal `apply` | `skip`; `mode` Literal `shadow` | `live`; `combinator` Literal `any` | `all`. The pool stage reads the block once and stores `handicap_factor` and the verdict on the membership row. The card stage reads that row, never the thresholds. Header names and units stay engine config (`export.handicap_headers`), not his values, and they are not added to `required_headers`. The desk writes the block only after a parser proof on the deployed build. His typed `approve` of `shadow → live` names the note path, the post-edit sha256, and the dry-run report path, and it is not given before H3 is in production."

- **Change path**: he rules, the desk edits his note (L65), shows the before/after diff, and runs a read-only parser proof (`cobalt radar sources`). The next scan reads it. No restart, because the note is parsed every cycle (`runner.py:178`). `cobalt settings load` is NOT involved, since no `card.*` key is added. The sha256 of the note lands in the mirror and the receipt.
- **Deploy order is load-bearing**: the code that accepts `handicap:` must be live BEFORE the desk writes the block. Old code would freeze the pool on the unknown key (F14). (Freeze versus crash on current main is experiment X4; the same fact governs a ROLLBACK of H1 while the block is in his note — derive report ESCALATE, owner: desk.)
- **ASSUMED**: no ASSUMED value ever reaches production. Until he rules, the block is absent and the header says `handicap: not configured`. The ASSUMED defaults exist only in the Owner items below, for his reading after the tribunal. A value stops being ASSUMED when he rules it and the desk writes it into his note. [F-05: O5 ADOPT by grok, gemini and Fable — proposal kept; the C2 assumed store is not reused.]

---

## 6. REPLAY + MISS LINE (L57)

**Stored per membership row** (one migration, additive, all nullable so pre-deploy rows stay valid):

| Column | Content |
|---|---|
| `raw_rank INTEGER` | the unhandicapped rank of this scan |
| `handicap_factor NUMERIC(6,4)` | `h` applied (1 when not in the group; NULL when the block is absent) |
| `handicap JSONB` | `{float_m, market_cap_m, verdict, reason, missing_rule, mode, position, effective_position, source, block_sha256}`, validated by a Pydantic model before write |

`last_rank` and `rank_at_entry` keep their meaning: the rank that governed admission. That is the effective rank when `mode: live`, and the raw rank in shadow, where the would-be rank sits in `handicap.effective_position`. (Under R2-1 position B the JSONB's `position` / `effective_position` become the would-be pool-wide rank; the field set of this JSONB is therefore settled by round 2.)

**Replay**: F8 means float and cap enter `SourceSet.metrics` (`runner.py:139-142` gains two keys). They are then in every receipt's `pool_unit`, together with the pool block, so `decide()` is replayable from stored inputs. The card path stores `handicap_factor` on the card row (`"user".aset_sizings`, H3). `card_score` is then recomputable exactly from the row: conviction, proximity, `h`, rounded once. `formula_sha256` changes with the formula, so an old card and a new card are distinguishable (`evaluate.py:161`). (Whether today's receipt already carries `metrics` is experiment X5.)

**[F-06 · F-13] Miss line — NEEDS ROUND 2 (R2-2): the houses SPLIT on the mechanism AND on the counterfactual.** The proposal's text stands as the base until round 2 rules: add ONE value, `handicap_cap`, additive and never a rename, into the `radar_membership` CHECK (`0004:42-43`), the `"user".missed` CHECK (`0009:76`), `replay/models.py:33`, `EPISODE_EXCLUSIONS` (`movers.py:102`), and `ExcludedBy` (`models.py:163-167`); definition: an `EXCLUDE` or `LEAVE` that would be `config_cap`, for a name with `h < 1`, whose rank with its own `h` set to 1 (everyone else unchanged) would have been inside `winners`. Three positions, each verbatim, none taken by this derive:

- **Position A — enum value, second full `decide()` (grok, O6 ADOPT WITH; gemini O6 ADOPT is the enum with the proposal's predicate):** "`handicap_cap` is a new `ExcludedBy` value, additive, never a rename and never a boolean column. Add it to the `radar_membership` CHECK (`0004_radar_pool.sql:42-43`), the `"user".missed` CHECK (`0009_picks_missed.sql:76-82`), `replay/models.py` `ExcludedBy`, and `EPISODE_EXCLUSIONS` (`replay/movers.py:102`). Do not add it to the card-formation vocabulary (`replay/cards.py`). The miss line already counts by `excluded_by` (`replay/line.py:97`) and needs no second key. A `config_cap` EXCLUDE or LEAVE is relabelled `handicap_cap` only when that name's `h < 1` and a second `decide()` with that name's `h` set to 1, every other input unchanged, admits the name (`ADMIT`, `RETAIN`, or `HOLD`) where the real `decide()` does not. Both calls include stickiness and held seats. Membership in `winners` is not the test." — and grok's item (g), verbatim: "The replay inputs for a penalised rank are `SourceSet.metrics` (float and cap included) and the membership columns `raw_rank`, `handicap_factor`, and `handicap` JSONB (`float_m`, `market_cap_m`, verdict, reason, `missing`, `combinator`, `mode`, position, effective position, source, `block_sha256`). H1's acceptance includes one receipt whose `pool_unit` shows the two metric keys. If today's receipt drops `metrics`, H1 stores them. `card_score` replays from stored conviction, proximity, and `handicap_factor`, rounded once, only when `formula_sha256` is an H3-or-later hash; an older hash recomputes without `h`. `handicap_cap` is additive to the sets in O6, with no renamed value. The single-name counterfactual is the second `decide()` in O6, run for each `config_cap` name. It is deterministic: `decide()` is a pure sort and the key ends with `ticker` (`pool.py:209-211`). Whether it stays inside the scan cycle is X4."
- **Position B — JSONB flag, no enum, O(1) counterfactual (Fable, O6 REJECT with replacement; see `## Dissents, verbatim`):** "`excluded_by` keeps its four pool values. A cap exclusion that the handicap decided is recorded as `handicap.decisive: true` in the membership row's `handicap` JSONB (H1's column). `replay/movers.py` copies `decisive` into each episode entry of `gate_detail.episodes` (`:452-458`); the miss line prints `config_cap (handicap)` for such a mover (`replay/line.py:119`). No CHECK changes, no enum change, no migration in H2." — and Fable's item (g), verbatim: "`raw_rank` = the rank from `_ranked()` with NO factor applied to anyone (in shadow it equals `last_rank`). `decisive` = the name is outside `winners`, its applied-or-would-be factor < 1, and `(Decimal(raw_rank), 0, raw_rank) <` the sort key of `winners[-1]`: one comparison per name. `decisive` means 'inside `winners` with its own factor at 1'; a newcomer inside `winners` can still lose its seat to a sticky member (`pool.py:366-378`), and the definition says so. The group test and the re-sort live in ONE new module `radar/handicap.py`, added to `FORMULA_FILES` (`evaluate.py:144-152`)."
- **What round 2 must answer:** (i) storage — an additive `ExcludedBy` value with two CHECK changes (A) or a flag inside the H1 JSONB with no CHECK change (B), given Fable's rollback sequence (a reverted `EPISODE_EXCLUSIONS` raises `ReplayInputError` on a stored `handicap_cap` row — `movers.py:444-447`, opened by the derive seat and present) and grok's "the boolean loses the miss line" (whose support, `line.py:97` counting by value, DOES NOT HOLD — [F-24]); (ii) the counterfactual — a second full `decide()` including stickiness and held seats (A) or a one-comparison test against `winners[-1]` that knowingly excludes stickiness (B); grok's scenario (a `winners` newcomer stickiness evicts anyway) says B mislabels, Fable's definition says B names exactly "inside `winners` with its own factor at 1". Both positions agree `raw_rank` is stored and `card_score` replays from the row with a formula-hash branch. Facts both need: `FORMULA_FILES` (`evaluate.py:144-152`) today lists `evaluate.py`, `anatomy/*`, `cards/scoring.py`, `cards/health.py`, `cards/radar.py`, `cards/expire.py`, `aset/engine.py` — not `radar/pool.py` (opened by the derive seat).

---

## 7. SHADOW / PROMOTION (L7, L10)

Cards stay advisory. Nothing here promotes a dot or a grade (L7 is untouched: no variable flips from human-fed to engine-fed). **Pool membership, however, is a ranking change that alters which names get polled and can form cards.** It therefore goes through:

1. **Dry-run (L10)**: `cobalt radar handicap-dry-run --day <d>` replays `decide()` scan by scan over the retained cache CSVs (F7, ≤7 days, no DB). It runs twice, with `h = 1` and with his block. The `h = 1` pass must reproduce the stored membership of that day, which is a with-DB sanity check against `radar_membership`, read-only. The report he sees, one screen per day: members in/out delta per scan; the handicapped names that KEPT their seat and the ones that LOST it (with position → effective position); the names that took their seats; the ladder of that day's stored cards re-ordered, as top ten before → after; and n (days, scans) stated. [F-16: grok's item (i) wording in §9 adds: every retained day, not a sample; the tier of each cut printed; `n` as days and scans. This dry-run is experiment X12 below.]
2. **Shadow**: `mode: shadow` computes and stores everything and shows `HANDICAP (shadow)` with the would-be rank, but ranks on raw. This runs for at least the sessions the tribunal sets (ASSUMED: 2). [F-01: in shadow NEITHER consumer sorts on a penalised figure — grok O1.]
3. **Promotion**: his "approve" in the desk chat (L61) for the exact edit `mode: shadow → live`. The desk edits the note (L65), logs the time, and runs the parser proof. No deploy is needed for the flip. [F-12: the approve names the note path, the post-edit sha256 and the dry-run report path, and is not given before H3 is in production — grok (f).]

---

## 8. INTERACTION with `SETUPS-AT-DEFAULTS-PROPOSAL-2026-09-21.md`

- **Shared lines**: C1 and C3–C7 touch the formation stage of `evaluate.py` (`:34-37`, `:635-647`, registries). They do not touch `card_score`, conviction or the pool, which that proposal states itself at its §L52 (b) and "does NOT do". **No overlap with H1 or H2.**
- **Collision: C2 ↔ H3.** Both change `"user".radar_cards_v` (C2 adds `assumed_keys` and a chip; H3 adds `handicap_factor` and raw score) and both take a migration number. The fix is procedural: whichever lands second rebases its `CREATE OR REPLACE VIEW` onto the first and takes the next free number, proven by the L68 stacked gate. I recommend **C2 first**, because its tribunal began earlier. H1 and H2 do not wait for it (L72: not a blocker). [F-16: grok's item (i) in §9 names the fuller collision — `FIELD_OWNERS` in `cards/radar.py` and `evaluate.py` as well; the setups proposal's `:161` names both, opened by the derive seat.]
- **Anti-rigidity**: the group test is "export columns vs his thresholds → h". A later swing or options universe adds a sibling group (for example "open interest below X") as a list entry under `handicap:`, without a refactor. The first build ships exactly one group and refuses a second until ruled. The membership columns are universe-agnostic.

---

## 9. CHUNKS

Each chunk builds offline in its own worktree, is checkable by ≥3 houses, and deploys alone in the evening pause (L43, L66). The builder seat for all three is **Opus 5 (or Sol-high)** under L29, because each is a ranking or scoring path, and H1–H3 each carry a migration.

**Before H1 [F-08]:** the ADR-0009 D4 amendment of §1 (b), a documentation change; no restart (L42).

| Chunk | What | Delivers | Size | Restarts (L42) | Migration | Needs from `cobalt_dev` |
|---|---|---|---|---|---|---|
| **H1** | float and cap into `SourceSet.metrics`; `radar.yaml` `export.handicap_headers`; `HandicapBlock` (optional) in `PoolBlock` — six required keys per [F-12]; `handicap_group()` with the `any` / `all` verdicts per [F-10]; `h` computed and stored (`raw_rank`, `handicap_factor`, `handicap`), **shadow only: ranking unchanged**; degraded source `handicap`; pool-row badge + header state; `handicap-dry-run` | **He sees the marked names on `/radar`** (would-be rank). Dry-run evidence exists. | M | radar, aset | yes (membership columns) | apply the migration; with-DB suite; dry-run sanity pass against stored membership |
| **H2** | `mode: live` in `pool.py` (the division of §1 as R2-1 settles it); the miss-line mechanism as R2-2 settles it (enum + two CHECKs + replay sets, or the JSONB flag) | **The 50 compete on the lowered rank.** The untapped ladder follows (F11). | M | radar (replay is a one-shot job, not a resident) | as R2-2 settles it (two CHECKs under A; none under B) | migration if any + a replay run on a stored day |
| **H3** | `scoring.card_score(…, h)`; `handicap_factor` on `aset_sizings` (set at creation and refresh, read at tap); `radar_cards_v` columns + `FIELD_OWNERS` badges; card face `H` chip + `raw → penalised`; `picks` `score_inputs` carries `h`; receipt replay passes `h`; the four `card_score` call sites and the `radar_score` copy per [F-15]; the `FACTOR_COMPUTERS` tripwire test per [F-11] | **Tapped ladder and F3 focus carry the handicap.** One authority holds end to end. | M | aset, radar | yes (column + view) | migration; the view test (owner badges); tap-path suite |

**[F-16] Order, dry-run, collision — grok's item (i) wording, verbatim, REPLACES the proposal's paragraph on chunk order (gemini ADOPT; Fable's ADOPT WITH — H1 split into H1a/H1b, H2/H3 in either order with `live` entering the Literal in whichever deploys last, and a one-place fail-soft catch — is the larger change and is carried as an owner item and in the derive report's ESCALATE):** "Order is H1, then H2, then H3. Each is size M as proposed (not re-measured here), each ships its own migration, and the builder seat for each is Opus 5 or Sol-high. H1 restarts radar and aset. H2 restarts radar. H3 restarts radar and aset. `mode: live` waits until H3 is in production (O1). H1's dry-run, before that approve, replays every retained cached day and prints `n` as days and scans. Per scan: members in and out; handicapped names that kept a seat and names that lost one, with position and effective position; who took the seat; which tier the cut was (priority, `first_from`, held, stickiness, or position); that day's stored ladder, top ten, raw beside would-be. The `h = 1` pass must match stored membership. H1 does not change `Transition.rank`. C2 collides with H3 on `"user".radar_cards_v`, on `FIELD_OWNERS` in `cards/radar.py`, and on `evaluate.py`. C1 and C3–C7 also edit `evaluate.py`. No C chunk edits `pool.py`. If both are in flight, C2 lands first and H3 rebases. H1 and H2 do not wait."

Tests (L45, L69): the group test runs against `pool-metrics.real-shape.csv` (all present) and the movers real-shape fixtures (blank-float and blank-cap equity rows present, F6). A redacted real-shape screen CSV containing a blank-float equity row is added from the retained cache under the L45 fixture policy. The homogeneous-screen case (every name in group) must change ranks under position ÷ h, and a test proves that a metric multiplier would NOT. A missing header produces the degraded banner, never a silent un-handicap. The miss-line predicate gets a counterfactual unit test (shape per R2-2). `card_score` must be exact on recompute from the row. Thresholds live on a constructed block, never read from settings to assert a value (L69). The [F-14] equality test (same dots at `h = 1` and at `handicap.factor`: proposed key, tap, share count, risk budget equal) ships with H3.

**[F-11] Double-counting guard — Fable's item (e) wording, verbatim (no hub file-check; the derive seat opened `evaluate.py:429`: `FACTOR_COMPUTERS = ("atrs_from_open", "rvol", "Extension.leg_count", "htf_level_proximity")`, present; gemini ADOPT keeps the rule as text; grok's ADOPT WITH — the rule restated plus a test over names and declared input keys, in H1 — is the larger mechanism and its own text says an undeclared factor is not caught, so the exact-tuple pin is taken):** "H3 adds a tripwire test that pins the exact tuple `FACTOR_COMPUTERS` (`evaluate.py:429`); its failure message quotes the composition rule. A new computer cannot ship without editing that test in front of three checkers."

**Evenings, from "tribunal ruled" to "he sees a handicapped name marked on `/radar`": 1** — after round 2 closes, H1's evening, then the next scan once the desk has written his block. The handicap acting on the 50 needs H2's evening plus his approve of `mode: live`, which per [F-01] and [F-12] is not given before H3 is in production. Acting on the whole ladder needs H3's evening. That is **3 evenings to full effect** in the order H1 → H2 → H3; under L43 as amended 2026-09-21 (R47) chunks that are built and checked by the same afternoon land in one deploy, so the evening count is the desk's, not this design's. The flip itself needs no deploy.

---

## L52 (a)–(d) — re-answered for v2

| Bar | Answer |
|---|---|
| (a) every number traceable | Float and cap come from the Finviz export Cobalt already fetches; headers and units are verified on the real-shape fixture (F4, F5) and the LIVE export's cell format is experiment X3 before H1. Missing values are `unknown` per [F-10], with the handling shown on the row and, when every row is unknown or a header is missing, on the degraded banner with its reason. `h`, the thresholds, the combinator and the missing rule are his ruled values, carried by KEY ([F-12]). None is modelled. |
| (b) ONE ranking authority | Per [F-07] and [F-08]: the one authority that reaches the card is `ladder_order` (`cards/radar.py:174-183`), fed by `card_score` (multiplied by `h`) and `pool_position` (the admission rank from the divided position); ADR-0009 D4's sentence "pool `last_rank` only admits" is withdrawn before H1 and `ladder_order` is not changed. The surfaces may disagree, pre-existing and now documented; each shows the raw figure beside the penalised one. In shadow neither consumer sorts on `h` ([F-01]). The two omitted consumers ([F-25]) read the same stored numbers. |
| (c) seam as a real artifact | The membership row (`raw_rank`, `handicap_factor`, `handicap` JSONB) written by the pool stage and read by the card stage (`evaluate.py:1225`); the JSONB's field set is fixed by R2-1 and R2-2 and validated by a Pydantic model; the CHECK changes, if any, by R2-2. `handicap_factor` on `aset_sizings` (H3) and the `radar_score` copy ([F-15]). |
| (d) auditable by another house | `handicap_group()` and the key change are pure functions; the inputs are stored (receipt `pool_unit` — experiment X5 — plus the membership row). The dry-run recomputes every retained day and prints `n` ([F-16]). `formula_sha256` versions the card formula; whether the pool-side code must join `FORMULA_FILES` is R2-2. |

## What this does NOT do

- It does not exclude anyone. The `exclude` block stays the only hard tool (`pool.py:292-297`).
- It does not touch conviction, dots, curves, `proposed_key`, sizing, or any grade he tapped.
- It does not add a float dot. If a computed float dot is ever proposed (F10's charter mentions float), that tribunal must state how it composes with `h`. A dot would feed conviction, and `h` would then also scale the result, which is a double count. The rule proposed here: **no float-derived dot may be promoted while `h` applies to the same group**, unless the tribunal rules the composition. [F-11: enforced by the `FACTOR_COMPUTERS` tripwire test in H3.]
- It does not change the priority rule (screens before lists), the cap, the metric or stickiness. Those are his (L53). [R2-1: whether the handicap may cross `priority` / `first_from` for a handicapped name is the round-2 question, and his.]
- It does not give a per-screen exemption. See Open 3.
- It does not replay from synthesized bars (§3).

## Open to the tribunal — round-1 outcome

1. **(c) over (a)** — ADOPTED by all three seats that ruled; grok's wording taken ([F-01]). H3 is wanted.
2. **Flat against graded `h`** — ADOPTED by all three ([F-02]); `shape:` stays a later key.
3. **The low-float morning screen** — not exempted, ADOPTED by all three ([F-03]); "in the group by construction" is experiment X8.
4. **Priority group dominance** — grok and gemini ADOPT; Fable REJECT (`first_from` is a second dominant tier the proposal never names) → **NEEDS ROUND 2 (R2-1)** with §1's division question.
5. **ASSUMED values never in production** — ADOPTED by all three ([F-05]).
6. **`handicap_cap` as a new enum value** — grok ADOPT WITH, gemini ADOPT; Fable REJECT → **NEEDS ROUND 2 (R2-2)**.

## Owner items (his by L53; read AFTER the tribunal; each carries an ASSUMED default, whose number is in the report and not in this file)

| Key (pool block `handicap:`) | What it decides | ASSUMED default |
|---|---|---|
| `float_below_m` | float threshold, in millions of shares | his R28 value (desk reading) |
| `market_cap_below_m` | market-cap threshold, in $ millions | his R28 value (desk reading) |
| `combinator` [F-12] | `any` or `all` — whether one threshold or both put a name in the group (R29: his to flip without a build) | the houses' proposed value is recorded by POINTER in the derive report's OWNER ITEMS, never here |
| `factor` | `h`, the flat multiplier | conservative: a small penalty (see report) |
| `missing` | `apply` or `skip` for `unknown` | `apply` |
| `mode` | `shadow` or `live` | `shadow` until his approve (§7) |
| shadow length | sessions in shadow before the approve | 2 sessions |

The full deduplicated owner list, including the houses' carried trade-offs, is in the derive report `## OWNER ITEMS (after the tribunal)`.

---

## First-gate experiments (L70)

Every "not checkable from reads" claim of round 1, placed before the chunk it gates. Each is run and read before that chunk's build prompt is drafted; a result is a fact, an unrun experiment is never a defect. `n` is stated on every result (L8: descriptive when n < 30).

| X | Experiment (house's own words, attributed) | Gates | Result that would change the design |
|---|---|---|---|
| X1 | grok X1 / Fable X1: count blank `Shares Float` and blank `Market Cap` on equity rows in `tests/fixtures/radar/pool-metrics.real-shape.csv`, the movers real-shape fixtures, and `data/radar-cache/2026-09-18` and `2026-09-21`, per source, per retained day. F6's counts are UNVERIFIED. | H1 (§3 [F-10]) | grok: "If any one source is mostly blank, `missing: apply` is an un-handicap on that source and the all-unknown banner must fire per source, not only per scan." Fable: blanks far above ~1 % or in ONE source → `handicap.missing` "may need to be per source". |
| X2 | grok X2 / Fable X2: re-run `decide()` on the stored 2026-09-18 RTH cache and record the marginal seat's tier; for every RTH scan after the `first_from` time, equity names on the `first_from` screen against `cap`, and all screen names against `cap`. The §2 screen counts are UNVERIFIED. | **R2-1**, then H1's dry-run wording | grok: "If that seat is a list name, a `first_from` name, a held seat, or a stickiness eviction, drop the sentence 'keeps its seat iff `p ≤ h × c`' from the dry-run summary." Fable: "If the `first_from` screen always fills `cap` by itself, O4's scenario never bites and the proposal's position mechanism is equivalent; if not, (c)'s replacement is needed." |
| X3 | grok X3 / gemini X2 / Fable X3: print `Shares Float`, `Market Cap`, `Shares Outstanding`, `Price` with the header names for ten equity rows of one live screen export and one live list export; run `_number` over every cell in the cache; compare `Price × shares-outstanding` (millions) with `Market Cap`. | H1 (§3 [F-10]) | "A currency symbol, a `B`/`M`/`K` suffix, or a unit mismatch changes `_number` and moves `handicap.float_below_m` / `handicap.market_cap_below_m` out of the export's units." (grok) |
| X4 | grok X5 / Fable X10: on current main, parse a pool note (a scratch copy) that contains `handicap:`; record freeze-with-`pool_error` versus cycle crash; then the same under H1 code (expect a parse). | H1 deploy guard (§5); every H1 rollback prompt | grok: "changes how hard the deploy-order guard must be, not the home of the keys." Fable: proves the (f) deploy guard and whether the proof command accepts a scratch path — a confirmed freeze means a ROLLBACK of H1 with the block in his note takes the pool down (Fable ESCALATE 2: the desk removes the block first). |
| X5 | grok X6: open one stored S5 receipt and see whether `pool_unit` includes `SourceSet.metrics`. | H1 (§6 replay, L57) | "If it does not, H1's receipt writer must store float and cap or the L57 claim is false after the cache expires." |
| X6 | grok X7: render a `degraded_sources` entry `{source: handicap, reason: column missing}` through the current panel joiner (`radar_panel.py:586`). | H1 (§4 header state) | "If the panel shows only the joined name, H1's panel change must render the reason." |
| X7 | grok X8: read `src/cobalt/radar/poller.py:84` for `last_rank` / `pool_position` usage; the staged poll construction is `runner.py:245`. | H1 (§1 consumer table) | "If the poller does not order by the `last_rank` it was given, the §1 poller row is wrong and stays out of the authority list." |
| X8 | grok X9: on one cached CSV of the low-float morning screen, count equity rows outside the group defined by the two keys. | H1 dry-run summary (O3) | "If any exist, delete 'in the group by construction'. The position penalty does not depend on that sentence." |
| X9 | grok X11: confirm one list cache file and one screen cache file both carry `Shares Float` and `Market Cap`. | H1 (§3, F4) | "If lists do not, every list-group name is `unknown` and the banner must say the list export has no handicap columns. F4's 'every source' is then false for lists." |
| X10 | Fable X4: tickers carried by two or more sources in ONE scan — do the two cells, or their blankness, differ? | H1 (§3 source of the values) | "If yes beyond noise, the design must state that the verdict follows `source_for[ticker]` and show that source on the row." |
| X11 | Fable X5: count (ticker, day) pairs whose group verdict changes between scans of one day under his thresholds. | H1 (O2 shape; §4 row) | "More than a handful a day → latch the verdict per open episode (carry it on `OpenMember`; S, H1a); otherwise nothing." |
| X12 | The proposal's §7 dry-run + Fable X7 + grok (i): `cobalt radar handicap-dry-run` over EVERY retained day; the `h = 1` pass reproduces the stored `radar_membership` of each day, scan by scan; the tier of every cut printed; `n` as days and scans. | delivered by H1; gates the `shadow → live` approve | Fable: "Any mismatch voids every dry-run figure shown to him." |
| X13 | grok X4 / gemini X3 / Fable X6: time one scan's `decide()` on a full-size cached scan today, plus one extra `decide()` per `config_cap` name (A) or the re-sort + `decisive` (B). `last_scan_ms` starts its clock AFTER `decide()` (`runner.py:186-187`), so production has never measured it. | **R2-2**, then H2 | grok: "If the added time exceeds his scan cadence, stamp `handicap_cap` in the post-scan replay, not inline. The O6 predicate stays." |
| X14 | grok X10 / Fable X9: for the positions on one retained screen and a grid of factors in `(0, 1]`, compare `float` division with `Decimal` division against the neighbouring integer position; unit test the exact-integer tie. | H2 (§1 division, either position) | grok: "A single flipped comparison makes the `Decimal` wording load-bearing in practice. No flip does not remove it: `handicap_factor` is `NUMERIC`." |
| X15 | gemini X1 / grok (c)(1) / Fable (c)(1): dry-run on the retained cache to see whether not-equity rows in `source.tickers` distort `effective_position` (a handicapped equity behind two funds pays `(p+2) ÷ h`). | **R2-1** (position basis), then H2 | gemini: "a distorting case would require a different not-equity handling order"; grok's wording counts positions among names that reach ranking; Fable: do not fix, the `h = 1` pass must reproduce stored membership. |
| X16 | Fable X8 (only if the enum of R2-2 position A survives): on `cobalt_dev`, re-add the four-value CHECK with a `handicap_cap` row present and read the failure. | **R2-2**, then H2 rollback file | Confirms or refutes "no rollback in the tree has ever narrowed a CHECK" and what an H2 rollback must do with stored rows. |

---

## Dissents, verbatim

Carried word for word (L37, L39): a REJECT this v2 does not follow. Both are the round-2 items; neither is settled here.

**Fable — O4 — REJECT (`pool.py:161-165`, `:209`):**
"The key tuple is `(priority[group], first, position, order, ticker)` (`pool.py:209`). The proposal names the FIRST element's dominance (screens over lists) and asks the tribunal to confirm it. It never names the SECOND: `first = 0` for the screen whose override sets `first_from`, once the clock passes it (`pool.py:161`), and the `min` at `:165` sorts on `first` before `position`. Failing scenario: an RTH scan after the `first_from` time; the `first_from` screen carries N equity names, N < `cap`; ticker ZZZZ, in the group, is last of the N. Its key is `(0, 0, N ÷ h, …)`, below every `(0, 1, …)` key whatever `handicap.factor` is → rank ≤ N, admitted, `pool_position` ≤ N on the ladder, ahead of the best unhandicapped name of every other screen. For every name of that screen "competes on its LOWERED score for the pool's 50" (R28 (b)) is false, and R28 names that screen ("or on a day scan"). Same shape whenever all screen names together number fewer than `cap`: no screen name can lose its seat to a list name at any `h`. Whether N < `cap` on real scans is X2. Replacement: ruling (c). The "third group" alternative stays rejected (a cliff)."

**Fable — O6 — REJECT (`replay/movers.py:444-447`; `0004_radar_pool.sql:42-43`; `0009_picks_missed.sql:76`):**
"`excluded_by` keeps its four pool values. A cap exclusion that the handicap decided is recorded as `handicap.decisive: true` in the membership row's `handicap` JSONB (H1's column). `replay/movers.py` copies `decisive` into each episode entry of `gate_detail.episodes` (`:452-458`); the miss line prints `config_cap (handicap)` for such a mover (`replay/line.py:119`). No CHECK changes, no enum change, no migration in H2."
"Failing sequence of the enum: H2 live on evening N writes `handicap_cap` on never-admitted episodes; evening N+1 H2 is reverted (L54: `git revert`); the replay of day N now runs code whose `EPISODE_EXCLUSIONS` (`movers.py:102`) lacks the value → `ReplayInputError` (`:444-447`) → that night's miss line FAILS. Restoring the old CHECK in a rollback file would also need a data rewrite first (X8; no rollback in the tree has ever narrowed a CHECK — `0004` / `0008` / `0009` rollbacks checked). A lossless rewrite needs the fact stored elsewhere anyway — then the enum is the second copy (L3). Price of the replacement: two edits (`movers.py` episode JSON, `line.py:119`), minus five enum sites, two CHECKs and one migration. The proposal's stated cost of the alternative ("a second grouping key", citing `line.py:97`) rests on a wrong fact: `:97` counts CARD gates only (WRONG FACTS 1)."

**Grok — O6 — the opposing position, verbatim, on why a `winners` test is wrong (carried beside the dissent so round 2 sees both):**
"The boolean loses the miss line, which groups by one column. The proposal's `winners` predicate (`pool.py:333`, before stickiness) mislabels a name stickiness would have evicted anyway."
"Scenario: 11:00 ET, `pool.cap = 3`, `stickiness_scans = 3`. Open members OLD1 and OLD2 occupy raw positions 1 and 3. LOWF is the only newcomer, raw position 2, and `h < 2/3` pushes `2/h` past OLD2's position 3, so the order is OLD1, OLD2, LOWF. LOWF is inside `winners` and is the only newcomer. STICKY is outside, `below_cap_streak = 1`, and stickiness evicts LOWF (`pool.py:366-375`). With LOWF's `h` set to 1 the order is OLD1, LOWF, OLD2; LOWF is still the only newcomer and is evicted again. `winners`-membership would say `handicap_cap`. Full admission says `config_cap`."

---

*Derived 2026-09-22 by the Fable seat under `40-float-handicap-tribunal-derive.md`; nothing built, launched or committed by that seat. Round 2 (R2-1, R2-2) is the next lawful step; Astra reads this file when its meter returns (R13, 2026-09-22).*
