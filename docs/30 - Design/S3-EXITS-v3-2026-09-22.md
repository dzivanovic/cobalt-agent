# S3 EXITS — v3 (derived 2026-09-22, second derive)

**ASTRA PENDING (R13)** — Astra did not rule round 1 or round 2 (`astra: METER — proceed on three`, 09-22 R13); **Astra reads the FINAL on Sat 2026-09-26.** Every `[R2F-nn]` tag below carries this mark.

**Derived from** v2, `docs/30 - Design/S3-EXITS-v2-2026-09-22.md` (committed `cda7bf19`), by the second derive seat `s3-exits-tribunal-derive-r2-0922` (`claude-opus-5-5`, row R36 of `cto-2026-09-22.md`; round 2 authorized by R62). This seat ruled nothing in either round and recommends nothing (L37). It builds nothing and commits nothing.

**Round-2 rulings folded (every seat that ruled round 2):**
- Round-2 hub report (collate + file-check): `docs/40 - DevDocs/reports/s3-exits-tribunal-r2-2026-09-22.md` — `S3 EXITS TRIBUNAL R2 DONE · grok: HARNESS · gemini: TRIBUNAL R2: BUILD · astra: METER — proceed on three · houses that ruled: 1 of 3 · converged: 2 of 3 · ESCALATE: 4` (committed `0017e34d`).
- Gemini, in full: `scratch/tribunal-bars-0920/s3-exits-tribunal/r2/gemini-ruling-r2.md` — `TRIBUNAL R2: BUILD`.
- Grok: **did not rule round 2** — HARNESS at launch (`Error: Operation not permitted (os error 1)`, before any read); under L67 a house that produced no ruling has not used a round. Grok's ROUND-1 texts (v2) stand as its position.
- Fable seat (row R36: `Fable seat: yes`, on `claude-opus-5-5`), in full: `docs/40 - DevDocs/reports/s3-exits-tribunal-fable-r2-2026-09-22.md` — `TRIBUNAL R2: BUILD AFTER derive folds R2-2 wording: CAS running check, locked stop edit, fill-price open risk` (committed `9adf4b38`); its 5 WITHDRAWN round-1 sentences are honoured (never folded).
- Astra: METER (above).
- **Fold table** (every change below, with its reason): `docs/40 - DevDocs/reports/s3-exits-tribunal-derive-r2-2026-09-22.md` `## Fold table` (rows `R2F-nn`). v2's own fold table stays at `docs/40 - DevDocs/reports/s3-exits-tribunal-derive-2026-09-22.md` (rows `F-nn`).

**How to read v3.** v3 is v2 whole. Every `[F-nn]` tag is v2's (round 1) and unchanged. A v2 point that round 2 settled is REPLACED in place and tagged `[R2F-nn]`; a point round 2 did NOT settle is replaced by, or points to, the **OPEN FOR DEJAN — R2-2** block in §3. Text quoted after "*seat* (item), verbatim:" is that seat's wording word for word; the derive neither edits nor blends it. `[user data]` spans are redacted (L32). Owner items O1–O22 in §12 (O21, O22 new in round 2).

## Status after round 2

| Item | Seats that ruled round 2 | Status | Gates |
|---|---|---|---|
| R2-1 — gemini's DO NOT BUILD against v2 | gemini (ADOPT A, `DO NOT BUILD withdrawn`) · Fable seat (ADOPT A, `DO NOT BUILD withdrawn`) | **CONVERGED** `[R2F-01]` — the dissent is spent; no round 3 is owed | whether a round 3 is owed: **none** |
| R2-2 — legs correction / running shares / current row / cache / `direction` / double tap / FILLED stop-edit read | gemini (ADOPT F) · Fable seat (NEITHER, own replacement) | **OPEN FOR DEJAN** `[R2F-04]` (§3 block) | **C1's `legs` DDL** (running column(s), index, view) and **C2** wait on his answer |
| R2-3 — which migration set carries M1 | gemini (ADOPT C) · Fable seat (ADOPT C) | **CONVERGED** `[R2F-09]` | C1's migration: settled (one `db_migrations` file + `.rollback.sql`) |

**Status: NOT FINAL until he rules R2-2.** No round 3 (no house said `DO NOT BUILD`, L39). The tribunal's rounds are spent on R2-1 and R2-3; R2-2 goes to Dejan as one A/B/C, never a vote. C1 and C2 are not built before his answer; the first-gate experiments that do not turn on R2-2 may run (L72; desk lane).

---

(Proposal header, kept:) Scope: ladder `S3-P1 · F11` + `S3-P2 · F22`. PROVEN = read in the file. UNPROVEN = could not be checked from reads (L70). **ASSUMED** = a value or behaviour that is his and is not in the ladder, Charter, settings or code (L53). It goes to §12 as an owner item and is never treated as settled. `[F-18]` The proposal's `file:line` refs were taken on main at `be58530`; main has moved since (hub file-check: `67bc19b`, then `1a3f5cf`; `ff63e95` at this derive). The staged excerpts matched the proposal's line numbers (grok WRONG FACTS #1); corrected citations are tagged where a ruling corrected them.

Out of scope: F14 (DRC note; its template is his sitting, `cto-2026-09-21.md` R48/R52) and F15 (S3-P3). §7 and §10 name only the seams they need.

---

## 1. FACT BASE

| # | Claim | Where | |
|---|---|---|---|
| F1 | Eight states. The edge table is the single authority: TRIGGERED→{FILLED, PASSED, EXPIRED}, FILLED→{CLOSED}, and the four terminal states have no exits | `cards/models.py:37-49`, `:92-114` | PROVEN |
| F2 | The stop is editable only in WATCH and FILLED. The key is editable only in WATCH | `cards/models.py:120`, `:126` | PROVEN |
| F3 | `transition()` does four things: gates on the session (it refuses in `market_reset`), checks the edge under a row lock, refuses ARM when the card is unsized, and folds pending stop edits into the evidence. It writes the ledger row and the cached state in one transaction | `cards/store.py:239-371` | PROVEN |
| F4 | `fill()` is THE one path to FILLED. A radar card goes the strict way (it must already be TRIGGERED). A manual card gets a one-click walk, actor cobalt. The F3 pick is written under a savepoint | `cards/store.py:429-530`, `:486` | PROVEN |
| F5 | The sheet's fill-recompute keeps the same grade dollars and the same stop, then uses shares = ⌊risk$ ÷ \|fill−stop\|⌋. There is a typo guard: `max_fill_distance_pct` (config, 5). The warning fires at `distance_change_pct ≥ 25`, and that 25 is a **hard-coded constant** | `aset/engine.py:267-312`, `:43`; `configs/dev/aset.yaml:53` | PROVEN |
| F6 | `mark_filled` calls `CardStore.fill()` with the recompute in the evidence. It then UPDATEs `aset_sizings.actual_fill`, `recomputed_shares`, `recomputed_used_risk`, `share_delta`, `distance_change_pct` and `filled_at` `[F-14: in a SECOND transaction — see F36]` | `aset/store.py:185-257` | PROVEN |
| F7 | `/fill` works only from the **manual sheet form**. It rebuilds the `SizingResult` from the posted form fields, and it needs `orig_timestamp` + `card_row_id` from `/size` | `aset/web.py:1046-1101` | PROVEN |
| F8 | A radar card reaches FILLED only through `/card/{id}/move` → `fill()`, and the evidence is just `{"via":"aset.sheet"}`. **No fill price and no shares are recorded.** The ladder says S2 fills radar cards "via the existing fill-recompute", but that path is not wired for radar cards `[F-14: "only" is false for CALLERS of fill() — see F35; the radar refusal stands]` | `aset/web.py:1181-1209` | PROVEN |
| F9 | The radar panel (`/radar`) has routes for key, dot, promote and release only. It has NO arm, triggered, fill, leg or stop route. Its IN-TRADE block shows the stop and 1R/2R and nothing else. NOTES says "no notes source wired to radar cards (S3)" | `aset/web.py:1294-1390`; `aset/radar_panel.py:978`, `:1001` | PROVEN |
| F10 | Radar card creation is automated. The soft attestation check refuses the **key tap** (radar) and `/size` (manual) when the attested `.htk` disagrees or nothing is attested | `aset/web.py:1314`, `:985`; `daymode/match.py:80-117` | PROVEN |
| F11 | The attestation lives on `day_modes` (`attested_sheet`, `attested_at`) | `daymode/store.py:132-169` | PROVEN |
| F12 | Stop edits go to `card_stop_edits` (from, to, actor, in_state). They are not a transition; the next transition folds them. In FILLED, the recompute changes open risk and leaves size alone `[F-29: the FILLED recompute reads the planned `shares` column — hub file-check HOLDS; see §5]` `[R2F-07 · ASTRA PENDING (R13)]` (round 2, Fable seat, read by the derive at `cards/store.py:677-680`, `:713-721`, `:729-739`: the FILLED recompute also takes per-share risk from the card's PLANNED `entry`, and the function runs on an autocommit connection, so its `FOR UPDATE` holds no lock past its SELECT — see §5 and the R2-2 block) | `cards/migrations/0002_card_stop_edits.sql`; `cards/store.py:643-740`; `aset/engine.py:126` | PROVEN |
| F13 | A radar card stores `structural_stop` + `trigger_price` as immutable formation evidence. `entry`/`stop` are the live inputs. `last_price` = the last closed working-TF bar close. A reset has no marker column | `db_migrations/0007_radar_cards.sql:3-8`, `:36-38`; `radar/evaluate.py:502`, `:776` | PROVEN |
| F14 | There is at most one open radar card per (member, def, direction), and FILLED counts as open | `0007_radar_cards.sql:90-92` | PROVEN |
| F15 | The radar resident expires radar cards from WATCH, ARMED and TRIGGERED via `expires_at`/avoid | `cards/expire.py:71`; `radar/evaluate.py:1351`; `cards/store.py:1055` | PROVEN (predicate) · the resident's caller for TRIGGERED is X5 `[F-10]` |
| F16 | No code moves a radar card to TRIGGERED. The S4 detector (F9) is owed. Today TRIGGERED is only his sheet button `[F-14: also `cobalt cards move <id> TRIGGERED` — see F35]` | grep `TRIGGERED` in `radar/`, `cards/` | PROVEN |
| F17 | Health pills are computed on FILLED radar cards from an entry snapshot and `card.stop` | `radar/evaluate.py:780-797` | PROVEN |
| F18 | Two ATRs are stored. `atr_working` = ATR(working TF, 14) Wilder, and it is in the receipt observations. The daily ATR(14) is an input of `htf_level_proximity` | `radar/evaluate.py:437-444`, `:467-475`, `:568`; `radar/anatomy/daily.py:122-126` | PROVEN |
| F19 | F12's counterfactual R: risk = \|card entry − stop in force at trigger\|, `cf_r = (exit − fill)/risk × sign`. There is one formula, shared | `replay/cards.py:223-288`; `replay/models.py:185-213` | PROVEN |
| F20 | `vaultwrite.VaultWriter` provides `create_if_absent`, `upsert_unit` (marker-bounded, human wins, override rows), `upsert_region` (frontmatter via `frontmatter_span`) and `restore` | `vaultwrite/writer.py:559`, `:644-680`, `:895`, `:1066` | PROVEN |
| F21 | **Slice-2's trade-note writer exists**: `prefill/trade_note.upsert_trade_note`. Cobalt owns five keys (date, symbol, direction, stop_price, entry_price). exit_price, entry_time, exit_time, profit_loss, trade_def and RVOL are his. The body is his template's `[F-34: a code copy of it — see §7]` | `prefill/trade_note.py:1-41`, `:118-169` | PROVEN |
| F22 | It is called at **`/size` (card sizing)**, not at FILLED, and only on the manual sheet. Radar cards never create a note | `aset/web.py:1019-1021` | PROVEN |
| F23 | Path is `1 - Trading/2 - Trades/Trade-%Y-%m-%d %H-%M-%S -{ticker}.md` | `configs/cobalt/prefill.yaml:8`, `:14` | PROVEN |
| F24 | The DRC prefill finds a card's note by **nearest filename timestamp to the card's `created_at`**. For a radar card, created at formation and filled later, it would miss | `prefill/drc.py:101-121` (tolerance `FILL_MATCH_TOLERANCE_SECONDS = 30`, `:67` — read by the derive, settles grok X7) | PROVEN |
| F25 | His template is `5 - Templates/Individual Trade Template.md`, with the same 12 frontmatter keys as `FIELD_ORDER`. His daily table selects `symbol, profit_loss, strategy, entry_time, direction` from `1 - Trading/2 - Trades` where `file.cday = this.file.day` | `5 - Templates/Individual Trade Template.md`; `5 - Templates/Daily.md:49-54` | PROVEN (text) · lights in Obsidian UNPROVEN → X2 |
| F26 | The daily table selects `strategy`, but new notes carry `trade_def` (ADR-0008 D4) | `Daily.md:50`; `trade_note.py:30-41` | PROVEN · his template → O12 |
| F27 | **There is no inbound DM path.** `notify/mattermost.py` is outbound HTTP only, "no websocket". The kill-phrase matcher is waiting "for the Mattermost DM listener the moment one lands" | `notify/mattermost.py:6-11`, `:130`; `jobs/killswitch.py:106-116` | PROVEN |
| F28 | `legs` and `fills` are declared USER-side but not built | `db_migrations/placement.py:103-111` | PROVEN |
| F29 `[F-15]` | Fable (a), verbatim: "`0013` is TAKEN by `setups/seven-0921` (`61a283f`, `0013_tunables_slug_nullable.sql`). The next free number is `0014`." | `git log main..setups/seven-0921` (derive read: `61a283f` adds `0013_tunables_slug_nullable.sql`; `bars/chunk-2-0920` `02d67a6` adds `0012`; both unmerged at this derive) | PROVEN |
| F30 `[F-16]` | Fable (a), verbatim: "At 13:4x ET 2026-09-22 both branches exist: `setups/seven-0921` is 17 commits ahead of main; `s2/stale-marker-0921` (`cddf32c`) is 0 commits ahead — already in main." | hub file-check: HOLDS, `setups/seven-0921` now 18 ahead | PROVEN |
| F31 | The mock shows TRIGGERED with three numbers. IN-TRADE has an editable stop (YOURS amber + delta + ↺ reset), ½ off / ⅓ off / flat / reset compounding off running shares, and a fill recompute that warns at >20 % | mock `card-spec.md:18`, `:53-62`; `decisions.md:25`, `:29`, `:33` | PROVEN |
| F32 `[F-35]` | Fable (j), verbatim: "Charter :253 '#16 single fill at MVP, partials post-MVP' answers the mock's question about partial ENTRIES; F11 :139-142 specifies scale-out EXIT legs and 'running shares 0 → CLOSED'. Reading: ONE entry fill (the partial UNIQUE on `kind = entry`), MANY exit legs. Charter :254 keeps '#2 scale-out presets' OPEN: the preset SET (which fractions exist) is his — O9." [Fable's O9 = v2 **O14**; his confirmation of the reading = **O13**] | `MVP-CHARTER-v0_2.md:253-254` (derive read), F11 `:135-147` | PROVEN (text) · reading stated by all three seats; his to confirm (O13) |
| F33 `[F-17]` | Fable (a), verbatim: "The ladder carries the ATR text (`SPRINT-LADDER-v0_1.md:610` 'drift warning scaled to ATR (his 09-03 defect)', `:621` 'ATR-scaled drift'); what has no source is the '09-03 defect' origin only." | ladder `:610`, `:621` (hub: HOLDS) | PROVEN (text) · the 09-03 origin UNPROVEN |
| F34 `[F-14]` | Fable (a), verbatim: "`aset/daily_note.py:229-248` `save_fill_update`: the manual `/fill` writes a FILL UPDATE unit into the DAILY note (`aset/web.py:1103`), keyed on the original card timestamp. The radar key tap writes the card block into the daily note (`save_card`, `aset/web.py:1336`). Neither is a trade-note writer, both are vault writers of the fill/card. PROVEN." | derive read: `aset/web.py:1006`, `:1103`, `:1336` | PROVEN |
| F35 `[F-14]` | Fable (a), verbatim: "`cards/cli.py:83` `cobalt cards move <id> FILLED` calls `fill()` with no price, evidence `{"via": "cobalt cards move"}`: a third caller of the one fill path, and a second way (with the sheet button, `aset/web.py:640`) to reach TRIGGERED. PROVEN." | hub file-check: HOLDS (`cards/cli.py:60-100`) | PROVEN |
| F36 `[F-14]` | Fable (a), verbatim: "`mark_filled` is TWO transactions: `fill()` commits (`cards/store.py:519`), then a second connection UPDATEs the fill columns (`aset/store.py:230-250`). PROVEN." | hub file-check: HOLDS (FC1; grok (b), gemini (b)); round-2 hub C8, C9 HOLD | PROVEN |
| — `[F-19]` | A second writer of `card_transitions`/`state` exists outside `transition()`: `create_state` (`cards/store.py:560-629`, `:607`, `:622`). Whether it can move a LIVE card is not settled (grok X8: unread; Fable: "genesis … not a fill path"; hub FC8: UNVERIFIABLE FROM READS) | hub file-check | UNPROVEN → X9 |

---

## 2. STATE MACHINE (proposed): the edge table is unchanged

The edges already exist (F1). F11 adds **rows and actors, not edges**. Every move is his. Cobalt-actor moves stay limited to expiry (F15) and the existing manual one-click hops (F4).

| Transition | Actor | Trigger | Stored row(s), same transaction | Inputs stored (L57) |
|---|---|---|---|---|
| ARMED → TRIGGERED | YOU (panel "TRIGGERED" tap) until the S4 detector, then COBALT | tap | `card_transitions` | `last_price` + its bar ts |
| TRIGGERED → FILLED | YOU | "FILLED @ [price] [shares]" tap, or DM line | `card_transitions` (via `fill()`) + entry `legs` row + `aset_sizings` fill columns (the F6 cache) + pick (F4) — ONE transaction per `[F-22]` below | price, its source (`last_poll`/`typed`/`dm`) and as-of; shares; stop in force; ~~`structural_stop`~~ `[F-28]`; ~~drift receipt (§4)~~ `[F-27]`; attested sheet + `day_modes` id + `sheet_mismatch` `[F-05]` |
| TRIGGERED → PASSED | YOU | PASS tap | `card_transitions` | — |
| WATCH/ARMED/TRIGGERED → EXPIRED | COBALT | existing radar expiry (F15). No new timer `[F-10]`; whether the resident's caller passes TRIGGERED cards is X5 | existing | existing |
| FILLED (leg) | YOU | ½ · ⅓ · flat · typed, or DM "out" | `legs` exit row; **no transition** (same principle as stop edits, F12) | running shares as stored or derived — **OPEN FOR DEJAN — R2-2** (§3) `[R2F-04 · ASTRA PENDING (R13)]`; preset, price source + as-of, flag |
| FILLED → CLOSED | YOU (his leg that brings running to 0) | the zero-running leg | `card_transitions` evidence `{leg_id}` + that leg, one transaction | legs ids |

Refusals, all loud: a leg on a non-FILLED card; a leg whose shares exceed running; a preset result of 0 shares ("½ of 1 is 0 — use flat or type"); a fill in `market_reset` (the existing gate). A fill on a radar card that is not TRIGGERED stays refused by `fill()`'s strict path. Whether to relax that before S4 is owner item O7 (priced at `[F-21]`, §12).

**The one fill path (L3).** `[F-22]` Grok (b), verbatim:

> "`fill(conn=...)` follows `transition()`'s rule: commit only if it opened the connection. `mark_filled` opens one connection, passes it in, and before commit inserts the entry leg and runs the fill-column UPDATE. `/fill` and the panel route both call `mark_filled`. `SizingResult.from_card(row)` is the only rebuild. `/fill` stops calling `_parse_input` + `compute_sizing` on the posted form. The form contributes the typed price and the typed share count. `/card/{id}/move` no longer calls `fill()` for FILLED. A FILLED tap with no price is refused. The CLI fill goes through `mark_filled` or it is refused."

> "`from_card` must copy `entry`, `stop`, `per_share_risk`, `shares`, `risk_budget`, and `direction` off the row. A default for any of those is a second sizing. Parity is X9." [grok's X9 = v2 **X10**]

`[R2F-01 · ASTRA PENDING (R13)]` Round 2 (R2-1, CONVERGED): gemini and the Fable seat both ADOPT A — v2's text above stands as written, and both write `DO NOT BUILD withdrawn`. Gemini (R2-1), verbatim: "The single-transaction orchestrator correctly closes the seam between `fill()` (`cards/store.py:429`) and the fill-column UPDATE (`aset/store.py:233`), and v2 acknowledges the defect of `record_stop_edit` reading the stale `shares` column (`cards/store.py:720`)." (hub file-check: both HOLD.)

`[R2F-03 · ASTRA PENDING (R13)]` Build note carried for the C1 prompt — NOT a design change and not a fold of a house's wording; the Fable seat's alone, UNCHECKED by a hub — read by the derive at `db.py:199` (`psycopg.connect(dsn, autocommit=True)`) and `aset/store.py:230` (`with self._connect() as conn:`, no `autocommit = False`). Fable seat (R2-1), verbatim: "BUILD NOTE for the derive (no design change): the C1 prompt names `conn.autocommit = False` on `mark_filled`'s connection and rollback on any exception, as `transition()` does (`:272`, `:365-368`), because `db.py:199` opens autocommit. `[F-40]`'s one-transaction case is the acceptance test; v2's X7 is its run." (The derive notes: the one-transaction case of `[F-40]` is exercised by X1; X7 is the two-tap run.)

(Proposal sentence, kept:) There is no second recompute. The hard-coded `FILL_DISTANCE_WARNING_PCT` (F5) **dies** and is replaced by his setting (§4).

This closes the author's ESCALATE 1 (radar fills record no price): the move route stops filling, and a FILLED tap without a price is refused. The alternative shape — one new orchestrator function that also lets C3 ∥ C4 ∥ C5 build in parallel (Fable (b), (l)) — is owner item **O17**.

---

## 3. THE `legs` TABLE (`"user"`, L32)

One table covers the entry fill and the exits. `[F-02]` Grok (Q2), verbatim: "One `"user".legs` table covers the entry and the exits. `fills` stays in `DECLARED_TABLES` as USER and is not built. Do not strike it." (Striking it — gemini, Fable — is owner item **O16**.)

| Column | Type | Note |
|---|---|---|
| id | bigint identity PK | |
| user_id | int NOT NULL DEFAULT GUC FK `"user".traders` | tenancy, same as `0007:120` (the GUC is confirmed on `cobalt_dev` by X4; grok: "Do not invent the GUC name") |
| card_id | bigint NOT NULL FK `"user".aset_sizings` | |
| seq | smallint NOT NULL | 0 = entry, 1..n = exits; the index and the current-row rule are **OPEN FOR DEJAN — R2-2** (below) `[R2F-04 · ASTRA PENDING (R13)]` |
| kind | text CHECK ∈ {entry, exit} | partial UNIQUE: one `entry` per card (single fill at MVP, F32) |
| shares | int NOT NULL > 0 | his broker's shares |
| price | numeric(14,4) NOT NULL > 0 | |
| at | timestamptz NOT NULL | when he tapped or sent |
| flag | text CHECK ∈ {estimated, confirmed} | |
| price_source | text CHECK ∈ {last_poll, typed, dm} | |
| price_asof | timestamptz NULL | the `last_price` bar ts when `last_poll`, else NULL |
| preset | text NULL CHECK ∈ {half, third, flat, typed} | exits only |
| running columns | int NOT NULL | **OPEN FOR DEJAN — R2-2** `[R2F-04 · ASTRA PENDING (R13)]`: `running_before, running_after` (G) · none, running derived (F) · `running_before` only, running derived, posted-count check (N) |
| stop_in_force | numeric(14,4) NOT NULL | the stop in force at that leg (R38) |
| ~~cobalt_stop~~ | — | `[F-28]` struck — Fable (f), verbatim: "Cobalt's stop is the card's `structural_stop` (immutable, `0007:6-8`); the gap per leg is `stop_in_force − structural_stop`, computed at render. `stop_in_force` stays on every leg as the stored input." (Keeping it per leg, NULL on a manual card — grok (f) — is owner item **O20**.) |
| direction (entry row) | — | **OPEN FOR DEJAN — R2-2** `[R2F-04 · ASTRA PENDING (R13)]`: on the entry leg (G) vs read from the card row (F, N) |
| source | text CHECK ∈ {panel, sheet, dm} | |
| corrects | bigint NULL FK legs(id) | a correction is a NEW row; the old row is never updated |
| session, account_mode | text NOT NULL | same stamps as the card |
| day_mode_id, attested_sheet, sheet_mismatch | FK / text / boolean NOT NULL | entry row only `[F-05]` (§8) |

Append-only via the existing `"user".refuse_row_update()` trigger (`0007:105-111`). A view `"user".legs_current_v` gives the current row per (card, seq) — **which row is current is OPEN FOR DEJAN — R2-2** `[R2F-04 · ASTRA PENDING (R13)]`.

**Migrations.** `[F-25]` Grok (c), verbatim: "Numbering from the packet: FORWARD ends at `0011` (`33-migrations.excerpt.sql`). `bars/chunk-2-0920` adds `0012` (`02-greps.txt:546`). `setups/seven-0921` adds `0013` (`:793`). The next free numbers are 0014 and 0015 only after both are on the base. Taking 0012 now collides with bars." The number is the desk's at the L68 gate. `[R2F-09 · ASTRA PENDING (R13)]` **Which migration set carries each change — R2-3, CONVERGED (gemini ADOPT C, Fable seat ADOPT C):** Fable (round 1, R2-3 option C), verbatim: "the precedent is that a numbered `db_migrations` file alters them. M1 = `db_migrations/0014_legs.sql` + `.rollback.sql`, carrying `legs`, `legs_current_v`, `card_stop_edits.kind`, `aset_sizings.trade_note_path` and the drift fields". (The number `0014` inside that verbatim text is not v3's: the migration NUMBER is the desk's at the L68 gate. v3 states the SET — one `db_migrations` file and its `.rollback.sql`, listed in `FORWARD` / `REVERSE`; the module sets `aset/migrations/` and `cards/migrations/` are not touched by S3.) Hub file-check on gemini's failing scenarios against A and B: `greps.txt:318` (no `.rollback.sql` in either module set) HOLDS; `aset/web.py:508` (`ensure_schema()` in request handlers) HOLDS. The fresh-DB precondition M1 shares with `0007:27` is X22.

**Corrections and running shares — `[R2F-04 · ASTRA PENDING (R13)]` OPEN FOR DEJAN — R2-2.** Round 2 did not converge (gemini ADOPT F; the Fable seat NEITHER with its own text; grok did not rule round 2 — HARNESS — and its round-1 Option G stands as its position). The derive settles nothing; he rules. The block, with every position verbatim:

> **OPEN FOR DEJAN — R2-2 — how Cobalt records a correction and counts the shares you still hold.**
>
> **F — v2 Option F, adopted by gemini (round 2).** *Option F — Fable (Q3, round 1), verbatim:* "`UNIQUE (card_id, seq) WHERE corrects IS NULL` (one ORIGINAL row per seq; corrections are excluded from the index). `legs_current_v` = for each (card_id, seq) the row with the greatest id. A correction row copies `seq` and `kind` from the row it corrects; the store checks that under the card row lock (`SELECT … FROM aset_sizings WHERE id = %s FOR UPDATE`, the same lock `transition()` takes, `cards/store.py:275-278`) because a cross-row CHECK cannot. `"user".refuse_row_update()` (`0007_radar_cards.sql:105-111`) is attached, as on `card_dots`." — and Fable (c), verbatim: "`running_before int NOT NULL` — the running count the preset was computed from, a STORED INPUT (L57). No `running_after` column: running = entry-leg shares − Σ current exit-leg shares, computed in the store under the card row lock, never chained row to row. CHECK on an exit row: `shares > 0`; the store refuses a leg whose shares exceed the running count it just computed. CLOSED is written in the same transaction as the leg that brings running to 0." · "Corrections: a correction may change price, time, flag, source; it may change shares only while Σ current exits ≤ entry shares AND (on a CLOSED card) the running count stays 0 — otherwise it is refused naming the rows, because CLOSED has no outgoing edge (`cards/models.py:106`) and a correction that reopens a position would make the card's state lie." — FILLED stop-edit form (v2 §5, Fable round 1), verbatim: "In FILLED, `record_stop_edit` recomputes open risk from the RUNNING share count read from `legs_current_v` (entry shares − Σ exits) under the card row lock … C2 changes that one argument." **WITHDRAWN by its author in round 2** (Fable seat `## Withdrawn from round 1` #1, #2, #3): "is attached, as on `card_dots`", "C2 changes that one argument", and the stop-edit sentence as a description of a one-argument change — a withdrawn sentence is never folded; if he rules F, those three sentences are not built as written.
> Gemini's sub-point answers (round 2), verbatim: "(a) index: F — `WHERE corrects IS NULL` enforces one original without an `is_current` flip, respecting `schema.excerpt.sql:105`." · "(b) current row: F — `MAX(id)` per `(card_id, seq)` is cleanly queryable without backwards-link traversal." · "(c) running shares: F — computed running avoids Option G's failure in scenario 1 (entry 100 corrected to 80 leaves an exit of 50 with stale `running_before=100` and `running_after=50`)." · "(d) FILLED stop-edit share read: F — `legs_current_v` gives the live position, replacing the stale `aset_sizings.shares` read at `cards/store.py:720`." · "(e) cache after entry-leg correction: F — rewriting the cache in the correction's transaction keeps it synchronized." · "(f) where `direction` is read: F — `aset_sizings.direction` is never updated (`greps.txt:101`), so duplicating it on `legs` is redundant." · "(g) two concurrent ½ taps: F — serialized under the card row lock (`cards/store.py:277`); tap 1 reads running 100 and writes 50, tap 2 reads running 50 and writes 25 (X7)."
> Reason, one sentence: derived running leaves no stored count to go stale after a correction, and the index needs no flip under the append-only trigger (`0007_radar_cards.sql:105`, hub HOLDS) — but gemini's (c) scenario numbers DO NOT HOLD against grok's own text (hub `## Checked against the files`, R2-2(c): grok's scenario is exit 40 / stale 60, not 50 / 50).
>
> **N — the Fable seat's NEITHER (round 2), verbatim:** "`legs` stores `running_before int NOT NULL` — the running count the tap was computed from, a stored input (L57) — and NO `running_after`. Index: `UNIQUE (card_id, seq) WHERE corrects IS NULL` (one original per seq; `corrects` is written at INSERT and never updated, so the predicate needs no flag). `"user".legs_current_v` = per `(card_id, seq)` the row with the greatest `id`. Running = entry-leg `shares` − Σ current exit-leg `shares`: ONE store function, the only running read (L3), called by the leg writer, the correction writer, the CLOSED check and the FILLED stop edit. Every one of those writes runs in ONE transaction on a connection with `autocommit = False` (`db.connect()` opens autocommit, `db.py:199`) under `SELECT … FROM aset_sizings WHERE id = %s FOR UPDATE`. An exit tap posts the `running_before` the panel computed from; the store refuses it, naming both counts, unless it equals the running computed under the lock — a stale or duplicate tap is refused, never re-applied to the smaller count. A correction row: `corrects` = the CURRENT row of that seq (refused otherwise, naming the current id); `seq` and `kind` copied from it; it may change price, `at`, flag, `price_source` and shares; a shares change is refused if Σ current exits would exceed the entry shares, or if it moves a CLOSED card's running off 0; a correction that brings a FILLED card's running to 0 writes FILLED → CLOSED in the same transaction, evidence `{leg_id}`, exactly as a leg would. A correction of the entry leg's price rewrites the `aset_sizings` fill cache (`actual_fill` and the `compute_fill_recompute` figures) in the same transaction; the fill transition's evidence keeps the fill-time figures (`aset/store.py:220-226`). `direction` and `entry` are read from the card row. FILLED stop edit: `record_stop_edit` reads running from that function inside the locked transaction; per-share risk is `|entry-leg price − new stop|` (no entry leg: `actual_fill`; neither: `entry`), side-checked against that price; the FILLED UPDATE writes `stop`, `per_share_risk`, `used_risk` and never `shares` (the planned count stays the plan). A FILLED card with no entry leg (filled before C1) reads `recomputed_shares`, else `shares`, and the render names which."
> Its sub-points: (a) F · (b) F + G's head check · (c) F · (d) neither · (e) F · (f) F · (g) G's outcome (one refused).
> Reason, one sentence: today's `record_stop_edit` holds no lock past its SELECT (`cards/store.py:677-680` on the autocommit connection of `db.py:199`; comment `cards/store.py:350`), passing the held count writes it into the planned `shares` column (`aset/engine.py:153-157` → `cards/store.py:730-735`), and a duplicate ½ tap recorded as a second exit would be an exit he never made — UNCHECKED by a hub, read by the derive at each of those lines.
>
> **G — grok's Option G (round 1; grok did not rule round 2 — HARNESS, not a used round, L67), verbatim:** "A correction is a new row with the same `(card_id, seq)` and `corrects` = the current head's id. No UPDATE and no `is_current` flag. There is no UNIQUE `(card_id, seq)` constraint. The head is the row of that seq that is not the target of another row's `corrects`. The writer takes `aset_sizings` FOR UPDATE, inserts only against that head, and refuses the insert unless `running_before` and `running_after` still meet the neighboring current legs. A share change on a non-tail leg is refused. A correction that moves the tail's `running_after` off 0 on a CLOSED card is refused. Price, flag, `price_source`, and the two stops may be corrected on any current leg." — FILLED stop-edit form (v2 §5), verbatim: "In FILLED, `record_stop_edit` passes `in_trade_shares` = the tail current leg's `running_after` when an entry leg exists, otherwise the `shares` column (legacy cards with no legs)." — and "`sign` comes from `direction` stored on the entry leg."
> Reason, one sentence: "The view invariant is a writer rule under the card lock, not a constraint" because "A partial unique index needs an `is_current` flip, which `refuse_row_update()` forbids" (`r1/grok-ruling.md:23`), with the two-tap outcome "one refused, running consistent" (`:268`) — that index objection is contradicted by the round-2 hub's C3 (HOLDS on its readable half: `WHERE corrects IS NULL` needs no UPDATE of an old row; DB acceptance = X4).
>
> **What he chooses between, in his terms (the desk brings ONE line each; no seat, and not this derive, recommends):**
> - **A = F (gemini).** A second ½ tap sent from a stale panel or a double-click is recorded as a second exit of 25 shares (gemini's own (g) text) — your broker holds 50, Cobalt says 25. Three sentences of F are withdrawn by their author and must be re-worded at build.
> - **B = N (Fable seat).** That second ½ tap is refused ("panel said 100, now 50 — tap again"); a stop move while in the trade prices open risk from your fill price on the shares you still hold and never rewrites the planned share count; one more stored column and ≈25 more lines in C2 (the seat's estimate, not measured); a correction that takes you to 0 closes the card (O22).
> - **C = G (grok, round 1).** A correction to your entry's share count after the first exit is refused (entry typed 100, broker filled 66: it stays 100); every exit row carries a stored running count; a card filled before C1 keeps pricing a stop move on the planned share count.
> - **Waits on his answer:** C1's `legs` DDL (the running column(s), the index, `legs_current_v`) and all of C2. No house named a default; nothing is built for R2-2 until he rules. X4 and X7 run on `cobalt_dev` before C1 / C2 whichever he keeps.

**Compounding.** Presets run off `running_before` (DAS semantics, mock decision 13). **Rounding (ASSUMED, O1):** ½ = ⌊running/2⌋ and ⅓ = ⌊running/3⌋. flat = running. typed = his count ≤ running. What his broker does is his fact, not ours. (The preset SET itself is his — O14.)

**Estimated vs confirmed.** A mid-trade preset tap takes `last_price` → `estimated`, with no keystroke (Charter test 2). A typed price or a DM price → `confirmed`. The closing leg's price is his (Charter F11): the flat control shows the prefilled price in an editable field and commits `confirmed` only on his explicit ✓. A plain flat tap with the untouched prefill stays `estimated` and is listed for correction. A later correction row with a typed price → `confirmed`.

**Realized R.** `[F-01]` Grok (Q1), verbatim:

> "R_unit is `|entry_leg.price − entry_leg.stop_in_force|` (actual fill, his stop at fill). It is not `|card.entry − stop|`. It is not recomputed when a later leg stores a different stop. Per current exit: `sign × (exit.price − entry_leg.price) × exit.shares ÷ (entry_leg.shares × R_unit)`, summed. `sign` comes from `direction` stored on the entry leg. `provisional` while any current leg is `estimated`. The number is not stored. The function's id is `realized_r.1`."

(The source of `direction` is **OPEN FOR DEJAN — R2-2** `[R2F-04 · ASTRA PENDING (R13)]`: the entry leg (G) or the card row (F, N); the formula is not.) All three seats rule the ACTUAL unit. A second output on the planned unit for F12 comparison (Fable Q1) is owner item **O15**. (Proposal sentence, kept:) No statistic is rendered here; §7 exit efficiency is F14's to render, with n and n≥30 (L8).

---

## 4. DRIFT WARNING `[F-27]`

Today (F5): `drift_pct = |new_dist − planned_dist| ÷ planned_dist × 100`, warn at ≥ 25 (constant). Charter and mock say >20 %. The ladder carries "drift warning scaled to ATR (his 09-03 defect)"; the 09-03 origin is unsourced (F33 as corrected).

The proposal's AND gate (`drift_pct > P` AND `drift_atr ≥ K`) is **dropped**: all three seats show it fails the Charter's own "27 % past plan → warning" test on a wide-ATR card (grok (e), gemini (e), Fable (e) REJECT; hub ESCALATE 4). Grok (e), verbatim:

> "The warning is `distance_change_pct > P`, P his setting in `"user".trader_settings`, no committed default. The hard-coded 25 dies. The ATR term, K, `atr_working` versus the daily ATR, `≥` on K, the AND, and "latest receipt" are not in the design. They are O9 if he brings the 09-03 text. A manual card is pct only. No "ATR n/a" line is required while the ATR term is out. The typo guard stays (`max_fill_distance_pct`, already config, 5). The warning never refuses a fill. A missing P records the fill and banners `RE-READ STOP — setting <key> missing, warning not evaluated`." [grok's O9 = v2 **O3**]

- **What the rulings decide about the proposal's refusal:** the proposal's "Missing setting → the fill is refused loudly" is struck; its "the warning … never blocks the fill, which is already done in DAS" stands (grok, gemini, Fable; hub WRONG FACTS: the two sentences contradicted each other — HOLDS). A missing P is loud (banner) and never refuses. v2 fills no value for P.
- Charter test "fill 27 % past plan → warning": read as `distance_change_pct = 27 > P` (Q6, unanimous). The proposal's "test fixture's ATR is chosen so that `drift_atr ≥ K`" is struck (all three seats).
- Author's ESCALATE 2 (the constant 25 vs >20 %): inside this design — the constant dies in C1; P and the comparator at equality are his (O2).
- Author's ESCALATE 3 (ATR term with no source): out of the design until he supplies the 09-03 text (O3). Computing, storing and displaying ATR drift without gating (Fable (e)) is O3's alternative.
- (Proposal sentence, kept:) The warning is a display on the fill plus a stored field. It never blocks the fill, which is already done in DAS.

---

## 5. STOP OVERRIDE (R38, "B with your addition")

- `card_stop_edits` gains `kind ∈ {edit, reset}` (a migration column, default `edit`; `[R2F-10 · ASTRA PENDING (R13)]` carried in M1, the one `db_migrations` file + `.rollback.sql` — R2-3 CONVERGED, §3). `[F-11]` Fable (Q11), verbatim: "Reset = a stop edit with `kind = reset` whose `to_stop` is the card's `structural_stop`. A manual card has no `structural_stop` (the column is added by `0007:38` and written only by `create_radar_card`, `cards/store.py:980-996`); on a manual card the ↺ control is not rendered and the sheet's existing wording stands (`aset/web.py:701`: reset = re-enter the card stop)." `[F-12]` Grok (Q11), verbatim: "`kind = reset` only from the ↺ control. A typed stop that happens to equal `structural_stop` stays `kind = edit` and the owner stays his." (Rendering ↺ on a manual card and refusing it with the reason named — grok (f) — is owner item **O19**.)
- "His stop is the plan": `aset_sizings.stop` is already the live stop (F13). ~~The edit path already recomputes open risk in FILLED (F12). Nothing new is needed for "plan".~~ `[F-29]` **Something IS needed** (grok (f), gemini (f), Fable (f); hub file-check HOLDS: `cards/store.py:720` passes the planned `shares` column, which `mark_filled`'s UPDATE never writes and no exit reduces). In FILLED, `record_stop_edit` must recompute open risk on the shares he still holds. `[R2F-07 · ASTRA PENDING (R13)]` **The form of that read is OPEN FOR DEJAN — R2-2** (§3 block: G's tail `running_after`; F's derived running — its "C2 changes that one argument" WITHDRAWN by its author; N's locked transaction, fill-price per-share risk, never writing `shares`). This is a **live defect on main today**, independent of S3 (Fable ESCALATE 3; hub ESCALATE 2): v3 places the fix in C2; whether the desk lifts it earlier is the desk's lane (L68). Round 2 named a **second** live defect in the same function, read by the derive (UNCHECKED by a hub): in FILLED the per-share risk comes from the card's PLANNED `entry` (`cards/store.py:713-716` → `stop_distance`, side check `aset/engine.py:106-113`), so a stop placed between planned entry and fill is refused and open risk is misstated by the drift — unproven until X20 (L70).
- Owner of the stop is **derived**: `yours` = the last `card_stop_edits` row for the card has actor YOU and kind `edit`. It is not stored twice.
- Render, IN-TRADE and WATCH: his stop with an amber `YOURS` badge and delta vs Cobalt; **Cobalt's structural stop always visible beside it**, never hidden; `↺ <cobalt value>` on the note line (mock decision 11).
- The gap is recorded on the trade: `[F-28]` every `legs` row carries `stop_in_force`; the gap per leg is `stop_in_force − structural_stop`, computed at render (Fable (f), quoted in §3); a manual card's gap is "`NULL — no Cobalt stop`" (Fable (f)). The FILLED/CLOSED evidence carries the folded stop edits (F3). The DRC/Friday review reads the stop path and both R figures: realized R on his stop, and "R had Cobalt's stop held" = F12's counterfactual formula applied from the fill (seam for F14/F15, a 21:10 replay-time figure per §7's seam 7; not computed in S3).
- STOP_EDITABLE (WATCH, FILLED) is unchanged. There is no stop edit in ARMED/TRIGGERED (mock decision 11). (Grok, Fable: no F11 flow needs one.)

---

## 6. THE DM LINE

**No inbound path exists (F27).** The DM path therefore needs a **new resident** (L2 watcher: a deterministic poller, no LLM). **The panel tap path is built FIRST** and is complete without it. **Whether the DM line is built in S3 is O8, his** (costs below, §12).

- Grammar (case-insensitive, one line):
  `<TICKER>[#<card_id>] filled <price> <shares>` → TRIGGERED → FILLED;
  `<TICKER>[#<card_id>] out <price> <shares|all|half|third>` → exit leg.
  The price is typed, so it is `confirmed`.
- Match: exactly one card for that ticker in the required state (TRIGGERED for `filled`, FILLED for `out`). Zero or more than one → **NOT RECORDED**, with a reply listing candidate `TICKER#id` lines. It never guesses. (Q7, unanimous.)
- The line goes through the same store functions as the panel (L3) and the same gates (session, edge, attestation record).
- Unparseable → reply `NOT RECORDED: could not read "<line>". Forms: [TICKER] filled [PRICE] 10 · [TICKER] out [PRICE] all` `[user data: S3-EXITS-PROPOSAL-2026-09-21.md:139 — 4 spans]`. Nothing is written. Every inbound line is stored with its parse result (`"user".dm_inbound`, its own migration; dedupe by post id).
- `[F-31]` Grok (g), verbatim: "Build the panel first and complete. The resident is REST (Q12), ambiguity is Q7, and every unparsed or unmatched line is `NOT RECORDED` with nothing written except `dm_inbound` (post id dedupe, parse result). Kill and resume are checked on the stripped line before the exits parser, by the existing exact match (`jobs/killswitch.py:106-118`). A fill line is not a kill unless it equals the phrase. One resident, not two pollers. If it is down, the kill phrase is not heard and the heartbeat says so. That is the same as today, where no listener exists (`mattermost.py` docstring; the kill matcher is waiting for one)."
- `[F-13]` Grok (Q12), verbatim: "REST poll, using the HTTP path `notify/mattermost.py` already has. No websocket. The poll interval is his (L53 cadence), not an engine tunable and not a committed default. Missing interval: the resident does not start and names the setting. The panel path does not read it." (The interval value is O9; the listener's RED rule is O10.)
- (Proposal, kept:) The resident `com.cobalt.dmlisten` polls the DM channel over REST (`notify/mattermost.py`'s credential path, VaultManager only, F19 redaction on every reply), has a heartbeat probe, a plist, and a `reads:` declaration (L42). It is Mattermost-only. Voice is post-MVP.
- The bot must skip its own posts in the poll (X11, Fable X8).

---

## 7. F22 TRADE NOTE

- **Event:** `[F-09]` Grok (Q9), verbatim: "Write the note in the same request, after the DB commit. On failure the card stays FILLED, the banner is `FILLED — trade note NOT written: <reason>`, and `cobalt cards trade-note <card_id>` runs the same `upsert_trade_note` plus one `upsert_unit` per current leg. `create_if_absent` alone is not the retry. A leg tap that finds no note banners `leg saved, note unit NOT written` and writes nothing in the vault." (The DB comes first — the 09-03 lesson, `aset/web.py:1081-1086`. This meets "fill at 10:12 → note by 10:13" synchronously; Q9 unanimous.) Two cards filled in the same second share one filename; whether the writer merges them is X16.
- **One writer (L3):** `prefill/trade_note.upsert_trade_note` is converted to take the card row + FILLED transition (not a `SizingResult` + form time) and serves both origins. Whether `/size` keeps writing notes at sizing time (today's slice-2 behaviour, F22) is owner item O4.
- **Path:** `[F-33]` Grok (h), verbatim: "Q9's retry. One file per card, path from the FILLED transition time in ET and `trade_filename_pattern` (`23-note-writers.excerpt.py`, `prefill.yaml`). Store that path on `aset_sizings.trade_note_path` even as the intent, and store NULL if the write failed. F14, when it is built, reads the column. If the card is FILLED and the column is NULL, it reports the note missing. It does not call `find_trade_note_for_card`." (Under O4 A a manual card keeps its `/size` note AND gets a fill note — two files, the column points at the fill note (grok (h), gemini (h)); one note per card is O4 B.) This settles the author's ESCALATE 4 inside this design; F14 reads the column (a seam, below).
- **Frontmatter:** his template's 12 keys, in his order (F25). Cobalt's five keys come from the card with `entry_price` = **the fill price**. His keys (exit_price, entry_time, exit_time, profit_loss, trade_def, RVOL) are created blank. Filling them from legs/card while blank via L28 clause 2a is owner item O5 / O6. Once he types a value, it is never touched.
- **Body:** his template body as copied in the writer, then one Cobalt section `cobalt-legs` with one unit per leg seq (`leg-<seq>`). Each unit is a line: `exit ½ · 50 sh @ [PRICE] · 10:31 · estimated` `[user data: S3-EXITS-PROPOSAL-2026-09-21.md:151 — 1 span]`. A correction row updates the same unit in place (same id, L28.2). `[F-34]` Grok (h), verbatim: "`_render_body` (`22-trade_note.py:93-104`) is a hardcoded copy, not a live read of the template. The template heading in `13-templates-keys.md` still has the Templater tag. Keep the copy. Do not execute Templater." Whether the copy still equals his template is X14.
- **He edited the note:** the frontmatter merge keeps his keys (F21). A changed Cobalt line in `cobalt-legs` → human wins, override row (F20). The DB leg is NOT changed from the note (no reverse parse, deterministic). The override surfaces in the DRC's "estimated legs to confirm" (Q8, unanimous).
- The dataview table lights by `file.cday` = the fill day (F25, X2). Its `strategy` column stays blank for `trade_def` notes (F26). That is his template, and it is flagged, not changed (O12; author's ESCALATE 5 — all three seats: outside S3).
- **Seams left for F14/F15** `[F-39]` — Fable (m), verbatim: "(1) `aset_sizings.trade_note_path` replaces `find_trade_note_for_card` (`prefill/drc.py:101-120`). (2) `_read_strategy` (`drc.py:123-130`) reads `strategy`; new notes carry `trade_def` (`trade_note.py:38-41`) — F14 reads `trade_def` from the note or `trade_def_slug` from the card (`0007:32`). (3) The DRC's fill data comes from the daily note's FILL UPDATE block (`drc.py:70` `parse_fill_updates`, written only by the manual `/fill`, `aset/web.py:1103`); a radar fill writes no such block — F14 reads `legs` and the fill columns (`aset/store.py:312`) instead. (4) Realized R + `provisional` + the estimated-legs list: the pure function of (d). (5) The stop path: `card_stop_edits` (kind added) + `stop_in_force` per leg + `structural_stop`. (6) The pick-vs-rank row per fill already exists (`cards/store.py:518`, `:533-556`, savepoint `picks.PICK_SAVEPOINT`). (7) 'R had Cobalt's stop held' is F12's counterfactual from the fill — it needs the day's bars through the close, so it is a 21:10 replay-time figure (`drc-sitting-packet-2026-09-21.md:49`: the miss line is written by replay at 21:10), never a 15:41 figure; the DRC at 15:41 shows realized R (provisional) and leaves that cell for the replay." — The F14 note itself is designed BY HIM (R48); these are seams, not a design of it. Whether a radar fill also writes the daily note's FILL UPDATE block is O11.

---

## 8. ATTESTATION, the soft check as the ladder words it

- Unchanged: card creation stays refused while the attestation disagrees or is absent. That means the key tap for radar and `/size` for manual (F10).
- New in F11: the entry leg records `day_mode_id` + `attested_sheet` (F11 row), so "a fill is recorded against that stated sheet".
- `[F-05]` Grok (Q5), verbatim: "Do not refuse the fill. The entry leg records `day_mode_id`, `attested_sheet`, and `sheet_mismatch boolean NOT NULL`. `sheet_mismatch` is true when the attested file's sheet is not the sheet of the current day mode, or when nothing is attested. Exit legs do not write these three. Creation stays refused (`aset/web.py:985`, `:1314`; `daymode/match.py:80-117`)." Grok (i), verbatim: "Q5's three fields on the entry leg only. Nothing else writes them. `day_mode_id` has no writer in the grep (`02-greps.txt:261-292` hits `attested_sheet` and `assert_sheet_matches` only). The proposal's "loud on the card" is the panel reading the entry leg, not a second column on `aset_sizings`." (Comparing against the sheet the card was SIZED on instead — Fable Q5 — is owner item **O18**.)
- Nothing reads DAS. Nothing overwrites the attestation (CLAUDE.md; ladder `:610` `[F-18]`).

---

## 9. DELIBERATELY NOT CHANGED

Scoring (`cards/scoring.py`), evaluation (`radar/evaluate.py`), dots, proximity, conviction, ranking and pinning (`cards/radar.py`), admission, `proposed_key`, sizing keys and dollars, health pills (F17), expiry rules, the F12 counterfactual formula, and `last_price`'s meaning (the stale-score design owns it; gemini's missed `last_price` writer `cards/store.py:1032` stays theirs — `[F-20]`).

**L52 finding:** re-answered for v3 in `## L52 and the bar` below.

---

## 10. CHUNKS (dependency order)

| # | Chunk | Write path (L29 → Opus 5 floor) | Migration | Depends on | Est. h `[F-37]` |
|---|---|---|---|---|---|
| C1 | `legs` + `legs_current_v` + `card_stop_edits.kind` + `aset_sizings.trade_note_path`; `SizingResult.from_card`; the ONE fill transaction per §2 `[F-22]`; drift per §4 `[F-27]` (pct only, his P); the constant dies | DB — yes | M1 — `[R2F-11 · ASTRA PENDING (R13)]` ONE `db_migrations` file + `.rollback.sql` (R2-3 CONVERGED; number the desk's, L68); **its `legs` running column(s), index and view wait on R2-2 (OPEN FOR DEJAN)** | setups one-build merged or rebased over (F29/F30); **his R2-2 answer** (for the `legs` DDL) | 8 |
| C2 | exit legs, presets, running shares, zero → CLOSED, corrections, realized-R function + provisional, the FILLED stop-edit share read (§5), `cobalt cards legs` CLI — `[R2F-04 · ASTRA PENDING (R13)]` **the whole chunk waits on R2-2 (OPEN FOR DEJAN)** | DB — yes | — | C1; **his R2-2 answer** | 6 |
| C3 | panel routes + render: TRIGGERED, FILLED @ [price][shares], PASS, ½ ⅓ flat typed, ✓ confirm, stop YOURS / Cobalt / ↺ | DB (routes) — yes | — | C1 (C2 for legs) | 7 |
| C4 | F22: converted `upsert_trade_note`, fill-time event, `cobalt-legs` units, `trade_note_path`, retry CLI | vault — yes | — | C1, C2, **C3** `[F-37]` | 5 |
| C5 | `com.cobalt.dmlisten` resident: poller, parser, replies, `dm_inbound`, kill/resume, heartbeat probe, plist — **only if O8 = A** | DB + outbound — yes | M2 | C2 | 8 |
| C6 | S3-P1/P2 smoke, DevDocs, ADR (F11/F22 decisions) | no | — | all | 4 |

- Write-path chunks: **5** (C1–C5; 4 if O8 = B). Migrations: **2** (M1, M2), numbers taken at the L68 gate (§3).
- `[F-37]` Grok (l), verbatim: "C3 and C4 are sequential, one fill request in `aset/web.py`. C5 is parallel with that pair only after C2, and only if O8 is A, and only as new files plus calls into the C2 functions. Seats stay the proposal's Opus 5 floor for C1–C5. L29's text is not in the packet, so that floor is not re-derived from the law. Re-derived hours: without the DM, 8+6+7+5+4 = 30h of seats and about 42h with one fix round each. With the DM, 38h and about 53h. The proposal's 35 / 50 undercounts the transaction seam and the correction rules and omits S3-P3. P3 is not priced here." Grok (l), verbatim: "C1 8 (was 7: the `conn=` seam, `from_card`, two migration sets, the FILLED share-count read). C2 6 (was 5: the chain refusals). C3 7. C4 5, after C3. C6 4 (was 3: the crash, race, correction, and vault-fail cases). C5 8, optional. Fix round ≈ 0.4× on each, the proposal's own pattern, giving 42 or 53. The window is 09-24 → 10-07 and also holds S3-P3 (`12-scope-and-rulings.md`). 42h plus an unpriced P3 is the load to show him. It is not by itself a reason to drop the panel." (All three seats: C3 ∥ C4 is false as the proposal wrote it; gemini (l), grok (l), Fable (l).)
- Seats (fact, not a ruling of this derive): L29's Opus 5 floor holds for C1–C5; under 09-22 R36 the Anthropic build and check seats run on `claude-opus-5-5` while R36 stands. Never auto mode on a write path (L29). C6 has no write path.
- **RESTARTS (a GUESS until `cobalt jobs restarts`, L42 — X12):** C1–C4 touch `cards/`, `aset/`, `prefill/` → `com.cobalt.aset` + `com.cobalt.radar` (it imports `cards.store`), restarted inside the 20:00–21:00 pause with the residents down first (L43/L66). C5 bootstraps a NEW resident.
- `[F-38]` Order — grok (l), verbatim: "Order: bars `0012` and the setups one-build `0013` land or are rebased under, then S3. `s2/stale-marker-0921` has no commits ahead of this packet's `main` in the log. Do not wait on an empty log. Do not re-decide `last_price`. The stale-score code on `67bc19b` is not shown by that one-line subject. X12 includes the 7-line store diff." (Proposal, kept:) The fill prefill uses `last_price` + its bar ts, and if `last_price` is NULL the field is empty and he types (never `entry`, the W6 defect pattern). `[F-18]` `radar_panel.py` is touched by four commits on main (`a660a7c`, `04b0dbd`, `71a8368`, `490c231`; hub file-check HOLDS); C3 rebases over all four. The deploy order against the seven-setups one-build and the stale-score fix, and the stacked set, are the desk's lane (L68/L43).

---

## 11. TEST PLAN (L45: real artifacts, L69 settings on a constructed config)

Charter acceptance, verbatim:
- **F11:** "Test: fill 27% past plan → warning, FILLED row, DRC counts it as a trade; a ½-off tap during the trade produces an estimated leg without a keystroke."
- **F22:** "Test: a fill at 10:12 has a trade note by 10:13."

Fixtures (real shape, personal values stripped): his `Individual Trade Template.md` frontmatter/body; one real `2 - Trades` note's frontmatter (keys, quoting); the `Daily.md` dataview block; ~~a real `radar_score_receipt.observations` row with `atr_working`~~ `[F-27]` (no ATR term in §4); a real Mattermost `GET /channels/{id}/posts` JSON (dev bot, hub capture; only if O8 = A).

With-DB (`cobalt_dev`), proposal: radar card TRIGGERED → panel fill at a price 27 % distance drift → FILLED row, entry leg, warning stored ~~with its ATR receipt~~ `[F-27]`, pick row; ½ tap → estimated leg, running halves; ⅓ on 1 share → refused; flat ✓ typed → confirmed, CLOSED in the same transaction; correction row → current view flips and realized R loses `provisional`; stop edit in FILLED then ↺ → `kind=reset`, owner derived back to Cobalt; attested-sheet mismatch → recorded, not refused; `market_reset` → refused, nothing written.

Dev vault, proposal: note created at fill with `entry_price` = fill; second leg updates unit in place; he edits a leg line → override row, DB unchanged; he typed `exit_price` → never overwritten; `trade_note_path` stored; write failure → banner + retry CLI creates it once. DM (only if O8 = A): the two Charter lines write the same rows as the panel; ambiguous ticker → NOT RECORDED + candidates; garbage → NOT RECORDED reply, zero rows; the kill phrase is still honoured. S3 smoke (ladder `:615-618` `[F-18]`) runs the tap path end-to-end on one live morning.

`[F-40]` Grok (n), verbatim: "Keep the proposal's `cobalt_dev` cases and the dev-vault cases, with these changes. The 27% case asserts `distance_change_pct` 27 and a warning, and it does not require an ATR fixture. Add: the fill, the entry leg, and the cache commit or roll back together. Add: a stop edit after a ½ uses `running_after`. Add: the card-9 correction off 0 is refused. Add: two concurrent ½ taps, one refused, running consistent. Add: a manual fill with NULL `structural_stop` succeeds. Add: vault failure, retry, then a leg, one file and one unit per seq. Keep `market_reset` writes nothing, the attestation mismatch recorded, and the pick row. Keep the DM cases off the S3 gate unless O8 is A." `[R2F-20 · ASTRA PENDING (R13)]` (The "stop edit after a ½ uses `running_after`" case and the concurrent-taps expectation follow **his ruling on R2-2 (OPEN FOR DEJAN)**: under F the second tap writes 25 (gemini (g)); under N and G it is refused. The "fill, entry leg and cache commit or roll back together" case is R2-1's, converged, and runs on a connection with `autocommit = False` — `[R2F-03]` build note.)

Also added (every seat): a stop edit in FILLED after a drifted fill recomputes open risk on the shares actually held (the live defect, X19). `[R2F-14 · ASTRA PENDING (R13)]` Round 2 (Fable seat) adds the per-share-risk half of the same defect: X20.

---

## First-gate experiments (L70)

Each runs on `cobalt_dev` or in the dev vault (a production read stays a hub-run read, L41). Ordered by the chunk it gates; none is a defect until run (L70). `[R2F-12, R2F-13, R2F-14, R2F-15, R2F-16 · ASTRA PENDING (R13)]` Round 2 added X20–X22 (Fable seat) and merged gemini's round-2 X4 and X7 into v2's rows (no duplicate).

| X | Gates | Runs on | Experiment | Result that would change the design | From |
|---|---|---|---|---|---|
| X12 | cut of S3-P1 (before C1) | hub, repo | `cobalt jobs restarts <range>` on the C1–C5 file set; the `setups/seven-0921` diff of `cards/store.py` and whether `com.cobalt.radar` imports `cards.store` | the measured derivation replaces §10's RESTARTS GUESS; a conflict in `fill()` changes C1's rebase cost, not the mechanism | grok X12, Fable X9 |
| X9 | C1 | hub read / `cobalt_dev` | name and trace `create_state` (`cards/store.py:560-629`, writers `:607`, `:622`) | if it can move a LIVE card, it is a second path S3 must route or delete before C1 (L3); if a one-shot genesis writer that refuses a card with a state, no hole | grok X8, hub FC8 |
| X10 | C1 | `cobalt_dev` | one persisted manual card: `SizingResult.from_card` vs `compute_sizing` of that row's stored inputs | any default that changes `per_share_risk` or `shares` changes `distance_change_pct` and the warning — fix the copy, never a second formula | grok X9 |
| X1 | C1 | `cobalt_dev` | the one fill transaction: make the fill-column UPDATE fail inside the transaction | expected: card stays TRIGGERED, no leg row, no transition row; a FILLED card with NULL fill columns means §2's seam is not closed (round 2, Fable seat: the same result on a connection left at `autocommit=True`, `db.py:199` — `[R2F-03]`) | grok X1 ("already in staged code"), Fable X7 |
| X15 | C1 (unit test) | offline | `compute_fill_recompute` entry 10.0000, stop 9.9000, fill 10.0270 → `distance_change_pct = 27.00`; fill 9.9730 → 27.00 | a different figure changes the Q6 reading of the Charter test | Fable X10; proposal §11; grok Q6 |
| X22 | C1 (M1) | fresh `cobalt_dev` | Fable seat (round 2), verbatim: "fresh `cobalt_dev`: (i) `cobalt db migrate` before any `ensure_schema()` — expected to fail at `0007:27` today (the precondition M1 shares); (ii) `0001` → `AsetStore().ensure_schema()` → FORWARD incl. M1 → `--rollback --down-to <M1−1>` → schema diff against the pre-M1 dump empty → FORWARD again clean." | "A non-empty diff changes M1's rollback file." (Fable seat) | Fable seat R2 X22 — UNCHECKED by a hub; read by the derive: `0007_radar_cards.sql:27` ALTERs `"user".aset_sizings`, `cards/migrations/0002_card_stop_edits.sql:15` creates the table |
| X4 | C1 (and his R2-2 answer) | `cobalt_dev` | create `"user".legs` with the trigger; UPDATE a row (must fail closed); INSERT two rows with the same `(card_id, seq)`, a correction, a correction of a correction; the view; the GUC default on `user_id` | UPDATE allowed → the append-only claim is false; the view returning two rows per seq → the option he rules on R2-2 changes its wording; under F / N the second same-seq original must be refused by the index | grok X4, Fable X5; gemini R2 X4 (same experiment, merged) |
| X6 | C1 / C3 | `cobalt_dev` | `structural_stop` on a manual `aset_sizings` row | expected NULL (derive read: the only writer is `create_radar_card`, `cards/store.py:980-996`); NOT NULL for every origin drops the manual-card case of §5 | grok X6 |
| X7 | C2 | `cobalt_dev` | two concurrent ½ taps on one FILLED 100-share card under the card row lock | two 50-share legs → the lock claim is insufficient. The expected outcome is **his R2-2 answer**: "one refused" (grok R1; Fable seat R2 — its round-1 "second writes 25" WITHDRAWN) or "tap 2 reads running 50 and writes 25" (gemini R2 (g)) | gemini X1, grok (c) #3, Fable X6; gemini R2 X7 (merged) |
| X21 | C2 | `cobalt_dev` | Fable seat (round 2), verbatim: "two sessions: A = `record_stop_edit` paused right after its `SELECT … FOR UPDATE` (today's code); B = `SELECT … FOR UPDATE` on the same card. Expected today: B does NOT wait (autocommit released the lock)." | "If B waits, (d)'s "one transaction, `autocommit = False`" clause is redundant and is struck." (Fable seat; applies if he rules N) | Fable seat R2 X21 — UNCHECKED by a hub; read by the derive: `db.py:199`, `cards/store.py:677-682`, comment `:350` |
| X19 | C2 (desk may lift earlier) | `cobalt_dev` | fill a manual card at a drifted price, edit the stop in FILLED, read `used_risk` | proves the live defect (§5) that C2 fixes; not a defect until run (L70) | Fable ESCALATE 3 |
| X20 | C2 (desk may lift earlier, with X19) | `cobalt_dev` | Fable seat (round 2), verbatim: "manual card long, planned entry 10.00 / stop 9.90 / 100 sh; `mark_filled` at 10.05 (→ `recomputed_shares` 66); `record_stop_edit` FILLED to 10.02 → today expected `SizingError` (planned-entry side check); to 9.95 → today `used_risk` 5.00 (0.05 × 100). After C2: 10.02 accepted; 9.95 → 0.10 × running; `aset_sizings.shares` still 100." (constructed values, not his) | "Any other result today changes the R2-1 addendum and (d)'s per-share clause." (Fable seat) | Fable seat R2 X20 — UNCHECKED by a hub; read by the derive: `cards/store.py:713-721`, `aset/engine.py:98-114`, `:152-157` |
| X5 | C3 | `cobalt_dev` | one radar card left TRIGGERED past `expires_at`, one resident cycle | not EXPIRED → the caller skips TRIGGERED: a caller fix in S3, still no new window | grok X5 |
| X14 | C4 | dev vault (hub/desk runs the diff, L32) | `upsert_trade_note(..., dry_run=True)` on a constructed card; diff the rendered body against his template's body | a difference records the divergence or changes §7 to read the template at write time (his template, R48) | Fable X2 |
| X3 | C4 | dev vault | `create_if_absent` twice on one path, `upsert_unit` of `leg-0`, hand-edit that line, retry; and the retry racing a leg `upsert_unit` on the same note | a duplicated body, a lost override, or a silent overwrite changes Q8/Q9 (serialize note writes per card) | grok X3, Fable X1 |
| X16 | C4 | dev vault | two constructed cards, same ticker, fill instants in the same second | if the writer merges the second card into the first card's note and reports `updated`, the collision refusal Fable offered (Q9) must be folded | Fable X11 |
| X18 | C4 (legacy / F14 seam) | dev vault + `cobalt_dev` | `find_trade_note_for_card` on a manual card that has both a sizing note and a fill note | confirms the column (§7) is the only reliable pointer; tolerance is 30 s (`drc.py:67`) | gemini X2 |
| X2 | C4 / S3-P2 acceptance | dev vault, Obsidian | a Cobalt-written note (`trade_def` blank, no `strategy` key) appears in the daily dataview for the fill day; its `strategy` cell | omitted rather than shown → F22's "lights the table" fails and the fix is his template (O12), not a Cobalt key; F25 stays UNPROVEN until run | grok X2, Fable X3/X4; proposal F25 |
| X11 | C5 (only if O8 = A) | dev Mattermost bot | `GET /api/v4/channels/{id}/posts` on a DM channel; then after the bot replied | 403 → C5 cannot ship on this credential; the bot's own reply returned → skip by user id, not only post id | grok X10, Fable X8 |
| X17 | C5 (only if O8 = A) | dev Mattermost | a `NOT RECORDED` reply carrying a ticker and a price passes `redact()` unchanged (F19) | stripped → the reply text changes | Fable X12 |

Settled by read, not experiments: `matches_kill_phrase` is equality of the stripped line (grok X11); `FILL_MATCH_TOLERANCE_SECONDS = 30` (`prefill/drc.py:67`, derive read; grok X7). Proposal UNPROVEN rows: F25 → X2; F30 → resolved (hub file-check HOLDS: both branches exist); F32 → a reading all three seats state, his to confirm (O13); F33 → resolved (the ladder carries the text; only the 09-03 origin is unsourced).

---

## 12. OPEN QUESTIONS (tribunal) and OWNER ITEMS (his)

**Tribunal (12) — dispositions after round 2:** Q1 actual unit `[F-01]` · Q2 one table, `fills` kept declared `[F-02]` (strike = O16) · Q3 `[R2F-21 · ASTRA PENDING (R13)]` **OPEN FOR DEJAN — R2-2** (§3 block; round 2 did not converge) · Q4 cache kept, one transaction `[F-04]` — grok (Q4), verbatim: "Keep `aset_sizings.actual_fill`, `recomputed_shares`, `recomputed_used_risk`, `share_delta`, `distance_change_pct`, and `filled_at` as a cache of the entry leg plus `compute_fill_recompute`. Write them in the same transaction as the FILLED transition and the entry leg. No other path writes them. `legs.shares` is the count he typed. `recomputed_shares` is the formula. They may differ." (whether an entry-leg correction also rewrites the cache is part of **OPEN FOR DEJAN — R2-2**: F and N rewrite it in the correction's transaction; G's "No other path writes them") · Q5 record + flag `[F-05]` · Q6 `distance_change_pct = 27` (proposal kept) · Q7 refuse with candidates (proposal kept) · Q8 override only (proposal kept) · Q9 synchronous `[F-09]` · Q10 no new timer, X5 (proposal kept) · Q11 `[F-11]`, `[F-12]` · Q12 REST, interval his `[F-13]`.

**Owner items (after the tribunal) — his, read by him after the tribunal; none is a precondition to building C1–C4 or C6 (tests use constructed settings, L69). (R2-2 is not an owner item: it is the tribunal's unresolved split, carried to him under L39 — §3 block.)**
- O1. ⅓ / ½ shares: A round down (⌊running/3⌋), B round to nearest? (proposal; grok, Fable as framed)
- O2. Drift pct threshold P, and the comparator at equality (`>` per the Charter or `≥` as the code has it): A 20 on radar AND the manual sheet, B 20 radar, 25 sheet? (proposal; Fable adds the comparator; grok: B reopens the Charter's >20 %)
- O3. ATR in the drift warning: A no ATR term until he supplies the 09-03 text (v2, grok (e)), B compute, store and display drift in working-TF ATRs beside the pct, gating nothing until he names K (Fable (e)); and, if he wants a term, which ATR (grok O9, Fable). (proposal O3 + hub O9)
- O4. Manual sheet notes: A keep creating a note at every sizing (today; with v2's §7 a manual card then has its sizing note and a fill note, the card pointing at the fill note), B one note per card — created only at FILLED (proposal B) or created at sizing and refreshed at fill (Fable (h))?
- O5. exit_price / entry_time / exit_time / profit_loss: A Cobalt fills each only while blank, B they stay yours, never written? (proposal)
- O6. `trade_def` on a radar trade note: A Cobalt fills it from the card while blank, B it stays yours? (proposal)
- O7. Radar fill before the S4 detector: A two taps (TRIGGERED, then FILLED), B one tap from ARMED writes both rows with actor you? `[F-21]` Priced — Fable (a), verbatim: "O7 B is a change to the `strict` rule at `:486` (walk the route for a radar card too, actor YOU on both rows, evidence naming "pre-S4 tap"), not a new edge; the docstring's reason for the refusal (`:469-473`, the detector's record) has no force until S4's detector exists (F16). Priced: ~10 lines in C1, one test. His A/B stands." (grok (a) agrees; gemini (a): today it raises `IllegalTransition`.)
- O8. DM line in S3: A build the listener in S3, B panel only in S3, DM after S3? `[F-32]` Costs — grok (g), verbatim: "Costs. REST in S3: C5's 8h plus a fix round, one new resident (plist, heartbeat, `reads:`), migration M2, and a new GET on the bot credential. Websocket: those costs plus a client the new core must not copy from the old tree. Panel only: 0h, no resident, no M2, no new credential use. The smoke (`12-scope-and-rulings.md`) is taps, a note, and a DRC. It does not lose a test line without the DM. It loses the charter's fallback sentence when he cannot reach the panel. O8 chooses. It is not a precondition." Fable (g), verbatim: "O8 A (build in S3) costs C5 = 8 h + a plist + a heartbeat probe + M2 (`dm_inbound`) + a RESTARTS row for a new resident + the bot token's first READ use (the same VaultManager credential `notify/mattermost.py:19-21`, no new secret). O8 B (after S3) sets aside the Charter's own F11 acceptance sentence 'Fallback capture: a DM/voice line … writes the same rows' (`MVP-CHARTER-v0_2.md:144-145`) and the ladder's S3 row (`:610`); the panel tap path is complete without it (§6 bullet 1), and the kill phrase stays unlistened, as it is today (`matches_kill_phrase` has no caller: grep → its definition and `__all__` only)." Kill-phrase coupling — gemini (g), verbatim: "Folding the kill/resume phrases into the exits listener (`dmlisten`) couples a critical safety path to a feature path. Scenario: The `dmlisten` resident crashes at 10:00. At 10:05 the platform goes wild and he types the kill phrase. It is ignored because the listener is down. The DM line demands a new resident with a plist, restarts, and heartbeat probe for a feature that is only "fallback capture"." v2 carries the coupling as the rulings leave it: one listener, kill checked first (grok (g), Fable (g)); gemini's objection stands beside it.
- O9. The DM poll interval value (only with O8 A). (grok O10, Fable O10)
- O10. The listener's heartbeat RED rule (N missed polls). (Fable O11)
- O11. Does a radar fill also write the daily note's FILL UPDATE block, as the manual sheet does? (Fable O12)
- O12. His daily dataview's `strategy` column vs `trade_def` notes — his template, outside S3. (Fable O13; gemini, grok (h); author ESCALATE 5)
- O13. Confirm the Charter §5 #16 reading: one entry fill, many exit legs. (gemini O9; Fable (j); author ESCALATE 6)
- O14. The preset SET (½ · ⅓ · flat · typed) — mock #2, still open. (Fable O9)
- O15. Realized R also on the planned unit, as a second output beside the actual-unit R. (Fable Q1; the smaller mechanism was taken)
- O16. The declared-but-unbuilt `fills` table name: keep (v2, grok) or strike (gemini, Fable). (Q2 split) `[R2F-17 · ASTRA PENDING (R13)]` Gemini (R2-3), verbatim: "OWNER: O16 (striking `fills` from DECLARED_TABLES) is his decision; the build keeps it declared if he says nothing."
- O17. Build shape: `mark_filled` as the one fill path with C3 → C4 sequential (v2, grok), or one new orchestrator function that lets C3 ∥ C4 ∥ C5 build in parallel (Fable (b), (l)).
- O18. `sheet_mismatch`: against today's attested day mode (v2, grok) or against the sheet the card was sized on (Fable Q5).
- O19. ↺ on a manual card: not rendered (v2, Fable Q11) or rendered and refused with the reason named (grok (f)).
- O20. Cobalt's stop per leg: computed at render from `structural_stop` (v2, Fable (f)) or stored on every leg, NULL on a manual card (grok (f)).
- O21. `[R2F-18 · ASTRA PENDING (R13)]` Fable seat (round 2), verbatim: "Open-risk share count for a FILLED card filled before C1 (no entry leg): `recomputed_shares` (default the build takes) or `shares`. Not a precondition." (Its "default" is the Fable seat's, inside position N of R2-2; under G the text reads `shares`. The question stands whichever position he rules.)
- O22. `[R2F-19 · ASTRA PENDING (R13)]` Fable seat (round 2), verbatim: "A correction that brings a FILLED card's running to 0: closes the card in the same transaction (default the build takes) or is refused with "use flat". Not a precondition." (Named by the Fable seat only; neither v2 option said what happens — `v2:80`.)

---

## Dissents, verbatim

- **Gemini — closing line (items (b), (f), and the design as specified):** "TRIBUNAL R1: DO NOT BUILD 5 write paths with a broken risk recompute and two-transaction seam." v2 does not stop the design; it folds grok's one-transaction fill (§2) and routes the share-count fix to round 2 (§5, `[R2-2]`), and asks gemini in round 2 whether the DO NOT BUILD stands against v2 (`[R2-1]`). `[R2F-02 · ASTRA PENDING (R13)]` **Round 2, gemini (R2-1), verbatim:** "ADOPT A" · "DO NOT BUILD withdrawn" · closing "TRIBUNAL R2: BUILD". The dissent is spent (R2-1 CONVERGED); its round-1 text stays here.
- **Gemini — (b), verbatim:** "`mark_filled` calls `fill()` (which opens, commits, and closes its own transaction) and THEN updates `aset_sizings` in a completely new transaction (`aset/store.py:217-233`). Scenario: DB goes down after `fill()` commits but before `mark_filled` runs the UPDATE; card 123 is FILLED in `card_transitions` but missing actual fill stats in `aset_sizings`. Additionally, `SizingResult.from_card` creates a second input rebuild path, bypassing the original `_parse_input(form)`."
- **Gemini — (f), verbatim:** "In FILLED, `record_stop_edit` recomputes open risk using `aset_sizings.shares` (`cards/store.py:720`). Scenario: Card 123 filled for 100 shares. Exit leg sells 50. Running is 50. He moves his stop. `record_stop_edit` reads 100 from `aset_sizings.shares` (which exits don't update) and recomputes his open risk on 100 shares instead of 50. A critical defect."
- No seat REJECTed an item v2 keeps. Fable's one REJECT (the (e) AND gate) is followed.
- `[R2F-22 · ASTRA PENDING (R13)]` **Round-2 answers v3 does not follow** (every one belongs to R2-2, which v3 does not settle; all carried verbatim in the §3 block): gemini R2-2 "ADOPT F" and its (a)–(g) lines; the Fable seat's R2-2 "NEITHER" and its replacement text; the Fable seat's closing "TRIBUNAL R2: BUILD AFTER derive folds R2-2 wording: CAS running check, locked stop edit, fill-price open risk" — its three folds are R2-2's and wait on his answer. Gemini's R2-2 (c) line is also a supporting claim that DOES NOT HOLD (round-2 hub, `## Checked against the files`).

---

## L52 and the bar

**The proposal's §9 L52 finding, re-answered for v3:** nothing v3 builds feeds a score, a rank, an admission or what a card's score shows. The fill, legs, drift warning, stop owner and note are records and displays on the card. v3, like v2, removes the only new numeric input the proposal took from the radar (the `atr_working` receipt read, §4). Hub file-check (round 1): HOLDS (row "§9 L52 finding", FC7: no chunk touches `cards/scoring.py`, `cards/radar.py` or `radar/evaluate.py`'s scoring); round 2 changed no chunk's file set. The (a)–(d) bar is therefore not triggered by scoring; v3 sits the L67 tribunal because it reaches the card's display, and states the bar anyway:
- (a) every number: fill price typed, DM-typed, or `last_price` with its bar ts; shares his; P his (no committed default); the drift % from stored inputs (F5 formula); no modelled number.
- (b) ranking authority: untouched.
- (c) seams as real artifacts: **NOT all met.** R2-3 converged (M1 = one `db_migrations` file + `.rollback.sql`, a real artifact); `aset_sizings.trade_note_path`, the one fill transaction (§2, R2-1 converged) and the seven F14/F15 seams (§7) are specified. **R2-2 did not converge**: the `legs` running column(s), index and `legs_current_v`'s current-row rule are named in three positions and are his to rule (OPEN FOR DEJAN) — named, not assumed, and not built until he rules.
- (d) auditable: realized R is a pure, versioned function over stored legs (`realized_r.1`), never a stored number; any house can replay it.

**L1 (fail-loud):** a missing P records the fill and banners `RE-READ STOP — setting <key> missing, warning not evaluated` (§4); a missing poll interval stops the resident from starting and names the setting (§6); a FILLED tap with no price is refused (§2); a vault-write failure banners with a retry command (§7); a fill that cannot write its entry leg and cache rolls back whole (§2, X1) — on a connection with `autocommit = False` (`[R2F-03]` build note). Nothing silently defaults. The proposal's refusal of a fill for a missing setting is struck because it would record a real trade as never taken.
**L3 (one path):** one fill path — `mark_filled` in one transaction, used by `/fill`, the panel and the CLI; `/card/{id}/move` stops filling; `from_card` is the only rebuild (§2). One migration runner for M1 (`db_migrations`, R2-3). One trade-note writer (§7). One DM listener carrying the kill phrase (§6). One formula for drift (F5) and one for realized R (§3). Open: `create_state` (X9); the one running-share read is R2-2's (his).
**L28 (vault writes):** the note is `create_if_absent` + marker-bounded `upsert_unit` per leg; human wins with an override row; no reverse parse; Cobalt fills his blank keys only if O5/O6 say so (clause 2a); writes proven on the dev vault first (X2, X3, X14, X16); live vault never a test target.
**L57 (explainability): NOT MET in v3** — every leg stores its price, source, as-of, shares, flag, preset, stop in force and (entry) attestation; the fill cache is written in the same transaction as the entry leg; realized R and the gap are computed from stored rows; the drift % from stored inputs. But whether running shares are stored (G), derived (F) or derived with a stored `running_before` checked at write (N), and where `direction` is read, are R2-2 — **OPEN FOR DEJAN** — so the explainability of the running count and of the FILLED open-risk figure is not settled until he rules.
**L32:** 5 spans of his traded ticker and prices from the proposal are written as `[TICKER]` / `[PRICE]` here (carried from v2); round 2 added none. The figures in X15 and X20 are constructed test values, not his.
**L53:** P, the comparator, the poll interval, the RED rule's N, the preset set and the rounding are his; none has a committed default. No round-2 seat proposed a value for one of his keys.
**CLAUDE.md boundary:** NONE — unanimous (grok, gemini, Fable (k)); hub-confirmed; round 2 changed nothing that touches it. Nothing in v3 reads, writes or infers from his trading platform.
